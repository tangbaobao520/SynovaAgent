{
  "version": "1.0",
  "task": "D38: PolicyEngine权限引擎",
  "brief": "2026-07-12-D38-PolicyEngine权限引擎.md",
  "current_phase": 1,
  "approach": "rewrite",
  "phases": [
    {
      "step": 1,
      "action": "create",
      "files": [
        "src/security/policy-engine.ts",
        "tests/security/policy-engine.test.ts"
      ],
      "checks": { "wiring": "deferred", "test_pairing": "enforce" }
    }
  ],
  "principles": [
    {"rule": "默认Deny (OWD=Private) — 无匹配规则时返回Deny", "verify": "grep 'deny_default' src/security/policy-engine.ts | wc -l | xargs test 1 -eq"},
    {"rule": "接口签名与安全规范§3.4一致 — evaluate(AccessRequest): PolicyDecision", "verify": "grep 'evaluate.*AccessRequest.*PolicyDecision' src/security/policy-engine.ts | wc -l | xargs test 1 -eq"},
    {"rule": "零 as any", "verify": "grep -c 'as any' src/security/policy-engine.ts | xargs test 1 -eq"}
  ],
  "memory_refs": []
}
