{
  "version": "1.0",
  "current_phase": 1,
  "principles": [
    {
      "rule": "traverse() must BFS traverse from start node",
      "verify": "grep -q \"traverse\" src/l4/graph-traversal.ts && echo PASS || echo FAIL"
    },
    {
      "rule": "TemporalBaseline must detect decelerating/stable trend",
      "verify": "grep -q \"decelerating\\|stable\" src/l4/temporal-baseline.ts && echo PASS || echo FAIL"
    }
  ],
  "approach": "reuse",
  "memory_refs": [
    "memory/stub-implementation-pattern.md",
    "memory/engine-core-bridge-files.md"
  ],
  "phases": [
    {
      "step": 1,
      "action": "create",
      "files": [
        "src/l4/graph-traversal.ts",
        "src/l4/temporal-baseline.ts",
        "tests/l4/graph-traversal.test.ts",
        "tests/l4/temporal-baseline.test.ts"
      ],
      "checks": {
        "wiring": "deferred",
        "test_pairing": "enforce"
      }
    }
  ]
}
