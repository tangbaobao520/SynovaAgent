{
  "version": "1.0",
  "current_phase": 3,
  "approach": "reuse",
  "principles": [
    {
      "rule": "删除任何 engine-core 相关文件前，必须 grep 全仓库确认零引用",
      "verify": "grep -rn 'orchestrator-adapter\\|run-pipeline-test\\|convert-frameworks\\|audit-engine-core\\|check-empty-modules' src/ packages/ scripts/ tests/ --include='*.ts' --include='*.tsx' --include='*.js' --include='*.mjs' --include='*.sh' | grep -v node_modules"
    },
    {
      "rule": "修复断裂 import 后，必须跑 tsc --noEmit + 相关 vitest 证明通过",
      "verify": "npx tsc --noEmit && npx vitest run tests/e2e/diagnosis-pipeline.test.ts tests/architecture/graphstore-unify.test.ts"
    },
    {
      "rule": "清理完成后，全仓库 engine-core 引用只剩禁令门禁本身",
      "verify": "grep -rn 'engine-core' src/ packages/ scripts/ tests/ --include='*.ts' --include='*.tsx' --include='*.js' --include='*.mjs' --include='*.sh' | grep -v node_modules | grep -v pre-commit-check.sh | grep -v check-bridge-files.sh | grep -v check-plan-integrity.sh | grep -v audit-rules.json"
    }
  ],
  "memory_refs": [
    "memory/engine-core-bridge-files.md"
  ],
  "phases": [
    {
      "step": 1,
      "action": "fix",
      "files": ["tests/e2e/diagnosis-pipeline.test.ts", "tests/architecture/graphstore-unify.test.ts"],
      "checks": {"wiring": "enforce", "test_pairing": "enforce"}
    },
    {
      "step": 2,
      "action": "delete",
      "files": ["src/agent/orchestrator-adapter.ts", "tests/mvp-pipeline.integration.test.ts", "tests/measurement-pipeline.test.ts", "tests/expert-pipeline.test.ts"],
      "checks": {"wiring": "deferred", "test_pairing": "deferred"}
    },
    {
      "step": 3,
      "action": "clean",
      "files": ["tsconfig.json", "vitest.config.ts", "packages/test-kit/"],
      "checks": {"wiring": "enforce", "test_pairing": "deferred"}
    }
  ]
}
