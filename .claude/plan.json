{
  "version": "1.0",
  "task": "D35: 数据管道可观测性",
  "brief": "2026-07-11-D35-数据管道可观测性.md",
  "current_phase": 1,
  "approach": "rewrite",
  "phases": [
    {
      "step": 1,
      "action": "create",
      "files": ["src/monitoring/freshness-tracker.ts"],
      "checks": { "wiring": "deferred", "test_pairing": "deferred" }
    },
    {
      "step": 2,
      "action": "create",
      "files": ["src/monitoring/pipeline-monitor.ts"],
      "checks": { "wiring": "enforce" }
    },
    {
      "step": 3,
      "action": "wire",
      "files": ["src/routes/data.ts"],
      "checks": { "wiring": "enforce" }
    }
  ],
  "principles": [
    {"rule": "监控代码独立于被监控对象，不侵入ingest内部逻辑", "verify": "grep 'PipelineMonitor\|FreshnessTracker' src/agent/data-ingest-service.ts | wc -l | xargs test 0 -eq"},
    {"rule": "复用 MetricsCollector + EventBus，不造新轮子", "verify": "grep 'class.*Collector\|class.*Bus' src/monitoring/ | grep -vc 'MetricsCollector\|EventBus' | xargs test 0 -eq"}
  ],
  "memory_refs": ["memory/engine-core-bridge-files.md"]
}
