{
  "version": "1.0",
  "current_phase": 1,
  "principles": [
    {
      "rule": "must compute diffPercent and classify",
      "verify": "grep -c diffPercent src/l4/migration-validator.ts | grep -q 1"
    },
    {
      "rule": "every catch has log",
      "verify": "grep -c catch src/l4/migration-validator.ts | grep -q 1"
    }
  ],
  "approach": "reuse",
  "memory_refs": [
    "memory/stub-implementation-pattern.md",
    "memory/engine-core-bridge-files.md"
  ],
  "phases": [
    {
      "step": 1,
      "action": "create",
      "files": [
        "src/l4/migration-validator.ts",
        "tests/l4/migration-validator.test.ts"
      ],
      "checks": {
        "wiring": "deferred",
        "test_pairing": "enforce"
      }
    }
  ]
}
