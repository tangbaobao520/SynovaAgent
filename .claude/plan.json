{
  "version": "1.0",
  "created": "2026-09-08T00:00:00Z",
  "approved_by": "founder",
  "current_phase": 2,
  "principles": [
    "原则2: 先设计验证标准，再设计实现",
    "策略：显式 options > provider 注册策略 > 默认，schema fail-fast + Object.freeze（DSH retry-policy 范式）",
    "超时语义：截止超时=调用方意图立即结果化不重试；流空闲=传输健康可重试，耗尽才落 TOOL_TIMEOUT"
  ],
  "approach": "extend",
  "memory_refs": [
    "memory/notes/implemented/2026-08-30-d564-win-bash-python-injection.md",
    "memory/notes/implemented/2026-09-06-ct62-freshness-timestamp.md"
  ],
  "phases": [
    {
      "step": 1,
      "action": "retry-projection 4 exports have no src consumers yet (L2 wiring is a later stage) - wiring deferred, D588 precedent",
      "files": ["src/store/retry-projection.ts"],
      "exports": ["RETRY_PROJECTION_KEY", "RETRY_EVENT_KIND", "retryProjectionDefinition", "appendRetryEvent"],
      "checks": { "wiring": "deferred", "test_pairing": "enforce" }
    },
    {
      "step": 2,
      "action": "timeout exports are wired into retry-middleware (group 4a physical hits); paired tests delivered in same commit",
      "files": [
        "src/llm/timeout.ts",
        "src/llm/types.ts",
        "src/llm/retry-middleware.ts",
        "tests/llm/timeout.test.ts",
        "tests/llm/call-resilience.test.ts",
        "tests/store/retry-projection.test.ts"
      ],
      "checks": { "wiring": "enforce", "test_pairing": "enforce" }
    }
  ]
}
