{
  "version": "1.0",
  "current_phase": 1,
  "phases": [
    {
      "step": 1,
      "action": "create",
      "files": [
        "src/l3/tool-guard.ts",
        "extensions/frameworks/fts5-cjk-tokenizer.json",
        "tests/fixtures/jsonl-runner.ts",
        "tests/fixtures/jsonl/sample-diagnosis.jsonl",
        "tests/l3/tool-guard.test.ts"
      ],
      "checks": {
        "wiring": "enforce",
        "test_pairing": "enforce"
      }
    },
    {
      "step": 2,
      "action": "wire",
      "files": [
        "src/agent/tool-loop-executor.ts"
      ],
      "checks": {
        "wiring": "enforce",
        "test_pairing": "deferred"
      }
    }
  ],
  "memory_refs": [
    "memory/stub-implementation-pattern.md",
    "memory/plan-actual-closure.md"
  ],
  "principles": [
    {
      "rule": "ToolGuard 必须与 ToolGuardrails 独立，不桥接不继承",
      "reason": "ToolGuardrails 在 L2 且 API 不匹配，新建 L3 ToolGuard 不走 import 桥接(铁律46)",
      "verify": "grep -rn 'ToolGuardrails' src/l3/tool-guard.ts"
    },
    {
      "rule": "ToolGuard 在 tool-loop-executor.ts 中替换 guardrails.check() 调用",
      "reason": "文档要求 L3 守卫接管工具调用安全，L2 不再直接调用 ToolGuardrails",
      "verify": "grep -c 'toolGuard.beforeCall' src/agent/tool-loop-executor.ts && test $(grep -c 'toolGuard.beforeCall' src/agent/tool-loop-executor.ts) -eq 2"
    },
    {
      "rule": "FTS5 配置是纯 JSON，零 TS 改动",
      "reason": "文件驱动：改配置不改代码",
      "verify": "grep -rn 'fts5.*cjk' src/ --include='*.ts' 2>/dev/null; test $? -eq 1"
    }
  ],
  "approach": "rewrite"
}
