# Task Brief: 修复专家工具桩: DocExtractor产出映射到SOG节点类型

> 生成时间: 2026-06-14 11:26:50
> 分支: feat/phase0-diagnosis-demo
> 代码库状态: tsc=2
0 errors, as any=6, 测试=}
}



测试运行失败

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 完整数据流：
  原始数据 → 本体层(电子病历) → 7维度×25测量器(compute)
    ├─ 按需(FDE触发): runModules() → Evidence池 → 专家 → 诊断报告
    └─ 定时(Cron触发): Sentinel.check() → SentinelFinding → 信号聚合
                                                              ↓
  信号聚合引擎 → 交叉关联+严重度升级+专家路由 → 6位专家(strategy/org/finance/tech/marketing/action)
       → ReAct推理+交叉验证 → 综合诊断报告 → FDE收到警报+报告

## 本任务在哪一层

L3(洞察) — 修复 DocExtractor(L3/engine-core) → SOG 图(L4) → 专家工具(L3) 的数据流。
无跨层风险。


## 文档引用

全量对齐手册 §7.1(数据获取) §7.3(测量器与哨兵) §8.3(专家Agent调度)


## 接口审计

packages/engine-core/src/pipeline/diagnosis/doc-extractor.ts: DocExtractor.extract(docId, content, teamId) → { dimensions, insufficientDimensions }
packages/engine-core/src/pipeline/diagnosis/graph-store.ts: createNode(type: SOGNodeType, props, teamId) → nodeId
src/routes/diagnosis-upload-v2.ts: graphStore (SOG 图写入)


## 数据流

DocExtractor.extract() → dimensions(D1-D8) → graphStore.createNode(维度类型, 提取内容) → 专家工具 graph query → 找到真实数据而非空


## 用户旅程

FDE上传文档 → DocExtractor提取 → 提取结果写入SOG图(按维度创建节点) → 专家工具查询时找到数据 → 返回真实分析而非占位符


## Done 标准

- [x] 入口: 文档上传后自动创建维度节点
- [x] 链路: extract() → createNode() → expert tool query → 返回数据
- [x] 结果: 专家工具不再返回 "接入连接器后自动填充"


## 验证命令
```bash
bash scripts/workflow/checkpoint-impl.sh <新函数名>
```
