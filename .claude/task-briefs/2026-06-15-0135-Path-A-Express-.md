# Task Brief: Path A 验证发现 — 修复 SentinelRunner 未启动 + MCP 阻塞

> 生成时间: 2026-06-15 01:35:42
> 更新: 2026-06-15 (Path A 验证后, 2 个阻断问题)
> 分支: feat/prompt-architecture
> 代码库状态: tsc=0 errors, tests=passing, server=启动成功

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 完整数据流：
  原始数据 → 本体层(电子病历) → 7维度×25测量器(compute)
    ├─ 按需(FDE触发): runModules() → Evidence池 → 专家 → 诊断报告
    └─ 定时(Cron触发): Sentinel.check() → SentinelFinding → 信号聚合
                                                                    ↓
  信号聚合引擎 → 交叉关联+严重度升级+专家路由 → 6位专家(strategy/org/finance/tech/marketing/action)
       → ReAct推理+交叉验证 → 综合诊断报告 → FDE收到警报+报告

## 本任务在哪一层
<!-- L1/L2/L3/L4/L5？触及哪几层？有没有跨层风险？ -->
- L1 (index.ts/路由) + L2 (SynovaAgent 编排) + L3 (SentinelRunner)
- 无跨层风险。index.ts → SynovaAgent(L2) → SentinelRunner(L3) 符合架构。
- 附带修复 L5 MCP bridge 启动阻塞 (不影响架构边界)。

## 文档引用
<!-- 全量对齐手册哪些章节和本任务相关？引用具体节号。 -->
- CLAUDE.md 铁律 24: 异常处理审计 (catch 必须 log + degraded，不阻断)
- CLAUDE.md 铁律 39: 五层架构边界
- `src/agent/synova-agent.ts`: SentinelRunner 正确的初始化位置
- `src/sentinel/runner.ts`: SentinelRunner.runOnce() 签名

## 接口审计
<!-- 本任务调用的关键函数签名（从代码 grep 来的，不凭记忆） -->
- `src/sentinel/runner.ts: setGlobalSentinelRunner(runner: SentinelRunner | null): void` — 设置全局 runner 单例
- `src/sentinel/runner.ts: getGlobalSentinelRunner(): SentinelRunner | null` — 获取全局 runner (routes/sentinel.ts 调用)
- `src/sentinel/runner.ts: class SentinelRunner` — constructor(scheduler: CronScheduler, db: unknown), start(), runOnce(id)
- `src/sentinel/builtins.ts: registerBuiltinSentinels(): Promise<void>` — 注册 23 个内置哨兵
- `src/agent/synova-agent.ts: class SynovaAgent` — constructor(db: Database), start(), stop()
- `src/init/engine-context.ts: initEngineContext(): void` — 初始化 SQLite + EngineContext (当前非幂等)
- `src/mcp/tool-registration.ts: registerMCPTools(registry: ToolRegistry): Promise<void>` — 注册 MCP 工具
- `src/mcp/bridge.ts: MCPBridge.connect(serverName: string): Promise<MCPToolDef[]>` — 连接 MCP server (30s 超时 ×2 = 60s/server)

## 数据流
<!-- 输入来自哪里 → 经过哪些文件/函数 → 输出到哪里（用户看到什么） -->
```
问题1: src/index.ts → createServer() → app.listen() → ❌ SentinelRunner 未初始化
                                       → POST /api/sentinel/run/:id → "SentinelRunner 未启动"

修复后: src/index.ts → initEngineContext() → SynovaAgent(db).start()
         → createServer() (initEngineContext 幂等跳过)
         → registerBuiltinSentinels() → SentinelRunner.start()
         → POST /api/sentinel/run/:id → ✅ 哨兵执行成功

问题2: createServer() → registerMCPTools() → bridge.connect(brave-search) 30s
         → bridge.connect(github) 30s → app.listen() 被阻塞 ~60s+
修复后: createServer() → registerMCPTools() fire-and-forget → app.listen() 立即返回
```

## 用户旅程
<!-- 用产品语言描述: 谁→什么场景→做了什么→看到什么结果 -->
1. 运维人员启动 SynovaAgent: `DEV_MODE=true npx tsx src/index.ts`
2. 服务器立即就绪 (不等待 MCP)
3. 运维人员调用 `POST /api/sentinel/run/hacd` → 哨兵执行并返回结果
4. 哨兵 cron 定时任务在后台运行，发现异常自动创建工单

## Done 标准
<!-- 铁律 7: 入口可触达 + 完整链路走通 + 结果可见 -->
- [ ] 入口可触达: `POST /api/sentinel/run/:id` 返回哨兵执行结果 (非 "SentinelRunner 未启动")
- [ ] 链路走通: index.ts → SynovaAgent → SentinelRunner.start() → 23 内置哨兵注册 → cron 调度
- [ ] 结果可见: `GET /api/sentinel/health` 显示已注册的哨兵列表
- [ ] MCP 阻塞修复: 服务器启动时间 < 5s (无 SYNOVA_SKIP_MCP 时)
- [ ] 铁律门禁通过: `npm run check:all` 零失败

## 验证命令
```bash
# 启动服务器
DEV_MODE=true npx tsx src/index.ts &
sleep 8

# 验证 SentinelRunner 启动
curl -s http://localhost:3000/api/sentinel/health | jq .summary

# 验证手动触发
curl -s -X POST http://localhost:3000/api/sentinel/run/test-sentinel

# 验证 MCP 非阻塞 (日志应显示 MCP 注册在服务器就绪之后)
grep -E "SynovaAgent 就绪|MCP 工具" /tmp/synova-server.log
```
