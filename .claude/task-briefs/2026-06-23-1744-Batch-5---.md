# Task Brief: Batch 5: 引擎配置 + 连接器/数据源 + 专家工具 + MCP 文件化

> 生成: 2026-06-23 17:44:02 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

流程约束: V3.8 — task brief 6 字段强制 + plan.json 分阶段 + pre-commit 8 组物理阻断。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层
        Sentinel Finding[] → 诊断引擎 Phase 2 → 8 位文件驱动专家解读

L1 入口: POST /api/diagnosis/consult (GA诊断) / Cron→Sentinel.check() (哨兵) / GET /chat (Web) / MCP
五层架构 (只能向下依赖相邻层):
  L1 交互: routes/ tui/ mcp/
  L2 编排: agent/ orchestrator/
  L3 洞察: l3/ sentinel/ expert-platform/ expert/ (8位文件驱动专家: strategy org finance tech marketing action business_model knowledge)
  L4 本体: l4/ evidence/ 企业事实层: AgentMemoryStore (enterprise_fact, 版本化+superseded_by链)
  L5 存储: store/ cron/
三层粒度: 专家→哨兵→计算。哨兵=可独立告警的最小子领域。compute=纯数学函数。
L0 进化: evolution/ 两路反馈→候选池→确认/执行验证→写入知识库
文件化扩展: expert/ knowledge/shared/ theory/ skills/ — 新增=加文件,不改代码
数据安全: L0公开摘要→L1聚合信号→L2脱敏证据→L3原始数据(仅客户内Agent可见,GA不可见)
引擎: packages/engine-core/ (Novis遗产,逐步迁移)。禁止src/新增engine-core引用(铁律46)。
安全: security/ (PIIScrubber, DataBoundary)
LLM: providers/ (DeepSeek, OpenAI, Gateway)

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
基础设施收尾 — 引擎配置在 L2/L3 交接处（引擎迁移 Claude 的口子 1），连接器/数据源在 L4/L5 边界，专家工具在 L3，MCP 在 L1/L3 边界。触及 L2(编排), L3(洞察), L4(本体)。

引擎配置: src/config-file.ts 已有 maxToolRounds + diagnosis config 的文件加载。引擎迁移 Claude 预留了 createSynovaDiagnosisEngine(llm, tools, config) 工厂接口。扩展已有 config-file 支持 extensions/engine/diagnosis.json 路径即可。

IM 连接器/数据源: src/connectors/ 已有 FeishuConnector/NemoClawConnector/IMA + unified-connector.ts (Airbyte 协议)。extensions/adapters/ 目录不存在。复用已有 connector 类型和 Python bridge 基础设施。

专家工具: src/agent/builtin-tools.ts 已有内置工具。extensions/tools/ 目录不存在。Task brief 提到的 3 个崩溃 Bug 在当前代码中 grep 无结果——可能已被 engine-core 迁移修复。

MCP: packages/extension-registry/ 已有 discover/hotload/生命周期管理。src/extensions/registry.ts 已有 re-export。复用已有 ExtensionRegistry，接线而非重建。

### b) 文件审计
grep engine config: src/config-file.ts (已有 maxToolRounds + diagnosis 配置加载)
grep adapters: extensions/adapters/ 不存在。src/connectors/ 已有 FeishuConnector 等。
grep tools: extensions/tools/ 不存在。src/agent/builtin-tools.ts 已有工具注册。
grep MCP: packages/extension-registry/ 已有完整实现。src/extensions/registry.ts re-export。
grep 崩溃 Bug: listModules/auto-interpreter/throw Error('removed') — src/ 内零结果。

关系:
- engine config: 扩展已有 config-file.ts → 新建 extensions/engine/diagnosis.json
- adapters: 新建 extensions/adapters/，复用 src/connectors/ 类型
- tools: 新建 extensions/tools/，复用 builtin-tools 注册模式
- MCP: 复用已有 ExtensionRegistry → 接线到 server.ts 启动流程

无冲突。全部是"无覆盖→新建"或"已有覆盖→复用扩展"。

### c) 决策
1. 引擎配置: 已有覆盖(config-file)→复用扩展。不改引擎内部实现。diagnosis.json 作为 config-file 的一个数据源。
2. 连接器/数据源: 无覆盖→新建 extensions/adapters/。复用 src/connectors/ 的 DataConnector 接口和 OntologyAdapter 类型。
3. 专家工具: 无覆盖→新建 extensions/tools/。复用 builtin-tools 的 ToolDefinition 类型。
4. MCP: 已有覆盖(ExtensionRegistry)→复用。接线到 server.ts，不重写 discover/hotload。
5. 崩溃 Bug: 当前 src/ 无残留。不处理。

## Q1: 调研

a) 12-factor app: 配置与代码分离。Airbyte 协议: spec/check/discover/read。
b) Anthropic: 复用现有基础设施，不建新系统。
c) Q0c 教训: Batch 1-4 双数据源。本批次确认旧系统活跃→复用扩展。

## Q2: 范围

做: diagnosis.ts 读 synova.json、extensions/adapters/ + loader、ExtensionRegistry 接线。
不做: extensions/engine/ (config-file 已处理)、extensions/tools/ (builtin-tools 正常)。
不碰 engine-core。不创建新双数据源。

## Q3: 验收

入口: synova.json 改 maxToolRounds → 引擎用新值
交互: loadAdapters() 返回 feishu+nemoclaw → 自动注册
结果: 新增连接器 = 加 manifest+adapter → 自动发现

## 本任务在哪一层

L2 (编排) — diagnosis.ts。L4 (本体) — adapter-loader, extensions/adapters/。不跨层。

## Done 标准
- [x] 入口可触达: diagnosis.ts 从 config.diagnosis 读参数
- [x] 链路走通: loadAdapters() >= 2 适配器
- [x] 结果可见: 新增连接器 = 加文件 → 自动注册
