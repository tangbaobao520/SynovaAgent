# Task Brief: Loop Engineering V4.2.9 → V4.3.0

> 生成: 2026-07-03 | 分支: feat/prompt-architecture | L4 本体层哲学 + 版本号同步

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
- [x] 文档 + 配置（CLAUDE.md + 脚本版本号同步）

本任务将 Loop Engineering 从 V4.2.9 升级到 V4.3.0。
核心变更：在 CLAUDE.md 中新增 L4 本体层设计哲学（图遍历思维 vs KV 读取）、compute 函数签名标准、已知问题与演进方向。

- 性质：文档升级 + 版本号同步
- 产出物：CLAUDE.md 新增本体层章节、15+ 个文件版本号从 V4.2.9 → V4.3.0

### b) 文件审计
- `CLAUDE.md` — 主修改文件（新增 L4 本体层设计哲学章节 + 版本号更新）
- `scripts/pre-push-check.sh` — 版本号更新
- `scripts/pre-commit-check.sh` — 版本号更新
- `scripts/workflow/*.sh` — 多个文件版本号更新
- `src/server.ts` — 注释中的 V4.2.9 版本号
- `src/sentinel/sentinel-loader.ts` — 注释版本号
- `src/sentinel/runner.ts` — 注释版本号
- `src/routes/ontology.ts` — 注释版本号
- `src/routes/diagnosis.ts` — 注释版本号

关系：文档升级 + 版本号同步

### c) 决策
无冲突。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
1. **为什么升级版本号**：V4.3.0 引入了一个重要的架构原则变更——compute 函数必须使用图遍历思维，不能退化为 KV 读取。这是对 46 个哨兵 compute 函数的设计范式约束，值得一次主版本号升级。
2. **为什么不是 minor**：因为这是对已有代码的设计范式要求，而非增量功能。所有现有 compute 函数需要被审计是否符合图遍历原则。
3. **范围**：文档 + 版本号同步。不改代码逻辑。

引用依据：
- 铁律 35: 自动化优先 — 版本号通过 sed 批量更新，不手动改

### b) 本任务执行约束
- rule: "所有 V4.2.9 版本号必须同步更新"
  verify: "grep -rn 'V4\.2\.9' .claude/ CLAUDE.md scripts/ src/ --include='*.{md,sh,ts}' 2>/dev/null | grep -v node_modules | grep -v '.test.'; test $? -eq 1"

## Q2: 范围 — 正确的最简方案是什么？

做什么：
1. CLAUDE.md 新增 L4 本体层设计哲学章节
2. CLAUDE.md 版本号 V4.2.9 → V4.3.0
3. 15+ 个源文件注释版本号同步
4. 不改变任何代码逻辑

不做什么：
- 不改 compute 函数（只是文档约束）
- 不改 GraphStore 实现
- 不改 ontology 类型定义

## Q3: 验收

入口：grep V4.2.9 全仓库 → 零结果
处理：sed 批量替换 + CLAUDE.md 手动编辑章节
结果：V4.3.0 版本号统一，CLAUDE.md 新增本体层哲学

## 本任务在哪一层
跨层（文档 + 脚本版本号）

## Done 标准
- [x] verify: grep -rn 'V4\.2\.9' .claude/ CLAUDE.md scripts/ src/ --include='*.{md,sh,ts}' 2>/dev/null | grep -v node_modules; test $? -eq 1
- [x] verify: grep -q '图遍历思维\|graph traversal\|compute.*GraphStoreReader' CLAUDE.md
