# Task Brief: Task 2.1: 新边类型 Schema — 核心因果边 8个JSON

> 生成: 2026-07-04 03:58:06 | 分支: feat/prompt-architecture | as any: 2

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。L1 交互层不知道 L4 用什么数据库，L3 洞察层不知道 L5 数据存在哪。换底层存储，上层零改动。pre-commit 物理阻断跨层 import——L2→L4 的代码提交不进去。

**横向解耦：11 个独立 Monorepo 包**
五层内部拆为独立包：@synova/sog-core（本体图类型）、@synova/sentinel-engine（哨兵调度）、@synova/expert-platform（专家加载）、@synova/connector-registry（数据连接器）。每个包接口边界明确，拆卸一个不影响其余 19 个。核心包已落地运行；已存在的功能规划从 src/ 迁移到独立包；未来新增须遵循此结构。

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码：
- 新 AI 专家 = 新建目录 + 10 个 Markdown 文件 → 自动注册到 ExpertDispatcher
- 新诊断哨兵 = 加 xxx-sentinel.ts → builtins 自动扫描加载
- 新行业 = 加行业目录（基准数据+阈值+案例库）→ 1-2 天上线，零 TypeScript 改动
- 新本体实体类型 = 加 JSON Schema 文件

流程约束: V4.3.0 — task brief 6 字段强制 + 免疫系统 + plan.json + 8 组物理阻断 + Plan-Actual 闭合 + engine-core 清零 + 时间戳顺序检查。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层
        Sentinel Finding[] → 诊断引擎 Phase 2 → 8 位文件驱动专家解读

L1 入口: POST /api/diagnosis/consult (GA诊断) / Cron→Sentinel.check() (哨兵) / GET /chat (Web) / MCP
五层架构 (只能向下依赖相邻层):
  L1 交互: routes/ tui/ mcp/
  L2 编排: agent/ orchestrator/
  L3 洞察: l3/ sentinel/ expert-platform/ expert/ (8位文件驱动专家: strategy org finance tech marketing action business_model knowledge)
  L4 本体: l4/ evidence/ 企业事实层: AgentMemoryStore (enterprise_fact, 版本化+superseded_by链)
  L5 存储: store/ cron/
三层粒度: 专家→哨兵→计算。哨兵=可独立告警的最小子领域。compute=纯数学函数。
L0 进化: evolution/ 两路反馈→候选池→确认/执行验证→写入知识库
文件化扩展: expert/ knowledge/shared/ theory/ skills/ — 新增=加文件,不改代码
数据安全: L0公开摘要→L1聚合信号→L2脱敏证据→L3原始数据(仅客户内Agent可见,GA不可见)
引擎: packages/engine-core/ (Novis遗产,逐步迁移)。禁止src/新增engine-core引用(铁律46)。
安全: security/ (PIIScrubber, DataBoundary)
LLM: providers/ (DeepSeek, OpenAI, Gateway)

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
- [ ] 纵向（改 L1-L5 代码/架构）
- [ ] 横向（迁移到独立包 / 新建包）
- [x] **扩展（文件驱动，不改 TypeScript）**

本任务属于 **基础设施** 系统，触及 **L4 本体层**。

**该层现有模块：**
- `extensions/ontology/node-types/` — 22 个旧节点类型（保留不动）
- `extensions/ontology/resource/` — 13 个新资源实体（已由 Task A Step 1 创建）
- `extensions/ontology/activity/` — 8 个新活动实体（已由 Task A Step 1 创建）
- `extensions/ontology/outcome/` — 8 个新结果实体（已由 Task A Step 1 创建）
- `extensions/ontology/edge-types/` — 17 个旧边类型（本任务替换前 8 个）
- `src/l4/ontology-loader.ts` — 扫描 edge-types/ 目录加载边类型
- `src/l4/graph-bridge.ts` — 图桥接消费边类型

**本任务性质：替换旧边类型文件为新的因果边 Schema。** 使用新命名空间（`edge/xxx` 而非 `edge-type/xxx`），allowedFrom/To 指向新 resource/activity/outcome 实体。

### b) 文件审计
```
当前 edge-types/ 目录（17个旧文件）:
  affects, aligns-with, belongs-to, competes-with, consumes,
  corresponds-to, cost-driven-by, depends-on, has-access-to,
  interacts-with, owns, provides, revenue-from, sells-to,
  sources-from, triggers, value-proposition

本次创建（8个新文件）:
  deploys, funds, produces, replenishes, depends_on,
  substitutes, augments, locks_in
```

**关系：替换。** 新边类型引用新 resource/activity/outcome 实体类型，替代引用旧 node-types 的旧边。

### c) 决策
旧边类型对应的节点类型已被新实体替代 → 替换为引用新实体类型的边。旧边文件逐步删除（本次删前8个，下一任务删剩余9个）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
本任务按以下顺序执行：
1. **SPEC** — 确定 8 条边的 allowedFrom/allowedTo 和 requiredProps（从 Task A 设计文档中提取）
2. **验证** — 创建边类型后，ontology-loader 能正确解析它们（loader 自动扫描 edge-types/ 目录）
3. **实现** — 创建 8 个 JSON 文件，每个符合 EdgeTypeDef 接口
4. **验证** — JSON 格式合法 + 删除的旧文件不再被代码引用 + tsc 零错误

引用依据：
- **铁律 0-2**: spec → test → impl → wire → review → merge。验证 loader 解析 = test。
- **铁律 37**: Dead code 入仓库即违规 — 删除旧边类型前确认无代码引用旧标签。
- **memory/stub-implementation-pattern.md**: 每个文件必须是真实内容 — JSON 字段必须完整。
- **铁律 46**: 禁止桥接文件 — JSON 文件不走 import，走文件驱动扫描。

### b) 本任务执行约束（写入 plan.json principles）

**rule 1: 每个新边 JSON 必须含 allowedFrom/allowedTo/requiredProps，且引用新实体类型**
verify: `python3 -c "import json,sys; [sys.exit(1) for f in sys.argv[1:] for d in [json.load(open(f))] if 'allowedFrom' not in d or 'allowedTo' not in d or 'requiredProps' not in d]" extensions/ontology/edge-types/deploys.json extensions/ontology/edge-types/funds.json extensions/ontology/edge-types/produces.json extensions/ontology/edge-types/replenishes.json extensions/ontology/edge-types/depends_on.json extensions/ontology/edge-types/substitutes.json extensions/ontology/edge-types/augments.json extensions/ontology/edge-types/locks_in.json && echo "PASS" || echo "FAIL"`

**rule 2: 新边 $id 使用 `edge/xxx` 命名空间（不是旧的 `edge-type/xxx`）**
verify: `grep -h '"$id"' extensions/ontology/edge-types/deploys.json extensions/ontology/edge-types/funds.json extensions/ontology/edge-types/produces.json extensions/ontology/edge-types/replenishes.json extensions/ontology/edge-types/depends_on.json extensions/ontology/edge-types/substitutes.json extensions/ontology/edge-types/augments.json extensions/ontology/edge-types/locks_in.json | grep -v '"edge/' && echo "FAIL: 有旧命名空间" || echo "PASS: 全部使用 edge/ 命名空间"`

**rule 3: 删除的旧边文件在代码中无引用（标注为已删除的旧 $id）**
verify: `grep -r '"edge-type/consumes\|"edge-type/affects\|"edge-type/aligns-with\|"edge-type/belongs-to\|"edge-type/competes-with\|"edge-type/corresponds-to\|"edge-type/has-access-to\|"edge-type/interacts-with' src/ --include="*.ts" | grep -v node_modules && echo "WARN: 有旧边引用" || echo "PASS: 无旧边引用"`

## Q2: 范围 — 正确的最简方案是什么？

**做什么：**
1. 创建 8 个新边类型 JSON 文件，替换旧边类型：
   - `deploys.json` — DEPLOYS: Resource→Activity
   - `funds.json` — FUNDS: MONEY→Activity
   - `produces.json` — PRODUCES: Activity→Outcome
   - `replenishes.json` — REPLENISHES: Outcome→Resource
   - `depends_on.json` — DEPENDS_ON: Activity→Activity
   - `substitutes.json` — SUBSTITUTES: Activity→Activity
   - `augments.json` — AUGMENTS: Resource→Resource
   - `locks_in.json` — LOCKS_IN: Resource→Resource
2. 删除 8 个旧边类型 JSON 文件（被新边覆盖的旧语义）
3. 验证 ontology-loader 能正确解析新边类型

**不做什么：**
- 不创建剩余 8 个新边类型（下一任务做）
- 不修改 `src/l4/ontology-loader.ts` — 已有读取 edge-types/ 的逻辑
- 不删除所有 17 个旧边文件 — 只删与被替换语义重叠的 8 个
- 不修改任何 compute 函数或 aggregate.ts
- 不替换专家提示词

## Q3: 验收 — 入口 → 交互 → 结果

**入口：** 无（基础设施数据文件，无用户入口）
**处理：** 创建 8 个新边 JSON 文件 → 删除 8 个旧边 JSON 文件 → ontology-loader 自动发现新文件
**结果：** 
- `npx tsc --noEmit` 零错误（新 JSON 被 LoadedOntology 接口正确解析）
- 验证脚本确认每个边文件包含 requiredProps
- ontology-loader 扫描 edge-types/ 后能正确返回新边类型列表

**验证方式：**
```bash
# 验证每个新边 JSON 包含必需字段
python3 -c "
import json, os, sys
d = 'extensions/ontology/edge-types'
for f in ['deploys','funds','produces','replenishes','depends_on','substitutes','augments','locks_in']:
    path = os.path.join(d, f+'.json')
    if not os.path.exists(path): print(f'FAIL: {path} 不存在'); sys.exit(1)
    data = json.load(open(path))
    for field in ['\$id','label','allowedFrom','allowedTo','requiredProps']:
        if field not in data: print(f'FAIL: {f}.json 缺少 {field}'); sys.exit(1)
    if not data['\$id'].startswith('edge/'): print(f'FAIL: {f}.json \$id 不是 edge/ 开头'); sys.exit(1)
    print(f'PASS: {f}.json')
print('ALL PASS')
"

# 验证旧边文件确实被删除
test ! -f extensions/ontology/edge-types/consumes.json && echo "PASS" || echo "FAIL: 旧文件残留"

# 验证代码无损坏
npx tsc --noEmit
npx vitest run --reporter=verbose 2>&1 | tail -5
```

## 本任务在哪一层

L1（scripts/ + electron-renderer/）+ L2（src/services/ + src/agent/）+ L3（src/l3/expert-dispatcher.ts）+ L5（src/server.ts）

## Done 标准

- [ ] 入口可触达: 不适用（基础设施数据文件）
- [ ] 链路走通: `python3 -c "import json; [json.load(open(f'extensions/ontology/edge-types/{e}.json')) for e in ['deploys','funds','produces','replenishes','depends_on','substitutes','augments','locks_in']]"` exit 0
- [ ] 结果可见: `ls extensions/ontology/edge-types/*.json | wc -l` = 旧数量从17减少（旧8个已删除，新8个已添加→总数变化取决于是否删除重叠旧文件）
- [ ] verify: `grep -h '"$id"' extensions/ontology/edge-types/deploys.json extensions/ontology/edge-types/funds.json extensions/ontology/edge-types/produces.json extensions/ontology/edge-types/replenishes.json extensions/ontology/edge-types/depends_on.json extensions/ontology/edge-types/substitutes.json extensions/ontology/edge-types/augments.json extensions/ontology/edge-types/locks_in.json | grep 'edge/' | wc -l | xargs -I{} bash -c 'test "{}" = "8" && echo "PASS: 8 new edge types" || echo "FAIL"'`
- [ ] verify: `npx tsc --noEmit` 零错误
- [ ] verify: `npx vitest run --reporter=verbose 2>&1 | tail -3` 确认无新增失败
