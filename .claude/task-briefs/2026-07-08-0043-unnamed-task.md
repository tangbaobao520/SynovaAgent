# Task Brief: 哨兵空壳清理

> 生成: 2026-07-08 | 分支: feat/prompt-architecture

## Q0: 定位
系统：哨兵基础设施。类型：清理。11个manifest.json缺aggregate.ts，运行时产生加载错误。
文件审计：11个目标在extensions/sentinels/{name}/，结构为manifest.json+computes/（无aggregate.ts）。
决策：清理。Phase 1的3个有等价覆盖，Phase 2的8个非当前方向。

## Q1: 调研
参考铁律0-2(spec→test→impl→wire→verify)、铁律37(dead code清理)。
原则：
1. 删除manifest.json后保留computes/归档（verify: ls docs/archive/sentinels/*/computes/*.ts | wc -l | grep -q 11）
2. sentinel加载errors=0（verify: npx vitest run tests/sentinel/sentinel-loader.test.ts | grep -q '5 passed'）
3. 壳包@synova/diagnosis-engine引用清除（verify: grep -rn "'@synova/diagnosis-engine'" src/ | grep -v "注释\|comment"）

## Q2: 范围
做什么：删除11个manifest.json，目录归档至docs/archive/sentinels/，清理壳包packages/diagnosis-engine/
不做什么（含具体文件路径）：
- ❌ 不修改sentinel aggregate.ts（extensions/sentinels/unit-economics/aggregate.ts等50个active哨兵）
- ❌ 不涉及src/sentinel/目录（sentinel-loader.ts、types.ts、builtins.ts、runner.ts、signal-aggregator.ts、registry.ts）
- ❌ 不涉及tests/目录（所有测试文件不变）

## Q3: 验收
入口→处理→结果：git commit
验证1: sentinel加载数50（ls manifest.json | wc -l = 50）
验证2: tsc零新增错误
验证3: pre-commit全通过

## 架构层级
L3（sentinel-loader）+ 文件驱动层（extensions/sentinels/）

## 本任务在哪一层
L3（sentinel/） + L5（文件驱动扩展层 extensions/sentinels/）

## Done 标准
- [x] verify: ls extensions/sentinels/*/manifest.json 2>/dev/null | wc -l | grep -q 50
- [x] verify: grep -c "SOGNodeType\|SOGEdgeType" src/ --include="*.ts" | tail -1 | grep -q 0
- [x] verify: npx vitest run tests/sentinel/sentinel-loader.test.ts 2>&1 | grep -q '5 passed'
