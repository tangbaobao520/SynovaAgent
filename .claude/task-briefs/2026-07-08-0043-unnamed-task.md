# Task Brief: 哨兵空壳清理

> 生成: 2026-07-08 00:44:01 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。

**横向解耦：11 个独立 Monorepo 包**

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码。

流程约束: V4.4.2 — task brief 6 字段强制 + 免疫系统 + plan.json + 8 组物理阻断 + Plan-Actual 闭合 + engine-core 清零 + 时间戳顺序检查。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [ ] 纵向（改 L1-L5 代码/架构）
- [ ] 横向（迁移到独立包 / 新建包）
- [x] 扩展（文件驱动，不改 TypeScript）

本任务属于哪个系统（GA诊断/哨兵/基础设施）？触及哪层？该层现有模块？新增/替换/扩展？
- 系统：**哨兵（Sentinel）基础设施**
- 触及：**L3 洞察层（sentinel/ → sentinel-loader.ts）** + **文件驱动层（extensions/sentinels/）**
- 现有模块：61 个 manifest.json 哨兵注册项 + sentinel-loader.ts 自动扫描加载
- 类型：**清理** — 不是新增/替换/扩展。删除 11 个注册了 manifest.json 但缺 aggregate.ts 的空壳目录（运行时产生错误日志）

### b) 文件审计
grep 本任务关键词在 expert/ sentinel/ extensions/ knowledge/ theory/ skills/ 中：
- 11 个目标哨兵均在 `extensions/sentinels/{name}/` 下，结构为 manifest.json + computes/ （无 aggregate.ts）
- `src/sentinel/` 中的引用仅为历史注释（"V4.2.4: 已删除"标记），无活跃 import
- `tests/l3/graphbridge-wiring.test.ts` 中 `hona` 仅作为 mock 模块名用于测试 graph bridge 接线，不依赖实际 sentinel 存在

关系：**清理** — 删除注册文件（manifest.json），保留理论代码（computes/ 目录）

### c) 决策
已有覆盖→复用，不准新建硬编码。无覆盖→新建走文件驱动（属扩展解耦）。冲突→取消任务，复用已有。
- Phase 1 的 3 个哨兵有等价覆盖（unit-economics / competitive-moat-structural / cost-health+profit-health+revenue-health+cash-runway）→ clean delete
- Phase 2 的 8 个哨兵无等价覆盖但非当前产品方向 → clean delete（保留 computes/ 作为理论归档）

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
本任务按以下顺序执行，每一步完成后才能进入下一步：
  ① SPEC / Done 标准 — task brief 已定义 11 个目标 + 3 条物理约束
  ② 实现 — 删除 11 个 manifest.json，保留 computes/ 目录
  ③ 接线 — 验证 sentinel-loader 不再加载已删除哨兵（sentinel 数量从 61→50）
  ④ 验证 — 自检 6 问 + 验证 3 项：加载数、文件列表、零意外影响

引用依据（至少引用两项）：
  - 铁律 0-2: spec → test → impl → wire → review → merge
  - 铁律 37: Dead code 入仓库即违规。删除旧文件 + grep 零引用确认
  - 铁律 46: 禁止桥接代理文件（本次不适用，因为不是迁移）
  - memory/ 历史教训：engine-core-split-fraud.md（确保删除后无残留引用）

### b) 本任务执行约束（写入 plan.json principles，pre-commit 组 6 验证）
根据决策链和本任务特点，提炼 3 条必须遵守的规则：
1. rule: "删除 manifest.json 后必须保留 computes/ 目录（可逆归档）"
   verify: "for s in token-economics seven-powers financial-snapshot collaboration-health eob hacd hona htm path-dependency self-awareness gap-dynamics; do test -d extensions/sentinels/\$s/computes && echo 'OK: \$s/computes preserved' || echo 'FAIL: \$s/computes missing'; done"
2. rule: "禁止修改任何非目标哨兵的文件"
   verify: "git diff --name-only --diff-filter=M | grep -v '.claude/' | grep -v 'CLAUDE.md' | grep -v 'LOOP' | grep -v 'docs/' || echo 'OK: no unexpected modifications'"
3. rule: "src/ 中不能有对已删除哨兵的活跃 import（注释中的历史引用除外）"
   verify: "grep -rn 'import.*token-economics\|import.*seven-powers\|import.*financial-snapshot\|import.*collaboration-health\|import.*eob\|import.*hacd\|import.*hona\|import.*htm\|import.*path-dependency\|import.*self-awareness\|import.*gap-dynamics' src/ --include='*.ts' && echo 'FAIL: active imports found' || echo 'OK: no active imports'"

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- 删除 11 个空壳哨兵的 manifest.json（sentinel-loader 不再扫描到它们）
- 保留 11 个 computes/ 目录（理论代码归档，可逆）
- commit message 中记录被删除 manifest.json 的完整内容

不做什么（附具体文件路径）：
- ❌ 不修改任何 aggregate.ts（如 `extensions/sentinels/unit-economics/aggregate.ts` 等 50 个 active 哨兵）
- ❌ 不涉及 `src/sentinel/` 下的任何文件（具体如 `sentinel-loader.ts`、`types.ts`、`builtins.ts` 等）
- ❌ 不修改 `tests/` 目录下的测试文件
- ❌ 不改动 `tests/` 目录下的测试文件

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：git commit（代码变更入口）。直接操作文件系统删除 extensions/sentinels/{name}/manifest.json。
处理（中间经过哪些步骤）：1) 删除 11 个 manifest.json → 2) vitest run + tsc 验证 → 3) pre-commit 8 组通过 → 4) git commit
结果（最终展示在哪）：sentinel 加载数从 61 减为 50，加载日志不再出现 "entryPoint 不存在" 错误。

## 本任务在哪一层
L3（sentinel-loader 运行时加载）+ 文件驱动层（extensions/sentinels/ 文件系统）

## Done 标准
- [x] verify: ls extensions/sentinels/*/manifest.json 2>/dev/null | wc -l | grep -q 50 && echo OK
  说明：入口可触达，sentinel 加载数从 61 减至 50
- [x] verify: npx tsc --noEmit 2>&1 | grep -v "npm notice" | wc -l | grep -q 0 && echo OK
  说明：链路走通，tsc 零新增错误
- [x] verify: npx vitest run tests/sentinel/sentinel-loader.test.ts 2>&1 | grep -q 'Tests.*5 passed' && echo OK
  说明：结果可见，loader errors=0，测试通过
- [x] verify: grep -q '11个空壳哨兵' .claude/task-briefs/2026-07-08-0043-unnamed-task.md && echo OK
  说明：可逆性，brief 记录了被删哨兵信息
