# T2：哨兵核心修复

## Q0:
a) 项目拼图: Synova = AI 诊断 Agent。L3 洞察层 — sentinel/ 子系统。4个财务哨兵(cash-runway/cost-health/profit-health/revenue-health)的 aggregate.ts 中内联了 compute 计算逻辑，需要抽取为独立 compute 文件。本任务是扩展已有模块，不新增哨兵。
b) 文件审计: grep 发现 extensions/sentinels/{4哨兵}/aggregate.ts 各含内联计算逻辑。已有文件驱动模式 extensions/sentinels/*/computes/ 可复用。
c) 决策: 无覆盖 — 新建 compute 走文件驱动架构(computes/独立文件)，不准硬编码在 TS 里。

## Q1:
a) 业界最佳实践: 遵循 V4.3.0 compute 纯函数模式。b) 参考 capital-efficiency/computes/wacc.ts 等已有 compute 签名。c) memory/ 里没有相关教训。

## Q2:
新增 8 个 compute 文件 + 对应测试 + 4个 aggregate.ts 修改 + 3个命名不匹配修复。不改 manifest.json(除path-dependency)。不改其他哨兵。不涉及 L1/L2/L4/L5 层。

## Q3:
入口: 哨兵 check() → aggregate.ts 调用 compute 函数 → 基于 compute 结果生成 Finding。测试: vitest 全量通过。结果: CI 验证通过 + 零新增失败。

## 本任务在哪一层
L3 洞察层 (sentinel/ 子系统)

## Done 标准
- [ ] 8 个 compute 文件存在且有 JSDoc 契约ID
- [ ] 8 个测试文件存在，有 expect() 断言
- [ ] vitest 全量通过零新增失败
- [ ] tsc --noEmit 零新增错误
- [ ] aggregate.ts 中无内联计算残留
