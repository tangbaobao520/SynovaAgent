---
task_id: "I2-3a"
---

Q0 定位: 审计12处旧边名引用 + D2死引用computeOccupancy。PRODUCES(9处) + AUGMENTS(2处) + EXTERNAL_ASSUMPTION_BINDS(1处)。

Q1 调研: 任务文档已提供完整引用清单。不改函数逻辑。

Q2 范围: 只改边名常量。PRODUCES→OPERATIONAL_EXECUTION(9) + AUGMENTS→TECH_INFRASTRUCTURE(2) + EXTERNAL_ASSUMPTION_BINDS→ASSUMPTION_TRIGGERED_REALLOCATION(1) + 删除computeOccupancy导出。
不动: DEPLOYS(36处)/FUNDS(22处) @deprecated。

Q3 验收: grep PRODUCES/AUGMENTS/EXTERNAL_ASSUMPTION_BINDS零残留。tsc零错误。vitest零新增失败。

架构层级: L3(哨兵compute) + L4(本体)

Done 标准: 旧名零残留 + computeOccupancy删除 + tsc零错误 + vitest零新增失败 + 零as any
