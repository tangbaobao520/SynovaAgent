---
task_id: "I2-Phase1"
---

Q0 定位:
a) 项目拼图 — 本任务在L4本体层。当前edge-types目录20条JSON Schema。本任务删除7条DELETE边JSON + 创建42条新边JSON Schema + 更新edge-consumption-map.json。不碰.ts文件、不碰EdgeType枚举、不碰compute函数。
b) 文件审计 — extensions/ontology/edge-types/ 20个JSON已全部审计。42条新边从SYNOVA-RESEARCH-纯净边列表-20260709.md推导。
c) 决策 — 新建42个JSON文件，使用新节点ID(pool/*, virtual/*, external/baseline)。

Q1 调研:
a) 依赖纯净边列表研究结论，allowedFrom/allowedTo端点与42条边纯净列表一致。
b) 已有produces.json/constrains.json等JSON Schema作为格式模板。
c) memory/ 中无同类教训。

Q2 范围:
- 只做: 删除7个DELETE边JSON + 创建42个新边JSON + 更新edge-consumption-map.json
- 不做: 修改任何.ts文件(约束2)、EdgeType枚举写入(阶段2)、MODIFY边改造(阶段2)、compute函数(阶段3)、哨兵迁移(阶段4)

Q3 验收:
入口: extensions/ontology/edge-types/ 目录
交互: ls | wc -l = 55(13旧+42新)
结果: ontology-loader.ts scanDir()可以加载全部42条边; edge-consumption-map.json含42条条目

架构层级: L4本体层 — 仅JSON文件，不涉及代码

Done 标准:
1. 7条DELETE边JSON已不存在于edge-types目录
2. 42条新边JSON全部存在且格式正确(9个必填字段)
3. edge-consumption-map.json含42条条目
4. 零 .ts 文件变更
5. 零 as any
6. tsc --noEmit 通过
