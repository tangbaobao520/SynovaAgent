---
task_id: "I2-Phase2"
---

Q0 定位:
a) 项目拼图 — I2阶段2，L4本体层。packages/ontology/src/edge-types.ts (20边→42边)。同步修改graph-bridge.ts中DELETE边引用。
b) 文件审计 — edge-types.ts 20个常量。graph-bridge.ts 3处引用DELETE边(INFORMS×1, DEPENDS_ON×2)。
c) 决策 — KEEP(7)不变 + MODIFY改名(4) + @deprecated保留(2) + NEW(29)追加 = 42。

Q1 调研:
a) 42条新边label已在阶段1 JSON中定义。
b) graph-bridge.ts的INFORMS→INFORMATION_FLOW, DEPENDS_ON→TALENT_DEPLOYMENT。
c) DEPLOYS(36引用)/FUNDS(22引用)保留+@deprecated — 阶段4才删除。

Q2 范围:
- 做: edge-types.ts重写(42边) + graph-bridge.ts 3处替换 + ALL_EDGE_TYPES同步
- 不做: 哨兵aggregate旧边名(阶段4)、compute函数(阶段3)、DEPLOYS/FUNDS枚举删除(阶段4)

Q3 验收:
入口: packages/ontology/src/edge-types.ts → 42边常量
交互: npx tsc --noEmit = 零新增错误
结果: grep -c "EdgeType\." edge-types.ts = 42; grep "DEPENDS_ON\|INFORMS" src/l4/graph-bridge.ts = 空

架构层级: L4本体层 — packages/ontology/ + src/l4/graph-bridge.ts

Done 标准:
- EdgeType枚举42个常量
- 8个DELETE边零残留
- graph-bridge.ts零INFORMS/DEPENDS_ON
- DEPLOYS/FUNDS保留+@deprecated
- tsc零新增错误
- 零 as any
- ALL_EDGE_TYPES同步
