## Q0: 定位 — 项目拼图 + 文件审计 + 决策
### a) 项目拼图
SynovaAgent — D14: 15概念节点NodeType枚举注册。I2阶段4完成所有边引用迁移后，边JSON中引用的概念节点前缀(pool/capital, pool/activity, pool/sensing, pool/governance, external/baseline)在node-types.ts中完全不存在。本任务将这些概念节点注册为字符串常量，使42边JSON的allowedFrom/allowedTo引用在TypeScript层面有效。

### b) 文件审计
grep pool/ 和 external/ 等前缀在 packages/ontology/src/node-types.ts 中零结果。
node-types.ts 现有29个类型(8 activity + 8 outcome + 13 resource)。
方案: 新增POOL_NODE_TYPES(15个)和EXTERNAL_TYPES(1个)，共16个新常量。
index.ts 导出需同步更新。

### c) 决策
新增16个常量，不删除不修改现有29个。ALL_NODE_TYPES从29扩展到45。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
引用铁律0-2(测试先行+接线验收)/铁律38(as any零容忍)。
memory教训: 本体层(I1)教训 — 新类型必须接线到导出否则pre-commit阻断。
42边JSON实际引用的概念节点前缀仅有5种: pool/capital, pool/activity, pool/sensing, pool/governance, external/baseline。
研究文档定义了15个概念节点(9存量池+6活动池)，边JSON仅用了部分前缀(所有活动池统一pool/activity)。

## Q2: 范围 — 正确的最简方案
做什么:
  1. node-types.ts 新增POOL_NODE_TYPES段 — 9个存量池 + 6个活动池字符串常量
  2. node-types.ts 新增EXTERNAL_NODE_TYPES段 — external/baseline
  3. ALL_NODE_TYPES 追加新类型(从29扩展到45)
  4. 更新packages/ontology/src/index.ts导出POOL_TYPES/EXTERNAL_TYPES
不做什么:
  不删除不修改现有29个类型; 不修改42边JSON; 不修改graph-bridge; 不修改哨兵
排除: extensions/ontology/edge-types/*.json(不改), src/l4/graph-bridge.ts(不改)

## Q3: 验收 — 入口 → 交互 → 结果
入口: packages/ontology/src/node-types.ts (追加新常量)
处理: 追加16个新字符常量 + 分类数组 + ALL_NODE_TYPES扩展
结果: tsc零错误, 新类型被edge-types.json引用的前缀格式覆盖, index.ts正确导出

## 架构层: L4(本体层) — packages/ontology/src/node-types.ts

## Done 标准
- [ ] verify: 15个概念节点 + external/baseline = 16个新常量在 node-types.ts 中
- [ ] verify: ALL_NODE_TYPES 包含所有45个类型 (29旧 + 16新)
- [ ] verify: index.ts 导出新类型
- [ ] verify: tsc零错误 / 零as any
- [ ] verify: pre-commit 8组全部通过
