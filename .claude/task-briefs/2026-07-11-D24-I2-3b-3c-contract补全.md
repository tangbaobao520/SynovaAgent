## Q0: 定位 — 项目拼图 + 文件审计 + 决策
SynovaAgent — D24。
审计: I2-3b (l1-input/12) + I2-3c (l2-internal/10 + l3-output/7) = 29个compute
全部缺失 @contract JSDoc (当前使用"契约ID:"非标准格式)
全部测试仅3 it() (正常/降级/边界)，缺第4组时序声明
全部是纯横截面计算(输入标量值)，不消费时序参数
决策: 逐文件补 @contract + 第4组fixture，时序标注"不适用"。
对标 l5-reinput 已验证的 @contract 格式。
### a) 项目拼图
D24 是 compute 层 JSDoc 和测试的规范化任务。29 个 compute 分布在 shared/computes/l1-input/ (12), l2-internal/ (10), l3-output/ (7)。
### b) 文件审计
29 个 .ts compute 文件 + 29 个 tests/sentinels/shared/*.test.ts 测试文件。
grep 关键词: @contract → 仅 l5-reinput/ 和 cross-cycle/ 有 (0/29 目标文件)。
### c) 决策
补全 @contract + 测试扩展第4组。不改函数逻辑。不创建新文件。

## Q1: 调研 — 业界最佳实践 / memory 历史教训
a) 测试体系权威规范 §2.2-2.3: @contract格式 + 4组fixture + 时序强制规则
b) l5-reinput/compute-retention-protects-knowledge.ts: 已验证的 @contract 格式
c) memory/: @contract缺失→pre-commit阻断(T6历史); as any零容忍

## Q2: 范围 — 做什么 + 不做什么
做什么:
  1) 29个compute的@contract JSDoc补全为完整格式
  2) 29个测试从3组fixture扩展为4组(正常/边界/异常/时序不适用声明)
不做什么:
  不改compute函数逻辑; 不创建新文件
排除: extensions/sentinels/*/aggregate.ts; extensions/ontology/edge-types/*.json

## Q3: 验收 — 入口→处理→结果
入口: 58个已有文件(29 compute + 29 test)
处理: 补@contract + 扩fixture组数
结果: 29个@contract完整 + 29个测试≥4 it()

## 架构层: L4
本任务属于 L4(本体层compute)。不涉及 L1-L3/L5 变更。
## Done 标准
- verify: 29个compute的@contract含{contractId} {input} {output} {degraded}
- verify: 29个测试各≥4 it() + expect
- verify: 每测试含时序不适用声明
- verify: tsc零错误 / vitest零失败 / 零as any
