# D31: 6个新增数据适配器JSON

> 生成: 2026-07-11 | 分支: feat/prompt-architecture | V4.4.5

## Q0: 定位 — 项目拼图 + 文件审计 + 决策
a) 项目拼图
   SynovaAgent — D31。纯JSON配置任务。
   当前仅 erp-standard.json 覆盖 Financial 节点。其余7个outcome JSON无适配器覆盖。
   数据层权威规范§1.2.3定义了6个需新建的适配器名称和目标outcome。
b) 文件审计
   extensions/ontology/field-mappings/ 当前仅1个文件: erp-standard.json
   extensions/ontology/outcome/ 下有8个 outcome JSON (financial/operational/people/market/competitive/external/innovation/risk)
   competitive-intel.json在规范中需覆盖competitive+external两个outcome，但现有adapter模板仅支持单targetNodeType。本任务将competitive-intel拆为两个独立适配器。
c) 决策
   按outcome JSON的optionalProps逐一写入映射，externalField用中文系统字段名。
   不修改data-ingest-service.ts，不修改现有erp-standard.json。

## Q1: 调研 — 引用来源 + memory教训
a) 数据层权威规范§1.2.3: 6个适配器清单 + 对应outcome
b) erp-standard.json: 现行模板 {name, label, targetNodeType, mappings[]}
c) 8个outcome JSON已全部审计 — optionalProps与任务文档完全一致
d) memory/: 适配器的externalField是中文系统字段名（中国企业ERP/HR/CRM系统多用中文表头）

## Q2: 范围 — 做什么 + 不做什么
做什么: 创建6个JSON文件
  erp-operational.json → Operational (7字段+period)
  hr-standard.json → People (6字段+period)
  crm-standard.json → Market (6字段+period)
  competitive-intel.json → Competitive (6字段+period)
  external-intel.json → External (6字段+period) — competitive-intel拆为两个
  innovation-pipeline.json → Innovation (4字段+period)
  risk-register.json → Risk (4字段+period)
不做什么:
  不修改data-ingest-service.ts; 不修改现有erp-standard.json; 不创建.ts代码文件
排除: src/agent/data-ingest-service.ts(不改), extensions/ontology/outcome/*.json(不改)

## Q3: 验收 — 入口→处理→结果
入口: extensions/ontology/field-mappings/ (当前仅1个文件)
处理: 追加6个JSON文件
结果: 7个适配器可覆盖全部8个outcome JSON

## 架构层: L5(数据层) — extensions/ontology/field-mappings/
## Done 标准
- [x] verify: 7个适配器文件存在
- [x] verify: 每个JSON可被JSON.parse()解析且格式正确({name,label,targetNodeType,mappings})
- [x] verify: 每个适配器的prop字段全部来自对应outcome JSON的optionalProps（含类型匹配）
- [x] verify: externalField全部使用中文系统字段名
- [x] verify: 零as any / tsc零错误（JSON文件不经过tsc）
