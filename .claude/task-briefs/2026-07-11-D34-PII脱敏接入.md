# D34: PII脱敏接入数据入口

> 生成: 2026-07-11 | 分支: feat/prompt-architecture | V4.4.5

## Q0: 定位 — 项目拼图 + 文件审计 + 决策
a) 项目拼图
   SynovaAgent — D34。PIIScrubber 功能完善(S1-S4四级+opt-out+角色掩盖)但零调用。
   数据层规范§4.1要求"PII脱敏在入口最前端强制"。
b) 文件审计
   pii-scrubber.ts(src/security/): scrub(text,level)返回{cleaned,matches,degraded} ✅功能完整
   data-ingest-service.ts(src/agent/): ingestRow中字符串字段直接写入→零PIIScrubber引用
   routes/data.ts(src/routes/): POST upload→零PIIScrubber引用
c) 决策
   两个入口点接入: ingestRow字符串字段调用scrub()→写入脱敏值; upload入口S4预检。

## Q1: 调研 — 引用来源 + memory教训
a) 数据层权威规范§4.1: PII脱敏入口强制+三通道脱敏规则
b) PIIScrubber.scrub(text,level='S2'): 返回{cleaned,matches,degraded}
c) memory/: 铁律24(log.error非log.warn) / 铁律31(降级不阻断) / 铁律38(as any零容忍)

## Q2: 范围 — 做什么 + 不做什么
做什么:
  1) data-ingest-service.ts ingestRow: 字符串字段调用scrub()→写入脱敏后值
  2) routes/data.ts POST upload: 批量行在ingestBatch前做S4预检→含密码/Token返回422
不做什么:
  不改PIIScrubber本身; 不创建新文件; 不修改erp-standard.json
排除: src/security/pii-scrubber.ts(不改)

## Q3: 验收 — 入口→处理→结果
入口: 2个文件(ingest-service + routes/data)
处理: 字符串字段经过scrub()脱敏后再写入GraphStore
结果: PII原始值不出现在数据节点中

## 架构层: L5(数据层入口) — src/agent/data-ingest-service.ts + src/routes/data.ts
src/agent/data-ingest-service.ts(L2编排) + src/routes/data.ts(L1交互) — PIIScrubber接入两道防线

## Done 标准

## Done 标准
- [ ] verify: grep "getPIIScrubber" src/agent/data-ingest-service.ts 有结果
- [ ] verify: grep "getPIIScrubber" src/routes/data.ts 有结果
- [ ] verify: npx tsc --noEmit 零新增错误
- [ ] verify: grep -c "as any" src/agent/data-ingest-service.ts src/routes/data.ts | grep ":0" | wc -l
