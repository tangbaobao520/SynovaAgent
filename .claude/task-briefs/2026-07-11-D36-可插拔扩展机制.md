# D36: 可插拔扩展机制 — 热注册 + 热加载 + 语义注册

> 生成: 2026-07-11 | 分支: feat/prompt-architecture | V4.4.5

## Q0: 定位 — 项目拼图 + 文件审计 + 决策
a) 项目拼图
   SynovaAgent — D36。D31已创建7个field-mapping适配器JSON。
   data-ingest-service.ts loadFieldMapping(name)通过文件名参数加载，非自动发现。
   无custom-adapters目录。无文件系统监听。无语义注册。
b) 文件审计
   sentinel-loader.ts: 哨兵manifest自动发现模式——对标
   field-mappings/*.json: 8个JSON(实际8个: erp-standard/erp-operational/hr-standard/crm-standard/competitive-intel/external-intel/innovation-pipeline/risk-register)
   data-ingest-service.ts: loadFieldMapping静态加载→改为from registry
c) 决策
   新建AdapterScanner(文件扫描) + AdapterRegistry(注册+生命周期) +
   扩展data-ingest支持扫描到的适配器自动可用。
   对标sentinel-loader的文件驱动自发现模式。

## Q1: 调研 — 引用来源 + memory教训
a) 数据层权威规范 §6: 热注册/热加载/语义注册/定制适配器
b) sentinel-loader.ts: 哨兵manifest自动发现模式——对标
c) D31: 8个适配器JSON已存在，直接可被自动发现
d) memory/: 文件驱动模式已验证(sentinels/)，复用同模式

## Q2: 范围 — 做什么 + 不做什么
做什么:
  1) 新建 AdapterScanner: 启动时扫描field-mappings/*.json → 自动注册
  2) 新建 AdapterRegistry: 管理适配器生命周期(register/unregister/list)
  3) 扩展 data-ingest: 从registry获取适配器而非硬编码名称
  4) 新建 routes/adapters.ts: GET /api/adapters + POST /api/adapters/reload
  5) 新建 custom-adapters/ 目录 + registry.ts 模板
不做什么:
  不实现文件系统监听(fs.watch开销大, 改为启动时扫描+手动reload API)
  不修改现有8个适配器JSON
排除: extensions/ontology/field-mappings/*.json(不改)

## Q3: 验收 — 入口→处理→结果
入口: field-mappings/目录扫描 + GET /api/adapters
处理: AdapterScanner发现8个JSON → AdapterRegistry注册 → data-ingest获取列表
结果: D31的8个适配器自动可用, 无需代码注册

## 架构层: L5(数据层扩展) — extensions/ontology/ + src/agent/ + src/routes/
## Done 标准
- [ ] verify: AdapterScanner 启动时返回 8 个适配器名称
- [ ] verify: AdapterRegistry.list() 返回已注册适配器
- [ ] verify: GET /api/adapters 返回已注册列表
- [ ] verify: POST /api/adapters/reload 重新扫描
- [ ] verify: custom-adapters/registry.ts 模板就绪
- [ ] verify: tsc零错误 / vitest零新增失败 / 零as any
