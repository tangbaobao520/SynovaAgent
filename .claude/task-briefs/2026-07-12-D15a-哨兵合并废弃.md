## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
SynovaAgent — 哨兵规范P0。D15a：3组合并(9源→4目标) + 4个废弃。哨兵总数从51降至50。
### b) 文件审计
| 审计项 | grep 结果 | 行动 |
|--------|-----------|------|
| 合并目标存在 | capital-health/competitive-position/competitive-moat/margin-health — 全部MISSING | 新建4个合并目标 |
| 合并源存在 | 9个哨兵全部存在(extensions/sentinels/) | 整合logic→_extinct/ |
| 废弃哨兵存在 | adaptation-velocity/connector-coverage/structural-change — 存在 | →_extinct/ |
| profit-health | 存在(合并到margin-health) | merge→_extinct/ |
| sentinel-loader.ts | 已有 `entry.name.startsWith('_')` skip逻辑(L73) | 零代码修改 |
| src/引用 | 13哨兵在src/中零引用(仅loadSentinels扫描) | 无影响 |
### c) 决策
合并+废弃。4合并目标整合源逻辑(import _extinct/ aggregate.ts)。sentinel-loader零修改(_extinct/自动跳过)。

## Q1: 调研 — 现有接口 / 内存教训
a) sentinel-loader.ts L73: `if (entry.name.startsWith('_')) continue;` — _extinct/自动跳过
b) registerLoadedSentinels: 动态import aggregate.ts + registry.register()
c) aggregate.ts模式: { check(store, teamId, traversal): Promise<SentinelFinding[]> }
d) 合并策略: 新aggregate.ts依次调用各源check() → 合并findings
e) 铁律37: 死代码不入仓库 — _extinct/保留源文件(审计可回滚)，但gitignore可配置

## Q2: 范围 — 正确的最简方案
**做什么:**
1. extensions/sentinels/_extinct/ 创建归档目录
2. 13哨兵移到_extinct/(4废弃+9合并源)
3. 4个合并目标目录新建(capital-health/competitive-position/competitive-moat/margin-health)
4. 每个合并目标: manifest.json + aggregate.ts(import源logic → 合并findings)
5. 测试: manifest.json可用 + loader跳过_extinct/ + 总数=50

**不做什么:**
- 不删除废弃哨兵的历史工单(extensions/sentinels/sentinel_tickets保留)
- 不新建10个哨兵(D59-D64)
- 不修改合并后哨兵的计算逻辑(整合源，不新写)
- 不修改 src/sentinel/sentinel-loader.ts（约束3: 零代码修改）

**排除:** extensions/sentinels/shared/(不改)，src/sentinel/sentinel-runner.ts(不改)

## Q3: 验收 — 入口→处理→结果
入口: sentinel-loader.ts → readdirSync(extensions/sentinels/)
处理: skip _extinct/ → 加载4合并目标+其余保留哨兵 → registerLoadedSentinels
结果: 哨兵总数=50。4合并目标注册+可执行。废弃哨兵不被加载但文件保留在_extinct/。

## 架构层: L4(本体层扩展) — extensions/sentinels/

## Done 标准
- [ ] 4合并目标目录存在(capital-health/competitive-position/competitive-moat/margin-health)
- [ ] 每个合并目标含 manifest.json + aggregate.ts(整合源逻辑)
- [ ] 4个废弃哨兵移至_extinct/(adaptation-velocity/connector-coverage/structural-change/profit-health)
- [ ] 9个合并源移至_extinct/
- [ ] sentinel-loader.ts 零修改(_extinct/自动跳过)
- [ ] 哨兵总数=50(grep manifest.json|wc -l)
- [ ] >=8测试覆盖(manifest.json可解析+loader跳过+总数)
- [ ] tsc零新增错误 / vitest零新增失败 / 零as any
