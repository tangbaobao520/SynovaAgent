# D30: 数据质量门禁 — 新鲜度+完整性+数据气味+三级降级+冷启动

> 生成: 2026-07-12 | 分支: feat/prompt-architecture | V4.4.5

## Q0: 定位 — 项目拼图 + 文件审计 + 决策
a) 项目拼图
   SynovaAgent — D30。D35的FreshnessTracker仅记录最后更新时间(被动), 不做主动质量判定。
   数据层规范§3.1定义了5类数据的新鲜度阈值。无完整性检查。无数据气味。无三级降级。无冷启动。
b) 文件审计
   freshness-tracker.ts(src/monitoring/): getStatusByPool/getDegradedSources ✅ 可复用
   pipeline-monitor.ts(src/monitoring/): getStats/recordFailure ✅ 可复用
   质量门禁: 零代码 → 新建
c) 决策
   新建DataQualityGate模块, 复用FreshnessTracker+PipelineMonitor, 零侵入data-ingest

## Q1: 调研 — 引用来源 + memory教训
a) 数据层规范 §3.1-3.6: 新鲜度阈值(5类×4级) + 完整性 + 数据气味 + 三级降级 + 冷启动
b) FreshnessTracker: getStatusByPool返回{sourceId,delayDays,freshnessStatus}
c) PipelineMonitor: getStats返回{total,successRate,byChannel}
d) memory/: 质量门禁不可阻断写入(仅标记)

## Q2: 范围 — 做什么 + 不做什么
做什么:
  1) 新建 DataQualityGate: evaluate() → {passed, checks[], dataMaturity}
  2) 新鲜度判定: 5类数据×4级阈值(green/yellow/orange/red)
  3) 完整性校验: 必填字段非空 + 类型匹配
  4) 数据气味: 更新突变/完整率下降
  5) 三级降级: 软(标记)/硬(暂停推送)/完全暂停
  6) 冷启动: 0-3月行业基准→3-6月混合→6月+自身
不做什么:
  不实现自动回滚, 不修改data-ingest
排除: src/agent/data-ingest-service.ts(不改), src/routes/data.ts(不改)

## Q3: 验收 — 入口→处理→结果
入口: DataQualityGate.evaluate(poolName, dataSourceId)
处理: 检查新鲜度→完整性→数据气味→返回质量报告
结果: 质量报告含passed/checks/freshness/completeness/dataMaturity

## 架构层: L3(洞察层)
src/monitoring/data-quality-gate.ts

## Done 标准
- [ ] verify: evaluate('financial','erp-connector')返回{passed,checks,freshness,completeness}
- [ ] verify: 新鲜度5类数据×4级阈值判定正确
- [ ] verify: 冷启动0月→行业基准, 6月+→自身基线
- [ ] verify: npx tsc --noEmit 零新增错误
