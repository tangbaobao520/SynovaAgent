## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova 8位专家 + host 主持人, 共9位专家。当前每位的角色/权限/推理域分散在 IDENTITY.md/RULES.md/THEORY.md/TOOLS.md/CROSS_EXPERT.md 四个文件中——无法被机器直接解析。D53 为每位专家创建结构化 manifest.json，格式对标哨兵manifest。
### b) 文件审计
grep: 9位专家目录 (host/strategy/org/finance/marketing/tech/action/business_model/knowledge), 每目录含5文件。
manifest.json: 零存在。全部新建。
### c) 决策
9个新建manifest.json。零修改现有5文件。数据从现有IDENTITY.md/RULES.md/TOOLS.md/THEORY.md/CROSS_EXPERT.md + expert-registry.yaml提取。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
a) 哨兵manifest实例: extensions/sentinels/cash-runway/manifest.json — name/version/type/displayName/description格式。
b) 补充修正文档: 专家manifest.json Schema (name/version/type/displayName/description/tone/boundaries/frameworks/edges/computes/crossDomainRule/moduleLoading/entryPoints/dependencies)。
c) 42边体系: SYNOVA-ARCH-本体层-20260707.md (E-01~E-42)。
d) expert-registry.yaml: 9位专家 enabled/background/model/tools。
e) memory/ 铁律38(零as any), 文件驱动原则。

## Q2: 范围 — 正确的最简方案
做什么：
1. 9个 manifest.json 文件: host/strategy/org/finance/marketing/tech/action/business_model/knowledge
2. 每个含 name/version/type/displayName/description/tone/priority/loading/enabled/background/model/boundaries/frameworks/edges/computes/crossDomainRule/moduleLoading/entryPoints/dependencies
3. edges引用42边E-XX格式。computes引用COMPUTE-格式
不做什么：
- 不修改 IDENTITY.md/RULES.md/TOOLS.md/THEORY.md/CROSS_EXPERT.md (零修改)
- 不修改 expert-registry.yaml
- 不创建加载器(归D65)
- 不创建 PROMPT.md(归D54)

## Q3: 验收 — 入口 → 交互 → 结果
入口: expert/{name}/manifest.json 文件存在
处理: JSON.parse() 可解析 + 必填字段完整
结果: 9位专家各有结构化声明，可被后续 SkillLoader(D65) 加载

## 架构层: 
L3(洞察层配置)

## Done 标准
- [x] verify: for d in expert/*/manifest.json; do test -f "$d"; done — 9个manifest.json全部存在
- [x] verify: for d in expert/*/manifest.json; do node -e "JSON.parse(require('fs').readFileSync('$d','utf-8'))"; done — 每个可通过 JSON.parse() 解析
- [x] verify: npx vitest run tests/expert/manifest-consistency.test.ts — 必填字段/edges/computes/boundaries校验
- [x] verify: grep -oP '"E-\d{2}"' expert/*/manifest.json | wc -l — edges字段引用真实的42边ID (E-01~E-42)
- [x] verify: node -e "const fs=require('fs');fs.readdirSync('expert').filter(f=>fs.statSync('expert/'+f).isDirectory()&&f!='_template').forEach(d=>{const j=JSON.parse(fs.readFileSync('expert/'+d+'/manifest.json','utf-8'));if(!j.boundaries.length)process.exit(1)})" — boundaries不可为空数组
- [x] verify: echo "纯JSON文件，零as any" — 零as any (纯JSON文件)
- [x] verify: npx vitest run tests/expert/manifest-consistency.test.ts | grep -q "16 passed" — 测试覆盖 >= 7 (实际16个测试)
