## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
SynovaAgent — 部署运维P0。D48：静默升级 + 版本回滚。基于 electron-main.ts 已有 autoUpdater 基础设施(check/download/install/notify/periodic) 和 D47 数据目录。
### b) 文件审计
| 审计项 | grep 结果 | 行动 |
|--------|-----------|------|
| schema-version.ts | 零存在 | 新建 — 破坏性DDL检测 |
| compute-version.ts | 零存在 | 新建 — contractId版本追踪 |
| rollback.ts | 零存在 | 新建 — 快照回滚 |
| electron-main.ts | autoUpdater已配(L310-394) | update-downloaded插入schema check |
| D47 data-directory.ts | 已存在(src/deploy/) | 回滚包定位 |
### c) 决策
3新文件 + electron-main.ts 插入一处schema check。不改autoUpdater核心。

## Q1: 调研 — 现有接口 / memory教训
a) electron-main.ts L332-353: autoUpdater.update-downloaded 事件 → 通知用户点击安装
b) 插入点: update-downloaded 事件中，安装前执行 schemaCheck()
c) 约束1: 不改autoUpdater核心 — 只插入schema check
d) 约束2: Schema只ADD不DROP — DROP/ALTER列类型 → blocked:true
e) 约束3: 回滚降级 — 快照不存在时 {available:false, warnings:[]}
f) 铁律24: catch 必须有 log + degraded 标记

## Q2: 范围 — 正确的最简方案
**做什么:**
1. src/deploy/schema-version.ts — checkSchemaCompatibility() 检测破坏性DDL
2. src/deploy/compute-version.ts — getComputeVersion() 追踪compute contractId版本
3. src/deploy/rollback.ts — rollbackToSnapshot() 快照恢复
4. electron-main.ts — update-downloaded事件中插入schema check

**不做什么:**
- 不改 electron-main.ts autoUpdater 核心逻辑
- 不改 src/deploy/data-directory.ts D47 文件
- 不改 src/deploy/startup-check.ts D47 文件
- 不实现增量包生成: 排除 packages/ scripts/ 目录

**排除:** electron-main.ts autoUpdater 部分, src/deploy/data-directory.ts, src/deploy/startup-check.ts

## Q3: 验收 — 入口→处理→结果
入口: autoUpdater update-downloaded → schemaCheck(新版本vs当前)
处理: schema兼容 → compute版本检查 → 允许安装 / 不兼容→阻止安装+通知
结果: 兼容→静默升级完成 / 不兼容→阻止+回滚选项

## 架构层: L4
部署基础设施 — src/deploy/

## Done 标准
- [ ] schema-version.ts: 破坏性DDL检测(checkSchemaCompatibility → {compatible, blockedReason})
- [ ] compute-version.ts: contractId版本追踪(getComputeVersion → {latestVersion, migrations})
- [ ] rollback.ts: 快照恢复(rollbackToSnapshot → {success, warnings})
- [ ] electron-main.ts: update-downloaded事件中插入schema check
- [ ] 约束1: autoUpdater核心零修改
- [ ] 约束2: Schema只ADD不DROP
- [ ] 约束3: 回滚降级(快照不存在→警告不阻断)
- [ ] >=7测试覆盖(schema正常/破坏性DDL/版本递增/回滚快照存在/不存在降级/回滚后数据完整)
- [ ] tsc零新增错误 / vitest零新增失败 / 零as any
