## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
SynovaAgent — 部署运维P0。D49：独立看门狗 + 三层监控。基于现有 GET /health 端点(version+uptime)升级为全量 /api/healthz。
### b) 文件审计
| 审计项 | grep 结果 | 行动 |
|--------|-----------|------|
| healthz.ts | 零存在 | 新建 — 6项健康检查 |
| watchdog.js | 零存在 | 新建 — 独立看门狗进程 |
| system-health.ts | 零存在 | 新建 — 7项健康指标审计类 |
| src/server.ts | Express路由已配 | 挂载 /api/healthz |
| src/agent/report-assembler.ts | 现存 | 注入systemHealth |
| GET /health | 已存在(src/routes/health.ts?) | 保留，不修改 |
### c) 决策
5交付物(3新建+2修改)，零外部依赖看门狗。

## Q1: 调研 — 现有接口 / memory教训
a) 权威文档§3.1-3.5: 看门狗进程/三层监控/healthz 6项检查/系统健康审计
b) server.ts: Express路由模式 app.use('/api/...', router)
c) D47/D48 deploy/ 基础设施: 数据目录、日志、版本管理
d) 铁律24: catch必须有log+degraded
e) 铁律31: 降级信号传播

## Q2: 范围 — 正确的最简方案
**做什么:**
1. scripts/watchdog.js — 独立看门狗(<500行, 零外部依赖)
2. src/routes/healthz.ts — GET /api/healthz (6项独立检查)
3. src/monitoring/system-health.ts — 系统健康审计(7指标)
4. src/server.ts — 挂载 healthz 路由
5. src/agent/report-assembler.ts — 注入 systemHealth

**不做什么:**
- 不改 electron-main.ts
- 不实现L3业务层监控
- 看门狗不做自动修复损坏数据
- 看门狗不依赖TypeScript(纯.js)

**排除:** electron-main.ts, src/server.ts(仅加一行路由挂载)

## Q3: 验收 — 入口→处理→结果
入口: 看门狗进程(独立) + GET /api/healthz(外部探测)
处理: 6项检查→独立try-catch→合并状态
结果: healthy/degraded/down + 每项 detail + uptime

## 架构层: L1+L4
L1(healthz路由) + L4(监控: scripts/watchdog.js + monitoring/system-health.ts)

## Done 标准
- [ ] watchdog.js: <500行, 零外部依赖, 5分钟探测周期
- [ ] healthz.ts: 6项独立检查+独立try-catch+格式{status,checks,uptime}
- [ ] system-health.ts: 7项指标(SystemHealthAudit类)
- [ ] server.ts: /api/healthz路由已注册
- [ ] report-assembler.ts: depth报告含systemHealth
- [ ] >=14测试: healthz 8 + system-health 6+edge
- [ ] tsc零新增错误 / vitest零新增失败 / 零as any
