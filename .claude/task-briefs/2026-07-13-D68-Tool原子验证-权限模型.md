## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。D68 是 Skill-Tool 体系 P0 任务，紧接 D65（ToolRegistry 骨架）和 D38（PolicyEngine）。
ToolRegistry 已有 register/get/invoke，缺原子性验证（3条件）和 PolicyEngine 门禁。

### b) 文件审计
- src/tools/tool-registry.ts — D65产物，71行，register/get/invoke 基础功能
- src/security/policy-engine.ts — D38产物，ABAC 权限引擎 evaluate({role,dataLevel,soi})
- src/l4/audit-store.ts — 审计日志存储（append-only，需DB）

### c) 决策
扩展 D65 的 tool-registry.ts，不改 register/get 签名。新增 validateAtomicity/invoke门禁/getToolsBySkill。
PolicyEngine 拒绝时不 throw，返回 {error: 'POLICY_DENIED'}。

## Q1: 调研
- 第12份文档第五章 5.1: 原子性验证3条件 — 输入/输出契约明确、可独立测试、被>=2个Skill复用
- 第12份文档第五章 6.1: 权限三元组 — PolicyEngine(D38)仲裁
- D38 PolicyEngine: evaluate({role, dataLevel, soi}) => {allow, denyReason}
- D65 tool-registry.ts: ToolDef 接口 (name/version/fn/inputSchema/outputType)
- 铁律38(零as any) + 铁律24(异常处理)

## Q2: 范围
做什么：
1. tool-registry.ts: ToolDef 新增可选字段 (contractId/skills/hasTests)
2. tool-registry.ts: 新增 validateAtomicity(tool) — 3条件检查
3. tool-registry.ts: invoke() 增加 PolicyEngine 门禁 + 审计日志
4. tool-registry.ts: 新增 getToolsBySkill(skillName) 反向查询
5. 更新 tests/tools/tool-registry.test.ts — ≥8测试用例

不做什么：
- 不改 D65 的 register()/get() 签名
- 不改 D38 PolicyEngine
- 不创建 25 个 Tool 原子（后续任务）
- 不修改 audit-store.ts

## Q3: 验收
入口: toolRegistry.invoke('computeDOL', params, {role:'manager', dataLevel:'S1', soi:'GRAPH_TRAVERSE'})
处理: PolicyEngine.evaluate() 裁决 → 允许则执行，拒绝则记录审计日志
结果: Tool 调用受权限保护，拒绝时返回 {error: 'POLICY_DENIED'}

## 架构层: L2（编排层扩展）
## Done 标准:
- [ ] validateAtomicity(tool): 3条件检查（契约/可测试/>=2复用）
- [ ] invoke() 增加 PolicyEngine 门禁
- [ ] 拒绝时返回 {error: 'POLICY_DENIED'} 而非 throw
- [ ] 拒绝时写入审计日志
- [ ] getToolsBySkill(skillName) 反向查询
- [ ] tsc零新增错误 / vitest零新增失败 / 零as any
