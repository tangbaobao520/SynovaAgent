# Task Brief: D296 控制塔数据真实性与完成度引擎修复

> 生成: 2026-08-01 04:43:08 | 分支: main | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。L1 交互层不知道 L4 用什么数据库，L3 洞察层不知道 L5 数据存在哪。换底层存储，上层零改动。pre-commit 物理阻断跨层 import——L2→L4 的代码提交不进去。

**横向解耦：11 个独立 Monorepo 包**
五层内部拆为独立包：@synova/sog-core（本体图类型）、@synova/sentinel-engine（哨兵调度）、@synova/expert-platform（专家加载）、@synova/connector-registry（数据连接器）。每个包接口边界明确，拆卸一个不影响其余 19 个。核心包已落地运行；已存在的功能规划从 src/ 迁移到独立包；未来新增须遵循此结构。

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码：
- 新 AI 专家 = 新建目录 + 10 个 Markdown 文件 → 自动注册到 ExpertDispatcher
- 新诊断哨兵 = 加 xxx-sentinel.ts → builtins 自动扫描加载
- 新行业 = 加行业目录（基准数据+阈值+案例库）→ 1-2 天上线，零 TypeScript 改动
- 新本体实体类型 = 加 JSON Schema 文件

流程约束: V4.5.0 — task brief 6 字段强制 + 免疫系统 + plan.json + 8 组物理阻断 + Plan-Actual 闭合 + engine-core 清零 + 时间戳顺序检查。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层
        Sentinel Finding[] → 诊断引擎 Phase 2 → 8 位文件驱动专家解读

L1 入口: POST /api/diagnosis/consult (GA诊断) / Cron→Sentinel.check() (哨兵) / GET /chat (Web) / MCP
五层架构 (只能向下依赖相邻层):
  L1 交互: routes/ tui/ mcp/
  L2 编排: agent/ orchestrator/
  L3 洞察: l3/ sentinel/ expert-platform/ expert/ (8位文件驱动专家: strategy org finance tech marketing action business_model knowledge)
  L4 本体: l4/ evidence/ 企业事实层: AgentMemoryStore (enterprise_fact, 版本化+superseded_by链)
  L5 存储: store/ cron/
三层粒度: 专家→哨兵→计算。哨兵=可独立告警的最小子领域。compute=纯数学函数。
L0 进化: evolution/ 两路反馈→候选池→确认/执行验证→写入知识库
文件化扩展: expert/ knowledge/shared/ theory/ skills/ — 新增=加文件,不改代码
数据安全: L0公开摘要→L1聚合信号→L2脱敏证据→L3原始数据(仅客户内Agent可见,GA不可见)
引擎: packages/engine-core/ (Novis遗产,逐步迁移)。禁止src/新增engine-core引用(铁律46)。
安全: security/ (PIIScrubber, DataBoundary)
LLM: providers/ (DeepSeek, OpenAI, Gateway)

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [ ] 纵向（改 L1-L5 代码/架构）
- [ ] 横向（迁移到独立包 / 新建包）
- [x] 扩展（基础设施工具链，非 TS 业务代码）

本任务属于哪个系统（GA诊断/哨兵/基础设施）？触及哪层？该层现有模块？新增/替换/扩展？
- 系统：**控制塔基础设施**（项目自诊断系统，权威文档17 §三.2 "C 结论有用 | 未知 | 从未用真实数据验证过"）
- 层级：工具链层（scripts/audit + scripts/control-tower），不碰 src/ 五层业务代码
- 现有模块：
  - scripts/audit/self-diagnosis.py（D267 六条件任务级完成度判定）— 修改
  - scripts/audit/completion-engine.py（D261 完成度引擎）— 修改
  - scripts/audit/check-gates-v2.py（17 门禁判定，写 gate-status.json）— 不改（已正确）
  - scripts/audit/audit-check.py（D290 已修 grep_in_files Windows 兼容）— 复用其模式
  - scripts/control-tower/views/completion.py（完成度视图）— 修改
  - scripts/control-tower/generate-dashboard.py（仪表盘渲染）— 修改
  - scripts/control-tower/emit-signal.py（D214 共享信号 CLI）— 复用（重建信号用）
- 新增：scripts/audit/completion_schema.py（统一 schema 契约）+ tests/control-tower/test_control_tower_truthfulness.py

### b) 文件审计
grep 本任务关键词（completion-scores / gate-status / signals / dependency-graph / checkpoints）：
- .codex/signals/*.json — 13 个被删（git 有、磁盘无），仅剩 .json 空壳 (152B, component="")
- .codex/snapshots/{ts}/completion-scores.json — 两个引擎都写、互相覆盖；20260730T085724Z 快照 systemScore=0.167 假数据
- .codex/dependency-graph.json — 47KB 陈旧 (07-30 16:57)，当前无写入方，仅 views/workflow_graph.py 读取
- .codex/checkpoints/ — cp3-commit-check.json 存在 (pre-commit 写入)，cp1/cp2 (D260) 未生成
- 无 expert/ sentinel/ extensions/ 冲突 — 本任务纯工具链，与文件驱动模块零交集

### c) 决策
- 复用：emit-signal.py（重建信号）、audit-check.py grep_in_files 模式（B4 修复）、check-gates-v2.py（gate-status 生成）
- 新建：completion_schema.py 统一契约（铁律47 契约优先）+ 测试文件
- 冲突：无。与 D291 (src/ 无关)、D286/D292 (packages/src 无关) 零共享文件

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
本任务按以下顺序执行，每一步完成后才能进入下一步：
  ① SPEC / Done 标准 — 任务文档 §6 完成标准 DS1-DS8
  ② 测试 — 先写测试，测试 = 产品的一部分（任务文档 §4 七个测试 T1-T7）
  ③ 实现 — 刚好满足以下全部条件：
     - Done 标准中列出的所有完成项
     - 测试全部通过
     - 接线完整（新 export 有引用：parse_brief_file_mapping / validate_completion_schema / freshness_check）
     - 错误路径有 log + degraded（铁律 24+31）
     - tsc + vitest 零失败
  ④ 接线 — 端到端走通（generate-dashboard.py → 5 视图 → /cockpit 可见）
  ⑤ 验证 — 自检 6 问（接线/异常/类型/测试/残留/文件驱动）

引用依据：
  - 铁律 0-2: spec → test → impl → wire → review → merge（先写 T1-T7 测试）
  - 铁律 7: 入口可触达（控制塔 → /cockpit 5 视图）+ 完整链路走通（self-diagnosis → 统一 schema → views） + 结果可见（无假 0%）
  - 铁律 24+31: 缺 gate-status.json 必须 degraded:true + 原因，禁止正常格式假数据
  - 铁律 47: 契约优先 — completion_schema.py SCHEMA 常量 + validate_completion_schema()
  - memory/engine-core-bridge-files.md 教训: 声称完成必须有 grep 物理证明（DS3/DS5 的 grep 验证）

### b) 本任务执行约束（写入 plan.json principles，pre-commit 组 6 验证）
  - rule: "completion-scores.json 只允许统一 schema 生成器写（self-diagnosis.py 任务级 + completion-engine.py 单文件判定）"
    verify: "grep -rn 'completion-scores.json' scripts/audit/ | grep -v completion_schema.py"
  - rule: "缺 gate-status.json 时 completion-engine 输出必须 degraded:true（禁止假 0 分）"
    verify: "grep -rn 'degradedReason' scripts/audit/completion-engine.py"
  - rule: "新增函数必须被生产调用方引用"
    verify: "grep -rn 'parse_brief_file_mapping\\|validate_completion_schema\\|freshness_check' scripts/"

## Q2: 范围 — 正确的最简方案是什么？

做什么（严格按任务文档 §3.3/§3.4）：
- scripts/audit/completion_schema.py: 新建 — SCHEMA 常量 (draft-07) + validate_completion_schema() 无依赖校验器
- scripts/audit/self-diagnosis.py: B1-B5 修复 — find_source_file 双通道（brief 文件审计优先 + 文件名降级带 degraded_reason）、d_id 正则 D\d+[a-z]? 取首个、按 D# 去重保留最新 brief、grep 子进程改 Python 原生扫描（复用 audit-check.py 模式）、输出统一 schema
- scripts/audit/completion-engine.py: 输出统一 schema + 缺 gate-status.json 时 degraded:true + degradedReason（exit 0）
- scripts/control-tower/views/completion.py: 只消费统一 schema（overallScore→systemScore）、时间解析 %Y%m%dT%H%M%SZ
- scripts/control-tower/views/pm_dashboard.py: 适配统一 schema 六键（A←code_exists 等）+ systemScore
- scripts/control-tower/generate-dashboard.py: 渲染前 freshness_check（数据源存在 + mtime<24h → 否则视图 degraded + 原因）
- .gitignore/.gitattributes: 信号+快照生成物不入 git（方案A）
- tests/control-tower/test_control_tower_truthfulness.py: 新建 — T1-T7 七个测试
- L1 恢复执行：重跑 check-gates-v2.py + self-diagnosis.py + emit-signal.py 重建 13 个信号

不做什么：
- 不改 src/server.ts（铁律 46 边界 — src/ 业务代码零改动）
- 不改 scripts/audit/check-gates-v2.py（已正确）
- 不改 views/agent_health.py 与 views/pipeline_health.py（views 模块化结构不动）
- 不做 schema 版本迁移兼容层（只有 2 个生成器，全量同步改）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：控制塔 /cockpit 仪表盘 + `python scripts/audit/self-diagnosis.py` CLI
处理（中间经过哪些步骤）：
  1. self-diagnosis.py / completion-engine.py 输出统一 schema 的 completion-scores.json
  2. generate-dashboard.py 渲染前 freshness_check 校验数据源新鲜度
  3. views/completion.py 读 systemScore + completionByCriteria 渲染
  4. L1 恢复：check-gates-v2.py 重新生成 17 门禁 gate-status.json，emit-signal.py 重建 13 个信号
结果（最终展示在哪）：
  - /cockpit V1-V5 无空数据、无假百分比；V2/V3 缺数据时显示 degraded + 原因而非旧数字
  - completion-scores.json 通过 validate_completion_schema() 校验，code_exists 通过率 ≥90%
  - 仓库无 "git 跟踪但工作区被删" 信号文件

## 本任务在哪一层:
工具链（scripts/ 控制塔，不属 L1-L5 业务层）

## Done 标准
- [ ] 入口可触达: `python scripts/audit/self-diagnosis.py` 运行成功，输出通过 validate_completion_schema()
- [ ] 链路走通: self-diagnosis → completion-scores.json → views/completion.py → generate-dashboard.py 全链路，7 个 pytest 全过
- [ ] 结果可见: /cockpit V1-V5 无假数据；DS1-DS8 全部达成；pre-commit 8 组通过；推送后 CI 通过
