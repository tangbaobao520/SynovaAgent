# D328 — commit 声明-内容一致性门禁（commit-msg-check.sh 消息 D# == 暂存认领 brief D#）

任务 ID: D328 | Agent: claude-code | 会话: 2026-08-10 | 依据: SYNOVA-IMPL-D328-commit声明内容一致性门禁-20260810.md

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
D320 劫持事故（RC-1）：`chore(D318)` 提交把 D320 的 8 个文件一起带走，G12（文件∈认领 brief 范围）与 commit-msg（仅格式校验）全部放行。根因 = 没有任何物理检查把"提交消息声明的 D#"与"暂存文件实际归属的 brief D#"绑定。本任务在 commit-msg hook 阶段追加一致性门禁：消息 scope D#（chore(D318): → D318）≠ 暂存文件认领 brief 的 D#（D320）→ 硬阻断 exit 1，拦截并行劫持。G12 职责是范围校验（不动），D# 一致性归 commit-msg（有消息上下文）。

### b) 文件审计
- scripts/commit-msg-check.sh（既有，~37 行）：格式校验（Conventional Commits）+ issue 引用 warning；Merge/Revert 先 exit 0——追加 D# 一致性检查点（格式通过后、exit 0 前）
- scripts/workflow/resolve-commit-brief.sh（D296 认领制，D317 自包含定位）：输入暂存文件列表（换行分隔）→ 输出认领 brief 绝对路径；只认今日 mtime brief；最终回退按日期前缀+parse_criteria——D329 独占写集，本任务只调用不修改
- scripts/control-tower/brief_parser.py：parse_q2 → {"include":[...]}、match_path(path,pat)=(^|/)pat$——GENUINE 检查（brief 真实认领 ≥1 暂存文件）防假阳性
- tests/control-tower/brief-parseable.test.sh：既有 shell 测试风格参照（set -euo pipefail + pass/fail/assert_exit）
- .git/hooks/commit-msg：已接线 `bash $(git rev-parse --show-toplevel)/scripts/commit-msg-check.sh "$1"`（现有）
- 关键设计决策：dev doc §3.2 参考代码用 `$(git rev-parse --show-toplevel)` 定位 resolver/brief_parser——临时 repo 测试场景会断（ROOT 无脚本）；按 D317 教训改用 BASH_SOURCE 自包含定位

### c) 决策
无既有 D# 一致性检查 → commit-msg-check.sh 追加（扩展既有门禁）。测试先行 6 用例 red→green（真实劫持场景 = red 基准）。resolve-commit-brief.sh/brief_parser.py 只调用不改（D329 写集）。

## Q1: 调研 — 决策链 + 执行约束

### a) 决策链
① SPEC — dev doc §3（写集/修复模式/不做的事）+ §4（测试要求）已批准
② 测试先行 — tests/control-tower/commit-msg-consistency.test.sh 新建 6 用例（临时 repo 隔离）：真实劫持场景（red 基准）、一致场景、消息无 D#、Merge 豁免、认领 brief 无 D# fail-open、无真实认领防假阳性
③ 实现 — commit-msg-check.sh 格式校验后追加：MSG_DID 解析 + STAGED_LIST + resolve-commit-brief 认领 + GENUINE 检查 + D# 比对 → 不一致/消息无 D# 认领有 D# → exit 1
④ 接线 — .git/hooks/commit-msg 已调 commit-msg-check.sh（现有接线不动）；grep 确认 commit-msg-check.sh 调用 resolve-commit-brief.sh
⑤ 验证 — DS1-DS7 全链
#CRITERIA: A

### b) 执行约束
- 铁律 0-2: spec（dev doc）→ test → impl → wire → review → merge
- 铁律 35: 能写 check-*.sh 的不靠 review — 本任务即自动化门禁本身
- 铁律 48: 新测试 ≥5 用例 × 真实断言（正常/降级/边界/劫持/豁免）
- D296 认领制: resolve-commit-brief 只认今日 mtime brief；跨天提交跳过检查（fail-open 不误伤）
- D317 自包含: resolver/brief_parser 定位用 BASH_SOURCE（临时 repo 测试隔离必需）
- D329 边界: 不改 resolve-commit-brief.sh/brief_parser.py/synova-commit/staging_guard/session_registry/VERSION.md
- verify 命令: `bash tests/control-tower/commit-msg-consistency.test.sh` 返回 exit 0

## Q2: 范围 — 正确的最简方案是什么？

做什么（严格按 dev doc §3.1 写集）：
- scripts/commit-msg-check.sh：修改。格式校验后追加 D# 一致性检查：MSG_DID（消息 scope）+ STAGED_LIST（git diff --cached）+ resolve-commit-brief.sh 认领 brief + GENUINE 真实认领确认 + CLAIM_DID 比对 → 两者都存在且不一致 → exit 1 硬阻断（明确提示）；消息无 D# 但认领 brief 有 D# → exit 1；Merge/Revert/无暂存/认领 brief 无 D# → 跳过（fail-open）
- tests/control-tower/commit-msg-consistency.test.sh：新建。6 用例（真实劫持 red 基准/一致/无 D#/Merge 豁免/无 D# brief fail-open/无真实认领防假阳性），临时 repo 隔离（mktemp + git init + 今日 mtime brief，文件名带日期前缀 + #CRITERIA 满足 resolver 最终回退）

不做什么（含文件路径）：
- 不改 scripts/workflow/resolve-commit-brief.sh（D329 独占写集；本任务只调用）
- 不改 scripts/control-tower/brief_parser.py（同源解析器，G12 语义对齐；本任务只 import 调用）
- 不改 .codex/control-tower/VERSION.md（版本编排 V4.7.1 由 D329 独占）
- 不改 scripts/pre-commit-check.sh（G12 职责是范围校验，D# 一致性归 commit-msg）
- 不改 scripts/control-tower/synova-commit.sh（D329 写集）
- 不改 scripts/control-tower/staging_guard.py（D329 写集）
- 不改 scripts/control-tower/session_registry.py（D329 写集）

## Q3: 验收 — 入口 → 交互 → 结果

入口：git commit 时 commit-msg hook → commit-msg-check.sh（消息文件 $1）
处理：格式校验通过 → 解析消息 scope D# → git diff --cached 取暂存文件 → resolve-commit-brief 认领 brief → GENUINE 真实认领确认 → CLAIM_DID 比对
结果：D# 不一致（劫持特征）→ exit 1 + 明确提示（提交被物理拦截）；一致/豁免路径 exit 0 放行；测试 6 用例全绿（真实劫持场景修复前 exit 0 → 修复后 exit 1 已证）

## 架构层: 基础设施
（commit-msg hook 门禁，L1-L5 之外；五层架构无涉）

## Done 标准
- [x] DS1 测试全过（≥5 用例；真实劫持场景修复前 exit 0 → 修复后 exit 1 已证 red→green）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS2 临时 repo stage c576e2b 的 8 个文件 + chore(D318) 消息 → 新门禁 exit 1（同类劫持物理拦截）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS3 一致提交（消息 D# == 认领 brief D#）不被误伤（测试用例 2）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS4 Merge/Revert/无暂存/无 D# 四条豁免路径全部 exit 0（测试覆盖）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS5 VERSION.md 不碰（V4.7.1 由 D329 独占）— verify: git diff VERSION.md | wc -l
- [x] DS6 全量审计与基线一致（439 FAIL）+ as any=0 — verify: python scripts/audit/audit-check.py --full
- [x] DS7 真实提交环境 12 组 pre-commit 全过、无 --no-verify、git diff --name-only 与写集一致 — verify: bash scripts/pre-commit-check.sh
