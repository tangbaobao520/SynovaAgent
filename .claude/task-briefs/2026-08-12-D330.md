# D330 — D328 审计 P1 修复（python 损坏探测 + 豁免测试补全 + 文档回填）

任务 ID: D330 | Agent: claude-code | 会话: prompt-architecture (2026-08-12) | 依据: SYNOVA-IMPL-D330-D328审计P1修复-20260812.md

> 注: 本 brief 原文件名为 2026-08-12-auto.md, 被并行 D331 会话覆盖占用（D331 为 KIMI K3 对 D329 的审计修复, 写集与本任务零交集）。按 D329 session 专属 current-brief 机制改用独立文件名 2026-08-12-D330.md + .claude/current-brief.prompt-architecture。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
KIMI K3 首审（2026-08-12）物理复现 3 个 P1：①PYBIN 探测只用 `command -v` 验存在性——Windows Store stub/损坏 shim 存在但执行即败 → GENUINE 静默归 0 → 劫持漏拦（当前树 shim 实测 2/6 败，违反铁律 24/31）；②DS4 声称"四条豁免全部测试覆盖"但 Revert/无暂存无用例；③bypass.log 空窗 → DS7 无物理证据（P1-3 归 CT 队列，本任务不做）。修复点在 commit-msg-check.sh（D329 折入 PYBIN 后的残留缺口）+ 测试补全 + 文档回填。

### b) 文件审计
- scripts/commit-msg-check.sh（D328 交付 + D329 PYBIN 折入）：PYBIN 探测仅 command -v；GENUINE `|| echo 0` 吞执行失败 → 本任务改造：可用性验证 + GENUINE 三态（degraded 显式提示）
- tests/control-tower/commit-msg-consistency.test.sh（6 用例）：缺 Revert/无暂存/broken-shim；用例 1 stage 7 文件声称 8 文件（P2-2 漏 `.claude/task-briefs/D320-dashboard-gitify.md`）→ 本任务补 4 用例 + 修正用例 1
- docs/plans/codex/implementation/SYNOVA-IMPL-D328-commit声明内容一致性门禁-20260810.md：§3.2 仍裸 python3（P2-1 未回填 D329 折入的 PYBIN）+ DS4 措辞过度（P1-2）→ 回填 + 修正
- .claude/task-briefs/2026-08-10-auto.md：P2-5 引用 `synova-commit.sh`，实际文件 `synova-commit` 无扩展名（git ls-files 确认）→ 笔误修正
- .codex/control-tower/VERSION.md：V4.7.1 已存在（D329）→ 追加 V4.7.2（PATCH，本任务独占版本编排）

### c) 决策
KIMI 复现已物理确认（当前树 2/6 败）→ 直接修复。broken-shim = 新 red 基准。测试先行（dev doc §4 表）→ 实现（§3.2 三态语义）→ 文档回填 → V4.7.2 → DS1-DS10（含推送+CI——D328/D329 未推送教训，dev doc DS10 恢复该 DS）。

## Q1: 调研 — 决策链 + 执行约束

### a) 决策链
① SPEC — D330 dev doc 已批准（§3.1 写集 5 文件 + §3.2 修复模式 + §4 测试表）
② 测试先行 — 测试文件补 4 用例（Revert/无暂存/broken-shim degraded/broken-shim 劫持）+ 用例 1 补 8 文件；broken-shim 用例修复前失败（red 已证: 10 过 2 败）
③ 实现 — commit-msg-check.sh：PYBIN 探测加可用性验证（`command -v && "$_c" -c "import sys"`）；GENUINE 三态（rc≠0 → 显式 degraded 提示，不静默）
④ 文档 — D328 dev doc §3.2 回填 PYBIN 最终实现 + DS4 措辞修正；D328 brief 路径笔误
⑤ 验证 — DS1-DS10 全链
#CRITERIA: A

### b) 执行约束
- 铁律 0-2: spec（dev doc）→ test → impl → wire → review → merge
- 铁律 24/31: 降级必须显式提示——本次修复核心（不再 `|| echo 0` 静默吞执行失败）
- 铁律 48: 测试非空壳 + 断言真实（broken-shim 断言输出含 degraded 提示）
- 三态语义（dev doc §3.2）: 输出 0 = 无真实认领（G12 兜底）；输出 1 = 有真实认领（比较 D#）；执行失败 rc≠0 = degraded 显式提示
- set -euo pipefail 兼容: GENUINE 失败须捕获 rc 不触发 set -e（`|| GENUINE_RC=$?` 同行 fallback）
- swallow-ok 门禁: 2>/dev/null 须同行 fallback 或注释
- D330 独占 V4.7.2 版本编排；resolve-commit-brief.sh/brief_parser.py 只调用不改
- 并行协调: D331（KIMI K3 对 D329 审计修复）并行进行，写集零交集（D331 不改 commit-msg-check.sh 并已声明）；全局 current-brief 被 D331 占用 → 本任务用 session 专属 current-brief.prompt-architecture
- verify 命令: `bash tests/control-tower/commit-msg-consistency.test.sh` 返回 exit 0

## Q2: 范围 — 正确的最简方案是什么？

做什么（严格按 dev doc §3.1 写集）：
- scripts/commit-msg-check.sh：修改。PYBIN 探测加可用性验证（command -v && "$_c" -c "import sys"），全无/损坏 → 显式 degraded 提示；GENUINE 捕获执行 rc，非 0 → 显式 degraded 提示（fail-open 可追溯，不静默）
- tests/control-tower/commit-msg-consistency.test.sh：修改。新增 Revert 豁免用例、无暂存 fail-open 用例、broken-shim degraded 用例（red 基准）、broken-shim 劫持拦截用例；用例 1 补 stage `.claude/task-briefs/D320-dashboard-gitify.md`（8 文件与声明一致）
- docs/plans/codex/implementation/SYNOVA-IMPL-D328-commit声明内容一致性门禁-20260810.md：修改。DS4 措辞修正（"Merge/无 D# 测试覆盖 + Revert/无暂存手动实测"）；§3.2 回填 PYBIN 最终实现（无裸 python3 片段）
- .claude/task-briefs/2026-08-10-auto.md：修改。P2-5 路径笔误：`synova-commit.sh` → `synova-commit`
- .codex/control-tower/VERSION.md：修改。追加 V4.7.2（PATCH，D330 D328 审计 P1 修复）

不做什么（含文件路径）：
- 不改 scripts/workflow/resolve-commit-brief.sh（只调用）
- 不改 scripts/control-tower/brief_parser.py（只调用）
- 不改 scripts/pre-commit-check.sh（G12 职责不变）
- 不改 scripts/control-tower/synova-commit（D329/D331 写集）
- 不改 scripts/control-tower/staging_guard.py（D329/D331 写集）
- 不改 scripts/control-tower/session_registry.py（D329/D331 写集）
- 不改 scripts/control-tower/check-bypass-log.sh 及 D331 写集任何文件
- 不改 .claude/task-briefs/2026-08-12-auto.md（D331 占用的 brief 文件）
- 不做三态 gate 输出全局改造 / bypass.log 对账 / DS verify 覆盖映射（L4 #1/#2/#3 归 CT 队列，dev doc §3.3）

## Q3: 验收 — 入口 → 交互 → 结果

入口：git commit 时 commit-msg hook → commit-msg-check.sh（消息文件 $1）
处理：格式校验通过 → MSG_DID 解析 → PYBIN 可用性探测（存在+可执行）→ 暂存列表 → resolver 认领 brief → GENUINE 三态判定（1 比较 D# / 0 跳过 / rc≠0 degraded 显式提示）
结果：python 损坏时不再静默漏拦（显式提示可追溯）；劫持仍硬阻断；Revert/无暂存豁免有测试用例；文档与实现一致；V4.7.2 bump 同 commit；推送 + CI 全绿

## 架构层: 基础设施
（commit-msg hook 门禁修复，L1-L5 之外；五层架构无涉）

## Done 标准
- [x] DS1 测试 10 用例全过（broken-shim red→green 已证：red 10 过 2 败；Revert/无暂存用例存在）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS2 broken-shim 注入 PATH → 输出含"不可用或损坏/degraded"提示（grep 断言）— verify: bash tests/control-tower/commit-msg-consistency.test.sh
- [x] DS5 brief 路径笔误零残留 — verify: grep -c "synova-commit.sh" .claude/task-briefs/2026-08-10-auto.md
- [x] DS6 §3.2 含 PYBIN 无裸 python3 — verify: grep -c "PYBIN" docs/plans/codex/implementation/SYNOVA-IMPL-D328-commit声明内容一致性门禁-20260810.md
- [x] DS7 VERSION.md 含 V4.7.2 + version.log 追加 — verify: grep -c "V4.7.2" .codex/control-tower/VERSION.md
- [x] DS8 全量审计与基线一致（439 FAIL）+ as any=0 — verify: python scripts/audit/audit-check.py --full
- [x] DS9 12 组 pre-commit 全过、无 --no-verify、写集一致 — verify: bash scripts/pre-commit-check.sh
- [x] DS10 推送后 origin..HEAD 为空 + CI 逐 job 全绿（npm audit/Architecture 预存失败单独标注）— verify: git log origin/feat/prompt-architecture..HEAD
