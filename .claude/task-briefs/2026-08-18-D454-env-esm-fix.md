# Task Brief: D454 环境崩溃修复（require→ESM import）

> 生成: 2026-08-18 | 分支: fix/d355-cash-contract | 角色: DeepSeek Harness (Mac)
> 依据: 创始人授权「继续攻坚」D454 + GS-03 转绿前置

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
GS-03 转绿三层阻塞之「环境崩溃」第一道坎：服务启动时 schema 迁移失败
（`require is not defined`，ESM 下 require 未定义 → 迁移被跳过 → 数据库 schema 可能漂移）。

### b) 文件审计（grep 实测）
- src/init/engine-context.ts:133 用 `require('../store/schema-migration')`（ESM 下未定义）
- schema-migration.ts 是 ESM export（`export function reconcileSchema`），无循环依赖
- initEngineContext(): void 是同步函数 → 需静态 import，不能用 await import

### c) 决策（D333）
参考：第一性原理（ESM 下 require 未定义是根因，静态 import 即修复）+ Anthropic（最小机制：
一处 import + 删除 require 行）。结论：改静态 import。

## Q1: 调研
铁律 47/48。修复 = 静态 import reconcileSchema + 删除 require 行。

## Q2: 范围 — 最简方案

做什么：
- src/init/engine-context.ts

不做什么：
- 不改 src/services/update-checker.ts（v18 path 问题，另一 bug）
- 不改 scripts/audit/audit-check.py（K3 红线）

## Q3: 验收 — 入口→交互→结果

入口：服务启动 initEngineContext()
处理：静态 import reconcileSchema → 迁移执行
结果：日志「Schema 迁移失败」消失（实测 0 次）

## 架构层: 基础设施

#CRITERIA: A

## Done 标准
- [ ] grep "Schema 迁移失败" 启动日志 = 0 次
- [ ] npx tsc --noEmit 无 engine-context.ts 新增错误
