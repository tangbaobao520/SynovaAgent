# Task Brief: D455 修复 D355 残留 — cashBalance↔cash 对齐 + filter bug

> 生成: 2026-08-18 | 分支: fix/d355-cash-contract | 角色: DeepSeek Harness (Mac)
> 依据: 创始人授权（08-18「你来修复 D355」）+ D355 99fa8df5 验收不完整

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
D355（99fa8df5）已入 main 但不完整：把 erp-standard `现金余额→cash` 改成 `→cashBalance`
（camelCase），却未同步 financial.json schema（仍是 `cash`），导致注入时 `cashBalance`
被 ingestBatch 字段校验跳过；且 compute filter bug `{[teamId]:teamId}` 未修。

### b) 文件审计（grep 实测）
- financial.json（本体真相源）: `cash` / `operating_expense` / `receivables`（snake_case）
- erp-standard.json: `现金余额→cashBalance`（camel，D355 引入的不一致）
- compute-cash-runway-months.ts:60 filter `{[teamId]:teamId}` + :62 读 `cashBalance`
- compute-receivable-overdue-rate.ts:46/48/49 同款 bug
- financing-constraint/aggregate.ts:28 `cash || cashBalance`

### c) 决策（D333，K3 可核）
参考：第一性原理（本体 schema financial.json 是属性名唯一真相源，field-mapping + compute
必须对齐它）+ Anthropic（fail-closed：注入层字段校验以 schema 白名单为准，错名即跳过）。
结论：统一 snake_case（cash/operating_expense/receivables），filter 改为无 filter（Financial
节点无 teamId 属性）。

## Q1: 调研 — 决策链 + 历史教训
引用铁律 9（关键变更 grep 全仓传播）/47/48。D355 曾误锚「compute 读侧 camelCase」为契约锚点，
本修复改锚「本体 schema snake_case」。filter bug 是 M3 同型（机制建成但 filter 永不匹配）。

## Q2: 范围 — 最简方案

做什么：
- extensions/ontology/field-mappings/erp-standard.json
- extensions/sentinels/cash-runway/computes/compute-cash-runway-months.ts
- extensions/sentinels/cash-runway/computes/compute-receivable-overdue-rate.ts
- extensions/sentinels/financing-constraint/aggregate.ts
- tests/sentinels/cash-runway/compute-cash-runway-months.test.ts
- tests/sentinel/sentinel-threshold-wiring.test.ts
- tests/contract/l4-contract.test.ts
- task-state/D455.json

不做什么：
- 不改 src/init/engine-context.ts（require-ESM 属 D454，另一任务）
- 不改 extensions/sentinels/_extinct/（已弃用，非本轮）
- 不改 money.json（Money 节点的 cash_balance 属 traversal 路径，独立契约）
- 不改 scripts/audit/audit-check.py（K3 红线）

## Q3: 验收 — 入口→交互→结果

入口：POST /api/data/upload（erp-standard 注入）→ cash-runway 哨兵
处理：注入写 `cash`（不再被跳过）→ compute 读 `cash`（filter 命中 Financial 节点）
结果：低现金触发 critical 阈值（GS-03 断言 2 转绿前置）

## 架构层: L4（本体契约）+ L3（compute）

#CRITERIA: A

## Done 标准
- [ ] grep 零残留 cashBalance/operatingExpenses/accountsReceivable（extensions 非 _extinct）
- [ ] npx vitest run 4 个相关测试全绿（24 用例）
- [ ] npx tsc --noEmit 无本次文件新增错误
