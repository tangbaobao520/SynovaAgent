# Task Brief: D445: GS-05 告警闭环场景脚本 — 越阈 fixture → sentinel_tickets 有行 + 推送去重键稳定（S0-3）

> 生成: 2026-08-19 21:10:33 | 分支: feat/d445-gs05-alert-loop | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。L1 交互层不知道 L4 用什么数据库，L3 洞察层不知道 L5 数据存在哪。换底层存储，上层零改动。pre-commit 物理阻断跨层 import——L2→L4 的代码提交不进去。

**横向解耦：11 个独立 Monorepo 包**
五层内部拆为独立包：@synova/sog-core（本体图类型）、@synova/sentinel-engine（哨兵调度）、@synova/expert-platform（专家加载）、@synova/connector-registry（数据连接器）。每个包接口边界明确，拆卸一个不影响其余 19 个。核心包已落地运行；已存在的功能规划从 src/ 迁移到独立包；未来新增须遵循此结构。

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码：
- 新 AI 专家 = 新建目录 + 10 个 Markdown 文件 → 自动注册到 ExpertDispatcher
- 新诊断哨兵 = 加 xxx-sentinel.ts → builtins 自动扫描加载
- 新行业 = 加行业目录（基准数据+阈值+案例库）→ 1-2 天上线，零 TypeScript 改动
- 新本体实体类型 = 加 JSON Schema 文件

流程约束: V4.5.0 — task brief 6 字段强制 + 免疫系统 + plan.json + 8 组物理阻断 + Plan-Actual 闭合 + engine-core 清零 + 时间戳顺序检查。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层
        Sentinel Finding[] → 诊断引擎 Phase 2 → 8 位文件驱动专家解读

L1 入口: POST /api/diagnosis/consult (GA诊断) / Cron→Sentinel.check() (哨兵) / GET /chat (Web) / MCP
五层架构 (只能向下依赖相邻层):
  L1 交互: routes/ tui/ mcp/
  L2 编排: agent/ orchestrator/
  L3 洞察: l3/ sentinel/ expert-platform/ expert/ (8位文件驱动专家: strategy org finance tech marketing action business_model knowledge)
  L4 本体: l4/ evidence/ 企业事实层: AgentMemoryStore (enterprise_fact, 版本化+superseded_by链)
  L5 存储: store/ cron/
三层粒度: 专家→哨兵→计算。哨兵=可独立告警的最小子领域。compute=纯数学函数。
L0 进化: evolution/ 两路反馈→候选池→确认/执行验证→写入知识库
文件化扩展: expert/ knowledge/shared/ theory/ skills/ — 新增=加文件,不改代码
数据安全: L0公开摘要→L1聚合信号→L2脱敏证据→L3原始数据(仅客户内Agent可见,GA不可见)
引擎: packages/engine-core/ (Novis遗产,逐步迁移)。禁止src/新增engine-core引用(铁律46)。
安全: security/ (PIIScrubber, DataBoundary)
LLM: providers/ (DeepSeek, OpenAI, Gateway)

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
- [x] 扩展（脚本场景，不改 TypeScript 产品代码）
本任务属于**哨兵系统的验收证据层**：为产品进度页（S0-3 告警闭环验收点）提供机器可执行的黄金场景脚本。不改任何 src/ 产品代码——只新增 `scripts/golden-scenarios/GS-05-alert-closure/` 场景目录（run.sh + fixtures + expect.json + README），复用 D361 common/ 四工具（bootstrap/fresh-db/inject/assert）。

### b) 文件审计
- `scripts/golden-scenarios/README.md`（运行契约 8 条）— 遵守
- `scripts/golden-scenarios/common/`（bootstrap/fresh-db/inject/assert）— 复用，零修改
- `scripts/golden-scenarios/GS-03-capital-cycle/`（同构先例）— 模板参照
- `src/routes/sentinel.ts`（POST /api/sentinel/run/:id + GET /api/sentinel/tickets）— 只调用
- `src/agent/sentinel-service.ts`（runSentinelOnce，D453 已修 db:undefined）— 只调用
- `src/sentinel/runner.ts`（emergency/critical → INSERT OR REPLACE sentinel_tickets；D6 通知去重 sentinelId 10min）— 只读
- `src/agent/proactive-push.ts`（D272 推送去重键 = finding.id）— 只读
- `extensions/sentinels/financing-constraint/computes/cash-runway`（越阈哨兵）— 只调用
关系: 全部复用，零修改产品代码（grep 实测 2026-08-19）。

### c) 决策
无冲突；复用 GS-03 同构模式 + 同一越阈 fixture（erp-low-cash → cash-runway 越阈 critical）。多选项取舍（触发/断言/去重键/fixture 四决策点）走 DECISION-REFERENCE 四步，结论见 Q1c。

## 注入上下文
### DECISION-REFERENCE

> D333 决策参考框架全文（创始人 2026-08-13 定）:

# 决策参考框架（双参考系）

> 2026-08-13 创始人定 | 用途：遇到难决策/多选项/最佳实践选择时，强制走四步参考，并记录所用参考系
> 触发条件：①多选项需取舍 ②设计/架构方案选择 ③优先级排序 ④"最佳实践是什么"类问题 ⑤实现与文档声称冲突时

## 四步框架

```
① 第一性原理（DeepSeek/梁文峰）：这个问题的最简本质是什么？最少机制能解决吗？
② Anthropic 工程基线：隔离/失败即关闭/脚本验证/机器可验契约——哪条适用？
③ 开源实证（DeepSeek）：有可克隆的代码/架构参考吗？clone 下来看实际做法（成本/效率/结构）
④ 收敛检查：两参考系是否指向同一答案？收敛 = 大概率正确；分歧 = 值得深挖
```

## 双参考系边界

| 参考系 | 适用 | 不适用 |
|--------|------|--------|
| **Anthropic 工程实践** | agent 隔离、门禁/fail-closed、脚本化验证、机器可验契约、并行协作 | 成本/产品定位/模型选择 |
| **DeepSeek 第一性原理 + 开源实证** | 产品哲学、成本/效率/架构取舍、反内卷、开源参考（clone 仓库） | 工程流程细节（其仓库是模型/推理代码，非 agent 协作） |

## 梁文峰原则摘要（DeepSeek 参考时使用）

- **第一性原理**：不做无意义的炫技，回到问题本质
- **极致成本**：能用最少机制解决就不用多的（这正好支持"worktree 隔离 = 最少机制"而非 N 个门禁）
- **开源开放**：能参考开源实证就不闭门造车
- **反内卷**：机制是为了减少摩擦，不是为了增加流程

## 记录要求（可验证，不靠记忆）

- Codex 决策：在 dev doc / 本会话回复中**明确写"参考：Anthropic/DeepSeek/第一性原理 + 结论"**
- Claude Code 决策：dev doc 要求完成报告含**决策记录**（决策点 + 参考系 + 理由），K3 审计可核

## 已用案例

| 日期 | 决策 | 参考系 | 结论 |
|------|------|--------|------|
| 2026-08-13 | 并行 agent 冲突（串行 vs 并行） | Anthropic（隔离基线）+ DeepSeek（最少机制） | 收敛：worktree 隔离（D307）优先解锁并行 |


## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
本任务按以下顺序执行：
  ① Done 标准（本 brief）—「怎么算做完」已定义
  ② 脚本骨架先行 — run.sh + expect.json（断言先于跑通，铁律 0-2）
  ③ 本地跑通 — fresh-db/bootstrap/inject/trigger/assert 全链路
  ④ 接线 — evidence JSON 落盘（calc-progress 消费，S0-3 验收点证据）
  ⑤ 验证 — 断言机器判定 exit 0/1 + 证据入库

引用依据：
  - 铁律 0-2: spec → test → impl → wire（脚本 = 断言先行）
  - 铁律 48: 断言不可为空壳（每条带 purpose，缺 purpose 引擎拒绝执行）
  - GSS README 断言规范: ≥3 条（正常 + 降级 + ≥1 负向）；真空零显式声明（==0）
  - memory 教训: memory/notes/archived/stub-implementation-pattern.md（把"没实现"包装成"不需要"——诚实 RED 先例）

### b) 本任务执行约束（全部带 verify 命令）
  - rule: "断言只认产品物理输出（HTTP 响应/sqlite 表行/文件），机器判定 exit 0/1"
    verify: "grep -cE 'purpose' scripts/golden-scenarios/GS-05-alert-closure/expect.json >= 3"
  - rule: "负向断言必须存在（真空零显式声明）"
    verify: "grep -cE 'notContains|==0' scripts/golden-scenarios/GS-05-alert-closure/expect.json >= 1"
  - rule: "临时库隔离 + 测后清理（fresh-db + trap）"
    verify: "grep -n 'fresh-db.ts' scripts/golden-scenarios/GS-05-alert-closure/run.sh && grep -n 'trap' scripts/golden-scenarios/GS-05-alert-closure/run.sh"

### c) 决策参考系（四决策点，双参考系收敛）
  - 触发通道（A 直接调 runner / **B POST /api/sentinel/run/:id 真实路由**）→ **B**：铁律 12（集成 cover 真实路由）+ GSS 契约（产品物理输出）；D453 已修 runSentinelOnce db:undefined
  - 断言通道（A 仅 HTTP / **B HTTP + sqlite 临时库表行数**）→ **B**：S0-3「sentinel_tickets 有行」须直查表行数（rows >= 1），去重稳定性须计数对比；API 响应用于负向/存在性
  - 去重键断言（A 预测 ticket id / **B 同窗口二次触发计数不变**）→ **B**：runner INSERT OR REPLACE（ticket-{signalId}-{expertType}）+ D6 去重窗口的物理可观察结果；信号 id 动态不可静态预测
  - fixture（A 新建行业 fixture / **B 复用 GS-03 erp-low-cash → cash-runway**）→ **B**：最少机制（同一领域同一哨兵，现金 3 万/月耗 12 万 → runway 0.25 < critical 6）
记录格式: 参考：Anthropic（真实路由/物理输出/机器可验）+ 第一性原理（最少机制复用）+ 结论：真实路由触发 + 表行数断言 + 二次触发计数 + 复用 fixture

### d) 相关 Note 引用
- [x] memory/notes/implemented/2026-08-19-d445-gs05-alert-loop.md（本任务决策已沉淀 Note，四字段头与目录状态一致）

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- scripts/golden-scenarios/GS-05-alert-closure/run.sh: 场景执行（fresh-db → bootstrap → 空库触发（负向基线）→ 注入越阈 fixture → 触发#1 → 触发#2 去重 → assert → 证据落盘 + trap 清理）
- scripts/golden-scenarios/GS-05-alert-closure/expect.json: ≥3 断言（ticket-created sqlite rows>=1 / dedup-stable rows==1 / no-false-ticket-empty 负向）
- scripts/golden-scenarios/GS-05-alert-closure/fixtures/erp-low-cash.json: 越阈数据（现金 3 万/月耗 12 万 → runway 0.25 < critical 6）
- scripts/golden-scenarios/GS-05-alert-closure/README.md: 场景说明 + C线标准 S0-3 + 当前状态（诚实实测）
- scripts/golden-scenarios/evidence/GS-05-2026-08-19.json: 场景运行证据（calc-progress 消费，git 跟踪）
- task-state/D445.json: impl 段回填 + status → impl_done

不做什么：
- 不改 src/routes/sentinel.ts — 触发/tickets API 只调用不修改
- 不改 scripts/golden-scenarios/common/ — D361 基建领地
- 不改 scripts/golden-scenarios/GS-03-capital-cycle/ — D442 领地
- 不改 scripts/audit/ — 审计红线（K3 专属）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：`bash scripts/golden-scenarios/GS-05-alert-closure/run.sh`（一行自举，K3 R2）
处理（中间经过哪些步骤）：fresh-db 隔离临时库 → bootstrap 临时端口起服务 + healthz → 空库触发 cash-runway（负向基线采样）→ 注入 erp-low-cash 越阈 fixture → 触发#1（越阈 → 告警 → 工单）→ 触发#2（同窗口去重键稳定）→ assert.ts 逐条断言 → evidence JSON 落盘
结果（最终展示在哪）：exit 0/1 机器判定 + `scripts/golden-scenarios/evidence/GS-05-<date>.json`（calc-progress 消费 → 产品进度页 S0-3 验收点证据）

## 架构层: 扩展层
scripts/golden-scenarios/ 证据工厂（GSS）——不改产品代码，只调 L1 API（/api/sentinel/run、/api/sentinel/tickets）+ 读 L5 临时库（fresh-db 产物 synova.db）。
#CRITERIA: A
<!-- #CRITERIA: A/B/C/D 条件归属（v3-FINAL），必填；pre-commit G10 + hook-block-write CP1 + pre-doc-audit CP2 消费 -->

## Done 标准
- [x] verify: bash scripts/golden-scenarios/GS-05-alert-closure/run.sh 可执行（exit 0/1 机器判定，不依赖人工）
- [x] verify: grep -c "purpose" scripts/golden-scenarios/GS-05-alert-closure/expect.json >= 3（断言非空壳，铁律 48）
- [x] verify: grep -cE "notContains|==0" scripts/golden-scenarios/GS-05-alert-closure/expect.json >= 1（负向断言）
- [x] verify: grep -n "sentinel_tickets" scripts/golden-scenarios/GS-05-alert-closure/expect.json（S0-3 表行数断言）
- [x] verify: grep -n "fresh-db.ts" scripts/golden-scenarios/GS-05-alert-closure/run.sh && grep -n "trap" run.sh（临时库隔离 + 清理）
- [x] verify: ls scripts/golden-scenarios/evidence/GS-05-*.json（证据落盘，calc-progress 消费）
- [x] verify: git diff --name-only 仅限 scripts/golden-scenarios/GS-05-alert-closure/ + task-state/D445.json + brief/note（写集一致）
