# Task Brief: packages/ as any 清理 + 铁律 38 审计测试扩围（D471, K3 P1-C1）

> 生成: 2026-08-22 | 分支: feat/win-d471-packages-as-any-cleanup | 权威 dev doc: docs/plans/codex/implementation/SYNOVA-IMPL-D471-packages-as-any-cleanup-20260822.md

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
诊断是手段，增长才是目的。核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务 = K3 审计 P1-C1 整改：铁律 38「as any 零容忍」声称失效——组 1 门禁只扫 src/，packages/ 实测 33 处生产 as any 无门禁。
触及横向解耦 packages/ 基础设施（层归属 L1：^packages/），三个包：@synova/sog-core（本体 schema/registry，类型守卫层）、@synova/connector-registry（连接器注册）、@synova/test-kit（架构审计测试）。
动作 = 修改既有代码（纯类型替换，零行为变化）+ 测试扩围；不新增任何生产接口、不新增模块、不改门禁脚本。

### b) 文件审计
grep 实测（2026-08-22）: rg -n "as any" packages --glob "*.ts" → sog-core-schema.ts 24 处（L332/346/350-434，全部类型守卫函数）、sog-schema-registry.ts 8 处（L57/61/62/69/73/74/105/106）、connector-registry/registry.ts:121 1 处、e2e 测试 2 处（L25/34）。
消费者核实: NODE_VALIDATORS/EDGE_VALIDATORS 由 packages/sog-core/src/sog-sdk.ts（truthiness 消费）+ sog-core 测试 + index.ts 重导出消费；registerNodeType/registerEdgeType 全仓库零调用方；ToolRegistryInterface.register 期望 handler 返回 Promise<Record<string, unknown>>（connector-registry/src/types.ts:86-94，executeTool 返回 Promise<unknown> 是 as any 根因）。
关系: 复用现有类型守卫结构做纯类型替换，无新建、无冲突。

### c) 决策
无冲突。决策点已在 dev doc §4.5 记录（决策参考: 第一性原理 + Anthropic + 结论）: ①动态 key 用 Record<string, unknown>、枚举字段用内联类型；②审计扩围放 test-kit 测试，不动 scripts/ 门禁（DSH 地盘）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC: dev doc SYNOVA-IMPL-D471 §6 DS1-DS7（可验证物理证据）→ ② 测试: RED 先行（扩围 + expect 断言，未清理时必失败 35 处）→ ③ 实现: 纯类型替换 → ④ 接线: 审计测试=门禁消费方（测试即接线，无新生产 export）→ ⑤ 验证: 自检 6 问。
引用铁律: 0-2（spec→test→impl→wire）; 38（as any 零容忍，官方替代 Record<string, unknown>/内联类型）; 47/48（契约优先 + 测试不可空壳——审计测试现只有 console.warn 无断言，违反 48）。
memory/ 引用: engine-core-split-fraud.md（声称必须 grep 物理证明）; 2026-08-16-d355-l4-contract.md（staged 状态才是零差铁证）。

### b) 本任务执行约束
- rule: 清理后 packages/ 生产代码 as any 零命中（.d.ts/.test.ts/注释除外）
  verify: rg -n "as any" packages --glob "*.ts"
- rule: 审计测试必须有 expect(violations).toEqual([]) 断言且扫描根含 packages/
  verify: grep -n "packages" packages/test-kit/tests/architecture/05-as-any-audit.test.ts
- rule: 根 tsc 错误数零新增（基线 28 = 28）
  verify: npx tsc --noEmit 2>&1 | grep -c "error TS"

### c) 决策参考系
参考：Anthropic/DeepSeek/第一性原理 + 结论（详见 dev doc §4.5 两决策点，已收敛）。
第 3 决策（实现期间新增）: sog-core/connector-registry 包测试套件基线坏 import（自 e9100e96: sog-core 测试 '../sog-core-schema' 应为 '../src/sog-core-schema'; connector-registry 测试 '../src/connectors/registry' 应为 '../src/registry'）→ DS4 回归根本无法运行。参考：第一性原理（回归套件必须能跑起才谈零回归，改 import 两行是最少机制）→ 结论: 前置修复两行 import 并回填 dev doc §3.2。

### d) 相关 Note 引用
- [ ] memory/notes/implemented/2026-08-22-d471-packages-as-any-cleanup.md（交付后归档，本会话创建）

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- packages/sog-core/src/sog-core-schema.ts — 24 处 as any → Record<string, unknown> + 内联类型，零行为变化
- packages/sog-core/src/sog-schema-registry.ts — 8 处 as any → 枚举语义化转换 + Record 索引类型
- packages/connector-registry/src/registry.ts — L121 as any → handler 返回内联类型转换
- packages/test-kit/tests/e2e/02-expert-contribution-journey.test.ts — 2 处 res.json() as any → 内联响应类型
- packages/test-kit/tests/architecture/05-as-any-audit.test.ts — 扫描根扩 src/+packages/ + expect 断言
- packages/sog-core/tests/sog-core-schema.test.ts — 前置修复坏 import（../src/sog-core-schema）
- packages/connector-registry/tests/connector-registry.test.ts — 前置修复坏 import（../src/registry）
- docs/plans/codex/implementation/SYNOVA-IMPL-D471-packages-as-any-cleanup-20260822.md — §3.1 写集表 + §3.2 同 commit 回填最终实现形态（S-6，8 个偏离点）
- memory/notes/implemented/2026-08-22-d471-packages-as-any-cleanup.md — 交付笔记归档（Q1d 承诺，第二个 commit）

不做什么：
- 不修改 scripts/pre-commit-check.sh（门禁扩围归 DSH 排期，本任务只在 test-kit 测试侧闭环）
- 不修改 src/ 任何文件（src/ 已零 as any，D470 写集）
- 不修改 extensions/ontology/（D470 写集）
- 不重写 sog-core 校验语义（纯类型替换，行为逐字节不变）
- 不修改 packages/sog-core/src/sog-sdk.ts（其 catch err: any 是 : any 注解非 as any，超出本任务审计正则范围）

## Q3: 验收 — 入口 → 交互 → 结果

入口（从哪触发）: 开发者提交 packages/ 代码 → pre-commit 全量门禁 → test-kit 架构审计测试作为铁律 38 的测试侧闭环；vitest 直接运行审计测试文件（cd packages/test-kit && npx vitest run tests/architecture/05-as-any-audit.test.ts）。
处理: 审计测试递归扫描 src/ + packages/ 生产 .ts（排除 .d.ts/.test.ts/node_modules/注释行）→ 正则 /\bas\s+any\b/ 逐行匹配 → 违规行入 violations 数组。
结果: expect(violations).toEqual([]) 断言——清理前 RED（35 处命中失败），清理后 GREEN；审计失败在 vitest 输出中可见 file:line 清单。

## 架构层: L1（packages/ 横向包，层映射 ^packages/）
#CRITERIA: A

## Done 标准
- [x] DS1 清理完成 verify: rg -n "as any" packages --glob "*.ts" 生产代码命中 0（仅注释/.d.ts/.test.ts 字符串）— 实测 2026-08-22 命中 0（剩余为审计测试 fixture 字符串 + security-scanners.ts:31 注释行）
- [x] DS3 审计测试全绿 verify: cd packages/test-kit && npx vitest run tests/architecture/05-as-any-audit.test.ts — 实测 3/3 pass（RED 时 33 处失败 → 清理后 0）
- [x] DS4 零回归 verify: 三包 vitest 无新增失败 + npx tsc --noEmit 28=28 — 实测 sog-core 67/67、connector-registry 7/7、test-kit 16=16（基线既有）零新增、根 tsc 28=28

## Q4: 历史教训（memory/ 引用）
- memory/2026-08-05-D313-D315-closeout-session.md: current-brief 须完整文件名（含 .md）；brief 日期前缀才被 G12 认领
- memory/2026-08-11-D329-session-identity.md: 排除项路径须紧跟动词；并行会话用 worktree 隔离
- memory/engine-core-split-fraud.md: dev doc 声称≠实现——基线必须实测（本次实测发现两包测试套件坏 import，已前置修复并回填 dev doc）
- memory/2026-08-18-D358-deextinct-delivery.md: hook 12 需字面「增长导航」+ 反敷衍词；提交前手跑 hook 自测

## 文档引用
- docs/plans/codex/implementation/SYNOVA-IMPL-D471-packages-as-any-cleanup-20260822.md §1-§8（权威 dev doc，写集/DS/测试要求）
- docs/synova/research/AUTHORITY-DEVIATION-REGISTRY-v2.md P1-C1（审计缺陷源头）
- docs/synova/coordination/TASK-ROUTING.md（packages/ = Win Claude 线）

## 接口审计
- packages/sog-core/src/sog-core-schema.ts: hasString
- packages/sog-core/src/sog-core-schema.ts: hasTeamType
- packages/sog-core/src/sog-schema-registry.ts: registerNodeType
- packages/sog-core/src/sog-sdk.ts: validateSOGNode
- packages/connector-registry/src/registry.ts: ConnectorRegistry.register
