# Task Brief: L1-C 切片 C（D527 首诊旅程端到端 + D528 升级不丢数据）

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。本任务在 L1 交互层（electron-renderer 桌面端 UI + GS-01 场景脚本 + scripts/desktop 验证脚本）。现有模块：useStreaming SSE hook / app-store / conversation-store / CenterPanel / RightPanel / GS-01 golden scenario / mac-install-verify.sh。本任务 = 扩展 + 补强（事件契约对齐 + reportId 接线 + 降级 UI + 升级实测脚本 + 单实例锁），非新建子系统。
### b) 文件审计
- SSE 事件类型：src/l3/synova-diagnosis-engine-impl.ts 实测发射 10 种（phase_started/phase_completed/report_ready/right_column_update/degraded/error/expert_hypothesis/hypothesis_generated/root_cause_identified + diagnosis.ts 补发 community_reports/entity_resolution/judgment_card/complete）——useStreaming 仅处理 9 种且无 default。
- setCurrentReportId：app-store.ts:69/:113 定义，零生产调用方（grep 实测）。
- errorMessage：conversation-store 存储但无 UI 读取。
- main.cjs（195 行，切片 A/B 合入后）：无 requestSingleInstanceLock。
- scripts/desktop/：mac-install-verify.sh 存在（evidence 落盘模式对齐）；upgrade-data-verify.sh / first-diagnosis-timing.sh 不存在。
### c) 决策
复用（stores/hooks 既有骨架）+ 新建纯函数 sse-contract.ts（事件契约单源）+ 新建两验证脚本。冲突：无。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 业界：SSE 事件类型 = 前后端共享契约，前端必须处理全部类型，未知类型不得静默丢弃；Electron 多实例标准 = app.requestSingleInstanceLock()。
- Anthropic：机器可验契约（物理断言 phase_started 0-5 全出现 / SQLite md5 断言）+ fail-closed（降级可见不静默）。
- memory：K3 切片 A C2 M7（文档-实现漂移——已重新核验 main.cjs 195 行/engine 事件 10 种/useStreaming 9 case 合入后现状）+ D510 F1（禁 grep 冒充实测）+ P2-2（指纹/时间戳落 evidence）。
- 铁律 0-2（接线验收）/4/5/7/24+31（log + degraded）/47+48（契约+非空壳测试）。
### Q1c 决策参考系
参考：Anthropic（物理可验/降级可见）+ 第一性原理（事件契约 = 共享接口；数据在 userData 物理隔离即安全）+ electron-builder 官方升级语义 + 结论：sse-contract 纯函数单源 / requestSingleInstanceLock / 升级实测物理断言 / 诚实 RED。

## Q2: 范围 — 正确的最简方案
做什么（D527 spec §5.1 12 条 + D528 spec §5.1 5 条写集，以 spec 为唯一契约）：
- D527：sse-contract.ts 新建（applySSEEvent reducer）、useStreaming.ts 改走契约 + setCurrentReportId、conversation-store 加 phaseIndex/phaseLabel/phaseTotal/setPhaseProgress、CenterPanel 六阶段进度条 + errorMessage 红条、RightPanel 诊断报告 tab（GET /consult/:id/report?format=markdown）、Composer /diagnosis 语义化、tests/electron/use-streaming-contract.test.ts、GS-01 run.sh GS01_LLM 组 + expect.json + README、scripts/desktop/first-diagnosis-timing.sh、docs/synova/runbooks/first-diagnosis-e2e.md。
- D528：scripts/desktop/upgrade-data-verify.sh、tests/electron/upgrade-data-verify.test.ts、electron/main.cjs 单实例锁、docs/synova/runbooks/upgrade-data-retention.md、build-synova.cjs 条件性（实测默认保留则零改动）。
不做什么（含文件路径）：
- 不改 src/ 任何文件（src/routes/diagnosis.ts、src/l3/synova-diagnosis-engine-impl.ts、src/config.ts——派单红线，零 src/ 需求已证）。
- 不改 scripts/audit/（K3 专属）。
- 不做报告持久化 / 自动更新 / right_column_update 消费（D527 §6）。
- 不做 SQLite 损坏修复 / 数据目录 UI（D528 §6）。

## Q3: 验收 — 入口 → 交互 → 结果
入口：桌面端 Composer 输入触发 POST /api/diagnosis/consult（LLM 环境）/ upgrade-data-verify.sh 脚本。
处理：SSE 六阶段 phase_started 0→5 进度推进 → complete 提取 reportId 落 app-store → RightPanel 报告 tab 渲染 onePager；升级脚本装 v1→造数据→升级 v2→SQLite 断言。
结果：报告在桌面端可见；降级 errorMessage 可见；GS-01 扩展断言（LLM 环境绿 / 无 key 如实 RED）；升级数据断言一致 evidence 落盘；30 分钟计时 JSON 落 evidence。

## 架构层: L1
（electron-renderer 桌面端 + scripts/ 验证脚本；sse-contract 纯函数零依赖；单实例锁为 Electron 主进程基建。零跨层。）

## Done 标准
1. npx vitest run tests/electron/ 全绿（新增 use-streaming-contract.test.ts ≥10 用例 + upgrade-data-verify.test.ts ≥8 用例，red 已证）。
2. GS01_LLM 缺省跑 GS-01 → 契约断言绿 + LLM 组 evidence 诚实 RED 标注；LLM 环境则全绿。
3. first-diagnosis-timing.sh --dry-run 幂等 exit 0；upgrade-data-verify.sh --dry-run 幂等 exit 0。
4. git diff --name-only = D527 写集 12 + D528 写集 ≤5（build-synova.cjs 条件性零改动则 4）+ task-state 2 + spec 2 + brief。
5. task-state/D527.json D528.json 回填（slice L1-C + impl 段）。
