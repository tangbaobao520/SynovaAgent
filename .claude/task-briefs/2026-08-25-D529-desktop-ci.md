# Task Brief — D529 desktop-build CI 修复（C1/D529）

> 来源: CTO 派单（C1 闭环）| spec: 无 dev doc（派单即 spec，CI 修复型任务）

## Q0: 定位 — 项目拼图 + 文件审计
L1 桌面端 CI 基建。已有 .github/workflows/desktop-build.yml（D517 交付，双平台红）。
文件审计: workflow + build-synova.cjs + scripts/desktop/* 为唯一写集；无重复覆盖 → 修复。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
GitHub Actions Windows runner 默认 shell = pwsh——`test -f` 是 bash 内建，pwsh 必 CommandNotFound。
Anthropic 基线 = 机器可验（CI 连续 2 次绿为准）+ 根因日志（不是猜）。
memory: D510 F1（禁 grep 冒充实测）/ DS4（禁伪造）——CI 日志拿不到时用 workflow 内 GITHUB_TOKEN 回传失败尾部，不猜根因。
### Q1c 决策参考系
参考：Anthropic/第一性原理 + 结论: ①Windows = shell 缺失（确定根因，加 shell: bash）；②mac = 待真实日志定位（hdiutil 失败），用 debug 回传步骤拿日志后最小修复。

## Q2: 范围 — 正确的最简方案
做什么：.github/workflows/desktop-build.yml（shell: bash 修复 + 分支触发 + 失败日志回传 debug 步骤，定位后移除）、build-synova.cjs / scripts/desktop/*（如根因在此）、task-state/D517+D519（CI 绿后回填 artifact URL）。
不做什么（含文件路径）：不碰 src/、不碰 scripts/audit/、不做代码签名（无证书）、不在 CI 跑启动实测（D519/D523 领地）。

## Q3: 验收 — 入口 → 交互 → 结果
入口：push main / workflow_dispatch → desktop-build CI
处理：三步构建链（backend.mjs → renderer → electron-builder）双平台
结果：CI 双平台连续 2 次 success + artifact（dmg/zip/exe）可下载 + task-state/D517 DS3 + D519 DS6 回填真实 URL

## 架构层: L1
CI 基建，零跨层。

## Done 标准
1. CI 双平台连续 2 次 success（GitHub Actions 实测）
2. artifact URL 真实可访问，回填 task-state/D517.json（DS3）+ D519.json（DS6）
3. 已知限制如实标注（未签名 Gatekeeper 警告等），不伪造
4. 写集外零改动；src/ 与 scripts/audit/ 零触碰
