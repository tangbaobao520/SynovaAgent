# Task Brief: V5.1.2 verify-parallel 豁免 CRLF 修复（Win 实测，创始人授权越界补丁）

> 生成: 2026-08-25 | 任务: V5.1.2 | 认领: Synova-Win | 来源: 创始人授权（verify-parallel 属 DSH 地盘，DSH 预审）
> 参考: V5.0.1 豁免逻辑（Win 8f33e82a）; task-start L44 CRLF 同型

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
V5.0.1 的"已完成任务文档豁免"在 Windows 失效：python print 输出 CRLF → read 的 $f 带 \r → cat-file -e 失败 → 已合并文档（D484）未跳过 → D485 被误判写集重叠。修法：read 循环清洗 \r。
### b) 文件审计
- scripts/control-tower/verify-parallel.sh — 豁免段 read 循环加 `_f="${_f%$'\r'}"`
- .codex/control-tower/VERSION.md — V5.1.2 bump

## Q1: 调研
Windows Python stdout 文本模式 print 输出 \r\n（与 task-start L44 的 \r 同源）；命令替换 $(...) 保留行内 \r。

## Q2: 范围 — 正确的最简方案
做什么：
- scripts/control-tower/verify-parallel.sh — 豁免段 read 循环清洗 \r（一行）
- .codex/control-tower/VERSION.md — V5.1.2（PATCH）
不做什么：
- 不改比对核心（--overlap json 单行解析无 CRLF 问题）
- 不改 scripts/audit/（K3 红线）

## Q3: 验收 — 入口 → 交互 → 结果
入口：--scan-today / --doc-a --doc-b
处理：已完成文档（写集在 origin/main）跳过，不受 \r 影响
结果：D484 vs D485 判定通过（D484 跳过）；verify-parallel.test.sh 绿

## 架构层:
scripts（控制塔）

## Done 标准
- [ ] D484 vs D485 --doc-a/--doc-b 判定 EXIT=0（D484 跳过）
- [ ] verify-parallel.test.sh 绿
