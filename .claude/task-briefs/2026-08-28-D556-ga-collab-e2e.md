# Task Brief: D556 GA 人机协同端到端（前端接线 + 回流层 2, slice: ga-collab-e2e）

> 生成: 2026-08-29 01:20 | 分支: feat/d556-ga-collab-e2e (worktree .wt-d556 @ origin/main fe9bac14) | as any: 0
> 执行契约: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D556-ga-collab-e2e-20260829.md（§3.3.1 写集 / §5-§8 实现 / §13 DS1-DS9）

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。L1 交互层不知道 L4 用什么数据库，L3 洞察层不知道 L5 数据存在哪。换底层存储，上层零改动。pre-commit 物理阻断跨层 import——L2→L4 的代码提交不进去。

**横向解耦：11 个独立 Monorepo 包**
五层内部拆为独立包：@synova/sog-core（本体图类型）、@synova/sentinel-engine（哨兵调度）、@synova/expert-platform（专家加载）、@synova/connector-registry（数据连接器）。每个包接口边界明确，拆卸一个不影响其余 19 个。

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码。

流程约束: V4.5.1 — task brief 6 字段强制 + 免疫系统 + plan.json + 13 组物理阻断 + Plan-Actual 闭合。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层（本单打通 diagnosis_conclusion 回流层 2）

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于纵向 L1 交互（electron-renderer GaDetail 接线 D551 四端点）+ 层 2 进化回流（src/loops/middle-evolution-engine.ts 追加 diagnosis_conclusion 信号类）。现有模块: D551 四端点已合 main（ga-calibration.ts L121/239/314/419）、GaDetail 占位（RightPanel.tsx L632-640）、白名单三组合（middle-evolution-engine.ts L78-79/L92-93/L106-107）。关系: 前端 = 消费扩展（占位转真实）；引擎 = 只追加 Signal 6（三既有信号类语义零变化）。

### b) 文件审计
grep "diagnosis_conclusion / ga_calibration / GaDetail" 在 src/ 与 electron-renderer/src/: feedback-collector.ts L123（CHECK 已含，只读消费）、loop-handlers.ts L377-399（消费线在位，只读）、agent-memory-store.ts（MemoryType 枚举 + DDL CHECK，不在写集）、capability.ts L43-45 canAccessCap（门控复用不改）。复用: canAccessCap / apiFetch / logCorrection sink 形态 / applyEvolutionActions 编排。新建: stores/ga-collab.ts（纯逻辑数据层）、components/ga-detail-sections.tsx（纯展示）、test-support/render.ts（渲染桥接）。

### c) 决策
已有覆盖→复用（capability 门控、D551 端点契约、agent_memory sink、feedback_log→processFeedbackSignals→applyEvolutionActions 管线）。无覆盖→新建 ga-collab 纯逻辑层 + ga-detail-sections 纯展示层（node 可测）。冲突→见 Q1c: spec §8 渲染桥接前提在 CI 不成立（react-dom/zustand/react-markdown 不在 root lockfile，CI vitest 仅 root npm ci）→ renderToStaticMarkup 以零依赖元素树序列化器等价实现 + GaDetailSections 独立文件（写集 +1，commit message 留证）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = D556 spec（唯一执行契约，DS1-DS9 完成标准已定义）。② 测试先行: logic/ui/engine 三层测试镜像 spec §8 断言矩阵，先写测试后补实现。③ 实现 = spec §5/§6/§7 逐条落地。④ 接线 = §9 九条 grep 全过（生产调用点断言）。⑤ 验证 = 自检 6 问 + verify-incremental + CI。

### b) 本任务执行约束
- rule: 层 2 只追加不修改——三既有信号类（sentinel_alert/goal/reject_path）filter 语义零变化
  verify: "git diff origin/main..HEAD -- src/loops/middle-evolution-engine.ts | grep -E '^[-+].*(sentinel_alert|reject_path)' | grep -v '^+' 仅删除行为空"
- rule: 新 export 必须有生产调用方（ga-collab.ts 各函数被 RightPanel.tsx/app-store.ts 调用）
  verify: "grep -n 'buildCalibrationRequest\\|mapStatsResponse\\|getSeedToken' electron-renderer/src/components/RightPanel.tsx"
- rule: 占位文案零残留（spec §9 第一条）
  verify: "test $(grep -c '后端校准接口待接入' electron-renderer/src/components/RightPanel.tsx) -eq 0"

### c) 决策参考系
参考：Anthropic（fail-closed + 最小依赖）+ 第一性原理（已付后端成本变现最短路径）+ 仓库实证（lockfile/ci.yml 物理事实）+ 结论: ① UI 断言=零依赖元素树序列化 renderToStaticMarkup 等价实现（spec §8 机制在 CI 不可解析——lockfile 零 react-dom/zustand/react-markdown + ci.yml 零 renderer 安装，两证据）② GaDetailSections 独立纯文件（+1 写集，RightPanel 容器真 import 非代理）③ 层 2 sink=agent_memory（logCorrection 同款形态；type 值落 MemoryType 枚举内 'ga_correction'——spec §6.1 'ga_calibration_review' 不在枚举/DDL CHECK 且 agent-memory-store.ts 不在写集，判别值冗余携带于 key 前缀+tags+value.actionType）④ seed=legacy x-synova-token + localStorage（不耦合 D483）。收敛检查: 无分歧。

### d) 相关 Note 引用
- [ ] memory/notes/ 无同主题 Note——本任务为特性实现非治理变更，决策沉淀于 spec §14 + 本 brief Q1c。

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- electron-renderer/src/stores/ga-collab.ts — 新建纯逻辑数据层（状态机/请求构建/响应映射/降级决策/seed 读取，零 react/zustand）
- electron-renderer/src/components/ga-detail-sections.tsx — 新建纯展示组件（props 驱动，五场景可断言）
- electron-renderer/src/components/RightPanel.tsx — GaDetail 占位重写为容器（role 防御 + fetch 编排 + 回显）+ apiFetch 附 legacy 头
- electron-renderer/src/stores/app-store.ts — boot seed（localStorage synova.dev-identity → userRole，无 seed admin 原语义）
- electron-renderer/src/test-support/render.ts — 新建渲染桥接（零依赖序列化器，仅 tests 引用）
- src/loops/middle-evolution-engine.ts — EvolutionActionType 扩 'diagnosis_calibration_review' + Signal 6 + applyDiagnosisCalibrationReview + apply case（只追加）
- tests/ga-collab-logic.test.ts — 数据层单测
- tests/ga-collab-ui.test.ts — UI 五场景断言
- tests/loops/ga-calibration-evolution.test.ts — 层 2 引擎测试（3 回流→3 动作+sink+阈值）
- docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D556-ga-collab-e2e-20260829.md — spec 随首 commit 入库（§3.3.1 提交策略）
- .claude/task-briefs/2026-08-28-D556-ga-collab-e2e.md — 本 brief（G12 认领必需，D551 教训 391734c3）
- task-state/D556.json — impl 段回填 + status=impl_done（DS9）

不做什么：
- 不改 src/routes/ga-calibration.ts（D551 交付面只读——spec §10）
- 不改 src/growth/feedback-collector.ts（回流上游已通，只读消费）
- 不改 src/agent/loop-handlers.ts（消费函数已调 processFeedbackSignals，新动作零接线即生效）
- 不改 src/middleware/auth.ts（D483 在途；seed 走既有 legacy 通道 L366-376）
- 不改 electron-renderer/src/stores/capability.ts（门控复用不改）
- 不改 electron-renderer/src/App.tsx（入口无变化）
- 不改 electron-renderer/package.json（零新依赖红线）
- 不改 electron-renderer/vite.config.ts（构建配置不动）
- 不改 vitest.config.ts（root include 已覆盖 tests/**）
- 不改 package.json（零新依赖红线——react-dom 不可引入，渲染桥接走零依赖序列化器）
- 不改 scripts/pre-commit-check.sh（治理冻结）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：
seed GA 身份（localStorage synova.dev-identity={"role":"ga","orgId":"default","userId":"ga-seed-1"}）打开桌面端 → 左栏 GA 项可点（canAccessCap('ga','ga')=true）→ GaDetail 三块渲染。
处理（中间经过哪些步骤）：
块1 四动作校准表单 → buildCalibrationRequest 校验（镜像 D551 服务端）→ POST /api/ga/calibration → 回显 calibrationId → 列表刷新；块2 五要素信号表单 → buildSignalRequest → POST /api/ga/calibration/signals → 回显 signalId/findingId；块3 GET stats → 计数 + note 原文透传；同类回流 ≥3（feedback_log→processFeedbackSignals Signal 6）→ 生成 diagnosis_calibration_review 动作 → applyEvolutionActions → agent_memory 审核条目。
结果（最终展示在哪）：
UI 三块真实数据渲染（tests/ga-collab-ui.test.ts 五场景断言）+ 引擎 3 回流→3 动作断言（tests/loops/ga-calibration-evolution.test.ts）+ evidence/D556/ 落盘 + PR。

## 架构层: L1 交互（electron-renderer GaDetail/stores）+ L2 编排（src/loops 进化管线追加，sink 走 L4 agent_memory 既有通道）
L1（RightPanel/ga-collab/ga-detail-sections）→ HTTP → L1 routes（D551 只读）；层 2 在 src/loops 引擎内闭环（processFeedbackSignals 纯函数 + applyEvolutionActions 既有编排，零新跨层）。
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: seed 后 GaDetail 渲染三块（tests/ga-collab-ui.test.ts 场景 2/3 绿）: `npx vitest run tests/ga-collab-ui.test.ts`
- [ ] 链路走通: 层 2 三回流→三动作+sink 写入+阈值断言: `npx vitest run tests/loops/ga-calibration-evolution.test.ts`
- [ ] 结果可见: 占位零残留 + 层 2 动作类型接线: `test $(grep -c '后端校准接口待接入' electron-renderer/src/components/RightPanel.tsx) -eq 0 && test $(grep -c 'diagnosis_calibration_review' src/loops/middle-evolution-engine.ts) -ge 3`
