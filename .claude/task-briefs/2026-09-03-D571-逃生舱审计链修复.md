# Task Brief: D571 pre-push 逃生舱审计链修复 + CTO 合并通道纪律固化

> 生成: 2026-09-03 | 任务: D571 | 认领: CTO (DeepSeek Harness)
> 参考: D333 决策四步 + D570 违规复盘（创始人 2026-09-03 要求防复发）

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔门禁层（scripts/pre-push-check.sh 门禁 0-2）+ CTO 交接技能（.claude/skills/cto-handover → 同步 .dsh）。
背景：CTO 用 SYNO_ALLOW_MAIN_PUSH=1 直推 main ×3（D569/D570/D551 补交），复盘发现两处缺陷：
① 违规本身（逃生舱误用于常规合并）；② 门禁 0-2 逃生舱输出「已记 bypass.log」但**未实现写入**
（grep 全脚本只有 echo 无写文件）——审计链断裂（M2 声称 vs 事实，在门禁自己的实现里）。
### b) 文件审计
- scripts/pre-push-check.sh L74-81：逃生舱分支，echo 声称已记但无写入
- tests/control-tower/push-sync-guard.test.sh：已有逃生舱放行用例（L82），缺 bypass.log 写入断言
- .claude/skills/cto-handover/SKILL.md：缺合并通道与逃生舱红线
### c) 决策
修门禁实现（真实写 bypass.log）+ 测试断言 + 技能红线。合并通道固化 = GitHub API token（~/.dsh/.credentials.yaml 有 GITHUB_TOKEN，2026-09-03 验证有效）走 PR merge。

## Q1: 调研
铁律 0-3（main 只进 PR）；铁律 11（降级/逃生舱必须留痕）；ctrl-tower-change 技能红线「逃生舱必须写 degraded-events.log + 测试」；D466/D438 bypass.log 补记先例；M2 模式（声称 vs 事实）。D333 参考：第一性原理（审计链断了 = 违规不可追责）+ Anthropic（机器可验日志）+ 收敛：真实写日志 + 测试断言日志存在。

## Q2: 范围
做什么：
- scripts/pre-push-check.sh：逃生舱分支真实写 .claude/bypass.log（ALLOW_MAIN_PUSH 条目）
- tests/control-tower/push-sync-guard.test.sh：加「逃生舱后 bypass.log 含 ALLOW_MAIN_PUSH」断言
- .claude/skills/cto-handover/SKILL.md：加「合并通道 + 逃生舱红线」节（同步 .dsh）
- 台账：D570 违规完整条目（3 次逃生舱 + 无审计记录发现 + token 位置事实）
不做什么：
- 不改 scripts/audit/（K3 红线）
- 不改门禁 0-2 的阻断语义（fail-closed 不变）
- 不回滚已发生的 3 次直推（内容正确，回滚风险更大）

## Q3: 验收
入口：SYNO_ALLOW_MAIN_PUSH=1 git push ssh main（测试沙箱内）
处理：门禁放行 + 写 bypass.log 一行
结果：bypass.log 尾部出现 ALLOW_MAIN_PUSH 条目；测试红转绿；技能含红线

## 架构层: scripts/control-tower（控制塔）

## Done 标准
- [ ] verify: bash -n scripts/pre-push-check.sh
- [ ] verify: bash tests/control-tower/push-sync-guard.test.sh 全绿（含新断言）
- [ ] verify: grep -c "ALLOW_MAIN_PUSH" scripts/pre-push-check.sh ≥ 1（真实写入实现）
- [ ] verify: sync-dsh-skills 后 .dsh/skills/cto-handover/SKILL.md 含「合并通道」节
