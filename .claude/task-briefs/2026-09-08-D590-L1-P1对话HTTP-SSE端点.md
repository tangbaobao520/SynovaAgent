# Task Brief: D590 L1-P1对话HTTP-SSE端点

> 生成: 2026-09-08 | 任务: D590 | 认领: DeepSeek Harness (DSH)
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）
> Spec: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md（唯一契约）
> 任务号: task-state/D590.json（alloc-task-id 已登记）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
L1 交互层（routes/ + l1-interaction/ + middleware/ + server 装配）+ 一处 L2 公开 API 消费（ConversationEngine，零 L2 修改）。现状：对话引擎生产调用方仅 CLI/TUI/MCP/IM 四个进程内入口，HTTP 面零对话端点（spec §4.1 grep 实测）；WebViewAdapter 零消费者（死代码）；auth 白名单不含 /api/diagnosis/consult（桌面首条消息 401）；diagnosis-upload-v2 假绿源占道。本任务把 ConversationEngine 接进 HTTP SSE + SessionStore 持久化 + 鉴权过桥 + upload-v2 410 下线。
### b) 文件审计
锚点全部当场 grep/read 复核（2026-09-08，基线 8087afe5）：
- conversation-engine.ts:453 setViewAdapter / :707 processMessageStream / :734 viewAdapter 调用 / :775 serialize / :789 fromState / :110 ProcessResult / :377 构造器
- session-store.ts:236 createSession / :253 updateSession / :317 addMessage(双写) / :381 deriveMessages / :422 getMessages / :433 saveState / :438 loadState / :40 isSqliteDatabase
- auth.ts:86-105 isWhitelisted（16 条，不含对话/consult/sessions——亲证）
- server.ts:54 upload import（spec 写 :48，行号微漂语义同）/ :297-298 llm-config 先于 JWT / :337-338 diagnosis+sessions 挂载 / :344 upload 挂载
- diagnosis.ts:127-133 provider 构造 / :175-194 D563 动态 import 范式 / :82-91 sseClose 形状
- ga-diagnosis.ts:147-162 pollResults 无限轮询
- web-adapter.ts:17/:45 messageBuffer 死字段 / chat.ts:321/:726/:823/:885 浏览器 log.warn（:44 服务端合法不动）
- sse-contract.ts:24-30 SSEEventType 15 类型 / graphstore-unify.test.ts:98 数组项
- spec §5.3 漂移发现：grep "diagnosis-upload-v2" 另有第 4 处注释提及 tests/routes/llm-config.test.ts:174（spec 只列 3 处）——纯注释非引用，不改（写集精确优先），完成报告如实标注
### c) 决策
复用 + 接线：SessionStore D500 双写/serialize/fromState 全部现成零 schema 变更；ViewAdapter 接口现成；新建仅 conversations 路由本体 + 集成测试。upload-v2 假绿源删除（裁决②）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 业界：SSE 对话流 = OpenAI chat completions 流式帧 + 注释帧心跳；Express 原生 res.write（单机引 socket.io = 过度设计）
- DSH 范式（零 import）：dsh-agent-loop 请求重建不变量（invariant.js:15-33）→ 用例 6 重建不变量测试；中断锚（index.js:637-655）→ 用例 5 断线完整落库；dsh-api-gateway 帧协议+心跳（:122-133,:199-215）→ open/token/agent_message/end/error 帧语义 + 15s 心跳
- 历史教训：D316（声称实测须物理证明）；M3 两例（im-inbound addToHistory?.() 静默 no-op、tui-v2 processUserInput?.()）→ 接线必须 read 被调用方真实定义（本任务全部实测）；D576（诚实交付）；D524（spec 行号漂移→开工重核，已发现 2 处微漂）
参考：Anthropic/DeepSeek/第一性原理 + 结论——spec §5.4 七项决策双参考系收敛，全盘采纳

## Q2: 范围 — 正确的最简方案
做什么：
- src/routes/conversations.ts — 新建对话 SSE 路由（POST /api/conversations + POST /api/conversations/:id/messages）
- src/l1-interaction/web-adapter.ts — 复活改造（对象帧/emitDiagnosisEvent/setOnClosed/心跳/删 messageBuffer）
- src/server.ts — 挂载 conversations + 删 upload import/挂载 → 410 拦截
- src/middleware/auth.ts — 白名单三前缀 + 单机信任 JSDoc
- src/routes/chat.ts — 4 处浏览器 log.warn → console.warn + 头注释诚实化
- src/routes/ga-diagnosis.ts — pollResults 410 止停
- electron-renderer/src/hooks/sse-contract.ts — +4 帧类型 + 归约分支
- README.md — 安全模型节
- tests/routes/conversations.test.ts — 新建集成测试 ≥12 用例（真实路由 + 真实 store + mock 三处）
- tests/electron/use-streaming-contract.test.ts — D590 帧归约用例
- tests/architecture/graphstore-unify.test.ts — 移除 upload-v2 数组项
- 删除：src/routes/diagnosis-upload-v2.ts、tests/test-http-route.ts（spec §5.3 裁决②）

不做什么（含文件路径）：
- 不改 electron-renderer/src/hooks/useStreaming.ts（管道健全，断因 401，归 D591）
- 不改 src/agent/conversation-engine.ts（L2 零修改，spec §5.4 决策 5 承重前提）
- 不改 src/store/session-store.ts、src/l3/、src/l4/、src/l5/（D588 已占 store 演进线）
- 不改 src/l1-interaction/types.ts（ViewAdapter 接口不动）
- 不改 tui-v2/、tui-v3/、src/mvp-server.ts（TUI 保留裁决③；mvp-server 归 D596）
- 不改 scripts/audit/（K3 红线）
- 不改 tests/routes/llm-config.test.ts（仅注释提及 upload-v2，spec §5.3 外文件，写集精确优先）
- 不改 src/routes/diagnosis.ts（consult 活路由零误伤，仅注释提及）

## Q3: 验收 — 入口 → 交互 → 结果
入口：POST /api/conversations {message, sessionId?, orgId?}（DEV_MODE=false 无 Authorization 可达，白名单）
处理：SSE 流 open(sessionId 回声) → token×N → agent_message → [诊断事件 flat 透传 → complete] → end；持久化五步（cli.ts:228-244 权威模板 + user 前置）
结果：GET /api/sessions/:id 全轮次可查；断线重连不丢已见内容；upload-v2 四路径 410 GONE；verify 见 Done

## 架构层:
L1 交互层（routes/ + l1-interaction/ + middleware/ + server 装配）。L1→L2 合法（引擎公开 API + DiagnosisLauncher + SessionManager + registerBuiltinTools + ToolRegistry）；L5 仅经 req.app.locals + 动态 import（D563 既有范式）；零 L2 修改；as any/as never = 0。

## Done 标准
- [ ] verify: npx vitest run tests/routes/conversations.test.ts tests/electron/use-streaming-contract.test.ts → 全绿（≥12 用例，red 已先证）
- [ ] verify: grep -rn "conversationsRoutes" src/server.ts | wc -l → ≥2；grep -rn "WebViewAdapter" src/routes/ | wc -l → ≥1；grep -n "/api/conversations" src/middleware/auth.ts → ≥1；grep -n "code: 'GONE'" src/server.ts → ≥1
- [ ] verify: grep -rn "diagnosis-upload-v2" src/ tests/ → 仅剩注释提及（spec §5.3 列 3 处 + llm-config.test.ts:174 注释，完成报告标注）；grep -rn "messageBuffer" src/l1-interaction/ → 零结果
- [ ] verify: npx tsc --noEmit → 零新增错误；grep -rn "as any\|as never\|as unknown as" src/routes/conversations.ts src/l1-interaction/web-adapter.ts → 零结果
- [ ] verify: npx vitest run → 全量零新失败
- [ ] verify: SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh && bash scripts/control-tower/simulate-ci.sh → 双 exit 0
