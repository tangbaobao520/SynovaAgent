# Task Brief: D591 桌面对话接线——真流式+提交回声+向导后直达对话

> 生成: 2026-09-08 | 任务: D591 | 认领: DeepSeek Harness（编码线，electron-renderer 写集例外内）
> Spec（编码唯一契约）: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D591-desktop-chat-wiring-20260908.md
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）；spec §5.4 六决策已双参考系收敛

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
L1 交互层桌面前端（electron-renderer/src/），消费 D590 已落 main（b54a0fb6）的对话 SSE 端点。
现状四处断裂：useStreaming 独指 /api/diagnosis/consult（token 帧零消费=假流式）；open 帧 sessionId
被丢弃（无对账）；conversation-store 纯内存（刷新失忆）；useConversation.ts 死代码 + app-store mock 假数据。
### b) 文件审计
grep/read 实测（main@b54a0fb6）: useStreaming.ts:200 consult 硬编码；sse-contract.ts:36/145-152
四新帧+open 归约已备；src/routes/sessions.ts:68-81 读回 {ok,session,messages}；useConversation.ts
:23 自认零消费者；app-store.ts:88-97 MOCK_* 注入 :118。WelcomePanel 快速行动门控 ready 态但
CenterPanel ready 不渲染 WelcomeScreen（生产不可达——spec §4.2 前提修正，见完成报告偏差 D-1）。
### c) 决策
已有覆盖→复用+接线（sse-contract 归约器/conversation-store CRUD/getApiBase/D575 boot 全现成）；
新建仅 conversation 模式分支+恢复投影纯函数+测试；端点双轨（spec §5.4 决策 1）；
useConversation.ts 删除（决策 2，铁律 37）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 业界: SSE 消费 = fetch+ReadableStream 手写解析（useStreaming:220-260 已合格，不引 eventsource 库）；
  乐观 UI 回声 = 本地立即上屏+服务端确认帧对账；会话恢复 = 持久标识+启动拉取投影。
- DSH 范式（零 import）: dsh-api-session-controller beginSubmission 提交回声（:875-896）——落为
  乐观上屏（已有）+open 帧 sessionId 捕获持久化（新增）+恢复对账（新增）。
- memory 教训: 铁律 41 bufferRef+16ms flush 模式刻意沿用（TUI 闪烁事故验证），禁 LineBuffer 类名；
  M3（接线必须 read 被调用方真实定义）；D316/D576 声称即物理证明。
参考：Anthropic/DeepSeek/第一性原理 + 结论：复用既有管道+最小机制（spec §5.3/§5.4 收敛）。

## Q2: 范围 — 正确的最简方案
做什么：
- electron-renderer/src/hooks/useStreaming.ts — sendMessage 加 mode 参数（缺省 conversation→/api/conversations，
  token/open/agent_message/end 四帧消费；diagnosis 分支逐字节不变）+ 抽 createStreamingController 工厂（可测性）
- electron-renderer/src/stores/conversation-store.ts — currentSessionId/draftPrefill/pendingIntent +
  setCurrentSessionId/restoreMessages/clearSession/setDraftPrefill/setPendingIntent + restoreMessages 投影纯函数 + readLastSessionId
- electron-renderer/src/components/CenterPanel.tsx — restoreLastSession 恢复时序 + handleSend mode 分流 +
  待发送条 + 空态快速行动（可达性修复，偏差 D-1）
- electron-renderer/src/components/WelcomeScreen.tsx — 快速行动三按钮经 onQuickAction 写 store
- electron-renderer/src/stores/app-store.ts — MOCK_WORKSPACES/MOCK_CONVERSATIONS 置空数组（spec §5.1 表下备注段登记写集）
- tests/electron/use-streaming-conversation.test.ts — 新建 §7 用例 1-9 全覆盖（先红后绿）
- electron-renderer/src/hooks/useConversation.ts — 删除（死代码，写集表外删除清单）
- .claude/task-briefs/2026-09-08-D591-桌面对话接线-真流式+提交回声+向导后直达对话.md — 本 brief
- docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D591-desktop-chat-wiring-20260908.md — spec 落库
- docs/synova/coordination/编码指令-D591桌面对话接线-20260908.md — 交付指令随任务落库
- docs/synova/audit-reports/D591-desktop-chat-wiring-evidence-20260908/README.md — 证据索引（目录内其余 evidence 文件随此提交）
- task-state/D591.json — 状态机回填
不做什么：
- 不改 src/routes/conversations.ts（D590 已闭环，后端零改动）
- 不改 src/server.ts（同上）
- 不改 electron-renderer/src/hooks/sse-contract.ts（契约单源，D590 已扩展，测试零回归锁定）
- 不改 tests/electron/use-streaming-contract.test.ts（零回归锁定）
- 不改 electron-renderer/src/components/Composer.tsx（不在写集；draftPrefill 消费在 CenterPanel，偏差 D-4）
- 不改 electron-renderer/src/components/LeftPanel.tsx 与 electron-renderer/src/components/CommandPalette.tsx（真会话列表归 D593）
- 不改 electron-renderer/src/App.tsx（D575 boot 判定已成立）
- 不改 src/tui-v2/use-streaming.ts（铁律 40 冻结域 + 铁律 40-45 适用域裁决不适用 electron，spec §5.4 决策 6）
- 不改 scripts/audit/（K3 审计红线）
- 不改 memory/notes/proposed/D599-dev-doc-交付前置换核与指令内嵌.md（dev-doc session 产物，非本任务写集）

## Q3: 验收 — 入口 → 交互 → 结果
入口：桌面端启动（已配置 LLM 直达主界面；未配置→向导→主界面）→ 中栏 Composer 发一句话。
处理：用户消息乐观上屏→POST /api/conversations→open 帧 sessionId 落 store+localStorage→token 帧
bufferRef+=text 16ms flush 逐字进 streamingText→agent_message 全文落列表→诊断事件 flat 透传复用 D527 管道。
结果：逐字完整回复可见；重启后消息区自动恢复（GET /api/sessions/:id 投影）；「诊断我的公司」链路走
consult 显式诊断（用例 8 回归锁定）；app-store 假数据清零、useConversation.ts 零引用。

## 架构层: L1（electron-renderer 桌面前端，纯消费侧；对后端仅 HTTP，零跨层 import）

## Done 标准
- [ ] verify: npx vitest run tests/electron/use-streaming-conversation.test.ts tests/electron/use-streaming-contract.test.ts → 全绿零失败
- [ ] verify: bash -c "grep -rn 'useConversation\b' electron-renderer/src | grep -v useConversationStore | wc -l | tr -d ' '" → 0
- [ ] verify: bash -c "grep -rn 'MOCK_CONVERSATIONS\|MOCK_WORKSPACES' electron-renderer/src | wc -l | tr -d ' '" → 0
- [ ] verify: bash -c "cd electron-renderer && npx tsc --noEmit" → 零新增错误
