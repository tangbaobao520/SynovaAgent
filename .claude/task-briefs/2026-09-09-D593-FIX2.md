# Task Brief: D593-FIX2 — 二次修复（上次修复声称提交实未提交，K3 复审 P0-1）

> 生成: 2026-09-09 | 分支: fix/d593-restore（基线 57596af2） | as any: 0
> 派单: 创始人（K3 物理核验：9c3f1672 diff 不含 diagnosis.ts，worktree 手术从未进入提交）

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
L1 交互层二次修复。根因（编码线自认）：D593-FIX 手术后**漏执行 git add src/routes/diagnosis.ts**——synova-commit 按暂存区显式路径提交，diagnosis.ts 不在其中；"green 21/21" 是 worktree 态假绿（工作树有手术、提交没有）。D316 老教训（声称提交实未提交）复犯，本次以三条物理验收闭环。

### b) 文件审计（开工 grep 实测）
- 提交态（57596af2）diagnosis.ts: saveDiagnosisCheckpoint=0（K3 属实）
- 工作树手术现场完好: saveDiagnosisCheckpoint=1 写点 + unstaged 248 行 diff（` M`）
- main 四恢复面全 0 命中（K3 属实）

### c) 决策
无新决策——恢复内容与 D593-FIX 完全一致（工作树手术即权威版本），本次只补「staged→commit→远端」三物理证据链。参考：Anthropic（机器可验契约：diff/grep/提交态测试三重物证）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = 派单三物理验收；② 恢复内容已验证（worktree 手术 21/21）；③ 本次零代码新改动，纯「入提交」动作；④ 验收 = 三物证；⑤ 自检。

### b) 本任务执行约束
- rule: "提交前 staged 必须含 diagnosis.ts"
  verify: "git diff --cached --name-only 含 src/routes/diagnosis.ts"
- rule: "提交后 git show --stat 必须显示 diagnosis.ts"
  verify: "git show HEAD --stat | grep src/routes/diagnosis.ts"
- rule: "远端分支 diagnosis.ts 必含 saveDiagnosisCheckpoint ≥1"
  verify: "git show origin/fix/d593-restore:src/routes/diagnosis.ts | grep -c saveDiagnosisCheckpoint ≥1"

### c) 决策参考系
参考：Anthropic/第一性原理 + 结论（三物证闭环：staged diff → commit stat → 远端 grep，全机器可验）。

### d) 相关 Note 引用
- [x] D316 教训（声称提交实未提交）复犯披露于完成报告；无新增治理决策。

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- src/routes/diagnosis.ts
- .claude/task-briefs/2026-09-09-D593-FIX2.md
- docs/synova/audit-reports/D593-FIX-restore-evidence-20260909/README.md
- task-state/D593.json

不做什么：
- 不改 src/store/session-store.ts（存活完好）
- 不改 src/routes/conversations.ts（存活完好）
- 不改 src/middleware/auth.ts（白名单存活）
- 不改 electron-renderer/src/（存活完好）
- 不改 README.md（存活完好）
- 不碰 D595 领地（D595 文档已于 9c3f1672 清理，本任务零触碰）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：诊断/对话桥完成 → GET report + GET /api/diagnosis/reports。
处理（中间经过哪些步骤）：提交态（非 worktree 态）代码承载 D593 四恢复面。
结果（最终展示在哪）：三物证齐 + 提交态 21/21 绿 + conversations.test.ts 绿 + 远端 grep ≥1。

## 架构层: L1 交互层（routes/）
#CRITERIA: A

## Done 标准
- [ ] 物证①staged: git diff --cached --name-only 含 src/routes/diagnosis.ts（已在本 brief 撰写时先行取证）
- [ ] 物证②commit: git show HEAD --stat 显示 src/routes/diagnosis.ts（verify: git show HEAD --stat | grep -c "src/routes/diagnosis.ts" ≥1）
- [ ] 物证③远端: git show origin/fix/d593-restore:src/routes/diagnosis.ts | grep -c saveDiagnosisCheckpoint ≥1
- [ ] 提交态绿: 临时 worktree checkout 新提交实跑 D593 对 21/21 + conversations.test.ts 13/13（非 worktree 工作树态）
- [ ] 红线: customerConfig=15；D595 零触碰
- [ ] 门禁: SYNO_CI=1 bash scripts/pre-commit-check.sh exit 0
