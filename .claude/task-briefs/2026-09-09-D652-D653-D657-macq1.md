# Task Brief: D652/D653/D657 Mac质量第一批：evidence入库铁律+合入即绿门禁+测试零副作用

> 生成: 2026-09-09 | 分支: feat/mac-quality-batch1（worktree .synova-wt-macq1 @ ssh/main 93209c57）| as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**（本任务不触 L1-L5 业务层）

**横向解耦：独立 Monorepo 包**（本任务不触 packages/）

**扩展解耦：文件驱动，不改代码**（本任务属控制塔/测试基础设施，非文件驱动扩展）

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
- [x] 基础设施（控制塔门禁 + 测试基建），不属 L1-L5 业务层

本任务属基础设施/控制塔系统：K3 D651 评估 Mac 侧 C7/C2 失分点的机制化修复。
- D652：task-state 引用不可见 evidence = 声明降级（K3 实测 evidence/D551、evidence/D575 未入库）→ 新门禁脚本物理校验"引用必须 git 跟踪"
- D653：D593 在 main 留红测试无人拦 → 基线快照 + 新增匿名红检测（check-ci-stale-red.sh 只查 CI run 红灯，不查套件红列表，互补不替代）
- D657：全量测试污染工作树（本机实测复现：extensions/industries/saas-tech/thresholds.json tracked 文件 aggregatedAt 被测试改写 + test-write/thresholds.json）→ vitest 生命周期恢复现场

### b) 文件审计
grep "check-evidence\|check-main-green\|heartbeat\|thresholds" 于 scripts/control-tower/、tests/、extensions/：
- scripts/control-tower/check-ci-stale-red.sh — 存在但语义不同（GitHub API run 级红灯超时），不冲突 → D653 新建并行脚本
- scripts/control-tower/ct-test-gate.sh — 已有控制塔脚本↔测试配对门禁，新脚本自动被覆盖（复用）
- vitest.config.ts — 已有 test.env（SYNOVA_DB_PATH=:memory:）→ 扩展 globalSetup
- extensions/industries/*/thresholds.json — tracked 真实配置，被 tests/evolution/global-analyzer.test.ts 触发 writeIndustryThresholds 改写（污染源实锤）
- src/loops/loop-scheduler.ts:71 HEARTBEAT_DIR=join(cwd,'.codex') — heartbeat 写入方（本机 CI 口径未触发，防御性纳入清理白名单）
关系：全部新建/扩展，零冲突。

### c) 决策
无已有同功能门禁 → 新建。冲突取舍走 DECISION-REFERENCE 四步，结论记 Q1c。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC/Done：三子任务各自 Done 见下方（可 bash 物理验证）
② 测试：每脚本配对 tests/control-tower/*.test.sh（mktemp 沙箱 + 注入缝，零真实目录零网络）
③ 实现：三态退出码（0 过/1 业务阻断/2 降级）+ 降级 stderr "degraded:"（ctrl-tower-change 模式 1/铁律 11）
④ 接线：pre-commit-check.sh 新组 + ci.yml canary 清单 +（D653）ci.yml test job 失败分支归属判定
⑤ 验证：SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh + bash scripts/control-tower/simulate-ci.sh

### b) 本任务执行约束
- rule: 新门禁脚本必须三态退出码且降级显式打 "degraded:"（铁律 11/ctrl-tower-change 模式 1）
  verify: grep -c "exit 2" scripts/control-tower/check-evidence-cited.sh
- rule: 新控制塔脚本必须有配对测试且测试绿（ct-test-gate 自动执法）
  verify: bash tests/control-tower/check-evidence-cited.test.sh && bash tests/control-tower/check-main-green.test.sh
- rule: D657 修复不改 src/ 与 packages/（写入方 loop-scheduler/global-analyzer 源码不动，恢复现场在测试基建层完成）
  verify: git diff --name-only origin/main..HEAD | grep -E "^(src|packages)/" (期望零输出)

### c) 决策参考系
参考：Anthropic（fail-closed 门禁三态 + hermetic tests）/第一性原理（引用不可见=声明降级 → git ls-files 物理校验；红列表漂移机器可验 → 基线快照存 git；测试污染 → 生命周期快照+恢复）+ 结论：三机制均收敛于"脚本化 fail-closed 门禁 + 密封测试"。
D652 存量语义决策：只校验本次变更触碰的 task-state 条目（staged/diff 口径），不溯及既往存量（存量 evidence/ 顶层引用被 .gitignore L76 全局忽略属历史遗留，文件被触碰时即须补入库——与 D544 P1-2 修复方向一致）。

### d) 相关 Note 引用
- [x] memory/notes/implemented/2026-09-09-mac-quality-batch1-gates.md（本批三机制决策沉淀，随 D652 commit 落地）

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- scripts/control-tower/check-evidence-cited.sh（D652 门禁：task-state 引用路径 git ls-files 校验，fail-closed）
- tests/control-tower/check-evidence-cited.test.sh（D652 密封测试三路径+接线）
- scripts/control-tower/check-main-green.sh（D653 门禁：当前红 vs 基线-豁免，新增匿名红点名阻断+待办）
- tests/control-tower/check-main-green.test.sh（D653 密封测试：新增红检测+豁免+降级）
- tests/control-tower/main-red-baseline.txt（D653 基线快照：CI 口径实测 5 文件 23 红 @ 93209c57）
- tests/control-tower/main-red-exemptions.txt（D653 豁免登记：带任务号/理由）
- scripts/pre-commit-check.sh（接线：D652/D653 各入一软门禁组，SYNO_CI=1 硬阻断）
- .github/workflows/ci.yml（接线：canary 清单 + test job 失败分支跑 check-main-green 归属判定）
- vitest.config.ts（D657 globalSetup + SYNOVA_DATA_DIR 临时目录）
- tests/global-setup.ts（D657：快照+白名单恢复现场）
- tests/zero-side-effect.test.ts（D657 三路径测试）
- task-state/D652.json（登记）
- task-state/D653.json（登记）
- task-state/D657.json（登记）
- memory/notes/implemented/2026-09-09-mac-quality-batch1-gates.md（铁律 49 Note）
- .claude/task-briefs/2026-09-09-D652-D653-D657-macq1.md（brief 自入库——*-auto.md 被 .gitignore:65 排除致 CI G12 无人认领全部误报的教训）
- docs/synova/audit-reports/D652-evidence-cited-batch-20260909/legacy-bad-citations.txt（存量坏引用清单）
- docs/synova/audit-reports/D652-evidence-cited-batch-20260909/README.md（交付证据落盘）

不做什么：
- 不改 src/loops/loop-scheduler.ts（D657 写入方源码不动——恢复现场在测试层）
- 不改 packages/evolution/src/global-analyzer.ts（同上，writeIndustryThresholds 不动）
- 不改 src/config.ts（SYNOVA_DATA_DIR 支持已存在，D657 只消费）
- 不改 scripts/audit/audit-check.py（K3 红线：审计脚本永禁触碰）
- 不改 scripts/control-tower/check-ci-stale-red.sh（CI run 红灯监测保持独立，D653 新建不重构）
- 不改 tests/evolution/global-analyzer.test.ts（触发写入的既有测试不改——污染由 globalSetup 恢复）
- 不改 tests/control-tower/ct-test-gate.sh（配对门禁机制复用不修改）

## Q3: 验收 — 入口 → 交互 → 结果

入口（从哪触发）：git commit（pre-commit 新组）/ push 后 CI（canary + test job 失败分支）/ npx vitest run（globalSetup）
处理（中间步骤）：check-evidence-cited.sh 解析触碰的 task-state/*.json 全部 *evidence* 键值 + audit.report → 提取仓库路径 token → git ls-files 校验跟踪态；check-main-green.sh 读当前失败列表 vs 基线−豁免 → 差集点名；global-setup.ts 测试前快照 git status → teardown 恢复白名单污染
结果（最终展示在哪）：非法引用 → commit 被拒并点名文件+路径；新增匿名红 → CI 阻断+点名+待办文件；全量测试后 git status 零测试污染

## 架构层: 基础设施
- 控制塔门禁域 scripts/control-tower/（不属 L1-L5 业务层）
- 接线点 scripts/pre-commit-check.sh 新组 + .github/workflows/ci.yml
- D657 位于测试基建（vitest.config.ts + tests/global-setup.ts）

#CRITERIA: A

## Done 标准
- [ ] D652 门禁脚本存在且三态：verify: bash -n scripts/control-tower/check-evidence-cited.sh && grep -q "exit 2" scripts/control-tower/check-evidence-cited.sh
- [ ] D652 测试绿（三路径+接线断言）：verify: bash tests/control-tower/check-evidence-cited.test.sh
- [ ] D652 pre-commit 接线（SYNO_CI 硬阻断组）：verify: grep -n "check-evidence-cited" scripts/pre-commit-check.sh
- [ ] D653 基线快照入库且非空：verify: test -s tests/control-tower/main-red-baseline.txt && grep -q "93209c57" tests/control-tower/main-red-baseline.txt
- [ ] D653 测试绿（新增红检测+豁免+降级）：verify: bash tests/control-tower/check-main-green.test.sh
- [ ] D657 globalSetup 接线：verify: grep -n "global-setup" vitest.config.ts && test -f tests/global-setup.ts
- [ ] D657 三路径测试绿：verify: npx vitest run tests/zero-side-effect.test.ts
- [ ] D657 全量后零污染：verify: CI=true npx vitest run 后 git status --porcelain 无 extensions/industries/.codex 新增（交付报告附实测输出）
- [ ] ci.yml canary 接线：verify: grep -c "check-evidence-cited.test.sh\|check-main-green.test.sh" .github/workflows/ci.yml
- [ ] 交付前全链：verify: SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh && bash scripts/control-tower/simulate-ci.sh
