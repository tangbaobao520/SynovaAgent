# Task Brief: D726 产品真红-状态机健壮性-产证命名

> 生成: 2026-09-13 | 任务: D726 | 认领: DeepSeek Harness（synova-dsh）
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务全在**控制塔 / 产品线仪表盘工具链**（scripts/，L1-L5 之外的基建层），不碰五层产品代码。
涉及三个既有模块：
- 产品完成度状态机 `scripts/product-lines/calc-progress.py`（六态：uncommitted/failed/pending_k3/verified/rejected/stale）——创始人驾驶舱的唯一算分口。
- 机器证据入库 `scripts/product-lines/evidence-writer.py`（A2）——双机（Mac/Win）+ CI 三源落盘。
- 任务号分配器 `scripts/control-tower/alloc-task-id.sh`（D384 唯一取号入口，含 brief 骨架生成）。
另一件（①）是既有测试 `tests/l3/graphbridge-wiring.test.ts` 真红的**归因判定**，修复面在 tests/。

### b) 文件审计（file:line 实测，全部 grep 自代码，非记忆）
- ① `tests/l3/graphbridge-wiring.test.ts:53-80` 第二个用例真红（实测 `expected +0 to be 1`）。
  - `src/l4/graph-bridge.ts:81` `createGraphBridge` **改写注入的 store.createNode**；`:90` `props.observed_at = ...` 无空值保护。
  - `src/orchestrator/module-runner.ts:122-126` afterRun 被调用（含 catch 降级）。
  - 生产适配器 `src/adapters/sqlite-graph-store.ts:103` **无 `createNodes`**（grep 该方法零命中）；注入点 `src/server.ts:261`。
  - `src/agent/post-diagnosis-processor.ts:151` 是 `upsertFromHONA` 唯一生产消费方。
- ② `scripts/product-lines/calc-progress.py:176-178` 分桶；`:198-209` k3 段（fail 优先 + `(date, at)` tiebreaker）；`:215-232` machine 段（`max(key=date)`，无 at、无 fail 优先）。
  - 列表顺序来源 `:83` `sorted(evidence_dir.glob("*.json"))` = 文件名字典序；`max` 同分取列表中首个 → 字典序决胜。
  - 真实触发案例（实测）：验收点 `1-4` 同日两条机器证据结论冲突——`evidence/D713-published-artifact-20260913.json`(fail) 与 `evidence/scenario-2026-09-13-1.json`(pass)，当前结果 `failed` 系 `'D'<'s'` 巧合。
- ③ `scripts/product-lines/evidence-writer.py:69-74` 文件名 `<type>-<date>[-n]`，无机器维度；`-n` 只在本地目录内递增 → 双机同日同类必撞名。
- ④ `scripts/control-tower/alloc-task-id.sh:210` 骨架已含 `#CRITERIA: A`（沙箱实测）；缺的是**回归网**（无测试钉住，改坏即静默）。
  - 存量残留：`.claude/task-briefs/` 下 D721-D740 共 20 份**未跟踪**骨架（mtime 14:47:37-14:47:46 < D718 守卫提交 14:55:50，属修复前历史残留）。

### c) 决策
- ① 复用既有测试文件，改测试替身 + 补接线断言（不新建文件）；产品缺陷（`createNodes` 缺失）**不在写集**，只判定+上报。
- ② 复用 calc-progress.py 既有分桶结构，只重写 machine 段选取规则；同步对齐 k3 的 `superseded_by` 过滤（实测当前数据零命中，零影响，属安全对齐）。
- ③ 扩展现有 `evidence-writer.py`：新增 `--machine`（默认按平台推断）+ 记录内 `machine` 字段；文件名保持 `<type>-<date>*` 前缀以兼容既有 glob 与 verify 命令。
- ④ 不改骨架内容（已正确），补回归网测试；清理 20 份未跟踪残留。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训

- 铁律 0-2/4/5：接线必须物理证明——① 的判据用「打点 afterRun 是否仍被调用」而不是读代码猜测。
- 铁律 24/31：降级必须 log + degraded 标记；② / ③ 的失败路径都要显式，不静默。
- 铁律 35：能变测试的不靠 review——② ③ ④ 全部补可执行回归网（含反向验证：拿掉修复必红）。
- 铁律 37：① 暴露的 `upsertFromHONA` 生产恒降级（死路径）属 dead code 类问题，登记上报。
- 铁律 48：测试覆盖正常 / 降级 / 边界三路径，禁止空壳。
- memory 教训：`memory/notes/implemented/2026-09-13-D718-ctl-minor-fixes.md:47-49` 记录了**同族缺陷**——「稳定排序 = 字典序」导致认领 tie-break 恒偏向旧 brief；本任务 ② 是同一根因（`max` 同分取列表中首个 = 字典序）在**另一处**复发，按「一类一机制」补显式语义序而非依赖排列顺序。
- memory 教训：`2026-09-13-D718-ctl-minor-fixes.md:73-75` 明确「brief 自身缺 `#CRITERIA`」为未闭环缺口 → ④ 补回归网。

参考：第一性原理（决胜规则必须由**语义**决定，不能由文件系统排列顺序决定）+ Anthropic 工程基线（fail-closed：同刻冲突取保守结论 fail 优先；契约必须可机器验证）+ DeepSeek 开源实证（Python `max` 文档明确同分返回首个遇到的最大元素 → 依赖输入顺序 = 未定义语义）→ 收敛：新增显式 `(date, at)` 排序 + 同刻 fail 优先，并补「改文件名不改变结果」的置换测试。

## Q2: 范围 — 正确的最简方案
做什么：
- tests/l3/graphbridge-wiring.test.ts — 修测试替身契约漂移（createNodes 丢 props）+ 补 afterRun 接线打点断言 + 补「store 不满足 GraphStore 契约时必须诚实降级」契约用例
- scripts/product-lines/calc-progress.py — machine 段对齐 k3 段语义：过滤 superseded_by + fail 优先 + (date, at) tiebreaker
- tests/control-tower/product-lines.test.py — 补 machine 段语义回归（同日冲突 / 文件名置换 / superseded_by / 降级）
- scripts/product-lines/evidence-writer.py — 产证文件名加机器维度 + 记录内 machine 字段
- scripts/product-lines/run-machine-evidence.sh — 契约注释同步（@output 文件名含机器维度）
- tests/control-tower/product-lines.test.py — 补 evidence-writer 机器维度与双机不撞车断言（同上文件）
- scripts/control-tower/alloc-task-id.sh — 骨架 #CRITERIA 行加「不可删」注释锚（防后续改动静默丢失）
- tests/control-tower/alloc-task-id.test.sh — 补骨架 #CRITERIA 存在性 + 可解析回归网
- .claude/task-briefs/2026-09-13-D721-并发测试-4.md — 删除未跟踪残留骨架（D721-D740 共 20 份，同一批次）
- memory/notes/implemented/2026-09-13-D726-product-red-and-state-machine.md — 决策 Note（铁律 49 要求）
- task-state/D726.json — 任务状态机登记（本任务）
不做什么：
- 不改 scripts/audit/（K3 红线，本任务不触碰任何审计脚本与审计标准）
- 不改 src/l4/graph-bridge.ts（产品缺陷 `createNodes` 缺失，须动 src/，超本任务写集 → 只判定+上报 CTO 派工）
- 不改 src/adapters/sqlite-graph-store.ts（同上，写集外）
- 不改 src/orchestrator/module-runner.ts（afterRun 接线实测完好，无需改）
- 不改 docs/synova/product-lines/product-lines.yaml（单一事实源，本任务只读）
- 不改 docs/synova/product-lines/evidence/ 下任何既有证据记录（只读消费，禁改写历史证据）

## Q3: 验收 — 入口 → 交互 → 结果
入口：① `npx vitest run tests/l3/graphbridge-wiring.test.ts`；② `python3 scripts/product-lines/calc-progress.py --out <tmp>`；③ `python3 scripts/product-lines/evidence-writer.py --type scenario ...`；④ `bash tests/control-tower/alloc-task-id.test.sh` + 真仓 brief 目录计数。
处理：① 打点计数 afterRun 调用次数与 payload → 与测试替身行为对照，判定红因归属；② 读 evidence/*.json 分桶 → 显式排序（date, at）→ 同刻 fail 优先 → 六态；③ 解析机器标识（--machine / 平台推断）→ 文件名加维度 + 记录内落字段；④ 骨架生成 → 断言 #CRITERIA 可被 brief_parser 解析。
结果：① 测试绿且红因有物理判据 + 产品缺陷有实测报错原文；② 同名/同刻证据冲突结果与文件名无关（置换不变）；③ 双机同日同类型产出两个不同文件名；④ 骨架 #CRITERIA 有测试钉住，真仓残留 20 份清零。

## 架构层:
scripts（控制塔 / 产品线工具链）
本任务不触碰 L1-L5 五层产品代码；唯一涉及产品代码的部分是 ① 的**只读判定**（src/ 不改）。

## Done 标准
- [ ] ① 判据落地且测试绿 — verify: `npx vitest run tests/l3/graphbridge-wiring.test.ts` → 3 passed（含 afterRun 打点断言）
- [ ] ① 产品缺陷有实测证据 — verify: `npx tsx .codex/control-tower/tmp/probe-real.ts` → `errors[0]` 含 `store.createNodes is not a function`（真 SqliteGraphStore）
- [ ] ② 机器段结果与文件名字典序无关 — verify: `python3 -m unittest tests.control-tower.product-lines -v` → 新增同日冲突/置换用例全绿
- [ ] ③ 双机同日同类不撞名 — verify: `python3 scripts/product-lines/evidence-writer.py --type scenario --date 2026-01-01 --verdict pass --points 9-9 --source t --machine mac --out-dir <tmp>` + 同命令 `--machine win` → 目录内两个不同文件名
- [ ] ④ 骨架 #CRITERIA 有回归网 — verify: `bash tests/control-tower/alloc-task-id.test.sh` → 含 #CRITERIA 断言且全绿
- [ ] ④ 存量残留清零 — verify: `ls .claude/task-briefs/ | grep -c '并发测试'` → 0
