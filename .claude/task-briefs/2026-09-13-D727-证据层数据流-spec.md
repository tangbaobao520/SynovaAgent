# Task Brief: D727 证据层数据流 spec

> 生成: 2026-09-13 | 任务: D727 | 认领: dev-doc（DSH，D336 四角色）
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）；派单 docs/synova/coordination/派单-第三批-D727-D731-20260913.md §一

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = 驻扎企业的 AI 诊断 Agent。本任务不是某一层的功能，而是**横切 L2→L3→L4→L5 的一条数据链规格**：证据从哪来（采集触发）→ 落到哪（表/字段/保留策略快照）→ 被谁引用（溯源关系）→ 怎么被查（查询口径）→ 失败怎么诚实降级。落点：L2 编排（采集触发，`agent/conversation-engine.ts` + `agent/diagnosis-launcher.ts`）+ L3 洞察（消费/引用装配）+ L4 本体（`src/evidence/` 数据面）+ L5 存储（SQLite 表）。**扩展 + 少量新建**，零新建架构。
### b) 文件审计（2026-09-13 基线实测 = origin/main @cff43d6f，逐条 grep 复核）
- `src/evidence/evidence-store.ts`（128 行）— `initSchema` 行 20-63（DDL 在行 22，**只由构造函数调用**）；`add` 行 66-80；`expireOld` 行 121-127（按 `collected_at` 删，**main 零调用方**）
- `src/evidence/index.ts`（186 行）— `EvidenceCollector` 行 18-49（`collectFromInterview` 行 32-48）；**`new EvidenceCollector` / `new EvidenceStore` 在 src/ 零命中**（8 处全在 tests/）
- `src/routes/diagnosis.ts:358` + `src/routes/conversations.ts:283` — `evidenceCollector: null` 硬编码（D489/D590 引入）→ 采集调用点 `conversation-engine.ts:625-629` 永不执行
- `src/agent/diagnosis-launcher.ts:129/134` — `evidenceSummary` **只写不读**（死变量，全仓 4 处命中）；`runConsultation` 在行 175
- `src/l3/synova-diagnosis-engine.ts:277-306` — `DiagnosisReport` **零 evidence 字段**（该文件 grep evidence 零命中）
- 生产库 `data/synova.db`：55 张表，**无 evidence 表**（`grep -ci evidence` → 0）；`graph_nodes WHERE type='Evidence'` → 0 行
- 同名异物：`scripts/product-lines/evidence-writer.py`（工程验收证据 JSON）↔ 诊断证据池——**另一个域**，本批只消歧不改
### c) 决策
已有覆盖 → 复用 `src/evidence/` 对象层 / `diagnosis_checkpoints` 报告落库 / bootstrap→`app.locals`→路由装配范式 / D725 已认领的 `evidence_refs` 引用表（**不重复建**）。无覆盖 → 新建最小件（采集侧降级契约 + 报告 provenance 装配 + 引用完整性规则 + 溯源验证口径）。冲突 → 一处：`expireOld` 时间谓词（`collected_at` vs `expires_at`）→ 裁决单口径 `expires_at`（表零行，零迁移成本）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 业界：W3C PROV（wasDerivedFrom 派生关系）/ OpenLineage·Marquez（血缘完整性先于丰富度）/ S3·GCS 生命周期（对象级策略快照，策略变更不追溯）。
- Anthropic 基线：fail-closed（悬空引用必须报错，不可静默跳过）+ 机器可验契约 + 降级显式可传播 + 单点写入。
- memory 教训：`dual-source-fraud`（双数据源，本仓证据概念实测 **5 路**）；`d6390bd1`「Wire Check ✅ EvidenceCollector=5 处引用」假接线（引用数 ≠ 生产装配，第 5 次接线失败）；M3 机制建成未接线（`expireOld` 家族）；M2 声称≠事实。
- 参考：Anthropic 工程基线 + 第一性原理（溯源 = 一条可被第三方重放的引用路径）+ 业界实证（PROV / OpenLineage / S3 生命周期）+ 结论：**唯一事实源 + 无条件可达 + 引用可解析 + 显式降级**。

## Q2: 范围 — 正确的最简方案
做什么：
- docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D727-evidence-chain-20260913.md — 本 spec（11 节 + 必答四问 + 写集缺口上报）
- docs/synova/coordination/编码指令-D728-证据链接线-Mac-20260913.md — D728（Mac 线）编码 session 启动指令
- task-state/D727.json — spec 段回填 + status=spec_done
- .claude/task-briefs/2026-09-13-D727-证据层数据流-spec.md — 本 brief

不做什么（含文件路径）：
- 不改 `scripts/audit/`（K3 红线）；不改 `.github/CODEOWNERS`（派单红线）；不写实现代码（实现归 D728/Mac）
- 不改 `src/sentinel/runner.ts`（哨兵合成证据落库 = 跨线改造）
- 不改 `src/l3/expert-dispatcher.ts` / `src/l3/quality-firewall.ts` / `src/l3/expert-output-schema.ts`（专家 evidenceRefs 校验 = L3 洞察线域）
- 不改 `src/l4/graph-bridge.ts`（本体 Evidence 节点写入 = 架构决策，不在本批）
- 不改 `src/l4/agent-memory-store.ts` / `src/l3/pkb-lifecycle.ts`（他人域，只读语义引用）
- 不改 `scripts/product-lines/evidence-writer.py` / `scripts/product-lines/calc-progress.py`（同名异物是另一个域的活系统）
- 不做 `src/store/schema-migration.ts` 迁移注册（P1 待办，避免双建表路径）

## Q3: 验收 — 入口 → 交互 → 结果
入口（从哪触发）：`POST /api/conversations`（GA 访谈每轮，Phase 0 采集）+ `POST /api/diagnosis/consult`（GA 发起诊断，读证据 + 挂 provenance）
处理（中间步骤）：bootstrap Phase 1 构造并注册 evidence 三件套 → `app.locals` → 路由注入 EngineContext（替换 `null`）→ Phase 0 `collectFromInterview` 落 `evidence` → 诊断读证据进输入（修死变量）→ 报告挂 provenance + 写 `evidence_refs` → 报告落 `diagnosis_checkpoints`
结果（最终展示）：`GET /api/diagnosis/consult/:consultId/report` 返回非空 `report.provenance.evidenceRefs`；sqlite3 三段查询逐条可解析；注入点回退 → 必红（R2）

## 架构层: L2 编排（采集触发）+ L3 洞察（消费/引用）+ L4 本体（数据面）+ L5 存储（表）；L1 只做装配注入（禁 import `/evidence/`）

## Done 标准
- [ ] verify: bash scripts/control-tower/dev-doc-gatekeeper.sh docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D727-evidence-chain-20260913.md → exit 0
- [ ] verify: python3 scripts/control-tower/devdoc_writeset.py --extract <本 spec> → status=ok, 条目数 = 4
- [ ] verify: bash scripts/workflow/check-dev-doc-write-set.sh <本 spec> → 漂移 0
- [ ] 必答四问（①根因 ②最小闭环 ③expires_at ④物理证明）在文内可定位且各有唯一答案（§4.3 / §5.4 / §5.5 / §5.6）
