# D738: `$VAR` 紧贴非 ASCII 常驻扫描 check（P1，红线条目）

> 派单: CTO「待分配号·B」—— 红线条目：**同类错误第二次 = 防线系统性失效**
> 取号: D738（经 `alloc-task-id.sh` + main ∪ #536 并集实分配）
> 背景: D370 记载 bash 在 `LC_ALL=C.UTF-8` 下把紧跟 `$VAR` 的全角标点并入**变量名**（变量即使已赋值也 unbound）；
>       批 A 修 4 处（check-pr-budget.sh×3 + check-ownership.test.sh×1），D735 开测**又 1 处**，
>       且**只在失败路径触发** —— 正常路径全绿，一到要报错就崩，门禁最需要说话时哑掉。
> 红线: 不改 scripts/audit/ ✅；不碰 ci.yml ✅

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔门禁基建。把一条**反复人工踩的坑**变成常驻机器检查（铁律 35：能变 check-*.sh 的不靠 review）。
### b) 文件审计
`grep -rl 'check-locale-var' scripts/` → 零命中（无同名能力，不撞车）。既有可复用惯例：pre-commit「as any 跳过注释行」（本扫描器同惯例跳过全行注释）；`PLATFORM-CHECKLIST #1` 的 PYBIN 三级探测。
### c) 决策
已有覆盖→复用 PYBIN 探测 + soft_check 语义。无覆盖→新建扫描器。冲突→无。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
业界: 把「只在异常路径才显形的缺陷」交给静态检查，而不是交给 code review —— 因为 review 看正常路径，异常路径没人读。Anthropic 工程基线: ① fail-closed 三态（0 通过 / 1 命中 / 2 检查本身失败）；② 机器可验契约 + 反向验证；③ 误报即噪音，噪音导致门禁链被绕过（V3.9 教训）→ 故明确**只跳全行注释**并把该边界写进契约与测试。memory: D370（本坑原始记载）、D520（`$VAR` 紧贴中文致 `unbound variable` 盖掉真实原因，全仓 45 处）、铁律 11/24/31。
Q1c 决策参考系: 参考 Anthropic（fail-closed + 机器可验契约）+ 第一性原理（「同类第二次」说明人工防线已失效，最低成本解 = 一条静态扫描）+ 开源实证（linter 类的 shellcheck 早有此规则，但本仓未接）。结论: 收敛 —— 自建扫描器 + soft_check 接线 + 反向验证测试。

## Q2: 范围 — 正确的最简方案
做什么：
- scripts/control-tower/check-locale-var.sh（新建：检出 `$VAR` 紧跟非 ASCII 且未加花括号；三态；无参=全仓 scripts/+tests/，有参=只扫传入文件）
- scripts/pre-commit-check.sh（改：**仅新增一个命名的检查块**，条件跳过——只在暂存区含 .sh 时跑，保持 <1s）
- tests/control-tower/check-locale-var.test.sh（新建：正常/边界/反向验证/接线）
- task-state/D738.json + 本 brief
不做什么：
- 不改 scripts/audit/audit-rules.sh（K3 审计红线，禁碰）
- 不改 .github/workflows/ci.yml（红区，批 C 归 CTO）
- 不改 scripts/pre-push-check.sh（本扫描器只接 pre-commit，避免双跑）
- 不改 tests/control-tower/check-orphan-tests.test.sh（D742 的产物，尚未创建；本任务不预造）
- 不改 scripts/control-tower/bypass-ledger.sh（反向验证样本，已还原；内容零净变更）

## Q3: 验收 — 入口 → 交互 → 结果
入口: `bash scripts/control-tower/check-locale-var.sh [FILE...]`；pre-commit 在暂存区含 .sh 时自动触发。
处理: PYBIN 探测 → python 逐行扫（跳过全行注释）→ 收集 `file:line  $VAR`。
结果: 命中清单 + 汇总；exit 0 无命中 / 1 有命中 / 2 检查执行失败。

## 架构层: 基础设施
控制塔门禁基建（scripts/control-tower/），不进运行时链路：不 import src/、不 import packages/、零跨层。

## Done 标准: 物理命令断言（每条可直接跑，exit 0 = 达标）
- [ ] DS1: 全仓扫描可跑并报残留: `bash scripts/control-tower/check-locale-var.sh` → 输出残留清单 + 扫描文件数（当前仓库实测 57 处 / 239 个 bash 文件），exit 1 属预期
- [ ] DS2: 反向验证必红: 干净样本（`scripts/control-tower/bypass-ledger.sh`）注入 `$VAR（` → exit 1 且点名 `bypass-ledger.sh:57  $src`
- [ ] DS3: 还原必绿: 还原后 exit 0 且 sha256 与注入前一致（`3a7752ea…`）
- [ ] DS4: `bash tests/control-tower/check-locale-var.test.sh` 全绿（14 项：正常/边界/反向验证/接线）
- [ ] DS5: 接线非死代码: `grep -c "check-locale-var.sh" scripts/pre-commit-check.sh` ≥ 1
- [ ] DS6: 本任务新增文件自身零残留（自扫通过）
- [ ] DS7: `SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh` + `bash scripts/control-tower/simulate-ci.sh` 均 exit 0
