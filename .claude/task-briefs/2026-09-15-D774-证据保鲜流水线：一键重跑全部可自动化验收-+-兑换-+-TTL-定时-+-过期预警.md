# Task Brief: D774 证据保鲜流水线：一键重跑全部可自动化验收 + 兑换 + TTL 定时 + 过期预警

> 生成: 2026-09-15 | 任务: D774 | 认领: synova-cto（并行 CTO session，域 mac）
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）
> 依据: docs/synova/coordination/编码指令-D774-evidence-pipeline-20260915.md（origin/docs/d777-batch5-product）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔域（scripts/product-lines/，非 src/ 产品代码）。现有链路：evidence-writer.py 写证据 →
calc-progress.py 六态判定（TTL 14 天 L67 + modules 变更失效 L33）→ refresh-all.sh 串联刷新。
缺口：**没有"重跑"通道**——证据只靠各任务交付时手写，14 天后全体 stale，无机制兜底。
本任务补第四环：rerun-evidence.sh（重跑）+ gen-expiry-warnings.py（预警）。

### b) 文件审计
- calc-progress.py:67 EVIDENCE_TTL_DAYS=14；:33 证据日期后 modules 变更即失效（不改）
- refresh-all.sh:42-49 A2 run-machine-evidence --skip-vitest 会无条件写 pass 证据（假绿风险，本任务加开关）
- run-machine-evidence.sh:112-119 写证据无同日去重（连跑两次产生 -1 重复文件，违反幂等验收）
- scripts/golden-scenarios/GS-*/run.sh ×8（场景证据写 scripts/golden-scenarios/evidence/GS-XX-date.json，
  assert.ts 同日同名覆盖=天然幂等）
- stale-reverify-D589-line1.json：tests/electron/ 全套件 → 点 1-1/1-3/1-4/1-5/1-6/1-7 的映射真值（先例）
- 实测（09-15）：164 点中 29 stale / 34 pending_k3 / 96 uncommitted

### c) 决策
复用：GS 场景 run.sh、evidence-writer.py、refresh-all.sh、run-machine-evidence.sh、D589 点映射。
新建：rerun-evidence.sh（编排器，不重写底层）、gen-expiry-warnings.py（派生预警，独立于 calc）。
不改：calc-progress.py 判分规则（保鲜≠改判分，硬约束）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 教训
- 铁律 11（静默降级禁止）：环境缺失（vitest 不可用）→ 显式 degraded + 不写 pass 证据（fail-closed）
- 铁律 24（异常分类）：ENOENT=正常默认 vs 执行失败=degraded 登记
- D328 三态退出码：0 过 / 1 有 fail / 2 检查本身降级（不与通过混同）
- D520 PLATFORM-CHECKLIST：PYBIN 三级探测（禁裸 python3）、禁 date +%s、禁 grep -P、UTF-8 头块、mktemp 沙箱
- M2（声称vs事实）：GS 场景证据 id 体系（L1-x/S0-x）与 yaml 点 id（1-x）断裂——calc 消费不到，
  本任务如实登记该缺口，**不编造映射**（编造=假绿）
- D734 PR 预算 ≤12 文件 + D758 证据目录 domain_neutral → 拆两 PR（机制 / 机器生成数据）
参考：Anthropic（fail-closed 门禁 + 单向数据流）+ 第一性原理（保鲜=定期重验，非改判分）+ 结论：编排器模式

## Q2: 范围 — 正确的最简方案
做什么：
- scripts/product-lines/rerun-evidence.sh — 新增：GS-01~08 逐项重跑（失败不中断）+ 线 1 vitest
  机器可验项 + A2 套件 + skip 清单 + 汇总 JSON + 自动 refresh-all + 前后六态对照；SYNO_RERUN_* 环境注入缝
- scripts/product-lines/gen-expiry-warnings.py — 新增：TTL 过期预警派生文件（只预警不改判分）
- scripts/product-lines/refresh-all.sh — 接 A9 预警步（+3 行）
- scripts/product-lines/run-machine-evidence.sh — 加 SYNO_A2_SKIP_WRITE 开关 + 同日去重（防假绿+防重复文件）
- scripts/product-lines/README.md — D774 用法 + 定时任务名 + 手动触发
- scripts/golden-scenarios/GS-05-alert-closure/run.sh — D370 全角紧贴修复（$CNT1（→${CNT1}）：
  重跑实测暴露场景在断言前崩溃（unbound variable）→ 修复后跑完断言、证据落盘。指令写集
  明确含 scripts/golden-scenarios/**（域 mac），本修复在该写集内
- .gitignore — GS 场景证据目录豁免（GSS 契约 git 跟踪，流水线每 7 天产物无需 -f）
- docs/synova/coordination/ownership.yaml — domain_neutral 加 `.gitignore`（CI D734 实证：
  .gitignore 命中 ** 兜底判 win → 本 PR 跨域红。各线共写共享文件 = neutral 定义，
  D758 evidence 目录豁免同型）
- tests/control-tower/rerun-evidence.test.sh — 三态（全绿/部分fail/环境缺失）+ 幂等 + 接线
- memory/notes/implemented/2026-09-15-d774-evidence-pipeline.md — Note（改 product-lines 必须）
- .claude/task-briefs/2026-09-15-D774-*.md + task-state/D774.json — 流程登记

不做什么：
- 不改 scripts/product-lines/calc-progress.py（TTL/失效规则冻结——保鲜≠改判分）
- 不改 scripts/audit/**（K3 红线）
- 不改 scripts/golden-scenarios/ 其他场景（GS-01/02/04 的断言失败是真实持续问题，
  属场景归属线另立修复任务，不在本流水线任务修——保鲜 ≠ 修产品）
- 不建 GS 场景点→产品点映射表（无既有真值，编造=M2 假绿；缺口如实登记）
- 不写 k3/founder_demo 类证据（审计点 1-8/2-7 等永远 skip——禁止自我审计）

## Q3: 验收 — 入口 → 交互 → 结果
入口：bash scripts/product-lines/rerun-evidence.sh（人 / 看板 7 天定时任务）
处理：GS 逐项重跑（exit code 逐项记录）→ vitest electron 套件 → A2 套件 → 汇总 → 自动 refresh-all
结果：证据文件落 git 目录 + rerun-evidence-summary-*.json（fresh/fail/skip/degraded 计数）+
终端打印刷新前后六态对照（stale↓ / pending_k3↑）+ exit 0/1/2 三态

## 架构层:
scripts（控制塔/产品线工具链，非 src/ L1-L5 产品代码）

## Done 标准
- [ ] verify: bash scripts/product-lines/rerun-evidence.sh → 汇总打印 + 证据文件出现 + exit 三态正确
- [ ] verify: 连跑两次 → 第二次零新增证据文件、汇总结论一致（幂等）
- [ ] verify: bash tests/control-tower/rerun-evidence.test.sh → 全断言过（三态+反向：伪造失败场景 → 汇总显式 fail + exit 1）
- [ ] verify: 看板定时任务可见 + 手动触发成功（贴任务名与下次运行时间）
- [ ] verify: 过期预警清单可读（贴前 5 行）；刷新前后六态对照表贴 PR
