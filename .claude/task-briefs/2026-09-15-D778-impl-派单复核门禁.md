# Task Brief — D778 派单复核门禁（impl 阶段：pre-commit 接线 + 测试）

> 2026-09-15 | 执行方: 并行 CTO session（synova-cto）| 基线: feat/d778-dispatch-review-gate（栈式于 docs/d778-review-gate / PR #570）

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
Synova 控制体系 = 门禁链（hook → verify → pre-commit 13 组 → pre-push）。本任务把「派单文档复核」从 CTO 习惯（pre-dispatch-check.sh 人工想起才跑）变成 pre-commit 物理环节。落在 scripts/control-tower/（门禁脚本域）+ scripts/pre-commit-check.sh（门禁链宿主）。

### b) 文件审计
- `scripts/control-tower/pre-dispatch-check.sh`（98 行，D771 已修全角边界）— 复核逻辑本体，本单**不改**（D771 刚修过，避免同域两次变更）
- `scripts/pre-commit-check.sh` 13 组 + CT-34 纯文档早退分支（L319）+ D734 附加块范式 — 接线宿主
- `tests/control-tower/pre-dispatch-check.test.sh`（8 断言基线）— 回归基线
- 复用（不新建同类）：check-pr-budget.sh（D734 附加块先例）、ct-test-gate.sh（配对测试约定）、check-notes-lifecycle.sh（CT-34 分支内补门禁先例 = D472）

### c) 决策
已有覆盖 → 复用 pre-dispatch-check.sh 全部逻辑，新写的只是「探测 + 调用 + 三态裁决」薄壳 check-dispatch-gate.sh。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训

- 参考：Anthropic/DeepSeek/第一性原理 + 结论（见下）
- 铁律 0-2（spec→test→impl→wire）：测试含接线 grep（check-dispatch-gate 真被 pre-commit 调用）
- 铁律 35（自动化优先）：门禁脚本独立成文件，可被 ct-test-gate 配对约束
- 铁律 11/24（降级显式）：exit 2 显式 degraded 留痕，不静默当绿
- D328 三态退出码 / D472（CT-34 早退分支内补门禁先例）/ D734（不占组号先例）/ V4.5.1（pre-commit 超时=--no-verify 教训，条件跳过 <1s）/ D521+D555（沙箱 git 剥 GIT_DIR/GIT_INDEX_FILE）
- windows-compat：UTF-8 头块 + core.quotepath=false（中文文件名 git 输出）+ grep 计数 tr 防 "0\n0"

## Q2: 范围 — 正确的最简方案

做什么：
- 新增 `scripts/control-tower/check-dispatch-gate.sh`（三态门禁薄壳：探测 staged 派单-*.md → 逐个跑 pre-dispatch-check → 三态裁决）
- 接线 `scripts/pre-commit-check.sh` 两处：CT-34 纯文档早退分支内 + 主流程（D734 块后）
- 【验收补丁, 创始人条件通过指令扩权】改 `scripts/control-tower/pre-dispatch-check.sh` ① 项：规则引用型 D#（按/参见/参考/依据/规则/规约/决策 + D#）豁免 task-state 校验，裸 D# 照旧校验（fail-closed）
- 新增 `tests/control-tower/check-dispatch-gate.test.sh`（绿/红×2/降级×2/边界×2/接线/CT-34 集成/性能）
- `memory/notes/implemented/2026-09-15-d778-dispatch-review-gate.md`（D534 Note）
- `task-state/D778.json` 回填 impl

不做什么（含文件路径）：
- pre-dispatch-check.sh 仅限「① 项规则引用豁免」一处改动（创始人验收指令扩权, 见上「做什么」）；其余逻辑不动（D771 修复区不触碰）
- 不改 scripts/audit/**（K3 红线）
- 不改 .github/workflows/ci.yml（CI 侧经 SYNO_CI strict 既有通道生效）
- 不改 .claude/skills/ 与 .dsh/skills/（交付物 3 已由 PR #570 完成，本单仅验证 sync --check）
- 不修 main 上 16 个 .sh 缺 UTF-8 头块的存量欠账（超 PR 预算，已登记 Note 待另派）

## Q3: 验收 — 入口 → 交互 → 结果

入口：git commit 时 staged 含 docs/synova/coordination/派单-*.md（本地 + CI SYNO_CI=1 两形态：纯派单提交走 CT-34 早退、混合提交走主流程）
处理：check-dispatch-gate.sh 逐文档跑 pre-dispatch-check（①任务号 ④写集 ⑥行号 ⑩计划锚定 ⑨自检段）
结果：SYNO_CI=1 下坏派单（如删「依据计划」行）→ ❌ 点名 D778 + exit 1 必红；恢复 → 绿；非派单文档 → 跳过不误拦；复核环境异常 → exit 2 degraded 显式留痕

## 架构层: 控制体系（scripts/control-tower + 门禁链，非 src/ L1-L5 产品代码）

## Done 标准: 至少一条可验证的完成标准

1. `bash tests/control-tower/check-dispatch-gate.test.sh` 19 断言全绿
2. 反向验证：删派单「依据计划」行 → SYNO_CI=1 pre-commit exit 1；恢复 → exit 0（三次原始输出入 PR）
3. `grep -n "check-dispatch-gate" scripts/pre-commit-check.sh` 命中（WIRE CHECK）
4. `bash scripts/workflow/sync-dsh-skills.sh --check` 通过
5. `time bash scripts/pre-commit-check.sh` ≤ 基线 3s + 5s

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/bypass.log | builtin（hook 运行期产物，自动豁免） |
| .claude/skills/cto-handover/SKILL.md | task |
| .claude/task-briefs/2026-09-15-D778-impl-派单复核门禁.md | task |
| .claude/task-briefs/2026-09-15-D778-review-gate.md | task |
| .dsh/skills/cto-handover/SKILL.md | task |
| docs/synova/coordination/编码指令-D778-派单复核门禁-20260915.md | task |
| memory/notes/implemented/2026-09-15-d778-dispatch-review-gate.md | task |
| scripts/control-tower/check-dispatch-gate.sh | task |
| scripts/control-tower/pre-dispatch-check.sh | task |
| scripts/pre-commit-check.sh | task |
| task-state/D778.json | task |
| tests/control-tower/check-dispatch-gate.test.sh | task |

