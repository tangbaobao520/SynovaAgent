# Task Brief: D778 交付物复核门禁 + CTO 交付三件套固化

> 生成: 2026-09-15 | 分支: docs/d778-review-gate | as any: 0
> 域: mac（.claude/.dsh 技能 + docs/synova/coordination + task-state；单域）
> 主线贡献: infra:交付质量门禁（创始人 2026-09-15「产出交付前必须复核，流程要固化」）
> 触发: 创始人质询「你写的派单文档是否符合过」

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
治理层：把 CTO 的「交付前复核」从**习惯**变成**门禁 + 成文规则**。当前 `pre-dispatch-check.sh` 只在人工想起时跑，没人跑也不拦；本文档把规则写进 CTO 必读技能，并把门禁实现派给并行 CTO（D778 编码指令）。

### b) 文件审计（实测）
- `scripts/control-tower/pre-dispatch-check.sh`：人工调用型脚本，**无任何门禁接线**（grep `pre-dispatch-check` 在 `scripts/pre-commit-check.sh` 零命中）
- 该脚本曾在第②项崩溃（D771 修复，PR #566）→ 第一遍复核结论不完整却像"跑过了"
- 复核结果依赖工作树：本机 main 停在 v1.0@0b5c396a，主线实际 v1.2@8d331729 → 假红/假绿
- 技能现状：`.claude/skills/cto-handover/SKILL.md` 有「派单前复核」段（2026-09-13 固化），但**无「交付三件套」与「汇报必含可转发指令」**

### c) 决策
规则成文（技能双写，立即生效）+ 门禁实现另派（D778 编码指令 → 并行 CTO）。**不在本单写代码**（创始人新规：CTO 不自己写代码）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = 三条规则进 CTO 技能（.claude/.dsh 逐字一致）+ 门禁编码指令落盘（含红/降级/边界测试与反向验证）。
② 测试 = 本单以 `sync-dsh-skills.sh --check` 与 pre-commit 为验收；门禁本身的测试由 D778 执行方交付。
③ 实现 = 文档 + 技能（本单）；门禁脚本（D778 执行方）。
④ 接线 = 技能是 CTO 开工必读（cto-handover 强制加载），规则即接线。
⑤ 验证 = SYNC-OK + pre-commit 全绿 + 编码指令含可转发全文。

引用依据：铁律 35（能变 check-*.sh 的不靠 review）· 铁律 0（协作对齐前置：规则要可引用）· M2（声称 vs 事实：**复核没跑就不能说跑过**）

### b) 本任务执行约束
- rule: "技能双写一致" verify: "bash scripts/workflow/sync-dsh-skills.sh --check → SYNC-OK"
- rule: "规则含三条 + 例外条款" verify: "grep -c '规则 [1-4]｜' .claude/skills/cto-handover/SKILL.md → ≥4"

### c) 决策参考系
参考：第一性原理（信任来自可核的物理输出，不来自承诺）+ Anthropic 基线（门禁化 > 自觉）→ 规则成文 + 门禁派单。

### d) 相关 Note 引用
无（未触治理脚本区；技能与协调文档）。

## Q2: 范围 — 正确的最简方案

做什么：
- `.claude/skills/cto-handover/SKILL.md` + `.dsh/skills/cto-handover/SKILL.md` — 新增「交付前三件套」四条规则（复核 / 同步树 / 可转发指令 / 例外显式）
- `docs/synova/coordination/编码指令-D778-派单复核门禁-20260915.md` — 门禁实现指令（派并行 CTO）
- `task-state/D778.json` — 本任务台账

不做什么：
- 不改 scripts/pre-commit-check.sh（由 D778 执行方改，含测试）
- 不改 scripts/control-tower/pre-dispatch-check.sh（D771 已修；本单不再动）
- 不改 scripts/audit/**（K3 红线）
- 不改 .github/workflows/ci.yml

## Q3: 验收 — 入口 → 交互 → 结果

入口：CTO 开工加载 cto-handover 技能 / 交付前跑复核
处理：规则进技能（必读）→ 执行方按编码指令落门禁 → 以后派单文档不合规直接拦
结果：交付物复核从"习惯"变"门禁 + 成文"；汇报格式含可转发指令

## 架构层: scripts（治理层：技能 + 协调文档 + task-state）；不触 L1-L5
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: verify: `grep -c "交付前三件套" .claude/skills/cto-handover/SKILL.md` → ≥1
- [ ] 链路走通: verify: `bash scripts/workflow/sync-dsh-skills.sh --check` → SYNC-OK
- [ ] 结果可见: verify: `grep -c "可以直接转发" .claude/skills/cto-handover/SKILL.md` → ≥1
- [ ] 门禁已派: verify: `test -f docs/synova/coordination/编码指令-D778-派单复核门禁-20260915.md && echo OK` → OK

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/skills/cto-handover/SKILL.md | task |
| .claude/task-briefs/2026-09-15-D778-review-gate.md | task |
| .dsh/skills/cto-handover/SKILL.md | task |
| docs/synova/coordination/编码指令-D778-派单复核门禁-20260915.md | task |
| task-state/D778.json | task |

