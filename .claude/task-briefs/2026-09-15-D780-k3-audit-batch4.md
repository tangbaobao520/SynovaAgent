# Task Brief: D780 K3 审计批次四（33 个 impl 无 audit + 本批新交付）

> 生成: 2026-09-15 | 分支: docs/d780-k3-audit-batch4 | as any: 0
> 域: mac（协调文档 + task-state；单域）
> 主线贡献: infra:审计吞吐（34 个 pending_k3 验收点变现的唯一通道）
> 触发: 创始人「我看到待办有一批待 K3 审计的，你是不是可以安排一批审计单子？」

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
审计线（K3 独占）。CTO 的职责 = **排单与供料**（选对象、给证据位置与复核命令、定节奏、回填口径），不碰审计标准、不预设结论。

### b) 文件审计（实测）
- task-state 实测：**33 个任务 `impl` 有 / `audit` 空**（含今日新交付 D751/D752/D754/D757/D758/D759）
- 产品线实测：**34 个验收点 `pending_k3`**（`product-progress.json`）——审计完即可 `→ verified`，是当前唯一能立刻抬高完成度的动作
- 反例：`calc-progress.py:67` TTL=14 天 → **审计拖久了被审证据会失效**（本批建议 7 天内完成）
- D757/D758/D759 的 task-state 当时缺 `impl`（我交付时未回写）→ 本单补齐（K3 无法审无 impl 的任务）

### c) 决策
分四组按优先排（A 今日新交付 / B 线 1 与证据链 / C 工程门禁 / D 治理流程）；D 组维持 2026-09-08 审计清单的「低优先合并粗审」口径。**只写派单，不写审计标准**。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = 每个审计对象都给「证据位置 + 复核要点（可重跑命令）」；A 组要求独立重跑。
② 测试等价物 = 派单文档过 `pre-dispatch-check.sh`（锚定计划 + 内部一致性自检）。
③ 实现 = 派单文档 + D780 台账 + 三个 `impl` 回写。
④ 接线 = 审计结论回填 `task-state.audit` → 看板与产品进度页自动反映（链路已存在）。
⑤ 验证 = 复核脚本全绿 + task-state 回写可读。

引用依据：D336 红线（开发者改过的门禁同样受审）· 计划 §五 原则 8（无证据=未完成）· M2/M4（审计必须独立重跑，不接受转述）

### b) 本任务执行约束
- rule: "A 组 6 项都给了复核命令" verify: "grep -c 'npx tsc\|vitest run\|check-pr-budget\|check-sentinel-type-net' docs/synova/coordination/审计派单-批次四-K3-20260915.md → ≥5"
- rule: "不动审计标准" verify: "git -c core.quotepath=false diff --name-only origin/main...HEAD | grep -c 'scripts/audit/' → 0"

### c) 决策参考系
参考：第一性原理（进度 = 证据 × 独立复核；证据有 TTL ⇒ 审计要快）+ Anthropic 基线（分批、可回填、可追踪）→ 四组分批 + 7 天窗口。

### d) 相关 Note 引用
无（协调文档 + task-state）。

## Q2: 范围 — 正确的最简方案

做什么：
- docs/synova/coordination/审计派单-批次四-K3-20260915.md — 审计派单（四组对象 + 复核要点 + 交付要求 + 节奏 + 边界）
- task-state/D780.json — 本批次台账
- task-state/D757.json / D758.json / D759.json — 补 `impl` 回写（含证据 4 条）

不做什么：
- 不写/不改任何审计标准（`scripts/audit/**` 与 `AUDIT-PROTOCOL.md` 属 K3）
- 不预设任何裁决（不写"预计通过"）
- 不改 scripts/control-tower/**、不改被审产物
- 不改 .github/workflows/ci.yml

## Q3: 验收 — 入口 → 交互 → 结果

入口：K3 session 按派单取单；CTO 复核脚本
处理：K3 独立重跑 → 出报告 → 回填 `task-state.audit` + FAIL 进台账
结果：`pending_k3` 点下降、`verified` 上升；看板可见裁决

## 架构层: scripts（治理层：协调文档 + task-state）；不触 L1-L5
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: verify: `bash scripts/control-tower/pre-dispatch-check.sh docs/synova/coordination/审计派单-批次四-K3-20260915.md` → 机械项全通过
- [ ] 链路走通: verify: `python3 -c "import json;print(json.load(open('task-state/D757.json'))['impl']['commit'])"` → 打印 commit（回写生效）
- [ ] 结果可见: verify: `grep -c "pending_k3" docs/synova/coordination/审计派单-批次四-K3-20260915.md` → ≥1
- [ ] 红线未越: verify: `git -c core.quotepath=false diff --name-only origin/main...HEAD | grep -c "scripts/audit/"` → 0

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/task-briefs/2026-09-15-D780-k3-audit-batch4.md | task |
| docs/synova/coordination/审计派单-批次四-K3-20260915.md | task |
| task-state/D757.json | task |
| task-state/D758.json | task |
| task-state/D759.json | task |
| task-state/D780.json | task |

