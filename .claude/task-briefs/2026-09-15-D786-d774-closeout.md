# Task Brief: D786 D774 收口补丁派单（刷新产物落 main + 自动通道抢救）

> 生成: 2026-09-15 | 分支: docs/d786-d774-closeout-dispatch | as any: 0
> 域: mac（协调文档 + 计划版本 + task-state；单域）
> 主线贡献: infra:仪表盘真假（产品可见性 + 进度产物自动刷新通道）
> 触发: 创始人追问「必须马上办的一项」——CTO 验收 D774 时实测出「兑换成果没进 main」并查到系统性根因

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
治理层 + 产品可见性层：`docs/synova/product-lines/product-progress.json` 是 26 线进度页与看板的**真相源产物**；它由 `scripts/product-lines/calc-progress.py` 生成、`refresh-all.sh` 刷新。本任务 = 把刷新后的真相落进 main（补 D774 的缺口），并修复"自动刷新"这条早已断掉的链。

### b) 文件审计（全部实测）
- main 产物过期：`git show origin/main:…/product-progress.json` → `generated_at 2026-09-12 23:41:24`、`pct 1`、`stale 29 / pending_k3 34`
- 本机实跑真相：`rerun-evidence.sh` → `before stale=29 pending_k3=34` → `after stale=27 pending_k3=36`
- 自动通道：`pulls/404`（author `github-actions[bot]`，head `auto/product-progress`，`mergeable_state=blocked`，`check-runs=0`，`statuses=0`，创建 2026-09-07）；`pulls/403`（head `auto/dashboard`，同症状）
- 根因：`branches/main/protection` 要求 8 个必需检查；`.github/workflows/dashboard-auto.yml:56` 用 `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` 开 PR → GITHUB_TOKEN 触发的事件不触发 workflow → 必需检查永不报告
- 产物路径实测：`docs/synova/product-lines/rerun-evidence-summary-2026-09-15.json` 存在；`gen-expiry-warnings.py --out` 默认 `docs/synova/product-lines/evidence-expiry.json`（实测 `expired 2 / expiring 15`）

### c) 决策
分两件派：T1 一次性落盘（今天就做，恢复产品可见性）；T2 通道修复 + 看门狗（防再断）。**不在本单写代码**（CTO 不自己写代码 → 派并行 CTO）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = T1 验收：main 上 `generated_at` 为当天且六态 = stale 27 / pending_k3 36；T2 验收：下一轮 PR 能拿到 check-runs（或显式改为人工合并并文档化）。
② 测试等价物 = 派单文档过 `pre-dispatch-check.sh`（本单已跑，见 PR）。
③ 实现 = 文档 + 计划版本补正 + D786 台账（执行由 T1/T2 完成）。
④ 接线 = 产物落 main 后，看板/进度页消费端自动反映（链路已存在）。
⑤ 验证 = T1/T2 的原始输出。

引用依据：铁律 35（能变 check 的不靠 review：本单 T2 就是"把断链变成可见告警"）· M4（执行证据链断裂：产物没进 main = 换了个人就看不见）· M2（声称 vs 事实：本次实测推翻"自动刷新在跑"的隐含假设）

### b) 本任务执行约束
- rule: "计划版本与内容一致" verify: "grep -m1 '版本: v1.3' 计划文档 → 命中（#568 改了内容未改版本号，本单补正）"
- rule: "派单可过复核" verify: "bash scripts/control-tower/pre-dispatch-check.sh <派单文档> → 机械项全通过"

### c) 决策参考系
参考：第一性原理（看板的可信度 = 产物是否新鲜 + 是否能自动更新）+ Anthropic 基线（把静默断链变成可见告警）→ T1 落真相、T2 建看门狗。

### d) 相关 Note 引用
无（协调文档 + task-state + 计划版本行；未触治理脚本区）。

## Q2: 范围 — 正确的最简方案

做什么：
- docs/synova/coordination/派单-D774-收口补丁与仪表盘通道抢救-20260915.md — 派单（事实 F1-F7 + T1/T2 + 硬约束 + 验收 + 自检）
- docs/synova/coordination/整体推进计划-主线-20260913.md — 版本 v1.2 → v1.3（补正 #568 的内容改动未同步版本号）+ 变更记录一行
- task-state/D786.json — 本派单台账

不做什么：
- 不改 scripts/product-lines/calc-progress.py（判分规则红线）
- 不改 scripts/product-lines/rerun-evidence.sh（D774 已交付，本单只派"用它"）
- 不改 .github/workflows/**（由 T2 执行方改并在 brief Q2 声明）
- 不改 scripts/audit/**（K3 红线）

## Q3: 验收 — 入口 → 交互 → 结果

入口：并行 CTO session 按派单取单 → 跑流水线 → 落产物 → 修通道
处理：产物进 main → 看板/进度页消费 → 数字 = 真相
结果：创始人看到的完成度反映真实状态（stale 27 / pending_k3 36），且以后自动刷新不再静默断链

## 架构层: scripts（治理层：协调文档 + 计划 + task-state）；不触 L1-L5
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: verify: `bash scripts/control-tower/pre-dispatch-check.sh docs/synova/coordination/派单-D774-收口补丁与仪表盘通道抢救-20260915.md` → 末行「✅ 机械项全通过」
- [ ] 链路走通: verify: `grep -m1 -oE '版本: v1.3' docs/synova/coordination/整体推进计划-主线-20260913.md` → 命中
- [ ] 结果可见: verify: `grep -c "generated_at 2026-09-12" docs/synova/coordination/派单-D774-收口补丁与仪表盘通道抢救-20260915.md` → ≥1（事实已具名可核）
- [ ] 红线未越: verify: `git -c core.quotepath=false diff --name-only origin/main...HEAD | grep -c "scripts/product-lines/calc-progress.py"` → 0

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/task-briefs/2026-09-15-D786-d774-closeout.md | task |
| docs/synova/coordination/整体推进计划-主线-20260913.md | task |
| docs/synova/coordination/派单-D774-收口补丁与仪表盘通道抢救-20260915.md | task |
| task-state/D786.json | task |

