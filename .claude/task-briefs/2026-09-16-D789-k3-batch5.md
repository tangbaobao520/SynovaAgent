# Task Brief: D789 K3 审计批次五派单（门禁/度量/流水线/防线）

> 生成: 2026-09-16 | 分支: docs/d789-k3-batch5 | as any: 0
> 域: mac（协调文档 + task-state；单域）
> 主线贡献: infra:审计吞吐（K3 当前空转；机制类交付优先审计）
> 触发: 创始人「除了 K3 的审计未派出，其他的都在执行中」→ CTO 立即排单

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
审计线排单：把「本日新落地的机制类交付」交给 K3 独立复核（门禁/度量改的是判据，影响所有后续 PR，优先级最高）。

### b) 文件审计（实测）
- 批四已交付并 PR #572 就绪（我解冲突后）；K3 无在跑审计 → 空转
- 本日新落地：D782 part1（#593，`scripts/pre-commit-check.sh` + `.github/workflows/ci.yml` + `tests/doc-system/*` 入 canary）、D774（#575-#582）、D786 T1/T2（#590/#591/#592）、D778（#574 待合）、D788（执行中）、D787（待实现）
- 复核要点所需命令已逐条写进派单（tsc/vitest/门禁脚本/双模式真跑）

### c) 决策
按影响面排四组（门禁与度量 → 流水线与可见性 → 防线接线 → 三机制）；未落地项不跳过，改为"等 PR 合后再审"。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC = 每组每个对象都有「证据位置 + 可跑命令 + 反向要求」。
② 测试等价物 = 派单过 `pre-dispatch-check`（本单已跑）。
③ 实现 = 派单 + D789 台账。
④ 接线 = 审计结论回填 task-state（链路已存在）。
⑤ 验证 = K3 出 PASS/FAIL 后由 CTO 复核并落台账。

引用依据：D336 红线（开发者改过的门禁同样受审）· 计划 §五 原则 8（无证据=未完成）· 批四 P2 已固化的「数字口径: <sha>（+环境）」

### b) 本任务执行约束
- rule: "派单可过机械复核" verify: "bash scripts/control-tower/pre-dispatch-check.sh <派单文档> → 全部通过"
- rule: "不碰审计领地" verify: "git diff --name-only origin/main...HEAD | grep -c 'scripts/audit/' → 0"

### c) 决策参考系
参考：第一性原理（改判据的交付风险最高，先审）+ Anthropic 基线（分批、可回填、可追踪）→ 四组分日推进。

### d) 相关 Note 引用
无。

## Q2: 范围 — 正确的最简方案

做什么：
- docs/synova/coordination/审计派单-批次五-K3-20260916.md — 派单（四组对象 + 复核要点 + 交付要求 + 节奏 + 自检）
- task-state/D789.json — 本批台账

不做什么：
- 不写/不改任何审计标准（`scripts/audit/**` 属 K3）
- 不预设任何裁决
- 不改被审产物（D778/D788/D774/D786/D782）
- 不改 scripts/pre-commit-check.sh（D787/D788 执行方在改）

## Q3: 验收 — 入口 → 交互 → 结果

入口：K3 按派单取单
处理：独立重跑 → 报告 → 回填 audit + FAIL 进台账
结果：看板/进度页反映真实裁决；门禁变更被独立验证

## 架构层: scripts（治理层：协调文档 + task-state）；不触 L1-L5
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: verify: `bash scripts/control-tower/pre-dispatch-check.sh docs/synova/coordination/审计派单-批次五-K3-20260916.md` → 末行「✅ 机械项全通过」
- [ ] 链路走通: verify: `grep -c "A 组\|B 组\|C 组\|D 组" docs/synova/coordination/审计派单-批次五-K3-20260916.md` → ≥4
- [ ] 结果可见: verify: `grep -c "数字口径" docs/synova/coordination/审计派单-批次五-K3-20260916.md` → ≥1
- [ ] 红线未越: verify: `git diff --name-only origin/main...HEAD | grep -c "scripts/audit/"` → 0

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/task-briefs/2026-09-16-D789-k3-batch5.md | task |
| docs/synova/coordination/审计派单-批次五-K3-20260916.md | task |
| task-state/D789.json | task |

