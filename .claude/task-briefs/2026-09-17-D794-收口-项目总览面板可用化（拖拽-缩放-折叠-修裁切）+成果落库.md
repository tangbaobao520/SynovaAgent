# Task Brief: D794 收口 — 项目总览面板可用化（拖拽/缩放/折叠/修裁切）+ D794 成果落库

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
L1 交互层（DSH Web GUI 插件），非产品代码。Synova 的「项目总览」是创始人唯一的管理可视化入口：
git 真相（task-state + product-lines + 证据）→ D795 派生 ledger.json → D794 插件渲染进 DSH 侧边栏。

本次两件事：
1) **落库**：上一轮 D794 成果（左栏入口 + 中央面板 + 三级取数路由 + 全局挂载安装脚本）至今仍是
   主工作区未提交状态 —— origin/main 上仍是旧的右栏版插件。本分支一并落库。
2) **可用化**：面板存在可用性缺陷（区块被 flex 压缩裁切/重叠；无拖动、缩放、折叠）。

### b) 文件审计
`dsh/plugins/synova-dashboards/` 现有：`lib/{client,collect,index,ledger}.js`、`cordis.patch.yml`、
`scripts/install-dashboards.sh`、`test/{ledger,host-route,client-panel}.test.js` + `test/harness.js`。
复用：`lib/client.js` 的 `.spo-*` 样式与面板组件；`test/harness.js` 的迷你 React 渲染夹具（可跑浏览器插件）。
无冲突：本次只改 client.js（Client 半）+ 测试 + README，不动 Host 半路由与取数逻辑。

### c) 决策
已有覆盖 → 复用（不新建插件、不新建入口）。缺陷根因已知（flex 收缩 + 缺 max-height）→ 定点修复。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训

- **业界**：浮动面板用 Pointer Events（`setPointerCapture` 统一鼠标/触控）+ 拖拽/缩放期间只改
  transform/尺寸、松手才落盘；边界 clamp 到视口内、给定 min/max。用户偏好经 localStorage 持久化，
  键带版本号（`synova.pm.panel.v1`）以便将来改结构不读脏数据。
- **Anthropic 决策链**：spec → test → impl → wire → review。先定「子项不被压缩」「记忆可复现」的断言，
  再改实现。
- **memory / 铁律**：
  - 铁律 37（dead code 入仓库即违规）→ 改造时清理不再使用的样式/分支。
  - 铁律 24/31（降级必须显式）→ 读写 localStorage 失败不得抛出，降级为「不记忆」而非白屏。
  - 铁律 48（测试不可空壳）→ 新增用例必须有真实断言。

## Q2: 范围 — 正确的最简方案

做什么：
- A 修裁切/重叠：`.spo-sec` 等 `.spo-body` 子项 `flex:none`；列表类区块 `max-height:40vh` + `overflow-y:auto`；
  断言明细行默认收起、点击展开（已有 expanded 状态，确认默认收起）。
- B 拖动 + 缩放：标题栏为拖拽区，右下角 resize 手柄；min 720×480，max 视口-32px；
  位置/尺寸写 localStorage（键 `synova.pm.panel.v1`）。
- C 折叠：每个区块标题右侧折叠开关（▸/▾），折叠态记 localStorage；
  「26 线总览 / 执行看板 / 健康 / 时间轴」四块均可独立折叠。
- D 测试：现有 28 条全绿 + 新增 3 条（尺寸位置记忆 / 折叠记忆 / 子项不被 flex 压缩）。

不做什么（含文件路径）：
- 不改 `lib/index.js` 路由与 `lib/ledger.js` 三级取数（本次不碰数据层）。
- 不改 `lib/collect.js`。
- 不碰 `src/**`、`scripts/audit/**`、`scripts/pre-commit-check.sh`、`.claude/` 下控制塔配置。
- 不提交 `docs/synova/project/ledger.json`（属 D795 写集，非本卡范围）。
- 不改任务看板插件与右栏（右栏已在上一轮删除）。

## Q3: 验收 — 入口 → 交互 → 结果

入口：DSH Web 左侧栏「项目总览」图标（`sidebar.panellist`，全局可见）。
交互：标题栏拖动移动面板；右下角手柄缩放；各区块标题右侧 ▸/▾ 折叠；26 线列表独立滚动。
结果：刷新浏览器后位置/尺寸/折叠态保持；26 线列表可滚到底、无区块重叠。

## 架构层: L1

## Done 标准

- [ ] `node --test test/*.test.js` → pass 31 / fail 0（现有 28 条全绿 + 新增 3 条）
- [ ] 验收① 拖动/缩放生效，且刷新后位置尺寸保持（localStorage `synova.pm.panel.v1`）
- [ ] 验收② 「26 线总览 / 阻塞清单 / 执行看板 / 健康 / 时间轴」可独立折叠（`synova.pm.collapse.v1`）
- [ ] 验收③ 26 线全列表可滚到底、无区块重叠（子项 flex:none + .spo-list 自带滚动）
- [ ] 验收④ 原始输出齐全；截图由创始人重启后补（本机无浏览器自动化，见「未做/开口」）
- [ ] 验收⑤ 分支已 push + compare 链接已回报
- [ ] 反向验证：分别破坏「子项 flex:none」「折叠记忆」「尺寸记忆」→ 测试必须变红（实测各 1 红）
- [ ] 红线自查：未碰 `src/**`、`scripts/audit/**`；未提交 D795 的 `docs/synova/project/ledger.json`

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/task-briefs/2026-09-17-D794-收口-项目总览面板可用化（拖拽-缩放-折叠-修裁切）+成果落库.md | task |
| dsh/plugins/synova-dashboards/README.md | task |
| dsh/plugins/synova-dashboards/cordis.patch.yml | task |
| dsh/plugins/synova-dashboards/lib/client.js | task |
| dsh/plugins/synova-dashboards/lib/index.js | task |
| dsh/plugins/synova-dashboards/lib/ledger.js | task |
| dsh/plugins/synova-dashboards/package.json | task |
| dsh/plugins/synova-dashboards/scripts/install-dashboards.sh | task |
| dsh/plugins/synova-dashboards/test/client-panel.test.js | task |
| dsh/plugins/synova-dashboards/test/harness.js | task |
| dsh/plugins/synova-dashboards/test/host-route.test.js | task |
| dsh/plugins/synova-dashboards/test/ledger.test.js | task |
