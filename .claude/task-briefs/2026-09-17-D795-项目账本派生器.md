# Task Brief: D795 项目账本派生器（gen-project-board.py → ledger.json）

> 派单: `docs/synova/coordination/派单-D794-D795-项目总览可视化-20260917.md` §B
> 上游规格: `docs/synova/project/26线-V1验收标准-草案v0.1-20260917.md`（125 条 V1 断言）
> 分支: `feat/d795-project-board`（stacked on `docs/v1-dod-draft-20260917` / PR #608 —— 上游规格尚未并入 main）

## 项目身份

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
本卡是**项目管理基建**，不进产品运行时：把 git 真相（task-state + product-lines + 证据）派生为
`ledger.json`，供 D794 插件在 DSH 侧边栏渲染「项目总览」。原则：**真相在 git，派生只读，每块独立降级**。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
- 层归属: **基础设施 / 控制塔派生层**（`scripts/`，不进 L1-L5 产品代码，不 import `src/**`）
- 现有模块: `scripts/product-lines/calc-progress.py`（旧判分：把"做没做完 / 证据新不新 / 在不在验"压成一个数，
  TTL 到期即扣完成度 → 完成度不单调，19-2 实证 1%→0%）；`scripts/product-lines/productline_yaml.py`（仓内纯 Python
  YAML 子集解析器，PyYAML 本机不可用）
- 本任务: **新增**独立派生器 `scripts/project/gen-project-board.py` + 五组测试，产出 `ledger.json`
- 关系: 复用 `productline_yaml`（只 import 不改）；与 `calc-progress.py` **并存不冲突**（后者零改动）

### b) 文件审计
- `ls scripts/project tests/project` → 两者均**不存在**（本卡首次建立，无冲突）
- `grep -rn "gen-project-board\|project-ledger" scripts/ docs/ tests/` → 零命中（无同名既有实现）
- 输入文件实测存在:
  - `task-state/*.json` → 261 张卡；实测 **0 张**有 `blocked` 对象（三要素口径下阻塞数=0）
  - `docs/synova/product-lines/product-lines.yaml` → 26 线 / 164 验收点
  - `docs/synova/project/26线-V1验收标准-草案v0.1-20260917.md` → 实测 **125 行断言**（`| N-M |` 行），
    证据列分布 scenario 72 / k3 31 / test 15 / founder-demo 7 = 125
  - `docs/synova/product-lines/evidence/*.json`（53 项）+ `scripts/golden-scenarios/evidence/*.json`
    → 两处证据记录 schema 均为 `{schema:1, record_type, date|at, verdicts:[{acceptance_point, verdict}]}`

### c) 决策
- 无既有覆盖 → **新建**（非文件驱动架构，属控制塔脚本，不涉及 expert/sentinel manifest）
- 复用而非重写: YAML 解析复用仓内 `productline_yaml`（零新增三方依赖，满足派单硬约束）
- 不取消任何既有资产: `calc-progress.py` / `product-progress.json` 保持原样

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC/Done 标准 → ② 测试先行（`tests/project/gen-project-board.test.sh` 五组）→ ③ 实现
→ ④ 接线（真实仓库跑出 ledger.json，D794 消费）→ ⑤ 自检 5 问

引用依据:
- 铁律 0-2: spec → test → impl → wire → review → merge（先写测试再写实现）
- 铁律 7/4/5: 入口可触达 + 链路走通 + 结果可见（ledger.json 被 D794 的 `GET /synova/pm/ledger` 消费）
- 铁律 24/31: ENOENT 与坏 JSON 分开处理 + `degraded_sources[]` 显式传播，不静默填 0
- 铁律 47/48: 先写契约（JSDoc @input/@output/@degraded）再实现；测试非空壳（五组含反向 + 边界）
- 铁律 33: 测试命名 `*.test.sh`；记忆教训: D793 Note「TTL 到期即扣完成度 → 完成度不单调」为口径变更根因

### b) 本任务执行约束
- rule: "ledger.json 必须由脚本生成，禁手工编辑"
  verify: `grep -rn "generated_by" docs/synova/project/ledger.json`
- rule: "证据龄只计数不扣交付度（口径红线）"
  verify: `python3 -c "import json;d=json.load(open('docs/synova/project/ledger.json'));t=d['totals'];assert t['v1_passed']>=t['freshness']['red']"`
- rule: "两处证据目录都计入，且 sources 标明来源"
  verify: `grep -rn "golden-scenarios/evidence" docs/synova/project/ledger.json`
- rule: "calc-progress.py 零提交"
  verify: `git log --name-only --pretty=format: -1 | grep -c "calc-progress.py" | grep -qx 0`

### c) 决策参考系
参考：第一性原理 + Anthropic 工程基线 + DeepSeek/开源实证 + 收敛检查 + 结论
- ① 第一性原理: 根因是"一个数承担三件事"（做完/新鲜/在验）。最简解 = **拆成三个独立量**：
  交付度（只增不减）/ 保鲜（单独计时）/ 阻塞（三要素）。最少机制 = 一个纯派生只读脚本。
- ② Anthropic 工程基线: fail-closed 不适用于派生视图（派生失败不应拖垮宿主）→ 采**显式降级契约**
  （`degraded:true` + `degraded_sources[]`，铁律 31），机器可验契约（schema 字段 frozen 分母）。
- ③ 开源实证: 仓内 `productline_yaml.py` 即现成纯 Python YAML 子集解析器（PyYAML 不可用实证），
  复用优于新增依赖；`calc-progress.py` 的 `load_evidence_records` degrade 模式可借鉴。
- ④ 收敛检查: 三系都指向"**只读派生 + 显式降级 + 复用仓内解析器 + 口径三项解耦**" → 收敛，执行。

### d) 相关 Note 引用
- 引用 `memory/notes/proposed/2026-09-17-project-management-v1-dod.md`（D793 口径重构决策，
  本卡是其 S1 "派生器四视图"落地）；本卡不新增 Note（无新决策，仅落地既有口径）

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- scripts/project/gen-project-board.py
- tests/project/gen-project-board.test.sh
- docs/synova/project/ledger.json
- task-state/D795.json
- .claude/task-briefs/2026-09-17-D795-项目账本派生器.md

不做什么：
- 不改 scripts/product-lines/calc-progress.py — 红线：判分器零提交，旧判分保持原样
- 不改 docs/synova/product-lines/product-lines.yaml — 只读输入，禁止写回
- 不改 scripts/audit/audit-check.py — 审计红线，scripts/audit/ 整目录不碰
- 不改 .github/workflows/ci.yml — 超出派单声明文件集，CI canary 入列另立派单
- 不改 docs/synova/product-lines/product-progress.json — 与旧判分并存不冲突
- 不改 docs/synova/product-lines/productline_yaml.py — 只 import 复用，不修改
- 不改 docs/synova/project/产品完成标准-可视版-20260917.html — D793 产出，非本卡文件

## 写集豁免

> 本分支 **stacked 于 `docs/v1-dod-draft-20260917`（D793 / PR #608）**——上游 V1 断言表尚未并入 main，
> 而本卡的分母与断言判定必须读它。故 `origin/main..HEAD` 会带上 D793 的 4 个文件。
> **本卡实现提交（9fd0b05a）实际只含 5 个声明文件**，下列文件一个字节都未改。
> PR #608 合并后 rebase 到 main，本段可整段删除（届时四文件随 base 消失）。

- docs/synova/project/26线-V1验收标准-草案v0.1-20260917.md — D793 上游规格（PR #608），本卡只读输入，非本卡改动
- docs/synova/project/产品完成标准-可视版-20260917.html — D793 派生视图（PR #608），非本卡改动
- memory/notes/proposed/2026-09-17-project-management-v1-dod.md — D793 决策 Note（PR #608），本卡口径依据，非本卡改动
- task-state/D793.json — D793 任务卡（PR #608），非本卡改动

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：
`python3 scripts/project/gen-project-board.py`（人工 / cron / CI 均可；默认 `--repo-root` 自动定位仓库根）

处理（中间经过哪些步骤）：
读 task-state/*.json（卡状态/阻塞三要素）→ 读 product-lines.yaml（26 线 + 164 点 + done_definition）
→ 解析 V1 断言表（125 条：ID/断言/verify/证据类型/fail_when）→ 扫两处证据目录（产品线 + GS 场景）
→ 按"证据类型匹配 + 优先级 k3>test>scenario>founder-demo"判每条断言 ok → 计交付度/验证率/保鲜分桶/阻塞

结果（最终展示在哪）：
`docs/synova/project/ledger.json`（schema `project-ledger/1`）→ D794 插件经 `GET /synova/pm/ledger`
渲染进 DSH 侧边栏「项目总览」；任一输入缺失/坏 → `degraded:true` + `degraded_sources[]`，前端显式降级文案

## 架构层: 基础设施
控制塔派生层（`scripts/project/`），不进 L1-L5 产品运行时，不 import `src/**`；
只读消费 git 事实（task-state / product-lines / evidence），产出单一派生 JSON。
#CRITERIA: A

## Done 标准
- [ ] 五组测试全绿: `bash tests/project/gen-project-board.test.sh` 返回 exit 0
      verify: `bash tests/project/gen-project-board.test.sh`
- [ ] 真实仓库口径正确: `totals.v1_total == 125` 且 `len(lines) == 26`
      verify: `python3 -c "import json;d=json.load(open('docs/synova/project/ledger.json'));assert d['totals']['v1_total']==125 and len(d['lines'])==26"`
- [ ] 反向可回退: 删证据 → 该断言 ok 由 true 回退 false（测试组 2）
      verify: `grep -c "反向" tests/project/gen-project-board.test.sh`
- [ ] 降级显式: 缺 yaml / 坏 JSON → `degraded:true` + `degraded_sources` 非空（测试组 3）
      verify: `python3 scripts/project/gen-project-board.py --repo-root /tmp/__nonexistent__ --out /tmp/x.json --strict; test $? -ne 0`
- [ ] 幂等: 连跑两次除 `generated_at`/`git_head` 外逐键相等（测试组 5）
      verify: `grep -c "幂等" tests/project/gen-project-board.test.sh`
- [ ] 红线零触碰: 最近一次提交不含 `scripts/product-lines/calc-progress.py` 与 `scripts/audit/`
      verify: `git log --name-only --pretty=format: -1 | grep -cE "calc-progress.py|scripts/audit/" | grep -qx 0`
