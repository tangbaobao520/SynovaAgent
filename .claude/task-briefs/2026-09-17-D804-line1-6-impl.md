# Task Brief: D804 实现 — M1「能装能用」线1 桌面端 + 线6 首诊端到端（切片 0-2）

> 生成: 2026-09-17 | 任务: D804（实现阶段）| 认领: mac-coding（DSH 编码 session，预设 synova-dsh）| 域: win（D773 代行：Mac 侧执行，域声明不变）
> 规格: `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D804-line1-6-onboarding-20260917.md`（唯一契约，八节 S1-S8）
> 派单: `docs/synova/coordination/派单-D804-D803-DSH标准-20260917.md` §一/§二（PR #618）
> 分母: `docs/synova/project/26线-V1验收标准-草案v0.1-20260917.md`（线1 5 条 / 线6 4 条）
> 参考: D333 决策四步（第一性原理→Anthropic 工程基线→开源实证→收敛检查）

#CRITERIA: A

## 主线贡献
line-1/1-1、line-1/1-4（能被装起来用 = 主线支柱①）+ line-6/6-1（首诊端到端）；
本卡实现期按 spec §S5 切片推进，本 brief 覆盖授权的切片 0-2（切片 3 与 D747 撞车待 CTO 裁决）。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属 **验收证据面**（不改产品逻辑）：
- 纵向：跨 L1（Electron 打包/自启）与 L2/L3（consult 六阶段 → 报告）——本卡只改**验收脚本与断言契约**，零 `src/**` 变更。
- 横向：不建包、不迁包。
- 扩展：不新增文件驱动模块。
- 三系统归属：GA 按需诊断（首诊六阶段）+ 基础设施（打包/安装/证据链）。

本卡 = spec §S5 的实现阶段；按派单 §四「规格冻结门」，**全程零 `src/**` 变更**。

### b) 文件审计（2026-09-17 实测于 origin/main @0f59e514，全部 file:line 可核）
| 关键词 | 实测结果 | 关系 |
|---|---|---|
| GS-01 场景 | `scripts/golden-scenarios/GS-01-first-diagnosis/run.sh`（8 断言，exit 1）+ `expect.json`（`evidence_map` 键 = S0-1/S0-2/L1-1/L1-4/L1-5/L1-6/L1-7） | **修改**（切片 0 契约校准 + V1 绑定；切片 1 产物段；切片 2 healthz 段） |
| 断言引擎 | `scripts/golden-scenarios/common/assert.ts`（四型检查 + 三态语义 + `buildEvidence`） | **只读复用**（不改——不在 §12.1 写集，且 GS-02~GS-08 共用） |
| 负向靶点 | `src/middleware/auth.ts:121`（consult 免 JWT 白名单，D590 裁决①）→ 旧断言 `noauth-401` 恒红（C1/C2）；`src/routes/config.ts:25`（`/api/config/dump`，server.ts:385 挂载，**未进白名单**） | **靶点迁移**（只改断言，不改白名单） |
| consult 校验 | `src/routes/diagnosis.ts:189`（POST consult）、`:197`（缺 teamId→400）、`:200-201`（缺 initiator.role→400 VALIDATION_ERROR）、`:620`（500 兜底） | **复用**（正向契约断言） |
| 服务自启 | `electron/backend-spawn.cjs:148`（ensureBackend: probe→spawn→degraded）、`electron/main.cjs:252`（调用点） | **切片 2 修改**（`probeOnce` 结构校验，F2） |
| 产物/签名 | `scripts/desktop/verify-package-signature.sh`（exit 0/1/2）、`release/`（0.1.0 arm64 dmg/zip） | **切片 1 复用**（断言化，不重建） |
| 进度判分 | `scripts/product-lines/calc-progress.py:67`（TTL 14 天）、`:177`（`acceptance_point` **精确匹配**） | **只读**（判分器红线，不改） |

### c) 决策
- 已有覆盖 → 复用（GS-01 / assert.ts / verify-package-signature.sh）。
- 无覆盖 → 扩展既有脚本与断言（**不新建平行体系**）。
- 冲突 → 新表：**不改 `common/assert.ts`**（不在写集；空背书护栏改由 `tests/golden-scenarios/gss-common.test.ts` 全场景契约扫描承担）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训

### a) 业界最佳实践
断言应描述**已生效契约**，契约变更后断言必须跟改——否则"红"不指向任何真实缺陷（恒红断言 = 噪声，最终导致门禁被整体绕过）。负向安全断言必须锚定**受保护资源**；白名单放宽时，被放开的方向改用**正向契约断言**覆盖，避免 fail-closed 覆盖净损失。

### b) Anthropic 决策链（spec → test → impl → wire → review → merge）
本片严格走：先写红（`gss-common.test.ts` 三条契约用例）→ 再改 `expect.json`/`run.sh` → 实跑 GS-01 exit 0 → 接线 grep → 提交。**迁靶 ≠ 删断言**：fail-closed 覆盖由 consult 正向契约断言接管。

### c) memory / 历史教训
- **D715 P1-4**：`[preload-check] OK` 只在 note 里自述、原始日志未落 git → K3 判"不可转绿"。教训：证据必须落 git 原件（`.txt`/`.json`，禁 `.log`——被 `.gitignore` 吞）。
- **D715 P1-1**：evidence-writer 双机产证同名撞车（add/add 冲突丢另一侧判定）→ 命名必须带平台/任务维度（切片 5 `--tag`）。
- **D524**：上游改动导致引用行号漂移，照旧行号写测试必红。
- **D527**：无 LLM key 时必须如实 `CONSULT_LLM_RED`，禁伪造全链路绿。
- **D773**：无 Win 机期间实机级证据**挂账**，禁用 Mac 结果声称 Win 实机通过。

### c2) 决策参考系（D333 四步）
参考：第一性原理 + Anthropic 工程基线 + 实测收敛。
- **切片 0 键空间**：spec §决策参考 D5 的 L1 要求是 **⊆**（键不得越出 V1 ID ∪ 非 V1 自有标签），切片 0 的 `⊇` 是**完工态**目标。`6-2` 的断言与绑定归切片 5——若在切片 0 抢占位，`buildEvidence` 对零背书条目**恒判 pass**（白送绿），违反引擎 §2.3 反恒真红线。故切片 0 绑定面 = `1-1`/`1-4`/`6-1`，并以全场景扫描护栏防后续误加空条目。
- **负向断言迁靶**：spec D1 结论 C（创始人 2026-09-17 批准口径变更）——保留断言不删（保 fail-closed 覆盖），靶点从已白名单化的 consult 迁至受保护的 `/api/config/dump`。

## Q2: 范围 — 正确的最简方案

**做什么**（本实现阶段，切片 0-2）：
- scripts/golden-scenarios/GS-01-first-diagnosis/run.sh
- scripts/golden-scenarios/GS-01-first-diagnosis/expect.json
- scripts/golden-scenarios/GS-01-first-diagnosis/README.md
- tests/golden-scenarios/gss-common.test.ts
- electron/backend-spawn.cjs
- tests/electron/backend-spawn.test.ts
- task-state/D804.json
- docs/synova/coordination/ownership.yaml（CTO 2026-09-17 裁定：D734 跨域假阳性，按 D782 先例补规则）
- .github/CODEOWNERS（同上：ownership.yaml 派生物，drift 门禁要求同步重生成）
- memory/notes/implemented/2026-09-17-d804-ownership-test-follows-tooling.md（铁律 49/D534：治理规则变更的决策沉淀）

**不做什么**（排除项，含文件路径）：
- 不改 src/middleware/auth.ts（consult 白名单口径 = D590 裁决①，创始人裁决面；本卡只把断言对齐裁决）
- 不改 src/routes/diagnosis.ts（只用其既有契约，不复活/改路由）
- 不改 src/routes/healthz.ts（1-4 只消费其既有 200 语义）
- 不改 scripts/product-lines/calc-progress.py（判分器红线，派单 §一）
- 不改 scripts/golden-scenarios/common/assert.ts（GS-02~GS-08 共用引擎；不在 §12.1 写集）
- 不改 scripts/desktop/mac-install-verify.sh（属切片 3；与 D747 claimed 写集重叠，待 CTO 裁决）
- 不改 scripts/desktop/first-diagnosis-timing.sh（属切片 5）
- 不改 scripts/product-lines/evidence-writer.py（属切片 5）
- 不改 docs/synova/runbooks/first-diagnosis-e2e.md（属切片 3）
- 不改 .github/workflows/ci.yml（控制塔/CI 域，非本卡写集）

## Q3: 验收 — 入口 → 交互 → 结果

- **入口**：`bash scripts/golden-scenarios/GS-01-first-diagnosis/run.sh`（场景脚本 = 生产验收入口）
- **处理**：临时库 + 临时端口 bootstrap → 真实 HTTP 断言（无 token `/api/config/dump`→401；无 token consult 缺 role→400 非 401；带 token 缺 teamId→400；reports→200）→ 无头 electron 契约 → 断言引擎落 evidence JSON
- **结果**：`exit 0`（9/9 断言）+ `scripts/golden-scenarios/evidence/GS-01-<date>.json` 中 `1-1`/`1-4`/`6-1` 有 verdict → `calc-progress.py` 可绑 → 产品进度页转 `pending_k3`

## 架构层: 基础设施
L1（Electron 桌面端）+ L2/L3（consult 编排链路）；本卡只改**验收脚本 / 断言契约 / 证据链**，零 `src/**` 变更。
写集落 `scripts/golden-scenarios`、`tests/golden-scenarios`、`tests/electron`、`electron/`（切片 2）。

## Done 标准
- [ ] 入口可触达: `env -u ELECTRON_RUN_AS_NODE bash scripts/golden-scenarios/GS-01-first-diagnosis/run.sh` → exit 0（9/9 断言）
- [ ] 链路走通: `npx vitest run tests/golden-scenarios/ tests/electron/` → 全绿（零回归）
- [ ] 结果可见: `scripts/golden-scenarios/evidence/GS-01-2026-09-17.json` 中 `1-1`/`1-4`/`6-1` verdict 为 `pass`
- [ ] verify: `python3 scripts/product-lines/calc-progress.py` 重算后 1-1/1-4/6-1 至少 3 条转 `pending_k3`
- [ ] verify: 反向验证——移走 `release/*.dmg` → 切片 1 产物断言必红（`ARTIFACT_MISSING`），移回复绿
- [ ] verify: 负向靶点实证——无 token `curl /api/config/dump` 返回 401 原文落 git 证据
- [ ] 红线自查: `git diff --name-only origin/main...HEAD` 零 `src/**`、零 `scripts/audit/**`

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/task-briefs/2026-09-17-D804-line1-6-impl.md | task |
| .github/CODEOWNERS | task |
| docs/synova/coordination/ownership.yaml | task |
| memory/notes/implemented/2026-09-17-d804-ownership-test-follows-tooling.md | task |
| scripts/golden-scenarios/GS-01-first-diagnosis/README.md | task |
| scripts/golden-scenarios/GS-01-first-diagnosis/expect.json | task |
| scripts/golden-scenarios/GS-01-first-diagnosis/run.sh | task |
| scripts/golden-scenarios/evidence/GS-01-2026-09-17.json | task |
| tests/golden-scenarios/gss-common.test.ts | task |

