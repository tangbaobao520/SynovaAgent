# Task Brief: D806 台账修复-DSH复用可见

> 生成: 2026-09-18 | 任务: D806 | 认领: DeepSeek Harness
> 依据: `docs/synova/coordination/DSH锚点重锚审计-20260917.md`（D797，分支 `docs/dsh-reanchor-20260917`）+ CTO 派单四缺陷
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。本任务**不进 L1-L5 产品运行时**，属**控制塔派生层**（`scripts/project/` + `docs/synova/product-lines/`）。
链路: `product-lines.yaml`（26 线真相源） + V1 断言表 + `evidence/*.json` → `scripts/project/gen-project-board.py` → `docs/synova/project/ledger.json` → D794 侧边栏「项目总览」看板。
现存三处断点（本单修的就是它们）:
① 11 条 `modules` 是**幻影路径**（目录不存在）→ A1 git 失效检测静默不触发（`calc-progress.py:106-121` 把 modules 当 git pathspec，不存在的路径永远零命中）；
② `src/llm/` `src/store/` `src/services/` `src/config/` 四个已交付借鉴卡落点**未进任何线 modules**；
③ 8 张已交付借鉴卡**无 V1 验收点绑定证据** → 看板 `v1_passed` 看不见这部分复用。

### b) 文件审计
- `docs/synova/product-lines/product-lines.yaml:90,137,564,601,638,675,759,875,917,996,1038` → 11 条幻影 modules 行（逐行实测见交付报告对照表）
- `scripts/project/gen-project-board.py:472-483` → `v1_passed` 判定：证据 `record_type` **必须等于** V1 断言表「证据」列 kind，且 `index_evidence:275` 要求 `verdict=="pass"`
- `scripts/product-lines/calc-progress.py:106` → `git_touched_after(modules,…)` 证明 modules 是 git pathspec（幻影 = 失效检测哑火）
- `tests/project/gen-project-board.test.sh:40` → 自带 v0.1 夹具（**自带 V1 文件**，新增 v0.2 不破它）
- `tests/control-tower/product-lines.test.py:120,285,407` → 断言 26 线（modules 改动不破断言）
- 借鉴卡落点实测: `src/llm/retry-middleware.ts`、`src/llm/timeout.ts`、`src/llm/token-meter.ts`、`src/llm/tool-result-pruner.ts`、`src/store/session-projection.ts`、`src/services/llm-cost.ts`、`src/config/config-layers.ts`

### c) 决策
- **复用** `gen-project-board.py`（不新建派生器、不改它）：断点在**数据源**，不在派生逻辑。
- **复用** 既有 evidence schema（`{schema,record_type,date,source,verdicts[]}`），**不发明新格式**（铁律 47 契约优先：沿用 `docs/synova/product-lines/evidence/d592-e2e-2-1.json` 已证契约）。
- **不取消**任何既有资产：`calc-progress.py` 本次零改动（验收④硬约束）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- **第一性原理**: 看板要回答的是「这条线做完了吗」。「做完了」必须机器可判 → 断言 ID + 证据 kind 的**机械配对**，不是再写一层说明文档。故修数据（paths/证据），不修叙事。
- **Anthropic 工程基线**: 派生只读（`ledger.json` 由脚本生成，禁手改）+ 机器可验契约（证据 kind ↔ 断言 kind 强配对）+ fail-closed。
- **memory/ 教训**: D793 Note「TTL 到期即扣完成度 → 完成度不单调」→ 本单**改的是可判定性，不动判分口径**；`派单-D760-D769-DSH借鉴卡` §三 G3「每卡必须挂产品线缺口，禁为借鉴而借鉴」→ ③ 的绑定必须逐卡对到真实落点。
- **铁律引用**: 铁律 7（Done 标准可验）、铁律 9（关键变更 grep 全仓库传播）、铁律 11/24/31（证据缺失不静默）、铁律 47/48（契约 + 非空壳测试）、铁律 0-5（不碰 `scripts/audit/**`）。
- 参考：Anthropic 工程基线（派生只读 + 机器可验契约）/ 第一性原理（能机械判定的不写成散文） + 结论：**修数据源三处、派生器与判分脚本零改动**。

## Q2: 范围 — 正确的最简方案
做什么：
- docs/synova/product-lines/product-lines.yaml — ① 11 条幻影 modules 换现验真实路径 + ② 落点归属按 D808 §12.2/R1/R5 收敛 + 元断言 M1–M5 注释（v0.2 §四 落点2）
- docs/synova/product-lines/evidence/D806-borrow-cards-20260918.json — ③ 8 张借鉴卡绑定 V1 验收点的证据记录（record_type 对齐 V1 断言表 kind）
- docs/synova/project/26线-V1验收标准-草案v0.2-20260918.md — ④ v0.2 落库版 128 条表（13 改写 + 20-5/25-6 提入 + 26-7 新增）
- docs/synova/project/ledger.json — 重跑 gen-project-board.py 派生刷新
- scripts/project/gen-project-board.py — 分母变更单：FROZEN_V1_TOTAL 125→128（CTO 授权扩写集）
- tests/project/gen-project-board.test.sh — 真实仓库读数 125→128 / 39→36（CTO 授权扩写集）
- task-state/D806.json — 状态机登记 + change_log（分母/backlog 变更）
- memory/notes/proposed/2026-09-18-d806-ledger-dsh-alignment.md — 铁律 49 决策沉淀
不做什么：
- 不改 scripts/product-lines/calc-progress.py（验收④硬约束：零提交）
- 不改 docs/synova/product-lines/evidence/d592-e2e-2-1.json（存量证据只增不改）
- 不改 scripts/audit/ 下任何文件（铁律 0-5 K3 红线）
- 不改 src/ 下任何产品代码（本单只修台账数据，不写产品功能）

## Q3: 验收 — 入口 → 交互 → 结果
入口：D794 侧边栏「项目总览」面板 → `GET /synova/pm/ledger`（读 `docs/synova/project/ledger.json`）
处理：`product-lines.yaml`（真实 modules）+ V1 断言表（v0.2 口径）+ `evidence/*.json`（借鉴卡绑定）→ `gen-project-board.py` 重跑
结果：`ledger.json` 的 `totals.v1_passed` 相对基线有**真实增量**，且对应线 `lines[].v1_passed` 同步上涨；删掉一张借鉴卡证据 → 对应点回退（反向可证伪）

## 架构层: 基础设施
控制塔派生层（`scripts/project/` + `docs/synova/product-lines/` + `docs/synova/project/`），不进 L1-L5 产品代码，不 import `src/**`。

## Done 标准
- [ ] ① 11 条幻影路径全部现验通过 — verify: `python3 -c "import re,pathlib;y=pathlib.Path('docs/synova/product-lines/product-lines.yaml').read_text();print([m for m in re.findall(r'\"([^\"]+/)\"',y) if not pathlib.Path(m).is_dir()])"` 输出 `['electron-renderer/', 'scripts/deploy/', ...]` 中不含本次修的 11 条（逐条 `[ -d ]` 对照表见交付报告）
- [ ] ② ledger 对应线 v1_passed 有真实增量 — verify: `python3 -c "import json;d=json.load(open('docs/synova/project/ledger.json'));t=d['totals'];assert t['v1_passed']>21, t['v1_passed']"` exit 0
- [ ] ③ 反向可证伪：删掉一张借鉴卡证据 → 对应点回退 — verify: `bash tests/project/gen-project-board.test.sh` exit 0（含组②反向用例）+ 交付报告附真实删除实测
- [ ] ④ calc-progress.py 零提交 — verify: `bash tests/control-tower/product-lines.test.py 2>&1 | grep -c "FAIL:"` 等于基线 2（不新增失败）
- [ ] ⑤ 派生器自测全绿 — verify: `bash tests/project/gen-project-board.test.sh 2>&1 | tail -1 | grep -q "FAIL=0"` exit 0
- [ ] ⑥ v0.2 分母 128 生效且 backlog=36 — verify: `python3 -c "import json;t=json.load(open('docs/synova/project/ledger.json'))['totals'];assert (t['v1_total'],t['backlog_points'])==(128,36),(t['v1_total'],t['backlog_points'])"` exit 0

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/bypass.log | builtin（hook 运行期产物，自动豁免） |
| .claude/task-briefs/2026-09-18-D806-台账修复-DSH复用可见.md | task |
| docs/synova/product-lines/evidence/D806-borrow-cards-20260918.json | task |
| docs/synova/product-lines/product-lines.yaml | task |
| docs/synova/project/26线-V1验收标准-草案v0.2-20260918.md | task |
| docs/synova/project/ledger.json | task |
| memory/notes/proposed/2026-09-18-d806-ledger-dsh-alignment.md | task |
| scripts/project/gen-project-board.py | task |
| task-state/D806.json | task |
| tests/project/gen-project-board.test.sh | task |

