# Task Brief: D817 第 0 项 — CI 能跑一次「生产入口级对话」（验证基础设施）

> 生成: 2026-09-19 | 分支: feat/d817-production-entry-conversation | 基线: origin/main 00c784fc | as any: 0
> 派单: docs/synova/coordination/派单-批十-第0项-CI生产入口级对话-20260918.md（分支 docs/dispatch-batch10-d0-ci-prod-entry）
> 验收口径: docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md（五态 / 三问自查 / 四条禁止）
> 卡: task-state/D817.json

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

## Q0: 定位

### a) 项目拼图
- [x] 测试基建（tests/**，非生产代码）——不改任何 src/** 行为，只加「能起真实服务、打真实路由、在假上游抓真实出站请求体」的验证能力
- 本任务在五层之外：它是**验证基础设施**。服务对象是全部 15 项任务书的验收标准（CTO 口径：第 0 项 = 公共前置）。
- 已有覆盖：tests/smoke.test.ts（起 createServer）、tests/routes/diagnosis-token-meter.integration.test.ts（listen(0)+fetch 真实路由）——但**没有任何测试穿过 POST /api/conversations/:id/messages 并观察出站 LLM 请求体**。
- 结论：新增（新建假上游 helper + 真实入口集成用例 + 证据文件），零生产代码改动。

### b) 文件审计（开工当场核过，行号基于 origin/main 00c784fc）
```
vitest.config.ts:29-38                     ✅ CI 下 exclude 含 'tests/e2e/**'（"Needs LLM API"）→ 穿入口验收今日无法自动判定
src/server.ts:124 createServer()           ✅ 真实 bootstrap（app.locals.orchestration 由 server.ts:286 装配）
src/routes/conversations.ts:359            ✅ POST /api/conversations/:id/messages（真实目标路由）
src/routes/conversations.ts:192-197        ✅ createProvider(detectProvider(), {apiKey, baseUrl, model}) ← 配置缝
src/config.ts:90-94                        ✅ llmBaseUrl = storedLlmRuntime?.baseUrl || LLM_BASE_URL || …（D575 凭证文件缝）
src/providers/index.ts:54-61               ✅ createProvider(type, config).baseUrl（派单指定的既有缝）
src/providers/base.ts:139-146              ❌ 出站 body = {model,messages,temperature,max_tokens}，无 tools（P0-1 事实）
src/providers/base.ts:215-238              ❌ chat() 只取 choices[0].message.content，未映射响应侧 tool_calls（D817-F1，本次新发现）
src/agent/tool-loop-executor.ts:270        ❌ 推 assistant 后被 conversation-engine.ts:785 再推一次（D817-F2，本次新发现）
tests/helpers/fake-llm-upstream.ts         ❌ 新建
tests/integration/production-entry-conversation.integration.test.ts ❌ 新建
docs/synova/product-lines/evidence/D817-capture-20260918.json      ❌ 新建（测试运行时机器生成）
```

### c) 决策（D333 四步）
参考：Anthropic 工程基线（真实入口 + 可观测抓取点，拒绝替身）+ 第一性原理（最少机制：一个假上游 + 一条真实路由用例）+ DeepSeek 开源实证（OpenAI 兼容协议即事实标准） → **收敛**：不引入新框架/依赖，node:http 起假上游，走既有配置缝。
- 上游指向用 **D575 凭证文件**（SYNOVA_DATA_DIR/llm-credentials.json，config.ts:90-94 读）而不是只设 LLM_BASE_URL：① 它就是生产里 GA 首次配置写的那条缝（与 routes/llm-config.ts 同源 API：setLlmCredential）② 只写 stored key 而不设 env LLM_API_KEY/DEEPSEEK_API_KEY/OPENAI_API_KEY，buildFallbackProvider（conversation-engine.ts:323-339）返回 null → **物理上不存在指向真实 API 的 failover 分支**（零外网可证）。
- 不引入 supertest/jest；不在 CI 跑真实 LLM；不新建第二套测试框架。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC（派单 §B 七条验收）→ ② 测试先行（先起假上游断言「出站请求体字段清单」，再谈修 P0-1）→ ③ 实现（helpers + 用例 + 证据）→ ④ 接线（CI=1 下被 vitest 包含）→ ⑤ 验证（命令 + 原始输出）
引用: 铁律 0-2（测试先行+接线验收）、铁律 12（集成测试走真实路由不 mock 管线）、铁律 24/31（catch 有 log + degraded）、铁律 33（命名约定）、铁律 38（as any 零容忍）、铁律 47/48（契约 + 非空壳断言）、验收标准《穿真实入口 v1》四条禁止。

### b) 执行约束
- rule: "假上游记录出站请求体字段清单" verify: grep -rn "bodyKeys" tests/helpers/fake-llm-upstream.ts
- rule: "测试打真实路由 + listen(0) + fetch" verify: grep -rn "createServer" tests/integration/production-entry-conversation.integration.test.ts
- rule: "上游经既有配置缝指向 127.0.0.1，src/** 零改动" verify: git diff --name-only origin/main...HEAD -- src/ | wc -l → 0
- rule: "P0-1 以 it.fails 证词化" verify: grep -rn "it.fails" tests/integration/production-entry-conversation.integration.test.ts
- rule: "CI 下被包含且通过" verify: CI=1 npx vitest run tests/integration/production-entry-conversation.integration.test.ts

## Q2: 范围

做什么：
- tests/helpers/fake-llm-upstream.ts（OpenAI 兼容假上游：监听 127.0.0.1:0；逐请求记录出站 body + bodyKeys + messages 角色序；脚本化响应，首轮带 tool_calls；GET /models 供健康检查；契约写在文件头 JSDoc）
- tests/integration/production-entry-conversation.integration.test.ts（起真实 createServer + listen(0) + 真实 fetch；打 POST /api/conversations 建会话与 POST /api/conversations/:id/messages 续轮；断言 HTTP/SSE 正常收束、假上游收到 ≥1 请求、出站体字段清单、零外网、CI 包含性；含三条 it.fails 证词与假上游降级路径用例）
- docs/synova/product-lines/evidence/D817-capture-20260918.json（测试运行时 machine-generated：出站请求体 Object.keys() + 断言结果 + 三条证词）
- task-state/D817.json（impl 段 + status=impl_done，编码侧填法）
- memory/notes/implemented/2026-09-18-D817-ci-production-entry.md（由 proposed/ git mv 落地，铁律 49 四态迁移；不迁移会被 check-notes-lifecycle 判僵尸阻断）
- .claude/task-briefs/2026-09-19-D817-生产入口级对话.md（本文件）

不做什么：
- 不改 src/providers/base.ts（P0-1 修复属下一张卡——本卡只证词化）
- 不改 src/agent/tool-loop-executor.ts（D817-F1/F2 修复不在本卡）
- 不改 src/agent/conversation-engine.ts（D817-F2 修复不在本卡）
- 不改 vitest.config.ts（不放宽/收紧 CI 排除清单——验收标准红线 2）
- 不改 scripts/audit/audit-rules.sh（K3 审计域，红线）
- 不改 scripts/control-tower/alloc-task-id.sh（控制塔另有两条线在改，本卡零改动）
- 不改 .github/workflows/ci.yml（派单 §A：仅在必须时改；本卡实测不需要）
- 不改 tests/e2e/production-entry-conversation.e2e.test.ts（不把文件挪回被 CI 排除的目录）

## Q3: 验收

入口: CI=1 npx vitest run tests/integration/production-entry-conversation.integration.test.ts（CI 面内被包含并执行）
处理: 起真实 createServer（真实 bootstrap + 真实 SQLite :memory: + 真实 express 路由）→ 经生产配置缝把 provider baseUrl 指向 127.0.0.1 假上游 → 打 POST /api/conversations 建会话 → 打 POST /api/conversations/:id/messages 续轮
结果: ① HTTP 200 + text/event-stream + SSE 帧序列正常收束（open/token/agent_message/end，无 error 帧）② 假上游 requests[] 抓到真实出站请求体（含字段清单）③ 证据文件落盘（含 body_keys）④ 三条缺陷证词（P0-1 tools 缺失 / D817-F1 响应侧 tool_calls 未映射 / D817-F2 assistant 重复入上下文）以 it.fails 固化——修好后自动翻转为红，强制删除

## 架构层: 测试基建（tests/**，非 L1-L5 生产链路；被验证的链路 = L1 src/routes/conversations.ts → L2 src/agent/conversation-engine.ts → src/providers/**）
#CRITERIA: C

## Done 标准
- [ ] CI=1 下被包含且全绿（9 用例：6 passed | 3 expected fail），且非 "no test files"：verify: CI=1 npx vitest run tests/integration/production-entry-conversation.integration.test.ts
- [ ] 该文件不在 CI 排除面内（负向对照 tests/e2e/** 仍在排除面内）：verify: CI=1 npx vitest run tests/integration/production-entry-conversation.integration.test.ts
- [ ] 假上游收到 ≥1 个真实出站请求且证据文件含出站体字段清单：verify: python3 -c "import json;d=json.load(open('docs/synova/product-lines/evidence/D817-capture-20260918.json'));print(d['upstream']['request_count'],d['upstream']['body_keys_union'])"
- [ ] src/** 零改动：verify: bash -c 'test "$(git diff --name-only origin/main...HEAD -- src/ | wc -l | tr -d " ")" = "0"'
- [ ] vitest.config.ts 的 exclude 未被改动：verify: bash -c 'test "$(git diff --name-only origin/main...HEAD -- vitest.config.ts | wc -l | tr -d " ")" = "0"'

## 写集（机器生成，禁手改）

| 文件 | 类型 |
|---|---|
| .claude/bypass.log | builtin（hook 运行期产物，自动豁免） |
| .claude/task-briefs/2026-09-19-D817-生产入口级对话.md | task |
| docs/synova/product-lines/evidence/D817-capture-20260918.json | task |
| memory/notes/implemented/2026-09-18-D817-ci-production-entry.md | task |
| task-state/D817.json | task |
| tests/helpers/fake-llm-upstream.ts | task |
| tests/integration/production-entry-conversation.integration.test.ts | task |

