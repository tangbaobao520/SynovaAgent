# Task Brief — D838（V1 标准变更单起草 + 两线登记）

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。本任务在**项目管理基建层**（非产品功能）：执行创始人 2026-09-19 三项裁定（两条新线登记 / 11 项 DSH 缺口归线不新开 / 前提清单优先级高于线），并为"新线进入计分分母"起草 V1 标准变更单。
### b) 文件审计
`docs/synova/product-lines/product-lines.yaml`（线集单一事实源，`total_lines` 与 `lines:`）、`docs/synova/product-lines/preconditions.yaml`（前提清单）、`docs/synova/product-lines/gap-line-mapping.yaml`（新建）、`docs/synova/project/26线-V1验收标准-v0.2-20260917.md`（冻结件，**本任务不改**）、`docs/synova/coordination/草案-新增两线-20260918.md`。
### c) 决策
复用既有 yaml 结构（新增线 27/28 与 `total_lines: 28`）；11 项归线写成**分离文件**（派生器不支持行内映射，实测 degraded）。V1 冻结件不直接改，走变更单。

## Q1: 调研 — 业界最佳实践 / decision 参考系
- 第一性原理：计分分母是"承诺"的量化，改它必须留痕 + 可追溯（变更单 + 哈希）。
- 参考系：本项目既有先例——V1 v0.2 冻结时用签名哈希 `c11841e6` + 附录 A 落库表；D806 台账修复已确立"规格源唯一"原则。
- memory 历史教训：铁律 49（决策沉淀）、D534（四态 Note）、D704 幻影路径（modules 必须真实存在）。
- 参考：Anthropic 工程基线 + 第一性原理 → 结论：**只起草、不落冻结件**。

## Q2: 范围 — 正确的最简方案
做什么：
- `docs/synova/product-lines/product-lines.yaml`：新增线 27（测量与参数标定）/ 28（多客户配置与租户分层）各 5 个验收点；`total_lines: 26 → 28`
- `docs/synova/product-lines/gap-line-mapping.yaml`（新建）：11 项缺口归线登记（含 ③/①/② 方式标注）
- `docs/synova/product-lines/preconditions.yaml`：`priority: above-lines` + 裁定来源
- `docs/synova/coordination/草案-新增两线-20260918.md`：标注"创始人已确认并登记"
- `task-state/D838.json` + 本 brief + 四态 Note

不做什么（含文件路径）：
- **不改** `docs/synova/project/26线-V1验收标准-v0.2-20260917.md`（冻结件，变更单为草案）
- **不改** `docs/synova/project/ledger.json`（属 D806 session 范围，且应由账本自动化刷新）
- 不动 `src/**`、`scripts/**`

## Q3: 验收 — 入口 → 交互 → 结果
入口：创始人裁定（2026-09-19）→ CTO 起草登记件
处理：改 yaml（线 + total_lines）→ 分离文件（缺口归线）→ 前提清单加优先级 → 更新草案状态
结果：看板派生器**不降级**（`degraded=False`）；待定项 36 → **46**；`total_lines=28`；新线待 V1 变更单确认后进计分

## 架构层: 项目管理基建（非 L1–L5）
## Done 标准:
1. `python3 scripts/project/gen-project-board.py` 输出 `degraded=False` 且 `backlog_points=46`（实测命令 + 原始输出）
2. `grep -c "gap_line_mapping" product-lines.yaml` → 0（已分离）；`gap-line-mapping.yaml` 存在且含 11 条
3. `preconditions.yaml` 含 `priority: above-lines` 与 `ruled_by`
4. 冻结件与 ledger.json 零改动（`git diff --name-only` 证据）
