# Task Brief — D811（未合 PR 队列收口：台账 / 关闭清单 / 上限提示层 / 旁路指标）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
治理层/吞吐基建（**项目管理基建层**）（非产品功能、非 L1–L5）。未合 PR 队列已到 46 个（上限 12），
每合并一次其余 PR 变 dirty → 全队列重跑 CI（派单实测反复 4 次）。本任务按 DSH 决策镜头
（`.claude/skills/dsh-decision-lens/SKILL.md`）：**先量不先加机制、超限即退役、诊断旁路不污染主路径**，
把队列变成机器算的派生指标 + 可签字退役清单 + 只提示不拦死的上限告警。
### b) 文件审计
- `scripts/project/`：现有仅 `gen-project-board.py`（D795 账本派生器，schema project-ledger/1，
  被 D794 项目总览消费）。**队列台账扫描器不存在** → 新建。
- `docs/synova/project/`：`ledger.json`（派生物，禁手改）+ V1 断言表 + 产品完成标准 HTML。旁路指标落此处。
- `docs/synova/coordination/`：队列收口报告 + `AUDIT-FINDINGS-LEDGER.md`（发现登记台账）。
- 硬边界：`scripts/control-tower/{staging_guard.py,synova-commit}`、`scripts/workflow/resolve-commit-brief.sh`
  存在且属 D839 地盘（PR #657 待 K3 审计）；`scripts/control-tower/claim_release.py` **main 上不存在**
  （在 PR #657 分支内）——三者一律不碰。
### c) 决策
复用既有派生链（`gen-project-board.py` 加**旁路段**，不新建第二条看板链）；扫描器走**纯读 + 纯函数核心**
（离线夹具可测）；上限做成**提示层**而非门禁（派单 §一.3 明令"只提示不拦死"）。

## Q1: 调研 — 业界最佳实践 / decision 参考系
- **第一性原理**：队列健康是"机械事实"，不是"业务判断"。人的价值在签字，不在数数。
- **DSH 决策镜头六原则**（本机实测复核，非凭印象）：
  `@deepseek-ai/dsh-experimental-agent-team/lib/index.js:564` `TEAM_MEMBER_LIMIT` **throw**、
  `:1327` `TEAM_TASK_LIMIT` **throw** → 上限即纪律（原则①）；
  `@deepseek-ai/dsh-invariants/lib/index.js:6-19` 每个包用 `./invariant` 伴生注册检查 +
  `InvariantError{code:"INVARIANT",packageName}` → 归属自有、失败带机器可读码（原则②）；
  `@deepseek-ai/dsh-tool-workflow/lib/index.js:106` "the agents do the work, the script only
  coordinates them" → 只协调不干活（原则③）；
  `@deepseek-ai/dsh-output-retention/lib/types/index.js`（实际文件为 `lib/index.js`，
  技能给的路径不存在 → 已按实际复核）→ 机械语义与业务语义分离、诊断不进主入口（原则④⑤）。
- **借鉴边界**：DSH 一包一能力、互不共享文件 → advisory 写集足够；本仓单仓共享文件 →
  保留文件级互斥（不照抄 advisory）。
- **memory 历史教训**：M2（声称 vs 事实）/ M3（机制建成未接线）/ M7（文档-实现漂移）——
  故本任务每条都要有"物理命令 + 原始输出"，且必须真接线到派生链。
- **参考**：Anthropic 工程基线（先量、可验证）+ DSH 六原则 + 第一性原理 → 结论：
  **上限只提示不拦死**；**退役清单给理由**；**指标走旁路**。

## Q2: 范围 — 正确的最简方案
做什么：
- scripts/project/pr-queue-scan.py
- tests/project/pr-queue-scan.test.sh
- scripts/project/gen-project-board.py
- tests/project/gen-project-board.test.sh
- docs/synova/project/pr-queue.json
- docs/synova/project/ledger.json
- docs/synova/coordination/队列收口-D811-20260920.md
- docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md
- task-state/D811.json
- .claude/task-briefs/2026-09-20-D811-队列收口.md
- memory/notes/proposed/2026-09-20-D811-queue-closeout.md

不做什么（含文件路径）：
- 不改 scripts/control-tower/staging_guard.py（D839 地盘）
- 不改 scripts/control-tower/synova-commit（D839 地盘）
- 不改 scripts/workflow/resolve-commit-brief.sh（D839 地盘）
- 不改 scripts/pre-commit-check.sh（主提交路径不得新增任何检查）
- 不改 scripts/control-tower/check-pr-budget.sh（既有预算门禁，本次只读不改）
- 不改 docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md（验收口径件）
- 不动 src/ 下任何产品代码（.ts）（属 in-flight 的 team/win-batch14）

## Q3: 验收 — 入口 → 交互 → 结果
入口：`python3 scripts/project/pr-queue-scan.py`（取 GitHub open PR + 本地只读 git + task-state 卡）
　　＋ `python3 scripts/project/gen-project-board.py`（账本派生，读扫描器快照）
处理：逐 PR 算 `behind=N / CI / age / 主人 / 可否自动关闭` → 落快照与台账 → 账本挂 `pr_queue` 旁路段
结果：① 46 个未合 PR 的全量台账 ② 可签字关闭清单（每条带理由码 + 证据）③ 超限告警（46 > 12）
　　　④ `ledger.json.pr_queue` 带队列长度 / 最老 PR 年龄 / behind 分布

## 架构层: 项目管理基建（治理层 scripts/ + docs/，不触 L1–L5）
## Done 标准:
- [x] verify: python3 scripts/project/pr-queue-scan.py --out-json /tmp/d811-done.json --print table 输出 46 行台账且退出码 0
- [x] verify: python3 -c "import json;d=json.load(open('docs/synova/project/pr-queue.json'));print(d['metrics']['over_limit'],d['degraded'])" 输出 True False
- [x] verify: bash tests/project/pr-queue-scan.test.sh 输出 PASS=46 FAIL=0
- [x] verify: bash tests/project/gen-project-board.test.sh 输出 FAIL=0（组⑥ 两条为存量数据漂移，非本任务引入）
- [x] verify: python3 -c "import json;print(json.load(open('docs/synova/project/ledger.json'))['pr_queue']['queue_length'])" 输出 46
- [x] verify: grep -c "pr-queue-scan" scripts/pre-commit-check.sh .git/hooks/pre-commit .git/hooks/pre-push 输出 0（主提交路径零新增检查）
