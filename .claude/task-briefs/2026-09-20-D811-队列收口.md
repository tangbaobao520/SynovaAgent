# Task Brief — D811 未合 PR 队列收口（DSH 式）

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
治理层/吞吐基建。未合 PR 40 个，合并时会互相 dirty → 需重同步 + 重跑全量 CI，形成吞吐瓶颈。
### b) 文件审计
- 现有派生链：`scripts/project/gen-project-board.py`（→ `docs/synova/project/ledger.json`）。
- 队列台账**尚无**扫描器（现状靠人工）。
- 与 D839 地盘（`staging_guard.py`/`synova-commit`/`claim_release.py`/`resolve-commit-brief.sh`）**不得重叠**。
### c) 决策
无覆盖 → 新建扫描器（只读）+ 派生指标（旁路）；**不新增主路径门禁**。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- DSH 原则①（上限即纪律，`dsh-experimental-agent-team/lib/index.js:564/1327`）、④⑤（机械语义分离 / 诊断旁路，`dsh-output-retention/lib/types/index.js` 头注）。
- 决策参考：第一性原理（先量后加机制）+ DSH 实证 → 结论：扫描器 + 关闭清单 + 提示层，不加硬门禁。

## Q2: 范围 — 正确的最简方案
做什么：
- scripts/project/ 队列扫描器 + 派生指标接线
- docs/synova/coordination/ 关闭清单
- docs/synova/project/ 派生元数据（如需）
- task-state/D811.json
不做什么（含文件路径）：
- 不改 `scripts/control-tower/staging_guard.py`、`scripts/control-tower/synova-commit`、`scripts/control-tower/claim_release.py`、`scripts/workflow/resolve-commit-brief.sh`（D839 地盘）
- 不改 `src/**`（属 in-flight 的 `team/win-batch14`）

## Q3: 验收 — 入口 → 交互 → 结果
入口：命令行运行扫描器。
处理：读 GitHub PR 列表 + 各 PR 状态 → 计算 behind/age/CI/主人。
结果：台账输出 + 关闭清单 + 上限告警 + 看板指标；主提交路径零新增检查（给证明）。

## 架构层
治理层（scripts/ + docs/），不触 L1-L5。

## Done 标准
- [ ] 对当前 40 个未合 PR 输出完整台账（命令 + 原始输出）
- [ ] 超限告警夹具先红后绿（两次原始输出）
- [ ] 证明主提交路径未新增检查（对比 pre-commit 组数/耗时）
- [ ] CI 全绿后合并，PR ≤12 文件且单域
