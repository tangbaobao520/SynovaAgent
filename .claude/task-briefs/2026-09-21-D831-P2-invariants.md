# Task Brief — D831 P-2 运行期不变量机制（小队 B，worktree .synova-wt-p2 / feat/p2-invariants）

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。本任务在 **L3 洞察层**（运行期不变量 = sentinel 域的信任机制）+ `src/deploy`（bootstrap 注册）+ `src/server.ts`（健康探针挂载，1-2 行）。现状：`src/sentinel/sentinel-events.ts:9-13` 的 I1/I2/I3 只有注释与错误码，无运行期断言/注册表/归属/fail-closed；静态门禁（check-architecture.sh）看不见运行期接线 → D819 类"schema 丢掉""降级被抹掉"会静默复发。
### b) 文件审计
- `ls src/invariants` → 不存在（新建）。
- 原型级 wrap 范式先例：`src/store/session-projection.ts:384-410`（wrap `SessionStore.prototype.appendEvent`，import 即接线）。
- provider 出站点：`src/providers/base.ts:142-159`（makeRequest 组装 body，`opts.tools` 非空才写 `body.tools`）→ 唯一不改 base.ts/agent 的出站缝 = **wrap globalThis.fetch**。
- 降级可见面：`src/store/session-store.ts:376-388`（addMessage 成功时重置 `lastDegraded=false`）。
- DSH 权威参照（只借鉴范式）：`@deepseek-ai/dsh-invariants/lib/index.js`（123 行；InvariantError 带 code+packageName；register 重名拒绝、失败释放注册）。
### c) 决策
无既有覆盖 → 新建 `src/invariants/`（registry + companions + phase + health route）。写集零交集（详见 Q2）。
接口审计（从代码 grep 核实）：`SessionStore.appendEvent(sessionId, eventType, payload): AppendEventResult`（session-store.ts:397）、`SessionStore.addMessage(sessionId, role, content): void`（:376）、`addMessage` 内部 `this.appendEvent` 走原型链（wrap 可见）、provider `chat(messages, opts)` 经 `fetch(url, init)` 出站（base.ts:154）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 参考：Anthropic/DeepSeek（DSH dsh-invariants 范式）+ 第一性原理 + 结论：注册表 + 伴生注册 + 失败带归属（code/owner/packageName）+ 失败释放注册 + 原子回滚。
- 总纲 §9.4 核心洞察（派单 §〇-c 引）：**每条不变量必须显式声明"本不变量不检查什么"**——防"断言存在但给人虚假安全感"。
- 铁律 0-2（测试先行+接线验收）/ 24+31（降级显式）/ 32（错误分类）/ 38（as any 零容忍）/ 48（测试非空壳）。
- memory 教训：D819（tools schema 在 provider 边界被丢、tool_call 配对断裂、降级被抹）正是本机制要防的复发形态。
- 决策点（派单复核项）：① 注册点放 **bootstrap 新 Phase（fatal+rollbackOnFail）**——理由：不变量必须在任何业务 Phase 产出前生效，且 registered==0 要启动即失败，只有 fatal Phase 能表达；② 断言失败语义 **fail-closed**（抛 InvariantError → HTTP 5xx 语义），不许 WARN；③ 每条伴生 notChecked 显式清单。

## Q2: 范围 — 正确的最简方案
### 做什么：
1. `src/invariants/registry.ts` — InvariantRegistry（install 同步 void、重名拒绝、失败释放注册、installAll 原子回滚、hitCount/lastFailure/code/owner/packageName）+ InvariantError（code+owner+packageName）。
2. `src/invariants/companions/` 三个伴生（`install: (ctx) => void`，ctx = {fail, hit, onRollback}）：
   - `INV-TOOL-SCHEMA-SENT`（wrap globalThis.fetch：出站 chat 请求含工具对话但 tools 缺失/为空 → fail）
   - `INV-TOOL-CALL-PAIRING`（wrap `SessionStore.prototype.appendEvent`：role=tool/tool_result 的 tool_call_id 必须命中前置 assistant.tool_calls）
   - `INV-DEGRADED-STICKY`（wrap appendEvent+addMessage：写入降级但可见标记被抹 → fail；聚合粘滞：成功不抹掉，恢复可见）
3. `src/invariants/index.ts` — 单例 + installRuntimeInvariants + createRuntimeInvariantsPhase；`src/invariants/health-route.ts` — GET /api/healthz/invariants。
4. `src/deploy/bootstrap.ts` — orderedIds 加 6 + 注册 Phase 6（fatal+rollbackOnFail；registered==0 → 抛错启动失败）。
5. `src/server.ts` — 紧邻 healthzRoutes 挂载 invariantHealthRoutes（超写集理由：接线必需，铁律 0-2；commit message 说明）。
6. `src/sentinel/sentinel-events.ts` — 仅头注释更新为指向 P-2 注册表现状说明。
7. `tests/sentinel/invariants.test.ts` — 负向/正向/探针/原子回滚/void 类型断言。
8. `task-state/D831.json` impl/status；交付文档 `docs/synova/coordination/D831-squadB-自验与交付-20260921.md`。
### 不做什么（含文件路径）：
- 不改 `src/routes/healthz.ts`（小队 A 地盘）、不改 `src/providers/base.ts`、不改 `src/agent/**`、不改 `src/store/session-store.ts`（用原型 wrap，不碰文件）。
- 不碰 `scripts/**`、`.github/workflows/**`、`src/connectors|tools|routes|agent|cli-manager|tui-v2/**`、不引用 `packages/engine-core`。
- 不实现 I1 可重建/I3 可审计的运行期断言（首批只做派单 3 条）。
- 禁 `as any`/`as never`/`as unknown as`、`--no-verify`、`git stash`、force push。

## Q3: 验收 — 入口 → 交互 → 结果
- 入口：进程启动 → bootstrap Phase 6 安装 3 条伴生（registered==0 → 启动失败）；运行期工具循环/事件写入触发检查点。
- 交互：注入违约（出站请求丢 tools / tool 消息无配对 / 降级被抹掉）→ 抛 InvariantError（HTTP 层 5xx 语义，错误消息含 owner 名）；正常路径无违约。
- 结果：`GET /api/healthz/invariants` 返回 { registered≥3, invariants:[{code,owner,hitCount,lastFailure}] }；跑完正向用例后 ≥3 条 hitCount>0；第 3 条 install 抛错 → 启动失败且第 1、2 条 wrap 撤销无残留。

## 架构层: L3
（src/invariants 属 L3 洞察层信任机制；phase 仅 import type 自 deploy；health-route 为 L1 暴露面，由 server.ts 挂载）

## Done 标准:
1. `npx tsc --noEmit` 零错误；2. `npx vitest run tests/sentinel/invariants.test.ts` 全绿（含负向/正向/探针/原子回滚/void 五组，全部 expect 断言）；3. 每条伴生含 code/owner/packageName/notChecked（"不检查什么"清单）；4. 分支已 push 且 ls-remote 回执；5. M6 三件交付文档落盘；6. 三问自查可答（调用点行号/穿真实实现的测试/删掉会坏什么）。
