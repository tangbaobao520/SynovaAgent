# Task Brief — D862/P-1 出站收敛（coder-b：routes/tui-v2/server）

## Q0: 定位 — 项目拼图 + 文件审计
Synova = AI 诊断 Agent。本任务在 L1 交互层（routes/、tui-v2/）+ 健康检查面。
D821 已交付唯一出口 `src/providers/http-exit.ts`（`outboundFetch`/`getProxyStatus`，契约冻结）。
本卡把 L1 层剩余 Node 侧真实出站迁到该出口；浏览器内嵌 fetch 物理豁免。
文件审计：本写集 grep `fetch(` 非 test 共 25 处（含 1 处注释）；已逐条读上下文分类。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 出口收敛单源：参考 DSH dsh-http-proxy 单出口模式（D821 已落地，本卡只读复用，禁改契约）。
- 铁律 0-2（spec→test→impl→wire）、24/31/32（catch log + degraded + 错误分类）、
  38（as any=0）、48（测试三路径）、12（集成测试穿真实路由，llm-config 测试已用真 http 上游）。
- memory 教训：D312 禁 stash；接线失败 4 次 → wire check 必做。
- 决策参考系：参考：Anthropic/DeepSeek/第一性原理 + 结论（迁移不改信号语义，测试以真 HTTP 上游验证）。

## Q2: 范围 — 正确的最简方案
做什么：
1. src/routes/llm-config.ts:192 上游 chat/completions → `outboundFetch`（init 仅 method/headers/body(string)/signal）。
2. src/tui-v2/chat.tsx:248 loopback 出站 → `outboundFetch`（出口恒绕过 loopback，行为不变）。
3. routes/healthz.ts 增第 7 项检查 `outbound_proxy`：`getProxyStatus()` 只暴露变量名/形态，永不含值。
4. 产出 docs/synova/coordination/D862-出站分类台账-20260921.md（25 处逐条 file:line + 归类 + before/after）。
5. task-state/D862.json 更新 + 派单件同目录写集勘误附注。
不做什么（含文件路径）：
- 不改 src/providers/http-exit.ts（契约冻结，只读复用）。
- 不碰 scripts/control-tower/**、scripts/audit/**、.github/workflows/**、src/sentinel/**。
- 不碰 coder-a 地盘：src/connectors/**、src/tools/**、src/agent/**、src/cli-manager.ts、src/l1/**、src/deploy/**（含 src/deploy/watchdog-entry.ts、src/mvp-server.ts）、src/services/deepseek-balance.ts。
- 浏览器内嵌 <script> fetch（workspace/ga-diagnosis/chat/home/department-workspace/ga-evolution）不改代码，只登记豁免。

## Q3: 验收 — 入口 → 交互 → 结果
入口：POST /api/llm/test（经 outboundFetch 打真上游）、TUI cron 探测、GET /api/healthz。
处理：出口按 env 决策 direct/proxy/tunnel；loopback 恒绕过；错误按 code/phase/retryable 分类。
结果：tests/routes/llm-config.test.ts 全绿（含 NETWORK/TIMEOUT/正常三路径）、
healthz 新增检查含 outbound_proxy 且 JSON 序列化后不含任何 env 值、台账计数闭合。

## 架构层: L1（routes/tui-v2）调用 providers 层出口（L1→providers 合法，同 D821 providers 消费先例 src/providers/base.ts）

## Done 标准: 至少一条可验证的完成标准
1. `grep -rn "fetch(" src/routes src/tui-v2 src/server.ts --include=*.ts --include=*.tsx | grep -v .test.`
   仅剩：HTML 模板内嵌浏览器 fetch + 注释（台账逐条对应，Node 侧真实出站=0）。
2. `npm run lint` + vitest 相关文件全绿。
3. 台账 25 处逐条闭合；healthz 输出反推凭据不可能（测试断言）。
