# Task Brief — D861（win 代行）main 红修复·测试侧

> 2026-09-22 | 分支 `fix/d861-win-tests` | 拆自 #696（D861+D860）
> **代行声明**：三件测试在 `ownership.yaml` 落 **win 域**（`tests/agent/**`、`tests/e2e/**`、顶层散装测试无规则 → 兜底 `**`=win）。
> 创始人 2026-09-20 口令：**win 机器不在线，所有 domain:win 任务一律 Mac 代行，直到创始人说「win 回归」**。本件即代行件。

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
main 的 CI 长期红。CTO 实测 check-runs（main @ 272dbd82）：**Vitest (1/2)/(2/2) 红**，根因＝三份测试写在「仓库现实变了、测试没跟上」的状态：
1. `tests/circular-dependency.test.ts` 用 `fs.readFileSync('../packages/...')` —— **相对 cwd** 解析到仓库外，任何机器 ENOENT；
2. `tests/e2e/p0-wane-baby.test.ts` 仍按**哨兵合并前**的目录/导入假设断言；
3. `tests/agent/expert-file-loader.integration.test.ts` 仍按 **registry v3.0 之前**的专家数假设。
### b) 文件审计
grep 确认三处断言与仓库现状的差异；三份文件均为**测试文件**，不含生产代码。所属域按 `docs/synova/coordination/ownership.yaml` = win。
### c) 决策
不新建测试、不改生产代码；**只把测试对齐仓库现实**（改路径解耦 cwd / 改旧结构假设）。复用现有 vitest 配置。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 铁律 36「vitest 必须全量通过，零失败才合并」——本件的目的就是恢复这条铁律的可信度。
- 铁律 0-2「spec → test → impl → wire → review → merge」：测试是验收基准，测试本身陈旧 = **验收基准失效**（比功能坏更危险）。
- memory 历史教训：D300 黄金案例、D474 黄金数据集 checksum —— 同一族「测试/夹具陈旧导致假红/假绿」。
- 关键判据：**红是「测试陈旧」还是「代码回归」**？证据＝把文件恢复到测试声明的前提（旧目录结构）即 ENOENT/断言失配 → 属陈旧，非回归。
- 参考：Anthropic 工程基线「测试失败先判『断言是否仍描述现实』」；第一性原理：测试的作用是**证伪现实**，前提变了却不动测试＝测试在描述过去。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- `tests/circular-dependency.test.ts` —— `fileURLToPath(import.meta.url)` 从测试文件自身定位，与 cwd 解耦
- `tests/e2e/p0-wane-baby.test.ts` —— 对齐哨兵合并后的目录结构/导入
- `tests/agent/expert-file-loader.integration.test.ts` —— 对齐 `expert-registry.yaml` v3.0 六专家口径
- `task-state/D861.json` —— 本卡（已在 main，写集含 `tests/**`）
- `.claude/task-briefs/2026-09-22-D861-win测试修复-代行.md` —— 本文档

不做什么（含文件路径）：
- 不改 `src/**`（zero-code-industry / graphbridge-wiring 两个真红属 src 域，另报）
- 不改 `scripts/**`、`.github/workflows/ci.yml`（D860/D861 控制塔侧走 PR-B `gate/d861-d860-mac`）
- 不碰 `scripts/audit/**`、不写审计标准
- **禁删测试、禁 skip/xfail**（删测试＝把红刷成绿，是假绿）

## 写集

> D749 单一事实源（机器块优先；下方 Q2 散文仅作说明）。格式对齐 `scripts/control-tower/brief_parser.py:49`。

| 文件 | 类别 |
|---|---|
| `tests/circular-dependency.test.ts` | task |
| `tests/e2e/p0-wane-baby.test.ts` | task |
| `tests/agent/expert-file-loader.integration.test.ts` | task |
| `task-state/D861.json` | task |
| `.claude/task-briefs/2026-09-22-D861-win测试修复-代行.md` | task |
| `.claude/bypass.log` | builtin（post-commit hook 运行期账本，与写集无关） |

## Q3: 验收 — 入口 → 交互 → 结果
入口（从哪触发）：`git push` → CI `quality` job（TypeScript + Lint + Iron Laws）→ `test` job（Vitest 1/2、2/2）
处理（中间步骤）：三份测试在 CI runner 上定位文件、跑断言
结果（最终展示）：`Vitest (1/2)`、`Vitest (2/2)` 均 success；给 run id

## 架构层: 测试基建（跨层例外：测试不在五层依赖图中，不产生 L1-L5 边）

## Done 标准: 至少一条可验证的完成标准
- CI run id + `Vitest (1/2)` 与 `Vitest (2/2)` 均 `success`（贴原始输出，禁转述）
- 反向验证：把 `tests/circular-dependency.test.ts` 的读取路径退回 `'../packages/...'` 必红（判别性，非 grep 型判据）
- 三份测试的断言数量不减（`expect(` 计数不下降）
