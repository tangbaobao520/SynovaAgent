# Task Brief — D955 D943 文档面 + DSH 断面接线（PR-2）

> 生成: 2026-09-24 | 任务: D955 | 认领: dsh-cto（并行 CTO，主 CTO 派单）
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
治理/门禁面，不动 src/**、不触五层依赖图。本单 = D943 拆分后的「文档面 + 接线面」：
机制本体（`check-dsh-anchor.py` / `check-k3-report.py` / `daily-cto-board.sh` / 两份夹具 / `DSH-断面.json`）
已由 PR-2 的前置 PR #745 落 main（squash `1145827`）。本单做两件事：
① 清掉协调文档里的**旧断面锚**（`0.1.7-alpha.2 @ 00102833` → `0.1.7-rc.1 @ 46a7f68b`），
② 把断面门禁**接上线**（CI 自扫 + pre-commit 软检查）——即「机制建成未接线」（M3）的收口。

### b) 文件审计
- 门禁脚本：`scripts/control-tower/check-dsh-anchor.py`（自带唯一源 `docs/synova/coordination/DSH-断面.json`；
  三态 0/1/2 fail-closed；`--no-tree-check` 为 CI 模式）——已存在，本单不改。
- CI 接线点：`.github/workflows/ci.yml` L268 `done` 之后（`control-tower-tests` job 的 run 块）。
  实测该 run 块结构 = `for t in <清单>; do ... bash "$t" ... done; bash check-canary-drift.sh || true; exit $FAIL`
  → **命令不能混进 for 清单**（清单项由 `bash "$t"` 执行），须独立命令 + 计入 `FAIL`。
- pre-commit 接线点：`scripts/pre-commit-check.sh` L1479-1492（V5 平台检查 if/else 块）。
  实测 `soft_check()`（L105-123）= `soft_check <名称> <命中描述串>`，**第二参不是命令**——
  传命令串会 `grep -c .` 恒得 1 → 本地恒 ⚠️、CI（`SYNO_CI=1`，ci.yml L84-86）恒 ❌ 硬阻断。
- 旧锚残留（实测 `git grep -l '00102833' origin/main -- docs/synova/coordination`）：3 份 ——
  `Win侧同步包-20260923.md`（派单 A 类已含）+ `CTO-固化-II-研究院权威结论-20260924.md:17`
  + `CTO-固化-最终方向与在制任务-20260924.md:81`（**后两份不在原派单写集内，本单补入**）。
- 另 2 份带旧锚的 CTO 派单件（`派单-K3审计-批次4-20260924.md:17`、`派单-并行CTO-PR2-新规范接线-20260924.md:83`）
  **只存在于未合分支 #738**（main 无此文件）→ 不在本单写集，已登记待其落地时一并清。

### c) 决策
- 复用既有机制（#745 已落 main），不新建脚本；接线形态按实测结构写（见 Q1）。
- 写集 12 份（原派单 10 + 2 份补入的 CTO 固化件）——**D734 上限 12，顶格无余量**（实测 PASS）。
- 不新建门禁语义：本单只把已有门禁接到 CI/pre-commit，判定逻辑仍归 `check-dsh-anchor.py`。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 参考系：Anthropic/第一性原理 + 结论=**门禁的价值在"被调用"，不在"存在"**；M3「机制建成未接线」是本项目
  记录在案的模式（cto-handover §十四），铁律 0-2 Step 5 WIRE CHECK 同理。
- memory 教训：
  - **M1 fail-open**：接线若嵌在无关条件块内会静默永不执行（本单初稿形态实测即此坑，已改到 `fi` 之外）。
  - **CI strict 放大**：本地软提示在 `SYNO_CI=1` 下转硬（ci.yml L86）→ 误用 `soft_check` 第二参会把 PR 打红。
  - **D370 全角变量边界**：`alloc-task-id.sh` L242 复现 `BRIEF_FILE）： unbound variable`（已由 D944 在途卡覆盖）。
  - 派单 §一 括号「文件 9/10 直接取 #738 版本」与派单正文「清单加一行」自相矛盾，且两种形态实测均不正确
    （见 Q0 b）→ 本单按**正确形态**实现并在 PR 正文逐条记录偏差。

## Q2: 范围 — 正确的最简方案
做什么（12 份，逐条；路径**不带反引号**——G12 用 brief_parser 逐字比对 Q2 路径）：
- docs/synova/coordination/TASK-ROUTING.md — A 类：旧锚清零 + D943 域归属唯一源声明
- docs/synova/coordination/Win侧同步包-20260923.md — A 类：断面表锚 + 期望值命令改 rc.1
- docs/synova/coordination/两侧协作方式-Mac与Win-20260923.md — A 类：旧锚清零
- docs/synova/coordination/开工指令-Mac小队-D937-20260924.md — A 类：旧锚清零
- docs/synova/coordination/派单-W0与首模块-Win侧-20260923.md — A 类：旧锚清零
- docs/synova/coordination/规格-M9-门禁三件套-唯一事实源.md — B 类：新增（M9 规格唯一源）
- docs/synova/coordination/派单模板.md — B 类：加 D943 强制表头 + 机制在场性三问 + 文件 owner 在飞检查
- docs/synova/coordination/CTO-固化-II-研究院权威结论-20260924.md — 补：L17 断面锚 rc.1（写集外补入）
- docs/synova/coordination/CTO-固化-最终方向与在制任务-20260924.md — 补：L81 断面锚 rc.1（写集外补入）
- .github/workflows/ci.yml — C 类：control-tower-tests job 加断面自扫（--no-tree-check）并计入 FAIL；
  **只新增 done 之后的独立命令段；两份夹具所在的 for 清单保持 #745 形态**（夹具登记已完成）
- scripts/pre-commit-check.sh — C 类：soft_check "DSH 断面一致性 (D943)"，置于平台检查 fi 之外，
  先跑命令取结论再喂描述串，PYBIN 三级探测（禁裸 python3）

拆分（D734 CI 实测强制，见 PR 正文）：`docs/synova/coordination/收件闸检查单.md` 移出本 PR →
由同卡（D955）另出 PR-2a 单独落地。原因：本 PR 计数集 13 份 > 上限 12，其中 1 份是 post-commit
hook **强制追加**的运行期账本 `.claude/bypass.log`（merge-writeset-gate 本身视其为"运行期产物，
与写集无关"，D860 豁免口径却未含它）→ 顶格 12 份写集在 CI 上必假红，属两道门禁口径冲突，
已登记待 K3/CTO 裁定；本轮按 D734 指示「拆 PR」处置（禁调高上限）。
治理件（D860 不计预算）：
- .claude/task-briefs/2026-09-24-D955-d943-docs-anchor.md
- task-state/D955.json

不做什么（含文件路径）：
- 不改 `scripts/audit/**`、不改 `docs/synova/audit-reports/**`（K3 红线）
- 不改 `src/**`；不改 `docs/synova/coordination/ownership.yaml`；不改 `task-state/D943.json`（属已合 PR #745）
- 不改 `scripts/control-tower/check-dsh-anchor.py` 判定逻辑（门禁语义变更须另立卡 + K3）
- 不含未合分支 #738 的其余文件（K3 派单件、台账、D944-D953 卡）

## Q3: 验收 — 入口 → 交互 → 结果
入口：`python3 scripts/control-tower/check-dsh-anchor.py --repo .`（本机带树）/ `--no-tree-check`（CI 模式）；
      CI `control-tower-tests` job；本地 `bash scripts/pre-commit-check.sh`
处理：三态判定 OK/VIOLATION/DEGRADED；夹具 10 例 + 8 例；CI 自扫独立命令计入 FAIL
结果：两次自扫均 `DSH-ANCHOR: OK` 且 exit 0；夹具 10/10 与 8/8；CI 上 `Control Tower Gate Tests` 绿；
      pre-commit 输出 `✅ DSH 断面一致性 (D943)`

## 架构层
scripts（控制塔/治理面）——不触五层依赖图

## Done 标准
- [ ] verify: `python3 scripts/control-tower/check-dsh-anchor.py --repo .` → `DSH-ANCHOR: OK`（exit 0）
- [ ] verify: `python3 scripts/control-tower/check-dsh-anchor.py --repo . --no-tree-check` → `DSH-ANCHOR: OK`（exit 0）
- [ ] verify: `bash tests/control-tower/check-dsh-anchor.test.sh` → 10 通过 0 失败；`check-k3-report.test.sh` → 8 通过 0 失败
- [ ] verify: 5 份 A 类文档 `grep -c '00102833'` = 0 且 `grep -c '0.1.7-alpha.2'` = 0
- [ ] verify: `bash scripts/control-tower/check-pr-budget.sh --base origin/main` → 12 文件 ≤ 12 + 单域 mac PASS
- [ ] verify: CI `Control Tower Gate Tests (ubuntu/windows)` 与 `TypeScript + Lint + Iron Laws` 全绿
