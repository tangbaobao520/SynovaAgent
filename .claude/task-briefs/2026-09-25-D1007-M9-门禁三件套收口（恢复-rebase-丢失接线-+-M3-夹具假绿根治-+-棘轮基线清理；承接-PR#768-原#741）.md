# Task Brief: D1007 M9 门禁三件套收口（恢复 rebase 丢失接线 + M3 夹具假绿根治 + 棘轮基线清理；承接 PR#768/原#741）

> 生成: 2026-09-25 | 任务: D1007 | 认领: coder-b
> 参考: D333 决策四步（第一性原理→Anthropic→开源实证→收敛）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔/门禁层（`scripts/control-tower/**` + `tests/control-tower/**` + `.github/workflows/ci.yml`）。
本卡是 **M9 门禁三件套**（A 模式哨兵 = grep 方言语法验证 / B CI 登记 gate = 密封面棘轮 / C 既有红基线
对账）的收口：① 恢复 rebase 静默丢失的 CI 接线 ② M3 判别性夹具的"空跑判绿"根治 ③ 棘轮基线清理与
方言无关化。**不新增机制，只把既有三件套修对、接上、验穿。**
### b) 文件审计（grep 实证）
- `scripts/control-tower/check-gate-integrity.sh` — 三件套本体（A/B/C 三段 + `_sentinel_grep` 方言分判）
- `tests/control-tower/check-gate-integrity.test.sh` — 配对夹具（55 断言；本卡修方言敏感 + 崩溃）
- `tests/control-tower/precommit-groups-injection.test.sh` — 注入抽检夹具（13 组判别性）
- `.github/workflows/ci.yml:353` — `gate-integrity:` job（step1 检查器 / step2 注入抽检 / step3 `--ci-reds`）
- `scripts/control-tower/{ci-red-baseline,gate-integrity-baseline}.txt` — 双棘轮基线
- `scripts/control-tower/merge_writeset_gate.py:122` — 写集对账门禁（**本卡不改**；DID 误判另立卡）
### c) 决策
既有三件套覆盖 → **复用/修复**，不新建。判据改动一律走"清单清理"而非"放宽口径"（棘轮只减不增）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 铁律 0-2（spec→test→impl→wire→review，接线缺一不可）：本卡事故正是 **wire 环丢失**。
- 铁律 35（自动化优先）+ 铁律 48（测试不可为空壳/判别性）：M3 夹具的"变异体未施加仍判绿"违反之。
- 铁律 11（静默降级禁止）：检查器三态 0/1/2，降级必须可见（`GATE-INTEGRITY: DEGRADED`）。
- D664「机制建成未接线」同型复发教训；D370 全角标点紧贴变量 → `unbound variable`。
- 方言教训（D938/D937）：凡期望值依赖"本平台把 X 判为非法 ERE"的断言 = 方言敏感，必改方言无关构造。
参考：Anthropic（fail-closed + 可核查证据）+ 第一性原理（门禁判据须方言无关、失败路径须可播报）
+ 收敛（改 1 行夹具 > 改判据语义）→ 结论：夹具换 `a(b`、`^+++` 只作探针，不动检查器判据。

## Q2: 范围 — 正确的最简方案
做什么（= 本卡写集，与 `git diff --name-only origin/main..HEAD` 逐条一致，共 16 条）：
- `.github/workflows/ci.yml` — 恢复 `gate-integrity:` job 三 step 接线（rebase 曾整套丢失）
- `scripts/control-tower/check-gate-integrity.sh` — 三件套本体（M9 交付物）
- `scripts/control-tower/ci-red-baseline.txt` — 登记存量红 `Vitest (1/2)`（分片名漂移）
- `scripts/control-tower/gate-integrity-baseline.txt` — 清 1 条 stale 条目 + 计数口径同步
- `scripts/control-tower/merge_writeset_gate.py` — D954 支持（写集对账门禁本体；本卡不改其 DID_RE）
- `tests/control-tower/check-gate-integrity.test.sh` — M3 真变异 + FIRST_FAIL + 方言无关夹具 + 崩溃修
- `tests/control-tower/merge_writeset_gate.test.sh` — 配对夹具扩展
- `tests/control-tower/precommit-groups-injection.test.sh` — 注入抽检夹具（b 面基线 + 7a 自误伤清零）
- `.claude/task-briefs/2026-09-24-M9-gate-integrity.md` — M9 卡 task brief
- `.claude/task-briefs/2026-09-25-D954-ci-reds-base-ref.md` — D954 卡 task brief
- `.claude/task-briefs/2026-09-25-D1007-M9-门禁三件套收口（恢复-rebase-丢失接线-+-M3-夹具假绿根治-+-棘轮基线清理；承接-PR#768-原#741）.md` — 本卡 brief（本文件）
- `memory/notes/proposed/2026-09-24-m9-gate-integrity.md` — M9 决策 Note（铁律 49）
- `memory/notes/proposed/2026-09-25-d954-ci-reds-base-ref.md` — D954 决策 Note
- `task-state/D954.json` — D954 声明
- `task-state/D1007.json` — 本卡声明（write_set 单源）
- `.claude/bypass.log` — post-commit hook 证据账本（运行期产物，随提交自动追加）
不做什么：
- 不改 `scripts/control-tower/merge_writeset_gate.py:122` 的 `DID_RE`（判据语义变更须过 K3，CTO 另立卡）
- 不改 `scripts/pre-commit-check.sh`（7a 排除正则 `_DIAG_EXCL` 属门禁判据，本卡只用运行时拼接规避自误伤）
- 不改 `scripts/audit/**`（K3 专属红线）、不改 `docs/synova/audit-reports/**`
- 不扩写集到另 10 个 `tests/control-tower/*.test.sh` 的全角紧贴变量（CTO 已另立卡）

## Q3: 验收 — 入口 → 交互 → 结果
入口：`bash scripts/control-tower/check-gate-integrity.sh [--patterns-only|--registry-only] [--ci-reds <json> --base-ref <ref>]`
处理：A 段解析全仓 grep 模式 → 方言语法验证 + 棘轮核对；B 段密封面登记核对；C 段 check-runs 对账
结果：末行 `GATE-INTEGRITY: OK`（exit 0）/ `VIOLATION(n)`（exit 1）/ `DEGRADED`（exit 2）；
     CI 侧 `gate-integrity` job 三 step 全绿 + 关键行以 `::notice` 注解公开可检索。

## 架构层:
scripts（控制塔 / 门禁层，L0 治理面，不涉 L1-L5 产品分层）

## Done 标准
- [ ] verify: `grep -c 'gate-integrity:' .github/workflows/ci.yml` → 3
- [ ] verify: `git log origin/main..HEAD -- .github/workflows/ci.yml` → 非空
- [ ] verify: `bash scripts/control-tower/check-gate-integrity.sh` → exit 0 且末行 `GATE-INTEGRITY: OK`
- [ ] verify: `bash tests/control-tower/check-gate-integrity.test.sh` → `PASS=55 FAIL=0`（BSD 与 GNU 仿真各一次）
- [ ] verify: `GITHUB_EVENT_PATH=<event> python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD --branch fix/d1007-m9-gate-trio-v2` → `✅ pass`
