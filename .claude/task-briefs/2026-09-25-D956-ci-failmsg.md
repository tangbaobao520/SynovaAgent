# Task Brief — D956 CI 失败消息观测性

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
CI 观测域：失败断言行优先入窗 + 截断自报 + 注解三态（D956 卡，#804）。
### b) 文件审计
分支改动 3 文件（git diff origin/main...fix/d956-ci-failmsg 实测）：ci.yml MSG 构造区 + 新判别夹具 + bypass.log（机械）。
### c) 决策
声明源与分支实况一一对应。

## Q1: 调研
坑清单"CI 失败断言给完整片段（禁 tail -8）"——D956 即其修复卡。三态语义（注解失败 ⇒ exit 2）。

## Q2: 范围 — 正确的最简方案
做什么（3 文件逐条，与 task-state/D956.json write_set 一致）:
- tests/control-tower/d956-failmsg.test.sh
- .github/workflows/ci.yml
- .claude/bypass.log
不做什么：不改门禁语义、不新增阻断、不碰 scripts/audit/**、src/**。

## Q3: 验收 — 入口 → 交互 → 结果
入口：ci.yml 注解生成路径 / d956-failmsg.test.sh
处理：失败行 grep -E 优先入窗；截断时注解含 truncated + 完整日志位置；注解失败 exit 2
结果：夹具全绿；多行失败注解必含失败断言行；超长输出必含 truncated

## 架构层: CI 观测域
## Done 标准:
- bash tests/control-tower/d956-failmsg.test.sh 全绿
- 注解三态可判（0 过/1 拦/2 降级）
