# D977 — PR 归属规则 R1–R4（新文件承载）+ 派单参数更正

## Q0: 定位
- 域: mac（流程/协调文档）
- 对象: 新建 `docs/synova/coordination/PR归属与派单参数-20260926.md`

## Q1: 调研
- 旧规则「PR 由 CTO 开」为 CTO 自加，非 DSH/行业实践；实测代价：交付滞留单点数小时
- 直接改 `派单模板.md` 会触发 D328（该文件归属 D931/D943）⇒ 本轮 CTO 亲历 3 次拒收
- `merge_writeset_gate.py:361-366` 实测参数为 `--repo-root`，无 `--repo`（FIX-010）

## Q2: 范围
- 做: R1–R4 全文 + 参数更正 + 为何不改共享文件的说明 + 未清项
- 不做: 不改 `派单模板.md`（待 FIX-011）；不碰 `scripts/audit/**`、`src/**`

## Q3: 验收
- 入口: 派单件与 PR 流程
- 处理: 产出者自开 PR + 六项模板；CTO 只做收件闸/K3/门禁批准
- 结果: 交付不再停在单点

## 架构层
流程（非五层产品码）

## Done 标准
1) 新文件含 R1–R4 与 `--repo-root` 更正（可 grep）
2) 未改任何有历史归属的共享文件
3) 未清项显式登记（指针待补 / R2 无机械校验）
