# Task Brief: FIX-002 — #796 与 D331 同改：新 clone 态对账器读面（无账本来源 → 显式放行）
> 认领: 🧭 fix-coder-a（synova-squad-lead 小队）｜ 分支: feat/d969-daily-cto-board-paired-test ｜ 基线: origin/main
#CRITERIA: A

## Q0: 定位
### a) 拼图
控制塔提交链路（scripts/control-tower/）的 D331 执行证据链对账器 `check-bypass-log.sh`。台账 FIX-002：**#796 只删账本、不改对账实现** ⇒ 先合则「本机无账本残留」的 clone 全员拒推。本卡补 D331 **读面**。
### b) 文件审计
- `scripts/control-tower/check-bypass-log.sh:48-52`（改前）无条件 `[[ ! -f "$LOG" ]] → exit 1`；`$LOG = $ROOT/.claude/bypass.log`，而 #796 已删其 git 跟踪、D970 Stage 2 停写旧路径 ⇒ 新 clone 上必然缺失 ⇒ 每次推送被判「执行证据链缺失」。
- 冲突扫描（改前，共 30 处）：唯一生产调用方 `scripts/pre-push-check.sh:425`（门禁 7，base 取 `origin/main`）；调用方 `scripts/control-tower/pre-audit-summary.sh:46`；配对测试 `tests/control-tower/check-bypass-log.test.sh`（改前 5 用例，其中「bypass.log 缺失 → exit 1」即本卡要改的语义）。
- 同批冲突：`fix/d970-bypass-ledger-stage2`（sha 1c7ab52f）改同一文件且读面口径在「范围非空 + 无来源」态互斥 ⇒ 已上报 CTO 裁决（不擅自扩写集、不宣称两口径一致）。
### c) 决策
复用既有门禁（铁律 35）：只把「本机零来源」这一态从 exit 1 改为**显式放行 exit 0**，其余红路径一条不减；不新建门禁、不改调用方、不碰 ci.yml。

## Q1: 调研
铁律 35（能变 check 的不靠 review）、铁律 48（正常/降级/边界 + 真断言）、铁律 11（降级显式不静默）、
ctrl-tower-change 模式 1（三态退出码 0 过 / 1 业务阻断 / 2 执行失败）。
历史教训：D414/U1c（`|| true` 把「git 失败空循环」当对账通过 = M1 假 PASS）、D451（纯补记豁免打断死循环）、
D508/D513（merge-base + 防御 fetch）、D735/D970（账本出库，登记缺口已显式登记）、D555（沙箱测试污染宿主 index）。
参考：第一性原理（可对账 ≠ 必须存在对账对象）+ Anthropic 基线（fail-closed 应绑定「确有东西要检查」）+ 开源实证（新 clone 上本地缓存必空，生态常规是「无本地态 → 跳过」）+ 收敛检查（台账 FIX-002 判据要求新 clone 态 rc=0，且必须保留全部既有红路径）＋ 结论：无来源 → 显式 vacuous pass，红路径不减。

## Q2: 范围 — 最简方案
做什么：
- scripts/control-tower/check-bypass-log.sh
- tests/control-tower/check-bypass-log.test.sh
不做什么（含文件路径）：
- 不改 .github/workflows/ci.yml （本批 ci.yml 单写者 = FIX-013 任务）
- 不改 scripts/control-tower/bypass-ledger.sh （D970/#799 写面）
- 不改 scripts/hooks/post-commit.sh （D970/#799 写面）
- 不改 scripts/pre-push-check.sh （调用方无需改：传 origin/main 语义不变）

## Q3: 验收
入口: `bash scripts/control-tower/check-bypass-log.sh`（pre-push 门禁 7 与测试同源调用同一脚本）。
处理: 计算账本来源 union（旧路径 + per-session + 归档）→ 无来源则显式放行；有来源则照旧逐提交对账。
结果: 新 clone 态（账本不在树 + 本机无残留）rc=0 且输出必须可见；测试 9/9 绿；两变异体下夹具报红（判别力）。

## 架构层: 基础设施（scripts/control-tower 控制塔）
## Done 标准
- [ ] verify: `bash tests/control-tower/check-bypass-log.test.sh`（exit 0；含「无账本来源 → exit 0 且显式可见」与「有账本 + detected-bypass + 提交无登记 → exit≠0」成对反例）
- [ ] verify: 新 clone 态 `bash scripts/control-tower/check-bypass-log.sh; echo rc=$?`（rc=0）
- [ ] verify: `SYNO_CT_STAGED="scripts/control-tower/check-bypass-log.sh" bash scripts/control-tower/ct-test-gate.sh`（配对测试门禁 exit 0）
