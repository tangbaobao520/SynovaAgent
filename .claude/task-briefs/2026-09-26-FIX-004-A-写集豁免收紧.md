# Task Brief — FIX-004 A 段（承 D964）：写集豁免收紧（audit-reports 精确键 + archive 仅真重命名）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
L0 控制塔门禁面：**合并级写集对账 gate**（`scripts/control-tower/merge_writeset_gate.py`，D708）。台账 FIX-004 A（P0）：D964 阶段 1 引入的两条**路径级 glob 内置豁免**（`docs/synova/audit-reports/**`、`archive/**`）让「任意文件放进这两条路径」即**静默豁免** ⇒ 夹带口子（K3 定罪 + verifier 三项复现）。本卡按台账指定口径收紧，并给成对反例。
### b) 文件审计（实测，2026-09-26，base=`chore/cto-doc-slim-phase1-writeset-exempt` @ `b8573c7a`）
- `scripts/control-tower/merge_writeset_gate.py:61-70` BUILTIN_EXEMPT（两条 glob）；`:461-470` 逐文件豁免分支用 `fnmatch.fnmatch(f, p)` 无差别匹配
- `changed_files():113-123` 用 `--name-only --no-renames`（看不出 R/A/D）⇒ 归档豁免无法区分「真重命名」与「新增/改写」
- 夹具：`tests/control-tower/merge_writeset_gate.test.sh`（分支版 33+ 断言；⑨ 段原断言只覆盖 glob 正向，无滥用反例）
- blast radius 实测：`docs/synova/audit-reports/` tracked = **151 文件**（含 `INDEX.md` 1 个）；顶层 `archive/` tracked = **0 文件**（D964 阶段 4 实际归档到 `.claude/task-briefs/archive/**`，属 `.claude/` 文档范围）
### c) 决策
按台账原文收紧：① audit-reports 豁免收窄为**精确键** `docs/synova/audit-reports/INDEX.md`；② `archive/**` 仅对 `git diff --find-renames` 的 **R 条目**放行（源与目标需有一端在 `archive/` 前缀内）。取 R 必须新增 `--name-status --find-renames` 取数（既有 `--no-renames` 取不到 R），失败即 degraded exit 2（不静默当无重命名）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 第一性原理：豁免必须**可枚举**（精确对象），路径级 glob = 不可枚举的放行口；「移动」与「新增/改写」的物理区别 = git 的 R vs A/D。
- Anthropic 基线：判据可执行 + **变异体改坏即红** + 降级不冒充通过。
- memory：D964 K3 请求 §二 A（要求 K3 自造反例）；K3 批次 4 已定罪「豁免不看内容与来源」；本仓先例 = 豁免条目必须逐条打印理由（D861）。
- 参考：Anthropic/第一性原理 + 结论=「精确键 + 仅 R 放行 + 成对反例 + 打印口径」。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- scripts/control-tower/merge_writeset_gate.py — 改：BUILTIN_EXEMPT 的 audit-reports 键收紧为精确路径；`archive/**` 移出 BUILTIN_EXEMPT，新增 `ARCHIVE_PREFIX` / `ARCHIVE_RENAME_REASON` + `renamed_paths()`（`--name-status --find-renames`，失败 fail-closed exit 2）+ 豁免分支只认 R（源/目标需一端在 archive/）
- tests/control-tower/merge_writeset_gate.test.sh — 改：⑨ 段重写为成对反例（⒜ INDEX.md 精确键绿 / ⒜-2 真 rename 绿 / ⒝-1 非 INDEX 红 / ⒝-2 源码夹带红 / ⒝-3 新增进 archive 红 / ⒝-4 mv+改写 A+D 红）+ glob 变异体反证
- docs/synova/product-lines/evidence/FIX-004-A-20260926.md — 新：原始输出
- memory/notes/proposed/2026-09-26-fix004-a-writeset-exempt-tighten.md — 新：决策 Note
- .claude/task-briefs/2026-09-26-FIX-004-A-写集豁免收紧.md — 本 brief

不做什么（含文件路径）：
- 不改 `scripts/doc-system/doc-registry-gate.sh`（B/C 段在另一条 stacked 分支）
- 不改 `.github/workflows/ci.yml`（单写者 fix-coder-a；本卡只改 gate 本体）
- 不动 `scripts/audit/**`、`docs/synova/audit-reports/**`（禁碰域，本卡零触碰）
- 不动 `docs/synova/coordination/问题修复台-20260925.md`（CTO 单写者）
- 不删/不放宽既有声明源与 `## 写集豁免` 机制

## Q3: 验收 — 入口 → 交互 → 结果
入口：CI PR job `Merge write-set reconciliation (D708)` → `python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD --branch <head>`
处理：取变更集（`--no-renames`）+ 取重命名对（`--find-renames`）→ 逐文件：精确键豁免 → 归档 R 豁免 → 声明 → 显式豁免 → 否则夹带
结果：合法 INDEX.md / 真 `git mv` → exit 0 且逐条打印豁免理由；非 INDEX 文件、新增进 archive、A+D 改写 → exit 1 并逐文件点名

## 架构层: 基础设施（L0 控制塔门禁面）

## Done 标准
- [ ] verify: `bash tests/control-tower/merge_writeset_gate.test.sh` → 58 通过 / 0 失败（含 ⑨⒜/⒝ 六条新断言）
- [ ] verify: 沙箱成对反例原始输出（⒜ 绿 ↔ ⒝ 红）入 evidence，且**修前 vs 修后**同场景对照（⒝-1/⒝-3 修前静默放行 → 修后必红）
- [ ] verify: `git diff --stat` 仅 2 文件（gate + 夹具）+ 证据/Note/brief；`scripts/audit/**`、`docs/synova/audit-reports/**` 零触碰
- [ ] verify: PR base 显式 = `chore/cto-doc-slim-phase1-writeset-exempt`（栈式 PR 声明 base）+ `git ls-remote --heads origin | grep <branch>` 回执
- [ ] 门禁语义变更 ⇒ **K3 复审通过前不得合并**（PR 正文显式标注）
