# Task Brief — FIX-004 B/C 段（承 D964）：同类文档唯一性两处修正（同目录约束 + 取代目标存在性）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
治理文档门禁面：`scripts/doc-system/doc-registry-gate.sh` 的「同类文档唯一性」（D964 阶段 3 新增判据）。台账 FIX-004 B/C（P1）：B = 归一化只取 basename ⇒ 新目录放 `README.md` 被误拦（假红）；C = 取代声明只匹配任意非空 token ⇒ 写一行「取代: 不存在」即放行（漏拦）。
### b) 文件审计（实测，2026-09-26，base=`chore/cto-doc-slim-phase3-dup-rule` @ `8d8b46a3`）
- `scripts/doc-system/doc-registry-gate.sh`（分支版）：`norm()` 在嵌入 python :70-74；撞名判定 `:84-88`（只比归一化名）；取代声明判定 `:113`（`re.search(r"^[ \t]*(取代|合并|supersedes)[ \t]*[:：][ \t]*\S", …)`）
- 夹具：`tests/doc-system/doc-dup-rule.test.sh`（分支版 13 断言，无 B/C 反例）
- 陷阱（实测）：该 python 段是 `$( … <<'PYEOF' …)` 命令替换内的 heredoc —— **反引号在其中仍被 bash 解析**（实测 2 个反引号 → `unexpected EOF looking for matching backtick`）；本卡改动因此零反引号（用 `chr(96)`）
### c) 决策
B：撞名判定加**同目录约束** —— 同目录撞名必判；跨目录仅**非通用名**判；通用名（README/index/CHANGELOG…）跨目录放行。C：取代声明目标必须 ∈ tracked 全集 ∪ 本次新增集合，否则判红并指名。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 第一性原理：唯一性判据的**对象**是「同一目录里的同一份文档」；跨目录同名是正常结构（每目录一个 README）。
- 第二性原理：声明式豁免必须**可解析**（指向真实对象），否则是免罚咒语。
- Anthropic 基线：判据两侧都要有反例（合法仍绿 / 滥用仍红），且修前/修后同夹具对照。
- memory：K3 批次 4 定罪「空声明绕过」；本仓先例 = 门禁不许被无声改软。
- 参考：Anthropic/第一性原理 + 结论=「同目录约束 + 目标存在性校验 + 成对反例」。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- scripts/doc-system/doc-registry-gate.sh — 改：新增 `GENERIC_NAMES` + `_same_dir()`，撞名判定分同目录/跨目录两支；取代声明改捕获目标并校验 ∈ `_tracked_all` ∪ `_newset`
- tests/doc-system/doc-dup-rule.test.sh — 改：新增 ⑦（B：⒜ 跨目录 README 绿 / ⒝-1 同目录撞名红 / ⒝-2 跨目录非通用名红）与 ⑧（C：⒜ 目标存在绿 / ⒝ 目标不存在红），断言 13 → 21
- docs/synova/product-lines/evidence/FIX-004-BC-20260926.md — 新：修前/修后原始输出
- memory/notes/proposed/2026-09-26-fix004-bc-doc-dup-rule-tighten.md — 新：决策 Note
- .claude/task-briefs/2026-09-26-FIX-004-BC-文档唯一性修正.md — 本 brief

不做什么（含文件路径）：
- 不改 `scripts/control-tower/merge_writeset_gate.py`（A 段在另一条 stacked 分支 `fix/fix004a-writeset-exempt-abuse`）
- 不改 `.github/workflows/ci.yml`（单写者 fix-coder-a）
- 不动 `scripts/audit/**`、`docs/synova/audit-reports/**`
- 不扩通用名清单之外的口径、不做「取代目标语义校验」（属新判据，须过 K3）

## Q3: 验收 — 入口 → 交互 → 结果
入口：`bash scripts/doc-system/doc-registry-gate.sh`（本地/CI 文档登记门禁）
处理：取新增 `.md` → 归一化名撞名判定（同目录必判 / 跨目录非通用名判）或内容指纹 → 命中则校验「取代/合并/supersedes」声明的目标是否真实存在
结果：合法新增（跨目录 README、指向真实目标的取代声明）exit 0；同目录撞名、跨目录非通用名撞名、空声明 → exit 1 且逐条点名（含「取代声明目标不存在」）

## 架构层: 基础设施（治理文档门禁）

## Done 标准
- [ ] verify: `bash tests/doc-system/doc-dup-rule.test.sh` → 21 通过 / 0 失败（含 ⑦/⑧ 八条新断言）
- [ ] verify: 修前对照（`git show <base>:scripts/doc-system/doc-registry-gate.sh` 副本 + 同一夹具）→ 5 条失败，证明每侧反例都有判别力
- [ ] verify: `bash -n scripts/doc-system/doc-registry-gate.sh` 通过（heredoc 内零反引号，实测坑已避）
- [ ] verify: PR base 显式 = `chore/cto-doc-slim-phase3-dup-rule`（栈式 PR）+ `git ls-remote --heads origin | grep fix/fix004b` 回执
- [ ] 门禁语义变更 ⇒ **K3 复审通过前不得合并**
