# Task Brief — FIX-006 取号 O(1) + 拒绝面使用读取面同源（alloc-task-id.sh）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔取号入口（D# 统一分配器）。台账 FIX-006：① 取号挂死（同类第 3 次 🔴）② 占用表读取面 ≠ 拒绝面。
### b) 文件审计（实测 2026-09-26，base=`origin/main` @ `ef299746`）
- 累加点（**实测 1 处**，台账"5 处"不成立）：`scripts/control-tower/alloc-task-id.sh:295-296` 字符串累加（双层循环 worktree × task-state 文件）
- 读取面：`:252` 本地 ∪ `:261` origin/main ∪ `:295` worktree task-state ∪ `:307-321` 分支 → `USED` → `NEXT`
- 拒绝面：`_occupy_locations():170-220` 查 ①本地 ②origin/main ③远端分支名 ④本地分支名 ⑤worktree **目录名** —— **独缺"其它 worktree 的 task-state"**（读取面同源）
- 规模：worktree **308**；主仓 task-state **364**；全 worktree task-state ≈87,130（verifier 同源）
- 注入缝齐备：`SYNO_TASK_STATE_DIR` / `SYNO_BRIEF_DIR` / `SYNO_LOCK_DIR` / `SYNO_ALLOC_NO_WORKTREE|REMOTE|BRANCH`
### c) 决策
① 累加改**追加写临时文件**（O(1)/次，末尾一次读入），同文件充当"号→路径"索引；② 拒绝面新增 ⑥：**优先复用读取面产物** `WT_USED_FILE`，索引缺失时按号定向探测（O(#worktree)，不全表扫描）。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 第一性原理：占用判定必须**单一事实源**——读取面与拒绝面若取不同源，必然漏号。
- Anthropic 基线：判据要可判别（修前/修后 + 变异体）；降级不静默。
- memory：D940 已修同型（每文件一次 basename 子进程 → 258s）；本卡是同一热路径的第二处（字符串累加）。
- 参考：Anthropic/第一性原理 + 结论=「O(1) 追加 + 拒绝面复用读取面结果 + 成对反例」。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- scripts/control-tower/alloc-task-id.sh — 改：worktree 扫描结果写临时文件（`WT_USED_FILE`）+ `WORKTREE_USED` 一次读入；`_lock_release` 内清理；`_occupy_locations` 新增 ⑥（复用索引 / 定向探测）
- tests/control-tower/alloc-task-id.test.sh — 改：新增 FIX-006 段（⒜ 未占用放行 / ⒝ 已占用拒绝+点名 / 变异体禁用 ⑥ → 重新漏判）
- docs/synova/product-lines/evidence/FIX-006-20260926.md — 新：计时 + 成对反例原始输出
- memory/notes/proposed/2026-09-26-fix006-alloc-task-id-o1-and-reject-face.md — 新：Note
- .claude/task-briefs/2026-09-26-FIX-006-取号O1与拒绝面.md — 本 brief

不做什么（含文件路径）：
- 不改 `scripts/pre-commit-check.sh`（fix-coder-a 单写者；另件 P1 单独串行处理）
- 不改 `.github/workflows/ci.yml`（单写者）
- 不碰 `scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`

## Q3: 验收 — 入口 → 交互 → 结果
入口：`bash scripts/control-tower/alloc-task-id.sh "<name>" [--dry-run]` / `--check-id <D#>`
处理：worktree 扫描（O(n) 追加）→ 占用表合并 → NEXT → `_occupy_locations`（含 ⑥ worktree task-state）
结果：取号 94s→7s（同输出）；别处已登记的号 → 拒绝并点名 `worktree-task-state`

## 架构层: 基础设施（控制塔取号入口）

## Done 标准
- [ ] verify: `bash tests/control-tower/alloc-task-id.test.sh` → PASS=58 FAIL=0
- [ ] verify: 计时对照（同口径 dry-run）修前 94s / 修后 7s，输出同为 D1009
- [ ] verify: 成对反例 ⒜ rc=0 / ⒝ 修前 rc=0（漏判）→ 修后 rc=1 + `worktree-task-state` 点名
- [ ] verify: `bash -n scripts/control-tower/alloc-task-id.sh` 通过
- [ ] verify: `git ls-remote --heads origin | grep fix/fix006-alloc-task-id` 回执
