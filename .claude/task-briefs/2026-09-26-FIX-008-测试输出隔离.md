# Task Brief — FIX-008 测试副作用碰 tracked 文件（founder-truth 生成物输出隔离）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
控制塔测试卫生面（M8 家族「测试副作用碰 tracked 文件」**第 3 例**）。`tests/control-tower/founder-truth.test.sh` 直接跑 `founder-truth.py --offline --html` → 生成器把 `docs/synova/founder-console.html` / `docs/synova/founder-alerts.md` 写进**仓库 tracked 路径** ⇒ 跑完工作区变脏（切分支/提交被拦）。
### b) 文件审计（实测 2026-09-26，base=`origin/main` @ `ef299746`）
- 写点：`scripts/control-tower/founder-truth.py:32-33`（`HTML_OUT`/`ALERT_OUT` 硬编码 `REPO/docs/synova/`）+ 写入调用 `:289/:298`（`write_alert`）与 `:422`（`--html`）
- 触发：`tests/control-tower/founder-truth.test.sh:75`（`python3 "$GEN" --offline --html`）
- 修前实测：跑一次测试 → `git status --porcelain` = ` M docs/synova/founder-alerts.md` + ` M docs/synova/founder-console.html`（共 2 处；diff 17+/9-）
- 调用方：仅该测试直接调用（`grep -rn "founder-truth"`）；无 CI workflow 调用（cron 拾取告警文件为运行期路径）
### c) 决策
加 **env 注入缝** `SYNO_FOUNDER_OUT_DIR`（缺省 = 现状路径，生产行为不变；符合控制塔既有 env 缝模式），测试改走 `mktemp -d` 隔离目录；判据 = 「跑完仓库脏度不增加 + 两个生成物 porcelain 为空」。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 第一性原理：测试的可观测副作用必须是**私有**的（临时目录），不得改写被测系统的持久状态。
- Anthropic 基线：判据要**可判别**（改坏即红）—— 故设 ⒝ 沙箱反例证明「干净判据」非恒真。
- memory：M8 家族第 3 例（前两例：product-lines refresh、founder-console 同源）；铁律 11（不静默）、铁律 48（三路径）。
- 参考：Anthropic/第一性原理 + 结论=「env 注入缝 + 隔离目录 + 增量式干净判据 + 沙箱反例」。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- scripts/control-tower/founder-truth.py — 改：`OUT_DIR`（`SYNO_FOUNDER_OUT_DIR` 可覆盖，缺省 `REPO/docs/synova`）；两处写前 `OUT_DIR.mkdir(parents=True, exist_ok=True)`。**不动生成内容/判据/退出码**
- tests/control-tower/founder-truth.test.sh — 改：`GENRUN()` 统一注入隔离目录；新增 ⒜（生成物 porcelain 前/后 + 仓库脏度增量）与 ⒝（沙箱脏树反例）断言；**既存失败断言原样保留**
- docs/synova/product-lines/evidence/FIX-008-20260926.md — 新：原始输出
- memory/notes/proposed/2026-09-26-fix008-founder-truth-test-isolation.md — 新：决策 Note
- .claude/task-briefs/2026-09-26-FIX-008-测试输出隔离.md — 本 brief

不做什么（含文件路径）：
- ~~不改该夹具的既存失败断言~~ → **追加修正（经物理门禁强制）**：6 处 `echo "$OUT" | grep -q` → `grep -q <<< "$OUT"`（**仅机制**，pattern/预期一字不动）。原因：pre-commit 组 2 **U7/CT-40 硬阻断**「控制塔脚本配对测试红」⇒ 加注入缝与配对测试红正面冲突，无其它通路（无 `--no-verify`）。成对证据：改前 8/3（抖动 3-5）→ 改后 **11/0**；变异体（pattern 改不存在）→ **10/1**（断言仍具判别力）。已显式回报 lead 备裁
- 不提交 `docs/synova/founder-console.html`、`docs/synova/founder-alerts.md` 的生成物改动（实验污染已 `git checkout --` 还原）
- 不改 `.github/workflows/ci.yml`（单写者 fix-coder-a）；不碰 `scripts/audit/**`、`docs/synova/audit-reports/**`

## Q3: 验收 — 入口 → 交互 → 结果
入口：`bash tests/control-tower/founder-truth.test.sh`
处理：全部生成调用经 `SYNO_FOUNDER_OUT_DIR=<mktemp -d>` 隔离；跑完比对生成物 porcelain 行数与仓库总脏度
结果：生成物 porcelain `前=0 后=0`；仓库脏度不增加；沙箱反例 `前=0 → 后=1`（判别性成立）

## 架构层: 基础设施（控制塔测试卫生）

## Done 标准
- [ ] verify: `bash tests/control-tower/founder-truth.test.sh` 后 `git status --porcelain` 仅含本卡改动文件（无生成物污染）
- [ ] verify: ⒜ 判据输出「生成物 porcelain 行数: 前=0 后=0」+「仓库脏度 前=后」
- [ ] verify: ⒝ 沙箱反例输出「前=0 → 后=1」
- [ ] verify: `git diff --stat` 仅 2 个源文件改动（+ evidence/Note/brief）
- [ ] verify: `git ls-remote --heads origin | grep fix/fix008-test-isolation` 回执
