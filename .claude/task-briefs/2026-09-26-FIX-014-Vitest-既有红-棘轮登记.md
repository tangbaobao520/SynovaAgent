# Task Brief — FIX-014 main `Vitest (2/2)` 既有红：复核 + D954 棘轮登记（含 disposition_due + 责任人）

#CRITERIA: A

## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
L0 控制塔门禁面（CI 既有红棘轮）。台账 FIX-014：main `Vitest (2/2)` = failure 已由 CTO 实测（本卡复核并出原始输出）；K3 批次 4 曾定罪「`disposition_due` 零消费 ⇒ 3 日 SLA 是文档承诺不是门禁」，D954 已补消费实现，但**基线文件里 0 条含该键** ⇒ 棘轮缺「处置期限」这层物理倒逼。本卡是**登记补键 + 另开卡**，不改判据语义、不改 CI。
### b) 文件审计（实测，2026-09-26，base=`origin/main` @ `ef299746`）
- `scripts/control-tower/ci-red-baseline.txt`：66 行 / 数据行 6 条；`grep -c disposition_due` = **0**；`Vitest (2/2)` = `owner=UNASSIGNED`；`Vitest (1/2)` = `owner=CTO-Mac`
- `scripts/control-tower/check-gate-integrity.sh:678` 默认棘轮路径（`SYNO_CI_RED_BASELINE` 注入缝）；`:749` 取 `disposition_due`；`:757-765` 逾期判红（键缺失不回溯）
- 上游：`memory/notes/proposed/2026-09-25-d954-ci-reds-base-ref.md`（D954）；`docs/synova/audit-reports/2026-09-24-K3批次4-PR745-746-747.md:161/166`（K3 定罪 + 冻结格式无守卫）
- 复现源：main push run `36146340288` @ `ef299746`；job `108108419949`（Vitest (2/2) failure）；父提交 `0f709900` run `36129254913` 同用例同 file:line 亦 failure（既存）
### c) 决策
登记**只加不删**：两条 Vitest 行补 `disposition_due` + owner 具名 + 表头 1 行说明第 6 可选键；新卡与证据落 coordination/ 与 product-lines/evidence/。分片名归一化、D721 放行分支不可达（ci.yml）两处**只登记事实、另立卡**，不在本卡做。

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
- 第一性原理：登记表的价值 = **有期限的追责**；无期限登记 = 无限期豁免（正是台账判据「不得豁免计绿」要防的）。
- Anthropic 基线：失败可见 + 到期可判 + **改名即红**（判别性夹具），不靠 grep 型静态判据。
- memory：D954 夹具 D4 已断言「逾期 → exit 1；未到期 rc=0；无键不回溯」；K3 批次 4 §166 记「表头自称冻结 5 字段、却加第 6 字段、无守卫」→ 本卡同时把该键写进表头，消文档/实现矛盾。
- 参考：Anthropic/第一性原理 + 结论=「补键 + 具名 owner + 只加不删 + 登记不改红（命中 ≠ 绿）」。

## Q2: 范围 — 正确的最简方案
做什么（逐条精确路径）：
- scripts/control-tower/ci-red-baseline.txt — 改：`Vitest (2/2)` 加 `disposition_due=2026-10-24` + owner `UNASSIGNED`→`Mac-小队` + 追加 2026-09-26 复现 evidence；`Vitest (1/2)` 加 `disposition_due=2026-10-25`；表头 +1 注释行（第 6 可选键口径）
- docs/synova/coordination/FIX-014-Vitest既有红-处置卡-20260926.md — 新（本卡）
- docs/synova/product-lines/evidence/FIX-014-20260926.md — 新（全部原始输出）
- memory/notes/proposed/2026-09-26-fix014-vitest-preexisting-red-ratchet-disposition.md — 新（决策 Note，D534 门禁）
- .claude/task-briefs/2026-09-26-FIX-014-Vitest-既有红-棘轮登记.md — 本 brief

不做什么（含文件路径）：
- 不改 `.github/workflows/ci.yml`（D721 放行逻辑在 :131-157；单写者 = fix-coder-a）
- 不改 `scripts/control-tower/check-gate-integrity.sh`（判据语义变更须过 K3）
- 不动 `scripts/audit/**`、`docs/synova/audit-reports/**`
- 不动 `docs/synova/coordination/问题修复台-20260925.md`（CTO 单写者）、`task-state/**`
- 不删/不放宽任何既有基线条目

## Q3: 验收 — 入口 → 交互 → 结果
入口：`bash scripts/control-tower/check-gate-integrity.sh --patterns-only --root $PWD --base-ref origin/main --ci-reds <check-runs.json>`（CI 上由 `Gate Integrity` job 调用）
处理：解析 base → 取该 base 的 check-runs failure 集 → 与 `ci-red-baseline.txt` 按键对账（命中/未登记/expires 过期/disposition 逾期）
结果：在期 → `基线命中 N 项；处置逾期 0 项` rc=0；逾期 → `VIOLATION: CI 红处置逾期` rc=1；条目删除 → `VIOLATION: 未登记 CI 失败` rc=1

## 架构层: 基础设施（L0 控制塔门禁面）

## Done 标准
- [ ] verify: `git diff --stat` 仅 1 个治理文件改动（ci-red-baseline.txt，3+/2-），无删除既有条目
- [ ] verify: `bash scripts/control-tower/check-gate-integrity.sh --patterns-only --root $PWD --base-ref origin/main --ci-reds /tmp/fix014-checkruns.json` → rc=0（真实 check-runs JSON）
- [ ] verify: 夹具 (b) 逾期副本 → rc=1 且输出含 `CI 红处置逾期`；夹具 (c) 删条目副本 → rc=1 且输出含 `未登记 CI 失败`
- [ ] verify: `docs/synova/product-lines/evidence/FIX-014-20260926.md` 含 run 36146340288 / job 108108419949 原始输出（含完整断言片段）
- [ ] verify: PR 六项齐全（卡号+分支+sha｜写集｜逐条判据｜改坏即红｜红线段｜未清项）+ `git ls-remote --heads origin | grep fix/fix014-vitest-preexisting-red` 回执
