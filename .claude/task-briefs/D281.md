## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
Synova = AI 诊断 Agent。D281 在 接入与控制层(C)。
D239 后端已实现 GAConstraints 和 isGaContractExpired()，但前端无到期日设置UI。
Enterprise.ts 已有 gaAccessTokens Map，admin.js 已有 GA token 生成功能。
### b) 文件审计
复用: src/routes/enterprise.ts L50-57 (GaAccessRecord + gaAccessTokens)
扩展: app/js/admin.js L92-101 (GA token 生成区)
### c) 决策
GaAccessRecord 加 contractExpiry? 字段 → PUT 端点 → admin.js date input

## Q1: 调研 — 业界最佳实践 / Anthropic 决策链 / memory 历史教训
引用铁律 0-2/7/24+31/33
- GaAccessRecord 当前为内存 Map，contractExpiry 直接加在 record 上
- PUT 端点校验: 仅 admin、未来日期、非冻结 GA
- admin.js: renderGaPanel() 模式复用现有 token 显示逻辑

## Q2: 范围 — 正确的最简方案
做什么：
- enterprise.ts: GaAccessRecord 新增 contractExpiry? 字段
- enterprise.ts: 新增 PUT /api/enterprise/ga-access/:token/expiry 端点
- admin.js: 重构 renderGaPanel() 含 date input + 保存按钮 + api.put 调用
- tests/routes/ga-enterprise.test.ts: 新增 4 个 D281 测试

不做什么：
- 不修改 rbac.ts（D239 已完成）
- 不修改 freeze/unfreeze 端点

## Q3: 验收 — 入口 → 交互 → 结果
入口: admin.html → GA Access 面板 → date input + Save Expiry 按钮
交互: 选择日期 → 点击 Save → api.put → 后端校验 → 返回 success
结果: contractExpiry 保存到 GaAccessRecord → isGaContractExpired() 可读取

## 架构层: L1 交互 (app/js/admin.js) + L5 存储 (enterprise.ts 内存 Map)

## Done 标准: 至少一条可验证的完成标准
[ ] contractExpiry 字段在 GaAccessRecord 中
[ ] PUT /api/enterprise/ga-access/:token/expiry 返回 200 + { ok, contractExpiry }
[ ] 过去日期返回 400
[ ] admin.js 含 ga-expiry-input + btn-save-ga-expiry
[ ] 4 个 D281 测试通过
[ ] tsc 零新增错误
[ ] check-gates-v2 PASS
