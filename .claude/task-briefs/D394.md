# Task Brief: D394 哨兵 findings 事件化（片1）：sentinel_events append-only 表 + 事件流投影 + durationMs bug 修复

> 生成: 2026-08-17 | 分支: feat/devdoc-4-spec-20260816 | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
L1 入口: POST /api/diagnosis/consult / Cron→Sentinel.check() / GET /chat / MCP
五层架构: L1 routes/tui/mcp → L2 agent/orchestrator → L3 sentinel/expert-platform → L4 l4/evidence → L5 store/cron

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [x] 纵向（改 L1-L5 代码/架构）

本任务属于哨兵系统（L3 洞察层 + L5 存储层）。触及 L3 `src/sentinel/runner.ts`（调度框架）+ L5 新增 `src/sentinel/sentinel-events.ts`（SQLite 事件存储）。L2 `src/agent/sentinel-service.ts` 修 durationMs bug。扩展已有模块（runner），新建事件存储抽象。

### b) 文件审计
grep 关键词在 sentinel/ 中：
- `src/sentinel/runner.ts` — `records` 纯内存 Map（L125），findings 重启即丢；`sentinel_tickets` 已落库（L153-166 + L422-429）但 findings/signals 未持久化。复用 runner，新增事件写/重放。
- `src/sentinel/types.ts` — `SentinelFinding`（L41-58）无 status 字段。扩展加 status。
- `src/agent/sentinel-service.ts` — L97 `new Date(run.result.durationMs)` 当时间戳 bug。修复。
- 已有 `src/orchestrator/event-store.ts` — 事件溯源参考实现（append-only 模式），sentinel-events 复用该模式（非 import，独立表）。
- 无冲突：`src/sentinel/sentinel-runner.ts` 是按需运行适配器（不同模块），不动。

### c) 决策
已有 `orchestrator/event-store.ts` 事件溯源模式 → 复用模式（独立 `sentinel_events` 表，不 import）。无文件驱动扩展需求（事件存储是 L5 基建，非哨兵扩展）。冲突 → 无。决策参考系见 Q1c（四决策点已收敛）。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC/Done 标准 — dev doc SYNOVA-IMPL-DSH-D394 已定 DS1-DS12（三条 invariant I1/I2/I3 + durationMs bug）。
② 测试 — 先写 `tests/sentinel/sentinel-events.test.ts`（≥12 用例，red 先证）。
③ 实现 — 契约优先（铁律 47），sentinel-events.ts 先 JSDoc 契约再实现。
④ 接线 — `appendSentinelEvent`/`replaySentinelEvents`/`createSentinelEventsTable` 被 runner.ts 真实调用（grep 命中）。
⑤ 验证 — 自检 5 问 + verify-incremental.sh。

引用依据：铁律 0-2（spec→test→impl→wire）；铁律 7（入口可触达）；铁律 24+31（catch log+degraded）；铁律 47/48（契约优先+测试非空壳）。

### b) 本任务执行约束
- rule: "事件写入口唯一 — appendSentinelEvent 是 sentinel_events 唯一写函数"
  verify: "grep -rn 'INSERT INTO sentinel_events' src/ | grep -v sentinel-events.ts | wc -l（应=0）"
- rule: "新增 export 必须在 runner.ts 有真实调用"
  verify: "grep -n 'appendSentinelEvent\\|replaySentinelEvents\\|createSentinelEventsTable' src/sentinel/runner.ts"
- rule: "durationMs bug 零残留"
  verify: "grep -n 'new Date(run.result.durationMs)' src/agent/sentinel-service.ts（应无输出）"

### c) 决策参考系（K3 审计可核）
四决策点（dev doc §4.5）均已收敛：
- 事件表位置：runner.start() 内建（同 sentinel_tickets）——第一性原理（DSH 哨兵切片领地内）+ 铁律 39（不新增跨层）
- 读路径形态：单源（events 唯一写入口，records 降级为投影）——K3 形似神不似预警 + I2 单源
- 事件粒度：finding 级逐条事件（run_completed 作聚合锚点）——I3 可审计 + 第一性原理
- 重放时机：启动重放建投影 + 运行期边写边投影——Anthropic（事件溯源标准）+ DeepSeek（最少机制）

参考：Anthropic + DeepSeek + 第一性原理。四决策点双参考系指向同一答案，无分歧。

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- src/sentinel/sentinel-events.ts
- src/sentinel/runner.ts
- src/sentinel/types.ts
- src/agent/sentinel-service.ts
- tests/sentinel/sentinel-events.test.ts
- task-state/D394.json

不做什么：
- 不改 src/init/engine-context.ts（表建 runner.start()，归 DSH 哨兵切片，不越界到 Win Claude 领地）
- 不改 src/sentinel/registry.ts（manifest 阈值契约无需改）
- 不改 src/sentinel/signal-aggregator.ts（聚合引擎核心不动，只加 signal 事件写入在 runner 侧）
- 不改 src/sentinel/sentinel-runner.ts（按需运行适配器，非本任务 runner）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：`npm run dev` 启动 → `POST /api/sentinel/run/:id` 手动触发哨兵（或 cron 定时触发）
处理（中间经过哪些步骤）：executeSentinel 执行 → 写 run_completed/finding 事件到 sentinel_events 表（L5 append-only）→ 物化 records 投影（L3 内存态）；kill -9 强杀进程 → 重启 → start() 重放 sentinel_events 重建 records 投影
结果（最终展示在哪）：`GET /api/sentinel/findings` 输出与 kill 前一致；`SELECT COUNT(*)=MAX(seq) AND MIN(seq)=1 FROM sentinel_events` 验证 seq 单调无洞

## 架构层: L3 洞察(哨兵 runner) + L5 存储(SQLite 事件表) + L2 编排(sentinel-service 修复)
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: `grep -n "getSentinelFindings" src/routes/sentinel.ts` 命中 GET /api/sentinel/findings 路由
- [ ] 链路走通: `grep -n "appendSentinelEvent\|replaySentinelEvents\|createSentinelEventsTable" src/sentinel/runner.ts` 命中真实调用（非仅 import）
- [ ] 结果可见: `vitest run tests/sentinel/sentinel-events.test.ts` 全绿（≥12 用例，I1/I2/I3 + durationMs + findings 生命周期）
- [ ] 类型安全: `grep -rn "as any" src/sentinel/sentinel-events.ts src/sentinel/runner.ts` 零结果
- [ ] bug 修复: `grep -n "new Date(run.result.durationMs)" src/agent/sentinel-service.ts` 零残留
- [ ] 全量验证: `bash scripts/workflow/verify-incremental.sh` 全绿（L1 oxlint → L2 tsc → L3 vitest --changed → L4 接线）
