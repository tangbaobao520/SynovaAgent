# Task Brief: D395-a Agent Notes 四态（开发组织版）— memory/notes/{四态} + 四字段头 + Note 引用门禁

> 生成: 2026-08-17 | 分支: feat/d395a-notes-four-state | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
L1 入口: POST /api/diagnosis/consult / Cron→Sentinel.check() / GET /chat / MCP
五层架构: L1 routes/tui/mcp → L2 agent/orchestrator → L3 sentinel/expert-platform → L4 l4/evidence → L5 store/cron

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [x] 基础设施（控制塔/门禁 + 开发组织知识库）

本任务属于开发组织决策沉淀体系（memory/ + commit-msg 门禁）。现有 memory/ 20 个非结构化教训文件平铺，无四态目录，无 Note 引用门禁。本模块改造为 memory/notes/{proposed,implemented,archived,rejected} 四态 + 四字段头（状态/日期/决策/理由）+ commit-msg Note 引用门禁（强化 M7 文档-实现漂移防线）。

### b) 文件审计
- `memory/*.md` — 20 个非结构化教训文件，git mv 归档到 memory/notes/archived/（不改正文）。
- `scripts/commit-msg-check.sh` — 现有 Conventional Commits + D328 一致性检查。追加 D395-a Note 引用门禁。
- `scripts/workflow/generate-task-brief.py` — brief 模板。Q1 追加「### d) 相关 Note 引用」字段。
- `src/orchestrator/` — 门禁触发路径之一（event-bus.ts/context-engine.ts 等，决策密集区）。

### c) 决策
已有 memory/ 教训文件 → 归档 + 新建四态目录（不删不改内容）。门禁落点 = commit-msg hook（查 commit message，非 pre-commit 组 6——K3 §4.2 L219 + spec §4.5 决策 D）。无冲突。决策参考系见 Q1c。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC/Done 标准 — dev doc SYNOVA-IMPL-DSH-D395a 已定 DS1-DS11（四态目录 + 归档 + 四字段头 + 门禁触发/放行/Note 存在/不触发 + 模板字段）。
② 测试 — 先写 tests/control-tower/notes-four-state.test.sh（red 先证）。
③ 实现 — 契约优先（铁律 47），README.md 固化四字段头契约 + git mv 规则。
④ 接线 — commit-msg-check.sh 真实调用 Note 引用门禁（grep 命中）。
⑤ 验证 — 自检 5 问 + verify-incremental.sh。

引用依据：铁律 0-2（spec→test→impl→wire）；铁律 35（自动化优先，物理 grep 门禁不靠自觉）；铁律 47/48（契约优先+测试非空壳）。

### b) 本任务执行约束
- rule: "Note 门禁只覆盖 control-tower/orchestrator（决策密集区），条件跳过 <1s"
  verify: "grep -n 'CT_ORCH_TOUCHED\|scripts/control-tower/\|src/orchestrator/' scripts/commit-msg-check.sh"
- rule: "门禁两层：commit message 含 memory/notes/ 引用 + 引用的 Note 文件真实存在"
  verify: "grep -n 'memory/notes/\|NOTE_PATHS\|NOTE_ROOT' scripts/commit-msg-check.sh"
- rule: "bash 全角标点贴变量须花括号（ctrl-tower-change 模式 2）"
  verify: "grep -nE '\\$[A-Z_]+[（）：」]' scripts/commit-msg-check.sh tests/control-tower/notes-four-state.test.sh（应零结果）"

### c) 决策参考系（K3 审计可核）
四决策点（dev doc §4.5）均已收敛：
- 状态迁移机制：git mv 换目录（目录名即状态）——K3 原文 + 第一性原理（grep 可验）
- Note 载体：独立 memory/notes/ 目录——K3 原文 + 第一性原理（CHANGELOG 是流水，Note 是决策条目）
- 门禁范围：只 control-tower/orchestrator——K3 原文 + Anthropic（最少机制，不扩散噪音）
- 门禁落点：commit-msg hook 查 commit message——K3 原文 + 第一性原理（commit message 在 commit-msg 阶段才确定）

补充决策（本任务新增，spec §3.1 未预见）：修复 commit-msg-check.sh 既有 bug `$CLAIM_RC）`（全角右括号贴变量 → unbound variable，ctrl-tower-change 模式 2）——在 temp repo 测试中触发，阻断我的门禁测试。参考：第一性原理（bug 修复是门禁文件改动的必要部分）。

参考：Anthropic + 第一性原理。决策点双参考系指向同一答案，无分歧。

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- memory/notes/README.md
- memory/notes/proposed/
- memory/notes/implemented/
- memory/notes/archived/
- memory/notes/rejected/
- scripts/workflow/generate-task-brief.py
- scripts/commit-msg-check.sh
- tests/control-tower/notes-four-state.test.sh
- task-state/D395.json

不做什么：
- 不改 src/orchestrator/event-bus.ts（门禁触发路径，本任务不改编排器代码）
- 不改 scripts/control-tower/synova-commit（门禁触发路径，本任务不改控制塔脚本）
- 不改 LOOP-ENGINEERING-CHANGELOG.md（保留为历史决策流水，不迁移）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：commit-msg hook（git commit 时自动触发 scripts/commit-msg-check.sh）
处理（中间经过哪些步骤）：commit message 检查 Conventional Commits 格式 → D328 一致性 → D395-a Note 引用门禁（改 control-tower/orchestrator 时须含 memory/notes/ 引用 + Note 文件存在）
结果（最终展示在哪）：改 control-tower/orchestrator 的 commit 无 Note 引用 → 阻断 exit 1；含 Note → 放行 exit 0；`bash tests/control-tower/notes-four-state.test.sh` 18 用例全绿

## 架构层: 基础设施（开发组织知识库 + commit-msg 门禁）
#CRITERIA: A

## Done 标准
- [ ] 四态目录齐备: `ls memory/notes/` 含 proposed/implemented/archived/rejected + README.md
- [ ] 旧文件归档: `find memory -maxdepth 1 -name '*.md'` 零结果 + `find memory/notes/archived -name '*.md' | wc -l` = 20
- [ ] 四字段头契约: `grep -E '状态|日期|决策|理由' memory/notes/README.md` 命中 + `grep 'git mv' memory/notes/README.md` 命中
- [ ] 门禁触发/放行: `bash tests/control-tower/notes-four-state.test.sh` 18 用例全绿（含触发 exit 1 + 放行 exit 0 + Note 不存在 + 不触发）
- [ ] 门禁落点: `grep -n 'memory/notes/' scripts/commit-msg-check.sh` 命中检查行（非仅注释）
- [ ] 模板字段: `grep -n '相关 Note 引用' scripts/workflow/generate-task-brief.py` 命中
- [ ] 全量验证: `bash scripts/workflow/verify-incremental.sh` 全绿
