# Task Brief: D396 snapshot 黄金用例固化回归门禁（三层快照 + 红-绿演练机器化）

> 生成: 2026-08-17 | 分支: feat/d396-golden-case-snapshot | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
L1 入口: POST /api/diagnosis/consult / Cron→Sentinel.check() / GET /chat / MCP
五层架构: L1 routes/tui/mcp → L2 agent/orchestrator → L3 sentinel/expert-platform → L4 l4/evidence → L5 store/cron

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [x] 基础设施（控制塔/CI 门禁脚本）

本任务属于 CI 门禁体系（scripts/ci/）。现有 `scripts/ci/golden-case-checker.ts`（D51/D300，pre-push 已接线）是**自洽假门禁**——`deriveActual()` 从 fixture 的 `input.sentinelFindings` 反推"实际"，从不调用真实 compute/哨兵/专家代码，改坏任一 compute 阈值门禁仍绿。本模块扩展该 checker（不改 F1 判定），加"真跑 compute/哨兵/专家报告 + diff 冻结快照"三层。

### b) 文件审计
- `scripts/ci/golden-case-checker.ts` — 现有 F1 门禁（computeF1Score/deriveActual/runAllChecks）。扩展 GoldenCase 类型 + runAllChecks 追加三层快照，不改 computeF1Score/deriveActual。
- `scripts/ci/golden-snapshot-runner.ts` — 新建快照执行器（computeFnRegistry + runComputeSnapshot + runFindingsSnapshot + runExpertReportAssertion），纯函数可单测。
- `extensions/sentinels/financing-constraint/computes/cash-runway.ts` — `computeCashRunway(financials)` 纯函数（D356 修复对象，阈值 `<6→critical`）。registry 登记的真实函数，import 不复制。
- `tests/fixtures/golden-cases/` — 现有 10 条（golden-case-01~10，均无 compute 段）。新建 golden-case-11 示范 compute 快照。
- `tests/ci/golden-case-checker.test.ts` — 现有 F1 单测。扩展快照层测试。
- `tests/ci/golden-case-gate.test.ts` — 现有 D300 接线测试，硬编码 `files.length = 10`。新增 golden-case-11 后须同步为 11（必要副作用）。

### c) 决策
已有 golden-case-checker 门禁 → 扩展（不做新体系，K3 明令）。新建独立 runner 执行器（隔离可单测，spec §4.5 决策 B）。无冲突。决策参考系见 Q1c。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC/Done 标准 — dev doc SYNOVA-IMPL-DSH-D396 已定 DS1-DS10（三层快照 + 红-绿演练 + 只扩用例不改判定）。
② 测试 — 先写快照层测试（red 先证：golden-snapshot-runner 不存在 → 失败）。
③ 实现 — 契约优先（铁律 47），golden-snapshot-runner.ts 先 JSDoc 契约再实现。
④ 接线 — runComputeSnapshot 被 golden-case-checker.ts runAllChecks 真实调用（grep 命中）。
⑤ 验证 — 自检 5 问 + verify-incremental.sh + 物理红-绿演练。

引用依据：铁律 0-2（spec→test→impl→wire）；铁律 35（机器可验不靠 review）；铁律 36（vitest 全量通过）；铁律 47/48（契约优先+测试非空壳）；铁律 38（as any=0）。

### b) 本任务执行约束
- rule: "不改 F1 判定逻辑 computeF1Score/deriveActual"
  verify: "git diff scripts/ci/golden-case-checker.ts | grep -E '^[-+]' | grep -E 'computeF1Score|deriveActual' | grep -v '^[-+].*(import|//|\\*)'（应仅 import 相关行）"
- rule: "computeFnRegistry 登记真实函数（import 自 extensions，非复制重写）"
  verify: "grep -n 'computeCashRunway' scripts/ci/golden-snapshot-runner.ts"
- rule: "as any = 0"
  verify: "grep -rn 'as any' scripts/ci/golden-snapshot-runner.ts scripts/ci/golden-case-checker.ts（应零结果）"

### c) 决策参考系（K3 审计可核）
四决策点（dev doc §4.5）均已收敛：
- 快照层实现位置：独立 golden-snapshot-runner.ts + checker 调用——Anthropic（隔离：执行器纯函数可单测）+ DeepSeek（最少机制：checker 只编排不改判定）
- 快照对象：直接 import compute 纯函数 + fixture 数据——第一性原理（D355-D360 修的是纯函数）+ 铁律 12（compute 纯函数不涉路由）
- 红-绿演练形态：测试文件内置机器复现 + 物理演练跑一次——铁律 35（机器可验）+ K3 验收锚点"红-绿演练必须跑一次"
- 专家报告断言：结构化断言（字段存在性+值域）——K3 原文"结构化断言，不对 LLM 全文 diff"

补充决策（本任务新增，spec §3.1 未预见）：golden-case-gate.test.ts 硬编码 `files.length=10`，新增 golden-case-11 必然打破 → 同步改为 11 并纳入写集。参考：第一性原理（声明=实际，D286"声明 16 实际 17"漂移教训）+ 铁律 36（vitest 全量通过）。

参考：Anthropic + DeepSeek + 第一性原理。决策点双参考系指向同一答案，无分歧。

## Q2: 范围 — 正确的最简方案是什么？

做什么：
- scripts/ci/golden-case-checker.ts
- scripts/ci/golden-snapshot-runner.ts
- tests/fixtures/golden-cases/golden-case-11-cash-runway-threshold.json
- tests/ci/golden-case-checker.test.ts
- tests/ci/golden-case-gate.test.ts
- task-state/D396.json

不做什么：
- 不改 scripts/pre-commit-check.sh（pre-push 接线已存在，无需改）
- 不改 .github/workflows/ci.yml（CI job 已存在，无需改）
- 不改 src/ 任何业务逻辑（compute 阈值/契约/filter 归 D355-D360 实现线 Win Claude）
- 不改 extensions/sentinels/financing-constraint/computes/cash-runway.ts（只 import，不复制不改）
- 不改 scripts/pre-push-check.sh（生产接线已存在 L221）

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：`npx tsx scripts/ci/golden-case-checker.ts`（pre-push 已接线）
处理（中间经过哪些步骤）：扫描 fixtures → 每 fixture 跑 F1 门禁（原逻辑不变）+ compute 快照全 diff（runComputeSnapshot）+ findings 快照全 diff（runFindingsSnapshot）+ 专家报告结构断言（runExpertReportAssertion）
结果（最终展示在哪）：改坏 compute 阈值 → exit 1（红）+ diff 点名 signal；修复 → exit 0（绿）；`npx tsx scripts/ci/golden-case-checker.ts` 输出含 `golden-case-11-cash-runway-threshold` 结果行

## 架构层: 基础设施（CI 门禁脚本）
#CRITERIA: A

## Done 标准
- [ ] 三层快照齐备: `grep -n "runComputeSnapshot\|runFindingsSnapshot\|runExpertReportAssertion" scripts/ci/golden-snapshot-runner.ts scripts/ci/golden-case-checker.ts` 命中定义+调用
- [ ] 只扩用例不改判定: `grep -n "computeF1Score\|deriveActual" scripts/ci/golden-case-checker.ts` 逻辑不变（回归测试证明输出与现状一致）
- [ ] 红-绿演练: 改坏 cash-runway.ts 阈值 → `npx tsx scripts/ci/golden-case-checker.ts` exit 1；恢复 → exit 0（物理证据）
- [ ] 向后兼容: 旧 10 条 fixture 只跑 F1，`npx tsx scripts/ci/golden-case-checker.ts` 全绿
- [ ] 测试全绿: `vitest run tests/ci/golden-case-checker.test.ts tests/ci/golden-case-gate.test.ts` 全过（≥10 新用例 + 回归）
- [ ] 类型安全: `grep -rn "as any" scripts/ci/golden-snapshot-runner.ts scripts/ci/golden-case-checker.ts` 零结果
- [ ] 全量验证: `bash scripts/workflow/verify-incremental.sh` 全绿（L1 oxlint → L2 tsc → L3 vitest --changed → L4 接线）
