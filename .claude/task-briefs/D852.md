# Task Brief: D852 §6-5 四项产品决定落地（线 8/4/3/25 各新增验收点 + live/restart 清单成文）

> 生成: 2026-09-20 | 工作树: `.synova-wt-d852` | 分支: `team/d852-product-decisions` | 基线: `origin/main dae40d96`
> 角色: D852 小队「编码 B」 | 结论口径: 队内自验，**不称审计**；结论标「可提请独立审计」

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务**不改产品代码**，属**文档/合约层**（产品完成度账目层）：
- 纵向解耦：不触及 L1–L5 任何一层代码（红线：不写产品代码）。
- 横向解耦：不新增包、不动 `packages/`。
- 扩展解耦：**本任务本身就是在补「验收点账目」** —— 把创始人已裁的四项产品决定写成 `product-lines.yaml` 里可证伪的验收点，供后续编码 session 实现与 K3 复核。

落地对象：`docs/synova/product-lines/product-lines.yaml`（28 条线的验收点账目）+ `docs/synova/product-lines/preconditions.yaml`（前提清单）+ `docs/synova/coordination/`（成文清单与变更单）。**只加验收点，不新增线**（28 保持不变）。

### b) 文件审计（实测行号，非凭记忆）
| 目标 | 实测 | 结论 |
|---|---|---|
| `product-lines.yaml` `lines:` 段 | :57 | 与任务卡一致 |
| 线 3 报告体系 | :158（8 点 3-1…3-8） | 一致 |
| 线 4 数据接入 | :210（8 点 4-1…4-8） | 一致 |
| 线 8 告警推送 | :403（6 点 8-1…8-6） | 一致 |
| 线 25 插件化扩展体系 | :1101（7 点 25-1…25-7） | 一致 |
| 冻结件附录 A 断言列格式 | 线3表 :169 / 线4表 :183 / 线8表 :241 | 一致（`| ID | 断言（判定式） | verify | 证据 | fail_when |`） |
| 冻结件 §五 签字 sha256 前 8 位 | :86 = `c11841e6`（口径：**不含签字块**） | 一致 |
| yaml 现有 acceptance_points 字段 | `id`/`desc`/`evidence`/`status`/`note`（`k3_only` 可选） | 新增点**只用现有字段**，不扩 schema |
| 分母现状 | yaml `points_total` = **174**（实测解析）；`FROZEN_V1_TOTAL` = 128（`scripts/project/gen-project-board.py:105`）；backlog = 174 − 128 = **46** | 与任务卡「现 46」一致 |

**一处与任务卡不符，已点名**：任务卡称「创始人 2026-09-20 已签的 §6-5」，但 `docs/synova/coordination/创始人裁定表-§6五项-20260920.md:28` 标题实为「**待签** §6-5｜四项卡在产品决定」且四项 ☐ **均未勾选**（§6-1/6-2/6-3 方为「已签」）。四项决定内容与表内「建议」选项**逐字一致**，故按任务卡（canonical 规格）落地，并把「签署状态未回写」列为**遗留项升级**，变更单亦按「待创始人批量确认」起草（与冻结件 §六 路径一致）。

### c) 决策
已有覆盖 → 复用（`acceptance_points` 现有字段与 id 编号序列 `N-9` 直接续号，不新建 schema、不新增线）。
无覆盖 → 新增 6 个验收点（8-7/8-8/4-9/3-9/25-8/25-9）。
冲突 → 无（四项决定均为「补强已有线」，与总纲 §2.1「11 项 DSH 缺口归线不新开」一致）。

## Q1: 调研 — 决策链 + 执行约束

### a) 权威件（引用必给全名 + 版本）
- `docs/synova/coordination/创始人裁定表-§6五项-20260920.md` §6-5（四项产品决定原文）
- `docs/authority/产品完成度定义与推进总纲-20260918.md` §1.2 / §2.2 / §5 / §6
- `docs/synova/project/26线-V1验收标准-v0.2-20260917.md` §〇（M1–M5）/ §三 / §五 / §六 + 附录 A
- `docs/authority/研究院材料权威级别与引用纪律-20260920.md`
- 仓库外：`/Users/wane/山河研究院/99-综合/交接/可复制-DSH对比与任务书-20260918.md` §二（9 项撤回，逐条对照）

### b) DSH 借鉴核查（施工图四色清单 → 借鉴边界 → DSH 源码 file:line）
DSH 实测版本 `@deepseek-ai/*` **0.1.6-alpha.2**（安装包 `node_modules/@deepseek-ai/`）：
- **spill**：`dsh-spill-policy/lib/index.js:102-103` `function apply(ctx, config) { const maxInlineBytes = config.maxInlineBytes; }`；`:104-105` 非法值 `throw`；`:106` `undefined → return`（no-op）；`:107-150` 无 owner / 无 backend / saveText 失败 / 替换超 cap → 一律 `ctx.logger.warn` + 保留 inline。schema `lib/types/index.js:53` `maxInlineBytes: z.number()`。**单位 = UTF-8 字节**（`:146` `Buffer.byteLength(..., "utf8")`）。后端 `dsh-spill`（`ctx.spillStore` 抽象缝）/ `dsh-spill-local`（fs 落盘，`saveText` :556 → 路径 locator :564）。
- **webhook**：`dsh-webhook/lib/index.js:245` `dispatch(delivery)`、`:90` `createWebhookSession(...)`、`:180-186` `snapshotDelivery` 校验 `receivedAt`/`deliveryId` → **纯入站**（provider 推给我们），**无出站投递 API**。佐证裁定表「DSH 的 webhook 是入站——不能照抄」。
- **live/restart**：`dsh-settings/lib/types/index.d.ts:21` `export type SettingsApplies = 'live' | 'restart';`；`dsh-settings/lib/index.js:288` `applies: options?.applies ?? "live"`（默认 live，声明后「surfaced to configuration UIs」）。→ 决定 ④ 是 **DSH 原生机制**，M1 强制「接入/配置」口径。
- **时间窗**：`dsh-time-context` 存在（`lib/index.js:161-162` `config.timeZone` / `config.refreshIntervalMs`），提供时点/时区语义。

### c) 决策参考系
**参考：DSH 源码实测 + 第一性原理 + 总纲 §5 禁「grep 当完成判据」**。结论：四项决定按 M1 分别归为「接入/配置 DSH」（②③④ 与 ①入站缝）与「自建」（①出站，DSH 无对应能力）；每条验收点的判定式一律写成**行为/不变量断言**，**禁止以 grep 命中作为通过依据**（撤回清单 #3 + 总纲 §5）。冲突/多选项问题（如 8,000 与 32,000 之间档位语义、DSH 单阈值 vs 我方双阈值、字节 vs 字符单位）一律登记为遗留项，**不替创始人做产品决定**。

### d) 相关 Note 引用
`memory/notes/proposed/2026-09-20-D852-product-decisions-6-points.md`（本任务决策沉淀，铁律 49）。

## Q2: 范围 — 正确的最简方案是什么？

做什么（写集，8 文件）：
- docs/synova/product-lines/product-lines.yaml
- docs/synova/product-lines/preconditions.yaml
- docs/synova/coordination/D852-live-restart-清单-20260920.md
- docs/synova/coordination/D852-V1-轻量变更单-20260920.md
- docs/synova/coordination/D852-派单回执-20260920.md
- memory/notes/proposed/2026-09-20-D852-product-decisions-6-points.md
- task-state/D852.json
- .claude/task-briefs/D852.md

不做什么：
- 不改 docs/synova/project/26线-V1验收标准-v0.2-20260917.md（V1 冻结件，sha256 前 8 位 c11841e6；改动只走 §六 轻量变更单）
- 不改 docs/synova/coordination/创始人裁定表-§6五项-20260920.md（创始人件，签署状态归属创始人）
- 不改 scripts/project/gen-project-board.py（A 写集；分母常量归 A/CTO）
- 不改 scripts/product-lines/productline_yaml.py（A 写集）
- 不改 .github/workflows/ci.yml（红线）
- 不改 scripts/control-tower/synova-commit（红线）
- 不写产品代码（src/ 零改动）；不新增线、不改任何既有点的 id/条文/status

## Q3: 验收 — 入口 → 交互 → 结果

入口（用户从哪触发）：
创始人/CTO 打开 `docs/synova/product-lines/product-lines.yaml` 与 `docs/synova/coordination/D852-*` 三份成文件；编码 session 读验收点 → 实现；K3 按附录 A 五列复核。

处理（中间经过哪些步骤）：
① 复核基线行号 → ② 落 6 个新增验收点到 yaml（只用现有字段，续号 N-9）→ ③ live/restart 清单成文（四类归属 + 判定边界 + 可复现核验方式）→ ④ V1 轻量变更单起草（逐条来源 + 分母 128+6=134 + backlog 46→52 中间态说明）→ ⑤ preconditions.yaml 登记或明确「不涉及，理由」→ ⑥ 派单 §〇-c 回执 + 9 项撤回清单逐条对照 → ⑦ 自验 + 提交 + PR。

结果（最终展示在哪）：
- `product-lines.yaml`：线 3/4/8/25 各多出可证伪验收点，总点数 174 → 180，**线数仍 28**
- `docs/synova/coordination/D852-live-restart-清单-20260920.md`：live/restart 四类归属成文
- `docs/synova/coordination/D852-V1-轻量变更单-20260920.md`：交创始人批量确认的变更单
- `docs/synova/coordination/D852-派单回执-20260920.md`：§〇-c 回执 + 撤回清单自检

## 架构层: 基础设施
L1/L2/L3/L4/L5
#CRITERIA: A

## Done 标准
- [ ] 入口可触达: `python3 -c "import sys;sys.path.insert(0,'scripts/product-lines');import productline_yaml as p;d=p.load_file('docs/synova/product-lines/product-lines.yaml');print(len(d['lines']))"` 输出 `28`（不增线）
- [ ] 链路走通: yaml 新增点共 6 条，续号 8-7/8-8/4-9/3-9/25-8/25-9，`git diff` 对该文件**零删除行**
- [ ] 结果可见: `ls docs/synova/coordination/D852-*.md` 三份成文件均存在且非空
- [ ] 门禁: `bash scripts/pre-commit-check.sh` 自过；`bash scripts/control-tower/check-pr-budget.sh` ≤12 文件单域
- [ ] 冻结件未动: `git diff --name-only origin/main...HEAD | grep -c '26线-V1验收标准'` 输出 `0`
