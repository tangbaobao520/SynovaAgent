# Task Brief: L1/L2增强:增量边界检查+层级确认+数据流对账+Auditor默认启用

> 生成时间: 2026-06-14 03:26:38
> 分支: feat/phase0-diagnosis-demo
> 代码库状态: tsc=2
0 errors, as any=6, 测试=}
}



测试运行失败

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 完整数据流：
  原始数据 → 本体层(电子病历) → 7维度×25测量器(compute)
    ├─ 按需(FDE触发): runModules() → Evidence池 → 专家 → 诊断报告
    └─ 定时(Cron触发): Sentinel.check() → SentinelFinding → 信号聚合
                                                              ↓
  信号聚合引擎 → 交叉关联+严重度升级+专家路由 → 6位专家(strategy/org/finance/tech/marketing/action)
       → ReAct推理+交叉验证 → 综合诊断报告 → FDE收到警报+报告

## 本任务在哪一层

L1 PreToolUse/L2 PostToolUse hook 增强。触及 L1(scripts/) + L2(scripts/workflow/)。无跨层风险——hook 脚本属于工具层，不参与业务架构分层。

## 文档引用

- 外部专家 Loop Engineering 升级文档 §4.1-4.4
- docs/workflow/ANTHROPIC-WORKFLOW.md §节点④⑤
- 全量对齐手册 §7.3（测量器与哨兵）、§9（五层依赖规则）

## 接口审计

- scripts/workflow/hook-block-write.sh: 新增层级确认逻辑
- scripts/workflow/check-boundaries-incremental.sh: 新建，增量架构边界检查
- scripts/workflow/check-dataflow-alignment.sh: 新建，数据流关键词对账
- scripts/pre-push-check.sh: Auditor 默认启用

## 数据流

Write 请求 → hook-block-write.sh(层级确认) → PostToolUse → verify-incremental.sh → check-boundaries-incremental.sh(增量边界) → pre-commit → check-dataflow-alignment.sh(数据流对账) → pre-push → ArchitectureAuditor(默认启用)

## 用户旅程

AI 写代码时自动触发——每写一个文件，hook 自动验证文件所属层级是否匹配 task brief、改动是否跨层引用、数据流关键词是否出现在代码中。FDE/开发者不需要手动操作，所有检查自动完成。失败则阻断并提示具体违规。

## Done 标准

- [ ] 入口可触达:
- [ ] 链路走通:
- [ ] 结果可见:

## 验证命令
```bash
bash scripts/workflow/checkpoint-impl.sh <新函数名>
```
