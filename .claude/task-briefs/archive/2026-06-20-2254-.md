# Task Brief: 工作区上下文隔离+已确认判断持久化——ConversationEngine workspaceId绑定+context-compressor summary策略+workspace-context-bridge

> 生成: 2026-06-20 22:54:15 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 8 位专家: strategy / org / finance / tech / marketing / action / business_model / knowledge
- 完整数据流:
  原始数据 → 本体层(电子病历) → 7维度×25测量器 → 证据池 → 专家ReAct推理 → 诊断报告

## Q1: 调研 — 这件事以前怎么做的？

### a) 业界最佳实践
- Claude API: system prompt 顶部放硬约束(constitutional principles)
- RAG: 检索增强生成——注入事实约束到prompt顶部
- context-compressor.ts: 已有 sliding-window + summary 策略(可复用)

### b) 顶级团队怎么做
- Anthropic: 先写测试(压缩不丢事实)→改压缩器→接入Engine→桥接服务
- 不改架构——只给压缩器加"不可丢弃"参数

### c) 我们犯过的错
- memory/file-first-paradigm.md: 铁律0-2 测试先行+接线验收——之前跳过测试
- 教训: 这次先写测试，再写实现

## Q2: 范围 — MVP

4文件: context-compressor(+confirmedFacts) + conversation-engine(workspaceId+加载事实) + workspace-context-bridge(L2桥接) + workspaces-api(传workspaceId)
不做: 完整跨工作区对话迁移、UI切换动画

## Q3: 验收

入口: workspace API 发消息→传workspaceId→ConversationEngine加载对话历史+事实→压缩时不丢已确认判断
结果: 35轮对话后FDE第5轮的纠正仍在上下文中。切换工作区=完全不同的上下文

## 本任务在哪一层
L2编排层(context-compressor+conversation-engine+workspace-context-bridge) + L4本体(AgentMemoryStore查找) + L1路由(workspaces-api传参)

## 文档引用
- C:\Users\Administrator\AppData\Local\Temp\上下文管理优化任务.md
- src/orchestrator/context-compressor.ts: compress(messages, systemPrompt, config)
- src/l4/agent-memory-store.ts: recall(query)
- src/agent/conversation-engine.ts: processMessage(text)

## 接口审计
- src/orchestrator/context-compressor.ts: compress(messages, systemPrompt, config) → CompressionResult
- src/l4/agent-memory-store.ts: recall(query) → MemoryEntry[]
- src/agent/conversation-engine.ts: processMessage(text) → Promise

## 数据流
workspaces-api 消息 → conversation-engine.processMessage(id, text) → WorkspaceContextBridge.loadContext(id) → AgentMemoryStore.recall() → 注入上下文 → context-compressor.compress(confirmedFacts) → Agent回复

## Done 标准
- [x] 测试先行: context-compressor +confirmedFacts 单元测试
- [x] 测试先行: workspace-context-bridge 加载事实测试
- [x] 实现: context-compressor summary策略合并已确认判断
- [x] 实现: conversation-engine 加载+注入+压缩时传递
- [x] 实现: workspace-context-bridge 跨工作区查询
- [x] 验证: tsc零错误 + 全量测试通过
