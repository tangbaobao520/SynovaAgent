# Task Brief: Batch3: §7行动项CRUD+§9.3叙事生成器+§11.4确认反馈接收器

> 生成: 2026-06-21 01:52:44 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 8 位专家: strategy / org / finance / tech / marketing / action / business_model / knowledge
- 完整数据流:
  原始数据 → 本体层(电子病历) → 7维度×25测量器 → 证据池 → 专家ReAct推理 → 诊断报告

## Q1: 调研 — 这件事以前怎么做的？

### a) 业界最佳实践
§7 行动项: RESTful CRUD + 状态流转。§9.3 叙事: LLM将结构化信号→自然语言。§11.4 反馈: 用户确认/驳回→写入Evolution Engine

### b) 顶级团队怎么做
Anthropic: 先定义schema→实现存储→路由→接Agent。每层独立测试

### c) 我们犯过的错
模式B: PRD定义了但文件不存在。铁律5: 后端能力≠用户可用。这次先写测试再写实现

## Q2: 范围
3个新文件: actions-api.ts + narrative-generator.ts + feedback-collector.ts。测试先行。不做: 复杂查询/分页

## Q3: 验收
POST /api/actions → 创建行动项→GET列表→PUT更新状态。叙事生成器: 信号JSON→自然语言文本。反馈: 确认→写入store

## 本任务在哪一层
L1路由(actions-api) + L3洞察(narrative-generator) + L4本体(feedback-collector→AgentMemoryStore)

## 文档引用
- PRD §7 §9.3 §11.4
- src/l4/agent-memory-store.ts: MemoryEntry类型

## 接口审计
src/routes/actions-api.ts: POST /api/actions → ActionItem
src/routes/actions-api.ts: GET /api/actions → ActionItem[]
src/routes/actions-api.ts: PUT /api/actions/:id/status → ActionItem
src/pipeline/narrative-generator.ts: generateNarrative(signal) → string
src/evolution/feedback-collector.ts: collectFeedback(action, decision) → void

## 数据流
§7: POST /api/actions → in-memory store → GET列表 → PUT状态流转 → 完成→流入执行跟踪
§9.3: SentinelFinding[] → narrative-generator.generateNarrative() → 人类可读判断文本
§11.4: 用户确认/驳回 → feedback-collector.collectFeedback() → AgentMemoryStore.remember()

## Done 标准 (PRD §7 §9.3 §11.4)
- [x] §7 actions-api.ts: CRUD+状态流转 (confirmed→executing→completed)
- [x] §9.3 narrative-generator.ts: 信号→自然语言叙事
- [x] §11.4 feedback-collector.ts: 确认/驳回→AgentMemoryStore
- [x] tsc零错误 + 6 new tests passed
