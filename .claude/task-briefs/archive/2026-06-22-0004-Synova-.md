# Task Brief: Synova 独立诊断引擎接口定义完成 — Step 1/4

> 生成: 2026-06-22 00:04:25 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读）

- SynovaAgent = 组织数字孪生诊断 + 持续增长导航系统。
  诊断是手段，目的是增长。
  核心问题：这家企业的增长卡在哪里？现在该做什么？
- Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。
- 五层架构：L1(交互)→L2(编排)→L3(洞察)→L4(本体)→L5(存储)，只能向下依赖相邻层。
- 8 位专家: strategy / org / finance / tech / marketing / action / business_model / knowledge

## Q1: 调研 — 这件事以前怎么做的？

### a) 业界最佳实践
接口优先设计：先定义 TypeScript discriminated union 契约，编译器验证 exhaustiveness check。工厂模式注入 LLM/Tools 依赖。

### b) 顶级团队怎么做
Anthropic 四步法：1)定义独立接口(零旧系统依赖) → 2)测试驱动实现 → 3)feature flag 原子切换 → 4)删除旧系统。全过程不建桥接文件。

### c) 我们犯过的错
[[engine-core-split-fraud]] — 一个月桥接文件糊弄，零实质进展。根因：选阻力最小的路径。本次预防：零 engine-core import，接口完全独立。

## Q2: 范围 — 最简方案是什么？

仅做接口定义 + 契约测试。不写实现代码。
- SynovaDiagnosisEngine 接口 (runConsultation + DiagnosisEngineFactory)
- 17 discriminated union DiagnosisEvent 类型
- InitiatorProfile / DiagnosisScope / DiagnosisReport / ConsultationResult
- LLMClient / ToolExecutor 接口
- 15 契约测试验证接口结构完整性

明确不做：引擎实现、调用方修改、删除 engine-core。这是 Step 1/4。

## Q3: 验收 — 做完后用户能看到什么？

入口: `import { SynovaDiagnosisEngine } from 'src/l3/synova-diagnosis-engine'`
处理: tsc 编译时 discriminated union exhaustiveness check
结果: 零 engine-core 依赖的类型系统，15 测试通过

## 本任务在哪一层
L3 洞察层 — 接口契约定义。纯类型，无跨层风险。

## 文档引用
- CLAUDE.md §七 铁律 46/47: 桥接文件欺诈阻断
- PRD v1.6 §5/§9: Slice 工程流程
- CODEX-ENGINE-CORE-SPLIT.md: engine-core 11-Slice 计划
- memory/engine-core-split-fraud.md: 拆分欺诈事故

## 接口审计

| 文件:函数签名 | 返回类型 |
|--------|---------|
| `synova-diagnosis-engine.ts:SynovaDiagnosisEngine.runConsultation(teamId, initiator, scope?, onEvent?)` | `Promise<ConsultationResult>` |
| `synova-diagnosis-engine.ts:DiagnosisEngineFactory(llm, tools, options?)` | `SynovaDiagnosisEngine` |
| `synova-diagnosis-engine.ts:DiagnosisEvent` 17 discriminated union | 事件类型契约 |

## 新建文件声明
- `src/l3/synova-diagnosis-engine.ts` (NEW, ~300行) — 接口定义
- `tests/l3/synova-diagnosis-engine.test.ts` (NEW, 15 assertions) — 契约测试

## 数据流（后续实现阶段）
```
L1 routes/diagnosis.ts → L2 diagnosis-launcher.ts
  → SynovaDiagnosisEngine.runConsultation(teamId, initiator, scope, onEvent)
    → onEvent(DiagnosisEvent) → SSE push
    → return ConsultationResult → report display
```

## Done 标准 (PRD §5/§9)
- [x] 入口可触达: `import { SynovaDiagnosisEngine }` 路径正确，tsc 零错误
- [x] 链路走通: discriminated union 17 类型 exhaustiveness check 编译通过
- [x] 结果可见: 15 契约测试通过，零 engine-core 依赖
- [x] 接线就绪: 接口 + 工厂模式供 Step 2 实现引用
- [x] grep 验证: `grep -r "engine-core" src/l3/synova-diagnosis-engine.ts` = 零结果
