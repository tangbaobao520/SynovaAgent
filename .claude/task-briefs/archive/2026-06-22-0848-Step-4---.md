# Task Brief: Step 4: 默认切换新引擎 + 旧引擎回退标记

> 生成: 2026-06-22 08:48:50 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读）

- SynovaAgent = AI 诊断 Agent。五层架构 L1→L5。8 专家。

## Q1: 调研

### a) 业界最佳实践
Feature flag 默认开启新路径，旧路径 opt-in 回退。验证期后删除旧分支。

### b) 顶级团队怎么做
Anthropic Step 4/4: 默认新引擎。SYNOVA_USE_OLD_ENGINE=true 回退旧引擎。旧 adapter 保留但默认不走。

### c) 我们犯过的错
[[engine-core-split-fraud]] — 本次预防：feature flag 原子切换，任何时刻可回退。

## Q2: 范围

翻转 feature flag 默认值：新引擎默认 → 旧引擎 opt-in。
只改 diagnosis.ts 一行条件判断。不删任何代码。

## Q3: 验收

入口: DEV_MODE=true npm run dev (无 SYNOVA_USE_NEW_ENGINE)
处理: POST /api/diagnosis/consult → 新引擎六阶段
结果: 6 phase + 3 hypothesis + 2 root_cause + report_ready

## 本任务在哪一层
L1 (routes/diagnosis.ts) — 条件翻转。

## 文档引用
- PRD v1.6 §5/§9 Step 4/4
- memory/engine-core-split-fraud.md

## 接口审计

| 文件:函数签名 | 返回类型 |
|--------|---------|
| `diagnosis.ts:createSynovaDiagnosisEngine(llm, tools, opts)` | `SynovaDiagnosisEngine` (默认) |
| `diagnosis.ts:new EngineCoreVendorAdapter(provider, registry)` | `DiagnosisEngine` (SYNOVA_USE_OLD_ENGINE=true) |

## 数据流
```
默认: → createSynovaDiagnosisEngine → 六阶段 → 报告
回退: SYNOVA_USE_OLD_ENGINE=true → EngineCoreVendorAdapter → engine-core
```

## Done 标准 (PRD §5/§9 Step 4/4)
- [x] 入口可触达: 不设环境变量启动 → POST /api/diagnosis/consult → 新引擎
- [x] 链路走通: 6 phase_started + 6 phase_completed + 3 hypothesis + 2 root_cause + report_ready
- [x] 结果可见: 默认新引擎零配置可用
- [x] 回退安全: SYNOVA_USE_OLD_ENGINE=true → 旧引擎
