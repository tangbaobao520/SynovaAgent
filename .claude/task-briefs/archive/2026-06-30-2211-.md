# Task Brief: B1 — Fix 5 sentinel test call-signature bugs

> 生成: 2026-06-30 22:11 | 更新: 22:16 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图
本任务属于三层解耦的哪一层？
- [ ] 纵向（改 L1-L5 代码/架构）
- [ ] 横向（迁移到独立包 / 新建包）
- [x] 扩展（文件驱动，不改 TypeScript）— 修复测试调用签名

本任务属于哪个系统？触及哪层？
- **系统**: 哨兵（Sentinel）— 46 个哨兵的 compute 函数测试
- **层**: L3 洞察层（修改 tests/sentinels/）
- **现有模块**: `extensions/sentinels/` 下 46 个哨兵
- **关系**: 修复 pre-existing 测试 bug（创建时就错了）

### b) 文件审计
grep "compute\w+(0," tests/sentinels/ — 找到 5 个测试文件传位置参数但 compute 期望对象参数
关系: 修复已有测试，不新建

### c) 决策
不改 compute 函数，只修测试调用签名。无新文件。

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链
① SPEC — 本 brief
② 测试修复 — 修正每个测试调用签名
③ 实现验证 — vitest 单文件通过
④ 自检 6 问

引用依据：
- 铁律 0-2: spec → test → impl → wire → review → merge
- 铁律 33: 测试命名约定

### b) 本任务执行约束
- rule: "修完测试后必须单文件 vitest 验证，`--maxWorkers=1` 避免崩溃"
  verify: "npx vitest run tests/sentinels/XXX.test.ts --reporter=verbose --maxWorkers=1"
- rule: "只改测试文件，不改 compute 函数"
  verify: "git diff --stat | grep -v tests/sentinels/ | wc -l"

## Q2: 范围 — 正确的最简方案是什么？

**做什么**：修复 5 个哨兵测试文件，把 `computeX(a, b)` 改为 `computeX({ param1: a, param2: b, ... })`：
1. `tests/sentinels/agent-deployment-maturity.test.ts` — 需要 6 字段对象
2. `tests/sentinels/ai-ecosystem-fit.test.ts` — 需要 5 字段对象
3. `tests/sentinels/ai-investment-return.test.ts` — 需要 5 字段对象（先读确认签名）
4. `tests/sentinels/api-coverage.test.ts` — 需要对象参数（先读确认签名）
5. `tests/sentinels/growth-quality.test.ts` — computeOrganicGrowthPct 期望数组但测试未传够数据（先确认）

**不做什么**：
1. 不改 compute 函数逻辑
2. 不改非此 5 文件
3. 不新增 engine-core 引用

## Q3: 验收 — 入口 → 处理 → 结果

入口: `npx vitest run tests/sentinels/<file> --maxWorkers=1`
处理: 测试传入正确参数 → 调用 compute 函数 → 断言
结果: vitest PASS，全部通过

## 本任务在哪一层
L3（测试层）

## Done 标准
- [ ] 5 个测试文件逐一 `vitest run --maxWorkers=1` PASS
- [ ] tsc --noEmit 零错误
- [ ] compute 函数未被修改（仅改测试）
