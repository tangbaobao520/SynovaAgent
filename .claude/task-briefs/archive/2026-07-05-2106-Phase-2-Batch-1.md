# Task Brief: Phase 2 Batch 1 — 4 financial aggregates migration (capital-efficiency, capital-structure, capital-turnover, financing-constraint)

> 生成: 2026-07-05 21:06 | 分支: session/04 | V4.4.0

## Q0

- [x] 纵向 — 改 4 个 `extensions/sentinels/*/aggregate.ts`

**系统**: 哨兵 — L4 extensions。4 个财务指标 aggregate，均为 Pattern 1（单一 FINANCIAL 池快照）。
**本任务**: 批量迁移 4 个 aggregate，全部使用同一模式。

**文件审计**:
- `extensions/sentinels/capital-efficiency/aggregate.ts` — 改
- `extensions/sentinels/capital-structure/aggregate.ts` — 改
- `extensions/sentinels/capital-turnover/aggregate.ts` — 改
- `extensions/sentinels/financing-constraint/aggregate.ts` — 改
- 各 aggregate 的 compute 函数目录 — 不改（纯函数）
- `src/sentinel/sentinel-loader.ts` — 不改（Phase 0 已完成）

**决策**: 无覆盖 → 新建迁移。复用 Phase 0 GraphTraversal 注入机制。

## Q1

**决策链**: 
① 4 个 aggregate 代码分析 → 全部是 queryNodes('FINANCIAL') 单池模式
② 每个加 try-traversal-fallback-queryNodes 模式
③ 字段映射保持一致：全用 MONEY/FINANCIAL_OUTCOME 字段
④ tsc + vitest 全通过

**约束**:
- rule: "traversal 返回空时必须降级到 queryNodes"
  verify: "for f in capital-efficiency capital-structure capital-turnover financing-constraint; do grep -c queryNodes extensions/sentinels/\$f/aggregate.ts | xargs test 1 -le || exit 1; done && echo PASS"
- rule: "每个 aggregate 有 traversal? 参数"
  verify: "for f in capital-efficiency capital-structure capital-turnover financing-constraint; do grep -c traversal? extensions/sentinels/\$f/aggregate.ts | xargs test 1 -eq || exit 1; done && echo PASS"
- rule: "不使用 nodes.length（V4.4.0）"
  verify: "for f in capital-efficiency capital-structure capital-turnover financing-constraint; do grep 'nodes.length' extensions/sentinels/\$f/aggregate.ts && exit 1; done && echo PASS"

## Q2

**做什么**: 4 个 aggregate.ts 加 traversal? 第 3 参 + 图遍历路径 + fallback

**不做**:
- 不改任何 compute 函数
- 不改 `src/sentinel/sentinel-loader.ts`
- 不改 `src/sentinel/types.ts`
- 不改 `src/l4/graph-traversal.ts`
- 不改 4 个 aggregate 的 manifest.json

## Q3

**入口**: Cron → Runner → sentinel-loader → check(store, teamId, traversal?)
**处理**: traversal traverse → MONEY/FINANCIAL_OUTCOME → 2-3 compute → finding
**结果**: SentinelFinding[]（4 个 aggregate 各自产出）

## 架构层: L4

## Done 标准
- [x] verify: npx tsc --noEmit 2>&1 | grep -c error | xargs -I{} bash -c 'test {} -eq 0 && echo PASS || echo FAIL'
- [x] verify: npx vitest run 2>&1 | grep -q "passed" && echo "PASS"
- [x] verify: for f in capital-efficiency capital-structure capital-turnover financing-constraint; do echo "=== \$f ===" && grep -c traversal? extensions/sentinels/\$f/aggregate.ts || echo "0"; done
- [x] verify: grep "as any" extensions/sentinels/capital-efficiency/aggregate.ts extensions/sentinels/capital-structure/aggregate.ts extensions/sentinels/capital-turnover/aggregate.ts extensions/sentinels/financing-constraint/aggregate.ts && echo FAIL || echo PASS
