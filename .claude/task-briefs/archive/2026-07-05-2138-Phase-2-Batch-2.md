# Task Brief: Phase 2 Batch 2 —4 financial aggregates (growth-quality, revenue-health, cost-health, profit-health)

> 生成: 2026-07-05 21:38 | 分支: session/04 | V4.4.0

## Q0 — 纵向，改 4 个 aggregate

**文件审计**:
- `extensions/sentinels/growth-quality/aggregate.ts` — 1 queryNodes + 2 compute
- `extensions/sentinels/revenue-health/aggregate.ts` — 2 queryNodes (Financial+Client) + 0 compute
- `extensions/sentinels/cost-health/aggregate.ts` — 1 queryNodes + 0 compute
- `extensions/sentinels/profit-health/aggregate.ts` — 1 queryNodes + 0 compute
- 以上 4 个 aggregate 的 compute 目录 — 不改
- `src/sentinel/sentinel-loader.ts` — 不改

## Q1 — 决策链: 每个加 traversal fallback 模式

**约束**:
- rule: "traversal 返回空时必须降级到 queryNodes"
- rule: "不使用 nodes.length"
- rule: "catch 必须有 log.warn/error"

## Q2 — 做什么/不做什么同 Batch 1

## Q3 — 入口→处理→结果同前

## 架构层: L4

## Done 标准
- [x] verify: npx tsc --noEmit 2>&1 | grep -c error | xargs -I{} bash -c 'test {} -eq 0 && echo PASS || echo FAIL'
- [x] verify: for f in growth-quality revenue-health cost-health profit-health; do grep -c traversal? extensions/sentinels/\$f/aggregate.ts | xargs test 1 -eq || echo "FAIL: \$f"; done && echo PASS
