# Task Brief: P0/P1 紧急修复 — 铁律违规修复 + 通知系统打通 + 对标补全合并 + 零调用方接线 + 前端功能补全

> 生成: 2026-07-06 02:23:43 | 分支: feat/prompt-architecture | as any: 0

## 项目身份（每次重读 — 源自 CLAUDE.md §项目身份）

SynovaAgent 是一个驻扎企业的 AI 诊断系统。
诊断是手段，增长才是目的。
核心问题：这家企业的增长卡在哪里？现在该做什么？
Agent，不是 ChatBot。驻扎企业，持续观测，主动发现，自动诊断，给出行动建议，跟踪执行。

目标: 成为组织诊断的 AWS。每个新客户、新行业、新数据源 → 加文件即可，不改代码。
能文件化的必须文件化。不能文件化的必须有明确的扩展点。

### 三层解耦体系

**纵向解耦：五层物理隔离**
代码按 L1-L5 架构分层，每层只与相邻层通信。L1 交互层不知道 L4 用什么数据库，L3 洞察层不知道 L5 数据存在哪。换底层存储，上层零改动。pre-commit 物理阻断跨层 import——L2→L4 的代码提交不进去。

**横向解耦：11 个独立 Monorepo 包**
五层内部拆为独立包：@synova/sog-core（本体图类型）、@synova/sentinel-engine（哨兵调度）、@synova/expert-platform（专家加载）、@synova/connector-registry（数据连接器）。每个包接口边界明确，拆卸一个不影响其余 19 个。核心包已落地运行；已存在的功能规划从 src/ 迁移到独立包；未来新增须遵循此结构。

**扩展解耦：文件驱动，不改代码**
新增能力靠文件，不靠改代码：
- 新 AI 专家 = 新建目录 + 10 个 Markdown 文件 → 自动注册到 ExpertDispatcher
- 新诊断哨兵 = 加 xxx-sentinel.ts → builtins 自动扫描加载
- 新行业 = 加行业目录（基准数据+阈值+案例库）→ 1-2 天上线，零 TypeScript 改动
- 新本体实体类型 = 加 JSON Schema 文件

流程约束: V4.4.0 — task brief 6 字段强制 + 免疫系统 + plan.json + 8 组物理阻断 + Plan-Actual 闭合 + engine-core 清零 + 时间戳顺序检查。

数据流: L5 存储 → L4 本体 → L3 洞察(哨兵定时+诊断按需) → L2 编排 → L1 交互
        反馈闭环: GA评审/客户反馈 → 记忆层 → 数据层
        Sentinel Finding[] → 诊断引擎 Phase 2 → 8 位文件驱动专家解读

L1 入口: POST /api/diagnosis/consult (GA诊断) / Cron→Sentinel.check() (哨兵) / GET /chat (Web) / MCP
五层架构 (只能向下依赖相邻层):
  L1 交互: routes/ tui/ mcp/
  L2 编排: agent/ orchestrator/
  L3 洞察: l3/ sentinel/ expert-platform/ expert/ (8位文件驱动专家: strategy org finance tech marketing action business_model knowledge)
  L4 本体: l4/ evidence/ 企业事实层: AgentMemoryStore (enterprise_fact, 版本化+superseded_by链)
  L5 存储: store/ cron/
三层粒度: 专家→哨兵→计算。哨兵=可独立告警的最小子领域。compute=纯数学函数。
L0 进化: evolution/ 两路反馈→候选池→确认/执行验证→写入知识库
文件化扩展: expert/ knowledge/shared/ theory/ skills/ — 新增=加文件,不改代码
数据安全: L0公开摘要→L1聚合信号→L2脱敏证据→L3原始数据(仅客户内Agent可见,GA不可见)
引擎: packages/engine-core/ (Novis遗产,逐步迁移)。禁止src/新增engine-core引用(铁律46)。
安全: security/ (PIIScrubber, DataBoundary)
LLM: providers/ (DeepSeek, OpenAI, Gateway)

---

## Q0: 定位 — 项目拼图 + 文件审计

### a) 项目拼图

本任务属于三层解耦的哪一层？
- [x] **纵向**（改 L1-L5 代码/架构）
- [ ] 横向（迁移到独立包 / 新建包）
- [ ] 扩展（文件驱动，不改 TypeScript）

**所属系统**: 基础设施修复 + L1 前端补全 + 通知系统 + 代码合并

**触及层及模块**:
| 层 | 涉及模块 | 操作 |
|----|---------|------|
| L1 交互 | `electron-renderer/src/components/` (LeftPanel, CenterPanel, RightPanel, MessageItem, NotificationCenter, Composer) | 修改 |
| L1 交互 | `electron-renderer/src/hooks/` (useNotifications, useConversation, useWorkspaces) | 新建 |
| L1 交互 | `electron-renderer/src/types/chat.ts` | 修改 |
| L1 交互 | `src/routes/notifications.ts` | 新建 |
| L1 交互 | `src/l1/im-channel.ts` | 修改 |
| L2 编排 | `src/agent/synova-agent.ts` | 接线 |
| L2 编排 | `src/server.ts` | 接线（rate-limit, config-recovery, memory-monitor） |
| L2 编排 | `src/init/engine-context.ts` | 接线（schema-migration） |
| L4 本体 | `src/l4/migration-validator.ts` | 标记 |
| L5 存储 | `src/store/schema-migration.ts` | 已存在，只需接线 |

**上下层依赖**:
- L1 修改不依赖 L2-L5
- L2 接线依赖 L5 存储（数据库初始化后调用）
- Phase 3 对标代码合并涉及 L2 (orchestrator/context-engine)、L3 (tool-guard)、L4 (escalation-engine)

### b) 文件审计

```bash
# 已有文件驱动模块（grep 结果）
electron-renderer/src/components/LeftPanel.tsx              — 已存在，修改
electron-renderer/src/components/CenterPanel.tsx             — 已存在，修改
electron-renderer/src/components/RightPanel.tsx              — 已存在，修改
electron-renderer/src/components/MessageItem.tsx             — 已存在，修改
electron-renderer/src/components/NotificationCenter.tsx       — 已存在，修改
electron-renderer/src/components/Composer.tsx                — 已存在，修改
electron-renderer/src/types/chat.ts                          — 已存在，修改
src/l1/im-channel.ts                                         — 已存在，修改
src/server.ts                                                — 已存在，接线
src/init/engine-context.ts                                   — 已存在，接线
src/middleware/rate-limit.ts                                  — 已存在（零调用方），接线
src/services/config-recovery.ts                              — 已存在（零调用方），接线
src/services/memory-monitor.ts                               — 已存在（零调用方），接线
src/infra/command-lanes.ts                                   — 已存在（零调用方），接线
src/store/schema-migration.ts                                — 已存在（零调用方），接线
src/l4/migration-validator.ts                                — 已存在（零调用方），标记
```

**关系**: 全部为已有模块的"修改"或"接线"操作。无新建文件驱动模块，不涉及 expert/ sentinel/ extensions/ knowledge/ 目录。

### c) 决策
全部为修复已有代码的违规 + 将已完成但未接线的功能接通。不新建硬编码替代。不新增文件驱动模块。

---

## Q1: 调研 — 决策链 + 执行约束

### a) Anthropic 决策链

本任务按以下顺序执行，每一步完成后才能进入下一步：
  ① **SPEC 对齐** — 本 brief + docs/plans/codex/implementation/SYNOVA-IMPL-紧急修复-P0P1-v1-20260706.md 为唯一执行依据
  ② **测试先行** — Phase 1 的纯修复可先改后测（无行为变更）。Phase 2/3/4/5 必须先写测试
  ③ **实现** — 刚好满足 Done 标准：
     - 5 Phase 全部完成
     - 测试全部通过
     - 接线完整（新 export 有引用）
     - 错误路径有 log + degraded
     - tsc + vitest 零失败
  ④ **接线** — 每个模块的入口可触达 + 链路完整 + 结果可见
  ⑤ **验证** — 自检 6 问 + verify 命令全量执行

**引用依据**：
  - **铁律 0-2**: spec → test → impl → wire → review → merge
  - **铁律 7**: 入口可触达 + 完整链路走通 + 结果可见（每个 Phase 的对话中确认 Done）
  - **铁律 11+24+31**: 空 catch 必须有 log + degraded。这是本任务 Phase 1 的核心
  - **铁律 38**: as any 零容忍。Phase 1.2 和 1.4 的核心
  - **铁律 46**: 禁止桥接代理文件。Phase 3 合并对标代码时不建桥接
  - **Memory: 2026-06-04-audit-p0-p1-fix-session**: 上次修复 5 处空 catch + 清 as any + 接线。结论：接线后必须 grep 确认引用方
  - **Memory: engine-core-split-fraud**: 桥接文件伪装成迁移。Phase 3 合并后 grep 确认无 import engine-core

### b) 本任务执行约束（写入 plan.json principles）

| # | Rule | Verify 命令 |
|---|------|-------------|
| 1 | 空 catch 必须有 log.warn/error + degraded | `grep -rn "\.catch(() => {})" electron-renderer/ src/ --include="*.ts" --include="*.tsx" \| grep -v node_modules \| wc -l` — 预期 0 |
| 2 | as any 零容忍 | `grep -rn "\bas any\b" electron-renderer/ src/ --include="*.ts" --include="*.tsx" \| grep -v "\.test\." \| grep -v node_modules \| wc -l` — 预期 0 |
| 3 | catch(err: any) 不允许 | `grep -rn "catch (err: any)" src/ --include="*.ts" \| grep -v node_modules \| wc -l` — 预期 0 |
| 4 | 零调用方模块接线后必须 grep 确认引用 | `grep -rn "createFixedWindowLimiter\|restoreConfig\|CommandLane\|migrateSchema\|memoryMonitor\.check" src/ --include="*.ts" \| grep -v "\.test\." \| head -10` |
| 5 | Phase 3 合并后无 engine-core 新引用 | `grep -rn "packages/engine-core" src/ --include="*.ts" \| grep -v node_modules \| head -10` — 只允许白名单 |

---

## Q2: 范围 — 正确的最简方案是什么？

### 做什么

按 `docs/plans/codex/implementation/SYNOVA-IMPL-紧急修复-P0P1-v1-20260706.md` 执行：

**Phase 1: 铁律违规修复（6 文件）**
- 1.1 LeftPanel.tsx: 2 处 `.catch(() => {})` → log.warn + 错误提示
- 1.2 CenterPanel.tsx: 1 处 `as any` → ChatMessage 加 `_id?` 字段后删除 as any
- 1.3 RightPanel.tsx: 1 处 `} catch {` → log.warn + setDegraded
- 1.4 im-channel.ts: 2 处 `catch (err: any)` → `catch (err: unknown)`
- 1.5 MessageItem.tsx: 1 处 `} catch {` → log.warn

**Phase 2: 通知系统打通（3 文件）**
- 2.1 新建 `src/routes/notifications.ts`（GET/POST 4 端点）
- 2.2 新建 `electron-renderer/src/hooks/useNotifications.ts`（30s 轮询）
- 2.3 修改 NotificationCenter.tsx（onClick 导航到工作区）

**Phase 3: 对标补全代码合并（1 操作）**
- 从 `D:\novis-backup-20260526\synova-session-02` 合并 12 个文件（context-engine, escalation-engine, tool-guard + 测试 + 配置）
- 不合并 V4.4.0 Loop Engineering 变更

**Phase 4: 零调用方接线（6 文件）**
- 4.1 rate-limit.ts → server.ts 注册中间件
- 4.2 config-recovery.ts → server.ts 启动流程调用
- 4.3 command-lanes.ts → synova-agent.ts 包装高风险工具
- 4.4 schema-migration.ts → engine-context.ts 初始化后调用
- 4.5 memory-monitor.ts → server.ts setInterval 轮询
- 4.6 migration-validator.ts → 标记为一次性工具，不接线

**Phase 5: 前端功能补全（4 文件）**
- 5.1 useConversation hook（sendMessage/messages/isStreaming 封装）
- 5.2 useWorkspaces hook（workspaces CRUD 封装）
- 5.3 Markdown 渲染（react-markdown + MessageItem 集成）
- 5.4 IME 组合态（Composer 添加 isComposing + composition 事件）

### 不做什么

- 不重构哨兵/计算函数/本体层/边类型（docs/plans/codex/ 已明确定义边界）
- 不创建新的专家或哨兵（expert/ sentinel/ 目录不变）
- 不迁移 engine-core 代码（铁律 46: 白名单外的 src/ 文件不准 import packages/engine-core/）
- 不改 TUI V2（铁律 40-45: patches/ink+5.2.1.patch + useStreaming 冻结）
- 不改 packages/engine-core/ 目录
- 不改 src/l3/ 或 src/sentinel/ 目录
- 不新建文件驱动模块（extensions/ manifest.json 不变）
- 不合并 synova-session-02 的 V4.4.0 Loop Engineering 变更（scripts/*.sh memory/*.md 不合并）
- migration-validator.ts 不接入生产路径（已标记为一次性工具）
- Phase 5 不作样式优化（electron-renderer/src/*.css 不变）

---

## Q3: 验收 — 入口 → 交互 → 结果

| Phase | 入口 | 处理 | 结果 |
|-------|------|------|------|
| Phase 1 | 触发异常路径（网络断连/类型错误） | catch 块记录 log + 设置错误状态 | 前端显示错误提示（非永久 loading），无静默失败 |
| Phase 2.1 | `curl /api/notifications` | 读取 AgentMemoryStore 中 type=sentinel_finding 的记录 | 200 + JSON 数组。`/unread-count` 返回 `{count: N}` |
| Phase 2.2 | 前端加载 NotificationCenter | useNotifications 30s 轮询 /api/notifications | 实时未读数显示在铃铛上，替换 MOCK_NOTIFICATIONS |
| Phase 2.3 | 点击通知项 | markAsRead + setActiveWorkspaceId(n.workspaceId) | 跳转到对应工作区 |
| Phase 3 | 主仓库文件系统 | 从 synova-session-02 复制 + git merge | context-engine/escalation-engine/tool-guard 在 src/ 下存在且可引用 |
| Phase 4.1 | 服务端启动 | rate-limit 中间件注册到 Express | 限流生效（100次/60s 窗口） |
| Phase 4.2 | 服务端启动 | 数据库初始化后 restoreConfig() 被调用 | 配置恢复执行，失败降级默认配置 |
| Phase 4.3 | 工具执行路径 | CommandLane 包装高风险工具 | 执行前 lane 检查通过 |
| Phase 4.4 | 服务端初始化 | engine-context 初始化后 migrateSchema(db) | 数据库 schema 自动迁移 |
| Phase 4.5 | 服务端启动后 | setInterval → memoryMonitor.check() 每 60s | 内存监控日志 |
| Phase 5.1 | CenterPanel + Composer | useConversation 封装 sendMessage/ messages/isStreaming | 消息发送/接收正常 |
| Phase 5.2 | LeftPanel + CommandPalette | useWorkspaces 封装 workspaces CRUD | 工作区列表加载正常 |
| Phase 5.3 | Agent 消息渲染 | `<ReactMarkdown>` 渲染 content | `**bold**` 显示为粗体 |
| Phase 5.4 | Composer 输入中文 | onCompositionStart/End 控制 isComposing | Enter 不在输入法组合态时发送消息 |

---

## 本任务在哪一层

**L1（交互层）** — 主要修改:
- `electron-renderer/src/components/` — LeftPanel, CenterPanel, RightPanel, MessageItem, NotificationCenter, Composer
- `electron-renderer/src/hooks/` — useNotifications, useConversation, useWorkspaces（新建）
- `electron-renderer/src/types/chat.ts` — ChatMessage 类型扩展
- `src/routes/notifications.ts` — 新建 API 路由
- `src/l1/im-channel.ts` — catch 类型修复

**L2（编排层）** — 接线:
- `src/server.ts` — rate-limit 中间件 + config-recovery 启动流程 + memory-monitor 轮询
- `src/agent/synova-agent.ts` — command-lanes 接线
- `src/init/engine-context.ts` — schema-migration 接线

**L4（本体层）** — 标记:
- `src/l4/migration-validator.ts` — 添加一次性工具注释

**L5（存储层）** — 接线:
- `src/store/schema-migration.ts` — 数据库初始化后调用

---

## Done 标准

```
[ ] Phase 1: grep "\.catch(() => {})" electron-renderer/ src/ = 0
[ ] Phase 1: grep "as any" electron-renderer/ = 0
[ ] Phase 1: grep "catch (err: any)" src/ --include="*.ts" = 0
[ ] Phase 2: curl /api/notifications → 200 JSON
[ ] Phase 2: curl /api/notifications/unread-count → 200 {count: N}
[ ] Phase 2: NotificationCenter onClick → setActiveWorkspaceId 被调用
[ ] Phase 3: context-engine/escalation-engine/tool-guard + 测试在主仓库存在
[ ] Phase 3: grep "packages/engine-core" src/ 零新引用（白名单除外）
[ ] Phase 4: grep -rn "createFixedWindowLimiter" src/server.ts → 有引用
[ ] Phase 4: grep -rn "restoreConfig" src/server.ts → 有引用
[ ] Phase 4: grep -rn "CommandLane" src/agent/synova-agent.ts → 有引用
[ ] Phase 4: grep -rn "migrateSchema" src/init/engine-context.ts → 有引用
[ ] Phase 4: grep -rn "memoryMonitor" src/server.ts → 有引用
[ ] Phase 5: Agent 消息渲染 Markdown（**bold** → 粗体）
[ ] Phase 5: Composer IME 组合态不误发送
[ ] npx tsc --noEmit 零错误
[ ] npx vitest run 全部通过
```
