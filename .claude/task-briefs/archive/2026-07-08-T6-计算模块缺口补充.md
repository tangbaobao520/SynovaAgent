# T6：计算模块15缺口补充

## Q0:
a) 项目拼图: L3 洞察层 — shared/computes/ 新增15个compute函数。基于审计发现753条ND覆盖度中15个函数零代码。按P0(2)→P1(8)→P2(5)分批。
b) 文件审计: grep 发现 extensions/sentinels/shared/ 有 baseline.ts/stats.ts/threshold.ts 但无 computes/ 目录。15个函数名在全仓库零结果（GAP）。
c) 决策: 无覆盖 — 新建 shared/computes/{l1-production,l2-value,l3-causal,l4-competition}/ 走文件驱动。

## Q1:
a) 业界最佳实践: 遵循现有 compute 纯函数模式(如 wacc.ts)。b) 使用代码真实16条边名(PRODUCES/DEPLOYS/FUNDS等)。c) memory/ 教训: 避免使用 JTBD 边名(FLOWS_TO/AFFECTS等)。

## Q2:
新增15个compute函数 + 15个测试文件 + shared/computes/index.ts 导出。不改现有82个compute函数。不涉及 L1/L2/L4/L5 层。不涉及 sentinel manifest 修改。

## Q3:
入口: 函数文件在 extensions/sentinels/shared/computes/{layer}/。测试: tests/sentinels/shared-computes/。结果: tsc零错误 + vitest 全量零新增失败。

## Q4 契约与测试:
- 每个函数有JSDoc契约ID（铁律47）
- 每个函数>=3个测试（正常/降级/边界），有expect()断言（铁律48）
- 使用代码真实边名，禁止JTBD边名

## 本任务在哪一层
L3 洞察层 (shared/computes/)

## Done 标准
- [ ] verify: grep "契约ID:" extensions/sentinels/shared/computes/ | wc -l >= 15
- [ ] verify: npx vitest run tests/sentinels/shared-computes/ 2>&1 | tail -5 | grep -q "passed"
- [ ] verify: npx tsc --noEmit 2>&1 | head -5 | grep -q "EXIT=0" && echo "OK"
- [ ] verify: grep -r "FLOWS_TO\|AFFECTS\|COMPETES_WITH\|BUYS_FROM\|CONSUMES" extensions/sentinels/shared/computes/ | wc -l = 0
