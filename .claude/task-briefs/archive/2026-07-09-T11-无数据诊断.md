---
task_id: "T11"
title: "无数据诊断能力（零数据冷启动）"
date: "2026-07-09"
version: "V4.4.4"
---

## Q0:

a) 项目拼图：
Synova = AI 诊断 Agent。五层架构 L1→L5。8 专家 7 维度。
本任务涉及：
- L2 编排层: src/interview/ — 新增 4 个新文件（signals.ts, question-bank.ts, signal-extractor.ts, gpi-estimator.ts）
- L3 洞察层: src/l3/expert-dispatcher.ts（修改——增加无数据访谈信号路径）
- L2 编排层: src/agent/report-assembler.ts（修改——增加预诊断模式）
- L1 交互层: src/routes/diagnosis-upload-v2.ts（修改——增加POST /interview 端点）
- 测试层: tests/interview/ — 4 个测试文件 + 1 个端到端

上层相关：L1 交互层通过新的 /interview 端点接收上传
下层相关：复用已有的 interview/engine.ts 的 detectContradictions() 和 aggregateResults()，不依赖 L4/L5

本任务是新增（完整的新子系统——无数据诊断管道）。

b) 文件审计：
- src/interview/ 现有 engine.ts（138行）和 roles.ts（57行）— 复用矛盾检测和角色权重
- src/orchestrator/phase0-engine.ts — Phase0State 提供 industry/painPoints/hypotheses 作为信号输入
- src/evidence/types.ts — Evidence 接口（类型安全转化目标）
- src/l3/expert-dispatcher.ts — 591行，dispatch 入口处增加 interviewSignals 分支
- src/agent/report-assembler.ts — 110行，增加 preliminary 模式参数
- docs/synova/research/产品战略研究-2026-07-07/深度研究-无数据诊断能力-20260707.md — 研究权威源

c) 决策：无冲突，可执行。新增无数据诊断子系统，不修改已有管线正常路径。

## Q1:

a) 业界最佳实践: Phase A MVP 设计——确定性规则优先于 LLM。GPI 代理估算标注不确定性区间。
b) 项目教训: memory/stub-implementation-pattern.md — 不写 stub；memory/plan-actual-mismatch.md — 声明完成需物理证明
c) 权威源确认: 研究文档 Phase A 清单 + 实施文档 Step 0-7 分步方案

## Q2:

做：
- 创建 src/interview/signals.ts（CausalSignal + ExtractedSignals 类型定义）
- 创建 src/interview/question-bank.ts（150道种子题：4通用锚题 + 7角色×5维度×~3题 + ~40行业拓展题）
- 创建 src/interview/signal-extractor.ts（R1跨角色矛盾检测/R2痛觉点映射/R3必须性得分）
- 创建 src/interview/gpi-estimator.ts（α/β/γ代理指标估算 + confidence:preliminary）
- 修改 src/l3/expert-dispatcher.ts（evidence为空时走 interviewSignals→Evidence 转换分支）
- 修改 src/agent/report-assembler.ts（增加 mode:'preliminary' 参数，报告打预诊断标记）
- 修改 src/routes/diagnosis-upload-v2.ts（增加 POST /api/diagnosis/upload/interview 端点）
- 创建 tests/interview/question-bank.test.ts（≥4 测试）
- 创建 tests/interview/signal-extractor.test.ts（≥5 测试）
- 创建 tests/interview/gpi-estimator.test.ts（≥3 测试）
- 创建 tests/interview/e2e-preliminary-diagnosis.test.ts（端到端）

不做：
- 不创建 question-generator.ts（Phase B）
- 不创建 LLM-S1/S2/S3 语义提取（Phase C）
- 不创建对比诊断报告（Phase D）
- 不修改已有管线的正常路径——只在入口处加分支
- 不修改 interview/engine.ts 和 roles.ts 的现有逻辑
- 不使用 node_modules 新增依赖

## Q3:

入口：POST /api/diagnosis/upload/interview 接收访谈文档（单个或多个）+ 角色标注
交互：系统按角色分离文档 → R1/R2/R3 提取信号 → 矛盾检测 → 信号聚合 → GPI 估算 → 专家推理
结果：返回预诊断报告（含 dataSource:'interview' 标注 + GPI 估算 + 关键判断 + 盲区）

## Q4: 约束

约束1：无修改已有管线正常路径——expert-dispatcher.ts/report-assembler.ts 只有 + 行, 无 - 行
约束2：signal-extractor.ts 的 R1/R2/R3 必须是用 if/else 的确定性逻辑，不依赖 LLM 自由发挥
约束3：GPI 估算输出含 confidence:'preliminary' + dataSource:'interview' + 报告中标注"预诊断"
约束4：question-bank 数据结构含 tags 数组，getQuestions() 通过 tags 过滤非 if-else
约束5：零 as any
约束6：每个新增模块 ≥3 个测试（正常路径+边界+降级）

## 架构层级:
L2（interview/ 新文件）→ L3（expert-dispatcher.ts 修改）→ L1（routes/ 端点和 report-assembler.ts 修改）

## Done 标准:
1. 4 个新文件存在（signals.ts / question-bank.ts / signal-extractor.ts / gpi-estimator.ts）
2. 3 个修改文件存在（expert-dispatcher.ts / report-assembler.ts / diagnosis-upload-v2.ts）
3. signal-extractor 实现 R1/R2/R3 三个确定性规则，每个有单元测试
4. GPI 估算输出含 confidence:'preliminary' + dataSource:'interview'
5. expert-dispatcher 在 evidence 为空时可通过 interviewSignals 转为 Evidence
6. 4 个测试文件 ≥3 个 it + expect
7. vitest --changed 零新增失败 | tsc 零新增错误 | 零 as any
