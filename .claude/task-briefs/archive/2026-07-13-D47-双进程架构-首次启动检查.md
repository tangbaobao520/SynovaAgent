## Q0: 定位 — 项目拼图 + 文件审计
### a) 项目拼图
SynovaAgent — 部署运维P0。D47：首次启动检查清单 + 数据目录三平台注册。双进程架构（Electron主进程 + Express子进程）。
现有 electron-main.ts 已含 spawn/Tray/Notification/autoUpdater/IPC (~400行)，但不含启动前检查和平台数据目录注册。
### b) 文件审计
| 审计项 | grep 结果 | 行动 |
|--------|-----------|------|
| electron-main.ts | 存在(~400行) — app.whenReady() → startServer → waitForServer → createTray+createWindow | 插入startupCheck()前置于startServer前 |
| src/deploy/ | 不存在 | 新建目录 + 2文件 |
| src/deploy/startup-check.ts | 不存在 | 新建 — 5项检查 |
| src/deploy/data-directory.ts | 不存在 | 新建 — 三平台路径 |
| process.platform 引用 | 全局零引用(仅app.getPath) | 直接使用 |
| dialog.showErrorBox | 已有(L227 L392) | 复用 |
### c) 决策
2新文件 + electron-main.ts 插入一处调用。不改 spawn/Tray/Notification/autoUpdater 核心逻辑。

## Q1: 调研 — 现有接口 / memory教训
a) electron-main.ts L359: app.whenReady() → startServer() → waitForServer() → setupIPC + createWindow + createTray
b) 插入点：startServer() 前执行 runStartupChecks()
c) 约束1：首次启动检查不阻断(除SQLite外) — 返回 {passed, failed, warnings[]}
d) 约束2：平台标准路径 — Windows=AppData/Local, macOS=Application Support, Linux=.local/share
e) 铁律24: catch 必须有 log + degraded 标记
f) 铁律31: 降级信号传播 — 每项检查独立 catch，不相互阻断

## Q2: 范围 — 正确的最简方案
**做什么:**
1. src/deploy/data-directory.ts — getDataDirectory(按平台) + registerDataDirectory(创建+写入)
2. src/deploy/startup-check.ts — runStartupChecks() 5项(SQLite/LLM/磁盘/基线/权限)
3. electron-main.ts — app.whenReady() 首行调用 runStartupChecks()

**不做什么:**
- 不创建新 electron/main.ts（已有 electron-main.ts）
- 不修改 spawn/Tray/Notification/autoUpdater 核心逻辑
- 不实现升级机制(D48)
- 不改 src/server.ts

**排除:** electron-main.ts 除插入点外零修改，src/server.ts 不改

## Q3: 验收 — 入口→处理→结果
入口: electron-main.ts → app.whenReady() → runStartupChecks()
处理: SQLite integrity_check → LLM连接检查 → 磁盘空间>500MB → 哨兵工位基线 → 文件权限
结果: SQLite失败→弹窗退出。其余失败→warnings[]→继续启动→窗口显示"系统就绪"

## 架构层: L4
部署基础设施 — electron-main.ts + src/deploy/

## Done 标准
- [ ] data-directory.ts: 三平台路径 getDataDirectory + registerDataDirectory
- [ ] startup-check.ts: 5项检查 + 返回 {passed, failed, warnings[]}
- [ ] electron-main.ts: app.whenReady() 中 startServer() 前调用 runStartupChecks()
- [ ] 约束1: SQLite外不阻断启动
- [ ] 约束2: 平台标准路径
- [ ] 约束3: git diff electron-main.ts 仅含 startupCheck 调用块
- [ ] >=7测试覆盖（三平台路径 + 各项检查 + 降级）
- [ ] tsc零新增错误 / vitest零新增失败 / 零as any
