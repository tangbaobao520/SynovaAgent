# 编码指令 — {{切片/任务名}}（{{D#}}）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: {{派单路径}}（CTO 派单）
> 审计: Kimi K3 会盯着你的任务，{{切片/任务}}完成后做最终审计（{{审计覆盖范围}}）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| {{D#}} spec | {{spec 路径 1}} | {{任务名 1}}——**编码唯一契约** |
| {{D#}} spec | {{spec 路径 2}} | {{任务名 2}}——**编码唯一契约** |
| 派单 | {{派单路径}} | 切片定义/写集约束/审计口径 |
| 北星 | .claude/PRODUCT-BRIEF.md §二/§六 + 施工图 {{施工图路径}} §3.1 | 产品方向锚点 |
| 前车之鉴 | {{K3 审计报告路径}} | 教训清单（M7 漂移/指纹落盘/物理实测等） |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §4（Current State，实测现状）/ §5（写集）/ §7（测试）/ §10（DS 验收）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 + §12 骨架 → 列出你的改动清单（文件级）→ 确认依赖前置（见 §三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`=0，铁律 38）、契约优先（新函数先 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded，铁律 24/31）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，red→green）。

## 三、{{切片/任务}}专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 当前工作区 = {{基线分支}}（{{基线描述}}），**{{上游切片/任务}}未合入 main**。
   - 编码前置（铁律 0-3）：`git fetch --all && git pull --ff-only`，确认 {{上游切片/任务分支}} 已合 main 后再动工。
   - 合入后 **重新核验 spec 引用的行号/契约**（{{会被上游改动的文件清单}}均被上游改过，行号必漂移）——照 spec 旧行号写测试会红（D524 教训：文档-实现漂移被 K3 C2 抓）。
   - {{D#}} 依赖 {{上游任务}}（{{依赖内容}}）。**前置未满足 → 进入 waiting，不伪造实测**。
2. **写集精确性（{{前车之鉴教训}}）**：只改 spec §5.1 写集表列出的文件（{{D#}} {{N}} 条 / {{D#}} {{M}} 条）；`git diff --name-only` 与实际改动**完全一致**；禁止改写集外文件、禁止"树终验声称不符"重演。
3. **诚实 RED**：{{依赖的外部能力，如 LLM consult}}——{{外部能力}}不可用 / 前置未合入时，相应 DS 如实标注 ⏸/❌ + 理由（README + evidence 双处），**禁止伪造全链路绿、禁止契约断言冒充全链路**。
4. **evidence 落盘规范（K3 独立复核 + 指纹落盘）**：{{计时记录/数据断言/产物指纹/时间戳}}必须落盘（{{evidence 目录}}），K3 独立重跑可复现；禁止只在 task-state 存单一副本。
5. **红线（违反 = 事故）**：不碰 `src/`（{{原因}}——本任务经分析零 src/ 需求）；不碰 `scripts/audit/`（K3 专属）；Stage 3 前零 DSH 依赖（不 `npm install @deepseek-ai/dsh`，不 copy DSH 代码）。
6. **环境坑**：{{宿主环境坑，如 DSH 默认 ELECTRON_RUN_AS_NODE=1}}——{{处理方式，如显式 env -u}}（实测注记）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：{{D#}} 的 DS1-DS{{N}} / {{D#}} 的 DS1-DS{{M}} 逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim；spec §12 骨架照做（{{§12 内容}}）。
2. **不违反铁律**：接线完整（新 export 有**生产**调用点，测试调用不计 S-3；spec §8 Wiring Verification 逐条 grep 实测）、降级诚实（铁律 24/31）、类型安全（as any=0，铁律 38）、契约优先（铁律 47）、测试非空壳（铁律 48）、架构边界（铁律 39/46）。
3. **无 bug**：spec §7 verify 命令逐条跑通 + `npx vitest run {{测试目录}}/` 全绿（回归既有文件不破）+ pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 每条 grep 出真实生产调用点（如 {{接线示例：setCurrentReportId 在 useStreaming 调用 + RightPanel 消费}}）。
5. **测试到位**：red→green 已证（改造前用例先红）、覆盖正常/降级/边界、expect 断言非空壳。
6. **其他你认为需要复核的点**：残留清理（死代码/旧引用 grep 零）、文件驱动（如新增 manifest/tags）、产物证据可复现性（脚本幂等 + dry-run）等——你判断需要就查。

## 五、K3 审计提示（收尾要求）

- {{切片/任务}}完成后**一次提审**（K3 审 {{切片/任务}}，报告覆盖 {{D#}}-{{D#}} + {{复核点}}）。
- 审计验收 = 验证点 {{验证点}} 从 uncommitted → verified（product-progress.json 重算后 **{{X}}/{{Y}}→{{X2}}/{{Y2}}**，{{收口描述}}）。
- 每个 D 完成后回填 `task-state/{{D#}}.json` / `task-state/{{D#}}.json` 的 **impl 段**（commit + by + files[]；`"slice": "{{切片}}"` 已由 dev-doc 预置）+ 验收证据原文（spec §10 DS 要求）。
- 审计员会**独立重跑你的验证脚本**——脚本必须幂等、可复现、无本机假设（{{环境坑}}、{{产物依赖}}等都在 spec 里标注过，跑前读 spec §4/§12）。

**开始吧。**
