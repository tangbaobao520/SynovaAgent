node_modules/
dist/
data/
# Python 虚拟环境（LLM verifier 等本地工具，2026-08-20）
.venv*/
/coverage/
.env
*.log
*.tgz
*.tar.gz
.DS_Store
Thumbs.db
*.html
!/*.html
!docs/**/*.html
!app/*.html
!app/**/*.html
synova_worker/__pycache__/
**/__pycache__/
.env
.env
STATE.md
docs/*.html
docs/research/*.html
docs/research/*.md
.claude/loop-state.json
# D329: current-brief 是运行时产物（多 session 互相覆盖，已提交值陈旧）
# session 专属 current-brief.<sid> 与全局 current-brief 均去跟踪
.claude/current-brief*

# 会话态运行时产物（D466 减负：post-commit bypass 检测 marker，每次 commit 覆盖）
.claude/last-precommit-success
.claude/gate-hits.log

# D296 (L3-2): 控制塔生成物不入 git — 运行时重新生成, 防止 "git 有、磁盘无" 中间态
# 重新生成命令: python scripts/audit/check-gates-v2.py && python scripts/audit/self-diagnosis.py
# 组件信号: 各 control-tower 组件脚本运行后经 emit-signal.py 写入
/.codex/signals/
/.codex/snapshots/
/.codex/checkpoints/
/.codex/dependency-graph.json

# pre-commit 并行缓存 — 含扫描输出，禁止入库
.claude/.precommit-par/

# ═══ 运行时 / 敏感产物（2026-08-14 GIT-SYNC-PLAN）═══
# 企业事实数据（敏感，不进 git）
.codex/enterprise/facts/
# 本地设置与环境快照
.claude/settings.local.json
.claude/workflow-state.json
.codex/env-snapshot.json
.codex/settings/
# 控制塔运行时产物
.codex/control-tower/tmp/
.codex/control-tower/session-registry.json
.codex/control-tower/health.json
.codex/control-tower/*.corrupt-*
# 审计运行时产物（区别于 docs/synova/audit-reports/ 正式报告）
.codex/audit/
.codex/audit-reports/
# 临时 brief 与 worktree 副本
.claude/task-briefs/*-auto.md
.claude/worktrees/
# Electron 打包产物（几百 MB 二进制，非源码）
release/
# 飞书桥会话状态
.feishu-bridge-state.json
# 写入锁临时目录
.write-locks/
# 根目录临时文档生成/审计/调试脚本（一次性脚手架，用完即弃）
/_*.py
/_*.txt
