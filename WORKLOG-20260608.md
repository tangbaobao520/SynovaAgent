# 工作记录 2026-06-08

## 完成

### 数据管道验证
- ✅ Python Bridge 修复（PYTHONPATH + 模块路径）
- ✅ 飞书 API 连通（token + 成员拉取，权限 `contact:contact.base:readonly`）
- ✅ 文档上传管道：INDEX.md / TEST-STRATEGY.md / DEVELOPMENT-PLAN.html → 实体提取 + GraphStore 写入
- ✅ 集成测试：`tests/data-pipeline.feishu.integration.test.ts` + `tests/data-pipeline.ingest.integration.test.ts` 全部通过

### PDE 工作流
- ✅ PDE 种子数据注入脚本：`tests/e2e/pde-seed-real-data.ts` — 15 节点 + 10 边 + 1 GapSnapshot
- ✅ 诊断引擎 E2E 框架：6 Phase 全部跑通（20 秒），49 事件

### engine-core 拆分
- ✅ 老副本 `server/vendor/@synova/engine-core` 已删除
- ✅ 迁移到 `packages/engine-core`
- ✅ `packages/diagnosis-engine/src/index.ts` 改用相对路径 `../../engine-core/src/...`
- ✅ `tsconfig.json` + `vitest.config.ts` 路径已更新
- ✅ 所有源码零 `@synova/engine-core` 导入

### 底部栏
- ✅ StatusBar 余额 + 月费显示，hints 不再硬编码
- ✅ `/balance` 命令

## 待解决

### vitest 缓存问题
- `packages/diagnosis-engine/src/index.ts` 文件内容确认为相对路径
- 但 vitest 运行时仍报 `@synova/engine-core` 模块找不到
- 可能原因：node_modules 下有 symlink 缓存指向旧内容
- 下一步：检查 `node_modules/@synova/diagnosis-engine` symlink 目标

### 18 模块降级
- 根因：engine context 模块解析双实例导致数据库不可用
- engine-core 拆分完成后应自动修复

## 下一步优先级

1. 跑通 PDE 诊断 E2E（修复 vitest 缓存问题后）
2. 29 模块完整验收
3. 飞书 contacts 完整权限
4. 输出生成（Markdown/HTML/PDF）
