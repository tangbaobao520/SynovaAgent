# 工作记录 2026-06-10 ~ 2026-06-11

## 完成

### 产品对齐（与创始人深度讨论）
- ✅ 全量对齐手册 21 章定稿（`docs/SYNOVA-MASTER-全量对齐手册-20260610.html`）
- ✅ 一页纸产品概要（`docs/SYNOVA-一页纸产品概要-20260610.html`）
- ✅ 理论框架对齐：六维混合组织能力模型为核心方法论，杨三角为辅助
- ✅ 战略三层诊断模型：市场引力 → 竞争力量 → 战略-组织匹配
- ✅ 7 Powers 数学公式工程化 + 力量适用性矩阵
- ✅ 六维组织能力数学表达式（D1-D6 每个维度公式）
- ✅ 模块+哨兵统一为"测量器"：一套代码，两种调用模式
- ✅ 六专家确认：战略/组织/财务/营销/技术/行动（知识库在后台，不在六专家）
- ✅ 输出格式：金字塔四层报告，结论先行，概念先解释，内部术语零出现
- ✅ 竞品防御：反定位 + 低端颠覆 + 价值网络（AI大厂不会做）

### 代码交付（4 个垂直切片）

**切片 1：通用测量管道**
- `measurement-pipeline.js` — 加载 N 个测量器 → 依次执行 → 聚合结果（6/6 测试通过）
- 单测失败不影响其他（degradedModules）
- 同维度加权平均（confidence 权重）

**切片 2：专家推理管道**
- `expert-pipeline.js` — 并行执行 N 个专家 LLM 推理（7/7 测试通过）
- LLM 失败 → 降级，JSON 解析失败 → 自动重试
- 六专家系统级定义（EXPERT_DEFINITIONS）

**切片 3：端到端管线**
- `run-e2e-pipeline.cjs` — 文档 → 提取 → 测量 → 专家 → 报告（5/5 验证通过，16.8s）
- 7 个真实测量器（含战略力量公式 + 组织能力公式）
- 报告结论来自真实 LLM 推理，非模板填充

**切片 4：Web UI + SQLite + 原始文档接入**
- `mvp-server.cjs` — Web 上传页面 + 文件上传 + SQLite 持久化
- 专家直接消费原始文档全文（不再只依赖测量分数）
- 效果：从"数据不足无法判断" → 基于文档内容给出领域洞察

### 自动化门禁系统
- ✅ SPEC 强制（`check-spec.sh`）：feat/ 分支必须有 SPEC.md
- ✅ 测试先行（`check-test-first.sh`）：新 public 函数检测测试引用
- ✅ 诚实门禁（`check-reality.sh`）：`@state:` 标记强制 + Mock 检测
- ✅ Secrets 扫描（`check-secrets.sh`）：LLM API Key + 飞书 App Secret + 回退值检测
- ✅ 接线审计（`check-wire.sh`）：pre-push 新 export 零引用阻断
- ✅ 决策流程（`decide-next.sh`）：post-commit 自动输出下一步建议 + 全局锚点
- ✅ 删除硬编码飞书 App Secret/ID（`tests/data-pipeline.feishu.integration.test.ts`）

### 环境修复
- ✅ phantom `@synova` 路径清理（Windows node_modules 递归 symlink）
- ✅ tsx 恢复正常运行
- ✅ `package.json` 依赖路径修正（`file:../packages` → `file:./packages`）

## 当前状态

### 代码主干（main 分支已推送）
```
packages/engine-core/src/pipeline/diagnosis/
  measurement-pipeline.js    — 通用测量管道 (6/6 测试)
  expert-pipeline.js          — 专家推理管道 (7/7 测试)
  real-measurers.js           — 7 个真实测量器 (含数学公式)
  doc-extractor.ts            — 八维度文档提取器
  report-builder.ts           — 金字塔报告生成器
  cron-scheduler.ts           — 测量器调度框架骨架
  measurement-pipeline.ts     — TS 版本（待与 JS 同步）

src/routes/
  diagnosis-upload-v2.ts      — HTTP 路由 V2（待接入真实管道）
  diagnosis-upload.ts         — 原版路由

mvp-server.cjs                — MVP Web 服务器（当前工作版本）
tests/run-e2e-pipeline.cjs    — 端到端测试脚本
tests/run-mvp-pipeline-real.ts— 真实 API 测试

自动化门禁:
  scripts/workflow/check-spec.sh
  scripts/workflow/check-test-first.sh
  scripts/workflow/decide-next.sh
  scripts/check-reality.sh
  scripts/check-secrets.sh
  scripts/check-wire.sh
```

### 启动方式
```bash
PORT=3066 node mvp-server.cjs   # Web 服务器（上传文档 → 诊断 → 报告）
node tests/run-e2e-pipeline.cjs # 独立端到端测试
```

### 全量对齐 vs 代码现状
```
🟢 完成: 输出格式, FDE工作流, 自动化门禁
🟡 部分: 专家管线, 架构, L2/L3, 测量器(7/25), 代码清理
🔴 空白: L1交互B/C, 无限扩展, 存储(SQLite占位), 运行时, 记忆, 部署, 真实哨兵
```

## 关键决策
1. 模块+哨兵统一为"测量器"——一个 compute() 两种调用模式
2. 六维混合组织能力模型为核心方法论，杨三角为辅助
3. 内部技术术语永远不出现在客户报告中
4. 文档不提交 git（docs/*.html 加入 .gitignore）
5. 专家必须同时消费原始文档和测量数据——不能只依赖结构化评分

## 已知问题
- phantom @synova 路径已清除但 Windows 进程锁导致端口 3000 无法释放
- vitest 14 个失败（旧测试，非新代码）
- engine-core TS 版本测量管道待与 JS 版本同步
- 完整 Express 服务器（src/index.ts）无法启动（其他包依赖问题）
