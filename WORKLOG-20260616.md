# 工作记录 — 2026-06-16

## 完成的工作

### Phase 1 文件优先范式 — 6 个基础能力模块（延续自 2026-06-15）

| # | 任务 | 文件 | 测试 | 接线 |
|---|------|------|------|------|
| C2 | 上下文预算追踪器 | `src/services/context-budget-tracker.ts` | 11 ✅ | app.locals.budgetTracker + GET /api/status/budget + orchestrator-adapter |
| C3 | 渐进式技能加载器 | `src/agent/skill-lazy-loader.ts` | 13 ✅ | ExpertDispatcher 构建 prompt 时注入技能目录 |
| C4 | 多策略上下文压缩器 | `src/orchestrator/context-compressor.ts` | 13 ✅ | ConversationEngine.processMessage() 消息 >30 时自动压缩 |
| C5 | synova.json + last-good | `synova.json` + `src/config-file.ts` | 13 ✅ | loadConfig() 优先加载文件，失败降级到环境变量 |
| C6 | CLI 管理体系 | `src/cli-manager.ts` + 4 命令文件 | 7 ✅ | 注册到 CLI，status 通过 HTTP 调 budget API |
| E2 | 专家模板 + Tool Profiles | `expert/_template/` + `src/agent/tool-profiles.ts` | 14 ✅ | ToolRegistry.execute()/listTools() 按角色过滤 |

### 接线修复（2026-06-16）

| 缺口 | 问题 | 修复方式 |
|------|------|---------|
| C3 未接线 | SkillLazyLoader 从未被生产代码调用 | `expert-dispatcher.ts:294` 在构建 system prompt 时追加 `buildCatalogText(type)` |
| C4 未接线 | ContextCompressor 从未被生产代码调用 | `conversation-engine.ts:460` processMessage 消息 >30 时自动压缩 |
| E2 未接线 | Tool Profiles 定义存在但未使用 | `tools.ts` 新增 `_role` + `setRole()` + execute/listTools 按 profile 过滤 |

### 其他

- OpenClaw 网关重启，更换 API Key
- 清理旧 Agent（删除 5月27日前 3 组重复蒸馏产物）
- Loop Engineering v2.5 部署验证（16 脚本 + 3 Git hooks）
- 文件优先范式转型决策文档产出：`docs/DECISION-文件优先设计范式转型-20260615.md`
- OpenClaw vs Synova-Agent 深度对比报告：`docs/COMPARISON-OpenClaw-vs-SynovaAgent-20260615.html`

---

## 晚间会话 (21:00-22:30)

### 架构违规修复 — 4 处跨层违规清零

| # | 文件 | 违规 | 修复 |
|---|------|------|------|
| 1 | `src/agent/knowledge-file-importer.ts:12` | L2→L4: import `KnowledgeStore` | 内联 `KnowledgeStoreLike` 接口镜像 |
| 2 | `src/routes/reload.ts:13` | L1→L3: import `ExpertRegistry` | 删除导入，用 DI 注入 + 内联类型 |
| 3 | `src/routes/diagnosis.ts:19` | L1→L4: import `GraphStore` | 新建 L2 服务 `post-diagnosis-processor.ts` |
| 4 | `src/routes/diagnosis.ts:20` | L1→L4: import `CommunityReport` | 同上，路由通过 L2 调用 L4 |

**额外修复**: `diagnosis.ts` 中 3 处 `import('../l4/...')` 动态调用移入 L2。
**验证**: tsc=0, check-architecture.sh=全部✅, vitest=158 passed.

### 死代码清理 — 铁律 37+41

- 删除 `src/tui-v2/lib/streaming.ts` — `StreamingPipeline` + `LineBuffer` + `StreamChunker` + `CommitTicker` 四层嵌套，零引用死代码，铁律 41 明令禁止。

### 模板修正

- `generate-task-brief.py`: "6位专家" → "8位专家" (新增 business_model + knowledge)

### Claude Code 粘贴卡顿排查

- 根因: Claude Code v2.1.177 → v2.1.178 自动升级 (今天 11:58)
- 干预: 清理 `history.jsonl` (912KB→1byte) + `paste-cache` + 降级到 v2.1.177
- 验证: 需关闭终端重新启动 Claude Code（内存中仍在跑 178）

## 变更文件

- `src/agent/knowledge-file-importer.ts` — 改
- `src/agent/post-diagnosis-processor.ts` — 新建
- `src/routes/reload.ts` — 改
- `src/routes/diagnosis.ts` — 改
- `src/tui-v2/lib/streaming.ts` — 删
- `scripts/workflow/generate-task-brief.py` — 改

---

## 深夜会话 (22:30-23:30) — Loop Engineering v2.5 物理强制加固

### 接线验证 — 3 个 Phase 1 缺口确认已接线

| 缺口 | 文件位置 | 确认状态 |
|------|---------|---------|
| C3 接线 | `expert-dispatcher.ts:296` → `buildCatalogText(type)` | ✅ 已接线 + 降级 |
| C4 接线 | `conversation-engine.ts:460-475` → `ContextCompressor.compress()` | ✅ 已接线 + 降级 |
| E2 接线 | `tools.ts:113-167` `_role`/`setRole()`/`listTools()`/`execute()` 按 profile 过滤 | ✅ 已接线 |

### 新增 v2.5 物理强制 — hook-enforce-v25.sh

> 背景: 用户指出口头承诺不可信，要求 100% 物理强制

| 措施 | 文件 | 强制力 |
|------|------|--------|
| PreToolUse 新钩子 | `scripts/hooks/hook-enforce-v25.sh` | 🔴 loop-state.json 存在 = 上一轮验证失败 → 物理阻断 Write |
| 注册到 settings | `.claude/settings.local.json` PreToolUse 第二钩 | 🔴 hook-block-write.sh → hook-enforce-v25.sh 串联 |
| Memory 文件 | `memory/loop-engineering-v25-enforcement.md` + MEMORY.md 索引 | 🟡 会话自动加载 |

### Anthropic 决策流程 — task brief 第 7 字段（物理强制）

> 背景: 用户指出另一个 Claude 设计的"如果 Anthropic 团队做这个任务会怎么做"步骤未纳入

| 改动 | 文件 | 效果 |
|------|------|------|
| task brief 模板 + 第7字段 | `scripts/workflow/generate-task-brief.py` | 生成时自带 Anthropic 决策思路 |
| task-start.sh + Q0 | `scripts/workflow/task-start.sh` | 任务最开始就问 Anthropic 做法 |
| hook-block-write.sh + 第7项检查 | `scripts/workflow/hook-block-write.sh` | 🔴 不填 → 物理阻断 Write |
| Memory 文件更新 | `memory/loop-engineering-v25-enforcement.md` | 7 字段清单持久化 |

### 尝试克隆 CodexGuide 仓库

> 尝试了 HTTPS 克隆、SSH 克隆、ZIP 下载、ghproxy/kkgithub 镜像，均因网络超时失败
> 建议用户在 Windows Terminal 手动克隆: `git clone --depth 1 https://github.com/freestylefly/CodexGuide.git`

### 当前完整强制节点 (共 10 个)

```
🧠 会话内:  CLAUDE.md / Memory 注入 / SessionStart hook-check-brief    (3, 软性)
🔴 写前:    hook-block-write (7字段) → hook-enforce-v25 (loop-state)   (2, 物理阻断)
🟡 写后:    verify-incremental.sh L1→L4                                (1, 联锁)
🔴 提交:    pre-commit (38项) / commit-msg / post-commit               (3, 物理阻断)
🔴 推送:    pre-push (6道门)                                           (1, 物理阻断)
```

### 变更文件清单

| 文件 | 操作 |
|------|------|
| `scripts/hooks/hook-enforce-v25.sh` | **新建** — v2.5 物理阻断 hook |
| `.claude/settings.local.json` | 改 — PreToolUse 追加 hook-enforce-v25 |
| `scripts/workflow/generate-task-brief.py` | 改 — 新增 Anthropic 决策思路 第7字段 |
| `scripts/workflow/task-start.sh` | 改 — 新增 Q0 |
| `scripts/workflow/hook-block-write.sh` | 改 — 新增第7项物理检查 |
| `.claude/task-briefs/2026-06-16-1108-Phase-1---3-.md` | 改 — 补填 Anthropic 决策思路 |
| `memory/loop-engineering-v25-enforcement.md` | **新建** → 改 |
| `memory/MEMORY.md` | 改 — 添加 v2.5 索引 |

## 待做（追加）

- [ ] 确认另一个 Claude 重启后自动继承 v2.5 强制体系
- [ ] 用户手动克隆 CodexGuide 后阅读

## 待做

- [ ] synova.json 端口改回 18790（用户已改），确认命令行测试
- [ ] 确认 synova.json + .last-good 回滚机制生产可用
- [ ] 验证 C2 budget API 返回正确数据
- [ ] 验证 C3 技能渐进加载在 ExpertDispatcher 中生效
- [ ] 验证 C4 上下文压缩在长对话中触发
- [ ] 验证 C6 CLI 命令正确工作

## 代码量

- 核心 TypeScript 源文件：236 文件 / 33,565 行
- 测试文件：117 文件 / 14,785 行
- Phase 1 新增：15 源文件 + 6 测试文件 / ~1,500 行
