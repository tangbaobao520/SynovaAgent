# 编码指令 — 对话 HTTP SSE 端点 + SessionStore 持久化 + 鉴权落地（D590）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: 创始人 2026-09-08（L1 审计收工计划 Phase 1 关键路径第一棒；任务号已取 task-state/D590.json）
> 审计: Kimi K3 会盯着你的任务，D590 完成后做最终审计（覆盖 DS1-DS13 全项 + red→green 复现 + 写集对账）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D590 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md | 对话 SSE 端点 + 持久化 + 鉴权 + 410 下线——**编码唯一契约** |
| L1 审计报告 | docs/synova/research/L1全量审计-20260907/L1-audit-report.md | §2 断链图谱 / §5 DSH 借鉴锚点 / §7 D590 行验收原文 / §8 风险 |
| 北星 | .claude/PRODUCT-BRIEF.md §三（怎么工作）+ §六（P0：没有这些不能给 GA 用） | 产品方向锚点 |
| 前车之鉴 | 审计报告附录A（M2/M7 漂移清单）+ spec §4.1 反面教材（im-inbound addToHistory 静默 no-op——禁从 import 线索猜接口） | 教训清单 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §4（Current State，全部 file:line 实测）/ §5（写集+实现契约）/ §7（测试 red→green 表）/ §8（Wiring Verification）/ §10（DS1-DS13）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表（9 修改 + 2 新建 + 2 删除表外）→ 列出你的改动清单（文件级）→ 确认依赖前置（§三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`/`as never`/`as unknown as`=0，铁律 38——spec §5.2-A 明令禁学 im-inbound 的 `as never` 窄化）、契约优先（新函数/端点先 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded，铁律 24/31——无 db 503 fail-closed 是本任务的存在理由）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，red→green 先证）。

## 三、D590 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - spec 基线 = origin/main `891dcdbe`（L1 审计复核更新）。D587/D588 已落 main（ccbbc9ac / eebd9c99），与写集零冲突，无需等待。
   - 编码前置（铁律 0-3 + D334）：`git fetch --all && git pull --ff-only`，feature 分支 `feat/d590-chat-sse` 开工（禁直接 commit main），提交走 synova-commit（**禁 git stash**）。
   - 开工后**重新核验 spec 引用的行号**（conversation-engine.ts / session-store.ts / auth.ts / server.ts 均为活跃文件，若有他线并行合入行号必漂移）——照 spec 旧行号写测试会红（D524 教训）。spec 行号全部标注了"锚定什么"（如 `serialize(): conversation-engine.ts:775`），按锚定语义找位，不盲抄行号。
2. **写集精确性（S-2/D316）**：只改 spec §5.1 写集表列出的文件（9 修改 + 2 新建，新建以目录级条目声明：routes/ 下 **conversations.ts**、tests/routes/ 下 **conversations.test.ts**）+ §5.3 删除清单 2 文件；`git diff --name-only` 与实际改动**完全一致**；check-dev-doc-write-set 实现提交时会按 git diff 对账（删除文件走 --diff-filter=ACMR 语义不入反向对账）。
3. **测试先行（铁律 0-2）**：conversations.test.ts 先写先红（404/401 即 red 证据，README/evidence 双处记录），再实现转 green。测试构造模式（真实路由 + mock 边界 + 先例文件）**严格按 spec §7 执行，此处不复述**。不需要真实 LLM key 即可完成 DS1/DS4-DS12。
4. **诚实 RED / 物理证据**：DS2/DS3 的生产态 curl 物理证据需要真实 LLM 配置（D575 向导或 .env key）。若拿不到 key：集成测试证据替代 + DS2/DS3 如实标注 ⏸ + 理由（README + evidence 双处），**禁止伪造绿、禁止契约断言冒充全链路**（D576 教训）。
5. **evidence 落盘规范**：red→green 双轮输出、curl SSE 原样输出、§8 接线 grep 结果、写集对账，落盘 `docs/synova/audit-reports/D590-chat-sse-evidence-20260908/`（`*.log` 被 .gitignore 全局忽略，改名 `*.log.txt`——D580 先例），K3 独立重跑可复现。
6. **红线（违反 = 事故）**：不碰 `scripts/audit/`（K3 专属）；不碰 src/l2、src/l3、src/l4、src/l5、src/store（L2 零修改是 spec §5.4 决策 5 的承重前提；store 层演进线归 D588 已落）；TUI 相关文件零触碰（创始人裁决 ③ TUI 保留不动）；Stage 3 前零 DSH 依赖（不 install、不 copy DSH 代码，dsh-* 锚点仅范式借鉴）。
7. **环境坑（本机实测）**：① 根 `npm run dev` 指向 scripts/agent-start.bat（Windows 批处理），Mac 起服务用 `npx tsx src/index.ts`；② 门禁 dev-doc-gatekeeper C2 在 BSD grep 下静默空转（`grep -P` 不存在）——不要把 C2"跳过"当路径校验通过，路径以 spec §5.1/§8 表为准；③ `*.log` 被 .gitignore 全局忽略（evidence 用 .log.txt）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：DS1-DS13 逐项对照（spec §10），声称 = 实现 + 验收（S-2），禁 overclaim、禁重编号/跳号/静默缺项（S-10）；七项决策记录（spec §5.4）在完成报告中复述（S-12）。
2. **不违反铁律**：接线完整（spec §8 表逐条 grep 出**生产**调用点——conversations 路由挂载于 server.ts、WebViewAdapter 被 conversations.ts 消费、白名单三前缀在 auth.ts 函数体内，测试调用不计 S-3）；降级诚实（24/31）；类型安全（38，`as never` 零容忍）；契约优先（47）；测试非空壳（48）；架构边界（39/46——新路由仅 L1→L2 静态 + 既有 D563 动态 L5 范式，零新增跨层违规）。
3. **无 bug**：spec §7 用例逐条跑通 + `npx vitest run tests/routes/conversations.test.ts tests/electron/use-streaming-contract.test.ts` 全绿 + 全量 vitest 零新失败 + tsc 零新增 + pre-commit 13 组全过（**禁 --no-verify**）。
4. **接线完整**：spec §8 每条 grep 实测（示例：`grep -rn "conversationsRoutes" src/server.ts` ≥2；`grep -rn "WebViewAdapter" src/routes/` ≥1；`grep -n "/api/conversations" src/middleware/auth.ts` ≥1）。
5. **测试到位**：red→green 已证（实现前用例先红）、覆盖正常/降级/边界/并发/断线、expect 断言非空壳。
6. **其他**：残留清理（`grep -rn "diagnosis-upload-v2" src/ tests/` 仅剩 spec §5.3 列明的 3 处注释；`grep -rn "messageBuffer" src/l1-interaction/` 零结果）；删除文件后 graphstore-unify.test.ts:98 数组项已同步移除且该测试绿。

## 五、K3 审计提示（收尾要求）

- D590 完成后**一次提审**（K3 审 D590，报告覆盖 DS1-DS13 + red→green 独立复现 + 写集对账 + 白名单安全姿态复核）。
- 审计锚点 = spec §10 DS 表 + task-state/D590.json impl 段（commit + branch + files[] + evidence 路径）+ evidence 目录。
- 每项 DS 的验证命令必须独立可重跑（幂等、无本机假设——LLM key 依赖项若标 ⏸，K3 按诚实 RED 口径复核而非判 FAIL）。
- 完成后回填 `task-state/D590.json` 的 **impl 段**（`"spec".path` 已由 dev-doc 预置，勿动 spec 段）。

**开始吧。**
