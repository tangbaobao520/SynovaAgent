# 编码指令 — D602 交互卡片呈现 + preload/IPC 收敛 + 通知 error 消费

> 交付: dev-doc 线（DeepSeek Harness）| 2026-09-09 | 编码线: Claude Code
> spec（编码唯一契约）: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D602-interactive-cards-preload-20260908.md

---

## 一、任务文档表（编码前必读，一个不省略）

| 文档 | 路径 | 读什么 |
|---|---|---|
| **spec（唯一契约）** | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D602-interactive-cards-preload-20260908.md | §4 Current State（file:line 现状）→ §5.1 写集表 → §5.2 契约 → §7 red→green 表 → §8 接线表 → §10 DS1-DS10 |
| L1 审计报告 | docs/synova/research/L1全量审计-20260907/L1-audit-report.md | §3.5.2/§3.5.3（P0-2 原文）、§3.5.1（通知组件判定）、§7 D594 行（=D602）验收原文 |
| D591 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D591-desktop-chat-wiring-20260908.md | §6 不做清单（IPC 修复归本任务的边界依据）；其 useStreaming/conversation-store 改动**不可破坏** |
| D593 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D593-report-persistence-20260908.md | §5.1 文件边界（RightPanel.tsx 归 D593，本任务不碰） |
| 北星 | .claude/PRODUCT-BRIEF.md §一/§三 系统二 | "主动替企业干活"——通知触达是哨兵闭环的可见段 |
| 测试模式先例 | tests/electron/right-panel-report-sentinel.test.ts | mock fetch + 真实 zustand + window 桩 + renderToStaticMarkup（新测试照此模式） |

## 二、执行要求

1. 认真阅读上表任务文档，然后执行任务。
2. 做到你的最高代码水平。
3. 任务复杂（8 修改 + 2 新建 + 1 删除，跨 main/renderer 两进程）——**先用 plan mode 列改动清单再动手**。
4. 先想清楚再动手（禁止没想清楚就改代码）。
5. 测试先行：两个新测试文件**先写先红**（ipc-contract 用例 1 现状必红：preload 2≠接口 10；用例 3 现状必红：main 零 ipcMain），再实现转绿（铁律 0-2）。
6. 做完复核：与 dev doc 一致 / 不违反铁律 / 无 bug / 接线完整 / 测试到位 / 其他你需要复核的点。

## 三、任务专属硬约束（违反 = 审计 FAIL）

1. **依赖基线**：D590/D591/D592/D593 已全部落 main（HEAD=dff18196）。开工前 `git fetch --all && git pull --ff-only`；在新分支（feat/d602-…）上工作，禁止直接 commit main（铁律 0-3/0-4）。若 main 上 sse-contract.ts/useStreaming.ts 行号与 spec §4 漂移，以**函数/语义定位**为准并完成报告注明（M7 漂移先例 D524）。
2. **写集精确性**：只改 spec §5.1 写集表 8 修改 + 2 新建 + 删除清单 1 文件（electron-renderer/preload.js）；`git diff --name-only` 与写集完全一致。**红线：不碰 src/ 任何文件、不碰 RightPanel.tsx（D593 边界）、不碰 StatusBar/TitleBar（显式 descope）、不碰 build-synova.cjs、不补 icon.png（已存在已打包）、零 scripts/audit/（K3 专属）**。
3. **协作边界**：TASK-ROUTING v4 electron 行归 Mac DSH——本任务沿用 L1 审计 §7 + D591/D593 写集例外先例派给 Claude 线；开工前 CTO 确认 Mac DSH 线无在途 electron 任务。主工作区当前有 CT-63 并发 session（分支 fix/ct63-alloc-remote-branch-scan + alloc 测试残留 untracked briefs）——**不要动它们的文件**，开工走独立 clone（.sessions/ 或 worktree）。
4. **诚实 RED/GREEN**：两个新测试实现前必须先红（贴 red 输出入 evidence）；实现后全绿（贴 green 输出）。禁伪造绿、禁为转绿削弱断言。
5. **DS4/DS5 物理证据**：卡片动作落库（POST transition 200 → GET tickets 状态变化）与 SSE error 可见，需真实后端证据（curl 前后对照/日志/截图）落 docs/synova/audit-reports/D602-*-evidence-*/（K3 独立重跑可复现，双处落盘：evidence 目录 + 完成报告）；mock 测试不算此二条证据。
6. **契约逐条落位**：§5.2-A 表 8 个方法逐个实现（main 侧校验：枚举/类型/长度，非法 warn+忽略不抛）；on* 必须返回退订函数（防 effect 重挂载双订阅）；删 bridge 死接口 3 个后 `grep -rn "minimizeToTray\|pauseNotifications\|resumeNotifications" electron-renderer/src/` 必须零结果。
7. **环境坑**：electron/ 是独立 package.json（type: commonjs，electron ^33.4.11 devDep）；vitest 跑 root（tests/electron/ 下 *.test.ts）；新测试不引 electron 运行时（静态 fs 解析 + renderer 侧桩），避免 CI 无 electron 二进制。

## 四、复核清单（做完逐项自查，K3 会盯着 + 最后审计）

- [ ] 与 dev doc 一致：§10 DS1-DS10 逐条对照（S-2 声称=实现+验收，禁 overclaim）；§1 四项勘误口径不得在实现中反转（icon.png 不补、10→8 收敛）
- [ ] 铁律：接线完整（§8 表逐条 grep 生产调用点，测试调用不计 S-3）；降级诚实（每个 catch log+degraded，铁律 24/31；消灭"暂无通知"假空态）；类型安全（as any/as never/as unknown as = 0，铁律 38）；契约优先（preload 方法 JSDoc，铁律 47）；测试非空壳（expect 断言+三路径，铁律 48）；死代码清零（孤儿 preload.js + 3 死接口删除后 grep 零引用，铁律 37）
- [ ] 无 bug：§7 verify 命令逐条跑通（ipc-contract + notification-center + use-streaming-conversation 21/21）+ `npx tsc --noEmit` 28=28 基线 + 全量 vitest 零新失败 + pre-commit 全过（**禁 --no-verify**）+ synova-commit 流程（**禁 git stash**，铁律 0-3）
- [ ] 接线完整：§8 每条 grep 出真实生产调用点并贴进完成报告
- [ ] 测试到位：red→green 已证、三路径覆盖、expect 非空壳
- [ ] 残留清理：删除清单 grep 零引用；无临时 console.log；无 TODO/mock 残留（铁律 8）
- [ ] task-state 回填：impl 段（commit/files/at）+ status=impl_done

## 五、审计提示

- 提审口径：一次提审覆盖 **D602**（原 D594 编号沿革见 task-state/D594.json 改号注记——审计产物命名带 "D602" 防混）。
- 验证点收口：审计 D594 验收①"通知→卡片动作→工单落库"以 DS4 物理证据收口；验收②"托盘/版本号恢复或显式移除"以 DS6 + 死接口删除收口；范围④ StatusBar/TitleBar 为显式 descope（非缺项），报告需引用 spec §6。
- spec 推送后 commit 字段回填 task-state/D602.json（当前 null——主工作区 CT-63 并发在途，提交走路径限定或等收口）。

---

认真阅读任务文档，然后执行任务。做到你的最高代码水平。任务复杂——先用 plan mode 做好计划再执行。先想清楚再动手。做完复核：与 dev doc 一致、不违反铁律、无 bug、接线完整、测试到位、其他你认为需要复核的点。Kimi K3 会盯着你的任务，也会做最后的审计。

开始吧。
