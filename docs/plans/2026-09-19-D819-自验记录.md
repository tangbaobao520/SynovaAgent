# D819 自验记录（试点）· 工具接线三修

> 自验队员：**verify-d819** ｜ 日期：2026-09-19 ｜ 工作树：`/Users/wane/SynovaAgent/.synova-wt-d819-pilot`（分支 `feat/d819-tool-wiring-pilot`）
> 自验起点 HEAD：`737dc634`（编码交付 `aee1d893` 已在内）
> 依据：`task-state/D819.json` 6 条验收点 + `docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`（五态 / 三问 / 四条禁止）+ task-3 指令
> **结论：可提请独立审计**（本记录是**自验**，不是审计——「审计通过」只能由队外 K3 出具；本文按裁定口径只给「可提请审计 / 退回」）
> 遗留问题 6 条见 §7；其中 L1/L2 是**落盘证据质量**问题（不阻断结论，建议 K3 重点复核）。

---

## 0. 范围与红线遵守

| 项 | 实际 |
|---|---|
| 写操作 | 仅本交付物 + 反向验收期间对 `src/providers/base.ts` 的 **1 行临时改动**（已 `git checkout --` 还原，见 §4） |
| 未碰 | `tests/**`、`scripts/audit/**`、编码队员产物（除下述证据文件副作用） |
| 运行副作用 | 跑 D817 用例会按其设计重写 `docs/synova/product-lines/evidence/D817-capture-20260918.json`（机器生成）→ 已按 lead 指示 `git checkout --` **还原、未提交**；实测值原文抄录于 §3.3 |
| 收尾状态 | `git status --short` 在还原后为空；自验起点与终点的 `src/providers/base.ts` `git diff` 均为 0 行 |

---

## 1. 自验起点核查（对应 prep A 段）

### A1 · 起点
```
$ git rev-parse HEAD
737dc63440e5d953d844886f015f38d1784c63d2
$ git status --short
 M docs/synova/product-lines/evidence/D817-capture-20260918.json   ← 编码队员跑 D817 用例后未还原的遗留（§7-L3）
```

### A2 · 写集外溢（`git diff --name-only bef98b2d..HEAD`，12 个文件）
```
.claude/bypass.log                                   ← hook 自动登记（§7-L3，非编码队员手写）
.claude/task-briefs/2026-09-18-D819-tools-in-body.md            ✅ lead 裁决④
docs/plans/2026-09-19-D819-改动清单.md                          ✅ 规格交付物（spec-d819）
docs/synova/product-lines/evidence/D819-capture-20260919.json   ✅ lead 裁决③
memory/notes/implemented/2026-09-18-D819-tools-and-tool-call-pairing.md   ⚠ 路径偏离（批准为 proposed/，见 §7-L3）
src/agent/tool-loop-executor.ts                     ✅
src/providers/base.ts                               ✅
src/providers/types.ts                              ✅ lead 裁决⑤
task-state/D819.json                                ✅
tests/agent/tool-loop-tool-pairing.integration.test.ts          ✅ 新建
tests/integration/production-entry-conversation.integration.test.ts  ✅ lead 裁决①
tests/stream-tool-loop.test.ts                      ✅ lead 裁决②
```
- `.claude/bypass.log` 新增两行均为 `COMMITTED | pre-commit PASS (hook 层登记)`，**无 `--no-verify` 绕过行**。
- 无 `package.json` / `package-lock.json` 改动（无新依赖）。

### A3 · it.fails 清零
```
$ grep -c "it\.fails" tests/integration/production-entry-conversation.integration.test.ts tests/agent/tool-loop-tool-pairing.integration.test.ts
tests/integration/production-entry-conversation.integration.test.ts:0
tests/agent/tool-loop-tool-pairing.integration.test.ts:0
```

### A4 · 契约未变（`git diff bef98b2d..HEAD -- src/providers/types.ts`）
仅**纯增量**：`ToolCall` 增加可选 `id?: string` / `type?: 'function'`；`function: { name; arguments }` 原字段未动；`ChatOptions.tools`、`ChatResult` 未动。
`src/agent/conversation-engine.ts` **零改动**（`git diff --stat` 为空）——F2 去重选择「executor 不推终态、引擎统一入上下文」，引擎保持终态 owner（与规格 §6-11 一致）。

### A5 · 无测试专用开关
```
$ git diff bef98b2d..HEAD -- src/ | grep -niE "^\+.*(NODE_ENV|process\.env\.(CI|VITEST|TEST)|test-only|测试专用|__test|isTest)"
（零命中）
```

---

## 2. 独立自跑（不采信编码队员输出）

### B1 · 主命令（工作树内，`CI=1` 开真 CI 排除面）
```
$ CI=1 npx vitest run --reporter=verbose tests/integration/production-entry-conversation.integration.test.ts tests/agent/tool-loop-tool-pairing.integration.test.ts
```
原始输出节选（完整日志见执行现场记录 / `/tmp/d819-rev-green.log`）：
```
 ✓ tests/integration/production-entry-conversation.integration.test.ts > … > ① POST /api/conversations 建会话：SSE open 帧回声 sessionId（真实入口可达） 152ms
 ✓ … > ② POST /api/conversations/:id/messages：SSE 收束 + 假上游收到真实出站请求 + 证据落盘 66ms
 ✓ … > ③ 上游地址确实指向 127.0.0.1 假上游（配置缝生效，非真实 provider） 0ms
 ✓ … > ④ P0-1 正向断言：出站请求体含 tools（形状合法且含注册工具名） 1ms
 ✓ … > ⑤ D817-F1 正向断言：工具循环被真实驱动（工具轮往返 + 工具注解） 0ms
 ✓ … > ⑥ CI 排除面不含本文件（负向对照：tests/e2e/** 仍在排除面内 = 检查不是空转） 1ms
 ✓ … > ⑦ 本文件确实在 vitest include 面内（tests/**/*.integration.test.ts） 1ms
 ✓ … > ⑧ 降级路径：假上游请求体非 JSON → 400 + parseError 留痕（不静默吞） 2ms
 ✓ … > ⑨ D817-F2 正向断言：出站上下文不重复 assistant 回复 0ms
 ✓ tests/agent/tool-loop-tool-pairing.integration.test.ts > … > A 正常路径：tools 进请求体 + assistant.tool_calls 与 tool.tool_call_id 严格配对 79ms
 ✓ … > B 降级路径：非规范上游缺 id → 确定性兜底且配对仍合法；未知工具失败不阻断对话 198ms
 ✓ … > C1 边界：content 为空 + tool_calls 非空 → 不抛 EMPTY_RESPONSE，配对不丢 37ms
 ✓ … > C2 边界：MAX_ROUNDS 用尽 → 收尾轮不带 tools，3 轮 tool_calls 各自配对且角色序精确 126ms
 ✓ … > 证据文件机器生成且含机器判据字段（体例自证） 0ms

 Test Files  2 passed (2)
      Tests  14 passed (14)
   Duration  4.04s
```
`VITEST_EXIT=0`。

### B2 · 相邻改动面回归（裁决②改过既有用例，我补跑）
```
$ CI=1 npx vitest run --reporter=verbose tests/stream-tool-loop.test.ts tests/tool-calling.test.ts
 Test Files  2 passed (2)
      Tests  17 passed (17)
VITEST_EXIT=0
```
`tests/stream-tool-loop.test.ts` 的旧断言（把 `tool_call_id` 锁成 UUID 形态）已按裁决改为「等于模型给的 `call_test_1` 且非 UUID 形态」，本跑通过。

### B3 · 复跑确定性
还原后二次运行主命令仍 14 passed（见 §4），且 **D819 证据文件两次运行 sha256 完全一致**。

---

## 3. 抓包核对（读我自己这轮跑出的证据文件，不采信转述）

### 3.1 D819 证据（`docs/synova/product-lines/evidence/D819-capture-20260919.json`，我跑出的文件与已提交版 `git status` 无差异 = 可复现）
| 机器面 | 实测值 |
|---|---|
| `registered_tool_count`（顶层） | **36** |
| 全部场景 `requests[].body_keys` | 含 `tools`（场景 C2 的收尾轮 index 3 除外——收尾轮按设计不带 tools） |
| 全部场景 `requests[].tools_len` | 36（收尾轮 0） |
| 角色序 A / B1 / B2 / C1 第 2 请求 | 精确 `['system','user','assistant','tool']` |
| C2 第 4 请求角色序 | `['system','user','assistant','tool','assistant','tool','assistant','tool']`（3 轮工具各自配对 + 收尾轮） |
| `pairing_ok` | 全部请求 `true` |
| `assistant_tool_call_ids` == `tool_message_ids` | A:`['call_d819_ok_1']`；B1:`['call_resp_1']`；B2:`['call_d819_unknown_1']`；C1:`['call_d819_empty_content_1']`；C2 逐轮 `r0 / r0,r1 / r0,r1,r2` |
| `sse_converged` / `sse_error_frames` | 各场景 `true` / `[]` |

### 3.2 ① 的独立复算（不是硬编码，也不是读测试自己的数）
我用**只读 tsx 探针**在同一进程内用同一注册函数复算：
```
$ npx tsx -e "…new ToolRegistry(); registerBuiltinTools(registry, new SessionStore(db), 'verify-probe', () => 0, () => 'verify-org'); registry.listTools()…"
{"service":"agent/builtin-tools","total":26,"msg":"全部工具已注册"}
{"service":"agent/builtin-tools","msg":"Pandoc 文档转换工具已注册"}
REGISTERED_TOOL_COUNT=36
HAS_SHOW_DIAGNOSIS_PROGRESS=true
```
→ 与证据 `tools_len=36`、顶层 `registered_tool_count=36` 三方一致。
（测试内的判据本身也来自同源复算：`tests/agent/tool-loop-tool-pairing.integration.test.ts:220-230` `registeredToolCount()`、`:282` `expectedToolCount`、`:326-327` `expect(toolsCountOf(first)).toBe(expectedToolCount)`。**不是**「长度>0」弱替身，**不是**硬编码。）

### 3.3 D817 证据实测值（我这次运行重写的值；运行后已按要求还原文件）
```
request_count          = 3
body_keys_union        = ['model', 'messages', 'temperature', 'max_tokens', 'tools']
saw_tool_result_message= True
 req 0 body_keys=[…,'tools'] roles=['system','user']                                  has_tools=True
 req 1 body_keys=[…,'tools'] roles=['system','user','assistant','tool']               has_tools=True
 req 2 body_keys=[…,'tools'] roles=['system','user','assistant','tool','assistant','user'] has_tools=True
external_network_calls = []
testimonies.p0_1.observed = True
testimonies.f1.observed   = True
testimonies.f2.observed   = False   （无重复 assistant）
```
→ ① 出站体有 tools；② 第 2 轮出现 `role='tool'`；③ 全部角色序**无** `assistant,assistant`；④ 全程零外网。

### 3.4 逐条判据与配对值的产生方式（防「自证」质疑）
- `pairing_ok` 由 `pairingOk()` 在**真实抓包 body** 上计算（`:91-108`、`:179`），并对每条 id 另有**直接等值断言**：`:348`（assistant id）、`:358-359`（tool id 相等且非 UUID）、`:404-405`、`:445`、`:475`、`:520`（逐请求 `toolIds` == `callIds`）。
- 确定性兜底 id：`:400-403` 断言形态 `^call_resp_\d+$` 且非 UUID；`:410-424` **另起一个假上游复跑同一脚本**，断言两次 `call_resp_1` 相同 → 跨实例确定性。
- 落盘证据的逐请求 `assistant_tool_call_ids` / `tool_message_ids` / `pairing_ok` 均取自同一次抓包（`:165-181`），我核对全部 `true` 且两侧 id 逐项相等（§3.1）。

---

## 4. 反向验收（关键，两次原始输出）

**取一条**：把 P0-1 修复行（`src/providers/base.ts:152` `if (opts?.tools && opts.tools.length > 0) body.tools = opts.tools;`）临时去掉，还原成缺陷形状。

### D2 · 临时改动（diff 原文）
```diff
@@ -147,9 +147,7 @@
     if (opts?.reasoningEffort) body.reasoning_effort = opts.reasoningEffort;
     if (stream) body.stream = true;
-    // D819: tools schema 进请求体（工具循环的生产接收入口 —— 此前在 provider 边界被丢弃）。
-    // 空数组/未传 → 不写字段（与旧行为一致，provider 契约语义不变）。
-    if (opts?.tools && opts.tools.length > 0) body.tools = opts.tools;
+    // [verify-d819 反向验收·临时改动] 还原缺陷形状：tools 不进 body（跑完立即 git checkout 还原）
```

### D3 · 改红（原始输出节选）
```
 × tests/agent/tool-loop-tool-pairing.integration.test.ts > … > A 正常路径：tools 进请求体 + assistant.tool_calls 与 tool.tool_call_id 严格配对 81ms
 × tests/integration/production-entry-conversation.integration.test.ts > … > ④ P0-1 正向断言：出站请求体含 tools（形状合法且含注册工具名） 3ms
⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯
 FAIL  tests/agent/tool-loop-tool-pairing.integration.test.ts > … > A 正常路径：…
AssertionError: expected [ 'model', 'messages', …(2) ] to include 'tools'
 FAIL  tests/integration/production-entry-conversation.integration.test.ts > … > ④ P0-1 正向断言：…
AssertionError: expected [ 'model', 'messages', …(2) ] to include 'tools'
 Test Files  2 failed (2)
      Tests  2 failed | 12 passed (14)
VITEST_EXIT=1
```
**边界披露（诚实）**：红的正好是 tools 断言组（新用例 A + D817 ④）。B/C 场景仍绿，因为假上游是**脚本化**返回 tool_calls、不读请求体里的 tools 字段——红信号可归因但范围窄；配对类断言的反事实由直接等值断言（`:358`、`:445`、`:520`）兜底，我本轮只按指令做了一条反向。

### D4/D5/D6 · 还原 + 复绿（原始输出节选）
```
$ git checkout -- src/providers/base.ts
BASE_TS_RESTORED_CLEAN=yes
restore diff stat: 0 lines
$ CI=1 npx vitest run … （同主命令）
VITEST_EXIT=0
 Test Files  2 passed (2)
      Tests  14 passed (14)
run2 sha256 = a65918291d567bfafb6199df30121ad8f5ad013d1ec216e8fdff584de1f217d4
run1 sha256 = a65918291d567bfafb6199df30121ad8f5ad013d1ec216e8fdff584de1f217d4
```
→ 还原后工作树对 `base.ts` 零差异；两次运行证据文件哈希一致（确定性声称成立，且我的红跑未留残迹）。

---

## 5. 三问自查（逐条作答，附文件:行）

**Q1 · 谁调用它？**
生产调用链（当前代码实测行号）：
```
POST /api/conversations/:id/messages
 → src/routes/conversations.ts:192  createProvider(detectProvider(), {baseUrl})   （provider 构造）
 → src/routes/conversations.ts:242  registerBuiltinTools(conv.getToolRegistry(), …)（工具注册）
 → src/agent/conversation-engine.ts:466  new ToolLoopExecutor(engineCtx)
 → src/agent/conversation-engine.ts:784 / :789 → :871-872  this.toolLoop.streamWithToolLoop(display)
 → src/agent/tool-loop-executor.ts:285-286  callLlm(messages, { tools: toolRegistry.toOpenAITools() })
 → src/providers/base.ts:152  body.tools = opts.tools      ← D819 修复点
 → src/providers/base.ts:154  fetch(`${baseUrl}${chatPath}`)
```
配对修复点：`tool-loop-executor.ts:57 normalizeToolCalls()`（`:164`/`:319` 调用）→ assistant 消息带 `tool_calls`（同源 id）→ 各 tool 消息 `tool_call_id: tc.id`（`:209`/`:214-218`/`:244`/`:355`/`:360-364`/`:393`）。`crypto.randomUUID()` 在该文件已**零残留**。

**Q2 · 有没有一条测试穿过真实实现？**
有，且**不是 fake provider 顶替**：`tests/agent/tool-loop-tool-pairing.integration.test.ts` 起真实 `createServer`（`:289`）+ 真 express 路由 `POST /api/conversations`（`:250`）+ 真实 `ConversationEngine → ToolLoopExecutor → callWithResilience → provider.chat → base.ts fetch`；唯一替身是「模型本身」（`tests/helpers/fake-llm-upstream.ts`，真跑在 127.0.0.1 的 OpenAI 兼容 HTTP 上游，只读复用未修改）。运行日志可见真实 bootstrap 与真实工具执行：
```
{"service":"agent/builtin-tools","total":26,"msg":"全部工具已注册"}
{"service":"agent/tools","name":"show_diagnosis_progress","params":{},"mode":"local","msg":"执行工具"}
```
D817 文件同样穿真实入口（`:212` 真实 createServer + `:249` 真实路由）。

**Q3 · 明天删掉它会坏什么？**
- 删 `base.ts:152` → 实测 A 场景 + D817 ④ 变红（§4），模型收不到 tools → 工具循环进不去。
- 删配对（`tc.id` 换回随机 UUID）→ `:358` `toBe('call_d819_ok_1')` / `:445` / `:520` 直接失败，且 `pairing_ok` 变 false（`pairingOk` 对不上 pending）。
- 删审计员：删除终态去重（executor 不再 double push）→ D817 ⑨ 与 B3 角色序断言失败。

---

## 6. 五态定位（离散计数，不用百分比）

| 修复项 | 态 | 依据 |
|---|---|---|
| P0-1 tools 进出站体 | **健康** | live（生产入口链 `routes→engine→executor→provider` 真跑）+ testedThroughEntry（§2 真实 createServer 用例，`git grep` 不算） |
| 响应侧 tool_calls → `ChatResult.toolCalls` | **健康** | 同上；D817 证据 request_count 2→3、第 2 轮出现 `role='tool'` |
| assistant 不重复入上下文（F2） | **健康** | 全部出站角色序无 `assistant,assistant`；D817 ⑨ 正向通过 |
| tool_call_id == 模型 id（配对） | **健康** | 全部 `pairing_ok=true`、id 逐项相等、非 UUID |

报告口径：**4 个健康 / 0 个写了没接 / 0 个缺**（本卡 4 个工作面）。
口径说明：判「健康」用的是本卡「穿真实入口」判据；《产品完成度总纲》条件 C 的**客户侧验证**属全局未决项，不在本卡范围。

---

## 7. 遗留问题清单（不阻断结论；L1/L2 建议 K3 复核）

| # | 问题 | 位置 | 严重度 | 说明 |
|---|---|---|---|---|
| **L1** | 证据文件**每个场景**的 `registered_tool_count` 恒为 `0`，与顶层 `36`、各请求 `tools_len=36` 自相矛盾 | `tests/agent/tool-loop-tool-pairing.integration.test.ts:151`（`let evidenceToolCount = 0`，**从未赋值**）→ `:164` 落盘使用 | 中（证据质量） | 真实判据走 `expectedToolCount`（`:282`/`:326-327`），**不影响通过结论**；但落盘文件带矛盾字段，K3 会抓。修法：删该字段或 `beforeAll` 内 `evidenceToolCount = expectedToolCount` |
| **L2** | 用例名 overclaim：`证据文件机器生成且含机器判据字段（体例自证）` 实际只断言 `expectedToolCount>0` 与 `entryBaseUrl`，**未读证据文件**（证据在 `afterAll:296-297` 才写） | `tests/agent/tool-loop-tool-pairing.integration.test.ts:533-537` | 低（标题与断言不符） | 建议改名或补「afterAll 后再读回校验」的独立用例 |
| **L3** | 写集/流程偏离 3 处：① memory note 落在 `memory/notes/implemented/…`（批准为 `proposed/`）② `.claude/bypass.log` 被 hook 自动提交（无 `--no-verify` 行，属正常登记）③ 编码队员跑完 D817 用例后**未还原** `docs/synova/product-lines/evidence/D817-capture-20260918.json`（我已按 lead 指示还原） | `memory/notes/implemented/2026-09-18-D819-tools-and-tool-call-pairing.md`；`.claude/bypass.log`；D817 证据文件 | 低 | 前两项是记账口径问题；第三项已闭合 |
| **L4** | **已提交的 D817 证据文件在 D819 修复后是过期事实**（`testimonies.*.observed=false`、`expected_now` 仍是「未修」文案）。按 lead 指示本轮不提交其重写（#644 未合，避免冲突）→ 分支上的该文件下一次跑 D817 用例时才会刷新 | `docs/synova/product-lines/evidence/D817-capture-20260918.json`（HEAD 版本） | 中（跨卡一致性） | 我跑出的实测值已原文抄录 §3.3；请 K3/CTO 知悉「文件过期 ≠ 修复未生效」 |
| **L5** | D473 reminder 路径会给**同一次工具调用**多推一条 `role='tool'` 消息，与真实结果共用同一 `tool_call_id` → 该结构下 `pairingOk` 会判 false（第二条 tool 的 id 不在 pending） | `src/agent/tool-loop-executor.ts:213-218`（非流式）、`:358-364`（流式）、结果推入 `:244`/`:393` | 中（既有设计，非本卡回归） | 本卡规格 §6-6 明确不改 reminder 语义；任何测试也未覆盖该触发面（C2 用例 `:487-488` 主动避开同工具同参数）。**静态判读，未跑用例**；建议另开卡 |
| **L6** | `call_resp_<n>` 兜底序号按 **provider 实例**重置（每个 HTTP 请求新建 provider）→ 非规范上游缺 id 时，跨请求/跨轮理论上可能重号 | `src/providers/base.ts:124`（`fallbackToolCallSeq`）、`:240-241` | 低 | 仅影响缺 id 的非规范上游；合法上游不受影响。`provider.stream()` 的 SSE `delta.tool_calls` 仍未映射（规格 §6-1 声明不在本卡，本记录仅作上下文） |

---

## 8. 结论

**可提请独立审计。**

- 卡内 6 条验收点 + CTO 扩围（响应映射 / 去重 / 角色序精确）**逐条通过**，全部由我独立跑出（命令 + 原始输出见 §2–§4）。
- 反向验收完成：缺陷形状 → **红**（A + D817 ④，断言原文在案）→ 还原 → **绿**（14 passed，两次证据哈希一致）。
- 落盘证据机器生成且可复现（`sha256=a6591829…` 两次相同），配对字段齐全，tools 长度三方一致（测试断言 / 我的独立探针 / 证据文件均为 36）。
- §7 六条遗留问题均**不构成退回理由**；L1/L2（证据文件内部矛盾字段与用例名 overclaim）与 L4（D817 证据文件过期）请 K3 重点复核后再定论。
- 本记录不含、也不代表任何「审计通过」结论；独立审计由队外 K3 执行。

---

### 附：本记录引用的命令清单
```bash
git rev-parse HEAD; git status --short
git diff --name-only bef98b2d..HEAD
grep -c "it\.fails" tests/integration/production-entry-conversation.integration.test.ts tests/agent/tool-loop-tool-pairing.integration.test.ts
git diff bef98b2d..HEAD -- src/providers/types.ts
CI=1 npx vitest run --reporter=verbose tests/integration/production-entry-conversation.integration.test.ts tests/agent/tool-loop-tool-pairing.integration.test.ts
CI=1 npx vitest run --reporter=verbose tests/stream-tool-loop.test.ts tests/tool-calling.test.ts
npx tsx -e "…new ToolRegistry()+registerBuiltinTools(…)…"   # 独立复算 36
shasum -a 256 docs/synova/product-lines/evidence/D819-capture-20260919.json
# 反向验收：edit base.ts:152 → 跑主命令（红）→ git checkout -- src/providers/base.ts → 跑主命令（绿）
git checkout -- docs/synova/product-lines/evidence/D817-capture-20260918.json   # 还原运行副作用
```
