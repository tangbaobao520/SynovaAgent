# Stage 1 任务卡序列（D1-D5 + 客户配置包雏形）

> 登记：DOC-0116 | type: devdoc | status: draft（待开发线认领）| owner: founder + dev-line
> 2026-08-20 | 上游：DOC-0114《DSH迁移施工图》§6 Stage 1 + §5.4 | 基线：origin/main `23b6de9d`
> 模板：遵循 docs/authority/COMMIT-PLAN-20260820.md §四 task brief 模板（Q0-Q3 + 架构层 + Done）
> 接口审计：卡中引用的全部接口已在 main 快照 grep 实证（文件:行号）

---

## 〇、序列总览（排序与依赖）

| 序 | 卡 | 内容 | 依赖 | 建议认领线 | 预估 |
|---|---|---|---|---|---|
| S1-1 | D1 | 事件溯源 session log | 无（地基，先行） | 开发线 | 5-7 天 |
| S1-2 | D2 | Agent Notes 四态记忆 | 无 | 开发线 | 2-3 天 |
| S1-3 | D3 | snapshot 测试 + 黄金数据集门禁 | 无 | 开发线 | 2-3 天 |
| S1-4 | D4 | guard 循环卫生 + 超时通用化 | 无 | 架构线（控制塔归口） | 2-3 天 |
| S1-5 | D5 | 组织记忆策略（OpenViking 应对） | 无 | 研究+开发 | 3-4 天 |
| S1-6 | CFG | 客户配置包雏形（哇呢宝贝为实例） | 无（但建议 D1 之后，校准写回依赖事件流） | 开发线 | 4-5 天 |

**铁律提醒**：① 六张卡互不阻塞，可并行认领，但每张单独 PR、单独过门禁；② Track A（哇呢宝贝部署）绝对优先——部署验收前这些卡全部只开发不合入主干交付物；③ 每卡必须含接线审计（grep 调用方 + 集成测试走真实路由，历史 4 次接线事故）；④ 借鉴 = 复制范式自研，**不引 DSH 代码依赖**（rc.5 破坏性变更承诺）；⑤ D5 红线：不复制 OpenViking 代码（AGPLv3）。

---

## S1-1【D1】事件溯源 session log

```text
## Q0: 定位 — 借鉴 DSH 事件溯源范式，改造 Synova 会话存储
### a) 项目拼图
施工图 §4 能力映射第 1 行 + §5.3 自进化 E2 前置。补 C 线 S3-5（自诊断可信度）/ S0（信任建立）根因：
没有可回放的真相流。现状 SessionStore 是普通存储（src/store/session-store.ts:57 export class SessionStore，实证）。
### b) 文件审计
主要改动面：src/store/session-store.ts（658 LOC 目录）；消费方：ConversationEngine（src/agent/conversation-engine.ts）。
### c) 决策
借鉴理念自研，不引 DSH 代码。消费方接口不变（capability seam 思想：能力可换、消费方不动）。

## Q1: 调研 — 历史教训
a) 业界：DSH core/session（append-only SessionEvent log + deriveMessages 投影 + model-visible⟺logged 不变量，证据 H-05）。
b) 我们犯过的错：4 次接线失败——新机制必须从生产入口可触达；铁律 11 静默降级禁止。

## Q2: 范围 — 正确的最简方案
做什么：SessionStore 增加 append-only 事件流 + deriveMessages() 投影 + model-visible⟺logged 运行时断言。
不做什么：不动 ConversationEngine 对外接口；不动 routes/；不做 UI 回放视图（Trajectory 留到后续）；不引 dsh 包。

## Q3: 验收 — 入口 → 交互 → 结果
入口：生产链路 src/index.ts → SynovaAgent → 一次真实对话/诊断。
处理：会话全程事件落 log；kill 进程后从 log 重建模型消息历史。
结果：① 回放重建的消息历史与实际一致（测试断言）② model-visible 输入未落 log 时断言报警 ③ 集成测试走真实路由
契约：输入=会话事件流；输出=消息历史投影；降级=log 写入失败 → log.error + degraded: true（铁律 24/31）
测试：正常路径 + 降级路径 + 边界（空 log/半截事件）（铁律 48，非空壳）

## 架构层: L5（src/store/）→ 消费方 L2
## Done 标准: 回放一致 + 断言生效 + 集成测试真实路由 + 生产入口可触达 + pre-commit 全绿
```

---

## S1-2【D2】Agent Notes 四态记忆

```text
## Q0: 定位 — memory/ + 铁律演进为结构化四态（proposed/implemented/archived/rejected）
### a) 项目拼图
施工图 §4 门禁体系行 + 补 C 线 S5-3（知识沉淀结构化）。现状 memory/ 20 份教训卡（实证存在）。
### b) 文件审计
改动面：memory/ 目录结构 + 读取方（hook-check-memory.sh 等注入点）。
### c) 决策
借鉴 DSH .agents/notes 四态（证据 H-13），与现有铁律体系合并而非推翻。

## Q1: 调研 — 历史教训
教训注入机制（hook-check-memory.sh）已在服役——格式变更必须向后兼容，老教训卡批量迁移不手写。

## Q2: 范围
做什么：定义 Note schema（四态 + 日期 + 关联决策 D#）；写迁移脚本把 memory/ 存量转四态；注入逻辑读四态（archived/rejected 不注入）。
不做什么：不改铁律内容本身；不动 pre-commit 其余 13 组。

## Q3: 验收
入口：PreToolUse hook 注入点。
处理：注入时只读 proposed+implemented 态。
结果：① 存量 20 份教训卡迁移零丢失 ② 注入内容符合四态过滤 ③ 迁移脚本可重入（幂等）
契约：输入=memory/ 文件集；输出=四态 Note；降级=单文件解析失败跳过 + log.warn + degraded
测试：正常/降级/边界（空目录、畸形 YAML）

## 架构层: L0（工程治理/开发侧）
## Done 标准: 存量迁移零丢失 + 注入链路实测生效 + 幂等
```

---

## S1-3【D3】snapshot 测试 + 黄金数据集门禁

```text
## Q0: 定位 — 模型/报告输出可复现，黄金数据集接入门禁
### a) 项目拼图
补 C 线 P1-2（黄金数据集未入门禁）。借鉴 DSH snapshot 测试（keyless 回放，证据 H-13）。
现状：vitest 524 个测试（实证）+ scripts/golden-scenarios/ 存在（实证）。
### b) 文件审计
改动面：tests/ + scripts/golden-scenarios/ + pre-push 门禁。
### c) 决策
与 vitest 体系并存，不替换；门禁挂 pre-push（不挂 pre-commit，不拖慢提交）。

## Q1: 调研 — 历史教训
pre-push 已有 golden-case F1 检查（AGENTS.md 门禁表）——本卡是把它升级为 snapshot 机制，不是另起炉灶。

## Q2: 范围
做什么：① 报告/诊断输出的 snapshot 录制与回放对比 ② 黄金数据集断言接入 pre-push。
不做什么：不做全量 100% 覆盖率门禁（DSH 的做法，对我们过重）；不改 vitest.config 现有结构。

## Q3: 验收
入口：git push（pre-push 触发）。
处理：黄金场景输出 vs snapshot 对比。
结果：① 输出变化 → 门禁报警并给出 diff ② 首次录制流程文档化 ③ 误报率零（首周全绿）
契约：输入=黄金场景集；输出=pass/diff；降级=snapshot 缺失 → 首次录制模式 + log.warn
测试：正常/降级/边界（新增场景无 snapshot）

## 架构层: L0（测试/门禁）
## Done 标准: pre-push 真实拦截一次人为变更 + 连续一周零误报
```

---

## S1-4【D4】guard 循环卫生 + 超时通用化

```text
## Q0: 定位 — 借鉴 DSH guard（timeout-policy + repeat-tool-reminder），通用化控制塔防跑偏
### a) 项目拼图
现状：src/infra/command-lanes.ts 已有 60s 超时的工具执行隔离（实证：synova-agent.ts:254 require 加载）；
控制塔有 guard 雏形。本卡是把"超时策略 + 循环卫生"从单点机制升级为通用策略层。
### b) 文件审计
改动面：src/infra/command-lanes.ts + scripts/control-tower/ + tool-loop-executor（src/agent/tool-loop-executor.ts）。
### c) 决策
融入控制塔/infra 现有机制，不新建平行系统（施工图 §8 R6：治理层独立节奏）。

## Q1: 调研 — 历史教训
CTO 审计教训（2026-08-20）：command-lanes 是运行时 require 的活跃安全机制——改它之前先读清调用链，禁止当死代码。

## Q2: 范围
做什么：统一 timeout-policy 配置层（每工具/每循环可配）+ repeat-tool-reminder（同工具重复调用检测与提醒）。
不做什么：不动 command-lanes 的隔离语义；不改哨兵调度（src/loops 是领域，不属本卡）。

## Q3: 验收
入口：一次真实 agent 会话（含工具调用）。
处理：超时触发降级路径 + 重复工具调用触发提醒。
结果：① 超时按策略降级（非静默）② 重复调用被检测并提醒 ③ 既有 60s 隔离行为不回归
契约：输入=工具调用流；输出=超时/重复事件；降级=策略加载失败 → 默认策略 + log.warn + degraded
测试：正常/降级/边界（刚好超时阈值、零调用）

## 架构层: L0/L2（infra + agent）
## Done 标准: 两类事件真实触发 + 不回归 + 生产入口可触达
```

---

## S1-5【D5】组织记忆策略（OpenViking 应对）

```text
## Q0: 定位 — S5-3/S5-4 从"自建通用记忆存储"转向"通用记忆底座思想 + Synova 领域知识结构"
### a) 项目拼图
施工图 §5.2 #15 + 8-16 研究 §7.4 OpenViking 威胁（记忆/知识底座化是真实威胁，通用存储层重复造轮子必输）。
差异化必须在知识结构层：42 边本体即知识组织方式。现状 knowledge/ 已三层（custom/industry/shared，实证）。
### b) 文件审计
改动面：knowledge/ + src/agent/knowledge-injector.ts + src/agent/knowledge-bridge-service.ts。
### c) 决策（红线）
不复制 OpenViking 代码（AGPLv3 传染）；只借鉴理念（三层加载 L0/L1/L2、目录递归检索、会话变记忆）自研；
42 边本体是知识组织方式，不做通用向量库。

## Q1: 调研 — 历史教训
8-16 研究第八章已完成 OpenViking 专项深挖（28575 star，机制已分析）——直接引用，不重复研究。

## Q2: 范围
做什么：① knowledge/ 三层加载策略（L0 常驻/L1 按需/L2 归档）② 诊断会话→领域知识的沉淀管道（挂 42 边索引）。
不做什么：不接 OpenViking 服务（本期纯自研）；不做向量检索；不动本体计算。

## Q3: 验收
入口：一次真实诊断完成后的知识沉淀。
处理：会话产出按 42 边索引沉淀进 knowledge/custom/。
结果：① 沉淀条目可被后续诊断检索命中 ② 三层加载策略生效（L2 不进上下文）③ 与知识冲突处理（KnowledgeConflictHandler，实证存在）联动不报错
契约：输入=诊断会话产出；输出=知识条目；降级=沉淀失败不阻塞诊断 + log.warn + degraded
测试：正常/降级/边界（空产出、冲突条目）

## 架构层: L3/L4（知识层）
## Done 标准: 沉淀→检索命中闭环实测 + 红线零违反
```

---

## S1-6【CFG】客户配置包雏形（哇呢宝贝为实例）

```text
## Q0: 定位 — 多客户命门机制的第一个实例（施工图 §5.4，最高业务价值的一张卡）
### a) 项目拼图
四层叠加：merge(全局默认, 行业包, 客户包, 工作区包)。目标：签新客户的全部技术动作 = 复制模板文件夹填配置，零代码改动。
现状证据（全部实证）：
  · src/l3/report-template-loader.ts:89 loadTemplate(name, clientId?) —— clientId 参数已存在，客户化雏形已在
  · src/sentinel/sentinel-loader.ts —— manifest thresholds（warning/critical）已声明式
  · src/sentinel/baseline-store.ts —— BaselineRecord/BaselineStats（实证 export）
  · src/security/credential-vault.ts:12 CredentialVault + :90 createVaultKey（实证）
  · expert/expert-registry.yaml —— 专家声明式启停
### b) 文件审计
改动面：新增 customers/ 目录（规范 + wbb 实例）+ 配置加载器（新文件，建议 src/config/ 或 src/l2/）；
接线点：sentinel-loader 阈值读取、report-template-loader clientId、credential-vault 按客户隔离。
### c) 决策
借鉴 DSH profile/bundle 思想（有序层级/上层覆盖/dump 可检查）自研，不引 DSH 代码。
哇呢宝贝 = 第一个实例，边部署边长规范。

## Q1: 调研 — 历史教训
文件驱动哲学已被本项目反复验证（expert-registry/sentinel-loader）——配置包沿用同一哲学；
铁律 47 契约优先：加载器输入/输出/降级契约先写 JSDoc 再实现。

## Q2: 范围
做什么：① customers/ 目录规范（模板 + wbb 实例：连接器凭证引用/阈值覆盖/启用哨兵集/报告品牌/GA 审批人/角色微调）
② 配置加载器：merge(L0,L1,L2,L3) + dump 实际生效配置（借鉴 --dump-config）③ 三处接线（阈值/模板/凭证）。
不做什么：不做 Web 配置 UI；不动 expert-registry.yaml 格式；不做跨客户聚合；权限四要素实测属部署验收清单，不在本卡。

## Q3: 验收
入口：以 wbb 客户包启动服务（配置指定客户）。
处理：哨兵阈值/报告模板/凭证按客户包生效。
结果：① dump 输出 = merge 结果（测试断言）② wbb 覆盖项生效、未覆盖项回落全局默认 ③ 缺客户包 → 清晰报错不静默
契约：输入=customerId；输出=生效配置对象；降级=客户包缺失 → 报错退出（启动期硬失败，优于静默用默认值）；单层解析失败 → log.error + degraded
测试：正常/降级/边界（空包、非法字段、层级冲突）

## 架构层: L2 配置层 → 消费方 L3/L5
## Done 标准: wbb 实例包真实生效 + dump 可检查 + 契约测试全过 + 生产入口可触达
```

---

## 附：认领与门禁说明

- 每张卡认领时：复制对应代码块进 `.claude/task-briefs/` 当前任务文件，补齐 hook 要求的字段（PreToolUse 门禁硬检查）
- 每张卡 PR 时：pre-commit 13 组 + G14 文档门禁全绿；新 export 接线审计（铁律 0-2 WIRE CHECK）
- 卡与卡之间无代码依赖，但 **D1 先行**能让 CFG 的"校准写回客户包"直接落在事件流上（施工图 §5.4 与 §5.3 的衔接）
- 部署验收前：全部卡只开发不合入主干交付物（施工图 §6 Stage 0 收紧条款）

> 关联文档：DOC-0114（施工图全量）/ DOC-0115（一页纸大纲）/ docs/authority/GOVERNANCE.md（文档规范）
