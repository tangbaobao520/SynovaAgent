---
title: "SynovaAgent 文档契约"
version: "1.0.0"
status: implemented（已生效）
approved_at: "2026-09-27"
effective: "2026-09-27"
owner: CTO
supersedes:
  - docs/synova/DOCUMENT-CONVENTIONS.md
  - docs/plans/codex/SYNOVA-哇呢宝贝-文档管理规范-20260805.md
  - docs/synova/DOCUMENT-INVENTORY.md
---

# SynovaAgent 文档契约 v1.0

> **给非技术读者的一句话**：这份契约规定了「什么该写进仓库、写成什么样、由谁维护」。
> **它要解决的一件事**：我们把「过程」也提交进了仓库 —— 每一份卡、日报、分类、派单都变成一次提交，
> 于是仓库臃肿、CI 满载、PR 积压、新人无从下手。**文档多不是问题，什么都提交才是问题。**

---

## 0. 第一性原理：为什么我们和 DSH 不同

我们借鉴 DeepSeek Harness（国内顶级团队）的文档体系，但**不复制**。因为前提不同：

| 维度 | DSH | **我们** |
|---|---|---|
| 团队 | 20 人 + 人的记忆沉淀 | **无技术背景创始人 + 几个 AI 助手** |
| 平台层 | PR / issue 有人读 | **创始人不会去翻 PR 列表；AI 没有人的记忆** |
| 记忆载体 | 人 + 平台 + 仓库 | **只有仓库** |
| ⇒ 结论 | 过程可以不入库 | **思考必须入库；痕迹不入库** |

**⇒ 我们与 DSH 唯一根本分歧**：它可以让过程不入库；**我们不能**。
**⇒ 因此本契约的命题是：痕迹不入库，思考必须入库，且思考必须「双读者可读」。**

**学 DSH 什么（形状） / 不学什么（前提）**：

| ✅ 学 | ❌ 不学 | 原因 |
|---|---|---|
| 四态生命周期 + 类别闭集 + **路径即元数据** | 双语 + 1743 个 i18n sidecar | 内部单语项目，抄它自增 3 倍文件量 |
| **格式由门禁强制**（不靠自律） | 3539 个 note 的规模 | 我们起步，先做质量 |
| **必须写「考虑过的其他方案」** | 「过程完全不入库」 | 见上表：我们的仓库就是记忆 |
| **禁止中央索引**（用 `git grep` 检索） | 15 组 gen/verify 对账 | 先做 3 组（见 §6） |
| **取代链强制 + 归档后冻结** | 用「词数 / 年龄 / 配额」决定归档 | 同 DSH：改用「是否仍有未来指导价值」 |
| PR 三段式模板（动机/变更/证据） | — | 过程留痕的平台层载体 |

---

## 1. 唯一判据（写任何东西之前先问）

> **❓「3 个月后一个新人，是否必须靠它才能理解我们为什么这样做？」**
>
> **✅ 是** ⇒ 写入 **B 决策层**（`decisions/`）
> **❌ 否** ⇒ **不写进仓库**（写 PR 正文 / commit message / 卡 note）

**这一问就砍掉我们大部分 md** —— ⚠️ **具体数字不写在本契约**（本契约只定规则）；
**现行数字见 `docs/synova/STATE.md` §4（那里每值带 as_of 与取数命令）。**

---

## 2. 五层归属（每份文档/记录必须归属且仅归属一层）

| 层 | 位置 | 内容 | 维护者 | 维护方式 |
|---|---|---|---|---|
| **A 入口** | `README.md`、`AGENTS.md`、`CLAUDE.md`、`docs/synova/STATE.md` | 这是什么 / 规则是什么 / **现在到哪了** | 创始人 + CTO | **重写式**（不追加） |
| **B 决策** | `decisions/{lifecycle}/{class}/YYYY-MM-DD-topic.md` | **为什么这么做**、考虑过什么、放弃了什么 | **做决定的人**（不代笔） | 四态 + 取代链 |
| **C 长期** | `docs/**` | 指南 / 架构 / API / 领域知识 / 术语表 | 各域作者 | 一主题一份，禁中央索引 |
| **D 过程** | **不入库** | 做了什么、进度、分类、派单、验收、探针、日报 | 各 session | **PR 正文 + commit + 卡 note** |
| **E 生成** | 不入库或标注 | 看板 / 水位 / 清单 / 测试输出 | 生成器 | 三条同时满足方可入库 |

### 2.1 A 层：入口（新人 30 分钟路径）

| 顺序 | 文件 | 回答 | 上限 |
|---|---|---|---|
| 0–5 min | `README.md` | 这是什么；怎么跑起来 | 重写式，短 |
| 5–15 min | `AGENTS.md` | 规则是什么（铁律 / 门禁 / 流程 / 怎么提交） | 重写式 |
| 15–20 min | `docs/synova/STATE.md` | **现在到哪了**（阶段 / 在做什么 / 下一步 / 卡在哪） | **≤200 行，重写式** |
| 20–25 min | `decisions/` 最近 5 条 | 关键决策与为什么 | 见 §3 |
| 25–30 min | `docs/synova/architecture/` | 系统结构 | 见 §4 |

**三条硬规矩**：
1. **A 层是「重写式」的** —— 每次更新**重写**，不做追加（追加即变流水账 = 痕迹）
2. **`STATE.md` 是「现在到哪了」的唯一答案** —— 不允许第二个进度文件
3. **禁止新建任何索引 / 台账 / 看板 md（主仓库范围内）** —— 库外资产仓另见 §11（其 `INDEX.md` 是**库外**索引，不在本条约束内）（照 DSH：*"Do not add a centralized INDEX.md"*）—— 要检索用 `git grep`

### 2.2 B 层：决策记录（本契约核心）

**结构（路径即元数据）**：
```
decisions/{lifecycle}/{class}/YYYY-MM-DD-topic-title.md
```

**lifecycle 四态**（文件在文件夹间移动 = 状态变化）：
- `proposed/` —— 提案，评审中，尚未落地（或部分落地）
- `implemented/` —— 已落地；**并与实际交付保持一致**（改路径/改名/改默认值时同批更新事实，不改决定本身）
- `rejected/` —— 否决；**仅当其理由能阻止一个诱人的错误时才保留**，否则连同三元组删除
- `archived/` —— 冻结：已完成、理由不再指导未来、但仍有历史价值

**class 闭集（六类，门禁强制；新增类别须先改门禁与本节）**：
| class | 覆盖 |
|---|---|
| `product` | **产品方向 / 用户 / 定位 / 商业模式** ← 我们独有（DSH 没有） |
| `architecture` | 结构、分层、数据流、契约 |
| `process` | 流程、门禁、工具、协作方式 |
| `feature` | 新的用户或模型可见能力 |
| `bug-fix` | 缺陷修复及其教训 |
| `simplification` | 删除 / 简化（不新增能力） |

**文件格式（门禁强制：六段缺一不可 —— 2026-09-26 按 K3 判据③定版）**：
```markdown
# 决策: <标题>

状态: <proposed | implemented | rejected — 原因>
日期: YYYY-MM-DD

## 一句话
（**给非技术读者**：这个决定改变了什么。≤50 字，禁止未解释术语）

## 问题
（什么场景、什么痛点。必须让不懂技术的人读懂）

## 决定
（决定了什么 + 为什么这么定）

## 考虑过的其他方案        ← 强制。缺少它 = 阻断
（有哪些、为什么不选。写不出就写「未记录（pre-format）」并登记）

## 后果
（好的 / 坏的 / 要长期承担什么）

## 取代
（取代了谁 / 被谁取代 —— **相对链接**，机器可核）
```

**两条我们独有的要求**（因创始人无技术背景）：
1. **`## 一句话` 必填** —— 让你 30 秒知道「这改变了什么」
2. **`## 问题` 段禁止未解释术语** —— 术语须在 `docs/synova/GLOSSARY.md` 中可查

**取代链（强制流程）**：
```
写新决策前 ⇒ 必做三步：
  ① grep 同主题旧决策 → 列出候选
  ② 判「全部取代 / 部分取代 / 不取代」
  ③ 全取代 ⇒ 同一 PR 内归档旧的（移动 + 加 `Archived:` + 冻结）
     部分取代 ⇒ 双方互加相对链接
⇒ 未输出「候选清单 + 判定」= 阻断（可为「无候选」，但必须显式写）
⇒ 目的：根治「同一决定有 5 份文档各说各话」
```

**归档即冻结**：归档的决策文件**不得再编辑、翻译、重排、移动或删除**。

### 2.3 C 层：长期文档

- **一主题一份**；发现重复 ⇒ **合并**，不新增
- **禁止中央索引**
- 允许子目录：`architecture/`、`implementation/`、`runbooks/`、`setup/`、`research/` 等
- 现有 `docs/synova/**` 按 §7 迁移（**数量不写在此** —— 见 `STATE.md` §4 或跑枚举命令）

### 2.4 D 层：过程（**不入库**）

**PR 三段式**（唯一过程载体）：
```markdown
## 为什么（Motivation）
（一句话：解决什么问题 / 给谁 / 不改会怎样）

## 改了什么（Changes）
（命令 / 配置 / 接口 / 行为变化；用户可观察的变化优先）

## 怎么证明（Testing）
- 方法：
  <details><summary>证据</summary>
  （原始输出 / 截图 / 日志）
  </details>
```

**其余过程痕迹**：`commit message` + 卡 note（`task-state/<D>.json` 的 `note` 字段，一卡一文件，**不派生 md**）。

### 2.5 E 层：生成物

**三条同时满足才可入库**：① 文件头标 `GENERATED` ② `.gitattributes` 标 `linguist-generated` ③ **配 `--check` 对账**
**缺任一 ⇒ 不得入库**。现状缺口：`DASHBOARD-CN.md`、`tests/output/**`、`.claude/reference-map.md`。

---

## 3. 三闸（机器可核 —— 不靠自律，铁律 35）

| 闸 | 检查 | 触发点 | 不过的后果 |
|---|---|---|---|
| **闸 1 · 格式闸** | 决策文件：头三行（标题/状态/日期）+ **六段**齐全 + 状态与所在目录交叉校验 | pre-commit + CI | 阻断 |
| **闸 2 · 取代闸** | 新增决策必须输出「同主题候选 + 全/部分取代判定」 | PR 模板 + CI | 阻断 |
| **闸 3 · 入库闸** | **新增 `.md` 必须命中白名单**，否则阻断 | pre-commit + CI | 阻断 |

**闸 3 白名单**：
✅ 入口四份：README.md / AGENTS.md / CLAUDE.md / docs/synova/STATE.md
✅ docs/**/*.md                    （C 层长期）
✅ decisions/**/*.md               （B 层决策）
🔴 **豁免类（P0-1 修复 · 2026-09-26 R2 补全 5 类）**：**文件驱动扩展资产 + 知识资产**
   —— 加了专家/技能/知识 = 新建目录 + 文件就自动注册（见 CLAUDE.md「文件驱动扩展」），
   **这些是产品运行时的资产，不是「过程文档」** ⇒ **必须放行**，否则契约生效即阻断扩展机制。
   | 路径 | 实测 md 数 | 为什么放行 |
   |---|---|---|
   | `expert/**/*.md` | 95 | 专家方法论（文件驱动） |
   | `extensions/skills/**/*.md` | 41 | 扩展技能（文件驱动） |
   | `.claude/skills/**/*.md` · `.dsh/skills/**/*.md` | 34 | **技能本体**（非纯文档；组 13 要求两份一致） |
   | `knowledge/**/*.md` | 10 | 共享知识单源 |
   | `theory/**/*.md` | 5 | 理论基础 |
   | `scripts/golden-scenarios/**/*.md` | 9 | GSS 契约要求 git 跟踪 |
   | **合计** | **194** | —— |

> ⚠️ 上表 7 个数字为 **as_of 2026-09-26 的快照（来源 K3 R2 实测），非判据**。
> 现行值请跑枚举命令（`git ls-files <路径> | grep -c "\.md$"`）。
✅ 证据类路径（P0-2 修复）：**证据 md 一律走 `docs/**/evidence/**`**
   ⇒ 特例：`scripts/golden-scenarios/evidence/**`（GSS 契约要求 git 跟踪，见 .gitignore:82）**放行**
❌ 其余一律阻断（coordination/ · task-briefs/ · reports/ · 派单 · 纪要 · 分类 · 看板 · 探针留档）

---

## 4. 维护责任（谁写、谁维护）

| 层 | 写的人 | 审的人 |
|---|---|---|
| A 入口 | README/STATE = CTO；AGENTS/CLAUDE = CTO + 创始人裁 | 创始人 |
| B 决策 | **做决定的人自己写**（CTO 不代笔） | CTO 审格式 + 取代链 |
| C 长期 | 各域作者 | CTO 审「一主题一份」 |
| D 过程 | 各 session 写进 PR 正文 | PR 审查人 |
| E 生成 | 生成器 | CI 对账 |

---

## 5. 与门禁 / CI 的同批关系（三合一）

| 动作 | 直接效果 |
|---|---|
| D 层停产出过程 md | **砍掉大部分 PR 来源** |
| 闸 3 入库闸 | 从机制上消灭「顺手加一份 md」 |
| 过程走 PR 正文 | **CI 负载下降** |
| 取代链 | 根治「同一决定多份文档」（我们反复重议的根因） |

---

## 6. 验收（可机器核 —— ③④⑥ 按本表定义生效）

```
① 入口四份齐备：README / AGENTS / CLAUDE / STATE（STATE ≤200 行）
② 新增 md 全部命中白名单（闸 3）⇒ 连续两周「新增过程 md = 0」
③ 每个决策含 **六段**（一句话/问题/决定/考虑过的其他方案/后果/取代）
④ 取代链可核：**解析 `## 取代` 段内的相对链接数 = 对账数**（链接存在性由闸 2 验证）
   ⚠️ 不用 `grep '取代'` 计数（会把正文里出现的"取代"二字都算进去 = 坏代理）
⑤ 生成物未入库（`git ls-files` 查不到）且各有 `--check` —— **E 层生成物名单**（哪几份 + 各自生成命令）登记在 `decisions/` 或本节附表
⑥ 新人 30 分钟路径可跑通（§2.1 五步）—— **"不过期"定义**：front-matter `updated:` 距今天数 ≤ 90
   （STATE.md 已有 `updated` 字段；decisions 有 `日期:`）
```

---

## 7. 存量迁移（不返工、不搬家式消耗）

| 存量 | 数量 | 处置 |
| ⚠️ **数量列一律填 `—`** —— 数字**不写进契约**（会过期）；现行值见 `STATE.md` §4 |  |  |
|---|---|---|
| `memory/notes/` | — | **迁 `decisions/`**：先补 `class` 与取代链，再逐步补**六段**；**不重写内容** |
| `.claude/task-briefs/` | — | **立即停产出**；存量分流：含决策 ⇒ 抽成决策文件；不含 ⇒ **出库** |
| `docs/synova/coordination/` | — | 三类分流：长期有效 ⇒ `docs/`；决策性 ⇒ `decisions/`；过程性 ⇒ **出库** |
| `docs/plans/` | — | 同分流 |
| `task-state/` 卡 | — | **保留**（机器可读状态，非文档）；**不再由它派生 md** |
| 归档区（archive） | — | **不再继续归档**（归档只是搬家）⇒ 按白名单判定后**出库** |

> **出库 ≠ 删除历史**：内容永远在 git 历史与 PR 中可查；只是不再参与每次 checkout、每次 CI、每次搜索。

**本契约自身对存量的第一次执行**（取代链示范）：
| 被取代/出库 | 理由 |
|---|---|
| `docs/synova/DOCUMENT-CONVENTIONS.md`（v1.1, 20 行） | 路径描述已失效（写 `docs-synova/`，实际 `docs/synova/`）；被本契约取代 |
| `docs/plans/codex/SYNOVA-哇呢宝贝-文档管理规范-20260805.md` | 第二份文档规范；被本契约取代 |
| `docs/synova/DOCUMENT-INVENTORY.md`（957 行） | 2026-08-14 的清单快照，**中文已乱码**，无未来指导价值 ⇒ **出库** |

---

## 8. 生效与修订

- **当前状态**：**implemented（已生效）** —— K3 复审 R3 判定通过（2026-09-27）
- **生效条件**：**K3 复审通过 + CTO 收件闸**；生效后同批翻 `implemented` 并附**批准凭据**（PR 链接 + 决策文件）
- **批准凭据**（三项，缺一不可）：
  ① **K3 独立审计**：`docs/synova/audit-reports/2026-09-26-K3-DOC-CONTRACT-PR853.md`（R1 不通过）
     + `…-R2.md`（不通过）+ **`…-R3.md`（✅ 通过）**
  ② **CTO 收件闸**：PR #853
  ③ **创始人批准**：2026-09-26 逐项批准（见 PR #853 描述）
- **修订**：本契约的变更走决策流程 —— 在 `decisions/process/` 下写一份新决策，并**在本文件头 `supersedes` 链中登记**
- **执法**：闸 1–3 由门禁治理线实现（见 `docs/synova/coordination/` 下的工程派单）；**CTO 不得豁免本契约**

---

## 9. 与既有门禁的过渡规则（2026-09-26 新增）

**背景**：本契约落地会与**两道既有门禁**冲突，必须显式处理，否则「按契约做事」的提交会被拦住。

| 既有门禁 | 冲突 | 过渡规则 |
|---|---|---|
| **铁律 49 / M7**（commit-msg 要求引用 `memory/notes/**`） | 契约方向是把 `memory/notes/` **迁往 `decisions/`** ⇒ 按契约做事反而被拦 | **契约生效时必须同批改 commit-msg 门禁**：要求从 `memory/notes/**` **扩到 `decisions/**`**（两者皆可）<br>⇒ **落地前**：允许写**指针式 `memory/notes/` note**（只写"见 decisions/…"）作为兼容
   🔴 **过渡期白名单补充（R2 修复）**：`memory/notes/**/*.md` **在过渡期内纳入闸 3 白名单**
   · **退出判据**：`memory/notes/**` 迁移完成（该目录下 md 归零）⇒ 本条自动失效，白名单同步移除
   （否则按 §9 写的指针 note 会被 §3 闸 3 自己拦死 —— 这是 R2 抓出的自相矛盾） |
| **doc-registry-gate**（`scripts/control-tower/doc-registry.json` 要求登记） | 契约的「出库」会删/移文档 ⇒ 注册表不一致 ⇒ 报红<br> | **"出库"动作必须与注册表同步同批做**；契约的存量迁移（§7）开始时，**第一批即同步注册表** |
| **组 13 · 技能同步**（`.claude/skills` ↔ `.dsh/skills` 漂移检查）<br> | 契约若把 skills 纳入"出库/迁移"，**两份会漂移** ⇒ 报红 | **skills 属豁免类（见 §3 闸 3）⇒ 永不出库、不迁移**；任何改动**必须双份同批** |
| **D1 · 文档真相防线**（`check-doc-truth.sh`，C1–C4 导航文档 vs 代码事实）<br> | 契约说"数量不写进定义"，而该防线**要求导航文档与代码事实一致** | **两者不冲突**（契约要求"以事实为准"），但**契约生效时须复核 C1–C4 的判据与 §1 一问是否同一取向** |

**⇒ 一条总原则**（已在多处适用）：**「移出」必与「接手」同批** —— 移走一个机制之前，接手它的机制必须先就位。

**⇒ 这也是契约自身的第一个实战教训**：契约不能只定义"想要的终态"，还必须定义"**从现状到终态的路上，与既有机制如何共存**"。

---

## 10. 审计与证据类产物的归属（S1 · 2026-09-27 生效）

> 来源：CTO 与创始人 2026-09-26 讨论，创始人批准；K3 R2 判"成立，不新增层"。

### 10.1 三种产物分开归属（**五层不动，不新增层**）

| 产物 | 归属 | 位置 |
|---|---|---|
| **① 报告本体**（叙述："我查了什么、发现什么"） | **D 过程（不入库）** | **产生它的独立仓库**（如 K3 独立仓）/ **PR 正文** |
| **② 结论/判决**（一行 + 依据） | **B 决策 或 卡 note** | `task-state/<D>.json` 的 `note`（**一卡一文件，不新增 md**）；无卡则 `decisions/` |
| **③a 证据·小件**（<100KB 文本/json） | **可入库** | `docs/**/evidence/**`，**须配 `--check` 对账** |
| **③b 证据·大件**（截图/长日志/二进制） | **不入库** | CI artifact / 独立仓 **+ tag 锚定** |
| **③c 证据指针** | **B 决策 / 卡 note 的字段** | `sha256 + 位置 + as_of + 怎么重跑`，**不新增文件** |

### 10.2 核心：**证据放「指针」，不放「本体」**

**三条理由**：① 防膨胀（证据动辄 MB–GB）② 可核（sha + 路径 + as_of ⇒ 任何人可重跑比对）
③ **借 DSH 的 `.i18n.yaml` 范式**（它记的是两侧 git blob hash，不是内容）—— **不发明新机制**。

### 10.3 🔴 主仓库唯一红线

**不得新增"报告类 md"**。存量（`docs/synova/audit-reports/`）按 **§7 归档线**出库。

### 10.4 ⚠️ 采纳时的同批动作（K3 R2 前提 2，**最容易漏**）

**必须同批修 `docs/synova/coordination/AUDIT-PROTOCOL.md` 的报告落点**
（它现规定报告落 `docs/synova/audit-reports/`，与本条"不入库"冲突）——
这是本契约 §9「**移出必与接手同批**」原则对自己的一次适用。

**⚠️ 归属**：`AUDIT-PROTOCOL.md` 是**审计标准** ⇒ **CTO 不写审计标准（红线）**
⇒ **本条的同批修改须由 K3 执行**（或创始人授权）；**CTO 只登记，不代改**。

---

## 11. 库外资产规范（2026-09-27 生效）

> 创始人 2026-09-26：**"审计报告不入仓库，我们已经确定的。那么不入仓库的那些文档，如何处理，也要有个规矩。
> 所有 agent、所有人统一规范。这样以后才能完整复盘。同时这个过程，也是我们自己犯过的错、做过的事情，完整的记录。
> **但是不推到主仓库，但是也要规范起来**。"**
>
> **⇒ §10 说"哪些不入库"；§11 说"不入库的怎么管"。两者互补。**

### 11.1 一句话

**不入库 ≠ 不管。** 过程资产要有**统一落点 + 统一命名 + 一份索引**，否则**复盘做不到**。

### 11.2 落点：**独立档案仓**

```
一个独立 git 仓库（与 K3 独立审计仓同级），当前路径：`~/Synova-过程档案/`

为什么是独立仓（四条）：
 ① **有版本控制** ⇒ 可追溯、可 diff（"完整复盘"必须有历史，不能只有一个快照）
 ② **不占主仓库** ✓（创始人硬要求）
 ③ **一个落点** ⇒ 规范简单（"所有库外产物都在这"）
 ④ **可搜**（`git grep`）✓
```

### 11.3 命名（强制）

```
<日期>-<线>-<类型>-<主题>.md

· 日期 = YYYY-MM-DD
· **线 ∈ 固定值集**：cto / k3 / 门禁线 / 积压线 / 文档线 / mac小队 / win侧 / 研究院
· **类型 ∈ 固定值集**：回执 / 交付 / 审计报告 / 通告 / 记录 / 规格 / 证据
· 主题 = 非空，用 `-` 连接，不含空格

例：2026-09-27-k3-审计报告-文档契约-R3.md
```

### 11.4 索引（**"完整复盘"的关键**）

```
档案仓根目录 `INDEX.md`，时间倒序，每行：
  日期 ｜ 线 ｜ 类型 ｜ 主题 ｜ **一句话** ｜ 关联卡/PR

⇒ 复盘时**看一页就够**；"一句话"栏目强制写清"这条记录讲了什么"
```

### 11.5 🔴 复核：**轻复核，不设严格门禁**（创始人 2026-09-26 明令）

**只查规范三行，全过即入**：
```
✅ ① **命名合规**（§11.3 的格式 + 线/类型在固定值集内）
✅ ② **索引同步**（INDEX.md 里有对应一行）
✅ ③ **front-matter 三行**（日期 / 线 / 类型，与文件名一致）
```

**❌ 明确不做（本仓一概不上）**：
```
· ❌ 不跑 CI
· ❌ 不做 lint / tsc / 测试 / 架构检查
· ❌ **不查内容对错**（那是 K3 的事，不是这个仓的事）
· ❌ **不查"该不该入库"**（那是主仓契约的事）
```

**形式（先不上脚本，符合"不臃肿"）**：
```
① **模板兜底**：仓根 `TEMPLATE.md`，新建文件复制它 ⇒ 命名/结构自动对
② **提交时自查**（三行，见上）
③ **抽检**：CTO / 文档线 每月抽检一次
⇒ **只有当"随便命名"真的发生时，才考虑脚本化**（一次生成，非每错加机制）
```

### 11.6 与主仓库的关系

```
· 主仓库**引用**库外档案（如 §10 指向 K3 仓）—— 但**不复制内容**
· 主仓库的**卡 note / PR 正文**保留"**指针**"（指向档案仓的具体路径）
⇒ **两边都能查到，但内容只有一份**
```

**收件闸规则（2026-09-27 加，来源 K3 R4 的 P1-B，零脚本）**：
> **K3 通过后新增的章节，须注明「入下一轮复审」，或先审后翻 `implemented`。**
> 历史：§10/§11 是在 R3 通过之后追加的，而 `implemented` 在同一轮 R4 之前翻 ——
> 对新增章节构成「事后补票」。⇒ 本条防同类。
