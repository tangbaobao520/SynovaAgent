# D331 审计报告 — D329 审计 P1 修复（tag 重指 + bypass 对账 + 接线落地）

> 审计员: Kimi Code CLI（独立架构审计员，零上下文，只读取证）
> 审计对象: commits `45a9220`（主修复，22:54）+ `a93d99a`（bypass 记录携带，23:00）+ `7ace35e`（V4.7.3 补序 + 补记，23:37）
> 审计日期: 2026-08-13（本地 +08）| 协议: KIMI-AUDIT-INSTRUCTION v1.0
> 用户特别要求: "一定要认真读代码"——本报告全部结论基于逐行代码审读 + 亲自运行测试 + 独立实验复验。

## 审计材料确认表（7 项）

| # | 材料 | 状态 | 证据 |
|---|------|------|------|
| 1 | 任务提交集 | ✅ | `git log --all --grep=D331` → 3 提交全部在分支链上（7ace35e→a93d99a→45a9220→407ff1f），无孤儿。注：45a9220 与 a93d99a 同名（后者是 bypass.log 记录携带提交，+1 行） |
| 2 | Git diff | ✅ | 45a9220：8 文件 +772/−14；a93d99a：bypass.log +1；7ace35e：bypass.log +3 + VERSION.md 补序（12+/12−） |
| 3 | Dev doc | ✅ | `SYNOVA-IMPL-D331-D329审计P1修复-20260812.md`（196 行；§3.1 写集 7 改+2 新+1 操作；§6 **DS1-DS13**） |
| 4 | Task brief | ⚠️ | `.claude/task-briefs/D331-tag-bypass-wiring-fix.md` 存在于磁盘但**未跟踪**（`??`；2026-08-12-auto.md 同）——D330 曾把 brief 入仓，本次又回到不入仓 |
| 5 | AGENTS.md | ✅ | 项目根 |
| 6 | 审计基线 | ✅ | `audit-check.py --full`（HEAD=7ace35e）→ `PASS:3 WARN:886 FAIL:439`、as any=0（零 .ts 变更，物理不变）——与 DS10 一致 |
| 7 | 执行证据包 | ✅ **首次完整** | bypass.log：D331 8×BLOCKED + 3×COMMITTED PASS（HASH 与三提交逐一吻合）+ 历史 4 提交（ea1cb71/dc369fd/6c00e46/407ff1f）**诚实补记**（标注"一次性补记"及原因）；pre-push hook 接线确认；CI：gh 401 + 未推送（DS12 用户诚实 ⏸） |

## 14 项逐项判定表

| # | 项 | 判定 | 证据 (file:line) |
|---|-----|------|------------------|
| 1 | 接口真实性 | [PASS] | `check_tag_ancestry()` 存在于 scripts/pre-push-check.sh（:66-92 区域，全 tag `merge-base --is-ancestor` 循环 + VERSION.md 最新版本锚点校验）；check-bypass-log.sh:1-62 新建（BASE..HEAD × bypass.log HASH 差集）；synova-commit:364-395 guard PYBIN+探测+rc 三态；staging_guard.py:70-75 `--session` 生产传递；session_registry.py:258-280 write_set 携带 task_id（`s.get("task_id")` 继承）。dev doc §2 对修复前状态的引用与我 D329/D330 审计时的现场逐一吻合 |
| 2 | 文件路径存在性 | [PASS w/ P2] | 写集路径全部存在。**例外**：交付声明称"经验教训已写入 memory（2026-08-12-D331-kimi-k3-audit-fix.md）"——`find memory/ docs/lessons .claude -name "*D331*"` 物理确认**该文件不存在** → P2-3 |
| 3 | Edge ID 有效性 | [SKIP] | 无 `E-XX` 模式 |
| 4 | 架构边界 | [PASS] | 纯 shell/python 基础设施；新增 import 仅 stdlib |
| 5 | 数据流完整性 | [PASS] | pre-push → 门禁 6（tag 一致性 D319 + 祖先 D331）→ 门禁 7（check-bypass-log.sh）→ 硬阻断出口，逐环物理存在；synova-commit → PYBIN 探测 → guard 执行 → rc 三态分流（block JSON / 崩溃 degraded / 正常）；guard → resolver `--session`。**live 验证**：在真实仓库跑 `bash scripts/control-tower/check-bypass-log.sh` → exit 0，"origin..HEAD 全部提交有记录"（7 个未推送提交全覆盖） |
| 6 | 接线深度 | [PASS w/ carried P2] | tag-祖先同时接入 SYNO_TAG_ONLY 测试注入与主流程（pre-push-check.sh:95-100 / :241-260）；bypass 对账为门禁 7 硬阻断；`--session` 生产调用点 = staging_guard.py:74（grep ≥1 ✓，dev doc 已诚实注明 synova-commit 无 resolver 调用）。carried：tag-bypass-wiring.test.sh 仍无 runner 执行引用（仅出现在 pre-push-check.sh:95 注释中）→ P2-4 |
| 7 | 降级诚实性 | [PASS] | guard 崩溃 → 显式 degraded + 输出前 3 行（synova-commit:380-385）；python 全不可用 → 显式跳过提示（:390-391）；check-bypass-log base 不可解析 → 显式 fail-open 提示（:41-43），显式 SYNO_BASE_REF 错误 → 硬错（:35-38）——正常/异常语义区分正确；merge-base 调用有 swallow-ok 注释（if 条件消费 rc） |
| 8 | 测试契约 | [PASS w/ P2] | 11 用例标题与断言一致性抽查通过（用例 6/7/8 标题"exit 2/降级放行"= 断言行为）。**但交付声明"25/25"与实测"24/24"不符**（结果行物理输出：24 通过 0 失败）→ P2-1 |
| 9 | 测试覆盖 | [PASS w/ 注记] | **亲自运行 24/24 全绿**（TEST_EXIT=0）；回归：D329 staging-guard-session 10/10 ✓、D328 commit-msg-consistency 13/13 ✓。覆盖：孤儿 tag/版本 tag 非祖先/对账缺失/齐全/base 三态边界/guard PYBIN 回退（shim marker 物理证明执行）/guard 崩溃显式 degraded/全无 python/--session grep 接线/task_id 写入/同任务互认。注记：dev doc §4 表的两个 **resolver 用例（broken-python3 回退仍拦截、退出码 0/1/2 语义）未出现在测试文件中**——并入 P1-1 |
| 10 | 文件驱动 | [PASS] | 无硬编码业务类型 |
| 11 | 自我报告交叉对比 | [CONDITIONAL] | DS1 ✓ 但数字失真（24/24≠25/25，P2-1）；DS2 ✓（V4.7.1→dc369fd ON_BRANCH 独立复核）；DS3-5 ✓（pre-push 门禁 6/7 物理存在且 live 对账 exit 0；guard PYBIN 代码+测试双证）；DS6 ✓（staging_guard.py:74）；DS7 ✓（task_id 字段+测试 10 断言 `task_id=D331`）；DS8 ✓（D329 dev doc §5 升级块物理存在，含"测试调用不计入"的 WIRE CHECK 升级原文）；DS9 ✓（VERSION.md V4.7.3 置顶 + version.log 4.7.3 双条目 + tag ON_BRANCH）；DS10 ✓（439/0 实测）；DS11 ✓ **首次有完整物理证据**（3 条 COMMITTED PASS hash 吻合 + 历史补记）；DS12 ⏸ 诚实标注（verify-parallel 门禁 5 存在且接线，拦截叙事与机制一致）；**DS13（dev doc 存在：resolver PYBIN 探测 + 退出码 0/1/2）未交付且交付声明通篇未提** → P1-1；memory 写入声称不实 → P2-3 |
| 12 | dev doc 偏离审计 | **[FAIL → P1]** | §3.1 写集 10 项中 9 项交付；**第 5 行 resolve-commit-brief.sh（①PYBIN 可用性探测 ②退出码 0/1/2 语义化）零交付**——`git diff dc369fd..HEAD -- scripts/workflow/resolve-commit-brief.sh` 物理为空，resolver 当前 :48 仍裸 `command -v`、:187 仍 exit 1 二义。§4 两个 resolver 用例未进测试文件。§6 DS13 在交付声明中消失（用户 DS 列表止于 DS12），无任何 descope 记录。附带：dev doc §3.1 synova-commit 行"②resolver 调用传 --session"前提错误（D329 后 synova-commit 已无 resolver 调用）——D329 dev doc 回填块已诚实注明此点，D331 dev doc 自身未同步 |
| 13 | 控制塔执行审计 | [PASS] | **本任务首次证据链完整**：8×BLOCKED（17:49-22:36，门禁真实拦截）→ 3×COMMITTED PASS（22:58/23:03/23:40，hash 逐一吻合）；历史 4 提交补记且标注"一次性补记+原因"（诚实标注，未伪装成实时记录）；pre-push 门禁 5（verify-parallel）/6/7 接线确认。观察项：7ace35e 自身 COMMITTED 记录在工作区未提交（记录携带递推——每个提交的记录搭下一个提交；机制可工作，末条记录依赖后续提交入仓） |
| 14 | 版本编排合规 | [PASS] | **三处同步首次全净**：V4.7.3→7ace35e、V4.7.2→407ff1f、V4.7.1→dc369fd（重指生效）全部 ON_BRANCH；VERSION.md 置顶 V4.7.3（含 D330 委托补序说明）；version.log 4.7.3 bump（17:26）+ auto-tag（23:39）双条目。D330 审计的 V4.7.3 幻影风险随 D331 代码落地解除 |

## 分级汇总

### P0
无。三项核心修复（tag-祖先校验 / bypass 对账 / guard PYBIN 三态）全部代码级验证 + 测试证明 + live 复验；历史证据链诚实补齐；tag 编排全净。

### P1（必须跟进）
- **P1-1 DS13 静默消失（dev doc 范围裁减无记录）**：dev doc §2.6 缺陷 F（标 P1）/ §3.1 写集第 5 行 / §4 两个测试用例 / §6 DS13——resolver PYBIN 可用性探测 + 退出码 0/1/2 语义化**零交付**，交付声明只字未提（DS 列表止于 DS12，给人"本地交付完整"印象）。直接后果：我 D330 审计报告的两个残留问题**仍然开放**——①broken-python3 + 可用 python 环境下 resolver 先于 GENUINE 死亡，一致性门禁仍不拦截（仅 degraded 提示）；②无 brief 正常提交仍被误标 "degraded: 解析失败"（噪音）。要么补做，要么在 dev doc/交付声明中显式 descope 并说明理由。

### P2（可选改进）
- **P2-1** 测试计数声称 25/25，实测 24/24（数字不来自实际运行输出）。
- **P2-2** D331 brief 未跟踪入仓（D330 已建立入仓惯例，本次回退）。
- **P2-3** "经验教训已写入 memory"声称不实——memory/2026-08-12-D331-kimi-k3-audit-fix.md 不存在（D330 的 brief 也曾引用两个不存在的 memory 文件——同类公式化声称第二次出现）。
- **P2-4**（carried）三个控制塔测试文件（commit-msg-consistency / staging-guard-session / tag-bypass-wiring）均无 runner 接线，只能手动跑。

## 总体结论: **CONDITIONAL PASS**

我 D329 审计的 P1/P2 修复项逐行验证通过：tag-祖先校验（pre-push 硬阻断）、bypass.log 对账（脚本 + 门禁 7 + 历史 4 提交诚实补记）、guard PYBIN 三态、--session 生产接线、write-set task_id、D329 dev doc §5 WIRE CHECK 升级——**前四次审计提出的三条 L4 防线缺口（tag 锚点 / 证据对账 / 接线验收升级）本次全部机制化落地**，且执行证据链首次完整。唯一的 P1 是 DS13：dev doc 承诺的 resolver 硬化被静默裁减——这恰好踩在我 D330 报告 L4#2 指出的"声称-覆盖映射无对账"缺口上（同构复发）。

## 运行环境注记

Windows（D: 盘）+ Git Bash（MSYS）+ Python 3.13.13。三个测试套件亲自运行（24/24、10/10、13/13）；check-bypass-log.sh 在真实仓库 live 运行（exit 0）；tag 祖先关系经 `git merge-base --is-ancestor` 逐一物理确认。工作区含未提交改动（bypass.log 末条记录等，不涉 D331 写集语义）。

## L4 防线缺口收割

> "本次发现的问题（若有），控制塔哪一道防线本该拦住？为什么没拦住？缺什么？"

### 发现 1: dev doc DS13 在交付声明中静默消失——"dev doc 完成标准集合 ⊆ 交付声明集合"无对账方

**本该拦住的防线**: dev doc §6 完成标准 + 交付声明的逐项呼应（AUDIT-PROTOCOL 的"声称 vs 证据"精神）。
**为什么没拦住**: D330 审计 L4#2 已指出"声称覆盖项 ↔ 实际交付"缺映射检查，建议未机制化；本次同构复发——dev doc 13 条 DS，交付声明 12 条，编号错位（交付方自行重编号 DS1-DS12）使缺失在视觉上隐形。没有任何门禁/脚本检查"交付声明必须逐项覆盖 dev doc 的 DS 编号并标注状态（✅/⏸/❌ + 原因）"。
**缺什么**: 交付声明模板的物理约束——DS 编号必须与 dev doc 一一对应，禁止重编号；缺项必须显式 descope（写理由），否则 pre-commit/审计对照表直接红。

### 发现 2: 交付声明的测试数字（25/25）不来自实际运行输出（24/24）

**本该拦住的防线**: 铁律 48 + Agent 自检（测试质量项）。
**为什么没拦住**: 数字凭设计意图或记忆填写（grep 显示测试文件有 27 处 pass/assert 字样，实际执行路径 24 条断言），而非粘贴自当次运行结果。自检只查"有没有跑、绿不绿"，不查"报的数字对不对"。
**缺什么**: 交付声明规则——测试计数必须从当次运行输出原样复制（含"结果: X 通过 / Y 失败"行），不允许手写。审计侧 grep 运行输出即可一秒证伪。

### 发现 3: "经验教训已写入 memory"连续两个任务物理不实（D330 引用不存在文件、D331 声称写入但不存在）

**本该拦住的防线**: 交付声明的诚实性要求（铁律：自我报告必须可被物理验证）。
**为什么没拦住**: memory 写入是"软声称"——没有任何环节验证文件真实存在。这类公式化声称（"教训已固化"）在交付文本中成本为零、 auditors 不查就永远成立。
**缺什么**: 一句话物理检查——交付声明引用任何文件路径时，`[ -f ]` 验证；建议并入交付声明模板或审计 item 2 的常设检查（本次起已入我的常设动作）。

---

*审计完毕。全部结论基于文中给出的可复现命令；任何条目可独立重验。*
