# 独立审计报告 — DSH-D356 哨兵阈值告警接线 + 降级误报修复

> 审计员: Kimi K3（独立工作区，零上下文） | 日期: 2026-08-16
> 提交: `6db5a17a` `fix(sentinel): 阈值告警接线 + 降级误报修复 (D356)`（分支 feat/d356-sentinel-threshold-alert，单提交，写集 6 文件全部映射到此提交）
> 材料: dev doc `SYNOVA-IMPL-DSH-D356-sentinel-threshold-alert-20260816.md` + brief `.claude/task-briefs/D356-sentinel-threshold-alert.md` + AGENTS.md + .claude/PRODUCT-BRIEF.md + 基线两次实测 + bypass.log

## 结论: **CONDITIONAL PASS**

代码本体（L1）与偏离（L2）全项通过，三处 K3 证明的缺陷修复均物理复现：测试 11/11 全绿、三处 `!degraded` 守卫、三哨兵 fail-closed、loader 注入全部命中。推送状态已物理证实（见 DS10）。**唯一残留缺口是 CI**：本审计环境 gh CLI 不存在、GitHub API 无凭据，CI 逐 job 结论无法物理验证（声称无据）。创始人 CI 页面逐 job 核对后转 PASS。

> 2026-08-16 补充（报告提交前）：网络恢复后 `git ls-remote origin feat/d356-sentinel-threshold-alert` = `6db5a17a` = 本地 HEAD → **推送已物理证实**；远端 main=`ef32a989`（PR #30 D363 已合并），D356 尚未合并入 main（PR 开放中）。

---

## 一、15 项语义审计清单

| # | 项 | 判定 | 证据 |
|---|-----|:---:|------|
| 1 | 接口真实性 | ✅ | `injectSentinelManifest` 真实存在 `src/sentinel/sentinel-loader.ts:128-134`；调用点 `:161` |
| 2 | 文件路径存在性 | ✅ | 写集 6 文件全部存在（测试 import 成功运行物理证明） |
| 3 | Edge ID 有效性 | N/A | 本任务无 E-XX 边定义 |
| 4 | 架构边界 | ✅ | diff 无新增跨层 import；修改均在 L3 洞察层（src/sentinel + extensions/sentinels） |
| 5 | 数据流完整性 | ✅ | dev doc 链：loader 挂 manifest（loader:161）→ aggregate 阈值判断（cash-runway:28-55）→ degraded 拦截（:31/:39/:46）物理一致 |
| 6 | 接线深度 | ✅ | `injectSentinelManifest` 被 `registerLoadedSentinels():161` 真实调用（非仅 import）；`registerLoadedSentinels` 生产调用方 2 处：`src/init/file-driven-loaders.ts:73`、`src/deploy/bootstrap.ts:376`（grep 实测，非测试） |
| 7 | 降级诚实性 | ✅ | capital-\* missing 检查均有 `log.warn(...关键字段缺失 — degraded...)` + `return []`；测试运行日志物理显示三条 warn 真实输出；cash-runway catch 有 `log.error`（:80） |
| 8 | 测试契约 | ✅ | `injectSentinelManifest` JSDoc 契约（输入 unknown/输出 boolean/降级不抛）与实现一致，function 输入用例验证不抛且返回 false |
| 9 | 测试覆盖 | ✅ | 11 用例全 expect：正常 3 + 降级 6 + 边界 2（function 输入、空 financials）+ 回归 1（revenue-health），非空壳（铁律 48） |
| 10 | 文件驱动 | ✅ | 无新增硬编码类型/manifest/tags；阈值契约 manifest.json 冻结未改（§3.3 遵守） |
| 11 | 自我报告交叉对比 | ✅ | dev doc §2.4 调用方行号 73/376 与实测一致；守卫行号声称 :30/:38/:45 实测 :31/:39/:46（注释行偏移，实质一致，见 P2-4） |
| 12 | dev doc 偏离审计 | ✅ | §3.2 三决策（B loader 注入 / B aggregate 检查 / A 只改 cash-runway）与实现逐行一致；写集偏离见下 DS9 |
| 13 | 控制塔执行审计 | ⚠️ | pre-commit PASS 机器证据：bypass.log `2026-08-16T10:52:07 \| COMMITTED \| pre-commit PASS \| TASK_ID=D356 \| HASH=6db5a17a`；无 --no-verify；**CI job 级证据缺失（环境不可达）** |
| 14 | 版本编排合规 | ✅ | 提交未触碰 VERSION.md / version.log / git tag（提交 7 文件清单物理证明） |
| 15 | 并行合规 | ✅ | session-registry 有 D356 登记（CP4，写集认领 7 文件全部 status=committed）；parallel-conflicts.log 无 D356 记录；同期 .wt-d379 写集不重叠 |

## 二、DS 逐条（L2，禁重编号）

| DS | 判定 | 证据 |
|----|:---:|------|
| DS1 测试全绿 ≥9 | ✅ | 复现 `npx vitest run tests/sentinel/sentinel-threshold-alert.test.ts`：**11/11 passed**（210ms） |
| DS2 loader 注入 | ✅ | `grep -c "\.manifest = manifest" src/sentinel/sentinel-loader.ts` = 1（:130） |
| DS3 三处守卫 | ✅ | `cash-runway/aggregate.ts:31/:39/:46`（!runwayResult.degraded ×2 + !overdueResult.degraded ×1） |
| DS4 capital-structure | ✅ | `=== undefined` ×2（:23-25 missing 检查）；缺 totalDebt 用例返回 [] 复现 |
| DS5 capital-turnover | ✅ | `=== undefined` ×2（revenue/totalAssets/accountsReceivable 检查） |
| DS6 capital-efficiency | ✅ | `=== undefined` ×2（revenue/totalDebt/equity 检查） |
| DS7 生产调用方 2 处 | ✅ | file-driven-loaders.ts:73 + bootstrap.ts:376（grep 实测） |
| DS8 as any=0 + tsc 零新增 | ✅ | 代码级 as any=0（朴素 grep 命中 1 为 JSDoc 注释字样，见 P2-1）；tsc 前后对比：父提交与 HEAD 均同样 6 行预存 TS2307（_extinct 模块解析），diff 为空 → **零新增** |
| DS9 基线一致 + 无 bypass + diff=写集 | ✅ | 基线 FAIL 父 434 = HEAD 434 零新增；WARN 884→885（+1 = 新 export 形式性 unwired，见 P2-2）；bypass.log 无 --no-verify；提交文件集 = 写集 6 + task brief（registry 认领同样 7 文件，D375/D374 同惯例） |
| DS10 推送 + CI job 级 | ⚠️ 半 | **推送 ✅**：`git ls-remote origin feat/d356-sentinel-threshold-alert` = `6db5a17a` = 本地 HEAD（物理证实）；**CI ❌ 声称无据**：gh CLI 不存在、API 无凭据，逐 job 结论无法验证 |
| DS11 决策记录 | ✅ | dev doc §4.5 三决策点双参考系（Anthropic + 第一性原理）完整；brief Q0c 同步 |

## 三、发现清单

### P1（1 项，记录跟进）

**P1-1 DS10 执行证据链断裂（残留：CI 无法物理验证）**
- 事实：审计时 `gh: command not found`、网络一度超时；报告提交前网络恢复，`git ls-remote` 物理证实推送完成（远端分支 = `6db5a17a` = 本地 HEAD）。CI 逐 job 结论仍无法验证（无 gh / API 无凭据）。
- 影响：推送无缺口；CI 绿否只能标"声称无据"。
- 归因: **control-tower**（CI 证据采集依赖外网 + gh CLI，无本地降级证据通道；命中 LEDGER M4 执行证据链断裂，强化该类已有防线）
- 处置：创始人 CI 页面逐 job 核对后，本项转 ✅，审计结论转 PASS。

### P2（4 项，可选改进）

**P2-1 brief 验收命令含注释误命中** — Done 标准 `grep -c "as any" src/sentinel/sentinel-loader.ts == 0` 实测输出 1，命中的是本次新增 JSDoc 注释 `非 as any（铁律 38）`（sentinel-loader.ts:126），非代码。代码级 as any = 0 合规。归因: **devdoc**（验收命令未排除注释，物理不可复现"==0"；命中 M2 边缘，建议验收命令统一 `grep -v '^\s*\*' ` 过滤注释）。

**P2-2 基线 unwired 检查同文件调用盲区** — 新 export `injectSentinelManifest` 被标 `no callers in src/`（WARN+1），实际被同文件 :161 真实调用 + 测试 import。基线脚本不识别同文件调用。归因: **control-tower**（audit-check.py 检查盲区，命中 M3 边缘；建议 unwired 检查纳入同文件调用）。

**P2-3 cash-runway 预存不一致（非本次引入）** — `aggregate.ts:36` `suggestion: runwayResult.degraded ? ... : ...` 在守卫后成死逻辑；`:57` constraintResult 缺 `!degraded` 守卫（同文件 replenish :68 有，不一致）。均在 D356 写集外，测试已证无害。归因: **implement**（建议后续任务顺带清理）。

**P2-4 dev doc 行号未回填** — §2.2/DS3 声称守卫行号 :30/:38/:45，实测 :31/:39/:46（P1-1 注释行插入偏移）。归因: **devdoc**（M7 轻微漂移，建议实现后同 commit 回填行号）。

## 四、跑偏第二道（PRODUCT-BRIEF 原文三问）

1. **服务真实用户场景？** ✅ PRODUCT-BRIEF §三.2「哨兵定时巡检：25 个哨兵 7×24 自动跑 → 发现异常 → 对比基线 → 信号路由给专家 → 严重信号自动建工单」——本任务让"发现异常"的阈值告警从死代码变真实、让误报不再骚扰企业主。服务对象 = 企业主（收真实告警）+ FDE（部署后自主看信号）。
2. **更接近终态？** ✅ §六 P0「哨兵真实数据流」的前置：告警链路先不死代码、不误报，真实数据流入后才可用。无横向添加（§3.3 六项不做全部遵守：compute filter bug 归 D355、去桥接归 D358、manifest 冻结）。
3. **变味？** ✅ 修复后 Synova 仍是"驻扎企业、主动诊断、为增长服务"的 Agent——告警诚实（有数据正确告警、无数据不误报）正是"驻扎企业"可信度的根基。

**判定：不跑偏。**

## 五、L4 防线缺口收割

> 本次无 P0 问题。P1-1 暴露的防线缺口：

| 缺口 | 本该拦住的防线 | 为什么没拦住 | 缺什么 | 模式归类 |
|------|---------------|-------------|--------|---------|
| 推送/CI 证据审计时不可达 | L3 执行审计（DS10 推送 + CI 验证） | 证据采集依赖外网（git ls-remote）+ gh CLI；审计/编码环境无 gh 或网络隔离时即失明 | post-push hook 本地落盘推送证据（hash + remote + 时间戳入 .claude/push-evidence.log）；CI 结论回写仓库文件（如 .codex/ci/<branch>.json 由 CI 或创始人回写） | **M4**（已有，强化：执行证据链加"本地降级通道"，不加新 M 类） |
| 验收命令含注释误命中（P2-1） | brief 验收命令物理门禁 | 命令设计时未考虑注释含关键字 | devdoc 规则：验收 grep 命令必须过滤注释行后物理复现 | **M2**（已有，强化 devdoc 验收命令规范） |
| 基线 unwired 同文件盲区（P2-2） | audit-check.py 接线检查 | 脚本只查跨文件 import | unwired 检查纳入同文件调用识别 | **M3**（已有，强化检查脚本） |

原则遵守：三类缺口全部命中已有 M 类（M4/M2/M3），只强化已有防线，不新增 M9。

## 六、运行环境注记

- 审计机：macOS（wanedeMacBook-Pro.local），Node v24.19.0，vitest 4.1.8
- 复现记录：测试 11/11 全绿（含 capital-\* 三条 fail-closed warn 日志物理输出）；tsc 前后对比基于 git worktree 父提交（5e07c578）+ symlink node_modules；基线两次全量实测（父 434/884 vs HEAD 434/885）
- 环境限制：gh CLI 缺失；git ls-remote 网络超时（GitHub 不可达）→ DS10 无法物理验证
- 沙箱注记：vitest 需写 node_modules/.vite-temp，默认工作区沙箱拦截，提权后复现成功——此为环境依赖，非被测代码问题

## 七、证据索引

| 声称 | 证据 |
|------|------|
| 提交集 | `git log --all -- <6 写集文件>` 全部唯一映射 6db5a17a |
| 测试 | vitest run 输出：Test Files 1 passed / Tests 11 passed |
| 注入 | sentinel-loader.ts:128-134（函数）:161（调用）:130（赋值） |
| 守卫 | cash-runway/aggregate.ts:31/:39/:46 |
| fail-closed | capital-structure:23-29 / capital-turnover:18-24 / capital-efficiency:39-45 |
| 生产接线 | file-driven-loaders.ts:73 + bootstrap.ts:376 |
| pre-commit | bypass.log `2026-08-16T10:52:07+08:00 \| COMMITTED \| pre-commit PASS \| TASK_ID=D356 \| HASH=6db5a17a` |
| 并行 | session-registry.json D356 条目（CP4，7 文件 committed）；parallel-conflicts.log 无记录 |
| 基线 | 父 434 FAIL/884 WARN vs HEAD 434 FAIL/885 WARN，集合 diff 仅新 export unwired 1 行 |
