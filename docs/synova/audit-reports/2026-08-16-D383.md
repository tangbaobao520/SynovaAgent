# K3 独立审计报告 — D383

> 审计员: Kimi K3（独立会话，零上下文）
> 审计对象: `98b0d4d1` — docs(D383): CTO 2026-08-16 batch（哨兵口径/派活 D379/380/B1/D381 固化/D382 状态机/台账拆分）
> 审计日期: 2026-08-16
> 运行环境: macOS（产品仓库 /Users/wane/SynovaAgent，分支 feat/d383-cto-batch；审计工作区 /Users/wane/Synova-k3独立审计）
> 环境注记: 审计工作区沙箱只读产品仓库（写 CTO-HEALTH.md 实测 PermissionError）——所有写操作在审计工作区完成；gen-cto-health.py 非 dry-run 崩溃为**沙箱权限所致（环境依赖），非脚本缺陷**；dry-run 纯计算验证通过。

---

## 一、结论

**CONDITIONAL PASS**（无 P0；P1×4 记录跟进 + 执行证据疑点需创始人核）

- 提交 98b0d4d1 已推 `ssh/feat/d383-cto-batch`（推送属实），未合 main（分支工作流符合铁律 34/0-3）
- 控制塔核心变更 C6 门禁实测生效（fail-open 已堵）：新测试 9/9 + 既有 5/5 全绿（物理复跑）
- 审计基线 434 FAIL 与 D356 审计一致，未恶化（scripts/audit/audit-check.py --full 实测）
- 哨兵口径 45+4=49 属实（extensions/sentinels 顶层 45 目录 + 4 适配器；path-dependency 空壳实锤）
- **P1 发现集中在"声称 vs 提交实际"**：写集漂移（25 声称 vs 23 实际）、D382 编号撞车、CTO-HEALTH 产物数据无源 + D383 无 bypass.log 记录（M4 复发）、幂等声称不实

---

## 二、14 项逐项表

| # | 项 | 判定 | 证据 |
|---|----|------|------|
| 1 | 接口真实性 | ✅ | gen-cto-health.py dry-run 全链路可跑（parse/analyze/render 正常）；C6 实测生效 |
| 2 | 文件路径存在性 | ✅ | 提交 23 文件全部存在；devdoc_writeset.py / dev-doc-gatekeeper.sh / task-state/* 均在 |
| 3 | Edge ID 有效性 | — | 本批无 E-XX 文档（控制塔/文档批次，不适用） |
| 4 | 架构边界 | ✅ | 仅 scripts/control-tower + docs + .claude/.dsh skill + task-state，无跨层问题 |
| 5 | 数据流完整性 | ⚠️ | gen-cto-health.py 数据流（bypass.log→解析→渲染）dry-run 通；但**产物 CTO-HEALTH 与提交内数据源不一致**（见 P1-3） |
| 6 | 接线深度 | ✅/⚠️ | C6 接线确认（gatekeeper.sh:203 调 devdoc_writeset.py）；gen-cto-health.py 无自动调用方（手动工具，P2） |
| 7 | 降级诚实性 | ✅ | read_binary_safe 读失败标注；C6 python 不可用→check_fail（fail-closed，不降级放行） |
| 8 | 测试契约 | ✅/⚠️ | C6 测试 9/9 覆盖正常/降级/边界；既有 5/5；**gen-cto-health.py 329 行零测试**（P1-4） |
| 9 | 测试覆盖 | ✅ | writeset-c6-gatekeeper.test.sh 五场景（空行/无空行/有表 PASS/无表 FAIL/空表边界）实测全绿 |
| 10 | 文件驱动 | ✅ | task-state/*.json 文件驱动；无硬编码类型问题 |
| 11 | 自我报告交叉对比 | ❌ | 台账"9/9+5/5 全绿"属实；但"幂等""25 文件""dev-doc spec 入库""bypass.log 有记录"四声称不实（P1-1/3/4） |
| 12 | dev doc 偏离审计 | ⚠️ | 写集 25 vs 实际 23（漂移 2 文件）；C6 实现 vs 台账描述一致 ✅ |
| 13 | 控制塔执行审计 | ⚠️ | pre-commit 时间戳(11:44:59)晚于提交(11:42:41)；D383 提交无 bypass.log 记录；CTO-HEALTH 数据无源 |
| 14 | 版本编排合规 | ✅ | 纯 docs 批次，无 VERSION.md / tag 变更；不适用项 |

---

## 三、L1 代码审计（物理验证）

### 3.1 C6 写集表门禁（D381）— 通过
- `dev-doc-gatekeeper.sh` 新增 C6（L183-222）：python 可用性探测（D330 教训：探测后试运行）→ `devdoc_writeset.py --extract` 失败/无写集表 → `check_fail`（exit 1 阻断），**fail-open 已堵**（原 check-dev-doc-write-set.sh 无写集表 SKIP+exit 0）
- `devdoc_writeset.py` 空行容忍修复（L52-62）：表格后空行不重置状态；新写集标题继续表格
- 实测：`bash tests/control-tower/writeset-c6-gatekeeper.test.sh` → **PASS=9 FAIL=0**；`python3 tests/control-tower/test-dev-doc-gatekeeper.py` → **5 tests OK**；`bash -n` 语法 OK
- 真实 dev doc 回归：D356 spec 提取 6 条目 / D379 spec 提取 2 条目，均 status ok（不误拦）
- 与台账 §五 D381 记录一致 ✅

### 3.2 gen-cto-health.py（新脚本 329 行）— 通过但有两缺陷
- **P1-4a 幂等声称不实**（gen-cto-health.py:22 声称"幂等: 自动区无变化 -> 不写文件"）：render() 内嵌 `datetime.now()` 时间戳（L211）且 full 头部也嵌（L316）→ 两次连续运行输出必然不同（实测 dry-run 两次仅差时间戳）→ L320 `OUT.read_text() == full` 恒 False → **每次运行都重写文件**。声明与实现不符（M2 类）。
- **P1-4b 零测试**：329 行新脚本无任何测试文件；幂等缺陷本可被"两次运行比较"测试拦截。
- 解析逻辑本身正确：m_recur 解析出 M1/M2/M4/M5（与台账 §二 吻合，M3/M6/M7/M8 无复发正确排除）；CT 队列 2 done/25 wip/7 todo；verdict 🟡 与提交内 CTO-HEALTH 一致。

### 3.3 task-state 状态机（D382）— 结构通过，两处契约/交付问题
- README 状态机、TEMPLATE、D356/D379 示范、gen-cto-health §五 汇总、persona ⑧/⑦b 规则、cto-handover §八b 铁律——落位自查表 5/5 [x]，阶段 2/3 诚实标注未做 ✅
- **P2-1**：`task-state/D383.json` 未提交（untracked）——审计登记在物理上不落库；且 files 字段为数字 `23` 而非 README 契约的 `files[]` 数组（README L29）
- **P1-2 D382 编号撞车**：CTO 用 D382 指"task-state 状态机"（README L1"（D382 设计）"、commit message"task-state D382"、skill §八b"（D382）"），而 dev-doc 线用 D382 指"文档提交豁免"（`SYNOVA-IMPL-DSH-D382-doc-commit-exempt-20260816.md`，CT-34，front-matter 自称 D382）——**同一 D# 指两个不同任务**。台账 D339 已有编号冲突教训（"D339 编号被 quotepath 修复占用"），本轮重演。

---

## 四、L2 偏离审计（声称 vs 提交实际）

### P1-1 写集漂移：brief 声称 25 文件，实际提交 23 文件（核心发现）
- brief `2026-08-16-D383-cto-batch.md` Q2 列出 25 文件（L36-61），实际 `git show 98b0d4d1 --stat` 23 文件
- **缺失 3 项（全部未提交，工作区可见）**：
  1. `docs/synova/coordination/TASK-ROUTING.md` — `git status` 显示 ` M`（未提交修改）：D379/D356 哨兵切片派活登记（"src/sentinel/ 进行中·DSH…D379 空壳补实现 进行中，D356 spec 已交付待实现"）。**派活登记未落库 = 其他机器/角色物理上看不到认领状态**（D315 教训：声称批量交付但文件未提交——重演）
  2. `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D356-sentinel-threshold-alert-20260816.md` — untracked。brief 声称"dev-doc spec 入库"（L16），**未入 98b0d4d1**。该文件在 dev-doc 分支 `feat/dsh-devdoc-spec`（bc552aae）存在但未合 main、未入库
  3. `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D379-path-dependency-shell-20260816.md` — 同上
- 附带：`SYNOVA-IMPL-DSH-D382-doc-commit-exempt-20260816.md`（CT-34 dev doc）与 `task-state/D383.json` 也 untracked
- brief Done 标准 1（"git show --stat HEAD 恰好本轮 25 文件"）**未达成（23）**
- 排除项（不做什么）验证：golden-scenarios/README.md、D361-gss-common.md、thresholds.json×2 均未出现在 98b0d4d1 ✅
- 交叉印证：提交内 CTO-HEALTH.md MANUAL 区 CTO 自认"工作区 ~335 文件未提交（含前任遗留 WIP，需清理归位）"——CTO 知情但未清理归位

### 归因
写集漂移 → **归因: control-tower**（brief 声称 vs 提交实际无物理对账门禁）+ CTO 操作执行
编号撞车 → **归因: control-tower**（无 D# 注册表/编号对账）
CTO-HEALTH 无源 → **归因: control-tower**（产物-数据源一致性无校验）+ 操作
幂等失效/零测试 → **归因: implement**（新脚本无测试、声称未验证）

---

## 五、L3 执行审计

| 证据 | 物理事实 | 判定 |
|------|---------|------|
| 推送 | 98b0d4d1 在 `ssh/feat/d383-cto-batch`（git log 证实） | ✅ 已推，未合 main |
| bypass.log | 提交内 `.claude/bypass.log` 27 COMMITTED，**08-16 零事件**；工作区同版本 | ⚠️ **D383 提交无执行证据记录** |
| pre-commit 时间戳 | `.claude/last-precommit-success` = 11:44:59，提交 98b0d4d1 = 11:42:41（时间戳在提交后 2'18"） | ⚠️ 存疑，需创始人核（无 bypass 记录，无法证明 --no-verify；可能为提交后手动复跑 pre-commit-check） |
| **CTO-HEALTH 数据无源** | 提交内 CTO-HEALTH 声称 COMMITTED=44/24h=6、近 7 天含 08-16:6；提交内 bypass.log 仅 27 COMMITTED、08-16 零事件 → 生成时(11:29)的数据源已被回滚，**产物数字在提交内数据源中不可复现** | ❌ **P1-3 执行证据链断裂（M4 第三次）** |
| CI | docs 批次，本地无 CI job 证据；CTO-HEALTH MANUAL 自认"CI 全红 10/10（含已知假红 CT-6）" | ⚠️ 待创始人逐 job 核（CT-6 已知） |

M4 历史链：D328 P1-3 → D329 P1-2（D331 建对账修复）→ **D383 P1-3（第三次）**。按台账红线"同类错误第二次出现 = 防线系统性失效，升级创始人"——D383 是 M4 第三次触发，**须升级创始人**。

---

## 六、north-star 第二道裁决（跑偏判定）

对照 `.claude/PRODUCT-BRIEF.md` 三问：

| 问 | 裁决 | 依据 |
|----|------|------|
| ① 服务真实用户场景吗 | ✅ 对齐 | 本批为控制体系/协作底座（派活、CODEOWNERS、C6 门禁、状态机、台账），是 PRODUCT-BRIEF §八「Loop Engineering 需要成为什么」的落地（产品对齐检查/全局状态感知/决策建议）；创始人的 4 个根本痛点（跑偏/欺骗/盯梢/无技术背景）由该体系承载（cto-handover skill §二） |
| ② 让模块更接近终态吗 | ✅ 对齐 | 控制塔终态 = "机器强制替代人盯"：CODEOWNERS（B1 第一层机器强制）、C6 fail-closed、task-state 状态机（结果传递自动化）、台账拆分——全部朝终态走一步，无横向加功能 |
| ③ 变味吗 | ✅ 对齐 | 未改产品形态；Synova 仍是"驻扎企业、主动诊断、为增长服务"的 Agent + 支撑其可靠交付的控制体系 |

**裁决：对齐**（本批为基础设施/流程任务，不直接服务 FDE，但属于产品使命的控制体系支柱，非偏离）。

---

## 七、L4 防线缺口收割

| 问题 | 本该拦住它的防线 | 为什么没拦住 | 缺什么 |
|------|----------------|-------------|--------|
| 写集漂移（25 vs 23） | brief Done 标准（人肉核对）；pre-commit 组 12 Task Scope 一致性 | 组 12 只查"超出 Q2 范围"，**不查"少于声称"**（漏交不报）；check-dev-doc-write-set.sh 只对 dev doc 写集，不对 task brief | 无"brief 声称文件集 vs 实际提交"对账门禁（提交后 `git show --stat` vs brief Q2 列表差集校验） |
| D382 编号撞车 | 认领制（组 12） | 只查文件冲突，不查编号冲突；台账 D339 教训未固化为编号注册表 | task-state/ 目录可充当 D# 注册表（撞车恰恰因状态机与 dev doc 并行建立、互不对账）——需"派发前查 task-state/ 编号占用"规则 |
| CTO-HEALTH 数据无源 | M4 对账（D331 check-bypass-log） | 对账只查"提交 vs bypass.log"，**不查"产物声称 vs 数据源可复现"**；bypass.log 被回滚后产物未重新生成 | gen-cto-health.py 增加产物校验：渲染前重算数字，若与已提交产物差异非时间戳 → 告警；或产物含数据源 hash |
| 幂等失效 + 零测试 | 铁律 48（新代码要有测试） | 无"新增 .py 脚本必须有测试配对"门禁（pre-commit 组 2 只对 .ts/.tsx）；幂等声明无验证 | 新脚本验收链扩展：gen-cto-health.py 幂等测试（连续两次运行 diff=空）+ 接线检查；纳入 ctrl-tower-change 模式 6 |

---

## 八、发现清单

### P0（阻断交付）
无。

### P1（建议修复，记录跟进）
1. **写集漂移**：brief 声称 25 文件实际 23；TASK-ROUTING.md 派活登记 + D356/D379 spec 未提交（"dev-doc spec 入库"声称不实）——按 D382 铁律另起 FIX 任务补交/对账，禁直接改原任务
2. **D382 编号撞车**：task-state 状态机 vs doc-commit-exempt dev doc 共用 D382——需重编一个（CT-34 台账标"新 D#"）
3. **CTO-HEALTH 数据无源 + D383 无 bypass.log 记录**（M4 第三次触发，升级创始人）
4. **gen-cto-health.py 幂等声称不实**（时间戳恒写文件）+ 329 行零测试

### P2（可选）
1. `task-state/D383.json` 未提交（登记未落库）+ files 字段 23 非数组（契约偏离 README L29）
2. gen-cto-health.py 无自动接线（手动工具，靠 CTO 自律运行）
3. 编号规范漂移：cto-handover skill §〇"不再拿 D#" vs 实际文件名带 D#（SYNOVA-IMPL-DSH-D382-…）
4. 工作区 ~335 文件未提交（CTO 自认，含前任遗留 WIP）——建议清理归位，防 D315 型事故
5. pre-commit 时间戳(11:44:59)晚于提交(11:42:41)——需创始人核

### 执行证据疑点（需创始人核）
- D383 提交是否走了完整 pre-commit（时间戳顺序异常）？
- CI 10/10 全红中真红/假红（CT-6）逐 job 判定？
- CTO-HEALTH 生成时(11:29)的 bypass.log 中间版本内容（44 COMMITTED）在哪里？

---

## 九、核实记录（claim-verifier 附注）

| 声称 | 判定 | 证据 |
|------|------|------|
| D381 测试"新 9/9 + 既有 5/5 全绿" | ✅ 属实 | 实测 9/9 + 5/5 |
| D378 哨兵口径 45+4=49 + path-dependency 空壳 | ✅ 属实 | extensions/sentinels 45 目录；manifest entryPoint→不存在的 computes/detect.ts |
| gen-cto-health.py"幂等: 无变化不写文件" | ❌ 不实 | 两次 dry-run 输出仅差时间戳；L320 比较恒 False |
| brief"25 文件""dev-doc spec 入库" | ❌ 不实 | 实际 23；spec 未入库（untracked，dev-doc 分支未合 main） |
| brief"bypass.log 有记录" | ❌ 不实 | 提交内 bypass.log 08-16 零事件 |
| 基线 434 | ✅ 属实 | audit-check.py --full 实测 434 FAIL（与 D356 一致） |
