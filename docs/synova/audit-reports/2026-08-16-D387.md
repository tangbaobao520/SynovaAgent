# K3 独立审计报告 — D387

> 审计员: Kimi K3（独立会话，零上下文）
> 审计对象: D387 — CT-34 纯文档提交豁免门禁
> 提交集（git log --all 映射，D320 教训）:
> - `de7b831d` feat(D387): CT-34 纯文档提交豁免门禁（scripts/pre-commit-check.sh 修改 + tests/control-tower/doc-commit-exempt.test.sh 新建 + .claude/task-briefs/2026-08-16-D387-doc-commit-exempt.md）
> - `1c72c2be` docs(D387): dev doc + task-state impl_done 登记（docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D387-doc-commit-exempt-20260816.md + task-state/D387.json）
> - 完备性核对：`git log --all -- scripts/pre-commit-check.sh` 前一次修改为 D366（eacfdf66，非本任务）；test 文件全历史仅 de7b831d。**提交集完备，无劫持**。
> 审计日期: 2026-08-16
> 运行环境: macOS。产品仓库 /Users/wane/SynovaAgent（只读，零写入）；测试沙箱 = `git archive` 导出本地 feat/d387-doc-commit-exempt（尖端 a641f288 = 已推送 1c72c2be + 1 个本地未推送 D388 提交）至 /tmp/d387-audit；D387 范围两文件与已推送分支逐字节一致（`git diff 1c72c2be..a641f288 -- <两文件>` 为空）。
> 环境注记: ①审计中段网络曾不可达（fetch/ls-remote 60s 超时）→ 初判推送/CI"不可核"；**同日补核**：网络恢复后本审计已自核（ls-remote 实测 + GitHub check-runs API + main 导出树本地复现 CI 红根因），见 §五。②测试复跑使用被审机制自身的 SYNO_GIT_CACHED_* 注入缝——复跑的是"豁免路径逻辑"，非完整真实 hook 链；真实 hook 链证据见 §五日志对账。③基线脚本对 bash 变更有盲区，仅作 TS 侧零新增证明。④GitHub check-runs 可读未认证（公开元数据）；job 日志下载需 admin 权限（403）→ CI 红根因改用 main 树本地复现取证。

---

## 一、结论

**PASS**（无 P0；P1×1 跟进 + P2×5；初判 CONDITIONAL PASS 的两项条件已由本审计补核消解，见 §五）

- 核心交付物理属实：12 用例测试独立复跑 **17 断言 / 0 失败 / exit 0**（声称"12 用例全绿"保守属实）；白名单正则正反矩阵 10+10 全中；fail-closed 全部成立。
- 活体验证声称属实：`1c72c2be` 的精确暂存集（dev doc + task-state/D387.json）在本审计沙箱复刻 → 输出 CT-34 豁免标记 + Secrets 全量扫描 + exit 0。
- 安全负测通过：混合提交（docs/x.md + src/evil.ts）→ 无豁免标记 → 全量 13 组 → 组 2 硬阻断（exit 1）。
- 审计基线 **435=435**（对父提交 7bc829bf），D387 零新增 FAIL。
- 无 --no-verify 证据：bypass.log 无 2026-08-16 detected-bypass；pre-commit-failures.log 在两个 D387 提交时点（15:45/16:20）零记录。
- **推送已核实**（补核，2026-08-16）：`git ls-remote origin` 实测 origin/feat/d387-doc-commit-exempt = `1c72c2be` = 本地提交，`..1c72c2be` range 为空。
- **CI 逐 job 已核实**（补核，GitHub API）：1c72c2be 共 16 check-runs（8 job × 2 事件）——6/8 绿 ×2；2 红（Architecture Check / npm audit）**与 main@261fe3f8 完全一致，均为预存红**，本审计已在 main 树本地复现根因（§五），与 D387 零关系（D387 零 src/ 零 package 文件触碰）。
- **P1-1（跟进，不阻断合并）：D387 新引入的 SYNO_GIT_CACHED_* 注入缝无武装守卫、无审计落盘**——组合既有 SYNO_SECRETS_ROOT（check-secrets.sh:13）可构成无痕迹全 13 组旁路，且 post-commit 绕过检测对其失效（豁免 exit 0 → 写新 success marker → 无 detected-bypass 记录）。建议本周期内派后续 CT 任务修复（§八）。

---

## 二、14 项逐项表

| # | 项 | 判定 | 证据 |
|---|----|------|------|
| 1 | 接口真实性 | ✅ | `is_doc_only()` pre-commit-check.sh:137-143、`DOC_PREFIX_RE` :133 物理存在；dev doc §5.1 声明四修改点全部命中（注入缝 :113-118 / 判定函数 :133-145 / 早退分支 :150-165 / 原 STAGED_ALL 重复定义已移除 :235 注释为证） |
| 2 | 文件路径存在性 | ✅ | dev doc 引用路径全部存在（check-secrets.sh、secrets-env-exempt.test.sh、today-by-name.test.sh、check-dev-doc-write-set.sh、台账、D366 报告） |
| 3 | Edge ID 有效性 | — | 本任务无 E-XX（控制塔门禁域，不适用） |
| 4 | 架构边界 | ✅ | 纯 bash 基建变更，零 src/ 触碰，无跨层 import 问题 |
| 5 | 数据流完整性 | ✅ | dev doc 数据流每箭头物理对应：GIT_CACHED(:113-118) → STAGED_ALL(:144) → is_doc_only(:145) → DOC_ONLY → 早退(:150) → check-secrets.sh(:157) |
| 6 | 接线深度 | ✅ | is_doc_only 被早退分支真实消费（同文件 :145/:150，非仅定义）；脚本被 .git/hooks/pre-commit wrapper（install-hooks.sh:41-52 生成体）+ synova-commit:35（`SYNO_PRE_COMMIT` 默认 pre-commit-check.sh）调用 |
| 7 | 降级诚实性 | ✅ | Secrets 失败 → 显式输出 + exit 1（:161-163）；正则/空输入 → 返回 0 走全量（fail-closed，D328 三态合规）；注入缝未设 → 默认真实 git（`${SYNO_GIT_CACHED_NAMES:-$(git ...)}`） |
| 8 | 测试契约 | ✅ | 契约注释（铁律 47）完整：@input/@output/@fail-closed/@error（:131-136），与实现逐字吻合 |
| 9 | 测试覆盖 | ✅ | 正常(T1/T2/T4/T5)+降级(T7)+边界(T3/T6/T10/T12)+接线(T8/T9/T11) 三路径齐备非空壳；独立复跑 17/0 |
| 10 | 文件驱动 | — | 无新类型/manifest/tags（门禁域不适用） |
| 11 | 自我报告交叉对比 | ✅/⚠️ | "12 用例全绿"属实（17 断言）；"活体验证"属实（本审计复刻成功）；"无 --no-verify"与日志一致；**DS14 交付声明未物理落库**（P2-2） |
| 12 | dev doc 偏离审计 | ⚠️ | 写集精确命中（1 改 1 新 + brief 认领惯例 + dev doc/task-state 走文档提交）；§3.2 七决策点与实现一致；**行号引用为改动前值未回填**（P2-1）；dev doc 提交晚于实现提交 35 分钟（见 §四注） |
| 13 | 控制塔执行审计 | ✅/⚠️ | pre-commit 通过证据链一致（双日志对账，§五）；推送 ls-remote 实测 ✅；CI 逐 job 已核（6/8 绿 ×2，2 红=main 预存，§五）；**豁免事件本身零审计落盘**（P1-1 跟进项） |
| 14 | 版本编排合规 | ✅ | dev doc 声明不动 VERSION.md/version.log/tag；两提交实际零版本文件触碰；门禁行为变更非版本发布的理由成立 |

---

## 三、L1 代码审计（物理验证）

### 3.1 is_doc_only 判定 + 白名单正则 — 通过（含攻击面复测）

实现（pre-commit-check.sh:133-143）：
```bash
DOC_PREFIX_RE='^(docs/.*\.(md|html|txt)$|\.claude/task-briefs/.*\.(md|html|txt)$|memory/.*\.(md|html|txt)$|task-state/.*\.(json|md)$|[^/]+\.(md|html|txt)$)'
```
- 目录+扩展名双约束 ✅：`memory/x.json`→0、`task-state/x.txt`→0、`.claude/task-briefs/x.json`→0、`docs/secret.ts`→0（矩阵含）。
- 本审计追加对抗用例（超矩阵）全部 fail-closed：`docs/X.MD`→0、`docs/x.Md`→0、`DOCS/x.md`→0、`docs/x.md.bak`→0。大小写保守误拦方向（走全量），无可利用逃逸。
- 技能文件 `.dsh/skills/`、`.claude/skills/` → 0（D370 组 13 契约不被绕过，T12a/b 复跑 ✅）。
- 空暂存 → 0（T10 ✅）；纯删除提交（diff-filter=ACMR 不含 D）→ STAGED_ALL 空 → 全量，保守 ✅。

### 3.2 早退分支位置与顺序 — 通过

- GATEKEEPER bypass 阻断 :103（detected-bypass 首行）< is_doc_only 定义 :137 < 早退分支 :150 < par_start :174。绕过审计先于豁免 ✅；纯文档零 par 启动 ✅（V4.5.1 性能教训兑现）。
- 全量路径回归：G12c check-dev-doc-write-set.sh 保留 :977（D383 P1-1 不回退，T11 ✅）；组 6 时间戳 :585、G12b :968 在全量路径原位置。

### 3.3 独立复跑记录（不信用自报）

| 复跑 | 结果 |
|------|------|
| `bash tests/control-tower/doc-commit-exempt.test.sh`（沙箱导出树） | **通过 17 / 失败 0，exit 0** |
| 复刻 1c72c2be 精确暂存集跑真实脚本 | CT-34 豁免标记 + Secrets 两阶段扫描 + exit 0 |
| 混合提交（docs/x.md + src/evil.ts）负测 | 无 CT-34 标记；全量 13 组执行；组 2"新文件配对"硬阻断 exit 1 |
| 基线 audit-check.py --full（D387 树 vs 父 7bc829bf 树） | **435=435**（WARN 883=883），零新增 |

### 3.4 P1-1：注入缝构成无审计全门禁旁路（本任务新引入面）

- D387 将 `GIT_CACHED_*` 四变量改为 `${SYNO_GIT_CACHED_*:-...}` 注入缝（:113-118），**无武装守卫**（如 SYNO_TEST_ARM 双确认），生产环境变量直达判定。
- 攻击/事故路径：`SYNO_GIT_CACHED_ALL_NAMES=docs/x.md SYNO_GIT_CACHED_NAMES=docs/x.md SYNO_GIT_CACHED_ADDED_NAMES= SYNO_GIT_CACHED_DIFF= SYNO_SECRETS_ROOT=/tmp/empty git commit` → is_doc_only=1 → 早退仅 Secrets（且扫空目录）→ exit 0 → hook wrapper 写新 last-precommit-success（install-hooks.sh:51）→ post-commit 检测不到绕过 → **任意代码提交跳过全部 13 组且零审计痕迹**。
- 对比既有 --no-verify：post-commit 可检测 → bypass.log → GATEKEEPER 次日硬阻断（:99-111）。注入缝旁路**比 --no-verify 差一个审计维度**（无痕）。
- 边界平衡：单用 SYNO_SECRETS_ROOT（D370 既有）不能跳过其余 12 组——本任务把"内容判定"也开了缝，两个缝组合才构成全旁路。属设计期威胁分析问题，非实现笔误。

---

## 四、L2 偏离审计（声称 vs 证据）

| dev doc 声称 | 物理证据 | 判定 |
|---|---|---|
| §3.1 写集 1 改 1 新 | de7b831d 实际 = 脚本改 + 测试新 + brief（认领惯例，D356 审计已认可口径）；1c72c2be = dev doc + task-state（文档提交） | ✅ 代码写集零漂移 |
| §3.2 七决策点（白名单双约束/技能不进/仅 Secrets/早退 par_start 前/判定函数化/GATEKEEPER 保留） | 逐条与实现吻合（:113-165） | ✅ |
| §5.1 四修改点 | diff 全部命中 | ✅ |
| DS1-DS12 | 见 §一复跑 + 逐项核对（测试/注入缝/判定/早退/Secrets/混合/task-state/顺序/G12c/空集/技能/接线） | ✅ 12/12 |
| DS13 真实提交 13 组全过、无 --no-verify、diff=写集 | 双日志对账一致（§五）；diff=写集+brief | ✅ |
| DS14 完成报告含决策记录、交付声明覆盖 DS1-DS14 逐项标注 | 决策记录 §3.2 在库 ✅；**DS1-DS14 逐项交付声明无入库文件**（task-state 仅 impl 元数据；会话消息非 git 可追溯载体） | ⚠️ P2-2 |
| 行号引用（G12c L929-937、组 6 L537-545、par_start L120、bypass L99-111） | 实测 G12c :977、组 6 :585、par_start :174、bypass :103-111——引用的是**改动前**行号，未回填 | ⚠️ P2-1（M7） |

> 注：dev doc 提交（16:20）晚于实现提交（15:45）35 分钟。铁律 0-2 顺序为 spec→test→impl；物理顺序为 impl→devdoc 入库。dev doc 或已先写未提交（D379"已写未入库"同型），git 层面无法证伪写时序，记观察不记发现——但 spec 先行入库是汇聚纪律（M7 防线）的应有动作。

---

## 五、L3 执行审计（机器证据）

| 证据 | 实测 | 判定 |
|---|---|---|
| bypass.log（分支内 + 产品仓库活动文件） | 2026-08-16 零 detected-bypass；最近条目均为 D388 正常记录（16:34 补记/16:35 BLOCKED/16:41 PASS） | ✅ 无绕过 |
| pre-commit-failures.log（活动文件） | 2026-08-16 仅 08:30:34-08:31:07Z（=16:30-16:31+08:00）4 条 exit=1，属 D388 提交窗口（与 bypass.log D388 BLOCKED 互证）；**D387 两提交时点（15:45/16:20）零失败记录** | ✅ 通过证据链一致 |
| 活体验证（1c72c2be 走豁免路径） | 本审计复刻其精确暂存集 → 豁免标记 + Secrets + exit 0；且无失败日志反证 | ✅ 属实 |
| 推送状态 | **补核（网络恢复后本审计自核）**：`git ls-remote origin` 实时返回 origin/feat/d387-doc-commit-exempt = `1c72c2be0b7a...` = 本地 D387 docs 提交；`origin/feat/d387-doc-commit-exempt..1c72c2be` 为空 | ✅ 属实 |
| CI 逐 job 结论（GitHub API check-runs，D320 教训：job 级，非整体 run） | 1c72c2be 共 16 check-runs（8 job × 2 事件：push 16:28+08:00 / 第二事件 18:03+08:00）：TypeScript+Lint+Iron Laws ✅×2、Vitest (1/2) ✅×2、Vitest (2/2) ✅×2、Integration Contract Check ✅×2、Checker Review ✅×2、Golden Case F1 Gate ✅×2；**Architecture Check ❌×2、npm audit ❌×2** | ✅ 已核（2 红为预存，见下） |
| CI 2 红的归因对照 | main@261fe3f8（D387 未存在时的 12:03+08:00 run）同样 Architecture Check ❌ + npm audit ❌；本审计在 main 导出树本地复现：**Architecture Check = `src/routes/admin-knowledge.ts:17` L1→L4 跨层 import KnowledgeStore（铁律 39）**；**npm audit = 33 漏洞（32 high/1 critical，node-gyp/make-fetch-happen/rimraf/tar 传递链）**。D387 零 src/ 零 package 触碰 → 预存红，非本任务引入 | ✅ 归因排除（见 P2-5） |
| 基线 | 435=435 vs 父提交 | ✅ |

> 分支形态注记：已推送 D387 分支干净终止于 1c72c2be，但其基底是 D383→D386 堆叠链（非 main@261fe3f8）——PR 合并需前序任务先合或接受堆叠 diff（本仓库多机堆叠惯例，D383 审计同构，记观察）。本地尖端含未推送 D388 提交：若从本地推送本分支，D388 代码将搭车进入 D387 PR——堆叠工作法风险备查（观察项）。

---

## 六、跑偏第二道（north-star 三问机械裁决）

对照 .claude/PRODUCT-BRIEF.md 原文：

1. **服务真实用户场景吗？** 直接用户 = 开发/审计线（跨机文档同步不再被代码门禁误拦，台账 CT-34 实证 D362/D366 反复卡）；最终受益 = FDE/企业主（迭代不停摆）。§八"Loop Engineering 需要成为什么"明文认可门禁系统自身演进是产品护航的一部分。✅
2. **让模块更接近终态吗？** 模块 = pre-commit 门禁；终态 = "拦得住代码问题、不误杀文档同步"（V4.5.1 --no-verify 泛滥教训的根治方向）。本任务消除一类已实证的误杀源，非横向加无关功能。✅
3. **做完后 Synova 还是"驻扎企业、主动诊断、为增长服务"的 Agent 吗？** 零产品行为变更，纯工程基建。✅

**裁决：对齐，不跑偏。** 豁免 12 组是创始人 2026-08-16 明文决策（台账 CT-34），保留 Secrets 与决策原文一致；dev doc 对 G12c 的精确化（保留在全量路径）有第一性原理论证（spec 阶段 (b) 变更命中必然漂移），不违背决策本意。

---

## 七、发现清单

| # | 级 | 发现 | 证据 | 归因 | 模式 |
|---|---|------|------|------|------|
| P1-1 | P1 | **SYNO_GIT_CACHED_* 注入缝无武装守卫、豁免事件零审计落盘**——组合 SYNO_SECRETS_ROOT 构成无痕迹全 13 组旁路，post-commit 绕过检测对其失效 | pre-commit-check.sh:113-118 + :150-165（无日志写）；check-secrets.sh:13；install-hooks.sh:51（exit 0 即写 success marker） | devdoc（主：§3.2 设计注入缝未做旁路面威胁分析）+ control-tower（次：豁免/旁路类事件无 exempt.log 类落盘机制） | M4（执行证据链断裂——豁免事件无证据链） |
| ~~P1-2~~ | ~~P1~~ | **已消解（2026-08-16 补核）**：CI 逐 job + 推送新鲜度原判"不可核"，网络恢复后本审计自核完成——推送属实、CI 6/8 绿 ×2、2 红=main 预存（§五）。原判原因：审计中 fetch/ls-remote 60s 超时（环境依赖，非证据缺失） | ls-remote 实测 + check-runs API + main 树本地复现 | audit（初判受环境限制，补核闭环） | M4（已闭环） |
| P2-1 | P2 | dev doc 行号引用为改动前值未回填（G12c L929-937→实测 :977；组 6 L537-545→:585；par_start L120→:174） | dev doc §4/§8 vs 分支脚本实测 | devdoc | M7 |
| P2-2 | P2 | DS14 要求"交付声明覆盖 DS1-DS14 逐项标注"——无入库完成报告文件（决策记录 §3.2 在库 ✅，缺逐项 DS 标注载体） | 分支全树 grep 无 D387 交付报告；task-state/D387.json 仅 impl 元数据 | implement | M2 |
| P2-3 | P2 | 基线 FAIL 434→435 漂移发生在 D383-D386 堆叠区间（非本任务，D387 前后 435=435）——建议属主任务定位新增项 | 本审计双树实测 | control-tower（观察项） | — |
| P2-4 | P2 | 豁免面治理含义备查：task-state/*.json（状态机）、memory/（教训注入）、根级 CLAUDE.md/AGENTS.md（行为注入）经豁免路径仅 Secrets 把关。现状 12 组本不查这些文件内容语义，实际覆盖损失≈0；但 task-state 状态机完整性自此单点依赖 K3 审计（编码 agent 可经纯文档提交自改状态）。创始人决策明文含 task-state ✅，记录备查不阻断 | 白名单 :133 + task-state/D387.json 契约 | control-tower（观察项） | — |
| P2-5 | P2 | **main 线 CI 双红预存**（补核新发现）：Architecture Check 红 = src/routes/admin-knowledge.ts:17 L1→L4 跨层（铁律 39）；npm audit 红 = 33 高危漏洞。main 预存红常态化 = CI 失去信号价值（D320 假红教训同型风险——红惯了没人看）。非 D387 引入，建议派单修复 | main@261fe3f8 check-runs + main 导出树本地复现（§五） | control-tower（观察项） | M1（CI 红常态化=信号失效） |

---

## 八、L4 防线缺口收割

> 本次发现的问题，控制塔哪道防线本该拦住？为什么没拦住？

**P1-1（无审计旁路）**：
- 本该拦截的防线：dev doc 评审（gatekeeper C 系）+ 决策参考框架（D333 四步）。
- 为什么没拦住：C 系门禁查"写集表存在性/格式"，不查"本变更是否创造无审计绕过面"；决策四步走了（§3.2 七决策点收敛），但参考系问答里**没有"此机制能否被滥用为无痕迹绕过"这一强制问**——D370 开 SYNO_SECRETS_ROOT 缝时无害（单缝不能绕全量），本次开内容判定缝时沿用了"注入缝惯例"而未重估组合爆炸。
- 缺口归类：**M4 强化**（豁免/旁路类事件必须落审计盘，与 bypass.log 同哲学）——命中已有 M 类，不立新 M。建议机制（一类一免疫细胞）：豁免路径追加 `echo "$(date) | EXEMPT | staged=<清单>" >> .claude/exempt.log` + post-commit 对账豁免事件（单机制防"无痕迹豁免"一类错）。注入缝加武装守卫（SYNO_TEST_ARM=1 双确认）可作为同一免疫细胞的第二半。

**P1-2（初判 CI 不可核）**：已补核闭环——审计环境网络恢复后自核完成（ls-remote + check-runs API + main 树复现）。教训归 audit 自身：环境依赖失败应显式重试窗口，不直接转派创始人（本次已纠正）。

**P2-5（main CI 双红预存）**：本该拦截的防线 = CI 红即修纪律。为什么没拦住：Architecture Check 与 npm audit 在 main 上持续红（至少 2026-08-16 04:03Z 起），无任务认领修复——红常态化即 M1 型信号失效（检查存在但无人响应 == 检查失效）。建议：派单修 admin-knowledge.ts:17 跨层 + npm audit fix 评审，不立新 M 类（M1 防线：CI 红超 24h 自动入 CTO 待办可作为该类既有防线的强化）。

**P2-1（行号漂移）**：M7 已有防线（dev doc 与最终实现同 commit 回填）——本例 dev doc 在实现后入库但行号未按实测回填，属 M7 既有防线执行不到位，不立新类。

---

## 九、任务状态写回

- verdict: **PASS**（初判 CONDITIONAL PASS；2026-08-16 补核后两项条件消解——推送/CI 本审计已自核，P1-1 转为不阻断的跟进项）
- 跟进项（不阻断合并）：① P1-1 派后续 CT 任务——豁免路径写 exempt.log + post-commit 对账 ± 注入缝武装守卫（SYNO_TEST_ARM）；② P2-5 派单修 main 预存 CI 双红（admin-knowledge.ts:17 跨层 + npm audit 33 漏洞）。
- 写回文件：docs/synova/coordination/task-state/D387.json（审计工作区侧）；根目录 task-state/D387.json 的 audit 段由 CTO 按 D383 先例在堆叠分支内更新。

---

> 审计边界声明：本审计只读产品仓库，零写入；未修改 scripts/audit/ 任何文件；报告与状态写回均在独立审计工作区完成。
