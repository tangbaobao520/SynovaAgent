# 战略咨询备忘：Harness 借鉴的正确打开方式与 Synova 最终形态

> **撰写**: K3（独立审计线）| **日期**: 2026-08-16
> **性质**: 咨询备忘（非审计报告）。应创始人请求，以"如果我是创始人"的视角，对 D394-D398 借鉴方向做深度评估。
> **方法**: 全部结论基于物理阅读——Harness 本地代码（`@deepseek-ai/dsh` v0.1.0-rc.6，194 包）、SynovaAgent 仓库源码、Win 线研究 11 文件原文、PRODUCT-BRIEF.md、K3 08-14 全链路审计报告。
> **一句话结论**: 战略方向（收敛通用工程、聚焦领域深度、路径 B 为主）**正确，我背书**；但 D394-D398 的任务卡目前全部按"借形"写法立项，照此执行必然形似神不似。本备忘给出每一项的"神"是什么、怎么借、怎么防歪，以及我作为创始人在此时点的具体选择。

---

## 目录

- 〇、核实基础：我读到的物理事实（含对研究的三处修正）
- 一、创始人视角：此时点的大局判断
- 二、Synova 的最终形态（目光长远部分）
- 三、借鉴总目录：三类借法，三种防歪
- 四、逐项深度方案（D394-D398 + 研究未列的第 6 项发现）
- 五、防"形似神不似"的元机制（制度，不是口号）
- 六、排序与依赖图（与 P0 积压、演示、部署的关系）
- 七、路径 A 评估 rubric（现在写，不等 Q4）
- 八、如果只做三件事
- 九、与研究 11 份文档的交叉佐证（全覆盖矩阵 + 偏离台账）

---

## 〇、核实基础：我读到的物理事实

### 0.1 已核实为真的研究结论

| 研究声称 | 我的物理核实 |
|---|---|
| Harness 约 200 包、纯底座 | ✅ 194 个 `@deepseek-ai/*` 包，全部通用基建，**零领域包** |
| 事件溯源 session log | ✅ 且比研究写的更深：append-only 事件流是唯一真相源，LLM 消息历史是**派生投影**（`dsh-session` README: "the LLM message history is *derived* from it"）；持久化格式是 zstd 帧 + 校验和 + 崩溃截断恢复（`dsh-session-persistence-jsonl` README "Durability and crash semantics" 整节）；`dsh-invariants` 会在 reload 时**重放已有 session** 校验单调 seq/turn 闭合/工具调用配对 |
| capability seam 三角色 | ✅ 每个能力拆 Definition/Provider/Consumer 三包（compaction README 表格为证），spill/skill/sandbox/subagent 同构 |
| guard/超时/循环卫生 | ✅ `dsh-timeout`（纯计时库，**明确不拥有终止权**："share the timing/classification, keep the hard kill local"）；`repeat-tool-reminder`（同参连调 3/5/8 次递进提醒，**advisory 永不否决**）；`dsh-invariants`（运行时关系断言，fail-closed） |
| Cordis 时空可组合 | ✅ 插件卸载即撤销其注册的一切（effect/disposer 模式），scope 分层注册 |

### 0.2 对研究的三处修正（影响任务定义）

**修正 1：Agent Notes 不是产品记忆机制，是开发组织的决策记录约定。**
物理证据：DSH 全库 grep 到的路径是 `.agents/notes/{implemented(引用396处), proposed(16), archived(16)}`，全部出现在 README 的设计决策引用中（如 "The project-session directory **decision** records this tradeoff"）。它是"每个非平凡设计决策留一份带状态的文档"的**工程纪律**，不是运行时记忆系统。D395 任务卡写"补 S5-3 知识沉淀"——那是产品侧需求，二者不是一个东西。直接后果：D395 必须拆成两个任务（见 §4.2）。

**修正 2（经附录交叉佐证后软化）："subagent 多后端"在 npm 发布物中未见外部后端，但源码仓库证据为强。**
物理证据：npm 安装物中只有 `subagent-fork-in-process` / `subagent-spawn-in-process` / `subagent-in-process-driver`；`dsh-subagent` README 原话是 "in another process, or through a **future** transport"。**但研究附录 H-07 标注强证据**（源码仓库 `packages/subagent/` 含 acp/claude-code/codex 包族，研究在 Win 机 `D:\deepseek-harness` 全源码上核对）——差异是"npm 发布物 vs 源码仓库"的口径，不是研究失实。处置：涉及 subagent 外部后端的排期决策时，以源码仓库为准再核实一次（D9/B8 排多客户后，届时核实成本很低）。战略判断不受影响。

**修正 3：npm 发布物中的 "snapshot" 指持久化数据的深冻结无损快照（数据完整性纪律），不是黄金测试基建。**
DSH 的 keyless 回放测试在 GitHub 仓库的工程体系里，不在本地代码中。D396 的素材需要读仓库测试设施而非本地包。同时物理事实：Synova 已有 `scripts/ci/golden-case-checker.ts`（pre-push D300 门禁，pre-push-check.sh:220-221）——D396 是**扩展现有门禁**，不是新建。

### 0.3 我发现的、研究没强调的一个关键事实

**Synova 的哨兵 findings 不持久化——进程重启即丢。**
物理证据：K3 08-14 全链路审计（FAIL 报告 §2.1 L1 行）："findings 来自 runner **内存** records（进程重启即丢）"；我复核 `src/store/` 与 `src/l5/` 无 findings 表（grep `CREATE TABLE.*finding` 零命中；只有 `sentinel_baselines`、`sentinel_tickets`、`cron_jobs` 三张相关表）。对照 Harness `dsh-schedule` 的设计："The Session event log **owns** reminder state; timers, tool values, and model follow-ups are **disposable projections** of that log."

**这改变了 D394 的切入点。** 事件溯源对 Synova 的第一价值不是"会话回放信任叙事"（那是 S3-5/S0 的故事），而是先治"产品核心输出重启即丢"这个真实缺陷。详见 §4.1。

---

## 一、创始人视角：此时点的大局判断

如果我是创始人，我在 2026-08-16 这个时点看到的世界是这样的：

### 1.1 三个已经发生的时代性事实

**事实一：通用 Agent 工程的商品化已经完成，且不可逆。**
DeepSeek 把 194 个包的成熟底座 MIT 开源了。事件溯源、能力接缝、沙箱、子 agent 编排——这些上周还是"自研资产"，这周已经是"公共水电"。在公共水电上继续自建发电厂，不是坚持，是浪费。战略三铁律①（不再自研通用 Agent 工程）是对这个事实的正确回应。**这一条没有任何可犹豫的。**

**事实二：底座越成熟，领域知识越贵。**
经济学很简单：当任何人都能免费获得"会循环、会调度、会记忆的 agent"，稀缺的就只剩"知道拿这个 agent 干什么"。42 边因果本体、诊断方法论、专家体系、GA 闭环——这些不是"功能"，是**对一个领域（组织增长）的建模权利**。Harness 越成功，这个权利越值钱。所以铁律②（预算压到 8 项独有）不仅正确，而且是唯一正确的资本配置。

**事实三：Synova 的真实瓶颈不是工程范式，是数据闭环没转起来。**
物理事实：26 条产品线 3% 完成度；三循环端到端断裂（K3 08-14：L5 无 CRM/财务/HR 连接器、L4 类型契约 Market≠Client、camelCase≠snake_case 属性断裂）；哨兵 findings 重启即丢；8/20 演示、9 月哇呢宝贝部署在即。这个处境下，**任何不能回答"它让哪个验收点前进了"的工程投入都是奢侈品**——包括借鉴 Harness。这是我对全部借鉴任务的第一道闸门。

### 1.2 此时点我作为创始人的三个判断

**判断一：路径 B 正确，但 B 的任务卡目前全部按"借形"写法立项，照此执行必败。**
"事件溯源 session log（1-3 天）"这种写法，交付出来的一定是"多了一个 JSONL 日志文件，读路径没切，双写不一致没人发现"——这就是形似神不似，而且比不做更糟（多了一个需要维护的伪资产）。本备忘 §4 逐项给出"神"的定义和验收。

**判断二：借鉴顺序应该由"Synova 哪里在流血"决定，不是由"Harness 什么最强"决定。**
研究的借鉴清单是按 Harness 的能力图谱排的（B1-B8）。正确的排法是按 Synova 的伤口排：
- 伤口 1（在流血）：P0 修复的场景验收（FOUNDER-OPERATING-MODE Phase 1，GS-02~05 转绿）是**一次性的**，未固化为可重复回归门禁 → **D396 提到 P0 同批**；
- 伤口 2（在流血）：findings 重启即丢 + 无冷恢复 → **D394 的第一刀切这里**，不切会话回放；
- 伤口 3（M4 三次复发）：执行证据链 → 控制塔**稳定优先于通用化**，D397 现定义砍掉；
- 伤口 4（9 月后才会疼）：知识沉淀/组织记忆 → D398 排最后，且需要先看到部署数据。

**判断三：这是最后一次战略调整，所以它必须包含"不再调整"的机制。**
"最后一次"的真正含义不是"这次定完永远对"，而是**把未来的调整判据现在也写死**：路径 A 的触发 rubric（§7）、借鉴项的 invariant 验收制（§5）、防跑偏的三问闸门。有了判据，未来 Q4 的评估是执行而不是又一次战略摇摆。

### 1.3 北星三问自裁决（对本备忘自己的建议，按 north-star-guard 逐问物理判断）

| 我的建议 | 问1 服务真实用户？ | 问2 朝终态走一步？ | 问3 产品变味？ | 裁决 |
|---|---|---|---|---|
| D396 提前（黄金用例同批） | 企业主："诊断对不对"必须可复现才算数（PRODUCT-BRIEF §一核心问题） | 信任层终态的直接构件 | 否 | ✅ 对齐 |
| D394 片 1（findings 事件化） | 企业主：PRODUCT-BRIEF §一承诺"7×24 运行，自己发现信号"——**重启即丢是对该承诺的直接违背** | 信任层 + 驻扎能力的根基 | 否 | ✅ 对齐（全部建议中对北星亏欠最大的一项） |
| D394 片 2/3（会话事件化/fork） | FDE：诊断过程可回放 = 交付物可自证 | 信任层完整形态 | 否，但片 3 是底座能力不直接服务用户 → 排最后并挂路径 A rubric 约束 | ✅ 附条件对齐 |
| D397 砍（控制塔通用化） | 不服务 FDE/企业主（dev-infra） | 横向加东西 | 工程组织层面变味 | ❌ 原任务偏离 → 砍 |
| D398 缓（先看数据再建记忆） | 企业主：知识沉淀要以真实企业数据为形 | 朝终态但拒绝提前臆造 | 否 | ✅ 对齐（克制本身即对齐） |
| D395-a（开发组织四态） | 间接：服务交付质量 | 流程防线强化（M7 类） | 否 | ✅ 对齐 |

裁决结论：六项建议——四项全过、一项附条件（D394 片 2/3）、一项砍（D397 现定义）。**唯一需要克制解释的是 D394 片 3（fork/resume）——它是"为路径 A 保留可行性"的期权，不是用户价值，所以排在全部用户价值之后。**

---

## 二、Synova 的最终形态（目光长远部分）

创始人问"这会决定产品的最终形态"。我把 24 个月后的终局画出来：

### 2.1 终局定义

**Synova 最终不是一个 Agent，而是"组织诊断知识的可执行载体"——一套领域知识系统，运行在可替换的通用底座上。**

```
┌─────────────────────────────────────────────────┐
│  领域知识层（Synova 的全部价值，全部插件化）        │
│  · 42 边本体 = 企业的 schema（边是语义，点是状态）   │
│  · 专家/哨兵 = 可装卸的领域插件（文件驱动→运行时热装卸）│
│  · 诊断方法论 = 6 阶段管道（领域流程编排）           │
│  · 行业模板 = 本体的行业变体包                     │
├─────────────────────────────────────────────────┤
│  信任层（事件溯源 + 黄金数据集 + invariant 守护）    │
│  · 全事件流可回放：任何诊断结论可重建               │
│  · model-visible ⟺ logged：进模型的必可审计        │
├─────────────────────────────────────────────────┤
│  底座层（可替换：自研残片 → Harness → 未来的更好底座）│
│  · session/loop/调度/沙箱/subagent —— seam 隔离     │
│  · 换底座不动领域层                                │
└─────────────────────────────────────────────────┘
```

### 2.2 关于插件化——你认可的方向，我要给它精确的定义

你说"非常认可一切插件化的产品思维"。物理事实是 Synova 已经有**两种**插件化，Harness 是第三种，最终形态是三者合一：

| 层级 | Synova 现状 | Harness 对照 | 终局形态 |
|---|---|---|---|
| **知识插件化**（改行为不改代码） | ✅ 已强：48 个哨兵目录（manifest.json + computes/ + aggregate.ts）、7 专家目录、55 边类型 JSON、行业模板——文件驱动自动注册 | Harness 的 skill-filesystem 同构（文件驱动 skill） | **保持，这是对的** |
| **运行时服务插件化**（seam 三角色：接口/实现/消费分离，实现可换） | ⚠️ 雏形：providers/ 有 registry；其余是静态 import | 全部能力 seam 化，卸载即撤销（Cordis effect/disposer） | **渐进吸收**（D6/B2，Q4 后） |
| **进程/生态插件化**（第三方可分发） | ❌ 无 | Harness 插件市场 | 路径 C，多客户后再说 |

**关键判断：Synova 的知识插件化已经比 Harness 的 skill 体系走得更远**（哨兵 manifest 带阈值/调度/数据权限声明，比 dsh-skill 的 invocation policy 更领域化）。所以插件化上 Synova 要借的不是"文件驱动"（自己已强），而是 **seam 的"实现可换"纪律**——它服务的是"底座可替换"，不是"知识可扩展"。两者别混。

### 2.3 终局的胜负手

Harness 越成功对 Synova 越有利——底座越便宜，领域知识越贵。**唯一会输的剧本**：有人用同样的免费底座 + 更快的领域知识积累速度超车。所以防御指标不是"我们有多少边/专家"，而是：

> **本体被真实数据验证的速率**——每月 GA 确认的边数、被确认/否决的记忆条目数、部署企业回流的校准事件数。

这个指标从今天起就该被追踪（哇呢宝贝是第一个数据源）。D398 的成功标准应该直接定义为这个指标，不是"记忆条目数"。

**研究第三章给这一节补了一块证据**："公司大脑"模板层也在商品化——Sylph（YC X25，README 全文证据）把"8 个 AI 员工 + cadence + self-improving skill"做成了纯 markdown 模板，但它**停留在"流程自动化 + 知识库"层，无诊断因果深度**。含义：Synova 不能退守"企业知识库模板"这一层（那层也在被开源填平），护城河的精确位置是**因果诊断深度 × 数据验证速率**——Sylph 的 "diff 重写 skill 文本"和 Synova N13 的"因果规则调权"是同构思想，但后者挂在 42 边本体上，这是深度差。

---

## 三、借鉴总目录：三类借法，三种防歪

把研究和转交文档的 B1-B8/D1-D9 重新归成三类——**三类的东西，借法完全不同，混在一起是形似神不似的总根源**：

| 类 | 内容 | 借法 | 神是什么 | 主要失败模式 |
|---|---|---|---|---|
| **A. 架构纪律类** | 事件溯源（B1/D394）、seam 三角色（B2）、model-visible⟺logged | 借**不变量**：先定义可验证性质，实现围绕性质 | 一条可物理验证的系统性质（如"杀进程后能从事件流重建等价状态"） | 交付了工件（日志文件/接口层）但性质不成立，且无人检查性质 |
| **B. 机制类** | spill（B6）、schedule 冷恢复、timeout/repeat-reminder（B5/D397）、三层加载（D398/OpenViking） | 借**机制设计**：读对方的决策记录（Agent Note/论文），理解 tradeoff，按自己的约束重新实现 | 机制解决的原始问题在 Synova 场景真实存在且被解决 | 机制抄过来了，但它解决的问题在 Synova 不存在（为借而借） |
| **C. 工程流程类** | Agent Notes 四态（B4/D395 开发组织版）、snapshot 测试（B3/D396）、README 决策锚点纪律 | 借**纪律**：直接改造 Synova 开发流程，成本极低 | 开发组织的知识/决策可沉淀、可检索、不腐化 | 流程建了没人用（M3 未接线的流程版） |

**规则：A 类必须过 invariant 验收（最严），B 类必须先证明"问题在 Synova 存在"，C 类随手做但要有使用门禁。**

---

## 四、逐项深度方案

### 4.1 D394 事件溯源（B1）——最重要，也最容易做歪

**Harness 的物理事实**（我核实过）：
- append-only `SessionEvent` 流是唯一真相源；消息历史 `deriveMessages()` 派生，不单独存（dsh-session README）
- 持久化：zstd 帧 + 每帧校验和 + 崩溃时截断到最近完整帧 + 合成闭合事件（dsh-session-persistence-jsonl "Crash recovery"）
- `ctx.sessions.flush(session)` 是持久化屏障——所有读决策前 await flush，持久化不确定时返回 `persistence_uncertain` 而不是猜（dsh-schedule README）
- invariants 在 reload 时重放全部已有日志校验（dsh-invariants README）

**神（invariant）是什么**：三条可验证性质——
1. **I1 可重建**：任意时刻 kill -9，重启后从事件流重建的会话状态与崩溃前等价（消息序列、诊断阶段、findings）
2. **I2 单源**：消息/状态只有事件流一个写入口；所有读路径从事件流（或其物化投影）派生——**读路径切换是检验单源的唯一标准**
3. **I3 可审计**：任何一次诊断结论（报告、finding）能从事件流回答"它由哪些输入事件产生"

**形似神不似长什么样**（这是对 D394 任务卡的直接预警）：
> 新增 `session_events` 表，写入时双写 messages + events；读路径不动（还读 messages 表）；events 表沦为没人读的日志副本；三个月后两处数据漂移，没人发现；审计时声称"已事件溯源"。**工件在，性质全不成立。**

**怎么借（我的具体方案，分三片，每片独立验收）**：

- **片 1（先做，治流血）：哨兵 findings + 信号事件化**
  - 落点：`src/sentinel/runner.ts`（findings 目前只在内存 records，K3 08-14 审计实证）→ 新增 `sentinel_events` append-only 表（L5），finding/signal/ticket 状态迁移全部作为事件写入；runner 内存态变成事件流的物化投影，启动时重放重建（这就是 Harness schedule 的 "durable state in log, timers are projections" 模式）
  - 同时修掉 `src/agent/sentinel-service.ts` 把 durationMs 当时间戳的 bug（`checkedAt: new Date(run.result.durationMs)`，当前 :97，K3 08-14 审计记录为 :101，文件已微移；恒输出 1970-01-01）
  - 验收（物理）：`npm run dev` 启动 → 触发哨兵 → kill -9 → 重启 → `GET /api/sentinel/findings` 输出与 kill 前一致；事件表 `seq` 单调无洞（grep/SQL 可验）
  - 工时实估：2-3 天（表设计 0.5 + 写入 0.5 + 重放重建 1 + 验收测试 0.5-1）

- **片 2（部署后做，信任叙事）：诊断会话（FDE 六阶段）事件化**
  - 落点：`src/store/session-store.ts`（314 行，messages 表 + FTS5）+ `src/agent/conversation-engine.ts`（845 行）+ `src/orchestrator/phase-state-machine.ts`
  - 做法：新增 session_events 流；**agent_messages 表降级为只读物化投影**（写路径切到事件流，读路径切到投影——SQLite 不扔，它变成查询层，这正是 `dsh-session-query-sqlite` 的模式：事件流是真相源，SQLite FTS5 是可重建的查询索引）
  - model-visible⟺logged 落地为一条 pre-commit 级检查：进 LLM 请求的上下文组装点（`prompt-assembler.ts`）必须有对应事件类型
  - **采纳 Harness feedback 的 log-only 纪律**（研究第一章 §1.6 标记的 Synova 缺口，交叉佐证补入）：GA/人类反馈一律先作为事件入流（可审计），**进不进模型上下文是显式决策而非默认**——`dsh-message-feedback` 同构（sidecar 存储 + 生命周期绑定 + 不进遥测）。这直接补强 N13：反馈先入流，才有"纠错→规则回流"的可追溯源
  - **数据安全边界（本备忘初版遗漏，补上——这是 append-only 的固有权衡，不设计就是事故）**：事件不可变 ⟹ PII 必须在 append **前**过 `security/PIIScrubber`，事后无法擦除；事件流继承现有数据安全四层（L0 公开摘要/L1 聚合信号/L2 脱敏证据/L3 原始数据仅客户企业内部可见）——事件流本身按 L3 处理，脱敏投影按 L2 派生；删除权诉求（企业主"忘了这条"）的答案是 per-org 事件流物理隔离 + 整流销毁，不做单事件删除。Harness 自己也承认同一权衡（dsh-session README："可回放提高可审计性**也扩大数据面**"）——我们面对的是企业数据，比它更敏感，必须在 schema 设计时把这一层焊死，不能事后补
  - 验收：I1 杀进程重建测试 + I3 "这份诊断报告由哪些输入产生"可查询 + 既有会话一次性迁移脚本 + 事件流 PII 抽查（append 后事件中零明文 PII）
  - 工时实估：**6-8 天，不是任务卡上的 1-3 天**（事件 schema 1 + 写入 1 + PII 前置接线 1 + 派生读路径 1.5 + 重放工具 1 + 迁移 0.5 + invariant 测试 1）

- **片 3（Q4 后，路径 A 的预备）：fork/resume + 完整回放视图**
  - 只有当片 1+2 的性质稳定后才做；做了它，路径 A 迁移成本降一半

**与 P0 的关系**：片 1 与 D355-D360 无冲突（不同表、不同代码路径），且为它们提供事件级观测；片 2 **硬依赖 D355-D360 的 L4/L5 契约先稳定**——在断裂的契约上叠事件流 = 把断点冻进事件 schema，事件 schema 上线后有迁移成本，比改代码贵。**顺序：D355-D360 → D394 片 2。**

---

### 4.2 D395 Agent Notes 四态（B4）——必须拆成两个任务

**修正后的认知**（§0.2 修正 1）：Harness 的 Agent Notes 是**开发组织的决策记录约定**（`.agents/notes/{proposed,implemented,archived,rejected}` 四态目录 + 非平凡变更强制带 Note + README 里用相对路径锚定决策原文）。

**拆成：**

- **D395-a（开发组织版，1 天，现在就做）**：
  - 把 Synova 现有 `memory/`（20 个非结构化教训文件）+ LOOP-ENGINEERING-CHANGELOG 的决策传统，改造为四态结构：`memory/notes/{proposed,implemented,archived,rejected}/YYYY-MM-DD-<主题>.md`
  - 每条 Note 头部四字段：状态/日期/决策/理由；状态迁移 = `git mv` 换目录
  - 门禁：非平凡 task brief 的 Q1 增加"相关 Note 引用"字段（grep 物理检查 Note 文件存在）
  - 神：**开发组织的决策可沉淀、可检索、不腐化**——这直接强化 M7（文档-实现漂移）防线，命中已有 M 类，**不需要新增机制类**（符合一类一机制原则）
  - 形似神不似预警：目录建了、20 个旧文件归档了，但新决策不写 Note——三个月后又是非结构化。防法：pre-commit 对"改 scripts/control-tower/ 或 src/orchestrator/ 的 commit"要求 commit message 引用 Note 路径（物理门禁，不靠自觉）

- **D395-b（产品版，取消独立存在，并入 D398）**：
  - "每次诊断沉淀为企业知识"是产品侧 S5-3——它的形态应该由 D398 的三层加载 + 42 边组织决定，不是抄一个四态目录。**独立做 D395-b = 在不知道记忆长什么样之前先建记忆目录，必返工。**

---

### 4.3 D396 snapshot 测试（B3）——从 P1 提到 P0 同批

**物理事实**：Synova 已有 `scripts/ci/golden-case-checker.ts`（pre-push D300 门禁）；DSH 的 keyless 回放测试在 GitHub 仓库工程体系（本地 npm 包不可见，§0.2 修正 3）。

**为什么必须提前**（这是本备忘最强的一条建议）：
- D355-D360 是对三循环断裂的**底层手术**（L4 类型契约 + L5 连接器），高回归风险
- 既有计划已有场景验收（FOUNDER-OPERATING-MODE Phase 1："每个修复以场景转绿为验收，GS-03 资本循环 → GS-02/04 契约对账 → GS-05 告警闭环"）——**但那是当次验收，修完即弃；缺的是把场景固化为每次 push 都重跑的回归门禁**。一次性转绿防的是"这次没修好"，固化门禁防的是"下次又改坏"——D396 补的是后者
- 战略链：路径 A 的 Q4 评估 ← 依赖数据回收 ← 依赖 9 月部署成功 ← 依赖 P0 修复质量 ← **依赖修复成果不被后续改动侵蚀**
- 等 9 月后补网 = 部署前的每次后续改动都在无网状态下叠加风险

**神（invariant）**：**可复现**——同一份企业数据副本，同一版本代码，诊断输出（findings 集合 + 报告结构）逐字节一致；模型非确定性部分用结构化断言（finding 的 id/severity/标题模式），不对 LLM 原文做全文 diff。

**形似神不似长什么样**：
> 快照的是 mock 数据（过的是假测试）；或快照 LLM 原文导致永远红、团队学会忽略红（门禁噪音化 = 门禁死亡，V3.9 教训："硬阻断 100% 有效，软机制 0% 有效"）。

**怎么借**：
1. 扩展 `golden-case-checker.ts`：从 1 条用例 → N 条，**每个 D355-D360 修复附带一条黄金用例同 PR 落地**（用例 = 该修复修复的断裂场景的最小数据副本）
2. 快照对象分层：compute 输出（确定性，全 diff）→ sentinel findings（确定性，全 diff）→ 专家报告（LLM，结构化断言）
3. 验收（物理）：故意改坏一个 compute 的阈值 → golden 门禁红 → 修复 → 绿。这个"红-绿"演练必须作为 D396 验收的一部分跑一次，证明门禁真能红
4. 工时实估：2-3 天（checker 扩展 1 + 6 条用例数据副本 1 + 结构化断言层 0.5 + 红绿演练 0.5）

**与演示的关系**：黄金用例扩充不碰 src/ 业务逻辑（加测试 + 加 CI 数据 + 扩展既有 checker），**与 D355-D360 同批落地——无论 P0 修复落在 8/20 演示前还是演示后**，不违反"演示前不动引擎"。唯一注意点：扩展 `golden-case-checker.ts` 本身是 pre-push 链路文件，变更当次需人工确认门禁行为无变化（只扩用例、不改判定逻辑）。

---

### 4.4 D397 guard（B5）——现定义砍掉，重新定义后降级

**问题**：任务卡写"通用化控制塔"。三个反对理由：
1. **违反战略铁律②**：控制塔是 dev-infra，不是 8 项独有领域能力。预算铁律刚立就开口子，"最后一次战略调整"就成了空话
2. **与既有积压重叠**：审计台账的控制塔改进队列**已排至 CT-35**（物理事实：AUDIT-FINDINGS-LEDGER.md，含 CT-35 版本号幻影门禁等 08-16 新增项），大量项未消化，M4 三次复发、CT-3 还挂着。旧账未清开新线 = 横向加东西
3. **时机危险**：演示 8/20 + P0 修复在飞，pre-commit 是提交链唯一物理阻断点——此时改控制塔 = 飞行中换引擎（V4.5.1 前 122s 超时 → --no-verify 泛滥的事故就是这么来的）

**但它借的对象里有一块真价值，给产品不给控制塔**：
- `dsh-timeout` 的设计："分享计时与分类，终止权留在本地"——适用于**诊断管道**（六阶段超时：现在单个 compute 卡死会拖死整个诊断）
- `repeat-tool-reminder`：同参连调 3/5/8 递进提醒、advisory 不否决——适用于 **`src/agent/tool-loop-executor.ts`**（产品的工具循环，防模型绕圈烧钱）
- `dsh-invariants`：运行时关系断言——思想适用于**哨兵体系**（如 "finding 必须有 evidence 引用"、"severity 与阈值档一致"）

**我的处置**：
- **D397 现定义（控制塔通用化）→ 砍掉**，合理诉求并入台账 CT 队列统一排期（不新开战略任务）
- **新立项 D397'（产品 loop 卫生，P2，Q4 后）**：诊断阶段超时 + tool-loop 重复提醒 + 哨兵 invariant。辩护理由：这是**诊断可靠性**——领域能力的一部分（S3 诊断方法论的工程质量），不是通用工程
- 神：**防跑偏的性质成立**——模拟一个卡死的 compute，诊断在超时内优雅降级（degraded: true，铁律 31）而不是挂死；模拟模型同参连调 5 次，收到递进提醒
- 形似神不似预警：超时加了但 catch 静默吞（违反铁律 24）= 超时机制反而掩盖故障。验收必须含"超时事件落事件流 + 用户可见降级提示"

---

### 4.5 D398 组织记忆（OpenViking 应对）——排最后，先看数据，红线流程化

**研究判断我部分背书**（"覆盖存储层不覆盖知识层"今天成立），但要推进一层：护城河的耐久性不在"42 边本体作为静态资产"，而在**本体被真实数据验证的速率**（§2.3）。OpenViking 让"积累"商品化后，防御不是积累更多，是积累得更结构化、更快被 GA 确认。

**Synova 的既有落点**（物理事实）：`src/l4/` 已有 `agent-memory-store.ts`（enterprise_fact 版本化 + superseded_by 链）、`department-memory-store.ts`、`knowledge-store.ts`——**不是从零开始**。

**怎么借（三片，全部"读论文/公开文档 → 独立实现"，红线流程化见下）**：

- **片 1（Q4，部署稳定后，串行位 ③）：三层投影**——给既有 enterprise_fact 生成 L0 一句话摘要（~100 token）/L1 概览/L2 原文三层；专家诊断的上下文组装（`knowledge-injector.ts`/`prompt-assembler.ts`）走 L0→L1→L2 按需下钻
  - 神：**相关性判断发生在读完整内容之前**（token 经济学）——验收：同一诊断任务，三层下钻 vs 现状全量注入的 token 消耗对比，下降可测（目标 ≥40%，参照 OpenViking 官方 34-91% 区间下限）
  - 形似神不似：三层摘要生成了但注入器没接（M3 未接线重演）——验收必须是端到端 token 对比，不是"摘要表存在"

- **片 2（看哇呢宝贝数据后做）：42 边即目录**——知识按本体边组织检索路径（"沿 CAPITAL_ALLOCATION 边找相关知识"），而不是按向量相似度。**这是与 OpenViking 的差异化核心：它有检索没有结构，Synova 有结构**——别去做向量 RAG，那是它已经赢了的战场
  - 神：检索路径可解释（沿边走的轨迹可审计，对照 OpenViking 的"可观测检索轨迹"）
  - 验收：给定一个诊断问题，知识检索轨迹可输出（走了哪些边），且 GA 能看懂

- **片 3（数据回流后，Q4 末~Q1）：会话变记忆**——诊断 session 结束后异步提取"企业偏好 + 诊断经验"到 L4，与 N13 反馈闭环并行（研究 8.6.2 方向正确）
  - 前置：D394 片 2 完成（session 已事件化，提取有源可溯）+ N13 闭环已通（否则提取出的经验无确认通道）

**红线流程化（AGPL）**："不复制代码"要从口号变成流程——① 实现者只读论文（arXiv:2605.29640）+ 官方公开文档；② **禁止打开 OpenViking 源码仓库**（写进任务卡 Q2 排除项："不访问 github.com/volcengine/OpenViking 的 /src"）；③ task brief Q1c 决策参考系记录信息来源清单（K3 审计可核）。

**研究 8.6.3 的"HTTP 服务集成"选项（我初版漏评，补上）**：研究给了第三条路——OpenViking 作为 standalone HTTP 服务集成（AGPL 服务隔离不传染）。我的评估：**现在不集成，但作为 D398 片 2 设计时的显式决策点保留**（走 D333 四步决策）。理由：① 数据边界——Synova 定位"驻扎企业内部"，企业上下文交给第三方托管服务直接违反数据安全四层（L3 原始数据仅企业内部可见）；**仅当 OpenViking 可在客户基础设施内自部署**时该选项才可成立；② 依赖风险——火山引擎可能转商业版（研究已标注），在记忆这种核心层引入外部依赖 = 把护城河地基租给别人；③ 时机——片 2 的形态取决于哇呢宝贝数据，现在选后端是提前决策。决策点触发时间：D398 片 2 开工时。

**叙事红线（研究方案§十 + 第七章 7.4 重申，交叉佐证补入）**：D398 片 3"会话变记忆"落地后，**对外叙事不得宣称"越用越准/自我进化"**——S5-1 反馈闭环未通前，这句话在产品内做了也不许在演示/销售里说。演示话术走 B 线剧本。

**工时实估**：片 1 约 1 周，片 2 约 1-2 周（且强依赖哇呢宝贝数据形态），片 3 约 1 周。**任务卡上的"1-3 天"是笑话，按笑话排期就是按谎言排期。**

---

### 4.6 研究没列、我发现的第 6 项：「findings 时间戳与生命周期正名」（不独立立项，并入 D394 片 1）

K3 08-14 审计记录的两个小而致命的缺陷，不属于任何现有 D 任务，且正好是"事件溯源思维"的最小练手场：
- `src/agent/sentinel-service.ts` 把 durationMs 当 checkedAt 时间戳（当前 :97；恒输出 1970-01-01）
- findings 无生命周期状态（open/acknowledged/resolved 在 tickets 表有，findings 本身无）

建议：并入 D394 片 1 一起做（同一批代码路径），不独立立项。**提出它的目的是示范一个原则：借鉴任务的切片应该沿着"正在流血的伤口"切，不沿着"Harness 的能力图谱"切。**

---

## 五、防"形似神不似"的元机制（制度，不是口号）

逐项防歪写在 §4 了。这里是**制度层**的五条——它们保证未来任何借鉴任务（不限于 D394-D398）自动带上防歪：

**M-1 任务卡强制两字段。** 借鉴类 task brief 增加两个 pre-commit 物理检查字段：
- `## 借的 Invariant:` ——一条可物理验证的系统性质（写不出来 → 不准开工。写不出 invariant 的借鉴 = 观光）
- `## 做歪了长什么样:` ——一段具体的反例描述（强迫任务作者在写代码前见过失败形态。这是从 Synova 自己的铁律传统学的：每条铁律都附历史事故）

**M-2 验收只验性质，不验工件。** 借鉴任务的 Done 标准禁止出现"XX 表已建/XX 文件已创建"这类工件陈述；必须是"杀进程→重建→输出一致"这类性质陈述 + 可执行命令。K3 审计时按命令复跑（claim-verifier 既有纪律的自然延伸，不新增机制类）。

**M-3 读路径切换才算数。** 凡涉及"新存储/新索引/新投影"的借鉴，读路径不切换 = 双写 = 形似。审计检查方式：grep 旧读路径调用点必须归零（铁律 47"拆完了由 grep 物理证明"的泛化）。

**M-4 神对照表回填。** 每个借鉴任务交付时回填一张三行表：Harness 原文怎么说的（README/Note 锚点）→ 我们的实现 → 偏离及理由。偏离不禁止，**不记录的偏离禁止**（M2/M7 防线的既有原则）。

**M-5 一道固定的 K3 审计问句。** 借鉴任务进审计时固定回答："这个任务的 invariant 是什么？给我看证据。"——写进 synova-audit skill 的检查清单（这是改审计预设，归因 audit，符合错误归因闭环）。

以上五条全部挂在既有防线上（task brief 门禁/claim-verifier/grep 物理证明/审计 skill），**新增机制数量 = 0**，符合一类一机制、防臃肿原则。

---

## 六、排序与依赖图（与 P0、演示、部署的关系）

```
现在 → 8/20 演示
│  · 借鉴任务零接触 src/ 业务逻辑（原决策正确，维持）
│  · 例外：D396 黄金用例扩充与 D355-D360 同批（加测试/CI 数据，不碰引擎）
│  · D395-a（开发组织四态，1 天，纯文档/流程）随手做
│
9 月部署期（P0 修复为绝对主线）
│  · D355-D360（P0 全链路修复，最高优先，每个修复带黄金用例）
│  · S4（GA 闭环）+ S8（无数据诊断）做深——哇呢宝贝部署验收项，优先级高于一切借鉴（研究第七章阶段 1，本备忘确认）
│  · D394 片 1（哨兵 findings 事件化 + §4.6 时间戳正名）——与 P0 并行，不同代码路径
│
Q4（部署数据开始回流；src/ 级借鉴严格串行，顺序如下）
│  · ① D394 片 2（诊断会话事件化，硬依赖 D355-D360 完成）
│  · ② D397'（产品 loop 卫生：诊断超时 + 重复提醒）
│  · ③ D398 片 1（三层投影）
│
数据回流后（Q4 末 ~ Q1）
│  · D398 片 2（42 边即目录）← 必须先看到真实企业数据形态
│  · D398 片 3（会话变记忆）← 前置 D394 片 2 + N13 闭环
│  · D394 片 3（fork/resume）→ 路径 A 评估的预备
│  · 路径 A 评估（按 §7 rubric，不是按日历）
│
多客户后
   · 路径 C（领域层 SDK/插件分发）、B6 spill、B7 sandbox、B8/D9 subagent seam
```

**并发硬上限：src/ 级借鉴任务在飞数 ≤ 1（Q4 三项严格按 ①②③ 串行）。** D396（测试/CI 数据）与 D395-a（纯文档/流程）不占此额度，但同样走 PR + K3 审计链。依据不是战略是带宽：M4 三次复发证明执行证据链在并行多线下已断过三次；审计线是 K3 单线；编码线同时要交 P0 修复。

**带宽预算上限（克制的量化，交叉佐证后新增）：借鉴类任务（含 D394/D397'/D398 全部片）在任意月度窗口 ≤ 总工程带宽的 25%，其余 ≥75% 给 P0 修复与 S1-S9 领域做深。** 没有这条，"路径 B 渐进"会在每个季度悄悄吃掉领域预算——预算铁律②需要一条可核账的数字。

**D397 现定义（控制塔通用化）正式建议砍掉**，合理部分并入台账 CT 队列。这不是"少做一件事"，是"保护刚立的预算铁律不被自己开口子"。

---

## 七、路径 A 评估 rubric（现在写，不等 Q4）

"Q4 数据回收后评估"如果没有判据，会退化成永久 deferral 或又一次拍脑袋。我建议本周就把触发条件写死（挂在 D394 任务卡或 CTO 交接文档）：

| # | 条件 | 判据来源 |
|---|------|---------|
| A1 | Harness 发布 ≥v0.2 且官方撤回"破坏性变更"承诺（或变更频率 < 每季度 1 次） | Harness CHANGELOG |
| A2 | 路径 B 已做到位：事件溯源片 1+2 完成、providers 已 seam 化（迁移成本已降） | Synova 代码物理状态 |
| A3 | ≥2 家企业部署跑通，且工程时间 >30% 消耗在通用工程维护上（双维护成本被数据证实） | 工时记录 |
| A4 | 出现 Synova 必需且自研成本 > 迁移成本的 Harness 独有能力（如远程沙箱 subagent） | 需求触发 |

**满足 ≥3 条 → 正式启动路径 A 评估（评估 ≠ 执行）；否则继续 B。** 注意 A2 的含义：路径 B 做得越扎实，路径 A 越便宜——**B 和 A 不是竞争关系，B 是 A 的降本前置**。这也是"接缝纪律"（每个借鉴件落在五层合法位置 + 包本地接口）的真正目的：不是工程洁癖，是保留 A 的可行性。

---

## 八、如果只做三件事

1. **D396 提前到 P0 同批**：每个 D355-D360 修复带一条黄金用例，扩展既有 golden-case-checker，不做新体系。成本 2-3 天，保护的是 9 月部署和整个 Q4 战略链。
2. **D394 改切片**：第一刀切"哨兵 findings 事件化"（治重启即丢的真伤口），不切"全量会话事件溯源"（那是按 Harness 图谱切的形）。任务卡写上三条 invariant（可重建/单源/可审计）+ "做歪了长什么样"。
3. **D397 砍、D398 慢**：控制塔通用化并入 CT 队列（守预算铁律）；组织记忆等哇呢宝贝数据回流再动手，成功标准定义为"GA 确认率"而非"条目数"。

其余维持已定决策：战略三铁律、路径 B 为主、演示部署优先、OpenViking 红线——**这些我核实后都背书**。

---

## 九、与研究 11 份文档的交叉佐证（全覆盖矩阵 + 偏离台账）

> 本节回应创始人要求：本备忘与研究系列（研究方案 + 第一~八章 + 附录 + 开发转交，共 11 份）逐份互核。结论：**主干互相印证，无方向冲突；本备忘对研究有 4 处显式偏离（均附理由）+ 从后读的 4 份中补入 4 处内容。**

### 9.1 逐份覆盖矩阵

| 研究文档 | 核心结论 | 本备忘立场 | 关系 |
|---|---|---|---|
| 研究方案 | 定位不动；克制纪律；持续学习红线；分治边界（冲击演示>2周即暂停对齐） | 全部背书；叙事红线已补入 §4.5；本备忘全部建议对演示冲击 ≤3 天且不碰 src/ 业务逻辑，合规 | ✅ 印证 |
| 第一章（包族解构） | 三范式（无特权核心/事件溯源/seam）；工程化体系（Notes/snapshot/verify） | 物理复核确认（§0.1）；feedback log-only 缺口补入 §4.1 片 2 | ✅ 印证 + 补强 |
| 第二章（插件生态） | 生态引爆（1320 插件/44.8 万 star）；"Agent 能力"类最大；路径 C 有现成分发管道；治理风险是软肋 | 印证铁律①；路径 C 维持"多客户后"；**新增运维红线（见 9.2-补 3）** | ✅ 印证 + 补强 |
| 第三章（同类项目） | Sylph 证明公司大脑模板化但无因果深度；框架层不冲突；OpenViking 唯一真威胁 | 已把 Sylph 证据并入 §2.3（护城河精确化为因果深度 × 验证速率） | ✅ 印证 + 补强 |
| 第四章（映射矩阵） | 8 独有 / 4 可替代 / 8 可借鉴 / 双缺 | 全部采纳为分析前提 | ✅ 印证 |
| 第五章（三路径） | A 现在不做 / B 主路径 / C 增强 | 背书；补路径 A 触发 rubric（§七）把"Q4 评估"从日历变成判据 | ✅ 印证 + 补强 |
| 第六章（借鉴+特色） | B1-B8 借鉴清单；S1-S9 特色清单 | B1-B8 全部覆盖（见 9.2 偏离台账）；S1-S9 做深是研究地盘，本备忘只加预算约束（§六 25% 上限） | ✅ 印证 + 4 偏离 |
| 第七章（路线图） | 四阶段；9 月部署期 S4+S8 做深 | S4/S8 已补入 §六时间线（此前遗漏） | ✅ 印证 + 补强 |
| 第八章（OpenViking） | 存储层 vs 知识层；AGPL 红线；S5-3/S5-4 转向；HTTP 集成选项 | 背书并推进一层（验证速率指标）；HTTP 集成选项已补评估（§4.5：现在不集成，留作片 2 显式决策点） | ✅ 印证 + 补强 |
| 附录（证据清单） | H-01~13 / E-01~09 / S-01~04 | H-07 促使本备忘软化修正 2（npm vs 源码口径）；S-01（N13 断裂）已体现为 D398 片 3 前置条件 | ✅ 印证 + 修正自身 |
| 开发转交 | D1-D5 P1 + D6-D9 P2；演示前不动 | D1-D9 全部覆盖：D1→D394 改切片、D2→D395 拆、D3→D396 提前、D4→D397 砍并、D5→D398 缓、D6-D9 维持多客户后 | ✅ 全覆盖 + 4 偏离 |

### 9.2 本备忘对研究的 4 处显式偏离（创始人需知晓：这是有证据的改写，不是误解）

| # | 研究/转交原文 | 本备忘处置 | 理由（证据） |
|---|---|---|---|
| 1 | D1/B1：事件溯源 session log，P1 部署期启动 | 改为三片，第一刀切**哨兵 findings 事件化**而非会话 | findings 重启即丢（08-14 审计实证 + grep 无 findings 表）；会话事件化硬依赖 L4/L5 契约先稳定 |
| 2 | D3/B3：snapshot 测试，P1（9 月后） | **提前到与 P0 修复同批** | GS 场景验收是一次性的（FOUNDER-OPERATING-MODE Phase 1），未固化为回归门禁；不碰 src/ 故不违反演示冻结 |
| 3 | D4/B5：guard"融入控制塔"，通用化 | **砍**（并入 CT 队列），产品 loop 卫生另立 D397' 排 Q4 | 预算铁律②；CT 队列已排至 CT-35 未消化；M4 三次复发期改控制塔 = 飞行中换引擎 |
| 4 | D5/8.6：S5-3/S5-4 转向"借机制+42边" | 方向背书，但**排期推后到数据回流后**，成功标准改为 GA 确认率 | 记忆形态必须先看到哇呢宝贝真实数据；提前臆造 = 建完返工 |

### 9.3 从后读 4 份中补入本备忘的 4 处内容

1. **叙事红线**（研究方案§十/第七章 7.4）：D398"会话变记忆"落地后对外不得宣称"越用越准/自我进化"（S5-1 未闭环）→ 已入 §4.5。
2. **Sylph 证据**（第三章）：公司大脑模板层也在商品化，护城河精确化为"因果诊断深度 × 数据验证速率" → 已入 §2.3。
3. **插件治理的近期运维红线**（第二章 2.5）：DSH 市场 1320 插件**无来源验证**（一个存储插件可掌握完整会话）——Synova 开发组织在装有产品仓库的机器上安装第三方 dsh 插件必须走白名单审批，**默认不装**；供应链风险直达产品仓库皇冠明珠。远期含义不变：路径 C 的差异化 = "可信企业级插件"（Synova 的控制塔/审计基因恰是 Harness 生态软肋的反面）。
4. **S4/S8 部署期做深**（第七章阶段 1）：9 月部署期主线除 P0 外含 GA 闭环 + 无数据诊断（部署验收项，优先级高于一切借鉴）→ 已入 §六时间线。

### 9.4 佐证总结论

- **无方向性矛盾**：研究的战略三铁律、路径 B 为主、OpenViking 应对方向，与本备忘全部一致。
- **本备忘的增量在"执行保真"**：研究回答了"借什么、走哪条路"（正确），本备忘回答"怎么防止借歪、按什么顺序、花多少预算"（invariant 验收制 / 伤口排序 / 25% 带宽上限 / 串行上限 1）。
- **两份文档的关系**：研究是战略（对），本备忘是工程的防滑垫（让对的战略不被执行变形消耗掉）。建议：本备忘 §五的 M-1~M-5 机制由创始人批准后落入 dev-doc 线任务卡模板，研究的 B1-B8 优先级以本备忘 §六时间线为准刷新。

---

> **免责与边界**：本备忘为咨询意见，不修改任何 dev doc/task brief/任务状态的效力；D394-D398 的任务卡改写归 dev-doc 线；砍/并/提级的最终决定权在创始人。
> **归因备查**：本备忘涉及的四类修正分别归——D395 拆分（归因 devdoc：任务卡对象误认）；D396 提前（归因 audit：原排序未发现回归网固化缺口）；D397 砍并（归因 control-tower：CT 积压与预算铁律冲突）；D398 工期与红线流程（归因 devdoc：工时失实 + 红线未流程化）。交叉佐证后新增：修正 2 软化（归因 audit 自身：初版未读附录，npm/源码口径未分）。
