# K3 复审报告 — D399「D393 审计 FAIL 修复」

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-08-16 | 协议: AUDIT-PROTOCOL v1.0 §步骤 6（修复后复审，只审变更项）
> 复审对象: `feat/d399-audit-fix` 分支 `082326f` 相对 `ffe3f52` 的变更
> 范围注解: `git diff ffe3f52..082326fc` 含 4 提交，其中 D399 自有变更 = `082326f` 单提交 8 文件 +167/-11；
> 其余为 merge 6f7f0a4/6b117b0 带入的 main 同步（11 研究文档，非 D399 修复内容，不计入本次判定——见 N4）。
> 首审: [2026-08-16-D393.md](2026-08-16-D393.md)（结论 FAIL，P0×1 / P1×4 / P2×8）

## 结论: 🟡 CONDITIONAL PASS（P0-1 已闭合；P1-1 未闭合且修复提交重现同款机制；P1-2 代码修复属实但交付产物仍带幻影；P1-3/P1-4/P2-1 闭合）

首审阻断项（P0-1）已消除：纯净 worktree @ 082326f 实跑验收测试 **PASS=7 FAIL=0, exit=0**，提交态断言成立。
但修复任务自设 Done 标准第 2 条「CTO-HEALTH 提交态可复现」在交付态**经实测不成立**——提交的产物仍含
5 个幻影行（D394-D398，源 json 未入库）+ 3 个幻影 spec ✅（D356/D379/D391）+ 1 处计数不可复现（门禁拒绝 7 vs 0）
+ D399 行自指矛盾（同 P0-1 机理在修复任务自身重现）。需一次「纯净态重生成 + 入库/剔除」收尾提交方可转 PASS。

---

## 运行环境注记

Mac (darwin) / 纯净 worktree `/tmp/d399-review` @ 082326f（已用后清理）/ python3 可用 / 网络受限仅本地 refs +
定向 fetch 成功（`git fetch origin feat/d399-audit-fix`）。「无 git 环境」用 `env -i PATH=/tmp/nogit-bin`
（仅含 python3 符号链接）构造，规避 /usr/bin/git shim 干扰。

## 材料清单

| # | 材料 | 状态 |
|---|------|------|
| 1 | 提交集 | 082326f（D399 自有）；ffe3f52 是其祖先（merge-base 实证） |
| 2 | Git diff | 082326f^..082326f 全量逐行读 + ffe3f52..082326fc range 对账 |
| 3 | 首审报告 | docs/synova/audit-reports/2026-08-16-D393.md ✓ |
| 4 | Task brief | .claude/task-briefs/2026-08-16-D399-audit-fix.md（随 082326f 入库）✓ |
| 5 | 首审修复指引 | D393 报告 §修复指引 6 条 |
| 6 | 实测 | 纯净 worktree dry-run + 全量测试 + 无 git 环境降级复验 |

---

## 逐项核对（P0-1 / P1×4 / P2-1）

### P0-1 验收测试交付态红灯 — ✅ 已闭合

- 修复方式 = 首审指引选项 1：断言对齐交付态真值。test.sh:70-71 `| D393 | claimed` → `| D393 | impl_done`，
  注释注明「提交即 impl 工件, 交付态对齐」。
- **物理复验**（worktree @ 082326f 实跑）: `bash tests/control-tower/gen-cto-health.test.sh` → **PASS=7 FAIL=0, exit=0**。
  首审红灯断言已转绿，且断言对象（D393 派生值）与生成器规则自洽：dry-run 输出 `| D393 | impl_done | — | ✅ |`。
- 提交的 CTO-HEALTH.md 中 D393 行 = impl_done ✓，仪表盘与工具规则在 D393 上不再矛盾。
- 残留（不阻断，见 N2）：同一自指机理在 D399 行重现——结构问题未解，仅解了 D393 个案。

### P1-1 提交产物不可复现 — ❌ 未闭合（部分修复 + 同款机制重现）

首审 4 处失真逐条核：

| 首审失真 | 修复判定 | 证据 |
|---------|---------|------|
| ① D393 行 claimed→impl_done 矛盾 | ✅ 闭合 | 提交产物 D393=impl_done，与派生一致 |
| ② D385/D391 行源自未入库 json | ⚠️ 半闭合 | task-state/D385.json、D391.json **已入库** ✓（082326f 新增）；D385 行复现一致 ✓；**但 D391 行仍是幻影**（见下） |
| ③ 事件计数源自未入库日志 | ✅ 闭合 | bypass.log +23 行入库；纯净重跑 §一 计数与提交产物**逐字一致**（45/18、08-16:22 等，diff 零 §一 块） |
| ④ 指纹机器相关 | ➖ 未动 | 仍机器相关（d5b9c795ceb7 vs 纯净 1fb6dfc2452d），属 P2-8 既知项 |

**未闭合证据（纯净态重生成 vs 提交产物，物理实测 diff）**：

1. **D394-D398 五个幻影行** — 提交产物含 `| D394 | claimed |`…`| D398 | claimed |` 共 5 行，但
   task-state/ 在 082326f 全树仅 13 个 json（D356/D379/D383-D393/D399），**无 D394-D398**。
   即修复提交本身用首审 P1-1 的同款机制（读未提交本地文件）引入了 5 个新的不可复现行。
2. **D391 行幻影 spec ✅** — 提交产物 `| D391 | spec_done | ✅ |`；纯净重跑 = `| D391 | claimed | — |`。
   D391.json 的 spec.path（SYNOVA-IMPL-DSH-D391-...md）在 082326f 全树不存在
   （`git cat-file -e` fatal；implementation/ 目录 182 个文件中无 D391）。
   新代码 exists() 兜底在作者脏工作区为真（本地有未提交 dev doc）、在仓库态为假 → 提交产物仍幻影。
3. **门禁拒绝计数不可复现** — 提交产物「累计拒绝: **7** 次 | 最近: 2026-08-16」，纯净重跑 =「0 次 | 无」。
   源 .claude/pre-commit-failures.log **全 git 未跟踪**（`git ls-files` 零命中），任何机器都复现不了 7。
4. **D399 行自指矛盾** — 提交产物 `| D399 | claimed |`；交付提交 subject `fix(D399): ...` 使派生 = impl_done
   （纯净 dry-run 实证）。P0-1 的仪表盘版在修复任务自身重现。
5. brief Q1b 自设 verify「worktree 纯净态重跑 == 提交产物（除时间戳/指纹）」+ Done 标准第 2 条「提交态可复现」
   —— 上述 1-4 证明该验收**在交付态不成立**（声称 vs 事实，M2 复发）。

首审指引第 2 条后半「生成器对未提交源告警」**未实现**（gen-cto-health.py diff 仅 14 行，无未提交源检测逻辑）。

### P1-2 spec 列双向失真 — ⚠️ 代码修复属实 / 交付产物仍失真 / 语义矛盾未解

- **假阴性（glob 漏检）**: ✅ 闭合。gen-cto-health.py:198 `SYNOVA-IMPL-DSH-D*.md` → `SYNOVA-IMPL-D*.md`，
  实测 082326f 树匹配 **182** 个文件（首审时仅 1 个）；主流命名 `SYNOVA-IMPL-D###-*` 全部覆盖
  （DSH- 前缀文件亦被 `D*` 兼容）。D387 spec ✅ 源自真实文件 ✓。
- **假阳性（幻影 spec ✅）**: 代码 ✅ / 产物 ❌。gen-cto-health.py:224-227 json 兜底增加
  `(REPO / d["spec"]["path"]).exists()` 真实存在校验；纯净重跑 D356/D379 spec 列已由 ✅ 转 **—**（幻影消除 ✓）。
  **但提交产物仍显示 D356/D379/D391 三个 spec ✅** —— 产物是在脏工作区生成的，代码修复未反映到交付物。
- **README/注释 vs 代码语义矛盾未解**: 首审指引第 3 条要求「json 兜底与 README 二选一」。
  实际：代码保留 json 参与派生（path 存在即算真），而 task-state/README.md:24 仍写「生成器忽略（deprecated）」，
  代码上方注释仍写「json 字段兜底展示但**不算真**」（082326f:223）——三者矛盾原样保留，二选一未做。
  （brief Q2 声明「不改 README（deprecated 语义已对）」，该声明与代码行为不符。）

### P1-3 CTO 备注区被清空 — ✅ 已闭合（保真度注记）

- 提交产物 MANUAL 区由占位符 `(CTO 备注区)` 恢复为实质内容，并带内联承认「（人工维护 — D399 P1-3 恢复）」
  「此前被误清」——满足首审指引第 4 条「恢复**或**明示删除决策」。
- 保真度注记：恢复文本为**改写版**而非原文。原文（ffe3f52^ 树取回）含「门禁健康 ✅ 零绕过 / CI 全红 10/10 /
  P0 积压 6 项 / ~335 文件未提交 / 员工状态」5 段；新版保留红灯主线（CI Architecture 预存红、P0 积压、D393 FAIL）
  并新增 npm audit 豁免、战略定调、审计闭环 3 条。信息主线等价、细节有取舍，可接受，但「恢复」严格说是「重写+补记」。

### P1-4 08-16 批次门禁记录未入库 — ✅ 已闭合

- bypass.log +23 行覆盖 08-16 批次：D388×3（含补记+CLARIFIED 误报说明）、D389×3、D390×2、D392×1、
  **D393×1（HASH=ffe3f526…，与首审提交逐字一致）**、merge 引入 Win 机补记 ×9、autostash 回滚补记 ×5。
- 效果实证：提交产物 §一 事件计数从提交态可复现（P1-1 表 ③）。
- 结构性注记（非缺陷）：082326f 自身的 pre-commit PASS 记录无法在自提交内入库，顺延下一批次——
  对账时请核下批 bypass.log 是否含 `HASH=082326f…`。
- 数据质量小注：D387 补记 ×4、D355 ×2 重复行（不同 HASH 各一条），已注明「补记」来源，不判失真。

### P2-1 git 失败静默降级（注释 overclaim + 无标记） — ✅ 已闭合

- gen-cto-health.py:193-196：注释删去 overclaim（「标注 degraded」→「派生降级」），并新增真实标记
  `print('⚠ degraded: git log 不可用, impl 派生降级为空集 (D399 P2-1)', file=sys.stderr)`。
- **物理复验**（env -i 无 git 环境实跑）：stderr 出现上述标记 ✓；impl 列正确降级为空集
  （D393/D390 转 claimed、impl —）✓；exit=0 不崩 ✓。
- 残留（P2 级可接受）：标记仅到 stderr，未传播进仪表盘产物本身（铁律 31 全义要求消费方可见）；
  对照首审定级 P2「可选改进」，现状已达「诚实注释 + 运行期可观测」，判闭合。

---

## 复审新发现（N 项）

| # | 级 | 项 | 证据 |
|---|----|----|------|
| N1 | **P1** | 修复提交重现 P1-1 机制：脏工作区生成 → 5 幻影行（D394-D398）+ 3 幻影 spec ✅ 随产物入库 | 本报告 P1-1 未闭合证据 1/2 |
| N2 | P2 | D399 行自指矛盾：断言对齐法只修测试不修产物；凡修复类任务提交，其行必在提交态失真。结构解 = 派生规则排除本任务自身提交 / 提交后重生成再 amend / 接受并文档化（设计决策归 CTO） | 纯净 dry-run `\| D399 \| impl_done` vs 提交产物 `claimed` |
| N3 | P2 | 门禁拒绝计数源 .claude/pre-commit-failures.log 全 git 未跟踪 → 该计数**永久性跨机不可复现**（提交 7 vs 纯净 0）；要么入库跟踪、要么从产物剔除该指标 | `git ls-files .claude/` 零命中 |
| N4 | P2 | 分支堆叠复发（首审 P2-6 同款）：复审 range 含 merge 带入的 11 研究文档（6da2a5c），PR diff ≠ D399-only | `git log ffe3f52..082326fc` 4 提交 |
| N5 | P1 | 首审指引第 5 条未执行：控制塔测试套件仍未接入任何门禁（pre-commit 13 组零引用、.github/workflows 零引用）——本次 7/7 绿仍无任何物理拦截，M1 缺口敞开 | grep pre-commit-check.sh / workflows 零命中 |

## 首审 P2 余项状态（不在本次复审范围，仅登记）

P2-2（多报告取最早）/ P2-3（impl ✅ 语义含 docs 提交）/ P2-4（测试副作用就地重写产物——**本次复审实测再现**：
跑测试即改写 worktree 的 CTO-HEALTH.md）/ P2-5（测试节号 1,2,3,5）/ P2-6（→N4 复发）/ P2-7（D399 仍无 dev doc，
brief 唯一来源）/ P2-8（指纹机器相关，P1-1 表 ④）—— 全部原样未动，可继续挂账。

## 变更项 14 项语义审计（针对 082326f diff）

| # | 项 | 结论 | 证据 |
|---|----|------|------|
| 1 | 接口真实性 | ✅ | exists()/print stderr 均为标准库真实调用 |
| 2 | 文件路径存在性 | ❌ | D391.json spec.path 指向不存在文件（幻影源） |
| 3 | Edge ID | N/A | — |
| 4 | 架构边界 | ✅ | 零 src/ 变更；scripts/+tests/+docs |
| 5 | 数据流完整性 | ⚠️ | 派生链通；但输入仍读工作区未校验入库态（N1） |
| 6 | 接线深度 | ❌ | 测试仍未接入门禁（N5，首审 ❌ 项未动） |
| 7 | 降级诚实性 | ✅ | P2-1 闭合（stderr 标记 + 注释去 overclaim，实测） |
| 8 | 测试契约 | ✅ | 断言对象与生成器规则自洽（P0-1 闭合） |
| 9 | 测试覆盖 | ⚠️ | 7/7 绿；仍缺幻影 spec/未提交源用例（N1 类缺陷无测试兜底） |
| 10 | 文件驱动 | N/A | — |
| 11 | 自我报告交叉对比 | ❌ | brief verify「纯净重跑 == 提交产物」与 Done#2 交付态不成立（实测 4 处差异） |
| 12 | dev doc 偏离 | ⚠️ | 写集 8 文件 = 实际 8 文件 ✓；Q2 排除项遵守 ✓；无 dev doc（P2-7 沿） |
| 13 | 控制塔执行 | ⚠️ | bypass.log 08-16 批次已入库 ✓；082326f 自身记录顺延下批（结构性）；CI 状态本次未核（复审范围外） |
| 14 | 版本编排 | N/A | — |

## L4 防线缺口收割（复审视角）

1. **N1（修复提交重现被修缺陷）**: 本该拦住的是 brief Q1b 自设 verify「纯净态重跑 == 提交产物」——声明了但
   **无任何物理执行机制**，全靠作者自觉，且作者显然在脏工作区跑了生成器。命中 M1（检查未执行==检查通过）+
   M2（声称 vs 事实）。强化（不新设 M 类）：把「纯净态重生成 diff 比对」做成 check-*.sh 挂 pre-commit 组 10，
   或将首审指引 #5（测试入 CI）与之一并落地——**首审已开出处方，本任务是第二次漏服**。
2. **N2（自指结构复发）**: 断言对齐法证伪了「修个案」路线的完备性。缺一道「交付后状态复验」的批次收尾门禁
   （首审 L4 已指出：Done 含派生指标须以推送后远端态复验）——仍未建制。
3. **有效防线登记**: 本次复审所依赖的纯净 worktree 重跑法再次证明可物理证伪产物诚实性（D393→D399 连续两次命中）。

**归因汇总**: implement ×2（N1 产物未纯净重生成 / N2 结构未解）· control-tower ×1（N5 指引 #5 未落地）· audit ×0。

## 转 PASS 条件（一次收尾提交即可全闭合）

1. 纯净态重跑 gen-cto-health.py 并以结果覆盖 CTO-HEALTH.md（消除 5 幻影行 + 3 幻影 ✅ + D399 行），
   **或**将 task-state/D394-D398.json、D391 dev doc 真实入库后再重生成；
2. json 兜底 vs README 二选一（建议按首审指引：json 仅展示不参与派生，同步改 082326f:223 注释）；
3. 下一批次 bypass.log 须含 082326f 的 pre-commit PASS 记录（对账项，非本提交职责）；
4. （强烈建议，首审指引 #5 第二次开具）控制塔测试套件接入 pre-commit 组 10 或 CI quality job。

---
*报告路径: docs/synova/audit-reports/2026-08-16-D399-review.md | 分支: audit/d399-review | K3 独立产出，未改产品代码/审计脚本*
