# K3 审计报告 — D394「哨兵 findings 事件化（片 1）」spec 关卡审计

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-08-17 | 协议: AUDIT-PROTOCOL v1.0
> 审计对象: D394 spec 交付 —— `feat/devdoc-4-spec-20260816` 分支 `4df237a`+`548b206`
> 任务状态: `task-state/D394.json` @devdoc 分支 = **spec_done**（spec/impl/audit 三段：spec ✅ / impl null / audit null）
> 性质注记: 本次审计的是 **spec（活规格）交付物**，非代码交付。代码尚未存在（impl=null），
> 本报告结论是"spec 是否可放行进入实现"的关卡判定，不替代未来的 impl 交付审计。

## 结论: 🟡 CONDITIONAL PASS（spec 可放行进入实现；P0×0 / P1×1 / P2×5）

spec 本身质量为派活批最高水准候选：14 项语义审计中**接口/路径/行号声称 17 处全部物理命中**，
对 K3 咨询 §4.1 的保真度 100%（I1/I2/I3 + 形似神不似预警 + 验收锚点逐字携带），
且 dev-doc 线自救补上了派活 brief 漏掉的 §4.6 后半（findings 生命周期正名）。
两项转 PASS 条件见文末，均不阻塞 spec 进入实现，但 **P2-1/P2-2 必须在写代码前收敛**（涉及事件 schema，事后改贵）。

---

## 运行环境注记

Mac (darwin) 独立审计工作区 `/Users/wane/Synova-k3独立审计` / git 网络可用（ls-remote + 定向 fetch 均成功）/
全部验证基于 `origin/main@df772d1` 与 `origin/feat/devdoc-4-spec-20260816@548b206` 的 git 树（`git show` 只读，不切工作区脏状态）/
无本地 git hooks（审计工作区常态，本审计不提交代码）。

## 材料清单（7 项自收集）

| # | 材料 | 状态 |
|---|------|------|
| 1 | 任务提交集 | `git log origin/main..origin/feat/devdoc-4-spec-20260816` = 4df237a（4 spec 批量交付）+ 548b206（spec.commit 回填） |
| 2 | Git diff | 4df237a --stat：4 份 spec（D394 232 行/D395a 192/D396 202/D402 206）+ 4 个 task-state json；零代码 |
| 3 | Dev doc | `docs/plans/codex/implementation/SYNOVA-IMPL-D394-sentinel-events-20260816.md`（实际文件名带 DSH 中缀：`SYNOVA-IMPL-DSH-D394-sentinel-events-20260816.md`）✅ |
| 4 | Task brief | `.claude/task-briefs/D394-sentinel-events.md`（D403 派活入库）✅ |
| 5 | AGENTS.md + PRODUCT-BRIEF.md | 铁律 0-2/24/31/32/39/47/48 + `.claude/PRODUCT-BRIEF.md` §一/§三 |
| 6 | 审计基线 | 免（零代码变更；纯 spec 提交） |
| 7 | 执行证据 | bypass.log @main 末行停于 `2026-08-16T21:26:14 D400 BLOCKED`——**6 个后续提交零记录**（见 P1-1） |

**溯源注记**：本 session 开工时本地仓库落后远程 25 个提交（D394 材料全部不在本地），
经 ls-remote 全量分支普查 + 定向 fetch main/devdoc 分支补齐——D391 复审同款"单 ref fetch 救场"。

---

## 14 项逐项审计

| # | 项 | 判定 | 证据（file:line） |
|---|----|------|------------------|
| 1 | 接口真实性 | ✅ 17/17 命中 | 见下「接口声称核验表」 |
| 2 | 文件路径存在性 | ✅ | spec 引用 5 路径全部存在于 main：runner.ts / sentinel-service.ts / types.ts / 咨询 memo / 08-14 审计报告 |
| 3 | Edge ID 有效性 | ➖ N/A | spec 无 E-XX 引用 |
| 4 | 架构边界 | ✅（论证成立） | sentinel_events 表由 runner（L3）持 db 句柄直写——与预存 `sentinel_tickets`（runner.ts:154 建表 / :423 INSERT）、`sentinel_baselines`（baseline-store.ts:198）同款先例，未新增跨层违规类型；spec §1 显式引用铁律 39 并给出同款论证 |
| 5 | 数据流完整性 | ✅ | executeSentinel→appendSentinelEvent→sentinel_events→replay→records 投影→getRecentResults→sentinel-service→`GET /api/sentinel/findings`（routes/sentinel.ts:25 实测存在），链路每环有出处 |
| 6 | 接线深度 | ✅（spec 层） | §2.4 现状接线表 grep 实测命中（sentinel-service.ts:13 `import { getGlobalSentinelRunner }`、:85 `getRecentResults()`）；§5 对未来接线的 grep 验证式定义明确 |
| 7 | 降级诚实性 | ✅ | append 契约 @degraded = log.error + 抛 `SentinelsEventError(.code/.phase='persist'/.retryable)`（铁律 32 三字段齐）；db 不可用 fail-closed 不静默——未复制 runner.ts:152-166 预存的 `catch { log.debug }` 静默模式（存量，见观察项 O1） |
| 8 | 测试契约（铁律 47） | ⚠️ P2-5 | appendSentinelEvent 有完整 @input/@output/@degraded/@error JSDoc 契约；createSentinelEventsTable/replaySentinelEvents 仅散文描述，spec 层可接受、impl 前需补齐 |
| 9 | 测试覆盖（铁律 48） | ✅ | §4 red→green 12 用例表：正常/降级/边界 + I1×2/I2/I3 + 生命周期×2 + tickets 回归，每用例标注修复前 red 形态 |
| 10 | 文件驱动 | ✅ | 不新增哨兵类型/不动 manifest（§3.3 显式排除 registry/manifest 变更） |
| 11 | 自我报告交叉对比 | ✅ | commit msg「K3 咨询 436 行原文校准」——memo 实测 436 行 ✅；「task-state 转 spec_done」diff 实证 ✅；548b206 回填 spec.commit=4df237a —— `git cat-file -e 4df237a:<spec>` 存在 ✅ |
| 12 | dev doc 偏离审计 | ✅（一处 brief 层缺口 P2-4） | spec §3.1 写集（3 修改+2 新建）与分支实际 diff（纯文档）性质一致——写集是实现期计划，spec 交付未夹带代码 ✅；§3.3 片 2/片 3 显式排除，守住咨询切片边界 |
| 13 | 控制塔执行审计 | ❌ P1-1 | bypass.log @main/@devdoc 分支末行均停于 D400 BLOCKED 21:26:14；其后的 bfb6b4a(D400 自身)/8cfac21(D403)/1336d6b(D404)/972aa87(D405)/4df237a/548b206 **6 提交零执行证据**；gatekeeper 运行亦无痕迹（dispatch 要求 `dev-doc-gatekeeper.sh → exit 0`，commit msg 与日志均无记录） |
| 14 | 版本编排合规 | ➖ N/A | 纯 spec 提交无版本声称 |

### 接口声称核验表（item 1 明细，全部 @main df772d1 实测）

| spec 声称 | 实测 | 判定 |
|-----------|------|------|
| runner.ts:125 `private records = new Map` | :125 逐字命中 | ✅ |
| runner.ts:652-664 push 后无落库、只留 50 条 | :659-663 `history.push`/`>50 shift`/`records.set`，无任何 INSERT | ✅ |
| runner.ts:153-166 建 sentinel_tickets 表 | :150 `start()` + :154 CREATE TABLE | ✅ |
| runner.ts:161 工单 status CHECK 四值 | :161 `CHECK(status IN ('open','acknowledged','resolved','dismissed'))` | ✅ |
| runner.ts:422-429 写工单 | :423 `INSERT OR REPLACE INTO sentinel_tickets` | ✅ |
| sentinel-service.ts:97 durationMs 当时间戳 | :97 `checkedAt: new Date(run.result.durationMs).toISOString()`（durationMs=15 → 1970-01-01T00:00:00.015Z，bug 成立） | ✅ |
| sentinel-service.ts:198/:226 已正确用 f.detectedAt | :198/:226 均 `f.detectedAt` | ✅ |
| sentinel-service.ts:13 getGlobalSentinelRunner | :13 import 命中 | ✅ |
| types.ts:71 `checkedAt: string` 已存在 | L65-75 窗口内 :71 命中 | ✅ |
| types.ts:41-58 SentinelFinding 无 status 字段 | 接口全文 grep `status` 零命中 | ✅ |
| 「findings/signals 未持久化」 | main 全树 CREATE TABLE 清单：sentinel_baselines + sentinel_tickets，无 findings/events 表 | ✅ |
| 验收锚点 `GET /api/sentinel/findings` | routes/sentinel.ts:25 路由存在 | ✅ |

## 北星复判（第二道，north-star-guard 三问）

spec 带完整 north-star front-matter（硬前提 ✅）。对照 `.claude/PRODUCT-BRIEF.md` 原文机械复核：

1. **服务真实用户场景吗** —— ✅。§一 L14「Synova：7×24 运行。自己发现信号」+ §三 L28「哨兵定时巡检：25 个哨兵 7×24 自动跑 → 发现异常」逐字命中；findings 重启即丢直接违背该承诺，本任务治的正是这道流血伤口。
2. **让模块更接近终态吗** —— ✅。哨兵状态持久化/可审计是驻扎 Agent 的地基能力，朝终态走一步，非横向加法。
3. **变味吗** —— ✅ 否。内部管道加固，产品仍是"驻扎企业、主动诊断、为增长服务"的 Agent。

front-matter 引用 §六 P0「哨兵真实数据流：表建了但空着」——PRODUCT-BRIEF L67 逐字命中 ✅。

## 发现清单

### P1（建议修复，不阻 spec 放行）

| # | 发现 | 证据 | 归因 |
|---|------|------|------|
| P1-1 | **执行证据链断裂（M4 第 N 次复发）**：bypass.log 末行停于 `2026-08-16T21:26:14 D400 BLOCKED (exit 1)`；其后 6 个内容提交——bfb6b4a（D400 自身，被拦后 **86 秒**即提交成功，无 PASS 也无 bypass 记录，审计不可判定是修复后通过还是 --no-verify）/8cfac21（D403）/1336d6b（D404）/972aa87（D405）/4df237a/548b206（devdoc 分支）——**仓库态零执行证据**。D399 复审确立的「PASS 记录顺延下一批补记」纪律在紧接的下一批（D403）就断了；dev-doc-gatekeeper 运行亦无痕迹 | `git show df772d1:.claude/bypass.log \| tail -1` = D400 BLOCKED；`git log df772d1 -- .claude/bypass.log` 末次变更 = bfb6b4a | **control-tower**（M4——台账 CT-17「bypass 对账物理执行」在队未建，本次为队列无执行体的第四次实证） |

### P2（实现开工前收敛 / 可选改进）

| # | 发现 | 证据 | 归因 |
|---|------|------|------|
| P2-1 | **spec 自相矛盾：payload 定义两处不一致**。SQL 注释写「完整对象（run/finding/signal/ticket）」，安全节却列字段白名单「id/severity/title/description/suggestion/relatedNodeId/status」——**漏 `detectedAt` 与 `evidence`**。漏 detectedAt 将直接打破 DS3 的 I1 断言（重放后 findings 全量一致）；含 evidence 则重开 P2-2 的 PII 问题。事件 schema 上线后有迁移成本，必须在写代码前收敛 | spec §3.2 SQL 块 vs 同节「数据安全边界」段；DS3 | **devdoc** |
| P2-2 | **数据安全框架与咨询原文偏离 + 断言无据**。咨询定调「事件流本身按 L3 处理，脱敏投影按 L2 派生」（§4.1 片 2 数据安全段）；spec 改称 finding payload「天然处于 L2 脱敏层（人名/金额已脱敏）」。物理实测：sentinel 全路径 PIIScrubber **零引用**（`git grep -c PIIScrubber df772d1 -- src/sentinel src/agent/sentinel-service.ts` exit=1），「已脱敏」无任何机制支撑；`SentinelFinding.description/evidence` 为自由文本，可携带实体名。实际暴露有限（库不出企业，API 已暴露同级内容），但 append-only 不可擦除 + 「天然 L2」错误框架会被片 2 继承。建议：表标注 L3-internal + impl 时对 description/evidence 实测抽查 | spec §3.2 数据安全段 vs 咨询 §4.1 L197 段；grep exit=1；types.ts:41-58 | **devdoc** |
| P2-3 | **派活包三处口径不一**：派活 brief 只有 §4.6 的 durationMs 半项，漏「findings 生命周期正名」；task-state json note 含全量；dispatch 表只指 §4.1。本次 dev-doc 线靠自检清单读原文自救（spec §2.3/DS8 已覆盖 ✅），但 brief 层缺口对下一个任务仍是敞口 | `.claude/task-briefs/D394-sentinel-events.md`（仅 durationMs）vs task-state/D394.json note vs DEV-DOC-DISPATCH-20260816.md | **control-tower**（派活材料） |
| P2-4 | **仪表盘产物不可复现（D399 P1-1 机制第三次复发，降级形态）**：main 的 CTO-HEALTH L66-68/L74 显示 D394/D395/D396/D402 `spec_done ✅`，但 4 份 spec 在 main 树不存在（`git ls-tree df772d1` 零命中，仅在 devdoc 分支）——刷新产物的工作区含未合并文件。与 D399 幻影行的区别：源文件已真实提交到 pushed 分支，合并后自愈，故降 P2。D399 首审指引「生成器对未提交/未合并源告警」至今未建，是复发的直接原因 | docs/synova/CTO-HEALTH.md:66-68,74 @main vs `git ls-tree df772d1 -- docs/plans/codex/implementation/ \| grep D394` 零命中 | **control-tower** |
| P2-5 | **spec 命名/契约完整度**：重放函数两名并存（§3.1 `replaySentinelEvents(db)` vs §3.2 `this.rebuildFromEvents()`），§5 grep 锚定前者，实现前需统一；三个新函数仅一个有完整 JSDoc 契约（见 item 8） | spec §3.1 vs §3.2 vs §5 | **devdoc** |

### 观察项（不计发现）

- **O1（存量）**：runner.ts:152-166 `catch { log.debug('哨兵工单表初始化失败…') }`——静默吞变种（log.debug 无 warn/error、无 degraded），铁律 24 存量违规。spec 的 fail-closed 契约正确避开了该模式；impl 时注意新表初始化不要 mimic。
- **O2**：4 任务 spec 单提交批量交付（4df237a）——纯文档走 CT-34/D387 豁免路径合法，但 4 个 task-state 同指一个 hash，证据链粒度变粗（D320 提交集教训的弱化版）。后续 impl 提交务必逐任务拆分。
- **O3**：task-state/D394.json @main 仍为 `claimed`（stale），spec_done 只在 devdoc 分支——合并后自动对齐，无需动作；本审计期间以 devdoc 分支 json 为准。

## 正面确认（防"只报忧不报捷"失真）

- spec 对咨询 §4.1 保真度 100%：I1/I2/I3、形似神不似预警、验收锚点（kill -9 + seq 单调无洞）逐字携带并落 DS3/DS4/DS5/DS9
- §4.6 双项全覆盖（durationMs 修复 = DS7；findings 生命周期 = DS8），dev-doc 线自救补上 brief 漏洞
- §4.5 决策参考四决策点双参考系收敛（D333/S-12 合规）；DS1-DS12 一一对应无跳号（S-10 合规）
- 写集与 D396/D395a/D402 零重叠声明（§3.3 并行冲突声明）；片 2/片 3 显式排除，守住切片边界
- 现状审计（§2）全部「read/grep 实测」且经本审计独立复核 17/17 命中——零凭记忆声称

## L4 防线缺口收割

> 本次发现的问题，控制塔哪道防线本该拦住？为什么没拦住？缺什么？

| 发现 | 本该拦住的防线 | 为什么没拦住 | 缺什么（收敛到已有 M 类，不新增） |
|------|--------------|------------|-------------------------------|
| P1-1 证据链断裂 | CT-17「bypass 对账物理执行」（台账在队，P0 优先级）+ D399 复审「顺延下一批补记」纪律 | 对账机制仍是队列里的纸面项，无物理执行体；补记靠人自觉 stage `.claude/bypass.log`，自觉在 D403 就断了 | **M4 已有防线强化**：pre-commit 增加「bypass.log 有未提交行则拦截提交」的物理门禁（机器查，不靠自觉）——仍是 CT-17 一项，不新增类别 |
| P2-4 产物不可复现 | D399 首审指引第 2 条后半「生成器对未提交/未合并源告警」 | 该指引至今未实现（D399 复审已点名为未闭合项），第三次复发 | **同一缺口的第三次实证**：gen-cto-health 生成时对 spec/task-state 源做 `git cat-file -e` 仓库态校验，缺源即告警/标 degraded——建议并入 CT-17 同批物理执行 |
| P2-3 brief 口径 | dev-doc §7 自检清单「核实咨询原文」 | **防线本次有效**（dev-doc 线自救补上 §4.6 后半）——brief 层无需新增机制 | 无需新机制；登记为「已有防线起效」正例 |
| P2-1/P2-2/P2-5 spec 内部质量 | dev-doc-gatekeeper.sh | 门禁查结构（写集表/Wiring 标题），不查语义矛盾（payload 两定义）与框架偏离（L2/L3 定调） | 语义层本就不属 bash 门禁——由本审计（K3 第二道）拦截，正在发生；无需新脚本（Anti-bloat） |

**机制收敛声明**：本次零新 M 类提议。M4 强化 1 项（CT-17 物理执行）+ 生成器源校验 1 项（D399 既有指引落地）——均为已有缺口的执行，非新增免疫细胞。

## 转 PASS 条件（impl 开工前/下一审计周期核验）

1. **P1-1**：bypass.log 补记 6 提交（bfb6b4a/8cfac21/1336d6b/972aa87/4df237a/548b206）的执行证据，或 CTO 显式登记豁免理由；CT-17 物理门禁立项排期。
2. **P2-1+P2-2**：impl 开工前在 spec 或首提交中收敛——payload 字段唯一定义（detectedAt 必须含，evidence 含/不含需与 I1 断言自洽）+ 事件表标注 L3-internal + description/evidence PII 实测抽查记录。

## 状态机写回说明

- `task-state/D394.json` **不动**：status 保持 `spec_done`（以 devdoc 分支为准），audit 段留空——本报告为 spec 关卡审计，impl 交付后再做交付审计并写 audit 段。
- 本报告路径：`docs/synova/audit-reports/2026-08-17-D394.md`（audit/d394-report 分支，只含本报告，待创始人合并）。
- D394 下一步：CTO 合并 devdoc 分支 → 派实现线按 spec 施工（先收敛 P2-1/P2-2）。
