# K3 审计报告 — D395-a「Agent Notes 四态（开发组织版）」spec 关卡审计

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-08-17 | 协议: AUDIT-PROTOCOL v1.0
> 审计对象: D395-a spec 交付 —— `feat/devdoc-4-spec-20260816` 分支 `4df237a`（spec 批量交付）+ `548b206`（spec.commit 回填）
> 任务状态: `task-state/D395.json` @devdoc 分支 = **spec_done**（spec ✅ path+commit 已回填 / impl null / audit null）
> 性质注记: 本次审计的是 **spec（活规格）交付物**，非代码交付。代码尚未存在（impl=null），
> 本报告结论是"spec 是否可放行进入实现"的关卡判定，不替代未来的 impl 交付审计。

## 结论: 🟡 CONDITIONAL PASS（spec 可放行进入实现；P0×0 / P1×2 / P2×5）

> ⚠️ 时效注记（2026-08-17 06:40 补）：本报告完稿期间 impl 已并发合并 main（71e0620），两项 P1 经定向核验**确认为已交付缺陷**，
> 性质转为修复单——见文末「补记」节。下述 spec 关卡判定不变。

spec 主体质量合格：§2 现状声称 **7/7 全部物理命中**（memory/ 恰 20 文件、无 notes/ 子目录、
generate-task-brief.py L109-116、pre-commit 组 6 无 Note 检查、commit-msg-check.sh 无 Note 检查、
src/orchestrator/ 存在、CHANGELOG 存在）；对 K3 咨询 §4.2 保真度高——四态目录名逐字一致、
神/形似神不似预警/防法完整携带、L217/L219 行号引用精确，且 L107 对"pre-commit→commit-msg"
落点偏离做了**显式声明 + 技术理由 + §4.5 决策记录**（诚实偏离的典范，非隐瞒）。
写集与 D394/D396/D402 零重叠声称经本审计独立实测为真。CI 8/8 job 级绿。north-star 三问对齐。

两项 P1 均为 **spec 完备性缺口**（写集与声称不齐），不推翻方案，按文末转 PASS 条件补写集条目即可。
**P1-1/P1-2 必须在写代码前收敛**（一个涉及门禁落点、一个涉及既有脚本改向，事后补 = M7 活实例）。

---

## 运行环境注记

Mac (darwin) 独立审计工作区 `/Users/wane/Synova-k3独立审计` / 网络可用（匿名 GitHub API 实测通）/
验证基于 `origin/main@df772d1` 与 `origin/feat/devdoc-4-spec-20260816@548b206` 的 git 树（`git show` 只读）+
工作区现状文件（main 内容）直接 read/grep。gh CLI 不存在 → CI 改走匿名 check-runs API（job 级，非整体 run 结论）。
`memory/notes/` 等目标物尚不存在属正常（spec 阶段），不作为扣分项。

## 材料清单（7 项自收集）

| # | 材料 | 状态 |
|---|------|------|
| 1 | 任务提交集 | `git log origin/main..origin/feat/devdoc-4-spec-20260816` = 4df237a + 548b206（D395-a 无其他提交，`git log --all --grep=D395` 全库仅命中 4df237a） |
| 2 | Git diff | 分支全 diff = 8 文件 869 插入：4 份 spec + 4 个 task-state json，**零代码**（纯 spec 提交实证） |
| 3 | Dev doc | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D395a-notes-four-state-20260816.md`（192 行）✅ |
| 4 | Task brief | `.claude/task-briefs/D395a-notes-four-state.md`（27 行，含神/形似神不似预警/验收锚点）✅ |
| 5 | AGENTS.md + PRODUCT-BRIEF.md | 铁律 0-2/35/47/48 + `.claude/PRODUCT-BRIEF.md` §七（L85）/§八.3（L120-122） |
| 6 | 审计基线 | 免（零代码变更；纯 spec 提交） |
| 7 | 执行证据 | CI 8/8 job 级 success（匿名 API 实测）；bypass.log 末行停于 `2026-08-16T21:26 D400 BLOCKED`——本提交零记录（同 D394 P1-1，见 P2-4） |

---

## 14 项逐项审计

| # | 项 | 判定 | 证据（file:line） |
|---|----|------|------------------|
| 1 | 接口真实性 | ✅ 7/7 命中 | 见下「现状声称核验表」 |
| 2 | 文件路径存在性 | ✅ | spec 引用路径全部存在：generate-task-brief.py / commit-msg-check.sh / pre-commit-check.sh / LOOP-ENGINEERING-CHANGELOG.md / 咨询 memo / task-state/D395.json（@devdoc 分支）/ 台账 M7（审计发现台账-DSH-CTO.md:46） |
| 3 | Edge ID 有效性 | ➖ N/A | spec 无 E-XX 引用 |
| 4 | 架构边界 | ✅ | 写集零 src/ 运行时代码（memory/ + scripts/ + tests/），不触五层边界 |
| 5 | 数据流完整性 | ✅（spec 层） | 决策流：新决策→Note（四字段头）→git mv 状态迁移→commit-msg 门禁强制引用；每环有写集承载（除层 1，见 P1-1） |
| 6 | 接线深度 | ✅（spec 层） | commit-msg-check.sh 被 install-hooks.sh:59 装入 .git/hooks/commit-msg（grep 实测）；generate-task-brief.py 被 task-start.sh:31 调用（grep 实测）——两个修改点的生产调用链真实存在 |
| 7 | 降级诚实性 | ✅ | 门禁设计 fail-closed（无 Note 引用→exit 1 阻断）+ 条件跳过 <1s（ctrl-tower-change 模式 3，无相关变更软过不跑 grep）；commit-msg-check.sh:12 既有 Merge/Revert 豁免保留 |
| 8 | 测试契约（铁律 47） | ✅ | §3.2 四字段头契约（状态/日期/决策/理由 + 取值枚举）+ git mv 迁移规则，先于实现定义 |
| 9 | 测试覆盖（铁律 48） | ✅ | §4 red→green 10 用例表：目录存在/归档/四字段头/状态迁移对账/门禁触发/放行/Note 不存在阻断/非目标路径软过/模板字段/Conventional Commits 回归——正常/边界/触发/放行/回归五类齐 |
| 10 | 文件驱动 | ✅ | 不新增硬编码类型；memory/notes/ 本身是文件驱动结构 |
| 11 | 自我报告交叉对比 | ✅ | commit msg「K3 咨询 436 行原文校准」——memo 实测 436 行 ✅；「task-state 转 spec_done」diff 实证 ✅；548b206 回填 spec.commit=4df237a 与真实 hash 一致 ✅；§7 自检 9 项声称逐项抽查属实（见核验表） |
| 12 | dev doc 偏离审计 | ⚠️ P1-1/P1-2 | 写集表（2 修改+6 新建）与实际 spec 交付性质一致（未夹带代码）✅；D395-b 显式排除守切片 ✅；**但两处"声称 vs 写集"漂移**，详见 P1 节 |
| 13 | 控制塔执行审计 | ⚠️ P2-4 | CI 8/8 job 级 success（Golden Case F1/Vitest×2/Architecture/TS+Lint+IronLaws/Checker Review/Integration Contract/npm audit，匿名 API 逐个 job 核实）；bypass.log 无本提交记录——同 D394 P1-1（M4），不重复计数 |
| 14 | 版本编排合规 | ➖ N/A | 纯 spec 提交无版本声称 |

### 现状声称核验表（item 1 明细，@main df772d1 实测）

| spec 声称 | 实测 | 判定 |
|-----------|------|------|
| §2.1「memory/ 20 个 .md 文件」 | `ls memory/*.md \| wc -l` = **20** | ✅ |
| §2.1「无 memory/notes/ 子目录」 | `ls memory/notes/` → No such file or directory | ✅ |
| §2.2「决策参考系记录在 generate-task-brief.py L109-116（Q1c）」 | read 实测 L109 `### c) 决策参考系` → L116 决策记录格式，行号精确 | ✅ |
| §2.2「LOOP-ENGINEERING-CHANGELOG.md 存在」 | 文件存在于仓库根 | ✅ |
| §2.3「pre-commit 组 6 只查 6 核心字段，无 Note 检查」 | pre-commit-check.sh:567 组 6 标题实证；`grep "memory/notes\|相关 Note"` 两脚本零命中（exit 1） | ✅ |
| §7「commit-msg-check.sh 无 Note 检查（grep 实测）」 | 同上零命中；commit-msg-check.sh 现状 = Conventional Commits + issue 引用 + D328 一致性 | ✅ |
| §7「src/orchestrator/ 目录存在」 | `ls -d src/orchestrator/` 存在 | ✅ |
| L16「与 D396/D394/D402 写集零重叠」 | 独立实测：D394→src/sentinel+tests/sentinel；D396→scripts/ci+tests/ci+tests/fixtures；D402→src/routes+tests/routes；D395a→memory/+scripts/workflow+scripts/+tests/control-tower。**零交集** | ✅（声称属实，但"见 §3.3"指针误指，P2-3） |
| §7「task-state/D395.json 已落」 | @devdoc 分支 spec_done + path + commit=4df237a 回填一致 | ✅ |

### 咨询 §4.2 保真度核对（item 12 明细）

| K3 原文（consult:行） | dev doc 携带 | 判定 |
|------------------------|--------------|------|
| L215 四态 `memory/notes/{proposed,implemented,archived,rejected}/YYYY-MM-DD-<主题>.md` | §3.1/§3.2 逐字一致 | ✅ |
| L216 四字段头 + 状态迁移=git mv | §3.2 逐字一致 | ✅ |
| L217 Q1 加"相关 Note 引用"字段（grep 物理检查 Note 文件存在） | L62/L94 引用并标注"K3 §4.2 L217"——行号精确；**但物理检查无写集承载（P1-1）** | ⚠️ |
| L218 神：可沉淀/可检索/不腐化 + 强化 M7 + 不新增机制类 | L21/L27/L31 完整携带；台账 M7:46 实证存在 | ✅ |
| L219 防法：pre-commit 对 control-tower/orchestrator commit 要求 commit message 引用 Note 路径 | L63/L96-104 携带并标注"K3 §4.2 L219"；**落点 pre-commit→commit-msg 偏离在 L107 显式声明 + §4.5 决策点 4 记录理由**（commit message 在 commit-msg 阶段才最终确定——技术判断正确，git hook 顺序物理事实） | ✅（诚实偏离，已授权级披露） |
| L221-222 D395-b 并入 D398 | §3.3 首行显式排除 | ✅ |

---

## P1 发现（转 PASS 条件，写代码前必须收敛）

### P1-1: 层 1（brief Q1 Note 引用）门禁声称无物理落点 —— 归因: devdoc（M7 同型）

**声称**：dev doc §3.2 层 1（L94）「非平凡 brief 的 Q1 增加字段，**grep 物理检查引用的 Note 文件真实存在**」，
溯源为 K3 咨询 L217 原文「grep 物理检查 Note 文件存在」；brief 验收锚点（L26）「门禁可查」。

**物理事实**：§3.1 写集（L53-64）中**没有任何文件承载此检查**——
generate-task-brief.py 只加模板字段；pre-commit-check.sh / hook-block-write.sh 均不在写集。
实测现状：hook-block-write.sh:151-154 对 Q1 仅查"非空 + ≥10 字符"，:198-201 仅查"引用至少 1 个来源"，
无任何 Note 存在性逻辑；pre-commit-check.sh 组 6 同理（grep 零命中）。
§4 测试表 10 用例中**无**"brief 引用不存在 Note → 阻断"用例；DS 集中 DS5 只验「模板含字段（grep -n 命中）」。

**后果**：按 spec 原样实现，层 1 交付即为**空挂字段**——填不填、填真的填假的，无任何门禁。
这正是本任务自己的形似神不似预警（L21「不靠自觉」）在层 1 落空：防别人腐化的任务，自己的第一层就靠自觉。

**修复（三选一，写代码前定）**：
a) pre-commit 组 6 或 hook-block-write.sh 入写集，加「非平凡 brief Q1d 引用的 Note 文件存在」grep 检查 + 对应测试用例 + DS；或
b) 显式 descope 层 1 为纯模板字段，从 §3.2/DS5 删除"grep 物理检查"声称——但此偏离 K3 咨询 L217 原文，需创始人确认授权；或
c) 将层 1 检查合并进层 2 的 commit-msg 实现（commit 触 brief 时一并校验），需在 §3.2 写明合并后的单点落点。

### P1-2: 写集漏列 check-lessons-learned.sh 改向 —— 腐化通道未关闭 —— 归因: devdoc

**物理事实**：`scripts/check-lessons-learned.sh:8` `MEMORY_DIR="$ROOT/memory"` + :22 `MEMFILE="$MEMORY_DIR/${NAME}.md"`——
该脚本是**现有的教训写入通道**（Q0c 审计发现 → memory/ 条目 + class 去重），固定向 memory/ 根平铺写入。
四态改造落地后，每次错误沉淀都会从第一天起**重建平铺堆**，恰是本任务要消灭的结构。
dev doc §2 现状审计未 grep memory/ 消费者（本审计实测消费者全清单：hook-check-memory.sh:21、
check-lessons-learned.sh:8、generate-task-brief.py:99 散文、pre-commit-check.sh:148/957 正则——V4.5.0 grep-refs 三步曲在 spec 期无执行痕迹）。

**兼容性注记（免误改）**：hook-check-memory.sh:58 教训扫描用 `find "$MEMORY_DIR" -name "*.md"` **递归**查找，
`git mv` 至 memory/notes/archived/ 后注入功能**不受影响**（本审计读码实证，非假设）——该脚本**不需要改**，
但 spec 应显式注记防 impl 期误伤。pre-commit-check.sh:148 `DOC_PREFIX_RE` 含 `memory/.*\.md` 前缀匹配，
子目录归档文件仍命中纯文档豁免 ✅。

**修复**：check-lessons-learned.sh 写入路径改向 `memory/notes/proposed/` + 四字段头模板，入 §3.1 写集 + §4 测试 + DS；
hook-check-memory.sh 兼容性注记入 §3.1 或 §3.3。

---

## P2 发现（建议修复，不阻塞）

| # | 发现 | 证据 | 归因 |
|---|------|------|------|
| P2-1 | 头部注释 L16 称「独占 memory/notes/ + generate-task-brief.py + **pre-commit-check.sh** 门禁」，与写集（改 commit-msg-check.sh）及 §4.5 落点决策（commit-msg）**自相矛盾** | dev doc L16 vs L63/L150 | devdoc |
| P2-2 | 用例数口径不一：§3.1「≥8 用例」（L64）vs §4 表 10 用例 / L140「≥10」/ DS1「≥10」 | dev doc L64 vs L123-134/L140/L167 | devdoc |
| P2-3 | L16「写集零重叠，**见 §3.3**」误指——§3.3 是"不做的事"，无重叠分析（重叠声称本身经本审计独立实测为真，仅指针错） | dev doc L16 vs L111-117 | devdoc |
| P2-4 | bypass.log（git 跟踪）末行停于 2026-08-16T21:26 D400 BLOCKED，4df237a（08-17 05:16）+548b206 **零执行证据**——与 D394 审计 P1-1 **同提交同型**（M4 证据链断裂），已在 D394 报告计数，此处不重复开单，仅标注复发未愈 | .claude/bypass.log 末 5 行实测 | control-tower（M4） |
| P2-5 | CI「聚合 → 计算 → 页面」job 自 D405 合入后**新生即红且 4 个连续 main 提交全红无人认领**（f6a4a8d/a049aef/d3f07f3/71e0620 实测），M1 信号失效同型；非 D395-a 引入，归 CT-39（CI 红超 24h 自动入待办）覆盖范围，此处登记不阻塞 | 匿名 check-runs API 逐提交实测 | control-tower（M1） |

**观察项（不定级）**：
- O1: DS10「git diff --name-only 与写集一致」——20 个 `git mv` 在 diff 中呈 R（rename）条目，impl 期需验证 check-dev-doc-write-set.sh 对 rename 的对账容错，防止门禁误报或放行。
- O2: 四态头字段"状态"与目录名的一致性对账（§4 用例 4）依赖新增校验逻辑，但该校验的载体脚本在写集中未单列（拟随门禁测试承载）——impl 期确认即可。

---

## 跑偏第二道（north-star 三问，对照 .claude/PRODUCT-BRIEF.md 原文）

| 问 | 裁决 | 依据 |
|----|------|------|
| ① 服务真实用户场景吗 | ✅ 对齐（**间接**） | front-matter 服务用户 = 开发线全体，非 FDE/企业主直接；属流程防线类任务。锚点物理存在：PRODUCT-BRIEF §七「我犯过的错」（L85）+ §八.3「决策建议——读取 memory/ 历史教训」（L120-122「而是读取 全量对齐手册 + 代码实际 + **memory/** → 给出具体的下一步建议」）。K3 咨询 L96 对齐表同判「间接：服务交付质量 ✅ 对齐」。开发组织的决策不腐化 = 交付质量防线，服务最终用户场景 |
| ② 让模块更接近终态吗 | ✅ | 现状平铺无结构 → 终态四态可检索 + 物理门禁，是朝终态一步；§3.3 五项排除（D395-b/CHANGELOG 本体/旧文件内容/门禁扩散/task-start 本体）守边界，无横向加东西 |
| ③ 会变味吗 | ✅ | 写集零 src/ 运行时代码，纯 memory/+scripts/+tests/——Synova 仍是"驻扎企业、主动诊断、为增长服务"的 Agent，产品形态零变化 |

front-matter 七字段齐全（服务用户/服务场景/模块终态/对齐北星/完成标准/当前进度）✅。

---

## L4 防线缺口收割

> 本次发现的问题，控制塔哪一道防线本该拦住？为什么没拦住？缺什么？

**P1-1/P1-2（声称 vs 写集漂移，M7 同型）**：
- 现有防线：dev-doc 门禁 C6 只查"写集表**存在**"（D381 固化）；S-6 管"dev doc §3.2 最终实现同 commit 回填"（impl 期）；
  Agent 自检有"不是凭记忆"。**没有任何一道查"声称的每个机制在写集中有承载文件"**——声称-写集对账是语义判断，目前无防线承载。
- V4.5.0 grep-refs.sh 三步曲是 **impl 期**防线（写代码前 grep 引用）；spec 期现状审计无等效强制步骤，§2"代码审计——现状"的彻底程度全凭 dev-doc 自觉（本次即漏 grep memory/ 消费者）。
- **缺口结论（一类一机制，不新增 M 类）**：命中已有 **M7**（文档-实现漂移的 spec 期变体：声称两层门禁，写集承载一层）。
  建议强化点 = dev-doc persona/模板自检清单加两问：「① 每个声称的门禁/机制，写集中有承载文件吗？② 现状审计 grep 过被改路径的全部消费者吗？」——
  一句话规则，不加新脚本（Anti-bloat 原则）。**此建议移交创始人/devdoc 预设维护者，本审计不代改。**

**P2-4（执行证据链，M4）**：D394 P1-1 已记录并计数（第四次复发），修复归属沿用 D394 报告结论，不重复收割。

---

## 转 PASS 条件

1. **P1-1**：层 1 门禁补物理落点（修复三选一，见 P1-1 节）+ 对应测试用例 + DS；选 descope 路径需创始人确认（偏离 K3 咨询 L217 原文）。
2. **P1-2**：check-lessons-learned.sh 写入改向 memory/notes/proposed/（入写集 + DS + 测试）+ hook-check-memory.sh 递归兼容性显式注记。

P2-1/P2-2/P2-3（文档口径）建议同批收敛，改动成本极低。满足条件 1+2 后本审计自动转 **PASS**，无需复审全量——
变更仅限 dev doc 文档本身时，创始人核对 diff 即可；若连带写集扩项，按新写集条目抽查。

---

## 补记（2026-08-17 06:40）：审计期间 impl 已合并 main —— 两项 P1 转化为已交付缺陷的物理确认

**并发事实**：本审计进行期间（06:31-06:35），`feat/d395a-notes-four-state` 经 `9c786b5`（四态+门禁实现）+
`5428a21`（20 旧文件 git mv 归档）→ `71e0620`（Merge into main）**完成实现并合并**。
本报告主结论仍是 spec 关卡判定；以下为对 merged impl 的**定向核验**（仅核两项 P1 + 核心交付物，非全量 impl 审计）。

### impl 定向核验表（@origin/main 71e0620 实测）

| 核验项 | 结果 | 证据 |
|--------|------|------|
| 四态目录 + README | ✅ 落位 | `git ls-tree origin/main memory/notes/` = README.md + proposed/implemented/archived/rejected |
| 20 旧文件归档 | ✅ 恰好 20，根目录零残留 | archived/ 计 20 .md；memory/ 根仅剩 notes/ |
| 层 2 commit-msg 门禁 | ✅ 落位 | commit-msg-check.sh:117-134 两层检查（message 含 memory/notes/ 引用 + Note 文件真实存在） |
| Q1d 模板字段 | ✅ 落位 | generate-task-brief.py:118 `### d) 相关 Note 引用` |
| 门禁测试文件 | ✅ 存在 | tests/control-tower/notes-four-state.test.sh |
| **P1-1 层 1 门禁落点** | ❌ **确认交付缺陷** | merged pre-commit-check.sh + hook-block-write.sh `grep "memory/notes\|相关 Note"` **零命中**——层 1 仍是无门禁空挂字段，与 spec 审计预测一致 |
| **P1-2 check-lessons-learned.sh 改向** | ❌ **确认交付缺陷** | merged check-lessons-learned.sh:8,23 仍 `MEMORY_DIR="$ROOT/memory"` 平铺写入——首次错误沉淀即在 memory/ 根重建平铺堆，腐化通道敞开 |
| CI @merge 71e0620（job 级） | ⚠️ 7 绿/1 skip/1 红 | 红 = 「聚合 → 计算 → 页面」dashboard job；**预存非引入**——f6a4a8d/a049aef/d3f07f3/71e0620 四个连续提交全红（D405 合入后新生即红，无人认领，M1 信号失效同型，归 CT-39 机制覆盖范围，此处记 P2-5 不阻塞） |

### 结论修正

spec 关卡判定 **CONDITIONAL PASS 不变**（spec 阶段的判定依据未动）。
但两项转 PASS 条件的性质从"写代码前收敛"变为"**已随 impl 落进 main 的活缺陷**"，建议转修复单（fix-forward）：
- **P1-1 → 修复单**：层 1 补物理落点（pre-commit 组 6 或 hook-block-write.sh 加「非平凡 brief Q1d 引用 Note 存在」grep 检查 + 测试），或创始人授权 descope 层 1。
- **P1-2 → 修复单**：check-lessons-learned.sh 写入改向 `memory/notes/proposed/` + 四字段头。**优先级高于 P1-1**——每发生一次错误沉淀，平铺堆长一页。
- impl 全量审计（DS1-DS11 逐项 + 测试实跑 + bypass 链）未执行；如创始人需要，排独立 impl 审计单。

## task-state 写回块（供创始人合并后落入 task-state/D395.json 的 audit 字段）

```json
"audit": {
  "verdict": "CONDITIONAL_PASS",
  "level": "spec",
  "report": "docs/synova/audit-reports/2026-08-17-D395a.md",
  "by": "K3",
  "at": "2026-08-17",
  "findings": "P0x0/P1x2/P2x5",
  "pass_conditions": "P1-1/P1-2 已于 impl 合并时落入 main（71e0620 定向核验确认）→ 转修复单: P1-2 check-lessons-learned.sh 改向 memory/notes/proposed/(优先); P1-1 层1门禁补物理落点或创始人授权 descope",
  "impl_note": "impl(9c786b5+5428a21) 审计期间并发合并; 核心交付物核验 5/5 落位; 全量 impl 审计未执行"
}
```

---

> 红线遵守注记：本审计只读产品仓库（git show/grep/read），未修改任何产品代码/brief/记忆/审计脚本；
> 报告写在独立工作区 audit/d395a-report 分支（只含本报告一个文件），回流由创始人合并。
