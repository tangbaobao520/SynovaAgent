# K3 独立审计报告 — D396 golden-case 三层快照门禁

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-08-17
> 协议: AUDIT-PROTOCOL.md L1-L4 + synova-audit 14 项清单
> 对象: commit `9aaf0cde`（feat/d396-golden-case-snapshot，单提交任务集——`git log --all` 确认无其他 D396 实现提交；spec 提交 `4df237a4` 另计）
> Dev doc: `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D396-golden-case-gate-20260816.md`（spec.commit `4df237a4`）
> Task brief: `.claude/task-briefs/D396.md`（随实现同 commit 提交）

## 结论: **PASS（无条件通过）**

P0 = 0，P1 = 0（1 项控制塔预存缺陷转入待办，非 D396 引入），P2 = 4。
K3 验收锚点「红-绿演练必须跑一次证明门禁真能红」由审计员**独立物理复现**（非引用实现方证据）：改坏阈值 → exit 1 + diff 点名 `signal`；恢复 → exit 0。

---

## 运行环境注记（防 D316/D317 环境误判）

- 审计机: macOS（Mac），node v24.19.0，vitest 4.1.8
- 方法: 产品仓库**只读**；`git clone` 至 `/tmp/d396-audit` + checkout `feat/d396-golden-case-snapshot`，node_modules 逐项符号链接（红-绿演练的代码改坏发生在沙盒副本，产品仓库零写入）
- CI 证据: GitHub 匿名 API 逐 job 拉取（`commits/9aaf0cde/check-runs`），非整体 run 结论（D320 教训）
- 基线对比: 同沙盒内 checkout 分支基点 `548b206d` 重跑同一脚本

## 提交集映射（D320 教训：非单提交假设）

| 提交 | 内容 | 角色 |
|------|------|------|
| `4df237a4` | D396+D394+D395a+D402 spec 交付 + task-state 转 spec_done | dev-doc |
| `548b206d` | task-state spec.commit 回填 | dev-doc |
| `9aaf0cde` | **D396 实现（唯一实现提交）** — 8 文件 +594/-18 | DeepSeek Harness |

`git log --all -- scripts/ci/golden-snapshot-runner.ts tests/fixtures/golden-cases/golden-case-11-*` 均仅命中 `9aaf0cde`。任务集完整。

## L1 代码审计 — 14 项逐项

| # | 项 | 结论 | 证据 |
|---|-----|:---:|------|
| 1 | 接口真实性 | ✅ | `runComputeSnapshot`/`runFindingsSnapshot`/`runExpertReportAssertion`/`computeFnRegistry`/`diffFindings` 全部存在于 `scripts/ci/golden-snapshot-runner.ts:140,209,228,73` |
| 2 | 文件路径存在性 | ✅ | 写集 5 代码文件全部存在于分支（git show 逐文件读取验证） |
| 3 | Edge ID 有效性 | ✅ | fixture 用 `E-05`/`CAPITAL_ACQUISITION`——golden fixture 为自洽测试数据（01-10 同模式预存），checker 内部比对，不依赖 extensions/ 定义 |
| 4 | 架构边界 | ✅ | `scripts/ci/` → `extensions/sentinels/**` import（scripts/ 豁免层检查，V4.4.0 明示）；checker → runner 同目录；零 src/ 跨层 |
| 5 | 数据流完整性 | ✅ | dev doc §3.2 四步流（F1→compute diff→findings diff→专家断言）与 `golden-case-checker.ts:263-266` 实现一一对应 |
| 6 | 接线深度 | ✅ | `runComputeSnapshot` 在 `runAllChecks` 真实调用（checker:263），非仅 import；生产调用点 2 处实测命中：`scripts/pre-push-check.sh:221` + `.github/workflows/ci.yml:130` |
| 7 | 降级诚实性 | ✅ | 全部 catch 有 `console.error` + `degraded:true` + diffs 说明（runner:151-155, 218-222）；snapshot 缺失 → 显式降级不静默 pass（runner:147-150） |
| 8 | 测试契约（铁律 47） | ✅ | runner.ts:3-15 JSDoc 契约（@input/@output/@degraded/@error）与实现逐条一致 |
| 9 | 测试覆盖（铁律 48） | ✅ | 新增 12 用例（compute 4 + findings 3 + 专家报告 3 + 集成 2），覆盖正常/降级/边界/红-绿；审计员复跑 **29/29 通过**（2 文件含回归） |
| 10 | 文件驱动 | ✅ | 无新硬编码类型；快照数据全在 fixture JSON；registry 为 spec 批准的扩展点（增量登记制） |
| 11 | 自我报告交叉对比 | ✅ | task-state impl.files 5 项 = 实际写集；commit message 声称与 diff 一致 |
| 12 | dev doc 偏离 | ✅ 无漂移 | 实现偏差（gate 断言 10→11、测试文件「新建」实为「修改」）**已在 dev doc §3.1 同 commit 回填并标注 S-12 可核**（M7 防线生效实例） |
| 13 | 控制塔执行 | ✅ | 见 L3 节 |
| 14 | 版本编排 | N/A | 仓库无 VERSION.md/version.log/git tag 体系（`ls` 实测零文件），本任务不涉及 |

## L2 偏离审计 — DS1-DS10 逐条

| DS | 声称 | 判定 | 物理证据 |
|----|------|:---:|------|
| DS1 | 测试全过 ≥10 用例 | ✅ | 复跑 29/29 通过（新增 12）；「red 已证」声称无法回溯验证（测试与实现同 commit，无 red 证据留存）——见 P2-4 |
| DS2 | registry 登记真实 computeCashRunway | ✅ | `golden-snapshot-runner.ts:17` import 自 `extensions/sentinels/financing-constraint/computes/cash-runway`，非复制重写 |
| DS3 | 改坏阈值 → passed:false + diff 点名 signal | ✅ | **审计员独立演练**：`sed` 改 `cash-runway.ts:51` `<6`→`<3` → checker exit 1，输出 `signal: 期望 "critical" 实际 "warning"`；恢复 → exit 0 |
| DS4 | 快照分层三态齐备 | ✅ | 三层机制均实现+单测覆盖。findings registry 当前为空、expertReport 无 fixture——**spec 明示的增量登记设计**（§3.1 说明 + runner:76-81 注释），非缺陷；后续 D355-D360 修复同 PR 登记 |
| DS5 | 向后兼容旧 10 条 | ✅ | 实跑 11/11 全绿 exit 0，旧 fixture 仅 F1 行无快照段输出 |
| DS6 | F1 判定逻辑不变 | ✅ | `git diff` 过滤后触 `computeF1Score`/`deriveActual` 的实质改动 = **0 行**（仅注释/import）；旧 11 测试全过 |
| DS7 | 生产接线真实调用 | ✅ | checker:263 调用点 + pre-push:221 + ci.yml:130（grep 实测 2 处） |
| DS8 | 基线一致 + 无 bypass + 写集一致 | ✅ | 基线 `433 FAIL/883 WARN/3 PASS` 在基点 `548b206d` 与 D396 头**完全相同**；gatekeeper 日志 `05:46:22 BLOCKED → 05:48:35 COMMITTED pre-commit PASS HASH=9aaf0cde`（门禁真实拦过一次后通过，无 --no-verify 标记）；写集 = §3.1 更新版 |
| DS9 | 推送 + CI 逐 job 绿 | ✅ | `origin/feat/d396-golden-case-snapshot` == `9aaf0cde`，未推送 0 commit；CI **8/8 job 级 success**（含 Golden Case F1 Gate / Architecture / Checker Review；npm audit 亦绿——D391 时的预存红已被 D392 消解） |
| DS10 | 决策记录 K3 可核 | ✅ | dev doc §4.5 四决策点双参考系 + brief Q1c 含补充决策（gate 断言同步），参考系标注齐全 |

## L3 执行审计（机器证据）

| 证据 | 结论 |
|------|------|
| gatekeeper 日志（`.claude/bypass.log`） | D396 实现提交 `9aaf0cde`：先 BLOCKED（05:46:22，pre-commit exit 1）后 PASS（05:48:35）——门禁**真实执行且非橡皮图章**；无 `--no-verify` 记录 |
| CI（匿名 API 逐 job） | 8/8 success：Golden Case F1 Gate / Vitest×2 / Architecture / Integration Contract / TypeScript+Lint+Iron Laws / npm audit / Checker Review |
| 推送状态 | 分支全量落库远程，本地无悬空 |
| ⚠️ CI 绿的成色注记 | `ci.yml:19` tsc 步骤带 `\|\| true`——本地沙盒 `tsc --noEmit` 有 37 行错误（基点与 D396 头**逐字节相同**，全在 `extensions/sentinels/_extinct/` 与预存 src/ 文件，D396 写集零涉及）。"TypeScript job success" 对 tsc 部分失真。**预存缺陷，非 D396 引入**——转 L4 |

## 跑偏第二道（north-star 三问）

对照 `.claude/PRODUCT-BRIEF.md` 原文：

1. **真实用户场景** ✅ — 服务创始人（push 前机器确认已修能力没被改坏）+ FDE/实现线（改 compute 阈值立即被拦）。锚定 §八「Loop Engineering 需要成为什么」（L96-119：门禁应从语法/安全扩展到机器验证产品行为）。引用略宽泛（§八原始愿景偏"产品对齐 4 问"，本任务是"修复固化回归"），但同属该节大方向且 FOUNDER-OPERATING-MODE §二 组件1（GSS 黄金场景集）锚点精确。
2. **更接近终态** ✅ — 扩展既有 checker（K3 明令不做新体系，dev doc §3.3 六项排除全部遵守：未改 F1 判定/未起新 runner 体系/未碰 src//未碰 manifest/未改接线），是朝 GSS 终态的纵向一步，非横向无关物。
3. **不变味** ✅ — 纯 CI 门禁设施，零产品行为变更。

**裁决：对齐。**

## 发现清单

### P1（0 项针对 D396 本身；1 项转控制塔待办）

无 D396 可归责 P1。

### P2（4 项）

| # | 发现 | 归因 | 证据 |
|---|------|------|------|
| P2-1 | `task-state/D396.json` 的 `impl.commit` 回填为 `null`（应为 `9aaf0cde`）——spec.commit 有回填惯例，impl.commit 无 | implement | `git show 9aaf0cde:task-state/D396.json` → `"commit": null` |
| P2-2 | 两份 D396 brief 并存于分支（`.claude/task-briefs/D396-snapshot-golden.md` + `D396.md`），impl 实际用后者——轻微冗余，无歧义危害 | implement | `ls .claude/task-briefs/ \| grep D396` 双命中（D396 分支） |
| P2-3 | findings 层 registry 空登记 + expertReport 无示范 fixture——spec 明示设计，但意味着三层中两层的拦截力要等后续 D355-D360 登记才生效；建议 CTO 在后续修复任务 brief 中强制"登记同 PR" | devdoc（提示，非缺陷） | `golden-snapshot-runner.ts:80` 空对象字面量 |
| P2-4 | DS1「red 已证」属不可回溯声称（测试与实现同 commit，无 red 阶段证据留存）——本次由 K3 独立演练补位证明机制有效；建议 red 证据以 `/tmp` 日志或 CI artifact 留存 | devdoc | commit `9aaf0cde` 单提交内含测试+实现 |

## L4 防线缺口收割

**问：本次发现的问题，控制塔哪道防线本该拦住？为什么没拦住？缺什么？**

1. **CI tsc `|| true`（ci.yml:19）→ 命中已有 M1（fail-open 静默失效）**。
   台账 M1 行早已以 D329 P2-5 记录过同款 `|| true`，防线 CT-1（三态输出）至今挂在「⏳ 健康审计队列」。本地 tsc 37 行错误 vs CI job success 的失真**持续存在**。本次复证：M1 防线缺口未愈合。**处置：不新增类，提升 CT-1 优先级——CI quality job 的 tsc 步骤应去掉 `|| true` 或改为"基线比对 fail-closed"（错误数 > 冻结基线才红）。归因: control-tower。**

2. **impl.commit 未回填（P2-1）→ 命中已有 M7（文档-实现漂移）**。
   S-6 防线只覆盖「dev doc §3.2 同 commit 回填」，task-state 的 `impl.commit` 字段无对应校验。**处置：不新增类——在既有 M7 防线（S-6 回填检查）上扩展一个字段校验：synova-commit/pre-push 断言 task-state `impl.commit` 非空。归因: implement（漏填）+ control-tower（无校验）。**

3. 双 brief 并存（P2-2）与 red 证据不存（P2-3/4 同类性质）：记录完整性瑕疵，现有 S-10/S-12 已使其实际无害（偏离均同 commit 回填可核）。不加新机制——**机制数量收敛于错误模式种类数，不为每次小瑕疵加脚本。**

**Anti-bloat 核对**：本次全部发现命中已有 M1/M7 两类，零新类提议。

## 状态机写回

- task-state/D396.json 建议：`audit.verdict = "PASS"`，`audit.report = "docs/synova/audit-reports/2026-08-17-D396.md"`，`audit.at = "2026-08-17"`，顺带补 `impl.commit = "9aaf0cde"`（P2-1）。
- 分支 `feat/d396-golden-case-snapshot` 可合并 main（PR 流程，铁律 0-3）。

---

**审计员独立复现命令留档**（任何 session 可重跑）:
```bash
git clone /Users/wane/SynovaAgent /tmp/d396-audit && cd /tmp/d396-audit
git checkout feat/d396-golden-case-snapshot   # 9aaf0cde
npx vitest run tests/ci/golden-case-checker.test.ts tests/ci/golden-case-gate.test.ts  # 29/29
npx tsx scripts/ci/golden-case-checker.ts                                              # exit 0, 11/11
sed -i '' 's/if (runwayMonths < 6) {/if (runwayMonths < 3) {/' \
  extensions/sentinels/financing-constraint/computes/cash-runway.ts
npx tsx scripts/ci/golden-case-checker.ts   # exit 1, diff: signal 期望 critical 实际 warning
git checkout -- extensions/                 # 恢复后 exit 0
```
