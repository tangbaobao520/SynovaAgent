# K3 独立评审报告 — 产品线验证体系（26 线 × 163 验收点）

> **审计员**: Kimi K3（独立会话，只读复核 + 报告，不改任何代码/线集/标准）
> **日期**: 2026-08-17 | **任务书**: K3-PRODUCT-LINES-VERIFICATION-TASK.md
> **对照**: K3-AUDIT-STANDARD-v1-20260815.md（维度 16-20 + 抽样规则）、AUDIT-PROTOCOL.md
> **方法**: 附录 A 数据全部独立复现（claim-verifier：先复现再采信）+ 物理事实 grep/复跑核验 + 独立裁决
> **红线**: 线集增删拆并 = 创始人权力；本报告只产出评审意见与建议，不直接改 product-lines.yaml。

---

## 〇、复核声明（附录 A 数据可信度）

附录 A 的全部关键数据已**逐项独立复现**，结果与 DSH 声称一致：

| 声称 | 复现结果 | 判定 |
|---|---|---|
| 总进度 4%（2026-08-17 12:09 生成） | `product_progress_pct=4` | ✅ 属实 |
| verified 7 / rejected 4 / failed 3 / uncommitted 149 | 逐点复现完全一致 | ✅ 属实 |
| evidence/ 仅 3 JSON 且 record_type 全 k3 | ls + 逐文件读 record_type 确认 | ✅ 属实 |
| GS-01~08 场景目录零实现（仅 common/evidence/README） | ls 确认 | ✅ 属实 |
| M 120 / F 10 / K 33（不相交分解） | 独立重算 M=120/F=10/K=33 | ✅ 属实 |
| 7 个仅 K 非终审点（7-1/7-2/9-2/11-1/11-2/18-1/22-5） | 逐点措辞提取一致 | ✅ 属实 |

**附录 A 数据可信，可作为裁决输入。** DSH 已自查修正 5 处的声称属实。

---

## 任务 1：26 线线集合理性独立评审

### 1.1 裁决：26 条线集**合理**，不建议增删拆并（维持创始人锁定）

逐维度核验：

| 维度 | 判定 | 物理依据 |
|---|---|---|
| **覆盖面** | ✅ 完整 | 26 线覆盖完整数据流：数据接入(线4)→本体(线5)→哨兵(线7)→专家(线15)→报告(线3)→行动(线16)→进化(线17)，加交互面(桌面1/对话2/告警8)+底座(运行20/模型21/自诊断22/权限23/安全24/插件25/MCP26)+业务循环(客户9/资本10/人才11)+维度(技术12/财务13/竞争14)。无漏环 |
| **粒度** | ✅ 合理 | 每线 5-8 点（最少 5，最多 8），单点可独立验证，无过大/过小点 |
| **验收点可验证性** | ✅ 大部分物理可判定 | 逐点措辞复核：103 scenario + 18 test + 2 contract-check 绑定点措辞均为物理可判定（如"哨兵调度真实运行（cron 注册并生效）"、"属性契约对齐 cashBalance↔cash"）；少数语义点（6-3"命中老板盲区"、3-x 报告质量）确需 K3 |
| **权重等权 1.0** | ⚠️ 可议但不阻断 | 见 1.2 裁决 3 |

**结论**：26 条线划分与北星对齐、覆盖面完整、粒度合理。DSH 附录 B.1"线集本身合理，不建议动 26 条线"的判断**成立**。

### 1.2 对 DSH 声称的 3 个结构问题的逐条裁决

**裁决 1：跨线重复验收点 —— 部分成立（2 对真重复，2 对部分重叠，1 对不重复）**

独立复核 5 对（物理对照 desc 原文）：

| 对 | desc 对照 | 裁决 |
|---|---|---|
| 5-2 ≈ 10-2 | 两处 desc **逐字相同**（"属性契约对齐 cashBalance↔cash 等 snake/camel"） | ✅ **真重复**，断言对象完全一致。建议：保留 5-2（本体层），10-2 改为"ERP 数据进 Financial 节点"的端到端侧（或删除其一） |
| 4-5 ≈ 10-1 | 10-1 字面包含 4-5 全部内容（ERP 连接器 ⊂ ERP 财务数据进 Financial 节点） | ✅ **包含关系（最高重叠）**。建议：4-5 保留（连接器存在），10-1 明确为"数据进节点"的验证（存在≠进节点） |
| 4-4 ≈ 9-1（CRM）/ 4-6 ≈ 11-3（HR） | 蕴含关系：贯通点（数据进 Client/Person 节点）蕴含连接器存在点 | 🟡 **部分重叠非重复**——连接器存在(4-x)与数据进节点(9-1/11-3)是两个层级，建议措辞区分，不算重复 |
| 2-2 ≈ 20-4 | 功能存在（流式逐字）vs 质量属性（不闪烁不卡死） | ❌ **不重复**——判定侧重点不同，DSH 自己也标注了"K3 裁"。维持两点 |

**裁决 2：K3 垄断计分 —— 成立（这是 4% 的结构性根因之一）**

物理证据：`calc-progress.py:140-145` verified 仅认 `k3:pass` 或 `founder_demo:pass`；`:164` 机器绿 → `pending_k3`（**不计分**）。即"机器全绿进度仍为 4%"属实。**成立**。但修法见任务 4（不是简单放开，要带防伪）。

**裁决 3：权重等权 1.0 存疑 —— 不成立（当前阶段等权合理）**

26 线当前等权 1.0。DSH 未给出"谁该更重"的客观依据，产品早期阶段等权是诚实且可辩护的默认。**不成立**（暂维持等权；若未来某线对增长权重显著更高，由创始人裁决调整，非技术问题）。

### 1.3 任务 1 结论

26 线集**合理**；验收点层存在 2 对真重复（5-2/10-2、4-5/10-1）+ 2 对重叠建议措辞区分。计分规则的 K3 垄断是真实结构问题（转任务 4）。

---

## 任务 2：163 验收点三分法定稿

### 2.1 三分法分布（独立重算确认）

| 类 | 点数 | 占比 | 含义 | 判定方式（无上下文执行者可物理执行） |
|---|---|---|---|---|
| **M 机器可验证** | **120** | 74% | 场景/单测/契约检查可客观判定 | 场景 `run.sh` exit 码 / vitest 绿 / 对账脚本 exit 码 |
| **F 创始人演示** | **10** | 6% | 必须人测（安装实测/手机看报告/复述报告/30 分钟首诊计时） | 演示记录 + 核验 checklist |
| **K 必须 K3** | **33** | 20% | 语义判断/质量裁决/线级终审 | K3 复核报告 |

（163 = 120 + 10 + 33；另有 3 点同时绑机器+founder-demo，计入 M。）

### 2.2 关键裁决：7 个"仅 K"实质点 → 6 个应改绑 M 类

逐点物理复核（验收点对应脚本/manifest 已 grep 验证存在）：

| 点 | desc 摘要 | K3 裁决 | 物理依据 |
|---|---|---|---|
| 7-1 | 哨兵调度真实运行（cron 注册并生效） | ✅ **改绑 M** | cron 注册链 `registry.ts:57`→`runner.ts:183`→`scheduler.ts:112`，45/48 manifest 含 schedule 字段——grep + 集合 diff 可判 |
| 7-2 | 哨兵全量注册（manifest 挂载无死代码） | ✅ **改绑 M** | manifest 挂载 `sentinel-loader.ts:75-83,127-129`，集合 diff 可判 |
| 9-2 | customer-demand-shift 哨兵计算真实（注入产出 critical） | ✅ **改绑 M** | desc 自带注入实验设计，哨兵目录（aggregate/computes/manifest）齐全，可写 mock GraphStore 注入 + severity 枚举断言 |
| 11-1 | key-person-risk 哨兵计算真实（Bus Factor） | ✅ **改绑 M** | 同上，注入断言可机器化 |
| 11-2 | talent-density 哨兵计算真实（含降级诚实） | ✅ **改绑 M** | 注入断言 + "降级诚实"在 `talent-density/aggregate.ts:38-39` 有明确代码形态 |
| 18-1 | AgentMemoryStore 9 类记忆存储 | ✅ **改绑 M** | `agent-memory-store.ts:24` type 定义 9 类与 `:312` DDL CHECK 完全对账，零语义成分 |
| 22-5 | 自我健康报告真实（完成度误判修复 D-G2） | 🟡 **保留 K** | D-G2 机制（`generate-dashboard.py:224/279`）存在性可验，但"自我健康报告**真实**"的指代与"真实"标准需人对齐——唯一含语义残留 |

**裁决：6/7 仅 K 点应改绑 M 类**（7-1/7-2/9-2/11-1/11-2/18-1），仅 22-5 保留 K。这 6 点的"仅 K"绑定是把"机器可验"误判为"需语义裁决"，改绑后可被 GSS/单测覆盖，直接减少 K3 负担。

### 2.3 改绑后的 K 类终稿（33 → 27）

- 26 个线级终审点（线 100% 门槛，K 类，合理保留——终审是质量裁决，非物理判定）
- 22-5（语义残留）
- 7 个仅 K 实质点改绑 M 后，**K 类从 33 降为 27**（26 终审 + 22-5）

### 2.4 任务 2 结论

三分法定稿：**M 126 / F 10 / K 27**（120+6 改绑 / 10 / 33-6）。M 类占比升至 77%。每类判定规则已给（机器 exit 码 / 演示 checklist / K3 复核报告），可直接回写 product-lines.yaml 证据绑定。

---

## 任务 3：非 LLM 验证套件方案评审与补强

### 3.1 覆盖矩阵裁决：方案覆盖面**成立**

DSH 附录 B.2 三层套件对 M 类点的覆盖核验：

| 层 | 方案 | 覆盖点数 | K3 核验 |
|---|---|---|---|
| L1 场景套件 | GS-01~08 | 103 scenario 点 | ✅ 映射真实（GSS 设计 §2.4 映射表逐线对应） |
| L2 单测映射 | suite-registry.yaml + A2 修复 | 18 test + 2 contract 点 | ✅ 可覆盖（15-4/21-2 golden-case 可复用现有 checker） |
| L3 契约对账器 | field-mappings snake/camel 对账 | 5-2/10-1（见 3.4 修正） | ⚠️ 零实现（见 3.4） |

覆盖面成立，**无"名义绑定实际验不到"的大面积虚标**——但有前置阻断（见 3.3/3.4）。

### 3.2 防伪充分性裁决：四道防伪方向对，但**第一道就是空的**

DSH 四道防伪核验：

| 防伪 | 裁决 | 依据 |
|---|---|---|
| fault-injection 红绿演练 | ✅ 充分（D396 范式：能证假绿被抓） | golden-case-checker.ts 已机器化红绿演练 |
| 证据仅 CI 入库 | 🔴 **当前为空** | A2 零入库（见 3.3）——CI 从未产出一条证据，"仅 CI 入库"无从谈起 |
| A1 失效检测（14 天 TTL + git 变更） | ✅ 已实现 | `calc-progress.py:151-163` 物理在跑 |
| K3 抽样重跑 | ✅ 可行 | 反向契约 R2（一行 run.sh 重跑）可支撑 |

**裁决：防伪四道中"证据仅 CI 入库"依赖的 A2 管线当前零产出，防伪链不完整。补齐 A2 是套件方案的前置条件（见 3.3）。**

### 3.3 A2 零入库断点定位（双断点）

A2 接线逻辑物理完整（`product-progress.yml:44-57`：vitest 全绿 → list-test-points → evidence-writer，fail-closed 不写假绿），但零入库，断点有二：

1. **vitest 从未全绿**：origin/main 快照复现 60 文件/98 用例失败，其中 57 处是 `tests/sentinels/` 引用 main 上物理不存在的 `extensions/sentinels/<旧名>/computes/`（git ls-tree 验证）。ci.yml 的 test job 有"pre-existing failures 不阻断"豁免 → PR 能绿着合并，但 A2 的 `npx vitest run` 永远非零 → 永不写证据。
2. **bot PR 链未走通**：产物提交到 bot 分支 + 开 PR 的链路历史零成功（`gh pr create` fail-closed 红灯），且无 auto-merge 机制 → 即使证据生成也到不了 main。

### 3.4 ⚠️ 关键发现：GSS 基建"建成但未合并进 main"（本评审最重要发现）

任务书附录 A 声称"GSS 基建四工具已建成（D361，有测试且全绿）"。K3 物理复核：

- **四工具（assert/bootstrap/fresh-db/inject + expect-schema.json）+ 测试（gss-common.test.ts）已建成**（含完整契约 JSDoc，工作区物理存在）。但它们**只提交到了一个 feature 分支**——`f67174c0` "feat(D361): GSS 基建"，位于 `feat/mac-d374-routing-d361-gss-base`（已推送该远程分支），**从未合并进 main**：`git ls-tree origin/main` 零命中，`git merge-base --is-ancestor f67174c0 origin/main` 物理证伪（未进 main），当前主工作区（feat/d395a 分支）里它们仍是 untracked。即"已建成"仅在一个未合并分支成立，对 main/CI/其他协作者而言**不存在**。
- 注：复核过程中一度有材料称 f67174c0"已合入 main"——经 `merge-base` 物理证伪（未进 main）。这正是"合并状态也要物理核验、不凭转述"的例证。

**含义**：基建"已建成"仅在一个未合并 feature 分支成立；对 main / CI / 其他协作者而言**它们不存在**。这正是产品线"机器证据层为零"的隐藏根因之一——**不是没有建，而是建了没合并进 main**。这也是"声称 vs 事实"（M2）在基建层的活样本：物理建成 ≠ 已交付 main。**修复前置：把 feat/mac-d374-routing-d361-gss-base 合并进 main（走 PR + K3 审计），套件方案才有地基。**

（另修正任务书表述：contract-check 绑定点为 5-2/10-1，非 5-2/10-2；10-2 仅绑 scenario:GS-03。）

### 3.5 缺口清单（机器方案覆盖不了、必须留在 K）

- 26 个线级终审点（质量裁决）
- 22-5（语义残留）
- 语义质量点：6-3 命中盲区、3-x 报告"麦肯锡级"质量、24-5 对抗性测试的语义部分
- F 类 10 点（人测）

### 3.6 任务 3 结论

套件方案**方向正确、覆盖面成立**，但落地有**三个前置阻断**：① GSS 基建四工具 + 测试提交进 git（3.4）；② A2 零入库双断点修复（vitest 全绿 + bot PR 链，3.3）；③ 契约对账器从零实现（3.1）。**顺序：先入库基建 → 修 A2 → 写契约对账器 → 再做 GS-01~08。**

---

## 任务 4：计分规则修订提案的审计意见

### 4.1 现状物理确认

`calc-progress.py` 六态状态机：K3/创始人演示 pass → `verified`（计分，:140-145）；机器绿（scenario/test/ci）→ 检查 TTL + A1 失效检测后 → `pending_k3`（**不计分**，:164）。即"163 点全部机器跑绿，进度仍为 4%"的声称**属实**。

### 4.2 对 DSH 附录 B.3 提案的裁决：**条件采纳**

| 提案 | K3 裁决 |
|---|---|
| 机器绿 → pending_k3 不计分 → 改为 机器绿 → verified 计分 | ✅ **采纳，但仅当满足 4.3 全部条件** |
| K3 从逐点裁判 → 线首次 100% 终审（20c）+ 日常抽样（20a，≤¥20/批）+ K 类点 | ✅ **采纳**——与 K3-AUDIT-STANDARD 维度 20 既定设计**完全一致**（20a 抽样 + 20c 终审本就是定稿） |
| 防伪从 K3 全量 → fault-injection + 证据仅 CI 入库 + A1 失效检测 + K3 抽样重跑 | 🟡 **部分采纳**——四道防伪方向对，但"证据仅 CI 入库"依赖 A2 修复（当前零入库，见任务 3），A2 未修复前机器证据链不完整 |

**裁决理由**：提案方向与已生效的维度 20 设计同向，不是"放开计分"，而是"把机器绿从排队等 K3 中解放出来，同时保留 K3 的终审与抽样兜底"。这是降本而非降防。

### 4.3 采纳的强制条件（不满足 = 否决）

1. **防伪四道必须全部落地**：① 每个 GSS 场景必须带 fault-injection 红绿演练（D396 范式——能证明"假绿会被抓"）；② 机器证据仅 CI 产出入库（禁止本地手写 evidence，A2 管线修复后）；③ A1 失效检测（14 天 TTL + git 变更转 stale，已实现于 :151-163）必须真实运行；④ K3 抽样重跑权保留。
2. **20c 线 100% 终审保留**：任一线首次达 100%（机器全绿）→ K3 对该线全部验收点全量复核，未过不得计 100%（这是防"机器全绿假象"的最后闸门，绝不可删）。
3. **20a 抽样保留**：无背书绿见一个查一个。
4. **K 类 27 点（26 终审 + 22-5）仍 K3 复核**，不进机器计分。

### 4.4 修改要求清单（K3 提要求，实现方执行，红线不变）

若创始人批准条件采纳，对 `calc-progress.py` + `product-lines.yaml` 的修改要求：

| # | 修改要求 | 对象 |
|---|---|---|
| R1 | `calc-progress.py:164` 机器绿分支：`pending_k3` 改为 `verified`，但**仅当该点 evidence 满足反向契约 R1-R7**（机器可解析/一行重跑/环境指纹/断言物理输出/重跑确定） | calc-progress.py |
| R2 | product-lines.yaml 每验收点补 `status` + `k3_verdict`(pending/pass/fail+报告ID) + `evidence_ref` 字段（反向契约 R5） | product-lines.yaml |
| R3 | 线进度达 100% 时强制 `k3_verdict=pending` 直到 K3 20c 终审 pass（封顶 99 的现有逻辑 `min(progress,99)` 保留并强化） | calc-progress.py |
| R4 | evidence 写入仅接受 CI 产物（带 run-id + git-hash 指纹），本地手写 evidence 拒收 | evidence-writer.py |

### 4.5 成本影响估算（供创始人参考，非承诺）

按抽样规则 §四成本口径：机器层全绿后各线至 ~75%（剩线级终审 + F 类点），总进度 4% → 70%+；K3 单批成本从"逐点全量"降为"终审 + ≤¥20/批抽样"，**降约一个数量级**。该估算待首批准后实测校准。

---

## 五、总体结论

### 对创始人两问的直接裁决

**问① 26 条线是否合理？** → **合理，维持不动。** 覆盖面完整（数据流+交互面+底座+业务循环无漏环）、粒度合理、验收点大部分物理可判定。仅需修 2 对真重复点（5-2/10-2、4-5/10-1）的措辞。

**问② 能否建非 LLM 测试套件替代大部分 K3 逐点审计？** → **能，且应该——但不是今天。** 套件方案方向正确、覆盖面成立、防伪设计（四道 + 反向契约 R1-R7）充分；K3 从"逐点裁判"转"终审+抽样"与既定维度 20 设计同向。**但落地有三个前置阻断，不解决则套件是空中楼阁。**

### 分级汇总

| 级 | 发现 |
|---|---|
| **P0** | **GSS 基建四工具 + 测试建成并提交到 feat 分支，但从未合并进 main**（§3.4）——套件方案的地基不在 main。先合并 feat/mac-d374-routing-d361-gss-base 进 main，一切才好谈 |
| **P1** | A2 零入库双断点：vitest 从未全绿（57 处 tests/sentinels 引用 main 不存在的旧 computes 路径）+ bot PR 链历史零成功（§3.3） |
| **P1** | 契约对账器零实现（5-2/10-1 两个 contract-check 点无对账器） |
| **P2** | 2 对真重复验收点（5-2/10-2 逐字重复、4-5/10-1 包含关系）需措辞裁决 |
| **P2** | 7 个仅 K 点中 6 个应改绑 M（7-1/7-2/9-2/11-1/11-2/18-1），可立即减负 |
| **P2** | 任务书附录 A 称四工具已合入 main（f67174c0）——经物理核验，该 commit 在未合并的 feat 分支，未进 main（转述失真已修正） |

### 给创始人的行动建议（按优先级）

1. **P0 先行：把 GSS 基建四工具 + 测试提交进 git**（走正常门禁 + K3 审计）。这是套件方案的地基，也是"机器证据层"零的突破口。工作量小（已建好，只是没提交）。
2. **P1：修 A2 双断点**——先修 57 处 tests/sentinels 的旧 computes 引用让 vitest 全绿，再修 bot PR 链（或改为 CI 直接写 main 的证据分支）。
3. **P1：批准计分规则"条件采纳"**（任务 4）——机器绿计分 + K3 终审/抽样兜底。
4. **P2：批准 6 个仅 K 点改绑 M**（任务 2），回写 product-lines.yaml。
5. **P2：裁决 2 对重复点措辞**（任务 1）。

### 转 PASS 条件（本评审当前为 CONDITIONAL PASS——数据复核通过，方案待前置阻断解决）

1. GSS 基建四工具 + 测试提交进 git（git ls-tree 可见）
2. vitest 全绿（tests/sentinels 旧引用修复）+ A2 产出至少 1 条 ci 证据入库
3. 契约对账器实现并通过 5-2/10-1 对账

满足后本评审转 PASS，套件方案进入全面实施。

---

*报告完。K3 独立评审，全部结论以可 grep/可复跑的物理事实为依据；附录 A 数据已逐项独立复现；本报告未修改任何代码/线集/审计标准。*

