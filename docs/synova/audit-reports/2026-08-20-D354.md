# 审计报告 2026-08-20-D354（模式 A v2 首用）

> 审计员: K3（Kimi，零上下文独立会话） | 范式: 声称↔证据驱动（K3-AUDIT-STANDARD-v2-20260820）
> 对象: D354 去重键稳定化 | commit dc4f4232（feat/win-d354-dedup-key-stability，已推送 origin，未合并 main）
> 运行环境注记: Windows / Git Bash / Node v24.15.0 / 工作区 HEAD 6d82ec77（落后 main b0560a63）
> 抽点 seed: 20260820 | 本批成本估算: ≈¥4（grep/git 类 12 条 + 格式代行校验 1 次；vitest 未执行）

## 0. 门禁与声称表存在性

| 检查 | 结果 | 判定 |
|------|------|------|
| verify-claims-table.sh 存在（工作区） | **不存在**（HEAD 落后 main；main:aadc1b04 有） | P1（部署漂移，见 §5） |
| pre-commit 接线（工作区） | `grep verify-claims-table scripts/pre-commit-check.sh` 零命中 | P1（同源漂移） |
| D354 分支含 G12d | `git merge-base --is-ancestor aadc1b04` ✓（提交时门禁已生效） | PASS |
| dev doc 交付声明节 | 存在（§8），K3 代行 G12d 格式校验 `exit=0` | PASS |

## 1. 声称↔证据对账表（U4 表逐行）

| # | 声称 | 证据命令 | 预期 | 实测摘要（@dc4f4232） | 语义有效性 | 判定 |
|---|------|---------|------|----------------------|-----------|------|
| 1 | 3 处 id 已去时间戳 | `grep -rn "Date.now()\|getTime()" src/sentinel/runner.ts src/sentinel/signal-aggregator.ts` | id 生成处零命中 | 5 命中但全部在非 id 生成处：runner.ts:807/813（窗口去重机制，dev doc §3.3 明示不改）、:833/859/927（耗时统计） | **有效**——预期措辞精确限定"id 生成处"，交付方预判了合法命中 | **PASS** |
| 2 | 稳定键已生效 | `grep -n "notif-\|conflict-\|sig_" ...` | 命中稳定 id 模板 | runner.ts:303 `notif-${signal.id}` / :963 `conflict-${relatedNodeId}` / aggregator:144 `sig_${entity}`，与 diff 物理一致（3 行改动，无夹带） | 有效 | **PASS** |
| 3 | 测试全绿 | `vitest run tests/sentinel/dedup-key-stability.test.ts` | 全 pass | **已补跑（2026-08-20 17:46）**：干净快照（git archive dc4f4232）6/6 全绿，exit 0，含「同 signal 跨 12 分钟两轮 → notif id 相同」关键用例 | 有效 | **PASS** |
| 4 | as any = 0 | `grep -rn "as any" ...` | 0 命中 | 0 命中（exit 1） | 有效 | **PASS** |

## 2. 沉默审计

- **DS 对账（S-10）**：dev doc 完成标准为 DS1-DS7 七条；U4 表仅 4 行（≈DS1/DS2/DS3 + as any）。**DS4（baseline 零回归）/DS5（范围一致）/DS6（无绕过）/DS7（推送+CI）无对应声称行、无显式 descope** → **P1-1**。dev doc 脚注自承诺"交付声明 DS 须与本文档 DS1-DS7 一一对应"，未做到。（常设项已代为核验 DS5/DS6/DS7 且均过，见 §4——表不齐是流程缺陷，不代表事实造假。）
- **diff 覆盖**：提交 4 文件 = runner.ts + signal-aggregator.ts + 新测试（均有声称行覆盖）+ `.claude/bypass.log`（无声称行；属控制塔工件，合理）→ 注记 P2-1：DS5 声称"与 §3.1 写集一致"，严格说写集表 3 文件 ≠ 提交 4 文件。
- **副作用沉默**：无新增 export / 无公共接口变更（diff 仅 3 行 id 模板修改）→ 无沉默面。

## 3. 逢绿必验 + 抽样

- 本任务无转绿声称（非产品线验收点），抽样池为空；U4 表 4 行即全部声称，已全验。
- 结转清单：~~D354 测试执行~~（已于 2026-08-20 17:46 补跑通过，结清零）。

## 4. 常设项（#13/#14/#15）

| 项 | 结果 | 判定 |
|----|------|------|
| #13 bypass 对账 | dc4f4232 提交内含 2 条 BLOCKED（20:45/20:47 pre-commit FAIL）→ 工作区 bypass.log 有 COMMITTED PASS（20:56:29，hash 吻合 dc4f4232）；无 --no-verify。证据链完整 | PASS |
| #13 CI job 级 | `gh` 不可用（command not found）→ 无法核实 CI job 结论 | **DEGRADED**（待用户补充 CI 链接） |
| #14 版本编排 | 提交不含 VERSION.md/version.log/tag 变更，commit message 无版本号幻影——与"纯产品修复不 bump"声明一致 | PASS |
| #15 并行合规 | 独立 feat 分支 + registry 含 worktree 字段（40 处）；D354 session 条目确认被审批阻断 | PASS（部分 DEGRADED） |

## 5. 分级汇总

| # | 级别 | 发现 | 证据 |
|---|:---:|------|------|
| P1-1 | P1 | 交付声明表 ≠ DS1-DS7 一一对应，DS4-DS7 缺项无 descope（S-10 违规） | dev doc §6 vs §8 |
| P1-2 | P1 | G12d 门禁部署漂移：在 main（aadc1b04）但本机工作区（HEAD 6d82ec77）无脚本无接线 | `ls` + `grep` 零命中 |
| P2-1 | P2 | 提交含写集外文件 .claude/bypass.log，DS5"与写集一致"措辞过强 | `git show --stat` |

## 6. 总体结论: **CONDITIONAL PASS（2026-08-20 17:46 补跑后定级）**

声称 1/2/3/4 + 常设项全部物理成立（测试补跑 6/6 绿，exit 0）。无 P0。P1×2（U4 表缺 DS 行 + G12d 部署漂移）+ P2×1（写集外 bypass.log）均为流程/环境缺陷，交付事实本身真实。

~~DEGRADED — 待补测试后定级~~（17:46 已在干净快照补跑通过，本行留存作审计轨迹）

补跑命令（工作区根目录，一条）：
```bash
mkdir -p .audit-tmp && cp -r /tmp/d354-t1 .audit-tmp/d354 && node_modules/.bin/vitest run --root .audit-tmp/d354 tests/sentinel/dedup-key-stability.test.ts; rm -rf .audit-tmp
```

## 7. L4 防线缺口收割

**发现**: 交付声明表与 DS 列表不齐（P1-1）在 D354 已是第二次发生（D331 DS13 首次）——S-10 已入 dev doc 脚注但仍靠自觉。
**本该拦住的防线**: G12d（U4 格式门禁）。
**为什么没拦住**: G12d 是格式版——只校验"有表、三列、命令白名单"，**不校验"表行 ↔ DS 列表对账"**。对账是语义判断，格式门禁管不到。
**缺什么**: G12d 第二版可加的机器校验：dev doc 含 `DS\d+` 编号集合 vs 交付声明表行数的**数量对账**（不等即警告，允许显式 descope 豁免行）。这是格式层可做、不需要语义的检查。另：G12d 的部署漂移（P1-2）印证了 v2 标准步骤 0 的必要性——门禁存在性必须每批核验，不能假设。
