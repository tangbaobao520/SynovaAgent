# 审计报告 2026-08-20-D357（模式 A v2）

> 审计员: K3（Kimi，零上下文独立会话） | 范式: 声称↔证据驱动（K3-AUDIT-STANDARD-v2-20260820）
> 对象: D357 L5 连接器声称诚实降级 | 主提交 b26e8d35（feat/win-d357-connector-descope，已推送 origin，未合并 main）
> 关联提交: b9cb6409（边界澄清协调文档，非交付主体）
> 运行环境注记: Windows / Git Bash / Node v24.15.0 / 工作区 HEAD 6d82ec77（落后 main）
> 抽点 seed: 20260820 | 本批成本估算: ≈¥2.5（纯 grep/git，无测试执行——任务性质为 grep 验证型）

## 0. 门禁与声称表存在性

| 检查 | 结果 | 判定 |
|------|------|------|
| verify-claims-table.sh（工作区） | 不存在（同 D354 漂移，HEAD 落后 main） | P1（沿用 D354 P1-2，不重复计） |
| G12d 代行格式校验 | `bash /tmp/verify-claims-table.sh <dev-doc>` exit=0 | PASS |
| D357 分支含 G12d | feat/win-d357-connector-descope 在 aadc1b04 的 contains 列表 | PASS |
| dev doc 交付声明节 | 存在（§8），三行三列 | PASS |

## 1. 声称↔证据对账表（U4 表逐行）

| # | 声称 | 证据命令 | 预期 | 实测摘要（@b26e8d35） | 语义有效性 | 判定 |
|---|------|---------|------|----------------------|-----------|------|
| 1 | 连接器声称已降级 | `grep -rn "CRM/ERP/HR 连接器" <arch-doc> <product-lines.yaml>` | 零命中 | 零命中（exit 1）；diff 确认 value 降级为「文件导入+问卷+飞书连接器（CRM/ERP/HR 直连推迟部署后）」，4-4/4-5/4-6 三验收点 uncommitted→deferred + note——与 §4.5 决策记录（deferred 非删除）一致 | 有效 | **PASS** |
| 2 | 注释已真实化 | `grep -n "飞书/钉钉/企微" src/connectors/index.ts` | 零命中 | 零命中（exit 1） | 有效 | **PASS** |
| 3 | 写集一致 | `git diff --name-only HEAD^` | 3 文件无越界 | **实测 4 文件**：3 目标文件 + `.claude/bypass.log`（控制塔工件） | 有效但预期不精确 | **P2**（见 §5） |

**附加语义核验（超出 U4 表的深验）**：架构文档 diff 确认 ①「连接器管道 ✅」→「⚠️ 管线已通、真实连接器未实现」② 1.1 节明确仅 feishu.py ③ IM 通道企微同步降级「待实现」④ 三处图文一致。无 overclaim 残留。**正面发现**：交付方主动降级了 U4 表未覆盖的 IM 企微声称——诚实度超出自证表范围。

## 2. 沉默审计

- **DS 对账（S-10）**：DS1-DS5 五条 vs U4 表 3 行——**DS4（无绕过）/DS5（推送+CI）缺行无 descope** → **P1-1**（与 D354 P1-1 同型，系统性复发，见 §7 L4）。
- **diff 覆盖**：3 目标文件均有声称行；bypass.log 无（工件，合理）。
- **副作用沉默**：`status: deferred` 是新状态值——CT-45 已登记 calc-progress.py SIX_STATES 无 deferred 态（渲染为 uncommitted、语义丢失，exit 0 不破 CI）。**非 D357 引入的缺陷，但 D357 是其触发源**——该跨域影响在 dev doc §3.1 有标注意识（"DSH 地盘"），记录为已知结转（CT-45，DSH 排期）。

## 3. 逢绿必验 + 抽样

- 本任务无转绿声称（反向任务：3 个验收点 uncommitted→deferred 是**诚实降级**，不触发逢绿必验；反而降低了假绿风险）。
- 抽样池为空。无结转。

## 4. 常设项（#13/#14/#15）

| 项 | 结果 | 判定 |
|----|------|------|
| #13 bypass 对账 | 工作区 bypass.log 有 COMMITTED PASS（2026-08-19T16:11:33，hash 吻合 b26e8d35），无 --no-verify，无前置 BLOCKED | PASS |
| #13 CI job 级 | gh 不可用 → 无法核实 | DEGRADED（待补充） |
| #14 版本编排 | 提交不含 VERSION.md 变更、消息无版本号——与"纯文档+注释不 bump"声明一致 | PASS |
| #15 并行合规 | 独立 feat 分支已推送（origin 同步 b26e8d35）；dev doc 头部已标注跨机冲突风险（product-lines.yaml 为 DSH 地盘） | PASS |

## 5. 分级汇总

| # | 级别 | 发现 | 证据 |
|---|:---:|------|------|
| P1-1 | P1 | U4 表 3 行 ≠ DS1-DS5 五条，DS4/DS5 缺行无 descope（S-10，D357 为第三例） | dev doc §6 vs §8 |
| P2-1 | P2 | 写集声称「3 文件」实测 4 文件（+bypass.log 工件），预期措辞不精确 | `git show --name-only` |

## 6. 总体结论: **CONDITIONAL PASS**

三条核心声称全部物理成立且语义有效；降级内容深验（deferred 标记、架构文档措辞、IM 同步降级）全部真实。无 P0。P1 为流程性缺陷（U4 表与 DS 不齐），不影响交付事实本身。

## 7. L4 防线缺口收割

**发现**: 「U4 表行数 ≠ DS 条数」在 D354/D357 连续两批复发（D331 为首次）——三批三例，已是**系统性模式**而非个案。
**本该拦住的防线**: G12d 格式门禁 + S-10 脚注承诺。
**为什么没拦住**: G12d 只验"有表且格式对"，不验"表↔DS 对账"；S-10 只是 dev doc 脚注文字，无机器执法。交付方写表时默认只写"代码事实型"声称，系统性漏掉"流程型" DS（无绕过/推送/CI）。
**缺什么**: 与 D354 报告 §7 同一缺口——G12d 第二版增加机器可做的数量对账：提取 dev doc `DS\d+` 编号集合 vs 交付声明表行数，不等即警告（允许显式 descope 豁免行，如「DS4-5 流程项，由 K3 常设项覆盖」）。**建议提升优先级：三批三例，应直接进 G12d 第二版需求而非排队。**
