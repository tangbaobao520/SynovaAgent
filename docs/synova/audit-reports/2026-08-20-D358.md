# 审计报告 2026-08-20-D358（模式 A v2）

> 审计员: K3（Kimi，零上下文独立会话） | 范式: 声称↔证据驱动（K3-AUDIT-STANDARD-v2-20260820）
> 对象: D358 合并哨兵去 _extinct 桥接 + props 契约对齐 erp-standard
> 提交: 38dda82f（feat/win-d358-merged-sentinel-deextinct）→ **已合并 main**（PR #58, e2c3f060，远程分支已清理）
> 材料注记: dev doc 以**提交版**（205 行，含 §3.2 最终实现回填）为准；工作区散落旧版 122 行（HEAD 6d82ec77 落后 main）
> 运行环境注记: Windows / Git Bash / Node v24.15.0 / 测试在干净快照（git archive 38dda82f）执行，依赖经工作区 node_modules 向上解析
> 抽点 seed: 20260820 | 本批成本估算: ≈¥5（快照解包 + 80 用例测试 + grep/git 类 10 条）

## 0. 门禁与声称表存在性

| 检查 | 结果 | 判定 |
|------|------|------|
| verify-claims-table.sh（工作区） | 不存在（同 D354/D357 漂移，HEAD 落后 main） | P1（同根因，不重复计） |
| G12d 代行格式校验 | exit=0（三行三列，白名单合规） | PASS |
| D358 分支含 G12d | feat/win-d358-merged-sentinel-deextinct 在 aadc1b04 contains 列表 | PASS |
| dev doc 交付声明节 | 存在（§8，4 行） | PASS |

## 1. 声称↔证据对账表（U4 表逐行）

| # | 声称 | 证据命令 | 预期 | 实测摘要（@38dda82f） | 语义有效性 | 判定 |
|---|------|---------|------|----------------------|-----------|------|
| 1 | _extinct 动态 import 清零 | `grep -rn "import('../_extinct" extensions/sentinels/margin-health extensions/sentinels/capital-health` | 零命中 | 零命中（exit 1） | 有效 | **PASS** |
| 2 | props 归一化对齐 erp-standard | `grep -n "total_revenue\|gross_margin\|total_debt\|total_assets" 两 aggregate` | 命中 snake_case | margin 10 命中 + capital 29 命中 ✓；**但 §3.2 DS3 证据摘要声称「margin 10 处 / capital 46 处」，按其自写口径实测 margin=12 / capital=42**——方向真实、数字失真（两个方向都偏，像交付中途计数） | 有效 | **PASS w/ P2**（见 §5 P2-1） |
| 3 | 测试全绿 | `vitest run tests/sentinel/margin-capital-deextinct.test.ts` | 全 pass | **干净快照执行 DS1 完整集（4 个测试目标）：18 文件 / 80 用例全绿，exit 0，3.17s**——与 DS1 声称「18 files / 80 tests 全绿」完全吻合；输出含预期降级 warn（分母 0 → log.warn + 跳过，决策 5/8 行为可见） | 有效 | **PASS** |
| 4 | as any = 0 | `grep -rn "as any" extensions/sentinels/margin-health extensions/sentinels/capital-health` | 0 命中 | 0 命中（exit 1） | 有效 | **PASS** |

**附加语义核验**：①接线链（决策 7）——`src/sentinel/types.ts:185-190` import-type 指向新 computes 位置（物理存在），两 manifest.json `entryPoint: ./aggregate.ts` 未变（loader 目录扫描注册保持，S-3 生产装配不断）；②写集规模——§3.1 原始声明 19 文件 vs 实际 43 文件，§3.2 已同 commit 回填（S-6 执行到位，G12c 双向对账记录「声明 42 = 实际 42」）；③决策记录 10 条含中途契约迁移（D455 上游合并 30 commits）处置——诚实且可核。

## 2. 沉默审计

- **DS 对账（S-10）**：DS1-DS8 八条 vs U4 表 4 行——DS5（零回归）/DS6（范围一致）/DS7（无绕过）/DS8（推送+CI）缺行无 descope → **P1-1**（第四例，系统性）。**缓解项**：§3.2「DS 证据摘要」实际覆盖了 DS1-DS8 全部八条（含 baseline 对照细节），信息在，只是没进 U4 表——表是给机器和 K3 快查的，摘要散文不满足"逐行可跑"。
- **diff 覆盖**：43 文件中 extensions 18 + tests 19 有声称覆盖；6 个 .claude/ 元文件（bypass.log/current-brief/plan.json/reference-map.md/task-brief/dev doc）无声称行——其中 plan.json +236 行、reference-map.md +236 行属会话状态工件，合理但建议以后在表里加一行元文件兜底声称 → P2-2。
- **副作用沉默**：决策 4（CCC 死代码接线 → 新增 f5-ccc-crit/warn finding 产出）是**行为新增**，已诚实记录在 §3.2——非沉默，正面案例。写集计数差 1（回填声称 42 vs git 实际 43）→ 并入 P2-2。

## 3. 逢绿必验 + 抽样

- 本任务无产品线转绿声称。U4 表 4 行全验完毕（含 T1 级快照测试）。
- 抽样池：优先级 4 随机项——15 项清单中未覆盖项抽查 1 点：**#4 架构边界**，`git grep` 两个 aggregate 无跨层 import（extensions 自含）→ PASS。批次合计 5 点 / ≈¥5，上限内。

## 4. 常设项（#13/#14/#15）

| 项 | 结果 | 判定 |
|----|------|------|
| #13 bypass 对账 | 提交内含 2 条 BLOCKED（21:00/21:51 pre-commit FAIL）→ 工作区 bypass.log 有 COMMITTED PASS（22:16:34，hash 吻合 38dda82f）；无 --no-verify。red→green 修正过程完整可见 | PASS |
| #13 CI job 级 | gh 不可用 → 无法核 CI；**但提交已经 PR #58 合并进 origin/main**（合并门禁通常含 CI，无法物理确认，记 DEGRADED） | DEGRADED |
| #14 版本编排 | 提交不含 VERSION.md、消息无版本号幻影，与「纯产品重构不 bump」声明一致 | PASS |
| #15 并行合规 | 独立 feat 分支；dev doc 头部声明与 D354/D359 的交集分析；D455 上游合并走了 merge 平基（非 rebase，D363 先例） | PASS |

## 5. 分级汇总

| # | 级别 | 发现 | 证据 |
|---|:---:|------|------|
| P1-1 | P1 | U4 表 4 行 ≠ DS1-DS8，DS5-DS8 缺行无 descope（S-10 第四例） | dev doc §6 vs §8 |
| P2-1 | P2 | §3.2 DS3 计数声称（10/46）与实测（12/42）双向不符——证据摘要数字非当次运行输出（D331「25/25≠24/24」同型） | git grep -c 实测 |
| P2-2 | P2 | 写集计数差 1（回填声称 42 = 实际 42，git 实际 43）；6 个 .claude/ 元文件无 U4 声称行 | git show --name-only \| wc -l = 43 |

## 6. 总体结论: **CONDITIONAL PASS**

四条 U4 声称全部物理成立（含干净快照 80/80 测试绿）；接线链、生产装配、决策回填、诚实记录（中途契约迁移处置）质量为三批最高。无 P0。P1 为 S-10 流程缺陷（系统性，见 L4）；P2 为计数精度问题。

## 7. L4 防线缺口收割

**发现**: S-10（U4 表 ↔ DS 一一对应）在 D354/D357/D358 连续三批全犯，加上 D331 共四例——100% 复发率说明这不是执行疏忽，而是**机制缺位**。
**本该拦住的防线**: G12d 格式门禁。
**为什么没拦住**: G12d 校验"有表、三列、命令白名单"，不校验"表行 ↔ DS 编号集合对账"。交付方写表时系统性只覆盖"代码事实型"声称（可 grep 的），漏掉"流程型"DS（baseline/范围/无绕过/推送 CI）。
**缺什么**（与 D354/D357 报告同一缺口，第三次记录，正式升级为 G12d 第二版需求）：
1. G12d 增加**数量对账**：提取 dev doc `DS\d+` 编号集合 vs U4 表行数，不等即阻断（允许显式 descope 行豁免，格式：`| DS5-DS8 流程项 | 由 K3 常设项覆盖 | descope |`）；
2. 证据数字规则：交付声明/证据摘要中的计数（N 处/N 文件/N 用例）必须从当次运行输出原样复制——D331（25/25≠24/24）与本批（10/46≠12/42）同型，建议入 dev doc skill 自检清单。
**附带正面记录**：D358 的 §3.2 同 commit 回填（S-6）+ 决策记录 10 条 + 中途上游合并处置，是本系列交付质量的标杆——建议 Codex 把这份 dev doc 作为 S-6 回填的参考样例。
