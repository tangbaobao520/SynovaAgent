# K3 独立审计报告 — D512（GS 场景全量刷新+进度重算）+ D511（版本守卫门禁）

> 审计员: Kimi K3（独立工作区，零上下文）| 日期: 2026-08-24 | 协议: AUDIT-PROTOCOL.md L1-L4
> 材料: 并行派单 97f85b20 / dev doc（D511=27d1ca5f、D512=988154a7→5818f2a0）/ task brief（仅 D511）/ AGENTS.md / PRODUCT-BRIEF / 审计基线（audit-check.py --full）/ 执行证据（bypass.log + git 提交链）
> 运行环境: macOS（synova-wt-D511 @ db1b9431、synova-wt-D512 @ 530adb1e，bare ~/SynovaAgent 只读）+ 沙箱提升跑 vitest/control-tower 测试

---

## 0. 结论

| 任务 | 判定 | 要点 |
|------|------|------|
| **D512** | **CONDITIONAL PASS** | DS1-DS11 主干声称全部物理属实；P1×1（main_sha 字段值非 main 的 sha）+ P2×3 |
| **D511** | **CONDITIONAL PASS** | DS1-DS11 主干声称全部物理属实；P1×1（version.log 混入 V9.9.9 测试残留）+ P2×2 |

两任务均无 P0。核心交付（run-all.sh 全量入口 + 诚实 RED 证据链；版本守卫三态 fail-closed）实测真实、接线完整、无 --no-verify 绕过。

---

## 1. D512 — GS 场景全量刷新+进度重算（feat/d512-gs-refresh @ 530adb1e）

### 1.1 声称 ↔ 证据（claim-verifier 独立实测）

| DS | 声称 | 物理证据（file:line / 命令输出） | 判定 |
|---|---|---|---|
| DS1 run-all.sh 交付 | 依赖排序 gs03→gs05→gs02→gs04→gs06→gs07→gs08→gs01；--skip=/--dry-run；逐场景退出码汇总 exit 1；main_sha/refreshed_at 注入 | `scripts/golden-scenarios/run-all.sh`（e9e2be93）：ORDER L50 `(gs03 gs05 gs02 gs04 gs06 gs07 gs08 gs01)`；`--skip=*`/`--dry-run` L54-59；未知参数 exit 2 L61-63；FAIL 汇总 `exit $FAIL` L115；`inject_sha()` L73-95 只注"文件名含今天日期"的证据（诚实性守卫），fail 场景同样注入（assert.ts 已产出 verdict=fail 证据） | ✅ |
| DS2 7 场景重跑 | evidence/GS-{02..08}-2026-08-24.json 7 个新证据，全部含 main_sha: 23c091ad；GS-01 skip（D510 未合 main） | 实测 7 文件存在且 JSON 合法，全部含 `"main_sha": "23c091ad"` + `refreshed_at`（GS-02/04 fail，其余 pass）；`git merge-base --is-ancestor d510… main` = NOT ancestor（D510 未合）→ skip 决策成立 | ✅ *（见 P1-1）* |
| DS3 GS-02/04 诚实记录 | fail + findings 空 + critical 未触发 | GS-02：`demand-shift-critical-triggered` verdict=fail（`"severity":"critical" 未命中`）；GS-04：`key-person-risk-triggered` verdict=fail 同型；负向断言（空库无 critical）pass。无假装绿 | ✅ |
| DS4 GS-07 矛盾统一 | 重跑 3/3 全过（pass 真实）；README 更正 | GS-07-2026-08-24.json 3 断言全 pass（pii-scrubbed/rbac-denied/pii-boundary-s1）；README diff：`契约级 RED 2/3` → `契约级 PASS 3/3 + 场景覆盖缺口（D338）显式标注`（e9e2be93 diff 确认） | ✅ |
| DS5 schema 向后兼容 | schema=1 + main_sha/refreshed_at；refresh-all.sh exit 0 消费 | 新证据 schema=1 + 新字段；**独立重算实测**：`python3 scripts/product-lines/calc-progress.py --out /tmp/…` exit 0、`产品总进度 4% | 已验证验收点 7 | 待重跑 0`、无报错 | ✅ |
| DS6 进度重算 | generated_at 08-17→08-24 01:54；verified_sum 7/163、pct 4%；L1 未上调是诚实结果 | progress.json `generated_at: 2026-08-24 01:54:10`；重算实测 sum verified 7 / 163、pct 4；L1 线 0/8（GS-01 skip → T-1-01 未兑现）→ 未上调 = 诚实，非失真 | ✅ |
| DS7 todos 消化 | 49 条重算；T-8-01 类保留理由 = 权威偏差登记册独立数据源 | todos.yaml `generated_at: 2026-08-24 01:54:10`；T-8-01（L64）`source: "权威偏差登记册 v2"` 保留；aggregate-todos.py L125 `parse_registry`（源②登记册）逻辑存在；新增 T-10-04 条目（D356 核销） | ✅ |
| DS8 CI 接入 | product-progress.yml:62 push main job 加 run-all.sh --skip=gs01，::warning 不阻断 A1-A4 | diff（e9e2be93）：push main job 内 `D512 GS 场景刷新` 步骤 + `--skip=gs01` + `echo "::warning::GS 场景部分失败…不阻断进度重算"`，位于 refresh-all.sh 步骤之前 | ✅ |
| DS9 质量门禁 | 13 组 pre-commit 全过；as any=0；vitest 63 失败全为 main 既有；写集零越界 | bypass.log：D512 5×BLOCKED（01:58-02:03）+ COMMITTED PASS（8af829f8 原始提交 + rebase 对账），无 --no-verify 记录；**D512 worktree 实测 vitest：Tests 63 failed | 3552 passed**（与声称数字完全一致），失败文件全在 tests/{acceptance,agent,expert,l3,l4,middleware,sentinels,e2e…}——与写集（scripts/golden-scenarios、workflows、docs/synova/product-lines）零交集；写集 diff 零 src/、零 scripts/control-tower/ | ✅ |
| DS10 推送 | origin/feat..HEAD 为空 | `git rev-list --left-right --count origin/feat/d512-gs-refresh...HEAD` = `0 0` | ✅ |
| DS11 决策记录 | §5.3 四决策点全按 A | dev doc §5.3 四行 A 决策（--skip 分批/诚实 RED/向后兼容/失败不阻断），实现逐一对应 | ✅ |

### 1.2 发现（D512）

**P1-1（数据语义）: main_sha 字段值与 main 不符** — `git show 23c091ad` = `chore(task-state): D512 spec.commit 回填`（rebase 前的 D512 分支中间提交），`git merge-base --is-ancestor 23c091ad main` = NOT ancestor。run-all.sh L51 `MAIN_SHA="$(git rev-parse --short HEAD)"` 取的是**运行分支 HEAD**，编码 session 在 feat/d512-gs-refresh 分支上跑场景 → 字段名叫 main_sha 但内容不是 main 的 sha。
- 归因: devdoc（契约 §3.5 定义 `main_sha = git rev-parse --short HEAD` 时未约束运行分支；完成标准 ①"证据带 main sha"验收未要求物理在 main 上跑）
- 影响: A1 失效归因参考值失真（未来审计拿 23c091ad 对不上 main 历史）；不阻断（A1 真正依据是证据日期 + 线 modules git log，非本字段）
- 修复建议: run-all.sh 在非 main 分支运行时将字段写为 `<branch>/<sha>` 或警告；dev doc 契约修正为"main_sha = main 分支 sha（非 main 运行时显式标注分支）"

**P2-1（契约不符）**: run-all.sh 头注释声称 `--skip` id "不区分大小写"（L10），但 `case ",$SKIP," in *",$id,"*`（L96）大小写敏感——`--skip=GS01` 不生效。归因: implement。

**P2-2（流程）: D512 无 task brief** — 全分支检索 `.claude/task-briefs/` 无 D512 条目（仅存未跟踪的模板占位 `2026-08-24-D512-并发测试-8.md`，Q0-Q3 全 `<占位符>`，从未入库）。pre-commit 组 6 仅对 `STAGED_SRC`（src/ 暂存）强制 brief（pre-commit-check.sh L638 `if [ -n "$STAGED_SRC" ]`）→ scripts/ 变更无 brief 硬门禁。D511 反而自写 brief。归因: control-tower（门禁检测面窄）。

**P2-3（措辞）**: `docs/synova/product-lines/evidence/test-2026-08-24.json` 的 A2 证据 quote 全为 "vitest 套件全绿"（run-machine-evidence.sh 只跑 yaml 绑定的测试文件子集，非全量）——与全量 63 失败并存，字面会误导。A2 既有机制行为，D512 未改，但作为写集产物入库，建议注明"绑定套件全绿（非全量）"。归因: audit（A2 证据措辞语义）。

### 1.3 亮点

- **诚实性加固属实**: `inject_sha()` 只对"文件名含今天日期"的证据注入（L79 `ls -t "$EVIDENCE_DIR"/GS-0*"${num}"-"$today".json`），杜绝"把今天 sha 盖到旧证据"——dev doc 蓝图（取最新 mtime 文件）有该缺陷，编码 session 实测发现并加固，注释留档（L62-70）。
- **macOS bash 3.2 多字节 bug 根治属实**: 无 `export LC_ALL`（仅 `export PYTHONIOENCODING=utf-8`），全脚本 `${var}` 花括号，注释详述 `"$rc）" → unbound variable` 机理（L42-49）。
- **GS-02/04 fail 证据链完整**: verdict=fail + 断言级 detail（`contains "severity":"critical" 未命中`）+ 负向断言 pass（防假转绿）——诚实 RED 红线守住。

---

## 2. D511 — 版本守卫门禁（feat/d511-version-guard @ db1b9431）

### 2.1 声称 ↔ 证据（claim-verifier 独立实测）

| DS | 声称 | 物理证据（file:line / 命令输出） | 判定 |
|---|---|---|---|
| DS1 守卫脚本交付 | 三态退出 0/1/2 + 检测面 + 逃生舱 | `scripts/control-tower/check-version-guard.sh`（ab0e81f4）：逃生舱 L33-41（记 degraded-events.log JSON 行）；git 失败 → exit 2 L54-59；GATE_FILES_RE L30；VERSION.md 不可解析 → exit 2 L78-83；无 bump → exit 1 L91-98 | ✅ |
| DS2 测试全过 | PASS=15 FAIL=0；red 已证（实现前 exit 127） | **实测 `bash tests/control-tower/check-version-guard.test.sh` = PASS=15 FAIL=0**；bypass.log 02:05:20 BLOCKED（pre-commit FAIL exit 1）证明拦截史真实 | ✅ |
| DS3 组 14 接线 | L1163 调用 + L1152 组标题；DOC_ONLY 早退（L189）之后；exit 1/2 均 hard_check | pre-commit-check.sh：L1163 `VG_OUT=$(bash "$ROOT/scripts/control-tower/check-version-guard.sh" 2>&1)`；L1155 组标题（声称 1152 差 3 行，无实质）；L1168/L1170 exit 1/2 → `hard_check`；组 14 位于 DOC_ONLY（L189）与组 13 之后、CP3（L1176）之前 | ✅ |
| DS4 模拟拦截复现 | 测试用例 1/2/6 + 命令行直测 exit=1 | 实测用例 1/2/6 exit 1；**独立直测** `SYNO_STAGED_FILES="scripts/control-tower/alloc-task-id.sh" … bash check-version-guard.sh` → exit=1 + 点名变更文件；带 VERSION.md → exit 0 | ✅ |
| DS5 吃自己的药 | ab0e81f4 含 VERSION.md + 4 个门禁文件 | `git show ab0e81f4 --name-only`：VERSION.md + check-version-guard.sh + pre-commit-check.sh + tests + version.log + brief + 决策 Note + bypass.log——同 commit 带 bump | ✅ *（ab0e81f4 的 pre-commit 记录为一次性补记，见 P2-2）* |
| DS6 version.log 追加 | control_tower_log.py version 写入 logs/version.log（非 dev doc 猜测路径），git add -f 入库 | version.log 入库（new file 2 行），路径 `.codex/control-tower/logs/version.log`（L95 `_append("version.log", …)`，非 `logs/version/v1/`） | ⚠️ *（见 P1-1）* |
| DS7 纯文档/非门禁零打扰 | 测试 4/5/7 全 exit 0 | 实测用例 4（docs/）exit 0、5（src/backup/golden-scenarios/product-lines）exit 0、7（仅 VERSION.md）exit 0 | ✅ |
| DS8 逃生舱显式降级 | 测试 9：SYNO_SKIP_VERSION_GUARD=1 → exit 0 + degraded-events.log 行数递增 | 实测：exit 0 + `degraded-events.log 追加降级记录 (0→1)` | ✅ |
| DS9 零回归 | 63 vitest 失败与基线（未改动 D512 worktree）diff 仅时间戳/pid/路径；as any=0；14 组全过；无 --no-verify | **D511 worktree 实测 vitest：Tests 63 failed | 3552 passed** —— 与 D512 worktree（63 failed）逐数字相同，失败测试文件集合一致，与 D511 写集（scripts/control-tower、pre-commit-check.sh、VERSION.md、tests/control-tower）零交集；**实测 tests/control-tower 全目录：49 个测试 13 个失败**（与声称"13 个存量失败"一致，失败文件均为既有测试）；写集 diff `as any` 计数 0；bypass.log 无 --no-verify 记录 | ✅ |
| DS10 推送完成 | c0ed0054..db1b9431 → origin；rev-list 0 0 | `git rev-list --left-right --count origin/feat/d511-version-guard...HEAD` = `0 0` | ✅ |
| DS11 决策记录 | V4.10.0 vs V4.9.1 分歧 + 三决策点 → dev doc §5.3 + 决策 Note + VERSION.md 条目 | dev doc §5.3 四决策点（B V4.10.0 / A 全拦+逃生舱 / A 独立脚本 / A 逃生舱）+ 决策 Note（memory/notes/implemented/2026-08-24-d511-version-guard.md）+ VERSION.md V4.10.0 条目；规范 §二原文"新增机制/组件/门禁组 MINOR"（版本管理规范 L17）→ V4.10.0 依据成立 | ✅ |

### 2.2 发现（D511）

**P1-1（数据污染）: version.log 混入 V9.9.9 测试残留** — `.codex/control-tower/logs/version.log` 两行：
```
{"version": "4.10.0", "changes": "D511 版本守卫门禁：…", "time": "2026-08-24T00:13:10+0800"}
{"version": "9.9.9", "changes": "auto-tag V9.9.9", "incident": "", "schema": "control-tower/logs/version/v1", "time": "2026-08-24T00:17:48+0800"}
```
V9.9.9 是 tag-consistency.test.sh 的测试假版本（tests/control-tower/tag-consistency.test.sh L6/73-85 `new_repo "$R1" "V9.9.9"`），经 synova-commit `auto_tag_and_version()`（L198-204 `git tag -a "$ver"` + `python3 "$LOG_PY" version --version "${ver#V}" --changes "auto-tag $ver"`）写入**真实仓库** version.log：`LOG_PY` 固定用 `$PROJECT_ROOT`（真实仓库路径）而 `VERSION_MD` 跟随 cwd repo（测试时 cwd = 临时仓库）——测试运行把 9.9.9 泄入真实 version.log，D511 提交时两行一起 `git add -f` 入库。
- 归因: control-tower（version.log 写入无测试隔离；三处同步机制只校验 VERSION.md↔tag，未校验 version.log）
- 影响: 机器可读证据链被污染——按 version.log 解析最新版本会得到 9.9.9 > 4.10.0（若未来有消费方读它判定版本即误判）；规范§一铁律 4"一处定义三处同步"被破坏
- 修复建议: 删除 9.9.9 行 + 给 auto_tag_and_version 的 LOG_PY 增加 `SYNO_VERSION_LOG` 注入缝或校验（非真实仓库根不写）；tag-consistency 测试后清理

**P2-1（流程）: G12 附带修复进主提交** — ab0e81f4 内含超出 dev doc §5.1 写集声明的改动：pre-commit-check.sh 组 12 认领 TSV 生成 `sed "s|^|$BNAME\\t|"` → `awk -v b="$BNAME" '{print b "\t" $0}'`（2 处，diff 确认语义等价）。**已显式声明**（完成报告 + VERSION.md 条目 + 决策 Note）且必要性自证成立（Mac 上 sed \t 不展开 → 所有代码文件误报不在 Q2 范围 → D511 自身无法过组 12）。透明合规，但控制塔脚本变更应独立评审——记为 P2 流程建议。归因: implement（已按透明纪律报告）。

**P2-2（证据弱一档）: ab0e81f4（吃自己的药提交）的 pre-commit 记录为"一次性补记（D331 对账）"** — bypass.log 02:02:54 补记而非实时 pre-commit PASS。db1b9431（tip）有实时 PASS（02:08:26）。不构成绕过（无 --no-verify），但"真实提交链路两次自证"的表述中实现提交那次是补记。归因: control-tower（synova-commit/rebase 链 pre-commit 记录偶漏，D331 对账机制已兜底）。

**P2-3（行号声称偏差）**: 组 14 标题实际 L1155（声称 L1152），L1163 调用准确。无实质。

### 2.3 附带事项核实

- **远端孤儿 tag V4.7.1 属实**: `git ls-remote --tags origin` → V4.7.1 指向 2b043c83（tag 对象）→ f685fa06（D329 事故孤儿）；本地重指 8192f5b0 未推送。处置建议移交 CTO ✅
- **本地 V4.10.0 已删**: 本地/远端均无 V4.10.0 tag（D511 自动打在 ab0e81f4 的 tag 被 D512 按门禁建议删除，D511 合并时重打）——两任务行为互相印证 ✅

---

## 3. L1-L4 逐层判定

| 层 | D512 | D511 |
|---|---|---|
| L1 代码 | run-all.sh 契约完整、诚实性加固到位；P2-1 大小写 | 守卫三态/检测面/逃生舱实现与契约一致；fail-closed 分支完备 |
| L2 偏离 | 写集与 dev doc 基本一致（§5.1 表漏列 todos.yaml/progress.html/run-all.test.sh/GS-07 README 4 个 DS 承诺产物，均在其他章节显式承诺——P2）；决策点全按 A | 写集 = dev doc + brief 列明的 6 文件 + 已声明的决策 Note/补记；附带修复 G12 超写集但已声明（P2-1） |
| L3 执行 | bypass.log 5×BLOCKED + PASS 链完整，无 --no-verify；CI 接入真实接线 | bypass.log BLOCKED（02:05:20）+ 实时 PASS（02:08:26）真实；实现提交为补记（P2-2）；测试实测 15/15 |
| L4 缺口 | 见 §5 | 见 §5 |

## 4. 北星三问裁决（north-star-guard）

| 问 | D512 | D511 |
|---|---|---|
| ① 服务真实用户场景？ | 创始人看完成度仪表盘 + CTO 部署轨决策——PRODUCT-BRIEF §六 P0"诊断报告质量验证"的机器证据化，间接服务 FDE 交付可信 | 编码 session + CTO（控制塔维护者）——版本管理规范 §四 CT 队列待办机器化 + 铁律 35 |
| ② 更接近终态？ | 是——进度链"数字不撒谎"是部署轨收官，证据带 sha + A1 归因精确 | 是——"bump 与代码同 commit"从条文变物理强制 |
| ③ Synova 还是 Agent？ | 不碰产品本体（src/ 零触碰），验证层强化，不变味 | 不碰产品本体，门禁纪律强化，不变味 |
| **裁决** | **对齐**（§六 P0 证据链真实化） | **对齐**（控制塔纪律物理化） |

## 5. L4 防线缺口收割

| 发现 | 本该拦住的防线 | 为什么没拦住 | 免疫细胞建议（一类一机制） |
|---|---|---|---|
| D512 P1-1 main_sha ≠ main | dev doc 契约审查（§3.5）+ 完成标准①验收 | 契约定义 `main_sha = git rev-parse --short HEAD` 未约束分支；验收无"物理在 main 上跑"检查 | run-all.sh 检测 `git branch --show-current`，非 main 时字段写 `<branch>/<sha>` 并 WARN（归因 devdoc） |
| D511 P1-1 version.log 9.9.9 污染 | tag-consistency 测试隔离 + version.log 消费校验 | auto_tag_and_version 的 LOG_PY 固定 PROJECT_ROOT（真实仓库），VERSION_MD 跟随 cwd（临时仓库）——测试时写穿到真实仓库；三处同步只测 VERSION.md↔tag，漏 version.log | control_tower_log.py 增加写入路径守卫（真实仓库根校验或 SYNO_VERSION_LOG 注入缝全覆盖）+ version.log 最新版本与 VERSION.md 一致性校验（9.9.9 立即暴露）（归因 control-tower） |
| D512 P2-2 无 task brief | pre-commit 组 6（Task Brief） | 组 6 仅对 `STAGED_SRC`（src/）强制——scripts/ 控制塔/场景脚本变更无 brief 门禁 | 组 6 检测面扩展至 `scripts/control-tower/`、`scripts/pre-commit-check.sh`、`scripts/golden-scenarios/`（最小改动；注意与 D511 版本守卫互补不重叠）（归因 control-tower） |

> 防再犯守门人核查: 三缺口均**命中既有 M 类模式**（M-契约定义缺陷 / M-测试环境写穿真实仓库 / M-门禁检测面窄），建议强化既有类防线而非新增 M 类——不新增脚本类机制（version.log 校验可并入 tag-consistency.test.sh 既有测试，非新脚本）。

## 6. 运行环境注记

- 全量 vitest 在 danger-full-access 沙箱下于两 worktree 实测（默认沙箱对 worktree 写入 EPERM——环境差异，非测试本身失败）
- tests/control-tower 49 个测试中 13 个失败为**存量**（baseline-check/baseline-exemption/brief-template-decision/commit-msg-consistency/daemon-smoke/gen-cto-health-repro/hooks-install/incident-loop/notes-four-state/synova-commit/tag-consistency/today-by-name/utf8——均与两任务写集零交集）
- 审计基线 `audit-check.py --full` = PASS:2 WARN:896 FAIL:458（main 存量，与两任务无关）

## 7. 移交清单（供 CTO/创始人）

1. **D511 P1**: 删 version.log 9.9.9 行 + 修 auto_tag_and_version 写穿（或并入 D513 控制塔返修批次）
2. **D512 P1**: run-all.sh 非 main 分支时 main_sha 标注分支（或契约为"main 分支跑"）
3. 远端孤儿 tag V4.7.1 处置（push -f 或删除）
4. D510 合并后补跑 GS-01（去掉 --skip=gs01）并移除 CI 步骤 skip
5. GS-02/04 业务修复另起任务（demand-shift 阈值链 / hr-upload 映射，疑似 D355 链路残留）
6. 63 vitest 存量失败 + 13 control-tower 存量失败排队清理
7. 组 6 brief 检测面扩展评估（P2）
