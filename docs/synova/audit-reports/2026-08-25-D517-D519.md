# K3 独立审计报告 — 切片 A（D517 安装包可产出 / D518 单一入口 / D519 Mac 实测）

> 审计员: Kimi K3（独立工作区，零上下文） | 2026-08-25
> 对象: 分支 `feat/slice-a-d517-d519`（tip `563807ed`，基线 `af8219e2`=PR#155，5 提交，27 文件）
> 口径: 切片 A 总览 §四——一次切片级审计覆盖三任务；复跑口径 = D517 构建链 + D519 mac-install-verify.sh
> 运行环境: macOS arm64（Mac 本机，与实现同机）；复跑时显式 `env -u ELECTRON_RUN_AS_NODE`（DSH 宿主默认置 1，实现方已预告此环境坑）

---

## 结论: **CONDITIONAL PASS**（切片级，覆盖 D517/D518/D519）

核心声称全部经 K3 独立物理复跑证实（全链构建 + 安装 + 四断言 exit 0）。两个条件项：
1. **C1（结构延期，非缺陷）**: 合并后 push main 触发 desktop-build CI，补 artifact URL 进 task-state/D517 DS3、D519 DS6——`on: push main` 决定本分支无法自证，dev doc 已显式 ⏸ 声明。
2. **C2（P1-1 返修）**: D518 dev doc 回填 prod 运行时契约变更（`node dist/src/index.js` → `dist/backend.mjs` + 包内 Electron node 模式），消除文档-实现漂移（M7）。

---

## 一、K3 独立物理复跑（最高权重证据）

| 复跑项 | 命令 | 结果 |
|---|---|---|
| D517 构建链 + D519 实测全链 | `env -u ELECTRON_RUN_AS_NODE bash scripts/desktop/mac-install-verify.sh`（实现工作区，无 --skip-build） | **exit 0**。build:backend(esbuild bundle) → renderer build → electron-builder --mac（x64+arm64 dmg+zip 四产物）→ 挂载 → cp /Applications → open → **四断言第 5s 全过**（进程/窗口/healthz/日志）→ 清理。新 evidence: `evidence/D519-mac-20260825-023653`，新 dmg md5=`dba4b4fb4a169cba44ff009a81a990f4` |
| 测试全组 | `npx vitest run tests/electron/`（独占运行 ×4） | **47/47 绿 ×4**（4 文件：desktop-build 23 + backend-spawn 10 + mac-install-verify 10 + auto-update 4） |
| F4 漂移 | `grep -n "dist/index.js" electron/*.cjs` | 零结果 ✅ |
| 接线 | `grep -n "ensureBackend(" electron/main.cjs` | main.cjs:128 whenReady 真实调用 ✅（声称 :127，行号微漂） |
| 写集红线 | `git diff --name-only af8219e2..HEAD \| grep "^src/\|scripts/audit/"` | 0 ✅ |
| 审计基线 | `python3 scripts/audit/audit-check.py --full`（切片 vs main 基线 worktree） | 两侧均 PASS:2 WARN:894 FAIL:460——**零新增** ✅ |

**环境注记（防 D316 型误判）**: ① 首次 vitest 与全量 electron-builder 构建并发跑出 1 失败（日志被截断未留名），独占资源后 4/4 全绿——判**环境依赖（资源争用）**，非恒失败。② 首次复跑脚本在 K3 默认沙箱下因越权写 dist/ 失败——脚本正确 fail-closed（exit 1 + 失败步 echo），提权后复跑通过；该次失败是沙箱问题，不计入交付缺陷。③ DSH 宿主 `ELECTRON_RUN_AS_NODE=1` 环境坑属实，复跑全程 `env -u`。

## 二、声称 ↔ 证据逐条对账（L1）

### D517（6 DS）

| DS | 声称 | K3 核验 | 判定 |
|---|---|---|---|
| DS1 | --dir 产物 >100MB（du=301） | `release/mac-arm64/SynovaAgent.app` 现盘 du=353MB（02:01 重建覆盖，>100 仍成立；301 为历史值不可复验） | ✅（数值为历史快照，见 P2-2） |
| DS2 | desktop-build 22 用例全绿 | 实测 **23** passed（20 it + 3 maybe 条件组，release/ 存在时全跑） | ✅（声称少报，见 P2-1） |
| DS3 | 本地 dmg+zip ×4；CI artifact ⏸ | 现盘 4 产物（dmg 124M/120M + zip 121M/116M，D518 变更后重建，原 md5 不可复验）；CI `on: push main` 本分支不触发 | ✅ 本地 / ⏸ CI（C1） |
| DS4 | 构建链契约头注释 + runbook | build-synova.cjs:L11-22 三步契约（D517+D518 修订版）+ docs/synova/runbooks/desktop-build.md 在库 | ✅ |
| DS5 | 27 文件全在写集 | 代码文件全部在三个 brief Q2 并集内；4 个流程文件（brief×3 + .claude/bypass.log）不在 Q2 | ✅（口径微疵，见 P2-4） |
| DS6 | task-state 回填 | impl_done + evidence 原文在库 | ✅ |

### D518（6 DS）

| DS | 声称 | K3 核验 | 判定 |
|---|---|---|---|
| DS1 | grep+运行双证 | main.cjs:128 调用 + :124 boot mode 日志；`/tmp/d518-electron-dev.log` 第 1 行 `[electron] boot mode=dev`、L8 起后端 pino 日志直通（stdio inherit 修复证据） | ✅ |
| DS2 | backend-spawn 10 用例 | 10 用例全绿；F4 回归（:239 lookbehind 零容忍）+ boot 日志回归（:251）在案 | ✅ |
| DS3 | F4 清零 | grep 零结果（本报告一节） | ✅ |
| DS4 | runbook 链路图 + dev 标注 | desktop-dev-prod.md 含 prod 链路图（双击→boot 日志→spawn→loadFile→首诊页）+「仅开发」标注 | ✅ |
| DS5 | prod healthz 200 | K3 复跑打包 app 实测：A3 `curl -sf localhost:18790/api/healthz` 通过（响应体业务级 degraded=首启未初始化，HTTP 200=服务健康——语义区分诚实） | ✅ |
| DS6 | 写集 + task-state | 在库（含 D333 自决补记） | ✅ |

**存量修复属实性抽查（交付报告 §二）**: ① renderer 双重全局声明冲突修复——diff 实证 api.ts 复用 bridge.ts ElectronAPI 单源、useConversation 收敛单参（零消费者声明与 `void sendMessage(text)` 一致）；② spawn stdio 死锁修复——backend-spawn.cjs:131 `logFile ? pipe : inherit` + 注释（64KB 缓冲机理）；③ prod 运行时修复——buildCommand prod=`process.execPath + dist/backend.mjs`（backend-spawn.cjs:69）、`ELECTRON_RUN_AS_NODE=1` 注入（:115）、build-backend.sh esbuild bundle 契约齐备、extraResources 原生模块映射（build-synova.cjs:57-65）。K3 全链复跑通过 = 三项修复的运行态终证。

### D519（7 DS）

| DS | 声称 | K3 核验 | 判定 |
|---|---|---|---|
| DS1 | 脚本 exit 0 四断言 | K3 独立复跑 exit 0（见一节）；原 evidence D519-mac-20260825-020611 十文件在盘，断言原文一致 | ✅ |
| DS2 | evidence + md5 | 原 dmg md5=`be55d9db…` 与磁盘实测一致 ✅；K3 复跑产新 md5=`dba4b4fb…`（重建产物，指纹漂移属正常） | ✅ |
| DS3 | 10 用例 | 10 用例全绿（含失败不静默/幂等/ELECTRON_RUN_AS_NODE 坑用例） | ✅ |
| DS4 | founder-demo 4 步 | founder-demo-mac.md 在库，测试锁定其引用脚本与四断言命令（D510 DS11 闭环） | ✅ |
| DS5 | 幂等 + dry-run | K3 复跑本身即二次运行（走清理路径）exit 0；--dry-run 分支在脚本 :100 与测试锁定 | ✅ |
| DS6 | Mac 实测 / Win=CI artifact ⏸ | Mac=K3 复跑证实；Win=⏸（C1） | ✅/⏸ |
| DS7 | 写集外零改动 + scripts/audit 零触碰 | 实证（见一节红线行） | ✅ |

## 三、14 项语义审计清单

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 接口真实性 | ✅ | ensureBackend/buildCommand/probeOnce 均实证；build:backend script=package.json:50 |
| 2 | 文件路径存在性 | ✅ | 27 文件逐一在库 |
| 3 | Edge ID | N/A | 无扩展边声明 |
| 4 | 架构边界 | ✅ | 构建配置/脚本零 import 层代码；探活走 HTTP /api/healthz（L1 同源） |
| 5 | 数据流完整性 | ✅ | 构建链三步→extraResources 映射→包内 spawn→healthz，逐环实证 |
| 6 | 接线深度 | ✅ | build-synova.cjs 被 package.json/electron/package.json 7 处 scripts 引用；mac-install-verify.sh 被 founder-demo runbook 引用 + K3 实跑 |
| 7 | 降级诚实性 | ✅ | 脚本 exit 0/1/2 契约 + die() echo 失败步 + fail.txt；backend degraded 显式；healthz 业务 degraded vs HTTP 200 语义区分诚实 |
| 8 | 测试契约 | ✅ | build-backend.sh/mac-install-verify.sh 头注释 @input/@output/@degraded 与实现一致 |
| 9 | 测试覆盖 | ✅ | 115 expect()；正常/降级（release 缺失 skip+warn）/边界（zip<10MB=空包红、幂等）三路径 |
| 10 | 文件驱动 | N/A | 无新类型 |
| 11 | 自我报告交叉对比 | ✅ 主干属实；4 处轻微口径差（P2-1/2/4 + 行号漂移） |
| 12 | dev doc 偏离 | ⚠️ **P1-1**（见下） |
| 13 | 控制塔执行 | ✅ | bypass.log 新增 5 行：D517 BLOCKED(17:53 pre-commit FAIL)→PASS(17:55) + D518×2 PASS + D519 PASS——门禁真实执行、拒后修复再提交链条完整；零 detected-bypass；commit-msg 合规 |
| 14 | 版本编排 | N/A | 非版本发布任务，VERSION.md 未触碰（正确） |

## 四、发现清单

### P1（建议修复——C2 条件项）

**P1-1 D518 dev doc 未回填 prod 运行时契约变更（M7 文档-实现漂移）｜归因: devdoc**
- dev doc `SYNOVA-IMPL-DSH-D518-single-entry-20260824.md` §2（:32）、§5.1（:55-58）、§7（:76）仍以 `node dist/src/index.js` 为 prod 契约，并规定测试应断言 "buildCommand('prod') === node dist/src/index.js（锁磁盘事实）"。
- 实现实际为 `dist/backend.mjs` + 包内 Electron node 模式（backend-spawn.cjs:69；测试 :235 断言 `prod.args === ['dist/backend.mjs']`）。**照 dev doc §7 写测试现在会红**。
- 缓解事实：brief Q2、task-state evidence、build-synova.cjs 头注释、runbook 均已同步新契约——漂移被约束在 dev doc 单文件内；且变更是 D333 自决 + 交付报告明示，非静默。
- 要求：dev doc 回填新契约（§2/§5.1/§7 三处），消除"最后一个过时副本"。

### P2（可选/观察项）

- **P2-1 用例计数少报**：声称 desktop-build 22 用例，实测 23（条件组 maybe×3 在 release/ 存在时计入）。少报方向安全，建议口径统一。｜归因: implement
- **P2-2 产物证据易失**：D517 DS1 du=301、DS3 md5（5a71c259/1933adcf）已被后续重建覆盖不可复验。D519 的"指纹随 evidence 落盘"做法（md5.txt）是正确的，建议 D517 同类产物指纹也落 evidence 而非仅 task-state。｜归因: devdoc（spec 证据规范粒度）
- **P2-3 并发环境敏感**：vitest 与全量构建并发出现 1 次失败（独占后 4/4 绿）——环境依赖，非交付缺陷，记录在案防 D316 型误判。｜归因: audit（环境注记义务）
- **P2-4 DS5 口径**：声称"27 文件全在声明写集"，其中 brief×3 + bypass.log 为流程产物不在 Q2；代码文件 23/23 全部在写集。建议 DS 口径写"代码文件全在写集 + 流程文件另计"。｜归因: implement

### 登记项核实（交付报告 §四）

- 孤儿 tag `V4.7.1`/`V5.0.2` **声称属实**（`git ls-remote --tags origin` 物理证据：V4.7.1→2b043c83/f685fa06、V5.0.2→43f00256/d0ba79d1）。M6 台账已登记类，待 CTO 裁决重指/删除。
- 分支基线 af8219e2（PR#155）落后 main（c149ec0f，PR#160）——合并前需 sync；写集重叠仅 `.claude/bypass.log`（append-only，冲突风险极低）。

## 五、跑偏第二道（北星三问）

对照 `.claude/PRODUCT-BRIEF.md` 原文：
1. **服务真实用户场景？** ✅ §二 L20「直接用户 = FDE……缺系统诊断工具」——FDE 不会命令行，"双击装→双击开→首诊页"是 FDE 能用的前提；三 dev doc 北星 front-matter 齐备且锚定 §二/§六。
2. **接近终态？** ✅ 桌面端 8 验证点 0→3（1-1/1-5/1-3），切片方法论既定路线，无横向无关项（descope 明确：签名/公证/Win 实测/单实例锁均延后）。
3. **产品变味？** ✅ 安装包只是分发形态，产品仍是"驻扎企业、主动诊断、为增长服务"的 Agent；healthz 检查项（数据库/哨兵/看门狗）证明诊断内核在包内真实运行。

**裁决：对齐北星。**

## 六、L4 防线缺口收割

> 本次实现期撞破的 3 个存量阻塞，每问"本该拦住它的防线是什么？"

| # | 问题 | 本该拦住的防线 | 为什么没拦住 | 缺口处置 |
|---|---|---|---|---|
| G1 | renderer 构建在 main 即红（TS2339×6+TS2554），直到 D517 物理打包才撞破 | PostToolUse verify（tsc）或 CI | 根 tsc 不覆盖 electron-renderer 子包（独立 tsconfig+vite）；ci.yml 无该构建步骤——**子包构建全程无门禁** | 命中既有 **M1**（fail-open：检查未执行==检查通过）。desktop-build.yml（本切片交付）合并后该缺口自闭；不新设机制 |
| G2 | spawn stdio 死锁（pipe 无读者 64KB 写满） | 单元测试/契约测试 | 契约测试 mock 了进程真实 IO，永填不满 64KB 缓冲——只有真实运行能暴露 | 命中既有 **M2/M3** 交集（契约绿≠运行通）。切片 A 红线 1（物理验证优先）+ mac-install-verify.sh 已建成新防线；不新设 |
| G3 | prod 后端从未物理可跑（ESM 无扩展名/asar/ABI 三重阻塞），D504 已审 CP 却无人发现 | K3 审计口径（D504 轮） | 当时审计接受"契约测试=接线完成"，未要求"打包产物真实启动"证据 | **归因: audit**——审计口径缺口，已由切片方法论（红线 1：禁静态 grep 冒充实测）修复，本切片即修复后的首次实践。机制已存在，不新设 |

原则核对：G1-G3 全部命中既有 M 类（M1/M2/M3），**无需新设 M9**；无"每犯一错加一脚本"。

## 七、状态机写回

- D517/D518/D519 → `audited`，verdict = **CONDITIONAL PASS**（条件 C1/C2 见结论节），本报告路径已写入各 task-state `audit` 段。
- C1 完成后（CI artifact URL 回填）+ C2 完成后（dev doc 回填）→ 可转 PASS，无需复审全量，K3 只核两项证据。

---

**审计物理证据索引**: 复跑日志 `/tmp/k3-d519-rerun.log`；新 evidence `synova-wt-sliceA-impl/evidence/D519-mac-20260825-023653/`（10 文件）；vitest 日志 `/tmp/k3-vitest-2.log`、`/tmp/k3-vitest-r{1,2,3}.log`；原 evidence `evidence/D519-mac-20260825-020611/`；运行日志 `/tmp/d518-electron-dev.log`。
