# K3 独立审计报告 — 切片 B（D522 服务自启 teardown / D523 Win 双击出窗验证基建）

> 审计员: Kimi K3（独立工作区，零上下文） | 2026-08-25
> 对象: 分支 `feat/d522-service-boot-teardown`（tip `443d9ebb`，3 提交）与 `feat/d523-win-double-click`（tip `fa0d3496`，4 提交，叠于 D522 之上）
> 基线: `5d6f487d`（切片 B 派单点，**早于切片 A 合入 main**——见 P0-1）
> 口径: 切片 B 一次审计覆盖两任务；复跑 = D522 teardown 测试 + D523 静态契约测试 + 全 electron 套件 + red 态复现
> 运行环境: macOS arm64（本机与实现同机）；**无 pwsh** → Win 侧物理实跑本机不可行（D523 DS1/DS2 ⏸ waiting 与其自身声明一致）

---

## 结论: **CONDITIONAL PASS**

交付声称（D522 DS1-DS7 + D523 DS1-DS7 + 自检 5 问 + 如实披露 3 项）**逐项独立物理复跑证实**，无一项不实。三项转 PASS 条件：

1. **C1（P0-1 阻断合并，必须先修）**: 两分支基点陈旧——落后 `origin/main` **34 提交**，基于切片 A 合入前的 `5d6f487d`。分支 `electron/backend-spawn.cjs` 缺失 D518 三项 prod 运行时修复（`buildCommand('prod')=node dist/src/index.js` 应为 `process.execPath + dist/backend.mjs`；缺 `ELECTRON_RUN_AS_NODE='1'` 注入；缺无 logFile 时 `stdio inherit` 回退），且测试 L233 仍断言旧 prod 命令——**直接合并将回归 D518 修复或测试失败**。须 rebase 至 `origin/main`、冲突保留 main 侧 D518 修复 + 分支侧 teardown 增强、更新 prod 命令断言、全量复跑。
2. **C2（P1-1）**: `win-install-verify.ps1` 前置缺失的 `exit 2` 语义在 `$ErrorActionPreference='Stop'`（L19）下**不可达**——`Write-Error`（L33）抛终止错误，`exit 2` 永远执行不到，实际退出码为 1。静态测试只查字符串不查控制流，漏网。修法：前置缺失分支改用 `Write-Host` + `exit 2`（或该分支内 `$ErrorActionPreference='Continue'`）。
3. **C3（P1-2）**: task-state D522/D523 `spec.branch` 声称 `docs/slice-b-specs`——该分支**不存在**（ls-remote 实测）；spec 提交 `5110fb21` 落在 `feat/d522-service-boot-teardown` 内。字段修正为实际位置。

---

## 一、K3 独立物理复跑（最高权重证据）

| 复跑项 | 命令 | 结果 |
|---|---|---|
| D522 测试 | `npx vitest run tests/electron/backend-spawn.test.ts`（分支工作树 .wt-slice-b @fa0d3496） | **15/15 绿**（13.91s）——与声明 "Tests 15 passed (15)" 一致 |
| D523 静态契约 | `npx vitest run tests/electron/win-install-verify.test.ts` | **7/7 绿**（126ms）——与声明一致 |
| 全 electron 套件 | `npx vitest run tests/electron/` | **39/39 绿**（4 文件：backend-spawn 15 + win-install-verify 7 + desktop-build 13 + auto-update 4）——与声明 "39/39" 一致 |
| **red 态复现** | 新测试文件 × 旧实现（`git show 0da174e0:electron/backend-spawn.cjs` 覆盖后跑） | **4 failed \| 11 passed (15)**——与声明 "改造前 4 failed \| 11 passed" **完全一致**；失败 4 例=预期 4：SIGKILL 升级 / 进程组孙进程 / taskkill 注入 / pid≤0 边界 |
| WIRE | `grep ensureBackend electron/main.cjs` | L19 require / L126 调用 / L189 `backendHandle.stop()`——声明命中；**main.cjs 零在写集**（分支 diff 对账 0） |
| 类型安全 | `git grep "as any"` 分支改动文件 | **零**（铁律 38） |
| DSH 依赖 | `git grep "@deepseek-ai/dsh"` electron/ + package.json | **零**（R1 红线） |
| 架构边界 | `bash scripts/check-architecture.sh` / `check-bridge-files.sh` | 全过 ✅（L1 纯 Node 模块 + 纯验证脚本，零跨层） |
| lint 预存 | `npm run lint` | `src/server.ts` **恰 2 个 TS 错误**（L395 MainAgent/MainAgentLike、L396 unknown/GraphBridgeLike）；src/ 零在写集 → **预存成立**（声明属实） |
| 审计基线 | `python3 scripts/audit/audit-check.py --full` | PASS:2 WARN:894 FAIL:460——**与切片 A 审计基线完全一致，零新增** |
| Secrets | 分支 diff 全量模式扫描 | 零命中（pre-push 终扫同类） |
| 前置产物 | `ls release/` | 不存在（.gitignore L67 `release/`）；分支内零跟踪——"release/*.exe 不存在"属实，D523 ⏸ waiting 诚实 |

**环境注记（防 D316 型误判）**: ① 本审计机无 pwsh/powershell——Win 侧物理实跑（D523 DS1/DS2）不可行，waiting 判定与实现方一致，非本机可证伪项。② ps1 `exit 2` 语义问题（P1-1）为 PowerShell 文档行为（Write-Error + EAP=Stop → 终止错误）的静态分析结论，Win 本机实跑时须复验。③ D522 进程组用例用 `sleep 60`（macOS 存在）——POSIX 路径实测有效。

## 二、声称 ↔ 证据逐条对账（L1）

### D522（DS1-DS7）

| DS | 声称 | K3 核验 | 判定 |
|---|---|---|---|
| DS1 | 测试 15/15 全绿；red 4 failed \| 11 passed | 15/15 复跑绿；red 态用旧实现物理复现 4/11，失败例名逐一吻合 | ✅ |
| DS2 | stop() 后 pid 已死（assertDead = kill(pid,0) 抛 ESRCH） | 测试 L281-285 `assertDead` 定义 + L320-321 使用；绿跑通过 | ✅ |
| DS3 | 孙进程无孤儿（进程组用例 kill(gcPid,0) 先证活 → stop 后 waitDead=true） | 测试 L380-420：L412 `process.kill(gcPid,0)` 前置存活 + L418 `waitDead(gcPid)` 后死 | ✅ |
| DS4 | SIGTERM→SIGKILL 升级（忽略 SIGTERM 子进程，graceMs:500 注入被 SIGKILL） | 测试 L352-378 + 实现 backend-spawn.cjs L110-119 `makeStop`（graceMs 默认 5000 可注入，L155） | ✅ |
| DS5 | WIRE 零改 main.cjs（L19/L126/L189） | 实测命中；分支写集零 main.cjs | ✅ |
| DS6 | 写集外零改动；@deepseek-ai/dsh 零；as any 零 | 实质成立（src/、main.cjs、build-synova.cjs、scripts/audit/、probeOnce/probeUntil 零触碰）；**枚举不全**（实际 8 文件含 2 dev doc + 2 task-state + bypass.log——见 P2-1） | ✅（P2-1） |
| DS7 | task-state/D522.json 回填 impl + evidence | 文件在分支，impl/evidence 原文在库（L12-27） | ✅ |

### D523（DS1-DS7）

| DS | 状态 | 声称 | K3 核验 | 判定 |
|---|---|---|---|---|
| DS1 | ⏸ | Win 本机 powershell 实跑 exit 0（前置 release/*.exe 不存在） | release/ 不存在 + .gitignore 排除——waiting 条件真实；本机无 pwsh 不可复跑，与声明一致 | ✅/⏸ |
| DS2 | ⏸ | evidence 五类文件（等 DS1） | 同上，诚实 waiting | ✅/⏸ |
| DS3 | ✅ | 无 .exe → exit 2 + waiting 提示；task-state 记 impl_done_waiting_exe | ps1 L31-35 有 exit 2 + waiting 文案；task-state status=`impl_done_waiting_exe` 如实；**但 exit 2 运行态不可达（P1-1）** | ⚠️ |
| DS4 | ✅ | founder-demo-win.md 4 步各含命令+预期+证据落点 | 4 步齐全，每步 命令/预期/证据落点 三列 + task-state.evidence 字段名映射（exe_md5/process/window/healthz/backend_log） | ✅ |
| DS5 | ✅ | 静态契约测试 7/7（有效代码零 taskkill，剥离 # 注释） | 7/7 复跑绿；测试 L58-63 确为剥离注释后检测 | ✅ |
| DS6 | ✅ | 承诺口径：进程+窗口+healthz 200+后端日志四断言与 Mac D519 同构 | ps1 L66-71 四断言（Get-Process/MainWindowTitle/Invoke-WebRequest/backend.log 非空）；D519 同构 | ✅ |
| DS7 | ✅ | 写集 = 3 新建 + task-state + brief；electron/src/build-synova.cjs/scripts/audit/ 零触碰 | fa0d3496 恰 5 文件（ps1 + founder-demo-win.md + 测试 + task-state + brief），零触碰成立 | ✅ |

### 自检 5 问与如实披露（3 项）

| 自检项 | 核验 |
|---|---|
| 1 接线 | 新 export（signalTree/taskkillProcessTree/makeStop）由测试直测（同 buildCommand/probeOnce 既有模式）；stop() 在 main.cjs L189 既有调用链，零改动——成立 |
| 2 异常处理 | taskkill 失败 → console.warn + 继续（backend-spawn.cjs L77-81）；POSIX 组信号失败回退 child.kill 再吞 already-gone（L97-101）；PS1 失败路径 Write-Error + failed.txt（L55-56/L86-88）——成立（铁律 24/31） |
| 3 类型安全 | as any 零（grep 实证）——成立 |
| 4 测试质量 | D522 新增 7 用例覆盖正常/降级/边界，全部 expect() 物理断言（assertDead/waitDead 基于 kill(pid,0)）——成立（铁律 48） |
| 5 残留清理 | 无死代码；探活逻辑零改动（红线②）——成立 |
| 披露 1: lint 2 预存 TS 错误 | 恰 2 个（src/server.ts L395/L396），src/ 写集外 → 预存成立；建议 CT 队列跟进 |
| 披露 2: bypass.log 补记 chore 用 --no-verify | 443d9ebb 实为 bypass.log 补记提交（9d394ad7 + 5110fb21 两条，D331 对账格式），gatekeeper exit 0 有据（task-state L10）；--no-verify 流程瑕疵已如实报备——**记账行为本身正确，流程瑕疵属实** |
| 披露 3: 本地删孤儿 tag V4.7.1 | 本工作区无法证伪开发机本地删除；**但"非任何分支祖先"定性与当前状态不符**——远端 tag 现指向 dc369fdb（main 祖先，非孤儿；切片 A 审计时曾见 f685fa0 孤儿态，其间已被重指）。动作本身无害（远端未动 ✓）。见 P2-3 |

## 三、14 项语义审计清单

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 接口真实性 | ✅ | ensureBackend/buildCommand/probeOnce/signalTree/taskkillProcessTree/makeStop 六符号全部在库（backend-spawn.cjs L219） |
| 2 | 文件路径存在性 | ✅ | 分支写集 12 文件逐一存在（含 ps1/founder-demo-win.md/两份 dev doc/brief×2） |
| 3 | Edge ID 有效性 | N/A | 无扩展边声明 |
| 4 | 架构边界 | ✅ | backend-spawn.cjs 纯 Node 零 require('electron')，零跨层 import；ps1 只调 PowerShell cmdlet |
| 5 | 数据流完整性 | ✅ | 探活→spawn→健康→stop 五段链路集成测试（test L447-476）真实走通（假后端自带 healthz） |
| 6 | 接线深度 | ✅ | main.cjs L19/L126/L189 真实调用；founder-demo-win.md 4 步引用 ps1 步骤；切片 B 验证点判据=脚本 exit code |
| 7 | 降级诚实性 | ✅ | backend degraded 显式（L209-211）；ps1 failed.txt + Write-Error 不静默；waiting 不伪造 |
| 8 | 测试契约 | ✅ | dev doc §7 契约（stop 幂等/升级/进程组/taskkill 降级）与实现、测试一一对应；ps1 头注释 @input/@exit 语义与实现一致（**exit 2 除外，P1-1**） |
| 9 | 测试覆盖 | ✅ | D522 7 用例正常/降级/边界全覆盖；D523 7 用例契约静态覆盖；非空壳（全部 expect） |
| 10 | 文件驱动 | N/A | 无新类型/manifest |
| 11 | 自我报告交叉对比 | ✅ 主干属实 | 两处口径差（P2-1 写集枚举、P2-2 "11 例"措辞）非实质 |
| 12 | dev doc 偏离 | ✅ | 两份 dev doc 写集表 vs 实际提交：D522 写集表（2 修改+0 新建）未含 dev doc 自身与 task-state（惯例），实质零偏离；D523 写集表 3 新建准确 |
| 13 | 控制塔执行 | ✅ | bypass.log 补记 2 条（9d394ad7 pre-commit PASS + 5110fb21 spec 补记）格式与 D331 对账一致；gatekeeper exit 0 有据；push 已过 pre-push（secrets 终扫复跑零命中） |
| 14 | 版本编排 | N/A | 非版本发布任务 |

## 四、发现清单

### P0（阻断项——C1 条件）

**P0-1 分支基点陈旧：落后 main 34 提交，backend-spawn.cjs 缺失 D518 prod 运行时修复，直接合并将回归｜归因: implement（分支同步纪律，铁律 0-3）**
- 实测：`git rev-list --count origin/feat/d52{2,3}..origin/main` = **34**；merge-base = `5d6f487d`（切片 B 派单点，早于切片 A 合入）。
- `git diff origin/main origin/feat/d523 -- electron/backend-spawn.cjs`：分支版 `buildCommand('prod') = { bin: 'node', args: ['dist/src/index.js'] }`（L62），**缺** main 侧 D518 三项修复——① prod=包内 Electron node 模式跑 esbuild bundle（main L69 `process.execPath + dist/backend.mjs`，注释列三重阻塞：ESM 无扩展名 import / asar 内 node_modules 不可达 / 原生模块 ABI 不匹配）；② `env.ELECTRON_RUN_AS_NODE='1'`（main L115）；③ 无 logFile 时 `stdio inherit` 回退（main L131，64KB pipe 缓冲死锁修复）。
- 测试 backend-spawn.test.ts L233 断言 `prod.args === ['dist/src/index.js']`——**与 main 当前实现直接矛盾，rebase 后必红**。
- 合并冲突点：spawn 调用（main 改 stdio / 分支加 detached，两方同区）；buildCommand（main 改函数体 / 分支改 JSDoc）；backend-spawn.test.ts（main 版 234 行差异，含 D518 F4 lookbehind + boot-log 回归用例）。
- 要求：rebase 至 `origin/main` → 冲突解决保留 main 侧 D518 修复 + 分支侧 teardown → 更新 buildCommand('prod') 断言（`['dist/backend.mjs']`）→ 全量 vitest + 两条回归红线复跑 → 重新 push。**未完成前 PR 不得合并。**

### P1（建议修复——C2/C3 条件）

**P1-1 ps1 前置缺失 exit 2 语义不可达（静态分析，Win 待验）｜归因: implement**
- ps1 L19 `$ErrorActionPreference='Stop'` + L33 `Write-Error "waiting: ..."` → Write-Error 抛终止错误，`exit 2`（L35）执行不到 → 实际退出码 **1**（≠ 声明/契约的 2）。
- 同类：L56/L86 Write-Error 后 `exit 1` 也是死代码（但退出码碰巧同为 1，行为无差异）。
- 静态测试 L46-56 只断言字符串 `exit 2` 存在，不验证控制流 → 漏网。修复后须在 Win 本机实跑前置缺失路径复验 `$LASTEXITCODE=2`。

**P1-2 task-state spec.branch 字段不实｜归因: implement**
- task-state/D522.json L8、D523.json L8 声称 `"branch": "docs/slice-b-specs"`——该分支不存在（`git ls-remote` 实测零结果）。spec 提交 `5110fb21` 实际落在 `feat/d522-service-boot-teardown`。修正字段或补建 docs 分支。

### P2（可选/观察项）

- **P2-1 DS6 写集枚举不全**: 交付声明 DS6 "git diff --name-only = backend-spawn.cjs + test + brief + task-state"——实际分支写集 8 文件（另含 2 份 dev doc + D523 task-state + bypass.log）。实质（写集外零改动）成立，口径建议改"代码文件全在写集 + 流程工件另计"（同切片 A P2-4）。
- **P2-2 "39/39（含 D504 既有 11 例无回归）"措辞**: "11 例"实为 red 态通过数（8 例 D504 既有 + 3 例新用例在旧实现下天然通过），非"既有 11 例"。39/39 复跑属实。
- **P2-3 V4.7.1 "孤儿"定性与当前状态不符**: 远端 tag `25a9f5cc → dc369fdb`（`merge-base --is-ancestor dc369fdb origin/main` = **YES**，main 祖先非孤儿；切片 A 审计时曾见 f685fa0 孤儿态 → 其间已重指）。开发机删除本地 tag 动作无害（远端未动 ✓，与声明一致）；建议 CT 队列按 ffc50298 裁决核远端 tag 处置。
- **P2-4 D523 分支叠于 D522**: fa0d3496 的 parent = 443d9ebb（D522 tip）。合并顺序必须 **D522 先于 D523**，否则 D523 PR 的 Files changed 含 D522 全部提交。
- **P2-5 D522 stop() SIGKILL 升级定时器 unref**: 病理场景（子进程忽略 SIGTERM 且 Electron 在 graceMs 内退出）下 unref 定时器不阻塞退出，SIGKILL 可能不触发。与 DSH 宿主退出直 SIGKILL 的处理不同。正常后端行为下无影响（SIGTERM 即退），记录观察。

## 五、跑偏第二道（北星三问）

1. **服务真实用户场景？** ✅ FDE 零命令行——"双击→后端自启→开窗即用→退出无孤儿"正是 FDE 可用前提；D522 补的正是退出回收这半边（D504 遗留 F2）。
2. **接近终态？** ✅ 桌面端验证点 1-4（自启）teardown 补全 + 1-2（Win 出窗）验证基建落地；D523 ⏸ 部分属前置依赖（切片 A .exe），诚实 waiting 而非伪造，符合切片方法论。
3. **产品变味？** ✅ 纯 L1 桌面基建（进程回收/验证脚本），零产品逻辑变更；src/ 零触碰（首诊后端生产可用不受影响）。

**裁决：对齐北星。**

## 六、L4 防线缺口收割

| # | 问题 | 本该拦住的防线 | 为什么没拦住 | 缺口处置 |
|---|---|---|---|---|
| G1 | 分支基点落后 main 34 提交（含同文件 D518 修复），交付声明零披露 | 铁律 0-3 开工前 `git pull --ff-only` / pre-push 门禁 0-1（behind 阻断） | 门禁 0-1 针对**共享分支**的上游 tracking ref；新分支无 upstream → behind 检查不适用；PR 阶段"merge-base 新鲜度"检查缺位 | 建议控制塔：PR 创建/合并前门禁增加 **merge-base 与 main 的距离检查**（基点早于 main 最近 N 提交或落后超阈值 → 阻断并提示 rebase）。归因: control-tower（新机制候选，等 CT 健康审计批次） |
| G2 | ps1 静态契约测试漏掉 exit 2 控制流语义（Write-Error+EAP=Stop） | 测试契约（铁律 48 边界路径） | 静态断言只查字符串存在，不查"分支是否可达/退出码是否如声明"——字符串存在 ≠ 行为正确 | 建议：ps1 类脚本的静态测试升级为 AST/parse 级或行为注入级断言；或 D523 类验证脚本先过 pwsh 语法解析（无 pwsh 时至少 `-NoProfile -Command parse`）。归因: implement + 测试规范 |

原则核对：G1/G2 均非"每犯一错加一脚本"——G1 建议并入既有 PR 门禁体系，G2 建议提升测试断言粒度；不新设独立脚本。

## 七、状态机写回

- D522 → `audited`，verdict = **CONDITIONAL PASS**（C1 rebase + 更新 prod 断言；C3 spec.branch 修正）。impl/evidence 在 `feat/d522-service-boot-teardown`，合并后 task-state 以分支版为准。
- D523 → `audited`，verdict = **CONDITIONAL PASS**（C2 ps1 exit 2 语义修复 + Win 实跑复验；C3 同上）。impl 状态 `impl_done_waiting_exe` 维持至切片 A .exe 落地后 Win 本机实跑。
- 本报告路径已写入 task-state audit 段（main 占位版）。
- C1/C2/C3 完成后可转 PASS，K3 只核三项证据，无需复审全量。

---

**审计物理证据索引**: 复跑 vitest 日志（backend-spawn 15/15 @13.91s；win-install-verify 7/7 @126ms；全套件 39/39）；red 态复现（4 failed \| 11 passed，失败例名 4 项）；`git diff origin/main origin/feat/d523 -- electron/backend-spawn.cjs`（D518 修复缺失实证）；`git rev-list --count origin/feat/d52x..origin/main` = 34；`git ls-remote`（docs/slice-b-specs 不存在；V4.7.1=25a9f5cc）；audit-check.py 基线 PASS:2 WARN:894 FAIL:460（零新增）；lint src/server.ts 2 错误（L395/L396）。

---

# 复审（2026-08-25 二次，P0-1 修复后）

> 对象: `feat/d522-service-boot-teardown` tip `323df2c7`（CTO 修复在 `8b8ad79c`）/ `feat/d523-win-double-click` tip `df2502eb`（= CTO 修复）。声明"tip 后均有 1 个 CTO 修复 commit"——d522 侧 tip 实为 bypass 登记 commit（`323df2c7`），CTO 修复在其下（P2-7）。
> 口径: 原三项条件（C1 P0-1 双侧保留 / C2 ps1 exit 2 / C3 task-state spec.branch）+ 树终验（重点，原声称在此失守）。

## 复审结论: **PASS**

C1/C2 物理闭合；C3 部分闭合降级 P2（非阻断）。合并顺序按 K3 P2-3: **D522 → D523 → 审计分支**。合并后 task-state 以分支版为准（audit 段 verdict 已更新为 PASS）。

## 复核证据（全部独立复跑）

| 复核项 | 命令/方法 | 结果 |
|---|---|---|
| 树终验（重点） | `git diff --name-status origin/main...<branch>` | **D522: 4A+5M / D523: 7A+5M，删除数 = 0**；`a.txt/b.txt/f.txt` 零存在 ✅ |
| task-briefs 一致性 | `git ls-tree -r origin/main` vs 分支 | 分支 498 = main 497 + **T1.md**（分支独有）；声明"478/479"数字过时（后续 main 又合入 19 个），**结构关系（main+1）成立**（P2-8） |
| grep 清零 | `git grep "dist/src/index.js"` 分支 electron/ tests/electron/ | **零结果** ✅ |
| 双侧保留 | backend-spawn.cjs 逐行 | main 侧 D518: `buildCommand('prod')=process.execPath+['dist/backend.mjs']` L70 + `ELECTRON_RUN_AS_NODE='1'` L172 + `stdio logFile?pipe:inherit` L188；分支侧 D522: signalTree/taskkillProcessTree/makeStop/SIGKILL 升级/幂等/detached 全在 ✅ |
| 断言更新 | test L234-235 | `expect(prod.bin).toBe(process.execPath); expect(prod.args).toEqual(['dist/backend.mjs'])` ✅ |
| 全量复跑（独占） | `vitest run tests/electron/` | **5 文件 59 passed \| 3 skipped (62)**——与声明一致；backend-spawn **17/17**；win-install **8/8**（含 P1-1 控制流回归用例）✅ |
| P1-1 ps1 | 逐行 + 测试 | 三处 Write-Error → Write-Host（L35/L58/L88）+ 显式 exit 2/1/0 + 注释说明；测试 L79-84 新增"零 Write-Error + exit 2 可达"回归断言 ✅（静态级；Win 实跑待验，task-state P11_ps1_exit2 诚实记录） |
| P1-2 task-state | D522/D523.json | p01_fix 段（merge 双侧保留 + 断言更新 + verify）+ audit 段（fix_dispatch/fix_status/report/audit_branch）✅；**但 `spec.branch` 字段仍 `docs/slice-b-specs`（该分支不存在）——未修，降级 P2-8** |
| CTO 修复实证 | `git show 8b8ad79c --stat` | 500 文件（恢复 task-briefs 478+ 个 = main 时点内容）+ 删 a/b/f.txt 3 文件——与披露一致 ✅ |
| 审计基线 | `audit-check.py --full` | PASS:2 WARN:894 FAIL:460——与 main 一致、零新增 ✅ |

## 复审新发现

- **P1-3 bypass.log 断链（非阻断，建议补记）**: CTO 修复 commit `8b8ad79c`/`df2502eb` 用 `--no-verify`（创始人披露，detached worktree 恢复操作），但 `.claude/bypass.log` **无这两条补记**（D331 对账机制未履行；同分支其他 commit 均有 "hook 层登记" 记录）。内容 = main 原始文件恢复，风险低；建议按 D331 补记两条。
- **P2-6 T1.md 测试模板残留**: 分支新增 `.claude/task-briefs/2026-08-25-T1.md`，内容为测试模板占位（Q2"改 src/foo.ts"假数据）——非 D522/D523 产物，建议合并前删除（删除不影响树终验零删除判定，因其为分支新增文件）。
- **P2-7 声明 tip 微差**: d522 tip `323df2c7` 是 bypass 登记 commit，非声明所指 CTO 修复（`8b8ad79c`）——实质无影响。
- **P2-8 计数/字段**: task-briefs 计数过时（478/479 vs 实测 497/498）；C3 的 spec.branch 字段未修，建议与 P2-6 一并处理。
- **P2-9 vitest 退出警告**: 复跑后 "something prevents Vite server from exiting"——非测试失败（59/62 绿），疑为 mac-install-verify 或 auto-update 的 server 句柄未关，观察。

## 复审状态机

- D522/D523 → **PASS**（C1/C2 闭合；C3 降级 P2 非阻断）。task-state 分支版 audit.fix_status → "K3 复审 PASS 2026-08-25"。
- 合并顺序: **D522 → D523 → 审计分支**（audit/k3-20260825-d522-d523）。合并后如有冲突（task-state/bypass.log append-only），以分支版为准。
- 非阻断跟进 3 项（P1-3 补记 / P2-6 T1.md 清理 / P2-8 spec.branch 修正）可在合并后 chore 处理，不阻塞本轮合并。
