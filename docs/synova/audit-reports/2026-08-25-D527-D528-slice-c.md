# K3 审计报告 — 切片 C（D527 首诊旅程端到端 + D528 升级/重装不丢数据）

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-08-25
> 审计对象: `feat/slice-c-d527-d528` @ `ae9e9044`（基点 `main@90c232f8`，切片 A/B 合入后）
> 结论: **PASS**（2 项 P2 非阻断跟进，见 §发现）

---

## 0. 材料清单（7 项自收集）

| # | 材料 | 证据 |
|---|------|------|
| 1 | 任务提交集 | `git log --all -- feat/slice-c-d527-d528`：5335711d(feat D527) + 11784bb2(feat D528) + 1ebd1fbd/ae9e9044（post-commit hook 自动 bypass 登记） |
| 2 | Git diff | `git diff 90c232f8..ae9e9044 --stat`：22 文件；`git show` 逐文件审 |
| 3 | Dev doc | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D527-first-diagnosis-e2e-20260825.md`（277 行）+ `SYNOVA-IMPL-DSH-D528-upgrade-data-retention-20260825.md`（215 行） |
| 4 | Task brief | `.claude/task-briefs/2026-08-25-D527-D528-slice-c.md`（6 字段 + Q1c 决策参考系） |
| 5 | AGENTS.md | 铁律判案依据（V4.5.1 13 组 pre-commit） |
| 6 | 审计基线 | 无 scripts/audit 基线变化需求——本分支零触碰 scripts/audit/（diff 物理证明） |
| 7 | 执行证据 | `.claude/bypass.log`（两笔 feat commit 均 `COMMITTED | pre-commit PASS (hook 层登记)`，零 detected-bypass）；编码会话 evidence `GS-01-2026-08-25.json`（17:52 生成） |

## 1. 结论摘要

- **写集对账**：90c232f8..ae9e9044 全量 22 文件 = D527 写集 12 + D528 写集 4 + dev doc×2 + task brief×1 + task-state×2 + bypass.log（hook 自动，1 行）。**src/ 与 scripts/audit/ 零触碰**（grep diff 物理证明）。`@deepseek-ai/dsh` 在 electron-renderer/ 零引用（grep 物理证明）。build-synova.cjs **零改动**（D528 条件性路径未触发，符合 spec §5.1 预设）。
- **测试**：21/21 新用例复跑绿（use-streaming-contract 12 + upgrade-data-verify 9）；tests/electron 全量回归 7 文件 **80 passed / 3 skipped**（与声称一致）。
- **脚本语义**：first-diagnosis-timing.sh `--dry-run` exit 0 / `--mode prod` 缺 `--installer` exit 2；upgrade-data-verify.sh `--dry-run` exit 0 / 缺 dmg exit 2 —— 全部复跑复现。
- **接线**：useStreaming.ts:20 import / :81 applySSEEvent 调用 / :88 setCurrentReportId(r.reportId)；RightPanel.tsx 12 处 currentReportId 消费 + :189 `GET /consult/:id/report?format=markdown`；CenterPanel.tsx:73 进度条 / :81 错误条；main.cjs:126 requestSingleInstanceLock —— 行号逐一核对一致。
- **执行链**：两笔 feat 提交 pre-commit PASS 登记在位，零 `--no-verify`（bypass.log 无 detected-bypass 行）。
- **诚实 ⏸**：DS2/DS3 运行态截图、DS4 LLM 全链路、DS2(D528) dmg 升级实测——task-state 中全部显式标注 ⏸ + 原因（无 LLM key / release/ 无 dmg），未伪造。

## 2. 14 项语义审计清单

| # | 项 | 判定 | 证据 |
|---|----|------|------|
| 1 | 接口真实性 | ✅ | `applySSEEvent`/`SSEEventType`/`SSEContractState` 在 sse-contract.ts 真实定义；`requestSingleInstanceLock` 在 main.cjs:126 真实调用；`upgrade-data-verify.sh`/`first-diagnosis-timing.sh` 存在且契约头完整 |
| 2 | 文件路径存在性 | ✅ | 写集 22 文件 `git show ae9e9044:<path>` 全部可读；worktree `.wt-slice-c`（编码克隆）HEAD=ae9e9044 一致 |
| 3 | 事件类型全集 | ✅ | engine 实测发射 = phase_started×6/phase_completed×6/report_ready/right_column_update/root_cause_identified/expert_hypothesis/hypothesis_generated/degraded/error（synova-diagnosis-engine-impl.ts）+ diagnosis.ts 补发 community_reports/entity_resolution/judgment_card/complete —— 15 类型契约覆盖全部 + 未知类型 console.warn 不静默（default 分支） |
| 4 | 架构边界 | ✅ | 零 src/ 改动；sse-contract 纯函数零 react/零 zustand/零跨层 import；useStreaming 走同层 HTTP；main.cjs 主进程基建；铁律 39 无违规 |
| 5 | 数据流完整性 | ✅ | engine 事件 → routes/diagnosis.ts onEvent→sseWrite → renderer fetch → applySSEEvent → setPhaseProgress（进度）/ setCurrentReportId（reportId）→ RightPanel 报告 tab GET /report → ReactMarkdown onePager。六阶段 0→5 与 complete/report_ready 均有消费路径 |
| 6 | 接线深度 | ✅ | grep 物理证明：applySSEEvent 调用点 useStreaming.ts:81；setCurrentReportId 调用点 :88；setPhaseProgress 定义 conversation-store:43 + 消费 CenterPanel；errorMessage 渲染 CenterPanel:81；单实例锁执行点 main.cjs:126-140。零"只 import 不调用" |
| 7 | 降级诚实性 | ✅ | JSON.parse 失败 catch → console.warn（useStreaming.ts:250-255，铁律 24）；报告 404 → `报告不可用（HTTP …；服务重启后内存缓存已清，请重新诊断）`（RightPanel.tsx:195-198，不静默）；degraded 事件 → 系统消息 + state.degraded=true；lock 异常 → try/catch + console.warn 不阻断首实例（main.cjs:124-129）；两脚本前置缺失 exit 2 显式提示 |
| 8 | 测试契约 | ✅ | sse-contract JSDoc @input/@output/@degraded 与实现一致（phase_completed 不回落/缺字段 warn 不抛均实现）；upgrade 脚本契约头 @input/@output/@degraded/@幂等 与 9 用例断言逐一对应 |
| 9 | 测试覆盖 | ✅ | 21 新用例全部 expect 断言非空壳：正常（六阶段推进/reportId 提取/脚本契约 8 项）+ 降级（error/degraded/缺字段/脚本 exit 2）+ 边界（乱序不回落/未知类型 warn/无 cp data/synova.db ⑥）；复跑 21/21 绿；回归 80/3 与声称一致 |
| 10 | 文件驱动 | ✅ | 无新增硬编码类型/扩展点；GS-01 expect.json 断言条目 7→8（+consult-llm-recorded）结构合规 |
| 11 | 自我报告交叉对比 | ✅ | 声称逐项物理核验：行号（useStreaming 20/81/88、CenterPanel 73/81、RightPanel 189、main.cjs 126）全部精确命中；测试数/脚本 exit 码/事件全集/写集对账全部复跑一致 |
| 12 | dev doc 偏离审计 | ✅（1 项 P2） | D527 §5.1 写集 12 条目 ↔ 提交 12 文件逐条命中；§12 骨架 ↔ 实际 sse-contract.ts 一致；D528 §5.1 5 条目 ↔ 提交 4 文件 + build-synova.cjs 条件性零改动（spec 预设路径）。**P2-1**：RightPanel 默认 tab `'pattern'→'report'`（RightPanel.tsx:229）为行为变更，dev doc §5.1 仅写"加第 4 个 tab"未描述默认值变更（非阻断） |
| 13 | 控制塔执行审计 | ✅ | bypass.log：5335711d/11784bb2 均为 `COMMITTED | pre-commit PASS (hook 层登记)`；无 detected-bypass；commit-msg 格式 `feat(D527):`/`feat(D528):` 合规；分支 push 成功远端 tip=ae9e9044 与声称一致 |
| 14 | 版本编排合规 | ✅ | 切片任务无版本号变更需求（VERSION.md/git tag 无关） |

## 3. 前置核验复核（防 M7 漂移声明）

- engine 事件全集：synova-diagnosis-engine-impl.ts 发射 10 种（phase_started×6/phase_completed×6/report_ready/right_column_update/degraded/error/expert_hypothesis/hypothesis_generated/root_cause_identified）——grep 复跑一致 ✅
- useStreaming（main）9 case 无 default —— 复跑一致 ✅
- setCurrentReportId（main）零生产调用（app-store.ts:69/:113 仅定义）—— 复跑一致 ✅
- main.cjs（main）无 requestSingleInstanceLock —— grep 零结果一致 ✅
- **GS-01 漂移修正**：main 上 run.sh:112 仍断言 `prod.bin !== "node"`，而 main 上 backend-spawn.cjs:70 已为 `process.execPath + ['dist/backend.mjs']`（切片 B）→ main 上该断言恒 fail。切片 C 已修正（run.sh:108-115 断言 `prod.bin === process.execPath && args[0] === "dist/backend.mjs"`，②注释标明）。修正正确、诚实（dev doc §4.2/任务汇报均披露，未当作预存在绿）。

## 4. 北星对齐裁决（第二道）

对照 `.claude/PRODUCT-BRIEF.md`：
- **问 1（真实用户场景）**：D527 = 企业主/FDE 双击桌面端 → 一句话首诊 → 六阶段进度 → 报告可见（§二直接用户=FDE，§六 P0"没有真实可用的诊断旅程不能给 FDE 用"）✅；D528 = 升级/重装后首诊数据与企业基线完好（企业数据安全底线，§六 P0 诊断质量依赖真实使用数据）✅
- **问 2（终态推进）**：桌面端"从安装到可诊断 ≤30 分钟 + 报告可达 + 降级可见"（D527）与"数据目录物理隔离 + 升级语义契约 + 单实例锁"（D528）均为桌面端终态的直接推进，非横向加料 ✅
- **问 3（不变味）**：无 ChatBot/报表化漂移；报告渲染、数据保护都服务于"驻扎企业、主动诊断、为增长服务"的 Agent 定位 ✅
- **裁决：对齐**（PRODUCT-BRIEF §二 + §六 P0；施工图 §3.1 electron/ 品牌表层 🟢 死守一致）。两份 dev doc 均带 north-star front-matter（服务用户/场景/终态/对齐北星）。

## 5. 运行环境注记（D316 教训——环境依赖 vs 恒失败）

- **复跑环境**：Mac darwin-arm64，Node v24.19.0，审计工作区 worktree `.wt-slice-c-verify`（从 origin/feat/slice-c-d527-d528 检出，node_modules 软链自审计工作区）。
- **GS-01 8/8 的复现分类**：
  - 编码会话证据：`scripts/golden-scenarios/evidence/GS-01-2026-08-25.json`（17:52 生成，7 验收点全 pass：noauth-401/consult-entry-validated/reports-endpoint-ok/electron-pack-config-valid/electron-backend-spawn-contract/electron-dual-bootstrap/electron-userdata-dbpath + L1-6 consult-llm-recorded）——**属实**。
  - 本审计工作区复跑失败路径：① 审计工作区 node_modules 的 better-sqlite3 原生模块在 Node 24 下 V8 native crash（better_sqlite3.node Statement 析构栈），服务 bootstrap 超时 exit 2——**环境依赖**（编码克隆 /Users/wane/SynovaAgent/node_modules 正常，服务就绪 3100 复现）；② 审计会话沙箱禁写产品仓库 worktree（expect.runtime.json `Operation not permitted`）——**沙箱限制**。两者均非分支缺陷。
  - 结论：GS-01 8/8 声称 = **属实**（编码环境证据 + 代码路径复核 + 本环境 RED 分支行为一致：`CONSULT_LLM_RED (LLM key 未提供…)`）。
- **red 前提**（12 用例 red / 9 用例 red）不可复现（复现需回退分支文件，违反审计只读红线）；red 前提结构可信（sse-contract.ts 缺失 → import 失败必然全红；脚本缺失 → fs.existsSync 失败必然全红），以测试文件头注释 + dev doc §7 记录为准。

## 6. 发现清单

| 级别 | 发现 | 证据 | 归因 |
|------|------|------|------|
| P2-1 | RightPanel 默认 tab `'pattern'→'report'`（:229）行为变更未入 dev doc §5.1 写集描述 | dev doc §5.1 仅写"加第 4 个 tab"；diff 显示 useState 默认值同步变更 | devdoc（写集表未描述行为变更；对账只查文件集） |
| P2-2 | GS-01 金场景断言漂移（切片 B 改 prod 契约后 main 上 GS-01 electron-backend-spawn-contract 恒 fail）——已被切片 C 修正，但漂移期间 main 上金场景静默红 | main run.sh:112 vs main backend-spawn.cjs:70；切片 C run.sh:108-115 修正 | control-tower/devdoc（金场景无 merge 后自动执行门禁；M7 家族：脚本-实现漂移） |
| P2-3 | 审计工作区 node_modules better-sqlite3 原生模块 Node24 ABI 崩溃（环境） | /tmp/srv-audit.log native stack（better_sqlite3.node destructor） | 环境（D462 better-sqlite3 v12/Node24 兼容已列 K3 清单 P1） |

## 7. L4 防线缺口收割（每任务必答）

> **本次发现的问题（若有），控制塔哪一道防线本该拦住？为什么没拦住？缺什么？**

1. **GS-01 断言漂移（P2-2，已由切片 C 修复）**：本该拦住 = merge 后 main 金场景保持绿的自动化（CI 金场景 job 或 pre-push GS 组）。没拦住 = golden-scenarios 不在 pre-commit 13 组/pre-push 3 项中执行；CI 未覆盖金场景组（D467 挪 CI 后本地软提示 + CI 权威，但金场景未入 CI job）。缺 = 金场景在 main 上的"绿保持"门禁（K3 清单 D467 复核项可挂靠）。防线缺口记入控制塔待办。
2. **默认 tab 行为变更未入 dev doc（P2-1）**：本该拦住 = dev doc §5.1 写集表的"行为变更描述"自检（K3 切片 A C2 M7 同类）。没拦住 = 写集对账（DS8）只比对文件集与 diff --name-only，不比对行为变更。缺 = dev doc 写集表增加"行为变更"列或编码自检补一条（低优先）。
3. **无新增防线需求**：本次未发现 M1-M8 之外的新错误模式（GS-01 漂移 = M7 家族强化，不新增免疫细胞）。按 anti-bloat 原则，机制数量不增。

## 8. 归因表

| 问题 | 归因 | 防再犯机制 |
|------|------|-----------|
| GS-01 断言漂移 | control-tower（金场景无自动门禁）+ devdoc（切片 B 改契约未同步金场景） | 金场景绿保持门禁（控制塔待办）；M7 家族既有防线 |
| 默认 tab 变更未描述 | devdoc（§5.1 写集表行为变更缺位） | dev doc 模板行为变更列（低优先） |

## 9. 结论

**PASS** —— 切片 C（D527+D528）交付与声称一致：写集/接线/测试/降级/执行链全部物理核验通过；前置核验声明属实；GS-01 漂移修正正确且诚实；⏸ 项如实标注未伪造。2 项 P2 非阻断跟进（默认 tab 文档化、金场景绿保持门禁）。

- 合并顺序建议：`feat/slice-c-d527-d528` → main 后，应用本审计分支的 task-state 回填（audited + verdict）。
- 部署提醒：合入后运行 `bash scripts/workflow/checkpoint-deploy.sh [服务器URL]`。
