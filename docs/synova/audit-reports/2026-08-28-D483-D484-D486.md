# K3 独立审计报告 — register 认证闭环批次（D483 / D484 / D486）

> 审计员: Kimi K3（独立会话，零上下文）
> 日期: 2026-08-28
> 协议: AUDIT-PROTOCOL.md L1-L4 四层
> 结论: **CONDITIONAL PASS**（0 P0 阻断 / 0 P1 代码缺陷 / 1 环境依赖条件 / 3 P2 措辞级观察）

---

## 0. 审计范围与方法

- **审计对象**: register 认证闭环批次三切片 D483（匿名注册可达）/ D484（企业邀请链路）/ D486（E2E 测试切片）。
- **关键事实更正核实**: 交付方声称"派单前提『三切片停在 spec_done 待实现』与 main 物理状态不符，三切片代码实现已于 08-25/26 由 win 线 Claude Code 完成并经 PR 合入 main"。**本审计独立核实属实**：三实现 commit 均存在于 git 历史且已 merged（见 §2）。
- **方法**: claim-verifier 场景 2（声称完成必须物理证明）+ 7 项材料自收集 + 14 项语义清单 + 北星三问。全部证据 file:line，物理实测（grep / git / vitest / tsc / GitHub check-runs API / 本机真实 server 复跑）。

---

## 1. 结论总表

| 任务 | 结论 | 核心证据 | 备注 |
|---|---|---|---|
| D483 匿名注册可达 | ✅ PASS | auth.ts L91 + 78c5a84f → PR #156 CI 全绿 | 实现属实 |
| D484 企业邀请链路 | ✅ PASS | auth.ts L92-93/L84 + a6d4af07 → PR #170 CI 全绿 | 内存存储遗留按 spec descope |
| D486 E2E 测试切片 | ⚠️ CONDITIONAL | e2e 文件 4 it/39 expect 属实；"4/4 非 skip"环境依赖不可在 K3 工作区复现 | 见 §5 环境注记 |

---

## 2. L1 代码审计 — 14 项语义清单

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| 1 | 接口真实性 | ✅ | `src/middleware/auth.ts` L91 `path === '/api/auth/register'`、L92 `path === '/api/enterprise/register'`、L93 `path.startsWith('/api/enterprise/invitation/')`（grep 实测命中） |
| 2 | 文件路径存在性 | ✅ | `tests/e2e/auth-register-flow.e2e.test.ts`（9872 bytes）、`tests/routes/enterprise.test.ts`（366 行）、`tests/middleware/auth.test.ts`、`tests/middleware/auth.integration.test.ts` 全部存在 |
| 3 | Edge ID 有效性 | N/A | 本任务为 auth 中间件，无 Edge ID 引入 |
| 4 | 架构边界 | ✅ | auth.ts（L1 middleware）、enterprise.ts（L1 routes）无跨层 import；isWhitelisted 仅在 auth.ts 内部被 jwtAuthMiddleware（L255）调用 |
| 5 | 数据流完整性 | ✅ | 注册→UserStore→登录→邀请→接受→企业状态 全链路，e2e ①②③④ 四阶段覆盖（见测试文件结构） |
| 6 | 接线深度 | ✅ | isWhitelisted（L86）→ jwtAuthMiddleware（L252，L255 调用）→ server.ts 全局挂载（spec §5 接线链，本 session 复核 server.ts 无独立白名单） |
| 7 | 降级诚实性 | ✅ | getSecret 缺失时 log.error（L56）/ 过短时 log.warn（L59）返回 null，devMode 降级明确；jwtAuthMiddleware catch 有 log.error + degraded:true（L340-341） |
| 8 | 测试契约 | ✅ | e2e 文件头注释明确 server 运行契约（PORT/JWT_SECRET/DB path）+ 降级语义（beforeAll 探活→各 it 显式 ctx.skip，非静默空跑） |
| 9 | 测试覆盖 | ✅ | 4 it 块 / 39 expect：正常路径（①注册 201）+ 边界（④个人调 admin 403 + 无 token 401）+ 降级（server 未启动 skip） |
| 10 | 文件驱动 | N/A | auth 非文件驱动扩展 |
| 11 | 自我报告交叉对比 | ✅（3 处 P2 措辞偏差） | 见 §4 |
| 12 | dev doc 偏离审计 | ✅ | 三 commit 写集与 spec §3.1 一致（D483=5 文件、D484=4 文件、D486=2 文件，git show --stat 复核） |
| 13 | 控制塔执行审计 | ✅ | CI check-runs job 级独立取证（GitHub API），见 §3 |
| 14 | 版本编排合规 | ✅ | 三 commit 均未改 VERSION.md（测试/白名单分支，非门禁/工具行为变化） |

---

## 3. L3 执行审计 — CI job 级结论（GitHub check-runs API 独立取证）

| PR | head commit | 结论 | 证据 |
|---|---|---|---|
| #156（D483） | 25dfec21 | ✅ 全 success | TypeScript+Lint+Iron Laws / Architecture / Vitest(1/2)(2/2) / Integration Contract / Checker Review / npm audit / Golden Case F1 / **Control Tower Gate (windows+ubuntu) 均 success** |
| #170（D484） | 0af8d7b5 | ✅ 全 success | 10 项检查全 success（含双平台 Control Tower Gate） |
| #208（D486） | 34a9c28d | ⚠️ 诚实分类属实 | 9 项 success；**Control Tower Gate (windows+ubuntu) 双 failure**（2026-08-26 门禁脚本高频变更窗口） |
| main 54ed1cf5 | 54ed1cf5 | ✅ 双平台已恢复 success | 失败已随 gate 修复消散（报告 §3 分类属实） |

**核实**: 交付报告 §3 对 PR #208 Control Tower Gate 双平台 failure 的"瞬态、无因果路径、已消散"分类**诚实且准确**。D486 写集仅 tests/e2e/**（CI 明确排除），与门禁测试失败无因果路径。

---

## 4. L2 偏离审计 — 声称 vs 证据 + 簿记闭环

### 4.1 全部核心声称逐条核实（属实）

| 声称 | 证据 |
|---|---|
| register 入白名单 | auth.ts L91（grep 命中） |
| enterprise 匿名端点两分支 + 管理端点保持认证 | auth.ts L92-93 + L84 注释 + enterprise.ts L171 `router.get('/api/enterprise/invitations')`（复数，不匹配 `invitation/` 前缀） |
| bootstrap 绕过移除 | `grep -c signJwtToken tests/middleware/auth.integration.test.ts` = 0 |
| 单元用例 register pass-through | auth.test.ts L237-243 |
| enterprise 17 用例 | `grep -c "it(" tests/routes/enterprise.test.ts` = 17（366 行） |
| e2e 4 阶段 ≥4 it + ≥10 expect | 实际 4 个 it 块 + 39 expect（grep "it(" = 5 含 L17 注释） |
| 零回归 57/57 | 本机实测 `npx vitest run` 4 套件 = **57 passed (57)** |
| tsc 28=28 零新增 | 本机实测 `npx tsc --noEmit` = **28 errors**（_extinct 25 + server.ts 2 + ima.ts 1，与 spec 基线一致） |
| as any 零新增 | 三 commit added lines grep "as any" 命中均为文档元数据（task-brief "as any: 0" 行 + 铁律 38 引用），**代码零 as any** |
| 三 commit 消息格式 | 78c5a84f `fix(D483):`、a6d4af07 `fix(D484):`、b036d380 `test: D486 ...`（**D486 无 ASCII 括号**） |

### 4.2 簿记闭环核实（属实）

- `scripts/control-tower/gen-cto-health.py` L214 `re.search(r"\(D(\d{3})\)", line)` — D486 impl commit `test: D486` 无括号包裹 → 派生不出 impl_done。**属实**。
- 修复路径：交付报告 commit 83aae166 subject `docs(D486): ...` 含 `(D486)` → merge 后派生 impl_done。**机制内修复，未动 scripts/**（守住审计红线）。
- ⚠️ **状态**: 分支 `feat/d483-register-auth` **尚未合并 main**（本审计时 origin/main=f1c9be5a 无 83aae166），簿记闭环 PENDING PR merge。

### 4.3 交付物核实

- 交付报告 `docs/synova/coordination/DELIVERY-REPORT-D483-D484-D486-register-auth-20260828.md`（149 行）+ task brief（116 行，6 字段齐全）随 commit 83aae166 入分支。**属实**。
- commit 1d321cda 追加 1 行 `.claude/bypass.log`（`COMMITTED | pre-commit PASS`）= 正常 PASS 登记，**非 --no-verify 绕过**（交付方 DS6"全程未用 --no-verify"属实）。

---

## 5. 环境注记（关键 —— E2E 复现受限于 K3 工作区依赖漂移）

**结论: D486 E2E "4/4 非 skip" 判定为「环境依赖」——交付方环境成立，K3 工作区不可复现。**

| 环境 | better-sqlite3 版本 | Node 24 表现 |
|---|---|---|
| 交付方主仓 `/Users/wane/SynovaAgent` | **12.11.1**（匹配 package.json ^12.11.1） | ✅ 可启动（D462 目标版本） |
| K3 审计工作区 `/Users/wane/Synova-k3独立审计` | **11.10.0**（Aug 16 安装，陈旧） | ❌ 原生崩溃 `Assertion failed: (env) != nullptr`（better-sqlite3 Statement::~Statement 于 GC 时） |

- K3 工作区 Node 22 下 better-sqlite3 加载失败（NODE_MODULE_VERSION 137 vs 127）→ server `启动失败`；Node 24 下加载成功但 GC 阶段原生崩溃（两次复跑均崩溃，确定性）。故 **K3 工作区无法启动真实 server，E2E 无法独立复跑**。
- 根因: K3 工作区 node_modules 陈旧（11.10.0），主仓已 12.11.1。**非 D486 代码缺陷**（D486 写集仅 e2e 测试文件，不涉及 better-sqlite3）。本问题即已知任务 **D462（better-sqlite3 v12 升级 Node 24 兼容）** 范畴，且 D546 审计已记录同款环境注记。
- 判定依据: e2e 测试代码正确（4 it/39 expect/显式 skip 语义）、被测 auth 代码正确（L91-93 已核实）、57/57 单测覆盖同款 auth 逻辑全绿、CI（含 auth 单测/集成测试）绿、win+DSH 双线独立声称跑绿 —— 综合判断"4/4 非 skip"**可信但未在 K3 工作区独立复现**。

---

## 6. P2 观察（措辞级，不阻断）

1. **报告 §4 措辞不精确（M2）**: 写"本交付报告 commit 消息含 `(D483) (D484) (D486)`"，实际 commit 83aae166 subject `docs(D486):` **只含 `(D486)`**；D483/D484 由各自 impl commit `fix(D483):`/`fix(D484):` 派生。功能结果正确（三任务均派生 impl_done），但措辞 overclaim。
2. **报告 §3 "5 it()"（M2）**: `grep -c "it("` = 5 含 L17 注释 `各 it() 显式 ctx.skip()`，实际测试块 = **4 个**。spec DS2 要求 ≥4，4 个达标，结论不变，但计数口径含注释。
3. **报告 §6 note 7 环境注记不完整（M2/M5）**: 只提"Node 24 ABI 匹配"，未提 better-sqlite3 **版本漂移**（主仓 12.11.1 vs 其他 clone 11.10.0）导致的 GC 崩溃风险——该注记在交付方环境成立，换环境即崩溃。

---

## 7. 北星对齐裁决（三问）

对照 PRODUCT-BRIEF.md（`.claude/PRODUCT-BRIEF.md`）:

| 问 | 裁决 | 依据 |
|---|---|---|
| ① 服务真实用户场景？ | ✅ 对齐 | register/enterprise-invite 是"一对多对话 + RBAC（多用户的前提）"（PRODUCT-BRIEF §六 P1 L73）的基础，服务 FDE 多客户 onboarding 与企业主多成员管理 |
| ② 更接近终态？ | ✅ 对齐 | 多租户 onboarding 底座 + 邀请链路是朝"驻扎企业、多用户"终态走一步，非横向无关 |
| ③ 变味？ | ✅ 不变味 | auth 是基础设施，不改变"主动诊断、为增长服务"的 Agent 本质，非 ChatBot 化 |

**裁决: 对齐北星 §六 P1（一对多对话 + RBAC 多用户前提）。**

---

## 8. L4 防线缺口收割

| 问题 | 归因 | M 模式 | 防线缺口 |
|---|---|---|---|
| K3 工作区 better-sqlite3 11.10.0 陈旧 → E2E 不可复现 | audit | M5（环境依赖） | 审计工作区开工前无依赖一致性预检（未比对主仓 node_modules 版本） |
| 交付报告 §4 "(D483)(D484)(D486)" 措辞 overclaim | implement | M2（声称 vs 事实） | 已有防线（DS verify + file:line）未拦截"措辞级" overclaim（非硬性文件路径类） |

- 两问题均命中已有 M 模式（M2/M5），**不新增 M9**。M5 已有防线面向"门禁脚本"，建议补一条审计工作区环境预检（比对关键原生依赖版本，如 better-sqlite3），归属 audit 预设。
- 原则: 机制数量收敛于错误模式种类数，不"每犯一错加一脚本"。

---

## 9. 审计结论

**CONDITIONAL PASS** — 三切片代码实现、测试覆盖、CI 结论、簿记机制全部物理核实属实，无 P0/P1 代码缺陷。

**条件（不阻断代码，仅需闭环）**:
1. D486 E2E "4/4 非 skip" 未能在 K3 工作区独立复现（环境依赖，见 §5）；建议 K3 工作区依赖同步（D462）后复跑确认，或采信 win+DSH 双线独立声称（代码/测试/CI 均已独立验证正确）。
2. 簿记闭环（D486 → impl_done）PENDING: `feat/d483-register-auth` 分支合并 main 后派生生效（交付方已提供 PR 链接）。

---

*报告由 K3 独立审计会话生成，仅含审计报告 + task-state 回填，零产品代码变更。*
