# K3 独立审计报告 — GA 线闭环批（D551 + D487）

> 审计员: K3（独立会话，零上下文）| 2026-08-29 | 派单: docs/synova/coordination/审计派单-20260829-GA线闭环批.md
> 范围: 初审。D551（Mac DSH，GA 校准后端，PR #281 合 6e9626f9）+ D487（Win，GA 诊断会话事件化装配，PR #261 合 6f2b69a8）
> 运行环境: macOS（审计工作区 /Users/wane/Synova-k3独立审计，分支 audit/k3-20260829-ga-line @ origin/main）
> **关键环境注记（D316 教训，先读）**: 本工作区 node_modules 的 better-sqlite3 按 Node 24（ABI 137）编译。用 Node 22 跑测试 = 8/40 假失败（NODE_MODULE_VERSION 127 mismatch，环境依赖失败，非产品缺陷）；用 Node 24（`/Users/wane/.nvm/versions/node/v24.19.0/bin/node`）复跑 = 全部通过。本报告所有"测试绿"结论均基于 Node 24 实测。

---

## 0. 结论摘要

| D# | verdict | 结论依据 |
|---|---|---|
| **D551** | **PASS** | 四端点契约逐条比对一致；回流层 2/层 3 诚实边界属实（engine 零 diagnosis_conclusion 分支）；双迁移（d551_target_type/d551_memory_type）事务+幂等+有测试覆盖；三测试 40/40 独立重跑绿；tsc 28=28 基线零新增；CI 产品门禁 job 级全 success。仅 P2 备注×3（限值边界/统计上限/继承式 L1→L4 惰性导入） |
| **D487** | **CONDITIONAL PASS** | 双写链路（persistingOnEvent 映射 + 报告事件 + 失败 error 落流）file:line 属实；迁移物理实测 PASS（行保留 + seq 连续 + 幂等）；5 实例化点 + D500 第二处悬空（tui-v2 bootstrap）全装配；写集 13+台账声明与 15 实际 diff 一致且零 scripts/；D469 信号4 豁免前提属实、门禁修复独立性成立。**P1×1**: session_events 重建表迁移无提交测试（D551 同类迁移有测试，D487 没有——spec §4 测试表未同步 §3.2 新增迁移）。P2×3（as never 门禁盲区 / red 先行声称无法事后核验 / Win 线 dev doc 缺北星 front-matter） |

**CI 表述精确化（转达 CTO）**: 「CI 全绿」在产品门禁 job 级属实；但 GitHub Pages `build` job（Jekyll）在两 PR merge commit 上均 failure、`deploy` skipped——**预存失败**（基线 660e4dc7 / 84fc5655 同失败），非本批引入。建议 CI 报告口径区分「产品门禁 job」与「Pages 发布 job」。

---

## 1. L1 代码审计

### 1.1 D551 — spec §8.2 契约表 vs 实现（重点审计项①）

| 契约点 | 命令 | 断言 | 证据（file:line，当前 main） |
|---|---|---|---|
| 四端点存在 | `grep -n "router.\(get\|post\)('/api/ga/calibration"` | ≥4 | src/routes/ga-calibration.ts L121/L239/L314/L419 |
| requireGa 三态（401/400/403） | `sed -n '20,35p' src/routes/ga-auth.ts` | 无 auth→401 UNAUTHORIZED；有 auth 缺 orgId→400 ORG_REQUIRED（fail-closed 不回 'default'）；role∉{ga,admin}→403 FORBIDDEN | ga-auth.ts L20-36 |
| requireGa 四端点全用 | `grep -c "requireGa(req, res)" src/routes/ga-calibration.ts` | =4 | L123/L241/L316/L421；定义 ga-auth.ts L20 |
| 端点1 校验（mark_error 必填 errorType 四值+correctedContent；rewrite_logic 双版本；demote_signal sentinelId） | 读 L139-156 | 逐条 400 VALIDATION_ERROR | L139-147/L148-150/L151-153/L154-156 |
| 端点1 supersedes 版本链（同 target 才可链，否则 400 CHAIN_ERROR） | 读 L160-172 | store.list 按 tags [ga_calibration,targetType,targetId] 过滤 + e.id===supersedes，未命中→CHAIN_ERROR | L166-170 |
| 端点1 响应 201 {ok, calibrationId, supersedes} | 读 L225 | 形状一致（降级时附加 refluxDegraded 传播，铁律 31） | L225 |
| 端点2 latest 链头/全链 | 读 L268-278 | supersededBy 反向索引（L269-272）；默认滤被取代条目（L274）；includeChain=1 按 calibratedAt 升序、默认最新优先（L276-278）；limit≤200 默认 50（L249） | L269-278 |
| 端点2 响应字段含 supersededBy | 读 L281-296 | calibrationId/targetType/targetId/action/.../supersededBy? | L281-296 |
| 端点3 信号五要素校验（10 枚举/severity 1-10/confidence 0-100/edges/nodes 数组） | 读 L321-343 | 越界/枚举外→400 VALIDATION_ERROR，路由层拦截不落事件 | L321-343 |
| 端点3 响应 201 {ok, signalId, findingId} | 读 L404 | 形状一致 | L404 |
| 端点4 stats 形状 {calibration:{total,byAction}, injection:{total,byType(10键)}, reflux:{feedbackCount,byDecision}, note} | 读 L426-473 | byAction 四键 L426；byType 十枚举键 L441-442；byDecision 三键 L458；note 诚实降级文案 L471 | L426-473 |
| 端点4 回流只读 | `grep -n "queryFeedback" src/routes/ga-calibration.ts` | enterpriseId 必填（D338 fail-closed）+ actorRole='ga' JS 过滤 | L457-464 |
| 500 降级（store 异常） | 读 L99-107 | internalError → log.error + 500 + degraded:true | L99-107（四个端点 catch 均走此路径 L226/L299/L405/L474） |
| 回流映射（mark_error→reject / rewrite_logic→modify / demote_signal→ineffective / add_context 不回流） | 读 L43-47 + L206-222 | REFLUX_DECISION Partial 表无 add_context；decision 存在才写 feedback_log；evidenceRefs=[entry.id] 互链；失败→log.warn+refluxDegraded | L43-47/L205-222 |
| 注入链 L1→L2→L3（零旁路） | 读 L345-356 | 路由只调 injectManualSignal，不直写 sentinel_events | L346-356 |

### 1.2 D551 — §7 回流边界未被越界（重点审计项②）

- **engine 白名单零 diagnosis_conclusion 分支 — 属实**:
  - `grep -n "diagnosis_conclusion" src/loops/middle-evolution-engine.ts src/agent/loop-handlers.ts src/l3/*.ts` → **零命中**（origin/main @ 84fc5655）。
  - `git log --all --oneline -3 -- src/loops/middle-evolution-engine.ts` → main 上最后改动为 6279f451（D333）与 080fc7e4（D454），D556（80a19f0d，加 Signal 6 diagnosis_conclusion 回流层 2）**尚未合 main**——D551 spec §7.2 声明的「层 2 显式 descope、后续任务承接」与现状完全一致。
  - `git diff 660e4dc7..6e9626f9 --name-only` → middle-evolution-engine.ts / loop-handlers.ts 均不在写集。✓
- **未假装闭环**: 回流收口 = feedback_log 行 + getAggregatedSignals 聚合可见（GROUP BY 无白名单，feedback-collector.ts L285-291 未改）；进化动作生成/采纳率未在本单——stats note 显式声明（ga-calibration.ts L471）。✓
- **单源三红线**: 存储 AgentMemoryStore（L69-73 惰性导入）／回流 getFeedbackCollector() 活单例（L208，未 import ga-collaboration 死链）／注入 sentinel-service→runner.injectManualFinding→persistRunEvents（runner.ts L804）+projectRunRecord（L810）。✓

### 1.3 D551 — agent_memory 迁移的存量库安全性（重点审计项③）

- `git diff 660e4dc7..6e9626f9 -- src/l4/agent-memory-store.ts`:
  - 事务: BEGIN（L459 区）→ 建新表（含 status 列，L460-478）→ INSERT SELECT 全列复制（L479-482）→ DROP → RENAME → 三索引重建（L485-489）→ schema_version 标记（L490）→ **FTS5 双表全量重索引**（L491-492，`INSERT INTO agent_memory_fts(agent_memory_fts) VALUES('rebuild')` + trigram 同款）→ COMMIT；内层异常 ROLLBACK 后 rethrow（L493-496）；外层 catch log.warn 不阻断启动（fail-closed：旧 CHECK 保留，新 type 写入被拒 → 调用方走 degraded）。✓
  - 幂等: schema_version 判定（L449）+ 新建库 sqlite_master 含 'ga_calibration' → 仅补标记（L456-459）。✓
  - **测试覆盖**: tests/routes/ga-calibration.test.ts L557 起「agent_memory 旧库（旧 type CHECK + FTS）→ 迁移后可写 + 存量零丢失」真实断言（40/40 中绿）。✓
- feedback_log 迁移同构（feedback-collector.ts L182-230，事务+幂等+三索引重建），测试用例⑦（ga-calibration.test.ts L494-551）覆盖。✓

### 1.4 D551 — D556 前端接入前的后端契约完整性（重点审计项④）

- D556（feat 分支 80a19f0d，未合 main）前端数据层已按本契约构建: `git show 80a19f0d:electron-renderer/src/stores/ga-collab.ts` → L146 引用「GET /api/ga/calibration 条目（D551 L262-283 映射字段）」、L164 引用 stats（D551 L458-465）、L269 POST 请求体（D551 §8.2 端点1）、L362 signals（端点3）——**契约在 D556 消费前已完整且行号可锚定**。✓
- 前端占位锚点属实: capability.ts L43-46 `cap === 'ga' → role === 'ga'`（fail-closed 置灰）；RightPanel.tsx L632-640 GaDetail 占位注释「后端校准接口不存在 → 不伪造、不发 fetch」。✓

### 1.5 D487 — session_events 双写链路（重点审计项①）

- **persistingOnEvent**（src/agent/diagnosis-launcher.ts）:
  - 透传: L98 `onEvent?.(event)`；
  - 映射: L99-104 `phase_started|phase_completed → diagnosis_phase`；**其余全部（interim_finding/evidence_contradictions/degraded/community_reports/entity_resolution/error）→ diagnosis_module**；
  - 报告终点: L187-193 runConsultation 成功后 `persistEvent('diagnosis_report', {teamId, phase:5, summary, totalDurationMs, degradedModules})`；
  - 失败 error 也落流: L323-326 catch 分支 `persistingOnEvent({type:'error', ...})`（内部降级不再抛）。✓
- **persistEvent 防护**: L84-91 无 sessionId → 跳过（不落 '' 桶，c796da55 补强）；store 未装配 → 跳过（可选依赖）；appendEvent 返回 ok:false → log.warn + 诊断继续（铁律 24/31）。✓
- **launcher 内 onEvent 无旁路**: `grep -n "onEvent" src/agent/diagnosis-launcher.ts` → 仅 L69（类型声明）/L98（persistingOnEvent 内部透传）；所有发射点已替换为 persistingOnEvent（L124/L141/L158/L180/L296/L323）。✓
- **投影隔离**: session-store.ts L357-365 `if (ev.eventType !== 'message' && ev.eventType !== 'tool_result') continue;`——诊断事件 log-only。✓

### 1.6 D487 — 迁移幂等 + 行保留 + seq 连续（重点审计项②，物理实测）

- 实现: session-store.ts L150-184（sqlite_master 建表 SQL 缺 diagnosis_phase 判定 → BEGIN/COMMIT 重建 + ROLLBACK 保护；幂等——每次构造判定）。
- **K3 物理实测**（throwaway 脚本 /tmp/d487-migration-verify.mjs，tsx 加载真实 SessionStore 源码，构造旧 CHECK 库 + 3 存量行）:
  - 迁移后存量 3 行保留（id/seq 1-3 原样）；
  - 新诊断事件可写（CHECK 已扩），`appendEvent → {ok:true, seq:4}`；
  - seq 连续（1,2,3,4）+ id AUTOINCREMENT 连续（max=4）；
  - 二次构造（幂等）行数不变 = 4。**VERDICT: PASS**。
- **缺口（P1）**: 该迁移**无提交测试**（D551 的两条同类迁移均有测试）。tests/agent/diagnosis-session-events.test.ts 5 用例未覆盖重建表迁移；spec §4 测试表（5 用例）未同步 §3.2 新增的迁移。归因: devdoc。

### 1.7 D487 — 5 实例化点装配（重点审计项③）

| 装配点 | 证据（file:line） |
|---|---|
| cli 恢复分支（fromState wiring） | src/cli.ts L97-103（sessionId + sessionManager + sessionStore） |
| cli 新建分支（先建会话再建引擎） | src/cli.ts L124-132 |
| im-inbound（builtinStore 先建） | src/l1/im-inbound.ts L180-188 |
| mcp（getDatabase 守卫，失败内存态降级 log.warn；每次 diagnose 独立会话） | src/mcp/index.ts L224-247 |
| tui-v2（BootstrapResult.store 透传） | src/tui-v2/index.ts L35-47 |
| **tui-v2/lib/bootstrap.ts（D500 第二处悬空）** | src/tui-v2/lib/bootstrap.ts L131 `new SessionManager({...}, store)` |
| deploy/bootstrap（具名 store + ctx.set + BootstrapServices.sessionStore） | src/deploy/bootstrap.ts L685-703 + L633 |
| fromState 可选 wiring 参数 | src/agent/conversation-engine.ts L787-798 |
| engine 注入 ctx（内联 cast，EngineContext 接口不加 L5 字段） | src/agent/conversation-engine.ts L441-445 |
| L674 发起人 'FDE'→'GA' | src/agent/conversation-engine.ts L674 |
| **server 无实例化点（声明属实）** | `grep -n "ConversationEngine" src/server.ts` → 零命中（exit 1） |
| fromState 唯一生产调用方 = cli（已 wiring） | `grep -rn "fromState(" src/` → 仅 cli.ts L99 |

### 1.8 D487 — 写集一致性 + D469 信号4 前提 + 门禁独立性（重点审计项④⑤）

- **写集**: `git diff fe9bac14..6f2b69a8 --name-only` = 15 文件 = brief Q2 声明 13（src×9 + tests×1 + dev doc + brief + task-state）+ 台账（brief Q2 明示声明）+ .claude/bypass.log（D521 auto-hook 簿记）。零越界。✓
- **零 scripts/**: `git diff fe9bac14..6f2b69a8 --name-only | grep -c "scripts/"` = 0——Win 未改门禁脚本，与 D555/D557 修复互相独立（修复 commit ac38ee64/d1a91c8f 不在 D487 PR 链 fe9bac14..6448a6f5 内）。✓
- **D469 信号4 豁免前提**: ① task-state/D469.json 不存在（`ls task-state/ | grep D469` exit 1）；② D469 dev doc 在 main（docs/plans/codex/implementation/SYNOVA-IMPL-D469-session-event-sourcing-20260821.md），写集 4 文件（session-store.ts/session-manager.ts/2 测试）均已合 main；③ D557（d1a91c8f，V5.2.6）信号4「无 task-state 且写集全在 base → 豁免」与上述前提一致，台账 L226 记录 D487 分支修复后实测 exit 0。前提属实。✓
- dev doc §3.2 同 commit 回填最终形态（偏离点 4 处：装配点/落流机制/CHECK 迁移/附加发现）——0e6895fd/bbb49607/c796da55 均在 PR 链内。✓

### 1.9 测试契约与覆盖（铁律 47/48）

- **D551**: ga-calibration.test.ts 26 + ga-manual-injection.test.ts 8 + ga-auth.test.ts 6 = 40 用例全含 expect（含认证三态/四动作/越界拦截/版本链/迁移/降级/注入链/投影/聚合可见）。K3 独立重跑 40/40 绿。✓
- **D487**: diagnosis-session-events.test.ts 5 用例（①② 事件流+回放、③ 双写失败诊断不崩、④ 投影隔离、⑤ 未装配降级）。K3 独立重跑 5/5 绿 + 回归 conversation-engine.test.ts 11/11 + session-event-log + session-manager-eventlog 21/21。✓
- **D551 域回归抽验**（52/365 已由 CTO 按清单复跑，K3 抽验）: ga-annotations/ga-enterprise/ga-evolution/ga-diagnosis/sentinel-events/ga-collaboration 6 文件 61/61 绿。✓
- **tsc**: 28 错误全部预存（26 extensions/sentinels/_extinct/* + 2 server.ts L397-398 D478 时代 + 1 connectors/ima.ts），D551/D487 文件零错误。`grep -c "as any"`（代码行）= 0（29 处命中全为文档注释「铁律 38: 零 as any」）。
- **P2 注**: D487 测试③只断言"诊断不崩"，未断言 degraded 显式传播（degraded 仅经 log.warn 体现，铁律 31 的 UI/响应级传播在本场景无承载面——launcher 是 fire-and-forget）。可接受，非阻断。

---

## 2. L2 偏离审计（dev doc vs 实现）

| 项 | D551 | D487 |
|---|---|---|
| 写集表 vs 实际 diff | spec §3.3.1 11 文件（含 agent-memory-store.ts「写集偏差，三处声明」——commit 35ccd574 消息 + spec L93 + brief Q0）与 PR diff 10 代码文件+task-state 完全一致 | brief Q2 13 文件 + 台账声明 = 实际 15（含 auto-hook bypass.log）；spec §3.2 四处偏离全部同 commit 回填（装配点/落流机制/CHECK 迁移/附加发现 tui-v2 bootstrap） |
| §方案 vs 实现 | §6/§7/§8 契约全落（§1.1 逐条比对） | §3.1 假设「bootstrap/server 装配」→ 实测 cli/im/mcp/tui 装配，回填属实且 server 零实例化声明经 grep 证实 |
| 每条 DS file:line | DS1-DS8 全满足（DS2 四端点+共享认证；DS3 版本链；DS4 双迁移+双写；DS5 注入链；DS6 stats；DS7 九条 grep 全命中——§1 实测；DS8 CI+回填） | DS1-DS7: DS1 装配 grep 双命中；DS2 appendEvent+类型扩展命中；DS3 5/5；DS4 回归绿；DS5 范围一致；DS6 零 detected-bypass（窗口 08-28/29 无命中，9 条历史 detected-bypass 均 ≤08-25）；DS7 推送+CI 绿 |
| 诚实边界 | §7.2 层 1 闭环/层 2-3 descope 属实（§1.2） | §3.2「附加发现 D500 第二处悬空」属实（tui-v2/lib/bootstrap.ts L131 修复在写集） |
| 不做清单 | Q2 排除项（三存量路由/engine/electron-renderer/scripts-红线）零违反（diff 验证） | Q2 排除项（session-manager.ts/server.ts/diagnosis.ts/verify-parallel 脚本）零违反（diff 验证；0 scripts/） |

---

## 3. L3 执行审计（控制塔门禁，机器证据优先）

| 门禁 | D551（6e9626f9，PR #281） | D487（6f2b69a8，PR #261） |
|---|---|---|
| CI check-runs（job 级，GitHub API 独立取证） | TypeScript+Lint+Iron Laws ✓ / Vitest (1/2) ✓ / Vitest (2/2) ✓ / Architecture Check ✓ / Golden Case F1 Gate ✓ / Integration Contract Check ✓ / Control Tower Gate Tests ubuntu+windows ✓ / macos+windows 打包 ✓；**build（GitHub Pages Jekyll）failure + deploy skipped = 预存失败**（基线 660e4dc7 与 84fc5655 同失败） | 同上全 success；build/deploy 同预存失败 |
| pre-commit 登记 | .claude/bypass.log 窗口内全部 COMMITTED pre-commit PASS；D555 修复期有 1 条 BLOCKED（门禁真实拦截，台账 L225） | 窗口内 COMMITTED PASS；无 detected-bypass |
| bypass 检测 | 9 条 detected-bypass 全部为 07-26~08-25 历史，08-28/29 窗口零命中 | 同左 |
| 门禁修复独立性 | — | D555（V5.2.4/5, PR #282/283）+ D557（V5.2.6, PR #286）均不在 D487 PR 链；台账 L220（Win 缺陷报告）+ L226（收尾）因果链完整 |
| 认领制/G12 | D551 首轮 CI 红根因（session 无 brief）已补 brief 后过——台账 L225 如实登记 | 无 G12 红 |
| 版本编排 | 两任务均无 VERSION bump（功能装配，合规）；无 tag 变更 | 同左 |

---

## 4. L4 防线缺口收割（每任务必答）

**本次发现的问题与漏过它的防线：**

1. **[D487, P1] session_events 重建表迁移无提交测试**——D551 同批双迁移均有测试（ga-calibration.test.ts L494-604），D487 没有。
   - 本该拦住: spec §4 测试表（铁律 48 边界条件）+ S-5（red 覆盖失败模式）。
   - 为什么没拦住: 迁移是 §3.2 偏离回填新增的，回填同步了实现描述但**未同步 §4 测试表**。
   - 防再犯机制（不新增脚本，改 devdoc 模板一条）: 「§3.2 回填新增迁移/DDL 变更 → §4 测试表必须同步加迁移用例（预置旧 schema → 构造 → 断言新值可写+存量保留+幂等）」。归因: **devdoc**。
2. **[D487, P2] `as never` 门禁盲区**——mcp/index.ts L236 `new SessionStore(getDatabase() as never)`（D487 +1；全仓 11 处，10 处预存）。pre-commit 组 1 只匹配字面 `as any`（全仓 29 处命中均为注释，代码 0），`as never`/`as unknown as` 同属类型逃逸但门禁不拦。
   - 防再犯机制（控制塔模式扩一行，CTO 队列）: pre-commit 组 1 模式扩展 `as never`/`as unknown as` 为新文件阻断 + 存量告警。归因: **control-tower**。
3. **[D551/D487, P2] GitHub Pages build job 预存失败污染「CI 全绿」口径**——job 级看不是全绿（build failure），但产品门禁 job 全绿且失败预存。报告纪律: CI 结论必须写「产品门禁 job 全绿 + Pages build 预存失败（基线同失败）」。
4. **[D487, P2] Win 线 dev doc 缺北星 front-matter**（D487 dev doc 为 HTML 注释头，无 north-star 块）——本审计已按三问独立裁决为对齐（见 §5），但文档模板未带锚定块，第一道自检无锚可依。归因: **devdoc**（Win 线模板对齐）。
5. **[转达 CTO] K3审计清单-20260822.md 过期**——D500 行（P0 表）已合并且被 D487 spec 标注 audited；D551/D487/D556 未入清单。本批按 2026-08-29 派单执行，清单待 CTO 更新。

**无新增 M 类**（对照 AUDIT-FINDINGS-LEDGER M1-M8）: 发现 1 属 M7 变体（文档-实现漂移：回填未同步测试表）；发现 2 属铁律 38 门禁模式盲区（V4.4.2「扫描范围扩展」同型文化）；其余为备注级。不提议 M9。

---

## 5. 跑偏第二道（north-star 三问，对照 .claude/PRODUCT-BRIEF.md 原文）

**D551**: ①服务真实用户 — GA（=FDE，PRODUCT-BRIEF §二「直接用户」）+ 企业主（校准后诊断更贴商业现实）；直接命中 §六 P0「诊断报告质量验证……需要真实反馈」——GA 校准正是反馈入口。②更接近终态 — Module-3 蓝图指定切片，把置灰占位（capability.ts L43-46/RightPanel.tsx L632-640）变成可执行后端，纵向推进非横向加戏。③不变味 — 校准=人类验证数据入口（护城河=本体被真实数据验证的速率），注入进哨兵管线=「主动发现」，仍是驻扎企业的诊断 Agent。**对齐** ✓
**D487**: ①服务真实用户 — GA 诊断可回放=交付物可自证（K3 咨询 D394 片2 权威），回答「上次诊断为什么得出这个结论」，服务 GA+企业主。②更接近终态 — D500 地基 → D487 生产装配，是 D394 片2 指定路径上的装配步（信任层完整形态），非横向。③不变味 — 事件溯源强化「驻扎、可审计」的 Agent 属性，不改变产品性质。**对齐** ✓（注：文档缺 front-matter 锚定块——见 §4 发现 4，裁决仍按文档内容+权威链完成。）

---

## 6. 错误归因闭环

| 发现 | 归因 | 处置 |
|---|---|---|
| D487 迁移无测试（P1） | devdoc | 建议另起 FIX 小任务（补 1 个迁移用例），禁直接改原任务 |
| `as never` 门禁盲区（P2） | control-tower | CTO 队列（pre-commit 组 1 模式扩展） |
| Pages build 预存失败口径（P2） | control-tower/报告纪律 | 转达 CTO |
| Win 线 dev doc 缺 front-matter（P2） | devdoc | 转达 CTO（Win 线模板对齐） |
| D551 限值边界（GET limit 负数不 guard / stats reflux 1000 硬上限 / supersedes 窗口 200 条）（P2） | implement | 记入本报告 P2 备注，不强制修复 |
| 审计脚本本身 | — | 无缺陷转达（audit-check.py 对「catch→internalError」启发式误报 4 处——属已知盲区非本次引入，仅记录） |

---

## 附: 复现命令（全部在审计工作区 @ origin/main 实测）

```bash
# 测试（必须 Node 24，见文首环境注记）
/Users/wane/.nvm/versions/node/v24.19.0/bin/node node_modules/vitest/vitest.mjs run \
  tests/routes/ga-calibration.test.ts tests/routes/ga-auth.test.ts tests/sentinel/ga-manual-injection.test.ts   # 40/40
/Users/wane/.nvm/versions/node/v24.19.0/bin/node node_modules/vitest/vitest.mjs run \
  tests/agent/diagnosis-session-events.test.ts tests/conversation-engine.test.ts \
  tests/store/session-event-log.test.ts tests/orchestrator/session-manager-eventlog.test.ts   # 5+11+21 绿
# tsc 基线
/Users/wane/.nvm/versions/node/v24.19.0/bin/node node_modules/typescript/bin/tsc --noEmit | grep -c "error TS"   # 28
# 迁移物理实测（D487）
/Users/wane/.nvm/versions/node/v24.19.0/bin/node node_modules/tsx/dist/cli.mjs /tmp/d487-migration-verify.mjs   # PASS
# CI job 级
curl -s https://api.github.com/repos/tangbaobao520/SynovaAgent/commits/6e9626f9/check-runs   # D551
curl -s https://api.github.com/repos/tangbaobao520/SynovaAgent/commits/6f2b69a8/check-runs   # D487
# 审计基线
python3 scripts/audit/audit-check.py --full   # 2 PASS/900 WARN/469 FAIL（全仓基线；D551/D487 文件无新增 as any、无真空 catch）
```

---

> 本报告只含报告与回填（task-state/D551.json、D487.json audit 段），不修代码、不改 dev doc、不碰 scripts/audit/。发现的问题处置权归 CTO（FIX 任务派单 / 控制塔队列）。
