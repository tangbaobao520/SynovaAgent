# K3 复审报告：D558 / D560 / D561 闭合验证

> 审计员: Kimi K3（独立审计工作区，零上下文）| 2026-08-29
> 类型: 复审（审计闭环铁律 D382——只审上轮结论→修复点，不重审全部）
> 派单: docs/synova/coordination/审计派单-20260829-K3复审批-D558-D561.md（main @ 8cdac2b2）
> 审计基线: origin/main = 8cdac2b2（含 #300/#301/#302/#303）
> 环境注记: **Node 24.19.0（better-sqlite3 ABI 137 实测可用）跑全部 Node 测试**；Node 22 结果不采信（D316 同型环境依赖，ABI 127 vs 137）。本机 macOS（192.168.1.7）。

## 结论总表

| D# | 上轮结论 | 修复 PR（main 合并提交） | 本复审判定 |
|---|---|---|---|
| D558 | GA 线批 D487 P1：session_events 重建迁移无提交测试 | #300（41227cc1） | **PASS**（4 复审项全闭合；零新发现） |
| D560 | impl-done 批 D549 FAIL：零代码 PR + 749p 硬编码 + canary 未入列 | #301（5be1e03d） | **PASS**（4 复审项全闭合；P2×1 证据编码过报，不影响结论——见 §K3 复核项） |
| D561 | impl-done 批 P1×3：g12 丢失 / incident-loop 4b 恒红 / bypass 注释不实 | #302（4df4f981） | **PASS**（3 复审项全闭合；P2×3 记录跟进，均非本批复审项违约） |

**预期兑现**: GA 线批 P1 + impl-done 批 FAIL/P1 全部闭合——上轮 3 条 P1/FAIL 结论的修复点经独立物理复验全部落地。

---

## §1 D558（PR #300 = 41227cc1）—— D487 迁移测试补写 P1 闭合

**物理 diff**: `git show 41227cc1 --stat` = 7 文件 +213/-13（真实代码，非壳）：
tests/store/session-event-log.test.ts +105、packages/test-kit/tests/architecture/05-as-any-audit.test.ts +81、src/mcp/index.ts ±4、spec ±4、task-state/D558.json +26、bypass.log +3。

### 复审① 4 断言真覆盖迁移且非空壳 → ✅ 属实

- **断言**: 4 个 it() 块真实存在且各有 expect：`tests/store/session-event-log.test.ts` L222（行保留：迁移后 3 行 `toEqual(before)` + `tableSql().toContain('diagnosis_phase')`）、L232（约束升级：迁移前旧 CHECK 物理拒绝 `diagnosis_phase`——red 自证——迁移后 `appendEvent` ok + 事件可见）、L252（seq 连续：`res.seq===4` = 旧 MAX+1）、L261（幂等：二次构造后 `afterSecond toEqual afterFirst` + `sqlite_master LIKE 'session_events%'` 计数=1 + id 续写=4）。**零空壳**（每 it ≥2 断言）。
- **语义对照实现**: 测试的旧库模拟（CHECK 三类事件 + 预置 seq 1..3）与真实迁移判定条件精确吻合——`src/store/session-store.ts` L156 `if (tpl?.sql && !tpl.sql.includes('diagnosis_phase'))`、L160-174（CREATE new → INSERT SELECT 保 id → DROP → RENAME → 重建索引）。
- **实测**: `npx vitest run tests/store/session-event-log.test.ts tests/agent/diagnosis-session-events.test.ts`（Node 24）= **22 passed（17+5），0 failed**。

### 复审② S-5 先红可复现 → ✅ 属实（独立复现，非采信声称）

- **命令**: 隔离 worktree（origin/main 8cdac2b2）+ symlink node_modules，sed 改 `src/store/session-store.ts` L156 迁移条件为 `if (false)`。
- **预期输出**: 新 describe 4 断言全红、13 旧用例不受影响。
- **实测输出**: `4 failed | 13 passed (17)`——恰好 4 红，失败原因全部正确：
  - 「旧库行保留」: `AssertionError: expected 'CREATE TABLE session_events (\n …' to contain 'diagnosis_phase'`（session-event-log.test.ts:229）
  - 「约束升级/seq/幂等」: 落流日志 `error":"CHECK constraint failed: event_type IN ('message','tool_result','system')"`——迁移缺失时旧 CHECK 物理拒绝诊断事件，断言按设计先红。
- **反证结论**: 若迁移被后续改动破坏，该测试组必然红——非「存在但测不到」的空转测试。

### 复审③ 棘轮基线 9/87 与全仓实测一致 → ✅ 属实

- **命令**: 独立复刻扫描器口径（src/+packages/，排除 .d.ts/.test.ts(x)/node_modules/隐藏目录，块注释跨行状态机剥离 + 行注释剥离，**不剥字符串**，命中即断行）→ 476 文件。
- **实测**: `as any = 0`、`as never = 9`、`as unknown as = 87`——与 `packages/test-kit/tests/architecture/05-as-any-audit.test.ts` L34-37 `ESCAPE_BASELINE` **逐项一致**。
- **棘轮非空转**: L58-74 临时夹具用例（代码行命中 1/1、注释行零误报）存在；`npx vitest run tests/architecture/05-as-any-audit.test.ts`（packages/test-kit 自身 vitest config）= **6/6 绿**。
- **附带核验**: test-kit architecture 全目录 3 个失败文件（01-layer-boundaries/02-wiring-audit/04-file-size-audit，4 用例）在父提交 d0960c40 **同样失败**（同名同数）——main 存量，非本 PR 引入。

### 复审④ mcp 零 as never → ✅ 属实

- **命令**: `grep -n "as never" src/mcp/index.ts` → exit 1（零命中）；全 src/ 计数 = 9（与棘轮基线一致，且 mcp 不在其中）。
- **类型前提核验**: 清理处 `src/mcp/index.ts` L146/L238 现为裸 `new SqliteGraphStore(getDatabase())` / `new SessionStore(getDatabase())`；`getDatabase(): Database.Database`（src/init/engine-context.ts:50）；两构造器 `constructor(db: Database.Database)`（src/adapters/sqlite-graph-store.ts:106 / src/store/session-store.ts:96）——断言删除类型安全。
- **tsc**: 28=28 零新增（父提交同 28；28 处全为存量 `extensions/sentinels/_extinct/*`、src/server.ts、src/connectors/ima.ts）。

### D558 附：spec §4 同步 → ✅

`SYNOVA-IMPL-D487-ga-session-events-slice2a-20260828.md` §3.2（L71）补迁移测试落点注 + §4 测试表（L92）补 L5 迁移行——上轮 P1 的归因项（devdoc 未同步测试表）已同步。

---

## §2 D560（PR #301 = 5be1e03d）—— D549 FAIL 重做闭合

**物理 diff**: `git show 5be1e03d --stat` = 4 文件 +43/-8：ci.yml +11（6 测试入列）、claim-regex-narrow.test.sh +14（T5b 动态化）、task-state/D560.json +24。**对比 D549（731d2469 仅 11 行 task-state 壳）——真实代码成立**。

### 复审① 真实代码 → ✅（逐文件已核，见上）

### 复审② 漂移弹性（guard +3 行 T5b 仍绿）→ ✅ 独立复现

- **命令**: 隔离 worktree 内 sed 在 `scripts/pre-commit-check.sh` guard 上方插 3 行（awk NR==754 后插入），重跑测试。
- **预期输出**: T5b 动态跟随（guard 755→758），仍 9/9。
- **实测**: guard 插入前 T5b 输出「L755，动态定位」；插入后「**L758，动态定位**」→ `结果: 9 通过, 0 失败` exit 0。
- **机制**: `tests/control-tower/claim-regex-narrow.test.sh` L49 先 grep 定位正则检查行 LINE，L76 取 LINE 上方最近 `[ -f "$BRIEF" ]` guard（≤10 行邻接）——不再有可漂移的行号硬编码（旧 `sed -n '749p'` 已删）。

### 复审③ 25 项清单与 D540-D547 报告 P1-3 具名表一致 → ✅

- **实测**: `grep -oE 'tests/control-tower/[a-z0-9-]+\.test\.sh' .github/workflows/ci.yml | sort -u` = **25 项**（19→25）。
- **逐名对照**（ci.yml L157-162 新增 6 项 ↔ 报告 §2 P1-3 表，2026-08-28-D540-D547-governance-closeout.md L154-163）:
  claim-regex-narrow(D541) / ci-strict-visible(D542) / skeleton-brief-gate(D547) / clone-config-init / clone-shadow-commit / verify-parallel-ci（后三者 D540）——**6/6 逐字一致，零多补零遗漏**。
- **入列前置条件核验**: 6 测试本地独立重跑全绿（9/9、6/6、6/6、13/13、9/9、14/14）。

### 复审④ 恒红根除（main 上原 8/9 红态不再）→ ✅

- **命令**: 父提交 d0960c40 worktree 跑旧版测试 vs head 跑新版。
- **实测**: 父 = `❌ T5b 无 brief guard 缺失` + `结果: 8 通过, 1 失败`（**上轮 K3 声称的恒红态独立复现**）；head = `✅ T5b …（L755，动态定位）` + `9 通过, 0 失败`。
- **CI 可见性根治**: canary job 为 `exit $FAIL`（ci.yml:176）——任一入列测试失败即 job 红。5be1e03d 的 CT job 双平台 success 意味着含 claim-regex-narrow 在内的 **25 项在 CI 真跑全绿**，D549 的「真空绿」根除。

### §K3 复核项：749p 编码 P2 过报 → 属实（过报成立），但不影响结论

- **复现**: `grep -c "sed -n '749p'" tests/control-tower/claim-regex-narrow.test.sh` = **1**（非声称的 0）——命中 L21 注释行「硬编码 sed -n '749p' 在 D542/D543 加行后漂移」。
- **实质判定**: 可执行行检查 `grep -n "^[^#]*sed -n '749p'"` = **0 命中**——门禁意图（无可执行硬编码行号残留）物理成立；残留 1 处是修复说明自身的注释（与 D561 bypass 注释同型：如实注记引用旧措辞）。
- **结论**: 编码 P2（证据命令不精确——Done verify 应排除注释行），**不改变 D560 判定**。归因: implement。

---

## §3 D561（PR #302 = 4df4f981）—— impl-done 批 P1×3 恢复闭合

**物理 diff**: 6 文件 +162/-8：g12-day-window.test.sh +79（恢复）、incident-loop.py +30、check-bypass-log.sh +10、note +24、task-state +25。

### 复审① g12 恢复版与 D506 ERE 语义一致（10/10）→ ✅

- **实测**: `bash tests/control-tower/g12-day-window.test.sh` = **10 通过, 0 失败** exit 0。
- **三接线断言 vs main 实现逐条命中**（测试 L35-37 ↔ 实现）:
  - `DAY_WINDOW_RE=$(python3 -c` → scripts/pre-commit-check.sh **L1198** ✓（与 task-state 声称 L1198 一致）
  - `[[ "$b" =~ $DAY_WINDOW_RE ]]` → **L1210** ✓（=~ RHS runtime 展开按 ERE，`|` alternation 生效——D506 P0-1 语义）
  - 死代码 `today_files_by_suffix()` → grep 零命中 ✓（现函数为 today_files_by_prefix）
- **恢复忠实度**: 与 D506 原版（2370fc88）逐行 diff = 仅 4 处适配（路径头 test/→tests/control-tower/、恢复注释、用法行、输出格式 `pass=$PASS` → `结果: N 通过`），**行为逻辑零变化**。
- **声称核验边界**: 「自 dangling 9cb09dbb 恢复」——该对象不在共享仓库（本 clone `git cat-file -t 9cb09dbb` = 不存在），来源声称**无法独立核验**；实质已按 2370fc88 原版对照闭环。P2 备注（证据可追溯性）。

### 复审② incident-loop 8/8 且 Windows 分支零变化 → ✅

- **先红复现**: 父提交（5be1e03d 前）worktree 跑 = `7 通过, 1 失败`（4b 恒红——**上轮 K3 声称的 7/8 独立复现**）；head = **8 通过, 0 失败** exit 0。
- **机制**: 4b 用例（incident-loop.test.sh L68-73）构造 Windows 风格受限 PATH `/c/Windows/system32:/c/Windows`——macOS 下旧 `_find_bash` 仅 Windows 候选 → None → degraded → 恒红；D561 增 `POSIX_BASH_CANDIDATES`（/bin/bash 等，incident-loop.py L62-68）→ 受限 PATH 下命中 /bin/bash → closed → 绿。
- **Windows 分支零变化（代码级）**: `_find_bash` 候选选择 `GIT_BASH_CANDIDATES if os.name == "nt" else POSIX_BASH_CANDIDATES`（L83）——os.name=="nt" 时与父提交逐字节同义；`_bash_env` Windows 路径逻辑（root/msys 构造）父 vs head 逐行一致，POSIX 分支为早退新增。D316 修复保持。

### 复审③ 无「6+ 次根治」残留、行为零变化（配对 5/5）→ ✅

- **残留检查**: `grep -rn "补记循环根治" scripts/control-tower/check-bypass-log.sh` = 1 命中——L69 为如实注记自身「原注释声称「6+ 次补记循环根治」不实」；**原虚假声称行已删**（diff L66-72 全部为注释行替换：merge-base 化 = 范围收窄、真根治 = D513 fetch 刷新 + D451 纯补记豁免）。
- **行为零变化**: `git diff 5be1e03d 4df4f981 -- scripts/control-tower/check-bypass-log.sh` 非注释变更行 = **0**。
- **配对测试**: `bash tests/control-tower/check-bypass-log.test.sh` = **5 通过, 0 失败**（含「D513/③: 防御 fetch 已接线」断言）。

---

## §4 CI job 级结论（check-runs API，禁整体 run 结论）

| commit | CT ubuntu | CT windows | Vitest 1/2+2/2 | TS+Lint+Iron | Golden F1 | build(desktop) |
|---|---|---|---|---|---|---|
| 41227cc1 (#300) | success | success | success | success | success | **failure（预存）** |
| 5be1e03d (#301) | success | success | success | success | success | **failure（预存）** |
| 4df4f981 (#302) | success | success | success | success | success | **failure（预存）** |
| 8cdac2b2 (#303 派单) | success | success | success | success | success | **failure（预存）** |

- **build failure 归因**: desktop-build.yml 的 Electron 打包 job，main 上至少自 2d3522e8（本批次之前）持续 failure，d0960c40/8cdac2b2 同红——**非三 PR 引入**，属 main 存量（D529 相关桌面构建问题），不参与本批复审判定。
- **真空绿根除判定**: 5be1e03d 起 canary = 25 项 + `exit $FAIL`（ci.yml:176）→ CT job success 即「25 项真跑全绿」的 job 级证据，claim-regex-narrow 的红态已被 CI 实际监控。

---

## §5 跑偏第二道（对照 PRODUCT-BRIEF 三问）

| 问 | 判定 |
|---|---|
| ① 服务真实用户场景？ | 三任务均为控制塔/迁移/门禁完整性修复——服务于 Synova「驻扎企业、主动诊断」的运行可靠性（诊断事件流回放、声称完成门禁、认领窗口、循环卫生），是 Agent 可用性的地基。**对齐** |
| ② 更接近终态？ | D558 补齐迁移回归保护（K3 P1 归因项同步 spec）；D560 闭合 D540-D547 批的 M3 接线缺口；D561 恢复丢失测试+跨平台修复+注释如实化。全部是既有主线的收口，非横向加新。**对齐** |
| ③ 还是「驻扎企业、主动诊断、为增长服务」的 Agent？ | 无变味。**不偏离** |

---

## §6 L4 防线缺口收割 + 归因

| # | 发现 | 级别 | 归因 | 本该拦住的防线 / 为什么没拦住 |
|---|---|---|---|---|
| 1 | D560 Done verify「grep -c 749p → 0」过报（实测 1——注释行残留） | P2 | **implement**（M2 家族，证据编码不精确） | Done verify 命令未排除注释行。防再犯（不新增机制，强化既有 M2 防线）：Done verify 的 grep 类断言一律加 `grep -v '^\s*#'`（或改可执行行断言），与 pre-commit 组 1「跳过注释行」口径一致 |
| 2 | D561 恢复/修复的 2 个 hermetic 门禁测试（g12-day-window、incident-loop）**未入 CI canary**（ci.yml 零命中）——D560 同批建立的「门禁测试入 canary」接线验收项只覆盖了 P1-3 点名 6 个 | P2 | **implement + control-tower**（M3 家族复发） | D526 check-canary-drift.sh 已点名（::warning 不阻断），可见性存在但无强制。建议：控制塔队列补一条——恢复/新建 hermetic 门禁测试时同步入 canary（D560 验收项泛化，同一 M3 防线，不开新类）。CTO 若判定属闭合范围，为 ci.yml 一行追加 |
| 3 | incident-loop.test.sh L69-70 注释仍描述 D316 Windows 机制（「_find_bash 显式查找 Git 安装路径」），未同步 D561 POSIX 候选——注释与实现漂移（与 D508 注释事故同族） | P2 | **implement** | 注释如实化本次只修了 check-bypass-log.sh，测试注释未同步。建议随下批顺手改 |
| 4 | 「自 dangling 9cb09dbb 恢复」来源对象不在共享仓库，不可独立核验（实质已按 2370fc88 对照闭环） | P2 | **implement** | 证据引用悬空对象应附「与最近可达版本 diff」作为替代核验路径（本次已由审计补做） |
| 5 | （上轮，已闭环）D549 零代码 PR 为何漏过 | —（已闭环） | implement/CTO（M2） | 上轮 FAIL 根因 = CI canary 未含断裂测试 → 真空绿。D560 已入列 25 项 + exit $FAIL，防线成立，本批 job 级证据确认 |

**防再犯机制收敛**：本批 4 条新发现全部命中既有 M 类（M2/M3），零新类——按 Anti-bloat 原则不加新脚本/新免疫细胞，只在报告层面给出「强化既有防线」的落点（Done verify 排除注释行；canary 验收项泛化到恢复测试）。台账 M2/M3 防线不变。

---

## §7 环境注记（D316 同型教训）

- Node 24.19.0（nvm v24.19.0）跑全部 vitest/tsc：better-sqlite3 ABI 137 实测 OK（`node -e` 建表冒烟通过）。Node 22（默认 PATH 的 v22.22.0）结果不采信（ABI 127 不兼容）。
- bash 门禁测试（claim-regex/incident-loop/g12/check-bypass-log/canary 6 件）为纯 bash + python3，与 Node 版本无关，本机 macOS 实测如上。
- test-kit architecture 全目录 3 文件 4 用例失败 = main 存量（父提交同失败），非本批引入；test-kit 需用 packages/test-kit 自身 vitest config 跑（根 config include 仅 tests/**）。
- 父提交对照跑均用 git worktree + symlink node_modules 隔离，主工作区零污染（`git status` 零 tracked 变更）。

## §8 回填

task-state/D558.json / D560.json / D561.json 的 audit 段已按本报告回填（verdict=PASS、blockers=[]、reaudit=true、by=K3、at=2026-08-29）。
