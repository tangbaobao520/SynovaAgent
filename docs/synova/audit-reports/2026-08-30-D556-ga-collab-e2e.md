# K3 独立审计报告 — D556 GA 人机协同端到端（前端接线 + 回流层 2）

> 审计员: K3（独立会话，零上下文）| 2026-08-30 | 派单: docs/synova/coordination/审计派单-20260830-D556-GA端到端.md
> 范围: 初审。dev-doc spec（PR #308，合 f8dadf7b）+ 编码实现（PR #309，合 69d81c58，squash 自 80a19f0d）
> 运行环境: macOS（审计工作区 /Users/wane/Synova-k3独立审计，分支 audit/k3-20260830-d556 @ origin/main = 581543d2）
> **关键环境注记（D316 教训，先读）**: 本工作区 node_modules 的 better-sqlite3 按 Node 24（ABI 137）编译。DSH 会话默认 shell node = v22.22.0（ABI 127）→ 引擎测试 2/9 假失败（NODE_MODULE_VERSION mismatch，环境依赖失败，非产品缺陷）；用 Node 24（`/Users/wane/.nvm/versions/node/v24.19.0/bin/node`）复跑 = 63/63 全绿。本报告所有"测试绿"结论均基于 Node 24 实测。

---

## 0. 结论摘要

| D# | verdict | 结论依据 |
|---|---|---|
| **D556** | **PASS** | 六项审计重点全部物理核实：五场景 UI 断言 17 用例（含源码级镜像断言）+ 引擎 3 回流→3 动作 9 用例逐 case 绿（Node 24 独立重跑 63/63）；层 3/采纳率 descope 边界未越界（stats note 原文透传 E2E 实测 + 引擎「待人工审核——不自动改诊断权重」）；seed 通道不可达偏离 ② 两段代码路径物理成立（DEV_MODE=true dev-admin 覆盖 / false 401 拦截）；三处受控偏离 ①③ + DS6 措辞更正逐条核属实；D556.json main 终版 JSON 合法零冲突标记。**P1×1**（seed 通道服务端不可达——已由 CTO 上报创始人裁决，非 D556 引入，层 2 回流不受影响）+ P2×3（"63 断言"措辞 / DS7 E2E 证据 gitignored 不可核 / PRODUCT-BRIEF.md 前端 UI 伪需求条目过期）。 |

**CI 表述精确化（转达 CTO，延续 D551 报告口径）**: 「CI job 级 10/10」在产品门禁 job 级属实——PR 分支 818dfccc/e440a086/b9d11032 十项 ci.yml 门禁 check-runs 全 success（TypeScript+Lint+Iron Laws / Vitest×2 / Architecture Check / Checker Review / Control Tower Gate Tests×2 / Integration Contract / npm audit / Golden Case F1）。merge commit 69d81c58 上 checker-review 为 skipped（`if: pull_request || feat/*`，main 推送不触发，预期）。GitHub Pages `build`（Jekyll）在 merge commit 上 failure——**预存失败**（spec commit f8dadf7b、dispatch commit 581543d2 同失败，且 D551 报告 §0 已登记），非本批引入。

---

## 1. L1 代码审计

### 1.1 重点① 五场景 UI 断言独立重跑（tests/ga-collab-ui.test.ts）

| 场景 | 断言（源码级镜像） | 证据 file:line |
|---|---|---|
| 占位零残留 | 输出不含「后端校准接口待接入」+ **读 RightPanel.tsx 源码断言**（`readFileSync` 读源文件 not.toContain 占位 + toContain 'GaDetailSections'——容器真接线非占位替换） | tests/ga-collab-ui.test.ts L63-87（源码级镜像 L77-86） |
| 三块渲染 | 三标题 + data-ga-block×3 + data-endpoint 与 D551 四端点三块映射一致 | L93-122 |
| 计数变化 + note 透传 | 5/2/3 计数 + `note='回流计数 ≠ 采纳率——…指标诚实降级'` 原文透传 + data-honesty="note" | L128-151（note 原文断言 L148） |
| 降级 UI（503） | cap-degraded-banner + 降级文案 + 重试 + 零计数假数据 | L157-176 |
| role 防御（blocked） | 「仅 GA 可见」+ 零 `<form`/零 data-ga-block（fail-closed 不渲染可交互块） | L182-196 |
| 分块独立降级 | 降级块内联降级条 + 成功块照常渲染 + 整面板降级条不出现（不连坐） | L202-224 |
| 提交回显 | calibrationId / signalId / findingId 回显 + 会话内注入历史 + 列表条目 | L230-257 |

**K3 独立重跑**: `node(24) vitest run tests/ga-collab-ui.test.ts` = **17/17 绿**（6ms）。断言形态与 spec §8 断言矩阵逐条一致（spec L187-195）。

### 1.2 重点② 3 回流→3 动作（tests/loops/ga-calibration-evolution.test.ts）

| 用例 | 断言 | 证据 |
|---|---|---|
| 3 回流→3 动作 | reject/modify/ineffective 各一组 → 恰 3 条 'diagnosis_calibration_review'；每动作 decision/targetIds/sampleCount/hint 逐项断言；confidence=min(count/10,0.9)=0.3 | L113-139 |
| Signal 4 重叠（DS6） | ineffective 组同时命中既有 expert_confidence_downgrade → 全动作 4 条 | L141-147 |
| 阈值 | count=2 → 零动作（MIN_TRIGGER_COUNT=3 同源） | L149-154 |
| targetType 纪律 | diagnosis_logic×reject≥3 → 不触发（白名单只认 diagnosis_conclusion） | L156-161 |
| 既有白名单不动 | reject×sentinel_alert 仍走 threshold_adjust | L163-169 |
| sink 真实落盘 | applied=3/skipped=0/errors=0；agent_memory 3 条目；key 前缀/tags/type/source/value.actionType 逐项断言 | L176-212 |
| 全链 | processFeedbackSignals(3 回流)→applyEvolutionActions→applied=4（3 审核+1 Signal 4） | L214-221 |

**K3 独立重跑**: Node 24 = **9/9 绿**；Node 22（DSH shell）= 2/9 假失败（better-sqlite3 ABI 137≠127）。sink 落盘断言 `entry.type === "ga_correction"`（L198）直接验证偏离 ③。

### 1.3 重点③ 层 3 / 采纳率 descope 边界未被越界

- **stats note 原文透传 E2E 实测**: ui test L134/L148 断言 `note='回流计数 ≠ 采纳率——采纳判定数据源不存在（spec §3.3 排除），指标诚实降级'` 经 renderToStaticMarkup 输出 `toContain` 原文 + `data-honesty="note"`。生产面: ga-detail-sections.tsx L193 标签「回流计数」+ L194-198 note 原文渲染；mapStatsResponse（ga-collab.ts L222-224）`note` 原文透传、`degraded` 标记透传（不加工不丢弃）。
- **引擎诚实边界**: applyDiagnosisCalibrationReview 落盘日志「GA 校准审核条目已写入 agent_memory（待人工审核——不自动改诊断权重，层 3 descope）」（middle-evolution-engine.ts L601-605）；sink 只写 agent_memory 审核条目，**无任何权重/阈值回写代码**（diff 审阅确认，仅 `result.applied++`）。
- **采纳率无数据源**: GaStatsData 三块（calibration/injection/reflux）无采纳率字段（ga-collab.ts L165-171）；回流计数字段命名 `feedbackCount` 非 `adoptionRate`。✓ 未越界。

### 1.4 重点④ seed 通道不可达偏离（deviation ②）——两段实测证据核实

偏离 ② 声明（task-state/D556.json impl.deviations[1]）: 「全局 jwtAuthMiddleware + 白名单不含 /api/ga/* → legacy x-synova-token 在两模式下均不可达 requireGa」。K3 逐条物理核实：

| 证据 | 命令/读码 | 断言 | 结果 |
|---|---|---|---|
| 全局中间件 | `grep -n "app.use(jwtAuthMiddleware)" src/server.ts` | 挂载于路由前 | server.ts L291（Phase 0.1 认证中间件）✓ |
| 白名单无 /api/ga/* | 读 auth.ts isWhitelisted L86-104 | 16 条路径（/health//api/auth/login/register//api/enterprise/*//api/status//assets/*/*.html/js/css/ico//cockpit//api/healthz//api/sentinel/*//api/cockpit/*），**零 /api/ga/** | L86-104 ✓ |
| DEV_MODE=true 覆盖 | 读 jwtAuthMiddleware L260-267 | `!secret && DEV_MODE==='true'` → 注入 `role:'admin'`（dev-admin）→ next | L261-265 ✓（seed 的 ga 身份被 admin 覆盖） |
| DEV_MODE=false 401 | 读 L273-290 | 无 `Authorization: Bearer` → 401 UNAUTHORIZED；x-synova-token 非 Bearer 不被读 | L274-281 ✓ |
| legacy 分支被影子 | 读 extractAuthFromRequest L359-364 | `if (req.auth) return {...req.auth}` **优先于** x-synova-token 分支 L366-376 | L359-364 ✓（legacy 分支仅白名单路由可达，/api/ga/* 不可达） |
| 层 2 不受影响 | 读 ga-calibration.ts L215/L461 | actorRole='ga' 硬编码写 feedback_log + stats 过滤，与身份来源无关 | L215/L461 ✓ |

**结论**: 偏离 ② 两段代码路径物理成立（DEV_MODE=true 被 dev-admin 覆盖 / false 被 401 拦截）。spec §7.2.3 前提（「requireGa 经 extractAuthFromRequest legacy 分支拿到 role='ga'」）在真实 auth 拓扑下不成立——**spec 前提错误**，实现层在 E2E 实测中发现并如实登记（未跨写集修，auth 领地 D483-D486 在途）。**修复路径判断**: D483-D486 真实 JWT 落地（派单既定替代方案）> auth 白名单放行 /api/ga/*（auth 领地，D483 在途禁碰）——CTO 待创始人裁决方向正确，无越界建议。**注**: 两段「实测」日志在 gitignored evidence/D556/（见 P2-2），本报告以代码路径物理核实替代。

### 1.5 重点⑤ 三处受控偏离逐条核 + DS6 措辞更正

| 偏离 | 声明 | K3 核实 | 结果 |
|---|---|---|---|
| ① 渲染桥接等价实现 | spec §8 renderToStaticMarkup 经 renderer node_modules 解析 react-dom 物理不成立 | `grep -c '"react-dom"' package-lock.json` = **0**；root package.json 零 react-dom/react-markdown/zustand；ci.yml vitest job 仅 root `npm ci`（L73/L79，零 renderer 安装）；render.ts 零依赖元素树序列化器（91 行，grep react-dom = 0）；仅 tests 引用（tests/ga-collab-ui.test.ts L18 import，生产零引用） | **属实** ✓ |
| ② seed 通道不可达 | 见 §1.4 | 代码路径物理成立 | **属实** ✓ |
| ③ 层 2 sink 枚举内值 | spec §6.1 'ga_calibration_review' 不在 MemoryType 枚举/DDL CHECK | MemoryType 枚举含 'ga_correction'（agent-memory-store.ts L34）**无** 'ga_calibration_review'；DDL CHECK（L355/L467）同；实现落 'ga_correction'（engine L602），判别值冗余于 key 前缀 `ga_calibration_review:`（L598）+ tags[0]（L607）+ value.actionType（L607）三处 | **属实** ✓ |
| DS6 措辞更正 | spec §6.2「Signal 5」→ 代码「Signal 6」 | 文件内既有注释 Signal 5 = 矛盾仲裁（cross_dept_arbitration），为保 DS6「只增不改」新信号顺延编号；代码注释 L138-141 明示 | **属实** ✓ |

### 1.6 重点⑥ D556.json main 终版合法性

- **JSON 合法**: `python3 -c "import json; json.load(open('task-state/D556.json'))"` = **valid**。
- **零冲突标记**: `grep -nE "^(<<<<<<<|=======|>>>>>>>)"` = 零命中。
- **spec/impl 块完整**: spec 块（doc/baseline/dispatch/source/by/at 六字段）+ impl 块（branch/baseline/commits/write_set/ds_matrix DS1-DS9/deviations ①②③/honesty_boundary/evidence/tests）齐全；`audit` 段为 null（待本报告回填）。
- **历史冲突标记提交**: 9b2e7cff（误提交）/ c8a18e99（修复）不在任何 ref/reflog（squash-merge 摧毁 PR 分支中间提交 + reflog 已 GC）——无法逐 commit 复核，但**终态干净**（main 版零冲突标记、JSON 合法），符合「审 main 终版」派单要求。

### 1.7 DS6 存量零回改 + 接线 + 类型安全

- **存量零回改**: `git diff 69d81c58^ 69d81c58 --name-only | grep -E "^src/routes/|^src/growth/|^src/middleware/|^src/agent/loop-handlers\.ts$|capability\.ts$"` = **零命中**。引擎 diff 删除行 = **恰 1 行**（`-  | 'cross_dept_arbitration';` union 终止符，为追加新成员移动分号——dispatch「引擎唯一删除行=union 终止符」属实）。
- **接线**: RightPanel L22 import GaDetailSections + L789 渲染；L664 canAccessCap 双保险零 fetch；L685/699/730/759 四端点调用；L155-159 seed 附 x-synova-token（仅 seed 存在）；每 fetch catch console.warn（L740/743/779/782，铁律 24）。ga-collab 双消费（RightPanel L10 getSeedToken / app-store L5 getSeedIdentity）。层 2: applyDiagnosisCalibrationReview → applyEvolutionActions case（engine L646-649）→ loop-handlers L399（生产消费线在位，新动作零接线改动即生效）。
- **类型安全**: 新增文件 `as any/as never/as unknown as` = **0**（ga-collab.ts 仅有注释「铁律 38 零 as any」；类型逃逸用类型守卫 isRecord/asNumber/asString + 内联 `as GaTargetType` 等经 `.includes()` 前置守卫）。
- **契约（铁律 47）**: ga-collab.ts 每个导出函数 JSDoc @input/@output/@degraded/@error 齐全；applyDiagnosisCalibrationReview 契约 JSDoc（engine L557-577）含诚实边界声明。

### 1.8 测试契约与覆盖（铁律 48）

- 三层 63 用例（logic 37 + ui 17 + engine 9），expect() 调用 187 处（95+52+40），全非空壳。
- logic 37 用例覆盖正常/降级/边界（seed 读取 6 / decideBlockState 4 / deriveOverallPhase 4 / mapStats 5 / mapCalibrations 4 / buildCalibrationRequest 8 / buildSignalRequest 5 / parseNumberInput 2——见 test 文件 L57-355）。
- **P2-1 措辞**: task-state「63 断言全绿」中「断言」= 测试用例（it 块）数，非 expect() 调用数（187）。不影响结论，但「断言」术语不精确（建议改「63 用例」）。

---

## 2. L2 偏离审计（dev doc vs 实现）

| 项 | D556 |
|---|---|
| 写集表 vs 实际 diff | spec §3.3.1 8 文件（3 改 + 5 新建）；实际代码 9 文件（+1 ga-detail-sections.tsx 独立文件 = 偏离 ①，commit message + task-state note 已登记）。协调文件（spec/brief/task-state/台账/bypass.log）为流程附属，非代码写集。零越界（受保护路径零改动，§1.7） |
| §方案 vs 实现 | §5 端点映射/§6 动作映射/§7 seed 全落；§8 renderToStaticMarkup → 零依赖序列化器等价实现（偏离 ①，断言形态不变）；§7.2.3 seed 服务端前提错误（偏离 ②，如实登记） |
| 每条 DS file:line | DS1-DS9 全满足：DS2 37 用例绿；DS3 五场景 17 绿；DS4 boot seed（app-store L96-101/L109）+ apiFetch 附头（RightPanel L155-159）；DS5 层 2 9 用例绿；DS6 存量零回改（§1.7）；DS7 E2E evidence（gitignored，见 P2-2）；DS8 as any=0 + 降级诚实；DS9 CI 十项 success（§3） |
| 诚实边界 | honesty_boundary（回流收口=动作生成+审核条目，非权重更新；采纳率无数据源；stats note 透传）逐条属实（§1.3） |
| 不做清单 | Q2 排除项（src/routes//src/growth//src/middleware//loop-handlers/capability/App.tsx/package.json/scripts）零违反（§1.7 diff 验证） |

---

## 3. L3 执行审计（控制塔门禁，机器证据优先）

| 门禁 | D556（69d81c58，PR #309） |
|---|---|
| CI check-runs（job 级，GitHub API 独立取证） | PR 分支 818dfccc/e440a086/b9d11032 十项全 success；merge 69d81c58 九 success + checker-review skipped（main 推送不触发，ci.yml L193 条件）+ GitHub Pages build（Jekyll）failure（**预存**，f8dadf7b/581543d2 同失败） |
| bypass 登记 | .claude/bypass.log 含 6 条「COMMITTED pre-commit PASS」登记（impl PR 链内 auto-hook），无 detected-bypass |
| 认领制/G12 | 有 G12 红（brief 文件名无日期前缀→CI 不可见）→ 修复 942fba71（补前缀）+ 23c2cfaa（对齐 UTC 时钟）→ 台账 CT-45 登记（brief resolver 运行日时区缺陷，控制塔领地） |
| 版本编排 | 无 VERSION bump（功能装配，合规）；无 tag 变更 |
| 提交集映射（D320 教训） | task-state/D556.json 提交链：da6a9178→0010eefb（派单）→80a19f0d（impl）→1248944c（impl 段回填）→942fba71/23c2cfaa（G12 修复）→90f56fe5（CT-45 台账）→e3d118b6（DS9 回填）→f8dadf7b（spec #308）→69d81c58（impl #309）→581543d2（提审 #312）——非单提交 |

---

## 4. L4 防线缺口收割（每任务必答）

**本次发现的问题与漏过它的防线：**

1. **[D556, P1] seed 通道服务端不可达（spec §7.2.3 前提错误）**——spec 声称「requireGa 经 extractAuthFromRequest legacy 分支拿到 role='ga' 四端点全通」，但全局 jwtAuthMiddleware（server.ts L291）在 requireGa 前拦截，/api/ga/* 不在白名单，legacy x-synova-token 被 dev-admin 覆盖或 401 拦截。
   - 本该拦住: spec §7 现状材料（auth.ts L366-376 被引为「seed 既有通道」时，未核对 server.ts 全局 jwtAuthMiddleware 的拦截顺序）+ dev-doc 接口真实性反向验证（hook 只 grep 函数签名，不验证「调用链可达性」）。
   - 为什么没拦住: spec 锚定 auth.ts legacy 分支的存在性，未追踪「该分支在运行时是否可达」（中间件拓扑），属 M7 变体（文档-实现漂移：前提锚定在错误的事实层）。
   - 防再犯机制（不新增脚本，devdoc 模板一条）: 「引用 auth/seed 通道时，必须验证中间件拓扑可达性——grep 全局 app.use(jwtAuthMiddleware) + isWhitelisted 是否含目标前缀 + extractAuth 的 req.auth 优先级」。归因: **devdoc**。
2. **[D556, P2] DS7 E2E evidence gitignored 不可核**——evidence/D556/（walkthrough-api.log/seed.md/render-snapshot）在 .gitignore L76 `evidence/`，main 零该目录。E2E「实测」声称只能以代码路径 + 测试间接核验。
   - 防再犯机制: evidence 落盘改 docs/synova/product-lines/evidence/（git 跟踪，已有 D524 先例）或 task-state 附关键日志摘要。归因: **implement**。
3. **[D556, P2] PRODUCT-BRIEF.md 前端 UI 伪需求条目过期**——.claude/PRODUCT-BRIEF.md L83「前端 UI：FDE 操作台用 API 就行」与桌面端（创始人已批准品牌表层）冲突；§二用户（FDE/企业主）无 GA 角色。方向裁决见 §5（对齐，创始人已裁决），但锚点文档需 CTO/创始人更新。归因: **devdoc**（产品文档同步）。

**无新增 M 类**（对照 AUDIT-FINDINGS-LEDGER M1-M8）: 发现 1 属 M7 变体（文档-实现漂移）；发现 2/3 为备注级。不提议 M9。

---

## 5. 跑偏第二道（north-star 三问，对照 .claude/PRODUCT-BRIEF.md 原文）

**D556**: ①服务真实用户 — spec front-matter 服务用户「GA」不在 PRODUCT-BRIEF §二（FDE/企业主）字面清单，但 GA 校准直接命中 §六 P0「诊断报告质量验证……需要真实反馈」（L68）——GA 校准正是该反馈入口，服务企业主（校准后诊断更贴商业现实）。②更接近终态 — Module-3 蓝图指定切片（创始人批准方向 A），把 D551 已付后端成本变现（前端置灰→真实）+ 修 S5-2 层 2 断裂，纵向推进非横向加戏。③不变味 — 校准回流=人类验证数据→系统进化（护城河=本体被真实数据验证的速率），且层 3 显式 descope 尊重 §六伪需求「专家知识自动进化：没真实纠错数据，自动进化=自动退化」（L82）——不自动改权重、只进人工审核队列。仍是「驻扎企业、主动诊断、为增长服务」的 Agent。**对齐** ✓（注：PRODUCT-BRIEF L83「前端 UI 伪需求」已过期——桌面端品牌表层为创始人 2026-08 批准，见 §4 发现 3，建议 CTO 更新锚点文档；GA 角色为创始人引入的架构扩展。）

---

## 6. 错误归因闭环

| 发现 | 归因 | 处置 |
|---|---|---|
| seed 通道不可达（P1） | devdoc | CTO 已上报创始人裁决（D483-D486 JWT vs auth 白名单）；K3 不越界建议 |
| evidence gitignored 不可核（P2） | implement | 转达 CTO（evidence 落盘路径改进） |
| PRODUCT-BRIEF 前端 UI 伪需求过期（P2） | devdoc | 转达 CTO/创始人更新锚点文档 |
| "63 断言"措辞（P2） | implement | 记入备注，不强制改（建议 task-state 改「63 用例」） |
| 审计脚本本身 | — | 无缺陷转达（本报告未依赖 audit-check.py 结论，全物理 grep/diff/独立重跑） |

---

## 附: 复现命令（全部在审计工作区 @ origin/main 实测）

```bash
# 测试（必须 Node 24，见文首环境注记）
/Users/wane/.nvm/versions/node/v24.19.0/bin/node node_modules/vitest/vitest.mjs run \
  tests/ga-collab-logic.test.ts tests/ga-collab-ui.test.ts tests/loops/ga-calibration-evolution.test.ts   # 63/63 绿
# （DSH shell 默认 node 22.22.0 复跑 = 引擎 2 假失败，better-sqlite3 ABI 137≠127，环境依赖非缺陷）

# DS6 存量零回改
git diff 69d81c58^ 69d81c58 --name-only | grep -E "^src/routes/|^src/growth/|^src/middleware/|^src/agent/loop-handlers\.ts$|capability\.ts$"   # 零命中
git diff 69d81c58^ 69d81c58 -- src/loops/middle-evolution-engine.ts | grep -E "^-" | grep -v "^---"   # 恰 1 行（union 终止符）

# 类型安全
git diff 69d81c58^ 69d81c58 | grep -E "^\+" | grep -E "as any|as never|as unknown as"   # 零命中

# D556.json 终版合法性
python3 -c "import json; json.load(open('task-state/D556.json'))"   # valid
grep -nE "^(<<<<<<<|=======|>>>>>>>)" task-state/D556.json   # 零冲突标记

# CI job 级（GitHub API 匿名取证）
curl -s https://api.github.com/repos/tangbaobao520/SynovaAgent/commits/818dfccc/check-runs   # PR 分支十项全 success
curl -s https://api.github.com/repos/tangbaobao520/SynovaAgent/commits/69d81c58ae78ff20aecb8e50b494765730832671/check-runs   # merge: checker-review skipped + Pages build 预存 failure

# seed 通道偏离 ② 代码路径
sed -n '252,290p' src/middleware/auth.ts   # jwtAuthMiddleware（白名单跳过/DEV_MODE admin/401）
sed -n '86,104p' src/middleware/auth.ts    # isWhitelisted 零 /api/ga/*
sed -n '359,376p' src/middleware/auth.ts   # extractAuthFromRequest req.auth 优先于 legacy
grep -n "app.use(jwtAuthMiddleware)" src/server.ts   # L291 全局挂载
```

---

> 本报告只含报告与回填（task-state/D556.json audit 段），不修代码、不改 dev doc、不碰 scripts/audit/。发现的问题处置权归 CTO（创始人裁决 seed 通道 / evidence 落盘路径 / 产品文档更新）。
