# K3 审计报告：D489 GA 片2-B 初审 + D563/D564 复审（一批闭环）

> 审计员: Kimi K3（独立审计工作区，零上下文）| 2026-09-02
> 类型: 初审（D489）+ 复审（D563/D564）
> 派单: docs/synova/coordination/审计派单-20260829-D489-GA片2B.md（main 权威）+ CTO 对话派单 2026-09-02（报告命名/审计分支更新版）
> 审计分支: audit/k3-20260902-d489-slice2b（基于 origin/main = c21f10d9）
> 审计基线: origin/main c21f10d9（含 #313/#314 已合）
> 环境注记: **Node 24.19.0（nvm，better-sqlite3 ABI 137 实测可用）跑全部 Node 测试**；Node 22.22.0 下 better-sqlite3 ABI 127 vs 137 直接 ERR_DLOPEN_FAILED（D316 同型环境依赖，Node 22 结论不采信）。本机 macOS。GitHub API 匿名可达（repo 公开读）。

## 结论总表

| D# | 类型 | 合并点（PR） | 判定 | 核心发现 |
|---|---|---|---|---|
| D489 | 初审 | f55a6dbf（#299） | **CONDITIONAL PASS** | 六重点物理核实全过；CT-47 红态合并二次实证（API 证据）；as never 逃逸已由 D563 修复闭环 |
| D563 | 复审 | fc4424d5（#313） | **PASS** | 谓词窄化语义零变化、棘轮 8 与全仓一致、Architecture 全绿、PR 全 job 绿；P1×1 棘轮未入 CI（自注观察项升级确认） |
| D564 | 复审 | 5f7d69af（#314） | **PASS** | cp1252 根因修复实证、macOS 9/9 + 回归 13/13+6/6、canary 27、PR #314 Windows job 级 success（唯一有效验收通道） |

**预期兑现**: D489 初审六重点全部有 file:line 物理证据；D563/D564 复审重点（谓词语义零变化/降级等价/棘轮 8/cp1252/Windows job 级/platform-checklist 14/14/canary 27）全部独立复现。

---

## §1 D489（f55a6dbf, PR #299）—— GA 片2-B consult 路由落流初审

**物理 diff**: 6 文件 +607/-58：src/routes/diagnosis.ts +72、tests/routes/diagnosis-consult-events.test.ts +174（新建）、task-state/D489.json +28（新建）+ brief/dev-doc/bypass.log。**launcher 与 session-store 零改动**（`git diff f55a6dbf^..f55a6dbf --stat -- src/agent/diagnosis-launcher.ts src/store/session-store.ts` = 空）——「复用 D487/D500，无内联复制」声称成立。

### ① 落流接线（dev doc DS1/§5）→ ✅ 属实

- 命令: `grep -n "new DiagnosisLauncher" src/routes/diagnosis.ts`
- 断言: 命中 ≥1 生产调用点
- 实测: **L217** `const launcher = new DiagnosisLauncher(engineCtx, engine);`（生产 consult 路径）。runConsultation 仅存于手工包装 `engine`（L167-168，透传第 3 参 undefined 第 4 参 onEvent，dev doc §3.2 步骤 2 声明的形态）——生产路径经 launcher.startDiagnosis（L279-283），不再直接调引擎。

### ② 会话装配（dev doc DS2 + D487 空 id 桶防线）→ ✅ 属实

- 实测（main 现态，含 D563 修复）: `orchestrationDb` 读取 L175 → 谓词校验 L183 → `new SessionStore(orchestrationDb)` L187 → `createSession(teamId).id` L189（非空 sessionId，事件不落 '' 桶）→ 构造失败 catch L190-194 `log.warn` + `sessionStore=undefined; sessionId=''` 降级继续（铁律 24/31）。
- f55a6dbf 原版为 `new SessionStore(orchestrationDb as never)`（**D489 引入的 as never 逃逸**，见 §1 发现 A）。

### ③ 行为差异四声明（dev doc §3.2）→ ✅ 逐条属实

| 声明 | 实现证据（diagnosis-launcher.ts） | 判定 |
|---|---|---|
| concerns 经 slice(0,200) | L179 `m.content.slice(0, 200)` | ✅ 属实（launcher 既有行为，与 ConversationEngine 路径一致） |
| SSE 首条多 launcher 初始 phase_started(1) | L124 `persistingOnEvent({ type: 'phase_started', phase: 1, ... })`（引擎运行前发射，双写落流 + 推 SSE） | ✅ 属实 |
| ctx.provider 在场触发 background-review（fire-and-forget） | L309-313 `if (this.ctx.provider) { import(...).then(...).catch(log.warn) }`；consult 路由传 provider（L201）→ **实际触发**（测试日志实证 `services/background-review ... this.provider.chat is not a function` → .catch 降级） | ✅ 属实 |
| report 事件只落库不推 SSE | L185-191 直接调 `persistEvent('diagnosis_report', ...)`（非 persistingOnEvent，不触 onEvent） | ✅ 属实 |

### ④ 测试质量（dev doc §4/DS3）→ ✅ 4/4 非空壳 + red 先行独立复现

- 结构: 4 个 it 全有 expect（tests/routes/diagnosis-consult-events.test.ts L111-173），覆盖正常（①事件落流 + 空 id 桶防线）、SSE 透传（②）、无 db 降级（③）、report 缓存零回归（④）。走真实 express 路由（铁律 12，mock 引擎工厂/provider/config 保密闭）。
- green: `npx vitest run tests/routes/diagnosis-consult-events.test.ts`（Node 24）= **4 passed (4)**。
- **red 独立复现**（未采信声称）: 隔离 worktree @ f55a6dbf^ + 仅取测试文件 → `SqliteError: no such table: agent_sessions`，**1 failed | 3 passed**——与 task-state/commit message 声称的 red 完全一致（现状直建引擎零事件零会话）。

### ⑤ SSE 透传 / post-processing / report 缓存零回归（dev doc DS4）→ ✅

- 实测: D487 回归 `tests/agent/diagnosis-session-events.test.ts` = **5 passed**；consult 路由尾段（post-processing L292-325、cacheCompletedReport L355-361、sseClose L362）在 diff 中零改动；report 缓存由测试④覆盖（GET /consult/:id/report 200 + 载荷一致）。
- tsc: `npx tsc --noEmit --pretty false | grep -cE "error TS"` = **28**（基线 28=28 零新增）。

### ⑥ 北星三问（PRODUCT-BRIEF.md 原文对照）→ ✅ 对齐

- 问 1: PRODUCT-BRIEF §三.1「FDE 按需诊断」是两条主线之一；D489 让 GA 诊断过程可回放（D394 片2「交付物可自证」信任层），服务 FDE/企业主 → ✅
- 问 2: D489 = D394 片2-B（片2-A D487 已合），朝 GA 事件化终态走一步，非横向加料 → ✅
- 问 3: consult 行为（SSE/报告/降级）零变化，仅增加落流可回放能力——仍是驻扎企业主动诊断的 Agent，不变味 → ✅

### D489 发现

- **发现 A（P1，已闭环）**: D489 引入 `as never` 类型逃逸（f55a6dbf 版 L181 `new SessionStore(orchestrationDb as never)`，另 L411 同型存量）。CT-46 门禁在 PR #299 CI 上正确拦截（Iron Laws failure），但 **CT-47 第二次实证**：API 物理证据 `merged_at 14:08:42Z` 早于 Iron Laws failure `completed_at 14:10:18Z`（检查运行中即合并）——门禁立功、合并通道失守。**D563 已修复**（main 现态 diagnosis.ts 零 as never，见 §2）。归因: control-tower（合并门禁缺失，升级创始人已由台账 L98 登记）。
- **发现 B（P2 观察）**: `} as EngineContext;`（diagnosis.ts L216）整对象断言——非棘轮三族（as never/as any/as unknown as），ConversationEngine L448 同型既有模式，dev doc §3.2 步骤 3 已如实声明。不阻断。归因: implement（建议后续改用显式字段装配）。
- **L4 防线缺口**: D489 的 as never 本该被「CI 权威阻断」拦住（组 1 正确红了），但 PR 合并通道无 required checks 硬门禁（pre-commit/pre-push 管不到 GitHub API/网页 squash 合并）→ CT-47 系统性失效第二次。缺: branch protection required checks 或 merge 前 check-runs 校验脚本。归因: control-tower。

---

## §2 D563（fc4424d5, PR #313）—— diagnosis.ts 双处 as never 窄化复审

**物理 diff**: 8 文件 +160/-18：src/routes/diagnosis.ts、src/store/session-store.ts +24（isSqliteDatabase 导出）、packages/test-kit/tests/architecture/05-as-any-audit.test.ts（基线 9→8）、tests/control-tower/platform-checklist.test.sh +22（三联缺陷修）、brief ×2/task-state/bypass.log。

### 复审① isSqliteDatabase 移 L5 后诊断语义零变化 → ✅

- 谓词实现: `src/store/session-store.ts` L40-44——`typeof v !== 'object' || v === null → false` + prepare/exec/pragma 三方法探测（含 JSDoc 契约，铁律 47）。L42 用 `as { prepare?: unknown; ... }` 内联类型式（AGENTS 铁律 38 认可替代式，非逃逸三族）。
- consult（diagnosis.ts L183-189）: 谓词失败 → throw TypeError → 既有 catch（L190-194）log.warn + 降级继续。resume（L419-424）: 谓词失败 → throw TypeError → catch（L432-436）log.warn + checkpoint=null。
- **降级路径等价论证**: 原代码（as never 版）对 undefined/非句柄传构造器 → initSchema 调 db.exec 必抛 TypeError → 同 catch。谓词只是把「构造器抛」前置为「显式抛」，两路径收敛到同一 catch 降级——语义零变化 ✅（resume 注释 L416-418 同步说明）。
- 架构位: 谓词归 L5（数据库驱动类型只归 L5），diagnosis.ts 无 better-sqlite3 字样（`grep better-sqlite3 src/routes/diagnosis.ts` = 0 命中），L1 经既有动态 import 通道解构（L182/L419）。

### 复审② 降级路径等价 → ✅（见上，consult + resume 双路径 catch 均保留）

### 复审③ 棘轮基线 8 与全仓一致 → ✅

- 基线: `packages/test-kit/tests/architecture/05-as-any-audit.test.ts` L35 `'as never': 8`（注释完整记录 CT-47 击穿 9→10→清理→8 过程）。
- 独立口径复核: `grep -rn "as never" src/` = **8**（全存量: knowledge-agent.ts:292、bootstrap.ts:756/1143、im-inbound.ts:183、server.ts:229-232）；packages/ 非 .test/.spec/.d.ts 零命中（9 处全在排除范围）；**diagnosis.ts 零命中**。
- 棘轮实测: `npx vitest run tests/architecture/05-as-any-audit.test.ts`（packages/test-kit 自身 config）= **6 passed**。
- `grep -n "as never\|as any\|as unknown as" src/routes/diagnosis.ts` = **exit 1 零命中**（D563 Done 标准 1 实测）。

### 复审④ Architecture Check → ✅ 全绿

- `bash scripts/check-architecture.sh` = L1→L3/L1→L4/L1→L5/L2→L4/L3→L5 全部 ✅（1d: L1→L5 无直接数据库操作——check-architecture.sh L67-69 扫描 better-sqlite3/.prepare/db.run/db.exec 字样，diagnosis.ts 零命中）。

### 复审⑤ platform-checklist 三联缺陷修复 → ✅ 14/14

- `bash tests/control-tower/platform-checklist.test.sh` = **14 通过, 0 失败**（实测）。注: 此文件的实际修复在 D563 提交内（fc4424d5 +22）；D564 与 D563 并行同修、合并后文件一致（`git diff fc4424d5..5f7d69af -- platform-checklist.test.sh` = 空），最终单一正确版本。

### 复审⑥ CI job 级 → ✅ 全绿

- GitHub API check-runs @ head 2ea9c87b: **10/10 job success**（TypeScript+Lint+Iron Laws、Vitest 1/2+2/2、Architecture、Control Tower ubuntu+windows、Integration、Checker、npm audit、Golden Case）。

### D563 发现

- **发现 C（P1，新确认）**: **棘轮基线测试未入 CI**。根 vitest.config.ts include 仅 `./tests/**/*.test.ts`（L26-27），不含 packages/test-kit/tests/；ci.yml test job（L60-99）在根目录跑 vitest、architecture job（L101-112）只跑 `tests/architecture/`。→ 05-as-any-audit 棘轮只在本地执行，CI 权威通道不覆盖存量逃逸检测（组 1 仅拦 diff 新增）。D563 task-state notes 已自注「疑 M3 家族同型风险」——**本审计确认成立**。命中 M3「机制建成未接线」：棘轮机制建成但未接线到 CI 执行通道。归因: control-tower。防再犯（M3 类强化）: 将 packages/test-kit vitest 纳入 CI（新增 step 或并入 architecture job），或用 check-canary-drift 同型机制把棘轮文件列入 CI 清单。
- **P2 观察**: D563 第 7 轮返工范围含 platform-checklist 修复（CTO 授权「全修三件」，台账登记）——范围扩展有授权，合规。

---

## §3 D564（5f7d69af, PR #314）—— incident-loop Windows 兼容修复复审

**物理 diff**: 10 文件：scripts/control-tower/incident-loop.py、scripts/hooks/hook-git-detect.sh、.github/workflows/ci.yml（incident-loop 回列 canary）、tests/control-tower/incident-loop.test.sh（+4c 断言 + 诊断输出）、memory Note + brief ×3/task-state/bypass.log。

### 复审① cp1252 解码修复（encoding+errors 参数）→ ✅ 根因正确

- 代码: `scripts/control-tower/incident-loop.py` L307-312 `subprocess.run(..., text=True, encoding="utf-8", errors="replace", env=_bash_env(bash))`——verify() 显式 UTF-8 解码（L302-306 注释完整记录根因链）。
- **机制实证**（本地 python3）: `b'\xe7\xa6\x81'.decode('cp1252')` → `UnicodeDecodeError: 'charmap' codec can't decode byte 0x81`；`decode('utf-8')` = 「禁」；`decode('cp1252', errors='replace')` 不抛。→ 根因链物理成立（Windows locale 编码解码 hook UTF-8 输出 → UnicodeDecodeError → except → degraded → verify 误报 open）。
- macOS/Linux 零行为变化论证: locale 本就 UTF-8，显式 encoding="utf-8" 与默认一致；errors="replace" 仅在极端字节时兜底。

### 复审② SYNO_PYTHON 注入（独立故障，第 2 轮修）→ ✅

- 注入端: incident-loop.py L117-118 `env["SYNO_PYTHON"] = sys.executable`（POSIX/nt 单一语义）。
- 消费端: `scripts/hooks/hook-git-detect.sh` L37-41 `PY_BIN="${SYNO_PYTHON:-}"` 优先 + 回落 `command -v python3`（D312 原行为向后兼容）。
- 回归: `bash tests/control-tower/hook-git-detect.test.sh` = **13 通过, 0 失败**；`ban-stash.test.sh` = **6 通过, 0 失败**。

### 复审③ 4b/4c 断言目标与平台适配 → ✅

- 4b（tests/control-tower/incident-loop.test.sh L72-87）: 受限 PATH 按平台构造（win: Git usr/bin——bash 可达 python3 不在；mac: `/c/Windows/system32:/c/Windows` 原值，D561 语义保留），断言目标不变（verify 仍 closed）。
- 4c（L90-107）: SYNO_PYTHON 注入下 PATH 无 python3 仍拦 stash——**新增回归断言**（8→9），双平台确定性。
- 诊断输出: L27-29 assert_contains 第 4 参 + diag_out（RESTRICTED_PATH/NO_PY3_BIN/PYBIN/bash 定位值 + OUT 压单行）——真 Win 若仍红可经 ::error annotation tail-8 定位（第 4/6 轮返工产物）。

### 复审④ macOS 语义零变化 + 本地全绿 → ✅

- `bash tests/control-tower/incident-loop.test.sh` = **9 通过, 0 失败**（原 8 断言零变化 + 4c 新增；fail() 含 FAIL_NAMES 汇总行「失败断言: 无」）。
- platform-checklist 14/14（§2⑤同）+ tsc 28=28 + `python3 -m py_compile` 通过（task-state 声称，代码可读时确认语法无破坏）。

### 复审⑤ canary 27 → ✅

- ci.yml L139-165 canary 清单 = **27 项**（verify-parallel → verify-parallel-ci），incident-loop 在 L158（g12-day-window 之后、check-canary-drift 之前 = 18ae1b80 原位，26→27）。
- check-canary-drift.sh 告警 57 个测试不在清单（K3 P2-4 全量密封化已知待办，非本批回归）。

### 复审⑥ CI Windows job 级（唯一有效验收通道）→ ✅

- GitHub API check-runs @ head f710527a: **10/10 job success**——含 **Control Tower Gate Tests (windows-latest) success**（job 级，非整体 run 结论）。D564 修复的唯一有效验收通道 = CI Windows job，物理确认通过。
- PR #314 base = fc4424d5（D563 先合 #313 → D564 后合 #314，串行无冲突）。

### D564 发现

- **P2 记录**: `hook-git-guard.sh` 内部 python3（窗口标记写入）在坏 python3 环境仍 fail-open degraded——task-state notes 已如实声明「本批不改，同型根因记入 Note 后续」。归因: implement（后续 FIX 任务）。
- **P2 记录**: D564 task-state evidence 写「③ platform-checklist.test.sh 存量三联缺陷修」——实际该文件由 D563 提交修复（5f7d69af 相对 parent 零改动该文件）。两任务并行独立发现并修复同一缺陷，最终合并内容一致（14/14 实测通过），功能无问题；表述精确性瑕疵记录。归因: devdoc（task-state 证据表述）。

---

## §4 L4 防线缺口收割（每任务必答）

**D489（as never 逃逸入 main）——本该拦住它的防线？**
→ CI Iron Laws 组 1 已正确拦截（PR #299 failure），**缺口在合并通道**：GitHub merge（API/网页 squash）不执行 required checks 硬门禁，pre-commit/pre-push 本地 hook 管不到远程合并。CT-47 同类第二次（D537 首次）→ 防线系统性失效，须创始人裁决（branch protection required checks / merge 前 check-runs 校验脚本 / 合并权回收，台账 L98 候选修法已列）。

**D563（棘轮未入 CI）——本该拦住它的防线？**
→ canary 机制（check-canary-drift.sh）本可把「门禁类测试是否入 CI」纳入告警，但只告警 tests/control-tower/ 清单、不覆盖 packages/test-kit vitest 套件；CI Vitest job 只跑根 include。缺: test-kit 套件（含棘轮）的 CI 接线。命中 M3「机制建成未接线」——强化 M3 类已有防线（接线要求扩展：门禁测试自身须接入 CI 权威通道）。

**D564（Win 兼容两独立故障）——本该拦住它的防线？**
→ canary 首次纳入 Windows runner（PR #305）即暴露「macOS 8/8 单平台口径」——防线是**矩阵双平台**（D520 已建，立功）；缺口是**真 Win 语义只能在 CI 复现**（本地 macOS 无法模拟 locale 编码），故 CTO 合并前查 job 级 windows-latest success 是唯一验收通道——本批已确认走通（PR #314 Windows job success）。此防线无缺口，机制本身正确。

---

## §5 归因与分级汇总

| # | 级别 | 发现 | 归因 | 状态 |
|---|---|---|---|---|
| A | P1 | D489 as never 逃逸（已修）→ CT-47 红态合并二次实证 | control-tower | 已闭环（D563）+ 治理升级待创始人 |
| B | P2 | `as EngineContext` 整对象断言（L216，非棘轮三族） | implement | 观察 |
| C | P1 | 棘轮基线测试未入 CI（M3 家族） | control-tower | 新发现，排 FIX 队列 |
| D | P2 | hook-git-guard.sh python3 fail-open 边界 | implement | 记录跟进 |
| E | P2 | D564 task-state 对 platform-checklist 修复归属表述不精确（实际 D563 修） | devdoc | 记录 |

**无 P0。**

---

## §6 三 task-state audit 段回填（随本报告同批）

- D489.json: verdict = CONDITIONAL PASS（六重点通过；CT-47 事件 + as never 已由 D563 闭环；条件=审计时 main 零 as never 已满足）
- D563.json: verdict = PASS（复审重点全闭合；P1×1 棘轮未入 CI 转 FIX 队列）
- D564.json: verdict = PASS（复审重点全闭合；P2×2 记录）

报告路径: docs/synova/audit-reports/2026-09-02-D489-D563-D564.md
