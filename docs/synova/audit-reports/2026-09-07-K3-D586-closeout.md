# K3 独立审计报告 — D586 LLM 稳定错误码 taxonomy 收敛对齐（DSH 借鉴卡 B-01）

> 审计员：Kimi K3（独立会话，零上下文）
> 协议：AUDIT-PROTOCOL.md v1.1（L1-L4 四层 15 项清单）
> 审计基线：origin/main @ 56c4dd1d（D586 squash 合并入 main，PR #400）
> 日期：2026-09-07
> 结论：**PASS**（无 P0 阻断；P1×1 + P2×3 建议修复，随附）

---

## 0. 结论摘要

| 项 | 判定 |
|---|---|
| 总判定 | **PASS** |
| P0 阻断 | 0 |
| P1 建议修复 | 1（task-state/D586.json 缺失） |
| P2 可选 | 3（写集计数 / INTERNAL retryable 语义缝 / chat catch 无 log） |
| 北星对齐 | 对齐（三问全过） |
| CI 门禁 | 主质量门全绿（Pages build 失败为无关 infra） |
| 写集漂移 | 零越界（3 源文件 + brief 精确，bypass.log 为 auto 工件） |

**核心判定依据**：交付方声称的 3 个 DSH 范式（归一化边界 `normalizeLlmFailure` / 合一 detail 正则分类 `isContextWindowExceededError`/`isQuotaExceededError` / canonical 词汇表对齐 `EMPTY_RESPONSE`）全部物理落地且与真实 DSH 源码（`@deepseek-ai/dsh-llm@0.1.2-rc.1`）锚点逐字核验一致；22 用例实测全绿；tsc 28 基线零新增；`as any`/`@deepseek-ai` 双零；接线真实（非 import-only）。无重复造轮子（S-14）审计成立——`DiagnosticAgentError` 消费方接口不变。

---

## 1. 运行环境注记（先行，D316 教训）

- 审计机：Mac（darwin-arm64），Node **v22.22.0**，vitest 4.1.8。
- 审计工作区：隔离 clone `/Users/wane/Synova-k3独立审计`，当前分支 `audit/k3-20260906-d579-d582`（HEAD 3ed2f24，早于 D586）。
- **D586 代码不在工作区 HEAD**，故创建 detached worktree `/tmp/d586-audit-wt @ 56c4dd1d`，symlink node_modules 后实测（读操作，未污染主工作区）。
- DSH 锚点核验源：本机 DSH 运行时 checkout `.../dependencies/dsh/node_modules/@deepseek-ai/dsh-llm@**0.1.2-rc.1**`（新于 spec 引用的 Win 0.1.1-rc.2，锚点未漂移，见 §3.1）。
- 注：本审计所引 `file:line` 均对 56c4dd1d（= origin/main 该时点）的物理文件。

---

## 2. 材料收集（7 项自收集）

| # | 材料 | 来源 | 状态 |
|---|---|---|---|
| 1 | 任务提交集 | `git log --all --grep=D586`：56c4dd1d（squash merge）/ da9b37db（dev doc）/ 43fe700e（dev doc 初版） | ✅ 三提交映射完整 |
| 2 | Git diff | `git show 56c4dd1d`：5 文件 +562/-5 | ✅ |
| 3 | Dev doc | `docs/plans/codex/implementation/SYNOVA-IMPL-D586-llm-error-taxonomy-align-20260907.md` | ✅ |
| 4 | Task brief | `.claude/task-briefs/2026-09-07-D586-win-llm-error-taxonomy.md` | ✅ |
| 5 | AGENTS.md + PRODUCT-BRIEF.md | 项目根 + `.claude/PRODUCT-BRIEF.md` | ✅ |
| 6 | 审计基线 | tsc --noEmit = 28（恒等）；`as any`/`as never`/`as unknown as` 扫描 | ✅ |
| 7 | 执行证据 | GitHub API check-runs（19 项）+ bypass.log | ✅ |

---

## 3. L1 代码审计 — 15 项逐项

### 3.1 锚点核验（先行，本任务借鉴真实性关键）

真实 DSH 源码 `@deepseek-ai/dsh-llm@0.1.2-rc.1/lib/types/error.js` 四 canonical 码逐字核验：

| DSH 锚点 | 行号 | 逐字内容 | Synova 对齐 |
|---|---|---|---|
| `CONTEXT_WINDOW_EXCEEDED_CODE` | error.js:22 | `'CONTEXT_WINDOW_EXCEEDED'` | → `CONTEXT_OVERFLOW`（既有码，types.ts 注释 L643） |
| `QUOTA_EXCEEDED_CODE` | error.js:24 | `'QUOTA'` | → `BILLING_EXCEEDED`（既有码） |
| `EMPTY_RESPONSE_CODE` | error.js:34 | `'EMPTY_RESPONSE'` | → 新增 `EMPTY_RESPONSE`（types.ts:50） |
| `INVALID_CREDENTIAL_CODE` | error.js:42 | `'INVALID_CREDENTIAL'` | → 不新增码，走 `AUTH_FAILED`+轮换（base.ts:133） |

**判定：逐字命中，未漂移**。spec 声称「四 canonical 码 L22-42 逐字命中」属实。`normalizeLlmFailure` 的 Synova 实现（types.ts:744）与 DSH `adapter-failure.js:13` 结构一致：跨包拷贝 `carried.code === ownErrorCode(error)` 才信任，否则 `{message, code: 自有类码 or 'UNKNOWN'}`——Synova 把 `harnessErrorCode`（DSH 用 `HarnessError`）正确替换为 `DiagnosticAgentError` 判定（types.ts:752），这是自研适配而非拷贝（零 `@deepseek-ai` 依赖，G1 满足）。

### 3.2 15 项清单

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| 1 | 接口真实性 | ✅ | `normalizeLlmFailure` types.ts:744 / `isContextWindowExceededError` :774 / `isQuotaExceededError` :788 / `finalizeAdapterFailure` base.ts:36 全部存在 |
| 2 | 文件路径存在性 | ✅ | 3 写集文件均物理存在（worktree @ 56c4dd1d） |
| 3 | Edge ID 有效性 | N/A | 本任务无 E-XX 边 ID（纯 LLM 底座，非本体图） |
| 4 | 架构边界 | ✅ | base.ts 仅 import `../errors/types`（L3 内部相邻，无跨层）；`../llm/circuit-breaker` 既有 |
| 5 | 数据流完整性 | ✅ | `normalizeLlmFailure(err)` base.ts:39 → `finalizeAdapterFailure` → `DiagnosticAgentError` 抛出于 chat/stream 三边界 |
| 6 | 接线深度 | ✅ | `normalizeLlmFailure` 非仅 import——base.ts:39 真调用；`finalizeAdapterFailure` 三调用点 base.ts:237（chat）/248（流式错误检查）/258（stream catch） |
| 7 | 降级诚实性 | ⚠️ P2 | stream catch 有 `log.warn`（base.ts:256）；chat catch（base.ts:236）`recordFailure()` 后 re-throw 无 log.warn（非空吞，但无日志）——**存量模式，非 D586 引入** |
| 8 | 测试契约 | ✅ | `normalizeLlmFailure`/两个 `isXxx` JSDoc 契约齐全（输入/输出/降级）；铁律 47 满足 |
| 9 | 测试覆盖 | ✅ | 22 用例，正常/降级/边界三路径，每例 ≥3 expect；实测 22/22 绿 |
| 10 | 文件驱动 | N/A | 无新增硬编码哨兵/专家类型 |
| 11 | 自我报告交叉对比 | ✅ | PR #400 body 的 DS1-DS5 全部独立复核为真（见 §5） |
| 12 | dev doc 偏离审计 | ⚠️ P2 | §4 写集表头「2 修改 + 0 新建」实为「2 修改 + 1 新建」（测试文件为新建）；文件清单本身零越界 |
| 13 | 控制塔执行审计 | ✅ | CI job 级 19 项：主质量门全绿；bypass.log 3 条均为「pre-commit PASS」非 bypass（见 §4） |
| 14 | 版本编排合规 | ✅ | commit message 无 `Vx.y.z` 幻影；无 VERSION.md 变更（feat 无需 bump）；CT-35 不触发 |
| 15 | 北星对齐 | ✅ | 三问全过（见 §6） |

---

## 4. L3 执行审计（CI job 级 + bypass 证据）

### 4.1 CI check-runs（merge commit 56c4dd1d，19 项，GitHub API 物理证据）

| 检查 | 结论 | 归属 workflow |
|---|---|---|
| TypeScript + Lint + Iron Laws | ✅ success | ci.yml（主质量门，含 tsc + 铁律） |
| Vitest (1/2) / (2/2) | ✅ success ×2 | ci.yml |
| Architecture Check | ✅ success | ci.yml |
| Integration Contract Check | ✅ success | ci.yml |
| Control Tower Gate Tests (ubuntu/windows) | ✅ success ×2 | ci.yml |
| Test-Kit Architecture Tests (ubuntu/windows) | ✅ success ×2 | ci.yml |
| npm audit | ✅ success | ci.yml |
| Golden Case F1 Gate | ✅ success | ci.yml |
| Checker Review (maker/checker) | ⏭ skipped | ci.yml（预期跳过） |
| macos (dmg + zip) / windows (nsis) | ✅ success ×2 | desktop-build |
| **build** | ❌ **failure** | **GitHub Pages build（`dynamic/pages/pages-build-deployment`，GitHub 托管，非仓库 workflow）** |
| deploy | ⏭ skipped | Pages（build 失败故跳过） |

**Pages build failure 判定**：经查 run 34089387522，该 `build` 失败属于 **GitHub Pages 部署**（`path: dynamic/pages/pages-build-deployment`，`event: dynamic`，`head_branch: main`），**不在仓库 4 个 workflow（ci/desktop-build/dashboard-auto/product-progress）之内**，是 GitHub 托管的 Pages 构建，与 D586 的 3 个纯 TS 源文件改动无关。主质量门（TS+Lint+Iron Laws / Vitest / Architecture / Contract / Control Tower / Test-Kit / npm audit / Golden Case）**全部 success**。**无门禁漏拦，无合并通道失守**（与 D489 的 CT-47 红态合并不同，此处主门全绿）。

### 4.2 bypass 证据

`.claude/bypass.log` 的 D586 3 条新条目（`git show 56c4dd1d -- .claude/bypass.log`）全部为：

```
2026-09-07T12:02:18 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=53881ba8...
2026-09-07T13:10:21 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=a45605eb...
2026-09-07T13:28:43 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=e2f07d91...
```

**判定**：均为「pre-commit PASS」登记（D521 auto-hook），**非 `--no-verify` bypass**。squash merge 中间 commit 的「chore: bypass COMMITTED 登记」措辞易误读，实际是 D521 机制登记 pre-commit **通过**，非绕过。spec §8「不用 --no-verify」自检属实。

---

## 5. 自我报告交叉对比（DS1-DS5 逐项复核）

| DS | 声称 | 独立复核 | 判定 |
|---|---|---|---|
| DS1 接线 | `grep normalizeLlmFailure src/providers/` → base.ts:23/30/39 | grep 命中 base.ts:23(import)/30(注释)/39(真调用)；`finalizeAdapterFailure` 三边界 237/248/258 | ✅ 属实 |
| DS2 零依赖 | `grep @deepseek-ai src/` = 0 | `grep -rn "@deepseek-ai" src/` = 0 | ✅ 属实 |
| DS3 red→green | red 21 failed\|1 passed → green 22 passed | green 独立实测 22/22；red 阶段为 TDD 自报（未独立回滚复现，注记） | ✅ green 属实；red 自报 |
| DS4 全绿+tsc | 25/25 全绿 + tsc 28 恒等 | 22(test)+3(providers)=25 实测全绿；`tsc --noEmit` = **28**，且 28 条错误**零命中** 3 写集文件 | ✅ 属实 |
| DS5 类型安全 | as any / as never / as unknown as = 0 | `git show 56c4dd1d -U0` 对 3 源文件 `+.*as (any|never|unknown as)` = **0** | ✅ 属实 |

补充：ErrorCode 由 23 码 → 24 码（+`EMPTY_RESPONSE`，types.ts:50），与 spec §2.2「23 码」基线一致；`EMPTY_RESPONSE` 已正确纳入 `isRetryable` 可重试集（types.ts:523）。

---

## 6. 跑偏第二道（north-star 三问）

对照 `.claude/PRODUCT-BRIEF.md` 原文机械判断：

1. **服务真实用户场景？** ✅ — LLM 是「8 专家并行分析」（§三.1）与「25 哨兵巡检」（§三.2）的底座；稳定错误码 taxonomy 使诊断/哨兵的 LLM 调用可正确重试/压缩/轮换/回退，服务 GA（§二）与企业主（§二）的真实诊断链路。非凭空造功能。
2. **更接近终态？** ✅ — L3 洞察层 provider 底座（§四 五层架构）的终态是「可靠、可路由、可重试的 LLM 调用层」；本次补齐 DSH 归一化边界 + 合一分类 + canonical 词汇表，是朝终态走一步（增量加固既有 `classifyApiError` 8 阶段流水线），非横向加无关物。
3. **产品变味？** ✅ 否 — 纯基础设施加固，Synova 仍是「驻扎企业、主动诊断、为增长服务」的 Agent，无 ChatBot/报表工具化漂移。

**裁决：对齐**。（注：spec 无显式北星 front-matter 锚定块——north-star-guard skill 的硬前提缺，属 dev-doc 流程 P2 观察，实质内容已对齐，不阻断。）

---

## 7. 发现清单（分级 + 归因）

| # | 级别 | 发现 | 证据 | 归因 |
|---|---|---|---|---|
| F1 | P1 | **task-state/D586.json 缺失**：交付 brief 声明「交付报告：task-state/D586.json」，但全仓库（含 origin/main、全部 remote 分支）零命中；状态机文件未落库（D587/D588 同缺，D589 已建） | `git ls-tree -r origin/main --name-only task-state/` 无 D586.json | **devdoc**（task-state 写回步骤未执行） |
| F2 | P2 | **spec §4 写集表计数不一致**：「2 修改 + 0 新建」实为「2 修改 + 1 新建」（测试文件 `tests/errors/llm-error-taxonomy.test.ts` 为新建，§5 已声明但 §4 头未计入） | spec §4 表头 vs 实际 commit 5 文件 | **devdoc**（M7 文档-实现漂移家族） |
| F3 | P2 | **INTERNAL fallback 语义缝**：`finalizeAdapterFailure` 未知失败回落 `code: INTERNAL, retryable: true`（base.ts:57-61），但 `isRetryable(ErrorCode.INTERNAL)` = false（types.ts 非可重试集）——`.retryable` 字段与 `isRetryable()` taxonomy 对 INTERNAL 不一致 | base.ts:57-61 vs types.ts `isRetryable` INTERNAL 分支 | **implement**（潜在语义缝，下游若用 `isRetryable(code)` 判断会读到 false） |
| F4 | P2 | **chat catch 无 log.warn**：base.ts:236 `catch` 内 `breaker.recordFailure()` 后 re-throw，无 log（非空吞但无日志）；stream catch（:256）有 log.warn | base.ts:236 vs :256 | **implement**（存量模式，非 D586 引入） |

---

## 8. L4 防线缺口收割

> 本次发现的问题（若有），控制塔哪一道防线本该拦住？为什么没拦住？缺什么？

| 发现 | 本该拦住的防线 | 为什么没拦住 | 缺口 |
|---|---|---|---|
| F1 task-state 缺失 | task-state 写回步骤（结果传递不靠人转达的状态机约定） | 状态机写回是「约定」非「门禁」——无 pre-commit/CI 物理检查「task-state/<D#>.json 存在」；Win 线（Claude Code）交付时未执行写回，主仓库合并时无门禁拦截 | **task-state 写回应门禁化**（或派单 SOP 显式校验）——与 K3审计清单追记「清单登记环节缺失，F1 教训同源」一致 |
| F2 写集计数漂移 | dev doc 写集表 vs 实际提交对账（S-6 回填 / 写集声称=实现+验收） | spec §4 表头「0 新建」与 §5「新增测试」自相矛盾，dev-doc 自检（第一道）未发现；K3 是第二道兜底 | dev-doc skill 的写集表应强制「修改 N + 新建 M」与文件清单逐行对账 |
| F3 retryable 语义缝 | 铁律 32（`.retryable` 与分类一致性）/ 契约优先（铁律 47） | `.retryable` 字段硬编码 vs `isRetryable()` taxonomy 两处口径未做一致性检查；无门禁检测「字段与函数对同一 code 判定相反」 | 可考虑门禁/契约检查「DiagnosticAgentError 构造处 retryable 与 isRetryable(code) 一致」（P2 可选，防再犯） |
| F4 chat catch 无 log | 铁律 24（catch 有 log.error/warn） | 存量模式（原 `throw err` 即无 log），D586 包装未顺手补齐；pre-commit 组 2 只查 empty catch，不查 re-throw catch 无 log | 铁律 24 门禁对「re-throw 型 catch 无 log」无覆盖（存量预警，非硬阻断） |

**M 类命中**：F1 → M4/M7 家族（执行证据链断裂/文档-实现漂移）已命中；F2 → M7 家族已命中；F3/F4 为本次新暴露的「语义缝」类，**不新建 M 类**（非跨任务复现模式，单点观察），归入既有铁律 32/24 防线强化方向。

---

## 9. 兑换建议（只出结论，兑换由 CTO 执行）

| 发现 | 建议 | 兑换方 |
|---|---|---|
| F1 | 本次审计写回时**新建** `task-state/D586.json`（audit 段 PASS）；派单 SOP 增加「task-state 写回」显式校验 | CTO |
| F2 | spec §4 表头修正为「2 修改 + 1 新建」；dev-doc skill 写集表对账规则强化 | CTO |
| F3 | 评估 `finalizeAdapterFailure` INTERNAL fallback 是否改 `retryable: isRetryable(ErrorCode.INTERNAL)`（=false）或同步 taxonomy 语义 | 产品/编码 |
| F4 | 存量预警，非本任务强制项；铁律 24 门禁可增 re-throw catch 无 log 预警 | 控制塔 |

---

## 10. 附：复核命令（可复跑）

```bash
# 1. 测试（22 用例）
git worktree add --detach /tmp/d586-audit-wt 56c4dd1d
ln -s "$(pwd)/node_modules" /tmp/d586-audit-wt/node_modules
cd /tmp/d586-audit-wt && npx vitest run tests/errors/llm-error-taxonomy.test.ts   # → 22 passed

# 2. tsc 基线
cd /tmp/d586-audit-wt && npx tsc --noEmit 2>&1 | grep -c "error TS"              # → 28

# 3. 接线
grep -rn "normalizeLlmFailure" src/providers/                                     # → base.ts:23/30/39

# 4. 零依赖
grep -rn "@deepseek-ai" src/ | wc -l                                              # → 0

# 5. 类型安全
git show 56c4dd1d -U0 -- src/errors/types.ts src/providers/base.ts tests/errors/llm-error-taxonomy.test.ts | grep -E "^\+.*as (any|never|unknown as)" | wc -l  # → 0
```
