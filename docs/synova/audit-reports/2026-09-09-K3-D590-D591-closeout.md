# K3 独立审计报告 — D590 + D591 合并批（对话 SSE 端点 + 桌面对话接线）

> 审计员: Kimi K3（独立会话，零上下文）| 日期: 2026-09-09
> 协议: AUDIT-PROTOCOL.md v1.1（L1-L4 四层 + 15 项清单）
> 基线: origin/main @ 1171b07c（D590 b54a0fb6 + D591 bdfad5c0 + D592 143a9924 之后；#440 状态回填已含）
> 独立复跑环境: MacBook Pro (darwin-arm64), node v22.22.0, vitest 4.1.8（与证据采集环境 /Users/wane/SynovaAgent 物理隔离）

## 结论

**D590: CONDITIONAL PASS** | **D591: CONDITIONAL PASS** | **批次: CONDITIONAL PASS（P1×3 + P2×8，无 P0）**

核心交付物理成立：对话 SSE 端点/持久化/鉴权三件齐（D590）、桌面真流式/回声/恢复接线（D591），
独立复跑 51/51 全绿（conversations 13/13 + use-streaming-conversation 21/21 + contract 17/17），
接线 grep 全命中、删除复核全零、`as any/as never/as unknown as` 零新增、L2/L5 零修改、北星三问全对齐。

条件：①D590 squash 夹带 7 份骨架 brief 入库并在本机 CI-strict 模拟中实证毒化门禁（P1-1，须清）；
②K3审计清单未收录 D590-D592（P1-3，CTO 台账维护）；③其余 P2 见清单。

已知披露全部核实属实（L2 fromState 缺陷、LLM 替身、410 上移、三微调 D333 记录）——不重复审
D592 已确认的 P1-2（toolLoop 脱钩），本报告只做交叉引用。

---

## L1 代码审计（15 项清单）

| # | 项 | 判定 | 证据 |
|---|-----|------|------|
| 1 | 接口真实性 | ✅ | 端点/帧协议/持久化时序全部存在：conversations.ts:323-331（两路由）、web-adapter.ts:88-138（sendFrame/七方法/close）、conversations.ts:246-269（open/token/agent_message/end）、:249-257（持久化五步）。engine 挂点实测：conversation-engine.ts:778 showAgentMessage（phaseComplete 轮）|
| 2 | 文件路径存在性 | ✅ | 写集 11 文件全部存在（b54a0fb6 diff 物理对账）；2 删除文件已不在 src/ tests/ |
| 3 | Edge ID 有效性 | N/A | 本批无 Edge ID 变更 |
| 4 | 架构边界 | ✅ | L1→L2 仅公开 API（conversations.ts:36-43）；L5 仅动态 import（:98，D563 先例同款，audit-check 同型噪音 flag 见 P2-7）；L2/L5 文件零触碰（git log 证明 conversation-engine.ts / session-store.ts 无 D590/D591 提交）|
| 5 | 数据流完整性 | ✅ | 用户消息前置落库（:249）→ 引擎流式（:252）→ assistant 落库+phase+快照（:255-257）→ 下轮 loadState/缓存复用（:224-240）——cli.ts 权威模板语义完整；SSE 交付 ≡ 事件流投影被用例⑥锁定 |
| 6 | 接线深度 | ✅ | spec §8 表逐条 grep 命中：conversationsRoutes server.ts:43/356；WebViewAdapter conversations.ts:43/213；setViewAdapter :243；emitDiagnosisEvent :294、setOnClosed :215；白名单 auth.ts:110-112；GONE server.ts:95；D591 侧 mode:'diagnosis' CenterPanel.tsx:86、restoreMessages :39、useStreaming() :65、applyQuickAction :172 |
| 7 | 降级诚实性 | ✅ | 503 fail-closed（conversations.ts:92-107）；双写降级流内 error 帧（:260-263）+ log.error；断线停写但仍完整落库（:214-219, web-adapter.ts:51-59）；前端 404/网络恢复降级 console.warn+清锚（CenterPanel.tsx:48-55）；error 帧不覆盖 end 的 phase（useStreaming.ts:170-175）|
| 8 | 测试契约 | ✅ | JSDoc @input/@output/@degraded 齐（conversations.ts:11-23、web-adapter.ts:9-15、useStreaming.ts:48-60、conversation-store.ts:64-90、CenterPanel.tsx:18-30）|
| 9 | 测试覆盖 | ✅ | 正常/降级/边界/并发/回归全覆盖：D590 14 用例（⑫在 contract 套件）+ D591 21 用例；red 证据真实（red 日志：Cannot find module / 21/21 failed）；非空壳（每用例 expect 断言，含 SQLite trigger 注入降级用例⑭）|
| 10 | 文件驱动 | ✅ | 无新增硬编码类型；WELCOME_QUICK_ACTIONS/QUICK_ACTION_PREFILL 单源常量（WelcomeScreen.tsx:42-53），非硬编码分散 |
| 11 | 自我报告交叉对比 | ⚠️→P2-4 | 声称与实际大体一致；"25 次 flush"实为 24（P2-5）；test ②标题"loadState→fromState 生效"实际走会话缓存路径（P2-4）|
| 12 | dev doc 偏离审计 | ⚠️→P1-1/P2 | 写集漂移=8 份越界 brief 文件随 b54a0fb6 入库（P1-1）；三微调（DiagnosisLauncher 直连/缓存 cap 200/410 上移）全部有 D333 记录 + 代码注释 + 证据 README 诚实登记（conversations.ts:25-29/:54-66、server.ts:87-98）——偏离但如实；D591 偏差 D-1/D-4 已登记 |
| 13 | 控制塔执行审计 | ⚠️→P1-1 | 本机 CI-strict 模拟（SYNO_CI=1）group 6 硬阻断（骨架 brief 毒化，实证）；simulate-ci 密封 gate 12/12 过、iron-laws 同点失败；job 级 CI 结论无法从工作区核验（无 gh/API）——"CI 12/12"为声称，合并产物与 D547 门禁预期矛盾（见 L4）|
| 14 | 版本编排合规 | ✅ | D590/D591 零触碰 VERSION.md/version.log/tag——与仓库实践一致（功能 PR 不 bump，VERSION.md 最后一次 D579）；无未授权版本变更 |
| 15 | 并行合规 | ✅ | 任务序 D590→D591→D592 串行依赖，提交顺序物理一致；写集与 D593/D596 零交集（D591 仅 electron-renderer/src + tests/electron，src/ 零触碰实证）；CI verify-parallel 在岗；无 parallel-conflicts 记录 |

## DS 逐条核验

### D590（DS1-DS13）

| DS | 判定 | 证据 |
|----|------|------|
| DS1 red→green | ✅ | red 日志（Cannot find module）→ 独立复跑 13/13 全绿零 skip |
| DS2 端点上线 | ✅ | e2e-sse-full-stream.log.txt: 42 token 帧/135 行/open/agent_message/end 实测；curl 日志 410×9/GONE×2/重启持久化 ✓ |
| DS3 会话落库可查 | ✅ | e2e-session-readback-restart.log.txt: ok:true + session/messages 全轮次；事件流投影干净（user/assistant×1，引擎快照内 assistant 重复系已披露 L2 行为）|
| DS4 白名单 | ✅ | auth.ts:110-112 三前缀 + JSDoc 单机信任声明（:86-89）；README.md 安全模型节；用例③含对照组探针 401 证明中间件在岗 |
| DS5 断线不丢 | ✅ | 用例⑤独立复跑绿（已见 token 串 ⊆ 落库 content）|
| DS6 重建不变量 | ✅ | 用例⑥绿（SSE≡事件流投影≡fromState 快照往返）|
| DS7 忙锁/边界/降级 | ✅ | 用例⑦（409 后锁释放）/⑧（400/404 流前 JSON）/⑨（503 degraded:true）全绿 |
| DS8 upload-v2 下线 | ✅ | server.ts:89-98 精确 410 拦截（先于 JWT，裁决②语义完整）；ga-diagnosis.ts 410 止停轮询；用例⑩ 4 路径 410 + consult 零误伤 |
| DS9 删除复核 | ✅ | grep 物理复核：diagnosis-upload-v2 仅 4 处注释（3 处 spec 列明 + llm-config.test.ts:174 已披露）；test-http-route 零引用；messageBuffer 零 |
| DS10 chat.ts 修复 | ✅ | 4 处 log.warn→console.warn（diff 行 324/729/826/888）+ 头注释诚实化 |
| DS11 sse-contract 扩展 | ✅ | 4 新帧入 SSEEventType（sse-contract.ts:26-30）+ open 归约返回 sessionId（:141-148）；contract 17/17 全绿零回归 |
| DS12 接线验证 | ✅ | §8 表全命中（见清单 6）；写集域 tsc/as-any 零新增；全量 vitest 写集域 51/51 绿 |
| DS13 完成报告 | ⚠️ P2-8 | 决策记录/北星三句以 evidence README + spec §5.4 + 代码注释承载，未见独立完成报告文件；CTO 验收 7 项过（task-state 登记）|

### D591（DS1-DS10）

| DS | 判定 | 证据 |
|----|------|------|
| DS1 red→green | ✅ | red 日志 21/21 全红 → 独立复跑 21/21 全绿 |
| DS2 真流式 | ✅ | e2e-ds2-desktop-chat.log.txt: 24 次 flush 逐字累积（README 写 25，P2-5）；单元用例②锁逐字累积；agent_message 后清流式区（用例④）|
| DS3 提交回声对账 | ✅ | e2e 日志: currentSessionId=localStorage=sess_mtsdtn59_avj9；续轮 body 携带 sessionId（[fetch] 行）；用例③ |
| DS4 刷新恢复 | ✅ | e2e 日志: restoreLastSession restored 消息数=4；用例⑦四态（restored/degraded×2/empty）；服务端 SessionStore 读回 |
| DS5 向导后直达 | ✅ | 快速行动三按钮写 store（applyQuickAction）；CenterPanel 空态快速行动可达落点（D-1 偏差如实登记）|
| DS6 显式诊断入口保留 | ✅ | 用例⑧ diagnosis 模式回归（URL/payload 逐字节等价）；用例⑤诊断事件 flat 透传 |
| DS7 mock 清理 | ✅ | grep MOCK_* 零结果；app-store.ts:110 workspaces/conversations 空数组（诚实空态）|
| DS8 死代码删除 | ✅ | useConversation.ts 已删；grep 零结果（排除 useConversationStore）|
| DS9 接线验证 | ✅ | §8 表全命中；contract 17/17 零改动全绿；electron-renderer tsc exit 0（交付证据）+ 本机复跑套件绿 |
| DS10 完成报告 | ⚠️ P2-8 | 六项决策在 spec §5.4 + brief Q1c；北星三句在 spec front-matter；同无独立完成报告文件 |

---

## 分级发现

### P1（建议修复，排入队列）

**P1-1 — D590 squash 夹带 7 份骨架 brief 入库，门禁毒化已实证（归因: devdoc + control-tower）**
- 物理事实：b54a0fb6 diff 含 `.claude/task-briefs/2026-09-08-D591-L1-P1桌面端对话接线.md`、
  D593/D594/D595/D596/D597 五份、`2026-09-07-D587-stale重验批.md`——共 7 份含
  `认领: <agent>` / `<具体文件路径>` 占位符的骨架模板（D592 brief 为已填正常件），
  全部不在 D590 spec §5.1 写集（9+2）与 brief Q2 清单内。
- 毒化实证：本机 `SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh`
  → exit 1，group 6 plan-integrity 硬阻断：`❌ Q2 排除项缺少文件路径 … brief 第 25 行:
  - 不改 <具体文件路径，排除项必须含文件名>`；simulate-ci 同点失败（密封 gate 12/12 反全过，
  证明失败仅此一处）。resolver 规则 3（无认领回退）落点即中毒，非 docs PR 会被全局误拦
  → 制造 --no-verify 压力（D544/D546 同族，第三次复发）。
- 防线悖论：D547 骨架门禁（pre-commit-check.sh:860-869，2026-08-28 入）应拦住这批文件
  （占位符 grep 必然命中），但 b54a0fb6 仍入库。机理无法从工作区判定（CI job 级结论无 API
  可查）——两种可能：CI 见到但合并未等 CI 绿，或 CI 未见（PR 时序）。无论哪种，控制塔缺
  "主树零骨架"的合并后兜底扫描。
- 修复建议（归口 FIX 任务，K3 不代改）：①删除/回填 7 份骨架 brief；②主树侧补一次骨架扫描
  （并入既有 main-tree guard 模式，如 parallel-main-tree-occupancy.test.sh 同型），
  D547 门禁保持 commit-time 职责不变（防再犯=一条机制，非每任务一脚本）。

**P1-2 — L2 fromState 缺陷存续（交叉引用，不重复审）**
- D592 K3 已确认（P1-2）：fromState 重赋值脱钩 toolLoop（conversations.ts:58-63 自认）。
  本批复核确认交付方披露准确（evidence README 条目 4 + 测试⑥注释如实标注 L2 双 push 行为）。
  归属 Stage 3 修复后应重验 2-5。

**P1-3 — K3审计清单过期（归因: control-tower/CTO 台账维护）**
- `docs/synova/coordination/K3审计清单-20260822.md`（2026-08-29 版）不含 D590/D591/D592；
  本批按派单直接审。清单需更新（D590/D591 本报告 closeout、D592 已 audited）。

### P2（可选改进）

- **P2-1** D590 test ②标题声称"loadState→fromState 生效"——实际同进程第二轮走会话级引擎缓存
  （sessionEngines Map 命中），fromState 仅在用例⑥直测。标题措辞应改"缓存复用/状态续轮"
  （底层缺陷已归 P1-2，此为声称精确性问题）。
- **P2-2** conversations.ts:267 注释行号漂移（"conversation-engine.ts:734"实际 :778）。
- **P2-3** D591 evidence README"25 次 16ms flush"实际 24 次（log 末帧 flush #24）。
- **P2-4** ds2-driver.ts 的 host.resetStreaming 桩只 log 不清空累加器 → e2e 日志 streamingText
  跨轮拼接，初读似双内容 bug。生产实现正确（useStreaming.ts:167 清流式区 + 用例④断言空），
  纯证据桩瑕疵；建议桩补 `streamingAcc = ''`。
- **P2-5** audit-check.py 基线对 conversations.ts:98（L1→L5）/ :120（L1→L3）的 flag 与 D563
  先例 diagnosis.ts 同型（6 处既有 flag），属动态 import 模式噪音，非新违规类。
- **P2-6** web-adapter.ts:30 `export type SseFramePayload` 零外部 import（结构类型消费），
  可改非导出或由 conversations.ts 显式 import 消警告。
- **P2-7** 派单文案数字漂移：conversations.ts 实际 333 行（派单写 631）；conversations.test.ts
  本文件 13 it（派单写"30 用例"= 13+17 contract 组合）。仅文案，代码无涉。
- **P2-8** 两任务均无独立"完成报告"文件；DS13/DS10 的决策记录以 spec §5.4 + evidence README +
  代码注释承载（D333 判定"已登记"成立），但规范若要求单文件，后续任务宜固定载体。

---

## 北星裁决（第二道，对照 PRODUCT-BRIEF.md）

| 问 | 裁决 | 依据 |
|----|------|------|
| ① 服务真实用户场景吗 | ✅ 对齐 | 用户=GA/企业主（PRODUCT-BRIEF §二）；多轮访谈是"GA 按需诊断"的入口（§三第一系统），对话端点让"配置完即可对话"（D575 承诺）落地；桌面端为施工图既定（AGENTS.md 项目身份，切片 A/B/C 闭环后）|
| ② 更接近模块终态吗 | ✅ 对齐 | D590 终态=引擎接 HTTP+桌面+落库（L1 审计 §7 D590 行原文）；D591 终态=桌面消费四断点闭环；均为终态推进非横向 |
| ③ 产品变味吗 | ✅ 对齐 | 对话≠替代诊断：spec §5.4 决策 1 保留 consult 显式入口（两套系统并行，§三）；phaseComplete 桥自动转入诊断管线（conversations.ts:273-306）；哨兵/主动诊断未动。"Agent 不是 ChatBot"锚定块在两侧 spec 均显式引用 |

**裁决：对齐**（三问全过）。无跑偏发现。

---

## L4 防线缺口收割

**"本次发现的问题，哪一道防线本该拦住？为什么没拦住？缺什么？"**

- **问题**：7 份骨架 brief 第三次进入 main（D544→D546→本批 D590）。
- **本该拦住的防线**：D547 骨架门禁（pre-commit-check.sh:860-869，2026-08-28 已入）
  ——占位符 grep 在 STAGED_ALL 命中即硬阻断，本机实测该门禁逻辑有效（skeleton-gate 测试
  套件 12/12 过 + 手测命中）。
- **为什么没拦住**：门禁是 commit/PR-time 防线；合并产物证明它要么没见到这批文件
  （PR 时序/CI 未重跑），要么合并未以 CI 绿为前置（branch protection 缺口）。两者 K3 均
  无法从工作区判定——**控制塔缺"合并后主树兜底扫描"与"PR↔CI job 级证据链"**。
  连带发现：K3 的 job 级核验（协议 #13）在没有 gh/API 的工作区无法执行，只能依赖
  simulate-ci 近似——审计面同样受此缺口制约。
- **缺什么（防再犯，一类一机制，不新增 M 类）**：该错误类已有防线（D547 门禁），本次是
  既有防线的覆盖面缺口而非新错误模式 → **强化 D547 类**：主树侧加一次骨架扫描兜底
  （CI main-push job 或并入既有 main-tree guard 模式），机制数量不增加。
- **次生发现（预存债，非本批引入）**：pre-commit-check.sh:1135 `grep -oP` 在 macOS BSD grep
  下报 `invalid option -- P`×2（2026-08-24 b8e1eb20b 引入）——本机模拟输出已见，CI ubuntu
  不受影响；建议列入控制塔 compat 待办（CT-46 家族）。

## 归因汇总

| 发现 | 归因 | 防再犯动作 |
|------|------|-----------|
| P1-1 骨架 brief 入库 | devdoc（brief 文件卫生）+ control-tower（主树兜底缺失）| D547 类强化：主树骨架扫描（一条机制）|
| P1-2 L2 缺陷 | implement（D590 存量，已如实披露）| Stage 3 修复 + 重验 2-5（D592 已立项）|
| P1-3 清单过期 | control-tower（CTO 台账维护）| CTO 更新 K3审计清单 |
| P2-1/2/3/4 声称精度 | devdoc（evidence 文案）| 证据落盘前数字对账（dev-doc-delivery-self-review 已覆盖同类，继续执行）|
| P2-5/6 基线噪音 | audit（脚本 flag 口径）+ implement（导出面）| 可选收窄 |

## 运行环境注记（防环境依赖误判）

- 本机 macOS darwin-arm64 + node v22.22.0；证据由交付方在 /Users/wane/SynovaAgent
  （node v22.23.2 per runner 脚本）采集——两侧环境隔离，结果一致部分为强证据。
- D591 套件在审计工作区首跑失败（react-markdown 缺失）系 electron-renderer 独立包依赖
  未装，非代码缺陷；npm install（本地 cache 重定向）后 21/21 绿。
- 全量 vitest 本机 21 failed 均在写集域外（analytical-lens/expert-file-loader/zero-code/
  graphbridge-wiring/circular 等，环境/数据依赖），与交付证据披露的基线失败家族一致；
  layer2-judge 的 .env 差异本机不存在（无该 .env），故总数 21≠交付侧 57，双向均环境性。
- CI-strict 模拟失败仅一处（骨架 brief），已在 P1-1 归因；BSD grep -P 报错为预存 compat 债。

---

*报告完。修复后复审只审变更部分（P1-1 清理 + 主树扫描）。*
