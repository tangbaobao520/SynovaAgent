# K3 独立审计报告 — D593 + D602 合并批 closeout

> 审计员: Kimi K3（独立会话，零上下文）| 2026-09-09
> 基线: origin/main @ 26858bac（两任务均以 squash 合并入 main）
> 审计分支: audit/k3-20260909-d593-d602-closeout
> 协议: docs/synova/coordination/AUDIT-PROTOCOL.md v1.1（L1-L4 四层 + 15 项清单）
> 运行环境: Mac（审计工作区 /Users/wane/Synova-k3独立审计）| Node 22.22.0（better-sqlite3 ABI 127 匹配，D589/D580 教训已复核）| 测试独立实跑，非引用证据包

---

## 0. 结论总览（先读）

| 对象 | 结论 | 关键发现 |
|---|---|---|
| **D593** 报告落盘（#442，c7eb8e0d） | **FAIL** | **P0-1：squash 合并丢失核心实现**——冲突消解（fcd22c84）取 main 侧，diagnosis.ts 全部 D593 变更（consult 落盘 + GET 冷读 fallback + /api/diagnosis/reports 列表端点 + 注释诚实化，分支态 +202 行）与 task-state impl 段回填**未进入 main**；main 上独立复跑：进程内 reportId→404（预期 200）、持久化测试 25 用例 **10 failed**。**P0-2：夹带**——PR #442 把 6 个写集外文件带进 main（.codex/heartbeat.json、2 新+1 改 .synova-wt-* 标记补丁、D595 两文档、D599 note）——与前批 P0-1（#449 夹带 D603 内容）同族，12 小时内第二次 |
| **D602** 交互卡片+preload/IPC（#448，749c3b51） | **CONDITIONAL PASS**（维持前批） | 实现质量高：23/23 测试独立复跑全绿、IPC 三方源码级物理断言、DS4/DS5 真实后端证据、写集合并零漂移。结转 P1-1（spec 未入库，#453 已登记"brief 即 spec 载体"）未关闭；本批新增 P2×4 微瑕 |

**分级计数：P0 ×2 ｜ P1 ×4 ｜ P2 ×6**
（P0/P1 全部属 D593；D602 零新增 P0/P1。）

**一句话**: D593 的实现是完整的（分支 feat/d593-report-persistence-p2 上 DS1-DS9 真实达成，DS2 物理证据真实），但**合并通道把核心一半吞掉了**——main 上的 D593 是"半成品"：对话桥路线落盘 ✓、白名单 ✓、桌面呈现脚手架 ✓；consult 路线落盘 ✗、列表端点 ✗、冷读 ✗、状态机回填 ✗。D602 的交付质量在 main 上完整可复现。

---

## 1. 运行环境注记（D316 教训——环境依赖判别先行）

- 审计机 Node **22.22.0**：better-sqlite3 ABI=127 匹配，测试可真实跑（非环境依赖失败）。
- 全部测试结论 = 本审计机独立实跑（npx vitest run），非引用证据包：
  - `tests/routes/diagnosis-report-persistence.test.ts`：**25 用例 10 failed / 15 passed**（main 上，恒失败——端点不存在，非环境依赖）。
  - `tests/routes/conversations.test.ts`：全绿。
  - `tests/electron/ipc-contract.test.ts` + `notification-center.test.ts`：**23/23 全绿**。
  - `tests/electron/use-streaming-conversation.test.ts`：21/21 全绿（文件被 D602 零改动）。
- DS2 物理 runner（D593-run-ds2.sh）独立重跑：**FAIL**（进程内 reportId→404，预期双 200）——main 上恒失败，与证据包中"HTTP_STATUS=200"（分支态产物）形成对照（见 §2 L2）。
- 审计基线 `python3 scripts/audit/audit-check.py --full`：**PASS:2 WARN:967 FAIL:476**，与前批（2026-09-09 L1 收工批记录 476）一致，无本批可归因漂移。
- gh CLI 不可用（本机无 gh）→ CI job 级结论无法经 GitHub API 独立调取；L3 结论基于 ci.yml 源码物理分析 + bypass.log + 本机复现（见 §2 L3，如实标注边界）。

---

## 2. 对象 1 — D593 报告落盘 + 桌面呈现 + 401 对齐（#442，c7eb8e0d）

### 2.1 提交集映射（材料 1/2——D320 教训：不止看合并提交）

| 提交 | 角色 | 是否在 main |
|---|---|---|
| 0224843b | docs(D593) dev doc + task-state spec_done | 内容经 squash 入 main ✓ |
| f81378a8 | feat(D593) 实现主体（20 文件 +3593/−33，含 **src/routes/diagnosis.ts +202**） | ⚠️ **部分丢失**（见 P0-1） |
| 3fab8922 | evidence 补充（tsc/全量 vitest 基线对比日志） | 入 main ✓ |
| bdf62c0e | task-state impl 段回填（**status=impl_done**） | ✗ **丢失** |
| fcd22c84 | Merge origin/main into d593-fix（**冲突消解：diagnosis.ts / task-state/D593.json / spec md 三文件冲突，取 main 侧**） | = PR 最终 head，squash 后即 main 现状 |
| c7eb8e0d | PR #442 squash 合并 | = main |

**物理证据链**（全部本机 git 操作复现）：
- 分支侧 `d4ca4a04`（merge 前第一父）: `git grep "diagnosis/reports" d4ca4a04 -- src/routes/diagnosis.ts` → **2 命中**（端点存在）；task-state = `"status": "impl_done"`。
- 合并结果 `fcd22c84`: 同 grep → **0 命中**；task-state 回退 = `"status": "spec_done"`。冲突消解把分支核心变更整体丢弃。
- `git diff c7eb8e0d fcd22c84` → **空**：squash 无差别照搬 lossy 状态入 main。
- main 上 `git log origin/main -- src/routes/diagnosis.ts` 顶部 = d600（45de7a03）——**D593 从未在 main 触碰过 diagnosis.ts**。

### 2.2 L1 代码审计（main 现状逐项）

**已落地的部分（物理成立）**：

| 声称 | 证据（main，file:line） | 判定 |
|---|---|---|
| 对话桥路线落盘（source='conversation'） | src/routes/conversations.ts:295-323（`store.saveDiagnosisCheckpoint({sessionId: reportId, phase: 5, ..., source: 'conversation'})`，reportId 缺失/写失败 log.warn 不阻断 SSE） | ✓ |
| SessionStore.listDiagnosisReports（铁律 47 JSDoc 契约） | src/store/session-store.ts:598-639（limit 夹取 1..200 / offset>=0 / 行级 JSON 损坏跳过+degraded / 查询失败 fail-closed） | ✓（但见 P0-1：main 上零生产调用方） |
| 白名单五前缀 | src/middleware/auth.ts:116,119,120,121,122（diagnosis/reports、notifications、solutions、ga/clients、ga/switch）+ JSDoc :92-95 + README 安全模型节 :107-111 | ✓ |
| 桌面报告 tab 恢复链 + 404 文案诚实化 | electron-renderer/src/components/RightPanel.tsx:184-186（fetchLatestReportId → GET /api/diagnosis/reports?limit=1）、:356（404 文案去"内存缓存已清"错误归因） | ✓（列表兜底因端点缺失不可达，见 P0-1） |
| 哨兵 tab 真数据（Empty → SentinelDetail） | RightPanel.tsx:284-301（loadSentinelData 形状守卫+降级提示）、:485（tab 分支挂载）、:660-679（SentinelDetail 容器） | ✓ |
| 会话列表接 GET /api/sessions | electron-renderer/src/components/LeftPanel.tsx:70-94（挂载 fetch → setConversations）；app-store.ts:247（setConversations action） | ✓ |
| localStorage 报告锚 | electron-renderer/src/stores/app-store.ts:131-135（LAST_REPORT_ID_STORAGE_KEY='synova:last-report-id'） | ✓ |
| 新测试文件（2 个，红先证 20 failed/1 passed） | tests/routes/diagnosis-report-persistence.test.ts（481 行）+ tests/electron/right-panel-report-sentinel.test.ts（287 行）；red 证据 docs/synova/audit-reports/D593-*-evidence-*/red-vitest.log.txt（20 failed/1 passed） | ✓ 文件在 main；**main 上 10 failed**（见 P0-1） |

**未落地的部分（物理不成立）**：

| 声称 | main 现状（file:line） | 判定 |
|---|---|---|
| consult 完成点落盘 | src/routes/diagnosis.ts:543-549 仅 `cacheCompletedReport(...)`，无 saveDiagnosisCheckpoint | ✗ |
| GET report 内存 miss → checkpoint 冷读 | src/routes/diagnosis.ts:643-661 仅 `completedReports.get(consultId)`，miss 直接 404（:646-648），无 getDiagnosisCheckpoint fallback | ✗ |
| GET /api/diagnosis/reports 列表端点 | `grep -rn "diagnosis/reports" src/routes/` → 仅 auth.ts 白名单，路由不存在 | ✗ |
| FIFO 语义诚实化注释 | diagnosis.ts:118-127 内存 Map 保留，但"退役为一级缓存/事实源=持久层"注释缺失 | ✗ |
| task-state impl 段回填 | task-state/D593.json 仍 `"status": "spec_done"`, `"impl": null` | ✗ |

### 2.3 L2 偏离审计（声称 vs 证据）

- **P0-1（归因: control-tower）— 合并冲突消解吞实现**：PR #442 名义 = D593 实现合入，实际 = 冲突消解后的 lossy 树。验收核心（重启存活）在 main 上物理不成立（§1 复跑记录）。DS1（测试全绿）/DS2（重启读回 200）/DS3（双路线落盘）/DS7（列表分页）在 main 上全部不成立；DS4/DS5（桌面呈现）/DS6（白名单）成立。**DS 成立 4/9**。
- **P1-1（归因: implement）— 合并提交消息 overclaim（M2）**：c7eb8e0d 消息正文宣称"checkpoint 冷读 + 列表端点 + 五前缀白名单"与"task-state impl 段回填"——合并 diff（`git diff c7eb8e0d^..c7eb8e0d --name-status`）无 src/routes/diagnosis.ts、无 task-state/D593.json。消息描述了分支态，树承载的是 lossy 态。
- **P1-4（归因: implement）— DS2 证据包未标注"分支态"**：docs/synova/audit-reports/D593-report-persistence-evidence-20260908/ds2-restart-readback.log.txt 的 HTTP_STATUS=200 是 2026-09-08T16:15Z 分支产物；README 未声明"本证据在主上不可复现（合并丢失）"。引用者会误读为 main 现状。证据本身真实（我逐条核过 runner 的诚实设计：端口真释放探活、替身模式声明），缺的是合并后的有效性标注。
- **P0-2（归因: control-tower + implement）— 夹带**：`git diff c7eb8e0d^..c7eb8e0d --name-status` 中写集外新增：`.codex/heartbeat.json`（Codex 会话心跳产物）、`.synova-wt-ct64`（新）/`.synova-wt-d593`（新）/`.synova-wt-d590`（改）——三者是 bypass 补记 diff 快照文件（287B），`docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D595-mcp-auth-20260908.md` + `docs/synova/coordination/编码指令-D595-MCP认证权限-20260908.md`（D595 两文档）、`memory/notes/proposed/D599-dev-doc-交付前置复核与指令内嵌.md`（D599 note）。溯源：全部经 fcd22c84 第一父侧（dev-doc 线在共享主树 d593 分支上提交他人任务产物，CT-65 族）。与前批 P0-1（#449 夹带 D603 主体）同族，**12 小时内第二次**。
- **P1-3（归因: control-tower）— task-state 状态机断裂**：D593.json 停在 spec_done 无人发现；前批 P1-3 家族（D600/D603 未登记）的"合并登记 gate 缺失"再次实证。

### 2.4 L3 执行审计

- bypass 链：f81378a8 / bdf62c0e 均有 `COMMITTED | pre-commit PASS` 登记（.claude/bypass.log:1176-1177）✓。
- **P1-2（归因: control-tower）— CI vitest 豁免盲区（M1 fail-open 变体）**：.github/workflows/ci.yml:79-95——vitest 失败时把失败测试文件映射为"对应源文件"（sed tests/→src/），源文件不在 PR diff 内 → "Pre-existing test failures from unchanged files — not blocking" exit 0。D593 场景：PR 自带新测试文件（diagnosis-report-persistence.test.ts）在合并后 10 failed，但映射源文件 `src/routes/diagnosis-report-persistence.ts` 不存在、diagnosis.ts 因冲突消解不在 diff 内 → 全部失败被豁免 → CI 绿。**"新测试文件失败"不在豁免判定面**——这是合并能带着 10 个失败测试过 CI 的物理机制。CI job 级结论无法经 API 独立调取（本机无 gh），以上为源码物理分析 + main 上失败实况，如实标注。
- pre-commit 13 组：分支侧 SYNO_CI=1 声称 exit 0（合并消息），无法独立重放（分支态），不采信不否定，记"声称可核但非本批重点"。

### 2.5 定向深查 4 项

1. **SQL 参数化**：`listDiagnosisReports` 全部 prepared statement + 绑定参数（session-store.ts:604 `SELECT COUNT(*) ... WHERE phase = 5`、:606-608 `... ORDER BY saved_at DESC, rowid DESC LIMIT ? OFFSET ?` + `.all(limit, offset)`）——**参数化 ✓ 无注入面**。落盘侧沿用既有 `saveDiagnosisCheckpoint`（session-store.ts:546，INSERT OR REPLACE 参数化，D487 既有）。
2. **冷读路径不触碰引擎**：分支态实现符合 spec（resolveStore 动态 import + getDiagnosisCheckpoint，不构造引擎）；**main 态冷读缺失**（P0-1）——此深查项在 main 上的结论 = 不成立。
3. **白名单五前缀是否过宽**：`/api/diagnosis/reports` 精确前缀未放宽 `/api/diagnosis/` 全前缀（auth.ts:116，spec §5.2-D 决策 9-B）✓；notifications/solutions/ga 暴露面 = 单机本地信任模型（auth.ts:88-91 JSDoc 声明 + README 安全模型节），与 /api/sentinel/* 先例一致，非本次新增风险面。已知披露属实：solutions.ts:36 内部 requireAuth（:93/:116 调用）致零凭证 401——白名单与路由内鉴权并存，行为 = "401 而非直达"，不影响 D593 验收面（D590 白名单先例同型）。
4. **50 条 FIFO → 持久化后上限策略**：内存 FIFO 50 保留为一级缓存（diagnosis.ts:118-127）；持久层 = 全保留 + limit/offset 分页（session-store.ts:602-603,606-608；spec §5.2-F 决策 5-A 全保留）——策略本身诚实合理；**main 上列表端点缺失 → 分页能力不可达**（P0-1 后果之一）。

### 2.6 DS 逐条（main 现状）

| DS | 内容 | main 判定 |
|---|---|---|
| DS1 | 两新测试文件全绿 | ✗（persistence 测试 10 failed，独立复跑） |
| DS2 | kill -9 → 重启 → GET 200 | ✗（独立复跑：进程内 reportId 即 404） |
| DS3 | 双路线落盘 | ✗（对话桥 ✓ / consult ✗） |
| DS4 | 桌面报告 tab 刷新恢复 | ⚠️ 部分（localStorage 锚 ✓；列表兜底 404 不可达） |
| DS5 | 哨兵 tab 真数据 | ✓（呈现层独立成立） |
| DS6 | 401 清零五前缀 | ✓（auth.ts:116-122 + 测试用例 6 在 main 通过——白名单在 middleware，测试可独立验证） |
| DS7 | 列表分页/边界 | ✗（端点不存在；listDiagnosisReports 的边界夹取逻辑存在但无路由消费） |
| DS8 | 接线 grep + tsc 零新增 + as any=0 | ⚠️ 部分（tsc/类型面成立；接线面 listDiagnosisReports 零生产调用方——M3） |
| DS9 | 决策记录 + 北星三句 | ✓（spec §5.4 九决策在 doc；合并消息含决策摘要） |

---

## 3. 对象 2 — D602 交互卡片 + preload/IPC 收敛 + 通知 error 消费（#448，749c3b51）

### 3.1 提交集与写集对账

`git diff 749c3b51^..749c3b51 --name-status` = 写集声明（8 修改 + 2 新建 + 1 删除 + brief + evidence + task-state）**+ 仅 .claude/bypass.log（惯例）**。**合并零漂移**——与 D593 形成对照，是本批的正面样本。

### 3.2 L1 代码审计（main 现状，全部物理成立）

| 声称 | 证据（file:line） | 判定 |
|---|---|---|
| preload 2→8 方法 + 退订函数 | electron/preload.cjs:42-59（8 方法；subscribe :36-40 返回 `() => ipcRenderer.off(channel, listener)`；CHANNELS 常量 :21-28 无动态通道） | ✓ |
| main.cjs 3 ipcMain 通道 + 全校验 | electron/main.cjs:158-165（app:get-version try/catch）、:168-189（tray:update-state 枚举+正整数校验，非法 warn+忽略）、:192-213（notify:show clampText 200 + 点击回传） | ✓ |
| Tray 菜单三项现役 UI | main.cjs:250-254（打开 Synova → navigate 'chat'；打开通知中心 → navigate 'notifications'；Quit）——旧页 `/cockpit`、`/app/admin.html` 页面引用清零；main.cjs:269 `/api/cockpit/data` 为 P0 轮询数据源合法保留 | ✓ |
| P0 轮询补 push 通道 | main.cjs:277-282（checkP0Alerts red → `webContents.send('push-notification', {title, body, id})`） | ✓ |
| bridge 接口 10→8 + 3 死接口物理清零 | electron-renderer/src/ipc/bridge.ts:19-36（ElectronAPI 8 方法）；`grep -rn "minimizeToTray\|pauseNotifications\|resumeNotifications" electron-renderer/src/` 零命中（含注释） | ✓ |
| IPC 三方一致（物理断言源码） | tests/electron/ipc-contract.test.ts:130-190（fs 读三份源码 + 正则解析：用例 1 preload≡bridge 双向相等且恰 8；用例 3 main 通道 ⊇ preload 请求通道且恰 3；用例 5 3 推送通道 + off 退订 + on 通道有 main 发送方） | ✓（独立复跑 5/5 绿） |
| app-store 本地通知 FIFO 20 | electron-renderer/src/stores/app-store.ts:38（MAX_LOCAL_NOTIFICATIONS=20）、:221-243（pushLocalNotification newest-first + slice(0,20) + electronAPI.showNotification 旁路 try/catch 不抛；dismissLocalNotification :242-243） | ✓ |
| 数据源切 /api/sentinel/tickets + 形状守卫 | electron-renderer/src/hooks/useNotifications.ts:146-182（loadTicketNotifications → GET /api/sentinel/tickets :148；asTicketViews :113-136 形状守卫畸形跳过） | ✓ |
| alertCount = open 工单数 | useNotifications.ts:175（openCount 过滤）、:237（setAlertCount(result.openCount)）；App.tsx:80-81（alertCount>0 → updateTrayState('unread', count)） | ✓（深查 2 语义正确） |
| actOnTicket → 真实状态机 transition | useNotifications.ts:192-214（POST /api/sentinel/tickets/:id/transition，body {to:'acknowledged'|'dismissed'}，4xx/5xx/网络异常 {ok:false,error} 不抛）；D580 契约对照：src/routes/sentinel.ts:115-123（router.post('/tickets/:id/transition')，body.to 校验 + 状态机错误码 400/404/409/503），挂载 server.ts:373（app.use('/api/sentinel', sentinelRoutes)） | ✓（深查 3 契约一致） |
| SSE error 帧 → 通知双可见 | electron-renderer/src/hooks/useStreaming.ts:244-251（case 'error' → pushLocalNotification）；消费链 grep 三处（useStreaming.ts:249 / App.tsx:95 / app-store.ts:234） | ✓（深查 4 见下） |
| NotificationCenter error banner + 动作按钮 | electron-renderer/src/components/NotificationCenter.tsx:169-171（error !== null → NotifErrorBanner）、:32-37（banner 组件）、:140-149（handleAction → actOnTicket + 项内失败反馈） | ✓ |
| App.tsx 3 订阅 + cleanup | electron-renderer/src/App.tsx:94-95（onPushNotification）、:107（onNotificationClick）、:113（onNavigate）——均经 bridge 退订函数返回 | ✓ |
| 孤儿 preload.js 删除 | `git diff 749c3b51^..749c3b51` D electron-renderer/preload.js；grep 零引用 | ✓ |
| red 23/23 先证 | docs/synova/audit-reports/D602-ipc-notification-evidence-20260909/red-green-evidence.md §1（两文件 5+18=23 全红原文，含现状必红失败原文） | ✓ |
| DS4 真实落库 + 重启持久 | evidence/ds4-transition-evidence.md + logs/（tickets-before.json open → transition-response.txt 200 → tickets-after.json acknowledged → tickets-after-restart.json source:'table' + resolved 持久；重启前后对照完整） | ✓（日志 7 份物理在位，wc/head 抽验过） |
| DS5 真实 SSE error 帧 | evidence/ds5-sse-error-evidence.md + logs/consult-sse-error-frame.txt（真实后端无 key 降级 error 帧原文）+ 同流 complete degraded 帧（logs/consult-sse-full-stream.log.txt） | ✓ |
| 零回归 | use-streaming-conversation.test.ts 21/21 独立复跑绿 + `git diff 749c3b51^..749c3b51` 该文件零改动；tests/electron 两新文件 23/23 独立复跑绿 | ✓ |
| tsc 28=28 | 未独立重放（根 tsc 28 为基线口径，前批已核；本批不做增量断言——如实标注） | — |

### 3.3 定向深查 5 项

1. **preload 输入校验**：preload 层不做参数校验（它是信任层——contextIsolation 下 renderer 只能调用暴露函数，暴露面即 8 个固定方法，无 fs/shell/openExternal 高风险面）；**防御在 main 侧**：tray:update-state 枚举+正整数校验（main.cjs:170-176）、notify:show clampText 截断 200（:195-196）、全 handler try/catch warn 不抛——恶意 renderer 乱传参数 = warn+忽略，不炸主进程。**PASS**（与 Electron 官方基线一致）。
2. **Tray 角标计数**：alertCount = **open 工单数**（useNotifications.ts:175 → :237 → App.tsx:80-81）——语义正确（"open 工单数 vs 全部工单数"选前者，与 brief Q3/规格声明一致，非全部工单数误用）。✓
3. **actOnTicket vs D580 契约**：URL/method/body 三方一致（见 §3.2 表行）✓；响应判定 `res.ok && data.ok===true`（useNotifications.ts:201）与 D580 成功形状 `{ok:true, ticket:{...}}` 匹配 ✓。alerts/:id/action 空洞端点不接 = spec 决策 4-B，诚实。
4. **SSE error 帧误触发**：push 仅绑定 `case 'error'`（useStreaming.ts:244），与 complete/done 分支互斥——正常 end 帧不会触发通知 ✓。残余观察（非阻断）：同一 error 帧若重放/重复投递会重复入栈（无去重键）——D590 契约 error→end 语义下正常流不会发生，记 P2 观察。
5. **notifications.ts 死数据源影响范围**：D602 后 renderer **零消费方**（grep electron-renderer/src 仅注释命中）；端点被 D593 白名单放行后返回空列表不 401——运行时影响 = 零；治理已登记哨兵线（task-state D602 note + brief Q2 不做清单）。披露诚实 ✓。

### 3.4 L2/L3 审计

- 写集合并零漂移（§3.1）✓；bypass 链 8045c96d COMMITTED pre-commit PASS（.claude/bypass.log:1204）✓。
- 写集内 task-state/D602.json 已随合并入库（status impl_done + 完整 impl 段）✓——与 D593 的 task-state 断裂形成对照。
- **P2-3（归因: implement）**：evidence README DS9 行"全量 vitest exit 0"与 task-state note"全量 vitest 失败文件集与基线 diff=空（9 个既有失败文件恒等）"自相矛盾——有失败文件时 vitest exit 非 0。表述缺陷（实际意图 = CI 豁免口径下可过），非事实缺陷。
- **P2-4（归因: audit）**：派单材料口径失准两处——①"preload 2→14"：物理 = 2→8 方法（preload.cjs:42-59）+ 6 通道，无 14 对应物；②"D593 测试 = conversations.test.ts（21 用例 91 expect）"：D593 声明的测试 = diagnosis-report-persistence.test.ts（main 上 25 用例），conversations.test.ts 是 D590 既有文件（全绿但与 D593 写集无关）。与前批 P1-1"派单材料表失准"同族（材料侧改进归 CTO/派单 SOP）。

### 3.5 D602 结转与结论

- 前批（2026-09-09 L1 收工批）给 CONDITIONAL PASS，P1-1 = spec 文件（SYNOVA-IMPL-DSH-D602-*.md）不在 main。**#453（26858bac）已登记**"D602 dev doc 确认不在 main（P1-1 记录，task-brief 即 spec 载体）"——P1 已登记但未关闭（spec 仍未入库，brief 95 行 + 编码指令 + evidence 充当契约载体）。本报告**维持 CONDITIONAL PASS**：结转 P1-1（状态=已登记待入库或模式正式获准）+ 本批 P2-1/P2-3/P2-4。
- **P2-1（归因: devdoc）**：brief Done 标准 5 verify 命令（`grep -n "cockpit\|admin.html" electron/main.cjs` 零结果）与交付态（1 命中 main.cjs:269）不符——evidence README:50 已显式披露该偏差（决策 6-B 合法保留），但 brief 未同步收尾。文档-实现口径微漂移（M7 族）。
- **P2-2（归因: implement）**：evidence README DS 表引用的 spec 路径（docs/plans/.../SYNOVA-IMPL-DSH-D602-*.md）在仓库不存在——引用不可核验文件（CT-40 族，但已由 #453 登记，不再重复计 P1）。

---

## 4. 15 项语义审计清单（逐项，两对象合并出表）

| # | 项 | D593（main） | D602（main） |
|---|---|---|---|
| 1 | 接口真实性 | ⚠️ spec 声明的 listDiagnosisReports ✓ 存在（session-store.ts:598）；/api/diagnosis/reports 路由 ✗ 不存在 | ✓ 8 接口三方均真实存在 |
| 2 | 文件路径存在性 | ⚠️ spec 写集 8 文件：7 落 main；diagnosis.ts 变更 ✗（合并丢失） | ✓ 写集 11 项全部与合并 diff 一致 |
| 3 | Edge ID | N/A（无哨兵扩展变更） | N/A |
| 4 | 架构边界 | ✓ 零新增静态跨层 import（对话桥沿 D563 动态通道；session-store L5 域内） | ✓ 纯 L1 桌面壳，零 src/ 后端改动 |
| 5 | 数据流完整性 | ✗ 写路径 consult 线断（诊断完成 → 内存 only）；读路径冷读断；列表断 | ✓ SSE error → pushLocalNotification → NotificationCenter 全链 grep 命中 |
| 6 | 接线深度 | ✗ listDiagnosisReports 零生产调用方（M3，因路由丢失） | ✓ 8 新 export 全有生产调用点（App.tsx 订阅 3、useStreaming 消费、按钮 onClick） |
| 7 | 降级诚实性 | ✓ 落盘失败 log.warn 不阻断（conversations.ts:320-322）；列表行级损坏 degraded（session-store.ts:617-631） | ✓ 5 处新 catch 全 console.warn + 显式降级；main 侧 handler 全 try/catch warn |
| 8 | 测试契约（铁律 47） | ✓ listDiagnosisReports JSDoc 契约完整（session-store.ts:583-597）；isDiagnosisReportArchive 类型守卫 | ✓ preload 逐方法 JSDoc；pushLocalNotification/actOnTicket 契约注释 |
| 9 | 测试覆盖三路径（铁律 48） | ⚠️ 用例设计覆盖正常/降级/边界（77 expect 非空壳）；但 main 上 10 failed = 交付态不绿 | ✓ 23/23 绿 + 降级路径显式（7b/7c/9b/9c/10c）+ 边界（6c FIFO、8b 已读） |
| 10 | 文件驱动 | N/A | N/A |
| 11 | 自我报告交叉对比 | ✗ 合并消息声称 ⊋ 合并树（P1-1）；证据包未标注分支态（P1-4） | ✓ 披露全诚实（headless 边界、cockpit 保留行、OS 通知半程） |
| 12 | dev doc 偏离审计 | ✗ 写集漂移（diagnosis.ts 丢失）+ 夹带 6 文件（P0-1/P0-2） | ✓ 写集零漂移；brief Done 5 口径微漂移（P2-1） |
| 13 | 控制塔执行审计 | ⚠️ bypass 链 ✓；CI vitest 豁免盲区让"自带测试 10 failed"过闸（P1-2）；CI job 级结论无 gh 无法独立调取（如实标注） | ✓ bypass 链 ✓；CI 绿口径（9 失败文件=基线）与 D593 破碎态一致，非 D602 引入 |
| 14 | 版本编排合规 | ✓ 无版本声称、未碰 VERSION.md/tag（VERSION.md 尾= V4.9.1，无 D593 条目，正确） | ✓ 同上 |
| 15 | 并行合规 | ✓ 两任务串行（D602 基线 92b094de 含 D593 合并）；worktree 隔离（.synova-wt-d593 / .synova-wt-d602）；写集零重叠（D593 声明的 RightPanel.tsx 边界被 D602 遵守） | ✓ 同上 |

---

## 5. 北星三问裁决（north-star-guard，两对象）

**D593**：
1. 服务真实用户场景吗？——✓ GA/企业主的诊断报告是核心产出（PRODUCT-BRIEF §三"GA 按需诊断 → HTML 诊断报告"），重启不丢是真实需求。
2. 更接近终态吗？——✓ 报告持久化 = 报告链路最后一公里（PRODUCT-BRIEF §五 W1 的延续），非横向扩展。
3. Synova 还是那个 Agent 吗？——✓ 桌面呈现服务于诊断闭环，未变成报表工具。
**裁决：对齐**（设计态）。但**合并态只交付了一半**——方向对齐不能豁免交付完整性（铁律 4）。

**D602**：
1. ✓ GA 在桌面端处理哨兵工单（确认收到/标记误报）= "跟踪执行"环节的真实场景（PRODUCT-BRIEF §三 哨兵巡检 → 工单）。
2. ✓ 桌面施工图 🟢（AGENTS.md 项目身份）内收尾棒，preload/IPC 收敛修复审计 P0-2。
3. ✓ 工单动作 = 增长跟踪执行闭环，产品本质不变。
**裁决：对齐**。

---

## 6. L4 防线缺口收割（每对象必答）

### D593（P0-1 合并冲突吞实现）——本该拦住的三道防线，为什么都没拦住？
1. **CI vitest 豁免逻辑（ci.yml:79-95）**：设计上只豁免"未变更源码对应的失败测试"，但判定面 = "失败测试的映射源文件是否在 PR diff 内"。D593 的冲突消解恰好把源文件（diagnosis.ts）从 diff 中抹掉 → PR **自带的新测试文件失败**被误判为"pre-existing"→ 绿。缺：**新测试文件本身在 PR diff 内 → 失败一律阻断**（一条 bash 规则改动即可）。
2. **PR 合并审查（squash 时刻）**：无人对"分支树 vs 合并树"做写集落位核验（`git grep 关键符号 合并树` 一行命令即可发现 `/api/diagnosis/reports` 丢失）。缺：**合并级写集对账 gate**（PR merge diff vs dev doc 写集 + 关键符号 grep）。
3. **task-state 登记 gate**：合并后无 job 读 task-state 对账（status=spec_done 的"已合并"任务无人报警）。前批 P1-3 已入 CT 队列（"CI task-state 注册 gate"），本次是**第三实证**（D600/D603 → D593）。

### D593（P0-2 夹带）——本该拦住？
CT-65（共享主树跨 session 提交污染）已登记未修复 → dev-doc 线把 D595/D599 文档 + 会话产物提交落在 d593 分支 → squash 全量带入 main。前批 P0-1（#449 夹带 D603）刚把"合并通道无白名单检查"定为 P0，**12 小时内 #442 重复**。缺：**合并 diff --name-status 白名单核验**（写集外文件逐项确认或 CI job 化）。归因: control-tower（通道缺陷）+ implement（dev-doc 线主树提交）。

### D602——本批无 P0/P1 新增
微瑕归口：brief 收尾同步（P2-1，S-6 族）、evidence 引用可核验性（P2-2，CT-40 族已登记）、派单材料口径（P2-4，派单 SOP）。

---

## 7. 错误归因闭环（每个发现 → 角色 → 防再犯）

| 发现 | 归因 | 防再犯机制（一类一机制，防臃肿） |
|---|---|---|
| P0-1 合并冲突消解吞实现 | control-tower | **合并级写集对账 gate**（合并树 vs dev doc 写集 + 关键符号 grep）——bash 门禁 1 个 |
| P0-1 连带（CI 豁免盲区） | control-tower | **CI：PR 自带新测试文件失败一律阻断**（豁免判定加"test 文件本身在 diff 内则不免"）——ci.yml 1 处 |
| P0-2 夹带（#442，#449 同族二次） | control-tower + implement | **合并 diff 白名单核验**（并入上述合并级 gate，同一机制覆盖） |
| P1-1 合并消息 overclaim | implement | 合并消息与合并树对账（并入合并级 gate 的报告面） |
| P1-2 已在 P0-1 连带 | control-tower | 同上 |
| P1-3 task-state 断裂（第三次） | control-tower | CT 队列已有"CI task-state 注册 gate"——升级优先级（三次实证） |
| P1-4 证据未标注分支态 | implement | DSH persona 规则：证据包标注"分支态/主态 + 合并后有效性" |
| D602 P2-1/P2-3 | devdoc/implement | S-6 族（交付汇报与实现同 commit 回填）已存在，本次执行漂移 |
| D602 P2-4 派单材料失准 | audit | 派单 SOP 材料口径核验（与前批 P1-1 合并处理） |

**M 模式匹配**（AUDIT-FINDINGS-LEDGER M1-M8）：P1-1/P1-4 = **M2**（声称 vs 事实，新盲区=合并消息与证据标注）；P1-2 = **M1**（fail-open，新变体=豁免判定面不含"新测试文件"）；P0-1 的触发路径 = **M3 家族新变体**（合并吞实现导致建成未接线）。**M1-M8 均不覆盖"合并冲突消解吞实现"本身 → 提议新类 M9**（每类一个免疫细胞：合并级写集对账 gate，见上表第一行）。

---

## 8. 修复路线（只报告，不立项——K3 不越界）

1. **D593 FIX 任务（P0，建议 CTO 立项）**：从 origin/feat/d593-report-persistence-p2（d4ca4a04 或 bdf62c0e 态）恢复 src/routes/diagnosis.ts 的 D593 变更（含 consult 落盘、GET 冷读、列表端点、注释诚实化）→ 重新冲突消解（对齐 d600 的 diagnosis.ts 演进）→ task-state/D593.json 回填 → persistence 测试 25/25 绿 → DS2 runner 重跑 200 → 清理夹带 6 文件（.codex/heartbeat.json、.synova-wt-ct64/d593/d590 标记补丁、D595 两文档待 D595 自线入库、D599 note 归位）。
2. **D602**：无修复需求；P2 可选（brief 收尾同步、evidence 引用修正）。
3. **控制塔**：合并级写集对账 gate + CI 新测试文件豁免收紧（归 DSH 领地，CT 队列）。

---

## 9. 审计边界声明

- 本报告只审 c7eb8e0d / 749c3b51 两次合并入 main 的内容（基线 26858bac），不审分支内部历史除溯源所需外。
- CI job 级结论：无 gh API 通道，以 ci.yml 源码分析 + 本机复现为据，已如实标注。
- 白名单安全模型的"单机本地信任"取舍是创始人裁决（D590 裁决①），本报告记录不翻案。

*报告完*
