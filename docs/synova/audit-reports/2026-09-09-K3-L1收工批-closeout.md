# K3 审计报告 — L1 收工批（8 对象合并批）closeout

> 审计员: Kimi K3（独立会话，零上下文）｜ 日期: 2026-09-09 ｜ 协议: AUDIT-PROTOCOL.md v1.1 + synova-audit skill v1.0 + 派单 `docs/synova/coordination/审计派单-K3-L1收工批-20260909.md`
> 审计工作区: `/Users/wane/Synova-k3独立审计`（独立 clone，与产品工作区物理隔离；branch `audit/k3-20260909-l1-closeout`，基于 `origin/main@01e39a7e`）
> 先例: `2026-09-09-K3-D590-D591-closeout.md`（#443）

---

## 0. 结论总览（先读）

| # | 对象 | 结论 | 关键发现 |
|---|---|---|---|
| 1 | D603 L1 收工 | **PASS**（内容全部核验通过） | 但**本批最高发现 P0 不在此对象内容，而在合并通道**：D603 代码内容经 **#449 夹带**进 main（9e5a44cd，标题"SYNO_CI env 注入"），#451（01e39a7e）名义"feat(D603)"实为 3 文件文档提交 → 合并溯源不实（P0） |
| 2 | D602 交互卡片 | **CONDITIONAL PASS** | dev doc 路径不存在（派单材料表所述 `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D602-*.md` 在主上找不到，P1）；内容/接线/降级均过 |
| 3 | D600 CFG 第一命令 | **PASS** | task-state/D600.json 未登记（P1，C 节） |
| 4 | D601 看板口径+扫描 | **PASS** | 提交集名义失准：8193e838 不在 main（被 #435 squash 吸收），内容在主 ✓ |
| 5 | D597 会话投影接线 | **PASS** | 契约（bootstrap.ts:24-27 副作用 import）在主 ✓，#421 终验收口闭合 |
| 6 | CT-64 四类漏网修补 | **PASS** | 四类沙箱全验证（vitest 12/12 + 独立沙箱复核）；P2×2（见 §5.6） |
| 7 | CT-63 扫远端分支名 | **PASS** | 逻辑实跑 15/15 全过；**配对测试未接线进 CI canary**（P1，铁律 0-2 类缺口） |
| 8 | #449 SYNO_CI 注入 | **CONDITIONAL PASS** | 注入层级/存量 42 全绿 ✓；但本 PR = P0 夹带通道本体（见 §1） |

**分级计数：P0 ×1（合并通道/溯源不实）｜ P1 ×5 ｜ P2 ×3**

- **P0-1**：D603 全部代码内容以 `9e5a44cd`（#449，"fix(CT-64): SYNO_CI env 注入"）的 squash diff 进入 main（51 文件 +860/−954；PR 名义变更为 2 行 ci.yml）。#451（01e39a7e，"feat(D603)…(#451)"）diff 仅 3 文件（+119 行：审计派单 doc + 编码指令 + bypass.log 1 行）——**D603 的名义合并提交不承载任何 D603 代码**。git 溯源（blame/log -S/审计复核）全部指向错误提交。归因: control-tower（PR 审查/范围门禁缺失）+ audit（派单前置核验口径不严）。
- P1-1（对象 2）：D602 dev-doc 缺失（派单材料表路径在主上 `git ls-tree` 不存在）。
- P1-2（对象 7）：`tests/control-tower/alloc-task-id.test.sh`（CT-63 配对测试）不在 ci.yml canary 27 项，全仓无任何接线 → 测试存在但永不在 CI 运行（D329 P2-2 同款"新 export 未接线"模式）。
- P1-3（对象 3/1）：task-state/D600.json、task-state/D603.json 未登记（对照已登记的 D601/D602/D597；D600 连 95e1c9fa 的 D601.json 都注明"改号自 D600"而 D600 自身无登记——编号体系盲区实证）。
- P1-4（L3 证据不实）：派单/派单人材料声明"D603 有 2 笔 GATE_FAIL_SOFT"，主上 `.claude/bypass.log` 实为 **3 笔**（03:55:44 / 04:10:02 / 04:11:11，branch=feat/d603-tui-cleanup-crosslayer）。
- P1-5（对象 1 微瑕，记录不阻断）：产品工作区 `.claude/task-briefs/` 未跟踪文件 **86 个**（含 D602~D607「并发测试-N」骨架 brief 20+，D603-并发测试-10 在内）——跨 session 工作区污染持续（派单 C 节预研属实）。
- P2-1（对象 6）：脚本对"基线文件整文件消失"（计数 0）不产生"收紧"提示（仅"文件存在且计数降低"才提示；CI 层 vitest 真实仓库双向断言可拦）。
- P2-2（对象 6）：脚本 L1 扫描目标清单仍含 `src/tui-v3`（D603 已删，运行时报"目标缺失(跳过)"一屏黄警告——按附录 A 清单硬编码路径，未随删除同步）。
- P2-3（对象 8）：#449 本身无配对测试（ci-strict-visible.test.sh 是 D515 既有测试，不针对 ci.yml env 注入内容）。

---

## 1. 合并状态事实（本批最关键前置——派单 Step 0 的实时修正）

派单前置条件"main 已包含 D603（24eba4e4、98cea78f、merge be312fa7）"在派单时刻**不成立**，审计过程中 main 推进，最终态如下（全部物理证据）：

| 检查 | 结果 |
|---|---|
| `git merge-base --is-ancestor 24eba4e4 origin/main` | **exit=1（非祖先）** — 派单所列 D603 主体提交**从未进入 main** |
| `git merge-base --is-ancestor 98cea78f origin/main` | **exit=1（非祖先）** — CI 打回修复提交同样从未进入 main |
| `be312fa7` 是什么 | "Merge remote-tracking branch 'origin/main' into feat/d603-tui-cleanup-crosslayer"——**是把 main 合进 feature 分支**（parents=41d4b0b9, 9e5a44cd），不是 D603 合 main |
| main 是否有 D603 merge | `git log origin/main --merges --oneline \| grep -i d603` → **0 命中** |
| `01e39a7e`（#451，"feat(D603)…(#451)"）diff | **仅 3 文件 +119 行**：`.claude/bypass.log +1`、`审计派单-K3-L1收工批-20260909.md +60`、`编码指令-D602-20260909.md +58`——**零代码** |
| `9e5a44cd`（#449，"fix(CT-64): SYNO_CI env 注入"）diff | **51 文件 +860/−954**：完整 D603 主体（tui-v2/tui-v3 删除、routes/*、agent/* L2 服务、CLAUDE.md、memory note、基线 68→42）+ 98cea78f 全部修复（6 个 agent 配对测试、agent-entry.test.ts、gate 测试 92 行改）+ D602 测试 + ci.yml 2 行 |
| 内容等价性 | main@01e39a7e 的 4 个关键文件（l1-cross-layer-baseline.txt / sessions.ts / tui-v2/README.md / D603 memory note）hash **与 24eba4e4、98cea78f 完全一致**；`check-architecture-gate.test.ts` 在 main == 98cea78f 版（75b4e607…） |

**判定**：D603 的**内容**已全量在 main（且是最终修复版），但**合入通道是 #449 的夹带**——#451 只是把派单文档/编码指令合进来的名义提交。"D603 已合 main"按派单字面（指定提交在 main）**不成立**，按意图（D603 内容在 main 且可审计）**成立**。本报告按后者继续审计（内容全过），并以前者产生 **P0-1**。

**为什么没拦住（L4）**：① #449 的 PR 分支 `fix/ct64-ci-syno-env` 基于未合入的 `feat/d603-tui-cleanup-crosslayer` 切出（CT-64/R2 需要在 D603 的 gate 测试状态上验证注入）——GitHub squash 按 merge-base 计算 diff，把基线上的 49 个 D603 文件一并吞入；② 三道防线均未拦：DSH pr-review（铁律 0-5）应比对"PR 声称范围 vs diff 文件集"（51 文件 vs 标题 2 行变更）；pre-commit/pre-push 只查仓库质量不查 PR 范围；CI 只在 PR head 上跑（head 已无骨架、代码全绿）即放行。③ **缺什么**：PR 级"范围一致性"门禁（PR 标题/正文文件清单 vs diff 文件集，超范围即标记）；以及"分支基底必须 main 或已合分支"规则（fix/ct64-ci-syno-env 基底未合分支 = 高风险模式，应显式禁止或人工确认）。

---

## 2. 运行环境注记（D316/D317 教训——环境依赖 vs 恒失败）

- **主机**：macOS（Darwin）。`timeout` 命令不存在（GNU coreutils 无）→ 长跑用 `&` + `kill`。
- **审计工作区**：`/Users/wane/Synova-k3独立审计` 独立 clone（branch 基于 origin/main），`node_modules` 已存在（vitest 4.1.8 可跑）——**产品工作区**（/Users/wane/SynovaAgent，feat/d603 分支检出）仅做只读 git 检索与工作区状态观察，未写任何文件（`git fetch` 在产品仓库被沙箱拒写 FETCH_HEAD，符合只读预期）。
- **vitest**：`npx vitest run tests/architecture/check-architecture-gate.test.ts` → **12/12 通过**（2.84s）；`alloc-task-id.test.sh` 实跑 **PASS=15 FAIL=0**。
- **TUI 冒烟**：`npm run tui`（12s 后台+kill）——UI 完整渲染（"Synova 增长导航 · 准备就绪 · 未配置" + Composer 输入框），随后 Ink 报 "Raw mode is not supported on the current process.stdin"——**无 TTY 环境固有错误，非代码回归**（D317 同款环境依赖判定）。作者声称"20s 存活"，本环境 12s 存活至渲染完成，一致。
- **python3**：可用；`scripts/audit/audit-check.py --full` 跑通（见 §7 基线）。
- **CI job 级结论**：无法取得（无 GitHub 凭证；gh CLI 未认证）——L3 的 CI 部分以"本地等价复现"替代（脚本 + 门禁测试在主态上实跑），并在 §1 标注证据缺口。

---

## 3. 对象 1：D603（深查 A1-A4 全答）

### A1 写集扩大（13 文件）

**派单前提核验：不成立。** 派单称"任务书可碰清单仅列 4 文件（sessions/cli/server/bootstrap），实际改 13 个未列文件"。物理核验：
- `.claude/task-briefs/2026-09-09-D603-L1P3-….md` 在 24eba4e4 与 main **同 hash**（1791f9f8…），其 `## Q2:` 明确列出全部涉改文件：`src/index.ts`、`src/l1/qa-router.ts`、`src/tui-v2/lib/commands.ts`、`src/routes/{sessions,auth,agent-observer,ontology,expert,ga-annotations,overflow,documents,knowledge,permissions,admin-knowledge}.ts` 等 39 项（Q2 原文逐行可查，brief:26-69）。
- 派单所指"13 个文件"**全部在 brief Q2 内** → 不存在"Q2 之外写集扩大"。
- **必要性**（数学约束核验）：三处违规源 = 扫描报告 §二 静态 26 处（L1→L3 3 / L1→L4 4 / L1→L5 19）。这 13 个文件正是静态 26 处的宿主文件（expert.ts:11-13 / ga-annotations.ts:28 / agent-observer.ts:11-12 / auth.ts:16-17 / ontology.ts:10-11 / overflow.ts:15 / documents.ts:10 / knowledge.ts:12 / permissions.ts:14 / admin-knowledge.ts:19 / index.ts:14 / qa-router.ts:13 / commands.ts:14）——静态清零 = 要么删点、要么下沉构造+注入，只有此路。**结论：必要，无横向扩散。**
- **领地核验**（申报"零触碰"）：
  - `src/routes/conversations.ts`（D590）：24eba4e4/98cea78f/41d4b0b9/bed1e3b0/9d1dc4ef 全部 diff `git show … --stat` **零命中** ✓
  - `src/middleware/auth.ts`（D590 领地）：零命中 ✓；`src/l1-interaction/web-adapter.ts`：零命中 ✓
  - `src/mcp/`（D595 领地）：零命中 ✓（扫描报告 2.1/2.2/2.3 的 mcp 条目均为动态，属后续批次，D603 未动——与 brief Q2 一致）
  - `electron/main.cjs`、`electron-renderer/src/main.jsx`（D602 领地）：零命中 ✓
- **commit message 一致性**：24eba4e4 message 自述与 diff 完全对应（TUI 清理 8 文件 / 六簇修复名单 / 基线 68→42 / 文档拉平 / 测试适配），无夸大项。✓

### A2 gate 契约测试两度重写（EXPECTED_68 → 42(file:line) → file=count 双向）

- **版本链**：2476edfa(#441, CT-64) 280 行新版 → 24eba4e4（7c1e150d，61 行改，file:line 逐行 + EXPECTED 42）→ 98cea78f/main（75b4e607，92 行改，**file=count 双向比对**）。
- **新断言是否弱化**：否。新旧对比：
  - 旧（file:line 逐行）：在 diagnosis/server.ts 因 D600/D593 合并漂移 7 行实证被打破 2 次（测试头注 :26-27 自述）——**行号断言不免疫合并行号漂移**；
  - 新（file=count 双向，check-architecture-gate.test.ts:102-114）：`mismatched`（基线有、实际不符——**含计数降至 0 的删除**）+ `extra`（基线外新增）+ `status=0` 三断言，且解析基线为唯一权威粒度（:39-51）。
  - 覆盖矩阵（:13-23）：新增（本地 exit0 点名 / SYNO_CI=1 exit1）、棘轮双向（超=新增 / 低于=收紧不阻断）、零误报四形态、动态形态、名字豁免删除回归、arch-allow 透明、exit 2 fail-closed——比旧版**只多不少**。
- **独立沙箱复核**（自己构造，非复述测试）：
  - 新增违规（`routes/new-violation.ts` 静态 SessionStore + 动态 sentinel/registry，空基线）→ 本地 exit 0 且逐行点名（:1/:2），`SYNO_CI=1` → **exit 1** ✓（/tmp/k3sbox 实跑记录）
  - 基线存量删除（基线 `routes/fixed.ts=3`，源树无该文件、仅干净文件）→ **脚本 exit 0 且无"收紧"提示**——发现 P2-1：脚本仅对"文件存在且计数降低"提示 FIXED（check-architecture.sh:161），**整文件消失＝0 命中的对比路径缺失**（CI 层 vitest 真实仓库 `mismatched` 断言可拦，故 P2 非 P1）。
- **基线 68→42 合法性**：对照扫描报告 §二 逐簇分类——静态 26（L1→L3 3 + L1→L4 4 + L1→L5 19，含 11 处 getDatabase 静态形态 + chat.tsx:15 cron 静态）与基线下调注释完全对应（l1-cross-layer-baseline.txt:9-12）；"42 动态仍在"实跑证实——我运行 `bash scripts/check-architecture.sh`（main 实态）输出 13/11/18 = **42 处全部为 `await import(`/`import(` 动态形态，静态零残留**，逐文件计数与基线文件 13+11+18 条目**双向完全一致**（vitest 真实仓库用例 + 我手动核对双通过）。

### A3 「L1 行为零变化」抽查（全部物理核验通过）

| 文件 | 核验结果 |
|---|---|
| `src/routes/sessions.ts` | getStore 改为 `getStore(req)`：`app.locals.sessionStore`（server.ts:261 注入 Bootstrap Phase 0 单例）优先，未注入回退 `createSystemSessionStore()`（L2 工厂 = `new SessionStore(getDatabase())` 逐字节等价，session-storage-service.ts:50-54）——调用点 6 处全部有 try/catch 保留（铁律 24），异常语义不变 ✓ |
| `src/routes/admin-knowledge.ts` | `knowledgeStore ??= createSystemKnowledgeStore()`：工厂内 getDatabase() 抛错 → ??= 不完成赋值 → null 重试（degraded 注释 :44-50 明示），与旧 `??= new KnowledgeStore(getDatabase())` 语义逐点等价 ✓；`tests/routes/admin-knowledge.test.ts:46-58` mock 包住 L2 工厂（CountingKnowledgeStore 仍计数构造）——T5b「构造仅 1 次」断言语义**未弱化**（该测试属 D603 同批 8 文件 88 用例声明，本审计只单验此一处） |
| `src/routes/auth.ts` | getUserStore() try-catch：`createSystemGraphStore()` 抛错（fail-closed）→ 降级内存 Map 路径保留（:42-50 catch 段未改动语义）✓ |
| `src/routes/agent-observer.ts` | `locals.graphStore ?? createSystemGraphStore()`——注入优先 + L2 兜底，修改前=直构，行为链等价 ✓ |
| `src/routes/ontology.ts` | `getDatabase` import 整行删除；仅剩 `import type { SqliteGraphStore }`（type-position）——`git grep -n "getDatabase" origin/main -- src/routes/ontology.ts` **零命中**，无运行时引用 ✓ |
| `src/routes/overflow.ts` | 转发 `agent/cycle-snapshot-service.ts`（纯 re-export，签名原样）✓ |

### A4 外来骨架 brief 清除

- **tracked 历史**：24eba4e4 加入 `D603-并发测试-4/7/16.md`（36 行/个，内容含 `认领: <agent>`/`<本任务在哪一层>` 骨架占位符）→ 98cea78f `git rm` 移除（stat 三项 −36×3）→ **main tree 零残留**（`git ls-tree -r origin/main .claude/task-briefs/ | grep -c 并发测试` = **0**）✓ 清除干净。
- **工作区残留**：产品工作区 `.claude/task-briefs/` 未跟踪文件 86 个，其中 D602~D607「并发测试-N」骨架 20+（含 `2026-09-09-D603-并发测试-10.md`，与已删的 4/7/16 不同文件，属**另一并发 session** 的占位符）→ 未入库，但工作区污染属实（P1-5，C 节细答）。
- **为何能进提交（L4）**：D547 门禁存在（pre-commit-check.sh:865-869，`认领: <agent>`/`<本任务在哪一层>` 硬检查，且接线测试 skeleton-brief-gate.test.sh 在 canary）——但 D515 本地软提示语义下 24eba4e4 本地提交被放行（bypass.log 显示 3×GATE_FAIL_SOFT + COMMITTED，见 P1-4）；**CI 权威**本应在其后拦截，但 #449 squash 的 PR head（16:43）已不含骨架（09:03 已删），CI 无中间态检查 → 骨架从未经 CI 门禁校验。**缺什么**：CI 对"PR 基底→head 的每步提交"无逐 commit 门禁（只有 head 态）；对 feature 分支的 CI 运行记录无可查证（GITHUB API 不可达，证据缺口）。

### 必核项其余（全部通过）

- 铁律 40-45 冻结项：`patches/ink+5.2.1.patch` 存在 ✓；package.json:12 `"postinstall": "patch-package"` ✓；React.memo 在 Message/StreamingText 组件（message.tsx:106、chat-panel.tsx:54、header.tsx:15、section-panel.tsx:20）✓；`git grep -E "LineBuffer|FrameRateLimiter|StreamChunker"` 零命中 ✓；`justifyContent.*flex-end` 零命中 ✓；chat.tsx diff **仅 1 行 import + 注释**（+2/−1），流式逻辑零触碰 ✓。
- 删除 8 文件零引用：`git grep -c` 在 `src/ package.json scripts/ electron*` —— tui-v3×3 / tui-v2/{index,app,demo} / use-event-bus / tui-adapter-v2 全部 **0**；唯一"引用"为 `src/tui-v2/README.md:6-7`（**描述删除事实的说明性文本**）与 docs/ 历史存档（SYNOVA-MASTER html、audit baseline txt——历史记录，P2 级不处理）。
- `npm run tui`：见 §2 环境注记（启动渲染成功，raw-mode 为无 TTY 环境错误）。
- 权威文档05 勘误：`git log --all -S "/api/chat/stream" --oneline -- src/routes/chat.ts` → **零命中**（与勘误注记"全历史零命中——该端点从未存在"一致；全库无 pathspec 时有 6 次命中，其中 5 次为文档/勘误文本自身、b54a0fb6 为 D590 相关文档——chat.ts 本身零命中，主上 chat.ts 路由实际仅 GET /api/status、GET /api/user-state、POST /api/proposal/:id/resolve、GET /chat（:24/:36/:56/:83）✓）；勘误注记 + 两处行内 `[勘误-D603]` 标记（权威文档05 补充研究 :9-19/:36/:190）✓。
- CLAUDE.md 铁律 40/41/44 拉平：CLAUDE.md 六章三处的"执法现状（2026-09-09 D603 拉平）"与 `memory/notes/implemented/2026-09-09-D603-iron-law-40-41-44-enforcement-flattening.md`（决策/理由/变更清单）**一致**；Note 诚实限定"42/43/45 的「pre-commit 警告」声称同属漂移但本轮不动"——范围声明可查 ✓（42/43/45 漂移仍在，作为遗留漂移记录：P2）。

### 对象 1 结论：PASS（内容侧零 P0/P1；合并通道 P0-1 见 §1）

---

## 4. 对象 2-8 逐对象结论

### 对象 2 — D602 交互卡片+preload/IPC+通知（#448，749c3b51）｜ CONDITIONAL PASS
- 提交在 main ✓（main log 09-09 08:25）；task-state/D602.json 已登记 impl_done ✓（commit: 749c3b51）。
- 内容：electron/preload.cjs（contextBridge + ipcRenderer.on 返回退订函数，:15-42）、electron-renderer/src/hooks/useNotifications.ts（degraded/error 三路径 :142-157）、NotificationCenter.tsx +161、useStreaming.ts +7、ipc/bridge.ts +55、app-store.ts +62、electron/main.cjs +93、tests/electron/{ipc-contract,notification-center}.test.ts（191+428 行）——具备配对测试 ✓。
- **P1-1**：派单材料表 dev doc 路径 `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D602-interactive-cards-preload-20260908.md` 在主上**不存在**（`git ls-tree` 全库仅 D600 一间）；实际 spec = task brief（.claude/task-briefs/2026-09-09-D602-interactive-cards-preload.md，95 行）+ 证据集 docs/synova/audit-reports/D602-ipc-notification-evidence-20260909/（README + ds4/ds5 证据 + logs）。→ 材料表失准或 dev doc 缺失，二选一，均需记录。
- 降级诚实性抽查：useNotifications.ts:151/157（HTTP 非 2xx → degraded+error 文案；形状非法 → degraded）✓。

### 对象 3 — D600 CFG 产品化诊断第一命令（#446，45de7a03+d0a903e1+e8db4647）｜ PASS
- 在 main ✓（merge e8db4647）；dev doc 存在（SYNOVA-IMPL-D600-cfg-productize-first-command-20260908.md）✓。
- src/routes/config.ts（+60）：JSDoc 契约 + 400 校验/200 degraded/防御兜底三路径（config.ts 全篇，:24-58）✓；diagnosis.ts +50（reportDepth/template 客户配置驱动）；tests/routes/config.test.ts（142 行）+ diagnosis-customer-config.test.ts（196 行）配对 ✓。
- **P1-3**：task-state/D600.json 未登记（见 §6）。
- 北星三问：服务 GA/运营者的"实际生效配置检查表面"，朝"产品化诊断第一命令"终态走一步，产品形态不变 → **对齐**。

### 对象 4 — D601 看板口径校准+L1 跨层扫描（#435，95e1c9fa）｜ PASS
- 95e1c9fa 在 main ✓；**8193e838 非 main 祖先**（exit=1）——内容（扫描报告 178 行）经 squash 合并在主 ✓；派单提交集映射失准（P2 记录）：名义"两个提交"实为 squash 单提交。
- 产出 `docs/synova/research/L1跨层违规扫描-20260908.md` 在主 ✓ 且与对象 1 双向核对一致（§二 68 清单 ↔ 基线 68→42 ↔ 实跑 42）✓。
- task-state/D601.json 已登记 impl_done ✓（其 title 注明"改号自 D600"——印证 P1-3 的 D600 编号绕行记录缺失）。

### 对象 5 — D597 D588 会话投影接线（#421，e8aafc8f）｜ PASS（终验收口闭合）
- e8aafc8f 在 main ✓（ancestor exit=0）；#423 挂的"待合 main"CONDITIONAL 条件已消除。
- 契约：`src/deploy/bootstrap.ts:24-27` 注释（"副作用导入——session-projection 模块加载时原型级 wrap…无此行 = 投影死代码"）+ `import '../store/session-projection'`（:27 实际存在）✓；`src/store/session-projection.ts` 存在且 `src/store/retry-projection.ts:28,105` 引用（D588 范式：import 即接线）✓。
- task-state/D597.json 已登记 ✓；8 个生产构造点零修改的声称与"原型级 wrap"实现自洽（本审计仅核接线存在，不重验 wrap 单元行为——#421 时已审）。

### 对象 6 — CT-64 四类漏网修补（#441，2476edfa=squash(bf517ecf,722fa6df)）｜ PASS（P2×2）
- main 上 `scripts/check-architecture.sh` hash 与 fix 分支 tip（83601241）**完全一致** ✓（squash 内容无损）；四类全验证：
  - ①动态 import：vitest「修补1回归」命中 await import(/import(/require( ✓；我的沙箱新增违规也被点名 ✓
  - ②名字豁免删除：`grep -v "agent-observer|ga-annotations"` 已删（测试「修补2」用两违规文件回归 ✓）；本审计另行 grep 脚本确认无 grep -v 名字豁免残留
  - ③路径形态：expert-platform/store/cron/l5/init-engine-context/sqlite-graph-store/cycles 全命中（测试「修补3」7 行逐行点名 ✓）；扫描范围扩至 L1 99 文件集（附录 A）
  - ④fail-closed：扫描目标缺失 → exit 2（测试「三态退出码」+ 我沙箱空目录实跑 exit 2 ✓）
- 零误报沙箱（类型四形态/URL/注释/L2 合法引用 / arch-allow 行内豁免透明）✓ 12/12。
- 棘轮双向：超基线（同文件同方向 count↑）→ SYNO_CI exit 1 ✓；低于基线 → 提示"收紧"不阻断 ✓（测试及我实跑）。
- P2-1/P2-2 见 §0。

### 对象 7 — CT-63 alloc-task-id 扫远端分支名（#445，92b094de）｜ PASS（P1-2 接线缺口）
- 92b094de 在 main ✓；实现：`scripts/control-tower/alloc-task-id.sh:128-146`（`git branch -r --format` 抓 D[0-9]+ 合入占用表 + SYNO_ALLOC_NO_BRANCH=1 测试注入缝）✓。
- **物理实跑**：`bash tests/control-tower/alloc-task-id.test.sh` → **PASS=15 FAIL=0**（含 `7. CT-63: 分支 d605 → 发 D606 > 605` ✓、`8. NO_BRANCH → D500` ✓）。
- **P1-2**：该配对测试**未进 CI canary**——ci.yml control-tower-tests job 27 项清单（:152-180）无 alloc-task-id.test.sh；全仓 grep 仅脚本内注释提及 → 测试只在本机手动跑 = 铁律 0-2 接线违反（D329 P2-2 同款）。→ 建议：进 canary 或 pre-push。
- 盲区回答：D600 被 Codex 线占用先例 → "扫远端分支名"确实堵住（分支名 D# 是跨线只有在途信号，实现含大小写不敏感 D[0-9]+）✓ 但注意：**远端分支名≠已登记占用**，若并发线不开分支（仅本地）仍漏——残留盲区（P2 记录进 L4）。

### 对象 8 — #449 SYNO_CI 注入 architecture job｜ CONDITIONAL PASS
- ci.yml architecture job **job 级 env** `SYNO_CI: "1"`（ci.yml:105-106）——覆盖 job 内全部 step（Layer boundary check + GraphStore compatibility），层级正确 ✓。
- **注入后 42 存量全绿**：主态 `bash scripts/check-architecture.sh`（SYNO_CI=1）→ **exit 0**（存量棘轮不阻断）；vitest「真实仓库 SYNO_CI=1 exit 0」✓；双向比对 42=基线 ✓ → **基线与 main 实态相符，无 P0** ✓。
- 本地复现：`SYNO_CI=1 bash scripts/check-architecture.sh` 在隔离工作区与 CI 语义一致 ✓。
- 但本对象 = **P0-1 通道本体**（§1）；且无配对测试（P2-3）。

---

## 5. 控制塔三连交叉核验（B 节必答）

| 门禁变更 | 配对测试 | 进 CI？ | 结论 |
|---|---|---|---|
| CT-64 四类漏网（#441） | check-architecture-gate.test.ts（12 用例） | ✓ architecture job `vitest run tests/architecture/`（ci.yml:114） | 完好 |
| #449 SYNO_CI 注入 | 无专门测试（ci-strict-visible.test.sh 为 D515 既有） | — | P2-3 |
| CT-63 扫远端分支（#445） | alloc-task-id.test.sh（15 断言） | ✗ **未接线** | **P1-2** |

- canary 27 项清单与主上 ci.yml **逐项核对存在**（verify-parallel → verify-parallel-ci 共 27 项）✓——但"27 项"固定清单自身缺增员流程记录（CT-63 该入未入，无人发现）。
- 交叉结论：三次门禁变更中 2/3 有 CI 级配对保护；CT-63 的缺口恰是"功能正确但保护未接线"——与 D329 P2-2、D328 接线失败四次的同型模式（铁律 0-2 反复失守点）。

---

## 6. 编号体系盲区（C 节）+ task-state 回填建议

**事实**：
- `task-state/`（**仓库根**，非派单所指 docs/.../task-state/——后者仅 3 个遗留文件）：已登记 D570-D579、D590-D597、D601、D602（均 impl_done/spec_done）；**D600.json、D603.json 不存在**。
- D601.json 的 title 自注"（改号自 D600）"——**D600 改号绕行有记录，D600 自身无登记**；D603 是"CTO 直派"同样无登记。
- 产品工作区 `.claude/task-briefs/` 未跟踪 86 文件（D602-D607 并发测试骨架 20+ + 其他会话产物）→ 无归属、无人认领、永不入库也不清理。

**哪道防线该拦、为何没拦、缺什么**：
1. "无登记先合入"：**分配器/控制塔没有"合并门禁读 task-state"**——CI 无 job 检查 PR 标题 D# ↔ task-state/D#.json 存在且状态合法。缺：CI 新增 `task-state-registry gate`（PR 标题解析 D# → 必须存在 task-state/D#.json 才放行；docs-only 例外需显式声明）。
2. "骨架 brief 工作区污染"：D547 门禁只拦**已暂存**的骨架（pre-commit-check.sh:862-869），未跟踪文件永远不可见；多 session 共享主树（M8）使每个 session 的 alloc 产物堆积。缺：alloc-task-id 的骨架 brief 生成后需在"认领时写入 task-state + 一周内未认领自动失效"的生命周期；或会话结束时清理脚本。
3. "改号绕行"（D600 被 Codex 线占 → 改 D601）：改号有"改号自 D600"记录（D601.json title），但 D600 编号本身无 task-state 文件 → 编号被占用的**证据链断裂**。缺：alloc-task-id 对"被占用编号"也应落一个占位 state（occupied/transferred）。

**回填建议（格式对照 task-state/D601.json：task_id/title/status/spec/impl{commit,by,at,files}/audit/fix_task_id/updated_at/updated_by）**：
```json
// task-state/D600.json（建议）
{ "task_id": "D600", "title": "CFG 产品化诊断第一命令（W 线，改号自 D601 占位）", "status": "impl_done",
  "spec": "docs/plans/codex/implementation/SYNOVA-IMPL-D600-cfg-productize-first-command-20260908.md",
  "impl": { "commit": "45de7a03", "by": "dsh-cto", "at": "2026-09-09", "files": [ "src/routes/config.ts", "src/routes/diagnosis.ts", "tests/routes/config.test.ts", "tests/routes/diagnosis-customer-config.test.ts" ] },
  "audit": null, "fix_task_id": null, "updated_at": "2026-09-09", "updated_by": "dsh-cto" }
```
```json
// task-state/D603.json（建议——含合并通道异常注记）
{ "task_id": "D603", "title": "L1-P3 TUI 清理+跨层静态 import 清零+文档拉平", "status": "impl_done",
  "spec": null, "impl": { "commit": "01e39a7e", "by": "dsh-dev", "at": "2026-09-09",
    "files": [ "src/routes/sessions.ts", "src/agent/session-storage-service.ts", "tests/architecture/check-architecture-gate.test.ts", "tests/architecture/l1-cross-layer-baseline.txt", "CLAUDE.md", "docs/synova/research/权威文档05-补充研究-线程流式子Agent交互-20260727.md" ],
    "note": "K3-20260909: 代码内容实际经 9e5a44cd(#449) 夹带进 main（P0-1），#451 仅合入派单/编码指令文档；提交集 24eba4e4/98cea78f 非 main 祖先" },
  "audit": null, "fix_task_id": null, "updated_at": "2026-09-09", "updated_by": "dsh-cto" }
```

---

## 7. 审计基线（材料 6）

`python3 scripts/audit/audit-check.py --full`（审计工作区、main 实态）→ **PASS:2 WARN:967 FAIL:476**（exit=1）。协议文档基线口径（2026-08-11 记录）：439 FAIL / as any 0 / arch 56。
- **as any = 0**（audit-check 输出 :451 "0 as any occurrences"）——铁律 38 主指标与基线一致，本批零新增 ✓。
- FAIL 476 vs 439（+37）：归因分析——审计脚本自身为**全库扫描**，D590/D593/D599/D600/D602 批（SEEs、customer-config-package.ts 空 catch 3 处、新 routes）在本批之前已合入 main，+37 主要来自**本批之前**的批次增代码；无法逐条与 439 快照 diff（无人保存 8-11 审计输出快照，历史遗留——P2 记录：建议 audit-check.py 每次输出留档）。
- arch（Python 原生扫描器）：63 处——与 56 基线差 +7，其中 L2→L4/L2→L5 老违规（subagent-coordinator.ts:8、session-manager.ts:51/60）与 L1 动态 42 混合；**D603 固定文件（sessions/admin-knowledge/expert/ontology/…）在此扫描器输出中零命中**（静态残留为 0）✓。D603 范围外类型引用仍存（mcp/index.ts:171 等，§2.4 低危，后续批次）——与申报一致。
- 结论：基线与主实态无"本批引入的类型安全/静默 catch 回归"；47 处静态 import 清零未引入新基线缺口。

---

## 8. 14 项语义审计表（对象 1 全表；对象 2-8 见 §4 要点）

| # | 项 | 对象 1 判定 | 证据 |
|---|---|---|---|
| 1 | 接口真实性 | ✓ createSystemKnowledgeStore/createSystemGraphStore/createSessionStorage/getExpertStore/startSynovaAgentProcess 等全部真实存在且被调用（grep 各 routes/cli/index 调用点） | session-storage-service.ts:35-55、graph-store-service.ts:21-27、agent-entry.ts:25-37 |
| 2 | 文件路径存在性 | ✓ 新增 6 服务 + agent-entry 全部存在；删除 8 文件全仓零引用 | §3 必核项 |
| 3 | Edge ID | 不适用（无 extensions/ 变更） | — |
| 4 | 架构边界 | ✓ 静态 26 清零实跑证实；新增 L2 服务均为 L2→L3/L4/L5 合法方向 | §3 A2 |
| 5 | 数据流 | ✓ DI 注入链：Bootstrap Phase0/Phase1c → app.locals.sessionStore/graphStore → routes 消费；兜底链 L2 工厂 | server.ts:258-262、sessions.ts:11-14 |
| 6 | 接线深度 | ✓ 新 export 均被路由/CLI/TUI 调用（非只 import） | 各调用点 grep |
| 7 | 降级诚实性 | ✓ 兜底工厂 fail-closed 抛错 + 调用方 catch/log（auth 内存 Map 降级、admin-knowledge ??= 重试、config 200+degraded） | config.ts:50-58、admin-knowledge.ts:44-52 |
| 8 | 测试契约（铁律 47） | ✓ 新 L2 服务全部 JSDoc 契约（input/output/degraded） | 各服务头注释 |
| 9 | 测试覆盖（铁律 48） | ✓ gate 12 用例 + admin-knowledge mock 适配（T5b 语义不变）+ 6 个 agent 配对测试（98cea78f 修复项） | tests/architecture/…test.ts:101-296 |
| 10 | 文件驱动 | 不适用（无新 manifest 类型） | — |
| 11 | 自报告交叉对比 | 多数属实（26 清零/42 动态/零引用/铁律冻结/文档拉平）；"2 笔 GATE_FAIL_SOFT"不实（实 3 笔，P1-4）；"写集扩大"前提不真实（A1）；"tsc 28=28"无法独立核验（无 CI 证据，P2） | §3 A1/A4、§0 P1-4 |
| 12 | dev doc 偏离 | 无 dev doc（任务书免 spec）——按 brief 判；brief Q2 与实际写集**零偏离**（含 13 文件） | A1 |
| 13 | 控制塔执行 | 本地 3×GATE_FAIL_SOFT+COMMITTED 链存在；CI job 级不可得（无凭证，证据缺口）；门禁测试本地全绿 | §1/§2 |
| 14 | 版本编排 | 本批无 VERSION.md 变更（正确——未授权不碰；对象 1-8 无版本号提交） | git log 各提交 stat |

---

## 9. L4 防线缺口（每对象必答）

| 对象 | 问题 | 本该拦的防线 | 为何没拦 | 缺什么 |
|---|---|---|---|---|
| 1 P0-1 | #449 夹带 D603 内容进 main；#451 名义合并零代码 | DSH pr-review（铁律 0-5）/ PR 范围检查 | PR 审查在 squash 界面核对 head 状态而非"diff vs 声称范围"；基底（未合 D603 分支）无规则约束 | PR 级文件集 vs 声称范围门禁 + "分支基底必须已合入"规则 + 合入后 k3 复核 merge-diff |
| 1 A4 | 骨架 brief 进入 24eba4e4 | D547 pre-commit 硬检查 | D515 本地软提示放行；CI 只验 PR head（骨架已在头前删除） | CI 必须有"该 PR 基底→head 全 diff"预检（对 squash 亦然）；feature 分支 CI 记录留档 |
| 2 | dev doc 缺失 | 派单/材料准备（铁律 0 对齐） | 材料表路径照抄未验证（claim-verifier 漏用） | 派单材料逐一 `git ls-tree` 验证后再发 |
| 3/1 | task-state 未登记即合入 | 分配器/合并门禁 | 无 job 读 task-state；登记靠自觉（D601/D602 登记了，D600/D603 漏了） | CI task-state 注册 gate |
| 4 | 提交集映射失准（8193e838 不在 main） | 派单提交集核验 | 单提交核验（D320 教训的反面——把 squash 误当双提交） | 派单前跑 `git branch --contains`/ancestry 验证每提交 |
| 6 P2 | 删除-至-零 无收紧提示；tui-v3 扫描清单残留 | CT-64 测试矩阵 | 测试矩阵未覆盖"整文件消失"边界 | 补 1 沙箱用例（基线有、源全无 → 应提示收紧） |
| 7 P1-2 | CT-63 测试未接线 | 铁律 0-2 WIRE CHECK | 接线检查只适用于"新 export"，测试文件本身无人 grep CI 接线 | canary 清单增员走 PR 自检（改 canary 须列新增项）+ 审计交叉核 |
| 8 P2-3 | #449 无配对测试 | 控制塔变更模式库（ctrl-tower-change 模式 1） | env 注入类改动未进"新能力必须接线"意识 | 改 ci.yml 也须配对测试（或至少 canary 记录） |

---

## 10. 修复后复审范围（协议步骤 6）

P0-1 的处置建议（非本审计越权——仅记录）：Codex 方向：① 立即在 `task-state/D603.json` 与台账登记通道异常（§6 模板）；② 决策是否将 9e5a44cd 的提交拆解/改写（rebase 重建提交链）或接受现状并补登记——由创始人裁决；③ CT-63 测试接线（P1-2）与 canary 增员。

复审仅审变更部分：`task-state/D600.json`、`task-state/D603.json`、ci.yml canary（若采纳）、check-architecture.sh（若采纳 P2-1/P2-2）。

---

## 附录 A：实跑命令索引（可复现）

```bash
# 合并态
git fetch origin && git merge-base --is-ancestor 24eba4e4 origin/main   # 1（非祖先）
git show 9e5a44cd --stat                       # 51 文件 +860/-954（夹带实证）
git show 01e39a7e --stat                       # 3 文件 +119（名义合并实证）
# 门禁实跑（隔离工作区 = main 实态）
bash scripts/check-architecture.sh                     # exit 0，输出 42 处动态存量（13/11/18）
SYNO_CI=1 bash scripts/check-architecture.sh           # exit 0（存量棘轮不阻断）
npx vitest run tests/architecture/check-architecture-gate.test.ts   # 12/12 passed
bash tests/control-tower/alloc-task-id.test.sh          # PASS=15 FAIL=0
python3 scripts/audit/audit-check.py --full             # FAIL:476 / as any 0
# 沙箱（A2 自构造）
SYNO_ARCH_SRC=/tmp/k3sbox/new SYNO_ARCH_BASELINE=empty bash scripts/check-architecture.sh  # exit 0 + 点名
SYNO_CI=1 SYNO_ARCH_SRC=/tmp/k3sbox/new SYNO_ARCH_BASELINE=empty bash scripts/check-architecture.sh  # exit 1
```

> 审计员声明：本报告仅记录物理证据，未修改产品仓库任何文件；报告提交于审计分支 `audit/k3-20260909-l1-closeout`（不 push main、不合并）。归因：P0-1 → control-tower；P1-2/P1-3 → control-tower+devdoc；P1-4 → audit（派单材料核实口径）；P1-1 → devdoc；P2 系列 → implement/devdoc 记录跟进。
