# K3 独立审计报告 — D593-FIX 复审 + D595 首审 合并批

> 审计员: Kimi K3（独立会话，零上下文）| 2026-09-10
> 基线: origin/main @ 5494e6f6（D593-FIX #457 + D595 #459 均已 squash 入 main）
> 审计分支: audit/k3-20260910-d593fix-d595
> 协议: docs/synova/coordination/AUDIT-PROTOCOL.md v1.1（L1-L4 四层 + 15 项清单）
> 运行环境: Mac（审计隔离 clone `.audit-clone-d593d595/`，独立于产品主工作区）| Node 22.22.0
> 前审报告: docs/synova/audit-reports/2026-09-09-K3-D593-D602-closeout.md（D593 FAIL：P0×2）

---

## 0. 结论总览（先读）

| 对象 | 前审 | 本次结论 | 关键发现 |
|---|---|---|---|
| **D593-FIX**（#457，f5f9c462） | FAIL（P0×2） | **维持 FAIL** | **P0-1 未修复**：修复提交 9c3f1672 与合并 diff **均不含 src/routes/diagnosis.ts**（12/13 文件，无一代码文件）。main 上 diagnosis.ts 四块恢复面（consult 落盘/冷读 fallback/列表端点/FIFO 注释）**全部缺席**；独立实跑 persistence+right-panel = **10 failed/11 passed**，与修复前 red 证据同签名。evidence 包 green 21/21 是 **worktree 态产物**（手术真实发生过，改动从未提交——D315 教训复刻）。**P0-2 部分修复**：heartbeat/D595 两文档/D599 note 4 项真正消失；3 个 gitlink（ct64/d590/d593）被晚 2 分钟合并的 #459 复活。 |
| **D595**（#459，91948780） | 首审 | **CONDITIONAL PASS** | 实现质量高：12 工具 permission 分级、-32000 权限门、fail-closed 模式、L2 化（棘轮 42→36 物理成立）、白名单 3 前缀行为收口；独立实跑 tool-definitions 38/38 + server-smoke 9/9 **47/47 全绿**；filterToolsByMode/assertToolAllowed 生产接线（index.ts:54/:61）属实；仲裁续接事实物理成立（首 session 01:53 → 续接 20:47）。**条件**：①写集漂移 P1-1（evidence 7 件 + 补实 brief 未随 PR 入库）；②spec 载体双缺失 P1-2（spec 被 D593-FIX 删除未恢复 + main 上 brief 是骨架模板）；③gitlink 夹带 P1-3（6 个写集外 gitlink，P0-2 同族 12 小时内第三例）。 |

**分级计数：P0 ×1（D593 维持）｜ P1 ×3（全属 D595）｜ P2 ×5**

**一句话**: D593 的修复在 worktree 里真实做过（red→green 证据不假），但**核心文件从未进入提交**——main 上的 D593 仍是前审时的半成品，修复合并消息声称的恢复面与合并树差集 = 整个 diagnosis.ts。D595 的代码交付质量是本批正面样本（47/47 绿、接线真实、协议诚实），坏在**交付通道**：证据/载体没入库、夹带 gitlink 第三次从合并通道漏入。

---

## 1. 运行环境注记（D316 教训——环境依赖判别先行）

- 审计在**独立 clone** `.audit-clone-d593d595/`（checkout 5494e6f6，分支 audit/k3-20260910-d593fix-d595）执行；npm ci 首次失败 = sandbox 拒写 `~/.npm/_logs`（exit 255，审计环境问题），改 `--cache /tmp/npm-cache-d593d595` 后成功——D595 测试首红为此环境依赖，重装后全绿，已排除。
- 全部测试结论 = 本审计 clone 独立实跑（npx vitest run），非引用证据包。
- **干净安装环境差异**：`tests/electron/use-streaming-conversation.test.ts` 在干净 npm ci 下 import 失败——`electron-renderer/src/components/MessageItem.tsx:11` import `react-markdown`，而 package.json/lock **无此依赖**（存量缺口，开发机主工作区靠提升偶然可用；前审 9-9 报告的"21/21 绿"为污染安装态）。全量 vitest 失败计数 ±1 与 main 基线 26 的差异即源于此，非本批引入。

---

## 2. 对象 1 — D593-FIX 复审（前审 P0 逐条对照）

### 2.1 提交集与写集对账

| 提交 | 角色 | diff 内容 |
|---|---|---|
| 9c3f1672 | 修复主体（fix/d593-restore） | 12 文件：brief + evidence×3 + **删除 7 项夹带** + task-state。**无 src/routes/diagnosis.ts** |
| 57596af2 | bypass 登记 | .claude/bypass.log +1 |
| f5f9c462 | PR #457 squash | = 9c3f1672 内容 + bypass 行（13 文件，无 diagnosis.ts） |

修复 brief Q2 做什么清单第一项 = `src/routes/diagnosis.ts`（.claude/task-briefs/2026-09-09-D593-FIX-restore.md:42）——**声明的核心文件未进入提交**。

### 2.2 P0-1（合并吞实现）复审——**未修复，维持 P0**

main @ 5494e6f6 上 diagnosis.ts 物理现状：

| 修复声称恢复的面 | main 现状（file:line / grep） | 判定 |
|---|---|---|
| ① consult 完成点落盘 saveDiagnosisCheckpoint(键=reportId) | `grep -n saveDiagnosisCheckpoint src/routes/diagnosis.ts` → **0 命中** | ✗ 未恢复 |
| ② GET report 内存 miss → 冷读 fallback（readReportFromCheckpoint/respondReport/ColdReadArchive/isFullDiagnosisReportLike） | 全 0 命中；GET /consult/:id/report 端点仍 `completedReports.get(consultId)` → 404（diagnosis.ts:645-648） | ✗ 未恢复 |
| ③ 列表端点 GET /api/diagnosis/reports | `git grep "diagnosis/reports" origin/main -- src/` → 仅 auth.ts 白名单 + 文档；**路由不存在** | ✗ 未恢复 |
| ④ FIFO 注释诚实化（内存 Map 退役一级缓存） | diagnosis.ts:118-127 仍 D480 旧注释"有界缓存写入（Map 插入序 FIFO 淘汰最旧）" | ✗ 未恢复 |

- **派单声称核验**："getDiagnosisCheckpoint 冷读 = 1 命中（diagnosis.ts:613）"物理为真，但 `git blame` 证实该行 = **453e9988d（2026-06-15，resume 端点 checkpoint 加载老代码）**，与 D593-FIX 无关——**1 命中 ≠ 修复落地**（派单材料口径失准，P2）。
- **独立复跑**（本 clone）：`npx vitest run tests/routes/diagnosis-report-persistence.test.ts tests/electron/right-panel-report-sentinel.test.ts` → **10 failed / 11 passed (21)**。失败签名与 evidence 包 red-before-restore（10 failed/2 passed）完全一致——**main 现状 = 修复前状态**。
- **green-after-restore.log.txt 溯源**：21/21 全绿（20:19:41），运行路径 `/Users/wane/SynovaAgent/.synova-wt-d593`——**worktree 态产物**。手术真实发生过（green 日志不假），但改动从未 add/commit（9c3f1672 diff 无该文件）。D315"声称交付但 38 文件未提交"教训的复刻。
- **evidence README 声称核验**："K3 独立重跑 … 21 全绿"——main 上实测 10 failed，**声称不实**（M2 族）。
- **全量 vitest 旁证**：25 failed（≈修复前 main 基线 26）——若恢复落地应 ≈16（persistence 10 failed 转绿）。
- **归因: implement**（提交丢失核心文件）+ **M9 族**（前审提议的"合并冲突消解吞实现"新类，本次在修复通道复犯：修复消息声称 ⊋ 修复树）。

### 2.3 P0-2（夹带清理）复审——**部分修复**

| 夹带项（前审 P0-2） | main 现状（git ls-tree origin/main） | 判定 |
|---|---|---|
| .codex/heartbeat.json | ABSENT | ✓ 清理成功 |
| docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D595-mcp-auth-20260908.md | ABSENT | ✓ 清理成功 |
| docs/synova/coordination/编码指令-D595-MCP认证权限-20260908.md | ABSENT | ✓ 清理成功 |
| memory/notes/proposed/D599-dev-doc-交付前置复核与指令内嵌.md | ABSENT | ✓ 清理成功 |
| .synova-wt-ct64 / .synova-wt-d590 / .synova-wt-d593 | **PRESENT（160000 commit gitlink）** | ✗ 被 #459 复活 |

复活机制：D595 分支 base 落后于 #457（其最后一次 merge main = #455），#459 squash 时把分支上自带的 6 个 gitlink（ct64/d590/d593 复活 + d595-cont/d602/.sessions/d595-mcp-auth/repo 新增）全量带入 main。**归因: control-tower**——合并 diff 白名单核验 gate 缺失（前审 L4 已提议，12 小时内第三例：D593-FIX 删除 20:45 → #459 复活 20:48）。

### 2.4 D599/D600 零误伤核验

- customerConfig 现状 **15 处**；#442 前（c7eb8e0d^ = 2476edfa）**14 处**；第 15 处 = `git blame` → **45de7a03（D600, :499）** `readDiagnosisConfig(customerConfig.config)`——合法演进，非误伤。
- **结论**：D599/D600 工作零丢失 ✓（因为修复对 diagnosis.ts 零改动——"零误伤"= "零恢复"的同一物理事实）。
- 修复合并消息"customerConfig 15=15 与 #442 前一致"口径**不实**（#442 前 = 14；真实语义 = 修复前后零变化）。P2 证据口径瑕疵。

### 2.5 执行偏差两处审查（派单定向问）

1. **"整文件 checkout 改手工合并"**：决策合理（brief Q0c——保护 main 上 D599/D600 工作，最小变更面，符合 Anthropic 基线）；但**执行断链**——手工合并的产物从未进入提交。决策对、执行丢。
2. **"gitlink 删除决策"**：删除优于回退陈旧哈希（worktree 已消亡为死指针）——合理；d577 等 7 个 D590 时代遗留 gitlink 明确划出范围（brief Q2 不做什么）——范围划分诚实。但删除被后续 PR 复活 = 通道缺陷（见 L4）。

### 2.6 其他核验

- bypass 链：57596af2 `COMMITTED | pre-commit PASS` 登记经 squash 入 main ✓（.claude/bypass.log +1）。
- "tsc 与 main 基线 36=36 逐字节一致"：tsc 实测 **28**（与前审基线口径一致）；**36 是棘轮数**（且 42→36 归 D595 下调，非 D593-FIX）。合并消息表述混乱（P2）。
- conversations.test.ts 实测 **13/13 绿**（task-state note 的 13/13 对；派单材料"21/21"错——21 是 persistence 12 + right-panel 9 的合计）。

### 2.7 D593 复审 DS 逐条（main 现状）

| DS | 内容 | 判定 |
|---|---|---|
| DS1 | 两新测试文件全绿 | ✗（persistence 10 failed，独立复跑） |
| DS2 | kill -9 → 重启 → GET 200 | ✗（冷读缺席，FIFO 满后 GET 404 复现） |
| DS3 | 双路线落盘 | ✗（对话桥 ✓ / consult 落盘 0 命中） |
| DS4/DS5 | 桌面呈现 | ⚠️ 部分（electron 9/9 绿为 mock 层；列表兜底端点不可达） |
| DS6 | 白名单五前缀 | ✓（auth.ts:122-128 在 main，行为测试通过） |
| DS7 | 列表分页 | ✗（路由不存在） |
| DS8/DS9 | 接线 + 类型 | ✗/✓（接线面 listDiagnosisReports 仍零生产调用方；tsc 28=基线 ✓） |

---

## 3. 对象 2 — D595 首审（MCP 认证/权限 + 工具补齐）

### 3.1 写集对账（task-state impl.files vs 合并 diff）

- impl.files 声明 24 项；合并 diff 实际 23 文件。**声明未入库 8 项**：evidence 7 件（d595-unit-red/green、smoke-red/green/manual-transcript、vitest-baseline-diff、evidence-index）+ 补实 task brief。
- **写集外入库 8 项**：.claude/bypass.log（惯例可接受）+ **6 个 gitlink**（.sessions/d595-mcp-auth/repo、.synova-wt-ct64/d590/d593/d595-cont/d602）+ MCP-SDK 评估结论（文件名编码对比假阳性，实际在 declared 列表）。
- task-state 声称"files[] 与 git diff --name-only 24 条完全一致（D286 对账口径）"——**不实**（P1-1）。

### 3.2 DS 逐项（派单 DS1-DS12 映射，main 现状）

| 派单重点 | 物理证据 | 判定 |
|---|---|---|
| 1. 权限分级：12 工具 permission + read-only 过滤 + -32000 | src/mcp/tool-definitions.ts:85-192（12 工具，write 恰 4：sentinel_run/run_all/diagnose_organization/ingest_document）；filterToolsByMode :216-218；assertToolAllowed :226-237（-32000 PERMISSION_DENIED 含工具名+模式；未知工具 -32601）；resolveMcpMode :203-209（非法值 log.warn + fail-closed read-only）；tool-definitions.test.ts 38/38 绿（T1/T2/T3/T6 断言真实非空壳） | ✓ |
| 2. L2 化：SqliteGraphStore 在 mcp/index.ts 零引用（棘轮 11→10） | index.ts 全文 74 行零 SqliteGraphStore；哨兵族 5 工具经 L2 agent/sentinel-service（tool-definitions.ts:309-379）；src/mcp 残留仅 tool-registration.ts:104/108（D601 存量，bootstrap.ts:1276 消费非死代码）；baseline txt 总量 36（L1→L3=9 + L1→L4=10 + L1→L5=17），graphstore-unify + gate 12/12 绿（实跑 34/34 含） | ✓ |
| 3. 白名单 3 前缀 | auth.ts:130-132（/api/ontology/graph/ 前缀 + /api/ontology/ingest 精确 + /api/knowledge/ask 前缀）；DS4 行为收口（放行 3 + 401 3，DEV_MODE=false 探针） | ✓ |
| 4. SDK 迁移 | package.json:54 `^1.30.0`；package-lock +592 行纯 SDK 依赖树（@hono/node-server/express 嵌套等）；index.ts:18-20 SDK import；评估结论文档入库（docs/synova/research/MCP-SDK-评估结论-20260908.md，四维对比真实） | ✓ |
| 5. 打包 "mcp" script | package.json:15 `"mcp": "tsx src/mcp/index.ts"` | ✓ |
| 6. 撞车仲裁记录 | 首 session 22bea264 09-09 01:53 → 续接 75f3c486 20:15（"续接交付"提交 + merge ×3）→ final 35323577 20:47；"创始人仲裁『续接既有分支』"字样仅 task-state 自述，独立决策记录未检索到——**续接事实物理成立，仲裁原文无独立证据** | ⚠️ 部分可证 |
| 7. filterToolsByMode/assertToolAllowed 生产接线 | index.ts:54 `tools: filterToolsByMode(TOOLS, mode)`、:61 `assertToolAllowed(name, mode)`——真实装配（非仅测试 import） | ✓ |
| 冒烟 9 用例 | server-smoke.test.ts S1-S7 真实 stdio 子进程（S3 -32000 指纹/S6c stdout 纯度/S7 fail-closed stderr），9/9 绿实跑 | ✓ |
| DS9 零新失败 | 全量 vitest 25 failed 全与 D595 写集零交集（expert 改名 D650 域/use-streaming react-markdown 未声明依赖/graphbridge/persistence D593 域/e2e 数据），≈main 基线 26（±1 环境差异已定性） | ✓ |
| DS10 类型安全 | 新代码（index.ts/tool-definitions.ts）as any/as never/as unknown as = 0（全文审读） | ✓ |
| 棘轮 42→36 | baseline txt 总量 36 ✓；42−6（D595 哨兵簇真修复）= 36，D601 四处平移计数——task-state ratchet 段数字自洽 | ✓ |
| CI 红披露 | ci.yml:105 vitest 豁免 exit 0 逻辑存在；Architecture job SYNO_CI="1" 注入（ci.yml:104）——"红为存量"解释源码层面有据；本地 gate 12/12 绿证明棘轮机制自洽；对照证据文件 d595-ci-parity-main.json **不在 main**（旧分支 c8ceb99a 内） | ⚠️ 披露可信，证据未入库 |

### 3.3 发现（分级）

- **P1-1（归因: implement）写集漂移**：evidence 7 件 + 补实 brief 未随 #459 入库（"files[] 与 diff 完全一致"声称不实，M2/M7 族）。
- **P1-2（归因: devdoc + control-tower）spec 载体双缺失**：spec 被 D593-FIX 删除（判"claimed 未交付纯夹带"）后 D595 未恢复；main 上 brief = 22bea264 骨架模板（36 行占位符，Q0-Q3 全空；补实版 24c932f6 停留旧分支 75f3c486）——D602 P1-1 同族，D595 更彻底（唯一载体骨架化）。
- **P1-3（归因: control-tower）gitlink 夹带第三次**：6 个写集外 gitlink（含 3 个复活）——#449/#442/#459 三连，合并级白名单核验 gate 仍未落地（前审 L4 提议 12 小时内被第三次实证）。
- **P2×4**：①派单材料口径失准（conversations 21/21 实为 13/13；getDiagnosisCheckpoint 1 命中 = 6/15 老代码；"棘轮基线 36"表述混淆）；②MCP-SDK 评估"体积增量 <1MB"与 lock 嵌套 express 树实况失准；③ci-parity 对照证据未随任务入库；④`/api/ontology/ingest` 白名单放行 POST 写端点（单机信任模型内，D590 裁决①延续，记录不翻案）。

### 3.4 北星三问（north-star-guard，D595）

1. **服务真实用户场景吗？** ✓ MCP 权限分级 = PRODUCT-BRIEF §六 P1"一对多对话 + RBAC"的最小前置（单机信任 + 读写两级）；工具补齐（sentinel_tickets/knowledge_ask/sentinel_reports）= §三哨兵巡检→工单闭环的服务面。AGENTS.md 项目身份"HTTP + MCP 对外服务"的声明接口之一。
2. **更接近终态吗？** ✓ SDK 化消三处协议漂移（评估文档实测举证）、L2 化修复 11 处跨层违规（棘轮 42→36）、权限从"隐式无认证"到"显式信任声明 + 分级"——纵向推进，非横向加物。
3. **Synova 还是那个 Agent 吗？** ✓ 12 工具全部服务于诊断/哨兵/本体/知识——仍是驻扎企业的诊断 Agent，未变味。
**裁决：对齐**。

（D593-FIX 北星：前审已裁决对齐（报告持久化 = §三诊断报告核心产出的最后一公里）；修复方向即恢复该对齐实现，不重复裁决。）

---

## 4. 15 项语义审计清单（两对象合并出表）

| # | 项 | D593-FIX（main） | D595（main） |
|---|---|---|---|
| 1 | 接口真实性 | ✗ 声称恢复的 readReportFromCheckpoint/ColdReadArchive/列表端点全部不存在（grep 0 命中） | ✓ TOOLS/filterToolsByMode/assertToolAllowed/listSentinels 全部真实存在 |
| 2 | 文件路径存在性 | ✗ brief Q2 首项 diagnosis.ts 未入提交（diff 无该文件） | ⚠️ 写集 24 项中 8 项未入库（evidence 7 + brief） |
| 3 | Edge ID | N/A | N/A |
| 4 | 架构边界 | ✓ 修复零代码改动（无新增违规）；D595 棘轮 36 基线自洽 | ✓ 哨兵族 L2 化净减 6 处；L2→L3 动态 import 相邻合法 |
| 5 | 数据流完整性 | ✗ consult 写路径断（完成 → 内存 only）；冷读断；列表断 | ✓ tools/list → filter → tools/call → assert → handle 全链装配（index.ts:53-64） |
| 6 | 接线深度 | ✗ listDiagnosisReports 仍零生产调用方（M3 未解） | ✓ 6 个新 export 全部生产调用（index.ts:22-31 import + :54/:61 调用） |
| 7 | 降级诚实性 | —（未提交代码无从审） | ✓ resolveMcpMode fail-closed warn；bridgeFetch log.warn；listSentinels degraded 空清单；sentinel_run degraded 上浮（铁律 31） |
| 8 | 测试契约（铁律 47） | — | ✓ tool-definitions.ts 头 JSDoc @input/@output/@degraded/@error 完整；listSentinels JSDoc 完整 |
| 9 | 测试覆盖三路径（铁律 48） | ⚠️ 测试文件设计好（非空壳）但 main 上 10 failed = 交付态不绿 | ✓ 38+9 全绿；正常/降级/边界显式（T5 降级、T8 不可达、T6 -32000、S6c 纯度） |
| 10 | 文件驱动 | N/A | N/A |
| 11 | 自我报告交叉对比 | ✗ 修复消息声称恢复 ⊋ 修复树（M2 第三次）；evidence README"21 全绿"不实 | ✗ files[] 与 diff"完全一致"声称不实（P1-1）；其余披露诚实（D601 存量归属、续接历史） |
| 12 | dev doc 偏离审计 | ✗ brief Q2 写集 vs 提交 diff 差集 = diagnosis.ts（核心） | ⚠️ 写集漂移 8 项 + gitlink 夹带 6 项（P1-1/P1-3） |
| 13 | 控制塔执行审计 | ⚠️ bypass 链 ✓；pre-commit 对"声称恢复未提交"零感知（查不到差集） | ✓ bypass 链 ✓；CI 豁免/存量红披露与源码相符；无 gh，CI job 级结论未独立调取（如实标注） |
| 14 | 版本编排合规 | ✓ 未碰 VERSION.md/tag | ✓ 同上 |
| 15 | 并行合规 | ✓ fix/d593-restore 单分支；与 #459 并行（后者 base 落后 → 复活 gitlink，见 P0-2） | ✓ 续接单线（feat/d595-mcp-auth → -final）；.sessions 隔离 clone 交付 |

---

## 5. 北星三问汇总

- D593-FIX：**对齐**（继承前审裁决；修复方向 = 恢复对齐实现）。
- D595：**对齐**（§3.4 三问全过）。

---

## 6. L4 防线缺口收割（每对象必答）

### D593-FIX（P0-1 未修复）——本该拦住的三道防线，为什么都没拦住？

1. **提交级"声称 vs diff"对账缺失**：修复 brief Q2 写集首项 = diagnosis.ts；commit 消息声称恢复该文件；`git show 9c3f1672 --name-only` 一行命令即见差集。commit-msg/提交流程无"Q2 写集 ⊆ 提交 diff"检查——**pre-commit 13 组查的是提交内容，查不到"声称但未提交"的差集**（M2 族第三次：D320 劫持 → D593 P1-1 overclaim → 本次）。
2. **evidence 包有效性标注缺失**：前审 P1-4 已要求"证据标注分支态/主态 + 合并后有效性"（DSH persona 规则）——修复 session 的 green 日志照旧未标注"worktree 态、未进提交"，README 反而给出"K3 独立重跑 21 全绿"的误导指令。**P1-4 规则未被执行**（规则存在但无物理执法——信息注入型规则对 agent 不可见，V3.9 教训）。
3. **task-state 状态机无"修复回填门禁"**：D593.json note 声称修复完成（"21/21 全绿"）但 audit 段 verdict 仍 FAIL、status 仍 audited——修复 claim 入 task-state 时无任何交叉验证（无 job 读"note 声称" vs "git diff 实况"）。

### D595（P1-1/P1-2/P1-3）——本该拦住？

1. **合并级写集白名单核验**（前审 L4 已提议，12 小时内第三例：gitlink 复活 + 新增夹带）——仍未落地。缺：合并 diff --name-status 写集外文件逐项确认（CI job 或合并审查 SOP 一行 bash）。
2. **写集对账声称未执行**：task-state 声称"files[] 与 diff 24 条完全一致（D286 口径）"——实际 8 项声明未入库。`check-dev-doc-write-set.sh` 类工具存在但未跑（或跑了结论被覆盖）——**对账工具的执行证据缺失**（M4 族）。
3. **spec/brief 载体生命周期无 gate**：D593-FIX 删 D595 spec（判"纯夹带"）时，D595 尚在途（claimed）——删除方与被删除方的任务状态对账无人做；D595 合并时无人发现"本任务 spec 已从 main 消失"。缺：task-state claimed→impl 流转时 spec 载体存在性检查。

---

## 7. 错误归因闭环（每个发现 → 角色 → 防再犯）

| 发现 | 归因 | 防再犯机制（一类一机制，防臃肿） |
|---|---|---|
| P0-1 修复提交丢核心文件（声称 vs diff 差集） | implement | **提交级写集对账**（Q2 写集 ⊆ `git diff --name-only`，并入 pre-commit 组 12 或 commit-msg hook——与 D320 G12 声明-内容一致性同族，一处机制覆盖） |
| P0-2 部分修复被复活 + D595 夹带 6 gitlink（第三例） | control-tower | **合并 diff 白名单核验 gate**（前审已提议，本次升级为三连实证——优先级提至最高；gitlink 类文件（.synova-wt-*/.sessions/*）默认拒绝进 main） |
| P1-1 写集漂移（声称对账一致实未执行） | implement | 对账工具执行证据落盘（check-dev-doc-write-set.sh 输出随 evidence 入库——已声明未入库恰是本问题本身，鸡生蛋；改为 pre-commit 组 12 消费声明清单直接 diff） |
| P1-2 spec/brief 载体双缺失 | devdoc + control-tower | task-state 流转 gate：claimed→impl 合并时校验 spec 或补实 brief 存在（与 D602 P1-1 合并为同一机制） |
| P1-3 已并入 P0-2 行 | — | 同上合并 gate |
| P2 evidence 口径（15=15、36=36、21/21、<1MB） | implement/audit | 派单材料口径核验（前批 P2-4 已提，继续并入派单 SOP）；证据数字必须附参照点 |
| use-streaming react-markdown 未声明依赖 | 存量（非本批） | 登记独立治理（干净安装 break——影响 CI 与本地绿差异解释） |

**M 模式匹配**（AUDIT-FINDINGS-LEDGER M1-M8）：P0-1 = **M2**（声称 vs 事实）+ M9 族（前审提议的合并吞实现类，本次在修复通道复犯）；P1-1 = M2/M7；P1-3 = 前审 P0-2 同族（合并通道无白名单核验，M9 附类）。**不新增模式类**——全部命中已有 M 类或 M9，强化对应防线即可（防再犯守门人：机制数量收敛于模式种类数）。

---

## 8. 修复路线（只报告，不立项——K3 不越界）

1. **D593 第二次 FIX（P0，建议 CTO 立项）**：把 worktree 手术产物真正提交——从修复 session 遗留的 worktree/分支找回 diagnosis.ts 恢复面（或按 brief Q0c 方法重做手工回植），提交 diff 必须含 src/routes/diagnosis.ts；`git show <fix> --name-only | grep diagnosis.ts` 为验收第一行；red→green 证据标注提交 hash；task-state 复审回填。
2. **D595 条件关闭**：evidence 7 件 + 补实 brief 补入库（或 CTO 正式豁免证据入库要求）；spec 载体恢复决策（重入库 or brief 补实版入库）；gitlink 清理随合并级 gate 一并治理（第二批 gitlink 清理任务：ct64/d590/d593/d595-cont/d602/.sessions + d577 等 7 个遗留）。
3. **控制塔**（CT 队列优先级重排）：合并 diff 白名单核验 gate（三连实证）+ 提交级写集对账（Q2 ⊆ diff）+ task-state 流转载体存在性检查。

---

## 9. 审计边界声明

- 本报告只审 f5f9c462 / 91948780 两次合并入 main 的内容（基线 5494e6f6），分支内部历史仅溯源所需。
- CI job 级结论：本机无 gh CLI，以 ci.yml 源码物理分析 + 本机实跑为据，已如实标注。
- 白名单安全模型"单机本地信任"取舍是创始人裁决（D590 裁决①），记录不翻案。
- 前审 P1-2（CI vitest 豁免盲区 ci.yml:79-95）本次未复核是否已修（写集外），仅确认其逻辑仍存在（ci.yml:105）。
- 全量 vitest 25 failed 中 expert 改名类失败属 D650 域、react-markdown 属存量缺口——均已定性非本批引入，登记不展开。

*报告完*
