# K3 独立审计报告 — D593-FIX2 复审（三物证闭环核验）

> 审计员: Kimi K3（独立会话，零上下文）| 2026-09-10
> 对象: D593 第二次修复 FIX2 —— commit e64bebd2（fix/d593-restore）→ PR #476 squash 入 main @ **e53b5984**
> 前审: 2026-09-10-K3-D593FIX-D595-closeout.md（D593-FIX 维持 FAIL：P0-1 未修复）
> 协议: docs/synova/coordination/AUDIT-PROTOCOL.md（L1-L4 四层 + 15 项清单）
> 运行环境: Mac | Node v22.22.0 | 隔离 worktree `/tmp/k3chk`（detached @ e53b5984，node_modules symlink 自审计主工作区）

---

## 0. 结论总览（先读）

| 前审 P0 | 本次复审判定 | 物理证据 |
|---|---|---|
| **P0-1 合并吞实现（diagnosis.ts 未入 main）** | ✅ **已修复** | e53b5984 --stat 含 `src/routes/diagnosis.ts \| 202 +++--`；`saveDiagnosisCheckpoint`=1、冷读 fallback/列表端点/注释诚实化四恢复面全部 grep 命中；独立实跑 3 测试文件 **34/34 绿** |
| **P0-2 夹带 gitlink 复活（ct64/d590/d593）** | ✅ **已修复** | `git ls-tree e53b5984` 三 gitlink 全 **0 命中**（ABSENT）；heartbeat/D595 两文档/D599 note 前审已清 |
| **D599/D600 零误伤** | ✅ **成立** | `customerConfig` 计数 **15**（与 D600:499 合法演进一致） |
| **D595 零触碰** | ✅ **成立** | 824c801b..e53b5984 diff 无任何 D595 源文件 |

**本次复审结论：CONDITIONAL PASS（条件通过）**

- **P0 全清**：前审两条 P0（吞实现 + gitlink 复活）物理闭环。
- **新增 P1 ×1（铁律 39 棘轮回归）**：FIX2 引入 **2 处基线外 L1→L5 跨层引用**（diagnosis.ts 2→4），`tests/architecture/l1-cross-layer-baseline.txt` 未同步收紧；`check-architecture.sh SYNO_CI=1` 实测 **exit 1（"基线外新增 2 处"）**。条件：① 基线文件 diagnosis.ts=2→4 收紧（或按铁律 39 正解改 DI 注入消跨层）；② CTO 确认 PR #476 的 CI Architecture job 结论（本机无 gh，无法调取 job 级状态）。

**一句话**: D593 的核心交付（诊断报告落盘 + 冷读 + 列表 + 桌面恢复）终于在 main 上物理落地且 34/34 绿——前审 P0 全部闭环；但这次"入提交"动作顺带引入了 2 处净增的 L1→L5 跨层债（棘轮"只减不增"被破），且架构门禁实测会红、合并却已入 main，属控制塔通道的第三次漏判信号。

---

## 1. 运行环境注记（D316 教训——环境依赖判别先行）

- 独立复跑在**临时 worktree** `/tmp/k3chk`（`git worktree add /tmp/k3chk e53b5984 --detach` + `ln -s <主工作区>/node_modules`）执行，幂等、无本机假设；非引用证据包。
- `main` 本地无同名分支（审计工作区长期驻留 audit 分支）——用户给的命令 `git show main:...` 在本机落 `fatal: invalid object name 'main'`，改用等价的 `origin/main` / `e53b5984` 复跑，结果一致（saveDiagnosisCheckpoint=1）。
- vitest 经 symlink node_modules（主工作区既有安装态）运行，非 npm ci 干净装。前审已定性：`use-streaming-conversation.test.ts` 的 react-markdown 未声明依赖为**存量缺口**（干净装 import 失败），本批 3 个目标测试文件不含该文件，34/34 结论不受其影响。

---

## 2. 三物证独立复跑（用户给的幂等命令，逐条实测）

### 物证① 远端 grep

```bash
git show origin/main:src/routes/diagnosis.ts | grep -c saveDiagnosisCheckpoint   # 期望 ≥1
```
**实测 = 1**（e53b5984:src/routes/diagnosis.ts:596 `sessionStore.saveDiagnosisCheckpoint?.({...})`）。✅

### 物证② 提交 --stat 命中

```bash
git show e53b5984 --stat | grep src/routes/diagnosis.ts                          # 期望命中
```
**实测命中**：` src/routes/diagnosis.ts | 202 +++++++++++++++++++--`。✅

### 物证③ 提交态测试全绿

```bash
git worktree add /tmp/k3chk e53b5984 --detach && ln -s <repo>/node_modules /tmp/k3chk/node_modules
cd /tmp/k3chk && npx vitest run tests/routes/diagnosis-report-persistence.test.ts tests/electron/right-panel-report-sentinel.test.ts tests/routes/conversations.test.ts
```
**实测**：`Test Files 3 passed (3)` / `Tests 34 passed (34)`（12 persistence + 9 right-panel + 13 conversations）。✅

> 三物证与 FIX2 brief Done 标准、CTO 2026-09-10 复核记录逐字吻合。前审"10 failed / 11 passed（修复前 red 同签名）"已不再复现——main 现状 ≠ 修复前状态。

---

## 3. P0-1 复审 —— 四恢复面物理核验（main @ e53b5984）

| 前审判"缺席"的面 | 本次 grep / file:line | 判定 |
|---|---|---|
| ① consult 完成点落盘 saveDiagnosisCheckpoint(键=reportId, source=consult) | diagnosis.ts:596 `saveDiagnosisCheckpoint?.({ sessionId: reportIdForArchive, phase:5, source:'consult' })` | ✅ 恢复 |
| ② GET report 内存 miss → 冷读 fallback | `resolveStoreFromRequest` :714、`readReportFromCheckpoint` :737、`respondReport` :780 附近、`isFullDiagnosisReportLike` 守卫 | ✅ 恢复 |
| ③ 列表端点 GET /api/diagnosis/reports | diagnosis.ts:808 路由 + :816/:820 调 `store.listDiagnosisReports`；session-store.ts:598 实现；auth.ts:122 白名单 | ✅ 恢复 |
| ④ FIFO 注释诚实化（内存 Map 退役一级缓存） | diagnosis.ts:102-104 注释改为 "D480/D593 … 一级缓存（事实源 = checkpoint 持久层）" | ✅ 恢复 |

**接口真实性（15 项清单 #1）**：diagnosis.ts 调用的 `saveDiagnosisCheckpoint / getDiagnosisCheckpoint / listDiagnosisReports / isDiagnosisReportArchive / isSqliteDatabase` 在 session-store.ts 全部存在（isSqliteDatabase :40、isDiagnosisReportArchive :84、listDiagnosisReports :598）。✅

**接线深度（#6）**：前审 M3 "listDiagnosisReports 零生产调用方"已解——diagnosis.ts:820 生产路由调用 + auth.ts:122 白名单放行。✅

**测试非空壳（#9）**：persistence 测试含真实 SQL 断言（`SELECT ... WHERE phase=5`）、重启冷读（用例 2）、双 ID 兼容（用例 3）、列表分页（用例 5）、白名单（用例 6）；right-panel 测试含正常路径（11a/12a）+ 降级路径（11b/12b，degraded=true 不抛）。非空壳。✅

**类型安全（#4 / 铁律 38）**：FIX2 diff 全文件 `as any / as never / as unknown as` = **0 命中**；`reportIdCandidate` 用 `unknown` + `typeof === 'string'` 窄化。✅

**降级诚实（#7 / 铁律 24/31）**：落盘失败 `log.warn + degraded 语义`；`resolveStoreFromRequest` db 缺失/谓词失败/构造抛错 → null → 调用方 404/503 fail-closed；列表端点无 store → 503 `STORE_UNAVAILABLE + degraded:true`。✅

---

## 4. P0-2 复审 —— gitlink 清理核验

```bash
git ls-tree e53b5984 .synova-wt-ct64 .synova-wt-d590 .synova-wt-d593
```
**实测三 gitlink 全 0 命中（ABSENT）**。前审"被 #459 复活"的三个 D593 gitlink 已随 #476 合并彻底清除（e53b5984 --stat 显示 `.synova-wt-ct64/d590/d593 | 1 -`）。

**残留 gitlink 归属**：main 上仍存 `.sessions/d595-mcp-auth/repo / .synova-wt-d595-cont / .synova-wt-d602`（D595 P1-3 待办）+ d577/d577-impl/d579/d580/d581/d589/d597（D590 时代遗留，第二批清理任务）。**均非 D593-FIX2 范围**（brief Q2 不做什么明确划出），不记入本批。

---

## 5. 新发现 —— P1（铁律 39 棘轮回归，本复审首次暴露）

### 5.1 物理事实

FIX2 在 diagnosis.ts 新增 2 处 L1→L5 动态 import：

| 行 | 内容 | 性质 |
|---|---|---|
| diagnosis.ts:718 | `resolveStoreFromRequest` 内 `await import('../store/session-store')` | **新增** |
| diagnosis.ts:743 | `readReportFromCheckpoint` 内 `await import('../store/session-store')` | **新增** |
| diagnosis.ts:332 / :672 | 存量 D563 冷读/never 窄化通道 | 前审已在基线 |

diagnosis.ts L1→L5 命中数 **2 → 4**。基线文件 `tests/architecture/l1-cross-layer-baseline.txt`（L1→L5 段第 53 行）仍记 `src/routes/diagnosis.ts=2`——**未同步收紧**。

### 5.2 门禁实测（本机复跑）

```bash
cd /tmp/k3chk && SYNO_CI=1 bash scripts/check-architecture.sh
```
```
↳ src/routes/diagnosis.ts: 实际 4 > 基线 2 (新增 2)
❌ 1d. L1→L5 跨层引用: 基线外新增 2 处 [CI strict——软提示在 CI 上为硬阻断]
exit = 1
```

即：**CI Architecture job（SYNO_CI=1）下该合并应为红灯**（ci.yml architecture job 明确 `SYNO_CI: "1"`）。而 e53b5984 已在 main——说明该门禁未拦下本次合并（红灯被 override，或 PR 合并通道未强制执行 Architecture job 通过）。**本机无 gh CLI，CI job 级结论无法独立调取，如实标注，待 CTO 确认。**

### 5.3 根因（spec 与 CT-64 语义漂移）

- D593 spec（SYNOVA-IMPL-DSH-D593-report-persistence-20260908.md）第 344/381 行自称"预期零新增违规""动态通道既有容忍"、第 340 行"零新增**静态** L1→L5 import"。
- 但 CT-64（2026-09-08 同日起效）已把 check-architecture.sh 重写为**静态/动态/require 三形态统一计数**（修补①），动态 import 同样计入棘轮。spec 撰写者基于 pre-CT-64 语义（动态 import 逃逸、不计违规）自检，结论失真。
- 结果：D593 设计采用"D563 动态 import 通道"复用范式（conversations.ts:98 / im.ts:42 同款），spec 判其"合法/容忍"，实际在 post-CT-64 门禁下 = 基线外新增违规。

### 5.4 分级与归因

- **P1（归因: devdoc + control-tower）**：铁律 39"只减不增"棘轮被破（36→38 方向逆行）；基线文件未随恢复代码同步收紧；spec §9 架构自检与 CT-64 语义漂移。
- **不升 P0 的理由**：功能正确（34/34 绿、fail-closed 诚实）；跨层形态为**既有 D563 范式**的增量复用（非新类违规）；非功能/安全阻断。**但若 CTO 确认 CI Architecture 红灯被 override 合并，则升级为 P0（门禁形同虚设）**。

---

## 6. 15 项语义审计清单（D593-FIX2）

| # | 项 | 判定 |
|---|---|---|
| 1 | 接口真实性 | ✅ 5 个 session-store 方法全存在（§3） |
| 2 | 文件路径存在性 | ✅ brief Q2 写集 4 项（diagnosis.ts/brief/README/task-state）全部入提交 |
| 3 | Edge ID | N/A |
| 4 | 架构边界 | ⚠️ **2 处基线外 L1→L5（P1，§5）**；无新增 L2→L4/L1→L3/L1→L4 |
| 5 | 数据流完整性 | ✅ consult 写路径落盘 + GET 冷读 + 列表 三链完整装配 |
| 6 | 接线深度 | ✅ listDiagnosisReports 生产调用（diagnosis.ts:820）+ auth.ts:122 白名单 |
| 7 | 降级诚实性 | ✅ 落盘/冷读/列表三处 catch 全 log + degraded/503 fail-closed |
| 8 | 测试契约（铁律 47） | ✅ 新函数 JSDoc @returns + 形状守卫注释完整 |
| 9 | 测试覆盖三路径（铁律 48） | ✅ 34/34 绿；正常/降级/边界显式（非空壳，§3） |
| 10 | 文件驱动 | N/A |
| 11 | 自我报告交叉对比 | ✅ 三物证声称与实测逐字吻合（§2）；brief Done 标准全勾 |
| 12 | dev doc 偏离 | ⚠️ spec §9"零新增违规"失真（§5.3，P1 devdoc 侧） |
| 13 | 控制塔执行审计 | ⚠️ bypass 链 ✓（GATE_FAIL_SOFT×2 + COMMITTED 登记）；Architecture 门禁实测 exit 1 但合并已入 main（§5.2，需 CTO 确认 job 级） |
| 14 | 版本编排合规 | ✅ 未碰 VERSION.md/tag |
| 15 | 并行合规 | ✅ fix/d593-restore 单线 → PR #476；无并行夹带 |

---

## 7. 北星三问（north-star-guard）

D593-FIX2 是"恢复已裁决对齐的实现"（前审已判报告持久化对齐 PRODUCT-BRIEF §三/§五 W1/§六 P0），方向无新变化，逐问复核：

1. **服务真实用户场景吗？** ✅ 诊断报告持久化 = GA/桌面用户"跑完诊断报告一直在那里"的核心场景（重启不丢、刷新可读、右栏真实数据）。
2. **更接近终态吗？** ✅ 报告链路最后一公里（落盘 + 冷读 + 列表 + 桌面恢复）落地，纵向推进非横向加物。
3. **还是那个 Agent 吗？** ✅ 仍是驻扎企业、为增长服务的诊断 Agent，未变味。
**裁决：对齐。**

---

## 8. L4 防线缺口收割

> "本次问题，控制塔哪一道防线本该拦住？为什么没拦住？缺什么？"

**P1（2 处基线外 L1→L5）——本该拦住，为什么没拦住？**

1. **架构棘轮门禁**（check-architecture.sh SYNO_CI=1）本应拦"基线外新增 2 处"——实测 exit 1，但合并已入 main。缺：**合并通道对 Architecture job 红灯的强制**（是否 override / 是否无 required status check）。与"合并 diff 白名单核验 gate"（前审 L4 已提议、已三连实证）同族——**合并级门禁强制执行**是共性缺口。
2. **基线文件与代码同步 gate**：FIX2 恢复 diagnosis.ts（含 2 新跨层 import）时，无人要求同步收紧 `l1-cross-layer-baseline.txt`。缺：**"基线外新增"即红的语义下，编码线应"要么消跨层、要么同步基线"**——本次两头都没做。
3. **spec 架构自检与 CT-64 语义同步**：spec §9 基于 pre-CT-64"动态 import 容忍"旧语义自检，与门禁实际语义漂移。缺：devdoc 撰写时的**架构检查语义注入**（CT-64 修补①生效后，spec 自检应改为"动态 import 同样计入棘轮"）。

**归因闭环**：P1 = devdoc（spec 自检失真）+ control-tower（棘轮门禁未强制执行 + 基线同步 gate 缺失）。**命中已有 M 类**：跨层债 = 铁律 39 治理方向的增量逆行（非 M 模式新类）；控制塔侧 = 前审"合并级门禁强制执行"三连的延续。**不新增模式类，强化合并级门禁即可。**

---

## 9. 修复路线（只报告，不立项——K3 不越界）

1. **P1 闭合（CTO 决策二选一）**：
   - (a) 基线收紧：`l1-cross-layer-baseline.txt` L1→L5 段 `src/routes/diagnosis.ts=2 → 4`（记录本次净增，纳入既有 36→38 治理队列）；
   - (b) **（正解，铁律 39）** 按 hint"存储实例由 L2 bootstrap 组装后 DI 注入"重构——`resolveStoreFromRequest`/`readReportFromCheckpoint` 改走 bootstrap DI，消掉 2 处 L1→L5 动态 import，基线可回 2 甚至续减。
2. **CI 结论确认**：CTO 调取 PR #476 的 Architecture job 结论——若红灯 override 合并，登记为门禁形同虚设事件（P0），并入合并级门禁强制执行治理。
3. **devdoc 语义注入**：spec 撰写/自检时注入 post-CT-64 架构检查语义（动态 import 计入棘轮、只减不增）。

---

## 10. 审计边界声明

- 本报告只审 e53b5984（PR #476 squash，基线 824c801b）内 D593-FIX2 内容 + 前审 P0 闭环；D595 遗留（P1-1/1-2/1-3、3 个 gitlink、spec 载体）不在本批，不翻案。
- CI job 级结论：本机无 gh CLI，以 ci.yml 源码 + 本机 `SYNO_CI=1` 实跑为据，已如实标注。
- 残留 gitlink（d595-cont/d602/.sessions + d577 等 7 遗留）属既定第二批清理任务，非本批引入。
- `catch (err: any)` 为 diagnosis.ts 存量类型注解（非 `as any` 断言，铁律 38 不涉），FIX2 未新增断言形态。

*报告完*
