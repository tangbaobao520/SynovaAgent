# K3 独立审计报告 — D725 控制塔修复批次（P0）

> 审计员: Kimi K3（独立会话，零上下文） | 日期: 2026-09-13
> 派单: `docs/synova/coordination/问题总账与下一批派单-20260913.md` §四 D725
> 审计基线: origin/main `40425f98`（桌面端代码与 `2bc0f724` 无差异；分支头快进记录在案）
> 协议: AUDIT-PROTOCOL.md L1-L4 四层 + 14 项清单 + claim-verifier 物理复现
> 红线遵守: scripts/audit/ 零触碰；未替创始人下业务结论；CTO 自改门禁（D721/D722）同等受审

---

## 〇、总结论

| 任务 | 内容 | PR / 状态 | **verdict** | 阻断项 |
|---|---|---|---|---|
| D713 | 桌面端 ad-hoc 签名 + 出货前签名门禁 | #512 已合 | **PASS**（P1×2, P2×1） | 无 |
| D714 | F3 打包态渲染层断链修复（含 P1-4 核） | #514 已合 | **PASS**（P1×1） | 无 |
| D717 | 产品真债两项（react-markdown + expireOld 接线） | #527 open | **PASS，可合并**（P2×1） | 无 |
| D718 | 四项：#518 骨架泄漏 / #522 跨日认领 / #523 BOM / #524 台账登记 | #518/#522/#523/#524 全部已合 | **PASS**（P2×2） | 无 |
| D721 | CI 棘轮基准改 merge-base 三点差 + 放行可见化（**CTO 自改**） | #520 open | **PASS，可合并**（P2×2） | 无 |
| D722 | brief 写集 markdown 包裹符认领洞（**CTO 自改**） | #521 open | **PASS，可合并**（P2×2） | 无 |
| D723 | K3 审计回流（失实订正 + 台账 M9 + 待审清单） | docs/d723 分支 open | **PASS** | 无 |
| 1-8 复验 | 绑 D714 后新产物重装实测 | — | **1-1/1-3/1-4/1-8 物理闭合**（见 §一） | 无 |

**批次结论: PASS（零 P0）。** 三个 open PR（#527/#520/#521）从 K3 侧放行。本报告即为 D721/D722/D723 的 verdict 源；其 `task-state/<D#>.json` 的 audit 段在各自 PR 合并后由 CTO 回填（避免与在途分支的 task-state 同文件冲突）。

---

## 一、1-8 复验（派单特别指令：必须绑 D714 后新产物）

**产物绑定**: 本 session 从 main `2bc0f724`（含 D713+D714）完整重走三步构建链 → `release/mac` + `release/mac-arm64` 双 .app（afterPack ad-hoc 签名自检通过）→ dmg（md5 `9326757d81da730b977ec20e14de6b3e`）。旧产物零复用。

| 点 | 结果 | 物理证据（git 跟踪路径） |
|---|---|---|
| 1-1 安装包产出 | ✅ dmg 126M 双次可复现；CI desktop-build @2bc0f724 全绿 | `evidence/k3-D725-mac-20260913/1-3-dmg-md5.txt` + run 34745476421 |
| 1-3 Mac 安装包可用 | ✅ 官方脚本 `mac-install-verify.sh --skip-build` 四断言第 42s 全过（挂载含空格卷名 → cp /Applications → open → 进程/窗口/healthz/backend.log） | `1-3-1-4-install-assertions.txt` `1-3-window.txt` `1-3-process.txt` |
| 1-4 开窗即用 | ✅ **`[preload-check] OK: electronAPI 已暴露, serverUrl=http://localhost:18790`**（打包态二进制直启 stdout 第 2 行）+ `/api/healthz` 200。F3 断链未复现 | `1-4-preload-check-stdout.txt` `1-4-healthz.json` `1-4-backend-tail.txt` |
| 1-8 K3 独立复核 | ✅ k3_only 点，本节即独立重跑记录（非任务方自兑） | `evidence/k3-D725-desktop-reverify-20260913.json` |
| 1-2/1-5/1-6/1-7 | 超出 Mac/本批范围（1-5 spec #519 已合待 Win 实施） | — |

**P1-4（D715 遗留）裁定: 已闭合。** D714 修复的代码在 git（`electron/main.cjs:89` additionalArguments / `electron/preload.cjs:23-30` argv 读取），当时缺的是复验证据——本次由 K3 绑新产物补齐且证据落 git 跟踪路径。产品侧可据 `k3-D725-desktop-reverify-20260913.json` 将 1-4 由 fail 翻绿（D713 证据 JSON `D713-published-artifact-20260913.json` 的 1-4=fail 是修复前判定，判定本身准确无需改写，新增证据记录前后对照即可）。

**签名门禁实测（D713 核心承诺）**:
- `verify-package-signature.sh --all --dir release` → exit 0（双 .app 封印有效）
- `--dmg` 新 dmg → exit 0；安装到 /Applications 后 `codesign --verify --deep --strict` 再过（封印经 cp 存活）
- **fail-closed 实证 ×2**: ① 首次构建 prebuild-install 失败 → `[D713] native 依赖准备失败` throw → 构建中止零错配包；② 对不存在的 dmg 执行门禁 → exit 2 拒绝绿（"校验执行失败…不得视为通过"）
- 自测 `tests/desktop/verify-package-signature.test.sh` 9 断言过（6b hdiutil create 在沙箱受限跳过——该分支语义已由我的真 dmg 端到端覆盖）

---

## 二、逐任务审计

### D713 — 桌面端打包链 ad-hoc 签名（#512 已合）

**L1 代码**: 根因链成立（`app-builder-lib@25.1.8` 无证书只 log skip；旧 CI 只断言产物存在）。修法正确: `build-synova.cjs` afterPack 签名+自检 throw（`build-synova.cjs:75-102` d713SignApp）、beforePack 按 arch 备 native（`:55-73` d713PrepNative）、`mac.identity:null`（`:199`）。门禁脚本契约完整（三态 0/1/2、非 darwin fail-closed exit 2、dmg 挂载点按 tab 解析含空格卷名回归，`scripts/desktop/verify-package-signature.sh:27-33,61-63,110-118`）。CI 接线: `desktop-build.yml:112-123`（自测+门禁，位于 upload-artifact 之前）✓；`package.json:45` electron:verify:mac ✓。

**L2 偏离**: ⚠ **P1-A（历史 M9 夹带）**: #512 实带 ~500 行 D711/product-lines 文件（D711 brief+决策文档+cockpit-override.yaml+product-progress.*+`scripts/product-lines/gen-progress-page.py`+`tests/control-tower/product-lines.test.py`+task-state/D711.json），D713 brief Q2 与 PR 正文均未声明。**时序物理证据**: D708 gate（#505=`56f062bc`）在 `75c8e5be` **之后**才进 main——`git cat-file -e 75c8e5be:scripts/control-tower/merge_writeset_gate.py` 不存在 → 当时无门禁可拦。**今日复放**（忠实分支名上下文 `fix/d713-desktop-signature-gate`）: gate 报 `block — 检测到 12 个写集外文件`，exit 1 → M9 免疫细胞对此模式有效，历史偏离已被覆盖，不需新机制。docs 范围豁免是已知设计洞（K3-P2-6 已登记，备查）。

**发现**:
- **P1-1 重复 `beforePack` 键（死键）**: `build-synova.cjs:141`（旧 D581 单守卫版）与 `:207`（D713 合并版）同对象双键。实测（`node -e` require 打印生效函数）: 后者生效，**行为正确**；但 L141 是被遮蔽死键——未来若有人"修复"L141 会是无效改动，且这是最高风险文件（ctrl-tower-change 语义域）。oxlint 实测**已能抓**（`no-dupe-keys` warning，指认 :208 重复），未接成阻断。修法一行: 删 L141（或把 build 脚本纳入 lint error 级）。归因: **implement**。
- **P2-1 本地构建破坏本地 vitest（DX 副作用）**: d713PrepNative 把共享 `node_modules/better-sqlite3` 换成 Electron ABI(130)，构建后本地 vitest（Node ABI 127）全红——本 session 亲历（21/21 红→`prebuild-install -r node` 恢复→全绿）。CI 不受影响（desktop-build 与 vitest 各自 npm ci）。建议: 构建尾部恢复 node ABI 或 README 注记。归因: **implement**。

**L3 执行**: CI job 级: `75c8e5be` = 18 success + 3 cancelled（deploy/report/build，被后续推送取消）+ 1 skipped；`macos (dmg+zip)` **success** → 签名门禁步骤在 main 上有绿证据；`2bc0f724` 起连续 4 次 desktop-build success。bypass.log 全为 `COMMITTED | pre-commit PASS` 正常登记，无 --no-verify 事件。

### D714 — F3 打包态渲染层断链（#514 已合）

**L1 代码**: 根因（沙箱 preload 禁 require 相对文件）与修法（`webPreferences.additionalArguments` 透传——Electron 官方沙箱通道；preload 改读 `process.argv` 带默认回退，dev 行为不变）均正确。行号: `electron/main.cjs:89`（透传）、`electron/preload.cjs:23-30`（readLaunchedArg）、启动哨兵 `electron/main.cjs:112-126`（did-finish-load 探针断言 electronAPI）。

**L2 偏离**: 无。D713 证据 1-4 判据修正（pass→fail）+ 撤回无效判据（backend.log 零请求）——**自我纠错诚实**，修正理由客观（判据不足 vs 事实）。

**发现**:
- **P1-2 哨兵 FAIL 路径仅 console.error**: `electron/main.cjs:121` FAIL 分支无 degraded 标记/退出码/证据落盘，且应用日志目录被 `*.log` gitignore（.gitignore:8）——P1-4 的复发结构仍在（探针红了也可能无人看见）。缓解: 探针输出已被本批证据化 + `PLAN-evidence-log-gitignored` 已登记（#524 明示未做、待派）。归因: **implement**（登记在案，随 PLAN 落地即闭）。

**P1-4 核验**: 见 §一。修复物理验证 + 证据入 git 双闭合。

### D717 — 产品真债两项（#527 open → 可合并）

**L1 代码 + 逐条声称核实**（CTO 复核过的 6 条，K3 全部独立复测）:

| 声称 | K3 实测 | 结果 |
|---|---|---|
| `package.json:88` react-markdown ^10.1.0 | `sed -n '88p'` 逐字命中 | ✅ |
| evidence-retention.ts 245 行 | `wc -l` = 245 | ✅ |
| bootstrap.ts:1189-1224 接线 | `:1189` 注释、`:1193` 动态 import、`:1199` degraded、`:1204` cron 注册 | ✅ |
| 测试 21 用例 / 51 expect | vitest **21/21 绿**；`grep -c expect(` = 51 | ✅ |
| CI 12/12 绿 @8b892995 | check-runs: 12 jobs 全 success | ✅ |
| expireOld 唯一生产调用方 | grep 全仓: 实现在 `evidence-store.ts:121`，生产调用仅 `evidence-retention.ts:184` | ✅ |

环境注记: 首跑 21/21 红——**本 session 自己造成的 ABI 错配**（见 D713 P2-1），非产品缺陷；恢复 node ABI 后全绿。

**L1 契约/降级**: 契约 JSDoc 完整（铁律 47）；配置非法→抛分类错误且**不注册作业不删证据**（fail-safe 方向正确）；catch 均有 log.warn + `ctx.addDegraded`（铁律 24/31/32）；零 as any；L3 经 EvidenceStore 操作不触 L5。**默认 permanent（不按时间删）**——与创始人 D660 方向一致且保守。

**发现**: P2（措辞，D717-N4 已由 CTO 自查登记: 报告把"提出"写成"已裁定"，代码注释本身准确）。北星: 依赖真债（干净安装不断链）+ M3 机制接线，均服务产品可靠性——对齐。

### D718 — 控制塔小修四项（全部已合）

**#518 alloc 骨架泄漏根治**: 修法是**源头 fail-closed**而非逐测试补丁（`alloc-task-id.sh:191-199`: task-state 已注入而 brief 目录未注入 → 拒绝生成骨架并显式告警）+ 测试双注入 + **反假绿断言**（沙箱必须真收到 20 份，防"注入被静默跳过"让零污染断言假绿）。实测 5/5 过（含"真实 .claude/task-briefs/ 606→606 零污染"）。"同类第 2 次复发→机制级防线"的归因路径正确。✅

**#522 跨日认领**: 候选集 = 日期窗口 ∪ D# 身份锚点（强锚点=分支名/task-state，弱锚点=current-brief）+ 同数认领 tie-break（强→弱→字典序）。实测 **32/32** 过（含场景 11: 陈旧共享 brief 不因字典序胜出）。CTO-5（陈旧 brief 抢认领误拦）实证缓解。✅

**#523 pre-doc-audit.sh 去 BOM**: 修复本体 2 行；回归网扩 38 断言过（含: 行首 BOM 存量清单 14 个与待清一致——ratchet 语义，新增/漏删都红；塞入 BOM 必被扫到的哨兵断言——防扫描器空转）。同族扫描坑（`od|grep` 永不命中）留痕登记。✅

**#524 台账登记**: 6 条实测发现登记（含 G12 `(^|/)pat$` 与 writeset gate `fnmatch` 的 **glob 语义分叉——仍未修**、golden-case 环境依赖误报、BOM 存量按域派工且明确 K3 审计域 CTO 禁碰）。声明"附带第 4 项未做"诚实标注。task-state 收口 impl_done。✅

**发现**: P2 ×2: ① G12/writeset glob 分叉已登记未修（建议随 D728 解析器收敛一并处理，同族）；② D718 task-state write_set 与最终 diff 有轻微出入（如 memory note 在 write_set 而实际落在 #524 补充提交）——不影响判定。

### D721 — CI 棘轮基准 merge-base 三点差（#520 open → 可合并；CTO 自改）

**L1 机制**: 核心判据正确——`ci.yml:86-95` 基准改 `git merge-base refs/remotes/origin/main HEAD`，无基准时回退旧判据且 `::warning::` 可见（降级不静默）；放行分支从一句话变 `::warning:: + 文件名清单 + 指向 board-backlog`（铁律 11 语义）。G12 同族第二处（`scripts/check-brief-vs-code.sh:39-61`）同构修复。fetch-depth:0 已确认（`ci.yml:15-17`）→ merge-base 在 CI 真实可用。

**声称核实**:
| 声称 | K3 实测 | 结果 |
|---|---|---|
| F2: #513 head 与 main 内容 diff=0 却判 3 新增红 | `git diff bf6ac999 5aae38fc --name-only` = **0 文件**；merge-base=5aae38fc（main 为祖先→三点差=空，判据自洽） | ✅ |
| F1: main 021897a0 Vitest 3 failed 286 passed 仍 success | check-runs: Vitest(1/2)(2/2) 均 **success**（假绿结构成立；3 个红文件清单在日志层，公开注解不可读，机制问题已由 sealed test 证明） | ✅（结构级） |
| 密封测试 12 断言 | 实测 **15/15 过**（含: 旧判据缺陷可复现②③、新判据排除 main 并入/保留本分支改动、回退带告警、G12 两断言、接线白名单） | ✅（数字漂移见 P2） |

**发现**: P2 ×2: ① **main 推送上棘轮空转**: push 到 main 时 origin/main==HEAD → merge-base=HEAD → 变更集恒空 → 任何失败都判"存量"放行。PR 侧门禁仍有效 + main 有 enforce_admins（D710）+ 放行已可见化 → 可接受的设计取舍，但应在 ci.yml 注释或 backlog 显式记一句"main run 的 Vitest 结论恒为放行，真门在 PR"。② task-state 写"12 断言"，终版 15 断言（后补 commit 扩了）——登记性漂移。归因: control-tower（取舍）/ task-state 卫生。

### D722 — brief 解析 markdown 包裹符认领洞（#521 open → 可合并；CTO 自改）

**L1 机制**: `brief_parser.py:166-188`（D722 版）: `_strip_markup` 剥两侧 `` ` * _ ' " [ ] `` 与空白；剥后空 pattern → **False（绝不退化为匹配一切）**；`(^|/)pat$` 后缀锚定保留（`x.ts` ≠ `x.tsx`）。None 输入 → False。

**声称核实**: 密封测试 **16/16 过**（4 种包裹+裸路径+父目录后缀+空/None+端到端认领+双消费方接线 staging_guard/resolve-commit-brief.sh）。**反向验证逐字复现**: K3 将 match_path 还原为旧实现重跑 → **8 过 4 失败**（与声称"4 失败"一致），恢复后 16/16；测试真实退出码 fail=1/pass=0（CI 拦截语义正确）。根因实证（D720 brief Q2 首项带反引号→旧 match_path 恒不命中）与代码路径吻合。✅

**边界诚实性**: ⚠ 内联回退副本仍在——`resolve-commit-brief.sh:125` 主路径 import brief_parser（已修），但 `:156` 的 fallback `match_path` 副本**仍是旧逻辑**（解析器缺失时认领洞复现）。这**正是 CTO-6，已登记派 D728**（删副本+显式 degraded），D722 task-state/brief 未宣称覆盖回退路径 → 属诚实分期，非隐瞒。P2 记录保可见性。另 P2: 剥壳同时作用于 path 侧——含反引号/方括号字面量的合法路径会被改写（边界罕见，测试未覆盖此负例，建议 D728 顺带补一条）。归因: control-tower（已派）/ implement（负例）。

### D723 — K3 审计回流（docs/d723-k3-audit-return open）

**红线检查（本任务最高风险点）**: diff 仅触 coordination 文档 + brief + task-state——**未改任何 K3 报告本体（docs/synova/audit-reports/）、未写审计标准、未触 scripts/audit/**。K3审计清单的更新属 CTO 维护职责（含本批派审排程），verdict 引用均来自 task-state/report 而非杜撰。✅

**声称核实**: ① K3-P1-2 失实订正——`#511 state=open merged=False`（head=feat/win-d712-reverify-evidence）实测属实，订正文本与物理状态一致 ✅；② M9 补录——`grep -c "| M9 |"` = 1 ✅；③ K3 清单新增节与 task-state 状态机核对（D706-D708 audited/PASS 与 main task-state 一致）✅。

**结论**: PASS。治理文档回流干净、可追溯（订正保留原文+原因，非静默改写）。

---

## 三、北星第二道（north-star-guard 三问）

对照 `PRODUCT-BRIEF.md`（§一 L10-16 "驻扎企业内部的 AI 诊断 Agent/7×24/主动干活"；§二 L20-21 用户=GA/企业主）:

| 任务 | 问1 服务真实用户 | 问2 接近终态 | 问3 是否变味 | 裁决 |
|---|---|---|---|---|
| D713/D714 | ✅ 修"创始人双击打不开"——最基础的用户触达承诺（AGENTS.md 项目身份: 桌面端能装/能开/能用） | ✅ 桌面端 8 验证点推进 | ✅ 不变味 | 对齐 |
| D717 | ✅ 安装不断链 + 证据生命周期（D660） | ✅ M3 接线闭环 | ✅ 默认不删证据（保守） | 对齐 |
| D718/D721/D722/D723 | 间接（多 Agent 协作协议 TASK-ROUTING 定义的第二类控制塔域） | ✅ 门禁真实可靠 = 终态 | ✅ 无横向加无关物 | 对齐 |

**本批无方向跑偏。** 无凭空造需求、无横向无关模块、产品仍是"驻扎企业、主动诊断、为增长服务"的 Agent。

---

## 四、L4 防线缺口收割（每问题: 本该谁拦/为何没拦/缺什么）

| # | 问题 | 本该拦住的防线 | 为何没拦 | 缺什么（收敛原则: 一类一机制） |
|---|---|---|---|---|
| 1 | D713 重复 beforePack 死键 | oxlint `no-dupe-keys`（**实测能抓**，warning 级） | warning 不阻断；build-synova.cjs 不在任何 error 级 lint 范围 | **不新增机制**——修法是删死键一行 + 把 oxlint warning 在 CI 对 `build-synova.cjs`/`scripts/**` 升 error 级（已有细胞收紧，M7 族） |
| 2 | #512 夹带 D711 文件 | D708 merge_writeset_gate（M9） | **时序**: gate 在该 PR 合并后才进 main（`56f062bc` 晚于 `75c8e5be`，物理验证） | 已闭合（今日复放 exit 1 拦 12 文件）；docs 范围豁免维持 K3-P2-6 登记不动 |
| 3 | D714 哨兵 FAIL 无升级 + 应用日志被 gitignore | 证据链防线（PLAN-evidence-log-gitignored） | 属登记未做的附带项（创始人本批指令三项） | 按 #524 登记派工即可，勿提前造机制 |
| 4 | D718 测试泄漏真仓（同类第 2 次） | 测试隔离注入 | 逐测试打补丁模式失败 | #518 已按"源头 fail-closed"修——**这正是防臃肿原则的正确样本**（D521 补一个测试 → 复发 → 源头一改全防） |
| 5 | K3 审计环境三拦（hdiutil 打包 .app、Electron Chromium 沙箱 init、osascript System Events、~/npm 写入） | 无（审计侧环境） | harness 沙箱必要限制 | 记入本报告环境注记供下批 K3 预判；与 D718-#524 发现⑤同族（M5 环境依赖），不另立机制 |
| 6 | 派单前置"D717 合并"与实际 #527 open | 派单前置机器校验（D728 ③ 已派: 派单声称的 PR 必须机器核验 merged） | 派单写就时 D717 尚未合（CTO 预审已过、待 K3） | D728 ③ 落地即闭，本批不重复提 |

**M 模式对照（AUDIT-FINDINGS-LEDGER）**: 本批全部发现命中既有 M 类（M3 接线/M5 环境依赖/M7 声称-实现漂移/M9 夹带）——**零新类提案，零新脚本提案**，全部走"既有细胞收紧/已登记待派"路径。

---

## 五、运行环境注记（复现口径）

- 环境: macOS 25.3.0 / arm64 / node v22.22.0 / npm 10.9.4，K3 独立审计工作区（DSH 沙箱 workspace-write）。
- **沙箱受限操作与处置**: `~/.npm` 写入（→ 工作区内 npm_config_cache）；hdiutil 打包 .app（→ 一次性 danger-full-access 升级，仅 dmg 制作/安装/启动实测三段）；Electron Chromium 沙箱 init（同上升级段内）；osascript System Events（同上）。CI 对照: desktop-build 在 GitHub runner 全绿 → 以上均为**审计侧环境依赖**，非产品缺陷。
- **旧实例干扰**: 复验开始时发现 09-13 早些时候的 SynovaAgent 实例仍在运行并持有 18790/单实例锁（D713 验证残留未清理）——官方脚本幂等清理段正确处理。**提醒产品线**: 验证脚本 cleanup 段依赖脚本跑完; 手动中断会留残骸（可考虑 trap 兜底，P2 级、不阻断）。
- 测试复跑口径: 分支任务在 `.wt-d717/.wt-d721/.wt-d722` worktree 于**分支最终头**（dd3d379f/2ca383d3/b525ce18）执行；主树测试在 main `40425f98`。
- 审计基线: `python3 scripts/audit/audit-check.py --full` → PASS:2 WARN:971 FAIL:475（存量口径，与本批无关；tracked 文件全程零污染——每次运行前后 `git status` 核对）。

---

## 六、给控制塔的行动清单（按优先级，均非阻断）

1. **删 `build-synova.cjs:141` 死键**（P1，一行；D726 顺带即可）+ oxlint 对构建脚本升 error 级。
2. **构建后恢复 node ABI**（P2，一行 poststep 或 README 注记；本次 K3 亲历 21/21 假红）。
3. **PLAN-evidence-log-gitignored 落地**（P1 复发结构，已登记待派）——建议证据目录统一收敛到 `docs/synova/product-lines/evidence/` 且扩展名避开全局 `*.log` 忽略（本批 K3 证据即按此落盘）。
4. D728 已派项照走: 内联回退副本收敛（CTO-6）+ G12/writeset glob 语义统一 + 派单前置机器校验。
5. 1-4 翻绿动作: 产品侧引用 `k3-D725-desktop-reverify-20260913.json` 在 product-lines.yaml/状态机登记（k3_only 点已由本报告完成 1-8 独立复核）。

— K3，2026-09-13
