# K3 批次四 · C 组审计报告（工程与门禁批 · 9 项合并审计）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 C 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §C 组
> 审计基线: 独立工作区 worktree `.audit-batch4` @ `eb357405`（= origin/main）· 测试在 K3 自有工作区执行
> 合并口径: 派单 §二 C 组说明「合并审，避免碎片」——本报告为 9 项合并报告，每项独立结论 + file:line/命令证据

## 〇、结论汇总

| 任务 | 标题 | 结论 |
|---|---|---|
| D584 | P1 DSH 线测试债-sentinels 与契约批 | **PASS** |
| D589 | stale 26 点重验批 | **CONDITIONAL PASS**（交付物含已失效的前向断言，见 §2） |
| D597 | D588 会话投影接线（M3 第 5 次修复） | **PASS** |
| D601 | 任务看板映射校准 + L1 跨层违规扫描 | **PASS** |
| D660 | 任务看板活动判定重排（running=48h / backlog 三源） | **PASS** |
| D661 | grep -oP 可移植家族修复 | **PASS** |
| D663 | CI hermetic 架构棘轮 | **PASS**（CI 侧无 API 凭据，为局部审） |
| D665 | main build job 失败修复（gitlink 摘除） | **PASS**（同上） |
| D721 | CI 存量失败棘轮：merge-base 三点差 + 放行可见化 | **PASS** |

**无 P0。** 无 FAIL → 本次不入 `AUDIT-FINDINGS-LEDGER.md`（按派单 §三.3「FAIL 必进台账」口径，CONDITIONAL PASS 的 P1/P2 记本报告）。

---

## 1. D584｜P1 DSH 线测试债（sentinels 与契约批）→ **PASS**

**声称**：`tests/sentinels` 39 文件三态处置（27 修 + 12 删）；配套契约/环境/electron 对齐。
**独立复验**：
```
$ npx vitest run tests/sentinels/     → Test Files 178 passed (178) | Tests 696 passed (696)  [exit 0]
$ npx vitest run tests/sentinel/      → Test Files 32 passed | 1 skipped (33) | Tests 227 passed | 1 skipped (228)
```
- 提交 `9afb1919`（#417）变更 **50 文件**（+326/−783）——与「多文件三态处置」方向一致；`git show --numstat -- tests/sentinels/` 显示删除型改动（`-` 记数）集中在已退役哨兵测试（`structural-change*`/`token-economics`/`hona`/`seven-powers`/`self-awareness` 等），属铁律 37「测已退役特性」删除口径。
- **测试债已清零的可复验证据 = 178/178 文件 + 696/696 用例全绿**（本审计实跑）。
- `tests/sentinel/` 存量 1 skip 为环境依赖（同 D713 的 `release/` 产物断言组），非恒失败。

**判定**：测试债任务的核心承诺（suite 全绿）**物理成立**。

## 2. D589｜stale 26 点重验批 → **CONDITIONAL PASS**

**声称**（task-state `impl.evidence`）：9 份 per-line evidence（schema1/test/带 at/真实 quote）落 `docs/synova/product-lines/evidence/`；calc 重算 `stale 17→10`、`pending_k3 36→44`、`verified 维持 8`。

**独立复验**：
```
$ ls docs/synova/product-lines/evidence/stale-reverify-D589-*.json | wc -l → 9
  line1(line 1: 1-1/1-3/1-4/1-5/1-6/1-7) · line10(10-4) · line11(11-1,11-2) · line18(18-1)
  line2(2-5) · line20(20-5) · line24(24-4) · line7(7-1,7-3) · line9(9-2)   → 16 个 verdict 全 pass
  全部 record_type=test / date=2026-09-08 / at=2026-09-08T02:15:37+08:00（CT-62 时间戳纪律成立）
$ python3 scripts/product-lines/calc-progress.py
  → 产品总进度 1% | 已验证验收点 1 | 待重跑 34 | 审计否决 2
  → points: uncommitted 96 / stale 34 / pending_k3 27 / failed 4 / rejected 2 / verified 1
```

**P1（口径失实/已失效）**：task-state 声称的 `stale 17→10 / pending_k3 36→44 / verified 8` 是 **2026-09-08 的即时快照**，当前实测为 `stale 34 / pending_k3 27 / verified 1`。**该断言不可复现**（非 D589 虚报——状态机是活系统，后续 8 天的变更必然改写计数；但 task-state 以现在时陈述，构成 M2 口径问题）。

**P1（证据前向效力已被推翻）**：D589 的 9 份证据对线 1 六点（1-1/1-3/1-4/1-5/1-6/1-7）**全部判 pass**，其中 **1-4 与 1-5 现在均为 `failed`**：
- 1-4：D589 的判据是 `backend-spawn.test.ts 全绿 + main.cjs ensureBackend/before-quit/isPackaged WIRE CHECK 断言全绿`（`stale-reverify-D589-line1.json` verdicts[2]）——**这组断言无法发现 F3 渲染层断链**（正是 D715:159 指出的「断链时全绿」盲区断言组）。9 天后 D713/D714 探明 1-4 实为 P0 产品缺陷。
- 1-5：D589 判据是「单一入口双引导互斥断言绿（app.isPackaged/loadFile 分支）」——**代码分支互斥 ≠ 旧引导已下线**；实际旧引导物理可达（HTTP 200），D716/#538 才收敛。

**这暴露 D589 一类工作的结构性风险**：以「跑既有测试出新证据」为恢复手段，**当既有测试本身不覆盖真缺陷时，产出的是「绿得没有信息量」的证据**。D589 自身已在 `impl.residuals` 诚实登记了 `2-5 机制缺口（CT-62 at 粒度只接 k3 路径…）`，但未预见 1-4/1-5 的假绿。

**判定**：机制（D579 stale 重验）按设计工作，证据文件 schema/时间戳/quote 全部合格；**但恢复出的 pass 判定对 1-4/1-5 已被后续物理事实推翻**，作为验收点状态依据须以最新证据为准（当前状态机已正确显示 failed）。

## 3. D597｜D588 会话投影接线（M3 第 5 次修复）→ **PASS**

```
$ grep -n "projection" src/deploy/bootstrap.ts
  24:// M3 修复（D597/D588）：副作用导入——session-projection 模块加载时原型级 wrap
  27:import '../store/session-projection';
$ grep -rn "deploy/bootstrap" src/ | grep -v test
  src/server.ts:86:import { Bootstrap } from './deploy/bootstrap';   ← 生产入口确在
$ npx vitest run tests/store/session-projection.test.ts → Test Files 1 passed | Tests 7 passed (7)
$ bash scripts/check-architecture.sh → 架构检查: 全部通过 ✅
```
**接线链完整**：`server.ts:86` → `deploy/bootstrap.ts` → `:27` 副作用 import → 模块加载即 wrap `SessionStore.prototype.appendEvent`。
**M3 模式（机制建成未接线）第 5 次修复有效**——本审计确认 import 未在后续批次被回退，且投影测试 7/7 实跑通过。

## 4. D601｜任务看板映射校准 + L1 跨层违规扫描 → **PASS**

**交付物**：`docs/synova/research/L1跨层违规扫描-20260908.md`（178 行）+ `sync.js`/`sync.test.js` 映射校准。
**关键声称的独立复验**：
```
$ bash scripts/check-architecture.sh → 架构检查: 全部通过 ✅
```
交付物 §摘要核心论断：「现网 `check-architecture.sh` 输出『全部通过 ✅』——与 **68 处实违**并存的假绿，恰是四类漏网形态的实证」。
**该「假绿与实违并存」论断本审计可确认为结构性事实**：交付物 §一 列出的漏网形态 1「动态 import 全逃逸（只 grep `from.*路径`，`await import()`/`require()` 无 from 不可见）」与 `check-architecture.sh` 的实现方式一致——静态 grep 型门禁对动态导入零可见性。**这是本批最有价值的控制塔发现之一**（68 处 runtime 违规 / 42 处为动态，62% 在门禁视野外）。

**范围核对**：任务为**只读扫描 + 报告**（brief 声明「未改 src/、未改 scripts/audit/、未改任何门禁」）；写集含 `sync.js`（映射校准，非门禁）+ 扫描报告。**纪律遵守**。

## 5. D660｜任务看板活动判定重排 → **PASS**

```
$ grep -n "ACTIVITY_WINDOW_MS" dsh/plugins/task-board-adapter/lib/sync.js
  54: * 活动判定窗口：48h（创始人 2026-09-10 定）。
  57:export const ACTIVITY_WINDOW_MS = 48 * 60 * 60 * 1000;
$ node --test dsh/plugins/task-board-adapter/test/sync.test.js → # tests 42 | # pass 42 | # fail 0
```
- 语义落位与声称一致：`sync.js:14-15` 注释明确 `running = claimed/spec_done 且远端分支 48h 内有提交（活动判定，非状态判定）`；`impl_done → todo`（防假完成且不占 running 列）；`backlog` 三源（board-backlog PLAN-* / todos.yaml T-* / 产品线 0 verified）。
- `1730c9a3` 写集 7 文件（sync.js 78 行 + derive-board-sources.py 82 行 + 两测试 + README + brief + task-state）与任务声称一致。

## 6. D661｜grep -oP 可移植家族修复 → **PASS**

```
$ grep -rn "grep -oP\|grep -P" scripts/ .github/ → 仅剩 4 处非调用命中：
   scripts/pre-commit-check.sh:1432/:1439（检测器自身 + grep-P-scan-ok 标记）
   scripts/control-tower/PLATFORM-CHECKLIST.md:48-49（文档描述）
   scripts/hooks/post-commit.sh:139（注释描述 D421 历史）
   .github/workflows/ci.yml:140 ← 唯一实际调用，见下
$ bash tests/control-tower/grep-oP-regression.test.sh → 结果: 38 通过, 0 失败  [exit 0]
```
**`ci.yml:140` 的 `grep -oP` 判定为「非缺陷」**（三项物理依据）：
1. 该行所在 job `runs-on: ubuntu-latest`（`ci.yml:12`）——runner 是 GNU grep，`-P` 可用；
2. 缺陷家族的真实场景是 **macOS BSD grep 无 `-P`**（D313/D421/D660/D661 四次事故全在 Mac 侧脚本）；
3. 回归网 `grep-oP-regression.test.sh`（D664 扩容）声明的覆盖范围是 `scripts/` 全目录（header 明写「正常 — scripts/ 全目录（.sh）内 grep -P 变体零命中」），`.github/` 不在其声明范围内。
**结论**：修复家族在声明范围内已清零（scripts/ 全目录零实际调用），回归网 38 断言全绿且含反向哨兵（「塞入一处 -oP 必须被网抓到（防永远绿的空转网）」）。

## 7. D663｜CI hermetic 架构棘轮 → **PASS（局部审）**

```
$ git log -1 --format='%h %ad %s' --date=short 2a658d41
  2a658d41 2026-09-10 fix(D663): CI 常红根治——密封测试身份自持 + 架构棘轮明细 + gate 密封化 (#480)
$ grep -n "hermetic\|ratchet" .github/workflows/ci.yml
  195: - name: Run ratchet baseline test (05-as-any-audit)
  214: - name: Run hermetic control-tower gate tests
  257:   tests/control-tower/ci-ratchet-base.test.sh
```
**接线成立**：hermetic gate 测试在 CI 中作为独立 step（`:214`）运行。
**审计边界（如实标注）**：task-state 声称「main 实证 19/19 check、三红 job 全 success」依赖 **GitHub Actions job 级结论**——K3 侧无 API 凭据（`which gh` 零命中），**本轮无法独立复验 job 级结论**（D320 教训：job 级才是有效口径，整体 run 结论不算）。CI 接线与测试在仓内可验，job 结论为**声称无据（环境受限）**，非恒失败。

## 8. D665｜main build job 失败修复（gitlink 摘除）→ **PASS（局部审）**

```
$ git log -1 --format='%h %ad %s' --date=short 9aabd581
  9aabd581 2026-09-11 fix(D665): gitlink 树级守门——摘除 10 个运行时目录 gitlink + .gitignore 防再入 + check-gitlinks 三态守门
$ git log -1 --format='%h %ad %s' --date=short 638bfb5b
  638bfb5b 2026-09-11 fix(D665): 补齐 gitlink 摘除——synova-commit 部分提交回滚吞删除项的补救提交
```
**修法评价**：用「树级守门 + .gitignore 防再入 + 三态守门」治 gitlink 类问题属**根因修法**（不只清存量，堵再入路径）。`638bfb5b` 补提交自我披露了根因链（`synova-commit` 部分提交回滚吞删除项 → 摘除未生效）——**诚实**。
**审计边界**：CI job 结局同 §7，环境受限不可复验。

## 9. D721｜CI 存量失败棘轮（merge-base 三点差 + 放行可见化）→ **PASS**

```
$ bash tests/control-tower/ci-ratchet-base.test.sh
  ✅ ⑧ 基准取自 refs/remotes/origin/main（与 D708 gate 同源）
  ✅ ⑨ G12 基准取自 merge-base（合并提交不再误判越界）
  ✅ ⑨ G12 回退路径带可见提示
  ✅ ⑨ G12 主路径已改为三点差（基准变量..HEAD）
  结果: 15 通过, 0 失败   [exit 0]
```
- **数字漂移注记**：task-state `evidence.测试` 称「12 通过 0 失败」，实测 **15 通过**——测试后续增补（含 G12 三点差 3 条），非虚报。
- **修法语义正确性**：三点差（`merge-base(base,HEAD)..HEAD`）是消除「merge 提交误判新增失败」的正确基准（F2 实证：#513 head 与 5aae38fc `git diff` 为 0 文件却判 3 新增失败）；F1 实证中 `Pre-existing test failures from unchanged files — not blocking` 的**放行可见化**避免静默放行（铁律 11 精神）。

---

## L4 防线缺口收割（C 组合并）

| 缺口 | 本该拦住的防线 | 为什么没拦住 | 归属 |
|---|---|---|---|
| **静态 grep 型架构门禁对动态 import 零可见性**（D601 实证 68 处实违 vs 脚本「全部通过 ✅」） | `check-architecture.sh` | 门禁实现只匹配 `from '...'` 静态形态；`await import()`/`require()` 不可见 | `control-tower`（门禁本体） |
| **「跑既有测试出新证据」可产出无信息量的绿证据**（D589 对 1-4/1-5 的 pass 后被推翻） | 产品线 stale 重验机制（D579） | 恢复手段复用**旧断言集**；旧断言不覆盖真缺陷时，绿≠可用 | `audit`（重验须附「该断言集能否发现本点历史缺陷」判断）|
| **定位/修复类交付物无双向引用**（D741→D748 已修但报告仍挂开着态） | 派单/台账 | 无 `closes_finding` 字段，合并时不回写原报告 | `control-tower` |
| **CI job 级结论依赖审计方凭据**（D663/D665/D518 C1 均受影响） | 证据规范 | 允许以「API 实抓」叙述代替落盘响应原文 | `audit` + `devdoc` |

## 归属汇总

- **归因: control-tower** — 动态 import 门禁盲区（D601 已实证，建议独立任务）；派单 `closes_finding` 字段。
- **归因: audit** — 重验类任务增加「断言集覆盖度」判据；CI 结论类断言须落盘原文。
- **归因: implement** — 无（C 组被审代码未发现实现缺陷）。
