# D651 K3 独立审计报告：双线编码管线代码质量对比评估（评估报告自审）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 C 组第 10 项） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §C 组（「评测口径可复现性」）
> 派单原件: `docs/synova/coordination/评估派单-20260909-D651-K3双线编码管线代码质量对比评估.md`
> 被审产物: `docs/synova/audit-reports/2026-09-09-D651-dual-pipeline-quality-eval.md`（46272 B / git 跟踪 `c2819e08`）
> **审计方法（关键）**: 不复用当前 main——在 K3 工作区新建 **pinned worktree @ `742a3416`**（= 报告自述的评估时点 main），
> 使复跑条件与评估时点严格一致（消除「树已变化导致数字对不上」的噪声）。8 样本 + 全局基线**全部按报告原文命令重跑**。
>
> **数字口径: `742a3416`（+环境）** —— 环境 = Mac / Node v22.22.0 / node_modules 为审计工作区**共享安装**（非 clean `npm ci`）。
> 创始人 2026-09-16 在 **clean pinned worktree @ `742a3416`** 独立复跑：`29 failed` ✅ / `24 skipped` ✅ 与本报告逐位一致；
> `passed` = **4266 vs 本报告 4245（+21）**——差异归因于安装态（fresh clone vs 共享 node_modules），**判定环境差异、非失实**。
> 此即 P2-1 的现场实证：**数字必须双列（sha + 环境）**，纯数字差不可直接判失实。

## 〇、结论：**PASS**（完美复现；自审边界已声明）

## 一、复现结果：8/8 样本 + 全局基线**逐数字命中**（@742a3416）

| 样本 | 任务 | 报告声称复跑结果 | K3 pinned 重跑实测 | 判定 |
|---|---|---|---|---|
| W1 | D485 | `tests/routes/enterprise.test.ts` → 20/20 | **20 passed (20)** | ✅ 逐数字一致 |
| W1 | D485 | `auth.test.ts + auth.integration.test.ts + routes/auth.test.ts` → 40/40 | **40 passed (40)** | ✅ |
| W2 | D487 | `diagnosis-session-events 5/5 + session-event-log 17/17 + conversation-engine 11/11` | **33 passed (33)** = 5+17+11 | ✅ |
| W3 | D489 | `diagnosis-consult-events.test.ts` → 4/4 | **4 passed (4)** | ✅ |
| W4 | D492 | `task-decomposer.test.ts` → 12/12 | **12 passed (12)** | ✅ |
| M1 | D551 | `ga-calibration + ga-auth + ga-manual-injection` → 40/40 | **40 passed (40)** | ✅ |
| M2 | D575 | `llm-credential-store + llm-config + llm-config-frontend` → 64/64 | **64 passed (64)** | ✅ |
| M3 | D577 | `threshold-injection` → 10/10；`threshold-manifest-flip`（DS8 物理 flip）→ 1/1 | **10 passed (10)**；`D577_FLIP_TEST=1` → **1 passed (1)** | ✅ |
| M4 | D567+D568 | `enterprise-fact-chain + expert-enum-propagation + manifest-consistency` → 21/21 | **21 passed (21)** | ✅ |

**全局基线**（报告 §2.2 称 `exit 1`，`10 failed | 582 passed | 3 skipped (595)` / `29 failed | 4245 passed | 24 skipped (4298)`）：

```
$ npx vitest run @742a3416
  Test Files  10 failed | 582 passed | 3 skipped (595)
  Tests  29 failed | 4245 passed | 24 skipped (4298)      ← 与报告逐数字一致（含失败数）
$ npx tsc --noEmit --pretty false | grep -c "error TS" → 28   ← 与报告 §2.1 "28 errors" 一致
```

**这是本批审计中唯一做到「全量数字逐位复现」的交付物**——包括**失败数**（29）这种通常无人复核的细节。

## 二、M3 的 `D577_FLIP_TEST=1` 门控：设计正确，非缺陷（重点澄清）

在**非 pinned** 的当前 main 上重跑 M3 时曾见 `threshold-manifest-flip.test.ts` **1 skipped**——追查后确认**是刻意的运行纪律门控，非失效**：

- `tests/sentinel/threshold-manifest-flip.test.ts:19-20`：`const RUN_FLIP = process.env.D577_FLIP_TEST === '1'`
- 文件头 `:11-14` 明示理由：「仅 `D577_FLIP_TEST=1` 时运行（fs 改 manifest 会污染并发读 manifest 的测试，禁止混入全量并行）」
- `:15-16` 恢复纪律：「fs 改动 try/finally 等价恢复（afterEach 无条件回写原始字节 + `clearSentinelCache`）」
- 实测带 flag 运行 → **1 passed**，且运行后 `git status --short extensions/` **为空**（manifest 已还原）

**评价**：该测试会物理改写 `extensions/sentinels/customer-demand-shift/manifest.json`——用环境变量门控隔离，避免污染并行/全量测试，同时保证可手动复现。**这是「会改盘的物理验证」的正确做法**（对照组见本批 D713 的封印测试 §6b：hdiutil 受限时显式 skip 而非静默）。

## 三、方法论与交付要求核查（逐条）

| 派单要求 | 核查 |
|---|---|
| 报告 git 跟踪 | ✅ `git log --oneline -1 -- <报告>` → `c2819e08 audit(D651): K3 双线质量评估 (#460)` |
| 每条结论附复现命令/退出码 | ✅ 8 样本每样本「③独立复跑」列命令 + exit 码（本报告 §一 逐条实测一致） |
| 环境受限项显式标注 | ✅ §6.1 明列 Windows GUI / 真实 DeepSeek 上游 / CI job 级 / 历史 RED 不可重放 → 「不作为评分依据」 |
| 维度表 + 制定理由放 §1 | ✅ §1 给 7 维（C1-C7）+ 权重（合计 100%）+ 10 分/0 分锚点 + 制定理由（Google CR 指南 / IEEE 25010 / Pragmatic Programmer，**刻意不采用仓库自定铁律**以保独立性） |
| 独立性（评估维度自定，CTO 不预设） | ✅ 派单第 4 行原文「评估维度、权重、评分标准、结论框架由评估人（K3）自行制定」；报告 §0.4 亦声明「仓库自定标准仅作背景，不作评分主轴」 |
| 不做胜负标签 | ✅ §4.1 表格只给两侧均值（82.38 vs 84.00）+ §6.2 明示「n=4/侧，不构成统计显著性主张」「仅作参考，不构成胜负标签」——**克制且诚实** |
| 只读纪律 | ✅ §6.4 声明未改 src/、electron-renderer/、门禁脚本、scripts/audit/、product-lines.yaml 及任何样本文件 |

**扣分项挂 file:line 可核对**（§6.3 自认「评分主观性」但「所有扣分项均挂 file:line 或复现命令，可被第三方复核对冲主观性」）——本审计验证了这一点：抽查 §4.3 的 3 条 Win 侧问题锚点：
- `src/growth/user-store.ts:218-224` catch 吞错返 void / `src/routes/enterprise.ts:241` 恒返 linked:true
- `src/routes/enterprise.ts:230-232` ACCOUNT_DISABLED 分支
- `src/agent/task-decomposer.ts:100,102`（DS1 声称「0 命中」实测 2 命中）
→ 锚点均真实存在于代码（非虚构 file:line）。

## 四、自审边界声明（本报告的特殊限制）

**这是「K3 审 K3」**——评估报告作者与本次审计员同属 K3 角色。因此本报告的效力边界必须明示：

| 本报告**能**确认 | 本报告**不能**确认 |
|---|---|
| 所有可复现的数字/命令/退出码是否与报告逐位一致（已做，全中） | 评分判断本身是否「正确」——0-10 打分含评估人主观权重（报告 §6.3 已自认） |
| 方法论、交付要求、只读纪律是否落实（已逐条核） | 样本选择是否公允（抽样不是随机抽取，报告 §6.2 已自认「被样本选择影响」） |
| file:line 锚点真实性（抽查 3 条，均真） | 「Win vs Mac 谁更好」——报告未下此结论，本审计亦不下（**双方均未越界**） |

**建议**：D651 的**评分结论**（两侧均值 82.38 vs 84.00）作为创始人裁断输入时，应视为**评估人专业判断**而非「已验证事实」；本审计确认的是**其证据基础与可复现性成立**（这一点是评分可信的前提，已达成）。

## 五、P2 观察（不影响结论）

### P2-1｜报告未给「复跑时点」与「评估时点」的数字差异声明

本审计在同一报告上做了两次复跑：**current main**（`eb357405`）与 **pinned 评估时点**（`742a3416`）。数字差异反映出这一周的自然增长：
- W1 `enterprise.test.ts`：**20 → 22**（+2 用例）
- W2 三文件：**33 → 33**
- W4 `task-decomposer.test.ts`：**12 → 13**
- M4 三文件：**21 → 25**

即报告数字**在其评估时点完全准确**（§一 已证），但**一周后不再对得上**。报告未附「数字绑定 742a3416」的显式声明——第三方若在 current main 复跑会误判为「数字失实」（本审计首轮即经历此陷阱）。

**这与本批 CT-67 同源**（证据数字须绑时点）：建议 D651 类评估报告头部固定加一行 `数字口径: <sha>`。**归因: audit**（评估报告规范）/ **devdoc**（证据规范）。

> **处置（2026-09-16，创始人验收指令）**：已采纳并**升级格式为 `数字口径: <sha>（+环境）`**——本报告头已加（见文件头），
> 并即日起固定写入 K3 全部报告头（同批已应用于 `2026-09-10-K3-D593FIX2-closeout.md` §11 / `2026-09-10-K3-D593FIX-D595-closeout.md` §10）。
> 创始人 clean pinned worktree 复跑（passed 4266 vs 4245）为 `+环境` 字段的实证依据。

### P2-2｜D485 / D492 两个样本任务本身仍无 K3 审计裁决

D651 的 Win 侧样本 W1=D485、W4=D492，其 task-state 现状：**D485 `impl_done` / audit 空**；**D492 `impl_done` / audit 空**（另 6 个样本 D487/D489/D551/D575/D577/D567/D568 均已有审计裁决）。
即：**D651 从「代码质量评估」角度审过它们，但从未从「任务交付验收」角度审计过**——两者不可互相替代（前者评质量、后者判验收点）。建议 CTO 将 D485/D492 排入后续审计批次（D485 是 register 认证闭环切片 C，用户可见链路）。

## 六、L4 防线缺口收割

**问题：D651 是本批唯一「全量数字逐位复现」的交付物，说明「精确留痕」是能做到的——那为何本批其它证据大量失实？**

| 防线 | 本该做什么 | 为什么没拦住 |
|---|---|---|
| 证据规范（数字 + 命令 + 退出码） | 让第三方可复现 | **D651 做到了**（派单 §六.2 明写「每条结论附复现命令/退出码」）——证明**要求写进派单即可落地** |
| 一般任务的 evidence 规范 | 同上 | 派单未强制同等要求 → D759/D751/D754 出现「数字与实测不符」（本批已记） |
| **缺的机制** | **把 D651 派单 §六.2 的要求升格为通用规则**：证据中的任何数字必须附「命令 + 原始输出 + 时点 sha」 | 无此通用条款 → 质量取决于单个派单写得细不细（**M2 的根因：要求靠个案传达而非机制**） |

**归属**：`control-tower`（派单模板）+ `devdoc`（证据规范）→ 已并入本批 CT-67 收口（一处机制，不为 D651 单开）。

## 七、归属与建议

- **归因: audit**（P2-1 评估报告时点声明）+ **control-tower**（P2-2 排 D485/D492 补审；CT-67 收口）。
- **无 P0/P1**。D651 交付质量为本批最高（唯一全量逐位复现），建议维持 `impl_done` → `audited` + PASS。
