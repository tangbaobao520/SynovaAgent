# D712 K3 独立审计报告：桌面端线 1 证据重验批（Mac 侧 6 点）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 B 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §B 组
> 复核方式: 证据目录/文件逐条内容对齐（B 组口径）+ 状态机流转交叉验证；Mac 实测本体不重跑（安装/打包属 D712 实施方实测，本单审「取证真实性」）

## 〇、结论：**PASS**（3 条轻微 finding，见 §三）

## 一、六点 quote ↔ 证据文件逐条对齐（6/6 一致）

| 点 | impl.points 声称 | 证据文件实测 | 判定 |
|---|---|---|---|
| 1-1 | arm64 dmg 356M md5=55b5…f70 + 三产物 + Info.plist 0.1.0==package.json | `D712-mac-20260913/1-1-artifacts.txt`: 356M/MD5 一致/arm64-mac.zip 106M/mac.zip 111M/版本一致性 3 处 0.1.0 全在 | ✅ |
| 1-3/1-4 | 四断言全过（A1 进程/A2 窗口/A3 healthz 200/A4 日志非空） | `1-3-1-4-mac-install-assertions.txt`: A1-A4 全 `1`；佐证 `1-3-dmg-ls.txt`/`1-3-dmg-md5.txt`/`1-3-healthz.json`（endpoint 真实响应，degraded=首启未初始化的业务态，与 HTTP 200 可达不矛盾）/`1-3-window.txt`=true | ✅ |
| 1-5 | FAIL: 旧引导 HTTP 200 + server.ts:300 挂载 + 3 文件仍在 | `1-5-dual-guide-check.txt`: 三项物理事实全在（curl 200+title、:300 挂载行、admin.js/setup.js/setup.js 时间戳） | ✅ |
| 1-6 | total_sec=8.7 / target 1800 → WITHIN_TARGET | `1-6-timing.json`: total_sec 8.7/target_sec 1800/verdict WITHIN_TARGET + 5 个里程碑时间戳 | ✅ |
| 1-7 | DATA_RETAINED / md5 v1=v2=d0c3…350 / integrity ok / tables 一致 | `1-7-upgrade-assertions.txt`: 全部原文在；tables.diff 空=一致；rows 探针瑕疵（no such table: sessions）已在文件内如实标注 | ✅ |
| 1-1~1-7 records | 5 条 scenario JSON（evidence-writer.py 写入） | 5 个 `scenario-2026-09-13*.json` 全存在，均 `"written_by": "evidence-writer.py"`、逐点 verdict+quote 且 quote 与证据文件内容一致 | ✅ |

## 二、1-5 判 fail 的口径成立（交叉互证）

1-5 验收点为「安装引导单一入口」。D712 时点（2026-09-13）：旧 Web 引导物理可达（HTTP 200）+ 新引导并存 → 双引导未收敛 → **fail 判定口径成立**。
后续互证：D716（PR #538，2026-09-14/15 合入）做的正是「旧引导退场（410 显式下线+回归守卫+删 app/setup 全套）」——若 1-5 当时非真，无需此修复。当前基线 `app/setup.html/app/js/setup.js` 已删（`ls app/` 无 setup*），`tests/routes/setup-guide-retired.test.ts` 276 行守卫在。

## 三、findings（轻微，不构成 FAIL）

1. **「tables 54 一致」的数字 54 无原始输出佐证**——`1-7-upgrade-assertions.txt` 的 tables.diff 为空（支撑「一致」），但表数 54 未落盘。
2. **refresh 快照未入 git**——impl.evidence `refresh_all` 声称「线 1 counts = failed 1 / pending_k3 6 / stale 1」为 09-13 主工作区运行态；d780 基线内 `product-progress.json` 是 09-12 旧版（线 1 全 stale），主 checkout 未提交版为 09-15 22:19（pending_k3 7/stale 1）。中间态无法从提交历史复现，但流转自洽：09-13 fail(1-5) → #538 修复 → 09-15 pending_k3 7/1-2 stale(Win)，与状态机语义及 #538 时间线吻合。快照固化习惯待改进（M4 轻微）。
3. **1-6 quote 中「consult 入口 HTTP 400 可提交」细节未在 timing.json 落盘**（仅 milestones 时间戳），口径瑕疵。

## 四、TTL 提示（给 CTO/创始人）

线 1 各点证据（09-13）在 #538 合入后按 A1 规则（证据日期后 modules 变更 → stale，`calc-progress.py:29`）已触发重验需求；主 checkout 09-15 版已回到 pending_k3 7 点。**pending_k3 → verified 的兑换窗口依赖 K3 复核**——这正是本批次四的价值所在（派单 TTL 硬约束实锤）。
