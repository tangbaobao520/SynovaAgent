# D713 K3 独立审计报告：桌面端打包链签名 + 出货前门禁（修 P0「打都打不开」）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 B 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §B 组 · PR #512（`75c8e5be`）
> 审计基线: 独立工作区 `/Users/wane/Synova-k3独立审计` 内 git worktree `.audit-batch4` @ `eb357405`（= origin/main，含 #512 合入态）
> 审计环境: macOS arm64 · node v22.22.0 · **测试在 K3 自有工作区执行**（非被审 worktree，排除宿主环境污染）

## 〇、结论：**CONDITIONAL PASS**

机制本体成立：签名门禁脚本可重跑且三态退出正确、CI 接线在产物上传**之前**、构建期签名失败/自检失败**真阻断**。
扣分项为**证据口径与覆盖面**（P1×2），不改变「构建链修复有效」的方向性结论，故不判 FAIL。

## 一、独立重跑（5 项，4 项全绿 + 1 项环境受限）

### 1. 签名门禁密封测试 → ALL PASS ✅

```
$ bash tests/desktop/verify-package-signature.test.sh
  1. 未签名 bundle → exit 1            ok
  2. ad-hoc 签名 bundle → exit 0        ok（fixture 封印已生成）
  3. 降级：路径不存在 → exit 2          ok（fail-closed）
  4. 降级：无参数 → exit 2              ok
  5. --all 对空目录 → exit 0            ok
  6. dmg 挂载点解析文法（含空格卷名）    ok ×2（含「旧实现确会截断」反向断言）
  6b. dmg 端到端                        **skip: hdiutil create 在本环境被拒**
  7. 接线: desktop-build.yml 真的调用本门禁  ok
  [verify-package-signature.test] ALL PASS    [exit 0]
```

**运行环境注记（D316 教训，区分环境依赖 vs 恒失败）**：`6b` 的 skip 由 `hdiutil create` 在本沙箱被拒导致（APFS 镜像创建权限），**非恒失败**——断言本体存在，CI macOS runner 会执行该分支。dmg→挂载→校验→卸载的真实路径在本机**无法复核**。
独立计数：实测 `ok:` 行 = **9**，与 impl.evidence `tasks` 字段称「9 断言全过」**一致**。

### 2. 门禁三态退出契约 = 真 fail-closed ✅

`scripts/desktop/verify-package-signature.sh`：
- `:46-49` 非 Darwin → `exit 2` + 显式 degraded 文案（**不静默放行**）
- `:51-54` `codesign` 不可用 → `exit 2`
- `:36 / :40-43` 未知参数 / 缺参数 → `exit 2`
- `:66-71` 无 `Contents/_CodeSignature/CodeResources` 封印 → `exit 1`
- `:72-76` `codesign --verify --deep --strict` 失败 → `exit 1`

即「执行不了」与「不合格」语义分离（2 vs 1），符合铁律 11/31 的降级诚实性。

### 3. 构建期阻断链（三条 throw 路径）✅

`build-synova.cjs`：
- `:89-91` `codesign --force --deep --sign -` 失败 → `throw`（签名失败即红）
- `:93-95` 签名后立即 `codesign --verify --deep --strict` 失败 → `throw`（**自检失败即红**）
- `:141` `beforePack: (context) => assertBackendArtifact(...)`（D581 构建守卫，封印缺失即红）
- `:200` `identity: null`（显式声明不追 Developer ID，避免 electron-builder 静默 skip）
- `:207-212` `beforePack`（按 arch 备 native）→ `afterPack`（签名+自检）钩子接线在位

### 4. CI 接线在「产物上传之前」✅（M3 接线模式的正解）

`.github/workflows/desktop-build.yml`：

| 行 | 步骤 | 说明 |
|---|---|---|
| `:100-108` | Assert artifacts exist | 只断言**存在**（原缺陷） |
| `:118` | Signature gate **self-test** | 先跑门禁自测（门禁自身坏了也能拦） |
| `:120-123` | Verify package signatures (D713) | `--all --dir release` 校验**产物本身** |
| `:125-130` | `upload-artifact` | 上传在签名门禁**之后** |

顺序正确：坏包在进入 artifact 之前即被拦（`if: runner.os == 'macOS'` 限定平台，非 mac 平台不误判）。「先自测再校验产物」是门禁设计里的正确防御（防门禁自身失效导致的整体放行）。

### 5. `desktop-build.test.ts` 重跑 → **26 tests = 23 passed + 3 skipped** ⚠️

```
$ npx vitest run tests/electron/desktop-build.test.ts
  ✓ tests/electron/desktop-build.test.ts (26 tests | 3 skipped) 30ms
  Tests  23 passed | 3 skipped (26)
  [desktop-build.test] release/ 不存在——产物断言组 skip（未构建环境，非失败）
```

## 二、Findings（P1×2，不影响方向性结论）

### P1-1｜证据文件「26/26」口径失实（实测 23 通过 + 3 跳过）

`docs/synova/product-lines/evidence/D713-published-artifact-20260913.json` 的 `context.tests` 字段称：

> `tests/electron/desktop-build.test.ts → 26/26`

实测该套件共 26 用例，其中 **3 个 `it.skip`**（`tests/electron/desktop-build.test.ts:168-170` 的「D517 产物物理断言组」，`release/` 目录不存在时 `it.skip`）。因此：
- 独立工作区（无 `release/`）→ `23 passed | 3 skipped`
- 即「26/26」只有在**含 `release/` 产物的构建机**上才成立；报为无条件 26/26 属口径不准。

方向性影响小（跳过项是**产物物理断言**，其覆盖内容已由 D713 自己的签名门禁承担），但**作为产品线验收证据的数字必须可复现**，如实记录。

### P1-2｜被引为该证据支撑的测试**完全不含 D713 签名覆盖**（M4 证据链断裂）

`tests/electron/desktop-build.test.ts` 全文 `grep -c "D713\|codesign\|verify-package-signature"` = **0**。
该文件的 23 个 `it` 全部属 D504 / D517 / D581 三批（标题实测：`D504 打包配置`、`D504 main.cjs`、`D517 打包配置`、`D517 CI`、`D517 产物物理断言组`、`D581 构建守卫`）。
即：**D713 的签名/门禁行为，其真实测试载体是 `tests/desktop/verify-package-signature.test.sh`（9 断言，已独立复核 ALL PASS）**；
证据文件把 `desktop-build.test.ts` 并列为 D713 测试支撑属**归因错误**——它验证的是打包配置与产物存在性，不验证签名。

### P2-1｜主验收点 1-3 的证据物已不在场，本轮无法物理复核

证据称 `release/SynovaAgent-0.1.0-arm64.dmg`（md5=`00cdeac3ba59dfe6632fbeff65372dc2`、114195341 bytes）签名封印有效。
`release/` 目录当前不存在（`.gitignore` 不入库），**该 dmg 与 md5 无法在本轮重算** → 1-3 的 `pass` 属「当时实测、现在不可复现」。
判据（机制侧）仍成立：门禁脚本对 dmg 的校验逻辑在位（`:59-79` app 封印 + dmg 挂载分支），且 CI 每次构建都会重跑。

## 三、验收点判定（D713 关联）

| 点 | 证据自称 | 独立复核 | 判定 |
|---|---|---|---|
| 1-3（发布物路径可安装可开窗） | pass | 机制在位（门禁脚本+CI 接线+构建期阻断）；**dmg 本身不可复现** | ✅ 成立（不可复现注记见 P2-1） |
| 1-4（开窗「即用」） | **fail**（CTO 自我修正） | 留待 D714 审（`f3-preload-sandbox-renderer-api-fix`） | ⏳ 转 D714 |

> 1-4 由 pass 自我修正为 fail 是**诚实行为**（证据文件 `context.correction` 显式记录），本报告予以确认而非扣分。

## 四、写集核对

`git show 75c8e5be --stat`：本提交为 iCloud 式 squash 合并（含 D711 通知模型等前序提交正文），D713 实质写集 = `build-synova.cjs` + `scripts/desktop/verify-package-signature.sh` + `tests/desktop/verify-package-signature.test.sh` + `.github/workflows/desktop-build.yml` + `package.json`，与 brief 声称的 6 项写集一致（`electron:verify:mac` 脚本 + memory Note + 证据 json 亦在位）。无 brief 外源码改动。

## 五、L4 防线缺口收割

**问题：D713 的机制本体做得很扎实（先自测再校验、fail-closed 三态、上传前拦截），但证据数字与测试归因失实——哪道防线本该拦住？为什么没拦住？**

| 防线 | 本该做什么 | 为什么没拦住 |
|---|---|---|
| dev doc 声称↔证据表（S-10） | 每条验收证据标注**可复现命令** | 证据 json 引用了 `desktop-build.test.ts`，而该文件 0 行 D713 覆盖——**引用关系没有被机器校验**（只有人写） |
| 产品线 `pending_k3` 兑换 | 兑换前由 K3 复核 | 本轮正是该防线生效：**两条 P1 就是这一层抓出来的**（防线有效，非缺口） |
| 门禁自测（`:118`） | 保证门禁自身有效 | 有效；但不覆盖「证据文件引用的测试是否真覆盖本任务」 |
| **缺的机制** | **证据引用的测试 ≥1 处命中被审任务 ID**（`grep -c "<D#>"` > 0） | 无此断言 → 证据可引用无关测试充数（**M4 证据链断裂**） |

**归属**：`audit`（证据引用关系未纳入审计断言集）→ 建议后续把「证据引用的测试须命中任务 ID」写入审计清单；实现侧无错。

## 六、归属与建议（不改代码，供 CTO 派修复卡）

- **归因: implement**（P1-1/P1-2）→ 修证据文件口径：`context.tests` 改为「`verify-package-signature.test.sh` 9 断言（D713 载体）；`desktop-build.test.ts` 26 用例中 23 通过 3 跳过（D504/D517/D581，不含 D713）」。
- **归因: devdoc**（P1-2 根因）→ 证据文件引用测试时须标注该测试**覆盖率归属**。
- 无 P0；建议随 D714 审计一并复核 1-4 闭环后重验 1-3/1-4。
