# D714 K3 独立审计报告：F3 打包态渲染层断链修复（preload 沙箱 + 渲染层 API）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 B 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §B 组 · PR #514（`7c4bf9ac`）
> 审计基线: 独立工作区 `/Users/wane/Synova-k3独立审计` worktree `.audit-batch4` @ `eb357405`（= origin/main）
> **前置去重声明**: D714 已在 `docs/synova/audit-reports/2026-09-13-D715.md:183-187` 被 K3 审计过（"D714 修复核查"节）。
> 本报告不重复全面审计，做**现状复验**（该报告点到未复跑的一项在此补跑），避免重复劳动。

## 〇、结论：**PASS**

## 一、前置审计已覆盖的结论（引用，非重做）

`2026-09-13-D715.md:185-186` 已核实：`additionalArguments` 透传 + `preload.cjs` 去相对 require 改 argv + 保持 renderer 沙箱（不采用 `sandbox:false` 省事解）+ 启动自检哨兵设计合理。
同报告 `:187` 明确标注：「回归 `tests/electron/` 12 文件 167 用例（commit 声称；**未在本审计复跑**）」——**该缺口由本报告补跑**。

## 二、现状复验（本轮独立重跑）

### 1. preload 收敛真实性 → 成立（无 nodeIntegration 泄漏）✅

| 检查项 | 实测 | 结论 |
|---|---|---|
| 相对 require 已去除 | `electron/preload.cjs:17` 仅 `require('electron')`；`:18-22` 注释记录原 `require('./config.json')` 移除原因 | ✅ |
| 配置来源改 argv | `electron/preload.cjs:23-30` `readLaunchedArg('--synova-server-url=')` + 缺省 `http://localhost:18790` | ✅ |
| 主进程透传 | `electron/main.cjs:89` `additionalArguments: [\`--synova-server-url=${serverUrl}\`]` | ✅ |
| nodeIntegration 未开 | 全 `electron/` 目录 grep `nodeIntegration` → 仅 `main.cjs:88` 一处 = `false`（另 `preload.cjs:4` 注释） | ✅ **无泄漏** |
| contextIsolation 保持 | `electron/main.cjs:87` `contextIsolation: true` | ✅ |
| 启动自检哨兵 | `electron/main.cjs:111-125` `did-finish-load` → `executeJavaScript` 探测 `window.electronAPI` → `[preload-check] OK/FAIL` | ✅ |

> **设计选择的正确性**：修复走 Electron 官方推荐的 `additionalArguments`（沙箱内 `process.argv` 可用）而非关闭沙箱——安全姿态未降级。（`memory/notes/implemented/2026-09-13-f3-preload-sandbox-and-startup-probe.md` 为决策留痕）

### 2. 回归测试补跑（本报告填补 D715 声明的缺口）✅

```
$ npx vitest run tests/electron/
  Test Files  13 passed (13)
  Tests  170 passed | 3 skipped (173)      [exit 0]
```

**运行环境注记（D316）**：首跑出现 1 红，实测为**环境依赖失败**非恒失败——K3 工作区 `node_modules` 快照早于 `package.json` 的 `react-markdown@^10.1.0`（`package.json:88`）新增，`Cannot find package 'react-markdown'`。补该依赖后 **13/13 全绿**。被审代码无缺陷。

**与 D715 声称的差异**：D715:187 记「12 文件 167 用例」（commit 声称），实测现为 **13 文件 173 用例（170 passed + 3 skipped）**——文件数与用例数均增长，属 D714 之后的正常测试积累（非本任务虚报）。

## 三、验收点 1-4 处置（承接 D713 报告的转办）

D713 报告 §三将 1-4 转本单。本报告裁定：

- **机制侧**：修复代码在当前 main 物理在位（§二），启动哨兵资产存在。
- **未闭合项（继承 D715:201 的 P1-4）**：`[preload-check] OK` 的**发布物路径实测原始日志未落 git 证据目录**——`docs/synova/product-lines/evidence/` 下无 D714 证据文件（`ls | grep -i "D714\|f3"` 零命中）。当前 1-4 状态机状态 = `failed`。
- **转绿条件（维持 D715 裁定）**：D714 后重打包 → 安装 → `[preload-check] OK` 日志 **+** 渲染层 API 实测请求成功，双双落盘。

**即：D714 修复本体 PASS，但 1-4 验收点本轮不可转绿**（缺发布物路径实测证据，非代码问题）。

## 四、写集核对

`git show 7c4bf9ac --stat` 与 brief §Q2 声称写集一致：`electron/preload.cjs` + `electron/main.cjs` + D713 证据 json（1-4 判据修正）+ `board-backlog.json` + brief + memory Note + task-state。**D713 证据修正（1-4 pass→fail）由 D714 同批完成**——这解释了 D713 报告中「1-4 由 pass 自我修正为 fail」的出处，两条线自洽。

## 五、L4 防线缺口收割

**问题：D714 修了「窗口绿但 UI 死」的真实 P0，修法与安全姿态都正确，但为什么 1-4 至今转不了绿？**

| 防线 | 本该做什么 | 为什么没拦住 |
|---|---|---|
| 启动自检哨兵（`main.cjs:111-125`） | 运行时自证 `electronAPI` 已暴露 | 机制**有效**，但输出只进 stdout 日志；`did-finish-load` 探针结果**不落盘**，人不在场就蒸发 |
| 记忆四态 Note（铁律 49） | 决策留痕 | 有留痕（memory note），但**不是证据**——D715 已判「仅存于 memory note 自述」不算物理证据 |
| 产品线证据目录 | 验收点转绿须有证据文件 | **无 D714 证据文件产出**（本批 9 个 D589 证据文件同族机制存在，说明能力具备——是执行漏了，非机制缺失） |
| **缺的机制** | **启动探针输出落盘为证据**（哨兵输出 → 文件，非仅 stdout） | 探针设计了却没接「输出捕获」→ 自证信号无法被审计消费（**M3 接线家族：机制建成、出口未接**） |

**归属**：`implement`（哨兵输出未落盘）+ `control-tower`（若要在 CI 自动捕获，需 desktop-build 增加日志采集步骤）。

## 六、归属与建议

- **归因: implement** → 1-4 转绿需一次「D714 后新产物 + 发布物路径实测 + `[preload-check] OK` 日志落 git 证据目录」，建议随线 1 下一次重打包批次一并做，避免单独为取证再打一次包。
- 无 P0/P1；修复本体与安全姿态均正确。
