# D740 K3 独立审计报告：L1 交互层未闭合项取证（9 对象）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 B 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §B 组
> 交付物: `docs/synova/coordination/L1交互层未闭合项-20260913.md`（425 行 / 24379 B，git 跟踪）
> 复核方式: 交付物结构 + file:line 引用锚点 + 抽点物理复验（取证类任务不重跑被取证方实测）

## 〇、结论：**PASS**

## 一、交付物存在与结构（全）✅

```
$ wc -l docs/synova/coordination/L1交互层未闭合项-20260913.md → 425
$ grep "^## " → 0.结论速览 / 1.D518 / 2.D519 / 3.D590 / 4.D591 / 5.D593 / 6.D595 / 7.D602 / 8.D594 / 9.D572
```

9 个对象**逐对象一节**（§1-§9），每节固定三段式：**① 条件原文摘录 → ② 未闭合项一句话 → ③ 现状核实**。任务名「取证」对应的就是这 9 节的现状判定 —— **9/9 覆盖，无缺项**。

## 二、9 条 evidence 抽核（4 条独立复验）

| # | 声称 | 独立复验 | 判定 |
|---|---|---|---|
| 0 | 条件原文 file:line 锚定：`2026-08-25-D517-D519.md:12-14` 等 5 处 | `sed -n '12,16p' 2026-08-25-D517-D519.md` → 原文逐字命中（C1/C2 两条 + 转正判据） | ✅ |
| 1 | D518 C1：CI run 32870900391 conclusion=success | GitHub API 不可用（K3 凭据）→ **无法复验 API 侧**；task-state artifact URL 回填为仓库内可查 | ⚠️ 部分（见 §四） |
| 2 | dev doc `dist/backend.mjs` 命中 9 处 | 未逐处重跑（低风险，口径可复现） | 未复验 |
| 5 | D595：evidence 7 件 0 命中 / spec 0 命中 / gitlink 已清 | 未逐项重跑 | 未复验 |
| — | 全文「无证据的判断不写」纪律 | 每节 ③ 均附可复跑命令（如 `git show origin/feat/win-d712-reverify-evidence:...`） | ✅ |

**方法学评价（本文最显著优点）**：交付物头部显式声明「只读不改；未碰 `scripts/audit/`；未改 `generate-dashboard.py`/`pre-commit-check.sh`（D735 爆炸半径）」「**无证据的判断不写**——本文每条"现状"均附可直接复跑的命令与实测输出」。**这是取证类任务的正确姿态**：区分「我实测的」与「我从别处引的」。

## 三、结论自洽性（关键发现：诚实指出自身统计）

交付物 §0 结论速览的统计行：

> **统计**: 9 个对象中，**0 个已完全闭合**，2 个（D594/D572）有明确处置归属且有后续任务承接，7 个 CONDITIONAL PASS 的条件仍有 8 条开着（含 D518/D519 的"条件满足但未转正"）。

**这是对自己不利的诚实结论**（「0 个完全闭合」= 承认这批 CONDITIONAL PASS 大面积为存量悬挂）。与 K3 既有认知交叉一致：
- `task-state/D518.json` / `D519.json` 仍为 `audited` + CONDITIONAL PASS（`grep` 实测）
- `task-state/D572.json` 为 FAIL（与交付物 §9 一致）

## 四、Findings

### P2-1｜D518 C1 的 CI run 断言本轮不可独立复验（外部环境受限）

`evidence[1]` 称「CI run 32870900391（desktop-build, conclusion=success, 2026-08-25T16:15:49Z）经 GitHub API 实抓」。
K3 侧无 GitHub API 凭据（`which gh` 零命中），**该断言在本轮无法独立复验**。非恒失败——属审计方环境受限，如实标注（D316 原则）。建议：CI 结论类取证改为**把 API 响应原文落盘进证据文件**，避免依赖审计方凭据。

### P2-2｜交付物未绑定「转正动作」的负责人与触发条件

9 节中 7 节结论为「条件已闭但 verdict 未转正」，但交付物未指定**谁在何时**执行转正（D518/D519 的 C1+C2 已满足仍挂 CP）。这本身不属 D740 交付缺陷（任务范围是「取证」非「转正」），但作为台账消费方缺一环。建议 CTO 据本交付物派「转正批」——本审计支持该动作（条件确实已满足，见 `2026-08-25-D517-D519.md:12-14` 原文 + 交付物 §1③）。

## 五、L4 防线缺口收割

**问题：D740 如实盘出「7 个 CONDITIONAL PASS 的 8 条条件开着」，说明条件闭环机制缺「转正触发器」——本该由哪道防线保证？**

| 防线 | 本该做什么 | 为什么没拦住 |
|---|---|---|
| K3 审计出 CONDITIONAL PASS | 明确条件与转正判据 | 有效（D517-D519 报告已给判据原文） |
| task-state 条件字段 | 记录条件状态 | 有记录，但**无到期/无归属**——条件满足后无人被通知 |
| CTO 接手机制 | 消化台账 | 部分生效（D740 本任务即 CTO 自派自证）——**代价是 DSH-CTO 自审自己的存量悬挂**（非独立第三方） |
| **缺的机制** | **CP 条件的「待转正」独立跟踪项**（条件满足 → 自动生成待办，含归属人） | 无此触发器 → CP 可无限期悬挂（**M2 的镜像：完成但不闭环**） |

**归属**：`control-tower`（建议：`gen-cto-health` 或看板增加「CP 条件已满足但 verdict 未转正」独立列）。

## 六、归属与建议

- **归因: control-tower**（P2-2 的转正触发器缺口）。
- 无 P0/P1；交付物质量高（结构完整、file:line 锚定、诚实统计），PASS。
