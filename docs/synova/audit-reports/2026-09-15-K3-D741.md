# D741 K3 独立审计报告：真相源状态写入链路定位

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 B 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §B 组
> 交付物: `docs/synova/coordination/真相源写入链路缺口-20260913.md`（git 跟踪）· 复核方式: 8 条 evidence **逐条物理复验**

## 〇、结论：**CONDITIONAL PASS**（1 条时点过时 P1 + 1 条范围声明 P2）

核心定位结论 **7/8 独立复验成立**，交付物质量高。扣分为 evidence[4] 描述的看板过滤行为**已被后续 D748 修复改变**，交付物未标注「本结论有时效性」。

## 一、8 条 evidence 逐条复验（7 成立 / 1 时点过时）

| # | 声称 | 独立复验 | 判定 |
|---|---|---|---|
| 0 | `product-progress.json` 26/26 线**无 line.status 键**；写入端 `calc-progress.py:316-330` 无 status 字段 | `json.load` → `lines` 长度 26，`Counter({'None': 26})`；`sed -n '316,335p'` → `lines_out.append({...})` 内含 id/name/value/weight/baseline_pct/done_definition/total/verified/progress_pct/k3_gate/status_counts/points，**确无 line 级 status** | ✅ |
| 1 | 3 处消费端均不读 `line.status` | `dsh/plugins/synova-dashboards/lib/collect.js:107-115`（取 id/name/progress_pct/verified/total/weight/status_counts）· `scripts/.../derive-board-sources.py:283-291`（同上）· `scripts/product-lines/gen-progress-page.py:114-122`（读 progress_pct/verified/total/k3_gate）→ 三处**确无 line.status** | ✅ |
| 2 | 5 个 FAIL：D393/D503/D515/D549/D572 | `grep -l '"verdict": "FAIL"' task-state/*.json` → 精确命中该 5 个 | ✅ |
| 3 | 看板过滤点 `sync.js:40-48` 映射 + 不读 `raw.audit.verdict` | `sync.js:40-48` `DEFAULT_STATUS_MAPPING`（audited→done；failed 列仅 cancelled/failed 可入）**确如所述**（**但见 §二：该行为已被 D748 改变**） | ✅（时点） |
| 4 | 渲染侧 `ledger-v2.json` 5/5 无一进 failed 列 | 依赖 Host 本地账本 `~/.dsh/task-board/ledger-v2.json`——K3 独立工作区无此文件，**无法复验**；机制侧 `sync.js` 在 D741 时点确会把 audited+FAIL 映射为 done | ⚠️ 环境受限 |
| 5 | `gen-cto-health.py --dry-run` → exit 1 崩溃于 `:305` | `:303-307` 确为 `spec_path = (d.get("spec") or {}).get("path")`——若 `spec` 为字符串（如 D600.json）则 `.get` 属性错误 → 与声称的崩溃根因一致 | ✅ **真缺陷存活** |
| 6 | `:326-331` 子串判定 PASS 先于 FAIL → CTO-HEALTH.md:69 把 D393 FAIL 显示为 PASS | `:326-331` 实测顺序：先 `"CONDITIONAL PASS"` → 再 `"PASS"` → 再 `"FAIL"`——`elif "PASS" in txt` 会先命中含 PASS 的串 → **判定顺序缺陷成立** | ✅ **真缺陷存活** |
| 7 | 审计列唯一源 = `.codex/audit/audit-result.json`（代码扫描非任务裁决）；`DASHBOARD-CN.md` 陈旧；`.github/workflows/` 无 job 跑两个看板生成器 | `gen-task-board.py:341-348` `read_audit()` docstring 明写 `.codex/audit/audit-result.json` · `grep -rln "gen-task-board\|gen-progress-page\|gen-cto-health" .github/workflows/` → **零命中** | ✅ |

## 二、P1｜evidence[4] 描述的缺陷已被 D748 修复，交付物未标时效（M2 时点漂移）

D741 于 2026-09-14 交付（brief 生成日）。**同日** `09d8754c`（2026-09-14，#553）合入 **D748**：`dsh/plugins/task-board-adapter/lib/sync.js:171-181` 新增 `verdictBoardStatus()` / `resolveBoardStatus()`：

```js
// D748（看板真话）: audit.verdict 是**独立维度** —— audited + FAIL ≠ done。
const VERDICT_FAIL_PATTERNS = Object.freeze(["FAIL", "NOT-AUDITABLE", "NOT_AUDITABLE"]);
```

即：**D741 定位的「`audited` + FAIL 显示为已完成」这一具体缺陷，在交付当日（或次日）已被 D748 修复**。D748 的 commit body 甚至引用了与 D741 完全相同的证据口径（「5 个 FAIL 裁决任务在看板显示为「已完成」」）——**说明 D741 的定位本身就是 D748 立项的依据**。

- **判定**：D741 的**定位结论在其交付时点正确且有效**（且直接驱动了修复），不构成虚报。
- **但**：交付物与 task-state 均未标注「本结论的消费方式 = 派修复卡，修复后本条即失效」。审计方若直接采信，会重复修一个已修的缺陷。
- **建议**：交付物头部加一行时效声明；或在 D748 合入时于本节追加「已由 D748 闭环」批注。

## 三、P2｜D748 自身尚未被 K3 审计（下游缺口提示）

`task-state/D748.json` 当前 `status: claimed` / `audit: null` —— **修复 D741 的机制本身未经独立审计**。D748 是看板真话机制（影响「FAIL 是否可见」这个控制塔根性问题），属应审范围。建议 CTO 排入下一批派单。

## 四、写集核对

`impl.files` 声称 5 文件（交付物 + 台账 + board-backlog + task-state + brief）。任务为**只定位不改**（brief §Q2 声明「不新增机制、不改生成器（D735 爆炸半径 + 只定位纪律）」）——本审计确认：D741 未改任何生成器代码（§一 对 `sync.js`/`gen-cto-health.py`/`gen-task-board.py` 的读取均为现状读，非 D741 所改）。**范围纪律遵守**。

## 五、L4 防线缺口收割

**问题：D741 精准定位了三条真相源链路缺陷（且驱动了 D748 修复），但交付物自身很快过时——哪道防线该管「定位类交付物」的时效？**

| 防线 | 本该做什么 | 为什么没拦住 |
|---|---|---|
| 定位类交付物「只读不改」纪律 | 防止分析变成改动 | **有效**（D741 严守） |
| 台账登记 | 记录缺陷与归属 | 有登记（`审计发现台账-DSH-CTO.md` 在写集内） |
| 消费侧（CTO/看板） | 读到定位报告 → 派修复卡 | 生效（D748 即产物）——**但闭环后未回写原报告** |
| **缺的机制** | **定位报告与修复任务的互相引用**（修复卡 cited 定位报告；合并时回写定位报告「已闭环」） | 无此双向引用 → 报告永挂「开着」态，审计方需自行考古 git log 才能发现已修（**M4 证据链单向性**） |

**归属**：`control-tower`（建议：派单模板增 `closes_finding: <报告路径>` 字段；合并时自动追加闭环批注）。

## 六、归属与建议

- **归因: control-tower**（P1 的时效标注 / P2 的双向引用）。
- 定位结论本体成立且高价值（7/8 独立复验通过）；无 P0。
