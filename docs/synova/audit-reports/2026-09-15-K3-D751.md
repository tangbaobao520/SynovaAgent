# D751 K3 独立审计报告：「新增生效」端到端硬断言（三环）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #559
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #559 合入态） · 独立重跑

## 〇、结论：**PASS**（含 1 条 evidence 数字失实注记，见 §三）

## 一、正向：三环测试全绿 ✅

```
$ npx vitest run tests/sentinel/d751-new-sentinel-e2e.test.ts
  ✓ tests/sentinel/d751-new-sentinel-e2e.test.ts (5 tests) 76ms
  Test Files 1 passed (1) | Tests 5 passed (5)
```
（日志真实穿过三环：`[runner] 工单已创建`、loader 扫描、去重表降级显式 warn、通知适配器 degraded 显式——与「真实管线」声称一致，非 mock 自证。）

## 二、反向：删夹具 `manifest.expert` → 必红 ✅

删除 `tests/sentinel/fixtures/d751-sentinel-root/sentinel-e2e-fixture-01/manifest.json` 的 `expert` 键（原值 `fundamental-efficiency`）：

```
  × 环 2 — 路由: 聚合信号 recommendedExperts 含 manifest.expert（D750 权威通道）
  × 环 3 — 派发（全公开管线）: runOnce 真实 check → aggregateAndDispatch → runExpert + 报告可见
  FAIL 环 1 — 发现 / 环 2 — 路由 / 环 3 — 派发
  Tests  3 failed | 2 passed (5)
```
三环全断言生效（降级路径正确保持绿）。`git checkout --` 恢复后 `expert` 字段回归，零残留。

## 三、evidence 数字核对（1 处失实）

| impl.evidence 声称 | K3 实测 | 判定 |
|---|---|---|
| 「绿: 5 passed」 | 5 passed | ✅ |
| 「反向: 3 failed（环1/2/3）」 | 3 failed（环1/2/3 精确一致） | ✅ |
| 「全量 tests/sentinel **919 passed** \| 1 skipped」 | `npx vitest run tests/sentinel/` → **227 passed \| 1 skipped**（33 文件）；全仓 → 4422 passed \| 24 failed \| 24 skipped；D751 分支时点（`session/d751-new-sentinel-e2e`@3554810d）全仓 → 4413 passed；该分支 tests/sentinel 声明数 217 | ❌ **919 在四个口径下均不可复现**（「\| 1 skipped」在 sentinel 口径精确成立）。方向性结论（无回归）成立，但该数字与事实不符，如实记录 |

## 四、接线核对

- 注入缝 `SENTINELS_FIXTURE_DIR`（`src/sentinel/sentinel-loader.ts` `sentinelsDir()`）生产路径不变（未设 env 时仍扫生产目录），夹具不污染真实哨兵注册表。
- 夹具 `manifest.json` 带 `expert`/`layer` 双域防假绿设计（fundamental-efficiency × technology），与 D750 权威通道断言对齐。

## 五、verdict 依据

Done 标准（vitest 全绿 + 反向删 expert 必红）全部独立复现；evidence 第 4 条的「919」为数字失实（不影响 Done 判定，不构成 FAIL），已记入报告与 task-state findings。
