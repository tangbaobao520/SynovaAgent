# D752 K3 独立审计报告：哨兵类型网登记硬门禁（软→硬）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #560
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #560 合入态） · 独立重跑

## 〇、结论：**PASS**

## 一、独立重跑（4/4 成立）

### 1. 配对 bash 测试 9 ✅ ✅

```
$ bash tests/control-tower/check-sentinel-type-net.test.sh
  全部通过: 9 ✅ / 0 ❌   [exit 0]
（尾部两项实测: 目录缺失降级 exit 2 fail-closed + 降级原因显式输出）
```

### 2. vitest 集成 4 ✅ ✅

```
$ npx vitest run tests/sentinel/d752-type-net-gate.integration.test.ts
  ✓ (4 tests) 333ms   Tests 4 passed (4)
```

### 3. 门禁正向：45 活跃哨兵全登记 ✅

```
$ bash scripts/control-tower/check-sentinel-type-net.sh
  ✅ D752 类型网硬门禁: 45 个活跃哨兵全部已登记（src/sentinel/types.ts）  [exit 0]
```

### 4. 反向：删一条登记 → 门禁 exit 1 点名 ✅

删除 `src/sentinel/types.ts:291`（`cashRunwaySentinel as _cashRunwaySentinelCheck` 登记行）→ 同门禁：

```
  ❌ D752 类型网硬门禁: 以下活跃哨兵未在 src/sentinel/types.ts 登记:
    cash-runway
  修法: 在 types.ts 类型网区补 import type ... 禁止静默跳过。
  [TRUE-EXIT=1]（重跑去管道直取退出码核实）
```

`git checkout --` 恢复，登记行回归（grep 计数 1）。

## 二、接线与边界核对

- 门禁本体 `scripts/control-tower/check-sentinel-type-net.sh`（豁免口径与 `sentinel-loader` `_` 前缀同源；降级 exit 2 fail-closed，非 fail-open）。
- CI 接线路径：ci.yml 属红区不可改 → 经 vitest 集成测试挂进全量步骤（impl.evidence 声称口径）；本报告实测该集成测试在套件内真实通过。
- `_extinct` 12 处引用未动（存量另单），与声称一致。

## 三、备注（基线卫生）

独立重跑期间发现 `extensions/industries/{saas-tech,test-write}/thresholds.json` 仅 `aggregatedAt` 时间戳被测试副作用刷新（无实质变更），已恢复——非本任务写集。
