# D754 K3 独立审计报告：runner 注释去漂移（零实现改动）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #561
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #561 合入态） · 独立重跑
> 审计范围注记: impl 为 mac_half（win_half 显式 claimed_待派，派单 §四只记台账）——本报告只裁定 mac 半。

## 〇、结论：**PASS**（mac_half）

## 一、纯注释机械验证 ✅

```
$ git show 6447d101 --numstat -- src/sentinel/runner.ts
19  3   src/sentinel/runner.ts          ← +19/−3 精确匹配声称
$ git show 6447d101 -- src/sentinel/runner.ts | grep -E "^[+-]" | grep -v "^+++" | grep -v "^---" \
    | grep -cvE "^[+-]\s*(/\*\*|\*|/\*|//|$)"
0                                  ← 过滤注释符后增删行为零 → 0 逻辑改动
```

## 二、注释事实链独立对照（K3 自行对代码，5/5 成立）

| 注释声称 | 代码事实 | 判定 |
|---|---|---|
| 优先级 1: `sentinel.config.route.experts` 非空优先 | `runner.ts:80-82` 该分支存在且居首 | ✅ |
| 该分支「恒未命中——manifest 无 route（0/45）+ loader 不透传」 | 45 个 `extensions/sentinels/*/manifest.json` 逐一 json 解析查 `route` 键 → **0 个命中**；`grep -n route src/sentinel/sentinel-loader.ts` → **0 命中**（不透传） | ✅ |
| 优先级 2（实际生效）: `layer` 默认映射，fallback 旧 `category`；interface 层按哨兵 ID 子串细化 | `runner.ts:88` `const layer = config.layer \|\| config.category`；`LAYER_EXPERTS` 表；`if (layer === 'interface')` 内 value-capture/moat/network 等子串分支 | ✅ |
| 优先级 3: 层未命中 → `['host']` 兜底 | `runner.ts:110` `LAYER_EXPERTS[layer] || ['host']` | ✅ |
| 失效模式: 失效专家 ID 流到 `dispatchSignalsToExperts`，在 `VALID_EXPERTS` 处逐个 `continue` 静默跳过（无日志），全失效时零派发无告警 | `runner.ts:697` `VALID_EXPERTS = new Set(getExpertRegistry().listTypes())`；`runner.ts:731-733` `const expertType = VALID_EXPERTS.has(rec) ? rec : null; if (!expertType) continue;` —— `continue` 前无任何日志语句（唯一 `log.info` 在 continue 之后的合法路径） | ✅ |

## 三、回归

- `grep -c '声明路由' src/sentinel/runner.ts` → 0（impl 声称改后归零，属实）。
- runner 相关测试实跑：`tests/sentinel/sentinel-runner.test.ts` + `sentinel-runner-auto-ticket.test.ts` → 2 文件 **8 passed**。⚠️ impl.evidence 写「runner 相关 3 测试文件 12 passed」——实际仓内 runner 直连测试文件为 2 个/8 用例（另有 6 个文件间接 import runner，已含在 sentinel 全量 227 passed 内）。**数字口径不符但不改变无回归结论**（全量 `tests/sentinel/` 227 passed | 1 skipped）。

## 四、备注

- 旧注释「过滤成空路由」确实不实（VALID_EXPERTS 过滤发生在下游派发循环，不在路由函数内）——本次校准方向正确。
- win_half（manifest 是否显式声明 route 的 Win 侧台账）维持 claimed_待派，不构成本单 FAIL 项。
