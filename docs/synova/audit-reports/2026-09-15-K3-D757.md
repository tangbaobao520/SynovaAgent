# D757 K3 独立审计报告：哨兵类型欠账修复（3 目录 31 处类型错）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #562
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #562 合入态） · 独立重跑

## 〇、结论：**PASS**

## 一、Done 标准独立重跑（4/4 成立）

### 1. 3 目录 0 类型错 ✅

```
$ npx tsc --noEmit --pretty false | grep -E "cash-runway|revenue-health|key-person-risk" | grep -c "error TS"
0
```
（同跑全量残余 28 处，逐处核对全部在 CI 既有过滤名单文件内——`_extinct/` 25 + `src/server.ts` 2 + `src/connectors/ima.ts` 1；即 3 个修复目录净 0 错，与「残余 28 处全在 CI 既有过滤名单内」声称一致。）

### 2. 反向必红 ✅

把 `extensions/sentinels/cash-runway/computes/compute-cash-runway-months.ts` 两行导入（:17/:18）`sed` 改回 `../../../src/l4/graph-traversal`（旧深度）→ 同一编译图：

```
compute-cash-runway-months.ts(17,39): error TS2307: Cannot find module '../../../src/l4/graph-traversal'
compute-cash-runway-months.ts(18,37): error TS2307: ...(同上)
compute-cash-runway-months.ts(50,44)/(50,47)/(51,46)/(51,49)/(62,35)/(62,38)/(63,37)/(63,40): error TS7006 ×8
```
该文件直报 10 处（TS2307×2 + TS7006×8）+ 连带 `compute-receivable-overdue-rate.ts` TS7006×4 → 全量 42 处（净增 14），全部在 CI 可见范围（不在过滤名单）→ 门禁必红。`git checkout --` 恢复后 `git status --porcelain` 为空，被审基线零残留。

### 3. 运行时 4/4 LOADED ✅

```
$ npx tsx -e "import('./extensions/sentinels/<dir>/<entry>').then(m=>console.log('LOADED',Object.keys(m)))"
cash-runway       LOADED keys=cashRunwaySentinel
revenue-health    LOADED keys=revenueHealthSentinel
key-person-risk   LOADED keys=keyPersonRiskSentinel
path-dependency   LOADED keys=detectPathDependency,pathDependencySentinel   (entryPoint=./computes/detect.ts, 按 manifest)
```

### 4. finding 补齐 evidence ✅

```
$ grep -c "evidence:" extensions/sentinels/revenue-health/aggregate.ts → 5（≥4 达标）
$ grep -c "evidence:" extensions/sentinels/cash-runway/aggregate.ts   → 8
$ grep -c "evidence:" extensions/sentinels/key-person-risk/aggregate.ts → 3
```

## 二、修法真实性核对（PR #562 = c11fbb1b）

- `cash-runway/aggregate.ts`：`GraphStoreReader` 导入源从 `../../../src/sentinel/sentinel-loader`（不导出该类型）改为 `../../../src/l4/graph-traversal`（真实出处，同文件本就从这里取 `GraphTraversal`）——修法与 brief 决策一致，非绿洗（未动 CI 过滤名单）。
- 3 个 computes 文件相对深度 `../../../src` → `../../../../src`。
- 4 条 warning finding 补 `evidence` + `suggestion`（复用同哨兵已算出的真实数值，diff 逐行核对非占位串）。

## 三、备注

- 「31 处」（TS2305×2/TS2307×6/TS2345×4/TS7006×19）为修复前历史值，无法在已合入 main 的基线上原样复现（属正常）；本报告以「修复后 0 错 + 反向改回即红（复现同类 TS2307+TS7006）」等价闭环。
