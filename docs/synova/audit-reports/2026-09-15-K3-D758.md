# D758 K3 独立审计报告：证据目录域判定豁免（D734 假阳性）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #563
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #563 合入态） · 独立重跑

## 〇、结论：**PASS**

## 一、独立重跑（4/4 成立）

### 1. 正向：#538 形态 → ② 单域 win ✅

`git show b1618971 --name-only`（#538 全部 12 文件）注入：

```
$ bash scripts/control-tower/check-pr-budget.sh --files "<#538 12 文件>"
  ✅ ① 变更文件数 12 ≤ 上限 12
  ✅ ② 变更单域: ✅ PASS 7 个文件同域: win（无归属 0，域判定豁免 5）
✅ PASS PR 预算内（12 文件）      [exit 0]
```
豁免配置在 `docs/synova/coordination/ownership.yaml:157` `domain_neutral` 列表含 `"docs/synova/product-lines/evidence/**"`（#563 新增项）。

### 2. 反向：移除豁免 → 跨域必红 ✅

`sed` 移除该项（恢复 ownership.yaml:157 为无 evidence 项）→ 同命令：

```
  ❌ ② 变更跨域 —— 一个 PR 只许一个域（D733 ownership.yaml）
       win  app/css/setup.css ... win  tests/routes/setup-guide-retired.test.ts（7 项）
       mac  docs/synova/product-lines/evidence/D716-win-20260913/1-5-dual-guide-win-evidence.txt
❌ FAIL PR 超预算                  [exit 1]
```
唯一异域项正是 #538 自带的验收证据文件——D734 假阳性根因（证据目录被按路径前缀判成 mac 域）被反向实验直接证实。`git checkout --` 恢复后基线零残留。

### 3/4. 两套回归测试 ✅

```
$ bash tests/control-tower/check-ownership.test.sh   → ✅ 全部通过: 51 项 [exit 0]
$ bash tests/control-tower/check-pr-budget.test.sh   → ✅ 全部通过: 24 项 [exit 0]
```

## 二、写集核对

PR #563（5bb6dbdb）写集 = ownership.yaml + 两个测试脚本，与 impl.files 声称一致；豁免口径与同族先例（task-briefs/task-state/memory-notes 同在 domain_neutral）同构，非单独开口子。
