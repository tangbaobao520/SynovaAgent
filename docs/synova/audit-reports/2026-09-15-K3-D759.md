# D759 K3 独立审计报告：nodemailer 10 回退（主树 tsc 止血）

> 审计员: K3 独立会话（预设 synova-k3-audit，批次四 A 组） | 日期: 2026-09-15
> 派单: `docs/synova/coordination/审计派单-批次四-K3-20260915.md` §A 组 · PR #564
> 审计基线: `.synova-wt-d780` @ b1192d71（含 #564 合入态；package.json/package-lock.json 与本机 node_modules 所在主 checkout 逐字节一致，diff 实测为空）
> 审计环境: macOS arm64 · node v22.22.0 · 独立重跑，未采信转述输出

## 〇、结论：**PASS**

## 一、独立重跑（3/3 成立）

### 1. `npx tsc --noEmit` → TS2503 归零 ✅

全量实跑（`--pretty false`，非 CI 过滤口径）：

```
$ npx tsc --noEmit --pretty false | grep -c "TS2503"
0
$ npx tsc --noEmit --pretty false | grep -c "error TS"
28
```

28 处残余全部位于 CI 既有过滤名单（`.github/workflows/ci.yml:77`）覆盖的文件：
`extensions/sentinels/_extinct/**` 25 处 + `src/server.ts` 2 处 + `src/connectors/ima.ts` 1 处。
按 CI 同口径复跑（`grep -v "_extinct/\|ima.ts\|server.ts\|sentinel-service.ts\|industry-loader.ts\|tool-registration.ts\|llm-provider-loader.ts"`）→ **0 error**，主树 TS 门禁恢复绿。
原事故点 `src/services/email-service.ts:35`（`nodemailer.Transporter`）当前无任何报错——`@types/nodemailer` 8.1.1 提供该命名空间类型。

### 2. `grep '"nodemailer"' package.json` → ^9.1.1 ✅

```
$ grep -n '"nodemailer"' package.json
68:    "nodemailer": "^9.1.1",
```

### 3. 锁文件 9.1.1 ✅

```
$ python3 -c "import json;d=json.load(open('package-lock.json'));print(d['packages']['node_modules/nodemailer']['version'])"
9.1.1
```

## 二、写集核对

`git show 3fef816f --stat`（#564 squash 合入态）: `package.json`(2 行) + `package-lock.json`(10 行) + brief/bypass.log/task-state 文档。与 impl.files 声称（package.json/package-lock.json）一致，无夹带源码改动。

## 三、备注

- impl.evidence 4 条（事故/根因/修法/验证）与本报告独立重跑结果一致；「残余 28 处全在 CI 既有过滤名单内」逐处核实为真。
- 根因叙述（#549 的 CI 绿跑在旧基线、三连合后组合态暴露）不在本单复核范围（回退效果单）。
