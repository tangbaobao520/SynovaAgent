# 台账追加建议（批次五 K3 审计 · 2026-09-16）

> 供 CTO 追加到 `docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md`。
> 归属：全部落在既有 M 类内（M1/M2/M3/M5）——**按「一类一机制」原则不新增 M 类**，只在各 M 类下强化已有防线。
> 编号：沿用台账 §三 CT 队列既有序号（main 当前最大 = CT-73，批五建议 **CT-74…CT-79**）。
> 数字口径: cdeba8e9（origin/main @ 2026-09-16）｜审计报告: `docs/synova/audit-reports/2026-09-16-K3-批次五-门禁与度量.md`

---

## §一 审计发现台账（追加 7 行）

| 日期 | D# | 级别 | 发现 | 根因（该拦的防线） | 修复 | 改进归属 |
|------|----|:---:|------|------------------|------|:---:|
| 09-16 | D789/K3-B5-01 | **P0** | **D778 派单门禁的计划哈希真相源 = 提交者本地工作树**（`pre-dispatch-check.sh:68-76`）：一份锚定与 `origin/main` **完全正确**的派单文档，在基线落后的树上必判红（本机实测：树内哈希 `8d331729` vs main `4e46603f` → `❌ 复核未过 (exit 1)`；同一文档在 main 树上绿）。一旦合入，任何"分支落后于计划变更"的提交者都吃假红 | 病已被识别（编码指令 §一.3），但约束只写进**人**（skill 规则「必须在与 origin/main 同步的 worktree 里跑」），没写进**机器**（门禁代码）——信息注入型约束对 agent 不可见（V3.9 教训）；D778 验收网只测"夹具自洽"，不测"跨树一致性" | 门禁输入取权威 ref（`origin/main:<path>` 或 git blob SHA），本地工作树仅显式降级；测试补"落后基线树里正确锚定必绿"金丝雀（先红） | control-tower + devdoc |
| 09-16 | D789/K3-B5-02 | **P0** | **D778 门禁 glob 只覆盖 `派单-*.md`，真派单文档从门禁旁走过**（`check-dispatch-gate.sh:39`）：K3 派单命名 `审计派单-批次五-…md` 不匹配 → 门禁整段不执行（本机复现：输出只有 CT-34 纯文档早退，`RC=0`）。全仓量化：`派单-*` 20 份（被查）vs `审计派单-*` **11 份**（全绕过）。**PR #601 自身即实例**：其改动集命中门禁 glob 的文件数 = 0，job 级 `TypeScript + Lint + Iron Laws` = **success** | ①规格与实现一致，但两者都把「命名」当「语义」——`编码指令-D778-…§二.1` 原文就写 `派单-*.md`，创建一个新前缀就顺手关掉了门禁；②测试夹具清一色自造 `派单-*` 名，**从未用真实派单文档名做一次实弹**（自我实现的盲区） | 文档类别判定从文件名 glob 升级为路径+内容判据（如 `coordination/*派单*.md`，或含 `依据计划:` 行即视为派单文档）；测试加真实文件名金丝雀（`审计派单-*` / `评估派单-*` / `返修反馈-*`）。与 K3-B5-01 **共用同一条金丝雀机制** | devdoc + control-tower |
| 09-16 | D789/K3-B5-03 | P1 | **门禁测试的"假安全感"**：`check-dispatch-gate.test.sh:35-37` 夹具的 `依据计划:` 用 `shasum` 算**被测树自己**的哈希 → 对"哈希来源该取 origin/main 还是本地树"**结构性不敏感**，23/23 在任何树里都绿（本机在 PR #574 树上亦 23/23 全绿，同时 P0-1 在该树上真红）；且全部夹具名 `派单-*.md`（`:43/:52/:54/:150/:163`），唯一贴真实文档的 ⑭ 只断言 D# 告警集合，不看 ⑩ | 铁律 48「测试非空壳」只查断言存在性，**不查"断言是否对目标缺陷敏感"**；门禁的验收网缺"输入必须含生产形态真实样本"这一条 | 不变式：门禁测试输入必须包含 ≥1 个生产形态真实样本（真实文件名 / 真实哈希来源）。作为 pre-commit 组 2 的评审清单加一行（不新增脚本） | implement |
| 09-16 | D789/K3-B5-04 | P1 | **证据→判分口径断裂比派单描述更前一层**（M1 静默失效）：`calc-progress.py:383` 只扫 `docs/synova/product-lines/evidence` 顶层 `*.json`（`:83` 非递归），而当日 GS 证据落在 `scripts/golden-scenarios/evidence/`（**29 份**）；且 GS 的点 id 为 `S0-1/S1-1/L1-1`，而 `product-lines.yaml` 合法 id 全无 `S*` 前缀（`verdicts_by_point.setdefault(ap, [])` 任意字符串都收，落空时**无 warn、无 degraded 条目**）→ 新产出的场景证据对六态判分**零贡献且零告警**，与"线1 仍 stale"可以同时为真 | 契约（`calc-progress.py:18-19`）只覆盖"记录损坏"，不覆盖"记录合法但点 id 无人认领"；两个 id 空间（证据侧 `S*`/`L*`、判分侧 `N-M`）从未被机械比对过 | 先在装载处把"未匹配证据"显式登记进 `degraded.sources`（一处改动让断裂可见），**再**修口径（目录纳入扫描 + `S*→*` 显式映射，或反之），禁改判分凑绿 | control-tower（或 implement，CTO 定） |
| 09-16 | D789/K3-B5-05 | P2 | `tests/doc-system/doc-truth-probe.test.sh` 本机绿，但不在 CI canary 清单（`.github/workflows/ci.yml:259-265` 只列举 6 个 doc-system 测试）→ 该测试的上线保护缺 CI 侧 | canary 清单是**手工枚举**的，新增测试文件时无"必须登记"的机械关联（同 job 已调用 `check-canary-drift.sh` 但只告警不阻断，`:277`） | 把 canary 漂移从"告警"提到"新增 control-tower/doc-system 测试必须出现在清单否则阻断"。**零新增脚本**（复用既有脚本） | implement |
| 09-16 | D789/K3-B5-06 | P2 | **D593「21/21 实跑」在本审计环境不可复现**（`npx vitest run …` → `EPERM open …/.vite-temp/…`，产品仓库与审计 worktree 两处同因）→ 本审计对该项"不采信也不否定"；已给旁证：三个相关文件在 `e53b5984` 与 `origin/main` 的 blob 逐字相同（7fc3e820/20d6a83e/2c36b342） | M5 族（环境依赖）：vitest 需在 `node_modules` 下写 `.vite-temp`，会话沙箱只允许写工作区 | 给 vitest 加 `--cache-dir <$TMPDIR>` 包装（一行参数），使审计/CI 沙箱可跑；审计侧固定"环境注记"行 | audit（环境）+ control-tower |
| 09-16 | D789/K3-B5-07 | P2 | **预算门禁「适用范围」结论（D788 任务 1）无任何文档化产物**：`grep -n "适用范围\|docs-only\|D515\|审查面" scripts/control-tower/check-pr-budget.sh` 零命中；`grep -rn "含审查面\|审查面文件" docs/ scripts/` 零命中；PR #589 的 6 个改动文件亦无 | 派单把"先给结论不改代码"设为任务 1，但交付只做了"派单范围修订"（把任务 1 写进修订稿），**没有产出"结论 + 落点"本身**——规格写进文档 ≠ 结论被文档化（M7 影子） | 结论落 `check-pr-budget.sh` 头注释（一行即可）并写清「本门禁只在含审查面文件的 PR 上生效；纯治理/审计 PR 走 D515 跳过」 | devdoc + control-tower |

---

## §三 控制塔改进队列（CT，追加 6 条）

| CT | 问题 | 发现 | 状态 |
|----|------|------|------|
| CT-74 | **门禁比对基准必须取权威 ref**：凡门禁做"哈希/tag/ref 比对"，基准只允许 `origin/main`（或 git blob SHA），本地工作树仅作显式降级留痕。落地：D778 ⑩ 项改造 + 全 control-tower 门禁普查同类（`grep -rn "shasum\|sha256sum" scripts/control-tower/` 逐个人工判定） | 09-16 K3 批次五（K3-B5-01，实证 exit 1） | 🔧 控制塔（D778 修复卡 + 普查） |
| CT-75 | **门禁触发面不能用文件名 glob 表达语义**：`check-dispatch-gate.sh` 的 `派单-*.md` 让 11 份 `审计派单-*` 绕过。落地：改为路径+内容判据 + 真实文件名金丝雀；同时普查其他"按前缀认类别"的门禁 | 09-16 K3 批次五（K3-B5-02，PR #601 实弹） | 🔧 控制塔（D778 修复卡同批） |
| CT-76 | **门禁测试的输入必须含生产形态真实样本**：禁止夹具自造哈希/自造名字闭环（K3-B5-03 实证：23/23 全绿与 P0 真红共存）。落地：组 2 评审清单加一行 + 在已有门禁测试里补 2 条真实样本断言 | 09-16 K3 批次五 | 🔧 控制塔 + skill |
| CT-77 | **证据-判分 id 全域校验**：`calc-progress.py` 装载处对"无人认领的 acceptance_point"显式登记 `degraded.sources`；并一次性产出 id 空间对账表（证据侧实际 id vs yaml 合法 id），把口径断裂从"静默"变"点名" | 09-16 K3 批次五（K3-B5-04：GS 证据 29 份零贡献） | 🔧 控制塔 |
| CT-78 | **D593 实跑留档**：在可写环境补跑 `tests/routes/diagnosis-report-persistence.test.ts` + `tests/electron/right-panel-report-sentinel.test.ts` 并把原始输出落 `docs/synova/audit-reports/`，闭合 K3-B5-06（本审计已给旁证：三文件 blob 与 main 逐字相同） | 09-16 K3 批次五（环境 EPERM） | 🔧 控制塔（10 分钟级） |
| CT-79 | **canary 清单机械关联**：新增 `tests/control-tower/*` / `tests/doc-system/*` 测试文件必须出现在 `ci.yml` canary 清单（否则阻断），复用既有 `check-canary-drift.sh`，升格为阻断 | 09-16 K3 批次五（K3-B5-05：doc-truth-probe 未入清单） | 🔧 控制塔 |

---

## §四 模式映射（为何不新增 M 类）

| 本批发现 | 命中既有 M 类 | 对应已有防线（强化即可） |
|---|---|---|
| K3-B5-01（哈希真相源）、K3-B5-04（证据静默丢弃） | **M1** fail-open 静默失效 | 三态输出（pass/degraded/fail）+ 降级诚实自检 |
| K3-B5-03（假安全感测试）、K3-B5-07（结论未文档化） | **M2** 声称 vs 事实 | 汇报必须文件+行号；"拆完了"由 grep 物理证明 |
| K3-B5-02（glob 覆盖缺口）、K3-B5-05（canary 未登记） | **M3** 机制建成未接线（覆盖缺口变体） | 接线要求"生产调用点真实传递"；本批扩展为"触发面必须覆盖真实形态" |
| K3-B5-06（环境阻断实跑） | **M5** 环境依赖门禁 | PYBIN 全局探测；本批扩展为 vitest `--cache-dir` 参数化 |

> **收敛检查**：本轮 7 条发现全部落在既有 4 类内，**不新增 M 类**（M9 仍空缺）。防线增量 = 复用既有脚本 + 各 1 处参数/判据改造，**零新增门禁脚本**。
