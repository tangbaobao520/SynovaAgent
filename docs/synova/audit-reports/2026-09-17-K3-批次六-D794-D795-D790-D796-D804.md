# K3 批次六定向审计 — D794 / D795 / D790 / D796 / D804

> 审计员: Kimi K3（独立会话，零上下文）
> 日期: 2026-09-17 | 协议: AUDIT-PROTOCOL.md L1-L4
> 数字口径: origin/main@0f59e514（tree 8008e370）+ 五 PR head refs（#606=3e3b28a1 / #611=82e245aa / #612=5895fb99 / #613=f2842bfb / #618=7e545c57），五 PR 均未合并
> 环境: macOS arm64 / bash 3.2.57 / Python 3.9.6 / node 22.22.0 / vitest 未用（本批无 TS 改动）
> 材料缺陷披露: docs/synova/coordination/task-state/ 下五任务状态文件**均不存在**（实际落点为仓库根 `task-state/*.json`；五任务的登记各自活在各自分支，main（b396bafc）上均 absent，D794 仅存在于两个派单 doc 分支——逐 ref 列证见 K3-B6-01）——状态机位置口径与协议约定不一致，K3 写回只能以"建议稿"交付（见文末）。
> 版本: v1.1（2026-09-17 订正 3 处，见文末「订正记录」——CTO 复核指出 K3-B6-01 全称断言过宽 + 归因口径不统一）

## 结论速览

| 任务 | PR | verdict | P0 | P1 | P2 |
|---|---|---|---|---|---|
| D794 插件（项目总览面板） | #613 | **CONDITIONAL PASS** | 0 | 1 | 2 |
| D795 账本（gen-project-board） | #612 | **CONDITIONAL PASS** | 0 | 1 | 1 |
| D790 度量口径（A1 时间戳） | #606 | **PASS** | 0 | 0 | 1 |
| D796 交付纪律三闸 | #611 | **PASS** | 0 | 0 | 2 |
| D804 派单（DSH 标准模板） | #618 | **CONDITIONAL PASS** | 0 | 1 | 2 |

无 P0。全部 5 项为独立实跑核验（非采信自报），含 7 次反向破坏复现。

---

## 一、D794 插件（PR #613）— CONDITIONAL PASS

### 定向点核验（4/4 过）

**① sidebar.panellist 与 main 成对注册 — PASS**
- `dsh/plugins/synova-dashboards/lib/client.js:634-643`：先注册 `main` keyed cell（:635-638，`{ name:"main", key:PANEL_ID }`），**再**注册 `sidebar.panellist` 入口行（:640-643，`{ name:"sidebar.panellist", id:PANEL_ID, order:50, label:"项目总览" }`），两处同用 `PANEL_ID = "synova-project-overview"`（:620）。注册顺序符合官方协议（:616-619 注释：未注册 main key 点击会抛错 ⇒ 必须先 main）。
- 测试断言真实且强：`test/client-panel.test.js:47-73` 断言①两者都注册②id/key 相同③**main 先于入口行**（:60-63）④不再注册 shell.overlay（:65-69）⑤全插件只允许这两个槽位（:71-73 `deepEqual(names, ["main","sidebar.panellist"])`）。

**② 广播降级（降级信号传播到 UI，铁律 24/31）— PASS**
- 审计口径说明：插件全目录 grep `broadcast|广播` 零命中——派单术语"广播降级"映射为"降级信号显式传播到前端"，依此判定。
- 四态显式呈现（`lib/client.js:121-125` 契约声明）：①网络/HTTP 失败 → `⚠ 降级：Failed to fetch`（:133-137 测试覆盖）②路由级 ok:false → degraded 横幅（:115-119 测试覆盖）③账本级 degraded:true → 警告条 + degraded_sources 列表（:144-149 测试覆盖）④健康路由独立降级文案（:305-311），互不拖垮（:432 注释"两条只读 GET 各自独立降级"）。
- Host 半同样显式：`lib/index.js:57-68`（ledger !ok → logger.warn + `{ok:false, degraded:true, error, attempts, path}`）；:41-49（data 路由 catch → warn + degraded，200 不 500）。

**③ 零写入 — PASS（三重证据）**
- grep 证据：`grep -rn "writeFile|appendFile|mkdirSync|rmSync|unlinkSync|createWriteStream|POST|PUT|DELETE" lib/` → **零命中（exit 1）**。
- 契约声明：`lib/index.js:15` `@write 零写入：只 readFile/execFile 只读 git（D794 红线）`；`lib/ledger.js` 头部 `@write 零写入`。
- 测试物理断言：`test 31 - 只读性：读取前后账本 mtime/内容不变（红线：插件零写入）`。
- 安装脚本只写 `$DSH_HOME`（`scripts/install-dashboards.sh:7,19-22`：DEST/PKG_JSON/PRESET_FILE 全在 DSH_HOME 下），仓库零写入。

**④ 重启必要性 — PASS**
- `install-dashboards.sh:13`「生效：重启 dsh web → 刷新浏览器」；:120-124 打印三步生效步骤并警告 restart 脚本会 kill 3080 端口进程；`scripts/restart-dsh-web.sh` 在场。与 DSH 事实一致（Host 半是 dsh web 进程内 Cordis 插件；client-plugin 热更仅在 dev:web watcher 运行时成立）。诚实、无"装完即生效"的虚假承诺。

### 独立实跑
- `node --test test/*.test.js` → **`# tests 31 / # pass 31 / # fail 0`**（与 brief Done"pass 31 / fail 0"一致，1.0s）。
- 反向①（破坏 `.spo-body>*{flex:none}` + `.spo-sec{flex:none`）→ `not ok 16 - 修裁切/重叠…`，pass 15 / **fail 1**（红）✓ 后已还原。
- 反向②（`synova.pm.panel.v1` → `.BROKEN`）→ `not ok 14 - 尺寸/位置记忆…`，pass 15 / **fail 1**（红）✓ 后已还原。
- brief:71 自报"实测各 1 红"与独立复现一致。

### Findings
- **P1 K3-B6-01（归因: control-tower，M7 登记/状态机断链）〔v1.1 措辞收窄〕**：`task-state/D794.json` 在**五个被审 PR head 与 main 上 absent；在 `docs/dispatch-d794-d795-20260917`、`docs/dispatch-batch7-20260917` 两个派单文档分支上存在**（status=claimed，dispatch 侧登记）。逐 ref 列证（`git cat-file -e <ref>:task-state/D794.json`）：

  | ref | 结果 |
  |---|---|
  | origin/pr/606 / pr/611 / pr/612 / pr/618 | absent ×4 |
  | origin/pr/613（D794 自身实现 PR） | **absent** |
  | origin/main（审计时 0f59e514 与订正时 b396bafc 双查） | **absent** |
  | origin/docs/dispatch-d794-d795-20260917 | PRESENT（claimed） |
  | origin/docs/dispatch-batch7-20260917 | PRESENT（claimed） |

  **统一判据（"task-state 不在本 PR 树"形态，D734/D796/D794 同一标准，v1.1 立）**：登记在案 = 满足以下任一臂——(a) task-state 已进入 main；或 (b) task-state 已随**该任务自身实现 PR** 提交。满足任一臂时，它在其它树上不可见属**环境作用域**（跨 PR 可见性差异，非缺口）；**两臂皆不满足 = 实质登记缺口，定 P1**。
  - D794：两臂皆不满足（doc 分支既非实现 PR #613 也非 main）→ **P1 维持**。后果：#613 合并时登记不随行，状态机对 D794 失明（K3 无法回填 verdict；看板适配器无法跟踪；正是 D796 要拦的"完工落库"前形态）。与批五 F1（task-state 登记环节缺失，第三次实证）同源第四次。修复：将 doc 分支上的登记并入实现 PR #613（impl 段含 PR 引用，恰好过 D796 C2 闸），或随 #613 合并带入 main。
  - D796：(b) 臂满足（`task-state/D796.json` PRESENT 于实现 PR #611）→ 在 #618 树/pre-dispatch-check 中不可见属环境作用域（见 D804 节）。
  - D734：所核查 6 ref（pr/611、pr/613、pr/618、main、两个 doc 分支）均 absent；D734 不在本批被审集（仅以"模式先例"身份被派单 :105 引用，非完工声称），其 (b) 臂无法在本批验证（无在审 D734 PR）→ **不定级，登记为待对账项移交 CT-80**。此处刻意不用"任何 ref 均不存在"的全称断言——订正①的教训即在于未穷举 refs 不得下全称结论。
- **P2 K3-B6-02（归因: implement，M2 写集漂移-轻）**：brief 写集表（brief:74-89"机器生成，禁手改"）含 `README.md`（brief:79），PR 实际 12 文件无 README；偏离由 chore 提交 `1e3bce29`（"README 移出本 PR 过 D734 预算（13→12 文件）"）显式留痕，但 brief 写集段未同步刷新——机器生成块与事实分叉。
- **P2 K3-B6-03（归因: implement）**：`cordis.patch.yml:16` 硬编码机器特定绝对路径 `repoRoot: /Users/wane/SynovaAgent`。缓解在场（install:35-46 安装时改写副本 + 注释声明 profile 可覆盖 + 默认回退 cwd），影响有限，但入库默认值非本机不可用。

---

## 二、D795 账本（PR #612）— CONDITIONAL PASS

### 定向点核验（3/3 过）

**① 125 分母解析 — PASS（三层独立复数）**
- 独立复数断言行：`grep -cE '^\| *[0-9]+-[0-9]+ *\|' docs/synova/project/26线-V1验收标准-草案v0.1-20260917.md` → **125**；证据类型分布独立统计 `scenario 72 / k3 31 / test 15 / founder-demo 7`（和=125），与 brief Q0.b 声称逐字一致。
- 产物核验：`ledger.json` `totals.v1_total == 125`、`len(lines) == 26`。
- 治理属性：`gen-project-board.py:428-430` 分母冻结（`FROZEN_V1_TOTAL`），漂移只告警不覆盖实测——冻结是治理属性不是隐藏数字，口径正确。

**② 反向删证据回退 — PASS（独立夹具，非套件复跑）**
- 自建夹具（真实输入拷贝至 /tmp）：run1 `v1_total 125 / passed 21`，FLIP-TARGET 断言 `1-1` 证据 = `scenario-2026-09-13.json`；删除该证据文件后 run2：**`1-1 ok = False | evidence = []`，passed 21→20**。非硬编码、删证据真实回退。套件组②（test.sh:219-228）另有同型断言（ok true→false / v1_passed 3→2 / green 1→0）。
- 套件整体独立实跑：`bash tests/project/gen-project-board.test.sh` → **PASS=61 FAIL=0 exit 0**（六组：正常/反向/降级/保鲜边界 7🟢/8🟡/14🟡/15🔴 且 15 天仍计交付度/幂等/真实仓库冒烟 v1_total=125+backlog 39+未留临时文件）。

**③ calc-progress.py 零提交 — PASS（全分支口径）**
- `git log --name-only --pretty=format: 0f59e514..origin/pr/612 | sort -u | grep -E 'calc-progress|productline_yaml|product-lines.yaml'` → **零命中**。判分器/解析器/只读输入全线未动，红线（brief Q2"不改 calc-progress.py"）成立。

### Findings
- **P1 K3-B6-04（归因: implement，M2+M5 产物字段环境依赖）**：`ledger.json` 的 `timeline[].actual.first_commit` **跨机不可复现**。提交版记录线 1 = `2026-06-03`（git_head=662965f3）；同一工作树重生成（仅 generated_at/git_head 除外逐键对比）→ `2026-08-01`，唯一差异键 = timeline。物理根因：**662965f3 的祖先图是双根历史**（`git rev-list --max-parents=0 662965f3` → `431e16b8`(2026-08-01) + `4589ffa9`(2026-06-13)），仓库原始根 `a6160381`(2026-06-03) **不在该祖先内**（`git merge-base --is-ancestor a6160381 662965f3` → NOT ancestor；它只活在旧 feature refs 上）。即：任何按 GitHub 对象图的新 clone 跑 `git_first_commit`（gen:391-407，`git log --format=%as -- <modules>`）都得到 2026-08-01，产物里的 2026-06-03 只在开发者本机旧史视角下成立。task-state known_open 虽披露"timeline 区分度≈0（25/26 线同为 2026-06-03）"，但把它当成"仓库初始导入日"的事实陈述——该归因在仓库真实图中不成立，且生成器对此**无告警**（成功即写入，非 None 降级）。影响面有限（timeline 不进交付度/保鲜口径、区分度≈0），但"机器可验、幂等"契约对 timeline 键在跨机场景失效。修复建议：timeline 字段加 provenance 标注（clone-dependent）或改由冻结数据源派生；known_open 修正"2026-06-03=本机旧史视角"表述。
- **P2 K3-B6-05（归因: devdoc/流程，已披露）**：`tests/project/gen-project-board.test.sh` 未入 CI canary（known_open 明示：ci.yml 不在派单文件集、正被 D782 在途改动 → 另立派单）。61 断言目前只在提交者本机跑过——本审计已独立复跑全绿，风险已缓解，但入列前 CI 不兜底。

---

## 三、D790 度量口径（PR #606）— PASS

### 定向点核验（不变量 + 基线，全过）

**① 不变量：TTL=14 / 六态 / 权重 — PASS（逐字对照）**
- TTL：main `calc-progress.py:67` `EVIDENCE_TTL_DAYS = 14` → PR `:71` 同值（仅行号位移）；`> EVIDENCE_TTL_DAYS` 判过界语义未动。
- 六态：main `:69` → PR `:73` `SIX_STATES = ("uncommitted","failed","pending_k3","verified","rejected","stale")` **逐字相同**。
- 权重/判分：`git diff ... | grep -iE '^[+-].*(weight|权重|score.*=)'` → 零命中；diff 落点全部限于新鲜度时间来源判定（`evidence_freshness_ts`/`git_last_commit_time`/`ingest_ts_plausible`/`parse_iso_ts`）+ `now` 注入缝 + 降级登记去重。
- 兑换/rerun/yaml 未触：PR name-only = calc-progress.py + evidence-writer.py + 测试×2 + Note + task-state + brief + bypass.log（8 文件与 brief 写集一致）。

**② 零新增红基线对照 — PASS（同机同夹具双跑）**
- `product-lines.test.py`：main 基线（6a06e853 worktree）`Ran 32 / FAILED (failures=2)`，失败集 = `{test_range_expansion_and_mapping, test_real_repo_capital_line_zero_of_eight}`；PR 分支 `Ran 41 / FAILED (failures=2)`，**失败集逐字相同** → D790 新增 9 用例全绿、零新增红。
- `calc-k3-stale.test.py`：main `Ran 15 / failures=1`（`test_a7_pairing_boundary_change`）→ PR `Ran 16 / failures=1`（同名）→ 零新增红。
- 环境注记（D316 纪律）：`test_real_repo_capital_line_zero_of_eight`（期望 sum(verified)=5）在审计机 main/PR **双双红**（实得 0）——与 D795 P1 同根（审计 clone 的 re-rooted 历史使"证据日期后 modules 有变更"判定更严）。**判"环境依赖存量红"，非 D790 引入**；dev 机"测试全绿"声称在其环境成立，增量全绿在两环境一致。

**③ 靶心独立复现（同日两时点六态一致）— PASS**
- 自写驱动（绕开套件，直接调 `compute(..., now=...)`）：`now=2026-09-17 06:00` vs `22:00` 两次 → **164 点逐点六态全等 True**；六态分布 `{pending_k3:42, stale:22, uncommitted:96, rejected:2, failed:2}`；`degraded.problems` 4 条显式登记（缺时间戳回退日期粒度均留痕，铁律 24/31 落实——`evidence_freshness_ts` 非 "at" 一律 append problems，PR:193-218）。

**④ 出产侧 at 落盘 — PASS**：`evidence-writer.py` 增 `now_iso()`（本地 ISO 8601 带偏移秒粒度）+ `valid_at()` + `--at` 非法值 `sys.exit(2)` fail-closed（diff 实见）。修复了"31/46 记录缺时间戳 → 只接 at 的修复无效"的存量实证（brief Q0.b）。

### Findings
- **P2 K3-B6-06（归因: implement/test，M5）**：`test_real_repo_capital_line_zero_of_eight` 对 git 历史形态敏感（审计机恒红、dev 机绿）——建议该用例改用注入 git 夹具或显式标注 env-scope，否则"全绿"声称跨机不可复现（本卡 Done 声称即属环境作用域内为真）。存量问题，非本卡引入。

---

## 四、D796 交付纪律三闸（PR #611）— PASS

### C1/C2/C3 真拦核验（含独立反向，非套件复跑）

套件先跑：`bash tests/control-tower/check-delivery-discipline.test.sh` → **14 通过 / 0 失败 exit 0**（含反向红用例）。随后**自建沙箱**（mktemp + git init + 拷贝引擎脚本，非套件夹具）独立复现：

| 探测 | 构造 | 结果 |
|---|---|---|
| 反向① C1 | brief=D999，分支=feat/d888-demo，staged src/a.ts | `❌ C1: 任务号不一致…（D320 劫持风险）` → **DISCIPLINE-FAIL, RC=1** ✓ |
| 反向② C2 | task-state/D888.json `status:impl_done` + `impl:""` | `❌ C2: D888 标完工但 impl 为空/无 PR·branch 引用` + `❌ C2: 缺 brief 或 brief 无机器写集块` → **RC=1** ✓ |
| 反向③ C3 | staged 派单-D888-demo.md（四件套全缺） | `❌ C3: 缺交付纪律四件套: ①worktree ②brief ③写集机器生成 ④完工判据=PR` → **RC=1** ✓ |
| 探测④ C1 盲区 | brief=D999，分支=feat/quick-fix（**无任务号**），staged src/a.ts | `✅ C1: 分支归属一致（D999）` → **DISCIPLINE-OK, RC=0** ✗（见 P2） |

**CI 硬阻断链坐实**：`pre-commit-check.sh:1483-1495` 接线在场（`grep -c check-delivery-discipline` ≥1，Done 第三项过）；违规走 `soft_check` → `:111-113` `SYNO_CI=1` 时 `HARD_FAIL++`（:128-132，ci.yml 注入 SYNO_CI:"1"）——本地软提示 + CI 权威的 V5.1.1 语义正确落地；exit 2 降级也走 soft_check（CI 阻断、本地留痕），不与通过混同。
**两处高危 bug 已被作者自证反向修复**（ctrl-tower-change 模式兑现）：任务号大小写不敏感（`check-delivery-discipline.sh:58-60`，注释"漏掉它 = D794 那类…完全不报警（本卡反向测试亲证）"）；写集块解析不停在空行（:142-146，"用 /^$/ 截止会解析出 0 条路径 = 该检查永不生效；本卡反向测试亲证"）。

### Findings
- **P2 K3-B6-07（归因: control-tower，M1 覆盖缺口）**：C1 对**无任务号分支**放行（探测④实证：feat/quick-fix + 有号 brief → 绿，且文案"分支归属一致（D999）"具有误导性——分支并无 D999 标记）。C1 仅在"分支号与 brief 号都存在且不等"时拦截；执行方在任意未按规范命名的分支上改代码不被拦。建议：分支无号 + 变更含代码路径时至少降级提示"分支未带任务号"。
- **P2 K3-B6-08（归因: devdoc，M7）**：`task-state/D796.json` `status: "claimed"`——实现（脚本+测试+接线）已随 PR 交付，状态未回填 impl_done；C2 闸因非完工态跳过自身（自指空洞：三闸卡自己的完工落库未被自己的闸检验）。
- C3 四件套关键词判定较宽（`brief`/`PR` 子串即可命中）——地板而非天花板，如实记 P2 观察不计编号。

---

## 五、D804 派单（PR #618）— CONDITIONAL PASS（只审派单质量，不审未开始实现）

### 八节模板与冻结门可执行性核验

**① 八节模板（§一 S1-S8）— PASS**：S1 价值入口/S2 契约（@input/@output/@degraded/@error/@idempotent，三态 0/1/2）/S3 数据流图（每步失败分支）/S4 失败模式表（**≥6 条**含量化下限+用户可见信号禁静默）/S5 切片（**≥4 片**，每片文件+断言+回滚）/S6 断言表（verify 命令/场景 ID/证据类型/fail_when）/S7 幂等与可观测/S8 回滚方案 + 复核门（CTO 复核冻结、改口径附变更说明）。八节齐、量化、可执行；汇报格式五件套（§一.五）与 CTO 验收四招（§五：抽查复跑 5 条逐字对上/切片完整性/反向必红/K3 独立审计）闭环。
**② 断言引用真实性 — PASS**：派单引用的 14 个断言 ID（1-1/1-2/1-3/1-4/1-8/6-1/6-2/6-3/10-1/10-2/10-3/10-4/10-6/10-8）在 V1 规格表中**逐一存在**（14/14 ✓）；线级数"线1=5 / 线6=4 / 线10=6"独立复数全对。
**③ 依据计划锚定 — PASS（内容寻址，可复现）**：`shasum -a 256 整体推进计划-主线-20260913.md | cut -c1-8` = **4e46603f**，与派单锚 `v1.2@4e46603f` 一致；PR 树与 main 树中该文件 blob 同（8078f15b），内容未漂移。注：该锚为 **shasum 内容校验和**（pre-dispatch-check.sh:55 `PH=$(shasum -a 256 …)`），非 git blob/树哈希——K3 首轮误用 git 哈希比对得出"锚定失效"，实测撤回；此口径跨机可复现（前提：文件内容一致）。
**④ 冻结门 — 部分**：§四.2"规格冻结门：dev doc 经 CTO 复核前不得提交 src/**（违反=PR 退回）"当前为**流程性**执法；物理门两处**诚实挂账**（§六：C4 闸待 #611 合并后追加；pre-commit 附加块排入 D796 下一版）。窗口期内靠 CTO review 拦截。
**⑤ task-state 登记 — PASS**：D804/D803 均已登记（claimed），且派单自身过 D796 C3 四件套（worktree/brief/declare-write-set/PR 全在场）。

### Findings
- **P1 K3-B6-09（归因: devdoc，M2 声称 vs 事实）**：brief Done 第一项声称 `pre-dispatch-check.sh … → 机械项全通过`，**独立实跑不成立**：同树同文档 `bash scripts/control-tower/pre-dispatch-check.sh 派单-D804-D803-DSH标准-20260917.md` → **exit 1**，`❌ 机械项发现问题`。其中 ⑨ 为**内容确定性失败项**：派单缺「派单内部一致性」自检段（`grep -c 内部一致性` 该文档 = 0），而该检查项自 2026-09-14 d47c3824 起就在检查器中（早于 09-17 派单书写）——非环境差异可解释。另有 ⚠️ D734/D796 无 task-state——按 K3-B6-01 统一判据（v1.1）：D796 (b) 臂满足（登记在实现 PR #611 上），此 ⚠️ 属**环境作用域**假警（跨 PR 可见性）；D734 所核查 6 ref 均 absent，不定级、移交 CT-80 对账。D804 派单以"模板从本单起强制"自我立法，其自身交付声称却未过自家机械门——正是该模板 §一.五"缺项即退回"要拦的形态。修复：补派单内部一致性自检节，重跑 pre-dispatch-check 留原始输出。
- **P2 K3-B6-10（归因: control-tower，M3 窗口期）**：冻结门在 C4/pre-commit 物理闸落地前（依赖 #611 合并 + D796 下一版）无机器执法，靠"PR 退回"人工把关；建议 C4 追加时把"派单锚定哈希语义（shasum 内容校验和）"一并写入脚本注释，防后续 K3/执行方再误读。

---

## L3 执行证据汇总（bypass + CI）

**bypass.log 全量对账（五 PR diff 内 .claude/bypass.log 追加共 10 条）**：
- #606×1、#611×1、#612×3、#618×2：全部为 `COMMITTED | pre-commit PASS (hook 层登记)` 正常提交标记，无 BLOCKED、无 detected-bypass。
- #613×3：`03:16:14 BLOCKED | pre-commit FAIL (exit 1) | TASK_ID=D794`（**门禁真实拦截一次后修正**）+ 2 条正常 COMMITTED。
- 结论：**无 --no-verify 绕行证据**；"chore: bypass COMMITTED 登记 (auto hook, D521)" 提交为 auto-hook 登记载体，非绕过（V4.5.1 Gatekeeper 口径）。
- **CI job 级结论不可达**：审计工作区无 GitHub API 凭据（gh/token 均无），五 PR 的 CI 结论**未核验，标记 NOT-VERIFIED**，不以 run 绿假定为 job 绿（D320 教训）。批五同款限制。

## L4 防线缺口收割（本批该拦未拦的，为什么）

1. **D794 无 task-state（K3-B6-01）**：D796 C2 闸只拦"staged 了完工态 task-state 但没落库"，对"压根不登记 task-state"的形态不触发（无文件=无检查对象）；--scan 模式能抓游离代码，但没人跑。缺：task-state 存在性门禁（对 brief 中的 D# 反查 task-state/D#.json 在场）——批五已列 CT 候选，本批第四次实证，建议升级为 P0 级 CT 项。
2. **D795 timeline 环境依赖（K3-B6-04）**：产物幂等测试只在同机跑两次，天然测不出跨 clone 漂移；生成器对"git 历史视角"这一输入无 provenance 概念。缺：派生产物对环境敏感输入（git log）的字段级标注或冻结化。
3. **D804 声称不实（K3-B6-09）**：pre-dispatch-check 已在仓且确定性红，但派单 Done 声称"全通过"无人复跑——汇报方=执行方同体（CTO 自派自报），缺独立复跑环节；pre-dispatch-check 未接入 CI（只在人工流程）。归因 control-tower：建议把 pre-dispatch-check 对 staged 派单文档的运行接入 PR CI（SYNO_CI 软硬均可），声称与实跑的缝即可机器弥合。
4. **D796 C1 无号分支盲区（K3-B6-07）**：门禁判定条件把"缺任务号"当"无法判定→放行"（:108-109 仅当 BRIEF_D 也缺才降级提示）——fail-open 家族（M1）再现于新闸。作者已用反向测试防住"大小写"和"空行解析"两个洞，第三个洞（无号分支）未被反向用例覆盖——**反向用例集本身需要覆盖矩阵化**（每个判定分支至少一条红路径），而非逐事故补。

## M 类映射（不新增 M 类）

| Finding | M 类 | 归因 |
|---|---|---|
| K3-B6-01 D794 task-state 缺失 | M7（登记漂移）+ M1 边缘 | control-tower |
| K3-B6-02 README 写集漂移 | M2 | implement |
| K3-B6-03 机器路径入库 | M2（轻） | implement |
| K3-B6-04 timeline 不可复现 | M2+M5 | implement |
| K3-B6-05 canary 未入列 | M3（已披露） | devdoc/流程 |
| K3-B6-06 真仓测试历史敏感 | M5 | implement/test |
| K3-B6-07 C1 无号放行 | M1（fail-open） | control-tower |
| K3-B6-08 D796 状态未回填 | M7 | devdoc |
| K3-B6-09 Done 声称不实 | M2 | devdoc |
| K3-B6-10 冻结门窗口期 | M3（挂账） | control-tower |

**CT 建议队列**：CT-80 task-state 存在性门禁（升级批五 F1 候选；K3-B6-01 第四次实证）；CT-81 派生产物环境敏感字段 provenance 规范（K3-B6-04）；CT-82 pre-dispatch-check 接入 PR CI + 锚定哈希语义注释（K3-B6-09/10）；CT-83 反向用例覆盖矩阵（每判定分支≥1 红，K3-B6-07）；CT-84 真仓集成测试注入化或 env-scope 标注（K3-B6-06）。

## 北星三问（批级）

① 服务真实用户场景：五卡全部锚定创始人/管理台真实诉求（D790 修"创始人看的数字随跑动时点跳变"；D795/D794 是创始人唯一管理可视化入口的数据与渲染；D796 源于创始人三次点名同类复发；D804 直指 M1 能装能用+M3 第一条 100% 线）——过。② 接近终态：D804 显式推进产品终态（装得上/开得开/首诊）；其余为度量与治理基建向终态收敛，无横向无关物——过。③ Synova 仍是"驻扎企业、主动诊断、为增长服务"的 Agent：五卡均未触碰产品运行时形态与定位（D794 为 L1 可视化插件；D790/D795/D796/D804 在 scripts/docs 域）——过。**批级裁决：对齐北星，无跑偏。**

## 遗留与环境注记

- 审计机与 dev 机 git 历史形态差异（re-rooted 双根 vs 原根）影响三类断言：D795 timeline、D790 真仓集成测试、pre-dispatch-check 的 D### task-state 可见性——全部按 D316 纪律区分"环境作用域"后定论，未误判为恒失败。
- 反向破坏均在隔离 worktree 内进行并已 `git checkout --` 还原；未修改任何 PR 分支内容；未触碰 scripts/audit/**。
- 批五落地：分支 `audit/k3-20260916-batch5-landing` 已推 origin（本机无 gh/token，PR 由创始人经 https://github.com/tangbaobao520/SynovaAgent/pull/new/audit/k3-20260916-batch5-landing 开立合并）。

## 订正记录（v1.1，2026-09-17，CTO 复核驱动）

| # | 原文问题 | 订正 | 证据 |
|---|---|---|---|
| ① | K3-B6-01 原文"`task-state/D794.json` 在任何 ref（五个 PR + main）…均不存在"——**全称断言过宽**：审计时未拉取派单 doc 分支即下了"任何 ref"结论 | 收窄为"五个被审 PR head 与 main 上 absent；在 docs/dispatch-d794-d795-20260917、docs/dispatch-batch7-20260917 两个文档分支上存在（status=claimed）"，附逐 ref `git cat-file -e` 列证表；**P1 定级维持**（两臂判据皆不满足，未落自身 impl PR/main 是实质缺口） | 本文件 K3-B6-01 表格（8 ref 实测） |
| ② | 同一形态（task-state 不在本 PR 树）双标：D734/D796 记"环境作用域"、D794 记 P1，未给判据 | 立**两臂统一判据**（(a) 已入 main 或 (b) 已随自身实现 PR 提交 = 登记在案；皆不满足 = P1），D734/D796/D794 三例同判据重裁：D796 环境作用域、D794 P1 维持、D734 不定级移交 CT-80 | K3-B6-01 判据块 + D804 节收窄句 |
| ③ | 头部"D790/D795/D796/D803/D804 都有根目录 task-state/*.json，唯独 D794 无登记"——隐含"其它五卡登记在案"的绝对表述 | 改为"五任务的登记各自活在各自分支（D790@#606 / D795@#612 / D796@#611 / D803+D804@#618），main（b396bafc，~230 卡）上均 absent"——同口径 | `git ls-tree origin/main task-state/` + 逐卡 `cat-file -e` 复核 |

**归因: audit（改审计预设）**——订正①是 K3 自己犯了 M2 类错误（声称 vs 事实：未穷举 refs 的全称否定）。防再犯：全称否定声称（"任何 ref/任何位置均无"）出具前必须先 `git ls-remote --heads origin` 穷举远端分支并逐 ref `cat-file -e`，只对**已核查 ref 集合**下限定性结论。此条并入 claim-verifier 预设强化，不新增 M 类、不加新脚本。
订正后重推 `audit/k3-20260917-batch6-landing`（PR #621 自动更新），CTO 复核后合并。
