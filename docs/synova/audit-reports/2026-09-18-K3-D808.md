# K3 审计报告 — D808 DSH 借鉴标准更新（治理文档件 + 登记件）

> 审计员: K3（独立会话，零上下文，工作区 `/Users/wane/Synova-k3独立审计`）
> 日期: 2026-09-18
> 审计基线: **origin/main @ `715a7daa`**（PR #631 合入后 HEAD；fetch 于本报告开工时执行，`50d8a3d9..715a7daa`）
> 被审对象（以 git 事实为准）:
> - PR #627 → `101e6ad2`（分支 `docs/d808-dsh-borrow-standard`，合并写集 5 文件）
> - PR #628 → `1322fcf6`（分支 `docs/d808-ledger-mac`，合并写集 2 文件）
> - PR #629 → `6448982f`（新建 `编码指令-D808后续-20260917.md`）
> - PR #631 → `715a7daa`（指令包行数锚点 336→338 修正）
> 合入顺序（git parent 链）: `50d8a3d9` → #628(`1322fcf6`) → #629(`6448982f`) → #627(`101e6ad2`) → #631(`715a7daa`)
> 红线遵守: 未改任何被审文件（主件/指令包/台账/backlog 判读全部走 `git show origin/main:` 与只读 worktree `.wt-d808-audit`）；未写审计标准；本报告由 K3 独立产出。

---

## 结论

**CONDITIONAL PASS**（0×P0 / 1×P1 / 4×P2；两条已知观察均判定为**实锤缺陷**，均属控制塔工具域、非 D808 交付物本身缺陷）

| 层 | 项 | 结论 |
|---|---|---|
| L1 | A1 三节原址更新 | **PASS** |
| L1 | A2 三条元断言逐字 | **NOT-AUDITABLE**（指定基线"指令包 §四"不存在；裁定原文未落库，byte 级比对外部基线缺失）+ 内部一致性 PASS |
| L1 | A3 附录 C | **PASS**（10 行 ✓；逐行有映射+判据 ✓；核验命令 stub 实跑 2/1 ✓）附 1×P2（第 10 行映射列非 M-id） |
| L2 | B1 卡零绑定证据 | **PASS**（精确复现：仅 README.md；41 个 pass 点，21-3/20-3 缺席） |
| L2 | B2 落点零归属 | **PASS**（grep exit 1 零命中；`providers/` 陈旧写法在 yaml L875/L917 坐实） |
| L2 | B3 pathspec 空转 | **PASS**（`-- providers/` 零输出；`-- src/providers/` 命中 D586/D598） |
| L2 | B4 旧锚点存量 | **PASS**（恰好 4 文件，与清单逐一吻合） |
| L2 | B5 现行版本+现验 demo | **PASS**（0.1.6-alpha.1；152 行；L22 符号逐字命中） |
| L3 | C1 代码域零改动 | **PASS**（4 PR 并集 8 文件，无 src/**、scripts/**、scripts/audit/**） |
| L3 | C2 判据源零改动 | **PASS**（yaml 双端 1168 行一致；v0.1 md5 双端一致） |
| L3 | C3 无自我审计 | **PASS**（origin/main 上无 D808 审计报告；本报告为 K3 首出） |
| L4 | D1 §11.3 已落代码复现 | **PASS**（7 个 D# 提交逐一在落点复现；9 个落点文件全部存在于 main） |
| L4 | D2 「回写 0/20」反证 | **反证失败 → 声称成立**（扩样 grep 卡号+能力名零命中；README 命中为新鲜度备注非绑定记录） |

---

## L1 事实核验

### A1 — 三节原址更新：**PASS**

**命令与原始输出**（主件 = `docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md`）：

```
$ git diff 101e6ad2^ 101e6ad2 --stat -- "docs/synova/research/DSH迁移施工图-20260820/"
 .../DSH借鉴指引-v2-20260904.md                     | 212 ++++++++++++++++++++
 1 file changed, 206 insertions(+), 6 deletions(-)

$ for h in 101e6ad2 1322fcf6 6448982f 715a7daa; do git diff --name-status ${h}^ $h -- "docs/synova/research/"; done | sort -u
M	docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md
```

- research 目录在全部 4 个 PR 中**只有该文件 M**，未新建目录、未新建并列文档（`git ls-tree` 同目录无新增文件）。
- 行数：旧版 138 行（`git show 101e6ad2^:<主件> | wc -l` = 138，旧版仅 §0-§9）→ 新版 338 行（`wc -l` = 338）。与「138 → 338 行」声称一致。
- 三节落位（新版 file:line）：`## 10.` L146（M3）/ `## 11.` L205（M2）/ `## 12.` L260（M1）/ `## 附录 C` L299；散点改动（L6-7 头部约束更新、L17 术语澄清、L42-47 §3 验收前缀、L112-116 §6 守卫、L128 §7 表行 7、L143-144 §9 声明 5/6）均为治理口径同步，未篡改 §3/§4 借鉴卡历史内容。
- 配套：簿记 3 件（`.claude/task-briefs/D808.md` A、`memory/notes/implemented/2026-09-17-D808-dsh-borrow-standard.md` A、`task-state/D808.json` A）+ hook 自写 `.claude/bypass.log` M = 合并写集 5 文件，与「win 域 5 文件」一致。

### A2 — 三条元断言逐字：**NOT-AUDITABLE（外部基线缺失）+ 内部一致性 PASS**

**事实 1：A2 指定的比对基线不存在。** 审计指令（指令包 L24）要求「与本文 §四 原文比对 → 期望 byte 级相同」，但指令包 `docs/synova/coordination/编码指令-D808后续-20260917.md` 全文只有两节：

```
$ git show 6448982f:<指令包> | grep -n "^#\|^## "
1:# D808 后续指令包（K3 审计 + 4 张派卡）· 2026-09-17
9:## 一、K3 审计指令（复制给 🔍 synova-k3-audit session）
49:## 二、四张派卡（复制给对应编码/dev-doc session 的开头）
```

PR #631（`715a7daa`）对该文件的完整 diff 仅改一处行数锚点（336→338，`git diff 6448982f 715a7daa -- <指令包>` 实测），**未补 §四**。

**事实 2：裁定原文未在仓库任何处独立落库。** 全 origin/main `git grep`（特征串「该点必须写成」「未回写、或无绑定证据者」「桥接引用 = 禁止」）仅命中主件自身（§引文块 + 附录 C 内嵌）与簿记件的**转述**（memory note / board-backlog note），不存在可作第三 方比对基线的独立原文文件。主件 L143 自述「逐字取自创始人 2026-09-17 裁定」、L332 自述「三个引文块与创始人裁定原文 byte 级逐字相同（提取代码块比对 M1 ✅ / M2 ✅ / M3 ✅）」——该比对的两侧都在实现者手里，**属自证循环，K3 无法以 git 事实复现**。

**K3 可复现的部分（内部一致性）：PASS。** 三个 ```text 块位于主件 L150-155（M3）/L209-213（M2）/L264-270（M1）；附录 C 核验命令 heredoc 内嵌句（L325/L326/L327）与引文块内容行（L152/L211/L266）：

```
$ diff <(sed -n '152p;211p;266p' "$GUIDE") <(sed -n '325p;326p;327p' "$GUIDE") && echo BYTE-IDENTICAL
BYTE-IDENTICAL ✓（§10/§11/§12 引文内容行 = 附录 C heredoc 行）
```

**判定**：A2 按 K3 审计标准**不可审**（NOT-AUDITABLE）——不是发现"不逐字"，而是"逐字"的比对对象在 git 之外且未随 PR 落库。登记为 **P1**（见发现清单）。主件 L143/L332 对「v0.2 落库后补跑」的递延是诚实声明，不构成 overclaim；但"byte 级一致 vs 创始人原文"在补基线落库前应视为**未证声称**。

### A3 — 附录 C：**PASS**（附 1×P2）

- **行数**：表体 10 数据行（L306-L315），与「10 行」一致（另有表头 L304-305 与说明行 L301-302）。
- **逐行映射+判据**：第 1-9 行每行均给出「V1 元断言 id ↔ 本文条款（引文位置）+ 一致性判据」（file:line：L306-L314；判据列如 L306「与 V1 v0.2 的 M1 段逐字相同」、L309「语义等价：回写 + 绑定证据 + 未回写不计通过」）。**例外**：第 10 行（L315）映射列为「口径作废声明」，非 M-id——按表头「与 V1 v0.2 元断言的一致性核对表」的口径属 schema 偏差 → **P2-2**（内容本身有意义：作废声明以 M3 为准）。
- **核验命令真实可跑（stub V02 实跑）**——原样执行附录 C L322-328 循环，`V02` 指向 stub（每句一次）：

```
抄代码 = 允许且鼓… → 本指引 2 / V1v0.2 1（期望 2 / 1）
每张 DSH 借鉴卡（B… → 本指引 2 / V1v0.2 1（期望 2 / 1）
凡某验收点的能力… → 本指引 2 / V1v0.2 1（期望 2 / 1）
```

三句全部命中期望 2/1（"本指引 2"= 引文块 1 次 + 核验命令自身 1 次，与附录 C L321 注释声明一致）。命令真实可跑。
- 对照真实现存文件：`26线-V1验收标准-草案v0.1-20260917.md` 三句各命中 **0**（v0.1 尚不含三条元断言；v0.2 未落库——仓库 `docs/synova/project/` 仅有草案 v0.1）。主件 L332「未跑前不得声称已对齐 v0.2」的递延声明与事实相符。
- 附带核验：§11.3 建议验收点 18 个 id（21-1/21-3/21-4/22-1/20-1/20-3/26-1/26-2/8-2/8-6/16-1/16-4/17-1/23-1/23-2/24-3/25-2/7-3）在 `product-lines.yaml` 逐一 grep = 1 命中，全部真实存在。

---

## L2 物理断言复核（命令原文 + 原始输出；取证树 = origin/main 只读 worktree）

### B1 — 卡零绑定证据：**PASS**

```
$ cd .wt-d808-audit && grep -rln "借鉴\|B-0\|D587\|D593\|D598\|D599" docs/synova/product-lines/evidence/
docs/synova/product-lines/evidence/README.md            ← 仅此 1 文件
```

41 个 pass 点复核（遍历 evidence 全部 JSON 的 verdicts[].acceptance_point，verdict=pass 去重）：

```
distinct pass 点数 = 41
21-3 in set: False | 20-3 in set: False
含 21-3/20-3 字样的 pass 点: []
```

→ 「仅 README.md」与「41 个 pass 点不含 21-3 / 20-3」**双双精确复现**（README.md 唯一命中在 L42，是 D587 提交触碰线 2 modules 的新鲜度备注，非卡绑定记录，见 D2）。

### B2 — 落点零归属：**PASS**

```
$ grep -n "src/llm\|src/store\|src/config\|src/services" docs/synova/product-lines/product-lines.yaml
（零输出）exit=1   ← 四目录零归属坐实
$ grep -n "providers/" docs/synova/product-lines/product-lines.yaml
875:    modules: ["src/agent/", "src/l2/", "src/infra/", "providers/"]     ← 线20「Agent 运行底座」
917:    modules: ["providers/", "src/infra/", "src/l2/"]                    ← 线21「模型底座」
```

### B3 — pathspec 空转：**PASS**

```
$ git log --oneline --name-only -- providers/
（零输出）                                    ← 目录不存在，A1 失效检查静默空转
$ git log --oneline --name-only -- src/providers/ | head
4c74faf0 feat(d598): token 四桶计量 + 成本护栏——DSH 借鉴卡 B-03 (#427)   ← D598 ✓
56c4dd1d feat(D586): LLM 稳定错误码 taxonomy 收敛对齐——DSH 借鉴卡 B-01 (#400) ← D586 ✓
```

### B4 — 旧锚点存量：**PASS**

```
$ grep -rln "deepseek-harness/packages\|D:/deepseek-harness" src/ --include="*.ts"
src/llm/timeout.ts            (L4  锚点: D:/deepseek-harness/packages/util/timeout/... 0.1.1-rc.2)
src/llm/tool-result-pruner.ts (L8  D:/deepseek-harness/packages/compaction/compaction-tool-result-pruner/... )
src/agent/context-compaction.ts (L5 D:/deepseek-harness/packages/compaction/compaction-basic/... )
src/store/session-projection.ts (L5 D:/deepseek-harness/packages/session/session-projection/... )
```

恰好 4 文件，与主件 §10.4 表（L192-200）及 memory note 清单逐一吻合。

### B5 — 现行安装版本 + 现验 demo：**PASS**（本机 Mac DSH 实装）

```
$ <DSH>/package.json → "name": "deepseek-harness-pkg", "version": "0.1.6-alpha.1"
$ <DSH>/node_modules/@deepseek-ai/dsh-llm/package.json → 0.1.6-alpha.1
$ wc -l <DSH>/node_modules/@deepseek-ai/dsh-llm/lib/types/error.js
     152 ...
$ grep -n 'CONTEXT_WINDOW_EXCEEDED_CODE' <DSH>/node_modules/@deepseek-ai/dsh-llm/lib/types/error.js
22:export const CONTEXT_WINDOW_EXCEEDED_CODE = 'CONTEXT_WINDOW_EXCEEDED';
```

主件 L188 自检声称「文件 ✓ 152 行 + 符号 ✓ 22:export const …」——**行数与行号逐字命中**。（注：K3 审计克隆与 `~/SynovaAgent` 均无此包；包在本机 DSH 桌面依赖目录，与主件 L190「目录 mtime 实测」口径一致。）

### 附：§11.2 台账读数复现

```
ledger.json: totals.v1_total=125 / v1_passed=21 / delivery_pct=16.8（generated_at 2026-09-17T02:33:58+08:00）
product-progress.json: 线20 pct=0 (0/6)、线21 pct=0 (0/5)、线22 pct=0 (0/6)（generated_at 2026-09-16 19:00:59）
```

与 §11.2（L222-232）及 memory note 声称一致（memory note 记 ledger 时点 01:26，现文件 02:33 再生成但三值未变，属良性再生成）。

---

## L3 红线

### C1 — 代码域零改动：**PASS**

```
$ { for h in 101e6ad2 1322fcf6 6448982f 715a7daa; do git diff --name-only ${h}^ $h; done; } | sort -u
.claude/bypass.log
.claude/task-briefs/D808.md
docs/synova/coordination/board-backlog.json
docs/synova/coordination/审计发现台账-DSH-CTO.md
docs/synova/coordination/编码指令-D808后续-20260917.md
docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md
memory/notes/implemented/2026-09-17-D808-dsh-borrow-standard.md
task-state/D808.json

$ … | grep -E "^src/|^scripts/|^security/"   → 零命中（exit 1）
```

`scripts/audit/**`、`src/**`、`scripts/**` 零改动。`.claude/bypass.log` 为 post-commit hook 自写台账（各 PR 各 1 行），非手工写集。

### C2 — 判据源零改动：**PASS**

```
$ git diff --name-only 50d8a3d9 715a7daa -- "docs/synova/product-lines/product-lines.yaml" "docs/synova/project/26线-V1验收标准-*"
（零输出）
$ 双端存在性+内容：product-lines.yaml 两端均 1168 行；
  26线-V1验收标准-草案v0.1-20260917.md 两端 md5 = f5a230ad936db832495302dc6b44258d（一致）
```

空 diff 已排除"pathspec 拼错假零"（双端 ls-tree 均存在）。

### C3 — 无自我审计：**PASS**

origin/main 的 `docs/synova/audit-reports/` 无任何 D808 报告（`git ls-tree -r origin/main -- docs/synova/audit-reports/ | grep -i d808` = 空）；`task-state/D808.json` 的 `audit` 字段为 `null`（待本报告回填）。本报告由 K3 独立产出。
⚠️ 观察项（非 D808 违规）：历史上批次五/六 K3 报告由 `synova-dsh` 账号代为提交（commit `8fb2b56d`/`9104292d`）——代提交通道目前无"内容确系 K3 产出"的物证绑定，列入 L4 收割建议。

---

## L4 反证

### D1 — §11.3「已落代码」逐卡复现：**PASS（全部可复现）**

按主件 L256 自定义判据「`git log -- <落点>` 有该卡提交」逐卡实跑（worktree）：

```
D586 ✓ 56c4dd1d feat(D586): LLM 稳定错误码 taxonomy 收敛对齐——DSH 借鉴卡 B-01 (#400)   @ src/providers/base.ts
D593 ✓ 14e54722 feat(D593): LLM 调用韧性层——DSH 借鉴卡 B-02+B-06                        @ src/llm/retry-middleware.ts（B-02）/ src/llm/timeout.ts（B-06）
D598 ✓ 4c74faf0 feat(d598): token 四桶计量 + 成本护栏——DSH 借鉴卡 B-03 (#427)          @ src/llm/token-meter.ts + src/providers/deepseek.ts
D587 ✓ 75f13b56 feat(D587): 工具结果修剪器——DSH 借鉴卡 B-04                             @ src/llm/tool-result-pruner.ts
D594 ✓ f509b3cf feat(D594): 上下文压缩引擎四件套——DSH 借鉴卡 B-05                       @ src/agent/context-compaction.ts
D588 ✓ eebd9c99 feat(D588): 会话投影注册表——DSH 借鉴卡 B-07                             @ src/store/session-projection.ts
D599 ✓ 95a71a8f feat(d599): 客户配置包机制蓝本——DSH 借鉴卡 B-09+B-10                    @ src/config/config-layers.ts + customer-config-package.ts
```

9 个落点文件在 origin/main 全部存在（`[ -f ]` 逐一 ✓）。**计数澄清**：合入版指令包 L40 写「7 张卡（7 个 D#）」；§11.3 表标 ✓ 的是 **8 行 / 9 张卡**（B-09+B-10 共行）——本次 K3 收到的会话指令写「8 张卡」，按红线以 git 事实为准：7 个提交 / 8 行 ✓ / 9 张卡，三种口径实指同一事实集。

### D2 — 「B-01…B-20 回写 0/20」反证尝试：**反证失败，声称成立**

扩样反证（超出 B1 模式：全部卡号 B-01…B-20 + 能力名）：

```
$ grep -rln "B-01\|B-02\|…\|B-20\|token 四桶\|会话投影\|错误码" docs/synova/product-lines/evidence/
（零输出）exit=1
```

evidence 目录零命中 → 20 张卡无任何绑定记录；结合 B1（41 个 pass 点不含卡直接建议点 21-3/20-3），「0/20 回写」（主件 L258）**不可反证**。按 M2 口径「今天没有任何一张借鉴卡通过」成立。

---

## 已知观察判定（两项均成立）

### ① D202「0 问题却判交叉对比不一致」——**判定：比较器假矛盾（实锤），另附 2 处审计器缺陷**

D202 对 D808 的实际输出（`/private/tmp/d808-wt/.codex/audit-reports/D808.md`，2026-09-18T01:12 生成）：

```
| P0 (严重) | 0 |  | P1 (警告) | 0 |  | P2 (建议) | 0 |
| 交叉对比不一致 | 1 |
| 接线检查 | 声称通过 | 发现 0 个 P0 接线问题 | ❌ 矛盾 |
```

根因（`scripts/control-tower/external-auditor.sh` L244-252，d808-wt 现场版本）：wiring 维度是**关键词在场检查**——brief 含「接线」（`D808.md` L46「④ 接线：文档被消费方引用…」）且**不含**「调用方/caller/grep 确认」字样（grep -c = 0）→ 判 `wiring:fail`。**「发现 0 个问题」与「声称通过」本是一致，被判为矛盾**；且本卡为纯文档 diff，接线维度根本不适用。附带缺陷：(a) `D808.json` 的 `dims` 字段为非法 JSON（`{ wiring:fail, testing:pass, ... }` 键值均无引号）；(b) `findings[]` 含一条全空占位记录。**处置建议**：0 findings 时禁止判矛盾；docs-only diff 跳过代码维度；修复 JSON 序列化。属控制塔工具域（外部审计器为基线扫描，非结论权威），不阻断 D808 交付。

### ② pre-push 黄金案例「环境失败报质量退化」——**判定：门禁二值化缺陷（实锤，同型第 2 次可信）**

`scripts/pre-push-check.sh` L276-280（origin/main）：

```bash
if ! npx tsx scripts/ci/golden-case-checker.ts; then
  echo "  ❌ 黄金案例 F1 门禁失败 — 诊断质量退化解冻, 见上方 diff"   # ← 环境失败也走此分支
  exit 1
fi
```

`golden-case-checker.ts` 退出码仅 0/1（L424/L434/L437），无「未执行」三态 → 任何非零（含 npm EPERM）都报「质量退化」。触发前提（`~/.npm/_cacache` root-owned 文件）**现已清零**（`find … -user root | wc -l` = 0，2026-09-17 的 EPERM 现场无法活体复现，实据为台账 L318 登记 + 卡 4 现状材料）——代码层缺陷独立成立，与是否复现无关。处置即已派 `PLAN-prepush-golden-tri-state`（backlog 在册，三态退出码 0/1/2），方向正确。注：「D718 §⑤ 先例」原文未能在 git 跟踪文档中定位（D718 仅有台账补记 commit `ea7dfdd1`），引用出处建议补链，但同型缺陷判定不受影响。

---

## 发现清单

| 级别 | id | 发现 | 证据 | 处置 |
|---|---|---|---|---|
| P0 | — | 无 | — | — |
| **P1** | K3-D808-01 | **A2 比对基线缺失**：指令包引用的「§四」不存在（全文仅 §一/§二，#629 与 #631 两版均如此）；创始人 9-17 裁定原文未随 PR 落库 → 「M1/M2/M3 byte 级逐字」在 git 内只能自证（主件 L143/L332、task-state impl.verify 同口径），第三方不可复现 | 本报告 A2 节 | 把裁定原文落为独立 git 文件（或在指令包补 §四），V1 v0.2 落库时按附录 C 补跑三句 2/1 对表；此前「byte 级一致」一律按未证声称对待 |
| P2 | K3-D808-02 | 台账行数锚点未同步：`审计发现台账-DSH-CTO.md` L318 仍写「138→336 行」，实际 338；#631 只修了指令包未修台账（同源数据两处不同步，恰违反主件自己「同一句话只允许一处原文」原则的登记面） | 本报告 + `git show 1322fcf6` diff | 随下批 mac 登记件顺手改 1 字符 |
| P2 | K3-D808-03 | 附录 C 第 10 行映射列「口径作废声明」非元断言 id，偏离表头 schema（其余 9 行合规） | 主件 L315 | 下次触改附录 C 时补「↺ M3（作废依据）」样式标注 |
| P2 | K3-D808-04 | 主件 §10.3 引用出处 `docs/synova/coordination/DSH锚点重锚审计-20260917.md`（D797）**在 origin/main 不存在**，仅存于未合分支 `docs/dsh-reanchor-20260917`（commit `17f9e6f8`） | `git cat-file -e origin/main:<该文件>` 失败 | D797 分支合入前，主件引用处加「分支待合」注 |
| P2 | K3-D808-05 | 观察①/②两项控制塔缺陷（D202 假矛盾 + 非法 JSON；pre-push 二值门禁）——非 D808 交付物缺陷，均已给根因行号 | 本报告观察①② | ① 另立控制塔卡（修复 external-auditor.sh L244-252 + JSON 序列化）；② 归 `PLAN-prepush-golden-tri-state` |

---

## L4 防线缺口收割（控制塔「待补」建议）

1. **D202 交叉比较器**（观察①）：0-findings 短路 + docs-only diff 跳过代码维度 + JSON schema 校验。本次拦住它的防线：无——该工具输出无人复核即可写着「矛盾」。
2. **pre-push 三态**（观察②）：`PLAN-prepush-golden-tri-state` 已派，需补「环境失败 → exit 2 → 文案=未执行+修复指引」与配对测试。
3. **引用完整性**：K3-D808-04（引用未合分支文件）与既有 P2（引用不存在的 D797 实体）同型——建议 doc 门禁加「文中 `docs/` 引用目标必须在本分支可达」的软检查（pre-commit 组 1 类，警告级）。
4. **同一事实多处登记**：K3-D808-02（336 残留）说明「行数锚点」这类易变事实不应在多处硬编码——登记件宜写「主件以 git wc -l 为准」而非数字。
5. **K3 报告代提交通道**（C3 观察）：`synova-dsh` 账号代提交 K3 报告时，建议在 commit message 附 K3 报告文件的内容 hash，形成「K3 产出 → 代提交」的物证链。

## 运行环境注记

- 取证机：Mac（darwin），K3 独立工作区；git 取证走 `origin/main@715a7daa` 只读 worktree `.wt-d808-audit`（工作树本体在旧分支 `audit/k3-20260916-d788-precheck`，两者内容不一致，**全部结论以 worktree/git show 为准**）。
- B5 现验在本机 DSH 桌面依赖目录执行（`~/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/dependencies/dsh/`）——K3 克隆与 `~/SynovaAgent` 均未安装该包；如他机复现需先装 0.1.6-alpha.1。
- 观察② 的 EPERM 触发现场已消失（root-owned 文件 0 个），该项判定基于代码层缺陷 + 台账登记，非活体复现。

## 交付回填

- 本报告：`docs/synova/audit-reports/2026-09-18-K3-D808.md`（K3 域，git 跟踪）
- `task-state/D808.json`：`audit` 字段回填 `{verdict: "CONDITIONAL PASS", report: 本报告路径, at: 2026-09-18, by: "k3", note: 摘要}`；其余字段未动。
