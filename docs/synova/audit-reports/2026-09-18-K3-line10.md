# K3 独立审计报告 — D815 线10 资本循环 V1 六点全链路复核

- 审计员: Kimi K3（独立会话，零上下文）
- 日期: 2026-09-20（文件名按派单 write_set 定为 2026-09-18）
- 审计树: `origin/main` `a04af01e`（独立 worktree `wt-d815`，git fetch 后建）
- 规格: `docs/synova/coordination/派单-批九-20260918.md` §A/D815
- 结论: **PASS —— 线10 V1 六点全 PASS，gen-project-board.py 重算 `v1_passed=6 / v1_total=6`，verified=6**
- K3 证据: `docs/synova/product-lines/evidence/k3-line10-2026-09-18.json`（kind: k3，含点级 6 verdicts + `line:10` 线级 pass）

## 一、V1 范围裁定（先于逐点）

账本 V1 口径（`26线-V1验收标准` → ledger `v1_total=6`）= **10-1 / 10-2 / 10-3 / 10-4 / 10-6 / 10-8**。派单标题「10-1~10-6」与此不一致，以账本 + 任务书（k3-task-line-10-2026-W37.md L54「V1 六条断言」）为准。**10-5 / 10-7 显式标注「V1 外」**（见 §三）。

## 二、逐点判定（每点附命令 + 原始输出）

### 10-1 ERP 上传 → Financial 节点 — ✅ PASS

- 生产接线（非测试引用）：
  - `src/routes/data.ts:16` `router.post('/api/data/upload')` → `:32 loadFieldMapping(mapping)` → `:37-40` 从 server.ts 注入的 GraphStore 写入 L4。映射文件 `extensions/ontology/field-mappings/erp-standard.json`（`现金余额→cash` 等 14 条，targetNodeType=Financial）。
- 实跑（本审计独立执行，含负向相位）：
  - 命令: `bash scripts/golden-scenarios/GS-03-capital-cycle/run.sh`
  - 输出（节选）: `[GS-03] 注入响应: {"ok":true,"nodeType":"Financial","nodesCreated":1,...}`；`[assert] GS-03: 9/9 断言通过 → verdict=pass`，exit 0。
  - `financial-node-created` 断言为 **sqlite 物理行查询**（`SELECT COUNT(*) ... type='Financial' AND json_extract(props,'$.cash')=30000` ≥1）——只认 DB 行，不认 HTTP 自述（K3 P0-3 假绿教训已吸收）。✅
- 证据: `scripts/golden-scenarios/evidence/GS-03-2026-09-20.json`（本审计产出，9/9 pass）。

### 10-2 属性契约对齐（cashBalance↔cash snake/camel） — ✅ PASS

- 实跑: `node_modules/.bin/vitest run tests/contract/l4-contract.test.ts` → `✓ tests/contract/l4-contract.test.ts (13 tests)`，5 files / **43 tests 全绿**（含读侧消费契约 describe 4 断言 + 现金链挪用 runtime 断言）。
- 生产接线: 读侧 `src/sentinel/adapters/cash-flow-sentinel.ts:95` `Number(p.cash_balance) || Number(p.cash) || Number(p.现金)`——生产适配器真实消费 snake/camel/中文三形态。✅

### 10-3 哨兵 manifest 挂载 + 阈值真实触发 — ✅ PASS

- 生产接线: `src/sentinel/sentinel-loader.ts:207-213`（`if ('manifest' in sentinelObj) sentinelObj.manifest = manifest`——P0-1 修复 + 守卫注释）；触发入口 `src/routes/sentinel.ts:64` `POST /api/sentinel/run/:id`。
- 阈值来自 manifest（非硬编码）: `extensions/sentinels/cash-runway/manifest.json` `cash_runway_months: {warning:12, critical:6}`。
- **变异验证（本审计执行）**: `sed 's/"critical": 6/"critical": 9/' manifest.json` → 重跑 GS-03 →
  `[assert] GS-03: 8/9 断言通过 → verdict=fail`，`✗ threshold-provenance (fail): contains "低于critical阈值6个月" 未命中`，断言 exit 1。变异已 `git checkout --` 还原，重跑恢复 9/9 pass。**断言真的会红，阈值真的来自 manifest。** ✅

### 10-4 现金流跑道计算正确（filter bug 修复） — ✅ PASS

- 实跑: `node_modules/.bin/vitest run tests/sentinels/cash-runway/` → `compute-cash-runway-months.test.ts (18 tests)` 等全绿。
- 代码: `extensions/sentinels/cash-runway/computes/compute-cash-runway-months.ts:69 pickPresentNumber`（存在性守卫原语）；`:101-120` 现金链 `cash_balance → cash`，`total_revenue` 挪用已删除（JSDoc L21-26 记载断裂②同构修复）。
- 生产消费: 同目录 `compute-receivable-overdue-rate.ts:28-29` 等 import 复用；哨兵经 manifest/adapter 挂载进 runner。✅
- GS-03 正相位实测: `cash_critical … 「现金跑道0.3个月，低于critical阈值6个月」 evidence:["总现金: 30000","月消耗: 120000"]`——30000/120000≈0.25→0.3 个月，数学正确。✅

### 10-6 无数据不误报 critical（降级诚实） — ✅ PASS

GS-03 自带两个负向相位，本审计实跑原始输出：

- 空库（正常边界）: `cr_runway_degraded warning「现金流数据不完整」`，**无 critical**。✅
- 有收入无现金（失败输入）: 注入 `[{"营业收入":500000,"期间":"2026-Q1"}]` → `cr_runway_degraded warning「现金流数据不完整 — 现金字段缺失」`，**无「现金流危急」**。✅（此相位即跨分支守卫：win 修复不在场时必红。）
- 断言 5 条（no-data-degraded-warning / no-data-no-critical / no-false-critical-zero-runway / no-cash-missing-degraded / no-cash-missing-no-critical）全过，9/9。

### 10-8 审计员全链路复核（k3_only，100% 门槛） — ✅ PASS（本报告即执行体）

### 10-5 告警→工单→推送链路 — **V1 外**；接线审计结果：生产调用点存在（无 NOT-PASS 缺口）

- Cron 入口: `src/agent/sentinel-service.ts:282` `await runner.aggregateAndDispatch()`（定时哨兵 → 聚合）。
- 告警→工单: `src/sentinel/runner.ts:767-775` emergency/critical 信号 `INSERT OR REPLACE INTO sentinel_tickets ... status='open'`；读/迁移 API `src/routes/sentinel.ts:96/:108`。
- 推送: `src/sentinel/runner.ts:620-637` critical → `proactivePush.onP0Finding(finding)`（D17，3 次重试，异常 log.warn 不阻断）；接线 `src/agent/synova-agent.ts:97-140`（ProactivePush 实例 + `sentinelRunner.setProactivePush`）；BossMailbox `src/deploy/bootstrap.ts:1072-1077`。
- 如实声明: 本审计核了**生产调用点**（链路闭合），未跑 GS-05 端到端运行时证据——该点属 V1 外，不进 6/6 判定。

### 10-7 报告一页纸含资本结论 — **V1 外**（不在本次判定）。

## 三、账本重算（终裁）

- 命令: `python3 scripts/project/gen-project-board.py`（在审计 worktree、含本审计 k3 证据的树上）
- 输出: `ledger.json → ... v1_total=128 v1_passed=27 verified=16`
- 线10: **`v1_passed=6 / v1_total=6, verified=6`**，freshness green×6。10-8 主证据 = `k3-line10-2026-09-18.json`。

## 四、审计发现（分级）

| 级 | 发现 | 归因 |
|---|---|---|
| P1 | **账本漂移**: main 已提交的 `docs/synova/project/ledger.json`（5b1df207，2026-09-18 10:59 生成）中线10 = 1/6、10-1/10-2/10-3/10-6 evidence 为空——而 2026-09-17 的 GS-03/test 证据文件当时已在库。重跑同一生成器（未加任何新证据前）即得 5/6。即 **D803 证据合并后账本未刷新**，「5/6」的派单背景数字靠人转述而非账本自证。本审计已在分支中附重算后的 ledger.json 修正。 | control-tower（证据合并后无「重跑账本」门禁；建议把 gen-project-board 纳入合并后钩子或每日 Cron） |
| P2 | 派单标题「六点（10-1~10-6）」与账本 V1 六点（…10-8）口径不一致，易误导审计范围。 | control-tower（派单措辞） |
| P2 | `k3-task-line-10-2026-W37.md` L130 自认 CI 不跑 GS 场景（.github/workflows 无 GS-0* 调用）——跨分支守卫只护证据层不护 CI。已知且如实声明，非隐瞒。 | implement（建议后续把 GS-03 纳入 CI，独立任务） |

## 五、L4 防线缺口（本该拦住什么？为什么没拦住？）

1. 账本漂移（P1）：**本该拦住它的是「合并后账本重算」防线**——不存在。证据合并（#624/#625）与账本生成（D806 #630）两个 PR 交错，后者先合即冻结了过期视图。缺口=机制缺口，不是执行失误。凑 5-8 次进控制塔健康审计。
2. GS 场景不进 CI：门禁链（pre-commit/CI）对「证据是否可复现」零覆盖，全靠 K3/D774 重跑。已有台账记录，此处仅确认仍未闭合。

## 六、红线自检

- 未改任何产品代码（变异测试后 `git checkout --` 还原，`git status` 仅余本审计写集文件 + node_modules 符号链接）。
- 未改 `scripts/audit/**`、未写审计标准、未改 product-lines.yaml 验收点定义 / 证据 kind / V1 v0.2 标准。
- GS-03 全程 `SYNOVA_DB_PATH` 临时库隔离，未碰 `data/synova.db`。
- 独立取证：fetch 后独立 worktree，全部命令本审计亲自执行，未引用开发者已有输出。

## 七、未覆盖面清单

- GS-05（10-5 运行时端到端）、GS-08（10-7）未跑（V1 外）。
- 10-2/10-4 仅跑了契约与跑道两套件，未跑全量 vitest（不属本卡判定式）。
- Win 侧真机证据（D799 next 提及）不在本审计环境。
