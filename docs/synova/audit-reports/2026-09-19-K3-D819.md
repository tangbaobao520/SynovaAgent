# K3 独立审计报告 — D819 工具接线三修（D836 抽审 / 试点指标③）

> 审计员: Kimi K3（零上下文独立会话）
> 审计对象: PR #646（实现面）/ PR #647（过程记录面），分支 `feat/d819-tool-wiring-pilot`（head 6b29ff5b）
> 审计日期: 2026-09-20 | 任务卡: task-state/D836.json
> 环境: macOS / Node + vitest（主工作区 node_modules 软链至独立 worktree，代码零复用小队会话）
> **结论: PASS（审计通过）** — 0×P0 / 1×P1 / 5×P2；6 条必查全部 ✅

## 0. 结论与计数

| 级别 | 数量 | 摘要 |
|---|---|---|
| P0 | 0 | — |
| P1 | 1 | F1 D473 reminder 为同一次 tool_call 双推 role='tool' 消息（同一 tool_call_id 出现两次，结构非法）— 小队自验 L5 已登记，静态核证属实，建议另卡修复 |
| P2 | 5 | F2 stream 路径 delta.tool_calls 未映射且未编号登记（K3 新发现）；F3 证据 sha256 确定性仅限同日（K3 新发现）；F4 D817 证据文件过期（小队 L4）；F5 memory note 路径偏离 + bypass.log 入库（小队 L3）；F6 兜底序号按 provider 实例重置可跨请求重号（小队 L6） |

**试点指标③数据源：K3 挑出 6 个问题（P1×1 + P2×5），其中 4 个是小队自验 L1-L6 已发现的（L3/L4/L5/L6），K3 独立新发现 2 个（F2/F3）。**

## 1. 独立取证方式

- 独立 worktree：`git worktree add audit-wt/d836 origin/feat/d819-tool-wiring-pilot`（不复用在飞 session 工作树）
- 小队测试复跑：`npx vitest run tests/agent/tool-loop-tool-pairing.integration.test.ts` → **5/5 通过**
- **K3 自建取证脚本**（`tmp-k3-d836/k3-verify.ts`，审计后已删除，不入库）：自建 30 行假 OpenAI 上游 + 真实 `createOpenAICompatibleProvider` + 真实 `ToolLoopExecutor` + 真实 `registerBuiltinTools`，dump 出站请求体 —— 不使用小队任何断言代码与 dump。

## 2. 必查 6 条逐条判定

### ① 工具真的发出且工具循环被驱动 — ✅ PASS

生产代码路径：
- `src/agent/tool-loop-executor.ts:142-144` — `callLlm(messages, { tools: toolRegistry.toOpenAITools() })`
- `src/agent/tool-loop-executor.ts:285-287` — 流式路径同样携带 tools
- `src/providers/base.ts:152-153` — `if (opts?.tools && opts.tools.length > 0) body.tools = opts.tools`（此前在 provider 边界被丢弃的缝，已接上）
- `src/providers/base.ts:230-247` — 响应侧 `message.tool_calls → ChatResult.toolCalls` 映射（D817-F1 修复）

独立复跑原始输出（K3 自建脚本，streamWithToolLoop 生产路径）：
```
[S1] reply="K3 done" registeredTools=36
[S1] req0 bodyKeys=model,messages,temperature,max_tokens,tools toolsLen=36
[S1] req1 roles=system,user,assistant,tool
```
出站 body 含 `tools` 且长度 36 == 进程内独立复算注册工具数 36；第二轮请求出现 `role='tool'` → 工具循环真实驱动。工具被真实执行（`show_diagnosis_progress` handler 产物非空壳，见小队测试 A 亦断言 totalPhases/percent）。

### ② tool_call_id 配对 + 缺 id 兜底 — ✅ PASS

- 配对：`src/agent/tool-loop-executor.ts:164` `normalizeToolCalls(result.toolCalls)` 归一化后，assistant（:165-169）与所有 tool 消息（:188/:209/:214/:241）共用同一 `tc.id`；`crypto.randomUUID` 已从本文件全部移除（import 已删，diff 实证）。
- 缺 id 兜底两层：provider 边界 `call_resp_<n>`（base.ts:240-241，log.warn + degraded:true，不静默）；executor 边界 `call_loop_<n>`（tool-loop-executor.ts:57-66）。

K3 自造「完全缺 id 字段」确定性场景（比小队的空串更苛刻），两遍全量独立复跑：
```
[S2] run1 ids=["call_resp_1"] toolIds=["call_resp_1"] roles=system,user,assistant,tool
[S2] run2 ids=["call_resp_1"] toolIds=["call_resp_1"] deterministic=true
```
兜底 id 确定性成立、消息不丢、配对合法、非 UUID。✅

### ③ 助手不重复入上下文（第 2 轮角色序） — ✅ PASS

K3 自己 dump（S1 req1）：`roles=system,user,assistant,tool` —— 精确等于验收要求的 `[system,user,assistant,tool]`，无连续 assistant。
代码面：`tool-loop-executor.ts` 两处最终态 assistant push 已删（diff 实证 `messages.push({role:'assistant'...})` 被移除，注释指明由 conversation-engine.ts:785/790 统一入上下文）——双推根因消除，非在别处抵消。

### ④ 自验记录 6 条遗留 L1-L6 逐条核 — ✅ 如实登记，无掩盖

| # | 小队登记 | K3 对代码核证 |
|---|---|---|
| L1 | 证据每场景 registered_tool_count 恒 0 | 属实（修复前 `let evidenceToolCount=0` 未赋值）；**已在 d8df1918 修复**——现测试 `:167` 使用同源 `expectedToolCount`，已提交证据文件 5 场景均 reg=36，与顶层一致 ✅ |
| L2 | 体例自证用例 overclaim | 属实；已修——测试 `:536-559` 现显式写盘后读回校验（scenario 数、每场景工具数同源）✅ |
| L3 | 写集偏离 3 处 | 属实：memory note 现位于 `memory/notes/implemented/`（批准为 proposed/，见 F5）；bypass.log 入库；D817 证据已还原（diff vs main 不含该文件）✅ |
| L4 | D817 证据文件过期 | 属实：HEAD 上 `D817-capture-20260918.json` 的 `p0_1_tools_field.observed` 仍为 `false`（实已修复）——见 F4 ✅ |
| L5 | reminder 双推 tool 消息 | **静态核证属实**：tool-loop-executor.ts:213-218（非流式）/358-364（流式）reminder 与 :244/:393 结果共用同一 `tc.id` → 同一 tool_call_id 两条 role='tool'，OpenAI 严格上游将拒收 → 升 P1（F1）✅ |
| L6 | 兜底序号按 provider 实例重置 | 属实：base.ts:124 `fallbackToolCallSeq` 闭包变量，routes 每请求新建 provider → 跨请求可重号（仅影响缺 id 非规范上游）→ P2（F6）✅ |

未发现被掩盖的问题；L1/L2 的问题已被小队在自验退回后真实修复（commit d8df1918），而非仅登记。

### ⑤ 证据文件自证 — ✅ PASS（附 1 个 P2 边界）

- **tools 长度同源复算，非硬编码**：测试探针 `registeredToolCount()`（:223-233）用与 routes 同一 `registerBuiltinTools` 进程内复算；K3 独立构造同款注册链得 **36** == 已提交证据 `registered_tool_count: 36` == 每场景 reg=36 == 每请求 `tools_len: 36`。证据文件与测试断言同源（`:167`），C2 收尾轮 `tools_len: 0`（tools 字段不写）行为亦与代码 `opts.tools.length>0` 条件一致。
- **确定性复跑**：K3 连续两次复跑测试，证据文件 sha256 均 `d42618db…` 完全一致 ✅。与已提交文件（`95cdefd1…`）差异经 JSON diff 仅 1 字段：`captured_date`（2026-09-19 vs 09-20）→ 确定性承诺**仅限同日**成立（见 F3，P2：captured_date 未列入 volatile_fields_excluded，跨日复跑 sha256 必不一致）。

### ⑥ 接线审计 — ✅ PASS

- `createOpenAICompatibleProvider`（改动文件 base.ts 的工厂）：生产调用方 `src/providers/deepseek.ts:20`、`src/providers/openai.ts:17`、`src/providers/gateway.ts:13`（grep 实证，非测试引用）。
- `ToolLoopExecutor`（改动文件）：`src/agent/conversation-engine.ts:466` 构造。
- `ToolCall.id/type`（types.ts）：被 base.ts 与 tool-loop-executor.ts 消费；K3 端到端脚本即走真实链路（ConversationEngine → executor → callWithResilience(retry-middleware.ts:272, pickChatOptions 透传 tools) → provider.chat → fetch）。
- 关联测试回归：`tests/stream-tool-loop.test.ts` 6/6 ✅、`tests/integration/production-entry-conversation.integration.test.ts` 9/9 ✅。

## 3. 问题清单（file:line + 复现）

| # | 级别 | 问题 | 位置 | 复现/证据 | 归因 |
|---|---|---|---|---|---|
| F1 | **P1** | D473 reminder 触发时同一次 tool_call 产生两条 role='tool' 消息（reminder + 结果），共用同一 tool_call_id → OpenAI 严格上游结构非法（400），且 pairingOk 判 false。小队 L5 已登记但未开修复卡 | src/agent/tool-loop-executor.ts:213-218, 244（非流式）；:358-364, 393（流式） | 静态核证（代码路径确定）；C2 用例 :490-492 注释主动避开该触发面 | implement（既有设计，D819 规格明确不改；建议另卡：reminder 内容并入结果消息 content） |
| F2 | P2（K3 新） | `provider.stream()` 的 SSE `delta.tool_calls` 未映射（仅非流式 chat 路径映射）；自验记录仅在 L6 附注一句"仅作上下文"，未编号登记为遗留 | src/providers/base.ts:274-293（stream 实现无 tool_calls 处理） | grep `delta.tool_calls` base.ts 零命中 | devdoc/implement（登记不完备） |
| F3 | P2（K3 新） | 证据 sha256 确定性仅同日成立：`captured_date` 为当日日期且未列入 volatile_fields_excluded，跨日复跑 sha256 必不同 | tests/agent/tool-loop-tool-pairing.integration.test.ts:199 | K3 跨日复跑 diff 仅此 1 字段；同日两跑 sha256 一致 | devdoc/verify（确定性口径应声明"同日"或剥离日期） |
| F4 | P2 | D817 证据文件在 D819 修复后仍是过期事实（observed=false） | docs/synova/product-lines/evidence/D817-capture-20260918.json | `git show HEAD:… | python3 -c …` 输出 observed:false | implement（挂账：下次跑 D817 用例刷新；跨卡一致性） |
| F5 | P2 | memory note 落 `implemented/` 而批准为 `proposed/`（铁律 49 四态流转偏离）；`.claude/bypass.log`（500+ 行历史）随分支入库 | memory/notes/implemented/2026-09-18-D819-…md；.claude/bypass.log | git diff origin/main...HEAD --stat 含两者 | implement/process |
| F6 | P2 | 缺 id 兜底序号按 provider 实例重置，routes 每请求新建 provider → 跨请求兜底 id 可能重号（`call_resp_1` 复用） | src/providers/base.ts:124, 240-241 | 静态核证（每请求 createProvider） | implement（低危：仅缺 id 非规范上游） |

## 4. L4 防线缺口收割

- F1（reminder 双推）：本该拦住的防线 = 集成测试的 pairingOk 全量校验——但 C2 用例主动避开同工具同参数触发面，**测试用例选择性地绕开了已知缺陷路径**。缺口：无"reminder 路径配对"专用用例；控制塔 grep 门禁无法覆盖此语义。归因 control-tower（建议在补丁卡中同时补用例）。
- F2/F3（登记不完备/确定性口径）：自验记录的"遗留清单"无机器校验（是否所有已知缺陷都编号登记）。属于流程软缺口，不值得加脚本——由 K3 抽审兜底（本卡即证据）。
- F5（写集偏离）：pre-commit 组 12 Task Scope 对 memory-note 四态目录与 hook 自动文件无豁免口径 → 偏离只能事后登记。可考虑在门禁中加 proposed/implemented 目录白名单说明（归 control-tower，凑够触发数再立项）。

## 5. 未覆盖面

- PR #646/#647 的 GitHub CI job 级结论未逐一核（本审计以本地物理复跑替代；分支 job 结论未拉取）。
- Windows 实机行为（D819 卡声明"代码级可验，Mac 必须验"）：本审计即 Mac 环境，Win 回归仍按原挂账。
- `provider.stream()` 真实流式上游的端到端行为（生产对话链路未走该路径，见 F2）。
- PR #647 过程记录面的逐 commit 时序复盘（试点复盘由 CTO 出，K3 只抽了证据自证性）。

## 6. 复盘指标③回答

**K3 挑出 6 个问题（1×P1 + 5×P2），其中 4 个（L3/L4/L5/L6 → F1/F4/F5/F6）是小队自验已发现的，2 个（F2/F3）为 K3 独立新发现。** 小队自验的 6 条遗留全部属实、无掩盖；L1/L2 还在审计前已真实修复。三修本体（tools 进 body / 配对 / 不双推）六条验收点全过。
