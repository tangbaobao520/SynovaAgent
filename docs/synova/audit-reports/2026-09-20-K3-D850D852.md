# K3 独立审计报告 — D850（度量口径改造）/ D852（§6-5 四项产品决定）

> 审计员: Kimi K3（独立会话，零上下文，不复用自验结论）
> 对象: `25082884`（D852，PR #681）/ `cb1bbcaf`（D850，PR #683）/ `272dbd82`（小队收口，PR #684）
> 审计工作区: 独立 worktree `.synova-wt-d850`（detached @ 272dbd82，只读复验 + /tmp 副作用）
> 环境: macOS，python3，git shallow 主仓 + 完整 worktree（`rev-parse --is-shallow-repository` = true 于主仓；worktree 内 git log 历史完整）
> 日期: 2026-09-21
> 禁项遵守: 未参考 D854/CT-70 那份审计的任何材料。

---

## 结论

**PASS（0 P0 / 1 P1 / 3 P2）**

- 五个对抗点全部独立复验**属实**（含自曝的四条强度缺口 CT-76/77 与 cosmetic 项）。
- 禁百分比、total_lines 26→29、三档计数闭合等"另核"项全部物理核实通过。
- 1 条 P1 为 K3 独立新发现：**D850 新增的两个测试脚本未接入任何 CI workflow / 门禁**（V-2 的修复只在本地手跑，回归防线不闭合）。

---

## 一、五对抗点独立复验（K3 亲测，非转述）

### ① 面板 203 条 evidence_cmd fail-closed —— **属实（通过）**

方法：解析提交件 `docs/synova/product-lines/product-progress.json`，递归枚举全部 `evidence_cmd`（排除 `cmd_semantics` 说明文本），得 **203 条**（顶层 7 + 线级 28×7=196，与自验口径一致）。删除 `/tmp/ro-pp.json` 后逐条 `bash -c` 执行：

```
rc=1: 203 / 203   （无一条依赖残留文件）
```

随后跑 `regenerate_cmd`（`calc-progress.py --out /tmp/ro-pp.json --today 2026-09-20`）→ 逐条重跑：**rc=0: 203/203**。fail-closed 与绿路径双向成立。

### ② 改源后提交件命令报 MISMATCH（可证伪性）—— **属实（通过）**

方法：篡改证据源 `docs/synova/product-lines/evidence/k3-D592-closeout.json` 点 2-1 `pass→fail`（备份于 /tmp，验后已还原，工作树终态干净）→ 重新生成中间件 → 逐条重跑 203 条：

```
rc=0: 199   rc!=0: 4
MISMATCH buckets.other_states.wired_broken.count: fresh=5 committed=4
MISMATCH buckets.other_states.stale.count:         fresh=35 committed=36
MISMATCH lines[id=2].buckets.other_states.wired_broken.count: fresh=1 committed=0
MISMATCH lines[id=2].buckets.other_states.stale.count:        fresh=3 committed=4
```

受影响档精确变红、未受影响档 199 条仍绿——**第三方跑它确实能得出与看板不同的结论**，第二轮退回（ⓐ"读回派生物"）的修复真实有效。

> 审计过程注记：K3 首次复验时误用了不存在的 `--quiet` 参数导致重生成静默失败、比对落在**陈旧中间件**上 → 203/203 假 rc=0。这本身即 ③ CT-76 场景的活体演示（见下）。修正方法后结论如上。

### ③ CT-76 陈旧中间件假绿 —— **属实（复现成功，维持台账 P1/强度类）**

方法：干净态重生成中间件 → 篡改源（同一 k3 点 fail）→ **不重生成** → 跑顶层 `wired_broken` evidence_cmd：

```
rc: 0   输出: wired_broken=4   ← 源已 fail，读数仍是旧的 4（假绿）
```

与自验 V-16 三步实测完全一致。命令断言了中间件**存在**，未断言其**新鲜**（无源指纹/mtime 校验）。危险窗口 = 复用 `/tmp` 残留件；CI 组①路径不触发。已登记 CT-76，修复方向（源指纹校验）合理，K3 无补充。

### ④ CT-77 线级 bound 判据 healthy 全 0 时恒真 —— **属实（代码级确认）**

提取线级 healthy 的 `independent_check_cmd`（`product-progress.json` lines[0].buckets.healthy），判定核心：

```python
if h is not None and h > len(covered):
    sys.exit('BOUND VIOLATION ...')
```

单向上界断言。当前 28 线 healthy 全 0 → `0 > 任何非负数` 恒假 → **恒真通过**，只能抓高估、抓不到低估/漏点。自验 V-17 自曝准确；"equality×1 / reconciliation×28 / bound×28" 的强度分层与覆盖缺口（线级 missing 28 档无 probe、other_states 112 档无独立判据）维持 CT-77 待修。

### ⑤ MISMATCH 路径前缀重复 `buckets` —— **属实（cosmetic，P2）**

②的实测输出中可见：`buckets.buckets.other_states.wired_broken.count`、`lines[id=2].buckets.buckets.other_states...`——路径标签把 schema 根键 `buckets` 与档前缀拼接了两次。不影响判定正确性（比对用的 key 正确），仅污染人读路径。即 V-19。

---

## 二、另核项

| 项 | K3 实测 | 结论 |
|---|---|---|
| 禁百分比 grep（6 文件 × 5 模式） | `grep -rn "product_progress_pct\|delivery_pct\|verify_pct\|产品总进度\|总完成度"` 于 gen-project-board.py / calc-progress.py / gen-progress-page.py / ledger.json / product-progress.json / product-progress.html → **零命中（rc=1）** | ✅ |
| HTML 渲染型百分比残留 | `grep -o ">[0-9]\+%<\|width: [0-9]\+%" \| grep -v "width: 100%"` → 零输出 | ✅ |
| HTML 派生件可复现 | `gen-progress-page.py` 重跑 → `git diff` 空（逐字节相同） | ✅ |
| total_lines 26→29 | `cb1bbcaf^:product-progress.json` = **26** → 提交件 = **29**（yaml lines 28 + v2_lines 1，K3 用 productline_yaml 独立解析 len(lines)=28 复核） | ✅ |
| ledger 三档计数闭合 | 重跑 `gen-project-board.py`：healthy=16 / written_not_wired=3 / live_unverified=11 / state_unknown=98，**16+3+11+98=128=v1_total** ✅ 闭合；`identity.holds=true` 且 note 已明示"完备性≠归类正确"（B-1 同义反复自曝被正确固化进产物） | ✅ |
| 面板自身计数自洽 | 面板 healthy=0 / wnw=3 / wired_broken=4 / live_unverified=32 / stale=36 / state_unknown=105，和=**180=denominator** ✅（与 ledger 分母不同，两件各自 `denominator_note` 明示禁混读） | ✅ |
| D852 yaml 只加不删 | `git diff --numstat 21b7e4af..25082884 -- product-lines.yaml` = `30 0`；28 线 / 180 点；6 新点（3-9/4-9/8-7/8-8/25-8/25-9）全部在册 | ✅ |
| 测试回归 | `gen-project-board.test.sh` → **PASS=117 FAIL=0**；`calc-progress-panel.test.sh` → 本环境 **SKIP（PASS=0 FAIL=0）**，见 P1 | ⚠️ |
| bypass 记录 | 三提交中 `.claude/bypass.log` 新增行全部为 `COMMITTED \| pre-commit PASS` 正常登记，无 detected-bypass | ✅ |
| 写集外"总完成度"残存（已登记 CT-74） | K3 复核命中：`scripts/control-tower/views/pm_dashboard.py:110`、`app/founder-dashboard.html:127` —— 行号与台账一致；属控制塔域，D850 未越界 | 登记 ✅ |

---

## 三、发现与分级

### P1-1（新发现，归因: implement）— D850 两个测试脚本未接入任何 CI / 门禁，V-2 修复只在本地成立

- 物理证据：`grep -rn "calc-progress-panel.test.sh\|gen-project-board.test.sh" .github/ scripts/ tests/` → 除 `calc-progress.py:462` 的注释性提及与测试文件自身外，**零调用方**。`ci.yml` 的测试清单（ci.yml:225-234 等）不含 `tests/project/*`；`product-progress.yml` 只跑 `refresh-all.sh`。
- 影响：第二轮退回的根因之一 V-2（"面板新契约无测试覆盖，117/0 全绿仍漏 V-1"）——修复产出了测试，但**没有任何自动化防线会跑它**。下一次 calc-progress.py 被改动，V-1 类回归仍只能靠人手跑。这正是铁律 0-2「接线验收」在测试域的同构缺口。
- 附注：`calc-progress-panel.test.sh` 的 ⓓ-5 git 闸在 shallow clone 下显式 SKIP + 原因 + exit 0（实测行为正确，不假红）；但 CI 各 job 均 `fetch-depth: 0`（ci.yml:16/115/213/298），接线无环境障碍——没接不是环境问题。
- 修复方向：把 `tests/project/{gen-project-board,calc-progress-panel}.test.sh` 加入 ci.yml 测试 job 清单（一行改动，控制塔域）。
- 注：自验记录 V-2 闭环段写"可进 CI"——"可进"为真（密封、SKIP 语义正确），"已进"为假。措辞边界如实，不定性为不实陈述，定 P1 缺口。

### P2-1（cosmetic，即 ⑤/CT 台账 V-19）— MISMATCH 路径标签 `buckets` 前缀重复

见 §一⑤。建议修（一行拼接修正），不阻塞。

### P2-2（登记项复核）— 自验记录内部数字漂移：V-2 闭环段称 panel 测试 "12/0"，第三轮回归称 "20/0"，收口件无该测试终态版本号锚定。不影响结论，建议后续自验记录对同一测试只引「commit + 实测数」。

### P2-3（已有台账复核通过，无新缺口）— CT-71（G12d `${line:3}` 多切 1 字符 → 虚设门禁恒 ✅）、CT-72（brief 无日期前缀 → G12 误判）、CT-73（控制塔测试写真实仓库 + shallow 假红）、CT-74/75（跨域百分比残存）、CT-76/77：台账描述与 K3 在本 worktree 的实测（③④及行号复核）一致，登记内容可信。

---

## 四、L4 防线缺口收割

> 本报告发现的问题，控制塔哪道防线本该拦住？为什么没拦住？

1. **P1-1（测试未接线 CI）**：本该拦住它的是「pre-commit 组 2 测试配对 / CI 测试清单」——但两者都只断言"测试文件存在"，**不断言"测试被某条自动化链路执行"**。V-2 的教训（全绿的边界=测试覆盖的边界）被修了一半：覆盖有了，执行没有。与铁律 0-2 WIRE CHECK 同构——**接线检查应同样适用于测试脚本本身**（新测试 → grep CI/门禁调用方）。缺口归属：control-tower（无此断言模式）。
2. **CT-76（陈旧中间件假绿）**：本该拦住的是 evidence_cmd 自身的新鲜度校验，属度量链设计缺口，队内已自曝并立卡——防线为何没拦：**「比对型证据」的信任模型默认中间件可信**，与 D320（生成器假红）同族的"证据链上游被隐式信任"模式。建议 M 类归并（比对型证据必须校验上游指纹），而非新加脚本。
3. **CT-71（G12d 虚设门禁）**：fail-open 门禁第二次（CT-64 同族）。已有升级建议（门禁类变更必须带反例测试）足够，K3 不新增机制——**机制数量收敛于错误模式种类数**。

---

## 五、运行环境注记

- 复验全部在独立 worktree `.synova-wt-d850`（detached @ 272dbd82）内完成；唯一仓库内副作用（篡改 k3-D592-closeout.json）已用 /tmp 备份逐字节还原，`git status --short` 终态为空。
- 主审计仓为 shallow clone；`calc-progress-panel.test.sh` 因此 SKIP——此为该测试的设计行为（ⓓ-5），非环境误判来源（K3 已读 SKIP 判定逻辑并区分）。
- 中间件 `/tmp/ro-pp.json` 终态为还原源后的重生成版本，不影响后续审计者（其 evidence_cmd 语义要求使用者自跑 regenerate_cmd）。

---

*K3 独立审计 · 报告 git 跟踪 · 结论机器可读：task-state/D850.json、task-state/D852.json verdict 待回填 `PASS(0P0/1P1)`*
