# K3 独立审计报告 — D821 出站 HTTP 唯一出口（P-1）

> 审计员：K3 独立会话（零上下文）｜ 2026-09-21
> 对象：PR #693 ｜ 分支 `feat/d821-http-proxy` ｜ head `717d67a1` ｜ 基线 `cc4e4166`
> 运行环境：macOS / node v22.22.0 / vitest（审计工作树 /tmp/d821-audit，已复现后清除）
> 结论三态判定：**PASS（附 1 条 P1 收尾跟进项）** —— P-1 口径维持**『部分关闭』**，D860 预算口径下 #693 在 D860 前不合并。

## 一、必核 7 项逐项判定（每条附命令 + 原始输出）

### 1. 收敛真实性（providers 4→0；全仓计数）— **属实**

独立复算（工作树 @717d67a1，非采信自述）：

```
$ grep -rn "fetch(" src/providers/ | grep -v test
（0 行）
$ git grep -n "fetch(" cc4e4166 -- src/providers/
cc4e4166:src/providers/base.ts:154 / :203、cc4e4166:src/providers/ernie.ts:40 / :110   → 4 处
$ grep -rn "fetch(" src/ --include="*.ts" | grep -v test | wc -l   → after 59（before 63）
```

接线点（after）：`src/providers/base.ts:180`、`:243`、`src/providers/ernie.ts:44`、`:136`（均 `outboundFetch(`，import 见 base.ts:19 / ernie.ts:14）。
代理 env 读点唯一性：`grep -riE "https?_proxy|all_proxy|no_proxy" src/` 排除 http-exit.ts 后仅 `src/providers/types.ts:75`（JSDoc 注释），无第二个读点。

### 2. 穿真实入口 + 假代理收 CONNECT（非 mock 断言）— **属实（已复现）**

`tests/providers/http-exit-proxy.integration.test.ts` ①（:246-）：`createOpenAICompatibleProvider`（真生产入口，base.ts:50 import）→ 断言假代理**原始字节流** `CONNECT llm-intranet.invalid:443 HTTP/1.1`（:278）+ 真 TLS 往返返回 content。无 `vi.mock` 管线；夹具为真 node socket 服务端。本机复跑：**①② 等 3 文件 94/94 全绿**（Duration 43.88s）。

### 3. 降级：代理不可达 → PROXY_UNREACHABLE + 无静默直连回退 — **属实**

用例 ⑥a/⑥b（integration :519-）：关闭代理端口 → `code='PROXY_UNREACHABLE'`、`phase='connect'`、`degraded=true`、**origin 0 命中**；⑥b 更以"直连可达的目标也不许被偷走"作强判别。实现侧：`http-exit.ts:925` `'代理不可达（CONNECT）— 抛错（禁止静默直连回退）'`，代码路径 reject 不回退（catch 无 direct 重试分支）。配置期降级（socks/invalid）仅在解析期直连 + `log.warn` + `degraded=true`（:951、:210）。自造夹具复跑通过。

### 4. 健康暴露只含变量名、不含值 — **属实（反推不可能）**

`getProxyStatus()`（http-exit.ts:495-538）：`variables = [...configured]`（纯变量名）；`reason` 为静态文案或变量名/协议名，无值拼接。降级 `warnDegraded`（:207-210）仅记录 `{variable, kind, degraded, reason}`；dedup key 里的 `entry.value.slice(0,24)`（:230）**只作 Map 键，不进任何日志/返回值**。凭据仅进 `Proxy-Authorization` 头（:545、:751、:811）。从全部输出面反推不出 env 值或凭据。

### 5. 反向验收（抽掉修复必须变红）— **属实（两条轴均独立复现）**

- 轴 1（收敛）：对 base.ts 两处**非注释代码行**做 `outboundFetch( → fetch(` 回退（diff 仅 2 行、均代码行）→ integration **3 failed / 9 passed**（①主证 chat、①healthCheck、②次证 http 全红，`fetch failed`）；恢复后全绿。
- 轴 2（taxonomy 桩迁移）：以 `git show 2d810075^:tests/errors/llm-error-taxonomy.test.ts` 旧版（stubGlobal）覆盖新版 → **5 failed / 17 passed**（与自验记录声称的 5 例一致）。非注释行核实：e34cc2b9 对该文件的改动含 `vi.mock`/`beforeEach`/`mockResolvedValue` 等真实代码行（非仅注释），"断言未改"经 diff 核对属实。

### 6. 冻结件哈希 — **吻合**

```
$ shasum -a 256 src/providers/http-exit.ts
fe5061f959db5a4363894d531f6a713c796fae5b8f693ae9cc142616926b1a80
```

与派单锚点一致；git diff 范围内该文件仅在 2d810075 落盘一次。

### 7. 未覆盖面 7 项 + P-1 口径 — **如实**

自验记录 §3.13 表**恰 7 行**（127.0.0.2 上游收 1 / Win 实机 / 真实内网拓扑 / SOCKS5 / 代理认证 / 自定义 CA 等），§3.12 明确 `P-1 = 部分关闭（不得标达成）`；task-state note 同口径。未以"通过/唯一出口已落地"概括。

## 二、执行审计（L3）与基线

- 提交集 = 4 commits（2d810075、e34cc2b9、两条 bypass 登记 hook 提交）；写集边界独立复核：`scripts/control-tower|scripts/audit|.github/workflows|src/store` **命中 0**（与 CTO 复算一致）。
- `.claude/bypass.log`：D821 时段两条均为 `COMMITTED | pre-commit PASS (hook 层登记)`（2026-09-21T11:03:08 / :03:48），另有一条 11:02:21 `BLOCKED | pre-commit FAIL` 后修正再提交 —— 无 --no-verify 绕过。
- tsc：基线 33 错 = head 33 错，`src/providers/` 两端均 0 —— **零新增**成立（见 P2-1 口径差）。
- `as any / as never / as unknown as`：四个改动 src 文件仅 http-exit.ts:623 **注释行**提及，代码行 0。
- 测试全量复跑：3 个 D821 测试文件 94/94 通过。

## 三、发现清单

| 级别 | 发现 | 证据 | 归因 |
|---|---|---|---|
| P1 | `docs/synova/product-lines/preconditions.yaml` P-1 `status: dispatched` 未回填『部分关闭』——规格 §2.3 承诺该处以部分关闭呈现；当前无虚假达成标注（不构成 P0），但收口/合并前须回填，否则状态面与自验口径漂移 | preconditions.yaml:15-25（head 未改，diff 该文件 0 行） | devdoc/收口流程 |
| P2 | 自验记录 §0.4 称 tsc 存量 28 错，本机复算基线/head 均 33（零增量结论不受影响）；计数口径差疑为环境/依赖差异，未据此外推 | 本报告 §二 | 自验记录口径 |

无 P0。

## 四、L4 防线缺口

本卡未发现门禁漏拦事故。两条跟进项（preconditions 回填、计数口径注记）属文档收口面：现有防线（pre-commit 组 12 task-scope、K3 复算）均已覆盖，无需新增机制。

## 五、判定

**PASS（部分关闭口径成立）**。#693 按 D860 前预算口径不合并；合并前置：P1-1 回填完成。反向验收两次红、冻结哈希吻合、真入口穿隧道证据齐备，质量在同期卡之上。
