# K3 独立审计报告 — D831「P-2 运行期不变量机制」（小队 B）

> 审计员：Kimi K3（独立会话，零上下文）｜ 审计日期：2026-09-21
> 审计对象：`feat/p2-invariants` @ **200869e5**（CTO 复核回执指向的 tip）
> 基线：`origin/main` = **74356ba6**（= 本分支 merge-base，落后 0）
> 命令环境：macOS / Node **v22.22.0**（better-sqlite3 ABI 127，原生可用）｜ 审计工作区独立 clone，未写产品仓库任何跟踪文件

---

## 〇 结论

**判定：FAIL（P0 × 2 / P1 × 3 / P2 × 6）——机制本体真实可用，但一条**明文验收点被物理证伪**，且审计 tip 的 CI 权威门禁为红，故阻断交付。**

一句话：**"违约必炸 + 知道是谁的锅"** 只兑现了后半句。违约在真实生产链上**炸不到 HTTP 层**——被韧性层吞成 outcome 降级，返回 **200 + SSE 降级文案**，正是验收点原文禁止的形态。

| # | 分级 | 结论 | 归因 |
|---|---|---|---|
| P0-1 | 阻断 | 必核 4 被证伪：真实链路注入违约（provider 组装请求丢 tools）→ **HTTP 200**（非 5xx），日志 WARN。`callWithResilience` 吞掉 InvariantError | implement + devdoc |
| P0-2 | 阻断 | 审计 tip 的 **CI 为红**（run 35567327453，Iron Laws job failure），且 **Vitest shard 被 skip** → 回归在 CI 上从未执行；本地门禁 4 次 GATE_FAIL_SOFT 放行 | control-tower + devdoc |
| P1-1 | 建议 | 「registered==0 → 真实 Bootstrap fatal Phase」用例**替换了 execute**（断言的是测试内复制的分支字符串，非生产代码）——声称 overclaim | implement |
| P1-2 | 建议 | INV-DEGRADED-STICKY 分支 B **反向改写 `store.lastDegraded`** → 路由层 `conversations.ts:260` 会对后续**健康轮次**误发 `STORE_DEGRADED` 错误帧（降级信号失真） | implement |
| P1-3 | 建议 | 反接线探针 `hitCount` **不能证明生产触发面**：真实服务器跑完整轮工具对话后 `INV-TOOL-CALL-PAIRING` hitCount **= 0**（该伴生在生产中永不触发） | devdoc + implement |
| P2-1 | 可选 | 回归数字口径：声称 1376 passed，实测 **1378 passed**（5 failed 一致，失败集逐条相同） | implement |
| P2-2 | 可选 | `task-state/D831.json` impl.summary 仍写「15 用例」，交付件实为 **17 用例**（M7 漂移） | implement |
| P2-3 | 可选 | fetch wrap 回滚遇「已被他人替换」只 warn 不还原 → 不变量可静默失效而探针仍显示 registered=3（假绿） | implement |
| P2-4 | 可选 | `src/invariants`（声明 L3）import `src/store/session-store`（L5）跨层；`check-architecture.sh` 的 L3→L5 规则**只扫 `src/l3/`**（结构盲区，既有先例同型） | control-tower |
| P2-5 | 可选 | `registered==0` 守卫**构造上不可达**（execute 先装 3 条再判 0）；commit 全部署名 `synova-cto`（小队 B 身份不可追溯） | implement |

**复审前提（FIX 后只需复审变更）**：P0-1 的验收口径必须同时修「实现」与「判据」——见 §7。

---

## 一 必核 1：三条伴生是否**真接线**（不是仅 import）

**结论：属实（三条全为运行期真 wrap），但其中 1 条在生产中永不触发。**

| 伴生 | wrap 对象 | file:line | 运行期实证 |
|---|---|---|---|
| INV-TOOL-SCHEMA-SENT | `globalThis.fetch` | `src/invariants/companions/tool-schema-sent.invariant.ts:132`（`globalThis.fetch = wrappedFetch`） | **真实服务器上一整轮对话中被真触发**：`PROBE-B-4` lastFailure 非空、hitCount=3 |
| INV-TOOL-CALL-PAIRING | `SessionStore.prototype.appendEvent` | `tool-call-pairing.invariant.ts:74`（原型赋值） | 单测穿真实 appendEvent 触发；**真实服务器 hitCount = 0**（生产 message 双写不携带 tool_calls → 该伴生在生产永不触发，见 P1-3） |
| INV-DEGRADED-STICKY | `SessionStore.prototype.appendEvent` + `.addMessage` | `degraded-sticky.invariant.ts:74,89` | **真实服务器 hitCount = 2**（addMessage 缝在真实链路被走过） |

接线点（非 import，是真调用）：

```
src/deploy/bootstrap.ts:37   import { createRuntimeInvariantsPhase } from '../invariants';
src/deploy/bootstrap.ts:667  this.registerPhase(createRuntimeInvariantsPhase());
src/server.ts:41             import { invariantHealthRoutes } from './invariants';
src/server.ts:384            app.use(invariantHealthRoutes);
```

原始输出（K3 独立探针，真实 `createServer()`，Phase 6 在启动序列内真装）：

```
[PROBE-B-0] registered = {"registered":3,"invariants":[{"code":"INV-TOOL-SCHEMA-SENT","owner":"squad-b/coding",...,"hitCount":0,"lastFailure":null},{"code":"INV-TOOL-CALL-PAIRING",...,"hitCount":0,...,null},{"code":"INV-DEGRADED-STICKY",...,"hitCount":0,...,null}]}
[PROBE-C-1] 有注册 status = 200 | body = {"registered":3,"invariants":[
   {"code":"INV-TOOL-SCHEMA-SENT","owner":"squad-b/coding","hitCount":3,"lastFailure":"出站 LLM 请求携带工具对话…"},
   {"code":"INV-TOOL-CALL-PAIRING","owner":"squad-b/coding","hitCount":0,"lastFailure":null},
   {"code":"INV-DEGRADED-STICKY","owner":"squad-b/coding","hitCount":2,"lastFailure":null}],"stickyDegradedSessions":0}
```

> 注：`PROBE-C-1` 的 hitCount 是上一用例驱动过真实工具对话之后的读数 → **pairing 在生产链上路过后仍为 0**，与实现自述「生产 message 双写不携带 tool_calls/tool_call_id」一致（见 P1-3）。

---

## 二 必核 2：registry 语义独立复算

**结论：四条全属实（K3 独立探针复算，非采信自述）。**

| 语义 | 实现 file:line | 独立复算原始输出 |
|---|---|---|
| install **同步 void** | `registry.ts:116`（签名 `): void`）；伴生 `install(ctx): void` | `[PROBE-E-1] install 返回值 = undefined | typeof = undefined` |
| **重名拒绝** | `registry.ts:123-125` | `[PROBE-E-2] 重名 = invariants: code "INV-TOOL-SCHEMA-SENT" is already registered | registered = 1` |
| **失败释放注册** | `registry.ts:147-152`（`entries.delete` + `runRollbacks`） | `[PROBE-E-4] 第 2 条抛错 = probe-bomb | registered = 0 | fetch 还原 = true` |
| **空 notChecked 拒绝注册** | `registry.ts:126-129` | `[PROBE-E-3] 空 notChecked = invariants: companion "INV-TOOL-SCHEMA-SENT" must declare non-empty notChecked (总纲 §9.4) | registered = 0` |

编译期 void 断言真实性：`tests/sentinel/invariants.test.ts:363-375` 的 `IsPromise extends Promise ? true : false` + `const notAPromise: IsPromise = false` 是**真**编译期断言（若 install 返回 Promise 则 `false` 不可赋给 `true`）。但断言对象是三条伴生**自带的 `): void` 标注**，属自证式（P2-5 附注）。

---

## 三 必核 3：健康面 `/api/healthz/invariants`

**结论：属实——真返回 registered + 每条 hitCount/lastFailure；空注册表 503。**

`src/invariants/health-route.ts:17-35`（有注册 → 200 + `{registered, invariants:[{code,owner,packageName,hitCount,lastFailure}], stickyDegradedSessions}`），`:20-24`（registered==0 → 503 + `error:'invariant registry empty — fail-closed'`）；挂载 `src/server.ts:384`。

K3 独立复跑（真实 HTTP，非单测替身）：

```
[PROBE-C-1] 有注册 status = 200 | body = {"registered":3, ... "hitCount":3,"lastFailure":"出站 LLM 请求携带工具对话…" ...}
[PROBE-C-2] 空注册 status = 503 | body = {"registered":0,"invariants":[],"error":"invariant registry empty — fail-closed"}
[PROBE-C-3] 复原 registered = 3
```

---

## 四 必核 4（强判别）：负向 fail-closed → 5xx？ —— **证伪（P0-1）**

**结论：不成立。真实链路注入违约 → HTTP 200 + SSE 降级文案 + 日志 WARN。调用方（韧性层）把 InvariantError 吞成 outcome。**

### 4.1 代码链（三段，缺一不可）

```
src/invariants/companions/tool-schema-sent.invariant.ts:117-121   fail() → throw InvariantError  ✅ 不变量自己抛了
src/agent/tool-loop-executor.ts:99                               this.callLlm(...) → callWithResilience
src/llm/retry-middleware.ts:272-276                              「永不抛异常（outcome 化）」
src/llm/retry-middleware.ts:290                                  catch (err) { ... }        ← 吞掉
src/llm/retry-middleware.ts:304-315                              normalizeLlmFailure → return {ok:false, degraded:true}
src/agent/tool-loop-executor.ts:288-294                          if (!outcome.ok) → return `抱歉，调用失败：${outcome.message}`
src/routes/conversations.ts:212                                  「此后错误一律走流内 error 帧，不再改状态码」
```

### 4.2 K3 独立复现（不采信测试，自建真实 HTTP 链路）

注入方式 = **在真实服务器上让出站请求体丢掉 `tools`**（provider 组装请求丢 tools 的忠实等价：请求交给已被不变量 wrap 的 fetch 时 `tools` 已不在 body），上游用 D819 同款假上游（`tests/helpers/fake-llm-upstream.ts`，零 mock 管线）：

```
[PROBE-B-0] registered = 3（Phase 6 在 createServer 内真装）
[PROBE-B-1] 摘掉 tools 的出站请求数 = 2
[PROBE-B-2] HTTP status = 200 | content-type = text/event-stream; charset=utf-8
[PROBE-B-3] SSE 原文 = "event: open … event: token … \n[工具调用: show_diagnosis_progress ]\n
   event: agent_message\ndata: {\"type\":\"agent_message\",\"content\":\"抱歉，调用失败：deepseek chat: invariant
   INV-TOOL-SCHEMA-SENT violated by \\\"@synova/agent\\\" (owner: squad-b/coding): 出站 LLM 请求携带工具对话
   （assistant.tool_calls / role=tool）但 tools schema 缺失，且该对话从未成功携带过 tools — D819 复发形态\"}
   \n\nevent: end\n\n"
[PROBE-B-4] 真不变量 lastFailure = {"code":"INV-TOOL-SCHEMA-SENT","hitCount":3,"lastFailure":"…tools schema 缺失…"}
[PROBE-B-5] 上游实际收到的轮次数 = 1 | bodyKeys = [["model","messages","temperature","max_tokens"]]
```

生产侧日志（同一次运行）：

```
{"level":40,"service":"agent/tool-loop","code":"INTERNAL","kind":"error","attempts":1,"degraded":true,"round":1,
 "msg":"streamWithToolLoop: LLM 调用降级（韧性层分类）"}
```

### 4.3 与验收点原文的对照

> D831 卡 acceptance_points[0] / 派单 §二 / task brief Q3:44：「负向：注入一次违约（provider 组装请求时丢掉 tools）→ **5xx** + 日志含 owner 名（**不许『返回 200 但日志 WARN』**）」

- 命题前半（5xx）：**假**（200 + `agent_message` 帧）。
- 命题后半（不许 200+WARN）：**命中被禁形态**（200 + WARN；owner 名只出现在日志 error 与用户可见文案里）。
- **连带**：`task-state/D831.json` impl.verification 与 M6 文档 §② 所称「HTTP 层 5xx 语义」属**声称无据**（无任何用例断言过 HTTP 状态码；`tests/sentinel/invariants.test.ts:95-128` 断言的是 `provider.chat` 直调 reject——**该调用形态在生产中不存在**，因为生产必经 `callWithResilience`）。

### 4.4 根因归属（"该改哪个预设"）

- **implement**：实现选了 fetch 缝（合理）但**未核下游吞错层**即宣称 fail-closed；测试用「直调 provider.chat」代替生产组成路径。
- **devdoc（含派单/brief）**：验收点要求「5xx」，而既有架构在 SSE 建流后**明确不改状态码**（`conversations.ts:212`）——该判据在写下来那一刻就与架构冲突，属**不可达判据**（同 D821 的「字面 HTTPS_PROXY 命中=0」空洞判据家族）。

---

## 五 必核 5：原子回滚（LIFO + 回滚后违约不再被拦）

**结论：属实。**

- LIFO：`registry.ts:159-172`（`installedCodes.reverse()` 逆安装序 uninstall）；`registry.ts:182-188`（`uninstallAll` 逆序）；同条内回滚钩子亦 `reverse()`（`registry.ts:219`）。
- 回滚后违约不再被拦：`tests/sentinel/invariants.test.ts:311-335` 断言 `SessionStore.prototype.appendEvent` 复位 + 违约 appendEvent `not.toThrow()`；K3 独立复跑该文件 **17/17 全绿**（原始输出见 §6）。
- K3 复算 fetch 侧还原：`[PROBE-E-4] … fetch 还原 = true`。
- 真实 Bootstrap 回滚：`[PROBE-E/§6]` + 真 Phase 回滚日志 `Phase 6 回滚: 运行期不变量 wrap 已全部撤销（无残留监听/部分注册）`。

---

## 六 必核 6：`registered==0` → 启动失败（bootstrap Phase 6，fatal + rollback）

**结论：生产代码属实（K3 用真 `execute` 独立证真）；但交付测试对该分支的覆盖是替身（P1-1）。**

实现：`src/invariants/index.ts:56-76`（`fatal: true`、`rollbackOnFail: true`、`:67-69` registered==0 抛错、`:72-75` 回滚）；执行位：`bootstrap.ts:242`（`orderedIds = [0,1,2,3,4,5,6]`），`:243-244`（aborted 即中断）。

**K3 独立证真（不替换 execute，只把单例 `installAll` 变空操作 → 真代码路径被判空）：**

```
[PROBE-D-1] 真 execute 抛出 = runtime-invariants: registered==0 — 不变量注册表空转，启动失败（fail-closed，总纲 P-2）
[PROBE-D-2] Bootstrap aborted = true | phase status = failed | errors = ["runtime-invariants: registered==0 — 不变量注册表空转，启动失败（fail-closed，总纲 P-2）"]
```

**P1-1 证据**：`tests/sentinel/invariants.test.ts:286-305` 把 `execute` **换成了测试内重写的同款分支**：

```ts
emptyBootstrap.registerPhase({ ...phase, execute: async () => {
  const snap = invariantRegistry.snapshot();
  const registered = snap.registered + 0;
  if (registered === 0) { throw new Error('runtime-invariants: registered==0 — …'); }  // ← 测试自己抛
} });
```

→ 生产 `index.ts:67-69` 若被删改，该用例**仍会绿**。M6 文档 §② 写「registered==0 → **真实 Bootstrap fatal Phase**」属 overclaim（M2）。K3 已用 PROBE-D 补齐该证据，故影响限于「测试无效 + 声称不实」。

---

## 七 必核 7：用例独立复跑 + 回归基线比对

**结论：17/17 属实；5 个失败确为 main 基线（逐条相同）；但数字口径有出入（P2-1）。**

```
$ npx vitest run tests/sentinel/invariants.test.ts          （K3 独立复跑）
 ✓ tests/sentinel/invariants.test.ts (17 tests) 92ms
 Test Files  1 passed (1)      Tests  17 passed (17)

$ npx vitest run tests/agent/tool-loop-tool-pairing.integration.test.ts
 ✓ (5 tests) 16039ms          Tests  5 passed (5)
```

回归（K3 **串行**复跑，分支 vs main 基线 74356ba6，同机同 ABI）：

```
分支 200869e5 : Test Files 1 failed | 259 passed | 1 skipped (261)   Tests 5 failed | 1378 passed | 1 skipped (1384)
main 74356ba6 : Test Files 1 failed | 258 passed | 1 skipped (260)   Tests 5 failed | 1361 passed | 1 skipped (1367)
diff <(分支 FAIL 集) <(基线 FAIL 集) → IDENTICAL FAILURE SET
```

失败 5 条全部为 `tests/agent/expert-file-loader.integration.test.ts > ExpertFileLoader — 8段组装集成测试`（Given FileScanner 扫描 / Given strategy 专家 / Given 完整索引 / Given 已加载专家→prompt 包含身份和规则 / →getPrompt）→ **确为 main 预存失败，非本卡引入**。

**环境依赖辨伪（D316 同型，K3 自曝口径）**：首轮我把分支与基线**并行**跑，分支多出 1 条 `margin-capital-deextinct.test.ts` hook 超时（`Error: Hook timed out in 10000ms`）；串行复跑后两侧失败集完全一致 → 该差异是**资源竞争导致的环境依赖抖动**，非恒失败。数字口径差异：声称 1376 passed，实测 **1378**；skipped 1 与实测 1 一致（我首轮并行的 12 skipped 亦为抖动）。

---

## 八 必核 8：`src/server.ts` 超写集 +5 行**逐行核**

**结论：属实——5 行全部为注释与挂载，零业务逻辑。**

```
+// D831 (P-2): 运行期不变量健康探针——GET /api/healthz/invariants。       ← 注释
+// 超派单写集（src/server.ts）理由：接线必需（铁律 0-2）——探针路由必须在生产  ← 注释
+// HTTP 入口挂载才可达；routes/healthz.ts 为小队 A 地盘一字未改，故走独立路由。  ← 注释
+import { invariantHealthRoutes } from './invariants';                     ← import
+  app.use(invariantHealthRoutes); // D831 (P-2): /api/healthz/invariants … ← 挂载
```

逐行核验：无分支/无状态/无数据处理；`git diff --name-only 74356ba6..200869e5 | grep -E "^src/(connectors|tools|routes|agent|cli-manager|tui-v2)/"` → **零命中**（未碰小队 A 地盘）；`routes/healthz.ts` 零改动。类型安全：本卡新增/改动文件 `as any|as never|as unknown as` **零命中**（`bootstrap.ts:772,1159`、`server.ts:272-275` 的 `as never` 均在 diff 外的既有行）。

---

## 九 必核 9：stash 事件独立核（M6 登记「误触发一次 git stash、同分钟 pop、零损失」）

**结论：一次 stash 确实发生过，且可证零损失；「同分钟 pop」的时间点无法由 git 证据确证。判「登记不升级」站得住。**

K3 用产品仓库（`/Users/wane/SynovaAgent`，p2 worktree `.synova-wt-p2` @ 200869e5）对象库取证：

```
$ git log -1 --format='%H %P %ci %s' e074c547
e074c547… 74356ba6… 956264b8… 2026-09-21 13:47:07 +0800 WIP on feat/p2-invariants: 74356ba6 Merge pull request #694…
（附带 index 提交 956264b8 = "index on feat/p2-invariants: 74356ba6" 13:47:07）

$ git stash list                      → 仅两条旧 stash（2026-09-05 / 09-08），无 p2 条目
$ git reflog show refs/stash          → 仅 09-05 / 09-08 两条（无 09-21 条目）
$ git diff --stat 74356ba6 e074c547   → 3 文件：src/deploy/bootstrap.ts 12+/2-、src/sentinel/sentinel-events.ts 12/4、src/server.ts 5+
$ git rev-parse e074c547^3            → fatal（无第三父 → 未用 -u，未跟踪新文件未被卷入）
```

**零损失判定（内容级，可证）**：

```
$ git diff e074c547:src/deploy/bootstrap.ts         200869e5:src/deploy/bootstrap.ts          → 空
$ git diff e074c547:src/sentinel/sentinel-events.ts 200869e5:src/sentinel/sentinel-events.ts  → 空
$ git diff e074c547:src/server.ts                   200869e5:src/server.ts                    → 空
```

→ 被 stash 的三个文件内容与最终提交**逐字节相同**；`refs/stash` 无 09-21 条目（pop 会删该条目，与「已 pop」相符）；p2 worktree `git status --short` 空（无悬空改动）。**「同分钟 pop」无法确证**（git 不记录 drop 时刻），但「零损失」成立 → **登记不升级成立**（铁律 0-3 教训已留痕 + 无内容损失）。附：产品仓库另有 2 条 2026-09-21 01:34/01:37 的 p-677 悬空 stash（属别的 session，与本卡无关）。

---

## 十 必核 10：红线

| 红线 | 结论 | 证据 |
|---|---|---|
| 无 `--no-verify` | **属实** | `.claude/bypass.log` 本卡新增 9 行全部为 `COMMITTED \| pre-commit PASS` 与 `GATE_FAIL_SOFT`；`grep detected-bypass` 命中最新为 2026-09-17，**本卡零 bypass 判定**。软放行是 D515 设计（`.git/hooks/pre-commit` 末尾 `exit 0` + 写 marker），非绕过 |
| 无 force push | **属实（无反向证据）** | GitHub PushEvent：`refs/heads/feat/p2-invariants before=6ceea07a head=200869e5 forced=None`（无 forced=true）；分支历史线性叠在 74356ba6 上，五个业务提交哈希未被改写 |
| 未碰 `scripts/audit/**` | **属实** | `git diff --name-only 74356ba6..200869e5 \| grep -E "^scripts/\|^\.github/"` → 零命中 |
| 未自我审计 | **属实** | 分支内无 `docs/synova/audit-reports/**` 变更；本报告为 D831 首份审计件 |
| 写集 | 13/14 在派单写集内 | 仅 `src/server.ts`（+5，已在 commit message/brief/M6 说明，K3 判**接线必需，合规**）；`tests/sentinel/invariants.test.ts` 属派单写集第 4 项 |

---

## 十一 L1 代码审计（15 项）

| # | 项 | 结论 |
|---|---|---|
| 1 | 接口真实性 | ✅ `createRuntimeInvariantsPhase`/`invariantHealthRoutes`/`installRuntimeInvariants` 实测可调（PROBE-B/D） |
| 2 | 文件路径存在性 | ✅ 6 新文件全在（`git diff --stat`） |
| 3 | Edge ID | ➖ 不适用（本卡无 E-XX） |
| 4 | 架构边界 | ⚠️ P2-4：`src/invariants`（声明 L3）import `src/store`（L5）；与 `src/l3/*.ts` 既有先例一致，门禁只扫 `src/l3/` |
| 5 | 数据流完整性 | ✅ 启动 Phase 6 → wrap → 探针读同一单例（实测同实例） |
| 6 | 接线深度 | ✅ 真 wrap（非仅 import）；但 pairing 生产触发面 = 0（P1-3） |
| 7 | 降级诚实性 | ⚠️ 检查器自身 catch 有 `log.warn/log.debug`（铁律 24 字面合规）；但**系统的**降级信号失真（P1-2） |
| 8 | 测试契约（铁律 47） | ✅ 三个伴生 + registry 均有输入/输出/降级/错误 JSDoc |
| 9 | 测试覆盖（铁律 48） | ✅ 17 用例含 expect；⚠️ 2 处为替身（P1-1） |
| 10 | 文件驱动 | ➖ 不适用（非扩展点） |
| 11 | 自报告交叉对比 | ⚠️ 三处 overclaim（5xx 语义 / 真实 fatal Phase / 零 as unknown as 已在返工中自纠） |
| 12 | dev doc 偏离 | ✅ 写集 13/14 相符；方案与最终实现一致；偏离已如实登记（+5 行、前缀豁免、stash、D819 证据文件还原） |
| 13 | 控制塔执行审计 | ❌ P0-2：CI 红 + Vitest skip；本地 4 次软放行 |
| 14 | 版本编排合规 | ➖ 本卡无版本/tag 动作 |

---

## 十二 L3 执行审计（机器证据优先）

**CI（job 级，非 run 级；GitHub API 未认证可读）**

```
$ curl …/actions/runs?branch=feat/p2-invariants
35567327453 200869e5 completed failure push 2026-09-21T06:10:39Z   ← 审计 tip
35566590602 6ceea07a completed failure push 2026-09-21T05:59:43Z

jobs(run 35567327453):
  Test-Kit Architecture Tests (ubuntu/windows)  success
  TypeScript + Lint + Iron Laws                 FAILURE  ← step 8 "Iron laws check" failure
  Control Tower Gate Tests (ubuntu/windows)     success
  Architecture Check / Checker Review / Integration Contract / npm audit  success
  Vitest (${{ matrix.shard }})                  SKIPPED   ← 回归在 CI 上从未执行
  Golden Case F1 Gate                           SKIPPED

main 基线对照：74356ba6 → run 35570154966 conclusion=success
```

**K3 本地复现（同一门禁脚本，注入 CI 语义 `SYNO_CI=1`）**

```
$ SYNO_CI=1 bash scripts/pre-commit-check.sh      （在审计 worktree，已暂存本分支 14 文件）
  ❌ D734 PR 预算超限 (拆 PR，禁调高上限——见 scripts/control-tower/check-pr-budget.sh): 16 处  [CI strict——软提示在 CI 上为硬阻断]
     ❌ ① 变更文件数 14 > 上限 12 —— 拆 PR（禁调高上限）
     ❌ ② 变更跨域 —— 一个 PR 只许一个域（D733 ownership.yaml）
  ❌ 1 组未通过 — 提交已拒绝          EXIT=1

$ bash scripts/control-tower/check-pr-budget.sh --base origin/main
  ❌ ① 变更文件数 14 > 上限 12
  ❌ ② 变更跨域 → mac: D831-自验与交付文档 / src/sentinel/sentinel-events.ts / tests/…；win: src/invariants/** / src/deploy/bootstrap.ts / src/server.ts
  ✅ ③ 落后 origin/main 0 ≤ 20
（main 基线同命令：✅ PASS 0 文件）
```

**本地门禁台账（软提示设计，非绕过）**

```
.claude/pre-commit-failures.log: 2026-09-21T05:58:21Z / 05:58:45Z / 05:58:51Z / 06:09:47Z exit=1 branch=feat/p2-invariants
（对应 4 次 GATE_FAIL_SOFT 后 COMMITTED —— D515「本地软提示 + CI 权威」按设计工作）
```

**判据**：CI 权威链**确实在工作**（它红了），但**交付时 CI 未绿**、且 **vitest shard 因前置 job 失败被 skip → 回归正确性只有本地证据**。同时 D734「单域」规则与 D773「win 线 Mac 代行」**结构性冲突**（Mac 改 win 路径 + mac 域流程件 → 必然跨域），本卡是无豁免口径下的必然红（见 L4）。

---

## 十三 L2 偏离审计（声称 vs 证据）

| 声称 | 证据 | 判定 |
|---|---|---|
| 「17 用例全绿」 | K3 复跑 17 passed | ✅ 属实 |
| 「回归 1376 passed / 5 failed，全部 expert-file-loader，main 同样失败」 | 复跑 1378 passed / 5 failed；失败集与 main 逐条相同 | ✅ 结论属实（数字 P2-1） |
| 「tsc 我方文件零新增错误」 | 未独立复跑全量 tsc（成本高）；本卡 diff 内零 `as any`/`as never`/`as unknown as` | ⚠️ 未复核（不据此定论） |
| 「HTTP 层 5xx 语义」 | PROBE-A/B：生产路径 200 | ❌ **不实**（P0-1） |
| 「registered==0 → 真实 Bootstrap fatal Phase」 | PROBE-D 证真生产代码；但交付用例是替身 | ⚠️ 结论对、**证据表述 overclaim**（P1-1） |
| 「探针路由已补自动化测试（2 用例）」 | `tests/sentinel/invariants.test.ts:390-451` 真 `app.listen(0)` + 原生 fetch，503/200 双断言 | ✅ 属实 |
| 「伴生 install 不得返回 Promise（类型上限制 void）」 | 接口 `install: (ctx) => void` + 编译期断言 | ✅ 属实（自证式，P2-5） |
| 写集「未触碰 scripts/** 等」 | grep 零命中 | ✅ 属实 |
| 「stash 零损失」 | 三文件内容逐字节一致 | ✅ 属实（时间点不可证） |

---

## 十四 跑偏第二道（北星三问，对照 `.claude/PRODUCT-BRIEF.md` 原文）

| 问 | 判据（PRODUCT-BRIEF 行号） | 结论 |
|---|---|---|
| ① 服务真实用户场景（GA/企业主）？ | §二 L20-21；§一 L14「它主动替企业干活」 | **对齐（间接）**：防的是"工具能力在 provider 边界静默丢失"——工具链路断 = GA 拿到假分析。**但价值兑现打折**：违约只降级为 200 文案（P0-1），未达「不可能静默复发」 |
| ② 让模块更接近终态？ | §四 L40-43（五层/文件优先/无限扩展）；§六 L66-68（P0 = 哨兵真实数据流、报告质量验证） | **不跑偏，但不推进 P0 用户价值**：属信任层基建（前提清单 P-2）。若长期只做基建不碰 §六 P0，则会退化为横向加东西——建议 CTO 在前提清单收口后回归产品线 |
| ③ 做完后还是「驻扎企业、主动诊断、为增长服务」的 Agent 吗？ | §一 L10-16 | **是**：不变更产品身份，无 ChatBot 化/报表工具化迹象 |

**裁定：无方向跑偏（记录备查）。** 唯一与产品价值相关的问题是 P0-1：机制对外承诺"违约必炸"，实际"违约变降级"，**给人虚假安全感**——恰是总纲 §9.4「notChecked 显式化」要防的反面，却在机制自身最核心的语义上发生。

---

## 十五 L4 防线缺口收割（本该拦住它的防线 → 为什么没拦住）

| 缺口 | 本该拦住的防线 | 为什么没拦住 | 补法（一类一机制，收敛） |
|---|---|---|---|
| **G1**：验收点「5xx」不可达（SSE 建流后不改状态码），写进卡时无人核 | dev doc / brief 的「接口审计 + 验收点可证伪性」；pre-commit 组 6「Done 可证伪性」 | 组 6 只查 **Done 标准**字段可证伪，**不查 acceptance_points**；且「可证伪」被当成"能被测试断言"，未要求"能被真实链路达到" | 强化 **M1**（fail-open 静默失效）已有防线：验收点写 HTTP 状态码类判据时，必须附「该状态码由哪条路由/哪一行决定」的 file:line（brief Q3 增一栏） |
| **G2**：实现宣称 fail-closed 但下游韧性层吞错 | 铁律 24/31 的"降级显式"、自检 5 问 2「每个 catch 有 log + degraded」 | 自检只逐 catch 看**本文件**，不看**调用链上游是否重新抛**；`callWithResilience` 的「永不抛」是**文档化契约**，反而被当成优点 | 强化 **M1**：新增"fail-closed 断言"必须给**穿真实入口的失败态断言**（HTTP 状态码/进程退出码），直调函数 reject 不算 |
| **G3**：CI 红 + Vitest skip 未被当交付条件 | 「CI 权威」（D467/D515 链）+ pre-push | CI 确实红了，但**没有任何环节把"tip 的 CI 结论"作为交付必附证据**；且 Iron Laws 失败会 skip 后续 job，vitest 从未跑——红=红，但没人读 job 级 | 强化 **M4**（执行证据链）：交付回执必须附 **job 级** CI 结论（禁 run 级整体结论；D320 同型教训） |
| **G4**：D734 单域/12 文件预算 vs D773 win→Mac 代行 + 新建子系统 | D734 PR 预算门禁 | 分类器按 ownership.yaml 把「win 路径改动 + mac 流程件/测试」判为跨域 → **合规任务必然红**；且流程件（task brief / bypass.log / task-state / 交付文档）计入 12 文件上限 | **control-tower 计数口径修正**（非新 M 类）：流程件排除出"变更文件数"；跨域判定对 D773 代行任务按"主域"归并 |
| **G5**：跨层 import 的门禁盲区 | `check-architecture.sh` L3→L5 规则 | 该规则只 `grep src/l3/`（`scripts/check-architecture.sh:184`），`src/invariants`、`src/sentinel` 等 L3 声明模块**结构上不在扫描面** | 强化既有 L1→L3/L4/L5 的「按层声明表」机制：把声明层做成文件头/白名单表，扫描面由声明生成（而非硬编码 `src/l3/`） |
| **G6**：不变量可反向改写业务状态（本次第 3 条） | 无（铁律未定义"不变量允许的写入面"） | 现有铁律只管 catch/降级/类型，**不限制"守卫是否可以改状态"** | 建议在注册表契约层加**能力面约束**（伴生只能 `fail/hit/onRollback`，不得持有 store 写权限）——属 implement/devdoc 预设，一处改，全类防 |

**M 模式映射（先查 M1-M8，避免新增类）**：
- P0-1 + P1-1 → **M1（fail-open 静默失效）** + **M2（声称 vs 事实）**，强化既有防线（G1/G2）。
- P0-2 → **M4（执行证据链断裂）**，强化 job 级回执（G3）+ 控制塔计数口径（G4）。
- P1-2（降级信号失真）→ **M1 变体（false-degraded）**，先按 M1 强化；若第三次复现再立 M9。
- P1-3（探针 hitCount 不证明触发面）→ **M3（机制建成未接线）** 的"邻域"：**接线 ≠ 触发**，建议并入 M3 判据（"生产调用点真实传递"补一句"且真实流量下计数 > 0"）。
- P2-4 → **控制塔缺口（G5）**，不改 M 表。
- **未新增 M 类**（遵守"一类一机制、禁每错加脚本"）。

---

## 十六 已知边界裁定（不得阻断，可登记）

| 边界 | 自述 | K3 裁定 |
|---|---|---|
| INV-TOOL-SCHEMA-SENT 的**前缀豁免**（已带过 tools 的对话放行收尾轮） | 遗留 #1，建议小队 A 用 `tool_choice:'none'` 收紧 | **可登记不阻断**：豁免已被 `notChecked #2` 显式声明（总纲 §9.4 合规），且 C2 收尾轮是既有行为 |
| **探针未加认证**（与 `/api/healthz` 同口径） | 遗留 #5 | **可登记不阻断**：与既有健康面口径一致；若 healthz 后续加门需同步 |
| **Windows 实机未验**（win 域 Mac 代行） | 遗留 #6；卡 acceptance_level 已挂账 | **可登记不阻断**：本审计全部结论为 mac/代码级；Win 实机行为按 D773 挂账 |

---

## 十七 环境注记

- 审计机 macOS / Node **v22.22.0**，`better-sqlite3` ABI **127** 原生可用（非 D316 型 ABI 假失败）。
- 审计 worktree 通过 symlink 复用主 clone 的 `node_modules`（产品仓库**跟踪文件零改动**；`git status` 仅新增未跟踪 `node_modules` symlink）。
- CI 证据来自 GitHub API 未认证读取（本机无 `gh`）；job 日志端点需认证（403），故 CI 失败**原因**用本地 `SYNO_CI=1` 复现补证，非读 CI 日志。
- 三次复跑差异（并行 → 串行）已按 D316 口径归为环境依赖抖动，结论以**串行**为准。

---

## 十八 复审范围建议（FIX 后）

1. **P0-1**（必做）：要么让违约真的到 5xx（如 `callWithResilience` 对 `InvariantError` 不吞、路由在可改状态码的时点映射），要么**改判据**为物理可达形态（例：流内 error 帧 + 探针 lastFailure + 进程退出策略），并在卡/brief 同步。两者都必须附**穿真实入口**的失败态断言。
2. **P0-2**：CI 绿（job 级，含 Vitest 非 skip）或 CTO 对 D734 口径作出书面裁决。
3. **P1-1**：`registered==0` 用例改为真 `execute` + 桩 `installAll`（K3 PROBE-D 已给可行做法）。
4. **P1-2**：评估 `lastDegraded` 粘滞的消费方（`conversations.ts:260`）→ 误报降级是否可接受。
5. **P1-3**：给反接线探针补"生产触发面"判据或显式降级声明。

---

## 附：证据文件

- K3 独立探针（不入仓库）：`tmp/k3-d831-probe/{vitest.config.ts,prodpath.probe.test.ts}`
- 原始日志：`/tmp/k3-d831-probe.log`（A-E 探针）、`/tmp/k3-d831-branch-regression2.log`、`/tmp/k3-d831-baseline-regression2.log`、`/tmp/k3-d831-precommit-strict.log`
- 复现命令（均在审计 worktree，产品仓库只读）：
  - `npx vitest run tests/sentinel/invariants.test.ts`
  - `npx vitest run tests/sentinel tests/agent`（分支 / 基线各一次，**串行**）
  - `SYNO_CI=1 bash scripts/pre-commit-check.sh`
  - `bash scripts/control-tower/check-pr-budget.sh --base origin/main`
  - `git log -1 --format='%H %P %ci %s' e074c547`（stash 取证）
