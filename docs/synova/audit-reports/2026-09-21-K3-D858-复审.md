# K3 独立复审 · D858（CT-70/D854 条件闭环）

> 审计员：Kimi K3（独立会话，零上下文，不采信交付方自述）
> 对象：PR #687 ｜ 分支 `gate/d858-canary-glob` ｜ head `3478111e` ｜ 冻结件 `44b49864` ｜ 基线 `origin/main cc4e4166`
> 运行环境：macOS / bash 3.2 / BSD awk（CI 证据经匿名 GitHub API 独立获取）
> 上游：K3 报告 `2026-09-20-K3-D854.md`（CONDITIONAL PASS，四条条件）

## 结论：**通过（PASS）** — D854 四条条件逐条物理闭环；PR #687 可合并

最小跟进条件（不阻断 #687 合并，阻断 #682 合并口径见 §四）：
1. **#682 合并前置**：#682 须 rebase（当前 mergeable=dirty），且其合并应与 **D862（ci.yml `|| true` 移除）同批或之后**——否则 L4-1 的 exit 1 在 CI 层仍被吞，只余 `::error` 注解。
2. task-state/D858.json `impl` 字段停留在 T6/V3 口径（sha `574d4465`/`cff8c1dc`、65 通过）——与最终 T8/V4（`ec7f49aa`/`addd1d69`、70 通过）不符，属文档滞后（P2，随本卡合并前顺手更新）。

## 一、冻结件哈希（无漂移）

| 件 | 声称 sha256 | 实测（`git show 3478111e:`） | 行数 |
|---|---|---|---|
| scripts/control-tower/check-canary-drift.sh | `ec7f49aa…5871e` | **一致** | 281 |
| tests/control-tower/check-canary-drift.test.sh | `addd1d69…565743` | **一致** | 339 |

冻结提交 `44b49864` 之后至 head `3478111e` 仅 docs/memory/task-state（`git diff --name-only` 物理核验），**代码零漂移**。分支全量 diff 未触碰 `scripts/audit/**`（审计红线核验通过）。

## 二、四条条件逐条物理验证（K3 自建夹具，非交付方夹具）

### ① P1 覆盖语义物理展开 — **闭环**
- 我自建夹具（/tmp，非交付方）：glob 执行行 + 字面行 → exit 0；**删除 glob 执行行、仅留注释声明** → **exit 1** + `::error title=canary-fake-coverage`（点名 3 个物理文件）；再删目标文件（glob 零命中）→ **仍 exit 1**（"glob 零命中"路径）。物理证据：check-canary-drift.sh:136-151（`_expand_glob` nullglob）、:154-164（LISTED=字面+物理命中）、:190-207（S2 假覆盖判定）、:278-280（exit 1）。
- 真实回归：main ci.yml → `729 | 清单 41 | 漂移 71 | exit 0`，stdout sha256 `b12210c408fb…` 与 D854 基线**逐字一致**（K3 本机实测）。PR #682 ci.yml 预演 → `729 | 清单 44 | exit 0`，stdout sha256 `78b5a299cc41…` MATCH（提请件口径 44=41+3 物理展开属实）。
- 配对测试 70 通过 0 失败（K3 本机复跑）。

### ② P2 告警绿腿可见 — **闭环**
- 匿名 API（无 token）取 run `35529862520`（head `5fe68972` = 冻结件+docs，V4 代码生效）：全部 12 job success，含 CT Gate windows `106128436902` + ubuntu `106128436921`。
- 双腿 check-runs annotations 匿名取到 `warning | canary-drift | 71 个测试不在 CI canary 清单: tests/control-tower/attach.test.sh…` ——绿腿告警正文可见，属实。
- 本机物理验证独立通道：stdout → /dev/null + `GITHUB_STEP_SUMMARY` → exit 1 且摘要含漂移+假覆盖正文（check-canary-drift.sh:240-275）。

### ③ L4-1 注释声明 > 物理覆盖报红 — **闭环**
- 我自建夹具：整行注释声明 `tests/b.test.sh`（文件存在、未被物理覆盖）→ **exit 1**，FAKE 只点名该条（check-canary-drift.sh:230-235）。夹具 B/B2 见 ①。

### ④ L4-3 匿名取 job 证据通道 — **闭环（本次审计即用该通道完成 ②）**
- `commits/<sha>/check-runs` → `check-runs/<id>/annotations` 匿名可用（60/h/IP），标准件成文于 `docs/synova/coordination/D858-证据包标准件-匿名取CI证据-20260921.md`。K3 全程零 token 复核成功。

## 三、指定裁决项

**(a) 重生成/回读是否被当复现** — 否。①③④均由 K3 自建夹具 + 物理变更（删行/删文件）独立复现，非重跑交付方断言；CI 证据经匿名 API 二次独立获取。本卡验收没有 grep 型静态判据充当复现的情况。

**(b) 已知边界三项是否构成未闭环** — 均不构成，判定"可登记"：
1. `$(...)#` 残余误红：fail-closed 方向（只会多红不会假绿），且截断内容仍进 DECLARED 受物理覆盖校验（代码 :100-107 词边界集 + :190-207 复核链，K3 读码确认）。真实 main/#682 ci.yml 命中 0。**登记，不阻断**。
2. 文本级固有边界（字符串内路径算覆盖）：P1 条件原文是"不得由注释承载"，已满足；不执行 ci.yml 语义属静态检查固有边界且已在脚本头注声明。**登记，不阻断**。
3. `**` 递归 glob 未启用：脚本 :136-139 显式声明（macOS bash 3.2 无 globstar，启用反而分叉）。当前真实形态为单层。**登记，不阻断**。
4. （追加）**ci.yml:277 `|| true` 吞 exit 1/2**：K3 物理确认存在（`.github/workflows/ci.yml:277`）——脚本侧 fail-closed 机制已就绪，但 CI 层传导被吞，仅 `::error` 注解可见。写集硬边界禁碰 ci.yml（#682/#657 在飞）合理，已登记 CT-78 + D862。**不阻断 #687；约束 #682 合并时序（见 §四）**。

**(c) 自曝 CI windows 真红与 V4 修复可信度** — 属实且可信。匿名 API 核实 run `35528922575`：windows CT Gate job `106125920439` = **failure**，其余 11 job success——与自曝完全一致。V4（`SYNO_AWK_BIN` 注入缝）K3 本机实测：空串与 `/nonexistent` → `exit 2` + `::error title=canary-exec`（check-canary-drift.sh:114-130），未设置时生产默认不变。自验员主动标注"V3 恰 1 失败复现不出、不引用条数"是诚实口径，予以采纳。"本地绿≠CI 绿"的机制缺口（软提示无物理门禁）已有 CI 双腿真实 runner 兜底 + 台账 CT-80，判定可登记。

**(d) main 主线 Vitest (2/2) 红** — **存量，非 D858 引入**。D858 全量 diff（cc4e4166..3478111e）零 TS/src 文件；匿名核验基线 cc4e4166 check-runs：Vitest (2/2) failure、(1/2) cancelled（与台账 CT-81 相符）；而 D858 分支 run 35528922575 / 35529862520 上 Vitest (2/2) 均 success。归 CT-81 主线轨道处理。

## 四、合并裁决与最小条件

| PR | 裁决 | 条件 |
|---|---|---|
| **#687（本卡）** | **可合并** | 合并前顺手更新 task-state/D858.json 的 V3 滞后口径（P2）；D862 保持排期不落空 |
| #682 | **暂缓** | ① owner rebase（mergeable=dirty）② 与 D862（去 `|| true`）同批或其后合并，使 L4-1 exit 1 真正传导 |

## L4 防线缺口

- **"本地绿 ≠ CI 绿"无本地物理门禁**（D520/CT-73 → D858-T7/CT-80 第二次同族）：本应拦住的是"跨平台确定性预检"，现状靠 CI 双腿事后抓。建议归 CT-80 单一机制收敛（跨平台注入缝模式已成文于 ctrl-tower-change 模式库），不加新脚本。归因：control-tower。
- **`|| true` 吞退出码类"门禁接了但不传导"**：本应拦住的是 ci.yml 改动审查（CT-70 时已知）。D862 闭环。归因：implement（ci.yml 域）。
- task-state `impl` 字段版本滞后：状态卡应随冻结件同步。归因：devdoc（收口流程）。

## 证据索引（K3 独立复现命令）

```
bash tests/control-tower/check-canary-drift.test.sh                  # 70/0
bash scripts/control-tower/check-canary-drift.sh                      # 729|41|71|exit0, sha b12210c4…
# 自建夹具 B：删 glob 行留注释 → exit 1 + ::error canary-fake-coverage
# SYNO_AWK_BIN="" → exit 2 + ::error canary-exec
curl api.github.com/.../runs/35529862520/jobs                         # 12 job success
curl api.github.com/.../check-runs/106128436921/annotations           # canary-drift 正文（匿名）
curl api.github.com/.../runs/35528922575/jobs                         # windows 106125920439 failure（自曝真红属实）
```
