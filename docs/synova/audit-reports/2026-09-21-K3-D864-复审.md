# K3 复审报告 — D864（回应 D862 原判 5 项条件）

> 审计员：Kimi K3（独立会话，零上下文，物理验证优先）｜ 出件 2026-09-21
> 对象：伞分支 `feat/p1-outbound-converge` @ **444b2f55**（CTO 已复算回执一致）
> 6 支域分支：`pr1` 47d82edb ｜ `pr2` 6f101df0 ｜ `pr3` 415e6ee6 ｜ `pr4` cd56ac67 ｜ `pr5` 30b3c9c2 ｜ `pr6` 6e2abeb3
> 原判：`docs/synova/audit-reports/2026-09-21-K3-D862.md`（CONDITIONAL PASS，1 P1 + 8 P2 + 3 挂账）
> 上游基线：`origin/main` = 3643db6a ｜ D821 分支 tip = 717d67a1（**仍未合 main**）

## 三态判定

**CONDITIONAL PASS（维持）** —— 5 项条件中 **3 项完全闭环**（P2-a / P2-b / 红线）、**1 项部分闭环**（P1-1：代码面闭环、PR 增量判据未在现状态复现）、**1 项部分成立**（P2-c：合并顺序可执行属实，唯一交集口径不成立）。**无 P0、无新增回归**；1 项 P1-2（PR 增量现状态超 12）。

| 条件 | 声称 | K3 独立复算 | 判定 |
|---|---|---|---|
| ① P1-1 pr5..pr6 = 12 文件 / src 重叠 0 / 各支 ≤12 | ✅ | pr5..pr6 **= 12、src 重叠 = 0** ✓；各支 ≤12 **仅在"剔除 D821 载荷 + main 同步文档"的口径下成立**；**PR 语义（三-dot）现状态 16–30 文件** | ⚠ **部分闭环** |
| ② P2-a backup-scheduler 第 3 条 loopback + 扩口径 37 行 + 8 族全零 + 基准命令入库 | ✅ | **逐项复现成立**（37 = 26 fetch + 11 非 fetch；import 面 0 命中；§7.1 含 3 条基准命令原样） | ✅ **闭环** |
| ③ P2-b healthz 零回显 + 整包断言 + 反例红→绿 | ✅ | **逐项复现成立**（固定文案；`objectContaining` + `JSON.stringify not.toContain`；K3 自拟反例红→绿） | ✅ **闭环** |
| ④ P2-c 15 对唯一交集 + 八步合并顺序可实建模拟 | ✅ | 15 对实测**唯一交集不成立**（实际共享 10–16 文件）；**八步合并顺序实建模拟通过且落完等于终树** ✓ | ⚠ **部分成立** |
| ⑤ 回归双轴零差集 + 差集归因 | ✅ | **双轴与基线逐字相同（文件级 7/7、用例级 17/17）** ✓；1 项归因成立、2 组归因与实测不符 | ⚠ **部分成立** |
| ⑥ 无新增回归 | ✅ | **成立**（D864 只改 2 个文件，全量跑零新失败） | ✅ |
| ⑦ 红线 | ✅ | **7/7 成立**（http-exit diff 0 行 / G1=0 / 无 --no-verify / 无 force push / P-1 未误改 / 未碰控制塔·审计·CI） | ✅ |

---

## 一、条件①：P1-1 栈式文件数

### 1.1 `pr5..pr6` — **成立（逐字复核）**

```bash
$ git diff --name-only 30b3c9c2 6e2abeb3
.claude/bypass.log
.claude/reference-map.md
.claude/task-briefs/2026-09-21-D862-出站收敛-coderB.md
.claude/task-briefs/2026-09-21-D864-P2aP2b-healthz收口-coderB.md
.claude/task-briefs/D862-P1-outbound-converge-a.md
docs/synova/coordination/D862-PR合并顺序说明-20260921.md
docs/synova/coordination/D862-出站分类台账-20260921.md
docs/synova/coordination/D862-自验记录-20260921.md
docs/synova/coordination/D864-自验记录-20260921.md
docs/synova/coordination/写集勘误附注-派单小队A-P1收尾-20260921.md
task-state/D862.json
task-state/D864.json
文件数 = 12 ｜ src/ 文件数 = 0
```

→ **12 文件、`src/**` 重叠 = 0**，与声称一致 ✅
→ 6 个 pr5 源码/测试文件在两侧 **blob 全同**（逐条 `git rev-parse`：`healthz.ts ac524b14` / `llm-config.ts 81008c2c` / `chat.tsx be3a7504` / `ontology-probe.ts 1a70bb63` / `healthz.test.ts c3e4f60a` / `ontology-probe.test.ts e1faa775`）→ 无漂移 ✅

### 1.2 「各支 ≤12」— **现状态不成立（口径缺一前提）**

对照三套可执行口径（`git` 实跑，非推算）：

| 分支 | ① 剔除 D821 载荷（文档口径） | ② 现状态 PR 语义（`main…branch` 三-dot） | ③ 两树直差（`main` 对 `branch`） |
|---|---|---|---|
| pr1-connectors | 4（D860 口径 3） | **16** | 16 |
| pr2-tools | 9（8） | **21** | 21 |
| pr3-agent | 8（7） | **20** | 20 |
| pr4-services-deploy | 7（6） | **18** | 18 |
| pr5-routes-tui | 8（7） | **19** | 19 |
| pr6-docs-state | **12**（10） | **30** | 30 |

**口径①是什么**：把各支的改动集减去 `git diff --name-only origin/main...origin/feat/d821-http-proxy` = 14 文件（D821 载荷），得到"本域增量"。该数在**每支自身的 diff vs 其自报 base**上可复现（口径① 与我 §一 的 `pr5..pr6` 12 一致，但 ①④⑤ 三项比自验记录多 1 —— 见 §1.3）。

**口径②为何不是 12**：`origin/main` 当前 tip = 3643db6a（D821 **未合**），六支的 merge-base 与 main 相同（= 3643db6a），故 `main…branch` 把 **D821 载荷 12 文件 + main 同步文档/卡 9–14 文件**一并计入。这不是"本卡超预算"，但**是 PR 门禁实际会看到的数**。

**独立实跑门禁**（用 PR 的真实 diff 喂 `check-pr-budget.sh`）：

```bash
# 在 717d67a1 工作树上对每支做 dry-run merge，取实际落地 diff，喂门禁：
pr1-connectors         files=13  ❌ FAIL PR 超预算
pr2-tools              files=18  ❌ FAIL PR 超预算
pr3-agent              files=17  ❌ FAIL PR 超预算
pr4-services-deploy    files=15  ❌ FAIL PR 超预算
pr5-routes-tui         files=16  ❌ FAIL PR 超预算
pr6-docs-state         files=26  ❌ FAIL PR 超预算
```

**但——把"必须发生的前置操作"执行一遍后，门禁全绿**：在 `origin/main` 工作树上先合 D821（冲突项 `task-state/D821.json` 取 main 侧）再逐支合并，**每支实际施加的 diff** 恰为文档口径值，逐支喂门禁：

```
2-pr1  剔除 bypass.log  3 文件  ✅ PASS PR 预算内（3 文件）| 变更单域 win
3-pr2  剔除 bypass.log  8 文件  ✅ PASS（8 文件）| 单域 win
4-pr3  剔除 bypass.log  7 文件  ✅ PASS（7 文件）| 单域 win
5-pr4  剔除 bypass.log  5 文件  ✅ PASS（5 文件）| 单域 win
6-pr5  剔除 bypass.log  6 文件  ✅ PASS（6 文件）| 单域 win
7-pr6  剔除 bypass.log 11 文件  ✅ ① 11 ≤ 12 ／ ❌ ② 变更跨域 —— 见 §1.4
```

→ **口径①的可达性经实跑证实**；**口径①在"不先合 D821"的现状态不可达**。

### 1.3 与自验记录的口径差（§1.2 表的 ①②）

自验记录称 `pr1=3、pr2=8、pr3=7、pr4=5、pr5=6`，我复算 = `4/9/8/7/8`（**各多 1**）；其"终值复核"已自曝归因为"同步基准对 `task-state/D821.json` 冲突的解法选择"——独立核对：该文件在三方合并中确有内容冲突（`main=d31db17e` vs `d821=78556b73` vs base=`a151712c`），解成 d821 侧时该文件进入差集、解成 main 侧则不进入 → **+1 的来源可解释**，且该文件是任务状态台账非代码。**判：可解释、不影响 ≤12 结论**；但两套数字并存，宜在台账固定唯一口径（P2-3）。

### 1.4 ⚠ P1-2（新）：pr6 在真实门禁下 `② 变更跨域` 仍 FAIL

pr6 的内部增量（11 文件，剔除 `bypass.log` 后）过 ①（11 ≤ 12），但 **② 域判定 FAIL**：

```
❌ ② 变更跨域 —— 一个 PR 只许一个域（D733 ownership.yaml）
     win  .claude/reference-map.md
     mac  docs/synova/coordination/D862-PR合并顺序说明-20260921.md
     mac  docs/synova/coordination/D862-出站分类台账-20260921.md
     mac  docs/synova/coordination/D862-自验记录-20260921.md
     mac  docs/synova/coordination/D864-自验记录-20260921.md
     mac  docs/synova/coordination/写集勘误附注-派单小队A-P1收尾-20260921.md
```

**根因收敛到单文件**：`.claude/reference-map.md` 由 `scripts/workflow/grep-refs.sh:18` 生成（`REFMAP="$ROOT/.claude/reference-map.md"`，D538 grep 物理门禁产物），在 `ownership.yaml` 里归属 **win**；而 `docs/synova/coordination/**` 归属 mac、`.claude/task-briefs/**` 与 `task-state/**` 在 `domain_neutral` 豁免列（`ownership.yaml:185`）。把这些逐个剔除实测：

```
剔除 .claude/reference-map.md → ✅ ① 11 ≤ 12 ／ ✅ ② 变更单域: PASS 5 个文件同域: mac（无归属 0，域判定豁免 6）
```

→ **pr6 的 PR 门禁 FAIL 由"一个 hook 生成物落在 win 域"单点造成**。处置建议（不改代码，二选一）：① 把 `.claude/reference-map.md` 加入 `ownership.yaml` 的 `domain_neutral`（与 `.claude/bypass.log` 同类——同为门禁生成物、无产品语义）；② PR 声明 owner=win，域判定即通过（5 个 mac 文件都不是代码）。

---

## 二、条件②：P2-a 扩口径扫描 — **完全闭环**

### 2.1 独立复跑（未采信自述）

```bash
$ grep -rnE "\b(fetch\(|http\.request\(|https\.request\(|axios|node-fetch|got\(|undici|XMLHttpRequest|request\()" \
    src/ --include=*.ts --include=*.tsx | grep -v "\.test\." | grep -v "^src/providers/http-exit.ts" | grep -vE "outboundFetch\(|bridgeFetch\(" | wc -l
27
# 拆解：fetch( 族 = 26 ／ 非 fetch 族 = 1（src/deploy/backup-scheduler.ts:182）
$ grep -nE "\b(fetch\(|http\.request\(|https\.request\(|axios|node-fetch|got\(|undici|XMLHttpRequest|request\()" src/providers/http-exit.ts | wc -l
10   # 出口本体自身（注释 5 + 传输实现 5）
→ 27 + 10 = 37 行  ✅ 与台账 §7.2「37 = 26 fetch + 11 非 fetch」逐字一致
```

### 2.2 `backup-scheduler.ts:182` = 第 3 条 loopback 豁免 — **成立**

```typescript
src/deploy/backup-scheduler.ts:182-186
  const PORT = process.env.PORT || '3000';
  const req = http.request({
    hostname: 'localhost', port: parseInt(PORT, 10),
    path: '/api/notifications/send', method: 'POST', …
```

`hostname` 硬编码 `'localhost'` → loopback 语义，与 `mcp/tool-definitions.ts:292`（`httpBase()` 硬编码 localhost）、`deploy/watchdog-entry.ts:33`（默认 `http://localhost:3000/health`）同类。**该文件在 D862/D864 全分支 diff 中均空**（D821 存量）。台账 §7 已登记为豁免 C ✅

### 2.3 各族零命中 — **成立**

```bash
$ grep -rnE "from ['\"](axios|node-fetch|got|ky|undici|ws)['\"]" src/ --include=*.ts --include=*.tsx | wc -l
0
$ grep -rn "new WebSocket(" src/ --include=*.ts --include=*.tsx | wc -l
0
# 字面出现点上下文（全部为注释，非依赖）：
src/providers/http-exit.ts:58   * - 不实现 SOCKS5 协议；不引 undici / node-fetch / axios。
src/providers/http-exit.ts:15,151  （undici 解析顺序对齐说明，注释）
src/providers/http-exit.ts:676  （`net.connect` 用于本机私有目标 lookup:false 说明，注释）
XMLHttpRequest → 0 命中
$ grep -rnE "from 'http'|from 'node:http'|from 'node:net'" src/ … | grep -v "import type"
src/deploy/backup-scheduler.ts:16:import * as http from 'http';   ← 运行时 import 唯一一处（即 §2.2）
```

### 2.4 基准命令入库 — **成立**

台账 `docs/synova/coordination/D862-出站分类台账-20260921.md` §7.1 含 **3 条**基准命令（多 API 面 / 防漏补充面 / 运行时 import 面）+ §7.2 权威命中与零命中族清单。原样复跑 §7.1 三条：

```
基准命令 1 → 37 行（与 §7.2 一致）
防漏口径 2 → 0 行（与台账 → 0 命中 一致）
口径 3     → 运行时 import 仅 backup-scheduler，余为 import type（与台账一致）
```

**条件② 判定：✅ 完全闭环**（复现、登记、口径入库三面齐备）。

---

## 三、条件③：P2-b healthz 零回显 + 整包断言 + 红→绿 — **完全闭环**

### 3.1 catch 零回显（`src/routes/healthz.ts:334-344`）

```typescript
} catch (err: unknown) {
  const errName = err instanceof Error ? err.name : String(err);
  const errCode = err !== null && typeof err === 'object' && 'code' in err
    ? String((err as { code: unknown }).code)
    : '';
  log.error({ check: 'outbound_proxy', errName, errCode }, '出站代理状态检查异常');
  return { status: 'degraded', detail: '出站代理状态检查异常（详见服务端日志）' };
}
```

- detail = **固定文案**，不含任何异常内容 ✅
- 日志 = 结构化 `{ check, errName, errCode }`，message 原文不再进入 ✅
- 铁律 24 保持：`log.error` + `status: 'degraded'`，零空吞 ✅
- `as { code: unknown }` 是内联窄化（铁律 38 允许写法，非 `as any`/`as never`/`as unknown as`）✅

### 3.2 整包断言升级（`tests/routes/healthz.test.ts`）

三路径全部改为整包断言：

```typescript
expect(body.checks).toEqual(expect.objectContaining({ outbound_proxy: { status: 'ok', detail: '未设置代理环境变量（全量直连）；已配置变量: 无' } }));
const whole = JSON.stringify(body);
expect(whole).not.toContain('super-secret-pass'); expect(whole).not.toContain('3128'); expect(whole).not.toContain('user:');
```

→ 覆盖范围从 `detail` 子串升级为**整包 JSON 序列化**，正对我 D862 P2-5 指出的缺口 ✅ 并新增反例用例（`mock getProxyStatus` 抛含凭据消息 → 断言 `toEqual({status:'degraded', detail:'出站代理状态检查异常（详见服务端日志）'})` + 4 个片段零命中）。

### 3.3 独立实跑 + **K3 自拟反例红→绿**（与交付方用例独立设计）

交付方文件独立实跑：`tests/routes/healthz.test.ts` → **11 passed (11)** ✅

K3 自拟反例（**设计差异**：抛**非 Error 对象**，其 `String()` 携带凭据 → 直接打 `String(err)` 这条分支），在两个 commit 树上跑同一份测试：

```
RED 树（9aaca76e^ = 48aa8709）:
  × 整包 JSON 不含凭据/主机/端口片段，且 outbound_proxy 仍 degraded 可见
  AssertionError: 整包不得含这些片段: k3-top-secret-9f3a,k3-user,user:,10.7.7.7,3128,boom
    body={…"outbound_proxy":{"status":"degraded",
          "detail":"出站代理状态检查异常: boom http://k3-user:k3-top-secret-9f3a@10.7.7.7:3128"}…}
  Test Files 1 failed (1) | Tests 1 failed (1)

GREEN 树（9aaca76e）:
  ✓ 整包 JSON 不含凭据/主机/端口片段，且 outbound_proxy 仍 degraded 可见
  Test Files 1 passed (1) | Tests 1 passed (1)
```

→ **修前必红（凭据原样回显）+ 修后必绿**，物理复现成立 ✅
（反例测试文件属 K3 探针，**未写入产品仓**；红/绿两棵树为临时 worktree，审计后拆除）

**条件③ 判定：✅ 完全闭环。**

---

## 四、条件④：P2-c 唯一交集 + 八步合并顺序

### 4.1 15 对交集 — **声称不成立（实测）**

**现状态**（改集 = 各支 vs 我复核用的公共基线）15 对 `comm -12` 实测：

```
set1∩set2 = 10   ┐ .claude/bypass.log + D821 载荷 12 中的 6 项
set1..set5 两两 = 10 │ （docs/synova/audit-reports/2026-09-21-D821.md、
…                    │  3 份派单件、task-state/D821·D860·D861·D862·D863）
set5∩set6 = 16     ┘ 以上 10 + 6 个 pr5 源码/测试文件
（D864 各支 vs D821 基线：set1∩set2 = 13；vs main 派生基线：10；含 pr5/pr6 时 16–19）
```

→ **"唯一交集 = `.claude/bypass.log`"仅在人工构造的"已减除 D821 载荷"口径下成立**；在真实分支状态里，10–13 个文件（D821 载荷 + main 同步文档/卡）被各支共享。

**为什么这不影响合并**：这些共享项在每支上的 **blob 与 main/D821 侧一致**（不是我 D862 审计中的"漂移"），故三方合并自动解为同一结果 → 不产生冲突。**这是实测结论，见 §4.2。**

### 4.2 八步合并顺序 — **可实建模拟，通过，落完 = 终树** ✅

在 `origin/main`（3643db6a）新建工作树，按文档 §二 顺序逐步 `git merge`：

```
1-d821      rc=1  ⚠ 冲突 task-state/D821.json（1 处，见 §4.3）
2-pr1       rc=0  files=4   conflicts=[]
3-pr2       rc=0  files=9   conflicts=[]
4-pr3       rc=0  files=8   conflicts=[]
5-pr4       rc=0  files=6   conflicts=[]
6-pr5       rc=0  files=7   conflicts=[]
7-pr6       rc=0  files=12  conflicts=[]
8-umbrella  rc=0  files=1 (.claude/bypass.log)
→ git diff --name-status HEAD 444b2f55   →  仅 M .claude/bypass.log（其余 36 文件全同，差异文件数 = 1）
```

**判**：① 步骤 2–8 **零冲突**，且**落完即等于伞分支终树**（唯一差异是 hook 台账 `bypass.log`，无产品语义）；② 步骤 8（伞分支）对已落地的完整栈只贡献 1 个文件（again `bypass.log`）→ 与文档"伞分支仅作对照，不单独合并"一致；③ 步骤 1 的冲突见 §4.3（**存量，非本卡引入**）。

### 4.3 ⚠ 八步顺序第 1 步的真实冲突（P2-2）

```
$ git merge 717d67a1   # 在 origin/main 上
Auto-merging .claude/bypass.log
Auto-merging task-state/D821.json
CONFLICT (content): Merge conflict in task-state/D821.json

三方 blob：main=d31db17e  d821=78556b73  base(cc4e4166)=a151712c
解法：git checkout --ours task-state/D821.json（取 main 侧）→ 落地后该文件与 main 版逐字一致
```

**性质**：main 侧已在 D821 未合入前先更新了 D821 的卡（K3 审计 PASS 回填，commit 41c4d201），D821 分支侧也更新了同一文件 → 任何"先合 D821"的路径都会在此冲突一次。**1 处、台账文件、取 main 侧即解，不阻断**，但文档 §二 的"按此序合并（零冲突）"表述对**第 1 步**不成立，宜补一句"第 1 步 `task-state/D821.json` 取 main 侧"。归因 devdoc。

**条件④ 判定：⚠ 部分成立**——合并顺序**可执行且落完等于终树**（核心诉求达成），但"唯一交集"口径与"八步零冲突"表述各有一处不实。

---

## 五、条件⑤/⑥：回归双轴 + 无新增回归

### 5.1 全量顺序跑（`--no-file-parallelism`，消除并发假失败）

```bash
# D864 伞树 444b2f55
Test Files  7 failed | 607 passed | 3 skipped (617)
Tests      17 failed | 4534 passed | 24 skipped (4575)

# 基线 origin/main 3643db6a
Test Files  7 failed | 600 passed | 3 skipped (610)
Tests      17 failed | 4429 passed | 24 skipped (4470)
```

**双轴逐条 diff（原始输出）**：

```
$ diff <(main 失败文件集) <(D864 失败文件集)   →  （空）ZERO DIFF   [7 = 7]
$ diff <(main 失败用例集) <(D864 失败用例集)   →  （空）ZERO DIFF   [17 = 17]
```

失败集（两树逐字相同）：

```
tests/acceptance/zero-code-industry.test.ts（2）       pizza-chain 特有节点类型 / queryByTags food_service
tests/agent/expert-file-loader.integration.test.ts（5） FileScanner / strategy 关键文件 / loadFromIndex / getPrompt 必要章节 / prompt 身份规则
tests/e2e/p0-wane-baby.test.ts（0 test，收集失败）      Cannot find module extensions/…/cash-conversion-cycle
tests/electron/use-streaming-conversation.test.ts（0 test） Cannot find package 'react-markdown'
tests/l3/graphbridge-wiring.test.ts（1）
tests/mcp/server-smoke.test.ts（9）                    MCP SDK 未安装 → 子进程 10s 超时
tests/mcp/tool-definitions.test.ts（0 test，收集失败）  Cannot find package '@modelcontextprotocol/sdk/types.js'
```

### 5.2 3 组差集的归因核验

| 组 | 交付方声称 | K3 独立复算 | 判定 |
|---|---|---|---|
| `zero-code-industry` 的「新增行业零 .ts 文件修改」 | 用例字面跑 `git diff --name-only` 过滤 `.ts` → 脏树时假红 | **成立**：`tests/acceptance/zero-code-industry.test.ts:73` = `execSync('git diff --name-only')`；我在 D864 树上**加一行未提交 `src/routes/healthz.ts` 注释 → 该用例红（3 failed）**；还原 → 绿（2 failed） | ✅ 归因正确 |
| `metrics.test.ts` / `sessions-api.test.ts` / `smoke.test.ts` / `routes/llm-config.test.ts` | 并发 Vite 悬挂进程造成资源争用 → 全量跑假红 | **成立**：D864 树上隔离重跑 → `3 passed (3) / 19 passed (19)`，全绿 | ✅ 归因正确 |
| `p0-wane-baby` / `electron/use-streaming-conversation` / `mcp/tool-definitions` | 同类全量跑抖动 | **不成立**：这三项在 **main 与 D864 两棵树上都失败**（隔离重跑亦失败），报错为**不可解析 import**（`cash-conversion-cycle` / `react-markdown` / `@modelcontextprotocol/sdk/types.js`）→ 属**恒定的环境依赖失败**，不是抖动 | ⚠ 归因不符（判据不实，见 P2-4） |

### 5.3 「确定性失败集 = 8 例」口径核对（P2-4）

自验记录称"确定性失败集 8 例（两个 integration 文件 6 例 + pizza-chain 2 例）"。K3 全量跑实测确定性集 = **7 文件 / 17 例**：

```
expert-file-loader(5) + graphbridge(1) + server-smoke(9) + zero-code 本体依赖 2 = 17 例
（另有 p0-wane-baby / electron / mcp·tool-definitions 三个"0 test 收集失败"文件，
  在 main 与 D864 两树全量跑中均在场 —— 见上表三行）
```

**判**：**"8 例"低估了确定性失败集**（漏掉 `mcp/server-smoke` 的 9 例与 `mcp/tool-definitions` 收集失败，且把两棵树同源的三项环境依赖失败归为抖动）。**核心结论不受影响**——两树失败集**双轴逐字相同**是我独立复算的，零差集成立。

### 5.4 条件⑥ 无新增回归 — **成立**

D864 对 `src/` + `tests/` 的改动**仅 2 个文件**（`src/routes/healthz.ts` 11 行、`tests/routes/healthz.test.ts` 79 行，`git diff 48aa8709 444b2f55 --stat -- src/ tests/`）；该文件独立实跑 **11 passed**；全量跑零新失败（§5.1）。→ **无新增回归** ✅

**条件⑤ 判定：⚠ 部分成立**（双轴零差集成立；1 组归因正确、1 组错误）｜**条件⑥ 判定：✅ 成立**。

---

## 六、条件⑦：红线 — **7/7 成立**

```bash
# ① http-exit.ts 冻结契约零改动（6 支 + 伞，逐支对 D821）
$ for r in pr1..pr6 444b2f55; do git diff 717d67a1 $r -- src/providers/http-exit.ts | wc -l; done
0 0 0 0 0 0 0        ✅
# ② G1
$ grep -rn "@deepseek-ai" src/ packages/ | wc -l
0                    ✅（packages/ 存在，12 个子包，一并扫描零命中）
# ③ --no-verify：D862/D864 区间
$ git log --oneline 48aa8709..444b2f55 --grep="no-verify" | wc -l
0                    ✅
#   （`git log <branch> --grep=no-verify` 各支 12 条命中，逐条核验均为 2026-09-06~09-10 的**历史文档 commit**
#    ——如 D530/D523/D522 的 "--no-verify 原因" 说明文字，非本卡）
# ④ stash：git stash list = 1 条（stash@{0}: WIP on (no branch): 717d67a1）——非 D864 产物
# ⑤ force push：本地 vs 远端哈希逐支一致
pr1 47d82edb ✓ pr2 6f101df0 ✓ pr3 415e6ee6 ✓ pr4 cd56ac67 ✓ pr5 30b3c9c2 ✓ pr6 6e2abeb3 ✓ umbrella 444b2f55 ✓
# ⑥ preconditions.yaml P-1 未误改 —— D862 已复核（三处 blob 全同 0f4f1eee），本轮无该文件改动
$ git diff --name-only 48aa8709 444b2f55 | grep preconditions
（空）               ✅
# ⑦ 未碰 scripts/control-tower/**、scripts/audit/**、.github/workflows/**、src/sentinel/**
$ git diff 48aa8709 444b2f55 --name-only -- scripts/ .github/ src/sentinel/
（空）               ✅
```

**条件⑦ 判定：✅ 7/7 成立。**

---

## 七、P1 / P2 清单（附归因）

### P1（1 项）

**P1-2 ｜受审分支的 PR 增量在现状态超 D734 上限，且 pr6 触发 D733 域判定 FAIL**
- 证据：`main…branch` 三-dot 实测 **16/21/20/18/19/30** 文件；用真实施加 diff 喂 `check-pr-budget.sh` → **六支全部 FAIL**（§1.2）。`pr6` 即便按内部增量剔除 `bypass.log` 也 FAIL 于 **`② 变更跨域`**，唯一越域文件是 `.claude/reference-map.md`（win）——剔除后 `✅ ① 11 ≤ 12 ／ ✅ ② 变更单域 mac`（§1.4）。
- 性质：**不是代码缺陷**（合并实测零冲突、落完等于终树）；是**PR 门禁在"前置未完成"状态下的必然红 + 一个 hook 生成物的域归属**。
- 处置（不改代码）：① PR 顺序执行"先合 D821 → 各支再同步 main"后重测（我在模拟中已实测可达 3/8/7/5/6/11 全 ≤12）；② `.claude/reference-map.md` 入 `ownership.yaml` 的 `domain_neutral`（与 `bypass.log` 同类），或在 PR 声明 owner=win。
- **归因：control-tower**（门禁口径 + 域表缺一条 hook 生成物）

### P2（4 项）

| # | 发现 | 证据 | 归因 |
|---|---|---|---|
| P2-1 | 合并顺序文档 §二 称"按此序合并，零冲突"，但**第 1 步 `task-state/D821.json` 必冲突一次**（main=d31db17e vs d821=78556b73，base=a151712c） | `git merge 717d67a1` 于 origin/main → `CONFLICT (content)`；取 main 侧即解，落地后与 main 版逐字一致 | devdoc |
| P2-2 | 15 对交集"唯一 `.claude/bypass.log`"在真实分支状态**不成立**（实测共享 10–16 文件 = D821 载荷 + main 同步文档/卡） | §4.1 三套口径 `comm -12` 原始输出 | devdoc（口径表述） |
| P2-3 | 「各支 ≤12」存在两套并存数字（自验 3/8/7/5/6/10 vs K3 4/9/8/7/8/12，差 1 源自 `task-state/D821.json` 冲突解法），文档未固定唯一口径 | §1.3；自验记录自身也已注记该差异 | devdoc |
| P2-4 | 「确定性失败集 8 例」低估：K3 实测 **7 文件 / 17 例**；且被归为"抖动"的 `p0-wane-baby` / `electron/use-streaming-conversation` / `mcp/tool-definitions` 在 **main 与 D864 两棵树都失败**（不可解析 import），属恒定环境依赖失败而非抖动 | §5.2/§5.3；两树隔离重跑均红 | devdoc（M2 家族：数字口径 + 归因） |

### 挂账 3 项（维持原判：**可登记不阻断**）

| # | 挂账 | 本轮复核 |
|---|---|---|
| ① `tool_choice:'none'` 后收紧前缀豁免 | D862/D864 交付面零涉及 → 仍挂账 ✅ |
| ② watchdog 探针认证未做 | 本轮无该文件改动（`git diff 48aa8709 444b2f55` 无 `src/deploy/**`）→ 仍挂账；**不得声称已做** ✅ |
| ③ Windows 实机未验 | 本轮新增面仅 healthz catch 文案 + 测试断言，零平台分支 → **禁止声称 Windows 已验**维持 ✅ |

---

## 八、L4 防线缺口收割

| # | 发现 | 本该拦住的防线 | 为什么没拦住 | 缺什么 |
|---|---|---|---|---|
| 1 | P1-2：六支 PR 增量现状态 >12、pr6 域判定 FAIL | D734 PR 预算门禁（`check-pr-budget.sh`，接在 pre-commit 第 7 组） | 门禁**只在提交时对暂存集判**，从不判「某分支将来作为 PR 落地时的 diff」；而叠栈分支的 PR diff 取决于**前置分支是否已合**，这个状态门禁看不见 | 在「切分/推送域分支」环节加一道**预检**：`check-pr-budget.sh --base <该支声明 base>`，并把 `.claude/bypass.log`/`.claude/reference-map.md` 纳入 `domain_neutral`（否则每支恒被 hook 生成物吃掉 1–2 个名额+一条越域） |
| 2 | P2-4：确定性失败集被低估、环境依赖失败被归为抖动 | 自验记录/回归基线（无机器校验） | 基线是**人在特定环境测得并手写**的，没有「环境指纹 + 机器重算」约束；同一棵树上跑两次的集合还可能因并发口径不同而变 | 基线记录加**环境指纹字段**（`node -v` + 关键依赖存在性 + vitest 并发口径）并把失败集**落成文件**（供 `diff` 复核），而非正文里的手写数字 |
| 3 | P2-1：合并顺序文档"零冲突"表述与第 1 步实测不符 | 派单/文档的自我校验 | 文档写「按序合并零冲突」时只在**同基线假设下**推演，未在真实 main 上跑一次（本审计一跑即出）。 | 合并顺序文档附**一次真实 dry-run 输出**（含已知冲突项与解法），把"零冲突"改成"已知 1 处台账冲突，取 main 侧" |

> **防再犯收敛（Anti-bloat）**：三条**全部命中已有 M 类**——#1 属 **M8（共享载体竞争）** 变体（载体从暂存区扩到"分支基线状态"）；#2 / #3 属 **M2（声称 vs 事实）**。按"一类一机制"原则**不提议 M9**，仅在既有类内各强化 1 个守法：M8 → 域分支预检；M2 → 基线/文档数字必须附"可复跑命令 + 环境指纹"。

---

## 九、跑偏第二道（对照 PRODUCT-BRIEF.md 三问）

| 三问 | 判定 | 依据 |
|---|---|---|
| ① 服务真实用户场景？ | ✅ | 目标场景仍是"客户内网只允许代理出网"（`preconditions.yaml` P-1 `why_not_a_line`）。本轮唯一产品代码改动是**收紧异常回显**（healthz catch 固定文案 + 日志收窄），直接减少凭据外泄面——对"部署在客户内网"的形态是正收益。 |
| ② 更接近终态？横向加无关东西？ | ✅ | 本轮对 `src/` 仅改 1 个文件 11 行 + 1 个测试文件；其余 15 个文件全是台账/自验/卡/顺序说明（治理产物）。零新功能、零新依赖。 |
| ③ 做完后还是"驻扎企业、主动诊断、为增长服务"的 Agent？ | ✅ | 未引入任何被动化/SaaS 化倾向；`degraded` 语义保持保守（不静默直连）。 |

**跑偏判定：不偏离。**

---

## 十、运行环境注记（务必随结论一起读）

| 项 | 值 |
|---|---|
| 审计机 | macOS（darwin-arm64）｜ Node **v22.22.0**（ABI 127，better-sqlite3 实跑通过）｜ vitest 4.1.8 |
| 依赖完整性 | 与主工作区共享 `node_modules`；**缺 `@modelcontextprotocol`、`react-markdown`** → `src/mcp/**`、`tests/electron/**` 的失败作环境依赖处理（**两棵树同样失败**，不构成差集） |
| 全量回归口径 | 必须 `--no-file-parallelism`。并发跑会把 8 个文件打成 `hookTimeout`（**未执行即计失败**），实测：并行下 `metrics/sessions-api/smoke/routes·llm-config/agent-observer/ima-knowledge/qa-router/e2e-autonomy` 8 文件假红，顺序跑全绿 |
| 反例树 | `/tmp/k3d864-red`（9aaca76e^）与 `/tmp/k3d864-green`（9aaca76e）为临时 worktree，反例测试文件在 `tests/k3probe/`（**未入库**），审计后拆除 |
| 合并模拟树 | `/tmp/k3d864-sim`（origin/main 起）、`/tmp/k3d864-prb`（717d67a1 起）、`/tmp/k3d864-base`（main+D821）——**均为临时工作区，未污染受审分支** |
| 受审树状态 | `.wt-k3-d864` 跑全量后被测试副作用改写 4 个被跟踪文件（`docs/synova/product-lines/evidence/D817/D819-capture-*.json`、`extensions/industries/{saas-tech,test-write}/thresholds.json`）；已 `git checkout --` 还原，受审内容零污染 |

---

## 十一、复审范围建议

本轮 P1-2 与 4 项 P2 **均不涉及产品代码正确性**（唯一代码改动已红→绿闭环）。下一次复审只需核三件事：

1. **PR 落地形态**：先合 D821 → 各支同步 main → 逐支 `check-pr-budget.sh --base <该支 base>` 全绿（含 `.claude/reference-map.md` 的域归属处置）；
2. **合并顺序文档**：补第 1 步 `task-state/D821.json` 冲突与解法，并把"零冲突"改为实测表述；
3. **回归基线**：按 `--no-file-parallelism` 重跑并**落文件**（供 `diff` 复核），确定性失败集数字与归因按 §5.3 修正。

---

## 十二、材料与证据索引

| 材料 | 位置 / 命令 |
|---|---|
| 1 任务提交集 | `git log --oneline 48aa8709..444b2f55`（12 真实提交 + 12 hook 登记）；六支同法 |
| 2 Git diff | `git diff --stat 48aa8709 444b2f55`；`git diff --stat 717d67a1 <每支>`；`git diff --name-only pr5..pr6` |
| 3 交付件 | `docs/synova/coordination/D862-PR合并顺序说明-20260921.md`、`D864-自验记录-20260921.md`、`D862-出站分类台账-20260921.md`（§七）、`.claude/task-briefs/2026-09-21-D864-P2aP2b-healthz收口-coderB.md` |
| 4 卡 | `task-state/D864.json`（status=in_progress，7 条 acceptance_points）、`task-state/D862.json`（audit 段已回填 D862 结论） |
| 5 铁律依据 | `AGENTS.md` / `CLAUDE.md` / `docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md` / `ownership.yaml` |
| 6 门禁复跑 | `bash scripts/control-tower/check-pr-budget.sh --base <ref> --files "<...>"`（逐支输出见 §1.2/§1.4）；`bash scripts/pre-commit-check.sh` |
| 7 回归证据 | `/tmp/main-seq.log`（基线）、`/tmp/d864-seq.log`（受审树）；双轴 `diff` 均空 |
| 独立夹具 | 红/绿反例工作树 + `tests/k3probe/k3-counter.test.ts`（未入库）；合并模拟工作树 3 个 |

---

**署名**：Kimi K3 独立审计 ｜ 本报告只写独立工作区，回流走只含报告的 PR 分支 `audit/k3-d864-reaudit`。
