# K3 独立复审报告 — D865「D831 退回闭环」（P-2 不变量机制收口）

> 审计员：Kimi K3（独立会话）｜ 复审日期：2026-09-21/22
> 对象（CTO 已复算回执一致）：
> `feat/p2-invariants-core` @ **39bce446** ｜ `feat/p2-invariants-failclosed` @ **67bd295e** ｜ `feat/p2-invariants-docs` @ **6642414a**
> 共同基线：`origin/main` = **3643db6a**（三支各自重挂，非祖先链）
> 上游：K3 D831 原判 `FAIL（P0×2/P1×3/P2×6）` — `docs/synova/audit-reports/2026-09-21-K3-D831.md`
> 环境：macOS / Node **v22.22.0**（better-sqlite3 ABI 127 原生可用）｜ 独立 clone + 4 个审计 worktree，产品仓库跟踪文件零改动

---

## 〇 结论

**判定：PASS（两 P0 物理闭环 / 必核 5 项逐条独立复现 / 登记 P1×2 + P2×7，全部不阻断合并）。**

| 必核 | 结论 | 独立复现证据（摘要） |
|---|---|---|
| 1 P0-1 真 fail-closed | ✅ **属实** | 自建探针：首轮违约 → **HTTP 500 + JSON `{code:INVARIANT_VIOLATED, invariant, owner}`**；头已发违约 → 200 + 流内 `INVARIANT_VIOLATED` 帧 + `end`；健康路径不误伤 |
| 2 F1 挂死修复 | ✅ **属实** | 2×2 矩阵自建复现：修复前 **elapsedMs=3005 HANG(AbortError)**；只修路由 **22ms**；只修 adapter **22ms**；两态皆修 **20ms** → 「任一侧修复即 ≤26ms」成立；定向 **97 passed / 0 failed** 精确对齐 |
| 3 P0-2 CI 归因 | ✅ **属实** | 三支 job 级回执逐条相符（Iron laws=FAILURE / Vitest+Golden=skipped）；本地 CI 等价 `SYNO_CI=1` 三支**唯一红项＝D734 PR 预算**（`❌ 1 组未通过`）；18/24/29 文件与 win/mac 归类逐文件相符 |
| 4 回归 | ✅ **属实** | 窄集与宽集两轴、docs tip vs main 基线：**失败集逐条相同**（5 条 expert-file-loader 预存） |
| 5 红线 | ✅ **属实** | 业务提交 100% 有 pre-commit 登记；无 stash（含悬空 WIP 取证）；10 次 push 全 `forced=None`；未碰 `scripts/**`、`.github/**`；无自我审计 |

**遗留（全部登记为不阻断，见 §7/§8）**：P1×2（D866–D869 无实体卡、门禁组 7 死检查）、P2×7（spec 指针失效、CI 回执锚定前一个提交、N3 文档未随裁决更新、500 不回 sessionId、`bypass.log` 手写补记、ci.yml 行号偏移、授权表未随新引用更新）。

---

## 一 必核 1：P0-1「真 fail-closed」— **闭环，逐字段复现**

### 1.1 代码链（三段全部落实）

```
src/invariants/companions/tool-schema-sent.invariant.ts   fail() → InvariantError（不变量自己抛）
src/llm/retry-middleware.ts:173 findInvariantError（≤6 层 cause 链，name 判型）
src/llm/retry-middleware.ts:330-338  InvariantError 不吞不重试 → 上抛链内本体（callWithResilience）
src/llm/retry-middleware.ts:423      streamWithRetry 同款（流式路径同样上抛）
src/agent/tool-loop-executor.ts:252,270,404,428  四处 catch 穿透（不再降级为「抱歉，调用失败」）
src/routes/conversations.ts:381-390  头未发 → 500 JSON；:395-397 头已发 → 流内帧 + end
src/l1-interaction/web-adapter.ts    deferHeaders 延迟建流（ensureActive 激活 / close 兜底）
```

### 1.2 K3 自建探针（真实 `createServer` + 假上游 + 出站注入器；**不使用小队测试**）

```
[K3-P01-1] status=500 content-type=application/json; charset=utf-8
  body={"ok":false,"code":"INVARIANT_VIOLATED","invariant":"INV-TOOL-SCHEMA-SENT",
        "owner":"squad-b/coding","message":"invariant INV-TOOL-SCHEMA-SENT violated by \"@synova/agent\" (owner: squad-b/coding):
        出站 LLM 请求 tools 为空数组 — 显式空 schema，无合法收尾语义（工具能力在 provider 边界被丢弃）"}

[K3-P01-2] status=200 content-type=text/event-stream; charset=utf-8
  types=["open","token"×9,"error","end"]
  violation={"type":"error","code":"INVARIANT_VIOLATED","invariant":"INV-TOOL-SCHEMA-SENT",
             "owner":"squad-b/coding","degraded":false}
  （断言 text 不含「抱歉，调用失败」→ 通过）

[K3-P01-3] 健康路径 status=200 types=["open","token"×6,"agent_message","end"]（零 error 帧，不误伤）
  probe={"registered":3,"expected":3,"missing":[],"invariants":[…,"lastFailure":"出站 LLM 请求携带工具对话…"],"anomalies":[]}
```

**逐项对照**：5xx ✅（实测 500）｜ body 含 `INVARIANT_VIOLATED` ✅ ｜ 含 invariant code ✅ ｜ 含 owner ✅ ｜ 非 200/SSE 文案 ✅ ｜ 头已发场景按声明「流内 `INVARIANT_VIOLATED` + 断流(`end`)」✅ ｜ 探针 lastFailure 可见 ✅

### 1.3 旧「HTTP 层 5xx 语义」不实声称的改写（grep 证据）

```
$ grep -rn "HTTP 层 5xx\|5xx 语义" src/ tests/ task-state/ .claude/task-briefs/ docs/
tests/sentinel/invariants.test.ts:8    旧文「HTTP 层 5xx 语义」为不实声称，已按 K3 §4.3 + D865 返工纠正
task-state/D865.json:25                …⑤ 旧「直调 provider.chat = 5xx 语义」声称为不实——已改写为「伴生层直调」口径
.claude/task-briefs/2026-09-21-D831-P2-invariants.md:21  （历史决策记录，原文保留）
.claude/task-briefs/2026-09-21-D831-P2-invariants.md:44  【历史规格文本·口径已纠正】原写「HTTP 层 5xx 语义」为不实声称…
docs/synova/coordination/D831-squadB-自验与交付-20260921.md:44/184  （逐条标注不实 + 纠正）
```

- 用例标题已改名：`tests/sentinel/invariants.test.ts:101,136` → `①【伴生层直调】…`（断言面 = 伴生层，不再冒充 HTTP 面）✅
- `task-state/D831.json` summary/verification 数字与口径同步改写 ✅
- **唯一残余**：brief 第 21 行（Q1 决策点历史记录）保留原句无注记——但该句「抛 InvariantError → HTTP 5xx 语义」**在本次修复后已成立为事实**（500 实测），同页第 44 行亦有纠正注记 → **不构成不实声称**（P2 提示：建议同页补同款注记，免 grep 误读）。

---

## 二 必核 2：F1（修复引入的新缺陷）— **闭环，2×2 矩阵自建复现**

缺陷面：`deferHeaders` 下「首个 token 之前的**非不变量**错误」→ 帧全进缓冲、无人写头、`close()` 又只清缓冲不 `end()` → 客户端永久挂死。

**K3 自建探针 + 真实修订版切换（F1 修复恰只动 2 文件，故可用 2×2 精确复现）：**

| 组合 | `src/routes/conversations.ts` | `src/l1-interaction/web-adapter.ts` | 实测 | 结果 |
|---|---|---|---|---|
| (P,P) 修复前 | 修复前（`8c86677d`） | 修复前 | `outcome=HANG_AT_HEADERS err=AbortError elapsedMs=3005 status=-1 frames=[]` | **挂死（复现）** |
| (F,P) 只修路由 | 修复后（tip） | 修复前 | `outcome=OK headersAt=21ms elapsedMs=22 status=200 frames=["open","error","end"]` | 有限返回 |
| (P,F) 只修 adapter | 修复前 | 修复后（tip） | `outcome=OK headersAt=22ms elapsedMs=22 status=200 frames=["open","error","end"]` | 有限返回 |
| (F,F) 两态皆修 | 修复后 | 修复后 | `outcome=OK headersAt=20ms elapsedMs=20 status=200 frames=["open","error","end"]` | 有限返回 |

- **「任一侧修复即 ≤26ms 返回」✅ 成立**（22/22/20ms）；**「修复前 ~3004ms 超时」✅ 成立**（3005ms，我的 AbortController 预算 3000ms）。
- 错误帧形态：`{"type":"error","code":"CONVERSATION_ERROR","message":"k3-f1-injected","degraded":true}` + 终帧 `end` ✅（保持 200+SSE 既有对外契约，未误改 5xx）。
- 注入方式为**非不变量真实故障**（文件 SQLite `BEFORE INSERT` 触发器 → `SqliteError`，命中 ⑧ `addMessage` 路径）✅

**定向 6 路径（K3 实跑，10 文件 / 97 用例）**

```
$ npx vitest run tests/sentinel/invariants.test.ts tests/invariants tests/deploy/bootstrap.test.ts \
    tests/routes/conversations.test.ts tests/integration/invariant-failclosed.integration.test.ts
 ✓ tests/invariants/registry.test.ts (12)   ✓ …/tool-call-pairing.invariant.test.ts (8)
 ✓ …/tool-schema-sent.invariant.test.ts (9) ✓ …/degraded-sticky.invariant.test.ts (7)
 ✓ tests/invariants/health-route.test.ts (4) ✓ tests/invariants/index.test.ts (6)
 ✓ tests/sentinel/invariants.test.ts (20)   ✓ tests/integration/invariant-failclosed.integration.test.ts (4)
 ✓ tests/deploy/bootstrap.test.ts (13)      ✓ tests/routes/conversations.test.ts (14)
 Test Files  10 passed (10)      Tests  97 passed (97)      ← 与「97 passed / 0 failed」精确一致
（[D865-F1] elapsedMs=5 status=200 frames=["open","error","end"]；[D865-E2E] RESPONSE status=500）
```

---

## 三 必核 3：P0-2 CI — **归因成立，独立复算**

### 3.1 三支 job 级回执（GitHub API 直取，非采信）

| 分支 | RUN | head | run 结论 | Iron laws | Vitest | Golden F1 | 其他 |
|---|---|---|---|---|---|---|---|
| core | 35611745060 | 39bce446 ✅ | failure | **FAILURE**(step: Iron laws check) | **skipped** | **skipped** | 全 success |
| failclosed | 35615404724 | 67bd295e ✅ | failure | **FAILURE**(同上) | **skipped** | **skipped** | +`Integration Contract Check` FAILURE(step: **Run npm ci**)，余全 success |
| docs | 35620058075 | **a224f097** ⚠️ | failure | **FAILURE**(同上) | **skipped** | **skipped** | 全 success |

→ 回执**逐条属实**（含 failclosed 支「Integration Contract Check / Run npm ci」这项偶发，卡内已如实登记）。
→ **口径提示（P2-c）**：docs 支回执锚定 `a224f097`，而该支 tip 为 `6642414a`；两者差 2 提交（`908b9560` 文档回填 + `6642414a` 簿记），**`git diff --stat a224f097 6642414a` = bypass.log + M6 文档 + task-state/D865.json，零代码** → 结论不受影响，但「tip 无自身回执」应登记。

### 3.2 「CI 红只剩 D734 预算口径」— 本地 CI 等价复现（`SYNO_CI=1`，三支各自全量暂存）

```
core : ❌ D734 PR 预算超限: 5 处 [CI strict]  → ❌ 1 组未通过 — 提交已拒绝   EXIT=1
failclosed : ❌ D734 PR 预算超限: 5 处 [CI strict] → ❌ 1 组未通过          EXIT=1
docs : ❌ D734 PR 预算超限: 17 处 [CI strict] → ❌ 1 组未通过               EXIT=1
三支日志中 ❌ 行仅 4 条/支，全部来自 D734 段；组 1-13（含组 2/组 4/组 6）全 ✅
```

→ **「组 2/4/6 已修、其余 12 组 ✅、唯一红项＝D734」✅ 成立。**

### 3.3 三支文件数与跨域（逐文件复算）

```
$ bash scripts/control-tower/check-pr-budget.sh --base origin/main（三支）
core       : ❌ ① 变更文件数 18 > 上限 12   ✅ ② 单域 win（15 win + 3 域判定豁免）
failclosed : ❌ ① 变更文件数 24 > 上限 12   ✅ ② 单域 win（21 win + 3 域判定豁免）
docs       : ❌ ① 变更文件数 29 > 上限 12   ❌ ② 跨域（win 21 + mac 3 + 豁免 5）

$ 逐文件 check-ownership.py（docs 支 29 文件）
mac  : docs/synova/coordination/D831-squadB-自验与交付-20260921.md
       src/sentinel/sentinel-events.ts
       tests/sentinel/invariants.test.ts
豁免 : .claude/bypass.log、2 份 task brief、task-state/D831.json、task-state/D865.json
win  : 其余 21 个（src/invariants 6 + 接线 4 源头文件 + 测试 9 + tests/deploy/bootstrap.test.ts 等）
```

**「win 最小自洽集 ≥15 > 12」✅ 成立（实测 core 支 win 域即 15）**，且 K3 独立给出下界论证：`src/invariants/**` 6 个新源码文件 → 门禁组 2「新文件配对」的映射是机械的 `src/X.ts → tests/X.test.ts`（`scripts/pre-commit-check.sh:561-577`），故 **6 实现 + 6 镜像测试 + bootstrap.ts + server.ts（接线必需）= 14 > 12**，再加 Phase 数适配的 `tests/deploy/bootstrap.test.ts` = 15。→「任何最小化都 >12」成立。

**跨域是否结构性必然**：3 个 mac 文件**全部可追溯到写集**——`tests/sentinel/invariants.test.ts`（D831 派单 + D865 卡 write_set 双列）、`src/sentinel/sentinel-events.ts`（D831 派单写集）、M6 交付文档（交付 SOP 规定目录）。且三支拆分**不能**降低计数：CI 与 D734 均以 `origin/main...HEAD`（三点）计算（`.github/workflows/ci.yml:86 SYNO_DIFF_BASE: "origin/main"` + `:87 SYNO_CI:"1"`；`scripts/pre-commit-check.sh:275/485-486` 同口径），故每支的 main 相对集都是**整栈并集** → 18/24/29 实测即证。→ **「结构性不可达」✅ 成立**（CTO 裁决 B 的前提为真）。
（K3 附注：docs 支的 `sentinel-events.ts` 仅头注释改动，理论上可省略；但该文件改动源自 D831 写集且修正的是 K3 自己在 §15-G4 记的「文档-实现漂移」，我不认为它应被要求回退。）

---

## 四 必核 4：回归 — 两轴失败集逐条相同

```
轴 1（窄，tests/sentinel tests/agent，串行）：
  docs tip 6642414a : Test Files 1 failed | 259 passed | 1 skipped (261)  Tests 5 failed | 1381 passed | 1 skipped (1387)
  main    3643db6a  : Test Files 1 failed | 258 passed | 1 skipped (260)  Tests 5 failed | 1361 passed | 1 skipped (1367)
轴 2（宽，+ tests/llm tests/routes tests/invariants tests/deploy tests/integration）：
  docs tip          : Test Files 1 failed | 325 passed | 1 skipped (327)  Tests 5 failed | 1928 passed | 2 skipped (1935)
  main    3643db6a  : Test Files 1 failed | 317 passed | 1 skipped (319)  Tests 5 failed | 1857 passed | 2 skipped (1864)

$ diff <(docs 失败集) <(main 失败集) → IDENTICAL FAILURE SET（两轴皆然）
  5 条全部为 tests/agent/expert-file-loader.integration.test.ts 预存失败（Given FileScanner 扫描 / Given strategy 专家 /
  Given 完整索引 / Given 已加载专家→prompt 包含身份和规则 / →getPrompt）
Δ 用例数：+71（D865 新增：invariants 20 + 镜像 46 + e2e 4 + conversations 1）= 预期
```

✅ 与卡内自述（1381/5/1 与 1361/5/1）**精确一致**（唯一口径差：卡写「main 基线 74356ba6」，我用分支真实 base 3643db6a，两者数字相同 → 不影响结论，P2-d）。

---

## 五 必核 5：红线

| 红线 | 结论 | 独立证据 |
|---|---|---|
| 无 `--no-verify` | ✅ | 业务提交逐笔对账（排除 auto-hook 登记笔）：core **7/7** 有 `COMMITTED \| pre-commit PASS` 登记、docs **20/20**、failclosed **12 中 11**（唯一缺登记笔＝`39de2ce5 chore(D865): bypass.log 一次性补记` 自身，结构使然）。`GATE_FAIL_SOFT` 行＝D515 软门禁按设计放行（`.git/hooks/pre-commit` 末尾 `exit 0`） |
| cherry-pick 触发的 `detected-bypass` 是否为真绕过 | ✅ 否（结构性误报） | bypass.log 内 5 条 `detected-bypass head-mismatch/no-precommit-marker` 对应 cherry-pick；`git diff 0b1c3937^..0b1c3937` 与 `git diff fe8c5dd3^..fe8c5dd3` **补丁逐字节相同**（130 行 vs 130 行，diff 空）→ 「同内容 cherry-pick」属实；且 git 对 cherry-pick 不跑 pre-commit 属既有语义 |
| 无 stash | ✅ | 产品仓 `refs/stash` 仅 2026-09-05/09-08 两条旧记录；`git fsck --unreachable` 全量悬空提交中 **无任何 p2-invariants 的 `WIP on`/`index on`**（当日 22:43 的 WIP 属 d862-* 他线） |
| 无 force push | ✅ | GitHub PushEvent 10 次（三支）：全部 `forced=None`，`before→head` 逐次线性衔接 |
| 未碰 `scripts/**`、`.github/**` | ✅ | 三支 `git diff --name-only 3643db6a..origin/<支> \| grep -c "^scripts/" == 0`、`^\.github/ == 0` |
| 未自我审计 | ✅ | 三支均无 `docs/synova/audit-reports/**` 变更（`grep -c audit-reports == 0`） |
| 类型安全 | ✅ | 新增行 `as any/as never/as unknown as` 命中经逐条核阅**全部为文档/注释文本**（brief 与 M6 文档中的禁令原文、JSDoc「零 as any」注记），代码零使用；门禁组 1 ✅ 佐证 |

---

## 六 P1×3 / P2×6 处置复核（抽检）

| 原判 | 卡内去向 | K3 抽检 |
|---|---|---|
| P1-1 registered==0 用例替换 execute | fixed | ✅ `tests/sentinel/invariants.test.ts:318-338` 现**真 `createRuntimeInvariantsPhase()` + 真 Bootstrap.run()**，只桩 `installAll`（K3 PROBE-D 做法），`execute` 不再被改写 |
| P1-2 守卫反向改写 `store.lastDegraded` | fixed | ✅ `degraded-sticky.invariant.ts` 全文件**零对 store 字段的赋值**（仅 `:102` 读 `visibleDegraded`）；粘滞只记 `stickyDegradedSessions`；新增用例断言 `store.lastDegraded===false` |
| P1-3 pairing 生产触发面=0 | new-card 建议 D866 | ⚠️ 仅文档建议，**无实体卡**（见 §7-F6），notChecked 已增列（属「如实声明」，可接受） |
| P2-3 fetch 回滚遇替换只 warn | fixed | ✅ `registry.flagAnomaly` + 探针 `anomalies` 暴露；K3 探针输出实测含 `"anomalies":[]` 字段（`snapshot.anomalies` 亦在） |
| P2-5 Phase 断言构造可达 | fixed | ✅ `index.ts` 改 `snap.registered !== RUNTIME_INVARIANT_COMPANIONS.length` 一致性断言；探针新增 `expected/missing` |
| P2-4 架构门禁只扫 src/l3 | new-card 建议 D867 | ⚠️ 仅文档建议，无实体卡 |
| P2-1/P2-2 数字口径 | fixed | ✅ D831 卡已改写为 20 用例 / 1381 / 1361 口径 |

---

## 七 已知边界裁定（逐项）

| 边界 | K3 裁定 | 依据 |
|---|---|---|
| **N3** 首 token 窗口无头无心跳（headerMs=1224 vs 非延迟 6ms；首轮 LLM 永不返回则客户端全静默） | **可登记不阻断** | 该窗口＝首轮首 token 延迟，且受 LLM 既有超时（`base.ts` `AbortSignal.timeout(120s)`）与租户侧代理超时约束；「立即写头」与「首轮违约可 500」在物理上互斥（正是 P0-1 的代价），取舍不可避免。CTO 已判不阻断并立 D869（补齐首 token 窗口心跳/超时）→ 属既定后续卡 |
| **N4** 非 deferred 构造路径头字面块重复（单源覆盖 2/3） | **可登记不阻断** | K3 实测全仓 `new WebViewAdapter(` 仅 1 处（`conversations.ts:240`，带 `deferHeaders:true`）；非 deferred 分支（`web-adapter.ts:71-75`）当前零构造点，无行为影响 |
| **F7** 门禁组 7 某子检查静默降级仍报 ✅ | **可登记不阻断（归控制塔）** | K3 **独立复现且定位**：`scripts/pre-commit-check.sh:991` 的排除模式含 `^+++`（ERE 中 `+` 无操作数）→ `grep -Ev` **exit 2 / `repetition-operator operand invalid`** → `\|\| true` → `NEW_DIAG=""` → `:992 soft_check` 恒过。用一条**本应被拦**的输入实测：`echo "+ 禁止新增 DiagnosticModule 的使用" \| grep -Ev '…^+++…'` → `EXIT=2` 无输出。→ 该检查**结构性不可能失败**（M1 家族），且 main 同存（三支未碰 scripts/**）→ 判控制塔缺陷，D868/另卡收口 |
| **F8** 主仓证据漂移（D819-capture `captured_date`） | **可登记不阻断（已还原；但副作用可复现 → 会复发）** | K3 实测 `/Users/wane/SynovaAgent` `git status --short` **空**（含 D817/D819 evidence、thresholds.json 等此前 5 项脏文件全部回到干净态）→ 与「CTO 已还原」一致，该边界已闭合。**K3 追加实测**：本次复审在**两个自有 audit worktree** 上跑回归后，`docs/synova/product-lines/evidence/D817-capture-20260918.json` 与 `D819-capture-20260919.json` **再次被测试改写**（与 F8 同型，K3 已 `git checkout --` 还原，未污染产品仓库）→ 结论：F8 不是一次性事故，而是**测试向被跟踪证据文件写时间的结构性副作用**，只要有人跑那两条集成测试就会复发（建议：该证据文件改由 afterAll 写临时目录/或断言改为剥离时间面，否则每次回归都会再造一次「主仓漂移」告警） |
| 前缀豁免（已带过 tools 的对话放行收尾轮） | **可登记不阻断** | `notChecked #2` 显式声明（总纲 §9.4 合规）；D819 C2 收尾轮为既有行为，收紧路径已留痕 |
| 探针未加认证（与 `/api/healthz` 同口径） | **可登记不阻断** | 与既有健康面口径一致；探针仅暴露 code/owner/hitCount/lastFailure/anomalies，无 PII |
| Windows 实机未验（win 域 Mac 代行） | **可登记不阻断** | 本次全部结论为 mac 代码级实测；Win 行为按 D773 挂账 |
| **F6** D866/D867/D868 仅文档建议、无 task-state 实体卡；**D869 在本仓 refs 中亦未检出** | **不阻断，但记 P1-1**（见 §8） | 卡内已如实标注「无实体卡」不构成 overclaim；风险在于**结构性缺陷（CI 计数口径）无归属方** |

---

## 八 L4 缺口收割 + 归因（先查 M1-M8，不新增 M 类）

| # | 发现 | 分级 | 归因 | 映射既有 M / 补法 |
|---|---|---|---|---|
| R1 | **D866/D867/D868/D869 无实体卡**：P0-2 的结构性根因（D734 计数口径 vs D773 代行 + 新建子系统）只停在「文档建议」；三支合并后同类任务仍会持续 CI 红，而缺陷无归属方 | P1（不阻断） | control-tower（立卡 SOP） | **M7 邻域（文档-实现/登记漂移）** → 强化既有「建议必须落卡」：派单回执要求 `task-state/` 实体卡存在，否则建议项在 K3 复审一律按未闭环记 |
| R2 | **F7 死检查**（`pre-commit-check.sh:991` `^+++` → grep exit 2 → `\|\| true` → 恒过） | P1（不阻断，控制塔域） | control-tower | **M1（fail-open 静默失效）真命中** → 按 M1 既有防线强化：`soft_check` 传入空串时应区分「无违规」与「检查失败」；`\|\| true` 处补 exit-code 三态（D328 三态输出） |
| R3 | `bypass.log` 被**人手追加**解释行（cherry-pick 补记）——证据链文件被人工写入，削弱其机器可判性 | P2 | control-tower | **M4（执行证据链断裂）** → 建议 bypass 检测器对 cherry-pick/merge 输出独立 `kind=cherry-pick` 记录，替代手写补记 |
| R4 | 500 路径不返回 `sessionId`：`conversations.ts:229` 已建会话行，`:381-390` 的 500 body 无 sessionId → 客户端无法续轮 + 孤儿会话行（对比：旧行为至少回 `open` 帧带 sessionId） | P2 | implement | 补 `sessionId` 字段（或声明孤儿可接受 + 排查脚本）即可，属实现细节 |
| R5 | D865 卡 `spec` 指针 `docs/synova/coordination/派单-D864-D865-退回闭环-20260921.md` **不存在**；真件为 `docs/synova/coordination/K3复审派单-D864-D865-20260921.md`，且只在 `origin/docs/k3-reaudit(-d864-d865)` 分支、未随三支落库 | P2 | devdoc/流程 | **M7** → 派单件随卡落库 + spec 指针纳入 g12/brief 校验 |
| R6 | ci.yml 行号偏移：卡写 `ci.yml:84`，实测 `SYNO_DIFF_BASE`/`SYNO_CI` 在 `:86/:87` | P2 | implement | 引用行号随代码漂移，回填时复核（最小项） |
| R7 | 授权表未随新引用更新：`AUDIT-FINDINGS-LEDGER.md` §四「DSH 防线映射」未登记本轮新发现的 M1 命中（F7 死检查）与 M7 命中（建议未落卡） | P2 | audit（K3 自记） | 本报告即登记入口；台账更新走 CTO 队列 |

**未新增 M 类**（M1/M4/M7 均命中既有类）。**P0 无残留**：原 P0-1 由「韧性层吞错 + 无延迟建流」两个物理根因分别修掉并实测（§1/§2）；原 P0-2 的结构性事实经 K3 逐文件/逐门槛复算成立，并已由 CTO 书面裁决 B 改判据（§3）。

---

## 九 跑偏第二道（北星三问，对照 `.claude/PRODUCT-BRIEF.md`）

| 问 | 判据 | 结论 |
|---|---|---|
| ① 服务真实用户场景？ | §一 L14「它主动替企业干活」；§二 L20-21 | **对齐且价值兑现改善**：D831 的「违约必炸」从「200+降级文案」变成「首轮违约 500 + owner / 中途违约流内帧断流」——D819 类工具能力静默丢失，现在**不可能静默**（三处可见：500/流内帧/探针 lastFailure+anomalies） |
| ② 让模块更接近终态？ | §四 L40-43；§六 L66-68 | **不跑偏**：信任层基建收口（前提清单 P-2）；未横向加无关物，且顺手关掉了 D831 引入的新缺陷 F1（不把一个挂死缺陷留给产品） |
| ③ 做完后还是「驻扎企业、主动诊断、为增长服务」的 Agent？ | §一 L10-16 | **是**：无身份变更，无 ChatBot 化迹象 |

**裁定：无方向跑偏。**

---

## 十 环境与口径注记

- 审计机 Node v22.22.0 / better-sqlite3 ABI 127 原生可用（非 D316 型 ABI 假失败）。
- 三个审计 worktree 通过 symlink 复用主 clone 的 `node_modules`；产品仓库（`/Users/wane/SynovaAgent`）跟踪文件零改动、工作区现为干净态。
- 窄/宽两轴回归**串行**执行（避免并行资源竞争导致的 hook 超时抖动，D316 同型教训）。
- CI 证据经 GitHub API 未认证读取；job 日志端点需认证（403），故「CI 红＝仅 D734」用**本地同脚本 + `SYNO_CI=1` 等价复现**补证（§3.2）。
- 口径差异已如实标注：卡写 main 基线 `74356ba6`（我用分支真实 base `3643db6a`，数字相同）；docs 支 CI 回执锚 `a224f097` 而 tip 为 `6642414a`（delta 零代码）；ci.yml 行号 84→86。

---

## 附：复现路径

- K3 自建探针（不入产品仓库）：`tmp/k3-865-probe/{vitest.config.ts,p01.probe.test.ts,f1.probe.test.ts}`
- 原始日志：`/tmp/k3-865-p01-docs.log`、`/tmp/k3-865-f1-{PP,FP,PF,FF}.log`、`/tmp/k3-865-targeted.log`、`/tmp/k3-865-reg{,-wide}-{docs,base}.log`、`/tmp/k3-865-gate-{core,failclosed,docs}.log`、`/tmp/k3-865-bashx.log`
- 关键命令：
  - `K3_WT=<worktree> npx vitest run --config tmp/k3-865-probe/vitest.config.ts p01.probe.test.ts`（P0-1）
  - `K3_WT=.wt-865-f1pre … f1.probe.test.ts`（F1，配合 2 文件 2×2 切换）
  - `npx vitest run tests/sentinel tests/agent tests/llm tests/routes tests/invariants tests/deploy tests/integration`（宽回归，串行×2）
  - `SYNO_CI=1 bash scripts/pre-commit-check.sh`（三支，各自全量暂存）
  - `bash scripts/control-tower/check-pr-budget.sh --base origin/main`（三支）
  - `git fsck --unreachable --no-reflogs`（stash 取证）；`api.github.com/.../actions/runs/<id>/jobs`（job 级回执）
