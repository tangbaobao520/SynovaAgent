# K3 独立审计报告 — 批次 3（PR #737 / #739 / #741 / #743 + 两项独立复核）

> 审计员: Kimi K3（独立会话，零上下文）
> 日期: 2026-09-24 ｜ 工作区: `/Users/wane/Synova-k3独立审计`（与产品仓库物理隔离）
> 基线: `origin/main = 2cda1528`（fetch 时间 2026-09-24）
> 协议: AUDIT-PROTOCOL.md L1-L4 ｜ **有条件通过 = 未通过**
> 环境: macOS (Darwin) / BSD grep 2.6.0-FreeBSD / bash 3.2 / python3 —— 所有结论均本机亲测，原始输出见 §六

---

## 〇、总裁决

| PR | 判决 | 一句话 |
|---|---|---|
| **#737** fix/d937-gate-failopen | **PASS** | fail-open 根治属实；M1–M5 五个变异体 K3 独立施加全部"改坏即红"，复原全绿 |
| **#739** fix/d938-alloc-task-id | **PASS** | A′ 成功哨兵物理堵住"报错却 rc=0"（实测 rc=1+回滚）；--domain 真经 check-ownership.py（stub 替换证明调用，fail-closed 属实） |
| **#741** feat/m9-gate-integrity | **不通过**（C 段棘轮；A/B 段通过） | **③ 独立复现成立且升级**：CI 公开注解显示 `CI-RED-CHECK: 失败检查 0 项`，而同一 SHA 终局有 3 个 failure —— 棘轮在 CI 里空转（CTO 判断正确，证据在此坐实） |
| **#743** fix/d940-name-allocation | **功能 PASS，合并序不通过** | 5 位置拒绝 + `--check-id` 只读全部实测属实；但 **#743 的 alloc-task-id.sh 基于 main，不含 #739 的 D938 修复** —— 若先合 #743 或不 rebase，D938 三修丢失 |
| B5-U6 归并裁定复核 | **维持 CTO 裁定** | main 实测崩 / #725 版 rc=0 且覆盖两态 spec —— 覆盖性成立 |
| gen-cto-health 崩溃起点复核 | **"坏了 10 天"低估** | 真相更糟：**D600 时代（09-14）的修复 PR #546 至今未合 main**，main 上的崩溃自 ~09-09 已存在（约 15 天） |

---

## 一、#737 — PASS（无 P0/P1）

### ① awk 结构化判定是否真消除非法 ERE 类 —— 是

- 修复点 `scripts/pre-commit-check.sh:998-1048`（pr/737: `8ca6329f`）：候选行提取改为纯字符串比较（`substr($0,1,4)=="+++ "` 逐文件切换 + 后缀判 .md/.html），文件豁免零正则 → 该类非法 ERE 无处产生。
- 病根复现（K3 本机）: `echo test | grep -Ev '^+++'` → **rc=2**（`grep: repetition-operator operand invalid`）——与改前注释声称一致。
- 家族回归网实测（本机）: `扫描 141 个 .sh ｜ 可判 277 ｜ 未解析 0 ｜ 非法 0`，金丝雀（`^+++`、括号不平衡）均被抓 → 网非空转。
- 降级路径 fail-closed 属实：注入非法排除模式（`SYNO_TEST_ARM=1 SYNO_DIAG_EXCL_OVERRIDE='^+++'`）→ 组 7a 打 ⚠️ 降级、无 ✅、CI strict 计 HARD_FAIL；未设 SYNO_TEST_ARM 时覆盖变量被忽略（T3f 两条，生产 fail-closed）。
- quotepath 引号形态处理（`+++ "b/<path>"`）已覆盖 —— CI 下 .html 豁免不静默失效。

### ② M1–M5 变异体是否真承重 —— 是（K3 独立施加，非采信自验）

K3 在 wt-pr737 工作树用 Python 定点替换自行施加 5 个变异（施加后跑夹具、复原后复跑）：

| # | K3 施加方式 | 变异后 | 复原后 |
|---|---|---|---|
| M1 | 默认排除模式放回 `^+++` | **13 通过 7 失败（rc=1）** | 20/0 |
| M2 | 降级分支改 `if false`（rc≥2 当无命中） | **19 通过 1 失败（rc=1）** | 20/0 |
| M3 | 删 awk 逐文件豁免（回到行子串口径） | **18 通过 2 失败（rc=1）** | 20/0 |
| M4 | 注释豁免 `^\+[[:space:]]*(//|/\*|\*|#)` → 裸 `//` | **19 通过 1 失败（rc=1）** | 20/0 |
| M5 | 整段删组 7a（56 行） | **8 通过 12 失败（rc=1）** | 20/0 |

5/5 改坏即红、5/5 复原全绿 —— 与交付方 D937-自验.md §三-D 矩阵吻合（M3 的 T5c 命中数"5 处"亦复现）。

### 备注（不构成阻断）

- 自验件已诚实披露：家族网对变量持有模式（`_DIAG_EXCL`）零编译覆盖，靠行为断言兜底（最坏=降级，非假绿）；20 条断言中 1 条不携带信号。K3 认可该披露，方向 fail-safe。
- 写集核对：#737 自有提交（8ca6329f 修 + b2137083 测 + 证据/派单文档），`git diff` 三点式里的 B5/task-state 噪音来自并入分支的 #740 谱系，**非本 PR 夹带**。与 `scripts/audit/**` 零交集 ✓。

---

## 二、#739 — PASS（无 P0/P1）

### ① A′"成功哨兵"是否堵住"报错却 rc=0" —— 是（K3 注入故障亲测）

- 哨兵实现: `wt-pr739:scripts/control-tower/alloc-task-id.sh:84-92`（`_lock_release` 首句 `local rc=$?`；`rc==0 且 DONE≠1 → rc=1`；显式 `exit "$rc"`）。
- **物理注入**（BRIEF 父目录 chmod 555 → 骨架 mkdir 失败）:
  ```
  ❌ brief 骨架生成失败（目录创建失败: …/ro/sub）→ 已回滚 D936 登记（防空烧号，fail-closed）
  rc=1
  state left: 0        ← 孤儿号回滚属实
  ```
- 空标题 → rc=1（fail-closed）✓；dry-run / D718 守卫提前退出均标 `DONE=1`（不被哨兵误判）✓。
- 测试套: alloc-task-id 44/44、lock 7/7、scanner 32/32，全绿。

### ② 扫描器 `--domain` 是否真经 check-ownership.py —— 是（stub 替换证明）

- K3 把 `check-ownership.py` 临时替换为 stub（写 marker 文件）→ 跑 `--domain mac` 后 marker 内容 = 被查路径 → **确经调用**。
- stub 改报 `win` → `--domain mac` 立即 `❌ …零文件（fail-closed）` rc=2 → 过滤结果真被消费，非装饰。
- python 不可用 → exit 2（不猜域）；仓库外路径 → fail-closed。均与契约一致。

### 备注

- `tr ' /' '--'` 处理路径分隔符/空格 ✓（D938 缺陷③修法实测于文件名）。
- 写集核对：自有提交只动 alloc 两缝 + scanner + 4 个 probe/检测脚本 + 测试 + 证据件；`scripts/audit/**` 零交集 ✓。

---

## 三、#741 — 不通过（C 段棘轮在 CI 空转；P0）

### ③ 棘轮 `--ci-reds` 在 CI 里是否真被行使 —— **否。CTO 的判断独立复现成立，且有公开不可抵赖证据。**

**公共证据链（任何人可匿名复取）：**

1. pr/741 head SHA = `c4206f9b`。匿名 API:
   `GET /repos/tangbaobao520/SynovaAgent/commits/c4206f9b…/check-runs`（K3 实测 http=200）
   → 该 SHA 终局：`Control Tower Gate Tests (windows-latest)` **failure**（completed 14:30:59）、`Control Tower Gate Tests (ubuntu-latest)` **failure**（14:05:15）、`TypeScript + Lint + Iron Laws` **failure**（14:05:41）。
2. 同 SHA 的 `Gate Integrity` job 运行窗口 **14:03:08 → 14:03:27（19 秒）** —— 取数时上述 3 个 job 全部 in_progress（conclusion=null）。
3. 该 job 公开注解（`GET …/check-runs/107667821140/annotations`，K3 实取）:
   ```
   notice | CI-RED-CHECK: 失败检查 0 项；基线命中 0 项；基线条目 5 条；今天 2026-09-24
   notice | GATE-INTEGRITY: OK
   ```

**判定**：`REF=${{ github.event.pull_request.head.sha || github.sha }}` 取的是**本 SHA 并发运行中**的 check-runs 快照；慢 job 未定态（null）不计 failure → 棘轮"未登记的失败必红"的探测域在取数时刻为空集 → **C 段在 CI 里恒空转**（"0 条 check-runs 打 warning"只兜了极端空集，兜不住"有记录但未定态"的主流形态）。这不是理论竞态，是本次运行就实际发生的确定性空转（19 秒 job vs 28 分钟 job）。

**次级证据（基线自身的口径污染）**：`ci-red-baseline.txt` 登记了 `Control Tower Gate Tests (windows-latest) | … conclusion=null(in-progress) …` —— 把**未定态观测**登记为既有红。基线语义被冲淡：终局若 success 则成死条目（文件自认无自动清理）。

**处置**：CTO 已立 D953/D952 族 ✓。#741 的 C 段在修复"取数须在全部同 SHA check-runs 定态后"（如 pull_request target 延迟触发 / 轮询定态 / 取 base SHA 定态快照）之前**不得声称生效**。A 段（模式哨兵）+ B 段（登记 gate）本地实测 36/36 + 注入夹具通过，本身成立。

**归因: control-tower**（CI 编排时序设计缺陷，非脚本内部逻辑错误——本地 `run_ci_reds` 对已定态 JSON 的逻辑 K3 单测式喂入均正确）。

---

## 四、#743 — 功能 PASS；发现 1 项 P1 合并序缺陷

### ④ 跨位置拒绝是否覆盖 5 个位置 —— 是（K3 沙箱逐位置构造）

K3 用临时 git 仓 + bare remote 逐位置注入，`--check-id D777` 输出（全部 rc=1）：

| 位置 | K3 构造 | 输出行 |
|---|---|---|
| task-state | `task-state/D777.json` | `task-state …/D777.json` ✓ |
| origin-main | push main 含该文件 | `origin-main origin/main:task-state/D777.json` ✓ |
| remote-branch | ls-remote 含 `fix/d777-remote`（无本地分支、无本地文件） | `remote-branch fix/d777-remote (refs/heads/fix/d777-remote)` ✓ |
| local-branch | `git branch fix/other-d777` | `local-branch fix/other-d777` ✓ |
| worktree-name | `git worktree add ./synova-wt-d777wt` | `worktree-name synova-wt-d777wt` ✓ |

词边界正则无假阳性：`fix/d77x` 对 D777 → rc=0 ✓；`fix/d977` 不误配 D777（K3 首轮自摆乌龙后确认正则正确）。

### ④b `--check-id` 是否真只读 —— 是

- 空闲号 rc=0；运行后 `$T` 目录 **无锁目录、零新文件**（find 比对 = 0）；代码路径在分配/写盘逻辑之前 return（wt-pr743 alloc:197-206）。
- ls-remote 快照拿锁前取、超时 20s、降级显式 `degraded:` stderr ✓（铁律 11）。
- 测试套: alloc 24/24 + check-name-allocation 21/21 全绿。

### P1（合并序）：#743 与 #739 同文件异基

```
pr/739: DONE-sentinel=4 | SAFE_TITLE=2 | occupy=0
pr/741: DONE-sentinel=4 | SAFE_TITLE=2 | occupy=0   （栈式于 #739 ✓）
pr/743: DONE-sentinel=0 | SAFE_TITLE=0 | occupy=3   ← 基于 main，不含 D938 三修
```
（复现: `git grep -c 'DONE=1\|SAFE_TITLE\|_occupy_locations' <ref> -- scripts/control-tower/alloc-task-id.sh`）

#743 的 alloc-task-id.sh 是从 main（0558f7fb）长出的版本——**A′ 哨兵、SAFE_TITLE、骨架回滚均不在其中**。合并顺序必须为 **#739 → rebase #741 → rebase #743**，否则 #743 若先合（或 #739 rebase 不当）将冲突或把 D938 修复挤出该文件。派单只声明了 #741 栈式于 #739，**未声明 #743 的基**——这是协调缺口。**归因: devdoc/派单**（写集基线未声明）。

---

## 五、两项独立复核

### B5-U6 归并裁定 —— 维持"已被 #725 覆盖"

| 版本 | K3 实测 |
|---|---|
| origin/main (2cda1528) | `python3 scripts/control-tower/gen-cto-health.py` → **rc=1**，`AttributeError: 'str' object has no attribute 'get'`（gen-cto-health.py:305，撞 D598.json 的 string spec） |
| pr/725 (d663b41c) | **rc=0**，`已生成: docs/synova/CTO-HEALTH.md`，两态 spec（dict/str）兼容 + verdict 歧义 fail-closed 判 `?` |

U6 的代码诉求（:305 吃下 string spec）确被 #725 覆盖，且 #725 的 verdict 口径（结论行正则 → 无歧义子串 → 歧义判 `?`）比 D744 版本更保守（fail-closed），K3 认可。CTO 实测指纹 6395aad85f8c ≠ K3 实测 b25b4f2e332c —— 生成物含时间敏感内容，指纹跨环境/跨时点不具复现性（D383 P1-④"幂等声称不实"已知口径），不构成阻断。

### gen-cto-health 崩溃起点 —— "自 D600 起坏了 10 天"**低估**，真相更糟

K3 逐提交抽测 + bisect：

| 提交 | 日期 | rc |
|---|---|---|
| 26858bac (fix K3-L1 #453) | 09-09 | **1（同一 AttributeError）** |
| 7c0e58b2（D744 前合并基） | ~09-10 | **1**（bisect 判 bad） |
| acb7b0cb = **D744 修复（PR #546）** | 09-14 | 0 |
| origin/main 09-14 之后全部抽测点 | 09-15…09-24 | 1（除 #725） |

关键事实：**D744（fix(D744): gen-cto-health 崩溃 + FAIL 判定被 PASS 子串吞掉（P0），PR #546）从未合入 main** —— `git merge-base --is-ancestor acb7b0cb origin/main` = 否，分支只存在于 `origin/feat/mac-gen-cto-health-crash` 与 pr/546。

即：main 上的崩溃自 string-spec task-state 文件落地（~09-09）起持续至今（**约 15 天**）；09-14 台账记 P0 时修复已写好却**滞留在未合并的 PR 上 10 天无人合**。当前 main 的 gen-cto-health.py 最后一次被改动是 1c084775（09-06）——修复从未到过 main，不是"回归"，是"修复没落地"。

**归因: control-tower**（P0 修复 PR 无合并跟踪；缺一道"已立项 P0 修复分支 X 天未合"的哨兵 —— 可并入既有 check-ci-stale-red 同类机制，不必新脚本）。

---

## 六、L1-L4 摘要 + 防线缺口收割

- L1 代码：#737/#739/#743 核心声称全部物理属实（本轮最高质量批次之一）。
- L2 偏离：#743 未按 #739 基线栈式（P1）；其余写集/声称一致、零夹带、与 scripts/audit/ 零交集。
- L3 执行：#741 CI 证据链（公开注解）反而被 K3 用作"空转"的定罪证据 —— 证据步设计（M9 task-11）本身是好的，暴露的正是 C 段时序缺陷。
- L4 防线缺口（本该拦住而没拦住）：
  1. **#741 C 段**：缺"取数时同 SHA check-runs 全部定态"前置断言 → 空转。为什么没拦住：本地测试喂的是**已定态 JSON**，时序形态测试盲区。→ 补：夹具加一条"含 null conclusion 输入 → 显式 warning/不计通过"（D953 内）。
  2. **D744 孤儿 PR**：P0 修复 PR #546 十天无人合、无哨兵提醒。→ 补：check-ci-stale-red 同类扫"fix/* P0 分支 age>N 天未合"。
  3. **#743/#739 异基**：派单未声明栈式基线。→ 补：pre-dispatch-check 八项里加"同文件多 PR 必须声明 base 与合并序"。

## 七、结论与处置建议

1. **#737 可合并**（PASS）。
2. **#739 可合并**（PASS）。
3. **#741 暂不合并 C 段声称**：A/B 段成立；C 段待 D953/D952 修复取数定态后复审（只审变更）。当前形态下 `--ci-reds` 是"看起来在执法的空转"，恰是本批要根治的 fail-open 家族的新成员。
4. **#743 功能可合，但必须先合 #739 并 rebase #743**（或明确声明合并序 + 冲突消解后 K3 复审 alloc-task-id.sh 单文件）。
5. B5-U6：维持 CTO 归并裁定（#725 覆盖）。D744/PR#546 建议关闭并归档，其两半诉求均由 #725 重实现。

— K3，2026-09-24
