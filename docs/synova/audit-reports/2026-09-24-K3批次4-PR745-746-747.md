# K3 独立审计报告 — 批次 4（PR #745 / #746 / #747）

> 审计员: Kimi K3（独立会话，零上下文）
> 日期: 2026-09-24 ｜ 分支: k3/batch4-audit（base=origin/main 004d816c）
> 判定口径: **有条件通过 = 未通过**。每条 P0/P1 附 file:line + 复现命令 + 原始输出。
> 审计工作区: /Users/wane/Synova-k3独立审计（worktree: audit-wt/{b745,b746,b747,main}）

## 〇、总裁决

| PR | 分支 | 判决 | 一句话 |
|---|---|---|---|
| #745 | feat/d943-mechanism | **不通过（P0×1，P1×4）** | 断面门禁本体三态真判真拦，但 CI 接线错位＝夹具从未被门禁化且 job 必红；写集两文件未声明 |
| #746 | feat/b3-d922-fixture | **通过（P2×1）** | T3/T3c/T3b 判别性经外部变异证实；32 vs 31+SKIP=1 差异可解释且未掩盖失败 |
| #747 | fix/m9-baseline-owners | **通过（P1×1，P2×1）** | 5/5 条 owner/expires/disposition_due 齐全；CT(windows) 定态换证经 GitHub API 独立核验属实；SLA 无物理执法 |

机制发现复核：① infer_did 分支名优先 → **复现成立**（建议支持回退，见 §四.1）；② rerun 旧事件体 → **代码路径证实**（runner 侧采信 GitHub 文档行为，见 §四.2）。

---

## 一、PR #745 feat/d943-mechanism — 不通过

### 1.1 三态判别性（派单问 1）——本体 ✅ 真判真拦

独立构造（非跑作者夹具，作者夹具另跑 10/10、8/8 全绿）：

**① 旧断面引用 + ② 未登记版本串 → VIOLATION 且点名 file:line**

```
cd audit-wt/b745 && mkdir -p tmp/k3probe/docs/synova/coordination && cp docs/synova/coordination/DSH-断面.json tmp/k3probe/docs/synova/coordination/
printf '# 测试文档\n现状基于 0.1.7-alpha.2 @ 00102833 进行审计。\n' > tmp/k3probe/docs/synova/coordination/t1.md
printf '# 测试文档2\n基于 0.1.8-rc.1 新断面。\n' > tmp/k3probe/docs/synova/coordination/t2.md
python3 scripts/control-tower/check-dsh-anchor.py --repo tmp/k3probe --no-tree-check
```
原始输出：
```
  ❌ docs/synova/coordination/t1.md:2 引用了已 superseded 的断面 0.1.7-alpha.2 @ 00102833
  ❌ docs/synova/coordination/t2.md:2 未登记的 DSH 版本串 0.1.8-rc.1
DSH-ANCHOR: VIOLATION(2)  [扫描 2 份]      exit=1
```

**③ 事实源 head 与真实树不一致 → DEGRADED(2)（不是 0）**

```
python3 scripts/control-tower/check-dsh-anchor.py --repo tmp/k3probe --tree "$(pwd)"
degraded: 树已移动 — 事实源 46a7f68b，实测 68a19ea4（所有绑旧 HEAD 的结论自动降级『待复核』）
DSH-ANCHOR: DEGRADED                       exit=2
```
事实源缺失/非法 JSON/树路径不存在亦 DEGRADED（夹具 T2/T3/T9 + 本机抽测）。**判别性本体成立。**

### 1.2 【P0】CI 接线错位：两夹具从未被门禁化，且 job 必红

- 位置: `.github/workflows/ci.yml:266-268`（b745 分支）
- 事实: 两行夹具路径插在 `for t in … ; do` **之后**（ci.yml:266 行尾是 `check-progress-freshness.test.sh; do`），即成了**循环体裸命令**，不是测试清单项——不进 `$t`、不设 `FAIL`、无 `::error` 注解。
- 加重: git mode = `100644`（`git ls-files -s` 实测），不可执行；该 step `shell: bash` = `bash --noprofile --norc -eo pipefail` → 首次迭代即 `Permission denied` exit 126，`-e` 直接终止 step。
- 复现（本机模拟 CI 语义）:
```
cd audit-wt/b745 && bash -c 'set -eo pipefail; FAIL=0; for t in tests/control-tower/check-citations.test.sh; do tests/control-tower/check-dsh-anchor.test.sh; tests/control-tower/check-k3-report.test.sh; echo "── $t"; if ! bash "$t" >/tmp/o 2>&1; then FAIL=1; fi; done; echo SURVIVED'
bash: tests/control-tower/check-dsh-anchor.test.sh: Permission denied
simulated-ci-exit=1
```
- 判定: 无论按哪种后果（job 红 → CI 全线阻断；或假设可执行 → 夹具被盲目执行 40+ 次且失败不判红），**"登记进 CI"这一声称不成立**。对照 #746 同一循环的正确写法（ci.yml:266 带 `\` 续行，b746 分支），本 PR 属接线失败（铁律 0-2 Step 5 WIRE CHECK / 铁律 4）。
- 归因: implement ｜ 修复: 两行上移入 for 清单（前一行补 `\`），并本地以 `bash -eo pipefail` 语义仿真跑一遍再推。

### 1.3 【P1】写集偏离：两文件变更未在 D943 write_set 声明

- `docs/synova/coordination/CTO-看板-自动.md`（+12）与 `docs/synova/coordination/派单模板.md`（-152，删除）不在 task-state/D943.json write_set（逐文件比对，`git diff --name-only origin/main...HEAD` × write_set 成员检查）。commit 4fd7fcc9 自述"移出3份规范件"是有意为之，但登记未同步 → 本 PR 应被自己的 merge_writeset_gate（D708）判红（intra-PR 未声明变更）。若 PR 正文已豁免须在正文可见。
- 归因: devdoc（write_set 登记滞后于拆分决定）

### 1.4 【P1】豁免机制单行逃逸向量

- 位置: `scripts/control-tower/check-dsh-anchor.py:76-78` —— 任一行含 `已作废口径表` 或 `superseded` 字样即 `exempt=True`，且**延续到下一个 `## ` 标题**。
- 复现:
```
printf '# 文档\nsuperseded 历史留档\n现状断面为 0.1.7-alpha.2 @ 00102833（本行含 superseded 逃逸字样）\n继续行 9.9.9-zzz\n' > evade.md
→ DSH-ANCHOR: OK   exit=0   （旧断面引用 + 未登记版本串双双静默放过）
```
- 判定: 豁免应锚定"标题行 = `## 引用豁免` 起段"，而非任意行内字样。fail-closed 门禁里开了一扇 fail-open 的窗。
- 归因: implement

### 1.5 K3 机械保险越界判定（派单问 2）

- **未触碰审计标准边界**：check-k3-report.py 全部 5 条规则为机械可判形式（锚点在册、sha 可 `git cat-file -e` 解析、`path:line` 存在且行数不越界、判定行带 file:line/命令/未核实标记、计数带命令块），不评价结论对错，不写判定口径——docstring 自述与实现一致（:2-14）。
- **但"形式合格但实质空洞"的报告可以通过**（本审计实测）：
```
# 报告含: 断面: 0.1.7-rc.1 @ 46a7f68b / commit: <真sha> / | PASS | README.md:1 / ```ls```
→ K3-REPORT: OK   exit=0        （README.md:1 与审计结论可完全无关）
```
  且 R4 只匹配**行首**判定行（check-k3-report.py:30 `RE_VERDICT` `^\s*[|\-*]?…`）——"结论：FAIL，彻底失败" 这类行中判定不被检查（实测 `midverdict exit=0`）。
- 判定（P1 注记，非阻断本 PR）: 该脚本只能作"防低级伪报告"的下限，不得在叙事上升级为"过 check-k3-report = 审计合格"。建议（供 K3 创始人域，本报告只记录不改）：R4 扩到行中判定词；引用行要求引用与被审对象 diff 相关文件。
- 归因: audit（口径宣导）

### 1.6 P2（两条）

- `docs/synova/coordination/DSH-断面.json:7` `current.path` 为本机绝对路径 `/Users/wane/src/deepseek-harness-017` → 任何无该路径的机器（CI / Win 值守）跑 daily-cto-board 恒 DEGRADED（fail-closed 但长期噪音，可能训练出"看板红=正常"的狼来了效应）。
- `scripts/control-tower/daily-cto-board.sh:2` 头注释标 `D948`，而交付归属 D943 —— 编号漂移。

### 1.7 #745 小结

| 面向 | 判定 |
|---|---|
| 门禁本体三态/判别性 | ✅（独立复现 + 作者夹具 10/10、8/8 本机复跑全绿） |
| CI 接线 | ❌ P0（从未被门禁化 + job 必红） |
| 写集一致性 | ❌ P1（2 文件未声明） |
| 豁免逻辑 | ⚠️ P1（单行逃逸向量） |
| K3 保险边界 | ✅ 未越界（覆盖有限，P1 注记） |

**判决: 不通过 → 返回修复。修复面 = ci.yml 两行位置 + write_set 补登/拆分 + 豁免锚定；夹具与门禁本体无需返工。**

---

## 二、PR #746 feat/b3-d922-fixture — 通过

### 2.1 T3/T3c/T3b 判别性（派单问 3）——真实

本机全量复跑（ubuntu 语义 / POSIX 平台）：
```
bash tests/control-tower/install-dsh-preset.test.sh
=== 结果: PASS=32 FAIL=0 SKIP=0 exit=0 ===
D922-FIXTURE: preset=install-dsh-preset T3/T3c=承重 T3b=负控 PASS=32 FAIL=0 SKIP=0
```

**外部变异（改坏被测脚本的漂移检测 → 承重项必红）**——把 install-dsh-preset.sh 的 persona diff 判定改为 `elif false; then`：
```
❌ T3(承重): 结构锚注入 persona 漂移 → --check exit 1 (got exit=0, want exit=1)
❌ T3(承重): 未点名漂移文件
❌ T3(承重): 漂移原因不是 persona 内容不一致…
T3-STATUS: FAIL    （夹具整体 rc=1，已还原被测脚本）
```
T3c 在此变异下仍 PASS 是**正确**行为（T3c 验证夹具自身探针落点，M4 元变异在夹具内防其恒真）。T3b 负控（旧字面量已死 → exit 0）实测 `旧字面量现存命中=0；rc=0`，与"负控非承重"的分流标注一致。块外注入 M2 → 面内计数 0 ✅。**三者区分真实。**

### 2.2 CI 双侧 PASS=32 vs 31+SKIP=1 ——可解释、未掩盖失败

- 差异来源 = T6（POSIX chmod 555 不可写断言）在 windows-latest 不具权限位语义 → 夹具内置平台探针（`POSIX_PERM_ENFORCED`）显式 `D922-FIXTURE-SKIP:` 并经 ci.yml 证据步进 `::notice` + step summary 公开（ci.yml b746: `Publish D922 preset fixture summary` step）。SKIP 是可见的、非静默；两侧 FAIL=0 的前提都经过承重 T3/T3c。
- 夹具登记写法正确（ci.yml for 清单续行 `\`，对照 #745 的错位）。
- 注记（P2）: 本审计无 gh/token，windows 侧 CI 原始日志未能本机取回；差异解释在机制层闭合，采信夹具自证的公开注解通道。
- 运行环境注记: 审计机 macOS（BSD 工具链），夹具内已消除 sed -i/-e 差异（实测无 BSD/GNU 告警）。

**判决: 通过。**

---

## 三、PR #747 fix/m9-baseline-owners — 通过（P1×1）

只审本 PR 增量 commit 5c83a339（栈式 base=#741/feat/m9-gate-integrity 不重审，遵守批次4 §四）。

### 3.1 owner/expires 齐全性

`scripts/control-tower/ci-red-baseline.txt`（b747 定稿）：数据行 5 条，`UNASSIGNED` 命中 = **0**；5/5 条含 `owner=`（4×mac-control-tower，1×mac-coding）+ `expires=2026-10-24` + `disposition_due=2026-09-27`。解析器按键取值（check-gate-integrity.sh:143 `_key_value`），第 6 字段新增不破坏解析。

### 3.2 CT(windows) 定态换证——独立核验属实 ✅

```
curl -sS "https://api.github.com/repos/tangbaobao520/SynovaAgent/check-runs/107618889188"
→ Control Tower Gate Tests (windows-latest)  failure
   started 2026-09-24T11:50:20Z  completed 2026-09-24T12:13:46Z
```
与登记条目逐字一致（换证成立，原 in-progress 措辞已清除）。同一 commit 94426c5b 的后续重跑（id 107670553428 / 107680188738）仍 failure，佐证"跨分支存量红非抖动"。main 侧 `Vitest (2/2)=failure` 现值（107691429566）仍命中登记；`产物新鲜度` 在最新 main 快照缺失，表内已显式注记"缺失 ≠ success，条目保留"——诚实。

### 3.3 【P1】3 日处置 SLA 无物理执法

- 表头声称"到期未出结论 → 由 --ci-reds 判红（物理倒逼）"，但解析器只消费 `expires`（check-gate-integrity.sh:694-698 只取 owner/expires；`disposition_due` 零消费）。`--ci-reds` 不会在 2026-09-27 判红，只会在 2026-10-24（expires）判红。**SLA 是文档承诺，不是门禁。**
- 归因: control-tower ｜ 建议: run_ci_reds 同读 disposition_due 并入过期判红（一处改，不新增脚本）。

### 3.4 P2

表头自称"格式（冻结，勿改）"，本提交自行把冻结格式从 5 字段改 6 字段；check-gate-integrity.test.sh 未加"6 字段/含 disposition_due"的形态断言——冻结格式变更无守卫。

**判决: 通过（P1 记账跟进）。**

---

## 四、机制发现独立复核

### 4.1 发现① infer_did 分支名优先 —— 复现成立

- 位置: `scripts/control-tower/merge_writeset_gate.py:137-156`（main）——分支名命中 D# 即 return，不回退。
- 复现（沙箱 /tmp/k3-infer，任务实体 D928、分支 fix/d929-cto-health-crash）:
```
inferred: D929 branch
sources: None None None        ← 三源全空 → fail-closed
no-branch-did fallback: D928 commit-subject   ← 分支无 D# 时兜底才生效
```
- 判定: **fail-closed 方向正确（误拦不漏拦），但应支持回退**。建议（P1，归因 control-tower）: branch 来源 D# 三源全空时，降级尝试 commit-subject D# 并在诊断输出标注 `did_source=branch→subject-fallback`；两源不一致时**仍不放行**（需显式修正分支名或登记卡）。纯改名 workaround 已被实测证实有效，但把"分支命名规范"变成隐性门禁前置条件而不给诊断，是把成本转嫁给每个撞上的人。
- 无 `--did` 显式覆盖参数（add_argument 仅 base/head/branch/repo-root/pr-body/json，:316-321）——建议一并补。

### 4.2 发现② rerun-failed-jobs 重放旧事件体 —— 代码路径证实

- 位置: `merge_writeset_gate.py:280-300` `resolve_pr_body_text` —— PR 正文**只**来自 `--pr-body` 文件或 `GITHUB_EVENT_PATH` 快照；ci.yml:102-104 未传 `--pr-body` → 完全依赖事件体。
- 本机模拟:
```
GITHUB_EVENT_PATH=/tmp/k3-ev.json（旧正文） → rerun 读到豁免段: '# PR\n## 写集豁免\n- 无\n'
换 /tmp/k3-ev2.json（新正文，含 [declared]） → close/reopen 读到: '…src_a  [declared] 新增豁免\n'
```
- GitHub 侧"rerun 复用原事件载荷"是平台文档行为，本审计无 runner 无法端到端复现——但 CTO/值守实测（改正文→rerun 读不到；close/reopen→4/4 新 run 且 [declared] 命中、零新 commit）与代码路径完全自洽，**采信成立**。
- 判定（P1，归因 control-tower）: 豁免语义绑在易腐快照上 = 脆。建议: ci.yml 用 `gh api`/`GITHUB_TOKEN` 拉实时 PR 正文传 `--pr-body`（改一行）；短期最低成本 = ci.yml 注释写明"改 PR 正文后 rerun 无效，须 close/reopen"。

---

## 五、L4 防线缺口收割

1. **#745 P0（夹具登记错位）本该谁拦？** —— 作者本地从未以 CI 语义（`bash -eo pipefail` + for 清单）跑过登记后的 ci.yml；check-canary-drift 只盯 canary 密封清单，不检查"新增 tests/*.test.sh 是否以续行项进入 for 清单"。缺口：**登记形态无机械查**。建议（归因 control-tower，M 类：命中 M3"建成未接线"）：给 check-canary-drift 或 gate-integrity 加一条——`git diff` 新增的 test 路径必须能在 ci.yml for 清单中以前缀 `\` 续行形态 grep 到。#746 做对、#745 做错，同类错误一窗之隔，正说明缺这道共用防线。
2. **写集声明滞后（1.3）**：拆分决策（"移出3份规范件"）发生在登记之后，无机制强制 write_set 与 diff 对账后才可推——merge_writeset_gate 会在 CI 拦（fail-closed 有效），但摩擦发生在远端而非本地。缺口：本地 pre-push 提示级对账（软提示即可）。
3. **SLA 字段无执法（3.3）**：文案先行、解析器未跟——登记表自述与代码能力漂移，属 M 类"文档-代码漂移"既有模式，强化 check-gate-integrity.test.sh 断言即可。

## 六、判定汇总表

| 项 | 结论 | 级别 | 归因 |
|---|---|---|---|
| #745 门禁本体三态 | 真判真拦（独立复现） | — | — |
| #745 ci.yml 接线错位 | 从未门禁化 + job 必红 | **P0** | implement |
| #745 写集 2 文件未声明 | intra-PR gate 应红 | P1 | devdoc |
| #745 豁免单行逃逸向量 | superseded 字样豁免后续行 | P1 | implement |
| #745 K3 保险越界 | 未越界；空洞报告可过（下限工具） | P1 注记 | audit |
| #745 path 绝对路径 / D948 编号 | 噪音 / 漂移 | P2 | implement |
| #746 T3/T3c/T3b 判别性 | 外部变异证实"改坏即红" | — | — |
| #746 32 vs 31+SKIP=1 | T6 平台 SKIP，公开可检索，未掩盖 | — | — |
| #747 owner/expires 齐全 + 换证 | API 独立核验属实 | — | — |
| #747 SLA 无物理执法 | --ci-reds 不消费 disposition_due | P1 | control-tower |
| 机制① infer_did | 复现成立；建议三源全空回退 + --did | P1 | control-tower |
| 机制② rerun 旧事件体 | 代码路径证实；建议 API 拉实时正文 | P1 | control-tower |

## 七、执行注记

- 运行环境: macOS（BSD grep/sed/od）；所有脚本结论均在本机复跑，未采信任何 agent 自报。
- CI 访问: 无 gh CLI/token——check-runs 公共 API 可达（已用于 3.2 定态核验），PR 页/job 日志/annotations 未能取回，相关项已在正文标注"未核实（无通道）"。
- task-state 写回: D943 卡仅存在于 feat/d943-mechanism 分支（main 无），为避免与 PR 冲突，本次写回**随 #745 修复分支执行**；D922 程序卡维持 spec_done（B3 属其子件，verdict 见本报告 §二）。
- 本报告分支: `k3/batch4-audit`（只含报告），PR 由创始人合并。
