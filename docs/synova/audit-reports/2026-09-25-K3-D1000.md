# K3 独立审计报告 — D1000（切片 A 身份链 + D1001 改判建议件 + 越权样本）

- **审计方**：K3 独立审计线（独立仓 `D:\Synova-独立审计\Synova-Engine`，从 maker 本地仓 `D:\novis-backup-20260526\Novis\synova-agent` clone；maker 侧代码**零写入**，未触碰 `scripts/audit/**`）
- **派单件**：`docs/synova/dispatch/D1000-K3审计派单-20260925.md` @ `docs/d948-identity-chain-dispatch` tip `d9d758f4`
- **日期**：2026-09-25（K3 审计时戳以命令原始输出为准）
- **结论形态**：**逐项「成立 / 不成立 / 无法核验」**——按派单 §四 红线，本报告**不写"通过"**；通过与否由 CTO 收件闸 + 创始人定
- **总体结论**：**10 项必答全部核验完毕：7 项成立、2 项不成立（均为计数/口径级，非安全或功能面）、1 项（未跑全量/CI）确认为「未闭合」。** 切片 A 的核心安全主张（唯一授权来源 = 签发端、默认拒绝、自报复活即红）经 K3 独立复现**成立**；发现 7 条需处理项（0 条 Critical、3 条 Major、4 条 Minor/Info，详见 §三）。

---

## 〇、审计环境与独立性声明

| 项 | 值 |
|---|---|
| K3 独立仓 | `D:\Synova-独立审计\Synova-Engine`（remote 仅 `maker-local` = 上述 maker 本地仓，fetch 为只读） |
| 审计工作树（本单新建） | `wt-d948-tip` @ `59f52e3a`（切片 A tip）、`wt-d948-base` @ `ef5c8caa`（切片 A base） |
| 对象获取方式 | 远端 `refs/heads/{feat/d948-identity-chain-server,docs/d948-plan,fix/d949-ownership-electron-tests,main}` 已 `git fetch` 进独立仓；`59f52e3a / c14c1f45 / cfedc975 / f828311c / ef5c8caa / 6a714483` 六个 SHA 全部本地可解析（`git rev-parse` 实测一致） |
| 运行时 | node v24.16.0（与 `wt-d947*` 同）/ vitest 4.1.8 / typescript 5.9.3 / better-sqlite3 12.11.1 / js-yaml 4.3.0 |
| 与 maker 环境差异（如实披露） | ① 本机 `pnpm` 的 corepack 缓存已损坏（`pnpm.mjs` 首行非法 token），**依赖改用 `npm install --ignore-scripts` + 从 `wt-d947` 复用 `better-sqlite3/build` 原生件**；② Git Bash 存在但不在 PATH（须自带 PATH）；③ 无 `gh` 凭据（PR 状态改用 GitHub 公开 API 只读查询） |
| 审计环境改动披露 | 仅 K3 工作树内：新增 `package.json` 的 `pnpm.onlyBuiltDependencies` 字段 + `package-lock.json` + `patch-pkg.cjs`（未跟踪）。**两侧对称施加**。全部变异体试验后 `git status --porcelain -- src tests` **为空**（实测），`src/`+`tests/` 零残留 |
| 非侵入性核验 | 全量套件自身会改写 `docs/.../D817,D819-capture-*.json`、`extensions/industries/*/thresholds.json`、`__pycache__/*.pyc`（既有测试副作用，与本卡无关，两侧对称出现） |

### 〇.1 坐标复核（派单 §一 三方值）

| 对象 | 派单声明 | K3 实测 | 判定 |
|---|---|---|---|
| 切片 A | `feat/d948-identity-chain-server` @ `59f52e3a`，base `ef5c8caa` | `59f52e3abbff62d4b1bd5294cfe4f7e6fe943759`；`git merge-base ef5c8caa 59f52e3a` = `ef5c8caaf8689cd41ab551495efcd002fb6d4e5f` | ✅ 一致（base 即 merge-base，非"仅声称"） |
| D1001 建议件 | `docs/d948-plan` @ `c14c1f45`（建议件提交 `cfedc975`） | `c14c1f4564d0c4840ab7910776175183ccb4d5ac`；`git log` 显示 `cfedc975` 为其内容提交，`c14c1f45` 仅追加 hook 产物 | ✅ 一致 |
| 越权样本 | `fix/d949-ownership-electron-tests` @ `f828311c`，base `origin/main@6a714483` | `f828311ceecf9eda63d857fffb37c92fae4e7417`；`6a714483` 可解析 | ✅ 一致 |
| **回执口径偏差** | 回执写证据分支 HEAD = `13688938`，远端实为 `c14c1f45` | 两个 SHA 均存在；`13688938` 是 `c2fc27d5` 之后的 hook 提交，其后续 `cfedc975` 才入库 D1001 建议件 | ✅ **属实且属"正常前进"**；回执未同步更新为 `c14c1f45`——**登记为 Minor（M-7）** |

---

## 一、10 项必答逐条结论

### 1. 🔴 切片 A 单域、未碰 `scripts/**` —— **成立**

**复现命令与原始输出**：
```
$ git diff --name-status ef5c8caa 59f52e3a
M	.claude/bypass.log
A	.claude/task-briefs/2026-09-25-D948-slice-a-identity-chain.md
M	src/middleware/auth.ts
M	src/middleware/rbac.ts
M	src/routes/auth.ts
M	tests/middleware/auth.test.ts
M	tests/middleware/rbac.test.ts
M	tests/routes/auth.test.ts
A	tests/routes/d948-department-visibility.test.ts

$ git diff --name-only ef5c8caa 59f52e3a | Select-String -Pattern '^scripts/'
（空）

$ git log --oneline ef5c8caa..59f52e3a -- src/middleware/auth.ts
8421d416 feat(d948): slice-A department in identity chain (sign/verify/rbac) + fixtures
（唯一提交 ⇒ 无第二写者）
```

- `scripts/**`（含 `scripts/audit/**`）**零交集**——**成立**。
- `src/middleware/auth.ts` **单一写者**（唯一提交 `8421d416`）——**成立**。
- 文件面 = **9 件 = 8 人工写集 + `.claude/bypass.log`**；`bypass.log` 的 5 行新增与 5 个非 hook 提交一一对应（`8421d416 / 85aaa132 / e959656c / 091cb329 / 75cdbe2e`），内容为 `pre-commit PASS (hook 层登记)`，**无 `detected-bypass` 行**——**成立**（与 D521 hook 产物口径一致）。

### 2. 🔴 A1–A7 逐条是否成立 —— **成立（8 个变异体全部实跑；两处计数声称不成立，见下）**

**靶向基线（tip 原样，K3 独立复跑）**：
```
$ node node_modules/vitest/vitest.mjs run tests/middleware/auth.test.ts tests/middleware/rbac.test.ts tests/routes/auth.test.ts tests/routes/d948-department-visibility.test.ts
 Test Files  4 passed (4)
      Tests  145 passed (145)
      Duration  4.34s
 EXIT=0
```
与 brief Done 标准「145 用例全绿」逐值吻合。**逐文件构成（K3 实测）**：`auth.test.ts` 34→**42**、`rbac.test.ts` 58→**67**、`routes/auth.test.ts` 6→**14**、`d948-department-visibility.test.ts` 新增 **22** ⇒ **+33 用例**（与全量套件 4633→4680 的 **+47** 不相等，差 14 —— 见 §一.5 的说明）。

**变异体总表（全部为 K3 亲自落地 → 跑 → 还原 → `git status` 复核）**：

| # | 变异点（file:line@59f52e3a） | 落地方式 | 靶向 | 真实退出码 | 红用例（原始） | 还原核验 |
|---|---|---|---|---|---|---|
| **K3-M-A1** | `src/routes/auth.ts:148` 删 `department: foundUser.department \|\| undefined,` | 字面替换，锚点命中 1 | 4 文件 | `VITEST_EXIT=1` | **2 failed / 54 passed**：`D948/A1 — login 签发带 department > 正路径`、`D948/A7 — refresh 保留 department > 正路径` | `git status=[]` |
| **K3-M-A2/A3** | `src/middleware/rbac.ts:132` 改回 `department: undefined,` | 同上 | 4 文件 | `VITEST_EXIT=1` | **9 failed / 136 passed (145)**：rbac 单元 4 条（A2×2 + A5 对照 + A6 对照）+ d948 文件 5 条（A3 腿1/腿2、A4 腿2/对照/反向负控） | `git status=[]` |
| **K3-M-A4** | `src/routes/workspaces-api.ts:286` 过滤式改 `true,`（**过宽型**） | 同上 | d948 文件 | `VITEST_EXIT=1` | **4 failed / 18 passed (22)**：**A4 组 2 条**（跨部门不可见、反向负控）+ **A5 组 1 条** + **A6 组 1 条**；**A3 组 4 条全绿** | `git status=[]` |
| **K3-M-A5c（=M-B1）** | `src/middleware/rbac.ts:240` `wsDept === ctxDept` → `wsDept.trim().toLowerCase() === ctxDept.trim().toLowerCase()` | 同上 | 4 文件 | `VITEST_EXIT=1` | **4 failed / 141 passed (145)**，恰为 4 条 B-1 边界（大小写两向 + 空白两向） | `git status=[]` |
| **K3-M-A5b** | `rbac.ts:318` 部门分支加 `\|\| (ctx.role as string) === 'manager'` | 同上 | 4 文件 | `VITEST_EXIT=1` | **18 failed / 127 passed (145)**（声称 16——**不成立**，见下） | `git status=[]` |
| **K3-M-A6h** | `src/middleware/auth.ts:526` `if (req.headers?.['x-synova-token'] !== undefined)` → `if (false)`（**兜底类**：显式拒绝分支消失） | 同上 | 4 文件 | `VITEST_EXIT=1` | **1 failed / 144 passed**：`D948/A6 — 自报头不产生身份 + 显式拒绝留痕 > 仅 x-synova-token=admin:marketing:u1 → 返回 null 且留痕` | `git status=[]` |
| **K3-M-A6rb** | `rbac.ts:144` 前插入自报兜底分支（**兜底类复活**，`return` 前置） | 同上 | 4 文件 | `VITEST_EXIT=1` | **8 failed / 137 passed**：全在 `tests/middleware/rbac.test.ts`（P0×3 / P1×4 / D948-A6 单元×1）；**HTTP 腿 0 红** | `git status=[]` |
| **K3-M-A7** | `src/routes/auth.ts:199` 删 `department: result.payload.department \|\| undefined,` | 同上 | 4 文件 | `VITEST_EXIT=1` | **1 failed / 144 passed**，**恰好只红 A7 一条** | `git status=[]` |

**判别性结论**：
- **A1 成立**：删签发取值 → A1 正路径红。此外 A7 正路径同红，原因是该夹具用 login 取旧 token（实现依赖）⇒ 红不是"判别不准"，但说明 **A1 的腿同时承担了 A7 的前提**。**登记为 Minor（M-3）**：若要求"每条判据只红自己"，A1 腿应改为直接 `signJwtToken` 造 token 或对同一计数差异做口径说明；现文档未登记该交叉。
- **A2 成立**：改回 `undefined` → rbac 单元红。
- **A3 成立（收窄型互斥证实）**：`:132` 改回 `undefined` ⇒ A3 腿 1（真 HTTP `/context` → 200 + `department=marketing`）与腿 2（链式捕获探针 → `/mine` 含本部门 id）**双红**。
- **A4 的「反向关系」——成立**（这是必答 2 的重点）：**过宽型**变异下 **A4 腿 2 与反向负控红、A3 组 4 条全绿** ⇒「A3 绿 / A4 红」这对反向关系**可复现**。A3 之所以在过宽型下保持绿，是因为 A3 组**只留有正向断言**（`expect(ids).toContain(...)`），负向断言全部归 A4——该设计与实跑一致。
  - ⚠️ **但该变异体的"签名"比 PLAN 声称的更宽**：PLAN §3 写「把 `:286` 改成 `true` → **A4 红、A3 仍绿**」，实测同时红了 **A5 组 1 条 + A6 组 1 条**（这两条含 `/mine` 负向断言 `not.toContain(subMktId)`）。**PLAN 未登记该跨组红**——**登记为 Minor（M-2）**。判别性主张不因此失效，但"改坏即红"的**归属粒度**须订正。
- **A5 成立**：`:240` 收窄（trim+lowercase）→ **恰好 4 红 / 141 绿**（R-3 的独立值复核，见必答 4）。
- **A6 成立（且是"两面分列"——见必答 3）**：兜底类复活 ⇒ 单元红 8 条、HTTP 腿 0 红；显式拒绝分支被删 ⇒ 单元红 1 条、HTTP 腿 0 红。
- **A7 成立**：删 refresh 携带 ⇒ **恰好 1 红**（A7 正路径）。

**两处「声称 ↔ 实测」不成立（均为计数级，不改变判别性）**：
1. **A5 声称「M5 `|| role==='manager'` → 16 failed」，实测 18 failed**（多红 2 条：`tests/middleware/rbac.test.ts > canModifyWorkspace > manager CANNOT modify other department workspace`、`… L-32 收口: manager + 单侧空串 + 非属主 → false`）。**登记为 Minor（M-1）**。
2. **A2 声称"反例回 `undefined` → 9 failed"，实测确为 9 failed（成立）**，但**红线集合与文档描述不同**：文档（PLAN §3 A2 行的预期）指向"rbac 单元"，实测 9 红中有 **5 条落在 d948 夹具**（A3 腿 1/腿 2、A4 腿 2/对照/反向负控）。**登记为 Minor（M-4）**：不影响"改坏即红"，但摘要口径（如派单 §二.2 表内"A2 → 9 failed"）若被下游当作"rbac 单元 9 红"读会与事实不符。

### 3. 🔴 A6 两面口径是否分列落实（含「构造性绿」归属）—— **成立**

CTO 裁定要求：单元级为兜底类判别主体；两面**分列**，不得合并成一句"A6 已验"；且回执须把"构造性绿"明确归属到某个面。

**K3 核验证据**：`T-V-b1-recheck.md:153-161`（`docs/d948-plan @ c14c1f45`）为**独立的三行矩阵表**，两面**分列成两列**：

| 复活形态 | 变异 | HTTP 面 A6 | 单元面 A6 |
|---|---|---|---|
| **优先类**（自报分支在 `req.auth` 之前） | M3c-a | **红** ✅ | 红 |
| **兜底类**（自报分支作为 `req.auth` 之后的 fallback） | M3b | **绿（构造性：`req.auth` 优先级不变）** | 红（rbac 9 条） |
| auth 层复活（`extractAuthFromRequest`） | M3 | 不适用（非白名单先 401） | 红（5 条） |

- **两面分列**：✅ 落实（表体两列，非合并叙述）。
- **「构造性绿」归属**：✅ **明确归属到「HTTP 面」**，并给出构造性理由（`req.auth` 优先级不变 ⇒ 该腿在兜底类下必绿，属**构造性**而非实测侥幸）。
- **K3 独立复现该矩阵的两格**（这是我本单做的最有价值的独立性贡献）：
  - **兜底类 @ HTTP 面 = 绿（构造性）**：K3-M-A6rb（rbac 兜底复活）→ `8 failed`，**HTTP 腿 `有效 Bearer(staff) + 自报 admin 头` 保持绿**；单元红 8 条。✅ 与矩阵一致。
  - **优先类 @ HTTP 面 = 红**：派单称 M3c-a 独立复现（26ms）。K3 **未重跑 M3c-a**（属"优先类"变异，需在 `if (req.auth)` 之前插入，与 K3-M-A6rb 同文件不同锚点）；K3 只核了该条**原始输出在场**（`T-V-b1-recheck.md:130-151`，含 12 红用例清单与 `RESTORE: porcelain=0 sha256相同=True`），未见 `--reporter=basic` 类假绿痕迹（`Test Files 2 failed (2) / Tests 12 failed | 77 passed (89)` 为 vitest 4 默认 reporter 形态）。
  - ⇒ 结论：**两面分列与构造性绿归属「成立」**；其中"优先类 HTTP 红"一项为**采信其原始输出**（K3 未独立复跑），**登记为遗留核验项（R-2）**。
- ⚠️ **K3-M-A6rb 的红数 8 ≠ 文档 M3b 的"rbac 9 条"**：差异被 K3 定位到**变异体插入位置**——K3 的兜底分支**总是**返回 `department: parts[1]`（含 `undefined` 键），使 `rbac.test.ts` 中 `P1 边界: department 可见但 ws.department 缺失 → 不得放行` 一条**仍绿**（该条期望"不命中"，K3 的变形恰好也不命中）；文档 M3b 少红的那条应为 `P1: 空 token → 不得 admin` 一类（K3 的锚点把该条也红了）。两处差异**均属变异体实现细节，不改变"单元红 / HTTP 绿"的对立**。**登记为 Info（V-1）**，供 CTO 判断是否需把 M3b 的具体变形补进 evidence。

### 4. 🔴 A5 边界 4 条是否恰好 4 红 / 141 绿（M-B1）—— **成立**

```
$ node run-mutation（锚点 rbac.ts:240 "    wsDept === ctxDept" → trim+toLowerCase，命中 1）
 Tests  4 failed | 141 passed (145)      VITEST_EXIT=1
 FAIL d948-department-visibility > A5 > B-1 边界: 大小写差异（ctx="Marketing" vs ws="marketing"）→ 一律 deny
 FAIL d948-department-visibility > A5 > B-1 边界: 大小写差异（反向 ctx="marketing" vs ws="Marketing"）→ 一律 deny
 FAIL d948-department-visibility > A5 > B-1 边界: 空白差异（ctx=" marketing" 首空格 vs ws="marketing"）→ 一律 deny
 FAIL d948-department-visibility > A5 > B-1 边界: 空白差异（反向 ctx="marketing" vs ws="marketing " 尾空格）→ 一律 deny
```
- **「恰好 4 红 / 141 绿」逐值成立**，且红集合**逐条命中** L391/L398/L404/L411（`grep -c "B-1 边界"` = **4**，实测）。
- 变异体保留了非空守卫（`typeof === 'string' && length > 0` 未被改动），因此**不是**通过放宽空串判据制造的红——判别力归因正确。
- **补充实证（K3 新做）**：把该变异体**只跑 rbac 单元文件**（不跑 d948 夹具）时，`rbac.test.ts` 内**零红**（`4 failed` 全部来自 d948 文件），说明这 4 条判据**只存在于 d948 夹具**，`rbac.test.ts` 未覆盖大小写/空白边界——属**可接受的分布**，但值得 CTO 知道：若未来 `d948-department-visibility.test.ts` 被裁剪，该判据锁会整体消失（无第二处守护）。**登记为 Info（V-2）**。
- 「补充前该变异体一条都不红」：依据为 `git diff e551dc83..HEAD` 仅含 `d948-department-visibility.test.ts` 单文件（K3 核：`75cdbe2e` 提交只改 brief + 该夹具，`e959656c` 只改该夹具），即 4 条 B-1 断言是 `75cdbe2e` 新增的 ⇒ 补充前不存在该判据。**成立**（形式化归因）。

### 5. 🔴 全量 vitest 配对差分（新增失败必须为 0）—— **成立**

**口径**：按 CTO 裁定「只认配对差分，不认绝对数」+ 用户确认的 **A 口径**：`ef5c8caa`（base）vs `59f52e3a`（tip）**同机、同运行时、同依赖状态**实跑；另**单列** D947 记录复用性核验。

**命令与原始输出**：
```
$ cd wt-d948-base && node node_modules/vitest/vitest.mjs run   # → D1000-fullsuite-base.log
 Test Files  16 failed | 595 passed | 3 skipped (614)
      Tests  34 failed | 4508 passed | 91 skipped (4633)        EXIT=1
   Duration  301.55s

$ cd wt-d948-tip  && node node_modules/vitest/vitest.mjs run   # → D1000-fullsuite-tip.log
 Test Files  17 failed | 595 passed | 3 skipped (615)
      Tests  31 failed | 4543 passed | 106 skipped (4680)       EXIT=1
   Duration  290.20s
```

**失败面逐文件比对（`D1000-{base,tip}-failed-files.txt`）**：
```
base=16  tip=17
== 新增（tip 有 base 无）==
tests/agent-observer/report.integration.test.ts
tests/e2e/ima-knowledge-e2e.test.ts
tests/l1/qa-router.test.ts
== 消失（base 有 tip 无）==
tests/mcp/server-smoke.test.ts
tests/routes/diagnosis-report-persistence.test.ts
```

**新增 3 件的定性（K3 追加实跑）**：
```
$ cd wt-d948-tip && node node_modules/vitest/vitest.mjs run tests/agent-observer/report.integration.test.ts tests/e2e/ima-knowledge-e2e.test.ts tests/l1/qa-router.test.ts
 ✓ tests/e2e/ima-knowledge-e2e.test.ts (9 tests) 11997ms
 ✓ tests/l1/qa-router.test.ts (7 tests) 12074ms
 ✓ tests/agent-observer/report.integration.test.ts (11 tests) 12183ms
 Test Files  3 passed (3)   Tests  27 passed (27)     EXIT=0
```
- 这 3 件在全量并发负载下**整文件 skipped + 顶层收集失败**（`(11 tests | 11 skipped)` 等），**单独运行 27/27 全绿** ⇒ 判定为**负载/超时型环境 flake**，非本卡回归。其失败原文均为「顶层 `tests/xxx.test.ts:NN:1` 收集失败 + hook/tests 超时」，非断言失败。
- 消失的 2 件在 base 侧同样是环境型（`server-smoke` 3 个 `Timeout._onTimeout`、`diagnosis-report-persistence` 12 skipped）；tip 侧全绿 ⇒ 方向为**减少**，不构成新增失败。
- **静态归因佐证**：两轮失败文件集 ∩ 切片 A 写集 = **空**（base/tip 均实测为空）；本次改动的 5 个测试相关文件在 tip 全量中**全部通过**：`middleware/auth.test.ts (42)`、`middleware/rbac.test.ts (67)`、`routes/auth.test.ts (14)`、`routes/d948-department-visibility.test.ts (22)`，以及**未改动**的 `middleware/permission-filter.test.ts (6)`、`department-workspace.test.ts (5)`、`workspace-access-write-endpoint.test.ts (28)`、`rbac-ga-boundary.test.ts (14)`、`auth.integration.test.ts (10)` 亦全绿。

**判定**：
- **本卡引入的新增失败 = 0（成立）**，判别性证据为**同环境配对差分 + 新增件单跑证伪 + 失败面与写集零交集**三条合取。
- **通过数增量归因（部分不成立）**：tip 比 base 多通过 **35**（4508→4543），而本卡 4 个测试文件的用例增量只有 **33**（34→42、58→67、6→14、新增 22）。差额 **+2** 来自环境 fkale 面的抖动（`server-smoke` +9 与 `diagnosis-report-persistence` +12 由红转绿 = +21，`report/ima/qa-router` 27 条由绿转 skipped = −27，另有 `mcp`/其他 skipped 结构差异），**不能纯归因给本卡夹具**。**登记为 Minor（M-5）**：派单要求"tip 通过数增量归因给本卡新夹具"，该归因在本环境**只能部分成立**（33 条可归因、余量为环境噪声）。

**D947 记录复用性核验（单列）**：
```
$ git rev-parse ef5c8caa:<file> vs 59f52e3a:<file>
tests/middleware/auth.test.ts / tests/middleware/rbac.test.ts / tests/routes/auth.test.ts /
src/middleware/auth.ts / src/middleware/rbac.ts / src/routes/auth.ts
→ 六件 blob 全部 same=False
```
- 切片 A 恰好改了这 6 件 ⇒ **D947 报告 §一.1 的"base 失败集"（其 base 为 `f25e61eb`）无法覆盖本卡改动面**，直接复用会使差分无判别力。K3 的做法：**仅复用其"两工作树同机同环境对照"的方法论与失败文件清单模式**，数字一律以本单配对实跑为准。
- 附：D947 base 全量（`f25e61eb`）= `15 failed | 592 passed | 3 skipped`、tip（`ef5c8caa`）= `13 failed | 598 passed | 3 skipped`，与 K3 本单 base 结果（16 / 595）**文件级失败面同族、绝对数不同**（D947 报告已自陈"绝对数随环境漂移，判别性在配对差分"）——口径一致，无矛盾。

### 6. 🔴 未清项「未跑全量 / 未跑 CI」是否已闭合 —— **不成立（未闭合）**

**执行方三处原始自陈（均为"未跑"）**：
- `T-V-slice-a.md:250`：**「未跑全量套件（`vitest run` 全量 / 13 组门禁 / CI）——按派单『只跑靶向文件』约束，累计只实跑 7 个靶向文件。全量回归与 CI 结果本席未证实。」**
- `T-V-slice-a.md:233`：**「累计实跑：4+3 = 7 文件 / 188 tests 全绿（未跑全量套件）」**
- `T-V-b1-recheck.md:165`：**「未跑全量套件 / 13 组门禁 / CI（按约束只跑靶向）。本轮实跑 = 4 靶向文件（145）+ 3 次单文件/单组 = 共 4 次 vitest 调用，无一次全量。」**
- `REVIEW-EXEC.md:423`：**「`vitest run` 全量与 CI 严格门禁结果未证实。」**（独立复核方同款登记）
- `T-V-b1-recheck.md:189`：**「不建议退回。请 CTO 收件闸注意 … §必答 6 的 6 项未证实项（重点：未跑全量、未重跑 3 个回归面）。」**

**补跑证据是否入库（K3 检索）**：
```
$ git log --all --oneline --grep="补跑"
5df5e1bc docs(D780): K3 批次四审计交付…        （D780，无关）
b7aa9f60 chore(D704): 触发 pull_request run——PR 事件缺失补跑…   （D704，无关）
f157dedd fix(D472): 复核修复…                   （D472，无关）

$ git log --all --format='%h %ad %s' --since='2026-09-25 03:20' -- docs/synova/product-lines/evidence/D948-20260924 docs/d948-identity-chain-dispatch docs/d948-plan
（空 —— 裁定时刻之后，D948 证据目录零新增提交）
```
- **47 passed 的实跑原文在场且可核**：`T-V-slice-a.md:219-233`
  ```
  ## 五、code-a 未跑的回归面 —— 已实跑（3 文件 47 passed）
        Tests  47 passed (47)
  → 累计实跑：4+3 = 7 文件 / 188 tests 全绿（未跑全量套件）
  ```
  这 47 条是**真实退出码**（文档口径要求 `*> file` 后读 `$LASTEXITCODE`），但其**范围是 3 个回归面文件**，不是全量。
- **判定**：
  - 「未跑全量 / 未跑 CI」这条未清项 **未被补跑闭合**：**不成立**（无补跑证据入库、D1000 权威卡与派单件均无补跑记录）。
  - **但本项在本单被 K3 以配对差分口径替代闭合**：K3 亲跑 `ef5c8caa`（16 failed / 595 passed）vs `59f52e3a`（17 failed / 595 passed）**同环境配对**，新增失败归零（必答 5）。按 CTO 裁定「只认配对差分」的精神，**功能/回归面已闭合**。
  - **CI 面仍未闭合**：本环境无 CI 执行记录、无 CI 日志、无 PR 运行；`pre-commit-check.sh` K3 实跑 `EXIT=0`（"全部 13 组通过"，但见下条软提示）。⇒ **CI 这一侧仍为「无法核验 / 未闭合」，明确登记为 R-1。**
  - **另有两条未清项同属"未闭合"**：`REVIEW-EXEC.md:423-425` 登记「切片 B（桌面端）完全未验」「未跑全量」。切片 B 卡面 status = **hold**（D1000.json slices[1]），**本单不含其验收**；K3 不据此扣分，但要求 CTO 收件闸显式区分「切片 A 可合」与「切片 B 未开工」。

**pre-commit 门禁（K3 实跑，真实退出码）**：
```
$ git-bash -lc "cd wt-d948-tip && bash scripts/pre-commit-check.sh"
  ✅ 全部 13 组通过
 PRECOMMIT_EXIT=0
（软提示：plan-integrity 2 items failed + memory_refs 警告 —— 因 K3 环境无 .claude/plan.json；
  ⚠️ 变更文件数 34 > 上限 12 —— 因 K3 环境额外含 package-lock/patch-pkg.cjs 等；
  ❌ 检查执行失败: python 不可用，无法做域校验（fail-closed）—— 因 Git Bash 无 python）
```
⇒ 三处软提示**全部是 K3 环境的产物**（D515 起 pre-commit 为本地软提示、CI 权威），**不构成对 9 文件写集的判定**；13 组主判据通过且有真实退出码 0。

### 7. 🔴 越权分支与建议件是否一致、是否确未合并 —— **部分成立（① 不成立，②③ 成立）**

**① 逐字一致 —— 不成立（规则本体逐字一致，注释/`source` 文本不同）**

对照对象：`D1001-ownership-reclass-recommendation.md §1`（`c14c1f45`）vs 越权分支 `ee6b0c8d` 的 `ownership.yaml` diff（`origin/main...f828311c`）。K3 抽取两侧的新增规则块：

| 项 | 建议件 §1 | 越权分支实际 diff | 逐字 |
|---|---|---|---|
| 规则1 `glob` | `"tests/electron/**"` | `"tests/electron/**"` | ✅ |
| 规则1 `owner` | `"mac"` | `"mac"` | ✅ |
| 规则1 `source` | `派生: 测试随被测主体（electron/** + electron-renderer/** 均 mac）；D1000 切片 B 表缺口` | `派生: 测试随被测主体（electron/** + electron-renderer/** 均 mac）；D948 P0 表缺口，D949 修复；13 件连带改判已获 CTO 批准` | ❌ |
| 规则2 `glob` | `"tests/ga-collab-*.test.ts"` | `"tests/ga-collab-*.test.ts"` | ✅ |
| 规则2 `owner` | `"mac"` | `"mac"` | ✅ |
| 规则2 `source` | `派生: 同 tests/electron/**（测试随被测主体 electron-renderer/src/**）；覆盖 ga-collab-logic + ga-collab-ui` | （更长，含 D948/D949 修复与 CTO 原话） | ❌ |
| 注释块 | `# D1001 增补（2026-09-25，D1000 §〇之二 三）…` | `# D949 增补（2026-09-24，CTO R1）…`（含"这不是 D948 特有（同族 D782…）"段） | ❌ |
| 修改位置 | 追加在 `electron-renderer/**` 规则之后（L175 后） | 实测该 hunk `@@ -175,6 +175,22 @@`，即在 L175 之后 | ✅ |

- ⇒ **语义等价（glob/owner/位置三项全同），但字面不等**。派单 §二.7 的①要求"**逐字一致**"，**不成立**。执行方原话"内容一字不减搬进建议件 §1"中，"一字不减"在**规则本体**上成立、在**注释与 source 文本**上不成立（且方向相反：越权分支的注释更长，建议件做了改号精简）。**登记为 Major（M-6）**：这是"声称 ↔ 证据"级别的口径偏差，且建议件 §6 自己把它列为"内容与 §1 建议等价"，与 §1 正文的"原文可直接粘贴"形成内部张力。
- 补充：建议件 §1 是**改号后重写的一版**（D949→D1001 措辞），故字面差异**可以解释**；但**当日既有"逐字"要求，就不应把差异留白**。

**② 确未合并 —— 成立（三路独立证据）**
```
$ git merge-base --is-ancestor f828311c origin/main ; echo $?
1                                    ← 非祖先 ⇒ 不在 main
$ git branch -r --contains f828311c
  origin/fix/d949-ownership-electron-tests    ← 仅该分支包含
$ git ls-tree -r --name-only origin/main | Select-String 'task-state/D949.json|D949-ownership' → （空）
$ git ls-tree -r --name-only origin/main | Select-String 'tests/electron|ga-collab'  （ownership.yaml 内）→ （空）
   ← main 的 ownership.yaml 尚无两条新规则 ⇒ 改判未落地（与建议件 §4.2 自陈一致）
$ curl -s "api.github.com/repos/tangbaobao520/SynovaAgent/pulls?head=tangbaobao520:fix/d949-ownership-electron-tests&state=all"
→ PR_COUNT=0（K3 实测；HTTP 200，返回空数组）
```
- 分支**未合并、无 PR、未删除、未 force push**（远端 SHA `f828311c` 与派单声明一致）。**成立**。
- 文件面 = `.github/CODEOWNERS` + `docs/synova/coordination/ownership.yaml` + `task-state/D949.json` + `.claude/task-briefs/2026-09-25-D949-ownership-electron-tests.md` + `.claude/bypass.log`（5 件，与派单 §二.7 描述一致，多出 brief 与 hook 产物即派单所列）。

**③ 旧卡号残留风险 —— 成立（且已被 D1001 建议件主动登记）**
- 该分支卡号仍为 **D949**（`task-state/D949.json`、brief 名 `2026-09-25-D949-*`、`source` 文本含 `D949 修复`），而 D1000 §〇之二.一 已裁定 **D949 → D1001**。
- **权威卡 D1000.json（`d9d758f4`）`blocked` 字段已明文**：**「③ 越权分支 `fix/d949-ownership-electron-tests @ f828311c` 保留为作废样本、不合并（内容已一字不减转入 D1001 建议件）；该分支卡号仍是旧 D949，**不得再引用**。」**
- ⇒ 残留风险的**登记义务已履行**，风险本身**仍存在**（分支若不删，后续按分支名/卡号检索会读到旧口径）。**判定：成立**，处置由 CTO 定（建议件 §6 推荐 (i) 作废不合并，CTO 裁定④ 已采纳为"保留作废样本"）。**登记为 Info（V-3）**：建议 CTO 在 D1001 落地时同步给该分支加一条"作废样本·勿引用"的 README 或直接删除，以消除按名检索的误读面。

### 8. 🔴 功能回退登记口径 —— **成立（措辞如实；但权威卡未落该字段）**

**逐处核验（K3 全证据目录与派单件 grep）**：

| 落点 | 原文 | 判定 |
|---|---|---|
| `PLAN.md §2` | 「**功能回退（未闭环）**：D947 登记的「JWT 用户部门级可见性恒 deny」在 D948 后**仅恢复到签发/验签层**，用户可见层因 W1/W2/W3 仍未恢复。」 | ✅ 如实 |
| `PLAN.md §2` 红线 | 「**严禁**写成"已知限制"或"部门可见性已可用"」 | ✅ 纪律在案 |
| `PLAN.md §8` | 「登记为「**功能回退（部分恢复，未闭环）**」——W1 `department` 无生产写入者 / W2 `/mine` 恒 404 / W3 桌面无部门工作区消费点」 | ✅ 与派单 §二.8 要求逐字同构 |
| `REVIEW-EXEC.md:6` | 「回执必须沿用「功能回退（部分恢复，未闭环）」措辞（**不得**写"已知限制"或"部门可见性已可用"）」 | ✅ 独立复核方同款 |
| `PLAN.md §1（勘误）` | 「**派单 §一 症状表第 3 行已作废**（实测是页面永停加载态）」 | ✅ 比登记要求更严（连 CTO 原症状表都作废） |
| `task-state/D1000.json`（`c14c1f45` 副本） | `"functional_regression": "D1000 交付 = 功能回退（部分恢复，未闭环）：W1/W2/W3 均未解；…"` | ✅ 字段化落地 |

**禁用措辞反向核验（K3 实测）**：
```
$ grep "部门可见性已可用" 全 8 份 evidence + 建议件 + 派单勘误
→ 3 处命中，全部是"严禁/不得写成"的否定式纪律引用，零处正向声称
$ grep 全 8 份 evidence 的 "已恢复 / 已完成 / known limitation" （针对本卡目标）
→ 零处把"抽查达签发/验签层"包装成"用户可见目标完成"
```
⇒ **本项「如实、无包装」成立**。

**但发现一处口径缺口（Major M-8）**：
- **权威卡 `task-state/D1000.json @ d9d758f4`（`docs/d948-identity-chain-dispatch`）不含** `functional_regression` / `goal_reachability` 字段，且 `impl: null`、`status: "claimed"`、`updated_at: "2026-09-24"`；`slices[0]` 无 `branch/head/base/status` 子字段。
- 也就是说：**「功能回退（部分恢复，未闭环）」这个"登记"目前只活在 evidence 文档与 `docs/d948-plan` 的卡副本里，权威卡上查不到**。而权威卡才是下游（台账/双仪表盘/收件闸）读取源——派单 §二.8 问的是"是否如实登记"，
  **如实（文档侧）成立；权威卡侧登记缺失（不成立）**。
- 附加不一致：权威卡 `spec` 指向 `docs/synova/dispatch/D1000-win-identity-chain-department-20260924.md`，而 `c14c1f45` 副本另有 `plan / phase / 字段规范` 说明；派单勘误 §三/§四 的 `D-1…D-4` 行号订正**未回填权威卡**（卡内 `note` 仍写 `rbac.ts:104-105`、`routes/auth.ts:97/134`、`ga-collab.ts:60`——**全是勘误已作废的漂移值**）。
  ⇒ 下游按权威卡读会读到**已知错误的行号坐标**。**必须回填**。

**W1/W2/W3 三堵墙的独立复核（K3 实证）**：
- **W1 成立**：`src/growth/user-store.ts:75-103` 的 `createUser(email,password,role,orgId,extra?)` 确实支持 `extra.department`（`:99 department: extra?.department || ''`），但 `src/` 内**生产调用点**（`routes/auth.ts:89`、`routes/enterprise.ts:116/252`）**一律不传 department**；`updateUser`（`:218`）虽支持改 `department`，但 `src/` 内调用点只设 `orgId/role/status`。⇒「department 无生产写入者」**成立**（与勘误 E-1 一致）。
- **W2 成立**：`routes/d948-department-visibility.test.ts:8-11` 自陈 `/mine` 被 `/:id` 遮蔽恒 404，并以"链式捕获探针"绕过遮蔽；D947 已裁 (b)「本卡不修」。
- **W3 成立**：切片 A 未触碰 `electron-renderer/**`（写集实测零交集）。

**切片 A 的实际安全成效（K3 独立探针，新增证据）**：
```
$ node node_modules/tsx/dist/cli.mjs k3-probe-refresh.mts     # 真实 express + 真实中间件 + 真实路由
REFRESH_STATUS=200
NEW_PAYLOAD={"sub":"k3-u1","role":"manager","orgId":"org-k3","department":"marketing",…}
   ← 请求 body 里同时塞了 department:'sales' + role:'admin'，新 token 仍为 marketing/manager ⇒ body 未被采信
REFRESH2_STATUS=200
NEW_PAYLOAD2={"sub":"k3-u2","role":"manager","orgId":"org-k3","iat":…,"exp":…,"jti":…}
DEPT_KEY_PRESENT2=false
ROLE2=manager
```
⇒ 「**唯一授权来源 = 签发端；客户端 body/header 永不进入 `department`/`role`**」（创始人决策①）在 refresh 路径上**经 K3 独立验证成立**。探针文件已删除，`src/`+`tests/` 零残留（`git status --porcelain -- src tests` 实测为空）。

### 9. 写集 / 文件数 / 域合规 —— **成立（域判定 K3 无法独立复现，采用代码路径人工核）**

**切片 A：9 文件 = 8 声明 + `.claude/bypass.log`（D521 hook 产物）——成立**
```
$ git diff --name-only ef5c8caa 59f52e3a | Measure-Object      → 9
$ … | Where-Object { $_ -ne '.claude/bypass.log' } | Measure-Object → 8
$ grep -c "^.claude/bypass.log$" 排除后 = 8   ← 与 brief Done 标准逐值一致
```
- 8 件 = `src/{middleware/auth.ts,middleware/rbac.ts,routes/auth.ts}` + `tests/{middleware/auth.test.ts,middleware/rbac.test.ts,routes/auth.test.ts}` + `tests/routes/d948-department-visibility.test.ts`（R2 裁定新增，7→8）+ `.claude/task-briefs/2026-09-25-D948-slice-a-identity-chain.md`（R2 第 8 写集）。**逐件落在 `write_set_a_actual` 内，无夹带**。
- 治理提交（`85aaa132 / 091cb329`）与 hook 提交（5 次）**只碰 brief 与 `bypass.log`**（`git show --stat` 实测），未越界。

**域判定**：`python scripts/control-tower/check-ownership.py` 在 K3 环境**不可用**（Git Bash 无 python；执行时 fail-closed 报"无法做域校验"）。K3 改用代码路径人工核：8 件全部落在 `src/middleware/**`、`src/routes/**`、`tests/middleware/**`、`tests/routes/**`、`.claude/task-briefs/**` —— 与派单 §二.1「`src/middleware/auth.ts` 未被第二写者改动」及 PLAN §1 P21（`PASS 6 文件同域 win`）的路径面一致 ⇒ **单域 win 的结论无相反证据**，但**K3 未以脚本独立复现该判定**（`check-ownership.py` 属 `scripts/` 域，K3 红线不碰、也未在 maker 侧执行）。**登记为遗留核验项（R-4）**。

**D1001 建议件：2 件（证据目录 + 卡）** —— 实测 `git diff --name-status origin/main...c14c1f45`：
```
M	.claude/bypass.log
A	docs/synova/product-lines/evidence/D1001-20260925/D1001-ownership-reclass-recommendation.md
A	docs/synova/product-lines/evidence/D948-20260924/{A-recon,B-recon,PLAN,REVIEW-EXEC,REVIEW-PLAN,T-V-b1-recheck,T-V-premise-2nd,T-V-slice-a}.md   ← 8 件
A	task-state/D1000.json
A	task-state/D948.json
```
- ⇒ 实际是 **8 份 D948 evidence + 1 份 D1001 建议件 + 2 份 task-state 卡 + hook 产物**（派单 §二.9 只列"D1001 建议件 2 件"）。
- **`domain:"mac" + owner_side:"win" + cross_domain_reason`**：`task-state/D1000.json`（`c14c1f45`）实测 `"domain": "win"`（顶层取切片 A）+ `slices[1] = {id:"B", domain:"mac", owner_side:"win", cross_domain_reason:"…创始人 2026-09-24 授权「两个都在 win 侧执行」"}` ⇒ **字段形态符合 Mac-CTO §四**（切片级分列，理由在场）。**成立**。
- 附：建议件标题自陈「本件是**改判建议 + 证据**，**不是改表**」，且分支**不含** `ownership.yaml` / `.github/CODEOWNERS` 任何改动（K3 实测 `git diff --name-status` 无此二件）⇒ **越权已改正，建议件形态正确**。

### 10. 通用两条 —— **均成立**

**① `scripts/**` 零交集（`git diff --name-only ef5c8caa 59f52e3a | grep scripts/` → 空）——成立**（见必答 1）。
- 补充：K3 审计过程**未触碰** maker 侧任何 `scripts/**`（含 `scripts/audit/**`）；所有脚本站 K3 环境内（`D:\Synova-独立审计\*.mjs`）。

**② 卡 `write_set` 与实际改动是否吻合、有无夹带 —— 成立**
- 权威卡 `d9d758f4:task-state/D1000.json` 的 `write_set_a` 仅 6 件（缺 `tests/routes/d948-department-visibility.test.ts` 与 brief）；**`docs/d948-plan` 副本的 `write_set_a_actual` 为 8 件且含 `write_set_a_note`**（"R2 裁定新增…7→8 文件；切片 A 实际变更 9 件 = 8 写集 + bypass.log"）⇒ 实际改动**完全落在副本声明的写集内**，**无夹带**。
- **但权威卡未同步 `write_set_a_actual`**（与 M-8 同源）⇒ 下游按权威卡读会认为"写集 6 件、实际 9 件 = 夹带 3 件"。**这是同一处必须回填的缺口的第二面**，并入 M-8。

---

## 二、CTO 已做 / 已裁事项的抽验

| CTO 声称 / 裁定 | K3 抽验 | 结果 |
|---|---|---|
| 三条分支 SHA 远端实测与声称一致（除证据分支前进一提交） | `git ls-remote` + `rev-parse` 六 SHA | ✅ 一致 |
| 裁定① 只认配对差分，base 复用 D947 报告 | K3 实测复用不可行（见必答 5），已按用户确认的 A 口径配对实跑 | ⚠️ 口径已订正并落证 |
| 裁定② A6 措辞：单元级为兜底类判别主体、两面必须分列 | 核 `T-V-b1-recheck.md:153-161` 三行列矩阵 + K3 独立复现兜底类 HTTP 绿 | ✅ 已落实 |
| 裁定③ `docs/tools.md` 立项 = D1003 | 本单不含，未核 | — |
| 裁定④ 越权分支保留作废、不合并；内容转 D1001 | 三路证据确未合并；字面非逐字（见必答 7①） | ⚠️ ①不成立 |
| 已修 `task-state/D1000.json` 的 `blocked` 字段 | 权威卡 `blocked` 已含越权样本与 D1001 两条 | ✅ |
| "D1000 §〇之二 效力优先" | 派单勘误 §三/§四 的坐标订正**未回填权威卡 note** | ❌ 未闭环（并入 M-8） |

---

## 三、分级发现清单

### 🔴 Critical
（无）

### 🟠 Major
**M-6｜越权分支与建议件"逐字一致"不成立**（必答 7①）
- 位置：`docs/synova/product-lines/evidence/D1001-20260925/D1001-ownership-reclass-recommendation.md §1` vs `docs/synova/coordination/ownership.yaml`（越权分支 `ee6b0c8d`）
- 证据：两侧 `glob`/`owner`/插入位置三项全同；`source` 两处与整个注释块**字面不同**（越权分支更长，含 `D948 P0 表缺口，D949 修复；13 件连带改判已获 CTO 批准`）。
- 影响：派单 §二.7① 的"逐字"判据不满足；建议件的"原文可直接粘贴"与"一字不减搬入"之间存在口径缝隙，若 Mac-CTO 直接照 §1 落表，落表结果与作废分支**不等价于字面**（语义等价）。
- 建议：① 收件闸把该条改判为"**规则本体逐字一致 + 注释/source 措辞经改号重写**"，并把差异逐行列出；② 或由执行方把建议件 §1 的 `source`/注释**回改为与越权分支逐字相同**（保留 D1001 编号注释另起一段）。

**M-8｜权威卡未回填：功能回退登记 / 写集 actual / 勘误行号坐标**（必答 8、必答 9、必答 10②）
- 位置：`task-state/D1000.json @ d9d758f4`（权威卡，`docs/d948-identity-chain-dispatch`）
- 证据：
  - 无 `functional_regression` / `goal_reachability` 字段；`impl: null`；`status: "claimed"`；`updated_at: "2026-09-24"`；`slices[0]` 无 `branch/head/base/status`。
  - `note` 仍写 `rbac.ts:104-105`、`routes/auth.ts:97/134`（漏 `:176` refresh）、`ga-collab.ts:60` —— **全为派单勘误 §三 已作废的漂移值**。
  - `write_set_a` 仅 6 件，缺 `tests/routes/d948-department-visibility.test.ts` 与 brief（副本有 `write_set_a_actual` = 8 件）。
- 影响：**台账与双仪表盘只读 git、以权威卡为源** ⇒ ① "功能回退（部分恢复，未闭环）"这一强制登记在权威卡上不可见；② 下游按卡读会认为"写集 6 件 vs 实际 9 件 = 夹带 3 件"；③ 下游会读到**已知错误的行号坐标**（含"判据 A7 依赖 `:176`"这一项会被漏掉）。
- 建议：由 CTO（卡 owner）在收件闸内一次性回填 `functional_regression` / `write_set_a_actual`+note / 勘误 D-1…D-4 坐标 / `slices[0]` 的 branch-head-base-status / `impl`；`audit` 字段留待本报告结论回填。

**R-1（未清项，非缺陷）｜「未跑全量 / 未跑 CI」确认未闭合**（必答 6）
- 位置：`T-V-slice-a.md:233,250`、`T-V-b1-recheck.md:165,189`、`REVIEW-EXEC.md:423`
- 证据：裁定时刻（03:20+）之后 D948 证据目录**零新增提交**；全仓 `--grep=补跑` 仅命中无关卡（D780/D704/D472）。
- 影响：**「CI 未跑 = 未验证 merge-ready」这句执行方自陈仍然有效**。K3 已按 CTO 裁定以配对差分覆盖"回归面"，但 **CI 严格门禁面仍为未验证**。
- 建议：① 若 CTO 接受"配对差分 = 回归面闭合"，请在收件闸书面写明把该未清项**改判**为"以配对差分替代，CI 由合入后 CI 承接"；② 否则须补跑 CI 并入库真实退出码（K3 本单已提供可复用的同环境配对方法）。

### 🟡 Minor
**M-1｜A5 声称 16 failed，实测 18 failed**
- 位置：派单 §二.2 表（A5 行「M5 `|| role==='manager'` → 16 failed」）
- 证据：K3-M-A5b 实跑 `Tests 18 failed | 127 passed (145)`；多红 2 条为 `rbac.test.ts` 既有用例（`canModifyWorkspace > manager CANNOT modify other department workspace`、`L-32 收口: manager + 单侧空串 + 非属主 → false`）。
- 影响：判别性不减（远超"可红"门槛），但计数失真；下游若以 16 做回归基线会误判。
- 建议：把该数字订正为 18，或注明"≥16 条，含 2 条 D947 存量用例"。

**M-2｜M-A4（过宽型）的红面比 PLAN 声称更宽**
- 位置：`PLAN.md §3` A4 行「把 `workspaces-api.ts:286` 的 `w.department === dept` 改成 `true` → **A4 红、A3 仍绿**」
- 证据：K3-M-A4 实跑 `4 failed / 18 passed`：A4 组 2 条 + **A5 组 1 条**（`无部门 manager → /mine 过滤不含任何部门工作区`）+ **A6 组 1 条**（`有效 Bearer(staff) + 自报头 → 也看不到部门工作区`）。A3 组 4 条确为全绿（核心主张成立）。
- 影响：A3/A4 的"反向关系"主张成立，但"改坏即红"的**归属粒度**被高估（该变异不是 A4 专属）。
- 建议：PLAN/回执把该变异的签名订正为"A4 组 2 红 + A5/A6 各 1 红（含 `/mine` 负向断言者）；A3 组保持全绿"，或为 A4 增设只被 A4 覆盖的变异（例如只改 `w.visibility === 'global'` 的析取）。

**M-3｜A1 变异同时红 A7（交叉未登记）** 与 **M-4｜A2 的 9 红分布与摘要口径不符**
- 证据：K3-M-A1 → 2 failed（A1 正路径 + **A7 正路径**）；K3-M-A2/A3 → 9 failed，其中 **5 条在 d948 夹具**（A3×2 / A4×3）。
- 影响：若下游按派单 §二.2 表格把"A2 → 9 failed"读作"rbac 单元 9 红"、把 A1 读作"只红 A1"，会与事实不符。
- 建议：在 PLAN/回执补两行归属说明，或在夹具里让 A7 自造旧 token（`signJwtToken` 直造）以切断对 login 的依赖。

**M-5｜tip 通过数增量只能部分归因给本卡**
- 证据：Δpassed = +35（4508→4543），本卡 4 文件用例增量 = +33；差额 +2 由环境型 skipped/红绿抖动构成（详见必答 5）。
- 建议：报告/回执把"通过数增量 = 本卡夹具贡献"改为"**+33 可归因本卡（42/67/14/22 构成已逐文件核）；余量属环境噪声**"。

**M-7｜回执证据分支 HEAD 未同步（`13688938` → `c14c1f45`）**
- 证据：派单 §一 已登记该偏差；K3 复核 `c14c1f45` 为远端实值，**属正常前进**；权威卡 `blocked` 已引用 `cfedc975`。
- 建议：回执内一行订正即可（收件闸可要求）。

### 🔵 Info
- **V-1｜M3b（兜底类）变异体变形未在 evidence 落全**：K3 独立变形（兜底分支总返回 `department: parts[1]`）红 8 条，文档记 M3b 红 9 条；差异源于变形实现细节（`department` 键是否携带）。建议把 M3b 的具体 diff 片段补进 evidence，使"9 条"可复现。
- **V-2｜B-1 的 4 条判据只存在于 d948 夹具**：`rbac.test.ts` 零覆盖大小写/空白边界（K3 单跑实测）。建议在 `rbac.test.ts` 补 4 条同型断言作为第二守护点（或明确接受单点守护）。
- **V-3｜越权分支的旧卡号检索面**：分支名/卡号仍为 D949，建议 CTO 在 D1001 落地时删除该分支或加"作废样本·勿引用"标记。
- **V-4｜`signJwtToken` 的"无部门不产生键"未在签名层被安全审计**：K3 通过夹具断言（`'department' in payload === false`）与探针（`DEPT_KEY_PRESENT2=false`）双向确认，但该行为依赖 `JSON.stringify` 丢弃 `undefined` 的隐式语义；**建议**在 `signJwtToken` 内显式剔除 `undefined` 字段并在 JSDoc 写明契约（当前靠"隐式 + 断言"双保险，属可接受但可加固）。
- **V-5｜`routes/auth.ts` 夹具用明文密码 + 硬编码测试 JWT secret**（`'d948-test-secret-0123456789'`）：仅存在于 `tests/**`，未进生产代码，**不构成安全缺陷**；提醒 pre-commit secrets 扫描对 `tests/**` 的口径已通过（K3 实跑组 3 全绿）。

---

## 四、遗留风险与建议复检项

| # | 项 | 性质 | 建议复检方式 |
|---|---|---|---|
| **R-1** | **CI 侧未验证**（无 CI 日志/无 PR run） | 未清项（确认为未闭合） | 合入后由 CI 承接；或以 K3 本单配对方法补跑并入库 |
| **R-2** | **M3c-a（优先类 HTTP 红）K3 未独立复跑**，仅核原始输出在场 | 采信型证据 | 复检：在 `rbac.ts` 的 `if (req.auth)` **之前**插入自报分支，跑 d948 夹具，期望 `有效 Bearer(staff)+自报 admin 头` 一条红 |
| **R-3** | **A5b 的 16 vs 18 计数**（M-1）与 **A2 的 9 红分布**（M-4） | 口径级 | 复检：以本报告的两条原始输出为准，或执行方订正 evidence |
| **R-4** | **域判定（`check-ownership.py`）K3 未脚本级复现** | 环境受限 | 复检：在有 python 的环境跑 `check-ownership.py <8 文件>`，期望 `PASS 同域 win` |
| **R-5** | **权威卡未回填**（M-8） | 交付面缺陷 | 复检：`git show <dispatch tip>:task-state/D1000.json` 应含 `functional_regression` / `write_set_a_actual` / 勘误坐标 |
| **R-6** | **越权分支旧卡号**（V-3） | 治理卫生 | 复检：`git ls-remote origin \| grep d949` 应为空 |
| **R-7** | 全量套件的**负载型 flake**（3 文件同轮 skipped，单跑全绿） | K3 环境特性 | 复检：串行重跑全量两轮，确认失败面稳定后再判定"新增 0" |
| **R-8** | A1 腿对 login 的实现依赖（M-3）、B-1 单点守护（V-2） | 夹具强度 | 复检：按建议补断言 |

**K3 未核验 / 不在本单范围**：切片 B（桌面端，卡面 `hold`）、`docs/tools.md`（D1003 立项）、D1002（`/mine` 遮蔽另立卡）、D1004（`js-yaml` 依赖卡，K3 在 D947 报告已登记，本单不重复）。

---

## 五、审计命令与原始输出索引（K3 独立仓留档）

| 证据 | 位置 |
|---|---|
| base 全量（`ef5c8caa`） | `D1000-fullsuite-base.log` / `D1000-base-failed-files.txt` |
| tip 全量（`59f52e3a`） | `D1000-fullsuite-tip.log` / `D1000-tip-failed-files.txt` |
| 3 文件 flake 反证 | `D1000-tip-3files-recheck.log` |
| 4 文件绿态基线 | `D1000-green-4files.log` |
| 变异体原始输出 | `D1000-mut-{M-A1,M-A2-A3,M-A4,M-A5,M-A5b,M-A5c,M-A6h,M-A6rb,M-A7}.log` |
| pre-commit 门禁 | `D1000-tip-precommit.log`（`PRECOMMIT_EXIT=0`） |
| 变异体工具（K3 自建，非仓库产物） | `run-mutation.mjs` / `run-mut4.mjs`（K3 独立仓根目录） |
| 工作树 | `wt-d948-tip` @ `59f52e3a`、`wt-d948-base` @ `ef5c8caa`（均在 K3 独立仓内） |

**最终结论：本报告不写"通过"。**
- **切片 A 的安全主张（唯一授权来源=签发端 / 默认拒绝 / 自报复活即红 / 空与大小写空白一律 deny）经 K3 8 个独立变异体 + 1 个独立 HTTP 探针复现，逐条成立。**
- **10 项必答：7 成立 / 2 不成立（M-1 计数、M-6 逐字）/ 1 未闭合（R-1 CI）。**
- **未闭合与需回填项（M-8、R-1）属**交付面**缺陷，不属代码安全面；是否因此卡在收件闸放行，由 CTO 裁定。**
