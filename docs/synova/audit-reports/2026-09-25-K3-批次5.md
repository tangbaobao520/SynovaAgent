# K3 独立审计 — 批次 5（2026-09-25）

> 审计员: Kimi K3（零上下文独立会话）｜协议: AUDIT-PROTOCOL.md L1-L4
> 判定口径: **有条件通过 = 未通过**。每条 P0/P1 附 file:line + 复现命令 + 原始输出。
> 审计对象 sha 均为派单指定；CI 结论取自 GitHub 匿名 check-runs/annotations API（2026-09-25 抓取）。
> 运行环境注记: macOS（BSD grep/awk + CommandLineTools python3）本地复现 + GitHub Actions 结论交叉。

---

## 〇、总裁决表

| 对象 | sha | 裁决 | 一句话理由 |
|---|---|---|---|
| #750 PR-2 文档锚+接线 | 22bc1efe | **通过**（附 P1 合并顺序耦合） | 旧锚清零 + CI/pre-commit 双接线物理证实；但其"绿"建立在旧豁免门禁上 |
| #749 收件闸检查单 | 110f0c8a | **通过** | 纯文档件，形态与 #753 豁免口径一致；依赖 #741 的 ci-red-baseline（登记为依赖，非缺陷） |
| #753 豁免逃逸修复 | 3d6f116e | **通过** | T11 main 版 rc=0（逃逸假绿）vs 修复版 rc=1 均本地复现；12/12 绿；无关键字触发残留 |
| #741 M9 gate-integrity | 4866b35e | **未通过** | 头 CI 3 项 failure + 与 main 冲突（rebase 未发生）+ 新发现 P1 夹具假绿（M3） |
| #737 fail-open 修复 | 108288d6 | **未通过** | 修复机制本身实测成立（21/21 绿），但头 CI 红（CT Gate Tests ubuntu failure） |
| #739 alloc 两缺陷 | 8387e955 | **未通过** | 头 CI 双平台红（alloc-task-id PASS=30 FAIL=1 / lock PASS=43 FAIL=1）；本地全绿 → 方言类失败未修净 |
| #743 名字分配 | f7b04d88 | **未通过** | P0×2：夹具自身 3 断言恒红（fail-closed 未生效，本地复现）+ `alloc-task-id.sh:406` 全角紧贴 `$BRIEF_FILE（` unbound 崩溃 |
| D945 预设 bundle | 9f63de0b | **未通过（仅差 CI 红；机制面零发现）** | 全部机制声称实测成立，但头 CI 红（Iron Laws "2 组未通过"）且因 M2 截断匿名不可诊断 |

**派单事实纠错（P0，归因: control-tower）**：派单称 8 对象"均 clean + CI 全绿"。物理证据：#739/#743（CT Gate Tests 双平台 failure）、D945（Iron Laws failure）、#741（3 项 failure）头 CI 全红。取数命令与输出见 §二各节。

---

## 一、逐对象审计

### 1.1 #750（feat/d955-d943-docs-anchor @ 22bc1efe）— 通过 + P1

**声称 1：5 份协调文档旧锚清零。** 证实。分支头部自扫：
```
$ cd wt/d750 && python3 scripts/control-tower/check-dsh-anchor.py --repo . --no-tree-check
DSH-ANCHOR: OK  [事实源 0.1.7-rc.1 @ 46a7f68b ｜ 扫描 212 份]   # rc=0，❌ 计 0
```
改动文档 = CTO-固化-II / CTO-固化-最终方向 / Win侧同步包 / 两侧协作方式 / 派单-W0与首模块（git diff --stat，5 份，与声称一致）。

**声称 2：ci.yml 仓库自扫 + pre-commit 断面门禁接线。** 证实：
- ci.yml:278-283（分支版）：`if ! python3 scripts/control-tower/check-dsh-anchor.py --repo . --no-tree-check; then … FAIL=1`——独立命令、失败置 FAIL、fail-closed。
- scripts/pre-commit-check.sh:1494-1515（分支版）：位置在 PLATFORM-CHECKLIST if/else 之外（规避 fail-open）、PYBIN 三级探测、无 python / 脚本缺失均显式判失败、`--no-tree-check` 语义注释正确（CI 无树，带树必假 DEGRADED 红）。soft_check 传参是"结论串"而非命令串——语义正确（SYNO_CI=1 下转硬）。

**P1（合并顺序耦合，归因: control-tower）**：#750 分支携带的是**旧版豁免门禁**（wt/d750 `check-dsh-anchor.py:76` 仍是 `"superseded" in ln → exempt` 关键字触发）。用 #753 修复版门禁交叉扫 #750 分支：
```
$ python3 <#753版>check-dsh-anchor.py --repo wt/d750 --no-tree-check
DSH-ANCHOR: VIOLATION(3)
  ❌ docs/synova/coordination/审计发现台账-DSH-CTO.md:266 未登记的 DSH 版本串 0.1.3-alpha.1
  ❌ docs/synova/coordination/审计发现台账-DSH-CTO.md:267 未登记的 DSH 版本串 0.1.2-rc.1
  ❌ docs/synova/coordination/已作废口径表.md:8 引用了已 superseded 的断面 0.1.7-alpha.2 @ 00102833
```
这 3 条恰由 #753 的登记出清（DSH-断面.json known_versions 补 0.1.2-rc.1/0.1.3-alpha.1 + 已作废口径表显式豁免段，见 #753 diff）。**要求：#753 不得晚于 #750 合并后单独落下而不带登记**——两 PR 需同批合并或 #753 先行；否则任一单合后 main CI 自扫将红。建议 CTO 在合并序列上显式登记该依赖。

**声称 3：3 份规范件**（规格-M9-门禁三件套-唯一事实源.md / 派单模板.md / 开工指令-Mac小队-D937）——git stat 证实新增/修改。TASK-ROUTING.md 增补 ownership.yaml 唯一源节，指向 `check-ownership.py`（机制在 #745/前批，本文不重审）。

**L4**：无新增防线缺口。#750 的 pre-commit 接线把"接线必须放 if/else 外否则永不执行"写成注释但无夹具保护——P2（可选：加"删掉该段 → 红"的判别断言）。

### 1.2 #749（feat/d955-pr2a-consignment-gate @ 110f0c8a）— 通过

纯新增单文件 `docs/synova/coordination/收件闸检查单.md`（50 行，git stat：仅 +.bypass.log 与本件）。逐项核对：断面一致性先查项与 DSH-断面.json 口径一致；"有条件通过 = 未通过"与本次派单口径一致；`## 引用豁免` 段内为逐行显式条目——与 #753 修复后的豁免语义严格一致（若写成"行内含已作废口径表字样"则会被 #753 判红，本件没犯）。
依赖注记（非缺陷）：§六引用 `ci-red-baseline.txt`——该文件由 #741 首建；#749 先合会出现引用物不存在，属文档引用，不阻断。

### 1.3 #753（fix/d943-anchor-exempt @ 3d6f116e）— 通过

**T11"改坏即红"双侧复现（派单要求重点）**：
```
# main 版门禁（携带关键字触发豁免）跑 T11 夹具：
$ python3 <main版 check-dsh-anchor.py> --repo . --anchor ok.json --tree . --scan-dir s4/
DSH-ANCHOR: OK  [事实源 0.1.7-rc.1 @ 3d6f116e ｜ 扫描 1 份]      ← rc=0 = 逃逸假绿（复现成立）
# 修复版（本 PR）：
  ❌ s4/a.md:3 未登记的 DSH 版本串 0.1.7-alpha.2
DSH-ANCHOR: VIOLATION(1)                                           ← rc=1 = 拦截成立
```
夹具全量：`bash tests/control-tower/check-dsh-anchor.test.sh` → **12/12 绿**（含 T11 rc=1+输出含 `VIOLATION(1)`、T12 rc=0+输出含 `DSH-ANCHOR: OK` 的**双断言** tg()——非仅退出码）。

**机制复核**：修复版 `check-dsh-anchor.py` 豁免只认 `## 引用豁免` 段且段内必须 ≥1 列表条目（EXEMPT_ENTRY_RE，:36-53），段内无条目 → 不生效（fail-closed）。grep 全文确认：`superseded`/`已作废口径表` 仅剩数据语义（superseded 对解析），**无任何关键字触发豁免残留**（M4 复核通过）。附带收紧：ver_re 加 `(?!-)` 尾断言（修 0.1.0-win32 伪命中），并登记台账 266/267 + 已作废口径表豁免段（见 1.1 交叉引用，闭环）。

**P2**：同 file:line 的违规重复打印两次（自扫 main 输出 Win侧同步包:94 出现两行）——展示层去重，不影响判定。

**L4**：批次4 已收割。本 PR 即防线闭合本体。

### 1.4 #741（feat/m9-gate-integrity @ 4866b35e）— 未通过

**① 注解含对账对象 + CI-RED-CHECK 不恒 0**：证实（匿名 annotations API，run 107739867694）：
```
"GATE-INTEGRITY: OK"
"CI-RED-CHECK: 失败检查 1 项；基线命中 1 项；基线条目 5 条；今天 2026-09-24；处置逾期 0 项"
"对账对象 = 114582799d55ffd43a36daf6fa5e1268488752a0 @ 114582799d55ffd43a36daf6fa5e1268488752a0"
```
不再恒 0，证实。注：ref 与 sha 相同（push 事件回落 origin/main 解析为完整 sha → 显示为 sha@sha），语义正确但 `<ref> @ <sha>` 形态退化为 sha@sha——P2（可显示 ref 名）。

**② 参数化/fail-closed/--did**：`bash tests/control-tower/check-gate-integrity.test.sh` → **55/55 绿**，覆盖：
- `--base-ref` 缺参 + 已给 --ci-reds → 显式 `SKIPPED(缺 --base-ref)`，rc=0 但**不伪造判定**（不输出 VIOLATION 也不输出通过——夹具断言含"不判/不放行"）；
- ref 不可解析 → exit 2 + `degraded` 五字段（code=C_BASE_REF_UNRESOLVED）；
- `merge_writeset_gate.py:137-169` `infer_did`：⓪ `--did` 显式覆盖最高优先 → ① 分支名 → ② 提交 subject 回退；`--did` 给了但非 D# 形态 → **fail-closed 拒绝静默改用其它来源**（:163-167）。`merge_writeset_gate.test.sh` 46/46 绿。
- ci.yml:382/409/425/431：REF 取 `pull_request.base.sha`（push 事件回落 origin/main，fetch 失败 fail-closed exit 1）；证据步"对账对象"行**优先占注解位**。

**③ disposition_due 逾期硬门**：夹具 D4 ✅（逾期 → exit 1 + 点名「CI 红处置逾期」；未到期 rc=0；无键不回溯）。

**P0（交付态）：头 CI 红 + 与 main 冲突**。check-runs @4866b35e：
```
total: 12
FAIL: TypeScript + Lint + Iron Laws
FAIL: Control Tower Gate Tests (windows-latest)
FAIL: Control Tower Gate Tests (ubuntu-latest)
```
派单已自知（要求 rebase 重跑）；截至本次审计（2026-09-25 fetch）分支 tip 仍为 4866b35e，**rebase 未发生** → 以现状判：未通过。

**P1（新发现，归因: implement）：M3 夹具假绿（判别性自证是空转的）**。
tests/control-tower/check-gate-integrity.test.sh:548-556 的变异体生成器断言 `len(out)==len(src)-1`（"恰好删掉 1 行"），但 gate 脚本内含 `对账对象 = ` 的行有 **3 处**（:21/:106 注释 + :694 代码行）：
```
$ python3 - <gate.sh> /tmp/mut.sh   # 同款变异逻辑
src lines: 820 out: 817 deleted: 3      ← assert 失败 → Traceback → 变异体文件未写出
```
随后 `bash "$MUT_G"`（不存在的文件）→ rc=127、输出空 → `grep -qF "对账对象 = "` 不命中 → **判 ✅**。即 M3 的 ✅ 在变异从未发生时也会打出（CI 输出里该 Traceback 与 ✅ M3 同现，实锤）。真实判别性由我独立补证（只删 :694 代码行的真变异体输出 0 处 `对账对象 =`），**机制本身有判别性，坏的是夹具**。修复方向：断言改为"删掉的行中恰含 1 行非注释代码行"，或变异失败时判 ❌ 而非空输出默认过。
**L4**：夹具的"变异失败 → 空输出 → 判过"是**fail-open 夹具模式**（M 类：判别性夹具的变异步自身必须 fail-loud）。建议入 AUDIT-FINDINGS-LEDGER 备查同类。

### 1.5 #737（fix/d937-gate-failopen @ 108288d6）— 未通过（机制成立，CI 红挡门）

**修复内容复核（L1）**：组 7a 重写（pre-commit-check.sh:991-1045，分支版）——
- ① `^+++` 非法 ERE 根因修法：候选行提取改 awk 结构化逐文件判定（`+++ b/<path>` 头切文件、.md/.html/自身整文件跳过、兼容 quotepath 引号形态）——**免正则，根除非法 ERE 类**；
- ② grep 退出码三态：rc≥2 → 显式降级（degraded-events.log 落盘 + SYNO_CI=1 转 HARD_FAIL）——不再 `|| true` 吞错；
- ③ 方言无关判据 `a(b` 括号不平衡 + T3p 前置实测（本平台 rc=2 不成立即 fail loud）。

**夹具**：`bash tests/control-tower/gate-failopen-net.test.sh` → **21/21 绿**（本地 macOS）。关键断言亲见：T3 降级 CI strict 计 HARD_FAIL、T3f 无缝合双路径（空 diff + 真实暂存区）、T5b 判别对照（同正文挂 .ts 头 → ❌，证明 T5 的 ✅ 非空转）、家族网 142 .sh / 可判 277 / 非法 0 / 未解析 0、金丝雀抓到 `^+++` 病根模式、ci.yml 密封清单接线断言。pre-commit-check.sh 内残留的 `grep -v '^+++'`（:492）为 **BRE**（`+` 字面量），合法且语义正确，非缺陷。

**P0（交付态）**：头 CI 红：
```
check-runs @108288d6: Control Tower Gate Tests (ubuntu-latest) failure（其余 11 项 success）
注解原文（截断处即 M2 实证）：
  ❌ 应 exit 0, 实际 1 :: 内层失败: ❌ tests/control-tower/gate-failopen-net.test.sh — 模拟红（与 CI 一致）…
  （另有 "71 个测试不在 CI canary 清单" ::warning）
```
ubuntu 失败、windows 过、本地过 → 平台相关；因 M2 截断，失败断言名匿名不可取。**判定：未通过，须修净 CI 再来。**
**L4**：无新缺口（CI 红本身就是防线在干活）。

### 1.6 #739（fix/d938-alloc-task-id @ 8387e955）— 未通过

**本地复现（全部绿）**：alloc-task-id.test.sh 44/44、alloc-task-id-lock.test.sh 7/7、scan-fullwidth-vars.test.sh 32/32（macOS）。
**P0：头 CI 双平台红**（与派单"CI 全绿"直接矛盾）：
```
check-runs @8387e955: Control Tower Gate Tests (ubuntu-latest) failure + (windows-latest) failure
注解原文：
  … alloc-task-id.test.sh: 结果: PASS=30 FAIL=1 SKIP=1
  … alloc-task-id-lock.test.sh: 结果: PASS=43 FAIL=1
  … ❌ 应 exit 0, 实际 1 :: 内层失败: ❌ tests/control-tower/alloc-task-id.test.sh — 模拟红（与 CI 一致）
```
本地全绿 + CI 双平台恒红 → 未修净的平台方言类失败（本卡自称"CI 方言红修复（CT ubuntu/windows）"——commit 8e188c91——未达）。失败断言名因 M2 截断不可匿名取证。
**判定：未通过。** 修 CI 红后，本地机制面（A′ 哨兵反吞退出码、全角扫描器、锁沙箱）无其它发现。

### 1.7 #743（fix/d940-name-allocation @ f7b04d88）— 未通过（P0×2）

**P0-A：夹具自身 3 断言恒红（本地恒复现，非环境）**：
```
$ bash tests/control-tower/alloc-task-id.test.sh
── 9. D940 判别性[真实样本名]: 远端分支占 942 而本地未 fetch → 取号必须拒绝并点名 ──
  ✅ 前置自校验: branch -r 看不见 d942/d943 (0)，ls-remote 看得见 (2)
  ❌ 未 fetch 的远端分支占用 → 拒绝（fail-closed，不发放） — 期望 exit≠0，实际 0（fail-closed 未生效）
  ❌ 点名冲突位置（远端分支全 ref 原文） — 未找到: docs/d942-cto-fixation
  ❌ 拒绝时不建壳 — 不应出现: 已登记:
  结果: PASS=21 FAIL=3
```
脱离夹具直接复现（离线 bare origin，前置成立）：工具不发 942 而发 **D944**（拒绝路径不可达——远端占用被"跳号"消化而非拒绝，与 alloc-task-id.sh:26 注释宣称的"ls-remote 只喂发号前校验、不喂计算表"矛盾）。核心卖点"跨位置拒绝重号"在主场景不可达。
```
$ SYNO_TASK_STATE_DIR=…/work/task-state bash scripts/control-tower/alloc-task-id.sh "判别性-未fetch远端占942"
D944
已登记: …/D944.json (status=claimed)
```
**P0-B：`scripts/control-tower/alloc-task-id.sh:406` 全角紧贴 `$VAR` → unbound variable 崩溃**：
```
406:  echo "brief 骨架已生成: $BRIEF_FILE（填写后开工）"
```
同一次复现的 stderr：`scripts/control-tower/alloc-task-id.sh: line 406: BRIEF_FILE（: unbound variable`。这**正是本批 #737/#739 宣称要根除的模式**（gate-failopen-net.test.sh:11-13 明文写法约束"$VAR 后面紧跟全 ASCII 外字符会崩"、scan-fullwidth-vars.sh 的存在意义）。本卡自带该病根且无扫描器覆盖（#739 未合）。
**P0-C：头 CI 双平台红**（CT Gate Tests failure ×2，同 §1.6 取证法）。
**判定：未通过。** 修复方向：ls-remote 快照的占用语义与跳号语义二选一定死并让夹具/实现一致；:406 改 `${BRIEF_FILE}（`。
**归因**: implement（A/B）+ control-tower（派单事实不实）。

### 1.8 D945（feat/d945-preset-bundle @ 9f63de0b）— 未通过（仅差 CI 红；机制面零发现）

**声称逐条证实（L1/L2）**：
- **bundle 声明行形态**：docs/synova/presets/synova-squad-lead/{package.json, cordis.patch.yml} 入库；`check-preset-bundles.sh --repo`（正确入参下）REPO-OK；`grep -c "agent-team" cordis.patch.yml` = **6**、`grep -c "id: delegation"` = **0**——正解形态（对照一致性报告 §2 的 installed agent-team=0/id-delegation=1 病态）。
- **legacy 零删除 + 备份 sha 复算**：legacy-inventory.md:5 "🔴红线：只读+备份，绝不删除，本件不含任何 rm"；:14/:18 逐文件 sha256+字节数、采集方式为命令原始输出（find|sort + shasum -a 256）；:106/:110 附 CTO 指定 legacy 源 sha 全量复算原文。
- **installer 停写 legacy**：install-dsh-preset.sh:9 "本脚本不再写 legacy，只保留已废弃提示分支"；:16 `--home` 已废弃 → 提示 + exit 2；:146 legacy 只读；:296 安装时**拒绝含 legacy 行的坏 bundle**（D931 delegation 行不得与 Agent Teams 共存）——停写且反向执法。
- **对账点名 delegation 残留 / agent-team 缺失**：consistency-report.md:10 与 §3（:153-159）原文名点 `preset-synova-squad-lead` 的 `delegation` 残留 + `agent-team` 缺失，附 installed vs repo 对照命令输出（installed agent-team=0 id-delegation=1 | repo agent-team=6 id-delegation=0）。
- **B3 夹具承重/负控**：install-dsh-preset.test.sh 本地 **43/43 绿**，末行 `D922-FIXTURE: preset=install-dsh-preset T3/T3c=承重 T3b=负控 PASS=43 FAIL=0 FAILED_IDS=[]`；check-preset-bundles.test.sh 39/39 绿。
- 附带复核：check-preset-bundles.sh 的 REPO-DRIFT 违规确实 exit 1（非 fail-open；初测 rc=0 系本人管道取 tail 退出码的测量错误，已纠正复测 rc=1）。

**P0（交付态）：头 CI 红**：
```
check-runs @9f63de0b: TypeScript + Lint + Iron Laws failure（其余 10 项 success）
注解原文: "2 组未通过（详见上方 ❌ 行）"   ← ❌ 行未进注解（M2 实锤）
```
因 M2 截断，匿名无法定位是哪 2 组。**高概率成因 = 机制发现③（铁律 47 正则误伤）**：D945 brief L7（"运行时侧已由 2026-09-23 的迁移完成"）与 L20（"迁移完成≠内容最新"——**反面教训句**）均命中 `pre-commit-check.sh:775` 的 `迁移.*完成`，SYNO_CI=1 下组 5c 转硬红。此为推断，需开发侧以完整日志证实；但无论成因，**头红即未通过**。修红（或修正则）后本卡可转通过——机制面无需返工。

---

## 二、机制发现四条独立复核（全部成立）

### ① 栈式 PR 无 CI — **证实（P0，归因: control-tower）**
- 证据：`.github/workflows/ci.yml:7`（main）`pull_request: branches: [main]`——按 **base 分支**过滤，base≠main 的 PR 0 run。
- 实证：#747（base=feat/m9-gate-integrity，head 971100c7）：
```
GET /commits/971100c7…/check-runs → total_count: 0
```
本批 #739/#743/#741 同样只有 12 条 push-run、无 PR-run（对照 #750 有 24 条）。叠加发现②，栈式 PR 等于"无 CI + 红了也不可诊断"。修法方向：`pull_request` 去掉 branches 过滤（push 侧保留），或统一改 workflow_run——属控制塔决策，此处仅定罪。

### ② CI 失败消息不可诊断 — **证实（P1，归因: control-tower）**
- 证据：`ci.yml:271` `MSG=$(tail -8 /tmp/ct-out.log | tr '\n' '|' | tr -d '%' | cut -c1-450)`——按字节截断且仅尾 8 行。
- 实证（本轮抓取的注解原文）：
  - #737 @108288d6：`…金丝雀扫描: 1 个 .sh ｜ 可判 1 ｜ 未解析 0 ｜ 未�`（多字节字符被拦腰截断）；
  - #739 @8387e955：只报 `PASS=30 FAIL=1`，**无失败断言名**；
  - D945 @9f63de0b：`2 组未通过（详见上方 ❌ 行）`——❌ 行根本不在注解里，匿名（含 K3）不可诊断。
- 本批 4 个"未通过"裁决中有 3 个的直接成因**无法匿名定位**，全部卡在这条。

### ③ 铁律 47 正则误伤 — **证实（P1，归因: control-tower）**
- 证据：`pre-commit-check.sh:775` `grep -qi "…\|迁移.*完成\|…" "$BRIEF"` → 组 5c `warn_check`，SYNO_CI=1 转硬（:177）。
- 实证：D945 brief `.claude/task-briefs/2026-09-25-D945-preset-bundle-migration.md`:
  - L7: `…运行时侧已由 2026-09-23 的迁移完成（实测 16 条 bundles…` ← 陈述句，非 engine-core 拆分声称
  - L20: `…D931（预设副本漂移 = 迁移完成≠内容最新）…` ← **反面教训句**
  两条均命中。铁律 47 的本意是"声称拆分完成须 grep 证明"，现判据把"提到迁移完成四字"一律当声称——误伤面 = 任何含该词组的 brief。
- 修法方向（供 CTO，不代决策）：模式锚定"本卡/本次/已(完成)拆分|迁移"的完成声称句式，或该组降为 ::warning。

### ④ 豁免逃逸已修 — **证实，且无其它关键字触发残留（#753 通过）**
`check-dsh-anchor.py` 修复版全文复核：豁免仅由 `## 引用豁免` 段 + 段内列表条目驱动；`superseded`/`已作废口径表` 字样不再出现在任何控制流判断里（仅剩数据/注释语义）。T11 双侧复现见 §1.3。

---

## 三、派单已定项确认（只确认，未重判）

- **#744 closed（未合并）**：API 证实 `744 closed merged_at=null` ✓
- **#746 已合入**：API 证实 `746 closed merged_at=2026-09-24`，与 main tip 6a714483 对应 ✓（#736 亦见 main log `Merge pull request #736`）✓
- **main 当前 `DSH-ANCHOR: VIOLATION(4)`、门禁未进 main CI**：本地 main 自扫输出 VIOLATION(4)（CTO-固化-II:17 / CTO-固化-最终方向:81 / Win侧同步包:94×2，3 份文档）✓；main `ci.yml` grep `check-dsh-anchor` = 0 命中（接线只在 #750）✓——即 main 存量 4 条尚无任何 CI 防线，#750 是首个接线，与本派单描述一致。

---

## 四、L4 防线缺口收割（本批新增）

| # | 缺口 | 该拦未拦的防线 | 归因 | 建议机制（查 M1-M8 后：均不命中，属新类） |
|---|---|---|---|---|
| G1 | **派单事实未过物理复核**："CI 全绿"与 4 个红头直接矛盾 | pre-dispatch-check 应含 CI check-runs 抽查 | control-tower | 派单模板加一项：附 `commits/<sha>/check-runs` 结论原文 |
| G2 | **变异体夹具 fail-open**（#741 M3）：变异生成崩溃 → 空输出判 ✅ | 判别性夹具的变异步自身须 fail-loud | implement | 夹具模板约定：变异产物存在性前置断言（`[ -s "$MUT" ]` 否则 ❌） |
| G3 | **栈式 PR 无 CI + 注解截断叠加** = K3 与人均不可诊断 | CI 触发面 + 错误摘要管道 | control-tower | 机制①②合并修：pull_request 全 base 触发 + MSG 按"❌ 行优先"选取而非 tail -8 |

## 五、审计痕迹

- 本地复现工作区：`wt/{d753,d750,d741,d737,d739,d743,d945}`（git worktree，只读审计，未改任何被审分支）。
- 远端取证：GitHub 匿名 REST（check-runs / annotations / pulls），抓取时刻 2026-09-25。
- 红线遵守：未合并、未改被审分支、未改 scripts/audit/；本报告落 K3 audit 分支 PR。
