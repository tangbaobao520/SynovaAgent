# K3 独立审计 — D979 / FIX-011（共享治理文件多卡归属机制，PR 栈）

> 审计员：Kimi K3（零上下文独立会话）｜协议：`docs/synova/coordination/AUDIT-PROTOCOL.md` L1–L4
> 判定口径：**缺一即「有条件通过 = 未通过」**；每条结论附 file:line + 复现命令 + 原始输出。
> 断面: 不涉及 DSH 断面（本栈不触碰 DSH 断面文件；纯控制塔/Bash/Python 门禁件）。
> 被审对象: PR 栈 `#809 → #815 → #820 → #823`（+ `#819`/`#825` 证据支、`#826` 队长收尾件）
> 被审对象 sha: `b16a29d78a3be77daaef492f2eaecb2e95239fe4`（#823 head）/ `f34f3f555957d555df3d2cf6b1c0cbf41342839f`（#820 head）/ `aa98eb7dd9501bd3d9761531e08d4910dfa4e5c0`（#815 head）
> 运行环境注记: macOS 26（arm64）｜GNU bash 3.2.57(1)｜`/usr/bin/python3` = 3.9.6｜全部结论为**本机物理复现**，网络侧结论取自 GitHub 匿名 REST（check-runs / actions runs / annotations，抓取时刻 2026-09-25 17:5x UTC）。

---

## 〇、总裁决

| 项 | 结果 |
|---|---|
| **D979 / FIX-011 整栈裁决** | **未通过**（P0×1 交付态 + 必审点⑥零落地；机制本体四面成立） |
| 前置（栈式平铺后是否取得 CI 结论） | **否** —— 且**已取得的 CI 结论为红**（见 §一） |
| 必审点① 两侧反例真实性 | ✅ 成立（授权放行 / 非授权仍拒，均用真实件 + 真实 hook + 真实 resolver 独立复现） |
| 必审点② 是否 blanket 豁免 | ✅ 不成立（拦截点唯一；双向 + 逐文件强制实测成立；diff 无新增 `exit 0`） |
| 必审点③ 判别性 | ✅ 成立（删声明行必红；变异放行分支必红且**回到原劫持判定**，非崩溃红） |
| 必审点④ 号段生成物 `--check` 幂等 + 反查活性 | ⚠️ 机制成立，**但跨克隆不可复算**（新发现 P1，见 §三 P1-1） |
| 必审点⑤ 迁移零丢失 | ✅ 独立复算成立（非空行 **163/163**、标题 **28/28**，双口径零未命中） |
| 必审点⑥ 「过渡件」标注三处 | ❌ **零落地** —— 代码 / PR 正文 / 迁移件三处**全树 0 命中** |
| 派单前提「全部 base≠main ⇒ CI 证据为零」 | ❌ **不实**（1/7 例外且有绿；另有 2 层存在 push-run CI，其中一层**已红**） |

**机器可读回填（编码 session 可直接读本节行动）**：

```json
{
  "task_id": "D979",
  "pr_stack": [809, 815, 820, 823],
  "evidence_prs": [819, 825, 826],
  "audited_head": "b16a29d78a3be77daaef492f2eaecb2e95239fe4",
  "audit_verdict": "FAIL",
  "verdict_reason": "P0×1（新夹具未登记 CI 密封清单 → Gate Integrity 硬红）+ 必审点⑥零落地；CI 前置=否（已取到的 CI 为红）",
  "checks": {
    "counterexample_authorized": "PASS",
    "counterexample_unauthorized": "PASS",
    "blanket_exemption": "ABSENT(PASS)",
    "discriminative": "PASS",
    "watermark_idempotent": "PASS_IN_ENV / FAIL_CROSS_CLONE",
    "migration_zero_loss": "PASS(163/163, 28/28)",
    "transitional_annotation_3x": "ABSENT(FAIL)"
  },
  "findings": [
    {"id": "P0-1", "owner": "implement+control-tower", "mcode": "M3", "summary": "shared_file_decl.test.sh / gen-watermark.test.sh 未登记 ci.yml 密封清单 → Gate Integrity VIOLATION（CI #820 已红）", "fix_scope": ".github/workflows/ci.yml + tests/control-tower/**"},
    {"id": "P1-1", "owner": "implement", "mcode": "M5", "summary": "gen-watermark --check 跨克隆不可复算（水位随本地 ref 集合变化）", "fix_scope": "scripts/control-tower/gen-watermark.sh"},
    {"id": "P1-2", "owner": "control-tower", "mcode": null, "summary": "D734 栈式两类硬红（#820 跨域 / #823 文件数 21>12）→ 合并序列须逐步重算 base", "fix_scope": "合并流程（scripts/control-tower/check-pr-budget.sh 口径）"},
    {"id": "P1-3", "owner": "control-tower", "mcode": "M2", "summary": "CI 注解截断致失败不可匿名诊断（批次5 后第二次复现，未修）", "fix_scope": ".github/workflows/ci.yml"},
    {"id": "P1-4", "owner": "control-tower", "mcode": null, "summary": "resolve-commit-brief.sh 锚点污染（分支名 d328 被当 D#），开发侧已登记未修", "fix_scope": "scripts/workflow/resolve-commit-brief.sh"},
    {"id": "P2-1", "owner": "devdoc", "mcode": "M7", "summary": "同一迁移两套覆盖口径（164/20 vs 163/28）未标注口径", "fix_scope": "迁移证据模板"},
    {"id": "P2-4", "owner": "control-tower", "mcode": "M7", "summary": "FIX-011 Note 头字段仍 proposed（铁律 49 无物理门禁）", "fix_scope": "memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md"}
  ],
  "next_action": "修复 P0-1（登记 3 个测试进 ci.yml 密封清单）→ 按 #809→#815→#820(+ownership 修复)→#823→证据支 顺序逐步 rebase 过 CI → 落实⑥三处标注 → 提请 K3 复审"
}
```

> 注：`task-state/D979.json` 只存在于本 PR 栈分支（main 无），为避免与 #815/#823 分支冲突，按批次4 先例**不在 main 侧新增/改写该文件**；上示 JSON 即等价机器可读回填。

---

## 一、前置判定：栈式平铺后是否取得 CI 结论？

### 1.1 触发面（物理事实）

```
$ sed -n '3,8p' .github/workflows/ci.yml
on:
  push:
    branches: [main, feat/*]
  pull_request:
    branches: [main]
```

`pull_request` 只对 **base=main** 触发；`push` 只对 **main / feat/\*** 触发。7 层栈的 base/分支逐层核对（GitHub REST `pulls/<n>`）：

| 层 | head 分支 | base | PR-run | push-run |
|---|---|---|---|---|
| #809 | docs/d979-fix011-shared-file-mechanism | **main** | ✅ 1 个（13/13 success） | ✗ |
| #815 | fix/d979-d328-shared-decl | #809 分支 | ✗ | ✗（非 feat/*） |
| #819 | docs/d979-verify-evidence | #815 分支 | ✗ | ✗ |
| #820 | feat/d979-dispatch-tpl-dir | #815 分支 | ✗ | ✅ 1 个（**2 job failure**） |
| #823 | feat/d979-watermark-gen | #820 分支 | ✗ | ✅ 5 个（head run **仍 queued，无结论**） |
| #825 | docs/d979-migrate-verify-evidence | #823 分支 | ✗ | ✗ |
| #826 | docs/d979-lead-closeout | #823 分支 | ✗ | ✗ |

```
$ curl -s ".../commits/feebf651.../check-runs" | jq  →total: 13（全 success）
$ curl -s ".../commits/aa98eb7d.../check-runs" | jq  →total: 0
$ curl -s ".../commits/da144d95.../check-runs" | jq  →total: 0
$ curl -s ".../commits/ed0f9fe8.../check-runs" | jq  →total: 0
$ curl -s ".../commits/ab27195e.../check-runs" | jq  →total: 0
```

### 1.2 结论

1. **#809 的绿不能代表本栈**：其 PR-run 只覆盖 5 个文件（`.claude/bypass.log` / D979 brief / 派单件 / Note / `task-state/D979.json`），**其中 0 行门禁代码**：
   ```
   $ B=$(git merge-base origin/main refs/k3audit/pr809); git diff --name-only $B..refs/k3audit/pr809
   .claude/bypass.log
   .claude/task-briefs/2026-09-26-D979-fix011-shared-file-mechanism.md
   docs/synova/coordination/派单-FIX-011-共享治理文件机制-20260926.md
   memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md
   task-state/D979.json
   ```
2. **承载机制本体的 #815（807 行新增，含 `scripts/commit-msg-check.sh` 改动 + 新解析器 + 两套夹具）自创建至今零 check-run**。
3. **#820 的 push-run 已经跑完并失败**（`actions/runs/36165671710`，created 2026-09-25T17:12:38Z）：

```
Gate Integrity (pattern sentinel + injection fixture + ci-reds)  completed failure   (17:47:04Z)
TypeScript + Lint + Iron Laws                                    completed failure   (17:50:16Z)
Control Tower Gate Tests (windows-latest)                        in_progress  —（30+ min 卡住）
Vitest / Golden Case F1 Gate                                     completed skipped
annotations（匿名可取）:
  [notice] GATE-INTEGRITY: VIOLATION(1)
  [notice] CI-REGISTRY: 测试文件 741（密封面 sh/py 131；ts 面 610）；ci.yml 登记（密封面）52；密封面未登记 79；基线 78 条；基线外新增 1
  [failure] 1 组未通过（详见上方 ❌ 行）        ← ❌ 行不在注解内（M2 截断，匿名不可诊断）
```

4. **K3 侧平铺后 CI 等价复跑 = 红（2 类，均已定位到具体原因，见 §三 P0-1 / P1-2）**：

```
$ cd wt/d979-flat && bash scripts/control-tower/check-gate-integrity.sh --root "$PWD"
VIOLATION: 新增测试未登记 CI 密封清单: tests/control-tower/gen-watermark.test.sh
VIOLATION: 新增测试未登记 CI 密封清单: tests/control-tower/shared_file_decl.test.sh
CI-REGISTRY: ... 基线外新增 2；基线过期 0
GATE-INTEGRITY: VIOLATION(2)

$ cd wt/d979-flat && SYNO_DIFF_BASE=origin/main SYNO_CI=1 bash scripts/pre-commit-check.sh; echo $?
❌ 时间戳顺序: brief 必须早于代码写入: 1 处            ← /tmp 陈旧证据，CI runner 无此文件
❌ D734 PR 预算超限: 6 处
   ❌ ① 变更文件数 21 > 上限 12 —— 拆 PR（禁调高上限）
1
```

> **对 CI 注解「1 组未通过」「VIOLATION(1)」的两处 M2 截断，本审计以本地等价复跑补齐定位**：#820 处 = `共享文件未登记 CI 密封清单: shared_file_decl.test.sh`（1 条）+ `D734 ② 变更跨域 mac+win`（`check-pr-budget.sh` 报 `❌ ② 变更跨域 —— 一个 PR 只许一个域`）；#823 处追加 `gen-watermark.test.sh` 未登记（变 2 条）+ D734 ① 文件数 21>12。

**判定：栈式平铺后未取得 CI 结论；能取到的 CI 证据是红的。**

---

## 二、必审点逐条（原始输出）

### ① 两侧反例的真实性 — 成立

复现方法（**不复用开发侧夹具**）：在平铺树上新建独立 worktree，用**真实文件** `docs/synova/coordination/派单模板.md`（其第 1 行即声明行）+ **真实 `git add` 索引** + **真实 resolver** + **真实 hook** `scripts/commit-msg-check.sh`，并造一张窗口内并发卡夹具 brief（`2026-09-26-D943-k3-concurrent-fixture.md`，Q2 认领该文件）使 `CLAIM_DID=D943`。

```
$ bash scripts/workflow/resolve-commit-brief.sh "$(git diff --cached --name-only)"
.../.claude/task-briefs/2026-09-26-D943-k3-concurrent-fixture.md

$ bash scripts/commit-msg-check.sh /tmp/k3-msg-a.txt        # msg = docs(D979): ...
✅ D328 共享声明放行（D979 FIX-011 ⒝）: 暂存文件逐文件双向声明命中
   DECL_ALLOW
   MSG_DID=D979 CLAIM_DID=D943 FILES=1
   FILE=docs/synova/coordination/派单模板.md SOURCE=index DECL=共享声明: D712, D931, D940, D943, D955, D979 — … HIT=D712,D931,D940,D943,D955,D979 MISSING=(无)
exit=0

$ bash scripts/commit-msg-check.sh /tmp/k3-msg-b.txt        # msg = docs(D999): ...
❌ D328: 提交声明(D999)与暂存文件归属(D943)不一致 — 疑似并行劫持
   DECL_DENY
   MSG_DID=D999 CLAIM_DID=D943 FILES=1
   FILE=docs/synova/coordination/派单模板.md SOURCE=index … MISSING=D999
exit=1
```

⒜ 授权（MSG=D979 / CLAIM=D943）⇒ 放行 ✅　⒝ 非授权（MSG=D999）⇒ 仍拒 ✅ ——**与派单指定的 D# 对完全一致**。
附带核实（派单未要求，但影响可判定性）：今日窗口（2026-09-25..27）内**无任何真实 brief 认领** `派单模板.md`，无夹具时该文件走 `GENUINE=0` 整段跳过（`exit 0` 且无 D328 行）——声明行当前**不产生门禁效果**；#825 §6.5 已自行如实登记该前置限定。

### ② 是否 blanket 豁免 — 不成立

```
$ grep -c "疑似并行劫持" scripts/commit-msg-check.sh        → 1        （唯一拦截点 :149）
$ git show 4a7a7d90^:scripts/commit-msg-check.sh | grep -c "疑似并行劫持" → 1   （修复前）
$ grep -rn "疑似并行劫持" scripts/                          → 仅 :149 一处代码行（另一处是 shared_file_decl.py 文档串）
$ git diff 4a7a7d90^..4a7a7d90 -- scripts/commit-msg-check.sh | grep -E '^\+.*(exit 0|SYNO_|no-verify|--force|\|\| true)'  → 空
$ grep -cE '^\s*exit 0' （改前/改后）→ 2 / 2   （无新增放行出口）
```

「双方 D# 必须出现在**被改文件自身**」——K3 自建 5 组矩阵（真实 index、逐组 `git add`）：

| 组 | 索引态 | 期望 | 实测 |
|---|---|---|---|
| a | probe 只声明 `共享声明: D979` | DENY | `exit=1` `DECL_DENY MISSING=D943` ✅ |
| b | probe 只声明 `共享声明: D943` | DENY | `exit=1` `DECL_DENY MISSING=D979` ✅ |
| c | probe 声明 `D943, D979` | ALLOW | `exit=0` `DECL_ALLOW` ✅ |
| d | probe 双侧 + `派单模板.md` 从 index 删掉声明行 | DENY（逐文件） | `exit=1`；probe 行 `MISSING=(无)`、派单模板行 `MISSING=D943,D979` ✅ |
| e | probe 无声明，**另一文件**声明双侧 | DENY | `exit=1` `MISSING=D943,D979` ✅ |

判定实现见 `scripts/control-tower/shared_file_decl.py:192`（`check_files`，`needed = {msg_did, claim_did}` :202，`if missing` :228）与放行口 `scripts/commit-msg-check.sh:139`。

### ③ 判别性 — 成立（含双向变异）

**③-1 删声明行 ⇒ 必红**（暂存版删掉第 1 行声明，HEAD 版仍含）：

```
$ git show :docs/synova/coordination/派单模板.md | grep -c '^共享声明:' → 0
❌ D328: 提交声明(D979)与暂存文件归属(D943)不一致 — 疑似并行劫持
   FILE=docs/synova/coordination/派单模板.md SOURCE=index DECL=(无) HIT=(无) MISSING=D943,D979
exit=1        # 恢复 index 后复跑 exit=0
```

> 此例同时否证了「HEAD 回退可当后门」的担心：`read_decl_source`（`scripts/control-tower/shared_file_decl.py:171`）**index 优先且命中即定案**，被改文件在 index 中删掉声明后不会下探到 HEAD。

**③-2 变异放行分支 ⇒ 必红，且必须是「回到原劫持判定」而非崩溃红**：

```
M1  把 if [ "$DECL_RC" = 0 ] 改为 if [ "$DECL_RC" = 99 ]（停用放行分支）
  $ bash -n mut1.sh → exit 0（未崩溃）
  授权例 exit=1，输出 ❌ D328 … 疑似并行劫持        （= 原判定，非崩溃红）✅
M2  把 if [ "$DECL_RC" = 0 ] 改为 if [ 1 = 1 ]（放行口恒真）
  $ bash -n mut2.sh → exit 0
  非授权例 exit=0，输出 ✅ D328 共享声明放行        （假放行 → 证明该条件承重）✅
恢复原版：授权 exit=0 / 非授权 exit=1
```

### ④ 号段生成物 `--check` 幂等 + 反查活性 — 机制成立，跨克隆不可复算（新发现）

沙箱（`git init` + `task-state/D901(mac)/D1001(win)/D905(无归属)`）三连跑：

```
$ bash gw.sh --root $SB               # 生成：Mac 水位=D905 / Win 水位=D1001 / 段回退命中=1
$ bash gw.sh --root $SB --check → ✅ 号段水位与仓库逐字节一致   exit=0   （第 1 次）
$ bash gw.sh --root $SB --check → ✅ …                          exit=0   （第 2 次）
$ bash gw.sh --root $SB >/dev/null; bash gw.sh --root $SB --check → ✅ exit=0（再生成后仍逐字节一致）
```

**反查活性**（加一条分支 `docs/d950-mac-work` ⇒ 水位必须动）：

```
$ git branch docs/d950-mac-work && bash gw.sh --root $SB --check
❌ 号段水位与仓库不一致（--check 漂移）: 漂移: …/Mac.md  …/README.md     exit=1
$ grep '水位（段内实测最大 D#）' Mac.md → | 水位（段内实测最大 D#） | D950 |   （D905 → D950）
```

**反查改常量 ⇒ 必须漂移报警**：

```
$ # 变异：render_owner 内 wm = inside[-1] … → wm = 950  # K3 MUTATION
$ bash -n gw-mut.sh → exit 0（非崩溃）
❌ 号段水位与仓库不一致（--check 漂移）: …/K3.md …/Win.md      exit=1
```

**新发现（本轮独立复现，开发侧未登记）**：生成物**不是跨克隆可复算的**——它依赖 `git for-each-ref refs/heads refs/remotes`（`scripts/control-tower/gen-watermark.sh:151`）所指的**本地 ref 集合**：

```
$ cd wt/d979-flat && bash scripts/control-tower/gen-watermark.sh --check
❌ 号段水位与仓库不一致（--check 漂移）:
  漂移: …/号段/K3.md  漂移: …/号段/Mac.md  漂移: …/号段/README.md  漂移: …/号段/Win.md
exit=1
$ bash scripts/control-tower/gen-watermark.sh        # 本 clone 重算
   Mac: 水位=D979（提交件写 D980）  Win: 水位=未定义（提交件写 D1002）  K3: 水位=D927（提交件写 D1000）
$ git branch docs/d980-gov-minimal-dispatch HEAD && bash scripts/control-tower/gen-watermark.sh | grep Mac:
   Mac: 段=D9xx 水位=D980   ← 只补这一条 ref，Mac 水位立刻与提交件对齐
```

即：**同一脚本、同一 task-state、不同 ref 集合 ⇒ 不同水位**。开发侧 `--check` 之所以绿，是因为其 worktree 共享了主 clone 的完整 ref（含 `docs/d980-gov-minimal-dispatch` 等在飞分支）；而 CI runner / 新 clone / 窄 fetch 环境**必然漂移**。这正是 PR #823 未清项 6「生成器未接 CI」的隐形状——一旦按 Phase 2 计划接入 CI，会**恒红**。

### ⑤ 迁移零丢失 — 独立复算成立

被审对象 = `派单模板.md` 目录化（#820 的 `c160a3c7`）。K3 独立复算（未采信开发侧 164/20，亦未采信 T4 的 163/28 数字，两者均为**基线版本口径差异**）：

```
$ git show 1938b13b^:docs/synova/coordination/派单模板.md > /tmp/orig.md   # 声明行落地前的基线
$ grep -c '[^[:space:]]' /tmp/orig.md  → 163        $ grep -c '^#' /tmp/orig.md  → 28
$ git show 1938b13b:…派单模板.md | grep -c '[^[:space:]]' → 164（含新增声明行，= 开发侧口径）
```
逐行/逐标题覆盖（对比件 = 指针 `派单模板.md` + `派单模板/{README,产品,门禁,哨兵,审计}.md`）：

```
orig 非空行 = 163
exact-line 命中 = 163/163  未命中=0
substring  命中 = 163/163  未命中=0
orig '^#' 标题 = 28
标题 exact/substring 未命中 = 0 / 0
声明版（164 行）复算：未命中 = 0
```

**判定：非空行 163/163、标题 28/28，零丢失成立**（双口径一致，非仅子串匹配）。

### ⑥ 「过渡件」标注三处 — 零落地 ❌

```
$ grep -rn "过渡件" . 2>/dev/null | grep -v node_modules | grep -v package-lock
（无输出）
【全树『过渡件』命中文件数】0

$ for f in scripts/control-tower/shared_file_decl.py scripts/commit-msg-check.sh \
           scripts/control-tower/gen-watermark.sh \
           .claude/task-briefs/2026-09-26-D979-fix011-shared-file-mechanism.md \
           docs/synova/coordination/派单-FIX-011-共享治理文件机制-20260926.md \
           docs/synova/coordination/派单模板.md \
           memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md; do
    printf "%-70s 过渡件=%s\n" "$f" "$(grep -c '过渡件' "$f")"; done
→ 7/7 全为 0
```

代码 / PR 正文 / 迁移件三处**全部为零**。7 个 PR 正文逐份复核（GitHub `pulls/<n>` body）亦**无「过渡」二字**；`docs/synova/coordination/派单模板.md`、`docs/synova/coordination/号段/**`、`docs/synova/product-lines/evidence/D979-*/**` 同样 0 命中。
最接近的替代表述（供 CTO 判断是否满足原意，**但不是「过渡件」标注**）：

- `docs/synova/product-lines/evidence/D979-队长收尾-20260926/队长收尾报告.md:72` 「## 四、Phase 2（⒜ `ci.yml` 拆分）— **未做，外部阻塞，附实测**」（在 #826，非 Code/PR 正文/迁移件三处之一）；
- PR #820 正文「声明的价值是**前瞻性**的」；PR #826 正文「判据①有前置限定」；
- 迁移件用词为「**短指针**」（`docs/synova/coordination/号段水位.md` 第 3 行、`docs/synova/coordination/派单模板.md` 正文），语义是「保留文件防断链」，非「过渡件」。

> 归因说明：本项**未发现任何开发侧声称**（无 overclaim），属**派单要求未落地**（归因: implement / devdoc）。按派单「缺一即未通过」口径，本项单独即构成未通过。

---

## 三、本轮新发现（按级别）

### P0-1（交付态，归因: implement + control-tower）新增夹具未登记 CI 密封清单 → Gate Integrity **硬红**

- 事实：`tests/control-tower/shared_file_decl.test.sh` 与 `tests/control-tower/gen-watermark.test.sh` 未登记进 `.github/workflows/ci.yml:224` 起的 `for t in` 密封清单 → `scripts/control-tower/check-gate-integrity.sh:647` 报 `新增测试未登记 CI 密封清单` → job 退 1。
- CI 已实证：#820 push-run 的 `Gate Integrity` job **failure**（`GATE-INTEGRITY: VIOLATION(1)`，即当时唯一新增的 `shared_file_decl.test.sh`）。
- 影响：**与合并顺序无关**——无论怎么拆，只要这两个新测试文件进 main，main 上的 Gate Integrity 就红。这不是栈式口径问题，是交付缺件。
- 修复方向（不代决策）：把 3 个测试登记进 `ci.yml` 密封清单（`tests/control-tower/commit-msg-consistency.test.sh` 亦是历史未登记项，`check-canary-drift.sh` 名单含它）。注意 CT-70 同类（`tests/project/**` 从未接线）已入台账，本条是**同一 M3 形态的新实例**。

### P1-1（归因: implement）`gen-watermark.sh --check` 跨环境不可复算 ⇒ 作为 CI 门禁会恒红/误红

见 §二④。根因 = 输出依赖本地 ref 集合（`:151` for-each-ref），而 ref 集合既不固定也不可复现；且方向性风险更大：**ref 不全的 clone 算出更低的水位**（本 clone Mac=D979 vs 提交件 D980；Win=未定义 vs D1002）⇒ 「下一个建议号」被低估 ⇒ 正是 CT-63「撞号三连」要防的事。
修复方向：`--check` 明确绑定 ref 源（如限定 `refs/remotes/origin/*` + `--prune` 语义），或在生成物头部落 `生成时 ref 指纹` 并把跨环境差异降级为显式 DEGRADED，而非把环境差异当漂移。

### P1-2（归因: control-tower）D734 PR 预算在栈式形态下两类硬红（已在本地定位）

- #820 处：`❌ ② 变更跨域 —— 一个 PR 只许一个域`（`scripts/commit-msg-check.sh` 落 `**` 兜底判 win；该缺口由 #823 的 `ownership.yaml` +8 才补，**在 #820 之后**）；
- #823 起：`❌ ① 变更文件数 21 > 上限 12`（整栈 vs origin/main 口径）。
- CI `push` 事件用 `origin/main` 作 base（`.github/workflows/ci.yml` 的 `SYNO_DIFF_BASE`），因此**栈式 PR 在 CI 权威门禁下结构性必红**；PR #823 风险节已如实登记，但登记 ≠ 解决。
- 要求（不代决策）：合并序列必须是「#809 → #815 → #820（**须带 ownership 修复**）→ #823 → 证据支」且每步以「已合 main 后重算的 diff」过 CI；否则 `#820` 单独合入即红。

### P1-3（归因: control-tower）CI 失败信息 M2 截断复现（同批次5 机制②，未修）

`#820` 的 Iron Laws 注解只有 `1 组未通过（详见上方 ❌ 行）`，❌ 行本身不在注解里；`GATE-INTEGRITY: VIOLATION(1)` 也不点名条目。**匿名（含 K3）无法诊断**，本轮靠本地等价复跑才补出两条具体原因。批次5 已把该项作为机制发现②登记，本次**第二次复现且未修**。

### P1-4（归因: control-tower，开发侧自报未修）`resolve-commit-brief.sh` 锚点污染

分支名全文抽 D#（`fix/d979-d328-shared-decl` 的 `d328` 被当强锚点）⇒ glob `*-D328-*` 命中陈旧 brief。PR #815/#820/#823 三处未清项均已如实登记、明确「不在写集、未修」。K3 复核：登记属实，属**已登记欠账**（非隐瞒）。

### P2-1（归因: devdoc）同一迁移的覆盖数字两套口径并存且未标注口径

`#820` 声称「非空行 164 → 未覆盖 0；真实标题 20」；`#825` 复算为「163 / 28」。两者**都对**，差别仅在「拆前」是否含声明行（163 = 声明前基线，164 = 声明后）+ 标题计数口径（28 = `^#` 全量，20 ≈ 剔代码块内标题）。缺的是一句口径说明 ⇒ 后续核对者必踩。建议迁移证据模板强制写「基线 blob + 计数命令」。

### P2-2（归因: implement）新测试可被本地门禁跑到、但 CI 不跑（机制建成未接线，M3 变体）

`ct-test-gate.sh` 的配对规则覆盖 `scripts/{control-tower,workflow,hooks}/**`：`shared_file_decl.py`↔`shared_file_decl.test.sh`、`gen-watermark.sh`↔`gen-watermark.test.sh` 配对成立（本地 `ct-test-gate.sh` → `SYNC-OK … (2 个脚本)` exit 0）；但 `scripts/commit-msg-check.sh` 不在该目录族，其配对测试 `commit-msg-consistency.test.sh` 无任何配对执法，且三件均不在 CI 密封清单（`check-canary-drift.sh` 列名，`exit 0` 恒不阻断）。

### P2-3（归因: devdoc）写集声明与实际变更的偏差（开发侧自报，K3 确认）

全栈 31 文件中，`docs/authority/DOCS-REGISTRY.yaml`(+29)、`docs/synova/coordination/ownership.yaml`(+8) 未进 brief/task-state 的 `write_set` 数组（仅在 `task-state/D979.json` 的 `note` 里以「CTO 授权」口头登记）；`.claude/task-briefs/…` 的 Q2 自指扩写由 #825 §6.1 主动点名。三处均为**自报并附原始输出**，判 P2（登记瑕疵，非隐瞒）。

### P2-4（归因: control-tower）Note 生命周期未收口（开发侧自报未修）

`memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md` 头字段仍写「状态：proposed」，而机制已落地（`#815` 未清项 1 自报）。铁律 49 的「proposed → implemented」收口无物理门禁。

---

## 四、派单事实纠错

| 派单表述 | 物理事实 | 归因 |
|---|---|---|
| 「全部 base≠main ⇒ CI 证据为零」 | **7 层中 #809 base=main 且 CI 13/13 绿**；#820/#823 分支命中 `push.branches: feat/*` 各有 push-run，**#820 已 concluded failure** | control-tower（派单前未复核 GitHub 侧） |
| 「号段生成物 `--check` 幂等」（隐含跨环境可用） | 仅**同 ref 集合内**幂等；跨 clone/clone 的 narrow fetch 必漂移 | implement |
| 「迁移零丢失 163/163、标题 28/28」 | ✅ 独立复算成立（双口径 0 未命中） | — |

---

## 五、北星第二道（对照 `.claude/PRODUCT-BRIEF.md` 原文机械判断）

| 三问 | 判断 | 依据（PRODUCT-BRIEF 行号） |
|---|---|---|
| ① 服务真实用户场景（GA / 企业主）？ | **间接**：本卡不产生任何面向 GA/企业主的场景，服务对象是**开发组织自身**（多卡并发改共享治理件） | `.claude/PRODUCT-BRIEF.md:18-21`（直接用户=GA，最终受益者=企业主）；本栈 31 文件中 `src/` = **0** 件 |
| ② 让模块更接近终态吗？ | **是**（就控制塔模块而言）：把「共享件被误判劫持」这一**结构性死结**变成可声明的显式机制，属该模块终态推进；但它推进的是**开发基础设施**，不是 Synova 产品模块 | `.claude/PRODUCT-BRIEF.md:38-43`（产品技术骨架 = L1-L5 + 本体层，均为 src/ 侧） |
| ③ 做完后 Synova 还是「驻扎企业、主动诊断、为增长服务」的 Agent 吗？ | **是（不变味）**：本栈零触碰产品行为、零触碰 `src/`，不改数据流、不改专家/哨兵、不改交互面 | `.claude/PRODUCT-BRIEF.md:10-16`、`:25-36` |

**裁决：不构成产品跑偏（非「偏离」，是「与产品无关的解阻基础设施」）。**
**观察项（P2，非本卡缺陷，属投放结构）**：本批 31 文件全部落在 `docs/synova/**`(20) / `tests/control-tower/**`(3) / `scripts/**`(3) / `task-state` / `memory/notes` / `.claude`；`src/` 及产品线（专家/哨兵/诊断链路）**零推进**。若控制塔批次连续占用主力工时，需创始人层面确认工时分配——这是**产品方向**领域，K3 只报事实，不代判断。

---

## 六、L4 防线缺口收割

> 先查模式表（`docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md` §二 M1–M8）：命中已有类 → 只强化该类防线，不新增机制。

| # | 缺口 | 本该拦住它的防线 | 归因 | M 类命中 | 强化方式（不新增脚本） |
|---|---|---|---|---|---|
| G1 | 两个新测试未登记 CI 密封清单 → Gate Integrity 硬红，直到 CI 跑完才暴露 | `ct-test-gate.sh` 只对 `scripts/{control-tower,workflow,hooks}/**` 配对；`.github/workflows/ci.yml` 密封清单**只增不自动**；`check-canary-drift.sh` 恒 `exit 0` | implement + control-tower | **M3**（机制建成未接线） | 强化 M3 已有防线：`ct-test-gate.sh` 的配对成立后，**同一处**再加一条「新 `tests/control-tower/*.test.sh` 必须已在 CI 密封清单」fail-closed 检查（一个免疫细胞，复用既有脚本，不新建） |
| G2 | 栈式 PR 在 CI 权威门禁下结构性必红（D734 ①）且**红信息匿名不可诊断**（M2 截断） | 无：CI 用 `origin/main` 当 base；注解按字节截断只留 N 行摘要 | control-tower | **M2**（声称 vs 事实的可见性面）+ 已有 CT-6/批次5 机制② | 强化 M2：注解发布步改为**优先发布 ❌ 行原文**（而非 `tail -8`），使匿名方（K3）可定位；D734 增加「栈式 base」显式声明口径 |
| G3 | 生成物 `--check` 跨环境不可复算（水位随 refinery 变化） | 生成物契约未声明「依赖哪些 ref」；无环境指纹 | implement | **M5**（环境依赖门禁） | 强化 M5：生成物头部落 `ref 指纹/来源声明`，`--check` 对「ref 集合不一致」报显式 DEGRADED（而非把环境差异当漂移） |
| G4 | 「过渡件」三处标注零落地，但**无任何声称** → 无门禁可抓 | 派单要求无「交付物清单核对」环节；K3 派单外的验收项未进 brief 的 Done 标准（本卡 Done 7 条**不含**⑥） | devdoc + control-tower | **M7**（文档-实现漂移） | 强化 M7：派单件「闭环判据」须逐条映射进 brief Done（本卡 6 条判据只落 5 条，⑥ 缺失即此因） |

> 机制数量收敛检查：G1→M3、G2→M2、G3→M5、G4→M7，**全部命中既有类，无新增 M 类、无新增脚本**；每条只提一个免疫细胞。

**批次5 已收割项的复现情况（防「队列无执行体」）**：机制①栈式无 CI、机制②注解截断、机制④豁免逃逸——其中 ①②**本次第二次复现且未修**（批次5 → 本批），G1/G2 即其续。建议 CTO 对该两项给出「改 or 明确不作」（`AUDIT-FINDINGS-LEDGER.md` §三 CT 队列已有 CT-6 同族）。

---

## 七、审计痕迹与红线遵守

- 材料自收集：GitHub REST（pulls / commits check-runs / actions runs / jobs / annotations）+ `git fetch refs/pull/*/head` 到本地 ref。
- 复现工作区（全部为 K3 独立 worktree，未改任何被审分支内容）：`wt/d979-815`(`aa98eb7d`)、`wt/d979-820`(`f34f3f55`)、`wt/d979-repro`、`wt/d979-826`(`ab27195e`)、`wt/d979-flat`（K3 本地平铺 = `pr823` merge `pr819`/`pr825`/`pr826` = `491c29178dc21ab8d31fb55597cb82d7f016c924`，唯一冲突为 `.claude/bypass.log` 自动追加行，已核各层内容完整性）。
- 沙箱：`/tmp/k3-wm-sb`（号段生成器幂等/活性/变异）、`/tmp/k3-5`（迁移覆盖复算）。
- 审计基线（平铺树）：`python3 scripts/audit/audit-check.py --full` → **PASS:1 WARN:985 FAIL:487**；本栈 `src/` 零改动 ⇒ 全部为存量。
- 红线：未合并任何 PR、未改被审分支、未改 `scripts/audit/**`、未写审计标准；本报告落 K3 audit 分支 PR 由创始人合并。
- 结论机器可读回填：`task-state/D979.json` **仅存在于 PR 栈分支（main 无该文件）**，为避免与 #815/#823 分支冲突，按批次4 先例①本次**不在 main 侧新增该文件**；裁决以本报告 §〇 与下行为准。
  > `D979 audit_verdict = FAIL(P0×1) ｜ 机制本体① ② ③ ⑤ 成立；④ 附加跨环境缺陷；⑥ 零落地；CI 前置=否（已取到的 CI 为红）`

---

*K3 独立审计 · 报告 git 跟踪 · 本报告已过 `scripts/control-tower/check-k3-report.py` 机械门禁（R1-R5）*

> **机械门禁复核口径（供 CTO 复跑）**：本报告中 `scripts/control-tower/shared_file_decl.py`、`scripts/control-tower/gen-watermark.sh` 等引用件**尚只存在于本 PR 栈分支**，故 `check-k3-report.py` 须对**平铺审计树**复跑：
>
> ```
> $ cd wt/d979-flat && python3 scripts/control-tower/check-k3-report.py \
>       docs/synova/audit-reports/2026-09-26-D979.md --repo .   →  K3-REPORT: OK
> ```
>
> 对未合栈的 main 跑会报 `R3 引用不存在` ×4（引用件未落 main），属**报审时点的正常状态**，栈合入后自动消失。`check-k3-report.py` 当前未接入 CI（`.github/workflows/ci.yml` 仅登记其测试文件），故不阻断本报告 PR。
