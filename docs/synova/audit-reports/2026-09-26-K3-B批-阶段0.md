# K3 独立审计 — B 批 · 阶段 0（并行 CTO 线）

> 审计员：Kimi K3（零上下文独立会话）｜协议：`docs/synova/coordination/AUDIT-PROTOCOL.md` L1–L4
> 判定口径：**缺一即「有条件通过 = 未通过」**；每条结论附 file:line / 复现命令 / 原始输出。
> 被审对象（快照 2026-09-26T03:2x +08:00）：

| PR | 分支 | 快照 head | 交付物 |
|---|---|---|---|
| #832 | `chore/D1009-stage0-log-archive` | `661b2c483702bbbe5b8b37c213c3d7e2d48cb44c` | `ci.yml` 归档步 + 3 份日志快照 + `阶段0-日志归档-20260926.md` |
| #833 | `chore/D1010-stage0-report` | `92c155c42d56d95c6224229ae7f84beff7dc6005` | `阶段0-报告-20260926.md` |
| #834 | `chore/D1011-stage0-backlog-classification` | `ec73dc3667643e6807d13e22cb01afb1ac20094c` | `阶段0-积压分类-20260926.md` + `阶段0-C16基线-20260926.json` |
| #835 | `chore/D1012-stage0-selfverify` | `29548dcb952168c77e1c113b4e9873c7fdbb5c7d` | `阶段0-自验报告-20260926.md` |

> 环境：macOS 26（arm64）｜bash 3.2.57｜python 3.9.6。CI 事实取自 GitHub 只读 REST（`GITHUB_TOKEN` 仅 GET），抓取 2026-09-26T03:20–03:31 +08:00。
> **本批 4 条 PR 的 CI 均真未跑**（`runs?head_sha=<40位>` 逐条 = 1 条 `pull_request` / `queued`，无结论）⇒ 本批**不存在** A 批那种「已红却标未跑」的记录失真。

---

## 〇、总裁决

**通过（PASS，限「机制/证据面」）**——B1、B2、B3、B4、B5 五项**全部独立复现成立**，且三项是「推翻既有权威结论」的更正（B2 判据措辞、B3 底账结论、B4 口径收敛），更正方向与物理事实一致。仅 2 项 P2 残余面（见 §七）。

**机器可读回填**
> 注：`task-state/D1009–D1012.json` 只存在于被审分支（main 无）；按批次 4 先例**不在 main 侧新增/改写**（红线：不改被审分支）。下示 JSON 即**等价机器可读回填**。

```json
{
  "task_id": "stage0-batch",
  "prs": [832, 833, 834, 835],
  "audited_heads": {
    "832": "661b2c483702bbbe5b8b37c213c3d7e2d48cb44c",
    "833": "92c155c42d56d95c6224229ae7f84beff7dc6005",
    "834": "ec73dc3667643e6807d13e22cb01afb1ac20094c",
    "835": "29548dcb952168c77e1c113b4e9873c7fdbb5c7d"
  },
  "ci_at_head": {"832": "queued(run 36176522020)", "833": "queued(run 36176205440)", "834": "queued(run 36177248370)", "835": "queued(run 36176086905)"},
  "checks": {
    "B1_artifact": "PASS(1170B / gate-hits.log 21 行 / expires = run 创建 +90d)",
    "B2_hook_hash_and_wording": "PASS(609788b1…/1209B 复现；写入目标不含 bypass.log 零命中；字样 2 处均为注释)",
    "B3_oldhook_selfblock_refuted": "PASS(注入 8 条当日 GATE_FAIL_SOFT → EXIT=0；对照 3 条 detected-bypass → EXIT=1)",
    "B4_C16_dual_metric": "PASS(独立复算 457/3654 @03:30；158/1546 = 副指标 C16-worktree；shallow clone 陷阱已证)",
    "B5_G6_upload_backstop": "PASS(四态夹具复现 ①0 ②0 ③1 ④0；非假绿；残余面见 P2)"
  },
  "findings": [
    {"id": "P2-1", "owner": "implement", "mcode": "M1", "summary": "if-no-files-found: warn + outcome=success ⇒ 「上传 0 文件」不被兜底覆盖（假绿面仍存，窄）"},
    {"id": "P2-2", "owner": "control-tower", "mcode": "M2", "summary": "连续 2 次判定依赖「同分支上一条 completed run」；首次失败可能与归档步引入前的历史 run 配对 → 首推即判红（假红面）"},
    {"id": "P2-3", "owner": "devdoc", "mcode": "M7", "summary": "底账 §A5「关键发现2」/结论先行 §3 结论需按本批实测重述（旧 hook 不构成当日自锁）；§A3 的 pre-push 表述可保留但宜加注"}
  ],
  "next_action": "① 按 §七 修订研究院底账 §A5/§3 措辞 ② 归档步补「0 文件即 warn→升级」判据（可选） ③ 4 条 PR 待 CI 队列恢复后重取结论"
}
```

---

## 一、B1 · artifact 实体 — ✅ 成立（三项数字逐字吻合）

```
$ GET /repos/tangbaobao520/SynovaAgent/actions/runs/36167351846
  status=completed conclusion=failure head_branch=chore/stage0-log-archive-20260926
  head_sha=a16d839eb0a6d8bbb8f8b43d48839376e8d2c3c1
  created_at=2026-09-25T17:28:34Z  updated_at=2026-09-25T18:22:35Z

$ GET /actions/runs/36167351846/artifacts
  gate-logs-36167351846  size=1170  created=2026-09-25T17:45:39Z  expires=2026-12-24T17:28:34Z  expired=False  id=10879601805

$ GET /actions/artifacts/10879601805/zip → 1170 B → unzip
  .claude/gate-hits.log
$ wc -lc .claude/gate-hits.log
      21    2833 .claude/gate-hits.log      ← 21 行、非空
$ head -5 .claude/gate-hits.log
{"time": "2026-09-25T17:45:35Z", "gate": "as any / as never / as unknown as 零容忍（新增，铁律 38；存量独立清理）", "result": "miss", "branch": "HEAD"}
…
```

| 判据 | 声称 | 实测 | 判定 |
|---|---|---|---|
| artifact 名/大小 | `gate-logs-36167351846` / 1170 B | 一致（1170 B，zip 亦 1170 B） | ✅ |
| 内容 | 含 `.claude/gate-hits.log` 21 行非空 | 一致（21 行 / 2833 B） | ✅ |
| expires | 「= run 创建 +90 天」 | **run 创建 17:28:34Z → expires 2026-12-24T17:28:34Z，秒级精确一致** | ✅（注意：**不是** artifact created_at +90d） |
| 限定 | degraded-events.log 不在其中 | 一致（zip 内仅 1 文件） | ✅ |

> 附带更正：自验件 §0-4「无 token 访问被拒（403/401）⇒ K3 需只读 token」——本轮以只读 token 可正常 GET；**匿名 403 是限流，不是权限**（`/rate_limit` 显示 core remaining=0）。属环境事实，非缺陷。

---

## 二、B2 · hook 与生成器同哈希 + 判据措辞 — ✅ 成立（**orerator 复现，哈希逐字一致**）

**方法 B（独立 oracle）**：在隔离夹具 repo 内跑**未改动**的 `scripts/install-hooks.sh`，对生成物取哈希：

```
$ F=/tmp/.../hookgen-fixture ; git init ; cp -R scripts $F/scripts ; bash scripts/install-hooks.sh
  ✅ pre-commit / ✅ commit-msg / ✅ pre-push / ✅ post-commit（安装清单）
$ shasum -a 256 .git/hooks/pre-commit ; wc -c .git/hooks/pre-commit
609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401  .git/hooks/pre-commit
    1209 .git/hooks/pre-commit
（声称值 = 609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401 / 1209 B ⇒ **逐字一致**）
```

**判据措辞（更正版）**：「hook 的**写入目标**不含 `bypass.log`」

```
$ grep -nE '>>[^>]*bypass\.log' .git/hooks/pre-commit
（零命中）                                   ← ✅ 更正版判据成立，且**可机器核**
$ grep -c 'bypass.log' .git/hooks/pre-commit
2                                            ← 字面「字样消失」不成立（2 处均为 D508 说明注释）
$ grep -n '>>' .git/hooks/pre-commit
  7: … >> "$ROOT/.claude/pre-commit-failures.log"
 11: … >> "$ROOT/.claude/gate-soft-warnings.log"
 15: … >  "$ROOT/.claude/last-precommit-success"
```

⇒ 生成器 body（`scripts/install-hooks.sh:43-58`）里必然含那 2 行注释，而阶段 0 明令「`install-hooks.sh` 执行、不修改」⇒ **「字样消失」与「同哈希」两判据不可能同时满足**，**CTO 已接受的措辞更正（实质口径）成立** ✅

- ⚠️ 无法核：**「安装件（`.git/hooks/pre-commit`）与之同哈希」**——安装件在 `.git/` 内、`git ls-files .git/hooks/` = 0（不入版本库），本审计无该机器现场；本轮复现的是**生成器产物**与声称哈希完全一致（= 其自验「方法 B」的可复现性成立）。

---

## 三、B3 · 「旧 hook ⇒ 当日自阻断」— ✅ **声称被推翻，独立复现**

**（a）判据代码核对（line 号逐字吻合）**

```
$ grep -n 'detected-bypass' scripts/pre-commit-check.sh | head -3
241:  # V4.5.1: 只匹配 detected-bypass 行。COMMITTED 行是正常提交成功标记，不是绕过。
242:  BYPASS_COUNT=$(grep -c "${TODAY}.*detected-bypass" "$BYPASS_LOG" …)
$ sed -n '238,256p' scripts/pre-commit-check.sh
238: # GATEKEEPER 检测"本地 --no-verify"只在本地跑；CI 上跳过…
239: if [ -f "$BYPASS_LOG" ] && [ "${GITHUB_ACTIONS:-}" != "true" ]; then
…
254:       exit 1
```

**（b）红队注入：8 条「当日」`GATE_FAIL_SOFT` ⇒ 真跑仍 EXIT=0**

```
$ T=$(date +%Y-%m-%d); for i in 1..8; do echo "$T 10:0$i:00+08:00 | GATE_FAIL_SOFT | exit=1 | branch=redteam-$i" >> .claude/bypass.log; done
注入后今日 GATE_FAIL_SOFT=8 ；注入后今日 detected-bypass=0
$ bash scripts/pre-commit-check.sh ; echo EXIT=$?
  ✅ 绕过审计
EXIT=0                                     ← 无 GATEKEEPER 阻断、无「检测到今日」行
（无任何 [GATEKEEPER] 输出）
```

**（c）对照（证明检查未空转，具判别性）**

```
$ 追加 3 条「当日」detected-bypass
$ bash scripts/pre-commit-check.sh ; echo EXIT=$?
[GATEKEEPER] 检测到今日 3 次 --no-verify 绕过记录
[GATEKEEPER] 请使用: git synova-commit --task-id <D#> --agent claude-code --message '...'
EXIT=1
（清理后 porcelain=0）
```

⇒ 「旧 hook 把软失败写进 `bypass.log` ⇒ **当日自阻断**」**未复现**；旧 hook 的真实危害是**污染证据账本**（101 行 `GATE_FAIL_SOFT` 混在 1,813 行 `bypass.log`），而非自锁 ✅

**（d）研究院底账 §A3 该条结论是否需重述 — ⚠️ 需重述（位置更正 + 结论更正）**

物理定位（`/Users/wane/山河研究院/04-技术研究/取证/取证-SynovaAgent治理机器底账-20260925.md`，1,303 行）：

| 位置 | 原文（摘） | 是否需重述 |
|---|---|---|
| 结论先行 **§3**（:15） | 「旧版把软失败写进 `.claude/bypass.log`，而 `pre-commit-check.sh:239-256` 会因 bypass.log 里的当日记录**硬阻断**——即**旧 hook 会自己把自己锁死**」 | **需重述**（机制不成立：`:242` 只匹配 `detected-bypass`；旧 hook 写的是 `GATE_FAIL_SOFT`） |
| **§A5 关键发现 2**（:340-365） | 同结论 + 「两处读日志的代码都在」 | **需重述**（同上） |
| **§A3**（:178） | 「`control-tower/check-bypass-log.sh` — bypass.log 执行证据链对账（D331，硬阻断）」 | **可不改但宜加注**：该表述本身正确（对账按 **commit hash** `grep -q "$h"`，见 `check-bypass-log.sh:102`；`GATE_FAIL_SOFT` 行无哈希 ⇒ 结构上不可能致阻断）。建议加一句「非哈希行不计入对账」以消歧 |
| §A5 表（:401） | 「`--no-verify` … → 写 bypass.log 的 `detected-bypass` 行 → 当日后续提交被 GATEKEEPER 硬阻断」 | **正确，保留**（本轮对照实验证实 EXIT=1） |

⇒ 派单所指「§A3 该条结论」在底账中的**落点是 §A5 关键发现2 + 结论先行 §3**（§A3 仅涉 pre-push 链，措辞无害）。**该条的「自锁」因果须整条重述**，并建议在 §A3 加注防再误读。

---

## 四、B4 · C16 双指标口径收敛 — ✅ 成立（本机独立复算）

**（a）独立复算（CTO 给定命令逐字，全量非 shallow clone）**

```
$ git rev-parse --is-shallow-repository → false ; .git = 177 MB
as_of_start 2026-09-26T03:30:33+0800
C16-remote: 457 3654
elapsed=17s
as_of_end   2026-09-26T03:30:50+0800
```

**（b）三读数收敛判定**

| 读数 | 实为 | 是否可比 | 结论 |
|---|---|---|---|
| 158 / 1,546（小队 @01:13:03，断面 `ee721b0a`） | **C16-worktree（副指标，口径A）**——只数含 `branch refs/heads/<b>` 行的 worktree | **与「457/3,4xx」不可比**（分母：工作树 vs 远端分支） | 报告 §2.0 已并列两指标并注明不可混用 ✅ |
| 447 / 3,402（早读）→ 457 / 3,630（CTO @02:48）→ 457 / 3,644（小队复跑 @02:58） | **C16-remote（主判据）** | 可比，**须绑 as_of** | 分支数 447→457（新分支出现），提交总数单调上升 ⇒ 属**同口径的时间漂移** |
| **457 / 3,654（K3 @03:30:33–03:30:50）** | 同上 | 可比 | **与声称同量级、同分支数** ✅ 独立佐证成立 |

**（c）新发现（P1，供 C16 判据使用者）**：**浅克隆会让本指标翻倍**

```
同一命令、同一 as_of 附近，在 shallow clone 上运行：
C16-remote: 470 7409        ← 5,9×倍（.git/shallow 截断共同祖先，rev-list --count 失真）
（git fetch --unshallow 后）C16-remote: 457 3654
```

⇒ 若 C16 未来变成**自动判据**，`actions/checkout` 默认 `fetch-depth: 1`（shallow）会给出**约 2 倍虚高**的「未合并提交数」；必须在取数前断言 `git rev-parse --is-shallow-repository = false`，否则指标不可用。**归因：control-tower（M1：环境依赖门禁）**。

---

## 五、B5 · G6 上传失败策略（甲）— ✅ 成立（非假绿），附 2 项残余面

**（a）机制原文（`ci.yml`，快照 head）**

```
116:        continue-on-error: true                 ← 甲：归档步失败不阻断 job
117:        uses: actions/upload-artifact@v4
119:          name: gate-logs-${{ github.run_id }}
122:          if-no-files-found: warn
123:          retention-days: 90
124:          include-hidden-files: true
131:      - name: Archive failure backstop (2 consecutive ⇒ red)
          if [ "${ARCHIVE_OUTCOME:-}" = "success" ]; then …exit 0
          失败 → ::warning「本次失败（黄灯，计数 +1）」
          取「同分支上一条 completed run」→ 查其 artifacts
            has     → 「计数 1（黄灯，不红）」exit 0
            missing → ::error「连续第 2 次失败，升级为红」exit 1
            unknown → ::warning「不升级（显式 unknown）」exit 0
```

**（b）独立四态复现**（从 `ci.yml` 抽取该 step 的 `run` 原文到 `/tmp/k3-20260926/backstop.sh`，用其自带注入缝 `ARCHIVE_PREV_RUNS_JSON/ARCHIVE_PREV_ARTIFACTS_JSON` 直跑，**未打 API**）

```
── ① 归档 success ⇒ 计数归零 ──            ✅ 归档步 success ⇒ 计数归零（不升级）        EXIT=0
── ② 失败 + 上一条有 artifact ⇒ 黄灯 ──    ::warning …（黄灯，计数 +1）
                                            ℹ️ 上一条 completed run（id=111）有 gate-logs-* ⇒ 本次为计数 1
                                            EXIT=0
── ③ 失败 + 上一条无 artifact ⇒ 红 ──      ::error title=gate-logs 归档连续 2 次失败::上一条 completed run（id=111）
                                            也无 gate-logs-* ⇒ 连续第 2 次失败，升级为红（真问题，非瞬时抖动）。
                                            失败步=archive(outcome=failure)；判定来源=runs/111/artifacts
                                            EXIT=1
── ④ 历史不可解析 ⇒ unknown ──             ::warning … 不升级（显式 unknown）            EXIT=0
```

**（c）「是否变成假绿」判定**

- **否（非假绿）**：单次失败 = 黄灯（甲的有意取舍，且**打印原因**）；**连续 2 次 = 真 `exit 1`**（job 级红，非 annotation-only）⇒ 上传服务真故障会在第 2 次拦住 ✅
- 出红判据是 **`ART_JSON` 里有无 `gate-logs-*`**（`:164`），即**以「有没有产物」为准**，不是以「步 outcome」自证 ⇒ 与 B1 的产物实体互为正反证 ✅

**残余面（P2）**

| # | 面 | 证据 | 影响 |
|---|---|---|---|
| P2-1 | **「上传 0 文件」不被兜底覆盖**：`if-no-files-found: warn`（`ci.yml:122`）时 upload-artifact 仍**成功**⇒ `steps.archive.outcome=success` ⇒ 兜底首分支 `exit 0`（「计数归零」） | `ci.yml:122` × backstop `if [ "${ARCHIVE_OUTCOME:-}" = "success" ]; then … exit 0` | 窄（路径写死且 `include-hidden-files: true` 已修 v4.4+ 的隐藏文件坑；真 run 已产 1170 B 实证）；但「产出为空」这一形态**不可见** |
| P2-2 | **首推假红面**：连续判定取「同分支上一条 completed run」，可能与**归档步引入之前**的历史 run 配对 ⇒ 迁移期首次上传失败即被判「连续 2 次」 | backstop `PREV_ID=…rs[0]`（按 head_branch 取最新 completed） | 迁移期一次性；建议按「上一条**含 archive 步**的 run」过滤 |

---

## 六、B 批逐项总表

| 项 | 判定 | 依据（本节原始输出） |
|---|---|---|
| B1 artifact 1170 B / gate-hits.log 21 行 / expires=run+90d | ✅ | §一（三项逐字吻合，秒级） |
| B2 hook↔生成器同哈希 + 措辞更正 | ✅（安装件侧不可核） | §二（609788b1…/1209 B 复现；`>>…bypass.log` 零命中） |
| B3 旧 hook 自阻断被推翻 | ✅ | §三（8 条注入 EXIT=0；对照 EXIT=1） |
| B3′ 底账 §A3/§A5 是否需重述 | ⚠️ **需重述**（§A5 + 结论先行§3；§A3 宜加注） | §三(d) |
| B4 C16 双指标口径 | ✅（三读数收敛为一个口径 + shallow 陷阱 P1） | §四 |
| B5 G6 上传失败策略非假绿 | ✅（四态复现；残余 P2×2） | §五 |

**与声称不符项**：仅 1 项外部文档（研究院底账）结论需重述；本批 **4 条 PR 自身的声称逐条成立**，未发现 overclaim。
**无法核项**：① 安装件 `.git/hooks/pre-commit` 的哈希（现场在 Mac 机器 `.git/`，不入库）；② #834 报告「head 以 PR 页面为准」的历史读数链（`ef58ca00` = rename 当时读数、`33ceabf5` = 03:00 读数、**本审计实测 = `ec73dc36`**）——该报告已改「不再写死 head」，写法正确，**不构成缺陷**；③ 4 条 PR 的 CI 结论（**真 queued**，不得以本地等价替代）。

---

## 七、L4 防线缺口收割

| 缺口 | 本该拦住它的防线 | 为什么没拦住 | 缺什么 | 归因 |
|---|---|---|---|---|
| **G1** 归档步「成功但零产物」不可见（P2-1） | `upload-artifact` + 兜底判定 | 判定只看步 outcome（`success` ⇒ 归零），而 `if-no-files-found: warn` 让零产物也 success | **M1 强化**：兜底首分支补「产物计数 ≥1」断言（`steps.archive.outputs.artifact-id` 非空）——**1 行判据**，不新增脚本 | control-tower |
| **G2** C16 在浅克隆下翻倍（§四 c） | —（C16 目前只是报告指标） | 尚无执行点；一旦自动化即中招 | **M1 强化**：任何取数入口先断言 `is-shallow-repository=false`（可复用 B5 的三态输出模式） | control-tower |
| **G3** 权威底账的结论缺少「与代码再核对」环节（B3） | 研究院取证流程 | 底账是**一次性取证快照**，各条结论无「近期复核/作废」字段；本次由开发侧在另一份文档里推翻，**底账本体仍带错结论** | **M2 强化**：底账加「条款级状态位（现行/需重述/已作废）+ 复核 as_of」，与 `号段水位.md`（生成物）同思路 —— 但**不新建脚本** | devdoc |

**Anti-bloat 结论**：3 个缺口全部命中 **M1/M2**（无新类）；建议只做「兜底首分支补产物计数断言」（1 行）与「取数入口 shallow 断言」（1 行）两处强化，**不提议 M9**。
