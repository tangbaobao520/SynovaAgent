# K3 独立审计 — C 批 · D979 过渡链（#809 / #815 / #819 / #820 / #823 / #826 + 证据 #825 / #831）

> 审计员：Kimi K3（零上下文独立会话）｜协议：`docs/synova/coordination/AUDIT-PROTOCOL.md` L1–L4
> 判定口径：**缺一即「有条件通过 = 未通过」**；每条结论附 file:line / 复现命令 / 原始输出。
> 被审对象（快照 2026-09-26T03:2x +08:00）：

| PR | 分支 | 快照 head | base（GitHub 实测） | CI @head |
|---|---|---|---|---|
| #809 | `docs/d979-fix011-shared-file-mechanism` | `feebf65102fc854bc34738a048c5427d066e1ee7` | `main` | ✅ completed / **success**（run 36161611337，13/13） |
| #815 | `fix/d979-d328-shared-decl` | `aa98eb7dd9501bd3d9761531e08d4910dfa4e5c0` | `docs/d979-fix011-shared-file-mechanism` | **零 run** |
| #819 | `docs/d979-verify-evidence` | `cd078321f0f235e1e19b885189d3767df282c558` | `fix/d979-d328-shared-decl` | **零 run** |
| #820 | `feat/d979-dispatch-tpl-dir` | `f34f3f555957d555df3d2cf6b1c0cbf41342839f` | `fix/d979-d328-shared-decl` | ❌ completed / **failure**（run 36165671710） |
| #823 | `feat/d979-watermark-gen` | `b16a29d78a3be77daaef492f2eaecb2e95239fe4` | `feat/d979-dispatch-tpl-dir` | ⛔ completed / **cancelled**（run 36165869942） |
| #826 | `docs/d979-lead-closeout` | `58b1fb52c21bd5a794f895bf497d8c0d461def3e` | `feat/d979-watermark-gen` | **零 run** |
| #825（证据） | `docs/d979-migrate-verify-evidence` | `ed0f9fe878d0963dc3345c380139a49d5688eb31` | `feat/d979-watermark-gen` | **零 run** |
| #831（证据） | `docs/d979-anno-verify-evidence` | `1e415fe3c447634ac85617a17eeb2a2a8a6f4984` | `docs/d979-transition-annotation` | **零 run** |

> 注：#829（`docs/d979-transition-annotation` @`4b9e08f1`，过渡机制三处标注本体）为 C6 的被核对象，见 §六。
> 环境：macOS 26（arm64）｜bash 3.2.57｜python 3.9.6。CI 事实取自 GitHub 只读 REST（仅 GET），抓取 2026-09-26T03:20–03:31 +08:00。

---

## 〇、总裁决

**未通过（CONDITIONAL FAIL）**。机制本体六项中 **C1–C5 成立**（含一项新 P1），**C6 缺一处**（迁移件零命中），另有一项**阻断性 P0 在上一轮已定罪、本轮仍未修**：

| # | 级别 | 结论 |
|---|---|---|
| **P0-1** | 阻断 | **新增测试未登记 CI 密封清单 ⇒ Gate Integrity 硬红**（独立复现）：#815 = `VIOLATION(1)`（`shared_file_decl.test.sh`）、#823/#826 = `VIOLATION(2)`（+ `gen-watermark.test.sh`）⇒ **这三条 head 不可合并**（上一轮 K3 报告 `2026-09-26-D979.md` P0-1 同项，**未修**） |
| **P1-1** | 高 | **`gen-watermark.sh --check` 跨克隆不可复算**：在**全新 worktree** 上首次运行即报 4/4 文件漂移（`号段/{README,Mac,Win,K3}.md`）；只有「先 write 再 check」才通过 ⇒ 生成物**不满足其自述的确定性契约**，CI/异机无法校验 |
| **P1-2** | 高 | C6「过渡件三处标注」按派单口径 代码 ✅ / PR 正文 ✅ / **迁移件 ❌（0 命中）** ⇒ 按「缺一即未通过」，C6 **未通过** |
| **P2-1** | 中 | 「`grep -c 疑似并行劫持` = 1」：判定行确为 1 处，但**全 `scripts/` 域为 2 处**（另一处在解析器 docstring 的背景说明）⇒ 口径需写明「判定行」 |

> 注：`task-state/D979.json` 只存在于 PR 栈分支（main 无）；按批次 4 先例**不在 main 侧新增/改写**（红线：不改被审分支）。下示 JSON 即**等价机器可读回填**。

```json
{
  "task_id": "D979",
  "pr_stack": [809, 815, 819, 820, 823, 826],
  "evidence_prs": [825, 831, 829],
  "audited_heads": {
    "809": "feebf65102fc854bc34738a048c5427d066e1ee7",
    "815": "aa98eb7dd9501bd3d9761531e08d4910dfa4e5c0",
    "819": "cd078321f0f235e1e19b885189d3767df282c558",
    "820": "f34f3f555957d555df3d2cf6b1c0cbf41342839f",
    "823": "b16a29d78a3be77daaef492f2eaecb2e95239fe4",
    "826": "58b1fb52c21bd5a794f895bf497d8c0d461def3e"
  },
  "ci_at_head": {
    "809": "completed/success(run 36161611337)",
    "815": "no_run",
    "819": "no_run",
    "820": "completed/failure(run 36165671710)",
    "823": "completed/cancelled(run 36165869942)",
    "826": "no_run"
  },
  "checks": {
    "C1_two_sided_counterexamples": "PASS(授权 ALLOW rc=0 / 非授权 D999 DENY rc=1)",
    "C2_not_blanket": "PASS(逐文件双向；判定行 1 处)",
    "C3_discriminative": "PASS(删声明行 DENY；变异放行分支 → 回到原劫持判定 exit 1，非崩溃)",
    "C4_watermark": "PASS(幂等) / PASS(反查活性) / FAIL(跨克隆不可复算 P1-1)",
    "C5_migration_zero_loss": "PASS(非空行 163/163、^# 标题 28/28)",
    "C6_transition_annotation_3x": "PARTIAL(代码✅ / PR正文✅ / 迁移件❌)"
  },
  "findings": [
    {"id": "P0-1", "owner": "implement+control-tower", "mcode": "M3", "summary": "#815/#823/#826 新增测试未登记 ci.yml 密封清单 → Gate Integrity VIOLATION(1)/(2)，CI 硬红（上一轮同项未修）", "fix_scope": ".github/workflows/ci.yml + 保持 head 重取 CI"},
    {"id": "P1-1", "owner": "implement", "mcode": "M5", "summary": "gen-watermark --check 跨克隆不可复算（水位随本地 ref 集合变化）；新 worktree 首次 --check 即 4/4 漂移", "fix_scope": "scripts/control-tower/gen-watermark.sh"},
    {"id": "P1-2", "owner": "implement", "mcode": "M7", "summary": "过渡件标注第三处（迁移件）零命中：号段/**、派单模板/**、号段水位.md、派单模板.md 对 过渡|阶段3|blame 全 0 命中"},
    {"id": "P2-1", "owner": "devdoc", "mcode": "M7", "summary": "「grep -c 疑似并行劫持 = 1」需写明「判定行」口径（全 scripts/ 域 = 2）"},
    {"id": "P2-2", "owner": "control-tower", "mcode": "M2", "summary": "栈式 6 件中 2 件零 run、1 件 cancelled ⇒ 全部按「CI 未跑」；其中 #820 为已红"}
  ],
  "next_action": "① 修 P0-1（登记 2 个测试入 ci.yml 密封清单）② P1-2 于迁移件（号段/** 或 派单模板/**）补过渡标注 ③ P1-1 决定「生成物确定性」的正式口径（或改为不入库生成物） ④ 逐件平铺后重取 CI 再提请复审"
}
```

---

## 一、C1 · 两侧反例 — ✅ 成立（本机 CLI 级独立复现 + 全套夹具 30/0）

**（a）用**真解析器 + 真文件**（非其夹具）直跑 CLI，逐例记录退出码：

```
SB=/tmp/.../decl-cli （git init 砂箱）
① 授权（MSG=D979 / CLAIM=D943，被改文件 `shared.yaml` 内声明 `共享声明: D979, D943`）:
$ printf 'shared.yaml\n' | python3 scripts/control-tower/shared_file_decl.py --msg-did D979 --claim-did D943 --root "$SB" --files-from -
DECL_ALLOW
MSG_DID=D979 CLAIM_DID=D943 FILES=1
FILE=shared.yaml SOURCE=worktree DECL=共享声明: D979, D943 HIT=D943,D979 MISSING=(无)
rc=0                                          ← 授权 ⇒ 放行 ✅

② 非授权（MSG=D999）:
DECL_DENY ｜ FILE=d999.yaml … HIT=D943,D979 MISSING=D999
rc=1                                          ← 非授权 ⇒ 仍拒 ✅

③ 只声明单侧（MSG=D979）：DECL_DENY ｜ MISSING=D943 ⇒ rc=1 ✅
④ 无声明：DECL_DENY ｜ DECL=(无) MISSING=D943,D979 ⇒ rc=1 ✅
```

**（b）夹具全量**（`pr/823` worktree，仅运行未改）：

```
bash tests/control-tower/commit-msg-consistency.test.sh → 结果: 30 通过 / 0 失败   EXIT=0
（含用例 14 授权放行 / 15 非授权仍拒，见 :383-388 与 :28 覆盖矩阵）
bash tests/control-tower/shared_file_decl.test.sh       → 结果: 40 通过 / 0 失败   EXIT=0
```

---

## 二、C2 · 非 blanket 豁免 — ✅ 成立（口径需补一句，P2-1）

**（a）判定点唯一性**

```
$ grep -rn "疑似并行劫持" scripts/
scripts/control-tower/shared_file_decl.py:1:… commit-msg-check.sh 的 D328 判定把**授权修改**误判为「疑似并行劫持」（24h 拦 3 次）。   ← docstring 背景说明
scripts/commit-msg-check.sh:149:  echo -e "…❌ D328: 提交声明(${MSG_DID:-无})与暂存文件归属($CLAIM_DID)不一致 — 疑似并行劫持…"
$ grep -r "疑似并行劫持" scripts/ | wc -l → 2 ；其中**实际阻断语句 = 1**（commit-msg-check.sh:149）
```

⇒ 「grep -c = 1」**按「判定行」口径成立**；按「全 scripts/ 域命中数」为 2 ⇒ 建议自验/台账写明是哪个口径（P2-1）。**不存在 blanket 豁免**（放行不是无条件 skip，而是走解析器逐文件判定）。

**（b）放行要求「双方 D# 同时出现在被改文件自身」**（本机 CLI 二阶验证）

- 同一文件内声明 `D979, D943`：MSG=D979+CLAIM=D943 ⇒ ALLOW ✅
- 该文件**只声明 D979** ⇒ MISSING=D943 ⇒ DENY ✅
- 声明写在**别的文件**里对本次被改文件无帮助（`check_files` 逐 `path` 调 `read_decl_source`，`shared_file_decl.py:216-262`；证据：④ 无声明文件 DENY）✅
- 解析读取顺序 = 暂存 index → 工作区 → HEAD（`:180-200`），三处皆不可读 ⇒ `SOURCE=missing` ⇒ DENY（fail-closed）✅

---

## 三、C3 · 判别性 — ✅ 成立（含「回到原劫持判定而非崩溃红」的原文证明）

**（a）删声明行 ⇒ 必红**：见 §一 ④（`plain.yaml` 无声明 → `DECL_DENY` rc=1）✅

**（b）变异放行分支 ⇒ 必红且回到原劫持判定**（本机自做变异：`commit-msg-check.sh` 内 `if [ "$DECL_RC" = 0 ]; then` → `if [ "MUTANT" = "disable-release" ]; then`）

```
$ bash tests/control-tower/commit-msg-consistency.test.sh
  ❌ 授权卡改共享件放行 (D979 FIX-011) — 期望 exit=0 实际=1
  ❌ 输出缺放行证据行标题
  结果: 28 通过 / 2 失败    EXIT=1

（变异体下该用例的原始输出，来自本审计在夹具副本上加的一行 cat）
✅ Commit 格式正确
❌ D328: 提交声明(D979)与暂存文件归属(D582)不一致 — 疑似并行劫持
   认领 brief: …/.claude/task-briefs/2026-09-26-D582-shared-owner.md
   DECL_ALLOW
   MSG_DID=D979 CLAIM_DID=D582 FILES=1
   FILE=scripts/gov-shared.sh SOURCE=index DECL=# 共享声明: D582, D979 HIT=D582,D979 MISSING=(无)
   请确认提交的是本任务文件，或拆分暂存区后再提交
```

⇒ **回到原劫持判定（`exit 1` + 「疑似并行劫持」原文），不是崩溃红**（其余 28 条断言全绿，无解析崩溃）✅

---

## 四、C4 · 号段生成物 — ⚠️ 幂等 ✅ / 反查活性 ✅ / **跨克隆不可复算 ❌（P1-1）**

**（a）幂等（同一 ref 环境内）**：`write → --check` 逐字节一致 ✅

```
$ bash scripts/control-tower/gen-watermark.sh >/dev/null ; echo write rc=$?
write rc=0
$ bash scripts/control-tower/gen-watermark.sh --check ; echo check EXIT=$?
✅ 号段水位与仓库逐字节一致（--check 通过）
check EXIT=0
```

**（b）反查活性（把反查改常量 ⇒ 必须漂移报警）** ✅

```
（改 `branches = [b for b in proc.stdout…]` → `branches = []`，仅此 1 处）
$ bash scripts/control-tower/gen-watermark.sh --check ; echo EXIT=$?
❌ 号段水位与仓库不一致（--check 漂移）:
  漂移: …/docs/synova/coordination/号段/K3.md
  漂移: …/Mac.md ｜ …/README.md ｜ …/Win.md
   修复: bash scripts/control-tower/gen-watermark.sh 后提交生成物
EXIT=1
```

**（c）🔴 跨克隆不可复算（P1 新发现，上一轮 P1-1 未修）** —— **全新 worktree 上首次 `--check` 即红**：

```
$ git worktree add --detach /tmp/k3-20260926/pr823 pr/823     # 干净检出，未做任何写操作
$ bash scripts/control-tower/gen-watermark.sh --check ; echo EXIT=$?
❌ 号段水位与仓库不一致（--check 漂移）:
  漂移: …/号段/K3.md ｜ …/Mac.md ｜ …/README.md ｜ …/Win.md
EXIT=1
（漂移实质，git diff 摘）
 K3.md:   段内卡数 32 → 39 ；最近卡号列表出现 D979
 Mac.md:  段内卡数 54 → 53
 README:  Win 水位 D1002 → D1012 ；K3 段内卡数 32 → 39 ；Win 卡数 2 → 7
```

**成因**：生成器输入 = `task-state/*.json` **+ `git for-each-ref refs/heads refs/remotes`（本地/远端 ref 集合）**（`gen-watermark.sh:149-158`）；本机 clone 的远端 ref 集合与作者机不同（本机 `refs/remotes/origin/*` = 779 条），故水位随之变化。**与脚本自述契约冲突**：

```
gen-watermark.sh:16-17  @output …（每归属一文件，内容确定性：无时间戳/无主机名/无路径 ⇒ 同输入必同输出）
```

⇒ **「同输入」不成立**（ref 集合本身是环境量）⇒ 生成物**不能作为可校验的仓库不变量**：CI（`fetch-depth` 各异）、异机、甚至同一机器不同 branch 缓存都会得出不同水位与 `--check` 结论。**归因：implement（M5 环境依赖门禁）**。修法方向（不属本审计结论）：或把 ref 源固定为 `origin/main`+显式清单并写入生成物头部，或声明「本生成物仅本机校验、不入库」。

---

## 五、C5 · 迁移零丢失 — ✅ 独立复算成立（163/163、28/28）

**口径**：拆前 = `origin/main:docs/synova/coordination/派单模板.md`；拆后 = `pr/820` 的 `派单模板/`（5 文件）+ 指针 `派单模板.md`。本审计自写逐行覆盖器（去尾空白后逐字命中，非子串）：

```
拆前非空行=163  未覆盖=0
拆前 ^# 标题行=28  未覆盖=0
after 独有（新增/改写）行数 = 52   ← 复本/说明/骨架所致，非丢失
```

⇒ 「非空行 163/163、标题 28/28」**逐字吻合** ✅（自验件另用 `1938b13b` 口径记 164/20，为**含/不含代码围栏内 `#` 行**之别 ⇒ 建议并列两口径，P2 级，不影响零丢失结论。）

---

## 六、C6 · 「过渡件」三处标注 — ⚠️ **代码 ✅ / PR 正文 ✅ / 迁移件 ❌** ⇒ 未通过

**（a）代码（2 处：判定分支 + 解析器）** ✅ —— `docs/d979-transition-annotation` @`4b9e08f1`（PR #829）

```
$ git show pr/831:scripts/commit-msg-check.sh | grep -n 过渡
58:#     【过渡机制】CTO 2026-09-26 裁定二（D979）：⒝ 显式共享声明批为**过渡机制**，非终态 ——
59:#     阶段 3 由行级 blame 取代（届时本注释块与本判定分支一并退役…）
$ git show pr/831:scripts/control-tower/shared_file_decl.py | grep -n 过渡
11:【过渡机制】CTO 2026-09-26 裁定二（D979）：本模块（⒝ 显式共享声明解析器）批为**过渡机制**，
$ git show pr/831:memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md | grep -n 过渡
 9:**过渡机制（CTO 2026-09-26 裁定二）**：… **阶段 3 由行级 blame 取代**（…）
```

**（b）PR 正文** ✅ —— PR **#829**（标注本体所在 PR）正文含「过渡机制」5 处（含三处清单表与 `阶段 3 由行级 blame 取代` 原文）：

```
$ GET pulls/829 → body 命中行（摘）
  依据 **CTO 2026-09-26 裁定二**：「⒝ 共享声明批为**过渡机制**（非终态）—— 三处标注『阶段 3 由行级 blame 取代』，不扩大范围」。
  | ① | `scripts/commit-msg-check.sh` | L58–59 … |
  | ③ | `memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md` | L9 … |
  | ⑤ CTO 措辞计数 | `阶段 3 由行级 blame 取代` 三个文件**各 = 1（合计 3）** |
#831 正文亦命中 2 处（引用 #829 与证据目录）
其余 7 条（#809/#815/#819/#820/#823/#825/#826）正文「过渡/blame」= 0 命中
```

**（c）迁移件** ❌ —— **零命中**（关键词 `过渡|阶段 3|阶段3|blame`，逐文件扫描）：

```
pr/831 与 docs/d979-transition-annotation 两 rev 结果一致：
  docs/synova/coordination/号段/README.md      hits=0
  docs/synova/coordination/号段/Mac.md         hits=0
  docs/synova/coordination/号段/Win.md         hits=0
  docs/synova/coordination/号段/K3.md          hits=0
  docs/synova/coordination/号段水位.md          hits=0
  docs/synova/coordination/派单模板.md          hits=0
  docs/synova/coordination/派单模板/*.md（5 件） hits=0
```

**判定**：派单口径为「代码 / PR 正文 / 迁移件」三处；**实际落地 = 代码 ×2 + 决策 Note ×1**（Note 既非「PR 正文」也非「迁移件」）。去掉 Note 后恰好有「代码 ✅ + PR 正文 ✅」，但**第三处（迁移件）零落地** ⇒ 按「缺一即未通过」，**C6 未通过**。

> 归因说明：小队侧依 CTO 裁定二自行**定义了**三处（`CTO裁定执行-20260926.md`「三处定为: commit-msg-check.sh + shared_file_decl.py + Note」），与 K3 派单口径不同 —— 属**口径未对齐**（归因：devdoc），非「谎报」。若 CTO 采纳小队口径，则 C6 可视作通过，须**书面改判**（本审计不代 CTO 改判）。

---

## 七、P0-1（承上轮，未修）· 密封清单违规 ⇒ Gate Integrity 硬红

**独立复现（本地跑 CI 同款检查，三条 head 各一次）**

```
#815  → VIOLATION: 新增测试未登记 CI 密封清单: tests/control-tower/shared_file_decl.test.sh
        GATE-INTEGRITY: VIOLATION(1)
#823  → VIOLATION: … tests/control-tower/gen-watermark.test.sh
        VIOLATION: … tests/control-tower/shared_file_decl.test.sh
        GATE-INTEGRITY: VIOLATION(2)
#826  → 同 #823（VIOLATION(2)）
（对照：#811/#812/#813/#817/#821 同项检查 = GATE-INTEGRITY: OK ⇒ 非环境问题）
```

CI 侧同源证据（#820 已红 / #818 同族）：

```
job 108171916371（run 36165435997，PR #816 系）: VIOLATION: 新增测试未登记 CI 密封清单: tests/control-tower/loop-context.test.sh
                                                 GATE-INTEGRITY: VIOLATION(1)
```

⇒ **#815/#823/#826 三条 head 的 Gate Integrity 必红**（与上一轮 K3 报告 `2026-09-26-D979.md` P0-1 完全同项）。**这不是「CI 未跑」，而是「已可判定会红」**（检查本体在仓库里、可在本机逐像素复现）。

---

## 八、与声称不符项 / 无法核项

**与声称不符**

| # | 声称 | 实测 | 级别 |
|---|---|---|---|
| 1 | 「CI 事实不可得 ⇒ 相关 PR 标 CI 未跑」（栈式） | #809 实为 **completed/success**；#820 实为 **completed/failure**；#823 为 **cancelled** ⇒ 3/6 有结论，**不得以「未跑」覆盖已红** | P0 关联（M2） |
| 2 | 过渡件三处标注（派单口径 代码/PR 正文/迁移件） | 迁移件 0 命中 | P1-2 |
| 3 | `grep -c 疑似并行劫持` = 1 | 判定行 1 处，`scripts/` 全域 2 处 | P2-1 |
| 4 | 生成物「内容确定性（同输入必同输出）」 | 跨 clone 首次 `--check` 即 4/4 漂移 | P1-1 |

**无法核项**

| # | 项 | 原因 |
|---|---|---|
| 1 | #815/#819/#826/#825/#831 的 CI 结论 | `runs?head_sha=<40位>` = 0 条（base 非 main、分支非 `feat/*` ⇒ 无触发面）。**不得以本地等价冒充实跑** |
| 2 | #823 的 CI 结论 | run 36165869942 = **cancelled**（非完成态）⇒ 无有效结论 |
| 3 | 「迁移件 sha256/逐字节」类断言 | 本批无此类声称；C5 的零丢失已用逐行覆盖器独立复算 |

---

## 九、L4 防线缺口收割

| 缺口 | 本该拦住它的防线 | 为什么没拦住 | 缺什么 | 归因 |
|---|---|---|---|---|
| **G1** 新增测试未登记密封清单（#815/#823/#826，**连续第二轮**） | `check-gate-integrity.sh` CI-REGISTRY（**存在且有效**） | 拦住了（本机可判定必红），但**栈式 base≠main ⇒ 无 CI 事件** ⇒ 检查在 CI 侧从不执行；开发侧本地也未跑该检查 | **M3 强化（非新类）**：栈式分支（base 非 main）必须显式声明其「CI 触发方案」；**每次 head 前移后本机跑一次 `check-gate-integrity.sh` 并把原始输出贴进自验件**（现成脚本，零新增） | implement + control-tower |
| **G2** 生成物跨环境不可复算（P1-1） | 生成物「确定性」契约（写在脚本注释里） | 契约只是**注释**，无门禁；且「输入」含环境量（ref 集合） | **M5 强化**：给「入库生成物」加**双环境复算断言**（至少在 CI 的 `fetch-depth: 0` 检出上跑一次 `--check`）—— 与 B 批 G2（shallow 陷阱）**同一机制**，可合并为一条「取数/生成物环境自证」判据 | control-tower |
| **G3** 派单口径（代码/PR 正文/迁移件）未映射进 brief Done（C6） | 派单件「闭环判据」→ brief Done 映射 | 上一轮已登记 M7，未落地；本轮小队自行定义三处即可交付 | **M7 强化**：派单件的每一条「附加条件」必须**逐条编号**进入 brief Done 与自验件判据表（否则 K3 无法判「缺一」） | devdoc |

**Anti-bloat 结论**：3 个缺口全部命中既有 **M3/M5/M7**；**不新增 M 类**。可执行的最小动作 = ① 栈式 PR 在自验件里贴一次 Gate Integrity 原始输出（现成脚本）② 生成物加一条 CI 侧 `--check`（与 B 批 shallow 断言合并）③ 派单附加条件编号化。

---

## 十、结论与提请

**未通过**。逐项：C1 ✅ ｜ C2 ✅ ｜ C3 ✅ ｜ C4 ⚠️（幂等/活性成立，跨克隆不可复算 P1）｜ C5 ✅ ｜ C6 ❌（迁移件零命中）｜ 另 P0-1（密封清单，上轮同项未修）。

**复审前置条件**：① 登记 `tests/control-tower/shared_file_decl.test.sh`、`tests/control-tower/gen-watermark.test.sh` 入 `ci.yml` 密封清单；② P1-2 补迁移件标注（或 CTO 书面改判口径为「代码×2 + Note」，二者择一）；③ P1-1 就「生成物确定性」给出正式口径；④ 6 件逐件平铺取得各自 CI 结论后重提请复审（**#809 已绿可先合**）。
