# K3 独立审计 — 第二批（N12 根治 + 阶段 1 三件 + 治理件抽验）

> 审计员：Kimi K3（零上下文独立会话）｜协议：`docs/synova/coordination/AUDIT-PROTOCOL.md` L1–L4
> 判定口径：**缺一即「有条件通过 = 未通过」**；每条结论附 file:line / `<ref>:<path>` L<n> + 复现命令 + 原始输出。
> 断面: 0.1.7-rc.1 @ 46a7f68b（与唯一源 `docs/synova/coordination/DSH-断面.json:1` 逐字一致；本批 6 件均不触碰 DSH 仓代码）
> 被审对象 PR #836 head 97cd57b9fa606dc1fe7127911962ad8320560c60（分支 chore/D1014-ci-concurrency）
> 被审对象 PR #838 head ab93086816e662b7b951e435fd560226d4fbc434（分支 chore/D1015-checkstar-caliber）
> 被审对象 PR #840 head 499d0a3e37ab21a1a77917967d8683e8de50da82（分支 chore/D1016-k3-input-repro）
> 被审对象 PR #841 head f3471008cea84be79f857e6f768f61ade4ab6f92（分支 chore/D1017-windows-hang）
> 被审对象 PR #822 head 570ad8e43bcafe62af13d70827305d7f9dc9ee48（D980 工程派单）
> 被审对象 PR #805 head ba89218604d120ba20a48f3a53f0d054efd3e2cd（D978 问题修复台）
> 被审对象基线 HEAD ee721b0abf52a4cdcc42caef44669d2cca423264（origin/main；全部读数以此为基准断面）
> 运行环境注记: macOS（arm64）｜`/usr/bin/python3` 3.9.6｜node + 仓库内 `js-yaml`｜GitHub REST **只读 GET**（token 取自 `~/.dsh/.credentials.yaml` 的 GITHUB_TOKEN，仅用于 GET，报告不含任何密文）。CI 侧读数 as_of = **2026-09-25T20:27Z**（= 2026-09-26T04:27+08:00），逐条读数自带时刻。
> 引用写法：main 树内文件写 `path:line`；**只存在于被审分支的文件不在 main 树上**（机械门禁 R3 仅解析 main 树路径），因此统一写成 `` `<ref>:<path>` L<n> ``，全部可用 `git show <ref>:<path>` 复核。
> 独立复现方式：全部结论为**本机物理复现**——本地 git 对象面 + 独立匿名 clone（`/private/tmp/k3-indep-b2-20260926`，**我自建**，非被审方产物）+ GitHub REST。未复现项在 §九 单列，绝不以"机制设计与蓝图一致"替代证据。

---

## 〇、总裁决

| PR | 任务 | 裁决 | P0 | P1 | P2 | 一句话 |
|---|---|---|---|---|---|---|
| **#836** | D1014 N12 根治（ci.yml concurrency + push 收窄 + PR types） | **通过** | 0 | 0 | 4 | 六必审点全部独立成立；⑥ 异常已定因（结构性非缺陷）；证据件 4 处数字/工件陈旧 |
| **#838** | D1015 `check-*` 口径定版 + 新增门禁探测 | **通过** | 0 | 0 | 3 | 定版命令独立复跑 = 48；正反例 0/1、7/24、0/0 逐条复现；`k3-merge-gate.sh` 声称成立（分支面） |
| **#840** | D1016 K3 最小输入集异机复跑 | **通过** | 0 | 0 | 2 | 真匿名 clone 成立；我用自建 clone 复现 8 项判据 + 快照 sha256 逐字符一致 |
| **#841** | D1017【P0】Windows 必需检查长挂 | **未通过** | 1 | 3 | 1 | 卡内 3/3「挂死」job **均 completed/success**（33–37 min），"永不 completed"被自家证据推翻；1 处 job_id 错配 |
| **#822** | D980 治理体系最小功率形态工程派单 | **通过（抽验）** | 0 | 0 | 2 | 强制表头/断面/冻结口径与实测一致；断面已前进须按件内规则重跑 |
| **#805** | D978 问题修复台 | **有条件通过 = 未通过** | 0 | 2 | 2 | 台账 19 行，摘要仍写 17；登记/在修互换；两处 ✅已闭环 的 PR 未合 main |

**机器可读回填（编码 session 可直接读本块行动；不靠口头转达）**：

```json
{
  "batch": "K3-2026-09-26-batch2",
  "auditor": "k3",
  "as_of": "2026-09-26T04:27+08:00",
  "base_head": "ee721b0abf52a4cdcc42caef44669d2cca423264",
  "report": "docs/synova/audit-reports/2026-09-26-K3-第二批-N12与阶段1三件.md",
  "items": [
    {"pr": 836, "task": "D1014", "head": "97cd57b9fa606dc1fe7127911962ad8320560c60", "verdict": "PASS", "p0": 0, "p1": 0, "p2": 4},
    {"pr": 838, "task": "D1015", "head": "ab93086816e662b7b951e435fd560226d4fbc434", "verdict": "PASS", "p0": 0, "p1": 0, "p2": 3},
    {"pr": 840, "task": "D1016", "head": "499d0a3e37ab21a1a77917967d8683e8de50da82", "verdict": "PASS", "p0": 0, "p1": 0, "p2": 2},
    {"pr": 841, "task": "D1017", "head": "f3471008cea84be79f857e6f768f61ade4ab6f92", "verdict": "FAIL", "p0": 1, "p1": 3, "p2": 1},
    {"pr": 822, "task": "D980", "head": "570ad8e43bcafe62af13d70827305d7f9dc9ee48", "verdict": "PASS", "p0": 0, "p1": 0, "p2": 2},
    {"pr": 805, "task": "D978", "head": "ba89218604d120ba20a48f3a53f0d054efd3e2cd", "verdict": "CONDITIONAL_PASS", "p0": 0, "p1": 2, "p2": 2}
  ],
  "must_fix_before_merge": {
    "841": "改写机理：Windows CT job 慢（34–37 min，ubuntu 3.5–4.4 min）+ run 级 runner 排队 30–40 min（外部）；删「永不 completed」；修 job_id 108210733295 指针；timeout-minutes 须 > 40 min 否则转红",
    "805": "摘要行改为 19 行实际计数；FIX-006/008 的 ✅已闭环 与 #817/#821 未合 main 对齐（改标「待收件/在修」）"
  }
}
```

---

## 一、#836 D1014 · N12 根治（六必审点逐项）

### ⒜ 同分支连推 ⇒ 旧 run 自动 cancelled、只留最新 —— ✅ 成立

REST `GET /repos/tangbaobao520/SynovaAgent/actions/runs?head_branch=chore/D1014-ci-concurrency`（该过滤参数实测被服务端忽略，`total_count=7110` = 全仓 run 数 ⇒ 改用全窗口扫描 + 客户端过滤，见 §十 命令）：

```text
      run_id branch                                        event         status       concl      created -> updated
 36182016153 chore/D1014-ci-concurrency                pull_request  in_progress  None       2026-09-25T19:49:29Z -> 2026-09-25T19:55:20Z
 36181788212 chore/D1014-ci-concurrency                pull_request  completed    cancelled  2026-09-25T19:47:11Z -> 2026-09-25T19:49:59Z
 36181480045 chore/D1014-ci-concurrency                pull_request  completed    cancelled  2026-09-25T19:44:08Z -> 2026-09-25T19:48:25Z
 36181349714 chore/D1014-ci-concurrency                pull_request  completed    cancelled  2026-09-25T19:42:49Z -> 2026-09-25T19:45:19Z
 36181301748 chore/D1014-ci-concurrency                pull_request  completed    cancelled  2026-09-25T19:42:20Z -> 2026-09-25T19:42:50Z
 36181193597 chore/D1014-ci-concurrency                pull_request  completed    cancelled  2026-09-25T19:41:16Z -> 2026-09-25T19:43:44Z
```

**判读**：该分支 6 条 run = 5 cancelled + 1 in_progress（与 CTO 实测一致）；因果链逐条可核——每条后继 run 的 created_at 早于前驱 run 的 updated_at（如 36181301748 created 19:42:20 取消 36181193597 于 19:43:44；36182016153 created 19:49:29 取消 36181788212 于 19:49:59），取消延迟 1–3 min。**关键佐证**：同窗口 `push` 事件 run（feat/d962-2a-merge 4 条，push 型）无一条由 concurrency 取消，其取消时刻集中在 19:33–19:35Z（人工批量取消）；即"旧 run 自动取消"这一现象**只出现在声明了 `concurrency` 的 ref 上**，归因唯一。

被审件 `` `origin/pr/836:docs/synova/coordination/D1014-N12-证据-20260926.md` L46-L56 `` 记录"run 36181480045 in_progress ← 最新（存活）"，在 as_of（03:52+08:00 = 19:52Z）为真；该 run 其后被 19:47/19:49 的两次推 cancel —— 这正是被审机制的正常语义，不构成回归。

### ⒝ push 到 `feat/*` ⇒ 不触发；开/更新 PR ⇒ 触发 —— ✅ 成立（**附证据边界，必读**）

全窗口扫描 `GET /actions/runs?created=>=2026-09-25T18:30:00Z&per_page=100`（`total_count=32`，完整）：

```text
2026-09-25T19:17:44Z  36178796631 feat/d962-2a-merge          push   completed cancelled   ← 窗口内最后一条 push 型 run
（19:30Z 之后全部 run 均为 event=pull_request；无 feat/n12-no-trigger-test-20260926 的任何 run）
```

⇒ 01:52+08:00 时点的自建测试分支 `feat/n12-no-trigger-test-20260926` 未产生 run，且该分支已删除（`git ls-remote origin | grep -c n12-no-trigger` = 0）。**该点成立**。

**证据边界（P2-1）**：被审件 L57-L64 的 ⒝ 只在**「被推 ref 自身携带该 ci.yml」**的拓扑上成立——测试分支指向 D1014 分支的 HEAD。`origin/main` 的触发面仍是 `branches: [main, feat/*]`（`.github/workflows/ci.yml:5`，我逐字复核），因此**在 #836 合入 main 之前，任何基于 main 的 `feat/*` 分支 push 依旧触发全量 CI**。同刻实测佐证：20:25Z 仍有 8 条 run 处于 in_progress（其中 7 条属非 D1014 分支：36184431894 / 36184489886 / 36184101443 / 36183028417 / 36182871315 / 36182638896 / 36182218912；1 条为本分支 36182016153）。结论：⒝ 是**合入后才全仓生效**的机制（非缺陷），但证据件未声明该边界，容易被读成"全仓已根治"。

### ⒞ `on.pull_request.types` 显式化 —— ✅ 成立

`node -e 'require("js-yaml")'` 解析 `origin/pr/836:.github/workflows/ci.yml` 实测：

```text
on.push: {"branches":["main"]}
on.pull_request: {"branches":["main"],"types":["opened","synchronize","reopened","ready_for_review"]}
concurrency: {"group":"${{ github.workflow }}-${{ github.ref }}","cancel-in-progress":true}
jobs: 10 ["quality","test","architecture","test-kit-architecture","control-tower-tests","integration-check","checker-review","audit","golden-case","gate-integrity"]
```

GitHub 默认三型 = opened/synchronize/reopened ⇒ 本次为**纯增量**（+ready_for_review），不产生触发面收缩。DSH 对照引用亦可核：`/Users/wane/src/deepseek-harness-017/.github/workflows/ci.yml:17-19` 的 `concurrency` 块与本次逐字相同，且该文件无任何 `paths:` 过滤（`grep -n 'paths:'` 零命中）——被审件 L88-L93 的 ⒟ 评估（不建议加 workflow 级 `paths:`）与 DSH 事实一致。

### ③ 必需检查名 / 数量未变 —— ✅ 成立（**独立取数，非转述**）

`GET /repos/tangbaobao520/SynovaAgent/branches/main/protection`（token 只读 GET，HTTP 200）：

```text
required count = 12  strict = False
  - Architecture Check
  - Checker Review (maker/checker)
  - Control Tower Gate Tests (ubuntu-latest)
  - Control Tower Gate Tests (windows-latest)
  - Golden Case F1 Gate
  - Integration Contract Check
  - Test-Kit Architecture Tests (ubuntu-latest)
  - Test-Kit Architecture Tests (windows-latest)
  - TypeScript + Lint + Iron Laws
  - Vitest (1/2)   - Vitest (2/2)   - npm audit
```

与 CTO 口径（12）及被审件 L73-L81 逐字一致。job 面：`git diff` 两侧 job 键名集合 diff = **0 行**（10 键全同，`quality` 仍名 `TypeScript + Lint + Iron Laws`）。

### ④ 未改任何 job / step 判定 —— ✅ 成立

`git diff ee721b0ab..origin/pr/836 -- .github/workflows/ci.yml` 的非注释增删行**仅** 5 行：

```text
-    branches: [main, feat/*]
+    branches: [main]
+    types: [opened, synchronize, reopened, ready_for_review]
+concurrency:
+  group: ${{ github.workflow }}-${{ github.ref }}
+  cancel-in-progress: true
```

### ⑤ 未新增跳过条件 —— ✅ 成立

同一 diff 的 `+` 行中 `if:` / `continue-on-error` / `needs:` / `timeout-minutes` **0 命中**（命令见 §十）。job 数 10 不变（`jobs` 键集合逐字相同）。

### ⑥ #833 旧 run 36176205440：取消未生效 vs 卡死 —— ⚠️ **两者皆不成立，已定因**

`GET /actions/runs/36176205440` + `/actions/runs/36176205440/jobs`（as_of 20:25Z）：

```text
run 36176205440  head_branch=chore/D1010-stage0-report  head_sha=92c155c42d  event=pull_request
  status=completed  conclusion=failure  created=18:52:46Z  updated=20:11:01Z
  job 108207302431 'Control Tower Gate Tests (windows-latest)'
      status=completed concl=success  started=19:35:06Z completed=20:11:00Z
      step 'Run hermetic control-tower gate tests' completed/success 19:35:21Z -> 20:10:56Z   (35m35s)
  job 108207302381 'TypeScript + Lint + Iron Laws'  completed/failure  step 'Merge write-set reconciliation (D708)' failure
```

**定因（两条物理事实）**：

1. **它不属于任何 concurrency 组** ⇒ 新 run 结构上无法取消它。其分支 `chore/D1010-stage0-report` 的 ci.yml 无 `concurrency`（`git show 92c155c42d:.github/workflows/ci.yml | grep -c '^concurrency:'` = **0**；分支 tip 同样 = 0）。机制只对"自己携带 `concurrency` 的工作流版本"生效，故"新 run 已存在而旧 run 仍在跑"是**预期行为**，不是取消机制失效。
2. **它没有卡死**：windows job 35m35s 后 **completed/success**，run 于 20:11:01Z 收口（conclusion=failure 的真实来源是同一 run 的 TypeScript job 在 D708 合并级写集 gate 上 failure，与 windows job 无关）。

⇒ 建议表述：**「取消未生效」= 结构性预期**（分支未携带 concurrency）；**「卡死」= 不成立**。若在 #836 合入 main 后仍复现同类现象，则须另立"+concurrency 仍不取消"的样本（本批未取得，见 §九）。

### #836 与声称不符项 / 未核项

| # | 项 | 事实 | 级别 |
|---|---|---|---|
| 1 | 被审件 L14（`` `D1014-N12-证据-20260926.md` L14 ``）标题「改动（10 insertions / 1 deletion）」+ L16-L25 声称 `git diff --numstat` = `10 1` | 最终交付面实测 **`12 1`**（`git diff --numstat ee721b0ab..origin/pr/836 -- .github/workflows/ci.yml`）。差异来自后续提交 `7fb0f476c`（`types:` 行 + 注释）；被审件 L16-L25 的 YAML 块也**缺 `types:` 行**，与同件 ⒞（L65-L71）自相矛盾。`origin/pr/836:task-state/D1014.json` 的 `changes.diff_stat` 同样写「10 insertions / 1 deletion」 | P2（M2/M7，文档-实现漂移） |
| 2 | L46 标题「⒜ 同分支连推 **3 次** ⇒ 前两次 cancelled」 | 正文与实测均为 **4 次 / 前三次**（标题为编辑残留） | P2 |
| 3 | L94-L117 §五声称核验了 CTO 补丁 `D981-ci-concurrency.patch`（811 B，+11/-1，`git apply` + js-yaml 逐键深比较） | 该补丁工件**全仓 0 命中**（`grep -rl D981 .` 排除 node_modules ⇒ 无文件；`git log --all --grep=D981` ⇒ 仅本件提交信息 1 处）。**等价性核验不可独立复现**；我能独立复现的只有"最终 ci.yml = push[main] + types 四型 + concurrency 三行"这一结论面 | P2（M2 + CT-40 同族） |
| 4 | L83-L87 ④「不得削弱真保护」 | 结论成立；但**未登记一条残余风险**：`cancel-in-progress` 与必需检查的交互（被取消的 sha 的 check 呈 cancelled）在本批未做对照实验 | P2（未核，见 §九） |

**#836 裁决：通过**（六必审点 6/6 独立成立；P2×4 不阻断合并，建议合并前一并修正 #1/#2/#3，否则又是一次 M7 存量）。

---

## 二、#838 D1015 · 口径定版 + 新增门禁探测

### 必审点① 口径命令可复跑 —— ✅ 成立（独立复跑 = 48）

在被审件指定的断面 `origin/main = ee721b0abf52`（我本地实测该引用仍 = ee721b0a，未漂移）：

```text
$ git ls-tree -r --name-only origin/main -- scripts | grep -E '(^|/)check-[^/]+$' | grep -v '^scripts/audit/' | wc -l
      48
$ git ls-tree -r --name-only origin/main -- scripts | grep -E '(^|/)check-[^/]+$' | wc -l
      49                       # 含 K3 域，= 件内 CHECKSTAR_WITH_AUDIT
$ git ls-tree -r --name-only origin/main -- scripts | grep -E '(^|/)check' | wc -l
      54                       # 前缀口径，= 件内 §2 ⑥b
$ git ls-tree -r --name-only origin/main | grep -E '(^|/)check-[^/]+$' | wc -l
      71                       # 全树口径，= 件内 §2 ⑦
$ git cat-file -p origin/main:scripts/pre-commit-check.sh | wc -l
    1603
```

与 CTO 实测（48）及 `` `origin/pr/838:docs/synova/coordination/阶段1-check星号-口径-20260926.md` L37 ``（对账式逐字输出 `CHECKSTAR_CANONICAL n=48 as_of=2026-09-26T03:52:52+0800 origin_main=ee721b0abf52`）一致。**同数不同物**的归因也被我独立证实：主工作树 `b8573c7a` 上 `scripts/control-tower/check-preset-bundles.sh` **不存在**（`git cat-file -e b8573c7ac83d:scripts/control-tower/check-preset-bundles.sh` → `exists on disk, but not in 'b8573c7ac83d'`），而该文件在 `origin/main` 存在（`git ls-tree origin/main -- scripts/control-tower/check-preset-bundles.sh` → `100755`）⇒ 48/49 差异来源 = 该文件，件内 §1 判据表成立。

### 必审点② 新增门禁探测正反例 —— ✅ 成立（三条逐字复现）

按被审件 §7.2/§7.3 的三条命令原样执行（base 固定 `origin/main`）：

```text
=== 正例 #832 (cited 661b2c483702) ===
NEWGATE_PROBE ref=661b2c483702 scripts_added=0 gate_surface_changed=1
=== 正例 #832 (current tip 43ae55b964) ===
NEWGATE_PROBE ref=origin/chore/D1009-stage0-log-archive scripts_added=0 gate_surface_changed=1
=== 反例 #729 (cited 8c89817e5365) ===
NEWGATE_PROBE ref=8c89817e5365 scripts_added=7 gate_surface_changed=24
=== 对照 D1015 分支 (1b7bbd381818) ===
NEWGATE_PROBE ref=1b7bbd381818 scripts_added=0 gate_surface_changed=0
```

与 `` `origin/pr/838:docs/synova/coordination/阶段1-check星号-口径-20260926.md` L189-L191 `` 及 §7.4 对账式（`661b2c483702/0/1`、`8c89817e5365/7/24`、`1b7bbd381818/0/0`）逐字一致；正例在分支 tip 前进后仍 = 0/1（`61b2c483`→`43ae55b96` 期间未增 scripts）⇒ 判据对 head 漂移稳健。

### 必审点③ CTO 的 ⚠️ `k3-merge-gate.sh`（100755 + `.github/workflows/ci.yml` L137 调用）—— ✅ 声称成立；main 0 命中已解释

```text
$ git ls-tree 8c89817e5365 -- scripts/control-tower/k3-merge-gate.sh
100755 blob fa6db05c3d7802c66d33a654164a78b5be593ca9	scripts/control-tower/k3-merge-gate.sh
$ git show 8c89817e5365:.github/workflows/ci.yml | sed -n '133,140p'
      - name: K3 merge gate (D923)
        ...
          bash scripts/control-tower/k3-merge-gate.sh \
            --base origin/main --head HEAD \
            --pr "${{ github.event.pull_request.number || 0 }}"
$ git ls-tree -r --name-only origin/main | grep -c k3-merge-gate
0
$ GET /repos/.../pulls/729 → state=open, merged=false, head.sha=8c89817e536598e25de58695439cf1841a87604e,
                              base.ref=feat/d925-deps-interface, mergeable_state=unstable
```

⇒ 三点全部核实：文件存在且**可执行位 100755**、**`ci.yml` L137 调用**（引用 `git show <ref>:.github/workflows/ci.yml` L137 逐字命中）、**main 上 0 命中**。CTO 的"main 上 0 命中"**不是声称不实**，而是因为 PR #729 仍未合并，且其 **base 是 `feat/d925-deps-interface`（栈式 PR，base≠main）**。被审件 L190 的行内写法 `.github/workflows/ci.yml:137` 未带 ref 前缀，是本条最容易被误读为 main 事实的地方（见 P2-1）。

### #838 与声称不符项 / 未核项

| # | 项 | 事实 | 级别 |
|---|---|---|---|
| 1 | L190 反例基数 | `scripts_added=7` 是**对 `origin/main` 的差**，其中 **5 件属栈式父 PR**（`feat/d925-deps-interface`），本 PR 自身只新增 2 件：`git diff --name-status origin/feat/d925-deps-interface..8c89817e5365 -- scripts` ⇒ 仅 `k3-gate-policy.yaml`、`k3-merge-gate.sh`（均被 CI 调用）。判据结论（FAIL → 点名真·新门禁）不受影响，但 §7.5「残余边界」未列"栈式 PR 基数含父 PR"这一条；同段"其余 4 个为 baseline/exempt/policy"的表述与该基数不符 | P2（口径边界，M2 轻微） |
| 2 | §3.5 四类争议计数 | 我独立复算 = **370 / 99 / 271**（`task-state/*.json` 取 `task_id` 匹配 `^D\d+$`），件内写 **370 / 98 / 272**。差异根因已定位：唯一非空但**非 list** 的 `write_set` 是 `task-state/D956.json:6` 的 **dict**（D708 合并级写集形态），按"非空 list"计数得 98、按"非空（含 dict）"得 99。件内未声明该口径 | P2（口径未声明，正是本卡要治的病） |
| 3 | L190 行内引用写法 | 建议统一写 `origin/feat/d923-k3-merge-gate:.github/workflows/ci.yml` L137（本批已按此写法复核） | P2 |

**#838 裁决：通过**（三条必审点独立成立；P2×3 均为口径/表述，建议在 §7.5 补一行边界）。

---

## 三、#840 D1016 · K3 最小输入集「异机复跑」

### 必审点 异机复跑为**真匿名 clone**（非本地等价）—— ✅ 成立

**（a）被审方 clone 的性质**（现存工件 `/private/tmp/k3-sim-20260926`，我直接检视其配置，未改动）：

```text
$ git -C /private/tmp/k3-sim-20260926 remote -v
origin	https://github.com/tangbaobao520/SynovaAgent.git (fetch)     ← 匿名 HTTPS，非本地路径、非 file://
$ git -C /private/tmp/k3-sim-20260926 rev-parse --short=12 HEAD
ee721b0abf52
$ git -C ... worktree list | wc -l          → 1                    （本机 301 个工作树在 clone 内不存在）
$ ls .claude/gate-hits.log .claude/pre-commit-failures.log .codex/control-tower/logs/degraded-events.log
  三个 gitignored 运行态日志：全部 No such file or directory
$ git for-each-ref refs/remotes/origin | wc -l → 782（含 refs/remotes/origin/HEAD）⇒ 分支数 781
```

⇒ 被审件 L28-L51 描述的"匿名远程 clone + 运行态日志缺席 + worktree=1"全部为真；**不是本地等价**（remote 是 https，clone 自带全部远端 ref，无需引用主仓对象）。

**（b）我不采信被审方产物，另建 clone 独立复跑**（`/private/tmp/k3-indep-b2-20260926`，`git clone --quiet https://github.com/...`，rc=0，HEAD `ee721b0abf52`）：

```text
check-* in scripts                = 49      （件内 §3.2 期望 49 ✅）
pre-commit-check.sh lines         = 1603    （= 件内 §3.2 基线 ✅）
外部调用点去重（§3.2 原命令）        = 29      （= 件内 §3.2 基线 ✅）
grep -rl '派单模板' task-state/*.json = 6    （= 件内 §3.4 基线 ✅）
git ls-tree -r origin/main | grep -c '号段水位'            = 0     ← quotepath 假阴性，件内 §3.4 已登记
git -c core.quotepath=false ls-tree -r origin/main | grep -c '号段水位' = 1   （= 件内 §3.4 结论 ✅）
快照 sha256（git show <ref>:<path> | shasum -a 256）：
  30929d4ed4e99c2c47b1f46d1567ef8e5f749e23d62b35b66a43b4e06df4b781  lines=5758  degraded-events.log.txt
  2a6a80ba467220da9d924df62352eb5eaccaf557874d6315e21322b8e35dc6f7  lines=4873  gate-hits.log.txt
  1148103f70ff3da63dbde71f0a3dd551e7aeb237a43bd4179dc1e80968187652  lines=156   pre-commit-failures.log.txt
  → 与被审件 §2.2 三个 sha256 与行数（4873/5758/156）**逐字符一致**
git ls-tree origin/main -- docs/synova/coordination/gate-logs-20260926/ | wc -l = 0   （件内 §2.3「未合 main」限制成立 ✅）
grep -c '"component": "verify-parallel"' <快照> = 5657                （= 件内 §3.3 + 蓝图基线 ✅）
§3.1 门禁命中率复算（快照 JSONL，键 time/gate/result）：
  门禁数 = 36 ｜ hit=0 的门禁 = 17 ｜ result 分布 = {miss: 4417, hit: 456}
  → 与件内 §3.1「门禁数=36 / hit=0=17」**完全一致**
C16-remote（远端 ref 口径，我自算，as_of 04:25+08:00）：
  远端分支总数=786 ｜ 领先 main 的分支数=464 ｜ 未合并提交总数=3709 ｜ 耗时 13.99s
  （件内 as_of 04:0x：782 / 459 / 3687，14s ⇒ 单调漂移，方法可复跑 ✅）
task-state D 卡 = 370（372 个文件中排除 task-state/TEMPLATE.json:1 与 task-state/ct-64.json:1）
```

**（c）artifact 通道（件内 §4 核心数值）**：`GET /actions/artifacts/10879601805`（token 只读 GET）⇒ `name=gate-logs-36167351846`、`size_in_bytes=1170`、`expired=false`、`expires_at=2026-12-24T17:28:34Z`。**1170 B 逐字成立**；`expires_at` = 该 artifact 所属 **run 创建时刻 17:28:34Z + 90 天**（artifact 对象自身 `created_at=17:45:39Z`）——件内 §4 写"创建 +整 90 天"未说明"创建"= run 创建，属表述可紧化，非数值错误。

### #840 与声称不符项 / 未核项

| # | 项 | 事实 | 级别 |
|---|---|---|---|
| 1 | 标题「**异机**可复跑」 | 执行现场是同机 `/private/tmp` 的**匿名 clone 模拟**（件内 L4 已自陈"模拟异机"、§6 已单列机器绑定项）。结论正确但**标题会诱导读者当作跨物理机证据**；建议改为"匿名 clone（同机模拟）可复跑" | P2（表述） |
| 2 | L14「克隆自带 **782** 个远程跟踪分支」vs L170「远端分支总数=**781**」 | 两者都真：782 = `refs/remotes/origin`（含 `refs/remotes/origin/HEAD`）entries，781 = 分支数。件内未说明口径差 | P2（口径） |
| 3 | §3.5 98/272 | 同 §二 P2-2（`task-state/D956.json:6` 的 dict `write_set`） | P2 |

**#840 裁决：通过**（必审点在"真匿名 clone"这一实质面上 8 项判据独立复现；P2×2 为表述/口径）。

---

## 四、#841 D1017 ·【P0】Windows 必需检查长挂 —— **未通过**

### 核心事实：卡内 3/3「挂死」job 全部 completed/success

`GET /actions/jobs/<job_id>`（as_of 20:25Z–20:27Z，逐条）：

```text
run 36176522020  job 108208345228 'Control Tower Gate Tests (windows-latest)'
   status=completed concl=success  started=19:37:17Z  completed=20:14:47Z
   step 'Run hermetic control-tower gate tests' completed/success 19:37:30Z -> 20:14:43Z   (37m13s)
run 36177248370  job 108210733404 'Control Tower Gate Tests (windows-latest)'
   status=completed concl=success  started=19:37:27Z  completed=20:11:43Z
   step 'Run hermetic control-tower gate tests' completed/success 19:37:40Z -> 20:11:40Z   (34m00s)
run 36182218912  job 108227059678 'Control Tower Gate Tests (windows-latest)'
   status=completed concl=success  started=19:51:44Z  completed=20:27:25Z
   step 'Run hermetic control-tower gate tests' completed/success 19:51:57Z -> 20:27:22Z   (35m25s)
同 run 的对照（ubuntu 变体，同一 step）：
   36176522020 job 108208345067  19:39:39Z -> 19:43:05Z  =  3m26s  success
   36177248370 job 108210733318  19:38:04Z -> 19:41:36Z  =  3m32s  success
   36182218912 job 108227059613  19:51:39Z -> 19:56:08Z  =  4m29s  success
```

⇒ 结论：Windows 侧 hermetic step **不是挂死，是慢**——**34–37 min vs ubuntu 3.5–4.5 min（≈9×）**，且三次全部 **success**。卡内 `` `origin/pr/841:task-state/D1017.json` L3 `` 的标题「长挂 —— run **永不 completed** ⇒ 所有 PR 合不了」与 L38 的 impact 句子**被自家引用的证据推翻**（三条 run 现均为 `completed`，我实测 36182218912 `completed/success @20:27:26Z`）。

### 机理错配（P0-1 的另一半）：主延迟在 **run 级 runner 排队**，不在 job 内

对同一三条 run 逐 job 取 `started_at` 最小值：

```text
run 36176522020  created 18:55:50Z → 首个 job 启动 19:34:20Z   ⇒ 空档 38m30s（期间无任何 job 在跑）
run 36177248370  created 19:02:50Z → 首个 job 启动 19:35:02Z   ⇒ 空档 32m12s
run 36176205440  created 18:52:46Z → 首个 job 启动 19:28:05Z   ⇒ 空档 35m19s
run 36182218912  created 19:51:31Z → 首个 job 启动 19:51:33Z   ⇒ 空档 2s（该时段队列已恢复）
```

⇒ 现象与 `` `origin/pr/805:docs/synova/coordination/问题修复台-20260925.md` L46 ``（FIX-019「33 queued 不消费；最老等 69 分钟；CTO 取消 4 条挂死 in_progress 90/72/69/46 分钟」）**同源**：run 创建后 30–38 min 无任何 job 获得 runner（外部队列停摆），恢复后各组 job 齐发。卡内 L47 的排除论证「三个 job 均已有 runner 分配 ⇒ **不是排队等 runner**，是 job 内挂起」在方法上无效——"有 runner 分配"是排队**结束之后**的状态，不能反推"从来没有排队"。

### 证据指针错配（P1）

卡内 L43 的第三条 `{"run": 36177248370, "job_id": 108210733295, ...}`：该 job_id 实为**同 run 的 `TypeScript + Lint + Iron Laws` job**（`GET /actions/jobs/108210733295` ⇒ `name='TypeScript + Lint + Iron Laws'`、`completed/success`、19:36:07Z→19:38:19Z = **2m12s**），真正对应该 run 的 windows job 是 **108210733404**。⇒ 3 条证据指针中 1 条指到了"2 分钟就绿了的 job"，证据可核性不成立。

### 候选止血方案的风险（P1）

卡内 L53 的止血项「给该 step 加 `timeout-minutes` ⇒ 把『无限挂』变成『有界红』」在本批实测下**有反效果风险**：该 step 的实测耗时 **34m00s / 35m25s / 37m13s（三次全部 success）**，任何 < 40 min 的 timeout 会把**绿转红**，直接制造新的一类误红。全仓现状：`timeout-minutes` 在 `.github/workflows/ci.yml` 出现 **0 次**，仅 `.github/workflows/desktop-build.yml:76`、`:91` 有先例。若仍要加，须先按 ≥45 min 定值并声明依据。

### #841 与声称不符项

| # | 项 | 事实 | 级别 |
|---|---|---|---|
| 1 | 标题/impact「永不 completed / 全仓所有 PR 无法合并」 | 3/3 被引 job 已 completed/success；症状（合并停滞）另有其因（慢 + 排队）。合并侧旁证：`search/issues?q=is:pr+is:merged+merged:>=2026-09-25T16:00:00Z` ⇒ 仅 #816(17:41:55Z)/#804(17:01:18Z)，此后 ~2h44m 无合并 —— **症状为真，机理不真** | P0（卡内 P0 结论不成立） |
| 2 | L43 job_id 108210733295 | 实为 TypeScript job（2m12s success） | P1 |
| 3 | L47 not_runner_supply 论证 | run 级空档 32–38 min 实测存在，排除法不成立 | P1 |
| 4 | L53 timeout-minutes 止血项 | 未给值；实测 34–37 min ⇒ <40 min 即转红 | P1 |
| 5 | 平台归因「Windows 专属」 | 成立（ubuntu 同 step 3.5–4.5 min） | ✅（保留） |

**#841 裁决：未通过**（P0×1 = 卡内 P0 判定被物理证据推翻；P1×3）。**退回要求**：把机理改写为「Windows hermetic step 慢（34–37 min，ubuntu 3.5–4.5 min，≈9×）+ run 级 runner 排队空档 30–38 min（外部，FIX-019 同源）」；删「永不 completed」；修正 job_id；`timeout-minutes` 若保留须写明 ≥45 min 及其依据。**底层问题真实存在，卡不该作废，只该重写。**

---

## 五、#822 / #805 治理件抽验

### #822 D980 工程派单（抽验）—— 通过

| 抽验项 | 结果 | 证据 |
|---|---|---|
| D943 强制表头（断面/单一事实源/作废口径/检查命令） | ✅ 齐备 | `` `origin/pr/822:docs/synova/coordination/派单-治理体系最小充分形态-工程派单-20260926.md` L3-L8 `` |
| 断面 `0.1.7-rc.1 @ 46a7f68b` 与唯一源一致 | ✅ | `docs/synova/coordination/DSH-断面.json:4-L5`（version/head 逐字相同）；superseded 口径（alpha.2 @ 00102833）在表内 ✅ |
| 冻结口径 `check-*` 48/49/47、五种数法 47/48/49/54/71 | ✅ 我实测 48 / 49 / 54 / 71，逐值吻合 | 见 §二 命令块；`47（main）` 的机制亦独立成立（`b8573c7a` 缺 `check-preset-bundles.sh`） |
| `pre-commit-check.sh` 1603 / 外部调用点 29 | ✅ 逐值吻合 | §二 命令块 |
| 门禁命中 36 标签 / 17 个 hit=0 / 4,873 行 | ✅ 我按快照独立复算 36 / 17 / 4873 | §三 命令块 |
| 写集 vs 实际提交 | ✅ 4/4 一致 + `.claude/bypass.log`（自动钩子登记） | `git diff --name-status ee721b0ab..origin/pr/822` |
| 铁律 49（决策沉淀） | ✅ memory note 在 `memory/notes/implemented/2026-09-26-gov-minimal-dispatch.md` | 同上 |
| 冻结断面 `origin/main = ef299746` | ⚠️ **已前进**（现 `ee721b0abf52`）；件内同段自带规则「若已前进 ⇒ 本单命令须在新断面重跑」✅ 自洽 | `` `派单-...-20260926.md` L49-L61 `` |
| `门禁标签 41`、`task-state 364 / 仅 30 张由 alloc-task-id 登记`、`src+tests : scripts+docs ≈ 1:326` | 未核（缺被审方计数脚本；我用自己的口径测得 D 卡 **370**（漂移 +6）、main 近 14 天非 merge 提交的文件级触碰 `src+tests=125 : scripts+docs=1613` = **1:12.9**，与"治理占绝对多数"同向） | 见 §九 |

**裁决：通过（抽验）**，P2×2：① 冻结断面已前进（按件内规则重跑即可）；② 三项数值未核（见 §九），建议随件附复算脚本。

### #805 D978 问题修复台（抽验）—— 有条件通过 = 未通过

| 抽验项 | 结果 | 证据 |
|---|---|---|
| 唯一登记处 + `FIX-###` 不占 D# 号段 + 属主/状态/闭环判据四列 | ✅ 结构齐备，19 条 | `` `origin/pr/805:docs/synova/coordination/问题修复台-20260925.md` L23-L46 `` |
| 台账行数 vs 摘要 | ❌ **19 行台账，摘要仍写「总条目 17」** | 件内 L10-L12 摘要行 vs L20-L46 的 19 条 `FIX-001…019` |
| 摘要内部分项 | ❌ 摘要列 `登记 6 ｜ 在修 7 ｜ 待收件 3 ｜ 已闭环 0 ｜ 已升级 4`：台账实际 `登记 7（现写"在修 6"）｜ 在修 6 ｜ 待收件 3 ｜ 升级 1`，且 6+7+3+0+4=20 ≠ 17（自相矛盾） | 同上（逐行计数命令见 §十） |
| 「✅已闭环」标注与 main 事实 | ❌ FIX-008（L34）写「✅ 已闭环（PR #817 5ebafe78）」、FIX-006 写「✅ 已由 Mac 小队实测闭环（PR #821 966a6374）」，但 `git merge-base --is-ancestor origin/pr/817 origin/main` / `origin/pr/821` 均**非祖先**（两 PR 未合 main）；同行"状态"列仍写"登记" ⇒ 同一行内自相矛盾 | §十 命令块 |
| FIX-019 与外部队列停摆的对应 | ✅ 与我 §四 的独立观测同源（30–38 min 空档 + 3/3 job 最终 success） | §四 命令块 |

**裁决：有条件通过 = 未通过**（P1×2）。**退回要求（成本约两行）**：① 摘要行改为实际计数（总条目 19，登记/在修/待收件/升级逐项重算）；② FIX-006/008 的「✅已闭环」与"PR 未合 main"对齐（改标「在修（#817/#821 未合）」或补 `merged_at` 证据）。

---

## 六、跑偏第二道（北星三问，零上下文复判）

对照 `.claude/PRODUCT-BRIEF.md`（L8-L16 定位、L18-L21 用户）逐问机械判断：

| 问 | 判读 | 依据 |
|---|---|---|
| ① 服务真实用户场景吗（GA / 企业主）？ | **不直接服务**——6 件全是治理/CI/台账面，无一触及诊断链路或用户可见行为。**不判偏离**，但必须承认"本批零产品推进" | `.claude/PRODUCT-BRIEF.md:20-L21`（真实用户 = GA / 企业主）；`` `派单-...-20260926.md` L13 ``（创始人明示：治理是"回归 48 格主线之前"的前置） |
| ② 让模块更接近终态吗？ | 治理面终态推进**成立且有终止条件**（六阶段 + §六"连续两周治理提交 < 主线提交"）；但**产品面终态零推进**，方向风险真实 | 我独立实测：`git log --since=2026-09-11 --no-merges origin/main` 文件级触碰 = `src+tests 125 : scripts+docs 1613`（**1:12.9**），与件内元判据同向 |
| ③ 做完后 Synova 还是"驻扎企业、主动诊断、为增长服务"的 Agent 吗？ | **不变味**——本批不触碰产品行为（仅 ci.yml 触发面 + 文档 + 卡） | `.claude/PRODUCT-BRIEF.md:10-L14` |

**结论：不判偏离**，登记 2 条方向风险（P2×2，归 CTO 派单流程）：
1. **6 件全部缺北星锚定块**（我逐件 grep `北星\|PRODUCT-BRIEF` ⇒ **0/6 命中**）。治理件虽非产品 dev doc，但"治理吞主线"只能靠终止判据 + 定期实测防住 —— 建议派单模板（与 D840 §〇-b 五必填同族）增一节「北星锚定 + 本批终止判据」。
2. **元判据必须有人在跑**：1:12.9 与件内 1:326 都指向"治理占绝对多数"，而 §六 只有判据、**没有任何一处机器化测量点**。治理批次本身也需要一个"何时停"的可核信号，否则第二道防线（本审计）也拦不住"治理自增"。

---

## 七、错误归因与 M 码（防再犯的钥匙）

| 发现 | 级别 | M 码 | 归因 | 该改的预设/防线 |
|---|---|---|---|---|
| #841 把"慢（34–37 min）"判成"永不 completed / job 内挂起" | P0 | **M2**（声称 vs 事实：把推断写成事实） | implement（卡/证据写作）+ control-tower（立卡判据缺失） | M2 强化（**不新增 M 类**）：时间性断言（"永不/一直/挂死"）必须附①观测时刻 ②该 run/job 的 `status`+`started_at` ③判定下界时长 ④同平台对照完成样本 |
| #841 job_id 108210733295 指错对象 | P1 | M2 | implement | 证据指针须"命令可复现"：写 job_id 的同时给出取数端点 |
| #841 timeout-minutes 未给值（实测 34–37 min 即会转红） | P1 | M2 | implement | 止血项必须带"参数取值依据"（本次实测值即依据） |
| #836 证据件 §二 numstat「10/1」≠ 最终 12/1；YAML 块缺 `types:` | P2 | M7（文档-实现漂移） | implement（交付件自带证据） | 证据件的每段命令输出须带 as_of+ref（#838 已示范）；或把 D703 回放扩到 `docs/synova/coordination/*证据*` |
| #836 §五 的 D981 patch 不在仓库 → 等价性核验不可复现 | P2 | M2 | control-tower（交付物入库缺口，CT-40 同族） | CT-40 扩"外部交付的补丁/工件" |
| #838 反例 `scripts_added=7` 含栈式父 PR 5 件 | P2 | M2 | devdoc（口径件） | §7.5 边界补一条"栈式 PR（base≠main）基数含父 PR" |
| #838/#840 `write_set` 计数 98/99（dict vs list）未声明 | P2 | M2 | devdoc | 口径件须声明"非空 list"或"非空任意形态" |
| #805 摘要 17 vs 台账 19；登记/在修互换；两处 ✅已闭环 而 PR 未合 main | P1 | M2 | implement（治理件）+ control-tower | 台账类交付加"摘要由台账行自动生成"（禁手写摘要）；闭环标注必须绑 `merged_at` |
| 门禁语义变更未 bump VERSION（V4.9.1 停在 2026-09-06） | P2 | M1 家族（缺执行体） | control-tower | CT-42 存量第二次实证（#836）；建议纳阶段 4 |

**M 表使用情况：全部命中既有 M1/M2/M7，无新增 M9 —— 按 anti-bloat 原则只强化既有类，不每错加脚本。**

---

## 八、L4 防线缺口收割（"本该拦住它的防线是什么？为什么没拦住？"）

| # | 该拦住的防线 | 为什么没拦住 | 收割动作（一类一机制） |
|---|---|---|---|
| G1 | CTO 立卡/证据件的"事实 vs 推断"分界（M2） | 现有 verifier 要求"给 file:line"，但**不区分**"观测到的状态"与"由状态外推的机理"；"永不/挂死"这类**时间性断言**没有任何下界要求，于是 33 分钟的 in_progress 被写成了"永不完成" | persona 规则一条（**不加脚本**）：时间性断言必须四件套（观测时刻 / status+started_at / 下界时长 / 同平台对照样本）。落 `squad-discipline` 或 dev-doc 写作规范 |
| G2 | 证据件"引用命令输出"的自动复算（D703 回放） | D703 的回放只覆盖 `docs/plans/codex/implementation/SYNOVA-IMPL-*.md`（按文件名 D 号找 `scripts/ci/verify-dXXX.sh`），**覆盖不到 `docs/synova/coordination/*证据*.md`** → #836 的 numstat 陈旧无人复算 | 阶段 4 时把回放清单扩到"证据件目录"，或统一要求证据件每段输出附 `as_of + ref`（#838 已自然形成范式） |
| G3 | 交付物入库硬要求（CT-40） | CT-40 只覆盖"dev doc + brief"，**不覆盖"外部交付的补丁工件"**（#836 §五 的 `D981-ci-concurrency.patch` 全仓 0 命中，等价性核验因此不可复现） | CT-40 扩一条：任何被作为证据引用的工件必须入库或给出可解析引用 |
| G4 | 栈式 PR 的 CI 保护（M3 家族：机制在、面没覆盖） | `.github/workflows/ci.yml:7` 只对 `pull_request: branches: [main]` 生效 ⇒ **PR #729（base=`feat/d925-deps-interface`）零 CI**；#838 的反例恰好引自这样一个 PR，基线因此被父 PR 污染 | 阶段 4「必需检查聚合」时一并设计（stacked PR 的触发面） |
| G5 | 门禁语义变更的版本锚（CT-42） | CT-42 只有判据、无执行体（CI workflow 也不在其脚本扫描面内）→ #836 改 CI 判定面而不 bump | 阶段 4 与"门禁脚本变更须 bump"合并落地 |
| G6 | 治理件的方向锚（北星 front-matter） | 北星锚定块是 dev doc 的硬前提，**治理/派单件不在其适用范围**（6/6 未命中），于是"治理吞主线"只能靠感知 | 派单模板加「北星锚定 + 本批终止判据」（与 D840 §〇-b 同族），一处改动覆盖此后所有派单 |

**本轮不新增 M 类**：G1–G3/G6 属 M2 家族强化，G4–G5 属既有 CT 队列条目。

---

## 九、无法核项清单（含原因，绝不含糊）

| # | 项 | 原因 |
|---|---|---|
| 1 | #836 的 `cancel-in-progress` 与**必需检查**的交互（被取消 sha 的 check 呈 cancelled 是否阻塞合并） | 需按时序构造多次推送的对照实验，本轮未取得样本 |
| 2 | #836 §五 `D981-ci-concurrency.patch` 原文（811 B / +11-1） | 工件不在仓库（`grep -rl D981 .` 0 命中）；只能核结论面 |
| 3 | #836 ⒝ 的自建测试分支 `feat/n12-no-trigger-test-20260926` 本体 | 分支已被删除（`git ls-remote` 0 命中）；只能以"全窗口 32 条 run 无该分支"间接成立 |
| 4 | #822 的「门禁标签 41」「task-state 364 / 仅 30 张由 alloc-task-id 登记」「src+tests : scripts+docs ≈ 1:326」 | 未随件提供复算脚本；我自算口径与件内不同（D 卡 370；文件级 1:12.9），只能判"同向" |
| 5 | #840 的 artifact **zip 下载**（200/1170B 解包 21 行） | 本轮改为核 `GET /actions/artifacts/<id>`（`size_in_bytes=1170` ✅）；未重复下载解包（避免与匿名额度/时点纠缠） |
| 6 | CI 侧读数在报告落库后仍会漂移 | 全部 CI 结论均自带 as_of（20:25Z–20:27Z）；复跑须带同时刻读数 |

---

## 十、复现命令清单（逐条可复制；CI 侧需 token 或匿名额度可用时）

```bash
# ── 0. 取被审对象（只读，不改任何被审分支）
git fetch origin --prune
git fetch origin '+refs/pull/*/head:refs/remotes/origin/pr/*'
git rev-parse origin/pr/836 origin/pr/838 origin/pr/840 origin/pr/841 origin/pr/822 origin/pr/805

# ── 1. #836 判定面（④⑤）
git diff --numstat ee721b0ab..origin/pr/836 -- .github/workflows/ci.yml        # 期望 12  1
git diff ee721b0ab..origin/pr/836 -- .github/workflows/ci.yml | grep -E '^\+' \
  | grep -E 'if:|continue-on-error|needs:|timeout-minutes'                     # 期望 0 命中
diff <(git show ee721b0ab:.github/workflows/ci.yml | grep -E '^  [a-zA-Z0-9_-]+:$') \
     <(git show origin/pr/836:.github/workflows/ci.yml | grep -E '^  [a-zA-Z0-9_-]+:$')   # 期望 0 差异
node -e 'const y=require("js-yaml"),f=require("fs");const d=y.load(f.readFileSync("/tmp/ci836.yml","utf8"));console.log(JSON.stringify(d.on),JSON.stringify(d.concurrency),Object.keys(d.jobs).length)'
git show origin/main:.github/workflows/ci.yml | sed -n '3,7p'                  # 合入前 main 仍是 [main, feat/*]

# ── 2. #836 ⑹ 定因（结构性无 concurrency 组）
git show 92c155c42d:.github/workflows/ci.yml | grep -c '^concurrency:'          # 期望 0（该 run 的 workflow 版本）
TOK=$(grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml | sed 's/.*GITHUB_TOKEN:[[:space:]]*//')
curl -s -H "Authorization: token $TOK" https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/36176205440/jobs \
  | python3 -c 'import json,sys;[print(j["name"],j["status"],j["conclusion"],j["started_at"],j["completed_at"]) for j in json.load(sys.stdin)["jobs"]]'

# ── 3. #841 P0 证伪（同一组 job，端点直取）
for j in 108208345228 108210733404 108227059678 108210733295; do
  curl -s -H "Authorization: token $TOK" "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/jobs/$j" \
   | python3 -c 'import json,sys;d=json.load(sys.stdin);print(d["run_id"],d["id"],d["name"],d["status"],d["conclusion"],d["started_at"],d["completed_at"])'
done

# ── 4. #838 口径 + 门禁探测（与件内逐字对照）
git ls-tree -r --name-only origin/main -- scripts | grep -E '(^|/)check-[^/]+$' | grep -v '^scripts/audit/' | wc -l   # 48
probe(){ printf 'NEWGATE_PROBE %s scripts_added=%s gate_surface_changed=%s\n' "$1" \
  "$(git diff --name-status origin/main..$1 -- scripts | awk '$1=="A"' | wc -l | tr -d ' ')" \
  "$(git diff --name-status origin/main..$1 -- scripts/pre-commit-check.sh scripts/pre-push-check.sh scripts/control-tower/ .github/workflows/ | wc -l | tr -d ' ')"; }
probe 661b2c483702 ; probe 8c89817e5365 ; probe 1b7bbd381818
git diff --name-status origin/feat/d925-deps-interface..8c89817e5365 -- scripts      # 只 2 件（栈式父 PR 污染基数）
git ls-tree 8c89817e5365 -- scripts/control-tower/k3-merge-gate.sh                   # 100755
git show 8c89817e5365:.github/workflows/ci.yml | sed -n '137p'                       # 调用点

# ── 5. #840 自建匿名 clone 独立复跑（不采信被审方产物）
git clone --quiet https://github.com/tangbaobao520/SynovaAgent.git /private/tmp/k3-indep-b2-check
cd /private/tmp/k3-indep-b2-check
find scripts -type f -name 'check-*' | wc -l ; wc -l < scripts/pre-commit-check.sh
git -c core.quotepath=false ls-tree -r --name-only origin/main | grep -c '号段水位'    # 1（不加 -c 得 0）
git fetch --quiet origin chore/D1009-stage0-log-archive:refs/remotes/k3
git ls-tree -r --name-only refs/remotes/k3 -- docs/synova/coordination/gate-logs-20260926/ | wc -l   # 3
for p in $(git ls-tree -r --name-only refs/remotes/k3 -- docs/synova/coordination/gate-logs-20260926/); do
  echo "$(git show refs/remotes/k3:$p | shasum -a 256 | cut -c1-64)  $(git show refs/remotes/k3:$p | wc -l)  $p"; done

# ── 6. #805 台账 vs 摘要
git show origin/pr/805:docs/synova/coordination/问题修复台-20260925.md \
  | grep -cE '^\|\s*\*\*FIX-[0-9]+\*\*'                                              # 19（摘要写 17）
for p in 817 821; do git merge-base --is-ancestor origin/pr/$p origin/main && echo "$p merged" || echo "$p NOT-merged"; done
```

---

## 十一、状态机回填（不靠口头转达）

- 本报告落 `docs/synova/audit-reports/`（分支 `audit/k3-20260926-b2`，**只含报告**，回流出创始人合并）。
- **未改动任何被审分支**，未触碰 `scripts/audit/**`，未修改任何审计脚本或审计标准。
- 各任务的 `task-state/D10xx.json`（只存在于各自 PR 分支）由**编码 session 按 §〇 机器可读块回填**：`audit.verdict` = PASS / PASS / PASS / FAIL / PASS / CONDITIONAL_PASS，`audit.report` = 本报告路径，`audit.p0p1p2` 取 §〇 表；#841、#805 需先按 §四/§五 的退回要求改写后再送复审（只审变更）。

---

## 附：本批机械门禁

```bash
python3 scripts/control-tower/check-k3-report.py docs/synova/audit-reports/2026-09-26-K3-第二批-N12与阶段1三件.md --repo .
```
