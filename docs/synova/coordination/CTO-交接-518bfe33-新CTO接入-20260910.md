# CTO 交接文档 — 旧 CTO 会话 518bfe33 → 新 CTO 会话

> 生成: 2026-09-10 | 交接人: Claude Code (Mac) | 接收方: 新 CTO SESSION
> 前置文档: `CTO履职总账-旧CTO-4d509874-提取20260909.md`（那是**上一任** CTO 4d509874 的账本，已归档）
> 本文交接的是**任期 CTO 会话 518bfe33**，已由创始人决定弃用。

---

## 〇、必读：518bfe33 的处置结论（禁止再动）

**结论：该 session 弃用，不再修复。** 创始人 2026-09-10 明确决定。

### 故障根因（已定位，非猜测）

`518bfe33` 是**全库 172 个 session 中唯一** `openTurnStartSeq != null` 的会话——这解释了"为什么只有它连不上 LLM"。

| tag | projSeq | v3 maxSeq | v0 maxSeq | 投影 openTurnStartSeq |
|---|---|---|---|---|
| **518bfe33** | 923224 | **9363** | 923273 | **911371** ← 越界 |
| cf7bad61 | 123658 | 716 | 123658 | null（正常）|
| 5c51cb…3 | 87410 | 603 | 87410 | null（正常）|

机理：v0→v3 迁移**对事件 seq 做了重编号**（v0 maxSeq 923273 → v3 maxSeq 9363），而投影缓存仍持 v0 空间的值。

1. `dsh-session-projection` 恢复校验 `row.seq >= beforeBase && row.seq <= endSeq` 失败（923224 ≤ 9363 为假）→ 抛 `cannot restore from seq N … re-read from seq 0`；
2. 同时 `dsh-agent-instructions` 的 `stepIsOpen()` 恒为 true（其 workspace-context 队列只有 `step/end` 才 flush）→ 主线程忙循环。

### 受伤时间线（诚实记录：确由恢复操作诱发）

```
13:51:17  投影写入，带 open turn
14:22:45  日志记录 turn/end {kind:"interrupted"}   ← 强制停内核所致
          投影未再同步 → 缓存与日志分叉
```

### 已做的处置（唯一改动，且只碰派生缓存）

- 文件：`~/.dsh/storages/session_projcache/sessions/session-518bfe33-….json`
- 改动：**2 行**（`openTurnStartSeq: 911371 → null`，`kind: "start" → "end"`）
- 验证：57948 → 57944 字节（-4）；`diff` 仅 2 行；其余 21 个 row 字段逐字节相同；identity/version 相同
- 备份：`~/.dsh-projfix-20260910-165158/projcache.json`
- 效果：全库 `openTurnStartSeq` 非空计数 0/173；内核 CPU 142% → 0.3%，RSS → 132MB
- **但该 session 仍无法对话** → 创始人决定弃用

⚠️ **源日志（`session.jsonl.zstd` / `session.v3.jsonl.zstd`）从未被修改。** 新 CTO 不得再尝试修复该 session。

---

## 一、当前环境事实

### DSH 两层结构（勿混）

| 层 | 路径 |
|---|---|
| 桌面壳 | `/Applications/Deepseek Harness Desktop.app` |
| 内核 | `~/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/dependencies/dsh/` |
| node | `…/…/runtime/bin/node` |
| 会话日志（权威） | `~/.dsh/sessions/--Users-wane-SynovaAgent--/session-<uuid>/` |
| 投影缓存（纯派生） | `~/.dsh/storages/session_projcache/sessions/session-<uuid>.json` |
| 预设 | `~/.dsh/.agent-presets/<id>/{agent.cordis.yml,preset.yml}` |

**版本现状：内核 0.1.5-rc.1**（2026-09-10 我升级；含 DeepSeek-V41-Flash 适配）。

### 本次升级暴露的坑（已修，但要知道）

`dsh-persona@0.1.5-rc.1` 把 schema 从 `text` 改成 **`prefix`（必填）** + `suffix`/`complete`/`includeRuntimeContext`。
旧 `text:` 会导致**全部 6 个预设无法切换**，报错 `$.prefix missing required value`。

已修（每文件 1 行 `text:` → `prefix:`），备份 `~/.dsh-preset-fix-20260910-225449/`：

- 实盘 6 个：`synova-cto` / `synova-devdoc` / `synova-dsh` / `synova-k3-audit` / `securities-research` / `liangshen` — **已用真实 Standard Schema 校验器验证 6/6 通过**
- 仓库源 4 个（防 `install-dsh-preset.sh --install` 重装再坏）：`dsh-cto-draft/agent.cordis.yml`、`dsh-audit-draft/agent.cordis.yml`、`dsh-preset-draft/persona-block.yml`、`dsh-devdoc-draft/persona-block.yml` — **已改但在工作区未提交**

> 校验方式备忘：`Config['~standard'].validate(cfg)`（Standard Schema 接口），**不是** `safeParse`/`parse`。

---

## 二、项目总体推进位置

主线：**L1 控制塔质量批收尾 + K3 审计条件清零**。近 20 提交显示重心在 `ctl`(控制塔) 与 `audit`。

最近合并（main `755e857e`）：

| commit | 内容 |
|---|---|
| `755e857e` | D593-FIX2 条件①落地——棘轮 2→4 + 例外记账 + DI 立项（**D663**）(#479) |
| `47a0e812` | K3 复审 D593-FIX2 = CONDITIONAL PASS (#478) |
| `478dae53` | D662 专家名残留全量收口 dev-doc (#477) |
| `e53b5984` | D593-FIX2 确定性重建，三物证闭环 (#476) |
| `824c801b` | D658 验收合并 + 专家改名线闭环 (#475) |

> ⚠️ main 当前 **behind origin/main by 13**，开工前必须 `git fetch --all && git pull --ff-only`（铁律 0-1/0-3 双端硬阻断）。

---

## 三、在飞任务（新 CTO 接手第一优先级）

| ID | 标题 | task-state | 真实状态 |
|---|---|---|---|
| **D663** | ct-ci-hermetic-arch-ratchet | `claimed`（spec/impl 皆 null） | **已实现并合并**（main `755e857e` #479）——task-state 未回填，**需修正** |
| **D664** | pre-commit-check-grep-p-self-clean | `claimed` | brief 是**未填写的空模板**，未派单 |
| **D665** | main-build-job-failure-fix | `claimed` | brief 是**未填写的空模板**，未派单 |
| **D660** | 任务看板活动判定重排 | `impl_done` @ `1730c9a3` | 已合并（#470/#471/#472/#473），audit 空 |

**K3 待复**：D593-FIX2 条件②（CI 结论确认）——CTO 已答复"#476 Architecture job 红，红态合并 = CT-47 第三次实证，加严提案已上报创始人"，**升 PASS 待 K3 下批复确认**。

---

## 四、未闭环债务（`board-backlog.json` 18 条，择要）

**P0 级（诚实债 / 门禁失效）**

- `PLAN-m9-writeset-gate` — 合并级写集对账 gate。M2 族**第三次**（#449 夹带 D603 51 文件 / #442 夹带 6 文件 / D593-FIX 声称提交实未提交）
- `PLAN-resolve-commit-brief-bsd` — `resolve-commit-brief.sh` L60 `grep -oP` 在 BSD grep 下静默失效 → 认领解析可回退到过期 brief 造成劫持误拦（D660 提交实测被拦一次）。修法 `grep -oE` 或 `sed`，一行
- `PLAN-ironlaw-enforcement` — 铁律 40/41/44 在 D603 拉平后**零自动执法**，补真实 check 属控制塔域

**架构债**

- `PLAN-diagnosis-l5-di` — `diagnosis.ts` L1→L5 DI 注入重构（榈轮逆向例外的收紧任务）。落地后基线 4→2→0
- `PLAN-d660-data-lifecycle` — 数据生命周期五层阶梯设计（创始人 2026-09-10 提出：按时间清理是错误模型）
- `PLAN-expireold-wiring` — `evidence expireOld()` 存在但全仓零生产调用方

**测试/依赖债**

- `PLAN-react-markdown-dep` — `use-streaming-conversation` 未声明依赖，**干净安装下必失败**，前审 21/21 绿是污染安装态
- `PLAN-vitest-typecheck` — D473 暴露 TS2322 静默通过

**其他**：`PLAN-u5-u6-gate`、`PLAN-k3-cond-clear`（D590/D591/D602 条件收口）、`PLAN-mac-quality-batch2`、`PLAN-evidence-refresh`（36 stale）、`PLAN-vector-direction`、`PLAN-expert-rename-d583`（挂起）、`PLAN-win-d338-d357`、`PLAN-d1-src-store-coord`

> 已闭环：`PLAN-d593-fix2`、`PLAN-ci-syno-leak`（已证伪关闭——CI 红真因是棘轮 +2 + 沙箱 git 全局身份缺失）

---

## 五、红线与工作纪律（不可违反）

1. **审计红线**：DeepSeek Harness 与 Claude Code **永不修改** `scripts/audit/`、永不编写审计标准、禁止自我审计。审计结论**只认 K3 报告**，PR 审查 ≠ 审计。
2. **多机 PR 工作流**：main 唯一真相。一人一事一分支。**禁止直接 push main**。开工前 `git fetch --all && git pull --ff-only`，禁止在 `[behind N]` 状态开工或 push。禁止 force push 共享分支。
3. **改 A 不断 B**（创始人反复强调，最高优先级）：任何修复前先 grep 全仓库影响面；改完列清单（文件+行号+为什么）。**不要**在修复过程中顺手改动无关文件。
4. **不改他人工作区**：`.synova-wt-*` / `.sessions/*/repo` 是各任务的隔离 worktree，**不是**主仓库，不要碰。
5. **测试先行**：spec → test → impl → wire → review → merge。`grep -rn "新函数名" src/` 零结果 = 未接线。
6. **数据资产**：`data/synova.db` 每日 03:30 launchd 备份到 iCloud。**禁止直接 cp 数据库**。
7. **审计台账**：`docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md`；决策走 `DECISION-REFERENCE.md` 四步。

---

## 六、新 CTO 第一步动作清单

- [ ] `git fetch --all && git pull --ff-only`（当前 behind 13）
- [ ] 回填 `task-state/D663.json`（`claimed` → `impl_done` + commit `755e857e`）——现状与 main 不符
- [ ] 决定 D664/D665：空白模板 brief，要么派单填写，要么撤号
- [ ] 处置工作区 4 个 persona 源文件改动（见 §一）——按 PR 流程走，勿直接 push main
- [ ] 追 K3 对 D593-FIX2 条件② 的批复
- [ ] 优先级建议：`PLAN-m9-writeset-gate`（M2 族第三次）> `PLAN-resolve-commit-brief-bsd`（一行修）> `PLAN-react-markdown-dep`（干净安装必失败）

---

## 七、已知遗留（我这边未完成，如实交接）

| 项 | 状态 |
|---|---|
| 桌面 app 重启验证预设修复 | **未做**——校验器已 6/6 通过，但需创始人实测确认 |
| persona 源文件 4 处 | 已改工作区，**未提交**（需 PR）|
| 518bfe33 | 弃用。仅派生投影被改，备份在 `~/.dsh-projfix-20260910-165158/` |
| 5 个凭证轮换 | 早前会话暴露 `DEEPSEEK_API_KEY`/`MOONSHOTAI_CN_API_KEY`/`ZAI_CODING_CN_API_KEY`/`ZAI_API_KEY`/`GITHUB_TOKEN`，**未轮换** |
