# CTO 固化件 II — 研究院权威结论（2026-09-24）

> 与 `CTO-固化-最终方向与在制任务-20260924.md` 配套：**前者是"我们怎么干"，本件是"院方已定的方向与纪律"**。
> 标注约定：**【我实读】**= CTO 亲自逐字读；**【复核实测】**= 独立复核/子代理实测并附原始输出（CTO 抽验过关键项）；**【未逐字】**= 仅由复核转述，引用前须回源。

## 一、宪章级文件（**凡与之冲突的设计一律否决**；宪章只能由创始人修订）

| 文件 | 内容要点 | 标注 |
|---|---|---|
| `01-可扩展架构的定义与约束` | 三层能力（可扩展／免编程／**可验证**，第三层是命门）；**每个扩展点三个可机器判定问题：加了吗／接上了吗／生效了吗**；五条约束（分层／三问／三道护栏／物理可见／版本化）；七层 16 项扩展清单 | **【已逐字+实测复核】** |
| `02-企业内agent一致性保障` | 一致性**五层**（知识／上下文／标准／目标／**反馈**）+ 四个机制（**机制1 Role Pack 自称核心**／机制2 知识分层四级／机制3 输出契约／机制4 培训替代） | **【已逐字+实测复核】** |
| `02-企业内agent一致性保障` | 一致性五层：**知识一致／上下文一致／标准一致／目标一致／反馈一致**；宪章级（2026-09-24 创始人确认） | 【未逐字】 |
| 落实状态 | X24/X25「已立项（宪章级）」，**待落地机制** → 属我院独立卡范围 | 【我实读】 |

## 二、TD-001（版本锚定与解冻预案，创始人 2026-09-24 决策）

- **锚定**：DSH **`0.1.7-alpha.2`**（内核源码构建）；**唯一判据 = 测量对象 + 绝对路径**（源码树 HEAD `00102833`）；Desktop 打包运行时当日为**回滚**，不作判据。
- **强度**：锁定 + **计划解冻**（非永久冻结）；一句话："用冻结换零跟版成本，账单延期支付"。
- **解冻触发 T1–T5**：T1 发布非 alpha/rc 版本／T2 影响我们的安全漏洞且上游已修／T3 产品需要无法绕过的新能力／T4 客户合规要求／T5 本版本已知问题阻塞交付且无变通。
- **七项准入闸门**（解冻必须全过）：契约测试套件／打包冒烟／旧 session 可恢复／回滚单元=容器级版本化发布／回滚不丢数据／**禁止就地改**／准入失败不发布；**且用例必须穿生产入口**（不是 grep 命中）。
- **回滚档**：采用"同时锚定上一个可用版本"；候选 `0.1.6-alpha.2` 现状态=**候选，待准入验证**（降级硬拒绝：能退版本但**数据无损回退不成立**）。
- **允许平移型解冻**（锚定 → 平移到另一个 alpha）；否则"等正式版"= 永不解冻。成本 1.5–3 人日/版本步。
- **随锚定锁进来的已知问题**：含 macOS 沙箱行为、`session-query-sqlite` 默认 `:memory:`、`mcp-client`/`schedule`/`webhook`/`time-context` 默认零挂载、`session-log-deepseek` 默认开、headers 关不掉、`web-search-deepseek` 直连等。⚠️ **其中"45 个包不可达"的数字已作废**（见 §五）。
- **纪律**：**不能自己修 DSH 的 bug**（禁止就地改）→ 我方借鉴实现只能**在自己侧绕过或自研**。

## 三、研究议程 §七 的纪律（**对全院生效，我方同步遵守**）【我实读】

1. 不再引用 M3-1 的"不存在／为 0"类读数（机制判据可用，**数字与"不存在"结论作废**）。
2. **所有对象绑定必须写明「哪个仓库的 HEAD」**（无 `.git` 的目录 `git rev-parse` 会落到父仓库）。
3. 凡靠"扫描 `process.cwd()` 下某目录"得出"不存在"的结论，**必须先证明该目录被完整拷贝**。
4. **判据必须是「穿生产入口的用例」，不是 grep 命中。**
5. **数字使用规则**：**任何进「决策依据段」的数字，必须先出现在未核实登记册且状态为"已核实"**；源头标【未核实】/【证据不足】者**不得进决策依据段**。
6. **宪章级文件的证据要求**：必须自带可复核性 = **证据节 + 反例与失效条件 + 未核实清单 + 推断与事实分标**（理由：*宪章级文件自己不可复核，就约束不了任何人*）。

**7.6 七环研究链条**：① 取证 → ② **综合（⚠️ 历次事故全部出在这一环）** → ③ 独立复核（复核者自写探针、**不看原结论**）→ ④ 登记 → ⑤ 分流 → ⑥ 使用 → ⑦ 约束。
**使用说明**：不必每次全走；**但凡进入「决策依据」的产出，必须走满 ②③④⑤⑥**。
**为什么**：五次事故共同点全是**转述失真**（口径错/事实反了/口径混/方法漏洞/限定词丢失），不是取证错误。

## 四、议题状态

| 编号 | 状态 |
|---|---|
| **X17** | **已结案（2026-09-24）**：「每客户一实例 vs 多租户」是两个正交问题被混为一个；维持**每客户一实例**，力气花在**实例内的组织/权限/知识边界** |
| X24 | 可扩展架构验收机制（三问）— 宪章级已立，**待落地机制** |
| X25 | 企业内 agent 一致性五层 — 宪章级已立，**待落地机制** |
| X26 | 决策台账与组织学习 — **设计对、实现有、零实际调用**；待启动 |
| X27 | **防误报机制**：同一形状 = **把「缺失」翻译成「通过」**；修三条：**根身份硬门禁／缺失进 errors／覆盖率判据**；待启动 |
| X28 | 版本锚定 + 解冻预案（= TD-001） |

## 五、边界评估（14 件）的错误清单与 CTO 裁定

**必须先处理（我复现过的 8 条）**：
1. **「45 个包不可达」+ 六档闭包规模 → 数字作废**：其复跑脚本 R3 是**空壳 heredoc**；探针目录**无任何闭包脚本**；多口径互不相等（45 ／ 62·68 ／ 63·65）。
2. `dsh-client-runtime` **在 0.1.7-alpha.2 不存在**，而我方插件 `synova-dsh-dashboards` 仍在注入它（死声明，静默跳过）。
3. **任务四判据指向错消费者**：`src/sentinel/sentinel-loader.ts:222` 与 `:228` 的谓词作用于哨兵 `manifest.dependsOn.nodeTypes`，与 `targetNodeType` 无关；真消费者 `src/agent/data-ingest-service.ts:128` 按**文件名约定**解析，JSDoc 明写"当前 8 个映射类型均成立" → 照文档做会得出**错误的数据迁移裁定**。
4. **【05】"web 档无联网工具"与源码相反**：host 行 disable，但 `web-app/presets/*.patch.yml` 按 agent 重新挂上。
5. **【05】"会话格式三次"少一代**：实为四个迁移包（v0→v4，且 `v3→v4` 正落在锚定版）；且**漏了 `0.1.7-rc.1` 已发布**（改变 T1 时间预期）。
6. 【05】标题「四项前提已满足」与正文「⚠️ 降级为基本成立」并存；§八仍在请创始人裁**已裁事项**。
7. **登记册 §五"8 条源文件未改"实测全部已改**（3 条写册前就改了）；登记册"已核实"实测 **2/76**（而非 0）。
8. `边界E` 引用**系统性 -3 行**（实测 4/4）；E 的「N=1／客户是老板本人」判据**挂错文件行**。

**方法上可复用（`边界D` 六件套）**：① 断面绑定先行（根非 git 仓时禁出现 `git rev-parse`，改逐文件 blob 比对）② 自写探针复跑 ③ 同一判据横向对照多输入 ④ 口径裁定表 ⑤ 三类系统性错误专查（口径／断面／时序）⑥ 置信度分层 + "证据不足"与"无法核实"两张独立清单。

**⚠️ 时效教训**：实验根在 `.tmp` 内**会消失**（`exp-m3-capability` 已不存在）→ 证据必须落 **生成方式 + 校验清单**，否则不可复现。

## 六、明确挂起 / 待创始人裁决

- **挂起（不派工）**：所有 **MOVO 对照类**条目（M2 员工身份平面／M3 临时能力授权／M5 产物指标／M6 待办／M7 连续失败计数）——MOVO 侧 file:line **从未回源**。
- **待创始人裁决**：① 第二台干净 Mac（任务一前置）② TD-001 锚点 vs `0.1.7-rc.1` ③ M5 报告"推送即走 vs 存档一年" ④ BS-6 `pre-upload-validator` 降级语义（现为真 fail-open，与企业合规冲突）⑤ `shared/computes/**` 89 文件归属（可裁剪 vs 参考自研）。

### 一.1 宪章级复核结论（独立读手逐字通读 + CTO 抽验）

**纪律⑥ 四项合规（实测）**：① 证据节 **有**（01 附.1~.6 / 02 附.1~.5，行号命中率极高）② 反例与失效条件 **有**（各 R-1~R-7 + 总口径，**是全文合规度最高的一项**）③ 未核实清单 **有**，且**已逐条汇进共享登记册**（`未核实登记册.md:60/105-127`，16 项全对应）→ **这是最正面的发现，它同时满足议程 7.3⑤ 数字使用规则** ④ 推断与事实分标 **部分**（附节完整；**正文仅 3 处内联**，读者直接引正文时看不到标注）。

**01 的最大缺口（宪章级但不可执行）**：约束 2「三问」只有**问句形状 + 归属**，**没有一条可机器判定判据**——§3.2 的 16 项 × 3 问 = **48 格，零格填写**；约束 1 的"公式参数只允许填值"**无 schema 落点**（`transfer_function` 现为自由文本 `TBD — to be defined in compute phase`）。

**02 的最大缺口**：**机制1 Role Pack（02 自称"本文件的核心"）零实现零判据**（全仓 `RolePack|角色预设` 零命中）；机制3 输出契约**无一份契约实例**（`outputSchema|outputContract|schemaValidator` 零命中）；知识分层四级**无字段载体**。

**🔴 P0 — 宪章自身不在版本控制下**：`边界评估/` 整目录**未被 git 跟踪**（`git ls-files …/边界评估/ | wc -l` = **0**，而同树有 205 份被跟踪）→ 宪章**不可 diff、不可审计、不可回滚**，与其自身约束 5（"定义改动必须留痕：谁/何时/改前是什么"）直接冲突。**已立机制条 G-6（P0）**。

**文档错误（提交研究院回改）**：① `01:260`/`02:259` **引错出处**（称纪律 2 在 `README.md:106-111`，实际在 `:117`）② `01:159`"语义**零校验**"**以偏概全**——边写入侧属实，但哨兵侧存在宿主语义校验（`src/sentinel/sentinel-loader.ts:214-236`，只 `log.warn`）⇒ **准确表述应为"校验存在但 fail-open"**，否则按字面执行会**删掉一段有效校验** ③ `02:208/:209` 正文仍写"❌ 不存在"而附节标【待核】/【未核实】（01 做过内联补丁、02 没做）④ 02 缺"与既有的关系"节（登记不闭合）⑤ R-7"若再出现给不出 file:line 的条目则应质疑规则本身"**无触发器、无责任人**。

**✅ 阻塞项解开的**：`targetNodeType` **0/8 vs 8/8 的分水岭 = 比对谓词是否做 `$id` 末段归一化**（本体把结果类命名为 `FINANCIAL_OUTCOME`（`$id=outcome/financial`）、资源类为 `CLIENT`）。四种谓词实测：与 `$id` 全等 0/8 ／ label 全等（区分大小写）0/8 ／ label 忽略大小写 3/8 ／ **`$id` 末段归一化 8/8**。⇒ 不是数据缺失而是命名归一口径；两组读数在案，**裁定权仍归产品线**。

### 一.2 采纳的宪章落地机制（CTO 域）

| # | 机制 | 判据形态 |
|---|---|---|
| **G-6（P0）** | **宪章入 git + 修订留痕** | `git ls-files …/边界评估/*.md \| wc -l` ≥ 2（当前 **0**）；宪章 mtime 变化必须伴随同路径 commit |
| **G-1** | **宪章合规门禁** | 输入两份宪章：①有附节 ②有"反例与失效条件" ③有"未核实清单" ④抽取全部 `path:line` 逐条断言文件存在且行数 ≥ line；任一失败 exit 1（抓 F-1 形态，语义层留人工抽样） |
| **G-5** | **权限过滤单点门禁** | `grep -rn "conditions: \[\]" src/ --include='*.ts'` 命中即报告——实测 `auth.ts:355` 外还有 **`routes/documents.ts:85,112` 绕过唯一漏斗**（02 附.2 未覆盖的第二条路径） |
| **G-4** | **fail-open 校验点清单** | 扫 `log.warn` 且含"不存在／未注册／校验失败"的语义校验点，判定其后 10 行是否有 `continue/reject/errors.push`；只 warn 者入 fail-open 清单（`sentinel-loader.ts:224,231,235` 会命中） |
| **G-3** | **transfer_function schema 门禁** | 断言 `extensions/ontology/edge-types/*.json` 的 `transfer_function` ∈ `number\|{param:number}\|{ref:string}`；占位串计数 >0 报告（存量 42 量级），**新增**占位串阻断 |
| **G-2** | 扩展点判据矩阵门禁 | ⏸ **挂起**：需产品线先裁 U-1（`targetNodeType` 谓词）与"填值"边界，故与 **D941** 同批处理 |
