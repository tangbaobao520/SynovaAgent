# D1014 · N12 根治证据（ci.yml concurrency + push 触发收窄）

> 产出者：并行 CTO（`ci.yml` 唯一写者）｜日期：2026-09-26｜批准：CTO（R3 门禁/CI 语义变更）
> 交付：PR #836 ／分支 `chore/D1014-ci-concurrency`

## 一、N12 根因（CTO 2026-09-26 定位）

| # | 根因 | 实测证据 |
|---|---|---|
| ① | `on.push.branches: [main, feat/*]` ⇒ 每次 feature push 跑全量 CI（放大器） | `ci.yml:3-7` 原文 |
| ② | 全 workflow `concurrency` = 0 ⇒ 无"新 run 取消旧 run"⇒ 同分支无限排队 | `grep -n 'concurrency' .github/workflows/*.yml` → **零命中**（5 个 workflow 全无） |
| ③ | 86 open PR × 反复 push ⇒ 触发量 ≫ 消费量 | 全队列一度 **38 queued 不消费**；`feat/d962-2a-merge` 独占 **18 条** |

## 二、改动（10 insertions / 1 deletion）

```yaml
on:
  push:
    branches: [main]          # ← 原 [main, feat/*]
  pull_request:
    branches: [main]

concurrency:                   # ← 新增（顶层）
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
```

**唯一删除行** = `-    branches: [main, feat/*]`（CTO 指定收窄，非误删）；job/step 判定逻辑零改动。

## 三、静态判据（已实测）

```
$ node -e 'const y=require("js-yaml")…; console.log(JSON.stringify(d.concurrency), JSON.stringify(d.on.push.branches))'
{"group":"${{ github.workflow }}-${{ github.ref }}","cancel-in-progress":true} ["main"]
$ git diff --numstat .github/workflows/ci.yml
10      1       .github/workflows/ci.yml
$ git diff -U0 .github/workflows/ci.yml | grep '^-[^-]'
-    branches: [main, feat/*]
```

## 四、运行证据（CTO 要求 ⒜/⒝）

### ⒜ 同分支连推 3 次 ⇒ 前两次 cancelled、只留最新
（运行记录见下）

### ⒜ 同分支连推 4 次 ⇒ 前三次 cancelled、只留最新 ✅
```
$ runs API, branch=chore/D1014-ci-concurrency, as_of 2026-09-26T03:52+08:00
  run 36181480045 in_progress/None   pull_request  2026-09-25T19:44:08Z  ← 最新（存活）
  run 36181349714 completed/cancelled pull_request 2026-09-25T19:42:49Z  ← 被 36181480045 取消
  run 36181301748 completed/cancelled pull_request 2026-09-25T19:42:20Z  ← 被 36181349714 取消
  run 36181193597 completed/cancelled pull_request 2026-09-25T19:41:16Z  ← 被 36181301748 取消
```
**判据满足**：连推 4 次（PR 开 + push#2/3/4），**前三次全部 `cancelled`，只留最新一条存活**。
过渡态观察：新 run 先 `pending`，抢占时先取消上一条 `pending`/`in_progress`；最终稳态如上。

### ⒝ push 到 `feat/*` ⇒ 不触发；开/更新 PR ⇒ 触发 ✅
```
$ git push origin HEAD:refs/heads/feat/n12-no-trigger-test-20260926   # 自建一次性测试分支
  → 该分支 runs API:  (none) — push 到 feat/* 未触发 CI         ✅
$ 同期 chore/D1014-ci-concurrency 的 runs 全部 event=pull_request  ⇒ PR 面触发正常 ✅
```
测试分支验证后已删除（本地 + 远端），`git ls-remote | grep -c n12-no-trigger` = **0**；删除走 pre-push 门禁（"删除操作 — 跳过同步/secrets/golden/对账"），**无任何提交被删**。

### ⒞ `on.pull_request.types` 显式化 ✅
```yaml
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
```
`node js-yaml` 实测：`pr.types = ["opened","synchronize","reopened","ready_for_review"]`（原 GitHub 默认三型 + 新增 `ready_for_review`）。

### ③ 必需检查名不变 ✅（**12 个**，与 CTO 口径一致）
```
$ branches/main/protection → required_status_checks 去重后 = 12
  Architecture Check ｜ Checker Review (maker/checker) ｜
  Control Tower Gate Tests (ubuntu-latest / windows-latest) ｜ Golden Case F1 Gate ｜
  Integration Contract Check ｜ Test-Kit Architecture Tests (ubuntu-latest / windows-latest) ｜
  TypeScript + Lint + Iron Laws ｜ Vitest (1/2) ｜ Vitest (2/2) ｜ npm audit
```
`ci.yml` 的 **10 个 job 名一字未改**（`quality` 仍是 `TypeScript + Lint + Iron Laws`）；本次只动 `on:` 与顶层 `concurrency` ⇒ **判定面零改动**。

### ④ 不得削弱真保护 ✅
- 被取消的只有**同一 ref 的过期 run**（同一变更的冗余重跑），不是任何检查项；
- feature push 不再跑全量 CI，但**同一 job 集合仍由 PR 面覆盖**（⒝ 已证 PR 触发正常）；
- **未改任何 job/step 的通过/失败判定**，未新增跳过条件。

### ⒟ 纯文档 `paths:` 过滤 —— **评估结论：不建议加（附 DSH 对照）**
- DSH 对照实测：`/Users/wane/src/deepseek-harness-017/.github/workflows/ci.yml:17-19` 有 `concurrency`（与 CTO 给的写法逐字一致，本次即照抄）；**但 DSH 的 `ci.yml` 没有任何 `paths:` 过滤**——`paths:` 只出现在 `expected-filenames.yml`（工具类）与 `node-addon-system.yml`。
- 第一性原理：`TypeScript + Lint + Iron Laws` 是 **main 的必需检查**；workflow 级 `paths:` 会让"纯文档 PR"**根本不产生该 check** ⇒ GitHub 侧显示 *Expected — Waiting for status to be reported* ⇒ **PR 卡死**（比现在更糟）。
- 现状已有等效且安全的机制：`quality` job 内的 **`docsonly` 守卫**（纯文档时跳过重步骤、**job 仍报告**）。
- ⇒ **建议保留现状，不加 workflow 级 `paths:`**；若仍要减负，应在阶段 4 与"聚合必需检查名"一起设计，并单独提 K3。

## 五、附：本次落地过程发现的两处门禁摩擦（如实登记，非本卡修复范围）

1. **Q2 路径加反引号 ⇒ 认领计数归零 ⇒ 被判「他人文件」硬阻断**：
   `brief_parser` 会把反引号算进路径 ⇒ `match_path` 0 命中 ⇒ `resolve-commit-brief.sh` 回退到"别的声明了同一热文件的 brief"（本卡为 `D945-preset-bundle-migration`，它声明了 `.github/workflows/ci.yml`）⇒ `staging_guard.py` 判 block。
   实测：反引号版 `n=0` vs D945 `n=1` ⇒ 归 D945；裸写路径后 `n=4` ⇒ 归本卡 ⇒ guard `warn`（放行）。
   ⇒ **`ci.yml` 是全仓最热共享文件（10 张卡声明）**，凡新卡要改它，都踩同一坑。
2. **session 专属 `current-brief.<sid>` 无自动写入**：新 worktree 里 `task-start.sh` 未被调用 ⇒ 无该指针 ⇒ resolver 只能走认领回退。手工补指针（`.claude/current-brief.D1014`，gitignored）后路径正常。

<!-- push#3 触发用：本条在 push#3 中记录运行结果 -->
