# D1020 本批闭环：`tests/output/` 生成物出库（tests 侧 + `.gitignore`）

> 卡：**D1021**（分配器实际发号；见 §0）｜分支：`chore/D1021-tests-output-evict`｜worktree：`/private/tmp/synova-wt-squad-out`（父 `origin/main` = `ee721b0abf52`）
> 纪律：每值绑 `as_of`；本地实测值一律给原始输出；**CTO 改写版判据③按"部分达成"登记**；CI 拿不到即标「CI 未跑」。
> 硬约束遵守：**未碰 `src/**`**、未碰 `scripts/audit/**`、未碰主树、未做全局分支操作、**未新增脚本**。

---

## 0. 卡号说明（分配器权威 vs 计划号）

| 项 | 值（as_of = 2026-09-26T04:46+08:00） |
|---|---|
| 队长消息里的计划号 | D1020 |
| **分配器实际发号** | **D1021**（`alloc-task-id.sh "tests-output 出库"` → 输出 `D1021` / 已登记 `task-state/D1021.json`；rc=0） |
| D1020 为何被跳过 | **`/private/tmp/*/task-state/D1020.json` 已存在 1 份**（另一 session 的 worktree 已占号）⇒ 分配器的跨位置校验（含 worktree task-state）正确跳过 D1020 |
| 取号耗时 | 第一次读超 120s（我按纪律 kill 观察），随后该进程自行完成并打印 `D1021`（**同一 session 内第 2 次"取号慢"**，根因 = 已知 P0/D1019 的无超时 `git ls-remote`；**未手工造号**） |

> 取号纪律执行：**未手工造号**；分支名按分配器结果定为 `chore/D1021-tests-output-evict`（先建 D1020 名 → 发号后 `git branch -m`）。

---

## 1. 判据①：`.gitignore` 命中（`git check-ignore -v` 有输出）

```console
$ git check-ignore -v tests/output/expert-quality-cross-industry.json
.gitignore:90:tests/output/	tests/output/expert-quality-cross-industry.json
$ git check-ignore -v tests/output/expert-quality-report.json
.gitignore:90:tests/output/	tests/output/expert-quality-report.json
$ git check-ignore -v tests/output/expert-test-results.json
.gitignore:90:tests/output/	tests/output/expert-test-results.json
$ git check-ignore -v tests/output/e2e-report.html
.gitignore:90:tests/output/	tests/output/e2e-report.html
```
**判定：✅**（4/4 命中，出处 `.gitignore:90`）

新增规则（含理由注释，风格对齐既有条目）：
```diff
 .synova-wt-*
 .sessions/
+# D1021: tests/output/ 为测试运行生成物（生成物离库）
+# 由 tests/expert-quality/layer2-judge.test.ts / tests/run-experts-real.ts / tests/run-e2e-pipeline.cjs
+# （及 src/mvp-server.ts 同类写入）运行时重建；历史入库的 3 个 json 已 git rm --cached 出索引。
+tests/output/
```

---

## 2. 判据②：出索引不删工作区（`git ls-files` = 0）

```console
$ git rm --cached tests/output/expert-quality-cross-industry.json tests/output/expert-quality-report.json tests/output/expert-test-results.json
rm 'tests/output/expert-quality-cross-industry.json'
rm 'tests/output/expert-quality-report.json'
rm 'tests/output/expert-test-results.json'
（rc=0）

$ git ls-files tests/output | wc -l
0

$ ls -la tests/output/          # 工作区文件仍在（--cached 语义）
-rw-r--r--  26707  expert-quality-cross-industry.json
-rw-r--r--  35956  expert-quality-report.json
-rw-r--r--   2224  expert-test-results.json

$ git status --porcelain | grep tests/output
D  tests/output/expert-quality-cross-industry.json
D  tests/output/expert-quality-report.json
D  tests/output/expert-test-results.json
```
**判定：✅**（`git ls-files` = **0**；三条为**已暂存的出库**（`D ` 首列），工作区文件**未删**）

---

## 3. 判据③（CTO 改写版）：跑三处 + `git status` 干净 —— **部分达成**（两个独立原因，不许写成完全达成）

### 3.1 三处逐个实跑（原始输出，as_of = 2026-09-26T04:47+08:00）

| # | 命令 | 结果 | 是否写入 `tests/output/` |
|---|---|---|---|
| 1 | `npx tsx tests/run-experts-real.ts` | **rc=1，1s**（7 位专家全 `❌ 无 prompt 文件`） | ✅ **写了** `expert-test-results.json`（mtime 04:47:31） |
| 2 | `node tests/run-e2e-pipeline.cjs` | **rc=1，1s** | ❌ **未能运行到写盘点** |
| 3 | `npx vitest run tests/expert-quality/layer2-judge.test.ts` | **rc=1，4s**（`Tests 36 failed \| 5 passed`） | ✅ **写了** `expert-quality-cross-industry.json`（mtime 04:47:51） |

关键原始输出：
```console
# 1) run-experts-real.ts 尾部
    ❌ org                  0.0s  无 prompt 文件
    …
  结果已保存: tests/output/expert-test-results.json

# 2) run-e2e-pipeline.cjs 尾部（★ 前置故障：模块已不存在）
  管线失败: Cannot find module '../packages/engine-core/src/pipeline/diagnosis/measurement-pipeline'
  Require stack:
  - /private/tmp/synova-wt-squad-out/tests/run-e2e-pipeline.cjs

# 3) layer2-judge.test.ts 尾部
     385|     expect(globalAvg).toBeGreaterThanOrEqual(3.0);
   Test Files  1 failed (1)
        Tests  36 failed | 5 passed (41)
     Duration  3.37s
```
> 注：①③ 的失败原因均为 **`无 prompt 文件`**（专家 prompt 加载路径在现状树中不存在）——**与本次出库无关**，但使这两处无法真正"跑绿"；② 的失败是 **engine-core 退役后该 runner 的 import 已断**（铁律 46 迁移遗留）。

### 3.2 确定性补证（等价写路径验证）——写的就是代码里的确切路径
```console
（写前指纹 → 改写/新建 → 写后指纹）
  expert-test-results.json          991 B  04:47:31  →  1036 B  04:47:54   （run-experts-real.ts:210 的路径）
  expert-quality-cross-industry.json 1935 B 04:47:51  →  1970 B  04:47:54   （layer2-judge.test.ts:381 的路径）
  expert-quality-report.json        35956 B 04:43:54  → 35991 B  04:47:54   （见 §3.3）
  e2e-report.html                   —                 →    25 B  04:47:54   （run-e2e-pipeline.cjs:149 的路径）
  http-proof.html                   —                 →    25 B  04:47:54   （src/mvp-server.ts:191 同类路径）
  json 合法性回读: True

$ git status --porcelain | grep tests/output | grep -vc '^D  '
0                      ← ★ tests/output 无任何「新增/修改」条目（只有 3 条出库 D）
```
**判定：⚠️ 部分达成**，两处未达成**逐条登记**：
1. **`src/mvp-server.ts:190-191` 未纳入本卡**（CTO 裁定：src 侧另立子卡）⇒ 它写的 `tests/output/http-<jobId>.html` 只在本卡被**规则覆盖**（§3.2 的 `http-proof.html` 已证），但**该写入方本身不在本批范围**。
2. **`tests/run-e2e-pipeline.cjs` 当前不可运行**（`Cannot find module '../packages/engine-core/...'`）⇒ 它的 `e2e-report.html` 写盘点**无法由真实运行触发**；已用 §3.2 的等价写路径覆盖证明。
   （另：`run-experts-real.ts` / `layer2-judge.test.ts` 虽已真实写入，但**测试本身红**，原因是缺失 prompt 文件，属**前置故障**，不在本卡范围。）

### 3.3 附带发现：`expert-quality-report.json` **当前无写入方**
```console
$ grep -rn "expert-quality-report" . --exclude-dir=node_modules --exclude-dir=.git
./tests/expert-quality/README.md:52:- `tests/output/expert-quality-report.json`：结构化评分报告（用于趋势跟踪）
```
⇒ 全仓**只有 README 引用**、无任何代码写入该文件（其 mtime 与 checkout 一致 = 04:43:54）。**它是历史生成物/陈旧产物**：出库后不会再生成；如需追溯生成源需另查（**登记，不属本卡**）。

---

## 4. 判据④ 定稿：`tests/output` 引用清单（写入/读取/文档）+ "是否存在程序化读取方"

**命令与计数（as_of = 2026-09-26T04:48+08:00，改动前状态）**：
```console
$ grep -rn "tests/output" src/ scripts/ tests/ | wc -l
11
$ grep -rn "tests/output" . --exclude-dir=node_modules --exclude-dir=.git | wc -l
20
```
> **口径差异如实登记**：队长/CTO 口径为 **13**，本卡按指定三目录口径实测 **11**；扩到全仓（含 `memory/`、`.claude/`、`docs/`、`mvp-server.cjs`）为 **20**。三数各有其口径，**指定口径（三目录）以 11 为准**；13 可能与"含 docs/.claude 部分行"或不同 pattern 有关（**同一判据多口径，建议判据文本写死口径**）。

**逐条判定（11 条 = 指定三目录口径）**：

| # | 位置 | 行内容（摘） | 判定 |
|---|---|---|---|
| 1 | `src/mvp-server.ts:190` | `fs.mkdirSync('tests/output', …)` | **写入** |
| 2 | `src/mvp-server.ts:191` | `fs.writeFileSync('tests/output/http-${jobId}.html', …)` | **写入** |
| 3 | `tests/run-experts-real.ts:208` | `fs.mkdirSync('tests/output', …)` | **写入** |
| 4 | `tests/run-experts-real.ts:210` | `fs.writeFileSync('tests/output/expert-test-results.json', …)` | **写入** |
| 5 | `tests/run-experts-real.ts:213` | `console.log('结果已保存: tests/output/…')` | 日志/提示 |
| 6 | `tests/expert-quality/README.md:52` | `` `tests/output/expert-quality-report.json`：…（用于趋势跟踪）`` | **文档** |
| 7 | `tests/expert-quality/layer2-judge.test.ts:380` | `fs.mkdirSync('tests/output', …)` | **写入** |
| 8 | `tests/expert-quality/layer2-judge.test.ts:381` | `fs.writeFileSync('tests/output/expert-quality-cross-industry.json', …)` | **写入** |
| 9 | `tests/expert-quality/layer2-judge.test.ts:382` | `console.log('完整报告: tests/output/…')` | 日志/提示 |
| 10 | `tests/run-e2e-pipeline.cjs:148` | `fs.mkdirSync('tests/output', …)` | **写入** |
| 11 | `tests/run-e2e-pipeline.cjs:149` | `fs.writeFileSync('tests/output/e2e-report.html', …)` | **写入** |

**"读取方"结论（定稿）**：**零程序化读取方**。定向检索：
```console
$ grep -rnE "readFile|readFileSync|require\(.*tests/output|import.*tests/output|readdir.*tests/output" src/ scripts/ tests/
（对 tests/output 无命中；上面批量输出里的 readFile* 命中全部指向其它路径，无一指向 tests/output）
```
⇒ 写多读零 ⇒ 出库**不破坏任何读取链路**；`tests/output` 纯属"运行产物"。
**判定：✅**（并附口径差异说明）

---

## 5. 变更清单（本卡全部改动）

```console
$ git status --porcelain
 M .gitignore
D  tests/output/expert-quality-cross-industry.json
D  tests/output/expert-quality-report.json
D  tests/output/expert-test-results.json
?? .claude/task-briefs/2026-09-26-D1021-tests-output-出库.md   （未入库：本地认领件）
?? task-state/D1021.json                                       （入库：S1 声明）
```
另说明：`tests/output/` 下 5 个文件（3 改写 + 2 新建）**均为运行/验证产物**，被新规则忽略 ⇒ **不进 PR**。

---

## 6. 未清项登记

1. **判据③为"部分达成"**（两处独立原因，见 §3.2）：`src/mvp-server.ts` 属 src 侧另卡；`run-e2e-pipeline.cjs` 因 engine-core 模块缺失**当前不可运行**。
2. **两个 runner 测试本身红**（`run-experts-real.ts` / `layer2-judge.test.ts`）——原因 = **无 prompt 文件**（前置故障，非本卡）。
3. **`expert-quality-report.json` 无写入方**（仅 README 引用）⇒ 出库后为**陈旧产物**；追溯生成源需另查。
4. **口径差异**（判据④）：指定三目录 = **11**，全仓 = **20**，CTO 口径 = **13**；建议判据文本写死口径。
5. **CI：未跑**（本 PR 触发的 run 在报送时仍 queued）⇒ 无 CI 侧结论，本文件全部为本地实测值（每值已绑 as_of）。
6. **取号慢（第 2 次）**：同 session 内第 2 次遇到分配器 >120s（根因 = D1019 的无超时 `git ls-remote`）；本次未挂死（自行完成），未手工造号。

---

## 附：关键证据摘要（供 K3 匿名核）

- **判据①**：`git check-ignore -v` 4/4 命中，出处 **`.gitignore:90:tests/output/`**
- **判据②**：`git rm --cached` 三文件（rc=0）｜`git ls-files tests/output | wc -l` = **0**｜工作区 3 文件仍在
- **判据③（部分达成）**：`run-experts-real.ts` rc=1 **已写** `expert-test-results.json`（mtime 04:47:31）｜`layer2-judge.test.ts` rc=1（36F/5P）**已写** `expert-quality-cross-industry.json`（mtime 04:47:51）｜`run-e2e-pipeline.cjs` rc=1 **因 `Cannot find module '../packages/engine-core/...'` 未运行到写盘点**；等价写路径验证 5 条路径（3 改写+2 新建，含 `e2e-report.html`/`http-proof.html`）后 **`git status` 中 tests/output 的「非-D 条目」= 0**
- **判据④**：指定三目录命中 **11**（全仓 20；CTO 口径 13）；**写入 8 / 日志 2 / 文档 1 / 读取 0** ⇒ **零程序化读取方**
- **未改**：`src/**`、`scripts/audit/**`；**未新增脚本**；主树零 git 写
