# D1022 · T3 CI 止血包：全 10 job `timeout-minutes` + `docsonly` 早退推广

> 卡：**D1022**（取号见 §0）｜分支：`chore/D1022-ci-timeouts-docsonly`｜父节点：`origin/main` = `4345d6d6`
> head：**以 PR 页面为准**（截至 2026-09-26T11:5x 实测 `4bd95c58230d`；认证 API 12 位交叉验证一致）｜PR **#846**
> 纪律：判据 ⒜–⒟ **只认真 run 证据**；本文件每条数值均绑 as_of；多口径标口径；基线声明**只取 success**。

---

## 0. 取号（第 3 次分配器挂起，未手工造号）

| 项 | 值（as_of = 2026-09-26T11:40+08:00） |
|---|---|
| `alloc-task-id.sh "T3 CI 止血包"` | **>120s 未返回**（按纪律 kill 观察）⇒ 与已知 **P0/D1019**（无 `timeout`/`gtimeout` ⇒ `alloc-task-id.sh:152-159` 走裸 `git ls-remote` 无超时）同源；**本 session 第 3 次** |
| 处置 | 改用分配器**只读** `--check-id`：`D1022 → local-branch chore/D1022-ci-timeouts-docsonly`（**仅我自己的分支**，无第三方占用）；`D1023`/`D1024` 空闲 ⇒ 采 **D1022** |
| 是否手工造号 | **否**（用分配器自身的只读校验；分支名随号） |

---

## 1. 实现（只动 ci.yml；未碰 scripts/**、tests/**、src/**）

```console
$ git diff --numstat origin/main -- .github/workflows/ci.yml
201	1	.github/workflows/ci.yml        # 唯一删除行 = gate-integrity 的 `if: always()` 被合并
  新增 job 级 timeout-minutes : 10
  新增 step 级 timeout-minutes: 1
  新增 Detect docs-only 步    : 9
  新增 if 门控行              : 27
  新增 shell: bash            : 10
  新增 fetch-depth: 0         : 5
```

### 1.1 全 10 job `timeout-minutes`（公式 `max(P95×1.5, 中位数×3)`；口径 = **只取 success**；n=12；as_of=2026-09-26T04:47:47+08:00）
| job | 值 |
|---|---|
| quality | 6 |
| test（Vitest 1/2、2/2） | 8 |
| architecture | 6 |
| test-kit-architecture | `${{ matrix.os == 'windows-latest' && 2 \|\| 1 }}` |
| **control-tower-tests** | `${{ matrix.os == 'windows-latest' && 106 \|\| 14 }}` |
| integration-check | 6 |
| checker-review | 6 |
| audit | 1 |
| golden-case | 1 |
| gate-integrity | 1 |

`control-tower-tests` 的关键 step 另加 step 级：`Run hermetic control-tower gate tests` → `${{ matrix.os == 'windows-latest' && 60 || 13 }}`
（windows = CTO 定值 **60**；ubuntu 取 **13** 以严格小于其 job 级 14 —— 避免 step 超时大于 job 超时的无效配置；**此取值待 CTO 确认**）。
注释块已按 CTO 硬要求写入：`n=12 / 中位数 35.2min / P95 38.5min / 筛选口径=只取 success / as_of=2026-09-26T04:47:47+08:00 / 公式=max(P95×1.5, 中位数×3)`（`grep -c` 逐项 = 10/1/1/10/10/10 ✅）。

### 1.2 `docsonly` 早退推广（10/10）
```console
$ node -e '…断言 10 个 job 的 steps…'
  quality                  timeout=6                                            detect=1 gated=7
  test                     timeout=8                                            detect=1 gated=3
  architecture             timeout=6                                            detect=1 gated=4
  test-kit-architecture    timeout=${{ matrix.os == 'windows-latest' && 2 || 1 }} detect=1 gated=3
  control-tower-tests      timeout=${{ matrix.os == 'windows-latest' && 106 || 14 }} detect=1 gated=1
  integration-check        timeout=6                                            detect=1 gated=3
  checker-review           timeout=6                                            detect=1 gated=4
  audit                    timeout=1                                            detect=1 gated=1
  golden-case              timeout=1                                            detect=1 gated=4
  gate-integrity           timeout=1                                            detect=1 gated=4
✅ 10/10 达标（detect=1，无未 gate 的重步）
```
- `gate-integrity` 的 `always()` 步合并为 `if: steps.docsonly.outputs.docs_only != 'true' && (always())`（**本 PR 唯一删除行**）。
- 5 个原本浅克隆的 job 补 `checkout fetch-depth: 0`（docsonly 需 `origin/main...HEAD` 三点差）。
- 9 个新增 detect 的判定式**与既有 quality 版同条件**（同 regex / 同三点差），另加 1 条 **fail-safe**：`origin/main` 不可解析 ⇒ 按**非** docs-only ⇒ 全量跑（**绝不误跳**）。
- ⒞ 不做 workflow 级 `paths:`（顶层 keys 仍为 `name/on/concurrency/jobs` ✅）。

---

## 2. 真 run 证据

### 2.1 ★ 首轮 run 暴露一个真实故障：Windows 上 detect 步全红（已修）
```console
run 36215467000（首轮）：
  Test-Kit Architecture Tests (windows-latest)   completed failure   detect=failure
  Control Tower Gate Tests (windows-latest)      completed failure   detect=failure   ← 必需检查
```
**根因**：我新增的 detect 步未声明 `shell:` ⇒ **Windows runner 默认用 pwsh** 执行 bash 语法 ⇒ 语法失败。
**修复**（`fix(D1022): detect 步补 shell: bash`）：10 个 detect 步显式 `shell: bash`
（含 quality 既有那 1 个——ubuntu 上默认即 bash、行为不变，仅为统一与防御；**如视为越界可回退该 1 行**）。

**修复后 `run 36215533855` 实测**：
```console
  所有 10 job 的 Detect docs-only 步 = success（含 Test-Kit(windows) 与 Control Tower(windows)）
```

### 2.2 判据 ⒞：非 doc PR ⇒ 行为不变（**真 run，run 36215533855**）
```console
job: TypeScript + Lint + Iron Laws | in_progress
   Run actions/checkout@v4                              completed  success
   Detect docs-only change (D515)                       completed  success
   Run actions/setup-node@v4                            completed  success
   Run npm ci                                           in_progress
   Replay changed dev-doc evidence commands (D703)      pending
   TypeScript check                                     pending
   Iron laws check                                      pending
   …
```
⇒ detect 通过后**重步照常执行**（未跳）⇒ 非 doc PR 行为与基线一致 ✅（该 run 的 10 个 job 均 detect=success，随后进入全量执行）。

### 2.3 判据 ⒜/⒝/⒟：⚠️ **合前不可产出（已实证；叠加探针亦不可能 —— 见 §2.4）**
**原因（平台语义）**：`pull_request` run 使用的是 **base+head 合并提交**里的 workflow 文件。只要 T3 尚未进入 `main`，任何 doc-only PR 跑的仍是 **main 的旧 ci.yml**（只有 quality 有 docsonly）。

**实证（真 run，探针 PR #847 / run 36215620092，base=main，diff 仅 1 个 .md + hook 的 bypass.log）**：
```console
  TypeScript + Lint + Iron Laws        completed   detect=success  已跳步=7     ← 唯一有 docsonly 的 job
  Architecture Check                   in_progress detect=(无该步) 在跑步=1      ← main 旧 ci.yml
  Integration Contract Check           in_progress detect=(无该步) 在跑步=1
  Control Tower Gate Tests (ubuntu)    in_progress detect=(无该步) 在跑步=1
  Control Tower Gate Tests (windows)   in_progress detect=(无该步) 在跑步=1      ← 35min 级
  Checker Review / Vitest(1/2) / Vitest(2/2) …         detect=(无该步) 在跑步=1
  Test-Kit(ubuntu/windows) / npm audit / Gate Integrity  completed（全量跑完）
```
⇒ **doc-only PR 在 T3 未入 main 时仍会全量跑 9 个 job**（探针 run 已按省载目的**取消**，证据已留档）。

**解除阻塞后的执行步骤（合入 main 后我立即执行，逐条给原始输出）**：
```bash
# ① doc-only 证据分支（仅 1 个 .md）→ 期望：12 个必需检查全 success 且总耗时 < 1min
git checkout -b chore/D1022-docsonly-evidence origin/main   # 注意：合后 main 已含 T3
#   加 1 个 .md（+ 可选 .claude/ 免测）
# ② 混合反例分支（同 1 个 .md + 1 个 .ts 改动）→ 期望 docs_only=false ⇒ 全量照跑
# ③ 取证据：GET /actions/runs?branch=… → /actions/runs/<id>/jobs → 逐必需检查结论 + run 起止时间
#    必需检查名单（12，来自 branches/main/protection）：
#    Architecture Check / Checker Review (maker/checker) / Control Tower Gate Tests (ubuntu-latest) /
#    Control Tower Gate Tests (windows-latest) / Golden Case F1 Gate / Integration Contract Check /
#    Test-Kit Architecture Tests (ubuntu-latest) / Test-Kit Architecture Tests (windows-latest) /
#    TypeScript + Lint + Iron Laws / Vitest (1/2) / Vitest (2/2) / npm audit
```


### 2.4 ★ CTO 批「叠加探针」实测结论：**该路线不能产出 ⒜/⒝（已给 run 证据）**

**做法（照 CTO 指示执行）**：`chore/D1022-docsonly-probe` → `git merge chore/D1022-ci-timeouts-docsonly`
（merge commit `10ff0c78…`）→ 改/加 1 个 `.md` → 推 → PR #847（base=main）。
**核验 merge commit 树里 T3 的 ci.yml**：`git show HEAD:.github/workflows/ci.yml | grep -c 'id: docsonly'` = **10**（>8 ✅）。

**实测 run `36216761774`（head=`8903c4592917`）**：
```console
job 数: 10 ｜含 Detect 步的 job 数: 10        ← ① T3 的 ci.yml **确实生效**（不是"仍在用 main 的 workflow"）
  TypeScript + Lint + Iron Laws        in_progress detect=success 在跑=1 跳步=0
  Architecture Check                   in_progress detect=success 在跑=1 跳步=0
  Control Tower Gate Tests (windows)   in_progress detect=success 在跑=1 跳步=0
  …（10/10 job 均 detect=success 且 跳步=0）
```
⇒ ② **`docs_only=false`**（全 10 job 跳步数 = 0、重步都在跑）⇒ **早退根本没被触发** ⇒ ⒜/⒝ 无法由叠加探针产出。
（该 run 已按省载**取消**；上表为取消前快照。注：`created→updated` 的 13s 是状态刷新间隔，**不是** ⒝ 的耗时读数，不能当 ⒝ 证据。）

**根因（与 CTO 预设的失败条件不同，须据实修正）**：不是"仍在用 main 的 workflow"，而是 ——
`docs_only` 由 `git diff --name-only origin/main...HEAD` 判定，而叠加分支**相对 main 带着 `ci.yml` 的改动** ⇒ 该文件不匹配 `\.(md|json)$|task-state/|\.claude/` ⇒ `docs_only=false`。

**不可能性证明（一句话）**：`docs_only=true` 要求 head 相对 main **只含 doc 面文件**（即不含 `ci.yml`）；
而 run 使用 T3 的 workflow 要求 T3 的 `ci.yml` 出现在 `base(main) + head` 的合并结果里。
两者同时成立 ⟺ **T3 的 `ci.yml` 已在 main 里**。
⇒ 叠加探针在数学上不可能产出 ⒜/⒝；这是**平台语义**，不是实现问题。

**已按 CTO 备选（先合后证）预先备好的取证包（合入后 30 分钟内执行；任一不过 ⇒ 立即 revert + 上报）**：
```bash
# ① doc-only 取证分支（从**新 main**起；不要叠加）
git fetch origin main && git checkout -q -b chore/D1022-docsonly-verify origin/main
printf 'verify %s\n' "$(date -Iseconds)" > docs/synova/coordination/D1022-verify-docsonly.md
git add docs && git commit -m "docs(D1022): post-merge doc-only 取证（临时·不合并）" && git push origin HEAD
#   开 PR（base=main）→ 取 run_id
# ⒜ ：GET /repos/tangbaobao520/SynovaAgent/commits/<head_sha>/check-runs → 12 个必需检查逐项 conclusion=success
# ⒝ ：run.created_at → updated_at（或 run_started_at）；要求 < 60s
# ② 混合反例分支（同样从新 main）：+1 个 .md 且 +1 个 .ts ⇒ 期望 detect 报 docs_only=false、重步照跑（⒟）
# ③ 若任一不过 ⇒ 立即 `git revert -m 1 <#846 合并 sha>`（或 revert ci.yml 提交）+ 上报（不自行决定保留）
```

---

## 3. 红线段

- **新增"超时"红路径**（CTO 已批）：任一 job 超 `timeout-minutes` ⇒ 该 job 红 —— 这正是止血目的。
- `docsonly` 是既有机制（D515）的推广：doc-only 时重步跳过 ⇒ 各 job **仍报 success**（**不是 skip**），必需检查不消失。
- main push（diff 为空）同样判 doc-only ⇒ 与既有 quality 行为一致（D515 注释已述）。
- **首次上线已出现一次真实回归**（§2.1 Windows detect 红）——已修并复验；这说明"上超时/上门控"本身有风险，故保留 §2.2/§2.3 的真 run 复核。

---

## 4. 未清项

1. **判据 ⒜/⒝/⒟ 阻塞于 T3 入 main**（§2.3 已实证原因）；解除后我按 §2.3 步骤执行并回填本文件 + PR 评论。
2. **ubuntu step 13min 取值待 CTO 确认**（原文只点名 windows 60；若要 ubuntu 也 60，需同时把其 job 级调 >60，偏离既定分布表）。
3. **探针 PR #847 与分支 `chore/D1022-docsonly-probe` 需处置**（关闭/删除由队长或 CTO 决定；我不删分支/PR）。
4. **取号第 3 次挂**（D1019）：建议把 `alloc-task-id.sh:152-159` 无超时兜底作为 P0 处置。
5. **CI 侧 `Gate Integrity` 不在 12 个必需检查内**（实测 `branches/main/protection`：12 项不含它）；本 PR 未改变其行为。

---

## 附：关键证据摘要（供 K3 匿名核）

- **实现**：`+201/−1`（唯一删除 = `always()` 合并）｜10 个 job `timeout-minutes`｜1 个 step 级 timeout｜9 个新 detect｜27 条 `if` 门控｜10 条 `shell: bash`｜5 条 `fetch-depth: 0`
- **CTO 硬要求注释**：`n=12` / `中位数 35.2min` / `P95 38.5min` / `只取 success` / `as_of=2026-09-26T04:47:47+08:00` / `公式=max(P95×1.5, 中位数×3)` 全部在文件内（grep 计数 10/1/1/10/10/10）
- **真 run**：首轮 `36215467000`（Windows detect=failure ×2）→ 修复后 `36215533855`（**10/10 detect=success**）；⒞ 由该 run 的步级状态证（detect 通过后重步照跑）
- **⒜/⒝/⒟**：⚠️ 阻塞（需 T3 入 main）；实证 run `36215620092`（探针 PR #847）：只有 quality 有 docsonly（跳 7 步），其余 job **无该步且全量在跑**
- **12 个必需检查名**：取自 `GET /repos/tangbaobao520/SynovaAgent/branches/main/protection`（原文见 §2.3）
