# 改动清单 · D850 / D852 小队（2026-09-20）— 三件交付之一

> 口径：**自验**（不是审计）｜ 队长：并行 CTO session ｜ 编制：编码 A（D850）＋ 编码 B（D852）＋ 独立自验员
> 复核命令：`git -C .synova-wt-d850 diff --stat origin/main...HEAD`（D850）｜ D852 已合 main，见 squash `25082884`

---

## 一、D852（§6-5 四项产品决定落地）— **已合 main**

- 载体：PR [#681](https://github.com/tangbaobao520/SynovaAgent/pull/681) → squash **`25082884`**
- 分支 / HEAD：`team/d852-product-decisions` / `d3a4def1`（merge-base `21b7e4af`）
- 规模：**9 文件 / +609 −1**（唯一删除行 = `preconditions.yaml` 的 `updated_at` 元数据）

| 文件 | 改了什么 | 为什么 |
|---|---|---|
| `docs/synova/product-lines/product-lines.yaml` | **+30 / −0**：在 4 条既有线的 `acceptance_points` 末尾续号插入 **6 个新验收点**（3-9 / 4-9 / 8-7 / 8-8 / 25-8 / 25-9），各 5 行 = `id`/`desc`（判定式）/`evidence`（verify＋证据类型）/`status`/`note`（fail_when ＋ 依赖 ＋ M1 口径） | 创始人 §6-5 四项决定的**唯一落点**；**只加验收点、不新增线**（`lines` 仍 28） |
| `docs/synova/product-lines/preconditions.yaml` | **+51 / −1**：新增 `not_applicable: NA-D852`（四项逐条「不涉及」＋按总纲 §2.2 两条判据给理由）＋ `cross_line_contracts: CLC-1/CLC-2`（统一时间窗口径 / spill 阈值单一来源——**是跨线口径不是前提**，登记防 M7 漂移） | 派单必做项「禁沉默跳过」；跨线口径写死单源防两处实现漂移 |
| `docs/synova/coordination/D852-V1-轻量变更单-20260920.md` | 新增：逐条来源 ＋ 分母 128 → 134 算术 ＋ M1–M5 自查 ＋ 5 项待创始人裁定 | 冻结件 §六「增补路径」要求**轻量变更单**；**禁改冻结件** |
| `docs/synova/coordination/D852-live-restart-清单-20260920.md` | 新增：四类归属 ＋ 判定边界（判据 = 值是否参与已建立驻留的连接/进程内状态）＋ 7 类逐条可复现核验 | 派单要求「live/restart 清单成文」 |
| `docs/synova/coordination/D852-派单回执-20260920.md` | 新增：§〇-c 写前核实 6 项 ＋ 写后自检 8 项 ＋ **仓库外 9 项撤回清单逐条对照** ＋ §D DSH 版本锚点表 | 派单纪律「权威件回执 ＋ 过一遍 9 项撤回清单」 |
| `memory/notes/proposed/2026-09-20-D852-product-decisions-6-points.md` | 新增决策 Note（铁律 49） | 非平凡变更必须沉淀 |
| `task-state/D852.json`、`.claude/task-briefs/D852.md` | 登记 ＋ brief | 流程 |

---

## 二、D850（度量口径改造）— **已合 main `cb1bbcaf`**（两轮退回后闭环）

- 载体：PR [#683](https://github.com/tangbaobao520/SynovaAgent/pull/683)（队长代开：编码环境无 `gh`/token）→ squash **`cb1bbcaf`**
- 分支 / HEAD：`team/d850-discrete-health` / `3b6e4e59`（merge-base 原 `7e4e169d`，已并入 main `25082884`）
- 规模：**12 文件 / +6077 −249**（PR 预算 12/12，正好卡上限；**两轮修复未新增文件**）
- 提交链：`2cda6eb5` 主体 → `b913cf90` schema 对齐 →〔第一轮退回 V-1/V-2〕→ `3540c808` →〔第二轮退回 ⓐ 语义〕→ `c2b0b4ff` → 合并 `3170eaa1` → hook 登记

| 文件 | 改了什么 | 为什么 |
|---|---|---|
| `scripts/project/gen-project-board.py` | `totals` 去两个顶层**百分比**字段；新增 `totals.buckets`（`healthy`/`written_not_wired`/`missing` ＋ 显式其它态 `wired_broken`/`live_unverified`/`state_unknown` ＋ `identity`）；**每档带 evidence_cmd**（源侧复算）；stdout 摘要行零百分比 | 创始人签字「取消总完成度百分比 → 离散三档，每档带可复现命令」 |
| `scripts/product-lines/calc-progress.py` | `total_lines` 计入 `v2_lines` → **29**（附 `lines_v1=28`/`lines_v2`）；顶层百分比改三档 `buckets`；线级 `progress_pct` 保留并标 `deprecated_fields`（兼容 win 域消费方）＋ 加线级 `buckets`；读 V1 表 `pending_wiring` 撤回标记 → 「写了没接」接到面板；汇总日志去百分比 | 面板 total_lines **26 → 29 追平**；五态折叠成三档报告口径 |
| `scripts/product-lines/gen-progress-page.py` | 面板**零百分比**：头部「29 条产品线」＋三档＋每档可复现命令；线级卡片改三档 chips；「无法判定」显示原因（禁显示 0）；页脚文案改离散口径 | 面板是创始人可见面，口径必须一致（防 M7 文档-实现漂移） |
| `tests/project/gen-project-board.test.sh` | 新增组⑩（恒等式闭合 / 不可判定档 null＋reason / 六档 evidence_cmd 逐条实跑对账 / 零百分比字段 / stdout 零百分比）；**修 CT-67**（组⑥ 三处 live 硬编码 36/22/lines=26 → 只断言不变量与跨源自洽）；修组⑤ 反条件断言 | CT-67 台账登记项；且**全绿的边界 = 测试覆盖的边界** |
| `tests/project/calc-progress-panel.test.sh` | **新增（V-2 闭环）**：203 条 evidence_cmd 逐条 rc=0 ＋ 值对账 / 源侧判据两类语义分开验 / null＋reason / 顶层与逐线恒等式 / **反漂移**（已提交派生件 == 现场重跑）/ 零渲染型百分比 —— `PASS=12 FAIL=0`（10 s，可进 CI） | 自验退回 V-2：面板新契约原本**无测试覆盖**，117/0 全绿仍漏 V-1 |
| `docs/synova/project/ledger.json` | 脚本重生成（禁手改）：`totals.buckets` 三档；`backlog_points` **46 → 52**（并入 D852 的 yaml `points_total 174→180`） | 派生件必须与源头一致；**该件无任何 CI 通道会重生成**（实测 `.github/workflows/` 只有 founder-dashboard/founder-console 有 bot 生成） |
| `docs/synova/product-lines/product-progress.json` | 脚本重生成：`total_lines=29`、顶层 `buckets`、`deprecated_fields` | 同上 |
| `docs/synova/product-lines/product-progress.html` | 脚本重生成（与现场重跑**逐字节相同**）：29 条产品线 ＋ 三档 ＋ 每档命令 | 同上 |
| `memory/notes/implemented/2026-09-20-D850-discrete-health-buckets.md` | 决策 Note（铁律 49）＋「被删除字段全名」唯一留档处 | 追溯信息移位不丢失 |
| `task-state/D850.json`、`.claude/task-briefs/2026-09-20-D850-discrete-health.md` | 登记 ＋ brief | 流程 |

### 三档口径（互斥且完备；分母 = V1 断言表 128）

| 档 | 五态对应 | 判定源 | ledger 实测 | 面板（yaml 验收点域，分母 180） |
|---|---|---|---|---|
| `healthy` | 健康（live && testedThroughEntry）的**可判定代理** | 断言已点亮 ＋ 另有 `record_type=k3` 独立 PASS | **16** | 0 |
| `written_not_wired` | 写了没接（implemented=true, wired=false） | V1 表「证据」列 = `pending_wiring`（D809 撤回标记，**显式声明非 grep**） | **3** | **3** |
| `missing` | 缺（implemented=false） | 判定手段 = 代码检索 → **本口径禁用** → 显式 `null` ＋ 原因 | **null** | null |
| `wired_broken` | 接了跑不通 | 判定手段 = 冒烟启动 → 无机器可读源 → `null` ＋ 原因 | null | 4 |
| `live_unverified` | 能跑未验证（**单独成态，绝不并进健康**） | 点亮但无 k3 独立 PASS | 11 | 32 |
| `state_unknown` | 显式残余（五态归属不可判定） | 剩余项 | 98 | 105 |

恒等式：`16＋3＋11＋98 = 128`（面板：`0＋3＋4＋32＋36＋105 = 180`）。**`identity.holds=true` 只保证完备性（丢点会显形为 `state_unknown`），不保证归类正确——已写入 note，不再当正确性证据。**

---

## 三、跨切片耦合（队长显式排序解掉）

| 事实 | 处置 |
|---|---|
| A 的 `ledger.json.backlog_points = yaml points_total − v1_total`，而 `points_total` 由 **B 的 PR #681** 从 174 改到 180 | **先合 D852，再让 A 在新 main 上重生成派生件**：`backlog_points 46 → 52`（两次原始输出已留档） |
| 两分支写集**两两不重叠**（A：`scripts/project/**`＋`scripts/product-lines/**`＋`tests/project/**`＋派生件；B：`docs/synova/product-lines/*.yaml`＋`docs/synova/coordination/**`） | 合并 main 零冲突（实测） |
| `ledger.json` **无 CI 重生成通道** | 必须由本 PR 带上（禁止指望 CI 兜底） |
