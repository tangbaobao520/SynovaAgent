# 自验记录 · D850 / D852 小队（2026-09-20）

> 队内**自验**（**不是审计**）。编制：编码 A（D850）自验 ｜ 编码 B（D852）自验 ｜ **独立自验员 `verifier`（不得由编码兼任）** ｜ **K3 队外**
> 口径：`docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md` §十一（三态：通过 / 退回 / 关闭）——本记录只允许 `可提请独立审计` / `退回（附理由）` 两种措辞
> 独立自验员全文（含完整原始输出）：落档时由 `/tmp/verify-report-D850-D852-20260920.md` 摘录入本文件；工作树终态两棵 `git status --short` 均为空

---

## 0. 锚点与范围

| 项 | 实测 |
|---|---|
| D850 分支 / HEAD | `team/d850-discrete-health` / `588fbff2`（PR [#683](https://github.com/tangbaobao520/SynovaAgent/pull/683)） |
| D852 分支 / HEAD | `team/d852-product-decisions` / `d3a4def1`（PR [#681](https://github.com/tangbaobao520/SynovaAgent/pull/681) → squash `25082884`，**已合 main**） |
| 自验起点基线 | D850 merge-base `7e4e169d`（＝当时 origin/main）｜D852 merge-base `21b7e4af` |
| 写操作 | 独立自验员**只读 + `/tmp`**；唯一副作用（跑控制塔测试生成的 `docs/synova/product-lines/evidence/test-2026-09-20.json`）已 `rm`，终态恢复为空 |

---

## 1. 独立自验员十条（task-3）

### ① 三档 + 逐档 evidence_cmd 实跑 → **ledger 通过 / 面板不通过**

**ledger（`gen-project-board.py`）6/6 跑通**：
```
healthy=16  written_not_wired=3  live_unverified=11  state_unknown=98
missing → implemented=SOURCE_ABSENT   wired_broken → wired=SOURCE_ABSENT
```
**面板（`calc-progress.py` / `product-progress.json`）4/7 rc=1，线级 140/196 rc=1 → 合计 144/203 条不可跑**：
```
$ bash -c "<other_states.live_unverified evidence_cmd>"   → rc=1  KeyError: 'other_states.live_unverified'
$ bash -c "<lines[0].buckets.healthy evidence_cmd>"       → rc=1  KeyError: 'lines[].buckets.healthy'
```
根因（file:line）：`scripts/product-lines/calc-progress.py:136-146 rerun_evidence_cmd()` 把**点分路径当单键**（`d['buckets']['<path>']['count']`），叠加 `:215 pre = "lines[].buckets."`。

> **队长独立复现**（非转述）：取 `product-progress.json → buckets.other_states.live_unverified.evidence_cmd` 原文执行 → `rc=1`，`KeyError: 'other_states.live_unverified'`。**确认成立** → 列为 **V-1（阻塞）**。

### ② 三档恒等式独立复算 → 通过（但恒等式本身是同义反复）
自验员**自写重算器**（不复用被验脚本任何函数；自解析 V1 表 kind ＋ 自扫两处证据目录）：
```
V1 表内断言=128；kind 分布={'scenario':72,'founder-demo':5,'k3':31,'test':17,'pending_wiring':3}
自算: healthy=16 written_not_wired=3 live_unverified=11 state_unknown=98
互斥完备: 三对交集全 0，并集=128
与 ledger 断言字段: passed=27 / pending_k3=3 / pending=98 —— 全部一致
```
**对抗性结论（B-1）**：`identity.holds=true` 是**同义反复**——`state_unknown` 由定义式相减得出（`gen-project-board.py:646-648` / `calc-progress.py:208`），`holds = known_sum == v1_total`。构造反例：重复计 1 点、真丢 1 点时 `holds` 仍为 `True`（残余吸收）；唯一能抓到的是「残余没跟着重算」。→ **`holds=true` 不含「三档归类正确」的信息**；它真正保证的是**完备性由构造**（丢点会显形为 `state_unknown` 而非消失）。**禁止当正确性证据使用**。

### ③ 反向验证（破坏 → 红 → 还原 → 绿）→ 通过
在 `/tmp/rev`（archive 拷贝，**未动工作树**）把 `evidence/task-D517.json` 的 k3 裁决 `pass→fail`：
```
BEFORE  healthy=16 wnw=3 live_unverified=11 state_unknown=98
BROKEN  healthy=15 wnw=3 live_unverified=12 state_unknown=98   ← 红
RESTORE healthy=16 wnw=3 live_unverified=11 state_unknown=98   ← 绿
```

### ④ 面板 total_lines=29 + HTML 无百分比渲染 → 通过
```
calc-progress 重跑：total_lines=29（lines 28 + v2_lines 1，独立核对 yaml 解析器 len(lines)=28）
HTML 渲染型百分比：grep -o ">[0-9]\+%<\|width: [0-9]\+%" | grep -v "width: 100%" → 0 命中
派生件可复现：gen-progress-page 重生成 HTML 与 committed **逐字节相同**；json/ledger 仅差 generated_at/git_head
```
残留 `%` 全部为 CSS `width:100%`、内嵌命令的 `%s/%d`、产品正文字样（「<30% 数据仍诊断」「100% 绑定 Goal」）→ **无进度百分比**。

### ⑤ 禁百分比 grep（6 文件 × 5 模式）→ 通过
```
$ grep -rn "product_progress_pct\|delivery_pct\|verify_pct\|产品总进度\|总完成度" \
    scripts/project/gen-project-board.py scripts/product-lines/calc-progress.py scripts/product-lines/gen-progress-page.py \
    docs/synova/project/ledger.json docs/synova/product-lines/product-progress.json docs/synova/product-lines/product-progress.html
命中行数 = 0
```
（**队长独立复跑同一命令**：命中 0，exit=1 ✓）

### ⑥ CT-67 组⑥ 不变量化 + 先红后绿 → 通过
```
bash tests/project/gen-project-board.test.sh   → PASS=117 FAIL=0   （队长与自验员各自独立跑过）
红①（新测试打旧脚本，/tmp/redgreen）: PASS=91 FAIL=26，含 "❌ ⑩ 派生器 stdout 仍含百分比: … delivery=50.0% …"
红②（基线 dae40d96 原形态）:            PASS=93 FAIL=2 → ❌ backlog_points 应 36 实 46 / ❌ v1_passed 应 22 实 27
```
组⑥ 现只断言：冻结分母 128 ＋ 不变量（`0≤passed≤total`、`verified≤passed`、`backlog≥0`、`lines≥1`、无百分比字段）＋ 跨源自洽。**旧三处 live 硬编码（36 / 22 / lines=26）全部移除**（含自验员额外点名的 `test.sh:351-352`）。

### ⑦ D852 yaml 独立解析 → 通过
```
git diff --numstat 21b7e4af..HEAD -- product-lines.yaml → 30  0     （30 增 / 0 删）
git diff … | grep -c "^-[^-]" → 0
lines 条数=28 ｜ 顶层 total_lines=28 ｜ v2_lines=['federated-evolution'] ｜ 验收点总数=180
```
6 个新点（3-9 / 4-9 / 8-7 / 8-8 / 25-8 / 25-9）逐条三件齐全（判定式 desc ＋ evidence 类型 ＋ note 含 fail_when ＋ 依赖），既有点 id/条文/status 未被改。

### ⑧ 四项决定逐字对照 §6-5 → 通过
| 决定 | 落点 | 逐字核对 |
|---|---|---|
| ① 线 8 出站为主 ＋ 入站仅扩展点（外部触发不进核心） | 8-7（出站投递）/ 8-8（入站边界，核心零依赖） | ✓ |
| ② 线 4 内存 **8,000** / 落盘 **32,000** 字符、**可配置** | 4-9（含配置读取位置与越界行为） | ✓ 数字逐字命中 |
| ③ 线 3 日粒度为主 ＋ 周/月聚合视图 | 3-9（note 明写 **D828 依赖**） | ✓ |
| ④ 线 25 展示·阈值·文案=live；连接·插件·数据源·鉴权=restart | 25-8（live）/ 25-9（restart） | ✓ 四类词全命中 |
DSH 借鉴锚点**现查现验**（本地 checkout 0.1.6-alpha.2）：`dsh-settings lib/types/index.d.ts:21`、`lib/index.js:288`、`dsh-time-context:161-162`、`dsh-webhook:245/:90`、`dsh-spill-local:556/:564`、`dsh-spill-policy:102-103 + types:53` —— 全部逐字命中（唯 4-9 两条区间行号不精确，见遗留 16）。

### ⑨ live/restart 清单可证伪性 ＋ 分母算术 → 通过
判据「值是否参与已建立驻留的连接/进程内状态」＋ 分类三问 ＋ 7 类各带不重启/重启两步核验 ＋ 否定面（「改完恰好要重启才生效」不算 live）→ **可证伪**。分母独立复算：`174 → 180`、`v1_total` 仍 128、`backlog 46 → 52`，与 `D852-V1-轻量变更单-20260920.md` §三「中间态漂移」叙述自洽；其引用的派生器行号 `:105 / :166+:500 / :504-506 / :645` 逐条命中。

### ⑩ preconditions.yaml → 通过
唯一删除行 = 与本决定无关的 `updated_at` 元数据（09-19 → 09-20）；新增 `not_applicable: NA-D852`（四项逐条「不涉及」＋理由，**禁沉默跳过已满足**）＋ `cross_line_contracts CLC-1/CLC-2`；P-1~P-3 未动。

---

## 2. 对抗性验证（队长点名 6 条）

| # | 题 | 结论 |
|---|---|---|
| 1 | `identity.holds` 是真约束还是同义反复？ | **同义反复**（见 ② 的构造反例）。保留可用，**不得当正确性证据** |
| 2 | `missing.count=null` 是否等于「三档离散计数」达标？ | **不达标（K 缺未交付）**。依据 总纲 §1.2:59「N 健康 / M 写了没接 / K 个缺 —— 离散计数」＋ 事实驱动 §四/§六 ＋ 撤回清单 #3；A 的「无机器可读源」经独立核实为**真**（yaml 字段无 `implemented/wired` 载体）→ **归属 = 派单口径 ＋ 度量基建缺源，不是编码 A**（卡明文授权 null＋reason）。**给创始人的诚实读数：16 健康 / 3 写了没接 / 0 可判定为缺 / 98 五态归属未知 —— 最大一档是「未知」** |
| 3 | 面板线级三档是否永远为 0（M3）？ | **不是 M3**：注入一条新鲜 k3 PASS 探针（只写 `/tmp`）后线级 `healthy` 立刻由 0 → 1。今天全 0 是**数据面**（34 个有 k3 PASS 的 yaml 点全落 stale23/pending_k3 9/uncommitted3）。但**线级读数缺可用命令**＝ V-1 真缺陷 |
| 4 | 可见面是否真的再无整体百分比？ | 6 文件零命中 ✓。**写集外事实登记（未改）**：`scripts/control-tower/views/pm_dashboard.py:109-110` 动态渲染 `{overall:.1f}%`「总完成度」；`app/founder-dashboard.html:126-127` 静态 `0.0%`「总完成度」→ 创始人首页仍有「总完成度」 |
| 5 | `PASS=117 FAIL=0` 可复现？组⑥ 是否真的不变量化？ | **可复现**（自验员独立跑）＋ 三处硬编码全清（见 ⑥） |
| 6 | 分母算术自洽？ | 自洽（见 ⑨） |

---

## 3. 自验结论

| 件 | 结论 | 依据 |
|---|---|---|
| **D852** | **可提请独立审计**（队长判定：**通过**，已合 main `25082884`） | 十条全过；四项决定逐字对得上；28 线零删除；分母算术自洽 |
| **D850** | **两轮退回 → 条件全部闭环 → 可提请独立审计**（队长判定：**通过**，已合 main `cb1bbcaf`） | 见 §4 第二轮/第三轮；三轮退回项各有独立原始输出 |
| **「缺」档** | **未闭环 → 呈创始人裁定** | 见对抗性 2；不属编码责任 |

**退回后的闭环路径（已全部走完）**：编码 A 在原分支 `team/d850-discrete-health` 追加两轮修复 → 独立自验员做**两轮限定范围复验**（每轮只验退回项 ＋ 回归）→ 三轮全过后由队长合并（PR #683 → `cb1bbcaf`）。

---

## 4. 第二轮（ⓐ 语义）与第三轮（限定复验）

### 第二轮退回：`evidence_cmd` 从「源侧重算」被降级为「读回派生物」

**独立自验员判定（队长采纳）**：**这是自证，不是复现** —— 依据 `方案-项目度量-从声明驱动到事实驱动.md` §四「每个数字必须带一条可复现的命令。不能复现的数字，不上看板」＋该节结句「**你能自己跑一遍验证——这就是可证伪，也就是信任的来源**」；读回派生物时源侧错它也错，第三方跑**不可能得出与看板不同的结论**。且作者上一版 `@contract` 自己写过「重跑即从权威输入重新派生，**不读回派生物**（读回自己的输出不构成证据）」→ **契约反转**（定性为需队长裁量的设计变更，非违约）。
**队长不让步的理由**：面板上挂 203 条不可证伪的命令，比没有命令更坏（假证据）。

### 第三轮限定复验（`3b6e4e59`）→ 全部通过

| 项 | 独立复验原始结果 |
|---|---|
| **ⓓ-1 词义分离** | 两件 `buckets` 顶层均有 `cmd_semantics`；面板 203 档 `evidence_cmd`（源侧重算）＋ 203 档 `artifact_selfcheck_cmd`（明文「不承担可证伪职责」）；ledger 6 档只有 `evidence_cmd` 且件内明写「本件不提供自查形态（宁缺勿造）」；`reproducibility_strength` 逐件写明差异 |
| **ⓓ-2 fail-closed（从零实测）** | 删 `/tmp/ro-pp.json` → **203/203 rc=1**，逐条 `FAIL-CLOSED: 缺 /tmp/ro-pp.json —— 先跑 regenerate_cmd…不得读回提交件`；跑 `regenerate_cmd`（real 0.87s）→ **203/203 rc=0**；不是靠残留文件命中 |
| **ⓓ-3 线级独立判据（有牙齿）** | 58 条全 rc=0，`kind={equality:1, bound:28, reconciliation:28}`；**56 条不读 `/tmp/ro-pp.json`**、不调管线 → 独立性成立。负例：篡改 `line1.healthy=99` → `BOUND VIOLATION 线 1: 99 > 证据面 k3 PASS 覆盖点=8`；给 V1 表线1 造 `pending_wiring` 行 → `两源不一致(线 1): V1断言表撤回行=1 vs 提交件=0 —— 需人工裁决` |
| **ⓓ-5 可移植性** | 无 `.git` 与**真 `--depth 1` shallow clone** 两条路径均 `⏭ SKIP + 原因 + PASS=0 FAIL=0 (SKIPPED)` + `exit 0`，不再报「派生件陈旧」 |
| **ⓐ 可证伪性已恢复** | 自验员在克隆里改**源**（k3 点 2-1 `pass→fail`）→ 提交件命令报 `MISMATCH …wired_broken.count: fresh=5 committed=4`（顶层）与 `lines[id=2]…fresh=1 committed=0`（线级），未受影响的档仍 rc=0 → **第三方跑它确实能得出与看板不同的结论** |
| **回归** | `calc-progress-panel.test.sh` **20/0**；`gen-project-board.test.sh` **117/0**；freshness **11/0**；禁百分比 grep 零命中；`pre-commit-check.sh` 13 组过＋无副作用；`check-pr-budget.sh` 12≤12 单域 mac；跨切片 `ledger 128/52`、面板 `29/180` |
| **误删恢复内容级核验** | `git diff origin/main -- docs/synova/product-lines/evidence/` = **0 行**；tracked **65 == HEAD 65 == origin/main 65** |

**第三轮新增（强度类，不阻塞，已立台账 CT-76/CT-77）**
- **V-16**：中间件「存在但陈旧」时该档 evidence_cmd **假绿**（实测三步：改源 → 不重生成 → `rc=0 wired_broken=4`）；即断言了「存在」没断言「新鲜」。文档两步走与 CI 路径都不触发，危险窗口 = 复用 `/tmp` 残留件。
- **V-17**：`bound` 是必要条件上界（healthy 全 0 时**恒真**，抓不到低估/漏点）；覆盖 = 线级 56/196；线级 `missing` 28 档无 probe、`other_states` 112 档无独立判据。
- **V-18（含队长转述更正）**：仓库记录 **5 次**读数（`12.68/7.29/1.68/2.09/1.47 s`）；队长曾转述「10 次/含 0.71」**不实**（自验员空载 10 次实测 `0.69–0.91 s`）。诚实区间 **0.7 s（空载）~12.7 s（重载）**，203 档 ≈ 2.3~43 分钟。
- **V-19**：`MISMATCH` 路径标签前缀重复 `buckets`（cosmetic）。

---

> 本记录为**自验**，不构成审计。独立审计结论**只认队外 K3** 出具。
