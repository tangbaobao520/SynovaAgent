# D852 派单回执 + 9 项撤回清单自检

> 任务: D852 ｜ 执行: D852 小队「编码 B」（`mac-dev-doc`）｜ 日期: 2026-09-20
> 工作树: `/Users/wane/SynovaAgent/.synova-wt-d852`（分支 `team/d852-product-decisions`，基线 `origin/main dae40d96`）
> 回执依据: `cto-handover` skill（`/Users/wane/SynovaAgent/.dsh/skills/cto-handover/SKILL.md`）**§〇c 派单质量 SOP** —— ① 写前核实（6 项）/ ③ 写后自检（8 项）
> 撤回清单原文（**仓库外**）: `/Users/wane/山河研究院/99-综合/交接/可复制-DSH对比与任务书-20260918.md` §二（9 条）
> 口径: 本回执为**队内自验**；结论标「可提请独立审计」，不构成审计意见（审计线独立）

---

## §A 写前核实回执（§〇c ① 必查 6 项）

### A-1 任务来源/依赖 — ✅ 已核
- 来源：创始人 §6-5 四项产品决定（任务卡 canonical 规格）。
- 上游状态：总纲 §6 项 5 把四项决定列为「需创始人裁定的事项（CTO 不扛）」；§6-1/6-2/6-3 已签并已登记（`product-lines.yaml` 28 线 = 26 原有 + v2 扩展 + 2 新增；`gap-line-mapping.yaml`；`preconditions.yaml`）。
- **⚠️ 发现一处不符（已点名）**：`docs/synova/coordination/创始人裁定表-§6五项-20260920.md:28` 标题为「**待签** §6-5｜四项卡在产品决定」，四项 ☐ **均未勾选**。任务卡称「已签」。四项决定的内容与表内「**建议**」选项**逐字一致**，故按任务卡落地；签署状态列为**遗留项升级创始人**（见 §C-9 与变更单 §七-3）。

### A-2 task-state 最新状态 — ✅ 已核
- `D852` 在 `origin/main` 无登记（本任务新取/队长已取号）；同批 `D850` 状态由队长掌管（`task-state/D850.json` 属 D850 小队，不在本写集）。
- 本回执对应写集见 `task-state/D852.json`。

### A-3 基线资产实际存在（物理确认，非凭记忆）— ✅ 已核（**行号逐条复核，全部命中**）

| 任务卡基线声称 | 实测 | 判定 |
|---|---|---|
| `product-lines.yaml` `lines:` :57 | :57 | ✅ 命中 |
| 线 3 :158 | :158 | ✅ |
| 线 4 :210 | :210 | ✅ |
| 线 8 :403 | :403 | ✅ |
| 线 25 :1101 | :1101 | ✅ |
| 附录 A 线 3 表 :169 | :169（表头 `\| ID \| 断言（判定式） \| verify \| 证据 \| fail_when \|`） | ✅ |
| 附录 A 线 4 表 :183 | :183 | ✅ |
| 附录 A 线 8 表 :241 | :241 | ✅ |
| 附录 A 线 25 表 :496 | :496 | ✅（变更单引用） |
| 冻结件 §五 签字 sha256 前 8 位 `c11841e6` | :86「文件 sha256 前 8 位（**不含本签字块**）: **c11841e6**」 | ✅（口径一致） |
| 分母 `FROZEN_V1_TOTAL=128` | `scripts/project/gen-project-board.py:105` = 128 | ✅ |
| `backlog_points` 现 46 | 实测解析 yaml `points_total` = **174**；174 − 128 = **46** | ✅ |

**基线零差错** —— 任务卡给出的 11 处行号/数值全部物理命中，无需点名修正。

### A-4 DSH 借鉴核查（§〇b 三步）— ✅ 已执行，见 §D（含版本锚点）
三步全做：① 施工图归属 → ② 借鉴边界（M1 逐条判定）→ ③ 源码 file:line（已给包 + 文件 + 行号）。**注意**：施工图 `DSH迁移施工图-20260820.md` 全文对 `webhook` **仅 1 处命中**（:258 客户配置包内的「飞书 webhook」，非本决定），对 `spill` / `settings` / `时间上下文` **零命中** —— 即**施工图四色清单未覆盖这四项**，其归线依据来自总纲 §2.1「11 项 DSH 缺口归线不新开」而非施工图。已在变更单 §二 引总纲原文，未虚构施工图依据。

### A-5 写集重叠检查 — ✅ 已核
- 写集 8 文件（见 `task-state/D852.json`），与**编码 A 写集**（`scripts/control-tower/**`、`scripts/project/**`、`scripts/product-lines/**`、`.github/workflows/ci.yml`）**零重叠**：A 改脚本/流水线，B 改产品线账目与协调文档。
- 与 D850（离散健康计数）零重叠：D850 动派生器/看板，B 不动 `scripts/**`。
- 唯一潜在交叠点：`docs/synova/product-lines/product-lines.yaml` 同时被 A（分母/线集登记）与 B（验收点）关注 —— **已用行级隔离**：B 只在 4 个既有线的 `acceptance_points` 末尾续号插入（+30 行 / −0 行），不动 A 关注的 yaml 头部/`total_lines`/线集。

### A-6 上一轮教训 — ✅ 已核并应用
| 教训 | 出处 | 本任务的应用 |
|---|---|---|
| **批十四「样板污染」**（空话式验收点） | 派单件 B-2 | 6 条断言全部写成**可证伪判定式** + verify/证据类型 + fail_when + 依赖；无一写成「支持 X」「实现 Y」 |
| **撤回清单 #3「机制判据是 grep」** | 仓库外 §二 | 8-8 note 显式写「判据是行为与不变量，**禁以 grep 命中作为通过依据**」；总纲 §5 同禁 |
| **撤回清单 #9「26 线不是完整完成度清单」** | 仓库外 §二 | 本任务**不声称**补完完成度；只落 4 项已裁决定，并在变更单 §七 明列 5 项待创始人裁定 |
| **M6 版本锚点断裂** | `AUDIT-FINDINGS-LEDGER.md` | 见 §D：DSH 存在**两个版本不同的安装**，已逐条锚定版本，防审计假红 |
| **M2 声称 vs 事实** | 同上 | 本回执所有「已核」均附物理输出/行号；未做的一律写「未做」 |

---

## §B 9 项撤回清单逐条对照（对照本任务声明）

原文全名与版本：`/Users/wane/山河研究院/99-综合/交接/可复制-DSH对比与任务书-20260918.md`（2026-09-18 版）§二「⚠️ 撤回记录（与结论同等醒目，务必先读）」。逐条比对**本任务（D852）的声明**是否沿用了已作废说法：

| # | 已作废的说法 | 本任务是否沿用？ | 对照证据 |
|---|---|---|---|
| **1** | 「engine-core 约 297 文件，还在桥接引用」 | **否** | 本任务**零代码改动**（不碰 `src/`、`packages/`），全文未提 engine-core 文件数。 |
| **2** | 「路径 A = 引擎重建在 DSH 上」 | **否** | 本任务只复用 DSH 的**机制语义**（spill/settings/time 的声明式配置），未提出任何「重建在 DSH 上」。变更单 §四 M3 行明写「禁 `import @deepseek-ai/*`、禁 re-export 壳」。 |
| **3** | 「**没有机制**能发现『写了没接上』的缺陷」→ 修正为「**有机制**（`pre-commit-check.sh` 组 4 接线完整性），**但判据是 grep，原理上抓不到『被引用但不生效』**」 | **否 —— 且主动应用了修正** | 本任务**未声称**「有 grep 就等于接线验证」。反面：8-8 的 note 显式写「**判据是行为与不变量，禁以 grep 命中作为通过依据**」；三份成文件（live/restart 清单 §三、变更单 §五）均以**行为观测**为通过判据，并给出「改完恰好重启了才生效不算 live 通过」「声明了分类不等于行为符合分类」两条防误判。**这正是 #3 修正的直接落地。** |
| **4** | 「26 线**没有参数线**」→ 修正为「覆盖了参数的配置，**缺的是标定与跨客户收敛**」 | **否** | 本任务引用线 8-1「阈值真实触发（读 manifest 配置，不硬编码）」时**未**说「没有参数线」；且本任务的 4-9 明确要求「两阈值**可配置**（非硬编码）」，与修正后口径一致（配置有，标定/收敛才缺）。 |
| **5** | 「`libreoffice-kit` 源码可得性有瑕疵、仓库 404」 | **不涉及** | 本任务未引用 `libreoffice-kit`（文档格式转换属线 4 的另一子项，非本决定范围）。 |
| **6** | 「DSH 的 storage 可参考其时序检索思路」 | **否** | 本任务**未**把 DSH storage 当时序能力参照。3-9 的时间窗口径依据是 `dsh-time-context`（**时点/时区语义**），**不是** storage 的检索（DSH 全家族无时序存储 → 时序表自建，与撤回 #6 一致）。 |
| **7** | 「DSH telemetry 是可观测性能力参照」 | **不涉及** | 本任务不涉遥测/可观测性（属线 22，非本决定）。 |
| **8** | 「可以复用 DSH 的前端交互/页面布局」→ 修正为「学它的**前端架构**，不是抄**界面**」 | **否 —— 且主动应用** | 本任务零 UI 断言。live/restart 清单 §四与变更单 §四 M5 行明写「只复用**机制语义**，**不抄 DSH 配置界面**（其界面露出工作区/文件树/终端 → 攻击定价）」。 |
| **9** | 「26 线**是完整的完成度清单**」→ 修正为「它是一份**不完整的**『单客户纯软件』产品定义：缺参数标定/多客户/**被验证**三处 + **咨询交付裂缝**」 | **否 —— 且主动应用** | 本任务：① **不新增线**（28 保持），不把 26/28 线当完整清单；② 在变更单 §七 显式列出**本任务解决不了**的 5 项待裁定（含 4-9 双阈值档位语义、3-9 与 D828 先后）；③ 明列 `backlog_points` 中间态漂移（46→52）作为**已知分类缺口**而非掩盖；④ 24-7 审计类点仍归 K3，本任务不自我点亮。 |

**自检结论**：9 条中 **7 条明确不沿用**，**2 条改为「不涉及」并写明理由**；其中 **#3 与 #9 是本次被点名重点，均做了主动应用（写进验收点的 fail_when/判据与变更单口径自查）**，非仅口头声明。另一条「本院自曝」（静态接线检测脚本 `metrics-facts.sh` L3 实测准确率 3/5=60%）同样应用于本任务：**不把静态检测当验收依据**。

---

## §C 写后自检（§〇c ③ 必查 8 项，按接收方口径改写）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | **验证点编号正确**（对照既有序列） | ✅ 续号无冲突：3-9 / 4-9 / 8-7 / 8-8 / 25-8 / 25-9，均在既有最大值（3-8 / 4-8 / 8-6 / 25-7）之后，**未复用/未改既有 id** |
| 2 | **D# 未占用** | ✅ D852 由队长分配；`task-state/D852.json` 本 PR 登记 |
| 3 | **依赖链正确** | ✅ 显式写出：3-9 ← **D828 趋势接线**（裁定表 §6-5 ③ 原文）；8-7 ← 8-3 去重键 + 线 4 凭据；25-9 ← 25-6 运行时插件化边界；8-7/8-8 ← 线 8 核心链路 |
| 4 | **DSH 借鉴核查三步完整** | ✅ §D 三步 + 版本锚点；8-7 明确「无 DSH 重叠能力，M1 不适用（自建）」并给原因（防执行方猜测） |
| 5 | **现状材料全部核实过**（非声称） | ✅ §A-3 表：11 处基线逐条物理命中；§D 所有 DSH 行号均为本次实测（含跨版本对照） |
| 6 | **验收物理可复现** | ✅ 6 条断言均含判定式 + verify/证据类型 + fail_when；live/restart 清单 §三 给出 7 行「可复现核验方式（命令级步骤 + 通过判据 + 失败信号）」 |
| 7 | **术语一致** | ✅ 统一使用「验收点 / 断言（判定式）/ 分母 / backlog_points / applies / live / restart」，与冻结件附录 A 五列、yaml 既有字段名一致；未引入新术语体系 |
| 8 | **无遗漏**（交付要求/审计口径） | ✅ 已含：live/restart 成文、变更单、preconditions 处置、本回执、memory note（铁律 49）、`task-state/D852.json`；结论标「可提请独立审计」 |

**另有一项主动登记**（§〇c ③ 第 8 项延伸）：本任务**不生成 M2 要求的「绑定证据文件」**（写集不含借鉴卡台账）——已在变更单 §四 M2 行显式声明「本变更单**不声称 M2 已满足**」，防 overclaim。

---

## §D DSH 版本锚点（**防审计假红，必读**）

本机存在**两个版本不同的 DSH 安装**，同名包的行号**不一致**，且其中一个**缺少** `dsh-webhook`：

| 安装 | 路径 | 版本 | `dsh-webhook` | `SettingsApplies` | `maxInlineBytes` 读取 | `config.timeZone` |
|---|---|---|---|---|---|---|
| **桌面端依赖（本次引用基准）** | `/Users/wane/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/dependencies/dsh/node_modules/@deepseek-ai/` | **0.1.6-alpha.2** | **有**（`dsh-webhook`） | `lib/types/index.d.ts:21` | `lib/index.js:102-103` | `lib/index.js:161-162` |
| **nvm 全局**（`cto-handover` skill §〇b 所载路径） | `~/.nvm/versions/node/v24.19.0/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/` | **0.1.0-rc.8** | **无**（零命中） | `lib/types/index.d.ts:22` | `lib/index.js:85` | 存在 |

**本任务全部 DSH 行号引用 = 桌面端安装 0.1.6-alpha.2**（yaml note、变更单、live/restart 清单均已标注该版本）。

**给 K3 的复核提示**：若在 rc.8 路径复核，会看到 **行号偏移 1~17 行** 且 **找不到 `dsh-webhook`** —— 这**不是**引用造假，而是安装版本不同。请按本表选择基准安装，或直接以 `package.json` 的 `version` 字段先锚定版本。

**未验证项（诚实登记）**：
- 未在 rc.8 中逐条比对 `dsh-time-context` / `dsh-spill` 的后端文件行号（仅确认包存在 + `SettingsApplies`/`maxInlineBytes` 两处锚点差异）。
- 未核实 rc.8 与 alpha.2 的**行为**差异（只核了文件与行号，未跑运行时）。

---

**可提请独立审计**：本回执 §A/§B/§C/§D 全部结论均附实测路径与行号，可现查复现。上述「未验证项」不隐藏。
