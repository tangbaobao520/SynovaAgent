# D858 开工前冲突扫描（M1 物理输出）

> 任务：D858 ｜ 分支 `gate/d858-canary-glob` ｜ 工作树 `.synova-wt-d858` ｜ 基线 `origin/main cc4e4166`
> 扫描时间：2026-09-21 02:00–02:20 (+08) ｜ 执行者：队长（并行 CTO session）
> 本文件为 M1 原始输出留档（命令 + 输出），非声称。

## 0. 工作树与分支唯一性

```
$ git worktree list | grep -c synova-wt-d858        → 0（开工前不存在，本卡新建）
$ git worktree list | grep d858
/Users/wane/SynovaAgent/.synova-wt-d858   cc4e4166 [gate/d858-canary-glob]   ← 本卡（新建后）
$ git branch --list gate/d858-canary-glob            → 新建（开工前无同名分支）
$ git ls-remote --heads origin | grep -E "d858|d854|ct70"
62a668fe refs/heads/chore/d858-closure          ← 他 session 的 D858 簿记分支（只含 task-state/D858.json）
03aa575f refs/heads/docs/d858-team              ← 派单件分支（docs only）
537cdaa5 refs/heads/docs/d858-v2  / docs/dispatch-d858  ← 派单件分支（docs only）
e854e098 refs/heads/fix/d854-ct70-project-tests-ci      ← CT-70 / PR #682（本卡不碰其写集）
```
结论：`gate/d858-canary-glob` 与 `.synova-wt-d858` 均为本卡独占，无复用。

## 1. 写集文件在全部工作树的未提交改动扫描（防 dirty 循环）

```
$ for wt in $(git worktree list --porcelain | grep '^worktree ' | sed 's/^worktree //'); do
    git -C "$wt" status --porcelain -- scripts/control-tower/check-canary-drift.sh tests/control-tower/check-canary-drift.test.sh
  done
扫描工作树数=169 命中脏文件工作树=0
```

## 2. 近 3 天全部分支对本卡写集的改动扫描（防并行写者）

```
$ git for-each-ref --sort=-committerdate --format='%(refname:short)' refs/heads refs/remotes/origin | head -40
  → 逐分支 git diff --name-only main...<branch> | grep -E "check-canary-drift|task-state/D858"
chore/d858-closure        -> task-state/D858.json
origin/chore/d858-closure -> task-state/D858.json
（其余全部无命中）
```
结论：本卡两文件**零并行写者**；`task-state/D858.json` 另有一份簿记版本（本卡工作树内重写为实际写集，见 §5）。

## 3. 在飞 PR 写集对照（GitHub API，open PRs）

| PR | 分支 | 是否触碰本卡写集 |
|---|---|---|
| #686（已由 CTO 合并，前置解除） | docs/k3-reports-merge | 否（audit-reports only）→ 提供本卡依据 K3 报告入库 main |
| #682 | fix/d854-ct70-project-tests-ci | **否**，但它是 `ci.yml` 单写者（本卡显式不碰 ci.yml） |
| #657 | fix/d839-claim-release | **也改 `.github/workflows/ci.yml`**（第二个 ci.yml 写者）→ 与 #682 串行，登记为 CTO 关注项 |
| #685 / #674 / #665 / #662 / #623 … | 见 API 输出 | 与本卡无交集 |

PR 状态快照（合并决策依据）：
```
PR #686 mergeable=True  state=clean  → 已合并 squash cc4e4166
PR #682 mergeable=False state=dirty  head=e854e098 base=25082884   ← 需其 owner rebase（登记）
PR #657 mergeable=False state=dirty  head=96e40079 base=7e4e169d   ← 同上（登记）
```

## 4. 卡面前提实测（§〇-e 三项 + 复核项）

```
$ grep -n "test\.sh" scripts/control-tower/check-canary-drift.sh
35:LISTED=$(grep -oE 'tests/[A-Za-z0-9_/.-]+\.test\.sh' "$CI_YML" | sort -u)     ← 字面量正则，"*" 不在字符类内 → glob 行零命中
（其余 9 处为注释/输出文案）

$ bash scripts/control-tower/check-canary-drift.sh
── canary 漂移对账 (D526) ──
  测试文件总数: 729 | canary 清单: 41 项
⚠ 漂移: 71 个 .test.sh 不在 CI canary 清单（…）
EXIT=0                                        ← 基线：41 / 729 / 71 / exit 0

$ git show origin/main:.github/workflows/ci.yml | grep -c "tests/project"
0                                             ← 故 ci.yml 归 CT-70

$ git show fix/d854-ct70-project-tests-ci:.github/workflows/ci.yml | …   #682 形态
非注释行字面路径 41 条 ｜ 注释行命中 2 条（gen-project-board / pr-queue-scan）
284: PROJECT_TESTS=(tests/project/*.test.sh)
304: bash scripts/control-tower/check-canary-drift.sh || true
                                             ← 覆盖语义确由注释文本承载（K3 P1 属实）
```

## 5. 前置解除（派单 §〇-d 要求）

```
$ git ls-tree -r --name-only main | grep "audit-reports/2026-09-20-K3-D854.md"
docs/synova/audit-reports/2026-09-20-K3-D854.md
$ diff -q <(git show origin/docs/k3-reports-merge:docs/synova/audit-reports/2026-09-20-K3-D854.md) \
          "/Users/wane/Synova-k3独立审计/docs/synova/audit-reports/2026-09-20-K3-D854.md"
IDENTICAL                                     ← 入库件与 K3 工作区报告逐字一致（92 行）
```
→ CTO 合并 PR #686（squash `cc4e4166`）后，卡面引用的报告路径在 main 物理存在，卡可开工。

## 6. 卡面声称独立核验（claim-verifier；不采信转述）

```
$ for t in tests/project/*.test.sh; do bash "$t"; done | tail -*
calc-progress-panel.test.sh   PASS=20  FAIL=0  rc=0
gen-project-board.test.sh     PASS=117 FAIL=0  rc=0      ← 派单"46+117 条断言"中的 117 属实
pr-queue-scan.test.sh         PASS=46  FAIL=0  rc=0      ← 46 属实
（数字为实跑输出，非引用 K3/派单）
```

## 7. ci.yml 单写者规则的物理依据（本卡红线）

- `ci.yml`（main）L275-277：canary 漂移检查调用被 `|| true` 包裹 → **新 exit 1 在此调用点会被吞**（遗留项，见收口件）。
- 同文件存在两个在飞 PR（#682 / #657）→ 本卡**不碰** ci.yml，避免多 session 抢同一文件（坑 13）。
