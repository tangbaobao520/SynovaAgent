# D862 出站分类台账 — coder-b 写集（routes/ tui-v2/ server.ts）

> 2026-09-21 ｜ 执行：小队 A coder-b ｜ 分支 `feat/p1-outbound-converge`（叠 `feat/d821-http-proxy`）
> 上游权威件：`src/providers/http-exit.ts`（D821 冻结契约，本卡**只读复用，零改动**）
> 派单件：`docs/synova/coordination/派单-小队A-P1收尾出站收敛-20260921.md`（main）

## 一 基线与命令

```bash
# before（HEAD 基线）
git grep -n "fetch(" HEAD -- src/routes src/tui-v2 src/server.ts | grep -v "\.test\." | wc -l
# → 25
# after（本卡完成后）
grep -rn "fetch(" src/routes src/tui-v2 src/server.ts --include=*.ts --include=*.tsx | grep -v "\.test\." | wc -l
# → 23（其中 Node 侧真实出站 = 0；余 23 = 22 处浏览器内嵌 + 1 处注释文字）
```

| 归类 | 计数 |
|---|---|
| 已迁移 `outboundFetch`（Node 服务端真实出站） | **2** |
| 浏览器内嵌 `<script>` 豁免（物理无法走 Node 出口） | 22 |
| 注释/字符串非调用 | 2 |
| 写集外已勘误（登记见 §四） | 4 文件 |
| **合计（写集基线）** | **25** |

> 自验追补（2026-09-21，p1b-verifier）：`src/services/retry.ts:32` JSDoc `@example` 原文含裸 `fetch(` 字样，属"注释/字符串非调用"类——已把示例改写为 `outboundFetch(url)`（一行注释改动，见自验 commit），归入下表非调用类（1 → 2）。另全仓 grep 复核新增一条同类观察：`src/security/connector-sandbox.ts:192` 在沙箱扫描**关键词字符串数组**中出现 `fetch(` 字面量（非调用，属安全扫描器词表），登记备查，不改代码。

## 二 已迁移（2 处）

| file:line（before → after） | 内容 | 说明 |
|---|---|---|
| src/routes/llm-config.ts:192 → :193 | POST /api/llm/test 上游 chat/completions 转发 | init 仅 method/headers/body(string)/signal。错误语义对齐（铁律 32）：调用方 AbortController 超时 → 出口原样传播 AbortError → `TIMEOUT`；出口 `UPSTREAM_TIMEOUT` 并入 `TIMEOUT`；其余 `OutboundHttpError`（PROXY_UNREACHABLE/UPSTREAM_UNREACHABLE/PROXY_TUNNEL_FAILED）→ `NETWORK`。对调用方保持 D575 六值枚举不变。集成测试穿真实 http 上游（tests/routes/llm-config.test.ts 91 例全绿，含 401/429/500/连接拒绝/挂起超时/200 全路径）。 |
| src/tui-v2/chat.tsx:248 → :249 | TUI cron `ontology-monitor` 探测本机 API | loopback 目标；出口契约恒绕过代理（行为不变）。顺手把该处 `catch (err: any)` 收窄为 `unknown` + 类型守卫。 |

## 三 浏览器内嵌豁免（22 处）+ 注释（1 处）

**豁免理由（统一）**：以下 fetch 全部位于 HTML 模板字符串内的 `<script>` 块，
由浏览器在用户代理中执行、打回本服务自身 API（同源相对路径或 `API` 前缀）——
**物理上不经过 Node 进程**，不可能走 `src/providers/http-exit.ts` 出口。不改代码，逐条登记。

| file:line | 证据（所处 `<script>` 块起始行） |
|---|---|
| src/routes/workspace.ts:80, 96, 103, 115, 127（5 处） | `<script>` @ workspace.ts:75 |
| src/routes/ga-diagnosis.ts:136, 150, 183, 192, 295, 331（6 处） | `<script>` @ ga-diagnosis.ts:125 |
| src/routes/chat.ts:290, 296, 485, 673, 772, 881（6 处） | `<script>` @ chat.ts:266（模板 `const html` @ :84） |
| src/routes/ga-evolution.ts:171（1 处） | `<script>` @ ga-evolution.ts:161（模板 @ :20） |
| src/routes/department-workspace.ts:75, 92, 104（3 处） | `<script>` @ department-workspace.ts:72 |
| src/routes/home.ts:148（1 处） | `<script>` @ home.ts:130（模板 @ :17） |

注释/字符串非调用（1 处）：

| file:line | 内容 |
|---|---|
| src/routes/ga-evolution.ts:5 | 文件头注释文字「所有数据通过 fetch() 调用…」——非调用。 |
| src/services/retry.ts:32（自验已改写） | JSDoc `@example` 示例原含 `fetch(url)` 字样——非调用；2026-09-21 自验改为 `outboundFetch(url)` 示例（p1b-verifier，一行注释）。 |
| src/security/connector-sandbox.ts:192 | 沙箱扫描关键词字符串数组中的 `fetch(` 字面量——非调用（安全扫描器词表），登记备查。 |

小计：5+6+6+1+3+1 = 22 处豁免 + 1 处注释（写集内）。写集外非调用追补 2 行（retry.ts / connector-sandbox.ts）见上表，不计入写集小计。

## 四 写集外勘误（4 文件，判定与归属）

| file:line | 判定 | 理由 | 归属 |
|---|---|---|---|
| src/services/deepseek-balance.ts | 迁移 | Node 侧真实上游出站 | **coder-a**（队长裁决勘误扩入其卡） |
| src/mvp-server.ts | 迁移 | Node 侧真实出站 | **coder-a**（同上） |
| src/mcp/tool-definitions.ts:292 | **豁免（判定）** | L1 HTTP 桥惯例：`fetchJSON(url)` 打**本进程 localhost API**（文件头 D595 注释明示「query_ontology/ingest_document/get_session/knowledge_ask → L1 HTTP 桥惯例（localhost fetch）」）。localhost 恒绕过代理（出口契约），迁移行为零差异；保留裸 fetch 不引入 providers 依赖，豁免成立。 | 本卡登记（coder-b 裁决，不迁移） |
| src/deploy/watchdog-entry.ts:33 | **豁免（判定，写集外未改）** | 看门狗健康探针：`fetch(HEALTH_URL)` 打本机 healthz，loopback 恒绕过代理，同上零差异。且 `src/deploy/**` 在 coder-b 硬边界（不碰），如需物理迁移须另派。 | 本卡登记（建议豁免；如裁决迁移另派单） |

## 五 健康检查「不含值」输出样例（D862 验收点 2，入口 routes/healthz.ts 归 coder-b）

GET /api/healthz → `checks.outbound_proxy`：

```json
// 未配置（直连，合法形态）
{ "status": "ok", "detail": "未设置代理环境变量（全量直连）；已配置变量: 无" }
// 配置可用
{ "status": "ok", "detail": "代理可用（kind=http）；已配置变量: https_proxy；loopback 恒绕过；本机 loopback 目标恒绕过（127.0.0.0/8 整段 / ::1 / localhost）" }
// 配置不可用（socks/畸形）→ degraded 可见，无静默
{ "status": "degraded", "detail": "代理已配置但不可用（kind=socks）→ 出站降级直连；已配置变量: all_proxy；all_proxy 是 SOCKS 代理（本模块不支持）— 该 scheme 直连；请改用 http:// 或 https:// 代理 URL" }
```

**不含值保证**：数据源 `getProxyStatus()` 的 `variables` 只含变量名（出口安全契约）；本检查零 env 值读取/拼接。测试（tests/routes/healthz.test.ts `D862:` 组）注入带凭据代理值 `http://user:super-secret-pass@127.0.0.1:3128` 与 `socks5://secret-cred@127.0.0.1:1080`，断言 detail 不含 `super-secret-pass`/`secret-cred`/端口/`user:` —— **反推凭据不可能**。

## 六 验证记录

- `npx tsc --noEmit`：本卡 4 个改动文件零错误（分支既有 `src/server.ts:465` 类型债 + coder-a WIP `src/tools/tech-expert-tools.ts` 缺 import 见 §七，均非本卡引入）。
- `npx vitest run tests/routes/llm-config.test.ts tests/routes/healthz.test.ts tests/providers/http-exit.test.ts` → **91/91 通过**（含新增 outbound_proxy 三路径：正常/边界带凭据/降级 socks）。
- 反向验收：llm-config.test.ts 的 NETWORK/TIMEOUT 用例（真实连接拒绝/挂起上游）即"抽掉迁移必红"的等价证据——它们只经 `outboundFetch` 这一条路径打真上游。
