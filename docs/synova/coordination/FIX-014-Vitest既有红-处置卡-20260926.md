# 卡 · FIX-014 — main `Vitest (2/2)` 既有红：复核 + D954 棘轮登记（含 disposition_due + 责任人）

> 编号 **FIX-014**（台账 `docs/synova/coordination/问题修复台-20260925.md` 已分配；FIX-### 不占 D# 号段）
> 类型：既有红登记 / 棘轮补键（**另开卡** = 本文件，替代在 CTO 单写台账里直改）
> 责任人（owner）：**Mac-小队**（台账 FIX-014 属主；棘轮条目 owner 字段同步为 `Mac-小队`）
> 处置期限（disposition_due）：**2026-10-24**（`Vitest (2/2)`，与 expires 对齐）；`Vitest (1/2)` = 2026-10-25
> 产出者：fix-coder-b｜日期：2026-09-26｜分支：`fix/fix014-vitest-preexisting-red`｜base = `origin/main` @ `ef299746`

---

## 1. 判据（台账原文）

> **另开卡 + 按 D954 棘轮登记（含 `disposition_due` + 责任人）；不得豁免计绿**；报告须分列「本 PR 引入」/「既存」。

| 判据项 | 达成 | 物理证据 |
|---|---|---|
| 另开卡 | ✅ 本文件（新文件承载，不占 D# 号段、不改 CTO 单写台账） | `docs/synova/coordination/FIX-014-Vitest既有红-处置卡-20260926.md` |
| D954 棘轮登记 | ✅ `Vitest (2/2)` / `Vitest (1/2)` 两条补 `disposition_due` + owner | `scripts/control-tower/ci-red-baseline.txt` diff（3+/2-） |
| 含 disposition_due | ✅ 2026-10-24 / 2026-10-25（在期；逾期由 C 段判红） | 夹具 (b)：逾期 → `VIOLATION: CI 红处置逾期` exit 1 |
| 含责任人 | ✅ `owner=UNASSIGNED` → `owner=Mac-小队`（FIX-014 属主） | 按键取值复算输出（evidence §3.3） |
| 不得豁免计绿 | ✅ 只计「基线命中」，红属性保留：vitest job 真 exit 1、run 全站 = failure | evidence §1（job 108108419949 / run 36146340288） |
| 分列本 PR 引入/既存 | ✅ 既存（父提交 0f709900 同名同 file:line 同红）；本推送引入 **0** | evidence §2 |
| skip 不计绿 | ✅ PR 侧 `Vitest (${{ matrix.shard }})` = skipped → 记「未执行」 | evidence §5 |

---

## 2. 事实基线（原始输出见 evidence）

- main push run **36146340288** @ `ef299746`：`Vitest (2/2)` = **failure**（job 108108419949）、`Vitest (1/2)` = cancelled（fail-fast 取消兄弟分片，非红非绿）、其余 8 job success、Checker Review / Golden Case F1 Gate = skipped → run **conclusion = failure**。
- 唯一失败用例：`tests/l3/graphbridge-wiring.test.ts:79` — `expect(graphNodes.length).toBe(1)` 收到 `0`（`Test Files 1 failed / 293 passed / 1 skipped (295)`）。
- 既存判定：父提交 `0f709900`（run 36129254913）**同名同 file:line 同红**；`ef299746` 改动 15 文件中命中该测试 = **0**。
- 棘轮登记前缺口：main 版 `ci-red-baseline.txt` 数据行 6 条，`disposition_due` 命中 = **0**，`Vitest (2/2)` owner = `UNASSIGNED`。

---

## 3. 交付物（本卡写集，逐文件）

| 文件 | 改动 | 说明 |
|---|---|---|
| `scripts/control-tower/ci-red-baseline.txt` | +1 表头注释；`Vitest (2/2)` 加 `disposition_due=2026-10-24` + owner→`Mac-小队` + 追加复现 evidence；`Vitest (1/2)` 加 `disposition_due=2026-10-25` | **只加不删**（既有 6 条 0 删除）；不动其它数据行 |
| `docs/synova/product-lines/evidence/FIX-014-20260926.md` | 新：全部原始输出（run/job/日志/夹具/PR skip） | 本卡证据全文 |
| `docs/synova/coordination/FIX-014-Vitest既有红-处置卡-20260926.md` | 新：本卡 | ← 本文件 |
| `memory/notes/proposed/2026-09-26-fix014-vitest-preexisting-red-ratchet-disposition.md` | 新：决策 Note | D534 物理门禁：治理脚本区变更的 commit 须引用 Note |

**未触碰**（红线）：`.github/workflows/ci.yml`（单写者 fix-coder-a）、`scripts/audit/**`、`docs/synova/audit-reports/**`、`docs/synova/coordination/问题修复台-20260925.md`（CTO 单写者）、`task-state/**`。

---

## 4. 改坏即红（判别性夹具，非 grep 型静态判据）

命令（夹具副本经 `SYNO_CI_RED_BASELINE` 注入缝，仓库零篡改）：

```
bash scripts/control-tower/check-gate-integrity.sh --patterns-only --root "$PWD" \
  --base-ref origin/main --ci-reds /tmp/fix014-checkruns.json
```

| 场景 | 结果 | 退出码 |
|---|---|---|
| (a) 登记后（在期 disposition_due） | `基线命中 1 项；处置逾期 0 项` → OK | **0** |
| (b) 滥用：`disposition_due` 改过去日期 | `VIOLATION: CI 红处置逾期（disposition_due 硬门）` | **1** |
| (c) 滥用：删除 `Vitest (2/2)` 条目 | `VIOLATION: 未登记 CI 失败: Vitest (2/2)` | **1** |
| (e) 边界：删除 `disposition_due` 键 | `处置逾期 0 项` → OK（D954「键缺失不回溯」明文语义） | 0 |

(a)/(b)/(c) 构成「合法仍绿 / 滥用仍红」的成对反例；夹具输入为**真实 check-runs JSON**（`failure names = ['Vitest (2/2)']`），非构造假数据。

---

## 5. 未清项（诚实登记，不由本卡自行放行）

1. **(e) 删键不红**：D954 明文「键缺失不回溯」→ `disposition_due` 是加严项，不是唯一门；`expires=2026-10-24` 仍兜底。若要「缺键即红」，属**判据语义变更**，须过 K3 另立卡（本卡不做）。
2. **D721 放行分支在 main push 上不可达**（结构性问题）：`ci.yml:131-157` 的 `BASE=merge-base(origin/main,HEAD)` 在 main push 恒 = HEAD → `CHANGED` 恒空 → 存量红放行分支永不触发 → **任何 main 存量红都恒红**。修属 ci.yml（单写者 fix-coder-a），已由 lead 转出，本卡只登记事实。
3. **分片名非确定**（`Vitest (N/2)` N 随 shard 配置漂移）：现靠两名并存登记；根治 = 分片名归一化（去 `/N` 或按家族匹配）→ 判据语义变更，须过 K3，另立卡。
4. **`task-state/FIX-014.json` 未建**（lead 条件项 ③）：实测分配器 `scripts/control-tower/alloc-task-id.sh:332 NEW_ID="D${NEXT}"` **只发 D#**（`--prefix` 仅影响 worktree/分支命名，`:66/:340`），与台账「FIX-### 不占 D# 号段」冲突；`gen-task-board.py` 亦不读 `task-state/`（`grep -n "task-state"` = 0 命中）→ 无物理门禁/看板要求，故**不建 task-state 条目**，由本卡承载。若需机器可读承载，须 CTO 裁「FIX 号段是否纳入分配器 `--prefix`」（工具变更，非本卡可自决）。
5. **死条目卫生**：`Vitest (2/2)` 转绿后须删条目（本文件无自动清理）；处置期限 2026-10-24 前若未修，C 段将按 `disposition_due` 判红（预期行为，非缺陷）。

---

## 6. 原始输出索引

- evidence 全文：`docs/synova/product-lines/evidence/FIX-014-20260926.md`（§1 复现 / §2 既存分列 / §3 登记前后 / §4 夹具 / §5 PR skip）
- 台账：`docs/synova/coordination/问题修复台-20260925.md` FIX-014 行（CTO 单写者，本卡不改）
- 上游决策：`memory/notes/proposed/2026-09-25-d954-ci-reds-base-ref.md`（D954 `--ci-reds` 对账对象 + `disposition_due` 消费）
