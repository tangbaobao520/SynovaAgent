# K3 复审派单｜D864（小队 A）+ D865（小队 B）· 两份独立

> 出件 2026-09-21 CTO ｜ 依据：两支均已按 K3 原判（A=CONDITIONAL PASS / B=FAIL）逐项闭环，CTO 收件通过
> **纪律**：两份**分开审、分开报告、互不引用**；报告分支必须推送并附 `git ls-remote --heads origin | grep k3` 回执（D859）

---

## A 件｜K3 复审 · D864（P-1 收尾出站收敛）

**对象（CTO 已复算回执一致）**：伞分支 `feat/p1-outbound-converge @ 444b2f55`；6 支 `pr1 47d82edb / pr2 6f101df0 / pr3 415e6ee6 / pr4 cd56ac67 / pr5 30b3c9c2 / pr6 6e2abeb3`
**报告**：`docs/synova/audit-reports/2026-09-21-K3-D864-复审.md`

回应你原判的 5 项条件（逐项独立复算，不采信自述）：
1 **P1-1**：`git diff --name-only origin/feat/d862-pr5-routes-tui..origin/feat/d862-pr6-docs-state` 是否 = 12 文件且 `src/**` 重叠 = 0；**6 支各自增量是否全 ≤12**（你给过的每支数字：4/3、9/8、8/7、7/6、8/7、12/10）
2 **P2-a**：`backup-scheduler.ts:182` 是否登记为第 3 条 loopback 豁免；**扩口径 sweep 是否真 37 行命中**（26 fetch + 11 非 fetch）且 axios/node-fetch/got/ky/undici/ws/net.connect/XMLHttpRequest 全零命中；基准命令是否写入台账 §7.1
3 **P2-b**：`healthz` catch 是否零回显（固定文案 + 日志收窄 name/code）；断言是否升级为**整包**（`objectContaining` + `JSON.stringify not.toContain`）；**反例红→绿**是否可复现（注入含凭据的 message）
4 **P2-c**：15 对两两 `comm` 是否唯一交集 = `.claude/bypass.log`；八步合并顺序是否可行（可实建工作树模拟）
5 **回归双轴**：确定性失败集是否与基线逐字相同（8 例）；3 项全量差集（zero-code .ts / metrics / p0-wane-baby）的"脏树与悬挂进程"归因是否成立
6 **无回归**：本轮改动是否引入任何新失败（与 D821 基线比对）
7 **红线**：`http-exit.ts` diff 0 行；G1=0；无 --no-verify/stash/force push

---

## B 件｜K3 复审 · D865（D831 P-2 不变量）

**对象（CTO 已复算回执一致）**：`feat/p2-invariants-core @ 39bce446`、`feat/p2-invariants-failclosed @ 67bd295e`、`feat/p2-invariants-docs @ 6642414a`
**报告**：`docs/synova/audit-reports/2026-09-21-K3-D865-复审.md`

回应你原判的 P0×2（逐项独立复算）：
1 **P0-1 真 fail-closed**：真实入口注入违约（组装请求丢 tools）→ **500 + JSON 含 `INVARIANT_VIOLATED` + `invariant` + `owner`**（不是 200/SSE 文案）；**头已发场景**是否按声明走"流内 INVARIANT_VIOLATED + 断流"；旧"HTTP 层 5xx 语义"的不实声称是否**已全部改写**（grep 证）
2 **F1（修复引入的新缺陷）**：首 token 前非不变量错误 → 客户端是否**不再挂死**（复现 2×2 矩阵：任一侧修复即 ≤26ms 返回 open/error/end；修复前 3004ms 超时）；定向 6 路径是否 97 passed / 0 failed
3 **P0-2 CI**：三支 job 级回执是否属实（`core RUN 35611745060 / failclosed RUN 35615404724 / docs RUN 35620058075`，`Iron laws check` = FAILURE、`Vitest`/`Golden` = skipped）；**"CI 红只剩 D734 预算口径"的归因是否成立**（本地 CI 等价复现：组 2/4/6 已修、其余 12 组 ✅）；三支实测文件数 18/24/29、win 最小自洽集 15 >12、`tests/sentinel/**(mac) ↔ src/invariants/**(win)` **跨域是否结构性必然**
4 **回归**：与 main 基线失败集是否逐条相同
5 **红线**：无 --no-verify/stash/force push；未碰 `scripts/audit/**`；未自我审计

**已知边界（两支）**：N3（首 token 窗口心跳/超时，CTO 已判"不阻断 + 立 P1 卡 D869"）、N4（web-adapter 非 deferred 路径）、F7（组 7 grep 静默降级，控制塔域）、F8（主仓证据漂移，**CTO 已还原**）、前缀豁免 / 探针认证 / Windows 实机 —— 请判"可登记不阻断"
