# K3 审计派单（2026-09-20）— 4 份，含 3 份**上轮欠账**

> 出件：CTO ｜ 用途：创始人**直接转发**给 K3 独立审计 session
> 协议：`docs/synova/coordination/AUDIT-PROTOCOL.md` + `K3-AUDIT-STANDARD-v2-20260820.md`
> 报告目录：`docs/synova/audit-reports/`（git 跟踪）
> **独立性要求**：最好**一份审计一个 K3 session**（零上下文）。若只能开一个 session，按序做，但**每份报告的取证必须独立完成**，且**不得以开发者自述/汇报作为证据**。

---

## 派单顺序与理由

| 序 | 卡 | 审计对象 | 为什么这个顺序 |
|---|---|---|---|
| ① | **D841（新）** | D839 认领门禁变更（PR #657） | 它在等审计才能合并；且被改的是**认领门禁自己**——门禁被削弱 = 全线保护失效 |
| ② | **D836（欠账）** | D819 工具接线三修（试点队产出） | 试点指标③：**审计到底能挑出几个问题**——判"小队模式可信吗" |
| ③ | **D830（欠账）** | D810 是否真覆盖（对着已合入 main） | D819 已合，仍缺独立结论 |
| ④ | **D815（欠账）** | 线 10 资本循环全链路复核 | 第一条 100% 线**只差这一步** |

---

## 通用纪律（每条审计都适用，转发时一并带上）

```
【K3 独立审计 · 通用纪律】
· 零上下文：只读任务卡 / 代码 / 命令原始输出。**不接受开发者的转述或汇报当证据**。
· 独立取证环境：新 clone 或独立 worktree（不得复用任何在飞 session 的工作树）。
· 每条发现必须带：file:line + 可复现命令 + 原始输出。分 P0/P1/P2。
· 禁止把「grep 命中 / 文件已创建 / 单测绿」当作结论（验收标准 v1.1 五态判定）。
· 红线：不改产品代码、不改被审对象、不写审计标准（标准已有 K3-AUDIT-STANDARD-v2）。
· 交回：报告路径 + P0/P1/P2 计数 + 每条发现一行摘要 + **未覆盖面清单**（没验到的地方必须写明）。
· 报告落 `docs/synova/audit-reports/2026-09-XX-K3-<卡>.md`，并回填对应卡的 audit 段。
```

---

## ① D841｜K3 独立审计：D839 认领门禁变更（PR #657）

```
审计对象：分支 fix/d839-claim-release（HEAD 3a0b05dc）、PR #657；被审文件
  scripts/control-tower/{claim_release.py,staging_guard.py,synova-commit}、
  scripts/workflow/resolve-commit-brief.sh、task-state/claim-releases.json、
  tests/control-tower/{staging_guard,claim_release}.test.sh
报告：docs/synova/audit-reports/2026-09-20-K3-D839.md ｜ 卡：task-state/D841.json

【必查 7 条（每条给 P0/P1/P2）】
① 释放语义可被伪造吗：若仅需改 task-state 状态即可解锁**他人**文件，谁能改 task-state？
   普通编码 session 能否自证 impl_done 去解锁别人的认领？→ 给出攻击路径或给出不可行的证据。
② fail-closed 真伪：拿不到"已完成"证据时必须仍 block（复跑场景 D，并自造 2 个反向用例：
   卡不存在 / status 字段缺失 / status 值非法）。
③ 批量释放护栏：release-stale 会不会把**进行中**任务释放？
   独立复现开发者的口径实验（全量派生口径 16→24，多出的含 D711/D819 等 claimed）——
   这个结论是真是假，直接决定"不做扩容"的决策对不对。
④ 并发保护未被放松：任务在飞时改其认领文件仍必须 block（复跑场景 B，并自造变体）。
⑤ 源头剔除的副作用：resolve-commit-brief.sh 有 6 个生产消费者，逐个核"被剔除 brief"后
   它们的行为是否仍正确（尤其 G12 范围检查 / commit-msg / staging_guard）。
⑥ 释放台账正确性：task-state/claim-releases.json 谁能写、并发冲突怎么办、
   **删掉某条 released 记录会不会重新被拦**（持久化真伪）。
⑦ 夹具有效性：把夹具搬到 origin/main 复跑，是否真报红（先红后绿是否成立）；
   夹具自身有没有"为通过而通过"的宽松断言。

命令起点：bash tests/control-tower/staging_guard.test.sh ； bash tests/control-tower/claim_release.test.sh
```

## ② D836（欠账）｜D819 工具接线三修抽审

```
审计对象：PR #646 / #647、分支 feat/d819-tool-wiring-pilot
报告：docs/synova/audit-reports/2026-09-19-K3-D819.md ｜ 卡：task-state/D836.json
卡内既有 6 条验收点（工具真发出 / tool_call_id 配对 / 助手不重复入上下文 / 自验遗留 L1-L6 …）逐条对照；
命令与原始输出自取，不复用开发者给的输出。
```

## ③ D830（欠账）｜D810 是否真覆盖

```
审计对象：D810 交付（对着**已合入 main** 的代码独立复核，不看 D810 自述）
报告按卡 D830 的 write_set ｜ 卡：task-state/D830.json
```

## ④ D815（欠账）｜线 10 资本循环全链路独立复核（第一条 100% 线收官）

```
审计对象：线 10（资本循环）全链路 V1 判据 6/6 的**证据链**是否 100% 真实可复现
报告按卡 D815 的 write_set ｜ 卡：task-state/D815.json
这是 10-8 门槛（面板第一条 100% 线）——宁可判"未达标"，不要给面子分。
```

---

## 卡内已登记的写集/依赖（供转发时核对）

| 卡 | 写集 | 依赖 | 原 next |
|---|---|---|---|
| D815 | ['docs/synova/audit-reports/2026-09-18-K3-line10.md', 'docs/synova/product-lines/evidence/k3-line10-2026-09-18.json', 'task-state/D799.json', 'task-state/D815.json'] | - | ① 读 origin/main 的 product-lines.yaml 线10 六点与证据 ② 实跑 GS-03（含负向用例）③ grep 生产调用点 ④ 出报告 + 回填 D799.audit ⑤ 若 PASS 落  |
| D830 | ['task-state/D830.json'] | ['D819'] | 见派单文档对应小节；开工第一步重新核行号（会漂移） |
| D836 | ['docs/synova/audit-reports/2026-09-19-K3-D819.md', 'task-state/D836.json'] | - | ① 读 PR #646/#647 与分支 feat/d819-tool-wiring-pilot ② 在独立 clone 内复跑验收命令 ③ 逐条对照卡内 6 条验收点 ④ 出报告 + 回填本卡 audit 段 |
| D841 | ['docs/synova/audit-reports/2026-09-20-K3-D839.md','task-state/D841.json'] | - | ① 读分支与 PR ② 独立复跑夹具 + 自造反向用例 ③ 必查 7 条 ④ 出报告 + 回填 audit |
