# K3 审计清单（2026-08-22，CTO 汇总）

> 数据源：task-state/ 状态机（impl_done 未 audited 的全部任务）
> 原则：K3 审计出问题 → 另起 FIX 任务（D382 审计闭环铁律），禁直接改原任务

---

## P0 — 近期核心（动架构/控制塔，优先审计）

| 任务 | 内容 | 为什么优先 |
|---|---|---|
| D467 | 方案1 挪CI——本地软提示+CI权威 | 动"零 AI 自律"执法位置，最大变更 |
| D468 | 方案3 同步降频（砍 D335） | 砍提交前同步，防覆盖改单端 |
| D465 | CI 门禁 diff 语义补齐（空暂存假绿消除） | CI 权威的基础 |
| D466 | check-bypass-log 注释同步 + tag-bypass-wiring 跨平台 | 消除 fail-open/fail-closed 漂移 |
| D500 | 事件溯源 session log（Stage1-D1） | 4 复核修复 + engine 悬空 seam 诚实降级 |
| D472 | Agent Notes 四态铁律结构化（Stage1-D2） | Stage 1 范式借鉴 |
| D473 | guard 循环卫生+超时（Stage1-D4） | 复核修复 TS2322 类型违规 |
| D474 | snapshot keyless 回放门禁（Stage1-D3） | 复核修复 main 入口误绿 |

## P1 — 部署轨 + 场景脚本

| 任务 | 内容 |
|---|---|
| D442-D449 | GS-02~08 场景脚本（8 个；GS-07 诚实 RED 2/3，D338 缺失已标注） |
| D454 | GSS 服务启动原生崩溃修复 env+ESM |
| D455 | 修复 D355 残留（cashBalance↔cash 对齐） |
| D462 | better-sqlite3 v12 升级（Node 24 兼容） |
| D463 | GS-05 告警闭环修复（run-once 接管线） |
| D412-D415/D419 | U 系列门禁（u3/u7/u1/u2/founder-truth，CTO 已核实脚本+接线） |

## P2 — 历史批次（8 月控制塔/治理，台账有部分审计记录，需核对哪些已 audited）

D379、D384-D410、D428-D430、D439-D441、D450-D453、D456-D461 等约 50 个。

> 注：这些历史任务多数在台账已有 K3 审计记录（CONDITIONAL PASS 等），但 task-state 未回填 audited。**建议**：K3 先核对台账已有审计记录，把已审的任务回填 audited，只对"台账无审计记录"的任务做新审计，避免重复审计。

---

## 给 K3 的提示

1. **D467（挪 CI）是最大变更**，重点审：本地软提示后 CI 权威是否真的兜底、GATE_FAIL_SOFT 证据链是否完整、有没有"本地放行 + CI 也漏拦"的盲区。
2. **D500 的 engine 悬空 seam 是诚实降级**（store 层事件流已生效，engine 全量装配是后续任务），审降级声明的合理性即可，不必当 P0 缺陷。
3. **D473 暴露的系统盲点**（vitest 不查类型 + pre-commit 组1 只查 as any）已记台账，是流程缺口，等 D500 后 CTO 补充。
