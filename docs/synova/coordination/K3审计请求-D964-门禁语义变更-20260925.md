# K3 审计请求 — D964 文档减负 + K3 报告指针式（门禁语义变更，必过 K3）

> 起草: 🧭 Mac-CTO（并行 CTO session）｜日期: 2026-09-25｜状态: **待 K3 执行**
> 前置: 本程序含**门禁语义变更**两处 ⇒ 按红线「门禁语义变更 ⇒ 必过 K3；K3 前不合并」，PR #764 / #766 **在 K3 结论出来前不得合并**。

## 一、审计对象（PR × 变更 × 风险）

| PR | 分支 | 变更 | 门禁语义 | 是否必过 K3 |
|---|---|---|---|---|
| **#764** | `chore/cto-doc-slim-phase1-writeset-exempt` | `scripts/control-tower/merge_writeset_gate.py` BUILTIN_EXEMPT 新增 `docs/synova/audit-reports/**`、`archive/**`；匹配从精确相等升级 fnmatch glob | **是**（放宽了夹带判定） | ✅ |
| **#766** | `chore/cto-doc-slim-phase3-dup-rule` | `scripts/doc-system/doc-registry-gate.sh` 新增「同类文档唯一性」（归一化撞名/内容指纹 ⇒ 须声明取代，否则 exit 1） | **是**（新增阻断判据） | ✅ |
| #765 | `chore/cto-doc-slim-phase5-daily-ratio` | `daily-cto-board.sh` 加一行旁路指标（不参与红项） | 否（旁路，不阻断） | 建议抽检 |
| #767 | `chore/cto-doc-slim-phase4-archive-briefs-1` | 274 份零引用 briefs 归档到 `.claude/task-briefs/archive/` | 否（纯移动） | 建议抽检 |

未提交部分（阶段 0 只读产出、台账登记）不算审计对象。

## 二、K3 必须独立判定的是什么（**不要采信本文件的结论转述**）

### A. #764 — 豁免是否「过宽」（核心风险：豁免即放行口子）
1. **判据**：`docs/synova/audit-reports/**` 与 `archive/**` 的内置豁免，是否让**夹带**（把不受控改动塞进 PR）变得可行？
   - 复核要求：K3 **自行**构造反例——把**任意非审计内容**（如 `src/**.ts`、`scripts/audit/**`）改名/移动到 `archive/**` 或 `docs/synova/audit-reports/**` 下，看是否能绕过写集对账。
   - 当前实现：豁免是**路径级**（glob），**不检查文件内容或来源**。K3 需判定该口子可接受与否，并给出裁定（可接受 / 需加约束 / 需撤回）。
2. **理由真实性**：两条豁免的 `理由` 是否与仓库现实一致（K3 自证：`audit-reports/` 目录现状、是否有 K3 独立仓指针式约定）。
3. **glob 语义**：`fnmatch` 与既有 `matches()`（declared 项用的匹配器）语义是否一致？不一致是否造成"声明豁免更严 / 内置豁免更松"的口径分裂？

### B. #766 — 新判据是否「误拦」（核心风险：假红阻断正常文档工作）
1. **误拦面**：K3 **自行**构造样例，验证以下是否被误判重复：
   - 同目录下**不同类型的同名文档**（如 `README.md` 与 `docs/x/README.md` —— 归一化只取 basename ⇒ 判撞名）
   - 常见占位/模板文档、`index.md` 类
   - 已有大量同类文档的目录（`docs/synova/coordination/` 有 213 份派单/周报）
2. **绕过面**：声明行 `取代:` / `合并:` / `supersedes:` 是**纯文本匹配**，是否可被无意义声明绕过（写一行空声明的路径即放行）？K3 判定该逃逸是否可接受。
3. **性能**：新增检查对 2100+ 文档仓库的耗时（K3 实测 `time`，判是否可能触发 pre-commit 超时 → 被迫 `--no-verify`，即 V4.5.1 的根因）。

### C. 两侧证据自核（K3 自行复跑，不采信 CTO 输出）
```
# #764 改前（夹带红）
python3 scripts/control-tower/merge_writeset_gate.py --base <base> --head HEAD --branch <br> --repo-root .
#   → 期望: ❌ block，点名 docs/synova/audit-reports/INDEX.md
# #764 改后（豁免绿）
#   → 期望: 豁免条目打印 [builtin] 理由，INDEX.md 不再点名
# #764 改坏即红
bash tests/control-tower/merge_writeset_gate.test.sh   # 期望 33 通过（含 ⑨ 反证「删豁免键 ⇒ 必红」）

# #766 改坏即红
bash tests/doc-system/doc-dup-rule.test.sh             # 期望 13 通过（② 构造重复文档 ⇒ exit 1）
bash tests/doc-system/doc-registry-gate.test.sh        # 期望 9 通过 / 0 失败（回归）
DOC_TRUTH_ROOT=. bash scripts/doc-system/doc-registry-gate.sh   # 期望 0 疑似重复（不误拦存量）
```

## 三、CTO 自述的已知缺口（**请 K3 验证是否成立，而非直接采信**）

1. `archive/**` 豁免**只加在** `merge-writeset-gate`；`check-citations.py` 的 archive 豁免根**未加**（当前无实际阻断，因该检查只挂夹具、未接全仓扫描）。
2. `alloc-task-id.sh` 挂死（>60s 无输出），本单**手工取号 D964**（撞号已核：origin/main 最高 D963）。
3. 归档不降低文档:代码比（`git mv` 仍在 `git ls-files` 内）。
4. 阶段 2 的 `#751` 清障改动含 `.claude/bypass.log`（union 解决）与 `task-state/D922.json`（取 main 侧）——**请 K3 核对 union 结果是否丢失历史条目**（对比 `origin/main` 与分支两版行数）。

## 四、K3 交付要求

- 报告落 K3 独立仓（**指针式**：本仓只在 `docs/synova/audit-reports/INDEX.md` 落一行索引）
- 结论须含：`通过 / 不通过 / 有条件通过`（**有条件通过 = 未通过**，按验收链纪律）
- 每条结论附 `file:line` + 原始输出
- 若判「需加约束」，给出**最小修法**（CTO 另起 FIX 卡，不塞回原卡）
