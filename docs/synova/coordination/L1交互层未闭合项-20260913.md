# L1 交互层未闭合项取证（D740）

> 任务: D740（P0，只取证不改） | 取证者: 🧭 synova-cto（DSH line，Mac 主 CTO）
> 基线: `origin/main @ 3f0a43b5`（取证时 `git fetch --all` 后）
> 纪律: 只读不改；未碰 `scripts/audit/`；未改 `generate-dashboard.py` / `pre-commit-check.sh`（D735 爆炸半径）
> 取证时间: 2026-09-14 01:48 +0800
> 无证据的判断不写——本文每条"现状"均附可直接复跑的命令与实测输出

---

## 0. 结论速览（先读）

| 任务 | 审计裁决 | 条件 | 现状 | 一句话 |
|---|---|---|---|---|
| D518 | CONDITIONAL PASS | C1 + C2 | **两项条件均物理满足，但 verdict 未转正（仍 CP）** | 条件做完了，转正动作没人做 |
| D519 | CONDITIONAL PASS | C1 + C2（同批共享） | 同上 | 同上 |
| D590 | CONDITIONAL PASS | ①骨架 brief 须清 ②台账维护 ③P2 | **①仍开着 ②仍开着** | 7 份骨架 brief 仍在 main；台账无 D590 条目 |
| D591 | CONDITIONAL PASS | 同批 ①③ + P1-2 交叉引用 | **①仍开着；P1-2（L2 缺陷）仍开着** | 同上 + L2 fromState 缺陷存续 |
| D593 | CONDITIONAL PASS | ①棘轮收紧 ②CTO 确认 CI | **①记账侧已落地/根治侧仍开着；②已由 CTO 确认** | DI 注入重构仍挂 backlog，未复审升 PASS |
| D595 | CONDITIONAL PASS | ①evidence 入库 ②spec 载体 ③gitlink | **①仍开着 ②仍开着 ③已闭合** | 7 件 evidence 与 spec 至今不在 main |
| D602 | CONDITIONAL PASS | spec 未入库（P1-1 结转） | **仍开着** | spec 仍不在 main（"brief 即 spec 载体"仅登记未获批） |
| D594 | **无裁决** | — | closed | 编号被重分配：同一交付由 D602 承载并已审 |
| D572 | **FAIL** | — | 部分整改 | P0 本体单 D578=PASS 但带条件未闭合；P1 stale 机制已闭合；P1 verdict 滞留仍开着 |

**统计**: 9 个对象中，**0 个已完全闭合**，2 个（D594/D572）有明确处置归属且有后续任务承接，7 个 CONDITIONAL PASS 的条件仍有 8 条开着（含 D518/D519 的"条件满足但未转正"）。

---

## 1. D518 — L1-A 安装引导单入口（1-5）

### ① 条件原文摘录

来源：`docs/synova/audit-reports/2026-08-25-D517-D519.md:12-14`（结论节，K3，2026-08-25）

> 核心声称全部经 K3 独立物理复跑证实（全链构建 + 安装 + 四断言 exit 0）。两个条件项：
> 1. **C1（结构延期，非缺陷）**: 合并后 push main 触发 desktop-build CI，补 artifact URL 进 task-state/D517 DS3、D519 DS6——`on: push main` 决定本分支无法自证，dev doc 已显式 ⏸ 声明。
> 2. **C2（P1-1 返修）**: D518 dev doc 回填 prod 运行时契约变更（`node dist/src/index.js` → `dist/backend.mjs` + 包内 Electron node 模式），消除文档-实现漂移（M7）。

转正判据原文（同报告 `:134`）：

> C1 完成后（CI artifact URL 回填）+ C2 完成后（dev doc 回填）→ 可转 PASS，无需复审全量，K3 只核两项证据。

### ② 未闭合项一句话

C1/C2 两项条件**物理上都已完成**，但没有任何一方执行"K3 只核两项证据 → 转 PASS"的转正动作，task-state 里 verdict 自 2026-08-29 起停在 `CONDITIONAL PASS`。

### ③ 现状核实：条件已闭合 / verdict 未转正（仍开着）

**C1 已闭合**（artifact URL 已回填 D517 DS3 + D519 DS6，且 CI run 物理存在）：

```bash
# 取证用命令（实测输出）
git show origin/main:task-state/D519.json | grep -o "actions/runs/[0-9]*/artifacts/[0-9]*"
# → actions/runs/32870900391/artifacts/9572118172
# → actions/runs/32870900391/artifacts/9572059369
git show origin/main:task-state/D517.json | grep -c "actions/runs/32870900391"   # → 1
# CI run 独立核验（GitHub API，read-only）
curl -s -H "Authorization: token $GITHUB_TOKEN" \
  https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/32870900391
# → id=32870900391 name=desktop-build status=completed conclusion=success
#   head_branch=fix/d529-desktop-ci event=push created=2026-08-25T16:15:49Z
```

**C2 已闭合**（dev doc 回填提交 `532aaa41`，prod 契约为 `dist/backend.mjs`）：

```bash
D=docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md
git show origin/main:$D | grep -c "dist/backend.mjs"        # → 9（§2:32 / §4 / §5.1:55-58 / §7:76 均已回填）
git show origin/main:$D | grep -c "dist/src/index.js"       # → 1（仅 :27 历史漂移叙述，非契约）
git log -1 --format="%h %s" -- $D
# → 532aaa41 docs(D524): D518 dev doc prod 契约回填——dist/backend.mjs + ELECTRON_RUN_AS_NODE=1（K3 C2 P1-1 返修）
```

**verdict 未转正（仍开着）**：

```bash
for d in D517 D518 D519; do git show origin/main:task-state/$d.json \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['task_id'], (d.get('audit') or {}).get('verdict'), d.get('updated_at'))"; done
# → D517 CONDITIONAL PASS 2026-08-29
# → D518 CONDITIONAL PASS 2026-08-29
# → D519 CONDITIONAL PASS 2026-08-29
```

**判定：条件已闭合，状态未转正。** 属于流程未收口（非缺陷），与 D572 的 P1"verdict 滞留"互证。

---

## 2. D519 — L1-A Mac 安装实测（1-3）

### ① 条件原文摘录

与 D518 同批同源（切片级 C1/C2 共享，`2026-08-25-D517-D519.md:12-14`，原文见 §1）。K3 对 D519 的 C1 落点是 **D519 DS6 双平台字段**。

### ② 未闭合项一句话

与 D518 同一条：C1 已回填（DS6 双平台字段含 CI artifact URL），但切片级 verdict 未转正。

### ③ 现状核实：条件已闭合（DS6 已回填）

```bash
git show origin/main:task-state/D519.json | grep -n "CI artifact（C1 闭环" | head -1
# → :16 "DS1_real_run": "... CI artifact（C1 闭环，run 32870900391）: macOS=...9572118172 / Windows=...9572059369"
```

Mac 实测本体（DS1-DS5）K3 已独立复跑证实（同报告 §一）；本次未复跑（只取证不改），仅核实条件字段与 CI run 真实存在（见 §1 命令）。

---

## 3. D590 — L1-P1 对话 HTTP-SSE 端点

### ① 条件原文摘录

来源：`docs/synova/audit-reports/2026-09-09-K3-D590-D591-closeout.md:16-18`（结论节）

> 条件：①D590 squash 夹带 7 份骨架 brief 入库并在本机 CI-strict 模拟中实证毒化门禁（P1-1，须清）；
> ②K3审计清单未收录 D590-D592（P1-3，CTO 台账维护）；③其余 P2 见清单。

P1-1 机理原文（同报告 P1 节，task-state/D590 转述）：
> D547 门禁（2026-08-28 已入）应拦未拦，机理无法从工作区判定（CI job 级无 API 可核）——修复=FIX 任务删除/回填 7 骨架 + 主树骨架扫描兜底

### ② 未闭合项一句话

**①仍开着**——7 份骨架 brief 至今仍在 `origin/main` 的 `.claude/task-briefs/`，占位符原文可 grep；**②仍开着**——CTO 台账（两份）中 D590/D591/D592 命中数均为 0。

### ③ 现状核实：仍开着（两项）

```bash
# ① 7 份骨架 brief 仍在树 + 占位符仍在
for f in "2026-09-08-D591-L1-P1桌面端对话接线.md" "2026-09-08-D593-L1-P2桌面报告工单.md" \
         "2026-09-08-D594-L1-P2交互卡片链路.md" "2026-09-08-D595-L1-P3MCP认证权限.md" \
         "2026-09-08-D596-L1-P3TUI清理与文档拉平.md" "2026-09-08-D597-D588会话投影接线-M3第5次修复.md" \
         "2026-09-07-D587-stale重验批.md"; do
  printf "%-56s " "$f"; git show "origin/main:.claude/task-briefs/$f" | grep -c '认领: <agent>\|<本任务在哪一层'; done
# → 每份均 2（实测输出，7/7 命中）
# 文件本体（36 行 = alloc-task-id.sh 骨架模板长度，未回填）

# ② 台账未收录
grep -c "D59[0-2]" docs/synova/coordination/审计发现台账-DSH-CTO.md        # → 0
grep -c "D59[0-2]" docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md       # → 0
```

**⚠️ 附带发现（M2 型）：** `docs/synova/coordination/board-backlog.json` 的 `PLAN-k3-cond-clear` note 写「D590/D591=D547 骨架 brief **已清**+审计清单**已补**」——与上表实测**不符**（brief 未清、台账未补）。该 backlog note 本身是失真声明。

**未复跑项（如实声明）**：骨架 brief 当前是否仍会毒化 `check-plan-integrity`（group 6），本次**未复跑门禁验证**（复跑会写 `.claude/pre-commit-failures.log`，属"只取证不改"边界外）；台账 2026-09-13 行称 `#522` 锚点修复后带 D# 的提交不再受影响，本次仅记录该声称、未证实。

---

## 4. D591 — L1-P1 桌面端对话接线

### ① 条件原文摘录

同批共享（`2026-09-09-K3-D590-D591-closeout.md:16-18`，原文见 §3），D591 另有独立 P1：

task-state/D591 `audit.p1` 原文：
> P1-2 交叉引用: L2 fromState 缺陷存续（D592 K3 已确认；本批复核披露属实，不重复审）

### ② 未闭合项一句话

共享条件 ①（骨架 brief）与 ③（台账）仍开着（同 §3）；独立项 P1-2（L2 上下文缺陷）登记为"Stage 3 修复后应重验"，**至今无承接任务**。

### ③ 现状核实：仍开着

```bash
# P1-2 存续证据：D592 审计 PASS 但把该缺陷记为待跟进 P1-2
grep -n "P1-2（L2 已知缺陷存续" docs/synova/audit-reports/2026-09-08-K3-D592-closeout.md
# → :148 P1-2（L2 已知缺陷存续——重启后模型上下文重置）
grep -n "hist_m=1" docs/synova/audit-reports/2026-09-08-K3-D592-closeout.md | head -1
# → :119 模型上下文跨重启重置（hist_m=1）为 L2 已知缺陷——Stage 3 修复后应重验（P1-2）
# 是否已建承接任务 / 登记待办
grep -c "fromState" docs/synova/coordination/board-backlog.json     # → 0
grep -c "D59[0-2]" docs/synova/coordination/审计发现台账-DSH-CTO.md  # → 0
```

**判定：仍开着**（有审计记录、无承接任务、无台账条目）。

---

## 5. D593 — L1-P2 桌面报告工单

### ① 条件原文摘录

来源：`docs/synova/audit-reports/2026-09-10-K3-D593FIX2-closeout.md:23`（复审结论，K3，2026-09-10）

> 条件：① 基线文件 diagnosis.ts=2→4 收紧（或按铁律 39 正解改 DI 注入消跨层）；
> ② CTO 确认 PR #476 的 CI Architecture job 结论（本机无 gh，无法调取 job 级状态）。

### ② 未闭合项一句话

条件①的**记账侧已落地**（基线如实上调并登记收紧任务），但**根治侧（DI 注入重构）仍挂 backlog 未动**；条件②已由 CTO 于 2026-09-11 出物证确认，但 K3 未据此升 PASS。

### ③ 现状核实：部分闭合（仍开着）

```bash
# 条件① 记账侧：基线已 2→4 且写明"棘轮唯一一次逆向例外"+ 收紧任务
git show origin/main:tests/architecture/l1-cross-layer-baseline.txt | sed -n '18,24p'
# → "D593-FIX2 上调（2026-09-10, CTO 落地；K3 复审 CONDITIONAL PASS 条件①）: 36 → 38，
#    diagnosis.ts(L1→L5) 2 → 4。⚠️ 棘轮唯一一次逆向例外 ... 收紧路径 = PLAN-diagnosis-l5-di"
git show origin/main:tests/architecture/l1-cross-layer-baseline.txt | grep -n "^src/routes/diagnosis.ts="
# → 39:src/routes/diagnosis.ts=1（[L1→L3] 段）/ 62:src/routes/diagnosis.ts=4（[L1→L5] 段）

# 条件① 根治侧：DI 重构仍在 backlog（未开工）
grep -c "PLAN-diagnosis-l5-di" docs/synova/coordination/board-backlog.json   # → 1（note：owner=编码线；期限=下个 L1 质量批）

# 条件②：CTO 物证确认已入 task-state
git show origin/main:task-state/D593.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d['cto_confirmation'],ensure_ascii=False)[:300])"
# → confirmed_at=2026-09-11, confirmed_by=synova-cto；物证=GitHub API check-runs @ e53b5984
#   (Architecture Check=failure / Control Tower Gate Tests=failure / build=failure)
git log --oneline --all --grep="D705" | head -2
# → 1036e9a9 / a260e765 chore(D705): CTO 批次——D593 条件②物证确认 + 台账 3 条 CTO 发现 + 本地散落物入库

# verdict 现状：仍 CONDITIONAL PASS
git show origin/main:task-state/D593.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['status'], (d['audit'] or {}).get('verdict'), d.get('updated_at'), d.get('updated_by'))"
# → audited CONDITIONAL PASS 2026-09-11 synova-cto
```

**判定：条件①记账侧闭合 / 根治侧仍开着；条件②闭合但未触发升 PASS。** 该单属于"条件已满足却无人收口"的第二例（与 D518/D519 同型）。

---

## 6. D595 — L1-P3 MCP 认证权限

### ① 条件原文摘录

来源：`docs/synova/audit-reports/2026-09-10-K3-D593FIX-D595-closeout.md:213`（§8 修复路线 第 2 条）

> **D595 条件关闭**：evidence 7 件 + 补实 brief 补入库（或 CTO 正式豁免证据入库要求）；spec 载体恢复决策（重入库 or brief 补实版入库）；gitlink 清理随合并级 gate 一并治理（第二批 gitlink 清理任务：ct64/d590/d593/d595-cont/d602/.sessions + d577 等 7 个遗留）。

结论节对三条条件的表述（同报告 `:17`）：①写集漂移（evidence 7 件 + 补实 brief 未随 PR 入库）②spec 载体双缺失 ③gitlink 夹带。

### ② 未闭合项一句话

**①仍开着**（7 件 evidence 至今不在 main）；**②仍开着**（spec 与 brief 载体双缺，brief 仍为骨架模板）；**③已闭合**（main 已无 gitlink）。

### ③ 现状核实

```bash
# ① evidence 7 件（d595-*）在 main：0 命中
git ls-tree --name-only origin/main docs/synova/product-lines/evidence/ | grep -ci d595        # → 0
# （对照：该目录 main 上共 47 项，含 D592/§D712/D713/k3-* 等，但无 d595-*）

# ② spec 载体：0 命中
git ls-tree --name-only origin/main docs/plans/codex/implementation/ | grep -c D595           # → 0
# brief 载体仍是骨架模板（占位符 2 命中，见 §3 命令）

# ③ gitlink：已清（main 全树 0 个 commit 型条目）
git ls-tree -r origin/main | awk '$2=="commit"' | wc -l                                       # → 0
```

**判定：③闭合；①②仍开着。**

---

## 7. D602 — L1-P2 交互卡片呈现 + preload/IPC 收敛 + 通知 error 消费

### ① 条件原文摘录

来源：`docs/synova/audit-reports/2026-09-09-K3-D593-D602-closeout.md:167`（§3.5 结转与结论）

> 前批（2026-09-09 L1 收工批）给 CONDITIONAL PASS，P1-1 = spec 文件（SYNOVA-IMPL-DSH-D602-*.md）不在 main。**#453（26858bac）已登记**"D602 dev doc 确认不在 main（P1-1 记录，task-brief 即 spec 载体）"——P1 已登记但未关闭（spec 仍未入库，brief 95 行 + 编码指令 + evidence 充当契约载体）。

### ② 未闭合项一句话

spec 文件至今不在 main，"brief 即 spec 载体"这一替代口径**只有登记、没有获批**（无 CTO 豁免 / 无 K3 认可记录）。

### ③ 现状核实：仍开着

```bash
git ls-tree --name-only origin/main docs/plans/codex/implementation/ | grep -c D602   # → 0
git log -1 --format="%h %ad %s" --date=short --grep="26858bac" 2>/dev/null
git show 26858bac --format="%s%n%b" --no-patch | head -6
# → 登记语句在 #453 内，未见任何 CTO 豁免 / K3 认可该替代口径的记录
grep -rn "D602" docs/synova/coordination/board-backlog.json | head -3                  # → 无命中
```

**判定：仍开着**（登记 ≠ 关闭，与 D595 条件②同型）。

**注**：D602 的交付质量本身 K3 已判高（23/23 独立复跑全绿、写集合并零漂移），本条条件属**载体合规**问题，非功能缺陷。

---

## 8. D594 — 为何 closed 而无审计裁决？是否被并入他单？

### ① 条件原文摘录

无——该单**从未有审计裁决**（`task-state/D594.json` 的 `spec`/`impl`/`audit` 三段全为 `null`）。

### ② 未闭合项一句话

**不是"漏审"，是编号重分配后未承载交付的壳**：D594 这一编号被两个不同任务先后占用，"L1-P2 交互卡片链路"那一支的实体交付由 **D602** 承载并已获 K3 裁决（CONDITIONAL PASS）。

### ③ 现状核实：closed 属正确处置，交付由 D602 承接

**证据 1 — 本单是空壳**：

```bash
git show origin/main:task-state/D594.json
# → {"task_id":"D594","title":"L1-P2交互卡片链路","status":"closed","spec":null,"impl":null,"audit":null,
#    "note":"2026-09-10 看板校准: 被 D602 取代（L1 审计编号重分配，CT-63）——close"}
```

**证据 2 — 关闭动作来自看板校准批，与其它僵尸同批处置**：

```bash
git show 30011654 --format="%h %s%n%b" --no-patch | head -8
# → fix(ctl): 看板校准——待规划 12 条 + 僵尸关闭 6 个 (#469)
#   僵尸: D397/D398/D411/D558（零活动）+ D594/D596（被 D602/D603 取代）
git log --all --oneline -- task-state/D594.json
# → 30011654 2026-09-10（closed）/ 5a0e3f02 / 465b9147 2026-09-08（spec_done 期）
```

**证据 3 — 承载体 D602 标题自证"原审计编号 D594"**：

```bash
git show origin/main:task-state/D602.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['title'])"
# → D602 L1-P2 交互卡片呈现+preload/IPC收敛+通知error消费（原审计编号 D594）
```

**证据 4 — 编号被复用的根因是撞号**（同一 D594 另有一支"上下文压缩引擎"且已实现）：

```bash
git ls-tree -r --name-only origin/main | grep -F "D594" | grep -v "\.synova-wt\|\.sessions"
# → .claude/task-briefs/2026-09-07-D594-context-compaction.md
# → .claude/task-briefs/2026-09-08-D594-L1-P2交互卡片链路.md
# → .claude/task-briefs/2026-09-08-D594-context-compaction.md
# → docs/plans/codex/implementation/SYNOVA-IMPL-D594-llm-context-compaction-20260908.md
# → task-state/D594.json
git log --all --oneline --grep="D594" | head -4
# → 4fb65dc1 fix(CT-63): alloc-task-id 扫远端分支名 D#——Win 自编号在途可见
```

**结论**：
- D594（L1-P2）**无裁决是正常的**——它从未交付，是 `alloc-task-id` 分配的壳，因 D594 号已被"上下文压缩引擎"占用（CT-63 撞号），交付改挂 D602 并在 D602 之下完成审计。
- **是否并入他单**：是。并入 **D602**（同一交付：交互卡片链路 → D602 的"交互卡片呈现 + preload/IPC 收敛"），D602 已获 K3 CONDITIONAL PASS（报告 `2026-09-09-K3-D593-D602-closeout.md`）。
- **反查合格性**：D602 自身条件（spec 未入库）**仍开着**（见 §7）——即 D594 的"实质闭合"是成立的，但 D602 的载体条件没有跟着关。

---

## 9. D572 — FAIL 的具体项，是否已被后续任务整改

### ① 条件原文摘录（FAIL 裁决原文）

来源：`task-state/D572.json` 的 `audit.blockers`（K3，2026-09-04；报告 `docs/synova/audit-reports/2026-09-04-D572-line1-desktop.md`）

> P0: 验收点1-2 Windows 双击安装→出窗从未真机实测（D523 DS1/DS2 pending、D536 win_status waiting、yaml note 仍'从未实测'），verified 为自动兑换假绿——k3_gate 不放行
> P1: k3 verdict 无 stale 机制（D556 electron-renderer 大变更后证据未重验）；1-6 计时证据不可复核；D517-519 verdict 滞留 CONDITIONAL PASS

原单的整改归属（同 json `fix_task_note`）：

> P0 本体修复=D578(Win真机实测)；机制侧=D579(stale/TTL)+D576已close(CT-53)；P2 登记 CT-56/57/59

### ② 未闭合项一句话

三条都要分开看：**P1-stale 机制已闭合**（D579 PASS）、**P0 本体"部分整改"**（D578 PASS 但带一条未闭合条件：Win 复跑四断言 evidence 未入库；且 yaml `1-2` note 至今仍写"从未实测"）、**P1-verdict 滞留仍未闭合**。

### ③ 现状核实（逐条）

**P0 本体（Windows 真机实测）→ 整改单 D578：PASS，但条件未闭合**

```bash
git show origin/main:task-state/D578.json | python3 -c "import json,sys; d=json.load(sys.stdin); a=d['audit']; print(d['title']); print(a['verdict'], a['report']); print(a.get('conditions'))"
# → D578 FIX-D572-Win真机实测1-2
#   PASS docs/synova/audit-reports/2026-09-08-K3-win-backlog-11-closeout.md
#   ['1-2 四断言 evidence 待 Win 复跑入 git（docs/synova/product-lines/evidence/）——K3 标注状态，不兑换；复跑证据入 git 后 K3 复核']
# 该条件今天的物理状态：Win 复跑证据仍不在 main
git ls-tree -r --name-only origin/main docs/synova/product-lines/evidence/ | grep -ciE "win|578"
# → 2（D712-mac-20260913/1-3-window.txt、task-D523.json —— 均非 D578 Win 复跑四断言证据）
# 真相源未同步：yaml 线1 点1-2 note 仍写"从未实测"、status 仍 uncommitted
git show origin/main:docs/synova/product-lines/product-lines.yaml | sed -n '46,50p'
# → id: "1-2" / desc: Windows 双击安装… / status: uncommitted / note: "从未实测"
```

**P1 stale/TTL 机制 → 整改单 D579：已闭合**

```bash
git show origin/main:task-state/D579.json | python3 -c "import json,sys; d=json.load(sys.stdin); a=d['audit']; print(d['title'], '|', a['verdict'], '|', a['at'])"
# → D579 FIX-D572-k3verdict-stale机制 | PASS | 2026-09-06
```

**P1 verdict 滞留（D517-519 滞留 CONDITIONAL PASS）→ 未闭合**

```bash
for d in D517 D518 D519; do git show origin/main:task-state/$d.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['task_id'], (d.get('audit') or {}).get('verdict'))"; done
# → D517/D518/D519 全为 CONDITIONAL PASS（2026-08-29 后未变）
grep -o "CT-58[^|]\{0,120\}" docs/synova/coordination/审计发现台账-DSH-CTO.md | head -1
# → CT-58: 批次审计报告 D501-D550 型文件名派生不可见中间 D# → D517-519 verdict 滞留 CONDITIONAL，机制=范围解析或 task-state 显式 audit_rep
```
（注：CT-58 的**数据侧机制**已由 D579 落地到 `gen-cto-health.py:207-229`，但 D517-519 的裁决**语义**升 PASS 需要 K3 重核，机制不会替 K3 下结论——故仍开着。）

**P2（CT-56 1-6 计时证据 / CT-59 1-7 yaml 绑定语义）→ 台账有登记、无承接任务**

```bash
grep -o "CT-56[^|]\{0,140\}" docs/synova/coordination/审计发现台账-DSH-CTO.md | head -1
# → CT-56（1-6 计时证据不可复核——evidence JSON 未入 git+探针无指纹，指纹已折入 D578，摘要入 git 口径归 CT-57）
grep -o "CT-59[^|]\{0,140\}" docs/synova/coordination/审计发现台账-DSH-CTO.md | head -1
# → CT-59（1-7 yaml 证据绑定语义错配——绑定 scenario:GS-07 …）
```

**判定：部分整改。** P0 的机制面（防假绿自动兑换：CT-53/D576）与 stale 面（D579）已闭合；**P0 的证据面（Win 真机四断言 evidence 入库 + yaml note 更新）与 P1 的 verdict 滞留仍开着**。

---

## 10. 取证纪律声明

- 全程只读：仅使用 `git show` / `git ls-tree` / `grep` / `python3 -c`（读）/ GitHub API GET（read-only）。**未修改任何文件**（含 `scripts/audit/`、`generate-dashboard.py`、`pre-commit-check.sh` 均零触碰）。
- 未复跑项已在各节显式标注（骨架 brief 门禁毒化复跑、Mac/Win 实测本体复跑）。
- 一次 `python3 scripts/control-tower/gen-cto-health.py --dry-run`（只打印不写盘）在 D741 取证中执行，**crash 于 `gen-cto-health.py:305`**——该产物未落盘，未改动 CTO-HEALTH.md。

---

## 11. 建议派工（CTO 供裁，不自行立项）

| # | 事项 | 归属 | 优先级 |
|---|---|---|---|
| 1 | **CONDITIONAL 收口批**：D518/D519/D593 条件已满足 → 请 K3 按各报告"只核两项证据"口径升 PASS（一次派单可合并） | K3 审计线 | P1 |
| 2 | **D595/D602 载体补入库批**：evidence 7 件 + D595/D602 spec（或 CTO 正式豁免口径，二者择一，须落纸） | 编码/CTO | P1 |
| 3 | **7 份骨架 brief 清理 + 主树兜底扫描**（D590 P1-1 原始条件，第四次复发风险） | CTO（scripts/control-tower 域） | P1 |
| 4 | **台账补录 D590/D591/D592**（D590 ②/D591 ③ 原始条件） | CTO | P2 |
| 5 | **D572 P0 证据面收尾**：Win 四断言 evidence 入 git + yaml `1-2` note/状态同步（K3 复核后转绿） | Win 线 + CTO | P1 |
| 6 | **L2 上下文缺陷（D591 P1-2 / D592 P1-2）承接任务立项**（Stage 3 修复重验） | 编码线 | P2 |
| 7 | backlog `PLAN-k3-cond-clear` note 失真订正（声称"已清/已补"与实测不符） | CTO | P2 |

---

**取证产出**: 本报告 + `D741` 报告 `docs/synova/coordination/真相源写入链路缺口-20260913.md`
**基线锚点**: `origin/main @ 3f0a43b5`
