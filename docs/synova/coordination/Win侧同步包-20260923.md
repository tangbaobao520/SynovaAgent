# Win 侧同步包（2026-09-23）— 控制塔 / 预设 / 技能 / 必读文档

依据计划: v1.2@4e46603f

> 用途：Win 机器上线后，**一次性把两侧对齐**。本件只做"信息与工具同步"，**不涉及分工细则**（分工下轮定）。
> 判定依据全部为 CTO 实测（命令 + 输出），非转述。

## 一、Win 侧现状判定：**落后 main 约 9 天，控制塔需整体更新**

| 项 | 实测 |
|---|---|
| Win 机器身份最后提交 | `6cfc7a9a`（2026-09-14，作者 `Synova-Win`） |
| 之后 Win 线活动 | 全部为 **Mac 代行**（作者 `synova-cto`，如 `9f67007b`/`816a117a`/`3c8cf14c`）→ Win 本机未参与 |
| 自该锚点以来的变更量 | `scripts/` **34 个提交**、`.claude/skills/` **6**、`.dsh/skills/` **6**、`docs/synova/coordination/` **93**、`docs/synova/presets/` **1** |

**⇒ 结论：需要更新，且是整体更新**（不是补几个文件）。Win 上线第一件事：`git fetch --all && git pull --ff-only` → 到最新 main，再跑第三节的自检。

## 二、缺口（CTO 实测，**必须在 Win 同步前修掉，否则同步会静默失败**）

| # | 缺口 | 证据 | 影响 | 修法 |
|---|---|---|---|---|
| G1 | `scripts/control-tower/install-dsh-preset.sh:90-92` 的 standard 源探测为 **Mac/nvm 专属**（`$DSH_INSTALL_DIR` / `npm root -g` / `$HOME/.nvm/current/...`） | Mac 上实测即 `degraded: standard 预设源未找到`（`~/.nvm/current` 不存在） | Win 上必然失败 → 预设落不了位（M3 型：以为同步了，实际没装） | 探测扫多候选：`DSH_INSTALL_DIR` → `npm root -g` → `~/.nvm/versions/node/*/…` → `%APPDATA%\\nvm\\…` → `%LOCALAPPDATA%\\…`；失败时**列出所有尝试过的候选路径** |
| G2 | `docs/synova/presets/install-squad-lead.sh:4` 硬编码 `REPO="${1:-/Users/wane/SynovaAgent}"` | 默认值 Mac 专属 | Win 上传参才能用；不传参即写错位置 | 默认改 `$(git rev-parse --show-toplevel 2>/dev/null || pwd)` |
| **G3** | **`synova-cto` 与 `synova-k3-audit` 预设未入仓** | `docs/synova/presets/` 下**只有** `synova-squad-lead/`；注册表只有 `synova-dsh` | **Win 侧要跑"K3 在独立仓库审计"就必须有 k3-audit 预设** → 现在无法用脚本落位 | 把 `synova-cto` / `synova-k3-audit` 的 `preset.yml` + `persona-block.yml` 入仓并注册（下轮随分工一起做） |
| G4 | 无**跨机预设/技能一致性检查** | 只有 `sync-dsh-skills.sh`（仓内 → `.dsh`）与 `install-dsh-preset.sh --check`（本机 vs 仓），**没有"Mac vs Win 指纹对账"** | 两侧漂移无人发现 | 加一条：导出"技能+预设指纹"（文件 sha256 清单）提交到 `docs/synova/parity/<machine>.json`，CI 或周检比对 |

## 三、Win 侧同步操作（可复制执行）

```bash
# 0) 前提：DSH 已装、gh/ssh 凭据可用（凭据见 ~/.dsh/.credentials.yaml，不入仓）
cd <Win 仓库路径> && git fetch --all && git status -sb     # 必须在 main 且与 origin/main 一致
git pull --ff-only

# 1) 技能同步（仓内单源 → .dsh）
bash scripts/workflow/sync-dsh-skills.sh --check   # 期望 SYNC-OK（当前应为 16 个技能）
bash scripts/workflow/sync-dsh-skills.sh           # 需要同步时执行

# 2) 预设落位（G1 修好后可自动探测；修复前用 --standard-from 显式给路径）
bash scripts/control-tower/install-dsh-preset.sh --install --standard-from "<DSH standard 预设目录>"
bash scripts/control-tower/install-dsh-preset.sh --check   --standard-from "<同上>"   # 期望 SYNC-OK

# 3) 小队队长预设
bash docs/synova/presets/install-squad-lead.sh "$(pwd)"

# 4) 门禁自检（必须全绿才开工）
bash scripts/pre-commit-check.sh
bash scripts/workflow/sync-dsh-skills.sh --check
```

## 四、Win 侧必读（按优先级，全部在 main 可读）

**第一优先（流程与形态，必须读）**
1. `docs/synova/coordination/TASK-ROUTING.md` — **含 2026-09-23 修订块**（devdoc 预设退役技能化 / 执行形态=小队 / K3 承重墙 / 长任务形状）
2. `docs/synova/coordination/MULTI-MACHINE-PR-WORKFLOW.md` — 多机 PR 纪律（一人一事一分支、合并走 PR、禁 behind 开工）
3. `docs/synova/coordination/小队模式-固化件-v1-20260919.md` — 小队编制与上限（≤4、自验独立、重型验证串行、PR ≤12）
4. `docs/synova/coordination/裁定-双DSH提升清单-20260922.md` — 10 条提升分档 + 失效条件（P0：K3 必经门禁 / Win 环境标准化）

**第二优先（新机制与门禁）**
5. `docs/synova/coordination/派单-W1-双DSH提升-20260923.md` — 程序卡 + 3 Wave + W1 三项规格（D923/D924/D925）
6. `docs/synova/coordination/派单-K3审计-D918D922D926-20260923.md` — K3 审计口径（12 必答题 + "有条件通过=未通过"）
7. `docs/synova/coordination/审计发现台账-DSH-CTO.md` — §五 最新两行（2026-09-23 两批共 12 项发现，含跨机账本缺口 / 时点漂移口径）
8. `scripts/control-tower/PLATFORM-CHECKLIST.md` — **Win 侧改脚本前必读**（8 条跨平台清单）

**第三优先（技能载体）**
9. `.claude/skills/squad-discipline/SKILL.md` — 队内行为纪律（门禁之外那 7 类）
10. `.claude/skills/dev-doc-spec/SKILL.md` — 触发式独立规格（跨层/新产品线/超预算）
11. `.claude/skills/cto-handover/SKILL.md`、`.claude/skills/ctrl-tower-change/SKILL.md`、`.claude/skills/windows-compat/SKILL.md`

## 五、待同步的流程变更（**本轮只登记，分工下轮定**）

Win 侧新形态（创始人 2026-09-23 定）：**Codex 做 CTO 任务 / DSH 小队执行编码 / K3 在独立仓库审计**。
需要改的文档（下轮随分工一起交付，本轮不动，避免两侧口径半途）：
- `TASK-ROUTING.md`：增补 Win 侧新形态（现为 Mac/Win 双轨旧口径）
- `docs/synova/coordination/dsh-division-draft/DIVISION-CHARTER-v4.md`：对应更新
- `MULTI-MACHINE-PR-WORKFLOW.md`：增补"多身份并存"（Mac-CTO / Win-Codex-CTO / Win-DSH小队 / K3 独立仓）
- K3 独立仓库的路径与凭据约定（Mac 侧现为 `/Users/wane/Synova-k3独立审计`；Win 侧需等价独立仓）

## 六、红线（跨机同样适用）

不碰 `scripts/audit/**`；禁 `--no-verify` / `git stash` / force push 共享分支；改 `scripts/**` 前先读 `PLATFORM-CHECKLIST.md`；跨机提交必须走 PR + 各自机器身份（**保留 `Synova-Win` 身份，不匿名** —— 质量归因与事故定位依赖它）。

## 引用豁免

- docs/synova/coordination/派单-K3审计-D918D922D926-20260923.md — 该派单件在 **PR #722** 内（本件撰写时尚未合并 main）；#722 合并后即在 main 可读，Win 侧按 main 路径取。

## 七、v2 增补（2026-09-23 晚 · 复核 D927 v2.1 / D931 后补）

> v1 成稿时 DSH 借鉴清单还在旧断面（D927 v1 已作废并删除文件）。本节是 **Win 侧必须一并吸收的 5 类新信息**。

### 7.1 断面判据（**只此一条**，避免再次搞错对比对象）

| 轨 | 变动 | 断面 | 是否判据 |
|---|---|---|---|
| **DSH 内核** | 0.1.7-alpha.1 → **0.1.7-alpha.2** | `/Users/wane/src/deepseek-harness-017` @ `00102833`（Merge PR #4978）；根/apps-desktop/apps-web 三处 package.json 均 0.1.7-alpha.2 | ✅ **唯一判据** |
| DeepSeek Harness **Desktop**（第三方封装） | 0.1.7-alpha.1 → 0.1.6-alpha.2（**回滚**） | `.store.dat.before-kernel-rollback` | ❌ **不作判据** |

⚠️ 借鉴/对比类工作，**必须写清"测量对象 + 绝对路径"**。上一轮两侧都在这上面栽过。

### 7.2 新增纪律（4 条，跨机同样适用）

1. **负面断言必须写明搜索范围**（"零命中"= 在哪个范围内零命中）
2. **判断面必须用 `git ls-files`，grep 必须限定 `src/`**（源码树 `lib/` 被 `.gitignore` 忽略：`git ls-files '*/lib/index.js'` = 0，但磁盘上存在 307 个 → **工作树干净 ≠ 没有产物**）
3. **计数必须带命令 + 排除项**（同一对象三个数就是三个口径：`*/lib/index.js`=307 / 全部 index.js 排除 node_modules=711 / 未排除 types=710）
4. **路径必须写全两层**：DSH 源码树是 `packages/<组>/<包>/…`（如 `packages/mcp/mcp-client/src/transport.ts`、`packages/settings/settings/src/index.ts`），**不是** `packages/<包>/src/…`

### 7.3 队长预设必须带 Agent Teams（D931 修复）

- 根因：原预设挂的是 legacy `delegation`（子代理），**Agent Teams 三件套没挂** → 队长物理上只会开子代理，小队变摆设。
- 修复（`docs/synova/presets/install-squad-lead.sh`）：`delegation` → `agent-team` + `tool-agent-team` + `ui-agent-team`，**`maxMembers: 4` 把"成员≤4"物理化**。
- **Win 侧落位后必须 grep 自证**：
  ```bash
  grep -cE "^- id: (agent-team|tool-agent-team|ui-agent-team)" ~/.dsh/.agent-presets/synova-squad-lead/agent.cordis.yml   # 期望 3
  grep -c "^- id: delegation" ~/.dsh/.agent-presets/synova-squad-lead/agent.cordis.yml                                      # 期望 0
  grep -n "maxMembers" ~/.dsh/.agent-presets/synova-squad-lead/agent.cordis.yml                                            # 期望 4
  ```
- ⚠️ **预设是 session 启动时加载**：落位后**必须重开 session** 才生效。
- ⚠️ 该修复在 **PR #727**（`fix/d931-squad-lead-team`）；若 Win 拉平的 main 里 `install-squad-lead.sh` 无 `agent-team` 字样，说明 #727 未合并 → 先按 7.3 手动补，或等合并。

### 7.4 派单必带「执行形态」硬字段（防"小队是摆设"复发）

DSH 的策略是"**未被明确要求就不创建队友**"→ 派单**不写执行形态 = 执行方走子代理**。派单必须写：
组队（Agent Teams）/ 成员编制（队长 + ≤2 编码 + 1 独立自验）/ 队长不下场写码 / 共享任务含写集+验收+写者 / 重型验证串行 ≤1 / **回执必须含团队成员运行记录**（成员名·状态·token·共享任务 id）；**只有队长在跑 = 退回**（M3 型）。

### 7.5 借鉴清单修正（D927 v2.1 后，Win 侧若涉及借鉴卡须按此）

- 三档重出：**仍成立 7 / 需改法 12 / 已无必要 1**
- **B-09 下线**（上游自己声明 `preset.yml + agent.cordis.yml` 目录范式已作废："Nothing reads that directory any more"；保留 `mountPreset:254`、`leakedServices:85` 两项活件）
- **B-11 重定义**（我方已有 MCP **server** ≠ 卡面要的 **client**；transport 不借——上游自己 import 官方 `@modelcontextprotocol/client`）
- **B-12 改法待你裁定**（自研 `crypto` ≈20 行 vs 引 `@octokit/webhooks`——依赖政策归创始人）
- **B-18 改判"需改法"**（机制完整但只 `sdk-minimal` 挂载 → 照抄机制不照抄装配 = 抄一个不会运行的检查）
- **B-10 锚点更新**（`mergeLayers` 活 / `patchNode` 死 / `settings-file` 包消失 → 改 `boot/config-editor` profile patch）
- 报告位置：`docs/synova/research/D927-DSH借鉴重新对比审计-20260923/`（PR 待合并）

### 7.6 W1 进度快照（供 Win 侧了解当前形态）

| 卡 | 状态 |
|---|---|
| D922 总计划 | 已交付并合并（`9631098c`） |
| D923 前置证据 | 已交付（平台假冲突，待 re-merge） |
| D924 子进程协议 | 已交付，走 K3 |
| D925 deps 接口标准 | 已交付（栈在 D924 上），走 K3 |
| D926/D927/D928/D930/D931 | 纪律技能化 / DSH 重锚审计 / 第③面生成器修复 / 本同步包 / Agent Teams 接线 |

### 7.7 一条跨机红线（刚发生的事故）

**主工作区只做同步与协调，任务一律在 `.synova-wt-*` 内。**
事故：D927 分支曾被 checkout 在主工作区 → CTO 侧 `--ff-only` 同步静默失败（落后 22 提交，"承重墙"一度失效）。Win 侧同理。

## 引用豁免

- docs/synova/research/D927-DSH借鉴重新对比审计-20260923/README.md — D927 v2.1 报告目录在 PR 内（未合并 main）；Win 侧按该 PR 分支或合并后取。
