# 决策记录：U5a 5 分钟绕过窗口（创始人拍板 2026-08-17）

> 决策人：创始人 | 日期：2026-08-17 | 状态：已采纳

## 决策
**选 A：接受 5 分钟窗口**。U5a 并发/amend 误报根治采用"三判 + 新鲜度 >300s"方案，接受
"合法提交后 5 分钟内的 --no-verify 绕过不可见"这一权衡。

## 背景（一句话）
并发/amend 会误判成绕过导致全线死锁（CT-29），修复用"新鲜度检查"兜底，代价是 5 分钟盲区。

## 为何可接受（三道兜底）
1. synova-commit 无 --no-verify 开关，绕过需刻意用原始 git commit。
2. U1 对账门禁在推送时核对 git 提交 vs bypass.log，缺记录的提交（含 --no-verify）会被点名拦下。
3. 故 5 分钟窗口 = "延迟发现"，非"永久漏掉"。

## 给 K3 的审计锚点
- 本权衡是创始人明示拍板，非漏网 bug。
- 若将来 K3 发现"5 分钟窗口内的真绕过"造成实际事故，可回本决策重议（方案 B 缩窗口 / 方案 C 每 session 独立记录）。
