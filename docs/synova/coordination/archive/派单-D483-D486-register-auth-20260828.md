# 派单：register 认证闭环实现（D483 → D484 → D486 串行）

> 派单: CTO | 2026-08-28 | 认领: 🛠 编码 session（按 spec 实现）→ K3 → CTO 合并
> 方法论: 看板大扫除后的产品任务优先级——spec 停 spec_done 自 08-24 未转实现（治理任务挤压产品的活样本）
> 流程: spec 已就绪（dev-doc 交付）→ 编码按 spec 实现（串行三切片）→ K3 审计 → CTO 合并
> 上一轮教训: D540 CI 红（本地绿≠CI 绿，交付贴 check-runs）；spec 停 spec_done 不转实现 = 产品阻塞

---

## 写前核实（强制清单 — §〇c ①）

- [x] ① 任务来源/依赖: 产品任务（L23 权限治理）。三切片串行：D483（匿名注册可达）→ D484（企业邀请链路，共享 auth.ts isWhitelisted 禁止并行）→ D486（E2E 验证，依赖 A/B 已合并）。上游 D481（auth 契约对齐）已合并。
- [x] ② task-state 最新状态: D483/D484/D486 均 spec_done（spec 已入 main，路径已核实）。
- [x] ③ 基线资产实际存在（物理确认）:
  - spec ×3 在 main：`docs/plans/codex/implementation/SYNOVA-IMPL-D483-register-auth-slice-a-20260824.md` / `...-D484-enterprise-invite-slice-b-20260825.md` / `...-D486-register-e2e-slice-20260825.md`（各含 file:line 代码审计 + 缺陷清单）
  - `src/middleware/auth.ts`（L83-99 白名单现状）、`src/routes/enterprise.ts`、`tests/middleware/auth.test.ts`
- [x] ④ DSH 借鉴核查: 见下「DSH 借鉴核查」章节。
- [x] ⑤ 写集重叠检查: 与在途 D548（product-lines/）零交集；与 Win 线（D355-D360/D487）零交集；三切片内部串行（auth.ts 共享）。
- [x] ⑥ 上一轮教训: spec 停 spec_done 未转实现（本单即纠正）；串行依赖禁并行（D484 spec 明示写集重叠）。

## DSH 借鉴核查（强制章节 — §〇b 三步）

1. **施工图四色归属**: `src/middleware/`（auth/rbac）——施工图 §3 混合模块表明确「auth/rbac 属"企业多用户"【双缺】机会 → 偏 🟢 做深」。
2. **借鉴边界判定**: **无 DSH 借鉴**——register 认证闭环是企业领域能力（邀请令牌/企业隔离），非通用管道（DSH 无对应范式）。显式写「无 DSH 借鉴：认证闭环是企业多用户领域能力（🟢 做深），非通用管道」。
3. **DSH 源码参考**: 无。

> 红线：不引 DSH 代码；验收含接线审计（铁律 0-2）。

---

## 切片定义（CTO 已定，按 spec 依赖链串行）

| 切片 | 用户可见价值 | 验证锚点 | 依赖 |
|---|---|---|---|
| D483 切片 A | 匿名注册可达（register 不被白名单 401 挡） | spec 缺陷 A/B 修复 + 测试绿 | 无（上游 D481 已合） |
| D484 切片 B | 企业邀请注册链路打通（邀请→接受→登录） | spec 缺口 A 修复 + 测试绿 | **串行 D483**（共享 isWhitelisted） |
| D486 E2E | 真实 server 全链路验证（注册→邀请→接受→登录→首诊） | 本地真实 server 跑绿 + 证据报告 | D483/D484 已合并 |

## 现状材料（编码必读，spec 是执行唯一依据）

| 资产 | 位置 | 状态 |
|---|---|---|
| spec A | docs/plans/codex/implementation/SYNOVA-IMPL-D483-register-auth-slice-a-20260824.md | dev-doc 已交付（缺陷 A：register 不在白名单 401；缺陷 B：测试 bootstrap 绕过） |
| spec B | .../SYNOVA-IMPL-D484-enterprise-invite-slice-b-20260825.md | 缺口 A（enterprise 匿名端点被挡）+ B（内存存储，记录遗留）+ C（测试真空） |
| spec E2E | .../SYNOVA-IMPL-D486-register-e2e-slice-20260825.md | 缺口 A（无 E2E 覆盖）+ B（双轨链路未验证）；CI 排除 tests/e2e/**（验收=本地真实 server 跑绿） |

## 执行要求

1. **串行纪律**: D483 完成合并后 → D484 → D486。禁止三切片并行（auth.ts isWhitelisted 共享，spec 明示）。
2. **按 spec 实现**: spec 的 file:line 代码审计 + 缺陷清单是唯一依据，不发明新范围（spec 已过 dev-doc 质量门禁）。
3. **每切片验收**: 从 spec 引用验收标准 + CI check-runs（D540 教训：本地绿≠CI 绿）。
4. **D486 特别**: tests/e2e 不进 CI（spec 明示）——验收 = 本地真实 server 跑绿 + 交付报告证据（D480 先例）。

## 写集约束

- **可碰**: `src/middleware/auth.ts`、`src/routes/enterprise.ts`、`tests/middleware/`、`tests/routes/`、`tests/e2e/auth-register-flow.e2e.test.ts`（D486 新建）。
- **不碰**: `scripts/audit/`（K3 红线）、`scripts/`（控制塔，DSH 领地）、`electron-renderer/`（D544 已收口）、`src/sentinel/`（D546 已收口）。
- **防膨胀**: 按 spec 缺陷修复，不新增机制；D484 缺口 B（内存存储）按 spec 记录遗留，不实现持久化（scope 控制）。

## 切片级审计

- 三切片完成 → K3 批次审计（报告覆盖 D483/D484/D486）。
- task-state 每切片回填（impl.commit/status）。
- 审计验收 = 每切片 spec 缺陷清单逐条闭环 + CI 绿 + 接线审计。

## 给编码 session 的交付要求

1. 每切片独立提交 + push + CI 绿（check-runs 贴报告）。
2. 交付报告逐条对照 spec 缺陷清单（file:line + 测试名），禁止空泛声称（M2）。
3. D486 的 E2E 证据：本地真实 server 跑绿输出 + 报告路径（tests/e2e 不进 CI 的既定先例）。
4. 三切片全部完成后提 K3 批次审计。

---

## 派单说明（给创始人复制——自包含，零查找）

```
【派单】register 认证闭环实现（D483 → D484 → D486 串行）
> 认领: 🛠 编码 session | 产品任务（L23 权限治理），spec 已就绪直接实现

## 背景（一句话）
三个切片的 spec 停在 spec_done 自 08-24（被治理任务挤压）——本单转实现：
匿名注册可达 → 企业邀请链路 → E2E 验证，串行依赖（auth.ts 共享禁并行）。

## spec（执行唯一依据，均已入 main）
- D483 切片A: docs/plans/codex/implementation/SYNOVA-IMPL-D483-register-auth-slice-a-20260824.md
  （缺陷：register 不在白名单 401 + 测试 bootstrap 绕过）
- D484 切片B: .../SYNOVA-IMPL-D484-enterprise-invite-slice-b-20260825.md
  （缺口：enterprise 匿名端点被挡 + 内存存储记遗留 + 测试真空）
- D486 E2E: .../SYNOVA-IMPL-D486-register-e2e-slice-20260825.md
  （缺口：无 E2E 覆盖 + 双轨链路未验证；tests/e2e 不进 CI，本地真实 server 跑绿验收）

## 执行要求
1. 串行纪律: D483 合并后 → D484 → D486（auth.ts isWhitelisted 共享，spec 明示禁并行）
2. 按 spec 缺陷清单实现，不发明新范围
3. 每切片 CI check-runs 贴报告（本地绿不算）
4. D486 E2E: 本地真实 server 跑绿 + 证据报告（D480 先例）

## 写集约束
- 可碰: src/middleware/auth.ts、src/routes/enterprise.ts、tests/middleware/、tests/routes/、tests/e2e/auth-register-flow.e2e.test.ts
- 不碰: scripts/audit/、scripts/、electron-renderer/、src/sentinel/
- 防膨胀: 按缺陷修复不新增机制；内存存储记遗留不实现持久化

## 交付要求
1. 每切片独立提交 + CI 绿
2. 交付报告逐条对照 spec 缺陷清单（file:line + 测试名，M2 禁空泛）
3. 三切片完成后提 K3 批次审计
```
