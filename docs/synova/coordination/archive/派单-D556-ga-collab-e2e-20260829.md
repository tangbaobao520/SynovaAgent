# 派单：GA 人机协同端到端（D556）

> 派单: CTO | 2026-08-29 | 认领: 📋 dev-doc（先出 spec）
> 方法论: 创始人批准切片方向 A（2026-08-28 全面评估：GA 人机协同端到端）——D551 后端已合 main，前端三项仍置灰 = 已付成本零用户价值，本切片闭环
> 流程: dev-doc 出 spec → 编码按依赖推进 → K3 切片级审计 → CTO 合并
> 上一轮教训: ① D538/D544 派单接口路径核实不足（/api/signals 实为 /api/sentinel/signals，P2）——本单接口路径全部 grep 核实；② D546 派单位置误标（durationMs 真凶 sentinel-service.ts:97）——本单基线行号全部 `git show origin/main:` 实测；③ D551 spec 写集表目录粒度被 verify-parallel CI 拦——本单写集表必须文件级（D540 先例）

---

## 写前核实（§〇c ①，6 项——已全部物理执行，证据如下）

- [x] ① 任务来源/依赖: 创始人批准方向 A（全面评估结论）；上游 D551 后端已合并 main（6e9626f9 起含）——`git merge-base --is-ancestor origin/main feat 验证过` ✅
- [x] ② task-state 最新状态: D551 = impl_done + slice=ga-module-3（origin/main 权威）；D556 新分配（alloc-task-id，占用表含 origin/main 合并）
- [x] ③ 基线资产实际存在（下述现状材料表，全部 `git show origin/main:` 实测行号）
- [x] ④ DSH 借鉴核查: 三步完整（见下节）
- [x] ⑤ 写集重叠检查: 在途 D483（auth 切片 A）= src/middleware/auth.ts + tests/middleware/*，与 electron-renderer/src/loops **零交集**；D548（l01 spec）= 文档任务零交集；D556 未占用（ls-tree origin/main task-state/ = 0 命中）
- [x] ⑥ 上一轮教训: 见头部「上一轮教训」三条，已化为本单验收方式

## DSH 借鉴核查（§〇b 三步，强制章节）

1. **施工图四色归属**（施工图 L72/74/77/87 + §4 借鉴清单第 7 行）：
   - `electron-renderer/`（前端）= **🟢 死守**——品牌表层（L77：品牌活在这一层）
   - `src/loops/`（middle-evolution-engine）= **🟢 死守**——领域循环调度，业务方法论不是通用 cron（L74）
   - `src/growth/feedback-collector` = 核心服务（只读消费，本单不改）
2. **借鉴边界判定**：**有 DSH 理念借鉴（长期借鉴，不引代码）**——
   - 前端：反馈动作 UI 范式——DSH 消息级反馈动作条（rate/toggle/note 三态动作），GA 校准面板四动作（标记错误/补背景/重写逻辑/降级标记）是其领域化实例；T-M-09「DSH 式输入框」同源借鉴
   - 层 2：**feedback log-only 纪律**——施工图 §4 第 7 行「反馈闭环 | feedback（log-only 不进模型）| Stage 1 借鉴（补 N13 断裂）」：回流只落记录 + 显式信号聚合生效，不直接注入模型上下文（正是 N13 断裂修复范式）
3. **DSH 源码参考**（读范式自研，禁止 copy / npm install）：
   - `~/.nvm/versions/node/v24.19.0/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-client-ui-message-feedback/lib/types/client/MessageFeedbackActions.d.ts` L20 —— MessageFeedbackActions 组件契约（messageId/ensure/rate/toggle/clearNote/useFeedback）
   - `.../dsh-client-ui-message-feedback/lib/client.js` L383 —— 反馈动作条实现（UI 层动作 → 记录，log-only 不触发模型上下文注入）

> 红线（R1/R3/R4）：读范式自研；不 copy 代码；不复制 OpenViking；验收含接线审计。

## 切片定义（CTO 已定，dev-doc 复核）

| 切片 | 用户可见价值 | 验证点 | 依赖 |
|---|---|---|---|
| **GA 人机协同端到端（本次）** | GA 用户在桌面端面板上校准诊断结论、注入手动信号、看到反馈效用仪表；校准回流产生真实进化动作 | L16 目标导航与行动闭环 + L17 进化闭环（点编号 dev-doc 对照 product-progress.json 精确填写） | D551 后端（✅ 已合） |
| 层 3（后续，本单不做） | 背景卡自动加载/权重自动更新/ManualSignal 本体节点/定向触发重评估/GA AI 副驾 | 后续切片 | 本切片 |

## 现状材料（dev-doc 必读，全部在 main，行号已物理实测）

| 资产 | 位置 | 状态（实测） | 与本单的关系 |
|---|---|---|---|
| GaDetail 占位组件 | `electron-renderer/src/components/RightPanel.tsx` L632-647 | 三块占位（诊断校准面板/手动信号注入/反馈效用仪表）+ L635 banner「⚠ 后端校准接口待接入」，**无 fetch** | 前端接线目标 |
| GA 权限门控 | `electron-renderer/src/stores/capability.ts` L15/21/43-44 | `canAccessCap: cap==='ga' → role==='ga'`（fail-closed） | 前端渲染门槛 |
| 校准端点族 | `src/routes/ga-calibration.ts` L121/239/314/419 | POST/GET /api/ga/calibration + POST /signals + GET /stats，各持 requireGa（src/routes/ga-auth.ts） | 前端数据源（只读消费，零回改） |
| 进化引擎信号 | `src/loops/middle-evolution-engine.ts` L77-79/92-93/105-107/119 | Signal 1 reject+sentinel_alert / 2 modify+goal / 3 reject_path / 4 ineffective+expert×breakpoint——**无 diagnosis_conclusion 分支**（层 2 缺口，本单修） | 层 2 扩展目标 |
| 回流通道 | `src/growth/feedback-collector.ts` L123/148（CHECK 已扩 diagnosis_conclusion）+ L281-305 getAggregatedSignals（无 targetType 白名单） | D551 已落地，回流数据已可达 | 层 2 数据源（只读） |
| 进化接线 | `src/agent/loop-handlers.ts` L363-377 defaultEvolutionHandler → getAggregatedSignals → processFeedbackSignals | 已真实化（D333） | 层 2 消费者（只读） |
| 前端角色来源 | `electron-renderer/src/stores/app-store.ts`（role 字段） | dev-doc 核实 GA 角色当前来源 + seed 方案 | GA 面板验收身份 |

## D556：GA 人机协同端到端（验证点 L16/L17 精确编号 dev-doc 填）

**目标**：GA 用户打开桌面端 → 左栏 GA 能力（有 role 门控）→ 面板三块发真实 fetch 到 D551 四端点 → 校准/注入/仪表数据真实可见；同一切片内：校准回流（diagnosis_conclusion × reject/modify/ineffective）经进化引擎产生真实进化动作（层 2）。

**依赖**：D551（✅）；GA 身份不依赖在途 D483-D486（seed 解耦，见必答题 3）。

**spec 必答题**：
1. **前端接线设计**：GaDetail 三块各调哪些端点（请求/响应契约引用 ga-calibration.ts file:line）？role≠ga 时的降级渲染（复用 L635 banner 还是升级为按 canAccessCap 显隐）？错误/降级路径（铁律 24/31：503 degraded 时 UI 显示什么）？
2. **层 2 动作语义**：diagnosis_conclusion × {reject, modify, ineffective} 各应触发什么进化动作？聚合阈值（沿用现有 ≥3 惯例？）？动作落点（applyEvolutionActions 现有动作类型够不够，还是要新动作类型）？**诚实边界**：采纳率不可得（沿用 D551「回流计数≠采纳率」声明），层 3 不做（显式 descope）。
3. **GA 角色来源与验收身份**：app-store 的 role 现状（file:line）？验收用 seed GA 用户/测试注入（不与在途 D483-D486 耦合）？验收命令给出可复现步骤。
4. **写集表文件级**（D551 教训：目录粒度被 verify-parallel CI 拦）+ DS 声明一一对应（S-10）。

**验收**（物理可复现，禁止静态 grep 冒充）：
- 前端：seed GA 身份 → 面板可见三块 → 发 POST 校准 → 列表回显 → 注入信号 → 仪表计数变化（curl 对照 + UI 断言，计时）——evidence 落盘
- 层 2：写入 3 条 diagnosis_conclusion 回流 → 触发进化引擎 → 断言动作生成（applyEvolutionActions 被调用/动作落库）
- 存量零回改：`git diff origin/main...HEAD -- src/routes/` = 空（三存量路由不动）

## 写集约束

- **可碰**：`electron-renderer/src/components/RightPanel.tsx`、`electron-renderer/src/stores/`（仅 GA 相关）、`src/loops/middle-evolution-engine.ts`、`tests/`（对应测试）；spec 文件
- **不碰**：`src/routes/ga-calibration.ts`、`src/routes/ga-auth.ts`（D551 已合，只读消费）、`src/growth/feedback-collector.ts`（只读）、`scripts/**`（治理冻结）、`scripts/audit/**`（K3 红线）、`src/middleware/**`（D483 在途）
- 与 D483-D486（auth）**零交集已核实**；仍要求 worktree 隔离开工

## 切片级审计

- 本切片 1 个 D（D556）：实现完成后一次提审 K3，报告覆盖 D551 复审项（前端接线 + 层 2）+ D556 初审
- task-state 加 `"slice": "ga-collab-e2e"`
- 审计验收 = L16/L17 验证点从 uncommitted → verified

## 给 dev-doc 的交付要求

1. spec 命名 `SYNOVA-IMPL-DSH-D556-ga-collab-e2e-<YYYYMMDD>.md`，写集表文件级 + §8 接线验收（铁律 0-2 WIRE CHECK）
2. 验收物理可复现（命令 + 断言 + 预期输出；计时/evidence 落盘）
3. 诚实声明：层 3 descope + 采纳率不可得 + 前置依赖声明（D551 已合 / GA 身份 seed 方案）

---

## 写后自检（§〇c ③，8 项）

- [x] ① 验证点编号: L16/L17 精确点号交 dev-doc 对照 product-progress.json（CTO 已给线号）
- [x] ② D# 未占用: alloc-task-id 分配 D556（占用表含 origin/main 合并，D550 修复后无撞号）
- [x] ③ 依赖链: D551（已合）→ D556；前端接线与层 2 同切片但可分段实现；层 3 显式排除
- [x] ④ DSH 借鉴核查三步完整: 四色/边界/源码 file:line 三处引用（见上节）
- [x] ⑤ 现状材料全部核实过: 7 行资产全部 `git show origin/main:` grep 实测（行号见表格）
- [x] ⑥ 验收物理可复现: 前端 fetch 断言 + 层 2 动作断言 + 存量零回改 diff 断言
- [x] ⑦ 术语一致: L1 桌面端（electron-renderer）vs Win 线 AUTH 切片零混淆；slice 名 ga-collab-e2e 唯一
- [x] ⑧ 无遗漏: LLM 环境（DeepSeek，前端零 LLM）/执行方（dev-doc→编码）/交付要求/审计验收项全列

## ③' 交付前物理复核（创始人定，勾 checklist ≠ 复核——命令输出即证据）

1. **基线引用复核**（逐条实测）：
   - `git show origin/main:electron-renderer/src/components/RightPanel.tsx | sed -n '632p;635p'` → 占位注释 + banner ✅（实测输出见派单生成记录）
   - `git show origin/main:electron-renderer/src/stores/capability.ts | sed -n '43,44p'` → canAccessCap ga ✅
   - `git show origin/main:src/routes/ga-calibration.ts | grep -nE "router\.(get|post)"` → L121/239/314/419 ✅
   - `git show origin/main:src/loops/middle-evolution-engine.ts | sed -n '77,79p'` → Signal 1 reject+sentinel_alert ✅
   - `git show origin/main:src/growth/feedback-collector.ts | sed -n '123p'` → CHECK 含 diagnosis_conclusion ✅
   - `git show origin/main:src/agent/loop-handlers.ts | sed -n '377p'` → getAggregatedSignals 调用 ✅
2. **缺失声明复核**: 本单声明「engine 无 diagnosis_conclusion 分支」→ `git grep "diagnosis_conclusion" origin/main -- src/loops/` 实测**零命中** ✅
3. **D# 占用复核**: `git ls-tree --name-only origin/main task-state/ | grep -oE 'D[0-9]+' | sort -V | tail -3` → D553/D554/D555；D556 未占用 ✅
4. **DSH 借鉴核查**: 施工图 L74/77 + §4 第 7 行引用具体行 ✅
5. **写集边界复核**: 可碰清单不含 scripts/；与 D483 写集（src/middleware/auth.ts + tests/middleware/*）零交集 ✅

**复核通过才交付** ✅（本派单复核命令全部执行，输出如上）

---

## 派单说明（给创始人复制——自包含，零查找）

```
【派单】GA 人机协同端到端（D556）
> 认领: 📋 dev-doc（先出 spec）| slice: ga-collab-e2e

## 背景
你批准的切片方向 A：D551 后端（校准/信号/仪表四端点）昨晚已合 main，前端三项仍置灰 = 已付成本零用户价值。本切片把置灰转真实 + 修 S5-2 断裂的层 2（回流→进化动作）。

## 现状材料（必读，行号已实测）
- 前端占位: electron-renderer/src/components/RightPanel.tsx L632-647（GaDetail 三块，无 fetch）
- 权限门控: electron-renderer/src/stores/capability.ts L43-44（canAccessCap ga）
- 四端点: src/routes/ga-calibration.ts L121/239/314/419（requireGa 已挂）
- 层 2 缺口: src/loops/middle-evolution-engine.ts L77-119（信号白名单无 diagnosis_conclusion）
- 回流已通: src/growth/feedback-collector.ts L123（CHECK 已扩）+ loop-handlers.ts L377（消费接线）

## spec 必答题
1. GaDetail 三块各调哪些端点？role≠ga 降级渲染？503 降级 UI？（铁律 24/31）
2. diagnosis_conclusion × {reject,modify,ineffective} 各触发什么进化动作？阈值？动作落点？（诚实边界：采纳率不可得、层 3 不做）
3. GA 角色来源（app-store role file:line）+ seed 验收身份（不与在途 D483-D486 耦合）
4. 写集表文件级（D551 教训）+ DS 一一对应

## 写集约束
- 可碰: RightPanel.tsx / stores（GA 相关）/ middle-evolution-engine.ts / tests
- 不碰: src/routes/**（D551 只读）、feedback-collector.ts（只读）、scripts/**（治理冻结）、src/middleware/**（D483 在途）

## 验收（物理可复现）
seed GA → 面板可见 → POST 校准回显 → 注入信号 → 仪表计数变化（UI 断言）；层 2: 3 条回流 → 进化动作生成断言；存量零回改 diff 断言

## 交付要求
1. spec: SYNOVA-IMPL-DSH-D556-ga-collab-e2e-<日期>.md
2. 验收命令 + 断言 + evidence 落盘
3. 诚实声明（层 3 descope / 采纳率不可得）
```
