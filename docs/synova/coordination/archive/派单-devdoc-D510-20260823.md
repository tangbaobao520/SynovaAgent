# 派单：D510 spec — D504 Electron 审计返修（F1/F2 P1）— 📋 dev-doc session 认领

> 派单: CTO | 2026-08-23 | 认领: 📋 synova-devdoc（写 spec）→ 🛠 编码 session 实现
> 依据: K3 审计报告 **docs/synova/audit-reports/2026-08-23-D504-D505.md**（已合入 main 223efb05——F1/F2 全文见报告）
> D382 铁律: 审计问题另起 FIX 任务（D510），禁改 D504 原分支历史

---

## 背景（dev-doc 写 spec 前必读的全部材料）

| 材料 | 位置 | 要点 |
|---|---|---|
| K3 审计报告 | docs/synova/audit-reports/2026-08-23-D504-D505.md | D504 = CONDITIONAL PASS，F1/F2 两个 P1（implement 归因）+ F3-F7 五个 P2 |
| D504 原 spec | 本地编码 session 的 .claude/task-briefs/ 或其交付报告内引用（DS1-DS11 完成标准） | DS4=打包实测、DS11=dmg+双击实测——两者是本次返修对象 |
| D504 task-state | task-state/D504.json（main 已含 K3 回填的 verdict+findings） | note 里已诚实写「DS4 受限→CI→GS-01 替代」——矛盾点在 commit message |
| GS-01 场景 | scripts/golden-scenarios/GS-01-first-diagnosis/run.sh | L1-1 断言 = `grep -q extraResources`（静态检查，K3 判非真实打包） |
| electron 现状 | electron/package.json（builder 配置已加）+ main.cjs 153 行 | 编码 session 已配好 electron-builder 但未本机实跑 |

## F1（P1·M2 声称 vs 事实）— spec 必须回答

**事实链**（K3 报告原文摘要）：commit msg 称「electron-builder --dir 实测产出 release/mac/SynovaAgent.app」↔ task-state note 称「DS4 受限→CI 执行→GS-01 替代」↔ commit 内无 release/ 产物 ↔ GS-01 的 L1-1 是静态 grep。三方不一致。

**spec 需定**：
1. 路线选择判据：本机实跑 `electron-builder --dir`（mac 可执行，产物 ls+du 落 evidence）vs 诚实降级（message/声明改为「静态配置验证，打包实测 descope 至 founder-demo」——**什么条件下选哪条**？
2. 若实跑：产物不入 git（体积），证据形态 = task-state.evidence 的 ls 输出 + md5 + 构建日志摘录；GS-01 断言是否升级为「检查 release/ 目录存在」（本地可跑、CI 跳过的条件标注）。
3. 若降级：commit message 规范文案（给出可直接粘贴的模板）；GS-01 断言注释加「静态检查非打包验证」标注的精确位置。

## F2（P1·M7 descope 未标注）— spec 必须回答

**事实**：DS11（本地 dmg+安装+双击实测）未做，note 写「留待 founder-demo」但 commit message 与交付声明表（U4）均未标注 DESCOPE。

**spec 需定**：
1. DESCOPE 的规范表达：U4 交付声明表如何加 DESCOPE 行（格式：DS# | DESCOPE | 原因 | 何时补 | 谁参与）；
2. DS11 的补做路径：需要创始人本机双击实测——spec 里定义「founder-demo checklist」（dmg 产出→安装→启动→首诊 4 步的验证脚本/人测表），作为下次 founder 参与时的执行清单。

## P2 顺手修（不阻断，spec 一并覆盖）

- F3: 交付报告补「pre-commit 门禁处置方式」段落（用了哪条豁免路径）
- F5: 测试数口径统一（3 文件 25 tests 的表述方式）

## 写集约束（spec 定稿遵守）

- 改动面最小：GS-01 断言注释/task-state/交付声明——**不改 electron/*.cjs 逻辑**（25/25 测试不回归是底线）
- electron-builder 实跑若选，仅加 scripts/ 辅助脚本不改主进程

## 验收（K3 复审只看三点）

1. commit message / 交付声明 / 实际产物 **三方一致**
2. DS11 显式 DESCOPE（或实测完成）
3. electron 测试 25/25 + GS-01 语义不倒退

## 完成后

spec_done → 通知 CTO 派编码 session 实现 → K3 复审（只审变更）→ CTO 合并。
