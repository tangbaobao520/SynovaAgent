# 派单｜小队 B：P-2 不变量机制（D831）

> 出件 2026-09-21 CTO ｜ 执行形态：**agent 小队（编码 1 + 自验 1；队长＝并行 CTO session）** ｜ 域：win → Mac 代行 ｜ K3 队外

## 〇 编制与工作树
工作树 `.synova-wt-p2` ｜ 分支 `feat/p2-invariants` ｜
写集：`src/invariants/**`（新建伴生目录）、`src/sentinel/sentinel-events.ts`、`src/deploy/bootstrap.ts`、`tests/sentinel/invariants.test.ts`、`task-state/D831.json`
**硬边界**：不碰 `scripts/**`、`.github/workflows/**`、`src/connectors|tools|routes|agent|cli-manager|tui-v2/**`（小队 A 地盘）

## 〇-b 为什么派它
**P-2 属前提清单第 2 项**（优先级高于线）。现状：`src/sentinel/sentinel-events.ts:9-13` 的 I1/I2/I3 **只有注释与错误码**，**无运行期断言/注册表/归属/fail-closed**；`check-architecture.sh` 是静态门禁，**看不见运行期接线** → D819 那类"schema 丢掉""降级被抹掉"会**静默复发**。
**做完推动**：运行期"违约必炸 + 知道是谁的锅"，D819 类事故不可能静默复发。
**架构层**：**L3 洞察层**（sentinel 事件/不变量）＋ `src/deploy` 注册。

## 〇-c 权威件回执
研究院《任务书-基础设施与信任层》DSH 借鉴 **缺口 1（不变量机制）**：`dsh-invariants/lib/index.js`（123 行，净逻辑约 40 行；每包 `./invariant` 伴生注册、失败带 `code + packageName`）｜总纲 §9.4（含"**显式声明本不变量不检查什么**"这条洞察）｜D819 卡（硬依赖，已合）
撤回自检：**未命中 9 项撤回**（本件是"照范式复用"，不抄 DSH 实现）｜G1 守卫 = 0

## 〇-d 旧信息漏洞自检
- [x] `dsh-invariants` 路径为 DSH 安装目录（**引用写全名+行号**，见派单 §〇-c）
- [x] write_set 与小队 A/控制塔线零交集

## 〇-e 前提实测 + 复核项
1 `sed -n '1,20p' src/sentinel/sentinel-events.ts` → 确认 I1/I2/I3 现状（注释/错误码）
2 `grep -rn "healthz" src/` → 现有健康检查暴露面
3 `ls src/invariants 2>/dev/null` → 是否已存在（无则新建）
**复核项**：① 注册点放 bootstrap 还是 sentinel 初始化（给理由）② 断言失败语义：**fail-closed**（5xx）而非 WARN ③ 每条不变量必须**显式声明"不检查什么"**（总纲 §9.4）

## 一 任务分解（首批 3 条伴生）
① 新增 `src/invariants/companions/*.invariant.ts`（每域一个伴生，`export const onXxx = ({ fail }) => {...}`）≈200 行；② 注册表 + 归属（失败含 owner/packagename）；③ 接线：在启动路径注册；④ 反接线探针：`/healthz` 暴露 `registered ≥3` 且每条 `hitCount`；跑完正向用例后 ≥3 条 `hitCount > 0`（`registered==0` 或全 0 → 启动即失败）；⑤ 原子回滚：第 3 条伴生 install 抛错 → 进程启动失败且无残留监听；⑥ 伴生 install 不得返回 Promise（类型上限制为 void）

## 二 完成判据
- [ ] 负向：注入一次违约（如 provider 组装请求时丢掉 tools）→ **5xx + 日志含 owner 名**（不许 200 + WARN）
- [ ] 正向：正常对话 + 一次真实工具调用 → 200 且 `lastFailure` 为空（不误报）
- [ ] 反接线探针：`registered ≥3`、`hitCount > 0`（给输出）
- [ ] 原子回滚夹具：第 3 条抛错 → 启动失败、无残缺监听
- [ ] **每条不变量显式声明"不检查什么"**（第 8 条验收点）
- [ ] M6 三件已提交｜PR ≤12 文件单域｜`ls-remote` 回执

## 三 红线
不改产品业务逻辑、不碰控制塔/审计/CI；禁 `--no-verify`/stash/force push；K3 复审前不合并
