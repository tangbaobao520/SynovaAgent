# 并行派单：D511 版本守卫门禁 + D512 GS 场景全量刷新（📋 dev-doc 各写 spec，独立 PR）

> 派单: CTO | 2026-08-23 | 与 D510（Electron 返修）**零领地重叠，可并行**
> 流程: dev-doc 写 spec → 编码 session 实现 → K3 审计 → CTO 合并

---

## 任务 1：D511 版本守卫门禁（控制塔，V4.9.1 候选）

### 背景（材料全在这）
- **规范**: docs/synova/coordination/版本管理规范-控制塔.md（main 已有）——§四明确"待办: pre-commit 版本守卫"
- **事故史**: V4.8.0→V4.9.0 之间**六批门禁行为变更全部未 bump**（D467/D501/D503/D506/D507/D508——CT-42 教训第二次违反），靠创始人指出才发现
- **版本文件**: .codex/control-tower/VERSION.md（人类可读）+ version.log（机器可读，gitignore 运行态）+ git tag 三同步

### spec 必答题
1. 检测面：暂存文件含 scripts/control-tower/、scripts/pre-commit-check.sh、scripts/workflow/、scripts/install-hooks.sh 且 **VERSION.md 无同 commit 变更** → 硬阻断？白名单怎么设计（纯注释/文案改动豁免的判定）？
2. bump 级别判定：能不能自动建议（新增门禁组=MINOR，判定逻辑改=PATCH）还是只提示人工定级？
3. 逃生舱：紧急修复来不及 bump 时 SYNO_SKIP_* 惯例（需记 degraded-events.log）
4. 测试：tests/control-tower/ 配对（改门禁无 bump → 拦；同 commit 带 bump → 放行；纯文档 → 跳过）

### 写集预期
- scripts/pre-commit-check.sh（新门禁组，组 14）或独立 check-version-guard.sh
- tests/control-tower/version-guard.test.sh
- 不动既有 13 组判定逻辑

### 验收
- [ ] 模拟"改门禁不 bump"被拦（测试可复现）
- [ ] 本次 D511 自身提交带 V4.9.1 bump 通过（吃自己的药）

---

## 任务 2：D512 GS 场景全量刷新 + 进度重算（部署轨验证层收官）

### 背景（材料全在这）
- **现状**: 8 个 GS 场景**全部有 pass 证据**（scripts/golden-scenarios/evidence/*.json——但全是 08-21/08-22 的，此后 main 已进 D505 哨兵自检/D508 减负/D504 electron 等）
- **疑点**: 证据 stale——尤其 GS-01 的 L1-1 断言是静态 grep（D510 正在修它的诚实性），GS-06 依赖的 loop 执行体（Win D475）状态未知
- **验收标准**: todos.yaml 的 T-1-01（GS-01 转绿）/T-3-01（GS-08 转绿）等 P0 条目的判定都挂在这

### spec 必答题
1. 刷新顺序：全量 8 场景一次重跑 vs 按依赖排序（GS-03/05 先——它们是其他场景的公共设施）？失败场景的处置（诚实 RED 记录 vs 阻断刷新）？
2. 证据规范：新证据文件命名（带 main sha？）、旧证据归档还是覆盖？
3. 进度重算联动：场景刷新后是否触发 scripts/product-lines/refresh-all.sh（todos/product-progress 重算——过时条目如 T-8-01 应被自动消化）？
4. CI 接入：这次刷新能否顺手把「push main → 自动跑全部 GS」加进 product-progress.yml（现在只有周五定时）？

### 写集预期
- scripts/golden-scenarios/evidence/（新证据）
- scripts/golden-scenarios/run-all.sh（如无全量入口则新增）
- .github/workflows/product-progress.yml（如接 CI）
- docs/synova/product-lines/product-progress.json（重算产物）

### 验收
- [ ] 8 场景在当前 main 全部重跑，证据带时间戳+main sha
- [ ] product-progress.json 重算，26 线完成度真实反映（预期 L1/L3 等线因 GS-01/08 绿而上调）
- [ ] 过时 todos 条目（T-8-01 类）被重算消化或显式保留理由

---

## 并行性声明

| 任务 | 领地 | 与 D510 冲突 |
|---|---|---|
| D510 | electron/ + GS-01 断言 + task-state | — |
| D511 | scripts/ 门禁 + tests/control-tower | 无（D510 不碰 scripts） |
| D512 | scripts/golden-scenarios + workflows | **与 D510 有 GS-01 交叉**——D512 spec 需声明：GS-01 的重跑**等 D510 合并后**执行（其余 7 场景先行），或接受 D510 修复前后各跑一次 |
