# 派单（批十·附）：三项开工前核实（D818）

> **依据计划: v1.2@4e46603f**（`docs/synova/coordination/整体推进计划-主线-20260913.md`，CTO 派单时实测哈希）
> 出单: synova-cto ｜ 2026-09-18 ｜ 归属：📋 规格线（只读核实，不占编码线）
> 来源：研究院四份任务书自曝 **15 项未核实（U1–U15）**，并明确建议「**先派三个 1 天级核实任务，再派 15 项正式任务**」。CTO 同意，合成一张卡（同属只读核实、同一交付形态）。

---

## 〇、为什么先核实再派工

15 项正式任务的缺口判定，**建立在若干未核实的面上**。研究院自己点名最危险的一处：

> **`src/security/` 13 个文件里只完整读了 2 个，余 11 个约 1,847 行未读 → 可能推翻第 1/3/4/6/8 项缺口判定。**

**所以在派「不变量/凭据/设置热改/溢出/文档转换」这五项之前，必须先把这 11 个文件看一遍**——否则我们可能花 20+ 天去"补"一个已经存在的东西，或者**漏掉一个已经在跑的风险**。

---

## A. 任务卡

### D818 三项开工前核实（只读，零代码改动）
- **缺口**：15 项缺口判定建立在未核实面上（研究院 U1–U15）。
- **判据**：**C**（缺了它，派出去的活可能建立在对现状的错误判断上）。
- **DSH 参考**：不适用（本卡不借鉴，只核实）。
- **借鉴什么**：沿用研究院同款取证方式——**只读逐行 + `wc -l` 实测 + `grep -n` 带行号**，否定性结论必须写明"关键词全扫过"。
- **不借鉴什么**：不做推断式结论（未读到就写"未读到"，不写"应该没有"）。
- **落地位置**：`docs/synova/coordination/核实单-D818-未核实面-20260918.md`（新建）+ `task-state/D818.json` + brief + memory note。
- **方式**：自建（零依赖，纯读码）。
- **验收标准**：三节齐备，**每节以「结论 + 证据（文件:行）+ 会改变哪一项派单判定」收束**；零代码改动；结论可复现。
- **依赖**：无。
- **工作量**：**1 天**。

### 三节内容

| 节 | 核实什么 | 为什么要紧 |
|---|---|---|
| **1** | `src/security/` 余 11 文件逐个读：`pii-scrubber.ts` / `policy-engine.ts` / `file-guard.ts` / `connector-sandbox.ts` / `pre-upload-validator.ts` / `prompt-injection-detector.ts` / `root-hash-publisher.ts` / `safety-guardrails.ts` / `connector-audit.ts` / `crypto-hash-utils.ts` / `external-hash-store.ts`（约 1,847 行）→ 逐条回答：**是否已覆盖** ① 不变量机制 ② 凭据管理 ③ 设置热改 ④ 溢出账本 ⑤ 文档转换 | 可能推翻 5 项缺口判定（研究院原话） |
| **2** | 数据层任务书未核实第 **1/2/5/10** 条：① `/api/sessions/:id/stats` 的 `agent_sessions.id` 与 consult id 是否同源 ② turn/step 边界产出点 ③ FTS5 `trigram` vs `unicode61` 中文召回与索引体积 ④ 指标保留期与 bucket 粒度 | 决定数据层 6 项能否开工与验收 URL 形状 |
| **3** | Node 运行基线：`package.json` engines + CI node 版本 + 部署假设 → 判定 **代理能否零依赖走 `NODE_USE_ENV_PROXY`** | 决定基础设施层第 2 项是 **3 天还是 6 天** |

---

## B. 红线

1. **只读**：零代码改动（`git diff --name-only` 只允许出现本卡的簿记文件）。
2. 不碰 `scripts/audit/**`（K3 域）、`scripts/control-tower/**`（另有两条线在改）。
3. **未读到就写"未读到"**，不得用"应该没有/大概已覆盖"填空——研究院的四份任务书之所以可用，正是因为它们把"答不上"逐条标了出来。
4. 每条结论必须附**可复现命令 + 原始输出**（数字不许手算）。

---

## C. 派单前复核（CTO 已亲自核实）

| 项 | 证据 |
|---|---|
| ① 任务号真实 | D818 全 refs 扫描占用 = 0 |
| ② 前置状态 | 无前置；研究院任务书已交付 |
| ③ 技术声称亲自复现（部分） | CTO 已替本卡解决两条：**U3 →** `src/routes/credentials.ts` **确实挂在生产 server**（`src/server.ts:394`）→ 凭据缺口是"生产里跑着明文 + 末 4 位回显"，**不是死代码**；**U11 的一部分 →** `vitest.config.ts:29-37` CI 排除 `tests/e2e/**`（已成 D817） |
| ④ 写集路径 | 新建文件已显式标注；`src/security/*.ts` 均为既有文件（只读） |
| ⑤ 写集零交集 | 与在飞的 D809/D810/D811–D817 零交集（本卡只写 1 个新文档 + 簿记） |
| ⑥ 引用行号 | `src/security/` 13 文件数、`src/server.ts:394` 均当场实测 |
| ⑦ 依赖图 | 无；其产出是**后续 15 项正式任务派工的前置输入** |
| ⑧ 交付物齐备 | 写集 / 红线 / Done / 前置 + 创始人复制块（§D） |
| ⑨ 派单内部一致性 | 「只读」与「要证据」不互斥（证据来自读码与命令输出）；「1 天」与「11 文件 1,847 行」相符（研究院同量级） |
| ⑩ 主线计划锚定 | `依据计划: v1.2@4e46603f` |

---

## D. 创始人复制块（可直接转发给 📋 规格线 session）

```
任务 D818｜三项开工前核实（只读，1 天量级）。
派单全文: docs/synova/coordination/派单-批十附-三项开工前核实-D818-20260918.md
为什么：研究院 15 项正式任务的缺口判定建立在未核实面上；最危险的一处是 src/security/ 余 11 个文件（约 1,847 行）未读，
        可能推翻"不变量/凭据/设置热改/溢出/文档转换"五项的缺口判定。
三节（每节以「结论 + 证据 文件:行 + 会改变哪项派单判定」收束）：
 ① src/security/ 除 credential-vault.ts / index.ts 外的 11 文件逐个读 → 是否已覆盖那五项缺口
 ② 数据层未核实第 1/2/5/10 条（stats URL 同源 / turn-step 产出点 / FTS tokenizer / 指标保留期与 bucket）
 ③ Node 运行基线（package.json engines + CI node 版本 + 部署假设）→ NODE_USE_ENV_PROXY 零依赖代理路线是否可行
红线：只读零代码改动；不碰 scripts/audit/** 与 scripts/control-tower/**；未读到就写"未读到"，不许"应该没有"；
      每条结论附可复现命令 + 原始输出。
产物：docs/synova/coordination/核实单-D818-未核实面-20260918.md（新建）+ task-state/D818.json 回填。
做完复核；K3 会抽审。开始吧。
```
