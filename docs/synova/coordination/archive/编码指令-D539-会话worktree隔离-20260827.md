# 编码指令 — D539 会话 worktree 隔离（session-worktree-isolation）

> 派单: CTO | 2026-08-27 | 给: 🛠 编码 session（Mac DSH 线，scripts/control-tower/ + scripts/workflow/ 归它）
> 前置: 认领本指令前确认 task-state/D539.json = spec_done（本 spec 已 gatekeeper ALL PASS）
> ⚠️ **基线红线**：本任务的**现状基线是 `origin/main`**（不是本地 `main` / `feat/d505-impl`——本地 main 与 feat/d505-impl 均落后 origin/main 数百 commit（本 session 实测 447）。**编码前必须 `git fetch --all && git checkout origin/main`（或基于 origin/main 建 worktree），禁止在 stale 的 feat/d505-impl 主树上实现。**

---

## 1. 任务文档表（先读后动，一条不落）

| 文档 | 路径 | 作用 |
|---|---|---|
| **spec（编码唯一契约）** | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D539-session-worktree-isolation-20260827.md` | 本指令的唯一事实源。§5 写集 / §7 测试 / §8 接线 / §10 验收 |
| 派单 | `docs/synova/coordination/派单-D539-session-worktree-isolation-20260827.md` | spec 必答题 1/2/3 + 验收 + 写集红线 |
| D507 三层防线 | `docs/synova/coordination/并行撞车根治方案-D507-20260823.md` | 主仓只读化 + 强制 worktree 设计意图 |
| D307 worktree 隔离 dev doc | `docs/plans/codex/implementation/SYNOVA-IMPL-D307-session级worktree隔离-20260812.md` | worktree-manager 设计（已交付，本单只接线） |
| 北星 | `.claude/PRODUCT-BRIEF.md` §七（流程/协作根因）+ §八（Loop Engineering 进程系统） | 方向锚定（治理层） |
| 前车之鉴 | `docs/plans/codex/implementation/SYNOVA-IMPL-D352-resolver硬化-20260813.md`（结构范例）+ AGENTS.md 铁律 0-2/11/24/31/35/38/39/47/48 | 结构 + 质量铁律 |

## 2. 执行要求

1. **认真阅读** spec 关键节：§1 Authority / §4 Current State（现状已实测 origin/main，勿再凭旧文案或本地 stale 分支）/ §5 写集（3 条目）/ §7 测试（red→green 三路径）/ §8 接线（每条 grep 出真实生产调用点）/ §10 验收（8 条可证伪）。
2. 任务较复杂（task-start 主改 + attach 兼容 + 新测试 + 接线）→ **先用 plan mode** 列改动清单（文件→改什么→测试→验证）再动手。**禁止没想清楚就改。**
3. **最高代码水平**：bash 函数契约 JSDoc 头注释（铁律 47）；降级诚实（每个分支 `log.warn/error` + degraded-events.log，铁律 24/31——ctrl-tower-change 模式 1 三态：0=通过/1=业务阻断/2=自身降级）；`as any`=0（无 src/，天然）；测试非空壳三路径 + grep/expect 断言（铁律 48）。

## 3. 任务专属硬约束（本任务特有，违反 = 审计 FAIL）

- **基线核验（防 M7 漂移）**：
  - 当前工作区必须 = `origin/main`（实测 `origin/main=0dc36c2b`）。编码前 `git fetch --all && git checkout origin/main`。**禁止**在 stale `feat/d505-impl` 上实现（它落后 447 commit）。
  - 合入后 **重新核验 spec 引用的行号/契约**——`scripts/workflow/task-start.sh`、`scripts/control-tower/attach.py`、`scripts/workflow/resolve-commit-brief.sh`、`scripts/control-tower/session_registry.py` 均可能被上游改过，行号必漂移。**照 spec 旧行号写测试会红**（D524 教训）。编码时以 `git show origin/main:<file>` 实际内容为准。
- **写集精确性（D286 教训）**：只改 spec §5.1 写集表 2 条文件 + 新建 1 条测试（`scripts/workflow/task-start.sh`、`scripts/control-tower/attach.py`、`tests/control-tower/session-worktree-isolation.test.sh`）；`git diff --name-only` 与实际改动完全一致；禁改写集外文件、禁"树终验声称不符"。
- **诚实 RED**：`worktree-manager.py create` 的程序化接线，若在实现中确认 task-start 只输出命令（不实际派发），**必须**至少让 task-start **程序化引用** worktree-manager.py 路径（`grep "worktree-manager" scripts/workflow/task-start.sh` ≥1 命中），否则 = 零接线（铁律 0-2，D307 "已落地零调用"活例重演）。若无法满足，如实标注 ⏸ + 理由，**禁止伪造接线 grep**。
- **接线红线（spec §8）**：`_assert_dev_worktree`/`_resolve_session_id`/`current-brief.<sid>` 写 / `worktree-manager.py` 引用都要有**生产**调用点（测试调用不计，S-3）。
- **红线**：不碰 `src/`（本任务零 src/ 需求）、不碰 `scripts/audit/`（K3 专属）、**不碰 `scripts/pre-commit-check.sh`**（D537 #2 已拦提交端；若确认必须改 pre-commit-check.sh → **单列**并 CTO 单独审，不并入本写集）、不碰 `scripts/workflow/hook-block-write.sh`（不同层）。
- **零新组件**：不新增守护进程/服务/launchd/DSH 依赖；复用 worktree-manager.py + 现有 hook。
- **环境坑（windows-compat）**：改的是 .sh/.py（跨平台）——每个 .sh 头部保留 D313 M5 UTF-8 头块（`export PYTHONIOENCODING=utf-8`），macOS BSD sed 不支持 `\+` 用 `sed -E` + `+`；新文件**LF 换行**（D533 教训：tests/control-tower/ 曾 renormalize 15 脏文件）。本机测试用 bash + python3（macOS 可用）。
- **gate 门禁三态**：任何 `2>/dev/null` 要有 `swallow-ok: 原因` 注释或 `|| true` 链路（verify 会扫）；`grep -c` 防 "0\n0" 用 `tr -d '\n'`（D313 M5）。

## 4. 复核清单（做完逐项自查，K3 会盯着）

- [ ] 与 dev doc 一致：spec §10 验收 8 条逐条对照（隔离断言 sha256 / 阻断断言 exit≠0 / 接线断言 grep / CT-42 闭环 / 主仓只读化 / 现有测试回归 / 语法 / 合规）
- [ ] 铁律：接线完整（spec §8 逐条 grep 出生产调用点——尤其 `worktree-manager.py` 在 task-start.sh 的程序化引用）、降级诚实（24+31，SYNO_ALLOW_MAIN 豁免/registry 不可读/git-dir 解析失败均写 degraded-events.log 不静默）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39，工具层）。
- [ ] 无 bug：`bash -n scripts/workflow/task-start.sh scripts/control-tower/attach.py` + `python3 -m py_compile scripts/control-tower/attach.py` 语法过；`bash tests/control-tower/session-worktree-isolation.test.sh` 沙箱全绿；现有 `tests/control-tower/` 回归零新增失败（worktree-manager.test.sh/.py、test-session-registry.py 等）；pre-commit 13 组全过（**禁 --no-verify**）+ `synova-commit`（**禁 git stash**，铁律 0-3）。
- [ ] 接线完整：spec §8 每条 grep 真实生产调用点。
- [ ] 测试到位：red→green 已证（先写 `session-worktree-isolation.test.sh` 现状未实现必失败 → 实现 → 全绿）；三路径（正常/降级/边界）；**物理隔离断言**（双 worktree，A commit → B 的 index/current-brief 零变化，sha256 指纹比对）。
- [ ] 其他：残留清理（删除全局 current-brief 写后 grep 确认 task-start 不再写全局）、无死代码、`SYNO_SKIP_PARALLEL_GUARD`/`SYNO_ALLOW_MAIN` 注入缝有测试覆盖。

## 5. 审计提示

- **提审口径**：一并覆盖 D539（会话 worktree 隔离——主仓只读化 + 强制 worktree + 会话专属 brief 闭环）。
- **验证点收口**：派单验收（隔离断言 / 阻断断言 / 接线断言）三者物理可复现，全过（3/3）；spec §10 8 条逐条收口。
- **task-state 回填**：实现提交后更新 `task-state/D539.json` impl 段（`{commit, by, at, files[]}`）+ status→`impl_done`（D382 状态机，见 task-state/README.md）。
- **脚本可复现**：审计员独立重跑 `bash tests/control-tower/session-worktree-isolation.test.sh` + 接线 grep（spec §8），禁仅 task-state 单副本声称；测试含 mktemp 沙箱 + SYNO_* 注入，无本机假设。

## 6. 结束

开始吧。
