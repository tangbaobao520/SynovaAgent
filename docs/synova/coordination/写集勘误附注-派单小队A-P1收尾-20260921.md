# 写集勘误附注 — 派单-小队A-P1收尾出站收敛-20260921（D862）

> 2026-09-21 coder-b 登记 ｜ 派单件在 main：`docs/synova/coordination/派单-小队A-P1收尾出站收敛-20260921.md`

1. **src/services/deepseek-balance.ts、src/mvp-server.ts**：队长已裁决勘误扩入 coder-a 卡（Node 侧真实出站，迁移 `outboundFetch`）。本 worktree 的 `src/tui-v2/chat.tsx` 消费 `fetchDeepseekBalance`（L1→services 只读），本卡未改该文件。
2. **src/mcp/tool-definitions.ts:292**：coder-b 判定**豁免**——L1 HTTP 桥惯例打本进程 localhost API（文件头 D595 注释明示），localhost 恒绕过代理，迁移行为零差异。理由详见台账 §四。
3. **src/deploy/watchdog-entry.ts:33**：coder-b 判定**豁免（建议）**——本机 healthz 探针，loopback 恒绕过；且 `src/deploy/**` 在 coder-b 硬边界内（不碰），如裁决物理迁移须另派单。
4. **基线计数勘误**：派单件前提「全仓 59 处」中，coder-b 写集（src/routes/**、src/tui-v2/**、src/server.ts）实测 **25 处**（git grep @HEAD，含 1 处注释）；逐条归类见 `D862-出站分类台账-20260921.md`（迁移 2 / 浏览器内嵌豁免 22 / 注释 1）。
5. **routes/server.ts 不存在**：健康检查真实入口为 `src/routes/healthz.ts`（GET /api/healthz），已在 routes/** 写集内由 coder-b 增第 7 项 `outbound_proxy` 检查并配三路径测试。
