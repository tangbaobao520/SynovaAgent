# 切片 A 总览 — L1 桌面端「能装」（D517/D518/D519）

> 归属: dev-doc (DSH) | 2026-08-24 | 派单: docs/synova/coordination/派单-L1切片A-D517-D519-20260824.md
> 方法论: 切片思维（创始人批准）——按用户可见行为切 A/B/C，本次 A = 0→3/8 验证点

---

## 一、基线声明（物理实测 2026-08-24，worktree synova-wt-sliceA-specs @ main ea89dee9）

**基线**: main（ea89dee9，PR #154 已合并 D504）。`feat/d504-impl`（0da174e0，K3 审计 CP）已入 main——electron/backend-spawn.cjs（156 行，**已在 main**）、electron/main.cjs（193 行，:126 真实调用 ensureBackend）、build-synova.cjs（extraResources 三映射）、tests/electron/ 3 文件（backend-spawn/desktop-build/auto-update）均已物理核验在 main。

main 已有（实测）: electron-renderer/（vite+react，outDir dist/renderer）、GS-01 断言、src/routes/healthz.ts `GET /api/healthz`、src/config.ts `SYNOVA_DB_PATH`。

## 二、依赖图（串行，写集重叠）

```
D504 已合入 main（ea89dee9 ✅ 前置完成）
        │
        ▼
D517 安装包可产出 (1-1)          ← electron-builder 配置收口 + CI 双平台矩阵 + 构建链契约
  写集: build-synova.cjs / .github/workflows/desktop-build.yml / tests/electron/desktop-build.test.ts
  runbook: docs/synova/runbooks/desktop-build.md（构建链三步契约 + Gatekeeper 警告说明）
        │
        ▼
D518 安装引导单一入口 (1-5)      ← 依赖 D517 的产物形态（resources/renderer + dist）
  写集: electron/main.cjs / docs/synova/runbooks/desktop-dev-prod.md / tests/electron/backend-spawn.test.ts
        │
        ▼
D519 Mac 安装包实测 (1-3)        ← 依赖 D517 产物 + D518 入口收敛
  写集: scripts/desktop/mac-install-verify.sh / docs/synova/runbooks/founder-demo-mac.md / task-state evidence
```

- 三个 D 写集都动 electron/ 领地 → **串行执行**（D517→D518→D519），禁并行派发（S-7/S8）。
- 共享资源: build-synova.cjs（D517 主改，D518/D519 只读）、electron/main.cjs（D518 主改，D517/D519 只读）、release/ 产物目录（D519 消费 D517 产物）。

## 三、验证点映射（product-progress.json 重算口径）

| 验证点 | 内容 | 负责 D | verified 判据（物理证据形态） |
|---|---|---|---|
| 1-1 | 安装包能打出来 | D517 | 本地 `--dir` 产物 `release/mac/*.app` 存在 + 大小 + CI job 绿 + artifact 可下载 |
| 1-5 | 安装引导单一入口 | D518 | 装包双击→后端自启→首诊页可达（evidence 链路图）；dev 路径文档化 |
| 1-3 | Mac 版安装包可用 | D519 | evidence/ 含 dmg 的 ls+md5+挂载安装+启动日志+窗口断言 |

切片 A 完成 = 上述三点从 uncommitted → verified（0→3/8）。1-2（Win 实测）在切片 B/D521，1-6/1-7/1-8 在切片 C。

## 四、审计单位声明

- **一次 K3 切片级审计覆盖 D517+D518+D519**（非逐 D 审）。三个 D 全部 impl_done 后统一提审。
- 每个 D 的 task-state 带 `"slice": "L1-A"` 字段（已由 CTO 预置）。
- K3 审计验收口径：独立复跑 D517 构建链 / D519 mac-install-verify.sh，产物与 evidence 一致。

## 五、切片 A 红线（继承派单 + D510 教训）

1. **物理验证优先**：每个 Done 标准必须是命令可复现的断言；**禁止静态 grep 配置冒充实测**（D510 F1 教训）。
2. **不碰 src/**（首诊后端生产可用，GS-01 绿证明链路通）与 **scripts/audit/**（K3 专属）。
3. 声称 = 实现 + 验收（S-2）；接线 = 生产调用点（S-3）；测试 red 覆盖失败模式（S-5）。
