# 号段水位 · Mac

> **生成物（禁手改）**：由 `bash scripts/control-tower/gen-watermark.sh` 从 `task-state/*.json` + 全分支名反查生成。
> 段约定（人工常量）：D9xx

| 项 | 值 |
|---|---|
| 段 | D9xx |
| 水位（段内实测最大 D#） | D980 |
| 下一个建议号 | D981 |
| 段内卡数 | 54 |
| 段外异常（越上界，需裁定） | D1007 |
| 历史卡（低于下界，分段约定前） | 93 张（D371–D870） |

## 归属判定口径（防漂移）

- ① 显式归属: `task-state/D#.json` 的 `domain`/`owner`/`executor` 含 `mac`，**或**分支名含 `d<数字>` + 该归属标记（分支名取**首个** D#；描述性数字不参与）
- ② 段回退: 无任何归属标记但卡号落在本归属段内 → 归本归属
- ③ 都不适用 → 未归属（历史卡，只计数，不入水位）
- 水位 = **段内**实际存在的最大 D#（不手写常量；段内无卡 → 未定义）；段外异常只报事实，由 CTO 裁定改号或释放

## 段内最近卡号（最大 10 个）

- D980
- D979
- D977
- D975
- D974
- D973
- D972
- D971
- D970
- D969

