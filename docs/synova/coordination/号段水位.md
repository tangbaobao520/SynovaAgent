# 号段水位

> ⚠️ 本文件由**实测反查生成**——下方「生成段」（本行到 MANUAL:BEGIN 之前）
>   每次 `--emit` 都会被覆盖，**手工编辑无效**。人写内容只应放在 MANUAL 段内。
>
> 生成命令: `bash scripts/control-tower/probes/task-id-watermark-probe.sh --emit`
> 生成时间(UTC): 2026-09-25T09:25:20Z
> 判定面: `--face union`（面 A 全 refs ∪ 面 C 当前工作树 task-state/）
> 探针校验命令: `bash scripts/control-tower/probes/task-id-watermark-probe.sh`

## 1. 机器判定位（探针校验此值，勿手改）

声明水位: D974

- 含义: 本次实测范围内**已分配的最大 D#**；下一个可用号 = D975。
- DRIFT 判据①: 实测 > 声明 ⇒ 存在未登记的更高号（撞号风险）→ exit 1
- DRIFT 判据②: 声明 − 实测 > 保留窗口（默认 64）⇒ 水位虚高，防手改绕过 → exit 1

## 2. 实测来源分解

| 面 | 口径 | 扫描量 | 最大号 |
|---|---|---|---|
| A 全 refs | refs/heads + refs/remotes/origin + refs/remotes/ssh（按 SHA 去重） | 1912 refs / 853 unique tips / 472 task-state 树 / 97109 条目 | D973 |
| B 已推 origin | refs/remotes/origin | — | D973 |
| C 当前工作树 | `task-state/` | 363 文件 | D974 |
| PR 标题 | 本次未纳入（PR_STATE=off，显式降级非静默） | 0 | — |
| **判定** | **本水位号段 900-999 内的面 union 上确界**（已排除作废号段 1008-1013,1003-1006） | 段外最高 面A=D1007/面C=D870；作废命中 面A=4/面C=0 | **D974** |

## 3. 判定面口径说明

- 面 A 是**保守超集**：含本地未推分支 refs，宁可把水位抬高也不漏号。
- 面 C 纳入判定：本批占号在未推前只存在于工作树 `task-state/`，只扫 refs 会漏掉
  正在途中的占号（D974 卡就是这个场景）。
- 面 B 与 PR 面仅作对照，不作判定面（面 B 会漏本地未推号）。
- **本水位号段**由 MANUAL 段的 `本水位号段: D<n>-D<n>` 行声明（结构性管辖范围）：
  段外的号（如 Win 段 D1000–D1099 落在 Mac 水位文件里）不参与判定，但仍打印最高号以保持可见。
- **作废号段**由 MANUAL 段的 `作废号段: D<n>-D<n>` 行声明（人的决策），探针读取后
  把该区间从实测最大号中排除 —— 否则已释放号的历史 commit 留在 refs 里会永远抬高水位。

<!-- MANUAL:BEGIN -->
## 4. 号段约定（人工段 · 不被 --emit 覆盖）

### 4.1 分段约定（本文件 = main 内唯一真相源）

| 归属 | 号段 | 水位 | 下一个可用号 |
|---|---|---|---|
| **Mac** | **D9xx**（D900–D999） | **D974** | **D975** |
| **Win** | **D1000–D1099（Win 侧号段）** | D1002 | D1003 |

> **CTO 裁决（2026-09-25）**：**D1000–D1099 是 Win 侧号段**；Mac 段为 D9xx。

以下**逐字抄录**自既有同名文件（不在 main，存在于已推分支
`chore/d964-p4-docs-b2-2`、`chore/d964-p4-docs-batch2-1`、`chore/d965-d968-declarations`、
`chore/d965-d968-declarations-v2`、`docs/D965-D968-coordination-cards`、`docs/d943-cto-a-items`、
`tmp-d942`；其中 4 条分支 blob 同为 `4b4829950670350b90172a361728bf6084f7a1f4`）：

| 归属 | 号段 | 水位 |
|---|---|---|
| Mac | D9xx | D968 |
| Win | D1000–D1099 | D1002 |

### 2026-09-25 更正记录（CTO）
- **误用**：CTO 用 `alloc-task-id.sh` 取号得到 **D1003–D1006**，落在 **Win 段（D1000–D1099）** ⇒ 违反自身分段约定。
- **处置**：四卡**改号**为 **D965–D968**（Mac 段）；D1003–D1006 **作废释放**（Win 可继续使用）。
- **根因**：`alloc-task-id.sh` 只递增**全局最大号**，不感知分段 ⇒ 跨段取号必须人工复核。
- **水位**：Mac = **D968**（下一个 D969）｜Win = **D1002**（下一个 D1003，仍可用）。

### 2026-09-25 第二次更正记录（CTO 裁决 · 本批改号）
- **误用**：Mac 并行 CTO 小队手工取号 **D1008–D1013**，**再次**落在 **Win 段（D1000–D1099）**
  ⇒ 与上一条更正记录**同类错误的第 2 次**（已按纪律升级 CTO 裁决）。
- **CTO 裁决**：**D1000–D1099 是 Win 侧号段** ⇒ 本批六卡**改号为 D969–D974（Mac 段）**。
- **处置**：
  - **D1008–D1013 作废释放**（Win 侧可继续使用）—— 见 §4.3 `作废号段` 声明；
  - 本批改用 **D969–D974**；先期执行改号的三卡：`D1013 → D974`、`D1011 → D972`、`D1012 → D973`；
    旧远端分支名已删除（`alloc-task-id.sh` 的拒绝面匹配分支名里的 `d<num>`，留旧名仍撞号）。
- **实测卡面 `domain` 旁证**：D1000=win、D1002=win、D1004=win；D1001=mac（owner=win-codex-cto）；D1007=未填。

### 4.2 下一号声明

- **Mac 段下一个可用号 = D975**；Win 段从其水位（上表 D1002）续编。
- **发号顺序不可反（先实测、再发号）**：
  1. `bash scripts/control-tower/probes/task-id-watermark-probe.sh --emit`（刷新生成段）
  2. `bash scripts/control-tower/probes/task-id-watermark-probe.sh`（**必须 exit 0** 才可发号）
- 段内发号前须人工核对上表「下一个可用号」，并注意 `alloc-task-id.sh` **不感知分段**
  （只递增全局最大号）—— 跨段取号必人工复核。

### 4.3 本水位号段 + 作废号段（人工段声明 · 供探针机械读取）

本水位号段: D900-D999

作废号段: D1008-D1013
作废号段: D1003-D1006

- **本水位号段**：本文件是 **Mac 水位文件**，只对 **D900–D999（Mac 段）** 内的号判定水位。
  段外号（含 Win 段 D1000–D1099）**不参与判定**，但探针仍打印其最高号（他段命中数）以保持可见 —— 不静默。
- **作废号段**：上述区间内已登记的号**不参与水位判定**。其历史 commit 可能仍留在 refs 里（分支未必已删），
  若不排除会把水位**永久抬高** ⇒ 探针恒报 DRIFT、真实新号反而被掩盖。
- 两类排除都属**号段约定层面的决策**，只能由人写入本节；探针不自动推断，仅在报告中打印本轮命中数。
<!-- MANUAL:END -->
