# 号段水位

> ⚠️ 本文件由**实测反查生成**——下方「生成段」（本行到 MANUAL:BEGIN 之前）
>   每次 `--emit` 都会被覆盖，**手工编辑无效**。人写内容只应放在 MANUAL 段内。
>
> 生成命令: `bash scripts/control-tower/probes/task-id-watermark-probe.sh --emit`
> 生成时间(UTC): 2026-09-25T08:56:45Z
> 判定面: `--face union`（面 A 全 refs ∪ 面 C 当前工作树 task-state/）
> 探针校验命令: `bash scripts/control-tower/probes/task-id-watermark-probe.sh`

## 1. 机器判定位（探针校验此值，勿手改）

声明水位: D1013

- 含义: 本次实测范围内**已分配的最大 D#**；下一个可用号 = D1014。
- DRIFT 判据①: 实测 > 声明 ⇒ 存在未登记的更高号（撞号风险）→ exit 1
- DRIFT 判据②: 声明 − 实测 > 保留窗口（默认 64）⇒ 水位虚高，防手改绕过 → exit 1

## 2. 实测来源分解

| 面 | 口径 | 扫描量 | 最大号 |
|---|---|---|---|
| A 全 refs | refs/heads + refs/remotes/origin + refs/remotes/ssh（按 SHA 去重） | 1905 refs / 848 unique tips / 468 task-state 树 / 95657 条目 | D1008 |
| B 已推 origin | refs/remotes/origin | — | D1008 |
| C 当前工作树 | `task-state/` | 363 文件 | D1013 |
| PR 标题 | 本次未纳入（PR_STATE=off，显式降级非静默） | 0 | — |
| **判定** | **面 union 上确界** | — | **D1013** |

## 3. 判定面口径说明

- 面 A 是**保守超集**：含本地未推分支 refs，宁可把水位抬高也不漏号。
- 面 C 纳入判定：本批占号在未推前只存在于工作树 `task-state/`，只扫 refs 会漏掉
  正在途中的占号（D1013 卡就是这个场景）。
- 面 B 与 PR 面仅作对照，不作判定面（面 B 会漏本地未推号）。

<!-- MANUAL:BEGIN -->
## 4. 号段约定（人工段 · 不被 --emit 覆盖）

以下**逐字抄录**自既有同名文件（不在 main，存在于已推分支
`chore/d964-p4-docs-b2-2`、`chore/d964-p4-docs-batch2-1`、`chore/d965-d968-declarations`、
`chore/d965-d968-declarations-v2`、`docs/D965-D968-coordination-cards`、`docs/d943-cto-a-items`、
`tmp-d942`；其中 4 条分支 blob 同为 `4b4829950670350b90172a361728bf6084f7a1f4`）：

| 归属 | 号段 | 水位 |
|---|---|---|
| Mac | D9xx | D968 |
| Win | D1000–D1099 | D1002 |

### 2026-09-25 更正记录（CTO）
- **误用**：CTO 用 `alloc-task-id.sh` 取号得到 **D1003–D1006**，落在 **Win 段（D1000–D1099）** ⇒ 违反自身分段约定。
- **处置**：四卡**改号**为 **D965–D968**（Mac 段）；D1003–D1006 **作废释放**（Win 可继续使用）。
- **根因**：`alloc-task-id.sh` 只递增**全局最大号**，不感知分段 ⇒ 跨段取号必须人工复核。
- **水位**：Mac = **D968**（下一个 D969）｜Win = **D1002**（下一个 D1003，仍可用）。

### 与上方生成段的差异（实测登记，非改写上述约定）
- 上表 Mac 水位 D968 / Win 水位 D1002 是**人工声明**；生成段的数是**本次实测**（见 §2 各面分解）。
- 二者不一致**不代表上表错误**，而是二者口径不同：上表按**归属分段**，生成段按**全库上确界**。
- 本批（Mac 并行 CTO 小队）工作号 **D1008–D1013** 落在上表划给 **Win** 的段（D1000–D1099）。
  实测卡面 `domain` 字段：D1000=win、D1002=win、D1004=win、D1001=mac（owner=win-codex-cto）、D1007=未填。
  ⇒ 与上表「2026-09-25 更正记录」是**同类错误的第 2 次**，已按纪律**升级 CTO 裁决**；
  在裁决下达前，D1008–D1013 保留为**工作号**，本文件不写死其最终归属。

### 下一号声明（人工段）
- **在 CTO 对上述分段冲突裁决前，不得据本文件发新号**；裁决策下达后：Mac 段新号从上表 Mac 水位续编，
  Win 段新号从上表 Win 水位续编，且发号前必须先跑一次生成段刷新（`--emit`）取实时上确界。
- 发号前置命令（先实测、再发号）：
  `bash scripts/control-tower/probes/task-id-watermark-probe.sh --emit`
  然后 `bash scripts/control-tower/probes/task-id-watermark-probe.sh`（必须 exit 0）。
<!-- MANUAL:END -->
