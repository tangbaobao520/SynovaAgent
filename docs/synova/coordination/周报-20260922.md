# 周报 · 2026-09-22（CTO → 创始人）

> 一页。先说结论，再给证据。数据全部来自今天实测，不给"感觉"。

---

## 一、现在到哪了

**26 线完成度（离散健康计数，不再是百分比）**

分母 = **180 个验收点**（`product-lines.yaml`，与账本 ledger.json 的分母口径不同，不可跨件混读）。

| 档 | 数量 | 含义 |
|---|---:|---|
| 健康 | **0** | 严格五态（live + 穿真实入口）**当前无机器可读源** |
| 写了没接 | 3 | 代码写了，生产入口没调用 |
| 接了但坏 | 4 | 接上了，跑不通 |
| 在用未验证 | 32 | 能用，但没有穿真实入口的证据 |
| 陈旧 | 36 | 证据过期 |
| **状态未知** | **105** | **不知道** |
| 合计 | **180** ✓ | 恒等式成立（机器校验通过） |

**必须同时读的一句话**：`healthy = 0` **不是"产品全坏"**，是**严格口径缺数据源**——e2e 不在 CI 里跑，所以"live + 穿真实入口"这一态**没有机器可读的证据来源**（总纲 §1.2 第 0 项 P-3 仍未立项）。当前那个 0 是**代理口径**，报告里带 `strict_source_absent` 自曝。

**真正的头号数字不是 0，是 105/180 = 58% 状态未知。**

数据时效：`product-progress.json` 生成于 **09-20 23:23**（2 天前）。

---

## 二、哪里红了（今天实测）

### 1) main 的 CI 三个 job 恒红 —— 修件已在路上

| job | 根因（已定位到行） |
|---|---|
| `Vitest (1/2)(2/2)` | 3 份测试**写死了 cwd 相对路径**（`'../packages/...'`）→ 任何机器 ENOENT |
| `Dashboard Auto` | `dashboard-auto.yml` 的 generate job 有一个**孤儿 `fi`**（PR #592 引入的 bash 语法错） |
| `Product Progress` | `redeem-progress.py` 对 str 型 `impl` 抛 AttributeError（7 张存量卡） |

修件拆两台（按域拆，非内容退回）：**#701**（测试侧，win 代行）+ **#702**（控制塔侧）。
> 顺带一个教训：**main 红期间，任何"CI 绿"都不可信**——它可能只是没跑到。

### 2) 给你看的三块面板，全部停更

| 面板 | main 上的日期 | 停更 | 原因 |
|---|---|---:|---|
| `docs/synova/DASHBOARD-CN.md`（任务进展） | **2026-08-28** | 25 天 | 生成器 `gen-task-board.py` **没接任何 CI/脚本**，只能手工跑 |
| `app/founder-dashboard.html` | **2026-09-07** | 15 天 | 见下 |
| `docs/synova/founder-console.html` | **2026-09-08** | 14 天 | 见下 |

**关键发现**：后两块面板的**数据其实是新的**——bot 分支 `auto/dashboard` **今天 12:34 UTC 刚更新过**（`bba092bb`）。
**它们只是从来没合回 main**，而 Pages 从 main 构建。上一个 bot PR **#403 被关闭但未合并**（`merged=None`）→ 通道断在 09-07。
已开 **#707** 做一次恢复；**治本**是 D786（PR #584，bot 通道抢救），需要你点头提优先级——否则每天都要人工捞一次。

### 3) 未合 PR 队列

今天从 **39 件**开始清理。**其中 5 件的红，不是它们内容的问题，是门禁的问题**（见第四节）。

---

## 三、有没有被绕过

| 检查 | 结果 |
|---|---|
| `bypass.log` 24h 内非 PASS 记录（即 `--no-verify`） | **0 条** ✓ |
| `pre-commit-failures.log` 今日失败 | **0 条** ✓ |

**结论：今天没有被绕过。** 门禁的红是**误拦**，不是被人绕开。

---

## 四、今天踩到的系统性问题（需要你知道）

**门禁开始阻止修复门禁。** 三条缺陷互补，都不是执行方做错了：

| 缺陷 | 表现 | 同类第几次 |
|---|---|---|
| **D708 写集门禁取错任务号** | 分支名不含 `D#` 时，回退扫描**扫到了被并入的 main 历史**里的任务号 → 拿错模板 → 合法 PR 被判"夹带" | **第 2 次**（第 1 次是 #694，当时的根因判断是错的） |
| **域门禁不认「代行」** | 你 09-20 定了「win 离线期 domain:win 一律 Mac 代行」，但门禁不知道 → **任何修 win 文件的 PR 恒红** | 同族**第 5 次** |
| **G12 与 D708 口径互斥** | 同一 PR，过一个门禁必倒另一个；且"今日 brief"以 CI 运行日为准 → **同一提交今天红、后天绿** | 首次定性 |

**处置**：立 **D911**（3 切片 + 1 自验），写明"**不新增门禁机制**，只去误拦 + 把判据钉死"。
**按我的红线（同类错误第二次出现＝防线系统性失效），必须升级你**——这就是本节。

**今天已顺带实修**：`ownership.yaml` 补登记 CTO 治理文档三目录（#708，同族第 5 次的那次）。
**我这边的过错也一并记下**：两份 brief 的格式不合门禁（`## 写集` 机器块缺失、`Done 标准` 内联写法）→ 已改并写进教训。

---

## 五、今天合进去了什么

| PR | 内容 | 状态 |
|---|---|---|
| **#700** | **研究院 8 份交接文档 + README 入库**（D 切片权威来源底座） | ✅ 已合 |
| **#697** | **专职小队队长预设 🎽 synova-squad-lead** 落 main | ✅ 已合 |
| #701 / #702 | main 红修复（测试侧 / 控制塔侧） | 等 CI |
| #703 | D911 立卡 + 门禁三缺陷裁定记录 + 根治派单件 | 等 CI |
| #704 | D856 DSH 决策四项（取代 #685） | 等 CI |
| #705 | D912 研究院材料**升权威级** + 引用纪律（原误标 D850 撞号重编） | 等 CI |
| #706 | 卡登记补齐 D864/D865/D866–D870 + **D870 K3 复审派单** | 等 CI |
| #707 | 创始人面板恢复（bot 分支 → main） | 等 CI |
| #708 | ownership 补 CTO 治理文档三目录 | 等 CI |

---

## 六、员工健康

| 员工 | 本周期 | 评价 |
|---|---|---|
| 🛠 编码 | D861 修 main 红：**三条根因定位准确**，无假绿 | 交付内容合格；**交付形态与门禁不兼容**（域 / brief 格式）是主因在门禁 |
| 📋 dev-doc | 本周期无新交付 | — |
| 🔍 审计 | 本周期无新交付 | — |
| D870 小队 | **自查更正 3 处**（漏列文件、遗留漏登、措辞把自查写成队员拦下） | **记功**——主动更正且逐条留痕 |

---

## 七、等你裁决（4 件）

1. **D911 是否直接开工？**（门禁三缺陷根治；开工前置 = #702 落 main。编制：编码 3 + 自验 1）
2. **D870 改法卡 D871–D910（40 张）是否立项？**——号段我已预留，未占用
3. **dependabot 5 件要不要升**（vitest 5 / @types/node 26 / react / nodemailer 10）——测试与依赖大版本升级，影响面大，我不擅自决定
4. **`auto/dashboard` 通道治本（D786 / #584）是否提优先级**——否则你每天看到的面板要靠人工捞

---

*数据来源：`docs/synova/product-lines/product-progress.json`（09-20 23:23 生成）· GitHub Actions / PR API 实测 · `.claude/bypass.log` · `.claude/pre-commit-failures.log` · main @ `7f84f4f0`*
