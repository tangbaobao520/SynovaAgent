# 审计发现台账（DSH-CTO 维护）

> **本文件由 DeepSeek Harness CTO（Mac DSH，2026-08-16 起）维护**，用于识别区分：
> - **本文件（审计发现台账-DSH-CTO.md）** = DSH-CTO 维护的完整台账——**承接原 `AUDIT-FINDINGS-LEDGER.md` 全部历史内容（不丢失）**，DSH-CTO 相关发现/固化登记于此（新增内容用 🆕 标注）。
> - **原 `AUDIT-FINDINGS-LEDGER.md`** = 历史文件（Codex/K3 维护期），**保留不动**，不再新增。
> - 用途：把 K3 审计发现 + DSH 控制塔固化固化为**开发过程迭代的关键素材**——改进控制塔 + 改进 dev doc 编写标准。
> - 关联：[AUDIT-PROTOCOL](AUDIT-PROTOCOL.md) / [ROLES](ROLES.md) / 审计报告 `docs/synova/audit-reports/` / 原台账 [AUDIT-FINDINGS-LEDGER.md](AUDIT-FINDINGS-LEDGER.md)

## 一、审计发现台账（历史，K3 审计发现 — 自原台账继承）

| 日期 | D# | 级别 | 发现 | 根因（该拦的防线） | 修复 | 改进归属 |
|------|----|:---:|------|------------------|------|:---:|
| 08-12 | D328 | P1-1 | python 损坏 shim 时一致性门禁**静默漏拦**（2/6 败，物理复现） | fail-open 把"检查未执行"与"检查通过"压成同一 exit 0；`command -v` 只探存在性不探可用性 | D330（探测+三态） | 控制塔+skill |
| 08-12 | D328 | P1-2 | DS4 声称"四条豁免全部测试覆盖"，Revert/无暂存**无用例** | DS verify 不映射"声称项↔用例"（自报无法自证） | D330（补 2 用例+措辞） | skill |
| 08-12 | D328 | P1-3 | DS7 无物理证据（bypass.log 空窗，ea1cb71 落窗内） | bypass.log 无对账方，执行证据链断裂无人发现 | D331（对账） | 控制塔 |
| 08-12 | D329 | P1-1 | tag V4.7.1 指向**孤儿提交** f685fa0（版本锚点断裂） | 无 tag-祖先校验（amend 重提交后 tag 未跟随） | D331（重指+校验） | 控制塔 |
| 08-12 | D329 | P1-2 | dc369fd 无 bypass.log 记录（**同型缺口第二次**） | 对账机制未建立即再犯 = 防线系统性失效 | D331（对账强制） | 控制塔 |
| 08-12 | D329 | P2-1 | write-set 记录无 task_id（dev doc §3.1 声称过度） | 文档-实现漂移：声称"记录含 X"但未实现/未验收 | D331 | skill |
| 08-12 | D329 | P2-2 | resolver --session **零生产调用方**（机制建成未接线） | dev doc §5 接线验收写浅：没要求"生产调用点真实传递" | D331（接线+§5 升级） | skill |
| 08-12 | D329 | P2-5 | synova-commit:367 裸 python3 + `\|\| true` 吞失败（同文件刚修过 commit-msg） | PYBIN 未全局对齐；`\|\| true` 连崩溃都吞 | D331（升级 P1） | 控制塔 |
| 08-12 | D329 | P2 其他 | 测试无 runner 接线 / genuine except 无 degraded / brief 引用不存在文件 / DS6"同 commit"与 gitignore 矛盾 / DS9 verify --self-test 无效 | 多个"声称 vs 物理"小缺口 | D331 清理 | skill+控制塔 |
| 08-09/12 | Mac ba653c3 | P2 | engine-core 退役清理"grep 物理证明零引用"**声称不实**：tests/run-e2e-pipeline.cjs（死文件）残留 3 处 packages/engine-core 引用 | M2 声称 vs 事实（清理时 grep 范围漏 tests/*.cjs） | 待清理 chore | skill |
| 08-12 | Mac 清理 | — | 审计基线 439→**434**（78K 删除，-5 FAIL / -1 arch / -2 WARN）；admin-knowledge.ts:17 L1→L4 违规**仍在**（D309 待做） | 大规模删除改变基线契约 | 基线已更新（2026-08-12） | 控制塔 |
| 08-12 | D330/D331 并行 | P0 | **共享暂存区拉锯**：D331 re-add 抢占 → D330 被 D328 门禁拒绝 → 循环（防护在工作，暴露 D307 缺失） | git index 是 worktree 级单例；门禁仲裁替代物理隔离；依赖任务被并行派发 | D332（软加固）+ D307（根治） | 控制塔+skill |
| 08-12 | K3 权威偏差审计 | P0×1 | **N13 反馈→规则闭环断裂**（middle-evolution 零调用、feedback-collector 无消费方） | 写了没接线（M3 类） | D333（P0） | 产品 |
| 08-12 | K3 权威偏差审计 | P1×5 | direction-monitor 未接线 / loop-4 无专属处理器 / GA 仅 type import / 静默升级回滚未实现 / orgId 未全量验证 | 多为"机制建成未接线/口径滞后" | D334-D338 | 产品+文档 |
| 08-12 | K3 权威偏差审计 | 文档×2 | AGENTS.md 哨兵口径 20 vs 45+4；N14 去重窗口 5 vs 30 | 口径滞后（代码无缺陷） | D339（待创始人定 N14） | 文档 |
| 08-12 | K3 D330 复审 | P1×1 | **bypass.log 连续第三个任务无记录**（6c00e46/407ff1f）——防线失效本身，两次排队进 CT 无物理执行体发现 | 对账机制只在队列，无自动执行 | **升级 P0 流程事项：D331 check-bypass-log 立即执行（K3 已给最小实现 comm -23）** | 控制塔 |
| 08-12 | K3 D330 复审 | 技术事实 | broken-python3 + 可用 python 时门禁**仍不拦截**（resolver PYBIN 无探测先死）——D330 只"不静默"非"仍拦截" | resolver 硬化未含探测 | D331 折入（§2.6/DS13） | 控制塔 |
| 08-12 | K3 D330 复审 | P2×5 | resolver exit 1 二义 / 用例 10 标题矛盾 / DS8 基线 439（我已误写 434 已修）/ V4.7.3 幻影条目 / 测试无 runner | 文档-代码小漂移 + 版本编排副作用 | D331 折入 + 文档已修 | skill+控制塔 |
| 08-12 | K3 权威偏差 v1.1 | P1×5 | ①V5 视图硬编码旧 6 专家（D282 未传播，铁律 9 违规实例）；②gate-status.json 缺失（3/19 信号仅 3 文件）；③铁律 36 无全量 vitest 强制点；④铁律 37 无 dead-code 扫描（direction-monitor 存活 11 天）；⑤铁律 9 无传播检查 | 控制塔视图/信号/铁律门禁缺失 | D340-D342 | 控制塔 |
| 08-12 | K3 权威偏差 v1.1 | 改判 | D-G2"已修复"→"引擎已修复，数据链路未担保"（gate-status 缺失 + 快照 11 天 + 自检盲区）；组 11 并入组 4（12 组计数口径 → D3）；铁律 38 确认全仓扫描 | 复核深化 | 演示前 checklist + D339 | 控制塔+文档 |
| 08-14 | K3 全链路审计 | P0×3 / P1×5 / P2×6 | **Agent 核心能力全链路 FAIL（0/3 贯通）**：L5 无 CRM/财务/HR 连接器；L4 类型契约断裂（Market≠Client、People≠Person、Event/Tool 零写入方）+ 属性契约断裂（cashBalance≠cash）+ filter bug（compute-cash-runway-months.ts:60）；P0 哨兵阈值告警生产死代码（sentinel-loader 从不挂 manifest）；L4 查询层静默 fail-open（schema 漂移只 warn 返回空）。活运行证明 L3 计算能力本身是真的，断裂在数据进出两端 | L5 连接器缺失 + L4 契约失配 + manifest 死代码 + fail-open | D355-D360（见仪表盘） | 产品 |
| 08-15 | K3 D366 审计 | P1×3 / P2×5 | **CONDITIONAL PASS（无 P0）**：DS1-DS8 八项物理一致（newermt=0、4 处生产调用、无 rm、T1/T2 双侧 15/15+12/12 绿、写集 9/9、版本三同步），自报偏离 a-i 全部诚实。**T3 故障注入发现 2 个新引入边缘回归**：P1-1 git commit --amend 必触发 detected-bypass head-mismatch 误报（marker 记旧 commit、HEAD^ 恒不等，一次 amend=当日提交死锁，CT-29 同型）；P1-2 D 前缀 brief 被"今日集合"物理排除（today_files_by_prefix 只匹配 YYYY-MM-DD-*，D 前缀 session 提交代码文件被 G12 阻断，§2.3 记录两种命名但方案只覆盖一种）；P1-3 dev doc 从未进入实现分支树（契约链分岔）。项 13 CI pending DEGRADED | 判定语义层缺口（dev doc 引用 CT-28 却语义层重蹈覆辙） | D373 | 控制塔 |

## 二、模式归纳（跨审计复现的根因类）

| # | 模式 | 首次 | 再次 | 对应防线 |
|---|------|------|------|---------|
| M1 | **fail-open 静默失效**（检查未执行==检查通过） | D328 P1-1 | D329 P2-5（`\|\| true`） | 三态输出（pass/degraded/fail） |
| M2 | **声称 vs 事实**（doc/报告 overclaim） | D328 P1-2 | D329 P2-1（task_id）、Mac ba653c3（零引用声称） | DS verify 覆盖映射；写集声称=实现+验收 |
| M3 | **机制建成未接线**（WIRE CHECK 失败） | D329 P2-2 | — | 接线要求"生产调用点真实传递"（测试不计） |
| M4 | **执行证据链断裂**（bypass.log） | D328 P1-3 | D329 P1-2（第二次） | git log vs bypass.log 对账 |
| M5 | **环境依赖门禁**（python3/broken shim） | D328 P1-1 | D329 P2-5 | PYBIN 全局 + 可用性探测 |
| M6 | **版本锚点断裂**（tag 孤儿） | D329 P1-1 | — | tag-祖先校验（pre-push/CI） |
| M7 | **文档-实现漂移**（dev doc 未回填/声称未实现） | D329 P2-1 | — | dev doc 与最终实现同 commit 回填 |
| M8 | **共享暂存区竞争**（并行 session 共用 worktree index） | D330/D331（08-12） | — | worktree 隔离（D307）+ staging-guard 指引/事件记录（D332） |

## 三、控制塔改进队列（CT）

| # | 项 | 来源 | 状态 |
|---|-----|------|------|
| CT-1 | fail-open 三态（gate 输出区分 pass/degraded/fail） | M1 | ⏳ 健康审计队列（D330 只做 commit-msg 局部） |
| CT-2 | WIRE CHECK 升级（≥1 生产调用点，测试不计） | M3 | ⏳ 健康审计队列（D331 §5 局部落地） |
| CT-3 | bypass.log 对账（post-commit + 定期/CI） | M4 | 🔄 D331 折入 |
| CT-4 | tag-祖先校验（pre-push） | M6 | 🔄 D331 折入 |
| CT-5 | PYBIN 全局对齐（全 scripts 裸 python3 清零） | M5 | 🔄 D331 折入（synova-commit）+ 剩余点待扫 |
| CT-6 | CI job 级判定（生成器假红） | D320 审计 | ⏳ 未排 |
| CT-7 | pre-commit 性能/双重执行 | D319 报告 | ⏳ 未排 |
| CT-8 | baseline-check.test.sh 3 失败 | D318 报告 | ⏳ 未排 |
| CT-9 | 10 个 .sh UTF-8 头块 | D318 报告 | ⏳ 未排 |
| CT-10 | staging-guard 被拒带指引（活跃 session/归属/建议） | M8 | 🔄 D332 折入 |
| CT-11 | attach.py SessionStart 强制 register session | M8 | 🔄 D332 折入 |
| CT-12 | wait_manager 暂存区竞争检测/提示 | M8 | 🔄 D332 折入 |
| CT-13 | 提交被拒事件记录（parallel-conflicts.log） | M8 | 🔄 D332 折入 |
| CT-14 | 审计协议 L3 加"并行合规"检查项 | M8 | ✅ 已入 AUDIT-PROTOCOL（2026-08-12） |
| CT-15 | worktree 隔离落地（D307：独立 index/暂存区/current-brief + worktree-manager 生命周期） | M8 根因 | 🔄 D307 dev doc 就绪（2026-08-12，V4.8.0）— 待派发 |
| CT-16 | 进化闭环接线（N13/middle-evolution 零调用） | K3 权威偏差 P0-A1 | 🔄 D333（P0）待写 dev doc |
| CT-17 | **bypass 对账物理执行**（防"队列无执行体"——连续 3 任务无记录） | K3 D330 复审 P1 | 🔄 D331 check-bypass-log（升级 P0 优先级） |
| CT-18 | resolver PYBIN 可用性探测 + 退出码 0/1/2 语义化 | K3 D330 复审 | 🔄 D331 折入（§2.6/DS13） |
| CT-19 | 控制塔 V5 视图动态化（agent_health 读 expert-registry） | v1.1 P1-B1 | 🔄 D340 |
| CT-20 | 控制塔信号完整性（gate-status 回填 + 自检部分缺失升级） | v1.1 P1-B2 | 🔄 D341 |
| CT-21 | 铁律 36/37/9 门禁补全 | v1.1 P1-B3~B5 | 🔄 D342 |
| CT-22 | P2 结转治理（测试 runner 接线 / 用例标题 / brief 引用 / genuine degraded）——3 审计连续 carried，禁再 carry | 多份 | ⏳ 控制塔健康审计批次 |
| CT-23 | DS 对账机制（交付声明 vs dev doc DS 一一对应，禁重编号，缺项显式 descope） | K3 D331 L4 | 🔄 S-10 已入 skill；门禁化待健康审计 |
| CT-24 | resolver 硬化交付（DS13：PYBIN 可用性探测 + 退出码 0/1/2） | K3 D331 P1-1 | 🔄 D352 |
| 演示前 | D-G2 数据链路动作（回填信号 + 刷快照 + 验红灯） | v1.1 改判 | 🔄 并入 D341 |
| CT-25 | 循环执行体真实化（loop-2/3/4/5/6 placeholder 假成功 → 真实执行 + 修假 completed + middle-evolution 接入） | v2 P0-A1/P1-C2 | 🔄 D333（P0） |
| CT-26 | 铁律 38 扫描范围扩至 packages/（as any 清理或声称降级） | v2 P1-C1 | 🔄 D353 |
| CT-27 | N14 去重键稳定（finding.id 去时间戳） | v2 N14 | 🔄 D354 |
| CT-28 | **verify-parallel --scan-today 语义缺陷**（L135-146 只按当天 mtime 圈 doc 两两比对，不理解「依赖/接力顺序」——D332/D307 与 D331 写集重叠被误判并行冲突硬阻断；自愈=离开当天范围即放行。K3 审计仅验「门禁 5 存在+接线」，未覆盖判定语义=控制塔语义审计盲区） | 2026-08-13 并行拦截复盘（K3 盲区，本机发现） | 🔧 建议并入 D332 写集或新建补丁 |
| CT-29 | **pre-commit marker 并发缺陷**：post-commit 靠全局单例 `.claude/last-precommit-success` 检测 --no-verify 绕过，多 session 并发时一个 session 的 post-commit `rm` 掉 marker，导致另一个 session 的正常提交被误判 `detected-bypass no-precommit-marker`，反过来触发 GATEKEEPER 硬阻断所有提交（2026-08-14 文档拉平 D362 死锁实证）。修复方向：per-session marker 或按 commit hash 对账，替代单文件时间戳 | 2026-08-14 D362 文档拉平死锁 | 🔧 建议新建补丁（D363+） |
| CT-30 | **Secrets 门禁 .env 过拦**：check-secrets.sh 全工作区扫描 + 本地 .env 检查把 gitignored 未跟踪的 .env（本地密钥库，产品运行依赖真实密钥的正常状态）当违规硬阻断 → 2026-08-14 23:04 .env 写入真实密钥起**所有提交被误拦**（上一 session WIP 卡死实证）。修复（D370）：未跟踪 .env 豁免；被 git 跟踪/被暂存 .env 仍阻断（泄漏路径不变）。测试 tests/control-tower/secrets-env-exempt.test.sh 7 用例 | 2026-08-15 D370 门禁自检发现 | ✅ D370 已修 |
| CT-31 | **hook 测试缺 git 操作矩阵维度**：改动 hook 判定逻辑的交付，测试必须枚举"git 操作 × marker 状态"矩阵（至少普通/amend/reset 重提 × 匹配/不匹配/缺失/陈旧）——amend 场景打破"一次 pre-commit 对应一次全新 parent"假设，D366 只枚举了 marker 内容维度漏了操作维度 | K3 D366 审计 L4 发现 1（P1-1） | 🔧 并入 D373 + S 队列 |
| CT-32 | **判定语义变更缺"新旧过滤器等价集对账"DS**：判定类门禁的语义变更，DS 必须含新旧逻辑等价集对账（真实目录跑新旧过滤器，输出集合 diff 为空或逐项豁免）——D366 改了 mtime→文件名日期，但 DS 只验"newermt 清零+调用数"，没验"新旧今日集合等价"，导致 D 前缀 brief 语义回归（dev doc 引用 CT-28 却语义层重蹈覆辙） | K3 D366 审计 L4 发现 2（P1-2） | 🔧 并入 D373 + S 队列 |
| CT-33 | **分支与 brief 无对账**：G12 skip_re 豁免 docs/ 使文档类越界提交对 scope 门禁不可见（分支污染）；brief 头部"分支"字段从不与 `git branch --show-current` 对账（死文本）——commit 时 brief 声明分支与当前分支对账，或删除该字段 | K3 D366 审计 L4 发现 3（P2-5） | 🔧 并入 D373 或健康审计批次 |
| CT-34 | **文档提交豁免严格门禁（只保留 Secrets 扫描）**：文档（docs/、.claude/task-briefs/、memory/ 等）提交仅为跨机器（Mac/未来同事）同步信息，不应与代码同跑 13 组严格门禁。现状卡点（实证）：①组 6 时间戳顺序（before-brief 残留）不区分文档/代码，文档提交被误拦（D362 文档拉平 + D366 审计登记反复卡）；②G12b brief 可解析性对暂存 brief 触发；③task brief/Q2 范围/测试/接线/架构/契约对纯文档无意义。豁免：12 组（task brief + Q2 范围 + 时间戳 + 测试 + 接线 + 架构 + 契约 + 类型 + 文件驱动 + CP3 + scope + 技能）；保留：**Secrets 扫描**（文档同样泄密，D312 settings.json token 实证）。文档"真实性"不靠 pre-commit，靠 K3 审计兜底（声称 vs 事实复核）。归属：门禁脚本 scripts/pre-commit-check.sh 是 DSH 地盘，由 DSH 实现 | 2026-08-16 创始人决策 + D362/D366 文档提交反复卡门禁实证 | 🔧 DSH 排期（新 D#） |
| CT-35 | **写集对账门禁**（brief 声称文件集 vs 实际提交 diff 校验——组 12 只查"超出范围"不查"少于声称"；D383 P1-1 漏交无门禁） | K3 D383 审计 L4-1 | 🔧 D384 折入 + 门禁化待健康审计 |
| CT-36 | **D# 注册表**（task-state/ 充当编号占用登记） | K3 D383 审计 L4-2 | ✅ **D384 已实现**：alloc-task-id.sh（查占用+单调递增+建壳登记，测试 12/12）+ persona/skill 取号纪律落位 |
| CT-37 | **产物-数据源一致性**（gen-cto-health 渲染前重算数字，与已提交产物差异非时间戳 → 告警；防 CTO-HEALTH 数据无源） | K3 D383 审计 L4-3 | 🔧 D384 折入 |
| CT-41 | **仪表盘完整性专项（创始人确认盲区 6 项）**：① CI 状态入仪表盘（CT-39 并入）；② 审计结论联动 26 线（D347/D349）；③ P0 积压自动化（防 MANUAL 误清）；④ 跨机一致性（P2-8）；⑤ 历史任务补录；⑥ PR 合并状态集成 | 2026-08-16 创始人 | 🔧 待排（主线 dev-doc 写 4 spec 并行） |
| CT-40 | **控制塔测试接入门禁**（tests/control-tower/*.test.sh 不跑于 pre-commit/CI → D393 P0-1 红灯无物理拦截；M1 强化） | K3 D393 审计 P0-1 防线缺口 | 🔧 待排 |
| CT-42 | **current-brief session 专属接线**（D329 机制已存在，写侧 current-brief.$SESSION_ID D539 已闭环；读侧解析器 --session 生产调用点） | 2026-08-16 D394 并行冲突实证 | 🔧 D539 已闭环写侧；读侧待验证 |
| CT-44 | **gen-cto-health.py 审计派生多 D# 支持**——L214 `re.search` 只取文件名第一个 D#，批次报告（D483-D484-D486）只自动派生首个任务 audited，其余靠手工回填（K3 2026-08-29 提醒；治理冻结期，非 P0 暂不修） | K3 D483-D486 批审计 | 🔧 待排 |
| CT-43 | **post-commit auto-hook（D521 登记）提交限定路径**——auto-hook 提交只应含 `.claude/bypass.log`（`git commit --only`），现状会卷走暂存区遗留文件（D552 实证：D311 guard 阻断后 dsh/plugins 插件被卷入 8b6deaf4「bypass 登记」提交，M8/D286 同型变体） | 2026-08-28 D552 回填实证 | ✅ **D554 已修（V5.2.3，PR #280）**：`git commit -o -m ... -- bypass.log` 路径限定 + 配对测试 7→12 断言（场景D 卷带防护） |
| CT-45 | **派单证据指针（impl.commit）必须验证为 origin/main 祖先**——17 任务审计派单 7 处 hash 错误（4 个废弃分支提交不在 main + 3 个影子登记提交），根因 = 写派单时取分支本地 git log 而非 `git merge-base --is-ancestor` 对 main 权威验证（D536 教训③/D547 撞号同根） | 2026-08-29 CTO ③′ 物理复核发现 | ✅ **已修（PR #292）**：7 处回填真实 hash + task-state commit_note 记录 + 派单文档指针行更新；③′ 复核清单固化「证据指针 = merge-base 验证 main 祖先」 |
| CT-48 | **M3 家族第三次复发：新 hermetic 测试未入 CI canary**——D540 P2-1 → D549 P1-3 → D561 P2-1（g12/incident-loop 未入列）；D526 漂移告警已可见但只是 warn，无物理拦截。治本选项：A. D526 在 SYNO_CI 下 warn→hard（新测试不入 canary = CI 红）；B. 派单模板把「新测试入 canary」列为默认 Done 项（信息注入，效力弱） | 2026-08-29 K3 复审批 P2-1 | 🔧 本次已补 g12-day-window 入列（25→26，随 #305）；**incident-loop 首次 Windows 实测失败**（simulate-ci 连带，从未入过 canary=从未 Win 跑过）→ Win 兼容性修复归编码线（D561 后续小修）；**根治方案 A/B 待创始人裁决**（治理加严，防膨胀权衡） |
| CT-47 | **PR 合并门禁缺失（CI 红态仍可合并）**——K3 D537 L4：PR #223 分支 CI 红（CT ubuntu+windows 双 FAILURE，e4cb41ab job 级实证）仍被合并入 main，红态存续 ~2 天；pre-commit/pre-push 管不到 GitHub merge 通道（API/网页 squash 不经本地 hook） | 2026-08-29 K3 impl-done 批 L4 + **PR #299 第二次实证（merged_at 14:08:42Z 早于 Iron Laws 失败 14:10:18Z——检查未完成即合并）** | 🔴 **待创始人裁决加严**（第二次出现=防线系统性失效；修法候选：branch protection required checks / merge 前 check-runs 校验脚本 / PR 合并权回收 CTO） |
| CT-46 | **pre-commit 组 1 类型安全模式盲区（as never / as unknown as）**——组 1 只匹配 `as any` 字面量，`getDatabase() as never`（src/mcp/index.ts L236，K3 GA 线闭环批审计发现）逃逸 | 2026-08-29 K3 GA 线闭环批 | ✅ **已修（V5.2.7，PR #295，D559）**：组 1 模式扩 as never / as unknown as + 连带 resolver 认领日期窗口 ±1 天（UTC 时区同型）；测试 26/26 + 17/17；tag V5.2.7 |
| CT-39 | **CI 红超 24h 自动入 CTO 待办**（红常态化 = 信号失效 M1 同型；D387 P2-5 实证 CI 双红无人认领） | K3 D387 补核 P2-5 | 🔧 待排（CI 状态回写仓库后自动触发） |
| CT-38 | **新 .py 脚本必须有测试配对门禁**（pre-commit 组 2 只对 .ts/.tsx；gen-cto-health.py 329 行零测试漏网） | K3 D383 审计 L4-4 | 🔧 D384 折入 + 门禁化 |
| CT-45 | **brief 解析的「运行日本地化」缺陷（时区敏感）**：resolve-commit-brief.sh（TODAY 精确匹配）+ pre-commit 组 6/12 brief 窗口（组 12 = ±1 天、resolver = 运行日当天）按**运行机时钟**判定 brief 归属——CI runner=UTC、Mac session=CST(+8)，同一 brief 文件名日期前缀不可能同时满足两侧「今日」：session 本地提交绿 ≠ CI 绿（D556 实证两轮 CI 红：① `D556.md` 无日期前缀 → 组 12 ±1 窗口不可见 7 文件无认领；② `2026-08-29-` 前缀 → CI(UTC 08-28) resolver TODAY 精确匹配不可见 → 回退认领数最多的 08-28 老 brief → 组 6 六字段检查对错 brief 执行 1 处缺失）。修复方向：resolver/G12 的「今日」判定改 UTC 锚定或 task-state 占用号反查（任务状态信号，D557 信号4 同哲学——任务状态信号优于存在性/日期信号），禁用运行机本地时区日期 | 2026-08-29 D556 CI 两轮红 + date-shim 双时钟复现闭环（UTC 08-28 shim 复现 ❌、CST 08-29 绿——根因物理闭环） | 🔧 待排（控制塔领地，D556 以 brief 前缀=CI 当日 2026-08-28 临时绕过 + PR 注明 48h 合并窗口） |
| 权威18 | 审计体系冲刺（7 任务重编号 D343-D349）：D343 bypass A+B（P0）/ D344 报告git+dispatcher（P0）/ D345 doc-audit / D346 组13 / D347 JSON规范 / D348 CLAIM标签化（5份核心80%，按§5.5+验收#7）/ D349 JSON生成器 | 权威文档 18（2026-08-12） | 📝 D343-D349 待写 dev doc |
| 决策 | N14 去重窗口 ✅ 裁决 A（文档改 5 分钟，任务地图 v2 已改 2026-08-13）｜P0-8 boss 角色 ✅ 裁决 A（ENT 补 → D351）｜npm audit ⏳ 待裁（建议豁免并入 D309/D310） | 创始人 2026-08-13 | 🔄 D351 待写 + D339 含 N14 |
| 08-13 | K3 D331 复审 | P1×1 | **DS13 静默消失**：dev doc 承诺 resolver 硬化（PYBIN 探测 + 退出码 0/1/2）零交付，交付声明止于 DS12 无 descope——D330 L4#2 同构复发；broken-python 门禁仍不拦截、无 brief 仍误标 degraded | dev doc 完成标准 ⊆ 交付声明无对账机制 | D352（补做）+ S-10（skill） | skill+控制塔 |
| 08-13 | K3 D331 复审 | P2×4 | 25/25 vs 24/24 计数失真；brief 未入仓；"已写 memory"声称不实（第 2 次公式化声称）；测试无 runner（carried） | 声称-事实小漂移 | S-11（skill）+ carried | skill |
| 08-13 | K3 权威偏差 v2 | P0 加深 | **N13 根因加深：loop-3/5 名义进化通道是 placeholder 假成功**（loop-handlers.ts:4-5 "D9 未兑现"），每次 cron 触发写伪造 'completed' 记录（main-agent.ts:182-199）——断裂在仪表盘不可见 | placeholder 假成功 + 假审计记录 | D333（P0，吸收 D335/P1-C2） | 产品 |
| 08-13 | K3 权威偏差 v2 | 新 P1×2 | P1-C1：铁律 38 "as any 零存在"不实（组 1 只扫 src/，packages/ 36 文件 81 处无门禁）；P1-C2：4 默认循环处理器全 placeholder（吸收 P1-A2） | 门禁扫描范围盲区 + 假成功 | D353 + D333 吸收 | 控制塔 |
| 08-13 | K3 权威偏差 v2 | 改判 | 铁律 38 从"实证全覆盖"**退回**（packages 盲区）；D-G2 精确化（freshness/CP1 被动记录型，cp1 停 08-01）；N14 真问题是**去重键不稳定**（finding.id 含时间戳），窗口是二级 | v1.1 复核不完整 | D354 + 台账修正 | 控制塔 |
| 08-13 | K3 权威偏差 v2 | 文档 D5 | C线 S5-3/T-2/A线 B6 的 ✅ 基于 placeholder 接线，证据失去支撑 | 声称基于假执行体 | D339 扩 | 文档 |

## 四、dev doc 编写 skill 改进队列（Codex synova-dev-doc）

| # | 改进 | 来源 | 状态 |
|---|------|------|------|
| S-1 | **MANDATORY push+CI DS**（推送后 origin..HEAD 空 + CI 逐 job 绿） | D328/D329 未推送教训 | ✅ 已入 requirements.md（2026-08-12） |
| S-2 | 写集表"记录含 X"声称 = 实现 + 验收（禁声称过度） | M2 | ✅ 已入 requirements.md（2026-08-12） |
| S-3 | §5 接线要求必须"生产调用点真实传递"（测试调用不计） | M3 | ✅ 已入 requirements.md + template.md（2026-08-12） |
| S-4 | DS verify 命令必须有效且映射"声称项↔用例" | M2/DS9 | ✅ 已入 requirements.md + template.md（2026-08-12） |
| S-5 | 测试 red 必须覆盖"失败模式"（broken shim/劫持/绕过），非仅 happy path | M1/M5 | ✅ 已入 requirements.md + template.md（2026-08-12） |
| S-6 | dev doc §3.2 最终实现同 commit 回填 | M7 | ✅ 已入 template.md（2026-08-12） |
| S-7 | 依赖非空 → 禁止并行派发（派发说明标注 worktree 要求） | M8 | ✅ 已入 requirements.md + template.md（2026-08-12） |
| S-8 | 写集表标注共享资源（VERSION.md/current-brief/暂存区） | M8 | ✅ 已入 requirements.md + template.md（2026-08-12） |
| S-9 | 任务前置含环境检查（session-registry 活跃 session） | M8 | ✅ 已入 requirements.md（2026-08-12） |

## 四、DSH 防线映射（synova-dsh persona 免疫细胞）

> 2026-08-15 创始人批准：K3 审计错误闭环覆盖 DeepSeek Harness。DSH 无 PreToolUse hook，
> 免疫细胞形态 = persona 规则（`~/.dsh/.agent-presets/synova-dsh/agent.cordis.yml`）。
> 闭环规则：**同类错误第二次出现 = 防线系统性失效，必须升级给创始人。**

| M 模式 | 根因类 | DSH persona 防线 |
|--------|--------|------------------|
| M1 | fail-open 静默失效（检查未执行==检查通过） | SOP ⑤ verify 必须真实执行；验证失败 ≠ 通过；降级诚实（自检 5 问 2） |
| M2 | 声称 vs 事实（doc/report overclaim） | 汇报必须文件 + 行号；"拆完了"由 grep 物理证明（铁律 47） |
| M3 | 机制建成未接线（WIRE CHECK 失败） | 自检 5 问 1：新 export 谁调用（grep 确认调用方存在） |
| M4 | 执行证据链断裂（bypass.log） | 禁止 --no-verify；提交走 synova-commit |
| M5 | 环境依赖门禁（python3/broken shim） | 关键脚本 PYBIN 探测；脚本失败不算通过 |
| M6 | 版本锚点断裂（tag 孤儿） | 铁律 0-3：开工前 git fetch + pull --ff-only；禁止 behind 开工 |
| M7 | 文档-实现漂移（dev doc 未回填） | 交付汇报与实现同 commit 回填 |
| M8 | 共享暂存区竞争（并行 session） | 分支隔离（铁律 0-3/34）+ 认领制（D296） |

**DSH 侧审计发现闭环流程**：
```
K3 发现 → 查本台账 M 模式 → 归属判定
  · DSH 犯的错 → 记入台账 + 长成 persona 规则（免疫细胞）
  · 控制塔/工程缺陷 → 记入 CT 队列（DSH 领地，负责修）
  · 审计脚本/审计标准 → 只转达 K3，绝不修改 scripts/audit/（红线）
→ 记录 → 同类第二次 → 升级创始人
```

## 五、演进记录（DSH-CTO 维护期 🆕 自此追加）

| 日期 | 事件 |
|------|------|
| 2026-08-12 | 建台账；登记 K3 D328/D329 两轮审计发现（P1×5 + 关键 P2）+ 7 模式 + CT/S 队列 |
| 2026-08-12 | S-2~S-6 写入 synova-dev-doc skill（requirements.md + template.md），skill 改进队列全部落地 |
| 2026-08-12 | 登记 Mac 双提交（ba653c3 engine-core 清理 + 46b9271 D321 Mac 兼容）；审计基线 439→434；admin-knowledge D309 待做；run-e2e-pipeline.cjs 残留待清；分支分歧待合并 |
| 2026-08-12 | D330/D331 共享暂存区拉锯事件 → 根因三层（物理/设计/流程）+ M8 模式 + CT-10~14 + S-7~9；D332 dev doc 就绪（V4.7.4）；PARALLEL-DISCIPLINE.md 建立 |
| 2026-08-12 | **D307 dev doc 补齐**（长期 backlog 未落地，本次承认疏漏并补写；V4.8.0 物理根治）→ CT-15 |
| 2026-08-12 | K3 权威偏差审计（A线复核）登记：P0×1/P1×5/文档×2 → D333-D339 队列；已闭环×4 防重定案；G6 改判；快照 08-01 观察项 |
| 2026-08-12 | K3 D330 复审：CONDITIONAL PASS（P1-1/1-2 独立复验通过，加分项诚实标注）；bypass 连续 3 任务无记录 → 升级 P0；resolver 探测折入 D331；DS8 基线 439 修正（我误写 434） |
| 2026-08-12 | K3 权威偏差 v1.1：P1-B1~B5 → D340-D342；D-G2 改判；铁律覆盖总表（36/37/9 无门禁）；组11 修正；D-G2 数据链路演示前动作；P0-8/N14 待决策 |
| 2026-08-13 | 权威文档 18（审计体系）登记：D# 冲突重编号（研究方案 D331-337 → D343-349，7 任务）；代码现状核（dispatcher/doc-audit-interface/claim-tag-spec 已存在，doc-audit.sh/bypass-log-reconcile.sh 缺失，报告未 git 跟踪）；标注方案内部矛盾（§五迁移 vs §5.5 不迁移，按 5.5+验收#7） |
| 2026-08-13 | 创始人裁决 N14=A（文档改 5 分钟已落实）+ P0-8=A（ENT 补 boss → D351）；建立"创始人待裁决区"（npm audit 仍待）；复盘：待裁决项此前只登记未主动提出，流程已纠正 |
| 2026-08-13 | K3 D331 复审：CONDITIONAL PASS——D329 的 P1/P2 全部落地（bypass 证据链首次完整）；P1-1 DS13 resolver 硬化零交付（D352 补做）；P2×4；L4 DS 对账机制 → S-10/S-11 已入 skill |
| 2026-08-13 | K3 权威偏差 v2：N13 根因加深（placeholder 假成功 + 伪造 completed）→ D333 扩为循环执行体真实化；铁律 38 改判退回（packages 81 处 as any）→ D353；N14 真问题去重键 → D354；D335 吸收；D5 文档 → D339 |
| 2026-08-13 | 决策参考机制建立（DECISION-REFERENCE.md）：四步框架（第一性原理→Anthropic→DeepSeek 开源实证→收敛检查）+ 记录参考系强制；S-12 入 dev-doc skill（多选项任务必填决策参考小节 + 完成报告决策记录） |
| 2026-08-15 | **DSH 审计闭环建立**：① 决策模式（D333）写入 synova-dsh persona——技术决策自决四步 + 记录参考系 + 收敛直接执行，只有产品/业务决策问创始人（创始人无代码基础，禁止转嫁技术决策）；② 审计免疫写入 persona——K3 发现 → 查台账 M 模式 → 归属判定（DSH 犯错→persona 规则；控制塔缺陷→CT 队列；审计脚本→只转达 K3）；③ 本台账新增 §四 DSH 防线映射（M1-M8 → persona 规则）；④ 台账维护权扩至 DSH（创始人批准） |
| 2026-08-16 | 🆕 **哨兵口径核实（D378，CTO 审计后更新）**：哨兵为双体系——文件驱动 45 个（extensions/sentinels/，42 个在顶层 manifest + 3 个规范外：path-dependency / sentinel-forecast-accuracy / sentinel-pricing-strategy）+ 内置适配器 4 个（src/sentinel/adapters/）= **49 活跃**，另有 12 个退役（_extinct/）。**发现 3 个新事实**：①交接文档与 AGENTS.md 的「26/20 哨兵」口径过时（源头=台账 D339 计划名未落地，D339 编号被 quotepath 修复占用，编号冲突）；②**path-dependency 哨兵空壳**（manifest entryPoint 指向不存在的 computes/detect.ts，registerLoadedSentinels 将报错，实际可注册 44/45）；③台账 D360「规范外哨兵 2 个」应为 3 个。修正：AGENTS.md / cto-handover skill（.claude+.dsh）/ DASHBOARD-CN 三处口径统一为 45+4=49；D360 计数修正。**遗留：path-dependency 空壳补实现（归属 DSH 哨兵切片）** |
| 2026-08-16 | 🆕 **控制塔固化 D381（maker 建议 → CTO 独立验证后实施，待 K3 审计）**：① `dev-doc-gatekeeper.sh` 新增 **C6 写集表存在性检查**——`devdoc_writeset.py --extract` 提取失败 = FAIL 阻断（堵 `check-dev-doc-write-set.sh` 的 fail-open：无写集表曾 SKIP + exit 0，M1 活实例）；② `devdoc_writeset.py` 容忍写集表标题后空行（解析器脆弱性修复）；③ dev-doc persona 新增三条纪律（写前必读 D352 范例对齐 7 样结构 / 格式契约：写集表标题 + §8 只用「Wiring Verification」/ 接线 grep 实测 + read 被调用方真实定义）；④ 顺手修 pre-existing 假失败：`test-dev-doc-gatekeeper.py` 引用不存在的 .py（D212 后回归 .sh 测试未跟上，32512 command not found，M7 漂移）。**测试：新 9/9 + 既有 5/5 全绿。改门禁 = 无豁免，待 K3 审计。生效条件：dev-doc session 重启加载新 persona。** |
| 2026-08-16 | 🆕 **任务状态机 D382 建立 + 审计闭环铁律（创始人裁决）**：① 新增 `task-state/`（README 状态机 + TEMPLATE + D356/D379 示范），「dev-doc spec + 代码 + 审计报告」三产物唯一串联点；② **创始人裁决：K3 审计出问题 → 一律另起 FIX 任务，禁止直接改原任务（证据链混淆）**——原任务已交付+写集 close，塞回修复污染证据且 K3 无法区分原问题与修复质量；折入例外需 CTO 判定（同领域+进行中+改动小）并标注；③ 第③面 CTO-HEALTH 新增 §五 任务状态汇总（读 task-state/ 显示 spec/impl/audit 三态）；④ cto-handover 技能固化审计闭环铁律（§八b）；⑤ dev-doc persona 加步骤 ⑧（spec 后更新 task-state）、编码 persona 加步骤 ⑦b（impl 后更新 task-state），已落位（重启生效）。**阶段 2（K3 JSON 自动填充）/ 阶段 3（门禁强制）待做。** |
| 2026-08-16 | 🆕 **D387 审计（K3）— CONDITIONAL PASS（无 P0；P1×2）**。CT-34 纯文档豁免门禁（pre-commit 白名单 + Secrets 保留），机制物理验证全绿（12 用例 17 断言、白名单矩阵、fail-closed、混合负测被拦、基线 435=435、north-star 对齐）。**P1-1（安全级）**：SYNO_GIT_CACHED_* 注入缝无武装守卫 + 豁免事件零审计落盘——组合 SYNO_SECRETS_ROOT 构成**无痕迹全 13 组旁路**（post-commit 检测失效，比 --no-verify 差一个审计维度），归 M4 强化；修复 = exempt.log 落盘 + SYNO_TEST_ARM 武装守卫（**已排 CT-P1-1**）。**P1-2**：CI 不可核（网络超时），需创始人本地核（git log origin/feat/d387-doc-commit-exempt..1c72c2be 为空 + CI 逐 job）。P2×4（行号未回填 M7 / DS14 交付声明无载体 M2 / 基线 434→435 漂移观察 / 豁免面 task-state 单点依赖 K3 审计观察）。报告: docs/synova/audit-reports/2026-08-16-D387.md。 |**补核（2026-08-16）→ verdict 转 PASS**：推送 range 空（1c72c2be 已推）+ CI 6/8 绿、2 红预存（admin-knowledge.ts:17 D309 + npm audit）本地复现排除 D387。**新发现 P2-5（M1 同型）**：CI 双红 ≥16:03 无人认领 = 信号失效 → D391 派单（admin-knowledge 修复）+ CT-39（CI 红超 24h 自动入待办）。**创始人裁决（2026-08-16）：npm audit 豁免**——33 漏洞在 node-gyp 编译链非产品运行时 + 本地部署攻击面不成立；CI 改 continue-on-error 黄灯；触发升级条件 = 上云/公网化。 |
| 2026-08-16 | 🆕 **D383 统一批次审计（K3）— CONDITIONAL PASS（无 P0；P1×4）+ M4 第三次复发（升级创始人）**。报告: docs/synova/audit-reports/2026-08-16-D383.md（审计工作区，已提交 99820d6）。通过面：C6 fail-open 已堵（9/9+5/5 物理复跑）、哨兵口径 45+4=49 属实、基线 434 未恶化、推送属实、north-star 对齐。**P1×4**：① 写集漂移（brief 声称 25 文件实际 23——TASK-ROUTING 未提交 + D356/D379 spec 未入库，M2 类）；② D382 编号撞车（task-state 状态机 vs dev-doc 线 doc-commit-exempt 共用 D382，D339 教训重演）；③ CTO-HEALTH 产物数据无源 + D383 无 bypass.log 记录（**M4 第三次触发**——D328→D329→D383；根因含 CTO 操作：git reset --hard 回滚致生成时 bypass.log 中间版本丢失，产物不可复现）；④ gen-cto-health.py 幂等声称不实（时间戳内嵌恒写文件）+ 329 行零测试。**L4 缺口 4 项**（写集对账门禁 / D# 注册表 / 产物-数据源一致性 / 新脚本测试门禁）→ CT 队列。**按铁律：P1 修复另起 FIX 任务（D384），禁直接改原任务 98b0d4d1。** |
| 2026-08-16 | 🆕 **战略定调（创始人确认）**：Win 研究《Harness与Synova战略再定位》（11 文件，PR #38）——**战略三铁律**：① 不再自研通用 Agent 工程（已被 Harness 底座化）；② 工程预算压到 8 项【独有】领域能力（42 边本体/专家/测量器哨兵/诊断方法论/GA 闭环/行业模板/增长导航/无数据诊断）；③ 定位与应用场景不动。**路径**：A 迁移底座=Q4 后评估 / B 范式借鉴=主路径（D1-D5，9 月启动）/ C 生态并存=领域层成熟后。**OpenViking 威胁**（AGPLv3 自进化上下文数据库覆盖 S5-3/S5-4）：只借机制或 HTTP 服务隔离，不复制代码（红线）。借鉴 B1-B8 对应任务 D394-D398。 |
| 2026-08-16 | 🆕 **D393 审计（K3）— FAIL（待修复）**：状态派生改造交付态自指矛盾——P0-1 验收测试红灯（D393 提交自身=impl 工件，断言 claimed 永假）+ P1×4（CTO-HEALTH 提交态不可复现 json 未入库 / spec 双向失真 glob 漏检+幻影 / MANUAL 区误清 / 08-16 门禁记录未入库 M4 苗头）+ P2×8。跑偏三问对齐（铁律 35 方向正确）。报告: docs/synova/audit-reports/2026-08-16-D393.md。 |
| 2026-08-16 | 🆕 **D399（D393 FAIL 修复，另起 FIX 任务）**：P0-1 impl 语义统一+断言对齐交付态（测试 7/7）；P1-1 D385/D391 json 补入库；P1-2 spec glob 兼容+幻影消除；P1-3 MANUAL 区恢复；P1-4 bypass.log 记录落库；P2-1 degraded 标记。提交 082326fc。**待 K3 复审（只审变更）**。P2-6（控制塔测试接入门禁 M1 强化）→ CT-40。 |
| 2026-08-16 | 🆕 **K3 战略咨询（D394-D398，创始人定以咨询为准）**：报告 docs/synova/audit-reports/2026-08-16-D394-D398-strategy-consult.md（381 行，北星三问自裁决）。**调整**：D396 提前 P0 同批（黄金用例固化回归门禁）；D394 改切片（片1 哨兵 findings 事件化先做 2-3 天 / 片2 诊断会话事件化 6-8 天部署后 / 片3 fork/resume Q4 期权）；D395 拆（a 开发 Notes 四态 1 天现在做 / b 并入 D398）；**D397 现定义砍**（控制塔通用化并入 CT 队列），重定义 D397' 产品 loop 卫生 P2；D398 排最后（数据回流后，成功标准=GA 确认率）。第 6 项发现（sentinel-service.ts durationMs 当时间戳 bug，恒 1970）并入 D394 片1。核心一句：护城河=本体被真实数据验证的速率，排序按哇呢宝贝数据回流倒推。**背书**：战略三铁律、路径 B、演示部署优先、OpenViking 红线。任务卡改写归 dev-doc 线。 | **终版（436 行，交叉佐证）补 4 处**：① 叙事红线（D398 会话变记忆不得宣称越用越准，S5-1 未闭环）；② Sylph 已开源模板层（护城河=因果诊断深度×数据验证速率，非知识库模板）；③ 插件供应链红线（装有产品仓库的机器不装第三方 dsh 插件，白名单审批，近期风险）；④ S4/S8 部署期做深（GA 闭环+无数据诊断优先级高于一切借鉴）。自我修正 1 处（subagent 过述软化，归因 audit 未读附录）。加强：带宽预算硬约束（借鉴类任务月度 ≤25% 工程带宽）；OpenViking HTTP 集成现在不做留 D398 片2 决策。
| 2026-08-16 | 🆕 **分工规划（创始人定）**：**Win 继续 D355-D360（P0 主线，已完成 2 项，不打断）**；战略借鉴按领地分配——**Mac DSH 现在做**：D396 黄金用例回归门禁（P0 同批 2-3 天）+ D394 片1 哨兵 findings 事件化（2-3 天）+ D395-a Notes 四态（1 天）；**Win 部署后**：D394 片2 诊断会话事件化（6-8 天，依赖 D355-D360 契约）+ D397' 产品 loop 卫生（Q4）+ D398 组织记忆（数据回流后）。带宽约束：借鉴类任务月度 ≤25%（K3 终版）。信息传递靠 git 文档（研究/K3 咨询/台账已落 main），不靠人在哪。 |
| 2026-08-16 | 🆕 **D391 审计（K3）— CONDITIONAL PASS（P1×2 闭合后合 PR）**：admin-knowledge L1→L4 修复物理验证全绿（基线 435→433 零回归、CI 逐 job 验证）；P1-1 federated 兜底写入即蒸发返回 201 假成功（功能性）；P1-2 无 dev doc/brief 偏离 D309 方案（流程性）。→ **D402 FIX**。报告: docs/synova/audit-reports/2026-08-16-D391.md。 |
| 2026-08-16 | 🆕 **并行冲突实证（D394 交付报告）：共享协调文件 current-brief 并发不安全（M8 变体）**——本机两个 DSH 会话（D394 编码 + D396）并行，`.claude/current-brief` 全局单文件被互相覆盖 → G12 误解析对方 scope 拦提交；编码 session 用「提交前重写 current-brief」临时绕过。**本质：D329 session 专属 brief（.claude/current-brief.<sid>）机制已存在但 DSH 会话未启用**（resolver --session 支持读专属文件）。处置建议：① 启用 session 专属 brief（D329 机制接线）；② 或并行任务串行化；③ verify-parallel 对协调文件的并发检测。→ CT-42。 |
| 2026-08-17 | 🆕 **D395-a 审计（K3）— CONDITIONAL PASS（P0×0/P1×2/P2×5）**：spec 主体合格（§2 声称 7/7 物理命中、对 K3 咨询保真度高、诚实偏离典范、写集零重叠、CI 8/8 绿）。P1-1 层1门禁声称无物理落点（pre-commit 不在写集）；**P1-2 写集漏列 check-lessons-learned.sh 改向（腐化通道未关，优先）**——已交付缺陷（impl 已合 main 71e0620 核验）。→ **D406 FIX**。并发事件：D396 审计 session 分支误提交已非破坏性修复。报告: docs/synova/audit-reports/2026-08-17-D395a.md。 |
| 2026-08-17 | 🆕 **D396 审计（K3）— PASS 无条件**：golden-case 三层快照门禁（9aaf0cde）红-绿演练独立复现 exit1→exit0、CI 8/8 绿、基线 433 零漂移、写集零偏离 + M7 回填生效。P2×4（M1 ci.yml \|\|true 复证未愈转待办）。报告: docs/synova/audit-reports/2026-08-17-D396.md。**P1-1 descope（D395-a 层1 grep 强制）——2026-08-17 创始人授权放弃，层2 commit-msg 物理门禁兜底。** |
| 2026-08-17 | ⏳ **CT 线任务未启动（创始人提醒）**：CT-35~CT-42 队列全部待排（CT-36 已实现、CT-37/38 折入 D384，剩余 CT-35 写集对账门禁 / CT-37 产物-数据源一致性 / CT-38 新py测试配对 / CT-39 CI红超24h自动入待办 / CT-40 控制塔测试接入门禁 / CT-41 仪表盘完整性专项 6 项 / CT-42 current-brief session 专属接线）。**创始人将补充指示（2026-08-17，待补充）**。 |
| 2026-08-21 | 🆕 **控制塔冻结决策（创始人定 + LLM-as-a-Verifier 佐证）**：近 7 天 233 提交中 chore+docs=149（64% 流程开销）、feat 仅 52（22%）；57 次门禁拦截中 56% 拦控制塔自己（32/57）——控制塔自我膨胀在吃开发带宽。**verifier 打分：冻结 1.0 vs 继续加固 0.0**。决策：①**控制塔进入冻结期**——暂停 CT-31/32/33 等锦上添花，带宽还给产品开发（GS 场景/部署主线）；②**worktree 收尾强制 = 必修**（D402/D445 实现躺分支未合并差点当孤儿删，verifier 0.9999）；③**CT-42 写侧挂起待验证**（current-brief 已去跟踪 D458，可能已间接解决，verifier 0.579 vs 0.5 分歧=证据不足不拍板）。记录于 docs/synova/coordination/DECISION-控制塔冻结-20260821.md。 |
| 2026-08-21 | 🆕 **DSH 迁移 Mac/Win 分工规划定稿（CTO，基于施工图 DOC-0114 全文 + 代码核对）**：① **D357 连接器修正**——之前"交回 Win 冻结"是误读（把连接器当 🔵 借DSH 层），施工图 §3 第75行明确 `src/connectors/` 归 **🟢 死守**（无数据诊断三路径），终态经 MCP 消费但连接器本体持续投入。修正：D357 = 🟢 死守，Win 继续开发。② **迁移轨冻结**：当前是部署轨窗口期，Stage 0-4 全部冻结（Stage 0 部署后/Stage 1 9~10月），带宽全给 GS 场景跑绿 + 哇呢宝贝部署。③ 分工规划落 TASK-ROUTING（D357 修正 + 迁移轨归属行）。规划文档：docs/synova/coordination/DSH-迁移分工规划-20260821.md。 |
| 2026-08-21 | 🆕 **黄金场景全景重梳（CTO，基于 evidence JSON + field-mapping 源码 + git 证据核实，非凭记忆）**：**8 场景中 4 个已落地 main**——GS-03 全绿（3/3）、GS-05 全绿（4/4）、GS-02 诚实 RED 2/3、GS-04 诚实 RED 2/3。**契约错位根因（源码核实）**：GS-02 = `extensions/ontology/field-mappings/crm-standard.json` 缺 revenue/churn（customer-demand-shift 无法触发 critical）；GS-04 = `hr-standard.json` 缺 name/domains/role（key-person-risk 无法识别关键人个体，现有 7 字段全是组织聚合指标）。**归属裁决**：field-mappings 在 extensions/ 下归 Win（TASK-ROUTING 第38行 + D355 同型先例 = Win 做的契约收敛），故**契约错位修复归 Win**；Mac 编码 session 已正确交付场景脚本（诚实 RED 是正确行为，符合 K3 P0-3 防假转绿），下一步是等 Win 补字段后重跑验证。**GS-01/06/07/08 前置**：D232/D233 Electron（Mac DSH 领地）/ D333 loop 执行体 / D338 orgId / 报告模板（K3 定稿）。**部署提醒**：D463 是 L2/L3 产品变更，合并后需跑 checkpoint-deploy.sh。分工文档已更新（DSH-迁移分工规划-20260821.md §一/§四）。 |
| 2026-08-21 | 🆕 **CTO 反馈（创始人裁决：门禁 bug 不能冻结，需修复）**：创始人据 codex（Win）PR #69 反馈的两处门禁坑裁决——**门禁 bug 是"减负"不是"加固"，不属于 08-21 冻结范围，必须修**。**CTO 接受并澄清冻结边界**：08-21 冻结决策冻结的是 CT-31/32/33 等"新增加固项"（锦上添花），**"修已有 bug"（尤其消耗执行带宽的）是减负，与冻结目标一致，不在冻结范围**——之前边界划粗了，把门禁 bug 修复也笼统归入冻结。**两个必修项（codex 反馈）**：① **G12c 中文文件名 quotepath 盲区**——架构文档中文文件名被 core.quotepath 转义导致 grep 匹配不上，K3 早已上报但一直未修（脚本归 DSH 地盘），codex 本次又踩、靠 GIT_CONFIG 临时开 quotepath=false 绕过；② **D331 rebase 哈希对账断裂**——rebase 改哈希致 bypass.log 完整哈希对账反复失败，codex 按 D451 豁免流程反复 amend 补记才通过。**处置：由 CTO（DSH）修复 scripts/，不冻结。** |
| 2026-08-21 | 🆕 **门禁减负修复完成（CTO 实施，两处 codex 反馈坑 + 1 存量发现）**：① **G12c 中文文件名 quotepath 盲区**——`check-dev-doc-write-set.sh` 3 处裸 `git diff`（正向核验/反向对账/DOCS 提取）+ `pre-commit-check.sh` 6 处 `GIT_CACHED_*` 源头，全部加 `-c core.quotepath=false`（synova-commit/commit-msg 早已加，这两处是漏网）。测试：check-dev-doc-write-set.test.sh 增 2 用例（接线 grep ≥3 + 真实 git 中文名还原），6/6 通过。② **D331 rebase 哈希对账断裂**——根因非脚本 bug，是流程文档 + 门禁提示把大家引向 `git rebase`（改 hash → full-hash 对账必裂）。修法：`pre-push-check.sh`/`check-branch-sync.sh`/`MULTI-MACHINE-PR-WORKFLOW.md` 三处提示从「推荐 rebase」改「推荐 merge（不改 hash）」，rebase 降为备选并注明「改 hash 需按 D451 补记」。测试：check-branch-sync.test.sh（原 branch-sync-guard.test.sh 重命名对齐 CT-40 配对）断言同步，11/11 通过。③ 顺手补 4 脚本 UTF-8 头块（D313 M5）。**存量发现（本次未修）**：tag-bypass-wiring.test.sh 4 项失败（check-bypass-log base 解析 fail-open + staging-guard shim PYBIN 回退），非本次改动引入，独立待修。 |
| 2026-08-21 | 🆕 **控制塔减负审计落地项1（CI gap 补齐，D465）**：审计发现 CI 上 pre-commit-check.sh 空暂存（clean checkout 无 --cached）→ 依赖暂存区的增量检查（as-any/测试配对/接线/brief/scope）全部空跑假绿。修法：① pre-commit-check.sh 生产路径加 CI 模式——仅 `GITHUB_ACTIONS=true` + `SYNO_DIFF_BASE` 注入时用 `base...HEAD` 替代 `--cached`（本地环境 GITHUB_ACTIONS 非 true → 忽略注入，堵 D390 注入缝旁路）；② ci.yml quality job 注入 `SYNO_DIFF_BASE=origin/main` + `fetch-depth:0`。验证：三点 diff 语法实测（main...HEAD 正确列出 PR 变更）+ 本地模式不回归 + 现有测试 6/6+11/11。**剩余待拍板（动铁律）**：方案1挪CI权威、方案2状态分两类、方案3同步降频——见 docs/synova/coordination/控制塔减负审计-20260821.md §四。 |
| 2026-08-21 | 🆕 **控制塔减负落地项2（D466）**：① check-bypass-log.sh 头部注释 fail-open→fail-closed 同步（D414 改了代码没改注释，误导开发者）+ tag-bypass-wiring 用例5断言认 fail-closed exit2；② tag-bypass-wiring 用例6/7 跨平台修复——macOS 系统自带 /usr/bin/python3，CLEAN_PATH 只剔除含 python 目录漏掉 /usr/bin，PYBIN 遍历先命中系统 python3 回退 shim 永不触发；修法 shim 同时提供 python+python3 + sys.executable 绝对路径。tag-bypass-wiring 24/24（原4失败）。**另：测试污染 bypass.log 根因已定位**——synova-commit PROJECT_ROOT 用 `$SCRIPT_DIR/../..`（脚本位置）而非 git rev-parse（cwd 仓库），测试沙箱跑 synova-commit 写真实仓库状态；正确修法需区分 REPO_ROOT/SCRIPT_ROOT，属 K3 P2-4 测试密封性专项，本次未修。 |
| 2026-08-21 | 🆕 **控制塔减负——方案落地依赖链厘清（CTO，round 3）**：核心砍流程（方案1/2/3）落地依赖已查清：① **方案1挪CI**——前置是 CI 的 `SKIP_AS_ANY`/`SKIP_EMPTY_CATCH` 需先清存量（47 处 as any + empty catch，产品代码活归编码 session），否则本地软提示后 as-any/empty-catch 无人拦（铁律38 47次教训）；本地 pre-commit 软提示 + degraded marker 是 CTO 可控部分。② **方案2状态分两类**——current-brief/session-registry/last-precommit-success 会话态 gitignore（部分 D458 已做）；task-state claimed 态本地化动 D382（K3 审计串联点）。③ **方案3同步降频**——动铁律0-3 防覆盖节奏。**结论**：方案1/2/3 主体需跨 session（编码 session 清存量）+ 创始人拍板（铁律0-3/D382），非 CTO 单轮可完；已完成的不碰铁律减负=D465 CI gap + D466 注释同步/测试跨平台。 |
| 2026-08-21 | 🆕 **方案1挪CI核心落地（D467，创始人授权完全实现）**：① pre-commit hook 硬阻断→软提示——门禁失败写 GATE_FAIL_SOFT 到 bypass.log（K3 审计证据）+ 软提示 + exit 0，CI 权威（merge 前必须绿）；② as any 检查全仓库 grep→只拦新增（限制 src/ + 注释排除，消除自引用误报）——关键纠正：as any 存量用完整逻辑匹配实为 0（此前宽泛 grep 28 处含注释，误判）；③ CI 去无效 SKIP_AS_ANY/SKIP_EMPTY_CATCH（pre-commit-check.sh 不读该变量 + 存量已 0）。**量化减负（创始人问"省多少时间/瘦身成怎样"）**：本地 13 组硬阻断→软提示，每 session 每天省 20-40 分钟流程摩擦，3 session 约省 1.5-2 小时/天；本地 hook 4→2、脚本砍约 1/3。测试回归 6/6+24/24。剩余：方案2 task-state claimed 态本地化、方案3 同步降频、post-commit 三判简化。 |
| 2026-08-21 | 🆕 **方案3同步降频落地（D468）**：砍 D335 提交前同步——synova-commit 不再调用 check-branch-sync.sh，删除 check-branch-sync.sh + test 死代码（铁律37）。提交时不再强制"基于最新 main"（拉平降到任务边界一次），push 时 D334 门禁 0 单端兜底（防覆盖物理保障保留，历史 D334 前双机互覆盖 11 commit）。剩余：拉平降频流程文档 + post-commit 三判简化（CI 权威后本地绕过检测弱化）。 |
| 2026-08-21 | 🆕 **控制塔减负收尾判断（CTO，方案3文档落地）**：MULTI-MACHINE-PR-WORKFLOW.md 补记同步降频（第7条：D335 提交前同步已砍，开工前拉平 + push 前防覆盖保留）。**两项剩余优化判断"不做"**：① post-commit 三判简化——三判运行 <1s（非运行摩擦），复杂度是代码维护负担，CI 权威后 --no-verify 后果已弱化，简化收益低且涉及 K3 审计证据链，保留；② task-state claimed 态本地化——claimed 态是 D296 认领制 + D382 K3 审计串联点，本地化会丢失认领证据，收益（减少低频冲突）低于成本（弱化 K3 审计），不做。**控制塔减负核心已完成**：方案1挪CI（D467）+ 方案2会话态gitignore + 方案3同步降频（D468）。 |
| 2026-08-22 | 🆕 **Stage 1 范式借鉴首批落地（D472/D473/D474，dev-doc 写 spec → 编码实现 → 复核修复）**：三个任务合并——D472 Notes 四态生命周期闭环（注入过滤+迁移门禁+字段契约）、D473 guard 循环卫生分级+工具超时策略、D474 snapshot keyless 录制+黄金数据集门禁。**复核（claim-verifier）发现并修复 4 个真实问题（3 高 1 中）**：① TS2322 类型违规（tools.ts withTimeout 返回 Promise<unknown>，vitest 不查类型 + pre-commit 组1 只查 as any → 31/31 绿掩盖类型违规）；② main 入口阶段5失败误绿（isMainModule 只看 failedCases，黄金数据集阶段失败时 exit 0）；③ CT-34 纯文档豁免绕过迁移门禁（proposed Note 命中白名单早退 exit 0）；④ timer 泄漏 + block 路径 level 不一致。**系统性盲点（需强化）**：vitest（esbuild）不查类型 + pre-commit 组1 只查 as any → 类型错误可能静默通过测试，建议在 verify-incremental L2 或 CI 强化类型检查，记 CT 队列。 |
| 2026-08-22 | 🆕 **Mac/DSH 线编号改 D500 起步（创始人定，D471 与 Win 段撞车）**：alloc-task-id.sh 加 min 500 下限（Win 继续用 <500 段，两线互不干扰）。D471（D1 事件溯源 spec，dev-doc 已交付 spec_done）重编号 D500。**流程盲区先记台账（编码 session 完成 D500 后 CTO 补充）**：vitest（esbuild）不查类型 + pre-commit 组1 只查 as any → 类型错误可能静默通过测试（D473 实证），待 D500 完成后在 verify-incremental L2 或 CI 强化类型检查。 |
| 2026-08-22 | 🆕 **跨分支污染事故（M8 变体）+ D500 合并收尾（CTO 核实处理）**：D500 复核报告声称"main 干净"，CTO 核实发现**不成立**——之前 PR #101（task-board backlog）合并时意外把 D500 的前 5 个提交（c3196067 核心 + 214ac7f2 第一个复核修复）混入 main，main 成了"半个 D500 + task-board"的混合体。**处置**：合并 D500 完整分支（PR #102），main 拿到 D500 完整交付（核心 + 4 复核修复，5 提交全部核实已在 main）。**D500 诚实降级记台账（后续任务）**：engine 悬空 seam——5 个引擎实例化点（cli/mcp/im-inbound/fromState/tui）未传带 store 的 SessionManager，conversation-engine:617 生产死代码；D500 写集=store 层事件流+直连双写（已生效），engine 全量装配（4 入口传 store）是独立后续任务。**D500 复核质量属实**：4 问题（降级信号粘滞/deriveMessages 零消费/seq 冲突测试/断言分支）+ 1 诚实降级，均已修复。 |
| 2026-08-22 | 🆕 **看板数据不准根因 + 修复（CTO 诊断）**：诊断确认 adapter 同步正常（5 分钟 interval 在跑），不准的根因是**僵尸任务**——adapter 的 import 只合并不删除，task-state 删除的任务（D470/D471 撞号残留）留在看板。**修复**：扩展 sync.js 的 syncOnce，import 后发 delete action 删除僵尸任务（看板有、task-state 无、且非 backlog 待规划），复用 fetchBoardState 的 boardTasks 避免重复请求。测试 25/25（新增 2 例：删除僵尸 + backlog 不误删）。**K3 审计清单已建**：docs/synova/coordination/K3审计清单-20260822.md（P0 挪CI/Stage1 8 项 + P1 部署轨 + P2 历史批次约 50 项）。 |
| 2026-08-22 | 🆕 **K3 三批审计（P0/P1/P2，清单 K3审计清单-20260822.md 全量执行）**：**P0 CONDITIONAL PASS**（P1×3）——D465/D466/D468/D500/D472/D473 PASS；D467 CP（**P1-1 CI tsc `\|\| true` fail-open → 类型防线真空，main 实测 28 个 tsc 错误永不红**（_extinct×25+ima.ts:143+server.ts:394，均 pre-existing）；**P1-2 as-any CI 只查 src/ 而本地全仓 + "存量 0"以偏概全（packages/ 33 处）**）；D474 CP（**P1-1 repoRootDir() 非 ASCII 路径 bug**——中文路径黄金门禁恒 degraded→push 恒拦→M4 绕过风险；测试 2 失败为审计工作区中文目录名编码，环境依赖）。**P1 PASS**——GS 8 场景（GS-03 3/3、GS-05 4/4 绿属实；GS-02/04 诚实 RED 属实）+ 部署轨 4 + U 系列引用 08-17 终审 + 接线核实全过。**P2 PASS**——回填 71 项（含 9 项历史报告回填 + 12 项 U 系列/终审引用），新审 35 项全部找到真实交付；D397/D398 NOT-AUDITABLE 显式标注；**共性 P2×3**（task-state impl.commit 大面积 PENDING/local 未回填 = M7 漂移；小任务声称表缺失；清单"多数已有审计记录"假设与台账事实不符——多数只有实施记录无 K3 结论）。**清单注修正**：P2 段提示的台账覆盖需逐 D# grep 核实。报告: 2026-08-22-P0-control-tower-stage1.md / 2026-08-22-P1-gs-deploy-u.md / 2026-08-22-P2-batch-audit.md（审计工作区 audit/k3-20260822 分支）。 |
| 2026-08-22 | 🆕 **K3 审计 P1 修复（control-tower 归因，CTO 落地）**：① **CI tsc `|| true` fail-open**（D467 P1-1）——CI 的 TypeScript check 是 `|| true`，28 个存量错误（_extinct 退役哨兵 25 + ima.ts:143 + server.ts:394）让 CI 永不红，D467 挪 CI 后类型防线完全真空。修复：去 `|| true`，改"过滤存量白名单（_extinct/ima.ts/server.ts/4 历史文件）+ 其余新错误 exit 1 阻断"。② **as-any 只查 src/**（D467 P1-2）——我之前"限制到 src/"防自引用，漏掉 packages/ 34 处 as any。修复：范围覆盖 `src/ packages/`，不查 scripts/ 防自引用。**存量清理归编码 session**（_extinct 死代码 + ima.ts/server.ts 类型错误）。 |
| 2026-08-22 | 🆕 **K3 审计后续动作收尾（CTO 归因落地）**：① 清理 4 个孤儿 worktree（.wt-gs05 feat/d443 + tmp 的 synova-review-d472/473/474，分支/commit 均已合并 main，未提交的只是运行时产物）→ 已 git worktree remove。② **task-state commit 回填纪律**（K3 P2×3 发现的 M7 漂移：impl.commit 大面积 PENDING/local）——记录为纪律：实现提交时 impl.commit 必须回填真实 hash，不允许 PENDING/local；CTO 周报/看板检查时发现 PENDING/local 即提醒。**剩余归因**：implement（D474 P1-1 repoRootDir() 非 ASCII 路径 bug，改 fileURLToPath）派编码 session；创始人核（GitHub branch protection 是否把 CI 设为 required check）。 |
| 2026-08-22 | 🆕 **K3 审计 D474 P1-1 修复（创始人简化方案，CTO 直接改）**：repoRootDir() 非 ASCII 路径 bug——旧法用 `metaUrl.pathname`（URL 百分号编码中文路径 → path.dirname 处理乱码 → 黄金门禁恒误拦），改 `fileURLToPath(import.meta.url)`（正确解码非 ASCII）。实测：中文路径 `/scripts/ci/审计-测试.ts` 旧法返回 `%E5%AE%A1%E8%AE%A1...` 乱码，新法返回 `审计-测试.ts` 正常。归因修正：golden-case-checker.ts 在 scripts/ci/（控制塔地盘）非 src/，归 control-tower 而非 implement。 |
| 2026-08-22 | 🆕 **CI 转绿修复（设 required check 前置）**：设 branch protection required check 前先修 2 个 CI 失败：① tsc 多行错误——`grep -v "server.ts"` 只滤第一行，后续行（`Type 'Promise<...>'`）不含文件名没滤 → ERRORS 非空 exit 1。修：`tsc --pretty false`（单行错误格式），过滤白名单正确生效。② alloc-task-id.test.sh 断言过时——D500 起步后测试仍期望 D101/D102/D103。修：预置 D499 期望 D500/D501/D502（正好测 min 500 边界）。测试 12/12 + 6/6 全绿。 |
| 2026-08-22 | 🆕 **CI 自阻断根因 + 修复（挪 CI 系统性矛盾）**：CI 失败的真凶不是 tsc，是 pre-commit-check.sh 的 GATEKEEPER 门禁——它检测 bypass.log 里"今日 detected-bypass"（本地 --no-verify 记录），CI 上跑时检测到 git 跟踪的本地记录 → 自阻断。**根因**：挪 CI(D467) 后本地软提示 + CI 权威，本地 --no-verify 已合法，但 GATEKEEPER 语义没跟上（还在检测"本地绕过"）。**修复**：GATEKEEPER 只在本地跑（非 GITHUB_ACTIONS），CI 上跳过——CI 权威不该检测本地绕过痕迹。**教训（M9 新增：挪 CI 后本地痕迹门禁要在 CI 屏蔽）**：改了门禁的执行位置（本地→CI），配套的"本地痕迹检测"门禁必须同步屏蔽 CI，否则 CI 检测自己的历史痕迹自阻断。 |
| 2026-08-22 | 🆕 **Win PR CI 失败根因闭环（创始人关切）+ D501 门禁 bug 修复**：创始人指出"我的调整导致 Win PR 一直失败重试"。定位两层根因叠加：① GATEKEEPER 自阻断（PR #114 已修）+ ② ci.yml tsc 白名单（PR #113 已修），main 00a1461a 转绿后，Win 4 个 PR 中 #94(D470)/#104(D338)/#105(D475) 经 merge main 直接全绿，仅 #95(D471) 残留失败。**#95 新根因（D501，M9 同型：范围扩展漏排除）**：K3 P1-2 把 as-any 范围从 src/ 扩到 src/+packages/ 时未排除测试文件，D471 引入的 as-any 审计测试（packages/test-kit/tests/architecture/05-as-any-audit.test.ts，fixture 含 `as any` 字符串）被 CI 误报 19 处——测试器自身 findTsFiles 排除 .test.ts/.d.ts，但 pre-commit-check.sh 组1 未落地该排除语义。**修复（D501）**：组1 as-any 检查 git diff 加 pathspec 排除 .test.ts/.test.tsx/.d.ts，与 findTsFiles 一致（只查生产代码）。本地验证 19→0 + 生产文件（registry.ts/sog-core-schema.ts/sog-schema-registry.ts）无残留。PR #115 合入 main（fb2449e5）。**教训（M10 新增：范围扩大的门禁必须同步排除非目标文件类型）**：门禁扫描范围扩大（src→src+packages）时，必须同步排除该范围内新增的文件类型（测试/声明），否则扫进测试 fixture 自误报。 |
| 2026-08-23 | 🆕 **D502 任务看板多源统一落地（创始人裁决：CTO 实现 + 待规划全量）+ D503 G12 时区容差修复**：看板从单源（task-state=Mac）升级四源聚合——①task-state（Mac 75 任务）②**Win git 派生**（origin/main log，作者 Synova-Win 系，窗口≥D328，48 任务；有 K3 审计报告→done/有提交无审计→running，"合并≠完成"与 Mac 口径统一）③**26 产品线**（product-progress.json 每线一卡 L01-L26 + L00 总览卡，当前 4%）④**待规划 todos.yaml 48 条 T-\* 全量**（标题带 [P0/P1][L##]）。**新架构**：derive-board-sources.py 每次同步前 fetch origin/main 写 snapshot（原子写、fail-closed 不覆盖旧快照），adapter 读 snapshot 而非工作区文件——根治工作区滞后（D501 踩过）。验证：33 单测 + 9 集成测试（临时 git 沙箱）+ 真实仓库 dry-run 203 卡。**过程中新发现并修复 D503（时区缺陷，M11 新增：环境差异敏感的过滤要有容差窗口）**：G12 认领 brief 按日期前缀"今日"过滤，Mac(UTC+8) 傍晚建的 brief 对 CI runner(UTC) 是"明天"→ CI 无人认领误报 7 处（D502 PR#117 首跑实证：本地 13 组全过、CI 红）；修复=认领窗口 ±1 天 glob（brief 前缀+dev doc 后缀对称修）。PR #117 合入 main（bc481b92）。**部署待办**：插件 cp 到 profile + 重启 dsh web（会中断会话，待创始人点头）。 |
| 2026-08-23 | 🆕 提交减负专项 V4.9.0（D508 + 三 PR #125/#126/#134 合并 + tag）：90% 时间黑洞根治（对账 merge-base/--check 全量/brief 骨架/双清洁）+ M12 教训（测试沙箱 GIT_DIR 结构性隔离——三次 index 污染根因）+ 版本纪律补课（V4.8.0 后六批未 bump → 规范建立 + CT 待办版本守卫）+ API 通道网络韧性实战。 |
| 2026-08-24 | 🆕 **D481/D482 交付复核通过 + 合并（Codex 执行，创始人授权）**：D481（auth.integration 契约对齐，PR #144 → 2ac8bba3）实测旧格式 6 处零残留 + registerAndLogin 9 处 + 3 文件 39 tests 全绿；D482（org-expert 声称降级，PR #150 → c6f58a6c）实测旧声称零命中 + 新声称 2 处 + 3 文件 27 tests 全绿；写集均干净（D515 merge 平基带入非本分支）。CI #144 18/18、#150 9/9 全绿。**D481 重要产品发现（另立任务）**：`/api/auth/register` 不在 jwtAuthMiddleware 白名单（src/middleware/auth.ts L83-99 仅 login 在列）——探针实测生产同构挂载下匿名注册 401，D102 遗留"注册（验证邀请令牌）"未实现；决策点：加白名单 vs 邀请令牌验证，待创始人/CTO 定。**并行干扰第三次复发（M8 变体实证，P1 清单确认）**：D481/D482 两 session 均报告主树被对方中途接管（checkout 劫持/暂存区污染/current-brief 覆盖），各自迁移 worktree 收尾——与 2026-08-16 D394 实证同根因，控制塔无"主树多 session 占用检测"强制门禁（已列给 CTO 的 P1）。 |
| 2026-08-23 | 🆕 **D478/D479/D480 三任务终审验证 + 合并（Win 线，Codex 执行）**：三交付全部实测通过（D478 overflow 挂载：server.ts:358 挂载 + :401 graphStore 注入，overflow-mount 4/4 + 回归 10/10；D479 auth orgId 收敛：auth.ts:261/368 双点 `SYNOVA_ORG_ID \|\| 'default'`，auth.test 23/23 + RED 先行；D480 一页纸渲染：report-assembler.ts:244 `renderOnePager` + diagnosis.ts:287/382 双生产调用 + report-templates 零改动，5/5）。PR #132（D479，09f19715）/ #131（D480，04328801）/ #133（D478，fe513999）已合并；主树补记 D480 COMMITTED 记录走 #136（c595e6a7，D334 要求 main 只进 PR）。**合并过程暴露控制塔问题 2 项（归 DSH）+ 操作性根因 1 项（我方纠正）**：① **D328 commit-msg merge 场景误伤**——注释声明 Merge 应 fail-open 但 resolve-commit-brief.sh 无 merge 检测，merge 提交必被「消息无 D# vs 认领 D508」拦死，本次用 SYNO_STAGED_FILES 官方注入缝（D395-a）以 D478 写集绕过（非 --no-verify，D331/CI 全绿）。修法：commit-msg-check.sh 检测 MERGE_HEAD 存在即跳过 D328（与 GitHub 网页 merge 不经本地 hook 语义一致，不削弱普通提交防劫持）。② **verify-parallel.sh 硬编码 python3**（Win Git Bash 无 python3，exit=127 degraded → pre-push 拦），本机以 /tmp/py3shim 临时解决，脚本应改 PYBIN 探测（D329 同型，commit-msg/resolver 已实现 verify-parallel 漏网）。③ **D331 补记 11 条 mac 记录的根因复盘（更正早期判断）**：初判"mac API 通道不写 COMMITTED 导致必须补记"，复查后实为**本次 fetch/push 全程走 https URL（SSH host key 故障绕行），`git push <URL>` 不更新本地 remote-tracking ref → pre-push 的 BYPASS_BASE（优先 PUSH_REMOTE/PUSH_BRANCH，fallback origin/main）解析到陈旧 ref → merge-base 化失效 → 对账范围扩大至 main 引入提交 → 需补记**。D508 merge-base 化本身正确；正确修复是保持本地 refs 新鲜（push/fetch 走 origin remote）或对账脚本对 BASE 做防御性 `git fetch`，**不应要求 mac 改 API 通道写 COMMITTED（mac 提交作为祖先本就不在 merge-base 化范围内）**。**环境事故 1 项**：主仓 `.git/config core.bare` 被误设 true → 「must be run in a work tree」，已修复（false）。**交付诚实发现（K3 可核，已写 PR 描述）**：D479 的 auth.integration.test.ts 在 main 基线即 7 failed（login 路由已改 bcrypt 契约，测试仍发旧格式 `{userId,role,orgId}`）→ 另立任务修复；D480 的 full-pipeline Stage 5b（expert/ 目录 ≥9 断言，实测 7）为 origin/main 既有失败，CI 本就排除 tests/e2e/**；D478 控制塔观察 3 项（D503 case 变量 `\|` 非 alternation 致认领窗口恒空 / task-start.sh 不再写 current-brief 而 Claude 无 attach → 陈旧指向 / hook-block-write.sh L118 `find\|head -1` 多 brief 时挑错误记证据）。**遗留**：d479 worktree 物理目录删除失败（可能被 session 占用，元数据已清，待手动删）。 |
| 2026-08-25 | 🆕 **M13 复发观察 + D521 执行时长观察（创始人报告，均先台账观察不干预）**：① **M13 复发（第二次）**——主仓 `.git/config` 再次被测试沙箱污染：`core.bare = true`（导致 git status/commit 全部报 "must be run in a work tree"）+ `user.name = test@test.local`（提交身份污染）。08-23 首次修复记录在案（D478/D479/D480 合并时"环境事故 1 项"），本次再犯 = 同类错误第二次 = 按红线防线失效观察。已修复（bare→false + user→synova-mac），**处置：先列台账观察，暂不定位污染源**（防第三次时再排查，避免干扰 D521 执行）。② **D521（控制塔提交链路收敛 V5.1.0）执行已运行 2 小时**（创始人报告）——与 D520 执行 2 小时同型（时间黑洞模式：spec 本体 25% + CI 调试 35% + 推送摩擦 20%）。当前 D521 执行中（并行 CTO session），**不干预，做完了再复盘**——复盘聚焦 D521 是否复现 D520 的三贵坑（无 CI 日志通道 / 无 push 前模拟 / bypass 竞态）。 |
| 2026-08-26 | 🆕 **D533 CI 调试可达性根治闭环（V5.1.3）**：并行 CTO 执行（凭证共享/CRLF 治本/debug 纪律——审计收敛 3 项）。**① 凭证**：GITHUB_TOKEN 落位 ~/.dsh/.credentials.yaml（0600，主 CTO 提供）——curl 拉日志验证可用（D529 最大时间黑洞根治）。**② CRLF**：git add --renormalize 规范化 15 个 CRLF 脏文件（D520 已加 eol 规则但从未 renormalize）+ .gitattributes 追加 scripts/audit/** -text（K3 红线）+ scripts/control-tower/*.py -text（CT-40 豁免）——零噪音。**③ debug 纪律**：CI-诊断通道.md §五（ci-debug/* 独立分支 + 首选 curl/gh 日志）。合并 PR #203（19b18734），tag V5.1.3 已打（顺延：V5.1.2 被 Win feat/win-d485-account-link-c 占用）。**⚠️ 版本链处理项**：Win 分支 V5.1.2（未合并）未来合并时，VERSION.md 需把 V5.1.2 条目插到 V5.1.3 前（版本链有序 V5.1.1→V5.1.2→V5.1.3），合并时人工核对。 |
| 2026-08-26 | 🆕 **D538 前端左栏派单统筹 + GA 后端待办登记（创始人要求台账固化，不许遗忘）**：Win 推送"前端交互设计-左栏 Codex 风格"设计规格（已合 main，PR #219）。CTO 重新核查（创始人提醒"统筹推动非照文档做"）纠正三处：① **DSH 借鉴**——从"无借鉴"改为"有理念借鉴"（DSH Slot 机制 slots.d.ts + dsh-dashboards 插件 sidebar/nav 组织，借鉴理念不引代码）② **前后端接线**——核实后端 4 能力接口：`/api/signals`（sentinel.ts:46）✅ / `/api/loops/status`（loops.ts:80）✅ / `/api/actions`（actions-api.ts:54）✅ 已存在，前端接真数据；③ **统筹**——创始人裁决方案 A：前端先行接 3 能力真数据，GA 项置灰（userRole=ga 显示"待后端接入"占位，不伪造）。**⚠️ 待办（另派任务，不许遗忘）**：`/api/ga/calibration` 后端缺失——GA 人机协同（诊断校准面板/手动信号注入/反馈效用仪表）属 Module-3 GA 协同模块，后端未做，需按模块节奏另派后端任务，届时前端 GA 项从置灰转真实接入。 |
| 2026-08-26 | 🆕 **D521-3 测试隔离泄漏待办（D537 验收发现，登记不遗忘）**：ct-test-gate 的 `env -u GIT_DIR` + 测试 unset GIT_DIR 双保险已存在，但 D537 首次 synova-commit 仍泄漏一次（a.txt/b.txt 测试 fixture 被误含进主提交 95b09aec，后续影子提交删除，squash 消除历史）。**根因未彻底定位**——后续补记提交未复发（fastlane 跳过 ct-test-gate），说明是"首次 + ct-test-gate 上下文"的组合触发。**待办（另起 D# 深挖）**：D521-3 测试隔离（GIT_DIR 泄漏）根因定位 + 修复——测试沙箱与真实 commit 的 GIT_DIR 环境边界。 |
| 2026-08-27 | 🆕 **D536 部署验收合并闭环（CTO 验收通过）+ CTO 操作教训 3 项（诚实登记）**：**合并**——PR #226（9 文件手动应用）+ #227（补漏），main=8498bad6，D536 写集 9 文件全落地（runbooks×3 desktop-deploy-acceptance/founder-demo-mac/founder-demo-win + first-diagnosis-timing.sh + GS-01 run.sh + task-state + spec + brief + bypass.log），无 src/audit/build-synova.cjs 触碰，BSD date 修复属实（first-diagnosis-timing.sh:73/155 python3 fallback）。**CTO 操作教训（本机 DSH 犯，按红线登记）**：① **手动应用漏文件（M7 类交付不完整）**——worktree 循环里 `git show origin/d536impl:$f > $f 2>/dev/null` 对 desktop-deploy-acceptance.md 误报 ❌（transient，单独重跑即成功），漏了 130 行核心 runbook，靠 fetch 后终验才发现 → 补漏 PR #227。教训：手动应用多文件时每个 ❌ 必须立即单独重跑 `git show` 复核，不能信任循环吞错结果。② **evidence 误判返修**——误判 D536 evidence 需进 git，实际 `.gitignore:75` 忽略 evidence/ 是既定设计（K3 靠 runbook 复跑，切片 A/B/C evidence 也不入 git），已撤回 + 清理 chore/d536-evidence worktree/branch。③ **#1 CRLF 核实误用落后主工作区**——主工作区 feat/d505-impl 落后 main ~146 commit，核实 D520 CRLF 修复时误用落后版 → 误判"未修"（实际 origin/main 已修）。教训固化：核实代码现状必须用 `git show origin/main:`，非落后主工作区。 |
| 2026-08-27 | 🔴 **P0 数据事故：data/synova.db 损坏 7 天 + 备份双重失效（CTO 诊断并恢复）**：诊断备份失败时发现**不是脚本 bug，是源 db 本体损坏**——`data/synova.db` 文件头被随机字节覆盖（`86a5 36de...`，非 SQLite 魔数，sqlite3 报 `file is not a database`），mtime 08-20 09:57。**双重失效**：iCloud 异地备份停更 08-19 18:36 + data/backups 本地备份停更 08-20 03:08（都是因源 db 损坏后 .backup 报错）。**恢复**：用 08-20 03:08 备份（`data/backups/synova-backup-2026-08-19T19-08-03-576Z.db`，integrity ok，30 表）覆盖恢复，恢复后 integrity ok；launchctl kickstart 触发 launchd 备份，iCloud 新备份 08-26T18-42Z 成功（960K，integrity ok）。**取证**：损坏原件 → `data/backups-corrupt/synova.db.corrupt-20260827-023842`。**丢失评估**：08-20 03:08~09:57 约 7 小时窗口，产品进程未运行、08-20 后无新写入，实际丢失 ≈0。**根因线索（待深挖）**：08-20 09:15~09:17 bypass.log 密集 `possible-bypass`（diff=72318s 等）→ 09:57 db 损坏，时间窗口吻合，**M13 同型嫌疑（测试沙箱污染真实仓库）**，前科 core.bare=true/user.name=test 两次。**⚠️ 防线缺口（本次事故真正教训）**：db 损坏 + 备份失败 **7 天无告警**，靠 CTO 诊断才偶然发现——备份失败/数据损坏必须加告警，不能靠人肉。**待办**：① 根因深挖（08-20 09:15~09:57 哪个 session/测试污染了 db）；② 备份失败+db 损坏告警机制（防线缺口）；③ backup.log 审计日志本身写失败（`Operation not permitted`，db 备份成功但日志停更 08-14，P2）。 |
| 2026-08-27 | ✅ **备份失败告警落地（待办②完成）**：① `backup-db.sh` trap EXIT 落盘 `.claude/backup-health.json`（成功 `status=ok`+时间戳+字节数 / 失败 `status=fail`+exit_code）；② `weekly-selfcheck.sh` 第⑦项备份健康三查（health 失败/db 完整性/备份新鲜度 48h，[ALERT] 前缀区分告警 vs 漂移）；③ 配对测试 `weekly-selfcheck.test.sh`（4/4 绿：接线+正常无 ALERT+降级 health fail+边界 db 损坏）；④ backup-health.json 运行时产物 gitignore。PR #230 合并（1b8cbc43）。**端到端验证**：kickstart 真实 launchd，backup-health.json 落盘 `status=ok` + iCloud 新备份成功。**过程中发现并修**：silent-swallow 门禁抓 3 处 `2>/dev/null`（fail-closed 场景需 swallow-ok 注释）+ U7/CT-40 门禁要求控制塔脚本配对测试。**剩余待办**：① 根因深挖（08-20 污染 db 的 session/测试）；③ backup.log 写失败 P2。 |
| 2026-08-28 | ✅ **K3 治理收口批审计（D540-D547）6 PASS + 2 CONDITIONAL PASS（0 FAIL）+ 处理**：审计分支 audit/k3-20260828-gov-closeout 回流 main（PR #271）。**FIX-D541（已闭环，D549）**：claim-regex-narrow.test.sh:71 硬编码 `sed -n '749p'` 被 D542 加行漂移 → T5b 断裂（9 断言仅 8 过）且未入 CI 密封清单 → CI 全绿掩盖。修 = 动态 grep + 6 个新密封测试入 CI canary（19→25，PR #272）。**FIX-D544（已澄清）**：brief 声称 evidence 落盘但 git 零记录——evidence/ 被 .gitignore:75 忽略是既定设计（D536 确认），8 条标注经 PR 描述可核（K3 复验通过），修复 = 如实声明非补交。**P1-3 系统级（已闭环，D549）**：新密封测试必须入 CI canary 设为门禁脚本变更接线验收项（M3 强化）。**P2×2 派单质量（归 devdoc）**：①D538/D544 派单「/api/signals」实际为 /api/sentinel/signals（实现方已识破）——派单接口路径核实不足；②D546 派单「runner 5 处 durationMs 恒 1970」位置误标（真凶 sentinel-service.ts:97）——派单基线核实不足。**环境注记**：D546 vitest 本地全挂 = better-sqlite3 NODE_MODULE_VERSION 137 vs 127（环境依赖，非测试缺陷，CI Vitest 绿）。
| 2026-08-27 | 🆕 **CTO 派单核实错误（M2 类，自报）**：写 D539 派单时 grep resolver 路径写错（`scripts/control-tower/resolve-commit-brief.sh`，实际在 `scripts/workflow/`），误判「D329 会话专属机制疑似未实现」——实际 `resolve-commit-brief.sh:18` 明确有 `--session <sid>` + `attach.py:13` 写 `current-brief.<sid>`，机制**已实现**。另把 D515 项1 软告警（pre-commit:749-761）张冠李戴为 D537 #2 主树硬阻断（实际 D537 #2 已合 main V5.1.4，pre-commit hard_check，last_seen_at 1800s）。**根因**：写前核实凭记忆/台账写路径，未 `ls` 物理确认真实路径（违反 SOP ① 第 3 项「grep/ls 物理确认，不凭记忆」）。**教训固化**：核实基线资产必须 `ls`/`find` 找真实路径 + `git show origin/main:` 读权威版，禁止凭记忆写路径。派单已修正（resolver 路径 + D329 已实现 + D537 #2 已硬阻断）。**触发纠正**：subagent 核实（非 dev-doc 预设）发现 5 处不一致，创始人纠正「subagent ≠ dev-doc 预设，以后派单交创始人转发真正 dev-doc」。 |
| 2026-08-27 | ✅ **D539 验收（K3 CONDITIONAL PASS，P0）**：并行隔离切片「一个 session 一个 worktree」从自觉升级为开工端硬门禁。9 条声称物理核实通过（测试 34 断言全绿含 sha256 隔离指纹/主树阻断 exit 1/worktree 放行 exit 0；接线 grep -c worktree-manager=8 + task-start.sh:103 真实派发 create；CT-42 写侧 current-brief.$SESSION_ID 闭环）。**P1（唯一实质发现，不阻塞合并）**：spec 产物从未提交 git——dev-doc 在 stale 主树 feat/d505-impl（落后 447）写 48KB spec 未提交，讽刺地正是 D539 要根治的「主树当 dev 工作区 + 悬空未提交」模式；**CTO 已补交（PR #235）**。**边界（诚实记录）**：① 只拦开工端+提交端，不走 task-start 直接主树 checkout/add 仍可能；② 不清理现有脏主树（feat/d505-impl 落后 447 + 大量 untracked 还在原地）；③ 依赖 workflow 被遵循（跳过 task-start 则门禁不触发）。**⚠️ 治理膨胀提醒（转创始人）**：D515/D537/D539 治理层连续投入，需创始人盯「治理投入 vs 产品推进」平衡——方向层面，非技术问题。合并：feat PR #234 + spec 补交 PR #235；审计回填 audit/k3-20260827-d539 待推送。 |
| 2026-08-27 | 🔴 **CTO 派单核实不实（第二次，M2 同类复发——按红线升级创始人）**：写 D540 派单时，凭创始人转述的「D485 影子提交失效」直接写「当前失效，步骤 0 修复」，未核实 main 最新状态——实际 **D537 #4（e4cb41ab，08-27 01:22）已恢复影子提交登记段**（`post-commit.sh` L69-84 D521/不变量2），D485 失效（8c89dfd3 08-26 22:29）是恢复前的事。另把影子提交机制位置写错（写成 `synova-commit` L573/647，实际在 `post-commit.sh` L69-84）。**这是继 D539 resolver 路径后第二次「写前核实第 3 项（基线资产状态）没到位」——同类错误第二次 = 防线系统性失效**。派单已修正（影子提交已恢复 + 机制位置 post-commit.sh L69-84 + verify-parallel 路径已核实）。**教训固化升级**：写前核实基线状态必须 `git show origin/main:` 读权威版 + `git log --oneline` 查合入时间戳，禁止凭转述/记忆判断「当前状态」——尤其「某机制失效」这类状态判断，必须核对修复 commit 是否已合 main。 |
| 2026-08-27 | 🔴 **D540 审计（K3）FAIL → 修复中 + P2 三项登记**：P0-1 CI 权威 Iron Laws 红（分支 f2bf896a，quality job failure）——brief「verify-parallel 迁移」命中 `pre-commit-check.sh:728` 正则裸词「迁移」→ 本地 warn 软提示 exit 0（编码声称「push 全门禁通过」= M4/M2 本地叙述冒充 CI 权威），CI strict 转硬 exit 1。修复面小（brief 措辞），折入编码 session（不另起 FIX）。**P2 三项（登记，不阻塞修复）**：① P2-1 三套新测试（clone-config-init/clone-shadow-commit/verify-parallel-ci）未入 CI 密封清单；② P2-2 worktree 试点提交对悬空于 clone 对象库（本地 bundle clone 副作用）；③ P2-3 main 预存 CT gate tests 双平台红（post-commit-marker 5 失败，非 D540 引入，入 CTO 队列）。**另立 D541（正则收窄）**：`pre-commit-check.sh:728` 裸词「拆分/迁移」误伤"工作描述"（非完成声称），收窄为完成语义「已拆/已迁移/拆分.*完成/迁移.*完成」，配测试。**编码诚实技术发现（K3 独立复现，记入）**：grep -q 接管道在 pipefail 下 SIGPIPE 141 非确定性（改 grep -c 读全量）；git 无 identity 自动派生身份（需 useConfigOnly 才真触发降级）。 |
| 2026-08-28 | ✅ **D540/D541 全闭环（main=60c4ae34，版本链 V5.2.1→V5.2.0→V5.1.4 连续）**：D540（clone 试点修复+silent-swallow 4 处）PR #243 合并 + V5.2.0 bump 随 #246 补入；D541（铁律47 正则收窄为完成语义三方向）PR #246 合并。**CI 红根因终局（三处，非两处——编码 session 用 CI 日志定位补全）**：① brief「迁移」裸词命中旧正则；② brief verify 行字面量『完成.*迁移』命中收窄后新正则（自指）；③ **brief Q2 写 `pre-commit-check.sh L750` 后缀被 brief_parser.parse_q2 保留 → G12 判越界**（devdoc_writeset 会剥 L\d+ 而 brief_parser 不会——解析器不一致缺陷，已绕过未根治，根治归 CT 队列）。**编码 session 质量跃升（记入评估）**：自产出六层重验报告（新鲜物理证据）+ 自纠 2 个核验方法错误（Q0b 误算进 Q2 / 在主 clone 树查 D541 memory_refs 误报——应 git cat-file 分支树内查）+ 主动纠偏过时信息（main 推进/待合并状态）——M4/M2 明显改善，诚实声明「本地绿≠CI 绿」已成习惯。**D542（CTO 自修，PR #244）**：soft_check/warn_check CI strict 打印 ❌（此前 ⚠️ 导致「N 组未通过」无组名可查——D541 排查黑洞根因），计数语义零变化，配对测试 6/6。**遗留立项建议**：① control-tower-tests 双平台预存红（P2-3 确认在新 main 仍成立）单独立项；② brief_parser/devdoc_writeset 的 L\d+ 后缀解析不一致根治。 |
| 2026-08-28 | ✅ **D544/D546 双线并行交付闭环（main=5439a6db）+ CTO 派单失误第三次（占位符骨架 brief 误入 main）**：D544（左栏验收合并，孤儿实现 910 行收口 PR #266）+ D546（哨兵 findings 事件化验收，src 零改动只补 3 测试 PR #265）双线并行交付。**CTO 失误（第三次，同类复发）**：PR #258 派单时误把 alloc-task-id 生成的占位符骨架 brief（`<agent>`/`<本任务在哪一层>`）随派单提交进 main → check-plan-integrity 在 CI 上（--cached 恒空）回退「今日最新」命中占位符 → **全局阻断所有非 docs PR**（D544/D546 实证）。修复 PR #262（删 D545 骨架 + D546 覆盖填好版）。**教训固化（第三次升级）**：派单提交时 alloc-task-id 骨架 brief 是「给认领 agent 填写的本地模板」，**不得随派单进 main**。**创始人提醒固化（防忽略）**：①前端交互设计文档末尾「下一版待确认项」3 条（能力导航上移/紧凑态/**DSH 式输入框——可借鉴 DSH**）→ T-M-09；②DSH 迁移施工图 §4 借鉴清单 9 项（会话存储/agent-loop/子代理/LLM/调度/后台作业/反馈闭环/安全边界/门禁体系）→ T-M-10。派单 SOP §〇b 借鉴核查必须执行完整（D544 曾轻率写「无新增借鉴」而漏了 DSH 式输入框）。**编码 session 质量记录**：D546 诚实声明（派单前提过时/durationMs 线索错位）可信 + src 零改动 + 自纠，质量高；但 task-state 回填出现 JSON 语法错误（D544.json 坏 JSON，CTO 重写）。 |
| 2026-08-28 | 🆕 **verify-parallel --ci-pr 缺「今日窗口」误拦串行演进（D487 实证，登记不实施——归属控制塔/DSH；**已修复：D555（V5.2.4/V5.2.5）+ D557（V5.2.6）信号1-4 豁免，D487 分支实测 exit 0**）**：ci-pr 模式实现为「base..head 写集 × 全部历史已合 doc」纯重叠判定，丢了 D540 派单原文「× **今日**已合 PR 写集」的日期窗口——D487（D500 地基的指定后继接线）在 CI 被误判为并行冲突，4 条重叠全部对已交付任务：diagnosis-launcher.ts（vs D292 20260731 / D338 20260822）、session-store.ts（vs D469 20260821）、conversation-engine.ts + session-store.ts（vs DSH-D500 20260822）；经核 4 者写集文件均已存在于 origin/main。同脚本 compare_docs（非 CI 模式）的 V5.0.1 教义明文「已交付任务写集被后继继承 = 串行演进，非并行冲突」——ci-pr 实现与自身教义冲突。**修复建议（供单独派单参考，未写入代码）**：ci-pr 物化已合 doc 前按 doc 文件名日期后缀 ±1 天窗口过滤（今日±1，D506 时区容差先例：CI runner UTC vs doc 日期 UTC+8；文件名无日期后缀视为窗口外不拦），python 不可用时不过滤 fail-closed；配套沙箱测试补「历史重叠串行接力 pass」+「昨日窗口内仍 block」用例，既有 T3-T5 日期动态化（硬编码日期次日即假绿）。实证 patch：/tmp/d487-verify-parallel-defect.patch（D487 会话产物，未入分支）。注：曾试 D500 doc 同路径接力登记 carve-out——实测级联新重叠（D500×D469/D286）即 revert（c1fad250），治本仍需窗口过滤。 |
| 2026-08-28 | 🔴 **D# 撞号三连（D545/D547/D548/D550）+ 根因闭环（alloc 读 origin/main 占用）**：主工作区（feat/d505-impl，落后 main 616 commit）陈旧致 alloc-task-id 只读本地 task-state 占用表 → 与 origin/main 已占号撞车：D545/D547/D548（骨架门禁/l01 回填二次分配）+ **D550 撞号**（ga-calibration 抢占 alloc-origin 修复号，ga-calibration 改号 D551）。修复（D550，PR 已合）：alloc-task-id 合并 origin/main 占用（REMOTE_USED + SYNO_ALLOC_NO_REMOTE 测试 seam）。**根因**：分配器读权威源（origin/main）而非本地陈旧工作区——与 D536 教训③（核实代码现状必须 git show origin/main:）同根。**连带格式教训（×2，D547/D550）**：brief 架构层「## 架构层: X」内容写在同一行 → awk 只取下一行 → CI「架构层: 未填写」；格式已固化为标题后内容换行 + Done 标准 `- [x]` checkbox。 |
| 2026-08-28 | ✅ **D552 治理资产回填 + 主工作区清扫（CTO 自办，PR #279）**：主工作区 rebase 至 origin/main（434d7211）后处置 54 个未跟踪文件——**回填 main 缺失治理资产**（dev-doc-delivery 技能双轨 / synova-dashboards 三仪表盘插件 / D391+D523 brief / 派单-切片C+编码指令×6 / K3-PRODUCT-LINES-VERIFICATION-TASK / founder-alerts / D393 计划 / research 会话并发seq乱序 / D382 归档 / DSH impl plan×7 / task-state D530-532 补登记（D530 claimed→impl_done，证据 734ab32e PR #191）/ proposed notes 四态索引修正 / 新版 founder-console+CTO-HEALTH 自 .wt-D539 救援）；**删除** 15 骨架 brief + dsh/desktop 垃圾 + 10 过期 worktree + ~100 stale 分支（保留 .wt-d483 在途 + d534/d537/d529 三未合并分支书证）。**回填修正（CI G12/verify-parallel 门禁实测后）**：① impl 计划历史件 7 份原计划回填 implementation/，CI 拦两处——G12 插件路径未认领（D552 brief Q2 补显式路径后过）+ verify-parallel「并行声明与实际写集不符」（历史文档写集与已关闭任务文档重叠，门禁不区分历史/在途）——终态：DSH-D356 重复件删除（main 已有 D356×2），其余 6 份移 docs/archive/（证据保留、门禁不扫）→ **CT 队列登记：verify-parallel 对历史回填文档的误伤（CT-28 同型）**。**新发现 3 项（登记）**：① **post-commit auto-hook（D521 登记）卷走暂存区遗留文件**——D311 guard 阻断后插件文件滞留暂存，auto-hook 把 dsh/plugins 插件整体卷入 8b6deaf4「bypass 登记」提交（内容已入库、消息不符）——M8/D286 同型变体，防线缺口 = auto-hook 提交未限定路径（应 `git commit --only .claude/bypass.log`）→ CT-43；② **D544 brief 裸 basename 认领触发 D311 暂存区误判**——认领制把 dsh/plugins/synova-dashboards/package.json 判给已关闭 D544 会话（brief Q2 裸写「package.json」）→ 阻断回填；修复 = D544 brief 认领路径收窄 electron-renderer/ 前缀（c0106535）；③ **dev-doc session 在主工作区直写 D551 文件**（TASK-ROUTING.md/task-state/D551.json/D551 spec/编码指令-D551，实时写入）——D539「一 session 一 worktree」隔离未遵守，文件未触碰留给在途任务，待观察。**CTO 自误（登记）**：误删 main 已跟踪 d472 note（已 restore）；commit subject 超 140 字节被格式门禁拦（短写通过）。 |
| 2026-08-28 | 🆕 **D553 impl-done 处置批（CTO 自办，PR #279 同批）**：main task-state 全景扫描——30 个 impl_done 分类：13 个已有 K3 审计报告（D502/D503/D504/D505/D533/D536/D540-D544/D546/D547，报告文件在 main，D393 派生制下由生成器置 audited）+ 17 个无审计结论（D501/D508/D509/D513/D515/D516/D520/D521/D524/D525/D526/D534/D535/D537/D538/D549/D550）。**处置**：① impl.commit=PENDING ×3 回填真实 hash（D509=dbdcdbcf / D513=90d787c2 / D516=503d04ca，M7 纪律执行）；② 审计派单 `审计派单-20260828-impl-done处置批.md`（17 任务表 + D515 FAIL 结论补报告要求——M4 类缺口：K3 FAIL 已出但报告文件不在 git）+ D521 专项（审 auto-hook 路径限定缺陷，CT-43）；③ CTO-HEALTH 重生成（派生状态刷新）。**登记**：D515 的 K3 FAIL 无报告文件 = M4 同型苗头（结论不在 git）。 |
| 2026-08-28 | ✅ **D554 CT-43 修复闭环（创始人批准「修复按版本管理规范」，PR #280，V5.2.3）**：post-commit.sh 影子登记提交加 `-o -m ... -- "$ROOT/.claude/bypass.log"` 路径限定（-m 必须在 -- 前——首版实现参数顺序错被测试抓到）；配对测试 7→12 断言（场景D：遗留文件不卷入 + 保持 staged + pathspec 语义保持）；**本分支影子提交 126d2d76 实测只含 bypass.log（修复生效现场自证）**。**过程教训 4 项（CTO 自误，登记）**：① brief 漏 `#CRITERIA: A` + 架构层内容同行（G12b 拦——自犯 D547 教训）；② commit 消息缺 `(D554)` 声明被 D328 拦（brief 显式认领时消息必须声明任务归属）；③ 消息超 140 字节（memory/notes 引用改放 body）；④ 主树占用门禁（D537 #2）拦 CTO 提交——门禁按设计工作（主树脏 + 多活跃 session → 应走 worktree，D539 意图），转 `.wt-d554` worktree 提交即过。**连带修复**：测试剥 GIT_INDEX_FILE（D521-3 泄漏未根治面，hook 上下文导出宿主 index 致 CT-40 门禁误报测试红——测试侧防御）。**新增 P2 观察**：G12 对「-P」开头 brief 行报 grep invalid option（软提示不阻断，模式注入未加 -- 防护）。 |
| 2026-08-28 | ✅ **D551 验收 + 合并闭环（创始人交办，PR #281）**：编码 session 交付 GA 校准后端（诊断校准/手动信号注入/反馈效用仪表，spec+代码+40 测试），本机 GitHub 通道不通卡在建 PR。**CTO 物理复核全部命中**：DS2 四端点 requireGa+server.ts 挂载 ✓ / DS3 supersedes 版本链 ✓ / DS4 双迁移（d551_target_type/d551_memory_type CHECK）✓ / DS5 注入链事件流单源 ✓ / DS6「回流计数≠采纳率」诚实声明 ✓ / DS7 三存量路由 diff=0 + as any 代码零命中 ✓；**40/40 新用例绿（node v24）+ 52/365 域回归按证据清单精确复跑绿（VITEST_EXIT=0）+ tsc 28=28 零新增 + 0 detected-bypass**。**补交 3 件（编码 session 未入库，D539 P1 同型）**：① spec 入库；② task-state/D551.json impl_done+slice 回填；③ brief 补写（G12 认领必需）。**CI 两轮红根因（登记）**：① G12 10 文件无 brief 认领（session 无 brief）→ 补 brief 过；② verify-parallel --ci-pr 串行复用误拦（spec 写集 vs 已关闭 D338/D478）→ 门禁修复（D555，V5.2.4/V5.2.5）+ spec 写集表收紧文件级（对齐 D540 先例）→ 全绿。**合并**：PR #281（6e9626f9）。**待办**：D551 K3 审计（下批派审）。 |
| 2026-08-29 | 🆕 **D557 verify-parallel 信号4（V5.2.6，PR #286）——Win 缺陷报告收尾（PR #261 评论 + 台账条目）**：Win 报告 --ci-pr 缺 D540 派单原文「今日窗口」误拦串行演进（D487 4 条重叠全部对已交付任务，登记不实施、边界合规）。D555（V5.2.4/V5.2.5）修了信号 1/2/3 后，**D469 仍无法豁免**（无 task-state、无审计报告、无括号式提交——Win 线合并未登记历史任务）。信号 4 = 无 task-state 且写集文件全在 base → 豁免；防漏网：D483 类在途有 task-state 不豁免、D485/D488 类在途 Win 任务 doc 未合 main 不进比较集。**与 Win 建议（日期窗口）的分歧**：选任务状态信号而非日期信号（日期窗口漏拦在途/误拦已关闭——V5.0.1 豁免被移除同因）。**验证**：verify-parallel-ci 14/14；D487 分支实测（144824d3）exit 0——D292/D338/D469/D500 四对全豁免零 ❌；**D487 分支合 main 后 PR #261 CI 全绿**（TypeScript+Iron Laws/Vitest×2/双平台 Gate/Golden Case F1 全 success）。 |
| 2026-08-28 | 🆕 **D555 verify-parallel 串行复用误拦根治（V5.2.4 PR #282 + V5.2.5 PR #283，创始人版本管理规范约束下执行）**：**V5.2.4**——_is_closed_doc() 三信号豁免已关闭任务（task-state audited / audit-reports 报告 / 括号式 (D#) 合入提交），只豁免已合 doc 侧 + 输出点名，fail-closed 不削弱（T9）；**V5.2.5**——信号 3 补 D478 类任务（Win 线终审无报告文件，M4 类记录缺口）；**关键正确性坑**：`git log` 无匹配也 exit 0（空输出）——`if git log` 恒真致全量豁免，T9 抓到后改判输出非空。**连带根治**：ct-test-gate 剥 GIT_INDEX_FILE（D521-3 泄漏根治——沙箱测试提交污染宿主 index 致 git commit「invalid object」，D555 实证）；alloc-task-id.test.sh 修 `$MAIN_MAX（` 全角标点当变量名（模式2 潜伏 bug，gate 改动后暴露）。**测试**：verify-parallel-ci 13/13 + ct-test-gate 6/6 + alloc 13/13；D551 实证本地复跑 exit 0（173 已关闭 doc 豁免零 ❌）。**教训沉淀**：门禁豁免信号必须选「任务状态信号」而非「存在性信号」（V5.0.1 豁免被移除的原因）。 |
| 2026-08-29 | ✅ **K3 审计两批回流 main（D483-D486 认证闭环批 PR #289 + D540-D547 回填精修 PR #290）**：**D483-D486 批**——CONDITIONAL PASS（2 PASS + 1 CONDITIONAL PASS，0 FAIL）：register 入白名单（auth.ts L91）/ enterprise 匿名端点+管理端点保持认证 / bootstrap 绕过移除 / enterprise 17 用例 / 零回归（57/57 + tsc 28=28）/ as any 零新增 / 三 commit+PR 全 merged / CI 独立取证（#156/#170 全 success；#208 双平台红属实且分类诚实，main 已恢复）。**唯一条件项（环境依赖）**：D486 E2E 非 skip 4/4 因 K3 工作区 better-sqlite3 11.10.0 vs 交付方 12.11.1 版本漂移无法复现（D462 范畴，D546 同款注记）——非代码缺陷。**P2 措辞级 3 条**（commit 消息 overclaim / it() 计数 / note 7 缺版本漂移提示）——归 devdoc 下批。**K3 工具层提醒 → CT-44**（gen-cto-health 多 D# 派生，已入队）。**D540-D547 回填精修**：D542-D547 audit 段完善 + D547.json 新建（报告本体早前经 PR #271 在 main）。**北星三问**：register/enterprise-invite = PRODUCT-BRIEF §六 P1 底座，对齐。 |
| 2026-08-29 | 🔴 **17 任务审计派单证据指针 7 处错误（创始人要求 ③′ 再次物理复核时发现，CTO 自误第 N 次同根）**：D553 派单的 impl.commit 证据指针 7 处指向非 main 真实实现——4 个废弃分支提交（D508=35367fe6 / D534=6bfe26ff / D535=9d9b3f8e / D537=95b09aec，不在 origin/main）+ 3 个影子登记提交（D521=01334c0c / D525=61182a97 / D526=61182a97，bypass.log 登记非代码提交）。**根因**：写派单时取分支本地 git log 的 hash，未做 `git merge-base --is-ancestor <hash> origin/main` 权威验证——与 D536 教训③（核实代码现状必须 git show origin/main:）同根、D547/D550 撞号同根（分配器读本地陈旧源）。**修正（PR #292）**：全部 17 个指针用 `git merge-base --is-ancestor` 逐一物理验证，7 处回填真实 hash（D508=4982f978 / D521=40b90520 / D525=D526=d5286c52 / D534=D535=02381ad7 / D537=e4cb41ab）+ task-state 7 文件 impl.commit 修正（commit_note 记录原始错误值）+ 派单文档指针行更新 → CT-45。**教训固化**：派单③′ 复核的证据指针检查 = 每个 impl.commit 必须 merge-base 验证为 origin/main 祖先 + 必须是非 bypass 登记类提交；勾 checklist ≠ 复核，命令输出才是证据。 |
| 2026-08-29 | ✅ **K3 GA 线闭环批审计验收 + 回流 main（PR #293）+ 转 CTO 队列三项处置**：**结论**——D551 PASS（四端点契约逐条一致/回流诚实边界/双迁移有测试/40+40 独立重跑绿）+ D487 CONDITIONAL PASS（双写链路/5 装配点/迁移实测/D469 前提全部物理证实）。**CTO 合并前物理复核**（与 main 权威比对）：四端点 L121/L239/L314/L419、requireGa 三态 ga-auth.ts L20-36、diagnosis-launcher.ts L98-104/L323-326 全 exact match；`as never` 盲区（mcp/index.ts L236）属实；P1 属实（测试内 DROP TABLE 为降级路径测试非迁移测试）。**P1×1 处置**：D487 session_events 重建迁移无提交测试（K3 自写 /tmp 脚本实测 PASS 但未入库）→ **立项 D558**（派单编码线：≥4 断言迁移回归测试 + spec §4 测试表同步；归因 devdoc：spec §4 未同步 §3.2 新增迁移）。**转 CTO 队列**：① as never/as unknown as 门禁盲区 → CT-46（V5.2.7 PATCH 待修）；② K3审计清单-20260822 全部 P0/P1 已 audited 过期 → 本批更新为当前状态版（17 任务批/GA 线批/D558 在途）；③ CI 报告口径（GitHub Pages build 预存失败 ≠ 产品门禁）→ 精确化建议登记。**K3 质量记录**：环境注记先行（D316 同型，Node 22 ABI 假失败 → Node 24 实测）、CI job 级取证、P1 归因清晰——审计质量良好。 |
| 2026-08-29 | ✅ **K3 impl-done 处置批审计验收 + 回流 main（PR #297，main=dc84af51）+ FIX 立项**：**结论**——10 PASS + 5 CONDITIONAL PASS + 2 FAIL（D515 历史结论收录入库 + **D549 本批新判 FAIL**）。**CTO 合并前物理复核（5 项全验）**：① D549 FAIL 属实且更重——PR #272 FIX 分支 731d2469 提交消息声称「修复+canary」但 diff 仅 task-state 11 行零代码（M2 最重级）+ claim-regex-narrow.test.sh:71 仍硬编码 749p；② D509 属实（g12-day-window.test.sh main 不存在，恢复源=dangling 9cb09dbb）；③ D535 属实（本地实测 7/8，4b 断言红）；④ D537 属实（e4cb41ab check-runs job 级 ubuntu FAILURE）；⑤ D508 合理（merge-base 化数学恒等，声称根治不实）。**D516 P1 不成立**（tag V5.0.0 实测指向 503d04ca 修复树含 SYNO_CI，非 K3 所述 8cdf9957——K3 审计状态陈旧或误读，记录供 K3 复核）。**D521/CT-43 确认 D554 已修**。**处置**：FIX 立项 D560（D549 重做，编码线）+ D561（D509/D535/D508 三恢复，编码线）；D537 L4「PR #223 CI 红仍被合并」→ **CT-47 合并门禁缺失**（治理冻结期登记待排）；K3 清单翻转 ✅。**教训固化（CT-45 扩展）**：证据指针验证「在 main」不足——必须再验「提交 diff 确含声称写集」（D549=756573df 在 main 但零代码，指针验证漏网）。**K3 质量记录**：job 级 CI 取证 + file:line 精确 + 环境依赖区分——本批审计质量高，D549 的 M2 挖得深；D516 一条状态陈旧。 |
| 2026-08-29 | ✅ **三连任务验收闭环（D558/D560/D561，PR #300/#301/#302，main=4df4f981）**：**CTO 物理复核全通过**——D558 17/17（新增迁移 4 断言含 S-5 先红实证）+ mcp as never 双清（L146+L236）+ test-kit 棘轮基线（9/87 含 .tsx 口径）+ spec §4 同步；D560 9/9 + canary 19→25（清单实测 25 项）+ T5b 动态化漂移弹性证明（插 3 行自动跟随）；D561 g12 10/10（自 9cb09dbb 恢复）+ incident-loop 8/8（4b POSIX 修复）+ bypass 注释如实化。**编码 session 质量记录（高）**：D333 自决 3 项如实登记 + 不冒领登记 + S-5 先红证据链完整。**CTO 自误（第四次 D547 复发）**：D558 brief 架构层 inline 系 CTO 派单时埋雷、已知 latent 未当轮修，被自己修的 resolver 窗口（V5.2.7）在 CI 暴露——教训固化：**发现 latent 缺陷必须当轮修复，不能留给门禁兜底**。**编码 P2 过报（转 K3 复审提示）**：D560 Done「grep -c 749p → 0」与实测不符（注释行含 749p，grep=1）——功能属实、报告小过报。**待办**：K3 复审批（D562 派单）+ checkpoint-deploy 提醒创始人。 |
| 2026-08-29 | ✅ **K3 复审批回流（PR #304，main=4f4dcb9a）——30 个 impl_done 审计结论全部完整闭环**：D558/D560/D561 三 PASS（reaudit=true、blockers=0）；CTO 合并前物理复核全命中（断言行号 :222/232/252/261、:1198/1210、ci.yml:176、canary=25、bypass 非注释变更 0 行）。**K3 质量记录（高）**：S-5 先红独立复现（4 failed|13 passed）+ 棘轮基线全仓独立扫描逐项一致 + 父提交红态复现法——复审批做成了独立验证而非照抄交付报告。**新 P2×3 登记**：① g12/incident-loop 未入 canary（M3 家族第三次 → CT-48，本批补 g12 入列 25→26；incident-loop 首测 Windows 失败，归编码线 D561 后续）；② incident-loop.test.sh L69-70 注释未同步 POSIX 机制（编码小修，归下批）；③ dangling 9cb09dbb 来源不可核验（实质已按 2370fc88 对照闭环，记档）。**编码 P2 过报确认**：D560 749p 注释行残留（grep=1）——功能属实、M2 家族、不影响 PASS（K3 已判）。**里程碑**：D393 派生制下 30 个 impl_done 全部有审计结论；CTO-HEALTH 待重生成。 |
| 2026-08-29 | 🔴 **D489 验收发现（CTO 物理验收，main=f55a6dbf）——门禁立功 + CT-47 第二次实证**：**① V5.2.7 组 1 门禁正确工作**：PR #299（feat/win-d489-consult-events @32d78014）CI Iron Laws 红 =「as any / as never / as unknown as 零容忍: 1 处」——新逃逸 `orchestrationDb as never`（diagnosis.ts L181）被当天上午刚修的 CT-46 门禁物理拦截（本地复现一致：diff 命中 1）。**② 但 PR 在检查完成前被合并**（merged_by=创始人账号，merged_at 14:08:42Z 早于失败时间戳 14:10:18Z）→ 逃逸现于 main；同文件 L411 同型存量（D489 引用其作「先例」——先例本身就是逃逸）。**处置**：D563 立项（两处类型窄化 + 棘轮基线 9→7，编码线）+ D489 入 K3 初审（D563 修复后一并复审，派单已写）。**③ canary 二次立功**：incident-loop.test.sh 首纳入 Windows 实测 6/8（D561「8/8」是 macOS 单平台口径）→ D564 立项（Win 兼容修复后回列 26→27）。**升级创始人**：CT-47 同类第二次 = 合并门禁缺失防线系统性失效，待裁决加严（branch protection / 合并权回收）。 |
| 2026-08-30 | ✅ **D489 验收 + 合并（Codex PASS，PR #299 → squash f55a6dbf，片2-B）**：D394 片2-B「GA 诊断会话事件化·consult 路由接线」——`src/routes/diagnosis.ts` consult 路由由直建引擎改经 `DiagnosisLauncher`（D487 片2-A 复用），会话装配（`orchestration.db` → `SessionStore` → `createSession` 非空 sessionId），诊断事件落 `session_events`，D394 片2「诊断过程可回放」端到端闭环（片2-A D487 + 片2-B D489 全落地）。**Codex 独立验收**：新测试 4/4 + D487 回归 5/5 + tsc 零新增 + `as any`=0 + CI 10/10 绿 + 写集 6 文件零越界（7 项物理证明齐全）。**合并处置**：PR #299 遇 bypass.log 尾追冲突（D489 开出后 Mac 推进 D558-D562 审计批 10 提交，仅 bypass.log 撞车、代码零交集）→ 本地 merge origin/main + bypass.log 双写合并去重 → 推送解阻 → squash 合并。**待办**：K3 正式审计（片2 完成后）。 |
| 2026-08-30 | ✅ **D556 双线验收合并（dev-doc spec #308 f8dadf7b + 编码实现 #309 69d81c58）**：**CTO 独立复核全通过**——63/63 三层测试实跑（logic 37+ui 17+engine 9，Node 24 03:14 独立重跑非读旧日志）+ CI job 级 10/10 + 接线 grep（RightPanel L22/L789、双消费）+ 占位零残留 + as any=0 + 新行 as never=0 + DS6 受限路径 diff 空 + 引擎唯一删除行=union 终止符。**三处受控偏离已登记**（渲染桥接零依赖等价实现/seed 通道 JWT 白名单不含 /api/ga/* 不可达/层2 sink 枚举内值）——**seed 通道裁决项转创始人**（修复路径 A: 等 D483-D486 真实 JWT 落地；B: auth 白名单放行 /api/ga/*——auth 领地，D483 在途禁碰）。**CTO 自误（M7 型吞错，登记）**：D556.json 合并冲突解决时 python 脚本崩（main 版 JSON 解析失败）未检退出码即 add/commit → 冲突标记文件误入 9b2e7cff 并推送；修复 c8a18e99（校验写回可解析）+ 纯补记。**教训固化**：合并冲突解决脚本必须先 assert 每步成功再写盘；写盘后强制 json.load 自检。**编码/复核 session 质量记录（高）**：独立复核不信自报 + 措辞更正诚实（DS6 零删除行→1 行终止符）+ 时间窗警告精确。**待办**：K3 提审（派单已写）+ checkpoint-deploy 提醒创始人。 |
| 2026-09-02 | 🆕 **架构缺口登记：哨兵测量值无时序存储（发现已存、原始值未存）**：哨兵「发现」已由 D394 片1 事件溯源落 `sentinel_events`（append-only，I1/I2/I3）+ `sentinel_tickets` 工单闭环 + `sentinel_baselines` 基线；但 compute（25 测量器）的**原始测量值**不落任何时序表——`SentinelFinding`（src/sentinel/types.ts:41-53）无 value 字段，全仓无 measurement/timeseries 表，compute 纯函数不写 GraphStore（本体层 graph_nodes/graph_triples 只存静态事实）。后果：只有「是否超阈值的告警」，无「测量数值/趋势/同比/时序回放」载体。**归属判定**：产品方向未定——若未来做持续增长导航的趋势分析/历史曲线，需立 measurement_timeseries 表（measure_id+org_id+时间戳+数值+来源快照）；若仅告警闭环，当前够用。**待创始人定产品方向后决定是否立项。** |
| 2026-09-02 | ✅ **D563/D564 终局合并（7 轮返工复盘）**：#313（fc4424d5）+ #314（5f7d69af），Windows 真验收达成。**真根因（第 6 轮 FAIL_NAMES 诊断后定位）**：cp1252 解码——incident-loop.py verify() 的 subprocess text=True 在 Windows 用 locale 编码（cp1252）解码 hook 的 UTF-8 输出（「禁止」0x81 未定义）→ UnicodeDecodeError → degraded → 误报 open；修法 encoding="utf-8" errors="replace"（macOS 行为零变化）。**连带修复**：platform-checklist.test.sh 存量三联缺陷（探针绝对路径 vs ^scripts/ 锚定空转 / rm 先于复用 / 点名断言被 ✅ 头行空洞满足——main 巧合掩盖，本地复现 13/14→14/14）。**7 轮反复根因复盘（已记）**：①CTO 派单 brief 格式缺陷×4（本地软提示 CI 硬拦的时间差）②brief 日期窗口（±1 天补丁，治本 CT-49 待修）③Windows 专属失败诊断需 CI 迭代（已修：编码预设加 CI 自诊断 + 交付前本地验收 SOP）④main 持续被多 session 推进致基移冲突。**编码 session 质量（高）**：每轮带物理证据、cp1252 根因自行挖到底（第 4 轮归因被第 6 轮证据修正后诚实改判）。**待办**：K3 派审（D489 初审 + D563/D564 复审，派单已备）；CT-49（认领窗口去日期化）；CT-48（canary 双平台验收 SOP）。 |
| 2026-09-02 | 🆕 **CT-50 候选：checkpoint-deploy.sh 与桌面形态不匹配**——deploy 验收模板（localhost:3000 HTTP 服务器形态，76c26ae9 时代产物）对当前 Electron 桌面架构失效：本轮 D563/D564 合并后执行该脚本 → 服务不可达（本机无 3000 服务，产品后端实际 18790）。**本轮替代验证已做**：dev 后端真实启动 + healthz degraded 正常 + consult 401/JWT 降级语义活 + 无回归。**建议**：checkpoint-deploy 增加 desktop 形态分支（读 desktop-dev-prod.md 收敛声明，prod=安装包路径，dev=18790 冒烟）或标注「HTTP 服务器项目专用」。CT-47/48/49 同队列。 |
| 2026-09-02 | ✅ **K3 批回流（PR #323，59b918d7）：D489 片2-B CONDITIONAL PASS + D563/D564 复审双 PASS——GA 事件化链（D487→D489→D556）+ D563/D564 全闭环**：**CTO 物理复核**：三 JSON 合法、cp1252 机制本地复现（b'\xe7\xa6\x81'.decode('cp1252') → UnicodeDecodeError）、P1 核实（test-kit 不在 CI vitest）。**新发现**：P1 = 棘轮基线测试未入 CI（M3 家族再现——test-kit 套件缺 CI 接线，归 control-tower，FIX 队列待派）；P2×3（as EngineContext 整对象断言 L216 / hook-git-guard python3 fail-open 边界 / D564 task-state 归属表述不精确）；L4：CT-47 系统性待创始人裁决（branch protection）。**K3 质量记录（高）**：red 独立复现（worktree @ f55a6dbf^ + SqliteError 逐字一致）+ CT-47 API 取证 + cp1252 机制实证——全部独立复现不采信声称。**待办**：test-kit CI 接线 FIX 待派；CT-47/48/49 待创始人裁决。 |
