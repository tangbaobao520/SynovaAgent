# 审计提请 · D858（K3 队外复审）— CT-70/D854 条件闭环

> 提请方：并行 CTO session（D858 小队队长）｜ 提请日：2026-09-21
> 对象：**PR #687** ｜ 分支 `gate/d858-canary-glob` ｜ 工作树 `.synova-wt-d858` ｜ 基线 `origin/main cc4e4166`（= 你 D854 报告的审计基线 `25082884` 之后 5 个提交）
> **审计红线自检**：本卡未碰 `scripts/audit/**`、未编写审计标准、未自我审计（队内结论只写"自验"）。

## 一、为什么提请

K3 报告 `docs/synova/audit-reports/2026-09-20-K3-D854.md` 对 CT-70（PR #682）判 **CONDITIONAL PASS**。
按创始人 2026-09-20 纪律「有条件通过 = 未通过」，D858 逐条闭环后提请**队外复审**；**复审通过前 PR #687 不合并，#682 亦不合并**。

## 二、四条条件的闭环主张（请逐条证伪）

| 条件（K3 原文位置） | 本卡主张 | 可核入口 |
|---|---|---|
| **P1**（§6）：canary 覆盖语义由注释文本承载 | 覆盖来源改为**非注释行**可执行项 + glob **文件系统物理展开**（`nullglob`，零命中不算覆盖）；注释不产生覆盖 | `scripts/control-tower/check-canary-drift.sh`（270 行，sha256 `574d4465…`）L63-140；夹具见证据件 §2 |
| **P2**（§5）：绿腿吞 ⚠ | 告警/摘要走独立通道 `$GITHUB_STEP_SUMMARY`（stdout 被重定向仍完整）+ 保留 `::warning`；真实 CI 绿腿证据 | 证据件 §4/§7；run `35529862520`（CT Gate ubuntu job `106128436921` success；windows job `106128436902`，结论见证据件 §7.3）+ 归档片段 |
| **L4-1**：注释声明与物理覆盖一致性 | 注释（整行 + 行尾）声明的路径/glob → DECLARED；未被物理覆盖 → `::error title=canary-fake-coverage` + **exit 1** | 同上 L118-165；夹具 F8/F9/H1/H3 |
| **L4-3**：匿名审计取不到 job 日志 | 标准件成文：匿名通道 `check-runs` + `check-runs/<id>/annotations`（可拿 `::warning`/`::error` 正文，绿腿亦可取；配额 60/h/IP，必须显式判 degraded）；完整日志由交付方归档 + sha256 + `raw.githubusercontent` 匿名取用 | `docs/synova/coordination/D858-证据包标准件-匿名取CI证据-20260921.md`（含 403/200 实测与示例输出） |
| **回归**（K3 §1-§4 已过的接线/无放宽） | 既有判定不变：真实 main `729 \| 清单 41 \| 漂移 71 \| exit 0`，stdout 与基线脚本**同 sha256 `b12210c4…`**；#682 的真实 ci.yml 预演 `清单 44 \| 漂移 68 \| 假覆盖 0 \| exit 0`（44 = 41 字面 + **3** 个 `tests/project/*.test.sh` 物理展开；你的报告写 43 = 41 + 2 条注释，那是**旧脚本的口径**） | 证据件 §5 |

## 三、本卡主动披露的已知边界（请判是否构成未闭环）

1. **`$(...)#` 残余误红**（fail-closed 方向）：`bash -c 'echo $(echo a)#c'` → `a#c`（命令替换后的 `)#` 不是注释），脚本一律把 `)` 当词边界 → 该形态下真实可执行路径被判为注释声明 → 误红。**反证**：截断内容仍进 DECLARED 且受"必须被物理覆盖"校验 → 该通道**无法隐藏路径**（不产生假绿）。真实 main/#682 ci.yml 中 `)#` 命中 0。
2. **文本级固有边界**：覆盖 = 非注释行文本 + 物理展开，不执行 ci.yml 语义（`echo "tests/x.test.sh"` 内的路径仍算覆盖）。P1 的要求是"不得由注释承载"，已满足。
3. **`ci.yml:277` 的 `|| true` 吞掉 exit 1/exit 2** → 假覆盖与执行失败在 CI 层不阻断（脚本层 `::error` 上面板）。**本卡写集硬边界禁碰 ci.yml**（#682/#657 在飞）→ 已登记 **CT-78 + D862** 排期，请判这是否影响 P1/L4-1 的"闭环"结论（我方主张：脚本侧机制已就绪，传导层缺陷另行闭环）。
4. **awk 方言**：本机仅 BSD/BWK awk；CI 双平台腿为真实 runner 证据（ubuntu 输出与本机逐字相同）。`**` 递归 glob 未启用（macOS bash 3.2 无 globstar）→ 脚本内显式声明。
5. **本卡自曝并被 CI 抓到的一次真红**：V2 的测试 H4 用「自制受限 PATH」构造 awk 缺失，在 **CI windows 腿**红（run `35528922575` job `106125920439` = failure，`❌ H4 降级口径漂移丢失`）；V4 以 `SYNO_AWK_BIN` 注入缝 + 可证伪探针根治，并已登记台账 **CT-80**（D520/CT-73 同族第二次）。请将此视为**已闭环**，但对"本地绿 ≠ CI 绿"的机制缺口（软提示无物理门禁）另作判定。

## 四、审计输入（可直接取用）

```
分支/PR：gate/d858-canary-glob / PR #687（head 以 PR 页面为准；代码冻结提交 = 44b49864（V4，最终件；其后提交仅 docs/evidence））
代码件：scripts/control-tower/check-canary-drift.sh        sha256 ec7f49aa7e57439ec1f59e154d2bd8c829ddddcba7ee361de7a0c327e2f5871e（281 行）
        tests/control-tower/check-canary-drift.test.sh     sha256 addd1d695c8b60fc16292a2b337524547c3920902438e97c795a14921b565743（339 行）
规格（契约/覆盖语义 S1-S6/夹具矩阵）：.claude/task-briefs/2026-09-21-D858-canary-glob-coverage.md
证据：docs/synova/product-lines/evidence/D858-evidence-20260921.md       （P1/L4-1/P2/回归/L4-3 + CI 归档）
      docs/synova/product-lines/evidence/D858-verify-20260921.md         （自验 T2/T4/T6 轮次与逐条实测）
      docs/synova/product-lines/evidence/D858-ci-35529313371-ctgate-{ubuntu,windows}.log（CI 归档片段 + sha256）
收口：docs/synova/coordination/收口-D858-canary-glob-20260921.md
M1：  docs/synova/coordination/D858-开工前冲突扫描-20260921.md
台账：docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md（新增 CT-78 / CT-79）
```

## 五、请给出的裁决

1. 四条条件是否**逐条闭环**（或指出未闭环项 + 最小复现）；
2. 对 §三 四项已知边界的判定（可登记 / 必须本卡闭合）；
3. D854/CT-70（#682）是否可在此后合并（#682 当前 `mergeable=dirty`，需其 owner rebase）；
4. 结论口径请沿用三态判定（通过 / 有条件通过 / 不通过）——若仍判"有条件通过"，请给可执行的最小条件。

> 交付纪律：本卡提交走 `synova-commit`（13 组门禁全绿）；无 `--no-verify`、无 `git stash`、无 force push；PR ≤12 文件 + 单域；**K3 复审通过前不合并**。
