# 审计派单：治理收口批（D540-D547）

> 派单: CTO | 2026-08-28 | 审计线: 🔍 K3（独立审计）
> 批次: 并行隔离治理收口（D540 clone 试点 / D541 正则收窄 / D542 失败可见性 / D543 密封 canary / D544 左栏验收 / D546 哨兵事件化 / D547 骨架 brief 门禁）
> 报告命名: `docs/synova/audit-reports/2026-08-28-D540-D547-governance-closeout.md`
> 审计分支: `audit/k3-20260828-gov-closeout`（独立审计工作区建分支，报告 + task-state 回填提交于此）
> 结论回填: 每 D# 的 task-state audit 段（verdict / blockers / findings / file:line）

---

## 批次背景

本批是并行隔离治理的收口任务（D540-D547），全部已合并 main（`d812e0f5` 起）。其中 D540/D541 是 K3 上轮 FAIL 的**修复后复审**；D542-D547 是**初审**。所有任务 CI 已绿（check-runs 实证），审计重点是**验证修复/实现质量与声称一致**，非重复 CI。

## 待审任务表

| D# | 任务 | 状态 | 上一轮审计 | 重点审计项（file:line 级） |
|---|---|---|---|---|
| D540 | 独立 clone 试点 + 影子提交验证修复 | 修复闭环未复审 | K3 FAIL（P0-1 CI 红） | ①修复是否真正解决 CI 红（PR #243/#246，brief 三处根因：迁移裸词/verify 字面量/brief_parser L\d+）②CI 三 job 绿实证（check-runs API）③silent-swallow 4 处修复 |
| D541 | 铁律47 正则收窄 | CI 红→修复闭环未复审 | K3 审 D540 时连带 FAIL | ①正则收窄正确性：完成声称（已迁移/迁移完成/完成迁移）触发 vs 工作描述（迁移改三处）零误伤（配测试 9 断言）②V5.2.1 bump + tag |
| D542 | CI 失败可见性（soft_check/warn_check CI strict 打 ❌）| 初审 | — | ①显示修复 vs 计数语义零变化（HARD_FAIL/SOFT_COUNT/WARN_COUNT 加减条件原样）②配对测试 ci-strict-visible 6 断言③「N 组未通过」日志可定位（M1 失败不点名修复）|
| D543 | 密封 canary 转绿 + 解析器对称 | 初审 | — | ①post-commit-marker 断言对齐 D521 hook 登记（S1a/S6a/S7/S8 期望 1 行 COMMITTED）②S10 密封性（new_repo `git init -b main`）③parse_q2 补 `\s+L\d+$` 剥离（对齐 devdoc_writeset.py:76）④CI 双平台 canary 两周来首次绿 |
| D544 | 左栏 Codex 验收合并（孤儿实现收口）| 初审 | — | ①8 条验收标注 file:line 真实（chapter2-acceptance.md）②三接口接线（/api/signals、/api/loops/status、/api/actions 调用点）③GA 置灰渲染分支④D538 实现 910 行收口进 main |
| D546 | 哨兵 findings 事件化验收（src 零改动）| 初审 | — | ①src 零改动（diff 仅 3 测试+brief+task-state）②3 测试真实性：契约对齐（sentinel_events vs session_events）、回放 sha256 全投影等价、durationMs 回归网③诚实声明核实（「runner 5 处恒 1970」与代码不符是否属实；sentinel-service.ts:97 历史缺陷是否已修）|
| D547 | 骨架 brief 占位符物理门禁 | 初审 | — | ①门禁逻辑：暂存 .claude/task-briefs/*.md 含 `认领: <agent>`/`<本任务在哪一层` → hard_check 阻断②防误伤（填好 brief 不触发）③配对测试 skeleton-brief-gate 6 断言④根因链：骨架 brief 曾误入 main 全局阻断非 docs PR |

## 审计要求（每条物理可复现，禁止文档声称冒充）

1. **验收方法**: 每条重点审计项给「命令 + 断言 + 预期输出」。代码逻辑用真实 grep/read 验证（file:line），声称 vs 实现比对（M2）。CI 结论用 GitHub check-runs API（job 级，禁整体 run 结论——D320 教训）。
2. **D540/D541 复审重点**: 上轮 FAIL 的具体 P0/P1 是否闭环——不是重审全部，是验修复点。对照 `docs/synova/audit-reports/2026-08-27-D540.md` 的 blockers 逐条。
3. **诚实声明核实（D546）**: 编码 session 声称「派单前提过时（事件层已 a8a5857e）/durationMs 线索错位（历史缺陷在 sentinel-service.ts:97 已修）」——K3 独立核实这两条是否属实（这是对编码诚实性的审计，也是对我们派单质量的审计）。
4. **报告结构**: L1-L4 四层（物理一致/接线/降级/声称表）+ 每条 D# 的 verdict（PASS/CONDITIONAL PASS/FAIL）+ blockers 带 file:line。
5. **结论回填**: task-state 各 D# 的 audit 段（verdict/blockers/findings/by=K3/at=日期）。K3 审计出问题 → 按审计闭环铁律另起 FIX 任务（禁直接改原任务）。
6. **归因**: 发现控制塔缺陷 → 记 CTO 队列；发现编码/派单缺陷 → 记台账；发现审计脚本缺陷 → 只转达（K3 自身防线）。

## 审计顺序建议

按依赖与复审优先：D540 → D541（复审，验修复闭环）→ D542 → D543（门禁健康）→ D547（门禁新加，防误伤）→ D544 → D546（前端/哨兵交付质量）。

## 产物

- 报告: `docs/synova/audit-reports/2026-08-28-D540-D547-governance-closeout.md`
- 回填: task-state/D540.json ~ D547.json 的 audit 段
- 分支: audit/k3-20260828-gov-closeout（推送 origin 后 CTO 合并回流 main）
