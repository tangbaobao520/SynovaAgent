# 审计派单：D489 GA 片2-B 初审 + D563/D564 复审要点

> 派单: CTO | 2026-08-29 | 审计线: 🔍 K3（独立审计）
> 类型: D489 初审（D563/D564 完成后一并复审）
> 报告命名: docs/synova/audit-reports/2026-08-29-D489-slice2b.md
> 审计分支: audit/k3-20260829-d489-slice2b（报告 + task-state 回填）

---

## 背景

D489 = D394 片2-B：GA consult 路由改经 DiagnosisLauncher 落流 session_events（复用 D487 片2-A + D500 地基，声称无内联复制）。经 PR #299 合 main（f55a6dbf）。

**CTO 验收发现（审计重点之一）**：① `src/routes/diagnosis.ts` L181 引入 `as never` 类型逃逸——V5.2.7 组 1 门禁在 PR CI 上正确拦截（Iron Laws 红：1 处硬阻断），但 PR 在检查完成前被合并（merged_at 14:08:42Z < 检查失败 14:10:18Z，CT-47 第二次实证）→ 逃逸现于 main；L411 同型存量。→ **D563 修复中**（类型窄化 + 棘轮基线 9→7）。② canary 首次实测暴露 incident-loop.test.sh Windows 6/8（D561 的「8/8」是 macOS 单平台口径）→ D564 修复中。

## D489 初审重点审计项

1. **落流接线**: consult → DiagnosisLauncher → persistingOnEvent 链与 D487 一致（无内联复制声称）：grep DiagnosisLauncher diagnosis.ts + 与 D487 diagnosis-launcher.ts 对照
2. **会话装配**: SessionStore 自 orchestration.db 构造（resume 先例）+ createSession 非空 id（D487 空 id 桶缺陷防线）；构造失败 log.warn 降级不崩（铁律 24/31）
3. **行为差异四声明（dev doc §3.2）**: concerns slice(0,200) / 首条 phase_started / background-review fire-and-forget / report 只落库——逐条 vs 实现
4. **测试质量**: diagnosis-consult-events.test.ts 4/4 非空壳 + S-5 先红证据（red=no such table agent_sessions）+ D487 回归 5/5
5. **SSE 透传/post-processing/report 缓存零回归**
6. **北星三问**: 片2-B 属 D394 GA 事件化计划（PRODUCT-BRIEF 对齐性）

## D563/D564 复审要点（初审同批）

- D563: diagnosis.ts 零 as never + 窄化不改降级语义 + 棘轮 9→7 与全仓实测一致
- D564: incident-loop Win 8/8（job 级）+ canary 26→27 + macOS 语义零变化

## 要求

命令+断言+预期输出（file:line 级）；CI 结论 job 级；回填 task-state/D489.json（audit 段）+ D563/D564（复审段）。

## 产物

报告 1 份 + 三 task-state 回填 + 分支推送 origin 后 CTO 合并回流。
