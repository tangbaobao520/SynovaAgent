# 审计派单：GA 线闭环批（D551 + D487）

> 派单: CTO | 2026-08-29 | 审计线: 🔍 K3（独立审计）
> 批次: GA 人机协同两条线刚合并 main——D551（Mac DSH，GA 校准后端，PR #281 已合 6e9626f9）+ D487（Win，GA 诊断会话事件化装配，PR #261 已合）
> 报告命名: `docs/synova/audit-reports/2026-08-29-D551-D487-ga-line.md`
> 审计分支: `audit/k3-20260829-ga-line`（独立审计工作区建分支，报告 + task-state 回填提交于此）
> 结论回填: task-state/D551.json、task-state/D487.json 的 audit 段（verdict / blockers / findings / file:line / by=K3 / at=日期）

---

## 批次背景

两条线各自完成并 CI 全绿合并，均未审计。D551 由 CTO 物理复核过（40/40 新用例 + 52/365 域回归 + tsc 28=28 + 三存量路由零回改），但 CTO 复核 ≠ 审计（审计红线）。D487 由 Win 交付（13 文件写集），CI 全绿（TS+Iron Laws/Vitest×2/双平台 Gate/Golden Case F1），未经独立复核。**本批是两任务的初审。**

## 待审任务表

| D# | 任务 | 类型 | 重点审计项（file:line 级） |
|---|---|---|---|
| D551 | GA 校准后端（诊断校准/手动信号注入/反馈效用仪表） | 初审 | ①spec §8.2 契约表 vs 实现一致性（四端点 + requireGa + 版本链 + 统计）②§7 回流边界未被越界（诚实边界第一查项：层 2/层 3 未假装闭环——engine 白名单零 diagnosis_conclusion 分支属实）③agent_memory 迁移的存量库安全性（d551_memory_type 重建表迁移：事务 + fail-closed + FTS 重索引）④D556 前端接入前的后端契约完整性（D556 spec 已依赖本契约） |
| D487 | GA 诊断会话事件化装配（D500 地基接线） | 初审 | ①session_events 双写链路（persistingOnEvent：phase_*→diagnosis_phase / 其余→diagnosis_module / 成功→diagnosis_report / 失败 error 也落流）②迁移幂等 + 行保留 + seq 连续（旧库升级实测）③5 实例化点装配（cli×2/im-inbound/mcp/tui-v2 + bootstrap）——D500 悬空第二处（tui-v2 bootstrap）④诚实声明：写集 13 文件 vs 实际 diff 一致（§3.2 同 commit 回填实测形态）⑤D469 信号4 豁免前提属实（D469 impl 在 main、无 task-state——与本次门禁修复的因果链） |

## 审计要求（每条物理可复现，禁止文档声称冒充）

1. **验收方法**: 每条重点审计项给「命令 + 断言 + 预期输出」；代码逻辑真实 grep/read 验证（file:line）；CI 结论用 check-runs API（job 级）。
2. **D551 特注**: CTO 已物理复核过（证据在台账 D551 验收行）——K3 独立重跑三测试（ga-calibration/ga-manual-injection/ga-auth）+ 契约表逐条比对即可，不必重跑全量域回归（52/365 已按清单复跑过，K3 抽验）。
3. **D487 特注**: Win 缺陷报告（--ci-pr 串行复用误拦）与本任务的关系链——K3 核实门禁修复（D555/D557）与 D487 交付互相独立（Win 未改门禁脚本，写集零 scripts/）。
4. **报告结构**: L1-L4 四层 + 每条 D# 的 verdict（PASS/CONDITIONAL PASS/FAIL）+ blockers 带 file:line。
5. **结论回填**: 两任务 audit 段。K3 审计出问题 → 另起 FIX 任务（禁直接改原任务）。
6. **归因**: 控制塔缺陷 → CTO 队列；编码/派单缺陷 → 台账；审计脚本缺陷 → 只转达。

## 产物

- 报告: `docs/synova/audit-reports/2026-08-29-D551-D487-ga-line.md`
- 回填: task-state/D551.json、D487.json 的 audit 段
- 分支: audit/k3-20260829-ga-line（推送 origin 后 CTO 合并回流 main）
