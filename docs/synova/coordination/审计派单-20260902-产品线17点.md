# 审计派单：产品线 17 点批量审计（🟡 待确认/待重跑 清账）

> 派单: CTO | 2026-09-02 | 审计线: 🔍 K3（独立审计）
> 类型: 产品完成度独立复核（26 线 156 点中 17 点 🟡：实现声称存在、从未独立复核）
> 报告命名: docs/synova/audit-reports/2026-09-02-productlines-17points.md
> 审计分支: audit/k3-20260902-productlines（报告 + product-progress.html 状态翻转 + 26 线 evidence 回填）

---

## 背景与目标

产品完成度 156 点：🟢 15 / 🟡 17 / ⚪ 124。本批 17 个 🟡 点全部是「代码实证存在（部分有旧审计线索）但未独立复核」——本批做独立复核后，每点三值裁决：**🟢 确认**（product-progress 翻绿）/ **🔴 不实**（转 FIX 任务，禁止直接改状态）/ **🟡 仍存疑**（列 blockers + 缺口）。

## 审计总则

1. **独立复现，不采信声称**：每点给「命令 + 断言 + 预期输出」；代码证据 file:line。
2. **凡涉 LLM 实测的点**（如 21-1 DeepSeek）：无 key 时以结构/契约/降级路径审计替代，如实标注「行为未实测」。
3. **状态翻转权威**：product-progress.html 状态列由 K3 在审计分支直接更新（本批唯一允许改仪表盘的角色），CTO 合并回流。
4. Node 24 实测（D316 同型：Node 22 better-sqlite3 ABI 假失败不采信）。

## 五组审计项（17 点）

### 组 A：专家与插件体系（15-1, 15-3, 25-1, 25-2, 25-3）——5 点

| 点 | 声称 | 验证入口 |
|---|---|---|
| 15-1 | 专家注册表 + 文件驱动自动发现（7 位全注册） | `expert/` 目录 7 位实数 + expert-registry.yaml 加载链 + **V5 视图是否硬编码旧 6 专家**（审计员 v1.1 遗留，任务 282 传播核查） |
| 15-3 | 工具/技能工作流接线（专家真能用工具） | ExpertDispatcher → tools 链路 grep + 一次真实调度 trace |
| 25-1 | 专家插件（新专家=加文件自动注册） | 复制一个测试专家目录 → 断言自动注册 → 删除（沙箱内完成，不污染仓库） |
| 25-2 | 技能插件（新技能=加目录自动发现） | 同上，skills/ 发现机制 |
| 25-3 | 哨兵插件（新哨兵=加文件自动加载） | extensions/sentinels builtins 自动扫描链 |

### 组 B：企业事实与记忆（18-2, 18-5）——2 点

| 点 | 声称 | 验证入口 |
|---|---|---|
| 18-2 | SessionStore + 重启恢复（重启后记忆还在） | **真实重启复现**：内存库写会话 → 重建实例 → 断言恢复（「重启恢复测试未作为证据登记」即本点缺口） |
| 18-5 | 版本化 + superseded_by 链（事实更新可追溯） | enterprise_fact 版本链写入→更新→superseded 断言（AgentMemoryStore） |

### 组 C：LLM 与 providers（21-1, 21-2）——2 点

| 点 | 声称 | 验证入口 |
|---|---|---|
| 21-1 | DeepSeek 适配器（标准 API 接入） | providers/deepseek.ts 契约审计（请求构造/错误处理/降级）——**无 key 时行为未实测如实标注** |
| 21-2 | 提示词优化策略（效果可测量） | 证据记录自认「深度优化 = 0」——预判 🔴 不实，审计确认后转 FIX |

### 组 D：循环与运行时（7-7, 10-4, 19-2, 20-5, 22-1, 24-4）——6 点

| 点 | 声称 | 验证入口 |
|---|---|---|
| 7-7 | 定时任务容错（失败有记录、可重试） | CronScheduler 失败记录 + 重试链 file:line + 一次真实失败注入 |
| 10-4 | 现金流跑道计算正确（months.ts:60 filter bug 修复） | bug 修复 diff + 修复后计算用例（旧 bug 输入 → 正确输出） |
| 19-2 | 方向失效信号定义（规则明确） | 方向失效信号的规则定义 file:line + 判定示例 |
| 20-5 | 熔断/优雅停机/卡会话检测 | 三组件 file:line + 各自触发条件真实注入一次 |
| 22-1 | 看门狗/熔断/健康检查 | healthz 已实证 degraded 语义（CTO 冒烟）；补看门狗周期 + 熔断阈值证据 |
| 24-4 | 防篡改（审计哈希链） | 哈希链实现 file:line + 篡改一位 → 校验失败实测 |

### 组 E：数据接入与对话（2-5, 4-3）——2 点

| 点 | 声称 | 验证入口 |
|---|---|---|
| 2-5 | 多轮对话上下文不丢（第 10 轮记得第 1 轮） | 线程/上下文管理 + 长会话注入实测（ConversationEngine + 线 20 交叉） |
| 4-3 | 飞书连接器 | 连接器 file:line + 数据契约 + 一次 mock 拉取（审计员严重问题-2 遗留线索） |

## 回填与产物

1. 报告（L1-L4 + 每点三值裁决 + file:line）
2. product-progress.html：🟢 点翻绿 / 🔴 点如实标注 RED + FIX 指针
3. task-state 或 26 线 evidence 按仓库惯例落位
4. 分支 audit/k3-20260902-productlines 推送 origin 后 CTO 合并回流

## 预期效果

17 点清账后：产品完成度 🟢 15→30±，⚪ 124→110±——「产品到底走到哪了」对创始人可见，后续产品任务排序有了真实地基。
