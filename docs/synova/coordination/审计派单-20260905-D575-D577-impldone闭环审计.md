# 审计派单 — D575 + D577 impl_done 闭环审计（双任务一次提审）

> 派单: CTO | 2026-09-05 | 审计线（K3 专属，Mac kimi-k3-audit 或 Win Kimi CLI 双轨任一）
> 来源: 闭环债清理——D575 / D577 均已合并 main（#354 / #355）但 audit 均为 null（task-state 物理核实）。
>   D577 审计同时是产品线兑换前置：7-2 / 8-1 / 10-3 三个 failed/uncommitted 验收点等 K3 结论才能走 redeem（CT-53：禁任务自我兑换）。
> 协议: docs/synova/coordination/AUDIT-PROTOCOL.md（L1-L4 四层）
> 上一轮教训注入（D572 K3 FAIL，2026-09-04）：①「verified 自动兑换假绿」P0——本单验收点级结论必须独立复核，不采信交付方自述；②「计时证据不可复核」P1——凡声称的命令证据必须可复跑或显式声明为何不可；③「verdict 滞留 CONDITIONAL」P1——本单每任务必须给终局 verdict；④环境注记先行（D316/D551 先例）。

---

## 一、任务定位

两个 impl_done 任务一次提审（一次 K3 会话，报告内每任务独立 verdict 章节）：

| 任务 | 内容 | 合并 | 为什么现在审 |
|---|---|---|---|
| D575 | LLM 配置首启向导（credential seam + 首启向导 + 热重载） | 6517c052 → c124f8c8 (#354)，2026-09-04 | 桌面端用户第一触点能力，impl_done 未审即已在 main 运行 |
| D577 | 哨兵阈值配置真实挂载（resolveThresholds 单一解析点 + 34 判定点接线 + DEPLOYS 降级修复） | 46d296bd → ece4e268 (#355)，2026-09-05 | 修 7-2/8-1/10-3 三个 P0 级验收点；K3 结论 = 兑换前置门 |

## 二、现状材料（全部 origin/main 已核实存在，执行方必读）

### D577（哨兵阈值挂载）

| 材料 | 路径 | 核实方式 |
|---|---|---|
| spec（5 必答 + 39 判定点对照表 + 写集） | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D577-sentinel-threshold-wiring-20260905.md | git cat-file HIT |
| 编码指令 | docs/synova/coordination/编码指令-D577-sentinel-threshold-wiring-20260905.md | git cat-file HIT |
| 证据包（git 跟踪，4 件） | docs/synova/audit-reports/D577-sentinel-threshold-wiring-evidence-20260905/（README + DS8-flip-physical-verification + T1-T10-red-green-evidence + wiring-and-hygiene-grep-evidence） | git ls-tree HIT |
| task-state | task-state/D577.json（impl 21 修改 + 2 新建文件清单） | git cat-file HIT |
| 独立复跑入口 | `npx vitest run tests/sentinel/threshold-injection.test.ts`；`D577_FLIP_TEST=1 npx vitest run tests/sentinel/threshold-manifest-flip.test.ts`（独占运行）；`npx vitest run tests/sentinel/`；`npx tsc --noEmit`（声称 28=28 基线） | 复跑命令见证据包 README |

注：证据包 README 自述 spec 提议的 `evidence/` 路径被 .gitignore:76 全局忽略，故编码方将证据落盘到上述 git 跟踪目录——该处置正确（先例 D524），且附了验收点级映射（7-2/8-1/10-3 → 证据文件）。

### D575（LLM 首启向导）

| 材料 | 路径 | 核实方式 |
|---|---|---|
| spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D575-llm-first-run-config-20260904.md | git cat-file HIT |
| task brief | .claude/task-briefs/2026-09-05-D575-llm-first-run-config.md | git cat-file HIT |
| 派单（DSH 借鉴锚点 A1-A3 带文件+行号） | docs/synova/coordination/派单-D575-llm-first-run-config-20260904.md | git cat-file HIT |
| task-state | task-state/D575.json（impl 13 文件清单） | git cat-file HIT |
| 代码+测试（可独立复跑部分） | src/services/llm-credential-store.ts、src/routes/llm-config.ts、src/config.ts、src/server.ts、electron-renderer/src/components/{LlmSetupCard,WelcomeScreen,StatusBar}.tsx、tests/services/llm-credential-store.test.ts、tests/routes/llm-config.test.ts、tests/llm-config-frontend.test.ts | git cat-file HIT ×6 抽核 |
| 独立复跑入口 | `npx vitest run tests/services/llm-credential-store.test.ts tests/routes/llm-config.test.ts tests/llm-config-frontend.test.ts`（声称 64 用例） | — |

### ⚠️ D575 证据链注记（审计前必读，CTO 2026-09-05 物理核实）

task-state/D575.json 声称 `evidence/D575/` 12 件（README + red/green 输出 + DS7 GUI 双重启截图 ×3 + DS8/DS9 生产态证据）。物理事实：
1. 根级 `evidence/` 被 .gitignore:76 全局忽略 → **零跟踪**（git ls-tree 空）；
2. Mac 主工作区 `/Users/wane/SynovaAgent/evidence/` 仅有 D544/D546/D556，**无 D575**；本机各 worktree 亦无；
3. 即 red-first 证据、GUI 截图、生产态探测**均不在 git、本机不可见**。

审计处置要求：上述不可见证据一律按「证据不可复核」独立评级，**不得因缺失而自动采信声称**；你可用的事实源 = git 跟踪的 13 个代码/测试文件 + 你本人的独立复跑结果。freshness 无虞：D575 合并（#354）之后 main 对 electron-renderer/ 与 src/routes/llm-config.ts 无后续变更（D576 只动 scripts/docs，D577 只动 src/sentinel）。

## 三、审什么（每任务独立章节；审计员定夺，以下为 CTO 建议重点）

### D577
1. **34 判定点接线真实性**：独立复跑 threshold-injection.test.ts（T1-T9）；T8 零裸阈值 grep 自行重跑（不采信 wiring-and-hygiene 自述）
2. **DS8 flip 幂等**：`D577_FLIP_TEST=1` 独占复跑；核对「改 manifest → findings 变化 → 恢复」物理验证链
3. **DEPLOYS 静默降级修复**：catch 有 log + degraded 传播（铁律 24/31），loader 降级是否可达消费方
4. **memStore 覆写进 check 闭环**（T5/T9）
5. **验收点级兑换建议**：对 7-2 / 8-1 / 10-3 逐点给 PASS/FAIL 结论（兑换由 CTO 走 redeem，你只出结论）

### D575
1. **64 用例独立复跑**（green 可验证；red-first 见注记按不可复核处理）
2. **G1 零 DSH 依赖**：`grep -r "@deepseek-ai" src/ electron-renderer/src/` 应为 0（施工图 R1：Stage 3 前零依赖）
3. **接线完整性**：server.ts 挂载 llm-config 路由；App.tsx → WelcomeScreen → LlmSetupCard 调用链真实（防「建成未接线」M3）
4. **热重载同进程语义**：API 集成 21 用例中的热重载断言是否真实验证「不重启进程配置生效」
5. **凭证安全**：key 不明文进 synova.json（只存引用）；凭证文件 0600；错误响应不回显 key（借鉴锚点 A2）
6. **验收点级结论**：该切片服务哪些验收点（对照 task-state impl 记录），逐点给结论

## 四、写集与边界

- 可写：docs/synova/audit-reports/2026-09-05-D575-D577-impldone-closeout.md（**文件名必须同时含 D575 与 D577**——task-state 审计态按报告文件名派生，缺一即有一任务无法闭环）
- 禁碰：scripts/audit/、product-lines.yaml（兑换由 CTO 执行）、src/、electron-renderer/、task-state/（审计态由生成器派生，无需手写）
- DSH 借鉴：无（纯审计任务，无代码产出）

## 五、验收

- 报告含 file:line / 证据文件引用；每任务独立终局 verdict（PASS / CONDITIONAL PASS / FAIL，禁「待定」滞留——D572 P1 教训）
- 每条复跑命令 + 退出码 + 时间戳；环境受限项如实标注（区分「复核证据」与「独立复跑」，不混同——D316/D551 先例）
- 7-2/8-1/10-3 兑换建议表（点级 PASS/FAIL + 一句依据）
- 结论回填与产品线兑换由 CTO 执行（审计员只出报告）

## 六、时限与优先级

P1（本周内）。完成后 CTO 回流台账 + 走 redeem 流程兑现 7-2/8-1/10-3。

---

## 附：裁决登记（非本单审计范围，留档防遗忘）

**8-3 推送去重窗口**：创始人 2026-09-05 裁决 = **同一异常键 24h 内不重推**（工单销单后可重推）。product-lines.yaml 8-3 note「窗口大小待创始人裁决」自此有解；将在 08 告警推送切片（8-2/8-4 + 8-3）派单时落 yaml 并作为该切片契约常量。
