# 审计派单 — K3 合并批审计：D579 + D580 + D581 + D582（四任务一次提审）

> 派单: CTO | 2026-09-06 | 审计基线: origin/main @ d53ed2a4
> 协议: AUDIT-PROTOCOL.md v1.1（L1-L4 四层 15 项）| 审计红线: K3 独立会话，非任何人的子代理；禁 CONDITIONAL 滞留；环境注记先行
> 上一轮教训注入（D572/D575-D577 批）: ①点级结论独立复核不采信自述 ②计时证据必须可复跑或显式声明不可 ③evidence 必须落 git 跟踪路径 ④点级兑换建议表格式（兑换由 CTO 执行）

## 一、四任务与独立复跑入口（全部已在 main）

| 任务 | 内容 | 独立复跑入口 |
|---|---|---|
| D579 | k3 verdict stale/TTL 机制 + 批次审计报告派生 | `python3 tests/control-tower/calc-k3-stale.test.py`（13 用例）+ `python3 tests/control-tower/gen-cto-health-batch-report.test.py`（5）+ 真数据对账：`python3 scripts/product-lines/calc-progress.py` 前后 diff（evidence: docs/synova/audit-reports/D579-k3-verdict-stale-evidence-20260906/calc-diff.md §6 指引）|
| D580 | 告警工单切片 8-2/8-3/8-4 | `npx vitest run tests/sentinel/ticket-store.test.ts tests/routes/sentinel-tickets.test.ts tests/sentinel/ticket-transition.test.ts tests/sentinel/finding-id-stability.test.ts`（44 文件全扫）+ flip 复跑 `D577_FLIP_TEST=1 npx vitest run tests/sentinel/threshold-manifest-flip.test.ts`；evidence: D580-ticket-slice-evidence-20260906/（red→green logs 17 件 + 独立复跑入口 README）|
| D581 | Win 验证脚本 BOM + evidence 路径 + beforePack 守卫 | BOM: `head -c 3 scripts/desktop/win-install-verify.ps1 \| xxd`（=efbbbf）；守卫: `node -e "require('./build-synova.cjs').beforePack()"` 无 dist 场景应 throw；测试: `npx vitest run tests/electron/desktop-build.test.ts`（23/3skip）；evidence 路径 grep |
| D582 | CT-60 D328 提取大小写/后缀兼容 | `bash tests/control-tower/commit-msg-consistency.test.sh`（16 用例——含小写一致/后缀提取/小写劫持仍拦）+ 兄弟套件 merge/note-mandatory |

## 二、审计焦点（CTO 指定 5 项）

1. **D579 机制正确性**: freshness_gate 语义——rejected 短路保持在前；k3_only 出口与通用出口均接入；11 点 verified→stale 的逐点 git 复核（calc-diff §3.1 声称"机制正确失效"——含 7-2/8-1/10-3 同日边界判定是否成立：K3 closeout 证据日期 09-05 vs D577 squash 提交 09-05 16:18 的"先于"关系）
2. **D580 去重键稳定性的完备性**: 45+4 aggregate 全扫之外，finding.id 的**消费方**（工单 signal_id LIKE 关联 L990、findingById L909、去重表 key）在稳定键语义下是否一致——spec 必答 4 的 signal_id 关联裁决落地了吗
3. **D580 _extinct 4 文件显式偏差**: competitive-moat/competitive-position 生产委托 4 个死文件的 id 行触碰——"不修则 finding-id-stability 物理不可 green"的声称是否成立（red 实证在 evidence）
4. **D581 守卫载体换轨**: beforeBuild → beforePack 的裁决链（electron-builder 25.1.8 源码实证 npmRebuild:false 下 beforeBuild 永不执行）+ beforePack 返回 false 会被解读为跳过 node_modules 的边界——成功路径返回 undefined 的契约
5. **四任务交叉**: 互不干扰验证（bypass.log 追加型冲突三批次解决痕迹）+ main 上全量失败集与合并前基线 diff（新失败=0 声称）

## 三、点级兑换建议表（如有，兑换由 CTO 执行）

8-2/8-3/8-4（线8）三点本批落地——如审计 PASS，按产品线兑换机制出建议表（K3 PASS = k3 类记录可翻绿；注意 D579 stale 机制下证据日期需晚于最后相关 commit）。

## 四、产出要求

报告: docs/synova/audit-reports/2026-09-06-K3-D579-D580-D581-D582-impldone-closeout.md（git 跟踪路径）
禁改: scripts/audit/、src/、product-lines.yaml、task-state 的 audit 字段（禁手工改派生字段 CT-58）
