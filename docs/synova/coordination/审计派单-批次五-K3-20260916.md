# K3 审计派单：批次五（证据流水线与看门狗 + 防线接线 + 门禁变更 + 度量口径）

> 派单: CTO（synova-cto）| 日期: 2026-09-16 | 审计预设: 🔍 `synova-k3-audit`
> 依据计划: v1.2@4e46603f（`整体推进计划-主线-20260913.md`）
> 审计红线（不变）: K3 独占 `scripts/audit/**` 与审计标准；CTO 不写审计标准、不自我审计、不预设结论
> 关联: task-state/D789.json · 批次四未审项（PR #572 待合）· 批次四派单与台账（PR #572）

---

## 一、本批为什么这样排（CTO 依据全局状态判断）

- 批次四交付已就绪（PR #572 我解完冲突，可合）；**批次五对应「本日新落地的一批机制类交付」**——机制类交付是审计优先级最高的（它们改变门禁行为，影响所有后续 PR）。
- 本批四组按「影响面」排：**门禁与度量**（改变判据）> **流水线与看门狗**（改变产物与可见性）> **防线接线**（改变执法点）。

## 二、审计对象（四组）

### A 组｜门禁与度量变更（最高优先；改的是"判据"，不是"产物"）
| 对象 | 位置 | 复核要点 |
|---|---|---|
| **派单文档复核门禁（PR #574）** | PR #574（`scripts/control-tower/check-dispatch-gate.sh` + `scripts/pre-commit-check.sh` 两处接线 + `tests/control-tower/check-dispatch-gate.test.sh`） | ① 23 断言全绿；② 反向：真任务号缺 task-state → 红；③ 规则引用词旁路（按/参见/参考/依据/规则/规约/决策）语义正确、豁免走 `ℹ️` 留痕；④ **接线两处**（主流程 + CT-34 纯文档早退分支）都真实生效（CI strict 下 exit 1）；⑤ 性能 ≤ 基线 +5s |
| **预算门禁口径修正（PR #589 派单 + 实现 PR 待开）** | 实现 PR（执行中，待合） | ① 先核「适用范围」结论与文档化（是否保留 D515 docs-only 跳过）；② **F2 fail-closed**：未列路径默认计入；③ **F3** `N_FILES=0` 语义与两类件数显性打印；④ 上限 12 未变；⑤ 反向：把 `src/` 移出白名单 → 上限用例必红；⑥ 对 PR #572 真跑（`--files` 与真 git 双模式） |
| **A1 度量口径修复**（若本批前落地） | `scripts/product-lines/calc-progress.py`（时间戳口径） | ① TTL 14 天与「证据须晚于代码」意图未变；② 同日先后的误判已消除（给 before/after 点级对照）；③ **不改判分口径的其他部分**（逐行 diff 可核）；④ 证据：线1 6 点在不同时间点的计数不再跳变 |

### B 组｜证据流水线与可见性（保鲜流水线 + 仪表盘通道抢救）
| 对象 | 位置 | 复核要点 |
|---|---|---|
| D774 证据保鲜流水线 | `scripts/product-lines/{rerun-evidence.sh,gen-expiry-warnings.py,run-machine-evidence.sh}` + `tests/control-tower/rerun-evidence.test.sh` | ① 一键重跑幂等；② 失败不中断全局且显式 fail（exit 1）；③ 证据 id 口径断裂（GS 用 S1-x / calc 期待 1-x）**如实保留**（不许改判分凑绿） |
| 仪表盘通道抢救（产物落 main + 通道 + 看门狗） | PR #590/#591/#592；`scripts/product-lines/check-progress-freshness.py`、`.github/workflows/progress-freshness-watchdog.yml`、`docs/synova/coordination/CI-诊断通道.md` PART B | ① 产物 `generated_at` 为当日、六态与重跑一致；② **F6 机理实证**：PAT 建的 PR 有 12 check-runs / bot 建的为 0；③ 看门狗三态（新鲜 0 / 过期 1 + 修复指引 / 降级）；④ 通道文档四节可复现 |

### C 组｜防线接线（D782 part1，已合 #593）
| 对象 | 复核要点 |
|---|---|
| D782 part1（`scripts/pre-commit-check.sh` + `.github/workflows/ci.yml` + `tests/doc-system/*` 入 canary） | ① W1/W2 断言 7/2 → **9/0**（给原始输出）；② doc-system 测试真被 CI 调用（grep 接线）；③ 未削弱既有组（13 组横幅/组数一致）；④ CI 时间增量可接受 |

### D 组｜控制塔三机制（PR #585 派单；实现 PR 若已落地）
| 对象 | 复核要点 |
|---|---|
| 三机制 A/B/C（PR #585） | 各自「正向 + 反向 + 接线」三类证据；A：声明 A 实改 B → 红；B：删非本单载体 → 红；C：骨架 brief 流转 impl_done → 拒 |

## 三、交付要求（不变）
1. 报告落 `docs/synova/audit-reports/2026-09-16-K3-*.md`，每条结论带 **file:line 或命令 + 原始输出**
2. **回填 `task-state/*.json` 的 `audit` 段**（看板与产品进度页唯一真相源）；FAIL 进 `AUDIT-FINDINGS-LEDGER.md`（含 M 模式归类）
3. **不许预设结论**：证据不足写 `NOT-AUDITABLE` + 缺什么；禁"基本通过"
4. 报告头固定写「**数字口径: <sha>（+环境）**」（批次四 P2 已固化的约定）
5. 只读被审产物 + 独立重跑；发现问题出报告 + 台账，**不改**被审代码（CTO 另派修复卡）
6. 每完成一项立刻回填 task-state（不攒最后）

## 四、节奏
- 第 1 天：A 组（门禁与度量，影响面最大）
- 第 2 天：B 组 → C 组 → D 组（按落地情况）
- 未落地项（预算门禁口径 / A1 / 三机制 实现未合）→ 先审已落地部分，其余等 PR 合后再审，**不要用"未落地"当跳过理由**

## 五、派单内部一致性自检
- 审计对象均存在于 task-state 或已有 PR/分支（① 项可核）；无与 `scripts/audit/**` 的写集交集（CTO 只写协调文档与 task-state）
- 与批次四未审项无重复：批次四（PR #572）为「33 个 impl 无 audit + 本批新交付」；本批为「本日新落地机制类」
- 无互斥表述；依赖无环（A 组不依赖 B/C/D）
- 数字口径统一：所有计数以 task-state / 产物文件实读为准，禁手算
