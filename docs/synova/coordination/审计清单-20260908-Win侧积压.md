# K3 审计清单 — Win 侧积压 impl_done 任务（2026-09-08）

> 产出: CTO | 基线: origin/main @ 5c94cd12
> 协议: AUDIT-PROTOCOL.md v1.1
> 范围: impl_done 且 audit 段为空的代码功能类任务（高优先 8 + D586/D587/D588 补审 3 = 11 任务）
> 排除: 纯治理/流程类（D530-D532/D552-D557/D562/D570/D571）——零 src/ 变更，不影响产品，低优先

---

## 高优先（代码功能类，8 任务）

### D556 — GA 校准前端接线 + 回路 layer2
| 项 | 内容 |
|---|---|
| 写集 | electron-renderer/src/components/ga-detail-sections.ts + src/agent/ + src/l3/ |
| 测试复跑 | `npx vitest run tests/ga-collab-ui.test.ts tests/ga-collab-logic.test.ts` |
| evidence | evidence/D556/（seed.md + walkthrough-api.log + walkthrough-evolution.log + renderer-screenshot.png）|
| 注意 | evidence/ 在根级（.gitignore 禁用）——evidence 文件**不在 git**，审计以代码+测试为依据 |

### D567 — 专家枚举硬编码残留 ×4 传播修复（K3 15-1）
| 项 | 内容 |
|---|---|
| 写集 | src/agent/expert.ts + src/tui-v2/chat.tsx + src/agent/cross-validator.ts + src/l3/engine-impl.ts + src/sentinel/runner.ts（10 文件） |
| 测试复跑 | `npx vitest run tests/expert/expert-enum-propagation.test.ts tests/store/manifest-consistency.test.ts` |
| evidence | task-state/D567.json impl.evidence（5 条详细声称含 grep 计数 + 回归对照） |
| 注意 | brief 路径勘误：cross-validator 实为 src/agent/（brief 误写 src/l3/） |

### D568 — enterprise-fact superseded_by 语义实现（K3 18-5）
| 项 | 内容 |
|---|---|
| 写集 | src/agent/enterprise-fact-store.ts + src/agent/agent-memory-store.ts + scripts/control-tower/enterprise-fact-store.ts |
| 测试复跑 | `npx vitest run tests/control-tower/enterprise-fact-chain.test.ts`（4 用例） |
| evidence | task-state/D568.json impl.evidence（4 条：red 基线 + red转绿 + 注释一致 + 回归对照） |
| 注意 | 实现语义 = createFact 更新时旧条目归档 + supersededBy 链 + listFacts 链头过滤 |

### D569 — 仪表盘 collector 改 git 权威读取
| 项 | 内容 |
|---|---|
| 写集 | scripts/control-tower/collect-dashboard.js + scripts/control-tower/gen-cto-health.py |
| 复跑 | `node scripts/control-tower/collect-dashboard.js` → states 数 + pct 数 |
| evidence | task-state/D569.json impl.evidence（3 条：语法 + 实测 + 接线 grep） |
| 注意 | CTO 审查修复追加了 gitLs 冷启动竞态修复（await fetch + gitLs await） |

### D575 — LLM 配置首启向导（DSH credential seam）
| 项 | 内容 |
|---|---|
| 写集 | src/services/llm-credential-store.ts + src/routes/llm-config.ts + src/config.ts + src/server.ts + electron-renderer/src/components/LlmSetupCard.tsx + WelcomeScreen.tsx + StatusBar.tsx + stores/llm-config.ts + app-store.ts |
| 测试复跑 | `npx vitest run tests/services/llm-credential-store.test.ts tests/routes/llm-config.test.ts tests/llm-config-frontend.test.ts`（64 用例） |
| evidence | docs/synova/audit-reports/D575-ticket-slice-evidence-20260906/ + evidence/D575/（根级，不在 git） |
| 注意 | 已有 K3 closeout PASS（2026-09-05）——本条为**合并后完整状态补充审计**；前审 P1×2 开放项存续 |

### D576 — 产品线兑换机制修复 CT-53 + CT-54
| 项 | 内容 |
|---|---|
| 写集 | scripts/product-lines/calc-progress.py + redeem-progress.py + alloc-task-id.sh + product-lines.yaml（k3_only）+ evidence/k3-D575-D577-closeout.json |
| 测试复跑 | `bash tests/control-tower/redeem-task-redeem.test.sh`（5/5）+ `bash tests/control-tower/alloc-task-id.test.sh`（13/13） |
| evidence | task-state/D576.json impl.evidence（4 条）+ calc 重跑 1%→5%（8 verified） |
| 注意 | calc-progress.py 后续被 D579/D582（CT-62）追改——审计以 main 最新版为准 |

### D577 — 哨兵阈值配置真实挂载
| 项 | 内容 |
|---|---|
| 写集 | src/sentinel/types.ts + sentinel-loader.ts + runner.ts + extensions/sentinels/*/aggregate.ts（14 个）+ tests/sentinel/ 新增 2 文件 |
| 测试复跑 | `npx vitest run tests/sentinel/threshold-injection.test.ts`（10/10）+ `D577_FLIP_TEST=1 npx vitest run tests/sentinel/threshold-manifest-flip.test.ts` + `npx vitest run tests/sentinel/`（214 绿） |
| evidence | docs/synova/audit-reports/D577-sentinel-threshold-wiring-evidence-20260905/（T1-T10 red→green + flip 物理 + 接线卫生 grep） |
| 注意 | 已有 K3 closeout PASS（2026-09-05）——合并后补充审计；7-2/8-1/10-3 维持点级 PASS |

### D578 — Win 真机实测 1-2
| 项 | 内容 |
|---|---|
| 写集 | 台账登记（1493539f）+ scripts/desktop/win-install-verify.ps1 修复 + build-synova.cjs beforePack 守卫 |
| 复核 | 台账 1493539f（坏包根因链 + CI 重打 + 新包真机验证）+ D581 beforePack 守卫测试 23 绿 |
| 注意 | **1-2 四断言 evidence 待 Win 复跑入 git——K3 标注此项状态，不兑换** |

---

## 补审 3 任务（D586/D587/D588——task-state 在 main 缺失）

### D586 — LLM 错误码 taxonomy（B-01）
| 项 | 内容 |
|---|---|
| 合并提交 | 56c4dd1d (#400) |
| 写集 | src/errors/types.ts + src/providers/base.ts + tests/errors/llm-error-taxonomy.test.ts |
| 测试复跑 | `npx vitest run tests/errors/llm-error-taxonomy.test.ts`（22 用例） |
| 注意 | **task-state/D586.json 不在 main**（D384 登记债）——审计以此文档 + git log 为准 |

### D587 — 工具结果修剪器（B-04）
| 项 | 内容 |
|---|---|
| 合并提交 | 75f13b56 (#408) |
| 写集 | src/agent/tool-loop-executor.ts + conversation-engine.ts + tests/l2/tool-result-pruner.test.ts |
| 测试复跑 | `npx vitest run tests/l2/tool-result-pruner.test.ts`（6 用例） |
| 注意 | **task-state/D587.json 不在 main** |

### D588 — 会话投影注册表（B-07）
| 项 | 内容 |
|---|---|
| 合并提交 | eebd9c99 (#405) |
| 写集 | src/store/session-projection.ts（410 行）+ tests/store/session-projection.test.ts（248 行） |
| 测试复跑 | `npx vitest run tests/store/session-projection.test.ts`（7 用例） |
| **CONDITIONAL** | src/ 内零生产引用（M3 第 5 次）——接线 D597 派单已入库，审计标注接线状态 |

---

## 排除清单（纯治理/流程类——低优先审计）

D530（gatekeeper merge-exempt）/ D531（AGENTS 对齐 V5.1.1）/ D532（自检脚本 launchd）/
D552（governance backfill）/ D553（impl-done disposal）/ D554（post-commit no-sweep CT43）/
D555（verify-parallel CI closed-task-exempt）/ D557（verify-parallel signal4）/
D562（K3 复审批派单）/ D570（AGENTS 版本对齐）/ D571（pre-push 逃生舱修复）

→ 这些全部是 scripts/control-tower/ 或文档变更，零 src/ 改动，不影响产品功能。可延后合并批审计。
