# 小队交付｜门禁加固三卡（D846 / D847 / D848）+ 连带 FIX（D849 / D853）— M6 收口

> 出件 2026-09-20 ｜ 队长 = 并行 CTO session（agent team）｜ 依据《[小队模式-固化件-v1](小队模式-固化件-v1-20260919.md)》M1–M6
> 派单件：[小队派单-门禁加固三卡-20260920.md](小队派单-门禁加固三卡-20260920.md)（PR #671）
> 状态：**四卡交付完毕并各有独立自验；合并链仍被一个新发现的系统性 Windows 缺陷阻塞（见 §五）**

---

## 〇、M1 本队信息（实际编制与工作树）

| 项 | 值 |
|---|---|
| 任务号 | D846 / D847 / D848（派单三卡）+ D849 / D853（创始人批准新开的 FIX 卡） |
| 队长 | 并行 CTO session（本队唯一队长） |
| 编码 A | `coding-a` — D846 → D847（同文件串行）→ 后接 D853 |
| 编码 B | `coding-b` — D849 → D848 |
| 自验 | `verifier-1` — 独立第三人，**不参与编码**；三轮独立复现 + 反例 |
| 审计 | K3，**队外，永不入队**（本队交付一律标『可提请独立审计』） |

| 切片 | 工作树 | 分支 | 基线 |
|---|---|---|---|
| 切片 A（D846/D847） | `.synova-wt-gates-a` | `gate/claim-release-a` | `aca42e30`（= PR #657 头，**未合**） |
| 切片 B（D849） | `.synova-wt-gates-b` | `gate/d839-win-b` | `aca42e30` |
| D848 | `.synova-wt-gates-b2` | `gate/ledger-fresh-b` | `1d8dcab6`（含 D811 结果） |
| D853 | `.synova-wt-gates-win` | `gate/d839-win-chain` | `5b7d0326` |
| 自验 | `.synova-wt-gates-v` | `verify/gates-v` | `aca42e30` |
| 队长收口 | `.synova-wt-gates-lead` | `gate/lead-closeout` | `dae40d96` |
| 队长绿车 | `.synova-wt-657green` | `tmp/d839-657-green` | `origin/fix/d839-claim-release` |

> **未碰主工作区**：编码 B 的 D848 与在飞的 D811 原本实体撞车（同一对文件），创始人裁决"串行"后 D848 才开枝，全程零冲突。

---

## 一、交付清单（逐卡：commit / 证据 / 自验）

| 卡 | 内容 | 分支 / commit | 关键物理证据 |
|---|---|---|---|
| **D846** | 释放判定只认**已提交**内容 + release 凭证/非 owner 理由 + Windows 兼容 | `gate/claim-release-a` `13a12923` | 先红 `PASS=47 FAIL=18` → 后绿 `PASS=65 FAIL=0`（端到端 guard：基线 `warn/exit 0` → 修复后 `block/exit 1`） |
| **D847** | 台账 RMW 进 `write_lock` 临界区 + 事实源不变量（`GIT_*` 剔除） | 同上 `2084910f` | 先红 `PASS=53 FAIL=34`（丢写+崩溃同时命中）/ `PASS=79 FAIL=8`（`GIT_DIR` 放行复现）→ 后绿 `87/0` + `write_lock.test.sh 20/0`；独立压测 N=16×4 |
| **D848** | 账本漂移门禁：`--check` 三态 + CI 旁路 job | `gate/ledger-fresh-b` `adf815c1` | 真实仓库复现 K3 D815 数字（`lines/9/v1_passed 1→6`、`totals/v1_passed 25→27`，`exit=1`）；夹具 `PASS=108 FAIL=0`；主路径零回归（输出逐键相等）；`grep` 零新增主路径检查 |
| **D849** | 夹具 Windows 兼容（子链 PATH 自包含 + 双命名空间沙箱 + 假绿围栏闭合） | `gate/d839-win-b` `f812d883` + `88ca13b6` | macOS `staging_guard PASS=33 FAIL=0`；根因：`resolve-commit-brief.sh:31` 的 `git rev-parse --show-toplevel` 在 Windows 落 pwd 回退 → MSYS 专有路径 → resolver 内嵌 native python 读不到沙箱 |
| **D853** | Windows 门禁 fail-open 根治（PXBIN 试运行口径 + stderr UTF-8 + cygpath 别名 + 假 git 拒绝） | `gate/d839-win-chain` `84afd4cc` | 因果实证四组（坏 shim → 基线 `rc=1 out=[]` / 修复选中可用回退；`PYTHONIOENCODING=cp1252` → 基线中文被 backslashreplace 吃 / 修复完整；cygpath 别名；PATH 假 git → 基线采信 `FAKE-TRACKED-FILE.md` / 修复 `rc=128` 用可信 git） |

---

## 二、M6 三件

### 2.1 diff
逐卡 diff（文件 + 行号 + 为什么）见各卡交付回报原文；汇总：
- `scripts/control-tower/claim_release.py`（406 → 726 行）：`_read_committed_json()` / `task_status()` 改读 HEAD / `ledger_record_verdict()` 凭证校验 / `save_ledger_locked()` / `_git_env()` / `git_bin()` / `release_task()` 鉴权
- `scripts/control-tower/write_lock.py`：`acquire()` 改 `O_CREAT|O_EXCL` 原子获取 / `_is_expired()` mtime 回退 / `release()` 只读目录回 D209 降级契约
- `tests/control-tower/{claim_release,write_lock,staging_guard}.test.sh`
- `scripts/project/gen-project-board.py` + `tests/project/gen-project-board.test.sh` + `.github/workflows/ci.yml`（末尾追加独立 job，19 insertions / 0 deletions）
- `scripts/workflow/resolve-commit-brief.sh` + `tests/control-tower/{staging_guard,claim_release}.test.sh`（D853）

### 2.2 自验结论（独立第三人，三轮，不采信编码自述）
**自验累计发现 8 个问题**（口径：『自验通过』/『自验发现 N 个问题』，不含审计结论）：

| # | 轮 | 问题 | 状态 |
|---|---|---|---|
| 1 | 1 | D846 工作树伪造解锁（K3 P1-1） | ✅ 第二轮验证**已修好**（工作树/暂存/符号链接/rm/非法值全 block；已提交 `impl_done` 正确放行且 `degraded=false`） |
| 2 | 1 | D847 台账并发丢写 + **共享 `.tmp` 崩溃**（K3 §⑥ 未记载：40 轮×N=8 崩溃 20/40） | 崩溃面 ✅ 已消除；丢写面 ❌ 见 #6 |
| 3 | 2 | **`degraded` 在门禁层断裂**：`claim_release status` 报 `degraded=true`，`staging_guard` 顶层 `degraded=false`（`staging_guard.py:189-194` 已释放分支只 append 不传播） | ❌ 未修（`staging_guard.py` 不在任何写集）→ 另立卡 |
| 4 | 2 | **`GIT_DIR` 继承环境 → 无痕解锁**（`claim_release.py:131`） | ✅ D853 已闭合（另测 13 个 `GIT_*`/`HOME`/`XDG` 全 block） |
| 5 | 3 | **`GIT_WORK_TREE` 经 `resolve-commit-brief.sh:31` 未清洗环境让认领判定失明**（block/1 → warn/0，零痕迹） | ✅ D853 已修（`_git_clean`） |
| 6 | 3 | **D847「无丢写」判据不成立**：N=16×40 → 丢写 3 轮；N=8×80 → 1 轮。机理：`write_lock.py:94-103` 回收路径 **judge-then-unlink 非原子**（`<stat:FileNotFoundError>` → `return True` → `unlink(missing_ok=True)` 删掉他人**活锁** → 双临界区） | ❌ 未修 → 另立卡（最小修法方向：按 inode/owner 校验后再删，或 rename 再判） |
| 7 | 3 | **`SYNO_CLAIM_LOCK_WAIT_SEC` 毒值 = 真绕过面**：`=abc`/空串 → `claim_release.py:106` ValueError → `staging_guard.py:52` import 崩 → `synova-commit:607-618` 走「执行异常 — 降级放行」→ 认领 block 整体失效；`1e999` 争用时无界挂起 | ❌ 未修 → 另立卡 |
| 8 | 3 | **`PATH` 可伪造 `git`**（`_git_env()` 只剔 `GIT_*`） | ✅ D853 已修（`git_bin()` 绝对路径 + `--version` 校验 + 拒绝仓库内 git） |

**自验通过项**：夹具 87/0 与 20/0、先红 79/8 与 53/34（与编码自述一致，但结论来自自验自己跑）；JSON 0 损坏；超时 `exit 2` 不落半截；锁降级三处可见；陈旧锁 0.13s 回收；`kill -9` 持有者后 fail-closed 有界（2.17s）且 age 后回收成功（无永久死锁）。
**未覆盖面 12 条**已逐条写入自验报告（Windows/Git Bash 未验、真实 `synova-commit` 端到端未跑、跨主机/NFS 未验、压测为单次样本未给置信区间等）。

### 2.3 遗留清单（逐卡，均已如实登记，未美化）
- **D846**：① 本地进程仍可直写**带齐凭证**的假台账记录 ② `.codex/control-tower/session-registry.json` 的 `archived` 证据源 **gitignore，永不进 git 历史** → 无法核
- **D847**：③ 见自验 #6（回收竞态）④ `write_lock.py:125-138` 引入**不可达死代码**（AST 判定）⑤ `SYNO_CLAIM_LOCK_WAIT_SEC` 注入缝是否该保留（K3 可判）⑥ 台账演进到 append-only JSONL / `_is_expired` 的 mtime 粒度（HFS+ 1s）
- **D848**：⑦ 真实仓库冒烟的**冻结数字**本质是"冻结移动目标"（根治应为不变量断言 `v1_passed == Σ lines[].v1_passed`）⑧ `tests/project/*.test.sh` 整体未接入 CI ⑨ 新增证据目录需同步 `EVIDENCE_TRIGGER_*`
- **D849**：⑩ D839 夹具的 Windows 红**未真正修好**（见 §五）
- **D853**：⑪ 假 git 若同时满足"合法 `git version` + 非临时目录 + 不在被判定仓库内"仍可能被采信（无外部信任锚）

---

## 三、归属逐张收口（owner 不自动释放）

| 卡 / 文件 | 收口动作 | 凭证 |
|---|---|---|
| **D839**（`claim_release.py` 等） | `claim_release release --task D839` | `git show HEAD:task-state/claim-releases.json` → `{"by":"D846","at":"2026-09-20T20:38:17+08:00","basis_at_release":"task-state:impl_done","owner_at_release":"mac-coding","reason":"D839 已 CONDITIONAL PASS，D846 承接其文件","released":true}` |
| **D846** 自身 | `session_registry.py archive --session-id D846` | 见 D847 交付回报（编码 A 实测，认领门禁随后对 7 个暂存文件全 `warn` 并逐条打印释放理由） |
| **D853** 承接 D839 文件 | `claim_release release --task D839 --by D853` | `task-state/claim-releases.json`（`13f1bf7f`），`basis_at_release=task-state:impl_done` |

---

## 四、本轮发现登记（CTO 职责，发现即登记）

编号仅用于本文件内引用；**正式卡号须走 `alloc-task-id.sh` 现场分配**（见 §五 的撞号教训）。

| # | 发现 | 级别 | 归属 |
|---|---|---|---|
| F1 | 派单件写「D839 的 CI 补丁已随 #657 合并完」——**经 API 核实为假**（#657 `state=open, merged=false`，且它根本没碰 `ci.yml`） | 派单失实（M2） | 队长 |
| F2 | 派单件写集与在飞 D811 实体撞车（`gen-project-board.py` + 其测试） | 撞车 | 队长（创始人已裁） |
| F3 | 派单件未预见：D846/D847 基线文件**不在 main**（只存在于未合 PR #657） | 派单失实（M2） | 队长 |
| F4 | PR #657 必需检查 `Control Tower Gate Tests (windows-latest)` **红**（main 全绿）→ 卡死切片 A | 阻塞 | 队长（创始人已裁） |
| F5 | `alloc-task-id.sh` **只读本地 + origin/main**，看不到未合分支上的登记 → 必然重发号（实测本地 max=D843，而 D846/D847/D848 已登记在未合 PR 上） | 门禁缺陷 | 后续卡 |
| F6 | **撞号实证**：本队 D850 与并行 CTO session 分配的 D850/D851/D852 撞车 → 队长人工重核全部 ref 改发 D853 | 门禁缺陷（D382/D384 同型） | 后续卡 |
| F7 | **同一文件被两条门禁给出互斥归属**：声明 `--task-id D849` → D328 报「归属 D839」；声明 `--task-id D839` → D311 报「属于 session D849」。队长无法在该枝提交夹具修复 | 门禁缺陷 | 后续卡 |
| F8 | **栈式 PR 拿不到 CI**：触发器 `push: [main, feat/*]` + `pull_request: [main]` → 以未合分支为 base 的 PR 不跑 CI；叠加在未合 PR 上的分支只能靠改写 PR 头或探针枝取证 | 门禁缺陷 | 后续卡 |
| F9 | **PR 预算上限 12 与"给未合 PR 修必需检查"冲突**：把 FIX 的治理件（卡/brief/note）留在该枝 → 15 文件 → 必需检查红。本队两次（D849、D853）都靠"移出治理件"化解 | 机制冲突 | 后续卡 |
| F10 | **Windows 上认领制门禁整体静默 fail-open**（最深发现）：子链起的是 **WSL `bash.exe` 桩**（`staging_guard.py:153-154` 裸 `["bash", ...]`）→ `OUT=Windows Subsystem for Linux has no installed distributions` → resolver 零输出 → 链断；而 `staging_guard.py:172-173` 在 `claimed` 为空时**跳过认领判定** → 链断被翻译成放行 | P1（fail-open，M1+M5） | D853 |
| F11 | **CI 环境下"该拦的没拦"（两平台共有）**：`synova-commit.test.sh` 断言 ② 在 ubuntu 与 windows **都** `exit=0`（应为非 0），本地 macOS 三路全绿 → CI 专属输入导致放行 | P1（fail-open） | D853 round-3 |
| F12 | **CI 失败断言不可见**：canary job 只把夹具输出 `tail -8` 塞进注解，失败断言被其后的通过行挤出 → 执行方只能靠"把诊断压进末尾 8 行"才能拿到断言名（本轮实证：不加 DIAG 时连续两轮只知 `pass=16 fail=1`，加了才拿到 ②） | 诊断通道缺陷 | 后续卡 |

---

## 五、合并链状态与阻塞

```
#657(fix/d839-claim-release @ c16a545f)
  ├─ ✅ 预算红已修（移出治理件 + 对齐 main 已有件，diff 15 → 12）
  ├─ ✅ TypeScript + Lint + Iron Laws / Vitest ×2 / Golden Case F1 / Architecture / Integration / Test-Kit ×2 全绿
  ├─ ❌ Control Tower Gate Tests (ubuntu-latest)
  │      └─ tests/control-tower/synova-commit.test.sh pass=16 fail=1
  │         CI 注解（D853 round-2 加的 DIAG 定位，此前该断言被 tail-8 挤出不可见）：
  │           DIAG-FAIL ② 应拦, 实际 exit=0
  │           DIAG-ENV bash=/usr/bin/bash py=/usr/bin/python3 git=/usr/bin/git rc2=2 rc=0
  │         ⇒ **该拦的没拦 = fail-open**；工具都在，不是"找不到工具"那类
  └─ ❌ Control Tower Gate Tests (windows-latest)
         ├─ 注解（round-2，D853 生效后）：
         │    DIAG1 A=ec0/warn B=ec1/block D=ec1/block C=ec0/warn G=ec1/block H=ec0/warn
         │    PASS=34 FAIL=4      ← round-1 是 A/B/C/D/G 全 `ec0/pass`、PASS=21 FAIL=12
         │    DIAG2 PROBE_RC=1 PY=NONE:NONE OUT=Windows Subsystem for Linux has no insta…
         ├─ ✅ B/D/G 已从"该拦却放行"翻成 `ec1/block`：`_find_bash` 试运行校验 + fail-closed 生效
         └─ ❌ 余 4 条：A/C/H = `ec0/warn` —— 夹具 PROBE/准备阶段仍走坏链（与 `staging_guard._find_bash` 不同路径）
```
**关键收敛（比"两个平台各修一遍"更值钱）**：ubuntu 与 windows 红的**是同一个断言 ②**（两平台 `DIAG-FAIL ② 应拦, 实际 exit=0`），而本地 macOS 三路（CI 环境变量组 / detached HEAD / ci.yml 全 43 项同 shell 串跑）全 17/0 ⇒ **不是 Windows 专属，是「CI 环境」专属**。怀疑面因此收窄为：`SYNO_CI=1` 下的分支选择、actions/checkout 注入的 `GIT_*`、detached HEAD/merge ref、`origin/main` 可达形态，以及 `rc2=2 → rc=0` 这一步在哪条路径上把"拦"变成了"放"。

**结论（截至本文件出件）**：五卡代码与证据均已交付且各有独立自验。CI 逐轮收敛（取证走 `feat/gates-657-verify`——实测推 #657 枝**不触发 CI**，同为待立卡发现）：

| 轮 | sha | ubuntu | windows | windows 夹具 |
|---|---|---|---|---|
| round-1 | `32cc6a02` | ❌ `synova-commit.test.sh` ② | ❌ | `A/B/C/D/G` 全 `ec0/pass`（**整体 fail-open**）`PASS=21 FAIL=12` |
| round-2 | `c16a545f` | ❌ ② | ❌ | `B/D/G` 已翻 `ec1/block`；余 `A/C/H=warn` `PASS=34 FAIL=4` |
| round-4 | `60432e70` | ✅ **success** | ❌ | 链已通（`PROBE_RC=0 PY=python3:0`），余 2 条 `PASS=36 FAIL=2` |
| round-5 | `89ed3c5d` | ✅ **success** | ❌ | 同 `PASS=36 FAIL=2`（余 2 条为夹具期望面，非生产 fail-open） |

- **ubuntu 断言 ② 的 fail-open 根因已定位并修好**：`synova-commit:601-611`「guard `rc≠0` 且 `status≠block` → 降级放行」+「python 全不可用 → 跳过」两条放行语义 → 改为 fail-closed 阻断 + 点名（CTO 授权折入 D853，同族 + 同文件 + 同卡目标）。
- **Windows 认领链 fail-open 的病因已根治**：子链起的是 WSL `bash.exe` 桩 → `_find_bash()` 试运行校验 + 自包含 env；链断 → `SYNO-RESOLVER-DEGRADED` → `_fail_closed()` 阻断 + 点名。夹具 12 红 → 2 红。
- **仍未达成**：`Control Tower Gate Tests (windows-latest)` 余 2 条（场景 A/C/H 期望面的夹具断言）。⇒ **#657 未转绿 ⇒ 切片 A 仍无法以 base=main 开 PR ⇒ D846/D847 尚未落 main**；D848 待 #657 清空后走自己的 PR（其 CI 已由 `feat/*` 探针枝实测 success）。

**并行风险已登记**：本队作业期间另有**并行 CTO session** 在同仓作业（同推 #657 枝、分配 D850–D854、推 `feat/d854-ct70-project-tests-ci`）。**main 在期后转红**（`25082884`：Vitest 2/2、聚合→计算→页面、generate 失败），与本队改动无关但属更高优先级 —— 见 §四 F13。

---

## 六、给创始人的可直接转发指令

### 指令 1｜给编码 A（继续 D853，收口两处必需检查）
```
任务：D853 续 —— 让 PR #657 的 ubuntu + windows 两条「Control Tower Gate Tests」都转绿。
工作树：/Users/wane/SynovaAgent/.synova-wt-gates-win ｜ 分支 gate/d839-win-chain
先读：docs/synova/coordination/小队交付-门禁加固三卡-20260920.md §五（两处阻塞的 file:line 根因）
做：
① ubuntu：tests/control-tower/synova-commit.test.sh 在 CI 下 pass=16 fail=1（本地 17/0）→ 定位到具体断言、修根因、不放宽断言；
   定位手段：按 ci.yml 清单同 shell 串跑复现；或把"失败断言名+关键变量"打进末尾 8 行（CI 注解只带 tail-8）。
② windows：真病因 = scripts/control-tower/staging_guard.py:153-154 裸 ["bash", ...] 在 Windows 命中 WSL 桩
   → bash 选择自包含（SYNO_BASH → Git Bash 安装位 → command -v + 试运行校验 → 全败显式失败，禁静默）；
   + :172-173「claimed 为空即跳过认领判定」改 fail-closed（拿不到认领列表 ≠ 没有认领）。
验收：CI 两个平台的 Control Tower Gate Tests 都绿；macOS 本地夹具不得变红。
红线：不改判定语义只为变绿；不碰 scripts/audit/**；禁 --no-verify / git stash / force push。
回报：sha（我用 token 读 CI 注解回传给你，你的 IP 限流）+ diff(file:line) + 原始输出 + 自检 5 问 + 遗留清单 +『可提请独立审计』。
```
### 指令 2｜给 K3（队外独立审计，本队**不**自行审计）
```
审计对象（当前可提请）：
  D846/D847：gate/claim-release-a @ 2084910f（+13a12923 的 D846）
  D848     ：gate/ledger-fresh-b @ adf815c1
  D853     ：gate/d839-win-chain @ 84afd4cc
范围：门禁变更属审计面 → 按 K3-AUDIT-STANDARD-v2 独立复核
必带材料：docs/synova/product-lines/evidence/D846-D847-自验-20260920.md（自验三轮 972 行，含 8 项发现与 12 条未覆盖面）
队内立场：自验只给「自验通过 / 自验发现 N 个问题」，不含审计结论；审计结论只认 K3。
```

---

## 七、经验教训（供下一队复用）

1. **派单件的技术声称必须逐条复现**：本轮 3 条失实（F1/F2/F3）全部由"派单前未核物理存在"造成；`pre-dispatch-check` 应把"依赖 PR 的 merged 状态"与"未合分支上的写集撞车"列为强制项。
2. **交叉文件必须提前划界**：D846 的语义收紧使 `staging_guard.test.sh` 场景 A 在 macOS 也变红 —— 编码 A 主动识别并交出，避免了双写。
3. **独立自验的价值是"不采信"**：自验三轮共 8 项发现，其中 4 项（#3/#6/#7 + 第一轮的共享 tmp 崩溃）**K3 报告与编码自述里都没有**；D847 的头部判据被自验证伪。
4. **栈式开发遇上"必需检查 + PR 预算"会卡死**：本轮两次靠移出治理件化解，属治标；建议 CTO 域单独立卡（F8/F9）。

---

## 八、PR 拆分收口（2026-09-21 裁决 B，#657 拆两台）

> 裁决全文：`docs/synova/coordination/裁决-657拆两台PR-B口径-20260921.md`。本节为 **PR-2（D849/D853 Windows 收口）** 的 M6 三件补遗，随 `fix/d853-pr2` 入仓。

### 8-1 最终 diff（7 文件，≤12 达标）
| # | 文件 | 变更 |
|---|------|------|
| 1 | `scripts/control-tower/synova-commit` | +55：guard block 文案打印可执行释放命令（D839 语义）+ python 不可用/guard 异常调用点 fail-closed（D853-5，第四轮 CTO 授权边界内） |
| 2 | `tests/control-tower/synova-commit.test.sh` | +78：⑨⑩ fail-closed 断言 + 失败诊断进末尾 8 行 |
| 3 | `.claude/task-briefs/2026-09-20-D853-Windows认领制fail-open根治.md` | Q2 改写：synova-commit 本体随本台交付（原「不做」条目按裁决调整） |
| 4 | `task-state/D853.json` | impl 段回填 PR-2 口径（branch=fix/d853-pr2，写集=本台 7 文件） |
| 5 | `memory/notes/implemented/2026-09-20-D853-windows-claim-gate-failopen.md` | 决策落地 Note |
| 6 | `memory/notes/proposed/2026-09-20-team-gates-closeout.md` | 铁律 49 决策沉淀 |
| 7 | `docs/synova/coordination/小队交付-门禁加固三卡-20260920.md` | 本 M6 载体 |

根因 A/B/C/D 对 resolver/claim_release/staging_guard 的修复随 **PR-1** 文件版本（fix/d839-pr1，源自 96e40079 最终态）合入；两台互不含对方文件。

### 8-2 自验结论（判别性先红后绿，两次原始输出；基线=PR-1 合入态）
红（PR-1 + 新夹具 + main 版旧 synova-commit）：
```
pass=18 fail=4
DIAG-FAIL ④ 异常路径未显式/fail-closed ; ⑨ guard 异常未阻断 rc=0 ; ⑩ python 不可用未阻断 rc=0 ; ⑩ 原因未点名 ;
```
绿（PR-1 + 本台 synova-commit）：
```
pass=22 fail=0
```
回归：claim_release PASS=33 FAIL=0；staging_guard PASS=43 FAIL=0（组合态不回退）。
ls-remote 回执见 PR 描述（推送后核对 SHA 一致方算交付）。

### 8-3 遗留清单
1. 合并顺序：**先 PR-1 后本台**；合 PR-1 后本台须 merge main（`.claude/bypass.log` 冲突按并集解）。
2. CI windows-latest 终态确认（ubuntu 已绿 / windows 收敛 12→2 红的剩余 2 红）须在本台 CI 运行后核销。
3. ci.yml 单写者：PR-1 合并释放后才轮到 #682 与 D863（K3 口径）。
4. 治本（brief 互斥规则级解）→ D860（K3 复审）。
