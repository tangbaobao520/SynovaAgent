# 小队派单｜门禁加固三卡（D846 / D847 / D848）— 来源：K3 独立审计

> 出件 2026-09-20 CTO ｜ 执行形态：**agent 小队（agent team）——不是散单、不是单人**　｜ 依据《[小队模式-固化件-v1](小队模式-固化件-v1-20260919.md)》M1–M6

## 〇、本队信息（M1 必填）

| 项 | 值 |
|---|---|
| 任务号 | D846 / D847 / D848（一队三卡） |
| 队长 | **并行 CTO session**（同一时间只带一支队） |
| 编制 | **编码 A 1 人**（D846→D847 同文件串行）＋ **编码 B 1 人**（D848）＋ **自验 1 人（不得由编码兼任）** |
| 审计 | **K3，队外**（门禁变更属审计面，复审通过前不得合并） |
| 专用工作树（M1） | A：`.synova-wt-gates-a`　分支 `gate/claim-release-a`；B：`.synova-wt-gates-b`　分支 `gate/ledger-fresh-b`（**不得复用任何在飞 worktree**） |
| 为什么是队而不是散单 | ① 两切片文件不相交 → 真并行；② **自验必须独立于编码**（M3）→ 需要第 3 人；③ 队长做 M6 收尾三件 + **归属逐张收口**（owner 不自动释放）；④ 三卡同源 K3、同属审计面 → 一份证据链一次 K3 复审 |
> 决策依据：《[DSH 决策镜头](../skills/dsh-decision-lens/SKILL.md)》（先量不先加机制；超限即退役；引用必给 file:line）
> 验收：v1.1 五态 + 三问 + 四条禁止；K3 复审（门禁变更属审计面）

---

## 〇、为什么派这三张（创始人必读）

| 卡 | **是什么** | **为什么做**（K3 实证） | **做完推动什么** |
|---|---|---|---|
| **D846** | 释放判定**防伪**：现在 `claim_release.py` 读 **工作树** 的 task-state 判断"任务是否完成" | K3 D841 P1-1：**任何人改自己工作树里他人的卡**（哪怕不提交）把 status 改成 `impl_done`，就能无痕解锁他人的文件认领（沙箱实测 block→warn）；release 子命令无鉴权、无留痕 | 认领制从"君子协定"变成**可防伪**：解锁必须基于**已提交**事实，且留可审计凭证 → 放行不再等于放弃保护 |
| **D847** | 台账**并发安全**：`task-state/claim-releases.json` 并发写丢更新 | K3 D841 P1-3：30 轮并发第 **0 轮**即中；同目录已有 `write_lock.py` 却没用 | 批量释放（本仓曾一次清 16 份）在并发下**不再丢记录** → 释放台账可信 |
| **D848** | 账本**刷新门禁**：证据/标准变更合入后必须重算派生 ledger | K3 D815 P1：main 的 ledger 停留在线10=**1/6**，而 D803 证据 09-17 已合入；**派单背景数字靠人转述**。CTO 独立复算：重算后线10=**6/6**、全局 25→27 | 你的看板**永远自证**：出现"证据已合、账本没动"时机器报警；派单不再引用过期数字（本轮差点以 1/6 的背景派线10 的活） |

**红线**：三卡均**不碰** `scripts/audit/**`、不写审计标准、禁 `--no-verify`/`git stash`/force push。

## 一、切片与写集互斥（M2 逐文件）

| 切片 | 卡 | 独占文件 | 并行判定 |
|---|---|---|---|
| **A** | **D846 + D847** | `scripts/control-tower/claim_release.py`、`tests/control-tower/claim_release.test.sh`（**同文件 → 同一人串行做，先 D846 后 D847**） | 与 B 不相交 |
| **B** | **D848** | `scripts/project/gen-project-board.py`、`tests/project/gen-project-board.test.sh`、`.github/workflows/ci.yml`（**只追加调用，不重排既有清单**） | 与 A 不相交 |

**写集冲突清单**：A 与 B 无交集；A 内部 D846/D847 同文件必须串行；`.github/workflows/ci.yml` 仅 B 可改（D839 的 CI 补丁已随 #657 合并完）。

## 二、纪律（照做）

- 每卡先写规格（输入/输出/降级 + 验收命令），再写码；**先红后绿夹具必须给两次原始输出**。
- 队内只说"自验"；交付标注 `可提请独立审计`；K3 复审通过前不得合并。
- 证据按任务号命名落 `docs/synova/product-lines/evidence/`；PR ≤12 文件 + 单域。
- `synova-commit` 自带 push；开工第一步**重新核行号**（脚本会漂移）。

## 三、完成判据（逐卡）

- **D846**：① 工作树改他人卡 status=impl_done → **仍 block**（先红：现在会放行）② 已提交的 impl_done → 正确放行 ③ release 留痕（谁/依据/时间）
- **D847**：并发夹具 N 进程同时释放 → 断言**全部记录在案**（先红：现在丢）
- **D848**：① 模拟"证据合入但账本没重算" → **报红** ② 无证据变更的普通 PR → **不红**（防误伤）③ 重算一步可执行 + CI 接线证明
