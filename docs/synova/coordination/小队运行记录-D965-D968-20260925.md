# D965–D968 哨兵体系整改 · 小队运行记录（收尾三件）

> 队长：`synova-squad-lead`（DeepSeek Harness）｜**批基线**：`origin/main` = `ce231ff1b1e419095117413badba8c6b55ae39fd`
> 本件是 **M6 收尾三件**（改动 / 自验结论 / 遗留清单）的载体，与各卡 evidence 件互为索引。
> **本件不含"审计通过"**：通过性判定归 **CTO 收件闸 + K3 终审**。

---

## 〇、批览

| 项 | 值 |
|---|---|
| 批次 | 哨兵体系整改（Mac 段 **D965–D968**；依据 `docs/synova/coordination/派单-哨兵体系整改-20260925.md`） |
| 规格 | `docs/synova/architecture/SYNOVA-哨兵体系-最终版-v1-20260925.md`（十环判据 / §4.1 measurements / 权限矩阵） |
| 成员 | 队长 `synova-squad-lead` ｜编码 `sentinel-coder-a` / `sentinel-coder-b` ｜独立复核 `sentinel-verifier`（**≤4 人，符合上限**） |
| 共享任务 | `task-1`(D965) `task-2`(D966) `task-3`(D967) `task-5`(D968) `task-4`(V-复核) `task-6`(D975 规则) |
| 派生卡 | **D975**（补 `ownership.yaml` 哨兵域规则，CTO 批准立卡） |

---

## 一、前提冻结（队长亲测，**不得采信派单件**）

探针：`evidence/D965-D968-队长前提冻结/probe.mts`（自写）＋ `stub-check.mts`；真 `SqliteGraphStore` + `data/synova.db` 副本 + 插桩 `queryNodes/queryEdges/getNode/traverse`。

| # | 派单件前提 | 实测 | 判定 |
|---|---|---|---|
| P1 | 哨兵总数 = 45 | 45（47 目录 − `shared` − `_extinct`） | ✅ |
| P2 | 返回数组 = 42 | 42 | ✅ |
| P3 | 抛错 = 3；`path-dependency` 缺 `aggregate.ts` | 抛错 = **0**；`manifest.entryPoint=./computes/detect.ts` 存在且返回数组（loader 尊重 entryPoint，`sentinel-loader.ts:193`） | ❌ **不成立** |
| P4 | 零 finding = 23 | 数组口径 29 ／ **loader 解包口径 30**（判定列） | ❌ **不成立** |
| P5 | 零调用 = 2 | = 2（但定性错，见 P6） | ✅ |
| P6 | 坏件 = 3 | 真坏件 = **2**（硬编码桩）；`customer-demand-shift` 为**设计内降级**（`sentinel-loader.ts:288-290` 明确支持） | ❌ **不成立** |
| P7 | （未列） | **`graph_triples` 整表结构漂移**（缺 `props` 且 `id` 为 `INTEGER PRIMARY KEY AUTOINCREMENT`）⇒ 边读写全失效 | 🆕 **P0** |
| P8 | （未列） | 两个"坏件"是**硬编码桩**：收错第 1 参、从不碰 store、`input` 为字面量 ⇒ loader 解包后**每次运行产出 4 条编造 finding**（`evidence: []`） | 🆕 **比"非数组"严重** |
| P10 | adapters = "5 个 @deprecated 哨兵" | 实测 **3 个** `@deprecated` + `cash-flow-sentinel.ts`（真哨兵）+ `helpers.ts`（工具库） | ❌ **不成立** |
| P12 | 派单/规格/号段/task-state 已落 main | **全部不在** ⇒ 违反小队禁令 5 | ❌ **不成立** |

**自我纠错（同一把尺子对队长也适用）**：首轮探针误记 `customer-demand-shift` 为第 3 个坏件 → 逐字读码后**撤回**。

---

## 二、逐卡交付（本地 == 远端，逐支回执）

### 治理件落地（解禁令 5）
| PR | 域 | 内容 | 结果 |
|---|---|---|---|
| #788 | 架构 | 规格件 244 行 | 已合 main（12/12 CI 绿） |
| #789 | coordination+卡件 | 派单件 122 行 / 号段水位 14 行 / D965–D968 卡 | 已合 main（含 D708 写集对账豁免，见下） |
| #790 | evidence | 复核员阶段一证据 20 文件（sha256 15/15 IDENTICAL） | 已合 main |

> **D708 首轮红与修复（改坏即红）**：gate 从**分支名首个 `D#`** 取号 → 读 `task-state/D965.json` 的 `write_set` 当声明 ⇒ 治理落地件被判"夹带 5 件"。按 gate 设计路径在 PR 正文加 `## 写集豁免`：
> ```
> 无豁免: ❌ block — 检测到 5 个写集外文件（夹带）   [exit=1]
> 有豁免: ✅ pass  — 提交文件集 ⊆ 声明写集（无夹带）   [exit=0]
> ```

### D965 — 裁撤 2 个硬编码桩哨兵
| 项 | 值 |
|---|---|
| 分支 / HEAD | `fix/D965-sentinel-stub-cut` @ `3a50b8da` |
| 变更 | 12 文件（4 个 `R100` 纯改名 → `_extinct/`，12→14）+ 连带 5 处修绿 |
| 关键结论 | 探针 **43/43 threw=0**；空库 finding **18→14**，消失集**恰为那 4 条编造**、反向差集 ∅（**零误伤**）；`path-dependency` **目录树 sha 逐字节相同** |
| 判别性 | 同一夹具 GREEN(exit 0) ⇄ 还原裁撤 RED(exit 1)，**金丝雀两态 PASS** |
| 复核 | **7/7 成立**（含动态取数双向：+1→44 绿／写死 43→红／−1→42 绿） |
| 未合原因 | D734 ② 跨域待 **D975** 规则合 main；链路 `D970 → D975 → D965 二次刷新` |

### D966 — 独立复现「空转/零调用」+ 三类清单
| 项 | 值 |
|---|---|
| 分支 / HEAD | `docs/D966-sentinel-idle-repro` @ `62e018f8` |
| 关键结论 | **三类清单**：缺件 0（loader 口径）／空转不读数据 **2**（硬编码桩）／真算 **43** |
| 双列口径 | 列A（`Array.isArray`）= 29 ／ **列B（loader 解包，判定列）= 30**；差值 1 = `customer-demand-shift` |
| **「20」差异归因** | **解答派单件「首要任务」**：`queryNodes` **方法级调用数 = 0** 口径下 = **20**（A–D 断面 20／E 断面 11／43 件断面 18，**两种独立测量名单一致**）；≠ 空转（20 中 18 件经 `traverse` 仍触达图）。CTO 23 / 院方 20 / 本卡 2 / 静态 3·3·2·2 **四者对四个不同问题** |
| 复核 | **9/9 成立**（含 P7 三态、方法层假绿证实、方向不单调） |

### D967 — measurements 时序层（**栈式两支**）
| | PR-A（win） | PR-B（mac，base=PR-A） |
|---|---|---|
| 分支 / HEAD | `feat/D967a-measurements-store` @ `2017a9cf` | `feat/D967b-sentinel-alert-stats` @ `80045321` |
| 内容 | measurements 8 列 + 时序索引 + append-only API；**P7 整表重建**；**迁移版本解析抗污染** | `sentinel_baselines → sentinel_alert_stats`（原地 `RENAME`）+ runner 接线 + PDP 去伪条目 |
| 复核 | **7/7 成立** | 同左（PR-B 树 = A∪B 联合验收） |

**两条比 P7 更根本的发现**：
1. **迁移机制非确定性失效**：`schema_version` 混入文本行 ⇒ `ORDER BY updated_at DESC LIMIT 1` 取到文本 ⇒ `m.version > '文本'` 为 **NaN** ⇒ pending 恒空 ⇒ **零迁移执行**；生产库最近两行（`rowid 17` text / `rowid 18` int）**同秒并列** ⇒ **有时跑、有时不跑**。复核员以**翻转物理行序即翻转结果**独立复现。
   **反例边界（必须并读）**：`graph_nodes.props` 确实存在 ⇒ **不是"从未跑过任何迁移"**。
2. **改名必须原地 `ALTER … RENAME TO`**：若建新表，历史数据孤悬旧表无人读 = **静默丢数据**（铁律 11/24 级）；判别性夹具已证（新表 0 行 + 应用层 `totalRuns=0`）。

### D968 — 统一注册为单入口（**栈式两支**）
| | PR-A（mac） | PR-B（win，base=PR-A） |
|---|---|---|
| 分支 / HEAD | `feat/D968a-sentinel-single-registry` @ `16c49cea` | `feat/D968b-win-single-entry` @ `cd93104a` |
| 内容 | 5 件 adapters 逐件裁定 + 路径1 关停（no-op，export 保留＝栈式编译约束）+ 路径3 去除（只读启动快照，**保留 H1 跨源语义**）+ 空壳测试修复 | 去路径1 调用点 + Gate4 断言换向 |
| 栈式闭环 | PR-A 单独 2 红（**联合验收点**）→ **A+B 27/27 绿**；`tests/sentinel/` **247/247 绿** | — |
| 生产级证据 | `server.ts:86` → `Bootstrap` → `deploy/bootstrap.ts` **Phase 3b** → `initFileDrivenLoaders()` ⇒ **「唯一入口 = 文件驱动」在生产路径成立**；去掉路径1 调用点**行为中性**（路径1 原本 `registered: 0`） | |
| 复核 | **见 §三（阶段四）** | |

**禁令 4 实证留痕**：改动前 `scanned: 4, registered: 0` + 4 条 key 不匹配原始输出；`filenameToExportKey()` **未修**、随路径1 **整体删除**（「修 1 行激活 3 个」确有其事 ⇒ 必须关停而非修复）。

### D975 — 补 `ownership.yaml` 哨兵域规则（CTO 批准立卡）
| 项 | 值 |
|---|---|
| 分支 / HEAD | `docs/D975-ownership-sentinel-domain` @ `e38e927c` |
| 改动 | **纯追加 12 行，0 删改**：`extensions/sentinels/**` 与 `tests/sentinels/**` → owner `mac` |
| 根因 | 哨兵**本体目录**无规则 ⇒ 落 `extensions/**` = win，而加载器/类型网/测试 = mac ⇒ **任何真实哨兵改动必然跨域** |
| 判别性 | 撤规则 → ❌ 跨域复现；加回 → ✅ `9 个文件同域 mac`；17 条代表路径回归 **14 条不变、恰 3 条按设计变** |
| 状态 | 等 CTO 串行顺序：**D970 先合 → D975 后合**（同文件双写者串行） |

---

## 三、独立复核汇总（`task-4`，四阶段）

| 阶段 | 对象 | 结论 |
|---|---|---|
| 一 | 批基线 + P7 | 4 条队长结论全部成立；canary 红绿证明判据非空；**更正队长两处口径**（基线 sha / 零 finding 双列） |
| 二 | D965 / D966 | **D965 7/7**；**D966 9/9**（含「20」可复现的即时上报） |
| 三 | D967 A+B | **7/7 成立**；三条关键声称全部成立；plan.json 只增不改独立核实 |
| 四 | D968 A+B | **PR-A ①–④ 成立、⑤ 部分成立（批级时序）**；**PR-B 全部成立**；栈式联合验收点复现（PR-A 单独 2 红 → 叠加 11 passed）；四条关键声称全部成立；`DECIDE` 类残留另见 §三.8 |

**复核员的独立性**：全程只读、产物仅 `/tmp`、**不复用被验方脚本作判据**、自建探针 + 改坏即红；**未参与任何编码**；只说 `自验结论`。

### 复核员独立发现的、发单方未列的内容（均已登记）
1. **P7 是"整表结构漂移"而非"缺一列"**（`id` 类型亦错 ⇒ 补列后写路径仍 `datatype mismatch`）。
2. **`createEdge` 写路径同死**。
3. **`queryEdges` 方法层 41/41「正常返回空数组」**——内部 catch 只 `log.warn`、**无 `degraded` 传播**（铁律 11/31）⇒ **只在方法层插桩会得到假绿**。
4. **多租户主键冲突 → 整批丢记录**：`entity_id=''` + 毫秒级 `run_id` ⇒ 两组织同毫秒跑同一哨兵产生**完全相同四元主键** ⇒ 第二次写入 `UNIQUE constraint failed`，被 catch 成 warn，**该次运行 3 条 metric 整批丢弃**。
5. **注释与行为不符**：`measurements.ts` 注释称"UPSERT 语义"，实测**裸 INSERT 抛错**。
6. **`bootstrap.ts` 有 Phase 2a + Phase 3b 两个调用点做同一件注册事**（重复注册，registry 走"覆盖旧实例"）⇒ 「唯一入口 = 文件驱动」作为**入口种类**成立，但**不能表述为"只有一处调用"**；影响面（竞态）未量化。
7. **`decide-next.sh:77,82` 仍引用已删的 `src/sentinel/adapters/`** ⇒ D5/D6 缺口检查将**永远看到"缺件"**（出写集、行为不崩）⇒ 建议立卡。
8. **递归 manifest 口径歧义**：复核员实测 **57**（不含根级 `extensions/sentinels/manifest.json`）／**58**（含）；D968 声称 58。⇒ 判据表应固化"是否计入根级 extension-manifest"。
9. **D968 Done⑤ 的批级时序**：D968 触及的断言已改动态，但同树**仍有 3 处写死 45**（`check-sentinel-type-net.test.sh:37`、`d752-...test.ts:57,60`、`path-dependency-sentinel.test.ts:82`）——**恰在 D965 写集内**（D965 分支已改动态、main 未合）⇒ 「已全部改动态取数」在 **D965 落地后**才成立；D965 不落则哨兵数一变即红。**建议 K3 前做一次跨卡联合复跑对拍**（复核员已备好手法）。

---

## 四、归属逐张收口（**owner 不自动释放**）

| 卡 | owner | 分支 | 归属状态 |
|---|---|---|---|
| D965 | `sentinel-coder-a` | `fix/D965-sentinel-stub-cut` | **持有**（等 D975 落地后二次刷新 → CTO 开 PR → K3） |
| D966 | `sentinel-coder-b` | `docs/D966-sentinel-idle-repro` | **持有**（同上） |
| D967 | `sentinel-coder-a` | `feat/D967a` / `feat/D967b` | **持有**（合并序 A→B） |
| D968 | `sentinel-coder-b` | `feat/D968a` / `feat/D968b` | **持有**（合并序 A→B） |
| D975 | `sentinel-coder-a` | `docs/D975-ownership-sentinel-domain` | **持有**（等 D970 先合） |
| task-4 复核 | `sentinel-verifier` | — | 待 D968 阶段四回报后收口 |

**无一 owner 被释放**；合并顺序与前置链见 §二、§五。

---

## 五、未清项诚实登记

### 5.1 前置链（需 CTO 动作，缺一则 CI 不全绿）
```
① D970 合 main（同文件双写者串行：D970 先）
② D975 合 main（唯一解 D965/D967/D968 的 D734 ② 跨域）
③ D965 二次刷新（刷新只解 ③，② 依赖 ② 的规则落地）
④ 各卡 PR 由 CTO 开 → K3 复审 → 合并
```

### 5.2 CTO 已裁定、待其执行的工具/规则修复（本批只报不修 —— `scripts/**` + `ci.yml` 为 CTO 单写者域）
1. **D734 无栈式 base 支持**：`pre-commit-check.sh:1516` 调 `check-pr-budget.sh --quiet`，**base 硬编码 `origin/main`**（脚本支持 `--base` 但调用方不传）⇒ 栈式 PR 会把自己 + 下栈改动一起算（D968a 实测 **18 文件/跨域**，真 base 下 **7 文件/单 mac**）。裁定：改为传 `--base "${SYNO_PR_BASE:-origin/main}"`，CI 导出 `SYNO_PR_BASE=origin/$GITHUB_BASE_REF`。
2. **D2 登记门禁排除正则不含 `docs/synova/product-lines/evidence/**`**（本地会拦、**CI 空扫集恒过**，实测确认）。裁定：加排除规则。
3. **`check-deprecated-mapping.sh` 恒真门禁（预先存在）**：`grep -qL` 的 `-q` 吞掉 `-L` 输出 ⇒ `UNDOCUMENTED` 恒 0 ⇒ 永远 ✅。
4. **`synova-commit --check` 干跑不忠实**：`--check` 只 grep 字串 `提交已拒绝`（`:443-445`），真跑看退出码（`:780`）；Gatekeeper 在 `pre-commit-check.sh:242-253` 直接 `exit 1`、早于 `:1589` 打印该字串 ⇒ **干跑报绿、真提交被闸**。
5. **Gatekeeper 跨分支误报冻结进 main**：`bypass.log:1758`（`ce5b13b9` / `head-mismatch` / 作者 `synova-cto`）的两端提交**均不在 main**，却被 commit 进跟踪账本 ⇒ 本机所有工作树当日无法本地提交（CI 因 `GITHUB_ACTIONS=true` 跳过，不受影响）。
6. **Gatekeeper 时区口径不一致**：`TODAY=$(date +%Y-%m-%d)` 本地时区 vs 账本 UTC `Z` ⇒ 本地 00:00–07:59 写入的记录永匹配不上，**该门禁每日前 8h 静默失效**。
7. **`STAGED_SRC` 为空时组 6 整块静默跳过仍打 ✅（空洞绿）**：曾误导本批首版证据，由 `synova-commit --check` 揭穿后自证伪。
8. **`架构层: L3`（2 字符）被判"未填写"硬阻断**（`pre-commit-check.sh:877-883`）；`CLAUDE.md` V4.2.7 声称已修，**实测仍误判**。
9. **`alloc-task-id.sh` 不感知分段**（dry-run 给 D1008 ≠ Mac 段 D969）——与派单件误取 D1003–D1006 同源；**D974 已在处理**。
10. **两点 diff 伪影随 main 前进增长**（复核员测时 2 件 → 复测 16 件）⇒ **两点 diff 不可作夹带判据**，唯一安全口径 = 三点 / merge-point（与 D708 gate 同源）。

### 5.3 产品/技术未清项（建议后续卡）
1. **`input_digest` 语义偏差**：用**运行指纹**（哨兵 id + ok/degraded + finding id 有序集）代替输入哈希 ⇒ 防住"观测形态不同的两点被混比"，**没防住**"图输入变但 finding id 集不变"。
2. **`entity_id` 恒 `''`** ⇒ `metric_id` 维度可分时点、**`entity_id` 维度不可分**；叠加毫秒级 `run_id` ⇒ **多租户同毫秒主键冲突、整批丢记录**（见 §三.4）。
3. **同名不同实**：`measurements.ts` 注释称 UPSERT，实为裸 INSERT 抛错（§三.5）。
4. **`tsc` 基线 28 条红**（25 条来自现存 12 个 `_extinct/` 同型 import；CI 白名单过滤后为 0）⇒「归档死代码也 tsc 干净」为独立卡。
5. **`src/server.ts:15` 死 import**（`initFileDrivenLoaders` 全文 0 次调用；真正调用在 `deploy/bootstrap.ts` Phase 3b）。
6. **生产库图数据为空的原因未查**（CTO 已裁：并入 D966 报告 + 登记后续卡）。
7. **`graph_nodes` 亦为旧结构**（`props_json` + `props` 并存），影响面未评估。
8. **测试写残留**：全量 `vitest` 会改脏 4 个 tracked 文件（`extensions/industries/*/thresholds.json` 的 `aggregatedAt` 等）——D580 同型第二次；成员在 `git add -A` 一关自行拦下。
9. **`PR-B` 未复跑 `tsc --noEmit`**（纯删除、无新增类型面，理由已登记）。
10. **院方 20/45 与 D966 的口径对拍已完成**，但**复核员六口径字母定义与 D966 断面定义未逐条闭合**，建议对拍后固化判据表。

### 5.4 ACK 使用登记（Gatekeeper 逃生舱）
- 授权链：CTO 2026-09-25 明确回执「**我确认是我本人的记录且已复核**」；小队按「**限本批、逐次授权**」执行。
- 仅对 `ce5b13b9` 这一条生效；**每次提交前后 `grep -c "2026-09-25.*detected-bypass"` 均 = 1**（**无新增记录**）；**未改** `.claude/bypass.log`；**无人使用** `--no-verify`。
- 已知留痕缺口：`degraded-events.log` 被 `.gitignore` 的 `*.log` 命中 ⇒ **ACK 留痕未入库**（本地审计）。

---

## 六、回执三件套索引

| 件 | 路径 |
|---|---|
| 队长前提冻结证据 | `docs/synova/product-lines/evidence/D965-D968-队长前提冻结/`（`probe.mts` / `stub-check.mts` / `runtime-rows.json` / `static-rows.json`） |
| 复核员阶段一证据 | `docs/synova/product-lines/evidence/D965-D968-独立复核-20260925/`（已随 #790 合 main） |
| D965 证据 | `docs/synova/product-lines/evidence/D965-sentinel-stub-cut-evidence.md` + `.../D965-sentinel-stub-cut/{probes,results}/` |
| D966 证据 | `docs/synova/coordination/哨兵-空转复现-20260925.md` + `docs/synova/product-lines/evidence/D966-sentinel-idle-repro-evidence.md` |
| D967 证据 | `.../evidence/D967a-measurements-store-evidence.md` ／ `.../evidence/D967b-sentinel-alert-stats-evidence.md` |
| D968 证据 | `.../evidence/D968-sentinel-single-registry-evidence.md` |
| D975 证据 | `.../evidence/D975-ownership-sentinel-domain-evidence.md` |
| 分支回执 | 各卡 `git ls-remote --heads origin | grep <branch>`（见各卡 evidence 件） |

---

## 七、效力声明

- 本件与各卡 evidence 件均为 **`自验结论` 级**材料；**通过性判定归 CTO 收件闸 + K3 终审**。
- 本批 **未做**自我审计；复核由**未参与编码**的独立成员完成；**审计结论只认 K3 报告**。
- 小数：本件所有数字均取自命令原始输出；**禁手写**。
