# 已作废口径表（D943）

> **派单件与卡必须引用本表**；**凡在交付物中把下列口径当现状引用 → K3 判 FAIL**。
> 追加规则：只增不删；每条须有"作废日期 + 依据 + 替代口径"。

## 引用豁免

- `0.1.7-alpha.2 @ 00102833` — 本表第 1 行登记的是该口径的**作废事实本身**（退役记录，非现状引用），属逐行显式豁免条目；本表全表为作废登记簿，同理豁免（D943-FIX：豁免只认本段显式条目，不再由关键字触发）。

| # | 作废口径 | 作废日期 | 依据 | 替代口径 |
|---|---|---|---|---|
| 1 | DSH 断面 = `0.1.7-alpha.2 @ 00102833` | 2026-09-24 | checkout 16:04:35 移至 `dsh-v0.1.7-rc.1`（`git reflog` 实测） | **`0.1.7-rc.1 @ 46a7f68b`**（唯一源 `DSH-断面.json`） |
| 2 | "会话格式已改 **三次**" | 2026-09-24 | rc.1 实为 `v0→v4` 四代（`01`/`04` 未回填） | **四次（v0→v4）** |
| 3 | "**45** 个包不可达 / 14.7%" | 2026-09-24 | `closure-rc1.mjs` 重算（`05:44` 已改） | **63/307（20.5%）**；分母 307 vs 316 待核 |
| 4 | "`.synova-wt-*` = **149** 个" | 2026-09-24 | 实测 `find -maxdepth 1 -type d -name ".synova-wt-*" \| wc -l` = 160 | **160**（或写"≥160，随开发漂移"） |
| 5 | "活跃专家文件 **50** 个" | 2026-09-24 | 实测排除 `_` 前缀后 = 51 | **51** |
| 6 | 预设"目录式需迁移**对本方不适用**"（`06:184` U14） | 2026-09-24 | 该 find 找的是名为 `presets` 的目录；我方是 `.agent-presets`（12 组实测） | 判定**作废**；且真实边界是 `0.1.7-alpha.1` 起目录式不可读 |
| 7 | "D931 已把队长预设接到 Agent Teams（重开即生效）" | 2026-09-24 | rc.1 **只读 bundle 声明行**；修复写在 `.agent-presets`（死层） | **未生效**；待「预设安装器迁移」卡完成后重验 |
| 8 | "`01:41,224` 20/45 哨兵不查 store" | 2026-09-24 | 静态复现两次均为 **2**（运行时口径未复跑） | 标**口径待核**，须按议程 7.3⑤ 登记后方可引用 |
