# 开工指令 — Mac 小队 · D937（P0）｜五卡 WIP=1 序列第 1 张

> 派单件：`docs/synova/coordination/派单-Mac小队-五卡-20260924.md`（本件是其开工令）
> 规则：**五卡已开在板上，一次只开工一张**；本张未通过收件闸前，不放行第 2 张。

## 一、执行形态（**硬要求，缺一即退回**）

1. **必须用 Agent Teams 组队**：预设 `synova-squad-lead` 已挂 `agent-team` + `tool-agent-team` + `ui-agent-team`，`maxMembers=4`。
2. **编制**：队长 + ≤2 编码 + **1 独立自验**（自验**不得**由编码兼任）；**队长不下场写码**。
3. **派活走共享任务**：每成员一份，正文含「**写集（逐文件） + 验收命令 + 写者**」；写集两两不重叠。
4. **重型验证串行 ≤1**（vitest／全量门禁／黄金门禁）。
5. **回执必须含「团队成员运行记录」**：成员名 · 运行状态 · token · 共享任务 id 与状态。
6. **判定与退回**：只队长在跑 / 成员未运行 / 无共享任务 id / 写集未逐人拆 / 自验由编码兼任 → **退回**。
   ⚠️ **严禁用子代理代替小队**：子代理无共享任务板、无 mailbox、无持久身份 → 无法独立自验、无法留运行记录 = 作者自判通过（已发生过的 M3 型事故）。

## 二、第 0 步：**先进 plan 模式出计划，停下等 CTO 复核**（不要直接动手）

计划必须包含六项，缺一不批：

| # | 计划项 | 要求 |
|---|---|---|
| ① | **依赖图** | 本卡与其它四卡/在飞 PR 的先后关系；前置不成立就停手报 CTO |
| ② | **逐文件写集** | 精确到文件；标明谁写（成员名）；两两不重叠 |
| ③ | **夹具设计** | 三路径（正常/降级/边界）+ **变异体反例设计**（"改坏哪一行 → 哪个夹具必须红"） |
| ④ | **失效条件** | 什么证据会推翻本卡结论（写进验收） |
| ⑤ | **前置冻结清单** | 每条附「命令 + 原始输出」；不成立 → 停手报 CTO 改卡 |
| ⑥ | **可复制验收命令** | 我按此复跑 |

**交付计划后停下**；CTO 复核放行 → 才进入执行。

## 三、D937 卡内容与一票判据

**事实（CTO 已复现）**：`scripts/pre-commit-check.sh` 组 7a 的 `grep -Ev` 模式含 `^+++`（**非法 ERE**，最小复现 `echo test | grep -Ev "^+++"` → **exit 2**）→ 管道整体失败 → `NEW_DIAG` 恒空 → 「禁止新 DiagnosticModule」**本地与 CI strict 均恒过**（检查自建立起从未生效）。

**写集**：`scripts/pre-commit-check.sh` + 对应测试文件（`tests/**` 内该组的测试）。

**一票判据（全为可执行项）**：
1. **变异体夹具**：构造一个含 `DiagnosticModule` 的新增 → 该检查**必须红**（贴"改坏 → 红"的原始输出）
2. **无误伤**：在 main 上跑 → 不产生新红
3. **三路径**：正常（干净树）/ 降级（grep 不可用时的行为，须显式）/ 边界（新增恰好只含注释里的 `DiagnosticModule` 字样 → 不得误报）
4. **口径**：所有计数带「命令 + 排除项 + 截至时刻」

## 四、回执格式（缺项视为未完成）

1) 计划（plan 模式产出）+ CTO 放行记录
2) **团队成员运行记录**（成员名·状态·token·共享任务 id 与状态）
3) 每处改动：`file:line` + 改前/改后
4) **变异体夹具的"改坏即红"原始输出**
5) 三路径夹具原始输出
6) `git diff --stat` + `ls-remote` 回执
7) **未清项清单**（诚实登记，不许写成"已完成"）

## 五、红线

不碰 `scripts/audit/**`；禁 `--no-verify`／`git stash`／force push 共享分支；任务在 `.synova-wt-*`（**名字必须带侧前缀**，如 `.synova-wt-squad-d937`，防撞号）；**主工作区只做同步**；**你不判"通过"**（归 CTO 收件闸 + K3）。

## 六、五卡队列（WIP=1）

| 序 | 卡 | 一句话 |
|---|---|---|
| **1（本张）** | **D937** | 门禁 fail-open 假绿（P0） |
| 2 | D938 | alloc-task-id 两缺陷 + 全角清扫 |
| 3 | D939 | 生成物注释漂移 |
| 4 | D940 | 名字统一分配 |
| 5 | D941 | ownership 生成式重写（必过 K3） |

---

## ⚠️ 补充（2026-09-24，D937 开工前必读）— 执行形态的判据已更换

**背景**：本令原写「预设已挂 agent-team；落位后跑三条 grep 验 `~/.dsh/.agent-presets/**`」。
**该目录已被上游作废**（`editing-cordis-compositions/SKILL.md`：legacy preset 是 `$DSH_HOME/.agent-presets/<id>/` 目录，**"Nothing reads that directory any more"**）。
⇒ **凡 `~/.dsh/.agent-presets/**` 的判据与动作一律作废**（含 CTO 此前做的"同步到 trial-017"，可能无效）。

**新的验证机制（上游指定）**：预设改为 **bundle 里的声明行**（`name: '@deepseek-ai/dsh-agent-preset'` + `config: {id, order, plugins}`）。
**新判据（两条，必须给原始输出）**：
1. `plugin_manager` 的 **`list_bundles`** 能列出已装 bundle；
2. **`list_plugins`** 出现 **`preset-<id>`** 行且**无诊断**；
3. 外加：**新开一个会话、选择器里可见该预设**。

### 这对 D937 的**直接影响（必须处理）**
- 本卡要求 **必须用 Agent Teams 组队**（队长不下场写码、成员共享任务、回执含成员运行记录）。
- **若上述三条拿不到**（无 `preset-<id>` 行 / 选择器不可见）→ **组不了队** → 请**停手报 CTO**。
- **严禁**因此改用子代理顶替：那会使回执**没有团队成员运行记录**，按派单**直接退回**（M3 型「机制建成未接线」）。
- 若你**已能组队并跑通**，请在回执里附**你实际用到的命令与原始输出**——CTO 将把它固化为两侧唯一判据。
