# 批十二 · 计划与派单（创始人四条裁决后的执行总计划）

> **依据计划: v1.2@4e46603f**（`docs/synova/coordination/整体推进计划-主线-20260913.md`）
> **上游依据**：`docs/authority/产品完成度定义与推进总纲-20260918.md`（已登记 DOC-0030）+ 四份任务书
> **出单**：synova-cto ｜ 2026-09-18 ｜ **创始人裁决**：全部要做 + 分清自研/借鉴 + 台账与面板跟踪 + 新验收标准固化到派单/dev doc/CTO 验收三处

---

## 〇、一句话计划

**先把"判定能力"建起来（第 0 项验证基建 + 不变量），再把 9 处断线接上，最后才谈新线扩张；每一步都按新的验收标准（穿真实入口）判定，不用 grep 冒充完成。**

---

## 一、创始人第 1 条要求的落地：每个工作项标注「自研 / 借鉴」（全清单）

| 工作项 | 方式 | 依据（一句话） |
|---|---|---|
| **P-3 验证基建**（第 0 项，D817 已派） | **③自建** | 复用我方既有 `listen(0)+fetch` 范式，零 DSH 依赖 |
| **P0-1+P0-2**（D819 已派） | **③自建** | 自研缺陷（工具 schema 未进请求体、tool 配对错误） |
| **P-2 不变量机制**（待派） | **①范式复用** | DSH `dsh-invariants`（123 行，净逻辑约 40 行）→ **读源码自研，不引包** |
| **P-1 HTTP 代理**（D821 本批派） | **①范式复用 + ③自建** | 借 DSH 的策略语义（解析顺序/loopback 网段/CIDR/降级姿态）；SOCKS5 与自定义 CA 自建 |
| T-D1 韧性层 / T-D10 重试落盘 | **已由 D810 覆盖**（PR-1 已合） | 自研 |
| T-D2 压缩入日志 / T-D9 工具事件 | **①范式复用** | DSH `tool/call`+`tool/result`+`sourceEventSeqs` 的事件形状 |
| T-D3 删会话级联（D820 已派） | **③自建** | 自研（函数已存在，零调用） |
| T-D4 `finish_reason` | **①范式复用** | DSH 压缩处显式拒绝 `length` 截断 |
| T-D5 降级自遮蔽（D822 本批派） | **③自建** | 自研缺陷（返回值已编码失败，无人透传） |
| T-D6 im-inbound 空转（D823 本批派） | **③自建** | 自研缺陷 |
| T-D7 ToolGuard 清零 | **①范式复用** | DSH `dsh-repeat-tool-reminder` 的"连续语义 + 新消息清链" |
| T-D8 不可信包裹 | **③自建** | 自研缺陷（前缀判据对不上真实工具名） |
| T-D11 类型欠账 | **③自建** | 自研（25 个在死代码，根因 import 少一层 `../`） |
| T-D12 孤儿脚本（D824 本批派） | **③自建** | 自研（防回潮门禁自己没接线） |
| 指标时序存储（线 27-1） | **③自建** | **DSH 全家族无时序存储**（`kv` 是唯一分面）→ 只借鉴写链纪律 + 原子发布协议 |
| 趋势接线（线 27-2） | **③自建** | 我方已有 121 行算法，零调用 |
| 参数标定 / 跨客户收敛（线 27-3/27-4） | **③自建** | DSH 无对应物（领域护城河） |
| 会话统计/用量（归线 22） | **①范式复用** | DSH `dsh-session-stats` 的字段口径 |
| 会话搜索 FTS5（归线 18） | **①范式复用** | DSH `dsh-session-query-sqlite` 的 `UNINDEXED`/字面短语/世代游标 |
| 遥测 / OTel（归线 22） | **③自建（走 OTel 标准）** | **DSH 的不是可观测性层**（默认关闭的日志外发管道、默认原样出外、不做 at-least-once） |
| 凭据管理（归线 4） | **①范式复用 seam + ③自建后端** | DSH `-local` **明确否决**（明文落盘）；seam 语义照抄 |
| 设置热改（归线 25） | **①范式复用** | DSH `dsh-settings` 的 `applies: live/restart` + revision |
| webhook（归线 8） | **5a ①范式复用 / 5b ③自建** | DSH 的 webhook 是**入站**；我方需求偏**出站**（DSH 无对应物） |
| spill / 输出保留（归线 4） | **①范式复用 seam + ③自建后端** | retention 285 行是纯函数可照抄；后端必须自建（DSH 本地路径 locator 在多租户不成立） |
| 时间上下文（归线 3） | **7a ①范式复用 / 7b ③自建** | DSH 只有"生成时点"，"数据时点/时间窗"DSH 完全没有 |
| 文档转换（归线 4） | **③自建（B 路线：客户侧转 PDF）** | `libreoffice-kit` 为 MPL-2.0 且许可义务依赖一个不可验证可达的仓库 → 不冒风险 |
| 多客户配置分层（线 28-1） | **①范式复用** | 我方 `config-layers.ts` 已按 DSH 语义自研过一次 |
| 租户数据隔离（线 28-2/28-3） | **③自建** | DSH 明确"无调用方授权，可信服务"——**对我方绝对不适用** |

> **G1 守卫结论（CTO 复核）**：以上**无一项**走 ②包复用 → **不需要动 G1 豁免**；`grep "@deepseek-ai" src/ packages/` 维持零命中。

---

## 二、全部工作清单与归属（谁做什么 · 依赖 · 顺序）

### 2.1 已派出（进行中）

| 卡 | 内容 | 归属 | 状态 |
|---|---|---|---|
| D817 | 第 0 项 验证基建（CI 跑生产入口级对话） | 编码线 | 已派（PR #637 合并） |
| D819 | P0-1+P0-2 工具接线（必须同时修） | 编码线 | 已派（前置 D817） |
| D820 | T-D3 删会话级联 | 编码线 | 已派 |
| D818 | 三项开工前核实（只读） | 规格线 | 已派 |
| D811–D816 | 队列收口 / 门禁补洞 / 控制塔卫生 | 编码线 | 已派（**建议排在任务书之后**） |

### 2.2 本批派出（批十二，4 张）

| 卡 | 内容 | 判据 | 量 | 依赖 |
|---|---|---|---|---|
| **D821** | P-1 HTTP 代理 / 内网适配 | A | 3–10 天（零依赖路线可压 3 天） | D818 §3（Node 基线）+ 冻结唯一出口 |
| **D822** | T-D5 降级信号自遮蔽 | C | 1–3 天 | 无 |
| **D823** | T-D6 IM 历史恢复空转 | C | 1–3 天 | 无 |
| **D824** | T-D12 孤儿门禁脚本处置 | C | ≤1 天 | **D813 先落**（同文件） |

### 2.3 下一批（批十三，CTO 起草 → 派工）

| 组 | 内容 | 归属 |
|---|---|---|
| **9 断线补卡** | T-D2（压缩入日志）/ search 租户隔离 / 会话统计两处（`agent_metrics` 零写入 + `checkpoint()` 零消费者）/ 趋势接线 / `packages/evolution` 接线（2,503 行）/ T-D1·T-D10 复核（D810 是否真覆盖） | 编码线 + K3（复核） |
| **P-2 不变量机制** | 首批 3 条断言（工具 schema 真发出 / tool 消息配对 / 降级不被抹） | 编码线（**硬依赖 D819 完成**） |
| **固化三件** | ① 验收标准进派单与规格（D825）② CTO 验收记录模板 + 检查（D826）③ 面板/台账跟踪三层清单（D827） | 编码线（门禁部分）+ CTO（模板与口径） |
| 其余 8 项 DSH 缺口 | 凭据 / 搜索 / 遥测 / 迁移链 / 设置热改 / 轨迹 / spill / 时间上下文 / 文档转换 Webhook | 分批派（第 2/3 批） |

### 2.4 CTO 自己做（不能派的）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 总纲登记进 `docs/authority/` + 台账（DOC-0030） | ✅ 本批完成 |
| 2 | 验收标准固化件 v1（DOC-0031） | ✅ 本批完成 |
| 3 | **前提清单** `preconditions.yaml`（3 项） | ✅ 本批完成（**优先级高于线待你点头**） |
| 4 | 两条新线草案（价值/完成定义/10 个验收点） | ✅ 本批完成（**待你确认后登记 yaml**） |
| 5 | 验收（按新标准执行 + 出记录） | 持续 |
| 6 | 周报（方向/真进度/节奏/风险/待裁决） | 持续 |

---

## 三、硬顺序（不得颠倒）

1. **P-3（验证基建）→ 所有"穿真实入口"的验收**：没有它，后面的"完成"都无法自动判定
2. **P0-1+P0-2 → P-2（不变量）**：否则不变量在生产中一次都不会被触发 → **新机制自己就变成"机制在场但不接线"**
3. **指标时序（27-1）→ 参数标定（27-3/27-4）**：没有历史测量值就无数据可标
4. **D813 → D824**（同碰 `scripts/pre-commit-check.sh`）
5. **D810 PR-2 → T-D1/T-D10 复核**：复核必须对着已合入的 main 做

---

## 四、需要你确认的（3 项待你点头 + 2 项已答）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 第 0 项立项（CI 跑生产入口） | ✅ 已答（已派 D817） |
| 2 | **确认新增两条线**（测量与参数标定 / 多客户配置与租户分层） | ⏳ 草案已写好：`docs/synova/coordination/草案-新增两线-20260918.md` |
| 3 | **确认 11 项 DSH 缺口"归线不新开"**（遥测/统计→22、搜索→18、溢出/凭据/文档/联网→4、设置热改→25、webhook→8、时间上下文/轨迹→3） | ⏳ |
| 4 | **立前提清单且优先级高于线**（会改看板默认排序） | ⏳ 清单已写好，等你点头生效 |
| 5 | 四个产品决定（webhook 入站/出站、两个 cap 数字、时间窗粒度、live/restart 清单） | ⏳ 未定前那 4 项不派工 |

---

## 五、派单前复核（CTO 已亲自核实）

| 项 | 证据 |
|---|---|
| ① 任务号真实 | D821–D824 全 refs 扫描占用 = 0 |
| ② 前置状态 | D817/D819/D820 已合入 main；D824 依赖 D813（在飞）；D821 依赖 D818 §3 |
| ③ 技术声称亲自复现 | P-1：全 src 代理相关 grep 零命中；T-D5：`session-store.ts:377-383` + `routes/conversations.ts:249/255/260` 已读；T-D6：`im-inbound.ts:195` 全仓唯一调用点；T-D12：`check-bridge-files.sh` 不在 hooks/pre-commit/CI/package.json 任一处 |
| ④ 写集路径 | 既有文件均存在；新建文件已标；**D824 与 D813 同碰一个文件 → 串行** |
| ⑤ 写集零交集 | D821（infra/providers/connectors）/ D822（store/routes）/ D823（l1）/ D824（scripts）互不重叠 |
| ⑥ 引用行号 | 均当场核过；执行方开工第一步重新核（会漂移） |
| ⑦ 依赖图 | 见 §三 五条硬顺序 |
| ⑧ 交付物齐备 | 每卡：写集/红线/Done（可证伪）/前置；验收统一引用验收标准固化件 |
| ⑨ 派单内部一致性 | 四卡验收方式一致（穿真实入口），无对同一路径互斥要求 |
| ⑩ 主线计划锚定 | `依据计划: v1.2@4e46603f` |

---

## 六、创始人复制块（可直接转发给 🛠 编码 session）

```
任务 D821｜P-1 HTTP 代理 / 内网适配（前提清单第 1 项：客户内网装不上 = 全盘归零）。
派单全文: docs/synova/coordination/批十二-计划与派单-20260918.md §2.2 + 卡 task-state/D821.json
现状：全 src 代理相关 grep 零命中；出站是裸 fetch（25+ 处，providers/connectors/tools/deploy 四层）。
前置：① 先冻结"出站 HTTP 唯一出口"（否则会出现"有时走代理有时不走"）② D818 §3 的 Node 基线结论（决定能否零依赖走 NODE_USE_ENV_PROXY）。
验收（全穿真实出站，假代理判定）：代理真生效 / loopback 整网段强制绕过（含 127.0.0.2）/ connector 也覆盖 / socks5 降级不阻止启动且健康检查暴露 / 静态断言"除 http-proxy.ts 外不得读 HTTPS_PROXY"。
参照 DSH dsh-http-proxy 的策略语义（解析顺序/loopback/CIDR 需改写/降级姿态），**不引 undici**；SOCKS5 与自定义 CA 自建。
验收标尺：docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md（禁 grep 命中；三问自查；报告含命令+原始输出）。开工第一步重新核行号。开始吧。
```

```
任务 D822｜T-D5 降级信号自遮蔽（1–3 天，性价比最高档）。
派单全文: docs/synova/coordination/批十二-计划与派单-20260918.md §2.2 + 卡 task-state/D822.json
现状（CTO 已核）：src/store/session-store.ts:377-383 addMessage 成功即重置 lastDegraded；routes/conversations.ts:249/255/260 第 2 次写成功后才检查 → 写失败信号被抹掉、STORE_DEGRADED 帧不发。**且该缺陷被 tests/store/session-event-log.test.ts:146-164 固化成预期（测试在保护缺陷）**。
要求：addMessage 透传已有的 AppendEventResult；改写那条固化缺陷的测试。
验收：注入一次写失败+一次成功 → 断言 STORE_DEGRADED 仍被发出；断言旧测试已改写。开始吧。
```

```
任务 D823｜T-D6 IM 历史恢复静默空转（1–3 天）。
派单全文: docs/synova/coordination/批十二-计划与派单-20260918.md §2.2 + 卡 task-state/D823.json
现状（CTO 已核）：src/l1/im-inbound.ts:195 调用 addToHistory —— 全仓仅此一处，且写法是先 as unknown as 再可选链 → 每轮模型只看到当前一句。
要求：接上真实历史读取；若确定不做该意图 → 显式删除，不留假动作；不得再有 as unknown as（铁律 38/CT-46）。
验收：经 IM 通道发两轮 → 第 2 轮上下文含第 1 轮。开始吧。
```

```
任务 D824｜T-D12 孤儿门禁脚本处置（≤1 天）——**须等 D813 落地后再动**（同碰 scripts/pre-commit-check.sh）。
派单全文: docs/synova/coordination/批十二-计划与派单-20260918.md §2.2 + 卡 task-state/D824.json
现状（CTO 已核）：scripts/check-bridge-files.sh（111 行，自述铁律 46）不在 hooks / 不在 pre-commit-check.sh / 不在 CI / 不在 package.json —— "写了没接上"的第 4 个实例，且是**防回潮门禁本身**；等效能力已在 pre-commit-check.sh 组 5b 实现。
要求（二选一，不留两个实现）：① 删除该脚本；或 ② 组 5b 改为调用它。
验收：故意造一个桥接文件 → 断言 pre-commit 拦住。开始吧。
```

---

## 七、本批不做

- 不派 P-2 不变量（硬依赖 D819 完成）、不派 8 项 DSH 缺口正式任务（第 2/3 批）
- 不派四个卡在产品决定上的项（线 8 webhook / 线 4 spill cap / 线 3 时间窗 / 线 25 live-restart 清单）
- 不改 `product-lines.yaml` 的线集（**只有创始人能改**；草案已备，等你确认）
