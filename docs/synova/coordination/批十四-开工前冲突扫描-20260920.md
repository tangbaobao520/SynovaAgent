# 批十四 · 开工前冲突扫描（M2 必做，开工前，一页）

> 依据：`docs/synova/coordination/小队派单-批十四-win并行-20260920.md` §三② + 《小队模式-固化件-v1》M2
> 执行：队长（并行 CTO session）｜ 2026-09-20 ｜ 工作树 `.synova-wt-team14` 分支 `team/win-batch14`
> 基线：`origin/main` @ `4f3bb21c`（本工作树基线 = `d527abcb` = main + 派单件）
> 目的：回答「**谁还会改这些文件**」——逐文件，禁目录级模糊

---

## 〇、勘误（ERRATA）— 本件自身 2 处错误，由队员独立抓出，已修正

> 记录原则：本条不是补充说明，是**本件曾出错**的物理留痕。M4「执行证据链断裂」的反面示范——若不留痕，后续读者会把下面的 §四 当作当时的权威结论。

| # | 位置 | 错在哪 | 根因 | 谁抓出 | 处置 |
|---|---|---|---|---|---|
| **E1** | §四 C‑D829 | 断言「`collectFeedback` 全仓零调用」 | 我的 grep 管道接了 `head -10`，输出被**截断在第 10 行**，`src/routes/chat.ts:70`（字母序靠后）被静默丢弃 | 自验队员（并独立复核） | 见 §四 修正版；并**再发现** `src/routes/evolution.ts:210` 亦为生产调用（同一根因漏掉） |
| **E2** | 全文方法（§一） | 扫描维度只有 `src/` / `packages/` / 分支 / PR，**漏了 `tests/`** | 我按「谁会改产品代码」建维度，未按「谁会改同一批文件」建维度 | 编码 C（请求把 2 个存量测试纳入其写集时暴露） | §四 写集表补 `tests/` 行；C 的 2 个存量测试文件经核无冲突后纳入 |

**教训（写给后续批次）**：两条都不是"判断"错，而是**证据采集面不完整**（`head` 截断 / 维度缺失）。凡"零命中"类断言，**必须**给完整命令且**不得接 `head`**；凡"无冲突"类结论，**必须**覆盖 `tests/`。

---

## 一、方法（四条独立证据，全部物理执行）

| # | 证据 | 命令 |
|---|---|---|
| 1 | 本机**所有** git worktree 的未提交改动是否覆盖本队写集 | `git -C <wt> status --porcelain \| grep -E '<本队文件>'`（遍历 `git worktree list`） |
| 2 | 本机所有分支 vs main 的差异是否覆盖本队写集 | `git diff --name-only origin/main...<branch>` |
| 3 | **GitHub 全部 39 张在飞 PR** 的改动文件 vs 本队写集 | GitHub API `/pulls?state=open` + `/pulls/{n}/files` |
| 4 | 哪些 task-state 卡的 `write_set` 声明了这些文件（+ 其状态） | `grep -l '<file>' task-state/*.json` + 读 `status`/`owner` |

---

## 二、逐文件结论（「谁还会改它」）

| 目标文件 | 本队切片 | 未提交改动 (证据1) | 分支差异 (证据2) | 在飞 PR (证据3) | 其他卡 (证据4) | **裁决** |
|---|---|---|---|---|---|---|
| `src/store/session-store.ts` | A‑D820 / A‑D822 / 波2 D826 | **无** | d593(09‑09, 386 behind) | **无** | D487/D500/audited、D563/impl_done | ✅ 本队独占。**A 内串行**（D820→D822）；**D826 排波2** |
| `src/routes/conversations.ts` | A‑D822 | **无** | d593(09‑09)、d810(已合) | **无** | D810/impl_done、D817/impl_done | ✅ 本队独占，无并发写者 |
| `src/l1/im-inbound.ts` | B‑D823 | **无** | 无 | **无** | D487/audited | ✅ B 独占 |
| `packages/evolution/**` | C‑D829 | **无** | session/03(07‑06, 2240 behind)、desktop‑phase0‑auth(07‑01) | **无** | 仅 D829 | ✅ C 独占（见 §四 写集登记） |
| `src/l4/temporal-baseline.ts` | 波2 D828 | **无** | session/04(07‑05, 2264 behind) | **无** | 仅 D828 | ✅ D828 独占 |
| `src/init/engine-context.ts` | 波2 D827 | **无** | 无 | **无** | D454/audited | ✅ D827 独占 |
| `src/store/session-projection.ts` | 波2 D827 | **无** | win‑d588(09‑08, 431 behind) | **无** | D588/audited | ✅ D827 独占 |
| `src/routes/sessions.ts` | 波2 D826 | **无** | 无 | **无** | **无任何卡声明** | ✅ D826 独占（首次接线） |

**结论：本队写集与全部在飞工作（worktree 未提交 / 分支 / 39 张在飞 PR / 其他卡）零重叠。**

---

## 三、陈旧分支警示（**禁止 merge / cherry-pick**）

以下分支触碰本队文件，但均**高度落后 main**，其内容已过时；从它们取代码 = 引入 400–2200 commit 的回退：

| 分支 | tip 日期 | 落后 main | 触碰本队文件 |
|---|---|---|---|
| `origin/feat/d593-report-persistence-p2` | 2026-09-09 | 386 | session-store.ts / conversations.ts |
| `origin/feat/win-d588-session-projection` | 2026-09-08 | 431 | session-projection.ts |
| `origin/session/03` | 2026-07-06 | 2240 | packages/evolution/src/* |
| `origin/session/04` | 2026-07-05 | 2264 | src/l4/temporal-baseline.ts |
| `origin/feat/desktop-phase0-auth` | 2026-07-01 | 2527 | packages/evolution/* |
| `origin/feat/d810-llm-resilience-wiring` | 2026-09-18 | 142 | conversations.ts（**其变更已在 main**，分支为残留） |

> D810 核实：卡面 `status=impl_done`、`impl.commit=28d54b37`，该 SHA **不是** main 祖先，但 main 上**已有**其接线（`src/llm/retry-middleware.ts` 存在、`callWithResilience` 在 `src/agent/tool-loop-executor.ts:19,99` + `src/store/retry-projection.ts:17` 有生产调用）→ 判定为 **squash 合并**，非未合并。**不影响本队。**

---

## 四、写集登记（M2：逐文件，两两不重叠）

| 切片 | 独占文件（本队内） | 声明人 |
|---|---|---|
| **A‑D820** | `src/store/session-store.ts`、`tests/store/delete-session-cascade.test.ts` | 队长 |
| **A‑D822** | `src/store/session-store.ts`（**与 D820 同人串行，不同波**）、`src/routes/conversations.ts`、`tests/store/session-event-log.test.ts`、`tests/store/degraded-signal-not-masked.test.ts` | 队长 |
| **B‑D823** | `src/l1/im-inbound.ts`、`tests/l1/im-inbound-history.test.ts` | 队长 |
| **C‑D829** | `packages/evolution/**` ＋ **`src/routes/ga-calibration.ts`**（接线点，见下）<br>＋ **[修正·E2] 存量测试** `tests/evolution/feedback-collector.test.ts`、`tests/routes/ga-calibration.test.ts`（均存在、队内无他人声明，已纳入）<br>＋ 新建 `tests/routes/evolution-rule-version.test.ts`（验收点③穿入口证据；该文件不存在，零交集） | 队长（派单 §二 授权 C 登记，此处由队长登记并冻结） |
| **波2 D826** | `src/store/session-store.ts`、`src/routes/sessions.ts`、其配对测试 | 队长 |
| **波2 D827** | `src/init/engine-context.ts`、`src/store/session-projection.ts`、用量出口路由、其配对测试 | 队长 |
| **波2 D828** | `src/l4/temporal-baseline.ts`、哨兵侧调用点、其配对测试 | 队长 |
| **各切片** | `task-state/D82x.json`、`.claude/task-briefs/<当日>-D82x-*.md`、`memory/notes/proposed/<当日>-D82x-*.md` | 派单 §二 |
| **证据文件（全员可写，按任务号隔离）** | `docs/synova/product-lines/evidence/<各自 D#>...`——**[修正·E3]** 本行原写"队长队外产物"，与派单 §三⑥ 冲突（M4：指令≠工具副作用）。正确规则：该目录**由各卡产出者写**，但**每个文件必须带自己的任务号**（M5 硬约束）⇒ 文件名唯一 ⇒ 天然无冲突。禁止写别的任务号命名的文件。 | 队长更正 |
| **队长（队外产物）** | 本件、`docs/synova/coordination/批十四-*.md`（收尾三件/自验结论汇总） | 队长 |

> **⚠️ 已知的良性重叠（看板 writeScopeWarnings，显式说明而非隐藏）**：task-3（C）与 task-4（自验）在 `docs/synova/product-lines/evidence/` 上**前缀包含**，看板报 `write scopes overlap with task-4`。
> **判定：良性，不阻断。** 依据 M5 —— 该目录内每个文件**必须带自己的任务号**：C 只写 `D829-*`，自验只写 `D8xx-verify-*`，**文件名唯一 ⇒ 无物理重叠**。write_scopes 是前缀语义、无法表达「同目录不同文件名」，故此告警是**维度不匹配的假阳**，不是真冲突。
> **残余风险**：仅当有人违反 M5（写不属于自己任务号的文件名）时才成真 —— 该违反本身即退回项（自验已登记）。


### C‑D829 接线点登记（派单 §二 要求「由 C 在开工前冲突扫描中登记」）

**核实结果（队长实测 `origin/main`）——派单件「2,503 行零生产引用」的说法需修正：**

| 事实 | 证据 |
|---|---|
| 静态 `from '@synova/evolution'` 在 `src/`（排除测试）**命中 0** | 派单件原话成立（字面） |
| **但动态 `import('@synova/evolution')` 已有 4 处生产调用点** | `src/l3/synova-diagnosis-engine-impl.ts:238`（`new SessionLearner()` + `:266` `getWeight` + `:280` `endSession`）、`src/agent/post-diagnosis-processor.ts:247`（`new OrgAdapter`）、`src/routes/evolution.ts:23`（`loadL0()`）、`src/sentinel/runner.ts:1176` |
| ~~**真实缺口 = `feedback-collector` 零消费方**~~ ⟵ **本行已被 E1 勘误推翻（见下）** | ~~`collectFeedback` 全仓零调用~~ **错** |
| **[修正·E1] `collectFeedback` 生产调用点 = 2 处，非 0** | `src/routes/chat.ts:70-71`（`await import('@synova/evolution')` + `collectFeedback({...})`，活路径 `POST /api/proposal/:id/resolve`，`chatRoutes` 已挂载 `src/server.ts:28` + `:367`）；`src/routes/evolution.ts:210`（`collectAllFeedback()`） |
| **[修正·E1] 真实缺口 = 调了不落库 / 落库没人读 / 下游读不到** | ① `chat.ts:71` **未传第 2 参 `memoryStore`** ⇒ `feedback-collector.ts:55` 走不到 ⇒ `persisted=false`，只落 `:42` 模块级内存 Map（进程重启即失）② `chat.ts:76` `.catch(() => {})` **空吞** + `:77 log.debug` **无 degraded 传播**（铁律 24 违规）③ `getFeedbackByAction`(`:66`)/`getFeedbackByOrg`(`:70`) 在 `src/` **零消费** ⇒ 结果不可查 ④ 下游消费者 `packages/evolution/src/org-adapter.ts:201-202,277-278` 读 `type:'enterprise_fact' + tags:['user_correction']`，而 `grep -rn "user_correction" src/` = **零命中**（无生产者）⇒ **org-adapter 恒空转** ⑤ `src/routes/evolution.ts:210` `collectAllFeedback()` **零参调用** ⇒ 全分支进不去 ⇒ 恒 `events: []` **假 200** |
| `RuleVersionManager` **已接线** | `src/routes/evolution.ts:93,101`（`new RuleVersionManager(...)`） |
| 同型陷阱：`src/l3/ga-collaboration.ts:62 setFeedbackCollector` 是**死缝** | 全仓零调用者；且 `src/routes/ga-calibration.ts:13` 注释明示「禁 ga-collaboration 死链 GAFeedbackHandler」 |
| 同名混淆（派单件已警示） | `src/growth/feedback-collector.ts` **已接线**（`src/routes/workspace-data.ts:141`、`src/routes/ga-calibration.ts:203-`、`src/agent/loop-handlers.ts:377`）——**不是**本卡对象 |

→ **C‑D829 判定**：本卡**不是**「整包 2,503 行零引用」，而是**线 17‑1（`feedback-collector` 有真实消费方）**。目标收窄为：把 `@synova/evolution` 的 `collectFeedback` 接到**真实业务路径**上。候选人=**`src/routes/ga-calibration.ts`**（GA 纠错回流的活单源，即「老板说『你判错了』」真正落地点），与 A/B/波2 **零交集**。
→ **边界**：C 若在规格阶段实证该点是错的，可提出**另一个**接线点，但**必须报队长批**且**不得落在** `src/store/**`、`src/l1/**`、`src/l4/**`、`src/init/**`、`src/routes/sessions.ts`（这些是 A/B/波2 的写集）。

---

## 五、波次与串行点（写集互斥的执行序）

```
波1（并行，三写集逐文件不相交）
  A：D820 ──串行──▶ D822        （同碰 src/store/session-store.ts，同一人）
  B：D823                        （src/l1/）
  C：D829                        （packages/evolution/ + src/routes/ga-calibration.ts）

        ▼ 等 A 完全释放 src/store/**

波2
  D826（src/store/session-store.ts + src/routes/sessions.ts）
  D827（src/init/engine-context.ts + src/store/session-projection.ts）
  D828（src/l4/temporal-baseline.ts + src/sentinel/**）
```

**串行点（不得颠倒）**：① D820 → D822（同文件同人）② A 全部完成 → 波2 的 D826（同文件）③ D826 与 D827 虽同碰 `src/store/` 但**不同文件**，可并行（若 B/C 有余量）。

---

## 六、开工前拦截清单（本次扫描实际产出）

1. **修正一处派单前提**：D829「2,503 行零生产引用」不成立（动态 import 已接 4 处）→ 目标收窄为 `feedback-collector` 零消费方（线 17‑1）。**若不修正，C 会去"接一个已经接了一半的包"，产出无法对齐线 17‑1 的验收点。**
2. **登记 C 的接线点**（派单 §二 要求）：`src/routes/ga-calibration.ts`，与全队零交集。
3. **隔离 6 条陈旧分支**：禁止 merge/cherry-pick（§三），否则引入 386–2527 commit 回退。
4. **39 张在飞 PR 零重叠**（证据3）→ 本队不受 PR 队列影响。

---

## 七、签核

| 项 | 值 |
|---|---|
| 扫描人 | 队长（并行 CTO session） |
| 扫描时间 | 2026-09-20 |
| 基线 | `origin/main` @ `4f3bb21c`；工作树基线 `d527abcb` |
| 结论 | ✅ 可开工（1 处派单前提已修正并登记；写集两两不相交） |
