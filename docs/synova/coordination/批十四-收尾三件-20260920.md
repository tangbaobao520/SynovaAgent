# 批十四 · 小队收尾三件（M6 交付）

> 队长：**并行 CTO session** ｜ 2026-09-20 ｜ 工作树 `.synova-wt-team14` ｜ 分支 `team/win-batch14`
> 依据：`docs/synova/coordination/小队派单-批十四-win并行-20260920.md` §三⑦/§五 + 《小队模式-固化件-v1》M1–M6
> 三件 = **① 最终 diff ② 自验结论 ③ 遗留清单**（本节即三件的载体；自验结论以自验队员署名的独立文件为准，本件只引用不改写）

---

## 〇、一句话结论

**波1 四张卡（D820 / D822 / D823 / D829）+ 1 张卡外阻塞缺陷修复（FTS 删除触发器）全部交付、提交、独立自验通过、无退回。**
**波2 三张全部未在本队执行**：`D826` 进行中（唯一在做）、`D827` **移交**（卡面写集不足以达成其自身验收点）、`D828` **移交**（跨域结构性阻塞）。移交均附已核实资产，接手方不需重查。

---

## 一、队伍与隔离（M1）

| 项 | 值 |
|---|---|
| 编制 | 编码 3（A/B/C）＋ 自验 1（**未由编码兼任**）＋ 审计 K3 **队外不入队** ✅ |
| 专用工作树 | `.synova-wt-team14`（新建；**未复用任何在飞 worktree**） |
| 分支 | `team/win-batch14`（基线 `d527abcb` = `origin/main` 派单件合并态） |
| 开工前冲突扫描 | [`批十四-开工前冲突扫描-20260920.md`](批十四-开工前冲突扫描-20260920.md)（含 3 处**对本件自身的勘误** E1/E2/E3，见 §七） |
| 证据链 | 未复用他人 worktree；`git worktree list` 遍历 + 39 张在飞 PR 交叉核 = 零重叠 |

**M1 漏洞（本批新发现，需回灌固化件）**：四个 session 共享同一工作树 ⇒ **也共享同一个 git index**（`.git/worktrees/-synova-wt-team14/index`）。**"一队一工作树"只隔离了工作目录，没隔离索引** ⇒ 编辑可并行，**提交必须串行**。本批由编码 C 独立发现、编码 B 独立撞上（一次反向验收因 `git checkout` 撞 `index.lock` 产生**假绿**，B 自查作废重做）。处置：队长担任提交调度（显式队列）＋ 反向验收禁用 git 还原动作（改 `cp` + sha256）。

---

## 二、完成判据逐条对照（派单件 §五）

| # | 判据 | 达成 | 证据 |
|---|---|---|---|
| 1 | D820/D822/D823/D829 全部进 `impl_done`，每张有链路证据 + 命令 + 原始输出 + 反向验收两次输出 | ✅ 4/4 | 各卡 `task-state/D8xx.json` + `docs/synova/product-lines/evidence/D8xx-evidence-20260920.md` |
| 2 | 波2 三张「按同标准推进**或明确移交下一队（附理由）**」 | ⚠️ 部分：D826 推进中；**D827/D828 移交并附理由 + 已核实资产** | board `task-5`/`task-6`/`task-7`；§四 |
| 3 | 队长收尾三件 + 归属收口完成；PR 已开（≤12 文件/PR，超了拆） | ⏳ 三件 = 本件；归属收口 = §六；**PR 待开（见 §八 待办）** | — |
| 4 | 队内自验结论已写；K3 抽审提请已登记 | ✅ 结论已写（四卡 `可提请独立审计`）；K3 提请见 §八 | [`批十四-自验结论-20260920.md`](批十四-自验结论-20260920.md) |

> **诚实标注**：判据 3 的「PR 已开」在本件落盘时**尚未完成**——本队交付物需先经一次合并簿记提交（把 untracked 证据件入仓 + 四卡 `impl.sha` 回填），再开 PR。见 §八。

---

## 三、① 最终 diff（相对 `merge-base(origin/main, HEAD)`）

**30 文件 / +3407 −130**（含 hook 自动登记的 `.claude/bypass.log` 与四态 Note）

### 生产代码（6 文件）

| 文件 | 卡 | 改动要点 |
|---|---|---|
| `src/store/session-store.ts` | D820 / D822 + FTS 折入 | FTS5 删除触发器改普通 `DELETE ... WHERE rowid=old.id`（插入触发器显式落 `rowid≡agent_messages.id`）+ 幂等迁移与一次性索引对齐；`diagnosis_checkpoints` 加 `origin_session_id`（幂等 ALTER）+ `deleteSession` 接级联 + `listDiagnosisReports` 加会话存在性谓词；`addMessage` 透传 `AppendEventResult` + 新增 `appendFailureCount`（只增不减） |
| `src/routes/conversations.ts` | D820 / D822 | 写链记 `originSessionId`；判定由「最后一次写」改为「**本轮是否发生过失败**」 |
| `src/l1/im-inbound.ts` | D823 | 删假恢复块；新增会话注册表（`IM_SESSION_MAX=200` + FIFO 逐出 + 清理器 + 每 userId 单飞链）；**清 5 处类型逃逸** |
| `src/routes/ga-calibration.ts` | D829 | L0 收集块（GA 纠错三动作 201 路径必经，传真 `AgentMemoryStore`）+ 读取出口 `GET /api/ga/calibration/evolution-feedback`（org 隔离 fail-closed） |
| `packages/evolution/src/feedback-collector.ts` | D829 | `nextFeedbackId()`（同毫秒 id 相撞修复）+ `collectGaCalibrationFeedback()`（含 JSDoc 契约） |
| `packages/evolution/src/index.ts` | D829 | 新函数/类型导出 |

### 测试（9 文件：3 新建 + 2 新建专项 + 4 存量改写/扩充）

新建：`tests/store/delete-session-cascade.test.ts`(8) ｜ `tests/store/degraded-signal-not-masked.test.ts`(3) ｜ `tests/store/fts-sync-trigger.test.ts`(5 断言) ｜ `tests/l1/im-inbound-history.test.ts`(6) ｜ `tests/routes/evolution-rule-version.test.ts`(2)
改写/扩充：`tests/store/session-event-log.test.ts`（**把固化缺陷的用例改成保护正确行为**）｜ `tests/evolution/feedback-collector.test.ts`（**改掉一条靠缺陷才通过的假绿用例**）｜ `tests/routes/ga-calibration.test.ts` ｜ `tests/store/…`

### 提交清单（非 merge，PR 范围内 11 个）

```
98c24872 fix(D820) FTS5 删除触发器改普通 DELETE + rowid 对齐（卡外阻塞缺陷，经队长授权折入本批）
3bafa7d5 feat(D829) GA 纠错接 L0 进化收集器（线 17-1 有真实消费方）
8e89f357 fix(D820) 删会话级联清诊断报告 + 报告列表会话存在性谓词
3a0d5b5c fix(D823) IM 通道会话历史连续 — 会话级引擎复用 + 清 5 处类型逃逸
66d1638e fix(D822) 降级信号不再被同轮后一次成功抹掉（改为逐条结果 + 累计入账）
8de56ed2 chore 补记（队长治理动作，见 §八）
+ 5× chore: bypass COMMITTED 登记 (auto hook)
```

> **卡外折入项披露**：`98c24872` 不是任何卡的验收内容，是 D820 的**硬前置**（不修则卡面「建会话→落报告→删会话」在真实入口上**物理走不通**：`DELETE /api/sessions/:id` 对任何有消息的会话都 500）。依《审计闭环铁律》D382 折入例外（同领域 + 进行中任务 + 改动小 + 标注）**折入并单独 commit**，供 K3 独立审。**未**采用「另开 FIX 卡」：拆开会让 D820 卡在假依赖上、拖住整批。

---

## 四、② 自验结论（引用，不改写）

**独立自验件（自验队员署名）**：[`批十四-自验结论-20260920.md`](批十四-自验结论-20260920.md)
**逐卡证据（M5 按任务号命名）**：`docs/synova/product-lines/evidence/D820-verify-20260920.json`、`D822-verify-…`、`D823-verify-…`、`D829-verify-…`

| 卡 | 核的 SHA | 自验结论 | 自验队员**亲自复现**的反向验收 |
|---|---|---|---|
| D820 | `8e89f357` | `可提请独立审计` | 抽级联半边 → 4 failed \| 4 passed；抽谓词半边 → 1 failed \| 7 passed（**仅用例 8 红**）；`-t "用例 [1-7]"` → 7 passed \| 1 skipped |
| D822 | `66d1638e` | `可提请独立审计` | 抽路由判定 → 1 failed \| 19 passed（**用例 2/3 仍绿 = 不靠误报取胜**）；抽累计计数 → 唯一红 = 改写后的 store 用例 |
| D823 | `3a0d5b5c` | `可提请独立审计` | 置入修复前实现 → 3 failed \| 3 passed |
| D829 | `3bafa7d5` | `可提请独立审计` | 抽 L0 收集调用 → 5 failed \| 2 passed；还原旧 id 实现 → 唯一红 = 同毫秒相撞用例 |
| FTS 折入 | `98c24872` | `可提请独立审计`（**单列，未并入 D820 叙述**） | 独立复刻基线 DDL/触发器 → 复现 `SQL logic error` + 已删内容仍可召回；修复后迁移/幂等/隐私面自测全过 |

**未覆盖面（不得读作已完成）**：波2（D826 进行中；D827/D828 已移交）、**客户侧验证**（产品级，不在本队范围）、`tests/routes/llm-config.test.ts` 并行超时问题。

**队长复核口径**：本人**未**对四卡做独立技术复核（那是自验队员的职责，且 M3 禁止自我审计）；本人只核**闸口级事实**：提交存在且 hash 正确、commit 内容边界（FTS commit 零 `origin_session_id` 污染）、写集未越界、**零绕过**（`2026-09-20` 全天 `detected-bypass`/`possible-bypass`/`ALLOW_MAIN_PUSH` **= 0**）。

---

## 五、③ 遗留清单

### 5.1 移交下一队（2 张卡，附已核实资产）

| 卡 | 移交理由（结构性） | 资产（已核实，接手方不需重查） |
|---|---|---|
| **D827** | 卡面 **3/3 验收点全部需要写集外管道** ⇒ 写集不足以达成自身验收点 | ①`agent_metrics` 真列定义无 `org_id`/`value`（`src/init/engine-context.ts:99-108`）⇒ 验收 1 与《验收标准》§六 存储类样板**逐字相同 = 样板污染** ②`llmMs`/`decodeTokens` 全仓不存在（`TokenMeter` 仅 consult 路线内存态）⇒ 验收 3 需新增 per-session 计量 + 持久化 ③`diagnosis_checkpoints` **存在**，缺的是**投影 checkpoint 持久表** ④DSH `dsh-session-stats` 实测路径与 6 处行号。详见 board `task-6` |
| **D828** | 卡面自身要求 **win/mac 拆两个 PR**，哨兵侧 `src/sentinel/**` 不在本队写集 ⇒ **本队无法产出穿入口级证据**；只做 win 侧 = 产出「写了没接」 | `computeTemporalBaseline` 定义唯一命中 `src/l4/temporal-baseline.ts:31`、生产零调用（**与卡面一致、无漂移**）；DSH 借鉴 = ③自建。详见 board `task-7` |

### 5.2 已登记台账的漏洞（不在本队任何写集，一律**另开卡**）

| # | 位置 | 内容 | 级别 |
|---|---|---|---|
| L1 | `src/agent/conversation-engine.ts:444` vs `:844` | **L2 缺陷**：构造器按引用注入 `messages`，`fromState` 赋值换数组 ⇒ 恢复后 toolLoop 引用脱钩，provider 只收到 1 条 system（**连本轮用户消息都进不去**）。仓库自认（`conversations.ts:58-63` 决策记录 + `tests/e2e/conversation-flow.e2e.test.ts:527-530` 划归 Stage 3）。同伤 `src/routes/conversations.ts:233` 进程重启路径 | **P0** |
| L2 | `src/store/session-store.ts:332-341` | `listSessions` 是 `SELECT s.*` 但 `.map()` 丢弃 `user_id` ⇒ `s.userId` 恒 undefined ⇒ 跨重启会话归位在 L5 层不可表达 | P1 |
| L3 | `src/routes/chat.ts:70-76` | `collectFeedback` 不传 `memoryStore` ⇒ `persisted=false`（只落模块内存 Map）；`.catch(() => {})` **空吞** + 无 degraded 传播（铁律 24） | P1 |
| L4 | `src/routes/evolution.ts:210` | `collectAllFeedback()` **零参调用** ⇒ 全分支进不去 ⇒ 恒 `events: []` **假 200** | P1 |
| L5 | `src/agent/diagnosis-launcher.ts:162` | `saveCheckpoint` **只定义零调用**（死函数）⇒ 生产不产生 phase<5 检查点行 | P2 |
| L6 | `tests/evolution/global-analyzer.test.ts:74`（+`:46/:54` 经 `src/../global-analyzer.ts:141,155`） | 测试**写被 git 跟踪的仓库文件** `extensions/industries/**/thresholds.json` ⇒ 提交态漂移（D817 / M5 同型） | P1 |
| L7 | `tests/routes/llm-config.test.ts` | 并行负载下**整文件超时**（`tests/store`+`tests/routes` 连跑可复现）⇒ 测试隔离/超时预算隐患。**注意**：四人共享工作树下"谁压垮谁"难判定，登记时不得断言单一肇因 | P2 |
| L8 | `tests/routes/conversations.test.ts:285` | 用例② 标题声称"loadState→fromState 生效"，但同进程同 sessionId 两次请求命中 `sessionEngines` 缓存（`conversations.ts:224`）⇒ **fromState 从未被该用例覆盖**（与 L1 同源） | P2 |

### 5.3 🔺 升级创始人（红线条目）

| # | 事项 | 为什么升级 |
|---|---|---|
| **E-1** | **控制塔门禁伪红**：`scripts/pre-commit-check.sh:881` `[ ${#LAYER_FILLED} -lt 3 ]` 对**合法裸 2 字符层值**（`L1`…`L5`）判「未填写」→ **硬阻断**。而 `CLAUDE.md:363` **白纸黑字声称** V4.2.7 已修「"L3"过短(2字符)导致6核心字段误阻断」；`git log -S` 显示该 `-lt 3` 自 `3343916e` 起一直在，历经 `b8e1eb20`(D520)/`503d04ca`(D516) 未动 | **M7 文档-实现漂移 + 同类第二次出现** ⇒ 按红线「同类错误第二次出现 = 防线系统性失效」升级。实际影响面窄（`brief_parser.py --layer` 返回整行值时可通过，仅裸值被拦），但**伪红正是把 agent 逼向 `--no-verify` 的经典路径**（V4.5.1 pre-commit 122s 危机的根因）。本批 **2 名编码独立撞上**、队长独立复现。**`scripts/**` 不在本队写集，未修** |
| **E-2** | **鉴权模型层漏洞**：`src/middleware/auth.ts:282-284` 白名单**先于** Authorization 解析早退 ⇒ 白名单路由（含 `/api/sessions`、`/api/conversations`、`/api/diagnosis/consult`、`/api/diagnosis/reports`、`/api/ga/*`）上 `req.auth` **恒空**，即使带合法 JWT。而 `extractAuthFromRequest:401-402` 的第二优先级是**无签名** `x-synova-token: <role>:<orgId>:<userId>`，`orgId` 直接取值 ⇒ **本机调用方可自称任意租户** | 这是 D590（创始人裁决①「单机本地信任＝信任边界是机器而非凭证」）在多租户阶段失效。D826 卡面锚点正是「**第 2 个客户接入前必须修**」⇒ 到达锚点前必须先解决身份信任根。**不在本队写集（属鉴权模型 + 创始人裁定），未修**。由编码 B 在 D826 实现中实测发现，**队长已独立复核 `:282-284` 与 `:401-402`** |

### 5.4 机制回灌建议（喂给《小队模式-固化件》下一版，本队不改该件）

| # | 缺口/建议 | 本批实证 |
|---|---|---|
| R1 | **M1 补：同工作树内提交串行化**（派单时给提交窗口/队列）—— "一队一工作树"未覆盖 git index | 编码 C 发现；编码 B 因 `git checkout` 撞 `index.lock` 产生**假绿**并自查作废 |
| R2 | **M2 补：写集登记必须机器可核**，且覆盖 `tests/` 维度 —— 本队队长**两次**因登记偏松被自验队员提醒补登（C 的测试文件、B 的证据文件） | 自验队员两次指出"实物与授权不一致" |
| R3 | **验收补：每一半修复都必须有一条能单独变红的断言** | D820 抽掉读侧谓词**首次全绿**（A 自查发现并补用例 8）；D822 两个半边各自可红 |
| R4 | **验收补：缺陷态仍绿的用例不具反向判别力，必须在结论里显式标注，不得计入防护强度** | D823 六条用例里 **3 条**属此列（自验队员主动标注） |
| R5 | **反向验收禁止依赖 git 还原动作**（共享树下会撞 index）；改用 `cp` + sha256 前后留证 + `git diff HEAD` 为空 | B 的假绿事故；自验队员全程采用此姿势 |
| R6 | **派单生成流程需加"卡面前提实测"闸**：本批 **4 次**"派单前提需修正"（D829 前提不成立 / D826 空判据 + 行号漂移 / D827 样板污染 + 口径不可得 / D828 跨域不可达） | §五 各条；均由执行方在**写码前**拦下 |
| R7 | **反向验收后清 `node_modules/.vite`（预防性，根因未证实）** | 编码 A 报"缓存致假红"，**队长受控实验未能复现**（注入改动被立即观察到、还原 sha256 一致、17 passed）⇒ **未写成已证实根因**，只作为廉价预防动作 |

---

## 六、归属收口（M6）

**四张卡全部 `impl_done` + 队内自验通过。** 依《小队模式-固化件-v1》M6「官方不自动释放 owner」，由队长逐张收口：

| 卡 | 状态 | 归属 | 证据 |
|---|---|---|---|
| D820 | `impl_done` | 已收口（board `task-1` completed） | `8e89f357` + `98c24872`（折入） |
| D822 | `impl_done` | 已收口（board `task-1` completed） | `66d1638e` |
| D823 | `impl_done` | 已收口（board `task-2` completed） | `3a0d5b5c` |
| D829 | `impl_done` | 已收口（board `task-3` completed） | `3bafa7d5` |
| D826 | 波2 进行中 | board `task-5`（编码 B 持有） | 未提交 |
| D827 | **移交下一队** | board `task-6`（**无 owner**，移交态 + 资产） | — |
| D828 | **移交下一队** | board `task-7`（**无 owner**，移交态 + 理由） | — |

**归属释放命令（机制侧）**：本批执行时 `scripts/control-tower/claim_release.py` **尚未并入 main**（属在飞 D839 / PR #657），故未能使用该工具；释放以 `task-state/<D#>.json` 的 `status` 显式声明 + board 状态为准（与 `claim_release.py` 的 `task-state:<st>` 自动释放路径同源）。**待 D839 并入后，本批四张卡应被 `scan` 自动判为已释放。**

---

## 七、队长自身的错误记录（本件诚实留痕）

《开工前冲突扫描》**曾出错 3 处，全部由队员独立抓出**：

| # | 错误 | 根因 | 谁抓出 |
|---|---|---|---|
| E1 | 断言「`collectFeedback` 全仓零调用」 | grep 管道接了 `head -10`，输出被**截断在第 10 行**，`src/routes/chat.ts:70`（字母序靠后）被静默丢弃；**并因此漏掉** `src/routes/evolution.ts:210` | 自验队员 + 编码 C（各自独立） |
| E2 | 写集扫描**漏 `tests/` 维度** | 我按「谁会改产品代码」建维度，未按「谁会改同一批文件」建维度 | 编码 C 请求纳入存量测试时暴露 |
| E3 | 把 `evidence/` 登记为"队长队外产物" | 与派单 §三⑥「证据按任务号命名落 evidence/」冲突（**M4：指令≠工具副作用**） | 编码 A 据此**没有** stage 自己的证据件 |

**教训**：三条都不是判断错，而是**证据采集面不完整**（`head` 截断 / 维度缺失 / 与派单件冲突未核）。已在扫描件内以 §〇 勘误节留痕。
**另一处（我作为审核方的失手）**：我两次给出的 org 权威链指令都**不成立**——第一次漏了白名单早退（会让"唯一权威"落到无签名 header），第二次的"无条件 401"会破坏 D590 创始人裁决。**两次都由编码 B 在写码前拦下**，我独立复核后确认其对、我错。→ 说明**派单/指令同样需要"写后代执行方复核"**（R6 的另一面）。

---

## 八、待办（未完成，不许读作已交付）

| # | 事项 | 归属 |
|---|---|---|
| 1 | **合并簿记提交**：把 untracked 证据件（`D820/D822-evidence`、`D820/D822/D823/D829-verify`、冲突扫描、自验结论、自验计划）入仓 + 四卡 `impl.sha` 回填（D820→`8e89f357`、D822→`66d1638e`、D823→`3a0d5b5c`、D829→`3bafa7d5`） | 队长 |
| 2 | **开 PR**（对照 `origin/main`）；按 **≤12 文件/PR** 复核是否需要拆分（本批 30 文件含测试与证据） | 队长 |
| 3 | **K3 抽审提请**：本案 `可提请独立审计` 四卡 + FTS 折入项（**须单列**，不并入 D820） | 队长 → K3 |
| 4 | **台账登记**：§5.2 的 L1–L8 + §5.3 的 E-1/E-2 + §5.4 的 R1–R7 | 队长 |
| 5 | 波2：D826 收尾（编码 B）；D827/D828 另派（资产已备） | 队长 |

---

## 九、红线自查

| 红线 | 状态 |
|---|---|
| 不碰 `scripts/audit/**` | ✅ 全批零改动（`git diff --stat` 无 `scripts/audit/` 项） |
| 不写审计标准 / 不自我审计 | ✅ K3 队外；队内一律「自验」；自验结论只出现 `可提请独立审计` / `退回（附理由）` 两种措辞 |
| 禁 `--no-verify` | ✅ `2026-09-20` 全天 `detected-bypass`/`possible-bypass` **= 0**（自验队员独立扫描） |
| 禁 `git stash` | ✅ 全批零使用（FTS 与 D820 的 hunk 拆分改用 `git apply --cached --unidiff-zero` + dry-run） |
| 禁 force push | ✅ 零使用；分支线性推进 |
| 隔离工作树放 `.synova-wt-*` | ✅ `.synova-wt-team14` |
| 不写产品代码（队长自身） | ✅ 队长只做：工作树/冲突扫描/写集裁定/规格批审/提交调度/治理动作（`8de56ed2` 补记 + 一次受控实验后**已完全还原** `src/store/session-store.ts`，sha256 一致、`git diff HEAD` 为空）/收尾三件 |
| 同类错误第二次出现 = 升级 | ✅ E-1（控制塔 `-lt 3`）已按此升级 |
