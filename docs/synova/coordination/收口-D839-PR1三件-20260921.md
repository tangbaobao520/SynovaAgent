# D839 PR-1 收口三件（M6）— 认领制根治本体

> 依据：裁决 `docs/synova/coordination/裁决-657拆两台PR-B口径-20260921.md`（创始人 2026-09-21 批准，走 B）。
> 本台 = #657 拆分后的 **PR-1｜D839 本体（认领制根治）**，与 PR-2（D849/D853 Windows 收口）互不含对方文件。
> 收口人：DeepSeek Harness（CTO session），2026-09-21。

## 一、最终 diff（10 文件，≤12 达标）

基线：`origin/main`（cc4e4166）。来源：`fix/d839-claim-release`（96e40079）按裁决清单摘取 + 恢复被移出的决策 Note（69e4ebcf^）。

| # | 文件 | 变更 |
|---|------|------|
| 1 | `scripts/control-tower/claim_release.py` | 新建 499 行：释放语义单一事实源（is_released/scan/release/release-stale） |
| 2 | `scripts/control-tower/staging_guard.py` | +247：认领制段加释放维度（已释放→warn+release_reason；block 附 release_cmd） |
| 3 | `scripts/workflow/resolve-commit-brief.sh` | 候选池剔除已释放 brief（含 D853 根因修复：PYBIN 试运行探测/ROOT_W/env 清洗） |
| 4 | `tests/control-tower/claim_release.test.sh` | 新建 219 行配对夹具 |
| 5 | `tests/control-tower/staging_guard.test.sh` | 新建/扩至 470 行七场景夹具（A-G） |
| 6 | `task-state/D839.json` | 状态机回填 |
| 7 | `task-state/claim-releases.json` | 新建：显式释放台账 |
| 8 | `memory/notes/proposed/2026-09-20-d839-claim-release.md` | 铁律 49 决策沉淀（从 69e4ebcf^ 恢复） |
| 9 | `.claude/task-briefs/2026-09-20-D839-认领制根治-…-释放持久化.md` | D839 brief（Q2 覆盖上表全部文件） |
| 10 | `.github/workflows/ci.yml` | K3 D841 P1-2：两个新夹具接入 CI（ci.yml 单写者=本台） |

注意：`scripts/control-tower/synova-commit` 与其夹具**不在本台**（按裁决归 PR-2；合并顺序先 PR-1 后 PR-2）。

## 二、自验结论（判别性先红后绿，两次原始输出）

红态 = `origin/main`（cc4e4166）+ 仅两个新夹具（实现不在）：

```
$ bash tests/control-tower/claim_release.test.sh
❌ 缺 scripts/control-tower/claim_release.py（未实现）

$ bash tests/control-tower/staging_guard.test.sh
  场景E(批量)       : FAIL
  场景F(沙箱围栏)   : PASS
  场景G(降级可见)   : FAIL
  PASS=9 FAIL=15
FAIL=15
```

绿态 = 本分支（10 文件齐）：

```
$ bash tests/control-tower/claim_release.test.sh
  PASS=33 FAIL=0
FAIL=0

$ bash tests/control-tower/staging_guard.test.sh
  PASS=43 FAIL=0
FAIL=0
```

判别性成立：同一夹具，仅实现差异即红↔绿翻转（claim_release 33 断言、staging_guard 43 断言全绿）。
完整原始输出见 PR 描述附录。ls-remote 回执见 PR 描述（推送后 SHA 与远端一致方算交付）。

## 三、遗留清单

1. `synova-commit` 的 block 文案改打印可执行释放命令 —— 归 **PR-2**（依赖本台 guard 语义），本台不携带。
2. K3 D841 CONDITIONAL PASS 的其余条件项（P1-2 已由本台 ci.yml 补齐；其余见 `docs/synova/audit-reports/2026-09-20-K3-D839.md`）。
3. 治本（两 brief 互斥的规则级解）→ D860（裁决 C，K3 复审），不在本台。
4. ci.yml 释放后 #682 与 D863 排队（合并顺序：本台 → PR-2 → #682/D863）。
