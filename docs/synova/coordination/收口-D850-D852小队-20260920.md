# 收口 · D850 / D852 小队（2026-09-20）

> 队长：并行 CTO session ｜ 编制：**编码 A（D850）＋ 编码 B（D852）＋ 自验 1（独立，不得由编码兼任）** ｜ 审计：**K3 队外**（队内只说「自验」）
> 派单件：`docs/synova/coordination/派单-CT70与D850-D852小队-20260920.md` §B（随 PR #678 已合 main，2026-09-20）
> 专用工作树：`.synova-wt-d850`（`team/d850-discrete-health`）｜ `.synova-wt-d852`（`team/d852-product-decisions`）｜队长收口树 `.synova-wt-lead-d850d852`
> 判定口径：`docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md` **§十一（三态：通过 / 退回 / 关闭；「有条件通过」= 未通过）**——创始人 2026-09-20 裁决，随 PR #679 合 main

---

## 一、一句话现状

**D852 = 通过（已合 main `25082884`）；D850 = 两轮退回后闭环、已合 main `cb1bbcaf`；「取消整体完成度百分数」在本仓度量链已落地，但控制塔域两处面板仍渲染「总完成度」→ 只落一半（台账 CT-74）；「缺」档计数仍呈创始人裁定（度量基建缺源）。**

| 件 | 结论 | 载体 | 状态 |
|---|---|---|---|
| **D852** §6-5 四项产品决定落地 | **通过** | PR [#681](https://github.com/tangbaobao520/SynovaAgent/pull/681) → squash `25082884` | ✅ 已合 main |
| **D850** 度量口径改造 | **退回 ×2 → 闭环 → 通过** | PR [#683](https://github.com/tangbaobao520/SynovaAgent/pull/683) → squash `cb1bbcaf` | ✅ 已合 main |
| **「缺」档计数** | **未闭环 → 呈创始人裁定** | `buckets.missing = null + reason` | ⏳ 待裁定 |

**退回/复验时间线（每轮都有独立自验员原始输出，非作者自述）**

| 轮 | 触发 | 条件 | 闭环证据 |
|---|---|---|---|
| 1 | 自验员十条 ① 的**面板侧不通过** | V-1 面板 144/203 条 evidence_cmd 不可跑（`calc-progress.py:136-146` 点分路径当单键）＋ V-2 面板新契约无测试覆盖 | `3540c808`：203/203 rc=0 且值对账；新增 `tests/project/calc-progress-panel.test.sh`（12/0，RED 8/4） |
| 2 | 自验员对抗性验证 ⓐ | **`evidence_cmd` 从「源侧重算」被降级为「读回派生物」= 自证，不可证伪**（违反创始人签字「不能复现的数字，不上看板」；且作者上一版 `@contract` 自己写过"不读回派生物"→ 契约反转） | `c2b0b4ff`：件内 `cmd_semantics` 词义分离；一次派生 + 逐档 fresh↔committed 比对（缺重算件 **fail-closed**，不得静默回退）；线级补 `bound×28 + reconciliation×28` 独立判据；测试在无 `.git`/shallow 下 SKIP+原因；测试 20/0 |

---

## 二、§〇-c 权威件回执（引用必给全名＋版本；本机物理路径）

| 权威件（全名） | 版本 / 日期 | 本机落点 | 核实方式 |
|---|---|---|---|
| **产品完成度定义与推进总纲** | v1.0（正文）/ v1.1（修订记录）｜2026-09-18 | `docs/authority/产品完成度定义与推进总纲-20260918.md`（在 main） | §1.2 五态与「N 健康 / M 写了没接 / K 缺」原文逐字读过 |
| **创始人裁定表 · 总纲 §6 五项** | 2026-09-20（§6-1/2/3 已签；**§6-4/§6-5 仓内仍为 ☐ 未勾选**） | `docs/synova/coordination/创始人裁定表-§6五项-20260920.md`（4243 B，在 main） | 勾选状态实测见 §六-遗留 6 |
| **方案 · 项目度量：从「声明驱动」到「事实驱动」** | 研究院件（日期 2026-09-18；**未入仓**） | 仓库外 `/Users/wane/山河研究院/99-综合/方案-项目度量-从声明驱动到事实驱动.md` | §四「每个数字必须带一条可复现的命令」、§六「静态检测 3/5=60% 只能粗筛」原文读过 |
| **研究院交接 · 9 项撤回清单** | 2026-09-18（**未入仓**，PR #674/#675 在途） | 仓库外 `/Users/wane/山河研究院/99-综合/交接/可复制-DSH对比与任务书-20260918.md` **§二** | 9 条逐条读；本批相关 = #3（grep 型判据）/ #4（参数线）/ #9（26 线非完整清单） |
| **26 线 V1 验收标准** | **v0.2 已冻结**｜2026-09-17｜签字 sha256 前 8 位 `c11841e6` | `docs/synova/project/26线-V1验收标准-v0.2-20260917.md` | §〇 M1–M5 / §三 分母 128 / §六 增补路径 / 附录 A 列格式 |
| **验收标准（穿真实入口）** | v1.1｜2026-09-19（§十一 三态于 2026-09-20 增补） | `docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md` | §十一 三态逐字读过 |

**⚠️ 派单件 §〇-c 引用路径与本机实际不符（已登记）**：派单写「研究院件 ingest 后路径 `docs/synova/research/研究院交接-20260918/`」——该目录**尚不在 main**（PR #674/#675 在途）；派单另引「`docs/authority/研究院材料权威级别与引用纪律-20260920.md`」——**该文件在 main 与 PR #674/#675 两个分支上均不存在**（实测 `git ls-tree` 零命中）。故 9 项撤回清单的实际可读源 = 上表仓库外路径。

---

## 三、三件交付（索引）

| 件 | 文件 |
|---|---|
| ① 最终 diff | `docs/synova/coordination/D850-D852-改动清单-20260920.md` |
| ② 自验结论 | `docs/synova/coordination/D850-D852-自验记录-20260920.md` |
| ③ 遗留清单 + 归属逐张收口 | **本文 §六 / §七** |

---

## 四、逐张收口（M6：队长收尾，DSH 不自动释放归属）

| 队内任务 | owner | 状态 | 载体 | 判定 |
|---|---|---|---|---|
| task-1（D850） | `coder-a-d850` | **reopen → pending**（曾 completed，自验员发现 V-1/V-2 后退回） | PR #683 | **退回** |
| task-2（D852） | `coder-b-d852` | completed | PR #681 → `25082884` | **通过** |
| task-3（自验） | `verifier` | completed（十条 ＋ 六条对抗性验证） | 自验记录 | **可提请独立审计** |

**写集互斥核查（M2）**：A（`scripts/project/**`＋`scripts/product-lines/**`＋`tests/project/**`＋三个派生件）与 B（`docs/synova/product-lines/{product-lines,preconditions}.yaml`＋`docs/synova/coordination/**`）**两两不重叠**；实测两分支合并 main 零冲突。派生件的跨切片耦合（A 的 `ledger.json.backlog_points` 依赖 B 的 yaml `points_total`）由队长显式排序解掉：**先合 D852，再让 A 在新 main 上重生成派生件**（46 → 52）。

---

## 五、三态判定（派单 §B-5 验收 × §十一 口径）

### D852 —— **通过**
| 派单验收点 | 实测 |
|---|---|
| 线 8/4/3/25 各新增验收点（写实到可证伪） | 6 条新增（3-9/4-9/8-7/8-8/25-8/25-9），**只加验收点、不新增线**（`lines` 仍 28）｜`git diff --numstat` = `30 增 / 0 删` |
| live/restart 清单成文 | `docs/synova/coordination/D852-live-restart-清单-20260920.md`（四类归属＋判定边界＋7 类可复现核验） |
| 四项决定逐字对得上 | ① 8-7/8-8 ② 4-9 含 `8,000`/`32,000`/`可配置` ③ 3-9 含日粒度＋周/月＋**D828** 依赖 ④ 25-8 live / 25-9 restart —— 自验员逐字核过 |
| 门禁 | `pre-commit-check.sh` 13 组全过；PR 预算 9 文件 / 单域 mac；CI 12/12 success |
| 先红后绿 | 两工作树终态 `git status --short` 为空；无 `--no-verify`、无 stash |

### D850 —— **退回（附理由，条件见下）**
通过项：三档结构互斥完备（自验员自写重算器复算 `healthy=16 / written_not_wired=3 / live_unverified=11 / state_unknown=98`，与 ledger 逐断言字段一致）；反向验证真实变数（破坏 k3 裁决 → `16→15` → 还原 → `16`）；面板 `total_lines=29`（自核 yaml 28＋`v2_lines` 1）；HTML 重生成**逐字节相同**；签字判据「禁百分比」grep 6 文件**零命中**；`tests/project/gen-project-board.test.sh` 独立重跑 **PASS=117 FAIL=0**，CT-67 三处 live 硬编码全清；门禁 13 组过、预算 11≤12 单域 mac。

**退回条件（两轮，均已闭环）**：
- **V-1（第一轮，阻塞）**：**面板侧 `evidence_cmd` 144 / 203 条不可跑**（顶层 4/7、线级 140/196）。根因 `scripts/product-lines/calc-progress.py:136-146 rerun_evidence_cmd()` 把**点分路径当单键**（生成 `d['buckets']['other_states.live_unverified']['count']` → `KeyError`），叠加 `:215 pre = "lines[].buckets."`。**队长已独立复现**：命令原文执行 `rc=1 KeyError: 'other_states.live_unverified'`。违反创始人签字的「不能复现的数字，不上看板」。→ 闭环：`3540c808`，203/203 rc=0 且值对账一致（并自查出退回单未点名的**第三子缺陷：57/203 值不匹配**）。
- **V-2（第一轮，阻塞，V-1 的成因）**：**面板新契约无测试覆盖**——`grep -rl "buckets\|evidence_cmd" tests/control-tower/*.py` 零命中，`gen-project-board.test.sh` 组⑩ 只对账 ledger 侧 → 117/0 全绿仍漏 V-1。→ 闭环：新增 `tests/project/calc-progress-panel.test.sh`（密封、可进 CI、12/0 `[工作树]`）。
- **ⓐ（第二轮，阻塞）**：修 V-1/V-2 时作者把 `evidence_cmd` **改成「读回已提交派生物」**（`d['buckets'][...]['count']`），并自述理由为性能（单次派生 1.42 s × 203 ≈ 5 min）。独立自验员判定并获队长采纳：**这是自证，不是复现** —— 读回时源侧错它也错，第三方跑**不可能得出与看板不同的结论**，不满足事实驱动 §四「每个数字必须带一条可复现的命令。不能复现的数字，不上看板」＋结句「你能自己跑一遍验证——这就是可证伪，也就是信任的来源」；且作者上一版 `@contract` 自写「不读回派生物」→ **契约反转**（定性为需队长裁量的设计变更，非违约）。**队长的理由**：面板上挂 203 条不可证伪的命令，比没有命令更坏（假证据）。→ 闭环：`c2b0b4ff` 走ⓓ 五条最小修法（词义分离 `cmd_semantics`／件级一次派生＋逐档 fail-closed 比对／线级补独立判据／披露复现强度／测试在无 git 下 SKIP）。
  **附带更正（V-12）**：作者的「1.42 s」本机不可复现——10 次原始读数 **0.71–12.68 s（强负载相关）** → 203 档 **2.4~43 分钟**（非 ~5 分钟）；方向对、量级偏乐观。

**归属**：V-1/V-2 → 编码 A（PR #683 内修）。**「缺」档 null 不算 A 的缺陷**（task-1 卡明文授权「无法判定者显式 null＋原因，禁猜 0」，A 合规）→ 归属**派单口径＋度量基建缺源**，呈创始人裁定（§七）。

---

## 六、遗留清单 ＋ 归属逐张收口

> 每条：**证据（file:line）→ 归属 → 是否阻塞**。红线：发现即登记，不问创始人。

| # | 遗留 | 证据（物理） | 归属 | 阻塞 |
|---|---|---|---|---|
| **1** | **控制塔 P0：`G12d`「生成物单点生成门禁」是虚设门禁（fail-open）**——`git diff --cached --name-status` 的输出是 `M<TAB>path`，脚本 `${line:3}` 多切 1 字符（应为 `${line:2}`）→ 永不命中、恒报 ✅。**队长已独立复现**：`M\tdocs/…` → `ocs/…`（不命中），修正后命中 | `scripts/pre-commit-check.sh:1339`（整段 :1331-1343） | **控制塔线（CTO，`scripts/pre-commit-check.sh`）** | 不阻塞本队；P0 |
| **2** | **win 域 3 处消费方读已删字段**：`collect.js:119` `?? 0` → **静默变 0**；`client.js:545-546` 有 `pct1()` fallback → **客户端自行算回百分比（口径在该面板复活）**；`derive-board-sources.py:292` → `None`；`lib/sync.js:341/366` 渲染 `%`；＋对应测试 | 各 file:line 见左 | **win 线（另起 FIX 卡）** | 不阻塞（A 已在 `product-progress.json.deprecated_fields` ＋ `calc-progress.py:35,621-624` 显式登记，禁沉默已满足） |
| **3** | **「取消整体完成度」只落一半**：控制塔两处面板仍渲染「总完成度」——`app/founder-dashboard.html:126-127`（静态 `0.0%`，由 `scripts/control-tower/generate-dashboard.py:775` 生成，且在 pre-commit 的 GENERATED_FILES 名单内）＋ `scripts/control-tower/views/pm_dashboard.py:109-110`（动态 `{overall:.1f}%`，源 `completion.systemScore`） | 各 file:line 见左 | **控制塔线（D846-D848 地盘，另起卡）** | 不阻塞；属**口径欠账**（须呈创始人） |
| **4** | **存量假红（A/B 已排除与本批无关）**：`tests/control-tower/product-lines.test.py` 7 败（含 `:285 total_lines==26`、`:407 class="line"==26` 两处 live 硬编码，CT-67 同族）＋ `calc-k3-stale.test.py:268` **时间炸弹**（夹具写死 2026-09-02＋TTL 14 天，自 09-17 起必红） | 基线 `dae40d96` 同环境失败集合逐条相同（自验员实测） | **tests/control-tower 域（另起卡）** | 不阻塞 |
| **5** | **「缺」档无机器可读源 → `missing=null`**：创始人签字要「N 健康 / M 写了没接 / **K 缺**」三档**计数**，本批实交 **2 计数＋1 null**。yaml 验收点字段仅 `{id,desc,evidence,status,note,k3_only}`，无 `implemented/wired` 载体；而总纲 §1.2「缺」的判定手段 = 代码检索，已被事实驱动 §六 ＋ 撤回清单 #3 禁用 | `gen-project-board.py` `buckets.missing.reason`；`product-progress.json.buckets.missing` | **派单口径 → 呈创始人裁定**（另立卡；**不是编码 A 的责任**） | **未闭环（对账必读）** |
| **6** | **裁定表 §6-5 在 main 上仍 ☐ 未勾选**；`§6-4/§6-5` 标题仍写「待签」。PR **#677**（分支 `docs/sign-record`）标题称「§6 五项全部签署落库」，但其 commit `9f6fd1be` 把裁定表以**0 字节空 blob** 提交（origin/main 版 4243 B），且分支 base 停在 `1d8dcab6` → **不得按现状合并**，须基于最新 main 重做 | `创始人裁定表-§6五项-20260920.md:28-49`；`git show origin/docs/sign-record:…` = 0 B | **创始人（补签/回写）＋ CTO（重做 #677）** | 不阻塞本队；**对账必读** |
| **7** | **PR #675 与 #674 同 head SHA（`9cce8b87`）= 重复 PR**；且 #675 标题写 `docs(D850)` 与本批 **D850 撞号**；其声称的「研究院材料权威级别与引用纪律」文件在 **两个分支上均不存在**（标题 vs 内容不符） | `GET /pulls/{674,675}`；`git ls-tree origin/chore/authority-v2-2 docs/authority/` | **CTO / dev-doc** | 不阻塞 |
| **8** | **V1 分母变更待批量裁定**：128 → **134**（＋6）；另含 **4-9 口径差**（DSH `maxInlineBytes` 是**单阈值＋UTF-8 字节**，我方是**双阈值＋字符**；8,000–32,000 之间档位语义**原裁定未定义**，B 未替创始人推定）；3-9 是否为 D828 硬前置；线 25 分类判据 | `D852-V1-轻量变更单-20260920.md` §三/§七 | **创始人** | 不阻塞 |
| **9** | `task-start.sh` 生成的 brief **无日期前缀**（`D850.md`）→ G12「今日 brief」认领窗口不含它 → 实测 2 文件被误判「不在 Q2 范围」（A 改名后通过） | `scripts/workflow/task-start.sh` ＋ pre-commit G12 段 | **控制塔线** | 不阻塞 |
| **10** | **CT-67 台账条目内「实测」数字不可复现**（记 `v1_passed=26 / delivery_pct=20.3`，两次独立实测均 **27 / 21.1**；`128/46` 可复现） | `AUDIT-FINDINGS-LEDGER.md:101` | **CTO（台账维护）** | 不阻塞 |
| **11** | **`memory/notes` × `task-state` 成对操作坑**：`proposed/` Note 引用 `impl_done` 的 D# → 僵尸门禁硬阻断 → 必须先 `git mv` 再改 status（本收口已按对操作处理 D852） | `check-notes-lifecycle.sh:16,79` | **CTO（已闭环于本 PR）** | — |
| **12** | `tests/control-tower/product-lines.test.py` 的 `TestRefreshAll.test_end_to_end` 会**跑 `refresh-all.sh` 往真实仓库写证据文件**（命名 `<type>-<date>.json` 不带任务号，违 M5） | 该测试 ＋ `run-machine-evidence.sh` | **tests/control-tower 域** | 不阻塞 |
| **13** | **未合 PR 队列 32 / 上限 12**（超限 20） | `docs/synova/project/pr-queue.json`（机器派生） | **CTO** | 在途 |
| **14** | CT-70（`tests/project/**` 未接 CI；`grep -c "tests/project" .github/workflows/ci.yml` = **0**）→ A 件 PR #682 在途（#680 已关闭换作 #682） | `.github/workflows/ci.yml` | **CT-70 线（队外）** | 队外 |
| **15** | **DSH 版本双锚点风险**：本机桌面端 `0.1.6-alpha.2` **有** `dsh-webhook`（B 的行号基准）／nvm 全局 `0.1.0-rc.8` **无**该包且同名文件行号偏移 1~17 行 → K3 若按 `cto-handover` §〇b 所载 nvm 路径复核会**假红（误判引用造假）** | B 的 `D852-派单回执-20260920.md` §D | **CTO（skill 版本锚点；skill 改动走创始人审）** | 不阻塞 |
| **16** | 4-9 note 两条 DSH 行号区间不精确（注「no-op（:106）」实为 `:104`；warn 块实为 `:115-152`）——语义与源码一致，仅行号 | `product-lines.yaml` 4-9 note | **编码 B（低优先，随下批）** | 不阻塞 |
| **17** | **耗时读数不可复现（V-12）**：D850 首版自述「单次派生 1.42 s → 203 档 ≈5 分钟」；独立自验员实测 **2.60/3.41/4.15 s**，作者复测 10 次得 **0.71–12.68 s（强负载相关）** → 203 档 **2.4~43 分钟**。方向对、量级偏乐观 | 自验记录 §2 / PR #683 评论 | **编码 A（口径已更正；教训入 Note）** | 不阻塞 |
| **18** | **面板测试环境敏感（V-13）**：`calc-progress-panel.test.sh` 在**无 `.git`/shallow** 下曾报「派生件陈旧」这类**误导性结论**（真因 = freshness gate 无 git → `live_unverified 32→54 / stale 36→14`）；`ci.yml` 多 job 为 `fetch-depth: 1` → 接入会假红（D520 同类）。已加 git 闸 SKIP+原因（exit 0）；**接 CI 时仍需在 depth=1 job 上实测一次** | 自验记录 / 台账 CT-73 | **编码 A（已闭环）＋ CT-70/D854 接 CI 时复验** | 不阻塞 |
| **19** | **一次操作失误（已恢复，如实登记）**：作者清副作用件时执行过宽的 `rm docs/synova/product-lines/evidence/test-*.json`，**误删 14 个 git 已跟踪证据记录**，随后 `git checkout --` 全量恢复。**队长独立核过**：`git status --porcelain` 空、`git ls-files --deleted` 空、tracked 65 == HEAD 65 == origin/main 65、`git diff origin/main...HEAD -- …/evidence/` 空 | 作者自报 + 队长复核 | **编码 A（教训：清副作用按 `git ls-files --others` 精确删，禁通配 rm）** | 不阻塞 |
| **20** | **V-16｜「可复现命令」断言了存在、没断言新鲜**：中间件（`/tmp/ro-pp.json`）**存在且陈旧**时该档 evidence_cmd **假绿**（实测三步：改源 → 不重生成 → `rc=0 wired_broken=4`）。文档两步走与 CI 测试路径都不触发；危险窗口 = 复用 `/tmp` 残留件 | 第三轮复验（原文 `/tmp/verify-report3-D850-20260920.md`） | **编码 A / 度量链（已立台账 CT-76，另起 FIX 卡）** | 不阻塞（强度类） |
| **21** | **V-17｜独立判据强度未分层**：线级 `bound×28` 在 healthy 全 0 时**恒真**（抓不到低估）、`reconciliation×28` 强度低于等值复现、顶层仅 1 条等值型；线级 `missing` 28 档无 probe、线级 `other_states` 112 档无独立判据（覆盖 56/196） | 第三轮复验（ⓒ） | **编码 A / 度量链 + 测试域（台账 CT-77）** | 不阻塞（强度类） |
| **22** | **V-18｜耗时读数两次转述失真**：仓库实际记录 **5 次**（`12.68/7.29/1.68/2.09/1.47 s`，load 18.85）；队长转述为「10 次/含 0.71」不实（自验员空载 10 次实测 `0.69–0.91 s`）。诚实区间 **0.7 s（空载）~12.7 s（重载）**，203 档 ≈ 2.3~43 分钟 | 第三轮复验（V-12/V-18） | **编码 A（口径）＋ 队长（转述失真，本条即更正）** | 不阻塞 |
| **23** | **V-19｜cosmetic**：`MISMATCH` 路径标签前缀重复 `buckets`（`buckets.buckets.…` / `lines[id=2].buckets.buckets.…`） | 第三轮复验 | **编码 A（随下批）** | 不阻塞 |

> **台账登记**：上表第 1/3/4/12/2 项已按红线「发现即登记」写入 `docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md`：**CT-71**（G12d 虚设门禁，fail-open，与 CT-64 同族）/ **CT-72**（task-start brief 无日期前缀 → G12 认领窗口漏判）/ **CT-73**（控制塔测试写真实仓库 + 环境敏感假红）/ **CT-74**（「取消整体完成度」只落一半：`app/founder-dashboard.html:126-127` ＋ `pm_dashboard.py:109-110`）/ **CT-75**（win 域消费方读已删百分比字段 ＋ `client.js:545-546` `pct1()` fallback 复活口径）。第 6/10 项分别由既有 CT-68（派单件未落 main）覆盖、CT-67 条目数字订正待办；第 20/21 项登记为 **CT-76**（「可复现命令」断言了存在没断言新鲜 → 陈旧中间件假绿）/ **CT-77**（独立判据强度未分层：bound 型在基线上恒真）。

---

## 七、待创始人裁定（3 项，均不阻塞合并）

1. **「缺」档怎么办**（遗留 5）：现状 `K 缺 = 无法判定`。给创始人的诚实读数是——**16 健康 / 3 写了没接 / 0 可判定为缺 / 98 五态归属未知**，**最大一档是「未知」**。选项：ⓐ 接受「缺＝不可判定」并保持 null（诚实但三档计数缺一）；ⓑ 立项补「实现态」机器可读源（度量基建，依赖第 0 项 P-3 立项 / e2e 进 CI）；ⓒ 放宽口径允许显式标注来源的静态判据只作**粗筛**（仍不得当验收依据）。
2. **裁定表 §6-5 签署回写**（遗留 6）：四项决定已按「创始人已签（经 CTO 派单转达）」落地，**但仓库内物证仍是 ☐**——需创始人回写或授权 CTO 落库；同时 **PR #677 必须废弃重做**。
3. **控制塔域两处「总完成度」**（遗留 3）：是否同步取消（含 `app/founder-dashboard.html` 与 PM 仪表盘）——属 `scripts/control-tower/**` 地盘，需授权另立卡。

---

## 八、队长自省（防线改进候选，一律不自行改门禁）

| 观察 | 改进候选 | 归属 |
|---|---|---|
| 自验员发现 **117/0 全绿仍漏 V-1** —— 因「新契约的生成物命令」无测试。**全绿的边界 = 测试覆盖的边界** | 把「生成物自带的 evidence_cmd 必须逐条可执行」变成**通用断言模式**（凡脚本产出含 `evidence_cmd`，测试必须全量执行） | CTO（控制塔线） |
| 我在 task-1 卡里给了「行号基线」但有 2 处不准（`:302` 实为 `:326`；卡片写「基于 dae40d96」而分支实际 merge-base 为 `7e4e169d`） | 派单/卡片的行号与 base 字段**改为让执行方开工第一步自查并回执**（已在本批执行：B 的派单回执含写前核实 6 项） | CTO |
| `G12d` 虚设门禁（遗留 1）与 CT-64 同族 = **同类错误第二次出现** | 按红线**升级**：门禁类变更必须带「反例测试」（故意命中一次证明门禁真能拦） | CTO → 创始人知悉 |
| **「证据」被降级成「自查」——两者输出长得几乎一样**（第二轮退回 ⓐ）：性能压力下最容易被"优化"掉的正是可证伪性 | 固化成一句可操作的验收问句：**「第三方跑这条命令，有没有可能得出与看板不同的结论？」** 答不出 → 它不是证据。建议写进 `contract-template`（skill 改动走创始人审） | CTO |
| 「可复现命令」断言了**存在**、没断言**新鲜**（V-16） | 凡「比对中间件」型证据，命令必须同时校验中间件**新鲜度/源指纹**，否则陈旧中间件会静默假绿 | CTO → 度量链 FIX 卡（CT-76） |
| 队长**转述**同事数字时失真（V-18：把 5 次读数说成 10 次） | 转述一律**只引原文**（file:line 或原样输出），不二次加工 | CTO（本条即自纠） |

---

## 九、复现命令（任何人可独立重跑）

```bash
# D852（已在 main）：yaml 仍 28 线 / 零删除 / points_total 180
python3 -c "import sys;sys.path.insert(0,'scripts/product-lines');import productline_yaml as p;d=p.load_file('docs/synova/product-lines/product-lines.yaml');print(len(d['lines']), sum(len(l.get('acceptance_points') or []) for l in d['lines']))"

# D850：三档 + 面板 29 条 + 禁百分比（工作树 .synova-wt-d850）
cd .synova-wt-d850
python3 scripts/project/gen-project-board.py --out /tmp/l.json --quiet
python3 -c "import json;d=json.load(open('/tmp/l.json'));print({k:(v.get('count') if isinstance(v,dict) else v) for k,v in d['totals']['buckets'].items()})"
python3 scripts/product-lines/calc-progress.py --out docs/synova/product-lines/product-progress.json
python3 -c "import json;print(json.load(open('docs/synova/product-lines/product-progress.json'))['total_lines'])"
grep -rn "product_progress_pct\|delivery_pct\|verify_pct\|产品总进度\|总完成度" \
  scripts/project/gen-project-board.py scripts/product-lines/calc-progress.py scripts/product-lines/gen-progress-page.py \
  docs/synova/project/ledger.json docs/synova/product-lines/product-progress.json docs/synova/product-lines/product-progress.html   # 期望：零命中
bash tests/project/gen-project-board.test.sh
```

---

> **本文件性质**：小队**自验**收口（不是审计）。结论只允许两种措辞：`可提请独立审计` / `退回（附理由）`；独立审计由**队外 K3** 出具。
