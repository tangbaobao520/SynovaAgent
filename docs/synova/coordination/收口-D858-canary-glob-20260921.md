# 收口 · D858（CT-70 审计条件闭环：canary 覆盖语义物理化）（2026-09-21）

> 队长：并行 CTO session ｜ 编码：`d858-coder` ｜ 自验：`d858-verifier`（独立，非编码兼任）｜ 审计：K3 队外（本卡**未**自我审计）
> 分支 `gate/d858-canary-glob` ｜ 工作树 `.synova-wt-d858` ｜ 基线 `origin/main cc4e4166` ｜ PR **#687**
> 卡面日期 20260920（派单件 `docs/synova/coordination/派单-D858-CT70审计条件闭环-20260920.md`，本卡入库），物理执行日 20260921

## 一、一句话现状

K3 给 D854/CT-70 的 **CONDITIONAL PASS** 四条条件（P1 覆盖语义物理化 / P2 绿腿告警可见 / L4-1 注释与物理覆盖一致性 / L4-3 证据包标准化）**全部闭环**，队内自验（独立）**通过**，回归判定与基线逐字一致；剩余 1 项载体缺陷（`ci.yml` 的 `|| true`）因写集硬边界不在本卡 → 已登记 **CT-78 + D862** 排期。**待 K3 队外复审；通过前不合并。**

## 二、三件交付（索引）

| # | 件 | 路径 |
|---|---|---|
| ① | **diff** | 本文 §三（原始 `git diff --stat/--numstat` 输出） |
| ② | **自验结论** | `docs/synova/product-lines/evidence/D858-verify-20260921.md` |
| ③ | **遗留清单 + 归属逐张收口** | 本文 §六 / §七 |
| 附 | 证据包（P1/L4-1/P2/回归/L4-3/CI） | `docs/synova/product-lines/evidence/D858-evidence-20260921.md` |
| 附 | L4-3 标准件 | `docs/synova/coordination/D858-证据包标准件-匿名取CI证据-20260921.md` |
| 附 | M1 冲突扫描 | `docs/synova/coordination/D858-开工前冲突扫描-20260921.md` |

## 三、diff（M6-①，原始输出）

```
$ git diff --stat origin/main...HEAD
 .claude/bypass.log                                 |   2 +
 .claude/task-briefs/2026-09-21-D858-canary-glob-coverage.md | 167 ++++++++++++++
 docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md  |   2 +
 docs/synova/coordination/D858-开工前冲突扫描-20260921.md    | 105 +++++++++
 docs/synova/coordination/D858-证据包标准件-匿名取CI证据-20260921.md | 120 ++++++++++
 docs/synova/coordination/派单-D858-CT70审计条件闭环-20260920.md |  64 ++++++
 memory/notes/proposed/2026-09-21-D858-canary-glob-physical-coverage.md |  35 +++
 scripts/control-tower/check-canary-drift.sh        | 223 +++++++++++++++++--
 task-state/D858.json                               |  52 +++++
 task-state/D862.json                               |  39 ++++
 tests/control-tower/check-canary-drift.test.sh     | 242 ++++++++++++++++++++-
 11 files changed, 1031 insertions(+), 20 deletions(-)

$ git diff --numstat origin/main...HEAD
205	18	scripts/control-tower/check-canary-drift.sh
240	2	tests/control-tower/check-canary-drift.test.sh
（其余为治理产物：brief / task-state ×2 / ledger / memory note / 证据与派单件 / bypass 登记）
```

- **代码面只有 2 个文件**（`scripts/control-tower/check-canary-drift.sh` 83→270 行；`tests/control-tower/check-canary-drift.test.sh` 76→314 行）；测试文件被删的 2 行是**头注**，既有断言零删除（自验员逐区间核过：`git diff` 仅 4 个 hunk，其中 1 个是授权的 H4 段改写、1 个纯新增）。
- 提交集：`0afc9aa5`（V2 主体）→ `b2966ddf`（V3 加固），各自带 `synova-commit` 的 bypass 登记提交；**无 `--no-verify` / 无 `git stash` / 无 force push**。
- ci.yml 零改动（`git diff --numstat origin/main...HEAD -- .github/workflows/ci.yml` 空）；`tests/project/**`、`scripts/audit/**` 零改动。
- PR 预算：代码/测试面 2 文件、单域（控制塔治理层）→ 在 12 文件 + 单域预算内。

## 四、自验结论（M6-②）

| 轮 | 冻结件 | 结论 |
|---|---|---|
| T2（V1） | `00afe8af…` / `9e282f04…` | **有条件通过** → C1（引号内 `#` 误红面）/ C2（行尾注释声明 fail-open）/ C3（ci.yml 吞 exit 1） |
| T4（V2） | `f5e9d37a…` / `df00ace8…` | **通过**（C1/C2 闭合、零退化）→ 新登记 R1（`;#` 残余假绿）/ R3（`\"` 误红）/ 降级 fail-open |
| T6（V3） | `574d4465…` / `cff8c1dc…` | **通过**（R1/R3 闭合 + 降级 fail-closed + 30/30 夹具零退化）→ 登记 4 项已知边界 |
| **T8（V4，最终）** | `ec7f49aa…` / `addd1d69…` | **通过** → 修掉 **CI windows 腿真红**（T7：`SYNO_AWK_BIN` 注入缝使「awk 不可用」跨平台确定性可测 + 受限 PATH 探针改为可证伪才断言；跨平台静态审计 10 类构造无剩余 Windows 风险项） |

最终件三方独立实测一致：配对测试 `结果: 70 通过, 0 失败`；真实 main ci.yml stdout sha256 `b12210c4…`（`729 | 清单 41 | 漂移 71 | exit 0`）**与基线逐字相同**；#682 预演 `78b5a299…`（`清单 44 | 漂移 68 | 假覆盖 0 | exit 0`）。反向验收：同夹具下**基线脚本 exit 0（把注释当覆盖 = 假绿）vs 新脚本 exit 1 + `::error title=canary-fake-coverage`** → 修复承重（load-bearing）成立。

> 纪律说明：T2 的「有条件通过」按创始人纪律＝未通过 → 未在此收口，而是派 V2/V3 逐条闭环后才判通过。

## 五、四条条件闭环对照

| 条件 | 判据 | 结果 |
|---|---|---|
| **P1** 覆盖语义由物理展开承载 | 删 glob 接入（注释仍声明）→ 必须报红；恢复 → 绿 | ✅ 夹具先红后绿（`❌ 假覆盖` + `::error` + exit 1 ↔ `✅ 零漂移` exit 0）；覆盖来源只认非注释行可执行项 + glob **文件系统**展开（零命中不算覆盖） |
| **P2** 绿腿告警可见 | 独立通道 + 真实运行证据 | ✅ `GITHUB_STEP_SUMMARY` 写入（stdout→`/dev/null` 仍完整）＋ 保留 `::warning`；真实 CI 绿腿：run `35529313371`（ubuntu job `106126971666` success）日志含 `##[warning]71 个测试不在 CI canary 清单…`，归档片段见证据件 §7 |
| **L4-1** 注释声明 > 物理覆盖 → 报红 | 声明 2 展开 1 / 仅注释声明 / 行尾注释声明 | ✅ 三种夹具均 exit 1 + 点名未覆盖项（`F8/F9/H1/H3`，自验员独立复现） |
| **L4-3** 证据包标准化 | 匿名/无 token 取 job 日志的**一条命令** + 示例输出 | ✅ 标准件成文；实测 `/actions/jobs/<id>/logs` 匿名 **403**、`check-runs` + `annotations` 匿名 **200**（含失败/告警正文，绿腿可取）、配额 60/h/IP、归档片段 + sha256 + `raw.githubusercontent` 匿名取用 |
| **回归** | 既有判定不变 | ✅ 真实 main `41 / 71 / exit 0` 与基线 stdout **同 sha256**；#682 预演 `44 / 68 / 0 / exit 0`（物理展开 3 个文件） |

## 六、遗留清单 ＋ 归属逐张收口

| # | 遗留 | 证据（物理） | 归属 | 阻塞 |
|---|---|---|---|---|
| **1** | **`ci.yml:277` `\|\| true` 吞掉 canary 的 exit 1 与 exit 2** → 假覆盖/执行失败在 CI 层不阻断（脚本层已 `::error` 上面板）；同处 **tests/project 绿腿 ⚠ 不可见**（K3 §5 P2 载体段） | `grep -n "check-canary-drift" .github/workflows/ci.yml` → `277: … \|\| true`；D858 自验 C3 | **D862**（`task-state/D862.json` 已登记；等 #682/#657 合并释放 ci.yml 单写者）＋ 台账 **CT-78** | **不阻塞本卡收口**；阻塞「canary fail-closed 真正生效」 |
| **2** | **`$(...)#` 残余误红**（`bash -c 'echo $(echo a)#c'` → `a#c`，脚本却把 `)` 当词边界 → 真实可执行路径被判注释声明 → 误红） | 自验员 T6 §8：最小夹具 exit 1 + `out/cmdsub-paren-proof.txt`；**反证**：该通道无法隐藏路径（截断内容仍进 DECLARED 受"必须被物理覆盖"校验）→ 只误红、不假绿 | 控制塔线（本卡写集内、**已知边界**；修法需命令替换深度感知） | 不阻塞（真实 main/#682 ci.yml 中 `)#` 命中 0） |
| **3** | **文本级固有边界**：覆盖 = 非注释行文本 + 物理展开，不执行 ci.yml 语义 → `echo "tests/x.test.sh"` 这类字符串内路径仍算覆盖 | 自验员 T6 §10②；卡面 P1 只要求"不得由**注释**承载" | 控制塔线（**声明为已知边界**，非缺陷） | 不阻塞 |
| **4** | **gawk/Windows awk 未在本机实机对拍**（本机仅 BSD/BWK awk） | 自验员 T6 §9 | 已由**真实 runner 双平台腿**补齐部分：ubuntu（`106126971666` success）输出 `729 / 41 / 71` 与本机逐字相同；Windows 腿结论见证据件 §7 | 不阻塞 |
| **5** | **`**` 递归 glob 不启用**（macOS bash 3.2 无 globstar）→ `**` 按单层语义 | 脚本 L64-67 显式声明 + 自验 H8 物理验证 | 控制塔线（脚本内已声明；当前真实形态为单层 glob） | 不阻塞 |
| **6** | **`d858-coder` 流程自纠**：T5 先动手后补 claim（期间无他人触碰两文件） | 编码 session 自述 + `git status` 全程仅 ` M` 两文件 | CTO（小队纪律：任务板先 claim 再动手） | 不阻塞（已自纠并记录） |

> 红线纪律：发现即登记——上表 1/2 已同步进 `docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md`（CT-78 / CT-79）。

## 七、归属逐张收口（M6：DSH 不自动释放归属）

| 对象 | 归属/状态 | 收口动作 |
|---|---|---|
| D858（本卡） | `task-state/D858.json` → **impl_done**（自验通过；审计待 K3） | 本 PR 内更新；K3 复审后再改 `audited` |
| D854 / CT-70（PR #682） | 仍 open、`mergeable=dirty`（需其 owner rebase） | **合并前置**：本卡闭环 + K3 复审通过 → CTO 复验后 D854 由「退回」改判；#682 与 #657 串行（同为 ci.yml 写者） |
| D862（新卡） | 已登记（`task-state/D862.json`，owner mac-coding，`blocked=等 #682/#657 合并`） | 本 PR 内登记；CTO 排期（§六-1） |
| D860/D861 等 | 不作为本卡对象 | — |
| K3 复审 | **待提请**（`docs/synova/coordination/审计提请-D858-K3-复审判-20260921.md`） | K3 队外复审；**通过前不合并** |

## 八、复现命令（任何人可独立重跑）

```bash
cd /Users/wane/SynovaAgent/.synova-wt-d858        # 或: git fetch origin gate/d858-canary-glob && git checkout FETCH_HEAD
shasum -a 256 scripts/control-tower/check-canary-drift.sh tests/control-tower/check-canary-drift.test.sh
#   → 574d4465…(270 行) / cff8c1dc…(314 行)
bash tests/control-tower/check-canary-drift.test.sh        # → 结果: 65 通过, 0 失败
bash scripts/control-tower/check-canary-drift.sh | tail -3 # → 729 | 清单 41 | 漂移 71；exit 0
git show fix/d854-ct70-project-tests-ci:.github/workflows/ci.yml > /tmp/pr682-ci.yml
SYNO_CI_YML=/tmp/pr682-ci.yml bash scripts/control-tower/check-canary-drift.sh | tail -3   # → 清单 44 | 漂移 68
# 反向验收（抽掉修复必须报红）：用 origin/main 版脚本对同一夹具
git show origin/main:scripts/control-tower/check-canary-drift.sh > /tmp/base-canary.sh
```
