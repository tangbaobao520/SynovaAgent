# 收口｜D911 切片 C — G12 三源并集 + commit-date 判据 + fail-open 显式化

> **队长收口件（M6）**｜2026-09-22｜小队 = D911-C（队长 1 ＋ 编码 1 ＋ 自验 1；自验**非**编码兼任）
> **交付分支**：`fix/D911-g12-writeset`｜**被独立自验对象**：`141a3d6c`｜**并入 main 后 HEAD**：`e80ec9e9`
> **权威件**：派单件 `docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md` §一切片 C；裁定记录 `docs/synova/coordination/裁定记录-门禁三缺陷-20260922.md`
> **自验报告**：`docs/synova/product-lines/evidence/D911-verify-20260922.md`（255 行，复核率 44/46 = 95.7%）＋ `D911-evidence-20260922.md`（594 行原始输出）
> **文件命名偏离说明**：派单件 §二 给「队长（收尾）」的文件名为 `收口-D911-20260922.md` / `D911-leftovers-20260922.md`。本 session 交付切片 **C**，而 A/B 切片由并行 session 在跑（实测两工作树均已开工）→ 为**避免同名相撞**，本件按切片加后缀（`-C-`），**未扩写集**（仍为队长行 2 件）。

---

## 一 三态判定建议

**`可提请独立审计`**（自验结论原文；本件不下「通过」——通过与否归 **CTO 收件闸 + K3 终审**）

- 6 条卡面判据 C1–C6 **全部先红后绿**，附反例夹具；自验独立重算 **44/46 = 95.7%**，**无退回项**。
- 队长终检（并入 main 后，本 session 亲跑）：门禁 13 组 `exit 0`；受影响测试 5 张全绿；工作树零污染。
- 遗留 6 条 + 新发现 7 条**均不在 C 切片写集**，未代为修复（见 `D911-C-leftovers-20260922.md`）。

---

## 二 最终 diff（路径清单 + 关键差异说明）

### 2.1 写集 5 件（编码提交 `e2a17507`）

| 文件 | 行数变化 | 关键差异说明 |
|---|---|---|
| `scripts/pre-commit-check.sh` | +194 / −24 | C2：组 12 基准改 `git log -1 --format=%cI`（`G12_COMMIT_REF` 缺省 HEAD；`G12_TODAY`/`TODAY_DASH` 同源；注入缝 `SYNO_G12_COMMIT`/`SYNO_G12_TODAY` **仅 `SYNO_TEST_ARM=1` 武装**；取不到 → 显式 ⚠️ + 回退）。C1：S1 `task-state/<D#>.json:write_set` ＋ S2 `devdoc_writeset.py --extract` ＋ S3 `brief_parser --q2-include` 三源并集写入 `SCOPE_TSV`（带来源列）；匹配口径 = 既有 basename 后缀匹配 ∪ 精确/目录前缀/glob。C3：无认领 brief 分支改 `soft_check`（`SYNO_CI=1` 转硬阻断）＋ ⚠️ 明细。C5：越界提示给 `## 写集` 机器块正解 + 三源并集说明。C6：886 行 awk 计入标题行内联值；阈值 `-le 1` → `-eq 0`（口径 = 内容 ≥1 行含内联 → 绿；0 行 → 红）。 |
| `scripts/control-tower/brief_parser.py` | +41 / −8 | C4：`parse_q2()` 后置剥壳对齐 `merge_writeset_gate._clean_entry()`（剥反引号 ×2 ＋ 按 `\s+[—–-]{1,2}\s+` 切说明）。C6：`parse_done()` 接受 `## Done 标准: <值>` 内联写法（含全角冒号/空格写法），空段与占位仍判 0（不放宽成"空也绿"）。 |
| `tests/control-tower/task-scope-g12.test.sh` | +319（新建） | 38 断言：C1–C6 夹具 ＋ 降级路径 ＋ 反例（自建临时 git repo，`trap` 清理，零真实仓库污染）。 |
| `.claude/task-briefs/2026-09-22-D911-C-g12-writeset.md` | +104（新建） | 6 核心字段 ＋ `#CRITERIA: A` ＋ `## 写集` 机器块 ＋ 6 条 `verify:`。 |
| `memory/notes/proposed/2026-09-22-d911-c-g12-writeset.md` | +50（新建） | 铁律 49 决策沉淀（commit-msg 物理门禁引用）。 |

### 2.2 自验证据 2 件（自验提交 `19cf5ab8`）

| 文件 | 行数 | 说明 |
|---|---|---|
| `docs/synova/product-lines/evidence/D911-verify-20260922.md` | +255 | 自验结论 ＋ 46 行复核率表 ＋ 逐条判据 ＋ 反例 ＋ 零新增红 ＋ 新发现 7 条 ＋ 遗留 6 条 |
| `docs/synova/product-lines/evidence/D911-evidence-20260922.md` | +594 | 原始输出存档（场景 S1–S9、两树对照、grep 原始结果） |

### 2.3 非写集产物（须 CTO 知悉，非作者写集）

- `.claude/bypass.log`：post-commit hook 自动登记行 ×3（每个提交一行；派单件 §六.8 明确「不动」，合并按**并集**解）。
- `e80ec9e9`：队长并入 `origin/main`（自验 N3；并入前落后 3 个提交，均为 D917 文档域，**未碰交付三文件**）。

### 2.4 「被验对象未被后续提交改动」物理证明

```
$ git -C .synova-wt-d911c diff --stat 141a3d6c HEAD -- scripts/pre-commit-check.sh scripts/control-tower/brief_parser.py tests/control-tower/task-scope-g12.test.sh
（空输出）
```

→ 自验对象 `141a3d6c` 之后的所有提交（自验证据、main 并入、本收口件）**未改动产品三件**，自验结论对当前分支 HEAD 仍然成立。

---

## 三 判据 C1–C6 验收证据（先红后绿，摘录；原始输出见自验证据件）

| 判据 | 修前（红） | 修后（绿） | 自验独立复算 |
|---|---|---|---|
| C1 三源并集 | `task-state/D911.json:write_set` 已含该测试，G12 仍判 `(不在 Q2 范围内)` 1 处 | 六形态探针（S1 精确/glob/目录两写法、S2、S3 剥壳形态）→ `✅ 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = …）`，越界 0 | ✅ S1 |
| C2 commit-date | 同一 commit：brief 日期在运行日窗口内 → `❌ 2 处`；改成 2026-09-15（掉出 ±1 天窗口）→ `✅ 所有文件均在 Q2 范围内`（**假绿**） | 同 commit，`SYNO_G12_TODAY` 取 2030-01-01 / 2050-12-31 → 判定段**逐字相同**；基准行显式 `commit-date(<sha>)` | ✅ S2（`diff` 空）＋ S3（生产默认注入**零生效** 0/0）＋ S4（换 commit → 窗口跟走） |
| C3 fail-open | 无 else 分支 → 无 ⚠️、无「本组未执行」，落假绿 | 本地 `⚠️ G12: 无认领 brief → 本组未执行 (D911 fail-open 显式化)` ＋ 假绿计数 0；`SYNO_CI=1` → `❌ …[CI strict——软提示在 CI 上为硬阻断]` | ✅ S4/S5 ＋ 静态核 `soft_check`→`HARD_FAIL++`(:113)→`exit 1`(:1668) |
| C4 剥壳同规则 | `parse_q2` 输出整条 `` `scripts/pre-commit-check.sh` —— 组 12 写集三源并集 ``（作模式永不匹配） | 输出 `scripts/pre-commit-check.sh`，与 `_clean_entry` **逐条一致**；裸路径/单破折号/exclude 尾斜杠契约不回归 | ✅ §1.1 四形态 ＋ 端到端（剥壳形态的声明**真被认领**）＋ 等价性夹具（`src/feat` 不误收 `src/feature-x.ts`） |
| C5 给正解 | 提示只指 `版本管理规范-控制塔.md` | 越界输出含 `## 写集` 机器块写法 ＋ `parse_write_set`（机器块优先）＋ 三源并集认领源 | ✅ S6 |
| C6 内联 Done | `## Done 标准: 跑 X 得 exit 0` → `DONE_EMPTY=0` → 判「至少需定义一条完成标准」（假红） | 真入口双路径：组 6 `✅ Task Brief: 6 核心字段…`；`Done 标准: 至少需定义一条完成标准`=**0**、`Done 标准无条目`=**0** | ✅ 4 fixture × 2 判定矩阵（内联 1/1、块式 2/2、**空段 0/0、空值 0/0 反例仍红**）＋ S8/S9 |

### 反例夹具（必须仍硬阻断）

```
❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     <探针> (不在 Q2 范围内 — 三源并集 S1 task-state.write_set / S2 dev doc 写集表 / S3 brief Q2 均未认领)
✅ 反例: 门禁退出码非 0（实测 1）
```

---

## 四 队长终检（并入 main 后，本 session 亲跑原始输出）

```
$ pgrep -fl 'pre-commit-check|vitest'        # 空 → 无重活并发；load 2.72
$ bash scripts/pre-commit-check.sh           # 工作树 .synova-wt-d911c 内
  ✅ 全部 13 组通过                            GATE_EXIT=0
  ✅ G12: 无暂存文件(跳过) / ✅ G12b: brief 可解析 (D313 M3)

$ 受影响测试（串行，一次一个）
task-scope-g12         rc=0 | 结果: 38 通过, 0 失败     ← 本交付新增
g12-day-window         rc=0 | 结果: 10 通过, 0 失败     ← 三条形状锁保持
today-by-name          rc=0 | 通过 15 / 失败 0          ← 存量红转绿（main 上 rc=1）
brief_parser           rc=0 | 结果: 6 通过, 0 失败
brief-parser-strip     rc=0 | 结果: 30 通过, 0 失败

$ git status --porcelain                     # 跑测试后复检
（空）→ 零工作树污染（派单件 §四 F8 教训）
```

### 零新增红（自验受控两树对照，摘录）

```
$ diff gate-base.verdicts.txt gate-head.verdicts.txt      # 每侧 61 条组级判定行
21c21
< ❌ G12b: brief 可解析性 (D313 M3): 3 处  [硬阻断]
---
> ❌ G12b: brief 可解析性 (D313 M3): 2 处  [硬阻断]
```

→ 唯一差异是**红度下降**；既有测试 9 张 × 2 树：3 张存量红 `diff -q` **逐字相同**（非本次引入）。

---

## 五 需 CTO 处置（卡面缺陷 / 收件闸注意）

| # | 项 | 证据 | 建议 |
|---|---|---|---|
| 1 | **§八② 探针字符串错误**：`grep -c '可粘贴'` 在本仓**永不命中**（实测 0，全历史 `git log -S` 亦为空）；真实串是 `可直接粘贴的精确豁免行`（`merge_writeset_gate.py:473`，命中 1） | 队长实测 | 派单件改探针；实质前置成立（#702 内容已在 main：`48971777`＋测试 29 通过/0 失败） |
| 2 | **§八④ 夹具已耗尽**：`origin/chore/ingest-institute` 已是 `origin/main` 祖先（merge-base = 该分支 head）→ 期望的 `block + 任务: D811` 不可复现 | 队长实测 | **已按 CTO 授权自建合成夹具**（见 §六）；A 切片可复用 |
| 3 | **派单件引用 §十二 不存在**：`验收标准-穿真实入口-v1-20260918.md` 121 行止于 §十一，全 `docs/` 检索「证据与回执」仅命中派单件自身 | 自验 N1 | 补 §十二 或改引用 |
| 4 | **C6 前提半错**：教内联写法的是 `CLAUDE.md:577` ＋ 存量 68/657 份 brief；`task-start.sh` 全文无 `Done`，`generate-task-brief.py:137` 生成的是**块式** | 自验 N2 | 派单件措辞订正（**推荐方向不变**：awk 兼容内联已落地） |
| 5 | **新测试未进 CI canary**：`grep -c 'task-scope-g12' .github/workflows/ci.yml` = 0 → GitHub **不会执行该 38 断言** | 自验 N4 | 另卡登记（`ci.yml` 单写者，不在本件写集）。**收件闸注意：不得以「CI 绿」代替该 38 断言通过** |
| 6 | **C4 有意未搬两步**：`_clean_entry` 的 `rstrip("/")` 与计数括号未搬进 `parse_q2`（两处既有断言钉死 `scripts/audit/` 尾斜杠）；目录前缀语义由 G12 matcher 承接 | 自验等价性夹具两则 → **不构成缺陷** | 知悉即可；若要单一事实源，另卡收敛 |

---

## 六 队级前置修复：§八④ 合成夹具（CTO 授权自建）

`origin/chore/ingest-institute` 耗尽后，本 session 按 CTO 授权在 `/tmp` 自建合成夹具（**特征**：分支名无 D# ＋ 历史含一次 `merge origin/main` ＋ 分支自有提交早于 main 新提交，复刻 #674 时序）：

```
$ python3 scripts/control-tower/merge_writeset_gate.py --repo-root /tmp/d911c-fixture/repo \
    --base main --head chore/ingest-institute --branch chore/ingest-institute
❌ 结论: block — 检测到 1 个写集外文件（夹带）
   任务: D811 | 分支: chore/ingest-institute          ← 取到并入的 main 历史里的号（缺陷① 复现）
EXIT=1

$ 同 commit，仅把 --branch 换成 docs/d849-ingest-institute
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）   任务: D849    EXIT=0
```

→ 缺陷① 在当前 main 的代码里**仍然活着**（`infer_did()` 的 `git log --max-count=20` 回退未限定 `merge-base(base,head)..head`，`merge_writeset_gate.py:146`）；夹具配方如上，A 切片可直接复用。

---

## 七 归属收口（M6：官方不自动释放）

| 板卡 | 归属 | 状态 | 说明 |
|---|---|---|---|
| task-1 编码 C 实现 | `d911-c-coder` | ✅ completed | 交付 `141a3d6c`（写集 5 件） |
| task-2 独立自验 | `d911-verifier` | ✅ completed | 证据 `19cf5ab8`（2 件 849 行） |

---

## 八 回执（`git ls-remote --heads origin | grep D911`）

```
$ git ls-remote --heads origin | grep D911
039c5f8c…  refs/heads/feat/D911-ownership-standby      ← 切片 B（他 session）
<本收口件提交 SHA>  refs/heads/fix/D911-g12-writeset   ← 本件（收口提交后由队长回报原始输出）
ee8361f4…  refs/heads/fix/D911-merge-writeset-gate     ← 切片 A（他 session）
```

**跨卡核对（M2，队长亲测）**：A 切片写集 = `{merge_writeset_gate.py, merge_writeset_gate.test.sh, 其 brief/note}`；C = `{pre-commit-check.sh, brief_parser.py, task-scope-g12.test.sh, 本件 brief/note}` → **零交集**；真实交集仅 `.claude/bypass.log`（hook 运行期账本，§六.8 豁免，合并按并集解）。C4 的对齐目标 `_clean_entry()` 在 A 分支与 main **逐字相同**（diff 空），A 的改动不引起 C4 漂移。
