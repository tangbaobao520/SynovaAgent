# 派单 — B 类并行安排 + 预规划（Mac 小队）

- 派单方：Mac-CTO ｜ 执行方：Mac DSH 小队 ｜ 终审：CTO 收件闸 + **K3 异构复审**
- 前提：D938（全角变量清扫）在制 → 本件规定**哪些能并行、哪些只能先出计划**

## 一、并行判据（三条，不看任务多少）

1. **写集是否重叠**（按文件，不按主题）
2. **是否撞热点文件**：`.github/workflows/ci.yml`、`scripts/pre-commit-check.sh`、`docs/synova/coordination/ownership.yaml`、`docs/synova/coordination/派单模板.md`
3. **同侧 WIP**：**代码卡一次一张**；**治理簿记卡（task-state / brief / 台账）不计 WIP**

## 二、裁定结果

| 卡 | 并行性 | 依据 |
|---|---|---|
| **B5 — 四卡 task-state 补登记** | ✅ **可立即并行（写码）** | 写集仅 `task-state/*.json`（domain_neutral），不撞任何热点文件 |
| **M9 — 门禁三件套** | ⚠️ **仅可并行"出计划"** | 实现要动 `scripts/control-tower/**` + `ci.yml` → 与 D938 撞 `ci.yml` |
| **B3 — D922 夹具登记 CI** | ⚠️ 同上 | 动 `ci.yml` |
| **B4 — D925 调用面接线** | ⚠️ 同上 | 动 `ci.yml`（+ 可能 `pre-commit-check.sh`） |
| **B2 — CI 既有红显式登记** | ⚠️ 同上（并入 M9 第③条） | 动 `ci.yml` + 新增豁免表 |

**⇒ 结论**：**写码层面只有 B5 能与 D938 并行**；M9/B2/B3/B4 **先并行做计划、串行做实现**（D938 过闸后按序动手）。

## 三、B5 派单（唯一可立即开工的）

**写集（逐个列名，禁「等」）**：
- `task-state/D928.json`（新）
- `task-state/D933.json`（新）
- `task-state/D934.json`（新）
- `task-state/D936.json`（新）

**判据（全部可执行）**：
1. 四卡 JSON 存在，字段完整：`task_id / title / status / executor / write_set / evidence / updated_at`
2. **内容与事实相符**（不许编）：D928 = 第③面生成器修复（PR #725，**尚未合并**）；D933 = Win 派单 + 模块归属基线（PR #730）；D934 = 两侧协作方式（PR #731）；D936 = 模块归属改为服从 `ownership.yaml` + 台账（PR #733）
3. **不改任何代码文件**（`git diff --name-only` 只允许出现 `task-state/`）
4. 每卡附"为什么当时漏登记"的一句话根因（F1 族第四次）

**执行形态（硬字段）**：Agent Teams 组队（队长 + ≤2 编码 + 1 独立自验）；队长不下场写码；每成员一份共享任务（写集+验收命令+写者）；回执含**团队成员运行记录**（成员名·状态·token·共享任务 id 与状态）。

## 四、预规划（三张卡，只出计划、不许写码）

**M9 / B3 / B4 各出一份 `/plan`**，六项齐备（依赖图／逐文件写集／三路径+变异体夹具设计／失效条件／前置冻结清单／可复制验收命令），**交付后停下等 CTO 复核**。
> 预规划**不写任何文件、不建工作树、不组队**（组队在放行后第一步做）；这样 D938 一过闸，下一张能立刻动手。

## 五、复核安排（独立 + 异构）

| 环节 | 谁 | 要求 |
|---|---|---|
| 执行侧自验 | 小队 **独立自验成员** | **不得由编码成员兼任**；只读 + /tmp |
| **独立复审（异构）** | **K3（Kimi）** | **复核模型 ≠ 执行模型**，满足"用不同模型复核"；结论有 `file:line` + 原始输出 |
| 收件闸 | CTO | 四判据：锚定／声称↔证据／**反例"改坏即红"**／**成员运行记录** |

**有条件通过 = 未通过**；执行方一律不判"通过"。

## 六、红线

不碰 `scripts/audit/**`；禁 `--no-verify`／`git stash`／force push 共享分支；任务在 `.synova-wt-*`（**名字带侧前缀**，如 `.synova-wt-squad-b5`）；主工作区只做同步；计数与引用带「命令 + 口径 + 截至时刻」。

## 引用豁免

- task-state/D928.json — 待建卡：本件要求新建的补登记卡，当前不存在即本卡要解决的事
- task-state/D933.json — 待建卡：同上
- task-state/D934.json — 待建卡：同上
- task-state/D936.json — 待建卡：同上
