# 派单：C1（D529）+ C2（D524）— 切片 A 条件项闭环（CI 修复 + devdoc 返修）

> 派单: CTO | 2026-08-25 | 认领: C1 → 🛠 编码 session（桌面端线）；C2 → 📋 dev-doc（K3 归因）
> 背景: 切片 A（D517-D519）CONDITIONAL PASS 的 C1/C2 两个条件项未闭环——创始人确认"可能之前遗漏"，现在正式派单
> 目标: C1/C2 都闭环后，切片 A 从 CONDITIONAL PASS 转 PASS（K3 只核两项证据）

---

## DSH 借鉴核查（强制章节 — cto-handover skill §〇b）

1. **施工图四色归属**：C1 → 🟢 死守（electron 桌面端构建链，品牌表层，施工图 §3.1）+ 构建链 CI（desktop-build.yml）；C2 → 文档返修（无代码模块）。依据：施工图 DOC-0114 §3.1。
2. **借鉴边界判定**：**无 DSH 代码可借鉴**——Electron 打包链（build-synova.cjs + electron-builder）是 Synova 特有，DSH 无 Electron；dev doc 返修是文档工作。
3. **DSH 源码参考**：无。

---

## C1（D529）：desktop-build CI 修复 → 绿后补 artifact URL

**问题**：`desktop-build` workflow（切片 A D517 交付）CI 一直红。已修 distutils（setup-python 3.11，PR #175），但**新失败点**（最近 run 实测）：
- **macos job：Step 7 Build installer (electron-builder) 失败**（backend/renderer 已过）
- **windows job：Step 5 Build backend 失败**

**目标**：CI 双平台全绿 → artifact（dmg/zip/exe）可下载 → **补 artifact URL 进 task-state/D517.json DS3 + D519.json DS6**（C1 闭环动作）。

**要求**：
1. **诊断失败根因**：rerun 拿干净日志（GitHub Actions 旧日志 BlobNotFound 不可用，需新 run 日志）——macos electron-builder 失败具体错误 + windows backend 失败具体错误（windows 是 npm ci/build:backend 哪一步？）
2. **修复**：改 `.github/workflows/desktop-build.yml` / `build-synova.cjs` / 构建脚本（`scripts/desktop/build-backend.sh` 等）——根因定位后最小修复
3. **CI 双平台全绿**：本地无法完全复现 CI 环境，以 GitHub Actions 双平台绿为准（连续 2 次绿）
4. **C1 闭环**：CI 绿后拿 artifact URL（Actions run 的 artifact 下载链接），回填 task-state/D517.json（DS3）+ D519.json（DS6），更新 slice 状态
5. **诚实**：若 CI 绿但 artifact 有已知限制（如 size/平台），如实标注，不伪造

**验证（物理可复现）**：
- desktop-build CI 双平台（macos+windows）全部 success（连续 2 次）
- artifact 下载链接真实可访问
- task-state/D517.json DS3 + D519.json DS6 含真实 artifact URL

**写集**：`.github/workflows/desktop-build.yml`、`build-synova.cjs`、`scripts/desktop/*`、`task-state/D517.json` + `D519.json`（URL 回填）
**不碰**：`src/`、`scripts/audit/`

## C2（D524）：D518 dev doc prod 契约漂移返修

**问题**（K3 切片 A 审计 P1-1，M7 漂移）：D518 dev doc §2/§5.1/§7 仍写旧 prod 契约 `node dist/src/index.js`，实现实为 `dist/backend.mjs` + 包内 Electron node 模式（backend-spawn.cjs:69）。**照 dev doc §7 写测试现在会红**。

**要求**（详见已有派单 `docs/synova/coordination/派单-D524-devdoc-fix-20260825.md`）：
1. D518 dev doc §2/§5.1/§7 三处回填新契约（`dist/backend.mjs` + `ELECTRON_RUN_AS_NODE=1` 注入说明）
2. §7 测试断言规范同步（`prod.args === ['dist/backend.mjs']`）
3. grep `node dist/src/index.js` 清零

**验证**：`grep -n "node dist/src/index.js" docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md` → 零结果；`grep -n "dist/backend.mjs"` → §2/§5.1/§7 三处

**写集**：仅 D518 dev doc + task-state/D524.json 回填
**不碰**：`electron/`、`tests/`、`scripts/audit/`

---

## 完成后的闭环动作（CTO 执行）

1. C1 绿 + URL 回填 + C2 完成 → 通知 K3 只核两项（CI artifact URL 真实 + dev doc grep 清零）
2. K3 核过 → 切片 A（D517-D519）CONDITIONAL PASS → **PASS**
3. 台账登记 C1/C2 闭环

## 给执行方的交付要求

1. C1（编码 session）：CI 全绿截图/日志 + artifact URL 回填 commit
2. C2（dev-doc）：D518 dev doc 回填 commit + grep 清零证据
3. 均需 task-state 回填（impl.commit + 证据），物理可复现，禁止文档声称冒充
