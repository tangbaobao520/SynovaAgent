# 派单｜CT-70 修复 + D850/D852 小队（2026-09-20 CTO 出件）

> 依据：创始人 2026-09-20 裁定（三态判定 + **退回只约束该件、无依赖可并行**）
> 权威件回执见 §〇-c；写集逐文件见 §二

---

# A 件｜CT-70：`tests/project/**` 接入 CI（单 session）

## A-1 是什么 / 为什么 / 推动什么

| 项 | 内容 |
|---|---|
| **是什么** | `tests/project/**` 里的测试（含 D811 刚交的 46 条断言）**从未被任何 CI 接线**：`grep -c "tests/project" .github/workflows/ci.yml` = **0**（CTO 实测） |
| **为什么现在做** | 它让"CI 全绿"变成**绿色假象**——PR 绿 ≠ 跑过这些测试。这是团队自曝的 CT-70（记功），但必须闭环 |
| **做完推动什么** | 队列台帐/派生器的测试从此**每次 PR 都跑**；后续任何"绿"都可信 |
| **依赖判定** | 不依赖任何件；**唯一约束**：`.github/workflows/ci.yml` 在本批内**只有它一个写者**（D848 让位，后改） |

## A-2 写集（逐文件）

`tests/project/**`、`.github/workflows/ci.yml`（只**追加** `tests/project/*.test.sh` 调用，不重排既有清单）、`task-state/D854.json`

## A-3 完成判据

- [ ] `ci.yml` 命中 `tests/project`（给行号）
- [ ] 一次真实 CI 运行证据（该 job 跑到新测试；给 job 名与链接/日志片段）
- [ ] 先红后绿：故意让一条 `tests/project` 断言失败 → CI 红 → 还原全绿（两次原始输出）
- [ ] 不重排既有测试清单（`git diff` 仅新增行）

---

# B 件｜小队：D850 度量口径 + D852 §6-5 四项落地（agent team）

## B-1 本队信息（M1）

| 项 | 值 |
|---|---|
| 任务号 | D850（度量口径改造）＋ D852（§6-5 四项产品决定落地） |
| 队长 | **并行 CTO session**（同一时间只带一支队） |
| 编制 | 编码 A（D850）＋ 编码 B（D852）＋ **自验 1（不得由编码兼任）** |
| 审计 | **K3，队外**（队内只说"自验"） |
| 专用工作树 | A：`.synova-wt-d850` / `team/d850-discrete-health`；B：`.synova-wt-d852` / `team/d852-product-decisions` |

## B-2 为什么派它 / 做完推动什么（创始人裁决已签，输入齐）

| 项 | 内容 |
|---|---|
| **为什么是它** | ① 你已签"取消总百分比、改离散健康计数"——**它决定你看到的数字真假**；② 你已签 §6-5 四项（webhook 出站为主 / spill 8,000+32,000 / 日粒度+周月聚合 / live-restart 清单）——**不定就没法派线 3/4/8/25** |
| **做完推动什么** | 面板/看板不再输出虚假精确的百分比，改 `N 健康 / M 写了没接 / K 缺` 且**每个数字带 evidenceCmd**；口径 26→**29 追平**；**解锁线 3/4/8/25 验收点**与 **D828 时间窗口径** |
| **不做会怎样** | 继续用"文件存在"当"装配成功"；线 3/4/8/25 只能派"样板污染"的验收点（批十四已吃过一次亏） |

## B-3 写集（M2 逐文件，A/B 两两不重叠）

| 切片 | 独占文件 |
|---|---|
| **A（D850）** | `scripts/project/gen-project-board.py`、`tests/project/gen-project-board.test.sh`、`docs/synova/project/ledger.json`（派生）、`docs/synova/product-lines/product-progress.json` |
| **B（D852）** | `docs/synova/product-lines/product-lines.yaml`（**只加验收点，不增线**）、`docs/synova/product-lines/preconditions.yaml`（如涉及 spill/时间窗参数）、`docs/synova/coordination/`（§6-5 落地记录） |
| 共享 | **各自**的 `task-state/D85x.json`、brief、memory note |
| **不得碰** | `.github/workflows/ci.yml`（A 件 CT-70 独占）、`scripts/control-tower/**`（D846-D848 地盘） |

## B-4 纪律（照做）

- 派单件 §〇-c **权威件回执**：引用研究院件必须写全名 + 版本；过一遍 **9 项撤回清单**（禁复活旧说法）；
- **判据**：离散计数三档 + `evidenceCmd`；**禁把 L3（grep 型，实测 3/5=60%）当验收依据**；
- **禁百分比**：`grep` 证明派生输出与文档中不再出现总完成度百分比；
- 先红后绿夹具两次原始输出；队内只说"自验"；交付标 `可提请独立审计`；
- PR ≤12 文件 + 单域；`synova-commit` 自带 push；**开工第一步重新核行号**。

## B-5 完成判据

- [ ] D850：派生器输出三档离散计数 + 每档 `evidenceCmd`；**面板 total_lines 26→29**；grep 证明无百分比
- [ ] D852：线 8/4/3/25 各**新增**验收点（写实到可证伪）+ live/restart 清单成文
- [ ] 两次夹具（超限/正常 或 健康/未接/缺 三档判定）先红后绿
- [ ] 交回三件：diff / 自验结论 / 遗留清单

---

## §〇-c 权威件回执（本派单）

| 项 | 内容 |
|---|---|
| 依据权威件 | 创始人裁定表 §6 五项（`docs/synova/coordination/创始人裁定表-§6五项-20260920.md`，2026-09-20 签署）｜总纲 §6（`docs/authority/产品完成度定义与推进总纲-20260918.md`）｜度量方案（研究院件，ingest 后路径 `docs/synova/research/研究院交接-20260918/`） |
| 禁旧说法自检 | 未命中 9 项撤回（本件不涉 DSH 存储/遥测/界面复用；不宣称"26 线全绿=完成"） |
| 8 条禁做自检 | 未涉 DSH 代码依赖；未把测量值塞 `agent_memory`；未把界面当交付物 |
| L3 限制 | 本派单**明确禁用** grep 型静态判据作验收 |
