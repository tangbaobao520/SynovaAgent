# 派单：D522/D523 P0-1 修复 — rebase 至 main + 保留 D518 prod 修复（K3 审计阻断项）

> 派单: CTO | 2026-08-25 | 认领: 🛠 编码 session（切片 B impl 作者）| slice: L1-B
> 来源: K3 切片 B 审计 P0-1（报告 `docs/synova/audit-reports/2026-08-25-D522-D523.md`）
> 状态: **阻断合并**——不修不合并，审计分支 audit/k3-20260825-d522-d523 待命

---

## 问题（P0-1：分支基陈旧，合并会回归 D518 修复）

`feat/d523-win-double-click` 分支（含 D522+D523 全部 commit，tip `fa0d3496`）基于 `5d6f487d`（切片 B 派单），**落后 main 68 commit**（切片 A 已合入）。

分支版 `electron/backend-spawn.cjs` L58-62 的 `buildCommand('prod')` 仍是旧契约：
```js
? { bin: 'node', args: ['dist/src/index.js'] }   // 旧（分支版 L62）
```
main 上 D518 修复后的新契约（已合入，K3 切片 A 复跑证实）：
```js
buildCommand('prod') = process.execPath + ['dist/backend.mjs']   // backend-spawn.cjs:69
+ ELECTRON_RUN_AS_NODE=1 注入（:115）
+ stdio inherit 死锁修复（:131 logFile ? pipe : inherit）
```

**后果**：直接合并 → 回归 D518 三项 prod 修复（或测试 L233 断言旧命令失败）。

**交付声明未披露基点陈旧**——审计发现的第二个问题（诚实性缺口，一并改进）。

## 修复要求（K3 指定 + CTO 确认）

1. **rebase/merge 至 origin/main**（推荐 merge 保留历史，K3 说"须 rebase"，二选一，最终树一致即可）
2. **冲突解决原则**（backend-spawn.cjs 是唯一冲突热点）：
   - **保留 main 侧**：D518 三项 prod 修复（buildCommand prod=dist/backend.mjs + ELECTRON_RUN_AS_NODE + stdio inherit）
   - **保留分支侧**：切片 B 的 teardown 新增（`signalTree`/`taskkillProcessTree`/`makeStop`/SIGKILL 升级/幂等/进程组）
3. **更新测试断言**：`tests/electron/backend-spawn.test.ts` L233 旧断言（`prod.args === ['dist/src/index.js']`）→ 新契约（`['dist/backend.mjs']`）
4. **复跑全量**：tests/electron/ 4 文件 39/39 绿（D522 15/15 + D523 7/7 + D518 既有无回归）
5. **审计基线复跑**：`python3 scripts/audit/audit-check.py --full` 与 main 一致（PASS:2 WARN:894 FAIL:460，零新增）

## 验证（物理可复现）

- `grep -n "dist/src/index.js" electron/backend-spawn.cjs tests/electron/` → 零结果
- `grep -n "dist/backend.mjs" electron/backend-spawn.cjs` → 在 buildCommand prod 分支
- tests/electron/ 39/39 绿 + audit-check.py 基线一致

## 写集约束

- 可碰：`electron/backend-spawn.cjs`（合并冲突）、`tests/electron/backend-spawn.test.ts`（断言更新）、task-state/D522.json + D523.json（impl 段补 rebase 后信息）
- **不碰**：`src/`、`scripts/audit/`、`.github/workflows/desktop-build.yml`（C1 已修）
- 合并顺序（K3 P2-3）：**先 D522 后 D523**（D523 叠于 D522）

## 完成后

- 复跑全绿 + evidence 落盘（截图/日志）
- 提 K3 复审（只核：grep 清零 + 39/39 绿 + 树终验）
- 复审通过 → 合并 impl → 合并审计分支（audit/k3-20260825-d522-d523）→ D522/D523 转 PASS
