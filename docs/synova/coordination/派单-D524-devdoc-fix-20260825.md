# 派单：D524 — D518 dev doc prod 契约返修（K3 审计 C2 条件项）

> 派单: CTO | 2026-08-25 | 认领: 📋 dev-doc | slice: L1-A | fix_task_id: 归属 D518
> 来源: K3 切片 A 审计 P1-1（报告 `docs/synova/audit-reports/2026-08-25-D517-D519.md`）
> 铁律: 八b 审计闭环——K3 出问题另起 FIX 任务，禁止直接改原任务（D518 已 audited）

---

## 问题（M7 文档-实现漂移）

D518 dev doc `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md` 三处仍写旧 prod 契约：

| 位置 | 旧契约（现文档） | 实现实为（已在 main，PR #166） |
|---|---|---|
| §2（:32） | `node dist/src/index.js` | `dist/backend.mjs` + 包内 Electron node 模式 |
| §5.1（:55-58） | 同上 | 同上（buildCommand prod = `process.execPath + dist/backend.mjs`，backend-spawn.cjs:69） |
| §7（:76） | 规定测试断言 `buildCommand('prod') === node dist/src/index.js` | 测试断言 `prod.args === ['dist/backend.mjs']`（backend-spawn.test.ts:235） |

**后果**：照 dev doc §7 写测试现在会红——文档是"最后一个过时副本"。

**缓解事实（已同步的部分）**：brief Q2、task-state evidence、build-synova.cjs 头注释、runbook 均已同步新契约——漂移被约束在 dev doc 单文件内。

## 要求

1. 回填 D518 dev doc §2/§5.1/§7 三处为**新 prod 契约**（`dist/backend.mjs` + 包内 Electron node 模式，含 `ELECTRON_RUN_AS_NODE=1` 注入说明——backend-spawn.cjs:115）
2. §7 的测试断言规范同步为新契约（`prod.args === ['dist/backend.mjs']`）
3. 若文档其他章节有同类旧契约引用，一并回填（grep `node dist/src/index.js` 清零）

## 验证（物理可复现）

- `grep -n "node dist/src/index.js" docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md` → 零结果
- `grep -n "dist/backend.mjs" docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md` → 三处（§2/§5.1/§7）
- 与 backend-spawn.cjs:69 的 buildCommand prod 契约一致（读实际代码，不凭记忆）

## 写集约束

- 只碰：`docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D518-single-entry-20260824.md`（+ task-state/D524.json 回填）
- 不碰：`electron/`、`tests/`（实现已验证，K3 物理复跑通过）、`scripts/audit/`

## 完成后

- task-state/D524.json 回填（impl_done + commit hash）
- 提 K3 复审 C2 证据（只核两项：grep 清零 + 三处回填）
- C2 转 PASS 后，D517/D518/D519 从 CONDITIONAL PASS 转 PASS
