# 派单：D525 + D526 — 控制塔红态清理 + CI 密封告警（提交链路线收尾）

> 派单: 主 CTO | 2026-08-25 | 认领: 🧭 并行 CTO session（提交链路线，D508→D520→D521 连续上下文）
> 来源: K3 D521 审计遗留（报告 docs/synova/audit-reports/2026-08-25-D521.md）
> 分工依据: 提交链路线连续性 + 防与主 CTO 撞车（主 CTO 专注切片 B 收尾）

---

## D525：P1-1 — synova-commit.test.sh 红态修复（控制塔测试漂移）

**问题**：`tests/control-tower/synova-commit.test.sh` 在 main 上自 2026-08-23 `f3e08ebd` 起红——D507 并行隔离门禁段在 D508 合并版被移除，**配对测试未更新**（main 上该测试仍含 6 个 D507 相关断言）。且该测试**不在 CI 密封清单 15 项内**——红态无任何防线感知。

**要求**：
1. 更新 `synova-commit.test.sh`：移除/替换已随 D508 消失的 D507 断言（6 处），与当前 synova-commit 行为对齐
2. 本地跑绿（`bash tests/control-tower/synova-commit.test.sh` exit 0）
3. 检查该测试是否应纳入 CI 密封清单（与 D508 后的门禁语义一致）——若纳入，同步更新 CI canary 清单

**验证**：测试 exit 0；CI 密封清单与测试文件数对账一致

## D526：L4 — CI canary 密封清单漂移告警

**问题**：CI canary 只覆盖 15 密封测试，而仓库测试文件 62 个——**62 vs 15 漂移 47 项无任何告警**，新测试加入/测试改名都不被感知（D525 的红态就是因此漏网）。

**要求**：
1. 加「测试文件数 vs CI 密封清单数」漂移检测：新测试文件加入后，若不在 CI 清单 → 告警（或 CI 检查）
2. 落点建议：CI canary 步骤或 pre-commit 检查（加一行对比 `find tests/ -name '*.test.*' | wc -l` vs CI 清单计数）
3. 告警而非阻断（防误伤），但必须可见（::warning 或日志）

**验证**：新增一个测试文件 → 告警出现；清单更新 → 告警消失

## 写集约束

- 可碰：`tests/control-tower/synova-commit.test.sh`、`.github/workflows/`（canary）、scripts/（如需加漂移检查）
- **不碰**：`src/`、`scripts/audit/`、切片 B 相关（D522/D523 由编码 session + 主 CTO 处理）
- 控制塔脚本变更前加载 `ctrl-tower-change` / `windows-compat` skill

## 完成后

- D525/D526 task-state 回填（impl_done + commit hash + 测试证据）
- 提 K3 复审（D521 审计遗留闭环）
- 分支 push 后开 PR，主 CTO 合并
