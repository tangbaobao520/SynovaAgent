# 派单：D533 — CI 调试可达性根治（收敛 3 项，并行 CTO 执行）

> 派单: 主 CTO | 2026-08-25 | 认领: 🧭 并行 CTO（控制塔线）
> 背景: D529 复盘后**先审计代码**再定方案——初版 5 项中 ③④ 经审计非真正问题（无 CI bot 提交/挂起根因已消除），按"一类一机制防臃肿"收敛为 3 项
> dev doc: `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D533-ci-diagnostics-20260825.md`

## DSH 借鉴核查（强制章节）

1. **施工图四色归属**：🟡 搬走/控制塔（凭证/CRLF 是开发环境治理）
2. **借鉴边界判定**：无 DSH 代码可借鉴（GitHub Actions 凭证/CRLF 是 GitHub 生态，DSH 无对应）
3. **DSH 源码参考**：无

## 3 项根治（dev doc 详设）

### ① 凭证共享：GitHub token 落 .credentials.yaml（真正问题：日志可达性）
- 创建/补 `.credentials.yaml` 的 `GITHUB_TOKEN` 条目（权限 actions:read+repo，0600，不 commit）——token 由主 CTO 提供
- 可选：装 gh CLI（darwin-arm64 二进制到 ~/.local/bin/gh）——便利工具非必须
- 验证：`curl -H "Authorization: token $GITHUB_TOKEN" .../actions/jobs/<id>/logs` 拉到最近失败 run 日志

### ② CRLF 治本：.gitattributes 加 eol 规则
- `.gitattributes` 加 `*.sh text eol=lf` + `*.py text eol=lf`（一行规则防一类）
- `git add --renormalize` 规范化 15 个脏文件
- 验证：`git status` 零噪音 + `git diff origin/main` 干净

### ③ debug 回传纪律（文档级）
- 控制塔文档加：未来 CI debug 回传必须推 `ci-debug/*` 独立分支；首选 curl/gh 日志通道
- 验证：文档条目存在

## 明确不做（防膨胀）

- ~~CI 挂起探针~~（挂起根因已消除：prebuild-install 无编译）
- ~~机器人 merge 豁免~~（无 CI bot 提交）
- ~~gh CLI 强制~~（可选工具）

## 写集约束

- 可碰：`.credentials.yaml`（token，本地）、`.gitattributes`、`tests/control-tower/*.test.sh`（renormalize）、控制塔文档
- **不碰**：`src/`、`scripts/audit/`、**不新增控制塔脚本**

## 交付要求

1. 每项验证物理可复现（curl 日志输出 / git status / 文档 grep）
2. task-state/D533.json 回填（impl.commit + 证据）
3. 分支 push 开 PR → 主 CTO 合并
