# 派单：D538 — 前端交互实现：左栏 Codex 风格（产品独有能力导航）

> 派单: CTO | 2026-08-26 | 认领: 🛠 编码 session（桌面端线，electron-renderer/ 归它）
> 来源: Win 侧 Codex 推送的设计规格（已合 main）`docs/synova/coordination/SYNOVA-IMPL-DSH-前端交互设计-左栏Codex风格-v1.md`
> 流程: 编码 session 按设计规格实现 → DSH 预审 → K3 审计 → CTO 合并
> 上一轮教训: 切片 A/B/C（electron-renderer 已有 LeftPanel/RightPanel/app-store 基线）；验收物理可复现（文档 8 条可证伪）

---

## 写前核实（SOP §〇c ①，6 项已核实）

- [x] ① 任务来源/依赖: Win 侧 Codex 推送设计规格（创始人确认"左栏 Codex 风格"）；依赖 electron-renderer 既有组件
- [x] ② task-state 最新状态: D538 新分配；D537（控制塔）/D536（部署）不重叠
- [x] ③ 基线资产实际存在: LeftPanel.tsx（ICON_ITEMS L11 + 折叠态 44px L4）/RightPanel.tsx/app-store.ts（userRole L38 + currentReportId L52，无 selectedCap）——grep 确认；lucide-react 未装（package.json 无）
- [x] ④ DSH 借鉴核查: 对标 **Codex（OpenAI）风格**（非 DSH）；lucide-react 开源图标库——见下
- [x] ⑤ 写集重叠: D537/D536 不重叠（前端 vs 控制塔/部署）；D538 已分配
- [x] ⑥ 上一轮教训: 验收物理可复现（文档已给 8 条可证伪判据）

## DSH 借鉴核查（§〇b 三步）

1. **施工图四色归属**：🟢 死守（品牌表层：electron-renderer/ 是客户可见的桌面端 UI，施工图 §3.1）
2. **借鉴边界判定**：**无 DSH 代码可借鉴**——本设计对标 **Codex（OpenAI 的 CLI 工具）的"一行一个导航项"UI 风格**（创始人确认），不是 DSH；图标库 lucide-react 是开源（MIT），非 DSH。显式写原因防猜测
3. **DSH 源码参考**：无（参考对象 = Codex UI 风格 + 设计规格文档 §一/§五）

## 任务（按设计规格实现）

**设计规格**（必读，已合 main）：`docs/synova/coordination/SYNOVA-IMPL-DSH-前端交互设计-左栏Codex风格-v1.md`

**核心**：左栏顶部 = **产品独有能力主导航**（Codex 风格：一行一个导航项，去掉折叠卡片 accordion），Lucide 线性图标（16px 单色 currentColor，无 emoji）。

**实现清单**（设计规格 §二/§三/§四/§五）：
1. **LeftPanel.tsx**：新增"产品独有能力位"（搜索之下、最近对话/工作区之上）——4 个能力导航项：主动触达(Radar)/五循环状态(RefreshCw)/Action闭环(ListChecks)/GA协同(Users) + 数字角标（红/橙/绿）+ 通用导航（对话/工单/设置）
2. **RightPanel.tsx**：右栏联动状态机 `selectedCap ∈ {null,reach,loops,action,ga}`——null=默认三标签；reach/loops/action/ga=各能力详情（信号 Story/5循环状态灯/行动项/GA校准面板）
3. **app-store.ts**：新增 `selectedCap` + `setSelectedCap`（复用 userRole 判 GA 权限）
4. **lucide-react**：`npm i lucide-react`，图标按设计规格 §五映射表引入
5. **global.css**：`.cap-item/.cap-item:hover/.cap-item.active/.cap-badge/.cap-chev/.cap-ico` 样式（对齐现有 token，勿硬编码颜色）

## 写集约束

- **可碰**：electron-renderer/src/components/LeftPanel.tsx、RightPanel.tsx、electron-renderer/src/stores/app-store.ts、electron-renderer/src/styles/global.css、electron-renderer/package.json（lucide-react）
- **不碰**：src/（后端）、scripts/audit/、后端接口（设计规格 §4.2 的接口**当前可能不存在**——前端只做"能显示的结构"，接口接入另议，**不伪造接口**）
- **图标规范**（设计规格 §五，硬约束）：Lucide 线性 stroke 图标、16px、单色 currentColor、**无 emoji 无彩色填充**；`as any` 零容忍（铁律 38）

## 验收（设计规格 §六，8 条可证伪，直接引用）

1. 左栏：产品独有能力位在搜索之下、最近对话/工作区之上，4 能力导航项一行一个，无折叠卡片边框
2. 图标风格：Lucide 线性 SVG（stroke/16px/currentColor），**无 emoji**；能力 Radar/RefreshCw/ListChecks/Users，通用 MessageSquare/Ticket/Settings，折叠 ChevronRight
3. 右栏联动：点"主动触达"→信号 Story；"五循环状态"→5循环灯；"Action闭环"→行动项；"GA协同"→GA校准面板
4. 取消选中：再点同一项→右栏回"行动/报告/工单"三标签
5. GA 权限：非 GA 角色看不到/置灰"GA 协同"
6. 折叠态：左栏收起后为 Lucide 线性图标条，点击图标右栏联动
7. 代码质量：as any=0；新导出有调用方（铁律 0-2）；异常有 log+degraded（铁律 24+31）；测试有 expect（铁律 48）
8. 术语：界面无 "FDE"，统一 "GA"

## 交付要求

1. 实现 + 测试（组件测试覆盖 selectedCap 状态机 + 权限 + 联动）+ task-state/D538.json 回填
2. 走 DSH 预审 + K3 审计（设计规格 §七）
3. 分支 push 开 PR → 主 CTO 合并
4. 验收 = 设计规格 8 条逐条对照（截图/测试断言，非声称）

---

## 写后自检（SOP §〇c ③，8 项）

- [x] ① 验证点编号: N/A（前端交互实现，非 product-progress 验证点——对应设计规格 §六 8 条验收）
- [x] ② D# 未占用: D538 alloc 分配确认
- [x] ③ 依赖链: D538 依赖设计规格（已合 main）+ electron-renderer 既有组件；无上游任务阻塞
- [x] ④ DSH 借鉴核查三步完整: 🟢 品牌表层/无 DSH 借鉴（对标 Codex 风格 + lucide-react）/无源码参考
- [x] ⑤ 现状材料全部核实过: LeftPanel(ICON_ITEMS/折叠态)/RightPanel/app-store(无 selectedCap)/lucide-react 未装——grep/ls 物理确认
- [x] ⑥ 验收物理可复现: 设计规格 8 条可证伪（左栏/图标/联动/取消/权限/折叠/质量/术语）——非文档声称
- [x] ⑦ 术语一致: "GA"（文档已全局替换 FDE→GA）——派单沿用 GA
- [x] ⑧ 无遗漏: 执行方（编码 session）/lucide-react 安装/接口不伪造（§4.2）/验收 8 条/预审+审计——齐全

**交付门槛**：编码 session 拿到可直接开工（设计规格 + 组件现状 + 验收 8 条已写死）+ K3 可核（物理断言）。
