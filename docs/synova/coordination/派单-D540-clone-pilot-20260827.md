# 派单：独立 clone 试点 + 影子提交 clone 环境验证（D540）

> 派单: CTO | 2026-08-27 | 认领: 📋 dev-doc（写 spec）→ 编码执行
> 方法论: 多 Agent 协作治理定稿 v3——层1 独立 clone（单机隔离治本）+ 层2 远端单源化（跨机协调）
> 流程: dev-doc 出 spec → 编码实现 → K3 审计 → CTO 合并
> 上一轮教训: 影子提交曾在 D530 重写 post-commit 时丢失登记段（D485 实证未生成），**D537 #4（e4cb41ab，08-27 01:22）已恢复**——本单步骤 0 是「验证独立 clone 环境照常」而非「修复失效」

---

## 写前核实（强制清单 — §〇c ①）

- [x] ① 任务来源/依赖: 多 Agent 协作治理定稿 v3（含 Codex 三缺口修正 + 创始人 4 补充）。上游：D539（worktree 开工端硬门禁）已合 main；D515/D537（软告警/提交端硬阻断）已合 main。
- [x] ② task-state 最新状态: D540 已 `alloc-task-id.sh` 登记（status=claimed），空壳 `task-state/D540.json`。
- [x] ③ 基线资产实际存在（物理确认）:
  - `scripts/hooks/post-commit.sh` — 做 bypass 判定（detected-bypass/possible-bypass），**不生成影子提交**
  - `scripts/control-tower/synova-commit` L573/647/669/685 — 写 COMMITTED 到 bypass.log + 证据链随提交入库（D451/D521 影子提交机制所在）
  - `scripts/install-hooks.sh` — 存在（clone 后 hooks 安装用）
  - `scripts/control-tower/worktree-manager.py` — 现有 worktree 工具（试点期保留）
- [x] ④ DSH 借鉴核查: 见下「DSH 借鉴核查」章节（独立 clone 是 git 原生业界标准，无 DSH 借鉴）。
- [x] ⑤ 写集重叠检查: D540 未占用（刚取号）；与在途 D538（前端左栏）不重叠。
- [x] ⑥ 上一轮教训: 影子提交 hook 失效（补充 1）+ 主工作区双写者污染（补充 3）+ clone git 配置缺失（补充 2）。

## DSH 借鉴核查（强制章节 — §〇b 三步）

1. **施工图四色归属**：本单是**协作模型切换**（worktree → 独立 clone），不是产品模块，不属于施工图四色清单任何一类——它是「工程治理层」的协作方式变更（🟡 治理层）。
2. **借鉴边界判定**：**无 DSH 借鉴**——独立 clone 是 git 原生能力（业界标准），影子提交是 Synova 自有机制（D451/D521），均不涉及 DSH 包/范式。防执行方猜测：明确写「无 DSH 借鉴，独立 clone = git 原生，影子提交 = Synova 自有」。
3. **DSH 源码参考**：无。

> 红线：借鉴 = 读范式自研；本单无 DSH 借鉴，不引代码、不 npm install。

---

## 切片定义（CTO 已定，dev-doc 复核）

| 切片 | 用户可见价值 | 验证锚点 | 依赖 |
|---|---|---|---|
| D540（本次） | 单机并行隔离切换到独立 clone + 跨机单源化，影子提交证据链恢复 | 影子提交恢复 + 并发冲突 0 + 拉平 1 次 + 门禁全绿 | 上游 D539/D515/D537 已合 main |

## 现状材料（dev-doc 必读，全部在 main，先读实际代码不凭记忆）

| 资产 | 位置 | 状态 | 与本单的关系 |
|---|---|---|---|
| 影子提交机制 | `scripts/hooks/post-commit.sh` L69-84（D521/不变量2） | **生成影子提交** `git commit -m "chore: bypass COMMITTED 登记"`，D537 #4 已恢复 | clone 环境需 identity 配置（L86 降级条件「identity 未配置?」） |
| synova-commit 登记 | `scripts/control-tower/synova-commit` L573/647/669 | 写 COMMITTED 到 bypass.log（synova-commit 路径，非影子提交） | 与 hook 层登记成对，两条路径 |
| post-commit 判定 | `scripts/hooks/post-commit.sh` L26-60 | 做 bypass 判定（detected/possible-bypass） | 影子提交在 PASS_WAY≠0 时触发 |
| hooks 安装 | `scripts/install-hooks.sh` | 存在 | clone 后安装用（适配 1） |
| worktree 工具 | `scripts/control-tower/worktree-manager.py` | create/finish/list | 试点期保留，clone 推广后退役 |
| verify-parallel | `scripts/control-tower/verify-parallel.sh` | 本地 --scan-today | 移 CI/PR 阶段（缺口 2） |

## D540：clone-pilot-shadow-commit（独立 clone 试点 + 影子提交 clone 验证）

**目标**：① 验证影子提交在独立 clone 环境照常工作（identity 配置为前置）；② 单机并行隔离切换到独立 clone（任务一律 clone + 主工作区 Codex 专用）；③ 跨机共享文件单源化。

**依赖**：无（上游 D539/D515/D537 已合 main）。

**spec 必答题**（dev-doc 必须回答，缺一返修）：

1. **影子提交 clone 环境验证（步骤 0，前置）**：影子提交机制已在 D537 #4 恢复（post-commit.sh L69-84）。本单验证**独立 clone 环境**照常工作——关键风险是 clone 的 `user.name/user.email` 未配置导致 L86 降级「identity 未配置?」。给出：clone 初始化时 identity/凭据配置的正确顺序（影子提交触发前必须配好）+ worktree 和 clone 各验一次真实 commit 生成影子提交的断言。

2. **clone 生命周期 + git 配置初始化（补充 2）**：clone 创建后与 install-hooks 同批执行 `user.name/user.email`（Synova-Win 系）、凭据（token/credential helper）、`core.quotepath=false`（中文文件名）。给出 clone 创建/工作/结束/清理的完整流程设计。

3. **三处控制塔适配（不是重构）**：① hooks 安装（clone 后一次）；② bypass 影子提交自然进 main（删 post-merge 脚本，复用 V5.0.4 机制）；③ 协调文件（current-brief/task-state/session-registry）git 同步。

4. **主工作区单写者（补充 3）**：主工作区 = Codex 专用（dev doc/台账/协调），**所有任务（含串行）一律 clone**。给出"任务 session 在主工作区操作被阻断 + 引导 clone"的设计（复用 D539 开工端阻断还是新检测？）。

5. **verify-parallel 移 CI/PR（缺口 2）**：本地 pre-push 不再强制 --scan-today；CI/PR 阶段对比 base..head 写集 × 今日已合 PR 写集。给出 CI 阶段实现 + fetch-depth:0 依赖确认。

**验收**（物理可复现，禁止静态 grep 冒充）：

- **影子提交断言**：worktree + clone 各一次真实 commit，post-commit 自动生成影子提交（bypass.log 含 COMMITTED + 影子提交随 PR 进 main）。
- **隔离断言**：双 session 并行，各自 clone 零干扰（B 的 index/current-brief 零变化）。
- **并发冲突断言**：试点任务全程幽灵对象/脏 index/覆盖 = 0 次。
- **门禁断言**：13 组门禁在 clone 内全绿 + CI 权威不变。
- **接线断言**：新函数 grep 有真实生产调用点（测试调用不计）。

## 写集约束

- **可碰**：`scripts/hooks/post-commit.sh`、`scripts/control-tower/synova-commit`、`scripts/install-hooks.sh`、verify-parallel 相关、`scripts/control-tower/worktree-manager.py`（试点期）。
- **不碰**：`src/`（产品代码，红线）、`scripts/audit/`（K3 专属，红线）、13 组门禁本体（`pre-commit-check.sh`）。
- **防膨胀（红线）**：零新组件、零新守护进程/服务/launchd；**删 post-merge 脚本（不需要，影子提交已覆盖）**；禁止引入 DSH 依赖。

## 切片级审计

- 本单 1 个 D 完成后一次提审（K3 审 D540）。
- task-state 加 `"slice": "clone-pilot"` 字段。
- 审计验收 = 验证锚点「影子提交恢复 + 并发 0 + 拉平 1 + 门禁全绿」从 claimed → verified。

## 给 dev-doc 的交付要求

1. **spec 文件命名**：`docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D540-clone-pilot-shadow-commit-20260827.md`
2. **验收物理可复现**：每条验收带「命令 + 断言 + 预期输出」，禁止文档声称冒充（M2 红线）。
3. **影子提交验证落到 clone 环境**：不是笼统"验证"，是明确「clone identity 配置 → 影子提交触发 → bypass.log 含 COMMITTED」的完整断言链。
4. **诚实声明**：clone 的磁盘/时间成本用实测值，不拍脑袋。

---

## 写后自检（强制清单 — §〇c ③）

- [x] ① 验证锚点正确（影子提交恢复 + 并发 0 + 拉平 1 + 门禁全绿）
- [x] ② D# 未占用（alloc-task-id 确认：D540，task-state 已登记）
- [x] ③ 依赖链正确（上游 D539/D515/D537 已合 main；本单内部：步骤 0 影子提交修复 → 三处适配 → 试点验证）
- [x] ④ DSH 借鉴核查三步完整（四色 🟡治理层 + 借鉴边界「无 DSH 借鉴，独立 clone=git 原生」+ 源码「无」）
- [x] ⑤ 现状材料全部核实过（synova-commit L573/647/669、post-commit.sh、install-hooks.sh、worktree-manager.py 均 ls/grep 确认）
- [x] ⑥ 验收物理可复现（影子提交/隔离/并发/门禁/接线，均命令+断言）
- [x] ⑦ 术语一致（「独立 clone」= git 原生，「影子提交」= D451/D521 机制，不混用）
- [x] ⑧ 无遗漏（执行方=dev-doc、影子提交 clone 验证、补充 2/3/4 全覆盖、审计验收项）

**交付门槛**：dev-doc 拿到派单可直接开工（基线/依赖/写集/借鉴全给）+ K3 审计可核（验收物理可复现）。

---

## 派单说明（给创始人复制——自包含，零查找）

```
【派单】独立 clone 试点 + 影子提交 clone 验证（D540）
> 认领: 📋 dev-doc（写 spec）→ 编码执行

## 背景（一句话）
多 Agent 协作切独立 clone（业界标准治本），前置验证影子提交在 clone 环境照常（D537 #4 已恢复机制，只需验 clone 环境）。

## 现状材料（执行方必读，均在 main）
- scripts/hooks/post-commit.sh L69-84 —— 影子提交生成机制（D521/不变量2，D537 #4 已恢复）
- scripts/control-tower/synova-commit L573/647/669 —— 写 COMMITTED 到 bypass.log（synova-commit 路径，非影子提交）
- scripts/install-hooks.sh —— clone 后 hooks 安装用
- scripts/control-tower/worktree-manager.py —— 试点期保留

## spec 必答题
1. 影子提交 clone 验证：clone identity 配置是触发前提（L86 降级「identity 未配置?」）+ worktree/clone 各验一次
2. clone 生命周期 + git 配置初始化（user/凭据/quotepath）
3. 三处适配：hooks 安装 / bypass 影子提交自然进 main（删 post-merge 脚本）/ 协调文件 git 同步
4. 主工作区单写者：任务一律 clone，主工作区 Codex 专用
5. verify-parallel 移 CI/PR

## 写集约束
- 可碰: scripts/hooks/post-commit.sh、scripts/control-tower/synova-commit、scripts/install-hooks.sh、verify-parallel 相关、worktree-manager.py
- 不碰: src/、scripts/audit/、13 组门禁本体（pre-commit-check.sh）
- 防膨胀: 零新组件，删 post-merge 脚本（影子提交已覆盖），禁止引入 DSH 依赖

## 验收（物理可复现）
- 影子提交断言: worktree + clone 各一次真实 commit 自动生成影子提交
- 并发冲突断言: 试点全程 0 次幽灵对象/脏 index/覆盖
- 拉平断言: 每任务开工 pull 1 次
- 门禁断言: 13 组 clone 内全绿 + CI 权威不变

## 交付要求
1. spec 命名: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D540-clone-pilot-shadow-commit-20260827.md
2. 验收物理可复现（命令+断言）
3. 影子提交验证落到 clone 环境（identity 配置 → 触发 → bypass.log 含 COMMITTED 断言链）
```
