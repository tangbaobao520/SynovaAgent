# 派单 — D578 FIX-D572：验收点 1-2 Windows 真机实测（P0 假绿本体修复 + 1-6 探针指纹折入）

> 派单: CTO | 2026-09-05 | 认领: Win 线 session（真机在 Windows，需创始人提供目标机访问——D536 卡点「GUI dsh-ssh 未配置 Windows 目标机」由此解）
> 来源: D572 K3 全量复核 FAIL（docs/synova/audit-reports/2026-09-04-D572-line1-desktop.md）P0-1——验收点 1-2「Windows 双击安装 → 启动 → 出窗」从未真机实测，verified 为兑换机制假绿（机制侧已由 D576/CT-53 修复）；本任务修复**本体**：把实测真做出来。审计闭环铁律（台账 §〇b）：另起 FIX 任务，禁改原任务。
> 上一轮教训: D572 P0（假绿）/ D536 win_status waiting（无目标机）/ P1-2 计时证据不可复核。

## 写前核实（6 项，CTO 已核）

- ① 来源/依赖: D572 审计 FAIL（P0）；兑换机制侧 D576 已 close；本单修证据本体
- ② task-state: D572 = audited（verdict FAIL，fix_task_id=D578 由 CTO 回填）；D523/D536 waiting
- ③ 基线资产（origin/main 全部 git cat-file HIT）: scripts/desktop/win-install-verify.ps1、scripts/desktop/first-diagnosis-timing.sh、task-state/D517.json（CI artifact run 32870900391 含 Win exe）、task-state/D523.json（DS1/DS2 pending 自述）、task-state/D536.json（win_status waiting）
- ④ DSH 借鉴核查: 无（Windows 真机 GUI 实测任务，纯验证 + 一处脚本指纹补丁；施工图四色不适用）
- ⑤ 写集重叠: D578/D579 均为 alloc 新号（claimed），无在途任务占用 desktop 验证域
- ⑥ 上一轮教训: 引用上栏；另 D316/D551——环境注记先行，不可复现项如实标注

## 任务定义（验证点 1-2 为主，1-6/1-7 连带）

| # | 内容 | 对应验收点 | 性质 |
|---|---|---|---|
| A | **Windows 真机实测**: 双击安装 → 启动 → 出窗（win-install-verify.ps1 四断言全绿） | 1-2（P0） | 本体 |
| B | **1-6 探针实例指纹绑定**（折入，CTO 判定：同域小改——scripts/desktop/first-diagnosis-timing.sh 的 probe_healthz（L119 起）打 127.0.0.1:18790 任意在跑服务都响应，未绑定本次启动实例；加实例指纹校验，如启动时随机 token 注入 healthz 查询或记录启动 PID 校验） | 1-6（P1） | 折入 |
| C | **1-7 NSIS 升级路径实测**（升级/重装不丢数据，Win 侧从未测；upgrade-data-verify Win 适配如有缺口如实登记不硬修） | 1-7（P2 连带） | 连带 |

## 现状材料（执行方必读，全部 origin/main）

| 资产 | 位置 | 关系 |
|---|---|---|
| Win 验证脚本（D523 交付） | scripts/desktop/win-install-verify.ps1 | 直接执行；若覆盖不了「出窗」断言，允许扩展脚本（写集内） |
| 被测产物 | CI run 32870900391 artifact（task-state/D517.json DS3 回填 URL，Win exe + macOS 包） | 用最新 main 的 desktop-build CI artifact（重跑一次 CI 取最新 exe 亦可，优先） |
| D572 审计报告（P0 物理链） | docs/synova/audit-reports/2026-09-04-D572-line1-desktop.md §一/§二 | 按 P0-1 五步物理链逐条反证 |
| 计时脚本（折入 B 项） | scripts/desktop/first-diagnosis-timing.sh（probe_healthz L119 起） | 指纹绑定改造对象 |
| D523/D536 waiting 自述 | task-state/D523.json、task-state/D536.json | 「诚实 waiting」的原始记录，本任务终结它 |

## 验收（物理可复现，禁文档声称）

1. Win 真机跑 win-install-verify.ps1（或其扩展版）exit 0 + 四断言逐条落盘 + 出窗截图（≥1 张，含任务栏时间）
2. evidence 落盘 **docs/synova/product-lines/evidence/D578-win-real-machine/**（git 跟踪路径——根级 evidence/ 被 .gitignore:76 全局忽略，D577 先例）：断言输出、截图、exe 版本/下载 URL、实测时间、机器标识
3. B 项：改后脚本在本机 dry-run exit 0 + 指纹不匹配场景的失败路径测试（red 一次 + green 恢复）
4. task-state/D578.json 回填 impl（evidence 指针全部指向 git 跟踪路径）；1-2 的兑换仍由 CTO 走 redeem + K3 复核

## 写集约束

- 可碰: scripts/desktop/win-install-verify.ps1（扩展断言）、scripts/desktop/first-diagnosis-timing.sh（指纹绑定）、docs/synova/product-lines/evidence/D578-win-real-machine/**、task-state/D578.json、.claude/task-briefs/2026-09-05-D578-*
- **不碰**: src/、electron-renderer/、门禁脚本（scripts/control-tower/、pre-commit*）、scripts/audit/、product-lines.yaml（兑换由 CTO）
- 门禁脚本零改动 → 无需 VERSION bump（CT-42 不触发）

## 交付要求

1. evidence 全部 git 跟踪（M4 防线：不在 git 的证据=不可复核=D572 同型假绿温床）
2. 环境注记先行：Windows 目标机不可得时，立即停下登记 blocked，不伪造
3. 治理脚本如改动（first-diagnosis-timing.sh 属 workflow 脚本）→ commit 引用 memory/notes 四态 Note（铁律 49）
