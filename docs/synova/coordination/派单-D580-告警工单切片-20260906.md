# 派单 — D580 08 告警工单切片：8-2 工单入库断言 + 8-3 去重键稳定化 + 8-4 状态机 API

> 派单: CTO | 2026-09-06 | 认领: dev-doc 写 spec → DSH 编码线实现（D556 先例 spec 先行）
> 来源: 产品线 8（哨兵告警推送）三点缺口 + K3 全链路审计 P2-3（API 不读表，CTO 已物理复核为真）
> 调度依据: D579 spec_done（dev-doc 空出）→ 本单 spec → P0 文档批穿插；编码阶段与 D579 实现写集零重叠可并行
> 上一轮教训: N14/D354（finding.id 含时间戳 → 去重窗口永不生效）；K3 P2-3（写路径与读路径分裂——表有 INSERT 但 API 读内存）

## 一、写前核实（6 项，CTO 已核，行号为 2026-09-06 origin/main 9a778850 现值）

- ① 来源/依赖: 线8 三点（8-2 uncommitted / 8-3 failed / 8-4 uncommitted）；K3 全链路审计 P2-3（持久化分裂）；无在途任务依赖本单
- ② task-state: D580 claimed（alloc 2026-09-06）；D579 spec_done（产品线治理域，与本单零重叠）；D578 claimed（scripts/desktop 域，零重叠）
- ③ 基线资产（全部 git cat-file / grep HIT 实测）:
  - `src/sentinel/runner.ts` L209-216（sentinel_tickets 表 DDL，status 四态枚举 open/acknowledged/resolved/dismissed）、L677/L720（INSERT OR REPLACE 写入路径）、L425-440（D463 自动工单：小时级稳定 id `self-check-YYYYMMDDHH`）、L988-996（自动销单 UPDATE resolved）、L185-187（**N14 缺口现场**：通知去重 = 内存 Map `notificationSentTimestamps`，10min 硬编码，重启即丢）
  - `src/agent/sentinel-service.ts` L253-273（**K3 P2-3 缺口现场**：`getSentinelTickets()` 从 `runner.getRecentResults()` 内存 findings 派生，**不读 sentinel_tickets 表**）
  - `src/routes/sentinel.ts` L88-98（GET /tickets 转发内存派生）、L101-130（POST /alerts/:id/action 是卡片级 confirm/dismiss，**非工单级状态机**）
  - `extensions/sentinels/customer-demand-shift/aggregate.ts` L78/L87（**N14 根因现场**：finding id = `e4-concent-crit-${now.getTime()}` 时间戳生成）
  - 无工单级状态变更 API（acknowledge/dismiss 对 sentinel_tickets 表行）
- ④ **DSH 借鉴核查: 有——B-19 持久化分级哲学**（`dsh-schedule` vs `dsh-jobs` 对照："按死后是否需要复活选路线"；借鉴指引 §4 B-19 明确标注适用「Sentinel 工单/哨兵任务的存储选型准则」）。DSH 源码锚点：`@deepseek-ai/dsh-schedule/lib/index.js`（durable-log + full-log replay + 单一状态迁移权威 L401-405）。落点：通知去重状态按 B-19 判定应走持久化（重启需复活），工单本体已在 SQLite（已符合）。spec 必答 3 裁决去重持久化载体（DB 表 vs journal 文件）。
- ⑤ 写集重叠: D580 新号；src/sentinel/ + extensions/sentinels/ + src/routes/sentinel.ts + tests/sentinel/ 无任何在途认领；与 D579（scripts/product-lines）/D578（scripts/desktop）/P0 文档批（docs）零重叠
- ⑥ 上一轮教训: N14 根因 = id 含时间戳（D354 诊断原文「同问题每轮新 id，5/30 分钟窗口都不生效」）；D339 创始人已裁决 A（窗口 5min，键稳定化前置——本单直接执行裁决，无需再问）；K3 P2-3 教训「写路径与读路径必须同源」；D572「点级结论独立复核」

## 二、任务定义（三点 + 一个横切）

| # | 内容 | 现场证据 | 对应点 | 性质 |
|---|---|---|---|---|
| A | **8-2 工单入库接线统一**: `getSentinelTickets()` 改为读 sentinel_tickets 表（写读同源），保留内存派生为表空时降级 + degraded 标注（铁律 24）；场景断言测试：哨兵 check 产出 critical → 表有行 → GET /tickets 反映该行 | sentinel-service.ts L253-273 vs runner.ts L677 | 8-2 | 主体 |
| B | **8-3 去重键稳定化**: ① aggregate finding.id 去时间戳 → 内容稳定键（sentinelId + finding 类别 + 关键维度值哈希；同问题重跑同 id，窗口去重才生效）② 通知去重状态持久化（内存 Map → DB 表或 journal，重启不丢；窗口 5min 按 D339 裁决 A，常量可配）③ B-19 哲学裁决持久化载体 | aggregate.ts L78/L87 + runner.ts L186/L1134-1142 | 8-3 | 主体 |
| C | **8-4 工单状态机 API**: 新增工单级状态变更端点（POST /api/sentinel/tickets/:id/transition，open→acknowledged→resolved / open→dismissed，非法迁移 409）；全生命周期场景测试（critical 触发 → 工单 open → acknowledge → resolve → GET 只见终态）；自动销单路径回归 | 表 DDL 四态 + L988 自动销单已有，API 缺失 | 8-4 | 主体 |

**注意（B 项波及面）**: finding.id 稳定化改动 aggregate 输出契约——改前必须 grep 全部消费 finding.id 的下游（runner.ts L909 findingById、去重、工单 signal_id 关联 L990 LIKE 匹配）确认稳定键不破坏现有关联语义；如 signal_id 关联依赖旧 id 形态，spec 必答裁决迁移策略。

## 三、spec 必答题（写代码前回答）

1. **finding.id 稳定键的组成**：sentinelId + 类别 + 哪些维度值参与哈希？同一异常「消失后复现」应该算新 finding 还是同 finding（窗口语义）？契约 JSDoc 三要素（铁律 47）
2. **去重持久化载体**（B-19 裁决）：复用 sentinel_tickets 表加列、独立 dedup 表、还是 journal 文件？重启恢复语义（TTL 过期的历史记录启动时怎么清）？
3. **8-2 降级语义**：表空时内存派生降级的 degraded 标注怎么暴露给前端？两条数据源并存时以哪边为准（表为准，内存只兜底）？
4. **真实用例对账**：customer-demand-shift 注入 churn_rate>critical → 工单落表且 GET 可见（8-2）；同 finding 二次 check 不重复开单/不重复通知（8-3，id 稳定验证）；工单 acknowledge→resolve 全链路（8-4）。三场景测试 = 验收测试

## 四、验收（物理可复现）

1. 契约 JSDoc（铁律 47）+ 测试三路径（正常/降级/边界）有 expect（铁律 48）
2. 真实用例对账（§三-4 三场景）全绿，输出留 evidence（git 跟踪路径 `docs/synova/audit-reports/D580-ticket-slice-evidence-20260906/`，禁用根级 evidence/——.gitignore:76）
3. 回归：tests/sentinel/ 26 文件全绿（含 D577 threshold-injection + flip）；tsc 28=28 基线；全量 vitest 失败集与 main 基线 diff=空
4. 治理影响：本单不碰 scripts/product-lines → 无 VERSION bump 触发（CT-42 不触发）；若 spec 必答判定需要（不应），升级 CTO 裁决
5. 现状复核红线：改 finding.id 前先 grep 消费方清单入 evidence（铁律 9 传播检查）

## 五、写集约束

- **可碰**: src/sentinel/runner.ts、src/agent/sentinel-service.ts、src/routes/sentinel.ts、extensions/sentinels/*/aggregate.ts（仅 id 生成行）、tests/sentinel/ 新增、tests/routes/ 配对、task-state/D580.json、.claude/task-briefs/2026-09-06-D580-*、memory/notes/、evidence 目录（§二）
- **不碰**: scripts/product-lines/（D579 领地）、scripts/desktop/（D578 领地）、scripts/audit/、docs/authority/ + 权威文档 research 目录（P0 文档批领地）、product-lines.yaml（兑换由 CTO）、src/server.ts、src/config.ts、electron-renderer/、pre-commit 门禁脚本
- 共享文件: 台账（追加型，尾追合并 D489 先例）；VERSION.md **禁碰**（D579 编码阶段将 bump，单写者纪律）

## 六、交付要求

1. spec 命名: docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D580-ticket-slice-20260906.md（spec-only 提交先行，D556 先例）
2. 交付声明 DS 与 spec 一一对应（S-10），缺项显式 descope
3. 编码完成后统一 K3 审计（创始人安排，审计独立非子代理）
