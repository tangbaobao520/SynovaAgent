# 派单 — D581 D578 收尾三残留：Win 验证脚本修复（BOM/证据路径）+ 构建守卫

> 派单: CTO | 2026-09-06 | 认领: DSH 编码线（小任务直派，D571 先例——脚本编码修复 + 路径常量 + 构建守卫，合计预估 ≤2h，不另立 spec）
> 来源: Win 侧 D578 真机实测完成（台账 1493539f）转来 3 项 DSH 残留——Win 明确移交 Mac DSH 线
> 上游: D578 主体已完成（坏包根因定位 + CI 重打 + 新包真机验证 通过），本单是脚本/构建层的收尾，让「四断言 + evidence 入 git」可复现，1-2 兑换才具备完整证据链

## 一、写前核实（6 项，CTO 已逐一物理取证 2026-09-06）

- ① 来源/依赖: D578 主体完成（台账 1493539f，Win co-author 直推 main）；三残留为 Win 移交；1-2 兑换被本单阻塞（四断言未跑 + evidence 未入 git）
- ② task-state: D578 claimed（主体完成待 CTO 回填 impl_done——本批一并处理）；D581 本单新号
- ③ 基线资产（全部实读 HIT）:
  - 残留①: `scripts/desktop/win-install-verify.ps1` = UTF-8 **无 BOM**（`file` 输出 "Unicode text, UTF-8 text" 且首字节 `23 20 73` = "# s"，非 EF BB BF）→ PowerShell 5.1 中文注释乱码解析失败（Win 实测 `Missing expression`）
  - 残留②: 同脚本 L24 `$EVIDENCE_DIR = Join-Path $REPO_ROOT ("evidence/D523-win-" + ...)` → 根级 evidence/ 被 .gitignore:76 禁用 → 证据不入 git（K3 P1-1 同型）
  - 残留③: `build-synova.cjs` L63-64 extraResources `{from:'dist',to:'dist'}` 依赖 `dist/backend.mjs`（D518 三步构建链步骤1），但 `npm run electron:build:win`（package.json L43）不自动跑 `build:backend`（L51）→ 本地构建出空包（CI 已有 `test -f dist/backend.mjs` 门禁，本地无守卫）
- ④ DSH 借鉴核查: 无（PowerShell 编码/构建守卫为本仓自有工具链问题；不涉 DSH 机制）
- ⑤ 写集重叠: `scripts/desktop/win-install-verify.ps1` + `build-synova.cjs`——D579 在途（scripts/product-lines）/D580 spec 在途（docs/plans）/D580 编码将做（src/sentinel）均零重叠 ✓
- ⑥ 上一轮教训: K3 P1-1（evidence 必须入 git）；CT-57（evidence 入 git 口径——断言摘要/md5 入 git）；D572（不可复核=假绿温床）；Windows-compat skill（subprocess/编码跨平台模式库——本单正是其适用场景）

## 二、任务定义（三残留 + 一个连带）

| # | 内容 | 现场 | 验收 |
|---|---|---|---|
| A | **ps1 编码修复**: win-install-verify.ps1 重存为 UTF-8 **with BOM**（PowerShell 5.1 兼容；runbook 注明 `powershell.exe` 5.1 与 `pwsh` 7 均可跑） | 首字节无 EF BB BF | Win 侧 `powershell -File win-install-verify.ps1` 中文注释不再乱码（可由 Win 侧复跑确认，Mac 侧以 BOM 字节断言） |
| B | **证据路径修复**: L24 `$EVIDENCE_DIR` 改为 `docs/synova/product-lines/evidence/D578-win-real-machine/`（git 跟踪路径）；顺带把硬编码的 `D523-win-` 前缀改为 `D578-win-real-machine`（与派单 evidence 目录一致） | L24 | 改后脚本输出的 evidence 文件 `git check-ignore` = 非忽略（git ls-files 可跟踪） |
| C | **构建守卫**: `electron:build:win` / `electron:build` / `electron:build:mac` 前置 `build:backend` 产物断言——方案二选一（spec 免答，编码自决记录依据）：a) package.json 改为 `npm run build:backend && electron-builder...`（每次构建强制重建，慢但稳）b) build-synova.cjs beforeBuild 钩子断言 `dist/backend.mjs` 存在，缺失则 fail-fast 并提示 `npm run build:backend`（快，但要求开发者记得先跑——推荐 b，与 CI `test -f` 门禁同语义） | package.json L42-44 + build-synova.cjs L63 | 本地 `npm run electron:build:win` 在 dist/backend.mjs 缺失时 fail-fast（缺产物时构建被阻断而非出空包） |
| D | **连带登记**: 1-7 NSIS 升级路径实测仍缺（D578 派单连带项，Win 侧未覆盖）——本单修好脚本后由 Win 侧下次真机会话补测；1-2 兑换等四断言 evidence 入 git 后由 CTO 执行 | 台账 | 登记（不在本单实现） |

## 三、验收（物理可复现）

1. `head -c 3 scripts/desktop/win-install-verify.ps1 | xxd` 首三字节 = `EF BB BF`（BOM 在）
2. `grep "docs/synova/product-lines/evidence" scripts/desktop/win-install-verify.ps1` 有命中 且 `evidence/D523` 零命中
3. `node -e "require('./build-synova.cjs')"` 正常（守卫不误伤已存在产物的情况）；临时 `mv dist/backend.mjs /tmp/` 后构建命令 fail-fast 且错误信息含 `build:backend`（守卫生效）
4. 回归：tests/electron/desktop-build.test.ts（产物断言组）全绿；tsc 28=28
5. Windows-compat skill 加载（本单为典型适用场景——编码/跨平台）
6. 完成后 1-2 兑换路径打开：Win 侧复跑四断言（脚本已修）→ evidence 入 git → CTO redeem

## 四、写集约束

- **可碰**: scripts/desktop/win-install-verify.ps1、build-synova.cjs、package.json（仅 scripts 段如选方案 a）、tests/electron/ 配对、task-state/D581.json + task-state/D578.json（回填）、.claude/task-briefs/2026-09-06-D581-*、memory/notes/、docs/synova/coordination/审计发现台账-DSH-CTO.md（追加登记）
- **不碰**: src/、electron-renderer/、scripts/product-lines/（D579）、scripts/audit/、product-lines.yaml、docs/authority/、pre-commit 门禁脚本
- VERSION.md：不 bump（无产品代码变更，纯脚本/构建配置——CT-42 不触发；如编码自检判定触发，升级 CTO）

## 五、交付要求

1. 单 commit 走 synova-commit（--task-id D581）→ PR → CI → CTO merge
2. 自检 5 问照答（接线/异常/类型/测试/残留）
3. 完成后 CTO 回填 D578 impl_done（Win 主体）+ D581 impl_done，1-2 兑换等 Win 复跑
