# 派单 — D584 P1 DSH 线测试债（tests/sentinels 批 + 契约/控制塔批）

> 派单: CTO | 2026-09-06 | 认领: DSH 编码线（与 D583 并行，写集互斥）
> 来源: Win 盘点三态之②「DSH 线 ~50 文件」
> 定位: tests/sentinels/**（~38 文件，哨兵 compute 契约演进的存量）+ 契约/控制塔/环境/electron（~12 文件）。可与 D583 同时开工（文件级零重叠），也可串行。

## 一、写前核实（6 项）

- ① 来源: 台账 898785d6；本单与 D583 拆双批因量级（50 文件）与归口不同
- ② task-state: D584 本单新号；D580 已合并（tests/sentinel/ 已绿——注意与 tests/sentinels/ 是不同目录）
- ③ 基线资产: tests/sentinels/** ~38 文件（哨兵 compute 测试——失败根因需逐文件判定：compute 签名标准化 `(store, teamId)`、SentinelConfig 四字段、_extinct 退役引用、阈值接线 D577 后语义）；tests/config-file.test.ts、tests/contract/{contract-gate,contract-store}.test.ts、tests/control-tower/signal-emitter.test.ts、tests/env/env-completion.test.ts、tests/deploy/data-directory.test.ts、tests/golden-scenarios/gss-common.test.ts、tests/data-pipeline.{feishu,ingest}.integration.test.ts、tests/electron/{backend-spawn,desktop-build,mac-install-verify}.test.ts
- ④ DSH 借鉴核查: 无
- ⑤ 写集: tests/sentinels/ + tests/{config-file,contract,env,deploy,golden-scenarios}/ + tests/control-tower/signal-emitter.test.ts + tests/data-pipeline.* + tests/electron/{backend-spawn,mac-install-verify}.test.ts（desktop-build 已由 D581 修属绿，如仍红按环境）——与 D583（expert 系）/在途零重叠
- ⑥ 教训: 铁律 36/48；平台豁免纪律（POSIX-only 行为登记不硬修）

## 二、任务定义（两子批，可分 commit）

- **批 1（大头）**: tests/sentinels/** 逐文件判定——测试过时（对齐现 compute 契约/现哨兵清单 45+4）vs 产品 bug（转 FIX 登记不混入）。特别核 D577 阈值接线后 aggregate 测试的断言语义（th() 源替换后旧断言可能假绿/假红）
- **批 2**: 契约/控制塔/环境/electron 逐文件——backend-spawn SIGTERM/SIGKILL POSIX-only → **平台豁免登记**（test.skipIf(win32) 或 describe.skip 环境注记，非删除）；desktop-build release 产物缺失 → 沿用既有 skip+warn 语义；data-pipeline integration 需外部服务 → 环境豁免或 mock 边界判定

## 三、验收

1. 两批文件实测绿（或显式平台/环境豁免——豁免清单进 commit body）
2. 全量失败文件集净减 ≥45；零新增
3. tsc 28=28；不碰 src/（真 bug 转单）

## 四、写集

- 可碰: 上列 tests/** + task-state/D584.json + brief + 台账（追加）+ memory/notes/
- 不碰: src/、scripts/product-lines/、scripts/desktop/、scripts/audit/、product-lines.yaml、electron-renderer/、VERSION.md

## 五、交付

同 D583（单 commit → PR → CI → CTO merge）。
