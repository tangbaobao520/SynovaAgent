# 派单 — D597 D588 会话投影接线（M3 第 5 次修复）

> 派单: CTO | 2026-09-08 | 认领: DSH 编码线（微任务直派，D571 先例——预计 1-2 行改动）
> 来源: K3 七任务批审计 D588 CONDITIONAL PASS 三条件之①——「接线任务 D# 显式立项」
> M3 第 5 次复发：代码 + 测试全绿但 src/ 零生产引用

## 一、写前核实（CTO 已核）

- ① D588 实现在 `src/store/session-projection.ts`（410 行，7/7 测试绿，whole-value 断言真实）
- ② 设计语义：「import 即接线」——模块加载时原型级 wrap `SessionStore.prototype.appendEvent`，生产 8+ 构造点（bootstrap/synova-agent/cli/im-inbound 等）零修改自动获得投影驱动
- ③ M3 根因：src/ 零 import → 原型 wrap 从未触发 → 死代码
- ④ K3 审计三条件：① 接线 D# 立项（本单）② 壳补登记（已做）③ DS1 统一（随接线 spec）

## 二、任务定义

**接线落点**：`src/deploy/bootstrap.ts`（应用入口，最早构造 SessionStore 的文件之前）

**改动**：在 bootstrap.ts 的 import 区追加一行：
```
import '../store/session-projection';
```
（副作用导入——原型 wrap 在模块加载时自动激活。加注释说明 M3 修复。）

**验收**：
1. `grep -rn "session-projection" src/deploy/bootstrap.ts` 有命中（import 在）
2. `npx vitest run tests/store/session-projection.test.ts` 7/7 仍绿（回归不破坏）
3. `bash scripts/check-architecture.sh` L1→L3 零违规（import 无类型泄漏）
4. tsc 28=28 基线

## 三、写集约束

- 可碰：src/deploy/bootstrap.ts（+1 行 import + 注释）
- 不碰：src/store/session-projection.ts（D588 交付物不动）、tests/、scripts/、product-lines.yaml、VERSION.md

## 四、交付

单 commit 走 synova-commit → PR → CI → CTO merge → D588 CONDITIONAL 条件① 满足 → K3 复审确认后升 PASS。
