# 派单：DSH 借鉴卡 B-11 ~ B-20（D760 ~ D769，十张）

> 依据计划: v1.2@8d331729（`docs/synova/coordination/整体推进计划-主线-20260913.md`）
> 派单: CTO | 日期: 2026-09-15 | 来源: 创始人 2026-09-15 指令「win 侧那些 DSH 迁移施工卡片还有 10 条，你也要一起安排」
> 登记: D770（本单同时完成登记——十张卡此前只有卡号，未进 task-state ⇒ 看板不可见）
> 上游: `docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md` §4（B 组简卡）+ `附录B-DSH运行时机制深潜-20260904.md`

---

## 〇、执行方规则（创始人 2026-09-15 两条指令，本批起生效）

1. **CTO 本 session 不再自己写代码**；CTO 域（`scripts/control-tower/**`、`scripts/workflow/**`、`src/sentinel/**`、`src/mcp/**`、`src/cron/**`）的编码 → **派给并行 CTO session（预设 `synova-cto`）执行**。
2. **Win 域编码同样给 Mac 侧执行**（创始人第二条指令：「这几天 win 不在身边，同一个模块或者切片，涉及到 win 工作范畴的，也一并安排给 Mac 侧执行」）→ 本批**十张卡全部由 Mac 侧执行**：Win 域产品代码交 Mac 编码 session（`synova-dsh`）；CTO 域两张（D760/D768）交并行 CTO session（`synova-cto`）。
3. 域声明与单域 PR 门禁**不变**（Win 文件仍写 `域: win`）；跨域切片在同一执行方内拆成多个单域 PR，连续做完、不等 Win。规约全文：`docs/synova/coordination/Win域代行规约-20260915.md`（D773）。
4. **验收分两级**：代码级（类型/单测/集成/真实路由接线）Mac 必须验、证据落 git；实机级（Windows 安装/升级/GUI 真机路径）→ **挂账**，Win 回归后补验，**禁止**用 Mac 结果声称实机通过。
5. 审计归 K3（`scripts/audit/**` 与审计标准红线不动）；CTO 不审自己。

## 一、十张卡与域（ownership.yaml 实测）

| 任务 | 卡 | 一句话 | 域（声明） | 落点 | 执行方（期间＝Mac） | 并行度 |
|---|---|---|---|---|---|---|
| D760 | B-11 | MCP client 双 transport（stdio/StreamableHTTP）+ 工具表变更通知重载 + 子进程环境擦除 | **mac** | `src/mcp/**` | 并行 CTO（`synova-cto`） | 与 Win 卡可并行 |
| D761 | B-12 | webhook 事件触发路（source/rule/delivery 三 id + provenance 注入 + HMAC 验签） | win | `src/routes/**` + `notifications/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D762 | B-13 | goal 事件溯源状态机四件套（foldGoal 投影 + 轮驱竞态围栏 + tool-goal authority） | win | `src/growth/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D763 | B-14 | 消息级反馈 sidecar（域 spec + schema 族 + 会话指纹围栏 createdAt+cwd） | win | `src/agent/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D764 | B-15 | 权限预设写穿（预设=旋钮打包；预设不做第二真相源） | win | `src/security/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D765 | B-16 | 不可信上下文标注（跨会话引用 URI 编解码/上限/限流 + injection 标记） | win | `src/agent/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D766 | B-17 | skill provider 两阶段触发（dispatcher 主动发现 + 模型主动拉取） | win | `skills/**` + `src/l3/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D767 | B-18 | 运行时不变量注册表（`InvariantRegistry`：检查可选、违约必炸） | win | `src/infra/**` | Mac 编码（`synova-dsh`） | Win 域串行 |
| D768 | B-19 | 持久化分级选型准则（日志 vs 内存：按「死后是否需要复活」选路线） | mac | `src/cron/**`（+ `src/store/**` 只读对照） | 并行 CTO（`synova-cto`） | 与 Win 卡可并行 |
| D769 | B-20 | 速览族六项小缺口（按需取用，每项单独 PR） | win | `src/agent/**` · `src/store/**` · `scripts/**` | Mac 编码（`synova-dsh`） | Win 域串行 |

> **同域并行度 = 1**（计划 §五 原则 7）：Win 域串行（Mac 编码 session 内一次一张卡）；CTO 域（D760/D768）由并行 CTO session 走各自序列。

## 二、开工顺序（按「价值 ÷ 成本 + 依赖」排）

1. **D767（B-18 不变量注册表）** —— 最小、最通用，交付即强化「静默降级 / 跨层调用」的**运行时**执法（现只有 pre-commit 静态检查）。Win 域。
2. **D768（B-19 持久化分级准则）** —— 准则类，给 Stage 2 存储选型定口径（含至少一处存量选型纠偏）。Mac 域，可与 1 并行。
3. **D763（B-14 反馈 sidecar）** —— E1 数据回流（N13 闭环）的定位粒度，直接服务哇呢宝贝验收。
4. **D762（B-13 goal 状态机）** —— 目标导航 0% 的主缺口。
5. **D765（B-16 injection 防线）** —— 安全与信任。
6. **D760（B-11 MCP client 双 transport）** —— **前置**：`#456`（D595 MCP 认证/权限）先合，避免同模块撞车。
7. D761（B-12 webhook）→ 8. D764（B-15 权限预设）→ 9. D766（B-17 skill provider）→ 10. D769（B-20 速览族，按需取用）。

## 三、硬守卫（指引 §6，逐卡物理验收，不可省）

- **G1 零 import**：`grep -rn "@deepseek-ai" src/ packages/ --include="*.ts" --include="*.json" | grep -v node_modules` → 零结果
- **G2 独立 PR + 接线审计**：每卡单独 PR；`grep -rn "<新符号>" src/` 有调用方 + 集成测试走真实路由（铁律 0-2）
- **G3 挂产品线缺口**：每卡在台账/报告里写明它补的是哪个缺口（禁「为借鉴而借鉴」）
- **G4 grep 物理证明 + 可回退**：声称完成必须给命令 + 原始输出；每个机制带 enable/disable 开关
- **G5 Stage 3 前零运行时依赖**：不装任何 DSH 包

## 四、每卡开工前置（缺一不许开工）

1. 读 `DSH借鉴指引-v2` 对应卡 + 附录 B 锚点；按指引 §1 重验程序 `git ls-remote` 核对上游版本，锚点失效**局部刷新后**再动手（§9.1）
2. 写集**机器生成**（`bash scripts/control-tower/declare-write-set.sh --brief <brief> --base origin/main`），不手写
3. brief 必填 `## 域`（本表域列）与 `## 主线贡献`（本批统一 `infra:DSH 借鉴卡（非 line-1）`，为诚实计不计入主线）
4. 契约优先（铁律 47）：新模块先写 JSDoc 输入/输出/降级；测试覆盖正常/降级/边界（铁律 48）

## 五、回报口径

每卡交付须含「声称 ↔ 命令 + 原始输出」对照表（计划 §五 原则 8）；K3 逐条跑；无证据 = 未完成。

## 六、派单内部一致性自检

- 域与落点一致：`src/mcp`/`src/cron` 实测 mac，`src/routes`/`src/growth`/`src/agent`/`src/security`/`src/infra`/`src/store`/`skills` 实测 win（`check-ownership.py` 逐条核过）
- 并行度不冲突：Win 域十卡串行、Mac 域两张（D760/D768）可并行 → 任意时刻 ≤2 卡在跑
- 依赖无环：唯一硬依赖是 D760 ← `#456`（已写明前置）；其余零依赖
- 无互斥表述：本单未对同一路径同时要求「必红」与「变绿」
- 与已交付卡不撞车：B-09/B-10（D599）、B-01（D586）、B-03（D598）、B-04（D587）、B-05（D594）、B-07（D588）已完成 → 本批不含这些

## 七、红线

- 不碰 `scripts/audit/**`、不写审计标准、不自我审计
- 不引 `@deepseek-ai` 运行时依赖（G1/G5）
- 不复制 OpenViking 代码（AGPLv3）
- 每卡文档/脚本产出照旧进 git 并受 K3 审计，无豁免
