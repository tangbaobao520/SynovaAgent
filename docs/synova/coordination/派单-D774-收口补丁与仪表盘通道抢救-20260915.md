# 派单：D774 收口补丁 + 仪表盘自动更新通道抢救

> 依据计划: v1.3@eef38a85（`整体推进计划-主线-20260913.md`）
> 派单: CTO | 日期: 2026-09-15 | 执行方: 并行 CTO session（预设 `synova-cto`）| 域: mac
> 触发: D774（证据保鲜流水线）验收时 CTO 实测发现的**产品可见性阻塞** + 自动通道根因
> 关联: task-state/D774.json（已交付，8 项验收 6 项 PASS / 2 项记录）· task-state/D786.json（本派单）· PR #404（自动更新作业产物）· PR #403（控制台自动更新）

---

## 一、事实（全部为 CTO 实测原始输出，非转述）

| # | 事实 | 证据（命令/输出） |
|---|---|---|
| F1 | **兑换成果没进 main**：main 上进度产物仍是 09-12 快照 | `git show origin/main:docs/synova/product-lines/product-progress.json` → `generated_at 2026-09-12 23:41:24 \| pct 1 \| stale 29 / pending_k3 34` |
| F2 | **流水线本机实跑真相 = stale 29→27 / pending_k3 34→36** | 在隔离 worktree 跑 `bash scripts/product-lines/rerun-evidence.sh` → `before stale=29 pending_k3=34` → `after stale=27 pending_k3=36`（Δ-2/Δ+2） |
| F3 | 失败显式、不静默 | 同次实跑末行 `✗ 存在 3 个显式 fail → exit 1`（GS-01 `noauth-401 未命中`、GS-02 `demand-shift-critical 未命中`） |
| F4 | 过期预警数字 | `python3 scripts/product-lines/gen-expiry-warnings.py` → 产物 `expired: 2 \| expiring: 15` |
| F5 | **自动通道永久 blocked**：#404/#403 均 `github-actions[bot]` 建、`mergeable_state=blocked`、**check-runs=0 / statuses=0**（2026-09-07 至今） | GitHub API：`pulls/404`（head `auto/product-progress`，sha 6395b014）· `pulls/403`（head `auto/dashboard`）· `commits/<sha>/check-runs` → `0` |
| F6 | **根因链条闭合**：main 保护要求 8 个必需检查（`Architecture Check`/`Control Tower Gate Tests (ubuntu+windows)`/`Golden Case F1 Gate`/`Integration Contract Check`/`Test-Kit Architecture Tests (ubuntu+windows)`/`Checker Review`）；两个 bot 作业用 `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` 开 PR → **GITHUB_TOKEN 触发的事件不再触发 workflow** → 那 8 个检查永不报告 → 永久 blocked | `branches/main/protection` → `required_status_checks.contexts`（8 项）· `.github/workflows/dashboard-auto.yml:56` `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` · `.github/workflows/product-progress.yml` 同款 |
| F7 | 因此"数字自动刷新"这条链**早就断了且无人发现**（没有"产物是否过期"的看门狗） | F1+F5：产物 3 天未更新，无任何告警 |

## 一之补｜写集冲突（CTO 复核 ⑤ 实测新增，必须先处置）

`#404`（`auto/product-progress`，bot 分支）**改的正是 T1 要落的同一批文件**：`docs/synova/product-lines/product-progress.json` · `product-progress.html` · `evidence-expiry.json`。

→ **T1 开工前先处置**：**关闭 #404**（由 T1 取代；它已永久 blocked、产物过期 3 天以上，无保留价值），或明确复用其分支再改。两选一，但**不允许让两个来源同时更新同一产物文件**。
（`#403` 是 `auto/dashboard` 控制台路，文件不重叠，可留到 T2 一并按通道修复处置。）

其余在此域内的 open PR（#456/#511/#533/#534）只动 `evidence/**` 的子文件，与 T1 的新证据不重叠——**不构成冲突**，无需等它们。

## 二、任务（两件，按序；第一件今天必须落地）

### T1｜把刷新后的真相落进 main（产品可见性）
1. 在**主工作区**（必须有 `node_modules`；否则 vitest/A2 会降级）跑：`bash scripts/product-lines/rerun-evidence.sh`
2. 提交这些**产物**（不是只提交证据）：`docs/synova/product-lines/product-progress.json`（刷新后）、`docs/synova/product-lines/rerun-evidence-summary-<日期>.json`、`docs/synova/product-lines/evidence-expiry.json`、本轮新增证据（`docs/synova/product-lines/evidence/**`、`scripts/golden-scenarios/evidence/GS-0X-<日期>.json`）
3. 单域 mac、PR ≤12 文件（超了按预算门禁拆单，禁调上限）；写集机器生成；提交走 `synova-commit`

### T2｜让"刷新"以后能自己落 main（通道修复 + 看门狗）
4. 二选一修 F6 的根因，并写清理由：
   - (a) **改用 PAT / GitHub App token** 开 PR（让 CI 正常触发，8 个必需检查能报告）；
   - (b) **改设计**：workflow 只产分支，**由 CTO session 显式合并**（把人工合并变成明确的一步，而不是静默卡死）。
   无论选哪条，都要**把"这条链怎么走"写进 `docs/synova/coordination/CI-诊断通道.md` 或等价文档**（下次断能查）
5. **产物过期看门狗**：`product-progress.json` 的 `generated_at` 超过 **3 天** → 告警可见（CI job 或仪表盘），防"通道死了没人知道"

## 三、硬约束
- 不改 `scripts/product-lines/calc-progress.py` 的判分规则（TTL=14 天与失效口径是红线：保鲜 ≠ 改判分）
- GS 场景失败**如实保留**（GS-01/02/04 产品侧失败已登记，不在本单范围）
- 不写 `k3` / `founder_demo` 类证据；不碰 `scripts/audit/**`
- 若改 `.github/workflows/**`（T2 大概率要改）：brief Q2 必须显式声明并说明理由（控制塔红区）
- 改 `scripts/**` 或 `.github/**` → commit 必须引用 `memory/notes/**` 的 Note（Note 引用门禁）
- 提交禁 `--no-verify`；CI 全绿才合

## 四、验收（可证伪，逐条贴原始输出）
- [ ] T1：合入后 `python3 -c "import json;d=json.load(open('docs/synova/product-lines/product-progress.json'));print(d['generated_at'],d['product_progress_pct'])"` → **当天日期**；六态 `stale=27 / pending_k3=36`（与 F2 一致）
- [ ] T1 反向：若某场景失败，`product-progress.json` 不得把它算成 verified（贴六态对照）
- [ ] T2：bot PR 的新一轮（或替代设计的一轮）**能拿到 check-runs**（贴 `commits/<sha>/check-runs` 非零输出），或明确采用 (b) 并把合并步骤写进文档
- [ ] T2：看门狗触发一例（把 `generated_at` 临时改旧 → 告警出现；恢复 → 消失）
- [ ] 红线：`git log -- scripts/product-lines/calc-progress.py` 在本单区间零提交

## 五、派单内部一致性自检
- 域与执行方一致：产物与脚本全在 `scripts/product-lines/**`、`docs/synova/product-lines/**`、`.github/workflows/**`（mac 域实测），执行方 = 并行 CTO（Mac 侧）
- 依赖无环：T2 依赖 T1 的产物形状（先落一次，再谈自动化）；T1 不依赖 T2
- 同域并行度=1：本单期间不开第二个 `scripts/product-lines/**` 写任务（与 D774 主体已交付，无重叠）
- 无互斥表述：本单未对同一路径同时要求「必红」与「变绿」
- 与已达成交付不撞车：D774 的脚本/测试/证据已合（#575~#582）；本单只补"产物落盘 + 通道"
- 数字口径已声明：六态数字来自 `product-progress.json`（生成产物），非手算

## 六、红线
- 不碰 `scripts/audit/**`、不写审计标准、不自我审计
- 不引 `@deepseek-ai` 依赖；不复制 OpenViking 代码
- 实机级证据只挂账（Windows 侧不在本单）
