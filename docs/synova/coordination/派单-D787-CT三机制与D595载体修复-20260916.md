# 派单：三个 CT 最高优先机制 + D595 载体修复（并附 D593 复核偏差裁决）

> 依据计划: v1.2@4e46603f（当前 main）｜ v1.3@eef38a85（PR #584 带入版本行后生效）——两者的哈希均列于此，避免合入后锚定失效
> 派单: CTO | 日期: 2026-09-16 | 执行方: 并行 CTO session（预设 `synova-cto`）| 域: mac
> 触发: K3「D593-FIX 复审 + D595 首审」交付验收时，CTO 独立复核发现 **1 处审计结论已过时** + 确认 **3 个机制缺口**（K3 提议，CTO 采纳并升级为 CT 最高优先）
> 关联: task-state/D787.json（本派单）· `docs/synova/audit-reports/2026-09-10-K3-D593FIX-D595-closeout.md`（K3 报告，分支 `audit/k3-20260910-d593fix-d595`，commit 1a6a7577）· task-state/D593.json · task-state/D595.json

---

## 一、CTO 复核结果（对 K3 结论的独立验证，全部本机实测）

### 1.1 被证实为真的（K3 报得准）
| 声称 | CTO 复核输出 |
|---|---|
| 9c3f1672 不含 `src/routes/diagnosis.ts` | `git show --stat 9c3f1672` → **12 文件，无 diagnosis.ts** ✅ |
| #457 合并 diff 不含该文件 | GitHub API `pulls/457/files` → 13 文件，`含 diagnosis.ts = False` ✅ |
| D595 spec 缺失 | `git cat-file -e origin/main:docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D595-mcp-auth-20260908.md` → **缺失**；删除者 = `f5f9c462 fix(D593): 恢复丢失实现 + 清理夹带 (#457)` ✅ |
| D595 brief 是骨架 | main 上 brief 仍为模板（`认领: <agent>`、`<本任务在哪一层？…>`） ✅ |
| codex heartbeat / D595 文档 夹带清理 | 9c3f1672 删除清单含 `.codex/heartbeat.json` 等 ✅ |

### 1.2 **被证伪／已过时的（必须订正）——D593-FIX「维持 FAIL」的核心结论**
K3 写「**main 上四块恢复面全部缺席**（saveDiagnosisCheckpoint 0 命中 / 列表端点无路由 / 冷读 fallback 无 / FIFO 注释未诚实化）」+「独立实跑 = 10 failed/11 passed」。

**CTO 实测（当前 main）：四块恢复面全部在场，且验收测试全绿。**

```
$ git log -1 --format="%h %ad %s" --date=short origin/main -- src/routes/diagnosis.ts
  e53b5984 2026-09-10 fix(D593): FIX2 确定性重建——diagnosis.ts 手术入提交，三物证闭环 (D593-FIX2) (#476)

$ git show origin/main:src/routes/diagnosis.ts | grep -nE "saveDiagnosisCheckpoint|getDiagnosisCheckpoint|冷读|FIFO"
  104-106  FIFO 上限 50 + 重启后经 checkpoint 冷读 fallback（诚实注释，含 DSH 范式引用）
  155      有界缓存 FIFO 淘汰
  596      sessionStore.saveDiagnosisCheckpoint?.({ ... })        ← 落盘写点在场
  678/741/794  store.getDiagnosisCheckpoint(...)                  ← 冷读 fallback 在场
  750      log.warn(... 'checkpoint 冷读失败 — 404（degraded，铁律 24）')
  808      router.get('/api/diagnosis/reports', ...)              ← 列表端点在场

$ npx vitest run tests/routes/diagnosis-report-persistence.test.ts tests/electron/right-panel-report-sentinel.test.ts
  Test Files 2 passed (2) | Tests 21 passed (21) | EXIT=0
```

**结论**：K3 对 **commit 9c3f1672** 的取证是对的（那一笔确实漏了核心文件——同类「提交漏 staged」事故复发），但它的**终局判定「main 上未修复／维持 FAIL」已被同日合入的 FIX2（#476, e53b5984, 2026-09-10）取代**。审计报告与 task-state 上的 FAIL 若不订正，会让看板长期显示一个**已经不存在的红**（同类问题此前由 D579 stale-verdict 机制处理过）。

→ **本单第 1 件：请 K3 复核 FIX2 并改写裁定**（不是重审整个 D593，只补一步复验，见 §二.1）。

### 1.3 其余复核
- **gitlink 夹带**：当前 main `git ls-tree | grep -c '^160000'` = **0** → K3 报的「三 gitlink 被 #459 复活」在其时点成立，**现已清零**（后续清理生效）。记录即可，不再派工。
- **D595 实现面**：CTO 独立跑 `npx vitest run tests/mcp/` → `bridge(4|3 skipped)` ✓ · `skill-audit-gate(7)` ✓ · `tool-definitions(38)` ✓ = **0 failed**（`server-smoke` 在本机未跑完，属环境；与 K3「47/47 绿」不冲突）。**实现高质、通道有损**的判断成立。

---

## 二、任务（三件）

### 任务 1｜给 K3：FIX2 复验 + 裁定订正（**先做，10 分钟级**）
1. 复核对象：`e53b5984`（#476, D593-FIX2）。要求三物证：① `git show e53b5984 --name-only` 含 `src/routes/diagnosis.ts`；② `git show e53b5984:src/routes/diagnosis.ts | grep -c saveDiagnosisCheckpoint` ≥1；③ **提交态**实跑 `npx vitest run tests/routes/diagnosis-report-persistence.test.ts tests/electron/right-panel-report-sentinel.test.ts` → 21/21
2. 若三项成立：把 `task-state/D593.json` 的 audit 段写成「**FAIL（9c3f1672）已由 FIX2 e53b5984 修复（复验 21/21），当前裁定 = PASS/或 CONDITIONAL PASS（按你的标准）**」+ 报告加一节「复审订正」，并把 1.2 的四块恢复面 file:line 贴进去
3. 若不成立：保留 FAIL 并写明与 CTO 复核的分歧点（附你的原始输出）

### 任务 2｜D595 载体修复（并行 CTO，代码/文档）
4. **恢复 spec**：`git show f5f9c462^:docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D595-mcp-auth-20260908.md`（从被删前一笔取回）→ 落 main；若与现状不符，在文件头注明「2026-09-09 被 #457 误删，2026-09-16 由 CTO 派单恢复」+ 差异说明
5. **补实 brief**：`.claude/task-briefs/2026-09-08-D595-L1-P3MCP认证权限.md` 现为骨架 → 按现状补 Q0-Q3/架构层/Done（或明确写「契约载体 = task-state/D595.json + 审计报告」，二选一，别留骨架）
6. **写集补入库**：K3 报的 evidence 7 件 + 补实 brief 未入库 → 补齐（或按 builtin 豁免显式登记理由）

### 任务 3｜三个 CT 最高优先机制（并行 CTO，控制塔域；这是本单主体）
> K3 三条提议 CTO 全部采纳；**M2 族第三次复发 + 三连实证**，按「一类一机制」加防线，不新增 M 类。

7. **机制 A｜提交级写集对账**（M2 第三次：修复提交声称恢复 X、diff 里没有 X —— 同类事故此前发生在提交漏 staged 的历史案例中）
   - 落点：`scripts/pre-commit-check.sh` 新增一组（或独立 check-*.sh + 接线，理由写进 brief）
   - 判据：**staged 变更集 vs brief/派单声明的写集** 差集非空即拦（本地软、CI 硬）；注入缝供测试
   - 反向验证：构造「brief 声明改 A、实际改 B」→ 必红；补正 → 绿
8. **机制 B｜合并 diff 白名单核验**（三连实证：三个合并各带进夹带物 + 「清理夹带」那一次**误删了另一个任务的 spec**）
   - 落点：合并/PR 门禁侧：对 diff 的**路径白名单**做核验（尤其删除类操作：删除非本任务载体 = 拦）
   - 最低要求：**删除 `docs/plans/**`、`extensions/**`、`src/**` 中的文件时，必须在本单写集里显式声明**，否则拦
   - 反向验证：模拟「删他人 spec」→ 必红
9. **机制 C｜task-state 流转载体存在性检查**（D595 spec+brief 双缺失的直接教训）
   - 落点：`claimed → impl_done` 回写时校验：`spec.path` 文件存在 且 brief 非骨架模板；不满足 → 拒绝流转并点名
   - 反向验证：把 brief 换成骨架 → 流转被拒

## 三、硬约束
- 不改 `scripts/audit/**`（K3 红线）；不写审计标准；不自我审计
- 不碰 `src/routes/diagnosis.ts`（已由 FIX2 修复，本单不重做手术）
- 三个机制都要**测试三路径**（正常/降级/边界）+ **反向验证**（贴两次原始输出）；写集机器生成；单域 mac；PR ≤12 文件（超了拆，禁调上限）
- 改 `scripts/**`/`.github/**` → commit 必须引用 `memory/notes/**` 的 Note；提交走 `synova-commit`（禁 `--no-verify`）
- 性能：`pre-commit` 不许明显变慢（`time` 前后对比 ≤ +5s）

## 四、验收（可证伪，逐条贴原始输出）
- [ ] 任务 1：K3 复验三项原始输出入报告；`task-state/D593.json` 裁定与当前 main 事实一致
- [ ] 任务 2：`git cat-file -e main:…D595-mcp-auth-20260908.md` 存在；brief 无 `<...>` 占位；evidence 件数补齐
- [ ] 机制 A：反向用例「声明 A 实改 B」→ 红；补正 → 绿
- [ ] 机制 B：反向用例「删非本单载体文件」→ 红；本单声明后 → 绿
- [ ] 机制 C：反向用例「骨架 brief 流转 impl_done」→ 拒
- [ ] 三个机制都有接线证明（`grep -n <脚本名> scripts/pre-commit-check.sh` 命中）

## 五、派单内部一致性自检
- 域与执行方：`scripts/**`（mac 域实测）+ `docs/plans/**`（win 域——**任务 2 恢复 spec 落在 win 域**）：按「Win 域代行规约」由 Mac 侧执行并声明 `域: win`；若同 PR 混域 → 拆成两个单域 PR
- 写集冲突：任务 3 的三个机制同属 `scripts/pre-commit-check.sh` → **必须串行**（同一文件三改，禁止并行）；任务 1/2 与任务 3 无交集
- 依赖：任务 1 独立；任务 2 独立；任务 3 内部 A→B→C 建议串行（同一文件）
- 无互斥表述：本单未对同一路径同时要求「必红」与「变绿」
- 与已达成交付不撞车：派单复核门禁（已交付，PR #570/#574 栈）已合；本单机制 A 是「提交级」对账，与那道「派单文档级」复核互补，不重复

## 六、红线
- 不碰 `scripts/audit/**`、不写审计标准、不自我审计
- 不引 `@deepseek-ai` 依赖；不复制 OpenViking 代码
- Windows 实机证据只挂账
