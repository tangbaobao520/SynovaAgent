# 派单｜D858：D854/CT-70 审计条件闭环（K3 判 CONDITIONAL PASS ⇒ 未通过）

> 出件 2026-09-20 CTO ｜ 执行形态：**agent 小队（最小队：编码 1 + 自验 1）**
> **为什么是最小队而不是单 session**：M3 要求「自验不得由编码兼任」；创始人 2026-09-20 定「执行类一律 agent team 模式」。
> **为什么不组 3 人**：本件四条条件在**同两个文件**上是**强串行链**，加第二个编码＝假并行（M2 写集互斥的实质） ｜ 依据：K3 报告 `docs/synova/audit-reports/2026-09-20-K3-D854.md`

## 〇、本件信息

| 项 | 值 |
|---|---|
| 卡 | `task-state/D858.json` |
| 工作树 | `.synova-wt-d858` ｜ 分支 `gate/d858-canary-glob` |
| 写集 | `scripts/control-tower/check-canary-drift.sh`、`tests/control-tower/check-canary-drift.test.sh`、`task-state/D858.json`、`.claude/task-briefs/<当日>-D858-*.md` |
| **写集硬边界** | **`.github/workflows/ci.yml` 本卡暂不动**——它由 CT-70（PR #682）独占；等 #682 合并后另行小改（P2 告警可见性若必须改 ci.yml，先报 CTO 排期） |
| 队长 | **并行 CTO session**（M6 收尾三件 + 归属逐张收口） |
| 编制 | **编码 1 人**（四条条件串行）＋ **自验 1 人（不得由编码兼任）** |
| 审计 | **K3 队外复审**（门禁变更属审计面；闭环后 CTO 复验 → D854 改判通过 → 才合 #682） |

## 〇-b、为什么派它 / 做完推动什么

| 项 | 内容 |
|---|---|
| **为什么是它** | K3 给 CT-70 的结论是 **CONDITIONAL PASS**；按创始人 2026-09-20 纪律「**有条件通过 = 未通过**」，必须逐条闭环。三条条件均为**"防线自己不可信"**类：① canary 覆盖语义靠**注释文本**承载（glob 删了注释还在 → 假覆盖）② Windows 分隔符告警**只在失败时可见**（绿态吞告警）③ 审计**取不到 job 日志**（匿名无 token） |
| **做完推动什么** | ① **解锁 #682** → `tests/project/**` 的 46+117 条断言**真正每次 PR 都跑**；② 同时消掉 D850/D852 审计的唯一 P1（同两支持测试未接 CI）；③ canary 漂移告警恢复可信 → **"CI 绿"不再可能是被注释骗的绿**；④ 证据包标准化 → **K3 审计成本下降**（可复用基建） |
| **不做会怎样** | #682 永远合不了 → CT-70 解决的问题依然存在；"有条件通过"第一次闭环流程走不完，**纪律形同虚设** |
| **可并行性判定** | 与在飞的 win 批、门禁三卡（D846/D847）、CT-70 的 ci.yml **无文件重叠**（本卡只碰 canary 脚本与其测试）→ 可并行；**必须串行**：`ci.yml` 等 #682 |

## 〇-c、权威件回执

| 项 | 内容 |
|---|---|
| 依据权威件 | K3 审计报告 `docs/synova/audit-reports/2026-09-20-K3-D854.md`（2026-09-20，P1/P2/L4 三条条件）｜[验收标准-穿真实入口](验收标准-穿真实入口-v1-20260918.md) §十一 三态判定 |
| 9 项撤回自检 | 未命中（本卡不涉 DSH 存储/遥测/界面；不宣称"线全绿=完成"） |
| 8 条禁做自检 | 未涉 DSH 代码依赖；未把测量值塞 `agent_memory` |
| L3 限制 | 本卡**禁用** grep 型静态判据当验收依据（实测 3/5=60%）；覆盖判定必须来自**物理展开** |

## 〇-d、旧信息漏洞自检

- [ ] 引用均已给**仓库内路径**（K3 报告已合 main？**未合则本卡暂停**——见下）
- [x] 卡级 write_set 已逐文件声明且与他卡不重叠
- [ ] **前置**：K3 报告分支需先合入 main（CTO 负责），否则本卡引用的报告路径仓内不存在

## 〇-e、卡面前提实测 + 执行方复核

| 前提 | 命令 | 预期结果 |
|---|---|---|
| canary 脚本不认 glob | `grep -n "test\\\\.sh" scripts/control-tower/check-canary-drift.sh` | 正则为字面路径形态，无 glob 展开 |
| 现有清单里两条 tests/project 路径靠注释收录 | 读脚本 + K3 报告 §L4-1 | 属实（K3 已复验） |
| `.github/workflows/ci.yml` 尚未含 tests/project | `git show origin/main:.github/workflows/ci.yml \| grep -c tests/project` | **0**（CTO 实测）→ 故 ci.yml 由 CT-70 独占 |

**复核项（执行方必须在写码前给结论）**：① 改 canary 脚本会不会影响既有 41 条清单的判定（需回归）；② 告警可见性的实现是否必须动 ci.yml（若必须 → 先报 CTO）。

## 一、完成判据（缺一不算）

- [ ] **P1**：`check-canary-drift.sh` **支持 glob 展开**，覆盖语义由**物理展开**承载；**夹具**：把 `tests/project/*.test.sh` 的接入删掉（模拟回退）→ 脚本**报红**（先红），恢复 → 绿（后绿），两次原始输出
- [ ] **P2**：分隔符告警在**绿腿可见**（`::warning` 或独立日志），给一次真实 CI/本地运行的可见性证据
- [ ] **L4-1**：注释与物理覆盖的**一致性检查**（注释声明 X 条，展开不足 X 条 → 报红）
- [ ] **L4-3**：**证据包标准化**——匿名/无 token 场景取 job 日志的可行方法（一条命令 + 示例输出）
- [ ] 回归：既有 41 条 canary 清单判定不变（给输出对比）
- [ ] 交回三件：diff / 自验结论 / 遗留清单

## 二、红线

不改产品代码、不碰 `scripts/audit/**`、不写审计标准；禁 `--no-verify` / `git stash` / force push；交付标 `可提请独立审计`（**K3 复审通过前不得合并**）；PR ≤12 文件 + 单域。
