# 派单｜退回闭环两件：D864（小队 A / P-1 收尾）+ D865（小队 B / P-2 不变量）

> 出件 2026-09-21 CTO ｜ 依据 K3 两份审计（均已推送并附 ls-remote 回执，D859 缺口已闭合）
> 纪律：**有条件通过 = 未通过**（A）；**FAIL 直接退回**（B）→ 两份都按"退回后闭环"处理

---

# A 件｜D864：小队 A（D862 / P-1 收尾）退回条件闭环

## 〇 编制与工作树
小队 A 原班（编码 2 + 自验 1，队长并行 CTO）；工作树 `.synova-wt-p1b`；分支沿用 `feat/d862-pr*` 与伞分支
**写集**：`src/backup/backup-scheduler.ts`、`src/invariants/health-route.ts`（或 `src/routes/healthz.ts`，**先核归属**）、`tests/**`、`task-state/D862.json`、`task-state/D864.json`
**硬边界**：不碰 `scripts/control-tower/**`、`scripts/audit/**`、`.github/workflows/**`、`src/sentinel/**`（B 队地盘）

## 一 条件（缺一不算闭环）
1. **P1-1（阻断项）二选一**：
   - ① **pr6 以 pr5 为 base 重建 diff**（推荐：`git rebase --onto` 或用 pr5 作 base 重新提交），使每支独立 PR 文件数 ≤12；
   - ② 保留叠栈，但在 **PR 正文显式声明 `pr5→pr6` 叠栈关系**，并按 **D860 新口径**重算文件数（治理产物不计入）。
   **判据**：给 `git diff --name-only <base>..<pr6>` 原始输出 + 文件数说明。
2. **P2-a**：`backup-scheduler.ts:182` 第 3 条 loopback 出站**登记进台账**；并**扩扫描口径**（根因＝清单只扫 `fetch(`，漏掉其他出站 API）——给出新口径的扫描命令与命中。
3. **P2-b**：`healthz` 的 catch **不得拼 `err.message`**（潜在泄漏口）；**测试断言整包**而非只断言 `detail`（给断言 diff）。
4. **P2-c**：`bypass.log` 为 6 支**唯一交集文件** → 在合并顺序说明里写明（M8），确保顺序合并不冲突。
5. 回归：与 D821 基线**零差集**（文件级 + 用例级双轴，给命令与输出）。
6. 挂账 3 项（`tool_choice` 前缀豁免 / 探针认证 / Windows 实机）保持登记，不阻断。

## 二 红线
不改 `http-exit.ts` 契约；不碰控制塔/审计/CI；禁 `--no-verify`/stash/force push；**闭环后 CTO 复验 → K3 复审**；PR ≤12（按 D860 口径）

---

# B 件｜D865：小队 B（D831 / P-2 不变量）FAIL 修复

## 〇 编制与工作树
小队 B 原班（编码 1 + 自验 1）；工作树 `.synova-wt-p2`；分支 `feat/p2-invariants`
**写集**：`src/invariants/**`、`src/agent/retry-middleware.ts`（**新增授权**：P0-1 根因在此）、`src/agent/tool-loop-executor.ts`、`src/routes/conversations.ts`、`tests/**`、`task-state/D831.json`、`task-state/D865.json`
**硬边界**：不碰 `scripts/**`、`.github/workflows/**`、小队 A 地盘（connectors/tools/routes 的**出站收敛**部分）

## 一 条件（P0 不闭环不得复审）
1. **P0-1（最高优先）违约必须真 fail-closed**：
   - 现状：`retry-middleware.ts:272-315`「永不抛异常」吞掉 `InvariantError` → `tool-loop-executor.ts:288-294` 返回文案 → `conversations.ts:212` SSE 建流后不改状态码 → **200 + agent_message**。
   - 要求：注入违约（组装请求丢 tools）→ **真实入口返回 5xx**（不是 200+文案）。**给端到端原始输出**（请求 + 响应码 + 日志含 owner）。
   - **并删除/改写"HTTP 层 5xx 语义"的无据声称**（审计明示其断言的是生产中不存在的直调形态）。
2. **P0-2 CI 必须真绿**：拆 PR 至预算内（**代码域 ≤12**）+ 消除跨域；**并确保 Vitest 在 CI 真跑**（给 run/job 证据，防 shard 被 skip）。
3. **P1×3 / P2×6**：逐条登记去向（本卡闭环 / 另立卡），不得口头带过。
4. 回归：分支与基线失败集逐条相同（给双轴比对）。

## 二 红线
机制对外承诺必须与行为一致（**"违约必炸"不得退化为"违约变降级"**——K3 点名此为最高价值风险：虚假安全感）；禁 `--no-verify`/stash/force push；闭环后 CTO 复验 → K3 复审
