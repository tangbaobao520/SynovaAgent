# 派单 — DSH 借鉴清单「重新对比审计」（D927）

依据计划: v1.2@4e46603f

- 派单号: D927
- 执行方: **山河研究院（原对比研究作者，优先）** 或 🔍 synova-k3-audit（异构，次选）——由创始人指派
- 触发: 创始人 2026-09-23 指令——「DSH 今天又再次更新了。可能我们有一些之前的借鉴已经过时，相关代码已经更新。**没动手的就按最新的来，已经动手的确保可以运行即可**」
- 性质: **对比审计**（不是开发）：只读两侧源码 + 只读本仓，产出「现状矩阵 + 锚点重核 + 过时判定 + 建议」

## 一、CTO 先给的现状（2026-09-23 实测，审计方以此起步并复核）

**借鉴卡 B-01…B-20 的代码/审计/证据三态**

| 项 | 实测 |
|---|---|
| 代码已落 | D586(B-01) / D593(B-02,B-06) / D598(B-03) / D587(B-04) / D594(B-05) / D588(B-07) / D599(B-09+B-10) / D575 等 ~10 张 |
| 已审计 | D586 PASS / D587 PASS / D575 PASS；**D588 = CONDITIONAL PASS（按纪律＝未通过）** |
| **未审计** | **D598、D599 = `impl_done` 且 `audit=None`** |
| **未动手** | **B-08 原子写+文件锁**（`grep -rn "writeFileAtomic\|withFileLock" src/ scripts/` 零命中）、**B-11 MCP client**（卡 D760 `claimed`） |
| **G3 证据回写** | `docs/synova/product-lines/evidence/` 含 `B-0x` 的文件**仅 2 个**（对照 20 张卡）→ 回写基本未做 |
| 版本锚点 | **三处不一致**：全局 npm `0.1.0-rc.8`（8-21）/ 桌面端 bundle `0.1.6-alpha.1` / 研究期锚点曾为 `0.1.6-alpha.2` + 试验版 `0.1.7-alpha.1`；**且今日再次更新** |

> ⚠️ 结论先行：**锚点漂移是本次重新对比的核心理由**——借鉴指引里的 `file:line` 锚点基于旧包版本，上游更新后**同一行号大概率已不是同一段代码**。

## 二、交付（四件，逐件可核）

1. **现状矩阵（B-01…B-20）**：每张卡给 `D# / 代码是否在 main（grep 证据）/ 审计 verdict / 证据回写 / 今日是否仍能运行`
   —「**今日是否仍能运行**」= 对已动手的卡做**冒烟验证**（不追新、只验可用），给命令 + 原始输出。
2. **锚点重核表**：旧锚点（包名 + 文件 + 行号 + 版本）→ 新版本对应位置；**已消失/被上游取代**的明确标注。
3. **过时判定**：逐项给 `仍成立 / 需改法 / 已无必要（上游已内建或范式已变）`，并给出判断依据（新版本 file:line）。
4. **处置建议**：未动手的（B-08 / B-11+）**按最新 DSH 范式重写实现方案**；已动手的**只列"能否运行"结论 + 若不能运行则登记 FIX 卡**（不在本审计内改代码）。

## 三、红线

- **只读**：不改本仓 `src/`、`scripts/`、`tests/`；不改 DSH 安装目录
- 借鉴纪律（施工图 R1/R3/R4）：**读范式自研，不 `npm install @deepseek-ai/dsh`、不 copy 代码**（Stage 3 前零依赖）
- 发现"已动手但不可运行"→ **登记 FIX 卡，不自行修复**（证据链不混）

## 四、验收

- 四件交付含 `file:line` 证据 + 原始命令输出；矩阵覆盖 B-01…B-20 **无遗漏**（缺项须显式写"未评估 + 原因"）
- 结论必须回答创始人三句：**哪些不用做了 / 哪些要改法 / 哪些没动手的按最新怎么做**

## 派单内部一致性自检

- 派单号 D928 已登记 `task-state/`；本派单不写代码、不占用 W1 小队（并发上限已满），与 W1 无依赖。
- 写集 = 审计报告 + 现状矩阵；「不做」= 本仓代码与 DSH 安装目录（只读）。
- 与既有审计派单（K3 D918/D919/D920/D922/D926）无重叠：本件**只审 DSH 上游借鉴**，不审 CTO 自产交付。
- 依据计划哈希按当前主线计划实测（v1.2@4e46603f）。
