# 派单 — FIX-011：共享治理文件的多卡归属机制（2026-09-26）

**卡号**：D979 ｜ **属主（执行）**：控制塔线 / 并行 CTO ｜ **发起**：CTO
**依据**：`docs/synova/coordination/问题修复台-20260925.md` FIX-011
**优先级**：**P0** —— 它是当前**唯一同时阻塞 CTO、并行 CTO、Mac 小队三方**的结构瓶颈

## 〇、为什么它是 P0（实测证据，CTO 本轮亲历）
`D328 提交声明与暂存文件归属不一致 — 疑似并行劫持` 在 24h 内拦下 **3 次**，全部因**共享文件有历史归属**：
| 被拒文件 | 门禁判给的归属 | 本次改动的声明 |
|---|---|---|
| `.github/workflows/ci.yml` | **D954** | D975 |
| `task-state/D954.json` | **D954** | D977 |
| `docs/synova/coordination/派单模板.md` | **D931 / D943** | D977 |
后果：**FIX-003 后半（把配对测试注册进 CI 清单）被迫移出本 PR**；**R1–R4 规则两轮无法落 main**。

## 一、要解决的问题（可判定）
> **同一份文件被多张卡声明过之后，任何新卡改它都会被判"并行劫持"并拒收。**
> 而治理件（ci.yml / 派单模板 / 号段水位 / 看板）**天然要被反复修改** ⇒ 这不是偶发，是**结构性死结**。

## 二、要求（闭环判据，缺一不算完成）
1. **给出机制**（二选一或组合，须说明取舍）：
   - ⒜ **一对象一文件**：如 `号段水位.md` → `号段/水位.yaml`；`派单模板.md` → `派单模板/` 目录（每类派单一文件）
   - ⒝ **显式多卡共享声明**：在卡内或文件头写 `共享声明: D931,D943,...` ⇒ 门禁认"多卡共享"而非"劫持"
2. **改门禁**（`D328` 判定逻辑）：落实所选机制；**同时保留防真劫持能力**
3. **反例必须存在**（缺一即未完成）：
   - ⒜ **共享文件被授权卡修改 ⇒ 通过**
   - ⒝ **非授权卡修改共享文件 ⇒ 仍然拒收**（防真劫持不能丢）
4. **迁移路径**：现有共享文件清单（至少：`ci.yml`、`派单模板.md`、`号段水位.md`、`CTO-看板-自动.md`、`派单-哨兵体系整改-20260925.md`）+ 各自新形态 + 分步迁移顺序
5. **不得**用"放宽门禁"换通过（禁止删检查/加 blanket 豁免）；**门禁语义变更 ⇒ 必过 K3**

## 三、写集（建议，执行方按需调整并回报）
- `scripts/pre-commit-check.sh`（D328 判定段）
- `scripts/control-tower/**`（相关校验脚本）
- 迁移涉及的共享文件（按所选机制）
- `tests/control-tower/**`（上条 3 的两侧反例）
- `task-state/D979.json`

## 四、红线
- 不碰 `scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`
- **不得**以改断言/删断言/blanket 豁免把红变绿
- 每步给原始输出；PR 由产出者自开（R1）；PR 模板六项强制（R2）
- **同类第 2 次**（D328 劫持误判今日已 3 次）⇒ 完成后**升级创始人知情**

## 五、完成后
1. 更新 `问题修复台`：FIX-011 状态 → 已闭环（附证据）
2. 补 `派单模板.md` 的一行指针 → 指向 `PR归属与派单参数-20260926.md`（FIX-012 的未清项）
3. 回报：机制取舍说明 + 两侧反例原始输出 + 迁移清单
