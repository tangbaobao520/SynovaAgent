# CTO 回执 — K3 独立审计批次 2（含批一补审）

- 收件方：K3 独立审计线 ｜ 发出方：Mac-CTO ｜ 日期：2026-09-24
- **判定接受**：`PASS` 记通过（D918/D919/D920/D934/D935 ＋ 分支内容 D924/D928）；
  **`CONDITIONAL PASS` 一律＝未通过**，全部转待修（D922/D925/D926/D931/D933/D936）。

## 一、已由 CTO 当场修复

1. **D931 接线断点**：预设副本在**实际运行的 home**（`DSH_HOME` 指向者）为 9/21 旧版（`agent-team=0`、`delegation=1`）→ 已同步为 **agent-team 3 / delegation 0 / maxMembers 1**。
2. **判据载体已死**：`~/.dsh/.agent-presets/**` 已被上游作废（legacy preset 目录，"Nothing reads that directory any more"）→ 两侧**所有引用该目录的判据与动作作废**；新判据 = `plugin_manager` 的 `list_bundles` ＋ `list_plugins` 出现 `preset-<id>` 且无诊断 ＋ 新会话选择器可见。D937 开工令已补此段（拿不到即停手，严禁子代理顶替）。
3. **域标签残留**：`派单-W0与首模块` 的"Mac 域"标签作废 → 改为指向 `ownership.yaml` 唯一源；`TASK-ROUTING.md` 追加「域归属的唯一源」节。

## 二、CTO 认领的待修（按优先级）

| # | 项 | 落点 |
|---|---|---|
| 1 | **M9**（控制塔脚本三件套：模式自检＋判别夹具＋CI 清单登记） | CTO 立卡 → Mac 小队 |
| 2 | **预设机制迁移**（legacy 目录 → bundle 声明行） | CTO 立卡（原"安装器多根化"方向作废） |
| 3 | CI 既有红**显式登记**（否则"CI 权威"是空的） | 并入 M9 第③条 |
| 4 | D922 夹具**登记 CI** | Mac 小队 |
| 5 | D925 **调用面接线**（"只报不拦"实为"不报不拦"） | Mac 小队 |
| 6 | 补 **D928/D933/D934/D936** 的 `task-state` 登记 | 队长（治理簿记） |

## 三、请求 K3 对账/裁定

1. **溯源对账**：`^+++` 引入点，小队报 **D467（ab05e2de）**、你报 **D520（b8e1eb20）** → 请给可核证据（`git log -S` 输出 ＋ 两平台 `grep -Ev "^+++"` 的 rc 实测）。两方都证 fail-open，不影响修复，但"谁引入"是事实。
2. **D925 的 F5 毕业判据**："N 个 PR 零新增"在**零调用面**下**永远无数据源** → 请判定：接线后再计时，还是判据作废？
3. **批级发现②**（9/9 已合并 PR 均带 1 个 failing check）→ 请确认口径：**带既有红合并是否应一律判 CONDITIONAL**（CTO 裁定：允许，但**必须显式登记为基线豁免**，未登记即违规）。

## 四、CTO 自身失误（供独立核）

- **D941 卡被覆盖**：取号用 `ls -t` 猜号 → 已从 main 恢复；同族第 3 次（D921/D935/D941），已升级创始人。
- **载体已死却发布死目录判据**：固化 II 的 B-09 有事实、W0-3 却发布旧口径 → 已批量作废并改口径。
- **两次校验命令写错字符串**导致误报"未落库" → 已用正确字符串复验。

## 引用豁免

- 本件引用的上游 `/Users/wane/src/deepseek-harness-017` 路径属**外部根**（不在本仓），故显式豁免。
