# 派单 — K3 独立审计：D918 / D919 / D920 / D922 / D926（CTO 自产交付，无豁免）

依据计划: v1.2@4e46603f

- 派单号: 沿用被审卡号（D918/D919/D920/D922/D926）；审计结论回填各自 `task-state` 卡，不另开号
- 执行方: 🔍 synova-k3-audit（K3 独立审计线）
- 性质: **CTO 自产治理/机制交付的强制独立审计**（铁律 0-5：开发者改过的门禁同样受 K3 审）
- 背景: 2026-09-23 CTO 接手批 − 解合并队列 + 建三套机制 + 组织形态变更。按纪律，**未过 K3 不算通过**；本派单即是「把没做完的做完」的最后一环。

## 一、审计对象（全部已合并 main，PR 号可核）

| D# | PR | 交付 | 自查证据 |
|---|---|---|---|
| D918 | #715 | 控制塔日期边界定时炸弹修复（gate-stats 夹具相对时间化） | 测试 9/0 |
| D919 | #716 | **引用可核验门禁**（fail-closed + 5 错误码可归因 + pre-dispatch ⑥ 接线 + CI 密封） | 测试 14/0；真实派单件 65 条引用 0 违规 |
| D920 | #717 | **合并队列推进器**（两根因纯函数化：工作树空→FAIL LOUD；DIRTY 二义→真冲突/缓存过期分流） | 测试 11/0 |
| D922 | #719 | W1 派单（程序卡 D922 + 子卡 D923/924/925）+ **dev-doc 预设退役技能化** | 预设测试 25/0；技能 SYNC-OK |
| D926 | #720 | **小队行为纪律技能化**（dsh persona 128→45 + squad-discipline + 队长预设接线） | 技能 SYNC-OK；pre-commit 13 组通过 |

## 二、必答题（逐条作答 + 原始输出，缺一不回）

**A. 机制真伪（D919）**
1. 构造「引用指向不存在文件」的派单件 → ⑥ 步是否**必红**？再构造「豁免段无理由」→ 是否**仍红**？（反向验证，不信正向断言）
2. 构造 40 条无效引用 → 是否**全部**报出（原实现 `head -25` 只报 25 条）？贴原始输出。
3. 给出一条真实违规的完整 JSON，确认含 `code`/`artifact`/`line`/`citation`/`owner`（可归因）。
4. **新增能力「待建声明」是否可被滥用**：行内写「新建」即可绕过文件不存在检查——请评估滥用面，并给判定（可接受 / 需收紧）。

**B. 根因防线（D920 / D918）**
5. D920：工作树参数为空时是否**中止**（而非回落主工作区）？贴复现命令与输出。
6. D920：对同一 `mergeable=false`，如何区分 `DIRTY_CONFLICT` 与 `DIRTY_STALE`？本地 `git merge-tree` 判据是否可信（举 #704 实证：GitHub 判 dirty、本地 merge rc=0）？
7. D918：夹具「相对 now 生成」后，是否仍存在其它**日期边界脆弱点**（同型复发面）？抽查 `tests/control-tower/` 另两个用写死时间的测试并给判定。

**C. 组织形态变更（D922 / D926）**
8. D922：`synova-devdoc` 退役是否**可逆且留痕**（注册表注释行 + 恢复方式）？退役后**规格职责是否有空洞**（跨层/新产品线/超预算时谁写）？
9. D926：persona 精简 128→45 行，是否**丢掉了门禁抓不到的必要条款**？请对照精简前版本（git 历史）逐条给「已删项 → 被哪个门禁覆盖」的映射，指出任何**无覆盖却被删**的项（这是本次审计最重要的一问）。
10. D926/D922：新技能是否**真接线**（队长预设是否引用、技能是否可被发现）？给发现路径证据。

**D. 通用**
11. 五件交付对 `scripts/audit/**` 是否**零改动**？（`git diff --name-only` 给证据）
12. 每件实际改动文件是否与卡内 `write_set` 一致？有无未声明夹带？

## 三、写集（审计线）

- `docs/synova/audit-reports/2026-09-23-K3-D918.md`、`-D919.md`、`-D920.md`、`-D922.md`、`-D926.md`（新建；可合并为一份但须逐件给结论）
- `task-state/D918.json` / `D919.json` / `D920.json` / `D922.json` / `D926.json`（仅回填 `audit` 段）
- `docs/synova/coordination/审计发现台账-DSH-CTO.md`（仅追加你的发现行）

不做什么（含文件路径）：

- 不改 `scripts/control-tower/check-citations.py` / `merge-pr-queue.py` / `install-dsh-preset.sh`（被审对象本体）
- 不改 `tests/control-tower/check-citations.test.sh` / `merge-pr-queue.test.sh` / `install-dsh-preset.test.sh`（同上）
- 不改 `.claude/skills/squad-discipline/SKILL.md`（同上）
- 不碰 `src/` 下任何产品代码

## 四、验收

- 报告含：结论（PASS / CONDITIONAL PASS / FAIL）+ 每条发现的 `file:line` + 复现命令 + 原始输出
- **有条件通过 = 未通过**（创始人 2026-09-20 纪律）
- `audit` 段回填 verdict；报告落 `docs/synova/audit-reports/`

## 派单内部一致性自检

- 审计对象 5 件均已在 main（PR #715/#716/#717/#719/#720 merged）；卡号均已登记 `task-state/`。
- 写集与「不做什么」无交集（写集 = 报告 + 卡 audit 段 + 台账追加；不做什么 = 被审对象本体与其测试）。
- 依赖链单向：本派单不依赖其它在途卡；不阻塞 W1 执行（W1 由 CTO 收件闸放行，不等本审计）。
- 依据计划哈希按当前主线计划实测（v1.2@4e46603f）。
