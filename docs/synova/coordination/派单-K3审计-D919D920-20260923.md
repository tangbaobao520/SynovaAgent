# 派单 — K3 独立审计：D919 引用可核验门禁 + D920 合并队列推进器

依据计划: v1.2@4e46603f

- 派单号: D921（本派单件自身的任务号）
- 审计对象: **D919**（PR #716，引用可核验门禁）、**D920**（PR #717，合并队列推进器）
- 执行方: 🔍 synova-k3-audit（K3 独立审计线）
- 性质: **CTO 自产交付的强制独立审计**（无豁免 —— 规则见 CLAUDE.md 铁律 0-5「开发者改过的门禁同样受 K3 审」）
- 依据: 2026-09-22 创始人质问「我写了一份不存在的权威引用——你怎么敢做这种事情」的机制化落地；交接文档要求「接手把没做完的做完」

## 一、为什么审这两件

1. D919 把「引用必须可核验」从结论落成 fail-closed 门禁 —— 它**本身要审**（CTO 自己的门禁同样是门禁）。
2. D920 把让整条合并队列卡死的两条根因收编为受审工具 —— 合并通道是最高风险变更面。
3. 两件都是**新机制**：新机制的风险 = 建成未接线 / 判据不可复现 / 报错不可归因（M1/M3 型）。

## 二、现状材料（file:line 已由 CTO 逐条核验，可直接 `git show <sha>:<path> | sed -n '<行>p'` 复核）

- 引用核验器: `scripts/control-tower/check-citations.py:1`（文件头契约 + 五错误码）
- ⑥ 步接线: `scripts/control-tower/pre-dispatch-check.sh:45`（原 `head -25` 截断实现已被替换）
- 门禁密封测试: `tests/control-tower/check-citations.test.sh:1`（13 断言）
- 队列推进器: `scripts/control-tower/merge-pr-queue.py:1`（两根因纯函数 + 注入缝）
- 推进器密封测试: `tests/control-tower/merge-pr-queue.test.sh:1`（11 断言）
- 参考系文档: `docs/synova/coordination/DECISION-REFERENCE.md:19`（Anthropic 适用域）
- 决策 Note: `memory/notes/proposed/2026-09-23-d919-citation-gate-mechanism.md`、`memory/notes/proposed/2026-09-23-d920-merge-queue-tool.md`
- 台账登记: `docs/synova/coordination/审计发现台账-DSH-CTO.md`（§五 末行 2026-09-23 条目）
- 任务卡: `task-state/D919.json`、`task-state/D920.json`

## 三、必答题（逐条作答，缺一不可）

1. **判据可复现**：`bash tests/control-tower/check-citations.test.sh` 与 `bash tests/control-tower/merge-pr-queue.test.sh` 在**你的环境**实跑输出是多少？与交付声称（13/0 与 11/0）是否一致？不一致 → 环境依赖还是恒失败？
2. **fail-closed 真伪**：构造一个「引用指向不存在的文件」的派单件，跑 ⑥ 步 —— 是否**必红**？再构造「豁免段无理由」——是否**仍红**？（反向验证，不信正向断言）
3. **截断回归**：构造 40 条无效引用，确认**全部**被报出（原实现 `head -25` 只报 25 条）。请贴原始输出。
4. **归因完整性**：每条违规是否含 `code` + `artifact` + `line` + `citation` + `owner`？给出一条真实违规的完整 JSON。
5. **根因1 防线**：`merge-pr-queue.py` 在「工作树参数为空」时是否**中止**（而非回落主工作区）？贴复现命令与输出。
6. **根因2 二义**：对同一 `mergeable=false`，工具如何区分 `DIRTY_CONFLICT` 与 `DIRTY_STALE`？本地 `git merge-tree` 判据是否可信（举 #704 实证：GitHub `dirty` 但本地 merge rc=0）？
7. **不碰审计红线**：确认两件交付对 `scripts/audit/**` 零改动（`git diff --name-only origin/main...<branch> | grep scripts/audit` 应为空）。
8. **超范围/夹带**：两 PR 的实际改动文件是否与各自 card 的 `write_set` 一致？有无未声明的夹带文件？

## 四、写集（审计线）

- `docs/synova/audit-reports/2026-09-23-K3-D919.md`（新建）
- `docs/synova/audit-reports/2026-09-23-K3-D920.md`（新建）
- `task-state/D919.json`、`task-state/D920.json`（仅回填 `audit` 段）
- `docs/synova/coordination/审计发现台账-DSH-CTO.md`（仅追加你的发现行）

不做什么（含文件路径）：

- 不改 `scripts/control-tower/check-citations.py`（待审对象本体，改它 = 污染审计对象）
- 不改 `scripts/control-tower/merge-pr-queue.py`（同上）
- 不改 `tests/control-tower/check-citations.test.sh`、`tests/control-tower/merge-pr-queue.test.sh`（同上）
- 不碰 `src/` 下任何产品代码

## 五、验收（物理可复现）

- 两份审计报告含：结论（PASS / CONDITIONAL PASS / FAIL）+ 每条发现的 `file:line` + 复现命令 + 原始输出
- 判定标准：**有条件通过 = 未通过**（创始人 2026-09-20 纪律）
- 报告落 `docs/synova/audit-reports/`，`task-state/D919.json`/`D920.json` 的 `audit` 段回填 verdict

## 派单内部一致性自检

- 派单号 D921 在 `task-state/D921.json` 已登记；审计对象 D919/D920 均已登记并可解析。
- 写集与「不做什么」无交集：写集只含审计报告 + 两张卡的 `audit` 段 + 台账追加；「不做什么」为被审对象本体与测试文件。
- 验收命令与必答题无互斥：所有断言均为「同类操作 + 同一判据」，不存在同一文件既要求「必红」又要求「exit 0」的情形。
- 依据计划哈希已按当前主线计划实测（v1.2@4e46603f），非凭记忆填写。

## 引用豁免

- task-state/D920.json — 该卡在 **PR #717**（D920 交付分支 `fix/d920-merge-queue-tool`）内，尚未合入 main；本派单件基于该分支内容撰写，K3 审时按 #717 的提交树核。
- scripts/control-tower/merge-pr-queue.py — 待审对象本体，在 **PR #717** 分支内，未合 main；同上。
- tests/control-tower/merge-pr-queue.test.sh — 同上（PR #717 内）。
- task-state/D921.json — 本派单件自身的登记卡，取号器写在 CTO 主工作区 `task-state/`，随本件 PR 落 main。
- memory/notes/proposed/2026-09-23-d919-citation-gate-mechanism.md — 属待合并 PR（#716 D919 / #717 D920）的产物或本派单件自身登记卡，主体不在 main；K3 按对应 PR 提交树核。
- memory/notes/proposed/2026-09-23-d920-merge-queue-tool.md — 属待合并 PR（#716 D919 / #717 D920）的产物或本派单件自身登记卡，主体不在 main；K3 按对应 PR 提交树核。
- scripts/control-tower/check-citations.py — 属待合并 PR（#716 D919 / #717 D920）的产物或本派单件自身登记卡，主体不在 main；K3 按对应 PR 提交树核。
- task-state/D919.json — 属待合并 PR（#716 D919 / #717 D920）的产物或本派单件自身登记卡，主体不在 main；K3 按对应 PR 提交树核。
- tests/control-tower/check-citations.test.sh — 属待合并 PR（#716 D919 / #717 D920）的产物或本派单件自身登记卡，主体不在 main；K3 按对应 PR 提交树核。
