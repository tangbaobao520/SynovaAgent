# 派单 — K3 独立审计（批次 2）

- 派单方：Mac-CTO ｜ 执行方：K3 独立审计线（独立仓，maker 侧无写权）
- 性质：**CTO 自产治理/门禁交付的强制独立审计**（无豁免；改门禁者不得自判通过）
- 纪律：**有条件通过 = 未通过**；报告须含 `file:line` + 复现命令 + 原始输出；执行方不判"通过"

## 一、批次 1（此前已派，如尚未回执请一并处理）

| 卡 | PR | 交付 |
|---|---|---|
| D918 | #715 | 日期边界炸弹修复（gate-stats 夹具相对时间化） |
| D919 | #716 | 引用可核验门禁（fail-closed + 5 错误码 + pre-dispatch ⑥ 接线） |
| D920 | #717 | 合并队列推进器（两根因纯函数化：工作树空→FAIL LOUD；DIRTY 二义分流） |
| D922 | #719 | W1 派单 + dev-doc 预设退役技能化 |
| D926 | #720 | 小队行为纪律技能化（persona 128→45 + squad-discipline 技能） |

## 二、本批（6 件，均已合并 main）

| 卡 | PR | 交付 | 本批重点问 |
|---|---|---|---|
| **D924** | #724 | 子进程输出协议 + 可执行检查器 + `scripts/golden-scenarios/common/assert.ts` 最小修复 | 变异体夹具是否**真承重**（改坏 R1/R2/EX1/EX2 必红）；`-o` 分流与旧 `-w` 尾部 LF 的字节级兼容是否成立 |
| **D925** | #728 | deps-interface 标准 + 27 夹具（首版只报不拦） | 唯一红是否确为 D734 结构性；`--strict` 才 1 是否会让"恒 0 默认"掩盖问题 |
| **D928** | #725 | 第③面生成器三处修复（spec 多态崩溃 / 散文当路径 / verdict 误判） | **verdict 判定顺序**是否真的修正（FAIL 不再显示为 PASS）；散文 spec 守卫是否有反例夹具 |
| **D931** | #727 | 队长预设接 Agent Teams（delegation → agent-team/tool-agent-team/ui-agent-team，maxMembers=4）+ 团队机制节 + 派单模板执行形态节 | **预设是否真接线**（落位后三条 grep 3/0/4）；`maxMembers=4` 是否物理生效 |
| **D933** | #730 | Win 派单（W0+首模块）+ 模块归属基线 | 派单件前提是否**已实测冻结**；基线与 `ownership.yaml` 的冲突**是否已按"服从表"改正**（见 #733） |
| **D934** | #731 | 两侧协作方式（Mac/Win 同构，唯一差异=CTO 角色由 Codex 执行） | "严禁子代理代替小队"的**判据是否可执行**；有无与现行法规/仓库现实冲突处 |
| **D935/M1** | #734 | `ownership.yaml` 补 `docs/synova/presets/** → mac` + 判别性夹具 | 纯新增是否成立（不改既有归属）；**D931 写集是否真的转绿** |
| **D936** | #733 | 模块归属改为服从 `ownership.yaml` + 台账三批登记 | 台账引用口径（39 条违规的构成）是否需豁免规则 |

## 三、必须回答的问题（逐条，缺一不回）

1. **D937（待开卡，P0）**：`scripts/pre-commit-check.sh` 组 7a 的 `grep -Ev "…^+++…"` 是否确为**非法 ERE** → 检查恒过？请**独立复现**（`echo test | grep -Ev "^+++"`）并判定影响面（本地 + CI strict）。
2. **D924/D925**：其夹具是否**穿生产入口**（而非 grep 命中型）；变异体是否真被语义抓到。
3. **D931**：预设接线是否**真落到机器**（三条 grep）+ 是否只需"重开 session"即生效。
4. **D933/D936**：我（CTO）关于归属的**两次自我纠错**是否已彻底（有无残留的第二事实源）。
5. **通用**：各件是否与 `scripts/audit/**` 零交集；卡内 `write_set` 与实际改动是否一致（无夹带）。
