# 派单 — K3 独立审计（批次 4）

- 派单方：Mac-CTO ｜ 执行方：K3（独立 session / 独立仓）｜ 转发：创始人
- 判定口径不变：**有条件通过 = 未通过**；报告须含 `file:line` + 复现命令 + 原始输出；
  **K3 报告请落 `docs/synova/audit-reports/` 并开 PR（创始人转发合并）**。

## 一、本批请审（批次 3 之外的增量）

| PR | 分支 | 内容 | 为什么必审 |
|---|---|---|---|
| **#745** | `feat/d943-mechanism` | **机制面**：`check-dsh-anchor.py`（DSH 断面门禁，三态 fail-closed）＋ `check-k3-report.py`（K3 报告机械保险）＋ `daily-cto-board.sh`（观测式看板 runner）＋ 2 份密闭夹具（10/10、8/8）＋ `DSH-断面.json` ＋ `已作废口径表.md` | **门禁/机制语义新增** |
| **#746** | `feat/b3-d922-fixture` | B3：D922 夹具登记 CI（T3 注入改文案无关 + T3c 面内承重 + T3b 负控 + UTF-8 字节级 IO + `MUTATION_NOT_APPLIED` 守卫） | **CI 机制变更** |
| **#747** | `fix/m9-baseline-owners` | M9 后随：`ci-red-baseline.txt` owner 指派（清 UNASSIGNED）＋ CT(windows) 定态换证 ＋ 3 日处置 SLA | **门禁基线表变更**（栈式 base=#741） |

## 二、本批重点问（逐条，缺一不回）

1. **#745 的断面门禁是否"真判、真拦"**：请独立构造 ① 引用旧断面（`0.1.7-alpha.2 @ 00102833`）的文档 ② 未登记的 DSH 版本串
   → 两者**必须 `VIOLATION(1)` 且点名 `file:line`**；再构造"事实源 head 与真实树不一致" → **必须 `DEGRADED(2)`**（不是 0）。
2. **#745 的 K3 机械保险是否越界**：它只查**形式与锚点**（断面声明／被审对象 sha 可解析／引用可解析／判定行带证据／计数带命令）。
   请判定：**是否存在"形式合格但实质空洞"的报告可通过**？以及**是否触碰了"审计标准"的边界**（CTO 不得写审计标准）。
3. **#746 的判别性**：`T3/T3c = 承重`、`T3b = 负控` 三者的区分是否真实（贴"改坏即红"原始输出）；CI 双侧（ubuntu+windows）的 `PASS=32` vs `31+SKIP=1` 差异是否**可解释且未掩盖失败**。
4. **#747 的 owner 指派与"定态换证"**：CT(windows) 从 `in-progress` 换成**定态快照**是否成立；5 条基线的 `owner/expires` 是否齐全。

## 三、请一并独立复核的两条机制发现（CTO 与值守实测，供你证伪）

1. **门禁按分支名推任务号**：`merge_writeset_gate.py` 的 `infer_did()` **分支名优先**（`:137-145`），
   从 `fix/d929-cto-health-crash` 推出 **D929**，而任务实体是 **D928** ⇒ 三源（卡/doc/brief）全按 D929 找 → 全空 → **fail-closed**。
   **改名成 `fix/d928-*` 后同一提交门禁 `✅ pass`（零正文豁免）**。请独立复现并判定"是否应支持回退"。
2. **`rerun-failed-jobs` 重放"旧事件体"**：改 PR 正文后 rerun **读不到新正文**；改用 **close/reopen** 后
   4/4 产生新 run 且豁免 `[declared]` 命中（**零新 commit**）。请独立复现该结论。

## 四、不需审的（避免重复劳动）

- 批次 3 已列的 **#737 / #739 / #741 / #743**（若已在审则继续）。
- **#744 已按创始人指令冻结**（`CTO-HEALTH.md` 判定过时，第③面改为观测式看板）——**不必审**。
- **#738** 正在拆分（拆成 #745 + PR-2 + PR-3），拆完再送。
