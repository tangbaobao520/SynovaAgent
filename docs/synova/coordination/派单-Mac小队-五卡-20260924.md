# 派单 — Mac 小队 五卡（按优先级；**WIP=1，一次开工一张**）

- 派单方：Mac-CTO ｜ 执行方：Mac DSH 小队（队长 + ≤2 编码 + 1 独立自验）｜ 终审：CTO 收件闸 + K3
- **规则**：五卡**已全部开好在板上**，但**一次只开工一张**；**前一张未通过收件闸，不放行下一张**。
- 执行形态（硬字段）：Agent Teams 组队（maxMembers=4）；队长不下场写码；每成员一份共享任务（写集+验收命令+写者）；
  重型验证串行 ≤1；**回执必须含团队成员运行记录**（成员名·状态·token·共享任务 id 与状态）。
  只队长在跑 / 无共享任务 id / 自验由编码兼任 → **退回**。

## 优先级与判据

### 卡 1（P0，立即开工）— 门禁 fail-open 假绿：组 7a ERE 非法致检查恒过
- 事实（CTO 复现）：`echo test | grep -Ev "^+++"` → **exit 2**（非法 ERE）；`scripts/pre-commit-check.sh:991` 的管道因此整体失败 → `NEW_DIAG` 恒空 → 「禁止新 DiagnosticModule」**本地与 CI strict 均恒过**（检查自建立起从未生效）。
- 写集：`scripts/pre-commit-check.sh`、其测试（`tests/**` 内对应组）
- 判据：① 改正则后**构造含 `DiagnosticModule` 的新增 → 检查必须红**（判别性夹具，贴原始输出）② 无误伤：现有 main 上跑 → 不产生新红 ③ 三路径（正常/降级/边界）

### 卡 2 — `alloc-task-id.sh` 两缺陷 + `scripts/**` 全角紧贴变量统一清扫
- 事实：`:242` 全角 `（` 紧贴 `$BRIEF_FILE` → `unbound variable`；`:204` title 含 `/` 未消毒 → 骨架静默不生成。
- 写集：`scripts/control-tower/alloc-task-id.sh` + 测试；（清扫若触及其它 scripts 文件，同卡内逐文件列出）
- 判据：① 中文/含 `/` 的 title 均能正常取号并生成骨架（贴输出）② 扫描 `\$[A-Za-z_][A-Za-z0-9_]*（` 形态全清（给命令与结果）③ 判别性夹具

### 卡 3 — 生成物注释漂移（CODEOWNERS / ownership.yaml 引用不存在的测试文件）
- 事实：`.github/CODEOWNERS:4` 与 `ownership.yaml` 注释均引 `tests/control-tower/ownership.test.sh`（全仓不存在，实为 `check-ownership.test.sh`）。
- 写集：`scripts/control-tower/check-ownership.py`、`docs/synova/coordination/ownership.yaml`（仅注释）、`.github/CODEOWNERS`（生成物）
- 判据：① 生成物注释与真实文件名一致 ② 重跑 `--emit-codeowners` 后与仓库逐字节一致 ③ 把「生成物注释必须与仓库一致」纳入 drift 断言

### 卡 4 — 名字统一分配（worktree / 分支 / 卡号）
- 事实：今天撞号 3 次（D921 卡号被覆盖；D935 卡号 + worktree 与他人撞；D936 被 CTO 重复使用）。
- 写集：`scripts/control-tower/**`（分配器或校验脚本）+ 文档
- 判据：① 取号后**立即检测**与远端分支 / `task-state` 的撞号并拒绝 ② 命名前缀规范（`cto-` / `squad-`）落到模板 ③ 判别性夹具（构造撞号 → 必须红）

### 卡 5 — M1b：`ownership.yaml` 生成式重写（**门禁语义变更，必过 K3**）
- 内容：从真实目录树**生成**路径→唯一域映射（禁手写）、最长前缀优先、兜底语义显式化、改 `check-ownership.py` 解析算法、同批 `--emit-codeowners`。
- 判据：① 生成物与现行表**逐条可比对**（差异清单）② 判别性夹具（删/改任一规则 → 必须红）③ 未登记路径行为**明确登记**（不得默认放行）④ CODEOWNERS 逐字节一致

## 引用豁免

- tests/control-tower/ownership.test.sh — 本派单**引用的是一个不存在的文件**（卡 3 的发现本身），故显式豁免。
