# 派单：迁移轨 Stage 1 续（D534/D535）— Agent Notes 四态 + guard 循环卫生

> 派单: CTO | 2026-08-25 | 认领: 📋 dev-doc（出 D534/D535 两个 spec，独立 PR/切片级审计）
> 方法论: 创始人批准切片思维——施工图迁移轨 Stage 1（范式借鉴，9-10 月排期），D1（D500 事件溯源）/D3（D474 snapshot）已完成，本单做 **D2（Agent Notes 四态）+ D4（guard 循环卫生）** 延续
> 流程: dev-doc 出 2 spec → 编码按依赖推进 → K3 切片级审计 → CTO 合并
> 上一轮教训: D529 时间黑洞（防膨胀——只治真问题不建新机制）；M7 文档-实现漂移；切片 B merge 事故（写集精确）

---

## 写前核实（SOP §〇c ①，6 项已核实）

- [x] ① 任务来源/依赖: 施工图 Stage 1（DOC-0114 §6）D1/D3 已完成（D500/D474 audited）；D2/D4 是 Stage 1 剩余项（开发转交方向已定稿，直接采用）
- [x] ② task-state 最新状态: D500/D474 = audited（main）；D534/D535 新分配未占用
- [x] ③ 基线资产实际存在: memory/notes/{archived,implemented,proposed,rejected} 四态目录已有（implemented 31/proposed 3）；控制塔 guard 雏形（incident-loop.py/staging_guard.py）
- [x] ④ DSH 借鉴核查: 施工图 §4（Agent Notes=🟡 治理层移植落点）；DSH 源码已定位（见下）
- [x] ⑤ 写集重叠: D533（CI 可达性）不重叠（memory/控制塔 vs CI）；D534/D535 已分配
- [x] ⑥ 上一轮教训: D529（防膨胀：借鉴理念融入，不新建机制）；M7（dev doc 与实现同步）

## DSH 借鉴核查（§〇b 三步）

1. **施工图四色归属**：🟡 搬走（治理层：memory/ + 铁律 + task-brief → DSH Agent Notes 四态；控制塔 guard → DSH timeout/reminder 范式）——施工图 §3.3 🟡 与 §4 映射表
2. **借鉴边界判定**：**有 DSH 范式可借鉴**——① D2: Agent Notes 四态（proposed/implemented/archived/rejected）是 DSH 知识沉淀范式（Stage 1 借鉴理念自研，Stage 3 后治理层移植落点）② D4: timeout-policy + repeat-tool-reminder（控制塔防跑偏通用化）。**均"长期借鉴不引代码"**（Stage 3 前零 DSH 依赖红线）
3. **DSH 源码参考**：
   - D2: `~/.nvm/versions/node/v24.19.0/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-agent-instructions/lib/types/state.d.ts`（四态状态类型定义）
   - D4: `dsh-tool-call-timeout-policy/lib/index.js`（L1 `deadline`/`timeoutOf`、L4 cooperative timeout enforcer、工具声明 `timeoutMs`）+ `dsh-repeat-tool-reminder`（重复工具提醒）

> 红线（施工图 R1/R3/R4）：借鉴 = 读范式自研，不 npm install/copy；验收含接线审计。

## 切片定义

| 切片 | 用户可见价值 | D | 依赖 |
|---|---|---|---|
| **Stage 1 续**（本次） | 知识结构化沉淀（四态）+ 控制塔防跑偏 | D534（Notes 四态）/ D535（guard） | D1/D3（D500/D474） |
| Stage 3 壳试点 | DSH 壳影子运行 | — | 入场券三条件 |

## 现状材料（dev-doc 必读，已核实存在）

| 资产 | 位置 | 状态 | 与本单关系 |
|---|---|---|---|
| memory/ 四态目录 | `memory/notes/{archived,implemented,proposed,rejected}` | 目录已有：implemented 31 / proposed 3（README.md） | **D534 基线**——目录在但"非平凡变更强制带 Note"机制缺 |
| 铁律体系 | AGENTS.md + memory/ | 铁律速览（0-48）是知识沉淀的载体 | D534 与铁律合并（不推翻） |
| 控制塔 guard 雏形 | `scripts/control-tower/incident-loop.py` + `staging_guard.py` | 存在 | **D535 基线**——防跑偏已有雏形 |
| 循环/超时现状 | scripts/control-tower/ | D521 做过 simulate-ci/::error（提交链路）；guard 超时未通用化 | D535 通用化对象 |
| DSH 四态范式 | dsh-agent-instructions/lib/types/state.d.ts | DSH 已证明"知识可沉淀、可归档、不腐化" | D534 范式借鉴 |
| DSH timeout 范式 | dsh-tool-call-timeout-policy + dsh-repeat-tool-reminder | DSH 已证明"超时 + 重复提醒"防跑偏 | D535 范式借鉴 |

## D534：Agent Notes 四态（Stage 1-D2，S5-3 知识沉淀）

**目标**：memory/ 从"四态目录存在"→"非平凡变更强制带 Note + 四态迁移语义 + 与铁律体系合并"——知识可沉淀、可归档、不腐化。

**spec 必答题**：
1. **现状盘点**：四态目录填充分布（implemented 31/proposed 3——archived/rejected 是否空？）；现有 Note 的格式/命名/与 task-brief 的关联；哪些变更类型该强制带 Note（非平凡定义）
2. **四态语义定义**：proposed（提案待审）/implemented（实现完成）/archived（归档不活跃）/rejected（拒绝留痕）的**迁移规则**（proposed→implemented 的门槛？implemented→archived 的触发？）——非平凡变更强制 Note 的门禁落点（pre-commit 组 6 或新检查）
3. **DSH 范式借鉴**：读 `dsh-agent-instructions/lib/types/state.d.ts` 四态类型——借鉴"状态机 + 迁移语义"理念自研，**不引代码**（Stage 3 前零依赖）；写清借鉴点 vs 自研边界
4. **与铁律合并**：AGENTS.md 铁律速览引用 memory/ Notes（不推翻现有铁律——合并而非替代，施工图边界）
5. **存量迁移**：31 implemented + 3 proposed 按新语义规整（标注/归档不活跃项）

**验收**：非平凡变更无 Note → 门禁拦（物理）；四态迁移规则文档化；存量 notes 规整后分布合理（archived/rejected 有实际内容）

## D535：guard 循环卫生 + 超时（Stage 1-D4，控制塔防跑偏）

**目标**：控制塔防跑偏通用化——借鉴 DSH timeout-policy + repeat-tool-reminder，融入现有 guard（不新建机制）。

**spec 必答题**：
1. **现状盘点**：incident-loop.py/staging_guard.py 现有防跑偏逻辑；控制塔长跑脚本的超时现状（D521 的 simulate-ci 覆盖提交链路，guard 超时未通用）
2. **DSH 范式借鉴**：读 `dsh-tool-call-timeout-policy/lib/index.js`（工具声明 timeoutMs + deadline/timeoutOf 执行）+ `dsh-repeat-tool-reminder`（重复工具提醒）——借鉴"超时执行 + 重复提醒"理念，**融入现有 guard 而非新建**
3. **通用化设计**：控制塔脚本的循环卫生标准（长跑超时 / 重复操作提醒 / 防跑偏信号）——最小机制（能融入现有 guard 的绝不新建；按 D529 防膨胀教训）
4. **边界**：不膨胀——明确"不做什么"（不新增门禁组/不新建 guard 脚本；只通用化现有）

**验收**：控制塔长跑脚本有超时/循环卫生（grep 物理证明）；防跑偏信号通用化（文档 + 接线）

## 写集约束

- **可碰**：`memory/notes/*`（四态规整）、`AGENTS.md`（铁律引用 Notes）、`scripts/control-tower/`（guard 融入）、pre-commit 相关（Note 门禁，如需）
- **不碰**：`src/`、`scripts/audit/`、产品代码
- **防膨胀**（D529 教训）：只做"现有机制的通用化/补机制"，**不新建 guard 脚本/门禁组**（除非 spec 论证必要）
- 两 D 独立（D534 memory/ + D535 控制塔）——允许并行出 spec

## 切片级审计

- D534/D535 完成后一次提审（K3 审 Stage 1 续，报告覆盖两 D）
- 每个 D 的 task-state 加 `"slice": "stage1-cont"` 字段
- 审计验收 = 四态迁移规则文档化 + Note 门禁物理 + guard 通用化接线（grep 调用方）

## 给 dev-doc 的交付要求

1. D534/D535 两个独立 spec（`SYNOVA-IMPL-DSH-D534-*/D535-*`），各含写集表/必答题/验收/自检
2. 两个 spec 的"DSH 借鉴"必须写清：借鉴什么理念 + 自研什么 + 不引代码（Stage 3 前零依赖）
3. 验收物理可复现（门禁拦截实测 / grep 接线 / 存量规整分布）
4. 防膨胀：spec 的"不做"清单必须显式（哪些机制不新建），K3 可核

---

## 写后自检（SOP §〇c ③，8 项）

- [x] ① 验证点编号: N/A（治理切片，非 product-progress 验证点——对应施工图 Stage 1 D2/D4）
- [x] ② D# 未占用: D534/D535 alloc 分配确认（D533 之后）
- [x] ③ 依赖链: D534/D535 依赖 D1/D3（D500/D474 已 audited）；两 D 互不依赖
- [x] ④ DSH 借鉴核查三步完整: 四色（🟡）/有借鉴（四态+timeout/reminder）/源码参考（dsh-agent-instructions + dsh-tool-call-timeout-policy，路径+行号）
- [x] ⑤ 现状材料全部核实过: memory/ 四态目录 + 31/3 分布、incident-loop/staging_guard、DSH 包存在（grep/ls 物理确认）
- [x] ⑥ 验收物理可复现: 门禁拦截实测 / grep 接线 / 存量分布——非文档声称
- [x] ⑦ 术语一致: "迁移轨 Stage 1" vs "L1 桌面端切片"——本单明确 Stage 1（施工图迁移轨），无混淆
- [x] ⑧ 无遗漏: 执行方（dev-doc）/依赖（D1/D3）/防膨胀约束/审计项——齐全

**交付门槛**：dev-doc 拿到可直接开工（基线/借鉴/写集/防膨胀已写死）+ K3 可核（物理可复现）。
