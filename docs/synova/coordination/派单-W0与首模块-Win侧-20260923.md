# 派单 — Win 侧 W0 前置 + 首模块（`src/middleware/**` 默认安全姿态）

- 派单方：Mac-CTO ｜ 执行方：Win-Codex-CTO（带队，DSH 小队执行）｜ 终审：CTO 收件闸 + K3
- 依据：本件为 **一次一个模块**（WIP=1）规则下的**第 0 单 + 第 1 单**；通过前不放行第 2 单。
- MOVO 对照类条目（M2/M3/M5/M6/M7）**全部挂起**，不进本派单。

## 一、W0 前置（非代码，先做，判据不过则停手报 CTO）

| # | 前置 | 判据（命令 + 期望） |
|---|---|---|
| W0-1 | 拉平 main | `git fetch --all && git status -sb`（无 behind）→ `git rev-parse --short HEAD` 记录值；技能 `bash scripts/workflow/sync-dsh-skills.sh --check` → SYNC-OK |
| W0-2 | 门禁可跑 | `bash scripts/pre-commit-check.sh` → 13 组通过。**若因 `~/.dsh`/凭据缺失跑不了 → 明确降级声明，不许静默跳过** |
| W0-3 | 自验独立性可达 | 重开队长 session → team 三 grep：agent-team/tool-agent-team/ui-agent-team 各 **1**、`delegation` **0**、`maxMembers` **4** |
| W0-4 | 主工作区归位 | 主工作区在 `main`（任务一律在 `.synova-wt-*` 内） |

**W0 不过 → 不派第 1 单。** W0 回报格式：上表四条判据的**原始输出**。

## 二、首模块：`src/middleware/**`（默认安全姿态）

**写集（整模块独占，均在 Win 域）**：`src/middleware/**` + 其消费点 `src/routes/**` + 装配点 `src/server.ts`。
**不做什么**：不动 `src/l3|l4|l5|sentinel|store|agent|orchestrator/**`（Mac 域）、不动 `scripts/**`、不动 `scripts/audit/**`。

### 完成标准（**全部为可执行判据，不是形容词**）

| # | 判据 | 形式 |
|---|---|---|
| 1 | 无凭据请求**不再获得 admin** | **穿入口用例**：构造无 token 请求 → 断言角色 ≠ admin（`src/middleware/rbac.ts:119` 兜底改拒绝/`staff`） |
| 2 | `getPermissionFilter` 返回**真实条件** | 调用点返回非空；**夹具：把过滤器改回恒空 → 必须变红** |
| 3 | `canAccessWorkspace` 在**写端点**生效 | 不再在 `src/server.ts:249` 被 `void` 丢弃；反例夹具同上形式 |
| 4 | 鉴权中间件顺序修正 | `src/server.ts:373` 之前的 9 组路由不再先于鉴权 |
| 5 | **每条修复各带 1 个"改坏即红"的变异体反例** | 自验须给"改坏 → 夹具红"的原始输出 |
| 6 | 三路径夹具：正常 / 降级 / **边界** | 降级必须显式（`log.warn` + `degraded: true`，铁律 11/24/31） |

### 执行形态（**硬字段，缺一不派**）

- 用 **Agent Teams** 组队（预设已挂 agent-team / tool-agent-team / ui-agent-team，`maxMembers=4`）；**队长不下场写码**
- 成员编制：队长 + ≤2 编码 + **1 独立自验**（自验不得由编码兼任）
- 每成员一份共享任务（**写集 + 验收命令 + 写者**），写集两两不重叠；`src/server.ts` 单写者
- 重型验证（vitest/全量门禁/黄金门禁）**串行 ≤1**
- **回执必含「团队成员运行记录」**（成员名 · 运行状态 · token · 共享任务 id 与状态）；**只有队长在跑 = 退回**

### 回执格式（缺项视为未完成）

1) W0 四条判据原始输出 2) 每条约修复：`file:line` + 改前/改后 + **反例夹具的原始输出** 3) 三路径夹具输出 4) `git diff --stat` + `ls-remote` 回执 5) **未清项清单**（诚实登记，不许写成"已完成"）6) 团队成员运行记录

### 红线

不碰 `scripts/audit/**`；禁 `--no-verify` / `git stash` / force push 共享分支；任务在 `.synova-wt-*`；计数/引用**必须带命令 + 口径 + 截至时刻**；**不判"通过"**——通过与否归 CTO 收件闸 + K3。

### 退回规则

**退回只退本单**，不冻结其它；但**第 2 模块不放行**，直到本模块通过。
