# 派单：D510 = D504 审计返修（F1/F2 两个 P1，K3 判 implement 归因）

> 派单: CTO | 2026-08-23 | 认领: 🛠 Mac 编码 session | 依据: K3 审计 2026-08-23-D504-D505.md（CONDITIONAL PASS）
> D382 铁律: 审计问题另起 FIX 任务（D510），禁改原 D504 分支历史

## F1（P1·M2 声称 vs 事实）commit message 与 task-state note 矛盾
- **事实**: commit msg 称「electron-builder --dir 实测产出 release/mac/SynovaAgent.app（DS4 物理证据）」；但 note 诚实写「DS4 受限→CI 执行→GS-01 替代」，且 commit 内无 release/ 产物；GS-01 的 L1-1 断言是 `grep -q extraResources` 静态检查非真实打包。
- **修法**: ①本地真实跑一次 `electron-builder --dir`（mac 本机可执行），产物截图/ls 输出作证据落 task-state.evidence；②跑不动则改 commit 策略——**新 FIX commit 的 message 诚实描述**（"DS4 静态配置检查 + 本地实测留待 founder-demo"），并在 GS-01 断言注释标明「静态检查非打包验证」。二选一，禁止声称与证据不符。

## F2（P1·M7）DS11 未完成未标注
- **事实**: spec DS11（本地 dmg 产出+安装+双击实测）未做，note 写「留待 founder-demo」但 commit message 未标注 descope。
- **修法**: D510 的交付声明表（U4）中显式列 DS11 = DESCOPE（原因: 需创始人本机参与双击实测，非编码可独立完成）+ 在 D504 task-state 补 descope 字段。descope 显式化即合规，藏着不写才是 M7。

## 附带（P2 顺手修，不阻断）
- F3: pre-commit 门禁处置方式在交付报告写明（用了什么豁免/放行路径）
- F5: 测试数口径统一（"25 用例"vs"3 文件 25 tests"的表述）

## 写集（预期）
- scripts/golden-scenarios/GS-01-first-diagnosis/（断言注释诚实化）
- task-state/D510.json + D504.json（descope 补注）
- electron/（如走 F1 ①路线，仅加 builder 脚本配置，不动 main.cjs 逻辑）

## 验收
- [ ] commit message / 交付声明 / 实际产物 三者一致（K3 复审核对点）
- [ ] DS11 显式 DESCOPE 或实测完成
- [ ] electron 测试 25/25 不回归

## 给编码 session 的上下文
- D504 原分支: 本地（未 push 远程）；K3 审计报告: docs/synova/audit-reports/2026-08-23-D504-D505.md（审计分支）
- 你的 D505 已 PASS（恭喜），D504 只差这两个诚信修复即可 PASS——不需要重写代码
