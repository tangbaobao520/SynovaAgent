# 派单：下一批四线并行（D715 K3 审计 / D716 dev-doc / D717 编码 / D718 并行 CTO）

> 派单: 主 CTO（synova-cto） | 2026-09-13 | 路由: **第二类（控制塔/治理，直派执行方）+ 第一类（D716 需 spec 先行）**
> 依据: `board-backlog.json`（39 条）+ 三仪表盘 + 本批实测发现
> 前置已完成: D713 签名修复(#512) / D714 F3 渲染层修复(#514 已入 main) / D712 双侧重验证据(#510/#511)
> 创始人要求: 处理完桌面端后「开始安排其他任务」

---

## 〇、批次总览

| D# | 任务 | 执行方 | 优先级 | 依赖 |
|---|---|---|---|---|
| **D715** | 控制塔收口切片审计 + 线 1 证据审计 | **🔍 K3 审计** | **P0（关键路径）** | D706/D707/D664/D708 已合并；D712 证据双侧齐 |
| **D716** | D-1 监测契约 schema + 1-5 双引导收敛 spec | **📋 dev-doc** | P0 | 创始人已裁 D-1（状态驱动通知模型） |
| **D717** | 产品真债两项：干净安装必失败 / 机制零调用 | **🛠 Mac 编码** | P1 | 无 |
| **D718** | 控制塔小修批三项（各 1 行级） | **🧭 并行 CTO** | P1 | 无 |

**并行性**：四线写集零交集（审计=只读 + 报告；dev-doc=`docs/plans/`；编码=`src/`+`package.json`；并行 CTO=`scripts/`+`tests/`）。**仅 D717 与 D718 需注意**：D717 动 `package.json`（依赖），D718 不动依赖。

---

## D715｜🔍 K3 审计批（P0 关键路径）

**为什么现在**：`product-progress` 显示 35 个点卡在 `pending_k3`，且线 1 的全部机器绿点（1-1/1-3/1-4/1-6/1-7 + Win 1-2/1-7）**必须经 K3 才能转 verified**——审计是整个体系的吞吐瓶颈，先清这一批。

**审计对象（两个切片）**：
1. **控制塔收口切片**：D706（提交链丢删除项）/ D707（brief 双解析器）/ D664（grep -P 家族）/ D708（合并级写集 gate）
   - 材料：`task-state/D706.json`、`D707`、`D664`、`D708`；各自 PR #501/#502/#504/#505 与 main 上的 squash 提交
   - 重点：① D706 的"索引精确提交 + 提交树不变量 + 失败回滚"是否真成立（沙箱复现删除场景）② D707 的单一解析器是否真的收敛（两写法 + 空值负例）③ D664 的 grep -P 是否真清零（注意：注释/检测器字面量属豁免，**不是**残留）④ D708 gate 的 T3 实证（人为夹带 → 红 → 撤回 → 绿）与 fail-closed 三态
   - **D708 特别注意**：其"只防多不防少"是**已声明边界**，不构成缺陷
2. **线 1 桌面端证据切片**：`docs/synova/product-lines/evidence/D712-mac-20260913/`（Mac）+ `D712-win-20260913/`（Win）+ `scenario-2026-09-13*.json`
   - 重点：① 机器证据是否真为实测（对照留档 transcript/process/window/timing）② Win 侧三个 failed（1-2/1-4/1-6）的**环境阻塞 vs 产品缺陷**判定是否成立（Win 侧已给出环境事实表；1-4 的 F3 已由 D714 修复，需判"修复后是否可转绿"）③ **1-4 的判据**：注意 CTO 已把 Mac 的 1-4 由 pass 修正为 **fail**（能开窗≠UI 可用）——请以修正后为准

**必答**：① 每个 D 的验证点从 unclaimed → verified/failed 的结论 + 物理复现命令 ② D708 的 gate 是否真的挡住一次夹带（贴 job 级 run 结论）③ 线 1 各点当前应处于什么状态（含 D714 修复后的重判）④ 发现的新缺陷（含 file:line）

**写集**：`docs/synova/audit-reports/**`、`task-state/D*.json`（仅 audit 段）、`docs/synova/coordination/AUDIT-FINDINGS-LEDGER*.md`
**红线**：审计结论只认 K3；不改 `scripts/audit/`；不替创始人下业务结论

---

## D716｜📋 dev-doc：两份 spec（P0）

**spec 1：监测契约 schema（D-1 决策落地前置）**
- 背景：创始人 2026-09-13 裁定 D-1 改为**状态驱动通知模型**（决策全文 `docs/synova/product-lines/DECISION-D1-状态驱动通知模型-20260912.md`）
- 交付：契约文件 schema（每客户/每指标：阈值、渠道、**节奏**、**格式**、升级规则、责任人）+ 文件位置 + 示例 + 默认值（24h 未回应提醒 / 7 天无进展进周报首页 / 默认周报一页纸 / 明确不处理每周回顾）
- **跨线边界必写**：契约文件目录 = Win 域，loader = Mac 域（`src/sentinel/`）——两侧均不得双写（见 `docs/synova/coordination/跨线同步-D1状态驱动通知模型-Mac-Win-20260912.md`）

**spec 2：1-5 双引导收敛（线 1 唯一 failed 点）**
- 现状（实测）：`src/server.ts:300` 仍 `app.use('/app', express.static(...))`，且 `app/setup.html` + `app/js/setup.js` 仍在仓库 → 打包态出现**双安装引导入口**
- 归属警示：`src/server.ts` = **Claude 专属**（DSH 不碰，CODEOWNERS）→ 本 spec 由 DSH dev-doc 写，**实施归 Win 线**
- 必答：收敛到哪个入口？另一入口如何退场（删除 or 重定向）？对既有用户的书签/URL 兼容怎么处理？验收怎么物理证明"只有一个入口"？

**写集**：`docs/plans/codex/implementation/SYNOVA-IMPL-DSH-*.md`、`task-state/D716.json`
**流程**：经 `dev-doc-gatekeeper` 结构门禁（Q0-Q3/写集/DS 清单/Done 可证伪）

---

## D717｜🛠 Mac 编码：产品真债两项（P1）

1. **`PLAN-react-markdown-dep`** — `use-streaming-conversation` 未声明依赖，**干净安装下必失败**（前审 21/21 绿是污染安装态）
   - 修法：在 `package.json` 正确声明依赖 + 干净安装复现（`rm -rf node_modules && npm ci`）→ 相关测试绿
   - 验收：干净安装后 `vitest run` 相关套件绿；**必须贴干净安装的输出**
2. **`PLAN-expireold-wiring`** — `evidence expireOld()` 存在但**全仓零生产调用方**（M3 机制建成未接线）
   - 修法：接线到真实调用点（谁该调用它？数据保留分级策略见 backlog 条目）+ 补接线断言
   - 验收：`grep -rn "expireOld" src/` ≥1 生产调用方 + 测试断言存在

**写集**：`src/**`（除 `src/sentinel/`、`src/cron/`、`src/mcp/` 属哨兵切片他人域）、`package.json`、`tests/**`
**禁止**：不改 `scripts/`（控制塔=CTO 域）；不同时改锁文件与其他依赖（每批一个 agent 改依赖，惯例）

---

## D718｜🧭 并行 CTO：控制塔小修批（P1，三项均 1 行级）

1. **alloc 测试泄漏骨架进真仓**：`tests/control-tower/alloc-task-id-lock.test.sh:42` 只设 `SYNO_TASK_STATE_DIR` 未设 `SYNO_BRIEF_DIR` → 每次运行把 20 份骨架 brief 写进**真实仓库**（实测泄漏 56 份，主树亦见 5 份残留）
2. **`resolve-commit-brief.sh:79-90` ±1 天窗口**：任务在 brief 创建日 **>1 天**后执行时**永远无法认领自己的提交** → D328 硬阻断（D664 被拦 2 次，真痛点）
3. **`pre-doc-audit.sh` 首行 BOM**：直接执行报 `#!/usr/bin/env: No such file or directory`（存量）
4. 附带：为「证据 .log 被 .gitignore 静默忽略」加白名单 + 写入后 `git check-ignore` 自检（见 `PLAN-evidence-log-gitignored`）

**写集**：`scripts/control-tower/`、`scripts/workflow/`、`tests/control-tower/`、`.gitignore`
**要求**：每项独立 PR；先复现再修；反向验证（移除修复必红）；`ctrl-tower-change` + `windows-compat` 技能必加载

---

## 派单说明（给创始人复制——四段，分别转给对应执行方）

### ① 转 K3 审计 session（🔍，模型选 kimi-k3）
```
【D715】控制塔收口切片 + 线 1 桌面端证据 · K3 独立审计（P0）
> 完整派单: docs/synova/coordination/派单-下一批四线并行-20260913.md §D715
> 材料: task-state/D{706,707,707,708}.json + PR #501/#502/#504/#505；证据目录 docs/synova/product-lines/evidence/D712-mac-20260913/ 与 D712-win-20260913/
> 必答: ① 各 D 验证点结论 + 物理复现命令 ② D708 gate 是否真挡住一次夹带（贴 job 级结论）③ 线 1 各点当前状态（含 D714 修复后的重判；注意 Mac 的 1-4 已由 CTO 修正为 fail）④ 新缺陷（file:line）
> 红线: 结论只认 K3；不改 scripts/audit/；不替创始人下业务结论
```

### ② 转 dev-doc session（📋）
```
【D716】两份 spec：监测契约 schema + 1-5 双引导收敛（P0）
> 完整派单: docs/synova/coordination/派单-下一批四线并行-20260913.md §D716
> spec1 依据: docs/synova/product-lines/DECISION-D1-状态驱动通知模型-20260912.md（创始人已裁）+ 跨线边界文档
> spec2 现状: src/server.ts:300 仍挂 /app 静态服务且 app/setup.html 仍在 → 双引导；**实施归 Win 线（src/server.ts = Claude 专属）**
> 要求: 过 dev-doc-gatekeeper；写集声明；Done 可证伪
```

### ③ 转 Mac 编码 session（🛠）
```
【D717】产品真债两项（P1）
> 完整派单: docs/synova/coordination/派单-下一批四线并行-20260913.md §D717
> ① react-markdown 未声明依赖 → 干净安装必失败：修复后必须贴 rm -rf node_modules && npm ci 的绿证据
> ② evidence expireOld() 全仓零生产调用方（M3）：接线 + 补断言
> 写集: src/**（sentinel/cron/mcp 除外）、package.json、tests/**；不改 scripts/
```

### ④ 转并行 CTO session（🧭）
```
【D718】控制塔小修批三项（P1，均 1 行级）
> 完整派单: docs/synova/coordination/派单-下一批四线并行-20260913.md §D718
> ① alloc-task-id-lock.test.sh:42 缺 SYNO_BRIEF_DIR → 泄漏骨架 brief 进真仓（实测 56 份）
> ② resolve-commit-brief.sh:79-90 ±1 天窗口 → 跨日任务永远认领不了自己的提交（D664 被拦 2 次）
> ③ pre-doc-audit.sh 首行 BOM → 直接执行报 env not found
> 要求: 每项独立 PR；先复现再修；反向验证必红；加载 ctrl-tower-change + windows-compat
```
