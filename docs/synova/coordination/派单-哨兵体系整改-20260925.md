# 派单 — 哨兵体系整改（2026-09-25）

**依据计划**: `docs/synova/coordination/整体推进计划-主线-20260913.md` @ `4e46603f`
**指引文档**: `docs/synova/architecture/SYNOVA-哨兵体系-最终版-v1-20260925.md`（十环判据 / measurements / 权限矩阵）
**号段说明**: 全局递增取号 D1003–D1006；**号段 ≠ 归属**，归属由卡内 `domain` 决定（本批 `domain=mac`）

---

## 〇、八项派单前复核（CTO 亲自跑，证据在此）

| # | 项 | 证据 |
|---|---|---|
| ① | **任务号真实** | 现场分配：D1003/D1004/D1005/D1006（`task-state/` 落盘） |
| ② | **前置状态真实** | #759 `merged=True`（D963 数据源在 main）｜`origin/main` = 本批基线；**未依赖任何未合 PR** |
| ③ | **技术声称亲自复现** | 见 §一（含**与院方声称不一致**的记录） |
| ④ | **写集路径在场** | `src/sentinel/sentinel-loader.ts`／`runner.ts`／`baseline-store.ts`／`src/agent/post-diagnosis-processor.ts`／`src/adapters/sqlite-graph-store.ts`／`src/sentinel/adapters`（@deprecated 待裁）／`extensions/sentinels` ✅ 全部存在；**`measurements` 写入点标注「待建」** |
| ⑤ | **写集零交集** | 见 §四（逐对比较；`extensions/sentinels/**` 由 D1003 独占，D1004 只读） |
| ⑥ | **引用用符号不用行号** | `src/sentinel/sentinel-loader.ts` 的 `sentinelsDir()`／`resolveThresholds()`；`src/sentinel/runner.ts` 的 `createAutoTicket` |
| ⑦ | **依赖图** | 见 §三 |
| ⑧ | **交付物齐备** | 每卡含 写集／红线／Done（可证伪）／前置 |
| ⑨ | **派单内部一致性** | 见 §五（互斥检查通过） |
| ⑩ | **主线计划锚定** | 计划 hash `4e46603f`（当场计算） |

---

## 一、CTO 亲自实测量（**执行方必须独立复现，不得采信本单**）

**方法**：真实 `SqliteGraphStore` + `data/synova.db` 副本 + `SENTINELS_FIXTURE_DIR` 默认；逐个 `await aggregate.check(store,'probe-team')`；插桩 `queryNodes/queryEdges/traverse` 计数。

```
哨兵总数 = 45
✅ check() 返回数组 = 42
❌ 抛错/异常返回 = 3
⚪ 跑通但零 finding = 23
   调用 store 次数 = 0 的哨兵 = 2（sentinel-forecast-accuracy, sentinel-pricing-strategy）
   异常清单：path-dependency → Cannot find module '.../path-dependency/aggregate...'
             sentinel-forecast-accuracy → 返回非数组
             sentinel-pricing-strategy → 返回非数组
```

**⚠️ 与院方《边界评估》声称的差异（必须裁清，禁止糊过去）**：
- 院方：**20/45 哨兵 `queryNodes` 调用 = 0**
- CTO 复现：**2/45 零调用**（且这 2 个是异常返回，计数停在 0）
- **差异原因未定**（可能：院方为**静态口径**「源码未出现 `queryNodes`」；或不同断面）
⇒ **D1004 的第一件事就是独立复现并给出差异原因**（静态口径 vs 运行时口径分别出数）。

---

## 二、四张卡（本批）

### D1003 — 修 3 个坏件（Mac 小队）
- **写集**：`extensions/sentinels/path-dependency/**`｜`extensions/sentinels/sentinel-forecast-accuracy/**`｜`extensions/sentinels/sentinel-pricing-strategy/**`｜`task-state/D1003.json`
- **Done（可证伪）**：重跑 CTO 的 45 哨兵探针 → **抛错 = 0**；三者的 `check()` 均返回 `SentinelFinding[]`
- **红线**：**不得为此新增哨兵**（院方禁令 3）；`path-dependency` 若判定应裁撤 ⇒ 走"裁剪 + 登记"而非留空目录
- **前置**：无

### D1004 — 独立复现「空转/零调用」并出三类清单（Mac 小队）
- **写集**：`tests/sentinel/**`（新增复现夹具）｜`docs/synova/coordination/哨兵-空转复现-20260925.md`｜`task-state/D1004.json`
- **Done**：① 独立复现 **静态口径**与**运行时口径**两套数字 ② 给出差异原因 ③ 输出三类清单：**真算的 / 空转的（不读数据）/ 缺件的**
- **红线**：**只出清单与证据，本卡不改任何哨兵**（改动归 D1003/后续卡）；**禁采信本单与院方数字**，以自己跑出的为准
- **前置**：无（可与 D1003 并行；写集无交集）

### D1005 — `measurements` 时序层（Mac 小队）
- **写集**：`src/store/**`（新建 measurements store 与 DDL）｜`src/sentinel/**`（接线点）｜`src/agent/post-diagnosis-processor.ts`（**修零值污染**）｜`src/sentinel/baseline-store.ts`（改名 `sentinel_alert_stats`）｜`tests/**`｜`task-state/D1005.json`
- **Done（缺一不算）**：
  ① 按指引 §4.1 **逐字段**建表 + 索引（含 `run_id`／`input_digest`／`def_version`）
  ② `UPDATE/DELETE measurements` 在 `src/` 内 **grep = 0**（append-only）
  ③ 跑一次哨兵 ⇒ `measurements` 有该 `metric_id` 行（贴 SQL）
  ④ `sentinel_baselines` → `sentinel_alert_stats` 改名 + 3 处读写点全绿 + `post-diagnosis-processor` 不再写 `finding_count=0` 伪条目
  ⑤ **产品价值验证**：同 `metric_id` 两个 `computed_at` 可相减（回答"上月 runway 3 个月、本月 1 个月"）
- **前置**：无（但 ④ 的改名会动 `baseline-store.ts` ⇒ 与 D1006 无交集）

### D1006 — 统一注册为单入口（Mac 小队）
- **写集**：`src/sentinel/adapters/**`（裁剪）｜`src/sentinel/builtins.ts`｜`src/init/file-driven-loaders.ts`｜`src/sentinel/runner.ts`｜`tests/**`｜`task-state/D1006.json`
- **Done**：① `adapters/` 5 个 @deprecated 哨兵**裁剪或迁移**（二选一，不留第三态）② `loadSentinels()` 加载数 = 目录数 ③ **"只加一个目录、不改一行代码 → 跑通十环"**证明（贴命令+输出）
- **红线**：**不得"修 1 行键名就宣布第二套哨兵活了"**（院方禁令 4）；需证明**真产出 finding**
- **前置**：D1004（其"三类清单"决定 adapters 里哪些可裁）

---

## 三、依赖图

```
D1003 ─┐
D1004 ─┼─→ D1006（依赖 D1004 的三类清单）
D1005 ─┘（独立）
后续批：excellence 正向信号 / dependsOn.nodeTypes 统一 / customer-growth 覆盖 / 权限映射（待前四落地）
```

## 四、写集零交集（逐对比较）

| 对 | 共同写集 | 结论 |
|---|---|---|
| D1003 × D1004 | 无（D1004 只读 `extensions/sentinels/**`，写入 `tests/**` + 文档） | ✅ 可并行 |
| D1003 × D1005 | 无 | ✅ |
| D1003 × D1006 | `extensions/sentinels/**`（D1006 只读判断） | ✅ 串行调用即可 |
| D1005 × D1006 | `src/sentinel/runner.ts`（D1005 接线点 × D1006 去冗余加载） | ⚠️ **串行：D1005 先，D1006 后** |

## 五、派单内部一致性自检

- D1003 要求"抛错 = 0"，D1004 要求"出三类清单（可能含缺件）" ⇒ **不互斥**（D1004 出清单，D1003 只修已列三者）。
- D1006 要求"裁剪 adapters"，D1004 要求"只出清单不改动" ⇒ **不互斥**（域不同）。
- `src/`: 本批四卡均**只动各自列出的文件**，无对同一路径互斥的要求。

## 六、红线（全批通用）

- 不碰 `scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**` 之外的越域文件；不改 `ci.yml` / `pre-commit-check.sh`（Mac-CTO 单写者）
- **禁为此新增哨兵**（院方禁令 3）｜**禁"修 1 行就说活了"**（禁令 4）
- 每卡独立分支 + 独立 PR；**PR 由 CTO 开**；**K3 前不合并**
- 验收命令与期望结果**逐卡列出**（见各卡 Done）
- 回执三件套：成员运行记录 / **改坏即红原始输出** / 未清项诚实登记
