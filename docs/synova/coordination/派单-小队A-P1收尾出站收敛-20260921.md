# 派单｜小队 A：P-1 收尾——全仓出站收敛（D862 + 收敛清单）

> 出件 2026-09-21 CTO ｜ 执行形态：**agent 小队（编码 2 + 自验 1；队长＝并行 CTO session）** ｜ 域：win → **Mac 代行** ｜ K3 队外

## 〇 编制与工作树
工作树 `.synova-wt-p1b` ｜ 分支 `feat/p1-outbound-converge` ｜
写集：`src/connectors/**`、`src/tools/**`、`src/routes/**`、`src/agent/**`、`src/cli-manager/**`、`src/l1/**`、`src/tui-v2/**`、`src/deploy/bootstrap.ts`、`src/providers/http-exit.ts`（**只读复用，不改契约**）、`tests/**`、`task-state/D862.json`
**硬边界**：不碰 `scripts/control-tower/**`、`scripts/audit/**`、`.github/workflows/**`、`src/sentinel/**`（小队 B 地盘）

## 〇-b 为什么派它
D821 只收敛 **providers 层**（4→0）；**connector/tools/routes/agent/cli-manager/l1/tui 仍裸 fetch**（真实出站 39–40 处）。客户内网"只允许代理出网"拓扑下这些**全部失败** → **P-1 只能算『部分关闭』**。
**做完推动**：P-1 由『部分关闭』→**关闭**（内网可部署从口号变实测）。
**架构层**：**L1/L2 出站面** + `src/deploy` 启动期。

## 〇-c 权威件回执
`src/providers/http-exit.ts` 契约（D821 规格 `docs/plans/2026-09-20-D821-出站HTTP唯一出口-规格.md` §3.3 收敛清单）｜D821 自验记录｜验收标准 §十一/§十二
撤回自检：**撤回 7 直接相关**（DSH telemetry 非可观测参照 → 不得照抄）｜禁 G1：`grep -rn "@deepseek-ai" src/ packages/` = 0

## 〇-d 旧信息漏洞自检
- [x] 引用均在仓库内｜[x] write_set 与小队 B/控制塔线**零交集**

## 〇-e 前提实测（完整输出，**注意 `.tsx`**）
1 `grep -rn "fetch(" src/ --include=*.ts --include=*.tsx | grep -v .test.` → 出站清单（**必须含 .tsx**：`src/tui-v2/chat.tsx:248` 是真实出站）
2 `grep -rn "@deepseek-ai" src/ packages/` → 必须 0
3 读 D821 规格 §3.3 收敛清单，逐条对照**是否与实测一致**（不一致 → 以实测为准并登记）
**复核项**：① 哪些调用**不该**走该出口（本地文件、MCP stdio、进程内）② connector 是否需要**独立凭据/超时策略**（不得把 provider 策略硬套）③ 启动期 bootstrap 走同一配置时，代理不可达是否 degraded 可见

## 一 任务分解
① D862 三项：connector 覆盖 / 健康检查**只暴露是否配置、不含值** / 启动期 bootstrap；② 全仓收敛清单逐条迁移；③ 每处迁移**信号语义对齐**（abort/timeout/error 分类不得丢）；④ 反向验收：抽掉一处迁移 → 夹具必须红

## 二 完成判据
- [ ] `grep fetch(` 全仓归零（给 before/after 计数 + 命令）
- [ ] 假代理**收到全部出站**（逐条日志，含 .tsx 路径）
- [ ] 健康检查输出**不含值**（尝试反推凭据 → 不可能）
- [ ] 代理不可达 → `degraded` 可见，**无静默直连回退**
- [ ] G1 = 0｜回归全绿｜M6 三件已提交｜PR ≤12 文件单域｜`ls-remote` 回执
- [ ] **P-1 在 `preconditions.yaml` 标『关闭』并附证据指针**（改前须先确认 D862 depends_on D821 已合）

## 三 红线
不改 `http-exit.ts` 契约（只复用）；不碰控制塔/审计/CI 文件；禁 `--no-verify`/stash/force push；K3 复审前不合并
