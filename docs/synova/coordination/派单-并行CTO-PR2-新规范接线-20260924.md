# 派单 — 并行 CTO：PR-2「新规范接线」（可直接开新会话执行）

> **接收方 = 新的 CTO session（无本次对话记忆）**。本件自包含：背景、任务、文件清单、坑、完成标准、红线。

## 〇、你是谁 / 在哪做

你是 SynovaAgent 的**并行 CTO**（与主 CTO 同权同责，只做本单）。
工作目录：`/Users/wane/SynovaAgent`。GitHub：`tangbaobao520/SynovaAgent`。
Token：`grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml` 取值（`tr -d '\r\n'`）。

## 一、任务：把「新规范」接上线（PR-2）

**背景**：机制本体已落在 PR #745（`feat/d943-mechanism`：断面门禁脚本 + K3 保险脚本 + 看板 runner + 夹具 + `DSH-断面.json` + 已作废口径表）。
**但 #745 故意不含接线** —— 因为协调文档里仍有**旧断面锚**（`0.1.7-alpha.2 @ 00102833`），自扫一开必红。
**PR-2 就是接线那一刀**。

### 待交付（**只动这 9 份文件**）

从分支 `docs/d943-cto-a-items` 取以下文件的内容（`git show origin/docs/d943-cto-a-items:<路径>`）：

**A. 修过旧断面锚的 5 份文档**（把 `00102833` → `46a7f68b`、`0.1.7-alpha.2` → `0.1.7-rc.1`）
1. `docs/synova/coordination/TASK-ROUTING.md`
2. `docs/synova/coordination/Win侧同步包-20260923.md`
3. `docs/synova/coordination/两侧协作方式-Mac与Win-20260923.md`
4. `docs/synova/coordination/开工指令-Mac小队-D937-20260924.md`
5. `docs/synova/coordination/派单-W0与首模块-Win侧-20260923.md`

**B. 3 份规范件**
6. `docs/synova/coordination/收件闸检查单.md`
7. `docs/synova/coordination/规格-M9-门禁三件套-唯一事实源.md`
8. `docs/synova/coordination/派单模板.md`（含「D943 强制表头 + 机制在场性三问 + 文件 owner 在飞检查」）

**C. 接线（关键）**
9. `.github/workflows/ci.yml`：在 `control-tower-tests` 清单里**加一行** `python3 scripts/control-tower/check-dsh-anchor.py --repo . --no-tree-check`
10. `scripts/pre-commit-check.sh`：加一行 `soft_check "DSH 断面一致性 (D943)" "python3 scripts/control-tower/check-dsh-anchor.py --repo . >/dev/null 2>&1"`

（注：文件 9/10 的改动形态已在 `docs/d943-cto-a-items` 上，直接取该分支版本即可。）

## 二、硬顺序（**先本地验证绿，再提交**）

```bash
# 1) 本地带树校验（本机有 DSH 树）
python3 scripts/control-tower/check-dsh-anchor.py --repo .            # 必须 DSH-ANCHOR: OK
# 2) CI 模式（无树）
python3 scripts/control-tower/check-dsh-anchor.py --repo . --no-tree-check   # 必须 OK
# 3) 两份夹具
bash tests/control-tower/check-dsh-anchor.test.sh   # 10/10
bash tests/control-tower/check-k3-report.test.sh    # 8/8
```
**若第 1/2 步不 OK ⇒ 停手报主 CTO**（不要靠"先提交再看 CI"）。

## 三、必须避开的坑（都踩过，别再踩）

1. **推送必须显式 https**：`origin` 是 SSH 且认证失效（`Permission denied (publickey)`）。用：
   ```bash
   HDR="Authorization: Basic $(printf 'x-access-token:%s' "$TOKEN" | base64 | tr -d '\n')"
   git -c http.extraheader="$HDR" push https://github.com/tangbaobao520/SynovaAgent.git HEAD:<分支名>
   ```
   **推送后必须验证远端 sha**（不能只看命令没报错）。
2. **写集声明**：新建 task brief（`.claude/task-briefs/<你的任务>.md`），**Q2 逐行列出上述 10 份文件**，
   并**把 brief 自身路径也写进 `task-state/<卡>.json` 的 `write_set`** —— 否则 `merge-writeset-gate` 判夹带。
3. **D734 预算**：变更文件数 **≤12**、**单域 mac**（`check-ownership.py` 必须 PASS）。**禁调高上限**。
4. **V5 平台敏感命令**：新控制塔脚本**禁裸 `python3`**（用 `PYBIN` 解析）／禁 `grep -P`／`date +%s`／`date -v`。
5. **改 PR 正文后不要 `rerun-failed-jobs`**（重放旧事件体）→ 用 **close/reopen**。
6. **正文豁免只允许** `.claude/task-briefs/**` 与 `docs/synova/product-lines/evidence/**`；**源码/脚本永不豁免**。
7. 禁 `git stash` / `--no-verify` / force push。

## 四、执行步骤

1. `git fetch -q origin`；建工作树：`git worktree add .synova-wt-cto-pr2 -b feat/d943-docs-anchor origin/main`
2. 按 §一 取 10 份文件（A/B 取 `docs/d943-cto-a-items` 版本；C 的两行按 §一 说明加）
3. 写 brief（含 Q2 写集）+ 卡（`write_set` 含全部 10 份 + brief 自身）
4. 跑 §二 的四条验证（**必须全绿**）
5. 提交（Conventional Commits；若 铁律49 要求决策 Note，则加 `memory/notes/proposed/<date>-<主题>.md` 并在 commit 里 `Note:` 引用）
6. **https 推送** + 验证远端 sha
7. **开 PR**（base=`main`）→ 报告 PR 号与 CI 结论

## 五、完成标准（五条，逐条给原始输出）

1. `check-dsh-anchor.py --repo .`（带树校验）→ **`DSH-ANCHOR: OK`**
2. `--no-tree-check`（CI 模式）→ **`OK`**
3. 两份夹具 → **10/10 与 8/8**
4. 5 份文档 **`grep -c '00102833'` 与 `grep -c '0.1.7-alpha.2'` 均 = 0**
5. 变更文件数 **≤12**、**单域 mac**（贴 `check-ownership.py` 与预算输出）

## 六、红线

- **不碰** `scripts/audit/**`、`docs/synova/audit-reports/**`（K3 域）、`src/**`（win 域）、
  `docs/synova/coordination/ownership.yaml`、`task-state/**`（除你自己的卡）。
- 交付回执须含：**声称↔证据逐条对应** + 原始输出 + **未清项诚实登记**（跑不过就写 ❌ + 原始输出，禁伪造绿）。
- 本 PR 属**门禁/机制变更 ⇒ 必过 K3**；**K3 前不合并**。
