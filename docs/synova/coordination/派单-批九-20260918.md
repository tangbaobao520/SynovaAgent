# 派单：批九 —— 第一条 100% 线收官（K3 复核）+ 控制塔卫生两个已复现缺陷

> **依据计划: v1.2@4e46603f**（`docs/synova/coordination/整体推进计划-主线-20260913.md`，CTO 派单时实测哈希）
> 出单: synova-cto ｜ 2026-09-18 ｜ 前置：批八（D811–D814）已落 main `f2ee46cb`

---

## 〇、为什么这两张卡（全局判断）

1. **线10 已到 5/6，差的最后一点是 K3 复核（10-8）** —— `ledger.json`（main `5cc619ac`）实测：`线10 资本循环 5/6`，未过点 = `10-8 审计员全链路复核（k3_only, 100% 门槛）`，`evidence: []`。D803 两条 PR 已合（#624 读侧契约守卫 + 跑道语义修复、#625 GS-03 双相位 7→9 断言 + 证据落点）。**第一条 100% 线只差独立复核这一脚** → D815。
2. **两个控制塔缺陷今天被两个不同 session 各自复现**（不是听来的）：
   - `alloc-task-id.sh` **只扫本工作树** → 跨 worktree 撞号：D810 session 首跑被分配 **D809**，而 D809 已被 `fix/d809-pending-wiring` 占用（实测：两 ref 同时存在）；CTO 本月取号时也踩过同型问题（D802 撞号 → 重编 D804）。
   - `.claude/bypass.log` 是 **post-commit hook 的运行期产物却入库** → **每个 PR 都与任何其他 PR 冲突**：今日 #614 为此返工 3 次（实测量：每次他人合并后 `mergeable` 立刻 `dirty`，`git merge-tree` 却零冲突）。→ D816。

---

## A. 两张卡

### D815｜线10 资本循环全链路独立复核（第一条 100% 线收官）（🔍 K3）

**目标**：对线10 的 6 个验收点做**独立、可复现**的全链路复核；PASS 即绑定 `10-8` 证据，线10 达 6/6。

**做什么**
1. 复核 10-1/10-2/10-3/10-4/10-6 的证据与**代码接线**（不接受测试冒充生产接线）：GS-03 真实跑（含负向用例）、ERP→Financial（erp-standard 契约）、属性契约 snake/camel（`cashBalance↔cash`）、P0 哨兵 manifest 挂载 + 阈值真实触发、现金流跑道计算、无数据不误报 critical。
2. 复核 10-5「告警 → 工单 → 推送链路」是否有生产调用点（若未接线，如实判 NOT-PASS 并给出接线缺口）。
3. 交付：`docs/synova/audit-reports/2026-09-18-K3-line10.md`（逐点 ✅/❌ + 复现命令 + 原始输出）+ `task-state/D799.json` 的 audit 回填；**若 PASS**：`docs/synova/product-lines/evidence/k3-line10-2026-09-18.json`（`kind: k3`，绑定 `10-8`）。

**写集**：`docs/synova/audit-reports/2026-09-18-K3-line10.md`（新建）、`docs/synova/product-lines/evidence/k3-line10-2026-09-18.json`（新建，仅 PASS 时）、`task-state/D799.json`、`task-state/D815.json`

**红线**：不改 `product-lines.yaml` 的验收点定义、不改证据 kind、不改 V1 v0.2 标准；PASS 必须附可复现命令；不得以"文件存在"当作"接线成立"（铁律 4/5）。

**Done（可证伪）**：报告每个点都有命令 + 原始输出；`gen-project-board.py` 重算后 **线10 = 6/6**（或如实给出未达点与缺口）。

---

### D816｜控制塔卫生：取号跨 worktree 可见 + `bypass.log` 停止入库（🛠 编码 · mac）

**两个已复现缺陷，各自独立可测。**

**缺陷 1：`scripts/control-tower/alloc-task-id.sh` 只扫本工作树**
- 实测：D810 session 首跑取号得 **D809**，而 `origin/fix/d809-pending-wiring` 已占用同号（两 ref 并存，CTO 用 `git for-each-ref` + `git worktree list` 复核）。
- 要求：取号前扫**全 refs（含未推送 worktree 的本地分支）+ 全 worktree**；或改为"分配即占位"（在 main 上先落一张空卡，冲突即拒）。
- 验收：夹具——两个 worktree 各跑一次取号 → **必须得到不同号**（当前实现会撞）。

**缺陷 2：`.claude/bypass.log` 入库导致 PR 互斥**
- 实测：今日 #614 因它反复变 `dirty`（`git merge-tree` 零冲突 = 纯 append 冲突）；D708 门禁本身已把它列为 `[builtin]` 豁免 → 说明它不属于任何写集。
- 要求：`git rm --cached .claude/bypass.log` + 加 `.gitignore`；确认依赖它的门禁（bypass 检测/D331 对账）**改从工作区读取而非 git 跟踪**，或给出替代落点；同步更新引用文档。
- 验收：`git ls-files .claude/bypass.log` 空 + `.gitignore` 含该路径 + 相关门禁测试全绿 + 新提交不再改动该文件（连续两次提交验证）。

**写集**：`scripts/control-tower/alloc-task-id.sh`、`scripts/control-tower/check-bypass-log.sh`（如需）、`.gitignore`、`tests/control-tower/alloc-task-id.test.sh`（新建/扩展）、`tests/control-tower/check-bypass-log.test.sh`（如需）、`task-state/D816.json`、`.claude/task-briefs/*D816*`、`memory/notes/**`；另：`.claude/bypass.log`（`git rm --cached`）

**红线**：不改门禁判定口径（只改取号范围与文件跟踪）；`scripts/audit/**` 不碰；不删除历史证据（只停止跟踪）。

**前置**：无（写集与 D811–D814 不重叠；改 `alloc-task-id.sh` 前加载 `ctrl-tower-change` 技能）。

**Done**：两缺陷各有一条"先红后绿"的夹具证据；`pre-commit` 全过；汇报含命令 + 原始输出。

---

## B. 顺序与 WIP 账

- **K3 线**：D815（独立，立即可开工）
- **编码线（mac）**：在途 D811 → D814 → D812（前置 #574 合入）→ D813 → **D816**（排在最后，避免与 D812/D813 同批改 `scripts/`）
- D810 session：PR-1/2（#634）合入后 rebase 推 PR-2/2；D809（#635）合入后执行附录 A 三格改回 `test`
- 已合今日：线10 两条、K3 D808 报告、批七派单、批八派单（5 条 PR）

---

## C. 本批不做（显式排除）

- 不碰 `scripts/audit/**`（K3 域）与 V1 v0.2 验收标准（冻结哈希 `c11841e6`）
- 不做 26-7 决策（创始人领地）、不做里程碑窗口（待创始人给窗口）
- 不批量关闭 PR（R1 清单需创始人签字；CTO 已具备 token 可执行）

---

## D. 派单前复核（八项）

| 项 | 证据 |
|---|---|
| ① 任务号真实 | D815 / D816 全 refs 扫描占用 = 0（`git for-each-ref` + 本地分支） |
| ② 前置状态真实 | API 实测：#634 / #635 已开（open）；D803 两条已 merged（#624/#625）；`fix/d809-pending-wiring` 分支已推（`949b06e8`） |
| ③ 技术声称亲自复现 | 线10 5/6：`gen-project-board.py` 实测输出；`10-8 evidence=[]`；取号撞号：`origin/fix/d809-pending-wiring` 与 D810 报告的 D809 撞号；`bypass.log` 互斥：#614 三次 `dirty` 实测 + `merge-tree` 零冲突 |
| ④ 写集路径验证 | 所有既有路径 `[ -e ]` 通过；三个新建文件已显式标「新建」 |
| ⑤ 写集零交集 | D815（audit-reports + evidence + D799/D815 卡）与 D816（control-tower 脚本 + .gitignore + 测试）零交集；D816 与 D812/D813 不同文件 |
| ⑥ 引用行号当场看 | `src/llm/retry-middleware.ts` 的 `pickChatOptions`、`src/agent/conversation-engine.ts` 压缩三处，CTO 均当场 grep 核对 |
| ⑦ 依赖图 | D816 与 D812/D813/D814 同属 scripts 面但不同文件；建议串行（见 §B） |
| ⑧ 交付物齐备 | 每卡：写集 / 红线 / Done（可证伪）/ 前置 + 创始人复制块（§E） |

---

## E. 创始人复制块（可直接转发）

### E1 → 🔍 synova-k3-audit：D815 线10 全链路复核

```
任务 D815｜线10 资本循环全链路独立复核——第一条 100% 线的收官门槛。
派单全文: docs/synova/coordination/派单-批九-20260918.md §A/D815
现状：线10 = 5/6；唯一未过点 10-8「审计员全链路复核（K3 PASS）」evidence 为空。
已合：PR #624（读侧属性契约守卫 + 跑道语义修复）、PR #625（GS-03 双相位 7→9 断言 + 证据落点）。
你要做：独立复核 10-1/10-2/10-3/10-4/10-6 的证据与代码接线（不接受测试冒充生产接线），并复核 10-5 告警→工单→推送是否有生产调用点（未接线如实判 NOT-PASS 并列缺口）。
交付：docs/synova/audit-reports/2026-09-18-K3-line10.md（逐点 ✅/❌ + 复现命令 + 原始输出）+ task-state/D799.json audit 回填；若 PASS 追加 docs/synova/product-lines/evidence/k3-line10-2026-09-18.json（kind: k3，绑定 10-8）。
红线：不改验收点定义 / 不改证据 kind / 不改 V1 v0.2 标准；PASS 必须可复现。
Done：gen-project-board.py 重算后线10 = 6/6，或如实给出未达点与缺口。
开始吧。
```

### E2 → 🛠 编码 session（mac）：D816 控制塔卫生

```
任务 D816｜控制塔卫生两缺陷（都已实测复现）。
派单全文: docs/synova/coordination/派单-批九-20260918.md §A/D816
缺陷1：scripts/control-tower/alloc-task-id.sh 只扫本工作树 → 跨 worktree 撞号（D810 session 首跑得 D809，而 origin/fix/d809-pending-wiring 已占用同号）。修：取号前扫全 refs + 全 worktree，或改为"分配即占位"。
缺陷2：.claude/bypass.log 是 post-commit 运行期产物却入库 → 每个 PR 互相冲突（今日 #614 因它返工 3 次；D708 门禁本就把它当 builtin 豁免）。修：git rm --cached + .gitignore，并确认依赖它的门禁改从工作区读。
写集：scripts/control-tower/alloc-task-id.sh、scripts/control-tower/check-bypass-log.sh（如需）、.gitignore、tests/control-tower/alloc-task-id.test.sh、tests/control-tower/check-bypass-log.test.sh（如需）、task-state/D816.json、brief、memory note。
红线：不改门禁判定口径；不碰 scripts/audit/**；不删历史证据（只停止跟踪）。改 alloc-task-id.sh 前加载 ctrl-tower-change 技能。
Done：两缺陷各有"先红后绿"夹具证据；pre-commit 全过；汇报含命令 + 原始输出。
做完复核；K3 会审计你的产出。
开始吧。
```
