# 派单｜控制塔线：验证基建三件（D861 → D860 → D863，**串行**）

> 出件 2026-09-21 CTO ｜ 执行形态：**最小 agent 小队（编码 1 + 自验 1；队长＝并行 CTO session）** ｜ 域：mac ｜ K3 队外复审（三件均属门禁/治理面）
> **为什么串行**：三件都动 CI/治理面，`ci.yml` 单写者；顺序＝**先恢复验证能力 → 再改口径 → 最后改传导**。

## 〇 编制与工作树
工作树 `.synova-wt-ctrl2` ｜ 分支 `gate/ctrl-verify-batch2` ｜
写集：`.github/workflows/ci.yml`、`scripts/control-tower/check-pr-budget.sh`、`tests/control-tower/check-pr-budget.test.sh`、`tests/project/**`、`task-state/D86{0,1,3}.json`
**硬边界**：不碰 `scripts/audit/**`、不写审计标准；不改 `src/**`（小队 A/B 地盘）

## 〇-b 为什么是这三件（顺序不可颠倒）
| 序 | 卡 | 解决什么 | 架构层 | 做完看得见 | 收获 |
|---|---|---|---|---|---|
| 1 | **D861** | main **红**（Vitest 2/2 + generate + 聚合→计算→页面）→ 一切"CI 绿"不可信 | 治理/CI（跨层验证基建） | main 恢复全绿（run id + job 全绿证据） | **恢复"能不能判断对错"的能力** |
| 2 | **D860** | PR 预算 ≤12 与"每文件须被 brief 覆盖"互斥 → #693(14件)/#657(13件) 双双卡死 | 控制塔/治理层 | 两件可按新口径合规合并 + 反例夹具 | 交付**不再被算术卡死**；消 F9/F12 一族 |
| 3 | **D863** | canary 假覆盖 `exit 1/2` 被 `\|\| true` 吞；绿腿 ⚠ 不可见（CT-78） | 治理/CI | 假覆盖**在 CI 上会红**；绿腿可见 ⚠ | 门禁**真拦**——防假绿链条闭环 |

**D863 前置**：#657 拆出的 **PR-1** 合并（ci.yml 释放）。**若前置未达成 → 停在 D860，报 CTO 排队**。

## 〇-c 权威件回执
依据：K3 审计报告 `docs/synova/audit-reports/2026-09-20-K3-D854.md`（CT-78）｜D850/D852 收口件（F9/F12）｜`docs/synova/coordination/D858-证据包标准件-匿名取CI证据-20260921.md`｜验收标准 §十一/§十二
9 项撤回自检：未命中（不涉 DSH 存储/遥测/界面）｜8 条禁做：未涉 DSH 代码依赖｜L3 限制：禁 grep 型判据当验收

## 〇-d 旧信息漏洞自检
- [x] 引用均在仓库内（若 K3 报告未入库 → 先报 CTO）
- [x] write_set 逐文件声明，与小队 A/B 不重叠

## 〇-e 前提实测（写码前贴完整输出）+ 执行方复核项
1 `git log --oneline -1 origin/main` + `check-runs` → 确认 main 红的具体 job（**禁 tail 截断**，给完整失败断言）
2 `git diff --name-only origin/main...origin/fix/d839-claim-release | wc -l` → #657 文件数（预算冲突实证）
3 `grep -n "check-canary-drift" .github/workflows/ci.yml` → 定位 `|| true` 与 tests/project 循环
**复核项**：① D861 若为外部依赖抖动，必须给证据而非"重跑就好" ② D860 的排除规则**必须带反例夹具**（伪装成治理产物的代码仍计数） ③ D863 不得误伤既有 71 条漂移（保持 exit 0）

## 一 任务分解（逐件）
- **D861**：定位三条失败真实根因（run/job id + 失败断言原文）→ 修复或回退到最近全绿（须证据）→ 复绿（12/12 或对应 job 全绿）
- **D860**：`check-pr-budget.sh` 排除 `.claude/task-briefs/**`、`task-state/**`、`memory/notes/**`；夹具先红后绿（13 件被拦 → 放行；纯代码 13 件仍被拦）
- **D863**：`|| true` → 接收退出码并传导（exit 1/2 → job 红；漂移/幽灵保持 exit 0）；tests/project 循环成功路径回捞 ⚠（`::warning` 或 `$GITHUB_STEP_SUMMARY`）；`git diff --numstat` 仅新增行

## 二 完成判据
- [ ] D861：main 全绿（run/job 证据，禁截断）
- [ ] D860：夹具先红后绿两次原始输出 + 反例夹具通过
- [ ] D863：真实 CI 运行证据（run/job id + **匿名可核命令** + 归档片段 + sha256，按证据包标准件）
- [ ] 三件各自 M6 三件已提交进仓库（给路径）｜归属逐张收口
- [ ] 交付附 `git ls-remote --heads origin | grep gate/ctrl-verify-batch2`

## 三 红线
不碰 `scripts/audit/**`、不写审计标准、禁自我审计；禁 `--no-verify`/`git stash`/force push；**K3 复审通过前不得合并**；PR ≤12 文件单域（D860 落地后按新口径）
