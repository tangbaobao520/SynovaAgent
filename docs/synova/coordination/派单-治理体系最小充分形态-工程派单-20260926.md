# 派单 · 治理体系最小充分形态（工程派单 · 并行 CTO 统筹）

<!-- ═══ 强制表头（D943；每次派单必须原样保留，缺一即 K3 判 FAIL）═══
断面: 0.1.7-rc.1 @ 46a7f68b        ← 唯一源 docs/synova/coordination/DSH-断面.json（禁手写猜）
单一事实源: synova-research@main:99-综合/综合报告/20260925-清单-执行session入口-治理体系最小充分形态.md
本单不得引用的已作废口径: ① DSH 断面 = 0.1.7-alpha.2 @ 00102833（已作废）② "会话格式三次"（实为 v0→v4 四次）③ ".synova-wt-* = 149 个"（已作废：改用 `git worktree list` 口径并注明日期）
检查命令: python3 scripts/control-tower/check-dsh-anchor.py --repo .   # 必须 OK
═══════════════════════════════════════════════════════════════════ -->

> 出件：Mac-CTO（本单为**交接件**，非执行件）｜日期：2026-09-26
> 受件：**并行 CTO（统筹）** ｜ 抄送：K3（审计）／创始人（决策）
> 路由：**第二类**（控制塔/治理/缺陷修复 —— 根因清楚、改动局限、验收物理可复现）→ 派执行方，无 spec
> 创始人指令（2026-09-26）：**"回归 48 格主线之前，必须彻底修复门禁与遗留；这一系列交并行 CTO 统筹；CTO 继续肩负主线使命，不带头做，以免污染其上下文与注意力。"**

---

## 〇、机制在场性三问（D943 必答）

1. **消费者是谁？** 并行 CTO（执行统筹）／K3（审计）／Mac-CTO（收件闸 + K3 派单 + 门禁语义批准）／创始人（决策与升级接收）。
2. **它在哪一层/哪个文件？** 本件 `SynovaAgent:docs/synova/coordination/派单-治理体系最小充分形态-工程派单-20260926.md`；**被改造对象**在 `/Users/wane/SynovaAgent`；**蓝图与证据**在 `/Users/wane/山河研究院`（`synova-research` 仓，**只读**）。
3. **用哪条命令证明它在跑？**
   ```bash
   python3 scripts/control-tower/check-dsh-anchor.py --repo .        # 期望末行 DSH-ANCHOR: OK，RC=0
   bash "/Users/wane/山河研究院/04-技术研究/取证/校验和台账.sh"      # 期望 9 件齐、缺失 0
   ```

---

## 一、唯一蓝图（**以研究院 v1.3 为准；CTO 自制口径一律作废**）

**根目录 `$R = /Users/wane/山河研究院/`（独立仓 `synova-research`，read-only）**

| # | 文件 | 行数 | 作用 |
|---|---|---|---|
| **入口** | `$R/99-综合/综合报告/20260925-清单-执行session入口-治理体系最小充分形态.md` | 164 | **只读这一份即可定位全部材料** |
| **权威正文** | `$R/99-综合/综合报告/20260925-治理体系最小充分形态-最终方案.md` | **876** | Q1–Q7 全答 / C1–C16 判据 / 七步迁移 / 命令附录 |
| 复核（自验） | `$R/99-综合/综合报告/20260925-复核报告-治理体系最小充分形态.md` | 173 | **打穿 3 处硬伤 + 5 项待 K3 抽验；先读它省时间** |
| 综合 | `$R/99-综合/综合报告/20260925-综合-治理体系最小充分形态.md` | 109 | 矛盾与缺口 / 议程 |
| 指针件 | `$R/99-综合/综合报告/20260925-指针-待回main-研究指针-治理最小形态.md` | 71 | **D4 执行它** |
| 证据·DSH | `$R/04-技术研究/取证/取证-DSH门禁结构性事实-20260925.md` | 1,513 | DSH 对照基准 |
| 证据·底账 | `$R/04-技术研究/取证/取证-SynovaAgent治理机器底账-20260925.md` | 1,303 | 门禁五口径 / 阻断面 / 写者榜 |
| 证据·8 例 | `$R/04-技术研究/取证/取证-失败案例8例原始证据-20260925.md` | 755 | 8 例 `file:line` + 复现命令 |
| 工具 | `$R/04-技术研究/取证/校验和台账.sh` | 45 | 现场生成 9 件哈希（**第一条命令就跑它**） |

**⚠️ 口径冲突处置**：CTO 于 2026-09-26 自产的 派单-FIX-011-共享治理文件机制-20260926.md（**待合 PR #809**）（"显式多卡共享声明"方案）**与蓝图 §三 的三分法冲突 ⇒ 该件作废**，其 D979 卡**降级为登记**，机制以蓝图为准（写者≥2 可推导 ⇒ 生成物离库；不可推导 ⇒ 一卡一文件）。

---

## 二、冻结口径（**别再数一遍；全部来自研究院 v1.3 实测**）

| 指标 | 冻结值 | 备注 |
|---|---|---|
| `SynovaAgent origin/main` | **`ef299746`** | 若已前进 ⇒ 本单命令须在新断面重跑 |
| 取证工作树 | `chore/cto-doc-slim-phase1-writeset-exempt` @ **`b8573c7a`** | 含未解 `UU src/deploy/bootstrap.ts` |
| DSH 断面 | **`0.1.7-rc.1 @ 46a7f68b`** | 唯一源 `DSH-断面.json`；该 checkout 为 **shallow clone** |
| `scripts/` 下 `check-*` | **48（当前分支）/ 49（origin/main）/ 47（main）**；五种数法 **47/48/49/54/71** | **"51"不可复现** ⇒ 必须先声明数法 |
| `pre-commit-check.sh` | **1,603 行**；门禁标签 **41**；外部调用点 **29** | |
| 门禁命中 | 36 标签中 **17 个 hit=0**（窗口 **17 天 / 237 次运行 / 4,873 行**） | ⚠️ 有强样本偏差；**"空转 vs 没跑到"未裁决** |
| 真阻断面 | **`scripts/control-tower/synova-commit`（890 行）**；`.git/hooks/pre-commit` 末尾 **`exit 0`** | **两个提交入口并存** |
| 在途积压 | **149 个工作树含未合并提交 / 合计 1,415 提交**（top = `tmp-d942` 110） | ⚠️ **判据只认 C16（单调下降）；工作树数会漂移，不作判据** |
| `task-state/D*.json` | **364** | 367 张卡仅 **30 张（8.2%）** 由 `alloc-task-id.sh` 登记 |
| 最热共享可变文件 | **`.github/workflows/ci.yml`（10 张卡声明）**；`merge_writeset_gate.py`（7 张） | |
| 元判据（四数字） | 跨文件契约项数 ↓｜`human_write_set_lines = 0`｜豁免数 ↓｜**`src+tests : scripts+docs` ↑**（现状约 **1:326**） | |

---

## 三、六阶段（每阶段：目标 / 动作 / 写集 / 判据 / 不进则停）

> **通则**：① 每阶段**独立分支 + 独立 PR**（R1 产出者自开）；② **不达标不进下一阶段**；③ 每阶段末交 CTO 收件闸 + K3 抽验；④ **阶段 0–4 期间冻结新增门禁**。

### 阶段 0 · 冻结与止血（零代码风险，最先做）
| 动作 | 写集 | 判据 |
|---|---|---|
| **冻结**：不新增任何门禁（延至阶段 4 结束） | — | 期间新增门禁数 = **0** |
| **重装 git hook**（解自锁：旧 hook 把软失败写进 `bypass.log` ⇒ 当日自阻断） | `scripts/install-hooks.sh`（执行，不修改） | hook 内 `bypass.log` 字样消失；改为 `gate-soft-warnings.log`；**hook 与生成器同哈希** |
| **归档 3 个未入库日志** | CI artifact 配置 | `gate-hits.log`／`degraded-events.log`／`pre-commit-failures.log` **可在异机复跑**（≥90 天保留） |
| **1,415 个未合并提交分类**（在途/废弃/重复） | 积压分类件（**待建**）（**待建**） | **分类完成前不删任何分支/工作树/提交**；产出分类表 + **C16 起始值与下降曲线** |

### 阶段 1 · 证据闭合（决定后续能否定稿）
| 动作 | 判据 |
|---|---|
| 取 **17 个 hit=0 门禁的 CI 侧失败率** | 裁决"**空转 vs 没跑到**"（蓝图矛盾 1 的唯一证据） |
| **K3 最小输入集可复跑** | 蓝图 §五 Q5 三条输入在异机可复现 |
| 定版 **`check-*` 口径**（一条命令） | 全队引用的数字**只有一个来源** |

### 阶段 2 · 交付路径唯一化（解"改门禁要过门禁"）
| 动作 | 判据 |
|---|---|
| **双提交入口合一**（hook `exit 0` vs `synova-commit` 890 行） | **只留一个阻断面**；另一个删除或降为薄壳 |
| **一卡一工作树 + 合并队列**；**主树设只读** | 主树零写入；`merge-base` 稳定 ⇒ 写集可机器生成 |
| 新 clone 自证 | `install-hooks.sh` 后 hook 与生成器同哈希 |

### 阶段 3 · 四类争议的**结构性消除**（核心）
| 争议 | 现状 | 目标形态 | 判据 |
|---|---|---|---|
| **夹带** | 人写 `write_set` | 写集 = `merge-base..HEAD`（git 生成）＋交付关闭时 CI 生成 `evidence/<D>/changes.json` | **新卡不需要写 write_set**；C14：`write_set` 类型契约 **fail-loud**（dict 不许静默归零） |
| **劫持** | 文件→历史卡反查 | **行级 blame 加权**（DSH 同款；**不引入 CODEOWNERS**） | 共享文件被合法修改**不再误判**；**真劫持仍拦** |
| **撞号** | `D#` 承担唯一性 + 手工水位 | **branch/worktree 名为主键**；`D#` 降级为人可读标签；水位改**生成物** | **`D#` 不出现在任何判定逻辑**；C15 |
| **假冲突** | union 声明（GitHub 不执行） | 运行时账本**出库**；**降级必须 fail-closed** | 新分支不再因账本判 dirty；**降级 ≠ 通过**（黄，且连续 N 天升级红） |

### 阶段 4 · 给门禁以**形状**
| 动作 | 判据 |
|---|---|
| 判据**声明成数据**（`needs`/`after`/`allowFailure`/`quick`）＋启动即**依赖图校验 + 环检测** | 参照 DSH DSH@46a7f68b 的 `run-gates.ts`（1,660 行） |
| **必需检查只暴露 1 个稳定名字**（聚合；**`skipped` 判失败**） | 参照 DSH `all-checks-passed` |
| **对门禁配置自身写测试** | 参照 DSH DSH@46a7f68b 的 `ci-workflow.spec.ts`（1,223 行） |
| **`check-*` 收敛** | 用阶段 1 定版的**那一条命令**核；目标由蓝图给（≈3–5 真保护 + 结构消除后余量） |

### 阶段 5 · 遗留收口（**"之前一直在做的门禁/控制塔"一次性清干净**）
| 来源 | 内容 | 判据 |
|---|---|---|
| **问题修复台 FIX-001…016** | 全部条目（唯一台账：**问题修复台**，**待合 PR #805**；落地前以 PR #805 分支为源）） | 每条的"闭环判据"达成 + 原始输出 |
| **D962 2b** | `|| true` 清零、`iron-laws` 硬阻断接线、2a② 续卡 | iron-laws 真跑；断言指向真实活点 |
| **D964 FIX** | 三漏洞（豁免夹带 P0／README 误拦 P1／取代声明绕过 P1） | 三处"滥用场景必须仍红"反例 |
| **D969–D979 批 + CTO 今日新增** | #806（D975 三处根治）／#807（D977 R1–R4）／#809（D979）／#803／#804 | 逐条收口或**显式关闭**（不得留着挂） |
| **红线升级 3 条** | 取号器挂死 + 占用表≠拒绝面｜Gatekeeper FP 熔断｜测试副作用碰 tracked | 各带复现 + 判据；**完成后升级创始人知情** |

### 阶段 6 · 不倒退判据（C1–C16）与回归主线
| 动作 | 判据 |
|---|---|
| **C1–C16 全机器可核**（命令见蓝图 §九） | 全部 ✅ 或**显式登记例外及理由** |
| 元判据四数字逐阶段记录 | 每个数字**方向正确**（含 `src+tests : scripts+docs` ↑） |
| **连续两周**：治理类提交 < 主线类提交 | `git log` 可算 |
| **回归 48 格主线** | 以上全达标后，Mac-CTO 回归主线（哨兵体系 + 48 格） |

---

## 四、角色与边界（**谁做什么、谁不做什么**）

| 角色 | 职责 | **明确不做** |
|---|---|---|
| **并行 CTO** | **统筹执行**六阶段；分组队；开 PR（R1）；维护问题修复台状态（经 CTO 确认）；例外上报 | 不改 `scripts/audit/**`、`docs/synova/audit-reports/**`（K3 域）；不自我审计 |
| **Mac-CTO（我）** | **只保留三件**：⒜ **收件闸**（验收，**不代开 PR、不代执行**）⒝ **K3 派单** ⒞ **门禁/CI 语义变更的批准** ＋ **主线唯一责任人**（哨兵体系 + 48 格） | **除主线件外不创建分支/PR**（今日净增 3 条的教训） |
| **K3** | 抽验（蓝图 §8 五项；每阶段末）｜C 判据机器核 | — |
| **创始人** | 决策与升级接收；**两条判据升格入宪章**（蓝图 §八：单调/非单调分类学；治理减负四数字） | — |

**⚠️ 唯一硬点**：**门禁/CI 语义变更必须过 Mac-CTO 批准**（R3），否则视为未授权变更。

---

## 五、R1–R4（本批沿用）与报告格式

- **R1** 产出者自开 PR（并行 CTO 及小队各自开）
- **R2** PR 模板六项：① 卡号+分支+sha ② 写集声明 ③ 逐条判据 ④ **改坏即红**原始输出 ⑤ 红线段 ⑥ 未清项登记
- **R3** Mac-CTO 三件（见上）
- **R4** 队列合并由值守/并行 CTO 推进，不经 Mac-CTO

**报告格式**（每阶段末）：
```
阶段 N ｜ 动作清单（逐条：状态 + 原始输出）｜ 判据逐条 ｜ 未清项 ｜ 需 CTO 裁决 ｜ 需创始人裁决
禁止写"通过/全绿"而无原始输出。
```

---

## 六、红线（违反即退回）

1. **冻结期不新增门禁**（阶段 0–4）；**不得以放宽门禁换通过**（禁删检查、禁 blanket 豁免）
2. 不碰 `scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`（除各阶段明确授权）
3. **不得以改断言/删断言/`|| true` 把红变绿**；"改指活点"必须给**活点三件套**（job 名+行号／无它则红反例／有它则绿 run）
4. **分类完成前不删任何分支/工作树/提交**（1,415 积压含真实交付）
5. 禁用 `git stash`／`--no-verify`／force push（共享分支）
6. 推送用 https + `http.extraheader`，**推后必须认证 `ls-remote` 交叉验证**（统一 12 位）；443 超时重试 1–3 次
7. **绝不在他人活跃工作区做 git 写操作**：进目录前核 `pwd` + `git branch --show-current`；`worktree add` 必须**验成再 cd**；`reset --hard` 前**必须先 `git reflog`**
8. 双平台各验；夹具判据**方言无关**（含金丝雀）；禁裸 `python3`（用 PYBIN）

---

## 七、例外上报机制

并行 CTO 遇到以下情形**立即停并上报 Mac-CTO**（不得自行决定）：
1. 需要**新增门禁**或**修改门禁语义**（⇒ CTO 批准 + K3 审）
2. 需要**删除分支/工作树/提交**（⇒ 先出示分类证据）
3. 需要**改 `scripts/audit/**`、`docs/synova/audit-reports/**`**（⇒ K3 域，禁）
4. 蓝图判据与实际**冲突**（⇒ 以蓝图为准并登记冲突）
5. **同类第 2 次及以上**复发（⇒ 按红线升级创始人）

---

## 八、风险与对冲

| 风险 | 对冲 |
|---|---|
| 并行 CTO 成为新单点 | 每阶段 **K3 抽验** + C 判据**机器核**（不靠人看） |
| 两套口径打架 | **唯一蓝图 = 研究院 v1.3**；CTO 自制件（D979）作废 |
| Mac-CTO "看不见" | **唯一台账 = 问题修复台（PR #805）**；每阶段 CTO 读一次 + 一页周报给创始人 |
| Mac-CTO 忍不住下场 | **禁令**：除主线件外不建分支/PR；只做收件闸/K3/门禁批准 |
| 快而返工 | **时间不是约束，判据才是**；**不达标不进下一阶段** |

---

## 九、完成定义

**全部满足才算完成**：
1. **阶段 0–6 全部达标**（每阶段判据逐条有原始输出）
2. **C1–C16 全绿**或例外已登记并被创始人裁决
3. 元判据四数字方向正确
4. **连续两周治理类提交 < 主线类提交**
5. **K3 每阶段抽验通过**
6. 遗留收口清单**无悬挂项**（全部"已闭环"或"显式关闭并登记"）

**⇒ 达标后 Mac-CTO 回归 48 格主线。**

---

## 十、附：权威命令清单（可复跑）

```bash
# 断面与蓝图校验
python3 scripts/control-tower/check-dsh-anchor.py --repo .              # 期望 OK, RC=0
bash "/Users/wane/山河研究院/04-技术研究/取证/校验和台账.sh"            # 期望 9 件齐、缺失 0
bash "/Users/wane/山河研究院/04-技术研究/取证/校验和台账.sh" && sed -n '1,140p' \
  "/Users/wane/山河研究院/99-综合/综合报告/20260925-治理体系最小充分形态-最终方案.md"   # 结论先行

# 关键基线（复核时重跑，别抄本单数字）
wc -l scripts/pre-commit-check.sh                                        # → 1603
find ./scripts -path './.synova-wt-*' -prune -o -name 'check-*' -type f -print | wc -l   # → 48
git worktree list | wc -l                                                # 工作树数（会漂移，仅趋势）
git rev-list --count origin/main..origin/<branch>                        # 单分支未合并提交数
# 积压总量（C16）
for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin | sed 's#^origin/##'); do
  [ "$b" = "HEAD" ] && continue; [ "$b" = "main" ] && continue
  c=$(git rev-list --count origin/main..origin/$b 2>/dev/null || echo 0); echo "$c $b"
done | awk '{s+=$1; if($1>0)n++} END {print "含未合并提交的分支:",n," 未合并提交总数:",s}'
```

**修订记录**
- 2026-09-26 v1.0 首版（Mac-CTO）。触发：创始人指令"回归主线前彻底修复门禁与遗留；交并行 CTO 统筹；CTO 不带头做以免污染上下文"。
