# 派单：第三批（D727–D731）— 2026-09-13

> 派单: 主 CTO（synova-cto） | 路由: 第一类（D727 spec 先行）→ 第二类（D728/D729/D730 直派）→ 第三类（D731 审计）
> 取号: 全部经 `alloc-task-id.sh` 实分配（D727/D728/D729/D730/D731），**禁凭计划号臆写**（D384 取号纪律，本批前已有一次臆写事故）
> 依据: 第三批所有前序任务已回报（D715 审计 / D716 spec+impl / D717 / D718 / D725 spec / D726）+ 我方亲历发现

---

## 〇、为什么是这五件（CTO 判断，不是按谁吵得响排）

前一批把「看得见的问题」清掉后，暴露出**一个产品级空洞和两个体系级缺陷**：

1. **证据层是空心的**（D725 spec 实测）：`evidence` 表在生产库**不存在**、`EvidenceStore` 零生产实例化、`expires_at` 零写入方 → D717 的清理作业跑在自建空表上。
   **这不是配置问题**：我们的产品卖点是「报告可溯源」，而**证据根本没落库**。D-1 状态驱动通知模型（创始人亲裁）也建立在「证据状态」之上 → **这是当前最高优先级的实现项**。
2. **一个 brief 声明，四道门禁四套解析口径**（我亲历 + D718 独立复现同一现象）：G12 认一行一文件、D708 不拆「、」、CI 棘轮要 merge-base 三点差、staging-guard 认领为空时回退猜 brief → 全队列被格式问题堵死。
3. **门禁只覆盖「提交方」，不覆盖「后来落进来的人」**（D716 impl session 亲历 + D726 session 亲历）：D537 拦住了提交者，却没拦住另一个 session 的提交落到别人分支上（M8 变体）。

**排序原则**：产品空洞（证据链）> 体系缺陷（门禁口径/隔离）> 小修（死码/死键）> 审计。

---

## 一、D727｜📋 dev-doc：证据层数据流 spec（P0）

**为什么现在**：先有契约再动数据模型（铁律 0-2）。D725 只确权了「证据怎么删」，没定义「证据怎么来、怎么被引用、怎么溯源」。

**交付**：
- `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D727-evidence-chain-20260913.md`
  1. **采集→落库契约**：谁写 `evidence`、写什么字段、`expires_at` 由谁负责、缺失时降级语义
  2. **溯源链**：一条诊断结论如何回溯到原始证据（`provenance` 语义，与 D725 五层阶梯的「被引用过才算完成」对齐）
  3. **与 D725 的边界**：D725 管「能不能删」，本 spec 管「怎么来/怎么留痕」；两者接口点写清
  4. **既有消费方盘点**：`EvidenceStore`、`evidence-writer.py`、product-lines 状态机各读什么
- `docs/synova/coordination/编码指令-D727-证据链-Win-20260913.md`（若实施归 Win，则出指令；归 Mac 则写明）

**必答**：① 生产库表不存在的根因是「没人建表」还是「建了没调用」？② 证据写入的最小闭环是什么（谁触发）？③ `expires_at` 该由谁写、写失败怎么办（fail-closed 还是 fail-safe）？④ 验收怎么物理证明「一条报告能溯源到证据」？

**写集**：`docs/plans/codex/implementation/`、`docs/synova/coordination/`、`task-state/D727.json`
**红线**：不写实现；不改 `scripts/audit/`；不改 `.github/CODEOWNERS`

---

## 二、D728｜🛠 Mac 编码：证据层接线实现（P0，依赖 D727）

**交付**：按 D727 spec 落地——让 `evidence` 表真实存在 + 采集链路真实写入 + 溯源可查。

**前置（缺一不开工）**：
1. `git fetch --all && git pull --ff-only`
2. **D727 spec 已入 main**
3. 确认 `#527`（D717 真债）已合 —— 本写集与它共享 `src/l3/evidence-retention.ts` / `src/deploy/bootstrap.ts`
4. **与 D725 实施（Win 线）串行**：两者都碰 `bootstrap.ts` 装配点 → 谁先合由 CTO 裁决，不得并行

**写集**：`src/evidence/**`（含新建 `src/evidence/**`）、`src/l3/evidence-retention.ts`（**该文件由 #527 引入，未合前不存在——前置须核**）、`src/deploy/bootstrap.ts`（仅装配点）、`tests/evidence/**`（新建）
**验收**：① 生产库 `evidence` 表存在且有真实写入（贴 sqlite3 查询）② 一条诊断报告能溯源到 ≥1 条原始证据（贴复现命令）③ 零静默降级 ④ 反向验证必红

---

## 三、D729｜🛠 Mac 编码：死码 / 死键清理小批（P1，可与 D728 并行）

**为什么**：K3 D715 **P1** + D726 实测上报，全是「看着在用、实际是死的」——正是我们最该防的假象。

| # | 项 | 证据 | 处置 |
|---|---|---|---|
| ① | `build-synova.cjs:141` **重复 `beforePack` 死键**（后者生效，前者永不被调用） | K3 D725 P1；oxlint 实测可抓（warning 级未拦截） | 删死键；顺带看能否升为 error 级 |
| ② | `upsertFromHONA` **生产死路径**（`SqliteGraphStore` 无 `createNodes`；`diagnosis-launcher.ts:235` 注明 HONA 哨兵已删除） | D726 探针实测恒降级 | 先 grep 确认无其他数据源 → **无则按铁律 37 删死路径 + 留防复活断言；有则补 `createNodes` 真实现**（禁静默降级） |
| ③ | `$VAR` 紧跟非 ASCII 未加花括号 → 真失败时被 `unbound variable` 盖掉真实原因（全仓 45 处，**15 处在他人 tests/**） | D726 全仓扫描 | 改 `${VAR}`；**只改本批写集内文件**（他人 tests/ 由 CTO 统一派，避免撞车） |

**写集**：`build-synova.cjs`、`src/l4/graph-bridge.ts`（**铁律 46 白名单桥接文件——K3 需重点关注**）、`src/agent/post-diagnosis-processor.ts`、`scripts/product-lines/**`、`tests/**`（仅本批涉及文件）
**红线**：不碰 `scripts/audit/`；不与 D728 改同一文件

---

## 四、D730｜🧭 并行 CTO：声明单一事实源 + 门禁覆盖后来者（P0，CTO 域）

**为什么**：本批 CTO 亲历——**同一个 brief 声明被四道门禁用四套口径读**，导致钥匙 PR 被轮流挡；D718 独立复现同一现象（「G12 与 merge_writeset_gate 对同一份声明用不同匹配器」）。两个 session 从两个方向撞同一堵墙 = 体系病。

**交付（每项独立 PR，先复现再修，反向验证必红）**：
1. **声明解析单一事实源**：`brief_parser` 成为唯一解析器，G12 / D708 gate / staging-guard / resolver 一律调用它；**路径规范化规则统一**（反引号、`、`分隔、glob、行号后缀）——规范化放在解析层，不再散落各处
2. **D537 覆盖「后来者」**：主树多 session 场景下，不仅要拦提交方，还要拦住「他人提交落到我分支」（D716 impl / D726 两例）
3. **`bypass.log` 退出 PR 合并树**：改 per-session 文件或移出合并路径，消除「一个日志文件决定所有 PR 能否合并」
4. **D718 漏交的第 4 项**：证据 `.log` 被 `*.log` 静默忽略 → 白名单 + 写入后 `git check-ignore` 自检（K3 P1-4 的复发结构）

**写集**：`scripts/control-tower/`、`scripts/workflow/`、`tests/control-tower/`、`.gitignore`
**要求**：加载 `ctrl-tower-change` + `windows-compat`；每项反向验证必红

---

## 五、D731｜🔍 K3 审计：第三批审计（P0，待前序合并后转）

**对象**：
- D717 真债两项（**若 #527 未合则从分支审**）+ D725 spec（#535）
- D716 spec1 实现（#534，Mac 侧）
- D726（#533）+ 本批 D727/D728/D729/D730
- **补审**：D713 构建 ABI 后本地 vitest 全红、`#512 历史夹带 D711 文件 ~500 行`（K3 上批自报，需正式条目）

**1-8 复验**：已按 D714 后新产物闭合（K3 上批完成）→ 本轮不再重复

**红线**：结论只认 K3；不改 `scripts/audit/`；不替创始人下业务结论

---

## 六、跨线（Win 侧，创始人已转）

| 任务 | 状态 |
|---|---|
| D725 数据生命周期五层阶梯**实施** | 已转 Win（含前置：等 #527 合并后重核行号） |
| D716 spec2 → **1-5 双引导收敛实施**（410 承接 + 删旧引导） | spec 已在 main，待 Win 实施 |
| Win 侧端到端收尾（1-2 / 1-6 安装段受提权阻塞） | 待人工桌面会话 |

---

## 七、依赖与串行规则（必须遵守，违者返工）

```
D727 spec ──→ D728 证据链接线实现 ──┐
                                    ├──→ D731 K3 审计
D729 死码/死键清理（并行）──────────┤
D730 门禁单一事实源（并行，CTO 域）─┘
D725 实施（Win）─╳─ D728 共享 bootstrap.ts → 串行，不得并行
```

**写集零交集检查**：D727=docs｜D728=src/evidence+bootstrap 装配点｜D729=build 脚本+upsertFromHONA+product-lines｜D730=scripts/control-tower+workflow+gitignore｜D731=audit-reports
**唯一冲突面**：D728 ↔ D725 实施（`src/deploy/bootstrap.ts`）→ 已定为串行。

---

## 八、创始人复制块

**① 转 dev-doc session（📋）**
```
【D727】证据层数据流 spec（P0）
> 完整派单: docs/synova/coordination/派单-第三批-D727-D731-20260913.md §一
> 背景: D725 spec 实测——evidence 表在生产库根本不存在 + EvidenceStore 零实例化 + expires_at 零写入方 → 我们的「报告可溯源」目前无物可溯
> 必答: ①表不存在的根因 ②证据写入最小闭环（谁触发）③expires_at 谁写/失败怎么办 ④怎么物理证明「一条报告能溯源到证据」
> 要求: 过 dev-doc-gatekeeper；写集声明；Done 可证伪
```

**② 转 Mac 编码 session（🛠，两单可并行）**
```
【D728】证据层接线实现（P0）
> 完整派单: §二 ｜ 前置: D727 spec 入 main + #527 已合 + 与 D725 实施串行（共享 bootstrap.ts）
> 交付: evidence 表真实存在 + 采集链路真实写入 + 溯源可查
> 验收: 贴 sqlite3 查询证明表存在且有写入；贴命令证明一条报告能溯源到 ≥1 条原始证据

【D729】死码/死键清理小批（P1）
> 完整派单: §三
> ① build-synova.cjs:141 重复 beforePack 死键（删）② upsertFromHONA 生产死路径（判无数据源则删+防复活断言；有则补 createNodes 真实现）③ $VAR 紧跟非 ASCII 未加花括号（仅本批写集内）
> 红线: 只改本批写集内文件；不改 scripts/audit/
```

**③ 转并行 CTO session（🧭）**
```
【D730】声明单一事实源 + 门禁覆盖后来者（P0）
> 完整派单: §四
> ① brief 声明解析收敛为单一事实源（G12/D708/staging-guard/resolver 共用；规范化规则统一）② D537 覆盖「他人提交落到我分支」③ bypass.log 退出 PR 合并树 ④ 证据 .log 白名单 + check-ignore 自检
> 要求: 每项独立 PR；先复现再修；反向验证必红；加载 ctrl-tower-change + windows-compat
```

**④ 转 K3 审计 session（🔍）**
```
【D731】第三批审计（P0）
> 完整派单: §五
> 对象: D717（若未合从分支审）/ D725 spec(#535) / D716 spec1 实现(#534) / D726(#533) / D727 / D728 / D729 / D730
> 补审: D713 构建 ABI 后本地 vitest 全红；#512 历史夹带 D711 文件 ~500 行（需要正式条目）
> 红线: 结论只认 K3；不改 scripts/audit/
```

---

## 八补、派单前复核证据（CTO 亲自复现，2026-09-13 补记）

> 依据新流程 `pre-dispatch-check`（八项）。创始人质询「派单前有没有复核」→ 当场复核，**抓出本派单自身两处错误**。

| 本派单的声称 | CTO 亲自复核（命令 + 输出） | 结论 |
|---|---|---|
| `build-synova.cjs` 有重复 `beforePack` 键（:141 与 :207） | `grep -cE "^\s*beforePack\s*:" build-synova.cjs` → **2**；行号确为 141 / 207 | ✅ 属实 |
| `evidence` 表在生产库不存在 | `sqlite3 data/synova.db ".tables" | grep -ci evidence` → **0** | ✅ 属实 |
| `diagnosis-launcher.ts:235` 注明 HONA 哨兵已删除 | `grep -n HONA src/agent/diagnosis-launcher.ts` → `235: // HONA — V4.2.4: hona 哨兵已删除 — 跳过` | ✅ 逐字属实 |
| `EvidenceStore` 零生产实例化 | `grep -rn "new EvidenceStore" src/ --include=*.ts | grep -v test` → 空 | ✅ 属实 |
| **D728 写集 `src/evidence/**`** | `ls -d src/evidence` → **不存在**；实际代码在 **`src/evidence/`**（`evidence-store.ts`） | ❌ **CTO 写错 → 已在本派单全文修正为 `src/evidence/**`** |
| D729 写集「`src/l4/**`（仅 upsertFromHONA 相关）」 | `grep -rln upsertFromHONA src/` → `src/l4/graph-bridge.ts`（**铁律 46 白名单桥接文件**）、`src/agent/post-diagnosis-processor.ts` | ⚠️ 写集过宽且触及白名单文件 → **收窄为这两个文件**，并注明触及桥接文件需 K3 关注 |
| `SqliteGraphStore` 无 `createNodes`（D726 声称） | `grep -rn SqliteGraphStore src/l4/*.ts` → **未定位到该类** | ⏳ **未独立确认** → 派单改为「执行方先复现再判定」，不写成结论 |

**流程教训（已固定为 skill）**：派单里每一句技术结论都必须 CTO 亲自复现；写集路径必须当场 `ls` 验证。

## 九、日志（CME——本批安排过程中需要留痕的事）

1. **#527 的 stacked 问题已由执行方自行拆分**：D725 spec 从 #527 拆为独立 PR #535（比 CTO 原先"一起合"的裁定更干净）→ 采纳，CTO 同步撤回 #527 正文的并集声明。
2. **本批取号**：D727–D731 全部经 `alloc-task-id.sh` 实分配。教训留痕：上一份派单我曾凭计划号写错（D727 vs 实际 D725），本批起**派单文档写 D# 前必须核 `task-state/`**。

---

## 十、派单前复核（本次实跑 `pre-dispatch-check.sh`，2026-09-13 v2）

> 依据创始人指令：**每次派单前必须复核**。本节留原始输出与修正项，口说无凭。

### 机械复核发现（脚本原文摘录）

| 复核项 | 脚本输出 | 处置 |
|---|---|---|
| 任务号真实性 | `✅ D727/D728/D729/D730/D731 已登记`；`⚠️ D725/D726 无 task-state` | D725/D726 的 task-state **在 main 上有**（本 worktree 落后）→ 脚本误报；**已记入脚本改进项：应同时核 `origin/main`** |
| 写集路径 | `⚠️ 不存在: docs/plans/.../SYNOVA-IMPL-DSH-D727-evidence-chain-20260913.md` | 该 spec **由 D727 新建** → 文档已显式标注「新建」 |
| 写集路径 | `⚠️ 不存在: src/l3/evidence-retention.ts` | 该文件由 **#527** 引入，未合前不存在 → 已写入 D728 前置「须核」 |
| 写集路径 | `⚠️ 不存在: src/l4/evidence` / `src/l4/evidence/**` | **CTO 写错路径**（真实代码在 `src/evidence/`）→ v2 全文修正，本项为上一版遗留 |

### 本轮修正项（v1 → v2）

1. **错路径全文修正**：`src/l4/evidence/**` → `src/evidence/**`（v1 只改了 2 处，机械复核抓出其余残留）
2. **D729 写集收窄**：`src/l4/**` → `src/l4/graph-bridge.ts` + `src/agent/post-diagnosis-processor.ts`（前者是**铁律 46 白名单桥接文件**，原写集过宽会误伤）
3. **新建文件显式标注**：D727 spec 路径、D728 的 `src/evidence/**` 与 `tests/evidence/**`
4. **依赖前置写明**：D728 依赖 `src/l3/evidence-retention.ts` ← 来自 #527

### 复核后的确认项（亲自复现，非转述）

| 声称 | 命令 | 结论 |
|---|---|---|
| `build-synova.cjs` 重复 `beforePack` | `grep -cE '^\s*beforePack\s*:'` → 2（:141/:207） | ✅ |
| `evidence` 表在生产库不存在 | `sqlite3 .tables \| grep -ci evidence` → 0 | ✅ |
| `diagnosis-launcher.ts:235` HONA 已删 | 逐字命中 | ✅ |
| `EvidenceStore` 零生产实例化 | `grep -rn "new EvidenceStore" src/` → 空 | ✅ |
| `SqliteGraphStore` 无 `createNodes` | `grep -rn SqliteGraphStore src/l4/*.ts` → 未定位 | ⏳ **未确认** → 派单改为「执行方先复现」 |

### 脚本自身改进项（转 D730）

- ① 任务号核验应同时查 `origin/main`（避免 worktree 落后导致误报）
- ② 本脚本尚未入 CI 密封清单（否则属「机制建成未接线」）

---

## 十一、CTO 裁决：D728 归属改判 + 三项问题处置（2026-09-13 补记，创始人质询后当场核）

> 触发：D727 spec 交付时上报「D728 写集缺口 6 文件，请 CTO 裁决」。CTO **先核归属再裁决**（依 pre-dispatch-check 第 ③⑤ 项）。

### A. 复核发现的派单错误：D728 派错了线（严重）

实测 `docs/synova/coordination/TASK-ROUTING.md` §域划分 + `.github/CODEOWNERS`：

| D728 需要的文件 | 域归属（实测） |
|---|---|
| `src/evidence/**` | **Win Claude**（路由表 §38：`src/` 除 sentinel/cron/mcp 外 = Win） |
| `src/routes/diagnosis.ts`、`src/routes/conversations.ts` | **Win Claude**（同上） |
| `src/agent/conversation-engine.ts`、`diagnosis-launcher.ts`、`engine-context.ts` | **Win Claude**（`src/` 非 sentinel/cron/mcp；`src/agent/sentinel-service*` 才是 Mac） |
| `src/server.ts` | **Claude 专属**（CODEOWNERS:13 + 路由表 §62「DSH 不碰」） |

**结论：证据链实现的写集 100% 落在 Win/Claude 域。我把它派给「Mac 编码」是错的**——违反域划分宪章（B1）。
`pre-dispatch-check` 的机械项**抓不到**这类错（它只核路径存在性，不核域归属）→ **已列为 D730 新增改进项：写集 × CODEOWNERS/路由域交叉校验**（否则同类错误必复发）。

### B. 裁决一：D728 写集缺口 → 采纳**方案 A（一次扩写集）**，但执行方改为 **Win Claude**

- **理由**：方案 B（拆两 PR）会产生「**表存在、零写入**」的中间态——那正是本任务要根治的形态；且跨线拆分会在 Win 域留下无人接管的注入点（`evidenceCollector: null`）。
- **执行方**：Win Claude（写集全在其域）。Mac 线本批**只出 spec，不改代码**。
- **跨线要求**：出「跨线同步」一页（写入 `docs/synova/coordination/`）：① Mac 线只读不改 ② Win 线改完后通知 Mac 线核 `src/sentinel/` 无回归 ③ `src/server.ts` 改动仅限注入点（≤3 行）。
- **写集（批准）**：spec §5.2/§5.3 的 `src/evidence/**` + `src/l3/evidence-retention.ts`（依赖 #527）+ `src/deploy/bootstrap.ts` + `src/routes/diagnosis.ts` + `src/routes/conversations.ts` + `src/agent/conversation-engine.ts` + `src/agent/diagnosis-launcher.ts` + `src/agent/engine-context.ts` + `src/server.ts`（≤3 行）+ `tests/**`
- **中间态护栏**：守卫测试必须断言 `evidenceCollector !== null`（禁静默 null），且 `grep -rc "new EvidenceStore" src/` 全仓恰 1 处。

### C. 裁决二：五路证据分裂 → 登记 backlog，本批只读不修

- 实测确认注入点：`src/routes/conversations.ts:283` 与 `src/routes/diagnosis.ts:358` 两处 `evidenceCollector: null`（CTO 亲测命中）；唯一采集调用点在 `src/agent/conversation-engine.ts:626`（`collectFromInterview`，实测存在）→ 与 dev-doc 结论一致。
- 4 路（表 / 专家 refs 未校验 / 哨兵内存合成 / 本体 0 行节点）+ 1 路同名异物（product-lines 验收证据）→ **拆为独立 backlog 条目**，D728 完成后逐路评估，不在本批扩面。

### D. 裁决三：`task-state/D727.json` both-added 冲突 → 并集解决

- D727.json 同时存在于 `docs/d727-evidence-chain-spec`（dev-doc）与 `docs/d732-dispatch-batch3`（CTO 派单分支）→ 合并按**字段并集**（先例：board-backlog 按 id 并集）。
- **暴露的机制问题**：CTO 与执行方**同时 alloc 同一任务号**（分配器无跨分支锁）→ **登记为 D730 项**（alloc 跨分支唯一性校验）。

### E. 处置动作清单

1. 本补记即裁决书；**D728 编码指令的「Mac 线」字样由 CTO 更正为「Win 线」**（dev-doc 交付的指令内容不变，仅改归属）
2. 派单 §二 的执行方、写集、护栏同步以上
3. D730 新增两项改进：**写集 × 域归属交叉校验**、**alloc 跨分支唯一性校验**
4. 五路证据分裂 4 路登记 backlog
