# 派单：部署轨切片 D（D536）— 桌面端实际部署验收（Track A 最高优先级）

> 派单: CTO | 2026-08-26 | 认领: 📋 dev-doc（出 spec）→ 🛠 编码 session（实测执行）
> 方法论: 创始人批准切片思维 + 施工图 Track A 部署轨（"永远不被阻塞"）——L1 桌面端 8 验证点代码闭环后，实际部署验收是自然下一步
> 流程: dev-doc 出 spec → 编码实测 → K3 切片级审计 → CTO 合并
> 上一轮教训: D529（CI artifact 拿取——现在 token 可达 ~/.dsh/.credentials.yaml，5 分钟下载）；切片 A/B/C（安装/启动/首诊/数据不丢的验证脚本已就绪，本单复用）

---

## 写前核实（SOP §〇c ①，6 项已核实）

- [x] ① 任务来源/依赖: 施工图 Track A 部署轨；依赖切片 A/B/C 产物（安装包 + 服务自启 + 首诊 + 数据不丢全部已 audited/impl_done）
- [x] ② task-state 最新状态: D517-519（A）/D522-523（B）/D527-528（C）均已进 main；D536 新分配
- [x] ③ 基线资产实际存在: CI artifact（macOS 1040MB + Windows 215MB，run 32870900391，expired:False）；验证脚本（mac-install-verify.sh/win-install-verify.ps1/upgrade-data-verify.sh/first-diagnosis-timing.sh）+ runbooks（founder-demo-mac/win 等）——grep 确认
- [x] ④ DSH 借鉴核查: 无 DSH 可借鉴（Electron 安装包分发/实测，DSH 无 Electron）——🟢 品牌表层
- [x] ⑤ 写集重叠: D534/D535 刚合并（不重叠）；D536 已分配
- [x] ⑥ 上一轮教训: D529（artifact 下载通道已通）；切片 C 的 LLM 环境依赖（首诊实测需 LLM key）

## DSH 借鉴核查（§〇b 三步）

1. **施工图四色归属**：🟢 死守（品牌表层：Electron 桌面端部署验收，施工图 §3.1）
2. **借鉴边界判定**：**无 DSH 可借鉴**——安装包分发/安装/实测是 Electron + macOS/Windows 生态，DSH 无 Electron；显式写原因防执行方猜测
3. **DSH 源码参考**：无

## 切片定义

| 切片 | 用户可见价值 | D | 依赖 |
|---|---|---|---|
| A/B/C（已完成） | 能装/能开/能用（8 验证点） | D517-528 | 无 |
| **D 部署验收**（本次） | 安装包真实装到目标机、启动、首诊跑通 | **D536** | A/B/C 产物 |

**切片 D 价值**：验证点从"代码层 verified"→"实际部署可用"——客户/创始人能真正装上用起来（Track A 部署轨验收）。

## 现状材料（dev-doc 必读，已核实存在）

| 资产 | 位置 | 状态 | 关系 |
|---|---|---|---|
| CI artifact（安装包） | run 32870900391 artifacts（macOS 1040MB/Windows 215MB） | 可下载（token ~/.dsh/.credentials.yaml） | **D536 的对象** |
| Mac 安装验证 | scripts/desktop/mac-install-verify.sh（四断言：进程/窗口/healthz/日志） | 切片 A 交付（K3 复跑过） | 复用 |
| Win 安装验证 | scripts/desktop/win-install-verify.ps1（ps1 四断言） | 切片 B 交付 | 复用 |
| 升级数据验证 | scripts/desktop/upgrade-data-verify.sh | 切片 C 交付 | 复用 |
| 首诊计时 | scripts/desktop/first-diagnosis-timing.sh | 切片 C 交付 | 复用 |
| 部署指引 | docs/synova/runbooks/（desktop-build/founder-demo-mac/win） | 已入库 | 验收依据 |

## D536：桌面端实际部署验收（Track A 部署轨）

**目标**：下载 CI 安装包 → 真实安装到目标机（Mac + Win）→ 启动 → 服务自启 → 首诊旅程 → 数据不丢——**部署验收记录（founder-demo checklist）**，客户/创始人可直接照着装。

**spec 必答题**：
1. **artifact 下载 + 校验**：用 ~/.dsh/.credentials.yaml 的 GITHUB_TOKEN 下载两个 artifact（curl Actions API）→ md5 校验（D519 先例 evidence 落盘）
2. **Mac 实测**：dmg 安装（复用 mac-install-verify.sh）→ 启动 → 服务自启（D522）→ 首诊旅程（D527，需 LLM key 环境）→ 数据不丢（D528）——**founder-demo-mac checklist 可执行**
3. **Win 实测**：NSIS 安装（复用 win-install-verify.ps1）→ 双击 → 出窗（D523）——founder-demo-win checklist
4. **部署验收记录**：evidence 落盘（安装日志/启动/首诊/数据断言）+ founder-demo checklist 完成态
5. **已知限制如实标注**：签名/公证未做（切片 A descope）→ 首次安装需右键打开/绕过 Gatekeeper，如实写进部署指引

**验收**：Mac + Win 实测 evidence（安装/启动/首诊/数据，物理断言）+ founder-demo checklist 可执行 + artifact md5 落盘

## 写集约束

- **可碰**：scripts/desktop/（实测脚本复用/微调）、evidence/（部署验收记录）、docs/synova/runbooks/（部署指引补全）、task-state/D536.json
- **不碰**：src/、scripts/audit/、build-synova.cjs（构建链已闭环，本单只消费产物）
- **防膨胀**：复用既有验证脚本（mac-install-verify/win-install-verify/upgrade-data-verify），**不新建验证机制**（除非实测暴露缺口）

## 切片级审计

- D536 完成后 K3 审（部署轨切片，验证部署验收真实可复现）
- task-state 加 `"slice": "deploy-acceptance"` 字段
- 审计验收 = founder-demo checklist（Mac+Win）可执行 + evidence 物理断言

## 给 dev-doc 的交付要求

1. D536 spec（SYNOVA-IMPL-DSH-D536-*），含写集表/必答题/验收/自检
2. 验收物理可复现（安装/启动/首诊/数据断言 + 截图/日志），禁止"下载了 artifact"冒充"装上了"
3. 已知限制（签名/公证）如实声明，不伪造
4. 复用既有验证脚本，新增只在暴露缺口时（spec 注明）

---

## 写后自检（SOP §〇c ③，8 项）

- [x] ① 验证点编号: N/A（部署轨切片，非 product-progress 验证点——对应 Track A 部署验收）
- [x] ② D# 未占用: D536 alloc 分配确认
- [x] ③ 依赖链: D536 依赖 A/B/C 产物（全部已进 main/audited）；无其他依赖
- [x] ④ DSH 借鉴核查三步完整: 🟢 品牌表层/无借鉴（Electron 生态）/无源码参考
- [x] ⑤ 现状材料全部核实过: artifact 可下载（API 验证 expired:False）+ 验证脚本/runbook 存在（git ls-tree）
- [x] ⑥ 验收物理可复现: 安装/启动/首诊/数据断言 + founder-demo checklist——非"下载了"冒充
- [x] ⑦ 术语一致: "部署轨切片 D"（Track A）明确，无混淆
- [x] ⑧ 无遗漏: 执行方（dev-doc→编码）/LLM key 环境（首诊实测）/已知限制（签名）/复用约束——齐全

**交付门槛**：dev-doc 拿到可直接开工（artifact 下载通道/验证脚本/checklist 已写死）+ K3 可核（物理断言）。
