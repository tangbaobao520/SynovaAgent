共享声明: D712, D931, D940, D943, D955, D979 — 本文件为**共享治理件**：任何卡修改本文件时，须把本卡 D# 追加进本行（同时保留历史卡号）；机制见 `docs/synova/coordination/派单-FIX-011-共享治理文件机制-20260926.md`（D979 FIX-011 ⒝，门禁实现 `scripts/control-tower/shared_file_decl.py`）。

# 派单模板（已目录化）→ 正文见 **[派单模板/README.md](派单模板/README.md)**

本文件自 2026-09-26（D979）起为**短指针**（正文已迁出，保留文件以防既有引用断链）：

- 正文目录：`docs/synova/coordination/派单模板/` —— `README.md`（短索引 + 横切章节：路由判定 / 命名规范 / 写前核实 / 写后自检 / 交付门槛 / 派单说明 / 执行形态 / owner 在飞检查）
- 四个类型分册：`产品.md`（类一 功能切片）/ `门禁.md`（类二 控制塔治理）/ `哨兵.md`（哨兵体系）/ `审计.md`（类三 验证证据审计）
- 既有引用指向本文件 ⇒ **不断链**（点开即到本指针，再一跳进正文）；**新派单一律从 `派单模板/README.md` 进入**。
