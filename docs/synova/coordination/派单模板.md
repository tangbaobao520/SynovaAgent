
<!-- ═══ D943 强制表头（每次派单必须原样保留这三行，缺一即 K3 判 FAIL）═══
断面: <version> @ <head>        ← 唯一源 docs/synova/coordination/DSH-断面.json（禁手写猜）
单一事实源: <本单判据所依据的权威文件路径>
本单不得引用的已作废口径: <从 docs/synova/coordination/已作废口径表.md 摘录相关条目>
检查命令: python3 scripts/control-tower/check-dsh-anchor.py --repo .   # 必须 OK
═══════════════════════════════════════════════════════════════════════ -->

## 机制在场性三问（D943 必答，写不出就不许派）

任何交付/派单必须能回答这三句——**今天三大事故（预设写死层 / D919 未接线 / 组 7a 恒过）全部由这三句拦住**：

1. **消费者是谁？**（哪个角色/哪个进程真的会读它）
2. **它在哪一层/哪个文件？**（绝对路径；若是"被读取的层"，须给出读取点 `file:line`）
3. **用哪条命令证明它在跑？**（命令 + 期望输出；**不得**用"我写了"代替）

## 借鉴核查（D943 加严）

涉及 DSH 借鉴的单据，除施工图四色清单外，**必须附**：①本单断面（rc.1 @ 46a7f68b）②所引 DSH 源码行号在**该断面树**上的复核命令与输出 ③是否命中 `已作废口径表.md`（命中即改口径，不得沿用）。

# 派单模板：{切片/任务名}（DXXX/DXXX）

> 派单: CTO | {日期} | 认领: {角色，如 📋 dev-doc}
> 方法论: 创始人批准切片思维 / 任务拆分（{一句话说明本单在全局的位置}）
> 流程: {角色} 出 spec → 编码按依赖推进 → K3 切片级审计 → CTO 合并
> 上一轮教训: {引用与本单相关的最新教训，防复发}
> **路由: {第几类}（见下表）→ 本单发出的下一跳 = {谁}；{是否经 dev-doc，为什么}**

---

## 〇、路由判定（2026-09-13 固化 —— 创始人提问"D712 究竟发给谁"暴露的歧义，写死防再猜）

**判定一句话**：这次要产出的是「**设计**」还是「**事实**」？

| 类 | 任务类型 | 下一跳 | 为什么 | 实例 |
|---|---|---|---|---|
| **一** | 产品功能 / 切片（要定接口、契约、数据流、写集） | **先 📋 dev-doc 出 spec（含编码指令）** → 再 🛠 编码实现 | spec 是必须产物，否则编码各写各的 | D577、D556、D-1 通知模型步 2-4 |
| **二** | 控制塔 / 治理 / 缺陷修复（根因清楚、改动局限、验收物理可复现） | **直接派执行方**（DSH 域内可派并行 CTO；🛠 编码） | `scripts/` 门禁 + coordination 文档 + 预设技能 = **Mac DSH 专属**（TASK-ROUTING §四）；派单文档本身即准 spec，再走 dev-doc 是空转 | D706 / D707 / D664 / D708 |
| **三** | 验证 / 证据（不是"实现什么"，而是"去实测并落盘证据"） | **直接派执行方，无 spec** | 无 spec 输入物可产出；dev doc 在这一类写不出东西 | **D712 桌面端重验** |

**多执行方派单的发送规则（D712 型）**：**都发，但不同时**——
1. 先发**做事方**（并行开工，各自只拿与自己相关的部分，避免互相等）
2. 后发**复核方**（如 K3）——复核的输入物是做事方的产出，早发没意义

**代码归属 ≠ 测试机器归属**：跨平台验证时，按"**谁拥有该目录**"定归属（如 `electron/` = Mac DSH），但"**在哪台机器跑**"按平台定（Windows 实测必须在 Win 机器上跑，由 Win 侧执行但不算 Win 的代码域）。

---

## 写前核实（强制清单 — cto-handover skill §〇c ①，6 项缺一不写）

- [ ] ① 任务来源/依赖: {切片定义 or 返修来源；上游任务状态（main 权威）}
- [ ] ② task-state 最新状态: {git show origin/main:task-state/DXXX.json → 状态}
- [ ] ③ 基线资产实际存在: {grep/ls 确认，位置}
- [ ] ④ DSH 借鉴核查: {施工图四色 → 借鉴边界 → 源码参考（§〇b 三步）}
- [ ] ⑤ 写集重叠检查: {与在途任务撞车？D# 未占用（ls task-state/）}
- [ ] ⑥ 上一轮教训: {同型任务历史坑 → 派单引用}

## DSH 借鉴核查（强制章节 — cto-handover skill §〇b 三步核查后填写）

1. **施工图四色归属**：{本任务模块是 🟢死守 / 🔵借DSH / 🟡搬走 / ⚫删除 / 混合？依据 = 施工图 §3 哪一行}
2. **借鉴边界判定**：{有 / 无 DSH 可借鉴；有 → 借鉴哪个包/范式（Stage 3 后替换 or 长期借鉴不引代码）；无 → 显式写原因，防执行方猜测}
3. **DSH 源码参考**：{文件路径 + 函数 + 行号，如 `~/.nvm/.../dsh-subprocess-local/lib/index.js` 的 `signalTree()` L757；无则写"无"}

> 红线（沿用施工图 R1/R3/R4）：借鉴 = 读范式自研，不 `npm install @deepseek-ai/dsh`、不 copy DSH 代码（Stage 3 前零依赖）；不复制 OpenViking（AGPLv3）；验收含接线审计。

---

## 切片定义（CTO 已定，{角色} 复核）

| 切片 | 用户可见价值 | 验证点 | 依赖 |
|---|---|---|---|
| {切片名}（本次） | {一句话价值} | {验证点编号} | {依赖} |
| {后续切片} | {一句话价值} | {验证点编号} | {本次} |

## 现状材料（{角色} 必读，全部在 main，先读实际代码不凭记忆）

| 资产 | 位置 | 状态 | 与本单的关系 |
|---|---|---|---|
| {资产} | {路径} | {状态} | {关系} |

## DXXX：{任务名}（验证点 {编号}）

**目标**：{用户可见行为，物理可复现}

**依赖**：{无 / 前置任务}

**spec 必答题**：
1. {必答题 1}
2. {必答题 2}
3. {必答题 3}

**验收**：{物理可复现的验收标准，禁止静态 grep 冒充 / 文档声称}

## 写集约束

- {可碰的文件/目录}
- **不碰**：{src/ 产品代码 / scripts/audit/ 等}
- {串行/并行说明}

## 切片级审计

- {N} 个 D 完成后一次提审（K3 审切片 {X}，报告覆盖 DXXX-DXXX）
- 每个 D 的 task-state 加 `"slice": "{切片}"` 字段
- 审计验收 = 验证点 {编号} 从 uncommitted → verified

## 给 {角色} 的交付要求

1. {spec 文件命名要求}
2. {验收物理可复现要求}
3. {前置依赖声明要求}

---

## 写后自检（强制清单 — cto-handover skill §〇c ③，8 项缺一不交付）

- [ ] ① 验证点编号正确（对照 product-progress.json：{核对结果}）
- [ ] ② D# 未占用（alloc-task-id 确认：{结果}）
- [ ] ③ 依赖链正确（谁依赖谁写死：{依赖关系}）
- [ ] ④ DSH 借鉴核查三步完整（无借鉴也写原因：{确认}）
- [ ] ⑤ 现状材料全部核实过（grep/ls 物理确认：{文件清单}）
- [ ] ⑥ 验收物理可复现（计时/断言/evidence：{验收方式}）
- [ ] ⑦ 术语一致（L1 桌面端切片 vs Win 线 AUTH：{确认}）
- [ ] ⑧ 无遗漏（LLM 环境/执行方/交付要求/审计验收项：{确认}）

**交付门槛**：执行方拿到派单可直接开工（无需再问基线/依赖/写集/借鉴）+ K3 审计可核（验证点物理可复现）。

---

## 派单说明（给创始人复制——自包含，零查找）

> 创始人直接复制本段转执行方（SOP §〇c ⑤）。内容 = 派单文档核心，执行方拿到即可开工。

```
【派单】{任务名}（DXXX）
> 认领: {角色} | slice: {切片}

## 背景（一句话）
{来源/依赖/为什么现在做}

## 现状材料（执行方必读）
- {关键资产}: {位置} —— {一句话状态}

## spec 必答题
1. {必答题 1}
2. {必答题 2}
3. {必答题 3}

## 写集约束
- 可碰: {范围}
- 不碰: {src/scripts/audit/等}；{防膨胀约束}

## 验收（物理可复现）
{验收标准}

## 交付要求
1. {spec 命名}
2. {evidence 要求}
3. {诚实声明}
```

---

## 执行形态（**硬字段，缺一不派** — 2026-09-23 立；上次漏此节导致"小队是摆设"）

> DSH 的策略是「未被明确要求就不创建队友」。**不写本节 = 执行方按默认走子代理**（无 mailbox、无共享任务、无并行收益）。

- **组队**：用 Agent Teams（预设 `synova-squad-lead` 已挂 `agent-team`/`tool-agent-team`/`ui-agent-team`，`maxMembers=4`）
- **成员编制**：队长 + ≤2 编码 + **1 独立自验**（自验不得由编码兼任）；**队长不下场写码**
- **派活**：每成员一份共享任务，含 **写集（逐文件）+ 验收命令 + 写者**；写集两两不重叠；热点文件单写者
- **并发**：重型验证（vitest/全量门禁/黄金门禁）串行 ≤1
- **回执必含**：**团队成员运行记录**（成员名/状态/token/共享任务 id 与状态）
- **退回判据**：只有队长在运行、成员未运行 → 等同"机制建成未接线"（M3），直接退回

## 派单前置：**文件 owner 在飞检查**（D943，防双写）

派单前**必须**对写集里每一个文件问一句：**它是否已有 owner 卡在飞？**

```bash
# 查该文件是否已被在飞卡声明（task-state 全量扫 write_set）
grep -l "<文件路径>" task-state/*.json 2>/dev/null
# 再查是否有活动分支在改它
git ls-remote --heads origin | wc -l   # 结合分支名判断
```

**裁定规则**：
- **已有 owner 卡在飞 ⇒ 后到者不得开写**；改为「**并入 owner 卡**」或等其合并后再开。
- 热点文件（`ci.yml`／`scripts/pre-commit-check.sh`／`ownership.yaml`／`派单模板.md`）**永远单写者**。
- 违反后果（今天真实发生）：B5-U6 与 #725 **同改 `gen-cto-health.py`** ⇒ B5-U6 停在"审级未定"空转，
  最后由 CTO 裁定归并、只审一次。**代价 = 一轮往返 + 一个悬案。**
