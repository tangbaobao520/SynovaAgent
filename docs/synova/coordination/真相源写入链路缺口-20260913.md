# 真相源状态写入链路缺口定位（D741）

> 任务: D741（P0，只定位不修） | 定位者: 🧭 synova-cto（DSH line，Mac 主 CTO）
> 基线: `origin/main @ 3f0a43b5`
> 边界: **未修改 `generate-dashboard.py` / `pre-commit-check.sh`（D735 爆炸半径）**；未修改任何文件；均用 `git show origin/main:*` 读权威基线，本地工作区与 origin/main 的 6 个关键文件 `git diff --stat` 为空（完全一致）
> 定位时间: 2026-09-14 01:50 +0800

---

## 0. 两个问题、两个结论（先读）

| # | 问题 | 结论 | 缺口层 | 关键行 |
|---|---|---|---|---|
| 1 | product-progress.json 里「桌面端/对话交互/MCP 集成」三条线状态为 None | **口径修正 + 系统性缺失**：不是三条，**实测 26/26 条线全无 `status` 字段**（点名的三条＝L1 交互层三条线）。写入端根本不产出该字段，三个消费端也都不读它 → 线级"状态"在真相源链路上**既无写入方也无消费方** | 写入点缺失（主）：`calc-progress.py:316-330`；消费端同缺（3 处）；契约层（product-lines.yaml）亦无线级 status | 见 §1 |
| 2 | task-state 的 5 个 FAIL 在看板不呈现 | **成立且已复现**：任务看板按 `task-state.status` 映射列，**从不读 `audit.verdict`** → 5 个 FAIL 落 `done`(2)/`todo`(3) 列，**failed 列结构上不可达**（全仓 task-state 无 `failed`/`cancelled` 状态产出方）。另发现 CTO 健康看板生成器**当前崩溃**，DASHBOARD-CN 的审计列**取错数据源** | 过滤点：`sync.js:40-48` + `:190-198` + `:286`；旁证链路 3 条 | 见 §2 |

**一句话**：两条问题都不是"某处漏写一个值"，而是**链路上缺少承载者**——线级状态无字段、审计失败态无列。

---

## 1. 问题一：product-progress.json 三条线状态为 None

### 1.1 口径修正（先纠事实）

派单说"三条线状态为 None"。**实测口径是 26 条线全部无状态**——被点名的三条（`id=1 桌面端` / `id=2 对话交互` / `id=26 MCP 集成`）正好是 **L1 交互层的三条线**，是"看这三条时注意到了"的抽样，不是这三条独有。

```bash
# 复跑命令（实测）
python3 -c "
import json
d=json.load(open('docs/synova/product-lines/product-progress.json'))
print('lines:', len(d['lines']))
from collections import Counter
print(Counter([str(l.get('status')) for l in d['lines']]))
print('有 status 键的线数:', sum(1 for l in d['lines'] if 'status' in l))
"
# → lines: 26
# → Counter({'None': 26})
# → 有 status 键的线数: 0
```

`git show origin/main:docs/synova/product-lines/product-progress.json` 实测同一结果（本地与 origin/main 逐字节一致，`git diff --stat` 为空）。第 1 条线的原始 JSON 对象（`product-progress.json:7-30`）字段清单为：
`id / name / value / weight / baseline_pct / baseline_note / done_definition / total / verified / progress_pct / k3_gate / status_counts / points` —— **没有 `status`**。

### 1.2 写入点缺失（file:line）

**本该写线级状态而没写的位置**：

```
scripts/product-lines/calc-progress.py:316-330   ← 线对象组装点（缺口本体）
```

```python
316	        lines_out.append({
317	            "id": line["id"],
318	            "name": line.get("name", ""),
319	            "value": line.get("value", ""),
320	            "weight": float(line.get("weight", 1.0)),
321	            "baseline_pct": int(line.get("baseline_pct", 0)),
322	            "baseline_note": line.get("baseline_note", ""),
323	            "done_definition": line.get("done_definition", ""),
324	            "total": total,
325	            "verified": verified,
326	            "progress_pct": progress,
327	            "k3_gate": k3_gate,
328	            "status_counts": counts,      # ← 只有"点级状态计数"，没有"线级状态"
329	            "points": points_out,
330	        })
```

- 同文件 `:294-303` 只对**点**写 `status`（`status_for_point()` 六态：`uncommitted/pending_k3/verified/rejected/stale/failed`，见 `:167-190`）；
- 文件头契约注释 `:15` 声明的线对象形状里同样**没有** line 级状态：`baseline_pct, status_counts:{...}, k3_gate, points:[{id,desc,status,...}]`；
- 全仓库无第二个写 `product-progress.json` 的程序：`grep -rln "product-progress.json" scripts/` → `refresh-all.sh / calc-progress.py / gen-progress-page.py / gen-k3-task.py / pre-commit-check.sh`（后三者只读/只校验）。

**契约层也缺**：`docs/synova/product-lines/product-lines.yaml` 里**只有点级** `status:` 种子，**没有线级** `status:` 键：

```bash
git show origin/main:docs/synova/product-lines/product-lines.yaml | grep -nE "^\s{2}status:|^\s{4}status:"   # → 空
git show origin/main:docs/synova/product-lines/product-lines.yaml | sed -n '46,50p'
# → - id: "1-2" / desc: ... / evidence: [...] / status: uncommitted   ← 点级（8 空格缩进）
```

### 1.3 消费端也都不读（3 处，全部实测）

| 消费端 | 位置 | 取用的字段 | 是否读 line.status |
|---|---|---|---|
| DSH 产品面板 | `dsh/plugins/synova-dashboards/lib/collect.js:107-115` | `id/name/progress_pct/verified/total/weight/status_counts` | **否** |
| 任务看板（产品线源③） | `dsh/plugins/task-board-adapter/scripts/derive-board-sources.py:283-291` | `id/name/total/verified/progress_pct` | **否** |
| 产品进度 HTML | `scripts/product-lines/gen-progress-page.py:114-122` | `progress_pct` + `k3_gate`（线级）；`p["status"]`（点级 `:127`） | **否** |

```bash
# 复跑命令
sed -n '107,115p' dsh/plugins/synova-dashboards/lib/collect.js
sed -n '283,291p' dsh/plugins/task-board-adapter/scripts/derive-board-sources.py
sed -n '112,127p' scripts/product-lines/gen-progress-page.py
# 看板快照侧同样无该字段（Host 已同步的产物）
python3 -c "
import json; s=json.load(open('$HOME/.dsh/task-board/source-snapshot.json'))
L=s['product_lines']['lines']; print('lines',len(L),'有 status 字段的线数:',sum(1 for l in L if 'status' in l))"
# → lines 26 有 status 字段的线数: 0
```

### 1.4 判定

- **写入点缺失（主因）**：`scripts/product-lines/calc-progress.py:316-330`。
- **消费端全缺（并列缺口）**：即使补上写入，当前三个消费端也不会显示——所以"让三条线有状态"至少需要**三处改动**（契约 + 写入 + 消费），单改一处无效。
- 副作用（本轮顺带实测）：`product-progress.html` 完全没有 "None" 字样（`grep -c None` = 0）；页面上可见的 `状态判定异常 3 处` 是**另**一指（`degraded.problems` = 点 4-4/4-5/4-6 非法 status 种子 `'deferred'`，渲染于 `gen-progress-page.py:210`），与三线 None **不是同一问题**（勿混淆）。

---

## 2. 问题二：task-state 的 5 个 FAIL 在看板不呈现

### 2.1 先定对象（5 个 FAIL 是谁）

```bash
grep -l '"verdict": "FAIL"' task-state/*.json
# → task-state/D393.json  D503.json  D515.json  D549.json  D572.json   （5 个，与派单一致）
for f in D393 D503 D515 D549 D572; do git show origin/main:task-state/$f.json \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['task_id'],'status=',d['status'],'verdict=',d['audit']['verdict'])"; done
# → D393 status=audited  verdict=FAIL
# → D503 status=impl_done verdict=FAIL
# → D515 status=impl_done verdict=FAIL
# → D549 status=impl_done verdict=FAIL
# → D572 status=audited  verdict=FAIL
```

### 2.2 主过滤点：任务看板按 `status` 映射列，从不读 `audit.verdict`

**文件**：`dsh/plugins/task-board-adapter/lib/sync.js`

```
sync.js:40-48    DEFAULT_STATUS_MAPPING（唯一映射表——键是 task-state.status，没有 verdict 维度）
sync.js:162-171  resolveBoardStatus()（按映射表 + 48h 分支活动决定列）
sync.js:190-198  mapTask()（构造看板任务：status 来自 synStatus，不碰 raw.audit）
sync.js:286-290  Win/git 派生任务同款：raw.status === "audited" ? "done" : "todo"
```

```js
40	export const DEFAULT_STATUS_MAPPING = Object.freeze({
41	  spec_done: "todo",
42	  claimed: "todo",
43	  impl_done: "todo",
44	  audited: "done",          // ← FAIL verdict 也走这一支：有报告就算 done
45	  closed: "done",
46	  cancelled: "failed",
47	  failed: "failed",         // ← failed 列只有这两个键能进
48	});
```

```js
286	  const status = raw?.status === "audited" ? "done" : "todo";
290	    `K3 审计: ${raw?.status === "audited" ? "有审计报告" : "未审计（合并≠完成，待 K3 审计）"}`,
```

**过滤结论**：看板的"审计"语义被压缩成**布尔**——"有报告 / 没报告"，**完全没有 verdict 这一维**。因此：

- `audited + FAIL` → **done 列**（D393、D572）
- `impl_done + FAIL` → **todo 列**（D503、D515、D549）
- **failed 列结构上不可达**：`cancelled`/`failed` 两个键在 task-state 中**零产出方**：

```bash
python3 -c "
import json,glob,collections
c=collections.Counter()
for p in glob.glob('task-state/*.json'):
    try: d=json.load(open(p))
    except: continue
    if isinstance(d,dict): c[d.get('status')]+=1
print(c.most_common())"
# → [('audited',107), ('impl_done',70), ('closed',14), ('claimed',11), ('spec_done',3)]
grep -l '"status": "failed"\|"status": "cancelled"' task-state/*.json    # → 零结果
```

### 2.3 看板渲染侧实测（Host 账本，物理呈现）

```bash
python3 -c "
import json
d=json.load(open('$HOME/.dsh/task-board/ledger-v2.json'))
for t in d['tasks']:
    if any(x in str(t.get('id')) for x in ['D393','D503','D515','D549','D572']):
        print(t['id'], '→ 列:', t['status'], '|', str(t['title'])[:36])"
# → D393 → 列: done | D393 · task-state 状态工件自动派生改造（D393）
# → D503 → 列: todo | D503 · G12 时区容差修复（brief 认领窗口 ±1 天）
# → D515 → 列: todo | D515 · 控制塔 V5.0.0 减负重构（三批 13 项）
# → D549 → 列: todo | D549 · sealed-tests-ci-canary
# → D572 → 列: done | D572 · 线1桌面端K3全量复核派单
# 5/5 无一进 failed 列
```

**这是"5 个 FAIL 在看板不呈现"的物理证据**：3 个 FAIL 挂在别人会当成"完成/待办"的列里，0 个进入失败列。

### 2.4 旁证链路（另外 3 条真实存在的展示缺口，均为 P0/P1 级）

#### (a) CTO 健康看板生成器**当前直接崩溃**（`gen-cto-health.py:305`）

```bash
python3 scripts/control-tower/gen-cto-health.py --dry-run > /tmp/d741.txt 2>/tmp/d741.err; echo exit=$?
# → exit=1
tail -3 /tmp/d741.err
# → File ".../gen-cto-health.py", line 563, in main
# →     t, ts_meta = analyze_task_state()
# → File ".../gen-cto-health.py", line 305, in analyze_task_state
# →     spec_path = (d.get("spec") or {}).get("path")
# → AttributeError: 'str' object has no attribute 'get'
```

**根因**：`task-state/D600.json` 的 `spec` 字段是**字符串**（`"docs/plans/codex/implementation/SYNOVA-IMPL-D600-..."`）而非对象，`gen-cto-health.py:305` 直接 `.get()` → 抛异常 → **第③面 CTO 健康看板自 D600 入库起无法再生成**（`--dry-run` 与写盘路径同崩）。

```bash
python3 -c "
import json,glob
bad=[p for p in glob.glob('task-state/*.json') if isinstance(json.load(open(p)),dict) and isinstance(json.load(open(p)).get('spec'),str)]
print(bad)"
# → ['task-state/D600.json']
git show origin/main:task-state/D600.json | python3 -c "import json,sys; print(type(json.load(sys.stdin).get('spec')).__name__)"
# → str   （origin/main 同样有此缺陷，非本地污染）
```

现有产物 `docs/synova/CTO-HEALTH.md` 内容停留在 **2026-08-29 23:24**（文件头 `:3` "生成: 2026-08-29 23:24:17"，末次提交 `8f34ccc4` 2026-08-29），**其中根本没有 D572 行**（`grep -c "^| D572 "` = 0；表内最大 D# = 564），5 个 FAIL 里只有 D515 显示为 FAIL。

#### (b) 即便生成器能跑，verdict 解析仍**优先级倒置**（`gen-cto-health.py:326-331`）

```python
326	                if "CONDITIONAL PASS" in txt:
327	                    audit_txt = "CONDITIONAL_PASS"
328	                elif "PASS" in txt:            # ← "PASS" 先于 "FAIL" 判定
329	                    audit_txt = "PASS"
330	                elif "FAIL" in txt:
331	                    audit_txt = "FAIL"
```

在整份报告文本上做**子串**判定，且 `PASS` 分支在 `FAIL` 之前 → **只要报告正文任意位置出现 "PASS" 字样，FAIL 裁决就永远读不到**。现产物中 D393 行就被显示成 `PASS`（`CTO-HEALTH.md:69`），而其 task-state verdict = **FAIL**。修复方向（择一）：先判 FAIL，或直接改用 `audit.verdict` 字段（`analyze_task_state()` 已经拿到了 `d.get("audit")`，见 `:321`）。

#### (c) DASHBOARD-CN.md 的"审计"列**取错数据源 + 产物停更 16 天**

- 数据源错：`gen-task-board.py:341-348` 的 `read_audit()` 读的是**唯一一个**全局文件 `.codex/audit/audit-result.json`（外部审计器的**代码模式扫描**结果：P0/P1 计数），与 K3 的**任务裁决**（`task-state/*.json → audit.verdict`）**不是一回事**；渲染于 `:607-613`。

```bash
grep -n "audit-result.json" scripts/control-tower/gen-task-board.py | head -3
# → :15 / :341（read_audit 定义）/:821（audit_path = root/".codex"/"audit"/"audit-result.json"）
head -6 .codex/audit/audit-result.json
# → {"ok":true,...,"p0Count":0,"p1Count":5,"findings":[{...源码 import 模式...}]}  ← 非任务裁决
grep -n "^| D393 \|^| D503 \|^| D515 \|^| D549 " docs/synova/DASHBOARD-CN.md | awk -F'|' '{print $2,$9}'
# → D393 — / D503 — / D515 — / D549 —        ← 审计列全 "—"，FAIL 不可见
```

- 停更：`DASHBOARD-CN.md` 末次生成 **2026-08-28**（`63e93c46`），表内最大 D# = **553**，而 task-state 已有 **D725**（落后 ≈172 个任务号）；且**没有任何 CI job 跑它**：

```bash
grep -rn "gen-task-board\|gen-cto-health" .github/workflows/    # → 零命中（两个看板都无自动化）
git log -1 --format="%h %ad %s" --date=short origin/main -- docs/synova/DASHBOARD-CN.md
# → 63e93c46 2026-08-28 docs(dashboard): DASHBOARD-CN 重生成 364 任务 git 投影刷新
git show origin/main:docs/synova/DASHBOARD-CN.md | grep -o "^| D[0-9]*" | grep -o "[0-9]*" | sort -n | tail -1   # → 553
```

（对照：`synova-dashboards` 面板**不丢** FAIL——`collect.js:167` 读 `raw.audit?.verdict`、`client.js:164` 渲染 "审计 + verdict"；说明缺口在**任务看板 + 两个 CTO 看板**链路，不是"没有数据"。）

### 2.5 过滤点汇总（file:line 清单）

| 链路 | 文件:行 | 行为 | 后果 |
|---|---|---|---|
| 任务看板（主） | `dsh/plugins/task-board-adapter/lib/sync.js:40-48` | 列映射表只认 `status`（`audited→done`、`failed/cancelled→failed`） | FAIL verdict 无维度 → 落 done/todo |
| 任务看板（主） | `…/sync.js:190-198`、`:286-290` | 构造任务时不读 `raw.audit.verdict`；文案只有"有审计报告/未审计" | 失败态不可见 |
| 任务看板（结构性） | 同文件 `:46-47` | `failed` 列唯一入口是 `cancelled/failed` 状态 | 全仓无该状态产出 → failed 列恒空 |
| CTO 健康看板 | `scripts/control-tower/gen-cto-health.py:305` | `spec` 为字符串时 `.get()` 抛异常 | 生成器自 D600 起**整体崩溃** |
| CTO 健康看板 | `…/gen-cto-health.py:326-331` | 子串判定且 PASS 优先于 FAIL | FAIL 报告被读成 PASS（D393 实例） |
| CTO 健康看板 | `…/gen-cto-health.py:471-482` | 任务表渲染 `audit_txt`（文本解析结果），不用 `audit.verdict` | 数据源本就绕开 task-state 权威字段 |
| DASHBOARD-CN 看板 | `scripts/control-tower/gen-task-board.py:341-348`、`:607-613`、`:821` | 审计列唯一来源 = `.codex/audit/audit-result.json`（代码扫描） | 5 个 FAIL 全列 "—" |
| DASHBOARD-CN 看板 | 产物 self | 末次生成 2026-08-28，无 CI job | 落后 172 个任务号 |
| 产品线状态（问题一） | `scripts/product-lines/calc-progress.py:316-330` | 线对象无 `status` 键 | 26/26 线 None |
| 产品线状态（问题一） | `dsh/plugins/synova-dashboards/lib/collect.js:107-115`、`derive-board-sources.py:283-291`、`gen-progress-page.py:114-122` | 三端均不读 `line.status` | 即使写入也无处显示 |

---

## 3. 定位纪律声明

- **只定位不修**：本轮未改动任何文件（含 D735 爆炸半径内的 `generate-dashboard.py` / `pre-commit-check.sh`；两者本次仅被 grep/sed 读取，零写入）。
- **未做的复跑（如实声明）**：未运行 `sync.js` 的 board 派生脚本（会写 `~/.dsh/task-board/*` 账本）；C 节 `gen-cto-health.py --dry-run` 是**只打印不落盘**路径，已在 D740 报告中登记；`ledger-v2.json` / `source-snapshot.json` 为 Host 已生成产物，本轮只读。
- **口实差异已标注**：派单"三条线 → 实测 26 条线"（§1.1）；页面"状态判定异常 3 处"与三线 None **非同一问题**（§1.4）。

---

## 4. 建议派工（CTO 供裁，不自行立项）

> 已按"发现即登记"红线同步入 `docs/synova/coordination/board-backlog.json`：`PLAN-gen-cto-health-spec-crash`（P0）、`PLAN-board-verdict-dimension`（P1）、`PLAN-dashboard-line-status`（P2）；另订正 `PLAN-k3-cond-clear` 的失真 note（原称「骨架 brief 已清+审计清单已补」与 D740 实测不符）。

| # | 事项 | 归属 | 优先级 |
|---|---|---|---|
| 1 | **修 `gen-cto-health.py:305` 崩溃**（兼容 `spec` 为字符串；或统一 task-state schema 并加校验）——第③面看板当前完全不可生成 | CTO（scripts/control-tower 域） | **P0** |
| 2 | **修 `gen-cto-health.py:326-331` verdict 优先级**（FAIL 先判，或直读 `audit.verdict`） | CTO | P1 |
| 3 | **任务看板补 verdict 维度**：映射表引入 `audit.verdict`（FAIL → failed 列 / CONDITIONAL → 待办带标记），并给 failed 列真实产出方 | 插件维护方（dsh-web 插件全家桶仓库） | P1 |
| 4 | **DASHBOARD-CN 审计列换源 + 接入 CI**（或正式宣布该看板退役，避免双真相） | CTO | P1 |
| 5 | **线级状态三处补全**（契约 yaml + `calc-progress.py` 写入 + 消费端呈现）——需先定"线级状态语义"（建议：由 `status_counts` 派生 + `k3_gate` 合成），属产品口径，建议创始人拍板后派 | 创始人裁决 + CTO/编码线 | P1 |
| 6 | 看板"在途任务"标题与实际内容不符（`client.js:150` 渲染全部 task-state，含已关闭）——文案/过滤口径待收敛 | 插件维护方 | P2 |

---

**产出**: 本报告 + `D740` 报告 `docs/synova/coordination/L1交互层未闭合项-20260913.md`
**基线锚点**: `origin/main @ 3f0a43b5`
