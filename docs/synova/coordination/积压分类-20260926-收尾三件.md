# 收尾三件（M6）· CI/分支积压处置小队 · 2026-09-26

> **本件不是第二份分类。** 分类正文唯一在 `docs/synova/coordination/阶段0-积压分类-20260926.md` **附录 D**（本分支内为该文件追加 3763 行）。
> 本件只承载 squad M6 收尾三件：**diff / 自验结论 / 遗留清单**，外加前提实测与团队运行记录。

- as_of：2026-09-26T04:4x+0800（各数字各自带 as_of，见下）
- 交付分支：`chore/stage0-backlog-classify-20260926`
- **PR base = `chore/D1011-stage0-backlog-classification`（栈式 PR，显式声明 base）**
- origin/main（全程未移动）：`ee721b0abf52a4cdcc42caef44669d2cca423264`

---

## §1 diff（原始输出）

```
$ git -C .synova-wt-backlog-d diff --stat
 .../synova/coordination/阶段0-积压分类-20260926.md | 3763 ++++++++++++++++++++
 1 file changed, 3763 insertions(+)

$ git -C .synova-wt-backlog-d diff --numstat
3763	0	docs/synova/coordination/阶段0-积压分类-20260926.md
```

- **既有 1020 行 0 删除**：独立自验逐字节核过 —— 工作区前 1020 行 sha256 == HEAD 版 == `936bab9555e4b23796c1e688c34fef29635bc18845f206e7f79463452bd1e02e`（`cmp` byte-for-byte IDENTICAL）
- 新增文件（未跟踪，本次提交纳入）：`docs/synova/coordination/积压快照-20260926.json`（19335 行 / 754008 B / sha256 `2c383781…f37d5`）
- **零删除、零分支引用操作、零 worktree 增删**（全队声明 + 自验复核）

---

## §2 自验结论（独立自验员，非分类员、非执行者）

**task-3（阶段A 快照 + 阶段B 分类）**：`自验结论：有保留通过`
- 报告：`/tmp/verify-stage0-backlog-20260926.md`（295 行 / 42165 B）
- **零误判**：90/90 PR 分类由自验员**独立重实现判据 P0–P5 后重跑，0 处不一致**；`可关` 空集经 90/90 独立证伪；撞号 9 组 / 31 refs 全量一致；既有 1020 行删除行 = 0 且逐字节相等；禁动清单被触碰 = 0；红证 `INJECTED-RED` = 0
- **核心风险面**："真实交付被误判为可关" = **0 例**（触产品代码 45 条中判 `可关` 为空、`可合 且触产品代码` 为空、`可合 且触热文件` 为空）
- 保留 4 项：(a) 表内 sha 相对 live 已漂移 3 条（#832 #838 #803）；(b) B.9 自读插入数过期（3744 vs 实测 3763，0 删除结论经三法独立证实）；(c) CTO 基线 `/tmp 24` 口径 7 种口径均未复现（归 CTO）；(d) 附录 D 数字为文件级 as_of 绑定

**task-5（阶段B 执行面）**：`待回`（独立自验员只读复核中，本件提交时尚未返回；返回后补记）

> **本队不判"通过"**。最终归 CTO 收件闸 + K3 终审。

---

## §3 遗留清单 / 需 CTO 裁决（逐条，含证据）

| # | 事项 | 证据（原始输出摘要） | 请求 |
|---|---|---|---|
| R1 | **PR 数是活数，判据"全部 PR 有 label"在活仓库上不可达** | CTO 称 87；队长实测 04:00=90、04:24=**92**、执行时 **93**；执行窗口内 open PR 93→93 但期间新增 #840/#841/#842 未被分类件 as_of 覆盖 | 请 CTO **给一个冻结 as_of 断面**（或以"截至某时刻的 PR 集合"为判据分母），本队按断面收口 |
| R2 | **20 条 P0「禁动覆盖层」无 5 类标签** | 附录 D 明写这 20 条（#803 805 806 807 809 815 819 820 822 823 825 826 829 831 832 833 834 835 836 837）**不属于任何 5 类之一**（覆盖层不与 5 类并列）。执行者不臆造 ⇒ 仅对其中 19 条打了其 5 类标签? **否**：19 条中带 5 类的已打，余 20 条无 5 类可打 ⇒ 未打 | 是否兜底打 `积压:在制`（判据里 P5="判不准"）？一句确认即批量补打（可逆） |
| R3 | **#837 保守降级** | `#837 audit/k3-20260926-d979` **不在** CTO D979 链四号（#815 #819 #820 #823）内，但 ref 含 `d979`、附录 D 旧 P0 正则会命中它。按风险不对称**按禁动处理（未打 label、未评论）** | 若认为 #837 不属 D979 链，请指示，补 label + 评论 |
| R4 | **`/tmp` worktree 计数 24 未复现** | CTO 基线 in_tmp=24；快照实测 59（porcelain 含 `/tmp/`），候选口径 26/23/11/45 均非 24；独立自验员 7 种口径（0/60/60/37/11/12/131）亦无一得 24 | 请 CTO 给 **24 的逐字命令**；本队未改口径、未凑数 |
| R5 | **CI doc/chore 占比 56% vs CTO 52%** | `/actions/runs?per_page=100` @04:36:46：chore 54 / doc 1 / audit 1 / other 44 ⇒ **56.0%** | 请给 52% 的逐字命令 + 分母时点；1 次调用可复算对齐 |
| R6 | **撞号面远超 CTO 单点断言** | CTO 断言 `D1015 × 2`（已逐字复现且成立）；**PR 域独立复算得 9 组 / 31 refs**（真撞号候选 6 组 1015/788/790/804/964/979；非撞号 3 组 923/947/970 同 blob）。**域 B（全部远端分支）同判据 = 139 组 / 397 refs** ⇒ 撞号组数是**域内量**，非绝对量 | 请裁 B.4 全部 9 组；**先裁可一次性释放被覆盖层推迟的 14 条治理产物 PR** |
| R7 | **栈式 PR base** | 分类正文由 CTO 令"扩展同一份"落在 `阶段0-积压分类-20260926.md`，该文件属 D1011 / PR #834（**在禁动清单**）⇒ 本 PR 必须 base = `chore/D1011-stage0-backlog-classification` | 确认 base 声明；或指示改为其他载体 |
| R8 | **号段未取** | CTO 白名单禁写 `task-state/D10xx*` 与号段水位 ⇒ 本队未取号、未建 task-state 卡 | 请配号（D1017+；D1009–D1016 已占） |
| R9 | **冻结条件已消失（事实陈述）** | 队长 04:23:49 与成员 04:24:43 **两方独立复采**：主树 branch=`main`、`rev-list --left-right --count origin/main...HEAD` = `0 0`、冲突条目 = 0（`UU src/deploy/bootstrap.ts` 已消失）。**处置主体非本队**（本队全程未对主树做任何 git 写） | 请 CTO 确认安全网① 冻结令是否随条件消失而解除 |
| R10 | **并行 session 在冻结期内做过全局 fetch** | `.git/logs/refs/remotes/origin/chore/D1016-k3-input-repro` reflog：`t <t@t>` @epoch 1790366616（04:03:36）执行 `fetch -q origin +refs/heads/*:refs/remotes/origin/*`；这是 C16 从 460/3673 → 461/3681 的**唯一归因**（新增 `chore/D1016-k3-input-repro` +4、快进 `chore/D1012-stage0-selfverify` 30→34） | 知悉即可；本队未参与 |

---

## §4 前提实测（第一原则：派单件每条事实性前提开工前实测）

| CTO 派单前提 | 实测（原始输出） | 结论 |
|---|---|---|
| 主树 `UU src/deploy/bootstrap.ts` 未解 | 开工时 `git ls-files -u` 3 条 stage 记录 + `git status --porcelain` 有 `UU`；**04:23:49 复采冲突=0** | 开工成立；**已消失（R9）** |
| C16 = 458 分支 / 3,667 提交 | 口径 = **含未合并提交的远端分支数**（非分支总头数）。时间序列：D1011 457/3630@02:48 → 457/3644@02:58 → 队长 460/3673@04:00:40 → CTO 461/3681@04:04 → 快照 461/3681@04:05:09 → 自验 462/3685@04:17:37 → 执行器 464/3705@04:33:36 → 465/3712@04:36:46 | **成立（同口径，持续上升）**。⚠️ `git ls-remote --heads origin \| wc -l` = **782→783→785**（分支总头数），**与 C16 口径不同，不可混用** |
| 87 个 open PR | 90@04:00 → 92@04:24 → **93**@04:33；draft=1；created>3 天 31 条；base=main 78/92 | 数字漂移（活仓库），**见 R1** |
| C16 基线件 PR #834 | PR #834 = `chore/D1011-stage0-backlog-classification`，**state=open、merged=False**（未落 main） | **未落 main ⇒ 按铁律不可作为 main 权威件引用**；本队仅作只读参考 |
| 参考件 PR #822 | state=open、merged=False；派单 md 在 main 树为 `??` 未跟踪 | **未落 main** |
| 87 PR 全部有 label（真空白） | 开工时 90/90 已带 label = **0**；仓库 label 仅 9 个原生，`积压:*` 5 个全部 **HTTP 404** | 真空白；label 需新建（已建） |
| 写集自由 | `git ls-tree -r origin/main \| grep -E 'docs/synova/coordination/(积压快照\|积压分类)'` → **零命中（exit 1）** | 无冲突 |
| 与 D1011 是否重复派单 | D1011 分类对象 = **worktree**（221 行，口径 A 158 + detached 63）；`committerdate` 命中 0、`积压:*` 标签命中 0、逐 ref 全量快照无 | **互补而非重复**（本队 = 远端 ref 783 条 + 90 PR 口径 + 4 label + 5 label 定义） |

---

## §5 冲突扫描（M2）

```
① main 上我的写集前缀：grep -E 'docs/synova/coordination/(积压快照|积压分类)' → exit 1（零命中）
② 在制分支上的同域文件：
   chore/D1011-stage0-backlog-classification:
     docs/synova/coordination/阶段0-C16基线-20260926.json
     docs/synova/coordination/阶段0-积压分类-20260926.md   ← 本队"扩展同一份"的目标（CTO 令）
     .claude/task-briefs/2026-09-26-D1011-阶段0-③-积压分类与-C16-基线.md
```
- **同文件单写者**：分类正文写者 = pr-classifier（单写者）；快照 JSON 写者 = snapshot-writer（单写者）；两文件互斥
- 本队三个工作树均 **detached / 独立**，未复用他人工作树：`.synova-wt-backlog-a`（@origin/main）、`.synova-wt-backlog-b`（@origin/main，作废未删）、`.synova-wt-backlog-c`（@D1011 tip）、`.synova-wt-backlog-d`（交付）
- **不碰热文件**：`ci.yml` / `pre-commit-check.sh` / `ownership.yaml` / 号段水位 —— 全程零改动（`scripts/**` `.github/**` `src/**` `tests/**` `task-state/D10xx*` 亦零改动）

---

## §6 阶段B 执行记录（GitHub 侧，全可逆）

- 建 label：建前 `GET /labels/{name}` **404×5** → `POST /labels` **201×5**（`积压:可合` `积压:可关` `积压:在制` `积压:疑似撞号` `积压:可关（被取代）`）
- 写入：**70** 个 PR 打 label（HTTP **200×70**）+ **51** 条依据评论（HTTP **201×51**）；**失败 0 条**
- 禁动清单 live 命中 40 条：19 条仅 label（**零评论**）、21 条无 5 类分类 ⇒ 跳过登记（见 R2）
- 执行者自证 + 独立复核：repo 级 `issues/comments?since=` 返回 52 条，其中 1 条为 #838 作者并行 session 所发（非本队）⇒ **本队评论 ∩ 禁动清单 = ∅**
- 对账：open PR **93→93**；C16 `464/3705`→`465/3712`（@04:33:36→04:36:46）⇒ **提交数未因本队操作减少**
- **未调用任何 PATCH/PUT/DELETE**；未关 PR、未合并、未改标题正文、未删 label/评论

---

## §7 团队运行记录（squad-discipline §21）

| 成员 | 角色 | 状态 | 共享任务 |
|---|---|---|---|
| lead | 队长（协调 / 判前提 / 验证据） | running | 前提冻结、冲突扫描、M6 收口、CTO 报件 |
| snapshot-writer | 编码/执行 1（**非**分类员、**非**自验员） | inactive（待命） | task-1（rev1/rev5/rev9，completed rev3/rev7/rev11）；task-4 阶段B 执行（completed rev4） |
| pr-classifier | 编码/执行 2（分类员） | inactive | task-2（completed rev4） |
| independent-verifier | 自验（**非**分类员、**非**执行者） | running | task-3（completed rev4，`有保留通过`）；task-5（执行面复核，进行中） |

- **单一执行者**：关 PR 类动作只由标记执行一人做 ⇒ 本批**未关任何 PR**（阶段 D 不做；阶段 C 关闭须 7 天无异议 + CTO 批）
- **独立自验不兼任**：自验员 ≠ 分类员 ≠ 执行者
- **重型验证串行**：全程任一时刻 ≤1 个重型步骤

---

## §8 本批未做（红线内）

- **阶段 D（物理删除）：本批不做。** 建议观察两周后再议，且需四条件齐备（关闭≥7天 + 无独有提交或已 bundle 归档 + CTO 批 & K3 抽验 + 清单公示）
- **未关闭任何 PR、未删除任何分支 / worktree / 提交**、未 force push、未 `git gc --prune`、未 `rm`
- **未改产品代码 / 门禁脚本 / CI 配置 / 号段水位**（`scripts/**` `.github/**` `src/**` `tests/**` `task-state/D10xx*` 零改动）
- 未对主树做任何 git 写操作

---

## §9 第二轮（CTO 裁 R1–R10 后）· 增量记录

as_of：2026-09-26T11:5x+0800

### §9.1 R2+R3 补打 label ⇒ 全覆盖（task-6，执行者 snapshot-writer）
```
【冻结断面】as_of 2026-09-26T04:04:28+0800（refs 783；open PR 分母 90@04:00）
  附录 D 表内 90 条中 7 条现已不在 open（#780–#785 关闭、#836 合并）
  仍 open 83 条，带 `积压:*` 83 条 ⇒ 断面存活部分覆盖率 = 83/83 = 100.0%
【活数】as_of 2026-09-26T11:54:56+0800
  open PR 91 ｜ 带 `积压:*` 91 ｜ 无标签 [] ⇒ 覆盖率 91/91 = 100.0%
  分布：可合 22 ｜ 在制 53 ｜ 疑似撞号 14 ｜ 可关（被取代）1 ｜ 在制（D979 链）1 = 91
```
- `积压:在制（D979 链）`：GET **404** → POST **201**；补打 **#837**
- R2：18 条 P0 覆盖层 → `积压:在制`（第 20 条 #836 已不在 open）
- 新 PR #840–#847 现场判类：`可合` = 840/841/842/844/847；`在制` = 843（栈式 base≠main）、845（含 `tests/output/**`）、846（触 `.github/workflows/ci.yml`）
- 27 label 全 **HTTP 200**、6 评论全 **201**、失败 **0**；禁动 PR **零评论**（独立复核：期间 6 条新评论全部为本队所发，∩ 禁动清单 = ∅）
- 对账：open PR 91→91；C16 **468/3744 → 468/3744**（差 0/0）⇒ 提交数未减少

### §9.2 PR #843 栈式登记（task-6）
正文 2219 → 2594 字符；**title / state / base 均未变**；`PATCH` 只带 `body` 一个键。
```
#834 head ec73dc366764 → check-runs 13 项，conclusions={"success":13}，失败 job = []
#843 head 7be83742b5ed → check-runs total_count = 0
  判别性交叉：actions/runs?head_sha=7be83742… = 0 ；commits/{sha}/status = pending/0
  ⇒ #843 head 未触发任何 workflow（非失败、非排队）
```
⚠️ **需 CTO 决定 #843 是否重跑 CI**（本队未获授权触发，未执行任何触发动作）。

### §9.3 附录 E（task-7，分类员 pr-classifier）
`--numstat` = **587 插入 / 0 删除**；既有 1020 行 sha256 逐字节未变（`936bab95…`）；附录 A–E 各 1 处无重号。
- **R6 九组撞号定性**：真撞号 **6** 组（1015/788/790/804/964/979）、非撞号 **3** 组（923/947/970）
- **释放清单事实修正（重要）**：声称的 14 条中 **5 条（#780/782/783/784/785）已 closed 未合并**且内容由 **#844** 承接 ⇒ **实际可释放 = 9 条**（#589 #602 #623 #723 #742 #809 #837 #838 #839）
- **阶段C 可合件 CI 三分类**（样本 22，**只取 success 样本**）：**绿 10 / 无 run 5 / 红 7**
  - 无 run = #572 #756 #757 #761 #810 —— **绝不当绿**
  - 红 = #584 #615 #616 #618 #808 #844 #847（#847 **8 个红 job**，含 `Golden Case F1 Gate`、`Vitest (1/2)(2/2)`、`Checker Review`）
  - **K3 必送 = 0 条**（样本 22 条全为纯 docs/治理产物；触 `scripts/**` `.github/**` `src/**` `tests/**` 的 PR 已先判 `在制`、不进本样本）
- **合并顺序 24 步**：`1 #834（栈底）→ 2 #843（栈顶，校验 #843.base == #834.head_ref = True）` → 3–12 绿腿 → 13–17 无 run（先补跑）→ 18–24 红腿（先修 job）
- **真实冲突面**：`.claude/bypass.log` 被几乎所有可合件共同修改 ⇒ **绿腿须串行合 + 逐条 rebase**
- 在制件按属主分组（口径 F，32 条）：CTO/其他 17 ｜ 门禁线 6 ｜ dependabot 5 ｜ 文档/审计线 3 ｜ Mac/Win 1（合计 32 ✅）

### §9.4 独立自验（第三轮，task-5 已回）
执行面复核 `自验结论：有保留通过`（详见 `/tmp/verify-task4-execution-20260926.md`）：
- **禁动 40 条本队评论 = 0/40（不退回）**；label 70 条分布与自报及附录 D 逐项相等
- **零破坏分两段**：本队窗口（20:33:50–20:36:41Z）**成立**（0 关闭/0 合并/0 unlabeled/0 标题改）；**截至复核 as_of 不成立** —— 窗口后 5.5–8 分钟出现 **7 起他方事件**（#780–#785 CLOSED、#836 MERGED），归因指向 **#844 D964 归档流** 与门禁线 D1014，**API 单账号不可区分责任主体 ⇒ 请 CTO 归责**
- 保留项另含：`#838` PR 正文被**他方**改动（3666→5686 字符）⇒「零正文改动」作绝对命题不成立；CI 指标无稳定窗口

### §9.5 主树冻结令（R9）
CTO **暂不解除**：解除条件 = `branch=main` + `ahead=0` + 无 UU/M（behind ≤1）。
```
$ git -C /Users/wane/SynovaAgent branch --show-current   → main
$ git -C /Users/wane/SynovaAgent rev-list --left-right --count origin/main...HEAD → 1	0
$ git -C /Users/wane/SynovaAgent status --porcelain      → 5 条 ??（无 UU/M）
```
⇒ **ahead=1，条件未满足，冻结令继续有效**（与 CTO 读数一致）。
