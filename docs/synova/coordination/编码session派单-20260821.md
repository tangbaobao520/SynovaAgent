# 编码 session 派单（2026-08-21，CTO 下发）

> 认领角色：🛠 synova-dsh（编码 session）
> 原则：部署轨优先；场景脚本 = 产品线交付，进审计无豁免（诚实 RED 可接受，假绿是事故）

---

## 任务 1（P0 最高优先）：D454 — GSS 服务启动原生崩溃修复

**问题**（task-state 已记）：
- Node v24.19.0 断言 `env!=nullptr`（哨兵动态 import 时触发）
- `require is not defined`（ESM 迁移残留，`src/init/engine-context.ts`）
- nodemailer 缺失（已补装）

**为什么 P0**：这是 D442 GS-03 转绿的前置之一，阻塞部署轨。

**⚠️ 写集核对**：`src/init/` 可能归 Win Claude（诊断体系 L1-L5），开工前先 `grep` 确认归属，与 Win 写集重叠就停手回来找我。

**验收**：`npm run dev` 服务能正常启动（无 env 断言崩溃 + 无 require is not defined），GS-03 场景可跑。

---

## 任务 2-5（P1）：GS-01/06/07/08 场景脚本

| 任务 | 场景 | 前置依赖（先确认，缺则回报不硬做） |
|---|---|---|
| D446 | GS-01 首诊旅程（问卷 → 首诊报告 ≤3 天路径） | D232/D233 Electron 一体化 |
| D447 | GS-06 进化闭环（反馈注入 → loop-3/5 真实执行） | D333 loop-3/5 真实执行体 |
| D448 | GS-07 数据安全（敏感数据 → PII 脱敏 + 越权拒绝） | D338 orgId + security/ |
| D449 | GS-08 报告可读（GS-01 产物 → 一页纸 + 移动端） | 报告模板（K3 定稿） |

**规格**：每个场景按 `scripts/golden-scenarios/` 既有规范（common/ 四工具 + expect.json ≥3 断言 + evidence JSON），参照已落地的 GS-02/03/04/05 结构。

**诚实 RED**：前置未就绪时，允许"诚实 RED 2/3"交付（暴露真实缺口），但**禁止假绿**。

**验收**：场景脚本 + evidence JSON 进 git，机器判定 exit 0/1，进审计无豁免。

---

## 开工前必读

1. `git fetch --all && git checkout main && git pull --ff-only`（main 已到 V4.9.0）
2. 读 `docs/synova/research/DSH迁移施工图-20260820/DSH迁移施工图-20260820.md` §6 Track A（部署轨优先）
3. 走 `bash scripts/control-tower/alloc-task-id.sh` 取号（如需新任务）
