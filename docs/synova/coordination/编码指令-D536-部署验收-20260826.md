# 编码指令-D536-部署验收-20260826

> 给编码 session（🛠 实测执行）的启动指令。**先读文档，后动手。** 违反本指令硬约束 = 审计 FAIL。

## 1. 任务文档表（先读后动）

| 文档 | 路径 | 作用 |
|------|------|------|
| **spec（唯一契约）** | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D536-deploy-acceptance-20260826.md`（commit 39a45814） | §1 Authority / §4 Current State（artifact API 实测、脚本行号、LLM 注入链路）/ §5 写集表 / §7 测试 / §10 DS1-7——照此执行 |
| 派单 | `docs/synova/coordination/派单-部署轨-D536-20260826.md` | 5 必答题 + 验收 + 写集约束 |
| 北星 | `.claude/PRODUCT-BRIEF.md` §二/§六 + 施工图 §6 Track A L291-298 | 部署轨最高优先级；部署前只动文档与配置 |
| 前车之鉴 | D510 F1（禁静态 grep 冒充实测）/ D523 DS4（无目标机 waiting 不伪造）/ D528 DS2（无 dmg 不伪造）/ D519 实跑踩坑（ELECTRON_RUN_AS_NODE unset、dmg 卷名解析） | 全部写进 spec §1/§4 |

## 2. 执行要求

1. **认真阅读** spec §1 Authority / §4 Current State / §5 写集 / §7 测试 / §10 DS 后再动手。
2. 做到你的**最高代码水平**（脚本改动：as any=0 铁律 38 / 契约优先 JSDoc 铁律 47 / 降级诚实 log+degraded 铁律 24+31）。
3. 任务复杂 → 先 plan mode 列改动清单再执行。先想清楚再动手。
4. **基线核验（铁律 0-3）**：开工前 `git fetch --all && git pull --ff-only`；spec 写于 `origin/main @ f341c43d`——若 main 已前进，**重新核验 spec 引用的行号/脚本内容**（防 M7 漂移，D524 教训）。

## 3. 任务专属硬约束（违反 = 审计 FAIL）

- **前置依赖**：
  1. CI artifact（run 32870900391）API 实测 expired:False——下载用 `~/.dsh/.credentials.yaml` 的 `GITHUB_TOKEN`（**token 值禁止落盘/进 commit/进 evidence**）。
  2. Win 实测需 **GUI dsh-ssh 已配置 Windows 目标机**（`ssh_list` 可见）——**未配置 → Win 侧如实 waiting + evidence 标注（D523 DS4），Mac 侧照常完成**；禁伪造 Win 实测。
  3. 首诊 LLM key：`.env` 有 `LLM_API_KEY`（src/config.ts:72 读取）；**open 启动不继承 shell env**——首诊实测用「直接执行二进制」（`$INSTALLED_APP/Contents/MacOS/SynovaAgent`）或 `launchctl setenv LLM_API_KEY <值>`，evidence 记录注入方式；无 key → GS-01 如实 RED，安装/启动/数据段仍须绿。
- **写集精确性**：只改 spec §5.1 写集表 6 文件（4 修改 + 2 新建）：
  - 修改：`docs/synova/runbooks/founder-demo-mac.md`、`docs/synova/runbooks/founder-demo-win.md`、`scripts/desktop/mac-install-verify.sh`（仅实测暴露缺口时）、`scripts/desktop/win-install-verify.ps1`（仅实测暴露缺口时）
  - 新建：`docs/synova/runbooks/desktop-deploy-acceptance.md`
  - task-state/D536.json impl 段回填
  - `git diff --name-only` 与实际改动完全一致；**不碰** src/、scripts/audit/、build-synova.cjs
- **证据落盘**：`evidence/D536-artifacts-<date>/`（下载日志 + md5.txt）、`evidence/D536-mac-<date>/`（断言原文）、`evidence/D536-win-<date>/`（回传）；关键摘录回填 task-state/D536.json impl 段。
- **环境坑**：宿主 `ELECTRON_RUN_AS_NODE` 若为 1 → 脚本已显式 unset；dmg 卷名含版本+arch → 挂载点从 mount.log 解析（mac-install-verify.sh 已处理）。

## 4. 复核清单（做完逐项自查，K3 会盯着）

- [ ] **与 dev doc 一致**：spec §10 DS1-7 逐项对照（✅/⏸/❌+理由），禁 overclaim（S-2）——特别：**"装上了"必须有进程/窗口/healthz/数据物理断言**，禁止"下载了 artifact"冒充
- [ ] **接线完整**：spec §8 每条 grep 真实生产调用点（founder-demo checklist 引用脚本 + desktop-deploy-acceptance.md 引用 + task-state 回填）——测试调用不计（S-3）
- [ ] **降级诚实**：Win 无目标机 → waiting 标注；无 LLM key → GS-01 如实 RED（铁律 24+31）
- [ ] **验证执行**：spec §7 verify 命令逐条跑通（artifact 下载 curl / mac 全链 3 脚本 / win 远程 / vitest 回归）
- [ ] **脚本契约**：复用脚本 exit 语义 0/1/2 不变；微调（若有）须 JSDoc 契约头 + 回填 spec §5.2
- [ ] **提交纪律**：synova-commit（禁 git stash，铁律 0-3）；pre-commit 全过（禁 --no-verify）；逐项 commit
- [ ] **残留清理**：死代码 grep 零；临时下载的 zip/dmg 不入库（release/ evidence/ gitignore）

## 5. 审计提示

- 提审口径：D536 一次提审（部署轨切片）；K3 按 `desktop-deploy-acceptance.md` 独立重跑复核。
- task-state/D536.json：impl 段回填（commit + evidence 摘录 + verdict）+ slice=deploy-acceptance 保留。
- 已知限制如实声明（签名/公证未做、Win waiting 若发生）——**不伪造**。

开始吧。
