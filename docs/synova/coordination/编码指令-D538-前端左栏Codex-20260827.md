# 编码指令 — D538 前端交互实现：左栏 Codex 风格（产品独有能力导航）

> 派单: CTO | 2026-08-27 | 给: 🛠 编码 session（Mac DSH 线，electron-renderer/ 归它）
> 前置: 认领本指令前确认 task-state/D538.json = spec_done（本 spec 已 gatekeeper ALL PASS）
> 与设计差异（本指令源头）：本指令下方 "⚠️ 修正确认" 是最新真相，**以 spec 为准，覆盖你看到的任何旧文案/设计规格 §4.2 建议形状**。

---

## 1. 任务文档表（先读后动，一条不落）

| 文档 | 路径 | 作用 |
|---|---|---|
| **spec（编码唯一契约）** | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D538-frontend-leftbar-codex-20260827.md` | 本指令的唯一事实源。§5 写集 / §7 测试 / §8 接线 / §10 验收 |
| 派单 | `docs/synova/coordination/派单-D538-前端左栏Codex-20260826.md` | 写集红线 + 接线修正 + 验收 8 条 |
| 设计规格 v1（Win 侧推送，创始人确认） | `docs/synova/coordination/SYNOVA-IMPL-DSH-前端交互设计-左栏Codex风格-v1.md` | 交互样貌 + §六 8 条可证伪验收（已合 main） |
| 北星 | `.claude/PRODUCT-BRIEF.md` §二（GA 用户）+ §四（L1 交互层）+ §五（桌面端品牌表层"能开/能用"） | 方向锚定 |
| 前车之鉴 | `docs/plans/codex/implementation/SYNOVA-IMPL-D352-resolver硬化-20260813.md`（结构范例）+ AGENTS.md 铁律 0-2/8/24/31/38/39/47/48 | 结构 + 质量铁律 |

## 2. 执行要求

1. **认真阅读** spec 关键节：§1 Authority / §4 Current State（真实接口形状，逐个 read 源码已核，勿再凭旧文案）/ §5 写集（7 条目）/ §7 测试（red→green 三路径）/ §8 接线（每条 grep 出真实生产调用点）/ §10 验收（8 条逐条给可证伪判据）。
2. 任务较复杂（4 组件 + 状态机 + 3 接口 + 样式 + 测试）→ **先用 plan mode** 列改动清单（文件→改什么→测试→验证）再动手。
3. **最高代码水平**：`as any`=0（铁律 38）；契约优先 JSDoc（铁律 47）；降级诚实 `console.warn/error` + `degraded` 标志（铁律 24/31）；测试非空壳三路径 + expect（铁律 48）。

## 3. 任务专属硬约束（本任务特有，违反 = 审计 FAIL）

- **⚠️ 修正确认（本次必须按 spec 执行，勿照旧文案）**：
  1. 主动触达真实路径是 **`GET /api/sentinel/signals`**（**不是**派单写的 `GET /api/signals`——少了 `/sentinel` 前缀，已修正）。
  2. 五循环是 **6 个 loop**（loop-1..6），前端**按 `loops.length` 动态渲染**，**禁止硬编码 5 或 6**（设计"5 循环"是伪信息）。
  3. Action 实响应 shape **无** `targetMetric/currentValue/tolerance/deadline/responsibleRole`，按真实 shape（`title/description/status/priority/owner`）渲染。
  4. **GA 校准接口不存在**（`/api/ga/calibration`），GA 项置灰/结构占位，**不伪造接口**；GA 详情**不发 fetch**。
- **依赖前置 + 基线**：`git fetch --all && git pull --ff-only`；`electron-renderer/` 在 main 树**未装依赖**（无 node_modules），lucide-react 未装 → `cd electron-renderer && npm i lucide-react`（+ 如需跑测试另装，见下）。
- **写集精确性**：只改 spec §5.1 写集表 7 条目文件；`git diff --name-only` 与实际改动完全一致；禁"树终验声称不符"。
- **诚实 RED**：Renderer 主树无 zustand/jsdom/testing-library（仅 root 有 react）。**测试用 spec §5.1 的纯逻辑 `capability.ts`（零 zustand/react/lucide 依赖）走根 vitest node env**（`tests/electron/capability.test.ts`，include `./tests/**/*.test.ts`）。**不要**尝试挂载 React DOM 组件测试（需 jsdom+testing-library+renderer vitest config，属另一基建任务，spec §7.4 已 descope，违者按 over-claim 审计）。
- **接线红线**（spec §8）：每个新 export 必须 ≥1 **生产**调用点（测试调用不计，S-3）。`toggleCap`/`canAccessCap`/`selectedCap`/`.cap-*`/3 详情组件都要在 `LeftPanel.tsx`/`RightPanel.tsx` 真实出现。
- **红线**：不碰 `src/`（后端只读消费）、不碰 `scripts/audit/`、不碰 `App.tsx`/`TitleBar.tsx`/其它 renderer 组件、不碰根 `vitest.config.ts`。
- **环境坑**：`computeNextTrigger`（loops.ts:62）为简化推算；loop `status` 值为 `'completed'|'failed'|'degraded'|'pending'`，状态灯映射见 spec §4.2（完成→绿 / 失败→红 / 降级→橙 / 其它→灰）。

## 4. 复核清单（做完逐项自查，K3 会盯着）

- [ ] 与 dev doc 一致：spec §10 验收 8 条逐条对照（真实数据渲染 / 取消选中 / GA 置灰 / 折叠态 / as any=0 / 术语 GA）
- [ ] 铁律：接线完整（spec §8 逐条 grep 出生产调用点）、降级诚实（24+31，各 fetch catch console.warn + degraded + 错误 UI）、类型安全（38 零 as any）、契约优先（47）、测试非空壳（48）、架构边界（39，纯前端）。
- [ ] 无 bug：`npm run test`（根 vitest node env）新增 `tests/electron/capability.test.ts` 通过 + 全量 vitest 无新失败 + `tsc`（electron-renderer）通过 + pre-commit 全过（禁 --no-verify）+ `synova-commit`（禁 git stash，铁律 0-3）。
- [ ] 接线完整：spec §8 每条 grep 真实生产调用点。
- [ ] 测试到位：red→green 已证（先写 `capability.test.ts` 现状未实现必失败 → 实现 → 全绿）；三路径（正常/降级/边界）；expect 非空壳。
- [ ] 其他：残留清理（emoji 图标 🔍💬📁🔔 删除后 grep 零引用）、无死代码、`as any` 零、GA 项无伪造接口（grep `api/ga/calibration` 零结果）。

## 5. 审计提示

- **提审口径**：一并覆盖 D538（前端左栏 Codex 风格 + 右栏联动）。
- **验证点收口**：设计规格 §六 8 条 + spec §10 接线断言收口（8/8）。
- **task-state 回填**：实现提交后更新 `task-state/D538.json` impl 段（`{commit, by, at, files[]}`）+ status→`impl_done`（D382 状态机，见 task-state/README.md）。
- **脚本可复现**：审计员独立重跑 `npm run test` + 接线 grep（spec §8），禁仅 task-state 单副本声称。

## 6. 结束

开始吧。
