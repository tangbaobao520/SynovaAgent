# 编码指令 — D556 GA 人机协同端到端（ga-collab-e2e）

> 交付: DeepSeek Harness · dev-doc | 2026-08-29 | 随 spec 交付，供编码 session 启动

---

## 1. 任务文档表（先读后动，顺序即优先级）

| 文档 | 路径 | 作用 |
|---|---|---|
| **实现 spec（编码唯一契约）** | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D556-ga-collab-e2e-20260829.md` | §5 端点映射+降级渲染 / §6 层 2 动作映射 / §7 角色来源+seed / §8 UI 断言方案 / §9 接线 / §13 DS1-DS9 |
| 派单 D556 | 创始人 DSH 派单（2026-08-29） | 四必答/写集/验收 |
| D551 spec（后端契约权威，已合 main） | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D551-ga-calibration-backend-20260828.md` | §8.2 四端点契约 + §6 数据模型 |
| Module-3 蓝图 | `docs/synova/research/权威文档05-Agent主动交互系统蓝图-20260710/SYNOVA-RESEARCH-Module-3-GA人机协同与反馈闭环-20260710.html` | 校准四操作/信号五要素 |
| 北星 | `.claude/PRODUCT-BRIEF.md` §二/§三.1 | 方向锚 |
| 铁律 | `AGENTS.md` 0-2/24/31/38/39/47/48 | 质量底线 |

## 2. 执行要求

认真阅读任务文档，然后执行任务。做到你的最高代码水平。任务复杂 → 先用 plan mode 做好计划再执行；先想清楚再动手（禁止没想清楚就改代码）。做完复核：与 dev doc 一致 / 不违反铁律 / 无 bug / 接线完整 / 测试到位 / 其他你认为需要复核的点。Kimi K3 会盯着你的任务，也会做最后的审计。

## 3. 任务专属硬约束（违反 = 审计 FAIL）

1. **基线与行号（M7 防漂移）**: 基线 = **origin/main @ 0010eefb**。开工先 `git fetch origin` 核对；抽验 3 处（RightPanel.tsx L632-640 占位 / ga-calibration.ts L121 POST / middle-evolution-engine.ts L78-79 白名单），漂移先对齐 spec 再动手。
2. **工作区**: `git fetch origin && git worktree add -b feat/d556-ga-collab-e2e .wt-d556 origin/main && cd .wt-d556`。禁 git stash（0-3）；禁直推 main。
3. **写集（文件级，spec §3.3.1——8 文件）**: 修改 RightPanel.tsx / app-store.ts / middle-evolution-engine.ts；新建 stores/ga-collab.ts + test-support/render.ts + tests/ga-collab-logic.test.ts + tests/ga-collab-ui.test.ts + tests/loops/ga-calibration-evolution.test.ts。**spec 文件随编码首个 commit 同批提交**（消解 8 条预期漂移——spec §3.3.1 提交策略）。收工 `git diff --name-only origin/main..HEAD` ⊆ 写集 + spec 文件。
4. **禁碰清单**: src/routes/**（D551 交付面只读）；src/growth/feedback-collector.ts 与 src/agent/loop-handlers.ts（回流上游只读）；src/middleware/auth.ts（D483 在途）；capability.ts / App.tsx / electron-renderer/package.json / vitest.config.ts / 根 package.json（零新依赖红线）；scripts/**（治理冻结）。
5. **防膨胀红线**: 层 2 只追加不修改——引擎三既有信号类（threshold_adjust/goal_formula_tweak/path_rank_downgrade）语义零变化（DS6 diff 审阅）；回流只走 feedback_log→processFeedbackSignals→applyEvolutionActions 既有管线，**禁第二套进化机制**；零新依赖（renderToStaticMarkup 桥接是 UI 断言成立的前提，test-support 仅 tests 引用，生产 bundle 零影响）。
6. **诚实声明传播（K3 首查项）**: 回流收口 = 进化动作生成 + agent_memory 审核条目；**"权重自动更新/采纳率/诊断变好"不在本单**（层 3 descope；采纳率无数据源——stats note 原文透传 UI）。PR 描述与交付声明必须携带此边界。
7. **诚实 RED（S-5）**: ①漏 requireGa 防御 → blocked 渲染断言红；②占位文案未删 → 零残留断言红；③层 2 漏新信号类 → 3 条回流动作断言红；④旁路写事件/跳过 applyEvolutionActions → applied 计数断言红。禁伪造 red/绿。
8. **evidence 落盘**: 三层测试输出 + §9 九条 grep 输出 + E2E 物理 walkthrough（seed→面板→POST 回显→注入→计数变化，截图）+ CI check-runs → evidence/D556/ + PR 描述。
9. **环境坑**: 本机 BSD grep 无 `-P`；root 有 react 无 react-dom——renderToStaticMarkup 必须经 electron-renderer/src/test-support/render.ts 桥接（react-dom 从 renderer node_modules 解析）；engine 测试对齐 tests/loops/ga-correction-feedback.test.ts（真实 fs + tmp 清理 + 阈值公式断言）；seed 身份走 legacy x-synova-token（auth.ts L366-376 既有兼容面，格式 `ga:default:ga-seed-1`，不改 middleware）。

## 4. 复核清单（做完逐项自查）

- [ ] DS1-DS9（spec §13）逐项对照，禁重编号/跳号/静默缺项（S-10）
- [ ] DS2 数据层: stores/ga-collab.ts 状态机/降级决策/请求构建/seed + tests/ga-collab-logic.test.ts 绿
- [ ] DS3 UI 断言: GaDetailSections 五场景 renderToStaticMarkup 全绿（含占位零残留 + 降级条 + role 防御空态）
- [ ] DS4 seed: app-store boot seed + apiFetch legacy 头（无 seed 时行为与现状完全一致）
- [ ] DS5 层 2: 新动作类型 + Signal 5 + applier + case；3 条回流 → 3 动作 + sink 写入 + count=2 零动作阈值断言
- [ ] DS6 存量零回改: §9 断言 + 引擎三既有信号类 diff 只增不改
- [ ] DS7 E2E 物理 walkthrough evidence 落盘
- [ ] DS8 as any=0 / 每 fetch catch console.warn+degraded UI（24/31）/ tsc 零新增
- [ ] DS9 CI 三 job 全 success 贴结果 + Architecture Check 绿（本地绿不算——D540 教训）
- [ ] spec 文件已随首个 commit 同批提交（写集漂移归零）；task-state/D556.json impl 段回填 + status=impl_done

## 5. 审计提示

- 提审覆盖 **D556（前端接线 + 层 2）**；K3 重点: evidence/D556/ 独立重跑三层测试 + 五场景 UI 断言 + 层 2 动作生成（3 条回流 → 3 动作）+ 诚实边界未被越界（层 3/采纳率仍是 descope——K3 首查项）。
- 验证点收口: 派单四必答从 spec 答案收口为 evidence 实测；S5-2 层 2 断裂从"记录"收口为"动作生成"。
- 与 D483-D486 关系: seed 走 legacy 通道零耦合；D483-D486 落地后真实 JWT 替代 seed（seed 仅 localStorage dev 语义）。

---

开始吧。
