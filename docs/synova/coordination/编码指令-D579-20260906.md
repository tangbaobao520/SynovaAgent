# 编码指令 — k3 verdict stale/TTL 机制 + 批次审计报告派生修正（D579）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/派单-D579-k3verdict-stale机制-20260905.md（CTO 派单）
> 审计: Kimi K3 会盯着你的任务，D579 完成后做最终审计（与 D578 同批或随后单审，覆盖 CT-55/CT-58 闭环）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D579 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D579-k3-verdict-stale-20260905.md | A 项 k3 失效门 + B 项 audit.report 权威派生——**编码唯一契约**（gatekeeper 6/6 ALL PASS，2026-09-06） |
| 派单 | docs/synova/coordination/派单-D579-k3verdict-stale机制-20260905.md | 任务定义 A/B / spec 必答 4 题 / 写集约束 / 验收 4 项 |
| 北星 | .claude/PRODUCT-BRIEF.md §一 + §七第 4 条 | 仪表盘失真=自欺；spec front-matter 北星块已锚定 |
| 前车之鉴 | docs/synova/audit-reports/2026-09-04-D572-line1-desktop.md（P1-1/G2/P1-3 原文）+ memory/notes/implemented/2026-09-04-d576-redeem-honesty-ct53-ct54.md（CT-53 同域先例） | 缺口出处 + mini yaml 测试模式 + 写集纪律 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority 原文引用）/ §3.6（spec 必答 4 题——四项机制决策已定，不得翻案除非 CTO 仲裁）/ §4（Current State 实测行号）/ §5.1（写集表）/ §5.2（契约 docstring 先行）/ §7（red→green 表）/ §10（DS1-DS11）——spec 是唯一契约，声称即引用。
2. **任务中等复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 → 列出你的改动清单（文件级）→ 确认基线（见 §三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：契约优先（`freshness_gate` / `resolve_audit_report` 先按 spec §5.2 docstring 落地再写调用，铁律 47）、降级诚实（problems 显式登记，铁律 24/31，禁静默）、测试非空壳（expect 断言 + 正常/降级/边界三路径 + red→green 留证，铁律 48）。

## 三、D579 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 开工前置（铁律 0-3）：`git fetch --all && git pull --ff-only`；在 **feat/d579-k3-verdict-stale** 分支上工作（禁直接 main commit，铁律 34）。
   - 基线 = D576 已合入 main（e9da5123）。**合并前重新核验 spec §4 全部行号**（calc-progress.py L145-179 / gen-cto-health.py L239-291 / product-lines.test.py L168/L233/L266）——若 main 有新合并，行号必漂移，照旧行号写测试会红（D524 教训）。
   - 本任务零外部依赖（无 LLM/无上游产物），无 waiting 场景；唯一前置 = CTO 对 §5.1 写集扩围申报的裁决（gen-cto-health.py 两文件）。
2. **写集精确性（S-2/S-6）**：只改 spec §5.1 写集表列出的文件（5 修改 + 7 新建）；`git diff --name-only` 与实际改动完全一致。**CTO 未批扩围 → B 项整体 descope**（gen-cto-health.py + gen-cto-health-batch-report.test.py 不碰，DS7 改标 ⏸ + 理由，A 项照常交付）。product-progress.json / product-progress.html 等派生产物**不入本 PR**（D576 先例 e9da5123，由 refresh-all/CI 常规链再生），其变化以 evidence diff 文件留证。
3. **诚实 RED / 基线不藏**：product-lines.test.py 当前 **28 用例 3 既有失败**（spec §4.3 实测：range_expansion / capital_line_zero / page_no_jargon）——**不修、不藏、不扩大**，回归门 = 其余 25 条绿 + 本单波及用例更新后绿。修复前先跑新测试留 red 输出（evidence/test-output.md 含 red、green 两轮）。禁伪造绿、禁把既有失败算到自己头上或悄悄顺手修。
4. **evidence 落盘规范（K3 独立重跑可复现）**：`docs/synova/audit-reports/D579-k3-verdict-stale-evidence-20260906/`（git 跟踪路径；根级 evidence/ 被 .gitignore 禁用）落两份：calc-diff.md（修复前后线 1/7-1/9-2/11-1/11-2/18-1/10-4/19-2/20-5/22-1/24-4/1-8 逐点对照 + 命令原文 + 运行日期）、test-output.md（red/green 两轮全文）。禁只在 task-state 存单一副本。
5. **红线（违反 = 事故）**：不碰 `scripts/audit/`（K3 专属）；不碰 `product-lines.yaml`、`src/` 全目录、`electron-renderer/`、pre-commit 门禁脚本；**禁手工修改 task-state/D517.json、D518.json、D519.json 的 audit 字段**（CT-58 原文"禁手工改派生字段"——B 项机制生效后派生自动可见）；task-state/D579.json 只允许回填 spec/impl 段。
6. **环境坑（本机实测注记）**：① macOS BSD grep 无 `-P` → dev-doc-gatekeeper C2 在本机静默降级，CI（GNU grep）才真执行——spec 已按 GNU 语义做过等价自检（5 个 src/ token 全部存在），你改动 spec 无效，但若你新建文档引用 src/ 路径须注意 token 后不能紧跟反引号/中文标点；② 本任务测试是 **Python unittest + bash**（`python3 tests/control-tower/product-lines.test.py` / `bash tests/control-tower/redeem-task-redeem.test.sh`），不经 vitest；③ TTL 用例必须用 `datetime.now()` 相对日期构造（D576 教训——固定日期 2026-08-13 已墙钟漂移）；④ calc-progress 幂等（L298-310）与 gen-cto-health 指纹幂等（D384）——验证脚本必须可重复跑。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：DS1-DS11（spec §10）逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim、禁重编号/跳号/静默缺项（S-10）；四项机制决策（spec §5.3）不得静默偏离。
2. **不违反铁律**：接线完整（freshness_gate 有两个 k3 出口 + refresh-all.sh L48/L55 生产链；resolve_audit_report 有 analyze_task_state 循环 + pre-audit-summary.sh L48 消费链——**grep 实测，测试调用不计**，S-3）、降级诚实（铁律 24/31）、契约优先（铁律 47）、测试非空壳（铁律 48）。
3. **无 bug**：spec §7 用例逐条跑通 + `python3 tests/control-tower/product-lines.test.py`（25/28 绿 + 3 既有失败不变）+ `bash tests/control-tower/redeem-task-redeem.test.sh` 5/5 + `bash tests/control-tower/alloc-task-id.test.sh` 13/13 + `bash tests/control-tower/gen-cto-health.test.sh` 幂等语义不变 + pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 两条生产链逐条 grep 出真实调用点（A4 calc → CI product-progress.yml；gen-cto-health → U3 门禁）。
5. **测试到位**：red→green 已证（evidence 双轮输出）、覆盖正常/降级/边界、expect 断言非空壳。
6. **其他**：残留清理（无死代码/无旧引用）；幂等复跑一致（calc 两次运行第二次"进度无变化"）；真数据对账矩阵与 spec §3.6 必答 4 完全一致（8 stale / 2 保持 / 线 1 零变化）；开工先跑 task-start（brief: .claude/task-briefs/2026-09-05-D579-k3-verdict-stale.md，写集约束从 spec §5.1 抄入 Q2）。

## 五、K3 审计提示（收尾要求）

- D579 完成后**单任务提审**（K3 审 D579；可与 D578 同批判次但报告分列）。审计焦点预判：① k3 fail → rejected 不失效（负向语义）；② 降级三场景显式留痕（不静默）；③ 真数据 8/2/线1 矩阵与 evidence diff 一致；④ CT-58"禁手工改派生字段"零触碰（git diff 可验）；⑤ 写集与扩围批准状态对账。
- 完成后回填 `task-state/D579.json` **impl 段**（commit + by + files[] + DS 逐项状态 ✅/⏸/❌+理由）+ evidence 路径；spec 段（dev-doc 已填）不得改写。
- 审计员会**独立重跑**：`python3 scripts/product-lines/calc-progress.py --out <tmp>`（对照 evidence/calc-diff.md）+ `python3 scripts/control-tower/gen-cto-health.py`（grep D517/D518/D519 行 CONDITIONAL_PASS）+ 两个新测试文件。脚本必须幂等、可复现、无本机假设。
- VERSION.md V4.9.1 + memory/notes/proposed/2026-09-06-d579-k3-stale-wiring-ct55-ct58.md 四态 Note 随 impl commit（铁律 49 commit-msg 物理门禁）。

**开始吧。**
