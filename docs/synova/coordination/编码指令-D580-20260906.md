# 编码指令 — 告警工单切片：8-2 入库 / 8-3 去重稳定化 / 8-4 状态机 API（D580）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/派单-D580-告警工单切片-20260906.md（CTO 派单）
> 审计: Kimi K3 会盯着你的任务，D580 完成后做最终审计（单任务提审，覆盖 8-2/8-3/8-4 三点 + 波及面红线执行）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D580 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D580-ticket-slice-20260906.md | 三点切片 + 必答 4 题裁决 + 写集——**编码唯一契约**（gatekeeper 6/6 ALL PASS，2026-09-06） |
| 派单 | docs/synova/coordination/派单-D580-告警工单切片-20260906.md | 任务定义 A/B/C / spec 必答 4 题 / 写集约束 / 波及面红线 / 验收 5 项 |
| 北星 | .claude/PRODUCT-BRIEF.md §三-2 + §二 | 「哨兵定时巡检 → 严重信号自动建工单」+ 直接用户 = GA；spec front-matter 北星块已锚定 |
| 前车之鉴 | tests/sentinel/dedup-key-stability.test.ts（D354 键稳定契约先例）+ docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md L106（D339 裁决 A）+ memory/notes/proposed/2026-09-06-d580-ticket-dedup-persistence.md（本单决策 note） | N14 根因 + 窗口语义 + 三项裁决不得翻案（除非 CTO 仲裁） |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority 原文引用）/ §4（Current State 实测行号与消费方清单）/ §5.1（写集表）/ §5.2-5.4（三契约 JSDoc 锚）/ §5.5（必答 4 题裁决 + 决策表——不得静默偏离）/ §7（red→green 表）/ §10（DS1-DS12）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 → 列出你的改动清单（文件级, 11 条目）→ 确认基线（见 §三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：契约优先（listSentinelTickets / transitionTicket 先按 spec §5.2-5.4 JSDoc 落地再写调用，铁律 47）、降级诚实（每个 catch log + degraded 分类，铁律 24/31——memory-fallback 必须带 source + degraded 双标记）、类型安全（`as any`=0，铁律 38）、测试非空壳（expect 断言 + 正常/降级/边界三路径 + red→green 留证，铁律 48）。

## 三、D580 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 开工前置（铁律 0-3）：`git fetch --all && git pull --ff-only`；在 **feat/d580-ticket-slice** 分支上工作（禁直接 main commit，铁律 34）。
   - 基线 = main 含 7afbb23f（D580 派单 + D579 spec 入库 commit）。**本单零上游依赖**（D579/D578 写集不同域），但 D579 编码可能并行合 main——合并后 spec §4 行号必漂移，**动手前重新核验全部行号**（runner.ts L185-187/L209-220/L677/L720/L909/L951/L988/L1131-1144、sentinel-service.ts L65-73/L253-279、routes/sentinel.ts L88-136、signal-aggregator.ts L144/L177-183）——照旧行号写测试会红（D524 教训）。
   - task brief 已预填: `.claude/task-briefs/2026-09-06-D580-08告警工单切片-8-2入库-8-3去重稳定化-8-4状态机API.md`（Q2 写集从 spec §5.1 抄入，直接用，勿重生成）。
2. **写集精确性（S-2/S-6）**：只改 spec §5.1 写集表列出的文件（**7 修改 + 4 新建**）；`git diff --name-only` 与实际改动完全一致。extensions/sentinels/ 的 43 个 aggregate **仅 id 生成行**触碰（删 `-${now.getTime()}` / `-${Date.now()}` 后缀），每文件其他行零触碰。spec 文件随编码首个 commit 同批入库（消解 spec-only 预期漂移，spec 文件头预登记在案）。
3. **诚实 RED + 波及面前置**：改任何 id 行**之前**先跑新建的 tests/sentinel/finding-id-stability.test.ts 留 red 输出；再按 spec §4.2 消费方清单 grep 复核（铁律 9 传播检查），修复 threshold-injection L134 / threshold-manifest-flip L50-51 两处尾横杠断言（`'e4-churn-crit'.startsWith('e4-churn-crit-')` = false——去后缀后必破）。禁伪造绿、禁跳过 red 留证。
4. **evidence 落盘规范（K3 独立重跑可复现）**：`docs/synova/audit-reports/D580-ticket-slice-evidence-20260906/`（git 跟踪路径；**根级 evidence/ 被 .gitignore L76 禁用**）至少三件：README.md（总览 + DS 对照）、finding-id-consumers-grep-evidence.md（§4.2 七项消费方 + 43 文件清单 + 修改后逐文件 `grep -c 'getTime()'` 归零输出）、red-green-evidence.md（双轮全文）。对齐 D577 evidence 结构（DS8-flip-physical-verification.md 先例）。禁只在 task-state 存单一副本。
5. **红线（违反 = 事故）**：不碰 `scripts/audit/`（K3 专属）；不碰 `scripts/product-lines/`（D579 领地）、`scripts/desktop/`（D578 领地）、`src/server.ts`、`src/config.ts`、`src/sentinel/signal-aggregator.ts`（写集外——title 实体键漂移是已知限制，spec §6，不顺手修）、`electron-renderer/`、`VERSION.md`（D579 编码阶段 bump，单写者纪律）、pre-commit 门禁脚本；closeTicket 的 signal_id LIKE 缺口**记录不修**（spec §5.5-裁决 4）。
6. **环境坑（本机实测注记）**：① macOS BSD grep 无 `-P` → dev-doc-gatekeeper C2/C1 本机静默跳过、CI（GNU）才真执行——不要被本机"跳过"麻痹，路径书写保持 GNU 安全（token 后不紧跟反引号/中文标点）；② 通知去重测试用 fake timers 模式（对齐 dedup-key-stability.test.ts: `vi.useFakeTimers({ toFake: ['Date'] })` + `vi.setSystemTime`）；③ db 测试用 better-sqlite3 `:memory:` + 自建 DDL（对齐 sentinel-runner-auto-ticket.test.ts，不调 runner.start() 避免 cron 副作用）；④ INSERT OR REPLACE 重复触发会重置 status='open'（D463 既有语义，spec §5.5-裁决 1 保留——测试别当 bug 修）；⑤ dedup 表用 INTEGER epoch ms（不用 datetime('now') 秒级 TEXT），窗口比较才精确。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：DS1-DS12（spec §10）逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim、禁重编号/跳号/静默缺项（S-10）；五项机制决策（spec §5.5 决策表）不得静默偏离。
2. **不违反铁律**：接线完整（listSentinelTickets/transitionTicket/transitionSentinelTicket/POST 端点有**生产**调用点，测试调用不计 S-3；spec §8 六条 grep 逐条实测）、降级诚实（表空/db 失败 → source + degraded 双标记 + log 非静默，铁律 24/31）、类型安全（as any=0，铁律 38）、契约优先（铁律 47）、测试非空壳（铁律 48）、架构边界（L1 只经 L2 服务，零新增跨层 import，铁律 39）。
3. **无 bug**：spec §7 用例逐条跑通 + `npx vitest run tests/sentinel/ tests/routes/sentinel-tickets.test.ts` 全绿（26 文件回归不破，含 D577 threshold-injection/flip 修复后）+ 全量 vitest 失败集与 main 基线 diff=空 + tsc 28=28 基线 + pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 表逐条 grep 出真实生产调用点（GET /tickets 改造点 routes L91 不变、POST /tickets/:id/transition 新注册 + server.ts L56 挂载链）。
5. **测试到位**：red→green 已证（evidence 双轮输出）、覆盖正常/降级/边界/接线四层（L1/L2a/L2b/L2c）、expect 断言非空壳。
6. **其他**：残留清理（sentinel-service 内存伪工单派生代码被替换后，旧派生逻辑 grep 零引用）；三场景验收（派单 §三-4）作为集成用例真实跑通；幂等复跑（finding-id-stability 重跑结果一致）；开工先跑 task-start（brief 已预填，见 §三-1）。

## 五、K3 审计提示（收尾要求）

- D580 完成后**单任务提审**（K3 审 D580）。审计焦点预判：① 43 文件 id 全部稳定无漏网（`grep -rn 'getTime()\|Date.now()' extensions/sentinels/*/aggregate.ts` 逐文件核对非 id 行）；② 降级三场景显式留痕（表空/db 失败/终态拒绝，不静默）；③ 状态机白名单无后门（任何非白名单迁移 409）；④ signal_id 关联裁决执行到位——closeTicket 缺口未被顺手修（git diff 无 signal-aggregator/closeTicket 写入值改动）；⑤ 写集对账（diff vs §5.1 表 11 条目零漂移）+ 波及面 grep 证据在 evidence。
- 完成后回填 `task-state/D580.json` **impl 段**（commit + by + files[] + DS 逐项状态 ✅/⏸/❌+理由）+ evidence 路径；spec 段（dev-doc 已填）不得改写。
- 审计员会**独立重跑**：finding-id-stability / ticket-store / ticket-transition / sentinel-tickets 四个新测试 + evidence 里的 grep 命令原文。脚本必须幂等、可复现、无本机假设（§三-6 环境坑已标注）。
- memory/notes/proposed/2026-09-06-d580-ticket-dedup-persistence.md 随 impl commit **git mv 到 implemented/**（铁律 49 commit-msg 物理门禁，commit body 引用 note 路径）；VERSION.md 禁碰（D579 单写者）。

**开始吧。**
