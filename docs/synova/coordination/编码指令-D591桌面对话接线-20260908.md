# 编码指令 — D591 桌面对话接线（真流式 + 提交回声 + 向导后直达对话）

> 日期: 2026-09-08 | 编码线: Claude Code | 依赖: D590（已落 main，b54a0fb6）

## 一、任务文档表（先读后动，缺一不可）

| 文档 | 路径 | 作用 |
|---|---|---|
| **Spec（编码唯一契约）** | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D591-desktop-chat-wiring-20260908.md | 11 节全读；重点 §3 Q4 契约 / §4 Current State / §5.1 写集 / §5.2 实现契约 / §7 测试 / §10 DS |
| L1 审计报告 | docs/synova/research/L1全量审计-20260907/L1-audit-report.md | §7 D591 行（范围+验收原文）、§5 DSH 借鉴锚点 |
| D590 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md | §5.2 帧协议/持久化时序——你消费的端点契约权威定义 |
| 北星 | .claude/PRODUCT-BRIEF.md | §三（两套系统并行）、§六 P0 |
| 铁律 | AGENTS.md | 0-2/4/5/24/31/37/38/47/48；40-45 适用域裁决见 spec §5.4 决策 6（不适用 electron，模式纪律沿用） |

## 二、执行要求

认真阅读上述任务文档，然后执行任务。做到你的最高代码水平。
任务复杂 → 先用 plan mode 列改动清单再执行；先想清楚再动手，禁止没想清楚就改代码。
契约优先（铁律 47）：useStreaming 扩展先按 spec §3 Q4 写 JSDoc 契约再实现。降级诚实（铁律 24/31）：每个 catch 有 log/warn，错误进 UI 错误条，禁静默。类型安全（铁律 38）：as any / as never / as unknown as = 0。测试非空壳（铁律 48）：每用例有真实 expect，正常/降级/边界三路径。

## 三、任务专属硬约束（违反 = 审计 FAIL）

1. **基线核验**：开工前 `git fetch --all && git pull --ff-only`，确认 main 含 b54a0fb6（D590）；feature 分支（铁律 34）。spec 中行号以 2026-09-08 main@b54a0fb6 为准，动手前重新 read 核验（防 M7 漂移）。
2. **写集精确性**：只改 spec §5.1 写集表文件（4 修改 + 1 新建）+ 表外删除清单（useConversation.ts）。`git diff --name-only` 必须与之一致。**不碰 src/ 后端任何文件**；**不碰 scripts/audit/**（K3 专属）；**零 DSH 依赖**（范式借鉴，零 import）。
3. **测试先行**：tests/electron/use-streaming-conversation.test.ts 先写先红（DS1 red 证据落盘），再实现转绿。use-streaming-contract.test.ts 零改动必须保持全绿。
4. **端点决策已定，勿翻案**：对话主链路 = /api/conversations（缺省 mode）；显式"诊断我的公司" = /api/diagnosis/consult（diagnosis 模式，行为与 main 逐字节等价，用例 8 锁回归）。不要把 consult 删掉或改 URL。
5. **真流式 = token 帧**：bufferRef += text + scheduleFlush()（16ms），禁 LineBuffer/FrameRateLimiter/StreamChunker 类名（模式纪律沿用铁律 41）。
6. **删除复核**：useConversation.ts 删除后 `grep -rn "useConversation\b" electron-renderer/src`（排除 useConversationStore）必须零结果；MOCK_WORKSPACES/MOCK_CONVERSATIONS 同理（spec DS7/DS8）。
7. **clearSession 预留 API**：本期 UI 零调用是 spec §8 显式登记过的，不构成接线缺口；除此之外任何新 export 必须有生产调用点（测试调用不计，S-3）。
8. **evidence 落盘**：DS2 真流式必须有真实后端物理证据（DEV_MODE=false 端到端日志/截图，mock 测试不算）落 docs/synova/audit-reports/D591-desktop-chat-wiring-evidence-20260908/，K3 独立可复现；red 日志一并落盘。
9. **提交纪律**：禁 git stash（铁律 0-3）；禁 --no-verify；逐项 commit；synova-commit 流程。

## 四、复核清单（做完逐项自查，K3 会盯着 + 最后审计）

- [ ] spec §10 DS1-DS10 逐项对照，声称 = 实现 + 验收（禁 overclaim）
- [ ] 接线：spec §8 表逐条 grep 出真实生产调用点
- [ ] 降级诚实：恢复 404/网络失败 console.warn + 空态；error 帧进错误条
- [ ] 类型安全：as any/never/unknown as = 0；恢复投影用类型守卫（沿 sse-contract.ts:147 形态）
- [ ] 测试：red→green 已证、三路径覆盖、expect 非空壳、contract 测试零回归
- [ ] 无 bug：tsc 零新增错误、全量 vitest 零新失败、pre-commit 全过
- [ ] 残留清理：两处 grep 零结果（DS7/DS8）；无新死代码
- [ ] task-state/D591.json 回填 impl 段 + status=impl_done
- [ ] 完成报告含 spec §5.4 六项决策记录 + 北星验收三句（附证据）

## 五、审计提示

提审口径：一次提审覆盖 D591（桌面接线全链）。验证点收口以 spec §10 DS1-DS10 为准，逐条附证据路径。审计员将独立重跑测试与 grep 复核——确保命令可复现、路径真实。

Kimi K3 会盯着你的任务，也会做最后的审计。

开始吧。
