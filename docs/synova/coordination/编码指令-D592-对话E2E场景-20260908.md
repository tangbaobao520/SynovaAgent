# 编码指令 — 对话 E2E 场景 + 产线 2-1/2-2/2-5 证据（D592）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: 创始人 2026-09-08 直接派单（任务书见本指令 §三）+ [L1 全量审计报告](../research/L1全量审计-20260907/L1-audit-report.md) §7 D592 行
> 审计: Kimi K3 会盯着你的任务，对话交互线完成后做最终审计（覆盖 D592 全部 DS + 2-1/2-2/2-5 证据复核 + 2-7 k3_only 复核点）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D592 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D592-chat-e2e-20260908.md | 对话 E2E + evidence——**编码唯一契约** |
| D590 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md | 被测端点的帧协议/忙锁/中断锚权威定义（§5.2） |
| D590 evidence | docs/synova/audit-reports/D590-chat-sse-evidence-20260908/ | 本地 LLM 替身 + scratch env 先例（run-ds2-ds3-evidence.sh 照抄骨架） |
| 证据机制 | docs/synova/product-lines/evidence/README.md + stale-reverify-D589-line2.json | evidence JSON 字段权威样例 + at 纪律/Node ABI/pending_k3 机制上限 |
| 派单源 | L1 审计报告 §7 D592 行 + §5 DSH 锚点 | 范围/验收原文 |
| 北星 | .claude/PRODUCT-BRIEF.md §二/§三 + docs/synova/product-lines/product-lines.yaml 线 2 | 产品方向锚点（线 2 value"老板用对话触发诊断，回答是流式的"） |
| 测试模式先例 | tests/e2e/auth-register-flow.e2e.test.ts | 外部 server + 探活 + ctx.skip 模式权威模板 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority，含 DSH invariant 原文）/ §3 Q4（环境与 evidence 契约）/ §4（Current State，被测链路 file:line 实测）/ §5.1（写集）/ §5.2（runner 与替身契约）/ §7（T1-T9 用例表）/ §10（DS 验收）——spec 是唯一契约，声称即引用。
2. **先 plan mode 再做**：读 spec §5.1 写集表 + §7 用例表 → 列出你的改动清单（7 个新建文件）→ 确认依赖前置（§三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`/`as never`/`as unknown as`=0，铁律 38——SSE 帧解析用 unknown + 类型守卫）、降级诚实（server 不可达显式 ctx.skip + console.warn，绝不静默空跑，铁律 24/31）、测试非空壳（每用例真 expect + 正常/降级/边界三路径，铁律 48；负向组 T4/T7/T8 是 S-5 失败模式覆盖，不许删）。

## 三、D592 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 基线 = main @ bdfad5c0 之后。D590（b54a0fb6）与 D591（#430）**已合 main**——前置已满足。
   - 开工前 `git fetch --all && git pull --ff-only`，在 feature 分支上做（铁律 34）。
   - spec 引用的行号基于 main@bdfad5c0（conversations.ts:324/:329/:246/:249-257/:268/:206-208、sessions.ts:68-81、auth.ts:110-112、server.ts:355-356、conversation-engine.ts:767-791/:881-889）。若 D593/D596 已合入并改动这些文件，**重新 grep 核验行号与帧协议再写断言**（D524 教训：照旧行号写测试会红）。
2. **写集精确性**：只**新建** spec §5.1 说明列的 7 个文件（1 测试 + 6 evidence 域）；**src/ 零 diff、零删除、零修改条目**；实现提交时把 §5.1 目录级条目精确化为 7 个确切文件名（D590 先例）；`git diff --name-only` 与实际改动完全一致。
3. **诚实 RED**：本任务默认模式**不需要真实 LLM key**（本地替身 §5.2-B）。`SYNOVA_E2E_REAL_LLM=1` 真实 key 门控组不可用时，如实标注（evidence note + 测试注释双处），**禁止伪造全链路绿、禁止把替身模式断言冒充真实 LLM 断言**（T6 两种模式断言强度不同，spec §5.2-C）。
4. **evidence 落盘规范（K3 独立重跑可复现）**：3 份 JSON 由 **runner 模板生成**（at=`date -Iseconds`，禁止手抄时间戳）；两段 vitest 全量日志落 evidence/；JSON 的 quote 必须含物理数字（token 帧数/消息计数/通过用例数）；runner 必须幂等可重跑（端口预清理 + scratch mktemp）。
5. **红线（违反 = 事故）**：不碰 `src/`（被测链路已闭环，零生产代码需求）；不碰 `scripts/audit/`（K3 专属）、`product-lines.yaml`、`VERSION.md`、`scripts/golden-scenarios/`（DSH 域）、`scripts/product-lines/calc-progress.py`（控制塔域）；**DSH 零 import**（G1——invariant/帧协议只作注释范式引用，`grep -rn "@deepseek-ai" tests/e2e/` 必须零结果）。
6. **环境坑**：① **Node 22 ABI 锁**——better-sqlite3 编译于 Node 22（NODE_MODULE_VERSION 127），Node 24 全量假败 350 例（D589 实证），runner 头部必须检查主版本并显式报错；② **CI 排除 tests/e2e/**（vitest.config.ts:32-35）——CI 绿 ≠ E2E 跑过，DS2 证据只能来自本地 runner 实跑日志；③ 本地全量 `npm run test` **包含** tests/e2e/*.test.ts——无 server 时靠探活 skip 保持绿，这是设计行为不是缺陷；④ 3099/9201 端口可能被残留进程占用——runner 幂等预清理（D590 先例 `lsof -ti | xargs kill`）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 DS1-DS6 逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim——特别是 DS4 的"pending_k3（非 verified）"上限表述；spec §12 自检清单照做。
2. **不违反铁律**：接线验证（spec §8 表——本任务接线 = E2E 物理消费生产端点 + runner 真实进程启动，跑通即证明）；降级诚实（24/31）；类型安全（38）；契约优先（47——runner/替身/state 文件契约按 §3 Q4 JSDoc 化）；测试非空壳（48）。
3. **无 bug**：`bash docs/synova/product-lines/evidence/D592-run-e2e.sh` exit 0 + 两段日志**零 skip 全 pass** + 本地全量 `npx vitest run tests/e2e/` 无 server 时全 skip 绿（不破坏他人工作流）+ pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 每条消费点在真实进程日志中可见（POST /api/conversations 帧序、GET /api/sessions/:id 读回、409/404/400 负向命中）。
5. **测试到位**：9 用例全绿零 skip；失败模式覆盖（忙锁/断线/404/400/白名单对照 401）；expect 断言非空壳；替身探针断言（T3/T9 消息计数）证明历史真实到达 provider。
6. **其他**：产物可复现性（runner 二次执行幂等）；残留清理（scratch 临时目录用完即删；evidence 目录无临时垃圾文件）。

## 五、K3 审计提示（收尾要求）

- 完成后**一次提审**（K3 审 D592，报告覆盖 DS1-DS6 + 独立重跑 runner + evidence 三 JSON 字段核验）。
- 审计验收 = 线 2 验证点 **2-1/2-2/2-5** 证据从单测级 → D592 E2E 级（calc 重算入 pending_k3，**K3 复核 pass 后翻 verified**——这正是审计动作本身）；顺带覆盖 **2-7**（k3_only"审计员复核对话触发诊断链路"）。
- 完成后回填 `task-state/D592.json` **impl 段**（commit + by + files[7 个新建文件]）+ 验收证据原文（spec §10 DS 要求）。
- 审计员会**独立重跑 D592-run-e2e.sh**——脚本必须幂等、可复现、无本机假设（Node 22 检查、端口预清理、scratch 隔离都在 spec §4.3/§5.2-B 标注过，跑前读 spec §4/§5.2）。

**开始吧。**
