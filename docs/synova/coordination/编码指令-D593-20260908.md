# 编码指令 — L1-P2 报告落盘 + 桌面报告/哨兵呈现 + 401 对齐（D593）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: 创始人 2026-09-08（L1 审计收工计划 Phase 2 首任务；任务号已取 task-state/D593.json）
> 审计: Kimi K3 会盯着你的任务，D593 完成后做最终审计（覆盖 DS1-DS9 全项 + red→green 复现 + 写集对账 + 报告重启存活物理证据）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D593 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D593-report-persistence-20260908.md | 报告落盘 + 桌面呈现 + 401 对齐——**编码唯一契约** |
| L1 审计报告 | docs/synova/research/L1全量审计-20260907/L1-audit-report.md | §7 D593 行验收原文 / §8.3 风险登记行 5（重启丢数据）/ §5 冷读激活锚点 |
| D590 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md | 白名单先例 + §6 归本任务的排除项（已实现 b54a0fb6） |
| D591 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D591-desktop-chat-wiring-20260908.md | §5.4 决策 5 会话列表遗留 + localStorage 模式（已实现 bdfad5c0） |
| 北星 | .claude/PRODUCT-BRIEF.md §三（两套系统的产出都是报告）+ §五 W1（上传→诊断→报告）+ §六 P0 | 产品方向锚点 |
| 前车之鉴 | D592 K3 审计报告（docs/synova/audit-reports/2026-09-08-K3-D592-closeout.md：P1-1 calc 声称 stale、P2 行号漂移）+ spec §4.1 闲置资产实证（M3 机制建成未接线） | 教训清单 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §4（Current State，全部 file:line 实测）/ §5（写集 + 六组实现契约）/ §7（测试 red→green 表）/ §8（Wiring Verification）/ §10（DS1-DS9）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表（8 修改 + 2 新建）→ 列出你的改动清单（文件级）→ 确认依赖前置（§三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`/`as never`/`as unknown as`=0，铁律 38——partialReport 反序列化用类型守卫不盲信 JSON，对齐 conversations.ts:75-84 isEngineStateLike 形态）、契约优先（listDiagnosisReports 先 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded，铁律 24/31——行级 JSON 损坏跳过不 500）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，red→green 先证）。

## 三、D593 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - spec 基线 = origin/main `2e9e06ad`（2026-09-08）。依赖 D590/D591/D592 **已全部合 main**（b54a0fb6 / bdfad5c0 / 143a9924 + 2e9e06ad K3 PASS）——零等待，无 blocking 依赖。
   - 编码前置（铁律 0-3 + D334）：`git fetch --all && git pull --ff-only`，feature 分支 `feat/d593-report-persistence` 开工（禁直接 commit main），提交走 synova-commit（**禁 git stash**）。
   - 开工后**重新核验 spec 引用的行号**（diagnosis.ts / conversations.ts / auth.ts / session-store.ts / RightPanel.tsx 均为活跃文件，行号必漂移）——照 spec 旧行号写测试会红（D524/D592 K3 P2 教训）。spec 行号全部标注了"锚定什么"（如 `completedReports` Map、`cacheCompletedReport`、`isWhitelisted` 函数体），按锚定语义找位，不盲抄行号。
2. **写集精确性（S-2/D316）**：只改 spec §5.1 写集表列出的文件（8 修改 + 2 新建，新建以**目录级条目**声明：tests/routes/ 下 **diagnosis-report-persistence.test.ts**、tests/electron/ 下 **right-panel-report-sentinel.test.tsx**——实现提交后精确化为确切文件名，并回改 spec §5.1 表）；`git diff --name-only` 与实际改动**完全一致**；实现提交时 `bash scripts/workflow/check-dev-doc-write-set.sh <spec路径>` exit 0（dev-doc 交付期该脚本报"声明修改但零实际变更"是预期——写集契约面向实现提交，D592 DS5 先例）。
3. **测试先行（铁律 0-2）**：两个新测试文件先写先红（404 路由不存在 / 401 / Empty 占位即 red 证据，README/evidence 双处记录），再实现转 green。测试构造模式（真实路由 + ':memory:' SessionStore + mock 引擎/LLM + "重启"= 同 db 新 app 实例）**严格按 spec §7 执行，此处不复述**。
4. **诚实 RED / 物理证据**：DS2 的"跑诊断→杀进程→重启→GET 200"物理证据需要 LLM——沿 D592 替身模式（OpenAI 兼容假端点 :9201，`SYNOVA_E2E_REAL_LLM` 环境开关留 K3 真实 key 复核）完成；替身不可用时集成测试证据替代 + DS2 如实标注 ⏸ + 理由（README + evidence 双处），**禁止伪造绿、禁止契约断言冒充全链路**（D576 教训）。
5. **evidence 落盘规范**：red→green 双轮输出、重启存活 curl 原样输出、SQL 断言（diagnosis_checkpoints phase=5 行）、§8 接线 grep 结果、写集对账，落盘 `docs/synova/audit-reports/D593-report-persistence-evidence-20260908/`（`*.log` 被 .gitignore 全局忽略，改名 `*.log.txt`——D580/D590 先例），K3 独立重跑可复现。
6. **红线（违反 = 事故）**：不碰 `scripts/audit/`（K3 专属）；**零 L2/L3/L4 修改**（src/agent/diagnosis-launcher.ts 的 saveCheckpoint 死代码明确不碰——spec §5.4 决策 8，登记待办即可）；src/store/ 只允许改 session-store.ts（加 listDiagnosisReports 一个方法，禁碰 session-projection.ts 与 schema）；useStreaming.ts / sse-contract.ts / conversation-store.ts 零触碰（D591 已闭环）；Stage 3 前零 DSH 依赖（不 install、不 copy DSH 代码）。
7. **与 D594 的文件边界**：RightPanel.tsx 归本任务（报告 tab + 哨兵 tab）；若 D594 并行且其写集需动 RightPanel.tsx → verify-parallel 报重叠 → 停手问 CTO 仲裁（TASK-ROUTING §一撞车协议），不自行协调。
8. **编号复用注意**：旧 `14e54722 feat(D593): LLM 调用韧性层` 已关闭（另一任务）；本任务 commit message 用 `feat(D593): L1-P2 报告落盘——……` 并在一处注明"编号复用（旧 D593=LLM 韧性层已关闭）"；**K3 审计报告文件名勿用 `2026-09-08-D593-llm-call-resilience` 同款**（用 D593-report-persistence 区分）；task-state 生成器按 git log 含 D# 派生 impl 状态（task-state/README.md D393）——旧提交可能造成派生误判，完成报告显式声明本任务 commit hash 清单供生成器/CTO 校正。
9. **环境坑（本机实测，D590 指令同款）**：① 根 `npm run dev` 指向 scripts/agent-start.bat（Windows 批处理），Mac 起服务用 `npx tsx src/index.ts`；② `*.log` 被 .gitignore 全局忽略（evidence 用 .log.txt）；③ dev-doc-gatekeeper C2 在 BSD grep 下静默空转——路径以 spec §5.1/§8 表为准。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：DS1-DS9 逐项对照（spec §10），声称 = 实现 + 验收（S-2），禁 overclaim、禁重编号/跳号/静默缺项（S-10）；九项决策记录（spec §5.4）在完成报告中复述（S-12）。
2. **不违反铁律**：接线完整（spec §8 表逐条 grep 出**生产**调用点——listDiagnosisReports 被 diagnosis.ts 列表端点调用、saveDiagnosisCheckpoint 在 diagnosis.ts + conversations.ts 双处新增调用、白名单五前缀在 auth.ts 函数体内、SentinelDetail 在 RightPanel 渲染、setConversations 被 LeftPanel 调用；测试调用不计 S-3）；降级诚实（24/31）；类型安全（38）；契约优先（47）；测试非空壳（48）；架构边界（39/46——零 L2/L3/L4 修改、L1→L5 仅经 D563 动态 import 通道既有范式）。
3. **无 bug**：spec §7 用例逐条跑通 + `npx vitest run tests/routes/diagnosis-report-persistence.test.ts tests/electron/right-panel-report-sentinel.test.tsx` 全绿 + 全量 vitest 零新失败 + tsc 零新增 + pre-commit 13 组全过（**禁 --no-verify**）+ 写集对账 exit 0（§三-2）。
4. **接线完整**：spec §8 每条 grep 实测（示例：`grep -rn "listDiagnosisReports" src/` ≥2；`grep -rn "saveDiagnosisCheckpoint" src/routes/` ≥2；`grep -n "/api/notifications" src/middleware/auth.ts` ≥1；`grep -rn "getDiagnosisCheckpoint" src/routes/diagnosis.ts` ≥2——:511 resume 既有 + 新增 fallback）。
5. **测试到位**：red→green 已证（实现前用例先红）、覆盖正常/降级/边界/白名单/双路线、expect 断言非空壳。
6. **其他**：残留清理（无删除清单；`grep -rn "内存缓存已清" electron-renderer/` 零结果——降级文案已诚实化）；FIFO 注释诚实化后 `grep -n "COMPLETED_REPORTS_MAX" src/routes/diagnosis.ts` 语义为"一级缓存"；README 安全模型节与 auth.ts JSDoc 同步。

## 五、K3 审计提示（收尾要求）

- D593 完成后**一次提审**（K3 审 D593，报告覆盖 DS1-DS9 + red→green 独立复现 + 写集对账 + 报告重启存活物理证据复核 + 白名单安全姿态复核）。
- 审计锚点 = spec §10 DS 表 + task-state/D593.json impl 段（commit + branch + files[] + evidence 路径）+ evidence 目录。
- 每项 DS 的验证命令必须独立可重跑（幂等、无本机假设——LLM 依赖项若标 ⏸，K3 按诚实 RED 口径复核而非判 FAIL）。
- 完成后回填 `task-state/D593.json` 的 **impl 段**（`"spec"` 段已由 dev-doc 预置，勿动；`"spec".commit` 为空待编码提交后由 CTO/生成器回填或你顺带补齐——按 spec 实际落库 commit 填）。

**开始吧。**
