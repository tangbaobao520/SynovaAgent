# 编码指令 — MCP 认证/权限 + 工具补齐（D595）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: 创始人直接派单（2026-09-08，dev-doc session 承接 spec）｜任务账本: task-state/D595.json（status=spec_done）
> 审计: Kimi K3 会盯着你的任务，D595 完成后做最终审计（覆盖：写集一致性 / 接线 S-3 / red→green / 降级诚实 / SDK 结论落盘）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D595 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D595-mcp-auth-20260908.md | MCP 认证/权限/工具补齐/SDK 迁移/打包——**编码唯一契约** |
| 北星 | .claude/PRODUCT-BRIEF.md §一/§四 | "Agent，不是 ChatBot"+HTTP+MCP 对外服务——方向锚点 |
| 产品决策 | docs/synova/coordination/INTERFACES-STRATEGY.md | 创始人 2026-08-12：MCP=P0 + 缺口四项（本任务逐项回应） |
| L1 审计 | docs/synova/research/L1全量审计-20260907/L1-audit-report.md §3.4/§3.6/§5/§7/§8.1 | D595 行验收标准 + dsh-acp 锚点（§5） |
| 跨层扫描 | docs/synova/research/L1跨层违规扫描-20260908.md §2/§三 | 哨兵簇修复路径 + 哪些违规**不在**本任务写集（§三 治理原则） |
| 裁决继承 | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D590-chat-sse-endpoint-20260908.md | 单机本地信任裁决①（已 impl_done，PR #424）——MCP 信任声明同域 |
| 前车之鉴 | docs/synova/coordination/审计发现台账-DSH-CTO.md（CT-46 条） | mcp/index.ts 曾以 `as never` 逃逸类型门禁——新代码零类型断言逃逸 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority，来源 A-G 带原文）/ §4（Current State，全部亲证 file:line）/ §5（写集+设计）/ §7（测试 red→green 表）/ §10（DS1-DS12）/ §12（五项决策记录——不要重新讨论已收敛的决策，执行即可）。
2. **先 plan mode 再做**：spec §5.1 写集表 10 条 → 列出你的文件级改动清单 → 确认 §三-1 前置 → 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（as any / as never / **as unknown as** 全零，铁律 38+CT-46——spec §5.4 特别点名：listSentinels 禁止照搬现 mcp/index.ts:126 的 `as unknown as` 断言，直接读 config.layer）、契约优先（listSentinels 与权限模块先 JSDoc @input/@output/@degraded/@error，铁律 47，spec §5.3/§5.4 已给模板）、降级诚实（铁律 24/31：degraded 标记随 L2→工具输出全链传播，T7）、测试非空壳（T1-T10 先红后绿 + 冒烟 S1-S7，铁律 48）。

## 三、D595 专属硬约束（违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 无硬上游依赖（审计 §7：D595 可并行启动）；唯一关联 D590 **已合 main**（task-state/D590.json: impl_done，PR #424）。
   - 开工前置（铁律 0-3）：`git fetch --all && git pull --ff-only`，禁止在 behind 状态开工。
   - 合入后**重新核验 spec 引用的行号**：src/middleware/auth.ts（白名单 :92-114，D590 加过三行 :110-112）、tests/architecture/graphstore-unify.test.ts（:88-114）、src/agent/sentinel-service.ts（:127-429）——任何上游合入都会漂移，照旧行号写断言会红（D524 教训）。行号漂移 = 以当前代码实况为准 + 完成报告注明，不视为 spec 违约。
   - 前置未满足（如 SDK 安装受阻）→ 进入 waiting 并上报，不伪造实测。
2. **写集精确性（D286 教训）**：只改 spec §5.1 写集表 10 条（7 修改 + 3 目录级新建：src/mcp/ 下 tool-definitions.ts、tests/mcp/ 下两个测试、docs/synova/research/ 下评估结论）。**实现提交时把目录级条目精确化为确切文件名**（回填 spec §5.1 表 + task-state impl 段 files[]）；`git diff --name-only` 与写集完全一致。特别注意：tests/architecture/graphstore-unify.test.ts **必须**在写集内（棘轮断言要求 src/mcp/index.ts 含 "SqliteGraphStore"，跨层修复后不同步此测试 = 全量 vitest 必红）。
3. **诚实 RED / 诚实绿**：
   - SDK 安装受阻 → 显式上报（环境坑见 §三-6），**禁止静默回退手写 readline**（spec §12 决策 3）。
   - 冒烟 S4（sentinel_list）是设计好的**诚实绿**：进程内 L2，无需 LLM key / HTTP server。除此之外不要声称任何"端到端全链路"——diagnose_organization 的 LLM 链路不在本任务验收内。
   - 白名单前缀的生产行为（DEV_MODE=false 非 401）以 auth.ts 单测断言收口；起真实 HTTP server 的集成不在写集内，不伪造。
4. **evidence 落盘规范**：冒烟 S1-S7 原始输出、-32000 拒绝指纹、red→green 两阶段截图/日志、时间戳，落 evidence 目录（K3 独立重跑可复现）；禁止只在完成报告里写"全绿"。
5. **红线（违反 = 事故）**：不碰 scripts/audit/（K3 专属）；零 DSH 依赖（dsh-acp 是范式参考，**不 import、不 copy DSH 代码**——spec 权威引用 G 只引用其行为语义）；不碰 src/mcp/bridge.ts 与 tool-registration.ts（反向 client，写集外）；不修 mcp/index.ts:193/:203/:231/:236 的存量跨层违规（D601 簇 FIX 任务，spec §6）。
6. **环境坑（dev-doc session 实测）**：
   - `~/.npm/_cacache` 存在 root 属主文件 → npm 报 EPERM；绕过：`npm install @modelcontextprotocol/sdk --cache /tmp/npmcache-d595`（或先 `sudo chown -R $(whoami) ~/.npm`）。
   - SDK 1.30.0 zod 依赖 `^3.25 || ^4.0`，与仓库 zod 4.4.3 **直接兼容**（已 npm view 实测），无需降级仓库 zod。
   - 本机（Mac/BSD grep）跑 dev-doc-gatekeeper 时 C1/C2 显示"跳过"（BSD grep 无 -P）——dev-doc 已用 python 模拟同款正则补验（8 路径全存在、零 Edge ID）；编码 session 不需要重跑门禁，但 **CI/Linux 环境 C2 会真扫**，保持"src/ 路径后不贴反引号/全角标点"的书写纪律无实际影响（那是 doc 层面的事）。
   - 冒烟 spawn tsx：win32 需 shell 或 .cmd 后缀（bridge.ts:23-28 resolveCommand 先例）——测试要双平台可跑。
   - Mac 上 `npm run dev` 指向 .bat（审计已知缺口）——与本任务无关，不要顺手修（写集外）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：DS1-DS12 逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim、禁重编号/跳号/静默缺项（S-10）；§12 五项决策照做，若实现中发现决策确有错误 → 停手上报，不擅自改向。
2. **不违反铁律**：接线完整（新 export 生产调用点 = spec §8 逐条 grep 实测：listSentinels→src/mcp/index.ts、filterToolsByMode→装配点、白名单前缀→真实 fetch；测试调用不计 S-3）；降级诚实（铁律 24/31）；类型安全（铁律 38+CT-46）；契约优先（铁律 47）；测试非空壳（铁律 48）；架构边界（铁律 39——哨兵族必须 L1→L2，grep 确认 src/mcp/index.ts 零 sentinel//engine-context/sqlite-graph-store/store//l4/ import）。
3. **无 bug**：spec §7 T1-T10 red→green 已证（先红后绿，红阶段输出留 evidence）+ 冒烟 S1-S7 全绿 + `npx vitest run tests/mcp/ tests/architecture/` 全绿 + 全量 vitest 零新失败（铁律 36）+ pre-commit 全过（**禁 --no-verify**）+ 提交走 feature 分支 + synova-commit（**禁 git stash**，铁律 0-3/34）。
4. **接线完整**：spec §8 六条逐条 grep 出真实生产调用点，命中数写进完成报告。
5. **测试到位**：三路径覆盖（正常/降级/边界——T3 fail-closed、T7 degraded 传播、T8 API 不可达、S6 边界、S7 非法模式）、expect 断言非空壳、集成测试不 mock 协议管线（铁律 12——SDK Server 可真跑 stdio）。
6. **其他需要复核的点**：残留清理（flywheel_speeds 改名后 grep 零残留引用；README/INTERFACES-STRATEGY 与代码一致——M7 防漂移）；产物可复现（冒烟脚本幂等、无本机假设）；README 的 mcpServers 配置片段用真实路径且可复制即用。

## 五、K3 审计提示（收尾要求）

- D595 完成后**一次提审**（K3 审 D595：写集对账 / 权限语义测试 / 哨兵族 L2 化 grep / SDK 结论文档 / 白名单三前缀）。
- 完成后回填 `task-state/D595.json` 的 **impl 段**（commit + by + files[] + 关键证据原文）+ status=impl_done；spec §5.1 目录级条目同步精确化。
- 审计员会**独立重跑冒烟与单测**——保证幂等、无本机假设（npm 缓存坑、win32 spawn 兼容都在 §三-6），evidence 目录指纹与本地一致。
- SDK 评估结论文档（docs/synova/research/ 下新建）是审计验收③的硬交付物——四维对比（协议正确性/维护成本/依赖体积/迁移风险）+ 最终结论，不许只写一句"选 SDK"。

**开始吧。**
