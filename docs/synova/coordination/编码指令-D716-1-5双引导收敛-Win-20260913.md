# 编码指令 — D716 spec 2「安装引导单一入口（1-5 双引导收敛）」（Win / Claude Code）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: `docs/synova/coordination/派单-下一批四线并行-20260913.md` §D716（spec 2）
> 归属: **本 spec 实施归 Win 线**（`src/server.ts` = Claude 专属，`.github/CODEOWNERS` 串行点表；DSH dev-doc 只写规格）
> 审计: Kimi K3 会盯着你的任务，本切片完成后做最终审计（覆盖 1-5 + 本 spec §10 全部 DS）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D716 spec 2 | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D716-1-5-dual-guide-convergence-20260913.md | 1-5 双引导收敛——**编码唯一契约**（含必答四项与判绿口径） |
| D518 runbook | docs/synova/runbooks/desktop-dev-prod.md | 目标形态（唯一路径 = 安装包双击）；本任务要把它从"声明"补成"机制" |
| D590 先例 | src/server.ts（L89-92 `uploadV2GoneRouter`） | 410 显式下线的形态与挂载纪律 |
| 派单 | docs/synova/coordination/派单-下一批四线并行-20260913.md | 切片定义 / 写集 / 审计口径（含必答四项） |
| 北星 | .claude/PRODUCT-BRIEF.md §二/§六 | 企业主双击即用；P0「没有这些不能给 GA 用」 |
| 验收点台账 | docs/synova/product-lines/product-lines.yaml 线 1（1-5） | 本切片对应验收点原文与状态（当前 uncommitted，D712 判 fail） |
| 前车之鉴 | CLAUDE.md 的 D603 段（4 条铁律"文档声称有执法、实际零执法"）+ AUDIT-FINDINGS-LEDGER.md（M2/M7） | 本次交付必须含守卫，否则只是第二份声明 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §4（Current State，含今天 14:36 的实时 curl 实测）/ §5（写集 + 退场契约）/ §7（测试）/ §10（DS + 判绿口径）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 → 列出文件级改动清单（含删除项）→ 先重核 spec 引用的行号（§三-1）→ 再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`=0，铁律 38）、契约优先（新路由/守卫先写契约注释，铁律 47）、降级诚实（本切片无 IO，但守卫测试的失败信息必须可读，铁律 24 精神）、测试非空壳（expect + 三路径 + 反向验证，铁律 48，先 red 再 green）。

## 三、本切片专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 写 spec 的基线 = main（2026-09-13）；**开工先** `git fetch --all && git pull --ff-only`。
   - spec 引用的行号（`src/server.ts` L89-92 / L300 / L301 / L302 / L316 / L319 / L320；`electron/main.cjs` L128-141 / L254）**动手前逐条重核**——同批次任务可能改到相邻位置；漂移以实测为准。
   - 本切片无上游阻塞；若发现依赖 → 进入 waiting，**不伪造实测**。
2. **写集精确性**：只改 spec §5.1 写集表内文件——修改 `src/server.ts`、修改 runbook `docs/synova/runbooks/desktop-dev-prod.md`、**删除** `app/setup.html` + `app/js/setup.js` + `app/css/setup.css`、新建 `tests/routes/` 与 `tests/electron/` 两个测试文件。`git diff --name-only` 与实际改动**完全一致**（删除项在 diff 里以 D 状态出现，属正常）。
3. **域约束（跨线红线）**：
   - **不改** `electron/`、`electron-renderer/`、`build-synova.cjs`（Mac DSH 域）——本任务对它们只做**只读断言**（spec §5.2-D 的 G-4/G-5）；需要动 → **停手报 CTO**。
   - **不改** `app/` 下其他 14 个页面、`app/js/admin.js`（D246 管理台 onboarding 不属"安装引导"，spec §5.4 决策 4 有判据）。
   - **不改** `docs/synova/product-lines/product-lines.yaml`（1-5 状态与证据绑定归 CTO/产品线维护者）。
   - **不改** D712 证据（`docs/synova/product-lines/evidence/D712-mac-20260913/` 是 red 基线）。
4. **诚实 RED**：本切片无可变外部依赖；若打包产物 `release/` 不存在 → 打包形状守卫按 spec §7 的静态断言路径执行（读 `build-synova.cjs` 文本），并在 evidence 中如实标注"未跑真实包内清单"（**不得**用静态断言冒充包内实测）。
5. **evidence 落盘规范**：curl 输出（410 三段：状态码/头/body）+ 两个测试文件输出 + 删除后 grep 零引用输出 + 守卫反向验证（红→移除→绿）两段，落盘到 `docs/synova/product-lines/evidence/` 下（**用 .txt / .json，不用 .log**——证据 .log 被 .gitignore 静默忽略，D718 待修）；同时在 PR 描述贴原始输出（evidence 落盘与 1-5 状态推进由 Mac/CTO 复核执行，spec §5.5）。
6. **红线**：不碰 `scripts/audit/`（K3 专属）；提交**禁 `--no-verify`**、**禁 `git stash`**（铁律 0-3）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 的 DS1-DS8 逐项对照（DS9/DS10 归 Mac/CTO）；声称 = 实现 + 验收（S-2），禁 overclaim；**必答四项**（收敛方向 / 退场方式 / 书签兼容 / 单入口物理验收）在 PR 描述逐条回答并附证据。
2. **不违反铁律**：接线完整（`setupGuideGoneRouter` 的**生产**挂载点存在，测试调用不计 S-3，spec §8 逐条 grep）、降级诚实（410 body 是人话非空）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39）、删除项 grep 零引用（37）。
3. **无 bug**：spec §7 verify 命令逐条跑通 + `npx vitest run tests/routes/setup-guide-retired.test.ts tests/electron/dual-guide-packaging-guard.test.ts` 全绿 + 相关既有套件零回归 + pre-commit 全过（禁 `--no-verify`）+ 提交走 `git synova-commit`。
4. **接线完整**：410 路由挂载顺序由**测试**保证（不是注释）；"文件存在时仍 410"用临时目录假文件证优先级（spec §7 T6）。
5. **测试到位**：red→green 已证（实现前 `curl /app/setup.html` 在仓库根形态 = 200，先跑出红）+ 守卫**反向验证**（人为放回 `app/setup.html` / 注入 `app/` 打包条目 → 守卫红 → 移除 → 绿，两段都贴）。
6. **其他**：runbook 措辞与代码事实一致（旧 URL 锚 + 退场机制 + 守卫测试名）；残留清理（`app/` 下 `setup*` 零命中）；**不得**以"打包态本来就 404"充当 1-5 判绿证据（spec §10 判绿口径明文禁止）。

## 五、K3 审计提示（收尾要求）

- 本切片完成后**一次提审**：K3 审 spec 2 全量（退场路由 / 删除完整性 / 守卫强度 / runbook 一致性）。
- 审计验收 = 验收点 **1-5**：证据落盘（Mac/CTO 执行）→ `pending_k3` → K3 复核 → `verified`；注意 K3 会核"打包态 404"是否被误用为通过证据（spec §4.3/§10 已给正确读法）。
- 完成后回填 `task-state/D716.json` 的 **impl 段**（commit + by + files[] + 每条 DS 的证据指针；本 spec 的 spec 段已由 dev-doc 预置）。
- 审计员会**独立重跑**你的验证命令——命令必须幂等、可复现、无本机假设（起后端用临时端口/临时 DB，不碰真实 `data/synova.db`）。

**开始吧。**
