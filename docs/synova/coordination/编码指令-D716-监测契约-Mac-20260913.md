# 编码指令 — D716 spec 1「监测契约（状态驱动通知的配置面）」（Mac DSH 编码线）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: `docs/synova/coordination/派单-下一批四线并行-20260913.md` §D716（spec 1）
> 审计: Kimi K3 会盯着你的任务，本切片完成后做最终审计（覆盖 8-6 + 本 spec §10 全部 DS）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D716 spec 1 | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D716-D1-monitoring-contract-20260913.md | 监测契约 schema + loader + 决策接线——**编码唯一契约** |
| 创始人裁定 | docs/synova/product-lines/DECISION-D1-状态驱动通知模型-20260912.md | 产品语义（六态 / 四条政策值 / 落地路径） |
| 跨线同步 | docs/synova/coordination/跨线同步-D1状态驱动通知模型-Mac-Win-20260912.md | Mac ⇄ Win 归属与接口（§二/§三/§四） |
| 派单 | docs/synova/coordination/派单-下一批四线并行-20260913.md | 切片定义 / 写集 / 审计口径 |
| 北星 | .claude/PRODUCT-BRIEF.md §二/§三 | 服务谁、在什么场景、核心问题 |
| 验收点台账 | docs/synova/product-lines/product-lines.yaml 线 8（8-6） | 本切片对应的验收点原文与当前状态 |
| 前车之鉴 | docs/synova/audit-reports/（D580 报告）+ AUDIT-FINDINGS-LEDGER.md（M2/M3） | 去重键稳定性 credit 不能丢；机制建成未接线是历史高频事故 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §4（Current State，全部带 file:line）/ §5（写集 + 契约正文）/ §7（测试）/ §10（DS 验收）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 + §5.2 契约正文 → 列出文件级改动清单 → 先重核 spec 引用的行号（见 §三-1）→ 再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`/`as never`/`as unknown as` = 0，铁律 38）、契约优先（新函数先 JSDoc `@input`/`@output`/`@degraded`，铁律 47）、降级诚实（每个 catch 有 `log.warn/error` + 返回 degraded，铁律 24/31）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，先 red 再 green）。

## 三、本切片专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 写 spec 的基线 = main（2026-09-13）；**开工先** `git fetch --all && git pull --ff-only`。
   - spec 引用的是 `src/sentinel/` 的行号（runner.ts L178/L259/L517/L588/L1058/L1092/L1313、sentinel-loader.ts L130、sentinel-events.ts L86 等）。**动手前逐条重核**（同批次 D713/D714/D717 都可能改到相邻文件）；行号漂移 → 以重新 grep 的实测为准，测试按实测写。
   - 本切片不依赖其他在途任务的产物（无上游阻塞）；若发现依赖 → 进入 waiting，**不伪造实测**。
2. **写集精确性**：只改 spec §5.1 写集表列出的文件——新建 `src/sentinel/` 下两个模块（monitoring-contract.ts / notification-policy.ts）+ 修改 runner.ts / sentinel-loader.ts / types.ts（条件）+ `tests/sentinel/` 新增测试。`git diff --name-only` 与实际改动**完全一致**。
3. **域约束（跨线红线）**：只碰 `src/sentinel/` + `tests/sentinel/`（+ 条件项 `tests/services/`）。
   - **不碰** `src/server.ts`（Claude 专属）、`src/routes/`、`src/agent/`、`app/`、`electron/`、`electron-renderer/`（均属 Win/Mac 他人域）——需要动 → **停手报 CTO**。
   - Win 侧写入 API / 沟通请求 UI / 周报渲染 = 别的任务，本切片只交付**可被 Win 消费的接口**（spec §5.2-F）。
4. **诚实 RED**：本切片不依赖 LLM/外部服务；若 DB 环境异常（better-sqlite3 不可用等）→ 测试如实标 ⏸/❌ + 理由（README + evidence 双处），**禁止伪造绿**。
5. **evidence 落盘规范**：测试输出（`npx vitest run` 原始输出）+ §7 的 grep 接线输出 + 时间戳落盘到 `docs/synova/product-lines/evidence/` 下（**用 .txt / .json，不用 .log**——证据 .log 被 .gitignore 静默忽略，D718 待修）；K3 独立重跑可复现；禁止只在 task-state 存单一副本。
6. **红线**：不碰 `scripts/audit/`（K3 专属）；不写审计标准；Stage 3 前零 DSH 依赖（不 `npm install @deepseek-ai/dsh`、不 copy DSH 代码）；提交**禁 `--no-verify`**、**禁 `git stash`**（铁律 0-3）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 的 DS1-DS12 逐项对照（DS11/DS12 归 CTO，其余归你）；声称 = 实现 + 验收（S-2），禁 overclaim；spec §5.2-H「行为等价性声明」逐条自证：除四条显式差异外不得有行为变化。
2. **不违反铁律**：接线完整（新 export 有**生产**调用点，测试调用不计 S-3 —— spec §8 表逐条 grep 实测）、降级诚实（24/31）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39）。
3. **无 bug**：spec §7 的 verify 命令逐条跑通 + `npx vitest run tests/sentinel/` 全绿（既有 case 零回归，尤其 threshold-injection / dedup-key-stability）+ pre-commit 全过（禁 `--no-verify`）+ 提交走 `git synova-commit`。
4. **接线完整**：`loadMonitoringContract` / `decideNotification` / 状态表三件套 / `listEscalationQueue` 各自有生产调用点（spec §8）。
5. **测试到位**：red→green 已证（实现前先跑出红）、L1/L2a/L2b/L2c 四层覆盖、expect 非空壳、边界含 24h±1ms 与幂等两段。
6. **其他**：残留清理（无死代码/旧引用 grep 零）、文件驱动（本切片不新增 manifest/tags；若新增 → 补 tags）、evidence 幂等可复现、`src/sentinel/` 内文件名与 spec §5.1 精确化对齐（实现提交后把目录级条目精确化为确切文件名）。

## 五、K3 审计提示（收尾要求）

- 本切片完成后**一次提审**：K3 审 spec 1 全量（loader 契约 / 决策矩阵 / 状态表 / 阈值层 / 接口）。
- 审计验收 = 验收点 **8-6**：机器证据（`test:tests/sentinel/...` 或场景）→ `pending_k3` → K3 复核 → `verified`；**8-3 的 K3 D580 credit 必须保留**（去重键不动，只降级窗口语义——这是创始人裁定，spec §5.4 决策 3/§1 权威①）。
- 完成后回填 `task-state/D716.json` 的 **impl 段**（commit + by + files[] + 每条 DS 的证据指针）；本 spec 的 spec 段已由 dev-doc 预置。
- 审计员会**独立重跑**你的验证命令——命令必须幂等、可复现、无本机假设（DB 用临时路径/`':memory:'`，不碰真实 `data/synova.db`）。

**开始吧。**
