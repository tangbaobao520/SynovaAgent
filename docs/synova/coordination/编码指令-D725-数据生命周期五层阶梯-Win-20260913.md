# 编码指令 — D660 数据生命周期五层阶梯（D725 → Win 线）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/问题总账与下一批派单-20260913.md §四 D727 行（CTO 台账；本单实际编号 D725，见下）
> 审计: Kimi K3 会盯着你的任务，五层阶梯实施完成后做最终审计（spec §10 DS1-DS10 + §7 全表）

> ⚠️ **编号说明（先看这条）**：CTO 台账把本任务记作 **D727**；按 D384 取号纪律走 alloc-task-id.sh 实际分配到 **D725**。**本指令与 spec 一律用 D725**；看到台账写 D727 指的是同一件事。

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D725 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D725-d660-data-lifecycle-20260913.md | **编码唯一契约**——§5.1 写集 / §5.2 五层阶梯 / §5.3 蒸馏判定 / §5.4 双键门 / §7 测试 / §10 DS |
| CTO 台账 | docs/synova/coordination/问题总账与下一批派单-20260913.md | 任务来源 + §一 C 表 D717-N4（本 spec 更正的那条失实表述） |
| 交接包格式先例 | docs/synova/coordination/派单-下一批四线并行-20260913.md §D716 | 本任务交接形态的来源 |
| 北星 | .claude/PRODUCT-BRIEF.md §一 / §二 / §六 P0 | 产品方向锚点（可溯源的证据链 = "靠数据不靠感觉"的物理前提） |
| D717 落地代码 | src/l3/evidence-retention.ts + tests/l3/evidence-retention.test.ts + src/deploy/bootstrap.ts | 被扩展对象（**PR #527，未合并 main**） |
| 前车之鉴 | docs/synova/coordination/问题总账与下一批派单-20260913.md §一 C（D717-N4） | 精度教训：**「提出」不得写成「裁定」**——你的交付报告同样受此约束 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority，含创始人三问逐字裁定）/ §4（Current State，实测现状 + 缺陷 D1-D6）/ §5（写集 + 五层阶梯 + 蒸馏契约 + 双键门）/ §7（测试四层）/ §10（DS1-DS10）——**spec 是唯一契约，声称即引用**。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 + §5.6 实现顺序 → 列出文件级改动清单 → 确认依赖前置（§三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`/`as never`/`as unknown as` = 0，铁律 38）、契约优先（新函数先写 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded + 分类错误码，铁律 24/31/32）、测试非空壳（`expect()` 断言 + 正常/降级/边界三路径，铁律 48，red→green）。

## 三、D725 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 当前工作区基线 = origin/main @2bc0f724 **+ PR #527**（feat/mac-d717-product-debt @d04d03cb）。**#527 未合入 main**。
   - 编码前置（铁律 0-3）：`git fetch --all && git pull --ff-only`，确认 **#527 已合 main**、且本 spec 分支已合 main 后再动工。
   - 合入后 **重新核验 spec §4 引用的全部行号**：src/l3/evidence-retention.ts 与 src/deploy/bootstrap.ts 均被 D717 改过，行号必漂移。**照 spec 旧行号写测试会红**（D524 教训：文档-实现漂移被 K3 C2 抓）。
   - 前置未满足 → 进入 waiting，**不伪造实测**。
2. **写集精确性**：只改 spec §5.1 写集表列出的文件（5 修改 + 5 新建，共 10 条）。`git diff --name-only` 与实际改动**完全一致**；**新增 src/ 下的文件时，把 §5.1 的目录级条目（src/l3/）同 commit 精确化为确切文件名**（S-6）。禁止改写集外文件。
3. **fail-closed 不可反（本任务最深的一条）**：任何判定失败路径必须返回"**不删**"。删除不可逆——判据不可判定时，唯一正确的默认值是不删。反向实现（失败即删）直接判审计 FAIL。
4. **L3 零 SQL（铁律 39）**：src/l3/ 下新增/修改的文件**零 db.prepare / db.exec**，所有 SQL 留在 L4 的 store（spec §9 硬验收；verify 命令：`grep -c "db.prepare\|db.exec" src/l3/evidence-retention.ts` = 0）。
5. **诚实 RED**：守卫测试（spec §7 B4/B5）与双键反向验证必须**真做**——人为改坏 → 贴红证据 → 撤回 → 贴绿证据。**禁止只用 happy path 冒充覆盖**。
6. **evidence 落盘规范（K3 独立复核）**：DS1-DS10 的物理证据（命令 + 原文输出 + 时间戳）落盘到 evidence 目录，K3 独立重跑可复现；禁止只在 task-state 存单一副本。
7. **红线（违反 = 事故）**：
   - **不碰 src/sentinel/ 下任何文件**（跨线边界，Mac DSH 领地——L1/L3 的归档实现归属主）；
   - **不碰 scripts/audit/**（K3 专属）；
   - **不碰 scripts/control-tower/ 与 scripts/workflow/**（CTO 域）；
   - 不改 .github/CODEOWNERS；
   - Stage 3 前零 DSH 依赖（不 `npm install @deepseek-ai/dsh`，不 copy DSH 代码）。
8. **defer 显式登记（防 M3 重演）**：spec §5.3 的 `recordDistillationRef()` 写入方**不在本任务写集**——这是**有意 defer**，不是遗漏。安全侧由 fail-closed 兜住（无引用 = 不删）。**你必须按 spec §10 DS8 在 board-backlog.json 登记两条 defer**（① 蒸馏引用写入方接线 ② L1/L3 归档导出管道），note 含 D725。**不要偷偷"顺手接上"**（那会扩大写集并越界 src/sentinel/）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 的 DS1-DS10 逐项对照，**声称 = 实现 + 验收**（S-2），禁 overclaim。特别核对三条可证伪点：① provenance 为空时 deleted 恒 0；② 只设 TIER 时不再删证据；③ enterprise_fact 无按时间物理删路径。
2. **不违反铁律**：接线完整（新 export 有**生产**调用点，**测试调用不计** S-3；spec §8 逐条 grep 实测）、降级诚实（24/31/32）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39）、dead code（37：`expireOld` 必须仍有 ≥1 生产调用方，否则 M3 复发）。
3. **无 bug**：spec §7 verify 命令逐条跑通 + `npx vitest run tests/l3 tests/l4 tests/architecture` 全绿（既有用例不破）+ **写集全量口径** `bash scripts/workflow/check-dev-doc-write-set.sh <spec>` 全绿（spec 期有 4 条"新建文件不存在"的预期命中，实现后必须清零）+ pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 每条 grep 出真实生产调用点，尤其 W5（`purgeExpired` 从零调用方 → ≥1 生产调用方）。
5. **测试到位**：red→green 已证（双键门改造前单键仍删 = 先红）、覆盖正常/降级/边界、`expect()` 非空壳、T1-T20 / W1-W6 / G1-G5 / B1-B5 逐条落地。
6. **其他你认为需要复核的点**：runbook（spec §5.1 的运维契约）是否含可复现命令 + 诚实声明（evidence 层当前为空表 → 恒不删）；`src/evidence/evidence-store.ts` 新建的 `evidence_refs` 表是否幂等；是否误引入 `as any`。

## 五、K3 审计提示（收尾要求）

- 五层阶梯实施完成后**一次提审**（K3 审 D725，报告覆盖 spec §10 DS1-DS10 + §7 全表 T1-T20 / W1-W6 / G1-G5 / B1-B5）。
- 审计验收 = DS1-DS10 逐条物理可复现；**审计员会独立重跑你的 verify 命令与 runbook 步骤**——所以脚本/命令必须幂等、可复现、无本机假设（例如不要写死某个临时路径）。
- 完成后回填 `task-state/D725.json` 的 **impl 段**（commit + by + files[] + evidence 路径），status → impl_done。
- **报告纪律**：写"创始人 2026-09-13 裁定 X"之前，先回 spec §1 权威① 逐字核对——**「提出」不得写成「裁定」**（D717-N4 的坑不重踩）。

**开始吧。**
