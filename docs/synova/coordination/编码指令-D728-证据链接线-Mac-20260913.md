# 编码指令 — 证据链接线（D728，Mac 线）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/派单-第三批-D727-D731-20260913.md §二（CTO 派单，D732）
> 审计: Kimi K3 会盯着你的任务，D728 完成后做最终审计（覆盖 D727 spec + D728 实现 + 同批 D729/D730）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| **D727 spec（本任务唯一契约）** | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D727-evidence-chain-20260913.md | 采集→落库契约 / expires_at 契约 / 溯源链契约 / 必答四问答案——**编码唯一依据** |
| D725 spec（边界，管"能不能删"） | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D725-d660-data-lifecycle-20260913.md | 引用表 `evidence_refs` 已由 D725 认领；蒸馏判定 / 双键门——**本任务不重复建、不改判据** |
| 派单 | docs/synova/coordination/派单-第三批-D727-D731-20260913.md | §一（D727）/§二（D728）/§七（依赖与串行规则） |
| 北星 | .claude/PRODUCT-BRIEF.md §一 / §二 / §三 / §六 P0 | 「靠数据不靠感觉」的物理前提 = 报告可溯源 |
| 前车之鉴 | 同 spec §4.4（缺陷分节）+ §1 权威⑤（d6390bd1「Wire Check ✅ 5 处引用」假接线先例） | 教训：grep 引用数 ≠ 生产接线；M3 机制建成未接线 |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority，权威原文）/ §4（Current State，全部实测含行号）/ §5.2-§5.7（契约）/ §7（测试要求）/ §8（接线验证表）/ §10（DS6-DS14 验收）。spec 是唯一契约，**声称即引用**——凡 spec 说不清的，回写 spec 而不是自行发明（S-6 同 commit 回填）。
2. **任务复杂 → 先 plan mode 再做**：读 §5.2 实施写集 + §5.3 缺口表 + §5.9 实现顺序 → 列出文件级改动清单 → 确认 §三-1 的前置 → **想清楚再动手，禁止没想清楚就改代码**。
3. **最高代码水平**：类型安全（`as any` / `as never` / `as unknown as` = 0，铁律 38 / CT-46）、契约优先（新函数先 JSDoc `@input` / `@output` / `@degraded`，铁律 47）、降级诚实（每个 catch 有 `log.warn/error` + `degraded` + 错误码，铁律 24/31/32）、测试非空壳（`expect()` 断言 + 正常/降级/边界三路径，铁律 48，red→green）、架构边界（铁律 39：L1 不得 import `/evidence/`，走 `engine-context` 注入结构类型）。

## 三、D728 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 行号漂移）**：
   - 本条 spec 基线 = **origin/main @cff43d6f**；`src/l3/` 下的 `evidence-retention.ts`（D717 引入，**属 #527，未合 main**）与 `src/deploy/bootstrap.ts` 的 D717 改动**只存在于 #527**。
   - 开工前置（缺一不开工）：`git fetch --all && git pull --ff-only` → **重新核验 spec §4.1 全部行号**（`bootstrap.ts` 与 `evidence-retention.ts` 行号必漂移，照 spec 旧行号写测试会红 —— D524 教训）→ 确认 **#527 是否已合**（未合则"时间谓词收敛"（§5.5）无处落地 → 停手上报 CTO，**不伪造**）。
   - **与 D725 实施（Win 线）串行**：两者共享 `src/deploy/bootstrap.ts` 与 `evidence-retention.ts` → 谁先合由 CTO 裁决，**不得并行**（派单 §七）。
   - 前置未满足 → 进入 waiting，如实上报；**禁止伪造实测**。

2. **写集精确性（D708 写集门禁）**：只改
   - spec §5.2 写集（`src/evidence/` 、`src/deploy/bootstrap.ts`（仅装配点）、`evidence-retention.ts` 、`tests/evidence/`）**+**
   - spec §5.3 缺口表 6 文件（`src/server.ts` 、`src/routes/diagnosis.ts` 、`src/routes/conversations.ts` 、`src/agent/conversation-engine.ts` 、`src/agent/diagnosis-launcher.ts` 、`src/agent/engine-context.ts`）——**这 6 个文件的扩写集已随 spec 提交 CTO 裁决**（§5.3 方案 A）；若 CTO 未批准，先做 §5.2 内的机制部分并如实标注端到端验收 ⏸（不得用 mock 冒充生产接线）。
   - `git diff --name-only` 与实际改动**完全一致**；禁改写集外文件、禁"树终验声称不符"。

3. **诚实 RED（本任务关键）**：
   - spec §5.6 的 **R2 反向验证必做**：把注入点改回 `null` → 真跑一次 consult → 观察红（`evidenceRefs` 空）→ 改回 → 观察绿。**两次原始输出都要贴**。只贴绿 = 未接线证明（S-3）。
   - 无 LLM Key / 前置未合 → 相应 DS 如实标 ⏸/❌ + 理由（报告 + evidence 双处），**禁止契约断言冒充全链路**。

4. **零静默降级**：新增/改动的每个 catch 必须有 `log.warn/error` + degraded 标记 + 错误码（`EVIDENCE_WRITE_FAILED` / `EVIDENCE_RETENTION_UNRESOLVED` / `EVIDENCE_QUERY_FAILED`）；**证据写失败绝不打断 GA 访谈主循环**（fail-soft 于采集），**判定失败绝不删证据**（fail-closed 于删除）。

5. **evidence 落盘规范（K3 独立重跑）**：`sqlite3` 原始输出（§5.6 段①-④ + R1/R2）落盘到本次 evidence 目录，含时间戳与命令原文；K3 可原样重放；禁只写 task-state 单副本。

6. **红线（违反 = 事故）**：不碰 `scripts/audit/`（K3 专属）；不碰 `.github/CODEOWNERS`；不碰 `src/sentinel/runner.ts` / `src/l3/expert-*` / `src/l3/quality-firewall.ts`（spec §6 跨线边界，第 2-4 路分裂本批只登记不修）；不改 D725 的删除判据与双键门。

7. **环境坑**：本机 DB = `data/synova.db`（生产库，**55 张表、无 evidence 表**）；在 **worktree 内**跑 `sqlite3 data/synova.db` 会**静默创建空库**（`data/` 被 `.gitignore` 忽略 → `sqlite3` 新建 0 字节文件，实测踩过）——查库前先确认路径指向主工作区的真库，或显式 `sqlite3 <绝对路径>`。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 的 **DS6-DS14 逐项对照**，声称 = 实现 + 验收（S-2），禁 overclaim；每项附原始输出。
2. **不违反铁律**：接线完整（spec §8 表**逐行 grep 出真实生产调用点**，测试调用不计 S-3；`grep -rn "new EvidenceStore" src/ | wc -l` = **1**）、降级诚实（24/31/32）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39，L1 不 import `/evidence/`）。
3. **无 bug**：spec §7 的 verify 命令逐条跑通 + `npx vitest run tests/evidence/` 全绿（**且不破既有 `tests/evidence-pool.test.ts` / `tests/l3/evidence-collector-wiring.test.ts` / `tests/orchestrator/l3-wiring.test.ts`**）+ pre-commit 13 组全过（**禁 `--no-verify`**）+ 提交走 synova-commit（**禁 `git stash`**，铁律 0-3）。
4. **接线完整**：§5.6 R2 红/绿双证据 + §8 每条 grep 实证（含反向：全仓只允许一处 `new EvidenceStore`）。
5. **测试到位**：red→green 已证（改前用例先红，且改前红的原因是**表不存在 / 注入为 null**，不是断言写错）、覆盖 L1/L2a/L2b/L2c、`expect()` 非空壳。
6. **溯源三段查询**：§5.6 段①-④ 在真库真跑过（K3 会重放）；R1 悬空引用被拦（非零退出）。
7. **其他你认为需要复核的点**：残留清理（`expireOld` 口径改完后旧签名/旧注释零残留）、契约头（铁律 47）齐全、`expires_at` 单点派生（全仓仅一处写）、§5.7 与 D725 的 4 个接口点未被打穿。

## 五、K3 审计提示（收尾要求）

- D728 完成后**一次提审**（K3 报告覆盖 D727 spec + D728 实现 + 同批 D729/D730）。
- 审计验收 = spec §10 的 DS6-DS14 全部有原始输出；**反向验证 R1/R2 是重点**（K3 会独立重跑）。
- 回填 `task-state/D728.json` 的 **impl 段**（commit + by + files[]），并附 DS 证据原文。
- 审计员会**独立重跑你的验证命令**——命令必须幂等、可复现、无本机假设（DB 路径、LLM Key、`#527` 合并状态都在 spec §4.1/§5.9 标注过，跑前先读）。

**开始吧。**
