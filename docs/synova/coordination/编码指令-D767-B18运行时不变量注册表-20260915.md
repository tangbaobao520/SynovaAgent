# 编码指令 — D767（DSH 借鉴卡 B-18：运行时不变量注册表）

> 生成: 2026-09-15 | 执行方: **并行 CTO session（预设 `synova-cto`）** | 域: **win**（`src/infra/**`，ownership.yaml 实测）
> 依据: 创始人 2026-09-15「win 不在身边，涉及 win 工作范畴的一并安排给 Mac 侧执行」+ 派单 `派单-D760-D769-DSH借鉴卡-20260915.md` §二第 1 条
> 上游: `docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md` §4（B-18 卡）+ 同目录《附录B-DSH运行时机制深潜-20260904.md》§202（dsh-invariants 锚点）
> 计划锚定: `整体推进计划-主线-20260913.md` v1.3（此单为 infra 类，不计入 line-1）

---

## 一、先读这些（读完再动手）

| 材料 | 路径 | 读什么 |
|---|---|---|
| 卡定义 | `DSH借鉴指引-v2-20260904.md` §4 B-18 行 | 借鉴物 = `InvariantRegistry`；落点 = `src/infra/**`；产品缺口 = Agent 运行底座；预期收益 = 把「检查可选、违约必炸」带到 Synova 运行时 |
| DSH 锚点 | `附录B-DSH运行时机制深潜-20260904.md` §202 | `class InvariantRegistry extends Service`（123 行）+ `InvariantError`（`code="INVARIANT"` + 包归属）；companion 插件登记检查；`package_allowlist/blocklist` + `enabled` 开关；普通包入口零诊断依赖 |
| 本任务台账 | `task-state/D767.json` | 状态 / 域 / 硬守卫 |
| 相关铁律 | `CLAUDE.md` 铁律 11（静默降级禁止）/ 24（catch 三问）/ 31（降级信号传播）/ 39（五层边界） | 这三条正是要**在运行时**被不变量检查覆盖的对象（现在只有 pre-commit 静态检查） |
| 前车之鉴 | `docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md` M1（fail-open 静默失效）/ M3（机制建成未接线） | 机制类任务最常见的两次翻车：没接线、静默降级 |

## 二、执行要求（创始人标准，逐条做到）

1. **认真阅读**上面全部材料，然后执行任务。
2. 任务复杂 → **先用 plan mode 做好计划再执行**；禁止没想清楚就改代码。
3. 做到你的**最高代码水平**：`as any`/`as never`/`as unknown as` = 0（铁律 38）；新模块先写契约 JSDoc（输入/输出/降级，铁律 47）；测试覆盖正常/降级/边界三路径（铁律 48）。
4. **先想清楚再动手**；改前 `grep` 引用面（`scripts/workflow/grep-refs.sh`），不靠记忆。

## 三、任务专属硬约束（违反 = K3 审计 FAIL）

1. **依赖前置 + 基线核验**：基线 = `origin/main` 最新；开工前 `git fetch --all`，用 `git worktree add .synova-wt-d767 -b feat/d767-invariant-registry origin/main` 隔离（禁 `git stash`，铁律 0-3）。若 `src/infra/**` 有在途 PR，先确认无写集交集（同域并行度=1）。
2. **写集机器生成**：`bash scripts/control-tower/declare-write-set.sh --brief <brief> --base origin/main`（禁手写路径），`git diff --name-only` 与写集**逐字一致**。
3. **零 DSH 依赖（G1/G5）**：`grep -rn "@deepseek-ai" src/ packages/ --include="*.ts" --include="*.json" | grep -v node_modules` → 零结果；**借鉴 = 读 DSH 源码自研**，不装包、不复制代码。
4. **可回退（G4）**：注册表必须带 enable/disable 开关，默认关闭；打开后违约必须**炸**（不可降级吞掉）——这是本卡与铁律 11 的分界：**不变量违约不是降级场景，是错误**。
5. **接线完整（G2/铁律 0-2）**：新 export 必须有**生产**调用点（`src/infra/**` 之外的真实入口），测试调用不计；给出 `grep -rn "<符号>" src/` 的原始输出。
6. **不碰红线**：`scripts/audit/**`（K3 专属）/ 审计标准 / `.github/workflows/ci.yml` 一律不动；`scripts/control-tower/**` 若确需改动，需在 brief Q2 显式声明。
7. **诚实 RED**：任何一步跑不过就如实写 ❌/⏸ + 理由（PR 正文 + task-state 双处），禁伪造绿。

## 四、验收（本卡最小可用切法，先窄后宽）

**做什么（建议范围，可在 plan mode 中收窄但不可缺项）**：
- `src/infra/invariant-registry.ts`：`InvariantRegistry`（register / 按包归属 / allowlist-blocklist / enabled 开关）+ `InvariantError`（`code='INVARIANT'` + `packageName`）
- 至少 **2 条真实不变量**接入并可在生产被触发（建议从三处自选其二）：
  ① **降级信号在场性**（铁律 31）：返回 `degraded: true` 的路径必须伴随 `log.warn/error`（`invariant-degradation-visible`）
  ② **跨层调用**（铁律 39）：L1/L2 不得直连 L4/L5 的运行时断言（与 `scripts/check-architecture.sh` 的静态口径同源）
  ③ **五层边界**或**静默吞错**的运行时实例
- 测试：`tests/infra/invariant-registry.test.ts`（正常注册/违约必炸/开关关闭时静默通过/allowlist 过滤/边界：重复注册同 key）

**不做什么**：不做全量不变量目录（后续卡增量登记）；不改 CI 门禁；不改 `scripts/audit/**`；不引入 DSH 包。

**Done 标准（可证伪）**：
- [ ] `npx vitest run tests/infra/invariant-registry.test.ts` 全绿（含「违约必炸」用例）
- [ ] 反例证明：临时去掉某条不变量的注册 → 对应断言红；恢复 → 绿（两次原始输出入 PR）
- [ ] 生产接线：`grep -rn "InvariantRegistry\|registerInvariant" src/ | grep -v test` ≥1 个真实调用点
- [ ] G1 零 import 命令零结果；`bash scripts/pre-commit-check.sh` 全绿；提交走 `synova-commit`（禁 `--no-verify`）

## 五、复核清单（做完逐项自查，K3 会逐条跑）

- 与卡定义一致：指引 §4 B-18 的三要素（注册表 / 包归属 / 开关）都在，且**违约必炸**未被降级
- 铁律：24+31（**注意**：不变量违约属错误不属降级，若你的实现把它吞成 warn 即违规）/ 38 / 39 / 46 / 47 / 48
- 接线完整：生产调用点真实存在（不是测试里调）
- 测试到位：red→green 已证、三路径覆盖、`expect` 非空壳
- 无残留：死代码 grep 零；旧实现删除并有零引用证明
- 产物可复现：测试幂等；`enable/disable` 开关默认关闭不影响存量行为

## 六、审计与回报口径

- 提审：一次提审覆盖本卡（D767）；K3 独立重跑上述 Done 命令
- task-state：交付后回填 `impl`（branch / files / evidence 4 条：红→绿→反向→接线）
- PR：单域（win）、≤12 文件；PR 正文贴「声称 ↔ 命令 + 原始输出」表（计划 §五 原则 8：无证据 = 未完成）

**开始吧。**
