# 编码指令 — 线 3 报告体系（一页纸四槽位 + 各维度循环结论 + 结论可溯源）（D791）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: `docs/synova/coordination/派单-批六首批三卡-20260916.md` 卡 2（CTO 派单，域: win）
> 审计: Kimi K3 会盯着你的任务，线 3 的 3-1/3-7 完成后做最终审计（spec 载体完整性 + GS-08 独立重跑 + 指针审计可复现）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D791 spec | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D791-line3-report-onepager-20260917.md` | **编码唯一契约**（11 节 + 写集表 + DS1-DS14） |
| 派单 | `docs/synova/coordination/派单-批六首批三卡-20260916.md` 卡 2 | 切片定义 / 硬约束 / 验收口径 |
| 北星 | `.claude/PRODUCT-BRIEF.md` §一/§二/§三 + `docs/synova/coordination/整体推进计划-主线-20260913.md` §一（支柱②「报告可溯源」） | 产品方向锚点（老板 10 分钟看懂 + 报告可溯源） |
| 验收点原文 | `docs/synova/product-lines/product-lines.yaml` 线 3（L134-173） | 3-1 / 3-7 定义 + evidence 绑定（`scenario:GS-08`） |
| 前车之鉴 | `docs/synova/coordination/编码指令-D774-evidence-pipeline-20260915.md`（evidence 落盘规范）+ spec §4 缺陷 A/B（**绿了不算分**的实测根因） | 教训清单（写集漂移 / id 不匹配 / 假绿） |

---

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority，含 DSH 基线更正）/ §4（Current State，全部 file:line 实测）/ §5（写集 + 槽位契约 + 指针文法 + 循环派生契约 + GS-08 改造 + 兑换链路）/ §7（测试 L1-L2c）/ §8（Wiring Verification）/ §10（DS1-DS14）——**spec 是唯一契约，声称即引用**。
2. **任务复杂 → 先 plan mode 再做**：读 §5.1 写集表 → 逐文件列改动清单（含每个新 export 的生产调用点）→ 确认 §三-1 依赖前置 → 再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any` / `as never` / `as unknown as` = 0，铁律 38）、契约优先（新函数先 JSDoc `@input/@output/@degraded`，铁律 47）、降级诚实（每个 catch 有 `log.warn/error` + degraded 语义，铁律 24/31）、测试非空壳（`expect` 断言 + 正常/降级/边界三路径，铁律 48，red→green）。
4. **零新指标**（spec §5.4/DS8）：S3 的内容**只取**既有 `DashboardRow` 字段（cycleName / currentOverflow / unit / trendArrow / trendDirection / maturityLabel）；**禁止**计算任何新比率/评分/阈值，禁止把 `hasOverflow` 包装成新指标名。
5. **不用 `--no-verify`**；提交走 `bash scripts/control-tower/synova-commit`；**禁 `git stash`**（铁律 0-3）。

---

## 三、D791 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - spec 基线 = `origin/main @ 6a06e853`（worktree `.synova-wt-d791`）。开工前：`git fetch --all && git pull --ff-only`，确认无 `[behind N]`。
   - 若 **D790（卡 1，度量口径修复）** 已合入 main：它改了 `scripts/product-lines/calc-progress.py` 的同日失效语义（date-only → 时间戳比较）⇒ 重新核验 spec §5.5 的「同日失效规避」是否仍需要 `at` 字段，并在 README/证据 quote 写明实际口径。**不得**为了迁就口径改判据（DS13 唯一判定 = `3-1`/`3-7` 状态 ∈ {`pending_k3`}）。
   - 若 spec 引用的行号因上游合并漂移（重点：`src/routes/diagnosis.ts`、`src/agent/report-assembler.ts`、`src/l3/report-templates.ts`、`scripts/product-lines/calc-progress.py`）→ **以重新 grep 的实测行号为准**，并把漂移写进 README（D524 教训：照旧行号写测试会红）。
2. **写集精确性（S-2/D286/D316）**：只改 spec §5.1 写集表列出的文件（6 修改 + 3 目录级新建 = 实际落盘 **12 文件上限**，D734）；`git diff --name-only` 与实际改动**完全一致**；新建文件已用**目录级条目**声明（`src/agent/` / `tests/agent/` / `scripts/golden-scenarios/GS-08-report-readable/`）⇒ **实现同 commit 把目录级条目精确化为确切文件名并回改 spec §5.1**（S-6 回填义务）。**禁止**再加第 13 个文件（超限请停手报 CTO 拆单）。
3. **不许改的边界（违反 = 事故）**：
   - **不改** `tests/agent/report-assembler.test.ts`（D480 既有 5 用例 = 回归红线；新槽位设计已保证不改既有输出字符串。若实现发现**必须**改它 → 停手，回报 CTO，不得自行扩张写集）。
   - **不改** `src/cycles/` 下任何文件（纯复用，改了 3-7 的「既有」就变成「新造」）。
   - **不改** `scripts/product-lines/` 下任何文件（只调用 `evidence-writer.py` / `calc-progress.py`）。
   - **不改** CI（`.github/`）、**不碰** `scripts/audit/`（K3 专属红线）、**不写**审计标准、**不自我审计**。
   - **零 DSH 依赖**：不引 `@deepseek-ai` 运行时依赖、不复制 DSH 代码、**不引用**旧 DSH 图纸（`docs/synova/research/DSH迁移施工图-20260820/`、`DSH源码锚点映射-20260907/`）里的 DSH 侧描述——本机实装已到 `0.1.5-rc.2`（241 包），旧图纸描述已过期（spec §1 权威⑥ 有实测命令）。
4. **架构门禁零新增违规（spec §4 环境实测）**：
   - `src/agent/` 新文件**禁止**写 `from '../l4/...'`（`scripts/check-architecture.sh:44-48` 对 L2→L4 无类型位置豁免，白名单仅 8 个既有文件）。正确形态：`type GraphStoreLike = import('../l4/graph-bridge').GraphStore;`（先例：`src/agent/loop-handlers.ts:670`）+ 边界一次 cast（同款注释说明）。
   - `src/routes/diagnosis.ts` 取 store：`req.app.locals.graphStore`（`src/server.ts:282` 注入），类型用 `type X = import(...)` 位置（`:92-95` 类型位置豁免）。
   - 完成前必跑 `bash scripts/check-architecture.sh` → 必须仍是「全部通过 ✅」（不得新增任何命中）。
5. **诚实 RED（不许假绿）**：
   - 本任务**不跑**真实 LLM 六阶段诊断（非确定性）。GS-08 用**fixture 报告 + 真实 checkpoint 归档行 + 真实生产渲染/读取路径**。⇒ 必须在**两处**（`scripts/golden-scenarios/GS-08-report-readable/README.md` + 点级证据的 `quote` 字段）标注「契约级：生产渲染与读取路径真实，LLM 产物为 fixture」。
   - **禁止**把契约断言冒充「全链路 LLM 诊断已验」；断言 `error` 态（查询失败）**不得**当通过（assert.ts 三态）。
6. **evidence 落盘规范（K3 可独立重跑）**：
   - 机器断言产物：`scripts/golden-scenarios/evidence/GS-08-<date>.json`（assert.ts 默认面）。
   - **点级证据（3-1/3-7 记分的唯一路径）**：`docs/synova/product-lines/evidence/scenario-<date>.json`（calc-progress 唯一扫描面，spec §4 缺陷 A/B 实测）。生成方式见 spec §5.5——`evidence-writer.py` + 补 `at`（全量 ISO 带时区，先例 `docs/synova/product-lines/evidence/d592-e2e-2-1.json`）；`at` 必须**晚于** `src/l3/` 最后一次提交时刻。
   - 全部审计工件（`onepager.md` / `onepager-degraded.md` / `cycle-slot.md` / `pointer-audit.json` / `dimension-audit.json` / `onepager-meta.json`）落盘并被断言引用；**禁止**只存 task-state 单副本。
   - 证据里写清**复跑命令**（`bash scripts/golden-scenarios/GS-08-report-readable/run.sh`）与 commit 短哈希。
7. **环境坑（实测注记）**：
   - 场景脚本在 Git Bash（Windows）/ bash（macOS）双端可跑：`set -euo pipefail`、`python3 -c` 取 JSON 字段、`node -e`/`npx tsx`；**禁** `taskkill //IM node.exe`、**禁** `cp data/synova.db`（铁律 0-4，用 `fresh-db.ts`）。
   - 输出编码：脚本/证据文件一律 UTF-8；中文 JSON 断言用完整子串（含 `[degraded]` / `### 各维度循环结论` 等**字面**固定串，勿改标点）。
   - dev-doc-gatekeeper C2 的路径提取器会吞「反引号/CJK/`:行号`」⇒ 若你在 spec 里补写路径，**路径后必须紧跟空格或 `)`**（新文件用目录级条目声明）。
   - 确定性：一页纸**禁含渲染时刻**（`new Date()`/`now()`不得进 markdown），否则 `deterministic` 断言与幂等重跑必红。

---

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：spec §10 的 **DS1–DS14 逐项对照**，声称 = 实现 + 验收（S-2），禁 overclaim；每条 DS 附「命令 + 原始输出摘要」；缺项显式标 ⏸/❌ + 理由（禁跳号 / 禁静默缺项）。
2. **不违反铁律**：接线完整（§8 每条 grep 出**生产**调用点，测试调用不计 S-3）、降级诚实（24/31）、类型安全（38）、契约优先（47）、测试非空壳（48）、架构边界（39/46）。
3. **无 bug**：spec §7 的 L1 用例全绿（`npx vitest run tests/agent/report-onepager.test.ts`）+ **D480 既有 5 用例仍全绿**（`npx vitest run tests/agent/report-assembler.test.ts`）+ 全量 `npx vitest run` 无新增失败（写明与 main 基线的差值）+ `bash scripts/pre-commit-check.sh` 13 组全过（**禁 `--no-verify`**）+ `synova-commit` 提交（**禁 `git stash`**）。
4. **接线完整**：§8 表逐条 grep 命中（`buildCycleConclusions` ≥2 处生产传递 / `report-onepager-trace` 双文件命中 / `getSentinelExpertReports` 新消费点 / `ReportData` 新字段双文件命中）。
5. **测试到位**：red→green 已证（spec §7 对照表逐行核：改造前必红的项目实测红过）、三路径覆盖、`expect` 非空壳；负向断言 3 条真实生效（断言 8/10-11/12）。
6. **证据可复现**：`run.sh` 幂等（连跑两次 exit 0，证据按日期/序号不覆盖）；K3 独立重跑能得到同一组工件（无本机假设、无残留临时目录）。
7. **兑换闭环**：`python3 scripts/product-lines/calc-progress.py` 后，线 3 的 `3-1`/`3-7` 状态 ∈ {`pending_k3`}（**这就是本任务的靶心**；若判 `stale` → 按 §5.5 顺序重跑，不得改口径）。
8. **其他你要复核的点**：残留清理（`grep -rn "dist/l3/report-template-loader" scripts/golden-scenarios/` 零命中——旧死代码已删，铁律 37）；写集对账 `bash scripts/workflow/check-dev-doc-write-set.sh <spec>` 漂移 0；`git diff --name-only` 与 §5.1 完全一致。

---

## 五、K3 审计提示（收尾要求）

- **一次提审**：D791 完成后一次提审，报告覆盖 `3-1` + `3-7` + 追加入库路径（`docs/synova/product-lines/evidence/scenario-<date>.json`）。
- **审计验收口径**（可独立重跑）：`bash scripts/golden-scenarios/GS-08-report-readable/run.sh` → exit 0；`scripts/golden-scenarios/evidence/GS-08-<date>.json` 的 `verdicts[].acceptance_point` 含 `3-1`/`3-7`；点级证据含 `at`；`product-progress.json` 线 3 的两点 = `pending_k3`。
- **task-state 回填**：完成实现后回填 `task-state/D791.json` 的 **impl 段**（`commit` / `by` / `at` / `files[]`）+ `status: impl_done`，并在 `spec` 段保留本 spec 路径（dev-doc 已预置）。
- **审计会独立重跑你的脚本**：脚本必须幂等、可复现、无本机假设（临时目录自清理、端口随机、JWT 自举）；证据原文入 git，不写"我跑过了"。
- **审判红线提醒**：`scripts/audit/**` 你一个字都不能碰；审计结论只认 K3 报告，你的自检 ≠ 审计。

**开始吧。**
