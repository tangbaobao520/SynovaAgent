# D808 后续指令包（K3 审计 + 4 张派卡）· 2026-09-17

> 产出: CTO（Mac DSH）| 配套交付: PR #627（win 域文档件）+ PR #628（mac 域登记件）
> 用途: 创始人**直接复制**给对应 session 执行；每条自带任务号/域/执行方/先读材料/做什么/不做什么/验收/回报口径
> 被审/被派主件: `docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md`（§10 合规口径 / §11 G3 物理化 / §12 落点归属 / 附录 C）

---

## 一、K3 审计指令（复制给 🔍 synova-k3-audit session）

```
【K3 审计任务】D808 — DSH 借鉴标准更新（治理文档）
域: win（文档件）+ mac（登记件）｜执行方: CTO（Mac DSH）｜审计方: K3（模型 kimi-k3，独立工作区 /Users/wane/Synova-k3独立审计）
红线: 不改被审文件、不写审计标准、禁止自我审计；结论只认 K3 报告

【审计对象（以 git 事实为准，禁以本指令转述为准）】
- PR #627 → 分支 docs/d808-dsh-borrow-standard（win 域，5 文件）
- PR #628 → 分支 docs/d808-ledger-mac（mac 域，2 文件）
- 主件: docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md（138 → 336 行）
- 簿记: .claude/task-briefs/D808.md、memory/notes/implemented/2026-09-17-D808-dsh-borrow-standard.md、task-state/D808.json

【L1 事实核验（逐条给 file:line）】
A1 三节是否**原址**更新（未新建目录/未新建并列文档）: git diff --name-status origin/main...HEAD
A2 三条元断言是否**逐字**（M1/M2/M3）: 抽文档内三个 ```text 代码块，与本文 §四 原文比对 → 期望 byte 级相同（改写/重排/合并 = 不通过）
A3 附录 C: 是否 10 行；每行是否给出「本文条款 ↔ 元断言 id」+ 一致性判据；核验命令是否真实可跑（stub V02 实跑 → 本指引 2 / V1 v0.2 1）

【L2 物理断言复核（每条给命令原文 + 原始输出）】
B1 卡零绑定证据: grep -rln "借鉴\|B-0\|D587\|D593\|D598\|D599" docs/synova/product-lines/evidence/ → 期望仅 README.md；41 个 pass 点是否不含 21-3 / 20-3
B2 落点零归属: product-lines.yaml 是否真的无任何线 modules 含 src/llm|src/store|src/config|src/services
B3 pathspec 空转: git log --oneline --name-only -- providers/（期望零输出）vs -- src/providers/（期望命中 D586/D598）
B4 旧锚点存量: grep -rln "deepseek-harness/packages\|D:/deepseek-harness" src/ --include="*.ts" → 期望 4 文件
B5 现行安装版本 + 现验 demo: 包 deepseek-harness-pkg = 0.1.6-alpha.1；dsh-llm lib/types/error.js 现验命中 CONTEXT_WINDOW_EXCEEDED_CODE

【L3 红线】
C1 scripts/audit/**、src/**、scripts/** 零改动（git diff --name-only origin/main...HEAD）
C2 判据源零改动: docs/synova/product-lines/product-lines.yaml、docs/synova/project/26线-V1验收标准-*.md
C3 无自我审计（审计报告须由 K3 出）

【L4 反证（至少 1 条，抓 M2 型 overclaim）】
D1 §11.3「已落代码」7 张卡（B-01 D586 / B-02+B-06 D593 / B-03 D598 / B-04 D587 / B-05 D594 / B-07 D588 / B-09+B-10 D599）是否逐一可复现（git log -- <落点>）
D2 「B-01…B-20 回写 0/20」是否可反证（若任一卡有绑定证据 → 结论错）

【交付】
- 报告落 docs/synova/audit-reports/（K3 域）；task-state/D808.json 回填 audit.verdict + 报告路径
- 每条结论必须含 file:line + 命令 + 原始输出；无证据的结论视为未审
- 已知观察（请判定）：① 外部审计器 D202 对本卡输出 P0/P1/P2=0 却判「交叉对比不一致」（0 问题 vs 声称通过被比较器判矛盾）② pre-push 黄金案例门禁在环境失败时报「诊断质量退解冻」（D718 §⑤ 同型第 2 次）
```

## 二、四张派卡（复制给对应编码/dev-doc session 的开头）

**卡 1（P1）B-01…B-10 借鉴卡回写 26 线验收点 + 绑定证据（M2 物理化）**
```
任务: 把已落代码的 7 张借鉴卡回写进 26 线验收点并生成绑定证据（决策依据: DSH借鉴指引 §11）
现状材料: src/llm/{token-meter,tool-result-pruner,retry-middleware,timeout}.ts、src/store/session-projection.ts、src/agent/context-compaction.ts、src/config/{config-layers,customer-config-package}.ts、src/errors/types.ts、src/providers/base.ts（全在 main）
做什么: ① 按 M1 口径把落点写成可判定验收点（「接入/配置/约束 DSH 的 <能力> 并验证」）② python3 scripts/product-lines/evidence-writer.py --type test --verdict pass --points <点id> --source <测试> --quote <佐证> ③ 核验 evidence json 的 verdicts[].acceptance_point
不做什么: 不改 scripts/audit/**；不改判据源（26线-V1验收标准）；不改 scripts/product-lines/** 逻辑（除非发现缺陷→另立卡）
验收: evidence 目录出现绑定记录（grep 点 id 命中）；点 id 与 product-lines.yaml 一致；calc-progress/ledger 读数变化可复现
回报: 每卡 → 验收点 id + 证据文件路径 + 命令输出
```

**卡 2（P1）落点归属落 product-lines.yaml（含 providers/ → src/providers/ 修正）**
```
任务: 按 DSH借鉴指引 §12 把借鉴落点归属写进 26 线 modules（决策依据 §12.1 R1-R6）
现状材料: src/llm（7 文件）/ src/store（6）/ src/config（2，B-09/B-10 落点）/ src/services（30）均零归属；yaml 线20、线21 的 "providers/" 实测 git pathspec 零命中
做什么: ① 定归属线（需 CTO+线主裁决: src/llm→线21 辅线20；src/store→线20；src/config→线23/25 待裁）② 追加 modules 值 ③ 把 "providers/" 改成 "src/providers/" ④ src/services 按文件粒度（示例: deepseek-balance.ts→线21、audit-service.ts/db-encryption.ts→线24）
不做什么: 不改线集/分母（只有创始人能改）；不整目录挂靠 src/services
验收: git log -- <modules 值> 对每个新值有真实命中（paths 有效）；改后 A1 惰性失效对相关线真实生效（构造一次变更验证转黄）
回报: modules 改动清单 + 每条 pathspec 实测输出
```

**卡 3（P2）4 文件 DSH 锚点按 M3 补齐（包+文件+符号 + 现验命令）**
```
任务: src/llm/tool-result-pruner.ts、src/llm/timeout.ts、src/agent/context-compaction.ts、src/store/session-projection.ts 的锚点注释按 M3 重写
现状材料: 4 文件现锚点为旧口径（Win 路径 D:/deepseek-harness/packages/... + 0.1.1-rc.2，无现验命令）；现行安装 deepseek-harness-pkg 0.1.6-alpha.1
做什么: 按 DSH借鉴指引 §10.3 模板写「包 + 文件 + 符号 + 现验命令 + 抄写日期/版本/行为差异」，行号现查（D797 锚点三原则）
不做什么: 不改实现逻辑（仅注释）；不改产品行为
验收: 每个注释的现验命令实测输出非空（文件 ✓ 行数 + 符号命中）；grep 旧 Win 路径 = 0
回报: 4 文件现验命令输出
```

**卡 4（P1）pre-push 黄金案例门禁三态退出码（控件塔域）**
```
任务: scripts/pre-push-check.sh 的 golden-case F1 阶段需区分「检查未执行 / 检查发现退化 / 检查通过」
现状材料: :276 npx tsx scripts/ci/golden-case-checker.ts；本机 ~/.npm/_cacache 含 root-owned 文件 → npm EPERM → 门禁输出现为「诊断质量退解冻, 见上方 diff」（把未执行报成发现退化）；D718 §⑤ 同型第 2 次（按红线已升级创始人）
做什么: 三态退出码（0/1/2）+ 文案区分 + 未执行时给出环境修复指引；配对测试覆盖三态
不做什么: 不放宽判据（发现退化仍硬阻断）
验收: 三态各有用例；环境失败不再输出「质量退解冻」；配对测试全绿
回报: 测试输出 + 三态实测
```

> 备注：卡 1/卡 2 触及 `product-lines.yaml` 与 evidence 目录，写集不重叠可并行；卡 3 纯注释；卡 4 属 scripts/pre-push-check.sh（控件塔域，改前加载 ctrl-tower-change + windows-compat）。
