# 编码指令 — L1 桌面端 切片 C（D527/D528）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/派单-L1切片C-D527-D528-20260825.md（CTO 派单）
> 审计: Kimi K3 会盯着你的任务，切片 C 完成后做最终审计（覆盖 D527-D528 + 1-8 复核）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D527 spec | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D527-first-diagnosis-e2e-20260825.md` | 首诊旅程端到端（验证点 1-6）——**编码唯一契约** |
| D528 spec | `docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D528-upgrade-data-retention-20260825.md` | 升级/重装不丢数据（验证点 1-7）——**编码唯一契约** |
| 派单 | `docs/synova/coordination/派单-L1切片C-D527-D528-20260825.md` | 切片定义/写集约束/审计口径 |
| 北星 | `.claude/PRODUCT-BRIEF.md` §二/§六 + 施工图 `docs/synova/research/DSH迁移施工图-20260820/DSH迁移施工图-20260820.md` §3.1 | 产品方向锚点 |
| K3 切片 A 审计 | `.wt-sliceB-specs/docs/synova/audit-reports/2026-08-25-D517-D519.md` | 前车之鉴（M7 漂移/P2-2 指纹落盘/G3 物理实测） |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读**两个 spec 的 §1（Authority）/ §4（Current State，实测现状）/ §5（写集）/ §7（测试）/ §10（DS 验收）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §5.1 写集表 + §12 骨架 → 列出你的改动清单（文件级）→ 确认依赖前置（见 §三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`=0，铁律 38）、契约优先（新函数先 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded，铁律 24/31）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，red→green）。

## 三、切片 C 专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 D524 M7 漂移）**：
   - 当前工作区 = feat/d505-impl（D504 基线），**切片 A/B 未合入 main**。
   - 编码前置（铁律 0-3）：`git fetch --all && git pull --ff-only`，确认切片 A（feat/slice-a-d517-d519）/ 切片 B（feat/d522-service-boot-teardown）已合 main 后再动工。
   - 合入后 **重新核验 spec 引用的行号/契约**（main.cjs / backend-spawn.cjs / build-synova.cjs 均被切片 A/B 改过，行号必漂移）——照 spec 旧行号写测试会红（D524 教训：文档-实现漂移被 K3 C2 抓）。
   - D527 依赖切片 B（桌面端能开 + 服务自启 + prod 契约 backend.mjs）；D528 依赖切片 A（安装包产物 + mac-install-verify.sh）。**前置未满足 → 进入 waiting，不伪造实测**（派单 §给 dev-doc 的交付要求 3）。
2. **写集精确性（切片 B merge 树事故教训）**：只改 spec §5.1 写集表列出的文件（D527 12 条 / D528 5 条）；`git diff --name-only` 与实际改动**完全一致**；禁止改写集外文件、禁止"树终验声称不符"重演。
3. **诚实 RED（派单 §给 dev-doc 的交付要求 4）**：consult 六阶段依赖 LLM——LLM key 不可用 / 切片前置未合入时，相应 DS 如实标注 ⏸/❌ + 理由（README + evidence 双处），**禁止伪造全链路绿、禁止契约断言冒充全链路**。
4. **evidence 落盘规范（K3 1-8 复核 + P2-2）**：计时记录/数据断言/产物指纹/时间戳必须落盘（`scripts/golden-scenarios/evidence/` 或 `docs/synova/product-lines/evidence/`），K3 独立重跑可复现；禁止只在 task-state 存单一副本。
5. **红线（违反 = 事故）**：不碰 `src/`（首诊管线生产可用 GS-01 绿——D527/D528 经分析均零 src/ 需求）；不碰 `scripts/audit/`（K3 专属）；Stage 3 前零 DSH 依赖（不 `npm install @deepseek-ai/dsh`，不 copy DSH 代码）。
6. **环境坑**：DSH 宿主默认 `ELECTRON_RUN_AS_NODE=1`——跑 Electron 实测/脚本时显式 `env -u ELECTRON_RUN_AS_NODE`（K3 切片 A 审计环境注记）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：D527 的 DS1-DS9 / D528 的 DS1-DS8 逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim；spec §12 骨架照做（sse-contract 契约 / 升级语义事实表）。
2. **不违反铁律**：接线完整（新 export 有**生产**调用点，测试调用不计 S-3；spec §8 Wiring Verification 逐条 grep 实测）、降级诚实（铁律 24/31）、类型安全（as any=0，铁律 38）、契约优先（铁律 47）、测试非空壳（铁律 48）、架构边界（铁律 39/46）。
3. **无 bug**：spec §7 verify 命令逐条跑通 + `npx vitest run tests/electron/` 全绿（回归 3 个既有文件不破）+ pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：spec §8 每条 grep 出真实生产调用点（如 `setCurrentReportId` 在 useStreaming 调用 + RightPanel 消费）。
5. **测试到位**：red→green 已证（改造前用例先红）、覆盖正常/降级/边界、expect 断言非空壳。
6. **其他你认为需要复核的点**：残留清理（死代码/旧引用 grep 零）、文件驱动（如新增 manifest/tags）、产物证据可复现性（脚本幂等 + dry-run）等——你判断需要就查。

## 五、K3 审计提示（收尾要求）

- 切片 C 完成后**一次提审**（K3 审切片 C，报告覆盖 D527-D528 + 1-8 复核）。
- 审计验收 = 验证点 1-6/1-7/1-8 从 uncommitted → verified（product-progress.json 重算后 **5/8→8/8**，L1 桌面端 8 验证点全闭环）。
- 每个 D 完成后回填 `task-state/D527.json` / `task-state/D528.json` 的 **impl 段**（commit + by + files[]；`"slice": "L1-C"` 已由 dev-doc 预置）+ 验收证据原文（spec §10 DS 要求）。
- 审计员会**独立重跑你的验证脚本**——脚本必须幂等、可复现、无本机假设（ELECTRON_RUN_AS_NODE 坑、release/ 产物依赖等都在 spec 里标注过，跑前读 spec §4/§12）。

**开始吧。**
