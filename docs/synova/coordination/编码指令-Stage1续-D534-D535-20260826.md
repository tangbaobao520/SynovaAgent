# 编码指令 — Stage 1 续（D534 + D535）

> 本指令随 dev doc 交付给编码 session。**认真阅读任务文档，然后执行任务。**
> 派单: docs/synova/coordination/派单-Stage1续-D534-D535-20260825.md（CTO 派单）
> 审计: Kimi K3 会盯着你的任务，Stage 1 续完成后做最终审计（一次提审，报告覆盖 D534+D535，slice=stage1-cont）

---

## 一、任务文档（必读，先读后动，读不完不动手）

| 文档 | 路径 | 作用 |
|---|---|---|
| D534 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D534-notes-four-state-mechanism-20260826.md | Notes 四态机制补全——**编码唯一契约** |
| D535 spec | docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D535-guard-loop-hygiene-20260826.md | guard 循环卫生+超时通用化——**编码唯一契约** |
| 派单 | docs/synova/coordination/派单-Stage1续-D534-D535-20260825.md | 切片定义/写集约束/审计口径/防膨胀约束 |
| 北星 | .claude/PRODUCT-BRIEF.md §七/§八 + 施工图 docs/synova/research/DSH迁移施工图-20260820/DSH迁移施工图-20260820.md §3.3/§5.3/§8 | 产品方向锚点 |
| 前车之鉴 | docs/synova/audit-reports/2026-08-22-P0-control-tower-stage1.md（D472/D473 审计 PASS + 盲点）+ AUDIT-FINDINGS-LEDGER.md CT-46（D529 时间黑洞） | 直接上游审计：Notes 四态 CT-34 绕过修复/guard 超时盲点；教训清单（防膨胀/写集精确/诚实 red） |

## 二、执行要求（做到你的最高代码水平）

1. **认真阅读** spec 的 §1（Authority）/ §3（Current State，实测现状）/ §4（写集）/ §5+§7（测试）/ §9（DS 验收）——spec 是唯一契约，声称即引用。
2. **任务复杂 → 先 plan mode 再做**：读 spec §4.1 写集表 → 列出你的改动清单（文件级）→ 确认依赖前置（见 §三-1）→ 想清楚再动手。**禁止没想清楚就改代码。**
3. **最高代码水平**：类型安全（`as any`=0，铁律 38）、契约优先（新函数先 JSDoc @input/@output/@degraded，铁律 47）、降级诚实（每个 catch 有 log + degraded，铁律 24/31）、测试非空壳（expect 断言 + 正常/降级/边界三路径，铁律 48，red→green）。

## 三、Stage 1 续专属硬约束（比通用铁律更具体，违反 = 审计 FAIL）

1. **依赖前置 + 基线核验（防 M7 漂移）**：
   - 当前工作区 = `feat/d505-impl`（D505 实现分支，**非本任务基线**）。Stage 1 续基线 = `origin/main`——派单（9c11525b）与 D533（3001cf2a）**已合入 main**。
   - 编码前置（铁律 0-3）：`git fetch --all && git pull --ff-only` 确认 `origin/main` 最新；**从 main 新开 `feat/d534-d535-stage1-cont` 分支**（铁律 34 Feature Branch），不在 d505 分支上做本任务。
   - 合入后 **重新核验 spec 引用的行号/契约**——`scripts/commit-msg-check.sh:127`、`memory/notes/README.md:16-18`、`scripts/control-tower/synova-commit:505-516`、`incident-loop.py:147-154` 均可能因并行提交漂移（D524 教训：照 spec 旧行号写测试会红）。
   - D534 依赖 D395-a/D472（已 audited，memory/notes/ 四态 + check-notes-lifecycle.sh 已存在）；D535 依赖 D473（已 audited，运行时 guard 理念）+ D521（simulate-ci/::error 已交付）——**前置未满足 → 进入 waiting，不伪造实测**。
2. **写集精确性（M7 教训 + 切片 B merge 事故）**：只改 spec §4.1 写集表列出的文件。写集表含 spec 自身（dev-doc 交付物，编码不动）；**编码实际要改的文件如下**：
   - D534 编码写集（5 个）：`scripts/commit-msg-check.sh`（修改）、`memory/notes/README.md`（修改）、`AGENTS.md`（修改）、`memory/notes/implemented/2026-08-22-d472-notes-lifecycle.md`（git mv 自 proposed/ + 头字段改 implemented）、`tests/control-tower/commit-msg-note-mandatory.test.sh`（新建）
   - D535 编码写集（4 个）：`scripts/control-tower/incident-loop.py`（修改）、`scripts/control-tower/synova-commit`（修改）、`docs/synova/coordination/控制塔循环卫生标准-20260826.md`（新建）、`tests/control-tower/incident-loop-hygiene.test.sh`（新建）
   - `git diff --name-only` 与实际改动**完全一致**；禁止改写集外文件、禁止"树终验声称不符"重演。
   - **防膨胀红线**（D529 教训，K3 会核）：不新建 guard 脚本/门禁组；不改写集外任何 scripts/ 文件；不批量改控制塔脚本 timeout 值。
3. **诚实 RED**：D535 的 red 用例（同 id 二次 record 无 reminder / synova-commit 无 incident-loop 调用）必须**先跑红再改**；改完跑绿。禁止"直接改完声称 red 已证"。外部依赖（DSH 源码）只读不引——**不 copy DSH 代码，不 npm install @deepseek-ai/dsh**（Stage 3 前零依赖红线）。
4. **evidence 落盘规范（K3 独立复核 + 指纹落盘）**：DS 验收的计时/断言/产物指纹（如 `grep` 输出、测试通过计数、incident.log 行数）落盘到 `.codex/control-tower/logs/` 或 commit message，K3 独立重跑可复现；禁止只在 task-state 存单一副本。
5. **红线（违反 = 事故）**：不碰 `src/`（本任务零 src/ 需求——D535 的运行时 guard 已在 D473 交付，不重复）；不碰 `scripts/audit/`（K3 专属）；Stage 3 前零 DSH 依赖（不 copy、不 install）；**不修改任何现有铁律条目**（D534 只追加铁律 49 引用行，施工图"合并而非替代"）。
6. **环境坑**：`tests/control-tower/` 目录被 D533（renormalize CRLF）共享——D534/D535 新建的测试文件**必须 LF 换行**（`file tests/control-tower/commit-msg-note-mandatory.test.sh` 应为 ASCII text）；bash 变量与全角标点紧贴会触发 `unbound variable`（`echo "结果（${VAR}）"` 用花括号，D370 教训）；`grep -c` 无匹配输出 "0" 且 exit 1 → `| tr -d '\n\r' || true`（V4.5.1 教训）。

## 四、做完之后的复核清单（逐项自查，K3 会盯着你，也会做最后的审计）

1. **与 dev doc 一致**：D534 的 DS1-DS13 / D535 的 DS1-DS11 逐项对照，声称 = 实现 + 验收（S-2），禁 overclaim；DS 逐项标注 ✅/⏸/❌+理由，禁跳号/静默缺项（S-10）。
2. **不违反铁律**：接线完整（新 export 有**生产**调用点，测试调用不计 S-3；spec §6 Wiring Verification 逐条 grep 实测）、降级诚实（铁律 24/31）、类型安全（as any=0，铁律 38）、契约优先（铁律 47）、测试非空壳（铁律 48）、架构边界（铁律 39/46）。
3. **无 bug**：spec §7 verify 命令逐条跑通 + `bash tests/control-tower/commit-msg-note-mandatory.test.sh` + `bash tests/control-tower/incident-loop-hygiene.test.sh` 全绿（回归既有 commit-msg-consistency / incident-loop / check-notes-lifecycle 测试不破）+ pre-commit 13 组全过（**禁 --no-verify**）+ 提交走 synova-commit（**禁 git stash**，铁律 0-3）。
4. **接线完整**：D534 的 `grep -n "workflow|hooks" scripts/commit-msg-check.sh` 命中触发面正则；D535 的 `grep -n "incident-loop" scripts/control-tower/synova-commit` 命中 block 分支调用（生产调用点，测试调用不计）。
5. **测试到位**：red→green 已证（改造前用例先红）、覆盖正常/降级/边界、expect 断言非空壳；D534 ≥10 用例、D535 ≥8 用例（5 L1 + 2 L2a + 1 L2b + 1 L2c）。
6. **其他你需要复核的点**：残留清理（死代码/旧引用 grep 零）、`memory/notes/proposed/` 迁移后分布（D472 Note 迁入 implemented/，proposed 剩 tool-cordis + MEMORY.md）、`AGENTS.md` diff 仅追加铁律 49（现有条目零修改）、写集核验 `bash scripts/workflow/check-dev-doc-write-set.sh`（实现后跑，漂移应归零）——你判断需要就查。

## 五、K3 审计提示（收尾要求）

- Stage 1 续（D534+D535）完成后**一次提审**（K3 审 Stage 1 续，报告覆盖两 D + 复核点）。
- 审计验收 = 四态迁移规则文档化 + Note 门禁物理（非平凡变更无 Note → 拦）+ guard 通用化接线（grep 调用方）——**物理可复现，非文档声称**。
- 每个 D 完成后回填 `task-state/D534.json` / `task-state/D535.json` 的 **impl 段**（commit + by + files[]；`"slice": "stage1-cont"` 已由 dev-doc 预置）+ 验收证据原文（spec §9 DS 要求）。
- 审计员会**独立重跑你的验证脚本**——脚本必须幂等、可复现、无本机假设（`tests/control-tower/` 共享目录、LF 换行、`git mv` 依赖已迁移状态等都在 spec 里标注过，跑前读 spec §3/§9）。

**开始吧。**
