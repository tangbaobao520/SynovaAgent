# 草案：新增两条能力线（待创始人确认后登记进 `product-lines.yaml`）
> **状态更新（2026-09-19）**：**创始人已确认**——两条新线（测量与参数标定 / 多客户配置与租户分层）**已登记进 `product-lines.yaml`**（`total_lines: 26 → 28`，各 5 个验收点 = 10 点进入待定区，待定项 36 → 46）；11 项 DSH 缺口「归线不新开」同时获确认（登记表：`docs/synova/product-lines/gap-line-mapping.yaml`）。
> **⚠️ 仍未进计分分母**：V1 验收标准是冻结件（签名哈希 `c11841e6`），新线要进计分走**变更单**（D838 起草 → 创始人确认 → 改冻结件并更新哈希）。

> **依据**：`docs/authority/产品完成度定义与推进总纲-20260918.md` §2.1（创始人 2026-09-18 拍板新增 2 条线）
> **状态**：**草案**——线集只有创始人能改；本文交创始人确认，确认后由 CTO 登记进 `product-lines.yaml`（`total_lines: 26 → 28`，另 1 条 v2 扩展线不计分）
> **红线**：`modules` 字段**只允许写真实存在的路径**（历史事故：11 条幻影路径让失效检测静默空转）；新路径必须显式标「待建」，**建好后才可写入 yaml**

---

## 线 27 · 测量与参数标定

| 字段 | 内容 |
|---|---|
| **name** | 测量与参数标定 |
| **value** | 诊断用的每个测量器都有**标定过的参数**，并且**跨客户收敛**——越用越准 |
| **done_definition** | 老板能在报告里看到"这条结论背后的参数是被验证过的、并且在同类企业里收敛"，而不是拍脑袋的阈值 |
| **modules（现存的）** | `src/l4/temporal-baseline.ts`（趋势算法，121 行，**当前零生产调用**）、`extensions/sentinels/`（202 个 compute）、`docs/synova/product-lines/` |
| **modules（待建，建好再写）** | `src/store/metric-series-store.ts`（指标时序，P0-1）、`src/evidence/time-window.ts`（数据时点/时间窗，7b） |
| **为什么必修** | 26 线全文 `标定`/`transfer_function`/`42 边`/`因果` **全 0 命中**；compute 202 个文件但生产仅 8 个引用；**42 条因果边 `transfer_function` 全为待定、运行时零计算**——**这是护城河所在** |

### 验收点草案（证据类型须与 V1 标准列一致）

| id | 断言（判定式） | verify | 证据类型 |
|---|---|---|---|
| 27-1 | 指标时序真的落盘：跑一次真实哨兵 → `metric_series` 新增 ≥1 行且 `value` 非空、`org_id` 正确 | 真实哨兵跑批 + 查库 | scenario |
| 27-2 | 趋势真的接线：注入 3 期历史 → 哨兵 finding 的 payload 含 `trend` 且方向与注入数据一致 | 集成用例 | test |
| 27-3 | 参数标定表存在且有来源：每个在生产被引用的测量器，其参数有标定值 + 标定依据 + 日期 | 表查询 + 与 compute 清单对账 | test |
| 27-4 | 跨客户收敛：≥2 个客户的同类测量值进入同一参数收敛过程，收敛结果可导出 | 双客户数据跑批 | k3 |
| 27-5 | 因果边可用：42 条因果边的 `transfer_function` 不再是"待定"，且运行时被真实计算 | 边表查询 + 运行时断言 | test |

**依赖**：27-1（指标时序）→ 27-2/27-3/27-4（**硬顺序**：先有历史测量值才谈标定）；27-5 与 27-3 并行。

---

## 线 28 · 多客户配置与租户分层

| 字段 | 内容 |
|---|---|
| **name** | 多客户配置与租户分层 |
| **value** | 同一套部署能安全地服务**多家企业**：配置分层、数据隔离、凭据分区、用量可分辨 |
| **done_definition** | 第 2 家企业接入时**不需要改代码**：配置按客户分层加载、数据互不可见、凭据各自加密存放、用量与结论可区分到客户 |
| **modules（现存的）** | `src/config/config-layers.ts`（四层叠加 + provenance，207 行）、`src/config/customer-config-package.ts`（447 行）、`src/middleware/rbac.ts`、`src/store/session-store.ts`（`org_id` 列已存在） |
| **modules（待建，建好再写）** | `customer-config/`（**当前 0 文件**） |
| **为什么必修** | 26 线全文 `多租户`/`多客户`/`租户`/`客户配置` 全 0 命中；施工图自认"**多客户时代最大缺口**"；**第 2 个客户是一周还是三个月的分水岭** |

### 验收点草案

| id | 断言（判定式） | verify | 证据类型 |
|---|---|---|---|
| 28-1 | 客户配置包可加载并生效：为 org-A 放一份配置 → 不重启，该 org 的请求按新配置行为 | 真实请求观测行为差 | scenario |
| 28-2 | 数据隔离：以 org-A 身份查 org-B 的数据（搜索/报告/指标）→ 返回空，且不泄漏"存在但无权" | 双 org 集成用例 | test |
| 28-3 | 凭据分区：两 org 各存一份凭据 → 各自出站只带自己的；GET 不回显任何明文/末 4 位 | 假上游观测 Authorization 头 | test |
| 28-4 | 用量可分辨：按客户/时段统计 token 用量并可导出 | 双 org 跑批 + 导出校验 | test |
| 28-5 | 第 2 个客户接入演练：从零接入一家新企业，全流程不改代码（含配置、隔离、凭据、报告署名） | 端到端演练 | k3 |

**依赖**：28-1/28-2/28-3 可并行；28-4 依赖会话统计（`agent_metrics` 零写入 + `checkpoint()` 零消费者两处断线修好）；28-5 依赖前四条全绿。
**已知锚点**：`search()` 缺 `org_id` 过滤（`session-store.ts:534`）定级 **P1，锚点 = 第 2 个客户接入前必须修**（当前仅 1 租户，实际风险 0）→ 归入本线 28-2。

---

## 登记后会发生什么（先讲清楚，避免"数字变了不知道为什么"）

| 项 | 现在 | 登记后 |
|---|---|---|
| `total_lines` | 26 | **28**（+1 条 v2 扩展线不计分 = 29 目标线集） |
| 验收点总数 | 164 | **+10**（两条新线各 5 点草案）= 174 |
| 计分分母（V1 v0.2 冻结） | 128 | **不变**——新线的点先进入"待定区"，**要进计分分母必须走 V1 标准的变更单**（口径冻结不可绕） |
| 看板 | 26 条线 | 28 条线（2 条新线初期为 0） |

> **⚠️ 需创始人确认的两处**：① 两条线的 `name`/`value`/`done_definition` 是否认可；② 10 个验收点草案是否认可（可增删改）。确认后 CTO 登记，并同步"待定区"计数。

---

## 自研 / 借鉴 标注（创始人第 1 条要求：必须分清）

| 工作项 | 自研 or 借鉴 |
|---|---|
| 线 27 的指标时序存储（27-1） | **③ 自建**（DSH 全家族无时序存储：`kv` 是唯一分面；只借鉴其"写链纪律 + 原子发布协议"） |
| 线 27 的趋势接线（27-2） | **③ 自建**（我方已有 121 行趋势算法，只是零调用） |
| 线 27 的参数标定 / 跨客户收敛（27-3/27-4） | **③ 自建**（DSH 无对应物；这是领域护城河） |
| 线 27 的因果边计算（27-5） | **③ 自建** |
| 线 28 的配置分层（28-1） | **① 范式复用**（DSH `dsh-settings` 语义；我方 `config-layers.ts` 已按此自研过一次，**零 DSH 代码依赖**） |
| 线 28 的租户隔离（28-2/28-3） | **③ 自建**（我方 `org_id` 设计已有，缺实现；DSH 明确"无调用方授权，可信服务"——**对我方绝对不适用**） |
| 线 28 的用量统计（28-4） | **① 范式复用**（DSH `dsh-session-stats` 的字段口径；不引包） |
