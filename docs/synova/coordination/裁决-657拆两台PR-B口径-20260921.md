# 裁决：B —— #657 拆两台 PR（创始人 2026-09-21 批准）

> 背景：门禁加固五卡（D839/D846/D847/D848/D849/D853）技术面全绿，但打包在一个 PR 内触发**两条规则互斥**：
> 规则一 PR ≤12 文件（D734）；规则二 每个改动文件必须被一份 brief 覆盖。为覆盖全部改动须同时带 **D839 + D853 两份 brief** → 13 文件 > 12。
> 小队实测三种挪法均撞另一堵墙（violation 跳文件 / D328 归属不一致 / 丢契约断言）。

## 裁决

**走 B：拆成两台 PR，各自合规、各自可合。不做一次性豁免（A 否决），治本另走 D860（C）。**

### PR-1｜D839 本体（认领制根治）
- 内容：`claim_release.py`、`staging_guard.py`、`resolve-commit-brief.sh`、两个夹具（`claim_release.test.sh` / `staging_guard.test.sh`）、`task-state/D839.json`、`task-state/claim-releases.json`、memory note、**D839 brief**、`.github/workflows/ci.yml`（D839 的 P1-2 夹具接线）
- 必须 ≤12 文件，且 D839 brief 的 Q2 覆盖本台全部改动
- K3 已审（D841，CONDITIONAL PASS 的 P1-2 已由本台补 CI 一行）

### PR-2｜D849/D853 Windows 收口
- 内容：`scripts/control-tower/synova-commit`、其夹具（如有）、**D853 brief**、`task-state/D853.json`（及 Windows 相关根因修复的其余文件）
- 必须 ≤12 文件，且 D853 brief 的 Q2 覆盖本台全部改动
- 两条 Windows 根因（`git rev-parse` 落 pwd 回退 / PYBIN 只探存在性）应在本台

### 两台的共同要求
1. **同源同步**：两台均以最新 `origin/main` 为基，**彼此不得互相包含对方的文件**（否则又超预算）；
2. **合并顺序**：先 PR-1 后 PR-2（PR-2 的 `synova-commit` 依赖 PR-1 的 guard 语义）；合并 PR-1 后 PR-2 需 `merge main`（bypass.log 冲突按并集解）；
3. **ci.yml 单写者**：PR-1 合并后 ci.yml 释放 → 才轮到 #682 与 **D863**（K3 已建议 #682 与 D863 同批或其后，使 `exit 1` 真传导）；
4. 两台都要：判别性先红后绿、`ls-remote` 回执、M6 三件在仓库内可读；
5. **不得**为了省事把 D853 brief 塞进 PR-1（那正是本次死结）。

## 预期收益

#657 一拆 → 两台合规 PR 可各自合并 → **ci.yml 释放** → **D863 开工** → CT-78（假覆盖在 CI 层不阻断 / 绿腿告警不可见）真正修复；同时 D854 已改判通过、#682 按 K3 口径随 D863 或其后合入。
