# 裁定 — B5-U6 归并 · U12 立卡 · 第③面生成器恢复（2026-09-24）

## 一、问题的准确定性（为什么"一直卡在这"）

**B5-U6 交付的是代码改动（`scripts/control-tower/gen-cto-health.py`），但审级未定**：
- 按 D940 同类口径（含代码 ⇒ 必过 K3）它应过 K3；
- 但它与 **#725（D928，同一文件的既有 owner）** **同改一个文件** ⇒ 各自开 PR = **双写**（我明令禁止）；
- 于是它停在"等 CTO 认定"，**谁也不许自行认定**——执行方停手是对的（stop-work 是正确动作）。

## 二、根因（两条在我这边）

1. **审级未定**：我从未写清"**含代码的收口**（如 U6）要不要 K3" ⇒ 执行方不敢自认 ⇒ 停在等。
2. **同文件双 owner 未拦**：D928 卡面 `write_set` 含 `scripts/control-tower/gen-cto-health.py`（既有 owner）；
   B5-U6 是**第二个写者**。我早该在 B5-U6 开工时按「热点文件单写者」拦下，**没拦**。
3. （对方发现的）**该门的"看不见"是设计缺陷**：`scripts/control-tower/pre-audit-summary.sh:74`
   `python3 - "$runner" "$full" "$@" >/dev/null 2>&1` **同时丢弃 stdout 与 stderr** ⇒ U3-artifact-repro 门
   （跑 `gen-cto-health.py --strict`）失败时**只留"未过"、看不到 traceback** ⇒ 故障不可归因。

## 三、CTO 实测（决定如何解决）

```
main 版 gen-cto-health.py     → AttributeError: 'str' object has no attribute 'get'（line 305）⇒ 生成器崩
#725 版（fix/d929-cto-health-crash）→ ✅ 已生成 CTO-HEALTH.md（指纹 6395aad85f8c）
```
且 #725 的实现**已优于 B5-U6 的诉求**：`isinstance(_spec, dict)` → `.get("path")`；`isinstance(_spec, str)` →
**只认「形如路径」者，散文视为无路径（不猜）**；注释里已记「**P0：gen-cto-health 自 D600 起无法生成**」
—— 即 **main 上的崩溃早于 B5**，B5 的两张 string 卡只是让更多卡触发。

## 四、裁定（一次解决，不留悬案）

| # | 项 | 裁定 |
|---|---|---|
| 1 | **B5-U6 是否单独过 K3** | **不单独过**。其**代码诉求已被 #725 覆盖**（消费者容忍 string）；其**产物属簿记**。⇒ **内容并入 #725，K3 只审 #725 一次**；B5-U6 分支 `fix/b5-u6-spec-unify` **关闭并标注「内容已由 D928/#725 覆盖，分支仅留证据」**；其收尾件须在 #725 的 PR 描述中被**引用**（保可追溯） |
| 2 | **B5-U6 的 spec 统一诉求** | 归 **D952**（统一 object `{"path"}` / 禁写死 null / 消费者容忍 string 且可见降级） |
| 3 | **第③面"打开即真相"恢复** | **优先合并 #725**（实测可恢复）；合并后**由 CTO 重生成 `CTO-HEALTH.md`**（N2 闭环） |
| 4 | **U12（`pre-audit-summary.sh:74` 丢弃 stdout+stderr）** | **立卡 P1**：失败必须留 traceback（stdout/stderr 至少一路落盘或进 step summary），否则"知道失败、不知为何失败" |
| 5 | 双写拦截机制 | **补进派单前置**：派单前必须查「该文件是否已有 owner 卡在飞」；已在飞的 ⇒ 后到者**不得开写**，改为「并入 owner 卡」 |

## 五、给执行方的答复要点

- **不需要你们自行认定审级**：本件即认定 —— **B5-U6 不单独过 K3，内容并入 #725**。
- **不要再为 B5-U6 开 PR**；分支保留（证据），收尾件路径将引用进 #725 的 PR 描述。
- **U12 已立卡 P1**（CTO 发号后派单）。
