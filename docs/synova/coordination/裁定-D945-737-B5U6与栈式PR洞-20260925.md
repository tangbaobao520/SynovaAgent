# 裁定 — D945 判据误伤 · #737 观测性 · B5-U6 复核 · 栈式 PR 洞（2026-09-25）

## 一、D945：CI 两处红 —— 先改文件，判据另立卡
| 红项 | 性质 | 裁定 |
|---|---|---|
| **铁律 47 `迁移.*完成` 正则命中 brief 反面教训句** | **字面误伤**（被命中的第 20 行原文恰是"迁移完成≠内容最新"这句**教训**） | **① 立即可做：改被判文件措辞**（「迁移完成」→「迁移落地」），**不动门禁**；**② 立卡**：该正则需排除否定语境（属门禁语义变更 ⇒ 必过 K3） |
| **V5 扫描器不豁免 PYBIN 三级探测行**（`check-preset-bundles.sh:51` `for _c in python3 python py`） | **扫描器豁免面过窄**（同一探针在 `install-dsh-preset.sh` 存在但因是 M 文件未被扫） | **① 立即可做：让该行含 `PYBIN` 字样**（注释即可，扫描器 `grep -v PYBIN` 即放行）；**② 立卡**：扫描器应豁免"探测/枚举"形态（门禁变更 ⇒ K3） |

**另**：D945 需 **rebase 解 `ci.yml` 冲突** + **补开 PR**（栈基仍在 B3 的旧 tip）。

## 二、#737：ubuntu 残留红**观测不到** ⇒ 先派观测性卡
`ci.yml:273` `MSG=$(tail -8 … | cut -c1-450)` 在 runner 字节 locale 下**按字节截断**（中文≈150 字），
❌ 断言行落在窗口外 ⇒ **CI 红但拿不到失败名**。
**裁定：立卡（优先，解除一切下游阻塞）**——CI 失败消息必须优先保留失败断言（如 `grep -E 'FAIL|❌|not ok'` 命中行），保留长度上限。
**并**：小队本轮 GNU shim 模拟得 20/0 绿、而 CI ubuntu 仍红 ⇒ **shim 不等价**；在观测性卡落地前，
小队须**扩大 shim 覆盖面**，不得以"本地模拟绿"作为判据。

## 三、B5-U6：接受事实纠正，结论不变但**改为物理冻结**
- 纠正成立：**main 上生成器仍崩**（`AttributeError` @`gen-cto-health.py:305`）⇒ "已被 #725 覆盖"**在 main 上不成立**。
- 但结论不变：**目标产物已按创始人指令作废** ⇒ 不需要合并修复。
- **落地动作（此前只说不做，本次物理化）**：① **#744 已 closed** 并留冻结说明；② **`CTO-HEALTH.md` 从"开工先读"清单移除**，改指 `CTO-看板-自动.md`；③ 立卡：**归档 `gen-cto-health.py` 与 `CTO-HEALTH.md`**（移除全部引用，禁再作为判据）。

## 四、栈式 PR 洞（新发现，机制级）
`ci.yml` `on.pull_request.branches=[main]` ⇒ **base≠main 的栈式 PR 拿不到 PR CI**（#747 至今 **0 run**）。
**裁定**：① **栈式 PR 只作临时过渡**；**在 rebase 到 main 之前，一律不得判"过闸"**（无门禁证据 = 未过闸）；
② 立卡：改 `pull_request` 触发器（去 `branches` 限制或放宽），使所有 PR 都有 CI（**门禁变更 ⇒ K3**）。

## 五、未定因（挂账，不放过）
`8e188c91` / `4a2fb5d2`（16:08Z 推送）**0 个 PR run** —— `fix/**` 只解释 push CI 缺席，**解释不了 `pull_request: synchronize` 缺席**。列入观测性卡一并查。
