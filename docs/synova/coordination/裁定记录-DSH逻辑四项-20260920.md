# 裁定记录：四项按《DSH 决策镜头》处理（2026-09-20）

> 依据：创始人「按照 DSH 决策逻辑做」｜纪律见 [dsh-decision-lens](../../../.claude/skills/dsh-decision-lens/SKILL.md)
> 原则映射：①上限即纪律 ②归属自有+可归因 ③只做协调 ④机械/业务语义分离 ⑤诊断不污染主路径 ⑥权威账本在 Lead

| # | 事项 | DSH 原则 | 处置（不再等拍板） |
|---|---|---|---|
| 1 | 「缺」档（16 健康 / 3 写了没接 / 0 缺 / **98 未知**） | ④（`truncated` ≠ 上游不完整） | **接受「缺 = 不可判定」**；**未知必须是第一等公民**（有名字/数量/原因），**禁止**用静态判据凑数（否决"放宽为粗筛"）。补"未知 98 的成分分解" + 立卡补实现态机器可读源 |
| 2 | §6-5 签署态两处不一致（仓内表 ☐ / 标题"待签"） | ②⑥（包自有检查 + 权威账本在 Lead） | **单一权威源**：签署只落一处（带日期）；**人读表为派生视图**；新增检查：表状态 ≠ 权威件 → 报红 |
| 3 | PR #677 裁定表落成 **0 字节空 blob** | ②（损坏产物不进主分支） | **拒绝按现状合并**；用最新 main 重做；把**非空/骨架字段校验**并入文档真相防线 D2 登记门禁 → 同类不可能再进 main |
| 4 | 控制塔两处仍渲染「总完成度」（CT-74） | ⑤（一处生产、多处消费） | 改 `app/founder-dashboard.html:126-127`、`views/pm_dashboard.py:109-110` **统一读 D850 离散三档**；把这两处补进 D850 的"禁百分比"grep 断言清单；确认**主路径零新增检查** |

**一句话**：能变成"机器可核验 + 单一来源"的，不用拍板解决；只有真属产品语义的（"未知"是否接受）才提交创始人——本次已按 DSH 逻辑自行定性为**接受不可判定 + 显式化**。
