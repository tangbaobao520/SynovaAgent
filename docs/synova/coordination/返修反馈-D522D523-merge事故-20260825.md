# 返修反馈：D522/D523 分支 merge 事故（合并阻断，K3 复审前必须修复）

> 反馈: 主 CTO | 2026-08-25 | 对象: 🛠 编码 session（feat/d522-service-boot-teardown + feat/d523-win-double-click）
> 状态: **合并阻断**——审计分支 audit/k3-20260825-d522-d523 继续待命

---

## 问题（监督核实，非 K3 发现）

两分支相对 origin/main 的树差异（`git diff --name-status origin/main...<branch>`）：

| 异常 | 数量 | 性质 |
|---|---|---|
| **删除 `.claude/task-briefs/` 全部历史 brief** | 496 | main 上有、分支上无——合并会把 main 的 task-briefs 目录删空 |
| **新增 `a.txt` / `b.txt` / `f.txt`** | 3 | main 上无（D521 502a363b 已移除的沙箱残留），分支重新带回——合并会污染 main |

**与交付声称不符**："树终验：git diff origin/main...HEAD 零非写集文件 ✓"——实测非写集差异 = 496 删除 + 3 新增（M2 声称 vs 事实，需在返修中说明根因）。

**不涉及 src/、scripts/audit/**（K3 写集红线仍干净），但 `.claude/task-briefs/` 删除 + 沙箱残留是硬阻断。

## 修复步骤（在各自 worktree，两分支各做一遍）

```bash
# ① 恢复 main 的 .claude/task-briefs/（覆盖分支上的删除）
git checkout origin/main -- .claude/task-briefs/

# ② 删除沙箱残留
git rm a.txt b.txt f.txt

# ③ 提交（走门禁；如 bypass.log 需补记，按 D451 流程）
git add -A
git commit -m "fix(D522/D523): 修复 merge 树事故——恢复 .claude/task-briefs/ + 移除沙箱残留 a/b/f.txt"

# ④ 验证（必须零非写集差异，除 task-briefs 恢复 + 写集文件外）
git fetch origin main
git diff --name-status origin/main...HEAD | grep -vE "task-briefs|bypass" 
# 期望输出只剩写集文件：electron/backend-spawn.cjs, tests/electron/*, scripts/desktop/win-install-verify.ps1, docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D522/D523*, docs/synova/runbooks/founder-demo-win.md, task-state/D522/D523.json, .claude/task-briefs/2026-08-25-*.md

# ⑤ 重新推送（force push 自己的分支）
git push --force-with-lease origin <分支>
```

## 验证标准（返修后）

1. `git diff --name-status origin/main...<分支> | grep -vE "task-briefs|bypass"` 只剩写集文件
2. `git diff --name-status origin/main...<分支> | grep -c "^D"` = 0（无删除）
3. `git cat-file -e origin/main:.claude/task-briefs/2026-06-14-0201-SentinelExpertDispatcher.md` 在分支 tip 存在
4. backend-spawn.cjs 双侧保留仍成立（D518 prod 修复 + D522 teardown）+ tests/electron 全绿

## 完成后

- 报告根因（为什么 merge 后删了 task-briefs + 带回沙箱残留——是 merge 冲突解决误删？还是 worktree 清理误删？）
- 重新提 K3 复审（三项：grep 清零 / tests 全绿 / 树终验——本次树终验必须真过）
- 复审通过 → 按序合并 D522 → D523 → 审计分支
