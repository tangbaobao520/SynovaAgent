# 逐条转发清单 · 2026-09-19（CTO 出一页，创始人逐条转发即可）

> **用途**：把已进 main、依赖已满足的卡，变成"可以立刻发出去"的指令块。**每条自包含**（员工零上下文可开工）。
> **依据计划**: v1.2@4e46603f ｜ 验收标尺：`docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`（v1.1）
> **顺序是依赖决定的，不是偏好**；请按 ①→⑥ 发（**③ D839 已提级：台账不解锁，后面所有台账类活都会被拦**）。

---

## ① D831｜不变量机制（前提清单第 2 项）——**现在可开工**（硬前置 D819 已合入 main）

```
任务 D831｜P-2 不变量机制（首批 3 条断言）——给刚修好的"工具接线"上断言，让这类缺陷以后自动炸。
卡: task-state/D831.json ｜ 派单: docs/synova/coordination/批十三-断线补卡与固化-20260918.md §五
形态照 DSH dsh-invariants（123 行，净逻辑约 40 行；①范式复用，不引包）：
  新增 src/invariants/{errors.ts,registry.ts,companions/*.invariant.ts} + bootstrap 注册 + /healthz 暴露 registered/lastFailure。
首批 3 条断言：① 工具 schema 真的发出去了（对照 D819 已修好的路径）② tool 消息与 assistant tool_calls 配对 ③ 降级信号不被后续成功写入抹掉。
必须避开 DSH 自己的两个坑：伴生 install 不得返回 Promise（异步违规会被吞成 warn）；只挂注册表=零保护 → 启动断言 registered>=3。
验收（禁 grep 命中）：注入违规 → 该请求 5xx 且日志含 owner（**不许 200+WARN**）；正常路径不误报；hitCount>0；第 3 条 install 抛错 → 启动失败且不残留监听器。
依赖：D819 已合入（前置满足）。开工第一步重新核行号。做完复核；K3 会审计。开始吧。
```

## ② 三张 1–3 天的独立卡（可并行发；彼此文件不重叠）

```
任务 D820｜T-D3：删会话不删诊断报告（1–3 天，性价比最高档）。
卡: task-state/D820.json ｜ 派单: docs/synova/coordination/派单-批十一-P0-1-P0-2-20260918.md §A/D820
现状（CTO 已核）：deleteSession 只删 agent_messages + agent_sessions；deleteDiagnosisCheckpoints 已写好但全仓零调用者；报告列表 SQL（WHERE phase=5）不 join 会话表。
验收：建会话→跑诊断落报告→删会话→断言 diagnosis_checkpoints 无残留；断言报告列表 API 查不到已删会话的报告；反向断言未删会话的报告仍可见（不误删）。
红线：不碰 scripts/audit/** 与 scripts/control-tower/**；测试走真实 store 不 mock。开始吧。
```

```
任务 D822｜T-D5 降级信号自遮蔽（1–3 天）——那条测试正在保护缺陷。
卡: task-state/D822.json ｜ 派单: docs/synova/coordination/批十二-计划与派单-20260918.md §二
现状（CTO 已核）：session-store.ts addMessage 成功即重置 lastDegraded；routes/conversations.ts 第 2 次写成功后才检查 → 写失败信号被抹掉、STORE_DEGRADED 帧不发；且该缺陷被 tests/store/session-event-log.test.ts:146-164 固化成预期。
要求：addMessage 透传已有的 AppendEventResult；改写那条固化缺陷的测试。
验收：注入一次写失败 + 一次成功 → 断言 STORE_DEGRADED 仍被发出；断言旧测试已改写。开始吧。
```

```
任务 D823｜T-D6：IM 历史恢复静默空转（1–3 天）。
卡: task-state/D823.json ｜ 派单: docs/synova/coordination/批十二-计划与派单-20260918.md §二
现状（CTO 已核）：src/l1/im-inbound.ts:195 调用 addToHistory —— 全仓仅此一处，且写法是先 as unknown as 再可选链 → 每轮模型只看到当前一句。
要求：接上真实历史读取；若确定不做该意图 → 显式删除，不留假动作；不得再有 as unknown as（铁律 38/CT-46）。
验收：经 IM 通道发两轮 → 第 2 轮上下文含第 1 轮。开始吧。
```

## ③ D839｜认领制根治：完成即释放 + 批量清历史失效认领 + 释放持久化（**新增，优先级高于 D837**）

> **为什么插到 ③**（2026-09-19 实测）：全仓 **667 份 brief 里有 16 份**仍认领 `docs/synova/product-lines/`、`docs/synova/project/` 两个路径族；其中 **`product-lines.yaml` 被 3 份 brief 认领**（`2026-09-12-D711-…` / `2026-09-17-D803-capital-cycle.md` / `D371-product-progress-phase1.md`）。已完成任务的 brief **不会释放认领** → 谁改台账谁被拦。D806 那次"改文本释放"只是**一次性解**：`declare-write-set.sh` 一重跑就用 ASCII 路径重生成机器块 → 认领复活。

```
任务 D839｜认领制根治（D296/D329 门禁）：完成即释放 + block 文案带释放命令 + 批量扫描 + 释放持久化。
卡: task-state/D839.json ｜ 机制背景: docs/synova/coordination/逐条转发清单-20260919.md §③

【四处必改（缺一不算完成）】
① 完成即释放：认领 brief 对应任务的 task-state 状态 ∈ {impl_done, audited}（或该 session 记录已归档）→ 该 brief 不再阻塞其他 session；门禁降为 warn 并打印释放理由。
② block 文案升级：必须给出**可执行**的释放命令（例如 `python3 scripts/control-tower/session_registry.py release --session-id <D#> --files <path>` 或等价），而不是只让人"去协调"。
③ 批量扫描：提供一条命令/子命令，列出**全部**已失效认领（brief 的 D# 状态 ∈ impl_done/audited 但仍在认领），并支持一次批量释放（本次实测需清 16 份）。
④ 释放持久化：释放状态**记在 task-state / session-registry**（不是只改 brief 文本）→ `declare-write-set.sh` 重跑不得复活认领（这是 D806 那次的脆弱点，必须封死）。

【夹具（先红后绿，必须给两次原始输出）】
- 场景 A：造「已完成任务 A 的 brief 声明文件 X + 新任务 B 改 X」→ 当前应 block（先红）→ 修复后 warn+放行（后绿）；
- 场景 B（反向，不得放松真实并发保护）：任务 A 仍在进行（task-state=claimed/in_progress）→ B 改 X **仍必须 block**；
- 场景 C（持久化）：释放后再跑一次 `declare-write-set.sh` → 断言认领**不复活**。

【红线】改 scripts/control-tower/** 前加载 ctrl-tower-change 技能；不碰 scripts/audit/**（K3 域）；K3 抽审（门禁变更属审计面）；禁 --no-verify。
【Done】三场景夹具全绿 + 批量扫描命令能在本仓列出并清掉 16 份失效认领（给命令与原始输出）+ 改完 pre-commit 全过。
开工第一步：重新核行号（脚本会漂移）。开始吧。
```

## ④ D837｜小队模式物理门禁（把六条机制从"纪律"变成"门禁"）

```
任务 D837｜小队模式物理门禁 check-team-protocol.sh（M1–M6 六项检查 + 六对夹具 + 接线）。
卡: task-state/D837.json ｜ 机制定义: docs/synova/coordination/小队模式-固化件-v1-20260919.md
六项：M1 工作树隔离 / M2 写集互斥（含前缀包含）/ M3 自验措辞（禁用"审计通过"）+ 必备结论词 / M4 指令与工具副作用冲突（如"不要 push"+"synova-commit"）/ M5 证据写入归属（测试写路径须含本任务号）/ M6 队伍收尾（无残留归属 + 三件交付）。
验收：每项一对"先红后绿"夹具全绿；接进 pre-commit（本地软 / SYNO_CI 硬）；降级诚实（脚本缺失→显式 degraded+非零）；无相关产物时 <1s 跳过。
改 scripts/control-tower/** 前加载 ctrl-tower-change 技能；不碰 scripts/audit/**。开始吧。
```

## ⑤ 四张产品线级卡（建议 ② 完成后再发，避免一个 session 手上太多）

| 卡 | 一句话 | 指令位置 |
|---|---|---|
| D826 | `search()` 缺租户过滤（锚点＝第 2 个客户接入前） | 批十三 §五 |
| D827 | 会话统计两处零接线（`agent_metrics` 零写入 + `checkpoint()` 零消费者） | 批十三 §五 |
| D828 | 趋势接线（`computeTemporalBaseline` 零生产调用） | 批十三 §五 |
| D829 | `packages/evolution` 接线（2,503 行零生产引用） | 批十三 §五 |

## ⑥ 旧的流程类卡（排最后：改善流程，不是补产品洞）

D811 队列收口 / D812 派单引用门禁 / D813 测试类型网 / D814 派单门禁推断修复 / D816 控制塔卫生 / D835 D708 重命名误判。

## 审计线（K3，队外）

- **D836** D819 独立审计（创始人 2026-09-19 指示"稍后再审"，卡已备）
- **D815** 线10 资本循环全链路复核（**第一条 100% 线只差这一步**）

## 仍未解的三项（挡着"看板与真实结构对齐"）

① 两条新线确认（测量与参数标定 / 多客户配置与租户分层）② 11 项 DSH 缺口归线不新开 ③ 前提清单优先级高于线。另**里程碑时间窗口**为空 → 报不出"慢了没有"。
