# 门禁说明：K3 必经合并门禁（D923）

> 卡号 D923 | 域 mac | 生效 2026-09-23 | 判定器 `scripts/control-tower/k3-merge-gate.sh`
> 政策外置 [k3-gate-policy.yaml](../../../scripts/control-tower/k3-gate-policy.yaml) | 夹具 `tests/control-tower/k3-merge-gate.test.sh`
> 宿主：`.github/workflows/ci.yml` → **独立 job `k3-gate`（`name: K3 Merge Gate`）** → step 「K3 merge gate (D923)」。
> **为什么必须是 job 而不是 step**（D923 收尾任务 0.5，实测两条）：① branch protection 的 required check context 是 **job 名**，
> step 不是可注册单元 ⇒ step 形态**无法被注册为必经门禁**；② job 内某 step 失败后其余 step 全部 `skipped`
> （实测：`Iron laws check` 失败 ⇒ 其后 `Verify parallel` / D708 / K3 三步全 skipped）⇒「被 skip」= 门禁在被依赖时**静默缺席**，违反裁定 3「永远会跑」。
> 故本 job **不挂 `needs:`**、**不加 docs-only 跳过**（required check 必须在每个 PR 上都有结论）；`fetch-depth: 0`（要算 merge-base/diff）；
> 保留 `GITHUB_EVENT_PATH`（供第三路 D# 归因读 PR 正文）。调用语义与 D708 同形（`--base origin/main --head HEAD --pr <event>` + 三态 0/1/2 + diff 口径）。
> **遗留（不在本卡改）**：D708 与 `Iron laws` 仍是 `quality` job 的 step，**同样会被 skip**。

---

## 1. 门禁要解决的问题

三类高风险路径（**安全/鉴权、财务计算、数据迁移/存储**）的变更，历史上出现过「代码已合、K3 结论缺失或为有条件通过」的情形。
本门禁把「**卡内必须有通过级 K3 结论**」变成合并前的物理条件。

**原则「宁窄勿宽」**（CTO 裁定 6）：误报会诱发绕过（V3.6/V4.5.1 实证：一个被绕过的门禁 = 没有门禁）。
故类目取值一律取**实测信噪比最好**的那条，而不是语义上"更大更全"的那条。

---

## 2. 三态语义（CTO 裁定 3，逐字落地）

| K3 结论 | 归一类 | 门禁 |
|---|---|---|
| `PASS` / `pass` | PASS | **绿**（exit 0） |
| `CONDITIONAL PASS` / `CONDITIONAL_PASS` | CONDITIONAL_PASS | **红 + 附理由**（"有条件通过 = 未通过"，创始人 2026-09-20 纪律） |
| `FAIL` | FAIL | **红 + 附理由** |
| `NOT-AUDITABLE` | NOT_AUDITABLE | **红 + 附理由**（**不可审计 ≠ 通过**） |
| `无 K3（V5.1.3 tag 锚点闭环）` / `DONE` / `CTO 验收通过（非 K3）` | NOT_K3 | **红 + 附理由**（非 K3 结论不得充当 K3 通过；出口：补 K3 结论，或由 **CTO 具名豁免**（写入卡/PR，可核）） |
| 键缺失 / `audit:null` / `verdict:null` | NO_VERDICT | **红 + 「K3 排队中」提示**（出口同上） |
| 非三类路径 + 未抽中 | — | 绿 |
| 非三类路径 + 抽中 20% | — | **绿（永不阻断，只记录 `::notice`）** |

**退出码**：`0` 通过（绿）／`1` 拒绝（红）／`2` 检查执行失败或降级（fail-closed，**绝不等同通过**）。

**降级与三类的交叉（裁定 3 附加条件 4）**：结论源/扫描器不可用时，**三类命中场景一律非 0**——不得静默放行。
（实现：扫描器整体不可用 → exit 2；能分类但结论源读不出 → 判 NO_VERDICT → exit 1。）

---

## 3. D# 解析：三路来源 + 必须可归因（CTO 裁定 3）

一个 PR 变更三类路径时，门禁必须知道「查哪张卡的结论」。解析三路，**红时必须报出查过哪几路、结果分别是什么**（禁只报"无结论"）：

| 路 | 来源 | 说明 |
|---|---|---|
| ① | **分支名** 中 `[Dd][0-9]+`（大小写不敏感，归一为大写） | 主路径；对齐 D708 现有 `--branch "${GITHUB_HEAD_REF}"` 做法 |
| ② | 变更集内 `.claude/task-briefs/*-D<id>-*.md` 的**文件名** | 兜底；有些分支名不含卡号 |
| ③ | **PR 正文**里的 `D\d+`（从 `GITHUB_EVENT_PATH` 读，**不硬编码**） | 兜底第二路 |

- **多 D# → 全部检查，任一非 PASS 即红**。
- **三路皆空 → `NO_CARD_ID` → 红**（fail-closed；出口：在分支名/PR 正文写明卡号，或 CTO 具名豁免）。
- 脚本始终打印一行**归因摘要**：`D# 归因 → ①分支名(x)：[… ] ②brief 文件名：[…] ③PR 正文：[…] ⇒ 采用：[…]`。

### 3.1 报告文件 D# 派生：**配方 B（文件名首个 D#）**——口径必须钉死

> **本节的由来**：同一问题用三种配方得到三个数，这件事本身就是「口径必须钉死」的最好实证（新纪律⑥）。

统计「`audit:null` 但库内已有 K3 报告文件」的卡时：

| 配方 | 定义 | 结果 |
|---|---|---|
| A | 文件名**破折号锚定** `*-D#-*.md` / `*-D#.md` | **7** |
| **B** | 文件名中**首个** `D\d+` | **8 ← 本门禁采用** |
| C | 任意位置含 `D#`（宽松子串） | **9** |

差异根因：D850 的报告是**复合名** `docs/synova/audit-reports/2026-09-20-K3-D850D852.md`——
配方 A 锚了破折号 ⇒ 漏掉；配方 B 取首个 D# ⇒ 得 `D850`；配方 C 会把同一文件的 `D852` 也算上。
**采用 B 的理由**：与既有唯一约定 `scripts/control-tower/gen-cto-health.py:320` 的 `resolve_audit_report` **同源语义**，
且与 `#723` 证据 §六的 8 张**逐张一致**。
**若将来要改用配方 C，必须走 §7 失效条件 + 重测**，不得直接改。

---

## 4. 类目映射（外置 policy；每条理由附实测依据）

完整取值与 `reason` / `evidence` 见 [k3-gate-policy.yaml](../../../scripts/control-tower/k3-gate-policy.yaml)。摘要：

| 类目 | 判据 | 关键实测依据（命令 + 值） |
|---|---|---|
| A 安全/鉴权 | `src/security/**`（13）+ **6 条精确枚举** + `packages/engine-auth/**`（7） | `git ls-files 'src/security/*' \| wc -l` → 13；6 条枚举**逐条 `--error-unmatch` 实测存在**；`git ls-files 'packages/engine-auth/*' \| wc -l` → 7。**不取整包 `src/routes/**`**：53 条中仅 4 条鉴权相关（误报 92%） |
| B 财务计算 | 五子域 `.../<domain>/computes/**` | `git ls-files 'extensions/sentinels/<d>/computes/*'` → 9/7/4/3/1（**口径 = computes 层**；用 `'<d>/*'` 得 11/9/6/5/3，不同口径） |
| B 排除 | `extensions/sentinels/_extinct/**` | `git ls-files 'extensions/sentinels/_extinct/*/computes/*' \| wc -l` → 33；`git grep -n '_extinct' -- 'src/*' \| wc -l` → 12（**全为 `import type`，无运行时加载路径**） |
| C 存储/迁移 | `src/store/**`（6）+ `**/*migrat*`（9） | `git ls-files 'src/store/*' \| wc -l` → 6；`git ls-files '**/*migrat*' \| wc -l` → 9（**'**/' 与 '*migrat*' 结果相同 ⇒ 本仓 pathspec 下 '**/' 为 no-op，实测**） |
| C DDL | **pickaxe** `git diff -G` | 全历史 `git log --first-parent -G'CREATE TABLE\|ALTER TABLE' main \| wc -l` → 99；近 30 天 → 28（本卡复算；#723 证据记 60/20，配方差异已登记） |

**pickaxe 必须限定生产路径**（本卡实测发现的必要修正）：不加排除时，**本门禁自己的说明/证据文档正文含 `CREATE TABLE` 字面量即被误命中**。
故实际命令的排除面为 ①`docs/**` ②`tests/**` ③`*.md` ④policy `C_storage.pickaxe.exclude_globs`（= 门禁自身产物 `scripts/control-tower/k3-*`）。

> **④ 是 D923 收尾任务 0 新增的**（缺陷 A 修复）：①②③ 之后仍存在**自命中** —— 判定器与 policy 正文
> 以「数据」形式承载 DDL 模式字面量（YAML `cmd:` 证据串 / `pattern:` 常量 / `reason:` 散文 / shell 注释，共 5 处），
> 实测 `--base origin/main --head HEAD` 下 `three_category_hits=2` **全部来自这两个文件**。
> 后果：**任何触碰门禁文件的 PR 都会被误红**（长期运营缺陷）。
> 排除条件（可核，非"为变绿"）：该文件**无 DB 执行能力** ——
> `git grep -nE 'sqlite3|better-sqlite3|\.prepare\(|\.exec\(|new Database' -- 'scripts/control-tower/k3-*' | wc -l` → **0**。
> 逐条理由与命令见 policy `exclude_globs_reason` / `exclude_globs_evidence`；回归夹具用例 15/16。

---

## 5. 20% 抽样（非三类路径）

- **取值 0.20 来自 CTO 裁定 3（拍板参数），不是本卡推导的最优值。**
  **本仓无实测数据支持「20% 优于 10%」——此项为未实测，如实标注**（新纪律⑥）。
- **可核的部分**：三类路径在近期 468 个 first-parent 提交中命中 30 次（**6.4%**）⇒ 93.6% 的提交不进强制路径，抽样只做记录性覆盖。
  依据：`D923-三类路径名单实测 §四`（窗口 `since 2026-08-24`）。
- **确定性**：`pr_number % 5 == 0`——同 PR 恒同判，任何人可复算；**不用随机**（随机不可复现，无法审计）。
- **永不阻断**：抽中只打 `::notice`，exit 0。

---

## 6. 已知后果 / 上线顺序（**必读**）

> 截至 2026-09-23 实测（配方 B）。

```
task-state 卡片总数: 347
无结论合计: 183 = 键缺失 2 + audit:null 181
  其中【有 K3 报告文件】= 8  (D558/D651/D819/D839/D850/D854/D862/D864)
  其中【无报告文件】    = 175
```

- **上线即红的面积**：任何**改动三类路径**且卡内无通过级结论的 PR 会红。存量 183 张无结论卡中，只有 8 张**库里已有报告**（可回填台账后转绿），**175 张确无报告**（须走 K3 或 CTO 具名豁免）。
- **本卡自身**：`task-state/D923.json` 当前 `audit:null` ⇒ **门禁注册后 D923 自己若改三类路径也会判③红**。
  **处置建议（二选一）**：① 注册前先回填本卡 verdict；② 首日接受自红并登记。
  本卡变更路径**不在三类内**（`scripts/` + `docs/` + `ci.yml` + `tests/`）⇒ **本 PR 预期绿**。
- **启用顺序**：门禁上线 → 观察期（建议 ≥1 周）→ 再纳入 branch protection required checks。
  注册（branch protection）**由 CTO 执行**，本卡不碰。

---

## 7. 失效条件（本门禁何时可撤 / 必须改）

| # | 条件 | 动作 |
|---|---|---|
| **F1** | 类目判据与产品结构脱节（如 `src/security/**` 被重构掉、五子域改名） | 按 policy `evidence` 的命令**重测**后更新 glob；**改 glob 必须同时更新该条的 `evidence`**，否则理由不可核 |
| **F2** | **常量驱动迁移漏报**（`SCHEMA_VERSION`/`user_version`，本仓 `src/store/schema-migration.ts:19` = `export const SCHEMA_VERSION = 2;`） | 已知漏报，登记在 policy `known_misses`。消除需引入「迁移语义」判据（如比对迁移数组长度），属后续卡 |
| **F3** | pickaxe pattern 未覆盖的 DDL 形态（`PRAGMA user_version` / `CREATE TRIGGER` / `CREATE VIEW` / DROP INDEX 等） | policy `known_misses` 已登记为**候选漏报面**（未逐形态实测）。实测确认漏拦后扩 pattern + 补夹具 |
| **F4** | 报告 D# 派生改用**配方 C**（宽松子串） | 必须重测「有报告」计数（会从 8 变 9）并同步更新 policy `backlog` 与本文 §3.1/§6，且随一次显式记录的行为变更卡落地 |
| **F5** | K3 审计流程本身变更（如 verdict 取值集合变化） | 重跑 `verdict` 分布统计（见 policy `verdict_normalization` 的命令），更新归一表 + 补夹具用例 |
| **F6** | 三类路径误伤率显著上升（>10%，当前 6.4%） | 重跑误伤率测算；上升则收紧 glob（宁可漏报不误报——误报诱发绕过） |
| **F7** | 门禁整体被取代（如 K3 结论改为 CI 内联签发） | 按控制塔流程归档；**判定器 + ci.yml 步骤 + 夹具登记三者同步移除，不得只删其一** |

---

## 8. 已否决判据（防后人重加）

| 判据 | 否决理由 | 依据 |
|---|---|---|
| `*.sql` | 本仓无被跟踪 `.sql` ⇒ 判据空转（100% 漏报） | `git ls-files '*.sql' \| wc -l` → 0 |
| `**/*orgid*` | 文件名口径 3 条全为文档/brief，生产 0；内容级口径 129 文件（误报极高） | `git ls-files '*orgid*' \| wc -l` → 3；生产口径 → 0 |
| `data/**` | 仅 2 个夹具，真实 `data/synova.db` 未被跟踪 ⇒ 空转 | `git ls-files 'data/*' \| wc -l` → 2 |
| `src/routes/**`（整包） | 53 文件中仅 4 条与鉴权直接相关（误报 92%） | `D923-三类路径名单实测 §一` |

---

## 9. 存量异常值（归一必须显式可见）

- `pass`（**小写，实测 2 张**）归一为 PASS——归一动作**显式**：检查器在判 PASS 时打印 `::notice` 并**具名列出卡号**，禁静默归一。
  具名清单列入证据文件（`docs/synova/product-lines/evidence/D923-k3-gate-*`），并标注"原值为小写异常，待裁定 8 卡回填时统一"。
- `NOT-AUDITABLE`（2 张）：理由串固定为「**不可审计 ≠ 通过**」，不得只写"非 PASS"。
- `NOT_K3`（3 张：tag 锚点 1 / DONE 1 / CTO 验收 1）：理由串必须给出出口「补 K3 结论，或由 CTO 具名豁免」。

---

## 10. 归属与维护

- 判定器 / policy / 夹具 / 本文档：控制塔域。**本卡不改任何产品代码**（`src/**` 零改动）。
- **自验不得由编码兼任**；本卡交付结论只到「可提请独立审计」，通过与否归 CTO 收件闸 + K3 终审。
- 改本门禁 = 治理产物 → 走 PR；**改门禁者不得自判通过**。
