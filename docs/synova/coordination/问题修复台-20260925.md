# 问题修复台（2026-09-25 起）

> **这是所有已知问题的唯一登记处**。创始人只需读这一页。
> **设计原则：统一登记 + 分散执行** —— 登记与追踪集中在这里（CTO 单写者），
> **但执行仍归原属主**（域权与写权不可乱：Mac 小队 / 并行 CTO / 控制塔线 / CTO）。
> **编号用 `FIX-###`，不占用 D# 号段**（避免再撞号）。

## 状态摘要（每次闭环后更新）

| 总条目 | 登记 | 在修 | 待收件 | 已闭环 | 已升级创始人 |
|---|---|---|---|---|---|
| **16** | 8 | 5 | 2 | 1 | **2** |

## 归属原则（回答"谁出的问题谁修"）

| 情形 | 处理 |
|---|---|
| 有明确属主（Mac 小队 / 并行 CTO / 控制塔线） | **原属主修**；本台只登记与追踪 |
| CTO 自造 | **CTO 修**（并在此显式标注"CTO 自造"，接受独立复核） |
| 跨域 / 无人认领 | **CTO 指定或承接**，并在表内标注 |
| **同类第 2 次及以上** | **升级创始人**（红线），在本台标 🔴 |

## 台账

| ID | 症状 | 证据 | 属主（谁修） | 状态 | 闭环判据 |
|---|---|---|---|---|---|
| **FIX-001** | **#799 CI 三红**：TypeScript job 死于「变更跨域」（win `.gitattributes` vs mac 10 件）＋ CT 测试红 `bypass 账本写入失败 (exit=127) … bypass-ledger.sh: No such file or directory`；回执「单域 mac」在 tip 复跑不成立 | 验收 B 原始输出（`/tmp/acc-b-out/`） | 并行 CTO | 在修（#799 保持 draft） | CI 全绿（既存红除外）且「单域」复跑成立 |
| **FIX-002** | **#796 未与 D331 同改**（只删账本、不改对账实现）⇒ **先合则"本机无账本残留"的 clone 全员拒推** | verifier 实验 Exp-B / Exp-2（`/tmp/cto-verify/`） | 并行 CTO | 待收件 | 新 clone 态 `check-bypass-log.sh` rc=0；或 #796 自带 D331 读面改动 |
| **FIX-003** | **#802（CTO 自造）测试判别力弱**：6 断言 vs #796 的 25；`red=1→red=0` 假绿回归**抓不到**；**未注册进 CI for 清单** | verifier 三变异体原始输出 | **CTO** | 在修 | 3/3 变异体变红；测试进 CI 清单并被 CI 执行 |
| **FIX-004** | **D964 三漏洞**：A 任意文件放 `archive/**`/`audit-reports/**` 即**静默豁免**（P0）｜B 新目录 `README.md` **误拦**（P1）｜C `取代: 不存在` **仍放行**（P1） | K3 报告 + verifier 三项复现 | 控制塔线（CTO 发卡） | 登记（未开卡） | 三处「滥用场景必须仍红」反例存在 |
| **FIX-005** | **号段三卡争 D969**（先占 #796）；main 水位文件过期 | verifier §四（三条 ADD 原始输出） | **CTO** | 在修 | #796 保 D969；#802→D975；ownership→D976；水位与实际一致 |
| **FIX-006** | **`alloc-task-id.sh` 挂死**（249 worktree / Σ69484 / O(n²)）＋**占用表读取面 ≠ 拒绝面** ⇒ 撞号风险 | 并行 CTO 升级（同类**第 3 次** 🔴） | 控制塔线 | 登记 | 取号 O(1) 且"先占检查"可拒；已升级创始人 |
| **FIX-007** | **Gatekeeper FP 熔断**（merge 提交 + 陈旧 marker ⇒ 全队合法提交被熔断） | 并行 CTO 升级（同类**第 2 次** 🔴） | 控制塔线 | 登记 | 熔断条件含"陈旧 marker"判定 + 反例；已升级创始人 |
| **FIX-008** | **测试副作用碰 tracked 文件**（第 3 例：`founder-truth.test.sh` 改写 `founder-console.html`/`founder-alerts.md`） | 并行 CTO 升级（**第 3 例** 🔴） | 控制塔线 | 登记 | 测试写入隔离目录；"跑测试后 git status 干净"判据 |
| **FIX-009** | **`reference-map.md` 同类 union latent**（main 仍跟踪 + 声明 `merge=union`） | verifier 已核 `check-attr`；我的 #802 内已处理 | **CTO** | 在修（#802 未合） | main 上 `check-attr` = `unspecified`；文件出仓 + `.gitignore` |
| **FIX-010** | **派单件参数笔误**（`merge_writeset_gate.py` 的 `--repo` 实为 `--repo-root`）——**CTO 自造** | 验收 B 第 5 条 | **CTO** | 登记 | 派单模板与历史派单件更正 |
| **FIX-011** | 🆕 **共享治理文件多卡归属冲突**：`派单模板.md` 属 D931/D943、`派单-哨兵体系整改.md` 属 D965/D966 ⇒ **任何新卡改它都判"并行劫持"** | 我今日两次被拒的原始输出 | 控制塔线（CTO 发卡） | 登记 | 改共享文件不再判劫持（每对象一文件 or 显式多卡声明） |
| **FIX-012** | 🆕 **R1–R4 规则未落 main**：内容已完成、引用 3/3，但**提交被归属门禁拒两次**（同 FIX-011 根因） | 我今日原始输出 | **CTO** | 在修 | 新文件承载 + 模板一行指针 ⇒ 过归属门禁并落 main |
| **FIX-013** | 🆕 **#803 保护真空**：D962 2a 把 33 项移出 pre-commit，接手的 CI `iron-laws` 归 2b 未接线 ⇒ 接线断言正确地报"无活点" | 小队 CI 原始日志 | 并行 CTO（CTO 已裁 **A**） | 在修 | 移出与接手**同批**：#803 内挂 iron-laws + 登记 + 断言改指**真实活点**（活点三件套证据） |
| **FIX-014** | 🆕 **`Vitest (2/2)` 既有红**（main@ef299746 上存在） | 小队报告 | Mac 小队（另开卡） | 登记 | 按 D954 棘轮：另卡 + `disposition_due` 登记；**不得豁免计绿** |
| **FIX-015** | 🆕 **#803 的 `as any` 9 处 + `DiagnosticModule` 3 处**（是否测试注入样例自伤待判） | 小队 CI 原始日志 | Mac 小队 | 待收件 | 分类表（A 测试注入/B 生产真红/C 注释字面量）逐类处置；A 类走显式豁免+成对反例 |
| **FIX-016** | 🆕 **`grep-oP-regression` CI 23/1 vs 本地 24/0**（平台/环境差异） | 小队报告 | Mac 小队 | 待收件 | 两平台原始输出 + 定位到具体断言 + 判定；**定位前不得改夹具** |

## 更新规则
1. **新问题只登记到本台**（不再散落到卡/PR/对话里）
2. **闭环 = 判据达成 + 原始输出**；状态改为"已闭环"并附证据链接
3. **同类第 2 次及以上 ⇒ 标 🔴 并升级创始人**
4. 本台**由 CTO 单写者维护**（避免又造一个共享冲突文件）
