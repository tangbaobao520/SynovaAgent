# 问题修复台（2026-09-25 起）

> **这是所有已知问题的唯一登记处**。创始人只需读这一页。
> **设计原则：统一登记 + 分散执行** —— 登记与追踪集中在这里（CTO 单写者），
> **但执行仍归原属主**（域权与写权不可乱：Mac 小队 / 并行 CTO / 控制塔线 / CTO）。
> **编号用 `FIX-###`，不占用 D# 号段**（避免再撞号）。

## 状态摘要（每次闭环后更新）

| 总条目 | 登记 | 在修 | 待收件 | 已闭环 | 已升级创始人 |
|---|---|---|---|---|---|
| **17** | 6 | 7 | 3 | **0** | **4** |

## 归属原则（回答"谁出的问题谁修"）

| 情形 | 处理 |
|---|---|
| 有明确属主（Mac 小队 / 并行 CTO / 控制塔线） | **原属主修**；本台只登记与追踪 |
| CTO 自造 | **CTO 修**（并在此显式标注"CTO 自造"，接受独立复核） |
| 跨域 / 无人认领 | **CTO 指定或承接**，并在表内标注 |
| **同类第 2 次及以上** | **升级创始人**（红线），在本台标 🔴 |

## 台账

| ID | 症状 | 证据 | 属主（谁修） | 状态 | 闭环判据 |
|---|---|---|---|---|---|
| **FIX-001** | **#799 CI 三红**：TypeScript job 死于「变更跨域」（win `.gitattributes` vs mac 10 件）＋ CT 测试红 `bypass 账本写入失败 (exit=127) … bypass-ledger.sh: No such file or directory`；回执「单域 mac」在 tip 复跑不成立 | 在修 → **可提请独立审计**（#799 d38eeabe；仅差 CI 结论） | 并行 CTO | 在修（#799 保持 draft） | CI 全绿（既存红除外）且「单域」复跑成立 |
| **FIX-002** | **#796 未与 D331 同改**（只删账本、不改对账实现）⇒ **先合则"本机无账本残留"的 clone 全员拒推** | 待收件 → **可提请独立审计**（#796 tip f4da7a82；新 clone rc=0 + 成对反例） | 并行 CTO | 待收件 | 新 clone 态 `check-bypass-log.sh` rc=0；或 #796 自带 D331 读面改动 |
| **FIX-003** | **#802（CTO 自造）测试判别力弱**：6 断言 vs #796 的 25；`red=1→red=0` 假绿回归**抓不到**；**未注册进 CI for 清单** | verifier 三变异体原始输出 | **CTO** | 在修 | 3/3 变异体变红；测试进 CI 清单并被 CI 执行 |
| **FIX-004** | **D964 三漏洞**：A 任意文件放 `archive/**`/`audit-reports/**` 即**静默豁免**（P0）｜B 新目录 `README.md` **误拦**（P1）｜C `取代: 不存在` **仍放行**（P1） | 登记 → **可提请独立审计**（#812 b7f39e1d / #813 af095d29） | 控制塔线（CTO 发卡） | 登记（未开卡） | 三处「滥用场景必须仍红」反例存在 |
| **FIX-005** | **号段三卡争 D969**（先占 #796）；main 水位文件过期 | verifier §四（三条 ADD 原始输出） | **CTO** | 在修 | #796 保 D969；#802→D975；ownership→D976；水位与实际一致 |
| **FIX-006** | **`alloc-task-id.sh` 挂死** ＋ **占用表读取面 ≠ 拒绝面** —— ✅ 已由 Mac 小队实测闭环（PR #821 966a6374）：取号 94s→7s（同输出 D1009，不烧号）；拒绝面补 ⑥（漏判 rc=0→rc=1）。**CTO 口径更正：「字符串累加 5 处」实测为 1 处** | 并行 CTO 升级（同类**第 3 次** 🔴） | 控制塔线 | 登记 | 取号 O(1) 且"先占检查"可拒；已升级创始人 |
| **FIX-007** | **Gatekeeper FP 熔断**（merge 提交 + 陈旧 marker ⇒ 全队合法提交被熔断） | 登记 → **可提请独立审计**（#818 9a5eb564；修后 11/0，修前 6/5） | 控制塔线 | 登记 | 熔断条件含"陈旧 marker"判定 + 反例；已升级创始人 |
| **FIX-008** | **测试副作用碰 tracked 文件** —— ✅ 已闭环（PR #817 5ebafe78）：**CTO 原判「读取非改写」被推翻**，写点在 `founder-truth.py:294/418`；隔离后跑测试 porcelain 前=0 后=0 | 并行 CTO 升级（**第 3 例** 🔴） | 控制塔线 | 登记 | 测试写入隔离目录；"跑测试后 git status 干净"判据 |
| **FIX-009** | **`reference-map.md` 同类 union latent**（main 仍跟踪 + 声明 `merge=union`） | verifier 已核 `check-attr`；我的 #802 内已处理 | **CTO** | 在修（#802 未合） | main 上 `check-attr` = `unspecified`；文件出仓 + `.gitignore` |
| **FIX-010** | **派单件参数笔误**（`merge_writeset_gate.py` 的 `--repo` 实为 `--repo-root`）——**CTO 自造** | 验收 B 第 5 条 | **CTO** | 登记 | 派单模板与历史派单件更正 |
| **FIX-011** | 🆕 **共享治理文件多卡归属冲突**：`派单模板.md` 属 D931/D943、`派单-哨兵体系整改.md` 属 D965/D966 ⇒ **任何新卡改它都判"并行劫持"** | 登记 → **已让位**（CTO 线栈式链 #815/#819/#820 在飞；Mac 小队零改动） | 控制塔线（CTO 发卡） | 登记 | 改共享文件不再判劫持（每对象一文件 or 显式多卡声明） |
| **FIX-012** | 🆕 **R1–R4 规则未落 main**：内容已完成、引用 3/3，但**提交被归属门禁拒两次**（同 FIX-011 根因） | 我今日原始输出 | **CTO** | 在修 | 新文件承载 + 模板一行指针 ⇒ 过归属门禁并落 main |
| **FIX-013** | 🆕 **#803 保护真空**：D962 2a 把 33 项移出 pre-commit，接手的 CI `iron-laws` 归 2b 未接线 ⇒ 接线断言正确地报"无活点" | 在修 → **可提请独立审计**（#816；活点三件套齐） | 并行 CTO（CTO 已裁 **A**） | 在修 | 移出与接手**同批**：#803 内挂 iron-laws + 登记 + 断言改指**真实活点**（活点三件套证据） |
| **FIX-014** | 🆕 **`Vitest (2/2)` 既有红**（main@ef299746 上存在） | 登记 → **可提请独立审计**（#811 31bc0e6c；棘轮只加不删） | Mac 小队（另开卡） | 登记 | 按 D954 棘轮：另卡 + `disposition_due` 登记；**不得豁免计绿** |
| **FIX-015** | 🆕 **#803 的 `as any` 9 处 + `DiagnosticModule` 3 处**（是否测试注入样例自伤待判） | 待收件 → **补证闭环 + CI 实证**（12 处 100% 自伤，B=0；CI 日志 945/950 两判定 ✅） | Mac 小队 | 待收件 | 分类表（A 测试注入/B 生产真红/C 注释字面量）逐类处置；A 类走显式豁免+成对反例 |
| **FIX-016** | **`grep-oP-regression` 双平台差异** —— **口径更正**：本地 **main 38/0 ｜ #803 24/0**（原写「本地 24/0」缺 ref）；CI 双平台 23/1。**未闭环**（结构性不可定位；遵红线未改夹具）⇒ **归阶段 1** | 小队报告 | Mac 小队 | 待收件 | 两平台原始输出 + 定位到具体断言 + 判定；**定位前不得改夹具** |

| **FIX-017** | 🔴 **同类第 2 次**：`merge_writeset_gate.py` 的 **DID 推断**对**无 D#/FIX-### 卡**结构性 fail-closed（与 FIX-011 同族）；且 `:150 DID_RE=[Dd]\d+` + `:187` 回退扫 HEAD ⇒ **可从 40 位 SHA 抠假号**（`Merge a16d839e…`→D839）、**可扫进 main 历史**（→D956）；同一 PR 重推即变号 | 并行 CTO 实测 + CTO 今日被误判 3 次（D954/D940/D947） | 并行 CTO（阶段 3 必收口） | 🔴 **升级** | ① 回退源**不得从 SHA 抠号、不得扫进 main 历史** ② **D814 `--first-parent` 落地** ③ 分支名含卡号 ⇒ 来源①优先（本阶段处置已做） |
| **FIX-018** | **C16 口径三方不一致**（CTO 实测口径① 150/1,583、口径② 461/3,678；小队报 158/1,546、447/3,402） | 各方原始命令 | **CTO** | 已裁定 | **C16 双指标**：**主判据 = C16-remote（远端分支含未合并提交）**，起点以**同刻同命令**重取为准（CTO 实测 461/3,678）；C16-local 仅作趋势 |
| **FIX-019** | **N12 CI 队列停摆**（外部）：33 queued 不消费；最老等 69 分钟；CTO 已取消 4 条挂死 in_progress（90/72/69/46 分钟），**队列仍未恢复**；Actions enabled、7 workflow 全 active、billing API 404 不可查 | CTO 实测 | **创始人**（查 GitHub Billing/额度） | 待查 | 恢复后两条线重跑并补 CI 结论；期间一律标「CI 未跑」，**不得以本地等价冒充实跑** |

## 更新规则
1. **新问题只登记到本台**（不再散落到卡/PR/对话里）
2. **闭环 = 判据达成 + 原始输出**；状态改为"已闭环"并附证据链接
3. **同类第 2 次及以上 ⇒ 标 🔴 并升级创始人**
4. 本台**由 CTO 单写者维护**（避免又造一个共享冲突文件）
