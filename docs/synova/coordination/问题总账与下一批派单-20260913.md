# 问题总账 + 下一批派单（2026-09-13）

> 维护: 主 CTO（synova-cto） | 依据: K3 D715 审计 + CTO 亲历发现 + 各 session 回传
> 本账所有条目**进 git、受 K3 审计、无豁免**（CTO 自己改的门禁同样在内）
> 状态图例: ✅已修并合 / 🔧已有在途 PR / 📋已登记待派 / ⛔阻塞待创始人裁

---

## 一、P0/P1 级（14 条）

### A. K3 D715 审计发现（4 条 P1）

| ID | 问题 | 物理证据 | 状态 | 归属 |
|---|---|---|---|---|
| K3-P1-1 | **双机并行产证同名撞车**：`evidence-writer` 文件名只有 `scenario-<date>[-N]`，无机器/平台维度，`target.exists()` 只看本地磁盘 → Mac/Win 并行必撞。合并时取任一侧即**丢另一侧判定** | `scripts/product-lines/evidence-writer.py:70-76`；CTO 独立复现 `git merge-tree` → 4 个 `scenario-2026-09-13*.json` add/add 冲突 | 📋 | 编码（Mac，产品线域） |
| K3-P1-2 | **派单前置声明失实**：文档称「D712 双侧重验证据(#510/#511)」，实际 #511 未合并、main 无 Win 证据目录 | `派单-下一批四线并行-20260913.md:5` vs #511 `state=open` + main 目录数 0 | ✅（D723 已订正并留依据） | CTO（**我的错**） |
| K3-P1-3 | **状态机同日矛盾证据由文件名字典序定胜负**：`machine` 段无 fail 优先、无时间戳 tiebreaker；1-4 现为 failed 只因 `'D713-…' < 'scenario-…'` 的巧合 | `calc-progress.py:83`（`sorted(glob)`）+ `:213`（`max(machine,key=date)` 取首个最大值）；对照 k3 段 `:188/:198` 语义正确 | 📋 | 编码（产品线域） |
| K3-P1-4 | **D714 修复的复验证据不在 git**：`[preload-check] OK` 日志只在 memory note 自述，未落证据文件 → 1-4 无法转绿 | 代码在 main（`electron/main.cjs` 3 处）；`grep -rn preload-check docs/` 仅命中 note | 📋 | Mac 编码（需重打新产物 + 证据落盘） |

### B. CTO 亲历发现（8 条）

| ID | 问题 | 物理证据 | 状态 | 归属 |
|---|---|---|---|---|
| CTO-1 | **CI 存量失败棘轮判据错**：`HEAD~1..HEAD` 在合并提交上 = 被并入的 main 全部改动 → 存量红被算成「本 PR 新增红」→ PR 永红 | #513 head 与 main 内容 `git diff` = 0 文件，却判 3 新增失败（重跑复现） | ✅ D721 / PR #520 | CTO |
| CTO-2 | **G12 同族第二处**：`check-brief-vs-code.sh` 用同一错误基准 → 刷新分支即报「越界」 | #520 的 CI 报 `G12 Q2 范围一致性: 1 处` | ✅ D721（同 PR） | CTO |
| CTO-3 | **仓库级假绿**：main 自己的 Vitest 一直有 **3 个真红文件**，被棘轮当「存量失败」静默放行（日志 `3 failed \| 286 passed` + `not blocking` → 作业 success） | main `021897a0` 的 Vitest 日志原文（CTO 抓取） | 🔧 D721 已让放行**可见**；红本身待烧 | 编码（见 D726） |
| CTO-4 | **brief 写集反引号 → 认领恒为空**：`parse_q2` 不剥反引号 + `match_path` 锚定比对 → 认领静默为空 → 归属校验退化为「猜 brief」 | `brief_parser.py:166` 旧实现；探针实测 D720 brief 首项带反引号 | ✅ D722 / PR #521 | CTO |
| CTO-5 | **陈旧 brief 抢走热文件认领** → 误判「并行劫持」阻断提交 | D721 提交被拦，报「声明(D721)与归属(D711)不一致」 | 🔧 D722 缓解 + D718 #522 已改身份锚点 | CTO/D718 |
| CTO-6 | **内联回退解析器与 brief_parser 分叉**：`resolve-commit-brief.sh` 内联副本仍是旧实现，降级分支症状只在特定环境出现 | `resolve-commit-brief.sh:121-188` 自持 `parse_q2/match_path` 副本 | 📋 | 并行 CTO |
| CTO-7 | **专家数 6 vs 8**：`expert/` 实为 6 个，文档/施工图写 8（`AGENTS.md` 一处写 7） → 真红之一无法修 | `tests/agent/expert-file-loader.integration.test.ts` 断言 ≥8 实得 6 | ⛔ **待创始人裁定** | 创始人 |
| CTO-8 | **alloc 骨架 brief 泄漏真仓**：测试只设 `SYNO_TASK_STATE_DIR` 未设 `SYNO_BRIEF_DIR` → 每次运行写 20 份骨架进真实仓库（实测泄漏 56 份） | `tests/control-tower/alloc-task-id-lock.test.sh:42` | ✅ D718 #518（双注入 + 源头守卫） | D718 |

### C. D717 履职回传（3 条 + 1 精度记录）

| ID | 问题 | 物理证据 | 状态 |
|---|---|---|---|
| D717-N1 | **alloc 骨架缺 `#CRITERIA`** → `check-brief-parseable.sh` 第②项在 CI strict 下必红（D715/D716/D718/D720 同缺） | alloc 生成的骨架模板无 `#CRITERIA` 行 | 📋 |
| D717-N2 | **D721-D740 骨架 brief 残留主树/各 worktree**（#518 只堵源头未清存量）→ 会把本地 brief 解析回退劫持到骨架模板，造成本地假红 | D717 本地实测被拦一次 | 📋 |
| D717-N3 | **本地全量 vitest 改写 tracked 生成物**（`extensions/industries/*/thresholds.json`、`tests/output/expert-quality-cross-industry.json`） | D717 已回滚；与 `PLAN-product-lines-test-side-effect` 同族 | 📋 |
| D717-N4 | **精度记录（非缺陷）**：D717 报告写「创始人 2026-09-10 **已裁定**」某模型，实际交接文档记的是创始人「**提出**」；「Evidence 过期 = 蒸馏完成」全仓零命中（属其推断）。**代码注释措辞准确**，仅报告行文偏强 | 交接文档 `PLAN-d660-data-lifecycle` | 已记录，D727 spec 须对「蒸馏完成」确权 |

---

## 二、P2 级（K3 D715 发现 7 条，均已登记）

| ID | 问题 | 位置 |
|---|---|---|
| K3-P2-1 | `grep -oP` 家族残留（GNU runner 无害） | `.github/workflows/ci.yml:93` |
| K3-P2-2 | Mac 1-6 timing 记录 `task` 字段为脚本默认值 `D527` | `evidence/D712-mac-20260913/1-6-timing.json:4` |
| K3-P2-3 | 测试向终端泄漏 git 噪音（`On branch main / nothing to commit`） | `tests/control-tower/merge_writeset_gate.test.sh` ⑨b |
| K3-P2-4 | Win run-F `timing.json` 用户字段与会话用户表述不一致 | `D712-win-20260913/run-F-nosandbox-runtime/timing.json` |
| K3-P2-5 | D707 无 python3 降级分支在 `SYNO_CI=1` 下同为 fail-open（实际不可达） | `scripts/pre-commit-check.sh:854-860` |
| K3-P2-6 | D708 gate 对纯文档 PR 双层放行 → 文档夹带不在防面（有意设计，备查） | `ci.yml:60` + `merge_writeset_gate.py:70-72` |
| K3-P2-7 | 台账滞后：M9 未入账 | ✅ D723 已补录 |

---

## 三、本批验收结论（4 项，全部 CTO 物理复核，非采信自报）

### D715 K3 审计 — ✅ 通过
报告 297 行 + task-state 回写（D706-D708=audited/PASS、D715=audit_done）；写集 6 文件、零审计脚本触碰。
K3 四项 P1 我逐条独立复核：P1-1 亲自跑 merge-tree 复现 4 冲突；P1-2 命中我自己的文档（已订正）；P1-3 代码路径核实；P1-4 表述精确化为「代码在 git、日志证据不在」。
**一处表述需精确**：聊天摘要写「未落 git」易被读成代码没进 git，报告正文是准确的。

### D716 两份 spec — ✅ 通过（PR #519）
| spec | 行数 | 必答要素 | 关键裁定 |
|---|---|---|---|
| 监测契约（D-1 落地） | 551 | 节奏/渠道/升级规则/责任人 35 处命中；Mac↔Win 边界 7 处；默认值（24h / 7 天 / 一页纸 / 周报）31 处命中 | 契约目录=Win 域、loader=Mac 域 |
| 1-5 双引导收敛 | 434 | Win/Mac 域边界 7 处；含 Test Requirements + Wiring Verification 段 | **裁定：`/app/setup.html` 用 410「已退场」页承接（挂在 L300 静态挂载之前，顺序即优先级），删除 `app/setup.html`+`setup.js`+`setup.css`，补守卫测试 + runbook 三行**；并回答了「为何 410 而非 302」（书签/URL 兼容） |

实施归 Win 线（`src/server.ts` = Claude 专属）。

### D717 产品真债两项 — ✅ 通过（PR #527）
| 声称 | 我的复核 |
|---|---|
| `package.json:88` 补 `react-markdown ^10.1.0` | ✅ 在位；锁文件命中 1 处 |
| `src/l3/evidence-retention.ts` 245 行、`:184` 调 `expireOld` | ✅ 行数/行号逐字命中 |
| `src/deploy/bootstrap.ts:1189-1224` 接线（启动即跑 + 每日 03:30 cron + degraded） | ✅ 在位 |
| 测试 21 用例 / 51 expect | ✅ 实测 21 / 51 |
| CI 12/12 全绿（head `8b892995`） | ✅ 实测 12 检查 / 0 非 success |
| 默认 `permanent`（不按时间删）+ 显式 env 才开启 + 配置非法绝不删 | ✅ 保守方向正确，CTO 认可 |

**措辞修正**：报告把创始人的「提出」写成「已裁定」（见 D717-N4）——代码注释无误，记录在案不阻断合并。

### D718 控制塔小修批 — 🔧 4 项中 2 项已合
✅ #518 骨架泄漏根治（双注入 + 源头守卫）｜✅ #522 跨日认领改身份锚点 + 同数 tie-break（**附带修掉我登记的 CTO-5 一部分**）｜🔧 #523 去 BOM｜🔧 #524 台账登记

---

## 四、下一批派单（5 条，写集零交集）

| D# | 执行方 | 内容 | 依赖 |
|---|---|---|---|
| **D725** | 🔍 K3 审计 | 控制塔修复批次审计：D713 / D714 / D717 / D718(#518+#522+#523+#524) / **D721 / D722**（CTO 自改门禁亦受审）/ D723；**1-8 复验须绑 D714 后新产物** | D717 合并；Mac 重打新产物 |
| **D726** | 🛠 Mac 编码 | ① `graphbridge-wiring` 真红定位与修复（产品缺陷 or 测试漂移二选一，需给判据）② K3-P1-3 状态机 machine 段对齐 k3 段（fail 优先 + (date,at) tiebreak）③ K3-P1-1 产证文件名加机器维度 ④ D717-N1/N2 骨架 #CRITERIA + 存量清理 | 专家数裁定（①的兄弟项） |
| **D727** | 📋 dev-doc | D660 数据生命周期五层阶梯 spec（**须对「蒸馏完成」确权** + 与已落地 `evidence-retention.ts` 对齐）+ 1-5 实施交接包给 Win | 无 |
| **D728** | 🧭 并行 CTO | ① CTO-6 内联回退解析器收敛（删副本 + 显式 degraded）② K3-P2-1/P2-3/P2-5 小修 ③ **派单前置机器校验**（防 K3-P1-2 类失实：派单文档声称的 PR 必须机器核验 merged） | 无 |
| **Win 线** | Claude（创始人转） | 1-5 双引导收敛**实施**（按 D716 spec2：410 承接 + 删旧引导）+ 端到端收尾 | D716 spec 已就绪 |

---

## 五、给创始人的复制块

**① 转 K3 审计 session（🔍）**
```
【D725】控制塔修复批次审计（P0）
> 清单: docs/synova/coordination/问题总账与下一批派单-20260913.md §四 D725
> 对象: D713 签名门禁 / D714 F3 修复（含其证据缺失 P1-4 一并核）/ D717 产品真债两项 / D718 四项（#518/#522/#523/#524）/ D721 CI 棘轮基准 / D722 认领解析 / D723 台账回流
> 特别说明: D721/D722 是 CTO 自己改的门禁——按红线同样受审，不给自己豁免
> 1-8 复验: 必须绑 D714 之后的新产物重装实测（旧产物已 stale）
> 红线: 结论只认 K3；不改 scripts/audit/；不替创始人下业务结论
```

**② 转 Mac 编码 session（🛠）**
```
【D726】产品真红 + 状态机健壮性 + 产证命名（P1）
> 清单: docs/synova/coordination/问题总账与下一批派单-20260913.md §一（K3-P1-1 / K3-P1-3 / CTO-3 部分）+ §三 D717 回传（D717-N1/N2）
> ① graphbridge-wiring 真红: 判「产品缺陷」还是「测试期望漂移」，必须给判据（打点确认 afterRun 是否仍被调用）
> ② 状态机 machine 段: 对齐 k3 段语义（fail 优先 + (date,at) tiebreaker）——现状靠文件名字典序决胜，纯属巧合
> ③ evidence-writer 产证文件名加机器/平台维度（修双机撞车）
> ④ alloc 骨架 brief 补 #CRITERIA + 清理 D721-D740 存量骨架残留
> 写集: scripts/product-lines/、scripts/control-tower/alloc-task-id.sh、tests/**；不改 scripts/audit/
```

**③ 转 dev-doc session（📋）**
```
【D727】D660 数据生命周期 spec（P0）
> 背景: D717 已按「默认不按时间删」落地 evidence-retention.ts，但其依据的「蒸馏完成」概念全仓无出处 → 需本 spec 确权
> 交付: 五层阶梯（保留分级/触发条件/降级契约）+ 与已落地代码对齐 + 1-5 实施交接包（Win 侧执行，按 D716 spec2）
> 要求: 过 dev-doc-gatekeeper；写集声明；Done 可证伪
```

**④ 转并行 CTO session（🧭）**
```
【D728】控制塔残余小修 + 派单前置机器校验（P1）
> ① resolve-commit-brief.sh 内联回退解析器与 brief_parser 分叉 → 收敛为单一路径（删副本 + 显式 degraded）
> ② K3-P2-1 grep -oP 残留 / K3-P2-3 测试噪音 / K3-P2-5 fail-open 语义
> ③ 新增「派单前置机器校验」: 派单文档声称的 PR 状态必须由 API 核验（防 K3-P1-2 类失实再犯）
> 要求: 每项独立 PR；先复现再修；反向验证必红；加载 ctrl-tower-change + windows-compat
```
