# 阶段 0 报告 · 冻结与止血｜并行 CTO

> 产出者：**并行 CTO（统筹执行）** ｜ 日期：2026-09-26
> 受件：创始人（转 Mac-CTO 收件闸 + K3 抽验）
> 依据派单：`docs/synova/coordination/派单-治理体系最小充分形态-工程派单-20260926.md`（阶段 0）
> 唯一蓝图：`/Users/wane/山河研究院/99-综合/综合报告/20260925-治理体系最小充分形态-最终方案.md`（v1.3）
> **断面**：执行时实测 `origin/main = ee721b0a`（派单冻结值 `ef299746` **已前进**，故本报告全部数字在新断面重跑，不抄派单）
> 报告格式：派单 §六（动作清单含原始输出｜判据逐条 ✅/❌/⚠️｜未清项｜需 CTO 裁决｜需创始人裁决）

---

## 〇、开工侦察（三仪表盘 + 冻结基线）

| 项 | 实测 | 原始命令 | 状态 |
|---|---|---|---|
| 断面漂移 | `origin/main = ee721b0abf52`（派单冻结 `ef299746`） | `git fetch --all --prune; git rev-parse --short=12 origin/main` | ⚠️ 已前进 ⇒ 派单全部冻结数字须重跑 |
| 主树状态 | `/Users/wane/SynovaAgent` 在 `chore/cto-doc-slim-phase1-writeset-exempt` @ `b8573c7a`，`[ahead 14, behind 22]`，**`UU src/deploy/bootstrap.ts` 未解冲突**，另有 3 个未跟踪文件 | `git status -sb` | ⚠️ 他人活跃工作区，本阶段**零写入** |
| 工作树总数 | **310**（蓝图实测 301 / 派单记 149 含未合并；工作树数只作趋势不作判据） | `git worktree list \| wc -l` | ⚠️ 仍在涨 |
| `bypass.log` | 1,813 行；今日 `detected-bypass` = **0**，昨日 = 1；`GATE_FAIL_SOFT` = **101** 行；`COMMITTED` = 1,401 行 | `grep -c "<今日>.*detected-bypass" .claude/bypass.log` | 🟢 无真绕过；⚠️ 101 行软失败污染（见 0-①） |
| `pre-commit-failures.log` | 156 行；**近 24h = 107 条 exit=1** | `awk -v d="$(date -u -v-1d +%Y-%m-%dT%H:%M:%SZ)" '$1>d' .claude/pre-commit-failures.log \| wc -l` | 🔴 远超设计阈值（>10/24h = 门禁过激告警） |
| 仪表盘②`DASHBOARD-CN.md` | mtime **2026-08-28**（月级未刷新） | `ls -l docs/synova/DASHBOARD-CN.md` | 🔴 陈旧 |
| 仪表盘①`product-progress.html` | mtime **2026-09-21** | `ls -l docs/synova/product-lines/product-progress.html` | ⚠️ 5 天未刷新 |
| **冻结基线** `check-*`（新增门禁=0 的测量面） | 工作树 **48**（排除 `scripts/audit/` = 47）｜`origin/main` **49**（排除 audit = **48**） | `find ./scripts -path './.synova-wt-*' -prune -o -name 'check-*' -type f -print \| wc -l`；`git ls-tree -r --name-only origin/main -- scripts \| grep -E '(^|/)check-[^/]+$' \| wc -l` | ✅ 与派单冻结值 48/49/47 逐位吻合 |
| `pre-commit-check.sh` | **1,603 行** | `wc -l < scripts/pre-commit-check.sh` | ✅ 与蓝图一致 |

**冻结证据（阶段 0–4 期间新增门禁数 = 0 的复跑命令）**：
```bash
git ls-tree -r --name-only origin/main -- scripts | grep -E '(^|/)check-[^/]+$' | grep -v '^scripts/audit/' | wc -l   # 起点 = 48
```

---

## 一、动作清单

### 0-① 重装 git hook（P0，解生成器漂移）— 已执行

**背景修正（claim vs fact，必须并列）**：派单称"旧 hook 把软失败写进 `bypass.log` ⇒ 会被当日自阻断"。
实测**该因果不成立**：当前的硬阻断只看 `detected-bypass`，不看软失败行——
- `scripts/pre-commit-check.sh:242` → `BYPASS_COUNT=$(grep -c "${TODAY}.*detected-bypass" "$BYPASS_LOG" ...)`
- `scripts/pre-commit-check.sh:1085` → `grep -cE "$(date +%Y-%m-%d).*detected-bypass" ...`
- `.claude/settings.json` 的 4 个 PreToolUse hook（`hook-block-write.sh`/`hook-git-detect.sh`/`hook-git-guard.sh`/`hook-session-start.sh`）**全部零 `bypass` 引用**（逐个 `grep -c bypass` = 0）。
⇒ 准确表述：旧 hook 污染的是**证据账本**（101 行 `GATE_FAIL_SOFT` 混在 1,813 行 `bypass.log` 里，`bypass.log` 是 D331 对账 + 多个看板的解析输入），**不是当日自锁**。**该声称未复现，登记为冲突项（见 §四-2）。**
"重装 hook" 本身仍是必要动作：`.git/hooks/` 与生成器漂移 7 天（C13 判据），且 D508 的设计意图明确要求软告警离库。

**动作与原始输出**

```
$ stat -f '%Sm | %z bytes | %N' -t '%Y-%m-%d %H:%M:%S' .git/hooks/pre-commit scripts/install-hooks.sh
2026-08-21 17:14:00 | 977 bytes | .git/hooks/pre-commit        ← 漂移 7 天
2026-08-28 21:05:37 | 9543 bytes | scripts/install-hooks.sh

$ grep -c 'bypass.log' .git/hooks/pre-commit        # 旧版
1

$ bash scripts/install-hooks.sh
=== SynovaAgent D318 Git Hooks 安装（双机可移植）===
  ✅ pre-commit
  ✅ commit-msg
  ✅ pre-push
  ✅ post-commit
  ✅ clone git 配置初始化 — user=synova-mac <claworg@users.noreply.github.com> / quotepath=false / credential.helper
  ✅ git config merge.union.driver — bypass.log 自动合并
  ✅ git config http.lowSpeedLimit/Time — 慢连接 30s 快速失败
  ✅ git alias synova-commit — bash=bash
install-rc=0
```

**判据逐条**

| 派单判据 | 结果 | 证据 |
|---|---|---|
| hook 内 `bypass.log` 字样消失 | ⚠️ **字面 ❌ / 实质 ✅** | `grep -c 'bypass.log' .git/hooks/pre-commit` = **2**，但两处均在 D508 说明**注释**里（"软门禁噪声移出 bypass.log…"）；**写入目标已换**，见下条与 diff |
| 改为 `gate-soft-warnings.log` | ✅ | `grep -c 'gate-soft-warnings.log'` = **1**，且是唯一写路径：`>> "$ROOT/.claude/gate-soft-warnings.log"` |
| **hook 与生成器同哈希** | ✅ | 从 `scripts/install-hooks.sh:43-58` 提取 `body` 变量（去掉 `body='` 与结尾 `'`，补 `printf '%s\n'` 的换行）后 `shasum -a 256` = `609788b1ac3bfb31`；`.git/hooks/pre-commit` = `609788b1ac3bfb31`；`diff` 无差异。生成器文件 `git hash-object scripts/install-hooks.sh` = `aeeb316af05bd2821384ddc47bc17e6f7db2bf6c`，且 `git rev-parse origin/main:scripts/install-hooks.sh` **同哈希**（= 装的就是 main 权威版） |
| 旧/新 hook 差量（唯一实质差异） | ✅ | `diff old new`：旧版 `>> "$ROOT/.claude/bypass.log"` → 新版 `>> "$ROOT/.claude/gate-soft-warnings.log"`（+2 行注释） |

**改坏即红（反例夹具，同一红灯输入 × 两个 hook）**
夹具：scratch worktree `/private/tmp/cto-stage0-hooktest` @ `ee721b0abf52`（detached，非他人工作区），staged `src/zz-hooktest-fail.ts` = `export const zzHooktestFail = { a: 1 } as any;`（铁律 38 红灯）。

```
BEFORE      bypass=1825  pre-commit-failures=NA  gate-soft-warnings=NA
旧 hook     exit=0       bypass=1826 (+1)  pre-commit-failures=1   gate-soft-warnings=NA
新 hook     exit=0       bypass=1826 (+0)  pre-commit-failures=2   gate-soft-warnings=1  ← 软失败在此
旧 hook stdout: ⚠️ 本地门禁未通过（exit=1）— 已放行，CI 将作为权威判定（merge 前必须绿）
新写入行: 2026-09-26T01:12:30+08:00 | GATE_FAIL_SOFT | exit=1 | branch=
```
⇒ **有它则绿/无它则红可判**：同一红灯下，旧 hook 让 `bypass.log` +1，新 hook 让 `gate-soft-warnings.log` +1 而 `bypass.log` 不变。夹具已清理。

**影响面（必须知情）**：`git config core.hooksPath` = `/Users/wane/SynovaAgent/.git/hooks`（**绝对路径**）⇒ 本次重装对**全部 310 个工作树**同时生效。语义未变（仍是"本地软提示 + CI 权威"，`exit 0`），仅日志去向变更。

### 0-② 归档 3 个未入库日志 — 队员执行中（task-1）

**队长勘误（4 条，逐条都会造成假绿，已下达）**：

| # | 事实（原始命令/行号） | 后果 | 处置 |
|---|---|---|---|
| E1 | `grep -n 'runs-on' .github/workflows/ci.yml` → 全 `ubuntu-latest`/`${{ matrix.os }}`；`grep -rn 'self-hosted'` → **零命中** | gitignored 的本地日志**在 CI 上不存在** | artifact 只能收 CI 当场产物 |
| E2 | `gate-hits.log` 由 `scripts/pre-commit-check.sh:125/128` 确定写（`ci.yml:84-87` quality job，`SYNO_CI=1`）；`degraded-events.log` 由 `scripts/control-tower/merge_writeset_gate.py:457` **条件**写 | 前者 ✅ 确定产生；后者 ⚠️ **仅走 skip/degraded 路径才写** | 判据必须三态，不许升格 |
| E3 | `actions/upload-artifact@v4` **默认排除隐藏文件**（`include-hidden-files` 默认 `false`）；两条 path 全在 `.claude/` `.codex/` 里 | **不加 `include-hidden-files: true` = 上传空 artifact 而 job 照绿 = 判据假绿** | 必须加，并要求变异体 M1（删它 → 断言必红） |
| E4 | 上传步骤本身失败会让 `quality` job 变红 | 相当于**新增 1 条 CI 红路径** | 采纳 fail-loud（不加 `continue-on-error`），但**登记为 CI 语义面扩张，报 Mac-CTO 批准** |
| E5 | `merge_writeset_gate.py:77-79` `DOC_SCOPE_RE` 仅含 `docs/ .claude/ memory/ task-state/ .github/ *.md`；`:444-453` 无声明且含 non-doc → **`return 2` fail-closed** | 快照若落仓根 `evidence/` 会让本 PR **CI 红** | 快照落 `docs/synova/coordination/evidence/gate-logs-20260926/`，**PR 必须透明声明该理由**（不是为绕门禁，本 PR 未增删改任何检查） |
| E6 | `scripts/check-secrets.sh:31-34`/`:136` 的扩展名白名单**不含 `.log`** | 门禁**不会**扫快照 | 作者必须自扫留证（我预扫：`ghp_`/`github_pat_`/`api_key` 均 0；`sk-…` 32 命中**全为误报**，唯一值 = `sk-decomposition`） |

**判据的面拆分（0-②）**：
- **CI 侧持续归档** = artifact（`retention-days: 90` + `include-hidden-files: true` + `if-no-files-found: warn`）；覆盖 `gate-hits.log` ✅ / `degraded-events.log` ⚠️条件 → **正好产出阶段 1 所需的「CI 侧门禁失败率」**（U3 的唯一证据来源）。
- **本地存量可复跑** = **一次性日期化不可变快照**入库（蓝图 D1 判据原文允许「`git ls-files` 能列出它们」）⇒ K3 的审计工作区（`/Users/wane/Synova-k3独立审计`，同机另一 clone）也能读到。**这是"一次性归档"，不是"日志回归入库"**；再入库成本 ≈ 3.2 MB（766,989 + 2,431,750 + 9,801 B）。
- `pre-commit-failures.log` 的**持续**覆盖 = ⚠️ **结构性不可达**（CI 不 commit ⇒ 无 hook ⇒ 无此文件），只能靠一次性快照，登记为未清项。

（队员回执前，本条状态 = **进行中**，不算达标。）

### 0-③ 1,415 未合并提交 / 149 工作树分类 — 已收件：交付成立
队员 `squad-analyst`｜分支 `chore/stage0-backlog-classification-20260926` @ `51f99889e7d4`（local = remote = PR head，三方一致）｜**PR #827**｜交付物：`阶段0-积压分类-20260926.md`（843 行，221 行分类表 + 附录 A 逐字原始输出）+ `阶段0-C16基线-20260926.json`（机读）
PR 改动面 **3 文件**（2 交付 + 1 个 post-commit hook 自动登记的 `.claude/bypass.log`，作者已披露）。

**C16 起点（口径A = 蓝图 §九.6 命令逐字口径）** = **158 工作树 / 1,546 未合并提交** @ `2026-09-26T01:13:03+08:00`，断面 `ee721b0a`。
与冻结值 149/1,415 差 **+9/+131**，同工作树集分解 = **工作树集增长 +16/+428、断面推进 −4/−229**；top `tmp-d942` 110 **与冻结值一致**（✅ 对账成立）。

**分类（221 行）**：在途 **162** ｜ 重复劳动 **42** ｜ 待定（疑似废弃）**17**（限口径A：124 / 17 / 17）。
重复劳动用 **patch-id**（非目录名/mtime）：124 对 ratio≥0.5；**95 行 `unique_pids=0`**。

**四个推翻既有假设的硬发现**：
1. **⚠️ 蓝图 C16 命令漏计 detached worktree**：`§九.6` 的 `if not b: continue` 跳过无 `branch` 行的 detached 树。**但量级与口径强相关，必须并列两个数（独立自验员复核后追加）**：
   - **按工作树计**：321 树中 detached = 103（32%），其中 65 个含未合并提交 ⇒ **"含未合并提交的工作树"里最多 29% 只存在于 detached 维度**；分析师的算式 = 63 树 / **+621 提交**。
   - **按提交可达去重计**：87 个唯一 detached HEAD 中 **85 个同时位于某远端分支上** ⇒ 真·唯一漏计只有 **2 工作树 / 14 提交（占口径B 的 0.62%）**。
   - ⇒ **正确表述是"按工作树枚举会漏 29%，按提交可达去重只漏 0.62%"**，不可用一句"漏计 621 提交"混谈（这正是蓝图 C1 要治的"同一指标多口径"病）。
2. **⚠️【口径矛盾·比漏计更重】冻结值与派单自带命令结构性不一致 —— 已裁决（独立自验员三条原始证据）**：
   - **冻结值「149 / 1,415 / top=`tmp-d942` 110」属口径①＝蓝图 §九.6 那条 `git worktree list` 命令**（证据：`b=ln[7:]` ⇒ 输出带 `refs/heads/` 前缀；复核报告逐字 `110  refs/heads/tmp-d942  (.synova-wt-cto-a)`；自验员逐字复跑 §九.6 **首行即 `110  refs/heads/tmp-d942`**）。
   - **反证**：同断面跑**派单 §十**那条命令 → **447 分支 / 3,402 提交，top = `feat/desktop-phase0-auth` 186，列表内无 `tmp-d942`**（`refs/remotes/origin/tmp-d942` 不存在）⇒ **该命令结构上永远复现不了 1,415**。
   - ⇒ **C16 起点 = 口径①**：**158 工作树 / 1,546 提交 @ `2026-09-26T01:13:03+08:00`，断面 `ee721b0a`**（单一快照同源，作者已定 `metrics.lane_A`）；其余 6 个口径入附注（自验员「C16 口径对账表」7 口径）。
   - **教训**：这正印证了蓝图 C1 判据那句"**必须先声明数法**"——同一对象 7 种口径得 7 个数，不声明口径就无法对账。

**口径对账表（独立自验员实测，全部带 as_of）**

| # | 口径定义 | 数字 | as_of |
|---|---|---|---|
| ① | 蓝图 §九.6 / 分析师 lane_A（worktree 挂载分支，跳 detached） | **158 / 1,546**（作者）·163/1,681（自验员复跑） | 01:13:03 / 01:27:56 |
| ② | 派单 §十（遍历 `refs/remotes/origin`） | **447 / 3,402**（top=`feat/desktop-phase0-auth` 186） | 01:20:38 |
| ③ | 工作树 HEAD 求和（含 detached） | 226 / 2,266 | 01:22 |
| ④ | 工作树 HEAD 唯一并集 | **1,388** | 01:22 |
| ⑤ | 全部本地分支（含未挂树） | 350 / 3,138（本地独有 116 分支 / 736 提交） | 01:21:43 |
| ⑥ | detached 唯一漏计（不在任何远端分支） | **2 树 / 14 提交** | 01:22 |
| ⑦ | 分析师 lane_B（=③，仅计入判据与否不同） | 221 / 2,167 | 01:13:03 |

⇒ **"149/1,415"只能与口径①对账**；②③④⑤⑥ 均不同源。

3. **`tmp-d942` 的 110 提交不是废弃堆**：tip `b3182f67` = **open PR #738** 的 head sha，`commits=110` 完全吻合。
4. **`fix/d962-2a-iron-laws-wiring` 本地 + 远端均已消失**（`git ls-remote` 空）⇒ 复核报告 §八-4「#803 线是否重复劳动」的**旧假设失效**，现为 d962 三支同源链。
5. **积压不是静止的**：口径A 在 **9 分钟内** 158/1,546 → 159/1,552 → 160/1,572 → 161/1,614 → **162/1,671**（worktree 总数 316→321→320）⇒ **曲线起点必须带 `as_of`，工作树总数不作判据**（与派单口径一致）。

**改坏即红（跨断面夹具，证明数字非硬编码）**：同一脚本 `ef299746` → 口径A **165 / 1,888**；`ee721b0a` → 口径A **162 / 1,673**。

**红线核验**：零删除 / 零 prune / 零 ref 改动；主树零 git 写操作；冻结期 `scripts/` 零改动。
**推送门禁**：首次 3 连败 = `golden-case` 门禁 `npx` 因 `~/.npm` **EPERM** + 作业树无 `node_modules`（**环境问题，非回归**）；按队长条件①在真实报红后**只读**软链 `node_modules`（gitignored、未入写集、已登记），随后全门禁通过。**未用 `--no-verify` / 未改门禁 / 未 `|| true`。**

**队长三项裁决**：① C16 含 detached ⇒ 口径A 作判据、口径B 作附注 + 上报，**不擅改判据**；② 待定 17 行**保持"疑似"、不删**（11 行有 closed-unmerged PR、6 行无 PR 且 >30 天）；③ 95 行 `unique_pids=0` 可作**阶段 2 候选**，但需单独批次 + 双批准 + "删了不丢交付"的反例证据。


### 0-④ 独立自验（波次 1）— 已收件：**通过**
队员 `squad-verifier`（非作者）｜分支 `chore/stage0-selfverify-20260926` @ `cfd74b7d20b7`（12 位 `ls-remote` 交叉验证一致）｜**PR #824**

独立重跑（不采信我的哈希）：
- **同哈希双法一致**：机械提取生成器 body（1,209 B）与"隔离夹具真跑 `install-hooks.sh`"两条路径 → `609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401`，**字节级 diff IDENTICAL**；附带 4 个 hook 全部 MATCH（全量重装）。
- **改坏即红（同一 `as any` 红灯夹具，两次门禁判决逐字相同）**：RUN A（旧 hook）`bypass.log` 1825→1826（+1，`GATE_FAIL_SOFT`）、`gate-soft-warnings` MISSING→MISSING；RUN B（新 hook）`bypass.log` 1825→**1825（+0）**、`gate-soft-warnings` MISSING→**1**。
- **语义未变**：旧↔新 diff **仅"日志落点"一行**（+2 注释），两端均 `exit 0`；`core.hooksPath` = `/Users/wane/SynovaAgent/.git/hooks`（绝对路径 ⇒ 全 310 工作树共享本次重装）。

**"自锁"声称核验（独立复现我的结论，且更强）**：向夹具 `bypass.log` 注入 **8 条今日** `GATE_FAIL_SOFT`（累计 109 条）后真跑 `pre-commit-check.sh` → **EXIT=0、绕过审计通过、全部 13 组通过**，GATEKEEPER 段根本不打印 ⇒ ❌ **声称未复现**。
**四态受控实验（自验员）**：CASE C = 3×今日 `COMMITTED`（**无软失败、无绕过，仅正常提交登记**）→ **EXIT=1**；CASE D = 3×今日 `GATE_FAIL_SOFT` → EXIT=1；CASE E = 昨日记录 → EXIT=0；CASE F = 3×`detected-bypass` → EXIT=1 ⇒ 证明该熔断与旧 hook **无因果**，是**口径过宽致误报**（详见未清项 8）。

### 0-④ 独立自验（波次 2）— 已收件：**通过**（1 项时间性未决）
报告 v1.4 @ `74739ec00e98`（PR #824）。逐条（11 项）：
- **CI 侧语义层全过**：`retention-days: 90` ✅；**`include-hidden-files: true` 的必要性成立**（两条 path 均在 dot 目录内；其"充分性=artifact 非空"待 CI）✅；`if: always()` ✅（step 在 Iron Laws 之后）；**`ci.yml` 0 删除行**（`grep -c '^-[^-]'` = 0）✅、新增 step = 1。
- **快照**：3/3 TRACKED（可 `git ls-files` 列出）✅；**3/3 sha256 与主树取证现场逐字一致**（自验员自算，不采信作者）：`2a6a80ba467220da` / `30929d4ed4e99c2c` / `1148103f70ff3da6`，字节数 766,989 / 2,431,750 / 9,801 逐一 MATCH；凭证自扫 0 命中 ✅。
- **PR 描述**：R2 六项齐 ✅；"非门禁语义变更 + 请 Mac-CTO 批准"✅；"静默失败面"话术已按裁决更新 ✅；**无 `## 写集豁免` 段**（按 (A) 裁决不使用豁免）✅。
- **改动面 vs 声明**：`task-state/D1009.json` 的 `write_set` 为 **list / 6 条**，实际 7 文件 = 6 声明 + `.claude/bypass.log`（D708 内置豁免）✅。
- **artifact 真下载 = ⏳ 时间性未决**（`run_id=36167351846` 与修复后 `36168645768` **均 queued**，as_of `2026-09-26T01:39:14+08:00`）⇒ **不判通过**，一键补验命令已入 task-1 文档 §5.5。

**G6（自验员发现，作者与队长均未预见 · 本轮最高价值）**：`quality` job（`TypeScript + Lint + Iron Laws`）是 **main 的必需检查** ⇒ fail-loud 的 artifact step 一旦瞬时故障会**阻塞全队所有 PR 合并**（详见 §四-3 与裁决）。



---

## 二、判据逐条（阶段 0 汇总）

| # | 派单判据 | 结果 | 备注 |
|---|---|---|---|
| 0-①-1 | hook 内 `bypass.log` 字样消失 | ⚠️ **字面 ❌ / 实质 ✅** | 2 处为 D508 注释；写路径已换。**判据自相矛盾**（与"同哈希"不可同时成立），见 §四-1 |
| 0-①-2 | 改为 `gate-soft-warnings.log` | ✅ | 唯一写路径；独立自验员双法同哈希 |
| 0-①-3 | hook 与生成器同哈希 | ✅ | `609788b1ac3bfb31…8401`（两种独立方法）；生成器 = main 版 `aeeb316a` |
| 0-② | 三个日志可在异机复跑（≥90 天） | ⚠️ **部分达标**：CI 侧语义层 ✅ / 快照 3/3 TRACKED ✅ + sha256 一致 ✅ / **artifact 非空 = ⏳ 时间性未决（CI queued）** / `pre-commit-failures.log` 持续覆盖 ⚠️ 结构性不可达 | 见 §一 0-② 与 §四-3 |
| 0-③ | 分类表 + C16 起始值与下降曲线 | ✅ | 221 行分类表 + 机读 JSON；**口径已裁决 = 口径①（158/1,546 @01:13:03）**；独立自验 12 抽 11 差 0（1 为漂移） |
| 0-④ | 独立自验（非作者） | ✅（1 项时间性未决） | PR #824；F1/F2/F4 已转作者返修并闭环 |
| 全局 | 期间新增门禁数 = 0 | ✅（起点 48，排除 audit 口径） | 阶段末复跑见 §〇 命令；本阶段 `scripts/` 治理面仅 0-① 的**执行**（未改生成器） |
| 全局 | 分类完成前不删分支/工作树/提交 | ✅ | 本阶段**零删除 / 零 prune / 零 ref 改动**；未执行任何 `worktree remove`/`branch -D`/`reset` |
| 全局 | 周期内不新增门禁/不改门禁语义 | ✅（待 Mac-CTO 确认 0-②） | 0-② 仅加 1 个非阻断 step；**未经批准不合并** |


---

## 三、未清项诚实登记

1. **判据字面冲突（0-①-1）**：`bypass.log` 字样未"消失"（2 处注释），实质达标。**不采用删注释的方式凑字面**（那属于改断言凑绿）。
2. **派单声称未复现**：旧 hook ⇒ 当日自阻断 **不成立**（见 0-① 背景修正，附 file:line）。真实危害是证据账本污染，不是自锁。
3. **0-② 第三个日志**（`pre-commit-failures.log`）在 GitHub 托管 CI 上不可见 ⇒ 需另起机制（建议：本机 `scripts/` 侧把三个本地日志快照推到专用 ref，≥90 天由 git 保留）——**属新机制，本阶段只登记，待 Mac-CTO 批准**。
4. **断面漂移**：`origin/main` 已 `ef299746 → ee721b0a`，派单/蓝图全部冻结数字须逐条重跑（本报告已重跑，未抄）。
5. **主树不清洁（C7）**：`/Users/wane/SynovaAgent` 有 `UU src/deploy/bootstrap.ts` + 3 个未跟踪文件，且 `[ahead 14, behind 22]`。属他人活跃工作区，本阶段不触碰；**阶段 2"主树设只读"必须把它作为首个输入**。
6. **门禁过激信号**：`pre-commit-failures.log` 近 24h **107 条 exit=1**（设计阈值 >10/24h 即告警）⇒ 建议阶段 1 一并联证，不单独下结论。
7. **仪表盘陈旧**：`DASHBOARD-CN.md` 停更在 2026-08-28、`product-progress.html` 停在 2026-09-21 ⇒ 与"创始人一眼看到"目标冲突，登记待处理（非阶段 0 判据）。
8. **【新发现·FP 熔断】`scripts/workflow/loop-context.sh:88`**：`BYPASS_COUNT=$(grep -c "$TODAY" "$BYPASS_LOG" ...)` —— 把当日**全部**行都当"绕过次数"（`COMMITTED`/`GATE_FAIL_SOFT` 一并计入），阈值 3 → `BREAKER_TRIPPED=1` → `exit 1`。
   - 实测：昨日（2026-09-25）该 grep 命中 **82 行**，其中真绕过 `detected-bypass` **仅 1**、软失败 `GATE_FAIL_SOFT` 33 ⇒ **82× 高估，必然熔断**。
   - 当前**未接线**（`git ls-files` 口径下引用者仅：`CLAUDE.md`/`LOOP.md`/`loop-run-log.md`/本任务 brief/`UPGRADE-SPEC-控制塔与审计流程-20260817.md`/`loop-score.sh`；`loop-score.sh:75/115` 只判**文件是否存在**，不调用）⇒ 危害为"一旦接线即误熔断"。
   - 归入阶段 5「红线升级」的 **Gatekeeper FP 熔断** 那一格（与 `pre-commit-check.sh:242` 已修正的 `detected-bypass` 口径同型，但这一处**没跟上修正**）。
9. **【新发现·结构地雷】`evidence/<D>/changes.json`**：蓝图 §八.4 计划新建该约定，但 `changes.json` **不匹配** `merge_writeset_gate.py:77-79` 的 `DOC_SCOPE_RE`（既不在 `docs/` 等前缀下，也不以 `.md` 结尾）⇒ 在"无写集声明"的卡上会命中 `:444-453` 的 **fail-closed**。阶段 3/4 落地前必须先解决声明来源，否则同样的红会复现。**双重障碍（队员实证第二重）**：`.gitignore:76` 有 `evidence/` 规则 ⇒ 任何名为 `evidence/` 的目录**整目录被忽略**，只能 `git add -f` 强塞，与"运行态不入库"设计冲突。
10. **【新发现·`infer_did` 误绑】`scripts/control-tower/merge_writeset_gate.py:187`**：`git log --max-count=$FALLBACK_SCAN_DEPTH --format=%s <head>` —— 扫的是 **HEAD 往回的最近 N 条**，**会扫进 main 的历史**。实测（队员 task-1）：分支 `chore/stage0-log-archive-20260926` 无 D#，回退扫到 main 上的 merge subject `Merge pull request #804 … fix/d956-ci-failmsg` ⇒ **被绑到已合任务 D956** ⇒ 用 D956 的 5 条声明去判本 PR 的 4 个新文件 ⇒ 判出 3 个"夹带"、`exit 1`。
   - **后果（比单次误判更重）**：短分支几乎**必然**被绑到某个已合 D# ⇒ `declared` 非空 ⇒ `:444-454` 的"无声明 + 全文档范围 ⇒ skip"宽松路径**实际不可达**。
   - 处置：阶段 0 用**真取号**（走 S1 `task-state/<D#>.json` 的 `write_set`）绕过；**不用豁免层**（豁免层正是 D964 FIX 未收口的 P0「豁免夹带」面，且 C5 要求豁免数单调下降）。结构性修复归阶段 3「归属/撞号」。
11. **【新发现·机器级共享状态】`/tmp/.synova-before-brief`**：非 per-worktree，现存 **2026-09-24 另一 session（D941）** 的历史记录 ⇒ **任何** session 在 `SYNO_CI=1` 下做"本地等价复现"都会假红一组（时间戳顺序检查）。CI runner 无此文件 ⇒ 不影响 CI。**处置：不删**（删 = 抹掉他人违规证据），登记转 Mac-CTO；该限制写入 task-1 文档"失效条件"。



---

## 四、需 Mac-CTO 裁决

1. **判据措辞修正（0-①-1）**：建议把判据改为"**hook 的写入目标不含 `bypass.log`**"（可机器核：`grep -nE '>>[^>]*bypass\.log' .git/hooks/pre-commit` 零命中），而不是"字样消失"——后者与 D508 的注释设计**天然冲突**：生成器 body 里就必须含那 2 行说明注释，而阶段 0 明令 `install-hooks.sh`「执行，不修改」⇒ **"字样消失"与"hook 与生成器同哈希"两条判据不可能同时满足**（独立自验员 U3 与我方结论一致）。
2. **派单声称更正（claim 级）**：请裁决以"**证据账本污染**"替换"旧 hook ⇒ 当日自阻断"。独立自验员的决定性反例：注入 8 条今日 `GATE_FAIL_SOFT` 后真跑门禁 → EXIT=0、全 13 组通过。**请同步给 K3 作为抽验输入（防止 K3 按错误声称核验）**。
3. **门禁/CI 语义变更批准（R3）｜含一条高危发现 G6**：0-② 的 `.github/workflows/ci.yml` 改动（+1 step：`upload-artifact`，`retention-days: 90`，`include-hidden-files: true`，`if-no-files-found: warn`，**`continue-on-error: true`**）。
   - **并行 CTO 声明**：不新增/不删除/不改任何**阻断式**检查 ⇒ 不构成门禁语义变更。
   - **G6（独立自验员发现，作者与队长均未预见）**：API 实测 `branches/main/protection` 的 `required_status_checks` **含 `TypeScript + Lint + Iron Laws`**（app_id=15368）= 就是 `quality` job ⇒ 若该 step **fail-loud**，artifact 服务瞬时故障会让 **main 必需检查变红 ⇒ 阻塞全队所有 PR 合并**。
   - **并行 CTO 已裁（技术）**：给**那一个 step** 加 `continue-on-error: true`（job 不加）⇒ **显式黄灯 + 计数**，不 fail-closed。理由：`quality` 是必需检查，制造阻塞正是本工程要消灭的形态（对齐蓝图 §4.2-bis「不许降级即通过，也不得制造阻塞」）。
   - **代价（必须并列，不淡化）**：本改动**不新增红路径，新增的是 1 条"静默失败面"**（上传失败仅黄灯）⇒ 因此**必须配一条计数机制**才发现静默停摆：*"每 N 次 main 运行应有 `gate-logs-*` artifact，缺失即计数 +1，进阶段 1 的 gov profile"*（**只写判据，不新增脚本**，蓝图 §七-1）。
   - **请 Mac-CTO 二选一确认**：(甲) `continue-on-error: true`（当前实现，不阻塞合并但需计数兜底）／(乙) 不加（fail-loud，红可被看见，但一次瞬时故障即阻塞全队合并）。

4. **0-② 第三日志机制**：`pre-commit-failures.log` 的**持续** CI 覆盖结构性不可达（CI 不 commit）。当前以**一次性日期化快照**补"异机可复跑"，持续面登记为未清项。是否批准后续另起机制（本机快照 → 专用 ref），本阶段**只登记不动手**。
5. **判据口径升级（阶段 1 前置）**：`loop-context.sh:88` 的 FP 熔断（未清项 8）与 `evidence/<D>/changes.json` 结构地雷（未清项 9），是否并入党派给阶段 5「红线升级」与阶段 3 的输入清单。


## 五、需创始人裁决

1. **`DASHBOARD-CN.md` 月级停更 + `product-progress.html` 5 天未刷新**：是否要求仪表盘纳入阶段判据（当前不在 C1–C16 内）。
2. **主树 `UU` 未解冲突归属**：`src/deploy/bootstrap.ts` 的 add/add 冲突属文档线在途工作区，是否需要创始人指派归属方清理（并行 CTO 不越界动他人工作区）。
3. （无则留空）

---

## 附：K3 抽验送审包（阶段 0）

| 抽验项 | 输入（可复跑） |
|---|---|
| 0-① hook 判据 | `.git/hooks/pre-commit`（sha256 前 16 = `609788b1ac3bfb31`）+ 生成器 `scripts/install-hooks.sh:43-58` + 备份件 `/private/tmp/cto-stage0-hook-backup/pre-commit.old` |
| 0-① 反例 | 复现命令见 §一 0-①（scratch worktree + `as any` 夹具） |
| 0-① 声称核验 | `scripts/pre-commit-check.sh:242` / `:1085` / `.claude/settings.json` 四 hook 零 `bypass` 引用 |
| 0-② 归档 | `.github/workflows/ci.yml` 改动 + CI run artifact 下载（队员回执附） |
| 0-③ 分类 | `docs/synova/coordination/阶段0-积压分类-20260926.md` + `阶段0-C16基线-20260926.json`（队员回执附） |
| 独立自验 | `docs/synova/coordination/阶段0-自验报告-20260926.md`（**PR #824** ／分支 `chore/stage0-selfverify-20260926` @ `cfd74b7d20b7`） |

**修订记录**
- 2026-09-26 v0.1 首稿（并行 CTO）：0-① 完成并附判据；0-②/0-③/自验 等待队员回执后补齐。
- 2026-09-26 v1.0（本版）：0-②/0-③/0-④ 回执到齐；补 C16 口径对账表、G6 高危发现、`infer_did` 误绑、`/tmp/.synova-before-brief` 等 11 条未清项。

---

## 六、阶段 0 达标判定（按"不达标不进下一阶段"）

**结论：阶段 0 判「有条件完成 = 未完成」。并行 CTO 不自行进入阶段 1。**

| 项 | 状态 | 卡在哪 |
|---|---|---|
| 0-① 重装 hook | ✅ 达标（判据措辞待更正） | 仅文字层面；判据自相矛盾已上报 |
| 0-② 日志归档 | ⚠️ **部分达标** | ① **artifact 非空 = 时间性未决**（CI 队列 queued，run `36167351846` / `36168645768`）；② `pre-commit-failures.log` 的**持续** CI 覆盖结构性不可达；③ PR #828 **需 Mac-CTO 批准**（CI 语义面）后才可合并 |
| 0-③ 积压分类 | ✅ 达标（口径已裁决） | C16 含 detached 的**判据升级**需 Mac-CTO 裁决 |
| 0-④ 独立自验 | ✅ 达标 | — |
| 全局冻结 | ✅ | — |

**解除阻塞的三个条件（任一由创始人/Mac-CTO 给出即可推进）**：
1. **CI 跑完** ⇒ 按 task-1 文档 §5.5 一键补验 artifact（下载 + `unzip -l` + `wc -l` > 0）⇒ 0-② 转 ✅。
2. **Mac-CTO 批准 PR #828**（含 G6 二选一：`continue-on-error: true` / fail-loud）⇒ 0-② 的 CI 面落地。
3. **Mac-CTO 裁决**：判据措辞更正（0-①-1）、"自锁"声称更正、"1,415 口径"归属、`loop-context.sh:88` 与 `evidence/<D>/changes.json` 是否并入党派清单。

**并行 CTO 建议**：条件 1/2 是**时间与批准**问题（不涉及新工作），条件 3 是**文本裁决**（可并行）。**在批准落地前，不启动阶段 1（证据闭合）**，以免出现"阶段 1 用着未定的 C16 口径"这类返工。

