# 阶段0-② 门禁日志归档 — 交付文档

> 卡：task-1（治理体系最小充分形态 · 阶段0-②）｜**卡号：D1009**（`alloc-task-id.sh` 分配）
> 分支：`chore/D1009-stage0-log-archive`（原 `chore/stage0-log-archive-20260926`，2026-09-26 经 GitHub rename API 改名，sha 不变 = `93e14ee0af90`）
> 父节点：`origin/main` = `ee721b0abf52`｜worktree：`/private/tmp/synova-wt-squad-ci`｜写集：见 §2.1（6 文件）
> PR：**#832**（#828 因改名被 GitHub 关闭，登记为"被 #832 取代"）｜判据：三条门禁日志「可在异机复跑（≥90 天保留）」

**一句话**：CI artifact 持续归档（≥90 天）+ 三日志日期化不可变快照入库，两条路互补。
**不含**：任何新增/修改/删除的检查（非门禁语义变更，见 §8）。

---

## 0. 判据逐条（三态，一个都不升格）

| # | 对象 | 判定 | 依据（原始输出见 §） |
|---|---|---|---|
| 1 | `.claude/gate-hits.log` | ✅ 达成 | CI `quality` job 跑 `scripts/pre-commit-check.sh`，`hard_check`/`soft_check` **两分支均调** `log_gate`（`scripts/pre-commit-check.sh:125`/`:128`）⇒ runner 当场确定性生成；本地忠实复现 21 行（§5.2/5.3）；artifact 下载待 CI（§5.5） |
| 2 | `.codex/control-tower/logs/degraded-events.log` | ⚠️ 条件达成 | 由 `merge_writeset_gate.py:457`（skip/degraded 路径）→ `:497` 写入，属 `quality` job 的 D708 step；**条件性**：有合法声明且 pass 的 PR 不写。本轮本地实验**未生成**（§5.4），故不写成无条件 ✅ |
| 3 | `.claude/pre-commit-failures.log` | ⚠️ 结构性不可达（CI 侧）→ 快照补证 | 唯一写入者是 `.git/hooks/pre-commit`（`scripts/install-hooks.sh:49`），CI 不 commit、无 hook ⇒ CI 永不产生。**改由入库快照补"异机可读"**（§6） |
| 4 | artifact **真可下载且非空**（≥90 天保留） | ✅（**带两点限定**） | `run_id=36167351846`：`gate-logs-36167351846`，1170 B，`created_at=2026-09-25T17:45:39Z` → `expires_at=2026-12-24T17:28:34Z` = **整 90 天**；`unzip -l` 内含 `.claude/gate-hits.log`（21 行）。**限定 1**：`degraded-events.log` **不在其中**（CI 未走 degraded 路径 ⇒ 与 §0-2 的 ⚠️ 一致）；**限定 2**：**匿名下载 HTTP 401** ⇒ K3 无 token 不可取（§9-7） |
| 4b | CI 侧 `quality`（main 必需检查）当前状态 | ❌ **红**（**非本改动导致**） | 失败步 = D708 写集对账；根因 = D-号推断从 merge subject 的 SHA 抠出假号 **D839** ⇒ 误绑 ⇒ 6 文件判夹带（§8.4）。**归档步 `success`**；三条出路待裁决（§9-12） |
| 5 | `ci.yml` 只加 1 step、**0 删除行** | ✅ | `git diff -U0 origin/main -- .github/workflows/ci.yml \| grep -c '^-[^-]'` = **0**；`--numstat` = `21 0`（§2.2） |
| 6 | `retention-days: 90` + `include-hidden-files: true` + `if: always()` + `if-no-files-found: warn` + `continue-on-error: true` | ✅（**语义层已证，行为层待 CI**） | 夹具 9 断言基线全 ✅；变异体 M1–M7 逐个 rc=1（§4） |
| 7 | 快照 3 文件可 `git ls-files` 列出 + sha256 与主树取证现场一致 | ✅ | §6.2/6.3（3/3 行；前后哈希逐字符相同） |
| 8 | 凭证自查（自扫留证，因 `check-secrets.sh` 不扫 `.log`） | ✅ | §7（8 模式；`sk-` 32 命中唯一值 `sk-decomposition`） |
| 9 | 非门禁语义变更已声明 + 请 Mac-CTO 批准（含 1 条静默失败面的显式取舍） | ✅（文本层） | §8.1/§8.3；PR #828 描述 ⑤ 段 |

**生效面拆分**（避免"一条 ✅ 掩盖另一面"）：

| 面 | 覆盖 | 判定 |
|---|---|---|
| **追溯面**：现存历史内容（4873/5758/156 行） | 日期化快照入库 ⇒ **任何 clone 可读** | ✅ `git ls-files` 3/3（§6.3） |
| **持续面**：CI 侧新产生的日志 | CI artifact，retention 90 天 | ✅ gate-hits 确定性；⚠️ degraded 条件性 |
| **CI 侧 pre-commit 失败日志** | 无（结构不可达） | ⚠️ 未清项（§9-1） |

---

## 1. 环境准备（可复现）

### 1.1 worktree 三道守卫

```bash
cd /Users/wane/SynovaAgent && git fetch --all --quiet
git worktree add -b chore/stage0-log-archive-20260926 /private/tmp/synova-wt-squad-ci origin/main || exit 1
[ -d /private/tmp/synova-wt-squad-ci ] || exit 1
cd /private/tmp/synova-wt-squad-ci || exit 1
git rev-parse --short=12 HEAD
```
```
Preparing worktree (new branch 'chore/stage0-log-archive-20260926')
HEAD is now at ee721b0a Merge pull request #804 from tangbaobao520/fix/d956-ci-failmsg
ee721b0abf52
```
> ⚠️ 主工作树 `/Users/wane/SynovaAgent` 当时在**另一分支** `chore/cto-doc-slim-phase1-writeset-exempt`（HEAD `b8573c7a`），其 `ci.yml` 为 452 行；**origin/main 的 `ci.yml` 是 469 行**。本任务所有行号/断言均以 worktree（= origin/main，blob `11fde1d2`）为准。**主树零 git 写操作。**

### 1.2 `node_modules` 符号链接（只读使用）

`/private/tmp` 下新建 worktree 无 `node_modules` ⇒ `npx tsx` 类门禁会因 npm cache `EPERM` 假红（小队情报，squad-verifier 实测）。
```bash
ln -sfn /Users/wane/SynovaAgent/node_modules node_modules
git check-ignore -v node_modules
```
```
/Users/wane/SynovaAgent/.git/info/exclude:20:node_modules	node_modules
```
**边界**：只读使用；未借它跑全量 `vitest`/`tsc`/探针（重型验证令牌制 ≤1）；untracked、被 `info/exclude` 忽略、不进写集。

### 1.3 本地认领件（**未入库**，故意为之）

本地 `commit-msg` 的认领制（`resolve-commit-brief.sh`）把暂存文件归属到"认领数最多的 brief"。本 PR 分支无 D#，而 2026-09-25 窗口内 `D945`/`D956` 等 brief 都声明了 `.github/workflows/ci.yml` ⇒ 首次提交被 `D328` 判"声明(无)≠归属(D945)"**硬阻断**。
处置：在 worktree 内建**本任务自己的当日 brief**（`#CRITERIA: A` + 6 字段 + Q2 逐条列本写集 5 路径 + `- [ ]` Done 3 条），认领数 5 > 他人 1 ⇒ 归属回到本任务；brief 名**不含 D#** ⇒ `CLAIM_DID` 为空 ⇒ `D328` 空对空放行（`commit-msg-check.sh:116-117` 仅当认领 brief 名含 D# 才比对）。
```bash
bash scripts/workflow/check-brief-parseable.sh .claude/task-briefs/2026-09-26-stage0-gate-log-archive.md
bash scripts/workflow/resolve-commit-brief.sh "$(git diff --cached --name-only)"
```
```
[check-brief-parseable] ✅ brief 可解析: .../2026-09-26-stage0-gate-log-archive.md (Q2 ✓ #CRITERIA=A 架构层=基础设施 Done=3)
/private/tmp/synova-wt-squad-ci/.claude/task-briefs/2026-09-26-stage0-gate-log-archive.md
```
**该 brief 未入库**（不扩张队长声明的写集）；它是 worktree 内的工作树产物，只服务本地 hook。

---

## 2. 改动

### 2.1 写集（实交，6 文件）

```
M  .github/workflows/ci.yml                                             （+17 行 / 0 删除）
A  docs/synova/coordination/阶段0-日志归档-20260926.md                    （本文档）
A  docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt        （快照，4873 行）
A  docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt  （快照，5758 行）
A  docs/synova/coordination/gate-logs-20260926/pre-commit-failures.log.txt（快照，156 行）
A  task-state/D1009.json                                               （S1 写集声明，见 §11）
```
第 7 项 `.claude/bypass.log`（+1 行）由 post-commit hook **自动**生成（每个提交都追加的**证据账本**），`merge_writeset_gate.py` 内置豁免并逐条打印理由；非本任务声明内容，如实列出。

### 2.2 ci.yml 改前/改后（`git diff origin/main...HEAD`）

插入位置：`quality` job 的 D708 step 之后、`test:` job 之前（既有 step **一字未动**，未加 job）。

```diff
@@ -101,6 +101,27 @@ jobs:
         run: |
           python3 scripts/control-tower/merge_writeset_gate.py \
             --base origin/main --head HEAD --branch "${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
+      # 阶段0-② 门禁原始日志归档（K3 异机可复跑 / ≥90 天）。非门禁语义变更：只新增
+      #   归档步，不改任何既有 step 的判定。continue-on-error: true 为**有意取舍**——
+      #   本 job 名是 main 必需检查（branches/main/protection 实测），fail-loud 会让
+      #   上传服务瞬时故障阻塞**全队所有 PR 合并**；故取「显式黄灯」而非 fail-closed
+      #   （对齐蓝图 §4.2-bis：不许降级即通过，但也不得制造阻塞）。
+      #   include-hidden-files: true 必需——upload-artifact@v4.4+ 默认排除「. 开头目录
+      #   内的文件」，两条 path 全在其中，缺它 = 上传 0 文件（假绿）。
+      #   gate-hits.log 由 pre-commit-check.sh:125/128 的 hard/soft_check 确定性产生；
+      #   degraded-events.log 由 merge_writeset_gate.py:457/497 在 skip/degraded 路径条件产生。
+      - name: Archive gate raw logs (stage-0②, ≥90d)
+        if: always()
+        continue-on-error: true
+        uses: actions/upload-artifact@v4
+        with:
+          name: gate-logs-${{ github.run_id }}
+          path: |
+            .claude/gate-hits.log
+            .codex/control-tower/logs/degraded-events.log
+          if-no-files-found: warn
+          retention-days: 90
+          include-hidden-files: true
 
   test:
     name: Vitest (${{ matrix.shard }})
```
```console
$ git diff --numstat origin/main -- .github/workflows/ci.yml
21	0	.github/workflows/ci.yml
$ git diff -U0 origin/main -- .github/workflows/ci.yml | grep -c '^-[^-]'
0
```
> **0 删除行**（两条命令都给 0）⇒ 结构上不可能"改断言/改既有判定"。21 行 = 10 行注释 + 11 行有效 YAML（`- name:` 起到 `include-hidden-files: true`）。hunk 头 `@@ -101,6 +101,27 @@`。

### 2.5 落位决策（两处偏离任务书原文，理由 + 独立复核）

| 偏离 | 原方案 | 实交 | 理由（可核） |
|---|---|---|---|
| 快照目录 | 仓根 `evidence/gate-logs-20260926/` | `docs/synova/coordination/gate-logs-20260926/` | `.gitignore:76` 的 `evidence/` 段使**任何名为 `evidence/` 的目录整目录被忽略**（`git check-ignore -v` 原始输出见 §2.4），且不匹配 `merge_writeset_gate.py:77-79` 的 `DOC_SCOPE_RE`（会触发 `:444-453` fail-closed）。**不是为了让任何检查变绿——本 PR 未新增/修改/删除任何检查**（0 删除行可证）。该偏离已获队长确认接受，并由独立自验员复核 `.gitignore` 事实成立。 |
| 快照后缀 | `*.log` | `*.log.txt` | `.gitignore:8` 的 `*.log` 会忽略 `.log`；仓内既有 `.log.txt` 先例（`docs/synova/product-lines/evidence/D592-*.log.txt`）。**未用 `git add -f`**（不绕"运行态不入库"设计）。内容不变：sha256 与主树取证现场逐字符一致（§6.2）。 |

### 2.3 `include-hidden-files: true` 的必要性（一手来源）

- 官方 README「Uploading Hidden Files」：**"By default, hidden files are ignored by this action…"**、
  **"Hidden files are defined as any file beginning with `.` or files within folders beginning with `.`."**
- `actions/upload-artifact@v4` 的 `action.yml`：`include-hidden-files` → `default: 'false'`
  （"If false, hidden files will be excluded from the artifact."）
- 本 PR 两条 path 全在 `.claude/`、`.codex/` 内 ⇒ 缺此项 = **artifact 为空**，而 `if-no-files-found: warn` 只出警告、job 照绿 ⇒ **判据假绿**。夹具 M1 专门抓这个（§4）。

### 2.4 快照命名的两处**被迫**调整（`.gitignore` 证据）

```console
$ git check-ignore -v docs/synova/coordination/evidence/gate-logs-20260926/gate-hits.log
.gitignore:76:evidence/	docs/synova/coordination/evidence/gate-logs-20260926/gate-hits.log
$ git check-ignore -v docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt
（无命中 = NOT IGNORED）
```
- `.gitignore:76` 是 `evidence/` ⇒ **任何名为 `evidence/` 的目录整目录被忽略**（不只是文件）。故目录去掉 `evidence/` 段。
- `.gitignore:8` 是 `*.log` ⇒ 快照用仓内**既有先例** `*.log.txt`（`docs/synova/product-lines/evidence/D592-e2e-create.log.txt`、`docs/synova/audit-reports/D580-*/logs/*.log.txt` 等）。
- **不用 `git add -f`**：那会与"`gate-hits.log` 属运行态不入库"的设计正面对撞，且属把忽略规则强行绕过；改用 `.log.txt` 既保有原始内容（sha256 逐字符一致，§6.2）又不改任何 `.gitignore`（`.gitignore` 本身不在 `DOC_SCOPE_RE` 内，改它会让 D708 fail-closed，见 §9-3）。

---

## 3. YAML 语法/结构校验

```bash
node -e 'const y=require("/Users/wane/SynovaAgent/node_modules/js-yaml"),fs=require("fs");
const s=y.load(fs.readFileSync(".github/workflows/ci.yml","utf8")).jobs.quality.steps
  .find(x=>String(x.uses||"").includes("upload-artifact"));console.log(JSON.stringify(s,null,2))'
```
```json
{
  "name": "Archive gate raw logs (stage-0②, ≥90d)",
  "if": "always()",
  "uses": "actions/upload-artifact@v4",
  "with": {
    "name": "gate-logs-${{ github.run_id }}",
    "path": ".claude/gate-hits.log\n.codex/control-tower/logs/degraded-events.log\n",
    "if-no-files-found": "warn",
    "retention-days": 90,
    "include-hidden-files": true
  }
}
```
js-yaml `4.3.0`（仓内 `node_modules`）解析成功；`on:` 保持字符串键（未出现 YAML 1.1 布尔化坑）。本机无 `actionlint`（如实登记，未装）。

---

## 4. 夹具：三路径 + 变异体（改坏即红）

夹具 = `node` 断言器（`js-yaml` 解析 → 逐条断言），**不入库**（写集只有 6 文件）。断言：

| ID | 断言 | 含义 |
|---|---|---|
| A0 | YAML 可解析 | 语法/结构底线 |
| Z-uniq | `quality` job 内 upload-artifact 步**恰好 1 个** | 防重复/误加 |
| A1 | `include-hidden-files === true` | **缺它 = 上传 0 文件假绿** |
| A2 | `path` 集合 == 两条预期路径（逐字符） | 路径抄错即抓 |
| A3 | `if-no-files-found ∈ {warn, ignore}`（≠`error`） | 保证"缺文件不红" |
| A4 | `retention-days === 90` | ≥90 天 |
| A5 | `if === "always()"` | 前序 step 失败仍归档 |
| A6 | `name` 以 `gate-logs-` 开头 | artifact 可检索 |
| A7 | `continue-on-error === true` | **本 job 名是 main 必需检查** ⇒ 上传故障不得红（§8.1） |

**基线**（真实 `ci.yml`，**9 条断言全 ✅**）：
```console
✅ A0 YAML 可解析（js-yaml 4.3.0）
✅ Z-uniq quality job 内 upload-artifact 步数量 = 1（期望 1）
✅ A1 include-hidden-files = true（期望 true；false/缺失 ⇒ artifact 为空）
✅ A2 path 集合 = [".claude/gate-hits.log",".codex/control-tower/logs/degraded-events.log"]
✅ A3 if-no-files-found = "warn"（期望 warn/ignore；error ⇒ 缺文件即红）
✅ A4 retention-days = 90（期望 90）
✅ A5 if = "always()"（期望 always()；前序 step 失败仍归档）
✅ A6 artifact name = "gate-logs-${{ github.run_id }}"（期望 gate-logs-*）
✅ A7 continue-on-error = true（期望 true；本 job 是 main 必需检查 ⇒ 上传故障不得红）
baseline rc=0
```

**变异体（7/7 全部被抓 ⇒ "改坏即红"）**：

| 变异体 | 注入 | rc | 抓它的断言（原始输出摘） |
|---|---|---|---|
| M1 | 删 `include-hidden-files: true` | **1** | `❌ A1 include-hidden-files = undefined（期望 true；false/缺失 ⇒ artifact 为空）` |
| M2 | `path` 抄错成 `claude/gate-hits.log` | **1** | `❌ A2 path 集合 = ["claude/gate-hits.log",…]` |
| M3 | `if-no-files-found: warn`→`error` | **1** | `❌ A3 if-no-files-found = "error"` |
| M4 | `retention-days: 90`→`14` | **1** | `❌ A4 retention-days = 14` |
| M5 | 删 `if: always()` | **1** | `❌ A5 if = undefined` |
| M6 | 整步删除 | **1** | `❌ Z-uniq … 步数量 = 0` |
| **M7** | 删 `continue-on-error: true` | **1** | `❌ A7 continue-on-error = undefined（期望 true；本 job 是 main 必需检查 ⇒ 上传故障不得红）` |

> **M1 是本任务最关键的一条**：删掉 `include-hidden-files` 后，YAML 仍合法、`path` 仍正确、`warn` 仍不红——**表面全绿，artifact 却是空的**。夹具把它变红。

---

## 5. 「CI 上这三条路径真实存在」的证据链

> 本机无法跑 GitHub 托管 runner（`grep -rn 'self-hosted' .github/workflows/` **零命中**），故本节 = **机制溯源 + 本地忠实复现**（可核验的替代证据），CI 侧真证据见 §5.5（未清项，第二提交回填）。

### 5.1 机制溯源（谁在 CI 写这些文件）

| 文件 | 写入者 | CI 可达性 |
|---|---|---|
| `.claude/gate-hits.log` | `scripts/pre-commit-check.sh:222/225/228`（`GATE_HITS_LOG`）+ `log_gate()`（`:76-82`；`hard_check`/`soft_check` 两分支调用 `:125`/`:128`） | ✅ `quality` job 跑该脚本（`ci.yml:84`，`SYNO_CI=1`）⇒ **确定性生成** |
| `.codex/control-tower/logs/degraded-events.log` | `merge_writeset_gate.py:497`（`_log_degraded`），触发点 `:442-457`（无声明/降级/无法判定）；另 `pre-commit-check.sh:249/876/1035` 三处**均条件性**且在 CI 不可达（`:249` 外层为 `GITHUB_ACTIONS != "true"`；`:876` 需无 python3；`:1035` 需 grep rc≥2） | ⚠️ **条件性**：D708 step（`ci.yml:99-103`）走 skip/degraded 路径才写 |
| `.claude/pre-commit-failures.log` | `scripts/install-hooks.sh:49`（`.git/hooks/pre-commit` 包装器） | ❌ CI 不 commit、无 hook ⇒ **永不生成** |

### 5.2 本地忠实复现（CI step 序列，隔离 worktree 内）

```bash
# ① 清空**本 worktree** 的 gitignored 运行态日志（主树取证现场未动，见 §6.2）
rm -f .claude/gate-hits.log .codex/control-tower/logs/degraded-events.log
# ② 复现 CI quality job 的 Iron laws step（同 env）
SYNO_DIFF_BASE=origin/main SYNO_CI=1 bash scripts/pre-commit-check.sh > /tmp/ci-gate-repro.log 2>&1; echo "rc=$?"
# ③ 复现 CI quality job 的 D708 step
python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
  --branch chore/stage0-log-archive-20260926
```
**①→② 结果**：`gate-hits.log` **当场生成**：
```console
$ wc -l .claude/gate-hits.log
      21 .claude/gate-hits.log
```
**② 的 rc=1 必须如实说明（不是本 PR 的问题）**：唯一失败组 = 「时间戳顺序: brief 必须早于代码写入」，来源是**机器级共享文件** `/tmp/.synova-before-brief`：
```console
$ ls -l /tmp/.synova-before-brief
-rw-r--r--@ 1 wane  wheel  234 Sep 24 16:31 /tmp/.synova-before-brief
$ cat /tmp/.synova-before-brief
2026-09-24T16:31:02+08:00 | FILE=/Users/wane/.claude/projects/-Users-wane-SynovaAgent/memory/dsh-electron-source-desktop.md | BRIEF=.claude/task-briefs/2026-09-24-D941-ownership-generative-rewrite.md | 代码在 brief 填写前写入
```
该文件是**另一 session（2026-09-24，D941）**的历史记录，非 per-worktree ⇒ 任何 session 的 `SYNO_CI=1` 本地复现都会假红一组；CI runner 无此文件 ⇒ **CI 免疫**。未删（remediate 命令会抹掉他人违规证据，越权），已登记 §9-5。

### 5.3 `gate-hits.log` 原始样例（本地复现产物，21 行）

```json
{"time": "2026-09-25T17:21:53Z", "gate": "as any / as never / as unknown as 零容忍（新增，铁律 38；存量独立清理）", "result": "miss", "branch": "chore/stage0-log-archive-20260926"}
…
{"time": "2026-09-25T17:21:55Z", "gate": "DSH 断面一致性 (D943)", "result": "miss", "branch": "chore/stage0-log-archive-20260926"}
```
⇒ 该文件确为"每次门禁检查触发的 JSONL 命中统计"，且 `branch` 字段可核对来源分支。

### 5.4 `degraded-events.log`：本轮**未生成**（如实）

```console
$ ls -l .codex/control-tower/logs/degraded-events.log
ls: .codex/control-tower/logs/degraded-events.log: No such file or directory
```
本轮 `merge_writeset_gate.py` 走的是 **`block`（夹带）**路径（§8.2），该路径**不调用** `_log_degraded`（只有 skip/degraded 路径调用）⇒ 没写。这恰好反证了 §5.1 的"条件性"结论，也是我**不把该文件判为无条件 ✅** 的原因。

### 5.5 CI 侧真证据：✅ **已回填**（artifact 真下载、非空、90 天整）

`run_id` = **36167351846**｜`head_sha` = `a16d839eb0a6`｜`created_at` = 2026-09-25T17:28:34Z
`as_of` = **2026-09-26T01:5x+08:00**｜**终态 = `completed`**（该 run 的 `TypeScript + Lint + Iron Laws` job 结论 = `failure`，但**原因与归档步无关**：失败在 D708 步；归档步 `Archive gate raw logs` 结论 = **`success`**，见 §8.4）

```console
$ curl -sH "Authorization: token $TOKEN" ".../actions/runs/36167351846/artifacts" | ...
total_count: 1
  - gate-logs-36167351846 | 1170 B | created: 2026-09-25T17:45:39Z | expires: 2026-12-24T17:28:34Z
$ curl -sL -H "Authorization: token $TOKEN" <archive_download_url> -o /tmp/art.zip
http=200 size=1170
$ unzip -l /tmp/art.zip
     2833  09-25-2026 17:45   .claude/gate-hits.log
$ find /tmp/gatelogs -type f | while read -r f; do echo "$(wc -l < "$f") $f"; done
21 行  /tmp/gatelogs/.claude/gate-hits.log
$ curl -s -o /dev/null -w "anon_http=%{http_code}\n" -L <archive_download_url>
anon_http=401
```
结论（逐条）：
- **artifact 名** = `gate-logs-36167351846`（符合 `gate-logs-<run_id>`）；**非空** = 1170 B，内含 **`.claude/gate-hits.log` 21 行** ✅
- **≥90 天**：`expires_at − created_at` = `2026-12-24T17:28:34Z − 2026-09-25T17:28:34Z` = **整 90 天**（`retention-days: 90` 被平台采纳）✅
- **`.codex/control-tower/logs/degraded-events.log` 不在 artifact 内** ✅ 与 §0-2「⚠️ 条件达成」一致（CI 侧 D708 未走 skip/degraded 路径 ⇒ 未产生该日志）——**这正是把该行判为 ⚠️ 而非 ✅ 的实测依据**
- **匿名可达性 = HTTP 401** ❌：公开仓的 artifact **仍需认证**才能下载 ⇒ **K3 无 token 时不可直接取用**（见未清项 §9-7；快照入库的 `git ls-files` 面不受影响）

一键补验命令（保留，供后续 run 复核；`<RUN_ID>` 按实际替换）：

```bash
TOKEN=$(grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml | sed 's/.*GITHUB_TOKEN:[[:space:]]*//' | tr -d '\r\n')
PYBIN="$(command -v python3)"
# 1) run 终态 + jobs（看 quality job 结论）
curl -sH "Authorization: token $TOKEN" "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/36167351846" \
  | "$PYBIN" -c 'import json,sys;d=json.load(sys.stdin);print(d["status"],d["conclusion"],str(d["head_sha"])[:12])'
# 2) artifacts 列表（name / size / created_at / expires_at）
curl -sH "Authorization: token $TOKEN" "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/36167351846/artifacts" -o /tmp/art.json
"$PYBIN" -c 'import json;d=json.load(open("/tmp/art.json"));print("total:",d["total_count"]);
[print(" -",a["name"],"|",a["size_in_bytes"],"B |",a["created_at"],"→",a["expires_at"]) for a in d["artifacts"]]'
# 3) 下载 + 解压 + 逐文件行数（证明两条路径真实存在且非空）
URL=$("$PYBIN" -c 'import json;d=json.load(open("/tmp/art.json"));print(d["artifacts"][0]["archive_download_url"])')
curl -sL -H "Authorization: token $TOKEN" "$URL" -o /tmp/art.zip && unzip -l /tmp/art.zip
rm -rf /tmp/gatelogs && unzip -o -q /tmp/art.zip -d /tmp/gatelogs
find /tmp/gatelogs -type f | sort | while read -r f; do echo "$(wc -l < "$f" | tr -d ' ') $f"; done
# 4) K3 无 token 可达性（公开仓匿名下载探测）
curl -s -o /dev/null -w "anon_http=%{http_code}\n" -L "$URL"
```

回填内容必须含：artifact 名、`size_in_bytes`、`created_at`、`expires_at`（**≥90 天验收 = `expires_at − created_at ≈ 90d`**）、`unzip -l` 内的两条路径、解压后**行数 > 0**，以及匿名可达性结果。

---

## 6. 快照入库（判据的充分条件：`git ls-files` 可列）

### 6.1 快照内容与来源

| 快照 | 行数 | 字节 | 源 |
|---|---|---|---|
| `gate-hits.log.txt` | 4873 | 766989 | `/Users/wane/SynovaAgent/.claude/gate-hits.log` |
| `degraded-events.log.txt` | 5758 | 2431750 | `/Users/wane/SynovaAgent/.codex/control-tower/logs/degraded-events.log` |
| `pre-commit-failures.log.txt` | 156 | 9801 | `/Users/wane/SynovaAgent/.claude/pre-commit-failures.log` |

一次性不可变归档（日期化目录 `gate-logs-20260926/`，**不再更新**）；**不是**"日志回归入库"——持续归档仍走 CI artifact。

### 6.2 完整性（主树取证现场未动 + sha256 逐字符一致）

```console
# 快照前（主树）
2a6a80ba467220da9d924df62352eb5eaccaf557874d6315e21322b8e35dc6f7  /Users/wane/SynovaAgent/.claude/gate-hits.log
30929d4ed4e99c2c47b1f46d1567ef8e5f749e23d62b35b66a43b4e06df4b781  /Users/wane/SynovaAgent/.codex/control-tower/logs/degraded-events.log
1148103f70ff3da63dbde71f0a3dd551e7aeb237a43bd4179dc1e80968187652  /Users/wane/SynovaAgent/.claude/pre-commit-failures.log
# 快照后（worktree 副本，cp -p 保留 mtime）
2a6a80ba467220da9d924df62352eb5eaccaf557874d6315e21322b8e35dc6f7  …/gate-logs-20260926/gate-hits.log.txt
30929d4ed4e99c2c47b1f46d1567ef8e5f749e23d62b35b66a43b4e06df4b781  …/degraded-events.log.txt
1148103f70ff3da63dbde71f0a3dd551e7aeb237a43bd4179dc1e80968187652  …/pre-commit-failures.log.txt
# 快照后再验主树（应与会话开始时同名同哈希 ⇒ 取证现场未被改动）
2a6a80ba467220da9d924df62352eb5eaccaf557874d6315e21322b8e35dc6f7  /Users/wane/SynovaAgent/.claude/gate-hits.log
30929d4ed4e99c2c47b1f46d1567ef8e5f749e23d62b35b66a43b4e06df4b781  /Users/wane/SynovaAgent/.codex/control-tower/logs/degraded-events.log
1148103f70ff3da63dbde71f0a3dd551e7aeb237a43bd4179dc1e80968187652  /Users/wane/SynovaAgent/.claude/pre-commit-failures.log
```

### 6.3 判据命令（任何 clone 可复跑）

```console
$ git ls-files docs/synova/coordination/gate-logs-20260926/
docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt
docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt
docs/synova/coordination/gate-logs-20260926/pre-commit-failures.log.txt
```
⇒ 3/3，K3 的另一 clone（看不到主树 gitignored 文件）**可直接读到原始内容**。

---

## 7. 凭证自查（门禁**不覆盖** `.log` ⇒ 人工补证）

`scripts/check-secrets.sh` 的 `--include` 列表与暂存过滤 `\.(ts|js|json|yaml|yml|md|html)$` **均不含 `.log`**（`:31-34`、`:136`）⇒ 快照不会被门禁扫 ⇒ 必须自扫留证：

```bash
for pat in 'ghp_' 'github_pat_' 'api[_-]?key' 'sk-[A-Za-z0-9]{10,}' 'cli_[a-z0-9]{10,}' \
           'AKIA[0-9A-Z]{16}' 'xox[baprs]-' 'BEGIN [A-Z ]*PRIVATE KEY'; do
  printf "%-32s : %s\n" "$pat" "$(grep -oE "$pat" "$f" | wc -l | tr -d ' ')"
done
```
| 模式 | gate-hits | degraded-events | pre-commit-failures |
|---|---|---|---|
| `ghp_` / `github_pat_` / `api[_-]?key` | 0 / 0 / 0 | 0 / 0 / 0 | 0 / 0 / 0 |
| `sk-[A-Za-z0-9]{10,}` | 0 | **32** | 0 |
| `cli_[a-z0-9]{10,}` / `AKIA…` / `xox…` / PRIVATE KEY | 0 | 0 | 0 |

`sk-` 命中的**唯一值**证明全为误报（来自 "task-decomposition" 词）：
```console
$ grep -ohE 'sk-[A-Za-z0-9]{10,}' docs/synova/coordination/gate-logs-20260926/*.log.txt | sort -u
sk-decomposition
$ grep -ohE 'sk-[A-Za-z0-9]{10,}' docs/synova/coordination/gate-logs-20260926/*.log.txt | sort -u | wc -l
1
```
⇒ 无凭证。**登记**：本 PR 把"门禁不覆盖的 `.log` 内容"首次入库，故该自扫是**人工补的覆盖缺口**，不是冗余动作。

---

## 8. 红线段（R2 ⑤）

### 8.1 红线段：**本改动不新增任何红路径**；新增的是 1 条**静默失败面**（有意取舍，请 Mac-CTO 二选一确认）

**实测前提**：`quality` job 的名字 `TypeScript + Lint + Iron Laws` 是 **main 的必需检查**（`GET /repos/{owner}/{repo}/branches/main/protection` 实测；由独立自验员执行）⇒ 该 job 转红 = **阻塞全队所有 PR 合并**。

**采用方案（本 PR 实交）**：给**那一个 upload step**（**不是** job，也不改其它任何 step）加 `continue-on-error: true`。
- 效果：上传服务瞬时故障 = **显式黄灯**（step 显示警告，**不红**）；文件缺失 = 原有 `if-no-files-found: warn`（同样不红、UI 可见）。
- **取舍明文**：本改动**不新增任何红路径**；它新增的是 **1 条静默失败面**（上传失败仅黄灯、不阻断）。理由 = 本 job 是 main 必需检查，且对齐蓝图 §4.2-bis「不许降级即通过，但也不得制造阻塞」⇒ 采「**显式黄灯 + 计数可见**」而非 fail-closed。
- **反向风险如实登记**（不淡化）：极端情形"归档坏了但黄灯没人看"。兜底 = §5.5 一键补验命令 + 本文件三态表把 artifact 行判为 **⚠️ 时间性未决**（不自动升格为通过）；见未清项 §9-6。

**被否方案（并列，供 Mac-CTO 二选一）**：不加 `continue-on-error`（原 fail-loud 设计）。
- 好处：归档失败立刻红，绝无静默。
- 否决理由：**上传服务瞬时故障 ⇒ main 必需检查变红 ⇒ 阻塞全队所有 PR 合并** —— 属本工程要消灭的"治理制造阻塞"形态，且瞬时故障的影响面与"归档日志"的收益不成比例。

### 8.2 D708 写集对账：一度 `block`（误绑已合任务）→ 真取号声明后 `pass`（豁免仅 1 条内置）

**修复前**（本地忠实复现；当时提交为 `b3517ec1`——该提交**从未 push**，amend 后已被 `0a491c1f` 取代，此处保留原始 sha 以便复核现场）：
```console
❌ 结论: block — 检测到 3 个写集外文件（夹带）
   任务: D956 | 分支: chore/stage0-log-archive-20260926
   D# 推断来源: commit-subject → D956
   夹带文件 3 个: …/{degraded-events,gate-hits,pre-commit-failures}.log.txt
gate rc=1  →  CI quality job 会红
```
**根因**：分支名无 D#（阶段 0 命名规范）⇒ `infer_did` 回退扫提交 subject（跳过 `bypass COMMITTED 登记` 影子提交）⇒ 命中 **main 历史**的 merge subject `fix/d956-ci-failmsg` ⇒ 绑到 **D956**；D956 的 brief 在 origin/main 上真实存在（S3 声明 5 条，含 `.github/workflows/ci.yml`）⇒ `declared` 非空 ⇒ 3 个快照不在其中 ⇒ `exit 1`。**这是结构缺陷，已登记 §9-3。**

**修复（队长裁定 (A)：真取号 + S1 声明，不使用豁免）**：`alloc-task-id.sh` 分配 **D1009** → `task-state/D1009.json` 的 `write_set`（**list**，C14 类型契约：dict 会被静默归零）逐条列全 6 个变更文件 → 提交 subject 含 `(D1009)` ⇒ D# 推断来源变为**本卡自身**（`find_declaration_files` 命中 `task-state/D1009.json`，走 S1）。

**修复后**（本地复跑，**未注入任何豁免**）：
```console
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
   任务: D1009 | 分支: chore/stage0-log-archive-20260926
   D# 推断来源: commit-subject → D1009
   变更集: 6 个文件（merge-base ee721b0a）
   声明写集 6 条（多源并集）: 全部 ← S1:task-state.write_set
   豁免 1 条（显式，逐条打印理由）:
     · .claude/bypass.log   ← [builtin] post-commit hook 每次提交追加的证据账本（运行期产物，与写集无关）
gate rc=0
```
> **豁免条数 = 1（仅内置）**：本 PR 未使用任何声明级豁免 ⇒ 元判据 C5（豁免数须单调下降）方向不受影响，也不触碰阶段 5 待收口的 D964 FIX「豁免夹带 P0」所在面。取号/撞号自查原始输出见 §11。

### 8.3 非门禁语义变更声明（R3）

**本改动非门禁语义变更，请 Mac-CTO 批准后合并。**
依据：`git diff --numstat origin/main -- .github/workflows/ci.yml` = `21  0`（**零删除行**，`git diff -U0 | grep -c '^-[^-]'` = `0`）；仅新增 1 个 `upload-artifact` step（归档，**不判定**）+ 其 `continue-on-error: true`（不让必需检查变红）；未新增/修改/删除任何检查；`scripts/**`、`scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**` 均未触碰。

---

### 8.4 CI 实测：`quality` job 红——**失败在 D708 步**（归档步 `success`），根因是 D-号推断被 **SHA 误导**（P0 级门禁缺陷，**非本改动**）

CI 原始输出（run `36167351846` job log；`quality` 各步结论：`Iron laws check` = success、`Merge write-set reconciliation (D708)` = **failure**、`Archive gate raw logs` = **success**）：
```console
── merge-writeset-gate (D708) 合并级写集对账 ──
❌ 结论: block — 检测到 6 个写集外文件（夹带）
   任务: D839 | 分支: chore/stage0-log-archive-20260926
   D# 推断来源: commit-subject → D839
   声明写集 7 条（多源并集）: [D839 的 write_set：staging_guard.py / session_registry.py / …]
   夹带文件 6 个: [本 PR 全部文件]
##[error]Process completed with exit code 1.
```
本机**精确复现**（构造与 CI 同构的 merge 提交：parents = `a16d839e` + `ee721b0a`、同 subject —— 两处输出逐字一致）：
```console
$ printf '%s' "Merge a16d839eb0a6d8bbb8f8b43d48839376e8d2c3c1 into ee721b0abf52a4cdcc42caef44669d2cca423264" | grep -oE '[Dd][0-9]+'
d839 d8 d48839376 d2 d2          ← ★ 正则从 SHA 里抠出的"假 D#"
$ "$PYBIN" scripts/control-tower/merge_writeset_gate.py --base ee721b0a --head <同构 merge> --branch chore/stage0-log-archive-20260926
❌ 结论: block — 检测到 6 个写集外文件（夹带）
   任务: D839 …                  ← 与 CI 输出逐字一致 ⇒ 复现成立
```
**根因（file:line）**：`parse_did` 用 `DID_RE = re.compile(r"[Dd]\d+")`（`scripts/control-tower/merge_writeset_gate.py:150`）直接匹配 subject；而 GitHub 合成 merge 提交的 subject 形如 `Merge <40-hex head-sha> into <40-hex base-sha>` ⇒ SHA 片段 `…a16d839e…` 被解析成 **D839**。`infer_did` 的**分支名**源此时无 D#（阶段 0 命名规范 `chore/stage0-log-archive-20260926`）⇒ 回退到 commit-subject ⇒ 绑到 **真实存在的 D839 卡**（`task-state/D839.json`，声明 7 条）⇒ `declared` 非空 ⇒ 本 PR 6 个文件全判夹带 ⇒ `exit 1` ⇒ main 必需检查变红。

**概率**：40 位 hex 中「`[dD]` 紧跟数字」的位置期望 ≈ **3.12 处/SHA**（本机算得）⇒ **几乎每个 PR 的 merge subject 都含假 D#**。是否踩雷 = 该假号**是否恰好是真卡**（`task-state/` 367 个号）∧ 变更集是否全在 `DOC_SCOPE_RE`。

**影响面（三条）**：① 分支名含 D# 的 PR 不受影响（分支名源优先）；② **分支名无 D# 的 PR（阶段 0 命名规范）几乎必然被误绑**；③ 同一 PR 重推一次 ⇒ SHA 变化 ⇒ 假号变化 ⇒ **判定闪变（可能绿可能红）**，**不可作稳定判据**。
**本 PR 现状**：`quality`（main **必需**检查）因此为 `failure` ⇒ **阻塞合并**；归档步本身 `success`（§5.5 证据有效）。**处置待裁决**（见未清项 §9-12 的三条出路）。

---

## 9. 未清项登记

1. **`.claude/pre-commit-failures.log` CI 侧结构不可达**：CI 不 commit ⇒ 无 hook 触发写入。本 PR 由快照补"异机可读"（§6），但**没有** CI 侧持续归档。需另起机制（方向：本机 `scripts/` 侧定时把三个本地日志快照推到专用 ref / 或产物上传），**属新机制、须 Mac-CTO 批准**，本阶段不动手。
2. **存量 vs 持续边界**：CI artifact **只**收集 runner 当场产物（4873/5758/156 行的本地历史**不在其中**）⇒ 本 PR 用"日期化快照"补追溯面。后续若再需要追溯，应沿用同一约定（`gate-logs-<date>/`）而不是把 `.log` 常态化入库。
3. **`infer_did` 误绑（结构缺陷；**已定位到 `file:line` 与精确机制**，建议转 Mac-CTO）**：`parse_did` 的 `DID_RE = r"[Dd]\d+"`（`merge_writeset_gate.py:150`）会**从 GitHub 合成 merge subject 内嵌的 40 位 SHA 里抠出"假 D#"**（本 PR 实测：`…a16d839e…` → **D839**），再叠加"分支名无 D# ⇒ 回退 commit-subject"（`:187` 扫 HEAD 往回 20 条）⇒ 新卡被绑进**无关卡（也可能是已合任务）的声明集**。期望 ≈3.12 处假号/SHA ⇒ **几乎每个 PR 都含假 D#**，判定随 SHA **闪变**。另：`declared` 非空后，`:444-453` 那条"D#-less 分支 + 全文档范围 ⇒ skip"的宽松路径**不可达**。触发证据 = §8.4（本机同构复现与 CI 逐字一致）。
4. **`DOC_SCOPE_RE` 不含 `evidence/`**（队长已发现，登记）：蓝图 §八.4 规划的 `evidence/<D>/changes.json` 不匹配 `^(docs/|\.claude/|memory/|task-state/|\.github/)`，且 **`.gitignore:76` 的 `evidence/` 段使该目录整目录被忽略** ⇒ 该规划路径有**两重**结构性障碍（需 `git add -f` 才能入库）。本 PR 的落位（`docs/synova/coordination/gate-logs-20260926/`）即为此规避。
5. **`/tmp/.synova-before-brief` 是机器级共享文件（非 per-worktree）**：现存另一 session（2026-09-24，D941）记录，使任何 session 的 `SYNO_CI=1` 本地复现假红一组；CI 免疫。remediate 命令（`rm /tmp/.synova-before-brief`）会抹掉他人违规证据 ⇒ **未动，待队长/Mac-CTO 定处置**（§5.2）。
6. ~~CI 侧 artifact 真证据待回填~~ → **已回填并达成**（§5.5）：`gate-logs-36167351846`、1170 B、`expires_at` = 起始 +**整 90 天**、内含 `.claude/gate-hits.log` 21 行。**遗留一点**：`degraded-events.log` 不在其中（条件性，见 §0-2）。
7. **K3 无 token 可达性 = ❌ 实测不可达**（§5.5）：公开仓的 artifact 下载端点**匿名请求返回 HTTP 401** ⇒ **K3 无 token 时无法直接取用 artifact**。缓解面 = 快照入库（任何 clone `git ls-files` 可读，不受影响）；若要 K3 直接取 artifact，需给 K3 配 token 或改用 `::notice` 注解等匿名可达通道 ⇒ **待 Mac-CTO 决策**。
8. **本地 `SYNO_CI=1` 复现 rc=1 的环境噪声**（§5.2）：CI 免疫，但削弱"本地 CI 等价复现"能力 —— 与 §9-5 同源。
9. **U12｜`.log.txt` 仍不入门禁扫描**：`scripts/check-secrets.sh:31-34` 的 `--include` 列表与 `:136` 的暂存过滤 `\.(ts|js|json|yaml|yml|md|html)$` **均不含 `*.txt`/`*.log`** ⇒ 本次快照的凭证覆盖靠**作者自扫（§7）+ 独立自验员自扫**双重人工补证。**扩 include 清单 = 门禁语义变更 ⇒ 需 Mac-CTO 批准，另行处理**（本 PR 不做）。
10. **U15｜docs-only run 下的归档步为空**：纯文档 PR 时 `quality` job 的 Iron Laws step 被 `docsonly` 守卫跳过（`ci.yml:83`）⇒ `gate-hits.log` 不被生成，而归档步仍会执行（`if: always()`）⇒ **无文件可归档**（`warn` 可见、不红）。语义上正确（该 run 本就没有门禁产物），登记备查。
11. **新 run 与判定闪变**：`continue-on-error` 修复推送后已生成新 run `36168645768`（`head_sha=55fb95fabba6`）；因 §9-3 的 SHA 假号机制，**重推一次假号即变**（新 SHA `55fb95fa…` 抠出的假号是否撞真卡决定红/绿）⇒ **同一 PR 的 D708 结论不稳定**，不得据单次绿判"该 PR 无夹带"。
12. **⚠️ 本 PR 曾被 D708 阻塞（main 必需检查红）——已采用出路 (a)，PR 变更为 #832**（**已落地**）：
    队长裁决并执行 **GitHub rename API 重命名分支**：`chore/stage0-log-archive-20260926` → **`chore/D1009-stage0-log-archive`**（sha 不变 `93e14ee0af90`）。**注意 GitHub 行为与预期不同**：rename **不会**把 open PR 重定向，而是**关闭了 #828**（head 分支消失 ⇒ PR closed）——已如实登记；随后以同一 sha 重开 **PR #832**，#828 记为"被 #832 取代"。
    **本机预跑（新分支名）**：`infer_did` 走**分支名源①** ⇒ `任务: D1009｜D# 推断来源: branch → D1009` ⇒ **`pass`（rc=0），豁免 1 条（仅内置）** ✅ —— 即"分支名含 D#"绕开了 §9-3 的 SHA 抠号回退。CI 侧以新分支的 run 复核（`run` 见 §5.5 同款命令，`branch=chore/D1009-stage0-log-archive`）。
    另两条出路未采用：(b) `--did` pin = 门禁语义变更（冻结期否）；(c) 等 `fix/D708-*` 根治属阶段 3。
    **遗留**：§9-3 的 SHA 抠号缺陷**仍会在任何 D#-less 分支上复发** ⇒ 建议作为独立发现升级（不是本 PR 能修的）。

---

## 10. 复现命令清单（可复制）

```bash
# ── 0. 建立隔离 worktree（主树零 git 写）──
cd /Users/wane/SynovaAgent && git fetch --all --quiet
git worktree add -b chore/stage0-log-archive-20260926 /private/tmp/synova-wt-squad-ci origin/main || exit 1
[ -d /private/tmp/synova-wt-squad-ci ] || exit 1; cd /private/tmp/synova-wt-squad-ci || exit 1
ln -sfn /Users/wane/SynovaAgent/node_modules node_modules     # 只读；ignored（info/exclude:20）

# ── 1. 判据 D1（异机可读，任何 clone 通用）──
git ls-files docs/synova/coordination/gate-logs-20260926/    # 期望 3 行
shasum -a 256 docs/synova/coordination/gate-logs-20260926/*.log.txt
wc -l docs/synova/coordination/gate-logs-20260926/*.log.txt  # 期望 4873 / 5758 / 156

# ── 2. YAML 结构 + 夹具（含变异体 M1 必红）──
node -e 'const y=require("./node_modules/js-yaml"),fs=require("fs");
const s=y.load(fs.readFileSync(".github/workflows/ci.yml","utf8")).jobs.quality.steps
  .find(x=>String(x.uses||"").includes("upload-artifact"));console.log(JSON.stringify(s.with,null,2))'
node /tmp/task1-fixture.js .github/workflows/ci.yml; echo "rc=$?"   # 期望 0（8 断言）

# ── 3. 改动形状（0 删除行 = 不可能改判定）──
git diff --numstat origin/main...HEAD -- .github/workflows/ci.yml   # 期望 17  0

# ── 4. CI 行为本地复现（worktree 内；主树取证现场不动）──
rm -f .claude/gate-hits.log .codex/control-tower/logs/degraded-events.log
SYNO_DIFF_BASE=origin/main SYNO_CI=1 bash scripts/pre-commit-check.sh > /tmp/ci-gate-repro.log 2>&1
wc -l .claude/gate-hits.log                                  # 期望 >0（本地实测 21）
python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
  --branch chore/stage0-log-archive-20260926                 # 观察 D708 判定（§8.2）
python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
  --branch chore/stage0-log-archive-20260926 --pr-body /tmp/task1-pr-body.md   # 期望 rc=0（豁免生效）

# ── 5. 凭证自查（门禁不覆盖 .log，必人工跑）──
for pat in 'ghp_' 'github_pat_' 'api[_-]?key' 'sk-[A-Za-z0-9]{10,}' 'cli_[a-z0-9]{10,}'; do
  grep -roE "$pat" docs/synova/coordination/gate-logs-20260926/ | wc -l | tr -d ' '
done
grep -ohE 'sk-[A-Za-z0-9]{10,}' docs/synova/coordination/gate-logs-20260926/*.log.txt | sort -u

# ── 6. CI 侧真证据（run 完成后）──
#   见 §5.5（artifacts API + 下载 + unzip -l + expires_at）
```

---

## 11. 取号与 S1 声明（§8.2 修复的原始证据）

```console
$ bash scripts/control-tower/alloc-task-id.sh "阶段0-门禁日志归档"
D1009
已登记: /private/tmp/synova-wt-squad-ci/task-state/D1009.json (status=claimed)
brief 骨架已生成: …/2026-09-26-D1009-阶段0-门禁日志归档.md（填写后开工）
ALLOC_RC=0
```
> 后台执行、上限 180s 内返回 `rc=0` ⇒ **取号器未挂死**，不触发"取号器挂死第 3 次"的红线升级。

撞号自查（`origin/main` 与远端分支）：
```console
$ git ls-tree --name-only origin/main task-state/ | grep -oE 'D[0-9]+' | sort -V | tail -3
D967
D968
D1007
$ git ls-remote --heads origin | grep -ci D1009
0        # 远端无含 D1009 的分支
$ git ls-tree --name-only origin/main task-state/ | grep -c D1009
0        # origin/main 无 D1009 卡
```

`task-state/D1009.json` 的 `write_set`（**list**，C14 类型契约；逐条 = 本 PR 变更文件）：
```json
"write_set": [
  ".github/workflows/ci.yml",
  "docs/synova/coordination/阶段0-日志归档-20260926.md",
  "docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt",
  "docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt",
  "docs/synova/coordination/gate-logs-20260926/pre-commit-failures.log.txt",
  "task-state/D1009.json"
]
```
本地认领制门禁链（原始输出）：
```console
$ bash scripts/workflow/check-brief-parseable.sh .claude/task-briefs/2026-09-26-D1009-阶段0-门禁日志归档.md
[check-brief-parseable] ✅ brief 可解析: … (Q2 ✓ #CRITERIA=A 架构层=基础设施 Done=4)
$ bash scripts/workflow/resolve-commit-brief.sh "$(git diff --cached --name-only)"
/private/tmp/synova-wt-squad-ci/.claude/task-briefs/2026-09-26-D1009-阶段0-门禁日志归档.md
```
> 该 brief **未入库**（不扩张写集；CI 侧声明只走 S1 `task-state/D1009.json`）。它只服务本地 `commit-msg` 认领制：本 PR 分支无 D#，而窗口内 `D945`/`D956` 等 brief 都声明过 `.github/workflows/ci.yml`，首轮本地提交曾被 `D328` 判"声明(无)≠归属(D945)"**硬阻断**；以本卡自己的 brief（认领数 5 > 他人 1）收回归属后放行。

---

## 附：本次交付的提交与验证币值

| 项 | 值 |
|---|---|
| 卡号 | **D1009**（`alloc-task-id.sh` 分配，撞号自查 0 命中） |
| 父节点 | `ee721b0abf52`（= origin/main） |
| 提交 | `0a491c1f`（本任务，subject 含 `(D1009)`）+ `fcd6b4de`（post-commit 影子，`.claude/bypass.log` +1） |
| 写集 | 6 文件（ci.yml / 本文档 / 3 快照 / `task-state/D1009.json`）+ hook 自动的 `.claude/bypass.log` |
| 改动面 | `git diff --numstat` = `21 0`；`git diff -U0 \| grep -c '^-[^-]'` = **0** |
| D708 合并级写集对账 | **pass**（`rc=0`），D# 来源 `commit-subject → D1009`，**豁免 1 条（仅内置）** |
| 夹具 | 基线 **9/9 ✅**；变异体 M1–M7 **7/7 rc=1** |
| YAML | js-yaml 4.3.0 解析 ✅ |
| PR 预算 | 6 文件 ≤12、单域 ✅（`check-pr-budget.sh` rc=0） |
| PR | **#832**（`https://github.com/tangbaobao520/SynovaAgent/pull/832`；#828 因分支改名被 GitHub 关闭 ⇒ 被 #832 取代） |
| **artifact 真可下载/非空/90 天** | ✅ `gate-logs-36167351846`｜1170 B｜`expires_at` = 起始 +**90 天**｜内含 `.claude/gate-hits.log` 21 行（§5.5）；`degraded-events.log` 不在内（条件性）｜**匿名下载 401**（§9-7） |
| **CI `quality`（必需检查）** | ❌ 红，**非本改动**：失败在 D708 步（D-号被 merge SHA 抠成假号 **D839**，§8.4）；**归档步 success**；出路待裁决（§9-12） |
| 凭证 | 8 模式扫 3 文件：除 `sk-`（32 命中 / 唯一值 1 = `sk-decomposition`）外全 0 |
| 快照完整性 | sha256 与主树取证现场逐字符一致；主树前后哈希不变 |
