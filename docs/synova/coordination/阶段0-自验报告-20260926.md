# 阶段0 独立自验报告 · 波次1（① 重装 git hook）

| 项 | 值 |
|---|---|
| **卡号** | `task-3`（共享任务板）· 阶段0-① 重装 git hook |
| **角色** | 独立自验员 `squad-verifier` — **非**被验产物作者；不写审计标准、不碰 `scripts/audit/**`、不做自我审计 |
| **分支** | `chore/stage0-selfverify-20260926` |
| **父节点（断面）** | `origin/main = ee721b0abf52a4cdcc42caef44669d2cca423264`（蓝图冻结值 `ef299746` **已过期** ⇒ 本报告全部数字自行重跑，不采信任何转述） |
| **工作树** | 自验交付 `/private/tmp/synova-wt-squad-verify`；反例夹具 `/private/tmp/synova-wt-stage0-redtest`（用后清理，见 §4.3） |
| **判据来源** | 工程派单 §三 阶段0「重装 git hook」行（`:75`）＋ 蓝图 §六.2 C13（`:635`）／ §九.11（`:812-816`）／ 自锁声称原文（`:190`、`:664`） |
| **判定纪律** | 有条件通过 = **未通过**；无原始输出不得写「通过」；发现缺陷只报告不改作者产物 |
| **波次** | 本文件 v1 = 波次1（①重装 hook）。波次2（②日志归档／③C16 分类）见 §6，待 task-1／task-2 交付后补 |

---

## 0. 判据汇总（先给结论）

| # | 判据 | 结论 |
|---|---|---|
| ① -1 | hook 内 `bypass.log` **字样消失** | ⚠️ **字面未满足 / 实质满足**（2 处均为注释行；写入目标里为 0） |
| ① -2 | 改为写 `gate-soft-warnings.log` | ✅ 通过（1 行实质写入，行 11） |
| ① -3 | **hook 与生成器同哈希** | ✅ 通过（两种独立方法同哈希 `609788b1…`） |
| ① -4 | 反例：改坏即红（旧/新 hook 落点差异） | ✅ 通过（同一红灯夹具；旧 +1 落 bypass.log，新 +1 落 gate-soft-warnings.log） |
| ① -5 | 声称核验：「旧 hook 把软失败写进 bypass.log ⇒ **当日自阻断**」 | ❌ **声称未复现**（主机制；唯一可熔断路径另立且未接线 ⇒ §3） |
| ① -6 | `core.hooksPath` 绝对路径影响面 ＋ 语义未变 | ✅ 通过（4 hook 全量重装且与生成器一致；语义 diff 仅"日志落点"一行） |
| **阶段0-① 综合判定** | — | **通过（实质口径）**，附 3 项登记待 CTO 处置（非阻塞，其中 1 项为判据文本自相矛盾） |

> 「通过（实质口径）」不豁免字面差异：判据① 的字面未满足、判据⑤ 的声称未复现均已如实登记，见 §7。

---

## 1. 判据① -1／① -2：写入目标的「字面 vs 实质」

**判据（派单 `:75` 原文）**：*「hook 内 `bypass.log` 字样消失；改为 `gate-soft-warnings.log`」*

### 1.1 原始命令与原始输出

```console
$ grep -n 'bypass\.log' /Users/wane/SynovaAgent/.git/hooks/pre-commit
9:  # D508/Win#10: 软门禁噪声移出 bypass.log（证据链只记真实提交/绕过；软告警独立日志——
10:  #   否则每次 commit 污染 bypass.log → 下次操作前必 checkout 清理，实测 10+ 次）
rc=0

$ grep -n 'gate-soft-warnings' /Users/wane/SynovaAgent/.git/hooks/pre-commit
11:  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"

$ grep -nE '>>?[[:space:]]*"\$ROOT/' /Users/wane/SynovaAgent/.git/hooks/pre-commit
7:  echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) exit=$EXIT_CODE branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/pre-commit-failures.log"
11:  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"
15:echo "$(git rev-parse HEAD 2>/dev/null || true)|$(date +%s)" > "$ROOT/.claude/last-precommit-success"
```

**行级定性（逐行判「注释行 vs 可执行行」）**

```console
line 9  -> 注释行 : # D508/Win#10: 软门禁噪声移出 bypass.log（…
line 10 -> 注释行 : #   否则每次 commit 污染 bypass.log → …
```

### 1.2 结论

- **实质**：hook 的**可执行写入目标共 3 个**——`pre-commit-failures.log`（行 7）、`gate-soft-warnings.log`（行 11）、`last-precommit-success`（行 15）。**没有任何一行把数据写进 `bypass.log`**。✅
- **字面**：`grep 'bypass.log'` **仍有 2 处命中**（行 9、10，均为注释），即派单「字样消失」**字面不成立**。⚠️
- 两处注释是**解释性注记**（记录 D508/Win#10 为何迁移），保留它们符合「哪条防线因何存在」的可读性要求；删除它们才是信息损失。
- **判据文本自相矛盾（须 CTO 澄清）**：同一行判据的第三个分句要求「hook 与生成器**同哈希**」，而生成器 `scripts/install-hooks.sh:51-52` 的 body 里就包含这 2 行注释；阶段0 又明令 `scripts/install-hooks.sh`（**执行，不修改**）。⇒ 三者不可同时满足。判据① 应以**实质口径**（写入目标不含 bypass.log）计，本报告据此判**通过**，并把文本矛盾登记为 U3（§7）。

---

## 2. 判据① -3：hook 与生成器同哈希（自行提取，不采信转述）

### 2.1 方法 A — 机械提取生成器 body（单引号语义解析）

```console
$ python3  # 从 scripts/install-hooks.sh 的 install_hook() 中提取 pre-commit 分支 body=' … '
==== extracted body (repr of first/last 60 chars) ====
first: '#!/bin/bash\n# v4.8.x 方案1(挪CI): pre-commit 软提示 — 本地门禁失败不阻断，CI'
last : '(date +%s)" > "$ROOT/.claude/last-precommit-success"\nexit 0\n'
extracted bytes: 1209
extracted sha256: 609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401

$ shasum -a 256 /Users/wane/SynovaAgent/.git/hooks/pre-commit
609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401  /Users/wane/SynovaAgent/.git/hooks/pre-commit

$ diff /private/tmp/extracted-precommit-body /Users/wane/SynovaAgent/.git/hooks/pre-commit
DIFF: IDENTICAL
```

### 2.2 方法 B — 可执行 oracle（在隔离夹具里真跑生成器）

在 `/private/tmp/synova-gen-fixture`（`git init` + 4 个最小 entry 夹具）执行**未改动的** `scripts/install-hooks.sh`，再比对它生成的文件：

```console
$ bash scripts/install-hooks.sh      # 在隔离夹具内执行
  ✅ pre-commit / ✅ commit-msg / ✅ pre-push / ✅ post-commit
$ shasum -a 256 .git/hooks/pre-commit
609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401  .git/hooks/pre-commit
$ diff .git/hooks/pre-commit /Users/wane/SynovaAgent/.git/hooks/pre-commit
ORACLE-DIFF: IDENTICAL
```

**附带发现（超出判据范围的加验）**：本次重装是**全量**而非仅 pre-commit——4 个 hook 全部与生成器一致：

```console
  pre-commit   生成=609788b1ac3bfb31 安装=609788b1ac3bfb31  MATCH
  commit-msg   生成=a6361b1fe873c440 安装=a6361b1fe873c440  MATCH
  pre-push     生成=08e6c8ceb7df2846 安装=08e6c8ceb7df2846  MATCH
  post-commit  生成=e6265838fa8a0c75 安装=e6265838fa8a0c75  MATCH
```

### 2.3 结论

**通过。** 两个哈希：安装件 `609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401`（1209 B）；方法 A 提取 body 与方法 B 夹具生成物**均同哈希、字节级一致**。

**旧件溯源（独立核，不采信 `pre-commit.old` 文件本身）**：

```console
$ for sha in $(git log --format='%h' -12 -- scripts/install-hooks.sh); do …提取该历史版 body 的 sha256…; done
  ab05e2de 2026-08-21  write-bypass.log=1  GATE_FAIL_SOFT=1
  4982f978 2026-08-23  write-bypass.log=0  GATE_FAIL_SOFT=1
  …
  ab05e2de: 历史版body sha256=4867f38c43d44ac9 len=977
  ab05e2de: 备份件    sha256=4867f38c43d44ac9 len=977  → MATCH
```

⇒ 备份件 `/private/tmp/cto-stage0-hook-backup/pre-commit.old` **逐字节等于** `ab05e2de`（D467「方案1挪CI」，2026-08-21）所生成的版本，即「写 bypass.log」的那一版；`4982f978`（D508，2026-08-23）起改为 `gate-soft-warnings.log`。溯源成立。

---

## 3. 判据① -4：反例（改坏即红）

**方法**：同一夹具 `src/__stage0_verify_asany_fixture__.ts`（含 `as any` ⇒ 铁律 38 组 1 `hard_check`），在 scratch worktree 内**先复位再分别跑**旧 hook / 新 hook，比较两份日志的行数 delta。脚本：`/private/tmp/stage0-verify/repro-hook-softfail.sh`。

### 3.1 同一条红灯（两次运行完全一致的门禁判决）

```console
    ❌ as any / as never / as unknown as 零容忍（新增，铁律 38；存量独立清理）: 1 处  [硬阻断]
       +export const stage0VerifyFixture = { shape: "x" } as any;
    ❌ 4 组未通过 — 提交已拒绝
    ⚠️ 本地门禁未通过（exit=1）— 已放行，CI 将作为权威判定（merge 前必须绿）
```

（RUN A / RUN B 的组 1 命中行、硬阻断组数 4、hook 侧 `exit=1` **逐字相同** ⇒ 差异只来自 hook 版本，非夹具漂移。）

### 3.2 RUN A：旧 hook（`pre-commit.old`）

```console
[BEFORE-A] bypass.log=1825行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=MISSING
  hook EXIT=0
[AFTER-A ] bypass.log=1826行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=1
--- 旧hook 本次新追加的行（bypass.log 尾行）---
2026-09-26T01:13:57+08:00 | GATE_FAIL_SOFT | exit=1 | branch=
--- gate-soft-warnings.log 运行后是否出现 ---
ls: .claude/gate-soft-warnings.log: No such file or directory
```

**delta：`bypass.log` +1（写入的是 `GATE_FAIL_SOFT` 行）；`gate-soft-warnings.log` ±0（未创建）。**

### 3.3 RUN B：新 hook（当前安装件）

```console
[BEFORE-B] bypass.log=1825行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=1
  hook EXIT=0
[AFTER-B ] bypass.log=1825行  gate-soft-warnings.log=1        pre-commit-failures.log=2
--- 新hook 写入 gate-soft-warnings.log 的原文 ---
2026-09-26T01:14:01+08:00 | GATE_FAIL_SOFT | exit=1 | branch=
--- bypass.log 是否被新hook 追加（尾1行）---
2026-09-26T00:14:19+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=3ccc6c6e4745e7ab568dc0411ccc06e9374068f4
```

**delta：`bypass.log` +0（尾行仍是提交登记的 COMMITTED 行，非本次写入）；`gate-soft-warnings.log` +1。**
（`pre-commit-failures.log` 两次均 +1，符合设计：门禁失败先落该日志。）

### 3.4 结论与清理

**反例成立（通过）**：同一红灯下，旧 hook **只**落 `bypass.log`；新 hook **只**落 `gate-soft-warnings.log`；两者对 `bypass.log` 的行为**互斥且可复现**。

**夹具清理（已执行）**：

```console
$ git status --short          # 清理后（片段内无输出）
  fixture removed: YES
```

- scratch worktree `/private/tmp/synova-wt-stage0-redtest` 内 `src/__stage0_verify_asany_fixture__.ts` 已删、暂存已复位、`bypass.log` 已 `git checkout --` 复位、`gate-soft-warnings.log` 已删；**主工作树 `/Users/wane/SynovaAgent` 未做任何 git 写操作**（全程只读）。
- 复跑瑕疵（自曝，不影响判据）：脚本首段的 `cat -n` 写在 `reset_fixture` 之前，故首屏打印 `No such file`；夹具内容以两次 RUN 的 `+export const … as any;` 命中行为准。

---

## 4. 判据① -5：声称核验——「旧 hook ⇒ **当日自阻断**」**未复现**

### 4.1 被核声称（原文）

- 蓝图 `:190`：*「旧版 hook 把软失败写进 `.claude/bypass.log`；而 `pre-commit-check.sh:239-256` 会因 `bypass.log` 里的**当日** `detected-bypass` 记录**硬阻断**（`exit 1`）→ **旧 hook 会自己把自己锁死**」*
- 蓝图 `:664`：*「旧版把软失败写进 `.claude/bypass.log` → 触发 `pre-commit-check.sh:239-256` 的当日硬阻断 = 自己把自己锁死」*
- 工程派单 `:75`：*「解自锁：旧 hook 把软失败写进 `bypass.log` ⇒ 当日自阻断」*

### 4.2 四个面的逐个核验（含 file:line）

| 面 | 证据（file:line） | 判定 |
|---|---|---|
| pre-commit GATEKEEPER | `pre-commit-check.sh:239` `[ "${GITHUB_ACTIONS:-}" != "true" ]`；`:242` `grep -c "${TODAY}.*detected-bypass"`；`:254` `exit 1` | 只匹配 **`detected-bypass`**。旧 hook 写的是 **`GATE_FAIL_SOFT`** ⇒ **不匹配、不阻断** |
| pre-commit 绕过审计 | `:1085`（`detected-bypass`）／`:1086`（`possible-bypass`）／`:1092-1101`（≥3 ⇒ **`SOFT_COUNT`**，非 exit） | 同上不匹配；即便命中也是**软提示**，非硬阻断 |
| pre-push 门禁 7 ＋ 对账脚本 | `pre-push-check.sh:420-444` → `check-bypass-log.sh:102` `grep -q "$h"`（按 **commit hash** 找记录） | `GATE_FAIL_SOFT` 行**无任何哈希** ⇒ 既不能"补上"记录、也**不可能造成缺记录**；多余行只会**增加**命中概率，结构上无法致阻断 |
| `.claude/settings.json` PreToolUse | hooks 实配 = `scripts/workflow/hook-block-write.sh` ＋ `scripts/hooks/hook-git-detect.sh`（读 bypass 者：**0**）；`hook-bypass-block.sh` **全仓不存在**（`find` 零结果） | 无 bypass 读取面 |
| （附加）CI 侧 | `.github/workflows/ci.yml` 全文 grep `bypass`／`detected-bypass` **零命中**；`:84-87` Iron Laws 以 `SYNO_CI: "1"` 跑 `pre-commit-check.sh` | CI 不消费 bypass.log |

### 4.3 决定性原始输出（重跑判据点 ＋ 注入红队沙箱）

```console
# ① 真实日志（主树）：101 条 GATE_FAIL_SOFT，今日 0 条
$ wc -l .claude/bypass.log                        → 1813
$ grep -c 'GATE_FAIL_SOFT' .claude/bypass.log     → 101
$ grep -c '2026-09-26.*GATE_FAIL_SOFT' …          → 0
  GATE_FAIL_SOFT 日期分布: 08-24:1  08-25:19  08-27:5  08-28:7  09-04:1  09-08:2
                            09-09:3  09-10:15  09-22:4  09-23:1  09-24:3  09-25:33

# ② 逐字重跑 :242 的判据 grep —— 结果 0（= 不阻断）
$ grep -cE "$(date +%Y-%m-%d).*detected-bypass" .claude/bypass.log   → 0
$ grep -cE "$(date +%Y-%m-%d).*possible-bypass" .claude/bypass.log   → 0

# ③ 红队注入：向夹具工作树的 bypass.log 追加 8 条「今日」GATE_FAIL_SOFT（累计 109 条）后，
#    真跑 pre-commit-check.sh（无夹具暂存，只看 GATEKEEPER / 绕过审计段）
  注入后今日 GATE_FAIL_SOFT 条数: 109
  注入后 grep -cE "$TODAY.*detected-bypass": 0
  pre-commit-check.sh EXIT=0
    68:  ✅ 绕过审计
    ✅ 全部 13 组通过
```

⇒ **即使把"当日 GATE_FAIL_SOFT 污染"造到 8 条，本地 pre-commit 仍 `exit 0`、GATEKEEPER 段根本不打印「检测到今日 N 次 --no-verify 绕过记录」。声称的主机制未复现。**

### 4.4 唯一真实可熔断路径（另立，非旧 hook 所致）——如实登记

核验中发现**唯一**会把"当日 bypass.log 行数"变成 `exit 1` 的代码路径：

```console
# scripts/workflow/loop-context.sh
85:  BYPASS_LOG="$ROOT/.claude/bypass.log"
88:    BYPASS_COUNT=$(grep -c "$TODAY" "$BYPASS_LOG" 2>/dev/null | tr -d '\r' || echo 0)   # ← 未过滤 detected-bypass
91:    if [ "${BYPASS_COUNT:-0}" -ge 3 ]; then
92:      echo -e "  ${RED}熔断: 今日 --no-verify ${BYPASS_COUNT} 次 (阈值 3)${RESET}"
94:      BREAKER_TRIPPED=1
105:    if [ "$BREAKER_TRIPPED" -gt 0 ]; then … exit 1

$ bash scripts/workflow/loop-context.sh --check        # 在上述注入状态下
  loop-context.sh EXIT=1
    熔断: 今日 --no-verify 10 次 (阈值 3)
    熔断触发 — 需要人工介入
```

但该路径**不构成"旧 hook ⇒ 当日自阻断"的证据**，理由三条（均物理可核）：

1. **未接线**：`loop-context.sh` 在 `package.json`、`.github/workflows/*.yml`、`pre-commit-check.sh`、`pre-push-check.sh` 中**调用点为 0**（grep rc=1）；仅 `LOOP.md` / `loop-score.sh`（存在性检查）/ 文档提及 ⇒ 只能人工显式调用。
2. **口径过宽、与旧 hook 无因果**：`:88` 数的是**任意含今日日期的行**，把 post-commit 的 `COMMITTED` 正常登记行一并计入。实测 10 条里 8 条是注入的 `GATE_FAIL_SOFT`、2 条是当日 `COMMITTED` 行 ⇒ **不经旧 hook（仅正常提交 3 次）同样会熔断**，不是旧 hook 的特征后果。
3. **已知未修**：该缺陷已被登记于 `docs/synova/coordination/UPGRADE-SPEC-控制塔与审计流程-20260817.md:60` 与 `:101`（U1d：「`loop-context.sh:88` `grep -c "$TODAY"` 未过滤 `detected-bypass`……是 GATEKEEPER 误报熔断源之一」），至本断面**未落地**。

**⇒ 判据① -5 结论：`❌ 声称未复现`（就该声称在「被点名的四个面」上的机制而言）。重装 hook 的真实收益是**证据链卫生**（`bypass.log` 是 git 跟踪的**证据**日志：101 条软噪声会污染 D331 对账面／Gatekeeper 计数面的可读性；D508 注释记为 Win 实测 10+ 次 checkout 清理），**而非**"解除当日硬阻断"。声称的措辞需更正（登记 U1，§7）。**

---

## 5. 判据① -6：`core.hooksPath` 绝对路径影响面 ＋ 语义未变

### 5.1 影响面：绝对路径 ⇒ 影响**所有** worktree

```console
$ git config --show-origin --get core.hooksPath
file:/Users/wane/SynovaAgent/.git/config	/Users/wane/SynovaAgent/.git/hooks
$ 是否绝对路径: YES
$ git -C /private/tmp/synova-wt-squad-verify      config --get core.hooksPath
/Users/wane/SynovaAgent/.git/hooks
$ git -C /private/tmp/synova-wt-stage0-redtest    config --get core.hooksPath
/Users/wane/SynovaAgent/.git/hooks
$ git rev-parse --git-common-dir
/Users/wane/SynovaAgent/.git
```

⇒ 配置在 **common config** 上，取值是**主树 .git 下的绝对路径**；任何 linked worktree（含 `git rev-parse --show-toplevel` 不同的树）都加载**同一份** hook 文件。本次重装对全部 worktree **同时生效**（含本任务之外的其他 session 工作树）。

```console
$ stat -f '%Sm %N' …
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/pre-commit
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/commit-msg
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/pre-push
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/post-commit
2026-08-28 21:05:37  /Users/wane/SynovaAgent/scripts/install-hooks.sh
```

（旧件 mtime `Aug 21 17:14` → 现件 `Sep 26 01:11`；生成器未改，仍为 `Aug 28 21:05`。前者与蓝图 `:189` 的「落后 7 天」观察一致。）

### 5.2 语义未变：旧↔新全文 diff（唯一语义差异 = 日志落点）

```diff
--- /private/tmp/cto-stage0-hook-backup/pre-commit.old	2026-08-21 17:14:00
+++ /Users/wane/SynovaAgent/.git/hooks/pre-commit	2026-09-26 01:11:13
@@ -6,7 +6,9 @@
 if [ $EXIT_CODE -ne 0 ]; then
   echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) exit=$EXIT_CODE branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/pre-commit-failures.log"
   # 软提示: 记录门禁失败但放行（K3 审计证据），CI 权威判定
-  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/bypass.log"
+  # D508/Win#10: 软门禁噪声移出 bypass.log（证据链只记真实提交/绕过；软告警独立日志——
+  #   否则每次 commit 污染 bypass.log → 下次操作前必 checkout 清理，实测 10+ 次）
+  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"
   echo "⚠️ 本地门禁未通过（exit=$EXIT_CODE）— 已放行，CI 将作为权威判定（merge 前必须绿）" >&2
 fi
 # 无论成败都写 marker（失败但放行 = 经过了 pre-commit，非 --no-verify）
```

**语义对照（新旧一致项）**：

| 语义 | 旧 hook | 新 hook | 证据 |
|---|---|---|---|
| 本地门禁失败是否阻断 `git commit` | **否**（软提示） | **否**（同一 `exit 0`） | 两文件末行均为 `exit 0`；§3.2/§3.3 两次 run `hook EXIT=0` 且"已放行" |
| 成功标记 `last-precommit-success`（`--no-verify` 检测依赖） | 写 | 写 | 行 13 / 行 15 逐字一致 |
| 失败进 `pre-commit-failures.log` | 写 | 写 | §3 delta 两次均 +1 |
| 硬阻断权威在哪 | CI | CI | `ci.yml:84-87`（`SYNO_CI: "1"` 跑同一 `pre-commit-check.sh`） |

### 5.3 结论

**通过。** 重装**不改变 hook 语义**：仍是"本地软提示 ＋ CI 权威"，唯一差异是软失败日志落点由 `bypass.log` 改为 `gate-soft-warnings.log`；且因 `core.hooksPath` 为绝对路径，该变更**已对所有 worktree 生效**（这正是"重装一次、全树生效"的机制，也意味着**没有"局部未重装"的中间态**——§2.2 四个 hook 全 MATCH 与之互证）。

---

## 6. 波次2（待交付，本文件 v1 未覆盖）

| # | 待验对象 | 判据 | 状态 |
|---|---|---|---|
| ② | task-1 日志归档 | `ci.yml`：`retention-days≥90`、artifact 名、`if-no-files-found: ignore`；**异机可复跑**是否成立（拿不到 CI 运行日志 ⇒ 判**未通过/有条件**，不放水） | ⏳ 等待 `squad-ci` 交付 |
| ③ | task-2 C16 分类表 | 自抽 **≥5 个**工作树重算 `git rev-list --count origin/main..<branch>` 与分类表逐条比对；核归类理由是否有可核证据（PR 号／patch-id／子树重合） | ⏳ 等待 `squad-analyst` 交付 |

---

## 7. 未清项登记（本自验员只报告，不改作者产物）

| 编号 | 事项 | 证据 | 建议归属 |
|---|---|---|---|
| **U1** | **声称未复现**：蓝图 `:190`／`:664`、派单 `:75` 的「旧 hook ⇒ **当日自阻断**」在主机制上不成立（`GATE_FAIL_SOFT` ≠ `detected-bypass`，`pre-commit-check.sh:242` 只匹配后者；注入 8 条同日软失败后仍 `exit 0`）。重装 hook 的真实收益是**证据链卫生**，判据措辞需更正 | 本报告 §4.2／§4.3 | CTO 更正判据文本（蓝图 v1.4 ＋ 派单）；**不建议**为"自锁"再新增机制 |
| **U2** | **潜在熔断源未修**：`scripts/workflow/loop-context.sh:88` 用未过滤的 `grep -c "$TODAY"`（把 `COMMITTED`／`GATE_FAIL_SOFT` 一并计为"绕过"）→ `--check` 下 `exit 1`（实测）。该脚本**未接线**（自动化调用点 0）；缺陷已登记于 `UPGRADE-SPEC-控制塔与审计流程-20260817.md:60/101`（U1d）但未落地 | 本报告 §4.4 | 阶段1／阶段5 收口；本自验员不动作者产物、**不新增门禁**（阶段0–4 冻结） |
| **U3** | **判据文本自相矛盾**：判据① 的「hook 内 `bypass.log` 字样消失」与「hook 与生成器同哈希」不可同时满足（生成器 body 自带 2 行注释，且阶段0 明令 install-hooks.sh「执行，不修改」）。判据① 已按**实质口径**判通过 | 本报告 §1.2 | CTO 澄清判据措辞（改写为"写入目标不含 bypass.log"） |
| **U4** | **无关本判据的观察**：门禁红灯夹具运行在 detached HEAD，故 hook 落盘行 `branch=` 为空；`pre-commit-check.sh` 在 scratch worktree 无夹具暂存时仍报「13 组通过 ＋ 1 项软提示」（软提示来源与本次判据无关，未深挖） | §3.2／§4.3 | 仅登记，无需处置 |
| **U5** | **波次2 未启动**（依赖 task-1／task-2 交付） | §6 | 待交付后补验并更新本报告 |

---

## 8. 复跑附录（K3 抽验用；全部命令可在 `origin/main` 断面重跑）

```bash
# ① 判据①-1/-2：写入目标
H=/Users/wane/SynovaAgent/.git/hooks/pre-commit
grep -n 'bypass\.log' "$H"                      # 期望 2 处（均为注释行）
grep -nE '>>?[[:space:]]*"\$ROOT/' "$H"         # 期望 3 个实质目标，无 bypass.log

# ② 判据①-3：同哈希（方法 A）
python3 - <<'PY'                                # 从 install-hooks.sh 提取 body → sha256
import pathlib,hashlib
s=pathlib.Path('/Users/wane/SynovaAgent/scripts/install-hooks.sh').read_text()
i=s.index('if [ "$name" = "pre-commit" ] && [ -f "$entry" ]; then')
j=s.index("body='", i)+6; k=s.index("'\n", j)
print(hashlib.sha256((s[j:k]+'\n').encode()).hexdigest())
PY
shasum -a 256 "$H"                              # 期望 609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401

# ③ 判据①-4：反例（自包含脚本，先在 scratch worktree 内建好树）
bash /private/tmp/stage0-verify/repro-hook-softfail.sh

# ④ 判据①-5：声称核验
grep -c 'GATE_FAIL_SOFT' /Users/wane/SynovaAgent/.claude/bypass.log        # 101
grep -cE "$(date +%Y-%m-%d).*detected-bypass" /Users/wane/SynovaAgent/.claude/bypass.log  # 0
sed -n '239,256p'   /Users/wane/SynovaAgent/scripts/pre-commit-check.sh    # 只匹配 detected-bypass
sed -n '1084,1108p' /Users/wane/SynovaAgent/scripts/pre-commit-check.sh    # ≥3 → SOFT_COUNT
sed -n '420,444p'   /Users/wane/SynovaAgent/scripts/pre-push-check.sh      # 门禁7 → check-bypass-log.sh
sed -n '88,94p'     /Users/wane/SynovaAgent/scripts/workflow/loop-context.sh  # 未过滤熔断源

# ⑤ 判据①-6：影响面 + 语义 diff
git config --show-origin --get core.hooksPath                             # 绝对路径
diff -u /private/tmp/cto-stage0-hook-backup/pre-commit.old "$H"           # 唯一差异 = 日志落点
```

**自验边界声明**：本报告由 `squad-verifier`（非作者）独立重跑；未改动任何被验产物；未接触 `scripts/audit/**`、`docs/synova/audit-reports/**`；未在主工作树 `/Users/wane/SynovaAgent` 执行任何 git 写操作；未新增门禁。K3 抽验输入（原始命令 ＋ 原始输出）已在本文备齐，「可复跑」以本文件 §8 为准。
