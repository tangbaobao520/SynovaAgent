# 阶段0 独立自验报告 · 波次1（① 重装 git hook）

| 项 | 值 |
|---|---|
| **卡号** | `task-3`（共享任务板）· 阶段0-① 重装 git hook |
| **角色** | 独立自验员 `squad-verifier` — **非**被验产物作者；不写审计标准、不碰 `scripts/audit/**`、不做自我审计 |
| **分支** | `chore/stage0-selfverify-20260926` |
| **父节点（断面）** | `origin/main = ee721b0abf52a4cdcc42caef44669d2cca423264`（蓝图冻结值 `ef299746` **已过期** ⇒ 本报告全部数字自行重跑，不采信任何转述） |
| **工作树** | 自验交付 `/private/tmp/synova-wt-squad-verify`；反例夹具 `/private/tmp/synova-wt-stage0-redtest`（用后清理，见 §4.3） |
| **判据来源** | 工程派单 §三 阶段0「重装 git hook」行（`:75`）＋ 蓝图 §六.2 C13（`:635`）／ §九.11（`:812-816`）／ 自锁声称原文（`:190`、`:664`） |
| **判定纪律** | 有条件通过 = **未通过**；无原始输出不得写「通过」；发现缺陷只报告不改作者产物 |
| **波次** | **v1.4** = 波次1（①重装 hook）＋ U2 加固（§4.4）＋ 口径对账表（§6.1/§7）＋ **波次2-A：task-2 自验（§8）** ＋ **波次2-B：task-1 自验（§9）** |

---

## 0. 判据汇总（先给结论）

| # | 判据 | 结论 |
|---|---|---|
| ① -1 | hook 内 `bypass.log` **字样消失** | ⚠️ **字面未满足 / 实质满足**（2 处均为注释行；写入目标里为 0） |
| ① -2 | 改为写 `gate-soft-warnings.log` | ✅ 通过（1 行实质写入，行 11） |
| ① -3 | **hook 与生成器同哈希** | ✅ 通过（两种独立方法同哈希 `609788b1…`） |
| ① -4 | 反例：改坏即红（旧/新 hook 落点差异） | ✅ 通过（同一红灯夹具；旧 +1 落 bypass.log，新 +1 落 gate-soft-warnings.log） |
| ① -5 | 声称核验：「旧 hook 把软失败写进 bypass.log ⇒ **当日自阻断**」 | ❌ **声称未复现**（主机制；唯一可熔断路径另立且未接线 ⇒ §3） |
| ① -6 | `core.hooksPath` 绝对路径影响面 ＋ 语义未变 | ✅ 通过（4 hook 全量重装且与生成器一致；语义 diff 仅"日志落点"一行） |
| ②-A | **波次2-A：task-2 交付（PR #827）6 项判据** | ✅ **通过（实质）**；F1/F2/F4 返修后**已复验通过**（§8.3，@`687f9758656f`），**U8/U9 闭环** |
| ②-B | **波次2-B：task-1 交付（PR #828）11 项判据** | **9 ✅ + 1 ⚠️（`if-no-files-found: warn` 偏离写法，U13）+ 1 ⏳（artifact 时间性未决，§9.1）** |
| **阶段0-① 综合判定** | — | **通过（实质口径）**，附 3 项登记待 CTO 处置（非阻塞，其中 1 项为判据文本自相矛盾） |

> 「通过（实质口径）」不豁免字面差异：判据① 的字面未满足、判据⑤ 的声称未复现均已如实登记，见 §7。

---

## 1. 判据① -1／① -2：写入目标的「字面 vs 实质」

**判据（派单 `:75` 原文）**：*「hook 内 `bypass.log` 字样消失；改为 `gate-soft-warnings.log`」*

### 1.1 原始命令与原始输出

```console
$ grep -n 'bypass\.log' /Users/wane/SynovaAgent/.git/hooks/pre-commit
9:  # D508/Win#10: 软门禁噪声移出 bypass.log（证据链只记真实提交/绕过；软告警独立日志——
10:  #   否则每次 commit 污染 bypass.log → 下次操作前必 checkout 清理，实测 10+ 次）
rc=0

$ grep -n 'gate-soft-warnings' /Users/wane/SynovaAgent/.git/hooks/pre-commit
11:  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"

$ grep -nE '>>?[[:space:]]*"\$ROOT/' /Users/wane/SynovaAgent/.git/hooks/pre-commit
7:  echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) exit=$EXIT_CODE branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/pre-commit-failures.log"
11:  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"
15:echo "$(git rev-parse HEAD 2>/dev/null || true)|$(date +%s)" > "$ROOT/.claude/last-precommit-success"
```

**行级定性（逐行判「注释行 vs 可执行行」）**

```console
line 9  -> 注释行 : # D508/Win#10: 软门禁噪声移出 bypass.log（…
line 10 -> 注释行 : #   否则每次 commit 污染 bypass.log → …
```

### 1.2 结论

- **实质**：hook 的**可执行写入目标共 3 个**——`pre-commit-failures.log`（行 7）、`gate-soft-warnings.log`（行 11）、`last-precommit-success`（行 15）。**没有任何一行把数据写进 `bypass.log`**。✅
- **字面**：`grep 'bypass.log'` **仍有 2 处命中**（行 9、10，均为注释），即派单「字样消失」**字面不成立**。⚠️
- 两处注释是**解释性注记**（记录 D508/Win#10 为何迁移），保留它们符合「哪条防线因何存在」的可读性要求；删除它们才是信息损失。
- **判据文本自相矛盾（须 CTO 澄清）**：同一行判据的第三个分句要求「hook 与生成器**同哈希**」，而生成器 `scripts/install-hooks.sh:51-52` 的 body 里就包含这 2 行注释；阶段0 又明令 `scripts/install-hooks.sh`（**执行，不修改**）。⇒ 三者不可同时满足。判据① 应以**实质口径**（写入目标不含 bypass.log）计，本报告据此判**通过**，并把文本矛盾登记为 U3（§7）。

---

## 2. 判据① -3：hook 与生成器同哈希（自行提取，不采信转述）

### 2.1 方法 A — 机械提取生成器 body（单引号语义解析）

```console
$ python3  # 从 scripts/install-hooks.sh 的 install_hook() 中提取 pre-commit 分支 body=' … '
==== extracted body (repr of first/last 60 chars) ====
first: '#!/bin/bash\n# v4.8.x 方案1(挪CI): pre-commit 软提示 — 本地门禁失败不阻断，CI'
last : '(date +%s)" > "$ROOT/.claude/last-precommit-success"\nexit 0\n'
extracted bytes: 1209
extracted sha256: 609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401

$ shasum -a 256 /Users/wane/SynovaAgent/.git/hooks/pre-commit
609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401  /Users/wane/SynovaAgent/.git/hooks/pre-commit

$ diff /private/tmp/extracted-precommit-body /Users/wane/SynovaAgent/.git/hooks/pre-commit
DIFF: IDENTICAL
```

### 2.2 方法 B — 可执行 oracle（在隔离夹具里真跑生成器）

在 `/private/tmp/synova-gen-fixture`（`git init` + 4 个最小 entry 夹具）执行**未改动的** `scripts/install-hooks.sh`，再比对它生成的文件：

```console
$ bash scripts/install-hooks.sh      # 在隔离夹具内执行
  ✅ pre-commit / ✅ commit-msg / ✅ pre-push / ✅ post-commit
$ shasum -a 256 .git/hooks/pre-commit
609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401  .git/hooks/pre-commit
$ diff .git/hooks/pre-commit /Users/wane/SynovaAgent/.git/hooks/pre-commit
ORACLE-DIFF: IDENTICAL
```

**附带发现（超出判据范围的加验）**：本次重装是**全量**而非仅 pre-commit——4 个 hook 全部与生成器一致：

```console
  pre-commit   生成=609788b1ac3bfb31 安装=609788b1ac3bfb31  MATCH
  commit-msg   生成=a6361b1fe873c440 安装=a6361b1fe873c440  MATCH
  pre-push     生成=08e6c8ceb7df2846 安装=08e6c8ceb7df2846  MATCH
  post-commit  生成=e6265838fa8a0c75 安装=e6265838fa8a0c75  MATCH
```

### 2.3 结论

**通过。** 两个哈希：安装件 `609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401`（1209 B）；方法 A 提取 body 与方法 B 夹具生成物**均同哈希、字节级一致**。

**旧件溯源（独立核，不采信 `pre-commit.old` 文件本身）**：

```console
$ for sha in $(git log --format='%h' -12 -- scripts/install-hooks.sh); do …提取该历史版 body 的 sha256…; done
  ab05e2de 2026-08-21  write-bypass.log=1  GATE_FAIL_SOFT=1
  4982f978 2026-08-23  write-bypass.log=0  GATE_FAIL_SOFT=1
  …
  ab05e2de: 历史版body sha256=4867f38c43d44ac9 len=977
  ab05e2de: 备份件    sha256=4867f38c43d44ac9 len=977  → MATCH
```

⇒ 备份件 `/private/tmp/cto-stage0-hook-backup/pre-commit.old` **逐字节等于** `ab05e2de`（D467「方案1挪CI」，2026-08-21）所生成的版本，即「写 bypass.log」的那一版；`4982f978`（D508，2026-08-23）起改为 `gate-soft-warnings.log`。溯源成立。

---

## 3. 判据① -4：反例（改坏即红）

**方法**：同一夹具 `src/__stage0_verify_asany_fixture__.ts`（含 `as any` ⇒ 铁律 38 组 1 `hard_check`），在 scratch worktree 内**先复位再分别跑**旧 hook / 新 hook，比较两份日志的行数 delta。脚本：`/private/tmp/stage0-verify/repro-hook-softfail.sh`。

### 3.1 同一条红灯（两次运行完全一致的门禁判决）

```console
    ❌ as any / as never / as unknown as 零容忍（新增，铁律 38；存量独立清理）: 1 处  [硬阻断]
       +export const stage0VerifyFixture = { shape: "x" } as any;
    ❌ 4 组未通过 — 提交已拒绝
    ⚠️ 本地门禁未通过（exit=1）— 已放行，CI 将作为权威判定（merge 前必须绿）
```

（RUN A / RUN B 的组 1 命中行、硬阻断组数 4、hook 侧 `exit=1` **逐字相同** ⇒ 差异只来自 hook 版本，非夹具漂移。）

### 3.2 RUN A：旧 hook（`pre-commit.old`）

```console
[BEFORE-A] bypass.log=1825行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=MISSING
  hook EXIT=0
[AFTER-A ] bypass.log=1826行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=1
--- 旧hook 本次新追加的行（bypass.log 尾行）---
2026-09-26T01:13:57+08:00 | GATE_FAIL_SOFT | exit=1 | branch=
--- gate-soft-warnings.log 运行后是否出现 ---
ls: .claude/gate-soft-warnings.log: No such file or directory
```

**delta：`bypass.log` +1（写入的是 `GATE_FAIL_SOFT` 行）；`gate-soft-warnings.log` ±0（未创建）。**

### 3.3 RUN B：新 hook（当前安装件）

```console
[BEFORE-B] bypass.log=1825行  gate-soft-warnings.log=MISSING  pre-commit-failures.log=1
  hook EXIT=0
[AFTER-B ] bypass.log=1825行  gate-soft-warnings.log=1        pre-commit-failures.log=2
--- 新hook 写入 gate-soft-warnings.log 的原文 ---
2026-09-26T01:14:01+08:00 | GATE_FAIL_SOFT | exit=1 | branch=
--- bypass.log 是否被新hook 追加（尾1行）---
2026-09-26T00:14:19+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=3ccc6c6e4745e7ab568dc0411ccc06e9374068f4
```

**delta：`bypass.log` +0（尾行仍是提交登记的 COMMITTED 行，非本次写入）；`gate-soft-warnings.log` +1。**
（`pre-commit-failures.log` 两次均 +1，符合设计：门禁失败先落该日志。）

### 3.4 结论与清理

**反例成立（通过）**：同一红灯下，旧 hook **只**落 `bypass.log`；新 hook **只**落 `gate-soft-warnings.log`；两者对 `bypass.log` 的行为**互斥且可复现**。

**夹具清理（已执行）**：

```console
$ git status --short          # 清理后（片段内无输出）
  fixture removed: YES
```

- scratch worktree `/private/tmp/synova-wt-stage0-redtest` 内 `src/__stage0_verify_asany_fixture__.ts` 已删、暂存已复位、`bypass.log` 已 `git checkout --` 复位、`gate-soft-warnings.log` 已删；**主工作树 `/Users/wane/SynovaAgent` 未做任何 git 写操作**（全程只读）。
- 复跑瑕疵（自曝，不影响判据）：脚本首段的 `cat -n` 写在 `reset_fixture` 之前，故首屏打印 `No such file`；夹具内容以两次 RUN 的 `+export const … as any;` 命中行为准。

---

## 4. 判据① -5：声称核验——「旧 hook ⇒ **当日自阻断**」**未复现**

### 4.1 被核声称（原文）

- 蓝图 `:190`：*「旧版 hook 把软失败写进 `.claude/bypass.log`；而 `pre-commit-check.sh:239-256` 会因 `bypass.log` 里的**当日** `detected-bypass` 记录**硬阻断**（`exit 1`）→ **旧 hook 会自己把自己锁死**」*
- 蓝图 `:664`：*「旧版把软失败写进 `.claude/bypass.log` → 触发 `pre-commit-check.sh:239-256` 的当日硬阻断 = 自己把自己锁死」*
- 工程派单 `:75`：*「解自锁：旧 hook 把软失败写进 `bypass.log` ⇒ 当日自阻断」*

### 4.2 四个面的逐个核验（含 file:line）

| 面 | 证据（file:line） | 判定 |
|---|---|---|
| pre-commit GATEKEEPER | `pre-commit-check.sh:239` `[ "${GITHUB_ACTIONS:-}" != "true" ]`；`:242` `grep -c "${TODAY}.*detected-bypass"`；`:254` `exit 1` | 只匹配 **`detected-bypass`**。旧 hook 写的是 **`GATE_FAIL_SOFT`** ⇒ **不匹配、不阻断** |
| pre-commit 绕过审计 | `:1085`（`detected-bypass`）／`:1086`（`possible-bypass`）／`:1092-1101`（≥3 ⇒ **`SOFT_COUNT`**，非 exit） | 同上不匹配；即便命中也是**软提示**，非硬阻断 |
| pre-push 门禁 7 ＋ 对账脚本 | `pre-push-check.sh:420-444` → `check-bypass-log.sh:102` `grep -q "$h"`（按 **commit hash** 找记录） | `GATE_FAIL_SOFT` 行**无任何哈希** ⇒ 既不能"补上"记录、也**不可能造成缺记录**；多余行只会**增加**命中概率，结构上无法致阻断 |
| `.claude/settings.json` PreToolUse | hooks 实配 = `scripts/workflow/hook-block-write.sh` ＋ `scripts/hooks/hook-git-detect.sh`（读 bypass 者：**0**）；`hook-bypass-block.sh` **全仓不存在**（`find` 零结果） | 无 bypass 读取面 |
| （附加）CI 侧 | `.github/workflows/ci.yml` 全文 grep `bypass`／`detected-bypass` **零命中**；`:84-87` Iron Laws 以 `SYNO_CI: "1"` 跑 `pre-commit-check.sh` | CI 不消费 bypass.log |

### 4.3 决定性原始输出（重跑判据点 ＋ 注入红队沙箱）

```console
# ① 真实日志（主树）：101 条 GATE_FAIL_SOFT，今日 0 条
$ wc -l .claude/bypass.log                        → 1813
$ grep -c 'GATE_FAIL_SOFT' .claude/bypass.log     → 101
$ grep -c '2026-09-26.*GATE_FAIL_SOFT' …          → 0
  GATE_FAIL_SOFT 日期分布: 08-24:1  08-25:19  08-27:5  08-28:7  09-04:1  09-08:2
                            09-09:3  09-10:15  09-22:4  09-23:1  09-24:3  09-25:33

# ② 逐字重跑 :242 的判据 grep —— 结果 0（= 不阻断）
$ grep -cE "$(date +%Y-%m-%d).*detected-bypass" .claude/bypass.log   → 0
$ grep -cE "$(date +%Y-%m-%d).*possible-bypass" .claude/bypass.log   → 0

# ③ 红队注入：向夹具工作树的 bypass.log 追加 8 条「今日」GATE_FAIL_SOFT（累计 109 条）后，
#    真跑 pre-commit-check.sh（无夹具暂存，只看 GATEKEEPER / 绕过审计段）
  注入后今日 GATE_FAIL_SOFT 条数: 109
  注入后 grep -cE "$TODAY.*detected-bypass": 0
  pre-commit-check.sh EXIT=0
    68:  ✅ 绕过审计
    ✅ 全部 13 组通过
```

⇒ **即使把"当日 GATE_FAIL_SOFT 污染"造到 8 条，本地 pre-commit 仍 `exit 0`、GATEKEEPER 段根本不打印「检测到今日 N 次 --no-verify 绕过记录」。声称的主机制未复现。**

### 4.4 唯一真实可熔断路径（另立，非旧 hook 所致）——加固证据（队长 2026-09-26 要求）

核验中发现**唯一**会把"当日 bypass.log 行数"变成 `exit 1` 的代码路径：`scripts/workflow/loop-context.sh`。

**(a) 源码原文片段**（`sed -n '84,100p' scripts/workflow/loop-context.sh`，左侧为文件真实行号）：

```console
84:  # 4. --no-verify 绕过检查
85:  BYPASS_LOG="$ROOT/.claude/bypass.log"
86:  if [ -f "$BYPASS_LOG" ]; then
87:    TODAY=$(date +%Y-%m-%d)
88:    BYPASS_COUNT=$(grep -c "$TODAY" "$BYPASS_LOG" 2>/dev/null | tr -d '\r' || echo 0)   # ← 未过滤 detected-bypass
89:    BYPASS_COUNT=${BYPASS_COUNT//[^0-9]/}
90:    [ -z "$BYPASS_COUNT" ] && BYPASS_COUNT=0
91:    if [ "${BYPASS_COUNT:-0}" -ge 3 ]; then
92:      echo -e "  ${RED}熔断: 今日 --no-verify ${BYPASS_COUNT} 次 (阈值 3)${RESET}"
93:      echo "    见 pre-commit 第 7 组检查。请修复根因而非绕过。"
94:      BREAKER_TRIPPED=1
95:    elif [ "${BYPASS_COUNT:-0}" -ge 2 ]; then
96:      echo -e "  ${YELLOW}警告: 今日 --no-verify ${BYPASS_COUNT} 次${RESET}"
97:    else
98:      echo -e "  ${GREEN}绕过检查: ${BYPASS_COUNT} 次 (正常)${RESET}"

$ sed -n '102,111p' scripts/workflow/loop-context.sh     # exit 1 路径
102:  # 结果
104:  if [ "$ONLY_CHECK" = true ]; then
105:    if [ "$BREAKER_TRIPPED" -gt 0 ]; then
106:      echo -e "${RED}熔断触发 — 需要人工介入${RESET}"
107:      exit 1
109:      echo -e "${GREEN}系统正常 — 无需熔断${RESET}"
110:      exit 0
```

**(b) 四态受控实验**（同一工作树，只替换 `bypass.log` 内容为 3 行；其余不变）：

```console
CASE C  3× COMMITTED（今日，正常提交登记）:  EXIT=1  熔断: 今日 --no-verify 3 次 (阈值 3)
CASE D  3× GATE_FAIL_SOFT（今日）:            EXIT=1  熔断: 今日 --no-verify 3 次 (阈值 3)
CASE E  3× GATE_FAIL_SOFT（昨日）:            EXIT=0  绕过检查: 00 次 (正常) / 系统正常 — 无需熔断
CASE F  3× detected-bypass（今日）:           EXIT=1  熔断: 今日 --no-verify 3 次 (阈值 3)

# CASE C 的 bypass.log 原文（无任何绕过语义，全是正常提交登记）
2026-09-26T10:01:00+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=deadbeef0001
2026-09-26T10:02:00+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=deadbeef0002
2026-09-26T10:03:00+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=deadbeef0003
```

**CASE C 是关键**：**完全没有 `GATE_FAIL_SOFT`、没有任何绕过**，仅 3 条正常提交登记 → 仍 `exit 1`。⇒ 该熔断与旧 hook **无因果**，是 `:88` 口径过宽（`grep -c "$TODAY"` 不过滤信号类型）造成的**误报熔断**。

**(c) "自动化调用点 = 0" 的搜索范围与命令**：

```console
$ grep -rn 'loop-context' package.json .github/workflows/ scripts/pre-commit-check.sh \
      scripts/pre-push-check.sh scripts/commit-msg-check.sh .claude/settings.json
rc=1                      # 零命中 —— 未接入质量/提交/推送/CI/预设 任一路径

$ grep -rIn 'loop-context' --include='*.sh' --include='*.json' --include='*.yml' \
      --include='*.yaml' --include='*.toml' --include='*.ts' . \
    | grep -v node_modules | grep -v '^./.git/' | grep -v '^./docs/' | grep -v '^./.claude/task-briefs/'
./scripts/workflow/loop-score.sh:11   # 注释里的名字列举
./scripts/workflow/loop-score.sh:75   # 仅 [ -f … ] 存在性检查（4 分评分项）
./scripts/workflow/loop-score.sh:115  # 缺失提示
./scripts/workflow/loop-context.sh:12 # 自身 usage 注释（bash … [--check] 手工调用）

$ grep -rn 'loop-context' Makefile .dsh/ dsh/      # 定时任务/预设入口
（无输出）
```

⇒ 除"人工显式执行 `bash scripts/workflow/loop-context.sh --check`"外，**无任何自动化触发器**；`loop-score.sh` 只检查文件**存在性**，从不执行它。

**(d) 附带发现（同一函数内的漂移）**：`:88` 用 `tr -d '\r'`（同文件 `:70` 同款），而 V4.5.1 已在 `pre-commit-check.sh` 把同类写法修为 `tr -d '\n\r'`（该文件 10 处）。漏 `\n` ⇒ `grep -c` 无匹配时 `"0"` ＋ `|| echo 0` 的第二行叠加成 **`00`**（CASE E 实测输出 `绕过检查: 00 次`）。`00` 与数值比较结果正确（=0），故**非阻断性缺陷**，但属同源写法漂移，登记为 U2 附注。

**(e) 已知未修**：该缺陷已被登记于 `docs/synova/coordination/UPGRADE-SPEC-控制塔与审计流程-20260817.md:60` 与 `:101`（U1d：「`loop-context.sh:88` `grep -c "$TODAY"` 未过滤 `detected-bypass`……是 GATEKEEPER 误报熔断源之一」），至本断面**未落地**。

**结论（供阶段5「Gatekeeper FP 熔断」格使用）**：该路径是**真实存在但未接线**的误报熔断源——它会把"一天内 3 次正常提交"判成"3 次 `--no-verify`"。当前不可达（无自动化调用点），一旦有人在阶段 1/5 把它接进提交/推送路径，将立即制造全队误熔断。**修法方向 = 口径对齐 `pre-commit-check.sh:1085`（`grep -cE "…detected-bypass"` ＋ `tr -d '\n\r'`），而非删除熔断**；本自验员不代改（属作者产物 ＋ 阶段0–4 冻结新增门禁）。

**⇒ 判据① -5 结论：`❌ 声称未复现`（就该声称在「被点名的四个面」上的机制而言）。重装 hook 的真实收益是**证据链卫生**（`bypass.log` 是 git 跟踪的**证据**日志：101 条软噪声会污染 D331 对账面／Gatekeeper 计数面的可读性；D508 注释记为 Win 实测 10+ 次 checkout 清理），**而非**"解除当日硬阻断"。声称的措辞需更正（登记 U1，§7）。**

---

## 5. 判据① -6：`core.hooksPath` 绝对路径影响面 ＋ 语义未变

### 5.1 影响面：绝对路径 ⇒ 影响**所有** worktree

```console
$ git config --show-origin --get core.hooksPath
file:/Users/wane/SynovaAgent/.git/config	/Users/wane/SynovaAgent/.git/hooks
$ 是否绝对路径: YES
$ git -C /private/tmp/synova-wt-squad-verify      config --get core.hooksPath
/Users/wane/SynovaAgent/.git/hooks
$ git -C /private/tmp/synova-wt-stage0-redtest    config --get core.hooksPath
/Users/wane/SynovaAgent/.git/hooks
$ git rev-parse --git-common-dir
/Users/wane/SynovaAgent/.git
```

⇒ 配置在 **common config** 上，取值是**主树 .git 下的绝对路径**；任何 linked worktree（含 `git rev-parse --show-toplevel` 不同的树）都加载**同一份** hook 文件。本次重装对全部 worktree **同时生效**（含本任务之外的其他 session 工作树）。

```console
$ stat -f '%Sm %N' …
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/pre-commit
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/commit-msg
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/pre-push
2026-09-26 01:11:13  /Users/wane/SynovaAgent/.git/hooks/post-commit
2026-08-28 21:05:37  /Users/wane/SynovaAgent/scripts/install-hooks.sh
```

（旧件 mtime `Aug 21 17:14` → 现件 `Sep 26 01:11`；生成器未改，仍为 `Aug 28 21:05`。前者与蓝图 `:189` 的「落后 7 天」观察一致。）

### 5.2 语义未变：旧↔新全文 diff（唯一语义差异 = 日志落点）

```diff
--- /private/tmp/cto-stage0-hook-backup/pre-commit.old	2026-08-21 17:14:00
+++ /Users/wane/SynovaAgent/.git/hooks/pre-commit	2026-09-26 01:11:13
@@ -6,7 +6,9 @@
 if [ $EXIT_CODE -ne 0 ]; then
   echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) exit=$EXIT_CODE branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/pre-commit-failures.log"
   # 软提示: 记录门禁失败但放行（K3 审计证据），CI 权威判定
-  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/bypass.log"
+  # D508/Win#10: 软门禁噪声移出 bypass.log（证据链只记真实提交/绕过；软告警独立日志——
+  #   否则每次 commit 污染 bypass.log → 下次操作前必 checkout 清理，实测 10+ 次）
+  echo "$(date -Iseconds) | GATE_FAIL_SOFT | exit=$EXIT_CODE | branch=$(git branch --show-current 2>/dev/null || echo unknown)" >> "$ROOT/.claude/gate-soft-warnings.log"
   echo "⚠️ 本地门禁未通过（exit=$EXIT_CODE）— 已放行，CI 将作为权威判定（merge 前必须绿）" >&2
 fi
 # 无论成败都写 marker（失败但放行 = 经过了 pre-commit，非 --no-verify）
```

**语义对照（新旧一致项）**：

| 语义 | 旧 hook | 新 hook | 证据 |
|---|---|---|---|
| 本地门禁失败是否阻断 `git commit` | **否**（软提示） | **否**（同一 `exit 0`） | 两文件末行均为 `exit 0`；§3.2/§3.3 两次 run `hook EXIT=0` 且"已放行" |
| 成功标记 `last-precommit-success`（`--no-verify` 检测依赖） | 写 | 写 | 行 13 / 行 15 逐字一致 |
| 失败进 `pre-commit-failures.log` | 写 | 写 | §3 delta 两次均 +1 |
| 硬阻断权威在哪 | CI | CI | `ci.yml:84-87`（`SYNO_CI: "1"` 跑同一 `pre-commit-check.sh`） |

### 5.3 结论

**通过。** 重装**不改变 hook 语义**：仍是"本地软提示 ＋ CI 权威"，唯一差异是软失败日志落点由 `bypass.log` 改为 `gate-soft-warnings.log`；且因 `core.hooksPath` 为绝对路径，该变更**已对所有 worktree 生效**（这正是"重装一次、全树生效"的机制，也意味着**没有"局部未重装"的中间态**——§2.2 四个 hook 全 MATCH 与之互证）。

---

## 6. 波次2（待交付）＋ 6.1 预备：C16 独立基线（自验员自己的数字）

### 6.1 预备：C16 口径矩阵与 detached 复核（**独立重跑，不采信 task-2 数字**）

在等 task-1／task-2 交付期间，自验员先自行建立基线，供交付后逐条对账。命令与原始产物：`/private/tmp/stage0-verify/c16-independent.sh` → 行级原始输出 `/private/tmp/stage0-verify/c16-independent.txt`、`/tmp/c16-rows.txt`、`/tmp/c16-scopeA.txt`、`/tmp/c16-scopeE.txt`、`/tmp/det-cls.txt`；**origin/main = `ee721b0abf52`（断面）**；cutoff = `2026-09-26T01:20~01:22+08:00`。

| 口径 | 定义 | 含未合并的单元数 | 未合并提交合计 |
|---|---|---|---|
| **A** | `origin/*` 分支（＝ 派单附录命令原样） | 447 分支 | **3,402** |
| **B** | **工作树 HEAD 求和**（`git worktree list --porcelain` 逐树 `rev-list --count origin/main..<HEAD>`） | **226 工作树** | **2,266** |
| **C** | 工作树 HEAD **唯一并集**（`git rev-list --count <唯一HEADs> --not origin/main`） | 274 唯一 HEAD | **1,388** |
| **D** | detached 工作树中**不在任何远端分支**上的部分（蓝图分支口径结构性漏计） | 2 工作树 | **14** |
| **E** | 全部本地分支（含 origin 无同名的本地独有分支） | 350 分支（其中本地独有 116 个 / >0 的 84 个） | 3,138（本地独有部分 **736**） |

工作树总量：`git worktree list --porcelain | grep -c '^worktree '` = **321**（唯一路径 321）；其中 **detached = 103**（占 32%），detached 且 >0 = **65**。

**独立复核队长交办的"detached 是否被漏计"**：

- **按"提交"计：影响小且可量化。** 87 个唯一 detached HEAD 中，**85 个的 HEAD 同时位于某条远端分支上**（`git branch -r --contains <sha>` 非空）⇒ 蓝图的分支遍历**仍能间接覆盖**它们的提交；只有 **2 个 HEAD（14 提交）不在任何远端分支上**，是真漏计：
  ```console
  MISS  8  b7d67305d9cfba5ce058dd8721b3df480ade1973  /private/tmp/w696m
  MISS  6  51b9063a625850312dc1642dd79592e325952ce6  /Users/wane/SynovaAgent/.synova-wt-decl
  ```
  （占口径 B 的 2,266 提交中的 **0.62%** ⇒ 提交维度上"漏计"不是数量级问题。）
- **按"工作树"计：影响是数量级的。** 103/321 工作树是 detached，其中 65 个含未合并提交 ⇒ 任何**遍历 `origin/<branch>` 的工作树普查会把它们全部跳过**（226 个"含未合并提交的工作树"里最多 **65 个（29%）** 只存在于 detached 维度）。**结论：detached 漏计在"工作树数"口径上成立且显著，在"提交数"口径上成立但量小（14 提交）——两个口径必须分别声明，不可混用一句"漏计"。**

**对账冻结值的结论（重要）**：派单冻结值「**149 个工作树 / 1,415 提交 / top = `tmp-d942` 110**」在**当前断面（`ee721b0a`）的任何单一口径下都无法复现**：

- 口径 B（工作树）当前 = 226 / 2,266；口径 A（origin 分支）= 447 / 3,402；
- 更关键的是**结构性矛盾**：`tmp-d942`（110 提交，冻结值的 top）**是本地独有分支**（`refs/remotes/origin/tmp-d942` 不存在），而**派单附录给出的那条"积压总量（C16）"命令正是遍历 `refs/remotes/origin`** ⇒ **该命令永远看不到 `tmp-d942`**（口径 A 的 top 5 里确无 `tmp-d942`，其 top 为 `feat/desktop-phase0-auth` 186）。因此冻结值的 top 与冻结值的测量命令**出自不同口径**，断面前进只能解释量的漂移、**解释不了这个结构性不一致**（U6）。

> **对 task-2 交付的最低要求**：分类表必须显式写明 ①用哪个口径（A/B/E）②是否含 detached ③cutoff 时刻 ④与冻结值对账时是"量差"还是"口径差"。自验员将用同一组命令逐条复核（§6.2）。

### 6.2 待验清单（交付后执行）

| # | 待验对象 | 判据 | 状态 |
|---|---|---|---|
| ② | task-1 日志归档 | `ci.yml`：`retention-days≥90`、artifact 名、`if-no-files-found: ignore`、`include-hidden-files: true`；**异机可复跑**须**真去下载 artifact 验证**（拿不到 ⇒ 判 **❌ 未通过**，不放水） | ⏳ 等待 `squad-ci` 交付 |
| ③ | task-2 C16 分类表 | 自抽 **≥5 个**工作树（**必含 ≥1 个 detached**）重算 `git rev-list --count origin/main..<branch/HEAD>` 与分类表逐条比对；核归类理由是否有可核证据（PR 号／patch-id／子树重合）；**独立复核"detached 漏计"结论**（§6.1 已备基线） | ⏳ 等待 `squad-analyst` 交付 |

---

## 7. C16 口径对账表（队长 2026-09-26 要求；含"冻结值属于哪个口径"裁决）

### 7.1 对账表（7 个口径，逐行：定义 + 复跑命令 + 数字 + as_of）

| # | 口径名 | 定义（一句话） | 复跑命令 | 数字 | as_of |
|---|---|---|---|---|---|
| **①** | **蓝图 §九.6 / 分析师口径A** | `git worktree list --porcelain` 中**含 `branch` 行**的 worktree，逐支 `rev-list --count origin/main..<branch>`；**detached 被 `if not b: continue` 跳过** | 蓝图 §九.6 python 片段（§10-⑨ 逐字） | **163 树 / 1,681 提交**（自验员复跑）<br>分析师：**158/1,546**（起点）→ **162/1,671**（附录末次） | 01:27:56（自验员）<br>01:13:03 / 01:22:19（分析师） |
| **②** | **派单 §十 命令** | 遍历 `refs/remotes/origin` **全部分支**（不看 worktree） | `for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin \| sed 's#^origin/##'); do git rev-list --count origin/main..origin/$b; done` | **447 分支 / 3,402 提交** | 01:20:38 |
| **③** | 工作树 HEAD 求和（含 detached） | 每个 worktree 的 HEAD（分支名 或 detached sha）各算一次 | `rev-list --count origin/main..<HEAD>` 逐树 | **226 树 / 2,266 提交** | 01:22 |
| **④** | 工作树 HEAD 唯一并集 | ③ 去重后的并集（真实唯一提交数） | `git rev-list --count <唯一HEADs> --not origin/main` | **1,388 提交**（274 唯一 HEAD） | 01:22 |
| **⑤** | 全部本地分支 | `refs/heads/*`（**含未挂 worktree 的本地分支**） | `for b in $(git for-each-ref refs/heads); do git rev-list --count origin/main..refs/heads/$b; done` | **350 分支 / 3,138 提交**；其中 **origin 无同名（本地独有）116 分支 / 736 提交** | 01:21:43 |
| **⑥** | detached 唯一漏计 | detached HEAD **不在任何 `origin/*` 分支上**者 | `git branch -r --contains <sha>`（空=漏计） | **2 树 / 14 提交**（占 ③ 的 0.62%） | 01:22 |
| **⑦** | 分析师口径B | ①＋detached（作者定义；与 ③ 同义，仅"是否计入判据"不同） | 同 ③ | **221/2,167**（JSON `metrics.lane_B`）<br>附录跨断面读数 **226/2,367** | 01:13:03 / 01:22 |

### 7.2 裁决：冻结值「149 / 1,415 / top=`tmp-d942` 110」属于**口径①**（队长初判**成立**）

**队长初判：冻结值来自蓝图 §九.6 那条遍历 `git worktree list` 的命令（复核报告原文为 `110 refs/heads/tmp-d942 (.synova-wt-cto-a)`），而非派单 §十 的 `refs/remotes/origin` 命令。三条原始证据——结论：初判成立，写死。**

1. **格式证据**：蓝图 §九.6 的 python 打印行为 `print(f'  {c:4d}  {b}')`，其中 `b = ln[7:]` ⇒ **带 `refs/heads/` 前缀**；复核报告逐字为 `110  refs/heads/tmp-d942` ＋ 人为补注 `(.synova-wt-cto-a)`（工作树路径）⇒ 正是 §九.6 的**工作树挂载分支**口径。派单 §十 命令打印的是 `"$c $b"`（`b` 为去掉 `origin/` 的**短名**，无 `refs/heads/` 前缀）。
2. **我的逐字复跑**：按 §九.6 逐字执行（§10-⑨）→ 顶部即 `110  refs/heads/tmp-d942`，与冻结 top **同形同值**。
3. **反证（口径②看不到 tmp-d942）**：同一断面用**派单 §十 命令**跑 ⇒ 447/3,402，**top = `feat/desktop-phase0-auth` 186**，列表内**无 `tmp-d942`** —— 因为 `refs/remotes/origin/tmp-d942` **不存在**（本地独有，属口径⑤）。

**⇒ 结论（写死）：「1,415 / 149」是口径① 的读数，可以用作 C16 起点口径的锚点；派单 §十 的附录命令与它不同源，不能用来复现/对账 1,415。**

### 7.3 C16 起点口径建议（只推荐，不擅自定）

- **推荐判据口径 = ①（蓝图 §九.6 = 分析师口径A）**：唯一与冻结值同源、可比、可复算，且分析师已把它定为 `metrics.lane_A`（JSON 机读）。
- **推荐起点值 = 分析师 `metrics.lane_A`：`158 工作树 / 1,546 提交 @ 2026-09-26T01:13:03+08:00`，断面 `ee721b0a`**（理由：该 as_of 同时生成了分类表与 JSON，单一快照同源；其余 5 次读数作为漂移附注）。曲线此后每次重跑必须带 **as_of + 断面 sha**（§4.3 已写死）。
- **口径②③④⑤ 作附注，不入判据**；其中 ⑤（本地独有 116 分支 / 736 提交）与 ⑥（detached 唯一 14 提交）是**两条独立的可见性缺口**，建议在阶段 1/2 并入"交付路径唯一化"的度量。
- ⚠️ **口径① 有结构性盲区**（分析师本次核心发现，自验员复核成立）：detached worktree 全部被跳过。

---

## 8. 波次2-A 自验结果（task-2 交付 · PR #827 @ `51f99889e7d4`；返修复验 @ `687f9758656f` §8.3）

被验对象：`docs/synova/coordination/阶段0-积压分类-20260926.md`（834 行 / PR 内 834 增）＋ `docs/synova/coordination/阶段0-C16基线-20260926.json`（8,598 增）。PR 改动面 3 文件 = 上述 2 文件 ＋ `.claude/bypass.log +2`（post-commit hook 自动登记，作者已披露）。**PR API：`additions 9434 / deletions 0`**。

| # | 判据（队长派活） | 原始命令 → 原始输出（摘要） | 结论 |
|---|---|---|---|
| 1 | 抽查 ≥5 工作树（跨在途/重复劳动/待定）重算 `rev-list` | 抽 **12 个**（在途 4 / 重复劳动 4 / 待定 4）：`tmp-d942` 110=110✓、`feat/d962-2a-merge` 71→**73**（+2，作者附录 01:22 top5 亦为 73 ⇒ 漂移）、`feat/p1-outbound-converge` 43=43✓、`971100c7…`(detached) 29=29✓、`4866b35e…` 31=31✓、`fix/d954-ci-reds-base-ref` 31=31✓、`c4206f9b…`(detached) 29=29✓、`gate/d839-win-chain` 21=21✓、`feat/d595-mcp-auth` 14=14✓、`feat/d512-gs-refresh` 12=12✓、`feat/d510-audit-remediation` 10=10✓、`fix/d516-ci-strict` 2=2✓ | ✅ **通过**（11/12 差 0；1 例 +2 已由作者自身的漂移登记解释） |
| 2 | 复核 detached 漏计：数总数 / 含未提交数 / 核 `63 个 / 621 提交` | ①**算式复核**：解析作者 §3.1 表 → **63 行 / 领先列求和 621** = 声称值，**逐字相符**；②**内部自洽**：`158+63=221`（= JSON `lane_B.worktrees`）、`1546+621=2167`（= JSON `lane_B.commits`）；③**蓝图命令会 skip detached**：`最终方案 v1.3 §九.6` python 片段 `if not b: continue`（无 `branch` 行即跳过）**逐字核实**；④**我的独立重跑**：detached=**103**/321 树，其中 >0 = **65**（合计 **652** 提交），与作者 101/63/621 的差异全部落在已登记的窗口漂移内 | ✅ **通过**（63/621 算式正确；结论成立） |
| 3 | 跨断面夹具：证"非硬编码" | 附录 A 逐字：`BASE=ee721b0a → 口径A 162/1673`、`BASE=ef299746 → 口径A 165/1888`（**两个断面两个数**）；**我独立重跑**（同脚本、同快照、仅换断面）：`ef299746 → 165/1,896`、`ee721b0a → 163/1,681` ⇒ **非硬编码成立** | ⚠️ **拆分判定**：「非硬编码」✅ 通过；**但 §2.2 归因表与 JSON 的数字与其逐字附录不一致 ⇒ 见 F1/F2，判 ❌ 不可核** |
| 4 | 三个指定问题 | ① `git ls-remote origin refs/heads/fix/d962-2a-iron-laws-wiring` → **空**（远端无）；`git rev-parse --verify refs/heads/…` → **不存在**；`.synova-wt-fix803` 现挂 `refs/heads/feat/d962-2b-iron-laws-wiring` ✓<br>② `tmp-d942` tip = **`b3182f672a5e`**；API `GET /pulls/738` → `state=open`、`head.ref=docs/d943-cto-a-items`、`head.sha=b3182f672a5e`、**`commits=110`** ✓<br>③ patch-id 真用：§5.1 判据写 `git patch-id --stable` 交集，JSON `commands.patch_id` 给出全文命令；**我独立复算**：`4866b35e` 28 个 patch-id ∩ `fix/d954-ci-reds-base-ref` 28 = **28**（作者：identical 28）；`c4206f9b` 26 ∩ `4866b35e` = **26**（作者：subset 26/28）✓ | ✅ **通过**（三条全部独立复现） |
| 5 | 口径核验：工作树总数是否被当判据 / 数字是否带 as_of+命令 | §0.4 与 §4.3 均写死「**工作树数不作判据**、未合并提交总数单调下降是唯一判据」；§1.2 逐时刻读数表、§4.1/4.2 命令+as_of、JSON `as_of`/`commands`/`exclusions`、附录 A 逐字 | ✅ **通过**（附提示：§5.2 表本身未就地标 as_of，需回溯 §1.2 第一行 / JSON.as_of） |
| 6 | 红线：零删除 / 零 prune / 零 ref 改动 | PR API `deletions: 0`；`git diff --name-only origin/main..origin/<branch>` = 3 文件，`scripts/**`、`src/**`、`scripts/audit/**`、`docs/synova/audit-reports/**` **零命中**；当前 worktree **320**（我 01:20 基线 321，同量级）、`prunable` 标记 0、reflog 无 prune/remove 痕迹；作者 §8.1-U10 **主动登记**窗口内 -1 树并声明未甄别 | ✅ **通过**（附限制：负断言无法独立归因"谁移除"，以作者披露 + 数量级一致为证） |

### 8.1 波次2-A 发现（只报告，不改作者产物）

| 编号 | 发现 | 证据 | 影响 |
|---|---|---|---|
| **F1** | **§2.2 归因表数字与它自己声明的证据（附录 A 逐字）不一致**：表内 `ef299746 → 165/**1843**`、`ee721b0a → 161/**1614**`；而附录 A 逐字为 `165/**1888**`、`162/**1673**`。据此推出的归因 `+428 / −229` 在原始输出中**无处可查**（按附录应为 `+473 / −215`） | 报告 §2.2（行 74-76）vs 附录 A（行 745/750） | **归因段落不可核**；「非硬编码」结论不受影响（附录两值本就不同）。**须作者改正为附录逐字值，或补各自的 as_of 与原始输出** |
| **F2** | **JSON `cross_section_fixture` 是第三组不一致的数**：`ef299746 lane_A 162/1731`、`ee721b0a lane_A 159/1552` —— 既非附录（165/1888、162/1673）亦非 §2.2（165/1843、161/1614）；该子块**无自带 as_of**（仅父级 01:13:03） | JSON `cross_section_fixture` | **不可核**；建议该块补 `as_of` + 命令，或与附录统一为一次读数 |
| **F3** | §8.1-U9 自曝：分类复跑脚本（`analyze.py`/`final_build.py`/`render.py`）**未入库**（在 `/private/tmp/squad-backlog-raw/`）⇒ 分类表"不能一键复跑"，只能按附录命令重实现 | 报告 §8.1-U9 | 不影响本次判据（附录含逐字命令与输出）；属阶段 1「定版一条命令」的输入 |

### 8.2 追加：PR #827 头部前进后的复核（`51f99889e7d4` → `778c02ba4b55`）

**快照纪律**：本报告 §8 的判定基于队长指定的 `51f99889e7d4`。随后作者又推了 4 个提交（`82dad734`、`a439c816`、`5186f88a`、`778c02ba`）。**`51f99889e7d4` 仍是新 head 的祖先 ⇒ 非 force push 改写**，原判定有效；以下为增量复核：

| 项 | 结果 |
|---|---|
| **F1/F2 是否已修正** | ❌ **未修正**：新 head 的 §2.2 仍为 `165/**1843**`／`161/**1614**`，JSON `cross_section_fixture` 仍为 `162/**1731**`／`159/**1552**`，与附录 A 逐字（`165/**1888**`／`162/**1673**`，**确认仍完整保留**，位于新 head 第 758-766 行）三者不一致 ⇒ **F1/F2 在最新 head 上依然成立** |
| **作者新增自捕更正**（新 head §6.3） | ✅ **抽查证实**：作者主动更正"17 行中 11 行有 closed-unmerged PR"为"**9 行实际已合并、仅 2 行已关未合**"。我抽 2 条走 API 复核：PR **#143** `merged=true / merged_at=2026-08-23T18:48:53Z / merge_commit=a856c154850f`（`merge-base --is-ancestor` → **在 origin/main 上** ✓）；PR **#354** `merged=true / merged_at=2026-09-05 / merge_commit=c124f8c8c847` → **在 origin/main 上** ✓ ⇒ 更正方向正确，且**主动登记不实声称**属正向行为 |
| **F4（新，轻微）** | 新 head 的 §8 子节**顺序错乱**：文件顺序为 `8.1 → 8.2 → 8.5 → 8.3 → 8.4`（§8.5 插在 §8.3/§8.4 之前） | 仅影响可读性/引用稳定性，不涉判据 |
| 新增 §8.5（回收三前置） | 与队长裁决 3 一致："本次不动、三条前置、需单独批准"；未放行任何删除 ⇒ 与红线不冲突 | ✅ 无需处置 |

**波次2-A 综合判定：通过（实质），附 F1/F2 两项须作者修正的不可核数字。** 按"有条件通过 = 未通过"纪律：F1/F2 不使核心判据失守（分类表口径、detached 发现、三指定问题、红线均已独立复现），但**§2.2 归因与 JSON 跨断面块在修正前不得被引用为证据**。

### 8.3 F1/F2/F4 返修复验（@ 新 head `687f9758656f`，自验员独立重跑）

**快照纪律**：作者返修提交 `ab52c5f1`（＋自动登记 `687f9758`）使 PR #827 head = **`687f9758656f`**（`changed_files 3 / +9847 / −0`）；`51f99889e7d4` **仍是祖先**（`merge-base --is-ancestor` 通过）⇒ 非 force push。以下四项**独立重跑，不采信转述**：

| # | 复验项（队长指定） | 原始命令 → 原始输出 | 结论 |
|---|---|---|---|
| ① | 旧归因值 `1,843` / `+428` / `−229` 是否 **0 命中** | `grep -c` on 报告 v1.3：`1,843`→**0**；`+428`→**0**；`−229`→**0**；`-229`→**0**。残余裸数字 `1843`(1 处)、`1614`(2 处) **全部位于附录 A.2 的历史读数块内**（L867 / L852 / L863），即"已标注 as_of 的逐字历史输出"，**不再作为归因数出现** | ✅ **通过**（归因值 0 命中） |
| ② | `sed -n '752p'` / `'764p'` / `'769p'` | L752 → `含未合并提交的工作树: 162 \| 未合并提交总数: 1671`；L764 → `口径A(分支型): worktrees=162 commits=1673`；L769 → `口径A(分支型): worktrees=165 commits=1888` | ✅ **通过**（三条逐一命中，与转述一致） |
| ③ | JSON：两组 fixture 各自 as_of／`readings_history` 5 条／`lane_A` 仍 158/1546 | `cross_section_fixture` 父级含 `note/command/as_of=01:22:19/same_run_note/reconciliation_note`；子块 `ef299746` = `as_of 01:22:19 / worktree_set_size 320 / detached 102 / lane_A 165,1888 / lane_B 231,2636 / evidence 附录 A L768–L770`；子块 `ee721b0a` = `as_of 01:22:19 / 320 / 102 / lane_A 162,1673 / lane_B 226,2367 / evidence 附录 A L763–L765`；`readings_history` = **list 5 条**（各带 `as_of`/`base`/`lane_A`/`worktree_set_size`/`evidence`）；`metrics.lane_A` = **158 / 1546** 未变 | ✅ **通过**（附注：`command` 为父级单条 `count_at_base.py <base>`，子块以 key 作 base ＋ `evidence` 指行号 ⇒ 可追溯，无歧义） |
| ④ | 附录 A.2 是否真存在且为逐字输出 | `### 附录 A.2 · 采集窗口内的历史读数（原样粘贴，各自带 as_of；该时点不可重放）` **位于 L812**；含 A.2.1（`count_at_base.py` 01:16:32，含 `BASE=… worktrees=316 detached=101`、`口径A(分支型)…`、`top5: [...]` 原文）、A.2.2（蓝图 §九.6 逐字复跑 01:18:47）、A.2.3（`repro.sh` 首跑 01:20:00，含 `$ git worktree list \| wc -l` → `321`）三段 **text 代码块原文**，并显式声明"§2.2 归因与 JSON 只采用附录 A（01:22:19）夹具块数字，不采用本节数字" | ✅ **通过**（真存在、确为逐字、且有采用边界声明） |
| ⑤ | F4：§8 子节顺序 | `grep -nE '^### 8\.'` → `8.1@675 → 8.2@690 → 8.3@696 → 8.4@704 → 8.5@712` | ✅ **通过**（顺序已修正，§8.5 在 L712） |
| ⑥ | 新归因算术自洽（加验） | `165−149=16` ✓；`162−165=−3` ✓；`1888−1415=473` ✓；`1673−1888=−215` ✓；`1546−1415=131` ✓ | ✅ **通过**（五项恒等式全对） |

**F1/F2/F4 复验结论：✅ 通过（F1 ✅ / F2 ✅ / F4 ✅）。** 另加验一项**正向**：作者把"同一次运行内蓝图块 1,671 vs 夹具块 1,673 的 2 提交秒级漂移"**主动登记**并写明两个消费者各取哪一个（§1.2 取 1,671；§2.2/JSON 取 1,673）——这正是"口径 + as_of 不混用"的做法。⇒ **U8/U9 由"待返修"改判为"已复验通过"**（见 §10）。

---

## 9. 波次2-B 自验结果（task-1 交付 · PR #828 @ `a16d839eb0a6`）

被验对象：`.github/workflows/ci.yml`（**+17/−0**）＋ `docs/synova/coordination/阶段0-日志归档-20260926.md`（+508）＋ 3 份快照 `docs/synova/coordination/gate-logs-20260926/*.log.txt`（4873/5758/156 行）＋ `task-state/D1009.json`（+19）＋ `.claude/bypass.log`（+4，hook 自动）。**PR API：`changed_files 7 / additions 11335 / deletions 0`**。

| # | 判据 | 原始命令 → 原始输出 | 结论 |
|---|---|---|---|
| 1 | `retention-days: 90` | `git show <branch>:.github/workflows/ci.yml` → `retention-days: 90` | ✅ 通过 |
| 2 | **`include-hidden-files: true`**（缺它=空 artifact 假绿） | 同上 → `include-hidden-files: true`；且两条 `path`（`.claude/gate-hits.log`、`.codex/control-tower/logs/degraded-events.log`）**均在 dot 目录内** ⇒ 该输入是**必要性**成立的（最终充分性 = 实下载 artifact 非空，见 §9.1） | ✅ 通过（写入面）；充分性待 §9.1 |
| 3 | `if-no-files-found` 取值 | 同上 → **`warn`**（任务书写法为 `ignore`；**队长裁决 U13：保持 `warn`，不改 `ignore`/`error`**） | **已裁决**：写法偏离但 CTO 接受。**注意后果被 G6 裁决加强**：该 step 加 `continue-on-error: true` 后，**「上传 0 文件」与「上传失败」都不再由 job 颜色暴露**（见 §9.3 裁决 1） |
| 4 | `if: always()` | 同上 → `if: always()`；且 step 位于 `quality` job（`name: TypeScript + Lint + Iron Laws`）**Iron laws check 之后**（ci.yml 步序：`82 Iron laws` → `91 Verify parallel` → `99 D708` → `110 Archive`）⇒ 同 job 内日志已生成、且前置 step 失败时仍会上传 | ✅ 通过 |
| 5 | **`ci.yml` 是否 0 删除行** | `git diff -U0 origin/main..<branch> -- .github/workflows/ci.yml \| grep -c '^-[^-]'` → **0**；`+ *- name:` 计数 → **1**（只加 1 个 step） | ✅ 通过 |
| 6 | 快照能否 `git ls-files` 列出 | 分支树 `git ls-tree -r --name-only` → 3/3 `TRACKED`（`gate-hits.log.txt`/`degraded-events.log.txt`/`pre-commit-failures.log.txt`）⇒ 蓝图 D1「`git ls-files` 能列出」**充分条件达成** | ✅ 通过 |
| 7 | 快照 sha256 **与主树取证现场是否一致**（作者声称逐字符一致，自算） | `git show <branch>:<snap> \| shasum -a 256` vs `shasum -a 256 <主树实况>`：`2a6a80ba467220da`=`2a6a80ba467220da`（gate-hits）／`30929d4ed4e99c2c`=`30929d4ed4e99c2c`（degraded-events）／`1148103f70ff3da6`=`1148103f70ff3da6`（pre-commit-failures） | ✅ 通过（3/3 **IDENTICAL**） |
| 8 | **真去下载 artifact** | 见 §9.1 | ⏳ 见 §9.1 |
| 9 | PR 描述 R2 六项 ＋ 两句必需声明 | body 检出：`卡号 ✓ 分支 ✓ sha ✓ 写集 ✓ 判据 ✓ 改坏即红 ✓ 红线 ✓ 未清项 ✓`；**「非门禁语义变更声明（R3）」有**（body 第 60 行，含"请 Mac-CTO 批准后合并"＋依据）；**「新增 1 条 CI 红路径」有**（第 59 行：`upload-artifact` 服务瞬时故障 ⇒ quality job 变红，并声明不加 `continue-on-error`） | ✅ 通过（**按交付时**的要求）；**但依 G6 裁决（§9.3 裁决 1），"新增 1 条 CI 红路径"这句话作废**，须由作者改为「新增 1 条**静默失败面（仅黄灯）**」 |
| 10 | 改动面 vs 写集声明一致性 | `task-state/D1009.json:write_set` = **list / 6 条**（ci.yml、归档文档、3 快照、D1009.json 自身）；实际 7 文件 = 6 声明 ＋ `.claude/bypass.log`（声明为 D708 builtin 豁免）⇒ **逐条对齐** | ✅ 通过 |
| 11 | 附加（自验员加验）：快照凭证自扫 | `check-secrets.sh` 的 `--include` 全清单**无 `*.txt`/`*.log`**（第 31-34 行）⇒ 快照**不入门禁扫描**（作者 E6 关注点成立）；我用门禁同款正则独立扫描 3 份快照：`sk-`=0、`cli_`=0、`ghp_`=0、`AKIA`=0 | ✅ 内容干净（门禁盲区仍在，见 U12） |

### 9.1 artifact 真下载（异机可复跑的唯一充分证据）

**判定：⏳ 时间性未决（time-blocked），不判通过。** 依据（自验员实测，非转述）：

```console
# 等待窗口 as_of 起点 2026-09-26T01:32:56+08:00；轮询脚本 /private/tmp/stage0-verify/wave2b-artifact.sh
# 原始等待日志 /private/tmp/stage0-verify/artifact-wait.log
[2026-09-26T01:32:56+08:00] iter=1  36167351846|queued|None|2026-09-25T17:28:34Z
[2026-09-26T01:33:57+08:00] iter=2  36167351846|queued|None|2026-09-25T17:28:34Z
# 仓库队列深度（同期采样）：queued 21→20，in_progress 7  ⇒ 外部阻塞，非本 PR 问题
# 该分支 run 数 = 1；无任何 completed run ⇒ 无 artifact 可下载
```

- **缺什么**：`run_id=36167351846` 需从 `queued` 进入 `completed` → 才可能产生 `gate-logs-*` artifact；未完成则"artifact 非空"这一充分条件**无法判定**。
- **已具备的前置证据**（§9 判据 2/4/5）：`include-hidden-files: true` 已写入；两条 path **均在 dot 目录**；step 位于 Iron laws check 之后且带 `if: always()` ⇒ 逻辑上具备产出条件。**但"应能产生" ≠ "已产生"**，本报告不以后者代替前者。
- **复跑方式**（窗口后任何人可一键判定）：`bash /private/tmp/stage0-verify/wave2b-artifact.sh`（含断点续传下载 + `unzip -t` 校验 + 逐文件 `wc -l`）；或 `GET /repos/tangbaobao520/SynovaAgent/actions/runs/<run_id>/artifacts`，`gate-logs-*` 的 `size_in_bytes>0` 且 `expires_at − created_at ≈ 90d`。
- **处置建议**：按队长口径 —— 未跑完写"时间性未决"，**不得写"通过"**；该 PR 的"异机可复跑"判据在 artifact 落地前应记 **⚠️ 未达**（作者本人 PR body 第 38 行亦声明"待 CI 证据回填…取不到即判 ❌ 并登记"）。
- **补充窗口（队列解堵后，有界 8 分钟）的最终读数**：主窗口收尾时 `run 36167351846` 由 `queued` 转入 **`in_progress`**，故另起有界补充窗口（脚本 `wave2b-artifact2.sh`，日志 `artifact-wait2.log`）；至 **`2026-09-26T01:59:57+08:00`（iter=16，窗口上限）该 run 仍 `in_progress`、另一 run `36168645768` 仍 `queued`** ⇒ **补充窗口亦告"时间性未决"**，未进入 `completed`、未产生 artifact。**两份轮询脚本均已退出（无后台残留）**，判定维持 ⏳。
- **对判定时点的诚实说明**：本报告签发时（`as_of=2026-09-26T02:00` 附近）CI **正在真跑**（`in_progress`）而非"卡死"；即该判据的落地只差时间，**但"未完成的证据"不等于"证据"** ⇒ 仍记 ⏳，由后续任一执行者用上条命令一键改判。

### 9.2 波次2-B 发现与观察

| 编号 | 事项 | 证据 | 影响 |
|---|---|---|---|
| **G1** | **`if-no-files-found: warn`（任务书写法为 `ignore`）**：两者**都不使 job 变红** ⇒ "上传 0 文件"在任一取值下都**不会由 job 颜色暴露**（`error` 才会）。作者自己在 M1 变异体中正是这么注记的（"缺该输入 = 上传 0 文件，而 `warn` 只出警告、job 照绿 ⇒ 判据假绿"） | ci.yml 该 step；PR body M1 段 | ⚠️ 需队长裁决：接受 `warn`（不失败＋更响亮）／要求 `ignore`（按任务书）／要求 `error`（fail-loud，与"宁可红不可假绿"一致） |
| **G2** | 该 step 的**行内注释**称"上传失败会让 job 可见变红（不静默）"——**上传失败**（服务故障）确会红，但**文件缺失**只警告 ⇒ 注释未区分两种情形 | ci.yml 注释 vs `if-no-files-found: warn` | 文档精度（轻微）；建议补"文件缺失=警告、上传失败=红" |
| **G3** | docs-only PR 情形：`Iron laws check` 有 `if: steps.docsonly…` 守卫会被跳过，而归档 step 是 `if: always()` ⇒ 仍会执行 → 无文件 → 警告、job 绿、无 artifact。即 **`≥90d` artifact 只在"门禁真跑"的运行上产生**（恰是需要归档的场景） | ci.yml 步序与两处 `if` | 可接受，登记备查 |
| **G4** | 快照落位**偏离任务书写法**（任务：`docs/synova/coordination/evidence/gate-logs-20260926/`；实际：`docs/synova/coordination/gate-logs-20260926/`，去掉了 `evidence/`）。作者在 PR ② 段**透明声明理由**（`evidence/` 被 `.gitignore:76` 整目录忽略且不匹配 `DOC_SCOPE_RE`），并登记未清项 | PR body 第 28 行 + ⑥-4 | ✅ 属"显式偏离＋理由＋登记"，非静默改路径；**但仍是与任务书写法不一致**，需队长确认接受 |
| **G5** | 3 快照命名加 `.txt` 后缀：**不使它们进入 `check-secrets.sh` 扫描**（include 清单无 `*.txt`）⇒ E6 的盲区未被该改名消除；本次由作者自扫 ＋ 我的独立自扫双重覆盖（0 命中） | §9 判据 11 | 登记为 U12：**门禁盲区仍在**——若未来快照常态化入库，应把扫描扩展到 `docs/**/*.log.txt` |
| **G6** | **⚠️「新增 1 条 CI 红路径」的影响面应升级描述：该 job 是 main 的必需检查。** 实测 `GET /repos/…/branches/main/protection` → `required_status_checks` 含 **`TypeScript + Lint + Iron Laws`**（新 step 所在 `quality` job 的名字，`app_id=15368`，`strict=false`）⇒ `upload-artifact` 服务故障会让**必需检查**变红 ⇒ **阻塞所有 PR 合并到 main**（不止"job 可见变红"） | API 原始响应（本条为自验员加验） | **Mac-CTO 批准时应把这一条纳入权衡**：作者已如实声明红路径且拒绝 `continue-on-error`（宁可红不可假绿），但影响面从"单 job 状态"升级为"**合并可用性**"——可考虑把归档 step 移入**非必需 job**（如 `control-tower-tests`）以保留 fail-loud 同时不阻塞合并 |
| **G7** | 作者引用的两条 `gitignore` 理由**均属实**：`.gitignore:8 = *.log`、`:76 = evidence/`；且 3 快照字节数与 PR 声称**逐一 MATCH**（766,989 / 2,431,750 / 9,801） | `.gitignore` 原文 + `git cat-file -s` | ✅ 无异议 |

---

### 9.3 队长裁决记录（2026-09-26）与随之产生的计数面判据

> **发现者归属（K3 抽验链路，队长要求原文保留）**：**G6 是「自验员（squad-verifier）」发现的，作者与队长均未预见。** 作者在 PR body 中把该变更声明为"新增 1 条 CI 红路径"（仅描述为 job 状态变化）；队长派活的判据清单亦只要求核对"是否声明该红路径"。**"该 job 是 main 必需检查 ⇒ 红路径 = 阻塞全队合并"这一升级，来源为自验员对 `branches/main/protection` 的独立取证**（§9.2-G6）。

**裁决 1（G6）**：给那**一个** upload step 加 `continue-on-error: true`（**job 不加、其它 step 不动**）。取舍 = **显式黄灯 ＋ 计数**，**不 fail-closed**——因 `quality` 是 main 必需检查，红会阻塞全队合并（对齐蓝图 §4.2-bis："不许降级即通过，但也不得制造阻塞"）。被否方案（不加 `continue-on-error`）须由作者与 PR 并列写清，请 Mac-CTO 二选一确认。
- **连带更正（写死）**：PR 中"**新增 1 条 CI 红路径**"这句话**作废**，改为"**新增 1 条静默失败面（仅黄灯）**"。
- **对判据 3 的加强**：`if-no-files-found: warn`（裁决 2 维持）＋ `continue-on-error: true` ⇒ **「上传 0 文件」与「上传失败」都不再由 job 颜色暴露** ⇒ "假绿"风险从"部分可见"变为"完全不可见"，**计数面因此成为唯一发现手段**（见下）。

**裁决 2（U13）**：保持 `if-no-files-found: warn`（不改 `ignore`／不改 `error`）。

**裁决 3**：**U14 接受**（作者落位偏离，理由经自验员独立核实：`.gitignore:8=*.log`、`:76=evidence/`）；**U12 接受但登记为未清项**（扩展 `check-secrets.sh` 的 `include` 清单 = **门禁语义变更 ⇒ 需 Mac-CTO 批准**）；**U15 可接受，登记备查**。

#### 9.3.1 【待补机制】artifact 计数面判据（**只写判据与命令草案，不新增脚本** — 蓝图 §七-1：新增校验只能进 `verify.sh` profile）

- **判据（写死）**：**每 N 次 main 的 `completed` CI 运行，应有 `gate-logs-*` artifact；缺失即计数 +1**，进**阶段 1 的 gov profile**。
- **阈值草案（三态）**：最近 **N=20** 次 main completed run 中，`gate-logs-*` 缺失 **0** 次 = 绿；缺失 **>0 且 ≤2** 次（≤10%）= **黄（显式黄灯 ＋ 计数）**；缺失 **>2** 次 = 红（阈值由阶段 1 profile 定版，本报告只给草案）。
- **命令草案（可复跑，无脚本）**：
  ```bash
  TOKEN=$(grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml | sed 's/.*GITHUB_TOKEN:[[:space:]]*//' | tr -d '\r\n')
  # ① 取最近 N=20 次 main completed run 的 id
  curl -sS -H "Authorization: token $TOKEN" \
    "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs?branch=main&status=completed&per_page=20" \
    | python3 -c "import json,sys;[print(r['id']) for r in json.load(sys.stdin)['workflow_runs']]"
  # ② 对每个 id 查 artifact 是否存在 name 以 gate-logs- 开头且 expired=false
  curl -sS -H "Authorization: token $TOKEN" \
    "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/<run_id>/artifacts" \
    | python3 -c "import json,sys;d=json.load(sys.stdin);print(sum(1 for a in d['artifacts'] if a['name'].startswith('gate-logs-')))"
  # ③ 计数：缺失次数 = N − 有 artifact 的 run 数；计数落点复用既有 .claude/gate-hits.log（log_gate hit/miss），不新建日志
  ```
- **生效前提（如实登记）**：该判据在 **#828 合并进 main 之后**才有意义——合并前的 main 运行使用的还是旧 `ci.yml`（无归档 step），此时计数会全为"缺失"，**不得据此判红**。首次有效窗口 = merge 后第 1 次 main completed run 起。
- **本自验员边界**：**未新增任何脚本/门禁**；本小节仅为"判据 + 命令草案"，落地属阶段 1 profile（需另批写集）。

### 9.4 裁决执行复核（PR #828 头部前进后，自验员增量核验）

**快照纪律**：§9 的 11 项判定基于队长指定的 `a16d839eb0a6`。随后作者推送 `bcea929e`（＋自动登记 `55fb95fa`），PR head = **`55fb95fabba6`**；`a16d839eb0a6` **仍是祖先**（`merge-base --is-ancestor` 通过）⇒ **非 force push 改写**，原判定有效。

| 裁决 | 执行情况（自验员独立核验） | 结论 |
|---|---|---|
| **裁决 1**：**只**给那一个 upload step 加 `continue-on-error: true`（job 不加、其它 step 不动） | `git show <head>:.github/workflows/ci.yml`：`continue-on-error` 共 3 处 = ① 第 105 行**注释内文字**；② **第 115 行 = 新归档 step 的 step 级设置**（位于 `if: always()` 与 `uses:` 之间）；③ 第 366 行 = **既存** job 级（`npm audit` job；`origin/main` 第 345 行同样存在 ⇒ **非本次新增**）。⇒ **"只给那一个 step、job 不加、其它 step 不动"逐条满足** | ✅ **执行到位** |
| 裁决 1 连带：`ci.yml` 删除行仍为 0 | `git diff -U0 origin/main..<head> -- .github/workflows/ci.yml \| grep -c '^-[^-]'` = **0** | ✅ |
| 裁决 1 连带：PR 措辞更正 | PR body 现含 **「静默失败面」✓、「continue-on-error」✓**，且**已不含「新增 1 条 CI 红路径」✓**（与该句作废一致） | ✅ |
| 作者文档同步登记 | `阶段0-日志归档-20260926.md`：§0 三态表（第 6/9 行）、§8.1「红线段：本改动**不新增任何红路径**；新增的是 1 条**静默失败面**」、断言 **A7**（`continue-on-error === true`，理由写明"本 job 名是 main 必需检查"）、变异体 **M7**（删 `continue-on-error` ⇒ A7 rc=1） | ✅ 与裁决一致 |
| 反向风险登记 | 作者 §8.1 如实登记"极端情形：归档坏了但黄灯没人看"，兜底 = 一键补验命令 ＋ 三态表把 artifact 行判 **⚠️ 时间性未决**（不自动升格通过） | ✅ 未淡化 |

⇒ **U16 视为已执行并复核通过**（本自验员在此确认，不改作者产物）。**新产生的 CI run = `36168645768`（推送 `bcea929e` 触发，仍 `queued`）** ⇒ artifact 判据的等待窗口顺延至该 run；本报告 §9.1 的 ⏳ 结论不变。

---

## 10. 未清项登记（本自验员只报告，不改作者产物）

| 编号 | 事项 | 证据 | 建议归属 |
|---|---|---|---|
| **U1** | **声称未复现**：蓝图 `:190`／`:664`、派单 `:75` 的「旧 hook ⇒ **当日自阻断**」在主机制上不成立（`GATE_FAIL_SOFT` ≠ `detected-bypass`，`pre-commit-check.sh:242` 只匹配后者；注入 8 条同日软失败后仍 `exit 0`）。重装 hook 的真实收益是**证据链卫生**，判据措辞需更正 | 本报告 §4.2／§4.3 | CTO 更正判据文本（蓝图 v1.4 ＋ 派单）；**不建议**为"自锁"再新增机制 |
| **U2** | **潜在熔断源未修（已加固证据）**：`scripts/workflow/loop-context.sh:88` 未过滤的 `grep -c "$TODAY"` ＋ `:91-94`／`:105-107`，四态受控实验实测 **CASE C（3 条正常 `COMMITTED` 登记）即 `exit 1`**、CASE D（3 条 `GATE_FAIL_SOFT`）`exit 1`、CASE E（昨日）`exit 0`；自动化调用点普查（`package.json`／`ci.yml`／`pre-commit-check.sh`／`pre-push-check.sh`／`commit-msg-check.sh`／`settings.json`）**rc=1 零命中**，`loop-score.sh` 仅做存在性检查 ⇒ **真实但当前不可达**。附注：`:70`/`:88` 用 `tr -d '\r'`（漏 `\n`）⇒ 输出 `00 次`（数值语义无害）。修法 = 对齐 `pre-commit-check.sh:1085` 口径 | §4.4 (a)–(e) | 阶段1／阶段5「Gatekeeper FP 熔断」格；本自验员不动作者产物、**不新增门禁**（阶段0–4 冻结） |
| **U3** | **判据文本自相矛盾**：判据① 的「hook 内 `bypass.log` 字样消失」与「hook 与生成器同哈希」不可同时满足（生成器 body 自带 2 行注释，且阶段0 明令 install-hooks.sh「执行，不修改」）。判据① 已按**实质口径**判通过 | 本报告 §1.2 | CTO 澄清判据措辞（改写为"写入目标不含 bypass.log"） |
| **U4** | **无关本判据的观察**：门禁红灯夹具运行在 detached HEAD，故 hook 落盘行 `branch=` 为空；`pre-commit-check.sh` 在 scratch worktree 无夹具暂存时仍报「13 组通过 ＋ 1 项软提示」（软提示来源与本次判据无关，未深挖） | §3.2／§4.3 | 仅登记，无需处置 |
| **U5** | **波次2 未启动正式判定**（依赖 task-1／task-2 交付）；预备基线已建（§6.1） | §6.2 | 待交付后补验并更新本报告 |
| **U6** | **派单冻结值与派单自带命令结构性不一致**：冻结「149 工作树 / 1,415 提交 / top = `tmp-d942` 110」，但派单 §十 的 C16 命令遍历 `refs/remotes/origin`，而 `tmp-d942` **无远端同名分支** ⇒ 该命令结构上**永远看不到**冻结值的 top；当前断面下口径 A = 447/3,402、口径 B = 226/2,266，**149/1,415 在任何单一口径下均不可复现**（断面前进只能解释量漂移，解释不了口径矛盾） | §6.1 | CTO：请 task-2 交付时**显式声明口径**（A/B/E ＋ 是否含 detached ＋ cutoff）；建议判据文本补"口径声明"为强制项 |
| **U7** | **detached 漏计的准确量级**（独立复核队长交办项）：按"提交"计漏计仅 **2 工作树 / 14 提交（0.62%）**（87 唯一 detached HEAD 中 85 个同时位于某远端分支上）；按"工作树"计漏计可达 **65/226（29%）** ⇒ 两个口径必须分开表述，不可用一句"detached 被漏计"混用。**对表分析师**：其 `+63 树 / +621 提交`（按工作树枚举）与我的 `2 树 / 14 提交`（按提交可达去重）**两者都对**，已并列写入 §6.1；其 63/621 算式经我逐行解析**精确复核通过** | §6.1、§8 | CTO／task-2：工作树普查须补 detached 维度 |
| **U8** | **F1（task-2 §2.2 归因不可核）** —— **已复验通过（关闭）**：作者 `ab52c5f1` 已改为附录 A 逐字值（`ef299746 → 165/1,888`、`ee721b0a → 162/1,673`）并按新数字重算分解（`+16/+473`、`−3/−215`，净 `+9/+131` 不变），每格标 as_of ＋ 命令 ＋ 附录行号。自验员独立复验：旧值 `1,843`/`+428`/`−229` **0 命中**；`sed -n '752p'/'764p'/'769p'` 命中 162/1671、162/1673、165/1888；五项算术恒等式全对 | §8.3 ①②⑥ | ✅ **已闭环**（原"待返修"撤销） |
| **U9** | **F2（JSON 跨断面块不可核）** —— **已复验通过（关闭）**：`cross_section_fixture` 父级补 `note/command/as_of/same_run_note/reconciliation_note`；两组子块各带 `as_of=01:22:19` ＋ `worktree_set_size=320` ＋ `detached=102` ＋ `evidence`（附录行号）；原第三组数移入 `readings_history`（**5 条**，各带 as_of）；**新增附录 A.2**（L812）逐字粘贴 01:16:32/01:18:47/01:20:00 三次历史读数并声明采用边界；`metrics.lane_A` 仍 **158/1546** | §8.3 ③④ | ✅ **已闭环**（原"待返修"撤销） |
| **U10** | **F3（复跑脚本未入库）**：`analyze.py`/`final_build.py`/`render.py` 在 `/private/tmp/squad-backlog-raw/`，分类表不能一键复跑（作者 §8.1-U9 已自曝） | §8.1-F3 | 阶段1「定版一条命令」时收编 |
| **U11** | **冻结值口径归属已裁决（写死）**：`149/1,415` 属**蓝图 §九.6 / 口径①**（工作树挂载分支，跳过 detached），**不是**派单 §十 的 origin 分支口径（该命令看不到本地独有的 `tmp-d942`）。C16 起点推荐 = 口径① ＋ `158/1,546 @01:13:03`（`metrics.lane_A`） | §7.2/§7.3 | CTO 采纳；建议把「口径声明」写进判据 |
| **U12** | **门禁扫描盲区仍在（波次2-B）**：`check-secrets.sh` 的 `--include` 清单**无 `*.txt`/`*.log`**（第 31-34 行）⇒ 3 份 `*.log.txt` 快照**不入门禁扫描**；本次由作者自扫 ＋ 自验员独立自扫双重覆盖（`sk-`/`cli_`/`ghp_`/`AKIA` 全 0 命中）。改 `.txt` 后缀**并未**消除该盲区 | §9 判据 11、§9.2-G5 | **队长裁决：接受 ＋ 登记未清项**。注意：扩展 `include` 清单 = **门禁语义变更 ⇒ 需 Mac-CTO 批准**；属阶段1/5 |
| **U13** | **`if-no-files-found: warn` vs 任务书写法 `ignore`**：两者都不使 job 变红 ⇒ "上传 0 文件"无法由 job 颜色发现。附 G2：step 注释称"上传失败会让 job 可见变红"，未区分"上传失败（红）"与"文件缺失（仅警告）" | §9 判据 3、§9.2-G1/G2 | **队长裁决：保持 `warn`**（不改 `ignore`／`error`）。**该裁决与 G6 裁决叠加后，"假绿"完全不可见 ⇒ 计数面（§9.3.1）成为唯一发现手段** |
| **U14** | **快照落位偏离任务书写法**：任务要求 `docs/synova/coordination/evidence/gate-logs-20260926/`，实际 `docs/synova/coordination/gate-logs-20260926/`（无 `evidence/`）。作者已在 PR ② 段**透明声明理由**（`.gitignore:8=*.log`、`:76=evidence/`，两条经自验员独立核实属实） | §9.2-G4、§9.3 | **队长裁决：接受** |
| **U15** | docs-only PR 时 Iron Laws step 被 `docsonly` 守卫跳过，而归档 step 为 `if: always()` ⇒ 无文件 → 警告、job 绿、无 artifact；即 `≥90d` artifact 只在"门禁真跑"的运行上产生 | §9.2-G3 | **队长裁决：可接受，登记备查** |
| **U16** | **G6 连带更正（待作者执行）**：PR body 第 59 行"新增 1 条 CI 红路径"依裁决 1 **作废** → 改为"新增 1 条**静默失败面（仅黄灯）**"；并须按裁决 1 给该 step（**仅该 step**）加 `continue-on-error: true`，同时在 PR 中与"不加"方案并列，请 Mac-CTO 二选一 | §9.3 裁决 1 | task-1 作者执行；本自验员**不改作者产物** |
| **U17** | **artifact 计数面判据待落地**（§9.3.1：判据＋命令草案已写死，**未新增脚本**）：每 N=20 次 main completed run 应有 `gate-logs-*`；缺失即计数 +1，进阶段 1 gov profile。**生效前提 = #828 合并进 main 之后**（合并前 main 用的还是旧 `ci.yml`，不得据此判红） | §9.3.1 | 阶段 1（`verify.sh` profile）；需另批写集 |

---

## 11. 复跑附录（K3 抽验用；全部命令可在 `origin/main` 断面重跑）

```bash
# ① 判据①-1/-2：写入目标
H=/Users/wane/SynovaAgent/.git/hooks/pre-commit
grep -n 'bypass\.log' "$H"                      # 期望 2 处（均为注释行）
grep -nE '>>?[[:space:]]*"\$ROOT/' "$H"         # 期望 3 个实质目标，无 bypass.log

# ② 判据①-3：同哈希（方法 A）
python3 - <<'PY'                                # 从 install-hooks.sh 提取 body → sha256
import pathlib,hashlib
s=pathlib.Path('/Users/wane/SynovaAgent/scripts/install-hooks.sh').read_text()
i=s.index('if [ "$name" = "pre-commit" ] && [ -f "$entry" ]; then')
j=s.index("body='", i)+6; k=s.index("'\n", j)
print(hashlib.sha256((s[j:k]+'\n').encode()).hexdigest())
PY
shasum -a 256 "$H"                              # 期望 609788b1ac3bfb31875c6b0b2836d511a7b542ba07523c2b751aa7b6f1a78401

# ③ 判据①-4：反例（自包含脚本，先在 scratch worktree 内建好树）
bash /private/tmp/stage0-verify/repro-hook-softfail.sh

# ④ 判据①-5：声称核验
grep -c 'GATE_FAIL_SOFT' /Users/wane/SynovaAgent/.claude/bypass.log        # 101
grep -cE "$(date +%Y-%m-%d).*detected-bypass" /Users/wane/SynovaAgent/.claude/bypass.log  # 0
sed -n '239,256p'   /Users/wane/SynovaAgent/scripts/pre-commit-check.sh    # 只匹配 detected-bypass
sed -n '1084,1108p' /Users/wane/SynovaAgent/scripts/pre-commit-check.sh    # ≥3 → SOFT_COUNT
sed -n '420,444p'   /Users/wane/SynovaAgent/scripts/pre-push-check.sh      # 门禁7 → check-bypass-log.sh
sed -n '88,94p'     /Users/wane/SynovaAgent/scripts/workflow/loop-context.sh  # 未过滤熔断源

# ⑤ 判据①-6：影响面 + 语义 diff
git config --show-origin --get core.hooksPath                             # 绝对路径
diff -u /private/tmp/cto-stage0-hook-backup/pre-commit.old "$H"           # 唯一差异 = 日志落点

# ⑥ U2 加固：熔断源（四态受控实验 —— 只换 bypass.log 内容为 3 行）
sed -n '84,100p'  /Users/wane/SynovaAgent/scripts/workflow/loop-context.sh   # :88 未过滤
sed -n '102,111p' /Users/wane/SynovaAgent/scripts/workflow/loop-context.sh   # exit 1 路径
#   在 scratch worktree 内：写入 3 行「今日 COMMITTED」→ --check ⇒ EXIT=1（CASE C，决定性）
#                          写入 3 行「昨日 COMMITTED」→ --check ⇒ EXIT=0（日期归一化对照）
grep -rn 'loop-context' /Users/wane/SynovaAgent/package.json \
  /Users/wane/SynovaAgent/.github/workflows/ \
  /Users/wane/SynovaAgent/scripts/pre-commit-check.sh \
  /Users/wane/SynovaAgent/scripts/pre-push-check.sh \
  /Users/wane/SynovaAgent/.claude/settings.json            # rc=1 ⇒ 自动化调用点 0

# ⑨ 口径①（蓝图 §九.6，与分析师口径A 同源）——逐字复跑
cd /Users/wane/SynovaAgent && python3 - <<'PY'
import subprocess
out=subprocess.run(['git','worktree','list','--porcelain'],capture_output=True,text=True).stdout
tot=0; n=0; rows=[]
for blk in out.strip().split('\n\n'):
    b=None
    for ln in blk.splitlines():
        if ln.startswith('branch '): b=ln[7:]
    if not b: continue                                    # ← detached 在此被跳过
    c=int((subprocess.run(['git','rev-list','--count',f'origin/main..{b}'],capture_output=True,text=True).stdout or '0').strip() or 0)
    if c: n+=1; tot+=c; rows.append((c,b))
rows.sort(reverse=True)
print(f'含未合并提交的工作树: {n} | 未合并提交总数: {tot}')
for c,b in rows[:7]: print(f'  {c:4d}  {b}')              # 期望首行 = 110  refs/heads/tmp-d942
PY

# ⑩ 口径②（派单 §十 命令）——与 ① 不同源
for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin | sed 's#^origin/##'); do
  [ "$b" = "HEAD" ] && continue; [ "$b" = "main" ] && continue
  echo "$(git rev-list --count origin/main..origin/$b 2>/dev/null || echo 0) $b"
done | awk '{s+=$1; if($1>0)n++} END {print "含未合并提交的分支:",n," 未合并提交总数:",s}'

# ⑪ 口径③/④/⑤/⑥（工作树 HEAD / 唯一并集 / 本地分支 / detached 唯一漏计）
git worktree list --porcelain | grep -c '^worktree '                    # 321（口径③ 分母）
git rev-list --count <全部唯一HEADs> --not "$(git rev-parse origin/main)" # 口径④：1,388
git branch -r --contains <detached-sha>                                  # 空 = 口径⑥ 漏计（2 树/14 提交）

# ⑫ 波次2-A 抽样复核（12 个 ref，跨三类）
for r in tmp-d942 feat/d962-2a-merge feat/p1-outbound-converge \
         4866b35e6b6ef2b73803ec3ef715b10b754d93de fix/d954-ci-reds-base-ref \
         c4206f9bbe68008bda451f1e4afbeefa3b477f86 gate/d839-win-chain \
         feat/d595-mcp-auth feat/d512-gs-refresh feat/d510-audit-remediation fix/d516-ci-strict; do
  echo "$(git rev-list --count origin/main..$r) $r"
done

# ⑬ patch-id 独立复算（重复劳动判定的核心证据，非目录名/mtime）
pids() { git log -p --no-merges --format='commit %H' "origin/main..$1" | git patch-id --stable | awk '{print $1}' | sort -u; }
pids 4866b35e6b6ef2b73803ec3ef715b10b754d93de > /tmp/a; pids fix/d954-ci-reds-base-ref > /tmp/b
comm -12 /tmp/a /tmp/b | wc -l        # 期望 28（作者声称 identical 28）

# ⑭ 波次2-A 指定问题原始输出
git ls-remote origin refs/heads/fix/d962-2a-iron-laws-wiring    # 期望空
git rev-parse tmp-d942                                          # 期望 b3182f672a5e…
curl -sS -H "Authorization: token $TOKEN" https://api.github.com/repos/tangbaobao520/SynovaAgent/pulls/738   # head.sha 同值、commits=110

# ⑮ 波次2-B（task-1 / PR #828）
git diff -U0 origin/main..origin/chore/stage0-log-archive-20260926 -- .github/workflows/ci.yml | grep -c '^-[^-]'   # 期望 0
git show origin/chore/stage0-log-archive-20260926:.github/workflows/ci.yml | sed -n '/Archive gate raw logs/,/include-hidden-files/p'
for s in gate-hits.log.txt degraded-events.log.txt pre-commit-failures.log.txt; do
  git show "origin/chore/stage0-log-archive-20260926:docs/synova/coordination/gate-logs-20260926/$s" | shasum -a 256
done                                                                  # 与主树实况 sha256 比对（期望相同）
bash /private/tmp/stage0-verify/wave2b-artifact.sh                    # artifact 等待+下载+解压+行数（时间性未决时脚本会自报）
curl -sS -H "Authorization: token $TOKEN" https://api.github.com/repos/tangbaobao520/SynovaAgent/branches/main/protection   # required_status_checks 含 TypeScript + Lint + Iron Laws
```

**环境与复跑注意（K3 抽验者必读，自验员实测踩坑）**：
1. `/private/tmp` 下**新建 worktree 无 `node_modules`** ⇒ `pre-push 门禁 2`（`npx tsx scripts/ci/golden-case-checker.ts`）会因 npm cache `EPERM` 报**环境性假红**（非代码回归）；处置：`ln -sfn /Users/wane/SynovaAgent/node_modules node_modules`（untracked、`.git/info/exclude` 已忽略），之后该门禁 **11/11 PASS**。本报告所有 git 操作**未使用** `--no-verify`／`git stash`／force push。
2. 本报告全部原始输出取自 `/private/tmp/synova-wt-squad-verify`（自验分支）与 `/private/tmp/synova-wt-stage0-redtest`（反例夹具，**已清理**）；**主工作树 `/Users/wane/SynovaAgent` 全程零 git 写操作**（只读 fetch/rev-list/ls-remote 除外）。
3. 计数类结论**一律带 as_of**：本阶段积压持续漂移（实测 9 分钟内 +4 工作树 / +125 提交），任何跨时刻比较必须重跑。


**自验边界声明**：本报告由 `squad-verifier`（非作者）独立重跑；未改动任何被验产物；未接触 `scripts/audit/**`、`docs/synova/audit-reports/**`；未在主工作树 `/Users/wane/SynovaAgent` 执行任何 git 写操作；未新增门禁。K3 抽验输入（原始命令 ＋ 原始输出）已在本文备齐，「可复跑」以本文件 §11 为准。
