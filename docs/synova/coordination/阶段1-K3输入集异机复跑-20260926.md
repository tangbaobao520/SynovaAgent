# 阶段1-B K3 最小输入集「异机可复跑」验证

> 卡：**D1015**（`alloc-task-id.sh` 分配）｜分支：`chore/D1015-k3-input-repro`
> 模拟异机：`/private/tmp/k3-sim-20260926`（**匿名远程 clone**，无 token）｜clone 内容 = `origin/main` @ `ee721b0abf52`
> 快照来源分支：`chore/D1009-stage0-log-archive`（PR #832）@ `661b2c483702`
> 纪律：**没在新 clone 里真跑出结果就不写"通过"**；拿不到 → 写"不可复跑 + 缺什么"。判据全部三态。

---

## 0. 结论速览（逐条三态）

| # | 输入项 | 异机可复跑？ | 证据/说明 |
|---|---|---|---|
| 1 | **git 原语**（clone/fetch/ls-files/ls-tree/rev-list/diff） | ✅ | 匿名 `git clone` 成功；克隆自带 **782** 个远程跟踪分支；见 §1 |
| 2 | **三个门禁日志的内容**（快照） | ✅（**带一条限制**） | 匿名 fetch D1009 分支后 `git ls-files` → **3 行**，sha256 与主树取证现场**逐字符一致**（§2）。**限制**：快照在 `chore/D1009-stage0-log-archive` 上，**未合 main 前 `git ls-files` 在 main 上为 0**（§2.3） |
| 3 | **蓝图 §九.1**（门禁命中率） | ✅ 用快照 / ❌ 用原文路径 | 原文 `.claude/gate-hits.log` → `FileNotFoundError`（rc=1）；改快照路径 → **门禁数=36、hit=0=17**（与蓝图 v1.3 修正值一致）（§3.1） |
| 4 | **蓝图 §九.2**（C1/C2） | ✅ | `pre-commit-check.sh` = **1603**（= 蓝图基线）、外部调用点去重 = **29**（= 基线）、`check-*` = **49**（基线 48，漂移 +1）（§3.2） |
| 5 | **蓝图 §九.3**（死机制证据） | ✅ 用快照 / ❌ 用原文路径 | 原文路径 grep rc=2；改快照 → `verify-parallel` = **5657**（**= 蓝图基线 5657**）、top1 占比 98%（§3.3） |
| 6 | **蓝图 §九.4**（共享文件写者数） | ⚠️ 需改一条命令 | `grep -rl '派单模板' task-state/*.json` = **6**（= 基线）✅；但 `git ls-tree … \| grep '号段水位'` **默认 quotepath 下 = 0（假阴性）**，加 `-c core.quotepath=false` → 1（§3.4） |
| 7 | **蓝图 §九.5**（四类争议 + 人工税） | ✅（数字有漂移） | 卡总数 **370**（基线 364）/ 有 write_set **98**（93）/ 无空 **272**（271）——漂移方向 = 卡在增（§3.5） |
| 8 | **蓝图 §九.6**（C6/C16） | ❌ 逐字（机器绑定）/ ✅ 远端口径 | `git worktree list` 在异机 = **1**（基线 301）；C16 逐字 = 0/0 ⇒ **该口径天生只能在本机跑**；**C16-remote**（远端 ref 口径）= **459 个领先分支 / 3687 个未合并提交**（14s）（§3.6） |
| 9 | **artifact 通道** | ⚠️ 需 token | 带 token：200 / 1170 B / 内含 `.claude/gate-hits.log` **21 行**；**无 token：401（01:5x 读数，认证要求）＋ 403-限流（03:55 复测）**（§4） |
| 10 | **`pre-commit-failures.log` 的持续面** | ❌ | 只有本机 `.git/hooks/pre-commit` 会写（CI 不 commit）⇒ 快照只补"某日断面"，补不了"持续"（§6-1） |
| 11 | **`/tmp/.synova-before-brief`（时间戳顺序门禁输入）** | ❌ | 机器级共享文件，异机不存在；异机跑 `SYNO_CI=1` 时会**跳过**该项（不假红，但也不复现本机的红）（§6-2） |

---

## 1. 异机 clone 的建立（匿名，无 token）

```bash
rm -rf /private/tmp/k3-sim-20260926
git clone --quiet https://github.com/tangbaobao520/SynovaAgent.git /private/tmp/k3-sim-20260926
```
```console
✅ clone 成功 → /private/tmp/k3-sim-20260926（第 1 次尝试；匿名）
=== clone 侧基本信息 ===
ee721b0abf52
ee721b0a Merge pull request #804 from tangbaobao520/fix/d956-ci-failmsg
=== 工作树数（异机只有它自己）===
1
=== gitignored 运行态日志是否存在（期望：全部不存在）===
  缺席: .claude/gate-hits.log
  缺席: .codex/control-tower/logs/degraded-events.log
  缺席: .claude/pre-commit-failures.log
=== main 上快照是否可见（期望：0，未合）===
0
```
⇒ **异机的关键性质成立**：三个 gitignored 运行态日志**全部缺席**；本机 301 个工作树在异机不存在（只有 1）。
另：克隆自带 **782** 个远程跟踪分支（`refs/remotes/origin/*`），**无需额外 fetch**；一次补充 fetch 曾遇 `HTTP2 framing layer` 错误，但该次为冗余操作，不影响任何判据。

---

## 2. 快照可复读（task-5 判据①）

### 2.1 匿名 fetch D1009 分支
```console
$ git fetch --quiet origin chore/D1009-stage0-log-archive:refs/remotes/origin/k3-task1
$ git rev-parse --short=12 refs/remotes/origin/k3-task1
661b2c483702
```
> 与 PR #832 的最终 HEAD 一致 ⇒ **K3 无 token 也能取到该分支**。

### 2.2 切到该分支后 `git ls-files` 与内容完整性
```console
$ git checkout -q -B k3-task1 refs/remotes/origin/k3-task1 && git rev-parse --short=12 HEAD
661b2c483702
$ git ls-files docs/synova/coordination/gate-logs-20260926/
docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt
docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt
docs/synova/coordination/gate-logs-20260926/pre-commit-failures.log.txt
行数=3
$ shasum -a 256 docs/synova/coordination/gate-logs-20260926/*.log.txt
30929d4ed4e99c2c47b1f46d1567ef8e5f749e23d62b35b66a43b4e06df4b781  …/degraded-events.log.txt
2a6a80ba467220da9d924df62352eb5eaccaf557874d6315e21322b8e35dc6f7  …/gate-hits.log.txt
1148103f70ff3da63dbde71f0a3dd551e7aeb237a43bd4179dc1e80968187652  …/pre-commit-failures.log.txt
$ wc -l docs/synova/coordination/gate-logs-20260926/*.log.txt
    5758 …/degraded-events.log.txt
    4873 …/gate-hits.log.txt
     156 …/pre-commit-failures.log.txt
   10787 total
```
⇒ **sha256 与主树取证现场（阶段0-② 记录值）逐字符一致**；行数 4873/5758/156 亦一致。

### 2.3 **必须如实登记的限制**：快照尚未合 main
```console
$ git checkout -q main && git ls-files docs/synova/coordination/gate-logs-20260926/ | wc -l
0
```
⇒ 在 `main` 上**列不出**这三个文件。**本判据目前成立的条件是"fetch 到 D1009 分支"**（PR #832 合并后才在 main 上成立）。
**结论表述**：*异机可复读 = 成立（经该分支）；"main 上可得" = **尚未**成立，取决于 #832 合并。不得写成"main 上可列出"。*

---

## 3. 蓝图 §九 逐条复算（全部在 clone 内执行）

> 版本说明：§3.1/§3.3 在 `k3-task1` 分支（需快照）；§3.2/§3.4/§3.5/§3.6 在 `main`（`ee721b0abf52`）。

### 3.1 §九.1 门禁命中率 —— 原文路径 ❌ / 快照 ✅
原文（逐字，只是文件路径来自蓝图）：
```console
$ python3 sec91.py .claude/gate-hits.log
rc=1
FileNotFoundError: [Errno 2] No such file or directory: '.claude/gate-hits.log'
```
改用入库快照（**唯一改动 = 文件路径**）：
```console
$ python3 sec91.py docs/synova/coordination/gate-logs-20260926/gate-hits.log.txt
门禁数=36  hit=0 的门禁=17
```
⇒ **与蓝图 v1.3 修正后的数字一致**（蓝图：门禁数 36 / "19 个 hit=0"改为 **17**）。§九.1 是"宽度是空的"这一结论的**唯一数据来源**，本项证明：**它的数据面已被快照救回，异机可复算**。

### 3.2 §九.2 脚本与调用点（C1/C2）—— ✅
```console
$ find scripts -type f -name 'check-*' | wc -l
49                       # 蓝图基线 48（漂移 +1，方向=增）
$ wc -l scripts/pre-commit-check.sh
1603                     # = 蓝图基线 1603 ✅ 完全一致
$ grep -oE '($ROOT|$CT_DIR|$SCRIPT_DIR)?/?(scripts|\.codex)/[A-Za-z0-9_./-]+\.(sh|py|ts)' scripts/pre-commit-check.sh | sort -u | wc -l
29                       # = 蓝图基线 29 ✅ 完全一致
```

### 3.3 §九.3 死机制证据 —— 原文路径 ❌ / 快照 ✅
```console
$ grep -c '"component": "verify-parallel"' .codex/control-tower/logs/degraded-events.log
grep: .codex/control-tower/logs/degraded-events.log: No such file or directory     # rc=2
$ grep -c '"component": "verify-parallel"' docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt
5657                     # = 蓝图基线 5657 ✅ 完全一致
$ python3 -c "…component 计数 top5（快照）…"
component 计数 top5: [('verify-parallel', 5657), ('check-branch-sync', 38), ('gatekeeper', 28), ('baseline-check', 16), ('check-brief-parseable', 11)]
```
⇒ 快照使"verify-parallel 占 98%"这一**死机制证据**在异机完整复现。

### 3.4 §九.4 共享文件写者数（Q3 判据）—— ⚠️ 一条命令有编码坑
```console
$ grep -rl '派单模板' task-state/*.json | wc -l
6                        # = 蓝图基线 6 ✅
$ git ls-tree -r --name-only origin/main | grep -c '号段水位'
0                        # ★ 假阴性（蓝图写"存在"）
$ git -c core.quotepath=false ls-tree -r --name-only origin/main | grep -c '号段水位'
1                        # ✅ 存在（docs/synova/coordination/号段水位.md）
$ git show origin/main:docs/synova/coordination/号段水位.md | head -6
# 号段水位
| 归属 | 号段 | 水位 |
| Mac | D9xx | D968 |
```
⇒ **复跑性缺陷**：`git ls-tree | grep <中文名>` 在**默认 `core.quotepath=true`** 下会把中文路径转义成八进制 ⇒ **恒 0**。蓝图该行命令在异机（或任何未显式关闭 quotepath 的会话）**结论会反**。修法：`git -c core.quotepath=false …`（本仓 `merge_writeset_gate.py` 已有同款注释，属同类坑）。

### 3.5 §九.5 四类争议 + 人工税（C3/C4/C5/C9）—— ✅（有漂移）
```console
$ python3 sec95.py
卡总数=370 有 write_set=98 无/空=272      # 蓝图基线 364 / 93 / 271
$ grep -c '写集豁免' scripts/control-tower/merge_writeset_gate.py
8
```
⇒ 三项**可复算**，差值 +6/+5/+1 = 期间新增卡（**单调方向 = 卡在增**，与阶段 3 目标"新卡不需要写 write_set"相关：有 write_set 的比例 98/370 = 26.5%，基线 25.5%）。

### 3.6 §九.6 工作树与主树（C6/C16）—— ❌ 逐字 / ✅ 远端口径
**逐字（worktree 口径）**：
```console
$ git worktree list | wc -l
1                        # 蓝图基线 301
$ python3 sec96-local.py
含未合并提交的工作树: 0 | 未合并提交总数: 0
```
⇒ **C16 的 worktree 口径天生机器本地**：异机上"未合并提交 = 0"，而真实积压（本机 149 个领先工作树 / 1,415 提交）**完全不可见** ⇒ 若把该命令当跨机判据，会得到**假零**。

**C16-remote（远端 ref 口径，本卡新增的可跨机等价物）**：
```console
$ time python3 c16-remote.py
[C16-remote] 远端分支总数=781 | 领先 main 的分支数=459 | 未合并提交总数=3687
   186  origin/feat/desktop-phase0-auth
   113  origin/feat/d962-2a-merge
   110  origin/docs/d943-cto-a-items
    58  origin/tmp/d839-657-green
    58  origin/fix/d839-claim-release
    58  origin/feat/gates-657-verify
    56  origin/gate/d839-brief-restore
real    0m13.686s
```
⇒ **异机可复跑**（匿名 clone 后 14s 出数）。**口径差异必须并列登记**：worktree 口径（本机）= 149/1,415；远端 ref 口径（异机）= **459/3,687**。二者差在"已推远端但本机无工作树的分支"。⇒ **判据 C16 必须声明口径**，否则同名两数字（正是本项目 C1 判据要治的"同名多口径"）。

---

## 4. artifact 通道（带 token / 无 token）

### 4.1 带 token：可下载、非空
```console
$ curl -sL -H "Authorization: token …" .../artifacts/10879601805/zip -o art.zip -w "http=%{http_code} size=%{size_download}"
http=200 size=1170
$ unzip -l art.zip
     2833  09-25-2026 17:45   .claude/gate-hits.log
$ wc -l 解压后
21 行  .claude/gate-hits.log
```
（与阶段0-② 记录一致：`gate-logs-36167351846`，1170 B，`expires_at` = 创建 +**整 90 天**。）

### 4.2 无 token：两次读数并列（各自方法与时点）
| 读数 | 时点（本机 +08:00） | 方法 | 结果 |
|---|---|---|---|
| A | 2026-09-26 01:5x（阶段0-② 期间） | `curl -s -o /dev/null -w "%{http_code}" -L <archive_download_url>`（**不带头**） | **401**（认证要求） |
| B | 2026-09-26 03:55 | 同上 + 匿名 `GET /actions/runs/<id>/artifacts` + 匿名 `GET /jobs/<id>/logs` | **403 + `API rate limit exceeded for 180.136.226.231`** |
| 对照 | 2026-09-26 03:55 | 匿名 `GET /repos/tangbaobao520/SynovaAgent`（**与 artifact 无关的端点**） | **403 同一条限流报文** ⇒ 该 403 是**IP 匿名配额耗尽**，不是端点语义 |
| 对照 2 | 2026-09-26 03:55 | 同端点**带 token** | **200** |
**判读**：403 是**匿名配额**噪声（连仓根都 403）；**401（读数 A，配额可用时）才是端点的真实认证要求** ⇒ 结论：**无 token 走不通 artifact 通道**（要么 401 认证要求，要么 403 配额）。
另：`github.com` 的 HTML artifacts 页匿名探测本次为 `http=000`（连接未完成）⇒ 未取得有效读数，**不列入结论**。

---

## 5. K3 在**无 token** 情况下能拿到什么（明确清单）

**能拿到（实测）**：
1. **完整 git 原语**：匿名 `git clone`（782 个远程跟踪分支随克隆到手）
2. **任意分支的提交历史**：匿名 `git fetch origin <branch>`（实测 D1009 分支 = `661b2c483702`）
3. **三个门禁日志的原始内容**：`git ls-files` + 读文件（sha256 与主仓取证现场一致）
4. **由 (3) 复算的判据**：§九.1（36/17）、§九.3（5657）——即"宽度是空的""verify-parallel 死机制"两条结论的**数据面**
5. **纯 git 判据**：§九.2（1603/29/49）、§九.4（6，需 `core.quotepath=false`）、§九.5（370/98/272）
6. **C16-remote 口径**：459 / 3687（14s）
7. 公开网页（PR/文档）——人读面

**拿不到（实测/结构性）**：
1. **CI artifact 下载**（401 / 403-限流）⇒ 除非给 K3 配**只读 token**
2. **CI job 原始日志**（本次 403；`ci.yml:422-424` 既注记匿名 404/403 ⇒ 长期不可靠）
3. **任何 CI 状态面**（run/jobs/check-runs 的匿名读取本次全部被配额拒）

---

## 6. 不可异机复跑清单（单列，不含糊）

| # | 对象 | 为什么不可 | 缺失物 / 处置方向 |
|---|---|---|---|
| 1 | `pre-commit-failures.log` 的**持续面** | 唯一写入者 = 本机 `.git/hooks/pre-commit`（`install-hooks.sh:49`）；CI 不 commit ⇒ 无 hook 触发 | 快照只覆盖**某日断面**；"持续"需另机制（本机侧定时快照推 ref / 或产物上传），**须 Mac-CTO 批准** |
| 2 | `/tmp/.synova-before-brief`（时间戳顺序门禁的输入） | **机器级共享文件**（非 per-worktree），异机不存在 | 异机跑 `SYNO_CI=1` 时该项**跳过**（不假红，但本机的红也不会复现）⇒ 跨机结论不一致 |
| 3 | C16 / C6 的 **worktree 口径** | `git worktree list` 是本机目录状态；异机恒为 1 | 用 **C16-remote**（远端 ref 口径）替代；两者数字必须并列标注 |
| 4 | **工作树/分支的物理清理动作** | 依赖本机工作树 | 属"分类后"动作（阶段 0-③），本卡不涉及 |
| 5 | **CI artifact / job logs**（无 token 时） | 端点要求认证；匿名还受配额限 | 给 K3 只读 token，或改匿名可达通道（如 `::notice` 注解 —— 本次因配额未取得读数，**未验证**） |
| 6 | 本机**未推送**的提交/工作 | 远端不存在 | 属"未合提交"（C16-remote 只能看到**已推**部分）⇒ 3687 是**下界** |

---

## 7. 本卡顺带发现的「复跑性缺陷」（4 条）

1. **蓝图 §九.4 中文路径命令恒假阴性**：默认 `core.quotepath=true` 下 `git ls-tree | grep '号段水位'` = 0（实际存在）⇒ 需 `-c core.quotepath=false`（§3.4）。
2. **蓝图 §九.6 / C16 口径机器绑定**：`git worktree list` 在异机 = 1 ⇒ C16 需改用远端 ref 口径（本卡给出 `C16-remote` 与实测值 459/3687）（§3.6）。
3. **§九.1 / §九.3 的数据面原本不可异机复跑**（依赖 gitignored 日志）——**已由阶段0-② 的快照救回**（§3.1/§3.3）；但**快照尚未合 main**（§2.3），当前成立条件是"fetch 到 D1009 分支"。
4. **同一判据存在两套合法口径**（C16: worktree=149/1415 vs 远端 ref=459/3687）⇒ 建议 C16 判据文本里**写死口径**，否则跨机/跨人必出"同名多口径"。

---

## 8. 复现命令清单（可复制）

```bash
# ── 0. 异机 clone（匿名，无 token）──
rm -rf /private/tmp/k3-sim-20260926
git clone --quiet https://github.com/tangbaobao520/SynovaAgent.git /private/tmp/k3-sim-20260926
cd /private/tmp/k3-sim-20260926

# ── 1. 三个日志的内容（快照）：fetch 分支后 git ls-files ──
git fetch --quiet origin chore/D1009-stage0-log-archive:refs/remotes/origin/k3-task1
git checkout -q -B k3-task1 refs/remotes/origin/k3-task1
git ls-files docs/synova/coordination/gate-logs-20260926/     # 期望 3 行
shasum -a 256 docs/synova/coordination/gate-logs-20260926/*.log.txt
wc -l docs/synova/coordination/gate-logs-20260926/*.log.txt   # 4873 / 5758 / 156

# ── 2. §九.1（改快照路径）与 §九.3（改快照路径）──
grep -c '"component": "verify-parallel"' docs/synova/coordination/gate-logs-20260926/degraded-events.log.txt   # 期望 5657
# §九.1 计算脚本见本文件 §3.1（把路径换成快照路径 ⇒ 门禁数=36 / hit=0=17）

# ── 3. 纯 git 判据（回 main）──
git checkout -q main
find scripts -type f -name 'check-*' | wc -l ; wc -l < scripts/pre-commit-check.sh
git -c core.quotepath=false ls-tree -r --name-only origin/main | grep -c '号段水位'   # 1（不加 -c 会得 0）

# ── 4. C16-remote（远端 ref 口径，本卡新增）──
#   等价内联：对 refs/remotes/origin/* 每个分支 git rev-list --count origin/main..<ref> 求和
#   实测：领先分支 459 / 未合并提交 3687 / 耗时 ~14s

# ── 5. artifact 通道 ──
TOKEN=$(grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml | sed 's/.*GITHUB_TOKEN:[[:space:]]*//' | tr -d '\r\n')
curl -sL -H "Authorization: token $TOKEN" "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/artifacts/10879601805/zip" -o art.zip -w "http=%{http_code} size=%{size_download}\n"   # 200 / 1170
curl -s -o /dev/null -w "anon=%{http_code}\n" -L "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/artifacts/10879601805/zip"   # 401 或 403-限流
```

---

## 附：本卡币值

| 项 | 值 |
|---|---|
| 卡号/分支 | **D1015** ｜ `chore/D1015-k3-input-repro` |
| 异机 clone | `/private/tmp/k3-sim-20260926`（**匿名**）｜main @ `ee721b0abf52` ｜自带 **782** 远端分支 |
| 快照分支 | `chore/D1009-stage0-log-archive` @ `661b2c483702`（**未合 main** ⇒ main 上 `git ls-files` = 0） |
| 复算命中 | §九.1 **36/17** ✅｜§九.2 **1603/29**（= 基线）✅｜§九.3 **5657**（= 基线）✅｜§九.4 **6** ✅（+编码坑）｜§九.5 **370/98/272**（漂移）｜**C16-remote 459/3687**（新增口径） |
| artifact | 带 token **200 / 1170 B / 21 行**；无 token **401**（01:5x）/ **403-限流**（03:55，对照仓根同 403） |
| 只读约束 | 未新增脚本、未改任何检查；主树零 git 写；只碰 `docs/` + `task-state/D1015.json` |
