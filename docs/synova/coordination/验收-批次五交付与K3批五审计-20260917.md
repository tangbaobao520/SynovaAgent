# 验收报告：批次五交付（D788 / D786 / D782 / D774）+ K3 批次五审计

> 验收人: CTO（synova-cto session）｜时间: 2026-09-17 00:12–00:35 CST
> 口径: `origin/main` = **6a06e853**；被验 PR head: D788=#604 `e78480ec`、K3 批五=`d67b201d`（K3 独立工作区）、其余交付均已合 main
> 隔离方式: 9 个 `/tmp/acc-*` detached worktree（各自 pinned SHA，互不复用）；主工作区零接触（HEAD 全程 `feat/d782-doc-truth-wiring` @ `73110752` 未变）
> 判定原则: 只认「命令 + 原始输出」；不复现即标「未复现」，不转述、不打折

---

## 0. 结论摘要

| 交付 | 判定 | 关键实证 | 遗留 |
|---|---|---|---|
| **D788** 预算门禁只计审查面（#604） | ✅ **通过**（附 1 处例证不实） | 39/39 测试绿；审查面 0+治理 51 → PASS（解锁 #572）；13 件超限拦；真跨域（mac vs win）拦；#572 双模式一致；本 PR 自身 审查面 3／治理 10 PASS | 报告表 item 4 例证复现为**单域**（不实）；`--max-files` 覆盖参数仍在（D734 遗留，非本单引入） |
| **D786** D774 收口 + 通道抢救 | ✅ **通过** | #590/#591/#592 在 main；`calc-progress.py` 自 `93a32779` 起 **0 提交**；F6 机理三例复现（bot=0 / PAT=12）；看门狗三态 + 11 断言；#404/#586/#403 已关 | D786 的 task-state 只在**未合的 #584** 里 → 台账未落 main |
| **D782** 文档真相防线接线 | ✅ **通过** | 8 套 doc-system 测试 **88 断言 0 失败**；反向摘线 7/2 复现；接线 :383/:1476 + 13 组横幅 :1523；探针三态；DRIFT-LEDGER 机器段在 main；I2=4 / I3=35 findings | #594/#595 关闭未合（被 #598/#600 取代）；docs-only PR 的 D1 CI 权威缺位（其自陈已知开口） |
| **D774** 证据保鲜流水线 | ✅ **通过**（幂等口径需注释） | 6 件交付物在 main；测试全绿；证据落库 GS×8 + test×2 ×2 天；连跑两次**零新增文件、六态不变**（仅 `generated_at` 变） | 我环境受限：worktree 无 node_modules → GS 8 全 exit=1 且**证据=no**、A2 degraded → 不能用于判 GS 真伪 |
| **K3 批五审计**（D789） | ✅ **结论成立**（2×P0 我复现并认同；1 处数字不实） | P0-1 同文档两树 §⑩ ✅→⚠️；P0-2 实弹 GATE-OK 跳过 + #601 十二检查全绿；B1-P1 我独立证实 | K3 表「派单-\* 20 份」实测 **40** 份；报告**未落产品仓库**（同分支 `d67b201d` 未推） |

**今天的产品真相（我方重算，诚实版）**：`uncommitted 96 / failed 2 / pending_k3 40 / verified 0 / rejected 2 / stale 24`（164 点 / 26 线）→ **加权进度 0%**。
09-16 仪表盘上的 **1% 已在 09-17 消失**：唯一 verified 点 `19-2` 的 K3 证据日期是 **2026-09-02**，在 09-16 恰好 14 天（`> 14` 不成立 → fresh），09-17 变成 15 天 → 跨过 `EVIDENCE_TTL_DAYS=14` → stale。**这是"证据过期"，不是产品退化**。

---

## 1. D788 — PR 预算门禁只计审查面（#604，栈在 #589 之上）

隔离树 `/tmp/acc-d788` @ `e78480ec`（父 = #589 head `5e59f186`）。

### 1.1 本单真实写集（相对 #589 基线，10 文件）

```
 .claude/bypass.log                                 |  1 +      ← 合法 COMMITTED 登记（非 --no-verify）
 .claude/task-briefs/2026-09-16-D788-impl-budget-scope.md
 docs/synova/coordination/MULTI-MACHINE-PR-WORKFLOW.md
 docs/synova/coordination/派单-D788-预算门禁口径修正-20260916.md
 docs/synova/coordination/派单模板.md
 memory/notes/implemented/2026-09-16-d788-budget-scope.md
 scripts/control-tower/check-pr-budget.sh           | 72 ++++++---
 scripts/control-tower/pre-dispatch-check.sh        |  2 +      ← 仅注释（逻辑零改动，diff 全文已核）
 task-state/D788.json
 tests/control-tower/check-pr-budget.test.sh        | 47 +++++
```
审查面 = 3（2 个脚本 + 1 个测试）；治理 = 7（+bypass.log 由 hook 生成）。`pre-dispatch-check.sh` 的 2 行**确为注释**（锚点语义说明），无逻辑改动 ✅

### 1.2 十项逐条（K3 §5 清单 ↔ 我的复跑）

| # | 声称 | 我的复跑 | 判定 |
|---|---|---|---|
| 1 | 上限未放宽 | `MAX_FILES=12`（:63） | ✅ |
| 2 | fail-closed | `--files unknown_dir/x.ts` → `✅ ① 审查面 1 件（上限 12）` rc=0 | ✅ |
| 3 | 审查面 0 + 治理 51 | 用 #572 真变更集（51 件）→ `✅ ① 审查面 0 件／治理产物 51 件（不计入）` + `✅ ② 域校验跳过` rc=0 | ✅ |
| 4 | 跨域仍拦（`src/x.ts` + `scripts/y.sh`） | **⚠️ 复现为单域**：`✅ ② 变更单域: win` rc=0。改用真跨域对（`src/sentinel/x.ts` mac + `src/x.ts` win）→ `❌ ② 变更跨域` rc=1 | ⚠️ 例证不实，机制成立 |
| 5 | 上限仍拦 | 13 审查面 → `❌ ① 审查面 13 件（上限 12）> 上限` rc=1 | ✅ |
| 6 | 反向验证（排除表真被读） | 测试内：`src/` 入排除表 → 13 src 计 0 → 绿；原脚本 → 红 | ✅ |
| 7 | #572 双模式 | 旧脚本原地跑 → `❌ 51 > 12` + 跨域 k3 rc=**1**；新脚本真 git → `✅ PASS 审查面 0／治理 51` rc=0；`--files` 同变更集 → 同结论 | ✅ |
| 8 | 输出显性 | 字面串存在于 :142/:144（`审查面 N 件（上限 12）／治理产物 M 件（不计入）`），测试断言 | ✅ |
| 9 | 不回归 + 性能 | `bash tests/control-tower/check-pr-budget.test.sh` → **全部通过: 39 项** rc=0；门禁本体 51 件耗时 **0.185s** | ✅ |
| 10 | 写集 + Note | `git diff --name-only 5e59f186..HEAD \| grep -c scripts/audit/` = **0**；本 PR 自身跑新门禁 → `审查面 3／治理 10` rc=0 | ✅ |

### 1.3 我补的两条（报告未写）

- **适用范围记录正确**：脚本头注释 :12-17 明写「纯治理/审计 PR 无自动执行点（CI 被 D515 跳过 + 本地软提示）」，与 `ci.yml:82-83`（`Iron laws check` 由 `steps.docsonly.outputs.docs_only != 'true'` 门控）实测一致；裁决「保留跳过、只显性化」符合 D515 设计原意 ✅
- **接线点**：`pre-commit-check.sh:1462-1473` 以 `--quiet` 调用（本地软提示），CI 侧随 Iron laws 生效 → 与 K3 F1 的机理表述一致。

---

## 2. D786 — D774 收口补丁 + 仪表盘通道抢救

隔离树 `/tmp/acc-d786` @ main。

```
$ git log --oneline origin/main | grep -E '\(#59[012]\)'
755b83e5 ... T2 仪表盘通道修复 + 产物过期看门狗 (#592)
31506d16 ... T1-b——刷新产物落 main (#591)
1ce2235d ... T1-a——当日重跑证据入库 GS×8+test×2 (#590)

$ git log --oneline 93a32779..origin/main -- scripts/product-lines/calc-progress.py | wc -l
0                                   ← 红线：判分规则零改动 ✅

$ grep -rn 'gh pr create' .github/workflows/ | wc -l
0                                   ← F6 修复=方案(b)：workflow 只产分支 ✅
```

**六态（产物 `generated_at 2026-09-16 19:00:59`，验收时距 5.2h ≤ 3 天阈值）**：`uncommitted 96 / pending_k3 42 / stale 21 / verified 1 / failed 2 / rejected 2` = 164 点 / 26 线 ✅ 与报告一致。

**F6 机理独立复现（不引用其自证）**

```
PR #404（github-actions[bot] 建，closed 未合） head=6395b014 → check-runs total_count=0
PR #586（bot）/#403（bot） → 同 0，均已 closed
PR #590（tangbaobao520 建，PAT） head=e50651ca → check-runs 12 项，conclusion 全 success
PR #592（PAT） head=7ecdf2ee → 12 项全 success
```

**看门狗（`scripts/product-lines/check-progress-freshness.py` + 每日 workflow :cron "30 1 * * *"）**

```
新鲜           → ✅ 产物新鲜 ... 距今 0.2 天 ≤ 阈值 3.0 天              rc=0
边界 09-19 19:00:59 → ✅ 距今 3.0 天 ≤ 阈值 3.0 天                    rc=0
刚过阈值 +1 分 → 🚨 过期（带具名修复指引: rerun-evidence.sh → PR）    rc=1
JSON 损坏      → degraded: 产物不可读/JSON 损坏 ...                    rc=2
文件不存在     → degraded: 产物不存在                                  rc=2
密封测试 tests/control-tower/check-progress-freshness.test.sh → 通过 11 / 失败 0  rc=0
```

（我先前一次 `rc=2` 是用非法秒 `19:00:60` 造的，属我的测量瑕疵，已改用合法时间戳复测。）

**缺口**：`task-state/D786.json` 在 main **不存在**，只存在于**未合**的 #584 树（commit `3d4c82f0`）。即台账有落点、但卡在队列里。

---

## 3. D782 — 文档真相防线接线

隔离树 `/tmp/acc-d782`（`3622a3d1` = 接线提交）+ `/tmp/acc-d782m`（main）。

```
# 8 套 doc-system 测试（main 树）全部 rc=0
check-doc-truth 5/0 | doc-categories 14/0 | doc-registry-gate 9/0 | doc-staleness 4/0
doc-triage 8/0 | doc-truth-probe 18/0 | generate-chronicle-monthly 9/0 | system-registry 21/0
→ 合计 88 断言 0 失败

# 反向验收：物理换回 wiring 前的 pre-commit（git show 3622a3d1~1:...）
  ❌ W1 接线: registry-gate 在 pre-commit (期望 0 实际 1)
  ❌ W2 接线: check-doc-truth 在 pre-commit (期望 0 实际 1)
  ── 汇总: 7 通过 / 2 失败 ──                        rc=1  ← 断言真在守，恢复后工作区 0 变更

# 接线与横幅
pre-commit-check.sh:1402 D782 块（soft_check，不并 13 组编号）；:1523 = "✅ 全部 13 组通过" 未动

# 探针三态（副本内造，跑后归零）
PROBE: FRESH-GREEN — 防线新鲜且绿（age=0.06d）        rc=0
PROBE: NEVER-RUN — 台账无机器段                        rc=1
PROBE: STALE — 台龄 19.60d > 7d                        rc=1

# DRIFT-LEDGER 机器段（#598 squash 丢失 → #605 补落，实测在 main）
lastVerifiedAt: 2026-09-16T22:51:56+08:00 | exitCode: 0 | firstRedAt: (空)

# 一致性校验器实跑
✅ I1: 零 findings ｜ ❌ I2: 4 findings ｜ ❌ I3: 35 findings ｜ 汇总 39 findings  rc=1
（I2 命中 K3 P0-01 同型；I3 含「哨兵 50≠45」声明噪音）
```

**观察（非缺陷）**：`verify-system-registry.sh` 运行时会刷新 `docs/authority/system-registry.json`（我这次改动 2 行，已 `git checkout` 复原）——机器段刷新属设计内，但**巡检即脏树**，值得在实现侧注一句。

**#594/#595 关闭未合**（被 #598/#600 取代）= 双分支连坐，与 D782 报告披露一致。

---

## 4. D774 — 证据保鲜流水线

隔离树 `/tmp/acc-d774` @ main。

- 交付物 6 件全部 tracked：`rerun-evidence.sh` / `gen-expiry-warnings.py` / `run-machine-evidence.sh` / `check-progress-freshness.py`（D786）/ `tests/control-tower/rerun-evidence.test.sh` / `progress-freshness-watchdog.yml` ✅
- 测试：`rerun-evidence.test.sh` rc=0，断言全通过（报告口径 37 项）
- 证据落库（合并提交枚举）：GS×8（09-15，`3cf914f5`）+ GS×8（09-16，`1ce2235d`）+ test×2 ×2 天 + 汇总 + `evidence-expiry.json`（expired 2 / expiring 15 / scanned 41）✅
- **反向验证（存档证据）**：09-16 汇总 `GS-01/02/04 exit=1 且证据=yes`，`fail=3` → 显式失败 + 非零退出，不静默 ✅
- **幂等（我的复跑，连跑 4 次）**：文件总数恒 75；`product-progress.json`/`.html` 字节级不变；汇总与过期清单除 `generated_at` 外**逐键相等** ✅（口径应写「零新增文件 + 结论一致」，非「字节级幂等」）

**我环境受限的部分（诚实标注，不作为判据）**：该 worktree 无 `node_modules` → 我这一轮 GS 8 个场景**全部 exit=1 且未写证据**、A2 `exit=2` degraded → **不能**据此判 GS 真伪；GS-01/02/04 的失败结论取 09-16 存档证据 + K3 独立复现。

**重算副作用（重要）**：我的 refresh 把六态从 `42/21/1` 重算为 `40/24/0`（3 点掉态），根因见 §6.1/§6.2——**不是判分被改**（`calc-progress.py` 我未碰，工作树内改动全部未提交）。

---

## 5. K3 批次五审计复核（D789）

### 5.1 两条 P0 —— 我独立复现，**认同**

**P0-1｜门禁的"计划哈希"真相源绑在提交者本地工作树**（`pre-dispatch-check.sh:68-76`）

同一份文档（`派单-D788-…md`，`依据计划: v1.2@4e46603f（当前 main）`），两个真实树：

```
在 main 树（计划文件 sha256 前 8 位 = 4e46603f）:
  ── ⑩ 主线计划锚定 ──
  ✅ 派单已锚定计划 v1.2@4e46603f

在 #574 树（该树计划文件 = 8d331729，因 main 多一行「Win（代行）」）:
  ── ⑩ 主线计划锚定 ──
  ⚠️ 派单引用的计划哈希与当前不符（当前 v1.2@8d331729）——计划已更新，重读后再派
```

→ **同一文档、同一锚，判定随树翻转**。真相源必须是权威 ref（`origin/main:<path>` 或 blob SHA），本地树只能作显式降级；否则「树落后/树领先」都吃假红/假绿（假绿更危险：锚旧哈希可静默过）。**P0-1 成立**。

**P0-2｜文件名 glob 让真派单文档从旁边走过**（`check-dispatch-gate.sh:39` + `pre-commit-check.sh:333` 同款 glob）

```
该树 coordination 目录：匹配 ^派单-[^/]+\.md$ = 40 份；^审计派单- = 10 份（main 现为 11）

# 实弹：把一份真「审计派单-20260828-impl-done处置批.md」stage 进索引
$ git diff --cached --name-only --diff-filter=ACMR | grep -cE '^docs/synova/coordination/派单-[^/]+\.md$'
0
$ bash scripts/control-tower/check-dispatch-gate.sh
GATE-OK: 无派单文档变更(跳过)          rc=0     ← 门禁整段未执行

# PR #601（审计派单-批次五）的 CI：12 项 check-runs 全 success（含 TypeScript + Lint + Iron Laws）
```
规格原文（`编码指令-D778-…` §二.1）确实只写 `派单-*.md` → **实现忠实于规格，缺口在规格**，归因 `devdoc`（规格词）+ `control-tower`（覆盖面），**不归 implement**——K3 的归因我认同。**P0-2 成立**。

### 5.2 P1-03 夹具自锚定 —— 成立

`tests/control-tower/check-dispatch-gate.test.sh:35-37`：`PH=$(shasum -a 256 "$REPO"/docs/synova/coordination/整体推进计划-主线-*.md …)` 取**本树**哈希；夹具名 100% 自造（`派单-fixture-*.md` 6 次 / `派单-sandbox-fixture.md` 3 次），**从未用真实文档名做一次实弹** → 23/23 全绿是假安全感 ✅

### 5.3 B1-P1（证据静默丢弃）—— 我独立证实，且比 K3 描述更靠前

```
GS 证据实际落点: scripts/golden-scenarios/evidence/GS-0X-2026-09-15|16.json（16 份，git 已跟踪）
calc 输入集:     docs/synova/product-lines/evidence/*.json（calc-progress.py:77-83，glob 非递归）
→ 16 份 GS 证据对六态**零贡献**，且 `setdefault(ap, [])` 落空时零 warn / 零 degraded（静默）
```
→ 归 **control-tower**（证据契约/输入集口径）还是 implement（产出侧写错目录），**我判定为 control-tower**：产出方按脚本契约写「`<GS_DIR>/evidence/`」，是判分输入集与产出契约不一致。

### 5.4 通过项抽样（全部复现）

| K3 声称 | 我的复跑 | 判定 |
|---|---|---|
| F6 12 vs 0 | #404=0 / #590=12 / #592=12 | ✅ |
| 看门狗三态 | 0/1/2 + 边界 + JSON 损坏 | ✅ |
| C 组 W1/W2 9/0 | 9/0（且反向 7/2） | ✅ |
| ci.yml:259-264 doc-system 6 测试 | 实测 6 条 + `check-progress-freshness.test.sh` :265 | ✅ |
| 横幅 pre-commit-check.sh:1523 | `✅ 全部 13 组通过` 原样 | ✅ |
| A1 接线两处 | `d778_dispatch_gate` :383（CT-34 分支）+ :1476（主流程），定义 :331-347 | ✅ |
| #572 双模式 FAIL 一致 | 旧脚本 rc=1、新脚本 PASS | ✅ |
| 红线：产品仓库零写入 | 主工作区 HEAD/refs 未变；K3 提交仅在独立克隆分支（推到 GitHub 的是 PR 分支，main 无 K3 直推） | ✅ |

### 5.5 数字不实 1 处

K3 表「以 `派单-` 开头 20 份」→ 我在同一树 `ce1d9469`、同一 glob 实测 **40 份**（`审计派单-` 10 份，main 现 11 份）。**结论方向不变且更强**（被覆盖 40 vs 被绕过 11），但数字须订正。

---

## 6. 互不污染专项（五组交付交叉）

```
文件集: D774 26 / D786 24 / D782 28 / D788 13 / K3-572 51
两两交集: .claude/bypass.log ×5组 | .github/workflows/ci.yml (D786∩D782)
          docs/synova/coordination/ownership.yaml (D774∩D782)
          docs/synova/coordination/AUDIT-FINDINGS-LEDGER.md (D788∩K3-572)
```

- **ci.yml 并集未被互相覆盖** ✅：`tests/doc-system/*` 6 条（:259-264，来自 #593）+ `tests/control-tower/check-progress-freshness.test.sh`（:265，来自 #592）**相邻共存** —— D786 报告的「冲突→并集解」在 main 上实证成立。
- **ownership.yaml** ✅：`scripts/doc-system/**`、`tests/doc-system/**`、`scripts/product-lines/**`、`scripts/golden-scenarios/**`、`tests/control-tower/**` 均归 mac，两侧增补无覆盖。
- **pre-commit-check.sh** ✅：D782 块（:1402）与 D734 预算块（:1462-1473）并存，编号未冲突（都不并 13 组编号）。
- **队列风险（须按序）**：
  1. `#572 ∩ #604 = {bypass.log, AUDIT-FINDINGS-LEDGER.md}` → **第二个落地必冲突**，须 union 解（沿用 #572 上次的并集教训），勿整文件覆盖。
  2. `#584`（计划 v1.3）落地后计划哈希 `4e46603f → eef38a85`：所有只锚 v1.2 的派单在 §⑩ 转红。D788 的派单文档已写双锚（`v1.2@4e46603f｜v1.3@eef38a85`）→ 不受影响；其余历史派单会集体告警 → **#584 应与 P0-1 修复同批或先行说明**。

---

## 7. 本轮新发现（按严重度）

1. **【红线级·同类第二次】`alloc-task-id.sh:242` 全角 `$VAR` 崩溃**：`echo "… $BRIEF_FILE（填写后开工）"` → bash 把全角括号并入变量名 → `unbound variable`。单点复现：
   `bash -c 'set -u; echo "$BRIEF_FILE（x）"'` → 崩；`"${BRIEF_FILE}（x）"` → 正常。
   现场后果：分配器**先写 `task-state/D###.json` 壳、后在 brief 骨架处崩溃** → 会话拿到号却看到报错，重试即消费第二个号（号泄漏）。
   全仓 + CI 均**无** `$VAR`+全角边界常驻扫描（grep 零命中；`task-state/D738.json` 亦不存在）→ 该类缺陷第二次出现却无免疫细胞 ⇒ 按红线**升级创始人**。
2. **【P1】证据失效面过宽：线 18 的 `modules` 含 `memory/`** → 写一条 Note 就把 18-2/18-5 打成 stale（实证：09-16 19:31 #590 提交命中 `memory/`）。建议随 D790 一并把文档类路径从 modules 剔除。
3. **【P1】"1%" 是 14 天 TTL 边界假象**：19-2 靠 09-02 K3 证据在 09-16 恰好第 14 天存活，09-17 归零。含意：**K3 复核吞吐 = 仪表盘天花板**，不重跑/不复核，数字必然滑向 0。
4. **【P1】`alloc-task-id.sh` 生产路径可挂住**：本机 110+ worktree（含大量 prunable 死路径）时，我的首次调用 >60s 未返回（已 kill），改用注入缝才跑完 → 与「先登记后使用」纪律正面冲突（会话等不起就会跳过取号）。同属 #1 的修复范围。
5. **【P2】K3 表 item 4 例证不实**（`src/x.ts`+`scripts/y.sh` 同为 win 域 → PASS rc=0）；机制本身成立（真跨域 rc=1）。
6. **【P2】K3 表「派单-\* 20 份」应为 40 份**（同一 glob 同树）。
7. **【P2】会话身份不可从 git 区分**：多个 session 的分支提交 author 均为 `synova-dsh`（含 K3 的 `d294dbcf`），main 上则统一被 squash 成 `tangbaobao520` → 员工健康度无法按作者统计，建议各 preset 设独立 `user.name`。
8. **【P2】`verify-system-registry.sh` 跑一次即改 `docs/authority/system-registry.json`**（锚点刷新）→ 巡检/CI 里使用会造成脏树，建议加 `--check` 只读模式。
9. **【P3】测量纪律**：我首次跑看门狗用了非法时间戳造出 `rc=2`，第二次用合法值复测为 `rc=1`——已改；沿用「先证伪自己的测量，再判被测对象」。

---

## 8. 红线自查（验收方）

| 红线 | 本轮执行 |
|---|---|
| 不写产品代码 | ✅ 零产品代码改动；唯一产出 = 本报告 + Note + 队列/裁决建议 |
| 不碰 `scripts/audit/**` | ✅ 未读写（K3 红线） |
| 不写审计标准、不自审 | ✅ 本报告是**验收**（CTO 职责），审计结论仍归 K3；未改任何审计产物 |
| 所有产出进 git 受 K3 审 | ✅ 本文件入 PR |
| 隔离 | ✅ 9 个 `/tmp/acc-*` worktree（pinned SHA，互不复用），跑完删除；主工作区 HEAD `73110752` 未动 |
| 不改判分 | ✅ `calc-progress.py` 未碰；`/tmp/acc-d774` 内的重算产物**未提交、已丢弃** |

**遗留清理**：`/tmp/acc-*` worktree ×9、`refs/acc/{572,574,604}`、临时分支 `docs/cto-acceptance-batch5-20260917`（本 PR 用）→ 验收结束一并清理。

---

## 9. 待创始人裁决（4 条）

1. **#574（派单复核门禁）处置**：K3 两条 P0 我已复现认同 → 建议**先修后合**（P0-2 glob 升为语义判据 + P0-1 真相源改权威 ref + 两条金丝雀：真实文件名 / 落后树），而不是先合后补（上线第一天即假红/假绿，噪音导致绕过，正是 D778 编码指令自己写的风险）。
2. **K3 批五报告落地**：报告 + 台账建议 + D789 建议稿仍在 K3 独立工作区（分支已有 `d67b201d` 但未推）→ 建议**派 K3 自己推分支 + 开 PR**（它有 remote），CTO 只验收与合并；台账追加建议（CT-74…CT-79 候选）由我合并入台账。
3. **台账落点**：D786.json 在 #584、D788.json 在 #604、D789 待 K3 回填、D790-792 在 #602 —— 均有落点，只是**队列未合**；建议按 §6.4 的顺序合（#589 → #604 → #584 → #572 并 union 冲突）。
4. **D774 定时任务口径 + 一次性任务清理**：实测注册的是 `task-b748b2d7`（每 7 天，anchor 09-23 10:00，enabled），与 D774 报告写的 `task-760cbfdc` 不一致；另有 `task-3f4bcfc9`（D767，`once @2026-09-15T13:50Z`，已过期）→ 请裁决：以 `b748b2d7` 为准并删除过期一次性任务？（删改走看板 GUI 一步）

## 10. 可转发指令（复制即用）

**给 K3（批五报告落地）**
```
任务：把批次五审计成果从独立工作区推进产品仓库（一条 PR，docs-only）
1) cd /Users/wane/Synova-k3独立审计
2) git fetch origin && git status      # 当前分支 audit/k3-20260916-d788-precheck 上 HEAD=d67b201d（批五报告），尚未推
3) git log --oneline -3                # 确认 d67b201d = 批五 A/B/C/D 四组审计
4) git push origin HEAD:audit/k3-20260916-batch5-landing
5) 开 PR（base=main，标题：audit(K3): 批次五报告落库（门禁与度量 + 台账追加建议 + D789 task-state 建议稿））
   正文附：数字口径（cdeba8e9）+ 报告 file:line 清单 + 本报告 §5 的 CTO 复核结论（CTO 已复现 2×P0）
6) 顺带按你的建议稿回填 task-state/D789.json 的 audited 字段（audit 建议稿里已备好）
注意：一条 PR ≤12 审查面文件；docs/ 属治理产物不计入 → 预期 PASS
```

**给编码 session（P0 修复，待创始人点头后另开卡；勿直接改 #574 分支以免与在途会话撞车）**
```
任务：修 D778 派单复核门禁的两条 P0（K3 批五审计 §A1.5 / §A1.3，CTO 已复现）
P0-1 真相源：pre-dispatch-check.sh:68-76 的 ⑩ 项改为读权威 ref 的 blob
  期望：HASH=$(git cat-file -p origin/main:<plan-path> | shasum -a 256 | cut -c1-8)
  本地树仅作显式降级（打印 ⚠️ degraded + 原因，不得静默）；测试补金丝雀：
  「落后基线树里、锚定 origin/main 的派单 → 必须绿」
P0-2 覆盖面：check-dispatch-gate.sh:39 与 pre-commit-check.sh:333 的
  ^docs/synova/coordination/派单-[^/]+\.md$ 升为语义判据（路径含「派单」或正文含「依据计划:」即视为派单文档）
  测试补金丝雀：真实文件名 审计派单-* / 评估派单-* / 返修反馈-* 各一，必须被拦
验收：改前反向必红（现存 7/2 类断言保持）、改后
  bash tests/control-tower/check-dispatch-gate.test.sh → 全绿（并新增 ≥2 条断言）
  且用「审计派单-批次五-K3-20260916.md」做实弹 → GATE-OK 变为真复核
红线：不碰 scripts/audit/**；不改 13 组编号；一条 PR 只动 scripts/control-tower + tests/control-tower
```

**给创始人（看板一步）**
```
任务看板 → 定时任务：
 ① 确认保留「D774 证据保鲜：一键重跑（每 7 天 · Mac 侧）」（id 尾号 b748b2d7，锚点 2026-09-23 10:00）
 ② 删除已过期的一次性任务「D767 · B-18 运行时不变量注册表」（id 尾号 3f4bcfc9，触发点 2026-09-15 已过）
```

---

## 11. 未复现 / 环境受限清单（诚实留痕）

| 项 | 状态 |
|---|---|
| D774 的 GS 场景真伪（我这轮） | ❌ 未复现（worktree 无 node_modules → 8 个场景 exit=1 且证据=no，A2 degraded）→ 结论取 09-16 存档证据 + K3 复现 |
| D788「pre-commit 2s→3s」 | ⚠️ 未端到端测（仅测门禁本体 0.185s）；与本单结论无关 |
| K3「23/23 全绿」原始输出 | ⚠️ 未逐条重跑（我复跑了 9/0 与反向 7/2 等关键断言；全量由 #574 分支的测试套件承载） |
| K3 报告 §D 组（三机制） | ✅ 间接验证：`#585` 文件集仅 docs + brief + task-state → 无实现可审，NOT-AUDITABLE 成立 |
