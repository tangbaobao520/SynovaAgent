# D1000 K3 独立审计派单 — 切片 A 身份链 + D1001 改判建议件 + 越权样本

> 出件：Win-Codex-CTO ｜ 2026-09-25
> 审计方：**Kimi K3 独立审计线**（独立仓，maker 侧零写权）
> 前置裁定：Mac-CTO 2026-09-25《Win 侧撞号处置》（D948 → D1000；D947 为历史保留号）
> **执行方不判通过**；本单只请 K3 做独立核验。

---

## 一、审计对象（三条坐标 × 三方值，CTO 已核）

| 对象 | 分支 | HEAD（远端实测） | base |
|---|---|---|---|
| **切片 A（代码交付）** | `feat/d948-identity-chain-server` | **`59f52e3a`** | `ef5c8caa`（D947 PR-2 链尾） |
| **D1001 改判建议件** | `docs/d948-plan` | **`c14c1f45`**（建议件提交 = `cfedc975`） | `origin/main` |
| **越权样本（已作废）** | `fix/d949-ownership-electron-tests` | **`f828311c`** | `origin/main@6a714483` |
| 证据集（8 份） | `docs/d948-plan` | 同上 | `docs/synova/product-lines/evidence/D948-20260924/` |

**⚠️ 回执口径偏差（已知，供 K3 计分参考）**：执行方回执写证据分支 HEAD = `13688938`；远端实为 **`c14c1f45`**（其后追加了 `cfedc975`「按新规返工 D1001 建议件」）。差异来自返工提交，属**正常前进**，但**回执未同步更新**，请 K3 按远端值为准并核该偏差是否构成"回执与远端不一致"。

---

## 二、10 项必答

### 1. 🔴 切片 A 是否真的单域、且未碰 `scripts/**`
`git diff --name-only ef5c8caa 59f52e3a` 逐文件过 `check-ownership.py`；确认 `scripts/**`（含 `scripts/audit/**`）零交集、`src/middleware/auth.ts` 未被第二写者改动。

### 2. 🔴 A1–A7 逐条是否成立
| 判据 | 执行方声称 |
|---|---|
| A1 | `routes/auth.ts:137-148` 签发带 `department`；反例删字段 → `expected undefined to be 'marketing'` |
| A2 | `rbac.ts:132`（内联类型 :127 加 `department?`）改 `req.auth.department`；反例回 `undefined` → 9 failed |
| A3/A4 | 消费端已就绪，无 src 落点；**M8 过宽型**（`:286` 部门析取恒真）→ A4 红、A3 全绿 |
| A5 | `isSameDepartment:236-242` 零改动；M4 天真相等 → 7 failed；M5 `\|\| role==='manager'` → 16 failed |
| A6 | 拒绝分支 `:526-531` 原样保留；M3b → rbac 单元 9 红；M3c-a → HTTP 面亦红 |
| A7 | `routes/auth.ts:199` 刷新带 `department`；反例 → **仅 A7 红** |
**请逐条独立复现，特别是"A3 绿 / A4 红"这对反向关系的判别性。**

### 3. 🔴 A6 两面口径是否成立（**CTO 已裁定措辞，请核是否落实**）
CTO 裁定：**确认**「单元级为兜底类判别主体」，但要求两面**分列**，不得合并成一句"A6 已验"。请核：
- 单元级对「自报兜底复活」可红（9 红）
- HTTP 面对「自报优先复活」可红（M3c-a 独立复现，26ms）
- 回执是否把 **"构造性绿"** 明确归属到哪个面

### 4. 🔴 A5 边界 4 条是否真的能红
`d948-department-visibility.test.ts` L391/L398/L404/L411（大小写两向 + 空白两向）。执行方称变异体 **M-B1**（`isSameDepartment` 加 `.trim().toLowerCase()`，保留非空守卫）→ **恰好这 4 条红 / 141 绿**，且**补充前该变异体一条都不红**。请独立复现该"恰好"。

### 5. 🔴 全量 vitest 配对差分（**新增失败必须为 0**）
CTO 裁定：**只认配对差分，不认绝对数**（沿用 D947 K3 口径）。
```
@ef5c8caa（base） vs @59f52e3a（tip）同机同环境
要求：失败面逐文件比对「新增 0 / 消失 0」；tip 通过数增量归因给本卡新夹具
```
（base 失败集可直接复用你在 D947 终审报告 §一.1 的记录，勿重复跑 base。）

### 6. 🔴 未清项「未跑全量 / 未跑 CI」是否已闭合
执行方原话：**「CI 未跑 = 未验证 merge-ready」**。CTO 裁定要求补跑。请核补跑证据是否入库、是否为**真实退出码**。

### 7. 🔴 越权分支与建议件是否一致、是否确未合并
- 越权分支 `fix/d949-ownership-electron-tests @ f828311c` 改动 = `.github/CODEOWNERS` + `docs/synova/coordination/ownership.yaml` + `task-state/D949.json` + brief + hook 产物
- 执行方称：**内容一字不减搬进建议件 §1**，规则不破例、交付不缩水，**未删分支、未 force push**
- **请核**：① 建议件规则原文与越权分支的 `ownership.yaml` diff **逐字一致**；② 该分支**确未合并**（不在 main、无 PR）；③ 该分支卡号仍是旧 `D949`，是否构成残留风险

### 8. 🔴 功能回退登记口径
执行方登记：**「功能回退（部分恢复，未闭环）」**——W1 `department` 无生产写入者 / W2 `/mine` 恒 404 / W3 桌面无部门工作区消费点；**严禁写"部门可见性已可用"**。请核是否如实、是否有任何一处包装成"已知限制"或"已完成"。

### 9. 写集 / 文件数 / 域合规
- 切片 A：9 文件 = 8 声明 + `.claude/bypass.log`（D521 hook 产物）；域判定是否全 win
- D1001 建议件：2 件（证据目录 + 卡）；`domain:"mac" + owner_side:"win" + cross_domain_reason` 是否符合 Mac-CTO §四

### 10. 通用两条
① `git diff --name-only ef5c8caa 59f52e3a | grep scripts/` → 空？② 卡 `write_set` 与实际改动是否吻合、有无夹带？

---

## 三、CTO 已做 / 已裁（供 K3 抽验，不再重复劳动）

| 项 | 内容 |
|---|---|
| 收件取证 | 三条分支 SHA 远端实测（见 §一），与执行方声称一致（除证据分支前进一提交） |
| 裁定① | **CI/全量**：只认配对差分（§二.5），base 复用 D947 报告 |
| 裁定② | **A6 措辞**：确认"单元级为兜底类判别主体"，但两面必须分列 |
| 裁定③ | **`docs/tools.md` 立项**：**D1003**（Win 段，从 D1003 起取号） |
| 裁定④ | **越权分支**：保留为作废样本、**不合并**；内容转 D1001 建议件 |
| 已修 | `task-state/D1000.json` 的 `blocked` 字段（原写"待 CTO 在 P0-a/b/c 中裁定，本卡执行改表"——与 §〇之二 冲突，下游按卡读会读回被覆盖的旧指令） |

---

## 四、纪律红线（同 D947 审计单）

- 只读复核；不写 maker 仓任何文件；不碰 `scripts/audit/**`
- 结论只写「成立 / 不成立 / 无法核验 + 理由」，**不写"通过"**——通过与否由 CTO 收件闸 + 创始人定
- 变异体必须给**真实退出码**，禁 `--reporter=basic`（D947 已踩过一次假绿）
- 环境差异如实披露（D947 先例：远端不可达期间以本地对象 SHA 对齐）
- 计数与引用必须带「命令 + 口径 + 截至时刻」

---

## 五、交付

审计报告落 **K3 独立仓** `docs/synova/audit-reports/2026-09-25-K3-D1000.md`，并**按惯例推一条 `audit/k3-*-d1000` 分支到本仓**（台账与双仪表盘只读 git；D947 那份报告未推分支，已被登记为未清项，请勿重复）。
