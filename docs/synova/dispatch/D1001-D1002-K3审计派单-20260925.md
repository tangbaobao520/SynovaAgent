# D1001 + D1002 K3 独立审计派单（2026-09-25）

> 出件：Win-Codex-CTO ｜ 审计方：**Kimi K3 独立审计线**（独立仓，maker 侧零写权）
> 前置裁定：Mac-CTO 2026-09-25《Win 侧撞号处置》§一/§二/§四（D949→D1001、D950→D1002；D947 为历史保留号）
> **执行方不判通过**；本单只请 K3 独立核验。

---

## 一、审计对象（三条坐标 × 三方值，CTO 已 ls-remote 核过）

| 对象 | 分支 | HEAD（远端实测） | base |
|---|---|---|---|
| **D1002（代码交付）** | `fix/d1002-workspaces-route-order` | **`d29a1362`** | `feat/d947-middleware-default-posture @ 8b6c95dd`（**栈式**） |
| **D1001（证据件交付）** | `fix/d1001-ownership-renderer-tests` | **`ede364a3`**（merge 提交） | merge 父本 = `9a31fab8` + `3d04a492` |
| 派单件分支 | `docs/d949-d950-dispatch` | `124d41dc` | `origin/main` |

**D1002 提交链**：`cd97c8e9`（产品+测试）→ `eab08189`（PLAN/brief/卡）→ `78e6f36d`（收尾三件）→ 2×D521 auto 台账
**证据落点**：`docs/synova/product-lines/evidence/D1002-20260925/`（PLAN.md v1.3 + RECEIPT.md）；D1001 = `docs/synova/product-lines/evidence/D1001-20260925/`（建议件 + 证据集）

---

## 二、10 项必答

### 1. 🔴 D1002 写集与"守卫+重排同一提交"（**硬要求**）
CTO 实测：写集 **8 件**（回执称 7 件，**事实修正**）；`git show --stat cd97c8e9` = `workspaces-api.ts` + 2 测试文件，diff 内含 `:51` 短式注释 **与** F-1 整块前移 ⇒ 同一提交。
**请独立核**：守卫插入与路由前移是否真的在同一提交（拆开即退回）；写集到底几件。

### 2. 🔴 路由序终态
CTO 实测行号：`/by-dept`@**133** · `/mine`@**142** · `/conflicts`@**172** **全部 <** `/:id`@**199**。
**请独立核**，并核 F3 的**双探针**：源码腿（全字面量 needle / 行过滤比位次，**裸路径 indexOf 会被 `:116/:6` 注释假绿**）+ 运行时腿（`router.stack` 下标）是否成对、是否真有判别力。

### 3. 🔴 `/conflicts` 守卫的语义分层
`:172-174` = `if (!requireVerifiedRbac(req, res)) return;`。执行方声明三态：无凭据→401（全局门先拦）｜直接 handler 无 rbac→403｜合法 JWT→200。
**请核**：HTTP 面无凭据到底是 401 还是 403（取决于全局中间件），以及**移除守卫后 HTTP 面是否不红**（执行方称 M2 的判别腿在执行 F2-P 直接 handler 探针，HTTP 面仍 401 不受骗）。

### 4. 🔴 活体复跑
CTO 独立工作树实测：`Test Files 2 passed (2)` / `Tests 47 passed (47)` / **REAL_EXIT=0**（两个靶向文件）。
**请独立复跑并给真实退出码**；并按 D947 先例做**全量配对差分**（base `8b6c95dd` vs tip `d29a1362`，判据 = 新增失败 0 / 消失 0，**不认绝对数**）。

### 5. 🔴 变异体 M1/M2/M3 红态（**CTO 未逐条复现，交 K3**）
执行方声称：M1 移回顺序 → exit 1 / 8 红（含 `expected 10081 to be less than 4620`）；M2 去守卫 → exit 1 / **恰 1 红**（`expected 200 to be 403`，用例名 F2-P）；M3 权限回退 → exit 1 / 3 红（`expected 400 to be 403`）；三段还原后 sha256 全等 + `git status` 零残留。
**请逐条复现，特别是 M2 的"恰好 1 红"**——若多于 1 红，说明 M2 判别性不纯。

### 6. 🔴 `/by-dept/context` 哨兵是否真有判别力
断言：合法 JWT `GET /api/workspaces/by-dept/context` → **200 + `body.department === 'context'`**，注释写明「有意：by-dept 优先」。
**请核**：该断言在"注册序回退"时是否**必然红**（即它是不是真哨兵，而不是一条恰好绿的断言）。

### 7. 🔴 D1001 交付物与 Mac 单写者边界
CTO 实测：merge 父本 = `9a31fab8` + `3d04a492`（逐值对）；`git diff 9a31fab8 ede364a3 -- <D1001 三件>` = **空**（合并零改动产物）；`git diff origin/main...ede364a3 -- ownership.yaml CODEOWNERS check-ownership.test.sh` = **空**。
**请独立核**，并核：建议件里两条 glob 规则原文是否为**可直粘的 yaml**（缩进/键名/引号与表内既有格式同型）、"红→绿前后对照"两条命令与"测试补丁 58→60"是否**可直接被 Mac-CTO 照做**。

### 8. 🔴 D1001 的旧号残留与合规留痕
CTO 实测：建议件 0 命中 / 证据集 0 命中 / `D1001.json` **3 处**（`renumber` 说明 + `spec` 里的分支名 `docs/d949-d950-dispatch` + review 记录）⇒ 判为**合规留痕**。
**请独立核**这 3 处，并独立判其性质（含与 Mac-CTO §四「改号必须留说明」的一致性）。

### 9. 🔴 两卡共同：功能回退登记口径
- **D1002**：F1 部门分支在卡内**完全未验证**（HTTP 面与 handler 面均未验；**不采用 `syntheticRbac` 合成 ctx 证明**）；部门可见性语义归 **D1000 身份链后继卡**。
- **D1001**：Mac-CTO 落表前 **D1000 切片 B 不得开工**（硬依赖）。
**请核**以上两条是否在回执/建议件里**如实登记**、有无任何一处被包装成"已验证"或"已知限制"。

### 10. 通用两条
① 两分支与 `scripts/audit/**` 零交集？② 卡 `write_set` 与实际改动是否吻合、有无夹带？

---

## 三、CTO 已做（供抽验，不重复劳动）

| 项 | 内容 |
|---|---|
| 收件闸复算 | 两卡各 7–8 条逐项实算（原始输出见本件 §二 各行内嵌值） |
| 活体复跑 | D1002 两个靶向文件 → 47/47 passed / REAL_EXIT=0 |
| **事实修正 1 条** | D1002 回执称"7 件"，**实测 8 件**（漏算 `.claude/task-briefs/2026-09-25-D1002-workspaces-route-order.md`） |
| 未复现项 | M1/M2/M3 红态、`tsc exit 2 全基线`、F2-P 恰 1 红 —— **CTO 只核了可核性，未逐个真做变异**，本单第 5 项交 K3 |
| PR | D1002 → **#769**（base = `feat/d947-middleware-default-posture`，栈式）；D1001 → **#761** |

---

## 四、⚠️ 必须告知 K3 的环境事实：**两条 PR 都合不进 main，且非交付方责任**

```
#754  mergeable=false  ms=dirty      ← 假冲突
#755  mergeable=true   ms=blocked    ← 12 项必需状态检查，1 failing
#761  mergeable=false  ms=dirty      ← 假冲突
main tip = 0ab457f2
```

根因（CTO 已独立定位并上报 Mac-CTO，**均为 mac 域治理问题**）：

1. **`.gitattributes` 声明 `merge=union`，但该驱动只在本地 `.git/config` 注册** —— 本地 `git merge origin/main` 四次全部"自动合并成功、零冲突"，GitHub 四次报冲突。**GitHub 不执行自定义 merge driver** ⇒ 任何改过 `.claude/bypass.log` 的分支（= 全部分支）都判 `dirty`。
2. **main 挂 12 个必需状态检查，当前 1 failing + 2 expected** ⇒ 即使 `mergeable=true` 也合不动（#755 即此）。

**请 K3 在报告里把这层与"交付质量"分开计分**——否则会把系统性问题记在交付方账上。

---

## 五、纪律红线

- 只读复核；不写 maker 仓任何文件；不碰 `scripts/audit/**`
- 结论只写「成立 / 不成立 / 无法核验 + 理由」，**不写"通过"**
- 变异体必须给**真实退出码**，禁 `--reporter=basic`（D947 已踩过一次假绿）
- 环境差异如实披露；计数与引用必须带「命令 + 口径 + 截至时刻」

## 六、交付

报告落 **K3 独立仓** `docs/synova/audit-reports/2026-09-25-K3-D1001-D1002.md`，**并按惯例推一条 `audit/k3-*-d1001-d1002` 分支到本仓**（台账与双仪表盘只读 git；D947 那份未推分支已被登记为未清项，请勿重复）。
