# 派单｜D870 官方参照系学习计划 —— K3 复审（独立审计）

> 出件 2026-09-22 CTO ｜ 执行形态：**K3 独立审计（队外，不入任何小队）**
> **开工自检（必做，读不到就停）**：
> ```bash
> git fetch origin && git show origin/main:docs/synova/dispatch/D870-K3复审派单-20260922.md | head -5
> git cat-file -e origin/main:task-state/D870.json && echo "卡在"
> ```
> **读不到 → 停止并报 CTO**（不得凭转发文本开工）。

---

## 〇 本件信息

| 项 | 值 |
|---|---|
| 审计对象 | 分支 **`feat/d870-official-baseline-study` @ `e1af78f0`**（11 文件 / 4847 insertions / 0 deletions） |
| 卡 | `task-state/D870.json`（已登记） |
| 交付件 | `docs/synova/research/official-electron-kernel-runtime.md`(1008) · `official-ui-patterns.md`(1095) · `official-model-switching.md`(627) · `official-borrowing-recheck.md`(490) · `official-borrowing-table.md`(606) · `official-change-cards.md`(291) |
| M6 三件 | `D870-diff`(106) · `D870-verify`(465) · `D870-leftovers`(76)，均已在分支 |
| 参照系 | `/Users/wane/src/deepseek-harness/` @ `dsh-v0.1.6-alpha.2` / `ddefc45`（**只读**） |
| 交付红线 | 报告分支必须推送 + 附 `git ls-remote --heads origin \| grep k3` 回执（D859） |

---

## 〇-b CTO 收件核验（已做，K3 需独立重做）

| 检查 | CTO 实测 | 结论 |
|---|---|---|
| 回执一致 | 远端 head = `e1af78f0` | ✅ |
| 变更量 | `11 files changed, 4847 insertions(+), 0 deletions(-)` | ✅ 与更正版逐字一致 |
| 文件清单 | 6 被验件 + 3 份 M6 + `task-state/D870.json` + `.claude/bypass.log` | ✅ 上一版漏列的 2 项已补 |
| M6 行数 | diff **106** / verify **465** / leftovers **76** | ✅ 与更正版一致 |
| 红线 | 主树脏 0 / 参照系树脏 0 | ✅ |
| 自行更正 | 3 处（漏列 2 文件、遗留漏登 3 条、措辞把自查写成队员拦下） | ✅ 主动更正、逐条留痕 → **记功** |

---

## 〇-c 关键口径（CTO 裁定；K3 复算必须按此，两数各自用途写死在报告头）

| 数字 | 口径 | 用途 |
|---|---|---|
| **103** | **借鉴项计数** —— 剔除「官方无实现」5 条 | 「我们借了官方多少」的分母 |
| **104** | **总表行数** —— 含那 5 条，且每条**明标「官方无实现／我方自研」** | 「总表覆盖多少行」的分母 |

**要求**：两数**都必须能从文件里机械重算**（给命令 + 输出），且**表内每行要能判定属于哪一类**；否则复算对不上即判不合格。

**D 切片口径**：任务书 **14 项为主口径**；另补 **总纲 §P2-5「联网检索」为第 15 项候选，单列一行**，说明与 14 项的关系（**不并入 14 的分母**）。《可复制-DSH对比与任务书》附录里的「15」由此解释为**口径混用**，不是计数错误。

---

## 一 必核项（逐条给原始输出，禁转述）

1. **回执与变更量**：`git ls-remote` 原文 + `git diff --shortstat origin/main...e1af78f0`。
2. **四份对照表 100% 覆盖**：每条结论是否带**官方 `file:line`**；随机抽 **≥30%** 到参照系树**实际复现**（给出复现命令与行内容）。
3. **「官方无实现」5 条**：逐一验证确属官方无实现，且**未被误计为借鉴项**（这是 103/104 之差）。
4. **失效清单 11 条**：可复算（给判据与命令）。
5. **第二层数**：`17/104 = 16.3%`、合计 `112/199 = 56.3%`、复核率 `109/109 = 100%` —— 逐项复算。
6. **改法卡 D871–D910**：40 张**无缺号**、每张可归档（核心自研/可参照/可直接复制）。
7. **红线**：交付是否触碰 `src/**`、`scripts/control-tower/**`、`.github/**`；是否引入任何依赖；是否只读官方目录（工作树脏 0）。
8. **A 切片 9 面**：托盘面「官方零实现」是否**明标且不计入借鉴数**。

---

## 二 对抗点（K3 必须主动找反例）

- 抽样的 `file:line` 是否**真能在参照系树复现**（版本错配会整体偏移——表头须钉 `dsh-v0.1.6-alpha.2 @ ddefc45`）。
- 「官方无实现」是否被**换一种说法**混进借鉴项分母。
- 自验的**复核率 100%** 是否名副其实（抽 3 条独立重算，看是否真复算而非转录）。
- 40 张改法卡是否存在**重复卡号/空壳卡**。
- `D870-leftovers` 是否把「没做」写成「不做」（两者语义不同）。

---

## 三 判定与回执

- 结论只允许 **PASS / CONDITIONAL PASS / FAIL**；**CONDITIONAL PASS 的每个条件必须可证伪**（带文件:行与判据）。
- **「有条件通过」在 CTO 收件闸上等于未通过**（验收标准 §十一）。
- 报告落 `docs/synova/audit-reports/`，分支推送到远端，附 `git ls-remote --heads origin | grep k3` 回执。
- 红线：**不改产出**（只读 + 报告）；**不写审计标准**；发现问题只登记，不代改。

---

## 四 相关背景（不是本件范围，避免重复派工）

- 本件只审 D870 交付本身。**#674（研究院 8 份入库）已由 PR #700 落 main**（`dadc52b2`）→ D 切片里 29 处「来源未入库」标注**现已可复验**，请以 main 为准。
- 表内「来源未入库」若在 `dadc52b2` 之后仍标，属**陈旧标注**（不影响交付，但请登记）。
