# 派单｜D870 官方参照系学习计划（Electron 封装 / UI / 模型切换 / 借鉴重核）

> 出件 2026-09-21 CTO ｜ 执行形态：**agent 小队（队长用新预设 🎽 synova-squad-lead）**，编制 编码 3 + 自验 1 ｜ K3 队外
> **开工自检（必做，读不到就停）**：`git fetch origin && git show origin/main:docs/synova/dispatch/D870-官方参照系学习计划-20260921.md | head -5` → **读不到 → 停止并报 CTO**（不得凭转发文本开工）

## 〇 本件信息
写集：`docs/synova/research/official-*.md`（四份对照表）、`docs/synova/research/`、`task-state/D870.json`
**硬边界**：不改 `src/**`、`scripts/control-tower/**`、`.github/**`；**不引入任何依赖**；官方安装目录**只读**

## 〇-b 为什么（创始人 2026-09-21）
Synova **自己也是 Electron 封装** → 官方 Electron 是**同构参照系**：内核如何在 Electron 内顺畅运行、UI 如何美观流畅简洁、模型选择/切换如何做稳，官方已踩过的坑不必再踩。**前车之鉴**：第三方插件桌面端**在模型切换上翻车**；此前两次"参照系错"（官方 Agent Teams 早已存在 / 插件准入）。
**规矩**：**核心能力自研；非核心可学习、参照、复制**。

## 〇-c 复制边界（必须先申报，再动手）
| 类别 | 允许 | 不允许 |
|---|---|---|
| 核心能力（诊断/哨兵/专家/本体/度量/参数库） | — | **自研** |
| 工程与体验（Electron 生命周期/打包/更新/崩溃恢复、UI 模式、模型切换时序） | 学习/参照/**复制实现思路**（在 Synova 重写） | **直接引入外部依赖**（G1 守卫：`grep -rn "@deepseek-ai" src/ packages/` = 0 仍生效） |
| UI 形态 | 学**模式**（刻度条/状态栏/轨迹/产出卡片） | 照抄**开发者工具形态**（露出工作区/终端 → 攻击定价，撤回 8 仍有效） |

## 一 四个切片（可并行，写集互斥）
**A 内核在 Electron 内的顺畅运行** → `official-electron-kernel-runtime.md`
 覆盖 9 面：启动时序与生命周期｜主-渲染进程边界与 IPC 契约｜长驻（cron/哨兵/后台）与省电挂起｜崩溃恢复与重启｜资源与内存｜打包与签名｜自动更新｜离线/弱网｜多窗口/托盘。**每面至少 1 条可执行借鉴项**。

**B UI 美观/流畅/简洁** → `official-ui-patterns.md`
 信息层次与密度｜状态可视化（健康/进行中/失败）｜对话刻度条｜轨迹/思考过程｜产出卡片｜权限与模式选择器｜空态与降级态｜动效与滚动流畅感｜深浅色与可读性。**≥10 条模式**，每条给"我们改到什么程度"的可验收描述。

**C 模型选择与切换（重点）** → `official-model-switching.md`
 选择器（UI+配置）｜**会话中切换**时序与状态｜失败回退与限流｜上下文与工具能力差异适配｜持久化与恢复｜多 provider 一致性｜切换时消息与 token 计量一致性。**≥8 条必测用例**（含"会话中切换 + 工具调用中途切换 + 切换失败回退"）。

**D 借鉴重核（参照系失效检查）** → `official-borrowing-recheck.md`
 逐项核：任务书 **DSH 借鉴 14 项**｜`gap-line-mapping.yaml` **11 项**｜memory 借鉴卡 **B-11~B-21**｜**所有引用 DSH 路径的产物**（skills/preset/脚本/文档）。
 每项：官方对应物（存在/不存在/已变更）→ 建议（沿用/改实现/撤回）→ **官方 file:line 依据**。**覆盖 100%，零漏项**。

## 二 流程（M1–M6 + 18 条坑清单）
只读官方目录；产出全在仓库；每条结论带官方 `file:line` + 命令原始输出（**禁"我记得"**）；自验独立抽 **≥30%** 复算并写复核率；任何"要装包"的结论 → **只提立项卡**，本件不执行；8GB 机器**重型验证串行**；交付四份对照表 + **可执行借鉴总表**（按 核心自研/可参照/可直接复制 三类归档，条目 ≥40）+ 改法卡建议清单 + M6 三件 + `ls-remote` 回执。

## 三 完成判据
1 四份对照表 100% 覆盖、每条带官方 `file:line`；2 借鉴总表 ≥40 条并分类；3 自验抽样 ≥30% 独立复算；4 改法卡建议清单（给建议卡号/归线，不在本件改码）；5 M6 三件已提交（给路径）+ `ls-remote` 回执；6 K3 复审通过前不合并。

---

# 附录｜CTO 对小队五项待确认前提的答复（2026-09-21，解封依据）

1 **官方参照系路径**：授权自行定位并报回，候选与命令：
   `ls -d ~/Library/Application\ Support/*deepseek* ~/Library/Application\ Support/*harness* 2>/dev/null`；
   `ls ~/.dsh/ | head -20`；`ls "/Users/wane/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/" 2>/dev/null`。
   已知参考位：`/Users/wane/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/dependencies/dsh/`（其 `node_modules/@deepseek-ai/*` 为官方件）。
   **要求**：给候选路径 + 实测存在性，指明当前官方运行实例；**都不存在 → 停并报 CTO**（不得用第三方插件路径充数）。
2 **G1 守卫仍生效**：`grep -rn "@deepseek-ai" src/ packages/` = 0；"复制实现思路"可以，**直接引入依赖需 CTO 先申报变更**。
3 **D 切片底座（逐条核是否在 main 可读）**：任务书 14 项 → `docs/synova/research/研究院交接-20260918/任务书-{数据与可观测层,基础设施与信任层}.md`；11 项归线 → `docs/synova/product-lines/gap-line-mapping.yaml`；memory 借鉴卡 B-11~B-21 → `memory/notes/**`。
   **读不到即标"未入库=未交办"，停下报 CTO，不得凭转发文本或记忆补。**
4 **"第三方插件模型切换翻车"证据落点**：`docs/synova/research/研究院交接-20260918/复盘-插件推荐失误与准入标准-20260919.md`、`…/插件尽调-三个第三方控制插件-20260919.md`（同上：读不到即按未入库处理）。
5 **写集口径（确认）**：**取窄口径**——`docs/synova/research/official-*.md` **四份** + `task-state/D870.json`。

## 派单件位置规范（即日生效）
| 项 | 规定 |
|---|---|
| 目录 | **新派单件一律 `docs/synova/dispatch/`**；旧 `coordination/派单-*.md`（74 个）**不回迁** |
| 命名 | `<任务号>-<slug>-<日期>.md` |
| **发布判据** | **必须先在 main 可读**（`git show origin/main:<路径>`）；只在分支 = 未发布 |
| 开工自检 | 队长第一条动作：`git fetch origin && git show origin/main:docs/synova/dispatch/D870-官方参照系学习计划-20260921.md \| head -5` → 读不到即停、报 CTO |
