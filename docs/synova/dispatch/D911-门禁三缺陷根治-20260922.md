# 派单｜D911 控制塔门禁三缺陷根治（D708 误拦 / D733 无代行 / D749 判据漂移）

> 出件 2026-09-22 CTO ｜ 执行形态：**agent 小队（队长用专职预设 🎽 synova-squad-lead）**，编制 **编码 3 + 自验 1**（队长不下场写码）｜ K3 队外
> 依据计划: `docs/synova/coordination/整体推进计划-主线-20260913.md` @ `4e46603f`
> **开工自检（必做，读不到就停）**：
> ```bash
> git fetch origin && git show origin/main:docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md | head -5
> git show origin/main:task-state/D911.json | head -3
> ```
> **读不到 → 停止并报 CTO**（不得凭转发的文本开工）。**开工前还必须核 §〇-h 的前置门禁。**

---

## 〇 本件信息

| 项 | 值 |
|---|---|
| 卡 | `task-state/D911.json` |
| 队长 | **并行 CTO session**（持 🎽 synova-squad-lead 预设；同一时间只带一支队） |
| 编制 | 规格 0（本派单即规格）/ 编码 3（A、B、C 三切片各一）/ 自验 1（**不得由编码兼任**） |
| 审计 | **K3，队外**（不在编制内，永不入队） |
| 专用工作树 | 每名编码**各自一个** `git worktree`，路径开工时由队长分配（M1）；**不得复用任何在飞 session 的工作区** |
| 并行车道 | `mac-控制塔`（本件独占此车道 → 同车道一次只跑一件） |
| 权威件 | `docs/synova/coordination/裁定记录-门禁三缺陷-20260922.md`（三缺陷的实测证据，**必读**） |
| 验收标尺 | `docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`（v1.1，**特别 §十一 三态判定 + §十二 证据与回执硬要求 8 条**） |

---

## 〇-b 为什么派它 / 做完推动什么

| 项 | 内容 |
|---|---|
| **为什么是它** | **门禁开始阻止修复门禁。** 未合 PR 39 件中至少 5 件（#674/#675/#685/#696/#687）被同一族门禁缺陷长期卡死，而 **#696 恰是修 main 恒红的 PR**（main 的 `Vitest (1/2)(2/2)`、`Dashboard Auto`、`Product Progress` 三个 job 恒红）。缺陷①D708 同类**第 2 次**（第 1 次 #694，当时归因错误）；缺陷②域兜底误判同类**第 4 次**（D782 / D793 / D795 / D861）。实测证据见裁定记录。 |
| **做完推动什么** | ① 队列解锁：合法 PR 不再被误拦（D708 取对 D#）；② win 离线期 Mac 代行**有门禁可走**（不再需要"拆 PR"这类绕行）；③ 同一 PR 不再"过一个门禁必倒另一个"；④ 门禁判据不再随运行日漂移（今天红后天绿）。 |
| **不做会怎样** | 每交一件 PR 都要先跟门禁搏斗；合格件反复拆/改名/绕行 → **绕行成为常态 = 一个被绕过的门禁等于没有门禁**（AGENTS.md Loop Engineering 的核心教训，`--no-verify` 泛滥就是这么来的）。 |
| **可并行性判定** | **必须串行**：写集与 **PR #702**（拆自 #696 的控制塔侧）四件重叠 → 先等 #702 落 main。**必须串行**：`scripts/pre-commit-check.sh` 与 **D869**（门禁组 7 死检查）同文件单写者，两卡由 CTO 排期定先后，不得并行。**可并行**：A/B/C 三切片之间（写集逐文件互斥，见 §二）。 |

---

## 〇-c 红线（不可让渡）

1. **不改审计标准、不碰 `scripts/audit/**`**；本件产物**仍受 K3 审计**（开发者改过的门禁同样受审）。
2. **不新增门禁机制**：三条缺陷全是「去误拦 + 把判据钉死」。`standby`（代行开关）是**显式、单点、可逆、逐条打印**的开关，不是自动放行通道。
3. **禁 `--no-verify` / `git stash` / force push**；提交走 `scripts/control-tower/synova-commit`。
4. **禁把"门禁判红"改成"门禁不判"**：任何一处 fail-open 都必须带 `⚠️` 显式打印（铁律 11），且**该拦的仍要拦住**（每条夹具必须配反例，见 §三）。
5. **禁 grep 型判据**（验收标准 §十二 第 5 条，L3 实测 grep 型 3/5 = 60%）：每条"修好了"的证明必须是**先红后绿的夹具输出**，不是"搜到了某字符串"。

---

## 〇-d 旧信息漏洞自检（每条先复核再用）

| 本件引用的旧信息 | 自检要求 |
|---|---|
| 「#694 的根因是 PR 正文写法」 | **已作废**（见裁定记录 §缺陷①）。不得据此推断豁免通道"已经能用"。 |
| 「win 机器不在线，domain:win 一律 Mac 代行」 | 创始人 2026-09-20 口令，**有效**；但不得据此认为"可以随便改 win 文件"——代行仍须**逐条申报**。 |
| 「D860 已让治理产物不计预算」 | **分片核对**：`D860 治理产物豁免` 只在 **PR #702**，**main 尚未有**；开工前用 §〇-h 命令核。 |
| 「`tests/control-tower/ownership.test.sh`」 | **路径是错的**，实际是 `tests/control-tower/check-ownership.test.sh`（CTO 已核，D911 卡注释里也按错的那条被写了，以本处为准）。 |
| 「D864/D865/D870 已在卡表」 | **不在**：`task-state/` 无这三张卡（登记缺口，另卡补）。引号内若写"卡已登记"必是旧信息。 |

---

## 〇-e 卡面前提实测 + 执行方复核

CTO 出件前实测（命令 + 结果，**执行方必须自己再跑一遍确认**）：

```bash
$ python3 scripts/control-tower/merge_writeset_gate.py --base origin/main \
    --head origin/chore/ingest-institute --branch chore/ingest-institute   # 缺陷①
❌ 结论: block — 检测到 10 个写集外文件（夹带）   任务: D811   ← 错取 main 历史的号

$ ... 同 commit --branch docs/d849-ingest-institute                        # 反证
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）   任务: D849

$ cd <refs/pull/696/merge 的工作树> && python3 scripts/control-tower/check-ownership.py \
    $(git diff --name-only origin/main...HEAD)                             # 缺陷②
win  tests/agent/expert-file-loader.integration.test.ts
win  tests/circular-dependency.test.ts
win  tests/e2e/p0-wane-baby.test.ts
❌ FAIL 跨域: 变更落在 2 个域 ['mac', 'win']    EXIT=1
```

**执行方复核清单（逐条打勾写进自验记录）**：① 上述三条是否在你机器上复现出同样结论；② 若复现不出，**停手上报**（可能是 main 已变），不得按"文档这么说"开工。

---

## 一 三个切片（写集逐文件互斥，可并行；各自一个编码）

### 切片 A — D708 三缺陷（`merge_writeset_gate.py`）

| # | 缺陷（file: 符号） | 要求 |
|---|---|---|
| A1 | `infer_did()` 的 commit-subject 回退**越界** | 回退只允许扫 **`merge-base(base,head)..head`** 的 subject；**并入的 main 历史不得提供 D#**。扫不到 → 返回 `None`（走既有文档范围降级路径），**不得**退化为"猜一个号"。 |
| A2 | `find_declaration_files()` 从**本地工作树**读声明 | 改为从**被检查的 head 树**读（`git show <head>:<path>`）。工作树不在该分支时也必须正确对账。 |
| A3 | CI 未传 `--pr-body` → 豁免通道在 CI 不可用 | **本件不改 `ci.yml`**（单写者，见 §〇 硬边界）。改为：`resolve_pr_body_text()` 在 `--pr-body` 缺失且 `GITHUB_EVENT_PATH` 缺失时，**显式打印"豁免通道不可用"**（当前已有此提示；把它升级为**结论字段**而非尾注），并把「改用声明文件内 `## 写集豁免`」写进修复指引（该路径不依赖 PR 正文，已验证可用）。 |

- 写集：`scripts/control-tower/merge_writeset_gate.py`、`tests/control-tower/merge_writeset_gate.test.sh`
- 判别性夹具（**先红后绿**）：分支名无 D# + 历史含一次 `merge origin/main`（main 历史里带 D# 提交）→ 修前取错号（红），修后取不到号并走降级（绿）。
- **反例夹具**：分支名带 D# 但该卡不存在 → 仍必须 `exit 2`（无法判定），**不得**静默 pass。

### 切片 B — D733/D734「代行」机制（ownership + 域门禁）

| # | 要求 |
|---|---|
| B1 | `docs/synova/coordination/ownership.yaml` 增 `standby:` 段（**单点开关**）：当前仅 `win`（`offline_since: 2026-09-20`、`proxy: mac`、`authority: 创始人 2026-09-20 口令原文`）。**win 回归 = 删该段，立即恢复严格。** 改完**必须重跑 `--emit-codeowners`** 并提交 `.github/CODEOWNERS`（drift 由 `tests/control-tower/check-ownership.test.sh` 逐字节断言）。 |
| B2 | `scripts/control-tower/check-ownership.py` 暴露 standby 语义：`--owner mac` 之外新增「**代行断言**」路径（如 `--proxy win=mac`），并在输出中**逐条打印**被代行文件的归属与理由。 |
| B3 | `scripts/control-tower/check-pr-budget.sh` ②：standby 生效时允许 mac PR 携带被代行域文件，**但**要求 PR 正文 `## 代行声明` 段落逐条列「路径 — 理由」（同 D708 豁免风格：**无理由不生效**）；无该段 → **仍硬阻断**；放行的每条必须打印。 |
| B4 | 判据钉死：`standby` 段缺失/格式非法 → **fail-closed**（`exit 2`，不静默放行）。 |

- 写集：`docs/synova/coordination/ownership.yaml`、`scripts/control-tower/check-ownership.py`、`scripts/control-tower/check-pr-budget.sh`、`.github/CODEOWNERS`、`tests/control-tower/check-ownership.test.sh`、`tests/control-tower/check-pr-budget.test.sh`
- 判别性夹具：**修前** `check-ownership.py` 对 #696 的变更集 `exit 1`（已实测）；**修后**在同 PR 有 `## 代行声明` 时 `exit 0` 且逐条打印。
- **反例夹具**：① 无 `## 代行声明` → 仍 `exit 1`；② 删掉 `standby:` 段 → 立即恢复 `exit 1`（**这是 win 回归的可执行验证**）。

### 切片 C — D749/G12 口径统一 + 判据确定性（`pre-commit-check.sh`）

| # | 要求 |
|---|---|
| C1 | **口径统一**：组 12 的写集源与 D708 一致＝**三源并集**（S1 `task-state/*.json:write_set` ＋ S2 dev doc 写集表 ＋ S3 brief Q2）。优先复用既有解析器（`scripts/control-tower/brief_parser.py`、`devdoc_writeset.py`），**不新写解析器**。 |
| C2 | **判据确定性**：`today_files_by_prefix()` 的「今日」**改为以被检查提交的 commit date 为基准**（`git log -1 --format=%cI <sha>`），**不得**用 `date`（CI 运行日）。同一 commit 任何一天跑，判据必须相同。 |
| C3 | **禁止静默 fail-open**：`ALL_TODAY_BRIEFS` 为空时的整段跳过必须打印 `⚠️` 并写明"无认领 brief → 本组未执行"，且**在 CI（`SYNO_CI=1`）下改为硬阻断**（本地保持软提示，与 §四 现有 D515/D516 分层一致）。 |

- 写集：`scripts/pre-commit-check.sh`、`tests/control-tower/task-scope-g12.test.sh`（**新建**）
- 判别性夹具：同一 commit 用两个不同"今天"（`--today` 注入缝或 `faketime` 等价物）跑 → 修前结论不同（红/绿翻转），修后结论相同。
- **反例夹具**：文件确实不在三源并集内 → 必须仍硬阻断。

---

## 二 写集与互斥（M2）

| 切片 | write_scopes（逐文件） |
|---|---|
| A | `scripts/control-tower/merge_writeset_gate.py`、`tests/control-tower/merge_writeset_gate.test.sh` |
| B | `docs/synova/coordination/ownership.yaml`、`scripts/control-tower/check-ownership.py`、`scripts/control-tower/check-pr-budget.sh`、`.github/CODEOWNERS`、`tests/control-tower/check-ownership.test.sh`、`tests/control-tower/check-pr-budget.test.sh` |
| C | `scripts/pre-commit-check.sh`、`tests/control-tower/task-scope-g12.test.sh`（新建） |
| 自验 | `docs/synova/product-lines/evidence/D911-verify-20260922.md`、`docs/synova/product-lines/evidence/D911-evidence-20260922.md` |
| 队长（收尾） | `docs/synova/coordination/收口-D911-20260922.md`、`docs/synova/product-lines/evidence/D911-leftovers-20260922.md` |

- **互斥自检**：A/B/C 三行两两**零交集**（逐文件比对，禁前缀包含误判）；自验/队长行不得与编码行重叠。
- **规格交付件必含**：若发现"必须改但不在写集"的路径 → 写进 `D911-leftovers`，**不自行扩写集**（队长也不得擅扩）。
- **队内任务逐张收口**：官方不自动释放归属，收尾时由队长逐张 `complete`（M6）。

---

## 三 验收判据（逐条可证伪；**禁 grep 命中**）

1. A1/A2/A3 三条各自**先红后绿**夹具输出（贴原始输出，含命令 + 退出码 + 关键行），且**每条带反例夹具**输出。
2. B 三条同理：修前 `exit 1` / 修后 `exit 0` 的**原始输出**，加两条反例（无 `## 代行声明` 仍红；删 `standby` 段立即恢复红）。
3. C 两条同理：跨日结论不变（贴两次运行输出），加反例（不在并集内仍红）。
4. **端到端反向验证**：把 **#674 的原分支**（`chore/ingest-institute`，分支名无 D#、历史含一次 `merge origin/main`）当夹具跑一次 `merge_writeset_gate.py` → **修前 block（已实测）、修后必须 pass**。这一条是"队列真的解锁了"的唯一硬证据。
5. `bash scripts/pre-commit-check.sh` 与 `scripts/control-tower/check-pr-budget.sh` 在 main 上**零新增红**；`tests/control-tower/` 既有测试全绿（含 B 切片重生成的 `CODEOWNERS` 逐字节 drift 断言）。
6. 自验独立抽 **≥30%** 复算并写**复核率**；自验**新发现**的问题**单列**（不计入"已发现"）。
7. M6 三件齐：最终 diff（路径清单 + 关键差异说明）/ 自验结论（含反向验收证据）/ 遗留清单（文件:行 + 为什么不修）。
8. 交付分支推送 + 附 `git ls-remote --heads origin | grep D911` 回执。

**三态判定（验收标准 §十一）**：只允许 `通过` / `退回` / `关闭`；**「有条件通过」＝不通过**；退回**只约束本件**，不冻结无依赖的其他任务；闭环必须＝卡号 + 责任人 + 可验证证据。

---

## 四 工具副作用核对（M4）

| 指令 | 工具 | 默认副作用 | 结论 |
|---|---|---|---|
| 提交 | `scripts/control-tower/synova-commit` | **自带 push** | 不得写"不要 push"；只本地提交用只读命令 |
| 分片验证 | `bash scripts/pre-commit-check.sh` | 只读检查，本地软提示 | 可反复跑 |
| 门禁脚本自测 | `bash tests/control-tower/*.test.sh` | 只读（夹具在临时目录） | 8GB 机器**重型验证串行**（一次一个切片跑） |
| 产出落盘 | `git worktree` 隔离 | 写仓库内新目录 | 证据文件按 §二 归属命名，**不写主树**（M5，F8 教训：跑回归会改写主树两文件） |

---

## 五 审计边界（M3）

- 队内角色叫**自验**；结论只允许 `可提请独立审计` / `退回（附理由）`。
- **禁用措辞**：审计通过 / 审计合格 / 已通过审计。
- K3 审计对象 = **已推送产物**（分支/PR），不审工作区。
- **开发者改过的门禁同样受 K3 审**（红线）；CTO 作为队长的交付**不得自判通过**。

---

## 六 通用坑清单（本件高相关条目，全文见派单模板 §〇-f）

1. **改门禁前先跑一遍原门禁**，把"修前行为"存证——否则改完无法证明是"修好"还是"改没了"。
2. **`grep` 型判据一律不算证据**（L3 实测 3/5 = 60%）；用夹具的退出码与输出。
3. **fail-open 必须显式打印**，禁止静默放过（铁律 11）。
4. **单写者**：`.github/workflows/ci.yml` 不在本件写集；`scripts/pre-commit-check.sh` 与 D869 串行。
5. **bash 版本**：macOS 自带 bash 3.2，**无关联数组**；`declare -A` 会静默失败。
6. **Windows 兼容**：`scripts/` 下脚本改了要按 `windows-compat` 模式库核 PATH/UTF-8/退出码（CI 有 windows-latest 腿）。
7. **改 `ownership.yaml` 必须重跑 `--emit-codeowners`**，否则 drift 测试红。
8. **不动 `bypass.log`**（内置豁免的运行期账本）；合并冲突按**并集**解。
9. **禁止 `git stash`**（D312 事故）；用 worktree 隔离。
10. **8GB 机器**：重型验证（pre-commit 全量、vitest）**串行**，不并发。

---

## 七 域与代行（§〇-g）

- 本件全部文件落 **mac 域**（`scripts/control-tower/**`、`docs/synova/**`、`tests/control-tower/**`）→ 单域，**不需要代行**。
- 但**本件的产物正是给代行开路的**：切片 B 落地后，win 离线期的 Mac 代行 PR 走 `## 代行声明` 即可进门。
- **win 回归信号**：创始人说「win 回归」→ 删 `standby` 段 + 重跑 `--emit-codeowners` + 跑反例夹具②确认恢复严格。

---

## 八 开工自检命令（§〇-h，必跑）

```bash
# ① 派单件与卡必须先落 main（读不到就停）
git fetch origin
git show origin/main:docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md | head -5
git show origin/main:task-state/D911.json | head -3

# ② 前置：PR #702 必须已合且其内容在 main（"已合"不看 PR 列表状态，看物理存在）
git show origin/main:scripts/control-tower/check-pr-budget.sh | grep -c 'D860'      # 期望 ≥1
git show origin/main:scripts/control-tower/merge_writeset_gate.py | grep -c '可粘贴'  # 期望 ≥1

# ③ D869 状态（同文件单写者）——由 CTO 在 §〇 派单前确认并在群内公告先后
ls task-state/D869.json 2>/dev/null && echo "D869 卡在，需确认先后" || echo "D869 卡未登记"

# ④ 三缺陷复现（§〇-e）
python3 scripts/control-tower/merge_writeset_gate.py --base origin/main \
  --head origin/chore/ingest-institute --branch chore/ingest-institute   # 期望 block + 任务: D811
```

**任一条不符 → 停止并报 CTO**（不得按"文档这么说"开工；派单出件与开工之间 main 会变）。

---

## 附录｜可直接转发给队长的指令（创始人复制用）

```
你是 D911 小队队长（预设 🎽 synova-squad-lead）。派单件：docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md
（在 main 上；读不到就停并报 CTO）。

第一步（不写码）：git fetch origin && 跑派单 §八 四条开工自检命令，贴原始输出回我。
第二步（不写码）：按 §一 把 A/B/C 三切片分给 3 名编码，各自 git worktree（M1），写集按 §二
逐文件互斥，把"给每人的指令"发我看（每人指令里必须含：角色授权条款 §三、写集、验收判据、
反例夹具要求）。
第三步（等编码）：队员交件后交 1 名自验（不得由编码兼任）独立复核 ≥30%，写复核率。
第四步：收口 M6 三件 + 队内任务逐张 complete，交我。

红线：不下场写码；不判"通过"（只出"可提请独立审计 / 退回"）；不擅扩写集；
不引用未落 main 的路径；不做一次性特例。上限：成员 ≤4；重型验证串行。
```
