# 派单 — K3 独立审计：D947（Win 首模块 `src/middleware/**` 默认安全姿态）

- **派单方**：Win-Codex-CTO（收件闸已过）｜ **执行方**：K3 独立审计线（**独立仓，maker 侧无写权**）
- **性质**：**必过终审**——本单删除了"客户端自报身份"通道，直接改变鉴权语义，属安全面变更，**有条件通过 = 未通过**
- **纪律**：报告须含 `file:line` + **复现命令 + 原始输出**；执行方**不判"通过"**；引用的 `path:line` 当场复核（本单已实测一例行号漂移）

---

## 一、交付物与基线（可复现坐标）

| 项 | 值 |
|---|---|
| 分支 | `feat/d947-middleware-default-posture` |
| 工作树 | `.synova-wt-d947-middleware` |
| base | `origin/main` = `f25e61eb`（栈式：PR-2 的 base = PR-1 链尾 `313150bf`） |
| 提交 | PR-1 `77e0d6aa`（8 文件）｜A2 fixup `313150bf`（2 文件）｜PR-2 `2ea4df10`（9 文件）｜治理/证据 `e01d1345` |
| 写集 | **17 代码/测试文件**（CTO 冻结口径）→ **PR-1 = 8** ｜ **PR-2 = 9** ｜零重叠；`src/server.ts` 单写者 |
| 证据 | `docs/synova/product-lines/evidence/D947-20260924/`（**13 份**，含 PLAN / RULINGS / 四份关卡报告 / 回执） |
| 回执 | `D947-RECEIPT.md`（十二节，结论形态 = `可提请独立审计`） |
| 派单件 | `docs/synova/dispatch/D947-win-middleware-default-posture-20260924.md`（字节 14505 / 行数 177 / sha256 前16 `1f74c0703d279de6`） |

**关键 diff 面（CTO 实测）**：
```
PR-1 77e0d6aa  = src/middleware/{auth,rbac}.ts + tests/middleware/{auth,rbac,rbac-default-deny,permission-filter}.test.ts + tests/routes/{ga-auth,diagnosis-report-persistence}.test.ts
PR-2 2ea4df10  = src/{server.ts,services/request-context.ts} + src/routes/{workspaces-api,department-workspace,documents}.ts + tests/routes/{department-workspace,overflow,middleware-order,workspace-access-write-endpoint}.test.ts
相对 f25e61eb 合计 = 17 代码/测试 + 1 治理（.claude/bypass.log，钩子自动写）
```

---

## 二、CTO 收件闸结论（四判据 + 我自己复现了什么）

**结论：通过收件闸，可提请 K3 终审**（附未清项）。

| 收件闸判据 | 我的核验 |
|---|---|
| ① 锚定 | 回执 §五 逐条对应派单 §二 的 P0–P8 + N1；§〇 探针带退出码 |
| ② 声称↔证据 | 逐条有命令 + 原始输出；**我独立复现 8 项**（见下） |
| ③ **反例夹具"改坏即红"** | **我亲自做了一次**（非采信其结论） |
| ④ 团队成员运行记录 | 回执 §十：6 人 × 状态 × 共享任务 id；**队长未代跑成员命令**（各成员原始命令与退出码由本人回传） |

**CTO 亲自复现（原始输出）**：

```
① 文件面：PR-1 = 8 / PR-2 = 9 / 合计 17 + 1 治理     → 与冻结口径逐值吻合、零重叠
② P2 唯一漏斗：grep -rn "conditions: \[\]" src/ → 0
③ P7 自欺点：grep -rn "admin::dev" src/ → 0
④ P1 兜底语义：rbac.ts:139 return { role: ANONYMOUS_ROLE, userId: ANONYMOUS_USER, authenticated: false }
   + 上方 log.warn({code:'RBAC_DENIED', reason:'no_authenticated_context'})  → fail-closed + 留痕
⑤ P0 残留意：x-synova-token 非注释命中 6 处 → 5 处注释 + 1 处（auth.ts:503）"检测到自报头即拒绝"
⑥ P7 残留意：manager: 4 处 = 函数参数名/访谈引擎角色权重/一行注释 → 非自报残留
⑦ P4 顺序：jwt@344 → rbac@366 → homeRoutes@376/workspacesApi@380；rbac 之前 13 条挂载 = 5 全局 + 3 静态 + 豁免集 5 组
⑧ 测试（真实退出码）：新建夹具 4 文件 4 passed/55 tests（exit=0）；存量中间件 2 文件 2 passed/92（exit=0）；存量路由 4 文件 4 passed/29（exit=0）

【CTO 自做变异体】把 rbac.ts:139 兜底改回 { role:'admin', userId:'dev' }
  → npx vitest run tests/middleware/rbac-default-deny.test.ts → exit=1 ｜ 7 failed | 2 passed   ✅ 改坏即红成立
  → 用变异前备份还原 → exit=0 ｜ 9 passed ｜ git diff 空 ｜ git status 空   ✅ 逐字节还原
```

**CTO 自曝**（供 K3 一并审）：
1. 本单**我自己的派单有两处硬错**，由执行方在 PLAN 阶段抓出：P4「前 12 组 → 全 0」字面不可达（唯一路径会破坏 `rbac.ts:102` 对 `req.auth` 的依赖）；P2 的"=0"在 12 文件写集内不可达。
2. **我把总盘子算错**（写 16，实为 17），由执行方指出。
3. 我做变异体时：① 用了 vitest 4 不存在的 `--reporter=basic` → 测试没跑却因 `tail` 吞退出码显示 `exit=0`（**假绿**），自查后重跑；② 还原时 `git checkout` 被沙箱挡在 `.git/index.lock`，改用备份还原并验证逐字节一致。

---

## 三、必须回答的问题（逐条，缺一不回）

1. 🔴 **全量套件 `16 failed | 595 passed | 3 skipped` 是否为"前置已存在"？**
   执行方归因为环境/域外（`zustand` 缺失、`@modelcontextprotocol/sdk` 缺失、`hookTimeout` 10s < 本机 bootstrap ≈15s、架构基线棘轮超时），证据是"失败面与 PR-1 轮次**逐文件相同**（新增 0 / 消失 0）+ 失败信息区 696 行扫 9 个改动面模式**全 0**"。
   **但他们自己登记：未做 `f25e61eb` 前置版本对照跑，不宣称已证明预先存在。**
   → **请 K3 在 base `f25e61eb` 上用第二工作树跑一次全量**，判定"新增 0"是否真成立。**这是本单最大的残余不确定性。**
2. **P3 × P4 成对验收**：回执声称二者在**同一次轮次内**给出红→绿对照（缺一即退）。请独立复现该成对性（P4 绿而 P3 未接线 ⇒ 未完成）。
3. **P2 跨两 PR 达成**：`conditions: [] = 0` 由 PR-1（`auth.ts`）+ PR-2（`documents.ts`/`request-context.ts`）**两批合起来**达成 ⇒ 回执自行登记"任一单 PR 不得单独声称 P2 已完成"。请判定该口径是否成立、合并顺序是否必须 PR-1 先。
4. **两条功能回退是否可接受、是否必须立卡**：
   ① JWT 用户**部门级可见性恒 deny**（`JwtPayload` 无 `department`）；
   ② 桌面 dev-seed 旁路失效 → `/api/ga/*`、`/api/audit`、`/api/enterprise`、`/api/ga/calibration`、`/api/ga/corrections` **返 401**，而 `RightPanel.tsx:155-159` / `ga-collab.ts:44,92` **仍在发 legacy 头**。
5. **E-2（低危）**：`PUT /:id/merge` 判据顺序 = 首层验签 403 → 形状校验 400/404 → `canModifyWorkspace` 403 ⇒ 已认证低权用户打**非子工作区** id 得 400 而非 403。请裁量是否可接受。
6. **F-1 未清**：`GET /api/workspaces/mine` 与 `/conflicts` 被 `router.get('/api/workspaces/:id')` 遮蔽 ⇒ **HTTP 恒 404**，且**先于本卡存在**。CTO 已裁 (b)「本卡不修」（修之使 `/conflicts` 由 404 变可达 = 可达面扩张），现以登记性断言锁现状。请复核该裁定。
7. **L-27**：P4 源码断言锚**标识符**（卡面硬要求"禁硬编码行号"）⇒ `rbacMiddleware` 行号区间无夹具守护（实测 366 落在 [357,366] 内）。
8. **L-11**：`department-workspace.ts:75` 页内自发送自报头**零测试覆盖** ⇒ 删头后的行为变化无守护。
9. **写集/PR 划分合规**：是否符合 CTO 冻结口径（17 / PR-1=8 / PR-2=9 / 零重叠 / `server.ts` 单写者）；卡面曾写"PR-2 恰好 11 文件"（**E-1 口径过期**），实测 9 —— 判定以哪份为准。
10. **通用两条**：① 与 `scripts/audit/**` 是否**零交集**（K3 红线）；② 卡内 `write_set` 与实际改动是否**一致（无夹带）**。

---

## 四、已知未清项（CTO 逐条登记，不得读作"已完成"）

1. **未推送 / 未开 PR / 未合并** —— 全部交付本地留存；PR-2 的 base 只能是栈式（= PR-1 链尾）。
   （**注：此条自本派单发出起已有变化** —— Win 侧已恢复写通道，可推分支；合并仍待 token。）
2. 回执 §九 共 13 条未清项（两条功能回退、F-1、全量套件、存量 tsc 36 行、`squad-discipline` §16「成员 ≤4」与 6 人编制的口径冲突、8 份 evidence 未登记 `DOCS-REGISTRY` 等）。
3. 派单件**未落 `origin/main`**：执行期按派单 §〇.3「本地优先例外」，指纹三方对账一致（14505/177/`1f74c0703d279de6`）；落 main 后须复核（保险 3）。

---

## 五、审计输入的可复现命令

```bash
cd .synova-wt-d947-middleware
git log --oneline f25e61eb..HEAD
git diff --name-only f25e61eb 2ea4df10
npx vitest run tests/middleware/{rbac-default-deny,permission-filter}.test.ts tests/routes/{middleware-order,workspace-access-write-endpoint}.test.ts
npx vitest run tests/middleware/{rbac,auth}.test.ts tests/routes/{department-workspace,ga-auth,diagnosis-report-persistence,overflow}.test.ts
grep -rn "conditions: \[\]" src/ --include='*.ts' | wc -l        # 期望 0
grep -rn "admin::dev" src/ | wc -l                                # 期望 0
bash scripts/pre-commit-check.sh                                   # 期望 13 组通过
# 前置对照（问题 1）：在 base f25e61eb 的第二工作树上跑全量套件
```

**取证坐标**：所有证据件在 `docs/synova/product-lines/evidence/D947-20260924/`；本派单落 `docs/synova/dispatch/`。
