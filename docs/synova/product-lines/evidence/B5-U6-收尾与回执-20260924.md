# B5-U6 收尾三件与小队回执（spec 形态归一 + 消费者显式降级）

> 卡号 B5-U6（CTO 裁定"本卡内做，不另立卡"）｜ 执行形态：Agent Teams 小队（队长 + 1 编码 + 1 独立自验）
> 分支 `fix/b5-u6-spec-unify` ｜ base `origin/main` = 9f0c8f65 ｜ **自验锚点 tip = `7f31f417`**
> 本文件由**队长**（synova-squad-lead）撰写，属治理产物（`domain_neutral`），非产品代码

---

## 一、起因与机制订正（**我卡面写错，b5-c1 实测推翻**）

| 我写的 | 实测（b5-c1 + b5-v 双独立复核） |
|---|---|
| 「string 形态被上方 `except Exception` 吞掉 = **静默失效**」 | ❌ **错**。`gen-cto-health.py:305` **不在任何 try 内**（最近 try `:248–257` 已在 `:305` 前闭合）→ 异常逐层穿出 `main` → **`exit=1` + traceback + stdout 0 行**（整个看板不生成）= **致命崩溃**，不是静默 |

**真正"看不见"在调用方**：`scripts/control-tower/pre-audit-summary.sh:74` `python3 - … >/dev/null 2>&1` **双丢 stdout+stderr** → U3-artifact-repro 门只留"未过"，**故障可归因性丧失** → 记 **U12（写集外）**。

**影响面（比派单假设更严重，双人独立实测）**：基线 `spec` 形态 `dict 81 / NoneType 159 / str 118` → **118 张 string 卡 ⇒ main 上生成器跑不完**；本卡改动后**首次跑通**。

---

## 二、团队成员运行记录

**token 列**：本会话工具链未暴露用量 → 按"数字禁手写"纪律**留空，不伪造**。

| 成员 | 角色 | 状态 | token | 共享任务 | 状态 | 写集 |
|---|---|---|---|---|---|---|
| `lead` | 队长（协调/判前提/验证据，不下场写码） | running | 未暴露 | — | — | 本文件 |
| `b5-c1` | 编码（spec 归一 + 消费者降级） | inactive 已交付 | 未暴露 | **task-10** | **completed**(rev 3) | 四卡 + `gen-cto-health.py` + 其测试 + 证据件 |
| `b5-v` | **独立自验**（只读 + `/tmp`） | inactive 已交付 | 未暴露 | **task-11** | in_progress→已报 | `B5-U6-自验.md` |

**自验不由编码兼任** ✅ ｜ 写集交集 **0** ✅ ｜ 重型验证串行 ✅

---

## 三、交付内容与判据

**10 文件 / +460−6**，内容提交 `75ff76c2`（+ hook 提交）+ `106947ee`（证据件补写）
- 四卡 `spec` → `{"path": …}`；D928/D933 的 `at/by/in_main/note` **原样**迁 `spec_meta`（逐键一致）
- `gen-cto-health.py`：string/非 dict → **显式 `degraded:`**（stderr + **报告正文双可见**）+ **向后兼容**（形如单一路径仍按路径用；**散文/多路径不得当路径**）
- `gen-cto-health.test.sh`：+6 条判别性断言

| 判据 | 结果（b5-v 独立） |
|---|---|
| A | ✅ 4/4 `spec` dict + 非空 path；`spec_meta` 与 main 基线逐键一致；全键 diff 仅 `spec`（无旁及） |
| B（核心判别性） | ✅ **main 版**：`exit=1` / stdout **0 行** / `AttributeError@:305` → **改后版**：`degraded:` 显式 + 报告 spec 列 ✅（path 仍采用）；散文样本 → 显式 degraded + **不当路径** |
| C | ✅ `ci.yml` 与 `alloc-task-id.sh` **均未出现**（禁改核验通过） |
| D | ✅ 三测试全绿 `13/0`、`7/0`、`Ran 5 OK`；**新测试 × 旧脚本 → 红**；**旧测试 × 旧脚本 → test.sh 中断 exit=1 / repro 2 通过 5 失败**（与 c1 报数逐字吻合） |
| E（仅登记） | `grep -c gen-cto-health ci.yml` = **0** → **M9 第③件待办**（非本卡引入） |
| 机制订正 | ✅ 独立复核成立 |
| **爆炸半径** | ✅ 独立复现：24 候选名集**完全一致**；形状敏感访问**仅 `gen-cto-health.py`**（`:241/:248/:251/:523`）⇒ **爆炸半径 = 1** |

**🔴 关键新发现（b5-v，`N5`）**：生产者 `alloc-task-id.sh:175` 骨架模板**硬编码 `"spec": null`** → **新建卡永不产生 `{"path":…}`** ⇒ 解释了 159 张 NoneType，并**预示"只归一存量会持续再产生"**。该文件在 **D940 写集** → **仅登记，未动**；已转 D940 编码A（见 §六）。

---

## 四、`git diff --stat` 与 ls-remote 回执

```
$ git diff --name-only origin/main...HEAD
  .claude/bypass.log
  .claude/task-briefs/2026-09-24-B5-U6-spec-unify.md
  docs/synova/product-lines/evidence/B5-U6-改动清单.md
  memory/notes/proposed/2026-09-24-b5-u6-spec-unify.md
  scripts/control-tower/gen-cto-health.py
  task-state/D928.json / D933.json / D934.json / D936.json
  tests/control-tower/gen-cto-health.test.sh

# ls-remote 回执（队长亲跑）
$ git ls-remote --heads origin fix/b5-u6-spec-unify
 7f31f41798ea885084543c67e1a18dcf5603f703	refs/heads/fix/b5-u6-spec-unify
$ git rev-parse HEAD
 7f31f41798ea885084543c67e1a18dcf5603f703
★ 远端 tip == 本地 HEAD（真已推送）
```

**两处写集外新增（队长追认）**：`2026-09-24-B5-U6-spec-unify.md`（**G12/D749** 硬门禁：改 `scripts/` 须认领 brief + 机器块；生成名还被 `.gitignore:65` `*-auto.md` 吃掉）｜`memory/notes/proposed/2026-09-24-b5-u6-spec-unify.md`（**commit-msg D395-a/D534** 硬门禁：须引用真实存在的 Note）。**均门禁倒逼，非自选扩范围。**

**收口提交后 artifact 不变性（可核）**
```bash
git diff 7f31f41798ea885084543c67e1a18dcf5603f703..HEAD -- \
  scripts/control-tower/gen-cto-health.py tests/control-tower/gen-cto-health.test.sh \
  task-state/D928.json task-state/D933.json task-state/D934.json task-state/D936.json
# 空 = 自验结论对本 HEAD 仍有效｜非空 = 锚点失效，须重跑
```

---

## 五、自验结论（b5-v，独立）

**`可提请独立审计`**（锚定 **`7f31f417`**）｜**未通过项：无**｜**永不写"审计通过"**

**b5-v 自曝一处假红（记正向）**：首轮 repro `5 通过 2 失败`，**未直接上报**，追查确认为**其副本环境漏了 `core.quotepath=false`**（交付仓 `core.quotepath=off`）→ `ls-files` 把中文名八进制转义 → 误判 `⚠phantom`。**决定性旁证**：交付工作树本体只读跑 `--dry-run --strict` → `exit=0`、零 phantom。补 config 后全绿。已如实记入报告。
**污染自查**：三个测试会写仓 → 全部在 `/tmp/b5u6*` 隔离副本跑；交付树**零污染**（`CTO-HEALTH.md` 0 改动、探针卡 0 残留）。

---

## 六、未清项（诚实登记）

| # | 未清项 | 归属 |
|---|---|---|
| **U12** | `pre-audit-summary.sh:74` 双丢 stdout+stderr → 故障可归因性丧失 | CTO 定：纳入 / 另立卡 |
| **N1** | **116 张 string 卡未归一**（本卡只归一 4 张，生成器已兼容 + 逐卡可见降级） | CTO |
| **N2** | `docs/synova/CTO-HEALTH.md` 未随本卡重生成（不在写集；本卡后生成器**首次可跑通**） | CTO |
| **N3** | **散文 spec 抽取策略**：76 张「散文/多路径/§节引用」现取 `path=None`（旧实现会对它调 `exists()` → `OSError: File name too long` 崩） | CTO |
| **N4** | **M9 第③件**：`gen-cto-health` 未进 `ci.yml` 密封清单（36 个测试名单内无它） | **M9** |
| **N5** | **生产者 `alloc-task-id.sh:175` 写死 `"spec": null`** → 新建卡永不产生 `{"path":…}` ⇒ **只归一存量会持续再产生** | **已转 D940 编码A**（该文件在其写集） |
| **N6** | `commit` 首行受 `LC_ALL=C` 下 `.{1,140}` **字节**约束（b5-c1 实测，首版超长被拒） | 技术留档 |
| **N7** | 快照口径：c1 报 `str 118`（改前）/ b5-v 报 `str 116`（改后）→ **两个数都对**，差值 2 = D934/D936 由 str→dict | 留档防误判 |

---

## 七、声明

本文件与 `B5-U6-自验.md` 均为**自验结论**，**不是审计结论**，**不含"通过"判定**。
**自验结论 = `可提请独立审计`**（锚定 **`7f31f417`**）；**K3 复审通过前不得合并**。
是否通过归 **CTO 收件闸 + K3 终审**。
