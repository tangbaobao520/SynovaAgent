# B5-U6 改动清单（M6 证据件）

> 写者 **b5-c1**（task-10，U6 返修）｜ 2026-09-24 ｜ 工作树 `/Users/wane/SynovaAgent/.synova-wt-squad-b5u6` ｜ 分支 `fix/b5-u6-spec-unify`
> 树基线 `origin/main` = `9f0c8f65`（B5 已由 PR #740 入 main；本分支**不栈式**）｜ 交付 tip 见 §七 ls-remote 回执
> ⚠️ **写集说明**：本件不在 task-10 字面写集内（写集 = 四卡 + `gen-cto-health.py` + `gen-cto-health.test.sh`），系依 lead「这段请写进证据件」的指令新建（M6 要求证据件）。**若判定越界，请驳回，本件可单独撤下，不影响其余 6 文件。**

---

## 一 改动清单（file:line）

| 文件 | 位置 | 改动 |
|---|---|---|
| `scripts/control-tower/gen-cto-health.py` | `:232–233` 新增 | `_SPEC_PATH_RE` / `_SPEC_STR_MAX`（路径判据 + 长度上限 512） |
| 同上 | `:236–266` 新增 | `_extract_spec_path()`（**契约优先**：JSDoc 写明 @input/@output/@degraded） |
| 同上 | `:279` 新增 | `spec_degraded_n = 0` 计数 |
| 同上 | `:341–345`（原 `:305` 单行） | `spec_path = (d.get("spec") or {}).get("path")` → 走 `_extract_spec_path()` + **按卡打 `⚠ degraded:` 到 stderr** |
| 同上 | `:395` 新增 | task 字典带出 `"spec_degraded"` |
| 同上 | `:414` | `ts_meta` 增 `spec_degraded` 计数 |
| 同上 | `:534–545` 新增 | `render()`：报告正文**可见**的 degraded 块（要求③：不得只写 stderr） |
| `tests/control-tower/gen-cto-health.test.sh` | `:81–108` 新增 | 第 6 节**判别性夹具**（6 条断言，含两个探针卡 D996/D997 + `trap` 清理） |
| `task-state/D928.json` | `:14–16`（spec）+ `:17–22`（新增 spec_meta） | `spec` 5 键 → **仅 `path`**；`by/at/in_main/note` → 同级 `spec_meta`（**逐字原样**） |
| `task-state/D933.json` | `:13–15`（spec）+ `:16–21`（新增 spec_meta） | 同上（`by/at/note` 3 键搬迁） |
| `task-state/D934.json` | `:16–18` | string → `{"path": …}` |
| `task-state/D936.json` | `:16–18` | string → `{"path": …}` |

`git diff --numstat` = **6 files changed, 95 insertions(+), 6 deletions(-)**（`scripts/...py` +55/-2 ｜ 四卡各 +3/-1 ｜ `test.sh` +28/-0）。

---

## 二 验收 6 条原始输出

### 1) JSON 合法
```
$ python3 -c "import json;[json.load(open(f'task-state/{n}.json')) for n in ['D928','D933','D934','D936']];print('OK')"
OK — 四卡 JSON 合法
```

### 2) 四卡 spec 形态（4/4 dict + 非空 path）
```
D928: type=dict path='.claude/task-briefs/2026-09-23-D928-cto-health-crash.md' → ✅
D933: type=dict path='docs/synova/coordination/派单-W0与首模块-Win侧-20260923.md' → ✅
D934: type=dict path='docs/synova/coordination/两侧协作方式-Mac与Win-20260923.md' → ✅
D936: type=dict path='.claude/task-briefs/2026-09-24-D936-ownership-defer.md' → ✅
```

### 3) `spec_meta` 零信息丢失（改前 vs 改后逐条比对）
```
D928: path 一致=True ｜ spec_meta 一致=True ｜ 搬迁键=['by', 'at', 'in_main', 'note']
D933: path 一致=True ｜ spec_meta 一致=True ｜ 搬迁键=['by', 'at', 'note']
D934: path 一致=True ｜ spec_meta 一致=True ｜ 搬迁键=（无，原为纯 string）
D936: path 一致=True ｜ spec_meta 一致=True ｜ 搬迁键=（无，原为纯 string）
```
判定方式：把改动前的 `spec` 逐字写进断言脚本，改后断言 `spec["path"] == 改前 path` 且 `spec_meta == 改前 spec 去掉 path`。**无键丢失、无内容改写。**

### 4) 判别性：改前 vs 改后（**口径按 lead 授权更正**，见 §四）
```
--- 4a 改前（/tmp 变异树，`git show 9f0c8f65` 版生成器，同输入）---
rc=1 ｜ stdout 行数=0
  File ".../gen-cto-health.py", line 305, in analyze_task_state
    spec_path = (d.get("spec") or {}).get("path")
AttributeError: 'str' object has no attribute 'get'

--- 4b 改后（当前树，同输入）---
rc=0 ｜ stdout 行数=467 ｜ stderr `⚠ degraded:` 行数=116
⚠ degraded: D598 spec 为 string（旧形），应为 {"path": …}（已按该路径继续使用，属兼容降级）
⚠ degraded: D751 spec 为 string 但非单一路径形态（散文/多路径/§节引用），**不按路径使用**（旧实现会对它调 exists() → OSError: File name too long 崩溃），应改为 {"path": …}
（报告正文）> ⚠ degraded(spec 形态): **116** 张卡的 `spec` 非规范形 {"path": …} —— 分两类：形如单一路径者**已按该路径继续使用**；散文/多路径/§节引用者**不按路径使用**（逐卡原因见下）。建议归一化（U6）。

--- 4c 四卡 path 被采用（报告 spec 列）---
| D928 | impl_done | — | ✅ | — |  |
| D933 | impl_done | ✅ | ✅ | — |  |
| D934 | impl_done | ✅ | ✅ | — |  |
| D936 | impl_done | ✅ | ✅ | — |  |
```
> D928 的 spec 列为 `—` 是**正确**值：其 `spec.path` 指向的 brief 只随未合并分支 `fix/d929-cto-health-crash` 走，main 侧不存在（卡面已声明 in_main:false）——降级不制造假 ✅。

**变异测试（证明夹具真承重）**：`git worktree add --detach /tmp/u6-mut 9f0c8f65`（旧实现）→ 建同两个探针卡 → 跑 → `rc=1`、stdout 0 行、line 305 AttributeError ⇒ 第 6 节断言 ②③④⑥ 全红 ✅（⑤ 见 §五 自纠）。

### 5) 三个配对测试（改后全绿；改前基线一并留证）
```
改前（消费者未改，仅四卡已归一）:
  gen-cto-health.test.sh        → exit=1（在「── 2. 连续运行 → 幂等不写 ──」处**中途死掉**，无结果行）
  gen-cto-health-repro.test.sh  → 结果: 2 通过, 5 失败
  gen-cto-health-batch-report.test.py → OK（隔离，不触 render）
改后:
  gen-cto-health.test.sh        → 结果: PASS=13 FAIL=0   （exit 0；新增第 6 节 6 条断言全绿）
  gen-cto-health-repro.test.sh  → 结果: 7 通过, 0 失败
  gen-cto-health-batch-report.test.py → Ran 5 tests ... OK
```

### 6) 语法
```
$ PYTHONPYCACHEPREFIX=/tmp/u6-pycache python3 -m py_compile scripts/control-tower/gen-cto-health.py → exit 0
$ bash -n tests/control-tower/gen-cto-health.test.sh → exit 0
```
> 环境注记（非代码问题）：裸 `python3 -m py_compile` 在本沙箱报 `PermissionError: ... ~/Library/Caches/com.apple.python/...`（写缓存目录被沙箱拒），故用 `PYTHONPYCACHEPREFIX=/tmp` 落缓存；并另跑 `ast.parse` 双保险（OK）。

---

## 三 U6 紧迫性依据（**为何不能当低优先**）

```
$ task-state 全量卡 spec 形态统计（基线 9f0c8f65）
spec 形态分布: {'dict': 81, 'NoneType': 159, 'str': 118}
$ 归一四卡后复测：string spec 总数 = 116（形如单一路径 40 ｜ 非单一路径 76）
```
→ 循环对卡片列表**无条件**取 `spec.path`，遇第一个 string 即抛 ⇒ **main 上该生成器跑不完**：
`docs/synova/CTO-HEALTH.md` 自该形态出现起**无法再生**（本次改动前实测 `--dry-run` exit=1、stdout 0 行）。
这正是 D928 卡面「第③面自 2026-09-14 起完全无法生成」的**真实形态 = 崩，不是静默**。
**注**：本卡只归一 4 张卡；其余 116 张**仍为 string**，靠本次新增的兼容分支跑通（显式降级、逐卡可见）。全量归一属后续卡。

### 三·补 接线传播证据（铁律 9：形态变更的爆炸半径）

命令与原始输出：
```
$ grep -rln "task-state\|TASK_STATE" scripts/ .github/ | grep -vE "\.test\.(sh|py)$|/audit/"
（24 个候选：refresh-all.sh / redeem-progress.py / pre-commit-check.sh / check-pr-budget.sh /
 gen-cto-health.py / pre-dispatch-check.sh / alloc-task-id.sh / founder-truth.py / synova-commit /
 verify-parallel.sh / check-orphan-worktrees.sh / pre-audit-summary.sh / generate-dashboard.py /
 merge_writeset_gate.py / declare-write-set.sh / gen-plan-status.py / check-citations.py /
 check-notes-lifecycle.sh / gen-project-board.py / pr-queue-scan.py / resolve-commit-brief.sh /
 check-dev-doc-write-set.sh / check-fde-terms.sh / ci.yml）

$ 逐个查形状敏感访问 ["spec"] / ['spec'] / get("spec") / d["spec"].get(...)
   命中并含真实访问的：**仅 scripts/control-tower/gen-cto-health.py**（:241 / :248 / :251 / :523）

$ 广口径复核（24 个文件里所有含 "spec" 的行，排除 importlib / 文件名等噪音）
   founder-truth.py:120        if claimed == "spec_done"        ← 状态字符串，非字段
   gen-project-board.py:895    timespec="seconds"               ← kwarg，无关
   pr-queue-scan.py:692        timespec="seconds"               ← kwarg，无关
   verify-parallel.sh:187      注释里的「spec 写集」             ← 散文，非字段
   → **无第二形状敏感消费者**
```
**结论**：`spec` 由 string 改为 `{"path": …}` 的**爆炸半径 = 1**（即 `gen-cto-health.py` 自身）；
诊断/看板/队列/引用门禁等其他 task-state 消费者**零接触**该字段 → `spec_meta` 搬迁**无下游波及**。

---

## 四 机制订正（lead 已授权改口径；原卡面描述与代码不符）

| 项 | 原卡面/派单描述 | 实测（本卡取证） |
|---|---|---|
| 失效形态 | 「被上方 `except Exception` 吞掉 = **静默失效**」 | **致命崩溃**：`:305` 不在任何 `try` 内（包围的 `try` 只到 `json.loads`，`:292–296`），调用点 `:563` 亦无 `try` → 异常穿到 `:613` ⇒ **exit=1 + traceback + stdout 0 行** |
| "看不见"的出处 | 被调用方静默 | **在调用方**：`scripts/control-tower/pre-audit-summary.sh:74` `python3 - … >/dev/null 2>&1` **丢弃 stderr** → U3-artifact-repro 门只留"未过"、**丢失可归因性**（记 **U12**，不在本卡写集，lead 上报 CTO） |

---

## 五 判别性自纠（我自己踩到的假绿）

第 6 节夹具**初版**的断言「散文 spec 未被当路径（D997 spec≠✅）」是**否定式**断言：旧实现下 stdout 0 行 → 行不存在 → 该断言**空过变绿**（变异测试暴露：旧实现下 ②③④⑥ 红、**⑤ 却"绿"**）。
**修法**：先断言行**存在**，再断言非 ✅ —— `[ -z "$D997_ROW" ] && fail "行缺失（断言不得空过）"`。
修后复核：旧实现输出中 D997 行缺失 → ⑤ **红**（不再空过）。同法加固了 ④ 的行存在性。

---

## 六 未清项

**一句话汇总（lead 收口要求）**：未清项 = **U12**（`pre-audit-summary.sh:74` 丢 stderr，写集外）/ **116 张 string 卡未归一** / **`CTO-HEALTH.md` 未重生成** / **散文 spec 策略待定**。

1. **116 张卡仍为 string spec**：本卡只归一 4 张；生成器已兼容（显式降级 + 报告可见）。全量归一是否立卡由 CTO 定。
2. **U12**：`pre-audit-summary.sh:74` 丢弃被调用者 stderr → 门失败不可归因（不在本卡写集）。
3. **`docs/synova/CTO-HEALTH.md` 未随本卡更新**：测试会重写该产物（本卡已还原，未提交——不在写集）。若 CTO 要本 PR 内含重新生成的看板，请把该文件加进写集，我重跑并提交。
4. **散文 spec 的处置**：76 张「散文/多路径/§节引用」目前 `path=None`（不当路径，显式降级）。若其中含可用文件（如 `…md §一` 形式的 `…md`），是否要"抽首个路径 token"由 CTO 定（本卡保守不抽，避免误判）。
5. **D928 上游 add/add**：其 `spec.path` 指向的文件在未合并分支 `fix/d929-cto-health-crash`（PR #725）内，合并时按 add/add 处置。

---

## 七 ls-remote 回执

```
$ git ls-remote --heads origin fix/b5-u6-spec-unify
（见交付消息；交付 tip 以提交后回执为准）
```
