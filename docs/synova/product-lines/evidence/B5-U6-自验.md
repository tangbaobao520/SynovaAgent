# B5-U6 独立自验报告（task-11）

> 自验员：**b5-v**（独立自验员，非 b5-c1 兼任）
> 工作树：`/Users/wane/SynovaAgent/.synova-wt-squad-b5u6`（**只读**；唯一写入 = 本文件）｜分支 `fix/b5-u6-spec-unify`
> 自验窗口：2026-09-24 20:14 – 20:30（+0800）
> **本报告只引 b5-v 自己跑出的原始输出；不引用 b5-c1 任何结论。**

**自验结论：`可提请独立审计`** —— 绑定冻结 tip **`7f31f41798ea885084543c67e1a18dcf5603f703`**。
未通过项：无。未清项见 §七。本件**不表示通过**；通过与否归 CTO 收件闸 + K3 终审。

---

## 〇 冻结回执（M6）

**终局坐标（lead 中途前进一次，已按新 tip 出结论）**：

```
$ git fetch origin --quiet
$ git ls-remote --heads origin | grep fix/b5-u6-spec-unify      # run#1
7f31f41798ea885084543c67e1a18dcf5603f703	refs/heads/fix/b5-u6-spec-unify
$ git rev-parse HEAD                                            # run#1
7f31f41798ea885084543c67e1a18dcf5603f703
$ git ls-remote --heads origin | grep fix/b5-u6-spec-unify      # run#2
7f31f41798ea885084543c67e1a18dcf5603f703	refs/heads/fix/b5-u6-spec-unify
$ git rev-parse HEAD                                            # run#2
7f31f41798ea885084543c67e1a18dcf5603f703
$ git rev-parse origin/fix/b5-u6-spec-unify                     # run#3
7f31f41798ea885084543c67e1a18dcf5603f703
$ git status --porcelain
（空）
```
**三路一致 + 工作树干净 → 冻结成立**：`7f31f41798ea885084543c67e1a18dcf5603f703`

> **历史沿革**：本轮最初冻结坐标为 `99dbc992`（同日 20:14 复核三路一致）。lead 于 20:18 通知 c1 补推证据件 → 我**未在已被取代的坐标上出结论**，改在 `7f31f417` 重冻。`99dbc992` 仅作沿革记录。

**增量（`99dbc992..7f31f417`）**：
```
$ git log --oneline 99dbc992..7f31f417
7f31f417 chore: bypass COMMITTED 登记 (auto hook, D521)
106947ee docs(B5-U6): 证据件补接线传播证据（爆炸半径=1）与未清项汇总

$ git diff --name-status 99dbc992..7f31f417
M	.claude/bypass.log
M	docs/synova/product-lines/evidence/B5-U6-改动清单.md
```
**六件被验对象 SAME/DIFF 判定**（预期：仅证据件 DIFF，代码/四卡/测试零变动）：

| 文件 | 99dbc992 | 7f31f417 | 判定 |
|---|---|---|---|
| `scripts/control-tower/gen-cto-health.py` | `2760a6dd` | `2760a6dd` | **SAME ✅** |
| `tests/control-tower/gen-cto-health.test.sh` | `58973b2e` | `58973b2e` | **SAME ✅** |
| `task-state/D928.json` | `50842cb6` | `50842cb6` | **SAME ✅** |
| `task-state/D933.json` | `76bc11b1` | `76bc11b1` | **SAME ✅** |
| `task-state/D934.json` | `d9436292` | `d9436292` | **SAME ✅** |
| `task-state/D936.json` | `290edba6` | `290edba6` | **SAME ✅** |

⇒ **代码/测试/四卡零变动** → §三 的测试结果与 §二 的判别性实验**在新 tip 上依然有效**（非重跑，而是同一 blob 的延续）。DIFF 面仅 `.claude/bypass.log`（hook 自动）+ 证据件（文档）。

**冻结 sha256（`7f31f417`，`git cat-file blob HEAD:<p>` 内容）**：
```
scripts/control-tower/gen-cto-health.py                 blob 2760a6dd  sha256 767117ec5c541d41e43180ddca81d7eacc84b193eaca8f69d0fd44cfc1cf53b4
tests/control-tower/gen-cto-health.test.sh              blob 58973b2e  sha256 0b08c1fb846160d8cc93350149c133f19d9809e9074eae3365fd684288bae819
task-state/D928.json                                    blob 50842cb6  sha256 9f43766b58b216a977fb63c652c8d362e19fab45a5ca2a2c4e615fac45ad5d61
task-state/D933.json                                    blob 76bc11b1  sha256 776c6723c99b8507bf76ce95a9dc265616f4af44f62fcfd00e86ddeea5d134df
task-state/D934.json                                    blob d9436292  sha256 92774e6111550688dc8f16e387c2f544ddc42097af74a85cb4871539e9357365
task-state/D936.json                                    blob 290edba6  sha256 efcc64bb26fff9f057a25746b0130a88f3e83f089caae8a1109f107b0fd18bde
docs/synova/product-lines/evidence/B5-U6-改动清单.md  blob 959e5021  sha256 e519831ff8b2f7adc92fe4e52287ddd24dd17501de45b1a9a10ca24be36f686a
```

---

## 一 A. 四卡 `spec` 形态 + `spec_meta` 与 main 基线逐键一致

方法：`git cat-file blob` 抽 **HEAD 冻结 blob** 与 **`origin/main` 基线 blob** 到 `/tmp/b5u6/`，脚本逐键比对。

```
--- D928 ---  spec 类型 = dict   形态判据(dict+非空 path): ✅
    spec = {"path": ".claude/task-briefs/2026-09-23-D928-cto-health-crash.md"}
    main 版额外键 = ['at', 'by', 'in_main', 'note']
    spec_meta == main 额外键（逐键）: ✅ 完全一致
    path 保持: ✅
--- D933 ---  spec 类型 = dict   ✅
    spec = {"path": "docs/synova/coordination/派单-W0与首模块-Win侧-20260923.md"}
    main 版额外键 = ['at', 'by', 'note']
    spec_meta == main 额外键（逐键）: ✅ 完全一致
    path 保持: ✅
--- D934 ---  spec 类型 = dict   ✅   （main 版为 string → path 原样迁入，spec_meta 不存在 ✅）
--- D936 ---  spec 类型 = dict   ✅   （同 D934）
```
**4/4 为 dict 且含非空 `path`** ✅；带额外键的两卡（D928/D933）其 `at/by/in_main/note` **逐键原样搬迁至 `spec_meta`，零信息丢失** ✅。

**全键 diff（证明改动仅限 `spec`，无旁及字段）**：
```
--- D928 ---  keys: main=30 → new=31   added=['spec_meta']  removed=（无）  值变化键=['spec']
--- D933 ---  keys: main=25 → new=26   added=['spec_meta']  removed=（无）  值变化键=['spec']
--- D934 ---  keys: main=24 → new=24   added=（无）         removed=（无）  值变化键=['spec']
--- D936 ---  keys: main=24 → new=24   added=（无）         removed=（无）  值变化键=['spec']
每卡「非 spec 的值变化键」= （无）
```
**判 A：✅ PASS。**

---

## 二 B. 判别性（核心）：string `spec` 的**崩溃 vs 显式降级**

**未只测 happy path**（M9 第②件要求）：既有真实基线复现，也有我自造的合成样本；两版消费者同输入对照。

### B① main 版消费者 × main 基线 task-state（**真实复现，非合成**）

```
$ cd /tmp/b5u6-run/main && python3 scripts/control-tower/gen-cto-health.py --dry-run
exit code = 1
stdout 行数 = 0
stderr 行数 = 8
--- stdout 全文 ---
（空 —— 0 行，整只看板零输出）
--- stderr 全文 ---
Traceback (most recent call last):
  File ".../scripts/control-tower/gen-cto-health.py", line 613, in <module>
    sys.exit(main())
  File ".../gen-cto-health.py", line 563, in main
    t, ts_meta = analyze_task_state()
  File ".../gen-cto-health.py", line 305, in analyze_task_state
    spec_path = (d.get("spec") or {}).get("path")
AttributeError: 'str' object has no attribute 'get'
```
触发源（真实卡，非伪造）：`task-state/D934.json` 的 `spec` = `'docs/synova/coordination/两侧协作方式-Mac与Win-20260923.md'`，类型 = **str**。
⇒ **旧行为 = 致命崩溃（exit 1 + traceback + stdout 0 行）**，不是"降级后继续"。

### B② 改后版消费者 × 改后 task-state

```
$ cd /tmp/b5u6-run/new && python3 scripts/control-tower/gen-cto-health.py --dry-run
exit=0   stdout行数=448   stderr行数=116
```
- 四张被归一卡（D928/D933/D934/D936）**不再出现在 degraded 列表**（已规范化）。
- 其余 116 张 string 卡走兼容分支：逐卡 stderr `⚠ degraded:`（含卡号 + 原因），**且报告正文可见**（非只 stderr）：
```
stdout 第 427 行:
> ⚠ degraded(spec 形态): **116** 张卡的 `spec` 非规范形 {"path": …} —— 分两类：形如单一路径者**已按该路径继续使用**；
  散文/多路径/§节引用者**不按路径使用**（逐卡原因见下）。建议归一化（U6）。
>   - `D598`: spec 为 string（旧形），应为 {"path": …}（已按该路径继续使用，属兼容降级）
>   - `D751`: spec 为 string 但非单一路径形态…**不按路径使用**（旧实现会对它调 exists() → OSError: File name too long 崩溃）
>   - …其余 111 张同因（stderr 有逐卡 `⚠ degraded:` 行）
```

### B③ 我自造的合成判别样本（两版同输入对照）

注入两张合成卡：`ZZTEST1`（`spec` = 单个真实存在的路径字符串）、`ZZTEST2`（`spec` = 216 字散文）。

| 版本 | 结果 |
|---|---|
| **main 版** | `exit=1`、`stdout 行数=0`、`AttributeError: 'str' object has no attribute 'get'` @ `:305` |
| **改后版** | `exit=0`；`⚠ degraded: ZZTEST1 spec 为 string（旧形）…（已按该路径继续使用，属兼容降级）`；`⚠ degraded: ZZTEST2 spec 为 string 但非单一路径形态…**不按路径使用**` |

**报告表格里的实际取值（验"path 是否仍被采用"）**：
```
| ZZTEST1 | spec_done | ✅ | — | — |  |     ← spec 列 = ✅（path 被采用）
| ZZTEST2 | claimed   | — | — | — |  |     ← spec 列 = —（散文未被当路径）
```
**判 B：✅ PASS —— 三点全中：① 旧行为是致命崩溃（非静默吞异常）；② 改后出现显式 `degraded:`；③ 形如路径的 string **仍被采用**（向后兼容），散文则不被采用（避免 `File name too long`）。**

---

## 三 C. 禁改核验（他人/串行写集）

```
$ git diff --name-only origin/main...HEAD        # 全量，无截断，共 10 件
.claude/bypass.log
.claude/task-briefs/2026-09-24-B5-U6-spec-unify.md
docs/synova/product-lines/evidence/B5-U6-改动清单.md
memory/notes/proposed/2026-09-24-b5-u6-spec-unify.md
scripts/control-tower/gen-cto-health.py
task-state/D928.json
task-state/D933.json
task-state/D934.json
task-state/D936.json
tests/control-tower/gen-cto-health.test.sh

$ git diff --name-only origin/main...HEAD | grep -E '^\.github/workflows/ci\.yml$|^scripts/control-tower/alloc-task-id\.sh$'
（无输出）
```
**✅ 禁改两件（`ci.yml` = M9→B3→B4 串行单写者 / `alloc-task-id.sh` = D940 写集）均未出现。**

**判 C：✅ PASS。**

---

## 四 D. 三个配对测试（改后全绿 + 改前基线对照）

**方法说明（防污染）**：三个测试会**写入交付仓**（探针卡 `D996/D997/D999`、`.claude/bypass.log`、`docs/synova/CTO-HEALTH.md`）→ 按 task-11「工作树只读」要求，全部在 `/tmp` 隔离副本中运行，交付工作树**未被污染**（§六 收尾 status 为空可证）。
三套副本：`now`（HEAD 全部）｜`oldscript`（HEAD 测试 × main 脚本）｜`oldold`（main 全部）。

### D-1 改后：新测试 × 新脚本 —— **全绿**

```
--- gen-cto-health.test.sh ---
  ✅ string spec 不再崩溃 (exit 0)
  ✅ stderr 显式 degraded 含卡号 (D996)
  ✅ 报告正文含 degraded 标记（非只 stderr）
  ✅ 形如路径的 string path 仍被采用 (D996 spec=✅)
  ✅ 散文 spec 未被当路径 (D997 spec≠✅)
  ✅ 散文 spec 亦显式 degraded (D997)
  结果: PASS=13 FAIL=0        exit=0

--- gen-cto-health-repro.test.sh ---
  结果: 7 通过, 0 失败        exit=0

--- gen-cto-health-batch-report.test.py ---
  Ran 5 tests — OK            exit=0
```

### D-2 判别性：**新测试 × 旧脚本 —— 必须红（实测红）**

```
── 1. 产物含数据源指纹行 ──
  ✅ 指纹行存在 (12 hex)
── 2. 连续运行 → 幂等不写 ──
（中断）
exit=1
```
原因即 §二 B① 的 `AttributeError`（`spec_path = (d.get("spec") or {}).get("path")` @ `:305`）。
⇒ **该夹具确有判别力**：改坏（回到旧脚本）即红，非空转网。

### D-3 改前基线（旧测试 × 旧脚本）—— 复现 c1 报的数

```
$ bash tests/control-tower/gen-cto-health.test.sh        # 旧
── 1. 产物含数据源指纹行 ── ✅ 指纹行存在 (12 hex)
── 2. 连续运行 → 幂等不写 ──（中断）  exit=1
     ← 与 c1 报「改前在『── 2. 连续运行 ──』中途 exit=1」逐字吻合

$ bash tests/control-tower/gen-cto-health-repro.test.sh  # 旧
  ✅ 接线 / ❌ 无 phantom(--strict 应 0 实际 1) / ❌ phantom 标记 / ❌ phantom+非 strict 应 0 / ❌ 清理后 / ❌ git 不可用应 2
  结果: 2 通过, 5 失败        exit=1
     ← 与 c1 报「repro 改前 2 通过 5 失败」逐字吻合

$ python3 tests/control-tower/gen-cto-health-batch-report.test.py   # 旧（未改动）
  Ran 5 tests — OK            exit=0
```
⇒ **改后 (13/0, 7/0, OK) vs 改前 (中断, 2/5, OK)** —— 改善明确，且 batch-report 无回归。

### ⚠️ 自曝：我第一次 D 跑出的是**假红**（我的副本环境错，不是交付缺陷）

首轮 D 我在 `/tmp` 副本上得到 `repro: 5 通过, 2 失败`（`❌ 无 phantom: --strict 应 exit 0, 实际 1`）。我没有直接上报，而是追查，结论是**我自己重建仓库时漏了 `core.quotepath=false`**：
```
交付 repo  : git config core.quotepath → off
我的副本    : 未设 → 默认 true

$ 中文文件名在 ls-files 下
交付 repo : docs/synova/audit-reports/2026-09-21-K3-D858-复审.md
我的副本  : "docs/synova/audit-reports/2026-09-21-K3-D858-\345\244\215\345\256\241.md"   ← 八进制转义
```
→ `head_files` 集合里是转义串，与真实路径比对失败 → D858/D864/D865 被误判 `⚠phantom` → `--strict` 假红。
**旁证（决定性）**：在**交付工作树本体**上跑只读 `--dry-run --strict` → **`exit=0`、零 phantom 行、status 前后皆空**。
补 `core.quotepath false` 后重跑 → 三个测试全绿（上表）。
**如实记录**：这是**我的验证环境假阳**，不计入交付缺陷；但说明"重建环境必须对齐原仓 git config"，否则 phantom 类判据会假红。

**判 D：✅ PASS —— 改后三测试全绿；判别性成立（新测试打旧脚本必红）；改前基线与 c1 报数逐字吻合。**

---

## 五 E. M9 三件套对照（**仅登记，不作判红依据**）

```
--- ① 是否进 ci.yml 密封清单？ ---
$ grep -c 'gen-cto-health' .github/workflows/ci.yml
0
（对照：control-tower-tests job 在 ci.yml:202，密封清单 ci.yml:225-266 共 36 个 test 文件，无 gen-cto-health 系）
→ ❌ 未进 — 登记为 M9 第③件待办
→ ✅ 同时确认：本卡 **未改 ci.yml**（尊重串行写者），故"未进清单"非本卡引入

--- ② 判别性夹具？ ---
tests/control-tower/gen-cto-health.test.sh（7306 B）；内含 U6/degraded 相关行 9 行（§四 D-1/D-2 已实证其判别力）→ ✅ 已有

--- ③ 模式自检 / degraded 可见性？ ---
脚本内 degraded 相关行 31 行；报告正文可见标记在 :537 → ✅ 可见
```
**判 E：登记完成。** ① 为**开放待办**（M9 第③件），②③ 已具备。

---

## 六 机制订正（lead 已授权改口径）—— 我独立复核**成立**

**卡面原话（错）**：「`spec` 为 string → AttributeError → 被上方 `except Exception` 吞掉 = 静默失效」。
**实测（对）**：**`:305` 不在任何 try 内** → 异常穿出 `analyze_task_state` → 穿出 `main` → `:613 sys.exit(main())` → **exit=1 + traceback + stdout 0 行**。**是"崩"，不是"静默"**。

我核验的两条物理证据：
1. **traceback 栈形**（§二 B①）：`line 613 in <module>` → `line 563 in main` → `line 305 in analyze_task_state` —— 异常**逐层穿出**，中途无捕获帧。
2. **try/except 分布**：main 版 `:305` 之前最近的 try 为 `:248`–`:257`（在 `:305` **之前已闭合**），`:292`–`:294` 亦为局部闭合；`240–400` 行内没有任何覆盖 `:305` 的 try。

**真正"看不见"在调用方**（记 **U12**，写集外）：
```
$ git show origin/main:scripts/control-tower/pre-audit-summary.sh | grep -n '>/dev/null 2>&1'
74:  python3 - "$runner" "$full" "$@" >/dev/null 2>&1 <<'PYEOF'
```
`:74` 把被调用者 **stdout + stderr 双双丢弃** → 生成器崩了，调用方侧"什么都没发生"。**这是"静默"的真实位置**，且**不在本卡写集**（属 `pre-audit-summary.sh`）。

---

## 七 复核项：影响面数 / 爆炸半径（lead 点名）

### 7.1 影响面数（我自己的数——与 c1 口径不同但**不矛盾**）

```
我独立统计（排除 TEMPLATE.json，共 358 张卡）：
  基线 origin/main : dict=81  NoneType=159  str=118
  改后 HEAD        : dict=83  NoneType=159  str=116
```
⇒ c1/lead 报的 `dict 81 ｜ NoneType 159 ｜ str 118` 是**改前基线**；改后为 `dict 83 ｜ str 116`，差值**恰好 2 = D934/D936 由 str→dict**（另两卡 D928/D933 原已是 dict，额外键迁 `spec_meta`）。**两者都对，是不同快照**；我据实并列，避免"数字不符"误判。
脚本自报与我的计数**完全一致**：stdout 报 `116`，stderr `⚠ degraded:` 行数 = `116`，我统计非空 str = `116`。

### 7.2 爆炸半径 = 1（独立复跑证据件原命令）

```
$ grep -rln "task-state\|TASK_STATE" scripts/ .github/ | grep -vE "\.test\.(sh|py)$|/audit/"
候选数 = 24     （与证据件所列 24 个：名集**完全一致**，无多无少）

$ 逐个查形状敏感访问 ["spec"] / ['spec'] / get("spec")
★ 命中 scripts/control-tower/gen-cto-health.py:
    241:  · dict → (d["spec"].get("path"), None)
    248:  （U6 要求③…）。旧实现 `(d.get("spec") or {}).get("path")`
    251:  spec = d.get("spec")
    523:  lines.append(f"| {t['task_id']} | {t['status']} | {t['spec']} | …")
（无第二个文件命中）
```
广口径复核（24 文件内所有含 `spec` 的行，剔除噪音）中仅见 `founder-truth.py:120`（`== "spec_done"` **状态字符串**）、`gen-project-board.py:895`/`pr-queue-scan.py:692`（`timespec="seconds"` **kwarg**）、`verify-parallel.sh:187`（注释散文）——**均非字段访问**。
⇒ **`spec` 形态变更爆炸半径 = 1 成立**；`spec_meta` 搬迁无下游波及。**我独立复现，结论与证据件一致。**

> **附加发现（供 CTO，非缺陷）**：生产者侧 `scripts/control-tower/alloc-task-id.sh:175` 骨架模板写死 `"spec": null,` → **新建卡默认得到 `spec: null`**，永远不产生 `{"path": …}`。这解释了 159 张 `NoneType` 的成因，也预示"归一只做存量"会**持续再产生**。该文件**不在本卡写集**（D940 写集），仅登记。

---

## 八 未清项

| # | 项 | 证据 | 归属 |
|---|---|---|---|
| **U12** | `pre-audit-summary.sh:74` `>/dev/null 2>&1` 丢 stdout+stderr → 生成器崩溃在调用方侧不可见 | 上述 `grep -n` 命中 `:74` | **写集外**，CTO 另派 |
| **N1** | **116 张 string `spec` 卡未归一**（本卡只归一 4 张；其余靠新增兼容分支跑通） | 我的计数 str=116；脚本自报 116 | 待 CTO 决是否立卡 |
| **N2** | `docs/synova/CTO-HEALTH.md` **未随本卡重生成**（测试会重写该产物，c1 已还原未提交） | c1 证据件 §六·3 | 待 CTO 决（若要含则须加入写集） |
| **N3** | **散文 spec 策略待定**（当前：不按路径使用 + 显式降级） | §二 B③ ZZTEST2 | 待 CTO |
| **N4** | **M9 第③件**：`gen-cto-health` 未进 `ci.yml` 密封清单（36 个测试在册，无此系） | `grep -c → 0` | M9/B3/B4 串行写者 |

**未通过项：无。**

---

## 九 红线自检

- ✅ 未用 `--no-verify` / `git stash` / `force push`（全程只读 git 查询 + `git fetch` + `--dry-run`）
- ✅ 未触碰 `scripts/audit/**`
- ✅ 只写 `docs/synova/product-lines/evidence/B5-U6-自验.md`；测试与实验全部在 `/tmp/b5u6*` 隔离副本，**交付工作树未被污染**
- ✅ 未在主工作区 `/Users/wane/SynovaAgent` 写文件
- ✅ 未引用 b5-c1 任何结论；全部数字来自 b5-v 自跑命令原始输出
- ✅ 未写"审计通过"；未宣称"产品被验证"

**自验结论：`可提请独立审计`**（锚 `7f31f41798ea885084543c67e1a18dcf5603f703`）。未通过项：无。
