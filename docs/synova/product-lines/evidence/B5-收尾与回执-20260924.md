# B5 收尾三件与小队回执（四卡 task-state 补登记）

> 卡号 B5（CTO「B 类并行」唯一获准立即开工项）｜ 派单：`派单-B类并行与预规划-20260924.md` §三
> 执行形态：Agent Teams 小队（队长 + 1 编码 + 1 独立自验）
> 分支 `chore/b5-task-state-backfill` ｜ 建树基线 `origin/main` = f25e61eb ｜ **自验锚点 tip = `f2d87e15`**（见 §四；本文件所在收口提交仅加治理产物，artifact blobs 全等 ── 用 §四 的不变性命令自证）
> 本文件由**队长**（synova-squad-lead）撰写，属治理产物（`domain_neutral`），非产品代码
> 计数截至 `2026-09-24T10:20:00Z`

---

## 一、交付范围与判据

**写集（4 卡，纯簿记，零代码）**：`task-state/D928.json`、`D933.json`、`D934.json`、`D936.json`
四卡事实（b5-v 独立核验：**无一编造**）：

| 卡 | 内容 | PR | 状态（独立核验） |
|---|---|---|---|
| D928 | 第③面生成器修复 | #725 | **未合并**（`--is-ancestor ffd47dda origin/main` 非 0；`--grep '#725'` 0 条）✅ 如实标注 |
| D933 | Win 派单 + 模块归属基线 | #730 | 已合并 `09466083` |
| D934 | 两侧协作方式 | #731 | 已合并 `1a1cccc8` |
| D936 | 模块归属服从 `ownership.yaml` + 台账 | #733 | 已合并 `12383f14` |

**CTO 三件额外要求**：① 推 origin ✅（tip `bddfe949` == 本地 HEAD）② 内容按事实再核并留证 ✅ ③ 每卡补 `writer` 字段 ✅（四卡 `writer='synova-squad-b5'` + `root_cause_field_alias='backfill_reason'`）

---

## 二、团队成员运行记录

**token 列如实说明**：本会话工具链**未暴露**成员 token 用量（`env`／`~/.dsh/sessions` 均无用量文件）→ 按"数字禁手写"纪律**留空，不伪造**。

| 成员 | 角色 | 运行状态 | token | 共享任务 id | 任务状态 | 写集 |
|---|---|---|---|---|---|---|
| `lead` | 队长（协调/判前提/验证据，**不下场写码**） | running | 未暴露 | — | — | `.claude/task-briefs/2026-09-24-B5-*.md`、本文件 |
| `b5-c1` | 编码·补登记 | inactive（已交付） | 未暴露 | **task-8** | completed | `task-state/D928/D933/D934/D936.json`、`B5-改动清单.md` |
| `b5-v` | **独立自验**（只读 + `/tmp`） | inactive（已交付） | 未暴露 | **task-9** | completed | `B5-自验.md` |

编制 3 ≤ 4 ｜ **自验不由编码兼任** ｜ 写集交集 **0** ｜ 零代码文件（越界核验：`.ts/.tsx/.js/.py/.sh/.yml` 硬检 = **0**）

---

## 三、本轮最重要的事：**队长自己判错了一次写者归属**（诚实登记）

**经过**：`D934/D936`（及随后 `D928/D933`）在 b5-c1 开工前 ~20 秒出现在工作树内。我据
`executor: "dsh-cto"` + `.synova-wt-cto-a/`（我 `ls` 到 `.alloc-task-id.lock` 目录）**推断写者是 CTO 侧**，
据此下了**停写令**（b5-c1 随即零写盘待命）并向 CTO 报"双写冲突"。

**CTO 更正（我接受）**：
> `executor` = 谁**执行这张卡的工作**（业务归属），**不是"谁写了 task-state 文件"**；四卡 `updated_by: synova-squad-b5` → 写者就是本小队。

**教训（已并入 D940 判据）**：
```
executor   = 谁执行这张卡的工作
writer     = 谁写这个文件        ← 本批新增该字段以消除歧义
updated_by = 最后改动者
判「双写」只能看 writer/updated_by + 文件实际所在工作树；禁止用 executor 推断。
```

**做对的部分（CTO 记正向）**：发现疑似双写时**先停手、不覆盖、来问归属** —— 宁可停手也不"先写再说"。
**账目**：因这一停，多了一轮往返（停写令 → 撤令 → 收口四件），但**零文件被覆盖**、零工作丢失。

**未清（留证不隐瞒）**：`writer` 按裁定口径填 `synova-squad-b5`，而**物理 provenance**（谁在 17:43–17:57 逐字节写了这四文件）**不可取证** —— b5-v 独立查明：`git config user.name = synova-cto`，全历史 `%an` Top = `synova-cto`（1443 次），`grep -c 'b5-c1'` = **0** ⇒ **提交作者身份对本仓不具区分力**，且 birth 已因本轮重写重置 ⇒ **既不能佐证也不能证伪**。b5-c1 已在四卡加 `writer_note` 留证。→ **U5，归 D940。**

---

## 四、`git diff --stat` 与 ls-remote 回执

```
# 全量净变更
$ git diff --stat origin/main...HEAD
 .claude/bypass.log                                  |   N +
 .claude/task-briefs/2026-09-24-B5-taskstate-backfill.md |  67 +++
 docs/synova/product-lines/evidence/B5-改动清单.md    | 176 +++
 task-state/D928.json                                |  ... 
 task-state/D933.json                                |  ...
 task-state/D934.json                                |  ...
 task-state/D936.json                                |  ...

# ls-remote 回执（队长亲跑，最终 anchor）
$ git ls-remote --heads origin chore/b5-task-state-backfill
 f2d87e1509aee895dea8e2e1205eebab39bf7272	refs/heads/chore/b5-task-state-backfill
$ git rev-parse HEAD
 f2d87e1509aee895dea8e2e1205eebab39bf7272
★ 远端 tip == 本地 HEAD（真已推送）
```

**自验锚点 tip = `f2d87e1509aee895dea8e2e1205eebab39bf7272`**（b5-v 三轮复验的最终锚）

**锚点不可变集（六件 blob / sha256，b5-v 独立记录）**

| 文件 | blob | sha256 |
|---|---|---|
| `task-state/D928.json` | `c93e5dfc` | `5d161c9cd56abf965f2c88a79f0af04e74d3d92276d8eef4fcf88b7e1bb8870d` |
| `task-state/D933.json` | `5bc2ca3e` | `7fe6d860dda8ebf7789f146b586d3bf8145ee2ed9be8523ddc6dea1ed993ed9d` |
| `task-state/D934.json` | `2a546b40` | `b79f6849b427d273947abedb473bbaabe053412323bc4d76917db32a067aadf3` |
| `task-state/D936.json` | `de2c3d6c` | `fde835d7a20f228a6d962c40f7c1c0b04a38aaaff0f1bbc48955fbab31968cbc` |
| `docs/synova/product-lines/evidence/B5-改动清单.md` | `eb2a6c88` | `7a42b288036774f8ef027cfb0210d6e442c262ebb764001515a7c56a640358eb` |
| `.claude/task-briefs/2026-09-24-B5-taskstate-backfill.md` | `4a844615` | `54c52aa3685ef924abe5b7cb45439004edcd59c2bf54826c938de0097c4d387b` |

**收口提交后 artifact 不变性（可核命令 —— 本文件自身提交后请跑它）**
```bash
git diff f2d87e1509aee895dea8e2e1205eebab39bf7272..HEAD -- \
  task-state/D928.json task-state/D933.json task-state/D934.json task-state/D936.json \
  docs/synova/product-lines/evidence/B5-改动清单.md \
  .claude/task-briefs/2026-09-24-B5-taskstate-backfill.md
# 空 = 自验结论对本 HEAD 仍有效（无需重跑）｜ 非空 = 锚点失效，须重跑出新锚点
```
**判别性证明（防空转网；b5-v 特意做了反证）**：`(a) f2d87e15..f2d87e15` → **0 行**（预期空 ✅）｜
`(b) bddfe949..f2d87e15` → **52 行 / 4 files changed**（有判别力 ✅）｜`(c)` 对照 `.claude/bypass.log` → **非空** ✅
⇒ 该命令**既稳定又灵敏**，可长期当判据。

> **本文件自身的 stale 教训（与 R1/U5 同族，队长自己踩到，已改）**：本节初版引 `bddfe949` + 四卡旧 sha256，
> 而 `bddfe949..f2d87e15` 四卡 blob **实变**（`a6c77cf4→c93e5dfc` 等）→ 冻结坐标**已过期**（由 b5-c1 抓到）。
> 处置**不是**再钉一个新 tip（下一次提交又会 stale），而是：**钉"自验锚点" + 配一条"收口提交后 artifact 不变性"可核命令**。
> 附：b5-c1 发现 `edit` 类写入换 inode → `birth` 每次重置（`17:45:06→17:46:35→18:08:02`）；
> 改用 **in-place 写入（同 inode）** 后 birth 稳定（`birth < mtime` 可判别）→ 降低 stale 频率；
> **但不消除**（跨机 clone / 换工作树 / rename / checkout 重写仍令 birth 归零）→ 锚点声明仍是兜底。

---

## 五、自验结论（b5-v，独立）

**`可提请独立审计`**（**自验锚点 tip `f2d87e1509aee895dea8e2e1205eebab39bf7272`**；六件 sha256 与不变性命令见 §四。
三轮复验沿革：`ef602c19` → **`退回`（R1）** → `bddfe949` → `可提请独立审计` → `f2d87e15`（U5 闭环）→ **本锚点**）

判据：**A ✅**（7 必填 + `writer` 4/4 非空）｜**B ✅**（PR 事实无编造）｜**C ✅**（零代码文件）｜
**D ✅**（根因零套话）｜**E ✅**（引用路径存在性）｜**R1 ✅ 已闭环**

**报告结构（审计链完整）**：`B5-自验.md` 579 行 —— **旧结论（`退回` @ `ef602c19`）原样保留为 §〇–§八**，
新增 §九 终局复验 @ `bddfe949` + §十 最终结论。**历史结论未删改**，可核首轮判据与修法。

**b5-v 的两处主动纠错（记正向）**：
1. 首轮在**未冻结靶**（分支未推/零提交/D933 缺失）上**拒绝出结论**，只报阻断 —— CTO 确认该处置正确。
2. 首轮 R1 判定脚本 `grep -q '工作树均不存在'` **假阳**（命中勘误句引号内的旧文）→ 自行改用分句定性，并把这次假阳写进报告 §9.6，未当作交付缺陷上报。

---

## 六、未清项清单（诚实登记）

| # | 未清项 | 状态 | 归属 |
|---|---|---|---|
| **U1** | `B5-改动清单.md` 缺件 | **✅ 已闭环**（IN HEAD，176 行 / 14273 B） | — |
| **U2** | 认领 brief 未入库 | **✅ 已闭环**（IN HEAD） | — |
| **U3** | 四卡在本轮**非唯一次写**：其他 session 先写、b5-c1 后收口；tip 前进须重跑自验 | **✅ 已正确响应**：第三轮 delta 复验 + **锚点声明 + 不变性命令** ⇒ tip 再前进可**零成本判失效**（§四） | CTO/K3 |
| **U4** | **本回填 PR 自身走 CT-34 豁免**（`6c37420e → DOC_ONLY=TRUE`）→ "补登卡的 PR"**无法被它补登的那道门禁拦住** | ⚠️ **保留开放观察**（机制层缺口，价值高） | CTO/K3 |
| **U5** | `writer_note` 的**过程声称**不可取证（提交身份恒为 `synova-cto`，无区分力） | **✅ 已闭环**（三段证据等级齐备 + 旧失效值清除 + 【可核】段 birth 逐卡与实测一致）。**注：闭环的是"陈述方式"，17:4x 物理写入者仍不可判定 → 转 D940** | 已闭环 / 余项归 D940 |
| **U10** | **队长收尾件自身 stale**：初版引 tip `bddfe949` + 四卡旧 sha256，而四卡 blob 实变（`a6c77cf4→c93e5dfc` 等） | **✅ 已闭环**（本节按锚点声明改写；并与 R1/U5 同族 —— 队长自己踩到） | 已闭环 |
| **U11** | `edit` 类写入换 inode → `birth` 每次重置 → "字段引用自身 birth"必然 stale（b5-c1 发现） | 已解：**in-place 写入（同 inode）**令 birth 稳定；**但不消除**（跨机 clone/换工作树/rename/checkout 重写仍归零）→ 锚点声明兜底 | 技术留档 |
| **U6** | `spec` 字段形态不一：D928/D933 = **object**、D934/D936 = **string**（存量 D922/D939 亦 string）；main 上唯一消费者 `gen-cto-health.py:305` 恰是 D928 待修的 P0 | ⚠️ 开放（本批未新增破坏） | CTO/K3 定口径 |
| **U7** | K3 批次2 报告（`2bf2630d` / `audit/k3-batch2`）**未落 main** → 四卡 `audit=pending` 的审计回执挂在分支上，分支被清理则审计链断 | ⚠️ 开放 | CTO |
| **U8** | D928 卡自身路径与 PR #725 分支内同名文件构成 **add/add** → 合并时不得静默覆盖 | ⚠️ 开放 | CTO |
| **U9** | 队长误判写者归属（用 `executor` 推 writer）→ 多一轮往返 | 已更正 + 口径已并入 D940 | 队长 |

---

## 七、声明

本文件与 `B5-自验.md` 均为**自验结论**，**不是审计结论**，**不含"通过"判定**。
**自验结论 = `可提请独立审计`**（**自验锚点 tip `f2d87e15`**；本文件所在收口提交仅加治理产物 —— 用 §四 不变性命令自证）；**K3 复审通过前不得合并**。
是否通过归 **CTO 收件闸 + K3 终审**。
