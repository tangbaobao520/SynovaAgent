# D822 交付证据（批十四小队 · 编码队员 A）

> 任务号: D822 ｜ 2026-09-20 ｜ 工作树 `.synova-wt-team14` ｜ 分支 `team/win-batch14`
> 提交: `66d1638e fix(D822): 降级信号不再被同轮后一次成功抹掉（改为逐条结果 + 累计入账）`
> 卡面: `task-state/D822.json`（status=impl_done）｜ 规格: `.claude/task-briefs/2026-09-20-D822-degraded-signal-not-masked.md`
> 五态定位: **健康**（有实现 + 有接线 + 穿真实入口验证）——路由判定已在生产路径、注入走测试侧触发器、e2e 穿 `POST /api/conversations`

---

## 一、Q-B 选项 (a) 落点：`appendFailureCount` 的 JSDoc 原文（`src/store/session-store.ts:196-211`）

```ts
  /**
   * D822: appendEvent 双写失败**累计**计数（本实例生命周期内只增不减）。
   *
   * 为什么需要它（范式来源：`@deepseek-ai/dsh-session-stats/lib/index.js:11,103,129`——
   * 事件流逐条累计落账，`steps: state.steps + 1` / `llmMs: state.llmMs + ...`，
   * **每个事件独立入账、不被后续事件覆盖**；其 step/end 放在 `finally` 里，
   * 所以 completed/failed/cancelled/max-tokens 四种结束都留一条）：
   * 本项目原实现用一个**会被后续成功重置的瞬时布尔**（`lastDegraded`）承载降级事实，
   * 一旦"同一轮内先失败后成功"，失败事实即消失——这正是 D822 的缺陷本体。
   * 修法是把失败**累加**下来，而不是**覆盖**掉。
   *
   * 消费者（防死代码，铁律 37）:
   *   ① 本卡两处测试真断言它：`tests/store/degraded-signal-not-masked.test.ts`、
   *      `tests/store/session-event-log.test.ts`（"失败不因后续成功消失"用例）；
   *   ② **D831（P-2 不变量机制）断言③「降级信号不被后续成功写入抹掉」的物理探针面**
   *      （`task-state/D831.json` 验收点 6 原文；队长 2026-09-20 批 Q-B 条件）。
   */
  appendFailureCount = 0;
```
（已复核 `task-state/D831.json` acceptance_points 第 6 条原文含「③ 降级信号不被后续成功写入抹掉」。）

## 二、改写后的旧测试：新断言（证明是"真事件化"而非"布尔别名"）

`tests/store/session-event-log.test.ts`（原 `:146-164` 固化缺陷那条已改写）：
```
✓ addMessage 必须透传失败结果（不再吞成 void）        → failed.ok === false && failed.degraded === true
✓ 最近一次写 = 失败                                  → store2.lastDegraded === true
✓ 失败必须累计入账（不被后续成功覆盖）                → store2.appendFailureCount === failuresBefore + 1
✓ 第二次写成功 → 透传 ok                             → recovered.ok === true
✓ lastDegraded 仍是"最近一次写的结果"（逐条语义）      → store2.lastDegraded === false
✓ 本轮聚合判定（路由层据此发 STORE_DEGRADED 帧）必须仍为真 → [failed, recovered].some(w => !w.ok) === true
✓ 累计计数只增不减（D822 的物理探针面）               → 计数不回退
✓ 不得被重置为 0                                     → > 0
```

## 三、验收命令与原始输出

```
$ node_modules/.bin/vitest run tests/store/degraded-signal-not-masked.test.ts tests/store/session-event-log.test.ts
 ✓ tests/store/degraded-signal-not-masked.test.ts (3 tests)
 ✓ tests/store/session-event-log.test.ts (17 tests)
 Test Files  2 passed (2) | Tests 20 passed (20)

$ node_modules/.bin/vitest run tests/store/degraded-signal-not-masked.test.ts tests/store/session-event-log.test.ts tests/routes/conversations.test.ts tests/orchestrator/session-manager-eventlog.test.ts
 ✓ tests/orchestrator/session-manager-eventlog.test.ts (8 tests)
 ✓ tests/store/session-event-log.test.ts (17 tests)
 ✓ tests/store/degraded-signal-not-masked.test.ts (3 tests)
 ✓ tests/routes/conversations.test.ts (13 tests)
 Test Files  4 passed (4) | Tests 41 passed (41)
```

注入真实性证据（用例 1 内置断言，防"触发器没生效"的假绿）：
```
兼容表 agent_messages 行数 = 2（两条都写成功）
事件流 session_events 行数 = 1 且 payload.role = 'assistant'   ← 缺的正是 user 那条 = 断裂成立
SSE 帧含 { type:'error', code:'STORE_DEGRADED', degraded:true }，且仍含 agent_message、末帧 end（未崩流）
```

## 四、反向验收（两次原始输出）

**抽掉路由判定**（还原 `if (store.lastDegraded)`）
```
     × 用例 1（核心）: 一次写失败 + 一次写成功 → STORE_DEGRADED 仍被发出（修复前无此帧）
     ✓ 用例 2（不误报）/ ✓ 用例 3（不粘滞）
 Test Files  1 failed | 1 passed (2)
      Tests  1 failed | 19 passed (20)
```
**抽掉累计计数**（注释 `appendFailureCount += 1`，对最终测试内容复跑）
```
     × D822 改写: appendEvent 失败 → 成功，**失败事实不被后续成功抹掉**（原用例固化的是缺陷行为）
 Test Files  1 failed (1)
      Tests  1 failed | 16 passed (17)
```
**两次还原后**
```
 Test Files  2 passed (2) | Tests 20 passed (20)
```
> 注意：抽路由判定时**只有用例 1 红**、用例 2/3 仍绿 ⇒ 该断言不是靠误报取胜（D820 学到的"每一半都要有一条单独能红的断言"同样适用本卡：store 一半由改写用例单独可红，路由一半由用例 1 单独可红）。

## 五、三问自查（逐条）

1. **谁调用它？** `addMessage` 返回值 ← `src/routes/conversations.ts:249`（`userWrite`）/ `:255`（`assistantWrite`），判定点 `:262`；`appendFailureCount` ← 本卡两处测试真断言 + **D831 断言③物理探针面**（`task-state/D831.json` 验收点 6）。
2. **有没有一条测试穿过真实实现？** 有：`tests/store/degraded-signal-not-masked.test.ts` 走真 express + `POST /api/conversations` + 真 better-sqlite3 + 真 `SessionStore` 双写；注入 = 测试侧 SQLite 触发器（`json_extract(payload,'$.role')='user'`）；**未在 `src/**` 加测试专用开关**；mock 仅四处既有配置缝。
3. **如果它明天被删掉，什么会坏？** 删 `appendFailureCount` → 改写用例的"失败不被抹掉"断言红 + D831 断言③失去物理探针面；删路由逐条判定 → 用例 1 红（`STORE_DEGRADED` 帧重新漏发，用户看不到落库降级）。

## 六、遗留

1. `lastDegraded` 有意保留为"最近一次写"的瞬时标志（`session-manager.ts:86` 逐条断言依赖，本卡不动其文件与测试）；**新调用方不得回退到读它判"本轮是否失败"**（已写进字段 JSDoc）。
2. `appendFailureCount` 暂未暴露 HTTP/健康检查出口；D831 落地若需 `/healthz` 可见再接线。
3. 注入手法修正记录：原拟 `WHEN NEW.seq = 1` 经实测**不成立**（seq 由 `SELECT MAX(seq)+1` 算，ABORT 后 MAX 仍 0 → 触发器持续命中，两条写全失败），改为按 payload 角色选择性注入；已报队长备案。
