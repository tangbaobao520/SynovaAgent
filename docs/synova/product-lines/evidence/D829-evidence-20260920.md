# D829 自验证据 — @synova/evolution feedback-collector 有真实消费方（线 17-1）

> 卡号：D829 ｜ 小队：批十四 · 编码队员 C ｜ 工作树 `.synova-wt-team14` ｜ 分支 `team/win-batch14`
> 基线：`origin/main@4f3bb21c`（工作树 `d527abcb`）｜ 日期：2026-09-20
> 验收口径：`docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`
> 证据性质：**队内自验**（非独立审计；结论只允许「可提请独立审计」）

---

## 一、五态定位

| 项 | 判定 | 依据 |
|---|---|---|
| 接线点 `POST /api/ga/calibration` → L0 收集器 | **健康**（`live && testedThroughEntry`） | 真实路由 handler + 内存 SQLite，7 用例穿入口 |
| 读取出口 `GET /api/ga/calibration/evolution-feedback` | **健康** | 同上（含 org 隔离 fail-closed 反证） |
| 下游 `OrgAdapter.adjustThresholds` 消费 | **健康** | 3 条同 sentinelId 纠错 → 真实返回阈值调整 |
| 规则版本记录（验收点③） | **健康** | 真实 approve 路由 → `evolution_snapshot` 记录落库 |
| `chat.ts:70-76` 既有调用点（不持久化 + 空吞） | **写了没接（部分）** | 非本卡写集 → 遗留①|
| `collectAllFeedback()`（`evolution.ts:210` 零参） | **接了跑不通（假成功）** | 非本卡写集 → 遗留② |

---

## 二、生产调用点 file:line + 为什么会被触发

**生产调用点**：`src/routes/ga-calibration.ts:244-245`（`await import('@synova/evolution')` → `collectGaCalibrationFeedback(...)`）

```
$ grep -rn "collectGaCalibrationFeedback" src/ | grep -v node_modules
src/routes/ga-calibration.ts:244:        const { collectGaCalibrationFeedback } = await import('@synova/evolution');
src/routes/ga-calibration.ts:245:        const l0 = await collectGaCalibrationFeedback({
```

**为什么会被触发**（用户动作 → 代码路径）：
1. 客户端调用 `POST /api/ga/calibration`（路由已挂载：`src/server.ts:406`）
2. 用户动作 = GA（增长顾问/老板）在 GA 协同面板对一条**诊断结论 / 诊断逻辑 / 信号**提交纠错：
   `mark_error`（标记错误）/ `rewrite_logic`（重写逻辑）/ `demote_signal`（降级信号）
3. `action ∈ {mark_error, rewrite_logic, demote_signal}` 时 `REFLUX_DECISION[action]` 非空 →
   既有 growth 回流后**必经** L0 收集块（`ga-calibration.ts:232-266`）
4. `add_context` 不进入（背景卡非纠错信号，对齐 spec §7.1 单源边界）

**非**死代码 / 非 cron-only / 非测试专用开关：三动作的 201 成功路径即触发；`src/` 全仓 grep 零其它调用者（无并行入口）。

---

## 三、命令 + 原始输出

### 3.1 本卡三个测试文件（穿真实入口）

```
$ node_modules/.bin/vitest run tests/evolution/feedback-collector.test.ts tests/routes/ga-calibration.test.ts tests/routes/evolution-rule-version.test.ts
 ✓ tests/evolution/feedback-collector.test.ts (20 tests) 13ms
 ✓ tests/routes/evolution-rule-version.test.ts (2 tests) 195ms
 ✓ tests/routes/ga-calibration.test.ts (33 tests) 171ms

 Test Files  3 passed (3)
      Tests  55 passed (55)
```

### 3.2 回归：全部 import `@synova/evolution` 的测试

```
$ node_modules/.bin/vitest run $(grep -rln "@synova/evolution" tests/ | tr '\n' ' ')
 ✓ tests/evolution/org-adapter.test.ts (8 tests) 12ms
 ✓ tests/evolution/global-analyzer.test.ts (4 tests) 8ms
 ✓ tests/evolution/evolution-metrics.test.ts (10 tests) 17ms
 ✓ tests/evolution/feedback-collector.test.ts (20 tests) 20ms
 ✓ tests/evolution/rule-version-manager.test.ts (14 tests) 26ms
 ✓ tests/routes/evolution-rule-version.test.ts (2 tests) 266ms
 ✓ tests/routes/ga-calibration.test.ts (33 tests) 232ms
 ✓ tests/evolution/expert-evolution.test.ts (7 tests) 9ms
 ✓ tests/evolution/session-learner.test.ts (12 tests) 5ms
 ✓ tests/evolution/evolution-proposals.test.ts (9 tests) 8ms
 Test Files  10 passed (10)
      Tests  119 passed (119)
```

```
$ node_modules/.bin/vitest run tests/loops/ga-calibration-evolution.test.ts tests/loops/ga-correction-feedback.test.ts tests/growth/feedback-collector.test.ts
 ✓ tests/growth/feedback-collector.test.ts (9 tests) 14ms
 ✓ tests/loops/ga-correction-feedback.test.ts (7 tests) 10ms
 ✓ tests/loops/ga-calibration-evolution.test.ts (9 tests) 25ms
 Test Files  3 passed (3)
      Tests  25 passed (25)
```

### 3.3 类型检查（铁律 38）

```
$ node_modules/.bin/tsc --noEmit > /tmp/d829-tsc.txt 2>&1; echo "tsc exit=$?"
tsc exit=2
$ grep -c "error TS" /tmp/d829-tsc.txt
28
$ grep -E "ga-calibration|packages/evolution|tests/evolution|evolution-rule-version" /tmp/d829-tsc.txt || echo "(none)"
(none)
```

**说明**：28 个 error 全部为**存量**（`extensions/sentinels/_extinct/**`、`src/connectors/ima.ts`、`src/server.ts`），
本卡改动文件（`git diff --name-only` 内）零命中。`as any` / `as never` / `as unknown as` = 0（本卡新增行内）。

---

## 四、反向验收（两次原始输出）

### 4.1 接线抽掉 → 报红

操作：`cp /tmp/d829-ga-calibration.wired.ts`（备份）→ `git checkout HEAD -- src/routes/ga-calibration.ts`（还原为接线前）

```
$ node_modules/.bin/vitest run tests/routes/ga-calibration.test.ts -t "D829"
 FAIL  tests/routes/ga-calibration.test.ts > D829 — GA 纠错落 L0 收集器 + 可查（线 17-1） > D829-7 evolution-feedback 认证三态（无 auth → 401）
Error: route 未注册: GET /api/ga/calibration/evolution-feedback
 ❯ getHandler tests/routes/ga-calibration.test.ts:84:9
     84|   throw new Error(`route 未注册: ${method.toUpperCase()} ${path}`);

 Test Files  1 failed (1)
      Tests  6 failed | 1 passed | 26 skipped (33)
```

### 4.2 接线还原 → 全绿

```
$ cp /tmp/d829-ga-calibration.wired.ts src/routes/ga-calibration.ts && grep -c "collectGaCalibrationFeedback" src/routes/ga-calibration.ts
2
$ node_modules/.bin/vitest run tests/routes/ga-calibration.test.ts -t "D829"
 ✓ tests/routes/ga-calibration.test.ts (33 tests | 26 skipped) 188ms

 Test Files  1 passed (1)
      Tests  7 passed | 26 skipped (33)
```

### 4.3 id 唯一性缺陷还原 → 报红（第二处修复）

操作：把 `nextFeedbackId()` 改回 `fb_${Date.now().toString(36)}`

```
$ node_modules/.bin/vitest run tests/evolution/feedback-collector.test.ts
     × D829 回归: 同毫秒连续纠错不得 id/key 相撞（旧实现 5 连写 distinct=1 → UPSERT 静默覆盖） 3ms
      Tests  1 failed | 19 passed (20)
```

### 4.4 缺陷修复还原 → 全绿

```
$ cp /tmp/d829-fc.fixed.ts packages/evolution/src/feedback-collector.ts
$ node_modules/.bin/vitest run tests/evolution/feedback-collector.test.ts
 ✓ tests/evolution/feedback-collector.test.ts (20 tests) 12ms
      Tests  20 passed (20)
```

### 4.5 缺陷原始证据（先于修复，独立探针）

```
PROBE ids ["fb_mu9q7bon","fb_mu9q7bon","fb_mu9q7bon","fb_mu9q7bon","fb_mu9q7bon"] distinct= 1
```

---

## 五、三问自查（逐条）

1. **谁调用它？** `src/routes/ga-calibration.ts:244-245`（生产调用点，全仓唯一）。
   触发用户动作：GA 对诊断结论/逻辑/信号提交 `mark_error` / `rewrite_logic` / `demote_signal`。
   读取方：`src/routes/ga-calibration.ts:539`（`GET /api/ga/calibration/evolution-feedback`）；下游消费者 `OrgAdapter`（`src/agent/post-diagnosis-processor.ts:247` 已接线）。
2. **有没有一条测试穿过真实实现？** 有：`tests/routes/ga-calibration.test.ts`（D829-1..7）走真实 express handler + 真实 better-sqlite3 + 真实 AgentMemoryStore + 真实 OrgAdapter（非 fake provider、非 mock 管线）；`tests/routes/evolution-rule-version.test.ts` 走真实 approve handler + 真实 SentinelRunner.getL0API()。
3. **如果它明天被删掉，什么会坏？**
   - 删 L0 收集块 → GA 纠错不再持久化 → `OrgAdapter.adjustThresholds` 恒空转（纠错无法触发阈值自适应）→ D829-1/2/3/5 报红（§4.1 实测 6 红）
   - 删读取出口 → 「纠错是否被系统收下」在生产不可查 → D829-4/7 报红
   - 删 id 唯一性修复 → 同毫秒纠错互相覆盖 → D829 回归用例报红 + 存量 `getFeedbackByAction` 用例回到「靠覆盖通过」的假绿

---

## 六、未覆盖 / 遗留（非本卡写集）

1. `src/routes/chat.ts:70-76`：既有 `collectFeedback` 调用未传 memoryStore（不落库）+ `.catch(() => {})` 空吞（铁律 24）→ 另开卡
2. `src/routes/evolution.ts:210`：`collectAllFeedback()` 零参 → 恒 `events: []` 假 200；且该函数内 `orgId: ''` 查询恒空 → 另开卡
3. 三路反馈（`user_behavior` / `external_data` / `diagnosis_contradiction`）仍无生产入口 → 线 17-2 / 17-4 范围

## 七、红线自律

- 只在 `.synova-wt-team14` 工作树内改动；未碰 `scripts/audit/**`、`scripts/control-tower/**`、`src/store/**`、`src/l1/**`、`src/l4/**`、`src/init/**`、`src/routes/sessions.ts`
- 未使用 `--no-verify` / `git stash` / force push；未在 `src/**` 加测试专用开关
- **不宣称**「越用越准」/「进化能力」（本卡只交付物理事实）
- 结论口径：**可提请独立审计**（非「审计通过」）
