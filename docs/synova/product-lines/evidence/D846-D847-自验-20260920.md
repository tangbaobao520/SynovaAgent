# D846 / D847 自验报告 — 第一阶段（先红复现）

> 自验员：**verifier-1**（独立第三人，非编码岗，永久不进编码岗）
> 工作树：`/Users/wane/SynovaAgent/.synova-wt-gates-v` ｜ 分支 `verify/gates-v` ｜ HEAD `73ad9611`
> 基线：`aca42e30`（PR #657 `fix/d839-claim-release` 头，已 merge origin/main `a7ff8e14`）
> 日期：2026-09-20 ｜ 队内口径：**自验**（非审计；审计结论只认 K3）
> 采信原则：**不采信编码员与 K3 的原始输出**，全部结论由本 session 在自己沙箱里重跑得出
> 交付标注：**可提请独立审计**

## 〇、基线与方法核验

被测文件与基线 `aca42e30` 逐字节一致（非"看起来一样"）：

```
$ for f in scripts/control-tower/claim_release.py scripts/control-tower/staging_guard.py scripts/workflow/resolve-commit-brief.sh; do
    a=$(git show aca42e30:$f | shasum -a 256 | cut -d' ' -f1); b=$(shasum -a 256 $f | cut -d' ' -f1)
    echo "$f  aca42e30=$a  worktree=$b  $( [ "$a" = "$b" ] && echo SAME || echo DIFF )"; done
scripts/control-tower/claim_release.py  aca42e30=dbc48b1dc9829128272631c518124a6fb7bda5db57e8d06d4e1bd7f6530324f3  worktree=dbc48b1d…  SAME
scripts/control-tower/staging_guard.py  aca42e30=b5cbe3a595521ec882b4ff5cc994a51c4d0c956d80400a6629c33401d374db03  worktree=b5cbe3a5…  SAME
scripts/workflow/resolve-commit-brief.sh  aca42e30=c438f0c270d1d727d149391ed1e234d824893c217f597a998f500745b08a8a52  worktree=c438f0c2…  SAME
```

方法：**破坏性实验全部在 `mktemp -d` 沙箱内做**；脚本以 `cp` 从 verify 树取副本，`REPO_ROOT = 副本 parents[2] = 沙箱` → 真实仓库 `task-state/` 零写入。
开工与收工时 `git -C .synova-wt-gates-v status --porcelain` 均为空（无污染）。主工作区（D811 在飞）与 `.synova-wt-gates-a/-b` 全程零访问。

---

## 一、D846 先红：释放判定可被"工作树脏改"无痕伪造 —— **复现**

### 1.1 沙箱构造与主攻击路径

沙箱仓库：`docs/x.md`（tracked）+ 两份 brief（`D901-a`、`D902-b` 均在 Q2 认领 `docs/x.md`）+ **两个 task-state 卡都以 `claimed` 进 git 历史**（`git commit init`），随后伪造。

```
$ python3 $G --session-id D902 --staged docs/x.md      # $G = 沙箱内 staging_guard.py 副本
--- STEP 1: 基线（D901=claimed，无伪造）---
{"status": "block", "foreign_files": [{"file": "<staged>", "owner_session": "2026-09-20-D901-a", "brief": "/private/tmp/v1-d846-SfGb2I/.claude/task-briefs/2026-09-20-D901-a.md", "reason": "认领 brief D# 与本 session 任务不一致"}], "stray_files": [], "degraded": true, "release_cmd": "python3 scripts/control-tower/claim_release.py release --task D901 --reason \"<为什么可以释放>\"    # 等价入口: python3 scripts/control-tower/session_registry.py archive --session-id D901", "degraded_reason": "registry 缺失: /private/tmp/v1-d846-SfGb2I/.codex/control-tower/session-registry.json"}
EXIT=1

--- STEP 2: 工作树伪造（不 git add、不 commit）---
$ printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"
$ git -C "$SB" status --porcelain
 M task-state/D901.json
?? .codex/
$ git -C "$SB" show HEAD:task-state/D901.json      # git 可信版本未变
{"task_id":"D901","status":"claimed"}
$ git -C "$SB" log --oneline -- task-state/D901.json
350cf3b init                                        # 伪造零提交痕迹
$ git -C "$SB" diff --cached --stat                 # 零暂存
(空)

--- STEP 3: 重跑 guard ---
{"status": "warn", "foreign_files": [], "stray_files": [], "degraded": true, "released_claims": [{"brief": "2026-09-20-D901-a.md", "task_id": "D901", "basis": "task-state:impl_done", "detail": "task-state/D901.json status=impl_done"}], "release_reason": "D901 已释放（依据 task-state:impl_done: task-state/D901.json status=impl_done)", "degraded_reason": "registry 缺失: …"}
EXIT=0
```

**结论：复现。** `block / exit=1` → `warn / exit=0`。他人认领被解锁，攻击全程**不 add、不 commit**，`git show HEAD:` 与 `git log` 证明伪造从未进 git 历史（无痕）。K3 §① 的攻击路径在本 session 独立重跑成立。

### 1.2 file:line 依据（读工作树那一行）

```
$ sed -n '134,148p' scripts/control-tower/claim_release.py | nl -ba -v134
   142	def task_status(...) 内：
   142	    data, err = _read_json(_as_repo(repo) / "task-state" / f"{tid}.json")
$ grep -n "_read_json(_as_repo(repo) / \"task-state\"" scripts/control-tower/claim_release.py
142:    data, err = _read_json(_as_repo(repo) / "task-state" / f"{tid}.json")
$ grep -n "RELEASED_STATUSES = \|st in RELEASED_STATUSES\|rec.get(\"released\")" scripts/control-tower/claim_release.py
62:RELEASED_STATUSES = ("impl_done", "audited")
182:    if isinstance(rec, dict) and rec.get("released"):
187:    if st in RELEASED_STATUSES:
$ grep -rn "git show\|HEAD:task-state" scripts/control-tower/claim_release.py
rc=1 → 零命中（**无任何 git 可信版本读取**；:205-213 的 `_git` 仅用于 `ls-files` 文件清单）
$ grep -n 'if rel\["released"\]\|result\["status"\] = "warn"' scripts/control-tower/staging_guard.py
189:                    if rel["released"]:
221:            result["status"] = "warn"
```

链路：`staging_guard.py:188 → claim_release.is_released → task_status:142（读工作树文件）→ :187 status∈RELEASED_STATUSES → released=True → staging_guard.py:189/221 warn`。

### 1.3 反例矩阵（本 session 自造，K3 报告未覆盖的形式）

```
== V0 基线（全 claimed，台账空）==                 status= block
== V-A 只删文件不改内容（rm task-state/D901.json）== status= block  → fail-closed 成立（无伪造面）
== V-B 伪造后 git add（暂存但未提交）==             status= warn | released= ['task-state:impl_done']
== V-C status=claimed 但伪造台账（工作树写 claim-releases.json，不 add）==
                                                    status= warn | released= ['ledger'] | reason= 台账显式释放（伪造）
== V-C2 对照：git show HEAD:task-state/claim-releases.json ==
                                                    {"version":1,"releases":{}}   ← HEAD 版干净
== V-D status 变体 ==
  status=[IMPL_DONE]        → block   （精确匹配，未误放行）
  status=[impl_done ]       → warn    （.strip() 后命中 :148）
  status=[impl-done!! 42]   → block
== V-E task-state/D901.json 为符号链接指向 docs/evil.json（工作树）==  status= warn
```

**结论：复现（且伪造面比 K3 §① 记载的更宽）。** 3 条独立伪造路径全部命中：
- 路径①工作树改 status（主路径，1.1）
- 路径②伪造后被 `git add` 暂存（读取端只看工作树，暂存与否无关）
- 路径③**不碰 status、直接伪造 git 跟踪的 `task-state/claim-releases.json`（工作树脏改）→ `basis=ledger` 优先命中（:182）**，HEAD 版仍干净
- 路径④符号链接间接指向伪造内容

另：`release` 子命令无鉴权（无 owner 校验、无理由强制、无审计留痕门槛）：

```
$ python3 scripts/control-tower/claim_release.py --repo "$SB" release --task D901 --by attacker
✅ 已释放 D901 → 写入台账 /private/tmp/v1-auth-0bX3FE/task-state/claim-releases.json
exit=0
{ "releases": { "D901": { "at": "2026-09-20T20:26:59+08:00", "basis_at_release": "explicit-only",
  "by": "attacker", "reason": "显式释放", "released": true } }, "version": 1 }
```
（`claim_release.py:341-344`：`--task` 必填，`--reason` 默认"显式释放"，`--by` 取环境变量，**无身份校验**。）

> 说明：以上为**先红事实的刻画**，不是对修复方案的判据；也未评估威胁模型定级（属 K3 域）。

---

## 二、D847 先红：台账并发丢写 —— **复现（且发现第二个独立故障模式）**

### 2.1 主复现（N=8 并发 `release` 不同任务，30 轮）

沙箱：空 `task-state/`，脚本副本 `claim_release.py` + `write_lock.py`。每轮先删台账，再并发起 8 个进程释放 `D901…D908`，`wait` 后数台账记录数。

```
$ bash /tmp/v1-repro-d847.sh          # 完整脚本见附录 A.2
round=1  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=2  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=3  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=4  释放数=8  台账记录数=5  ❌ LOST-UPDATE（丢 3 条）
round=5  释放数=8  台账记录数=4  ❌ LOST-UPDATE（丢 4 条）
…
round=8  释放数=8  台账记录数=2  ❌ LOST-UPDATE（丢 6 条）
…
round=21 释放数=8  台账记录数=7  ❌ LOST-UPDATE（丢 1 条）
…
round=30 释放数=8  台账记录数=5  ❌ LOST-UPDATE（丢 3 条）
FIRST_BAD_ROUND=1                     # 第 1 轮即中（K3 记 2 进程 round0 中；本 session 8 进程第 1 轮中，等价结论）
== 复现后隔离验证：单独重放同一并发（5 次）==
replay#1 台账记录数=5/8
replay#2 台账记录数=5/8
replay#3 台账记录数=5/8
replay#4 台账记录数=5/8
replay#5 台账记录数=6/8
```

存活记录与丢失记录（原始台账摘要）：

```
记录数=4/8 ['D901', 'D905', 'D907', 'D908']
丢失= ['D902', 'D903', 'D904', 'D906']
```

**结论：复现。** 30/30 轮丢记录，丢失进程**自身仍打印 `✅ 已释放 …` 并 `exit 0`**——静默丢写（read-modify-write 无锁，`claim_release.py:105-124`）。

### 2.2 发现：丢写之外还有 **共享 tmp 文件名导致的未捕获崩溃**（K3 §⑥ 未记载）

同一实验逐进程捕获 stdout / exit code：

```
== 各进程独立 stdout / exit code ==
p1 rc=1 | Traceback (most recent call last):
  File "…/claim_release.py", line 406, in <module>   sys.exit(main())
  File "…/claim_release.py", line 380, in main       r = release_task(repo, args.task, args.reason, args.by)
  File "…/claim_release.py", line 313, in release_task   save_ledger(repo, ledger)
  File "…/claim_release.py", line 124, in save_ledger    os.replace(tmp, p)
FileNotFoundError: [Errno 2] No such file or directory: '…/task-state/claim-releases.json.tmp' -> '…/task-state/claim-releases.json'
p2 rc=0 | ✅ 已释放 D902 → 写入台账 …/task-state/claim-releases.json
p3 rc=0 | ✅ 已释放 D903 → …
p4 rc=0 | ✅ 已释放 D904 → …
p5 rc=0 | ✅ 已释放 D905 → …
p6 rc=0 | ✅ 已释放 D906 → …
p7 rc=0 | ✅ 已释放 D907 → …
p8 rc=0 | ✅ 已释放 D908 → …
```

机理：`tmp = p.with_suffix(".json.tmp")`（`claim_release.py:121`）→ **所有进程共用同一个 tmp 路径** `claim-releases.json.tmp`；A 写完 tmp 后，B 若已把该 tmp `os.replace` 掉，A 的 `os.replace` 即 `FileNotFoundError`。**该异常未捕获**（:117-124 无 try），进程 rc=1，stderr 抛 traceback。

40 轮 × N=8 分类计数：

```
$ bash /tmp/v1-repro-d847-instrumented.sh
ROUNDS=40 N=8
干净轮次(8/8 且零崩溃) = 0
lost-update 轮次(静默丢记录) = 40   首现: 1(entries=4,rcs= 0 1 0 1 0 0 0 0)
崩溃轮次(存在 rc!=0)        = 20  首现 round=1
台账 JSON 损坏轮次          = 0   首现: none
```

**结论：复现（两个独立故障模式）。**
- 模式①（静默丢写）：40/40 轮，rc=0 且打印成功 → 台账少记，与 K3 P1-3 同源，本 session 独立复现。
- 模式②（共享 tmp 崩溃）：20/40 轮存在 rc≠0 的未捕获 traceback → 算子只看到命令失败，**不知道失败原因、也不知道另一个进程已把台账改掉**；且失败进程的释放请求**可能已落在台账里也可能没落**（无幂等确认）。此为 K3 §⑥ 之外的**新增观察**，一并交 K3 判定级。
- 台账 JSON 损坏（半写文件被 replace 进正式路径）：**40 轮内未观测到**（`os.replace` 原子性在本次样本下生效）→ 标为**理论路径，未复现**，不作为结论。

### 2.3 file:line 依据

```
$ grep -n "write_lock" scripts/control-tower/claim_release.py
（无输出，rc=1 → 零引用：同目录已存在 write_lock.py，claim_release.py 未复用）
$ ls -la "$SB/scripts/control-tower/write_lock.py"   → 存在
$ grep -n "^class \|^def " scripts/control-tower/write_lock.py
42:class WriteLock:
176:def _emit_signal(status: str, reason: str) -> None:
188:def main():
$ grep -n 'with_suffix(".json.tmp")\|os.replace' scripts/control-tower/claim_release.py
121:    tmp = p.with_suffix(".json.tmp")
124:    os.replace(tmp, p)
```
（`claim_release.py:105-124` = read-modify-write 全程无锁；`task-status` 与台账读取均在锁外。）

---

## 三、第一阶段结论（口径）

**自验发现 3 个问题（第一阶段，先红事实成立）**：

| # | 问题 | 状态 | 证据 |
|---|---|---|---|
| 1 | D846：工作树脏改 `task-state/<D#>.json` → 他人认领 `block→warn`，零暂存零提交、无痕 | **复现** | §1.1 |
| 2 | D846：伪造面共 4 条（工作树 status / 暂存后 status / 工作树台账 ledger 优先级 / 符号链接），`release` 子命令无鉴权无留痕门槛 | **复现** | §1.3 |
| 3 | D847：台账并发丢写（30/30 轮）＋ **共享 tmp 文件名致未捕获 FileNotFoundError（20/40 轮崩溃）** | **复现** | §2.1 / §2.2 |

- 本阶段**无"未复现"项**（台账 JSON 半写损坏为理论路径未观测，已单列标注，不计入问题数）。
- 以上均为**先红刻画**，不含"修复是否有效"判断（第二阶段对 `gate/claim-release-a` 独立验证）。
- **可提请独立审计**（审计结论只认 K3；本报告自称"自验"，不含审计结论）。

## 四、未覆盖面（第一阶段，逐条列明）

1. **Windows / Git Bash 环境未验证**：本 session 仅在 macOS bash + 系统 python3 复现；跨平台差异（`os.replace`、路径分隔符、`shasum` 替代品）未跑。
2. **未走真实 `synova-commit` 端到端流程**：`staging_guard.py` 的生产调用点（synova-commit 内）未实跑，只直接调 guard CLI（K3 §L3 亦列为未覆盖）。
3. **未评估威胁模型定级**（P0/P1/P2）：属审计面，非自验面。
4. **未验证真实仓库全量 `release-stale --apply` 效果**（只跑沙箱）；真实 `task-state/claim-releases.json`（16 条）本 session 零写入、未做指纹比对以外的验证。
5. **并发场景未覆盖**：`claim_release.py scan` / `release-stale` 的并发行为、`write_lock.py` 现有锁原语自身的正确性、跨主机（NFS/多机）并发、以及"持锁进程被 kill"（第二阶段项，需先有锁实现）。
6. **未验证 `session-archived` 证据源（`claim_release.py:151-163`）的伪造面**：本次只验了 `task-state` 与 `ledger` 两个证据源。
7. **夹具 `tests/control-tower/staging_guard.test.sh` 自身的先红后绿未由本 session 复跑**（属第二阶段：对 A 分支跑夹具）。
8. 未复跑 K3 报告的其余条目（P1-2 CI 接线、P2-1…P2-5）。

## 五、附录：复现脚本（可原样重放）

### A.1 D846

```bash
#!/bin/bash
set -uo pipefail
W=/Users/wane/SynovaAgent/.synova-wt-gates-v
TODAY=$(date +%Y-%m-%d)
SB=$(mktemp -d /tmp/v1-d846-XXXXXX); echo "SANDBOX=$SB"
git -C "$SB" init -q; git -C "$SB" config user.email v@v.local; git -C "$SB" config user.name v
mkdir -p "$SB/.claude/task-briefs" "$SB/task-state" "$SB/scripts/control-tower" "$SB/scripts/workflow" "$SB/docs"
for f in staging_guard.py session_registry.py brief_parser.py write_lock.py declare-write-set.sh claim_release.py; do
  cp "$W/scripts/control-tower/$f" "$SB/scripts/control-tower/$f"; done
cp "$W/scripts/workflow/resolve-commit-brief.sh" "$SB/scripts/workflow/resolve-commit-brief.sh"
printf 'x\n' > "$SB/docs/x.md"
mk_brief() { cat > "$SB/.claude/task-briefs/${TODAY}-${1}-${2}.md" <<EOF
# Task Brief: ${1}
#CRITERIA: A

## Q0: 定位 — 夹具
## Q1: 调研 — 夹具
## Q2: 范围 — 夹具
做什么：
- ${3}

不做什么：
- 不改 docs/x.md.bak

## Q3: 验收 — 夹具
## 架构层: 基础设施
## Done 标准
- [ ] 夹具
EOF
}
mk_brief D901 a "docs/x.md"; mk_brief D902 b "docs/x.md"
printf '{"task_id":"D901","status":"claimed"}\n' > "$SB/task-state/D901.json"
printf '{"task_id":"D902","status":"claimed"}\n' > "$SB/task-state/D902.json"
git -C "$SB" add -A >/dev/null 2>&1; git -C "$SB" commit -qm init >/dev/null 2>&1   # claimed 进 git 历史
G="$SB/scripts/control-tower/staging_guard.py"
run() { (cd "$SB" && python3 "$G" --session-id D902 --staged docs/x.md 2>&1); echo "EXIT=$?"; }
run                                                                  # 基线 → block/1
printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"   # 不 add 不 commit
git -C "$SB" status --porcelain; git -C "$SB" show HEAD:task-state/D901.json
run                                                                  # → warn/0
```

### A.2 D847

```bash
#!/bin/bash
set -uo pipefail
W=/Users/wane/SynovaAgent/.synova-wt-gates-v
SB=$(mktemp -d /tmp/v1-d847-XXXXXX)
mkdir -p "$SB/task-state" "$SB/scripts/control-tower"
cp "$W/scripts/control-tower/claim_release.py" "$W/scripts/control-tower/write_lock.py" "$SB/scripts/control-tower/"
C="$SB/scripts/control-tower/claim_release.py"
N=8
count() { python3 -c "
import json
try: print(len(json.load(open('$SB/task-state/claim-releases.json')).get('releases',{})))
except FileNotFoundError: print(-1)"; }
for round in $(seq 1 30); do
  rm -f "$SB/task-state/claim-releases.json"
  for i in $(seq 1 $N); do
    python3 "$C" --repo "$SB" release --task "D90$i" --reason "并发轮次 $round 任务 $i" --by "proc$i" >/dev/null 2>&1 &
  done
  wait
  got=$(count)
  [ "$got" = "$N" ] && echo "round=$round 台账=$got ✅" || echo "round=$round 释放=$N 台账=$got ❌ LOST-UPDATE"
done
```

---
---

# 第二阶段 · 对象 `13a12923`（D846；D847 未验）

> 自验员：**verifier-1** ｜ 日期：2026-09-20 ｜ 队内口径：**自验**（非审计）
> 验证对象：**固定 sha `13a12923`**（不追浮动分支头 `gate/claim-release-a`）；提交主题 `fix(D846): 释放判定只认已提交内容（闭合 K3 P1-1 无痕解锁）+ release 凭证/非 owner 理由 + Windows 兼容`
> 取码方式：`git show 13a12923:<path>` 落到 `mktemp -d` 干净树（**不读 A 的自述当证据**，不读 A 的工作树）
> 交付标注：**可提请独立审计**

## 六、验证对象与干净沙箱

```
$ git fetch origin gate/claim-release-a && git cat-file -t 13a12923
commit
$ git show --stat 13a12923 | head
 scripts/control-tower/claim_release.py | 275 ++++++++++++++++++++---
 task-state/D846.json                   |  19 +-
 tests/control-tower/claim_release.test.sh | 301 ++++++++++++++------
 (+ memory/notes/implemented/2026-09-20-D846-claim-release-committed-evidence.md 等，共 5 文件)
```

干净树构建（**全部来自 git show，无一处取自 A 的工作树**）：

```
CLEAN=$(mktemp -d /tmp/v2-clean-XXXXXX)
for p in scripts/control-tower/{claim_release.py,brief_parser.py,staging_guard.py,session_registry.py,write_lock.py,declare-write-set.sh} \
         scripts/workflow/resolve-commit-brief.sh tests/control-tower/claim_release.test.sh \
         task-state/claim-releases.json; do
  git show 13a12923:$p > "$CLEAN/$p"; done
$ # 同源核验（A 是否顺带动了别的脚本）
scripts/control-tower/staging_guard.py     SAME as aca42e30   ← 未改，仍是基线同源
scripts/workflow/resolve-commit-brief.sh   SAME as aca42e30   ← 未改
```

## 七、夹具独立复跑（不采信 A 的 PASS 数字）

```
$ cd "$CLEAN" && bash tests/control-tower/claim_release.test.sh
  ✅ ⑨ 真实仓库释放台账指纹未变 (=c348cbfba6a80940d0d64c554afcdf0af7a9058dfce24b90f1c7f5d3f1fee2bb)
  ✅ ⑨ 真实仓库无 task-state/D901.json / D906.json / D910.json
  PASS=65 FAIL=0
FAIL=0

$ cd "$CLEAN" && SYNO_CLAIM_SRC=<aca42e30 版 claim_release.py> bash tests/control-tower/claim_release.test.sh
  ❌ ①a 工作树伪造 status=impl_done（不提交）→ 仍 exit 1（不解锁） — 实际 0 期望 1
  ❌ ①b 暂存（git add）但不 commit → 仍 exit 1（索引不是事实） — 实际 0 期望 1
  ❌ ①c 端到端 guard: 工作树伪造 → 仍 exit 1（他人认领未被解锁） — 实际 0 期望 1
  ❌ ③a 台账记录无凭证（裸 released:true）→ 仍 exit 1（不构成证据） — 实际 0 期望 1
  ❌ ③d 非 owner 无显式理由 → exit 2（契约不满足） — 实际 0 期望 2
  …（共 18 项红）
  PASS=47 FAIL=18
```

**结论：夹具真绿（65/0）且真红（基线实现 47/18）——本 session 独立复跑，与 A 自述数字一致（但结论不依赖其自述）。**

## 八、第一阶段 3 个问题逐条对照

| # | 第一阶段问题 | 第二阶段（13a12923）结论 |
|---|---|---|
| ① | 工作树改 `task-state/<D#>.json` status → 他人认领 `block→warn`（无痕） | **已修好**：矩阵 A1–A5 全部 `block/exit 1`（见 §九） |
| ② | 伪造面 4 条 + `release` 子命令无鉴权 | **部分收窄**：3/4 伪造路径封死 + 鉴权加固（§九、§十）；1 条按 A 显式设计仍放行但标注文本（发现 #1：结构化 degraded 丢失）；**另发现新的无痕通道**（发现 #2：`GIT_DIR` 注入换证据源） |
| ③ | D847 台账丢写 + 共享 tmp 崩溃 | **未验（等 D847）**。不判通过/失败。仅记代码事实：`save_ledger` 的 tmp 名已含 pid（`claim_release.py:239` `tmp = p.with_name(f".{p.name}.{os.getpid()}.tmp")`），并发互斥是否落地不在本卡（模块头 :234-235 自述"真正的并发互斥是 D847 的写集"）——**未做任何并发复测** |

## 九、伪造面换形式：前后对照矩阵（端到端 `staging_guard`）

沙箱：`docs/x.md` + brief `D901-a`/`D902-b`（均认领 `docs/x.md`）+ 两卡 `claimed` 已提交；`session-registry.json` 预置（隔离"registry 缺失"造成的 degraded 混淆）。

| 用例（命令见 §十四） | 阶段一 `aca42e30` | 阶段二 `13a12923` | 判定 |
|---|---|---|---|
| A0 基线（双 claimed 已提交） | block / 1 | block / 1 | 一致 |
| A1 工作树改 `status=impl_done`（**不 add 不 commit**） | **warn / 0** | **block / 1** | ✅ 已封 |
| A2 伪造后 `git add`（暂存不提交） | warn / 0 | **block / 1** | ✅ 已封 |
| A3 工作树卡=符号链接→站外伪造 JSON | warn / 0 | **block / 1** | ✅ 已封 |
| A4 工作树删卡（HEAD 仍 claimed） | block / 1 | block / 1 | 一致 |
| A5a `status=IMPL_DONE`（大小写变体） | block / 1 | block / 1 | 一致 |
| A5b `status=impl-done!! 42`（非法值） | block / 1 | block / 1 | 一致 |
| A6 脏改台账（`released:true` 裸记录） | **warn / 0（basis=ledger）** | **block / 1** + detail 点名缺项 | ✅ 已封 |
| A7 脏改台账（`by`/`at`/`reason` **齐全**但未提交） | warn / 0 | warn / 0（A 显式设计）+ ⚠文本；**顶层 `degraded=false`** | ⚠ 发现 #1 |
| A8 已提交 `impl_done`（**合法放行路径**） | warn / 0 | warn / 0 + `degraded=false` | ✅ 正确放行 |

A1 原始输出（关键字段）：

```
$ python3 $G --session-id D902 --staged docs/x.md          # 工作树已改 impl_done，未 add 未 commit
{"status": "block", "foreign_files": [{"file": "<staged>", "owner_session": "2026-09-20-D901-a", …,
 "reason": "认领 brief D# 与本 session 任务不一致"}], "stray_files": ["docs/x.md"], "degraded": false,
 "release_cmd": "python3 scripts/control-tower/claim_release.py release --task D901 --reason \"…\""}
EXIT=1
```

A8 原始输出（合法放行，含 CLI 一致性）：

```
$ python3 $G --session-id D902 --staged docs/x.md
{"status": "warn", …, "released_claims": [{"task_id": "D901", "basis": "task-state:impl_done",
 "detail": "已提交 task-state/D901.json status=impl_done"}], …}                     EXIT=0
$ python3 $C --repo . status --task D901
{"task_id": "D901", "released": true, "basis": "task-state:impl_done",
 "detail": "已提交 task-state/D901.json status=impl_done", "degraded": false}       EXIT=0
```

file:line（A 版）：`claim_release.py:131`（`git show HEAD:<rel>` 唯一可信入口）、`claim_release.py:267-268`（`rel=f"task-state/{tid}.json"` → `_read_committed_json`）、`:269`（工作树 status **只用于降级提示**）、`:281-283`（工作树与已提交不一致 → 不作为依据）。

## 十、`release` 凭证 / 留痕真落盘（不是只 echo）

```
-- B1 非 owner 无显式理由 → 应拒 --
❌ 非 owner 释放需显式理由：--by=attacker，owner=未知（已提交卡无 owner/executor 字段，从严处理）；请加 --reason "<为什么可以释放>"（禁默认值/占位值）
   EXIT=2      台账文件是否存在? 无          ← 被拒时零落盘
-- B2 非 owner + 显式理由 → 允许 --
✅ 已释放 D901 → 写入台账 …/task-state/claim-releases.json
   凭证: by=attacker at=2026-09-20T20:49:54+08:00 owner_at_release=— basis=explicit-only reason=D901 已并入 main 并由 K3 复审通过
   ⚠ 记录尚未提交（工作树变更）：凭证在 git 历史中不可核 → 建议 `git add …` 后随任务提交
   EXIT=0
-- B3 owner 自释放（已提交卡 owner=attacker）→ 允许 --
✅ 已释放 D901 … 凭证: by=attacker at=… owner_at_release=attacker … reason=owner 自释放（attacker），未附额外理由
-- B4 台账真落盘内容（磁盘文件，非 stdout 回显）--
{ "releases": { "D901": { "at": "2026-09-20T20:49:54+08:00", "basis_at_release": "explicit-only",
  "by": "attacker", "owner_at_release": "attacker", "reason": "owner 自释放（attacker），未附额外理由",
  "released": true } }, "version": 1 }
-- B6 台账提交后：git 历史里真能查到 谁/依据/时间 --
$ git show HEAD:task-state/claim-releases.json
{ "releases": { "D901": { "at": "…", "basis_at_release": "explicit-only", "by": "attacker",
  "owner_at_release": "attacker", "reason": "…", "released": true } }, "version": 1 }
$ python3 $C --repo . status --task D901
{"task_id": "D901", "released": true, "basis": "ledger", "detail": "台账显式释放（… ；by=attacker at=…）", "degraded": false}   EXIT=0
```

缺凭证逐项（B7，**缺任一即不构成证据**）：

```
-- 缺 by --     {"released": false, "basis": "", "detail": "台账记录缺凭证（by）→ 不构成释放证据（fail-closed 不释放…）", "degraded": true}      EXIT=1
-- 缺 at --     {"released": false, … "detail": "台账记录缺凭证（at）→ …", "degraded": true}                                                        EXIT=1
-- 缺 reason -- {"released": false, … "detail": "台账记录缺凭证（reason）→ …", "degraded": true}                                                    EXIT=1
```

file:line：`claim_release.py:206`（`LEDGER_CREDENTIALS=(by,at,reason)` 逐项校验）、`:481-483`（`by` 空 → exit 2）、`:486-490`（非 owner + 占位理由 → exit 2）、`:497-504`（落 `by/at/reason/basis_at_release/owner_at_release`）、`:584-591`（CLI 回显凭证 + 未提交告警）。

### 发现 #1（自验第二阶段）：`degraded` 在门禁层被丢弃（"未提交台账"路径）

A 的设计（模块头 :182-185 + `is_released` :344-346）是"台账仍读工作树；凭证完备即释放；台账未提交 → 释放照旧 + `degraded=true` + detail 标注"。**实测：CLI 层 `degraded:true` 可见，门禁层（`staging_guard`）顶层 `degraded=false`**——detail 文本带 ⚠ 说明，结构化标志丢失：

```
$ python3 $C --repo . status --task D901          # 台账凭证齐全但未提交
{"task_id": "D901", "released": true, "basis": "ledger",
 "detail": "台账显式释放（伪造但凭证齐全；by=attacker at=2026-09-20T00:00:00+08:00）；⚠ 台账记录未提交（工作树变更）→ 凭证在 git 历史中不可核；提交 task-state/claim-releases.json 后即变为可核",
 "degraded": true}                                                    ← CLI: true

$ python3 $G --session-id D902 --staged docs/x.md     # 同一状态，门禁端（stdout 单解析）
  status=warn   degraded=False                                        ← 门禁: false  ⚠
  released_claims[0].detail=台账显式释放（伪造但凭证齐全；…）；⚠ 台账记录未提交（工作树变更）→ 凭证在 git 历史中不可核；…
  release_reason=D901 已释放（依据 ledger: … ⚠ 台账记录未提交（工作树变更）→ …）
  degraded_reason=—
  EXIT=0        stderr=（空）
```

代码依据（**A 版与基线同源**）：`staging_guard.py:189` 的 `if rel["released"]:` 分支只把 claim 追加进 `released_claims`（:191-194），**`if rel.get("degraded")` 的传播只写在 `else`（block）分支里（:203-205）** → 已释放路径的降级标志无出口。

**性质（据实）**：不是"完全静默"——warn 文案里那行 ⚠ 是可读的；但**结构化 `degraded` 与 CLI 层自相矛盾**，只看 `degraded` 标志或 exit code 的消费者拿不到任何信号。属"降级信号传播链在门禁层断裂"（对照铁律 31）。

## 十一、边界 / 新 fail-open 探寻（A 没在夹具里覆盖的方向）

| 用例 | 结果 | 判定 |
|---|---|---|
| C1 非 git 仓库（有 `task-state/` 无 `.git`） | `released:false` + `degraded:true`，detail=`git show … rc=128: fatal: not a git repository` | ✅ fail-closed |
| C2 空仓库（`git init` 无提交 = unborn HEAD） | `released:false` + `degraded:true`，detail=`未提交（HEAD 无 …，工作树有 status=impl_done…）` | ✅ fail-closed |
| C3 shallow clone（`--depth 1`） | HEAD tree 内卡正常读到（`impl_done`→released；`claimed`→未释放），无 fail-open | ✅ |
| C4 `git rm --cached` 该卡（暂存删除，HEAD 仍 claimed） | block / 1 | ✅ |
| C5 `git rm --cached` + commit（HEAD 无该卡） | block / 1 + `degraded:true`（ENOENT:HEAD 但工作树有卡 → 显式点名） | ✅ fail-closed |
| C6 HEAD 中该路径是 **symlink**（blob=链接目标文本） | block / 1 + `degraded:true`（`JSONDecodeError`） | ✅ fail-closed |
| C7 HEAD 中该卡为**空文件** | block / 1 + `degraded:true`（`JSONDecodeError`） | ✅ fail-closed |
| C8 HEAD 中该卡 JSON 合法但**无 status** | block / 1 + `degraded:true`（detail 点名） | ✅ fail-closed |
| C9 **本地提交**伪造 `impl_done` | warn / 0（设计内：解锁需"已提交事实"）；`git reset --hard HEAD~1` 后 HEAD/工作树回 claimed、`git status` 干净、guard 恢复 block / 1；reflog 保留 `commit: forge committed` | ⚠ 残留刻画（见下） |
| C10 partial clone（`--filter=blob:none`）离线 | **实验无效**：`warning: filtering not recognized by server, ignoring`（本地 file:// server 不支持 filter）→ 该路径**未验** | ⛔ 未验 |
| C11/D1 **`GIT_DIR` 环境注入**换证据源 | 见发现 #2 | ❌ 发现 #2 |
| D5 blob 不可读（`GIT_OBJECT_DIRECTORY` 指空目录） | `released:false` + `degraded:true` | ✅ fail-closed |
| D4 同仓两分支 HEAD 内容不同 | 证据随**当前分支 HEAD** 变（`main`→claimed 不释放 / `forge`→impl_done 释放） | 设计内（需留提交） |

C9 原始输出（残留刻画）：

```
   伪造提交后:  status=warn  degraded=False  release_reason=D901 已释放（依据 task-state:impl_done: 已提交 …）EXIT=0
   reset 后 HEAD: {"…","status":"claimed"} | 工作树: claimed | git status: （空）
   reset 后 reflog: b95974a HEAD@{0}: reset: moving to HEAD~1|69907c1 HEAD@{1}: commit: forge committed|…
   reset 后 guard:  status=block  EXIT=1
```

**刻画（非 fail-open 判定）**：伪造从"零痕迹"升高为"**需一次本地提交**"；该提交可被 `reset`/`amend`/`rebase` 丢弃后**不再出现在 PR 差异**里（仅本地 reflog 可查）。卡内威胁模型（"解锁必须基于已提交事实"）本身接受此口径 → 记为**残余风险**，交 K3 判级。

### 发现 #2（自验第二阶段）：证据源可被 `GIT_DIR`/`GIT_WORK_TREE` 环境变量替换 → 端到端无痕解锁

```
── D0 对照：无注入（本仓 HEAD 卡=claimed）──
  status=block degraded=False                                                     EXIT=1
── D1 端到端注入 GIT_DIR=<外部仓>/.git（本仓零改动、零提交）──
   注入前本仓状态: （空） / HEAD 卡: {"task_id":"D901","title":"t","status":"claimed"}
  status=warn degraded=False release_reason=D901 已释放（依据 task-state:impl_done: 已提交 task-state/D901.json status=impl_done）
   注入后本仓状态: （仍为空 = 零痕迹）
── D2 同注入下 claim_release 直测 ──
{"task_id": "D901", "released": true, "basis": "task-state:impl_done", "detail": "已提交 task-state/D901.json status=impl_done", "degraded": false}   EXIT=0
── D3 反面对照：GIT_DIR 指向本仓 → 仍 block ──
  status=block degraded=False                                                     EXIT=1
```

机理：`claim_release.py:131` 的 `subprocess.run(["git", "-C", str(repo), "show", f"HEAD:{rel}"])` **继承调用者环境**，未固定 `GIT_DIR`/`GIT_WORK_TREE`；`git -C <dir>` 只改工作目录，`GIT_DIR` 仍优先生效（对照命令：`GIT_DIR=<R2>/.git git -C <R1> show HEAD:task-state/D901.json` → 输出 `impl_done`；不注入 → `claimed`）。

**同时满足**：① 门禁输出 `warn`/`exit 0`（他人认领被解锁）；② `degraded=false`；③ 被守护仓库 `git status` 全程为空、无提交、无任何可核痕迹。**前提**：能控制门禁进程的环境（同机同用户；非远程）——属同一"本地可篡改"类，但**是本卡修复后仍然存在的无痕通道**（第一阶段的文件级无痕路径已封）。

## 十二、第二阶段结论（口径）

**自验发现 2 个问题（第二阶段，对象 `13a12923`）**：

| # | 问题 | 复现 | 位置 |
|---|---|---|---|
| 1 | "台账未提交"路径的 `degraded` 在门禁层丢失：`claim_release` CLI `degraded=true`，`staging_guard` 顶层 `degraded=false`（仅 warn 文案含 ⚠ 文本）→ 降级信号在门禁层断裂 | 复现（§十） | `staging_guard.py:189-194` vs `:203-205`（A 版与基线同源，A 未改此文件） |
| 2 | 证据源可被 `GIT_DIR`/`GIT_WORK_TREE` 环境变量替换为另一仓库 → 端到端 `warn/exit 0`、`degraded=false`、被守护仓库零痕迹 | 复现（§十一 D1） | `claim_release.py:131` |

**第一阶段 3 个问题逐条对照**：① 已修好；② 部分收窄（3/4 伪造路径封死 + `release` 鉴权加固；1 条按设计放行但标注，且新暴露 2 个新问题）；③ **未验（等 D847）**。
**夹具**：真绿 65/0、基线实现真红 47/18（本 session 独立复跑）。
**本阶段未"放行"任何东西**：以上均为自验事实与位置，不含审计结论；定级与"是否可接受"交 K3。**可提请独立审计**。

## 十三、未覆盖面（第二阶段，逐条）

1. **Windows / Git Bash 未验**：A 声称"去 shasum 改 python3 hashlib + 路径走 argv"的 Windows 兼容改动，本 session 仅在 macOS bash 复现，未在 Git Bash / MSYS 下跑（`os.getpid()` tmp 名、argv 路径转换均未验）。
2. **真实 `synova-commit` 端到端未走**：只直接调 `staging_guard.py` CLI 与 `resolve-commit-brief.sh`；未验证 synova-commit 是否清洗环境变量（**发现 #2 在真实调用链下是否同样生效未验**）。
3. **未做威胁模型定级 / 可接受性判断**（P0/P1/P2 属审计面）。
4. **`release-stale --apply` 的并发与失败路径未验**（属 D847）。
5. **D847 任何内容未验**（按指令，等 A 报完成）：台账并发丢写是否消除、共享 tmp 崩溃是否消除，均**未做任何复测**（仅记 `save_ledger:239` 的 pid 后缀代码事实）。
6. **C10 partial clone 未构成有效实验**（本地 server 不支持 `--filter`）→ 该路径未验。
7. **未复跑 K3 报告的其余条目**：P1-2（CI 接线）、P2-1…P2-5、`synova-commit` 真实提交路径的 block 文案三出路。
8. **未审 L1/L2 面**：`memory/notes/implemented/2026-09-20-D846-claim-release-committed-evidence.md` 内容、`task-state/D846.json` 写集一致性；除已复跑的 47/18 与 65/0 两组数字外，A 的其他量化声称未核。
9. **未验**：多机/远程场景（另一台机器 push 是否影响本机 HEAD 证据）、`safe.directory`/dubious-ownership 报错路径、`.git` 权限损坏路径（只测了对象目录为空）。
10. **未验**：`scan` / `release-stale` 在新凭证规则下的行为面（只测了 `release` 与 `status`）。
11. **单机可重复性**：并发/时序类结论（发现 #2 的注入、A7 的 degraded 丢失）为确定性复现；但本阶段**未做 N 轮稳定性统计**（与第一阶段 D847 的 30/40 轮统计不同）。

## 十四、复现命令集（第二阶段，可原样重放）

```bash
# 0) 取码（固定 sha）+ 干净树
CLEAN=$(mktemp -d /tmp/v2-clean-XXXXXX)
W=/Users/wane/SynovaAgent/.synova-wt-gates-v
for p in scripts/control-tower/{claim_release.py,brief_parser.py,staging_guard.py,session_registry.py,write_lock.py,declare-write-set.sh} \
         scripts/workflow/resolve-commit-brief.sh tests/control-tower/claim_release.test.sh \
         task-state/claim-releases.json; do (cd "$W" && git show 13a12923:$p > "$CLEAN/$p"); done

# 1) 夹具（绿 / 先红）
(cd "$CLEAN" && bash tests/control-tower/claim_release.test.sh)                                    # PASS=65 FAIL=0
(cd "$W" && git show aca42e30:scripts/control-tower/claim_release.py) > /tmp/base_cr.py
(cd "$CLEAN" && SYNO_CLAIM_SRC=/tmp/base_cr.py bash tests/control-tower/claim_release.test.sh)     # PASS=47 FAIL=18

# 2) 端到端沙箱（registry 预置以隔离 "registry 缺失" 造成的 degraded 混淆）
SB=$(mktemp -d); git -C "$SB" init -q; git -C "$SB" config user.email v@v; git -C "$SB" config user.name v
mkdir -p "$SB"/{.claude/task-briefs,task-state,scripts/control-tower,scripts/workflow,docs,.codex/control-tower}
cp "$CLEAN"/scripts/control-tower/*.py "$CLEAN"/scripts/control-tower/*.sh "$SB/scripts/control-tower/"
cp "$CLEAN/scripts/workflow/resolve-commit-brief.sh" "$SB/scripts/workflow/"
printf '{"sessions":[],"archived":[]}\n' > "$SB/.codex/control-tower/session-registry.json"
printf 'x\n' > "$SB/docs/x.md"
# brief D901-a / D902-b 均认领 docs/x.md（mkb 形态见 §五 A.1）；两卡 claimed；git commit 后打 tag base
G="$SB/scripts/control-tower/staging_guard.py"; C="$SB/scripts/control-tower/claim_release.py"
reset_sb() { git -C "$SB" reset -q --hard base; git -C "$SB" clean -qfd; }
guard() { (cd "$SB" && python3 "$G" --session-id D902 --staged docs/x.md); echo "EXIT=$?"; }

# 3) 用例（每条前缀 reset_sb）
printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"                          # A1（不 add）
printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"; git -C "$SB" add task-state/D901.json   # A2
ln -s ../../docs/x.md.evil "$SB/task-state/D901.json"                                                    # A3
printf '{"version":1,"releases":{"D901":{"released":true}}}\n' > "$SB/task-state/claim-releases.json"     # A6
printf '{"version":1,"releases":{"D901":{"released":true,"reason":"伪造","by":"attacker","at":"2026-09-20T00:00:00+08:00"}}}\n' > "$SB/task-state/claim-releases.json"   # A7 / 发现#1
(cd "$SB" && python3 "$C" --repo . release --task D901 --by attacker)                                     # B1 → exit 2
(cd "$SB" && python3 "$C" --repo . release --task D901 --by attacker --reason "真理由")                   # B2 → exit 0
git -C "$SB" rm -q --cached task-state/D901.json                                                         # C4
(cd "$SB" && GIT_DIR=<外部仓>/.git python3 "$G" --session-id D902 --staged docs/x.md)                     # D1 / 发现#2
(cd "$SB" && GIT_OBJECT_DIRECTORY=<空目录> python3 "$C" --repo . status --task D901)                      # D5
```
