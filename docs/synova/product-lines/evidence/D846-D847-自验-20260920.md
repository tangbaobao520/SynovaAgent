# D846 / D847 自验报告 — 第一阶段（先红复现）

> 自验员：**verifier-1**（独立第三人，非编码岗，永久不进编码岗）
> 工作树：`/Users/wane/SynovaAgent/.synova-wt-gates-v` ｜ 分支 `verify/gates-v` ｜ HEAD `73ad9611`
> 基线：`aca42e30`（PR #657 `fix/d839-claim-release` 头，已 merge origin/main `a7ff8e14`）
> 日期：2026-09-20 ｜ 队内口径：**自验**（非审计；审计结论只认 K3）
> 采信原则：**不采信编码员与 K3 的原始输出**，全部结论由本 session 在自己沙箱里重跑得出
> 交付标注：**可提请独立审计**

## 〇、基线与方法核验

被测文件与基线 `aca42e30` 逐字节一致（非"看起来一样"）：

```
$ for f in scripts/control-tower/claim_release.py scripts/control-tower/staging_guard.py scripts/workflow/resolve-commit-brief.sh; do
    a=$(git show aca42e30:$f | shasum -a 256 | cut -d' ' -f1); b=$(shasum -a 256 $f | cut -d' ' -f1)
    echo "$f  aca42e30=$a  worktree=$b  $( [ "$a" = "$b" ] && echo SAME || echo DIFF )"; done
scripts/control-tower/claim_release.py  aca42e30=dbc48b1dc9829128272631c518124a6fb7bda5db57e8d06d4e1bd7f6530324f3  worktree=dbc48b1d…  SAME
scripts/control-tower/staging_guard.py  aca42e30=b5cbe3a595521ec882b4ff5cc994a51c4d0c956d80400a6629c33401d374db03  worktree=b5cbe3a5…  SAME
scripts/workflow/resolve-commit-brief.sh  aca42e30=c438f0c270d1d727d149391ed1e234d824893c217f597a998f500745b08a8a52  worktree=c438f0c2…  SAME
```

方法：**破坏性实验全部在 `mktemp -d` 沙箱内做**；脚本以 `cp` 从 verify 树取副本，`REPO_ROOT = 副本 parents[2] = 沙箱` → 真实仓库 `task-state/` 零写入。
开工与收工时 `git -C .synova-wt-gates-v status --porcelain` 均为空（无污染）。主工作区（D811 在飞）与 `.synova-wt-gates-a/-b` 全程零访问。

---

## 一、D846 先红：释放判定可被"工作树脏改"无痕伪造 —— **复现**

### 1.1 沙箱构造与主攻击路径

沙箱仓库：`docs/x.md`（tracked）+ 两份 brief（`D901-a`、`D902-b` 均在 Q2 认领 `docs/x.md`）+ **两个 task-state 卡都以 `claimed` 进 git 历史**（`git commit init`），随后伪造。

```
$ python3 $G --session-id D902 --staged docs/x.md      # $G = 沙箱内 staging_guard.py 副本
--- STEP 1: 基线（D901=claimed，无伪造）---
{"status": "block", "foreign_files": [{"file": "<staged>", "owner_session": "2026-09-20-D901-a", "brief": "/private/tmp/v1-d846-SfGb2I/.claude/task-briefs/2026-09-20-D901-a.md", "reason": "认领 brief D# 与本 session 任务不一致"}], "stray_files": [], "degraded": true, "release_cmd": "python3 scripts/control-tower/claim_release.py release --task D901 --reason \"<为什么可以释放>\"    # 等价入口: python3 scripts/control-tower/session_registry.py archive --session-id D901", "degraded_reason": "registry 缺失: /private/tmp/v1-d846-SfGb2I/.codex/control-tower/session-registry.json"}
EXIT=1

--- STEP 2: 工作树伪造（不 git add、不 commit）---
$ printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"
$ git -C "$SB" status --porcelain
 M task-state/D901.json
?? .codex/
$ git -C "$SB" show HEAD:task-state/D901.json      # git 可信版本未变
{"task_id":"D901","status":"claimed"}
$ git -C "$SB" log --oneline -- task-state/D901.json
350cf3b init                                        # 伪造零提交痕迹
$ git -C "$SB" diff --cached --stat                 # 零暂存
(空)

--- STEP 3: 重跑 guard ---
{"status": "warn", "foreign_files": [], "stray_files": [], "degraded": true, "released_claims": [{"brief": "2026-09-20-D901-a.md", "task_id": "D901", "basis": "task-state:impl_done", "detail": "task-state/D901.json status=impl_done"}], "release_reason": "D901 已释放（依据 task-state:impl_done: task-state/D901.json status=impl_done)", "degraded_reason": "registry 缺失: …"}
EXIT=0
```

**结论：复现。** `block / exit=1` → `warn / exit=0`。他人认领被解锁，攻击全程**不 add、不 commit**，`git show HEAD:` 与 `git log` 证明伪造从未进 git 历史（无痕）。K3 §① 的攻击路径在本 session 独立重跑成立。

### 1.2 file:line 依据（读工作树那一行）

```
$ sed -n '134,148p' scripts/control-tower/claim_release.py | nl -ba -v134
   142	def task_status(...) 内：
   142	    data, err = _read_json(_as_repo(repo) / "task-state" / f"{tid}.json")
$ grep -n "_read_json(_as_repo(repo) / \"task-state\"" scripts/control-tower/claim_release.py
142:    data, err = _read_json(_as_repo(repo) / "task-state" / f"{tid}.json")
$ grep -n "RELEASED_STATUSES = \|st in RELEASED_STATUSES\|rec.get(\"released\")" scripts/control-tower/claim_release.py
62:RELEASED_STATUSES = ("impl_done", "audited")
182:    if isinstance(rec, dict) and rec.get("released"):
187:    if st in RELEASED_STATUSES:
$ grep -rn "git show\|HEAD:task-state" scripts/control-tower/claim_release.py
rc=1 → 零命中（**无任何 git 可信版本读取**；:205-213 的 `_git` 仅用于 `ls-files` 文件清单）
$ grep -n 'if rel\["released"\]\|result\["status"\] = "warn"' scripts/control-tower/staging_guard.py
189:                    if rel["released"]:
221:            result["status"] = "warn"
```

链路：`staging_guard.py:188 → claim_release.is_released → task_status:142（读工作树文件）→ :187 status∈RELEASED_STATUSES → released=True → staging_guard.py:189/221 warn`。

### 1.3 反例矩阵（本 session 自造，K3 报告未覆盖的形式）

```
== V0 基线（全 claimed，台账空）==                 status= block
== V-A 只删文件不改内容（rm task-state/D901.json）== status= block  → fail-closed 成立（无伪造面）
== V-B 伪造后 git add（暂存但未提交）==             status= warn | released= ['task-state:impl_done']
== V-C status=claimed 但伪造台账（工作树写 claim-releases.json，不 add）==
                                                    status= warn | released= ['ledger'] | reason= 台账显式释放（伪造）
== V-C2 对照：git show HEAD:task-state/claim-releases.json ==
                                                    {"version":1,"releases":{}}   ← HEAD 版干净
== V-D status 变体 ==
  status=[IMPL_DONE]        → block   （精确匹配，未误放行）
  status=[impl_done ]       → warn    （.strip() 后命中 :148）
  status=[impl-done!! 42]   → block
== V-E task-state/D901.json 为符号链接指向 docs/evil.json（工作树）==  status= warn
```

**结论：复现（且伪造面比 K3 §① 记载的更宽）。** 3 条独立伪造路径全部命中：
- 路径①工作树改 status（主路径，1.1）
- 路径②伪造后被 `git add` 暂存（读取端只看工作树，暂存与否无关）
- 路径③**不碰 status、直接伪造 git 跟踪的 `task-state/claim-releases.json`（工作树脏改）→ `basis=ledger` 优先命中（:182）**，HEAD 版仍干净
- 路径④符号链接间接指向伪造内容

另：`release` 子命令无鉴权（无 owner 校验、无理由强制、无审计留痕门槛）：

```
$ python3 scripts/control-tower/claim_release.py --repo "$SB" release --task D901 --by attacker
✅ 已释放 D901 → 写入台账 /private/tmp/v1-auth-0bX3FE/task-state/claim-releases.json
exit=0
{ "releases": { "D901": { "at": "2026-09-20T20:26:59+08:00", "basis_at_release": "explicit-only",
  "by": "attacker", "reason": "显式释放", "released": true } }, "version": 1 }
```
（`claim_release.py:341-344`：`--task` 必填，`--reason` 默认"显式释放"，`--by` 取环境变量，**无身份校验**。）

> 说明：以上为**先红事实的刻画**，不是对修复方案的判据；也未评估威胁模型定级（属 K3 域）。

---

## 二、D847 先红：台账并发丢写 —— **复现（且发现第二个独立故障模式）**

### 2.1 主复现（N=8 并发 `release` 不同任务，30 轮）

沙箱：空 `task-state/`，脚本副本 `claim_release.py` + `write_lock.py`。每轮先删台账，再并发起 8 个进程释放 `D901…D908`，`wait` 后数台账记录数。

```
$ bash /tmp/v1-repro-d847.sh          # 完整脚本见附录 A.2
round=1  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=2  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=3  释放数=8  台账记录数=6  ❌ LOST-UPDATE（丢 2 条）
round=4  释放数=8  台账记录数=5  ❌ LOST-UPDATE（丢 3 条）
round=5  释放数=8  台账记录数=4  ❌ LOST-UPDATE（丢 4 条）
…
round=8  释放数=8  台账记录数=2  ❌ LOST-UPDATE（丢 6 条）
…
round=21 释放数=8  台账记录数=7  ❌ LOST-UPDATE（丢 1 条）
…
round=30 释放数=8  台账记录数=5  ❌ LOST-UPDATE（丢 3 条）
FIRST_BAD_ROUND=1                     # 第 1 轮即中（K3 记 2 进程 round0 中；本 session 8 进程第 1 轮中，等价结论）
== 复现后隔离验证：单独重放同一并发（5 次）==
replay#1 台账记录数=5/8
replay#2 台账记录数=5/8
replay#3 台账记录数=5/8
replay#4 台账记录数=5/8
replay#5 台账记录数=6/8
```

存活记录与丢失记录（原始台账摘要）：

```
记录数=4/8 ['D901', 'D905', 'D907', 'D908']
丢失= ['D902', 'D903', 'D904', 'D906']
```

**结论：复现。** 30/30 轮丢记录，丢失进程**自身仍打印 `✅ 已释放 …` 并 `exit 0`**——静默丢写（read-modify-write 无锁，`claim_release.py:105-124`）。

### 2.2 发现：丢写之外还有 **共享 tmp 文件名导致的未捕获崩溃**（K3 §⑥ 未记载）

同一实验逐进程捕获 stdout / exit code：

```
== 各进程独立 stdout / exit code ==
p1 rc=1 | Traceback (most recent call last):
  File "…/claim_release.py", line 406, in <module>   sys.exit(main())
  File "…/claim_release.py", line 380, in main       r = release_task(repo, args.task, args.reason, args.by)
  File "…/claim_release.py", line 313, in release_task   save_ledger(repo, ledger)
  File "…/claim_release.py", line 124, in save_ledger    os.replace(tmp, p)
FileNotFoundError: [Errno 2] No such file or directory: '…/task-state/claim-releases.json.tmp' -> '…/task-state/claim-releases.json'
p2 rc=0 | ✅ 已释放 D902 → 写入台账 …/task-state/claim-releases.json
p3 rc=0 | ✅ 已释放 D903 → …
p4 rc=0 | ✅ 已释放 D904 → …
p5 rc=0 | ✅ 已释放 D905 → …
p6 rc=0 | ✅ 已释放 D906 → …
p7 rc=0 | ✅ 已释放 D907 → …
p8 rc=0 | ✅ 已释放 D908 → …
```

机理：`tmp = p.with_suffix(".json.tmp")`（`claim_release.py:121`）→ **所有进程共用同一个 tmp 路径** `claim-releases.json.tmp`；A 写完 tmp 后，B 若已把该 tmp `os.replace` 掉，A 的 `os.replace` 即 `FileNotFoundError`。**该异常未捕获**（:117-124 无 try），进程 rc=1，stderr 抛 traceback。

40 轮 × N=8 分类计数：

```
$ bash /tmp/v1-repro-d847-instrumented.sh
ROUNDS=40 N=8
干净轮次(8/8 且零崩溃) = 0
lost-update 轮次(静默丢记录) = 40   首现: 1(entries=4,rcs= 0 1 0 1 0 0 0 0)
崩溃轮次(存在 rc!=0)        = 20  首现 round=1
台账 JSON 损坏轮次          = 0   首现: none
```

**结论：复现（两个独立故障模式）。**
- 模式①（静默丢写）：40/40 轮，rc=0 且打印成功 → 台账少记，与 K3 P1-3 同源，本 session 独立复现。
- 模式②（共享 tmp 崩溃）：20/40 轮存在 rc≠0 的未捕获 traceback → 算子只看到命令失败，**不知道失败原因、也不知道另一个进程已把台账改掉**；且失败进程的释放请求**可能已落在台账里也可能没落**（无幂等确认）。此为 K3 §⑥ 之外的**新增观察**，一并交 K3 判定级。
- 台账 JSON 损坏（半写文件被 replace 进正式路径）：**40 轮内未观测到**（`os.replace` 原子性在本次样本下生效）→ 标为**理论路径，未复现**，不作为结论。

### 2.3 file:line 依据

```
$ grep -n "write_lock" scripts/control-tower/claim_release.py
（无输出，rc=1 → 零引用：同目录已存在 write_lock.py，claim_release.py 未复用）
$ ls -la "$SB/scripts/control-tower/write_lock.py"   → 存在
$ grep -n "^class \|^def " scripts/control-tower/write_lock.py
42:class WriteLock:
176:def _emit_signal(status: str, reason: str) -> None:
188:def main():
$ grep -n 'with_suffix(".json.tmp")\|os.replace' scripts/control-tower/claim_release.py
121:    tmp = p.with_suffix(".json.tmp")
124:    os.replace(tmp, p)
```
（`claim_release.py:105-124` = read-modify-write 全程无锁；`task-status` 与台账读取均在锁外。）

---

## 三、第一阶段结论（口径）

**自验发现 3 个问题（第一阶段，先红事实成立）**：

| # | 问题 | 状态 | 证据 |
|---|---|---|---|
| 1 | D846：工作树脏改 `task-state/<D#>.json` → 他人认领 `block→warn`，零暂存零提交、无痕 | **复现** | §1.1 |
| 2 | D846：伪造面共 4 条（工作树 status / 暂存后 status / 工作树台账 ledger 优先级 / 符号链接），`release` 子命令无鉴权无留痕门槛 | **复现** | §1.3 |
| 3 | D847：台账并发丢写（30/30 轮）＋ **共享 tmp 文件名致未捕获 FileNotFoundError（20/40 轮崩溃）** | **复现** | §2.1 / §2.2 |

- 本阶段**无"未复现"项**（台账 JSON 半写损坏为理论路径未观测，已单列标注，不计入问题数）。
- 以上均为**先红刻画**，不含"修复是否有效"判断（第二阶段对 `gate/claim-release-a` 独立验证）。
- **可提请独立审计**（审计结论只认 K3；本报告自称"自验"，不含审计结论）。

## 四、未覆盖面（第一阶段，逐条列明）

1. **Windows / Git Bash 环境未验证**：本 session 仅在 macOS bash + 系统 python3 复现；跨平台差异（`os.replace`、路径分隔符、`shasum` 替代品）未跑。
2. **未走真实 `synova-commit` 端到端流程**：`staging_guard.py` 的生产调用点（synova-commit 内）未实跑，只直接调 guard CLI（K3 §L3 亦列为未覆盖）。
3. **未评估威胁模型定级**（P0/P1/P2）：属审计面，非自验面。
4. **未验证真实仓库全量 `release-stale --apply` 效果**（只跑沙箱）；真实 `task-state/claim-releases.json`（16 条）本 session 零写入、未做指纹比对以外的验证。
5. **并发场景未覆盖**：`claim_release.py scan` / `release-stale` 的并发行为、`write_lock.py` 现有锁原语自身的正确性、跨主机（NFS/多机）并发、以及"持锁进程被 kill"（第二阶段项，需先有锁实现）。
6. **未验证 `session-archived` 证据源（`claim_release.py:151-163`）的伪造面**：本次只验了 `task-state` 与 `ledger` 两个证据源。
7. **夹具 `tests/control-tower/staging_guard.test.sh` 自身的先红后绿未由本 session 复跑**（属第二阶段：对 A 分支跑夹具）。
8. 未复跑 K3 报告的其余条目（P1-2 CI 接线、P2-1…P2-5）。

## 五、附录：复现脚本（可原样重放）

### A.1 D846

```bash
#!/bin/bash
set -uo pipefail
W=/Users/wane/SynovaAgent/.synova-wt-gates-v
TODAY=$(date +%Y-%m-%d)
SB=$(mktemp -d /tmp/v1-d846-XXXXXX); echo "SANDBOX=$SB"
git -C "$SB" init -q; git -C "$SB" config user.email v@v.local; git -C "$SB" config user.name v
mkdir -p "$SB/.claude/task-briefs" "$SB/task-state" "$SB/scripts/control-tower" "$SB/scripts/workflow" "$SB/docs"
for f in staging_guard.py session_registry.py brief_parser.py write_lock.py declare-write-set.sh claim_release.py; do
  cp "$W/scripts/control-tower/$f" "$SB/scripts/control-tower/$f"; done
cp "$W/scripts/workflow/resolve-commit-brief.sh" "$SB/scripts/workflow/resolve-commit-brief.sh"
printf 'x\n' > "$SB/docs/x.md"
mk_brief() { cat > "$SB/.claude/task-briefs/${TODAY}-${1}-${2}.md" <<EOF
# Task Brief: ${1}
#CRITERIA: A

## Q0: 定位 — 夹具
## Q1: 调研 — 夹具
## Q2: 范围 — 夹具
做什么：
- ${3}

不做什么：
- 不改 docs/x.md.bak

## Q3: 验收 — 夹具
## 架构层: 基础设施
## Done 标准
- [ ] 夹具
EOF
}
mk_brief D901 a "docs/x.md"; mk_brief D902 b "docs/x.md"
printf '{"task_id":"D901","status":"claimed"}\n' > "$SB/task-state/D901.json"
printf '{"task_id":"D902","status":"claimed"}\n' > "$SB/task-state/D902.json"
git -C "$SB" add -A >/dev/null 2>&1; git -C "$SB" commit -qm init >/dev/null 2>&1   # claimed 进 git 历史
G="$SB/scripts/control-tower/staging_guard.py"
run() { (cd "$SB" && python3 "$G" --session-id D902 --staged docs/x.md 2>&1); echo "EXIT=$?"; }
run                                                                  # 基线 → block/1
printf '{"task_id":"D901","status":"impl_done"}\n' > "$SB/task-state/D901.json"   # 不 add 不 commit
git -C "$SB" status --porcelain; git -C "$SB" show HEAD:task-state/D901.json
run                                                                  # → warn/0
```

### A.2 D847

```bash
#!/bin/bash
set -uo pipefail
W=/Users/wane/SynovaAgent/.synova-wt-gates-v
SB=$(mktemp -d /tmp/v1-d847-XXXXXX)
mkdir -p "$SB/task-state" "$SB/scripts/control-tower"
cp "$W/scripts/control-tower/claim_release.py" "$W/scripts/control-tower/write_lock.py" "$SB/scripts/control-tower/"
C="$SB/scripts/control-tower/claim_release.py"
N=8
count() { python3 -c "
import json
try: print(len(json.load(open('$SB/task-state/claim-releases.json')).get('releases',{})))
except FileNotFoundError: print(-1)"; }
for round in $(seq 1 30); do
  rm -f "$SB/task-state/claim-releases.json"
  for i in $(seq 1 $N); do
    python3 "$C" --repo "$SB" release --task "D90$i" --reason "并发轮次 $round 任务 $i" --by "proc$i" >/dev/null 2>&1 &
  done
  wait
  got=$(count)
  [ "$got" = "$N" ] && echo "round=$round 台账=$got ✅" || echo "round=$round 释放=$N 台账=$got ❌ LOST-UPDATE"
done
```
