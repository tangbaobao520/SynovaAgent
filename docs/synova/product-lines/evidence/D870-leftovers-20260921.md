# D870 遗留清单（M6 三件之三）

> 队长 🎽 synova-squad-lead ｜ 2026-09-21 ｜ 工作树 `/Users/wane/SynovaAgent/.synova-wt-d870`（分支 `feat/d870-official-baseline-study`）
> 参照系（唯一）：`/Users/wane/src/deepseek-harness/` @ tag `dsh-v0.1.6-alpha.2` @ commit `ddefc45fbc7f8e46dd73185e68295696d1297887`
> 本件只登记**未闭环项与已知边界**，不做"通过"评语；是否可采用、能否合并归 CTO 收件闸 + K3 终审。

---

## 一 来源未入库（等 CTO 回"可读"后复验）

| # | 项 | 现状 | 影响面 |
|---|---|---|---|
| 1 | 任务书 **DSH 借鉴 14 项**（数据与可观测 6 + 基础设施与信任 8） | 宿主件在 **PR #674 未合**，D 切片只读 `origin/chore/ingest-institute` @ `9cce8b87` 取证 | D 件 **29 处**逐条标注"来源未入库"（条目行 14 + 9，另表头/出处/存疑项） | 
| 2 | 任务书 §二 **9 项撤回清单**（宿主同上） | 同上 | 9/9 已核，但取材来源未入库 |
| 3 | 插件两件（复盘-插件推荐失误 / 插件尽调-三个第三方控制插件） | 同上（PR #674 未合） | **C 切片不依赖它们**（措辞裁定后取证全由官方源码推导），但若将来要谈"插件准入"，其事实基础仍待入库 |

**复验动作**：#674 落 main 后，按 main 版重跑 D 件的 14 项 / 9 项撤回 / 两件插件材料，更新"来源未入库"标注。

## 二 悬置待裁定

1. **第 15 项实体**：附录件《可复制-DSH对比与任务书》写"15 项"，两件任务书实际 6+8=**14**；D 查出唯一候选实体 = 总纲 **P2-5 联网检索**（无 8 问章节、未列 DSH 参考包），已单列备查。**按 14 收口，第 15 项实体悬置待 CTO 裁定。**
2. **`dsh-client-runtime` 归属未定**：官方树无此名，最近似 `dsh-client-test-runtime`（`packages/test-support/client-runtime`）但**非同物**；我方 `dsh/plugins/synova-dashboards/package.json:25` 的依赖声明意图未查（属插件侧，超出本件写集）。

## 三 取证面未穷尽（各切片自报，如实登记）

| 切片 | 未覆盖 |
|---|---|
| A | `apps/desktop/src` 41 个 `.ts` 精读 20 个，**21 个未精读**（`mandatory-update-*.ts`(3)、`locale.ts`、`policy-test-auth.ts`、`project-manager.ts`、其余 `preload-*` 等）；`apps/desktop/tests` 75 个 spec 的测试手法未纳入 39 条；Windows 侧（NSIS/signtool）只有构建脚本证据，**本机无实测断面** |
| B | `packages/client` **55 个插件目录精读约 14 个，41 个未精读**（含 `ui-approval` / `ui-agent-preset` / `ui-layout` / `ui-session` / `ui-goal` / `ui-plan` / `ui-workflow-run` / `ui-slots` / `ui-dockkit` 面板引擎）；**未运行官方前端，无任何观感结论** |
| C | `maxTokens` 与上下文溢出完整适配未展开为独立用例；图片请求字节预算（`LlmImageRequestBudget` / `IMAGE_OFFLOAD_REQUIRED`）仅提示存在；`reasoningEffort` 归一化取值域未穷举；**打包运行时 0.1.6-alpha.1 与 checkout 0.1.6-alpha.2 的差异未交叉验证**；ACP 侧 `serialize()` 队列语义只取到调用点；`llm-replay` override/patch 机制只取到接口 |
| D | **表格内联锚点**（`path:12-16,49-52` 式）写法不可机械解析，**未逐条量化——本件最大未覆盖面**；打包运行时断面未作判据（3 条 OUT_OF_REF 单列）；`vendor/cordis` 三方属性未核 |
| V | 未跑官方树构建/测试 → **`lib/` 构建产物是否由 pinned commit 构建无法验证**；打包运行时 0.1.6-alpha.1 断面未独立取证；部分条目仅锚点级复算；改法卡量级为作者**初估未实测**；归线编号未逐条回 `product-lines.yaml` 核对（只核了 `gap-line-mapping.yaml` 11/11） |

## 四 参照系可复现性边界（重要，须与结论成对引用）

**D 切片 10 条借鉴项的 `official` 锚点指向官方树 `.gitignore:7` 忽略的 `lib/` 构建产物**（`git ls-files` 不跟踪；`git ls-tree ddefc45 | grep -c` = 0）：

- 在**本机**（= 当前官方运行实例，`lsof` 证明 `127.0.0.1:19387` 由它派生的 Electron 持有）**可复现**——文件在磁盘、内容与断言逐行吻合；
- 换**干净 checkout**（`git clone` + `git checkout ddefc45`）**不可复现**。

→ 已在总表 §6.1 逐条标注 + 单列改法卡 **D903**（与 D901 引用存在性、D902 行号漂移并列为三个互不相交的维度）。
→ **下游禁令（已写入总表）**：不得把它当"失效引用"；不得在干净 checkout 上据此判定作者断言不实。

**受影响的 10 条**：D-01 / D-03 / D-04 / D-05 / D-06 / D-07 / D-08 / D-09 / D-10 / D-12（**全部出自 D 切片，A/B/C 各 0 条**；命令判定：`git check-ignore -v` 命中 `.gitignore:7:lib/` + `git ls-files --error-unmatch` exit 1）。

## 五 计数与口径（须成对引用，防交叉复算对不上）

1. **总表三分类双口径**：口径 A（按 `official` 字面含"官方无实现"切分）= **104**（66 / 36 / 2，**对外主计数**，源自 CTO 原口径直译）；口径 B（按是否指向官方对应物）= **103**。差值恒为 `C-23` 一条（边界声明项）。两数**并记在总表 §2.0**。
2. **行数一律 `wc -l`**；**行数必须与其 sha256 成对引用**——本件过程中出现过 3 次口径漂移（详见 §六），单独引用行数会误判。
3. **改法卡卡数**：40 张 = 直接执行 33（D871–D903）+ 自研立项 7（D904–D910）；其中 **D901/D902/D903 三张治理卡不消费借鉴条目** → "消费条目的直接卡" = **30**，与 §一的 33 不同义（总表已写死口径）。

## 六 过程偏差（本次实际发生，如实登记）

| # | 偏差 | 成因 | 处置 |
|---|---|---|---|
| 1 | **密度数字偏高（3/12 项）** | lead 探针用**文件系统 grep**，计入 untracked 构建产物（`apps/desktop/lib/types/*.d.ts`）；canonical 应为 `git grep`（tracked-only） | coder-ab 在 A 切片拦下 → lead 定点复算 → 修正表下发 A/B/C/D 四切片并落进文件（BrowserWindow 77→63、electron-updater 22→19/23、requestSingleInstanceLock 6→5、theme 3290→2865、thinking 1982→1809、density 3004→2610、scroll 2504→2067、trace 1106→998、animate 683→603、EmptyState 25→19、switchModel 132→119、defaultModel 117→115、tokenMeter 203→176、reasoning_effort 486→458） |
| 2 | **探针假阴性** | lead 首轮误用 `-E` 与 `\|` 混写 → `(a\|b)` 被当字面量 → B/C 面密度误判为 0 | lead 自纠重跑（此为**同源第二次**，已按坑清单"同类第二次升级 CTO"报备） |
| 3 | **基线口径漂移** | D 切片④原报 198 为旧 main `3643db6a` 口径，工作树已推进至 `1062ba2f` | 冻结为 **199**（净增 1 = 派单件自身含 `@deepseek-ai` 字串），已通知 coder-d |
| 4 | **前提勘误（非数字）** | 原报 `(EmptyState\|…)` 25 行曾被当作"官方有空态组件"的暗示；实测 `git grep 'EmptyState' -- 'packages/**/*.tsx'` = **0**，19 处命中无一是 React 组件 | coder-ab 提出 → lead 复核成立 → B 切片新增 **B-35**（官方空态真实机制 = 具名槽位 + 名单键内联文案；`slots.ts:19-20`"无人占用则入口整体不存在、不留死按钮"），不判为"官方无实现" |
| 5 | **证据文件落点偏差** | **责任在 lead**：任务描述只给相对路径、未锚定工作树根 → verifier 写入主工作树，违反 CTO 硬边界/M5「禁止写主树」 | verifier 按指示迁入工作树内 `docs/synova/product-lines/evidence/`，删除主树 stray，主树 `git status --porcelain` 恢复**空**（前后原始输出见证据文件 §6.5）|
| 6 | **行数口径自纠** | coder-c 用 `count('\n')+1` → 多算 1（C 628→627）；D 一栏为两种成因叠加（+1 公式 + D 件自身修订 472→485） | 一律改 `wc -l`；总表 §1.2 写死口径并注明时点 |
| 7 | **一行数字不可复现** | D 件 §4.2 #7「597 键」系抽取脚本中间产物（`pkgmap 291 + pkgmap_full 306`，同包两种键形各计一次） | verifier 复核拦下 → coder-d 自查确认按设计不可复现 → 换为可复现数字 + 3 条命令（291/302/0），结论不变 |
| 8 | **一行结论表述过宽** | D 件 §4.2 #7 建议列写「官方无对应包」；实测官方 `docs/dependency-catalog.json:3196` **登记该包名为外部依赖**（`direct: true`） | lead 复核后自推翻"永久冻结"、解除冻结修 #7 两列（判据收窄为"官方 workspace 无此包名"，建议改为指向 G1 合规问题）；#8/#9 实测无此问题，不动 |
| 9 | **落点/文件数口径** | 证据文件并入后工作树不再"恰好七份" | 按 D860 口径分列：**被验件 6（不计…）+ 治理产物 2**，并在证据文件 §6 注明"恰好七份"的判定时点为并入前 |

## 七 未做的事（边界声明）

1. **本件不改码**：全部为对照表 + 借鉴总表 + 改法卡**建议**；一行产品代码未动。
2. **未引入任何依赖**：G1 守卫实测仍为 0（`grep -rn "@deepseek-ai" src/ packages/`）。
3. **未跑重型验证**：写集全为 `docs/**` + `task-state/**` → CT-34 纯文档豁免（豁免 12 组，仅 Secrets 扫描），**无 vitest/tsc 触发**，故 8GB 机器的"重型验证串行 ≤1"约束本件不触发。
4. **未推送前的状态**：K3 复审通过前不合并；交付时附 `git ls-remote --heads origin | grep feat/d870-official-baseline-study` 回执。
5. **改法卡编号为建议**：D871–D910 由 coder-c 实测未占用后顺延（`task-state/` 最高号 D870；`board-backlog.json` 与 tracked 全仓库对 D871 均 0 命中），**最终编号权在 CTO**。
6. **`official-*` 后续**：改动任一份被验件 → 证据文件 §1 的行数/哈希对即失效，须新开一轮重测（verifier 脚本留在 `/tmp/d870-verify/`）。
