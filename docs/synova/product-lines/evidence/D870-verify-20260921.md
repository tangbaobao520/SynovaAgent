# D870-V 独立自验证据（task-6）

> 自验员：verifier（D870 小队独立成员，与三名编码员平级，**未参与任何切片写作**）
> 自验时间：2026-09-21（本机时区 CST；命令原样可复现）
> 参照系（唯一）：`/Users/wane/src/deepseek-harness/` @ tag `dsh-v0.1.6-alpha.2` @ commit `ddefc45fbc7f8e46dd73185e68295696d1297887`
> 工作树：`/Users/wane/SynovaAgent/.synova-wt-d870`（分支 `feat/d870-official-baseline-study`）
> 本文件为只读自验的证据登记；**verifier 不改任何编码员产出文件、未 commit、未 push**。
> **落点（2026-09-21 纠偏）**：本证据文件按 CTO 硬边界 / M5 落在 **D870 工作树内** `docs/synova/product-lines/evidence/D870-verify-20260921.md`。此前误落的**主工作树同名 stray 副本已按 lead 指示删除**（删除前后 `git status --porcelain` 原始输出见 §6.5）。

## 0. 口径（先钉）

- 自验只输出 **通过项 / 不一致项 / 未覆盖项** 三类"自验结论"；**不说"审计通过"**、**不宣称产品被验证**。
- 一切数字用我自己重跑命令的**原始输出**，不采信作者贴的输出（作者贴的输出另做逐字节比对，见 §3）。
- "grep 命中"不作为验收依据：凡"该机制被引用/被执行"类断言，另找**引用点**（§5 判别性检查）。
- 复核率分母 = 四切片机器可读块条目总数 109（含"官方无实现"条），另附辅助面（数字断言 / 用例 / 对账表 / 卡清单）。

## 1. 被验对象与复核时哈希（shasum -a 256）

```
a26b047567b7dc644334ec90feedae6a7a111d041bc4c725d16efd3bf905f9b8  official-electron-kernel-runtime.md   (A, 1008 行)
fae478a8a68171581fd2740298a12cd1392a0281932f4886b97737f149dffc81  official-ui-patterns.md               (B, 1095 行) ← 退回修复后版本
53cabf6959c3c9753183af6e14bb14d0207ed235467d881228f34f9312de66ed  official-model-switching.md           (C, 627 行)
92ddcff5d4dd12f47b9584048ce9c3ab23a3655f92c7984de30fca786a8f17d7  official-borrowing-recheck.md         (D, 490 行) ← 「#7 判据射程收窄」修复后版本（R3，终值）
47b05878b2acea5ac611945f657b8ed8f32a464801127c7b1c27502f15097079  official-borrowing-table.md           (总表, 606 行) ← §1.2 二次同步（D 490 指纹）后版本（R3，终值）；行数由我 `wc -l` 实测
bde42e690d96767e6dce0585a5a9e9f9dd09634066846492fd4737be1fb9869b  official-change-cards.md              (改法卡, 291 行) ← 计数修正后版本（按 lead 指示对新哈希重跑受影响项）
```

> **行数/哈希对必须成对冻结**：本表每行的"哈希 + 行数"是**同一时刻的实测对**。R1 记录的总表 `eebbb93a…`（443 行）在其后被 §1.2 指纹段同步取代（新哈希 `3e25a796…`、**606 行**，我 `wc -l` 实测；lead 通知的 601 我在当前版本**未能复现**，以实测为准）——这正是 D 切片查出的"引用锚点随被引文件改动而漂移"同类现象，故 R2 起本表**随哈希同步更新行数**。R3 定稿时该件再次同步（§1.2 补 D 的 490 指纹），哈希变为 `47b05878…`、仍 606 行，已按**定稿时实测**更新。

## 2. 复核率

| 面 | 分子（我独立复算） | 分母 | 复核率 |
|---|---|---|---|
| 机器可读块条目 `official` 锚点（file:line → 非空真实代码行） | **109** | 109 | **100%** |
| 其中：锚点可解析且内容与断言吻合 | 103（另 6 条为"官方无实现/无行号/边界项"或已人工说明） | 109 | 94.5% |
| 语义级抽验（读实际代码行核对机制，非只证明字符串存在） | ≥ 63 | 109 | ≥ 57.8% |
| 数字断言（重跑同命令） | 42 项数字/命令组 | — | 全部重跑 |
| 作者粘贴的 `sed` 输出逐字节比对（A/B） | 128 行（74 + 54） | 128 | 100%，mismatch = 0 |
| C 必测用例四要素 | 12 条全齐 | 12 | 100% |
| D 四项覆盖对账 | 14 / 11 / 10 / 199 全核 | 4 项 | 100% |
| 总表 ↔ 四切片条目归属 | 109 条 | 109 | 100% |

**总口径复核率 = 109 / 109 = 100%（条目级锚点全量复算）；语义级 ≥ 57.8%（远超任务要求的 ≥30%）。**

**R3（定稿）复跑记录**：D 件换数升级后（`92ddcff5…` / 490 行）12 条 JSON 条目 + 11 条锚点落到真实代码行 + `来源未入库` 29 行含串（出现 30 次；条目行 23）复跑不变；**新增 #7 断言逐项独立复现**（见 §3.2 R3）。总表二次同步后（`47b05878…` / 606 行）聚合复算：**109 条 / 66+36+2 / 官方无实现 5**，五处 id 清单零差异、109 行五字段逐字保留；总表 §1.2 记录的四件指纹（A/B/C/D 哈希+行数）与我实测**逐字一致**。

**R2（终版）复跑记录**：① D 件换数后（`2ce8fca6…`）12 条 JSON 条目重跑——11 条锚点落到真实代码行、1 条（D-11）无行号锚点（同 R1 判定）；`来源未入库` 逐条标注 **14（§1.2 条目行）+ 9（§1.4 条目行）= 23 行**（全文含该串 29 行、出现 30 次，`:442` 一行两次），与"应有 29"口径一致（按行计）；JSON 块仍解析出 **12 条**；3 条新增命令我逐条重跑 = **291 / 302 / 0**。② 总表同步后（`3e25a796…`）聚合复算：**109 条 / 三分类 66+36+2 / 官方无实现 5**，§4.1/4.2/4.3/§五/§2.1 五处 id 清单与源机器块零差异、109 行五字段逐字保留。**复核率表述不变。**

## 3. 抽样清单（条目 ID + 我的复算命令 + 原始输出 + 判定）

### 3.1 全量逐条锚点复算（109 条，命令与原始输出见 §7 附件 dump）

命令（我对每条 `official` 的首个 file:line 独立取行）：

```bash
# 解析四份文件末尾 json 块 → 逐条 sed；脚本 /tmp/d870-verify/final_items.py（只读）
python3 /tmp/d870-verify/final_items.py
```

原始输出（汇总）：

```
A: items=39 锚点解析到非空行=37 flagged=2
   FLAG: ('A-16', 'no file:line in official', '（官方无实现）')
   FLAG: ('A-38', 'no file:line in official', '（官方无实现）')
B: items=35 锚点解析到非空行=35 flagged=0
C: items=23 锚点解析到非空行=20 flagged=3
   FLAG: ('C-14', 'EMPTY', 'packages/llm/token-meter/src/projection.ts:19')
   FLAG: ('C-20', 'FILE-MISSING', 'retry.spec.ts:464')
   FLAG: ('C-23', 'no file:line in official', '借鉴边界(按 G1 守卫与 CTO 口径)，非官方对应物')
D: items=12 锚点解析到非空行=11 flagged=1
   FLAG: ('D-11', 'no file:line in official', 'packages/web/tool-web（README.zh.md 概述段）')

合计: 103/109 条目的 official 锚点独立重跑解析到真实非空代码行 = 94.5%
```

6 条 flagged 的逐条人工判定：

| 条目 | 现象 | 我的独立判定 |
|---|---|---|
| A-16 / A-38 | `official` = `（官方无实现）` | **一致**：A 件按 CTO 裁定明标官方无实现；我独立反证 `git grep -n 'powerSaveBlocker'` = 0、`git grep -in '\btray\b'` = 0 |
| C-14 | 区间起点 `projection.ts:19` 是空行 | **实质一致（格式瑕疵）**：区间 19-38 内的 `:22-28` 正是所声称的"允许不一致"原文（见 §5） |
| C-20 | `retry.spec.ts:464` 为裸文件名 | **实质一致（写法瑕疵）**：树内唯一解析为 `packages/llm/llm-retry/tests/retry.spec.ts:464`，其 `it('selects policy by the failed request provider')` 正是"重试停留在失败 provider"的断言 |
| C-23 | 无官方对应物（边界项） | **一致**：C 件已自声明为边界项；总表 §六 已单列 |
| D-11 | 只有包路径无行号 | **一致**：总表 §六 已单列并说明"提卡前须回官方树补行号" |

### 3.2 数字断言复算（重跑作者/派单件同命令）

**A 切片**（作者命令：`git grep -c -- <pat> -- 'apps/*' 'packages/*'` 行计数）

```console
$ cd /Users/wane/src/deepseek-harness
$ for pat in BrowserWindow "electron-updater" contextBridge "ipcRenderer.invoke" "ipcMain.handle" powerMonitor requestSingleInstanceLock "before-quit" "app.whenReady" "window-all-closed"; do printf "%-26s %s\n" "$pat" "$(git grep -c -- "$pat" -- 'apps/*' 'packages/*' | awk -F: '{s+=$NF} END {print s+0}')"; done
BrowserWindow              63
electron-updater           23
contextBridge              18
ipcRenderer.invoke         15
ipcMain.handle             10
powerMonitor                9
requestSingleInstanceLock   5
before-quit                 5
app.whenReady               4
window-all-closed           2
```

> A 件 §一 表（63/23/18/15/10/9/5/5/4/2）**逐字一致**。旁证：`grep -rn "BrowserWindow" apps/desktop packages --exclude-dir=node_modules | wc -l` = 93；`git grep -o -- "BrowserWindow" -- 'apps/*' 'packages/*' | wc -l` = 69；`git -C … status --porcelain --untracked-files=all | wc -l` = 0；`git grep -cw -- "Tray" | wc -l` = 0；`grep -rni "tray" apps/desktop/src/ apps/desktop-host/src/ | wc -l` = 0；`grep -rni "Tray" apps/ packages/ --include=*.ts --include=*.tsx | wc -l` = 182 —— 全部与 A 件声称一致。
> 派单卡原值 77/22/6 与 A 件口径均不重合，A 件已如实登记"成因未查明"并改用 tracked 口径（不构成不一致，属前提偏差已声明）。

**B 切片**（canonical：`git grep -n -E`，范围 `apps/**` + `packages/**`，扩展名 `.ts/.tsx/.mjs/.css`）

```
(EmptyState|empty-state|emptyState)      19
(dark|light|theme)                       2865
(animate|animation|transition)           603
(trace|trajectory)                       998
(thinking|reasoning)                     1809
(density|dense|compact)                  2610
scroll                                   2067
```

> B 件 §1.2 七个数字**逐字一致**；`(EmptyState|…)` 逐文件分解（4/3/2/2/2/2/1/1/1/1）亦一致。
> `git ls-files 'packages/client/**/*.tsx'` = 412、`*.css` = 154、`git ls-files 'apps/web/src/*.ts'` = 4、`git ls-files 'apps/web/*'` = 278、`ls -d packages/client/*/` = 55 —— 全部一致。
> 退回项（148）：见 §4 不一致清单 1。

**C 切片**（canonical：`git grep -n -E … -- 'apps/**/*.ts' 'apps/**/*.tsx' 'packages/**/*.ts' 'packages/**/*.tsx'`）

```
(switchModel|setModel|selectModel|modelId)     119
(defaultModel|default-model)                   115
(tokenMeter|token-meter|tokenUsage)            176
(modelConfig|model-config)                       0
(reasoning_effort|reasoningEffort)             458
```

> C 件 §〇-3 五行**逐字一致**（与派单卡 132/117/203/486 的差异，C 件已归因于口径并声明）。
> C 件 §1.2 复算：`git show bf325e51:<两件插件文件> | grep -c 模型/切换` = 0/0/0/0；两文件 205/253 行；`git branch -a --contains bf325e51` 列出 `chore/ingest-institute`/`rs-674`/`remotes/origin/…`；`git merge-base --is-ancestor bf325e51 HEAD` → **非 main 祖先**；`git -C /Users/wane/SynovaAgent grep -l "模型切换" HEAD | wc -l` = 5 —— **全部一致**。

**D 切片**（canonical：tracked-only `git grep -l -E "deepseek-harness|@deepseek-ai" HEAD`）

```
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 grep -l -E "deepseek-harness|@deepseek-ai" HEAD | wc -l
     199
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 grep -l -E "deepseek-harness|@deepseek-ai" 3643db6a | wc -l
     198
$ comm -13 <(…3643db6a…) <(…HEAD…)   # HEAD 比旧 main 新增的唯一命中
docs/synova/dispatch/D870-官方参照系学习计划-20260921.md
```

> D 件 §0.2 的 199（canonical）/198（旧 main）/逐目录分解（69/47/28/22/6/6/3/3/3/3/2/1×7 = 199）**逐字一致**；"+1 唯一来源 = #699 派单件"经我 `comm` 实测确认。
> D 件 §1.1：任务书缺口 `grep -c '^## 缺口'` = 6（数据层）/ 8（信任层）= 14；`可复制-DSH对比…:195` 写"15 项"；§二 撤回记录 9 条 —— 全部一致。
> D 件 §三：`ledger.json` B-11…B-20 行号 4942/4953/4964/4975/4986/4997/5008/5019/5030/5041（:4953-5008 覆盖 B-12~B-17）；`board-backlog.json:304-307` = B-20 未登记；`DSH借鉴指引-v2-20260904.md` B-11 在 :95、B-18 :102、B-19 :103、B-20 :104 —— 全部一致（卡面"96-101"确为偏窄）。
> D 件 §4.1：第一层 199 文件 / 219 提及 / 81 唯一引用；§4.2 失效清单 11 条；§4.3 第二层全量组 95 + 抽样组 17 → 抽样率 **16.3% ≥ 10%** —— 结构自洽，我与前提核对无冲突。
> **R3 补充（D 射程收窄后 `92ddcff5…` / 490 行）—— #7 新增断言逐项独立取证（不采信作者）**：
> ① `grep -n 'dsh-workflow-worker-thread' $R/docs/dependency-catalog.json` → **3196**（`"name": "@deepseek-ai/dsh-workflow-worker-thread",`）与 **3200**（`"location": "node_modules/@deepseek-ai/dsh-workflow-worker-thread",`），**与作者所述一致**；
> ② 该条目的三个字段我实读 `sed -n '3194,3205p'`：`"version": "0.1.5-rc.2"`、`"direct": true`（`optional: false / peer: false`）、`location = node_modules/@deepseek-ai/dsh-workflow-worker-thread` —— **全部如作者所述**；
> ③ 「当前磁盘该目录 MISSING」：`ls -d $R/node_modules/@deepseek-ai/dsh-workflow-worker-thread` → `No such file or directory`（exit=1）**成立**；`ls -1 $R/node_modules/@deepseek-ai | wc -l` = **13**，**与作者"仅 13 个包"一致**；
> ④ 两个计数口径复现：`grep -c '"name": "@deepseek-ai/'` = **243**；裸 `grep -c '"@deepseek-ai/'` = **245** —— **两者成立**（catalog 共 7312 行）；
> ⑤ 旁证：catalog 顶层 = `"package": "@deepseek-ai/dsh"` / `"version": "0.1.5-rc.1"` / `"capturedAt": "2026-09-12T08:56:20.434Z"` —— 与作者"顶层 = 官方 dsh 0.1.5-rc.1、capturedAt 2026-09-12"一致。
> **判定**：#7 收窄后的口径（「官方 workspace 无此包名」+「官方确有该名，系外部依赖 direct=true」）**成立**，且修正了 R1/R2 之前"官方无对应包"的射程过宽；建议列改判"核必要性 + G1 合规、按'复制思路不引入依赖'重写"**与本件 G1 守卫一致**。

> **R2 补充（D 换数后 `2ce8fca6…`）**：① 我重跑 coder-d 新增的 3 条命令，**逐条复现 291 / 302 / 0**（`find $R/packages -maxdepth 3 -name package.json -not -path '*/node_modules/*' | wc -l` = 291；`grep -rho '"@deepseek-ai/[a-z0-9-]*"' <上述 manifest> | sort -u | wc -l` = 302；`grep -rl 'dsh-workflow-worker-thread' $R --include=package.json | wc -l` = 0）；② 交叉核补充事实：该包名在官方树**并非零出现**——`docs/dependency-catalog.json:3196` 以 `node_modules/@deepseek-ai/dsh-workflow-worker-thread` 登记为**外部依赖**，另有 3 份 `.agents/notes/archived/**` 提到该名（含 2026-08-11 命名契约重命名台账）。故权威读法应为「**官方树内无此 workspace 包（无 `packages/**/package.json` 声明、无目录）**」，#7 的修订稿把判据绑定在 `--include=package.json` 上，措辞已精确，结论（PKG_NOT_IN_TREE → 我方草稿须改自研命名）不变。

**总表 / 改法卡**（我自写聚合脚本独立复算）

```
$ python3 /tmp/d870-verify/aggregate.py
A: items=39 unique=39 class={'可直接复制': 24, '可参照': 13, '核心自研': 2}
B: items=35 unique=35 class={'可直接复制': 25, '可参照': 10}
C: items=23 unique=23 class={'可参照': 8, '可直接复制': 11, '核心自研': 4}
D: items=12 unique=12 class={'可直接复制': 6, '可参照': 5, '核心自研': 1}
total items = 109
$ python3 /tmp/d870-verify/check_table.py
预期（我按 lead 判据自算）: 可直接复制=66 可参照=36 核心自研=2 官方无实现=5
总表 §4.1=66 §4.2=36 §4.3=2 §五=5 §2.1=5
  可直接复制: 漏=[] 多=[]   可参照: 漏=[] 多=[]   核心自研: 漏=[] 多=[]   官方无实现: 漏=[] 多=[]
§2.1 索引与 §五 一致: True
四切片全部 id: 109 | 总表未提及的 id: []
总表行=109 五字段逐字全含=109
```

> 总表 **109 条 / 三分类 66+36+2 / 官方无实现 5** 与我独立聚合**逐条一致**；109 行的 title/class/official/action/scope_for_us 五字段对源机器块**逐字保留**（含 `\|` 转义归一后 109/109 全含）。
> 改法卡（初版哈希 `71158aca…`）：`D871…D910` 编号连续无缺号重号；40 张卡（§一 33 + §二 7）；109 条借鉴项 **109/109 被覆盖**（102 条进直接卡 + 7 条进立项卡），未覆盖 0。该版 §三 标题与 §四 计数含内部陈旧数字（见 §4 不一致 #2/#3）。
> 改法卡（**修正后哈希 `bde42e69…`**，按 lead 指示对新哈希重跑受影响项）：§三 标题 = 「覆盖 6 + 部分 2 + 未提卡 3」✓（与正文 `:191` 及 JSON `gap_line_reconcile` 三处一致）；§四 标题 = 「109 条借鉴项 → 40 张卡〔直接执行 33 + 自研立项 7〕」✓、合计行 = 「102 条 / 30 卡」✓、新增卡数口径块「30 = 33 − 3 治理卡；30+7=37 张卡 / 102+7=109 条」✓；JSON `counts={total:40,direct:33,self_dev:7,governance_cards:3}` ✓。编号连续性与 109/109 条目归属复跑不变 ✓。

### 3.3 §6.1"构建产物路径条目 = 10 条"独立复算

```
pinned 树内 tracked 路径的条目: 79
磁盘存在但 pinned 不跟踪（构建产物）的条目: 10
    ('D', 'D-01', 'packages/runtime-diagnostics/invariants/lib/index.js')
    ('D', 'D-03', 'packages/util/output-retention/lib/index.js')
    ('D', 'D-04', 'packages/spill/spill/lib/index.js')
    ('D', 'D-05', 'packages/webhook/webhook/lib/types/brand.d.ts')
    ('D', 'D-06', 'packages/util/http-proxy/lib/index.js')
    ('D', 'D-07', 'packages/settings/settings/lib/types/index.d.ts')
    ('D', 'D-08', 'packages/session/session-stats/lib/types/projection.js')
    ('D', 'D-09', 'packages/context/session-reference/lib/index.js')
    ('D', 'D-10', 'packages/context/time-context/lib/index.js')
    ('D', 'D-12', 'packages/credentials/credentials-local/lib/index.js')
```

> 判定 **一致**：确为 10 条、全部来自 D 切片、判据（磁盘存在 + `git ls-files --error-unmatch` 失败 + `.gitignore:7:lib/`）成立。
> 注：此 10 条的**行内容**我已逐条 `sed` 复现（§3.1 dump + §5 抽验），锚点在**本机工作树**可复现；但它们是 **pinned commit 不跟踪的构建产物**（干净 checkout 无此目录）——总表 §6.1 已显性标注，改法卡 D903 已就该口径提卡。verifier 记录：**"本机可复现"≠"由 pinned commit 可复现"**，两者须并列声明。

## 4. 不一致清单（作者断言 vs 我的实测）

| # | 件 / 位置 | 作者断言 | 我的实测 | 处理 |
|---|---|---|---|---|
| 1 | B `official-ui-patterns.md`（旧哈希 `b56384b2…`）L50/L56 | "apps/web 全量 tracked … 其中 **148** 个在 tests/（e2e 规格）" | `git ls-files 'apps/web/tests/*'` = **265**；`*.e2e.ts` = **118**；148 仅为该目录**非递归顶层文件数**（`ls -1p apps/web/tests \| grep -vc '/'` = 148；目录另有 4 子目录） | **已退回**（send_message → coder-ab + 报 lead）。作者已改：新哈希 `fae478a8…`，148 只作"非递归顶层文件数"出现并附勘误留痕 + 3 条可复现命令。**我复验：新版本该行/数字一致、JSON 块 35 条未受影响、35/35 锚点与 54/54 逐字节比对仍通过 → 本项关闭** |
| 2 | 改法卡 `official-change-cards.md:175` §三 标题 | "（如实：**覆盖 7 + 部分 1** + 未提卡 3）" | 同件正文 `:191` 与机器可读 `gap_line_reconcile` 均为 **已提卡 6 + 部分覆盖 2 + 未提卡 3**；逐行点数支持 6/2/3（#2/#4/#5/#8/#9/#10 = 6；#7/#11 = 2） | **已修 + 已复验关闭**（lead 决定修 → coder-c 改；新哈希 `bde42e69…`）：标题改为「覆盖 6 + 部分 2 + 未提卡 3」，我复验与正文/JSON 一致 |
| 3 | 改法卡 `official-change-cards.md:195,203` §四 | 标题"109 条借鉴项 → **39 张卡**"；合计行"**102 条 / 32 卡**" | 同件 §一 = D871–D903（**33 张**）、§二 = D904–D910（**7 张**）、JSON `counts.total = 40`；其中 D901/D902/D903 三张治理卡不消费条目 → 消费条目的直接卡 = **30**。条目级覆盖 102 + 7 = 109 正确 | **已修 + 已复验关闭**（新哈希 `bde42e69…`）：标题改「40 张卡〔直接执行 33 + 自研立项 7〕」、合计行改「102 条 / 30 卡」并补卡数口径块；我复验四处自洽 |

| 4 | D `official-borrowing-recheck.md` §4.2 #7 | "全树 package.json（**597 键**）无此名" | 该**结论**成立（我独立复算：`@deepseek-ai/dsh-workflow-worker-thread` 在树内 package.json 依赖名 0 命中）；但 **597 键**未能复现：tracked `package.json` = **324** 个、唯一依赖名 = **432**、唯一包名 = **322**、并集 = **463**、磁盘（排除 node_modules）= **327**，件内未给该数字的命令 | **已修 + 已复验关闭（含射程升级）**：R2 换数（597 → 291/302/0，我重跑 ✓）；**R3 进一步把 #7 判据收窄为「官方 workspace（`packages/**/package.json`）无此包名」并补「官方确有该名」事实（`docs/dependency-catalog.json:3196`，`version 0.1.5-rc.2`、`direct: true`、`location node_modules/@deepseek-ai/…`、磁盘 MISSING）**——我逐项独立复核全部成立（见 §3.2 R3）；建议列从"改自研命名"改为"核必要性 + G1 合规"。新哈希 `92ddcff5…`、490 行 |

**说明**：以上 4 条均**不影响**判据性结论（A 9 面 / B ≥10 模式 / C ≥8 用例四要素 / D 四项覆盖 / 总表 ≥40 条与三分类计数）。另有一条**已被吸收**的备注：D 切片锚点落在 pinned 不跟踪的 `lib/` 构建产物 —— 总表 §6.1 + 改法卡 D903 已显性化，本项不再计为不一致。

## 5. 判别性检查（"删掉即报红"，非"grep 命中即通过"）

**A（官方树实读 + 引用点）**

- A-01 单实例锁**先于** profile 生命周期：`sed -n '774p' apps/desktop/src/main.ts` = `const ownsDesktopInstance = claimDesktopSingleInstance(app, () => { focusPrimaryWindow() })`（模块顶层、`whenReady` 之前）；`single-instance.ts` 文档串即 `/** Electron single-instance ownership before any Desktop profile lifecycle begins. */` —— 删掉该调用则第二实例不再唤醒既有窗口（行为可判）。
- A-06/A-08 常量化 + 来源校验：10 个 `ipcMain.handle` **全部**引用 `DESKTOP_IPC/MANDATORY_IPC/UPDATE_DIALOG_IPC` 常量（主代码零频道字面量，仅测试 1 处）；`assertDesktopSender` 生产调用点 5 处 + `mandatory-update-window.assertSender` + `update-dialog.owned()`，覆盖 10 handler。
- A-15 停机准入闸：`update-tasks.ts:33-49` 实读 = `lock → await Promise.all(pendingRequests) → 复核 generation 未被顶替 → 再判 running/stopping`（逐行与该条断言吻合）。
- A-16 反向举证：`git grep -n 'powerSaveBlocker'` = **0**；`git grep -n "on('suspend'"` = **0**；官方仅有 `powerMonitor.on('resume')`（`main.ts:571`）与 `will-quit` 反注册（`:574`）。
- A-30：`update-coordinator.ts:186` = `gt(version, this.currentVersion()) ? version : undefined`（实读，非字符串存在）。
- A-39：`update-journal.ts:20-31` 白名单字段（phase/targetVersion/percent/errorCode）+ `:63-65` 去重 + `:71-72` `{ flush: true }` 逐行吻合。

**B（官方树实读 + 排除项自述溯源）**

- B-01：`chat-settings.ts:12/18` = `TRANSCRIPT_VIEW_MODES = ['normal','compact']` / `DEFAULT_TRANSCRIPT_VIEW_MODE = 'compact'`。
- B-04：`StateDot.tsx:8` = `'done'|'warning'|'ongoing'|'error'|'idle'`；`JobListAction.tsx:23` = `function assertNever(value: never): never`（关闭联合穷尽）。
- B-13：`ReasoningRow.module.css:37` = `animation: dsh-reasoning-row-sweep 2.6s ease-out infinite;`（2.6s 数值实读）。
- B-31：`theme-settings.ts:24/27/30/43` = `FONT_SIZE_MIN=12` / `MAX=17` / `DEFAULT=14` / `z.number().step(1).min().max().default()`。
- B-32：`trajectory-virtual-rows.ts:6-8` = `CONTENT_ROW_HEIGHT=30` / `COLLAPSED_SUMMARY_HEIGHT=20` / `TERMINAL_BOUNDARY_HEIGHT=9`。
- X-01~X-06（"开发者工具形态"❌）：6 个包的 `package.json:description` 与件内引文逐字吻合；X-04 的官方自述 `Chrome-Network-style overview timeline` 实读于 `TrajectoryTimeline.tsx:1` —— 排除项**有真实官方对应物**，非凭空设限。

**C（接线点：声明处 → 生产调用处，7/7 全查）**

| 被调用物 | 声明处（实读） | 生产调用处（实读） |
|---|---|---|
| `selectForNextRequest` | `agent.ts:333` | `commands.ts:151` |
| `consumeSelection` | `agent.ts:346` | `index.ts:161`（`session/event` → `request/header` 分支） |
| `installModelSelectionProjection` | `model-selection-projection.ts:81` | `index.ts:122` |
| `installModelSelection` | `core/agent/src/model-selection.ts:76` | `agent.ts:323`／`acp model-control.ts:59`／`bundle/headless index.ts:340` |
| `AgentDefaultModelConfig.saveSelection` | `agent-default-model/src/index.ts:100` | `commands.ts:153` |
| `modelSwitchNotice` | `model-selection.ts:41` | `model-selection.ts:118`（`agent/pre-step`）+ 测试断言精确文案 `system-prompt-admission.spec.ts:212-222` |
| `directoryFor` | `ui-model-selection/src/client/service.ts:69` | 座位注入（`ModelSelect.tsx:55-60` 消费 `directory`）+ `/model` 贡献注册断言 `browser-plugin-client.spec.ts:209` |

- C-20/C-21/C-22（官方无实现）反向举证：`(fallbackModel\|…)` = **0**、`(supportsTools\|…)` = **0**（我重跑）；`selectModel`（`commands.ts:133-169`）内 `cancel/abort` 命中 0，`cancel` 在 `:499-511` —— 三条"官方无实现"**成立**。
- C 措辞裁定：全文 `第三方插件`/`翻车`/`创始人陈述` 命中仅 2 处 —— `L95`（引用 CTO 禁令原文本身）与 `L128`（规定写法「**创始人陈述（2026-09-21，无仓库文件证据）**——本件不以此作为事实或推断依据」）。**无一处被写成事实**。
- C 必测用例：C1–C12 每条四要素（前置状态/操作/期望断言/失败判据）+ 官方依据 **12/12 齐备**。

**D（台账/分支/漂移的三处硬核）**

- `b-14` 指引写法纠错：`ls packages/feedback/message-feedback/lib/types/` 实为 `index`/`types`，**无 `spec.js`** → D 件"锚点需改"成立。
- 指引行号纠错：`grep -n "B-1[1-9]\|B-20" DSH借鉴指引-v2-20260904.md` → B-11 `:95` … B-20 `:104`（卡面 `:96-101` 偏窄）成立。
- 199 口径：`comm` 集合差证明"+1 = #699 派单件"；`.claude/bypass.log` 在两个断面**都**命中（非新增）。
- 来源未入库标注：§1.2 14 行**逐行**带〔**来源未入库**〕+ 表头声明；§1.4 9 行**逐行**带〔**来源未入库**〕；§六 第 1 条再次声明"待 CTO 回『可读』后复验"。插件两件：D 件未取材；C 件 §1.2 已声明"两件均不在 main、不可用作任何技术结论依据"。

## 6. 红线检查（原始输出）

```console
$ grep -rn "@deepseek-ai" /Users/wane/SynovaAgent/.synova-wt-d870/src/ /Users/wane/SynovaAgent/.synova-wt-d870/packages/ 2>/dev/null | wc -l
       0
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 status --porcelain
?? docs/synova/research/official-borrowing-recheck.md
?? docs/synova/research/official-borrowing-table.md
?? docs/synova/research/official-change-cards.md
?? docs/synova/research/official-electron-kernel-runtime.md
?? docs/synova/research/official-model-switching.md
?? docs/synova/research/official-ui-patterns.md
?? task-state/D870.json
$ git -C /Users/wane/src/deepseek-harness status --porcelain | wc -l
       0
$ ls -ld /Users/wane/src/deepseek-harness
drwxr-xr-x@ 66 wane  staff  2112  9月 21 23:37 /Users/wane/src/deepseek-harness
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 diff --name-only | wc -l
       0
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 diff --cached --name-only | wc -l
       0
$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 ls-remote --heads origin | grep -E 'd870'
(无 d870 分支=未推送；本件与四切片均未 commit/push，符合"K3 复审通过前不合并")
```

| 红线 | 判据 | 结论 |
|---|---|---|
| G1 | `grep -rn "@deepseek-ai" src/ packages/` | **= 0** ✅ |
| 写集越界 | **被验件 = 六份产物**（四对照表 + 借鉴总表 + 改法卡）与**治理产物单列**（`task-state/D870.json` 队长写、本证据文件 verifier 写） | 自验时点：工作树 `??` **恰好七份**（六份被验件 + task-state）✅，无 `M`、无多余文件；本证据文件并入后 = **八份**（被验件 6 + 治理 2）；**R3 定稿实测 = 九份**（被验件 6 + 治理 3：`task-state/D870.json`、本证据文件、队长 M6 的 `D870-leftovers-20260921.md`）。按 D860 口径治理产物不计入 PR 文件预算，本件在 §9 分列 |
| 官方树只读 | `git status --porcelain` = 0 行 + 权限未变 | **0 脏** ✅；权限 `drwxr-xr-x@ 66 wane staff 2112 9月 21 23:37`（开工时记录值一致） |
| 禁改区 | diff 不含 `src/**`、`scripts/control-tower/**`、`.github/**` | **tracked/staged diff 均 0 行** ✅ |

## 6.5 主工作树 stray 副本删除证据（lead 指示）

```console
$ git -C /Users/wane/SynovaAgent status --porcelain          # 删除前（主树仅此一条 stray）
?? docs/synova/product-lines/evidence/D870-verify-20260921.md

$ rm /Users/wane/SynovaAgent/docs/synova/product-lines/evidence/D870-verify-20260921.md
（rm exit=0；test -e → no）

$ git -C /Users/wane/SynovaAgent status --porcelain          # 删除后（空输出 = 主树恢复干净）
（空）

$ git -C /Users/wane/SynovaAgent/.synova-wt-d870 status --porcelain   # 并入后工作树 = 八份
?? docs/synova/product-lines/evidence/D870-verify-20260921.md
?? docs/synova/research/official-borrowing-recheck.md
?? docs/synova/research/official-borrowing-table.md
?? docs/synova/research/official-change-cards.md
?? docs/synova/research/official-electron-kernel-runtime.md
?? docs/synova/research/official-model-switching.md
?? docs/synova/research/official-ui-patterns.md
?? task-state/D870.json
```

## 7. 附件：109 条逐条锚点复算原始输出

命令：`python3 /tmp/d870-verify/dump_items.py`（对每条 `official` 首个 file:line 独立 `sed`；区间另附 `[range->]`）

```

===== 切片 A（official-electron-kernel-runtime.md） items=39 =====
A-01  | apps/desktop/src/single-instance.ts:20 | if (!application.requestSingleInstanceLock()) {
A-02  | apps/desktop/src/main.ts:776 | if (ownsDesktopInstance) void app.whenReady().then(main).catch(async (error: unknown) => {
A-03  | apps/desktop/src/main.ts:699 | app.on('before-quit', (event) => {
A-04  | apps/desktop/src/main.ts:45 | let shuttingDown = false
A-05  | apps/desktop/src/main.ts:136 | nodeIntegration: false,
A-06  | apps/desktop/src/ipc.ts:6 | export const DESKTOP_IPC = {
A-07  | apps/desktop/src/ipc.ts:54 | export interface DshDesktopProductApi {
A-08  | apps/desktop/src/ipc.ts:71 | export function assertDesktopSender(event: IpcMainInvokeEvent, hostnames: readonly string[]): void {
A-09  | apps/desktop/src/main.ts:228 | if (mainWindow === undefined || mainWindow.isDestroyed() || event.sender !== mainWindow.webContents
A-10  | apps/desktop/src/main.ts:142 | window.webContents.setWindowOpenHandler(({ url }) => {
A-11  | apps/desktop/src/main.ts:70 | protocol.registerSchemesAsPrivileged([{
A-12  | apps/desktop/src/preload-app.ts:22 | if (location.protocol === `${SCHEME}:` && location.hostname === 'app') {
A-13  | apps/desktop/src/main.ts:571 | powerMonitor.on('resume', automaticCheck)
A-14  | apps/desktop/src/update-schedule.ts:102 | this.delay = failed ? Math.min(maxBackoffMs, this.delay * 2) : intervalMs
A-15  | apps/desktop-host/src/update-tasks.ts:40 | await Promise.all(pendingRequests)
A-16  | （官方无实现）                                                                | (无行号锚点/官方无实现)
A-17  | apps/desktop/src/fatal-recovery.ts:40 | if (this.reported) return
A-18  | apps/desktop/src/main.ts:665 | reportFatal(new Error(`Desktop page failed to load: ${url} (${String(code)}: ${description})`))
A-19  | apps/desktop/src/startup-error.ts:11 | : error instanceof Error ? error.message : String(error)
A-20  | apps/desktop/src/host-process.ts:25 | const MAX_HOST_DIAGNOSTIC_CHARS = 64 * 1024
A-21  | apps/desktop/src/host-process.ts:49 | timer.unref()
A-22  | apps/desktop/src/host-process.ts:201 | if (!graceful) child.kill('SIGTERM')
A-23  | apps/desktop/src/main.ts:213 | const controller = new AbortController()
A-24  | apps/desktop/scripts/electron-builder-config.mjs:52 | if (unsigned && resolvedPlatform !== 'win32') throw new Error('desktop package: unsigned builds require Windows')
A-25  | apps/desktop/scripts/electron-builder-config.mjs:126 | hardenedRuntime: true,
A-26  | apps/desktop/scripts/electron-builder-config.mjs:166 | verifyMacOSSignatureAfterSign(context, macOSSigning ?? resolveMacOSSigningEnvironment(env))
A-27  | apps/desktop/scripts/electron-builder-config.mjs:178 | forceCodeSigning: !unsigned,
A-28  | apps/desktop/src/runtime-tree.ts:77 | return { path: name, bytes: body.byteLength, sha256: createHash('sha256').update(body).digest('hex'),
A-29  | apps/desktop/src/update-coordinator.ts:65 | this.updater.autoDownload = false
A-30  | apps/desktop/src/update-coordinator.ts:70 | this.updater.allowDowngrade = false
A-31  | apps/desktop/src/update-coordinator.ts:127 | if (!this.downloaded || this.downloadOperation !== undefined || version !== this.candidate) throw new Error('desktop update: confirmed target is not r
A-32  | apps/desktop/src/main.ts:348 | const active = await host.updateTasks('inspect')
A-33  | apps/desktop/src/update-http-executor.ts:13 | if (!Number.isSafeInteger(idleTimeoutMs) || idleTimeoutMs < 1000 || idleTimeoutMs > 2_147_483_647) {
A-34  | apps/desktop/src/update-presentation.ts:5 | const NETWORK_FAILURE = /\b(?:ERR_CONNECTION_CLOSED|ERR_CONNECTION_RESET|ERR_INTERNET_DISCONNECTED|ERR_NAME_NOT_RESOLVED|ETIMEDOUT)\b/u
A-35  | apps/desktop/src/main.ts:406 | return Promise.resolve(new Response(null, { status: 503 }))
A-36  | apps/desktop/src/update-overlay.ts:13 | ...parent.getContentBounds(), resizable: false, minimizable: false, maximizable: false,
A-37  | apps/desktop/src/update-dialog.ts:107 | if (active === undefined || event.sender !== active.window.webContents
A-38  | （官方无实现）                                                                | (无行号锚点/官方无实现)
A-39  | apps/desktop/src/update-journal.ts:20 | export function desktopUpdateJournalState(state: DesktopUpdateState): object {

===== 切片 B（official-ui-patterns.md） items=35 =====
B-01  | packages/client/ui-chat/src/chat-settings.ts:12 | export const TRANSCRIPT_VIEW_MODES = ['normal', 'compact'] as const
B-02  | packages/client/ui-deliverables/src/client/Deliverables.module.css:10 | .file { position: relative; box-sizing: border-box; display: flex; align-items: center; gap: 10px; min-width: 0; height: 60px; padding: 8px 10px; over
B-03  | packages/client/ui-deliverables/src/client/Deliverables.module.css:34 | @container (max-width: 620px) { .presented { grid-template-columns: minmax(0, 1fr); } }
B-04  | packages/client/ui-primitives/src/StateDot.tsx:8 | export type StateDotState = 'done' | 'warning' | 'ongoing' | 'error' | 'idle'
B-05  | packages/client/ui-jobs/src/client/JobListAction.tsx:62 | function formatDuration(elapsedMs: number, t: TranslateNS<typeof NS>): string {
B-06  | packages/client/ui-jobs/src/client/JobListAction.tsx:44 | function statusLabel(status: JobView['status'], t: TranslateNS<typeof NS>): string {
B-07  | packages/client/ui-chat/src/client/chat/StatsPills.tsx:332 | if (stats.steps === 0 && !hasTokens) return null
B-08  | packages/client/ui-chat/src/client/chat/StatsPills.tsx:327 | const stats = useMemo(() => projected ?? deriveStats(settledNodes), [projected, settledNodes])
B-09  | packages/client/ui-chat/src/client/chat/StatsPills.tsx:321 | const [openPill, setOpenPill] = useState<'time' | 'usage' | null>(null)
B-10  | packages/client/ui-chat/src/client/chat/StatsPills.tsx:317 | export const StatsPills = memo(function StatsPills({ useChat, useProjection, t }: StatsPillsProps) {
B-11  | packages/client/ui-chat/src/client/chat/ReasoningRow.tsx:32 | const summary = (running ? latestLine(text) : firstLine(text)).replaceAll('**', '')
B-12  | packages/client/ui-chat/src/client/chat/ReasoningRow.module.css:7 | contain: size layout;
B-13  | packages/client/ui-chat/src/client/chat/ReasoningRow.module.css:37 | animation: dsh-reasoning-row-sweep 2.6s ease-out infinite;
B-14  | packages/client/ui-chat/src/client/chat/ReasoningRow.tsx:41 | {running && <span className={a11yCss.visuallyHidden}>{t('row.running')}</span>}
B-15  | packages/client/ui-deliverables/src/client/PresentedFileCard.tsx:21 | * Render independent file actions without nesting buttons inside a clickable card.
B-16  | packages/client/ui-deliverables/src/client/PresentedFileCard.tsx:46 | const status = phase === undefined
B-17  | packages/client/ui-deliverables/src/client/Deliverables.module.css:20 | .previewHint { display: none; }
B-18  | packages/client/ui-deliverables/src/client/PresentedFileCard.tsx:39 | const act = (action: PresentedAction) => {
B-19  | packages/client/ui-permission-presets/src/client/PermissionSelect.tsx:29 | const permissionGlyphs = new Map<string, ReactNode>([
B-20  | packages/client/ui-permission-presets/src/client/catalog.ts:17 | readonly store: SnapshotStore<PermissionCatalogState> = createSnapshotStore({
B-21  | packages/client/ui-permission-presets/src/client/catalog.ts:1 | /** Identity-stable process permission catalog shared by both selection surfaces. */
B-22  | packages/client/ui-workspace/src/client/locales.ts:23 | 'empty.none': '暂无会话',
B-23  | packages/client/ui-model-selection/src/client/locales.ts:32 | 'empty.models': '没有可用的模型。',
B-24  | packages/client/ui-conversation/src/client/locales.ts:18 | 'placeholder.parentOffline': '父会话已离线，无法继续发送；仍可停止当前运行',
B-25  | packages/client/ui-deliverables/src/client/PresentedFileCard.tsx:58 | <span className={css.description} role={phase === undefined ? undefined : 'status'}
B-26  | packages/client/ui-chat/src/client/chat/TurnNavigator.module.css:40 | transition: height 220ms cubic-bezier(0.2, 0.8, 0.2, 1);
B-27  | packages/client/ui-attachment/src/FileCard.module.css:117 | transition: none;
B-28  | packages/client/ui-theme/src/styles/scrollbar.css:16 | body {
B-29  | packages/client/ui-theme/src/boot-theme.ts:16 | const light = `:root{color-scheme:light}body{background-color:${LIGHT_BACKGROUND};--dsh-boot-bg:${LIGHT_BACKGROUND}}`
B-30  | packages/client/ui-theme/src/styles/design-platform.css:80 | body[data-ds-dark-theme] {
B-31  | packages/client/ui-theme/src/theme-settings.ts:24 | export const FONT_SIZE_MIN = 12
B-32  | packages/client/ui-trajectory/src/client/trajectory-virtual-rows.ts:6 | const CONTENT_ROW_HEIGHT = 30
B-33  | packages/client/ui-chat/src/client/chat/StatsPills.tsx:5 | // Mounted on 'conversation.composer.dock' so it sticks with the composer in the
B-34  | packages/client/ui-deliverables/src/client/PresentedFileCard.tsx:52 | <button type="button" className={css.cardPreview} title={resolveWorkspacePath(cwd, file.path)}
B-35  | packages/client/ui-workspace/src/client/contract/slots.ts:1 | /**

===== 切片 C（official-model-switching.md） items=23 =====
C-01  | packages/client/ui-model-selection/src/client/service.ts:69 | directoryFor(sessionId: SessionId): ModelDirectory { [range->]directoryFor(sessionId: SessionId): ModelDirectory { ⏎     const { live } = this ⏎     const sessions = this.ctx.session
C-02  | packages/client/ui-model-selection/src/client/directory.ts:45 | /** Latest selection operation wins; an older response never overwrites a newer one. */ [range->]/** Latest selection operation wins; an older response never overwrites a newer one. */ ⏎   private generation = 0
C-03  | packages/api/session-controller/src/model-selection-projection.ts:35 | function applyModelSelectionProjection( [range->]function applyModelSelectionProjection( ⏎   state: ModelSelectionProjectionState, ⏎   event: SessionEvent, ⏎ ): ModelSel
C-04  | packages/core/agent/src/model-selection.ts:26 | export interface ModelSelectionRef { [range->]export interface ModelSelectionRef { ⏎   /** Model selected for the next step that enters prompt assembly. */ ⏎   curren
C-05  | packages/core/agent/src/model-selection.ts:41 | function modelSwitchNotice(previous: ModelSelection, selected: ModelSelection) { [range->]function modelSwitchNotice(previous: ModelSelection, selected: ModelSelection) { ⏎   const from = routeLabel(previous, s
C-06  | packages/api/session-controller/src/commands.ts:137 | const resolved = await this.ctx.llm.resolveCallConfig({ [range->]const resolved = await this.ctx.llm.resolveCallConfig({ ⏎           provider: request.provider, ⏎           model: reque
C-07  | packages/api/session-controller/src/commands.ts:152 | try { [range->]try { ⏎           await this.ctx.agentDefaultModel.saveSelection(selected) ⏎         } catch (error) { ⏎           this.
C-08  | packages/api/session-controller/src/agent.ts:303 | // An effort the adapter defaulted is not a conversation choice: restoring [range->]// An effort the adapter defaulted is not a conversation choice: restoring ⏎           // it as one would make an unchan
C-09  | packages/client/ui-settings-models/src/client/operations.ts:96 | writeSettings: async (ns, ops, expectedRevision) => { [range->]writeSettings: async (ns, ops, expectedRevision) => { ⏎       const response = await ctx.remote.settings.mutate(ns, ops,
C-10  | packages/client/ui-model-selection/src/client/directory.ts:20 | /** [range->]/** ⏎    * Whether an adapter serves the current selection's provider, as the host reports ⏎    * it — null before the f
C-11  | packages/api/session-controller/src/types.ts:137 | export interface ModelCatalogFailure { [range->]export interface ModelCatalogFailure { ⏎   readonly id: string ⏎   readonly name: string ⏎   readonly message: string ⏎ 
C-12  | packages/api/session-controller/src/commands.ts:338 | if (model.inputModalities !== undefined && !model.inputModalities.includes('image')) { [range->]if (model.inputModalities !== undefined && !model.inputModalities.includes('image')) { ⏎             throw new RemoteErr
C-13  | packages/llm/token-meter/src/turn-usage.ts:6 | /** One provider/model route that contributed a billed request attempt. */ [range->]/** One provider/model route that contributed a billed request attempt. */ ⏎ export interface TurnTokenUsageRoute { ⏎   
C-14  | packages/llm/token-meter/src/projection.ts:19 |  [range->]/** ⏎  * Approximate context occupancy for a status display. ⏎  * ⏎  * The fields, when present, are deliberately NOT on
C-15  | packages/llm/llm/src/retry-policy.ts:17 | const DEFAULT_JITTER_RATIO = 0.1 [range->]const DEFAULT_JITTER_RATIO = 0.1 ⏎ const DEFAULT_RETRYABLE_CODES = Object.freeze([ ⏎   EMPTY_RESPONSE_CODE, ⏎   'RATE_LI
C-16  | packages/llm/llm/src/error.ts:14 | /** Stable machine-routable failure class (e.g. `RATE_LIMIT`); route on this, never by parsing `message`. */
C-17  | packages/subagent/tool-subagent/src/model-selection-state.ts:31 | const modelSelectionPolicySchema: zod.ZodType<AllowedModelRoute[] | null> = zod.array(zod.object({ [range->]const modelSelectionPolicySchema: zod.ZodType<AllowedModelRoute[] | null> = zod.array(zod.object({ ⏎   provider: zod.str
C-18  | packages/test-support/llm-replay/src/index.ts:167 | export interface ReplayHandle { [range->]export interface ReplayHandle { ⏎   /** Remove the registered adapter or waterfall listener (HMR safety). Freestanding c
C-19  | packages/test-support/llm-mock-server/src/index.ts:16 | export const MOCK_LLM_BEHAVIORS = [ [range->]export const MOCK_LLM_BEHAVIORS = [ ⏎   'connection_reset', ⏎   'stream_disconnect', ⏎   'empty', ⏎   'empty_body', ⏎   
C-20  | retry.spec.ts:464 | FILE-MISSING
C-21  | packages/api/session-controller/src/commands.ts:133 | async selectModel(request: SessionSelectModelRequest): Promise<SessionSelectModelValue> { [range->]async selectModel(request: SessionSelectModelRequest): Promise<SessionSelectModelValue> { ⏎     const agent = await this
C-22  | packages/llm/llm/src/types.ts:309 | contextWindow?: number
C-23  | 借鉴边界(按 G1 守卫与 CTO 口径)，非官方对应物                                           | (无行号锚点/边界项)

===== 切片 D（official-borrowing-recheck.md） items=12 =====
D-01  | packages/runtime-diagnostics/invariants/lib/index.js:12 | var InvariantError = class extends Error {
D-02  | packages/jobs/jobs/README.zh.md:53 | 任何预检拒绝都不会留下 job id 或已注册的工作。由随附的进程内注册表管理的任务会随 harness 进程终止而消失；跨重启的持久执行需要一个实现本约定的不同后端。
D-03  | packages/util/output-retention/lib/index.js:109 | function trimTrailingPartialUtf8(bytes) {
D-04  | packages/spill/spill/lib/index.js:22 | * Service Definition for the spill storage capability seam (`ctx.spillStore`): an abstract service defining WHAT a [range->]* Service Definition for the spill storage capability seam (`ctx.spillStore`): an abstract service defining WHAT a ⏎ * s
D-05  | packages/webhook/webhook/lib/types/brand.d.ts:4 | export type WebhookRuleId = Branded<'WebhookRuleId'>;
D-06  | packages/util/http-proxy/lib/index.js:19 | const LOOPBACK_NO_PROXY = [
D-07  | packages/settings/settings/lib/types/index.d.ts:21 | export type SettingsApplies = 'live' | 'restart';
D-08  | packages/session/session-stats/lib/types/projection.js:6 | * `step/end` — not `assistant/message` — is the counted step event because it [range->]* `step/end` — not `assistant/message` — is the counted step event because it ⏎  * is the step lifecycle authority: the 
D-09  | packages/context/session-reference/lib/index.js:94 | const MAX_REFERENCES = 3;
D-10  | packages/context/time-context/lib/index.js:68 | function createTimestampFormatter(timeZone) {
D-11  | packages/web/tool-web（README.zh.md 概述段）                                | (无行号锚点/边界项)
D-12  | packages/credentials/credentials-local/lib/index.js:104 | throw new Error(`credentials-local: ${filename} is readable beyond its owner (mode ${(mode & 511).toString(8)}); run "chmod 600 ${filename}" before st
```

## 8. 未覆盖项（诚实列出）

1. **未跑官方树的构建/测试**：本件全部为只读命令（`sed/grep/git/wc/ls`）；未执行 `pnpm build/test`，故"lib/ 构建产物是否由 pinned commit `ddefc45` 构建而来"**无法验证**（该不确定性已由总表 §6.1 + 改法卡 D903 显性登记）。
2. **打包运行时断面（0.1.6-alpha.1）未复算**：A/C/D 三件均声明该断面"仅登记不作判据"；我沿用该边界，未在 `~/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/` 做独立取证。
3. **B/C 部分条目仅做锚点级复算，未逐条重读机制**：语义级抽验覆盖 ≥63 条（A ≥20、B ≥21、C ≥20、D 12）；未展开机制复算的条目（如 B-06/B-08/B-09/B-17/B-18/B-20/B-21/B-24/B-25/B-28/B-29/B-30/B-33/B-35、C-12/C-14 全区间、A-17/A-19/A-23/A-25~A-28/A-32~A-35）**只证明"锚点指向真实代码行"，未证明机制语义**。
4. **改法卡的"量级（大/中/小）"为作者初估**（件内自陈"未实测"），我未复核工作量。
5. **改法卡的归线编号**（如"21 模型底座与 DeepSeek 深度优化""20 Agent 运行底座"）未逐条回 `product-lines.yaml` 核对行号/名称**（只核了 `gap-line-mapping.yaml` 11 项的对账：11/11 名称命中）。
6. **B 件 CSS 计数偏差（派单件 184 vs 实测 154）成因未查明**：件内已如实留档，我复算确认 154（`git ls-files`）/184（`find` 含未跟踪件），成因未继续追（属派单件前提面）。
7. **A/B/C/D 四件互相之间的口径是否应统一（src/ vs lib/）**：我只登记差异事实，不做口径裁定（归 CTO）。

## 9. 自验结论

- **通过项（自验口径）**：条目级锚点全量复算 109/109；A 9 面（8 面官方 file:line + 托盘面明标无实现）、B 11 模式（≥10）、C 12 用例四要素齐备（≥8）、D 四项 14/11/10/199 全覆盖 + 第二层抽样 16.3%（≥10%）、总表 109 条三分类 66/36/2 + 官方无实现 5（与我独立聚合逐条一致，109 行五字段逐字保留）、改法卡 D871–D910 编号连续 + 109/109 条目有归属；四项红线全绿。
- **不一致项**：4 条，**全部关闭** —— #1（B 148）退回修复后复验关闭；#2/#3（改法卡 §三 标题、§四 卡数陈旧）已修，新哈希 `bde42e69…` 复跑关闭；#4（D 件「597 键」不可复现）已换数为可复现的 291/302/0，并**进一步升级处理**：#7 判据射程由"官方无对应包"收窄为「官方 **workspace** 无此包名」+ 补「官方确有该名（`dependency-catalog.json:3196`，外部依赖、`direct: true`）」——我独立复核五项事实全部成立。
- **未覆盖项**：见 §8（7 条）。
- 本件仅给出**自验结论**，**不判"通过"**；是否可采用、能否合并，归 **CTO 收件闸 + K3 终审**。

## 10. 本文件自身的修订记录

| 轮 | 触发 | 本文件变更 |
|---|---|---|
| R1 | 首轮自验（task-6） | 建立 §1–§9；钉住六件哈希（A/B/C/D/总表 eebbb93a/改法卡 71158aca） |
| R1.5 | 改法卡计数修复 | §1 改法卡哈希 → `bde42e69…`；§4 #2/#3 关闭；§3.2 补复跑 |
| R2 | lead 末轮要求：①D 换数重跑 ②本文件 §1 行数须随哈希更新 ③总表 §1.2 同步后复跑 | §1 的 D → `2ce8fca6…`(485 行)、总表 → `3e25a796…`(606 行)；§2 加 R2 复跑记录；§3.2 补 D/总表复跑与原「行数笔误」说明；§4 #4 关闭；§9 更新；新增本表 |
| R3 | lead 终轮：D 件 #7 射程收窄（workspace vs 官方发布包）+ 总表 §1.2 二次同步 | §1 的 D → `92ddcff5…`(490 行)、总表 → `47b05878…`(606 行)；§2 加 R3 复跑记录；§3.2 补 #7 五项事实独立取证；§4 #4 升级记录；§9 更新 |

> **自验件自身数字的复算纪律（本轮教训，如实记录）**：R1 记录的 `总表 443 行` 是**其哈希 `eebbb93a…` 下的实测值**，随该件后续被同步（§1.2 指纹段）而失效；lead 指出后我以 `wc -l` 重测终值 = **606 行**（lead 通知的 601 未能复现）。结论：**本表所有行数必须与其哈希成对引用**，任何被验件改版后本表的行数行即失效，须重测。
