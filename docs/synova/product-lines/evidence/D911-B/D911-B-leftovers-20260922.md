# D911-B 遗留清单（切片 B）

> 出件 2026-09-22 ｜ 分支 `feat/D911-ownership-standby`
> 规则：发现「必须改但不在写集」的路径 → **写进本件，不自行扩写集**（派单 §二）。
> **每项含 `文件:行` + 为什么不修。**

---

## A. 需 CTO 处置（建议立卡）

### A1 ⭐ 绿腿证据不可见（`文件:行`）
`scripts/pre-commit-check.sh:1462-1474`
```
PRB_OUT=$(bash "$ROOT/scripts/control-tower/check-pr-budget.sh" --quiet 2>&1)
PRB_EXIT=$?
if [ "$PRB_EXIT" -eq 0 ]; then
  soft_pass "D734 PR 预算: 文件数 / 单域 / 落后基线 均在预算内"     # ← 固定串，丢弃 PRB_OUT
elif [ "$PRB_EXIT" -eq 1 ]; then
  soft_check "D734 PR 预算超限 …" "$PRB_OUT"                      # ← 只有失败路径消费输出
```
- **现象**：`check-pr-budget.sh` 已按 B3 要求**逐条打印放行条目 + 声明源**，但调用点**只在失败分支**消费 `PRB_OUT`；成功走 `soft_pass` 固定串 → **CI 日志里看不到放行条目**。
- **为什么不修**：`scripts/pre-commit-check.sh` **不在本卡写集**（派单 §二 划给切片 C），且 §六 第 4 条「与 D869 同文件单写者」→ 本件**不扩写集**。
- **为什么不能只记一笔**：违反坑清单「**绿腿告警必须可见**」——放行证据不可见 = K3 无法从 CI 日志核「谁被代行放行了」。
- **可执行建议**：成功路径也回显放行条目（或落 `log_gate` 账），与失败路径同等可见。

---

## B. 存量问题（非本件引入，本件零改动）

### B2 `check-silent-swallow.sh --utf8` 基线红
`scripts/workflow/check-silent-swallow.sh`
```
$ bash scripts/workflow/check-silent-swallow.sh --utf8
❌ 16 个 .sh 缺头块
EXIT=1
```
- **为什么不修**：16 个文件**均不在本卡写集**；属存量基线红。
- **本件零责任证据**：`--diff` 模式 → `✅ 无新增静默吞错`；本件 2 个 `.sh|.py` 文件不在 16 件名单内。

### B3 门禁组 7 `grep: repetition-operator operand invalid`
`scripts/control-tower/validate-expert-config.sh` / `check-architecture.sh` / `check-file-driven.sh`（**均非本件写集**）
- **现象**：`pre-commit-check.sh` 组 7 输出 stderr 噪音（**非阻断**）。
- **证据**：自验员实测**本件改动在这三个文件上 = 0**。
- **为什么记这里**：属 **D869（门禁组 7 死检查）** 域 → 归 D869，本件不修。

---

## C. 未验证项（如实声明，勿当已验）

### C1 Windows 腿未实测
`scripts/control-tower/check-ownership.py`、`scripts/control-tower/check-pr-budget.sh`
- **原因**：本机仅 macOS（bash 3.2，**无关联数组**；`declare -A` 会静默失败）。
- **已做的静态防护**：`bash -n` 两文件通过；保留 `PYBIN` 三级探测；pre-commit 的 `V5 平台敏感命令 ✅` 绿；未使用 `declare -A`。
- **为什么不声称已过**：无 `windows-latest` 实跑环境 → 须由 CI 覆盖。**本件不声称 Windows 已过。**

### C2 「与 main 基线告警集 diff 空」未独立复现
- **原因**：需一份 main 工作树，重型且基线已移动（`ee0c4eb1 → f2b251f3`）。
- **已满足的实质判据**：`pre-commit-check.sh` **13 组全过** + 1 项软警告（不阻断）。
- **转**：CTO 收件闸 / K3 终审。

---

## D. 派单件/上游文档不一致（本件**未改**派单件，如实登记）

### D4 ⭐ 派单 §八 开工自检② 期望值写错
`docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md:217`
```
git show origin/main:scripts/control-tower/merge_writeset_gate.py | grep -c '可粘贴'  # 期望 ≥1
```
- **实测 = 0**。溯源 `48971777`（= PR #702 内容：11 files, +145/−18）**确实已落 main 且确实动过该文件（7 行）**，但**不含「可粘贴」字样**。
- **同组姊妹条** `check-pr-budget.sh | grep -c 'D860'` = **2** ✅。
- **判定**：**命令 expected 值写错**，**非** #702 未落地。B 切片与该文件零交集 → **不据此停 B**（已上报 CTO）。

### D5 派单 §〇-e 缺陷① 的夹具分支已失效（**影响切片 A**）
`docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md:67-69`
```
--head origin/chore/ingest-institute --branch chore/ingest-institute   # 期望 block + 任务: D811
```
- **实测**：`origin/chore/ingest-institute` 现为 `origin/main` 的**祖先**（`merge-base --is-ancestor` = yes，`9cce8b87`）→ 变更集必空：
```
✅ 结论: pass — 变更集为空
   变更集: 0 个文件（merge-base 9cce8b87）
EXIT=0
```
- **缺陷①本身仍在**：用 `refs/pull/696/head` 可复现错取号（`task_id_source: "commit-subject"`）。
- **为什么不修**：属**切片 A** 的夹具，A 的写集/授权不在本件；本件只报事实。

### D6 派单件 B4 与 §三反例② 字面矛盾
`docs/synova/dispatch/D911-门禁三缺陷根治-20260922.md:109`（B4「缺失 → exit 2」）vs `:114`（反例②「删段 → exit 1」）
- **队长定案**（已在 §一 验收执行）：**整段不存在 = 严格（exit 1）**；**段存在但字段/结构非法 = fail-closed（exit 2）**。
- **依据**：`:106` B1「win 回归 = 删该段，立即恢复严格」+ `task-state/D911.json:43`（只写「结构非法 → fail-closed」）。
- **为什么不改**：派单件是**上游权威**，不在写集 → 只登记。

---

## E. 设计取舍（**非缺陷**，防后人误判）

### E7 standby 缺失时跨域 FAIL 会多打 2–6 行 `⚠️ --proxy … 不生效`
- **为什么故意不做**「先探测 yaml 里有没有 `standby` 段，再决定要不要试代行」：那是**脆弱静态判据**（标题/缩进一变就静默跳过代行 → 正是本卡要治的病）。
- **多打的那几行是「试过且被拒」的诚实证据**。

### E8 「理由是否有效」刻意比 D708 严一格
`scripts/control-tower/check-pr-budget.sh:228-232` + `reason_ok()` docstring
- D708 的 `parts[1].strip()` **挡不住「纯标点理由」** → 本件要求理由含至少一个 字母/数字/记号。
- **段标题/收段/条目分隔/条目前缀四者仍逐字同 D708** → **不是口径分叉，是补 D708 的已知洞**。

### E9 `--emit-codeowners` 与 `.github/CODEOWNERS` 零 diff
- 见 verify 记录 §四。**重跑已执行**；因 `standby` 不参与生成，产物与 main 逐字节相同 → git 无内容可提交，改由 drift 断言守。

---

## F. 引用前提登记

### F10 ⭐ 派单 §〇 引用的「§十二」在 main 上**不存在**
- 派单件 `:25` 写「验收标尺 …（v1.1，**特别 §十一 三态判定 + §十二 证据与回执硬要求 8 条**）」。
- **实测**：`docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md` 在 main 为 **v1.1 / 121 行**，`grep -n '^## '` 只有 §一…§十一（+「版本」节），**无 §十二**；`git log` 只追加过 §九/§十一；**全 refs 搜「十二、证据」零命中**。
- **处置**：**不阻塞**（§十二 非开工前置），证据按**「命令 + 原始输出 + 退出码 + 反例 + 判别性内建」**交付（即 `SYNOVA-SQUAD-LEAD` 预设 `:29` 可核的同款纪律）。已上报 CTO。

### F11 派单 §〇-h ③ 与实测一致 ✅
`task-state/D869.json` 不存在 —— 派单件 `:220` 已预判此分支，**无偏差**。

### F12 本队指令中的 brief/Note 文件名与派单 §二 表不同
- 本队指令给的：`.claude/task-briefs/2026-09-22-D911-B-ownership-standby.md`、`memory/notes/proposed/2026-09-22-d911-b-ownership-standby.md`
- 派单 §二 表**未列**这两件（§二 只列 A/B/C 三行 + 自验行 + 队长行）。
- **处置**：两件**均在 D860 治理豁免前缀**（`.claude/task-briefs/` + `.md`｜`memory/notes/` + `.md`）→ **零 ≤12 预算摩擦**，编号/命名遵循卡内 D911 自身惯例（同派单 §二 的「自验」行亦未列而实存）。**登记备核，不计为冲突。**
