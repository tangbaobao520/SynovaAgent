# D911 切片 C · 遗留清单（文件:行 + 为什么不修）

> **队长 M6 第 3 件**｜2026-09-22｜分支 `fix/D911-g12-writeset`
> **口径**：以下全部**不在 C 切片写集**（派单件 §二），按「不擅自扩写集」**未代为修复**；每条给 **文件:行 + 为什么不修 + 建议**。
> **前置**：本件**不含**本次引入的红——「零新增红」已由自验受控两树对照证明（门禁级 61 条判定行唯一差异 = G12b 红度 3→2；既有测试 9 张 × 2 树，3 张存量红 `diff -q` 逐字相同）。原始输出见 `D911-verify-20260922.md` / `D911-evidence-20260922.md`。

| # | 文件:行 | 问题 | 为什么不修 | 建议 |
|---|---|---|---|---|
| L1 | `.github/workflows/ci.yml:224-265` | 本交付新增的 `tests/control-tower/task-scope-g12.test.sh`（38 断言）**未登记 canary 列表** → GitHub 上**不会执行**；`grep -c 'task-scope-g12' ci.yml` = 0 | `ci.yml` 是**单写者**，派单件 §六.4 明确不在本件写集 | 另卡登记。**收件闸注意：不得以「CI 绿」代替该 38 断言通过** |
| L2 | `scripts/workflow/resolve-commit-brief.sh:189-191` | 内嵌降级解析器仍为**旧剥壳规则**（不剥反引号、不剥 ` —— `），与 C4 修复后的主解析器 `parse_q2()` **漂移** | 不在写集；该路径仅在 `brief_parser.py` 不可用时触发 | 与 C4 同源收敛（D707「一类一机制」：降级副本应调用/同步同一规则） |
| L3 | `scripts/workflow/resolve-commit-brief.sh:32,115` | 「今日 brief」仍以**墙钟** `date +%Y-%m-%d` 选（组 6 同病根，与 D749/3b 同类）→ 同 commit 跨日结论可能不同 | 不在写集（C2 只覆盖 `pre-commit-check.sh` 组 12） | 立卡：与 C2 同法改以被检查 commit 的 date 为基准 |
| L4 | `scripts/pre-commit-check.sh:1505-1510`（G12b 调用点） | **存量 fail-open**：`check-brief-parseable.sh` 缺失时输出不含 `❌` → 走 `soft_pass` 输出 `✅ G12b: brief 可解析`（**假绿**）。自验在首个沙箱实测到（`bash: … No such file or directory` 却输出 ✅） | 存量缺陷，非本次引入；改调用点判定属扩写集 | 另卡：调用点加 `[ -f ]` 守卫，或按**非零退出码**判红而非按输出文本 |
| L5 | `scripts/check-verifiable-done.sh:29`、`scripts/check-plan-integrity.sh:162` | 同病根**内联盲 awk**（`{found=1; next}`）→ 内联 `## Done 标准: …` 仍被判无条目。C6 只覆盖 `pre-commit-check.sh:886`（卡面口径） | 不在写集；扩到这两处会放宽/收紧卡面未授权的判定 | 按 D707 收敛到 `brief_parser.parse_done()` 单一事实源 |
| L6 | `scripts/pre-commit-check.sh:855` | Q0–Q3 的 `awk "/## ${q}(:| )/{found=1; next}…"` 同为**内联盲区**（Q0–Q3 内联写法可能被误判「未填写」） | C6 卡面只点 886 行；扩到 855 会**放宽 4 个字段**的判定，超卡面授权 | 立卡：与 C6 同法兼容内联，并同步 `brief_parser` |
| L7 | `tests/control-tower/hard-gate-convergence.test.sh:102,111` | **存量红**：行为 A4/B 期望 `exit 0`（"G12 越界只告警不拦"），与 `decl_check` 对真实提交已是**硬阻断**的现状冲突；修前/修后同为 `rc=1`、同一行 ❌（**未恶化**） | 测试不在写集；「改测试期望 or 改口径」属口径裁定 | **需 CTO 裁**：要么更新测试期望，要么澄清 `decl_check` 口径 |
| L8 | `tests/control-tower/g12-taskstate-exempt.test.sh:53` | **存量红**：`EVAL=$(python -c …)` 硬编码 `python`，本机只有 `python3` → 5 条断言拿到空值（修前同红） | 测试不在写集 | 改用 PYBIN 三级探测（PLATFORM-CHECKLIST #1 既有惯例） |
| L9 | `tests/control-tower/utf8.test.sh`（2 失败） | **存量红**：`scripts/ci/check-contract-gaps.sh`、`scripts/ci/diagnosis-quality-check.sh` 缺 UTF-8 头块（本次 diff 未触及这两文件；`diff -q` 两树逐字相同） | 不在写集 | 各自立卡 |
| L10 | `scripts/pre-commit-check.sh:1348/1368`（S1/S2 读取） | 组 12 的 S1/S2 **读工作树**，而非**被检查的 head 树**（与 D708 切片 A2 的「声明源改读 head 树」口径尚有差） | 本卡只做「源并集」；读树口径属 D708-A2 范畴 | A 切片落地后如需完全对齐再立卡 |
| L11 | `docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`（121 行，止于 §十一） | 派单件 §〇 引用的 **§十二「证据与回执硬要求 8 条」不存在**（全 `docs/` 检索「证据与回执」仅命中派单件自身） | 文书缺口，非代码；本件按派单件 §三 自带 8 条 + §十一 三态执行 | 补 §十二 或订正派单件引用 |
| L12 | `.claude/bypass.log` | 本分支有 post-commit hook 自动登记的 3 行（每次提交一行） | 派单件 §六.8 明确「不动 bypass.log」（运行期账本） | 合并冲突按**并集**解 |

## 附：已受理的口径偏差（**不计为遗留缺陷**，附等价性证明）

- **C4 未搬 `_clean_entry` 的两步**：`parse_q2()` 有意未搬 `rstrip("/")` 与计数括号 `(N 修改)`——两处既有断言钉死目录条目尾斜杠（`tests/control-tower/brief_parser.test.sh:50`、`brief-parser-strip.test.sh:67` 的 `grep -qx "scripts/audit/"`），且这两个测试**不在写集**。
  等价性由自验独立夹具证明：① S1 声明 `tests/probe-dir/`（带尾斜杠）与 `src/feat`（不带）**两种写法均命中**目录下文件；② S7 声明 `src/feat` 时 `src/feat/inside.ts` 被认领、`src/feature-x.ts` **未被误收**（前缀 + `/` 边界）→ **不产生新误拦/漏拦**。
- **`today_files_by_prefix()` 缺省自持**：`pre-commit-check.sh` 在生产函数内加 `local DAY_WINDOW_RE="${DAY_WINDOW_RE:-}"` 缺省 → 使 `today-by-name.test.sh`（main 上 `DAY_WINDOW_RE: unbound variable` 预存红，`rc=1`）转绿（`rc=0`，15 通过/0 失败），且与 `g12-day-window.test.sh:36` 的字面契约同时成立。自验核对：全脚本 `TODAY_DASH=` 仅 1 处且 `="$G12_TODAY"`（commit-date 同源），组 12 段内唯一墙钟调用在**降级分支**且紧随显式 ⚠️ → **未退回运行日**。
