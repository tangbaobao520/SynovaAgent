# D911 切片 C 自验 · 夹具原始输出存档（2026-09-22）

> **角色**：D911 小队 · **自验**（非编码；结论只用「可提请独立审计 / 退回」）。
> **工作树**：`.synova-wt-d911c` ｜ **分支**：`fix/D911-g12-writeset`
> **受控基线**：`f2b251f3`（= `merge-base(origin/main, 交付分支)`，即分叉前的 main 树）
> **归档目录**：`/tmp/d911v-out`（沙箱 `/tmp/d911v-sb`，**自验员自建，独立于编码方测试夹具**）
> **原则**：本文件所有输出均由自验员**自行重建夹具、自行运行命令**采集；未采信编码方贴出的输出。
> 每条证据标注「产生命令」，可逐条复跑。

## 0 证据可核性

### 0.1 被审产物的内容指纹（base=受控基线树 / head=交付树）

```
$ for f in scripts/pre-commit-check.sh scripts/control-tower/brief_parser.py tests/control-tower/task-scope-g12.test.sh; do
    shasum -a 256 "$f"; done   # 分别在 merge-base 导出树与交付导出树内
-- 受控基线树 (f2b251f3):
   2106c86c629f6a11511e8bcd43a34a82  scripts/pre-commit-check.sh
   4f7001c8e6102d9baf540da8729ac688  scripts/control-tower/brief_parser.py
     tests/control-tower/task-scope-g12.test.sh
-- 交付树 (141a3d6c):
   2022b001ab88e32af58dd3f396ae515a  scripts/pre-commit-check.sh
   803f0b2cc8a5771fd6dbfdf2f1e59ca8  scripts/control-tower/brief_parser.py
   fffd2c8f43ccecebe146ab6a807bb094  tests/control-tower/task-scope-g12.test.sh
```

### 0.2 远端回执

```
$ git ls-remote --heads origin | grep D911
039c5f8c4753b093a04ae1997b33ef2aed893bd7	refs/heads/feat/D911-ownership-standby
141a3d6cbb37a8b6b5f15576505f0a4cda5a84da	refs/heads/fix/D911-g12-writeset
ee8361f4b34f82f62f7497fe6776d1eebbbaf96a	refs/heads/fix/D911-merge-writeset-gate
```

### 0.3 受控基线树 vs 交付树 的差异（证明对照只差交付内容）

```
$ diff -rq /tmp/d911v-mb /tmp/d911v-head
Files BASE/.claude/.precommit-par/plan-integrity.out and HEAD/.claude/.precommit-par/plan-integrity.out differ
Files BASE/.claude/.precommit-par/verifiable-done.out and HEAD/.claude/.precommit-par/verifiable-done.out differ
Files BASE/.claude/bypass.log and HEAD/.claude/bypass.log differ
Only in HEAD/.claude/task-briefs: 2026-09-22-D911-C-g12-writeset.md
Files BASE/.codex/control-tower/logs/degraded-events.log and HEAD/.codex/control-tower/logs/degraded-events.log differ
Files BASE/.codex/control-tower/tmp/bp-template.md and HEAD/.codex/control-tower/tmp/bp-template.md differ
Only in HEAD/memory/notes/proposed: 2026-09-22-d911-c-g12-writeset.md
Files BASE/scripts/control-tower/brief_parser.py and HEAD/scripts/control-tower/brief_parser.py differ
Files BASE/scripts/pre-commit-check.sh and HEAD/scripts/pre-commit-check.sh differ
Only in HEAD/tests/control-tower: task-scope-g12.test.sh
```

## 1 C4 / C6 解析器级复算（自验员自建夹具）

### 1.1 C4 — `parse_q2()` 剥壳（反引号 + ` — ` / ` —— ` + 裸路径 + exclude 尾斜杠）

夹具 `/tmp/d911-verify/c4-verifier.md`：

```
## Q2: 范围
做什么:
- `scripts/pre-commit-check.sh` —— 双破折号
- `scripts/control-tower/brief_parser.py` — 单破折号
- scripts/plain/bare.sh
- tests/probe-dir/
不做什么:
- 不改 scripts/audit/（红线，尾斜杠契约）
```

命令与输出：

```
$ python3 scripts/control-tower/brief_parser.py --q2-include <夹具>
scripts/pre-commit-check.sh
scripts/control-tower/brief_parser.py
scripts/plain/bare.sh
tests/probe-dir/
```

```
$ python3 scripts/control-tower/brief_parser.py --q2-exclude <夹具>
scripts/audit/
```

### 1.2 C6 — 四条 Done fixture ×两套判定（生产 awk 行 eval + `parse_done()`）

命令：抽取交付版生产 awk 行原样 eval（非复制实现），并用交付版解析器取 `done_count`。

```
$ grep -m1 'DONE_SECTION=\$(awk' scripts/pre-commit-check.sh
    DONE_SECTION=$(awk '/^## Done 标准/{found=1; inline=$0; sub(/^##[[:space:]]*Done[[:space:]]*标准[[:space:]]*[:：]?[[:space:]]*/, "", inline); if (inline != "") print inline; next} /^## /{if(found) exit} found' "$BRIEF" 2>/dev/null)  # swallow-ok: brief 读不到 → 段空 → 判「未定义完成标准」（fail-closed，不假绿）
```

```
fixture                            awk DONE_EMPTY   parse_done N    
brief-c6-inline                    1                1               
brief-c6-block                     2                2               
brief-c6-empty-heading             0                0               
brief-c6-empty-inline              0                0               
```

> 修前对照（准备阶段采集，同夹具）：inline 红／done=0；block 绿／2；empty-heading 红／0；empty-inline 红／0

## 2 场景矩阵（自建沙箱 `/tmp/d911v-sb`，逐场景取 G12 判定段）

沙箱结构（自验员自建，独立于编码方测试夹具 `/tmp/g12d911.*`）：

- 夹具 brief：`2026-03-10-D9501-c-main.md`（Q2 用 `` `path` —— 说明 `` 习惯写法）、`2026-03-10-D9502-c-s2.md`
- S1 声明：`task-state/D9501.json` → `write_set: [probe-s1-exact.sh, tests/probe-dir/, src/feat]`（**尾斜杠 / 无尾斜杠两种目录写法**）
- S2 声明：`docs/plans/codex/implementation/SYNOVA-IMPL-D9502-verify-20260310.md` §写集表
- commit 日期：`C1=2026-03-10`、`C2=2026-04-20`、`C3=<真实今天>`
- 探针文件由脚本在**暂存时新建**（未预先提交，确保 `git diff --cached` 有真实变更）

### S1 — C1 三源并集（S3 剥壳 / S3 裸 / S1 精确 / S1 目录尾斜杠 / S1 目录无尾斜杠 / S2）

```
$ git checkout C1 && <暂存 6 个探针> && SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
    bash scripts/pre-commit-check.sh        # 生产模式（无 SYNO_TEST_ARM）
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = 2026-03-10，窗口 ±1 天）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

越界计数（`grep -c '不在 Q2 范围内'`）= 0。

### S2 — C2 判别性：同一 commit + 两个不同注入「今天」

```
$ SYNO_TEST_ARM=1 SYNO_G12_COMMIT=C1 SYNO_G12_TODAY=2030-01-01 SYNO_CI=1 bash scripts/pre-commit-check.sh
$ SYNO_TEST_ARM=1 SYNO_G12_COMMIT=C1 SYNO_G12_TODAY=2040-12-31 SYNO_CI=1 bash scripts/pre-commit-check.sh
$ diff <(G12 段-2030) <(G12 段-2040)
```

```
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内（认领基准 commit-date(1c2555a69e79a5324d512d67724bb40a34449691) = 2026-03-10，窗口 ±1 天）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

```
$ diff -q <(2030-01-01 G12 段) <(2040-12-31 G12 段)
✅ 无差异（输出为空）—— 判定段逐字相同
```

### S3 — 注入缝生产默认（item 1）：不设 `SYNO_TEST_ARM`，同时注入脏值

```
$ SYNO_G12_TODAY=2030-01-01 SYNO_G12_COMMIT=C2(2026-04-20) bash scripts/pre-commit-check.sh   # 无 SYNO_TEST_ARM
```

```
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = 2026-03-10，窗口 ±1 天）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

污染计数（注入值是否泄入判定）: '2030-01-01'=0  '2026-04-20'=0

### S4 — C3 本地：commit 换到 C2(2026-04-20) → brief 掉出 ±1 天窗口

```
$ git checkout C2 && SYNO_CI=0 bash scripts/pre-commit-check.sh
```

```
── 组 12/13: Task Scope 一致性 ──
  ⚠️  G12: 无认领 brief → 本组未执行 (D911 fail-open 显式化): 1 处  [V5 软提示——CI 为权威，本地不阻断]
     ⚠️ 无认领 brief → 本组未执行（暂存 1 个文件未做写集对账；认领窗口 = 提交日 2026-04-20 ±1 天，基准 src=commit-date(HEAD)）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

假绿计数（`所有文件均在 Q2 范围内`）= 0

### S5 — C3 CI strict：同场景 `SYNO_CI=1`

```
$ SYNO_CI=1 bash scripts/pre-commit-check.sh
```

```
── 组 12/13: Task Scope 一致性 ──
  ❌ G12: 无认领 brief → 本组未执行 (D911 fail-open 显式化): 1 处  [CI strict——软提示在 CI 上为硬阻断]
     ⚠️ 无认领 brief → 本组未执行（暂存 1 个文件未做写集对账；认领窗口 = 提交日 2026-04-20 ±1 天，基准 src=commit-date(HEAD)）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

## 3 反例夹具（必跑项）

### S6 — 文件确实不在三源并集内 → 生产模式硬阻断

```
$ git checkout C1 && <暂存 scripts/control-tower/probe-unclaimed.sh（三源均未声明）> \
    && bash scripts/pre-commit-check.sh          # 生产模式，无注入缝
rc=1
```

完整 G12 判定段：

```
```

```
── 组 12/13: Task Scope 一致性 ──
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     scripts/control-tower/probe-unclaimed.sh (不在 Q2 范围内 — 三源并集 S1 task-state.write_set / S2 dev doc 写集表 / S3 brief Q2 均未认领)
     💡 改 scripts/ 需先认领 brief（Q2 写集声明）——见 docs/synova/coordination/版本管理规范-控制塔.md
     💡 正解（D749 单一事实源）: 在 brief 里写 `## 写集` 机器块——Markdown 表格，每行 | `path` | task |
        机器块**优先于** Q2 散文（scripts/control-tower/brief_parser.py::parse_write_set）；
        或把 Q2 路径写成裸路径（不带反引号与后置说明）。
     💡 G12 认领源 = 三源并集: S1 task-state/<D#>.json:write_set ＋ S2 dev doc §写集表 ＋ S3 brief Q2。
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

### S7 — 目录前缀语义边界（item 3② 等价性夹具）

声明 `src/feat`（**无尾斜杠**）；暂存 `src/feat/inside.ts`（应被认领）与 `src/feature-x.ts`（**不得**被认领）。

```
$ git checkout C1 && <暂存 src/feat/inside.ts src/feature-x.ts> && bash scripts/pre-commit-check.sh
```

```
── 组 12/13: Task Scope 一致性 ──
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     src/feature-x.ts (不在 Q2 范围内 — 三源并集 S1 task-state.write_set / S2 dev doc 写集表 / S3 brief Q2 均未认领)
     💡 改 scripts/ 需先认领 brief（Q2 写集声明）——见 docs/synova/coordination/版本管理规范-控制塔.md
     💡 正解（D749 单一事实源）: 在 brief 里写 `## 写集` 机器块——Markdown 表格，每行 | `path` | task |
        机器块**优先于** Q2 散文（scripts/control-tower/brief_parser.py::parse_write_set）；
        或把 Q2 路径写成裸路径（不带反引号与后置说明）。
     💡 G12 认领源 = 三源并集: S1 task-state/<D#>.json:write_set ＋ S2 dev doc §写集表 ＋ S3 brief Q2。
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

> 结论：`src/feat` 认领 `src/feat/inside.ts`（前缀 + `/` 边界），**不**认领 `src/feature-x.ts`。
> 与 `_clean_entry` 的 `rstrip("/")` 语义等价：S1 中 `tests/probe-dir/`（带尾斜杠）同样认领到 `tests/probe-dir/deep/probe-glob.sh`。
> 两种写法（带/不带尾斜杠）均命中，且未出现「前缀包含误判」。

## 4 C6 端到端（真入口 G6 + G12b 两条路径）

### S8 — 内联 `## Done 标准: …` 的 brief，走**真实入口** `pre-commit-check.sh`

沙箱 commit `C3`（commit date = 真实今天），brief `2026-09-22-D9503-c6-inline.md` 的 Done 为**内联写法**。

```
$ git checkout C3 && <暂存 scripts/control-tower/probe-c6.sh> && bash scripts/pre-commit-check.sh
rc=1（其余组；G12/G6 相关见下）
```

G12 判定段：

```
```

```
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = 2026-09-22，窗口 ±1 天）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
## 3 反例夹具（必跑项）

### S6 — 文件确实不在三源并集内 → 生产模式硬阻断

```
$ git checkout C1 && <暂存 scripts/control-tower/probe-unclaimed.sh（三源均未声明）>
    && bash scripts/pre-commit-check.sh          # 生产模式，无注入缝
rc=1
```

完整 G12 判定段：

```
── 组 12/13: Task Scope 一致性 ──
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     scripts/control-tower/probe-unclaimed.sh (不在 Q2 范围内 — 三源并集 S1 task-state.write_set / S2 dev doc 写集表 / S3 brief Q2 均未认领)
     💡 改 scripts/ 需先认领 brief（Q2 写集声明）——见 docs/synova/coordination/版本管理规范-控制塔.md
     💡 正解（D749 单一事实源）: 在 brief 里写 `## 写集` 机器块——Markdown 表格，每行 | `path` | task |
        机器块**优先于** Q2 散文（scripts/control-tower/brief_parser.py::parse_write_set）；
        或把 Q2 路径写成裸路径（不带反引号与后置说明）。
     💡 G12 认领源 = 三源并集: S1 task-state/<D#>.json:write_set ＋ S2 dev doc §写集表 ＋ S3 brief Q2。
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

### S7 — 目录前缀语义边界（item 3② 等价性夹具）

声明 `src/feat`（**无尾斜杠**）；暂存 `src/feat/inside.ts`（应被认领）与 `src/feature-x.ts`（**不得**被认领）。

```
$ git checkout C1 && <暂存 src/feat/inside.ts src/feature-x.ts> && bash scripts/pre-commit-check.sh
rc=1
```

完整 G12 判定段：

```
── 组 12/13: Task Scope 一致性 ──
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     src/feature-x.ts (不在 Q2 范围内 — 三源并集 S1 task-state.write_set / S2 dev doc 写集表 / S3 brief Q2 均未认领)
     💡 改 scripts/ 需先认领 brief（Q2 写集声明）——见 docs/synova/coordination/版本管理规范-控制塔.md
     💡 正解（D749 单一事实源）: 在 brief 里写 `## 写集` 机器块——Markdown 表格，每行 | `path` | task |
        机器块**优先于** Q2 散文（scripts/control-tower/brief_parser.py::parse_write_set）；
        或把 Q2 路径写成裸路径（不带反引号与后置说明）。
     💡 G12 认领源 = 三源并集: S1 task-state/<D#>.json:write_set ＋ S2 dev doc §写集表 ＋ S3 brief Q2。
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

> **结论**：`src/feat` 认领 `src/feat/inside.ts`（前缀 + `/` 边界），**不**认领 `src/feature-x.ts`。
> 与 `_clean_entry` 的 `rstrip("/")` 语义等价：S1 中 `tests/probe-dir/`（**带**尾斜杠）同样认领到 `tests/probe-dir/deep/probe-glob.sh`。
> 带 / 不带尾斜杠两种写法均命中，且未出现「前缀包含误判」（`src/feature-x.ts` 未被 `src/feat` 误收）。

## 4 C6 端到端（真入口 G6 + G12b 两条路径）

### S8 — 内联 `## Done 标准: …` 的 brief，走**真实入口** `pre-commit-check.sh`

沙箱 commit `C3`（commit date = 真实今天），brief `2026-09-22-D9503-c6-inline.md` 的 Done 为**内联写法**。

```
$ git checkout C3 && <暂存 scripts/control-tower/probe-c6.sh> && bash scripts/pre-commit-check.sh
rc=1（其余组在沙箱内缺依赖；G6 / G12 / G12b 见下）
```

G12 判定段：

```
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = 2026-09-22，窗口 ±1 天）
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)
```

组 6（Task Brief）原始行：

```
── 组 6/13: Task Brief (6 核心字段) ──
  ✅ 主树占用检测 (D537 #2): 主树脏 + 多活跃 session
  ✅ Task Brief: 编码变更须有今日 task brief
  ✅ Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)
  ✅ 骨架 brief 占位符检测（认领 agent 填写后提交，禁提交骨架）
  ✅ 时间戳顺序: brief 必须早于代码写入
  ✅ Notes 迁移门禁: 无 proposed/ 变更（跳过）
bash: /private/tmp/d911v-sb/scripts/check-plan-integrity.sh: No such file or directory
```

关键计数（对 S8 完整原始输出取 `grep -c`）——两条路径都必须为 **0**：

```
「Done 标准: 至少需定义一条完成标准」出现次数 = 0
「Done 标准无条目」出现次数                        = 0
```

### S9 — 消费侧反例：`check-brief-parseable.sh`（pre-commit 组 12b 的消费者）

```
```
$ bash scripts/workflow/check-brief-parseable.sh <内联 Done fixture>    # 期望：不再报「无条目」
  [check-brief-parseable] ❌ brief 不可解析: /tmp/d911v-out/d-inline.md
    Q2 做什么 无路径条目（至少 1 条）
    #CRITERIA 缺失（必填 A-D）
  EXIT=1   「Done 标准无条目」计数 = 0
  （其余 ❌ 项为最小夹具缺 Q2/#CRITERIA —— 与 Done 无关，两侧同样报）

$ bash scripts/workflow/check-brief-parseable.sh <空 Done fixture>      # 反例：必须仍报「无条目」
  [check-brief-parseable] ❌ brief 不可解析: /tmp/d911v-out/d-empty.md
    Q2 做什么 无路径条目（至少 1 条）
    #CRITERIA 缺失（必填 A-D）
    Done 标准无条目（至少 1 条）
  EXIT=1   「Done 标准无条目」计数 = 1
```
```

## 5 零新增红 · 既有测试两侧对照（受控基线树 vs 交付树，同命令同环境）

两树均为 `git archive` 导出（无 `.git` 顶层），条件对称；差异见 §0.3（仅交付 6 文件）。

```
test                     BASE(main@f2b251f3)        HEAD(交付)                   对照
today-by-name            rc=1  tests/control-tower/today-by-name.test.sh: lin rc=0  结果: 通过 15 / 失败 0       ★变化★
hard-gate-convergence    rc=1  结果: 21 通过, 5 失败        rc=1  结果: 21 通过, 5 失败        同(红)
g12-taskstate-exempt     rc=1  结果: 通过 2 / 失败 5        rc=1  结果: 通过 2 / 失败 5        同(红)
utf8                     rc=1  ══════════════════════════════════════════════ rc=1  ══════════════════════════════════════════════ 同(红)
g12-day-window           rc=0  结果: 10 通过, 0 失败        rc=0  结果: 10 通过, 0 失败        同(绿)
brief_parser             rc=0  结果: 6 通过, 0 失败         rc=0  结果: 6 通过, 0 失败         同(绿)
brief-parser-strip       rc=0  结果: 30 通过, 0 失败        rc=0  结果: 30 通过, 0 失败        同(绿)
resolve-commit-brief     rc=0  ══════════════════════════════════════════════ rc=0  ══════════════════════════════════════════════ 同(绿)
brief-parseable          rc=0  ══════════════════════════════════════════════ rc=0  ══════════════════════════════════════════════ 同(绿)
```

> 结论：**零新增红**。唯一变化 `today-by-name` 为 **rc=1 → rc=0**（存量红转绿），非新增红。
> 存量红 3 张（`hard-gate-convergence` / `g12-taskstate-exempt` / `utf8`）在两侧逐字相同。

### 5.1 存量红「非本次引入」证明（逐字对照）

```
$ diff -q hard-gate-convergence.base.txt hard-gate-convergence.head.txt
  ✅ 无差异（两树逐字相同）   (diff rc=0)
$ diff -q g12-taskstate-exempt.base.txt g12-taskstate-exempt.head.txt
  ✅ 无差异（两树逐字相同）   (diff rc=0)
$ diff -q utf8.base.txt utf8.head.txt
  ✅ 无差异（两树逐字相同）   (diff rc=0)
$ diff -q today-by-name.base.txt today-by-name.head.txt
  Files /tmp/d911v-out/tests/today-by-name.base.txt and /tmp/d911v-out/tests/today-by-name.head.txt differ   (diff rc=1)
```

三张存量红的失败要点（BASE 侧原文，HEAD 侧同文）：

```
── hard-gate-convergence
   ❌ 未按 spec 降软: soft_check "Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)"
   ❌ 未按 spec 降软: soft_check "G12: task brief Q2 范围一致性"
   ❌ 行为A4: 应 exit 0, 实际 1 ::
   [0;31m❌ 1 组未通过 — 提交已拒绝[0m
   ❌ 行为B: 应 exit 0, 实际 1 ::
   [0;31m❌ 1 组未通过 — 提交已拒绝[0m
   ❌ 行为B: 越界文件未点名
   结果: 21 通过, 5 失败
── g12-taskstate-exempt
   ❌ T2 task-state/D481.json 应豁免 (期望 [SKIP] 实际 [])
   ❌ T2b task-state/D481.md 应豁免 (期望 [SKIP] 实际 [])
   ❌ T3 task-state/evil.ts 不应豁免（防藏代码） (期望 [CHECK] 实际 [])
   ❌ T4 src/foo.ts 不应豁免（代码保护不削弱） (期望 [CHECK] 实际 [])
   ❌ T5 docs/x.md 应豁免（D366 先例保持） (期望 [SKIP] 实际 [])
   结果: 通过 2 / 失败 5
── utf8
   ✅ GBK 文件被报告（输出含 ❌）
   ❌ scripts/ci/check-contract-gaps.sh 仍被报缺 UTF-8 头块
   ❌ scripts/ci/diagnosis-quality-check.sh 仍被报缺 UTF-8 头块
   结果: 8 通过, 2 失败
   Status: ❌ utf8 测试未通过
```

### 5.2 today-by-name（item 2）由红转绿的直接证据

```
$ head -6 today-by-name.base.txt   # BASE: 提取函数体后崩于 DAY_WINDOW_RE: unbound variable
── 0. 生产定义提取 ──
  ✓ pre-commit-check.sh 定义 today_files_by_prefix
  ✓ verify-parallel.sh 定义 today_files_by_suffix
  ✓ 提取函数体 bash -n 语法合法

── 1. brief 前缀筛选 (D362 死锁复现) ──

$ tail -4 today-by-name.head.txt   # HEAD: 通过 15 / 失败 0
── 4. 生产调用点 (DS2: ≥4) ──
  ✓ 4 脚本 today_files_by 生产调用 ≥4 (实际 18)

结果: 通过 15 / 失败 0
```

## 6 门禁级零新增红（同暂存探针，生产模式）

两树各自 `git init` + 单次提交，写入**同一个** `.claude/current-brief`（消除 brief 选择差异），
暂存**同一个**中立探针 `scripts/control-tower/verify-probe-d911.sh`（两侧均无 brief 认领），
再跑 `bash scripts/pre-commit-check.sh`，对**组级判定行**取差集：

```
$ diff gate-base.verdicts.txt gate-head.verdicts.txt    # 61 条判定行 / 每侧
21c21
< ❌ G12b: brief 可解析性 (D313 M3): 3 处  [硬阻断]
---
> ❌ G12b: brief 可解析性 (D313 M3): 2 处  [硬阻断]
```

唯一差异为 **G12b 由 3 处降为 2 处**（红度下降，非新增红），明细：

```
── BASE
   ❌ G12b: brief 可解析性 (D313 M3): 3 处  [硬阻断]
   [check-brief-parseable] ❌ brief 不可解析: /private/tmp/d911v-gbase/.claude/task-briefs/2026-09-22-D861-win测试修复-代行.md
   #CRITERIA 缺失（必填 A-D）
   Done 标准无条目（至少 1 条）
   
── HEAD
   ❌ G12b: brief 可解析性 (D313 M3): 2 处  [硬阻断]
   [check-brief-parseable] ❌ brief 不可解析: /private/tmp/d911v-ghead/.claude/task-briefs/2026-09-22-D861-win测试修复-代行.md
   #CRITERIA 缺失（必填 A-D）
   
   ── 组 13/13: 技能同步一致性 ──
```

> 差异来源：真实仓库 brief `.claude/task-briefs/2026-09-22-D861-win测试修复-代行.md:59` 采用**内联**
> `## Done 标准: 至少一条可验证的完成标准`。BASE 报 `Done 标准无条目（至少 1 条）`，HEAD 不再报
> （`#CRITERIA 缺失` 两侧同报 = 该 brief 真实缺字段，与本交付无关）。
> 两侧最终 `exit=1`，`⚠️ 1 项警告` 相同。

## 7 红证不残留（item 5）

> **约定出处（自验员核实）**：`INJECTED-RED` **是**既有约定 ——
> ① 定义：`docs/synova/presets/synova-squad-lead/SYSTEM-PROMPT.md:29`「红证不残留（`grep -c INJECTED-RED`=0）」；
> ② 事故前例：`docs/synova/audit-reports/2026-09-20-K3-D854.md:40`（D854：测试内注入 `[ "INJECTED-RED" = "expected-green" ]` 断言残留在 PR diff）。
> 本自验据此采用**双重判据**：① 哨兵/调试/临时路径全零；② 红夹具**重跑后已变绿**（§1、§2、§4 修前/修后对照）。
>
> ⚠️ **自污染声明**：本证据文件自身包含 `INJECTED-RED` 字样（就是本段），因此全域 `grep` 必须**排除本自验两件证据文件**，
> 否则会把自己的说明文字当残留命中（自验员首次扫描即踩此坑，已修正）。

```
pattern           pre-commit-check.sh   brief_parser.py   new_test
INJECTED-RED                        0                 0          0
RED-INJECT                          0                 0          0
FIXME                               0                 0          0
DEV-ONLY                            0                 0          0
/tmp/g12d911                        0                 0          0
/tmp/d911                           0                 0          0
echo DEBUG                          0                 0          0
set -x                              0                 0          0

$ grep -c 'XXX' tests/control-tower/task-scope-g12.test.sh   # 唯一非零：mktemp 模板 XXXXXX
  49:SB="$(mktemp -d "${TMPDIR:-/tmp}/g12d911.XXXXXX")"

$ grep -rn 'INJECTED-RED' scripts/ tests/ docs/synova/ 2>/dev/null \    # 全域（排除本自验证据文件）
    | grep -v 'D911-evidence-20260922.md|D911-verify-20260922.md' | wc -l
  2
  → 两处均为**存量文档**，非残留：
    docs/synova/presets/synova-squad-lead/SYSTEM-PROMPT.md:29      （约定定义本身）
    docs/synova/audit-reports/2026-09-20-K3-D854.md:40             （K3 记录的 D854 注入残留事故）

$ grep -c 'INJECTED-RED' scripts/pre-commit-check.sh scripts/control-tower/brief_parser.py \
      tests/control-tower/task-scope-g12.test.sh      # 只扫交付三件 = 真正要判的对象
  scripts/pre-commit-check.sh:0
  scripts/control-tower/brief_parser.py:0
  tests/control-tower/task-scope-g12.test.sh:0
```

## 8 契约与既有测试形状（g12-day-window 三条 + item 2）

```
$ grep -c 'DAY_WINDOW_RE=$(python3 -c' scripts/pre-commit-check.sh   → 1   (期望 1)
$ grep -c '[[ "$b" =~ $DAY_WINDOW_RE ]]' scripts/pre-commit-check.sh   → 1   (期望 1)
$ grep -cE '^today_files_by_suffix\(\)' scripts/pre-commit-check.sh   → 0   (期望 0 = 死代码已删)

$ grep -n 'TODAY_DASH=' scripts/pre-commit-check.sh     # 只能有一处，且来源 = G12_TODAY
  1277:TODAY_DASH="$G12_TODAY"   # D911/C2: 与 G12_TODAY 同源（commit date），不再各自取墙钟

$ awk 'NR>=1200 && NR<=1320 && /date /' scripts/pre-commit-check.sh   # 组12 区域内墙体时钟调用
  #   @degraded commit date 取不到 → 回退墙钟/注入缝 + **显式 ⚠️ 打印**（铁律 11，禁静默）
# 背景（裁定记录 §缺陷③ 3b）: 旧实现用 `date +%Y-%m-%d`（运行日）→ brief 掉出 ±1 天窗口
  # 降级路径: commit date 不可得（非 git 树/浅克隆/ref 不存在）→ 显式打印后回退
    G12_TODAY="$G12_DAY_OVERRIDE"; G12_DAY_SRC="override(commit-date 不可得)"
    G12_TODAY="$(date +%Y-%m-%d)"; G12_DAY_SRC="wall-clock(降级)"
  echo -e "  ${YELLOW}⚠️  G12: 取不到提交 ${G12_COMMIT_REF} 的 commit date — 「今日」降级为 ${G12_DAY_SRC} = ${G12_TODAY}（判据不再确定，铁律 11 显式登记）${RESET}"
  → 唯一真实调用在**降级分支** L1245 (date +%Y-%m-%d)，紧随 L1247 显式 ⚠️ 打印（契约允许）

$ sed -n '<today_files_by_prefix 段>' scripts/pre-commit-check.sh     # 缺省自持
today_files_by_prefix() {
  local dir="$1" f b
  # D911/C2: 窗口正则缺省自持 —— tests/control-tower/today-by-name.test.sh:41 只提取
  #   本函数体 eval（不 eval 顶层 DAY_WINDOW_RE）→ 旧实现读未设的全局 → set -u 下
  #   "DAY_WINDOW_RE: unbound variable" 直接崩（该测试在 main 上预存红，实测见交付证据）。
  #   生产行为不变: 顶层已设 → local 取到同一个值；仅未设时按单日 TODAY_DASH 回退。
  local DAY_WINDOW_RE="${DAY_WINDOW_RE:-}"
  [ -z "$DAY_WINDOW_RE" ] && DAY_WINDOW_RE="^${TODAY_DASH}-"
  dir="${dir%/}"
  [ -d "$dir" ] || return 0
  for f in "$dir"/*.md; do
```

## 9 Windows 兼容抽查 / bash 3.2 / 写集纪律

```
$ bash -n scripts/pre-commit-check.sh
  bash -n pre-commit-check.sh: OK
$ bash -n tests/control-tower/task-scope-g12.test.sh
  bash -n new_test: OK
$ python3 -c 'import ast; ast.parse(...brief_parser.py...)'
  ast.parse OK
$ grep -c 'declare -A' scripts/pre-commit-check.sh   → 0   (bash 3.2 无关联数组，期望 0)
$ head -3 tests/control-tower/task-scope-g12.test.sh   # UTF-8 头块
  #!/usr/bin/env bash
  # ═══════════════════════════════════════════════════════════════════════════════
  # task-scope-g12.test.sh — D911 切片 C：组 12 判据（三源并集 / commit-date / fail-open / 剥壳）
$ grep -n 'tr -d' scripts/pre-commit-check.sh | head -4   # CRLF/换行清洗（含交付新增两处）
  59:      _first=$(echo "$matches" | head -1 | tr -d '%' | cut -c1-300)
  242:  BYPASS_COUNT=$(grep -c "${TODAY}.*detected-bypass" "$BYPASS_LOG" 2>/dev/null | tr -d '\n\r' || echo 0)
  405:  [ -f "$PAR_DIR/$name.code" ] && code=$(cat "$PAR_DIR/$name.code" 2>/dev/null | tr -d '\n\r')
  593:    EXPECT_COUNT=$(grep -c 'expect(' "$tf" 2>/dev/null | tr -d '\n\r' || echo 0)

$ git diff --name-only origin/main...origin/fix/D911-g12-writeset
.claude/bypass.log
.claude/task-briefs/2026-09-22-D911-C-g12-writeset.md
memory/notes/proposed/2026-09-22-d911-c-g12-writeset.md
scripts/control-tower/brief_parser.py
scripts/pre-commit-check.sh
tests/control-tower/task-scope-g12.test.sh

$ git diff --name-only origin/main...origin/fix/D911-g12-writeset | grep -c 'ci.yml\|workflows'
  0   (单写者边界：.github/workflows/ci.yml 未触碰)
```

