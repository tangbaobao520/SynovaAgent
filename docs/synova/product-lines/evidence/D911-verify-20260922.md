# D911 切片 C 独立自验报告（2026-09-22）

> **角色**：D911 小队 · **自验**（**非编码兼任**；本报告只出「自验结论」，不下"通过"判定——通过与否归 CTO 收件闸 + K3 终审）
> **被审产物**：`fix/D911-g12-writeset` @ **`141a3d6c`**（= 认领 task-2 时的远端 HEAD）
> **受控基线**：`f2b251f3`（= `git merge-base origin/main origin/fix/D911-g12-writeset`，即分叉前 main 树；与准备阶段采基线的 SHA 一致）
> **工作树**：`.synova-wt-d911c`（只读产品代码；仅写本报告与 `D911-evidence-20260922.md` 两件）
> **原始输出存档**：`docs/synova/product-lines/evidence/D911-evidence-20260922.md`（含全部夹具输出、对照表、grep 原始结果）
> **自验自建夹具**：沙箱 `/tmp/d911v-sb`（**独立于编码方测试夹具** `/tmp/g12d911.*`）、受控两树 `/tmp/d911v-mb`·`/tmp/d911v-head`、归档 `/tmp/d911v-out`

---

## 一 自验结论

# `可提请独立审计`

- **未发现**编码交付与卡面/实测不符之处；**未发现需退回的缺陷**。
- 全部 6 条卡面判据（C1–C6）+ 6 条 lead 指定必核项（item 1–6）**均独立复现**。
- 3 张存量红（`hard-gate-convergence` / `g12-taskstate-exempt` / `utf8`）经**两树逐字对照**证明**非本次引入**；
  另有 1 张存量红 `today-by-name` 由 **rc=1 → rc=0**（转绿），亦非新增红。
- 遗留项 **6 条**（§六），均**不在 C 切片写集**，**未代为修复**（守「不擅自扩写集」）。

---

## 二 复核率

**复核率 = 44 / 46 = 95.7%**（分子 = 自验员**自建夹具/自跑命令**独立复算的验收点；分母 = 本交付声明的验收点全集）

> 口径声明：分母由自验员按卡面 §一 切片 C（C1–C6）＋派单件 §三 验收判据 ＋ lead item 1–6 **逐条枚举**得到（下表逐项列出，可核）。
> 未计入分子的 2 条（#7、#14）**并非未验**，而是其可直接观测的注入路径仅存在于编码方测试中，自验侧只能**静态审读 + 编码测试佐证**——故不计入分子，以保持复核率不虚高。

| # | 验收点（分组） | 独立复算 | 自验证据 |
|---|---|---|---|
| 1 | C1 · S1 精确条目认领 | ✅ | S1 判定段「所有文件均在 Q2 范围内」 |
| 2 | C1 · S1 glob 条目认领 | ✅ | S1（`tests/probe-dir/`） |
| 3 | C1 · S1 目录条目（尾斜杠写法） | ✅ | S1 |
| 4 | C1 · S1 目录条目（无尾斜杠写法） | ✅ | S1（`src/feat`） |
| 5 | C1 · S2 dev doc 写集表认领 | ✅ | S1（`probe-s2.sh`） |
| 6 | C1 · S3 brief Q2 认领（含剥壳形态） | ✅ | S1 |
| 7 | C1 · 声明源不可读 → 显式 ⚠️（禁静默） | — | 编码测试断言 + 静态审读（`_S1_RC=3` 分支） |
| 8 | C2 · 基准 = commit date | ✅ | S1/S2/S3 判定语 `commit-date(…)` |
| 9 | C2 · 与运行日解耦（两"今天"结论相同） | ✅ | S2 `diff` 空 |
| 10 | C2 · 判别器活性（换 commit → 窗口跟走） | ✅ | S4 基准变 2026-04-20 → brief 掉窗口 |
| 11 | C2 · 生产默认不读注入缝 | ✅ | S3 污染计数 = 0 / 0 |
| 12 | C2 · `TODAY_DASH` 与 `G12_TODAY` 同源 | ✅ | `grep -n 'TODAY_DASH='` 单处 = `"$G12_TODAY"` |
| 13 | C2 · `today_files_by_prefix` 缺省自持 | ✅ | 两树对照 `today-by-name` rc 1→0 + 函数体 |
| 14 | C2 · 降级路径显式 ⚠️ + 回退（禁静默） | — | 编码测试断言 + 静态审读（L1245 墙钟调用紧随 L1247 ⚠️） |
| 15 | C2 · 既有形状契约（两处字面）保持 | ✅ | `grep -c` → 1 / 1 |
| 16 | C3 · 空认领 → 本地 ⚠️ 显式 | ✅ | S4 |
| 17 | C3 · 禁假绿（不再输出"所有文件均在 Q2 范围内"） | ✅ | S4 假绿计数 = 0 |
| 18 | C3 · CI strict 转硬阻断 | ✅ | S5 `❌ … [CI strict…]` |
| 19 | C4 · 剥反引号 | ✅ | §1.1 复算 |
| 20 | C4 · 剥单破折号 ` — ` | ✅ | §1.1 复算 |
| 21 | C4 · 剥双破折号 ` —— ` | ✅ | §1.1 复算 |
| 22 | C4 · 裸路径不回归 | ✅ | §1.1 复算 |
| 23 | C4 · exclude 尾斜杠契约保持 | ✅ | §1.1 复算（`scripts/audit/`） |
| 24 | C4 · 目录前缀语义 ≡ `_clean_entry` rstrip | ✅ | S1 两写法均命中 |
| 25 | C4 · 目录前缀边界（不误收 `src/feature-x.ts`） | ✅ | S7 |
| 26 | C5 · 提示含 `## 写集` 正解 | ✅ | S6 完整段 |
| 27 | C5 · 指向可核函数 `parse_write_set` | ✅ | S6 |
| 28 | C5 · 写明认领源 = 三源并集 | ✅ | S6 |
| 29 | C6 · G6 awk 接受内联 | ✅ | §1.2（inline `DONE_EMPTY=1`） |
| 30 | C6 · G6 反例：空段仍红 | ✅ | §1.2（`=0`） |
| 31 | C6 · G6 反例：仅占位仍红 | ✅ | 编码测试 + 自验 4-fixture 矩阵 |
| 32 | C6 · `parse_done` 接受内联 | ✅ | §1.2（`done_count=1`） |
| 33 | C6 · 消费侧内联不报「无条目」 | ✅ | S9 |
| 34 | C6 · 消费侧反例：空段仍报「无条目」 | ✅ | S9 |
| 35 | C6 · **真入口 G6**（组 6 六字段，内联不假红） | ✅ | S8 组 6 原始行 |
| 36 | C6 · **真入口 G12b**（消费侧，内联不报） | ✅ | S8 / S9 |
| 37 | 反例 · 不在三源并集 → 硬阻断 + 退出码非 0 | ✅ | S6（`[硬阻断]`，rc=1） |
| 38 | 不变量 · 红证不残留 | ✅ | §七 grep 矩阵（交付三件全 0） |
| 39 | 不变量 · 门禁级零新增红 | ✅ | §五（判定行差集 = 1 处红度下降） |
| 40 | 不变量 · 既有测试零新增红（9 测试 × 2 树） | ✅ | §五 |
| 41 | 不变量 · 存量红非本次引入（3 张逐字相同） | ✅ | §五.1 |
| 42 | 不变量 · 交付新测试绿（38 断言） | ✅ | 38 通过 / 0 失败 / EXIT=0 |
| 43 | 不变量 · 工作树无污染 | ✅ | `git status --porcelain` = 0（跑测试后复检） |
| 44 | 纪律 · 写集未越界（未碰 `ci.yml`） | ✅ | `git diff --name-only` 6 文件 |
| 45 | 兼容 · Windows 抽查（语法/CRLF/UTF-8） | ✅ | §8/§9 |
| 46 | 约束 · bash 3.2（`declare -A` = 0） | ✅ | §9 |

---

## 三 C1–C6 逐条独立判据（命令 + 结论，原始输出见 evidence §1–§4）

### C1 口径统一 = 三源并集
- **证据**：自建沙箱 S1（生产模式）暂存 6 个探针，分别由 **S1 精确 / S1 glob / S1 目录（两种写法）/ S2 dev doc / S3 brief** 认领 →
  G12 判定段 `✅ 所有文件均在 Q2 范围内（认领基准 commit-date(HEAD) = 2026-03-10，窗口 ±1 天）`，**越界计数 = 0**。
- **口径核对**：S1/S2/S3 均**复用既有解析器**（`brief_parser.py --q2-include`、`devdoc_writeset.py --extract`、`task-state/<D#>.json:write_set` 直读），未新写解析器。

### C2 判据确定性（commit date 基准）
- **S2 判别性夹具（自跑两遍）**：同一 commit，`SYNO_G12_TODAY` 取 `2030-01-01` 与 `2040-12-31` →
  **判定段 `diff` 输出为空（逐字相同）**；基准行显式给出 `commit-date(<sha>) = 2026-03-10`。
- **S3（item 1 生产默认）**：不设 `SYNO_TEST_ARM`，同时注入 `SYNO_G12_TODAY=2030-01-01`、`SYNO_G12_COMMIT=<2026-04-20 commit>` →
  基准仍为 `commit-date(HEAD) = 2026-03-10`；**注入值出现次数 0 / 0**（未生效）。
- **S4 判别器活性**：commit 换到 2026-04-20 → 窗口基准随之变为 2026-04-20，brief 掉出窗口 → C3 分支触发（证明基准是活的、不是常数）。
- **静态核对**：`grep -n 'TODAY_DASH='` 仅 1 处且 = `"$G12_TODAY"`；组 12 段内唯一真实墙钟调用在**降级分支**（L1245）且紧随显式 ⚠️（L1247）。

### C3 禁止静默 fail-open
- **S4（本地）**：`⚠️ G12: 无认领 brief → 本组未执行 (D911 fail-open 显式化)` + 明细行含窗口基准；
  **假绿计数「所有文件均在 Q2 范围内」= 0**（旧实现此处输出假绿）。
- **S5（CI）**：同场景 `SYNO_CI=1` → `❌ … [CI strict——软提示在 CI 上为硬阻断]`。
  静态核对：`soft_check` 在 `SYNO_CI=1` 时 `HARD_FAIL=$((HARD_FAIL+1))`（`pre-commit-check.sh:113`），终局 `exit 1`（:1668-1675）→ **CI 下确为硬阻断**。

### C4 剥壳与 `_clean_entry` 同规则
- **§1.1 复算**（自建 fixture）：`` `path` —— 说明 `` → `scripts/pre-commit-check.sh`；`` `path` — 说明 `` → `scripts/control-tower/brief_parser.py`；裸路径原样；exclude `` `不改 scripts/audit/（红线）` `` → **保留尾斜杠** `scripts/audit/`。
- **端到端**：S1 中该剥壳写法声明的探针**真的被认领**（修前基线此形态必判「不在 Q2 范围内」，见准备阶段实测）。
- **口径偏差（编码主动申报，已核）**：未搬 `_clean_entry` 的 `rstrip("/")` 与计数括号两步。自验**等价性夹具**：
  ① S1 用 `tests/probe-dir/`（带尾斜杠）与 `src/feat`（不带）两种写法，**均认领到目录下文件**；
  ② S7 声明 `src/feat`，`src/feat/inside.ts` 被认领、`src/feature-x.ts` **未被误收**（前缀 + `/` 边界，与 `rstrip("/")` 语义等价）。
  → **不构成缺陷**；`rstrip` 差异由消费者承接，未产生新误拦/漏拦。

### C5 失败提示给出正解
- **S6**：越界输出含 `## 写集` 机器块写法、`parse_write_set`（`brief_parser.py:121` 机器块优先，行号已核）、`三源并集` 认领源说明，原「见版本管理规范」提示保留。

### C6 内联 `## Done 标准: …` 被接受（**两条路径 + 反例**）
- **§1.2 四 fixture × 两套判定**（自建夹具）：

  | fixture | 修前 | 修后 awk `DONE_EMPTY` | 修后 `parse_done` N |
  |---|---|---|---|
  | `## Done 标准: 值`（内联） | 红 / 0 | **1** | **1** |
  | `## Done 标准` + 2 行（块式） | 绿 / 2 | 2 | 2 |
  | `## Done 标准`（0 行）**反例** | 红 / 0 | **0（仍红）** | **0** |
  | `## Done 标准:  `（空值）**反例** | 红 / 0 | **0（仍红）** | **0** |

- **真入口 S8**：commit date = 真实今天 + 内联 Done brief 走 `bash scripts/pre-commit-check.sh` →
  组 6 输出 `✅ Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)`；
  `「Done 标准: 至少需定义一条完成标准」= 0`、`「Done 标准无条目」= 0`（**两条路径均不再假红**）。
- **反例 S9**：空段 fixture 经消费侧仍报 `Done 标准无条目（至少 1 条）`（未放宽成"空也绿"）。
- **真实仓库附加验证**：`2026-09-22-D861-win测试修复-代行.md:59` 正是内联写法 → 门禁对照中 G12b `3 处 → 2 处`（红度下降）。

---

## 四 反例夹具（必跑项）

| 反例 | 期望 | 实测 |
|---|---|---|
| 文件确实不在三源并集内 | 硬阻断 + 退出码非 0 | S6：`❌ … 1 处 [硬阻断]` + 逐条点名 + **rc=1** ✅ |
| 目录前缀边界 | 不误收兄弟路径 | S7：`src/feat` 未收 `src/feature-x.ts`；仅 1 处越界 ✅ |
| C6 空段 Done | 仍红（不放宽） | §1.2 = 0 / 0；S9 消费侧仍报「无条目」 ✅ |
| C6 仅占位 Done | 仍红 | 编码测试 + 判定矩阵 ✅ |

---

## 五 零新增红（独立不变量）

### 5.1 门禁级（同暂存探针、生产模式）
两树各自 `git init` + 提交，写入**同一个** `.claude/current-brief`（消除 brief 选择差异），暂存**同一个**中立探针，跑 `pre-commit-check.sh`：

```
$ diff gate-base.verdicts.txt gate-head.verdicts.txt      # 每侧 61 条组级判定行
21c21
< ❌ G12b: brief 可解析性 (D313 M3): 3 处  [硬阻断]
---
> ❌ G12b: brief 可解析性 (D313 M3): 2 处  [硬阻断]
```

→ 唯一差异是 **G12b 红度下降（3→2）**，成因已定位为 C6 修复（真实 inline Done brief）→ **零新增红**。

### 5.2 既有测试（9 张 × 受控两树）

| test | BASE rc | HEAD rc | 对照 |
|---|---|---|---|
| `today-by-name` | 1 | **0**（15 通过 / 0 失败） | **由红转绿** |
| `hard-gate-convergence` | 1（21 通过 / 5 失败） | 1（同） | 同（存量红） |
| `g12-taskstate-exempt` | 1（通过 2 / 失败 5） | 1（同） | 同（存量红） |
| `utf8` | 1（8 通过 / 2 失败） | 1（同） | 同（存量红） |
| `g12-day-window` | 0（10 通过） | 0（同） | 同（绿）✅ 三条形状断言保持 |
| `brief_parser` | 0（6 通过） | 0（同） | 同（绿） |
| `brief-parser-strip` | 0（30 通过） | 0（同） | 同（绿） |
| `resolve-commit-brief` | 0 | 0 | 同（绿） |
| `brief-parseable` | 0 | 0 | 同（绿） |

**存量红「非本次引入」证明**：`diff -q` 三张红测试的两树输出 → **两两逐字相同**（evidence §5.1 原始输出）。
**交付新测试**：`bash tests/control-tower/task-scope-g12.test.sh` → **38 通过 / 0 失败 / EXIT=0**（与编码自报 38 断言一致），
跑完 `git status --porcelain` = 0（**无污染**）。

### 5.3 lead item 4 指定的四条预存红 —— 处置
| 预存红 | BASE | HEAD | 结论 |
|---|---|---|---|
| `today-by-name`（`DAY_WINDOW_RE: unbound variable`） | rc=1 | **rc=0** | 本交付**修复**（item 2），非新增 |
| `hard-gate-convergence`（A4/B 期望 exit 0 与 `decl_check` 硬阻断冲突） | rc=1，5 失败 | 同 rc、**输出逐字相同** | 存量红，**未修复也未恶化** |
| `g12-taskstate-exempt`（硬编码 `python`） | rc=1，5 失败 | 同 rc、**输出逐字相同** | 存量红，同上 |
| `utf8`（两条 `scripts/ci/*.sh` 缺头块） | rc=1，2 失败 | 同 rc、**输出逐字相同** | 存量红，同上 |

---

## 六 遗留清单（不在 C 切片写集，未代为修复）

| # | 文件:行 | 为什么不修 | 建议 |
|---|---|---|---|
| L1 | `tests/control-tower/task-scope-g12.test.sh`（新建） vs `.github/workflows/ci.yml:224-265` | `ci.yml` 单写者，明确不在本件写集（派单件 §六.4） | 另卡把该测试登记进 canary 列表；**当前 CI 不会跑它** |
| L2 | `scripts/workflow/resolve-commit-brief.sh:189-191` | 内嵌降级解析器仍为**旧规则**（不剥反引号、不剥 ` —— `），与 C4 修复后的主解析器存在漂移；仅在 `brief_parser.py` 不可用的降级路径触发 | 与 C4 同源同步，另卡 |
| L3 | `scripts/pre-commit-check.sh:1505-1510`（G12b 调用点） | 消费者缺失时输出不含 `❌` → 走 `soft_pass` = **假绿**（存量 fail-open，非本次引入；自验在首个沙箱中实测到） | 另卡：调用点加 `[ -f ]` 守卫或非零退出码判定 |
| L4 | `scripts/check-verifiable-done.sh:29`、`scripts/check-plan-integrity.sh:162` | 同病根（`{found=1; next}`）内联盲 awk，C6 未同步（不在写集） | 按 D707「一类一机制」收敛到 `brief_parser.parse_done` |
| L5 | 存量红 3 张（见 §5.3） | 与 C 切片无依赖 | 各自立卡 |
| L6 | `docs/synova/coordination/验收标准-穿真实入口-v1-20260918.md`（121 行，止于 §十一） | 文书缺口，非代码 | 补 §十二 或改派单件引用（见 N1） |

---

## 七 红证不残留（item 5）

- **约定出处已核**：`docs/synova/presets/synova-squad-lead/SYSTEM-PROMPT.md:29`（`grep -c INJECTED-RED`=0）；
  事故前例 `docs/synova/audit-reports/2026-09-20-K3-D854.md:40`（D854 注入断言残留）。
- **交付三件**：`grep -c 'INJECTED-RED'` → `pre-commit-check.sh:0`、`brief_parser.py:0`、`task-scope-g12.test.sh:0`。
- **扩展扫描**（自验自选）：`RED-INJECT`/`FIXME`/`DEV-ONLY`/`/tmp/g12d911`/`/tmp/d911`/`echo DEBUG`/`set -x` → 三件均 **0**；
  唯一 `XXX` 命中为 `mktemp -d "${TMPDIR:-/tmp}/g12d911.XXXXXX"`（mktemp 模板，合法）。
- **第二判据（更重要）**：全部红夹具**重跑后已变绿**（§1/§3 修前 vs 修后），且反例仍红 → 证明不是"把门禁判红改成不判"。

---

## 八 Windows 兼容 / bash 3.2（抽查）

| 项 | 命令 | 结果 |
|---|---|---|
| 语法 | `bash -n`（两件 sh）、`ast.parse`（py） | OK |
| 注入值格式白名单 | `SYNO_G12_TODAY` 仅接受 `YYYY-MM-DD`（`case` 白名单，防注入进 python 源码） | 代码已核 |
| 脏值不静默采用 | 非白名单值 → ⚠️ 提示后忽略 | 代码已核 |
| CRLF/换行清洗 | 交付新增两处 `grep -c … \| tr -d '\n\r'`；`G12_COMMIT_DATE` 加 `tr -d '[:space:]'` | 已核（:892-893 等） |
| UTF-8 | 新测试头块 `PYTHONIOENCODING`/`LC_ALL`；`brief_parser.py` 已有 `sys.stdout.reconfigure` | 已核 |
| 三态退出码 | 非法/降级走显式 ⚠️ + 回退（契约允许），未新增静默 `exit 0` | 已核 |
| bash 3.2 | `declare -A` 命中 **0** | 已核 |
| 全角标点变量边界 | 交付新增行未见 `$VAR）` 形态（D370 坑） | 已核 |

---

## 九 新发现（**单列**，不计入"已发现"；每条给 文件:行 + 复现命令 + 原始输出）

| # | 发现 | 文件:行 | 复现命令 / 证据 |
|---|---|---|---|
| N1 | **派单件引用的 §十二 不存在**：`验收标准-穿真实入口-v1-20260918.md` 仅 121 行、止于 §十一；全 `docs/` 检索「证据与回执」只命中派单件自身引用 | 派单件 `D911-…-20260922.md:25` | `wc -l` = 121；`grep -rn '证据与回执' docs/` → 仅派单件 1 处（准备阶段已报 lead） |
| N2 | **C6 前提半错**：教内联写法的是 `CLAUDE.md:577` 与存量 **68/657** 份 brief；`task-start.sh` 全文无 `Done` 字串，其调用的 `generate-task-brief.py:137` 生成的是**块式** | `CLAUDE.md:577`、`scripts/workflow/generate-task-brief.py:137` | `grep -n 'Done' scripts/workflow/task-start.sh` → 空；存量分布 68 inline / 513 block |
| N3 | **交付分支落后 main 3 个提交**（`2ddb07ee`/`06242439`/`0bbfb97a`，D917 文档入库）；三个提交均**未碰**交付三文件 → 无冲突风险，但 PR 合并前须并入 main | — | `git rev-list --count origin/fix/D911-g12-writeset..origin/main` = 3；`git diff --stat <merge-base>..origin/main` 仅 docs/task-state |
| N4 | **新测试未登记 CI canary 列表** → GitHub 上**不会执行**该 38 断言（CI 绿≠该测试绿） | `.github/workflows/ci.yml:224-265` | `grep -c 'task-scope-g12' .github/workflows/ci.yml` = 0（→ L1） |
| N5 | **G12b 存量 fail-open**：`check-brief-parseable.sh` 缺失时输出不含 `❌` → 判 `✅ G12b: brief 可解析`（假绿） | `scripts/pre-commit-check.sh:1505-1510` | 自验首个沙箱未复制该脚本时实测：`bash: … No such file or directory` 却输出 `✅ G12b: brief 可解析`（→ L3） |
| N6 | **`resolve-commit-brief.sh` 仍以墙钟选 brief**（`TODAY=$(date +%Y-%m-%d)`），与 C2「判据不随运行日漂移」相邻但不在 C 切片范围；组 6 的 brief 选择仍与运行日耦合 | `scripts/workflow/resolve-commit-brief.sh:32,115` | `grep -n 'date +%Y-%m-%d' …` → :32、:115 |
| N7 | **`today-by-name.test.sh` 不在 CI canary 列表** → 其存量红长期不可见（这正是它自 D509/D516 起红而无人察觉的原因）；本交付已将其转绿 | `.github/workflows/ci.yml:224-265` | 该测试名不在 224-265 列表中 |

### 自验员自身更正（诚实披露，非交付问题）
- 准备阶段我曾报「`INJECTED-RED` 非既有约定、全域零命中」——**该结论错误**。原因是当时命令 `timeout 25 grep …` 中 `timeout` 在本机不存在，
  命令直接失败，而 `|| echo "（零命中）"` 把**命令失败**当成了**零命中**（"吞错当绿"的典型）。本轮已用无 `timeout` 的命令重跑并更正为：
  **`INJECTED-RED` 是既有约定**（出处见 §七），交付三件命中数仍为 0，结论不变但依据已改正。

---

## 十 停手/边界声明

- **未采信编码方输出**：全部夹具由自验员在 `/tmp` 重建（沙箱 `/tmp/d911v-sb`，独立于编码测试的 `/tmp/g12d911.*`）；命令自行编写、自行运行。
- **未改产品代码/测试/卡面**：本自验只写两件证据文件（写入前后 `git status --porcelain` 已核；跑新测试后复检 = 0）。
- **未跑**：vitest / 全量门禁之外的重型套件；重型验证全队串行，本轮单发串行执行（跑前 `pgrep` 核空，两次让行切片 A 的门禁运行）。
- **未代为修复**任何 §六 遗留项。
- **措辞纪律**：本报告未使用「审计通过 / 审计合格 / 已通过审计」；结论仅 `可提请独立审计`。

---

**自验员**：`d911-verifier`（task-2）｜**日期**：2026-09-22
**证据存档**：`docs/synova/product-lines/evidence/D911-evidence-20260922.md`（594 行，含全部原始输出）
