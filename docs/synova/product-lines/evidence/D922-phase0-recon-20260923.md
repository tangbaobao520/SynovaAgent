# D922 程序 W1 波 Phase 0 — 只读侦察 + 冲突扫描

> 侦察员: recon-w1（synova-squad-lead 麾下）｜ 2026-09-23
> 基线: **main@6c23006a**（`main` 工作树，HEAD 提交 `6c23006a1a0c3d93cca4b6ec762ad588c0874dd2`，2026-09-23 12:37:44 +0800，`Merge pull request #716 from tangbaobao520/fix/d919-citation-gate`）
> 性质: **只读侦察**。全程未改任何仓库文件（git add/commit/push/checkout/stash 零执行）；未跑任何重型验证（无 vitest / 无 tsc 全量 / 无门禁全量）。
> 本文所有数字均来自命令原始输出，无手写。
> 命令限定: 一律 `--exclude-dir=.synova-wt-* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git`，无裸 `find .`。

---

## 〇、基线与工作区状态（先行核对）

```bash
$ pwd && git -C /Users/wane/SynovaAgent rev-parse --short HEAD && git -C /Users/wane/SynovaAgent rev-parse --abbrev-ref HEAD
/Users/wane/SynovaAgent
6c23006a
main

$ git log -1 --format="%H%n%ad%n%s" --date=iso
6c23006a1a0c3d93cca4b6ec762ad588c0874dd2
2026-09-23 12:37:44 +0800
Merge pull request #716 from tangbaobao520/fix/d919-citation-gate

$ git status --porcelain
?? .claude/task-briefs/2026-09-23-D919-check-citations-gate.md
?? .claude/task-briefs/2026-09-23-D920-merge-pr-queue-tool.md
?? .claude/task-briefs/2026-09-23-D921-k3-audit-dispatch-d919-d920.md
?? .claude/task-briefs/2026-09-23-D922-program-double-dsh-improvement.md
?? .claude/task-briefs/2026-09-23-D923-w1-d1-k3-merge-gate.md
?? .claude/task-briefs/2026-09-23-D924-w1-b4-subprocess-protocol.md
?? .claude/task-briefs/2026-09-23-D925-w1-c2-deps-interface-standard.md
?? .claude/task-briefs/2026-09-23-D926-squad-discipline-skill.md
?? task-state/D922.json
?? task-state/D923.json
?? task-state/D924.json
?? task-state/D925.json
task-state/D926.json
```

**结论行 〇:** 基线 = main@6c23006a 确认。工作区 13 项未跟踪（全部是 D919–D926 的 brief + task-state，**无已跟踪文件被修改**）→ 侦察未污染工作区。D923/D924/D925/D926 四份 brief 均**未跟踪**。

---

# A) D923 落点 — K3 必经合并门禁 的现有基础设施

## A-1 候选脚本清单

```bash
$ ls scripts/control-tower/ | grep -iE "merge|audit|k3|citation"
audit-rules.json
check-citations.py
external-auditor.sh
merge-pr-queue.py
merge_writeset_gate.py
pre-audit-summary.sh
```

```bash
$ wc -l scripts/control-tower/merge_writeset_gate.py scripts/control-tower/merge-pr-queue.py scripts/control-tower/check-citations.py
     485 scripts/control-tower/merge_writeset_gate.py
     348 scripts/control-tower/merge-pr-queue.py
     275 scripts/control-tower/check-citations.py
    1108 total
```

### 三脚本头注释（各前 30 行）

```bash
$ for f in merge_writeset_gate.py merge-pr-queue.py check-citations.py; do echo "===== scripts/control-tower/$f (first 30 lines) ====="; sed -n '1,30p' "scripts/control-tower/$f"; done
===== scripts/control-tower/merge_writeset_gate.py (first 30 lines) =====
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
scripts/control-tower/merge_writeset_gate.py — D708 合并级写集对账 gate

背景（M2 族第三次止血）:
  #449 夹带 D603 共 51 文件 / #442 夹带 6 文件 / D593-FIX 声称提交实未提交 —— 三次
  「PR 声称的写集」与「真正进入 main 的文件集」不一致，而现有门禁都抓不到：
    - pre-commit 13 组：单提交粒度，只看暂存区，不看 PR 整体
    - verify-parallel.sh（ci.yml L51-57）：**已合 PR 之间**的写集重叠（inter-PR），
      不校验单个 PR 与**自己声明**的一致性（intra-PR）
  本 gate 补的正是 intra-PR 这一格：一个 PR 从「通过的写集」到「真正进入 main 的文件集」
  之间不许出现夹带；出现即红并逐文件点名。

与现有 gate 的边界（不重复造）:
  | gate | 比较对象 | 抓什么 |
  |---|---|---|
  | ci.yml L51-57 verify-parallel --ci-pr | 本 PR 变更集 × 已合 PR 写集 | 两个 PR 撞同一批文件 |
  | 本 gate | 本 PR 变更集 × **本 PR 自己的声明** | 本 PR 里混入未声明文件（夹带） |
  两者正交，触发点同为 PR job。

声明源与权威顺序（S1 > S2 > S3，多源存在时取**并集**）:
  S1 `task-state/<D#>.json` 的 `write_set` 数组        —— 结构化，机器可读
  S2 dev doc `docs/plans/codex/implementation/SYNOVA-IMPL-*<D#>*.md` §写集表 —— 复用 devdoc_writeset.py
  S3 task brief `.claude/task-briefs/*<D#>*.md` 的 Q2「做什么」  —— 复用 brief_parser.py
  取并集的理由：三者都是**作者自己的声明**；用交集会把「在 A 声明、在 B 未列」当成夹带，
  属对声明语义的误读。被哪个源收录会在输出里逐条打印（可审计）。

契约（铁律 47）:
  @input   --base <ref>（缺省 origin/main） --head <ref>（缺省 HEAD） --branch <name>
===== scripts/control-tower/merge-pr-queue.py (first 30 lines) =====
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""merge-pr-queue.py — 合并队列推进器（CTO 合并纪律机器化，D920）

来历: 2026-09-22 串行合并流水线 (/tmp/serialmerge.py) 在 PR #712 上卡死 22 轮（每轮 70s），
  根因两条（本次接手实测）:
  根因1 **工作树参数缺失/为空** → 脚本 cwd 落到主工作区 → 对 main 反复 `git merge origin/main`
          （永远 "Already up to date"）→ 误判「等 GitHub 重算」空转。日志物证: `#712 === 开始（工作树 ）===`。
  根因2 **DIRTY 判据混淆** —— GitHub `mergeable=false` 有两种成因：
          (a) 真冲突；(b) **合并性缓存过期**（base 停在旧 main；实测 #704：GitHub 判 dirty，
              本地 `git merge origin/main` rc=0 干净）。旧脚本对两者一律"等"，且不验证。
  修正:
    ① 工作树解析强校验 —— 缺失即 FAIL LOUD（绝不回落主工作区）；无现成工作树则显式新建并留痕。
    ② DIRTY 不再"等"：本地 `git merge-tree` 复验 —— 真冲突 → 报冲突文件清单并停；
       缓存过期 → 推一次 re-merge 强制 GitHub 重算（实测 #704 dirty→clean 生效）。
    ③ 失败 job 自动重跑（`rerun-failed-jobs`）—— 基线修复落地后让在途 PR 复绿的正路
       （PR check 跑在 merge ref 上，重跑即含新 main，无需改分支）。
    ④ 合并后立即同步主工作区（承重墙：创始人界面里的相对链接依赖它）。

契约 (铁律 47):
  @input  — --prs <n> [<n>...]（必填）；--repo <path>（默认 git root）
            --wait-merged <n>（可多次；先等这些 PR 合并 —— 基线修复未落地时推进别的是空转）
            --max-rounds <n>（每 PR 轮询上限，默认 60）/ --sleep <s>（默认 20）
            --dry-run（只分类不动作：不 merge / 不 push / 不 rerun）
            --fixture-classify <json> / --fixture-worktree <file>（密封测试注入缝，零网络）
  @output — 每 PR 一行状态日志（时间戳 + 状态 + 依据）；末行 `QUEUE-DONE all_ok=<bool>`
  @exit   — 0 = 全部目标达成（merged 或已 merged）；1 = 有目标未达成（冲突/失败/超时，明确列出）
            2 = 入参/环境错误（fail-closed：无 --prs / repo 非法 / fixture 不可读）
  @degraded — GitHub API 不可达 → 状态 ERR + 重试；超上限 → exit 1 并点名（不静默当作成功）
  @error  — 状态词汇: MERGED / READY / PEND / BAD / DIRTY_CONFLICT / DIRTY_STALE / UNKNOWN / CLOSED / ERR
===== scripts/control-tower/check-citations.py (first 30 lines) =====
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""check-citations.py — 引用可核验门禁（D919）

背景（创始人 2026-09-22 质问「我写了一份不存在的权威引用——你怎么敢做这种事情」）:
  上一任 CTO 的结论 = 借 Anthropic 的「机器可验契约」+ DSH 的 InvariantError 形态（违规从不匿名、
  声明在注册时校验），但**未落成机制**。本脚本即该结论的机制化。

既有缺口（D919 修复，逐条实测于 scripts/control-tower/pre-dispatch-check.sh ⑥）:
  - `head -25` 截断 → 第 26 条引用起永不校验（M1: 检查未执行 == 检查通过）
  - 扩展名白名单 `ts|cjs|mjs|sh|py|json|yml` 不含 .md/.html/.txt
    → 「docs/**.md:行号」这类**权威引用整体漏检**（上一任翻车正是该形态）
  - 无仓外根 → DSH 源码引用（如 dsh-subprocess-local/lib/index.js:757）无处解析 → 外部权威引用静默漏检
  - 无错误码 / 无责任方 → 违规不可归因

契约 (铁律 47):
  @input  — <artifact> [<artifact>...] 派单/交回件/规格等文本文件
            选项: --repo <path>            仓库根（默认 git rev-parse --show-toplevel）
                  --external-root <path>   仓外权威源根（可多次；如 DSH 安装目录）
                  --external-prefix <str>  外部包名前缀（可多次；默认 dsh-）
                  --owner <str>            责任方（写入每条违规，可归因；默认仓库目录名）
                  --json                   机器可读输出
                  --quiet                  仅输出汇总
  @output — 违规逐条（code / artifact / line / citation / owner / roots_tried）+ 汇总行
            （含「待建声明」计数：行内标注『新建』的交付物路径不计违规 —— 本仓既有约定，防误拦）
            `--json` → {"artifacts": [...], "violations": [...], "summary": {...}}
  @exit   — 0 = 全部引用可核验；1 = 存在不可核验引用（fail-closed 业务阻断）；
            2 = 检查本身失败（artifact 不可读 / 无有效输入 / repo 非法）
  @degraded — artifact 不存在或不可读 → exit 2 + stderr `degraded: ...`（绝不当作通过，铁律 11）
  @error  — code ∈ {CITE_FILE_NOT_FOUND, CITE_LINE_OUT_OF_RANGE, CITE_BAD_RANGE,
```

**结论行 A-1:** 三脚本**均已在 main**（`git ls-tree HEAD` 实测，见 A-6）。行数 485 / 348 / 275。三者均为「PR 级 / 队列级 / 引用级」工具，**均不读 K3 审计结论**。

## A-2 ci.yml 全量 job + 门禁脚本

```bash
$ wc -l .github/workflows/ci.yml
     331 .github/workflows/ci.yml

$ shasum -a 256 .github/workflows/ci.yml
6a5eacc43348c737ca7cec0a5ebc164f4271884b416d43623b2daa55a09e86fa  .github/workflows/ci.yml
```

```bash
$ grep -nE "^  [a-z-]+:|bash scripts/|python3 scripts/|python scripts/" .github/workflows/ci.yml
4:  push:
10:  quality:
84:        run: bash scripts/pre-commit-check.sh
95:          bash scripts/control-tower/verify-parallel.sh --ci-pr origin/main
102:          python3 scripts/control-tower/merge_writeset_gate.py \
105:  test:
163:  architecture:
174:        run: bash scripts/check-architecture.sh
178:  test-kit-architecture:
202:  control-tower-tests:
278:          bash scripts/control-tower/check-canary-drift.sh || true
281:  integration-check:
290:        run: bash scripts/workflow/check-integration.sh
292:  checker-review:
304:        run: bash scripts/checker-review.sh
306:        run: bash scripts/workflow/check-brief-vs-code.sh --strict || true
308:  audit:
319:  golden-case:
331:        run: bash scripts/ci/diagnosis-quality-check.sh
```

```bash
$ grep -nE "^on:|^jobs:|^  [a-zA-Z_-]+:$|^    name:" .github/workflows/ci.yml
3:on:
4:  push:
6:  pull_request:
9:jobs:
10:  quality:
11:    name: TypeScript + Lint + Iron Laws
105:  test:
106:    name: Vitest (${{ matrix.shard }})
163:  architecture:
164:    name: Architecture Check
178:  test-kit-architecture:
179:    name: Test-Kit Architecture Tests (${{ matrix.os }})
202:  control-tower-tests:
203:    name: Control Tower Gate Tests (${{ matrix.os }})
281:  integration-check:
282:    name: Integration Contract Check
292:  checker-review:
293:    name: Checker Review (maker/checker)
308:  audit:
309:    name: npm audit
319:  golden-case:
320:    name: Golden Case F1 Gate
```

### job → 门禁脚本 映射（逐 job 判定）

| # | job | 调用的门禁脚本 | 备注（原文摘） |
|---|---|---|---|
| 1 | `quality` (L10) | `scripts/pre-commit-check.sh`（L84） | `SYNO_CI: "1"` → CI strict 转硬阻断 |
| 1 | `quality` | `scripts/control-tower/verify-parallel.sh --ci-pr origin/main`（L95） | inter-PR 写集重叠 |
| 1 | `quality` | `scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD`（L102） | D708 intra-PR 夹带 |
| 1 | `quality` | `npx tsc --noEmit --pretty false`（L79，非脚本调用） | **白名单 grep -v 过滤 7 个历史文件** |
| 1 | `quality` | `scripts/ci/verify-dXXX.sh`（L55-78，动态回放） | 缺脚本 → `::warning` 显式 skip |
| 2 | `test` (L105) | 无脚本调用（vitest 分片矩阵） | `needs: quality` |
| 3 | `architecture` (L163) | `scripts/check-architecture.sh`（L174） | — |
| 4 | `test-kit-architecture` (L178) | 无脚本调用（独立 package） | `packages/test-kit` |
| 5 | `control-tower-tests` (L202) | **41 个测试**逐个 `bash "$t"`（L225-266 硬编码清单）+ `scripts/control-tower/check-canary-drift.sh \|\| true`（L278） | 双平台矩阵 ubuntu + windows |
| 6 | `integration-check` (L281) | `scripts/workflow/check-integration.sh`（L290） | — |
| 7 | `checker-review` (L292) | `scripts/checker-review.sh`（L304）+ `scripts/workflow/check-brief-vs-code.sh --strict \|\| true`（L306） | 后者 **fail-open** |
| 8 | `audit` (L308) | `npm audit --audit-level=high \|\| echo ...`（L317） | **依赖漏洞审计，非 K3 审计**；`continue-on-error: true`（L314） |
| 9 | `golden-case` (L319) | `npx tsx scripts/ci/golden-case-checker.ts`（L329）+ `scripts/ci/diagnosis-quality-check.sh`（L331） | `needs: test` |

**结论行 A-2:** ci.yml = **331 行**，sha256 `6a5eacc43348c737ca7cec0a5ebc164f4271884b416d43623b2daa55a09e86fa`。共 **9 个 job**。**9 个 job 中没有任何一个读取 K3 审计结论**（`audit` job 名为 "npm audit"，是依赖漏洞扫描器且 `continue-on-error: true`，与 K3 无关）。合并放行判据全部是「写集/类型/测试/架构/融合/golden-case」，**无一格是「审计结论」**。

## A-3 是否存在「K3 审计结论 → 合并放行」的现有机制

```bash
$ grep -rniE "k3|audit-report|verdict" .github/workflows/ scripts/control-tower/ --include=*.yml --include=*.sh --include=*.py 2>/dev/null | grep -v "^Binary"
.github/workflows/ci.yml:34:      # D703 (K3 W-1) 证据命令机器化: 非 docs-only PR 改动 dev-doc → 按文件名 D 号
.github/workflows/ci.yml:73:        # K3 审计 P1-1 修复：去掉 `|| true` fail-open（CI 类型检查永不红 → 挪 CI 后类型防线真空）。
.github/workflows/ci.yml:87:          SYNO_CI: "1"  # D516/K3 P0-1: CI strict——本地 V5 软提示在 CI 转硬（权威物理落地）
.github/workflows/ci.yml:180:    # D565 (K3 D489 批 P1, M3 家族): packages/test-kit 是独立 package（自带 lock，
.github/workflows/ci.yml:218:        # 全量密封化是单独立项（K3 P2-4）。此 job 是"门禁坏了能抓到"的 canary。
.github/workflows/ci.yml:219:      # D560（D549 重做，K3 FAIL 闭合）：补 P1-3 点名的 6 个密封测试入列（19→25）——
.github/workflows/ci.yml:220:      #   D540-D547 批产出（K3 报告 2026-08-28-D540-D547 §2 P1-3 具名）；D549 声称入列
.github/workflows/ci.yml:277:          #   存量漂移由 K3/人工月度清理，全量密封化是 K3 P2-4 单独立项）
.github/workflows/product-progress.yml:52:                --type ci --verdict pass --points "$POINTS" \
scripts/control-tower/check-pr-budget.sh:8:# 背景（派单 §二）: D721 一个 PR 背三类门禁问题挂半天；K3 审计分支落后 main
scripts/control-tower/check-pr-budget.sh:154:    printf '%s\n' "$DOMAIN_OUT" | grep -E '^(mac|win|k3|⚠️)' | head -12 | sed 's/^/       /'
scripts/control-tower/staging_guard.py:71:        # 支持已实现但零生产调用方（KIMI K3 审计: D329 dev doc §5 只要求"resolver
scripts/control-tower/check-canary-drift.sh:12:#   @exit   — 恒 0（告警不阻断——派单明确防误伤；漂移由 K3/人工月度清理）
scripts/control-tower/check-bypass-log.sh:77:#   如实注记（D561，K3 P1-D508——原注释声称「6+ 次补记循环根治」不实）:
scripts/control-tower/external-auditor.sh:29:REPORT_DIR="$PROJECT_ROOT/.codex/audit-reports"
scripts/control-tower/gen-cto-health.py:203:                既定语义链一环，不新增降级态；调用方 verdict 解析失败路径维持现状）
scripts/control-tower/gen-cto-health.py:238:      audit ← docs/synova/audit-reports/ 含 D# 的报告存在 → 解析 verdict
scripts/control-tower/gen-cto-health.py:281:    audit_dir = REPO / "docs" / "synova" / "audit-reports"
scripts/control-tower/gen-cto-health.py:325:                # D395a verdict 优先序不变: CONDITIONAL PASS > PASS > FAIL > ?
scripts/control-tower/gen-cto-health.py:393:def verdict(bypass: dict, fail: dict, m_recur: list) -> str:
scripts/control-tower/gen-cto-health.py:408:    v = verdict(bypass, fail, fail_ledger["m_recur"])
scripts/control-tower/baseline-check.sh:259:  echo "{\"verdict\":\"$([ \"$BLOCKED\" -eq 1 ] && echo block || echo pass)\",\"baselines\":[${JSON_PARTS%,}]}"
scripts/control-tower/alloc-task-id.sh:228:- 不改 scripts/audit/（K3 红线）
scripts/control-tower/founder-truth.py:293:    lines += ["", "点开 founder-console.html 看证据；K3 可复核：git log --all | grep \"(D#)\"。"]
scripts/control-tower/verify-parallel.sh:191:#   2) docs/synova/audit-reports/ 含 *-<D#>[-.md]（历史任务无 task-state，D393 派生制同源信号）
scripts/control-tower/verify-parallel.sh:208:  if ls "$REPO_DIR"/docs/synova/audit-reports/*-"$did"-*.md >/dev/null 2>&1; then return 0; fi  # swallow-ok: 无匹配=不豁免，glob 失败非错误
scripts/control-tower/verify-parallel.sh:209:  if ls "$REPO_DIR"/docs/synova/audit-reports/*-"$did".md >/dev/null 2>&1; then return 0; fi    # swallow-ok: 同上
scripts/control-tower/verify-claims-table.sh:5:# 背景 (M2, K3 降本前提): 交付方声称没有绑定可执行证据，K3 只能当侦探重新查。
scripts/control-tower/pre-audit-summary.sh:5:# 背景 (U8, K3 降本前提): K3 成本高的根因 = 把机器 1 秒能查的物理项
scripts/control-tower/pre-audit-summary.sh:8:#   输出"预审是否已过"——没过直接打回，不浪费 K3 语义大脑。
scripts/control-tower/pre-audit-summary.sh:11:#   风险分级表是"建议"（采纳权在创始人 + K3），本脚本只读取并展示，不裁决。
scripts/control-tower/pre-audit-summary.sh:22:# 退出码: 0 = 机器预审全过(可进 K3 语义终审); 1 = 有机器可查项未过(打回);
scripts/control-tower/pre-audit-summary.sh:117:  low) SUGGEST="低风险 → 机器预审 + K3 抽查" ;;
scripts/control-tower/pre-audit-summary.sh:118:  high) SUGGEST="高风险 → 机器预审 + K3 全量" ;;
scripts/control-tower/pre-audit-summary.sh:119:  medium) SUGGEST="中风险 → 机器预审 + K3 抽查(偏全量)" ;;
scripts/control-tower/pre-audit-summary.sh:132:verdict = "fail" if fail_n > 0 else ("degraded" if (degraded_n + missing_n) > 0 else "pass")
scripts/control-tower/pre-audit-summary.sh:133:print(json.dumps({"component": "pre-audit-summary", "verdict": verdict,
scripts/control-tower/pre-audit-summary.sh:140:echo "── 机器预审汇总（U8 第0层, K3 语义终审前物理预检）──"
scripts/control-tower/pre-audit-summary.sh:159:  echo "❌ 机器预审未过: $FAIL_N 项未过 — 打回，不浪费 K3 语义大脑"
scripts/control-tower/pre-audit-summary.sh:165:echo "✅ 机器预审全过 ($PASS_N 项) — 可进 K3 语义终审"
scripts/control-tower/gen-plan-status.py:6:#       本脚本用 task-state/D*.json（真相源）+ audit.verdict 重写该区块 →
scripts/control-tower/gen-plan-status.py:37:        rows.append((i, d.get("status") or "", a.get("verdict") or "", (d.get("title") or "")[:36]))
scripts/control-tower/check-ownership.py:15:            选项 --owner {mac|win|k3}  断言每个文件归属该 owner（越域 → exit 1）
scripts/control-tower/check-ownership.py:163:        "# （兜底 = 创始人最终把关）。建议建三个团队 @synova-dsh / @synova-claude / @synova-k3，",
scripts/control-tower/check-ownership.py:188:    ap.add_argument("--owner", choices=["mac", "win", "k3"], default=None, help="声明 owner；逐文件断言归属")
scripts/control-tower/check-ownership.py:201:        _die("未给出待校验文件（用法: check-ownership.py <文件...> [--owner mac|win|k3]）")
scripts/control-tower/incident-loop.py:65:# D561 (K3 P1-D535): POSIX 平台 bash 恒存于标准路径（macOS /bin/bash）——
```

```bash
$ grep -rniE "k3|audit-report|verdict" .github/workflows/ scripts/control-tower/ --include=*.yml --include=*.sh --include=*.py 2>/dev/null | grep -v "^Binary" | wc -l
      47

$ grep -rnicE "k3|audit-report|verdict" .github/workflows/ scripts/control-tower/ --include=*.yml --include=*.sh --include=*.py 2>/dev/null | grep -v ":0$"
.github/workflows/ci.yml:8
.github/workflows/product-progress.yml:1
scripts/control-tower/check-pr-budget.sh:2
scripts/control-tower/staging_guard.py:1
scripts/control-tower/check-canary-drift.sh:1
scripts/control-tower/check-bypass-log.sh:1
scripts/control-tower/external-auditor.sh:1
scripts/control-tower/gen-cto-health.py:6
scripts/control-tower/baseline-check.sh:1
scripts/control-tower/alloc-task-id.sh:1
scripts/control-tower/founder-truth.py:1
scripts/control-tower/verify-parallel.sh:3
scripts/control-tower/verify-claims-table.sh:1
scripts/control-tower/pre-audit-summary.sh:12
scripts/control-tower/gen-plan-status.py:2
scripts/control-tower/check-ownership.py:4
scripts/control-tower/incident-loop.py:1
```

### 逐处判定（47 处，按文件归并）

| 出处 | 处数 | 判定 |
|---|---|---|
| `ci.yml:34,73,87,180,218,219,220,277` | 8 | **只是注释** —— 引用历史上的 K3 审计发现（"K3 审计 P1-1 修复"等），无一行读审计结论做放行 |
| `product-progress.yml:52` | 1 | **只是簿记** —— 向进度系统上报 `--type ci --verdict pass`（CI 结果上报，非 K3 结论） |
| `check-pr-budget.sh:8,154` | 2 | 注释 + 把 `k3` 当 owner 名过滤显示（**簿记**） |
| `staging_guard.py:71` | 1 | **只是注释**（引 K3 审计历史） |
| `check-canary-drift.sh:12` | 1 | **只是注释** —— `@exit 恒 0（告警不阻断）`（**反门禁**：明确不阻断） |
| `check-bypass-log.sh:77` | 1 | **只是注释** |
| `external-auditor.sh:29` | 1 | **簿记** —— `REPORT_DIR=".codex/audit-reports"`（旧路径；输出目录，非门禁） |
| `gen-cto-health.py:203,238,281,325,393,408` | 6 | **只是簿记/展示** —— 读 `docs/synova/audit-reports/` 解析 verdict 用于**仪表盘渲染**（L325 注释自陈口径 `CONDITIONAL PASS > PASS > FAIL > ?`）；`verdict()` 函数算的是 bypass/fail 聚合，不是 K3 结论 |
| `baseline-check.sh:259` | 1 | **簿记** —— 输出 `{"verdict":"pass\|block"}` 给基线对比，非 K3 |
| `alloc-task-id.sh:228` | 1 | **只是注释**（"不改 scripts/audit/（K3 红线）"） |
| `founder-truth.py:293` | 1 | **只是簿记**（渲染文案） |
| `verify-parallel.sh:191,208,209` | 3 | **是被当作「任务已关闭」豁免信号** —— `docs/synova/audit-reports/` 存在即豁免写集重叠比对（L191 注释: "关闭信号"）。**方向与门禁相反：有 K3 报告 = 放宽，不是收紧** |
| `verify-claims-table.sh:5` | 1 | **只是注释** |
| `pre-audit-summary.sh:5,8,11,22,117,118,119,132,133,140,159,165` | 12 | **是机器预审聚合器**（5 门禁 + verdict pass/fail/degraded + exit 0/1/2）—— 但**未被任何门禁/CI/hook 调用**（见 A-3-b） |
| `gen-plan-status.py:6,37` | 2 | **簿记** —— 读 task-state 的 `audit.verdict` 重写计划状态区块（展示） |
| `check-ownership.py:15,163,188,201` | 4 | **域校验** —— 但这里的 `k3` 是 **owner 名**（`--owner mac\|win\|k3` 三团队之一），与「K3 审计结论」无关 |
| `incident-loop.py:65` | 1 | **只是注释** |

### A-3-b pre-audit-summary.sh 接线实测

```bash
$ grep -rn "pre-audit-summary" . --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.synova-wt-\* --exclude-dir=.sessions 2>/dev/null
（去掉 .ci*.log 与 docs/briefs 历史引用后，代码侧仅下列）
./tests/control-tower/pre-audit-summary.test.sh:6:# pre-audit-summary.test.sh — U8 机器预审汇总测试
./tests/control-tower/pre-audit-summary.test.sh:17:TOOL="$REPO/scripts/control-tower/pre-audit-summary.sh"
./tests/control-tower/pre-audit-summary.test.sh:22:echo "=== U8 pre-audit-summary 测试 ==="
./tests/control-tower/pre-audit-summary.test.sh:50:if echo "$JSON" | python3 -c "...assert d['component']=='pre-audit-summary'..." 2>/dev/null; then
./scripts/control-tower/pre-audit-summary.sh:3:# pre-audit-summary.sh — U8 工程侧: 机器预审汇总（三层审计模型 第0层）
./scripts/control-tower/pre-audit-summary.sh:21:# 用法: pre-audit-summary.sh [--task-id <D#>] [--json]
./scripts/control-tower/pre-audit-summary.sh:133:print(json.dumps({"component": "pre-audit-summary", "verdict": verdict,

$ grep -c "pre-audit-summary" .github/workflows/ci.yml
0
$ grep -c "pre-audit-summary" scripts/pre-push-check.sh
0
$ grep -c "pre-audit-summary" .claude/settings.json
0
```

正向对照（证明扫描器有效，非零命中假阴性）：

```bash
$ grep -rn "external-auditor.sh" . --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.synova-wt-\* --exclude-dir=.sessions 2>/dev/null | wc -l
      88
```

`pre-audit-summary.sh` 聚合的 5 道门禁（原文 L43-51）：

```bash
$ sed -n '42,51p' scripts/control-tower/pre-audit-summary.sh
# ── 门禁清单: name | script(相对 REPO) | args | 描述 ──
GATES=(
  # U1: check-bypass-log.sh 是 U1 实际落地的对账脚本（spec 契约里拟新建的对账脚本未单独建，U1 改为修复存量脚本）
  #     显式 base=origin/main（原默认 origin/feat/prompt-architecture 已过时）；run_gate 已注入 GIT_SSH_COMMAND 防 SSH hang
  "U1-bypass-reconcile|scripts/control-tower/check-bypass-log.sh|origin/main|绕过证据链对账"
  "U2-writeset-reconcile|scripts/workflow/check-dev-doc-write-set.sh||dev doc 写集双向对账"
  "U3-artifact-repro|scripts/control-tower/gen-cto-health.py|--strict|生成器产物可复现"
  "U4-claims-table|scripts/control-tower/verify-claims-table.sh|@DEV_DOCS|声称↔证据自证表"
  "U7-ct-test-gate|scripts/control-tower/ct-test-gate.sh||控制塔脚本测试门禁"
)
```

**结论行 A-3:**
1. **「K3 审计结论 → 合并放行」机制：不存在。** 47 处命中经逐处判定 = **注释 15 处 / 簿记展示 22 处 / 域校验(owner 名) 4 处 / 反向豁免 3 处 / 未接线聚合器 12 处**，「真门禁」**0 处**。
2. 唯一**方向相反**的现存耦合：`verify-parallel.sh:191,198-209` 把「`docs/synova/audit-reports/` 里有该 D# 的报告」当作**写集豁免信号**（有报告 → 放宽比对），而非放行前置条件。
3. 最接近的现存基础设施 = **`pre-audit-summary.sh`（166 行，5 门禁聚合 + verdict 三态 + exit 0/1/2）**，但它在 `ci.yml` / `pre-push-check.sh` / `.claude/settings.json` 中引用数 **全为 0**（仅自己的测试引用）→ **属「机制建成未接线」**（M3 同族）。D923 若从零建，需自行回答「为何不复用/接线它」。
4. verdict 的现存**唯一机器可读约定**是 `gen-cto-health.py:326-330` 对报告正文做**子串匹配**（`"CONDITIONAL PASS" in txt` → `"PASS" in txt` → `"FAIL" in txt`），**无 YAML/JSON 结构化 verdict 字段**。

## A-4 三类路径（安全 / 财务计算 / 数据迁移）可被机器识别的证据

```bash
$ grep -rniE "security|finance|financial|migration|data-migration" scripts/control-tower/*.py scripts/control-tower/*.sh .github/workflows/*.yml 2>/dev/null
scripts/control-tower/check-citations.py:68:                 "expert/", "knowledge/", "theory/", "skills/", "security/", "providers/")
```

```bash
$ grep -rniE "security|finance|financial|migration|data-migration" scripts/control-tower/*.py scripts/control-tower/*.sh .github/workflows/*.yml 2>/dev/null | wc -l
       1

$ grep -rnicE "security|finance|financial|migration|data-migration" scripts/control-tower/*.py scripts/control-tower/*.sh .github/workflows/*.yml 2>/dev/null | grep -v ":0$"
scripts/control-tower/check-citations.py:1
```

### 是否存在「按 PR 改动路径分类」的分类器（ownership 之外）？

```bash
$ grep -rniE "category|categor" scripts/control-tower/*.py scripts/control-tower/*.sh .github/workflows/*.yml 2>/dev/null
scripts/control-tower/external-auditor.sh:150:FINDINGS=()  # 每行: P0|P1|P2|category|file:line|message
scripts/control-tower/external-auditor.sh:155:  local severity="$1" category="$2" location="$3" message="$4"
scripts/control-tower/external-auditor.sh:156:  FINDINGS+=("$severity|$category|$location|$message")
scripts/control-tower/external-auditor.sh:321:    echo -n '    {"severity":"'"$sev"'","category":"'"$cat"'","location":"'"$loc"'","message":"'"$msg"'"}'
.github/workflows/ci.yml:261:            tests/doc-system/doc-categories.test.sh \
（共 5 处）
```

唯一现存的路径→归属映射（`ownership.yaml`，glob→owner，**非** path→category）：

```bash
$ grep -nE "glob|owner" docs/synova/coordination/ownership.yaml | head -30
16:# owner 语义（人读；机器只用 key）
17:owners:
22:# CODEOWNERS 落地账号（三团队建好后只改这里 + 重跑 --emit-codeowners）
35:  - glob: "**"
36:    owner: "win"
41:  - glob: "extensions/**"
42:    owner: "win"
44:  - glob: "packages/**"
45:    owner: "win"
47:  - glob: "synova_worker/**"
48:    owner: "win"
50:  - glob: "docs/plans/**"
51:    owner: "win"
55:  - glob: "src/sentinel/**"
56:    owner: "mac"
58:  - glob: "tests/sentinel/**"
59:    owner: mac
61:  - glob: "src/cron/**"
62:    owner: "mac"
64:  - glob: "src/mcp/**"
65:    owner: "mac"
67:  - glob: "src/agent/sentinel-service*"
68:    owner: "mac"

$ grep -rniE "security|finance|migration" docs/synova/coordination/ownership.yaml 2>/dev/null | wc -l
       0
```

`check-ownership.py` 的匹配机制（fnmatch + 最后匹配者胜出，**只判 owner 域**）：

```bash
$ grep -nE "fnmatch|glob_match|最后匹配|def resolve" scripts/control-tower/check-ownership.py
111:def glob_match(glob: str, path: str) -> bool:
118:    if glob == "**":
120:    if glob.endswith("/**"):
122:        return path == prefix or path.startswith(prefix + "/")
123:    return fnmatch.fnmatchcase(path, glob)
135:    """按「最后匹配者胜出」解析单文件归属（与 .github/CODEOWNERS 官方语义一致）。
```

**结论行 A-4:** 三类路径（安全/财务计算/数据迁移）在控制塔与 CI 中**无任何机器可识别证据**——关键词全域仅 **1 处命中**，且该处是 `check-citations.py:68` 的**搜索根白名单**（把 `security/` 当目录扫描根），不是分类判据。**现存分类器只有一种：`ownership.yaml` 的 glob→owner（mac/win/k3 三域），且 `ownership.yaml` 内 security/finance/migration 命中 = 0。** 即：D923 若要「按 PR 改动路径分类」，**分类器须从零建**（无 path→category 映射可复用）。

## A-5 冲突扫描（D923 写集）

```bash
$ grep -rn "k3-merge-gate\|merge-gate" scripts/ tests/ .github/ docs/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null
（无输出）

$ echo "EXIT=$?"
EXIT=1

$ grep -rn "k3-merge-gate\|merge-gate" scripts/ tests/ .github/ docs/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l
       0
```

零命中 → 按坑清单「零命中结论必须先验证扫描器能命中已知阳性样本」，跑正向对照：

```bash
$ grep -rn "merge_writeset_gate" scripts/ tests/ .github/ docs/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l
      45
$ grep -rni "k3" scripts/ tests/ .github/ docs/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l
    4224
$ for p in "merge-gate" "merge_gate" "k3-merge" "K3Merge" "k3_merge"; do printf '%s => ' "$p"; grep -rn "$p" scripts/ tests/ .github/ docs/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l; done
merge-gate =>        0
merge_gate =>        0
k3-merge =>        0
K3Merge =>        0
k3_merge =>        0
```

测试目录现状：

```bash
$ ls tests/control-tower/ | head -60
__init__.py
alloc-task-id-lock.test.sh
alloc-task-id.test.sh
attach.test.sh
backup-db.test.sh
ban-stash.test.sh
baseline-check.test.sh
baseline-exemption.test.sh
brief-parseable.test.sh
brief-parser-strip.test.sh
brief-template-decision.test.sh
brief_parser.test.sh
bypass-ledger.test.sh
bypass-union-merge.test.sh
calc-k3-stale.test.py
check-bypass-log.test.sh
check-canary-drift.test.sh
check-ci-stale-red.test.sh
check-citations.test.sh
check-dataflow-alignment.test.sh
check-dev-doc-write-set.test.sh
check-gitlinks.test.sh
check-notes-lifecycle.test.sh
check-orphan-worktrees.test.sh
check-ownership.test.sh
check-pr-budget.test.sh
check-progress-freshness.test.sh
check-secrets.test.sh
check-sentinel-type-net.test.sh
check-test-first.test.sh
ci-ratchet-base.test.sh
ci-strict-mode.test.sh
ci-strict-visible.test.sh
claim-regex-narrow.test.sh
clone-config-init.test.sh
clone-shadow-commit.test.sh
commit-msg-consistency.test.sh
commit-msg-merge.test.sh
commit-msg-note-mandatory.test.sh
ct-test-gate.test.sh
daemon-smoke.test.sh
dashboard-alignment.test.py
decide-next.test.sh
declare-write-set.test.sh
dev-doc-gatekeeper.test.sh
doc-commit-exempt.test.sh
enterprise-fact-chain.test.ts
enterprise-facts.test.ts
external-auditor.test.sh
fastlane-bypass-only.test.sh
fastlane-extended.test.sh
founder-truth.test.sh
g12-day-window.test.sh
g12-taskstate-exempt.test.sh
gate-stats.test.sh
gen-cto-health-batch-report.test.py
gen-cto-health-repro.test.sh
gen-cto-health.test.sh
gen-plan-status.test.py
gen-task-board.test.py

$ ls tests/control-tower/ | wc -l
     122
```

命名形态分布：

```bash
$ printf '.test.sh = %s\n' "$(ls tests/control-tower/*.test.sh 2>/dev/null | wc -l)"; printf '.test.py = %s\n' "$(ls tests/control-tower/*.test.py 2>/dev/null | wc -l)"; printf '.test.ts = %s\n' "$(ls tests/control-tower/*.test.ts 2>/dev/null | wc -l)"; printf '其他      = %s\n' "$(ls tests/control-tower/ | grep -vE '\.test\.(sh|py|ts)$' | wc -l)"
.test.sh = 102
.test.py = 7
.test.ts = 3
其他      = 10

$ ls tests/control-tower/ | grep -vE '\.test\.(sh|py|ts)$'
__init__.py
test-dev-doc-gatekeeper.py
test-session-registry.py
test-staging-guard.py
test-wait-manager.py
test-write-lock.py
test_control_tower_truthfulness.py
test_env_validator.py
test_product_health.py
test_views_45.py
```

**结论行 A-5:** `k3-merge-gate` / `merge-gate`（及 4 种变体拼写）在 `scripts/ tests/ .github/ docs/` 全域 **共 0 处**（扫描器经 45 + 4224 正向对照证明有效）→ **D923 命名零冲突，无存量占用**。`tests/control-tower/` 共 **122 个文件**（102 `.test.sh` + 7 `.test.py` + 3 `.test.ts` + 10 其他）；新增 D923 测试按惯例应为 `tests/control-tower/<name>.test.sh`。

## A-6 D923 前置条件实测（裁定件 §三 明文前置）

裁定件 `docs/synova/coordination/裁定-双DSH提升清单-20260922.md:58-60` 明文：

> **P0-5（K3 升为必经门禁）** 的**前置不是改协议，是让报告物理入库**：K3 有 5 份报告（D821 / D858复审 / D862 / D864复审 / D865复审）**至今卡在分支上没进 main**（CTO 已开 #712/#713）。**门禁读不到结论就无法闭合** → 先做「报告必入库 + verdict 回填」这个物理环节，再谈门禁化。

实测该前置的当前状态：

```bash
$ for f in 2026-09-21-D821.md 2026-09-21-K3-D858-复审.md 2026-09-21-K3-D862.md 2026-09-21-K3-D864-复审.md 2026-09-21-K3-D865-复审.md; do printf '%-32s ' "$f"; git ls-tree -r --name-only HEAD -- "docs/synova/audit-reports/$f" | grep -q . && echo "IN MAIN" || echo "NOT IN MAIN"; done
2026-09-21-D821.md               IN MAIN
2026-09-21-K3-D858-复审.md     IN MAIN
2026-09-21-K3-D862.md            IN MAIN
2026-09-21-K3-D864-复审.md     IN MAIN
2026-09-21-K3-D865-复审.md     IN MAIN

$ git ls-tree -r --name-only HEAD -- docs/synova/audit-reports/ | wc -l
     140
```

verdict 回填（`task-state` 状态机）：

```bash
$ for d in D821 D858 D862 D864 D865; do printf '%-6s ' "$d"; if [ -f "task-state/$d.json" ]; then grep -oE '"status"[[:space:]]*:[[:space:]]*"[a-z]+"' "task-state/$d.json" | head -1; else echo "(无 task-state)"; fi; done
D821   "status": "audited"
D858   "status": "audited"
D862   "status": "claimed"
D864   "status": "claimed"
D865   "status": "audited"

$ grep -rl '"status"[[:space:]]*:[[:space:]]*"audited"' task-state/*.json 2>/dev/null | wc -l
     119
$ ls task-state/*.json 2>/dev/null | wc -l
     347
```

报告正文的 verdict 形态样例（**散文式，非结构化字段**）：

```bash
$ sed -n '1,8p' docs/synova/audit-reports/2026-09-21-D821.md
# K3 独立审计报告 — D821 出站 HTTP 唯一出口（P-1）

> 审计员：K3 独立会话（零上下文）｜ 2026-09-21
> 对象：PR #693 ｜ 分支 `feat/d821-http-proxy` ｜ head `717d67a1` ｜ 基线 `cc4e4166`
> 运行环境：macOS / node v22.22.0 / vitest（审计工作树 /tmp/d821-audit，已复现后清除）
> 结论三态判定：**PASS（附 1 条 P1 收尾跟进项）** —— P-1 口径维持**『部分关闭』**，D860 预算口径下 #693 在 D860 前不合并。
```

**结论行 A-6（相对裁定件有进展，前置**部分**满足）:** 5 份报告**全部已在 main**（`git ls-tree HEAD` 实测，审计报告目录 tracked 总数 **140**）；verdict 回填 **3/5**（D821/D858/D865 = `audited`；**D862/D864 仍为 `claimed`**，未回填）。即裁定件所写「5 份报告至今卡在分支上没进 main」**与实测不符（已入库）**；但「verdict 回填」尚有 **2 份未闭合**。报告 verdict **无结构化字段**，只有正文散文 + 子串匹配。

---

# B) D924 落点 — 子进程输出协议（标记字符串分割混合流）

## B-1 活证据：`scripts/golden-scenarios/common/assert.ts:95-115` 原文

```bash
$ read scripts/golden-scenarios/common/assert.ts (offset=95, limit=21)
```

```ts
95: }
96: 
97: // ─── check 执行器（三态: pass/fail/error） ───
98: function runHttp(check: Record<string, unknown>): { status: number; body: string } {
99:   // 同步执行: 用子进程 curl 保证零异步依赖（Windows Git Bash 自带 curl）
100:   const url = String(check.url);
101:   const r = childProcess.spawnSync('curl', ['-sS', '-w', '\n__STATUS__:%{http_code}', url], {
102:     encoding: 'utf-8', timeout: Number(check.timeoutMs || 30000),
103:   });
104:   if (r.error) {
105:     const err = new Error(`HTTP 请求失败: ${r.error.message}`);
106:     (err as Error & { code: string }).code = 'ASSERT_HTTP_ERROR';
107:     throw err;
108:   }
109:   const parts = r.stdout.split('__STATUS__:');
110:   return { status: Number(parts[1]?.trim() || 0), body: parts[0] || '' };
111: }
112: 
113: function runSqlite(check: Record<string, unknown>): { rows: number; firstRow: Record<string, unknown> | null } {
114:   const Database = require('better-sqlite3');
115:   const db = new Database(String(check.db), { readonly: true });
```

**结论行 B-1:** 活证据确认，**与裁定件 §二 描述逐字一致**（`spawnSync('curl', ['-sS','-w','\n__STATUS__:%{http_code}', url])` @L101 + `stdout.split('__STATUS__:')` @L109）。这是**单个 stdout 流内混合「body + 状态码」再字符串分割**的形态；脆弱点在于 body 若含 `__STATUS__:` 字面量即错位，且 `parts[1]` 缺失时静默 `Number(|| 0)` → 状态码 0（非降级、非抛错）。

## B-2 全仓存量扫描（主树）

### 扫描①：`split('__`

```bash
$ grep -rnE "split\(['\"]__" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null
scripts/golden-scenarios/common/assert.ts:109:  const parts = r.stdout.split('__STATUS__:');
```

```bash
$ grep -rnE "split\(['\"]__" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null | wc -l
       1
```

### 扫描②：标记字符串字面量（全文贴，禁截断）

```bash
$ grep -rnE "__STATUS__|__END__|__EOF__|__MARKER__|__SEP__|<<<|---MARKER" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null
scripts/check-verifiable-done.sh:48:done <<< "$(echo "$DONE_SECTION" | grep -E '^\s*- \[x\]')"
scripts/check-brief-vs-code.sh:183:  done <<< "$LISTED_MODULES"
scripts/check-brief-vs-code.sh:206:  done <<< "$MEMORY_REFS"
scripts/check-brief-vs-code.sh:240:  done <<< "$EXCLUDED"
scripts/check-brief-vs-code.sh:268:  done <<< "$ENTRY_FILES"
scripts/pre-commit-check.sh:207:  done <<< "$matches"
scripts/pre-commit-check.sh:547:      done <<< "$CATCHES"
scripts/pre-commit-check.sh:549:  done <<< "$STAGED"
scripts/pre-commit-check.sh:577:  done <<< "$NEW_IMPL"
scripts/pre-commit-check.sh:671:  done <<< "$NEW_IMPL"
scripts/pre-commit-check.sh:765:  done <<< "$SHELL_PKGS"
scripts/pre-commit-check.sh:1146:      done <<< "$CRITERIA_GLOBS"
scripts/pre-commit-check.sh:1275:  done <<< "$ALL_TODAY_BRIEFS"
scripts/pre-commit-check.sh:1343:done <<< "$(git diff --cached --name-status 2>/dev/null)"
scripts/pre-commit-check.sh:1487:    done <<< "$_PLAT_NEW"
scripts/doc-system/doc-triage.sh:20:#        ⚠️ 禁止用 here-string（<<<）传递大输出——MSYS bash 下会卡死。
scripts/check-architecture.sh:123: ' <<< "$2" || true
scripts/ci/verify-doc.sh:184:done <<< "$CMDS"
scripts/checks/check-test-quality.sh:76:      done <<< "$test_lines"
scripts/checks/check-test-quality.sh:83:  done <<< "$EXPORTS"
scripts/checks/check-test-quality.sh:89:done <<< "$NEW_IMPL"
scripts/golden-scenarios/common/assert.ts:101:  const r = childProcess.spawnSync('curl', ['-sS', '-w', '\n__STATUS__:%{http_code}', url], {
scripts/golden-scenarios/common/assert.ts:109:  const parts = r.stdout.split('__STATUS__:');
scripts/control-tower/check-canary-drift.sh:54:done <<< "$ALL"
scripts/control-tower/check-canary-drift.sh:61:done <<< "$LISTED"
scripts/control-tower/simulate-ci.sh:64:  done <<< "$TESTS"
scripts/control-tower/external-auditor.sh:83:      done <<< "$MATCHES"
scripts/control-tower/check-sentinel-type-net.sh:78:done <<< "$ACTIVE_SENTINELS"
scripts/control-tower/install-dsh-preset.sh:78:  done <<< "$PRESET_REGISTRY"
scripts/control-tower/verify-parallel.sh:108:      done <<< "$_files"
scripts/control-tower/verify-parallel.sh:323:  done <<< "$DOCS"
scripts/control-tower/verify-parallel.sh:383:  done <<< "$PR_DOCS"
scripts/control-tower/pre-audit-summary.sh:94:  IFS='|' read -r gname gscript gargs gdesc <<< "$gate"
scripts/control-tower/pre-audit-summary.sh:143:  IFS='|' read -r gname gcode gdesc <<< "$r"
scripts/control-tower/ct-test-gate.sh:58:done <<< "$CT_SCRIPTS"
scripts/control-tower/check-notes-lifecycle.sh:98:  done <<< "$ZOMBIES"
scripts/anthropic-decide.sh:94:  done <<< "$RAW_CATCHES"
scripts/check-secrets.sh:76:  done <<< "$FULL_SCAN"
scripts/check-plan-integrity.sh:123:      done <<< "$EXCLUDED"
scripts/check-plan-integrity.sh:148:      done <<< "$EXCLUDED_FILES"
scripts/hooks/hook-check-memory.sh:58:  done <<< "$KEYWORDS"
scripts/hooks/hook-check-memory.sh:94:done <<< "$MATCHED_MEMORIES"
scripts/hooks/hook-check-memory.sh:162:done <<< "$MATCHED_MEMORIES"
scripts/workflow/check-brief-vs-code.sh:89:   done <<< "$ACTUAL_FILES"
scripts/workflow/check-brief-vs-code.sh:98:   done <<< "$DECLARED_FILES"
scripts/workflow/check-brief-vs-code.sh:175:   done <<< "$VERIFY_CMDS"
scripts/workflow/check-brief-vs-code.sh:202:   done <<< "$NO_GO_ZONES"
scripts/workflow/check-dataflow-alignment.sh:48:done <<< "$KEYWORDS"
scripts/workflow/check-test-first.sh:44:done <<< "$STAGED_NEW"
scripts/workflow/verify-incremental.sh:147:  done <<< "$CHANGED_SRC"
scripts/workflow/verify-incremental.sh:182:  done <<< "$NEW_FILES"
scripts/workflow/verify-incremental.sh:215:    done <<< "$NEW_CATCHES"
scripts/workflow/check-integration.sh:70:      grep -qxF "$e" <<< "$REG_EDGES" 2>/dev/null || { fail "Skill $name 引用了未注册边: $e"; serr=$((serr+1)); }
scripts/workflow/check-integration.sh:89:      grep -qxF "$e" <<< "$REG_PLAYBOOKS" 2>/dev/null || { fail "Playbook $name 引用了未注册边: $e"; perr=$((perr+1)); }
scripts/workflow/check-integration.sh:90:    done <<< "$edges"
scripts/workflow/check-integration.sh:106:      grep -qxF "$c" <<< "$REG_COMPUTES" 2>/dev/null || { fail "Skill $name 引用了未注册 compute: $c"; cerr=$((cerr+1)); }
scripts/workflow/check-integration.sh:118:      grep -qxF "$s" <<< "$REG_SENTINELS" 2>/dev/null || { warn "Skill $name 引用了未注册哨兵: $s"; serr=$((serr+1)); }
scripts/workflow/check-integration.sh:137:      grep -qxF "$sk" <<< "$REG_SKILLS" 2>/dev/null || { warn "Playbook $name 引用了未注册 Skill: $sk"; pserr=$((pserr+1)); }
scripts/workflow/check-integration.sh:138:    done <<< "$skills"
scripts/workflow/check-silent-swallow.sh:136:      done <<< "$DIFF_LINES"
scripts/workflow/check-silent-swallow.sh:138:  done <<< "$SH_FILES"
scripts/workflow/hook-check-task-scope.sh:152:  done <<< "$SCOPE_PATHS"
scripts/workflow/hook-check-task-scope.sh:153:done <<< "$ALL_TODAY_BRIEFS"
scripts/workflow/hook-check-task-scope.sh:164:  done <<< "$ALL_TODAY_BRIEFS"
scripts/workflow/hook-check-task-scope.sh:212:    done <<< "$EXCLUDE_PATHS"
scripts/workflow/hook-check-task-scope.sh:214:done <<< "$ALL_TODAY_BRIEFS"
scripts/workflow/grep-refs.sh:50:  done <<< "$RESULTS"
scripts/workflow/grep-refs.sh:63:  done <<< "$TEST_RESULTS"
scripts/workflow/scope-check.sh:187:      done <<< "$KEYWORDS"
scripts/workflow/check-dev-doc-write-set.sh:123:done <<< "$CLEANED"
scripts/workflow/check-boundaries-incremental.sh:63:done <<< "$CHANGED_FILES"
scripts/workflow/hook-block-write.sh:297:  done <<< "$IFACE_SECTION"
scripts/check-fde-terms.sh:64:done <<< "$RAW"
scripts/checker-review.sh:54:done <<< "$CHANGED"
scripts/checker-review.sh:69:done <<< "$CHANGED"
scripts/checker-review.sh:84:done <<< "$CHANGED"
scripts/checker-review.sh:147:    done <<< "$FAILED_FILES"
scripts/check-file-driven.sh:59:  done <<< "$STAGED_MANIFESTS"
scripts/check-file-driven.sh:79:  done <<< "$STAGED_ONTOLOGY_JSON"
scripts/pre-doc-audit.sh:209:    done <<< "$DOC_REFS"
tests/control-tower/incident-loop.test.sh:80:if grep -qiE 'MINGW|MSYS|CYGWIN' <<< "$(uname -s)"; then
tests/control-tower/incident-loop.test.sh:97:if grep -qiE 'MINGW|MSYS|CYGWIN' <<< "$(uname -s)"; then
tests/control-tower/doc-commit-exempt.test.sh:132:done <<< "$DOC_OK_PATHS"
tests/control-tower/doc-commit-exempt.test.sh:137:done <<< "$DOC_NO_PATHS"
```

```bash
$ grep -rnE "__STATUS__|__END__|__EOF__|__MARKER__|__SEP__|<<<|---MARKER" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null | wc -l
      84
```

### 扫描②-分类（把 84 处拆开，避免把 shell 语法误判为协议问题）

```bash
$ grep -rnE "^[^#]*<<<" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null | grep -vE "^\S+:[0-9]+:\s*#" | wc -l
      81

$ grep -rnE "__[A-Z]+__|%\{http_code\}|echo ['\"]__|print\(['\"]__|console\.log\(['\"]__" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null
scripts/deploy/smoke-test.sh:51:  code=$(curl -s -o /dev/null -w "%{http_code}" "$BASE_URL$ep" 2>/dev/null)
scripts/deploy/smoke-test.sh:58:  code=$(curl -s -o /dev/null -w "%{http_code}" "$BASE_URL$ep" 2>/dev/null)
scripts/deploy/smoke-test.sh:84:  code=$(curl -s -o /dev/null -w "%{http_code}" -X "$method" \
scripts/deploy/smoke-test.sh:101:code=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
scripts/deploy/smoke-test.sh:109:code=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
scripts/golden-scenarios/GS-07-data-security/run.sh:85:curl -sS -o "$DATA_DIR/ws-create-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-07-data-security/run.sh:102:curl -sS -o "$DATA_DIR/rbac-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-01-first-diagnosis/run.sh:71:curl -sS -o "$DATA_DIR/noauth-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-01-first-diagnosis/run.sh:80:curl -sS -o "$DATA_DIR/validate-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-01-first-diagnosis/run.sh:90:curl -sS -o "$DATA_DIR/reports-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-01-first-diagnosis/run.sh:179:    REPORT_CODE="$(curl -sS -o "$DATA_DIR/consult-report.md" -w "%{http_code}" \
scripts/golden-scenarios/common/assert.ts:101:  const r = childProcess.spawnSync('curl', ['-sS', '-w', '\n__STATUS__:%{http_code}', url], {
scripts/golden-scenarios/common/assert.ts:109:  const parts = r.stdout.split('__STATUS__:');
scripts/golden-scenarios/GS-06-evolution-loop/run.sh:69:curl -sS -o "$DATA_DIR/feedback-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-06-evolution-loop/run.sh:78:curl -sS -o "$DATA_DIR/proposals-response.json" -w "%{http_code}" \
scripts/golden-scenarios/GS-06-evolution-loop/run.sh:86:curl -sS -o "$DATA_DIR/status-response.json" -w "%{http_code}" \
scripts/desktop/first-diagnosis-timing.sh:122:    code="$(curl -sS -o /dev/null -w "%{http_code}" --max-time 2 "$SERVER_URL/api/healthz" 2>/dev/null)" || code=000
scripts/desktop/first-diagnosis-timing.sh:135:READY_CODE="$(curl -sS -o /dev/null -w "%{http_code}" --max-time 5 \
scripts/desktop/upgrade-data-verify.sh:113:  CODE="$(curl -sS -o /dev/null -w "%{http_code}" --max-time 2 "$SERVER_URL/api/healthz" 2>/dev/null)" || CODE=000
scripts/workflow/checkpoint-deploy.sh:31:HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${BASE_URL}/api/health" 2>/dev/null || echo "000")
scripts/workflow/checkpoint-deploy.sh:45:GRAPH_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${BASE_URL}/api/ontology/graph/default" 2>/dev/null || echo "000")
scripts/workflow/checkpoint-deploy.sh:54:OBSERVER_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST "${BASE_URL}/api/agent-observer/report" \
scripts/workflow/checkpoint-deploy.sh:67:  HTTPS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${BASE_URL}/api/health" 2>/dev/null || echo "000")
tests/control-tower/check-progress-freshness.test.sh:49:mkjson() { # $1=文件名 $2=generated_at 值（或 __NONE__ 表示缺字段）
tests/control-tower/check-progress-freshness.test.sh:51:  if [ "$2" = "__NONE__" ]; then
tests/control-tower/check-progress-freshness.test.sh:88:NOGEN_F=$(mkjson nogen.json "__NONE__")

$ grep -rnE "__[A-Z]+__|%\{http_code\}|echo ['\"]__|print\(['\"]__|console\.log\(['\"]__" --include=*.ts --include=*.sh --include=*.js scripts/ src/ tests/ 2>/dev/null | wc -l
      26
```

### 扫描③：子进程调用面

```bash
$ grep -rnE "spawnSync|execFileSync|execSync" --include=*.ts --include=*.sh scripts/ src/ 2>/dev/null | wc -l
      24

$ grep -rncE "spawnSync|execFileSync|execSync" --include=*.ts --include=*.sh scripts/ src/ 2>/dev/null | grep -v ":0$"
scripts/golden-scenarios/common/assert.ts:2
scripts/golden-scenarios/common/bootstrap.ts:1
src/contract/contract-gate.ts:3
src/deploy/startup-check.ts:2
src/l4/department-memory-store.ts:2
src/tui-v2/lib/bootstrap.ts:3
src/agent/proactive-push.ts:2
src/agent/synova-agent.ts:3
src/routes/healthz.ts:2
src/routes/cockpit.ts:2
src/services/resource-monitor.ts:2
```

### 交集判定：**同时**含「子进程调用」与「标记字符串」的文件

```bash
$ for f in $(grep -rlE "spawnSync|execFileSync|execSync" --include=*.ts --include=*.sh scripts/ src/ 2>/dev/null); do if grep -qE "__[A-Z]+__|%\{http_code\}|['\"]__" "$f" 2>/dev/null; then echo "HIT: $f"; grep -nE "__[A-Z]+__|%\{http_code\}|['\"]__" "$f"; fi; done
HIT: scripts/golden-scenarios/common/assert.ts
101:  const r = childProcess.spawnSync('curl', ['-sS', '-w', '\n__STATUS__:%{http_code}', url], {
109:  const parts = r.stdout.split('__STATUS__:');
=== 完成 ===
```

### 存量分类表

| 形态 | 处数 | 是否目标形态 | 依据 |
|---|---|---|---|
| **`stdout.split('__XXX__')` —— 状态码混进同一输出流再分割** | **1 处（1 文件 / 2 行）** | ✅ **目标** | 交集判定唯一命中：`scripts/golden-scenarios/common/assert.ts:101,109` |
| bash here-string `<<<` | 81 | ❌ 无关 | shell 语法（`done <<< "$VAR"`），非子进程输出协议 |
| `curl -w "%{http_code}"` + `-o <file>` / `-o /dev/null` | 22 | ❌ **反形态（正确分离）** | body 落文件、状态码落 stdout —— 正是「分离流」的正面样板（`smoke-test.sh`、`GS-0*/run.sh`、`checkpoint-deploy.sh`、`first-diagnosis-timing.sh`、`upgrade-data-verify.sh`） |
| `__NONE__` 哨兵值 `=` 比较 | 3 | ❌ 无关 | `check-progress-freshness.test.sh:49,51,88` 用 `[ "$2" = "__NONE__" ]` 判缺字段，非分割 |
| 合计（原 84 行） | 84 | — | 84 = 81 + 1 + 3 - 1（assert.ts 的 2 行被 here-string 计法排除，见上） |

**结论行 B-2:**
1. **存量处数 = 1 处**（`scripts/golden-scenarios/common/assert.ts:101,109`），**逐文件清单 = 1 个文件**。全仓 `spawnSync|execFileSync|execSync` 共 **24 处 / 11 个文件**，其中**唯一**同时出现标记字符串的仍是 `assert.ts`。
2. 「标记字符串混流」在本仓**不是普遍形态**——81/84 是 shell here-string（`<<<`），22 处 `%{http_code}` 是**已正确分离**的样板（`-o` 重定向 body）。
3. ⇒ **「存量清单化 + 新增即拦」可行性：高**（存量基数 = 1，不是数十处），且**已有 22 处正面样板可作规范范例**。但**必须先把 here-string 与 `%{http_code}` 分离形态排除在判据外**，否则新检查器会产出 103 处误报（81 + 22）——这正是坑清单「禁 grep 型静态判据当验收」的直接风险点。

## B-3 检查器承载点

```bash
$ printf 'sh  = %s\n' "$(ls scripts/control-tower/*.sh 2>/dev/null | wc -l)"; printf 'py  = %s\n' "$(ls scripts/control-tower/*.py 2>/dev/null | wc -l)"; printf 'ts  = %s\n' "$(ls scripts/control-tower/*.ts 2>/dev/null | wc -l)"
sh  = 26
py  = 26
ts  = 3

$ ls scripts/control-tower/*.ts
scripts/control-tower/conflict-scanner.ts
scripts/control-tower/enterprise-fact-store.ts
scripts/control-tower/fact-approval-service.ts
```

`tests/control-tower/` 测试文件命名与运行方式（样例：最近新增的门禁测试，头 40 行）：

```bash
$ sed -n '1,40p' tests/control-tower/check-citations.test.sh
#!/bin/bash
# D313 M5 UTF-8 强制: Windows 控制台/子进程统一 UTF-8
export PYTHONIOENCODING=utf-8
export LC_ALL=C.UTF-8 2>/dev/null || true
# ═══════════════════════════════════════════════════════════════
# check-citations.test.sh — D919 引用可核验门禁（创始人必修项机制化）
#
# 覆盖矩阵（铁律 48 三路径 + 接线）:
#   正常 — 仓内 file:line + 仓外根（伪 DSH 布局 node_modules/@deepseek-ai/）→ exit 0
#   违规 — 文件不存在 / 行号越界 / 区间倒置 / **中文名文档不存在**（原 ASCII 字符集静默漏检）
#          / 伪造外部权威（dsh- 前缀但仓外根无此包）→ exit 1 且错误码点名
#   降级 — artifact 不存在 → exit 2（fail-closed，绝不当作通过）
#   边界 — 无引用 → exit 0；`## 引用豁免` 带理由放行 / 无理由仍违规；**>25 条不截断**（原 head -25 漏洞）
#   接线 — pre-dispatch-check.sh ⑥ 调用 check-citations.py（WIRE CHECK，铁律 0-2）
# 沙箱: 全部夹具在 mktemp -d 内；仓外根为伪 DSH 布局（不依赖真实安装）
# ═══════════════════════════════════════════════════════════════
set -uo pipefail
REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
C="$REPO/scripts/control-tower/check-citations.py"
PD="$REPO/scripts/control-tower/pre-dispatch-check.sh"
PY="$(command -v python3 || command -v python || true)"
PASS=0; FAIL=0
ok(){ echo "  ✅ $1"; PASS=$((PASS+1)); }
no(){ echo "  ❌ $1"; FAIL=$((FAIL+1)); }
TMPD="$(mktemp -d)"; trap 'rm -rf "$TMPD"' EXIT

if [ -z "$PY" ]; then echo "  ⚠️ python 不可用 — 跳过（fail-open，铁律 11 显式）"; exit 0; fi
[ -f "$C" ] || { echo "  ❌ 被测脚本缺失: $C"; exit 1; }

echo "=== D919 引用可核验门禁 ==="

# ── 接线（WIRE CHECK）──
echo "── 接线 ──"
if grep -q "check-citations.py" "$PD"; then ok "接线: pre-dispatch-check.sh ⑥ 调用 check-citations.py"
else no "接线: pre-dispatch-check.sh 未调用（新能力零调用方 = 未接线）"; fi
grep -q "check-citations" "$REPO/.github/workflows/ci.yml" \
  && ok "接线: CI canary 清单含本测试" || no "接线: CI canary 清单缺本测试"

# ── 伪 DSH 仓外根（确定性，不依赖真实安装）──
ROOT="$TMPD/extroot"
```

ci.yml 中的 `control-tower` 引用点：

```bash
$ grep -n "control-tower" .github/workflows/ci.yml
95:          bash scripts/control-tower/verify-parallel.sh --ci-pr origin/main
102:          python3 scripts/control-tower/merge_writeset_gate.py \
202:  control-tower-tests:
214:      - name: Run hermetic control-tower gate tests
225:            tests/control-tower/verify-parallel.test.sh \
226:            tests/control-tower/verify-claims-table.test.sh \
227:            tests/control-tower/write-set-check.test.sh \
228:            tests/control-tower/post-commit-marker.test.sh \
229:            tests/control-tower/ct-test-gate.test.sh \
230:            tests/control-tower/task-start-parallel.test.sh \
231:            tests/control-tower/fastlane-bypass-only.test.sh \
232:            tests/control-tower/gate-stats.test.sh \
233:            tests/control-tower/q2-error-locating.test.sh \
234:            tests/control-tower/platform-checklist.test.sh \
235:            tests/control-tower/tag-ancestry.test.sh \
236:            tests/control-tower/brief-parser-strip.test.sh \
237:            tests/control-tower/post-commit.test.sh \
238:            tests/control-tower/simulate-ci.test.sh \
239:            tests/control-tower/synova-submit.test.sh \
240:            tests/control-tower/synova-commit.test.sh \
241:            tests/control-tower/parallel-main-tree-occupancy.test.sh \
242:            tests/control-tower/fastlane-extended.test.sh \
243:            tests/control-tower/g12-day-window.test.sh \
244:            tests/control-tower/incident-loop.test.sh \
245:            tests/control-tower/check-canary-drift.test.sh \
246:            tests/control-tower/claim-regex-narrow.test.sh \
247:            tests/control-tower/ci-strict-visible.test.sh \
248:            tests/control-tower/skeleton-brief-gate.test.sh \
249:            tests/control-tower/clone-config-init.test.sh \
250:            tests/control-tower/clone-shadow-commit.test.sh \
251:            tests/control-tower/verify-parallel-ci.test.sh \
252:            tests/control-tower/alloc-task-id.test.sh \
253:            tests/control-tower/alloc-task-id-lock.test.sh \
254:            tests/control-tower/check-gitlinks.test.sh \
255:            tests/control-tower/grep-oP-regression.test.sh \
256:            tests/control-tower/merge_writeset_gate.test.sh \
257:            tests/control-tower/ci-ratchet-base.test.sh \
258:            tests/control-tower/verify-doc.test.sh \
259:            tests/doc-system/check-doc-truth.test.sh \
260:            tests/doc-system/doc-registry-gate.test.sh \
261:            tests/doc-system/doc-categories.test.sh \
262:            tests/doc-system/doc-staleness.test.sh \
263:            tests/doc-system/doc-triage.test.sh \
264:            tests/doc-system/generate-chronicle-monthly.test.sh \
265:            tests/control-tower/check-citations.test.sh \
266:            tests/control-tower/check-progress-freshness.test.sh; do
278:          bash scripts/control-tower/check-canary-drift.sh || true

$ grep -c "control-tower" .github/workflows/ci.yml
      41
```

**结论行 B-3:** 检查器承载点：`scripts/control-tower/` 共 **55 个脚本 = 26 `.sh` + 26 `.py` + 3 `.ts`**。`tests/control-tower/` 共 **122 文件**（102 `.test.sh` / 7 `.test.py` / 3 `.test.ts` / 10 其他）。**测试运行方式 = 硬编码清单**：`ci.yml:225-266` 的 `control-tower-tests` job 逐个 `bash "$t"`（41 个条目，跨 `tests/control-tower/` + `tests/doc-system/`），失败用 `tail -8` 进 `::error` 注解（L271）。**新增检查器必须同时做两件事**：① 加 `tests/control-tower/<name>.test.sh`；② **把该测试路径加进 `ci.yml:225-266` 的硬编码清单**（否则测试永不运行——`check-citations.test.sh` 的 L36-38「接线: CI canary 清单含本测试」断言即此惯例的自我固化）。注意 41 个清单条目 vs 122 个测试文件 → **多数测试不在 CI canary 清单内**。

## B-4 tsconfig 是否覆盖 `scripts/**/*.ts`

```bash
$ cat tsconfig.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "jsx": "react",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "noImplicitAny": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "sourceMap": true,
    "outDir": "./dist",
    "paths": {
      "@synova/sog-core": ["./packages/sog-core/src/index.ts"],
      "@synova/engine-auth": ["./packages/engine-auth/src/index.ts"],
      "@synova/logger": ["./packages/logger/src/index.ts"],
      "@synova/error-types": ["./packages/error-types/src/index.ts"],
      "@synova/connector-registry": ["./packages/connector-registry/src/index.ts"],
      "@synova/extension-registry": ["./packages/extension-registry/src/index.ts"],
      "@synova/knowledge-ingest": ["./packages/knowledge-ingest/src/index.ts"],
      "@synova/evolution": ["./packages/evolution/src/index.ts"],
      "@synova/ontology": ["./packages/ontology/src/index.ts"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "dist", "tests"]
}
```

```bash
$ grep -nE "\"include\"|\"exclude\"|\"files\"|scripts" tsconfig.json
29:  "include": ["src/**/*.ts", "src/**/*.tsx"],
30:  "exclude": ["node_modules", "dist", "tests"]
```

CI 的类型检查入口（决定 `scripts/**` 是否被纳入）：

```bash
$ npm run lint  →  package.json:26: "lint": "tsc --noEmit"
$ ci.yml L79  →  ERRORS=$(npx tsc --noEmit --pretty false 2>&1 | grep -v "_extinct/\|ima.ts\|server.ts\|sentinel-service.ts\|industry-loader.ts\|tool-registration.ts\|llm-provider-loader.ts" | grep -E "error TS" || true)
```

是否有任何 tsc 配置覆盖 `scripts/`：

```bash
$ grep -rnE "tsc .*scripts|--project.*scripts|tsconfig.*scripts" scripts/ .github/ package.json --include=*.sh --include=*.yml --include=*.json 2>/dev/null
scripts/ci/verify-doc.sh:19:#      npx vitest / npx tsc / bash(仅限 scripts/ci/verify-*.sh 与 tests/control-tower/*.test.sh)

$ ls tsconfig*.json
tsconfig.json

$ find . -maxdepth 3 -name "tsconfig*.json" -not -path "./node_modules/*" -not -path "./.git/*" -not -path "./.synova-wt-*" -not -path "./.sessions/*" 2>/dev/null
./electron-renderer/tsconfig.json
./packages/sog-core/tsconfig.json
./packages/agent-observer-mcp/tsconfig.json
./packages/test-kit/tsconfig.json
./packages/ontology/tsconfig.json
./packages/engine-auth/tsconfig.json
./tsconfig.json
```

现存 3 个 `scripts/control-tower/*.ts` 为何仍受 tsc 约束（**经 src/ 传递可达**）：

```bash
$ grep -rn "control-tower/conflict-scanner\|control-tower/enterprise-fact-store\|control-tower/fact-approval-service" src/ scripts/ tests/ --include=*.ts --include=*.sh 2>/dev/null
src/deploy/bootstrap.ts:1125:                const { ConflictScanner } = await import('../../scripts/control-tower/conflict-scanner');
src/l4/agent-memory-store.ts:20:import { EnterpriseFactStore, type FactStatus } from '../../scripts/control-tower/enterprise-fact-store';
scripts/control-tower/enterprise-fact-store.ts:3: * scripts/control-tower/enterprise-fact-store.ts — 企业事实文件 CRUD (D240)
scripts/control-tower/fact-approval-service.ts:3: * scripts/control-tower/fact-approval-service.ts — 企业事实审批服务 (D240)
scripts/control-tower/fact-approval-service.ts:108://   npx tsx scripts/control-tower/fact-approval-service.ts list
scripts/control-tower/conflict-scanner.ts:3: * scripts/control-tower/conflict-scanner.ts — 企业事实冲突检测 (D240)
scripts/control-tower/conflict-scanner.ts:117://   npx tsx scripts/control-tower/conflict-scanner.ts [category]
tests/control-tower/enterprise-fact-chain.test.ts:18:import { EnterpriseFactStore } from '../../scripts/control-tower/enterprise-fact-store';
tests/control-tower/enterprise-facts.test.ts:17:import { EnterpriseFactStore } from "../../scripts/control-tower/enterprise-fact-store";
tests/control-tower/enterprise-facts.test.ts:18:import { FactApprovalService } from "../../scripts/control-tower/fact-approval-service";
tests/control-tower/enterprise-facts.test.ts:19:import { ConflictScanner } from "../../scripts/control-tower/conflict-scanner";
```

**结论行 B-4:** `tsconfig.json` 的 `include = ["src/**/*.ts", "src/**/*.tsx"]`（L29），**不覆盖 `scripts/**/*.ts`**；`exclude = ["node_modules","dist","tests"]`（L30）。CI 入口是 `npx tsc --noEmit`（`package.json:26` lint 同）→ **新增 `scripts/control-tower/*.ts` 检查器默认不会被 tsc 纳入**。现存 3 个 `.ts` 之所以受约束，是因为被 `src/deploy/bootstrap.ts:1125` 与 `src/l4/agent-memory-store.ts:20` **import**（传递可达），以及 `tests/**/*.test.ts` 经 vitest 编译。⇒ D924 若把检查器写成 `scripts/control-tower/*.ts`，**必须显式选择**：(a) 加进 `tsconfig.json` include（改全局配置，影响面大）；(b) 由 `src/` import 传递可达（会引入生产依赖，须过铁律 39 架构边界）；(c) 沿用仓内主流 `.py` / `.sh`（26 + 26 个，无此问题）。已实测：**当前无任何 tsc 配置或脚本覆盖 `scripts/`**（唯一命中是一行注释）。

---

# C) D925 落点 — deps-interface + setter 注入标准

## C-1 精确清点 setter 注入点（关键前提判定）

```bash
$ grep -rnE "export function set[A-Za-z]*Deps" --include=*.ts scripts/ src/ packages/ tests/ 2>/dev/null
src/agent/loop-handlers.ts:105:export function setDiagnosisDeps(deps: DiagnosisDeps | null): void {
src/agent/loop-handlers.ts:294:export function setNavigationDeps(deps: NavigationDeps | null): void {
src/agent/loop-handlers.ts:444:export function setSelfCheckDeps(deps: SelfCheckDeps | null): void {
src/agent/loop-handlers.ts:560:export function setKnowledgeDeps(deps: KnowledgeDeps | null): void {
src/agent/loop-handlers.ts:617:export function setOverflowDeps(deps: OverflowDeps | null): void {

$ grep -rnE "export function set[A-Za-z]*Deps" --include=*.ts scripts/ src/ packages/ tests/ 2>/dev/null | wc -l
       5
```

放宽形态（含非 export / 任意 setter 名）仍为同样 5 处：

```bash
$ grep -rnE "function set[A-Za-z]*Deps" --include=*.ts scripts/ src/ packages/ tests/ 2>/dev/null
src/agent/loop-handlers.ts:105:export function setDiagnosisDeps(deps: DiagnosisDeps | null): void {
src/agent/loop-handlers.ts:294:export function setNavigationDeps(deps: NavigationDeps | null): void {
src/agent/loop-handlers.ts:444:export function setSelfCheckDeps(deps: SelfCheckDeps | null): void {
src/agent/loop-handlers.ts:560:export function setKnowledgeDeps(deps: KnowledgeDeps | null): void {
src/agent/loop-handlers.ts:617:export function setOverflowDeps(deps: OverflowDeps | null): void {
```

其他注入缝形态（`ForTest` / `__set` / `configureDeps` / `injectDeps`）全仓零命中：

```bash
$ grep -rnE "ForTest|__set|configureDeps|injectDeps" --include=*.ts src/ tests/ scripts/ 2>/dev/null
（无输出）

$ grep -rnE "ForTest|__set|configureDeps|injectDeps" --include=*.ts src/ tests/ scripts/ 2>/dev/null | wc -l
       0
```

### 逐一列名 + file:line（5 组）

| # | 注入点 | file:line | 对应接口 | 对应 handler |
|---|---|---|---|---|
| 1 | `setDiagnosisDeps` | `src/agent/loop-handlers.ts:105` | `DiagnosisDeps`（L91） | `defaultDiagnosisHandler`（L204） |
| 2 | `setNavigationDeps` | `src/agent/loop-handlers.ts:294` | `NavigationDeps`（L287） | `defaultNavigationHandler`（L307） |
| 3 | `setSelfCheckDeps` | `src/agent/loop-handlers.ts:444` | `SelfCheckDeps`（L435） | `defaultSelfCheckHandler`（L468） |
| 4 | `setKnowledgeDeps` | `src/agent/loop-handlers.ts:560` | `KnowledgeDeps`（L553） | `defaultKnowledgeAccumulationHandler`（L578） |
| 5 | `setOverflowDeps` | `src/agent/loop-handlers.ts:617` | `OverflowDeps`（L609） | `defaultOverflowHandler`（L644） |

### 第 6 组在哪？—— 实测是「6 个 handler / 5 个 setter」，缺口在 `defaultEvolutionHandler`

```bash
$ wc -l src/agent/loop-handlers.ts
     716 src/agent/loop-handlers.ts

$ grep -nE "^export (async )?function [a-zA-Z]+" src/agent/loop-handlers.ts
105:export function setDiagnosisDeps(deps: DiagnosisDeps | null): void {
204:export async function defaultDiagnosisHandler(scale: ScaleName): Promise<LoopExecutionResult> {
294:export function setNavigationDeps(deps: NavigationDeps | null): void {
307:export async function defaultNavigationHandler(scale: ScaleName): Promise<LoopExecutionResult> {
374:export async function defaultEvolutionHandler(scale: ScaleName): Promise<LoopExecutionResult> {   ← ⚠ 无 setter
444:export function setSelfCheckDeps(deps: SelfCheckDeps | null): void {
468:export async function defaultSelfCheckHandler(scale: ScaleName): Promise<LoopExecutionResult> {
560:export function setKnowledgeDeps(deps: KnowledgeDeps | null): void {
578:export async function defaultKnowledgeAccumulationHandler(scale: ScaleName): Promise<LoopExecutionResult> {
617:export function setOverflowDeps(deps: OverflowDeps | null): void {
644:export async function defaultOverflowHandler(scale: ScaleName): Promise<LoopExecutionResult> {

$ grep -nE "interface [A-Za-z]*Deps|type [A-Za-z]*Deps" src/agent/loop-handlers.ts
27:  type LightweightReDiagnosisDeps,
91:export interface DiagnosisDeps {
287:export interface NavigationDeps {
435:export interface SelfCheckDeps {
553:export interface KnowledgeDeps {
609:export interface OverflowDeps {
```

`defaultEvolutionHandler` 体内取依赖的方式（**无注入缝，直连活单例**）：

```bash
$ sed -n '374,378p' src/agent/loop-handlers.ts
export async function defaultEvolutionHandler(scale: ScaleName): Promise<LoopExecutionResult> {
  try {
    // 1. 聚合信号 (D93 feedback-collector)
    const signals = getFeedbackCollector().getAggregatedSignals();
```

### 全历史核查：历史上是否曾有 6 组？

```bash
$ git log --all --format="%H" -- src/agent/loop-handlers.ts | while read h; do git show "$h:src/agent/loop-handlers.ts" 2>/dev/null | grep -oE "export function set[A-Za-z]*Deps"; done | sort -u
export function setDiagnosisDeps
export function setKnowledgeDeps
export function setNavigationDeps
export function setOverflowDeps
export function setSelfCheckDeps

$ git log --oneline -S "export function set" -- src/agent/loop-handlers.ts
2b6aaad6 feat(D475): loop 执行体真实化 — 五处理器 + loop-4/5 专属路由（K3 P0/P1 清零）
```

### 前提原文与实测的对照

```bash
$ grep -n "setXxxDeps" docs/synova/research/研究院交接-20260922/双DSH执行提升清单.md
45:**证据**：DSH 的 `ga-calibration.ts`（动态 import + `getFeedbackCollector()` 活单例，盲评 R5=3/3.5）与 `customer-config-package.ts`（私有函数直连 fs 无注入点，R5=3.5）测试须起真实环境；而 **Win 的 `loop-handlers.ts` 有全场最佳的可测试性范式**——6 组 `setXxxDeps` 接口 + setter 注入 + 传 null 恢复生产默认（盲评 R5 高分，4/4.5）。

$ grep -n "setDeps\|setXxxDeps" docs/synova/research/研究院交接-20260922/DSH-agent-team与Win侧Claude-Code质量对比.md
128:- WIN `loop-handlers.ts`（716 行）：实为规整的 5 组 setDeps/Handler 依赖注入模板，可测试性设计良好；写后复读验证是全场最成熟的可靠性工程。
```

测试侧的实际使用（证明 5 组，且「传 null 恢复生产默认」被 `afterEach` 固化为惯例）：

```bash
$ grep -nE "set[A-Za-z]*Deps" tests/agent/loop-handlers.test.ts
40:  setDiagnosisDeps,
41:  setNavigationDeps,
42:  setSelfCheckDeps,
43:  setKnowledgeDeps,
44:  setOverflowDeps,
259:  afterEach(() => setDiagnosisDeps(null));
293:    setDiagnosisDeps(deps);
364:  afterEach(() => setNavigationDeps(null));
368:    setNavigationDeps({ getStore: () => store });
415:    setNavigationDeps({ getStore: () => { throw new Error('db unavailable'); } });
430:  afterEach(() => setSelfCheckDeps(null));
439:    setSelfCheckDeps(deps);
499:  afterEach(() => setKnowledgeDeps(null));
507:    setKnowledgeDeps({ getStore: () => ({ recentStats }) });
534:    setKnowledgeDeps({ getStore: () => { throw new Error('db unavailable'); } });
558:  afterEach(() => setOverflowDeps(null));
615:    setOverflowDeps({ getStore: () => store, getCycles });
657:    setOverflowDeps({
676:    setSelfCheckDeps(null);
677:    setKnowledgeDeps(null);
681:    setSelfCheckDeps({
697:    setKnowledgeDeps({

$ grep -cE "set[A-Za-z]*Deps" tests/agent/loop-handlers.test.ts
      22
$ wc -l tests/agent/loop-handlers.test.ts
     715 tests/agent/loop-handlers.test.ts
```

**结论行 C-1（⚠ 前提不成立）:**

> **派单前提与实测不符：派单件/裁定写「6 组 `setXxxDeps`」，实测 = 5 组。**

- 实测注入点 = **5 组**：`setDiagnosisDeps`(L105) / `setNavigationDeps`(L294) / `setSelfCheckDeps`(L444) / `setKnowledgeDeps`(L560) / `setOverflowDeps`(L617)，**全部在 `src/agent/loop-handlers.ts` 单一文件内**。
- 全历史 `git log --all` 核查：该文件**从未**出现过第 6 个 `set*Deps`（唯一引入提交 `2b6aaad6`，commit subject 自陈「**五处理器**」）。
- **被计/漏计的候选判定**：`set*Deps` 之外的注入缝（`ForTest` / `__set` / `configureDeps` / `injectDeps`）**全仓 0 命中**——即不存在「第 6 个被漏计的命名变体」。
- 「6」的真实来源 = **handler 个数**：该文件共 **6 个 `default*Handler`**（Diagnosis / Navigation / **Evolution** / SelfCheck / KnowledgeAccumulation / Overflow），但只有 5 个配了 setter。**缺口 = `defaultEvolutionHandler`（L374）**：它直连 `getFeedbackCollector()` 活单例（L377），**恰是研究清单同一段落批评 DSH 的反模式本身**（"动态 import + `getFeedbackCollector()` 活单例"）。
- **同源研究件内部自相矛盾**：`双DSH执行提升清单.md:45` 写「**6 组**」，而同目录 `DSH-agent-team与Win侧Claude-Code质量对比.md:128` 写「实为规整的 **5 组** setDeps/Handler」。两处对同一文件（716 行）给出不同数字 → 判据须以**实测 5 组**为准。
- 附带：`loop-handlers.ts` 实测 **716 行**，与对比研究 L128 所述「716 行」一致。

## C-2 模板落点现状

```bash
$ ls .claude/skills/
brief-compose
claim-verifier
contract-template
cto-handover
ctrl-tower-change
dev-doc-delivery
dsh-decision-lens
git-sync-pr
north-star-guard
pr-review
pre-dispatch-check
synova-audit
synova-verify
windows-compat
count=      14

$ ls .dsh/skills/
brief-compose
claim-verifier
contract-template
cto-handover
ctrl-tower-change
dev-doc-delivery
dsh-decision-lens
git-sync-pr
north-star-guard
pr-review
pre-dispatch-check
synova-audit
synova-verify
windows-compat
count=      14
```

两者是否逐字节同源：

```bash
$ diff -r .claude/skills .dsh/skills
（无输出）
$ echo "diff 退出码=$?"
diff 退出码=0
$ diff -r .claude/skills .dsh/skills 2>&1 | wc -l
       0
```

```bash
$ ls .claude/task-briefs/ | wc -l
     671

$ wc -l scripts/control-tower/dev-doc-gatekeeper.sh scripts/control-tower/devdoc_writeset.py
     243 scripts/control-tower/dev-doc-gatekeeper.sh
     146 scripts/control-tower/devdoc_writeset.py
     389 total
```

`dev-doc-gatekeeper.sh` 头 30 行：

```bash
$ sed -n '1,30p' scripts/control-tower/dev-doc-gatekeeper.sh
#!/bin/bash
# ═══════════════════════════════════════════════════════════════════════════════
# dev-doc-gatekeeper.sh — Dev Doc 校验网守 (D206)
#
# 权威文档 #17 Ch3 S1.1：在 dev doc 分发给 Claude Code 前，必须通过 6 项
# 机械验证。防止已知错误模式传播到实现阶段。
#
# 用法:
#   bash dev-doc-gatekeeper.sh <path-to-dev-doc.md>
#
# 退出码:
#   0 = ALL PASS（可以分发）
#   1 = FAIL（不能分发 — 有阻断项）
#   2 = DEGRADED（检查自身降级，允许分发但告警）
#
# 6 项强制检查:
#   C1: Edge ID 存在性 — 每个 E-XX 在代码中真实存在
#   C2: 文件路径存在性 — 每个 src/extensions/packages 路径真实存在
#   C3: Test Requirements 章节 — 包含 L1/L2a/L2b/L2c
#   C4: Wiring Verification 章节 — 包含调用方文件路径
#   C5: Authority Doc Verification 章节 — 包含来源路径引用
#   C6: 写集表存在性 — devdoc_writeset --extract 提取失败 = FAIL (D381 堵 fail-open)
# ═══════════════════════════════════════════════════════════════════════════════
set -uo pipefail
```

`devdoc_writeset.py` 头 30 行：

```bash
$ sed -n '1,30p' scripts/control-tower/devdoc_writeset.py
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
scripts/control-tower/devdoc_writeset.py — D313 dev doc 写集解析共享库 (M3b)

从 verify-parallel.sh 提取的写集表解析 + 4 形态清洗，统一供:
  - verify-parallel.sh（两 doc 零交集比对）
  - check-dev-doc-write-set.sh（声明 vs 代码 grep 差异）

写集表格式（VERSION.md 契约）:
  标题: `### N.N 写集 (N 修改 + M 新建)`（正则 ^#{2,4}\\s*\\d+(\\.\\d+)*\\s*写集）
  表头: `| 文件 | 操作 | 说明 |`，第一列 4 形态:
    纯路径 / [text](url) 链接 / 行号后缀 L750 / 计数 (N 个) / 目录级（/ 结尾）

fail-open: 文件不存在/无写集表 → (None, err)；解析异常 → (None, err)。
UTF-8: stdout reconfigure。
"""
import argparse
import json
import re
import sys
from pathlib import Path
from typing import List, Optional, Tuple
```

**结论行 C-2:** `.claude/skills/` 与 `.dsh/skills/` 各 **14 个**，`diff -r` **零差异（exit 0 / 0 行输出）→ 当前逐字节同源**。`.claude/task-briefs/` 现有 **671 个文件**。dev-doc 落点：`dev-doc-gatekeeper.sh` **243 行**（6 项机械验证：C1 Edge ID / C2 路径 / C3 Test Requirements / C4 Wiring / C5 Authority Doc / C6 写集表）、`devdoc_writeset.py` **146 行**（写集表解析共享库，被 `verify-parallel.sh` + `check-dev-doc-write-set.sh` 消费）。

## C-3 D370 技能同步门禁（决定 D925 是否被现门禁约束）

```bash
$ grep -n -A12 "D370\|技能同步" scripts/pre-commit-check.sh
8:#   接线物理事实）+ 特例 G12d 生成物单点（D458）与 G13 技能同步（D370）保持硬阻断。
9:#   其余检查 hard_check → soft_check：判定代码与输出原样保留（--check 报告与 K3
10:#   审计依赖），只是本地不再阻断——CI Iron Laws job 为权威（ci.yml 已有）。
11:#   保留理由（防未来误删）：G12d 防 CI 单点产物污染（D429/D452/D455 实证）、
12:#   G13 防双目录漂移（误报率低、命中即真事故）。
13:#   命中统计（项4）：hard_check/soft_check 每次触发追加 JSONL 到 .claude/gate-hits.log，
14:#   由 scripts/control-tower/gate-stats.sh 汇总（月度清理数据地基）。
15-#
16-# v3.6 → v3.8 核心变化 (2026-06-23):
17-#   + plan.json 支持: 分阶段任务可 deferred wiring/test_pairing 检查
18-#   + 双日志: pre-commit-failures.log (门禁正常拒绝) vs bypass.log (--no-verify 绕过)
19-#   + as any 跳过注释行 (不再把 "Iron law #38: as any = 0" 误报为违规)；CT-46 扩 as never / as unknown as
20-#   + bash 退位: 只做物理验证 (符号存在? 文件存在? 语法合法?)
21-#
33:#  13. 技能同步一致性            (.claude/skills ↔ .dsh/skills 漂移, 调用 sync-dsh-skills.sh --check)
34-#
35-# 设计哲学:
36-#   bash 只回答"物理事实" — 符号被引用过吗？文件存在吗？
37-#   agent 自检回答"语义判断" — 引用在正确的调用链中吗？
38-#   plan.json 声明"架构步骤" — 这个文件接线在后续阶段
39-# ═══════════════════════════════════════════════════════════════════════════════
40-set +e
41-
42-HARD_FAIL=0
43-WARN_COUNT=0
44-SOFT_COUNT=0
45-SOFT_COUNT=0
46-
296:#   ❌ 排除: .claude/skills/、.dsh/skills/(行为配置, D370 有独立同步契约 → 非纯文档,
297-#      技能提交保持组 13 全门禁; 纯文档提交时 SKILL_FILES_STAGED 为空天然不触发)
298-#   ❌ 排除: .codex/(契约/映射配置, 组 9 依赖)、.github/(CI 配置, 非同步信息)、
299-#      .claude/settings.json(含 token 风险, D312)
300-# 契约 (铁律 47):
301-#   @input  — $1: STAGED_ALL（git diff --cached --name-only 全暂存文件, 换行分隔）
302-#   @output — stdout: 1 = 纯文档提交; 0 = 非纯文档（含任一白名单外文件/空输入）
303-#   @fail-closed — 正则匹配失败/空输入 → 0（走全量 13 组, 不静默放行, D328）
304-#   @error  — 无异常路径（纯 bash 字符串匹配）
305-DOC_PREFIX_RE='^(docs/.*\.(md|html|txt)$|\.claude/task-briefs/.*\.(md|html|txt)$|memory/.*\.(md|html|txt)$|task-state/.*\.(json|md)$|[^/]+\.(md|html|txt)$)'
1382:# ═══ 组 13/13: 技能同步一致性 (.claude/skills ↔ .dsh/skills, D370) ═══
1383-# 背景: DSH 技能发现根 .dsh/skills（rank 100）不读 .claude/skills → 单源复制 + 漂移门禁。
1384-# fail-closed (D328): 检查脚本 exit 2 = 检查执行失败 → 同样硬阻断, 不与"通过"混同。
1385-echo ""
1386:echo -e "${CYAN}── 组 13/13: 技能同步一致性 ──${RESET}"
1387-SKILL_FILES_STAGED=$(echo "$STAGED_ALL" | grep -E "\.claude/skills/|\.dsh/skills/" || true)
1388-if [ -n "$SKILL_FILES_STAGED" ]; then
1389-  SKILL_SYNC_OUT=$(bash "$ROOT/scripts/workflow/sync-dsh-skills.sh" --check 2>&1)
1390-  SKILL_SYNC_EXIT=$?
1391-  if [ "$SKILL_SYNC_EXIT" -eq 0 ]; then
1392:    soft_pass "G13: 技能同步一致 ($(echo "$SKILL_SYNC_OUT" | head -1 | sed 's/SYNC-OK: //'))"
1393-  elif [ "$SKILL_SYNC_EXIT" -eq 1 ]; then
1394:    hard_check "G13: 技能漂移 — 运行 bash scripts/workflow/sync-dsh-skills.sh 后重新暂存" "$SKILL_SYNC_OUT"
1395-  else
1396:    hard_check "G13: 技能同步检查执行失败 (exit=$SKILL_SYNC_EXIT, D328 三态)" "$SKILL_SYNC_OUT"
1397-  fi
1398-else
1399-  soft_pass "G13: 无技能文件变更(跳过)"
1400-fi
```

**结论行 C-3:** D370 门禁**存在且仍是硬阻断**。机制：`pre-commit-check.sh` 组 13/13（L1382-1400）—— 当暂存区含 `.claude/skills/` 或 `.dsh/skills/` 文件时（L1387-1388），调用 `scripts/workflow/sync-dsh-skills.sh --check`（L1389），**exit 1（漂移）→ `hard_check` 阻断**（L1394），**exit 2（检查执行失败）→ 同样 `hard_check` 阻断**（L1396，D328 三态 fail-closed）。**⚠ 关键**：该组**在 V5 的「本地软提示」改革中被明确保留为硬阻断**（L8-12 原文：`特例 G12d ... 与 G13 技能同步（D370）保持硬阻断`，理由「G13 防双目录漂移（误报率低、命中即真事故）」）→ **D925 同时改 `.claude/skills` + `.dsh/skills` 必被此门禁约束**，改完须跑 `bash scripts/workflow/sync-dsh-skills.sh` 再重新暂存，否则本地即硬阻断。

## C-4 冲突扫描

```bash
$ grep -rniE "setXxxDeps|deps-interface|依赖注入" docs/ .claude/skills/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null
docs/archive/08-代码审计报告-20260603.md:570:| P3-08 | `src/orchestrator/subagent-coordinator.ts:190-213` | `ExpertAutonomyEngine` 在协调器内直接 `new` | 改为依赖注入 |
docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D791-line3-report-onepager-20260917.md:481:| `src/agent/report-assembler.ts `（改）+ 两个新 `src/agent/ ` 文件 | **L2 编排** | 报告装配与派生是编排职责；依赖注入（store/findings 由 L1 传入）+ 纯函数便于单测；L2→L3（模板 registry）与 L2→cycles（先例 `src/agent/cycle-snapshot-service.ts :13 `）均合法 |
docs/plans/codex/implementation/SYNOVA-IMPL-D21-action-closed-loop-v1-20260722.md:123:- ActionStore 的 GraphStore 依赖注入（构造函数接受 store 参数）
docs/plans/codex/implementation/SYNOVA-IMPL-DSH-D551-ga-calibration-backend-20260828.md:136:| feedback_log ← interactive-card handleDismiss | 存疑（依赖注入参数） | src/agent/interactive-card.ts L267-277 |
docs/plans/codex/implementation/SYNOVA-IMPL-D72-Proposal引擎-三选一确认-v1-20260714.md:112:[ ] 消费D71 goal-store的createGoal函数（依赖注入）
docs/synova/research/研究院交接-20260922/双DSH执行提升清单.md:45:**证据**：DSH 的 `ga-calibration.ts`（动态 import + `getFeedbackCollector()` 活单例，盲评 R5=3/3.5）与 `customer-config-package.ts`（私有函数直连 fs 无注入点，R5=3.5）测试须起真实环境；而 **Win 的 `loop-handlers.ts` 有全场最佳的可测试性范式**——6 组 `setXxxDeps` 接口 + setter 注入 + 传 null 恢复生产默认（盲评 R5 高分，4/4.5）。
docs/synova/research/研究院交接-20260922/双DSH执行提升清单.md:47:**提升**：把 Win 的 deps-interface + setter 注入提炼为仓库标准模式（吸收被替换者的长处）；新模块验收加一条「单测无需真实环境/mock 即可跑」。
docs/synova/research/研究院交接-20260922/双DSH执行提升清单.md:112:| 3 | deps-interface 注入模式立为标准 | P2 | 半天（模板） | 规则 |
docs/synova/research/研究院交接-20260922/DSH-agent-team与Win侧Claude-Code质量对比.md:128:- WIN `loop-handlers.ts`（716 行）：实为规整的 5 组 setDeps/Handler 依赖注入模板，可测试性设计良好；写后复读验证是全场最成熟的可靠性工程。
docs/synova/research/Harness研究与Synova战略再定位-20260816/第一章-Harness全包族深度解构-20260816.md:39:3. **依赖注入**：`inject` 声明所需服务，加载顺序由服务依赖表达，非手动 boot 排序
docs/synova/research/Harness研究与Synova战略再定位-20260816/附录-证据清单-20260816.md:12:| H-02 | Cordis 五思想（插件/上下文/依赖注入/类型化事件/可逆副作用） | docs/cordis-primer.md | 强 |
docs/synova/research/archive/2026-05-06/SYNOVA-知识库权限对话-KnowledgeAuthChat-20260605.html:68:<p>权限管理是<strong>横切关注点</strong>，以独立包 <code>@synova/engine-auth</code> 存在，各层通过依赖注入使用，不归属任何单一层。</p>
docs/synova/research/archive/2026-05-06/SYNOVA-知识库权限对话-KnowledgeAuthChat-20260605.html:469:通过依赖注入切换，不修改检索逻辑。</pre>
docs/synova/research/official-electron-kernel-runtime.md:917:7. **官方测试资产未展开**：`apps/desktop/tests/` 有 75 个 `*.spec.ts`，本件只在 A-16 反向举证时引用了测试夹具 2 处；其测试手法（如 `main-startup.spec.ts` 的依赖注入夹具）本身可能是高价值借鉴项，**未纳入本次 39 条**。
docs/synova/research/研究院交接-20260918/任务书-基础设施与信任层.md:157:| cordis 的 `Service` 基类 + 子 fiber + `inject` 依赖注入 | 我方无 cordis；引入即破坏 G1，且我只用到「注册表 + 子作用域 + disposer」三件事 | `dsh-invariants/lib/index.js:43,88-114,96` |
docs/synova/research/DSH迁移施工图-20260820/附录A-DSH-224包处置表-20260904.md:18:| cordis | 元框架：插件生命周期、依赖注入、事件总线 | 基建-无需 | DSH"一切皆插件"的地基；不引依赖，用壳时获得 |
docs/synova/coordination/裁定-双DSH提升清单-20260922.md:76:| **C2** | P2-3 deps-interface 注入模式立为标准 | 立卡（半天，模板） | dev-doc 线 | — | 新模块默认采纳后，模板条目可并入通用规范 |
docs/synova/coordination/CTO履职总账-旧CTO-4d509874-提取20260909.md:328:2. **Cordis 五思想**（插件/上下文/依赖注入/类型化事件/可逆副作用）+ 四种事件分发（emit/waterfall/parallel/serial）构成"时空可组合性"范式——插件卸载后副作用随之撤销（时间），依赖变化时重建协作（空间）。Synova 是 L1-L5 静态分层，**无对应（❌）**，这是最根本的范式差距。
docs/synova/archive/2026-05-06/SYNOVA-全量静态代码分析报告-20260608.html:304:<h3>3.3 依赖注入评估</h3>
docs/synova/audit/audit-report-20260605.html:386:  <p><strong>建议：</strong>改为依赖注入，通过构造函数传递实例。保留单例仅用于日志和配置。</p>
docs/07-TEST-STRATEGY-20260605.md:281:// 运行时验证关键模块是否通过依赖注入创建

$ grep -rniE "setXxxDeps|deps-interface|依赖注入" docs/ .claude/skills/ --exclude-dir=.synova-wt-\* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l
      21
```

**结论行 C-4:** 共 **21 处**，**`.claude/skills/` 内零命中**（即 14 个技能里**尚无任何**提到 deps-interface/setter 注入标准 → D925 标准落点是**空白**，非冲突）。21 处分布：`双DSH执行提升清单.md` 3 处（L45/L47/L112，**本卡来源**）、`DSH-agent-team与Win侧Claude-Code质量对比.md` 1 处（L128，**与本卡数字矛盾**）、`裁定-双DSH提升清单-20260922.md` 1 处（L76，落地卡）、其余 16 处是**历史审计/研究/dev-doc 里的「依赖注入」泛提**（含 1 处 `docs/archive/` 归档件、2 处 `.html` 报告、1 处 cordis 对比研究），**均无 `setXxxDeps` 或 `deps-interface` 字面量**（仅匹配到「依赖注入」中文词）。⇒ D925 若新增模板条目，**无既有条目需合并或去重**，但需注意 `docs/archive/` 与 `.html` 报告不应作为写集对象。

---

# 汇总：三条最关键发现

### 发现 1（A/D923）— **K3 门禁须从零建；但「从零」不等于「无地基」，且前置条件与裁定件记载不符**

- 「K3 审计结论 → 合并放行」机制实测 **不存在**：47 处关键词命中逐处判定 = 注释 15 / 簿记展示 22 / owner 域校验 4 / **反向豁免 3** / 未接线聚合器 12，**真门禁 0**。
- 现存唯一耦合**方向相反**：`verify-parallel.sh:191,208-209` 用「audit-reports 里有该 D# 报告」当**写集豁免**（放宽）信号，不是放行前置。
- 最接近的地基 = **`pre-audit-summary.sh`（166 行，5 门禁聚合 + verdict 三态 + exit 0/1/2）**，但在 `ci.yml` / `pre-push-check.sh` / `.claude/settings.json` 中引用数**全为 0** → 未接线（M3 同族）；D923 须回答「复用/接线它 还是 新建」。
- **三类路径（安全/财务计算/数据迁移）机器可识别证据 = 0**：全域 1 处命中还是 `check-citations.py:68` 的搜索根白名单。现存唯一分类器 = `ownership.yaml` 的 glob→owner（mac/win/k3），**无 path→category**。⇒ 分类器须从零建。
- **前置条件（裁定件 L58-60「5 份报告卡在分支上没进 main」）与实测不符**：D821/D858复审/D862/D864复审/D865复审 **5 份全部已在 main**（audit-reports tracked 140）；但 **verdict 回填仅 3/5**（D862/D864 仍 `claimed`）。且报告 verdict **无结构化字段**，只有散文 + `gen-cto-health.py:326-330` 的子串匹配。

### 发现 2（B/D924）— **标记分割存量 = 1 处（不是普遍形态），但真风险在「误报」而非「漏拦」**

- 真形态存量 **1 处 / 1 文件**：`scripts/golden-scenarios/common/assert.ts:101,109`（与裁定件 §二 描述逐字一致）。
- 84 处标记类命中里 **81 处是 bash here-string `<<<`**；**22 处 `%{http_code}` 是正确分离形态**（`curl -o file`/`-o /dev/null` 已把 body 与状态码分流）→ **是正面样板，不是待改对象**。
- ⇒ 「存量清单化 + 新增即拦」**可行（基数 = 1）**，但**判据若只做 grep，会产出 103 处误报（81 + 22）**，正撞坑清单「禁 grep 型静态判据当验收」。
- 承载点约束：`scripts/control-tower/` = 26 `.sh` + 26 `.py` + 3 `.ts`；**`tsconfig.json` include 仅 `src/**`（L29）→ 新增 `scripts/control-tower/*.ts` 默认不被 tsc 纳入**（现存 3 个 `.ts` 靠 `src/` import 传递可达）；`.sh` 检查器测试还须**手工加进 `ci.yml:225-266` 硬编码清单**（41 条 vs 目录内 122 个测试文件）否则永不运行。

### 发现 3（C/D925）— **setter 注入实测 5 组，派单前提「6 组」不成立；第 6 席是缺口（不是被漏计）**

- `export function set[A-Za-z]*Deps` 全仓 = **5**（非 6），全部在 `src/agent/loop-handlers.ts`：L105 / L294 / L444 / L560 / L617。
- `git log --all` 全历史核查：该文件**从未**有过第 6 个（唯一引入提交 `2b6aaad6`，subject 自陈「**五处理器**」）；其他注入缝（`ForTest`/`__set`/`configureDeps`/`injectDeps`）**全仓 0 命中**。
- 「6」的来源 = **handler 个数**：6 个 `default*Handler`，仅 5 个配 setter；**缺口 = `defaultEvolutionHandler`（L374）**，它直连 `getFeedbackCollector()` 活单例（L377）—— 正是研究清单同段批评 DSH 的反模式本身。
- **同源研究件自相矛盾**：`双DSH执行提升清单.md:45`「**6 组**」vs `DSH-agent-team与Win侧Claude-Code质量对比.md:128`「实为规整的 **5 组**」。
- 落点现状：`.claude/skills` / `.dsh/skills` 各 14 且 `diff -r` **零差异**；`deps-interface|setXxxDeps|依赖注入` 在 `.claude/skills/` **零命中**（标准落点为空白，无冲突）；但 D925 同时改双目录**必被 D370 G13 硬阻断**（`pre-commit-check.sh:1382-1400`，漂移/检查失败均 `hard_check`，且该组在本地软提示改革中被**明确保留为硬阻断**，L8-12）。

---

# 附录：前提核验对照表（派单/裁定 声称 vs 实测）

| # | 被核前提（出处） | 声称 | 实测 | 判定 |
|---|---|---|---|---|
| 1 | 裁定 `裁定-双DSH提升清单.md:58-60` | 5 份 K3 报告「至今卡在分支上**没进 main**」 | **5 份全部 IN MAIN**（`git ls-tree HEAD`） | ⚠ **前提不成立（已进展）** |
| 2 | 同上 | 「verdict 回填」待做 | **3/5 已回填**（D821/D858/D865=`audited`）；**D862/D864 仍 `claimed`** | ⚠ **部分成立（2 份未闭合）** |
| 3 | 研究 `双DSH执行提升清单.md:45` + 本卡派单 | `loop-handlers.ts` 有 **6 组** `setXxxDeps` | **5 组**（L105/294/444/560/617）；全历史从未有过 6 | ❌ **前提不成立** |
| 4 | 同源研究 `质量对比.md:128` | 「实为规整的 **5 组**」 | **5 组** | ✅ 成立（**但与第 3 项自相矛盾**） |
| 5 | 裁定 `裁定-双DSH提升清单.md:32` | `assert.ts` 标记字符串分割活证据 | L101 + L109 逐字一致 | ✅ 成立 |
| 6 | 裁定 `裁定-双DSH提升清单.md:74` (B4) | 「全仓无『标记字符串分割混合流』后，规范转为 lint 规则」 | **存量 1 处**（assert.ts），其余 81 here-string + 22 分流形态**非本形态** | ⚠ 声称隐含「当前有存量」，实测**仅 1 处**（B4 失效条件近乎已达） |
| 7 | 裁定 `裁定-双DSH提升清单.md:34` | `conflict-scanner.ts:125` 用 `exitCode`（对照 Win 长处） | 文件存在（`scripts/control-tower/conflict-scanner.ts`） | ✅ 文件在（行号未逐字复核，超本波范围） |
| 8 | 派单件 D923/D924/D925/D926 四份 brief | （应含 Q0-Q3 填写内容） | **全部 36 行、13 处 `<...>` 占位符、未填写**（未跟踪） | ⚠ **brief 为空骨架**（写集声明尚未落纸） |
| 9 | 裁定 `裁定-双DSH提升清单.md:77` (D1) | D1 = 「立卡（1 天）」 | 现存无任何可复用 K3 门禁基建（见发现 1） | ⚠ 1 天估算**无实测依据支撑**（分类器 + 门禁 + 报告结构均须新建） |

---

# 附录：本次侦察的只读性证明与边界

- **写入**：仅本文件（`docs/synova/product-lines/evidence/D922-phase0-recon-20260923.md`），新建。
- **未执行**：`git add` / `git commit` / `git push` / `git checkout` / `git stash` **零执行**；红证夹具（`INJECTED` 前缀标记）**未创建**（本波无需使用；本条刻意不写完整标记字面量，以免本文件自身触发该 ratchet 检查）。
- **只读性物理证明**：`git status --porcelain | grep -vE "^\?\?" | wc -l` → **0**（零个已跟踪文件被修改）；唯一新增 = 本文件。
- **未跑重型验证**：无 `vitest`、无全量 `tsc`、无任何门禁脚本全量调用。仅 `grep` / `ls` / `sed` / `git`（只读子命令）/ `wc` / `diff` / `printf`。
- **命令限定**：全部加 `--exclude-dir=.synova-wt-* --exclude-dir=.sessions --exclude-dir=node_modules --exclude-dir=.git`；无裸 `find .`（唯一 `find` 均带 `-maxdepth` + 排除主树 200+ 工作树）。
- **零命中结论均带正向对照**：A-5（45 + 4224）、A-3-b（88）。
- **本文不含**「通过 / 不通过」判定——只陈述事实与实测值。是否放行归 CTO 收件闸与 K3 终审。
