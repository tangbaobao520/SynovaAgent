# D922 Phase 0 — 独立自验记录（自验员报告，逐字转录）

> **本文件的落盘说明（队长如实登记）**：自验员 `verify-d922` 的写集被纪律限制为 `/tmp`（"自验只读仓库，可写 /tmp"），**它不能写入仓库**。故由**队长**把其报告的**逐字原文**转录落盘；**结论行未改写、未润色**。凡队长补充的说明，一律以「【队长补注】」前缀标出，与自验员原文区分。
>
> - 自验员：`verify-d922`（独立成员，**非编码成员、非队长**）
> - 自验轮次：**第 1 轮 → 结论「退回」**（理由 5 条，见 §1）
> - 被验对象修订：总计划 `479 行 / sha256 e308dea41beccfefcce311fb4d6ebf6922427029435442e756c0f7b7e5f5f5df`（mtime 2026-09-23 16:09）——该修订**已过期**（自验后队长又按其退回意见修订，见 §2）
>
> 【队长补注】第 2 轮回验记录追加在本文件 §3（终态冻结版 hash 见 §3）。

---

## §1 自验员报告（第 1 轮，逐字原文）

自验完成。以下是独立复算结果（全程只读仓库，写集仅 `/tmp`；未执行 `git add/commit/push/checkout/stash`，未跑 vitest/tsc/全量门禁）。

### 0. 基线与钉版（先说清"我验的是哪个字节"）

```
main tip       6c23006a1a0c3d93cca4b6ec762ad588c0874dd2  refs/heads/main
ls-remote 回执 c6b6c36574873c6dcc76586fb383a3bb1404a360  refs/heads/docs/d922-w1-dispatch
```
总计划文档在**自验期间被改动过**（56901B/477 行 @16:07 → 58514B/479 行 @16:09），故钉版：
```
479 行 | sha256 e308dea41beccfefcce311fb4d6ebf6922427029435442e756c0f7b7e5f5f5df | mtime 2026-09-23 16:09
evidence/recon  sha256 2404b55a…（主树与 lead 树逐字节相同）
evidence/probes sha256 b71b7804…
```

### 1. C1–C15 判定表

| # | 判定 | 关键原始输出摘要 |
|---|---|---|
| C1 | **相符（带保留）** | `task-state/` 内 D922–D925 四卡存在（`status=claimed`，`audit=null`，`updated_by=alloc-task-id`）；无任何 `B1/B2/B3/C1/D2/D3.json`，`task_id` 亦无匹配 → 六卡确无实体卡。**保留**：4 份卡在主树是 `??`(untracked)，main 只到 D921 |
| C2 | **相符** | API：`state=open merged=False head=docs/d922-w1-dispatch c6b6c365 base=main changed_files=14`；`git merge-base --is-ancestor c6b6c365 origin/main` → `NOT-ancestor(unmerged)`；`git ls-tree origin/main \| grep 派单-W1` → exit 1（不在 main） |
| C3 | **相符** | 5/5 在 main：`2026-09-21-D821.md`、`-K3-D858-复审.md`、`-K3-D862.md`、`-K3-D864-复审.md`、`-K3-D865-复审.md` |
| C4 | **相符** | D821/D858/D865 `status=audited` + `audit.verdict=PASS`；D862/D864 `status=claimed` + `audit=None`（3/5 回填） |
| C5 | **相符** | `enforce_admins={'enabled':True}`、`required_approving_review_count=0`、`num_contexts=12`（含 `npm audit`）；`ci.yml:314 continue-on-error: true`，`ci.yml:317 npm audit … \|\| echo` |
| C6 | **相符** | `control-tower-tests`（`ci.yml:202`）用显式 `for t in` 清单，**42 条**（control-tower 36 + doc-system 6），无 glob；`ls tests/control-tower/*.test.sh`=**102**，doc-system=**8** → 未登记 66 + 2 |
| C7 | **相符（路径需更正）** | `wc -l` = **166**；`ci.yml`=0、`.claude/settings.json`=0、`scripts/pre-push-check.sh`=0（**表里给的 `scripts/workflow/pre-push-check.sh` 不存在**，真路径在 `scripts/`）；`git grep pre-audit-summary \| wc -l`=**38**（全部为自身/测试/历史 brief/文档/task-state note，无一调用方）→ "未接线"成立 |
| C8 | **相符** | `git grep -nE "split\('__"` → **1 处**（`scripts/golden-scenarios/common/assert.ts:109`；`:101` 是同一形态的 `-w '\n__STATUS__:%{http_code}'`）；误报面 `<<<`=**82**、`http_code`=**42**、`curl -o/--output`=**38** |
| C9 | **相符** | 接口 **5**（`:91/:287/:435/:553/:609`）、setter **5**（`:105/:294/:444/:560/:617`）、handler **6**；`defaultEvolutionHandler` 在 `:374` 无 setter，`:377` 直连 `getFeedbackCollector()`；放宽 `ForTest\|__set\|configureDeps\|injectDeps`=**0** |
| C10 | **相符** | `check-ownership.py --quiet <7 路径>` → `✅ PASS 7 个文件同域: mac（无归属 0，域判定豁免 0）` exit=0 |
| C11 | **相符** | `ls -d extensions/sentinels/capital-*/computes` → 仅 `capital-health/computes`；`capital-efficiency/computes` 不存在（另有历史件在 `_extinct/capital-efficiency/computes`） |
| C12 | **相符** | 三份 brief 各 **36 行**、各 **14 个 `<...>` 占位符**（13 行含占位符），Q0/Q1/Q2/Q3/架构层/Done 全空；且**均 untracked** |
| C13 | **相符** | `tsconfig.json:29 include=["src/**/*.ts","src/**/*.tsx"]`；`vitest.config.ts:28 include=['./tests/**/*.test.ts',…]`；`scripts/**/*.ts` 9 个文件两网皆不入 |
| C14 | **相符** | `check-pr-budget.sh:104 GOV_PREFIX_RE='^(\.claude/task-briefs/\|task-state/\|memory/notes/\|docs/plans/\|docs/synova/product-lines/evidence/)'`；`grep coordination` → exit 1（不在豁免内） |
| C15 | **相符** | `sync-dsh-skills.sh --check` → `SYNC-OK: 技能一致（… 14 个技能）` `exit=0`；`diff -rq .claude/skills .dsh/skills` → 无差异（14/14） |

**C1–C15 无一"不符"、无一"无法判定"。**

### 2. 前提不成立清单

队长自己已具名的（我复算全部成立）：P-04（6→5，实测 5 接口/5 setter）✓、P-01（派单件未落 main）✓、P-08（`npm audit` 绿腿先例）✓、P-20（`capital-efficiency` 陈旧指针）✓、P-24（brief 空骨架）✓、P-26（`verify-parallel.sh` 反向耦合：`_is_closed_doc()` 以 `status=audited` 或 `audit-reports/*-D#*` 任一命中即豁免写集）✓。P-21/P-22 我用 `/tmp` 探针逐字复现：④ **只报行首那条**（行中 `aaa-midline.sh`/`ccc-midline2.sh` 不被 ④ 检查），⑨ 输出 `⚠️ 疑似互斥: …ddd-mutex.sh 同时出现「必红/非零」与「变绿/exit 0」——请人核` ✓。

**我新发现的（队长未列）**：

- **N1（高）"本件过门禁 exit 0" 不可复现。** DS8 声称 `本件通过 pre-dispatch-check.sh`，附录 B-3 记 `违规 0 / [exit=0]`。我在**同一工作树、同一命令**独立复跑：16:07 版 → `引用核验: 62 条 / 通过 47 / 待建声明 12 / **违规 3**` **EXIT=1**；16:09 版（当前）→ `64 条 / 通过 51 / 待建声明 12 / **违规 1**` **EXIT=1**；换到主树跑 → `64 条 / 通过 43 / **违规 9**` **EXIT=1**。**两个工作树都跑不出 0**。且 B-3 自相矛盾：其"修订前"记录了 `:116 → task-state/D9xx.json`，而"修订后"同一行号同一引用却记 `违规 0`；其自记算术 `49+12+0=61 ≠ 62`。另 DS8 的引用目标是"§五 原始输出（exit code 见下）"，而 §五（`424-438`）**零个代码块、零 exit code**——输出实际在另一份文件的附录 B-3。
- **N2（高）交付物无一处落库/落远端。** 三份 Phase-0 产物 `总计划-双DSH提升-W1波-20260923.md`、`D922-phase0-recon-20260923.md`、`D922-phase0-lead-probes-20260923.md` **全部 UNTRACKED**；lead 树所在分支 `docs/d922-w1-master-plan` **远端不存在**（`ls-remote \| grep d922` 只有 `docs/d922-w1-dispatch`）；recon 文件曾只存在于**主工作树**（M1 专用工作树纪律）。⇒ 给不出 M6 要求的"提交进仓库 + 路径"，也给不出 `git ls-remote` 回执。
- **N3（中）DS3 的核验命令不核验。** DS3 声称"三卡预算 5/5/7"并给命令 `check-pr-budget.sh --base origin/docs/d922-w1-dispatch` ——我实跑该命令：`✅ ① 变更文件数 0 ≤ 上限 12 … ✅ PASS PR 预算内（0 文件）`。**该命令恒读不到三卡写集**（即坑清单"检查未执行 == 检查通过"形态）。5/5/7 本身是真的（见 §3，我用 `--files` 注入实测）。
- **N4（低）P-20 数字 off-by-one。** 声称"**48 个** sentinel 目录"；实测 `ls -d extensions/sentinels/*/`=**47**、`git ls-tree -d origin/main:extensions/sentinels`=**47**（48 是含 `manifest.json` 的条目数，或 `find … -type d` 把父目录自身算进的数）。
- **N5（低）交付未冻结。** 自验期间文档被改动（477→479 行、+1613B），行号漂移（原 `:459/:461` → 现 `:461/:463`）——"可被独立验证"的前提是字节级冻结。
- **N6（提示，非队长错）** 坑清单写"红证不残留（`grep -c INJECTED-RED`=0）"，实测字面 grep 有 **2 行**命中，均为散文（`docs/synova/audit-reports/2026-09-20-K3-D854.md:40` 叙述该注入、`presets/synova-squad-lead/SYSTEM-PROMPT.md:29` 规则原文本身），**非活夹具残留**；恰好印证"grep 型静态判据"会误报。

### 3. 三卡计数实测（D860 口径，机器计数 + 我手算一致）

| 卡 | 计件 | 豁免件 | 域 | 原始输出 |
|---|---|---|---|---|
| D923 | **5** | 4 | mac | `ℹ️ D860 治理产物豁免: 4 件` / `✅ ① 5 ≤ 12` / `✅ PASS 5 个文件同域: mac` |
| D924 | **5** | 4 | mac | `✅ ① 变更文件数 5 ≤ 上限 12` / `✅ PASS 5 个文件同域: mac` |
| D925 | **7** | 4 | mac | `✅ ① 变更文件数 7 ≤ 上限 12` / `✅ PASS 7 个文件同域: mac` |

手算口径核对：D923=`k3-merge-gate.sh`+`k3-gate-policy.yaml`+`k3-merge-gate.test.sh`+`coordination/门禁说明….md`+`ci.yml`；D924=`check-subprocess-protocol.sh`+`subprocess-protocol-baseline.txt`+测试+`coordination/规范….md`+`ci.yml`；D925=`.claude`+`.dsh` 两份 `SKILL.md`+`coordination/标准….md`+`check-deps-injection.sh`+`deps-injection-baseline.txt`+测试+`ci.yml`。全部不落 5 个豁免前缀（`.claude/skills/`、`.dsh/`、`scripts/`、`tests/`、`docs/synova/coordination/` 均**不**在豁免内），故均计数。**与文档 §4.3 的 5/5/7 一致**。冲突扫描亦复算为 0（`k3-merge-gate`/`k3-gate-policy`/`check-subprocess-protocol`/`subprocess-protocol-baseline`/`check-deps-injection`/`deps-injection-baseline` 六个名字 repo 全域 `git grep -Il`=0）。

### 4. 引用抽查（≥5 条 `path:line`，逐条实读）

| 引用 | 实读结果 | 判 |
|---|---|---|
| `scripts/control-tower/gen-cto-health.py:326-330` | `:326 # D395a verdict 优先序不变`、`:327 if "CONDITIONAL PASS" in txt` → `:330 elif "PASS"` | 真 |
| `scripts/pre-commit-check.sh:1382-1389` | `:1382 组 13/13 技能同步一致性`、`:1384 fail-closed (D328)`、`:1388 bash …/sync-dsh-skills.sh --check` | 真 |
| `ci.yml:278` | `:278 bash scripts/control-tower/check-canary-drift.sh \|\| true` | 真（逐行命中） |
| `ci.yml:314` | `:314 continue-on-error: true`（audit job） | 真 |
| DS4"3 处引用 control-tower-tests" | `ci.yml:202`、`check-canary-drift.sh:34`、`simulate-ci.sh:13` —— `git grep` 恰好 3 条 | 真 |
| `verify-parallel.sh:185-212` `_is_closed_doc()` | `:204` 起 `grep -qE '"status"…"audited"'`；`:210/:211` `ls audit-reports/*-"$did"-*.md`/`*-"$did".md` → `return 0` | 真 |
| `loop-handlers.ts:374/:377` | `:374 export async function defaultEvolutionHandler`；`:377 const signals = getFeedbackCollector().getAggregatedSignals()` | 真 |
| 计划锚定 `v1.2@4e46603f` | `shasum -a 256 整体推进计划-主线-20260913.md` = `4e46603facae…` | 真 |
| `ci.yml` sha256 `6a5eacc4…` | `6a5eacc43348…`；`wc -l`=331 | 真 |

**抽查 9 条，9 条为真，0 条虚引。**

### 5. 自验结论（第 1 轮）

**退回（附理由）** —— 技术实体（C1–C15 全相符、三卡 5/5/7 单域 mac 我手算+机器双证、9/9 引用可核验、P-04/P-20 等前提纠错均成立）**质量很高**，退的是**交付载体与两条 DS 的诚实性**：① DS8/附录 B-3 声称的"过门禁 exit 0 / 违规 0"在**两个工作树、两个版本**上都复现为 **exit 1**（违规 3→1→9），且 §五 无任何原始输出；② 总计划 + 3 份 evidence **全未提交、master-plan 分支未推送**，recon 文件曾只存在于主工作树，给不出 M6/证据可核要求的路径与 `ls-remote` 回执；③ DS3 的核验命令实为恒空转（`0 文件`）；④ P-20 "48 个目录"实为 47；⑤ 自验期间文件仍在变（未冻结）。退回**只约束本件（Phase 0 交付载体）**，不冻结 W1 技术方案——但请注意 E1（PR #719 合并）仍是三卡的真硬前置，`docs/d922-w1-dispatch` 至今 `state=open merged=False`。

一句话给 CTO 的最小修复面：把三份产物 commit 进 `docs/d922-w1-master-plan` 并 push（补 ls-remote 回执）→ 重跑并**逐字**贴 `pre-dispatch-check.sh` 输出（当前剩余违规唯一来源是 `:463` 指向 `D922-phase0-verify-20260923.md`，该文件按我的只读纪律我**不能**写入仓库，须由 CTO 决定谁来落）→ 修 DS3 命令为 `--files` 注入式 → 改 P-20 的 48→47 → 对齐 DS8 的证据指针。

---

## §2 队长对第 1 轮退回的处置（逐条，可核）

| 项 | 处置 | 证据位置 |
|---|---|---|
| **N1**（假 exit 0） | **全部采纳**。删除预写的"修订后 exit 0"块；§五 与附录 B-3 一律改为**实跑终态输出 + 真实退出码**（由命令捕获，不手写） | 总计划 §五（C）＋附录 B-3 |
| **N2**（未落库未推送） | **全部采纳**。三份产物 + verify 本文件 commit 进 `docs/d922-w1-master-plan` 并 push；附 `git ls-remote --heads origin \| grep d922` 回执 | 总计划 §六 6.4 |
| **N3**（DS3 空转） | **全部采纳**。DS3 改为 `--files` 注入式；§4.3 贴三卡逐卡原始输出（**该命令在本件上读 0 文件恒过 = 未核验**，已写入 DS3 正文） | 总计划 DS3 + §4.3 |
| **N4**（48→47） | **全部采纳**。P-20 改为实测 **47**，并注明 48 的口径来源（`find -type d` 含父目录） | 总计划 P-20 |
| **N5**（未冻结） | **部分采纳**。第 2 轮回验前先冻结（记 sha256），第 2 轮结论追加在 §3；**自验后仍会有一次「转录落盘」动作**（本文件由队长落盘，非自验员），该动作不改总计划正文 | 本文件 §3 |
| **N6**（grep 误报提示） | 记录不处置（非本件缺陷；已印证"grep 型静态判据"坑） | 本文件 §1 |

【队长补注】退回只约束本件（Phase 0 交付载体），不冻结 W1 技术方案——与自验员口径一致。
