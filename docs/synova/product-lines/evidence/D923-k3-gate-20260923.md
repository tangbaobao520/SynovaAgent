# D923 证据 — K3 必经合并门禁

> 卡号 D923 | 分支 `feat/d923-k3-merge-gate` | base `feat/d925-deps-interface`（栈式，已声明） | 域 mac
> 测量时刻: **截至 2026-09-23T20:1x+0800**（各处另注）
> 说明文档: [门禁说明-K3必经门禁-20260923.md](../../coordination/门禁说明-K3必经门禁-20260923.md) | 政策: `scripts/control-tower/k3-gate-policy.yaml`

---

## §1 声称 ↔ 证据（**含每条理由的依据**，CTO 裁定 7-新）

| # | 声称 | 命令 | 原始输出 |
|---|------|------|---------|
| S1 | 判定器语法合法 | `bash -n scripts/control-tower/k3-merge-gate.sh` | `exit=0` |
| S2 | 夹具 **29/29**（原 23/23 + 收尾新增缺陷 A/B 回归用例 15/16 + L9 回归用例 17） | `bash tests/control-tower/k3-merge-gate.test.sh` | `D923-FIXTURE: PASS(29/29)` `exit=0`（截至 2026-09-23T21:4x+0800） |
| S3 | 本卡变更**不在三类内** ⇒ 门禁对本 PR 判绿 | `bash …/k3-merge-gate.sh --base origin/main --head HEAD --pr 999` | **修正（收尾实读核验，见 §6.1）**：原记 `three_category_hits=0 → PASS` **在 tip `bf92b4d5` 不可复现**——实测 `hits=2 → REJECT`，`REAL_RC=1`。根因＝该数在 `HEAD=2b9bfddc`（门禁两文件**尚未提交**）时测得；`92cdd2a8` 提交后两文件进入 diff ⇒ pickaxe **自命中**。**任务 0 修复后复测：`three_category_hits=0 (glob=0 pickaxe=0)` → `K3-GATE: PASS`，`REAL_RC=0`** |
| S4 | D# 三路**可归因** | 同 S3 输出首两行 | `D# 归因 → ①分支名(feat/d923-k3-merge-gate)：[D923 ] ②brief 文件名：[D924 D925 ] ③PR 正文：[无] ⇒ 采用：[D923 D924 D925 ]` |
| S5 | 夹具**已登记** canary 第 45 条 | 见 §7.2（收尾任务 0.5 改 job 后行号已变） | **第 45 条 = `tests/control-tower/k3-merge-gate.test.sh`，`.github/workflows/ci.yml:305`**（canary 块 `for t in \` @ `ci.yml:260` → `; do` @ `ci.yml:305`，块内条目 45 条；**自验修复 F-4 后重测**：push 口径注释 +12 行致行号再下移；截至 2026-09-23T20:50+0800） |
| S6 | 新增 CI **job** 名（供 CTO 注册） | `grep -nE '^  k3-gate:|^    name: K3 Merge Gate' .github/workflows/ci.yml` | `126:  k3-gate:` / `127:    name: K3 Merge Gate`（step 名 @ `133`；自验修复 F-4 后重测） —— **required check context = `K3 Merge Gate`**（收尾任务 0.5 由 step 改为独立 job，见 §7） |
| S7 | **本卡不改产品代码** | `git diff --cached --name-only \| grep -c '^src/'` | `0` |
| S8 | 结论归一覆盖 10 种存量取值 | policy `verdict_normalization`（命令见其注释） | 逐取值映射；夹具用例 5/6/7/8 分别断言 CONDITIONAL PASS / NOT-AUDITABLE / NOT_K3 / 小写 pass |
| S9 | 小写 `pass` 归一**显式可见 + 具名** | 判定器 `::notice` 分支；卡号查询脚本 | 具名卡号：**D799 / D815**（`verdict='pass'`，2026-09-23 实测） |

### 理由的依据（policy 每条 reason 均带 evidence；此处列关键项）

| 判据/参数 | 为什么这样取 | 依据（命令 → 值） |
|---|---|---|
| A 取 `src/security/**` + 6 条精确枚举，**不取** `src/routes/**` 整包 | 整包 53 文件中仅 4 条与鉴权直接相关（误报 92%） | `git ls-files 'src/security/*' \| wc -l` → 13；`git ls-files 'src/routes/*' \| wc -l` → 53 |
| A 的 6 条枚举**逐条实测存在** | 防止写进不存在的路径 | `git ls-files --error-unmatch <每条>` → **6/6 EXISTS** |
| `packages/engine-auth/**` 纳入 | 该包实际存在且是鉴权实现 | `git ls-files 'packages/engine-auth/*' \| wc -l` → **7** |
| B 取五子域 `computes/**` 白名单 | 全量 computes 202 个里多为非财务；只有五子域是真财务 | `git ls-files 'extensions/sentinels/<d>/computes/*'` → 9/7/4/3/1（合计 24） |
| B **排除** `_extinct/**` | 退役代码：仅 type-only 引用，无运行时加载路径 | `git ls-files 'extensions/sentinels/_extinct/*/computes/*' \| wc -l` → 33；`git grep -n '_extinct' -- 'src/*' \| wc -l` → 12（全 `import type`） |
| C 用 **pickaxe** 判 DDL 而非静态含 DDL | 静态口径文件数随树变化（主工作区 98 / 本分支树 104），无法表达"本次是否改了 DDL" | `git log --first-parent -G'CREATE TABLE\|ALTER TABLE' main \| wc -l` → **60** 全历史；近 30 天 → **20**（**自验修复 F-1**：原记 99/28 复跑不出，截至 2026-09-23T20:50+0800 实跑为 60/20） |
| **pickaxe 必须限定生产路径** | 本卡实测：不加排除时**门禁自己的文档正文含 `CREATE TABLE` 字面量即被误命中**（首轮 `three_category_hits=1` 假红） | 实际命令加 `-- . ':!docs/**' ':!tests/**' ':!*.md'` 后 → 0 |
| `**/*migrat*` 的 pathspec 语义 | `**/` 在本仓是否生效需实测 | `git ls-files '**/*migrat*' \| wc -l` → **9**；`git ls-files '*migrat*' \| wc -l` → **9**（相同 ⇒ `**/` 为 no-op） |
| 20% 抽样率 | **CTO 裁定 3 的拍板参数**；**本仓无实测数据支持 20% 优于 10%（未实测）** | 可核部分：三类路径误伤率 **6.4%**（30/468，窗口 since 2026-08-24）见 `D923-三类路径名单实测 §四` |
| 抽样按 `pr%5==0` 而非随机 | 随机不可复现、无法审计 | — |
| 报告 D# 派生用**配方 B（首个 D#）** | 三配方三结果，必须钉死；B 与 `gen-cto-health.py:320` 同源且与 #723 证据 8 张逐张一致 | 配方 A=7 / **B=8** / C=9（本卡复算，见说明文档 §3.1） |

### 存量面（配方 B，本卡复算）

```
task-state 卡片 347 | 无结论 183 = 键缺失 2 + audit:null 181
  有报告文件 = 8  (D558/D651/D819/D839/D850/D854/D862/D864)
  无报告文件 = 175
verdict 非空取值 10 种；小写 pass = D799/D815；NOT-AUDITABLE = D397/D398
```

---

## §2 M6-1 `git diff --stat` 原始输出

```
$ git diff --cached --stat
 .github/workflows/ci.yml                           |  17 +-
 .../coordination/门禁说明-K3必经门禁-20260923.md   | 159 +++++++++++
 scripts/control-tower/k3-gate-policy.yaml          | 195 +++++++++++++
 scripts/control-tower/k3-merge-gate.sh             | 302 +++++++++++++++++++++
 tests/control-tower/k3-merge-gate.test.sh          | 157 +++++++++++
 5 files changed, 829 insertions(+), 1 deletion(-)
```

## §3 M6-2 自验结论

**可提请独立审计。** 依据：S1–S9 均有命令 + 原始输出；夹具 23 断言覆盖三路径/多结论形态/降级/变异体/抽样；
**两态先证**见 §5。**本结论不代表"通过"**——通过与否归 CTO 收件闸 + K3 终审。

## §4 M6-3 遗留清单

| # | 事项 | 处置 |
|---|------|------|
| L1 | **R-1 类口径**：#723 证据 §五/§三 C4 的 pickaxe 计数（60/20）与本卡复算（99/28）**配方不同**（本卡用 `--first-parent`） | 本卡 policy 已写本卡配方；建议 CTO 统一口径 |
| L2 | pickaxe 未覆盖 DDL 形态（`PRAGMA user_version` / TRIGGER / VIEW 等） | policy `known_misses` 登记为**候选漏报面（未逐形态实测）** |
| L3 | 常量驱动迁移漏报（`SCHEMA_VERSION`，`src/store/schema-migration.ts:19`） | policy `known_misses` 登记；消除需后续卡 |
| L4 | 20% 抽样率为**拍板参数**，无实测最优依据 | 已如实标注（未实测） |
| L5 | 存量 **175 张卡确无报告** + 本卡自身 `audit:null` | 说明文档 §6 登记上线顺序与处置建议 |
| L6 | 本卡未做 backfill（裁定 8） | 属后续卡 |
| L7 | branch protection 注册 | **由 CTO 执行**，本卡不碰 |
| L8 | ls-remote 回执随交付报告附 | 见交付报告 |

## §5 两态先证（裁定 2 / 4）

- **态①（CI 红）**：临时探针 `src/security/__k3gate_probe__.ts` → 三类命中 + NO_VERDICT → 红。贴合 CI 原始输出。
- **态②（PASS→绿分支）**：**沙箱夹具**证（`--state-dir <mktemp>` 放假卡 `verdict=PASS` + 同一三类变更集 → exit 0）——
  **不向仓库写假 verdict**（裁定 4）。夹具用例 4 即此断言。
- **态③（CI 绿·收尾）**：撤探针 → 变更集回到非三类 → CI 绿。
- **收尾断言三条**：`git status --porcelain` 空 ／ `git ls-tree HEAD src/security/__k3gate_probe__.ts` 无输出 ／
  `task-state/D923.json` 的 `audit` 仍为 `null` 且与 base 逐字节一致。

---

## §6 收尾任务 0：两处缺陷修复（d923-prep 接手，2026-09-23 21:0x+0800 起）

> 口径：本条全部数字来自命令原始输出；`REAL_RC` 为**未接管道**的真实退出码（管道会吞 `$?`）。

### 6.1 复现（修复前，tip `bf92b4d5`）

```
$ bash scripts/control-tower/k3-merge-gate.sh --base origin/main --head HEAD --pr 999
[k3-gate] mode=diff changed=30 three_category_hits=2
[k3-gate] D# 归因 → ①分支名(feat/d923-k3-merge-gate)：[D923 ] ②brief 文件名：[D923 D924 D925 ] ③PR 正文：[无] ⇒ 采用：[D923 D924 D925 ]
::error title=K3门禁::三类路径命中（2 项）且卡内无通过级 K3 结论：D923: 无 K3 结论 —— K3 排队中；…
[k3-gate] 三类命中清单：
K3-GATE: REJECT(…)
REAL_RC=1
```

- **缺陷 A（自命中）**：那 2 项**全部来自 pickaxe**，逐条为
  `scripts/control-tower/k3-gate-policy.yaml`、`scripts/control-tower/k3-merge-gate.sh` —— **门禁自己的文件**。
- **缺陷 B（不可归因）**：`三类命中清单` 后**为空**（只打印 glob 命中 `HIT3`，pickaxe 命中不在其中）。

### 6.2 S3 差异的根因（"提交顺序"，非报告不实）

```
$ for ref in 2b9bfddc 92cdd2a8 bf92b4d5; do echo "$ref pickaxe命中=$(git diff -G'<PAT>' --name-only origin/main..$ref -- . ':!docs/**' ':!tests/**' ':!*.md' | wc -l) 变更集=$(git diff --name-only origin/main..$ref | wc -l) k3文件已提交=$(git ls-tree -r --name-only $ref | grep -c 'scripts/control-tower/k3-'); done
2b9bfddc pickaxe命中=0 变更集=23 k3文件已提交=0
92cdd2a8 pickaxe命中=2 变更集=30 k3文件已提交=2
bf92b4d5 pickaxe命中=2 变更集=30 k3文件已提交=2
```

**结论**：作者报告的 `hits=0 → PASS` 在其测量时刻（`HEAD=2b9bfddc`，门禁两文件仍是**工作区未提交**文件）**为真**；
但 `92cdd2a8` 把两文件提交后，它们进入 `origin/main..HEAD` 的 diff ⇒ pickaxe 自命中 ⇒ `hits=2`。
**报告缺陷 = 未记录测量时的 HEAD**（"截至"缺失），导致同一命令在 tip 上不可复现。非虚报，属**时点漂移**（与裁定 10 同族）。

### 6.3 修复内容

| 文件 | 改动 |
|---|---|
| `scripts/control-tower/k3-merge-gate.sh` | ① pickaxe 排除面改为「硬编码 3 条 + policy `exclude_globs` 驱动」的**数组**（`PICK_EXCLUDES`）；② 规则抽取带**规则标签**（`A_security.globs` / `…exact_files` / `…extra_globs` / `B_finance.globs` / `C_storage.globs`）；③ `match3()` 命中时写 `MATCH_RULE`；④ pickaxe 保留**命中文件清单**（`PICK_HIT_LIST`）而非只留计数；⑤ 新增 `print_hits()`：**glob 与 pickaxe 逐条列出 + 标明规则名**，红/NO_CARD_ID 两出口均调用 |
| `scripts/control-tower/k3-gate-policy.yaml` | `C_storage.pickaxe` 新增 `exclude_globs` / `exclude_globs_reason` / `exclude_globs_evidence`（排除面 = `scripts/control-tower/k3-*`，**附 3 条可核命令**） |
| `tests/control-tower/k3-merge-gate.test.sh` | 新增用例 **15**（缺陷 A 回归）/ **16**（缺陷 B 回归） |
| `docs/synova/coordination/门禁说明-K3必经门禁-20260923.md` | §4 补记排除面 ④ 及其可核条件 |

**排除面的可核理由（不是"为变绿加白名单"）—— 2026-09-23T20:50+0800 实读（自验修复 F-3：原记数字/行号均与实测不符，已改）**：

```
$ git grep -cE 'CREATE TABLE|ALTER TABLE|CREATE INDEX|DROP TABLE|ALTER INDEX' -- 'scripts/control-tower/k3-*'
scripts/control-tower/k3-gate-policy.yaml:7          ← policy 7 处；checker **0**（零命中文件不出现在 -c 输出）
$ git grep -nE 'CREATE TABLE|ALTER TABLE|CREATE INDEX|DROP TABLE|ALTER INDEX' -- 'scripts/control-tower/k3-*'
scripts/control-tower/k3-gate-policy.yaml:98
scripts/control-tower/k3-gate-policy.yaml:103
scripts/control-tower/k3-gate-policy.yaml:105
scripts/control-tower/k3-gate-policy.yaml:132
scripts/control-tower/k3-gate-policy.yaml:134
scripts/control-tower/k3-gate-policy.yaml:140
scripts/control-tower/k3-gate-policy.yaml:148
$ git grep -nE 'sqlite3|better-sqlite3|\.prepare\(|\.exec\(|new Database|Database\(' -- 'scripts/control-tower/k3-*' | wc -l
2
$ git grep -nE 'sqlite3|better-sqlite3|\.prepare\(|\.exec\(|new Database|Database\(' -- 'scripts/control-tower/k3-*'
scripts/control-tower/k3-gate-policy.yaml:136:        - cmd: "git grep -nE 'sqlite3|better-sqlite3|\\.prepare\\(|\\.exec\\(|new Database|Database\\(' -- 'scripts/control-tower/k3-*' | wc -l"
scripts/control-tower/k3-merge-gate.sh:159:#      且无任何真实 DB 执行能力（探测 `sqlite3|.prepare(|.exec(|new Database` 命中 **2** 处，
$ git ls-files 'scripts/control-tower/k3-*'
scripts/control-tower/k3-gate-policy.yaml
scripts/control-tower/k3-merge-gate.sh
```

**7 处 DDL 关键字（全在 policy，判定器 0 处）逐处都是数据/散文**（YAML `cmd:` 证据串 ×3、`pattern:` 常量、`known_misses` 散文、本节说明）。
DB 探测的 **2 处命中均为本判据自身的文本（自指）** —— `policy:136` 的 cmd 串 + `checker:159` 的描述注释，**无任何真实 DB 调用**
⇒ 语义结论不变：**不可能产生真 DDL 变更**。
（行号会随 policy 编辑漂移；**改动本文件后须重跑上面两条 `-n` 命令并把输出原样更新**——本节即 F-3 的正面修复样本。）

### 6.4 修复后（同一条命令，同口径）

```
$ bash scripts/control-tower/k3-merge-gate.sh --base origin/main --head HEAD --pr 999 --verbose
[k3-gate] mode=diff changed=30 three_category_hits=0 (glob=0 pickaxe=0)
[k3-gate] D# 归因 → ①分支名(feat/d923-k3-merge-gate)：[D923 ] ②brief 文件名：[D923 D924 D925 ] ③PR 正文：[无] ⇒ 采用：[D923 D924 D925 ]
[k3-gate] verbose: pickaxe 排除面 = :(exclude)docs/** :(exclude)tests/** :(exclude)*.md :(exclude)scripts/control-tower/k3-*
K3-GATE: PASS
REAL_RC=0
```

### 6.5 判别性对照（证明修复不是"为变绿"，排除是承重的）

三组沙箱对照（`mktemp -d` 自建 git 仓，判定器 + policy 复制进沙箱）：

| # | 场景 | 期望 | 实测 |
|---|---|---|---|
| ① **阳性对照** | 真 DDL（`CREATE TABLE`）写入**非三类**路径 `src/adapters/x.ts` | pickaxe 必须命中 | `three_category_hits=1 (glob=0 pickaxe=1)`，清单逐条 + 规则名，`REAL_RC=1` ✅ |
| ② **阴性对照（排除面窄）** | DDL 写入 `scripts/control-tower/other-tool.sh`（**非 `k3-*`**） | 必须**仍命中** | `hits=2 (glob=0 pickaxe=2)`，两项均逐条列出，`REAL_RC=1` ✅ |
| ③ **修复目标 + 变异体** | DDL 字面量写入 `k3-gate-policy.yaml` + `k3-merge-gate.sh` | 修复后 = 0；**去掉 `exclude_globs` 后必须复红** | 修复后 `hits=0 / PASS / REAL_RC=0`；**变异体（policy 去掉 `exclude_globs`）→ `hits=2` 且逐条点名为这两个文件，`REAL_RC=1`** ✅ |

②证明排除**没有**扩大到 `scripts/control-tower/**` 整目录；③证明该排除**承重**（删掉即复红 ⇒ 缺陷真实存在）。

### 6.6 回归夹具

```
$ bash tests/control-tower/k3-merge-gate.test.sh
[15] 缺陷A回归: k3-* 自身含 DDL 字面量 → 不自命中
  PASS  exit (=0)
[16] 缺陷B回归: pickaxe 命中可归因（清单逐条 + 规则名）
  PASS  exit (=1)
  PASS  清单含 pickaxe 逐条 + 规则名 (hit=yes)

D923-FIXTURE: PASS(26/26)
EXIT=0
```

（修复前基线同为 `PASS(23/23)`；新增两用例后 **26/26**。用例 15 的判别性：去掉 policy `exclude_globs` 即失败。）
> **后续更新**：任务 0.5 又新增 L9 回归用例 17，夹具现为 **`PASS(29/29)`**（见 §7.4）。

### 6.7 本节遗留（交 CTO）

| # | 事项 | 状态 |
|---|---|---|
| L9 | `B_finance.exclude`（`extensions/sentinels/_extinct/**`）在 policy 中**已声明但抽取器未实现** —— 当前因 B 的 globs 是五子域白名单而**恰好不命中** `_extinct`，故为**惰性配置**（无当前影响） | **未修**（超出任务 0 范围），报 CTO |
| L10 | 排除面为「按命名空间 `k3-*`」而非「按能力判定」：未来若新增别的**承载 pattern 字面量**的门禁文件（不在 `k3-*` 下），仍会自命中 | 已在 policy `exclude_globs_reason` 写明准入条件 |
| L11 | 缺陷 A 的**根本面**是「判定器把自身产物纳入被判定集合」这一类问题；`scripts/control-tower/` 下其它门禁是否有同类自命中**未普查** | 报 CTO 决定是否立卡 |
| L12 | **D708 与 `Iron laws` 仍是 `quality` job 的 step** ⇒ 前一步失败时同样被 `skipped`（实测见 §7.1） | **不在本卡改**（队长裁定：报 CTO 归入口径卡） |
| L13 | `.claude/task-briefs/2026-09-23-D923-k3-merge-gate.md` 仍写「D708 同构 **step**」/「`quality` job 的 step」/「新增 **step** 名」——与改后的**独立 job** 不符。**实测当前行号 = `:23` / `:81` / `:108`**（本卡已改该 brief 的写集机器块与 Q2，行号上移；原注 :76/:103 已失效） | **未改**（task-9 范围限定为 4 处；brief 为开工时的冻结契约）；建议 CTO 决定补注，或以本文件 §7.2 为准 |
| L14 | `exclude_note` 引入的规则级排除仅 `B_finance` 使用；A/C 规则当前无 `exclude:` | 已知边界，无需动作 |

---

## §7 收尾任务 0.5：门禁由 **step** 改为**独立 job**（可注册 + 永远会跑）

### 7.1 问题（队长实测 + 我复现的 CI 证据）

改前 `- name: K3 merge gate (D923)` 缩进 6 空格 ⇒ 它是 `quality` **job 的 step**。两条后果：

```
$ .../actions/runs/35858062669/jobs   # tip d82bc484（改前）
 7. TypeScript check                          success
 8. Iron laws check                           failure    ← 唯一失败组 = D734 PR 预算（18 > 12）
 9. Verify parallel declaration (D540)        skipped
10. Merge write-set reconciliation (D708)     skipped
11. K3 merge gate (D923)                      skipped    ← 永远跑不到
```

① **无法注册**：branch protection 的 required check context 是 **job 名**，step 不是可注册单元；
② **静默缺席**：前置任一步失败 ⇒ 后续 step 全 `skipped`，违反裁定 3「永远会跑」。
（`always()` 只能治 ②，治不了 ① ⇒ 采用**独立 job**。）

### 7.2 改法（实读 `ci.yml:126-140`）

```yaml
  k3-gate:
    name: K3 Merge Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # gate 要算 merge-base / base..head diff（照 D708 所在 job 的写法）
      - name: K3 merge gate (D923)
        env:
          GITHUB_EVENT_PATH: ${{ github.event_path }}   # 供第三路 D# 归因读 PR 正文
        run: |
          bash scripts/control-tower/k3-merge-gate.sh \
            --base origin/main --head HEAD \
            --pr "${{ github.event.pull_request.number || 0 }}"
```

- **不挂 `needs:`**（不被上游 job 失败拖成 skip）
- **不加 docs-only 跳过**（required check 必须在每个 PR 上都有结论；纯文档 PR 无三类命中 ⇒ PASS）
- `fetch-depth: 0`；保留 `GITHUB_EVENT_PATH`
- 调用语义与 D708 同形：`--base origin/main --head HEAD --pr <event>` + 三态 0/1/2 + diff 口径
- **required check context（供 CTO 注册）= `K3 Merge Gate`**（job `name`；job id = `k3-gate`）
- 副作用：canary 块与第 45 条行号下移 12 行 ⇒ `for t in \` = `ci.yml:250`（原 238），第 45 条 = `ci.yml:295`（原 283）。
  **自验修复 F-4 后再次重测**（push 口径注释扩写 +10 行）⇒ `for t in \` = **`ci.yml:260`**，第 45 条 = **`ci.yml:305`**，job 片段 = **`ci.yml:126-140`**

### 7.3 L9 处置（`B_finance.exclude` 声明与实现一致性）

采 **(a) 实现它**（队长口径：声明与实现一致优先）：

- `k3-merge-gate.sh` 新增 `excludes()`（抽 policy `<规则>.exclude`）+ `glob_hit()` / `rule_excluded()`；`match3()` 命中后按**规则标签**过滤排除面
- 抽取器输出 `EXCL\t<规则标签>|<exclude-glob>`，实测：

```
$ python3 /tmp/extract.py scripts/control-tower/k3-gate-policy.yaml <files>
EXCL	B_finance.globs|extensions/sentinels/_extinct/**
```

- policy `B_finance.exclude_note` 记录：此前**已声明但从未被消费**（惰性配置），现按「声明即生效」实现
- **判别性回归用例 17**（宽 glob 沙箱下：`_extinct` 内变更**不命中** / 子域变更**仍命中**）；**变异体对照**（去掉 `rule_excluded` 分支 ⇒ 同一 `_extinct` 变更复红 `RC=1`）⇒ 排除承重，非装饰
- 注：在**当前**五子域 glob 下该排除本就不可达（子域路径与 `_extinct` 互斥），故对现行判据**无行为变化**；实现的价值是**消除声明/实现不一致**并在未来放宽 glob 时立即生效。

### 7.4 本节回归结果

```
$ bash tests/control-tower/k3-merge-gate.test.sh
D923-FIXTURE: PASS(29/29)
EXIT=0
```

（26/26 → **29/29**：新增用例 17 的两条断言 + 一条规则名断言。）
