# D924 证据 — 子进程输出协议规范 + 可执行检查器 + assert.ts 最小修复

> 卡号: D924 | 分支: `feat/d924-subprocess-protocol` | 工作树: `.synova-wt-d924` | 域: mac
> 基线 commit: `967a58e5`（= origin/main） | 测量时刻: **截至 2026-09-23T16:51:32+0800**（`date +%Y-%m-%dT%H:%M:%S%z`）
> 规范: [规范-子进程输出协议-20260923.md](../../coordination/规范-子进程输出协议-20260923.md)

---

## §1 声称 ↔ 证据（逐条）

| # | 声称 | 命令 | 原始输出摘要 |
|---|------|------|-------------|
| S1 | 检查器语法合法 | `bash -n scripts/control-tower/check-subprocess-protocol.sh` | `exit=0` |
| S2 | 夹具 20/20 全绿（含变异体判别） | `bash tests/control-tower/check-subprocess-protocol.test.sh` | `D924-FIXTURE: PASS(20/20)` `exit=0` |
| S3 | 全仓存量 0 | `bash scripts/control-tower/check-subprocess-protocol.sh --dir .` | `SUBPROCESS-PROTOCOL: OK (files=1931, violations=0, exempted=0, baseline=0)` `exit=0` |
| S4 | 修复**前**检查器**报红**（非事后编造） | 同上（在 assert.ts 修复前运行） | `VIOLATION  scripts/golden-scenarios/common/assert.ts:101  R1` + `:109  R2`；`SUBPROCESS-PROTOCOL: VIOLATION(2)` `exit=1` |
| S5 | 新文件有 UTF-8 头块 | `bash scripts/workflow/check-silent-swallow.sh --utf8 <新脚本>` | `[utf8] ✅ ... 带头块` `exit=0` |
| S6 | 无新增静默吞错 | `bash scripts/workflow/check-silent-swallow.sh --diff` | `[silent-swallow] ✅ 无新增静默吞错` `exit=0` |
| S7 | 夹具**已登记** CI（未登记=永不运行） | `sed -n '/for t in \\/,/; do/p' .github/workflows/ci.yml \| grep -cE '^\s+(tests\|scripts)/'` | `43`（登记前 42） |
| S8 | assert.ts 改动**只落在 runHttp** | `git diff --cached -- scripts/golden-scenarios/common/assert.ts` | 仅 `@@ -20,6 +20,7 @@`（+1 `import os`）与 `@@ -95,19 +96,35 @@`（runHttp 函数体）两处 hunk，文件其余部分零改动 |
| S9 | 旧哨兵形态在可执行源码面清零 | `git grep -nE '__STATUS__' -- '*.sh' '*.ts' '*.js' '*.py' '*.yml' \| wc -l` | `0` |
| S10 | 回归未被破坏（真实测试） | `npx vitest run tests/golden-scenarios/gss-common.test.ts` | `Test Files 1 passed (1)` / `Tests 13 passed (13)` `vitest_exit=0` |
| S11 | 裁定 4 行为证明：响应体含哨兵不再错乱 | 真实引擎红→绿实跑（见 §3） | 旧 `exit=1`（status=NaN、正文截断）→ 新 `exit=0`（6/6 通过） |
| S12 | body 字节级兼容（末尾恰 +1 LF） | 见 §3 字节级断言 A4/A5/A6 | 新旧两态在 `/plain` 上**同为** `...}\n\n`（A6 两态同过） |

---

## §2 验收链原始输出（完整粘贴，未 head 截断）

```
$ bash -n scripts/control-tower/check-subprocess-protocol.sh
exit=0

$ bash scripts/workflow/check-silent-swallow.sh --utf8
[utf8] ❌ 16 个 .sh 缺头块
exit=1
（16 处全部为**存量**：主工作区同命令同结论；见 §6 遗留清单 L2）

$ bash scripts/workflow/check-silent-swallow.sh --utf8 scripts/control-tower/check-subprocess-protocol.sh
[utf8] ✅ scripts/control-tower/check-subprocess-protocol.sh 带头块
exit=0

$ bash scripts/workflow/check-silent-swallow.sh --diff
[silent-swallow] ✅ 无新增静默吞错
exit=0

$ bash scripts/control-tower/check-subprocess-protocol.sh --dir .
[subprocess-protocol] dir=. files=1931 baseline=0
SUBPROCESS-PROTOCOL: OK (files=1931, violations=0, exempted=0, baseline=0)
exit=0
```

### 修复前（红）— 同一检查器，assert.ts 未改时

```
$ bash scripts/control-tower/check-subprocess-protocol.sh --dir .
[subprocess-protocol] dir=. files=1930 baseline=0
VIOLATION  scripts/golden-scenarios/common/assert.ts:101  R1  哨兵混流: curl 写出口模板含自定义哨兵（负载未分流）
VIOLATION  scripts/golden-scenarios/common/assert.ts:109  R2  哨兵分割解析: 依赖负载不含哨兵的不可控假设
SUBPROCESS-PROTOCOL: VIOLATION(2)
exit=1
```

> 说明：1930 → 1931 是新增夹具文件本身（检查器会扫自己所在的仓）。

### 夹具完整输出（`bash tests/control-tower/check-subprocess-protocol.test.sh`）

```
[1] 正常路径: 合规样例（负载已分流）→ 期望 exit 0
  PASS  合规样例 exit (=0)
  PASS  输出 OK 标记 (hit=yes)
[2] 违规 R1: 哨兵混流 → 期望 exit 1 + 点中文件:行
  PASS  R1 样例 exit (=1)
  PASS  点中 sample.sh:3 (hit=yes)
[3] 违规 R2: 哨兵分割解析 → 期望 exit 1
  PASS  R2 样例 exit (=1)
  PASS  点中 sample.ts:1 R2 (hit=yes)
[4] 排除面 EX1: here-string 行（匹配 R1 但属输入重定向）→ 期望 exit 0
  PASS  EX1 样例 exit (=0)
  PASS  示例确含 here-string 记号 (hit=yes)
[5] 排除面 EX2: -o 已分流但 -w 仍含哨兵（合规形态）→ 期望 exit 0
  PASS  EX2 样例 exit (=0)
  PASS  示例确含 -o 旗标 (hit=yes)
[6] 降级路径: grep 注入缝不可用 → 期望 exit 2 + degraded 显式声明
  PASS  扫描器不可用 exit (=2)
  PASS  stderr 显式 degraded (hit=yes)
  PASS  降级码 SUBPROCESS_SCAN_UNAVAILABLE (hit=yes)
[7] 变异体判别: 把 EX2 排除面改坏 → 同一豁免样例必须被判违规
  PASS  坏判别体必须报红（证明排除面承重） (=1)
[8] 变异体判别: 把 R1 主判据改坏 → 同一违规样例必须漏判
  PASS  坏判据体必须漏判（证明红样只由真判据捕获） (=0)
[9] 存量闸门: 真仓库全扫 → 期望 exit 0（存量 0，新增即拦）
  PASS  真仓库 exit (=0)
  PASS  输出 violations=0 (hit=yes)
[10] 源码护栏: assert.ts 保留 D924 字节级兼容标记（弱判据，非行为判据）
  PASS  assert.ts 含 D924 字节级兼容标记 (hit=yes)
  PASS  assert.ts 含 os.tmpdir（Windows 兼容） (hit=yes)
  PASS  assert.ts 不再含旧哨兵形态 (hit=no)

D924-FIXTURE: PASS(20/20)
exit=0
```

> **诚实标注**：用例 10 是**源码护栏**（grep 型弱判据，防"顺手清理"），
> **不是**行为判据。本卡的行为证明在 §3，二者不可互相冒充（CTO 坑清单：「禁 grep 型静态判据当验收」）。
> 该护栏之所以必要：CI 的 `control-tower-tests` job **不装 node_modules**，夹具无法在其中驱动 TS 引擎。

---

## §3 裁定 4 的行为证明（先红后绿，真实引擎实跑）

### 3.1 红态 — HEAD 版 assert.ts（`git show HEAD:...` 导出到临时区实跑）

```
$ npx tsx /private/tmp/d924-probe/assert-old.ts --expect /private/tmp/d924-probe/expect-marker.json --out ... --date 2026-09-23
exit=1
STDOUT >>>
[assert] GS-99: 2/6 断言通过 → verdict=fail
STDERR >>>
  ✗ A1 (fail): status 期望 200 实际 NaN
  ✗ A2 (fail): contains "__STATUS__:999" 未命中
  ✗ A3 (fail): contains ""ok":true" 未命中
  ✗ A4 (fail): contains "true}
" 未命中
```

### 3.2 绿态 — 工作树版 assert.ts（同一 expect.json）

```
$ npx tsx scripts/golden-scenarios/common/assert.ts --expect /private/tmp/d924-probe/expect-marker.json --out ... --date 2026-09-23
exit=0
STDOUT >>>
[assert] GS-99: 6/6 断言通过 → verdict=pass
STDERR >>>（空）
```

### 3.3 逐断言对照

```
 OLD verdict= fail {'A1': 'fail', 'A2': 'fail', 'A3': 'fail', 'A4': 'fail', 'A5': 'pass', 'A6': 'pass'}
 NEW verdict= pass {'A1': 'pass', 'A2': 'pass', 'A3': 'pass', 'A4': 'pass', 'A5': 'pass', 'A6': 'pass'}
```

- **A1/A2/A3 = 裁定 4 的行为要求**：探针正文为 `{"note":"__STATUS__:999","ok":true}`。
  旧形态 `status` 被正文带偏成 `NaN`、正文被截断（`parts.length=3`）；新形态 `status=200`、正文完整。
- **A6 两态同过 = 字节级兼容的直接证据**：`/plain`（正文以 `\n` 结尾）在**新旧两态**下
  `body` 均为 `{"ok":true,"note":"hello"}\n\n` —— 即"原文 + 1 个 LF"语义被保留。
- **A4/A5 = 恰好 1 个 LF**（新形态 `contains 'true}\n'` 通过、`notContains 'true}\n\n'` 通过）。

### 3.4 字节级原始输出（探针 od）

```
$ curl -sS -w '\n__STATUS__:%{http_code}' http://127.0.0.1:8732/marker | od -c   # 旧形态
0000000    {   "   n   o   t   e   "   :   "   _   _   S   T   A   T   U
0000020    S   _   _   :   9   9   9   "   ,   "   o   k   "   :   t   r
0000040    u   e   }  \n   _   _   S   T   A   T   U   S   _   _   :   2
0000060    0   0
旧 split: parts.length = 3 ; status = '999","ok":true}' ; body = '{"note":"'

$ T=$(mktemp); curl -sS -o "$T" -w '%{http_code}' http://127.0.0.1:8732/marker   # 新形态
stdout = '200'(3 字节) ; body = '{"note":"__STATUS__:999","ok":true}'

/plain（正文以 \n 结尾）:
  旧 body = '{"ok":true,"note":"hello"}\n\n'
  新 body(文件) = '{"ok":true,"note":"hello"}\n'
```

### 3.5 真实黄金场景可跑性（诚实标注）

- 8 个 `GS-*/expect.json` 中 `http` 型断言与 `contains`/`notContains` 的**组合命中数 = 0**
  （命令：逐场景 JSON 解析后按 `check.type=='http' and ('contains' in expect or 'notContains' in expect)` 过滤）。
- **未跑既有 GS 场景**：`GS-01/run.sh` 需自举真实服务（`bootstrap.ts` + JWT 自签 + 真实端口），
  其 `expect.json` 为 8 条 `file` 型断言、**不含 http**，对本次改动的回归判别力为 0；
  本机未自举该链路。⇒ **如实写"未跑 + 原因"，不写成已跑**。
- 替代的真实回归证据 = §1 S10（`gss-common.test.ts` 13/13，该测试 `runAssertions` 真实执行 assert.ts）。

---

## §4 M6-1 `git diff --stat` 原始输出

```
$ git diff --cached --stat
 .github/workflows/ci.yml                           |   3 +-
 .../coordination/规范-子进程输出协议-20260923.md   | 221 +++++++++++++++++++++
 scripts/control-tower/check-subprocess-protocol.sh | 182 +++++++++++++++++
 .../control-tower/subprocess-protocol-baseline.txt |  14 ++
 scripts/golden-scenarios/common/assert.ts          |  35 +++-
 .../check-subprocess-protocol.test.sh              | 162 +++++++++++++++
 6 files changed, 607 insertions(+), 10 deletions(-)

$ git diff --cached --numstat
2	1	.github/workflows/ci.yml
221	0	docs/synova/coordination/规范-子进程输出协议-20260923.md
182	0	scripts/control-tower/check-subprocess-protocol.sh
14	0	scripts/control-tower/subprocess-protocol-baseline.txt
26	9	scripts/golden-scenarios/common/assert.ts
162	0	tests/control-tower/check-subprocess-protocol.test.sh
```

口径：**交付物 6 件**（D860 计件）；治理件（task brief / 本证据 / memory note）不计入，
三项另见本文件与提交。PR 文件数 = 6 ≤ 12 ✓；单域 mac ✓。

---

## §5 M6-2 自验结论

**可提请独立审计。**

依据：S1–S12 全部有命令 + 原始输出支撑；红→绿两态均可复现；变异体判别证明判据承重；
降级路径 exit 2 显式（非静默 0）。
**本结论不代表"通过"** —— 通过与否归 CTO 收件闸 + K3 终审。

---

## §6 M6-3 遗留清单

| # | 事项 | 处置建议 |
|---|------|---------|
| L1 | **卡面写集与 D472 门禁冲突**：派单件指定 Note 落 `memory/notes/proposed/`；`check-notes-lifecycle.sh` 把 `task-state` 状态 ∈ {`impl_done`,`spec_done`} 视为"实现已落地"→ 要求 `git mv` 到 `implemented/`。而 `memory/notes/README.md`「四态迁移语义」表规定 proposed → implemented 门槛为 ∈ {`impl_done`,`audited`}。**D924 = `spec_done`，两规则结论相反。** 本卡按写集留在 `proposed/`，Note 不带门禁可提取的 `任务:` 字段（该字段非四字段契约必填项）。**请 CTO 裁定：是改门禁口径，还是改 README 迁移表。** | 立卡（控制塔域） |
| L2 | `check-silent-swallow.sh --utf8` 全量 **16 个 .sh 缺 UTF-8 头块**（存量，非本卡引入：主工作区同命令同结论 `exit=1`） | 已有独立立项，本卡不越写集 |
| L3 | `PAT_R1` **只识别 `curl`**；其它工具（`wget`/`gh`/自研 CLI）同类混流会漏检 | 规范 §8-2 已写失效条件；出现首个实例时扩展 + 补夹具 |
| L4 | `EX2` 排除面**偏宽**：行内任意位置出现 `-o`（如路径名）即豁免 R1 | 规范 §8-4 已知精度代价（换 27 处真实误报消除）；实际漏检时收紧为"同命令段" |
| L5 | `assert.ts` 修复含 **+1 行 `import os`** 与 **`try/finally` 包裹**，严格说超出"只改 :101/:109 两个调用点"的字面范围 | 二者是结构化输出的**必要后果**（`os.tmpdir()` 为 Windows 兼容硬要求；临时文件必须清理）。请 CTO 确认接受 |
| L6 | 本证据 §3.5 的既有 GS 场景**未实跑**（需自举真实服务）；未在 CI 增加 `assert.ts --expect` 的端到端 job | 若要端到端防回归，需新卡（CI 需装 node_modules，该 job 目前不装） |
| L7 | 夹具用例 10 为**源码护栏弱判据**（非行为判据） | 已在夹具与证据中显式标注；行为证明在 §3 |
| L8 | 交付时 `git ls-remote --heads origin \| grep d924` 回执**随交付报告附上**，不在本文件内（提交早于推送，文件内无法自证） | 见交付报告 |
| L9 | **本卡 brief 初版缺 D749 `## 写集` 机器块** ⇒ `parse_write_set.present=False` 且 Q2 散文路径带 `—— 说明` 后缀致 `match_path` 失配 ⇒ `staging_guard.py` D329 认领制判本 session「认领为空」，暂存文件被解析给他卡 brief（**D919 亦声明 `ci.yml`**）→ 提交被硬阻断。已按 D749 补齐机器块（属本卡写集内文件） | **已修**；建议 CTO 评估：brief 生成器是否应默认产出 `## 写集` 机器块（否则同类阻断会复发） |
| L10 | **`docs/authority/DOCS-REGISTRY.yaml` 曾被我越界登记，已回退**。经过：本地提交时 D2 登记门禁给软提示（未登记 2 份新文档）→ 我**推断**"CI 以 `SYNO_CI=1` 跑 pre-commit ⇒ 不登记则 PR 必红"并登记 DOC-0119/0120。**该推断被 CI 证伪**：`TypeScript + Lint + Iron Laws` job 报 `merge-writeset-gate (D708): ❌ 检测到 1 个写集外文件（夹带）— docs/authority/DOCS-REGISTRY.yaml`。**证伪原因**：CI 是干净检出，`doc-registry-gate.sh` 只扫 `git ls-files --others`（未跟踪）+ `git diff --cached --diff-filter=A`（暂存新增），CI 下二者皆空 ⇒ D2 在 CI 检查 0 个文件、**不阻断**；D2 只在本地暂存新文档时给软提示。 | **已 `git checkout <merge-base> --` 回退**，PR 回到声明写集内。**留作 CTO 处置**：若确需登记这两份文档，请由 CTO/架构线统一登记，或扩本卡写集后由我补记 |
| L11 | **`synova-commit` 的 `REGISTER_BRIEF` glob 与仓库 brief 命名约定不一致**：glob 为 `${TASK_ID}.md` / `${TASK_ID}-*.md`，而实际约定是 `YYYY-MM-DD-D<id>-*.md`（近 10 份 D9xx brief 全为日期前缀）⇒ `REGISTER_BRIEF=""` ⇒ session 以**空 brief** 注册（实测 registry: `{"session_id":"D924","brief":""}`），resolver 无法按 session 归属 ⇒ 回退到他人 brief。本卡临时以 `session_registry.py archive` + `register --brief` 修复（设计内 API） | **建议立卡（控制塔域）**：让 glob 兼容日期前缀，否则每张用日期前缀 brief 的卡的 session 归属都是空的 |
| L12 | **D328 提交声明取自 conventional-commit 括号内 scope**（`commit-msg-check.sh:60`）：`feat(control-tower): D924 ...` 被判「声明(无)」 → 必须写 `feat(D924): ...` | 建议在派单模板注明该格式，避免每卡踩一次 |

---

## §7 自检 5 问（铁律）

1. **接线检查**：新脚本谁调用？
   `grep -n 'check-subprocess-protocol' .github/workflows/ci.yml` → CI `control-tower-tests` 清单第 43 条
   `tests/control-tower/check-subprocess-protocol.test.sh` → 用例 1–9 真实调用检查器
   检查器本身由人/CI 直接执行（`bash scripts/control-tower/check-subprocess-protocol.sh --dir .`）。
2. **异常处理**：每个 catch/降级有 log + degraded？
   检查器无 `catch`；降级路径 `degrade()` → stderr `degraded: ...` + `.codex/.../degraded-events.log` + `exit 2`（夹具用例 6 断言）。
   `assert.ts` 内**刻意不使用空 catch**：改用 `fs.existsSync` 守卫与 `fs.rmSync(force:true)`，避免吞错。
3. **类型安全**：`as any` = 0？
   `git grep -nE 'as any|as never|as unknown as' -- <本卡文件>` → 0（`assert.ts` 仅沿用既有的 `as Error & { code: string }` 内联类型）。
4. **测试质量**：有断言且覆盖正常/降级/边界？
   夹具 20 条断言覆盖：正常（用例 1）、违规（2/3）、**排除面边界**（4/5）、**降级**（6）、**变异体判别**（7/8）、存量闸门（9）、护栏（10）。
5. **残留清理**：有死代码/旧引用残留？
   - 实现过程中发现并**已删除**检查器内一段 no-op 死代码（空 baseline 分支的 `:` 体）。
   - 旧哨兵形态在可执行源码面清零（S9 = 0）。
   - `node_modules/.bin` 符号链接为**本地环境补件**（工作树缺件导致 7 项 vitest 假失败），
     `git status` 零条目（`node_modules/` 已在 `.gitignore`），不进提交。
