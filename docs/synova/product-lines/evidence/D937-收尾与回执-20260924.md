# D937 收尾三件与小队回执

> 卡号 D937（P0）｜ 派单：`docs/synova/coordination/派单-Mac小队-五卡-20260924.md` 卡 1
> 执行形态：Agent Teams 小队（队长 + 2 编码 + 1 独立自验）
> 分支 `fix/d937-gate-failopen` ｜ 建树基线 `origin/main` = 2cda1528
> 本文件由**队长**（synova-squad-lead）撰写，属治理产物（`domain_neutral`），非产品代码
> 计数截至 `2026-09-24T00:40:47Z`

---

## 一、计划与放行记录

- 第 0 步：plan 模式出计划（六项：依赖图 / 逐文件写集 / 夹具设计含变异体 / 失效条件 /
  前置冻结清单（命令+原始输出）/ 可复制验收命令），**停下等 CTO 复核**。
- CTO 回执：「按计划执行」→ 进入执行。
- **未清（流程）**：派单件与开工指令**未落 main**（实测 `git cat-file -e main:<path>` → `fatal: ... does not exist`），
  只存在于分支 `docs/d937-five-cards`（`c8e2296e`）。按"不引用未落 main 的路径"红线本应先落 main；
  CTO 以「按计划执行」放行 = 记为**豁免**，条目见 §七-1。

### 计划阶段拦下的 4 项（已随计划上报，CTO 放行时未异议）

| 项 | 内容 | 处置 |
|---|---|---|
| **B 事实纠偏** | 卡面「该检查**自建立起**从未生效」不准确。实测：建立期 `2752a138`(2026-06-18) 用 **BRE** `grep -v`，`^+++` 在 BRE 合法 → **当时生效**（行为实测 `exit=0` 命中）。失效引入点 = **`ab05e2de`(2026-08-21, D467) 的 BRE→ERE 迁移**（`grep -v`→`grep -Ev`、`\|`→`|`，`^+++` 未同步转义） | 代码内注释已按**更正后**溯源写入（`scripts/pre-commit-check.sh:993`） |
| **C 前提待补测** | 「CI strict 亦恒过」无法在 Mac 实测（本机仅 BSD grep，无 ggrep/docker） | 见 §七-6（仍在未清项：矩阵外平台未实测） |
| **D 方法论自纠** | 队长首遍家族扫描因 shell 引号拼接提取错误**误报 2 处**（`dev-doc-gatekeeper.sh:109`、`pre-doc-audit.sh:198`）；重扫后家族**实为 1 处** | 该反例被 c2 的 POSIX 引号解析器继承为设计约束 |
| **§二 第 7/8 项** | brief 由队长落地是否越界 | 队长落地，已声明 |

---

## 二、团队成员运行记录

**token 列如实说明**：本会话工具链**未暴露**成员 token 用量（`env | grep -iE '^DSH_|TOKEN|USAGE'` 无用量变量；
`~/.dsh/sessions/**` 无 token/usage 文件）。按"数字禁手写"纪律**留空，不伪造**。

| 成员 | 角色 | 运行状态 | token | 共享任务 id | 任务状态 | 写集（逐文件） |
|---|---|---|---|---|---|---|
| `lead` | 队长（协调/判前提/验证据，**不下场写码**） | running | 未暴露 | — | — | `.claude/task-briefs/2026-09-24-D937-*.md`（治理）、本文件（治理） |
| `d937-c1` | 编码A·门禁本体 | inactive（已交付） | 未暴露 | **task-1** | **completed**(rev 3) | `scripts/pre-commit-check.sh`、`docs/synova/product-lines/evidence/D937-改动清单.md` |
| `d937-c2` | 编码B·夹具与接线 | inactive（已交付） | 未暴露 | **task-2** | **completed**(rev 3) | `tests/control-tower/gate-failopen-net.test.sh`、`.github/workflows/ci.yml`、`docs/synova/product-lines/evidence/D937-夹具原始输出.md` |
| `d937-v` | **独立自验**（只读 + `/tmp`） | inactive（已交付） | 未暴露 | **task-4** | **completed**(rev 3) | `docs/synova/product-lines/evidence/D937-自验.md` |

编制 = 队长 + 2 编码 + 1 自验 = **4 = maxMembers** ｜ **自验不由编码兼任**（v 独立于 c1/c2）
写集**两两不重叠**，实测交集 **0 处**（队长侧核验）
重型验证（`pre-commit-check.sh` 全跑 / 夹具全量 ~30s/次）**串行 ≤1**（队长在自验期间暂停对交付工作树的一切操作，避免污染 v 的时序敏感实验）

---

## 三、每处改动：file:line + 改前 / 改后

### 3.1 `scripts/pre-commit-check.sh` — 组 7a，L990-992 → **L990-1045**（单 hunk `@@ -988,8 +988,61 @@`）

**改前（L991-992）**
```bash
NEW_DIAG=$(echo "$GIT_CACHED_DIFF" | grep "^+.*DiagnosticModule" | grep -Ev "scripts/pre-commit-check.sh|.md|.html|//|@deprecated|import type|^+++|hard_check|禁止新 DiagnosticModule|不要再使用 DiagnosticModule" || true)
soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" "${NEW_DIAG:-}"
```

**改后（L1004-1045，要点）**
```bash
_DIAG_SELF='scripts/pre-commit-check.sh'
_DIAG_EXCL='@deprecated|import type|^\+[[:space:]]*(//|/\*|\*|#)|hard_check|禁止 ?新? ?DiagnosticModule|不要再使用 DiagnosticModule'
# D390 武装缝: 覆盖仅 SYNO_TEST_ARM=1 时生效（生产路径忽略，fail-closed）
if [ "${SYNO_TEST_ARM:-0}" = "1" ]; then _DIAG_EXCL="${SYNO_DIAG_EXCL_OVERRIDE:-$_DIAG_EXCL}"; fi
_DIAG_CAND=$(printf '%s\n' "$GIT_CACHED_DIFF" | awk -v self="$_DIAG_SELF" '
  substr($0,1,4)=="+++ " { f=substr($0,5); n=length(f)
    ... 去 git quotepath 引号 / 去 b/ 前缀 ...
    skip=(f==self || (n>=3 && substr(f,n-2)==".md") || (n>=5 && substr(f,n-4)==".html")); next }
  skip { next }
  substr($0,1,1)=="+" && index($0,"DiagnosticModule")>0 { print }')
_DIAG_RC=0
if command -v grep >/dev/null 2>&1; then NEW_DIAG=$(printf '%s\n' "$_DIAG_CAND" | grep -Ev -- "$_DIAG_EXCL"); _DIAG_RC=$?; else _DIAG_RC=127; fi
if [ "$_DIAG_RC" -ge 2 ]; then   # ← 三态：检查自身失败 → 显式降级，绝不判 ✅
  echo -e "  ${YELLOW}⚠️  禁止新 DiagnosticModule: 检查降级 — grep 不可用或排除模式非法 (rc=${_DIAG_RC})；本项未判定${RESET}"
  ... degraded-events.log 登记 ... ; SYNO_CI=1 → HARD_FAIL++ : SOFT_COUNT++
  log_gate "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" degraded
else
  soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" "${NEW_DIAG:-}"
fi
```

| 必做项 | 落点 | 证据 |
|---|---|---|
| ① diff 头免正则 | `:1013-1023`（`substr` 纯字符串比较，兼容 quotepath 引号形态） | 根除非法 ERE 类 |
| ② grep 三态 | `:1024-1042` | rc≥2 → 显式降级；CI strict 计 `HARD_FAIL` |
| ③ 文件级豁免按文件 | `:1014-1019` | 修前 58a19796 误报 5 → 修后 **0** |
| ④ 注释豁免锚定 | `:1005`（`^\+[[:space:]]*(//\|/\*\|\*\|#)`） | `+#` / `+ *` 形态修前误报 → 修后不报 |
| ⑤ 字面串保全 | `:1044` | `hard-gate-convergence.test.sh:59` 结构断言仍 ✅ |
| ⑥ 武装缝 fail-closed | `:1006-1009` | 不设 `SYNO_TEST_ARM` 时 override 被忽略（正反判别对） |

**有据偏离参考骨架 2 处**（队长批准，理由已写入证据文档）：`[[:space:]]` 替代 `\s`（D664 口径：`\s` 是 GNU 扩展，POSIX ERE 未定义）；`substr` 尾部比较替代 awk 正则（严格满足必做①"免正则"）。

### 3.2 `tests/control-tower/gate-failopen-net.test.sh`（新建 473 行）
### 3.3 `.github/workflows/ci.yml` — **仅 +1 行**（`:256` 密封清单）
### 3.4 治理产物（`domain_neutral`）
`.claude/task-briefs/2026-09-24-D937-gate-failopen-fakegreen.md`（含 D749 机器可读 `## 写集` 块）、
`docs/synova/product-lines/evidence/D937-{改动清单,夹具原始输出,自验}.md`、`.claude/bypass.log`（hook 自动）

---

## 四、变异体夹具「改坏即红」原始输出

**由独立自验员 `d937-v` 在 `/tmp/d937-v-mut` 副本中自行改坏复现**（非编码方自述）——
原始输出见 `docs/synova/product-lines/evidence/D937-自验.md`（§三-D）。

| 变异体 | 改坏内容 | 结果（v 独立复现） | 判别性 |
|---|---|---|---|
| **M1** | `^\+\+\+` 转义 → 复原 `^+++` | **13 通过 / 7 失败** | ✅ |
| **M2** | 删降级分支，恢复裸 `\|\| true` | **18 通过 / 2 失败** | ✅ |
| **M3** | 逐文件豁免 → 按行子串 | **18 通过 / 2 失败**（58a19796 命中数**精确回到 5**） | ✅ |
| **M4** | 注释豁免 → 裸 `//` | **19 通过 / 1 失败**（2 处误报） | ✅ |
| **M5** | 整段删组 7a | **9 通过 / 11 失败** | ✅ |

**5/5 复原后复跑 `20 通过 0 失败` rc=0。**
另：可执行判据**改前树对照**（`git archive 2cda1528` + 同夹具）→ **13 通过 7 失败**，
家族网点名 `scripts/pre-commit-check.sh:991` 非法 ERE 1 个。

---

## 五、三路径夹具原始输出（索引）

原始输出见 `docs/synova/product-lines/evidence/D937-夹具原始输出.md`（c2）与 `D937-自验.md`（v）。

| 路径 | 驱动 | 结果 |
|---|---|---|
| **正常** | 干净树 + `SYNO_CI=1` | 组 7a ✅、exit 0，**无新增红** |
| **降级** | `SYNO_DIAG_EXCL_OVERRIDE='^+++'`（`SYNO_TEST_ARM=1`） | 显式「检查降级」两行（`:1033`/`:1037`），**7a 的 ✅ 行数 = 0**；CI strict rc=1 |
| **降级·无缝合** | 不设 `SYNO_TEST_ARM` + 同 override | override 被忽略（生产 fail-closed），rc=0、0 条降级 |
| **边界·注释** | `+// ` / `+# ` / `+ * ` 三形态 | 组 7a ✅ 零误报 |
| **边界·文档** | `.html` 正文提及 | 组 7a ✅ 零误伤 |
| **边界·判别对照** | 同正文挂 `.ts` 头 | **必须 ❌ —— 实测 ❌**（证明前一条的 ✅ 来自豁免而非空转） |
| **真实样本** | `git show 58a19796` 全文 | 命中 **5 → 0** |

**判据② 无误伤**：`58a19796`（真实文档提交，含大量合法 `DiagnosticModule` 提及）修后 **0 命中**；
干净树改前/改后输出**逐行删去 stderr 噪音后仅 1 行差异**（基线多 `grep: repetition-operator operand invalid`），
`hard-gate-convergence.test.sh` 在基线与改后**含 ANSI 原始文件 sha256 相同**。

---

## 六、`git diff --stat` 与 ls-remote 回执

```
# 代码交付（域内）
$ git diff --stat origin/main...HEAD -- scripts tests .github
 .github/workflows/ci.yml                      |   1 +
 scripts/pre-commit-check.sh                   |  57 +++-
 tests/control-tower/gate-failopen-net.test.sh | 473 ++++++++++++++++++++++++++
 3 files changed, 529 insertions(+), 2 deletions(-)

# 治理产物（域中立，不占 PR 预算）
$ git diff --stat origin/main...HEAD -- docs .claude
 .../task-briefs/2026-09-24-D937-gate-failopen-fakegreen.md |  91 ++++
 .../product-lines/evidence/D937-夹具原始输出.md            | 267 +++++++++++++
 .../synova/product-lines/evidence/D937-改动清单.md         | 376 +++++++++++++++++
 .claude/bypass.log                                        |   4 +
 4 files changed, 738 insertions(+)

# 预算门禁
$ bash scripts/control-tower/check-pr-budget.sh --files "<3 域内文件>"
 ✅ ① 变更文件数 3 ≤ 上限 12 ｜ ✅ ② 变更单域: mac ｜ ✅ PASS PR 预算内（3 文件） exit=0

# ls-remote 回执（队长亲跑，最终状态）
$ git ls-remote --heads origin fix/d937-gate-failopen
 449463f9a6b849bca8517d4bc2707c3cb4edccc1	refs/heads/fix/d937-gate-failopen
$ git rev-parse HEAD
 449463f9a6b849bca8517d4bc2707c3cb4edccc1
★ 远端 tip == 本地 HEAD（真已推送，非"只在本地"）
> 注：本文件的**最后一次修订提交**会使 tip 前进；上表回执对应**核验时刻**的 tip（即含自验报告与本回执首版的提交）。最终 tip 以 `git ls-remote --heads origin fix/d937-gate-failopen` 与 `git rev-parse HEAD` 双跑一致为准。
$ git status -sb | head -1
 ## fix/d937-gate-failopen...origin/main [ahead 11]     ← behind 0
$ git status --short
 (空 = 工作树干净)
```

**交付提交链**（去 post-commit 钩子噪音）：
```
984d67bf docs(D937): M6 收尾三件（独立自验结论 + 变异体矩阵 + 未清项13条 + 团队运行记录）
054aecc4 Merge remote-tracking branch 'origin/main' into fix/d937-gate-failopen
019b38a3 docs(D937): 回执指向真实交付提交 b2137083（hook 登记提交非交付物）
a82f59ec docs(D937): 夹具原始输出补远端回执 §10
b2137083 test(D937): 组7a fail-open 判别性夹具 + grep -E 家族回归网 + ci.yml 密封接线
8ca6329f fix(D937): 组7a DiagnosticModule 检查 fail-open 假绿（非法ERE+吞错+文件级豁免）
```
旁路：`chore: bypass COMMITTED 登记` 若干，为 post-commit 钩子（D521）自动提交，**非交付物**。

**最终净变更**（`git diff --stat origin/main...HEAD`）：9 files changed, 1847 insertions(+), 2 deletions(-)
（域内 3 文件 529+/2−；其余 6 个为 `domain_neutral` 治理产物）

**分支同步**：`[behind 6]` 已用 **merge**（**非 rebase、非 force push**）清除 → `behind 0`；
合并带入的 4 个文件（`bypass.log` + D936 文档）**零触碰本卡交付文件**。

---

## 六-b、队长最终核验（在交付 tip `449463f9` 上亲跑）

```
# 判据① 变异体：注入真实新增 → 必须红
$ SYNO_TEST_ARM=1 SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
  SYNO_GIT_CACHED_DIFF='+const x = new DiagnosticModule();' bash scripts/pre-commit-check.sh
exit=1
  ❌ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口: 1 处  [CI strict——软提示在 CI 上为硬阻断]
     +const x = new DiagnosticModule();
  ❌ 1 组未通过 — 提交已拒绝

# 判据② 无误伤：干净树
$ SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 bash scripts/pre-commit-check.sh
exit=0
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
  ✅ 全部 13 组通过

# 夹具全跑（最终状态）
$ bash tests/control-tower/gate-failopen-net.test.sh
exit=0 ｜ 结果: 20 通过, 0 失败
```

**改前 → 改后 对照（同一命令）**：改前 `exit=0` + `✅ 禁止 DiagnosticModule` + `✅ 全部 13 组通过`
（唯一痕迹 = stderr `grep: repetition-operator operand invalid`）→ 改后 `exit=1` + 点名组 7a + 提交已拒绝。


---

## 七、未清项清单（诚实登记，不写作"已完成"）

| # | 未清项 | 性质 | 归属 |
|---|---|---|---|
| 1 | **派单件与开工指令未落 main**（只在 `docs/d937-five-cards`）；红线要求先在 main 可读 | 流程/豁免 | **CTO** |
| 2 | 夹具 **T3「stdout 含显式降级字样」断言不具判别性**：M2 下仍 ✅，字面量来源是 `pre-commit-check.sh:99` 沙箱后缀（`✅ Task Brief: …（沙箱夹具，降级）`）→ 20 条断言中 **19 条有效** | 测试质量（v 发现，非阻断） | CT 队列（建议立卡） |
| 3 | **家族回归网对本次修复的核心模式零覆盖**：排除模式抽成 `_DIAG_EXCL` 变量后计入"动态不可判"→ M1 把非法 `^+++` 放回默认模式时网仍报"非法 0"。最坏后果 = **降级（fail-closed）**，非假绿 | 机制覆盖缺口 | CT 队列（建议立卡） |
| 4 | **矩阵外平台未实测**：本机无 GNU grep/gawk/mawk（未安装未联网）→ ubuntu/windows 的家族网判定未独立验证（已做平台稳健设计 + 探针可见） | 环境限制 | CT 队列 / Win 侧 |
| 5 | **队长侧一次 `19 通过 1 失败` 未解释**：v 复现 19 次全绿 + 并发暂存/竞态探针 8 轮无反例；两条假设「事实成立但翻转不了结果」。**CI 结论：不会因并发而红**（单 job 单树串行） | 未解释现象 | 保留（若再现，先抓失败断言原文） |
| 6 | **卡面「CI strict 亦恒过」未直接取证**（C 项）：需 GNU grep 环境 | 前提未闭合 | CTO / Win 侧 |
| 7 | `doc-registry-gate.sh:22` 的 `EXCLUDE` 缺 `docs/synova/product-lines/evidence/` → 每份 evidence `.md` **首次提交**触发 D2 软提示（CI 干净检出下 `CHECKED=0` 自过，**不产 CI 新红**；同批 D935/D922/D870 先例一致） | 规则级缺口 | CT 队列（**不要逐文档特例**） |
| 8 | `synova-commit:801-807`（D414）会**越过 `--files` 自动并入** `bypass.log`（c1 提交前手工还原才守住写集） | 工具越权 | CT 队列 |
| 9 | 铁律 49 / D534 **Note 引用门禁正则缺口**：只覆盖 `scripts/{control-tower,workflow,hooks}/`，根级治理脚本 `scripts/pre-commit-check.sh`、`scripts/pre-push-check.sh` 在网外 → 最治理级的文件反而不受约束 | 门禁覆盖缺口 | CT 队列 |
| 10 | `tests/control-tower/hard-gate-convergence.test.sh` **21 通过 / 5 失败**（存量漂移，D749 改 `decl_check` 后断言未同步）；该测试**不在 ci.yml 密封清单** → 漂移无人发现（M3 未接线）。**与 D937 无关**（c1 改动仅 1 个 hunk；基线与改后含 ANSI 原始文件 sha256 相同） | 存量红 + M3 | CT 队列 |
| 11 | 该测试 `cleanup` 在部分路径未从索引移除探针（曾残留 `A src/tmp-d515-asany-probe.ts`，会使后续任何人 G12 变红；不稳复现） | 存量缺陷 | CT 队列 |
| 12 | `.claude/.g12-scope.tsv` / `.g12-excl.tsv` **未进 `.gitignore`** → `git add -A` 会误提交 session 产物 | 卫生 | CT 队列 |
| 13 | c1 证据文档 §四-2 曾称「D2 门禁 CI 会红」——**v 实测不成立**（CI 等价状态 `检查 0 个文档，0 个未登记 / exit 0`）。队长侧早先判定与 v 一致 | 文档订正 | 已在本文件 §七-7 订正 |

---

## 八、声明

本文件与 `D937-自验.md` 均为**自验结论**，**不是审计结论**，**不含"通过"判定**。
是否通过归 **CTO 收件闸 + K3 终审**；**K3 复审通过前不得合并**。
交付状态：**可提请独立审计**。
