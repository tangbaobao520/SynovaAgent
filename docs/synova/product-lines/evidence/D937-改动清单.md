# D937 改动清单 — scripts/pre-commit-check.sh 组 7a（fail-open 假绿）

> 卡：task-1（D937-T1）｜写者：**d937-c1**（编码A·门禁本体，本卡唯一写者）
> 工作树：`/Users/wane/SynovaAgent/.synova-wt-squad-d937`｜分支：`fix/d937-gate-failopen`
> 基线：`origin/main` = `2cda1528`（本卡建树时）→ 交付时 `origin/main` 已前进至 `12383f14`（见"未清项 2"）
> 机器：macOS / `grep (BSD grep, GNU compatible) 2.6.0-FreeBSD`
> **本文件是编码方的工程验收输出，不是自验结论。**独立自验归 `d937-v`；`自验结论` 只能由 d937-v 给出。

---

## 一、改动清单（file:line + 改前/改后）

| # | 文件 | 行号（改后） | 改前 | 改后 |
|---|------|-------------|------|------|
| 1 | `scripts/pre-commit-check.sh` | L990-1045（原 L990-992；其后 L1047 为 `# 7b.`） | 见下"改前原文" | 见下"改后结构" |
| 2 | `docs/synova/product-lines/evidence/D937-改动清单.md` | 本文件 | （新增） | — |

```
$ git diff HEAD --stat
 scripts/pre-commit-check.sh | 57 +++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 55 insertions(+), 2 deletions(-)
```

单一 hunk（`@@ -988,8 +988,61 @@`）——**除组 7a 外，其余 12 组判定逻辑与组数一字未动**（下一组 `# 7b.` 由原 L994 移至 L1047）。

### 改前原文（L990-992，3 行）

```bash
# 7a. DiagnosticModule 禁止 (原 6)
NEW_DIAG=$(echo "$GIT_CACHED_DIFF" | grep "^+.*DiagnosticModule" | grep -Ev "scripts/pre-commit-check.sh|.md|.html|//|@deprecated|import type|^+++|hard_check|禁止新 DiagnosticModule|不要再使用 DiagnosticModule" || true)
soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" "${NEW_DIAG:-}"
```

### 改后结构（L990-1045，逐段）

| 行号 | 内容 | 对应必做项 |
|------|------|-----------|
| L990-1003 | 头注释：三缺陷 + 三修法 + 实证命令 | — |
| L1004 | `_DIAG_SELF='scripts/pre-commit-check.sh'` | ③ |
| L1005 | `_DIAG_EXCL='@deprecated\|import type\|^\+[[:space:]]*(//\|/\*\|\*\|#)\|hard_check\|禁止 ?新? ?DiagnosticModule\|不要再使用 DiagnosticModule'` | ②④ |
| L1006-1010 | D390 武装缝：仅 `SYNO_TEST_ARM=1` 时 `SYNO_DIAG_EXCL_OVERRIDE` 生效 | ⑥ |
| L1012-1023 | `awk` 逐文件判定：`+++ b/<path>` 头切文件，自身/`.md`/`.html` 整文件跳过（纯字符串比较，免正则；兼容 quotepath 引号形态） | ①③ |
| L1025-1031 | grep 退出码三态抓取（无 grep → `_DIAG_RC=127`） | ② |
| L1032-1043 | `_DIAG_RC>=2` → 显式降级 + `degraded-events.log` + CI strict 计 `HARD_FAIL`／本地计 `SOFT_COUNT` | ② |
| L1044 | `else` → `soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" ...`（**字面串保全**） | ⑤ |

**⑤ 字面串保全取证**（`tests/control-tower/hard-gate-convergence.test.sh:59` 结构断言钉住）：

```
$ grep -c 'soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口"' scripts/pre-commit-check.sh
1
```
改后脚本运行该测试时该条通过：`✅ 放[软]: soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口"`（见 §三-7）。

### 与参考骨架的两处**有据偏离**（请自验方复核）

| 项 | 参考骨架 | 本实现 | 依据（可核） |
|----|---------|--------|-------------|
| 行级注释豁免写法 | `^\+\s*(...)` | `^\+[[:space:]]*(...)` | 仓库 D664 既有口径明示「`\s` / `\S` —— PCRE 无 ERE 等价物，必须转 POSIX 类」（`tests/control-tower/grep-oP-regression.test.sh` 头注释）；`\s` 属 GNU 扩展，POSIX ERE 未定义。**语义等价，意图（锚定注释行）不变。** |
| diff 头取路径 | `f ~ /\.(md\|html)$/`（awk 正则） | `substr` 尾部字符串比较 | 必做①要求"结构化字符串比较（免正则）→ 根除非法 ERE 类"；本实现对 diff 头**完全无正则** |

---

## 二、根因（三缺陷）与实测实证

### ① 未转义 `^+++` → ERE 非法 → BSD grep exit 2 → 管道整体失败 → `NEW_DIAG` 恒空

```
$ echo test | grep -Ev "^+++"
rc=2
[stderr] grep: repetition-operator operand invalid
```

```
$ grep -n "^set -" scripts/pre-commit-check.sh
(无 set -e / set -u / set -o pipefail)
```
→ 无 `set -e`，管道失败后脚本继续跑完并打 ✅；`|| true` 把 grep 自身错误吞成"无命中"。

### ② 改前基线（**假绿**实测）

```
$ SYNO_TEST_ARM=1 SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
  SYNO_GATE_HITS_LOG="$(mktemp)" \
  SYNO_GIT_CACHED_DIFF='+const x = new DiagnosticModule();' \
  bash scripts/pre-commit-check.sh
exit=0
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口          ← 假绿
  ✅ 全部 13 组通过
[stderr] grep: repetition-operator operand invalid               ← 唯一痕迹
```
（对照组：改后同一命令 `exit=1` / `❌ ... 1 处 ... 提交已拒绝`，见 §三-2）

### ③ 文件级豁免与行级豁免混按行子串匹配 → 误报（无误伤判据）

朴素修复（仅把 `^+++` 转义为 `^\+\+\+`，其余不动）对 `58a19796` 全文：

```
$ git show 58a19796 | grep "^+.*DiagnosticModule" \
    | grep -Ev "scripts/pre-commit-check.sh|.md|.html|//|@deprecated|import type|^\+\+\+|hard_check|禁止新 DiagnosticModule|不要再使用 DiagnosticModule" | wc -l
5
```
命中 5 处**全部来自文档正文**（该 commit 头含 `+++ b/docs/SYNOVA-MASTER-全量对齐手册-20260610.html`），逐例如：
```
+    pre-commit hook 阻断新增 <code>DiagnosticModule</code> 注册——只允许迁移列表内的文件保留 <code>registerModule</code>。<br>
+    不经过 DiagnosticModule 注册表，不走 module-registry。<br>
+    ③ pre-commit 阻断新 DiagnosticModule 注册<br>
+  <li><strong>规则用编译器阻断，不靠文档约定。</strong> Sentinel 接口上线 → 旧 DiagnosticModule 注册 = 编译器报错。</li>
+  <li><strong>规则用编译器阻断。</strong> Sentinel 接口上线 → 旧 DiagnosticModule 注册 = 编译错误。</li>
```
→ 按**行**匹配时，正文行不含 `.html` 子串，故文件级豁免失效。改为按**文件**判定后：0 命中（见 §三-5）。

### 失效起点

`D467`（`ab05e2de`，2026-08-21）BRE→ERE 迁移（`grep -v`→`grep -Ev`、`\|`→`|`，`^+++` 未同步转义）。即：本检查自该日起**从未生效**。

---

## 三、验收原始输出（7 条，逐条照贴）

### 1) `bash -n` 语法

```
$ bash -n scripts/pre-commit-check.sh; echo "exit=$?"
exit=0
```

### 2) 判据①：注入真实新增 → exit 1 且点名组 7a（不再是 ✅）

```
$ SYNO_TEST_ARM=1 SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
  SYNO_GATE_HITS_LOG="$(mktemp)" \
  SYNO_GIT_CACHED_DIFF='+const x = new DiagnosticModule();' \
  bash scripts/pre-commit-check.sh; echo "exit=$?"
exit=1
  ❌ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口: 1 处  [CI strict——软提示在 CI 上为硬阻断]
     +const x = new DiagnosticModule();
  ❌ 1 组未通过 — 提交已拒绝
[stderr] （空）
```

### 3) 判据③降级：模式非法 → 显式降级，且组 7a 无 ✅

```
$ SYNO_TEST_ARM=1 SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
  SYNO_GATE_HITS_LOG="$(mktemp)" SYNO_DIAG_EXCL_OVERRIDE='^+++' \
  SYNO_GIT_CACHED_DIFF='+const x = new DiagnosticModule();' \
  bash scripts/pre-commit-check.sh; echo "exit=$?"
exit=1
  ⚠️  禁止新 DiagnosticModule: 检查降级 — grep 不可用或排除模式非法 (rc=2)；本项未判定
  ❌ 禁止新 DiagnosticModule: 检查降级  [CI strict——降级即失败]

$ grep -c "✅ 禁止 DiagnosticModule" <上述 stdout>
0
```
降级登记（`.codex/control-tower/logs/degraded-events.log`，沿 L876 既有格式）：
```json
{"time": "2026-09-23T23:58:04+00:00", "component": "pre-commit-group7a-diagnosticmodule", "reason": "grep rc=2 — 排除模式非法或 grep 不可用 (degraded, 非通过)"}
```

### 4) 判据③边界：三形态注释注入 → 组 7a 均 ✅（不误报）

```
注入 [+// DiagnosticModule]        → exit=0  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口   stderr 空
注入 [+# DiagnosticModule]         → exit=0  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口   stderr 空
注入 [+ * DiagnosticModule]        → exit=0  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口   stderr 空
```
反例（真代码仍须被抓）：`+const x = new DiagnosticModule();` → ❌（见 §三-2）——即"删掉锚定豁免即报红"的判别性对照成立。

### 4b) ⑥ 武装缝 fail-closed（补充判据，非验收清单项）

```
$ env -u SYNO_TEST_ARM SYNO_DIAG_EXCL_OVERRIDE='^+++' SYNO_GIT_CACHED_DIFF='+const x = new DiagnosticModule();' \
  SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 SYNO_GATE_HITS_LOG="$(mktemp)" \
  bash scripts/pre-commit-check.sh; echo "exit=$?"
exit=0
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
$ grep -c "检查降级" <上述 stdout>
0
```
说明：未设 `SYNO_TEST_ARM` 时 `SYNO_DIAG_EXCL_OVERRIDE` 被忽略（生产 fail-closed）；与 §三-3（设了 arm → 同一 override 触发降级）构成**判别性对**——缝真的在起作用，不是恒真。

### 5) 判据②无误伤：`58a19796` 全文 → 0 命中

```
$ SYNO_TEST_ARM=1 SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 \
  SYNO_GATE_HITS_LOG="$(mktemp)" SYNO_GIT_CACHED_DIFF="$(git show 58a19796)" \
  bash scripts/pre-commit-check.sh; echo "exit=$?"
exit=0
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
[stderr] （空）

# 同一 diff 经"改后组 7a 管道"的命中数（命令+排除项+截至时刻: 2026-09-24 本地，git show 58a19796）
0
```
对照：朴素修复 = 5 处误报（§二-③）。误伤清零。

### 6) 干净树：exit 0，无新增红

```
$ SYNO_CI=1 SYNO_GATEKEEPER_ACK=1 SYNO_SKIP_PARALLEL_WARN=1 SYNO_GATE_HITS_LOG="$(mktemp)" \
  bash scripts/pre-commit-check.sh; echo "exit=$?"
exit=0
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
  ✅ 全部 13 组通过
  ⚠️  1 项警告 (不阻断)
[stderr] （空）
```
红集合（仅既有非阻断项）：`⚠️ PRD 对照: Done 标准引用 PRD 章节(可选)` + `⚠️ 1 项警告 (不阻断)`。

**"无新增红"的对照取证**（同一暂存态，基线脚本 vs 改后脚本）；
基线脚本 = `git show HEAD:scripts/pre-commit-check.sh`（本卡改动前的现行 main 版本）落到 `/tmp` 执行：

```
$ diff <(基线脚本红集合) <(改后脚本红集合)
（空）                     ← 红集合逐字节一致，未新增任何红
```
两者红集合均为：`⚠️ PRD 对照` / `❌ G12: task brief Q2 范围一致性` / `❌ 1 组未通过` / `⚠️ 1 项警告`。
两脚本 stdout 的**全部**差异只有一行（基线多出 grep 自身报错噪音）：
```
$ diff <(sed 's/\x1b\[[0-9;]*m//g' <基线 stdout>) <(sed 's/\x1b\[[0-9;]*m//g' <改后 stdout>)
64d63
< grep: repetition-operator operand invalid
```
即：改后**行为变化只有"假绿消失 + 报错噪音消失"**。

### 7) 相关既有测试不回归

```
$ bash tests/control-tower/grep-oP-regression.test.sh
  结果: 38 通过, 0 失败
  Status: ✅ grep -P 回归网全部通过
exit=0
```

```
$ bash tests/control-tower/hard-gate-convergence.test.sh
  ✅ 放[软]: soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口"     ← ⑤ 字面串保全
  结果: 21 通过, 5 失败
exit=1
```
该测试的 5 项失败**全部为既有失败，非本卡引入**——在**未改动的基线工作树**（`origin/main` = `12383f14`，`/tmp/d937-basewt`，已 `git worktree remove`）跑同一测试：

```
$ bash tests/control-tower/hard-gate-convergence.test.sh    # 基线工作树（origin/main 12383f14，未改动）
  结果: 21 通过, 5 失败
exit=1
```
**两侧测试输出逐字节一致的原始取证**（自验方可复跑：先 `git worktree add --detach /tmp/x origin/main`，两侧各跑一次该测试）：
```
$ diff <(sed 's/\x1b\[[0-9;]*m//g' /tmp/d937-hgc-base.out) <(sed 's/\x1b\[[0-9;]*m//g' /tmp/d937-hgc-mine.out)
diff exit=0
$ diff <(sed 's/\x1b\[[0-9;]*m//g' /tmp/d937-hgc-base.out) <(sed 's/\x1b\[[0-9;]*m//g' /tmp/d937-hgc-mine.out) | wc -c
       0                       ← 差异 0 字节（空输出，非"省略"）

$ shasum -a 256 /tmp/d937-hgc-base.out /tmp/d937-hgc-mine.out
70218deda0bd4ee391511c6041275ab71e35b66b3eb1c4e072e3909937513103  /tmp/d937-hgc-base.out
70218deda0bd4ee391511c6041275ab71e35b66b3eb1c4e072e3909937513103  /tmp/d937-hgc-mine.out
            ← 含 ANSI 的原始文件哈希相同 = 逐字节一致（非仅"去色后"一致）

$ 两侧结果行
基线: 结果: 21 通过, 5 失败
改后: 结果: 21 通过, 5 失败
```
5 项失败清单（两侧相同）：
```
❌ 未按 spec 降软: soft_check "Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)"
❌ 未按 spec 降软: soft_check "G12: task brief Q2 范围一致性"
❌ 行为A4: 应 exit 0, 实际 1
❌ 行为B: 应 exit 0, 实际 1
❌ 行为B: 缺软提示标记
```
根因（**非本卡写集**）：D749 把这两项由 `soft_check` 改为 `decl_check`（现行 L895 / L1376，距本卡 hunk 三百余行），D515 时代的本测试结构断言随之过期；行为 A4/B 的 exit 1 由 G12 硬判定主导。三项独立佐证：
```
$ grep -cF 'soft_check "Task Brief: 6 核心字段必须填写' 当前 → 0 ;  基线(HEAD) → 0     （改前改后一致）
$ grep -cF 'soft_check "G12: task brief Q2 范围一致性"'   当前 → 0 ;  基线(HEAD) → 0     （改前改后一致）
$ grep -c 'V5 软提示'                                      当前 → 9 ;  基线(HEAD) → 9     （未受影响）
行为B 复现（暂存未认领 code 文件）: rc(改后)=1, rc(基线)=1；G12 行逐字节一致
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 2 处  [硬阻断]
```

---

## 四、未清项（交 CTO／队长裁决，**均不在本卡写集内**）

### 1) G12 硬红阻断本卡 commit 后的 CI 绿腿（非本卡引入）

**现象**：暂存 `scripts/pre-commit-check.sh` 时 G12 报
`❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处 [硬阻断]` → `scripts/pre-commit-check.sh (不在 Q2 范围内)`。

**根因（已定位到判据本体）**：本卡 task brief `.claude/task-briefs/2026-09-24-D937-gate-failopen-fakegreen.md`
**缺 D749 机器可读「## 写集」块**（实测 `parse_write_set(text)['present'] = False`）→ `brief_parser.parse_q2` 退回散文 Q2，
而散文行写作 `` - `scripts/pre-commit-check.sh` 组 7a（L990-992）： `` 时，解析结果为 `` `scripts/pre-commit-check.sh` 组 7a ``（**不剥反引号、不剥「组 7a」尾巴**，该解析器只剥 `[:：] — （` 与 `path L\d+` 后缀）→ 认领值 ≠ 实际路径 → 认领失效。

**佐证（同批已落地 brief 均带该机器块，解析正常）**：
```
2026-09-24-D935-M1-ownership-presets-域修正.md → write-set present=True → docs/synova/coordination/ownership.yaml | .github/CODEOWNERS | tests/...
2026-09-24-D936-ownership-defer.md             → write-set present=True → docs/synova/coordination/... | .claude/task-briefs/...
2026-09-23-D931-squad-team-wiring.md           → write-set present=True → docs/synova/presets/... | task-state/D931.json | ...
```

**修法（一处，**brief 侧**，不动控制塔脚本）**：给该 brief 补 D749 机器块。已模拟验证：
```
补表后 write-set present = True
parse_q2 include = ['scripts/pre-commit-check.sh', 'docs/synova/product-lines/evidence/D937-改动清单.md']
认领判定 matches(staged, 'scripts/pre-commit-check.sh') = True      ← G12 通过
```
**建议**（`## Q3` 之前插入，两行即可）：
```markdown
## 写集
| 文件 | 类别 |
|---|---|
| `scripts/pre-commit-check.sh` | task |
| `docs/synova/product-lines/evidence/D937-改动清单.md` | task |
```
> 说明：本卡写集不含 brief 文件本身，**故由队长/CTO 施加，我不越界改**。

**影响半径（实测）**：本地 **git hook** 为软提示（`.git/hooks/pre-commit` 恒 `exit 0`，仅记 `GATE_FAIL_SOFT`）；但**唯一指定提交路径 `synova-commit` 硬阻断**——实测：

```
$ bash scripts/control-tower/synova-commit --task-id D937 --agent d937-c1 \
    --message "fix(gate): D937 组 7a fail-open 假绿 — 结构化文件判定 + grep 三态降级" \
    --files docs/synova/product-lines/evidence/D937-改动清单.md scripts/pre-commit-check.sh
exit=1
  ❌ G12: task brief Q2 范围一致性（写集单一事实源，D749）: 1 处  [硬阻断]
     scripts/pre-commit-check.sh (不在 Q2 范围内)
  ❌ 1 组未通过 — 提交已拒绝
❌ pre-commit 检查未通过（exit 1）
  提交被阻断 — 请修复上述问题后重试
$ git rev-parse --short HEAD
2cda1528            ← 未产生 commit（HEAD 未变）
```
（`synova-commit:791` 仅在 `PRECOMMIT_EXIT -eq 0` 时才走提交路径；`:880-889` 失败分支只记 `BLOCKED` 并 `exit 1`。）
**故本卡交付物已就绪并暂存，但提交被此 G12 阻断 —— 卡在 brief（非本卡写集），请队长/CTO 施加。**

### 2) D2 登记门禁：本证据文档未登记（本地软提示，**CI 权威**）

```
⚠️  D2 登记门禁: 有未登记文档 — 登记 docs/authority/DOCS-REGISTRY.yaml 或核对排除规则: 2 处  [V5 软提示——CI 为权威，本地不阻断]
   ❌ 未登记: docs/synova/product-lines/evidence/D937-改动清单.md （请加入 docs/authority/DOCS-REGISTRY.yaml）
   ── 汇总: 检查 1 个文档，1 个未登记 ──
```
- 产出脚本：`scripts/pre-commit-check.sh:1487` → `scripts/doc-system/doc-registry-gate.sh`（**均非本卡写集**）。
- 现状：`docs/authority/DOCS-REGISTRY.yaml` 中 `product-lines/evidence` 路径条目数 = **0**（`grep -c` 实测）。
- 即：M5 约定的证据落盘目录与 D2 登记门禁存在口径缺口——**每份** evidence 文档都会触发该软提示。修法二选一（请队长/CTO 裁定）：① 为证据目录加登记/排除规则（规则级，推荐）；② 逐文档登记（特例级，与"不做一次性特例"相悖）。
- 该门禁本地不阻断，但 **CI 会红**，故 merge 前需一并处置。

### 3) 基线漂移：`[behind 6]`

建树基线 `2cda1528`，交付时 `origin/main = 12383f14`（+6 commit，均为 `docs/D936` 归属域）。
实测这 6 个 commit **未触碰 `scripts/pre-commit-check.sh`** → 与本卡零冲突；本卡分支当前 0 个本地 commit，提交后需 ff/rebase 再 PR。同步动作**留待队长裁定后执行**（避免扰动本共享工作树上并行作业的 `d937-t`）。

### 4) 旁路观察（供队列参考，非本卡职责）

- `tests/control-tower/hard-gate-convergence.test.sh` 的 `cleanup()` 在部分路径下未能从索引移除探针（实测一次残留 `A src/tmp-d515-asany-probe.ts` 暂存项），残留会使后续任何人的 G12 变红。已确认该残留随后消失（既有行为，未复现稳定径）。
- G12 临时产物 `.claude/.g12-scope.tsv` / `.claude/.g12-excl.tsv` 未被 `.gitignore` 忽略（`.gitignore:8` 只忽略 `*.log`），任何人 `git add -A` 都会误提交为 session 产物。
- `synova-commit:801-807`（D414）会在 `bypass.log` 被改动时**自动并入本次提交**，与"不提交旁路日志"的指令冲突（提交前已按队长指令 `git restore` 回 HEAD，故本次提交不含该文件）。被还原的那一行是本次提交尝试自身的记录，原文如下，已全量留档于本文件 §四-1，未做任何删改：
  `2026-09-24T08:04:57+08:00 | BLOCKED | pre-commit FAIL (exit 1) | TASK_ID=D937 | AGENT=d937-c1`
  （该工具行为建议按规则级修：`--files` 模式下不应越过显式文件集自动加文件。）

---

## 五、编码方自检 5 问

1. **接线检查**：无新 export。改动位于 `scripts/pre-commit-check.sh` 执行流内（组 7a 原地替换，非函数），无接线缺口。
2. **异常处理**：新增的唯一失败面（grep 自身失败）走**显式降级**分支：可见告警 + `degraded-events.log` 登记 + CI strict 计硬失败，**不判 ✅**（铁律 11/24/31；对照改前的 `|| true` 静默吞错）。
3. **类型安全**：不适用（bash）；未引入 `as any` 等。
4. **测试质量**：本卡写集**不含测试文件**（`tests/**` 属卡 2 写集）。编码方自跑的判别性对照见 §三-2 vs §三-4（真代码报红 / 注释不误报）、§三-3 vs §三-4b（同一 override 设不设 arm 结果相反）。
5. **残留清理**：无死代码——`NEW_DIAG` 仍在 else 分支被消费；被删除的旧管道无其他引用（`grep -n NEW_DIAG` 共 2 处，均为本块内）。临时取证文件（`/tmp/d937-*`、`/tmp/d937-basewt`）已清理；`git worktree remove` 回执 rc=0。

---

## 六、声明

- 本文件所载数字**全部来自上述命令的原始输出**（未手写估值）；关键输出未用 `head` 截断，计数均带"命令 + 排除项 + 截至时刻"。
- 编码方给出的是**工程验收输出**，不是 `自验结论`。**是否可提请独立审计 / 是否通过，由队长与 K3 判定。**
