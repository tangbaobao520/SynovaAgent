# D937 独立自验报告 — 组 7a fail-open 假绿修复 + 判别性夹具

> **写者**：`d937-v`（独立自验员，**非** d937-c1 / d937-c2 的延续）
> **卡**：task-4（D937-T3 独立自验）
> **工作树**：`/Users/wane/SynovaAgent/.synova-wt-squad-d937`（**只读**；本文件是唯一写入物）
> **核验对象**：分支 `fix/d937-gate-failopen`，HEAD = `68b3c2f2176d833f71917bc5347356d1bad17217`
> **远端回执**（抓取时刻 `2026-09-24T08:37:56+08:00`）：
> ```
> $ git ls-remote --heads origin | grep fix/d937-gate-failopen
> 68b3c2f2176d833f71917bc5347356d1bad17217	refs/heads/fix/d937-gate-failopen
> $ git rev-parse HEAD
> 68b3c2f2176d833f71917bc5347356d1bad17217
> $ git rev-list --left-right --count HEAD...origin/fix/d937-gate-failopen
> 0	0
> ```
> **机器 / 工具链**：macOS｜`grep (BSD grep, GNU compatible) 2.6.0-FreeBSD`｜`awk version 20200816`（BWK）｜`bash 3.2`（本机 `/bin/bash`，`${var,,}` 不可用——已在自建脚本中规避）
> **本文件所有数字均来自命令原始输出**；关键输出未用 `head` 截断；计数均附「命令 + 排除项 + 截至时刻」。

---

## 一、自验结论

**`可提请独立审计`**（K3 复审前不得合并；本报告**不是**审计结论，也不含"通过"判定）。

逐条覆盖情况：**A–G 全部完成**，其中：

| 判据 | 结果 | 一句话依据 |
|---|---|---|
| A 判据① 注入真违规 → exit 1 点名 7a | **成立** | T2/T2b 手跑；M1 对照组反向成立（§三-D） |
| B 判据② 无误伤 | **成立** | 干净树 19 跑全绿；`58a19796` 新管道命中 **0**（朴素修复口径 **5**，逐条复现） |
| C 判据③ 三路径 | **成立** | 正常 ✅／降级「无 ✅ + 降级行」／边界零误报 + 判别对照 ❌（§三-C） |
| D 变异体 M1–M5 独立复现 | **成立** | 5/5 变异各自打红对应夹具项；5/5 复原后复跑全绿（§三-D） |
| E 家族网 + **自造**金丝雀 | **成立** | 我自建的 `zz-v-canary-bad.sh` 被点名 `file:line`，对照文件未被误判（§三-E） |
| F 反查「接线了≠被执行」 | **成立** | ci.yml:256 在密封清单 + gate-hits 日志留下 7a `result:"miss"` 执行记录（§三-F） |
| G 数字逐条复核 | **成立** | 7 个数字全部逐条复跑一致（§三-G） |

**必须与结论一起读的三件事**：
1. **队长侧那次 `19 通过, 1 失败` 我未能复现**（19 次基线全绿 + 6 次 /tmp 副本全绿 + 8 轮并发探针无反例），三条候选机制均被实测否决或未能触发；同时我确认了 2 处**结构性脆弱**（§二）。
2. **5 条独立新发现**（1 条夹具判别力缺陷 + 2 条覆盖盲区 + 1 条并发共享路径机制 + 1 条与 c1 声称相反的实测结论）见 §四。
3. **未清项 5 条**（含「矩阵外平台未实测」与「本次失败仍未解释」）在 §五 显式披露。

---

## 二、首要任务：队长侧未解释现象（flaky 复现）

### 2.1 ① 失败断言名与原文：**未能复现**

命令：`bash tests/control-tower/gate-failopen-net.test.sh`（交付工作树，`68b3c2f2`，同机同 grep）。

**基线（12 次连续串行跑，后台作业逐跑记录 rc）**：
```
run-0 rc=0 结果: 20 通过, 0 失败
run-1 rc=0 结果: 20 通过, 0 失败
run-2 rc=0 结果: 20 通过, 0 失败
run-3 rc=0 结果: 20 通过, 0 失败
run-4 rc=0 结果: 20 通过, 0 失败
run-5 rc=0 结果: 20 通过, 0 失败
run-6 rc=0 结果: 20 通过, 0 失败
run-7 rc=0 结果: 20 通过, 0 失败
run-8 rc=0 结果: 20 通过, 0 失败
run-9 rc=0 结果: 20 通过, 0 失败
run-10 rc=0 结果: 20 通过, 0 失败
run-11 rc=0 结果: 20 通过, 0 失败
=== ALL DONE ===
```
累计 **19 次连续全绿**（上列 12 次 + 首跑 1 次 + /tmp 副本 A/B 阶段 6 次），**0 次失败**。故 **无失败断言原文可贴**——这是本项的反证证据，不是我漏做。

### 2.2 ② 并发/暂存态试验（/tmp/d937-v-stage 副本；忠实重建 c2 提交窗口）

重建方式：`git -C /tmp/d937-v-stage reset --mixed 44b7e3f1`（工作树保留 `68b3c2f2` 内容 → 索引/HEAD 回到交付提交的父提交）。重建后暂存态：
```
 M .claude/bypass.log
 M .github/workflows/ci.yml
?? docs/synova/product-lines/evidence/D937-夹具原始输出.md
?? tests/control-tower/gate-failopen-net.test.sh
```

**A 阶段（非空真实暂存 diff，无并发噪声）：3 跑全绿**
```
### A run1 rc=0  结果: 20 通过, 0 失败
### A run2 rc=0  结果: 20 通过, 0 失败
### A run3 rc=0  结果: 20 通过, 0 失败
```

**B 阶段（同一暂存态 + 每跑全程并发注入真实模式 `pre-commit-check.sh` 噪声循环）：3 跑全绿**
```
### B run1 rc=0  结果: 20 通过, 0 失败
### B run2 rc=0  结果: 20 通过, 0 失败
### B run3 rc=0  结果: 20 通过, 0 失败
```

**T3f 第二跑专项探针**（直接跑门禁本体，不看夹具自述）：
```
# 副本干净态:
rc=0 ｜ 降级 命中数: 0
# 副本暂存 1 个文件后:
rc=0 ｜ 降级 命中数: 0
```

**同树并发竞态探针**（`/tmp/d937-v-par-race.sh`：A = 夹具 `run_pc` 密封形态，B = 真实模式，A/B 并发 8 轮）：
```
  round 1: A_rc=0 A_红行=1 | B_rc=1
  round 2: A_rc=0 A_红行=1 | B_rc=1
  round 3: A_rc=0 A_红行=1 | B_rc=1
  round 4: A_rc=0 A_红行=1 | B_rc=1
  round 5: A_rc=0 A_红行=1 | B_rc=1
  round 6: A_rc=0 A_红行=1 | B_rc=1
  round 7: A_rc=0 A_红行=1 | B_rc=1
  round 8: A_rc=0 A_红行=1 | B_rc=1
自然竞态: A 非零退出的轮数 = 0 / 8
```
（`A_红行=1` 是探针 grep 的口径噪声：软模式下 D2 软提示会把内层 `❌ 未登记:` 行原样列出，A 的 rc 仍为 0；已单独核对。）

**结论 ②**：**同树并发 git/门禁活动未能翻转夹具结果**（6 次夹具跑 + 8 轮 A/B 并发 + 2 次 T3f 专项探针，反例 0）。

### 2.3 对队长两条假设的逐条判定

**假设 1（T3f 第二跑走真实 `git diff --cached` → 非密封）——事实成立，但翻转不了结果。**
- 事实核验 ✅：`tests/control-tower/gate-failopen-net.test.sh:181-185` 用 `env -u SYNO_TEST_ARM -u GITHUB_ACTIONS -u SYNO_DIFF_BASE -u SYNO_GIT_CACHED_DIFF` → `scripts/pre-commit-check.sh:281-284` 走真实 `git diff --cached`，注入缝整体失效（我另用 C2b 证明：未武装时连 `SYNO_GIT_CACHED_DIFF=+const x = new DiagnosticModule();` 都被忽略 → 7a 仍 ✅）。
- 但该断言（L186）只做一件事：`grep -q "降级"`。我穷举了生产路径上「会打印『降级』」的全部落点（`grep -c "降级" scripts/pre-commit-check.sh` = **22 处出现**，其中**真正会被打印的 5 处 echo + 1 处 soft_check 后缀**）：
  | 落点 | 触发条件 | 在 T3f 第二跑是否可达 |
  |---|---|---|
  | `:99` `soft_check "$1（沙箱夹具，降级）"` | `_SANDBOX=1`（`:92-96`） | **不可达**：T3f 显式 `-u SYNO_TEST_ARM`，`_SANDBOX=0` |
  | `:246` `[GATEKEEPER] 已人工确认 (SYNO_GATEKEEPER_ACK=1) — 降级为告警放行本次提交` | `.claude/bypass.log` 存在 + 当日 `detected-bypass` 计数 > 0 | **实测 0 条/今日 → 不可达**：`grep -c "$(date +%Y-%m-%d).*detected-bypass" .claude/bypass.log` → `0`（全文件 19 条，最新一条 2026-09-17）。**且该落点一旦可达会让 T3f #1 与 #2 同时红（=2 失败）；无 ACK 时更会在 `:252` 提前 `exit 1` 让 T1/T1s/T4/T5/T5c 一起红** —— 两种形态都与「19 通过, 1 失败」不符，故**作为本次失败的解释被排除**（唯一无法回溯的窗口：它在 T3f #1 与 #2 之间的秒级间隙出现，随即被 `git restore` 抹去） |
  | `:837` `主树占用检测: session-registry 不可读 — 降级放行` | 主树脏 + registry 不可读 | **不可达**：`:808` 被夹具的 `SYNO_SKIP_PARALLEL_WARN=1` 整块跳过 |
  | `:874` `D707: python3 不可用 — 降级登记` | `STAGED_SRC` 非空 + 无 python3 | **不可达**：暂存无 `.ts` 时整块不进入；本机/python3 在 CI 均在 |
  | `:1033/:1037` 组 7a 自身降级 | `_DIAG_EXCL` 非法或 grep 不可用 | **不可达**：生产模式 `_DIAG_EXCL` 为硬编码合法模式（覆写仅在 `SYNO_TEST_ARM=1` 生效） |
  - 另核 `check-secrets.sh:62` 的降级打印位于 `if [ -n "$FULL_SCAN" ]` 内，而门禁调用它时**不带参**（`:331/:366/:413`）→ 不可达。

**假设 2（L175/L186 扫整份输出，其它组也打「降级」）——断言设计脆弱性成立，但当前无反例。**
- 脆性 ✅：L175/L186 的 `grep -q "降级"` 确实扫**整份输出**，而非只扫组 7a 行。
- 反例 ✗：可用性上只能由 §2.3 表中断言的 5 个落点提供，而这些落点当前全部被门控（上表）。
- **另有反向证据（假绿而非假红）**：我实测证明「降级字样」这一断言**不具判别性**——见 §四-①。

### 2.4 ③ 结论：该夹具在 CI（干净检出、单线程、ubuntu+windows）是否可能红？

**判定：不会因「并发」而红；但存在两条结构性风险面，其中平台面我无法在本机证伪。**

**（a）并发面——结构上排除。** CI 的 `control-tower-tests` job（`.github/workflows/ci.yml:202-280`）是单 job、单工作树、串行 `for t in ...; do bash "$t"`，不存在第二个 `pre-commit-check.sh` 与该夹具同树并发。同树并发是本机复现失败的**唯一**可疑面（共享临时路径，见下），CI 里该面不存在。

**（b）CI 下的 T3f 第二跑输入 = 空索引。** `actions/checkout@v4` 干净检出 → `git diff --cached` 为空 → 与我的 §三-C 的 C2a 情形完全同构（未武装 + 真实暂存区(空) + `SYNO_DIAG_EXCL_OVERRIDE='^+++'`）：
```
── C-c2a(未武装+override+真空暂存) rc=0
       ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
     降级字样行数=0
```
且我核了密封清单里**夹具之前**的全部会改索引的测试（`grep -nE 'git +(add|restore|reset|stash|checkout)'`），其目标全为临时沙箱（`$SB` / `$TMP*` / `$CT63_DIR` / `$CLEAN` / `$SUB`）；`post-commit-marker.test.sh:46` 的 `REPO="$TMPROOT/$1"` 亦为沙箱 → **CI 检出树的索引不会被前序测试污染**。

**（c）T5c 在 CI 可达**：`ci.yml:211-213` `fetch-depth: 0` → `git show 58a19796` 可解析，T5c 不会因浅克隆转红（改前树的 `T5c 不可达` 红只出现在 `git archive` 无历史副本里，见 §三-G6）。

**（d）无法证伪的平台面（未清项 1）**：家族网对 `scripts/**/*.sh` 的**全部字面 `grep -E` 模式**做 `grep -E -e PAT /dev/null`（rc==2 判非法）。本机只有 BSD grep / BWK awk（`ggrep`/`gawk`/`mawk` 均缺，我未安装任何工具），**GNU grep（ubuntu）与 windows Git Bash 下的判定未实测**。设计上已做平台探针（`平台探针: grep -E -e '^+++' /dev/null → rc=?`）与「括号不平衡」这类三平台一致非法的金丝雀，但**"BSD 接受 / GNU 拒绝"的模式差异只能由 ubuntu 实跑证伪**。

**（e）最可能的失败面（若 CI 真红）不是并发**：家族网读的是**工作树** `scripts/**/*.sh` 而非提交对象 —— 任何在夹具运行期对 `scripts/` 的写操作都会改变计数。CI 中该面由"同一 runner 串行、无写入者"消解。

---

## 三、判据 A–G 逐条原始输出

### A 判据①：注入真违规 → `exit 1` 且点名组 7a

（夹具 T2/T2b 手跑，`68b3c2f2`）
```
  ✅ T2 判别: 注入被禁模块用法 → CI strict exit 1 且组 7a 判 ❌（非假绿）
  ✅ T2b 判别: 带 +++ 头的 src/x.ts 命中 → exit 1 + 组 7a ❌
```
直调生产脚本（我自己的命令，不经夹具）：
```
── C-c4-ts-control  rc=1
     ❌ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口: 1 处  [CI strict——软提示在 CI 上为硬阻断]
        +const y = "<p>DiagnosticModule 已迁移到 Sentinel</p>";
```
反向对照（M1 变异把 `^+++` 放回默认模式 → 同一注入不再被点名）：见 §三-D 的 M1 行。

### B 判据②：无误伤

- 干净树：§二-2.1 的 19 次全绿（`rc=0`，唯一 ✅/⚠️ 红集合是既有的 `⚠️ PRD 对照: Done 标准引用 PRD 章节(可选)`）。
- `58a19796` 全文：
```
# 朴素修复口径（仅把 ^+++ 转义）
$ git show 58a19796 | grep "^+.*DiagnosticModule" | grep -Ev "scripts/pre-commit-check.sh|.md|.html|//|@deprecated|import type|^\+\+\+|hard_check|禁止新 DiagnosticModule|不要再使用 DiagnosticModule" | wc -l
5
# 新管道口径（自脚本 L1004-1023 原样抽取执行）
候选行数: 1
grep rc=1（1=无命中）
命中数: 0
```
（5 条命中原文与 c1 §二-③ 逐字一致，全部来自 `.html` 文档正文。）

### C 判据③：三路径（我自己的命令，不依赖夹具自述）

```
── C-c0-normal  rc=0        ✅ 禁止 DiagnosticModule…（7a ✅ 行=1）        降级字样行数=2
── C-c1-degraded  rc=1
     ⚠️  禁止新 DiagnosticModule: 检查降级 — grep 不可用或排除模式非法 (rc=2)；本项未判定
     ❌ 禁止新 DiagnosticModule: 检查降级  [CI strict——降级即失败]        7a-✅行=0
── C-c3-comments  rc=0      ✅（三注释形态零误报，7a ✅ 行=1）
── C-c3b-html  rc=0         ✅（.html 正文零误伤，7a ✅ 行=1）
── C-c4-ts-control  rc=1    ❌ 1 处（同正文挂 .ts 头 → 判别对照成立）     7a-✅行=0
──────────────── C2 生产 fail-closed（不武装 SYNO_TEST_ARM）────────────────
── C-c2a(未武装+override='^+++' + 真实(空)暂存) rc=0  ✅ 7a 降级字样行数=0
── C-c2b(未武装+注入缝 SYNO_GIT_CACHED_DIFF=真违规 + 真实(空)暂存) rc=0  ✅ 7a
```
**降级行的来源逐条核对（C1 输出，去 ANSI 后行号）**：
```
47:  ✅ Task Brief: 编码变更须有今日 task brief（沙箱夹具，降级）      ← 与组 7a 无关（scripts/pre-commit-check.sh:99）
48:  ✅ Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)（沙箱夹具，降级）← 同上
65:  ⚠️  禁止新 DiagnosticModule: 检查降级 — grep 不可用或排除模式非法 (rc=2)；本项未判定   ← 组 7a 本体
66:  ❌ 禁止新 DiagnosticModule: 检查降级  [CI strict——降级即失败]                          ← 组 7a 本体
沙箱后缀出现次数（与组 7a 无关者）= 2
```
即：**降级路径确实显式降级且不判 ✅**（7a ✅ 行 = 0），同时暴露了 §四-① 的判别力问题。

### D 变异体独立复现（本卡核心独立性；全部在 `/tmp/d937-v-mut` 由我自行改坏）

变异施加方式：Python 定点替换（每处 `assert count==1` 锚点校验），施加后用 `git diff --stat` 确认改动面，跑完后 `git -C /tmp/d937-v-mut checkout -- scripts/pre-commit-check.sh` 复原并复跑。

| # | 变异内容 | 施加后 diff | 变异后（红） | 复原后（绿） |
|---|---|---|---|---|
| M1 | 排除模式转义 → 放入非法 `^+++`（恢复病根） | `2 +-`（1 增 1 删） | **rc=1, 13 通过 7 失败** | **rc=0, 20 通过 0 失败** |
| M2 | 删降级分支 → 裸 `\|\| true`（静默吞错） | `2 +-` | **rc=1, 18 通过 2 失败** | **rc=0, 20 通过 0 失败** |
| M3 | 逐文件豁免 → 按行子串（回到旧口径） | `14 +-`（2 增 12 删） | **rc=1, 18 通过 2 失败** | **rc=0, 20 通过 0 失败** |
| M4 | 注释豁免 `^\+[[:space:]]*(//\|/\*\|\*\|#)` → 裸 `//` | `2 +-` | **rc=1, 19 通过 1 失败** | **rc=0, 20 通过 0 失败** |
| M5 | 整段删组 7a（56 行） | `56 ----`（56 删） | **rc=1, 9 通过 11 失败** | **rc=0, 20 通过 0 失败** |

**变异后打红的具体夹具项（原始行）**：
```
M1-red:  ❌ T1 正常: 干净 diff 应 7a ✅，实际: ⚠️ 禁止新 DiagnosticModule: 检查降级 — grep 不可用或排除模式非法 (rc=2)；本项未判定
         ❌ T1s 正常(CI strict): 应 7a ✅，实际: 同上
         ❌ T3f 无缝合: 未武装时覆盖变量仍生效（生产可被覆盖 = fail-open）: 同上
         ❌ T3f 无缝合: 真实暂存区路径下覆盖变量生效（fail-open）
         ❌ T4 边界: 注释形态被误报，exit=0 行=[…检查降级…]
         ❌ T5 边界: 文档正文被误伤，exit=0 行=[…检查降级…]
         ❌ T5c 真实样本: 58a19796 误报，行=[…检查降级…]
M2-red:  ❌ T3 降级: 组 7a 在非法排除模式下仍判 ✅（fail-open 未修）: ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
         ❌ T3 降级: CI strict 下降级应 exit 1（fail-closed），实际 0
M3-red:  ❌ T5 边界: 文档正文被误伤，exit=0 行=[⚠️ 禁止 DiagnosticModule…: 1 处 [V5 软提示…]]
         ❌ T5c 真实样本: 58a19796 误报，行=[⚠️ 禁止 DiagnosticModule…: 5 处 [V5 软提示…]]
M4-red:  ❌ T4 边界: 注释形态被误报，exit=0 行=[⚠️ 禁止 DiagnosticModule…: 2 处 [V5 软提示…]]
M5-red:  ❌ T1 / T1s / T2 / T2b / T3(×2) / T3f(#1) / T4 / T5 / T5b / T5c —— 全部 7a 相关项
         （T1 原文：❌ T1 正常: 干净 diff 应 7a ✅，实际: <无 7a 行>；T2 原文：❌ T2 判别: 应 exit 1 + 组 7a ❌，实际 exit=0 行=[<无 7a 行>]）
```
**判别力说明**：M1→T1/T4/T5/T5c 红（`^+++` 病根回归即被抓）；M2→T3 两条红；M3→T5/T5c 红且 `58a19796` 命中数精确回到 **5 处**（与 c1 §二-③ 的朴素修复数字同源）；M4→T4 红（未锚定注释豁免 → 2 处误报）；M5→11 项全红（删掉即报红 = 组 7a 确实在夹具的测量路径上）。

### E 家族网 + **我自造**的金丝雀（不采信测试自述）

在 `/tmp/d937-v-canary`（独立 worktree）自建 3 个文件：
```
scripts/zz-v-canary-bad.sh : grep -E "a(b" /dev/null                       ← 非法 ERE
scripts/zz-v-canary-good.sh: grep -E "^[[:space:]]*(a|b)+$" /dev/null      ← 合法 ERE（对照）
scripts/zz-v-canary-dyn.sh : P="$1"; grep -E "$P" /dev/null                ← 动态（应入"动态不可判"）
```
独立（不经夹具）确认该模式在本平台非法：
```
$ LC_ALL=C grep -E -e 'a(b' /dev/null >/dev/null 2>&1; echo rc=$?
rc=2
$ grep -E -e 'a(b' /dev/null 2>&1
grep: parentheses not balanced
```
跑夹具：
```
rc=1
    扫描 144 个 .sh ｜ 可判 278 ｜ 未解析 0 ｜ 非法 1
    （动态不可判 22 ｜ 非 ERE 跳过 592）
    ❌ 家族网: 非法 ERE 1 个:
        /tmp/d937-v-canary/scripts/zz-v-canary-bad.sh:3	a(b
    ✅ 家族网: 未解析 0 个（无静默跳过）
  结果: 19 通过, 1 失败
```
**计数自洽**：基线 141/277/20 → 加 3 文件后 144/278/22 = 文件 +3、可判 +1（bad）、动态 +2（dyn 的 `$P` + good 的双引号内含 `$` 锚点，见 §四-③）。**只有 bad 被点名，两个对照文件未被误判**，且全跑只有 1 条红。

### F 反查「接线了≠被执行」

```
── F1 ci.yml 密封清单 ──
     256:            tests/control-tower/gate-failopen-net.test.sh \
── F2 夹具语法 ──  bash -n exit=0
── F3 gate-hits 日志（组 7a 是否真被执行并登记）──
     DiagnosticModule 命门禁登记行数: 1
     17:{"time": "2026-09-24T00:36:04Z", "gate": "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口", "result": "miss", "branch": "fix/d937-gate-failopen"}
── F4 组 7a 在脚本执行流中的位置（下一组 # 7b 紧随其后）──
     1047:# 7b. 专家配置校验 (原 9)
```
- 清单在 `for t in …; do … if ! bash "$t" …; then FAIL=1; fi; done; exit $FAIL`（`ci.yml:224-280`）→ 夹具失败会真红。
- F3 是**执行证据**（不是"定义了"）：组 7a 每次运行都把 `result:"miss"` 写进门禁日志。
- 独立第二重：M5（删掉组 7a）使 11 项夹具断言转红 → 组 7a 确实位于被测量的执行路径上。

### G 数字逐条复核（全部一致）

| 声称（c1 / c2） | 我的复跑命令 | 我的结果 | 判定 |
|---|---|---|---|
| `141 .sh / 可判 277 / 未解析 0 / 非法 0` | `bash tests/control-tower/gate-failopen-net.test.sh`（68b3c2f2，08:2x–08:37 本地） | `扫描 141 个 .sh ｜ 可判 277 ｜ 未解析 0 ｜ 非法 0` | **一致** |
| `ci.yml +1 行` | `git diff origin/main...HEAD --stat -- .github/workflows/ci.yml` | `1 file changed, 1 insertion(+)` | **一致** |
| `+522/-2` | `git show --stat 8ca6329f` | `3 files changed, 522 insertions(+), 2 deletions(-)` | **一致** |
| `+712` | `git show --stat b2137083` | `3 files changed, 712 insertions(+)` | **一致** |
| `20 通过` | 夹具结果行（19 次） | `结果: 20 通过, 0 失败` | **一致** |
| 改前树 `13 通过 7 失败` | `git archive 2cda1528 \| tar -x -C /tmp/d937-v-prefix` + 拷入夹具后跑 | `结果: 13 通过, 7 失败`，rc=1；失败集与 c2 §6 逐项一致（T2/T2b/T3#1/T5b/T5c/家族网非法 1/接线） | **一致** |
| `58a19796 命中 5→0` | 见 §三-B 两条命令 | 朴素 5 / 新管道 0 | **一致** |

补充复核（我自己发起的）：
```
$ git diff --stat 2cda1528 origin/main -- scripts/pre-commit-check.sh
（空 = 无差异）
```
→ 改前基线 `2cda1528` 与当前 `origin/main`(`12383f14`) 在被测文件上**无差异**，"改前基线"结论对当前 main 仍成立。

---

## 四、独立新发现（我跑出来、c1/c2 报告未载）

**① 夹具 `T3 降级: stdout 含显式「降级」字样`（L154-158）不具判别性 —— 实测可被「沙箱后缀」满足。**
变异 M2（删掉降级分支）下该断言**仍为 ✅**：
```
    ❌ T3 降级: 组 7a 在非法排除模式下仍判 ✅（fail-open 未修）:   ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
    ✅ T3 降级: stdout 含显式「降级」字样
    ❌ T3 降级: CI strict 下降级应 exit 1（fail-closed），实际 0
```
证明其字面量来源（M2 下按 T3 原命令形态直调生产脚本，只看门禁 stdout）：
```
rc=0  门禁 stdout 中「降级」行数=2
     47:  ✅ Task Brief: 编码变更须有今日 task brief（沙箱夹具，降级）
     48:  ✅ Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)（沙箱夹具，降级）
7a 判定行: ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
```
→ 该 ✅ 的真实来源是 `scripts/pre-commit-check.sh:99` 的沙箱后缀，**与组 7a 的降级语义无关**。本次修复的有效性不受影响（T3 另两条断言在 M2 下均红），但 **20 条断言里这 1 条不携带信号**；"20 通过"的强度应按 19 条有效断言理解。

**② 家族网对本次修复的核心模式**（`_DIAG_EXCL`）**零覆盖 —— 变异 M1 可绕过它。**
M1 把非法 `^+++` 放回默认排除模式后，家族网仍报：
```
    扫描 141 个 .sh ｜ 可判 277 ｜ 未解析 0 ｜ 非法 0
```
原因：模式已抽到变量（`scripts/pre-commit-check.sh:1005`）→ 经 `"$_DIAG_EXCL"` 传递 → 计入"动态不可判"桶（可判 277 / 动态 20 与修复前完全相同）。擦除该回归的**只有行为断言**（T1/T4/T5/T5c 红）。c2 §8-1 已披露此盲区，我实测确认，并补充：**改前树的同类非法 ERE 能被抓到（改前 `非法 1`，点名 `pre-commit-check.sh:991`），修复后抓不到了**——覆盖方向从"抓到"变成"靠行为断言兜底"。最坏后果是降级（fail-closed），不是假绿。

**③ 解析器把双引号内含 `$` 的合法 ERE 计入"动态不可判"。**
我自建的对照文件 `grep -E "^[[:space:]]*(a|b)+$"`（双引号、合法）未进"可判"桶（可判仅 +1 = bad 那条），动态 +2 —— 原因是 AWK 分类器在双引号分支对 `$` 一律置 `dyn=1`（`tests/control-tower/gate-failopen-net.test.sh:278`）。方向是 fail-safe（跳过而非误判），但意味着**任何双引号包裹且含 `$` 锚点的模式都不进编译检查**，覆盖应记为"可判 ∩ 不含 `$`"。

**④ 同一工作树内并发运行 `pre-commit-check.sh` 存在共享固定路径（机制存在，实测未翻转结论）。**
```
scripts/pre-commit-check.sh:391-407  PAR_DIR="$ROOT/.claude/.precommit-par"; rm -rf "$PAR_DIR"; par_start 写 $name.out/$name.code; par_collect 读
scripts/pre-commit-check.sh:1317-1319,1371  SCOPE_TSV="$ROOT/.claude/.g12-scope.tsv"; rm -f; 写; python 读; rm -f
```
→ 第二个并发运行会 `rm -rf` 掉第一个的中间产物，`par_collect` 此时读到"无 `.code` → 当 0"（**假绿**方向）或读到对方的 `.code/.out`（**交叉读取**方向）。我在 8 轮 A/B 并发探针中**未观测到翻转**（A 全 rc=0），故此条只作"再现时的候选机制"披露，不作为本次失败的解释。

**⑤ 与 c1 「未清项 2」相反的实测结论：D2 登记门禁在 CI 的干净检出下为 0 检查，不会红。**
c1 `D937-改动清单.md` §四-2 写「该门禁本地不阻断，但 **CI 会红**」。我在CI 等价状态（索引干净 + D937 文档已跟踪）实测：
```
$ git ls-files --others --exclude-standard | grep -cE '\.(md|yaml)$'      → 0
$ git diff --cached --name-only --diff-filter=A | grep -cE '\.(md|yaml)$' → 0
$ bash scripts/doc-system/doc-registry-gate.sh
── 汇总: 检查 0 个文档，0 个未登记 ──
  ✅ 登记门禁通过                                    exit=0
```
依据脚本自身的扫描基准（`scripts/doc-system/doc-registry-gate.sh:47-50`：只取 `ls-files --others` + `diff --cached --diff-filter=A`），**CI 里 PR 文件已在提交中、索引为空 → 判断域为空**；本地那次 ⚠️ 是因为当时这些文档尚未 `git add`。且我核了 `quality` job 的步骤序列（`ci.yml:10-104`）中**没有** `git add` 类前置动作。→ 「CI 会红」这一声称与我实测不符（**不实**，非"环境依赖"）。**该条不影响本次修复**，但会误导合并决策（CI 权威 vs 本地软提示的口径）。

---

## 五、未清项 / 未验证面（诚实披露，非"通过"声明）

1. **矩阵外平台未实测**：本机无 GNU grep / gawk / mawk（`ggrep`/`gawk`/`mawk` 均 `(缺)`，我未安装任何工具、未联网），故 ubuntu-latest / windows-latest 的家族网判定**未独立验证**。可证伪路径：在 ubuntu runner 跑一次 `bash tests/control-tower/gate-failopen-net.test.sh`，比对 `非法` 与 `未解析` 两个桶。
2. **队长侧那次失败仍未解释**：19 次基线 + 6 次副本 + 8 轮并发探针无反例；我给出的 4 条结构性候选（§2.3、§四-②④）均被实测否决或未能触发。**若再现，请先抓失败断言原文**（`bash tests/control-tower/gate-failopen-net.test.sh 2>&1 | grep '❌'`）。
3. **夹具运行会产生工作树瞬态文件**（`$ROOT/.claude/.precommit-par/`、`$ROOT/.claude/.g12-*.tsv`、`$ROOT/.codex/checkpoints/cp3-commit-check.json`、`$ROOT/.codex/control-tower/logs/degraded-events.log`）。前三者被 `.gitignore` 覆盖或在足迹内自清理，`.g12-*.tsv` **不在 `.gitignore`**（c1 §四-4 已提）：正常跑会自删，但被 kill 的跑会留下 `?? .claude/.g12-*.tsv`，任何人的 `git add -A` 会误提交。我本次收尾已确认交付工作树无此残留。
4. **分支落后基线 6 个 commit**（`origin/main = 12383f14`，`git rev-list --left-right --count origin/main...HEAD` = `6 8`）。这 6 个 commit 不触碰 `scripts/pre-commit-check.sh`（§三-G 补充复核），与本卡零冲突；合并前需 rebase/ff（**队长/CTO 处置**）。
5. **本文件自身未登记 `docs/authority/DOCS-REGISTRY.yaml`**：作为未跟踪新 `.md`，本地跑门禁会得到一条 **软提示**（不阻断）；按 §四-⑤，CI 下不会红。

---

## 六、红线遵守 + 收尾状态

- **只读交付工作树**：唯一写入 = 本文件。所有"改坏源码"动作只在 `/tmp/d937-v-mut`（独立 worktree）发生，每次施加都用锚点 `assert count==1`，每次跑完 `git checkout -- scripts/pre-commit-check.sh` 并复跑证明绿。
- 未使用 `--no-verify` / `git stash` / `force push`；未触碰 `scripts/audit/**`；未改任何审计标准；未自我审计（本报告不含"通过"结论）。
- 独立 worktree 清理：`/tmp/d937-v-mut`、`/tmp/d937-v-stage`、`/tmp/d937-v-canary` 已 `git worktree remove`（回执见交付消息）。
- 交付工作树收尾：`git status --short` 仅剩本文件（见交付消息中的最终回执）。
- 原始输出留档（非仓库内，/tmp）：`/tmp/d937-v-flaky/`、`/tmp/d937-v-mut-logs/`、`/tmp/d937-v-CEF/`、`/tmp/d937-v-prefix-logs/`、`/tmp/d937-v-canary.out`。

---

**声明**：本文件是**独立自验结论**（`可提请独立审计`），不是审计报告，也不对"是否通过/是否合并"作判定。是否通过归 CTO 收件闸 + K3 终审。
