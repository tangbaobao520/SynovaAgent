# D938-夹具原始输出（编码B · d938-c2 · task-6）

> 生成方式: `bash /tmp/gen-d938-evidence.sh` —— **本文所有数字均来自命令原始输出**（无手写）。
> 工作树: `/Users/wane/SynovaAgent/.synova-wt-squad-d938` ｜ 分支 `fix/d938-alloc-task-id` ｜ 基线 `origin/main` = `f25e61eb`

## 0. 交付物（写集内 6 文件）

| 文件 | 动作 | 说明 |
|---|---|---|
| `scripts/control-tower/scan-fullwidth-vars.sh` | 新建 | 参数化扫描器（`--paths`/`--paths-file`/`--domain mac\|win`/`--json`），三桶分类，退出码三态 0/1/2 |
| `tests/control-tower/scan-fullwidth-vars.test.sh` | 新建 | 配对测试（接线 / 金丝雀反空转 / 三桶边界 / fail-closed / 负例 / 自清 / 棘轮） |
| `tests/control-tower/alloc-task-id.test.sh` | 扩 | 第 9 节判别性夹具（中文·斜杠 title / A′ 反吞 / 合法提前退出） |
| `.github/workflows/ci.yml` | +1 行 | 新测试入 `control-tower-tests` 密封清单（防 D664「建成未接线」M3） |
| `tests/control-tower/alloc-task-id-lock.test.sh` | **不动** | 见 §5 三条实测证据 |
| `docs/synova/product-lines/evidence/D938-夹具原始输出.md` | 新建 | 本文 |

## 1. 前提冻结（开工前实测，不采信转述）

### 1.1 全角标点危害矩阵（bash 3.2.57 = macOS /bin/bash = 控制塔实际执行环境）

逐字符实测 `set -u; FOO=bar; echo "$FOO<c>"`：命中 `unbound variable` = 该字符被 bash 当作变量名字符。

```
$ bash --version | head -1
GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25)
$ for c in （）：，。；、) ; echo "set -u;FOO=bar;echo \"\$FOO$c\"" | bash ; done   # 逐字符
  全角 （ → rc=1 （unbound：真危害）
  全角 ） → rc=1 （unbound：真危害）
  全角 ： → rc=1 （unbound：真危害）
  全角 ， → rc=1 （unbound：真危害）
  全角 。 → rc=1 （unbound：真危害）
  全角 ； → rc=1 （unbound：真危害）
  全角 、 → rc=1 （unbound：真危害）
  ASCII ) → rc=0 （无害，对照组）
  ASCII : → rc=0 （无害，对照组）
  ASCII , → rc=0 （无害，对照组）
  ASCII . → rc=0 （无害，对照组）
  ASCII ; → rc=0 （无害，对照组）
$ 非 set -u 时的静默吞字（字节级）: FOO=bar; echo "[$FOO）]" | od -c
0000000    [ 274 211   ]  \n                                            
0000005
   → 输出 [ BC 89 ] = 全角「）」剩后 2 字节：首字节被 bash 当变量名吃进 → 文本损坏（无 set -u 也坏）
[exit code: 0]
```

### 1.2 真实生产行摘录（set -euo pipefail 下）
```
$ printf "set -euo pipefail\nEXIT_CODE=1\necho \"⚠️ 本地门禁未通过（exit=\$EXIT_CODE）— 已放行\"\necho TAIL\n" > /tmp/real1.sh; echo "--- install-hooks.sh:54 摘录 ---"; bash /tmp/real1.sh; echo "rc=$?"; printf "set -euo pipefail\nSUMMARY=x\necho \"PROBE: FRESH-GREEN — 台账已刷新（\$SUMMARY）\"\necho TAIL\n" > /tmp/real2.sh; echo "--- doc-truth-probe.sh:207 摘录 ---"; bash /tmp/real2.sh; echo "rc=$?"
--- install-hooks.sh:54 摘录 ---
/tmp/real1.sh: line 3: EXIT_CODE: unbound variable
rc=1
--- doc-truth-probe.sh:207 摘录 ---
/tmp/real2.sh: line 3: SUMMARY: unbound variable
rc=1
[exit code: 0]
```

### 1.3 正确口径 vs 卡面口径（基线对照 + 当前树实测）
```
$ bash /tmp/d938-count.sh
== 基线 f25e61eb（git grep，卡面基线所在的提交）==
  单字符口径（卡面前提）: 23 处 / 14 文件
  正确口径（6 字符类）  : 40 处 / 22 文件（代码违规 36 处，注释 4 处无害）
  → 卡面前提漏掉: 17 处 / 8 文件
== 当前树（c1 已修 + 本卡新增扫描器）==
  单字符口径: 20 处
  正确口径  : 35 处 / 19 文件
计数口径: grep -rnE + 排除项=无 + 截至 2026-09-24T07:03:31Z
[exit code: 0]
```

### 1.4 卡面前提不成立 → 已上报 → CTO 裁定 A1
```
实测（原始输出见 1.3，本机独立复跑，非转述）:
  · 基线 f25e61eb: 单字符口径 = 卡面前提（23 处 / 14 文件）；正确口径 = 40 处 / 22 文件
    → 卡面前提**漏 17 处 / 8 文件**（全角右括号等 5 类标点紧跟 $VAR 的形态全在被漏之列）
  · 当前树（c1 已修 + 本卡新增扫描器）: 正确口径 34 处 / 19 文件；单字符口径 19 处
卡面前提: 「mac 域闭集 = 4 文件 / 6 处」同理只在单字符口径下成立；正确口径下 mac 域
          写集外另有 6 文件（8 处）真炸 → 卡面「mac 域残留 0」在全 mac 域不可达。

CTO 裁定 A1（lead 转达 2026-09-24）:
  · 扫描器按 6 字符口径实现（不缩回单字符 → 不漏 11 处真缺陷）
  · 写集保持 9 文件（扩到 15 会撞 PR 预算 12，check-pr-budget.sh 硬拦，禁调高上限）
  · 判据②改为「**本卡写集内** mac 域残留 = 0」；全 mac 域残余作登记信息
  · win 域期望 8 → 10 文件

字节清洗声明: 复现改前缺陷时 bash 的错误文本里变量名尾部含一个被它吃进的全角首字节
  （0xEF，非法 UTF-8 单字节）。本文用 `iconv -f UTF-8 -t UTF-8 -c` 丢弃该类非法字节，
  其余逐字保留 —— 否则产出文件本身不是合法 UTF-8（sed/awk/read 会直接报错）。
[exit code: 0]
```
## 2. 验收（task-6 五条 + 裁定后新增）

### 2.1 验收1: 扫描器配对测试（含金丝雀）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && bash tests/control-tower/scan-fullwidth-vars.test.sh
═══════════════════════════════════════════════════════════
  D938 scan-fullwidth-vars 扫描器测试
═══════════════════════════════════════════════════════════

── 0. 接线（扫描器存在 + 真匹配 + ci.yml 密封）──
  ✅ 扫描器存在: scripts/control-tower/scan-fullwidth-vars.sh
  ✅ 接线: 走 grep -E 家族（非 -P）
  ✅ 接线: 已入 .github/workflows/ci.yml control-tower-tests 密封清单

── 1. 负例: grep -P 静默失败陷阱（假绿家族）──
  ✅ 陷阱复现: grep -P → rc=2，但 `2>/dev/null` 后读到 0 命中（这就是假绿来源）
  ✅ 扫描器代码行零 grep -P（若出现 -P → 本测试必红）

── 2. 反空转金丝雀: 含 $VAR 紧贴全角的 .sh 必须被网抓到 ──
  ✅ 金丝雀被抓到: rc=1
  ✅ 金丝雀文件名出现在输出里
  ✅ 违规计数 = 1 处 / 1 文件

── 3. 边界: .ps1 计「需专项定性」，**不计违规**（防伪缺陷）──
  ✅ .ps1 不计违规: rc=0
  ✅ triage 桶收下 .ps1: 1 处 / 1 文件
  ✅ .ps1 未出现在违规清单里

── 4. 边界: 注释行金丝雀（无害）──
  ✅ 注释行不计违规: rc=0
  ✅ 注释桶收下: 1 处 / 1 文件

── 5. 边界: .md / .py 同样落 triage（文档/其他语言非本缺陷）──
  ✅ .md/.py 落 triage 且不计违规（rc=0, triage=2 处/2 文件）

── 6. fail-closed: 参数/路径/空集异常一律 exit 2（不与 0 混同）──
  ✅ 不存在路径 → rc=2
  ✅ 空扫描集 → rc=2（不判绿）
  ✅ 未知参数 → rc=2
  ✅ --domain 下域外路径 → rc=2（不猜域）
  ✅ 不可读文件 → rc=2（grep rc≥2 不当作「0 命中」）

── 7. 自清: 扫描器自身文件在网内零违规 ──
  ✅ 扫描器自身零违规（作者未把被测缺陷写进检测器）

── 8. 判据②: 本卡写集内 mac 域残留 = 0（严格判红/绿）──
  ✅ 本卡写集内 mac 域残留 = 0 处 / 0 文件（5 文件：c1 写集 4 + 扫描器自身）
  ✅ 写集模式零违规 → rc=0

── 8b. 登记: 全 mac 域残余（棘轮上限 6 文件 / 8 处）──
  ✅ 全 mac 域残余 6 文件 / 8 处 ≤ 上限 6 / 8
  ⚠️  全 mac 域残余 6 文件 / 8 处（写集外，按 CTO 裁定 A1 归新 mac 卡，本卡不动）—— 见 --domain mac 逐行清单
  ✅ 全 mac 域有残余时 rc=1

── 9. 接口: --domain win（Win 卡可直接复用；棘轮上限 10 文件）──
  ✅ win 待处理 10 文件 ∈ [1, 10]（域过滤生效）
  ✅ 全 win 域残余标签存在：10 18（文件 / 违规处）
  ✅ 域过滤真伪: win 模式列出的 9 个文件经 check-ownership.py --owner win 复核全部属 win

── 10. --json: stdout 是合法 JSON ──
  ✅ --json 输出可被 python json.load 解析且字段完整

── 11. --paths-file: 路径集从文件读入 ──
  ✅ --paths-file 生效（忽略注释/空行，抓到金丝雀 1 处）

═══════════════════════════════════════════════════════════
  结果: PASS=29 FAIL=0 SKIP=0
═══════════════════════════════════════════════════════════
[exit code: 0]
```

### 2.2 验收2: alloc-task-id 判别性夹具测试
```
$ bash tests/control-tower/alloc-task-id.test.sh
═══════════════════════════════════════════════════════════
  D384 alloc-task-id 分配器测试
═══════════════════════════════════════════════════════════

── 1. 正常分配 → D500 + 建壳 ──
  ✅ 分配 D500 (max=499 → +1=500 起步)
  ✅ 登记提示
  ✅ 空壳已建
  ✅ status=claimed

── 2. 连续分配 → D501 (单调递增) ──
  ✅ 第二次分配 D501

── 3. dry-run → 只预览不建壳 ──
  ✅ dry-run 预览 D502
  ✅ dry-run 标注
  ✅ dry-run 未建壳

── 4. 空任务名 → exit 1 + 用法 ──
  ✅ 空名拒绝 (exit=1)
  ✅ 用法提示

── 5. 撞车防护逻辑存在（并发竞态防御；单进程不可触发，靠唯一入口 + 建壳原子检查）──
  ✅ 防护逻辑存在 (建壳前检查)
  ✅ 正常流程不误触发 (exit 0) (exit=0)

── 6. D550: origin/main 占用合并（落后本地不漏号——D547/D548 撞号实证）──
  ✅ origin/main 合并: 空本地发 D942 > main max D941（不漏号）

── 7. CT-63: 远端分支名 D# 扫描 ──
  ✅ CT-63: 分支 d605 → 发 D606 > 605

── 8. CT-63 注入缝 NO_BRANCH=1 ──
  ✅ CT-63 NO_BRANCH: 发 D500 = 500

── 9. D938 判别性夹具: 中文/斜杠 title + A′ 反吞 + 合法提前退出 ──
  ✅ 9.1 中文 title rc (exit=0)
  ✅ 9.1 中文 title → stderr 空
  ✅ 9.1 分配 D500
  ✅ 9.1 骨架 1 个
  ✅ 9.2 含 / title rc (exit=0)
  ✅ 9.2 含 / title → stderr 空
  ✅ 9.2 骨架 1 个
  ✅ 9.2 无孤儿号（登记 1 个 ↔ 骨架 1 个）
  ✅ 9.2 骨架未裂成子目录（title 分隔符已消毒）
  ✅ 9.3 混合 title rc (exit=0)
  ✅ 9.3 混合 title → stderr 空
  ✅ 9.3 骨架 1 个
  ✅ 9.4 已从生产脚本提取 _lock_release（含显式 exit）
  ✅ 9.4 ① 生产 handler: unbound 中止 → rc=1 ≠ 0（fail-closed）
  ✅ 9.4 ② 对照组(改前形态): 同场景 rc=0 → 夹具能看见 fail-open（非空转网）
  ✅ 9.5 可达中止 → rc=1 ≠ 0（不留 rc=0 的假成功）
  ✅ 9.5 同成同败: 登记 0 个 / 骨架 0 个（回滚生效，无孤儿号）
  ✅ 9.6 dry-run 合法提前退出 rc (exit=0)
  ✅ 9.6 dry-run 标注
  ✅ 9.6 dry-run stderr 空
  ✅ 9.7 D718 守卫合法提前退出 rc（防哨兵误伤） (exit=0)
  ✅ 9.7 守卫仍按降级路径告警

═══════════════════════════════════════════════════════════
  结果: PASS=37 FAIL=0
═══════════════════════════════════════════════════════════
[exit code: 0]
```

### 2.3 验收3a: 判据②（裁定后口径）本卡写集内 mac 域残留 = 0
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac --paths scripts/control-tower/alloc-task-id.sh,scripts/control-tower/check-sentinel-type-net.sh,scripts/doc-system/check-doc-truth.sh,scripts/doc-system/doc-truth-probe.sh,scripts/control-tower/scan-fullwidth-vars.sh
═══ scan-fullwidth-vars（D938）— $VAR 紧贴全角标点 扫描 ═══
口径: 命令=bash scripts/control-tower/scan-fullwidth-vars.sh --paths scripts/control-tower/alloc-task-id.sh,scripts/control-tower/check-sentinel-type-net.sh,scripts/doc-system/check-doc-truth.sh,scripts/doc-system/doc-truth-probe.sh,scripts/control-tower/scan-fullwidth-vars.sh --domain mac
      模式=\$[A-Za-z_][A-Za-z0-9_]*(（|）|：|，|。|；|、)
      排除=无（非 POSIX shell 文件归「需专项定性」，不计违规）
      截至=2026-09-24T15:03:35+08:00 ｜ locale=C.UTF-8
扫描集: 5 文件（--paths/--paths-file 指定）｜ 域过滤=mac
────────────────────────────────────────────────────────────
【违规（代码行）】0 处 / 0 文件
【注释行（无害）】3 处 / 1 文件
  scripts/control-tower/scan-fullwidth-vars.sh:5:# 背景: bash 在 UTF-8 locale 下把全角标点当变量名字符——`$BRIEF_FILE（填写后开工）`
  scripts/control-tower/scan-fullwidth-vars.sh:44:#   · 单引号字符串内的 `$VAR（` **字面量**同样会被计入「违规（代码行）」（典型: 生成该形态文本
  scripts/control-tower/scan-fullwidth-vars.sh:317:# 纯注释命中（`# …$VAR（…`）在 shell / PowerShell 里都无害 → 不算残余。
【需专项定性】0 处 / 0 文件（非 POSIX shell，语义不同 → 不计违规）
────────────────────────────────────────────────────────────
标签口径: 已清=命中但零违规的文件数 ｜ 违规=代码行（处 / 文件）｜ 残余=非注释命中文件数
本卡写集内 mac 域残留：0 处 / 0 文件
mac 域：1 文件已清 / 违规 0 处 / 0 文件
win 域：未扫描（--domain mac 限定）
退出码: 0（扫描集内零违规）
[exit code: 0]
```

### 2.4 验收3b: 全 mac 域残余（登记信息，非失败条件）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac
═══ scan-fullwidth-vars（D938）— $VAR 紧贴全角标点 扫描 ═══
口径: 命令=bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac
      模式=\$[A-Za-z_][A-Za-z0-9_]*(（|）|：|，|。|；|、)
      排除=无（非 POSIX shell 文件归「需专项定性」，不计违规）
      截至=2026-09-24T15:03:35+08:00 ｜ locale=C.UTF-8
扫描集: 208 文件（默认 scripts/**）｜ 域过滤=mac
────────────────────────────────────────────────────────────
【违规（代码行）】8 处 / 6 文件
  scripts/control-tower/check-ci-stale-red.sh:67:  echo "degraded: CI API 拉取失败（$RESP）" >&2
  scripts/control-tower/pre-dispatch-check.sh:52:  echo "  ⚠️ degraded: 引用核验器缺失（$CITE_PY）— 本项跳过（显式，不静默）"
  scripts/doc-system/generate-chronicle-monthly.sh:46:  echo "  ⚠️ 降级: 当前目录不是 git 仓库（DOC_TRUTH_ROOT=$ROOT），跳过提交统计"
  scripts/install-hooks.sh:54:  echo "⚠️ 本地门禁未通过（exit=$EXIT_CODE）— 已放行，CI 将作为权威判定（merge 前必须绿）" >&2
  scripts/install-hooks.sh:77:    echo "  !! $name 无入口（$entry / $tracked）— 跳过"
  scripts/workflow/task-start.sh:81:    echo "⚠ D539: 无法解析 git-dir（$PROJECT_ROOT）— worktree 检查降级放行（铁律 11，不静默）" >&2
  scripts/workflow/task-start.sh:102:      echo "   → 正在创建 worktree（worktree-manager.py create $CREATE_WT）" >&2
  scripts/workflow/verify-incremental.sh:66:  echo -e "${RED}[LOOP] 已达最大循环次数 $MAX，停止自动修正。请人工介入。${RESET}"
【注释行（无害）】5 处 / 3 文件
  scripts/control-tower/scan-fullwidth-vars.sh:5:# 背景: bash 在 UTF-8 locale 下把全角标点当变量名字符——`$BRIEF_FILE（填写后开工）`
  scripts/control-tower/scan-fullwidth-vars.sh:44:#   · 单引号字符串内的 `$VAR（` **字面量**同样会被计入「违规（代码行）」（典型: 生成该形态文本
  scripts/control-tower/scan-fullwidth-vars.sh:317:# 纯注释命中（`# …$VAR（…`）在 shell / PowerShell 里都无害 → 不算残余。
  scripts/install-hooks.sh:99:  #   @input  — $ROOT（仓库根）+ env: SYNO_GIT_NAME(默认 synova-mac) / SYNO_GIT_EMAIL(默认 claworg@users.noreply.github.com)
  scripts/pre-push-check.sh:428:  # 优先 $PUSH_REMOTE/$PUSH_BRANCH（存在时），fallback origin/main（main 是唯一真相）。
【需专项定性】0 处 / 0 文件（非 POSIX shell，语义不同 → 不计违规）
────────────────────────────────────────────────────────────
标签口径: 已清=命中但零违规的文件数 ｜ 违规=代码行（处 / 文件）｜ 残余=非注释命中文件数
mac 域：2 文件已清 / 违规 8 处 / 6 文件
全 mac 域残余：6 文件 / 8 处违规（本跑已限定 --domain mac）
win 域：未扫描（--domain mac 限定）
退出码: 1（有违规，须修）
[exit code: 1]
```

### 2.5 验收3c: 全 win 域残余（供 Win 侧卡消费，裁定订正为 10 文件）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && bash scripts/control-tower/scan-fullwidth-vars.sh --domain win
═══ scan-fullwidth-vars（D938）— $VAR 紧贴全角标点 扫描 ═══
口径: 命令=bash scripts/control-tower/scan-fullwidth-vars.sh --domain win
      模式=\$[A-Za-z_][A-Za-z0-9_]*(（|）|：|，|。|；|、)
      排除=无（非 POSIX shell 文件归「需专项定性」，不计违规）
      截至=2026-09-24T15:03:35+08:00 ｜ locale=C.UTF-8
扫描集: 67 文件（默认 scripts/**）｜ 域过滤=win
────────────────────────────────────────────────────────────
【违规（代码行）】18 处 / 9 文件
  scripts/check-fde-terms.sh:35:  echo "[check-fde-terms] grep 执行异常（exit=$GREP_EXIT）— 检查失败按阻断处理" >&2
  scripts/ci/verify-d703.sh:49:    pass "DS2 CI 接线: grep -c verify-doc ci.yml = $N（≥2）"
  scripts/ci/verify-d703.sh:62:    fail "DS3 失败模式: 坏 doc 回放 exit $RC（须 1）"
  scripts/ci/verify-d703.sh:129:    fail "D702 DS1: void 写方法命中 $D702_DS1（须 0）"
  scripts/ci/verify-d703.sh:169:    fail "D702 DS6: 类型逃逸命中 $D702_DS6（须 0）"
  scripts/ci/verify-d703.sh:198:echo "verify-d703 汇总: PASS=$N_PASS FAIL=$N_FAIL SKIP=$N_SKIP（skip 均带理由，不算失败）"
  scripts/ci/verify-doc.sh:56:echo "verify-doc: 回放 $DOC_ARG（共 $(printf '%s\n' "$CMDS" | wc -l | tr -d ' ') 条命令）"
  scripts/ci/verify-doc.sh:157:      echo "  [FAIL] 引用不存在的测试文件/路径: $BAD_PATH（W2 事故形态，快速失败不进入 vitest）"
  scripts/ci/verify-doc.sh:169:      echo "  [OK] exit $RC（grep 零命中属合法结果）"
  scripts/ci/verify-doc.sh:172:      echo "  [FAIL] exit $RC（grep 引用错误，如文件不存在）— $(printf '%s' "$OUT" | head -2 | tr '\n' ' ')"
  scripts/deploy/batch-upgrade.sh:41:$VALID_MODES || fail "未知模式: $MODE（可用: --canary, --staged, --full, --rollback）"
  scripts/desktop/first-diagnosis-timing.sh:46:  echo "[$SCRIPT_NAME] --mode 必须 dev|prod（当前: $MODE）"; exit 2
  scripts/desktop/first-diagnosis-timing.sh:141:  FAILURES="${FAILURES:+$FAILURES; }first_diagnosis_ready: consult 入口 HTTP $READY_CODE（预期 400/401）"
  scripts/desktop/mac-install-verify.sh:46:    *) echo "ac-verify] 未知参数: $arg（支持 --dry-run/--skip-build/--keep-data）"; exit 2 ;;
  scripts/desktop/upgrade-data-verify.sh:94:log "evidence → $EVIDENCE_DIR；临时 userData → $USER_DATA（真实 ~/Library/Application Support/synova-agent 零触碰）"
  scripts/desktop/upgrade-data-verify.sh:104:log "② v1 已安装（临时位 $INSTALL_APP）"
  scripts/desktop/verify-package-signature.sh:106:    echo "  ❌ dmg 内未找到 .app: $dmg（挂载点: $mnt）"
  scripts/setup/verify-hooks-installed.sh:54:    echo "  ❌ core.hooksPath 是 Windows 绝对路径（$CHP）"
【注释行（无害）】1 处 / 1 文件
  scripts/desktop/mac-install-verify.sh:129:# 实际挂载点从 mount.log 解析（electron-builder APFS dmg 卷名含版本+arch，非固定 $APP_NAME）
【需专项定性】3 处 / 2 文件（非 POSIX shell，语义不同 → 不计违规）
  scripts/desktop/win-install-verify.ps1:92:  Write-Host "[win-install-verify] 断言失败: $failStep（evidence 已落 $EVIDENCE_DIR）" # P1-1: EAP=Stop 下错误型输出会跳过清理与显式 exit，故用 Write-Host
  scripts/desktop/win-install-verify.ps1:99:Write-Step "④ 四断言全过（进程+窗口+healthz+日志）；⑤ evidence: $EVIDENCE_DIR；⑥ 清理完成"
  scripts/setup.ps1:165:      # 创建启动脚本 (使用安装目录 $installDir，非源码临时目录)
────────────────────────────────────────────────────────────
标签口径: 已清=命中但零违规的文件数 ｜ 违规=代码行（处 / 文件）｜ 残余=非注释命中文件数
mac 域：未扫描（--domain win 限定）
win 域：10 文件待 <Win 侧新卡>（违规 18 处 / 9 文件）
全 win 域残余：10 文件 / 18 处违规（另有 2 文件落「需专项定性」桶：非 POSIX shell，不计违规）→ 待 Win 侧卡
退出码: 1（有违规，须修）
[exit code: 1]
```

### 2.6 验收4: ci.yml 密封清单接线
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && grep -n "scan-fullwidth-vars" .github/workflows/ci.yml; echo "--- ci.yml 增量 ---"; git diff --stat .github/workflows/ci.yml
256:            tests/control-tower/scan-fullwidth-vars.test.sh \
--- ci.yml 增量 ---
[exit code: 0]
```

### 2.7 验收5: bash -n 语法检查（新脚本 + 两个测试）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && for f in scripts/control-tower/scan-fullwidth-vars.sh tests/control-tower/scan-fullwidth-vars.test.sh tests/control-tower/alloc-task-id.test.sh; do bash -n "$f" && echo "OK  $f"; done
OK  scripts/control-tower/scan-fullwidth-vars.sh
OK  tests/control-tower/scan-fullwidth-vars.test.sh
OK  tests/control-tower/alloc-task-id.test.sh
[exit code: 0]
```
## 3. 判别性证据（夹具不是空转网）

### 3.1 alloc 夹具对改前基线（origin/main）必须红
```
$ W=/tmp/d938-mut; echo "基线 sha1=$(shasum "$W/scripts/control-tower/alloc-task-id.sh" | cut -c1-12)（origin/main 版本）"; bash "$W/tests/control-tower/alloc-task-id.test.sh" 2>&1 | sed -n "/── 9\./,\$p"; echo "MUT-RC=${PIPESTATUS[0]}"
基线 sha1=2690ddfd4173（origin/main 版本）
── 9. D938 判别性夹具: 中文/斜杠 title + A′ 反吞 + 合法提前退出 ──
  ✅ 9.1 中文 title rc (exit=0)
tr: Illegal byte sequence
  ❌ 9.1 中文 title → stderr 非空（改前即此形态: unbound variable）: /tmp/d938-mut/scripts/control-tower/alloc-task-id.sh: line 242: BRIEF_FILE
  ✅ 9.1 分配 D500
  ✅ 9.1 骨架 1 个
  ❌ 9.2 含 / title rc — 期望 exit=0 实际=1
  ❌ 9.2 含 / title → stderr 非空: /tmp/d938-mut/scripts/control-tower/alloc-task-id.sh: line 204: /var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.quPpIqsbG5/slash/briefs/2026-09-24-D500-M1/ownership-域修正.md: No such file or directory|
  ❌ 9.2 骨架应 1 个，实得 0（改前实测 0 个）
  ❌ 9.2 孤儿号: 登记 1 个但骨架 0 个（号已烧、无人认领）
  ✅ 9.2 骨架未裂成子目录（title 分隔符已消毒）
  ✅ 9.3 混合 title rc (exit=0)
tr: Illegal byte sequence
  ❌ 9.3 混合 title → stderr 非空: /tmp/d938-mut/scripts/control-tower/alloc-task-id.sh: line 242: BRIEF_FILE
  ✅ 9.3 骨架 1 个
  ❌ 9.4 _lock_release 提取失败或未含显式 exit（提取器与源码漂移 —— fail-loud，不静默跳过）
  ❌ 9.4 ① 生产 handler: unbound 中止 → rc=0（EXIT trap 仍把失败洗成成功 = fail-open）
  ✅ 9.4 ② 对照组(改前形态): 同场景 rc=0 → 夹具能看见 fail-open（非空转网）
  ✅ 9.5 可达中止 → rc=1 ≠ 0（不留 rc=0 的假成功）
  ❌ 9.5 回滚未生效: 登记 1 个 / 骨架 0 个（应为 0/0）
  ✅ 9.6 dry-run 合法提前退出 rc (exit=0)
  ✅ 9.6 dry-run 标注
  ✅ 9.6 dry-run stderr 空
  ✅ 9.7 D718 守卫合法提前退出 rc（防哨兵误伤） (exit=0)
  ✅ 9.7 守卫仍按降级路径告警

═══════════════════════════════════════════════════════════
  结果: PASS=28 FAIL=9
═══════════════════════════════════════════════════════════
MUT-RC=1
[exit code: 0]
```

### 3.2 扫描器金丝雀三态（.sh 抓 / .ps1 不判违规 / 注释不判违规）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938; S=scripts/control-tower/scan-fullwidth-vars.sh; FW=（; T=$(mktemp -d); printf "#!/bin/bash\necho \"\$D938_CANARY_VAR%sx)\"\n" "$FW" > "$T/a.sh"; printf "Write-Host \"\$failStep%sx)\"\n" "$FW" > "$T/b.ps1"; printf "#!/bin/bash\n# echo \"\$D938_CANARY_VAR%sx)\"\n" "$FW" > "$T/c.sh"; echo "--- 三类文件同目录一起扫 ---"; bash $S --paths "$T" | sed -n "/【违规/,/退出码/p"; echo "RC=${PIPESTATUS[0]}"; rm -rf "$T"
--- 三类文件同目录一起扫 ---
⚠️  scan-fullwidth-vars: 域统计不可用（命中文件全在仓库外（无域归属））—— 标签降级，违规判定不受影响（不静默）
【违规（代码行）】1 处 / 1 文件
  /var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.99dLtgjC10/a.sh:2:echo "$D938_CANARY_VAR（x)"
【注释行（无害）】1 处 / 1 文件
  /var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.99dLtgjC10/c.sh:2:# echo "$D938_CANARY_VAR（x)"
【需专项定性】1 处 / 1 文件（非 POSIX shell，语义不同 → 不计违规）
  /var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.99dLtgjC10/b.ps1:1:Write-Host "$failStep（x)"
────────────────────────────────────────────────────────────
标签口径: 已清=命中但零违规的文件数 ｜ 违规=代码行（处 / 文件）｜ 残余=非注释命中文件数
本卡写集内残留：1 处 / 1 文件
mac 域：域统计不可用（命中文件全在仓库外（无域归属））
win 域：域统计不可用（命中文件全在仓库外（无域归属））
退出码: 1（有违规，须修）
RC=1
[exit code: 0]
```

### 3.3 grep -P 假绿陷阱复现（BSD grep）+ 扫描器不用 -P
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938; S=scripts/control-tower/scan-fullwidth-vars.sh; echo "--- 陷阱：-P 报错 rc=2，但 2>/dev/null 后管道读到 0 命中 ---"; grep -rnP "\$\\w+（" scripts/ 2>/dev/null | wc -l; echo "pipeline-rc=$? （0 命中 = 假绿来源）"; grep -rnP x scripts/ >/dev/null 2>&1; echo "裸 grep -P rc=$?"; echo "--- 扫描器代码行（去注释）零 -P ---"; grep -vE "^[[:space:]]*#" "$S" | grep -E "grep[^|]*[[:space:]]-[A-Za-z]*P[A-Za-z]*([[:space:]]|$)" | wc -l; echo "--- 扫描器自身零违规 ---"; bash $S --paths "$S" | grep -E "^(【|mac 域)"
--- 陷阱：-P 报错 rc=2，但 2>/dev/null 后管道读到 0 命中 ---
       0
pipeline-rc=0 （0 命中 = 假绿来源）
裸 grep -P rc=2
--- 扫描器代码行（去注释）零 -P ---
       0
--- 扫描器自身零违规 ---
【违规（代码行）】0 处 / 0 文件
【注释行（无害）】3 处 / 1 文件
【需专项定性】0 处 / 0 文件（非 POSIX shell，语义不同 → 不计违规）
mac 域：1 文件已清 / 违规 0 处 / 0 文件
[exit code: 0]
```
## 4. 参数化接口（Win 卡可直接复用）

### 4.1 --json（机器可读）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && bash scripts/control-tower/scan-fullwidth-vars.sh --json | python3 -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d[\"domains\"],ensure_ascii=False,indent=1)); print(json.dumps(d[\"counts\"],ensure_ascii=False))"
{
 "mac": {
  "clean_files": 2,
  "violation_lines": 8,
  "violation_files": 6,
  "residual_files": 6
 },
 "win": {
  "clean_files": 0,
  "violation_lines": 18,
  "violation_files": 9,
  "residual_files": 10,
  "triage_files": 2
 },
 "outside_repo_hits": 0
}
{"violations": 26, "violation_files": 15, "comments": 6, "comment_files": 4, "needs_triage": 3, "triage_files": 2}
[exit code: 0]
```

### 4.2 退出码三态 + fail-closed 负例
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938; S=scripts/control-tower/scan-fullwidth-vars.sh; bash $S --domain mac >/dev/null 2>&1; echo "全 mac 有违规 → rc=$? （期望 1）"; bash $S --domain win >/dev/null 2>&1; echo "全 win 有违规 → rc=$? （期望 1）"; bash $S --paths /tmp/no-such-938.sh >/dev/null 2>&1; echo "不存在路径 → rc=$? （期望 2）"; E=$(mktemp -d); bash $S --paths "$E" >/dev/null 2>&1; echo "空扫描集 → rc=$? （期望 2）"; rmdir "$E"; bash $S --domain k3 >/dev/null 2>&1; echo "非法 --domain → rc=$? （期望 2）"
全 mac 有违规 → rc=1 （期望 1）
全 win 有违规 → rc=1 （期望 1）
不存在路径 → rc=2 （期望 2）
空扫描集 → rc=2 （期望 2）
非法 --domain → rc=2 （期望 2）
[exit code: 0]
```
## 5. ④ `alloc-task-id-lock.test.sh` 处置：**不动** + 三条实测证据

```
结论: A′（EXIT trap 退出码语义）**不改变该测试任何断言的结论** → 本卡不动它。
证据 1（静态）: 该测试从不读取被测脚本退出码 —— 全文 grep `$?`/`RC=`/`EXIT=` 只有：
$ grep -nE '$\?|RC=|EXIT=|if ! bash|exit [0-9$]' tests/control-tower/alloc-task-id-lock.test.sh
79:exit $FAIL
  （唯一命中 = 测试自己的 exit $FAIL；对 GATE 的调用一律只看 stdout/文件计数）
[exit code: 0]
```

### 5.2 证据 B（强）：删掉全部 DONE=1 哨兵标记（所有成功路径强制 rc=1）→ 该测试仍全绿
```
$ echo "live 脚本 DONE=1 行数:"; grep -c "^ *DONE=1" /tmp/d938-lockmut/live-alloc.sh; echo "变异后剩余:"; grep -c "^ *DONE=1" /tmp/d938-lockmut/scripts/control-tower/alloc-task-id.sh || true; echo "--- 变异体跑 lock 测试 ---"; bash /tmp/d938-lockmut/tests/control-tower/alloc-task-id-lock.test.sh 2>&1 | tail -7; echo "B-RC=${PIPESTATUS[0]}"
live 脚本 DONE=1 行数:
3
变异后剩余:
0
--- 变异体跑 lock 测试 ---
  ✅ dry-run 不建壳（输出: D942 (dry-run, 未建壳)）
  ✅ 并发 20 进程 → 20 个唯一号（无撞号）
  ✅ 真实 .claude/task-briefs/ 零污染（0 → 0 份）
  ✅ 沙箱 brief 目录收到 20 份骨架（注入缝真实生效，非静默跳过）

  结果: 5 通过, 0 失败
  Status: ✅ alloc-task-id 并发锁测试通过
B-RC=0
[exit code: 0]
```

### 5.3 证据 C：origin/main 基线（A′ 改前，无 DONE 哨兵）→ 结论与改后一致（全绿）
```
$ echo "基线 sha1=$(shasum /tmp/d938-lockmut/scripts/control-tower/alloc-task-id.sh | cut -c1-12) 行数=$(wc -l < /tmp/d938-lockmut/scripts/control-tower/alloc-task-id.sh) DONE 出现次数=$(grep -c DONE /tmp/d938-lockmut/scripts/control-tower/alloc-task-id.sh || true)"; bash /tmp/d938-lockmut/tests/control-tower/alloc-task-id-lock.test.sh 2>&1 | tail -7; echo "C-RC=${PIPESTATUS[0]}"
基线 sha1=2690ddfd4173 行数=     244 DONE 出现次数=0
  ✅ dry-run 不建壳（输出: D942 (dry-run, 未建壳)）
  ✅ 并发 20 进程 → 20 个唯一号（无撞号）
  ✅ 真实 .claude/task-briefs/ 零污染（0 → 0 份）
  ✅ 沙箱 brief 目录收到 20 份骨架（注入缝真实生效，非静默跳过）

  结果: 5 通过, 0 失败
  Status: ✅ alloc-task-id 并发锁测试通过
C-RC=0
[exit code: 0]
```

证据 A（对照，见 §6 全量复跑）：交付树（c1 已修）下同一测试 = 5 通过 / 0 失败，注释里那条自我清理 trap 照常释放锁目录。

**同时登记的缺口（不是本次交付的问题，但必须可见）**: 该测试对退出码完全不敏感（证据 B 即证明）
→ 它**结构上无法**捕获 A′ 类回归。退出码语义由本卡 ③ 夹具覆盖（`alloc-task-id.test.sh` 第 9.4/9.5/9.6/9.7 项：
生产 handler 在 unbound 中止下 rc≠0 + 对照组证明看得见 fail-open + 可达中止 rc≠0 且回滚 + 两处合法提前退出 rc=0）。
## 6. 复跑回执（截至时刻）

### 6.1 三件复跑 + 树状态
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938; echo "=== git status --short ==="; git status --short; echo "=== 测试复跑 ==="; bash tests/control-tower/scan-fullwidth-vars.test.sh >/tmp/e1.log 2>&1; echo "scan test rc=$? → $(tail -3 /tmp/e1.log | head -1)"; bash tests/control-tower/alloc-task-id.test.sh >/tmp/e2.log 2>&1; echo "alloc test rc=$? → $(tail -3 /tmp/e2.log | head -1)"; echo "截至: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
=== git status --short ===
 M docs/synova/product-lines/evidence/D938-夹具原始输出.md
?? .claude/task-briefs/2026-09-24-D938-alloc-task-id-defects.md
=== 测试复跑 ===
scan test rc=0 → ═══════════════════════════════════════════════════════════
alloc test rc=0 → ═══════════════════════════════════════════════════════════
截至: 2026-09-24T07:03:54Z
[exit code: 0]
```
## 7. 未清项 / 已知边界（不得当成"全清"）

1. **全 mac 域残余 6 文件 / 8 处**（写集外）: 按 CTO 裁定 A1 归新 mac 卡，本卡不动 —— 
   逐行清单见 §2.4。文件: check-ci-stale-red.sh / pre-dispatch-check.sh / generate-chronicle-monthly.sh /
   install-hooks.sh / task-start.sh / verify-incremental.sh。
2. **全 win 域残余 10 文件 / 18 处**（+2 文件落 triage 桶）: 待 Win 侧卡 —— 逐行清单见 §2.5。
3. 扫描器是**文本级**判据: 单引号字符串内的字面量也会被计为违规（生成该形态文本的脚本）。
   scripts/** 实测无此类；other 域出现时按「需专项定性」人工复核（已在扫描器头部登记该边界）。
4. `alloc-task-id-lock.test.sh` 结构上无法覆盖退出码语义（§5 证据 B 证明）—— 该覆盖由本卡 ③ 夹具承担，
   若未来有人删掉 ③ 的 9.4/9.5/9.6/9.7 项，退出码回归将**无网**。
5. 本卡**未**改动: 写集外 6 个 mac 文件 + 10 个 win 文件（含真炸的 install-hooks.sh:54,77）——见 §8 零改动核验。
6. 域标签里的 `待新卡（号由 CTO 发，Mac 侧）` —— D# 号一律不经本工具起（唯一入口 = alloc-task-id.sh）。
[exit code: 0]
```
## 8. 零改动核验（写集外文件未被本卡触碰）

### 8.1 写集外 16 文件对照 origin/main 的 diff（须为空）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938; F="scripts/control-tower/check-ci-stale-red.sh scripts/control-tower/pre-dispatch-check.sh scripts/doc-system/generate-chronicle-monthly.sh scripts/install-hooks.sh scripts/workflow/task-start.sh scripts/workflow/verify-incremental.sh scripts/check-fde-terms.sh scripts/ci/verify-d703.sh scripts/ci/verify-doc.sh scripts/deploy/batch-upgrade.sh scripts/desktop/first-diagnosis-timing.sh scripts/desktop/mac-install-verify.sh scripts/desktop/upgrade-data-verify.sh scripts/desktop/verify-package-signature.sh scripts/desktop/win-install-verify.ps1 scripts/setup/verify-hooks-installed.sh scripts/setup.ps1"; echo "文件数: $(echo $F | wc -w | tr -d " ")"; git diff --stat origin/main -- $F; echo "[git diff 空 = 零改动] exit=$?"; echo "--- 本卡全部改动（git status）---"; git status --short
文件数: 17
[git diff 空 = 零改动] exit=0
--- 本卡全部改动（git status）---
 M docs/synova/product-lines/evidence/D938-夹具原始输出.md
?? .claude/task-briefs/2026-09-24-D938-alloc-task-id-defects.md
[exit code: 0]
```
## 9. 提交回执 + 门禁复检（终态）

### 9.1 push 回执（ls-remote）+ 本卡提交链
```
$ bash -c cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && echo "--- ls-remote（生成时 head）---"; git ls-remote --heads origin | grep fix/d938-alloc-task-id; echo "--- 本卡 D938 提交链 ---"; git log --oneline --grep="D938" | head -5; echo "--- 回执口径（防误读）---"; echo "本行 SHA = 生成时 head；本文件自身的提交会再前移 head（正常，非不一致）。"; echo "复核: [ \"$(git rev-parse HEAD)\" = \"$(git ls-remote --heads origin | grep fix/d938-alloc-task-id | cut -f1)\" ] && echo 本地head==远端head（已推送）"; echo "本卡提交链须含: d4e9c91f（扫描器+夹具+接线）与 aa303818（V5 归零+证据回执）及本文件所在提交"
--- ls-remote（生成时 head）---
490004bc984abb6b2bae332a6fba59a9e99d94a1	refs/heads/fix/d938-alloc-task-id
--- 本卡 D938 提交链 ---
aa303818 fix(D938): V5 平台提示归零（PYBIN 探测行标注）+ 证据补提交回执/门禁复检
d4e9c91f feat(D938): 全角紧贴变量扫描器（参数化+三桶）+ 判别性夹具 + ci.yml 接线
118ccd0e fix(D938): alloc-task-id 三缺陷（全角/trap吞码/title未消毒）+ mac 域 7 处全角紧贴变量清扫
--- 回执口径（防误读）---
本行 SHA = 生成时 head；本文件自身的提交会再前移 head（正常，非不一致）。
复核: [ "490004bc984abb6b2bae332a6fba59a9e99d94a1" = "490004bc984abb6b2bae332a6fba59a9e99d94a1" ] && echo 本地head==远端head（已推送）
本卡提交链须含: d4e9c91f（扫描器+夹具+接线）与 aa303818（V5 归零+证据回执）及本文件所在提交
[exit code: 0]
```

### 9.2 V5 平台敏感命令 + D2 登记门禁复检
```
$ bash /tmp/d938-v5check.sh
--- V5 平台敏感命令模式复检（新控制塔脚本）---
0 命中（已清：PYBIN 三级探测行按 PLATFORM-CHECKLIST #1 标注）
--- D2 登记门禁扫描源复检（未跟踪 .md + 新暂存 .md）---
未跟踪 .md/.yaml: 1 个
新暂存 .md/.yaml: 0 个
未跟踪明细（D2 门禁 EXCLUDE 规则: .claude/ memory/ ...）:
.claude/task-briefs/2026-09-24-D938-alloc-task-id-defects.md
→ 被 EXCLUDE 规则豁免的: 1 个（门禁不计）
→ 本卡证据 md 已跟踪 → 不再触发 D2 未登记告警
[exit code: 0]
```

### 9.3 pre-commit 13 组复检（提交后，本地软提示模式）
```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && SYNO_CI=0 bash scripts/pre-commit-check.sh 2>&1 | tail -22
── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)

── 组 13/13: 技能同步一致性 ──
  ✅ G13: 无技能文件变更(跳过)

── D782: 文档真相防线（D1 真相验证 + D2 登记门禁）──
  ✅ D1 文档真相: 全部硬检查通过 (15 ✅)
  ✅ D2 登记门禁: ── 汇总: 检查 0 个文档，0 个未登记 ──

── PR 预算门禁 (D734) ──
  ✅ D734 PR 预算: 文件数 / 单域 / 落后基线 均在预算内
  ✅ V5 平台敏感命令: 新控制塔脚本对照 PLATFORM-CHECKLIST.md (D520)

═══════════════════════════════════════════════════════════
  ✅ 全部 13 组通过
  ⚠️  1 项警告 (不阻断)
  ⚠ V5: 1 项软提示（详情见上）——CI 为权威，本地不阻断
═══════════════════════════════════════════════════════════

[exit code: 0]
```
