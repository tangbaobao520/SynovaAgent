# D938 收尾三件与小队回执

> 卡号 D938（WIP=1 第 2 位）｜ 派单：`docs/synova/coordination/派单-Mac小队-五卡-20260924.md` 卡 2
> 执行形态：Agent Teams 小队（队长 + 2 编码 + 1 独立自验）
> 分支 `fix/d938-alloc-task-id` ｜ 建树基线 `origin/main` = f25e61eb ｜ **交付冻结 tip = `1e32ccf8`**
> 本文件由**队长**（synova-squad-lead）撰写，属治理产物（`domain_neutral`），非产品代码
> 计数截至 `2026-09-24T09:26:51Z`

---

## 一、计划与裁定记录

- 第 0 步 plan 六项 → **停在 CTO 复核门口** → CTO 裁定：
  - **① 接受 9 文件写集**（不压回 6："多出的 3 件是'参数化留接口给 Win'这条硬要求直接产生的，不能反过来算你们超预算"）
  - **② `alloc-task-id-lock.test.sh` 保留在写集**，允许"实测后不动则划掉"（**不动须附证据**）
  - 卡面订正三条全部采纳；**A′（trap 吞码 fail-open）并入本卡**
- **P7 前提自纠（正向）→ CTO 裁定 A1**：队长首版扫描只测 `（`（23 处/14 文件、12 文件）为**低估**；
  c2 提出 + 队长复核后订正为 **6 全角标点族**（`（）：，。；、`）。CTO 裁定 **A1**：
  本卡**判据② 口径 = 「本卡写集内 mac 域残留 = 0」**（非"全 mac 域 0"——后者 15 文件 > 12 上限）；
  mac 域残余 6 文件 → CTO 另立第二张 mac 卡；win 域订正为 **10 文件** → Win 侧卡；扫描器须**真参数化**。
- **CTO 裁定 a（锁缝）**：`LOCK_DIR` 仓库级、不随注入缝变化 → 两 alloc 测试不可并发（假红源）→ **本卡一并修**（同文件、0 新增文件）。
- 队长侧另有 2 次自纠并如实登记：① `grep -rnP` 在 BSD grep 静默失败被 `2>/dev/null` 吞成"0 命中"（**同族第七次活复发**）；② D718 守卫复测时**未先建目录**导致误判 rc=1（实为既定 fail-closed，正确行为）——已自纠，未记在 c1 名下。

---

## 二、团队成员运行记录

**token 列如实说明**：本会话工具链**未暴露**成员 token 用量（`env | grep -iE '^DSH_|TOKEN|USAGE'` 无用量变量；`~/.dsh/sessions/**` 无 token/usage 文件）。按"数字禁手写"纪律**留空，不伪造**。

| 成员 | 角色 | 运行状态 | token | 共享任务 id | 任务状态 | 写集（逐文件） |
|---|---|---|---|---|---|---|
| `lead` | 队长（协调/判前提/验证据，**不下场写码**） | running | 未暴露 | — | — | `.claude/task-briefs/2026-09-24-D938-*.md`、本文件（治理） |
| `d938-c1` | 编码A·分配器与 mac 域清扫 | inactive（已交付） | 未暴露 | **task-5** | **completed**(rev 6) | `scripts/control-tower/alloc-task-id.sh`、`scripts/control-tower/check-sentinel-type-net.sh`、`scripts/doc-system/check-doc-truth.sh`、`scripts/doc-system/doc-truth-probe.sh`、`D938-改动清单.md` |
| `d938-c2` | 编码B·扫描器与夹具 | inactive（已交付） | 未暴露 | **task-6** | **completed**(rev 6) | `scripts/control-tower/scan-fullwidth-vars.sh`、`tests/control-tower/scan-fullwidth-vars.test.sh`、`tests/control-tower/alloc-task-id.test.sh`、`tests/control-tower/alloc-task-id-lock.test.sh`、`.github/workflows/ci.yml`、`D938-夹具原始输出.md` |
| `d938-v` | **独立自验**（只读 + `/tmp`） | inactive（已交付） | 未暴露 | **task-7** | **completed**(rev 4) | `D938-自验.md` |

编制 = 队长 + 2 编码 + 1 自验 = **4 = maxMembers** ｜ **自验不由编码兼任**（v 独立于 c1/c2）
写集**两两不重叠**（队长侧核验：12 条目，交集 **0**）
重型验证**串行 ≤1**（v 全程串行跑夹具；两个 alloc 测试原本不可并发，本卡已修缝）

---

## 三、每处改动：file:line + 改前 / 改后（9 域内文件）

### 3.1 `scripts/control-tower/alloc-task-id.sh`（+59/−?，本卡核心）
| 缺陷 | 位置 | 改前 | 改后 |
|---|---|---|---|
| **① 全角紧贴** | `:273`（基线 :242） | `$BRIEF_FILE（填写后开工）` | `${BRIEF_FILE}（填写后开工）` |
| **② trap 吞退出码（fail-open）** | `:50` + `:75-82` | `_lock_release(){ rmdir … \|\| true; }` + `trap _lock_release EXIT` → **报错却 rc=0** | `DONE=0` 成功哨兵；`_lock_release` 首句 `local rc=$?` → `rc==0 && DONE!=1 → rc=1` → 末句 `exit "$rc"`；`:175`/`:222`/`:275` 三处合法 `exit 0` 前置 `DONE=1` |
| **③ title 未消毒 → 孤儿号** | `:184-188`（基线 :201） | `BRIEF_FILE="…$(echo "$TITLE"\|tr " " "-").md"`，且**先登记后生成** | `SAFE_TITLE`（`/`、`\`、空格 → `-`）+ BRIEF_FILE **上移到登记之前** + 骨架失败 `rm STATE_FILE` 回滚（同成同败） |
| **④ 锁目录注入缝（CTO 裁定 a）** | `:52` | `LOCK_DIR="$ROOT/.alloc-task-id.lock"` | `LOCK_DIR="${SYNO_LOCK_DIR:-$ROOT/.alloc-task-id.lock}"`（默认表达式逐字节不变）；`:17-23` 契约块加 `@seam` 说明。**`LOCK_WAIT_SEC`/`_lock_acquire`/陈旧锁清理语义一行未动** |

### 3.2 全角族清扫（mac 域，6 字符口径）
`check-sentinel-type-net.sh:49,:53`｜`check-doc-truth.sh:111`｜`doc-truth-probe.sh:110,:136,:203,:207` → 全部 `$VAR`+全角标点 → `${VAR}`+全角标点（共 **7 处 / 3 文件**）
> c1 实测口径订正：`check-sentinel-type-net.sh:49/:53` 被脚本自身 `export LC_ALL=C` 屏蔽 → **潜在**（端到端复现不出损坏）；`check-doc-truth.sh:111` + `doc-truth-probe.sh:110/:136` 为**活跃**（改前 `degraded: D1 脚本缺失 ��D328 exit 2）`、值消失 + 坏字节）。7 处仍全改（潜在态一去掉 locale 防御即成活跃）。

### 3.3 `scripts/control-tower/scan-fullwidth-vars.sh`（新建 522 行，参数化扫描器）
CLI：`--paths` / `--paths-file` / `--domain mac|win` / `--json`；三桶分类（违规=代码行 / 注释行=无害 / `需专项定性`=非 POSIX shell 如 `.ps1`）；**退出码三态** 0/1/2；**零 `grep -P`**。→ **给 Win 卡的复用接口**

### 3.4 `tests/control-tower/*`（3 个）
| 文件 | 改动 |
|---|---|
| `scan-fullwidth-vars.test.sh`（新建 331 行） | 金丝雀防"永远绿"空转；**写集断言覆盖面 = 9 文件 + 条数自检（≠9 即红）**；`:213/:229` 自身 2 处同类缺陷已修 |
| `alloc-task-id.test.sh`（+208） | 12 → **44** 断言：中文/含斜杠 title、**A′ 反吞 rc≠0**、D718 守卫 rc=0、骨架失败回滚、**消费 `SYNO_LOCK_DIR` 缝 + 并发隔离快检** |
| `alloc-task-id-lock.test.sh`（+30） | 「不动」→「动」：**消费缝做并发隔离**（非退出码语义）；§5 三条"不动"证据保留 |

### 3.5 `.github/workflows/ci.yml` — **+1 行**（`:256` 密封清单）
### 3.6 治理产物（`domain_neutral`）：brief、3 份证据、`.claude/bypass.log`（hook 自动）

---

## 四、变异体夹具「改坏即红」原始输出

**由独立自验员 `d938-v` 在 `/tmp/d938-v-mut{,2}` 副本中自行改坏复现**（非编码方自述）——
原始输出见 `docs/synova/product-lines/evidence/D938-自验.md` §三/§九。

| 变异体 | 改坏 | 结果（v 独立复现） |
|---|---|---|
| **M1** | 任一 `${VAR}`+全角标点 → 还原 | ✓红（点名 file:line） |
| **M2** | `$BRIEF_FILE（` 写法还原 | ✓红（直探针 rc=1 + stderr 103B） |
| **M3** | trap 恢复原样（不 `exit`） | ✓红 42/2 |
| **M4** | 去掉 title 消毒 | ✓红 41/3 |
| **M5** | 扫描器去掉金丝雀 | ✓红 |
| **M5′** | 静默空转（`grep` 行换成 `: > hits.txt`） | ✓红（金丝雀一票打红） |
| **M6** | 去掉 `SYNO_LOCK_DIR` 缝 | ✓红**双侧**：缝路径特异红 + **并发双红**（alloc rc=1 / lock rc=2，2 条 ❌） |

**每条均已 `git checkout` 复原并复跑绿**（复原 diff=0）。**改前对照**：用**现夹具**打改前基线（`origin/main` f25e61eb 镜像）→ `rc=1 / PASS=33 FAIL=11`（逐条点名，含 `9.4① rc=0` 的 fail-open 铁证）。

---

## 五、三路径夹具原始输出（索引）

原始输出见 `docs/synova/product-lines/evidence/D938-夹具原始输出.md`（1024 行）与 `D938-自验.md`（700 行）。

| 路径 | 驱动 | 结果 |
|---|---|---|
| **正常** | 中文 title | stderr **空** + rc=0 + 骨架 1 + 无孤儿号 |
| **正常** | 含 `/` title | stderr **空** + rc=0 + 骨架 `…-D500-M1-ownership-域修正.md` + 子目录 0 + **无孤儿号**（改前：rc=1、骨架 0、`D500.json` 已登记 = **孤儿号**） |
| **降级** | 未注入 `SYNO_BRIEF_DIR`（D718 守卫，目录存在） | rc=**0**（**未被哨兵误伤**） |
| **降级** | task-state 目录不存在 | rc=1 + `❌ task-state/ 目录不存在 … (fail-closed)` = **正确**行为 |
| **边界/反吞** | **A′** 注入未定义变量 | **rc≠0**（改前 rc=0 = 报错却宣称成功） |
| **边界/真炸** | 骨架写入失败（`SYNO_BRIEF_DIR` 指向非法路径） | rc=1 + `mkdir: Not a directory` + **回滚 D500 登记**（剩余号仅 D499） |
| **并发** | 缝未落地（改前） | A 组 **FAIL=4**（等锁超时假红） |
| **并发** | 缝已落地 | 两份测试并发 **各全绿**；20 并发同缝锁 → 21 号全唯一 |

---

## 六、`git diff --stat` 与 ls-remote 回执

```
# 全量净变更（含治理产物）
$ git diff --stat origin/main...HEAD
 .claude/bypass.log                                 |    9 +
 .github/workflows/ci.yml                           |    1 +
 .../product-lines/evidence/D938-夹具原始输出.md    | 1024 +++++
 .../synova/product-lines/evidence/D938-改动清单.md |  375 +++
 scripts/control-tower/alloc-task-id.sh             |   59 +-
 scripts/control-tower/check-sentinel-type-net.sh   |    4 +-
 scripts/control-tower/scan-fullwidth-vars.sh       |  522 +++++
 scripts/doc-system/check-doc-truth.sh              |    2 +-
 scripts/doc-system/doc-truth-probe.sh              |    8 +-
 tests/control-tower/alloc-task-id-lock.test.sh     |   30 +-
 tests/control-tower/alloc-task-id.test.sh          |  208 ++-
 tests/control-tower/scan-fullwidth-vars.test.sh    |  331 +++
 12 files changed, 2551 insertions(+), 22 deletions(-)

# 域内（代码/CI）文件 = 9（预算口径）
$ bash scripts/control-tower/check-pr-budget.sh --files "<9 文件>"
 ✅ ① 变更文件数 9 ≤ 上限 12 ｜ ✅ ② 变更单域: PASS 9 个文件同域: mac ｜ ✅ PASS   exit=0

# ls-remote 回执（队长亲跑）
$ git ls-remote --heads origin fix/d938-alloc-task-id
 1e32ccf88e81c529d885ff48e21ec5ee93de7ec3	refs/heads/fix/d938-alloc-task-id
$ git rev-parse HEAD
 1e32ccf88e81c529d885ff48e21ec5ee93de7ec3
★ 远端 tip == 本地 HEAD（真已推送，非"只在本地"）
$ git status --short     →  (工作树干净)
```
**交付提交链**（9 笔，去 post-commit 钩子登记噪音）：`118ccd0e` 三缺陷 → `d4e9c91f` 扫描器+夹具 → `b2137083`-类证据修订 → `63579f3a` 锁缝 → `7f831d70` 测试消费缝 → `85a47f91` 写集断言 5→9 + 自身 2 处 → `85558b07` 证据重建
**分支同步**：`behind 0`（无 merge/rebase/force push）

---

## 六-b、队长侧最终验证（U5）与两组 CI-strict 失败的**归因**

```
# 本地软提示口径
$ bash scripts/pre-commit-check.sh            → exit=0 ｜ ✅ 全部 13 组通过（1 项软提示：D2）

# CI strict 口径
$ SYNO_CI=1 bash scripts/pre-commit-check.sh  → exit=1 ｜ ❌ 2 组未通过 — 提交已拒绝
  ❌ 时间戳顺序: brief 必须早于代码写入: 1 处
  ❌ D2 登记门禁: 有未登记文档: 1 处 — docs/synova/product-lines/evidence/D938-自验.md
```

**两条均与 D938 无关，且均不会在 CI 触发**（归因证据）：

| # | 失败项 | 归因（实测） | CI 是否会红 |
|---|---|---|---|
| 1 | 时间戳顺序 | 输入 = **机器级** `/tmp/.synova-before-brief`（非 session/worktree 级）。其内容指向 **D941**（卡 5）与 `/Users/wane/.claude/projects/-Users-wane-SynovaAgent/memory/dsh-electron-source-desktop.md`（**另一 session** 的产物）；**`D938` 出现 0 次**；本卡 9 文件**不含任何门禁脚本**（`git diff` 零命中 pre-commit/hook） | **否**（CI 无该文件） |
| 2 | D2 登记门禁 | D2 只查 **untracked + staged-new** 的 `.md/.yaml`；`D938-自验.md` 当前未跟踪。CI 是干净检出 → `CHECKED=0` → 自动 exit 0；本卡提交后该文件即 tracked → 不再命中 | **否** |

> 附记：本条 #1 暴露一个**机器级共享可变状态**缺陷 —— 任一 session 的"brief 晚于代码写入"会**阻断所有其他 session** 的 strict 门禁（与 D938 刚修的 D1075 锁同类：共享状态未按 session 隔离）。已登记 CT 项。

---

## 七、未清项清单（诚实登记，不写作"已完成"）

| # | 未清项 | 性质 | 归属 |
|---|---|---|---|
| **U5** | 全量门禁 + 预算：**队长已补跑**（见 §六-b）——本地 exit 0、预算 PASS；**CI strict 另有 2 组红，已归因为非本卡**（见上表） | 已取证 + 归因 | 本条闭环 |
| **U9** | A-5① 两测试并发**双绿**：**部分取证** —— alloc 侧与 lock 并发时 rc=0 / `PASS=44 FAIL=0`（两次）；**lock 侧并发完成未取到** | 未完全取证 | 保留（见下一行障碍） |
| **U10** | A-5② 改前（`origin/main` 无缝合照）假红：**未取证**（v **未采信** c2 报的 `33/4`，也未引用） | 未取证 | 保留 |
| **U11** | C-3 改前**运行时** 37/5：**未取证**（静态已证改前标签 **0 条缺失**：alloc 24 个调用点 / lock 16 个全部仍在） | 未取证 | 保留 |
| — | 上述 U9/U10/U11 共同障碍：均需「改前对照跑」或「并发对跑」，单跑即 **409s** 量级，而后台作业**在本会话不可跨回合存活**（v 两次后台重链被中断回收） | 环境限制 | 保留 |
| **F3** | **适用域限制（开放，前置存量）**：`alloc-task-id.sh` 生产形态单次分配 **3m13.9s–3m46.7s**（`git worktree list` 扫描；**测于 218，prune 后 196**）≫ `LOCK_WAIT_SEC=30` → 真实仓并发第二调用**必超时**；夹具全绿走的是"沙箱非 git → 扫描段跳过"路径。**本卡结论只到「两个测试可并发」；不主张、也不得被引用为「alloc 生产并发已解决」** | 前置存量 | CT 队列（建议立卡） |
| **F4** | 锁目录不可建时注释称 fail-open、实测 fail-closed（注释漂移；`_lock_acquire` 与 main 逐字节同 = 存量） | 低危存量 | CT 队列 |
| **F5** | `.claude/bypass.log` 9 行 hook 噪音进 diff（D860 治理产物，不计预算） | 卫生 | CT 队列 |
| **CT-1** | `tests/**` 存量：**9 文件 / 20 处**全角紧贴变量违规（10 mac + 1 win 中，本卡修了自己那 2 处）→ 扫描器默认半径 `scripts/**` **不含 `tests/**`**，故"全 mac 域残余 6 文件/8 处"是 **scripts/ 半径内**口径 | 前置存量 + 半径口径 | CT 队列 |
| **CT-2** | 全 mac 域残余 **6 文件 / 8 处**（scripts/ 半径内，写集外）→ **CTO 已裁定另立第二张 mac 卡**（因 12 文件上限而拆） | 卡形 | CTO |
| **CT-3** | 全 win 域残余 **10 文件 / 18 处**（scripts/ 半径内）→ **Win 侧（Codex）立卡**；本卡已交付参数化扫描器接口 | 卡形 | Win 侧 |
| **CT-4** | **机器级共享状态**：`/tmp/.synova-before-brief` 非 session 隔离 → 任一 session 的 brief 时序问题会阻断其他所有 session 的 strict 门禁 | 机制缺陷 | CT 队列 |
| **CT-5** | 本卡**未新建 memory/notes 决策 Note**：写集不含 `memory/**`，commit 引用既有祖先 Note（D664/D718，均真实存在）。规范做法应由写集持有者另立 D938 Note | 治理归属 | CTO |
| **CT-6** | `_lock_acquire` 用 `mkdir`（非 `-p`）→ 缝路径**父目录不存在**时 40s 假红；本卡按 CTO"只加缝"未改 acquire 语义，已在契约块给出用法（统一 `$(mktemp -d)/lock`） | 低危 | CT 队列 |
| **CT-7** | c2 执行了 `git worktree prune`（清 22 条陈旧登记，**无数据删除**，218→196）—— **写集之外的共享状态变更**，已主动报备 + 登记环境变更；口径：此类变更今后**先报 Lead 再动** | 流程 | 队长观察 |
| **CT-8** | **派单件仍未落 main**（D937 遗留项①的延续；CTO 已以"按计划执行"放行 = 豁免） | 流程/豁免 | CTO |

---

## 八、声明

本文件与 `D938-自验.md` 均为**自验结论**，**不是审计结论**，**不含"通过"判定**。
**自验结论 = `可提请独立审计`**（锚定交付冻结 tip `1e32ccf8`）；**K3 复审通过前不得合并**。
是否通过归 **CTO 收件闸 + K3 终审**。
