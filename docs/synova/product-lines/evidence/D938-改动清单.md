# D938 改动清单（写者 c1：分配器三缺陷 + mac 域全角紧贴变量清扫）

> 任务: D938 / 小队 task-5（写者 d938-c1）｜日期: 2026-09-24
> 工作树: `/Users/wane/SynovaAgent/.synova-wt-squad-d938`｜分支: `fix/d938-alloc-task-id`｜基线: `origin/main` = `f25e61eb`
> 本件是 **编码方（c1）的改动清单与自跑验收原始输出**；**不是自验结论**（自验由独立成员 v 出，见 brief 写集）。
> 编号口径提示: task-5 的 ①②③ = 全角写法 / trap 吞码 / title 未消毒；CTO brief Q0 的 ①②③ = 全角写法 / title 未消毒 / trap 吞码。
> **两文档编号不一致，故本件一律按名称标注**，避免误读。

## 一、改动清单（逐文件，file:line）

| # | 文件 | 行（新） | 改前 | 改后 |
|---|---|---|---|---|
| 1 | `scripts/control-tower/alloc-task-id.sh` | `:273`（基线 `:242`） | `echo "brief 骨架已生成: $BRIEF_FILE（填写后开工）"` | `echo "brief 骨架已生成: ${BRIEF_FILE}（填写后开工）"` |
| 2 | 同上 | `:50` / `:75-82` | `_lock_release() { rmdir "$LOCK_DIR" 2>/dev/null \|\| true; }`（末句决定退出码 → 恒 0） | 新增 `DONE=0` 成功哨兵；`_lock_release` 首句 `local rc=$?`，`rc=0 && DONE!=1 → rc=1`，末句 `exit "$rc"` |
| 3 | 同上 | `:175` / `:222` / `:275` | 3 处 `exit 0` 无哨兵 | 三处**合法成功退出点**前各置 `DONE=1`（`:175` dry-run；`:222` D718 守卫提前退出；`:275` 末尾成功） |
| 4 | 同上 | `:184-188`（基线 `:201`） | `BRIEF_FILE="$BRIEF_DIR/$(date +%Y-%m-%d)-${NEW_ID}-$(echo "$TITLE" \| tr " " "-").md"`（**在登记之后**算） | 新增 `SAFE_TITLE="$(printf '%s' "$TITLE" \| tr ' /' '--' \| tr '\\' '-')"` + `BRIEF_FILE` 用 `${SAFE_TITLE}`；整段**上移到 `STATE_FILE` 登记之前**（纯计算，零副作用） |
| 5 | 同上 | `:228-235` | `mkdir -p "$BRIEF_DIR"` / `cat > "$BRIEF_FILE" <<SKEL`（失败 → 已烧号留孤儿） | 新增 `_brief_generation_failed()`：两处失败分支 `\|\| _brief_generation_failed …` → `rm -f "$STATE_FILE"` 回滚登记 + 非零退出（登记与骨架**同成同败**） |
| 6 | 同上 | `:14-16`（契约块） | 只有 `@input/@output/@degraded` | 增 `@exit` 行，显式声明「非 0 = 失败；成功哨兵语义；骨架失败回滚」 |
| 7 | `scripts/control-tower/check-sentinel-type-net.sh` | `:49`、`:53` | `$SENTINELS_DIR（` / `$TYPES_FILE（` | `${SENTINELS_DIR}（` / `${TYPES_FILE}（` |
| 8 | `scripts/doc-system/check-doc-truth.sh` | `:111` | `$LATEST_TAG（` | `${LATEST_TAG}（` |
| 9 | `scripts/doc-system/doc-truth-probe.sh` | `:110`、`:136` | `$D1（` | `${D1}（` |
| 10 | 同上 | `:203`、`:207`（lead 追加） | `$FIRST_RED）` / `$SUMMARY）` | `${FIRST_RED}）` / `${SUMMARY}）` |

改动统计（原始输出，逐字）：

```
$ git diff --stat -- scripts/control-tower/alloc-task-id.sh scripts/control-tower/check-sentinel-type-net.sh scripts/doc-system/check-doc-truth.sh scripts/doc-system/doc-truth-probe.sh
 scripts/control-tower/alloc-task-id.sh           | 48 ++++++++++++++++++++----
 scripts/control-tower/check-sentinel-type-net.sh |  4 +-
 scripts/doc-system/check-doc-truth.sh            |  2 +-
 scripts/doc-system/doc-truth-probe.sh            |  8 ++--
 4 files changed, 47 insertions(+), 15 deletions(-)
```

（注：上表为**本卡 4 文件口径**；全工作区 `git diff --stat` 另含 c2 写集 `.github/workflows/ci.yml`、`tests/control-tower/alloc-task-id.test.sh`，不属本件。）

## 二、开工前前提实测（前提冻结，含**一处订正**）

| 前提（卡面） | 实测命令 | 输出摘要 | 判定 |
|---|---|---|---|
| `:19 set -euo pipefail`｜`:74 trap _lock_release EXIT`｜`:160/:199/:244` 三个 `exit 0`｜`:201 BRIEF_FILE=`｜`:242` 全角 `（` | `grep -n 'set -euo pipefail\|trap _lock_release\|exit 0\|BRIEF_FILE=' scripts/control-tower/alloc-task-id.sh` | `19:set -euo pipefail` / `74:trap _lock_release EXIT` / `160:  exit 0` / `199:  exit 0` / `201:BRIEF_FILE=` / `244:exit 0` / `242:  echo "brief 骨架已生成: $BRIEF_FILE（填写后开工）"` | ✅ 成立 |
| mac 域 5 处行号 | `grep -rnE '\$[A-Za-z_][A-Za-z0-9_]*（' <4 文件>` | `check-sentinel-type-net.sh:49,53`｜`check-doc-truth.sh:111`｜`doc-truth-probe.sh:110,136`（+ 本卡 `alloc-task-id.sh:242`） | ✅ 行号精确成立 |
| 缺陷①（全角写法）报错形态 | 基线脚本 + 中文 title（`/tmp/d938-repro-base`） | `rc=0`，stderr `alloc-task-id.sh: line 242: BRIEF_FILE�: unbound variable` | ✅ 成立（且 rc=0，与缺陷②叠加） |
| 缺陷③（title 未消毒 → 孤儿号） | 基线脚本 + `"M1/ownership 域修正"` | `rc=1`，stderr `line 204: /tmp/…/briefs/2026-09-24-D500-M1/ownership-域修正.md: No such file or directory`；**号 1 个（D500.json）/ 骨架 0 个** | ✅ 成立（孤儿号实证） |
| 缺陷②（trap 吞退出码）根因 (a) | 基线脚本异常路径 | 报错却 `rc=0`（见上两行） | ✅ 成立 |
| 缺陷② 根因 (b)「trap 内 `$?` 读 0」 | 注入未定义变量夹具（下 §三-4） | 基线 `rc=0`；修复版 `rc=1` | ✅ 成立（可判别） |
| **订正 1**：卡面「mac 域 5 处全部为活跃缺陷」 | 真实脚本逐个触发（下 §三-6d） | `check-sentinel-type-net.sh:49/:53` **未复现损坏**（该脚本 `:37-38 export LC_ALL=C; export LANG=C` 屏蔽了多字节变量名扫描）；`check-doc-truth.sh:111`、`doc-truth-probe.sh:110/:136` **复现损坏**（`:110` 实测 `degraded: D1 脚本缺失 ��D328 exit 2）`，`$D1` 值消失 + 坏字节 `274 210`） | ⚠️ **2/5 为潜在（被 C locale 屏蔽），3/5 为活跃**。7 处仍全改（潜在态一旦去掉 defensive locale 行即转活跃；且 c2 的 6 字符扫描器要求本卡写集内=0） |
| **订正 2**：卡面扫描口径只有 `（` | lead 追加指令 + 本机复跑 | 6 字符族 `（）：，。；、` 在本卡 `doc-truth-probe.sh` 另有 `:203`、`:207` 两处 | ✅ 已并入（见 §一-10），扫描口径升级为 6 字符（§三-6b） |

## 三、验收原始输出（自跑，逐条）

### 1) `bash -n` 四个 .sh

```
  scripts/control-tower/alloc-task-id.sh → rc=0
  scripts/control-tower/check-sentinel-type-net.sh → rc=0
  scripts/doc-system/check-doc-truth.sh → rc=0
  scripts/doc-system/doc-truth-probe.sh → rc=0
```

### 2) 中文 title（卡片原命令：`SYNO_BRIEF_DIR=… SYNO_TASK_STATE_DIR=…`，未注入 NO_* 缝）

```
rc=0
stdout:
D942
已登记: /tmp/d938-clean/ts/D942.json (status=claimed)
brief 骨架已生成: /tmp/d938-clean/briefs/2026-09-24-D942-测试中文标题.md（填写后开工）
stderr 字节数=0 内容=[]
骨架数=1  号数=2（D499 夹具 + D942）
锁残留（跑后）: 无
```

**改前同夹具**：`rc=0` + stderr `line 242: BRIEF_FILE�: unbound variable`（stderr 非空）→ 改后 stderr 空、rc=0。✅

### 3) 含斜杠 title `"M1/ownership 域修正"` → 无孤儿号

```
rc=0
stdout:
D500
已登记: /tmp/d938-a2/f3/ts/D500.json (status=claimed)
brief 骨架已生成: /tmp/d938-a2/f3/briefs/2026-09-24-D500-M1-ownership-域修正.md（填写后开工）
stderr 字节数=0 内容=[]
号数=2（D499+D500） 骨架数=1
骨架: 2026-09-24-D500-M1-ownership-域修正.md
子目录数(应 0)=0
title 字段:   "title": "M1/ownership 域修正"      ← JSON 保留原文
骨架首行: # Task Brief: D500 M1/ownership 域修正  ← 骨架头保留原文
```

**改前同夹具**：`rc=1`、号 1 个 / 骨架 0 个 = 孤儿号。✅ 改后「号数-夹具号 == 骨架数」= 1/1，无孤儿。

### 4) A′ 反吞：注入未定义变量（**同一夹具，改前 vs 改后**，两处注入位）

夹具：`git show HEAD:alloc-task-id.sh`（改前）与工作树版（改后）各注入 `: "$D938_A_PRIME_UNDEFINED_PROBE"`；为避开共享锁竞争，脚本镜像到 `/tmp/d938-a3/mirror/`（ROOT 独立 → 私有锁目录）。

```
=== 注入自检（必须真的插进去了）===
/tmp/d938-a3/mirror/scripts/control-tower/base-A.sh:75::  "$D938_A_PRIME_UNDEFINED_PROBE"
/tmp/d938-a3/mirror/scripts/control-tower/base-B.sh:244:: "$D938_A_PRIME_UNDEFINED_PROBE"
/tmp/d938-a3/mirror/scripts/control-tower/fixed-A.sh:90:: "$D938_A_PRIME_UNDEFINED_PROBE"
/tmp/d938-a3/mirror/scripts/control-tower/fixed-B.sh:275:: "$D938_A_PRIME_UNDEFINED_PROBE"

位置A [base]  rc=0  stderr=[…base-A.sh: line 75: D938_A_PRIME_UNDEFINED_PROBE: unbound variable]  号数=1 骨架=0
位置A [fixed] rc=1  stderr=[…fixed-A.sh: line 90: D938_A_PRIME_UNDEFINED_PROBE: unbound variable] 号数=1 骨架=0
  锁残留检查（应无）: 无

位置B [base]  rc=0  stderr=[…base-B.sh: line 242: BRIEF_FILE�: unbound variable]                 号数=2 骨架=1
位置B [fixed] rc=1  stderr=[…fixed-B.sh: line 275: D938_A_PRIME_UNDEFINED_PROBE: unbound variable] 号数=2 骨架=1
  锁残留检查（应无）: 无
```

- 位置 A = `trap _lock_release EXIT` 之后立即崩（两端锚点逐字节相同）；位置 B = 「成功退出点之前」（改前锚=末个 `exit 0`；改后锚=末个 `DONE=1`）。
- **改前 rc=0（fail-open）→ 改后 rc=1（fail-closed）**，两处位置一致；锁均正常释放。✅
- ⚠️ 夹具注记：首个版本的注入点落在 `DONE=1` **之后**，两端都 rc=0（哨兵已置位=已判定成功），属夹具缺陷而非实现缺陷 —— 已改注入点到「成功点之前」重测，上表为重测结果。

### 4b) A′ 真实失败路径：骨架目录不可创建 → 非零 + 回滚登记

```
$ SYNO_BRIEF_DIR=<file>/sub … bash scripts/control-tower/alloc-task-id.sh "回滚夹具"
rc=1
stdout: D500
        已登记: /tmp/d938-a2/rollback/ts/D500.json (status=claimed)
stderr: mkdir: /tmp/d938-a2/rollback/blocked: Not a directory
        ❌ brief 骨架生成失败（目录创建失败: /tmp/d938-a2/rollback/blocked/sub）→ 已回滚 D500 登记（防空烧号，fail-closed）
剩余号: [D499.json ]        ← 仅夹具号，D500 登记已被回滚
```

### 5) 合法提前退出（D718 守卫 `:222`）不被误判 + 真实仓库零污染

```
$ SYNO_TASK_STATE_DIR=<sandbox> bash scripts/control-tower/alloc-task-id.sh "D718夹具"   # 不注入 SYNO_BRIEF_DIR
rc=0
stdout: D500
        已登记: /tmp/d938-a2/d718/ts/D500.json (status=claimed)
stderr: ⚠ alloc-task-id: task-state 已注入（/tmp/d938-a2/d718/ts）但未注入 SYNO_BRIEF_DIR
          → 跳过 brief 骨架生成，防污染真实仓库（测试请同时设 SYNO_BRIEF_DIR）
真实 brief 目录: 672 → 672 份（相等=零污染）
```

### 6) 残留扫描（禁 `grep -P`）

```
--- 6a) 卡片原口径 （ ---
$ grep -rnE '\$[A-Za-z_][A-Za-z0-9_]*（' <4 文件>
（无输出）  rc=1
--- 6b) lead 追加 6 字符口径 ---
$ grep -rnE '\$[A-Za-z_][A-Za-z0-9_]*(（|）|：|，|。|；|、)' <4 文件>
（无输出）  rc=1
--- 6c) 反例自检（证明扫描式有效，非假绿）---
$ grep -nE '\$[A-Za-z_][A-Za-z0-9_]*(（|）|：|，|。|；|、)' <夹具: printf 'v=1; echo "[$v）]"\nprint "$v，x"\n'>
1:v=1; echo "[$v）]"
2:print "$v，x"
```

6b 的 4 文件 = 本卡写集 4 脚本（`alloc-task-id.sh`、`check-sentinel-type-net.sh`、`check-doc-truth.sh`、`doc-truth-probe.sh`）；截至 2026-09-24 14:5x（本机）。✅ 0 命中，且扫描式经反例证明非恒过。

### 6e) 交叉核验：用 c2 的扫描器（工具口径独立于本人的 grep）

```
$ bash scripts/control-tower/scan-fullwidth-vars.sh --paths <本卡 4 文件>     # 扫描器 blob=9917cea5de982cc33f7f0a9111599abf4a4086a3
扫描集: 4 文件（--paths/--paths-file 指定）｜ 域过滤=无（全量）
【违规（代码行）】0 处 / 0 文件
【注释行（无害）】0 处 / 0 文件
【需专项定性】0 处 / 0 文件
本卡写集内残留：0 处 / 0 文件
退出码: 0（扫描集内零违规）        rc=0

$ bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac
扫描集: 208 文件（默认 scripts/**）｜ 域过滤=mac
【违规（代码行）】8 处 / 6 文件
  scripts/control-tower/check-ci-stale-red.sh:67
  scripts/control-tower/pre-dispatch-check.sh:52
  scripts/doc-system/generate-chronicle-monthly.sh:46
  scripts/install-hooks.sh:54, :77
  scripts/workflow/task-start.sh:81, :102
  scripts/workflow/verify-incremental.sh:66
全 mac 域残余：6 文件 / 8 处违规（本跑已限定 --domain mac）
退出码: 1（有违规，须修）          rc=1
```

- 全域 `--domain mac` 的 6 个残余文件 = CTO 裁定 A1 排除本卡的跨文件残余（lead 明确「不要动那 6 个文件」），**本卡 4 文件不在其中**。✅
- 逐文件单跑（4 次）均输出 `本卡写集内残留：0 处 / 0 文件`；本人独立 grep（6 字符口径，含注释）亦 `rc=1`（零命中）。✅ 两种工具口径一致。

### 6d) mac 域改后真实复测（生产脚本本体，非副本）

```
--- doc-truth W1 ---    ⚠️  W1 版本滞后 — 文档头部 V1.0.0 vs 最新 tag v9.9.9（版本轴可能不同，仅提示）
--- probe :110 ---      degraded: D1 脚本缺失 /tmp/d938-mac/probe/scripts/doc-system/check-doc-truth.sh（D328 exit 2）
--- probe :136 ---      degraded: D1 脚本缺失 /tmp/d938-mac/probe/scripts/doc-system/check-doc-truth.sh（D328 exit 2）
--- net :49 ---         degraded: 哨兵目录不存在: /tmp/d938-mac/extensions/sentinels（fail-closed，不判绿）
--- net :53 ---         degraded: 类型网文件不存在: /tmp/d938-mac/net53/src/sentinel/types.ts（fail-closed，不判绿）
```

改前同夹具（原件 `git show HEAD:`）：W1 行 = `… 最新 tag ��版本轴可能不同，仅提示）`（tag 值消失 + 坏字节）、probe = `degraded: D1 脚本缺失 ��D328 exit 2）`。✅ 7 处（含 lead 追加 2 处所在文件的同族值）改后均完整透出。

### 7) 既有/新增测试不回归

```
alloc-task-id.test.sh rc=0                结果: PASS=37 FAIL=0
alloc-task-id-lock.test.sh rc=0           结果: 5 通过, 0 失败   Status: ✅ alloc-task-id 并发锁测试通过
doc-truth-probe.test.sh rc=0              （末 6 行全 ✅：STALE/FRESH-GREEN/D1缺失 degraded 等）
check-sentinel-type-net.test.sh rc=0
check-doc-truth.test.sh rc=0              ── 汇总: 5 通过 / 0 失败 ──
```

实测时 `tests/control-tower/alloc-task-id.test.sh` 修订锚（c2 扩写版）：`blob=fca9464616421ee99947fa537ae8e02a1a00ba68`、`276 行`、mtime `2026-09-24 14:45:29`。**若 c2 之后继续改该测试，本行结论需重跑**（c2 写集，非本件写者）。
`alloc-task-id-lock.test.sh` 未改动：`blob=34dc75b5a64c60c5ac65b3ca1a4f73d88a3ec81a`（与基线同 = 零改动），通过。

## 四、M2 写集互斥 + 冲突扫描（完整输出见下）

- 本卡写者 c1 的写集 = 4 个脚本 + 本证据件；**与 c2（`tests/**`、`.github/workflows/ci.yml`、新建 `scan-fullwidth-vars*`）零文件重叠**；win 域 8 文件、`scripts/audit/**` 未触碰（`git status` 已核）。
- 冲突扫描（`scripts/ tests/ .github/ src/ electron/ packages/`）：

```
alloc-task-id 相关: 共 32 处 —— 生产接线点: tests/control-tower/alloc-task-id.test.sh:18(TOOL)、
  alloc-task-id-lock.test.sh:18(GATE)、scripts/control-tower/pre-dispatch-check.sh:30（仅提示文案）、
  tests/control-tower/ct-test-gate.test.sh:47（以本脚本为配对基线）、
  tests/control-tower/check-ownership.test.sh:62（mac 归属断言）、
  tests/control-tower/scan-fullwidth-vars.test.sh:203（c2 新扫描器的 D938 写集清单，含本卡 4 文件）
mac 域 3 脚本相关: 共 36 处 —— 生产接线点: scripts/pre-commit-check.sh:1417(调 check-doc-truth.sh)、
  tests/doc-system/{check-doc-truth,doc-truth-probe,doc-registry-gate}.test.sh、
  tests/control-tower/check-sentinel-type-net.test.sh、tests/sentinel/d752-type-net-gate.integration.test.ts、
  .github/workflows/ci.yml:260（c2 写集，仅密封清单）
```

- 结论：本卡 4 个脚本**无第二写者**（brief 写集表标注 c1）；改动未新增/删除任何被调用符号（`_lock_release`、`_lock_acquire` 签名不变），调用方无需同步改。✅

## 五、遗留项 / 未清项（如实登记）

1. **无独立自验**：本件是编码方产出。`D938-自验.md`（独立成员 v）与 `D938-夹具原始输出.md`（c2）另出，`K3 复审` 未做 → 本件**不得**当「审计通过」。
2. **删除/回滚路径的锁语义**：`_brief_generation_failed` 走 `exit 1` → EXIT trap 正常释放锁（§三-4b 后锁残留=无），但**未覆盖 SIGKILL 场景**（进程被强杀时 `rmdir` 不会执行 → 陈旧锁；既有 60s 陈旧锁强制清理在 `_lock_acquire` 已兜底，非本卡范围）。
3. **`LOCK_DIR` 无注入缝**：`LOCK_DIR="$ROOT/.alloc-task-id.lock"` 固定取脚本仓库根 → 同一工作树内并发跑夹具/测试会互相等锁（本次 A′ 首测即因此撞上「陈旧锁 61s 强制清理」告警）。夹具已改为镜像 ROOT 规避；是否给锁加 `SYNO_LOCK_DIR` 注入缝属控制塔卫生，**未在本卡范围**，建议另立卡。
4. **`memory/notes/` 决策 Note 未由本件新建**：本件写集不含 `memory/notes/**`。commit message 引用既有 Note `memory/notes/proposed/2026-09-23-devdoc-to-skill-and-w1-program.md`（该 Note :28 已登记 `alloc-task-id.sh` :242 缺陷，满足 commit-msg 门禁的「引用真实存在 Note」要求）；**规范做法（本卡决策沉淀）应由写集持有者另立 D938 Note**。
5. **mac 域跨文件残余 6 文件 + win 域 10 文件**：按 CTO 裁定 A1 **本卡不动**（lead 明确「不要动那 6 个文件」）→ 待新卡。
6. **订正 1（§二）需回灌卡面**：`check-sentinel-type-net.sh:49/:53` 在生产路径下被 `export LC_ALL=C` 屏蔽、当前不可复现损坏 —— 若「活跃缺陷」口径进入审计台账/统计，须按实测改为「潜在 2 + 活跃 3」。

## 六、可核回执锚

- 基线: `git rev-parse HEAD` → `f25e61eb105690359dbe13baf64695e5ff1748f0`（`git log --oneline -1` = `f25e61eb Merge pull request #735 from tangbaobao520/docs/d937-five-cards`）
- 分支: `fix/d938-alloc-task-id`；推送回执见 Lead 收尾件（`git ls-remote --heads origin | grep d938` 口径）。
