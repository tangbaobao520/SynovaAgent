# D938 独立自验报告（成员 d938-v / 仅本文件可写）

> 自验员：`d938-v`（**非** c1/c2 延续；本报告只引我本人跑出的输出）
> 交付工作树：`/Users/wane/SynovaAgent/.synova-wt-squad-d938`（分支 `fix/d938-alloc-task-id`）——**只读**
> 变异体试验区：`/tmp/d938-v-mut`（detached）、`/tmp/d938-v-mut2`（detached）——改坏源码只在此处
> 报告落盘时刻：2026-09-24T16:10+08:00（后续增量更新见文末「§九 追加」）
> **结论文本约定**：全篇只出现 `可提请独立审计` / `退回（附理由）` / `未清项`；**不下任何「通过」型结论**（该字面在此文件出现次数 = 0）。

---

## 〇 交付 tip 回执（双跑一致）

```
$ cd /Users/wane/SynovaAgent/.synova-wt-squad-d938 && git fetch origin --quiet
$ git ls-remote --heads origin | grep fix/d938-alloc-task-id
6deb1ab2aa2dfdb4b8e84394846c1ff21be58e94	refs/heads/fix/d938-alloc-task-id
$ git rev-parse HEAD
6deb1ab2aa2dfdb4b8e84394846c1ff21be58e94
```

→ 起始核验时的交付 tip = `6deb1ab2`（与 task-7 卡面一致）。
**⚠️ 自验期间 tip 两次前进：`6deb1ab2` → `8e51fce5` → 冻结于 `1e32ccf8`。本报告最终锚点 = `1e32ccf8`**
（§二–§五锚 `6deb1ab2`；§九 增量复验锚 `1e32ccf8`。两 tip 之间 **9 文件写集 blob 全等**（逐文件 `SAME` × 9），
故 §三/§五 结论按 blob 身份继承，§九 另做抽验）：

```
$ git fetch origin --quiet
$ git ls-remote --heads origin | grep fix/d938-alloc-task-id
1e32ccf88e81c529d885ff48e21ec5ee93de7ec3	refs/heads/fix/d938-alloc-task-id
$ git rev-parse HEAD
1e32ccf88e81c529d885ff48e21ec5ee93de7ec3
```

工作树 `git status --short` 起始只有未跟踪的
`.claude/task-briefs/2026-09-24-D938-alloc-task-id-defects.md`（队长治理产物，非我所建）。

写集 diff（`git diff origin/main --stat`）：

```
 .claude/bypass.log                                 |   7 +
 .github/workflows/ci.yml                           |   1 +
 docs/synova/product-lines/evidence/D938-夹具原始输出.md | 780 ++++++++++++
 docs/synova/product-lines/evidence/D938-改动清单.md     | 375 ++++++++++
 scripts/control-tower/alloc-task-id.sh             |  59 +-
 scripts/control-tower/check-sentinel-type-net.sh   |   4 +-
 scripts/control-tower/scan-fullwidth-vars.sh       | 511 ++++++++++++++
 scripts/doc-system/check-doc-truth.sh              |   2 +-
 scripts/doc-system/doc-truth-probe.sh              |   8 +-
 tests/control-tower/alloc-task-id-lock.test.sh     |  30 +-
 tests/control-tower/alloc-task-id.test.sh          | 208 +++++-
 tests/control-tower/scan-fullwidth-vars.test.sh    | 307 ++++++++++++++
 12 files changed, 2270 insertions(+), 22 deletions(-)
```

按 D860 口径（治理产物不计入）：**代码/CI 写集 = 9 文件、单域 mac**（`.claude/bypass.log` 为 hook 自动登记的治理日志，2 份 evidence md 为治理产物）。

---

## 一 前提冻结 —— 卡面两条事实性前提，我本人实测（不是复述）

### 前提① `$VAR` 紧贴全角标点 = 真损害

本机 `bash` 即 `/bin/bash`：

```
$ /bin/bash --version | head -1
GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25)
$ locale | head -1
LANG="C.UTF-8"

$ /bin/bash -c 'V=abc; echo "X=$V（Y）"; echo "rc=$?"'
X=��Y）
rc=0                      ← 变量值被吞 + 全角首字节被吃 = 静默损坏，rc=0

$ /bin/bash -c 'set -u; V=abc; echo "X=$V（Y）"'
/bin/bash: V�: unbound variable     (rc=127)   ← set -u 下直接中止
```

**前提成立**：无 `set -u` 时**静默损坏且 rc=0**（最危险形态），有 `set -u` 时 rc=127 中止。

### 前提② EXIT trap 吞退出码 = fail-open

```
$ /bin/bash -c 'set -euo pipefail; rel(){ rmdir "/nonexistent-938" 2>/dev/null || true; }; trap rel EXIT; echo "[[${UNDEF_938}]]"'
rc=0        ← 改前形态：报错却 rc=0（fail-open 确认）

$ /bin/bash -c 'set -euo pipefail; DONE=0; rel(){ local rc=$?; if [ "$rc" -eq 0 ] && [ "${DONE:-0}" != "1" ]; then rc=1; fi; rmdir "/nonexistent-938" 2>/dev/null || true; exit "$rc"; }; trap rel EXIT; echo "[[${UNDEF_938}]]"'
rc=1        ← 交付的哨兵修法：fail-closed

$ 同上但 DONE=1（成功路径）→ rc=0      ← 修法不误伤成功退出
```

**前提成立**，且修法方向正确（不误伤合法退出）。

---

## 二 A 独立复跑（A-1 … A-8）

### A-7 三个夹具串行全绿（先跑，建立基线）

| 夹具 | rc | 结果行 |
|---|---|---|
| `tests/control-tower/alloc-task-id.test.sh` | 0 | `结果: PASS=44 FAIL=0` |
| `tests/control-tower/alloc-task-id-lock.test.sh` | 0 | `结果: 7 通过, 0 失败` |
| `tests/control-tower/scan-fullwidth-vars.test.sh` | 0 | `结果: PASS=29 FAIL=0 SKIP=0` |

（串行耗时实测：1s / **405s** / 2s —— 405s 的成因见 §六 F3，非本轮新增）

### A-1 中文 title → stderr 空 + rc=0 + 骨架生成 ✅

```
=== A1 中文 title ===
rc=0
stderr 字节数=0
stderr=<<>>
stdout=<<D500|已登记: /tmp/d938-v-out/probeA/cn/ts/D500.json (status=claimed)|brief 骨架已生成: /tmp/d938-v-out/probeA/cn/br/2026-09-24-D500-中文标题回归探针.md（填写后开工）>>
brief 数=1
```

### A-2 含 `/` title → stderr 空 + rc=0 + 骨架生成 + 无孤儿号 ✅

```
=== A2 含 / title ===
rc=0
stderr 字节数=0
stderr=<<>>
登记新号=1 骨架=1 孤儿=无
骨架文件名=2026-09-24-D500-M1-ownership-域修正.md
子目录=0
```

（对照：改前形态由 M4 变异体复现，见 §三 M4；原始轨迹含 `line 244: …/D500-M1/ownership 域修正.md: No such file or directory`）

### A-3 A′ 反吞（本轮最值钱判据）✅

```
=== A3a 真实 handler + 合成 unbound ===
修后 handler rc=1 （期望 ≠0）
改前 handler rc=0 （期望 0 = fail-open 对照）
=== A3c 可达中止（真实脚本路径）===
rc=1
stderr=<<mkdir: …/blocker: Not a directory|❌ brief 骨架生成失败（目录创建失败: …/blocker/br）→ 已回滚 D500 登记（防空烧号，fail-closed）|>>
回滚后登记新号=0 （期望 0）
```

① 生产 handler 源码（`awk` 从交付脚本现取，非副本函数体）+ 合成 unbound → rc=1；
② 改前 handler（`origin/main` 现取）同场景 → rc=0（证明夹具**能看见** fail-open，非空转网）；
③ 真实脚本可达中止路径 → rc=1 且**登记回滚为 0**（无孤儿号）。

### A-4 合法提前退出 / fail-closed ✅（两条退出码路径）

```
=== A4a D718 守卫（task-state 已注入、brief 未注入、目录存在）===
rc=0
stderr=<<⚠ alloc-task-id: task-state 已注入（…/a4a/ts）但未注入 SYNO_BRIEF_DIR|  → 跳过 brief 骨架生成，防污染真实仓库（测试请同时设 SYNO_BRIEF_DIR）>>
stdout=<<D500|已登记: …/a4a/ts/D500.json (status=claimed)>>
锁目录运行后是否残留=已清理

=== A4b task-state 目录不存在 ===
rc=1
stderr=<<❌ task-state/ 目录不存在: …/nope-ts (fail-closed)>>
stdout=<<>>
```

→ D718 守卫 **rc=0，未被成功哨兵误伤** ✅；目录不存在 **rc=1 + 明示 fail-closed = 正确行为**（按卡面提示，**不判为缺陷**）✅。
（附注：我自己的首版 A-4 探针因把锁目录**预先建好**而误得 rc=1「获取分配锁超时」——是**探针设计错误**，已改正后复测；非产品缺陷。）

### A-5 锁缝独立复现

**③ 路径特异判据（主判据，非「陈旧锁告警」）✅**

```
=== A5c 锁缝路径特异（注入陈旧锁目录 → 是否被清理）===
rc=0
注入锁目录是否仍在=已清理-缝被消费
输出含陈旧锁告警=1
```

→ 注入的锁目录**运行后被清理** = 该路径确被脚本使用（路径特异，不依赖告警文字）。
按卡面要求**未**把「陈旧锁告警出现」当判据（该文字在仓库锁恰好陈旧时会假阳）。

**① 两测试并发全绿 / ② 改前无缝合照假红** → 见 §九 追加（重链执行中）。

### A-6 扫描器口径 ✅

```
$ bash scripts/control-tower/scan-fullwidth-vars.sh            # 默认全域（scripts/**）
mac 域：2 文件已清 / 违规 8 处 / 6 文件
win 域：10 文件待 <Win 侧新卡>（违规 18 处 / 9 文件）
全 mac 域残余：6 文件 / 8 处违规 → 待新卡（号由 CTO 发，Mac 侧）
全 win 域残余：10 文件 / 18 处违规（另有 2 文件落「需专项定性」桶：非 POSIX shell，不计违规）→ 待 Win 侧卡

$ bash scripts/control-tower/scan-fullwidth-vars.sh --domain win
win 域：10 文件待 <Win 侧新卡>（违规 18 处 / 9 文件）
```

→ 与卡面口径**逐项一致**：win = **10 文件**（不是 8）；mac 全域残余 **6 文件 / 8 处**；win 全域残余 **10 文件 / 18 处**。

写集模式（卡面 Done 要求的口径）——**注意此处出现分歧，见 §六 F1**：

```
$ bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac --paths \
    "scripts/control-tower/alloc-task-id.sh,scripts/control-tower/check-sentinel-type-net.sh,scripts/doc-system/check-doc-truth.sh,scripts/doc-system/doc-truth-probe.sh,scripts/control-tower/scan-fullwidth-vars.sh"
本卡写集内 mac 域残留：0 处 / 0 文件
退出码: 0（扫描集内零违规）
```

### A-8 反查接线 ✅

```
$ grep -n scan-fullwidth-vars .github/workflows/ci.yml
256:            tests/control-tower/scan-fullwidth-vars.test.sh \
```
`git diff origin/main -- .github/workflows/ci.yml` = **+1 行**（仅密封清单）。
**精确陈述**：ci.yml 挂的是**扫描器的测试**（进入 control-tower-tests 密封清单），**扫描器本体不是 CI 门禁**——它由测试驱动。卡面 A-8 要求「命中」→ 成立。

---

## 三 B 变异体独立复现（我改坏 → 夹具红 → 复原 → 复跑绿）

> **口径声明**：本节所有「红」= **变异体导致的预期红**（证明夹具能看见缺陷），**不是产品缺陷**。每条均附「复原后复跑绿」。

| 变异体 | 改坏内容 | 夹具 | 变异体红（原始） | 复原 | 复原后 |
|---|---|---|---|---|---|
| M1 | `alloc-task-id.sh:282` `${BRIEF_FILE}（` → `$BRIEF_FILE（`（精确 1 处） | 扫描器测试「写集判据②」 | rc=1（§2） | `git checkout --`，diff 行数=0 | rc=0 `PASS=29 FAIL=0` |
| M2 | 同上（精确 1 处） | alloc 测试「stderr 空 + rc=0」 | rc=1（§2 直探针：rc=1 + stderr 103B） | 同上 | rc=0 `PASS=44 FAIL=0` |
| M3 | `_lock_release` 恢复原形态（无 `local rc` / 无哨兵 / 无 `exit`） | alloc 测试 9.4① | `PASS=42 FAIL=2` | 同上 | rc=0 `PASS=44 FAIL=0` |
| M4 | 去掉 title 消毒（`SAFE_TITLE="$TITLE"`） | alloc 测试 9.2 | `PASS=41 FAIL=3` | 同上 | rc=0 `PASS=44 FAIL=0` |
| M5 | 扫描器 `PATTERN` 改为永不匹配 | 扫描器测试「金丝雀」 | `PASS=11 FAIL=16 SKIP=1` | 同上 | rc=0 `PASS=29 FAIL=0` |
| M5′ | **静默空转**：`grep … > hits.txt` 整行替换为 `: > hits.txt` | 扫描器测试「金丝雀」 | rc=1，金丝雀 3 条全红 | 同上 | rc=0 `PASS=29 FAIL=0` |
| M6 | 去掉 `SYNO_LOCK_DIR` 缝 | 并发全绿 + 缝路径特异 | **红**：缝路径特异半边红（§9.8）；并发双红 alloc rc=1 / lock rc=2（§9.8b） | `git checkout --`，diff=0 | 串行 44/0 与 7/0（§9.5） |

### M3 原始输出（精确变异）

```
83:_lock_release() {
84-  rmdir "$LOCK_DIR" 2>/dev/null || true
85-}
49:  ❌ 9.4 _lock_release 提取失败或未含显式 exit（提取器与源码漂移 —— fail-loud，不静默跳过）
50:  ❌ 9.4 ① 生产 handler: unbound 中止 → rc=0（EXIT trap 仍把失败洗成成功 = fail-open）
51:  ✅ 9.4 ② 对照组(改前形态): 同场景 rc=0 → 夹具能看见 fail-open（非空转网）
70:  结果: PASS=42 FAIL=2
复原后 rc=0   结果: PASS=44 FAIL=0
```

### M4 原始输出（复现了改前的**同一缺陷签名**）

```
❌ 9.2 含 / title rc — 期望 exit=0 实际=1
❌ 9.2 含 / title → stderr 非空: …/alloc-task-id.sh: line 244: …/D500-M1/ownership 域修正.md: No such file or directory|❌ brief 骨架生成失败（写入失败: …）→ 已回滚 D500 登记（防空烧号，fail-closed）|
❌ 9.2 骨架应 1 个，实得 0（改前实测 0 个）
✅ 9.2 无孤儿号（登记 0 个 ↔ 骨架 0 个）      ← 回滚生效
结果: PASS=41 FAIL=3
复原后 rc=0   结果: PASS=44 FAIL=0
```

### M5′ 原始输出（静默空转网必须被金丝雀一票打红）

```
── 2. 反空转金丝雀: 含 $VAR 紧贴全角的 .sh 必须被网抓到 ──
  ❌ 金丝雀未被抓到: rc=0（期望 1）—— 网可能在空转
  ❌ 输出未点名金丝雀文件
  ❌ 违规计数应为 '1 1'，实得 '0 0'
复原后 rc=0   结果: PASS=29 FAIL=0 SKIP=0
```

### M2 直探针原始输出（精确到卡面判据）

```
=== M2 直接探针（改坏副本，中文 title；副本在 /tmp，非交付树）===
M2 直接探针 匹配次数 = 1
rc=1 （期望 ≠0 = 红；改前实测 rc=0 且 stderr 非空）
stderr 字节数=103
stderr=<</tmp/d938-v-out/m2copy/scripts/control-tower/alloc-task-id.sh: line 282: BRIEF_FILE>>
stdout=<<D500|已登记: …/D500.json (status=claimed)>>
骨架数=1
```

→ 判据「stderr 空 + rc=0」两项**同时变红**（stderr 非空、rc=1）✅

### M1 原始输出要点（**同时暴露 §六 F2，务必连读**）

```
── 8. 判据②: 本卡写集内 mac 域残留 = 0（严格判红/绿）──
tests/control-tower/scan-fullwidth-vars.test.sh: line 213: WS�: unbound variable
```

→ 判据②**确实变红**（rc=1）✅；但**红的形态是夹具自身崩溃**（见 F2），不是断言输出。

---

## 四 C 计数复核（我自己跑的数）

### C-1 基线（`f25e61eb`）双口径

```
$ git grep -nE '\$[A-Za-z_][A-Za-z0-9_]*[（）：，。；、]' f25e61eb -- scripts/ | wc -l
40
$ git grep -lE '…同上…' f25e61eb -- scripts/ | wc -l
22
```
→ **40 处 / 22 文件**，与队长实测一致 ✅

### C-2 交付 tip 分域

| 口径 | 我实测 | 卡面 | 一致 |
|---|---|---|---|
| mac 域残余（非注释命中文件） | **6 文件 / 8 处** | 6 文件 / 8 处 | ✅ |
| win 域残余 | **10 文件 / 18 处违规**（9 个违规文件 + 1 个 `.ps1` triage 文件） | 10 文件 / 18 处 | ✅ |
| 扫描器自身写集（5 文件）残留 | **0 处 / 0 文件** | 0 | ✅ |
| **本卡声明写集（含测试）残留** | **2 处 / 1 文件** | 0 | ❌ 见 §六 F1 |
| ci.yml 增量 | **+1 行** | 仅密封清单 | ✅ |

（`--domain win --json` 的 `counts`：`violations:18 → violation_files:9`；`domains.win.residual_files:10`。
「残余 10」= 9 违规文件 + `scripts/desktop/win-install-verify.ps1`（triage 桶）；`scripts/setup.ps1` 虽有命中但命中行为注释行 → 不计残余。此口径差异我已核对，卡面 10/18 成立。）

### C-3 断言行数：改前一条未删

**静态标签集包含（我自写脚本，比对两条引号串标签）：**

```
==== alloc-task-id.test.sh ====
  改前断言调用点 = 24 ｜ 改后 = 79 ｜ 差 = +55
  改前标签在改后缺失 = 0 条
  → 改前 24 条断言一条未删（全部仍在）
==== alloc-task-id-lock.test.sh ====
  改前断言调用点 = 16 ｜ 改后 = 20 ｜ 差 = +4
  改前标签在改后缺失 = 0 条
  → 改前 16 条断言一条未删（全部仍在）
```
→ **「删旧换绿」不成立**：改前断言的**标签集是改后的子集，缺失 0 条** ✅
（说明：调用点计数 ≠ 运行时 PASS 数，因 `if/else` 两分支各计 1 个调用点。运行时口径 44 / 7 已实测；
改前运行时口径 37 / 5 见 §九 追加。）

---

## 五 D 毒点专查（四条）

### D-1 扫描器零 `grep -P` ✅

```
$ for f in scripts/control-tower/scan-fullwidth-vars.sh tests/control-tower/scan-fullwidth-vars.test.sh; do
    LC_ALL=C grep -nE -- '-P' "$f" | LC_ALL=C grep -vE '^[0-9]+:[[:space:]]*#' | wc -l; done
scripts/control-tower/scan-fullwidth-vars.sh 代码行 -P 命中 = 0
tests/control-tower/scan-fullwidth-vars.test.sh 代码行 -P 命中 = 7
```
→ **被测扫描器代码行 0 处 `-P`** ✅（唯一 `-P` 出现在第 50 行**注释**里，即「禁 grep -P」的说明文字）
→ 测试文件的 7 处均为 **echo/标签字符串** + **负例段刻意演示陷阱**（`:100-101` 真实执行 `grep -rnP` 以证明本平台 rc=2 → `2>/dev/null` 后读成 0 命中 = 假绿来源）。**属设计内负例，非违规。**
（本平台实测：`grep` 为 BSD grep，`-P` 不可用 → 陷阱真实存在，测试第 1 段已复现并 ✅）

### D-2 `.ps1` 不计违规 ✅

```
【需专项定性】3 处 / 2 文件（非 POSIX shell，语义不同 → 不计违规）
  scripts/desktop/win-install-verify.ps1:92: …$failStep（evidence 已落 $EVIDENCE_DIR）…
  scripts/desktop/win-install-verify.ps1:99: …
  scripts/setup.ps1:165: …
```
夹具侧：`scan-fullwidth-vars.test.sh` 第 3 段 `.ps1 金丝雀 → rc=0`、`.ps1 未出现在违规清单里` —— 串行跑 ✅（`PASS=29 FAIL=0`）。

### D-3 win 10 + mac 残余 6（共 16 文件）零改动 ✅

```
$ git diff origin/main --stat -- $(<16 文件清单>)
<<< 空 >>>
$ 逐文件 blob 对比
SAME=16 DIFF=0
```
（16 = win 10 + mac 6；清单落 `/tmp/d938-v-out/sixteen.txt`。逐文件 `git rev-parse origin/main:<f>` == `git rev-parse HEAD:<f>`。）

### D-4 `LOCK_WAIT_SEC` / `_lock_acquire` / 陈旧锁清理语义「一行未动」✅

```
=== _lock_acquire : main vs delivered diff ===
(无差异)      main 行数=21 delivered 行数=21

=== LOCK_WAIT_SEC / LOCK_POLL ===
--main--     41:LOCK_WAIT_SEC=30   42:LOCK_POLL=0.2
--delivered-- 53:LOCK_WAIT_SEC=30   54:LOCK_POLL=0.2

=== 陈旧锁清理（lock_age / 告警 / rmdir）===
--main--     48-52 与 --delivered-- 65-69 逐字相同（含 `${lock_age}s` 告警与 `rmdir … || true`）

=== _lock_release ===
1a2,7  > local rc=$? / 哨兵 if 块 / 注释
2a9    > exit "$rc"
```
→ CTO 只批「加缝」：**缝 + 哨兵**是仅有的语义改动；`_lock_acquire` 与陈旧锁清理**逐字节未动** ✅

### D-5 锁测试「由不动改动」= 动的是并发隔离，不是退出码语义 ✅

`git diff origin/main -- tests/control-tower/alloc-task-id-lock.test.sh` 全文仅含：
① `LOCK_ROOT="$(mktemp -d)"` + trap 清理；② 新增「缝消费」路径特异断言 2 条；③ `dry-run` 探针与 20 进程循环各加 `SYNO_LOCK_DIR=…`；④ 注释。
**零处**触碰退出码/`$?` 断言语义 ✅
§5 三条「不动」证据仍在：`D938-夹具原始输出.md:471 / :482 / :501`（标题 `§5 …处置：不动 + 三条实测证据`、`5.2 证据 B`、`5.3 证据 C`）✅

---

## 六 【未通过项 / 与 c1·c2 报告不符】—— 请 K3 优先看这里

### F1（**已修复并复核 @ `1e32ccf8`** — 由我 d938-v 首报）卡面判据「本卡写集内残留 0」在其**声明的写集**上不成立

> **状态更新（增量复验后）**：已由 c2 于 `85a47f91` 修复 —— 写集断言 5 → **9 文件全集**（= brief 的 task 文件集合）
> **+ 条数自检**（`D938_WS_COUNT` ≠ 9 即红，杜绝再次窄化），并把本测试文件自身纳入 §7 自清。
> 修复后我在冻结 tip 实测：9 文件全集 `本卡写集内 mac 域残留：0 处 / 0 文件`（§九 第 2 条），
> 且 c2 以「**5 文件口径假绿**」作为本发现的实证（§九 第 3 条我独立复做）。**F1 结论：已闭环。**

- 卡面 Done 原文：`scan-fullwidth-vars.sh --domain mac` → **本卡写集内残留 0**。
- task brief 自己的「写集」表（`.claude/task-briefs/2026-09-24-D938-alloc-task-id-defects.md:94`）**明确列入**
  `tests/control-tower/scan-fullwidth-vars.test.sh`（task / 写者 c2 / **新建**）。
- 但夹具 `scan-fullwidth-vars.test.sh:204` 把「本卡写集」**收窄为 5 文件**（c1 的 4 个 `scripts/` 文件 + 扫描器自身），**不含该测试文件自己**：

```
$ bash scripts/control-tower/scan-fullwidth-vars.sh --paths \
   "scripts/control-tower/alloc-task-id.sh,scripts/control-tower/scan-fullwidth-vars.sh,scripts/control-tower/check-sentinel-type-net.sh,scripts/doc-system/check-doc-truth.sh,scripts/doc-system/doc-truth-probe.sh,tests/control-tower/alloc-task-id.test.sh,tests/control-tower/alloc-task-id-lock.test.sh,tests/control-tower/scan-fullwidth-vars.test.sh,.github/workflows/ci.yml"
本卡写集内残留：2 处 / 1 文件
mac 域：1 文件已清 / 违规 2 处 / 1 文件
  tests/control-tower/scan-fullwidth-vars.test.sh:213:  no "本卡写集内 mac 域残留 = $WS（应为 '0 0'）—— 本卡写集内仍有违规"
  tests/control-tower/scan-fullwidth-vars.test.sh:229:    no "全 mac 域残余 $M_FILES 文件 / $M_LINES 处 > 上限 $MAC_RES_BASELINE / $MAC_VIOL_BASELINE（**回退/新增**，真红）"
```

- 即：**本卡自己新增的夹具文件里，存在 2 处本卡正在消灭的缺陷形态**（`$VAR` 紧贴全角标点、代码行、非注释）。
- 且扫描器**结构上看不到它**：`scan-fullwidth-vars.sh:15` 声明「默认扫描 `<repo>/scripts/**`」——
  `tests/**` 不在扫描半径内，所以「全 mac 域残余 6 文件/8 处」这个棘轮数字里**也不包含这 2 处**。
- 夹具头的自我说明写的是「本卡写集（5 文件: c1 的 4 个 mac 源文件 + 扫描器自身）残留 = **0**」——
  它**如实写了 5 文件**，但把标签命名为「本卡写集」，与 brief 的 9 文件写集表不一致 → **窄化口径把红读成绿**。

**我的判定**：这不是「产品逻辑错」，而是**验收口径与卡面不一致**，且残留在本卡新增文件内。
按卡面字面判据，本卡**未达标**；按夹具自定的 5 文件口径，达标。**该分歧归 CTO 裁定 / K3 终审**，我不代判。

### F2（**已修复并复核 @ `1e32ccf8`** — 2 字修复）F1 的两行让夹具在**红路径**上自毁，判别信息丢失

> **状态更新（增量复验后）**：`$WS（` / `$M_FILES（` / `$M_LINES（` 已改为 `${WS}（` / `${M_FILES}（` / `${M_LINES}（`，
> 且红分支新增**逐行清单**输出。修复后我在冻结 tip 复做 M1 类变异实测：红输出**点名
> `scripts/control-tower/alloc-task-id.sh:282`**，`unbound variable` 计数 = **0**（§九 第 4 条）。**F2 结论：已闭环。**

第 213 行 `$WS（` 与第 229 行 `$MAC_VIOL_BASELINE（` 位于 `set -uo pipefail`（`scan-fullwidth-vars.test.sh:36`）之下。
我在**交付文件原句**上直接复现：

```
$ /bin/bash -c 'set -uo pipefail; WS="2 1"; no(){ echo "❌ $1"; }; no "本卡写集内 mac 域残留 = $WS（应为 '"'"'0 0'"'"'）—— 本卡写集内仍有违规"'
/bin/bash: WS�: unbound variable                       rc=127

$ /bin/bash -c 'set -uo pipefail; M_FILES=7; M_LINES=9; MAC_RES_BASELINE=6; MAC_VIOL_BASELINE=8; no(){ echo "❌ $1"; }; no "全 mac 域残余 $M_FILES 文件 / $M_LINES 处 > 上限 $MAC_RES_BASELINE / $MAC_VIOL_BASELINE（**回退/新增**，真红）"'
/bin/bash: MAC_VIOL_BASELINE�: unbound variable        rc=127

$ 去掉 set -u 的同一句 → 静默损坏：  ❌ 本卡写集内 mac 域残留 = ��应为 '0 0'）—— 本卡写集内仍有违规     rc=0
```

**后果**：判据真变红时（M1 已实测），夹具**不会**打印「本卡写集内 mac 域残留 = N 处 / M 文件」——
而是整进程 `unbound variable` 中止（见 §三 M1 原始输出）。**rc 仍非 0（不会假绿）**，但**判别信息恰好在你最需要它的时候被销毁**，且这正是本卡要消灭的缺陷形态。
**建议**：`$WS（` → `${WS}（`、`$MAC_VIOL_BASELINE（` → `${MAC_VIOL_BASELINE}（`（2 字修复）。

### F3（**保持开放限制 — 前置存量，非本卡引入；已进未清项 U7**）生产形态单次分配 3m38s，临界区 ≫ `LOCK_WAIT_SEC`=30

```
=== (a) 真 task-state + NO_WORKTREE=1 + NO_BRANCH=1 ===   real 0m0.057s
=== (b) 真 task-state + NO_BRANCH=1（worktree 扫描开）===   real 3m20.513s
=== (c) 真 task-state（全开，= 生产形态）===                real 3m38.603s
```
- 根因：`scripts/control-tower/alloc-task-id.sh:125-152`（D576/CT-54）worktree 扫描；本仓注册 **218 个 worktree**。
  ⚠️ **环境事实变更（防复核者判「数字对不上」）**：上述 218 是**我的测量时刻**值；此后 c2 执行过一次
  `git worktree prune`（清 22 条「目录已不存在」的陈旧登记，未删数据、未影响活工作树，已报备 lead）。
  **我在冻结 tip 复核时**：`git worktree list --porcelain | grep -c '^worktree '` = **196**（`prunable` = 0）。
  故请读作「**测于 218，prune 后 196**」——**量级不变（分钟级 ≫ 30s），F3 结论不变**。
- 该代码块与 `origin/main` **逐字节相同（33 行，diff 无差异）** → **前置存量，非 D938 回归**。
- **但**：`_lock_acquire` 在扫描**之前**获取锁，故**生产形态下锁被持有 218s ≫ `LOCK_WAIT_SEC=30`** →
  真实仓里两个并发 alloc，后到者**必然**等锁超时（rc=1 fail-closed）。
- 夹具之所以 20/20 全绿，是因为它把 `SYNO_TASK_STATE_DIR` 指到**非 git 的 mktemp 沙箱** → worktree/分支扫描整段跳过 → 临界区 ~0.05s。
  **即：夹具验证的是「锁机制正确」，未覆盖「生产临界区时长」**。卡面 F3 之外的「并发全绿」结论**不适用于生产时长形态**。

### F4（**文档-行为漂移，前置存量**）锁目录无法创建时，注释说 fail-open，实测 fail-closed

`alloc-task-id.sh:47` 注释：`降级: 锁目录无法创建（权限/磁盘）→ 显式告警 + 继续（fail-open 不静默，铁律 11）`。
实测（锁目录的**父目录不存在**，我首版 A-4 探针踩到）：

```
rc=1
stderr=<<❌ 获取分配锁超时（30s）— 可能有并发 session 卡住|❌ 无法获取分配锁 — 分配号中止（fail-closed，防撞号）>>
```
→ 实际是「等 30s 后 fail-closed」，**不是**「显式告警 + 继续」。行为更安全，但**注释与行为不符**。
`_lock_acquire` 与 `origin/main` 逐字节相同 → **前置存量，非本卡引入**；建议随新卡修正注释。

### F5（**PR 卫生，低severity**）`.claude/bypass.log` 7 行进了 diff

```
 .claude/bypass.log | 7 +
+2026-09-24T14:53:49+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=118ccd0e…
…（共 7 行，全为 hook 自动登记的「pre-commit PASS」）
```
按 D860 口径属治理产物、不计入 12 文件预算，但会进 PR diff。**登记，不判红。**

---

## 七 未清项（Unresolved）

| # | 未清项 | 状态 |
|---|---|---|
| U1 | A-5① 交付 tip 两测试**并发双绿** | **部分取证**（alloc 侧并发 rc=0 / 44-0 ×2；lock 侧并发未取到）→ §9.7 **U9** |
| U2 | A-5② 改前（`origin/main` 无缝合照）同场景**假红** | **未取证** → §9.7 **U10**（不引用 c2 的 33/4） |
| U3 | M6 去掉 `SYNO_LOCK_DIR` 缝 →「并发全绿」与「缝路径特异」**必须红** | 缝路径特异半边**已取证**（A5③）；**并发半边未取证** → §9.7 **U8** |
| U4 | C-3 改前**运行时**断言数 37 / 5（静态已证 0 条删除） | **未取证**（静态部分已过）→ §9.7 **U11** |
| U5 | 卡面 Done 另两项 `bash scripts/pre-commit-check.sh` + `check-pr-budget.sh` | **未取证**（不在 task-7 A–D 清单内；重活，串行规则下未跑）→ 交接队长 |
| U6 | F1/F2 的修复 | **已闭环于 `1e32ccf8`**（见 §六 状态更新 + §9.2/9.3/9.4） |
| U7 | F3 生产临界区 ≫ `LOCK_WAIT_SEC`（前置存量） | **保持开放**，属新卡范畴 |

---

## 八 自验结论

**`可提请独立审计`（锚定交付冻结 tip `1e32ccf8`）**

原先 3 条保留，**2 条已在冻结 tip 闭环，1 条保持开放**：

1. **F1 —— 已修复并复核 @ `1e32ccf8`**（由我首报；c2 于 `85a47f91` 把写集断言扩到 9 文件全集 + 条数自检）。
   冻结 tip 复测：9 文件全集 → `本卡写集内 mac 域残留：0 处 / 0 文件`；覆盖面自检 = 9 文件。§九 第 2/3 条。
2. **F2 —— 已修复并复核 @ `1e32ccf8`**（`${VAR}（` 加括号 + 红分支逐行清单）。
   冻结 tip 复做 M1 类变异：红输出点名 `alloc-task-id.sh:282`，`unbound variable` = 0。§九 第 4 条。
3. **F3 —— 保持开放限制（未清项 U7）**：生产形态单次分配 3m38s（**前置存量，非本卡引入**，该代码块与 `origin/main`
   逐字节相同），临界区 ≫ `LOCK_WAIT_SEC` → 真实仓并发第二调用必超时。「并发全绿」只覆盖**测试隔离**，
   交付侧 §12 已显式登记该边界并声明**不主张**生产并发已解决（§九 第 6 条已复核原文）。

其余 A–D 各项：**均以我本人命令输出取证**（A1–A4、A5③、A6–A8、M1–M5′、**M6 双侧**、C1–C3、D1–D5，§九 抽验 2/3/4/5/6/8）。
**仍未取证（不写绿）**：U5（`pre-commit-check.sh` / `check-pr-budget.sh`）、U9（两测试并发双绿之 lock 侧）、
U10（改前无缝合照假红）、U11（改前运行时断言数 37/5）。**我不下通过型结论**——通过与否归 CTO 收件闸 + K3 终审。

---

## 九 增量复验 @ 冻结 tip `1e32ccf8`（队长第二轮指令 6 条，逐条我本人原始输出）

### 9.1 tip 双跑一致 ✅

```
$ git fetch origin --quiet
$ git ls-remote --heads origin | grep fix/d938-alloc-task-id
1e32ccf88e81c529d885ff48e21ec5ee93de7ec3	refs/heads/fix/d938-alloc-task-id
$ git rev-parse HEAD
1e32ccf88e81c529d885ff48e21ec5ee93de7ec3
```

`8e51fce5..1e32ccf8` 差异 = `.claude/bypass.log`（+1 行 hook 噪音）+ `D938-夹具原始输出.md`（仅证据）。
**9 文件写集逐文件 `git rev-parse <tip>:<f>` 对比 = `SAME` × 9**（含 `alloc-task-id.sh`、两个 alloc 测试、scanner、scanner 测试、ci.yml）
→ §三/§五 的变异体与毒点结论按 **blob 身份**继承到冻结 tip。

### 9.2 判据今日是否成立：**9 文件全集**跑扫描器 → 0 处 / 0 文件 ✅

```
$ bash scripts/control-tower/scan-fullwidth-vars.sh --domain mac --paths \
  "scripts/control-tower/alloc-task-id.sh,scripts/control-tower/check-sentinel-type-net.sh,scripts/doc-system/check-doc-truth.sh,scripts/doc-system/doc-truth-probe.sh,scripts/control-tower/scan-fullwidth-vars.sh,tests/control-tower/scan-fullwidth-vars.test.sh,tests/control-tower/alloc-task-id.test.sh,tests/control-tower/alloc-task-id-lock.test.sh,.github/workflows/ci.yml"
【违规（代码行）】0 处 / 0 文件
本卡写集内 mac 域残留：0 处 / 0 文件
退出码: 0（扫描集内零违规）
rc=0
```

覆盖面**确实 = 9 文件**（`tests/control-tower/scan-fullwidth-vars.test.sh:208-214`）：

```
208:D938_WS_COUNT=$(printf '%s\n' "$D938_WS" | tr ',' '\n' | grep -c . || true)
211:if [ "$D938_WS_COUNT" = "9" ]; then
212:  ok "写集断言覆盖面 = ${D938_WS_COUNT} 文件（brief 的 task 文件全集：4 scripts + 扫描器 + 3 测试 + ci.yml）"
214:  fail "写集断言只覆盖 ${D938_WS_COUNT} 文件（须 9）—— 窄化即判据空转（D938-v 实测过）"
```
（实测运行输出亦打印 `✅ 写集断言覆盖面 = 9 文件`，见 9.4。）

### 9.3 独立复做「假绿」实证（F1 的价值兑现）✅ —— 9 文件口径红 / 旧 5 文件口径**假绿**

变异：`tests/control-tower/alloc-task-id.test.sh:182` `${NB}（` → `$NB（`（精确 1 处；在 `/tmp/d938-v-mut2` 试验区）

```
--- 9 文件口径（新，应红）---
【违规（代码行）】1 处 / 1 文件
  tests/control-tower/alloc-task-id.test.sh:182:[ "$NB" = "1" ] && pass "9.2 骨架 1 个" || fail "9.2 骨架应 1 个，实得 $NB（改前实测 0 个）"
本卡写集内 mac 域残留：1 处 / 1 文件
退出码: 1（有违规，须修）          rc=1

--- 旧 5 文件口径（应绿 = 假绿）---
本卡写集内 mac 域残留：0 处 / 0 文件
退出码: 0（扫描集内零违规）        rc=0
```

→ **同一处真实缺陷**：9 文件口径点名 `file:line` 判红；旧 5 文件口径 **✅ 全绿**。
这就是我 F1 首报的「判据空转」被 c2 用作实证的同一现象，**我独立复做成功**。

### 9.4 红形态不再自毁：点名 file:line，`unbound variable` = 0 ✅

变异：`scripts/control-tower/alloc-task-id.sh:282` `${BRIEF_FILE}（` → `$BRIEF_FILE（`（精确 1 处），跑 scan 夹具：

```
rc=1
── 8. 判据②: 本卡写集内 mac 域残留 = 0（严格判红/绿）──
  ✅ 写集断言覆盖面 = 9 文件（brief 的 task 文件全集：4 scripts + 扫描器 + 3 测试 + ci.yml）
  ❌ 本卡写集内 mac 域残留 = 1 1（应为 '0 0'）—— 本卡写集内仍有违规，逐行清单如下:
      【违规（代码行）】1 处 / 1 文件
        scripts/control-tower/alloc-task-id.sh:282:  echo "brief 骨架已生成: $BRIEF_FILE（填写后开工）"
      【注释行（无害）】3 处 / 1 文件
  ❌ 写集模式应 rc=0，实得 1
unbound variable 计数 = 0
  结果: PASS=29 FAIL=3 SKIP=0
```

→ 对照 §三 M1（同一变异在 `6deb1ab2` 上）：那时红形态是 `line 213: WS<byte>: unbound variable` + rc=127、**无断言输出**；
现在**点名 `alloc-task-id.sh:282` 并打印逐行清单**，`unbound variable` = **0**。**F2 闭环。** 变异已 `git checkout` 复原（diff=0）。

### 9.5 零回归（串行）✅

```
$ for f in tests/control-tower/alloc-task-id.test.sh tests/control-tower/alloc-task-id-lock.test.sh \
           tests/control-tower/scan-fullwidth-vars.test.sh scripts/control-tower/alloc-task-id.sh \
           scripts/control-tower/scan-fullwidth-vars.sh; do bash -n "$f"; echo "rc=$?"; done
  rc=0 ×5

$ bash tests/control-tower/scan-fullwidth-vars.test.sh
  结果: PASS=32 FAIL=0 SKIP=0            rc=0

$ bash tests/control-tower/alloc-task-id.test.sh
  结果: PASS=44 FAIL=0                   rc=0

$ bash tests/control-tower/alloc-task-id-lock.test.sh     # WALL=409s
  结果: 7 通过, 0 失败
  Status: ✅ alloc-task-id 并发锁测试通过    rc=0
  逐条：接线 mkdir 锁 ✅ ／ 消费 SYNO_LOCK_DIR 注入缝（路径特异）✅ ／ 陈旧清理分支可达 ✅ ／ dry-run 不建壳 ✅
        ／ 并发 20 进程 → 20 个唯一号 ✅ ／ 真仓 brief 零污染（672 → 672）✅ ／ 沙箱收到 20 份骨架 ✅
```
（`scan` 由 `29` → **`32`** = 新增半径注记与覆盖面自检断言；`alloc 44/0`、`lock 7/0` 与改前一致。）

### 9.6 §12 适用域限定在位 ✅（原文）

```
784:## 12. 适用域限制（必须随交付一起读）: 并发「全绿」只覆盖测试隔离，**不覆盖生产时长形态**
864:· **不是**「生产并发已解决」—— 生产时长形态（218 worktree × 单次 ~3m20s ≫ LOCK_WAIT_SEC=30s）**未修**，
868:· 因此本卡不主张、也不得被引用为「alloc 的生产并发问题已解决」
866:· 夹具 20/20 全绿**不构成生产并发证据**: 那一跑走的正是「沙箱非 git → worktree/branch 段整体跳过」路径，
```
（取 `git show HEAD:docs/synova/product-lines/evidence/D938-夹具原始输出.md`，即冻结 tip 的**提交版**。）
→ 与我的 F3 **互相印证**：交付侧已把「并发全绿」限定为**测试隔离**，并显式**不主张**生产并发已解决。

### 9.7 追加未清项 U7–U11（诚实登记，未取证就不写绿）

| # | 未清项 | 状态与理由 |
|---|---|---|
| **U7** | F3（生产临界区 ≫ `LOCK_WAIT_SEC`，前置存量） | **保持开放**；已由交付侧 §12 登记边界 + 已进未清项，属**新卡**范畴 |
| **U8** | B-M6「去掉 `SYNO_LOCK_DIR` 缝 → **并发全绿必须红**」 | **已全部取证 = 红**（冻结 tip）：缝路径特异半边红（§9.8）、**并发双红**（alloc rc=1 / lock rc=2，§9.8b）；复原后 diff=0 |
| **U9** | A-5① 两测试并发**双绿**（lock 侧） | **部分取证**：alloc 侧在与 lock 并发时两次 rc=0 / `PASS=44 FAIL=0`（15:44、16:15 落 `/tmp/d938-v-out/conc-alloc.*`、`heavy/A5-1.alloc.*`）；**lock 侧并发完成未取到** → 记未取证（lock 串行 7/0 已过） |
| **U10** | A-5② 改前（`origin/main` 无缝合照）同场景**假红** | **未取证**（同上，需并发对跑）。c2 曾报 `PASS=33 FAIL=4`，**我不引用其结论** |
| **U11** | C-3 改前**运行时**断言数 37 / 5 | **未取证**（静态已证改前标签 0 条缺失）；改前 lock 单跑同样受 409s 级时长影响 |
| **U5** | 卡面 Done 另两项 `pre-commit-check.sh` + `check-pr-budget.sh` | **未取证**（不在 task-7 A–D 清单；重活，串行规则下未跑）→ 交接队长 |

**说明**：**U8 已闭环**（§9.8/9.8b：去缝 = 路径特异红 + 并发双红，复原 diff=0）。
**U9/U10/U11 仍未取证**——共同障碍是同一件事：余下几条都要求「**改前对照跑**」或「**并发对跑**」，
单跑即在 409s 量级，而后台作业在本会话**不可跨回合存活**（两次被中断回收）、前台长跑亦被中断。
我按纪律**如实登记为未取证**：**不放宽断言、不写绿、不引用 c1/c2 的对应结论**（c2 报的改前 `33/4` 我**未采信**）。

### 9.8 B-M6 变异（冻结 tip）：**缝路径特异半边 = 红** ✅

变异（`/tmp/d938-v-mut2` 试验区，精确 1 处）：
`LOCK_DIR="${SYNO_LOCK_DIR:-$ROOT/.alloc-task-id.lock}"` → `LOCK_DIR="$ROOT/.alloc-task-id.lock"`

```
M6 变异匹配次数 = 1
已去掉缝
52:LOCK_DIR="$ROOT/.alloc-task-id.lock"

=== M6 缝路径特异（去缝后）===
rc=0
注入锁目录是否仍在（=红/缝未被消费）: 仍在=红            ← 注入陈旧锁目录**未被动过**
输出: D500 (dry-run, 未建壳)

=== 复原（git checkout --，diff=0）后同探针 ===
rc=0 ｜ 注入锁目录: 已清理=绿                            ← 缝恢复后被真实消费
```

→ 与 §9.3 同一手法：**同一探针，改坏=红 / 复原=绿**，证明该缝**是承载行为的**，不是空转摆设。

**（9.8b）M6「并发全绿必须红」半边 —— 已取证 = 红** ✅（冻结 tip，去缝后同树并发跑两个夹具）

```
WALL=405s
=== M6 并发：alloc rc=1 / lock rc=2 ===
--- alloc（全文仅 5 行，崩在第 1 段，无 ❌ 输出）---
  D384 alloc-task-id 分配器测试
  ── 1. 正常分配 → D500 + 建壳 ──          ← 到此中止（set -e：gate 非零 → 整测试中止）
--- lock ---
  ✅ 接线: mkdir 原子锁…存在
  ❌ 接线: 未消费 SYNO_LOCK_DIR —— 注入的锁目录仍在（并发仍会与 alloc-task-id.test.sh 撞仓库锁）
  ❌ 接线: 陈旧清理分支未触发（次级证据缺失）
  ✅ dry-run 不建壳（输出: D946 (dry-run, 未建壳)）
  ✅ 并发 20 进程 → 20 个唯一号（无撞号）
  ✅ 真实 .claude/task-briefs/ 零污染（671 → 671 份）
  ✅ 沙箱 brief 目录收到 20 份骨架（注入缝真实生效，非静默跳过）
  结果: 5 通过, 2 失败     Status: ❌ alloc-task-id 并发锁测试未通过
alloc ❌ 计数=0（崩在断言前） ｜ lock ❌ 计数=3 ｜ 等锁超时字样出现次数 = 0
```

**机制说明（防误读）**：去缝后两个夹具共用 `$ROOT/.alloc-task-id.lock`；lock 夹具的前两个探针走真实 task-state
（worktree 扫描，分钟级）**持锁不放**，alloc 夹具第 1 段的 gate 调用等锁超时返非零 → 被 `set -e` 中止。
alloc 侧之所以看不到「获取分配锁超时」字样，是因为该测试第 1 段用 `OUT=$(… 2>&1)` **吞进了变量**且中止前未打印
（不是「没有发生超时」）。
→ **结论**：去缝 = **并发双红**（alloc rc=1 / lock rc=2）∴ 缝**是并发隔离的承重件**；复原后 diff=0，串行回归 44/0 与 7/0 已过（§9.5）。
→ 与 **F3** 不矛盾：这里红的是**夹具并发**（测试隔离域），正说明该缝在测试域内承重；生产时长形态另有 §12/U7。
