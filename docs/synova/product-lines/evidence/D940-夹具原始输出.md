# D940 夹具原始输出（task-13 / 执行者 d937-c2）

> **交付件**：`tests/control-tower/alloc-task-id.test.sh`（扩写判别性夹具）·
> `tests/control-tower/check-name-allocation.test.sh`（新建）·
> `docs/synova/coordination/派单模板.md`（新增「〇-b 命名规范」）·
> `.github/workflows/ci.yml`（密封清单 **+1 行**）
> **工作树** `/Users/wane/SynovaAgent/.synova-wt-squad-d940`｜**分支** `fix/d940-name-allocation`
> **基线** `origin/main = 9f0c8f65`（**非栈式**）
> **本文件所有数字均来自下列命令的原始输出，无手写；关键输出未 head 截断。**

```
$ date -Iseconds
2026-09-24T20:23:07+08:00

$ git status --short --branch
## fix/d940-name-allocation...origin/main
 M .github/workflows/ci.yml
 M docs/synova/coordination/派单模板.md
 M scripts/control-tower/alloc-task-id.sh
 M tests/control-tower/alloc-task-id.test.sh
?? .claude/task-briefs/2026-09-24-D940-name-allocation-registry.md
?? docs/synova/product-lines/evidence/D940-夹具原始输出.md
?? scripts/control-tower/check-name-allocation.sh
?? tests/control-tower/check-name-allocation.test.sh

$ git diff --numstat
1	0	.github/workflows/ci.yml
45	2	docs/synova/coordination/派单模板.md
171	7	scripts/control-tower/alloc-task-id.sh
95	0	tests/control-tower/alloc-task-id.test.sh

$ wc -l 两个测试文件
     162 tests/control-tower/check-name-allocation.test.sh
     219 tests/control-tower/alloc-task-id.test.sh
     381 total
```

## 1. 验收 1 — `bash tests/control-tower/check-name-allocation.test.sh`

退出码：**0**（0 = 全绿）

```
=== D940: 命名三元组校验器（check-name-allocation.sh）===
  ✅ 接线: /Users/wane/SynovaAgent/.synova-wt-squad-d940/scripts/control-tower/check-name-allocation.sh 存在
  ✅ 夹具前置: branch -r 看不见 d942 (0)，ls-remote 看得见 (1)
  ✅ 夹具前置: worktree 目录名 .synova-wt-squad-d99888 已登记

── 1. 正常: 三者一致且未占 → rc=0（--json status=free）──
  ✅ 正常输入 rc=0
  ✅ --json 正常: status=free 且 rc=0

── 2. 冲突·remote-branch: 942 只在 bare origin 上（本地未 fetch）→ rc=1 + 点名 ──
  ✅ 远端分支占用 rc=1
  ✅ 点名冲突位置原文 docs/d942-cto-fixation
  ✅ --json 冲突: status=conflict + conflicts 含 remote-branch:docs/d942-cto-fixation

── 3. 冲突·task-state: 941 已登记 → rc=1 + 点名 task-state ──
  ✅ task-state 占用 rc=1
  ✅ 点名冲突位置标签 task-state

── 4. 冲突·worktree-name: 目录名 .synova-wt-squad-d99888 已登记 → rc=1 + 点名 ──
  ✅ worktree 目录名占用 rc=1
  ✅ 点名冲突位置标签 worktree-name

── 5. 冲突·不一致: 三处 D# 互不相同（号本身全空闲）→ rc=1（一致性面，非占用面）──
  ✅ 三处 D# 不一致 rc=1（号空闲也拦）

── 6. 边界·rc=2 三路径（校验器自身失败，不得与 0 混同）──
  ✅ 无任何有效输入 → rc=2
  ✅ --id 格式非法（NOTANID）→ rc=2
  ✅ --id 缺值 → rc=2
  ✅ 未知选项 --bogus → rc=2

── 7. 降级: origin 不可达 → degraded: 可见 + 仍可用（绝不静默放行）──
  ✅ 降级显式: 输出含 degraded: 前缀
  ✅ 降级后仍可用（rc=0，按本地占用判定）

── 8. 反向金丝雀: 同一夹具只改 id → 占用必须 rc=1、空闲必须 rc=0 ──
  ✅ 金丝雀: 占用输入 rc=1 / 空闲输入 rc=0（校验器非空转网）

结果: 20 通过, 0 失败
```

## 2. 验收 2 — `bash tests/control-tower/alloc-task-id.test.sh`

退出码：**0**（0 = 全绿）

```
═══════════════════════════════════════════════════════════
  D384 alloc-task-id 分配器测试
═══════════════════════════════════════════════════════════

── 1. 正常分配 → D500 + 建壳 ──
  ✅ 分配 D500 (max=499 → +1=500 起步)
  ✅ 登记提示
  ✅ 空壳已建
  ✅ status=claimed

── 2. 连续分配 → D501 (单调递增) ──
  ✅ 第二次分配 D501

── 3. dry-run → 只预览不建壳 ──
  ✅ dry-run 预览 D502
  ✅ dry-run 标注
  ✅ dry-run 未建壳

── 4. 空任务名 → exit 1 + 用法 ──
  ✅ 空名拒绝 (exit=1)
  ✅ 用法提示

── 5. 撞车防护逻辑存在（并发竞态防御；单进程不可触发，靠唯一入口 + 建壳原子检查）──
  ✅ 防护逻辑存在 (建壳前检查)
  ✅ 正常流程不误触发 (exit 0) (exit=0)

── 6. D550: origin/main 占用合并（落后本地不漏号——D547/D548 撞号实证）──
  ✅ origin/main 合并: 空本地发 D942 > main max D941（不漏号）

── 7. CT-63: 远端分支名 D# 扫描 ──
  ✅ CT-63: 分支 d605 → 发 D606 > 605

── 8. CT-63 注入缝 NO_BRANCH=1 ──
  ✅ CT-63 NO_BRANCH: 发 D500 = 500

── 9. D940 判别性[真实样本名]: 远端分支占 942 而本地未 fetch → 取号必须拒绝并点名 ──
  ✅ 前置自校验: branch -r 看不见 d942/d943 (0)，ls-remote 看得见 (2)
  ✅ 未 fetch 的远端分支占用 → 拒绝（fail-closed，不发放） (exit=1)
  ✅ 点名冲突位置（远端分支全 ref 原文）
  ✅ 拒绝时不建壳
  ✅ 拒绝后未建 D942 壳

── 10. D940 判别性: worktree 目录名占 942（其它位置均无 942）→ 不得发放 942 ──
  ✅ 前置自校验: worktree 内无 D942.json（信号只在目录名）
  ✅ 拒绝并点名冲突位置（worktree 名原文）

── 11. D940 回归守卫: task-state 已占 942 → 绝不发放 942 ──
  ✅ 已占 942 → 未发放 942（实际 D943）

═══════════════════════════════════════════════════════════
  结果: PASS=23 FAIL=0
═══════════════════════════════════════════════════════════
```

## 3. 验收 3 — 判别性两段：「改动前基线」必须红

基线树 = `git archive origin/main | tar -x`（origin/main = 9f0c8f65 的**未改** scripts/ 全树），
同一份夹具源码原样拷入后运行 —— **只有被测生产脚本是新旧之别**。

**3a. 改前基线 — `alloc-task-id.test.sh`（5 条新判别性断言全红）**

```
═══════════════════════════════════════════════════════════
  D384 alloc-task-id 分配器测试
═══════════════════════════════════════════════════════════

── 1. 正常分配 → D500 + 建壳 ──
  ✅ 分配 D500 (max=499 → +1=500 起步)
  ✅ 登记提示
  ✅ 空壳已建
  ✅ status=claimed

── 2. 连续分配 → D501 (单调递增) ──
  ✅ 第二次分配 D501

── 3. dry-run → 只预览不建壳 ──
  ✅ dry-run 预览 D502
  ✅ dry-run 标注
  ✅ dry-run 未建壳

── 4. 空任务名 → exit 1 + 用法 ──
  ✅ 空名拒绝 (exit=1)
  ✅ 用法提示

── 5. 撞车防护逻辑存在（并发竞态防御；单进程不可触发，靠唯一入口 + 建壳原子检查）──
  ✅ 防护逻辑存在 (建壳前检查)
  ✅ 正常流程不误触发 (exit 0) (exit=0)

── 6. D550: origin/main 占用合并（落后本地不漏号——D547/D548 撞号实证）──
  ✅ origin/main 合并: 空本地发 D942 > main max D941（不漏号）

── 7. CT-63: 远端分支名 D# 扫描 ──
  ✅ CT-63: 分支 d605 → 发 D606 > 605

── 8. CT-63 注入缝 NO_BRANCH=1 ──
  ✅ CT-63 NO_BRANCH: 发 D500 = 500

── 9. D940 判别性[真实样本名]: 远端分支占 942 而本地未 fetch → 取号必须拒绝并点名 ──
  ✅ 前置自校验: branch -r 看不见 d942/d943 (0)，ls-remote 看得见 (2)
  ❌ 未 fetch 的远端分支占用 → 拒绝（fail-closed，不发放） — 期望 exit≠0，实际 0（fail-closed 未生效）
  ❌ 点名冲突位置（远端分支全 ref 原文） — 未找到: docs/d942-cto-fixation
  ❌ 拒绝时不建壳 — 不应出现: 已登记:
  ❌ 拒绝却建了 D942 壳

── 10. D940 判别性: worktree 目录名占 942（其它位置均无 942）→ 不得发放 942 ──
  ✅ 前置自校验: worktree 内无 D942.json（信号只在目录名）
  ❌ worktree 目录名已占 942，却仍发放 D942（漏号）

── 11. D940 回归守卫: task-state 已占 942 → 绝不发放 942 ──
  ✅ 已占 942 → 未发放 942（实际 D943）

═══════════════════════════════════════════════════════════
  结果: PASS=18 FAIL=5
═══════════════════════════════════════════════════════════
exit=1
```

**3b. 改前基线 — `check-name-allocation.test.sh`（校验器尚不存在 → 除夹具前置自校验外全红）**

```
=== D940: 命名三元组校验器（check-name-allocation.sh）===
  ❌ 接线: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh 不存在（D940-T1 未落地 → 本测试全红属预期，禁放宽）
  ✅ 夹具前置: branch -r 看不见 d942 (0)，ls-remote 看得见 (1)
  ✅ 夹具前置: worktree 目录名 .synova-wt-squad-d99888 已登记

── 1. 正常: 三者一致且未占 → rc=0（--json status=free）──
  ❌ 正常输入应 rc=0，实际 rc=127：bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory
  ❌ --json 正常应 status=free + rc=0，实际 rc=127：bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory

── 2. 冲突·remote-branch: 942 只在 bare origin 上（本地未 fetch）→ rc=1 + 点名 ──
  ❌ 应 rc=1，实际 rc=127
  ❌ 未点名冲突位置: bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory
  ❌ --json 冲突形态不符，rc=127：bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory

── 3. 冲突·task-state: 941 已登记 → rc=1 + 点名 task-state ──
  ❌ 应 rc=1，实际 rc=127
  ❌ 未点名 task-state: bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory

── 4. 冲突·worktree-name: 目录名 .synova-wt-squad-d99888 已登记 → rc=1 + 点名 ──
  ❌ 应 rc=1，实际 rc=127
  ❌ 未点名 worktree-name: bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory

── 5. 冲突·不一致: 三处 D# 互不相同（号本身全空闲）→ rc=1（一致性面，非占用面）──
  ❌ 应 rc=1，实际 rc=127

── 6. 边界·rc=2 三路径（校验器自身失败，不得与 0 混同）──
  ❌ 无输入应 rc=2，实际 rc=127
  ❌ --id 非法应 rc=2，实际 rc=127
  ❌ --id 缺值应 rc=2，实际 rc=127
  ❌ 未知选项应 rc=2，实际 rc=127

── 7. 降级: origin 不可达 → degraded: 可见 + 仍可用（绝不静默放行）──
  ❌ 缺 degraded: 前缀（静默降级）: bash: /tmp/d940-base/scripts/control-tower/check-name-allocation.sh: No such file or directory
  ❌ 降级后应仍可用 rc=0，实际 rc=127

── 8. 反向金丝雀: 同一夹具只改 id → 占用必须 rc=1、空闲必须 rc=0 ──
  ❌ 金丝雀失败: 占用 rc=127（应 1）、空闲 rc=127（应 0）—— 校验器恒返回同一值即空转

结果: 2 通过, 18 失败
exit=1
```

> 对照：改前基线 alloc 夹具 **PASS=18 FAIL=5**（5 红全在新增判别性用例：
> 远端未 fetch 占用不拒绝 / 不点名 / 拒绝时仍建壳 / worktree 目录名占用仍发号）；
> 校验器夹具 **2 通过 18 失败**（2 = 夹具前置自校验，证明夹具构造真实）。
> 当前树同两份夹具 = **23/0** 与 **20/0**。

## 4. 验收 4 — `ci.yml` 密封接线（+1 行）

```
$ grep -n "check-name-allocation\|alloc-task-id" .github/workflows/ci.yml
252:            tests/control-tower/alloc-task-id.test.sh \
253:            tests/control-tower/alloc-task-id-lock.test.sh \
254:            tests/control-tower/check-name-allocation.test.sh \

$ git diff .github/workflows/ci.yml
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index e4b51a0c..f5d70fc5 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -251,6 +251,7 @@ jobs:
             tests/control-tower/verify-parallel-ci.test.sh \
             tests/control-tower/alloc-task-id.test.sh \
             tests/control-tower/alloc-task-id-lock.test.sh \
+            tests/control-tower/check-name-allocation.test.sh \
             tests/control-tower/check-gitlinks.test.sh \
             tests/control-tower/grep-oP-regression.test.sh \
             tests/control-tower/merge_writeset_gate.test.sh \
```

## 5. 验收 5 — `派单模板.md` 命名规范节

```
$ grep -nE "cto-|squad-" docs/synova/coordination/派单模板.md
32:> 撞名的代价不是重命名：`.synova-wt-squad-b5` 预存在目录无 owner/分支 → `git worktree add` 直接 `fatal: already exists`（撞名第 4 次实例）。
39:| **worktree 名** | `.synova-wt-<侧前缀>-<卡号小写>-<主题>` | `.synova-wt-squad-d940` | `.synova-wt-940`、`.synova-wt-squad-b5`（无卡号） |
50:| `cto-` | CTO 侧发出的卡 | `.synova-wt-cto-d940-<主题>` |
51:| `squad-` | 小队（Agent Teams）执行的卡 | `.synova-wt-squad-d940-<主题>` |
72:## 写前核实（强制清单 — cto-handover skill §〇c ①，6 项缺一不写）
81:## DSH 借鉴核查（强制章节 — cto-handover skill §〇b 三步核查后填写）
137:## 写后自检（强制清单 — cto-handover skill §〇c ③，8 项缺一不交付）
190:- **组队**：用 Agent Teams（预设 `synova-squad-lead` 已挂 `agent-team`/`tool-agent-team`/`ui-agent-team`，`maxMembers=4`）

$ git diff --numstat docs/synova/coordination/派单模板.md
45	2	docs/synova/coordination/派单模板.md
```

## 6. 验收 6 — `bash -n`

```
$ bash -n tests/control-tower/check-name-allocation.test.sh  → 0
$ bash -n tests/control-tower/alloc-task-id.test.sh          → 0
（0 = 0 错误）
```

## 7. 夹具设计要点（为什么这样写才对得上契约）

1. **密封性**：全部用 `mktemp -d` 临时 git 仓 + **本地 bare 仓当 `origin`**，离线、不连真网、
   不依赖真仓 tracking ref（真仓当前**有** `origin/docs/d942-cto-fixation`，"未 fetch"在真仓**不可复现** ——
   与 c1 独立实测同结论）。
2. **判别性（旧实现下必红）**：每条新断言都钉住"发放了被占号"或"没点名冲突位置"这两个旧行为；
   §8.1 给出改前基线的 5 条红。
3. **接口口径**：与 c1 逐条对齐后写（拒绝语义=rc≠0+逐字点名；5 个位置标签；
   rc=2 三触发条件；`degraded:` 字面量；`--json` 键形）。夹具只钉**契约**，不反向猜实现。
4. **防空转**：金丝雀 = 同一夹具只改 id，占用必须 rc=1、空闲必须 rc=0；两者相等即"恒返回同一值"= 红。
   另有两处**夹具前置自校验**（`branch -r` 看不见 d942 + `ls-remote` 看得见 + worktree 已登记），
   构造失败即红，不让后续断言空转。

## 8. `alloc-task-id-lock.test.sh` 处置（卡面要求汇报）

本卡写集**不含**该文件 → **未改动**。lead 2026-09-24 裁定原文：
「`alloc-task-id-lock.test.sh`：**不动**（你的判断对，是我 task-12 措辞有歧义）……真实归属是 **D938-T2**」。

实测本树未被 c1 改动破坏（保留为证据）：

```
=== D456 alloc-task-id 并发锁测试 ===
  ✅ 接线: mkdir 原子锁（_lock_acquire/_lock_release）存在
  ✅ dry-run 不建壳（输出: D949 (dry-run, 未建壳)）
  ✅ 并发 20 进程 → 20 个唯一号（无撞号）
  ✅ 真实 .claude/task-briefs/ 零污染（673 → 673 份）
  ✅ 沙箱 brief 目录收到 20 份骨架（注入缝真实生效，非静默跳过）

  结果: 5 通过, 0 失败
  Status: ✅ alloc-task-id 并发锁测试通过
exit=0
```

## 9. 未清项 / 已知边界（诚实披露，非「通过」声明）

### 9.1 真仓 `--dry-run` 241s 是**存量**成本，非本卡引入（双人独立复现）
```
$ git worktree list --porcelain | grep -c '^worktree '   → 200
$ 全 worktree 的 task-state/D*.json 命中数
   含主工作区 TS_TOP                        → 51145
   不含 TS_TOP（仅链接 worktree）           → 50788   ← 与 c1 独立实测 50788 逐字一致
   差 357 = 主工作区自身贡献（解释两口径差异）
```
- **我的实测**：当前树真仓 `--dry-run` = **241s**；**origin/main 基线脚本 + 真仓 TS_TOP = 251s**
  → 差值在噪声内 ⇒ **存量**。
- **c1 独立实测**（其消息原文；改前/改后四组数，**各自测的是什么**已标明，防被 K3 判「数字打架」）：
  - **① 隔离循环 A/B（唯一 apples-to-apples 对照）**：探针 = 把 `alloc-task-id.sh:101-129` 逐字复刻成两变体，
    **仅** bn 取值一行不同 →
    `basename` **238s** vs `${f##*/}` **105s**（**×2.27**）；两侧输出 **203152 字节 / 50788 行**，
    **sha256 两侧相同**（`47108a6f…5da1`）、**diff 逐字节一致（0 差异）** ⇒ **语义等价硬证据**。
  - **② 端到端同命令**（真仓 `alloc-task-id.sh "e2e-probe" --dry-run`，两侧脚本 sha256 已核）：
    基线工作树 **200s** → 本卡树 **107s**（**×1.87**）；两侧分配结果**同为 `D949`**（行为等价旁证）。
  - **③ 改后 lock 测试**（含 1 次真仓 dry-run + 20 并发沙箱）：改前 **3m53s（233s，我实测）** → 改后 **109s**（**×2.14**）。
  - **④ 口径提醒**：**258s** = 首测单跑完整循环（冷缓存）；**238s** = A/B 探针里的同一循环 ——
    **同一被测对象的两次测量，不是两个对象**。c1 已在 `D940-改动清单.md` §3.1 列「耗时口径对照表」。
  - `ls-remote` 6s / 666 分支、`branch -r` 2s、`for-each-ref` 2s、`worktree list --porcelain` 2s → 均**非瓶颈**。
- **机理**：`scripts/control-tower/alloc-task-id.sh:254`（**改前**）对上述 **50,788** 个命中文件各 fork 一次 `basename`
  （D576 引入的 worktree 扫描；`memory/notes` D793 已记「生产路径 110+ worktree 挂住」）。
- **CI 不受影响**：Actions checkout 只有 **1** 个 worktree → 该循环规模≈1。
- c1 已获 lead 授权在本卡内把 `:254` 的 `basename "$f"` 改为 `"${f##*/}"`（卡面范围外必要前提修复，追认待 CTO），
  已落在 `fix/d940-name-allocation`（提交 `2322939d`）；本件 §9.1 同时保留**改前**基线数字与之对照。

### 9.2 卡面「合成样本 task-state 已占」为何换面
`alloc-task-id.sh` 的 NEXT = MAX + 1，而 MAX 取自占用表的 task-state；**当 task-state 里已有 D942 时
MAX ≥ 942 → NEXT ≥ 943，分配器永不可能撞上已登记的号** → 该条在分配器上**不可达**。
故落到 `check-name-allocation.sh --id D###` 的校验面（§1 中「冲突·task-state」一条），与 c1 对齐后执行；
分配器侧保留 §11 回归守卫（已注明旧实现下也绿、非判别性）。

### 9.3 `--json` 契约偏离 → 夹具发现 → 生产者返工（**断言未放宽**）
首版 `conflicts[]` 元素为 `remote-branch:<sha>\trefs/heads/... (<sha>\trefs/heads/...>)`，
契约是 `<标签>:<短名>`。夹具按契约断言并保持红，回报 c1。c1 定位真根因（`ls-remote` 行首字段是 **SHA**，
`short="${ref#refs/heads/}"` 对整行不生效 → `short` 与 `ref` 同为原始行 → 括号重复 + JSON 被污染；
**匹配之所以仍命中是因为原始行恰好含短名 = 潜在假绿面**），已修为 `awk '{print $2}'` 取 ref 名。
修复随 c1 主提交 **`2322939d`** 落地；现 §1 全绿（`{"id":"D942","status":"conflict","degraded":false,"conflicts":["remote-branch:docs/d942-cto-fixation"]}`）。

### 9.4 跨卡写集撞车与合并顺序（lead 裁定，2026-09-24）
task-6（**D938-T2**，completed，**未并入 main**）声明的写集含同样两个文件
（`tests/control-tower/alloc-task-id.test.sh`、`.github/workflows/ci.yml`）。
lead 裁定：**合并顺序 `D938 → D940`**（先过收件闸者先合）；本卡改动与其**区域不重叠**
（D938 = A′/title 用例 + 扫描器接线；本卡 = §9-11 + 新文件）；
**本 PR 声明 `base = origin/main`，若 D938 先合则本卡 rebase 到含 D938 的 main 后再合**；
`ci.yml` 全序 **`D938 → D940 → M9 → B3 → B4`**，两卡在密封清单里的两行**相邻**
（D938 的 `scan-fullwidth-vars.test.sh` 与本卡的 `check-name-allocation.test.sh`）。

### 9.5 矩阵外平台未实测
本夹具在 macOS 实跑全绿；CI 跑 ubuntu + windows，只做了平台稳健性设计（临时仓 + 本地 bare origin、
不连真网、禁 BSD 特有行为、`LC_ALL=C` 定点使用），**未在本地实跑那两个平台**。

### 9.6 改动范围
未触碰 `scripts/**`（c1 写集）、`scripts/audit/**`（红线）、`docs/synova/coordination/ownership.yaml`、
`docs/synova/coordination/派单-Mac小队-五卡-20260924.md`、`tests/control-tower/alloc-task-id-lock.test.sh`。

## 10. 自验 5 问自答（squad-discipline §二.6）

1. **新 export 谁调用？** 无 export。新测试由 `.github/workflows/ci.yml:254` 密封清单指名调用（见 §4）。
2. **每个 catch 有 log + degraded？** 无 try/catch；等价失败面已覆盖：夹具前置自校验、金丝雀、
   rc=2 三路径（自身失败不得与 0 混同）、降级路径打印 `degraded:`。
3. **as any = 0？** 本卡不含 TypeScript。
4. **测试有断言且覆盖正常/降级/边界？** 是。校验器夹具 20 条：正常（rc=0 / `--json status=free`）、
   降级（origin 不可达 → `degraded:` + 仍可用）、边界（rc=2 ×4）、冲突（remote-branch / task-state /
   worktree-name / 三处 D# 不一致）、金丝雀、接线、夹具前置自校验 ×2。
   分配器夹具 23 条：既有 1-8 全保留 + §9/§10 判别性 + §11 回归守卫。
5. **有死代码/旧引用残留？** 无。夹具全部 `mktemp -d` + `trap` 清理；不改真仓 task-state / brief 目录
   （沿用 `SYNO_TASK_STATE_DIR` / `SYNO_BRIEF_DIR` 注入缝）。

## 11. 远端回执（防「声称已推送、实际只在本地」）

**交付提交** = `8ff325732bc541d76d8eb96d4f698e6933b5df18`（5 files changed, 701 insertions(+), 2 deletions(-)）；
其后 `5bc0d391` 是 post-commit hook 自动追加的 `chore: bypass COMMITTED 登记 (auto hook, D521)`
证据链登记提交，不是本卡交付内容 —— 核验交付看 `8ff32573`。
抓取时刻 `2026-09-24T20:24:04+08:00`（本节由**纯文档追加提交**写入，故远端 head 会在下述回执之后再前移一次）。

```
$ git ls-remote --heads origin | grep fix/d940-name-allocation
5bc0d3914f133b1b2f1c50d874ce7065b4262296	refs/heads/fix/d940-name-allocation

$ git rev-parse HEAD          # 与远端一致 = 已真实推送
5bc0d3914f133b1b2f1c50d874ce7065b4262296

$ git show --stat --format="%H%n%s" 8ff32573
8ff325732bc541d76d8eb96d4f698e6933b5df18
test(D940): 命名三元组判别性夹具 + 撞号拒绝用例 + 命名规范落模板 + ci.yml 接线

 .github/workflows/ci.yml                           |   1 +
 docs/synova/coordination/派单模板.md               |  47 ++-
 .../product-lines/evidence/D940-夹具原始输出.md    | 398 +++++++++++++++++++++
 tests/control-tower/alloc-task-id.test.sh          |  95 +++++
 tests/control-tower/check-name-allocation.test.sh  | 162 +++++++++
 5 files changed, 701 insertions(+), 2 deletions(-)
```
