# D940 收尾三件与小队回执（名字统一分配：卡号 / worktree / 分支）

> 卡号 D940（WIP=1 队列第 3 位）｜ 执行形态：Agent Teams 小队（队长 + 2 编码 + 1 独立自验）
> 分支 `fix/d940-name-allocation` ｜ base `origin/main` = 9f0c8f65（**不栈式**）｜ **自验锚点 tip = `114e129e`**
> 本文件由**队长**（synova-squad-lead）撰写，属治理产物（`domain_neutral`），非产品代码

---

## 一、成员运行记录（**本轮为"复用成员"**，如实登记）

**背景**：本 session 团队成员**已达上限 8**（`spawn_teammate` 返回 `Error: Team member limit 8 reached`），无 `dismiss` 工具 → 复用**已收口且通过收件闸**的 D937 成员。

| 成员（target） | 原卡 | 本卡角色 | 状态 | token | 共享任务 | 状态 |
|---|---|---|---|---|---|---|
| `d937-c1` | D937 编码A（已过闸） | **D940 编码A**（分配器+校验器） | inactive 已交付 | 未暴露 | **task-12** | **completed**(rev 3) |
| `d937-c2` | D937 编码B（已过闸） | **D940 编码B**（夹具+模板+ci.yml） | inactive 已交付 | 未暴露 | **task-13** | **completed** |
| `d937-v` | D937 自验（已过闸） | **D940 独立自验** | inactive 已交付 | 未暴露 | **task-14** | **completed** |
| `lead` | — | 队长（协调/判前提/验证据，**不下场写码**） | running | 未暴露 | — | — |

编制 4 = maxMembers ｜ **自验不由编码兼任** ｜ 写集交集 **0** ｜ 重型验证串行
**token 列留空**：本会话工具链未暴露用量，按"数字禁手写"纪律**不伪造**。

---

## 二、交付内容（7 项写集 + 2 项授权/门禁件）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `scripts/control-tower/alloc-task-id.sh` | numstat **+171/−7**；4 缺口 + `--check-id` 只读复用面 + 授权的前提修复 |
| 2 | `scripts/control-tower/check-name-allocation.sh` | **新建 165 行**；三态 0/1/2 + `degraded:` + `--json`；占用判定**委派** allocator（无第二副本） |
| 3 | `tests/control-tower/check-name-allocation.test.sh` | 新建；**21 断言**（含 §0 接线判别性） |
| 4 | `tests/control-tower/alloc-task-id.test.sh` | **PASS=24**（含 §12 接线断言） |
| 5 | `docs/synova/coordination/派单模板.md` | 新增「〇-b 命名规范」**+45/−2** |
| 6 | `.github/workflows/ci.yml` | **+1 行**（L254 密封清单） |
| 7 | `docs/synova/product-lines/evidence/D940-改动清单.md` | 359 行 |
| 8 | `docs/synova/product-lines/evidence/D940-夹具原始输出.md` | 424 行 |
| 9 | `docs/synova/product-lines/evidence/D940-自验.md` | **376 行**（一轮 280 为历史 + §〇/§六 增量复验） |
| — | `memory/notes/implemented/2026-09-24-d940-cross-location-alloc.md` | **门禁倒逼**（commit-msg D395-a/D534） |
| — | `.claude/task-briefs/2026-09-24-D940-name-allocation-registry.md` | `builtin`（G12/D749 倒逼） |

**域内（代码/CI）= 5 文件 ≤ 12** ｜ **单域 mac**（实测 `check-ownership.py` PASS）

---

## 三、两处「有据偏离」（**已获队长书面批准，待 CTO 追认**）

1. **`ls-remote` 只喂「发号前校验」，不喂「下一个号」计算表** —— 卡面 ① 措辞与卡面验收 1 **互斥**：若同喂，`NEXT` 直接跳过远端已占号 → **「拒绝并点名」永不触发**。取**决策点 fail-closed** 是唯一两全读法。
2. **`basename` → `"${f##*/}"`**（**卡面范围外的必要前提修复**）：存量 200 worktree × 50,788 task-state 文件 → `basename` **5 万次 fork**。
   - **仅此一处**：`$(basename` 真实调用 **1 → 0**（余 1 为注释）
   - **等价性**：两侧输出**逐字节一致**（c1 203,152B/50,788 行 sha256 同；d937-v 206,008B/51,502 行 sha256 同）
   - **耗时**：c1 隔离 **238s→105s**、端到端 **200s→107s（同 `D949`）**；d937-v **189s→93s**、**180s→101s**（环境负载差异，**绝对值不可跨环境对账**，一致的是**定性与倍率**）

---

## 四、独立自验：`可提请独立审计`（d937-v，锚 `114e129e`）

**未通过项：无**（一轮 3 项退回已全部解除）；**永不写"审计通过"**

| 判据 | 结果 |
|---|---|
| **A** 跨位置拒绝 | `rc=1` + 逐行点名 `remote-branch docs/d942-cto-fixation` + **拒绝后无 `D942.json`** |
| **B** 三态 / `--json` | rc=1/0/1 + **4 种坏输入全 rc=2**；`--json` = `["remote-branch:docs/d942-cto-fixation"]`（元素 1、无 sha、无重复、可解析） |
| **C** 降级显式 | 物理坏 origin（rc=128）→ `degraded:` 可见 + **rc=0 仍可用** + `"degraded":true` |
| **D** 等价性 | 见 §三.2（**逐字节一致**） |
| **E** 变异体 | M1–M4/M6 逐条红 + **复原全绿**；**M5 三段判别性**（见下） |
| **F/G/H** | 他人文件命中 **0**｜6 项计数成立｜前提 4 处订正 **4/4** |

**E-M5 的判别性（本轮最值钱的一段）**：删 **只 254** → 只有 `checker` 接线断言红；删 **只 252** → 只有 `alloc` 接线断言红 ⇒ **逐条钉住自己的登记行**（**非"顺带红了"**）；两行全删 → 双红；复原 → 回绿且 `ci.yml` **sha256 与 HEAD 逐字节一致**（`09ac0bc44ac7410d…`，c2 与 d937-v 两侧**逐字相同**）。

**一轮退回 3 项 → 修法与复核**
1. **E-M5 接线判别性缺失**（删登记行后三夹具全绿；`check-name-allocation.test.sh:40-45` 只检脚本存在）→ c2 `c50dbeed` 照抄仓内先例（`check-citations.test.sh:36` 等）加 `grep -q '<测试名>' ci.yml` → **已解除**
2. **冲突扫描计数** `181→185` 错 → c1 `8c0f4832` 订正为 **`180→186`（tracked 180→185）** + 命令/排除项/截至时刻 + **「报告自身含该串 ⇒ 落盘 +1」** 因果 → **已解除**
3. **「真实调用点 6」不可复现**（初版枚举 11 条却写 6）→ 删「6」，改 **测试 4 + 提示语 1 + skill 6 = 11** → **已解除**

---

## 五、`git diff --stat` 与 ls-remote 回执

```
$ git ls-remote --heads origin fix/d940-name-allocation
 <tip>	refs/heads/fix/d940-name-allocation
$ git rev-parse HEAD          → 同值
$ git merge-base --is-ancestor <各交付提交> <tip>   → 全部 ✅
$ git status --short          → 仅 ?? docs/synova/product-lines/evidence/D940-自验.md（本回执提交后清空）
$ git diff --name-only origin/main...HEAD  → 11 项（域内 5 + 治理 6）
```
**收口提交后 artifact 不变性（可核）**
```bash
git diff 114e129e7ae2d9888d3c9a719391de8c39a756c1..HEAD -- \
  scripts/control-tower/alloc-task-id.sh scripts/control-tower/check-name-allocation.sh \
  tests/control-tower/alloc-task-id.test.sh tests/control-tower/check-name-allocation.test.sh \
  .github/workflows/ci.yml docs/synova/coordination/派单模板.md
# 空 = 自验结论对本 HEAD 仍有效｜非空 = 锚点失效，须重跑
```
> **口径提示（c1 亲踩）**：判"是否已在远端"用 **`merge-base --is-ancestor`**，**不要**用"本地 sha == 远端 tip" —— 本卡分支由两人共享追加，两次查询之间可能被对方推送导致**误报"不同"**。

---

## 六、未清项 / 残余风险（诚实登记）

| # | 项 | 归属 |
|---|---|---|
| **N8** | **降级发放缺可追溯标记**：`ls-remote` 不可达时按卡面"仍可用"继续发号 ⇒ 该次发放**无法证明远端未占**，且 `degraded` 只在**当次** stderr/`--json`。定性 = **"有条件的 fail-open 面"，非纯口径问题**。建议口径：`task-state/<卡>.json` 写 `issued_degraded: true` + 缺失校验位置；`check-name-allocation.sh` 对这类卡**持续告警**直至含远端校验的复验通过 | **CTO** |
| **残余风险** | **`alloc` 夹具 §9 人类可读断言是子串检查**（`assert_contains … "docs/d942-cto-fixation"`）→ 整行形态下**侥幸通过**；真正拦住 M6 的是 `--json` 元素断言。该文件属 **D938 写集**，本卡**不改断言强度**（避免双写） | **CTO/K3** |
| **N6** | `WORKTREE_USED` O(n²) 未动（改后仍 **~93–101s** 的主因）→ 建议另立卡 | CTO（建议另立卡） |
| **N5** | 生产者 `alloc-task-id.sh:337` 骨架模板写死 `"spec": null` → **持续再产 NoneType**（实测 158 份 / dict 80 / string 118）→ 建议另立卡 | CTO（建议另立卡） |
| **N9** | `--dry-run` 现因跨位置冲突 `exit 1`（**行为变化**，语义合理；stdout 首行仍恒为 `D### (dry-run, 未建壳)`，`alloc-task-id-lock.test.sh:33` 的 `head -1` 依赖此点且仍绿） | 登记备知 |
| **N7** | `alloc-task-id.sh` 缺 UTF-8 头块（存量；新建校验器有） | 存量 |
| — | 矩阵外平台（ubuntu/windows）**未实测**（仅本机 macOS） | 环境限制 |
| — | 耗时**绝对值不可跨环境对账**（输入 +714 文件差异） | 口径提示 |
| — | **合并顺序**：`D938 → D940`（`[ahead 14 / behind 0]`）；`ci.yml` 全序 `D938 → D940 → M9 → B3 → B4` | CTO |

---

## 七、本次值得进坑清单的四条（实测所得）

1. **`merge-base --is-ancestor` 判"是否已在远端"优于"比 tip 相等"**（c1 因对方并发推送**误报过"不同"**）
2. **共享机器禁固定名临时文件**（c2 的 `/tmp/r1.txt` 被同机其他进程覆盖 → 复原后首跑 **假红** → 改 `mktemp -d` 后 21/0）
3. **`synova-commit` 的 D706 用 `--no-renames` 比对 `--files`** → 有 `git mv` 时**旧路径也必须写进 `--files`**，否则报"暂存区含声明之外的变更"
4. **D472 迁移门禁口径**：`task-state.status ∈ {impl_done, spec_done}` 时，`proposed/` 下的 Note 被判**僵尸条目**（CI 权威）→ Note 应**直接落 `implemented/`**

**另一条被独立复现两次的老坑**：bash 3.2 下**全角标点紧贴 `$VAR`**（`$RC（`）会被吞进变量名 → `unbound variable`（D370 已记，本轮 c1/c2/d937-v 各自再踩）。

---

## 八、声明

本文件与 `D940-自验.md` 均为**自验结论**，**不是审计结论**，**不含"通过"判定**。
**自验结论 = `可提请独立审计`**（**自验锚点 `114e129e7ae2d9888d3c9a719391de8c39a756c1`**；本文件所在收口提交仅加治理产物 —— 用 §五 不变性命令自证）。
**K3 复审通过前不得合并**；是否通过归 **CTO 收件闸 + K3 终审**。
