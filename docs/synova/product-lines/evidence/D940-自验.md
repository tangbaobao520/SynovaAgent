# D940 独立自验报告 — 分配器跨位置拒绝重号 + 三元组校验器

> **写者**：`d940-v`（独立自验员；**非** d940-c1 / d940-c2 的延续）｜**卡**：task-14（D940-T3）
> **工作树**：`/Users/wane/SynovaAgent/.synova-wt-squad-d940`（**只读**；本文件是唯一写入物）
> **冻结坐标（我自己 fetch + ls-remote 取，未采信转述）**：
> ```
> $ git fetch --all -q
> $ git ls-remote --heads origin | grep fix/d940-name-allocation
> 8b894784a105e3ceef6f1052baaadda82c01f362	refs/heads/fix/d940-name-allocation
> $ git rev-parse HEAD
> 8b894784a105e3ceef6f1052baaadda82c01f362
> $ git rev-parse origin/fix/d940-name-allocation
> 8b894784a105e3ceef6f1052baaadda82c01f362      ← 本地 == 远端（0/0）
> ```
> **tip 移动取证（我第一次取到的是 `c7dae85b`，后按 lead 通知复核）**：
> ```
> $ git diff --name-only c7dae85b..8b894784
> .claude/bypass.log
> docs/synova/product-lines/evidence/D940-夹具原始输出.md
> $ git diff --numstat c7dae85b..8b894784
> 1	0	.claude/bypass.log
> 15	6	docs/synova/product-lines/evidence/D940-夹具原始输出.md
> $ git diff --name-only c7dae85b..8b894784 | grep -cE '\.(sh|py|ts|tsx|js|yml|yaml|json)$' → 0
> ```
> ⇒ 冻结面 `8b894784` 与 `c7dae85b` **代码零差异**（仅 bypass 日志 +1 行与证据件 +15/−6），故 A–H 判据结论对两者同时成立。
> **机器/负载**：macOS｜`bash 3.2`｜测时 `uptime` load avg 1.41（20:38）；重型测量全程**串行**，未并发跑其它重型验证。

---

## 〇、增量结论 @ `114e129e`（**取代** §一 的一轮结论；§一 保留为历史，不删）

**自验结论：`可提请独立审计`**（新锚 `114e129e`；K3 复审通过前不得合并）。

一轮的 3 项退回**已全部修复并推送**，我逐条独立复跑确认（原始输出见 §六）：
1. **退回项 1（E-M5 接线判别性）→ 已修，且我实测确具判别性**：新 tip 上两条接线断言绿（checker **21/0**、alloc **24/0**）；**只删 checker 的登记行** → checker **20/1**（**唯一红 = 接线断言**）、alloc 仍 **24/0**；**只删 alloc 的登记行** → alloc **23/1**（唯一红 = 接线断言）、checker 仍 **21/0**；两条全删 → 20/1 + 23/1；复原后 `ci.yml` sha256 `09ac0bc44ac7410d` **与 HEAD 版本逐字节一致**、工作树零变更、两测试回绿。
2. **退回项 2（冲突扫描计数）→ 证据件已订正为我的实测口径**：`180 → 186`（tracked `180 → 185`，即 +6），并写明「命令 + 排除项 + 截至时刻」与「本报告自身含该串 ⇒ 落盘 +1（186 → 187）」。
3. **退回项 3（「真实调用点 6」）→ 已删掉「6」，改为可复现口径**：测试 4 + 提示语 1 + skill 文档 6 = **11**（与我实测逐条一致）。
4. **numstat 口径 → 已订正**：权威口径 `+171 / −7`；`178` 已显式标注为**变动总行数**（增删合计），并留痕「初版误写 +178/−9」。
5. **A–D / F / H 结论对两锚同真**：`git diff --stat 8b894784..114e129e -- scripts/` **为空**，两脚本 sha256 前后同为 `b263a635e0db55f8` / `f601790b68917172` ⇒ 产品代码零变化，一轮实测的 A/B/C/D + 变异 M1–M4/M6 + F/H 全部继续成立（§二–§四）。

**仍开放（不作为退回项；按 lead 口径登记为残余风险）**：
- **alloc 夹具 §9 的人类可读断言仍是子串检查**（`assert_contains "$OUT" "docs/d942-cto-fixation"`）：在 `conflicts[]` 回整行形态（M6 变异）下**仍会侥幸通过**（整行文本"恰好含短名"），真正拦住 M6 的是 `--json` 元素断言。该文件属 D938 写集，本卡不改断言强度 → **残余风险，保留为开放项**。
- 矩阵外平台（ubuntu/windows）未实测；耗时绝对值不可跨环境对账；`WORKTREE_USED` O(n²)；骨架模板 `"spec": null` 生产者；D938→D940 合并顺序。详见 §四。

---

## 一、（历史 · 一轮 @ `8b894784`，已被 §〇 取代）自验结论：**`退回（附理由）`**

**退回只约束本件的 3 处（判据 E-M5 + G 两处计数声称），不否决修复本体**：A/B/C/D/F/H 与 E 的 M1–M4/M6 **全部独立复跑成立**，分配器与校验器的行为契约（跨位置拒绝、三态、降级、`--json` 契约）我逐条实测通过。

| 判据 | 结果 | 依据 |
|---|---|---|
| A 跨位置拒绝 + 拒绝不建壳 | ✅ 成立 | §三-A（rc=1、点名、无 D942 壳） |
| B 三元组校验器三态 + 4 坏输入 + `--json` 契约 | ✅ 成立 | §三-B（0/1/2 三态 + 契约串逐字） |
| C 降级显式且仍可用 | ✅ 成立 | §三-C（物理坏 origin rc=128 → `degraded:` + rc=0 + `degraded:true`） |
| D 等价性 + 耗时 | ✅ 等价性成立（sha256/字节数逐字节相同）｜耗时=**我自己的数**，与 c1 绝对值不同（§三-D） | |
| E 变异体 M1–M6 | ⚠️ **5 红 1 不红**：M1/M2/M3/M4/M6 改坏即红、复原即绿；**M5（删 ci.yml 登记行）无任何夹具变红** → **退回项 1** | §三-E |
| F 禁改核验 | ✅ 成立 | §三-F（11 文件，他人文件命中 0） |
| G 计数复核 | ⚠️ **5 项一致 / 2 项不符**：三测试 23-5-20 ✅、ci.yml +1(L254) ✅、模板 +45/−2 ✅、ls-remote 11 ✅、域内 5 ✅；**冲突扫描 181→185 不符（我实测 180→186）**、**真实调用点 6 不可复现** → **退回项 2、3** | §三-G |
| H 前提 4 处订正 | ✅ 4/4 成立 | §三-H |

### 退回项（逐条附理由与修法）

**退回项 1（判据 E-M5 · 接线判别性缺失）**：卡面要求「从 `ci.yml` 删登记行 → 「接线」断言必须红」。我按此改坏后，**D940 的三个夹具全绿**：
```
[M5-red] alloc-task-id.test.sh       结果: PASS=23 FAIL=0  rc=0
[M5-red] check-name-allocation.test.sh 结果: 20 通过, 0 失败  rc=0
复原后（同）: 23/0、20/0
```
`check-name-allocation.test.sh:40-45` 的「接线」断言只检**被测脚本文件存在**，不检 `ci.yml` 成员资格。全仓唯一察觉者是 `scripts/control-tower/check-canary-drift.sh`（打印 `⚠ 漂移: 73 个 .test.sh 不在 CI canary 清单`，其中含本测试），但该脚本**契约恒 `exit 0`**（其头注 L11 明写「恒 0（告警不阻断）」），CI 里又写作 `bash scripts/control-tower/check-canary-drift.sh || true`（`ci.yml:279`）→ **告警不是判别性防线**，且 73 项基线噪声中新增 1 项不可见。
**仓内先例（同一断言形态已存在于兄弟测试）**：
```
tests/control-tower/check-citations.test.sh:36:grep -q "check-citations" "$REPO/.github/workflows/ci.yml" \
tests/control-tower/check-citations.test.sh:37:  && ok "接线: CI canary 清单含本测试" || no "接线: CI canary 清单缺本测试"
tests/control-tower/check-progress-freshness.test.sh:44:grep -q 'check-progress-freshness.test.sh' "$REPO/.github/workflows/ci.yml" \
tests/control-tower/merge_writeset_gate.test.sh:36:grep -q 'merge_writeset_gate.py' "$REPO/.github/workflows/ci.yml" \
```
**修法（一行，属 c2 写集）**：在 `tests/control-tower/check-name-allocation.test.sh` 按上述同款加一条 `grep -qF 'tests/control-tower/check-name-allocation.test.sh' "$REPO/.github/workflows/ci.yml"` 断言。**注**：接线本身**真实存在**（`ci.yml:254` 在密封清单内，CI 循环 `exit $FAIL` 会真红）——缺的是「删掉即报红」的判别性夹具，非接线缺失。

**退回项 2（判据 G · 冲突扫描计数不符）**：
| 来源 | 开工 | 收尾 |
|---|---|---|
| c1 报告 §2.1 | 181（git 跟踪 180） | 185（git 跟踪 181） |
| **我实测** | **180（git 跟踪 180）**——`origin/main` 干净树 | **186（git 跟踪 185）**——`8b894784` 收尾树 |
命令与口径：`grep -rl "alloc-task-id" . --exclude-dir=.git --exclude-dir=node_modules | wc -l`（截至 `2026-09-24 20:38:47`，交付工作树）。delta 我逐文件归因（完整新增集，无文件消失）：
```
.claude/task-briefs/2026-09-24-D940-name-allocation-registry.md   (tracked)
.codex/control-tower/session-registry.json                        (untracked/ignored 运行态)
docs/synova/product-lines/evidence/D940-夹具原始输出.md            (tracked)
docs/synova/product-lines/evidence/D940-改动清单.md                (tracked)
memory/notes/implemented/2026-09-24-d940-cross-location-alloc.md   (tracked)
scripts/control-tower/check-name-allocation.sh                     (tracked)
```
⇒ tracked +5、含运行态 +6；c1 报的 +4 与我实测 +6 差 2，且两端各差 1。**修法**：证据件按「命令 + 排除项 + 工作树 + 时刻」订正为实测值（或注明其测量时刻的工作树与当时的未跟踪文件）。
**口径自洽说明（防下一次复核对不上）**：本报告自身含 `alloc-task-id` 字面量 → 落盘（`20:53:28`）后同命令计数 = **187**（= 186 + 本文件 1，我实测）。后续复核请把本文件计入。

**退回项 3（判据 G · 「真实调用点 6」口径不可复现）**：我按「路径被喂给运行器」逐条核，得到 **4 个测试文件**（`alloc-task-id.test.sh`、`alloc-task-id-lock.test.sh`、`ct-test-gate.test.sh`、`check-ownership.test.sh`）+ **1 处提示语**（`pre-dispatch-check.sh:30`）+ **6 份文档**（`.claude/`/`.dsh/` 下 3 个 skill × 2 目录），**没有任何口径得到 6**。c1 的枚举实际列了 11 个路径却写作「6 个文件」。**结论**：实质（可执行影响面极小、其余为记录/文档）成立；数字不成立。**修法**：改成可核的口径（如「喂入运行器的测试 4 个 + 提示语 1 处 + 文档 6 份」）。

---

## 二、A–H 逐条原始输出

### A 判据① 跨位置拒绝（我自己的命令，夹具 = c1 遗留 `/tmp/d940-fix`，bare origin、本地无 tracking ref、task-state max=D941）
```
夹具前置自核:
  bare heads: docs/d942-cto-fixation
  repo branch -r 计数（应 0 = 未 fetch）: 0
  repo ls-remote 可见 d942: 1
  task-state max: 941

$ SYNO_TASK_STATE_DIR=/tmp/d940-fix/repo/task-state SYNO_BRIEF_DIR=… bash alloc-task-id.sh "d940v-占号测试" --dry-run
  ⚠ alloc-task-id: origin/main 不可读——仅按本地 task-state 发号（可能漏号，建议先 git fetch）
  D942 (dry-run, 未建壳)
  ❌ 撞车: D942 已被占用 — 拒绝发号（fail-closed）
     冲突位置: remote-branch  docs/d942-cto-fixation (refs/heads/docs/d942-cto-fixation)
  rc=1
  点名 docs/d942-cto-fixation 命中数: 1
  拒绝后 task-state/D942.json 是否存在: 否
```
✅ **rc≠0 + 逐行点名 + 拒绝时不建壳**，三项全中。

### B 判据② 三元组校验器（三态 0/1/2 不混同）
```
[OK] b1 已占号 D942（远端未 fetch）        → rc=1  ❌ 冲突: D942 不可用 / 冲突位置: remote-branch:docs/d942-cto-fixation (refs/heads/docs/d942-cto-fixation)
[OK] b2 一致空闲 D99999                    → rc=0  ✅ D99999 可用（可判定范围内未见占用，且命名一致）
[OK] b3 命名不一致（D99999 vs worktree d941）→ rc=1  ❌ 冲突位置: naming:worktree D941 ≠ --id D99999（三元组不一致）
[OK] b4 坏输入: 无参                       → rc=2  check-name-allocation: 至少需要 --id / --worktree / --branch 之一
[OK] b5 坏输入: --id 非法                  → rc=2  check-name-allocation: 非法卡号 'NOTANID'（应形如 D942 或 942）
[OK] b6 坏输入: --id 缺值                  → rc=2  check-name-allocation: --id 缺值
[OK] b7 坏输入: 未知选项                   → rc=2  check-name-allocation: 未知选项 '--bogus'
```
**`--json` 契约（c2 抓到过的假绿面，我独立核）**：
```
$ check-name-allocation.sh --json --id D942 --worktree .synova-wt-squad-d942 --branch docs/d942-cto-fixation
{"id":"D942","status":"conflict","degraded":false,"conflicts":["remote-branch:docs/d942-cto-fixation"]}
rc=1
① 元素数（应 1）: 1        ② JSON 含 40 位 sha（应 0）: 0
③ 含重复 ref（refs/heads.*refs/heads，应 0）: 0
④ 契约子串 'remote-branch:docs/d942-cto-fixation'（应 ≥1）: 1
⑤ python3 解析: conflict ['remote-branch:docs/d942-cto-fixation']
```
✅ 元素**恰为 1 个**、无 sha、无重复、可解析 ⇒ c2 所述假绿面**已闭环**（且 M6 证明该断言真能抓住回归，见 §三-E）。

### C 判据③ 降级显式（**物理构造**坏 origin，非开关）
```
$ git -C /tmp/d940-fix/repo remote set-url origin /tmp/d940-fix/NONEXISTENT-origin.git
$ git ls-remote --heads origin → rc=128
--- alloc --dry-run ---
degraded: 远端分支不可达 (origin 未配置/网络不可达, rc=128) — 仅按可判定位置校验
⚠ alloc-task-id: origin/main 不可读——仅按本地 task-state 发号（可能漏号，建议先 git fetch）
D942 (dry-run, 未建壳)
alloc rc=0 ｜ degraded: 命中=1
--- checker ---
degraded: 远端分支不可达 (origin 未配置/网络不可达, rc=128) — 仅按可判定位置校验
✅ D99977 可用（可判定范围内未见占用，且命名一致）
   ⚠️ 远端不可达，本结论仅覆盖可判定位置（见上 degraded 行）
checker rc=0 ｜ degraded: 命中=1
--- checker --json ---
{"id":"D99977","status":"free","degraded":true,"conflicts":[]}   degraded:true 命中=1
```
✅ `degraded:` 逐字可见 + **仍可用**（rc=0）+ 结论显式限定 + `--json degraded:true`；夹具 origin 已复原。

### D 授权前提修复（`basename` → `"${f##*/}"`）的等价性与耗时

**等价性（方法：从两个真实版本按起/止标记抽取 worktree 扫描块原文，包进同一 harness 跑同一输入；不手抄）**：
```
抽取块行数: 改前=29  改后=34   ← 差 5 行 = 新增说明性注释
块内差异（非注释）:  <  bn=$(basename "$f")   >  bn="${f##*/}"      ← 唯一语义差异（逐行核）
两版整体 diff 行数: 178（= 171 增 + 7 删，见 §三-G）

改前(basename) rc=0 耗时=189s   20:42:50
改后(param)    rc=0 耗时=93s    20:44:23
输出规模: 206008 字节 / 51502 行（两侧相同）
sha256: 28dc3e0177450fd57f16dae87376ab570c516d74f2c83e752c3ae43bda54d986  out-base.txt
        28dc3e0177450fd57f16dae87376ab570c516d74f2c83e752c3ae43bda54d986  out-cur.txt
diff: 逐字节一致（0 差异）
```
**端到端同命令（`alloc-task-id.sh "d940v-e2e-probe" --dry-run`，同 worktree、同输入；worktree 数 202）**：
```
基线脚本 sha256(前16)=6168ed10bc6f2df3  （与 c1 报告同值）
改后脚本 sha256(前16)=b263a635e0db55f8  （与 c1 报告同值）
改前: D949 (dry-run, 未建壳)  rc=0  耗时=180s
改后: D949 (dry-run, 未建壳)  rc=0  耗时=101s
```
**我的数 vs c1 的数（必须标明不可跨环境对账）**：

| 项 | c1 报 | 我实测 | 说明 |
|---|---|---|---|
| 隔离循环 basename | 238s | **189s** | 输入规模不同（c1 50,788 文件；我 51,502 = 其 +714，来自我新建的 2 个 worktree）+ 机器负载不同 |
| 隔离循环 param | 105s | **93s** | 同上 |
| 比值 | ×2.27 | **×2.03** | 同向、同量级 |
| 端到端 | 200s → 107s | **180s → 101s** | 同命令、同 worktree；比值 ×1.87 vs ×1.78 |
| 等价性 | 203,152 字节 / 50,788 行 / sha256 相同 | **206,008 字节 / 51,502 行 / sha256 相同** | **逐字节一致，0 差异**（两侧同输入⇒同规模） |

⇒ **等价性（唯一实质判据）成立且可逐字节复核**；耗时绝对值**依环境而变，两者不可对齐**（c1 的 238/105/200/107 与我的 189/93/180/101 各自成立，不能互证）。据卡面「若与它不符→记退回」，此项按**口径差异**登记（非不实声称）：c1 已在其 §3.1 列口径对照表，我的测量与之一致地落在「改后 ≈ 改前 1/2」的结论上。

### E 变异体矩阵（我在 `/tmp/d940-v-mut` 自行改坏，锚点 `assert count==1`，每轮 `git checkout --` 复原）

| # | 变异内容 | 施加后 diff | **改坏后（红）** | 复原后（绿） |
|---|---|---|---|---|
| M1 | 丢弃 `ls-remote` 权威快照（`REMOTE_BRANCH_REFS=""`） | 1 行 | alloc **19/4 rc=1**、checker **16/4 rc=1** | 23/0、20/0 |
| M2 | 决策点回到只查本 `task-state` | 1 行 | alloc **18/5 rc=1**（§9 + §10） | 23/0 |
| M3 | 冲突改为放行（fail-open） | 1 增 4 删 | alloc **18/5 rc=1**（§9 + §10） | 23/0 |
| M4 | 吞掉 `degraded:` 输出 | 1 行 | checker **19/1 rc=1** | 20/0 |
| **M5** | **从 `ci.yml` 删登记行** | 1 删 | **无任何夹具变红（23/0、20/0）** ← 见 §一 退回项 1 | 23/0、20/0 |
| M6 | `conflicts[]` 回整行形态（revert `awk '{print $2}'`） | 1 增 2 删 | checker **19/1 rc=1**；alloc **23/0（不变红）** | 23/0、20/0 |

**M1 红的具体断言**：
```
❌ 未 fetch 的远端分支占用 → 拒绝（fail-closed，不发放） — 期望 exit≠0，实际 0
❌ 点名冲突位置（远端分支全 ref 原文） — 未找到: docs/d942-cto-fixation
❌ 拒绝时不建壳 — 不应出现: 已登记:      ｜ ❌ 拒绝却建了 D942 壳
checker: ❌ 应 rc=1，实际 rc=0 ｜ ❌ --json 冲突形态不符：{"id":"D942","status":"free",…,"conflicts":[]} ｜ ❌ 金丝雀失败
```
**M2/M3 红的具体断言**：§9 四条 + `❌ worktree 目录名已占 942，却仍发放 D942（漏号）`。
**M4 红的具体断言**：`❌ 缺 degraded: 前缀（静默降级）`。
**M6 红的具体断言（新发现，见 §四-②）**：
```
❌ --json 冲突形态不符，rc=1：{"id":"D942","status":"conflict","degraded":false,
    "conflicts":["remote-branch:89820c0ee460df59893c05a7403422d4fe56ee8e	refs/heads/docs/d942-cto-fixation"]}
alloc 夹具同变异下 23/0（人类可读输出仍含短名 → 不报红）
```

### F 禁改核验
```
$ git diff --name-only origin/main...HEAD        # 完整 11 项
.claude/bypass.log
.claude/task-briefs/2026-09-24-D940-name-allocation-registry.md
.github/workflows/ci.yml
docs/synova/coordination/派单模板.md
docs/synova/product-lines/evidence/D940-夹具原始输出.md
docs/synova/product-lines/evidence/D940-改动清单.md
memory/notes/implemented/2026-09-24-d940-cross-location-alloc.md
scripts/control-tower/alloc-task-id.sh
scripts/control-tower/check-name-allocation.sh
tests/control-tower/alloc-task-id.test.sh
tests/control-tower/check-name-allocation.test.sh

$ git diff --name-only origin/main...HEAD | grep -cE 'scan-fullwidth-vars|scripts/pre-commit-check\.sh|gen-cto-health\.py|^task-state/D9[23]'
0
```
✅ 四个他人文件族**零命中**；`scripts/audit/**` 零命中。

### G 计数复核

| 声称 | 我的复跑 | 判定 |
|---|---|---|
| 三测试 `23/0`、`5/0`、`20/0` | `check-name-allocation.test.sh` **20 通过 0 失败** rc=0 (2s)｜`alloc-task-id.test.sh` **PASS=23 FAIL=0** rc=0 (2s)｜`alloc-task-id-lock.test.sh` **5 通过 0 失败** rc=0 (**108s**) | ✅ 一致 |
| `ci.yml` 增量 +1 行（L254） | `git diff --numstat` = `1 0 .github/workflows/ci.yml`；`grep -n` → `254: tests/control-tower/check-name-allocation.test.sh \` | ✅ 一致 |
| 模板 `+45/−2` | `git diff --numstat -- 派单模板.md` = `45 2` | ✅ 一致 |
| `ls-remote` 出现 11 处 | `grep -c 'ls-remote' scripts/control-tower/alloc-task-id.sh` = **11** | ✅ 一致 |
| 域内文件 5 | `git show --stat 8ff32573` = **5 files changed, 701 insertions(+), 2 deletions(-)** | ✅ 一致（按该提交口径） |
| 冲突扫描 `181→185` | **180 → 186**（tracked 180 → 185） | ❌ **不符（退回项 2）** |
| 真实调用点 6 | 4 测试文件 + 1 提示语 + 6 文档（无口径得 6） | ❌ **不符（退回项 3）** |

补充：`alloc-task-id.sh` 的 stat 数为 **178**（= 171 增 + 7 删；`git diff --stat` 显示的 178 是增删合计，非新增数）——c1 表格写作「+178/-9」为口径混用，权威值见 `--numstat`。

### H 前提 4 处订正复核（4/4 成立）
1. **`:132` 行号错** ✅：`git show origin/main:… | sed -n '128,137p'` 第 5 行（= L132）是 `# CT-63: 远端分支名 D# 扫描——…` 注释；真实扫描为 `WORKTREE_USED=""`(L101) → `for f in "$wt"/task-state/D*.json`(L112) → 收于 L129。
2. **`branch -r` 条件性缺口** ✅：真仓 `git branch -r | grep -icE 'd94[0-9]'` = **8**（含 `origin/docs/d942-cto-fixation`），`ls-remote` = **10** ⇒ 当前能看见，缺口仅在 ref 陈旧时成立 ⇒ 不能依赖本机 fetch 状态。
3. **`.synova-wt-squad-b5` 样本已失效** ✅：`git worktree list` 显示它为**已注册 worktree**（`9026e814 [chore/b5-task-state-backfill]`），非"无 owner 孤儿目录"。
4. **引用数 181≠55** ✅：我实测收尾树 **186**、`origin/main` 干净树 **180** —— 均 ≠ 55。

---

## 三、独立新发现（我跑出来、c1/c2 报告未载）

**① `--json` 断言是唯一判别面；人类可读输出对 M6 变体不报红。** M6（`conflicts[]` 回整行）下 checker 夹具 19/1 红，而 **alloc 夹具仍 23/0** —— 因为 `assert_contains "$OUT" "docs/d942-cto-fixation"` 在整行形态（`remote-branch  <sha>\trefs/heads/docs/d942-cto-fixation (…)`）下**恰好含短名**而通过。这正是 c2 §9.3 描述的"侥幸通过"面，**在人类可读断言上仍然存在**（未修复），只有 `--json` 那个元素断言挡住了。建议：把 human-readable 断言也收紧为契约形态（`remote-branch:<短名>` 或断言不含 40 位 sha）。

**② `ci.yml` 接线无判别性夹具（= 退回项 1）**，且仓内已有同款先例可照抄（`check-citations.test.sh:36`）。

**③ 分配器 `--dry-run` 现在也会因跨位置冲突 `exit 1`**（`alloc-task-id.sh:307-315`）—— 即"只预览"也会被拒绝。这是卡面语义的合理延伸（绝不发放），但与旧行为（dry-run 恒 rc=0）不同；现有 23 条断言覆盖了该形态（§9 即 dry-run 用例之一）。**登记备知**，非缺陷。

---

## 四、未清项 / 未验证面（诚实披露，非「通过」声明）

1. **矩阵外平台未实测**：只在本机 macOS（BSD grep/BWK awk、bash 3.2）实跑；CI 跑 ubuntu + windows，**未在本地实跑**。夹具已做密封设计（`mktemp -d` + 本地 bare origin、不连真网）。
2. **耗时绝对值不可跨环境对账**（§二-D）：我的 189/93/180/101 与 c1 的 238/105/200/107 各自成立；输入规模亦不同（我 +714 文件）。任何"238s"作为**可复现数字**引用时应附该输入口径。
3. **`alloc-task-id.sh` 仍缺 D313 M5 UTF-8 头块**（存量，非本卡引入；我核基线头 3 行同样为 0 命中，新建的 `check-name-allocation.sh` 有头块）——与 c1 N7 一致。
4. **`WORKTREE_USED` 字符串累计展开 O(n²)** 未动（c1 N6）——是"改后仍 ~93–101s"的剩余主因；建议另立卡。
5. **骨架模板硬编码 `"spec": null`**（c1 N5）**已复核**：当前树 `task-state/D*.json` 中 `"spec": null` **158** 份、`"spec": {` **80** 份、`"spec": "` **118** 份，模板行在改后 `:337` / 基线 `:175` —— 与 c1 数字逐条一致；生产者仍在产出 NoneType，建议另立卡。
6. **分支 `[behind 0 / ahead 10]`**（`origin/main = 9f0c8f65`，非栈式）；若 D938-T2 先合，本卡需 rebase（c2 §9.4 已声明合并顺序 `D938 → D940`）——**该跨卡顺序由队长/CTO 处置**。

---

## 五、红线遵守 + 收尾

- **只读交付工作树**：唯一写入 = 本文件；所有"改坏源码"只在 `/tmp/d940-v-mut`，每次施加后用 `git diff --stat` 确认改动面、跑完 `git checkout --` 复原并复跑证明绿（M1–M6 共 12 次测试跑，无残留变异）。
- 未使用 `--no-verify` / `git stash` / force push；未触碰 `scripts/audit/**`；未写审计标准；未自我审计（本报告不含"通过"结论）。
- `/tmp` 脚手架清理：`/tmp/d940-v-mut`、`/tmp/d940-v-eq`、`/tmp/d940-v-base` 已 `git worktree remove`。
- 交付工作树收尾：`git status --short` 仅剩本文件（最终回执见交付消息）。
- 原始输出留档（仓库外）：`/tmp/d940-v-D/`、`/tmp/d940-v-tests/`、`/tmp/d940-v-mut-logs/`、`/tmp/d940-v-{all,trk2,base,now}.txt`。

---

## 六、增量复验 @ `114e129e`（delta；原始输出，我自己跑）

### 6.1 冻结坐标（用 `--is-ancestor` 判「已在远端」，不用"比 tip 相等"）
```
$ git fetch --all -q
$ git rev-parse HEAD                 → 114e129e7ae2d9888d3c9a719391de8c39a756c1
$ git ls-remote --heads origin | grep fix/d940-name-allocation
114e129e7ae2d9888d3c9a719391de8c39a756c1	refs/heads/fix/d940-name-allocation

$ git merge-base --is-ancestor 8b894784 114e129e → ✅（一轮锚已是 tip 祖先）
$ git merge-base --is-ancestor 8c0f4832 114e129e → ✅（c1 计数返工已在远端）
$ git merge-base --is-ancestor c50dbeed 114e129e → ✅（c2 E-M5 返修已在远端）
$ git merge-base --is-ancestor c50dbeed origin/fix/d940-name-allocation → ✅
```

### 6.2 delta 面（`8b894784..114e129e`，完整 name-only）
```
.claude/bypass.log                                    (+2/−0，hook 登记)
docs/synova/product-lines/evidence/D940-夹具原始输出.md  (+72/−3)
docs/synova/product-lines/evidence/D940-改动清单.md      (+36/−10)
tests/control-tower/alloc-task-id.test.sh             (+9/−0)
tests/control-tower/check-name-allocation.test.sh      (+10/−1)
```
⇒ 实测为 **2 个测试文件 + 2 个证据件 + 1 个 hook 日志**（lead 口径写「3 个测试文件」，我核为 **2 个**，其余一致）；
**`scripts/` 零变化**（`git diff --stat 8b894784..114e129e -- scripts/` 为空；两脚本 sha256 前后同为 `b263a635e0db55f8` / `f601790b68917172`），`.github/workflows/ci.yml` 亦零变化（登记行仍 252/253/254）。
`git show --stat c50dbeed` = `3 files changed, 91 insertions(+), 4 deletions(-)`（= alloc 测试 9 + checker 测试 10 + 夹具原始输出 72 增；与 c2 报的「3 文件 +91/−4」一致）。

### 6.3 退回项 1 复核：E-M5 接线判别性（在副本 `/tmp/d940-v-mut2` @ `114e129e` 自行改坏；交付工作树零触碰）
```
前置: ci.yml sha256(前16)=09ac0bc44ac7410d ｜ 与 HEAD 版本一致: 是
      登记行: 252 alloc-task-id.test.sh ／ 254 check-name-allocation.test.sh

阶段① 基线（当前树）
  [base-checker] 结果: 21 通过, 0 失败  rc=0
  [base-alloc]   结果: PASS=24 FAIL=0  rc=0
  ✅ 接线: 本测试在 ci.yml control-tower-tests 密封清单（删掉该行即红）   ← 两条新断言均为 ✅

阶段② 只删 checker 的登记行（行 254）
  [m5a-checker] 结果: 20 通过, 1 失败  rc=1
        ❌ 接线: 本测试不在 ci.yml 密封清单（CI 不跑 = 摆设，M3 未接线）   ← 唯一红
  [m5a-alloc]   结果: PASS=24 FAIL=0  rc=0                                ← 不受影响

阶段③ 只删 alloc 的登记行（行 252）
  [m5b-alloc]   结果: PASS=23 FAIL=1  rc=1
        ❌ 接线: 本测试不在 ci.yml 密封清单（CI 不跑 = 摆设，M3 未接线）   ← 唯一红
  [m5b-checker] 结果: 21 通过, 0 失败  rc=0                                ← 不受影响

阶段④ 两行全删
  [m5c-checker] 结果: 20 通过, 1 失败  rc=1      [m5c-alloc] 结果: PASS=23 FAIL=1  rc=1

阶段⑤ 复原
  ci.yml sha256(前16)=09ac0bc44ac7410d  与阶段①相同: 是   （== HEAD 版本，逐字节一致）
  工作树变更: (空)
  [fin-checker] 结果: 21 通过, 0 失败  rc=0       [fin-alloc] 结果: PASS=24 FAIL=0  rc=0
```
**判别性判定**：断言**逐条钉住自己的登记行**（②③ 各只红一条且只影响自身），不是"顺带红了"；**改坏即红、复原即绿**、`ci.yml` sha256 与 HEAD 逐字节一致 ⇒ **退回项 1 解除**。

### 6.4 退回项 2/3 + numstat 口径复核（证据件原文，行号为 `114e129e` 版本）
```
D940-改动清单.md:74  **权威结论：`180 → 186`（tracked `180 → 185`），即 +6。**
D940-改动清单.md:75  ⚠️ …**本报告自身含 `alloc-task-id` 串，落盘即把该计数 +1**…**凡引用此数必须带「命令 + 排除项 + 截至时刻」**
D940-改动清单.md:77  - **真实调用点…返工订正：不存在"6"这个读法**。实测枚举 **11 条路径**…
D940-改动清单.md:78-80  测试 = 4 ／ 提示语 = 1 ／ skill 文档 = 6
D940-改动清单.md:81  → **合计 4 + 1 + 6 = 11；无任一读法等于 6。**
D940-改动清单.md:14  改（**权威 numstat `+171 / −7`**）
D940-改动清单.md:25  ⚠️ **口径订正（返工项）**：`git diff --stat` 的「178」是**变动总行数**（= 171 增 + 7 删）…初版误写「+178/−9」
```
**我的独立复测（新 tip）**：`grep -rl "alloc-task-id" . --exclude-dir=.git --exclude-dir=node_modules | wc -l` = **187**（186 + 本自验报告 1，自洽）；`git grep -l "alloc-task-id" -- . | wc -l` = **185**（tracked）⇒ 与订正后口径一致。

### 6.5 抽验未回归（新 tip，三个测试全跑）
```
[check-name-allocation] 结果: 21 通过, 0 失败   rc=0（2s；一轮锚上为 20/0，本 tip 新增第 21 条 = 新接线断言）
[alloc-task-id]         PASS=24 FAIL=0        rc=0（2s；一轮锚上为 23/0，本 tip 新增第 24 条 = 新接线断言）
[alloc-task-id-lock]    结果: 5 通过, 0 失败 / Status: ✅   rc=0 耗时=107s
```

---

**声明**：本文件是**独立自验结论**。**当前坐标 `114e129e` 的结论 = `可提请独立审计`**（见 §〇）；§一–§五 为锚 `8b894784` 的一轮历史结论（`退回（附理由）`），保留不删以留痕修订链。本文件不是审计报告，也不含"通过"判定；是否通过/是否合并归 CTO 收件闸 + K3 终审。

