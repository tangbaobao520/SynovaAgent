# D962 task-26 回执（并入 2b + 项3 A 档 + 2a② + 项2/项4）

- **分支**：`feat/d962-2a-merge` @ **68747574**（远端 = 本地，`git ls-remote` 回执见 §推送）
  - **入档前快照**：`e74aae55`（当时写集 88 = 24A+26D+38M）；随后按 V2 自验 F1/F2/F3 收口 + 8 件证据入库 → 末态 `68747574`（写集 **89 = 25A+26D+38M**）
- **工作树**：`.synova-wt-d962c`（本任务自建；主树只读未碰）
- **写集**：**89 文件 = 25A+26D+38M**（brief Q2 ⟷ `task-state/D962.json#write_set` 双源一致；方向A 夹带=0 / 方向B 多余=0）
- **判定**：待 CTO 收件闸 + K3 终审（**本人只给自验结论，不自称"通过"**）
- **真 CI**：队列积压（`36168743118`/`36168749183`/`36170408913`/`36170416178` 全 `queued`）⇒ 本文 ⒞ 均为**本地 CI 等价复跑**，真 run **待补**

## 一、项1：2b 并入 #803（CTO 裁决 A）

| 步骤 | 命令原始输出摘要 | 产物 |
|---|---|---|
| 2b 再同步 main | `git merge origin/main`（机制 a，无 force）→ 175/176 冲突 3 件（D956 brief / task-state / pre-commit），取 main 权威版 | `fix/d962-2b-wiring-merge` @ `176d2e06` |
| 并入 #803 | `git checkout -b fix/d962-803-final 80ce4ec6 && git merge 176d2e06` → 冲突 3 件解 → `8602493d` | `fix/d962-803-final` |
| 关键冲突判定 | `scripts/pre-commit-check.sh` 取 2b 版：`shasum -a 256` = `b0e6c1c6…`，与 `176d2e06:<path>` **逐字节相同**；2a 独有 22 行（`diff | grep '^<'`）全是被 FIX-015 取代的机制 | — |
| 推送 | `25e1b414`（首推）→ `e74aae55` → **`68747574`**（末推）。**口径（F4 修订）**：本机 `git reflog`/`git ls-remote` **可观测的末次发布为 fast-forward**（未用 force）——**不得**据此声称"全部历史推送均为 FF"（不可观测） | 远端 `feat/d962-2a-merge` |

### FIX-013 活点三件套（在并入后的树上复跑）
- **⒜** `.github/workflows/ci.yml:111 → job 'iron-laws'`（无 `needs`、跑 `scripts/pre-commit-check.sh` + `SYNO_CI=1` + `SYNO_DIFF_BASE=origin/main`）
- **⒝** 真文件临时删 job → `4 通过, 5 失败` rc=1（`❌ 活点缺失` / `❌ 未执行` / `❌ 未注入 SYNO_CI` / `❌ 变异体行号对照失败`）；`git checkout --` 恢复后 sha256 = `7d779fae…`（前后一致）、复跑 `9 通过, 0 失败`
- **⒞** 本机 CI 等价：live-point `9/9`、selfdiff `7/7`、grep-oP `24/24`、`GITHUB_ACTIONS=true SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh` rc=0

## 二、项1附：合并期冲突 **6 件**（标记 4 件 + 内容 2 件）——「已发布但坏」4 件

全树扫描 `git grep -n -E '^(<<<<<<< |>>>>>>> )' HEAD -- .` → **修复前 4 件**（冲突**标记**形态）、**修复后 0 件**（HEAD 全树 0，不变）。

另 **2 件内容冲突**（add/add，无标记形态 ⇒ 不在标记扫描命中内）：
`.claude/task-briefs/2026-09-25-D956-ci-failmsg.md`、`task-state/D956.json`
—— 随 main 再同步以 **main 权威版**解决；`git diff --name-status origin/main..HEAD -- <二者>` **无输出**（已不在本 PR 变更集）。
⇒ 合并期冲突合计 **6 件**；其中「已发布但坏」（标记已提交进 `80ce4ec6`）为 **4 件**。

| 文件 | main | 80ce4ec6 | 2b | 处置 |
|---|---|---|---|---|
| `scripts/workflow/loop-score.sh` | 0 | 2 | 0 | 取 2b（1 行注释） |
| `tests/control-tower/g10-cp3.test.sh` | 0 | 4 | 0 | 取 2b（V5.3 侧内容，语句集合 0 多 0 少） |
| `tests/control-tower/g9-contract.test.sh` | 0 | 4 | 0 | 同上 |
| `tests/control-tower/grep-oP-regression.test.sh` | 0 | 4 | 0 | 取 2b（含 4 条指向已退役文件/已消失模式的条目移除，逐条用 `eq()` 接线守卫实测必失败；保留 3 条真身条目守卫全 PASS） |

四件替换后 `shasum -a 256` 与 `176d2e06:<path>` **逐字节一致**；`bash -n` 全通过。

## 三、项3 A 档四点（接线断言改指真实活点）

| 点 | ⒜ 活点 | ⒝ 无它则红（原始输出摘要） | ⒞ 有它则绿 |
|---|---|---|---|
| ① doc-registry W1/W2 | `pre-commit-check.sh` CI 权威区 D782 块（D1 `:480-519`） | 删整块 → `❌ W1 接线: registry-gate 在 pre-commit (期望 0 实际 1)` + `❌ W2 …`，`7 通过 / 2 失败` rc=1 | `9 通过 / 0 失败`；CI 等价 `✅ D1 …(12 ✅)` + `✅ D2 检查 0 个文档，0 个未登记` |
| ② g12-day-window | CI 权威区 G12 三日窗（`timedelta(days=k) for k in (-1,0,1)` + `glob('.claude/task-briefs/%s-*.md')`） | `(-1,0,1)→(-1,1)`：`❌ 活点② 行为 … 实得[09-25, 09-27]` + `❌ 活点③ 能力等价失败` rc=1 | `14 通过, 0 失败`（含**能力等价**断言：新 glob 三日 == 旧三日正则） |
| ③ gate-failopen T3/T4/T5b | 组 7a 三态降级块（`_DIAG_RC≥2` ⇒ 显式降级）+ `_DIAG_EXCL` 行首锚 + `code_added_lines` docs 收窄 | `rc 强制 0`（静默吞错）：`❌ T3 …仍判 ✅（fail-open 未修）` / `❌ 缺显式「降级」字样` / `❌ CI strict … 实际 0` → `17/3` | `20/0` rc=0 |
| ④ scan-fullwidth | 扫描器写集模式（真因：夹具指向**已退役** `check-sentinel-type-net.sh` ⇒ fail-closed rc=2，非标签漂移） | 夹具改回退役脚本 → `❌ … fail-closed (rc=2): 路径不存在: scripts/control-tower/check-sentinel-type-net.sh`，`PASS=30 FAIL=2` | `PASS=32 FAIL=0`；阈值不变（覆盖 9 文件 / 写集残留 0-0 / mac 6-8 / win 10） |

### 跨修订证据表（4 处标记 + 8 点活点状态）
- 8 点活点状态：**全部零生产调用点**（`grep -rn` 逐条实测）⇒ 红断言是**真警报**；A 档④ 中 4 点已收敛为「有真实活点」的断言，另 3 点（ct-test-gate / platform-checklist / claim-regex）待 CTO 裁 wire/retire。
- 计划依据：`docs/synova/coordination/D962-phase2-plan.md`（完整版 §〇-§八 已并回本分支，逐字取自 `origin/docs/d962-disposition@120f65ea`，来源 sha256 前16 `d21113c10000a043`）。

## 四、项5(a)：4 处关键路径 `|| true` 清零（commit `8792450e`）

| 站点 | 注入失败 | 修前（静默） | 修后（显式） |
|---|---|---|---|
| CT-34 纯文档早退 | `DOC_PREFIX_RE='a(b'`（grep rc=2） | `纯文档提交 (CT-34): 豁免 — 仅 Secrets + Notes` rc=0（整链绕过） | `⚠️ 纯文档早退判定降级 — grep rc=2；按非纯文档继续（fail-closed）` |
| diff 采集族 | `GIT_INDEX_FILE=<garbage>`（`git diff --cached` rc=128） | `✅ 本地清单通过` rc=0（判定面看到"无变更"） | `❌ diff 采集降级: git 返回非 0 — 判定面无输入（fail-closed）` + `❌ 1 组未通过 — 提交已拒绝` rc=1 |
| G10 criteria-map | python 强制 `exit 3` | 无任何降级提示（`|| true` 吞掉） | `⚠️ G10: criteria-map 解析降级（python3 rc=3，不当作无映射）` |
| G12 写集判定 | python 抛异常（rc≠0） | `✅ G12: 所有文件均在 Q2 范围内`（**假绿**） | `❌ G12: 写集判定执行失败（python3 rc=1）— fail-closed，不当作范围全通过 [CI strict]` |

复跑：g10-cp3 / g9-contract / g12-day-window / precommit-groups-injection / hard-gate-convergence / ci-strict-mode / ci-strict-visible / gate-failopen-net / iron-laws-selfdiff / iron-laws-live-point / doc-registry-gate / scan-fullwidth-vars 全 PASS；CI 口径 rc=0。

### 2a② 附：F1（V2 自验必修）——内联 python 注释里的反引号触发 bash 命令替换报错
`pre-commit-check.sh` G12 内联 python 的异常分支注释写成 `` 靠 bash `|| true` ``，反引号落在**双引号命令替换内** ⇒
`SYNO_CI=1` 每跑 G12 打 2 行 stderr。修法：去反引号（改「外层无 try + bash 吞错」）。
```
修前: GITHUB_ACTIONS=true SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh
      rc=0 ｜ stderr 行数=2
      scripts/pre-commit-check.sh: command substitution: line 462: syntax error near unexpected token `||'
      scripts/pre-commit-check.sh: command substitution: line 462: `|| true'
修后: rc=0 ｜ stderr 行数=0（原文为空）｜ ✅ G12: 所有文件均在 Q2 范围内 ｜ ✅ 本地清单通过
```

## 五、项2：D1008 卡入库（独立分支，未掺 #803）

- 分支 `chore/d1008-ratchet-card` @ **`1837773d`**（`git ls-remote --heads origin | grep d1008` 已回执）
- 内容逐字节取自 `.synova-wt-d956b`（`shasum -a 256` 前后一致）：`task-state/D1008.json` + `.claude/task-briefs/2026-09-26-D1008-Vitest-shard2-既有红登记.md`
- 号源说明：原 D1008 已被 `ab12d4d4` 改号 D969；本卡为 `alloc-task-id` 于 17:02Z **重新分配**的 D1008（非撞号）

## 六、项4：PR #803 正文六项模板（API 已同步，5569 bytes）

正文要点：写集 88；判据 12 条；改坏即红 4 则；**红线段分列**：
- **本 PR 引入的红**（main 基线对照 = main 绿/本分支红）：`ct-test-gate` / `platform-checklist` / `claim-regex-narrow`（真丢失）/ `check-k3-report`（夹具行号越界，已修）/ `simulate-ci`（级联）
- **既存红**：Vitest 2/2 @main（卡 D1008）
- **真 run 待补**标注 + 合规声明（未 force / 未 `--no-verify`；D331 按 D451 补记路径闭合）

## 七、自检 5 问
1. **接线**：4 处 `|| true` 均已三态化并被注入反例证明（§四）；D782 两脚本有 CI 权威区调用点（W1/W2 转绿）。
2. **异常处理**：4 处降级均写 `degraded-events.log` + 显式输出（铁律 11）；G12/G10/DOC_ONLY/diff 采集四路失败不再静默。
3. **类型安全**：本任务未新增 TS 代码（改动集中在 shell/测试/治理文档）；`as any` 零新增。
4. **测试质量**：所有改动点均配 ⒝ 变异反例（删/改坏 ⇒ 必红）并留原始输出；无 `|| true` 式变绿、无断言删除（grep-oP 4 条移除均以 `eq()` 守卫实测其不可满足为前提）。
5. **残留清理**：冲突标记全树 0；`|| true` 关键路径 4 处清零；本回执与 Note 均已入库。

## 八、遗留清单（未清）

| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| L1 | B 档三点 wire/retire：`ct-test-gate`、`platform-checklist`、`claim-regex`（真丢失，建议接回 CI 权威区） | CTO | 待裁 |
| L2 | 可退役清单：`24 − 2 retire（check-hardcoded.sh / control-tower/check-canary-drift.sh）+ −2 待裁（check-name-allocation.sh / check-preset-bundles.sh）= 20`；另 4 件按 plan 属 wire（file-driven / gitlinks / k3-report / pr-budget） | CTO 批后执行 | 待裁 |
| L3 | 真 CI 队列积压 ⇒ 本 PR 与 main 两侧 ⒞ 待补（**不得以本地等价冒充实跑**） | 环境 | 未清 |
| L4 | D2 登记门禁在 CI checkout 下输入集为空（空转）——脚本注释已如实登记；要真判需给 `doc-registry-gate.sh` 加 PR-diff 输入缝 | CTO | 登记 |
| L5 | `check-k3-report.test.sh` 夹具行号已改指在库行（同 A4 族）；同类"夹具随重写失效"面未做全仓扫描 | 本卡/后续 | 部分清 |
| L6 | 小瑕疵：`doc-registry-gate.test.sh` 首行 BOM；`loop-score.test.sh` 内层 `ROOT: unbound variable` stderr 噪音 | 后续 | 登记 |
| L7 | D721（main push 存量红放行分支不可达）独立 FIX 条目；`D964 rebase`（等本 PR 合入） | 控制塔线 | 登记 |
| L8 | **ARM 注入缝 `102/104/105` 的 `|| true`**（`GIT_CACHED_*` 环境变量回退路径）：属 D390 **设计内测试缝**，仅 `SYNO_TEST_ARM=1` 时进入；**生产路径（未武装）不受影响**——本卡不清，**如实登记**（清它需重设计注入缝语义） | 本卡/后续 | 登记（不修） |
| L9 | **G10 降级后仍打印 ✅ 跳过行**：`criteria-map 解析降级` 已显式报出，但同一块随后仍打印 `✅ G10: 无 task brief 变更或无映射(跳过)` ⇒ 建议改「**降级即止**」（降级分支输出后不再打印跳过/通过行），免得读者以为该面已判定 | 后续（控制塔线） | 登记 |
| L10 | **证据交付路径系统性缺陷（本轮修复）**：`docs/synova/coordination/evidence/` 被 `.gitignore:76` 忽略、**0 文件 tracked** ⇒ D962 全部自验记录从未入库（M6 违规）。本轮已把 8 件 `D962-*` 迁入豁免跟踪路径 `docs/synova/product-lines/evidence/`（同名、逐字节 sha256 一致）；**建议把"证据目录必须在 track 路径"写成门禁**（防复发） | CTO（改规则） | 本轮已迁，规则待立 |
| L11 | **迁入的证据文件含"门禁夹具假 key"，暂存即判红**：`docs/synova/product-lines/evidence/D962-2a2-selfverify.md:35` 记录 `sk-Abcdefghijklmnopqrstuv`（原文为该卡 secrets 门禁的**假** key 示例）。实测：**暂存该文件** → `❌ 硬编码凭证: 1 处` + 点名该行（rc=1，本轮迁移提交那一刻即因此判红）；**未暂存/工作区态** 3 次复跑均 rc=0（`check-secrets.sh` 工作区扫描的 `--include` 不含 `*.md`，.md 只在"暂存文件扫描"段落被查）。影响：CI（不暂存）不受影响；但**今后任何改动并暂存该文件的提交都会撞硬红**。**建议二选一（我不自行改历史证据）**：① 脱敏该夹具串 + 就地加迁移说明；② 给 `docs/synova/product-lines/evidence/**` 加显式 secrets 豁免规则（须写理由） | CTO | 登记（待裁） |

## 九、事故教训（队长要求登记）
**「推送/发布前未做全树冲突标记扫描」**：`80ce4ec6` 把 4 件未解冲突标记一并提交，而现有 13 组 pre-commit 与 CI lane **无一形态可抓**（只能等 bash 解析失败）。建议把
`git grep -n -E '^(<<<<<<< |>>>>>>> )'` 列为推送前置必跑项（已写入 task-26 决策 Note）。
