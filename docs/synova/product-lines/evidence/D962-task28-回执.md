# D962 task-28 回执（CTO 8 项裁定执行：3 退役 / 4 接线 / L11 / 计数 / N12）

- **分支**：`feat/d962-2a-merge`（推送 sha 见 §推送；本回执入档时 base = `933bf1de`）
- **工作树**：`.synova-wt-d962c`（自建；主树只读未碰）
- **判定**：待 CTO 收件闸 + K3 终审（**只给自验结论，不自称"通过"**）
- **真 CI**：**未跑（外部队列停摆）** — 本卡所有 ⒞ 均为本地 CI 等价复跑，**真 run 待补**（见 §N12）

## 一、退役 3（每条附 ⒜/⒝/⒞）

### ① `scripts/check-hardcoded.sh` → 退役（承接 = pre-commit 内联 #3）
- ⒜ 承接面：`scripts/pre-commit-check.sh:306` `soft_check "硬编码业务数据/类型 (#3 保留·本地)"`；数据源 `HARDCODE_DATA`（:297-303）
- ⒝ 承接面实测（注入 `'研发部'` 探针 html，相对路径注入）：
  `⚠️  硬编码业务数据/类型 (#3 保留·本地): 1 处  [V5 软提示——CI 为权威]` + 点名 `scripts/control-tower/tmp-hardcode-probe.html`
- ⒞ 零引用复核：`grep -rn check-hardcoded scripts/ tests/ .github/ package.json` = **0**；`git rm` 后无测试/入口断裂
- **如实登记（新发现）**：该承接面对 **`'marketing' / 'sales' / 'finance'` 三个字面量恒空转** —— :303 的 `grep -v` 排除链**恰好包含这三个字面量自身**，故它们永不可能命中（实测注入 `'marketing'` 探针 → ✅ 未命中；注入 `'研发部'` → ⚠️ 命中）。即"承接已存在但覆盖面比被退役脚本窄"，**建议单独立卡修排除链**（本卡不改判定语义）

### ② `scripts/control-tower/check-canary-drift.sh` → 退役（承接 = ct-health 内联）
- ⒜ 承接面：`scripts/control-tower/ct-health.sh:206` `run_canary_drift()`（原脚本逐字迁移，`exit`→`return`）；调用点 `.github/workflows/ci.yml:339` `if ! bash scripts/control-tower/ct-health.sh canary-drift; then … ::warning …`（**`|| true` 已清除**，见 §二）
- ⒝ 变异：把 `run_canary_drift` 内 `DRIFT` 报告中和（保留脚本合法）→ `tests/control-tower/check-canary-drift.test.sh` 红：`❌ 未点名漂移项`；恢复后 sha256 一致、**11 通过 0 失败**
- ⒞ 有它则绿：`check-canary-drift.test.sh` `11 通过, 0 失败`；`ct-health.sh canary-drift` 实跑 rc=0

### ③ `scripts/control-tower/check-name-allocation.sh` → 退役（**换主键**）
- ⒜ **主键替换点**：`scripts/control-tower/alloc-task-id.sh:226-236` `--check-id <D###>`（只读、不拿锁/不写盘/不算 MAX），实现 = 同文件 `:170 _occupy_locations()`（**单一副本**，注释原述"供 check-name-allocation.sh 复用同一份占用判定，避免第二副本漂移"）
- ⒝ 变异：把 `_occupy_locations` 的 task-state 占用面注掉（`if false`）→ 守卫测试红：`❌ 已占应 rc=1，实得 0`；恢复后 sha256 一致、**9 通过 0 失败**
- ⒞ 有它则绿：`check-name-allocation.test.sh`（**改名不改，改为"主键守卫"**，保留 ci.yml 密封登记）`9 通过, 0 失败`；断言含 `--check-id` 存在 + `_occupy_locations` 在位 + 已占/未占/非法三态
- **覆盖不丢**：原 171 行三元组测试→ 主键守卫 9 断言（已占 / 未占 / 非法 rc=2 / 接线 4 条 + 退役守卫）；原 `--worktree/--branch` 面的判别性用例随主键收敛到 `alloc-task-id.test.sh:459` 引用的同一实现

## 二、接线 4（每条附 ⒜/⒝/⒞；活点 = `iron-laws` job，ci.yml:111，无 needs）

| # | 判定面 | ⒜ 落点 | ⒝ 无它/坏它则红 | ⒞ 有它则绿 |
|---|---|---|---|---|
| ④ | **CT-40** 控制塔脚本↔配对测试 | `pre-commit-check.sh:540-552`（CI 权威区）+ 新生产 diff 基缝 `ct-test-gate.sh:29-33`（`SYNO_CT_DIFF_BASE...HEAD`，仅 GITHUB_ACTIONS=true 生效） | 变异性名 → `❌ 接线: ct-test-gate.sh 未接入 pre-commit` | 本地 CI 等价：`✅ CT-40 控制塔脚本测试配对: SYNC-OK: 控制塔脚本配对测试全绿 (7 个脚本)`；`ct-test-gate.test.sh` PASS |
| ⑤ | **platform-checklist**（D520 平台敏感命令） | `pre-commit-check.sh:395-408`（**原位恢复**，本地软 + CI 转硬） | 变异注释标记 → `❌ pre-commit 接线缺失` | `platform-checklist.test.sh` PASS；CI 口径 `✅ V5 平台敏感命令…` |
| ⑥ | **claim-regex / 铁律47 声称完成** | `pre-commit-check.sh:551-559`（按 V5.2.x `:775` 原文接回，`warn_check`；`BRIEF` 守卫紧随其上） | 变异收窄正则首词 → `❌ T1 收窄后正则缺失` | `claim-regex-narrow.test.sh` `9 通过, 0 失败`（含 T5b 动态守卫定位 L554） |
| ⑦ | **check-preset-bundles**（D945 预设 bundle 源） | `pre-commit-check.sh:564-575`（CI 权威区） | 调用改指不存在脚本 → `❌ D945 预设 bundle 源校验（rc=127）: 1 处 [CI strict]` + `::error` 注解 | CI 口径 `✅ D945 预设 bundle 源形态: --repo 汇总: 发现 1 个预设, 违规 0 个`；`check-preset-bundles.test.sh` PASS |

### ② 的 `|| true` 清理（裁定5 指定调用点）
```
- bash scripts/control-tower/check-canary-drift.sh || true
+ if ! bash scripts/control-tower/ct-health.sh canary-drift; then
+   echo "::warning title=canary-drift::漂移对账非零退出（告警不阻断，K3 P2-4 单独立项）"
+ fi
```
（原注释语义"告警不阻断"保留；`|| true` 的静默吞错改为显式 if + `::warning`。）

### 连带真修复（wire⑤ 的真阳性，非权宜）
恢复的 D520 检查实测点名 **`scripts/control-tower/ct-health.sh`**（该脚本 5 处裸 `python3`）⇒ 已 PYBIN 化（`:28-34` 三级探测 + 5 处调用改 `"$PYBIN"`）＋探针行补 `PYBIN` 注释（与仓内既有口径一致）。CI 口径复跑由 ❌ 转 ✅。

### 反 fail-open 修复（本卡自查发现）
④⑦ 的失败分支原先以 `soft_check <名字> "$(grep …)"` 传载荷：当被调脚本**非零退出但输出无匹配行**时载荷为空 ⇒ `soft_check` 空匹配**打 ✅**（实测 rc=127 曾打 `✅ …（rc=127）`）。已修：载荷为空时回填 `rc=<code> — <前 2 行输出>`，保证**失败必计红**。

## 三、L11 脱敏（不加豁免）
`docs/synova/product-lines/evidence/D962-2a2-selfverify.md:35` 的夹具假 key 串 → 改为 `sk-<22 位字母的门禁夹具串，task-28 按 CTO 裁定脱敏>`。验证（暂存该文件后跑门禁）：
```
✅ 暂存文件无硬编码凭证
✅ Secrets 扫描: 全部通过 ✅      （rc=0）
全目录残留复核：grep -rnE 'sk-[a-zA-Z0-9]{20,}|cli_[a-z0-9]{10,}' docs/synova/product-lines/evidence/ → 0
```

## 四、计数（权威口径，实测不凑数）
```
$ find scripts -type f -name 'check-*' ! -path 'scripts/audit/*' | wc -l
21
main 基线（同口径 @origin/main）= 48
```
**新版算式：`48（main） − 27（本 PR 合并/退役）= 21`**，其中本卡退役 3：
`check-hardcoded.sh`、`control-tower/check-canary-drift.sh`、`control-tower/check-name-allocation.sh`。
- **目标 ≤20 尚差 1**：CTO 算式第四项「−1」未指明对象。按 plan §一 属 **wire** 的 4 件（`check-file-driven.sh` / `check-gitlinks.sh` / `check-k3-report.py` / `check-pr-budget.sh`）**本卡已按裁定接线，不得再计退役**。
- **候选（请 CTO 点名，我不擅自退役）**：若必须再减 1，实测零生产调用且 plan 未明列"保留"的只剩本卡已接线的这 4 件；其余 17 件均有真实调用点（`check-gate-integrity.sh` ci.yml:412/466/484；`check-bypass-log.sh` pre-push 等）。
- **结论：实测 21，不凑 20**；差 1 需 CTO 明确对象后另行执行。

## 五、N12 口径（PR #803 正文）
正文首部已改为显式：**`CI 未跑（外部队列停摆 @2026-09-25T19:04:20Z）`** + 「禁以本地等价冒充实跑」+「CI 恢复后必须重跑并补结论；**合并前无真 run 需创始人书面接受风险**」。

## 六、后果与遗留（未清）
| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| L12 | 内联 #3 排除链自吞 `'marketing'/'sales'/'finance'`（承接面窄于被退役脚本） | CTO 新卡 | 登记 |
| L13 | 计数差 1（21 vs ≤20）；需 CTO 点名第 4 项 | CTO | 待裁 |
| L14 | `sync-dsh-skills` / `.claude ↔ .dsh` 技能同步未涉及本卡 | 并行 | — |
| L3/L4/L11 | 真 CI ⒞ 待补 / D2 空转 / L11 已清 | 混合 | 部分清 |
| L15 | 本卡 CI-zone 四处新判定面均**只在 SYNO_CI=1 路径**生效（本地快检不跑）——符合"CI 权威"口径，**如实登记** | — | 登记 |
