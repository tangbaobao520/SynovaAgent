# D962 task-30（rev3）回执 — 3 红 job 定位/修 + 供件 + T3/A9/T5/T6 + 口径更正

- **as_of**：2026-09-26T04:05+08:00（本地 CI 等价复跑窗口）；CI 侧数据 as_of 见各条
- **分支/head**：`feat/d962-2a-merge` @ `a964c302`（task-30 P0 修复提交；后续 T6/口径提交见 §推送）
- **判定**：待 CTO 收件闸 + K3 终审（**只给自验结论**）

## T2 ｜ 3 红 job 逐条定位 + 修（head `52f5aa1f` 实测：绿 11 / cancelled 13 / 红 3；as_of 2026-09-25T19:40Z）

### (1) `Gate Integrity (pattern sentinel + injection fixture + ci-reds)` — 修点在写集内 ✅ 已修
**失败片段（job 108215839357 原始日志，完整不截尾）**：
```
── 场景 0: baseline (干净副本，无任何注入) ──
   [BASELINE_FAIL] 全部 ❌ 行:
     ❌ CT-40 控制塔脚本测试配对（rc=2）: 1 处  [CI strict——软提示转硬]
     ❌ 1 组未通过 — 提交已拒绝
```
**根因**：task-28 ④ 的 CT-40 wire 把 `SYNO_DIFF_BASE`（默认 `origin/main`）**无条件传给门禁**；Gate Integrity 在
**干净副本**里跑 pre-commit（副本无 `origin/main`）⇒ `git diff origin/main...HEAD` 失败 ⇒ 门禁 `rc=2 degraded`
⇒ CI strict 计红 ⇒ 基线场景假红。
**修**（`scripts/pre-commit-check.sh`，写集内）：**先 `git rev-parse --verify -q` 验证基存在再传**，不存在传空 ⇒ 门禁回落暂存区（空 ⇒ `SYNC-OK` 跳过）。
**本地验证（原始输出）**：
```
$ env GITHUB_ACTIONS=true SYNO_CI=1 SYNO_DIFF_BASE=origin/NOPE-D962 bash scripts/pre-commit-check.sh
  ✅ CT-40 控制塔脚本测试配对: SYNC-OK: 无控制塔脚本变更(跳过)          # 旧版此处为 ❌ rc=2
```
（注：该次整跑仍 rc=1，因为"基不存在"同时影响其它判定面 —— 属该人工场景的预期；Gate Integrity 副本的真实基
由 harness 决定，其基线唯一 ❌ 即 CT-40 行，见上。）
**injection 夹具复跑**：`precommit-groups-injection.test.sh` → **PASS**（其 baseline 场景此前同因 rc=2 判 `BASELINE_FAIL`）。

### (2) `Control Tower Gate Tests (windows-latest)` — 修点在写集内 ✅ 已修（真因=Windows 平台差异）
**失败片段（job 108215840089 原始日志）**：
```
 FAIL: tests/control-tower/g10-cp3.test.sh
 ##[error]  ❌ G10 红分支未触发（死分支回归或映射缺失）|  ✅ G11 CI strict 转硬（❌ 可见）|  结果: PASS=12 FAIL=1
 FAIL: tests/control-tower/g12-day-window.test.sh
 ##[error]  ❌ 活点② 行为: 生产实现集合不符 —— 期望 [.claude/task-briefs/2026-09-24-D100-x.md …] 实得 [
 FAIL: tests/control-tower/grep-oP-regression.test.sh
 ##[error]  ❌ backtick 路径（workflow brief-vs-code） — 期望 '`src/routes/a.ts`' 实得 ''
```
**Windows 差异说明（平台方言 + 路径）**：
- **G10**：内联 python 用 `open('$CRITERIA_MAP')`，而 `$CRITERIA_MAP` = `$(git rev-parse --show-toplevel)/…`
  是 **Git-Bash POSIX 路径**（`/d/a/…`）→ **原生 Windows python 打不开** ⇒ JSON 读取抛异常（被
  `except Exception: pass` 吞）⇒ glob 集空 ⇒ G10 走"无映射跳过" ⇒ 红分支不触发。（macOS/Linux 的 python 认 POSIX 路径 ⇒ 本机绿。）
- **G12**：`glob.glob()` 在 Windows 返回**反斜杠路径** ⇒ 后续 `[ -f "$B" ]`（Git Bash 内建）失配 ⇒ 集空。
- **修**（写集内）：① 新增 `_TO_PY()`（`cygpath -w` 转换，**无 cygpath 的平台原样** ⇒ macOS/Linux 行为不变），
  凡把路径喂 python 一律经它（G10 criteria-map 改用 **env 传参**，避免反斜杠被当转义；G12 的 `SCOPE_TSV/EXCL_TSV` 同）；
  ② G12 窗口 python 输出归一化 `f.replace(os.sep,'/')`。
**本地验证**：`g10-cp3` PASS ｜ `g12-day-window` PASS（14/0）｜`ct-test-gate` PASS ｜ CI 口径整跑 `rc=0 stderr=0`。

### (3) `Control Tower Gate Tests (ubuntu-latest)` — **唯一剩余真因落在写集外 ⇒ 供件（未动）**
**失败片段（job 108215839412 原始日志）**：
```
 FAIL: tests/control-tower/grep-oP-regression.test.sh
 ##[error]  ❌ backtick 路径（workflow brief-vs-code） — 期望 '`src/routes/a.ts`' 实得 ''|  Status: ❌ grep -P 回归网未通过
```
**根因（跨方言，非平台偶发）**：断言/生产脚本用的是 `` '\`[^\`]+\.[a-z]{2,5}\`' ``。**GNU grep 把 `\`` 解析为
"缓冲区起始锚"**（GNU 扩展），BSD grep 解析为字面反引号 ⇒ **同一模式在 ubuntu 恒不命中**（`grep -oE` 输出空）。
本机（BSD）恒绿 ⇒ 类"CI-only 红"。
**该模式在库真身**：`scripts/workflow/check-brief-vs-code.sh:59-60`（生产抽取），即 **Linux 上该抽取函数恒返空**（真缺陷，非仅测试）。
**供件（两文件同时落，缺一即红）**：
```diff
--- a/scripts/workflow/check-brief-vs-code.sh
+++ b/scripts/workflow/check-brief-vs-code.sh
@@ -56,8 +56,8 @@
    # 兜底: 反引号路径（旧格式 brief 兼容）
    DECLARED_FILES=$(sed -n '/^## Q2:/,/^## Q3:/p' "$BRIEF" 2>/dev/null \
-     | grep -oE '\`[^\`]+\.[a-z]{2,5}\`' \
-     | sed 's/\`//g' \
+     | grep -oE '[`][^`]+\.[a-z]{2,5}[`]' \    # POSIX 等价：方括号包裹反引号（两方言一致）
+     | sed 's/`//g' \
      | grep -v 'node_modules\|\.test\.' \
--- a/tests/control-tower/grep-oP-regression.test.sh   （**该文件在写集内，但须与上行同批才好使**）
+++ b/tests/control-tower/grep-oP-regression.test.sh
@@ -134,2 +134,2 @@
-eq 'backtick 路径（workflow brief-vs-code）' "scripts/workflow/check-brief-vs-code.sh" '\`[^\`]+\.[a-z]{2,5}\`' \
-   '改 `src/routes/a.ts` 完成' '`src/routes/a.ts`'
+eq 'backtick 路径（workflow brief-vs-code）' "scripts/workflow/check-brief-vs-code.sh" '[`][^`]+\.[a-z]{2,5}[`]' \
+   '改 `src/routes/a.ts` 完成' '`src/routes/a.ts`'
```
（判据强度不变：仍要求"生产脚本含该模式 ∧ 抽取结果 == 期望"。**未动任何文件**：按 rev3 边界，修复点落在
门禁脚本 ⇒ 停手供件。）

## T2-P0-2 ｜ 供件：`loop-context.test.sh` 密封清单登记（**前置未满足，先备件**）
- **前置实测（as_of 2026-09-26T04:05+08:00）**：`ls tests/control-tower/loop-context.test.sh` → **不存在**；
  `grep -c "loop-context.test.sh" .github/workflows/ci.yml` → **0**。（与"前置 #836 未合，先备件"一致。）
- **落点**：`.github/workflows/ci.yml` 的 `control-tower-tests` job `for t in \` 清单（授权段 216–292）。
  建议插在 **`tests/control-tower/loop-score.test.sh \`（当前 :259）之后一行**，保持同族相邻：
```diff
             tests/control-tower/loop-score.test.sh \
+            tests/control-tower/loop-context.test.sh \
             tests/control-tower/gate-stats.test.sh \
```
- **前置条件**：#836 合并（提供该测试文件）后落本行；**落行不落文件 = CI 必红（文件不存在）**，故不得提前落。

## T6 ｜ ownership.yaml 只加一行 ✅ 完成（as_of 2026-09-26T04:10+08:00）
```diff
--- a/docs/synova/coordination/ownership.yaml
+++ b/docs/synova/coordination/ownership.yaml
@@
   - glob: "scripts/pre-push-check.sh"
     owner: "mac"
     source: "TASK-ROUTING.md L63 门禁脚本 DSH 专属"
+  - glob: "scripts/commit-msg-check.sh"
+    owner: "mac"
+    source: "TASK-ROUTING.md L63 门禁脚本 DSH 专属（task-30 T6 补登记；此前落 ** 兜底误判 win）"
```
**判据（原始输出）**：
```
$ python3 scripts/control-tower/check-ownership.py scripts/commit-msg-check.sh --owner mac
mac  scripts/commit-msg-check.sh

✅ PASS 1 个文件全部归属 owner=mac（无归属 0）      rc=0
```
**连带（yaml 头部契约要求）**：`--emit-codeowners` 与 `.github/CODEOWNERS` 出现 1 行漂移
（`+scripts/commit-msg-check.sh              @tangbaobao520`）⇒ 已按契约重生成，漂移复核无 diff、
`check-ownership.test.sh` **58 项全通过**（原 1 项失败）。**边界说明**：`.github/CODEOWNERS` 不在 rev3
禁止清单（禁止项=ci.yml/门禁脚本/scripts/control-tower/**/audit/src/主树），且 yaml 明文要求"改完必须重跑"，故执行；如 CTO 认为该文件亦须供件，我下轮改为供件。

## T3 ｜ 证据表 file:line 逐条更正（更正前 → 实测，本回执提交后的 head）
| 锚点 | 回执原文 | **实测（file:line）** | 偏差 |
|---|---|---|---|
| 内联 #3 承接 | `pre-commit-check.sh:306` | **311** | +5 |
| D782 文档防线块 | `pre-commit-check.sh:480-519` | **507**（块首注释） | +27 |
| V5 平台敏感命令块 | `pre-commit-check.sh:395-408` | **413** | +18 |
| CT-40 wire 块 | `pre-commit-check.sh:540-552` | **548** | +8 |
| claim-regex（铁律47） | `pre-commit-check.sh:551-559` | **565** | +14 |
| D945 preset-bundles | `pre-commit-check.sh:564-575` | **577** | +13 |
| G12 CI 块（三日窗） | `pre-commit-check.sh:356-360` | **456** | +100 |
| 组 7a 三态降级块 | 未给 | **331** | 补 |
| canary 内联函数 | `ct-health.sh:206` | **206** | 0 ✅ |
| `--check-id` 主键段 | `alloc-task-id.sh:226-236` | **226** | 0 ✅ |
| `_occupy_locations()` | `alloc-task-id.sh:170` | **170** | 0 ✅ |
| CT-40 diff 基缝 | `ct-test-gate.sh:29-33` | **25** | −4（task-30 重写该段） |
| iron-laws job | `ci.yml:111` | **111** | 0 ✅ |
| canary→ct-health 调用 | `ci.yml:339` | **339** | 0 ✅ |
**偏差根因**：回执锚点是在 **task-26/28 当时的行号**上取的；此后同一提交链内又插入了 task-28/30 的块，
`pre-commit-check.sh` 行号整体位移（+5…+100）。**教训（自持条款）**：跨提交的证据锚点必须绑 `as_of` + 当时的 blob sha，
否则审计复核必然对不上（本表即为更正）。

## A9 ｜ sha256 迁移 8/8 的 **before 侧清单**（补齐，可核）
before 侧源 = 主树被忽略目录 `docs/synova/coordination/evidence/`（原件未改动，as_of 2026-09-26T04:05+08:00）：
```
D962-2a-selfverify.md        b800c9f01b95f362bd03a59584e01b7dc2d19a71e7ace103dd5ef99a43e2be6c
D962-2a2-selfverify.md       91cbbaf2774a7a9f761de2b9bd3f903b02b63c41f1adae43d5e63a5e803fa258
D962-2a3-reverify.md         370bc089babfa71bbf8679a35b10d56ab7b07a1efa6785f19f0fb7f61e619318
D962-decl-selfverify.md      20d7e5ba2d5f4229596a55e7acd8d0fbb2afbc2c47290b638a4955fc28eb3155
D962-merge-selfverify.md     d99e01393e8de85fc065b2d85ca130a2035da0cec59d5165ef3cfb44389683a8
D962-refresh-selfverify.md   f6f6aa677ab534c9071d0fb8bc34af66a8662022a424f2e032233e3e62f4da04
D962-selfverify.md           c0694f843585fa0261a4f3a9fe5146c942e34caeba8c900fd76caefbfd8ccf50
D962-task26-selfverify.md    0da05a4cb34cfddd5e8f01687993d0687fe3124845a4d242ef3061255adfe01f
```
**after 侧**：7 件与 before **逐字节相同**（迁移时已逐文件 sha256 对照）；**唯一例外 `D962-2a2-selfverify.md`**：
`after = ` 见 §L11 —— 该件按 CTO 裁定**脱敏**（`sk-<22 位…>`），故 after ≠ before（差异可核、原因可核）。

## T5 ｜ L13：21 条完整清单（脚本 + 一行用途 + 活点 file:line + 零接线标注）
> 口径：`find scripts -type f -name 'check-*' ! -path 'scripts/audit/*' | wc -l` = **21**（main 同口径 = 48）。**不凑数**。

| # | 脚本 | 一行用途 | 活点证据（file:line） |
|---|---|---|---|
| 1 | `scripts/check-architecture.sh` | 五层架构边界 + 桥接违规 | `baseline-check.sh:121`；`ci.yml` architecture job |
| 2 | `scripts/check-file-driven.sh` | 文件驱动完整性（manifest/tags/目录） | **⚠ 零生产调用**（仅 g8 测试登记） |
| 3 | `scripts/check-plan-integrity.sh` | brief 声明完整性（吸收 parseable/verifiable-done/q0c） | `attach.py:11`；`synova-commit:433` |
| 4 | `scripts/check-secrets.sh` | 凭证三处防护（本地/pre-push/CI） | `pre-commit-check.sh:208,221,388`；`pre-push-check.sh:262` |
| 5 | `scripts/control-tower/check-bypass-log.sh` | bypass 执行证据链对账（D331） | `pre-push-check.sh:425`；`pre-audit-summary.sh:44` |
| 6 | `scripts/control-tower/check-citations.py` | 派单引用可解析性 | `pre-dispatch-check.sh:50` |
| 7 | `scripts/control-tower/check-dsh-anchor.py` | DSH 断面一致性 | `pre-commit-check.sh:592-598` |
| 8 | `scripts/control-tower/check-gate-integrity.sh` | 门禁注入自测 + 棘轮基线 + ci-reds | `ci.yml:412/466/484` |
| 9 | `scripts/control-tower/check-gitlinks.sh` | 记忆双链 `[[D#]]` 可解析 | **⚠ 零生产调用**（plan #27 判"接线而非退役"） |
| 10 | `scripts/control-tower/check-k3-report.py` | K3 报告格式/证据/计数校验 | **⚠ 零生产调用**（plan #28 P0"保留"） |
| 11 | `scripts/control-tower/check-notes-lifecycle.sh` | Notes 四态生命周期（僵尸 proposed） | `pre-commit-check.sh:218` |
| 12 | `scripts/control-tower/check-ownership.py` | 模块域归属（越域即拦） | `check-pr-budget.sh:37` |
| 13 | `scripts/control-tower/check-pr-budget.sh` | PR 文件数/域预算 | **⚠ 零生产调用**（plan #32 P0"保留"） |
| 14 | `scripts/control-tower/check-preset-bundles.sh` | DSH 预设仓库源↔运行时对账 | `pre-commit-check.sh:577`（task-28 ⑦ wire） |
| 15 | `scripts/doc-system/check-doc-truth.sh` | 导航层文档 vs 代码事实 | `pre-commit-check.sh:507`（task-26 接回） |
| 16 | `scripts/product-lines/check-progress-freshness.py` | 产品进度产物过期看门狗 | `progress-freshness-watchdog.yml:32` |
| 17 | `scripts/workflow/check-brief-vs-code.sh` | brief Q2 声明 ↔ 代码一致性 | `ci.yml` checker-review job |
| 18 | `scripts/workflow/check-dev-doc-write-set.sh` | dev doc 写集双向对账 | `pre-audit-summary.sh:15,47` |
| 19 | `scripts/workflow/check-golden-regression.sh` | 黄金数据集回归（F1） | `pre-push-check.sh:283` |
| 20 | `scripts/workflow/check-integration.sh` | 集成契约检查 | `ci.yml:353` integration-check job |
| 21 | `scripts/workflow/check-silent-swallow.sh` | 静默吞错扫描（`|| true` 家族） | **⚠ 零生产调用**（原始调用点 `pre-commit:554 --diff` 随 V5.3 移除后无落点） |

**零接线 5 条**：#2 / #9 / #10 / #13 / #21。**建议**（**非退役建议**，逐条理由）：
- #9/#10/#13：plan §一 明列"保留（P0 或接线而非退役）" ⇒ 应**补调用点**（活点可为 iron-laws CI 权威区），非退役；
- #2：plan §一 #9 判"保留（域宿主）" ⇒ 同样应接线；
- #21：原活点 `pre-commit:554 --diff` 随 V5.3 移除且无 CI 落点 ⇒ **唯一可议退役项**（承接面需 CTO 判，例如并入 check-file-driven 或 §五 linter 化）。
**故本清单不构成任何退役建议**（CTO 已取消 ≤20 硬指标；实测 21，不凑数）。

## T1 / T9 ｜ 口径更正（PR #803 正文，已改）
- **删除** `⛔ CI 未跑（外部队列停摆 …）` 整段（**该口径错误**：K3 实测 head `52f5aa1f` = 绿 11 / cancelled 13 / 红 3）。
- 改为**逐 head 三类分列**（绿列 job 名 / 红列 job 名 / 无 run 标注）+ `as_of`；**本 PR 引入的红** vs **既存红** 分列（见正文 §5）。
- D1008（Vitest 2/2 @main）**不豁免**：仍列「既存红」，卡在 `chore/d1008-ratchet-card @1837773d`。

## 未清项 / 需 CTO 裁决
| # | 事项 | 需 CTO |
|---|---|---|
| U1 | grep-oP 跨方言供件（§T2(3)）：**是否授权落 `scripts/workflow/check-brief-vs-code.sh`**（我方可自行落）+ 同批改 `tests/control-tower/grep-oP-regression.test.sh` 模式 | 授权 |
| U2 | `loop-context.test.sh` 登记行待 #836 合并后落（供件已备，§T2-P0-2） | 时序 |
| U3 | POSIX 等价式替换后，是否要求 K3 复核对 BSD/GNU 双方言（我方可补 arm 断言） | 口径 |
| U4 | T5 中 #2/#9/#10/#13 的**接线卡**是否由我方立（活点=iron-laws） | 派工 |
