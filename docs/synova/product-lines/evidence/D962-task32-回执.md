# D962 task-32 回执 — R1 U1 跨方言修复 + R5 + R3 + R4

- **as_of**：2026-09-26T05:05+08:00（本地验证窗口）；GNU 侧证据 as_of 见各条（来源逐条标注）
- **head**：`59f2b6f6` 起（本回执提交 sha 见 §推送）
- **判定**：**实现+证据已交**；门禁脚本语义变更**待 K3 审**（我不自判通过）

---

## 项｜判定｜原始命令+原始输出

### R1 ｜ 判定=**已实现（BSD 侧实证；GNU 侧待真 run 作判据）**

**改动（两文件同批）**
| 文件 | 行 | 改动 |
|---|---|---|
| `scripts/workflow/check-brief-vs-code.sh` | :57-65（兜底块） | 模式 `'\`[^\`]+\.[a-z]{2,5}\`'` → `'[`][^`]+\.[a-z]{2,5}[`]'`；`sed 's/\`//g'` → `sed 's/`//g'`；块首加 3 行缘由注释 |
| `tests/control-tower/grep-oP-regression.test.sh` | :134-136 | 同式替换（断言强度不变：仍要求「生产脚本含该模式 ∧ 抽取结果==期望」） |

**① BSD 侧反例（macOS 本机，BSD grep；as_of 2026-09-26T05:05+08:00）**
```
$ printf '%s\n' '改 `src/routes/a.ts` 完成' | grep -oE '\`[^\`]+\.[a-z]{2,5}\`'
`src/routes/a.ts`                       # ✅ 旧模式在 BSD **命中** ⇒ 解释「本机恒绿」
$ sed -n '59p' scripts/workflow/check-brief-vs-code.sh   # 改动前

$ bash tests/control-tower/grep-oP-regression.test.sh
   ✅ backtick 路径（workflow brief-vs-code）
  结果: 24 通过, 0 失败
```
端到端兜底抽取（合成 brief + 生产同式，`/tmp` 字面 heredoc 免引号歧义）：
```
PAT=`\`[^\`]+\.[a-z]{2,5}\``   EXTRACT=[src/foo/bar.ts]      # 旧模式（BSD 命中）
PAT=[`][^`]+\.[a-z]{2,5}[`]    EXTRACT=[src/foo/bar.ts]      # 新模式（BSD 命中）
```

**① GNU 侧反例（来源=CI 真 run；as_of 2026-09-25T20:34:07Z）**
```
repo=tangbaobao520/SynovaAgent  run=36185734275  head=807b6b67  job=108238505073 (Control Tower Gate Tests (ubuntu-latest))
 FAIL: tests/control-tower/grep-oP-regression.test.sh
 ##[error]  ❌ backtick 路径（workflow brief-vs-code） — 期望 '`src/routes/a.ts`' 实得 ''|  Status: ❌ grep -P 回归网未通过
```
（同断言在 head `52f5aa1f` 的 job `108215839412` 亦同式失败；windows-latest 腿 = Git-Bash 自带 GNU grep，同断言同红 ⇒ **两腿共同拦截点**。）
**本机无 GNU grep**（实测无 `ggrep`/`busybox`/`docker`/`colima`）⇒ GNU 侧**仅能以 CI 真 run 为判据**。
**结论表述（按 CTO ④）**：旧模式「**实测在 GNU 下恒不匹配**」（`\`` 在 GNU BRE 是扩展锚点；本卡 `-E` 下结论以实测为准）。

**④ 判据（真 run，非本地等价）**：本批 push 后在 #803 新 head 上等 CI 定态，要求
`Control Tower Gate Tests (ubuntu-latest)` **与** `(windows-latest)` **两腿由红转绿**（见 §推送与 CI 结果）。

**③ K3**：本件仅交**实现+证据**；门禁脚本语义变更**由队长走 K3**，我**不自判过**。

### R5 ｜ 判定=**已入库（before/after 双清单 + 非版本锚）**
`before` 侧源 = 主树被忽略目录 `docs/synova/coordination/evidence/`（原件未改动，as_of 2026-09-26T05:05+08:00）：
```
D962-2a-selfverify.md        b800c9f01b95f362bd03a59584e01b7dc2d19a71e7ace103dd5ef99a43e2be6c
D962-2a2-selfverify.md       91cbbaf2774a7a9f761de2b9bd3f903b02b63c41f1adae43d5e63a5e803fa258
D962-2a3-reverify.md         370bc089babfa71bbf8679a35b10d56ab7b07a1efa6785f19f0fb7f61e619318
D962-decl-selfverify.md      20d7e5ba2d5f4229596a55e7acd8d0fbb2afbc2c47290b638a4955fc28eb3155
D962-merge-selfverify.md     d99e01393e8de85fc065b2d85ca130a2035da0cec59d5165ef3cfb44389683a8
D962-refresh-selfverify.md   f6f6aa677ab534c9071d0fb8bc34af66a8662022a424f2e032233e3e62f4da04
D962-selfverify.md           c0694f843585fa0261a4f3a9fe5146c942e34caeba8c900fd76caefbfd8ccf50
D962-task26-selfverify.md    0da05a4cb34cfddd5e8f01687993d0687fe3124845a4d242ef3061255adfe01f
```
`after` 侧（入库件，可核）= `docs/synova/product-lines/evidence/` 同名 7 件；**7/7 与 before 逐字节相同**，
**唯一例外 `D962-2a2-selfverify.md`**：按 CTO L11 裁定脱敏（夹具假 key → `sk-<22 位…>`），故 after ≠ before（差异与原因均可核）。
**非版本锚声明**：上表为**内容锚（sha256）**，不依赖任何 commit hash/行号 ⇒ 后续重写、变基、行号位移**不断链**（R5 关切点）。

### R3 ｜ 判定=**清单已出（5 条逐条定性；我方不承接线）**
| # | 脚本 | 一行用途 | 活点实测 | 定性 | 归属阶段 |
|---|---|---|---|---|---|
| #2 | `scripts/check-file-driven.sh` | 文件驱动完整性（manifest/tags/目录/pizza-chain） | **零生产调用**（仅 g8 测试登记） | **真保护**（plan §一 判「保留·域宿主」） | **接线 → 阶段 4** |
| #9 | `scripts/control-tower/check-gitlinks.sh` | 记忆双链 `[[D#]]` 可解析性 | **零生产调用**（plan 明写「接线而非退役」） | **真保护** | **接线 → 阶段 4** |
| #10 | `scripts/control-tower/check-k3-report.py` | K3 报告格式/证据/计数校验 | **零生产调用**（plan §一 #28：P0 保留） | **真保护** | **接线 → 阶段 4** |
| #13 | `scripts/control-tower/check-pr-budget.sh` | PR 文件数/域预算（D734） | **零生产调用**（plan §一 #32：P0 保留） | **真保护** | **接线 → 阶段 4** |
| #21 | `scripts/workflow/check-silent-swallow.sh` | 静默吞错扫描（`2>/dev/null` 家族） | **零生产调用**：原活点 `pre-commit:554 --diff` 随 V5.3 改写消失 | **真保护，但需重定活点**（建议并回 pre-commit 本地组或 iron-laws） | **接线 → 阶段 4**（若 CTO 判其判定面已被 V5.3 `_DIAG_EXCL`/`swallow-ok` 覆盖，则**列阶段 5 退役**） |
**退役清单（阶段 5）**：**本卡不主动列入任何一条**（5 条均为真保护；#21 是否退役取决于 CTO 对「判定面是否已被覆盖」的裁决）。
**我方不承接线**：上表仅为清单与定性，接线派工归门禁线（阶段 4）。

### R4 ｜ 判定=**同批入库 + 漂移 0**
`docs/synova/coordination/ownership.yaml` +1 条目（`scripts/commit-msg-check.sh → mac`）与 `.github/CODEOWNERS`
**同批**重生成（task-30 提交 `7db9e0cd`；本卡再次复核 as_of 2026-09-26T05:05+08:00）：
```
$ python3 scripts/control-tower/check-ownership.py scripts/commit-msg-check.sh --owner mac
mac  scripts/commit-msg-check.sh
✅ PASS 1 个文件全部归属 owner=mac（无归属 0）
$ python3 scripts/control-tower/check-ownership.py --emit-codeowners | diff .github/CODEOWNERS -
（无输出 = 漂移 0）
$ bash tests/control-tower/check-ownership.test.sh   → ✅ 全部通过: 58 项
```
**登记**：`.github/CODEOWNERS` 系**生成物入库** ⇒ 归类 **阶段 3**（与 `tests/output/**`、`reference-map.md` 同族：
由脚本产出、入库以便逐字节漂移断言；手改即红）。

---

## 未清项 / 需 CTO 裁决
| # | 事项 | 状态 |
|---|---|---|
| U1 | R1 的 GNU 侧**正向**证据 = 本批 push 后的真 run（两腿转绿）；若 CI 仍 `cancelled` 我会补触发并说明 | 待真 run |
| U2 | R1 门禁语义变更 **K3 审**（队长走） | 待 K3 |
| U3 | #21 是否列阶段 5 退役（判定面是否已被 V5.3 覆盖） | 需 CTO |
| U4 | #2/#9/#10/#13 接线卡派工（活点建议=iron-laws CI 权威区） | 需 CTO |
