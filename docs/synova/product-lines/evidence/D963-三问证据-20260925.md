# D963 · DSH 侧边栏面板「宪章三问 48 格」— 三问证据包 + 收尾三件

> 任务：D963（win 域，DSH 小队执行）｜分支 `feat/d963-charter-grid-panel`｜base `main@a6a8ccf9`
> 交付时间：2026-09-25｜小队：队长（协调/前提/证据）+ d963-coder（编码）+ d963-verifier（独立自验）
> **K3 复审通过前不得合并；PR 由 CTO 开。**

---

## 〇、推送回执（原文）

```
$ git ls-remote --heads origin | grep feat/d963-charter-grid-panel
9119141daf58ababfb17c305943aa9001db9e93c	refs/heads/feat/d963-charter-grid-panel
```

## 一、收尾三件之一：改动清单（diff）

```
$ git diff main --stat | tail -1
10 files changed, 1945 insertions(+), 13 deletions(-)
```

| 文件 | 作用 |
|---|---|
| `dsh/plugins/synova-dashboards/package.json` | P1 删死声明 `@deepseek-ai/dsh-client-runtime`（修前 1 处 → 修后 0 处） |
| `lib/charter.js`（新增） | 三级取数（工作区 → origin/main → 显式 degraded）+ `adaptCharterGrid`（真源 `cells[]` → 面板 `rows[]`） |
| `lib/index.js` | 只读路由 `GET /synova/charter/grid`（经 adapter 下发） |
| `lib/client.js` | 「宪章三问」面板（main keyed + `sidebar.panellist` 成对注册，id `synova-charter-grid`，order 60）+ 四色渲染 + 框架态显式降级 |
| `test/charter-grid.test.js`（新增） | adapter/四色/契约一致性/框架态/X27 用例 |
| `test/fixtures/charter-48.json`（新增） | 真源夹具，与 `origin/main` blob 逐字节一致（sha256 `872c409a…f772d3`） |
| `test/client-panel.test.js`、`test/host-route.test.js` | 适配第二面板 |
| `README.md` | 「框架态降级」表（框架侧两态声明 + file:line 依据） |
| `.claude/bypass.log` | D521 hook 自动登记（非本任务写集，编码全程未用 `--no-verify`） |

## 二、前提冻结（开工前实测，不可让渡 #1）

| 前提 | 命令 | 输出摘要 | 结论 |
|---|---|---|---|
| slot 名可用 | `grep -n "id:" …/cordis-client-runner/src/client/slot-catalog.ts` | `sidebar.panellist` @ **:3035**（list 型）；root `main` keyed；由 client-ui-layout/client-ui-sidebar 声明 | ✅ |
| 骨架已在 | `grep -n "sidebar\|inject" lib/client.js` | :635 `inject("main")` + :640 `inject("sidebar.panellist")`（照此成对模式） | ✅ |
| P1 死声明 | `grep -rn "dsh-client-runtime"` 全仓 / DSH 锚定仓 | SynovaAgent：1 处（package.json:25，零消费者）；DSH：2 处纯文档，无此模块 | ✅ 确认死声明 |
| 数据源已备 | `ls` + `git ls-tree origin/main` | 开工时**不在 main**（仅 CTO 本地分支 tmp-d942）⇒ 停工报 CTO；#759 合入后恢复 | ❌→已修 |

## 三、三问判据

### ① 加了吗 — ✅
- 面板组件：`lib/client.js`（`CharterGridPanel` + `CharterGridIcon`）
- slot 注册（成对，main 先于入口行）：
  - `lib/client.js:804` `slots.inject("main", … register({name:"main", key:"synova-charter-grid"}))`
  - `lib/client.js:808` `slots.register({name:"sidebar.panellist", id:"synova-charter-grid", order:60, label:"宪章三问"}, CharterGridIcon)`
- Host 路由：`lib/index.js:87–114` `GET /synova/charter/grid`

### ② 接上了吗 — ✅
- slot 权威声明：DSH 锚定仓 `packages/extensions/cordis-client-runner/src/client/slot-catalog.ts:2989/3035`；声明方 `packages/client/ui-sidebar/src/client/contract/slots.ts:34`
- 宿主消费实证（运行中的桌面 App）：
  ```
  $ curl -s http://127.0.0.1:19387/synova/charter/grid
  {"ok":true,"source":"origin/main","counts":{"cells":48,"ext_points":16,"questions":3,"filled":0},…}
  ```
- 面板入口被宿主消费（Founder 截图，2026-09-25 11:41）：左栏出现「宪章三问」，点击打开面板

### ③ 生效了吗 — 分两段（关键教训）
- **第一段（Founder 实测 11:41，截图）**：入口在、面板开、四色图例在、头部「源 origin/main」，但正文 **0 行**——「数据已就绪但 rows 为空」。
  - **根因（队长字段级实测）**：真源返回 `cells[]`（48 项，`ext_point/question/question_text/status/…`），客户端 `lib/client.js:696` 读 `data.rows[]` —— **契约不一致，Host 半原样透传、无 adapter**。
  - **为何 44/44 全绿仍空板**：测试夹具是**自造 rows 形状**，不是真源形状 ⇒ 自证循环（本任务最大教训）。
- **第二段（修复后，代码级真源端到端）**：
  ```
  $ node --input-type=module -e "…readCharter+adaptCharterGrid(repoRoot=worktree)…"
  source: origin/main | filled: 0 | rows: 16 | cols: 3 | questions: 加了吗/接上了吗/生效了吗
  tone dist: {"empty":48}
  ```
- **GUI 截图（修复版）**：待 Founder 重启桌面 App 后补（host 半为进程启动时加载；已实测摘除/重挂 bundle 无法热更新已加载模块，必须重启）。**此格未闭合前本件不得声称"已生效"。**

### ④ 改坏即红 — ✅（测试级，独立自验复做）
- 删 tone 白名单映射 → `tests 18 / pass 12`（红 6，含「真源当前态不是空板」「X27」「契约一致性」）
- `rows` 字段改名（复现线上空板）→ `tests 18 / pass 9 / fail 9`
- 实验全在 `/tmp`，工作树 `git status --short` 空、HEAD 与远端一致（零残留零 commit）

### ⑤ 降级显式 — ✅（可检测态）+ 声明（框架侧）
| 态 | 处理 | 依据 |
|---|---|---|
| 数据源缺失 | 200 + `{ok:false,degraded:true,missing:"宪章三问-48格.json"}` + 面板横幅 | `lib/index.js:92–104`、`lib/client.js:709` |
| 坏 JSON / 取数失败 | 记因 → 继续 origin/main → 仍失败则显式 degraded | `lib/charter.js:33–79` |
| DSH 版本不足（API 面缺失） | 探测 + `console.warn` + 提前返回，不抛错 | `lib/client.js` apply 头部 |
| slot 未声明 | `specDynamic/spec` 探测 + warn「等待声明」 | guard.ts:127 同款用法 |
| register 抛错 | `guardedRegister` × 4 注册点捕获 + warn，不穿透 | registry.ts:238（inject 对未声明 slot 静默不执行） |
| service 级缺失 / 插件未装 | **框架侧**，插件无执行点，README 声明不可自报 | runtime.ts:392–395；README「框架态降级」表 |

### ⑥ X27 空格=待办 — ✅
- 权威口径：`scripts/control-tower/gen-charter-grid.py:17-18` `COL.get(st, COL["empty"])`
- 四色词表：`green=生效了 / yellow=接了但没生效 / red=缺失 / empty=未填（待办）`
- 真源当前 48 格全 `empty` ⇒ 渲染 **48 格「⚪ 未填」**，测试断言 16×3 全 empty + 混色计数

## 四、收尾三件之二：自验结论（独立成员，非编码兼任）

- **d963-verifier 最终结论：`可提请独立审计`**
- 关键判据（自验员原始输出）：真源端到端 16×3/48 empty；夹具与 origin/main blob sha256 逐字节一致；四色与权威生成器对齐；两项改坏实验均红且零残留；`npm test` **49/49**；写集合规。
- 队长侧独立抽查：adapter 真源跑通（16 行/3 列/48 empty）；`npm test` 49 pass 复跑；`ls-remote` 回执一致。
- **口径**：队内只说「自验结论」；通过与否归 CTO 收件闸 + K3 终审。

## 五、收尾三件之三：遗留清单

1. **三问③ GUI 截图（修复版）未取**：需 Founder 重启桌面 App（host 半进程启动时加载）→ 刷新 → 截图；**未取之前不得声称"已生效"**。
2. **Framework-side 两态**（service 级缺失 / 插件未装）插件物理上不可自报，已在 README 声明 + file:line 依据；K3 若判定须由框架兜底，请转 DSH 上游议题。
3. **环境动作（写集外，CTO 已授权）**：插件副本装入 `~/.dsh-trial-017/profiles/desktop`（live 桌面 profile，备份 `package.json.d963-bak`，`cordis.patch.yml` repoRoot 改写为工作树根）；误装的 `~/.dsh/profiles/web` 已还原至改动前（依赖指回 `/tmp/v-ux`）。**合并后需按正式流程重装并回滚本次临时指向。**
4. **分支含 D521 hook 自动登记提交**（4 个 `chore: bypass COMMITTED 登记`，非 `--no-verify`）；CTO 裁定「随 PR 走」。
5. **数据源新增落 main 后**（#759 已合入 `645d3e20`），面板「有数据」路径即真态；无需改代码，只需重跑验收。
