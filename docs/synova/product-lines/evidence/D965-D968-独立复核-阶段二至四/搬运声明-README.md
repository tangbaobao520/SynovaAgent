# 搬运声明 — D965–D968 独立复核（阶段二至四）

| 项 | 值 |
|---|---|
| **内容作者** | `sentinel-verifier`（独立复核员，未参与任何编码；只读仓库，产物落 `/tmp/verify-d96{5,7,8}/**`） |
| **搬运人** | `synova-squad-lead`（队长）——**原样搬运，未改一字**（延续 CTO 2026-09-25 对阶段一的批准口径） |
| **共享任务** | `task-4` |

## 原样性证据（sha256 前 16 位）

```
f4a793de8782575f   D965-D966-独立复核-evidence-阶段二.md   IDENTICAL
fddd705b61a9603d   D967-独立复核-evidence-阶段三.md        IDENTICAL
07138870d12ad9ab   D968-独立复核-evidence-阶段四.md        IDENTICAL
```
`probes/` 为复核员自建探针（21 个 `.mts`，**未复用队长/CTO/被验方任何脚本**）；`table-45.md` 为其 45 哨兵对照表。

## 效力声明

- 只出 **`自验结论`**，**不含"审计通过"**；通过性判定归 **CTO 收件闸 + K3 终审**。
- 判据纪律：**不使用 grep 型静态判据当验收**；运行时断言 + **改坏即红**；夹具**方言无关**（含金丝雀）。
- 全程只读：各验收工作树 `git status` 为空；生产库 `data/synova.db` sha256 `0d1e4cff…` 未变；红对照只落 `/tmp` 副本且已还原。

## 阶段二至四的关键更正（相对发单方/被验方）

| # | 发单方/被验方 | 复核实测 |
|---|---|---|
| 1 | D966「20 无法复现」 | **20 可复现**（`queryNodes` 方法级计数口径；两种独立测量名单一致） |
| 2 | 「缺 traversal 差 6 件」 | 净差 6 对，但逐件 **7 出 / 1 进**（补 `strategy-capability-fit`） |
| 3 | D968「硬编码数已全部改动态」 | **部分成立**：3 处写死 45 仍在（属 D965 写集、未合入） |
| 4 | 「唯一入口 = 文件驱动」 | 应精确为「唯一入口**种类**」：`bootstrap.ts` 有 **Phase 2a + 3b** 两个调用点做同一注册 |
| 5 | 递归 manifest 口径 58 | 实测 **57（不含根级）/58（含）** ⇒ 判据表需固化 |
| 6 | `check-deprecated-mapping.sh`「裁撤导致退化」 | **预先存在的恒真门禁**（`grep -q` 抑制 `-L` 输出） |

**未能复现项**（诚实登记）：未运行被验方脚本作判据；未跑全仓库 vitest（本轮 = sentinel/integration 全量 + 定向 + e2e + 多处红对照）；`bootstrap.ts` Phase 2a/3b 重复注册未做竞态实测；`decide-next.sh` 残留仅静态定位。
