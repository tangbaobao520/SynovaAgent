# D969 回执 — daily-cto-board 配对测试 + 对账探针 runner + 默认 REPO 去硬编码

> 分支: `feat/d969-daily-cto-board-paired-test` ｜ 基线: `origin/main` = `25e081ce`
> 内容提交: `ee649323`（5 文件）｜ 影子登记提交: `a42b8940`（D521 post-commit，`.claude/bypass.log` +1）
> 交付结论: **可提请独立审计**（不写「通过」——通过与否归主 CTO 收件闸 + K3 终审）
> 本文件为收尾件 1/2/3 的落库载体（M6）；它自身落在内容提交之后的追加提交里。

## 一、收尾件 1 —— diff

内容提交 `ee649323` 的原始输出（`git show --format='' --numstat ee649323`）：

```
42	0	.claude/task-briefs/2026-09-25-D969-daily-cto-board-paired-test.md
48	0	memory/notes/proposed/2026-09-25-d969-daily-cto-board-default-repo.md
44	8	scripts/control-tower/daily-cto-board.sh
23	0	task-state/D969.json
183	0	tests/control-tower/daily-cto-board.test.sh
```

按 D860 治理产物口径：`.claude/task-briefs/**`、`memory/notes/**`、`task-state/**` 属 GOV_PREFIX 且扩展名 ∈ {md,json} ⇒ **不计入 ≤12 文件预算**；
实际计数 = **2 个交付文件**（`scripts/control-tower/daily-cto-board.sh`、`tests/control-tower/daily-cto-board.test.sh`），单域（控制塔）。

## 二、判据逐条 ↔ 证据（声称 ↔ 物理证据）

| # | 声称 | 物理证据（原始输出摘要） |
|---|------|------------------------|
| ① | U7/CT-40 配对门禁由红转绿 | 改前 `SYNO_TEST_ARM=1 SYNO_CT_STAGED="scripts/control-tower/daily-cto-board.sh" bash scripts/control-tower/ct-test-gate.sh` → `❌ … 缺配对测试 tests/control-tower/daily-cto-board.test.sh` / `exit=1`；改后 → `SYNC-OK: 控制塔脚本配对测试全绿 (1 个脚本)` / `exit=0` |
| ② | 配对测试全绿 | `bash tests/control-tower/daily-cto-board.test.sh` → `结果: 25 通过, 0 失败` / `exit=0` |
| ③ | 改坏即红（3 夹具） | A 默认值改回硬编码绝对路径 → `❌ ⑥ 沙箱内无产物`（24 通过/1 失败）+ 注入版把 541B 看板写到沙箱外（注入前目录空 → 注入后有，16:44:57→16:44:59）；B 主流程注入 `exit 3` → `结果: 1 通过, 24 失败`；C 拆掉探针分支 `red=1` → `❌ ④a rc 应为 1，实为 0` / `❌ ④b rc 应为 1，实为 0`（23/2） |
| ③b | 红证不残留（逐字节） | `git hash-object <script>` = `dc6ceb044da71945f0317a10e634966959c36dcc` == `git rev-parse :<script>`；`git diff -- <script>` 行数 = **0**；两文件 `grep -c INJECTED-RED` = **0** |
| ④ | 方言无关 | 测试内 `grep -c 'sed -i'` = 0；`grep -c '\\+'` = 0；`PYBIN` 三级探测（`command -v python3 \|\| python \|\| py`），无裸 `python3` 调用 |
| ⑤ | 门禁自过 | `check-plan-integrity.sh` / `check-brief-parseable.sh` / `check-notes-lifecycle.sh` / `check-silent-swallow.sh --diff` 全 `exit=0`；`check-silent-swallow.sh --utf8 <两文件>` 均 `✅ 带头块` |
| ⑥ | 兄弟测试未回归 | `bash tests/control-tower/daily-cto-board-ratio.test.sh` → `结果: 6 通过, 0 失败` / `exit=0` |
| ⑦ | 本地↔远端一致 | 本地 `git rev-parse HEAD \| cut -c1-12` = `a42b8940d01b`；`git ls-remote --heads origin feat/d969-daily-cto-board-paired-test \| cut -c1-12` = `a42b8940d01b` |

## 三、收尾件 2 —— 自验结论

1. 判据 ①–⑦ **逐条成立**，全部数字取自命令原始输出。
2. **接线承重性**（回应「接线了 ≠ 被执行」）：夹具 C 证明「探针结果 → 看板红项」这条线是**承重**的（拆掉 `red=1` 即两条断言变红）；
   夹具 ① 断言 `✅ 对账探针 ok-probe.sh: 一致 (rc=0)` 证明探针**真的被执行**，非 grep 型静态判据。
3. **默认 REPO 去硬编码**：夹具 A 证明判别性——硬编码绝对路径回归时 `⑥` 必红；schedule 路径不变（只读解析
   `git -C /Users/wane/SynovaAgent/scripts/control-tower rev-parse --show-toplevel` → `/Users/wane/SynovaAgent`，
   与旧硬编码值逐字相同；worktree 侧 → `.synova-wt-ctobatch-a`）。
4. **未由编码自判「通过」**：本文件只给自验结论，标 `可提请独立审计`。

## 四、收尾件 3 —— 遗留清单（诚实登记，不美化）

1. **探针 runner 无超时保护**：`daily-cto-board.sh` 逐个 `bash "$p"` 同步执行；探针若无 `--max-time` 而挂死，看板随之挂起。
   本卡未加（避免扩写集），**登记为待办**，建议 D971 探针自带 `--max-time`。
2. **事故登记（coder-a 误写主工作区，2026-09-25 16:41）**：在 worktree 内跑 `daily-cto-board.sh`，因旧默认硬编码
   `REPO=/Users/wane/SynovaAgent`，改写了主工作区 `docs/synova/coordination/CTO-看板-自动.md`（tracked, 7+/6-）与
   gitignored 日志。队长独立复核：该文件**在本次之前已 dirty**（当日 01:22:26 / 13:21:58 两次 schedule 已改写且未提交）
   ⇒ 本次为**覆盖既有 dirty**，非引入者；裁定**维持现状**、不回退。根因修复已并入本卡（默认值去硬编码）。
3. **`.gitattributes:20` `.claude/reference-map.md merge=union`**：与 D1009 治理对象**同类第二例**（同 latent bug），
   不在本卡写集，**需升级**（是否一并出库由 CTO 裁决）。
4. **Gatekeeper 误报熔断（本次提交的实际阻塞点）**：`origin/main` 的 `.claude/bypass.log` 含 2026-09-25 唯一一条
   `detected-bypass head-mismatch marker=ce5b13b9… parent=2b38d177…`；marker 是 **merge 提交**（`Merge remote-tracking branch
   'origin/main' into HEAD`）、且 marker 与 parent **均不在 main 历史中** ⇒ 陈旧 marker 型误判（`post-commit.sh:43-53` 自陈同类
   D524 已实证）。它使**全队**本地 pre-commit 熔断。本次经队长**书面授权**使用控制塔自带的误报逃生舱 `SYNO_GATEKEEPER_ACK=1`
   （非 `--no-verify`），留痕见 `degraded-events.log`。**候选新卡**：merge 提交的子提交仍走 `head-mismatch` 判定 → FP。
   本批不修（越界），登记为遗留。
5. **本地 pre-commit 1 项软提示（非本卡引入）**：`时间戳顺序: brief 必须早于代码写入: 1 处`，指向
   `2026-09-24T16:31:02+08:00 / .claude/task-briefs/2026-09-24-D941-ownership-generative-rewrite.md`，与 D969 无关。
6. **门禁覆盖边界**：本测试以**桩**替代 `check-dsh-anchor.py` 与 `scripts/pre-commit-check.sh` —— 二者本体不在本测试覆盖范围。

## 五、ACK 使用留痕（队长三条件之 2）

`bash scripts/pre-commit-check.sh`（带 `SYNO_GATEKEEPER_ACK=1`）退出 0，**其余 13 组照常硬跑**（完整 112 行原始输出见本文件**附录**），
`.codex/control-tower/logs/degraded-events.log`（gitignored）留痕：

```
{"time": "2026-09-25T08:47:52Z", "component": "gatekeeper", "reason": "SYNO_GATEKEEPER_ACK=1 放行 1 次 detected-bypass"}
{"time": "2026-09-25T08:48:07Z", "component": "gatekeeper", "reason": "SYNO_GATEKEEPER_ACK=1 放行 1 次 detected-bypass"}
{"time": "2026-09-25T08:48:13Z", "component": "gatekeeper", "reason": "SYNO_GATEKEEPER_ACK=1 放行 1 次 detected-bypass"}
```

**未用 ACK 掩盖任何真实失败**：全 13 组逐组 ✅，唯一非 ✅ 项为上述第 5 条软提示（D941 遗留，非本卡）。


## 附录 —— `SYNO_GATEKEEPER_ACK=1 bash scripts/pre-commit-check.sh` 完整原始输出（112 行，已剥 ANSI 色码）

```
[GATEKEEPER] 检测到今日 1 次 --no-verify 绕过记录
[GATEKEEPER] 已人工确认 (SYNO_GATEKEEPER_ACK=1) — 降级为告警放行本次提交
[GATEKEEPER] 绕过根因仍需修复; 今日记录将在次日自动清零

═══════════════════════════════════════════════════════════
  Loop Engineering V4.5.1 — pre-commit (13 组 + 免疫 + plan-integrity)
═══════════════════════════════════════════════════════════

── 组 1/13: 类型安全 + 硬编码数据 ──
  ✅ as any / as never / as unknown as 零容忍（新增，铁律 38；存量独立清理）
  ✅ from ????: from??????? (D93/D95??)
  ✅ 硬编码业务数据/类型 (禁止硬编码部门名/可扩展实体列表)
  ✅ 旧适配器映射: 全部已标注 @deprecated

── 组 2/13: 测试质量 ──
  ✅ empty catch 无 log (铁律 24+31)
  ✅ 静默吞错扫描 (D313 M5b)
  ✅ 新文件配对: impl 须同 commit 有 test
  ✅ 桩测试: 新测试需 ≥3 expect()
  ✅ 跨模块集成: bridge/context 类需 .integration.test.ts
  ✅ 控制塔脚本测试门禁 (U7/CT-40)

── 组 3/13: Secrets ──

═══ Secrets 扫描 ═══

── 全工作区扫描 ──
  ✅ 工作区无真实凭证

── .claude/ 目录扫描 ──
  ✅ .claude/ 目录无凭证

  ✅ .env 未被暂存
  ✅ .gitignore 包含 .env
  ✅ 暂存文件无硬编码凭证
  ✅ 无 .env 文件

  Secrets 扫描: 全部通过 ✅

── 组 4/13: 接线完整性 ──
  ✅ 接线审计: 新 export 必须被引用
  ✅ 接线深度: 新 export 必须被调用(非仅 import)

── 组 5/13: 架构边界 + 桥接文件 ──
  ✅ 架构边界: 禁止跨层引用 (铁律 39)
  ✅ 铁律 46: 桥接文件欺诈 + 包级 engine-core + 壳包检测

── 组 6/13: Task Brief (6 核心字段) ──
  ✅ 主树占用检测 (D537 #2): 主树脏 + 多活跃 session
  ✅ Task Brief: 编码变更须有今日 task brief
  ✅ Task Brief: 6 核心字段必须填写 (Q0/Q1/Q2/Q3/架构层/Done)
  ✅ 骨架 brief 占位符检测（认领 agent 填写后提交，禁提交骨架）
  ⚠️  时间戳顺序: brief 必须早于代码写入: 1 处  [V5 软提示——CI 为权威，本地不阻断]
     代码在 brief 填写前已写入:\n2026-09-24T16:31:02+08:00 | FILE=/Users/wane/.claude/projects/-Users-wane-SynovaAgent/memory/dsh-electron-source-desktop.md | BRIEF=.claude/task-briefs/2026-09-24-D941-ownership-generative-rewrite.md | 代码在 brief 填写前写入\n解决方法: rm /tmp/.synova-before-brief && git checkout -- . && bash scripts/workflow/task-start.sh
✅ [notes-lifecycle] proposed/ 无僵尸条目
  ✅ Notes 迁移门禁: proposed/ 无僵尸条目
  ✅ plan.principles (3 条, Done verify: 3)
  ✅ plan.approach = extend
  ✅ plan.memory_refs (2 文件, 全部存在)
  ✅ brief 模板已清理 (无 <!-- 残留)
  ✅ Q2 排除项均含文件路径
  ✅ Q2 排除项: 声明不改的文件未在本次提交中出现
  ✅ Done 可证伪性 (无 checked 项)
  ✅ Q0c 跟踪
  ⚠️  PRD 对照: Done 标准引用 PRD 章节(可选): 1 处  [可选提示——永不阻断]
     Done 标准未引用 PRD 章节 - 重大 feature 建议标注 secX.Y

── 组 7/13: 架构合规 ──
  ✅ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口
  ✅ 专家配置校验
  ✅ 门禁故障审计
  ✅ 绕过审计
  ✅ 数据流: 路由文件须含 API 调用证据

── 组 8/13: 文件驱动架构完整性 (V3.9) ──
  ✅ 能力验收 CI (       1 测试, 全部通过)
  ✅ manifest.json 必填字段 ($schema/name/version/type/entryPoint)
  ✅ tags 引用完整性 (所有标签值必须在 tags.json 中存在)
  ✅ 硬编码类型回归 (禁止在 src/ 新增本体类型定义)
  ✅ extensions/ 目录结构 (新子目录须有 manifest.json)
  ✅ pizza-chain 验收测试存在
  ✅ Feature Flag 审计 (新增文件驱动路径须有回退 flag)
── 组 9/13: 契约门禁 ──
  ✅ 契约门禁: 声明产出须在暂存区

── 组 10/13: V3 流水线健康度 ──
  ✅ G10: 无 task brief 变更(跳过)
  ✅ G11: 无 task brief 变更(跳过)

── 组 12/13: Task Scope 一致性 ──
  ✅ G12: 所有文件均在 Q2 范围内
  ✅ G12d: 无 session 提交生成物 (CI 单点)
  ✅ G12b: brief 可解析 (D313 M3)

── 组 13/13: 技能同步一致性 ──
  ✅ G13: 无技能文件变更(跳过)

── D782: 文档真相防线（D1 真相验证 + D2 登记门禁）──
  ✅ D1 文档真相: 全部硬检查通过 (15 ✅)
  ✅ D2 登记门禁: ── 汇总: 检查 0 个文档，0 个未登记 ──

── PR 预算门禁 (D734) ──
  ✅ D734 PR 预算: 文件数 / 单域 / 落后基线 均在预算内
  ✅ V5 平台敏感命令: 新控制塔脚本对照 PLATFORM-CHECKLIST.md (D520)
  ✅ DSH 断面一致性 (D943)

═══════════════════════════════════════════════════════════
  ✅ 全部 13 组通过
  ⚠️  1 项警告 (不阻断)
  ⚠ V5: 1 项软提示（详情见上）——CI 为权威，本地不阻断
═══════════════════════════════════════════════════════════
```
