# D970 回执 — D735 Stage 2：bypass 证据账本出库（含 Gatekeeper/组 7c 读面同步）

> 卡: D970（原号 D1009，CTO 裁决改号：Mac 段 D9xx）｜ 分支: `fix/d970-bypass-ledger-stage2`
> 基线: `origin/main` `057d8ca0`（切支点）→ 合并 `481805a8`
> 提交链: `4336fc00`（内容）→ `545f7a64`（merge origin/main）→ `d3d080c9`（归档冻结保真 + README 注记）→ `<本文件所在提交>`
> 交付结论: **可提请独立审计**（不写「通过」——通过与否归主 CTO 收件闸 + K3 终审；本卡含**门禁语义变更**，K3 事后审必经）

## 一、收尾件 1 —— diff

`git diff --name-only origin/main...HEAD` 共 **18 项**（含删除侧）；`bash scripts/control-tower/check-pr-budget.sh` 的计数口径（`--diff-filter=ACMR`，删除不计）判定 **14 件计入预算 / 3 件治理豁免**：

```
win  .gitattributes
mac  docs/authority/bypass-ledger-archive/README.md
mac  docs/authority/bypass-ledger-archive/bypass-ledger-frozen-2026-09-25.txt
mac  scripts/control-tower/bypass-ledger.sh
mac  scripts/control-tower/check-bypass-log.sh
mac  scripts/hooks/post-commit.sh
mac  scripts/pre-commit-check.sh
mac  tests/control-tower/bypass-ledger.test.sh
mac  tests/control-tower/bypass-union-merge.test.sh
mac  tests/control-tower/check-bypass-log.test.sh
mac  tests/control-tower/post-commit-marker.test.sh
mac  tests/control-tower/post-commit.test.sh
```

⚠️ **未清项（阻塞合并，非代码缺陷）**：`❌ ① 变更文件数 14 > 上限 12` + `❌ ② 变更跨域（.gitattributes 落 win 默认兜底）`。CI 权威面会硬阻断；本地 V5 为软提示。**已上报队长裁决拆 PR 口径**（见 §四-7）。

## 二、判据逐条 ↔ 证据

| # | 声称 | 原始输出 |
|---|------|---------|
| ① | 不再返回 union | 改前 `git check-attr merge -- .claude/bypass.log` → `.claude/bypass.log: merge: union`；改后 → `.claude/bypass.log: merge: unspecified`（对照 `.claude/reference-map.md: merge: union` 未动） |
| ② | 改一次账本零 git 变更 | 追加 1 行探针 → `git status --porcelain -- .claude/bypass.log` = 空；全量 `git status --porcelain | grep -c bypass.log` = **0**；随后撤销探针行 |
| ③ | D331 不再拒推 | `git push --dry-run` → `✅ bypass.log 对账通过: 481805a88bbc7c26a17a1812c82c8b8d6478cdc9..HEAD 全部提交有记录` + `✅ 全部门禁通过 — 允许推送` / rc=0 |
| ④ | 改坏即红（真覆盖） | 沙箱夹具：正控（全部有记录）`exit=0` → 含无记录提交 `exit=1` 且点名 `feat: D1009-INJECTED-RED unrecorded commit [0971ce2c]` → **把检测行改成 `if false; then` 后同一场景 `exit=0`**（证明检测承重）→ 补记后回 `exit=0`（可修复非死锁）；`grep -c 'D1009-INJECTED-RED'`（改号前脚本）= **0** |
| ⑤ | 三态 exit 2 | `bash scripts/control-tower/check-bypass-log.sh nonexistent-remote/nonexistent-branch` → `⚠️ base 引用缺失 … （fail-closed，exit 2 不当作通过）` / `exit=2`；对照 `SYNO_BASE_REF=origin/main` → `exit=0` |
| ⑥ | 改门禁者先过门禁 | `SYNO_GATEKEEPER_ACK=1 bash scripts/pre-commit-check.sh` → `exit=0` / `✅ 全部 13 组通过` |
| ⑦ | 5 条 dirty PR 根因对照 | 5 条 PR head（API 取）：`#761/#756/#757/#754/#742`。**union 在场** → `git merge-tree --write-tree origin/main <head>` 全 `rc=0 CONFLICT行=0`；**移除该属性行后** → 全 `rc=1 CONFLICT行=1`，冲突文件**全部且仅** `.claude/bypass.log` |
| ⑧ | 只改一行 | `git diff --numstat 057d8ca0 HEAD -- .gitattributes` → `0	1	.gitattributes`（**0 增 / 1 删**） |
| 补 | modify/delete 实验（队长要求原样复跑） | 用 `git commit-tree` 造「只做本卡核心动作（删账本）」的仿真提交 `278526532879…`：**[A] union 在场** → `rc=1`；**[B] union 移除** → `rc=1`；两组同为 `CONFLICT (modify/delete): .claude/bypass.log deleted in 27852653… and modified in origin/main.` ⇒ **「删除」而非「union」才是根因**；也解释了本卡自身必须先 merge main 才不 dirty |

## 三、C 批（读面同步）—— 三条判别性夹具 + 逐字不变

新增 `tests/control-tower/pre-commit-bypass-read.test.sh`（14 断言全绿）：

| 夹具 | 结果 |
|---|---|
| ① 逐字不变（旧路径单源） | NEW 与**改动前脚本**（`057d8ca0:scripts/pre-commit-check.sh`）的 `[GATEKEEPER]` 决策行**逐字节一致**且 `exit` 同（1/1） |
| ② 判别性（仅 per-session） | NEW 看得见（`检测到今日 1 次` / `exit=1`）；**改动前脚本看不见**（无 GATEKEEPER 行）⇒ 修的是真缺陷 |
| ③ ACK 放行 | `SYNO_GATEKEEPER_ACK=1` → `已人工确认 (SYNO_GATEKEEPER_ACK=1) — 降级为告警放行`；不含 `[GATEKEEPER] 请使用:`（未阻断） |
| ④ 组 7c 审计 | 3 条 detected-bypass + ACK → `绕过审计: 24h 内 --no-verify 3 次`（读得到合并来源） |
| ⑤ 降级（解析器缺失） | `⚠️ bypass-ledger.sh 缺失 — 回落旧路径单源 [degraded]` + 仍能看见 + `exit=1` |
| ⑥ 边界（来源全空） | `⚠️ bypass 账本合并来源为空（归档/旧路径/per-session 均无）— 计数按 0 [degraded]` + 无 GATEKEEPER 行 |

**执行中发现并修复的真缺陷（去重）**：冻结归档是旧路径的**副本**，`read` 不去重会让 Gatekeeper/组 7c **计数翻倍**（实测：今日 1 条被读成 `2`）。修法：`read` 经 `awk '!seen[$0]++'` 去重（保首次出现顺序）。修复后 `read` 行数 = 1814 = `sort -u` 行数、今日计数 = 1。已加判别性断言（同 1 行同时存在于归档+旧路径 → 只输出一次；且不误删其它行）。

## 四、收尾件 2 —— 自验结论（含未清项）

1. 判据 ①–⑧ + 补实验**逐条成立**；全部数字取自命令原始输出。
2. **本卡 6 个测试全绿**：`bypass-ledger 29 项` / `check-bypass-log 6` / `bypass-union-merge 8`（改为「出库冲突根因回归」：被跟踪+无 union → 冲突；出库后 → 零冲突）/ `post-commit 15`（改写：per-session 落点 + 无影子提交 + 链长 1:1 + 降级不回退）/ `post-commit-marker 18`（断言目标改 per-session）/ `pre-commit-bypass-read 14`。
3. **逐字节恢复**（改坏即红实验）：`bash hash-object` == `git rev-parse :<path>`、`git diff` 行数 0、`grep -c INJECTED-RED` = 0。
4. **事故与污染诚实登记**（见 §五）——未美化。
5. **未自判「通过」**：本文件只给自验结论，标 `可提请独立审计`。

## 五、收尾件 3 —— 遗留清单（不美化）

1. **`scripts/control-tower/synova-commit` 仍是旧路径写者**（`:701/:718/:720/:725/:863/:877/:888`）——队长已裁定**本卡不动**（最高风险脚本）。实测其 D414「随提交入库」块在出库后自失效（`git diff --quiet -- .claude/bypass.log` rc=0 ⇒ 跳过）。**立后续卡**。
2. **`.gitattributes:20` `.claude/reference-map.md merge=union` 同类第二例**（append-only 写者 + GitHub 不认 union）——按派单未动，**需升级**。
3. **`tests/control-tower/tag-bypass-wiring.test.sh` 基线即红**：纯净 clone（`519edadf`，零本卡改动）同样 `exit=1 / 通过 24 失败 2`（`guard 未经 shim 执行`）⇒ 非本卡引入，未修。
4. **`tests/control-tower/fastlane-bypass-only.test.sh` 环境依赖型红**：当仓库暂存区恰为 doc-only 时，CT-34 早退使横幅断言落空；纯净 clone 只 `git add` 一份 doc 即复现同一 2 条失败 ⇒ 非本卡引入。本卡提交后（暂存区清空）复跑恢复绿。
5. **测试污染源（新发现）**：`tests/control-tower/founder-truth.test.sh` 调用**真实** `scripts/control-tower/founder-truth.py`（默认输出路径）⇒ 会改写被跟踪的 `docs/synova/founder-console.html` 与 `docs/synova/founder-alerts.md`（实测 mtime 17:27:16）。本卡已回退这两个文件（非本卡写集；`founder-console.html` 另受 G12d 生成物门禁硬阻断）。**与 `.claude/bypass.log` 同型：测试副作用碰被跟踪文件** ⇒ 建议立卡。
6. **跨机/跨工作区缺口（显式登记）**：冻结之后他机产生的登记只落其本机 `.sessions/`（不随仓走）⇒ 合并他机分支后 D331 仍可能拒推，需沿用一次性补记（D451）。该缺口出库前已存在；甲案不使其恶化，亦**不解决**它。后续方向：让「pre-commit 真跑过」随提交走（提交 trailer / git notes）。
7. **D734 PR 预算超限 → 已按 CTO 裁决拆两支（甲′）**：原 14 > 12 且跨域（`.gitattributes` 落 win 兜底）。
   - **PR-2 `docs/d970-archive-and-ownership`（先合）**：`docs/synova/coordination/ownership.yaml`（把 `.gitattributes` 判 `domain_neutral`，D774 `.gitignore` 先例同型，理由：多主体共写 2 mac/1 win/1 dsh-cto/1 ClawOrg）+ 归档 `README.md` + 归档冻结 `.txt`；自查 `✅ PASS PR 预算内`、`✅ 变更单域: mac`。
   - **PR-1 `fix/d970-bypass-ledger-stage2`（后合，本支）**：`.gitattributes` + `.gitignore` + 4 scripts + 6 tests = 恰 **12 件 / 单域 mac**。
   - 原子性说明：核心修复（union 声明移除 + 账本出库 + 读面切换 + 测试）**全在 PR-1**，未被跨 PR 切开；外移的只有「归档产物 + 归属规则」。

## 六、Gatekeeper ACK 使用留痕（队长批准）

`.codex/control-tower/logs/degraded-events.log`（gitignored）新增多条，形如：
```
{"time": "2026-09-25T08:47:52Z", "component": "gatekeeper", "reason": "SYNO_GATEKEEPER_ACK=1 放行 1 次 detected-bypass"}
```
本卡所有提交均走 `SYNO_GATEKEEPER_ACK=1 bash scripts/control-tower/synova-commit …`（**非 `--no-verify`**），且 `pre-commit` 其余 13 组照常硬跑（`✅ 全部 13 组通过`）。**未用 ACK 掩盖任何真实失败**——唯一非 ✅ 项是 §五-7 的预算未清项与 §五 的既有软提示。

## 七、PR 可合性预检（GitHub 语义：不认 merge=union）

在 D970 worktree（其 `.gitattributes` **已无** `bypass.log merge=union`，故本地 `merge-tree` 即等值 GitHub 语义）实测：

```
$ git merge-tree --write-tree origin/main fix/d970-bypass-ledger-stage2        → rc=0 CONFLICT行=0
$ git merge-tree --write-tree origin/main feat/d969-daily-cto-board-paired-test → rc=0 CONFLICT行=0
```

⇒ 本卡与 D969 两条分支的 **tip 相对 origin/main 均无冲突**（`merge origin/main` + 接受删除侧 的处置有效）。
