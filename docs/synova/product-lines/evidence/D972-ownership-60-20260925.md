# D972 交付回执 — ownership 测试 58→60 用例（缩卡）+ CODEOWNERS 无 BOM

> 卡片: `task-state/D972.json` | 执行: coder-b（并行 CTO 小队） | 日期: 2026-09-25
> 分支: `fix/d972-ownership-60-cases`（base = `origin/main` `057d8ca0`）
> 结论: **可提请独立审计**（本件不写「通过」；通过与否归 CTO 收件闸 + K3 终审）

## 〇、收尾三件

| # | 件 | 位置 |
|---|---|---|
| 1 | diff | `git show --stat 323711cf`（3 文件 / 114 insertions） |
| 2 | 自验结论 | 本文件 §二（判据逐条挂命令原始输出） |
| 3 | 遗留清单 | 本文件 §四 |

> base 变更留痕：卡面写 base = `25e081ce`，但开工前 `git fetch --all` 实测 `origin/main` 已前移到
> `057d8ca0`（PR #771 / D964-P1b）⇒ 按 D334「禁在 behind 状态开工」**重建分支到 `057d8ca0`**，
> 重建后复测基线仍为 58 项。

## 一、声称 ↔ 证据逐条对应

### ① 58 → 60 用例

```
── 改前（base 057d8ca0 基线实跑）──
  ✅ 全部通过: 58 项          exit=0

── 改后 ──
  ✅ 全部通过: 60 项          exit=0

── §5c 段原始输出 ──
── 5c. D972 判别性夹具: .dsh→mac（沿用 §5b D935 同范式；删该规则即红 = 判据真读数据）──
  ✅ D972 .dsh 路径 = Mac (exit=0)
  ✅ D972 删 .dsh 规则 → .dsh 路径派 mac 必红 (exit=1)
```

### ② CODEOWNERS 无 BOM

```
$ head -c 3 .github/CODEOWNERS | od -An -tx1
           23  20  2e
```
首 3 字节 = `23 20 2e`（`# .`），**非 `ef bb bf`** ⇒ 无需重生成 ⇒ `.github/CODEOWNERS`
**未纳入本次 diff**（与卡面「仅当实测有 BOM 或需重生成才动」一致）。测试第 7 节既有
「CODEOWNERS == --emit-codeowners 逐字节一致」drift 断言在本轮 60 项中继续通过。

### ③ 改坏即红（判别性夹具，非 grep 型静态判据）

**工具层证据（删规则 ⇒ 真红）**：沙箱副本删掉 `.dsh/**` 规则后，同一路径判 mac 由 0 翻为 1：

```
$ python3 scripts/control-tower/check-ownership.py .dsh/skills/squad-discipline/SKILL.md --owner mac --yaml <原 yaml>
mac  .dsh/skills/squad-discipline/SKILL.md
✅ PASS 1 个文件全部归属 owner=mac（无归属 0）        exit=0

$ python3 scripts/control-tower/check-ownership.py .dsh/skills/squad-discipline/SKILL.md --owner mac --yaml <删掉 .dsh 规则的沙箱副本>
win  .dsh/skills/squad-discipline/SKILL.md
❌ 越域: … —— 声明 owner=mac，实际 owner=win
❌ FAIL 越域 1 处（声明 owner=mac）                   exit=1
```

**测试层证据（断言非空壳）**：把夹具的删除动作注入为 no-op（`end = start  # INJECTED-RED`）：

```
$ bash tests/control-tower/check-ownership.test.sh; echo "exit=$?"
  ❌ D972 改坏前置: 沙箱副本里 .dsh 规则未删掉（后续必红断言不可信）
  ❌ D972 删 .dsh 规则 → .dsh 路径派 mac 必红 — 期望 exit=1 实际 exit=0
  ❌ 2 项失败 / 59 项通过
exit=1
```

恢复后：

```
$ bash tests/control-tower/check-ownership.test.sh; echo "exit=$?"
  ✅ 全部通过: 60 项
exit=0

$ grep -c 'INJECTED-RED' tests/control-tower/check-ownership.test.sh
0
$ git diff | grep -c 'INJECTED-RED'
0
```

### ④ 配对门禁

```
$ SYNO_TEST_ARM=1 SYNO_CT_STAGED="scripts/control-tower/check-ownership.py" bash scripts/control-tower/ct-test-gate.sh
SYNC-OK: 控制塔脚本配对测试全绿 (1 个脚本)          exit=0

$ SYNO_GATEKEEPER_ACK=1 bash scripts/pre-commit-check.sh
  ✅ 全部 13 组通过                                  exit=0
  （仅 1 项 V5 软提示：时间戳顺序 1 处，属 D941 既有项，非本卡文件）
```

## 二、缩卡依据（卡面前提复核）

| 卡面声称 | 我实测 | 处置 |
|---|---|---|
| 「ownership.yaml 两条 glob 未落」 | **已落**：`docs/synova/presets/**`（L148）、`.dsh/**`（L169） | 推翻 ⇒ 按队长口径缩卡，不重做落表 |
| 「补 2 项用例至 60」 | 落 `.dsh/**`（真缺口） | 执行 |
| 「建议落 presets + .dsh 两处」 | `presets/**` 已被 **§5b（D935）覆盖 14 处**（含删规则夹具） | 不重复堆用例；改为把**同范式**施于零覆盖的 `.dsh/**` |

实测覆盖缺口证据：

```
$ grep -n '\.dsh' tests/control-tower/check-ownership.test.sh   → 零结果（§5c 之前）
$ grep -c 'presets' tests/control-tower/check-ownership.test.sh → 14
```

⇒ 新增的 2 项**不是为凑数**：`.dsh/**` 此前零覆盖，且带判别性夹具。

## 三、Agent 自检 5 问

1. **接线**：测试文件是 `scripts/control-tower/check-ownership.py` 的配对测试，由 `ct-test-gate.sh`
   （U7/CT-40）按 `tests/control-tower/<name>.test.sh` 规则自动发现，实测 SYNC-OK。
2. **异常/降级**：本卡只改测试；测试自身沿用既有 PYBIN 三级探测（探存在性 + 试运行），无 Python → exit 2。
3. **类型安全**：无 TS 代码。
4. **测试质量**：新增 2 项均为**可判别**断言（正例 + 删规则必红），非空壳；已证「改坏即红」。
5. **残留清理**：`INJECTED-RED` 在本文件与本次 diff 中计数均为 0；沙箱副本经 `trap` 清理；
   未修改 `ownership.yaml` / `check-ownership.py` / `.github/CODEOWNERS`。

## 四、遗留清单（未清项）

1. **`.github/CODEOWNERS` 未纳入 diff** —— 实测无 BOM 且 drift 断言通过 ⇒ 按卡面「仅当需要才动」不动。
   其 BOM/drift 的长期保障依赖测试第 7 节既有断言（本卡未新增守卫）。
2. **`presets/**` 未新增用例** —— 已被 §5b（D935）覆盖 14 处，本卡**刻意不重复**；
   若队长要求「严格按卡面两处各补一项」，则需追加 presets 重复项（属凑数，建议不做）。
3. **`grep -c INJECTED-RED` 的全仓计数非 0** —— `origin/main` 基线本就有 7 文件 13 处该字样
   （历史证据/规范文档），非本卡引入；本卡口径为「被改文件内 = 0」且「diff 新增面 = 0」（两者均实测 0）。
4. **未跑重型验证** —— 本卡为测试文件 + 簿记，无产品码改动，未跑 vitest / 全量门禁 / 黄金门禁。
5. **无 Note** —— 按队长口径，本卡写集（`tests/**` + `.claude/**` + `task-state/**`）不命中
   `commit-msg-check.sh` 的 Note 触发面（`scripts/{control-tower,workflow,hooks}/` 等），故未新建 Note。

## 五、SHA 回执

```
$ git rev-parse HEAD | cut -c1-12
747caa6c168e
$ git ls-remote --heads origin fix/d972-ownership-60-cases | cut -c1-12
747caa6c168e
$ git log -3 --format='%h %s'
747caa6c chore: bypass COMMITTED 登记 (auto hook, D521)        ← post-commit hook 影子登记
323711cf test(D972): ownership 测试 58→60 用例（.dsh/** 判别性夹具）+ CODEOWNERS 无 BOM 核查   ← 本卡实体提交
057d8ca0 feat(D964-P1b): check-citations 加 archive/** 豁免根（归档断链不误拦） (#771)      ← base
```

本地 HEAD 与远端 **12 位一致**（`747caa6c168e`）；本卡实体提交 = `323711cf`。

---

## 六、D972 改号（CTO 裁决：D1000–D1099 属 Win 侧号段）

| 项 | 旧 | 新 |
|---|---|---|
| 卡号 | D1011 | **D972** |
| 分支 | `fix/d1011-ownership-60-cases` | `fix/d972-ownership-60-cases` |
| task-state | `task-state/D1011.json` | `task-state/D972.json` |
| brief | `.claude/task-briefs/2026-09-25-D1011-ownership-60-cases.md` | `…-D972-ownership-60-cases.md` |
| 本回执 | `…/evidence/D1011-ownership-60-20260925.md` | `…/evidence/D972-ownership-60-20260925.md` |

做法：`git branch -m` + `git mv`（3 文件）+ 内容全量替换 `D1011`→`D972` / `d1011`→`d972`
（含 `tests/control-tower/check-ownership.test.sh` 内 5 处引用 —— 该文件不含号在文件名里，首轮替换漏过，已补）。
**无 force push**；旧远端分支名按队长指令删除。残留检查：`git grep -c 'D1011\|d1011'` → **0**。

### 6.1 改号后判据复跑（新分支上）

```
$ bash tests/control-tower/check-ownership.test.sh
  ✅ 全部通过: 60 项            exit=0
$ head -c 3 .github/CODEOWNERS | od -An -tx1
           23  20  2e          （非 ef bb bf，无 BOM）
```

### 6.2 D708 夹带修复（本批系统性缺口）

`task-state/D972.json` 的 `write_set` 补入本卡自产治理产物
`docs/synova/product-lines/evidence/D972-ownership-60-20260925.md`
（原缺失 ⇒ D708 判「写集外文件夹带」⇒ `quality` 红 ⇒ `test` 被 skip ⇒ 必需 Vitest 永不报告）；
brief Q2「做什么」清单同步补入（G12 范围一致性）。write_set 现 4 项。

### 6.3 verifier 结论（供参考，本卡未自证）

verifier 已判本卡 **绿 · 可提请独立审计**（60 项、无 BOM `23 20 2e`、他自造夹具删 `presets/**` 与 `.dsh/**` 均 exit 1）。
本卡在改号后**仅复跑**判据以保证仍绿，未重跑全部判据（队长口径）。
