  ✅ python 语法编译通过
═══════════════════════════════════════════════════════════
  D979 共享声明解析器 — 测试
  SUT: /Users/wane/SynovaAgent/.synova-wt-d979-verify/scripts/control-tower/shared_file_decl.py
═══════════════════════════════════════════════════════════

── 1. 文本载体 .sh: '# 共享声明: D582, D979' → 允许 ──
  ✅ 文本载体允许 (exit=0)
  ✅ 输出 DECL_ALLOW
  ✅ 来源 = 暂存 index（读取顺序首位）
  ✅ 证据含声明原文

── 2. 文本载体 全角冒号/逗号: '共享声明：D582，D979' → 允许 ──
  ✅ 全角标点允许 (exit=0)

── 3. 文本载体 小写: '# 共享声明: d582, d979' → 允许（归一化）──
  ✅ 小写声明 + 小写 D# 入参允许 (exit=0)
  ✅ D# 归一化为大写

── 4. 边界: 列表后的散落 D# 不计入 SHARED(F) ──
  ✅ 列表内双方命中允许 (exit=0)
  ✅ HIT 仅含列表内 D#
  ✅ 纯函数读取成功 (exit=0)
  ✅ SHARED(F) 仅含列表内 D#（尾随 D123 不计入）

── 5. json 结构化: {"shared_with": ["D582","D979"]} → 允许 ──
  ✅ json 结构化允许 (exit=0)
  ✅ 证据含结构化键

── 6. yml flow（裸）: shared_with: [D582, D979] → 允许 ──
  ✅ yml flow 裸数组允许 (exit=0)

── 7. yml flow（引号 + 中文键）: 共享声明: ["D582", "D979"] → 允许 ──
  ✅ yml flow 引号数组允许 (exit=0)

── 8. 降级: 无声明 → 拒绝（fail-closed）──
  ✅ 无声明拒绝 (exit=1)
  ✅ 输出 DECL_DENY
  ✅ 原因行列出缺双方 D#

── 9. 双向强制: 仅声明自己（只有 D979）→ 拒绝 ──
  ✅ 单独把自己加进声明不放行 (exit=1)
  ✅ 缺 CLAIM_DID=D582

── 10. 逐文件强制: 两文件其一未声明 → 拒绝 ──
  ✅ M 中每个 F 都要命中 (exit=1)
  ✅ 原因行点名未声明文件
  ✅ 校验文件数 = M 大小

── 11. 边界: .py 内的文本声明行不认（规格 ⒝ 扩展名白名单）→ 拒绝 ──
  ✅ 非白名单扩展名不认声明 (exit=1)

── 12. 读取顺序: 暂存版含声明 + 工作区版删掉 → 允许（读 index）──
  ✅ index 版本优先（提交内容 = index 内容） (exit=0)
  ✅ 来源标注 index

── 13. 读取顺序: 暂存版无声明 + 工作区版有 → 拒绝（不越过 index 下探）──
  ✅ index 命中即定案（工作区未暂存声明不放行） (exit=1)

── 14. 读取顺序: 文件未入 index（仅工作区）→ 允许 + SOURCE=worktree ──
  ✅ 工作区回退允许 (exit=0)
  ✅ 来源标注 worktree

── 15. 读取顺序: 文件仅存于 HEAD → 允许 + SOURCE=HEAD ──
  ✅ HEAD 回退允许 (exit=0)
  ✅ 来源标注 HEAD

── 16. 降级: 三处皆不可读 → SOURCE=missing + 拒绝 ──
  ✅ 不可读 → 按未声明处理（拒绝） (exit=1)
  ✅ 来源标注 missing

── 17. 三态: 空文件列表 / 非法 D# → exit 2 ──
  ✅ 空文件列表 = 调用错误 (exit=2)
  ✅ 非法 --msg-did = 调用错误 (exit=2)
  ✅ 缺 --files-from = 调用错误 (exit=2)

── 18. 纯函数: 非法 JSON / 未声明扩展名 → 空集（不抛异常）──
  ✅ 纯函数调用不抛异常 (exit=0)
  ✅ 非法 JSON → 空集
  ✅ 文本载体解析命中双方

═══════════════════════════════════════════════════════════
  结果: 40 通过 / 0 失败
═══════════════════════════════════════════════════════════
