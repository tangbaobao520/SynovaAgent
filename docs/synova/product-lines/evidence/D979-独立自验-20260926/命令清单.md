# D979 独立自验 · 我实际运行的命令清单（可复现）

工作树：`/Users/wane/SynovaAgent/.synova-wt-d979-verify`（detached）
最终被验对象：`fe01c3af1f561b5bc45e130624202c740edb925d`（= `4a7a7d90` + `.claude/bypass.log` 一行）
夹具/临时目录：`/tmp/d979-verify/`（我的唯一可写处，除本证据目录）

## 0. 定位与版本

```bash
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify checkout fe01c3af
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify rev-parse HEAD
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify log --oneline -3
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify diff --stat 4a7a7d90 fe01c3af     # 仅 .claude/bypass.log +1
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify show fe01c3af -- .claude/bypass.log
git -C /Users/wane/SynovaAgent/.synova-wt-d979-impl ls-remote --heads origin | grep d979
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify diff feebf651 fe01c3af -- scripts/commit-msg-check.sh
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify diff --name-status feebf651 4a7a7d90
```

## 1. H 条 —— base 前提复核

```bash
cd /Users/wane/SynovaAgent/.synova-wt-d979-verify            # base feebf651
bash /tmp/d979-verify/h_repro.sh /Users/wane/SynovaAgent/.synova-wt-d979-verify D977   # → H-前提复核-D977.txt
bash /tmp/d979-verify/h_repro.sh /Users/wane/SynovaAgent/.synova-wt-d979-verify D979   # → H-前提复核-D979.txt
bash scripts/workflow/resolve-commit-brief.sh "docs/synova/coordination/派单模板.md"   # 选 D1007（不认领该文件）
git grep -l -F "docs/synova/coordination/派单模板.md" -- .claude/task-briefs           # 7 张精确认领
```
脚本内循环体：
```bash
MSG=$(mktemp); printf 'fix(D977): 前提复现\n' > "$MSG"
for f in ".github/workflows/ci.yml" "task-state/D954.json" "docs/synova/coordination/派单模板.md"; do
  echo "=== $f ==="; SYNO_STAGED_FILES="$f" bash scripts/commit-msg-check.sh "$MSG"; echo "exit=$?"
done
```

## 2. 夹具构建 / 用例矩阵（A–C + 追加）

```bash
bash /tmp/d979-verify/build_fixture.sh  <src_tree> /tmp/d979-verify/fx-base          # v1 夹具（base 红基线）
bash /tmp/d979-verify/build_fixture2.sh <src_tree> <dest>                            # v2 夹具（HEAD 已提交 + 多载体）
bash /tmp/d979-verify/run_cases.sh /tmp/d979-verify/base-src /tmp/d979-verify/cases_base.log
bash /tmp/d979-verify/run_cases.sh /Users/wane/SynovaAgent/.synova-wt-d979-verify /tmp/d979-verify/cases_t1.log
```
用例集（每例独立沙箱，逐个重建夹具）：A / B / C / M1 / M2 / O1 / O2 / X1 / DEL / PM / N1 / N2 / N3。
每例执行体：
```bash
cd "$FX"
MSG=$(mktemp); printf 'fix(D979): 夹具提交\n' > "$MSG"
SYNO_STAGED_FILES="$STAGED" bash scripts/commit-msg-check.sh "$MSG"; echo "exit=$?"
```

## 3. D 条 —— 判别性夹具（变异→红，恢复→绿）

```bash
grep -c '\[ "$DECL_RC" = 0 \]' scripts/commit-msg-check.sh    # =1（变异目标唯一）
grep -c 'DECL_RC=0$'          scripts/commit-msg-check.sh    # =1
bash /tmp/d979-verify/run_D.sh /Users/wane/SynovaAgent/.synova-wt-d979-verify /tmp/d979-verify/D_mutation.log
```
`run_D.sh` 内部每次变异后的固定动作：
```bash
( cd "$FX" && bash -n scripts/commit-msg-check.sh ); echo "bash_n_exit=$?"    # 证明非崩溃红
( cd "$FX" && SYNO_STAGED_FILES="$SHARED" bash scripts/commit-msg-check.sh "$MSG" )
```
变异（python 精确字符串替换，命中即断言）：
- `[ "$DECL_RC" = 0 ]` → `[ "$DECL_RC" = 99 ]`
- `DECL_RC=0\n          DECL_OUT=` → `DECL_RC=1\n          DECL_OUT=`

## 4. E 条 —— 零回归

```bash
bash tests/control-tower/commit-msg-consistency.test.sh > /tmp/d979-verify/E1_commit-msg-consistency.log 2>&1; echo $?
bash tests/control-tower/shared_file_decl.test.sh       > /tmp/d979-verify/E2_shared_file_decl.log 2>&1; echo $?
```
（跑前查任务板：无并发重型验证；跑后 `git status --short` 除证据目录外干净。）

## 5. F / G 条

```bash
grep -n "疑似并行劫持" scripts/commit-msg-check.sh
grep -c "疑似并行劫持" scripts/commit-msg-check.sh
grep -rn "疑似并行劫持" scripts/ tests/
grep -nE "SYNO_SKIP|no-verify|NO_VERIFY|D328_SKIP|SKIP_D328" scripts/commit-msg-check.sh
grep -nE "SYNO_SKIP|no-verify|NO_VERIFY|SYNO_|os.environ|getenv" scripts/control-tower/shared_file_decl.py
git diff feebf651 fe01c3af -- scripts/commit-msg-check.sh | grep -nE "^\+.*exit "
git diff feebf651 fe01c3af -- scripts/commit-msg-check.sh | grep -nE "^-.*exit "
grep -n "exit 0" scripts/commit-msg-check.sh
grep -rn "INJECTED-RED\|FIXME-TEMP\|XXX-RED" scripts/ tests/; echo "grep_exit=$?"
git diff HEAD --stat -- scripts/                                        # 空 = 工作区未被改动
git show HEAD:scripts/commit-msg-check.sh | grep -n "DECL_RC"
shasum -a 256 scripts/commit-msg-check.sh
git show HEAD:scripts/commit-msg-check.sh | shasum -a 256
```

## 6. 追加反例与性能

```bash
# Z1: MSG_DID 空（chore 无 scope）+ 声明齐全 → 必须仍拒
printf 'chore: 无 D# 声明\n' > "$M1"; SYNO_STAGED_FILES="$SHARED" bash scripts/commit-msg-check.sh "$M1"
# Z2: CLAIM_DID == MSG_DID → 零回归放行
printf 'fix(D945): 同卡提交\n' > "$M2"; SYNO_STAGED_FILES="$SHARED" bash scripts/commit-msg-check.sh "$M2"
# 真实共享件在 T1 上的行为（仍红，因文件内尚无声明）
for f in ".github/workflows/ci.yml" "task-state/D954.json" "docs/synova/coordination/派单模板.md"; do
  SYNO_STAGED_FILES="$f" bash scripts/commit-msg-check.sh "$MSG"; echo "exit=$?"
done
# 性能（放行/拒绝各 3 次，base 对照 1 次）——见 Z-边界与性能.txt
```
