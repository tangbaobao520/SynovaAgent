# D979 独立自验报告（T2）

> **自验结论：通过**（A–H 全部判据符合预期，另加队长追加 3 条与 2 条边界反例）
> ⚠️ 本报告是**自验结论**，**不是审计结论**。判定"通过/不通过"归 CTO 收件闸 + K3 终审；
> 本报告只声明"我按 task-2 判据独立复算后所见"，不代行 K3。

- 任务号：**D979**（T2 独立自验，共享任务 `task-2`）
- 验证对象：`fe01c3af1f561b5bc45e130624202c740edb925d`（分支 `fix/d979-d328-shared-decl` HEAD，已在 origin）
  - 代码提交 = `4a7a7d90`；`fe01c3af` 仅追加 `.claude/bypass.log` 一行 `COMMITTED | pre-commit PASS`（非 bypass）。
  - 独立核验：`git diff --stat 4a7a7d90 fe01c3af` = `.claude/bypass.log | 1 +`（只有这一处）。
- 基线：`feebf651`（base，未打补丁）
- 我的工作树：`/Users/wane/SynovaAgent/.synova-wt-d979-verify`（detached；**未复用** T1 工作树 `.synova-wt-d979-impl`）
- 日期：2026-09-26 | 自验员：`verifier`（独立自验员，**未参与编码**）
- 被验文件 = **提交内容本身**（非工作区副本）：`git diff HEAD` 空；worktree 与 commit blob 的 sha256 均为
  `be6b759074c9fceee7faaf37410b1fc9f7cb2b6b0dde54407c9403bade6e3352`

---

## 一、A / B / C 三条反例（自建独立夹具，非采信 T1 测试）

夹具：`/tmp/d979-verify/{build_fixture2.sh,run_cases.sh}`（脚本原文也在本目录 `.txt`）。
机制：`SYNO_STAGED_FILES` 注入暂存集 + 假 commit message 文件；夹具内放"被 D945 brief 认领的沙箱文件"，
`CLAIM_DID=D945` 恒定可控，故 `MSG_DID≠CLAIM_DID` 分支必然进入。

| 用例 | 设计 | BASE `feebf651` | T1 `fe01c3af` | 判据 |
|---|---|---|---|---|
| **A** 授权（单共享件，声明 D945+D979） | msg=D979 / 认领=D945 | exit=1 ❌劫持 | **exit=0 ✅放行** | ✅ 符合 |
| **B** 非授权（MSG_DID 不在声明） | msg=D981 | exit=1 | **exit=1 ❌劫持** `MISSING=D981` | ✅ 符合 |
| **C** 真劫持回归（无声明普通件） | msg=D979 | exit=1 | **exit=1 ❌劫持** `DECL=(无)` | ✅ 符合 |

原始输出：`A-C-用例矩阵-T1-fe01c3af.txt`（T1 全 13 例完整输出）、`A-C-用例矩阵-base-feebf651.txt`（base 对照）。

**A 条的关键点**：base 上 A 就是 exit=1（红）→ 说明"放行"确实**不是门禁本来就不拦**；T1 上变绿并打印
`✅ D328 共享声明放行（D979 FIX-011 ⒝）` + `DECL_ALLOW` + `SOURCE=index` 证据行。

## 二、D 条 —— 判别性夹具（本次自验核心）

做法：在 **/tmp 夹具副本**里停用 T1 新增的放行分支（我的写集不含 `scripts/`，仓库脚本一字未动，
由 sha256/blob 一致性与 `git diff HEAD` 为空证明等价），每次变异后先 `bash -n` 再跑反例①：

| 步骤 | 变异 | `bash -n` | 反例① 结果 |
|---|---|---|---|
| D-1 | 无（原始） | exit=0 | **exit=0 ✅放行**（打印放行证据行） |
| D-2 | 放行条件 `[ "$DECL_RC" = 0 ]` → `[ "$DECL_RC" = 99 ]` | **exit=0**（脚本仍可解析） | **exit=1 ❌劫持** |
| D-3 | 放行初值 `DECL_RC=0` → `DECL_RC=1`（放行永不成立） | **exit=0** | **exit=1 ❌劫持** |
| D-4 | 重建夹具回到原始 | exit=0 | **exit=0 ✅放行** |

**区分"语义红"与"崩溃红"**：D-2/D-3 两次变异后 `bash -n` 均 **exit=0**（脚本语法合法、未崩溃），
且输出是**原劫持判定文案**（`❌ D328: 提交声明(D979)与暂存文件归属(D945)不一致 — 疑似并行劫持`）——
即走的是 **(a) 回到原劫持判定而红**，**不是 (b) 脚本崩溃而红**。原始输出：`D-判别性夹具-变异红绿两次.txt`。
另：变异目标唯一性预检 `grep -c '[ "$DECL_RC" = 0 ]'`=1、`grep -c 'DECL_RC=0$'`=1。

## 三、E 条 —— 零回归

| 套件 | 结果 |
|---|---|
| `bash tests/control-tower/commit-msg-consistency.test.sh` | **30 通过 / 0 失败**（exit=0），含新增用例 14–18 |
| `bash tests/control-tower/shared_file_decl.test.sh` | **40 通过 / 0 失败**（exit=0），含三态 rc=2、读取顺序 index/worktree/HEAD |

原始输出：`E1-零回归-commit-msg-consistency.txt` / `E2-零回归-shared_file_decl.txt`（全文，未截断）。
串行纪律：跑前查任务板，仅 `task-2`（我）在跑，无并发重型验证；两套件均为轻量 bash（无 vitest/tsc）。
跑后 `git status --short` 除我的证据目录外干净 → 测试无仓库副作用。

## 四、F 条 —— 无豁免后门

- `grep -n "疑似并行劫持" scripts/commit-msg-check.sh` → **唯一一处：149 行**，`grep -c` = **1**。
- `scripts/` + `tests/` 全量命中该文案的文件 = 3：门禁脚本 149 行（唯一拦截点）、
  `shared_file_decl.py:7`（docstring 叙述）、测试断言（`commit-msg-consistency.test.sh:410`）——**无第二处拦截/放行实现**。
- 逃生舱扫描：`SYNO_SKIP|no-verify|NO_VERIFY|D328_SKIP|SKIP_D328` 在 `commit-msg-check.sh` **0 命中**；
  `SYNO_SKIP|no-verify|NO_VERIFY|SYNO_|os.environ|getenv` 在 `shared_file_decl.py` **0 命中**。
- diff 级：新增行只有 1 处 `exit 1`（劫持），删除行只有原 1 处 `exit 1`（位置搬迁）；**无新增 `exit 0`**。
  脚本内 `exit 0` 仅 3 处：12 行（MERGE_HEAD 豁免，原有）、20 行（Merge/Revert 豁免，原有）、204 行（脚本尾，原有）。
- `task-state/D979.json:128` 出现的 `DECL_RC=1` 是 **T1 原始代码的 fail-closed 初值**（提交 blob 内 128/134/137/139/146 行原始输出见 `F-G-无后门与红证.txt`），非我的变异残留。

## 五、G 条 —— 红证不残留

- `grep -rn "INJECTED-RED\|FIXME-TEMP\|XXX-RED" scripts/ tests/` → **命中数 0**（grep exit=1）。
- 我的全部变异/注入只存在于 `/tmp/d979-verify/**` 夹具副本；仓库内脚本与提交 blob 完全一致（sha256 相同、`git diff HEAD` 空）。

## 六、H 条 —— 前提复核（base `feebf651`，独立复算）

| 共享件 | BASE 结果 | 说明 |
|---|---|---|
| `.github/workflows/ci.yml` | **exit=1 ❌劫持**（认领 brief D945） | 前提**成立** |
| `task-state/D954.json` | **exit=1 ❌劫持**（认领 brief D954） | 前提**成立** |
| `docs/synova/coordination/派单模板.md` | **exit=0（未拦）** | 前提**不成立**，如实登记 |

**第 3 条不拦的机理（非修复、非漏拦）**：本工作树 resolver 对它有 7 张精确认领 brief，
但 `resolve-commit-brief.sh` 落到 **D718 强锚点空 → D317「最新日期可解析」最终回退**，
选中 `2026-09-25-D1007-M9-…md`（Q2 不含该文件）→ `GENUINE=0` → 既有「无真实认领 → 跳过（G12 兜底）」
fail-open → exit=0。另两条的选中 brief **真认领**（GENUINE=1）→ D328 生效。
原始输出：`H-前提复核-D977.txt`、`H-前提复核-D979.txt`（MSG_DID 取 D977/D979 各一遍，结论一致）。

**队长裁定（已收到）**：H 口径改为 **≥1 条**（我 2 条）+ 第 3 条依 brief 池登记，不追 3/3。
队长主工作区（HEAD=b8573c7a）第 3 条 exit=1（认领 D943-cto-a-items）——**属队长提供，未经我独立复算，不计入我的 A–H 结论**。

## 七、队长追加的 3 条（我独立构造并执行）

| 追加项 | 用例 | T1 结果 | 判据 |
|---|---|---|---|
| ① 多文件混合不得整批放行 | **M1** 共享件（已声明）+ 普通件（无声明） | **exit=1 ❌** `FILES=2`，点名 plain 件 `MISSING=D945,D979` | ✅ 逐文件强制成立 |
| ② 删除态走 HEAD 回退 | **DEL** `git rm -f` 共享件（index/worktree 均无） | **exit=0 ✅** `SOURCE=HEAD` `DECL_ALLOW` | ✅ Phase 3 迁移可用 |
| ③ 解析器失败不得构成放行口 | **PM** 删除 `shared_file_decl.py`（模拟部署残缺） | **exit=1 ❌** + `⚠ D328 共享声明检查 degraded (rc=2) … 按未声明处理（不静默放行）` | ✅ fail-closed |

另加：**M2** 两个共享件均双向声明 → exit=0（正例边界）；**N1** json 结构化载体
（`task-state/*.json` 同型）→ exit=0；**N2** 非法 json（声明不可解析）→ exit=1 fail-closed；
**N3** yml flow 载体（`ci.yml` 同型）→ exit=0。

## 八、补充边界（我自行加判）

| 用例 | T1 结果 | 判据 |
|---|---|---|
| **O1** 单侧声明（只声明 D979，未声明认领方 D945） | **exit=1 ❌** `MISSING=D945` | ✅ "单独把自己加进声明不足以放行"成立 |
| **O2** 错认领（声明 D979+D981，认领方 D945） | **exit=1 ❌** `MISSING=D945` | ✅ 双向严格 |
| **X1** 三处皆不可读（missing 文件） | **exit=1 ❌** `SOURCE=missing` | ✅ fail-closed |
| **Z1** MSG_DID 为空 + 声明齐全 | **exit=1 ❌** `提交声明(无)` | ✅ 未把"无声明归属"洗成放行 |
| **Z2** CLAIM_DID == MSG_DID（=D945） | exit=0 | ✅ 不进新分支，零回归 |

**全量矩阵**：BASE **13/13 exit≠0（全红）**；T1 **5/13 exit=0**（A、M2、DEL、N1、N3——恰好是"逐文件双向声明命中"的 5 例），
**8/13 exit=1**。base↔T1 差异面与设计完全吻合 → 放行由新增代码产生，且只在应放行处放行。

## 九、性能（条件跳过，<1s 口径）

T1 放行路径 0.378 / 0.382 / 0.371s；拒绝路径 0.365 / 0.364 / 0.364s；base 对照 0.290s
（新增约 +0.07~0.09s，含 1 次 python 进程）。原始输出：`Z-边界与性能.txt`。

## 十、我**未能**验证的部分（诚实登记）

1. **Windows 实机未跑**：仅静态核对（`sed -E` 无 GNU `\+` 依赖、中文字符串变量用 `${VAR}` 花括号、
   `cygpath -w` 双处转换、PYBIN 回退沿用、`--files-from -` 用 printf 喂 stdin）。**未在 Windows/MSYS 执行**。
2. **未跑全量门禁/CI**：未跑 `scripts/pre-commit-check.sh`（13 组）、未跑 vitest、未跑 CI；
   本次只跑 2 个专项 bash 套件（E 条）。理由：变更为 bash+python 门禁脚本、任务书只要求 E 两条；
   **这不是"CI 全绿"的证明**。
3. **未在仓库内真跑 `git commit`**：A–C/D 用的是 `SYNO_STAGED_FILES` 注入 + 假 message 文件（task-2 指定的夹具方式）。
   真实 hook 链（pre-commit → commit-msg → post-commit）端到端未由我复现——`fe01c3af` 的
   `.claude/bypass.log` 自记 `COMMITTED | pre-commit PASS`，我**只核了该行文本，未核 hook 日志本体**。
4. **未验证"多卡实际迁移"（Phase 3）**：真实共享件当前**仍被拦**——
   `.github/workflows/ci.yml`、`task-state/D954.json` 在 T1 上仍 exit=1，因为它们**文件内还没有声明行**
   （`DECL=(无)`）。机制是"有声明才放行"，不自动消解历史文件的 D328 噪音（`H2-真实共享件在T1.txt`）。
5. **未复核 K3 审计域**：`scripts/audit/**`、审计标准一律未碰（红线），也未独立审计本变更的审计面。
6. **未做独立 clone 验证**：只做了 `git ls-remote --heads origin | grep d979` 一致性核对
   （`fix/d979-d328-shared-decl` → `fe01c3af`），未在独立 clone 内复算。
7. **D 条变异的等价性**：变异施加在 /tmp 夹具副本（我的写集不含 `scripts/`），不是直接改仓库脚本；
   等价性由「提交 blob sha256 == 工作区 sha256」+「`git diff HEAD` 空」+「变异目标 `grep -c`=1」三点支撑。
8. **未验证**：resolver 在 CI 干净检出（无 staged / 无 current-brief）路径下的新分支交互；
   `git show :F` 在 Windows 下的路径/编码行为；多卡并发同时提交的真实竞争。
9. **第 3 个共享件（派单模板.md）**：在 base 与 T1 均 fail-open 跳过（机理见 §六），
   故它**不能**作为"修复 Δ"的证据；该差异已按队长裁定登记，未强行凑 3/3。

## 十一、遗留清单（交队长处置，非我权限内）

1. **证据目录未提交**：本目录文件当前是 `.synova-wt-d979-verify` 工作树内的 **untracked** 文件。
   原因：我的工作树是 detached HEAD，`fix/d979-d328-shared-decl` 正被 T1 工作树占用（git 禁止双检出），
   且提交会触发该仓库 hooks（pre-commit/post-commit 自动登记）——超出我的写集与授权。
   **请队长决定**：纳入 T1 分支的 PR（治理产物按 D860 口径不计入 12 文件预算），或另行提交。
   目录内容 = 本报告 + 16 个原始日志/脚本。
2. **真实共享件仍红**（§十.4）：若期望"今天起 3 个共享件不再被误拦"，还需 Phase 3 在各文件内加
   `共享声明：<卡号列表>`（.md/.sh/.txt 文本载体；`task-state/*.json` 用 `"shared_with": [...]`；
   `ci.yml` 可用 `shared_with: [D1, D2]`）。**未在本次 T1 写集内，需另派卡**。
3. **既有 fail-open 行为登记**（非本次引入）：resolver 回退选中"不认领该文件"的 brief 时，
   D328 静默跳过（派单模板.md 即此种）。本次修复未触碰该路径；是否收敛属另立卡议题。
