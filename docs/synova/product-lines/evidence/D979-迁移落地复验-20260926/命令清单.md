# D979 迁移落地复验（T4）· 我实际运行的命令清单（可复现）

被验对象：PR2 `#820` head `f34f3f55`（base `fix/d979-d328-shared-decl` @`aa98eb7d`）｜PR3 `#823` head `b16a29d7`（base `feat/d979-dispatch-tpl-dir` @`f34f3f55`）
我的临时工作树（一次性）：`/tmp/d979-t4/pr2`、`/tmp/d979-t4/pr3`；对照仓库：`/Users/wane/SynovaAgent/.synova-wt-d979-verify`

## 0. 环境与事实核对

```bash
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify fetch --all --prune
git ls-remote --heads origin | grep -E "d979|watermark|dispatch"
git worktree add --detach /tmp/d979-t4/pr2 f34f3f55
git worktree add --detach /tmp/d979-t4/pr3 b16a29d7
# PR 事实（API，只读；令牌从 ~/.dsh/.credentials.yaml 读入变量，不打印）
curl -sS -H "Authorization: token $TOK" .../pulls/820 ; curl -sS -H "Authorization: token $TOK" .../pulls/823
curl -sS -H "Authorization: token $TOK" .../pulls/820/files ; ... /pulls/823/files
```

## 1. A / B —— 真实件 + 真实 git index

```bash
cd /tmp/d979-t4/pr2
F="docs/synova/coordination/派单模板.md"
python3 - <<'PY'   # 诊断：哪些 brief 真认领该文件（Q2 include 命中）
... brief_parser.parse_q2 / match_path ...
PY
bash scripts/workflow/resolve-commit-brief.sh "$F"
# 夹具：把真实 D943 brief 复制成今日日期（仅在 /tmp 工作树，未入库）
cp .claude/task-briefs/2026-09-24-D943-cto-a-items.md .claude/task-briefs/2026-09-26-D943-cto-a-items.md
# A1
printf '\n<!-- T4 复验探针：A1 -->\n' >> "$F" && git add -- "$F"
printf 'docs(D979): T4 真实件双向命中\n' > /tmp/msg && bash scripts/commit-msg-check.sh /tmp/msg   # → DECL_ALLOW exit 0
# B
printf 'docs(D999): T4 非授权卡\n' > /tmp/msg && bash scripts/commit-msg-check.sh /tmp/msg          # → 劫持 exit 1, MISSING=D999
# A2（去声明行）
git checkout -- "$F"; python3 - <去第 1 行>; git add -- "$F"
bash scripts/commit-msg-check.sh /tmp/msg                                                          # → 劫持 exit 1, MISSING=D943,D979
git checkout HEAD -- "$F"   # 还原
```

## 2. C —— 零丢失核算

```bash
git -C <WT> merge-base aa98eb7d f34f3f55                                   # = aa98eb7dd9501bd3d9761531e08d4910dfa4e5c0
git -C <WT> show aa98eb7d:docs/synova/coordination/派单模板.md | wc -l      # 232
git -C <WT> diff --numstat aa98eb7d f34f3f55 -- docs/synova/coordination/派单模板.md   # 6  229
python3 - <<'PY'   # 规范化非空行精确命中 + 标题覆盖（脚本全文见 复现驱动-run_all.sh.txt）
PY
```

## 3. D —— 幂等 + 判别性

```bash
cd /tmp/d979-t4/pr3
bash scripts/control-tower/gen-watermark.sh                         # 写模式
git diff --exit-code -- docs/synova/coordination/号段/ ; echo $?     # 0 = 幂等
bash scripts/control-tower/gen-watermark.sh --check                  # ✅ 逐字节一致
# 判别性：两处渲染路径 wm 写死常量（/tmp 副本）
python3 - <把 196/224 行改为 wm = 1> ; bash -n scripts/control-tower/gen-watermark.sh
bash scripts/control-tower/gen-watermark.sh --check                  # ❌ 漂移 exit 1
# 对照变异：只改 282 行（摘要打印） ⇒ --check 仍 exit 0（排除"改了就红"）
git checkout HEAD -- scripts/control-tower/gen-watermark.sh          # 还原
```

## 4. E —— 归属表判别性

```bash
git -C <WT> diff aa98eb7d? ) ; git -C <WT> diff f34f3f55 b16a29d7 -- docs/synova/coordination/ownership.yaml
git -C <WT> diff --numstat f34f3f55 b16a29d7 -- docs/synova/coordination/ownership.yaml     # 8 0
for k in '"\*\*"' 'domain_neutral' 'glob:' 'owner:'; do git show <rev>:ownership.yaml | grep -c "$k"; done
python3 - <删掉 head 新增 8 行块后与 base 逐字节 sha256 比对>
cd /tmp/d979-t4/pr3
printf '\n// T4 探针\n' >> src/index.ts; printf '\n# T4 探针\n' >> scripts/commit-msg-check.sh
git add -- src/index.ts scripts/commit-msg-check.sh
python3 scripts/control-tower/check-ownership.py $(git diff --cached --name-only)            # ❌ 跨域 exit 1
python3 scripts/control-tower/check-ownership.py --owner mac $(git diff --cached --name-only) # ❌ 越域 exit 1
git checkout HEAD -- src/index.ts; printf '\n# T4 探针2\n' >> scripts/pre-commit-check.sh; git add -- scripts/pre-commit-check.sh
python3 scripts/control-tower/check-ownership.py $(git diff --cached --name-only)            # ✅ 同域 exit 0
python3 scripts/control-tower/check-ownership.py --yaml /tmp/d979-t4/own-base.yaml scripts/commit-msg-check.sh   # win
python3 scripts/control-tower/check-ownership.py --yaml /tmp/d979-t4/own-head.yaml scripts/commit-msg-check.sh   # mac
git checkout HEAD -- src/index.ts scripts/pre-commit-check.sh scripts/commit-msg-check.sh    # 还原
```

## 5. F / G

```bash
git -C <WT> diff --numstat f34f3f55 b16a29d7 -- docs/authority/DOCS-REGISTRY.yaml            # 29 0
git -C <WT> diff f34f3f55 b16a29d7 -- docs/authority/DOCS-REGISTRY.yaml | grep -c '^+  - id:'   # 4 条目
for p in "aa98eb7d f34f3f55 PR2" "f34f3f55 b16a29d7 PR3"; do
  for pat in SYNO_SKIP no-verify NO_VERIFY '|| true' ; do
    git -C <WT> diff --unified=0 <A> <B> | grep '^+' | grep -v '^+++' | grep -cF "$pat"
  done
done
git -C <WT> diff --name-only aa98eb7d b16a29d7 | grep -c 'scripts/commit-msg-check.sh'        # 0
git -C <WT> show b16a29d7:scripts/commit-msg-check.sh | grep -c '疑似并行劫持'                 # 1
git -C <WT> diff --name-status aa98eb7d f34f3f55     # PR2 文件面
git -C <WT> diff --name-status f34f3f55 b16a29d7     # PR3 文件面
git -C <WT> diff f34f3f55 b16a29d7 -- .claude/task-briefs/                                  # 声明外文件内容
```

## 6. 附加

```bash
cd /tmp/d979-t4/pr3
bash tests/control-tower/gen-watermark.test.sh          # 36 通过 / 0 失败
cat docs/synova/coordination/号段水位.md                 # 短指针核验
python3 - <DOCS-REGISTRY id 唯一性 + 新路径存在性>
grep -n "共享声明" docs/synova/coordination/派单模板/README.md   # 重复 2 行（现象登记）
```

## 7. 收尾

```bash
git -C /tmp/d979-t4/pr2 status --porcelain   # 空
git -C /tmp/d979-t4/pr3 status --porcelain   # 空
# 本证据 PR：我的工作树内自建分支 docs/d979-migrate-verify-evidence（base=feat/d979-watermark-gen）
```
