# D979 迁移落地复验（T4）自验报告

> **自验结论：通过**（A–G 七条判据全部符合预期；另加 2 项附加核查）
> ⚠️ 本报告是**自验结论**，**不是审计结论**。通过与否归 **CTO 收件闸 + K3 终审**。
>
> **另有 3 项「声明外文件」与 3 项「口径订正/前提观察」需队长裁定**（§四），
> 它们不影响 A–G 判据结论，但按写集纪律必须点名，不由我处置。

- 任务号：**D979**（T4 独立复验，共享任务 `task-4`）
- 验证对象：
  - **PR2 = #820**（head `f34f3f55`，base `fix/d979-d328-shared-decl` @`aa98eb7d`，API 实测 11 文件 / +721 −231）
  - **PR3 = #823**（head `b16a29d7`，base `feat/d979-dispatch-tpl-dir` @`f34f3f55`，API 实测 16 文件 / +877 −13）
- 自验员：`verifier`（独立于编码者 `coder-a`，未参与 T3 任何编码）
- 我的临时工作树（**一次性，非他人工作树**）：`/tmp/d979-t4/pr2`、`/tmp/d979-t4/pr3`（`git worktree add --detach`，跑完已还原）
- 日期：2026-09-26

---

## 一、A–G 逐条结果

| 条 | 判据 | 结果 | 原始输出 |
|---|---|---|---|
| **A** 声明放行（真实件·真实 `git add`） | 双向命中 ⇒ `DECL_ALLOW` exit 0；去声明行 ⇒ 劫持 exit 1 | ✅ 两次都按预期 | `A-B-真实件放行与拒绝.txt` |
| **B** 非授权仍拒 | 第三卡不在声明集 ⇒ exit 1 + `MISSING=<卡>` | ✅ `MISSING=D999` | 同上 |
| **C** 零丢失（PR2） | 拆前→拆后逐行/逐标题覆盖 | ✅ 163/163 非空行命中、28/28 标题覆盖、未命中 0 | `C-零丢失核算.txt` |
| **D** 幂等 + 判别性（PR3） | 生成物幂等 = 0 diff；反查改常量必红 | ✅ 见下 | `D-幂等与判别性.txt` |
| **E** 归属表判别性（**最高优先**） | 只增不删；跨域仍红；同域绿 | ✅ 三项全中 | `E-归属表判别性.txt` |
| **F** 无 blanket/无后门 | 登记只增；无逃生舱；拦截点唯一 | ✅（1 处 `|| true` 已甄别，见 §四.4） | `F-G-无后门与写集.txt` |
| **G** 写集越界 | 逐文件对照 task-3 声明写集 | ✅ 已逐条对照，**点名 3 个声明外文件**（§四.1） | 同上 |

### A / B —— 真实共享件 + 真实 git index（非注入缝）
在 `/tmp/d979-t4/pr2`（PR2 head 的独立工作树）里，用**真实 `git add`** 暂存**真实共享件**
`docs/synova/coordination/派单模板.md`（其第 1 行即声明行：`共享声明: D712, D931, D940, D943, D955, D979`）：

- **A1（授权）**：`MSG_DID=D979` + `CLAIM_DID=D943` ⇒ `DECL_ALLOW`，`SOURCE=index`，
  `HIT=D712,D931,D940,D943,D955,D979`，`MISSING=(无)`，**exit=0**。
- **B（非授权）**：`MSG_DID=D999` ⇒ `DECL_DENY`，`MISSING=D999`，**exit=1** + `❌ … 疑似并行劫持`。
- **A2（去声明行）**：同文件删掉声明行后 ⇒ `DECL=(无)`，`MISSING=D943,D979`，**exit=1**（回到原劫持判定）。

> **构造说明（诚实登记）**：该文件在本工作树当日的 resolver 日期窗口（±1 日）内**没有**真认领 brief
> （5 张真认领 brief 的文件名日期为 09-13/09-23/09-24，已出窗）⇒ 直接用真实件会走 `GENUINE=0`
> 整段跳过、**不触达 D328 分支**（我已先实测到这一点，见 §四.5）。
> 因此我把**真实卡 D943 的 brief 内容**复制成今日日期副本（`.claude/task-briefs/2026-09-26-D943-cto-a-items.md`，
> 仅在 /tmp 工作树，未入库）使真认领进入窗口 ⇒ `CLAIM_DID=D943`。文件、index、resolver、门禁全为真实件。

### C —— 零丢失（**我自己的算法与数字**，未采信 coder-a 的 164/20）
- 拆前 = `git show aa98eb7d:docs/synova/coordination/派单模板.md`（232 行）；拆后 = 目录 5 文件（`README.md` 150 / `产品.md` 50 / `哨兵.md` 48 / `门禁.md` 50 / `审计.md` 50 非空行）。
- 算法：两侧均**规范化**（去首尾空白 + 折叠内部连续空白）后取**非空行**；逐源行在拆后 5 文件的**并集**里做**精确命中**；标题另按 `^\s*#` 单独核算。
- **我的数字**：拆前非空行 **163**，拆后非空行 **348**，逐行精确命中 **163**，**未命中 0（0.0%）**；拆前标题 **28**，拆后标题集合 **33**，**未覆盖标题 0**；未命中清单 = 空。
- 指针版 `派单模板.md` 9 行（`+6 −229`），正文仅一句指向 `派单模板/README.md`；目录与 README 实测存在 ⇒ **非"复制一份再删原件"式假迁移**。
- 口径差异说明：我用"非空行 163 / 标题 28"，与 coder-a 的"164/20"计数基准不同（不影响结论：两套口径下**未覆盖均为 0**）。

### D —— 幂等 + 判别性（PR3）
- **幂等**：`bash scripts/control-tower/gen-watermark.sh`（写模式）后 `git diff --exit-code -- docs/synova/coordination/号段/` = **0**（exit=0）。
- **`--check` 原文**：`✅ 号段水位与仓库逐字节一致（--check 通过）`，exit=0。
- **判别性（写死常量）**：在 /tmp 副本把**两处渲染路径**的 `wm = inside[-1] if inside else None` 改成 `wm = 1`
  ⇒ `bash -n` **exit=0**（语法合法，非崩溃红）⇒ `--check` 输出 **4 个文件全部"漂移"** + **exit=1**。
- **对照变异**：只改**摘要打印行**（第 282 行，不改产物）⇒ `--check` 仍 **exit=0 绿** ⇒ 排除"改了就红"的假判别性。
- 还原后 `--check` 回绿，工作树无残留。
- **水位值独立交叉核算**：我**自己**从 `task-state/*.json` + 全分支名反查（复刻生成器口径）得
  **Mac 段内 54 张 / 最大 D980**、**Win 段内 2 张 / 最大 D1002**、**段回退命中 17** —— 与生成物自报 **完全一致**
  ⇒ 水位**真的来自反查**，非手写常量。

### E —— 归属表变更判别性（最高优先）
1. **只增不删 / 其他条目未动**：`numstat = 8 / 0`（+8 行、−0 行）；diff **只有一个 hunk、只有 `+` 行**；
   把这 8 行块从 head 删掉后与 base **逐字节相同**（sha256 双方均为 `fe9cd04d…`）⇒ 除该块外 base/head 完全一致。
   `**` 兜底行仍为第 35 行 1 处；`domain_neutral` 段 **1 处且完全一致**；`glob:` **43→44**、`owner:` **43→44**；
   base 中 `commit-msg-check.sh` 规则 **0 条**，head 新增 **1 条**（第 102 行）。
   > 口径订正：队长简写"只 +1 条"，原始输出是 **+1 条规则（= +8 行，含 5 行注释）**，两者不矛盾。
2. **真跨域暂存对**（真实 `git add`：`scripts/commit-msg-check.sh`[mac] + `src/index.ts`[win]）
   ⇒ `❌ FAIL 跨域: 变更落在 2 个域 ['mac','win'] …` **exit=1**（门禁未被放宽）；
   `--owner mac` 断言 ⇒ `❌ 越域: src/index.ts` **exit=1**。
3. **同域暂存对**（`scripts/commit-msg-check.sh` + `scripts/pre-commit-check.sh`）⇒ `✅ PASS 2 个文件同域: mac` **exit=0**。
4. **Δ 证据**：同一文件在 **base yaml = win**、**head yaml = mac**（`--yaml` 双跑）⇒ 本次表变更**只改变了该文件的归属判定**，未改变门禁强度。
   **结论：改了归属表 ≠ 放宽门禁**（跨域仍红、同域才绿，均有原始输出）。

### F —— 无 blanket 豁免 / 无后门
- `docs/authority/DOCS-REGISTRY.yaml`：`numstat = 29 / 0`，新增 **4 个条目**（`DOC-0119…DOC-0122`，每个 6 行）+ 5 行注释/空行；删除 **0**。
  （口径订正：队长简写"只 +4 条"，原始输出是 **+4 条目 = +29 行**，不矛盾。）
  id 唯一性实测：43 条目 / 43 唯一 id / 无重复；4 个新登记路径**全部存在**。
- **新增行逃生舱扫描**：PR2（721 新增行）与 PR3（877 新增行）中 `SYNO_SKIP`=0、`no-verify`=0、`NO_VERIFY`=0、`--force`=0；
  `|| true` 命中 **1 处**：`+export LC_ALL=C.UTF-8 2>/dev/null || true`（D313 M5 UTF-8 头块，属仓库既有模板写法，
  **不是**门禁吞错/逃生舱 —— 已按原文甄别，见 §四.4）。
- `scripts/commit-msg-check.sh` **未被 PR2/PR3 触碰**（`git diff --name-only` 命中 0 次）；
  在 PR3 head 上 `grep -c "疑似并行劫持"` = **1**，唯一位置 **149 行**（拦截点未被复制/绕过）。

### G —— 写集越界检查（逐文件对照 task-3 声明写集）
- **PR2（11 文件）**：`派单模板.md` ✓、`派单模板/{README,产品,哨兵,门禁,审计}.md` ✓（5）、`task-state/D979.json` ✓、
  evidence 3 份 ✓（`D979-迁移落地-20260926/`）、`.claude/bypass.log` = **hook 自动产物**。→ **无越界**。
- **PR3（16 文件，相对其 base 真实面 11 件）**：`号段水位.md` ✓、`号段/{README,Mac,Win,K3}.md` ✓（4）、
  `scripts/control-tower/gen-watermark.sh` ✓、`tests/control-tower/gen-watermark.test.sh` ✓、`task-state/D979.json` ✓、
  evidence 4 份 ✓、`.claude/bypass.log` = hook 自动产物；
  **点名 3 个声明外文件**（§四.1）。

---

## 二、附加核查（我自加，超出 A–G）

| 附加 | 结果 |
|---|---|
| PR3 自带生成器测试套件 `bash tests/control-tower/gen-watermark.test.sh` | **36 通过 / 0 失败**（含 缺失→exit 1、无 task-state→exit 2 显式 degraded、损坏 shim→exit 2、指针目标==生成物目录 的接线断言）→ `附加-生成器测试套件.txt` |
| `号段水位.md` 是否真短指针 | 正文仅指向 `号段/README.md` + 披露生成器；目录实测存在 ⇒ 不断链 → `附加-短指针与登记核查.txt` |
| DOCS-REGISTRY 新登记完整性 | 43 条目全唯一；4 个新路径均存在 → 同上 |
| `派单模板/README.md` 声明行**重复**（第 1 行与第 27 行同内容） | 现象已登记（解析器取并集 ⇒ 功能无碍；卫生问题，值得一句说明）→ 同上 |

---

## 三、未能验证的部分（诚实登记）

1. **Windows 实机未跑**：PR2/PR3 的新脚本（`gen-watermark.sh`）只做静态核对（`sed -E` / `${VAR}` 花括号 / PYBIN 回退 / `cygpath`），未在 Windows/MSYS 执行。
2. **未跑 CI / 13 组门禁 / vitest**：只跑了 PR3 自带的生成器 bash 套件（36/0）与 T1 时期的两个一致性套件（T2 已跑）；**不构成"CI 全绿"证明**。
3. **未在 GitHub PR 上下文跑 `check-pr-budget.sh`**：E 条用的是其底层消费者 `check-ownership.py`（单域模式）；
   CI 侧 `check-pr-budget.sh ② 变更跨域` 未逐字重跑（同源脚本，但未做端到端复现）。
4. **C 条为"行级"零丢失**：逐行精确匹配 + 标题覆盖；若存在**同义改写**（内容意思保留但逐字改动），该算法会判为未命中。
   实测未命中 0 ⇒ **无改写迹象**，但"语义等价"本身不由该算法证明。
5. **未复算 PR3 的 4 份 coder-a 证据文件内容**（`PR3-*.txt`）：我只复算了它们**声称的机制**，未逐条核对其内部数字。
6. **resolver 日期窗口依赖"今天"**：A/B 的 `CLAIMED_FILES` 结论以 2026-09-26（±1 日窗口）为基；跨日/他机 brief 池不同则窗口成员不同（§四.5 已如实登记机理）。
7. **未验证 DOCS-REGISTRY 的登记门禁**（如 `check-doc-registry`/doc-truth 是否对这些新条目通过）：未跑该门禁。
8. **未验证 PR2 的判据⑤（引用面 grep）**：那是 task-3 的验收项，不在 T4 的 A–G 内，未由我独立复算。

---

## 四、须队长裁定 / 登记项（我无权处置）

1. **PR3 的 3 个「声明外文件」**（task-3 的 PR3 写集未列）：
   - `docs/synova/coordination/ownership.yaml`（+8）：队长 T4 的 E 条点名要求，**疑似已口头授权**；
   - `docs/authority/DOCS-REGISTRY.yaml`（+29）：队长 T4 的 F 条点名要求，**疑似已口头授权**；
   - `.claude/task-briefs/2026-09-26-D979-fix011-shared-file-mechanism.md`（+8）：内容 = 把 `gen-watermark.sh` /
     `gen-watermark.test.sh` 追加进本卡 Q2 写集 + 勾选两条 Done + T3 说明行。属**自指式扩写**（让门禁能认领新文件），
     但仍**不在任何已声明写集内**（该文件属 T1 写集，T1 已闭合）。→ 请裁定：追认 or 记违规。
2. **口径订正（非缺陷）**：`ownership.yaml` 实际 **+8 行 / 1 条规则**（队长简写"+1 条"）；`DOCS-REGISTRY.yaml` 实际 **+29 行 / 4 条目**（队长简写"+4 条"）。原始计数均已在 §一 贴出。
3. **PR2 `派单模板/README.md` 声明行重复 2 次**（第 1、27 行）——现象登记，建议 coder-a 后续清理一处。
4. **`|| true` 1 处命中的甄别**：`export LC_ALL=C.UTF-8 2>/dev/null || true`（UTF-8 locale 头块，仓库既有模板同款），
   非门禁吞错；若队长口径要求"新增行内 `|| true` 一律为 0"，则这里需要显式豁免说明。
5. **前提观察（影响"声明落地是否立即解锁"的预期）**：在 PR2 head 上，**今日窗口内无 brief 真认领** `派单模板.md`
   （5 张真认领 brief 的日期均已出 ±1 日窗），因此**只暂存该文件的提交会走 `GENUINE=0` → D328 整段跳过**（fail-open），
   **声明行当前不产生门禁效果**。声明机制本身在"有真认领"时按设计工作（A/B 已证）。
   这是 resolver 日期窗口的既有行为，**不是 PR2 引入的缺陷**，但意味着"加了声明行 ⇒ 该文件从此不再被误拦"的说法需要限定条件。

---

## 五、复现方式

- 一键复现驱动：`复现驱动-run_all.sh.txt`（A–G 各段落盘，只写 `/tmp`）
- 逐条命令：`命令清单.md`
- 证据文件命名：全部 `.txt` / `.md`（**无 `.log`**，规避 `.gitignore:8 *.log`；该规避依据见 T2 证据 PR #819）
