  ✅ 生成器存在
  ✅ bash -n 语法通过
═══════════════════════════════════════════════════════════
  D979 T3 号段水位生成器 — 测试
  SUT: /tmp/d979-t4/pr3/scripts/control-tower/gen-watermark.sh
═══════════════════════════════════════════════════════════

── 1. 正常: mac/win/k3 → 水位各取实测最大 D# ──
  ✅ 正常生成 (exit=0)
  ✅ Mac 水位 = D968（实测最大）
  ✅ Win 水位 = D1002
  ✅ K3 水位 = D865
  ✅ README 汇总含下一个建议号
  ✅ stdout 摘要含水位

── 2. 段外异常: Mac 段出现 D1007 → 点名（事实上报，不改数）──
  ✅ 含段外异常仍生成成功 (exit=0)
  ✅ Mac.md 点名段外 D1007

── 3. 归属双值: domain=win/mac → 两个分册都列 ──
  ✅ Win.md: 双归属卡计入段内水位
  ✅ Mac.md: 双归属卡列为越上界异常

── 4. 归属来自 owner 关键字（domain 缺失）→ 计入 Mac ──
  ✅ owner 关键字归属 Mac

── 5. 归属来自分支名: chore/win-d1050-x（无 task-state 卡）→ Win 水位 D1050 ──
  ✅ 分支名归属抬升 Win 水位

── 6. 幂等: 连跑两次内容一致 + --check exit 0 ──
  ✅ 第二次生成成功 (exit=0)
  ✅ 幂等: README.md 两跑一致
  ✅ 幂等: Mac.md 两跑一致
  ✅ 幂等: Win.md 两跑一致
  ✅ 幂等: K3.md 两跑一致
  ✅ --check 通过（生成物 == 仓库） (exit=0)
  ✅ --check 输出确认

── 7. 漂移: 手改一字节 → --check exit 1 ──
  ✅ 漂移检出（业务态 exit 1） (exit=1)
  ✅ 漂移行点名
  ✅ 重生成后复原 (exit=0)
  ✅ 复原后 --check 通过 (exit=0)

── 8. 缺失: 删一个生成物 → --check exit 1 ──
  ✅ 缺失检出 exit 1 (exit=1)
  ✅ 缺失行点名

── 9. 降级: 无 task-state 目录 → exit 2 + 显式 degraded ──
  ✅ 无 task-state → exit 2（降级，非静默） (exit=2)
  ✅ 显式 degraded 文案
  ✅ 文案点名原因

── 10. 降级: python3/python/py 全损坏（PATH 前置 exit 127）→ exit 2 ──
  ✅ 损坏 shim → exit 2（可用性探测生效） (exit=2)
  ✅ 文案点名无可用 PYBIN（D520 标注后文案）

── 11. 边界: task-state 存在但无可解析卡号/坏 json → 未定义 + exit 0 ──
  ✅ 无卡号不崩（exit 0） (exit=0)
  ✅ 水位标未定义

── 12. 接线: 号段水位.md 指针目标 == 生成物目录 ──
  ✅ 指针指向 号段/README.md
  ✅ 指针披露生成器（可复算）

═══════════════════════════════════════════════════════════
  结果: 36 通过 / 0 失败
═══════════════════════════════════════════════════════════
