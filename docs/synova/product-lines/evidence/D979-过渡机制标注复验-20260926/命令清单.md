# D979 过渡机制标注复验（T6）· 我实际运行的命令清单（可复现）

被验对象：PR #829 head `29c646f5`（base `docs/d979-lead-closeout` @`9b3e2645`）
我的临时工作树：`/tmp/d979-t6/base`（9b3e2645）、`/tmp/d979-t6/anno`（29c646f5）｜判定器：`/tmp/d979-t6/zero_semantic.py`（自写，源码见证据目录 `.txt`）

## 0. 事实核对 / 建树

```bash
git -C /Users/wane/SynovaAgent/.synova-wt-d979-verify fetch --all --prune
git ls-remote --heads origin | grep -E "d979|transition|lead-closeout"
git worktree add --detach /tmp/d979-t6/base 9b3e2645
git worktree add --detach /tmp/d979-t6/anno 29c646f5
git log --oneline 9b3e2645..29c646f5
git diff --name-status 9b3e2645 29c646f5
```

## 1. A 零语义 + 判别性

```bash
# A1 只增不删
git diff --numstat 9b3e2645 29c646f5 -- scripts/commit-msg-check.sh \
    scripts/control-tower/shared_file_decl.py \
    memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md
git diff -U0 9b3e2645 29c646f5 -- <同上三文件>
# A2/A3 我的判定器
python3 /tmp/d979-t6/zero_semantic.py scripts/commit-msg-check.sh --base /tmp/d979-t6/base --head /tmp/d979-t6/anno
python3 /tmp/d979-t6/zero_semantic.py scripts/control-tower/shared_file_decl.py --base /tmp/d979-t6/base --head /tmp/d979-t6/anno
python3 /tmp/d979-t6/zero_semantic.py memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md --base /tmp/d979-t6/base --head /tmp/d979-t6/anno
# A4-a 判别性（.sh 真语义变异，仅 /tmp 副本）
cp /tmp/d979-t6/anno/scripts/commit-msg-check.sh /tmp/d979-t6/mut/commit-msg-check.sh
python3 - <<'PY'
p='/tmp/d979-t6/mut/commit-msg-check.sh'; s=open(p,encoding='utf-8').read()
assert s.count('[ "$DECL_RC" = 0 ]')==1
open(p,'w',encoding='utf-8').write(s.replace('[ "$DECL_RC" = 0 ]','[ "$DECL_RC" = 99 ]'))
PY
bash -n /tmp/d979-t6/mut/commit-msg-check.sh; echo $?      # 必须 0
python3 /tmp/d979-t6/zero_semantic.py scripts/commit-msg-check.sh --base /tmp/d979-t6/base \
    --head-override /tmp/d979-t6/mut/commit-msg-check.sh    # 必须 RED(1)
# A4-b 恢复 ⇒ GREEN；A4-c 对照变异（追加注释行）⇒ 仍 GREEN
# A4-d .py 真语义变异
cp /tmp/d979-t6/anno/scripts/control-tower/shared_file_decl.py /tmp/d979-t6/mut/shared_file_decl.py
python3 - <<'PY'
p='/tmp/d979-t6/mut/shared_file_decl.py'; s=open(p,encoding='utf-8').read()
assert s.count('return 0 if allowed else 1')==1
open(p,'w',encoding='utf-8').write(s.replace('return 0 if allowed else 1','return 0  # T6-MUT: 真语义'))
PY
python3 -c "import sys;compile(open(sys.argv[1],encoding='utf-8').read(),sys.argv[1],'exec')" /tmp/d979-t6/mut/shared_file_decl.py
python3 /tmp/d979-t6/zero_semantic.py scripts/control-tower/shared_file_decl.py --base /tmp/d979-t6/base \
    --head-override /tmp/d979-t6/mut/shared_file_decl.py    # 必须 RED(1)
# 一键：bash /tmp/d979-t6/run_A.sh
```

## 2. B 三套测试两版

```bash
for w in base anno; do
  cd /tmp/d979-t6/$w
  bash tests/control-tower/commit-msg-consistency.test.sh
  bash tests/control-tower/shared_file_decl.test.sh
  bash tests/control-tower/gen-watermark.test.sh
  grep -c "疑似并行劫持" scripts/commit-msg-check.sh
  git status --porcelain | wc -l
done
```

## 3. C / D / F

```bash
git grep -n "阶段 3 由行级 blame 取代" 29c646f5 -- scripts/commit-msg-check.sh \
    scripts/control-tower/shared_file_decl.py \
    memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md
git grep -l "阶段 3 由行级 blame 取代" 29c646f5
git grep -l "阶段 3 由行级 blame 取代" 29c646f5 -- scripts tests src packages .github
git diff -U0 9b3e2645 29c646f5 | awk '/^\+\+\+ b\//{f=substr($0,7)} /^\+/ && !/^\+\+\+/ && /阶段 3 由行级 blame 取代/ {print f}' | sort | uniq -c

git ls-remote --heads origin | grep d979
git show aa98eb7d:scripts/commit-msg-check.sh | grep -c "阶段 3 由行级 blame 取代"
git show aa98eb7d:scripts/control-tower/shared_file_decl.py | grep -c "阶段 3 由行级 blame 取代"
git branch -r --contains 4b9e08f1
git merge-base --is-ancestor 4b9e08f1 origin/fix/d979-d328-shared-decl; echo $?

git rev-list --count origin/main..29c646f5
git diff --name-only origin/main...29c646f5 | wc -l
```

## 4. E PR #829 读回

```bash
# 令牌从 ~/.dsh/.credentials.yaml 读入变量（不打印）
curl -sS -H "Authorization: token $TOK" -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/tangbaobao520/SynovaAgent/pulls/829
# 检查 base.ref / base.sha / head.ref / head.sha / mergeable_state / body 首行
```

## 5. 收尾

```bash
git worktree remove /tmp/d979-t6/base
git worktree remove /tmp/d979-t6/anno
git worktree prune
# 本证据 PR：分支 docs/d979-anno-verify-evidence（base=docs/d979-transition-annotation）
```
