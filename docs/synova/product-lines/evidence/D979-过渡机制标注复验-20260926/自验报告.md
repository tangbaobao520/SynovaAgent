# D979 过渡机制标注复验（T6）自验报告

> **自验结论：通过**（A–F 六条判据全部符合预期，含核心 A4 判别性）
> ⚠️ 本报告是**自验结论**，**不是审计结论**。通过与否归 **CTO 收件闸 + K3 终审**。
>
> 另登记 **2 项观察**（PR 正文 base sha 落后、全仓含该串 6 文件的两口径）（§四），
> 不影响 A–F 结论。

- 任务号：**D979**（T6 独立复验，共享任务 `task-6`）
- 验证对象：**PR #829**（`docs(D979): ⒝ 过渡机制三处标注（CTO 裁定二；纯注释零语义）`）
  - head `29c646f5a8a8c045bb22bf24f303a2ce4618d1a1`（分支 `docs/d979-transition-annotation`）
  - base `docs/d979-lead-closeout` @ `9b3e2645c7c27d19563f1e4738e537d789c19a63`（API 读回，≠ main）
- 自验员：`verifier`（**独立于编码者 coder-a**，未参与 T5 任何编码）
- 我的临时工作树（一次性）：`/tmp/d979-t6/base`（`9b3e2645`）、`/tmp/d979-t6/anno`（`29c646f5`）——**未碰 coder-a 的工作树、未碰 #829/#815 分支**
- 判定器：`判定器-zero_semantic.py.txt`（**我自写**，未复用 coder-a 的实现或其数字）
- 日期：2026-09-26

---

## 一、A —— 零语义证明（本任务核心）

### A1 只增不删（原始 numstat）
| 文件 | 新增 | 删除 |
|---|---|---|
| `scripts/commit-msg-check.sh` | 2 | **0** |
| `scripts/control-tower/shared_file_decl.py` | 4 | **0** |
| `memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md` | 1 | **0** |

删除列**全 0**；`-U0` 全量 diff 已贴（`A-零语义与判别性.txt`），三处均为**新增整行注释/docstring 续行**。

### A2 `.sh` —— 过滤整行注释与空行后可执行行比对
- 可执行行数：base **128** / head **128**
- 可执行行序列 sha256：base `ac34413752479290…` / head `ac34413752479290…`（**相同**）
- **判定器 VERDICT: GREEN（零语义），exit=0**

### A3 `.py` —— 剥离 docstring 后用 `ast` 算（我自己算）
- docstring 行数：base 49 / head 53（标注落在模块 docstring 内 ⇒ docstring 变长 4 行）
- **AST 指纹（剥 docstring 后）**：base `ece22d72b89f2723f8987d00…` / head `ece22d72b89f2723f8987d00…`（**相同**）
- 非 docstring 源码行数：base 190 / head 190；行序列 sha256 `a3c3f5df3454c058…`（**相同**）
- **判定器 VERDICT: GREEN（零语义），exit=0**

### A4 **判别性**（没有这条，"零语义"判定不成立）
| 步骤 | 施加 | 语法检查 | 我的判定器 |
|---|---|---|---|
| A4-a `.sh` 真语义变异 `[ "$DECL_RC" = 0 ]` → `= 99` | /tmp 副本，目标串唯一命中 | **`bash -n` exit=0**（排除"崩了才红"） | **RED exit=1**，diff 精确指向该行 |
| A4-b 恢复 | 重取 head 原文 | — | **GREEN exit=0** |
| A4-c 对照变异（只在 .sh 追加一行注释） | /tmp 副本 | `bash -n` exit=0 | **GREEN exit=0**（排除"改了就红"） |
| A4-d `.py` 真语义变异 `return 0 if allowed else 1` → `return 0` | /tmp 副本，唯一命中 | `compile()` OK（无缓存写） | **RED exit=1**（AST 指纹不同 + 行序列不同） |
| A4-e 恢复 | 重取 head 原文 | — | **GREEN exit=0** |

⇒ **A4-a/d 证明判定器能捕获真语义变更；A4-c 证明它不是"任何改动都报红"** ⇒ 判定器**对 .sh/.py 的零语义结论可信**。

---

## 二、B —— 三套测试两版同结果

| 套件 | base `9b3e2645` | head `29c646f5` | 一致 |
|---|---|---|---|
| `commit-msg-consistency.test.sh` | **30 通过 / 0 失败**（exit 0） | **30 通过 / 0 失败**（exit 0） | ✅ |
| `shared_file_decl.test.sh` | **40 通过 / 0 失败**（exit 0） | **40 通过 / 0 失败**（exit 0） | ✅ |
| `gen-watermark.test.sh` | **36 通过 / 0 失败**（exit 0） | **36 通过 / 0 失败**（exit 0） | ✅ |

- `grep -c "疑似并行劫持" scripts/commit-msg-check.sh`：**两版均 = 1** ✅
- 两版跑完工作树残留均为 **0 处**（测试无副作用）
- 原始输出：`B-三套测试两版.txt`（两版各自汇总行，非截断）

---

## 三、C / D / E / F

### C 三处标注定位 + 无第四处
- 三个指定文件**各 1 处**：`scripts/commit-msg-check.sh:59`、`scripts/control-tower/shared_file_decl.py:12`、`memory/notes/implemented/2026-09-26-fix011-shared-file-mechanism.md:9`（原文已贴）。
- **代码/测试区**（`scripts|tests|src|packages|.github`）命中文件数 = **2**（.sh + .py）⇒ **无第四处标注落点**。
- 全仓含该串 = **6 文件**（第三处的 3 个额外命中均为**引用/登记**性质，非新增加标注）：
  `docs/…/evidence/D979-过渡机制标注-20260926/零语义证明与diff.txt`（引用 diff，9 行）、
  `docs/…/evidence/D979-队长收尾-20260926/CTO裁定执行-20260926.md`（CTO 裁定原文，**base 既有**）、
  `task-state/D979.json`（note 字段登记 T5 事实）。
  ⇒ 口径说明：**"第四处标注"不存在**；若按"文件含该串"计数则为 6，两个口径的原始输出均已贴出。
- **PR 新增行**里出现该串的文件 = 5（3 标注 + 证据 txt + task-state note）——新增行归属亦已逐条列出。

### D #815 分支未被触碰（K3 审查中的分支）
- `git ls-remote --heads origin | grep d979`（原始）⇒ `fix/d979-d328-shared-decl` = **`aa98eb7dd9501bd3d9761531e08d4910dfa4e5c0`**，与 T3 结束时同值 ✅
- **标注不在 #815 上**：`git show aa98eb7d:scripts/commit-msg-check.sh | grep -c` = **0**；`shared_file_decl.py` = **0** ✅
- 标注提交 `4b9e08f1` **仅**被 `origin/docs/d979-transition-annotation`（及本地 ssh/ 镜像）包含；
  `git merge-base --is-ancestor 4b9e08f1 origin/fix/d979-d328-shared-decl` = **否（exit 1）** ✅

### E PR #829 base 声明
- API 读回：`base=docs/d979-lead-closeout @9b3e2645`（**≠ main** ✅）、`head=docs/d979-transition-annotation @29c646f5`、
  `mergeable_state=**clean**`、`changed_files=6`、`+111 −1`、`merged=False`。
- 正文**首行**原文：`> **栈式 PR**：base = docs/d979-lead-closeout（@ab27195e，栈顶），head = docs/d979-transition-annotation。**非 main**。` ⇒ **声明了栈式 base** ✅

### F D734 ① 口径（登记）
- `git rev-list --count origin/main..29c646f5` = **27**
- `git diff --name-only origin/main...29c646f5 | wc -l` = **35**
- `origin/main` tip = `ee721b0a`；两值**均 > 12** ⇒ 栈式 PR 在该口径下必然超限（**既有问题，非本次引入**），按要求登记。

---

## 四、观察 / 登记项（不影响 A–F 结论）

1. **PR #829 正文声明的 base sha 落后于 base 分支现值**：正文写 `@ab27195e`，而 `docs/d979-lead-closeout` 现 tip = `9b3e2645`（相差 **2** 个提交：`cebae968 docs(D979): CTO 裁定执行记录 + 栈式平铺可行性实测 + 未清项收口表 refs #826`、`9b3e2645 chore: bypass COMMITTED 登记`）。
   **base 分支名声明正确**；且 head `29c646f5` 本身已 merge 了 `9b3e2645`（其 merge 提交标题可证），故 PR 实际 diff 仍只有 6 文件、`mergeable_state=clean`。登记事实，供队长决定是否刷新正文 sha。
2. **"没有第四处"的口径**：按**标注落点** = 3（无第四处）；按**文件含该串** = 6（另 3 处为引用/登记）。两个口径的原始输出都已在 `C-D-F-….txt` 内，避免下游引错口径。

---

## 五、未能验证的部分（诚实登记）

1. **Windows 实机未跑**：标注为纯注释/docstring，跨平台风险低，但未在 Windows/MSYS 实测；未验证 `git grep`/`ast` 在该环境的差异。
2. **未跑 CI**：只跑了三套控制塔 bash 套件（30/40/36）；**不构成"CI 全绿"证明**。
3. **未复算 coder-a 的 `零语义证明与diff.txt` 内部数字**：我只复算了机制与三个标注文件本身；该证据文件里的 9 行引用已列入 C 条清单，但其内部结论未逐条核对。
4. **未验证 K3 对 #815 的审查进度**（是否已开始/已出具意见）：我只验证了**分支未变**这一物理事实。
5. **未验证合并顺序**：#829 合并后 `docs/d979-lead-closeout` 若再前进，PR diff 面会变化（由队长控制，不在我写集）。
6. **判定器口径局限**（诚实披露）：`.sh` 只过滤"整行注释"，**行内注释**的语义性变更会被判为差异（本次三处均为整行注释，不影响结论）；`.py` 只剥离模块/类/函数**首个**字符串表达式 docstring（本次标注正是模块 docstring 续行，命中该口径）。

---

## 六、复现方式

- 判定器源码：`判定器-zero_semantic.py.txt`（用法见文件头）
- 一键复现：`复现驱动-run_A.sh.txt`（A 全条 + 变异；只写 `/tmp`）
- 逐条命令：`命令清单.md`
- 原始输出：`A-零语义与判别性.txt` / `B-三套测试两版.txt` / `C-D-F-….txt` / `E-PR829读回.txt`
- 证据文件名**全部 `.txt`/`.md`**（**无 `.log`**，规避 `.gitignore:8 *.log`；未用 `git add -f`）
