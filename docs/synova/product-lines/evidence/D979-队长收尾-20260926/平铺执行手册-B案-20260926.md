# D979 — 栈式平铺执行手册（CTO 裁定 B 案：按序合并）

> 依据：CTO 2026-09-26 裁定三① + 二次裁定选 **B（CTO/并行 CTO 按序合）**
> 本手册由控制塔线小队产出；**合并动作由 CTO/并行 CTO 执行**，本队负责逐步核验并回报。
> 前提：裁定三②（门禁语义变更未过 K3 前不得合并）仍然生效。

---

## 一、⚠️ 两个前置件（**不先落 main，整条链必红**）

| 前置件 | 内容 | 不做的后果（实测） | 冲突点 |
|---|---|---|---|
| **P-a** | `docs/synova/coordination/ownership.yaml` +1 条：`scripts/commit-msg-check.sh → mac` | #815 / #820 / #823 **全部** `❌ ② 变更跨域`（实测 #820 复现，唯一 win 文件即此件） | 无（纯归属表缺口修正，同 `pre-commit-check.sh`/`pre-push-check.sh` 先例） |
| **P-b** | `.github/workflows/ci.yml` 密封清单登记 **3 个新测试**：`tests/control-tower/shared_file_decl.test.sh`、`tests/control-tower/gen-watermark.test.sh`（+ 复核 `commit-msg-consistency.test.sh` 是否已在册） | #815 / #820 / #823 全部 `❌ Gate Integrity: 新增测试未登记 CI 密封清单` | ⚠️ **与裁定一/二 直接冲突**：`ci.yml` 已 ⛔ 冻结且与 #800/#803/#806 串行 |

**P-b 是派单件 §〇 记载的「FIX-003 后半（把配对测试注册进 CI 清单）被迫移出」的未偿债务** ——
D328 劫持把它挡出了上一批；**现在 D328 已修，但它仍被 Phase 2 冻结挡着**。
⇒ **需要 CTO 就"ci.yml 登记区解冻"给出明确指令**（只解冻密封清单登记段，还是等 #800/#803/#806 全合）。**本队不擅自解冻。**

---

## 二、栈拓扑（实测，`git ls-remote` 2026-09-26）

```
main
└─ #809 docs/d979-fix011-shared-file-mechanism   feebf651   2 commits  (base=main)
   └─ #815 fix/d979-d328-shared-decl             aa98eb7d   6 commits  ← 门禁语义变更，K3 审中
      ├─ #819 docs/d979-verify-evidence           da144d95   （T2 证据）
      └─ #820 feat/d979-dispatch-tpl-dir          f34f3f55
         └─ #823 feat/d979-watermark-gen          b16a29d7
            ├─ #825 docs/d979-migrate-verify-evidence ed0f9fe8 （T4 证据）
            └─ #826 docs/d979-lead-closeout       9b3e2645
               └─ #829 docs/d979-transition-annotation  29c646f5
                  └─ #831 docs/d979-anno-verify-evidence  26d07b29
```
`origin/main` 现值：`ee721b0a`。

---

## 三、合并顺序 + 每步预期

> 每步做完后，**GitHub 会自动把下一层的 PR base retarget 到 main**，其 diff 与 CI 才逐层孤立。
> **每步的实际观测我逐条回报**（check-runs 数量 + 名称 + 结论；mergeable_state；retarget 是否生效）。

| # | 合并对象 | 合并后应得到 | 本队核验项 |
|---|---|---|---|
| 0 | **P-a + P-b** 落 main | main 具备 mac 归属规则 + 密封清单在册 | 两条修正的 CI 结论 |
| 1 | **#809** | 派单件入 main | PR 首次触达 CI（base=main 触发面） |
| 2 | **#815** | 机制入 main（K3 通过为前提） | check-runs 全绿？7 文件？base 已 retarget |
| 3 | **#819** 与 **#820**（同层，可任一先后） | T2 证据 + 派单模板目录化 | 各自独立 CI；#820 ≤12 文件 |
| 4 | **#823** | 号段生成物 + 登记 + 归属 | 独立 CI；PowerShell/bash 双平台 job |
| 5 | **#825** 与 **#826**（同层） | T4 证据 + 队长收尾 | 各自独立 CI |
| 6 | **#829** | 过渡机制三处标注 | 独立 CI；确认 diff 仍为纯注释 |
| 7 | **#831** | T6 标注复验证据 | 独立 CI |

---

## 四、本队在每个步骤后的回报格式（承诺）

```
步骤 <n> 完成核验 — <PR #> @ <merge base 后的新 head>
① check-runs 集合：数量 = N；名称与结论逐条列出（贴 API 原始输出）
② mergeable_state / merged 状态
③ base 是否已 retarget 到 main（贴 API 原始输出）
④ 若红：完整失败片段（禁 tail 截断）+ 我的根因判定（只陈述事实，不下"应如何"结论）
⑤ 本步是否引入新的未清项
```

---

## 五、本队不做的事（红线重申）

- **不执行任何合并**（收件闸归 CTO/K3）
- **不 force push**（未获 CTO 明示豁免；本次 CTO 已选 B，即不需要豁免）
- **不擅自改 `ci.yml`**（P-b 需 CTO 明确解冻指令）
- **不动 K3 审查中的 #815 分支**（实测已验证标注件未落在其上）

---

*本手册所有数字取自命令原始输出；未做项标 ⛔/待指令。*
