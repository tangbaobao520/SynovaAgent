# D979 / FIX-011 — 队长收尾报告（控制塔线小队）

> **状态：可提请独立审计**（K3 复审通过前不得合并任何一件）
> 日期：2026-09-26 ｜ 卡号：D979 ｜ 依据：`docs/synova/coordination/问题修复台-20260925.md` FIX-011
> 队长：synova-squad-lead ｜ 成员：**coder-a**（编码 T1/T3）、**verifier**（独立自验 T2/T4）
> 编制：队长 + 2 编码位（实占 1）+ 1 独立自验 = 3 人，符合 ≤4 上限；**自验未由编码兼任**

---

## 〇、一句话结论

CTO 裁定的组合方案**已落地**：⒝ 显式多卡共享声明机制（T1）+ ⒜ 的两件目录化（T3）。**⒜ 的 `ci.yml` 拆分未做**，原因是**外部阻塞**（三件未合并 PR 同时持有 `ci.yml`），已附实测证据登记，未强行推进以免四方互踩。

**本报告不含"通过/全绿"字样以外的事实性结论仍以原始输出为准**；CI 至今 **queued 无结论**，故本件**不构成 CI 验证**，见 §五。

---

## 一、前提冻结表（开工前实测；**两条不成立，已修正**）

| # | 派单件前提 | 实测命令（原始） | 实测结果 | 判定 |
|---|---|---|---|---|
| P1 | D328 判定段在 `scripts/pre-commit-check.sh` | `git grep -n "劫持" origin/main -- scripts` | 命中 **`scripts/commit-msg-check.sh:46-127`**；`pre-commit-check.sh` 的 G10=「条件区域」、G12=「Task Scope」，**均非 D328** | ❌ **不成立** |
| P2 | 归属来源为卡内声明 | 读 `resolve-commit-brief.sh`(308 行) + `brief_parser.py` | 成立；但**两文件均不在派单件写集** | ✅／写集缺 |
| P3 | PR #809 已带本件 | API `pulls/809` | 成立（5 文件，head `docs/d979-fix011-shared-file-mechanism`） | ✅ |
| P4 | #800/#803/#806 均改 `ci.yml`，需串行 | API `pulls/{800,803,806}/files` | 成立；三件**全部未合并**：`dirty` / `blocked` / `dirty` | ✅ → §四 |
| P5 | 5 个共享件存在 | `git cat-file -e origin/main:<f>` | 全部存在（ci.yml 453 / 派单模板.md 232 / 号段水位.md 14 / D954.json 33 / CTO-看板-自动.md 12 行） | ✅ |
| P6 | `task-state/D979.json` 在 main | 同上 | **不在** main，仅在 #809 分支 | ✅（栈式） |
| P7 | 「24h 内 D328 拦 3 次」 | 主工作区 + verifier 工作树各跑一遍 `SYNO_STAGED_FILES=<f> bash scripts/commit-msg-check.sh <fix(D977/D979)>` | 主工作区 **3/3 exit=1**；verifier 独立工作树 **2/3**（第 3 条 resolver 落到不认领该文件的 brief ⇒ 既有 fail-open） | ⚠️ **部分成立** |
| P8 | 共享件真认领数（我初报 7 张） | coder-a 用 `brief_parser` 逐 brief 复算 | **5 张**（D712/D931/D940/D943/D955）；我的 7 是 `git grep -l`「正文提及」口径 | ❌ **我错，已认账** |

**P7/P8 的处置**：验收口径由「3/3」改为「**≥1 条共享件在 base 上被判劫持 exit=1** + 逐条如实登记」；「认领 7 张」更正为「**5 张**」。理由：`.claude/task-briefs/` 是本机局部状态、非 main 内容，同一命令在不同工作树判定不同。

**队长前提复现原始输出**：见本目录 `0-队长前提复现-原始输出.txt`（3 条完整输出 + exit code，标注为**队长实测、非自验**）。

---

## 二、写集修正（**请 CTO 确认**；派单件 §三 原文为「写集（**建议**，执行方按需调整并回报）」）

| 派单件写的 | 实测 | 处置 |
|---|---|---|
| `scripts/pre-commit-check.sh`（D328 判定段） | 该文件**无** D328 判定段 | **移出**（未改动该文件一个字节） |
| —（未列） | `scripts/commit-msg-check.sh` = D328 真实落点 | **加入**（T1 已改） |
| —（未列） | `.claude/task-briefs/2026-09-26-D979-*.md` | **加入**（须补 Q2 逐文件路径 + Done 格式，否则 resolver 认领不到本卡 / G12b 红） |
| —（未列） | `docs/synova/coordination/派单模板.md` ｜ `派单模板/**` ｜ `号段水位.md` ｜ `号段/**` | **加入**（⒜ 迁移） |
| —（未列） | `docs/authority/DOCS-REGISTRY.yaml`（+29 行/4 条目） | **加入**：**门禁强制**——新文档不登记即红（**非**放宽门禁，是满足门禁） |
| —（未列） | `docs/synova/coordination/ownership.yaml`（+8 行/1 条规则） | **加入**：归属表缺口修正（`scripts/commit-msg-check.sh` 落 `**`→win 兜底；同族门禁脚本已全为 mac）**附判别性证据** |
| —（未列） | `task-state/D954.json` | **不加**：实测 **4 个未合并分支**在改它 + 自我授权味道 ⇒ 仅演示未提交，登记未清项 |
| `.github/workflows/**`（ci.yml 拆分） | 外部阻塞 | **本轮未动**，见 §四 |

**处置依据**：§三 明写「建议，执行方按需调整并回报」+ §五「每步给原始输出」⇒ 本次为**授权范围内的调整 + 回报**。
**我的簿记缺口（记我账上）**：D1/D2/D3 与 brief 扩写均经队长口头授权，但我**未同步改写 task-3 的 `write_scopes`** ⇒ verifier 在 T4 §G 点名 3 个「声明外文件」。**追认全部 3 项**（`ownership.yaml`、`DOCS-REGISTRY.yaml`、brief），并登记该流程缺口。

---

## 三、闭环判据对照（缺一不算完成）

| 判据 | 状态 | 证据落点 |
|---|---|---|
| ① 授权卡改共享件 ⇒ 通过 | ✅ **已构造并双人复算** | T1 夹具 + T2 §A；T3 真实件红→绿（`evidence/D979-迁移落地-20260926/`）；T4 §A 独立真实 `git add` 复算 |
| ② 非授权卡改共享件 ⇒ 仍拒收 | ✅ | T2 §B（`MISSING=D981` exit 1）；T4 §B（`MSG=D999` ⇒ `MISSING=D999` exit 1） |
| ③ `ci.yml` 拆分后 CI 行为不变 | ⛔ **未做（外部阻塞）** | §四；#800/#803/#806 实测 `dirty/blocked/dirty` 未合并 |
| ④ 号段水位改为生成物 | ✅ **幂等已双人复算** | T3：`git diff --exit-code -- 号段/` = 0、`--check` 逐字节一致；T4 §D 独立复跑 + **判别性**（写死常量 ⇒ 4 文件漂移 exit 1，`bash -n`=0 非崩溃红）+ 对照变异仍绿 |
| ⑤ 迁移后旧路径不残留 | ✅ **（限定口径）** | 派单模板.md：35 行命中，`scripts/` **0 行**；号段水位.md：6 行，其中 `scripts/` 2 行为生成器自述/模板文本（非代码依赖）。两文件均保留**短指针**指向新目录 |
| ⑥ 两侧反例进 `tests/control-tower/**` | ✅ | `tests/control-tower/commit-msg-consistency.test.sh` 用例 14–18（**30 ✅/0 ❌**，含既有 13 例回归）；`shared_file_decl.test.sh`（**40 ✅/0 ❌**）；`gen-watermark.test.sh`（**36 ✅/0 ❌**） |

### ⚠️ 判据①的一条**重要限定条件**（verifier T4 P-4 观察，**不得省略**）
机制是**放行口**，不是**免检牌**。D328 只在「resolver 解析出的 brief **真认领**了暂存文件」（`GENUINE=1`）时才进入 D# 比较；若某日窗口内无任何 brief 真认领该共享件，D328 **整段跳过**（既有 fail-open）。
⇒ 「加了声明行 ⇒ 从此不再被误拦」**不成立**。准确表述是：**当同日窗口内有并发卡改该共享件、且旧路径会误判劫持时，声明行使其放行**——这恰是 09-24/09-25 三起被拒场景的成因，故机制对症。该 fail-open 为既有行为，本次未触碰。

---

## 四、Phase 2（⒜ `ci.yml` 拆分）— **未做，外部阻塞，附实测**

```
#800  updated=2026-09-25T10:02:49Z  mergeable_state=dirty    ci.yml +44/-1
#803  updated=2026-09-25T15:51:35Z  mergeable_state=blocked  ci.yml +7/-0
#806  updated=2026-09-25T16:26:37Z  mergeable_state=dirty    ci.yml +2/-1
```
三件**均未合并**，且 `ci.yml` 的触发面为 `push: [main, feat/*]` + `pull_request: [main]`。
此时拆分 `ci.yml` = 与三件形成**四方冲突面**。派单件 §四 建议亦为「本批 ⒝ 先合，拆分放最后」。
**判定：另派卡，触发条件 = #800/#803/#806 全部合并或 CTO 裁定作废。**

---

## 五、CI 实况（**不构成 CI 验证**，如实登记）

| PR | head | check-runs |
|---|---|---|
| #815（机制） | `aa98eb7d` | **0 个** —— 分支 `fix/*` 不在 `push` 触发面、且 PR base ≠ main ⇒ **零 CI** |
| #819（T2 证据） | `da144d95` | **0 个** —— 同上 |
| #820（PR2） | `f34f3f55` | **10 个，全 `queued`，无结论** |
| #823（PR3） | `b16a29d7` | **10 个，全 `queued`，无结论** |
| #825（T4 证据） | `ed0f9fe8` | `feat/` 触发面（观测中） |

**两条须 CTO 知情**：
1. **栈式 PR 的 CI 盲区**：`ci.yml` 的 `pull_request: branches: [main]` ⇒ **base ≠ main 的 PR 完全不触发 CI**。本批 #815/#819 因此**零 CI 覆盖**。#815 的代码改动**被 #820 的 `feat/*` push-run 覆盖**（同 SHA 血统），但那是**间接**覆盖，不能表述为「#815 通过 CI」。
2. **CI 至今未出结论**（20/20 `queued`，runner 积压），因此**本批任何一件都不得声称 CI 验证**。

---

## 六、独立自验结论（**非审计结论**）

| 任务 | 对象 | 自验结论 | 证据 |
|---|---|---|---|
| T2 | #815 @ `fe01c3af` | **通过** | `evidence/D979-独立自验-20260926/`（18 文件，PR #819） |
| T4 | #820 @ `f34f3f55` / #823 @ `b16a29d7` | **通过** | `evidence/D979-迁移落地复验-20260926/`（10 文件，PR #825） |

**核心判别性证据**（坑清单「接线了 ≠ 被执行」）：
- T2 §D：两种变异（`=0`→`=99`、`DECL_RC=0`→`1`）后 `bash -n=0`（**未崩溃**）+ 反例①变红，输出为**原劫持判定文案** ⇒ 属「回到原劫持判定而红」，**非崩溃红**；恢复后复绿。
- T4 §D：生成器写死常量 ⇒ 4 文件漂移红（`bash -n=0`）；**对照变异（仅改摘要行）仍绿** ⇒ 排除「改了就红」的假判别性。
- T4 §E（最高优先）：`ownership.yaml` `+8/−0`，**删掉新增块后与 base 逐字节相同**（sha256 双方 `fe9cd04d…`）；`**` 兜底与 `domain_neutral` 未动；**真跨域仍 `❌ FAIL 跨域` exit 1**、同域 `✅ PASS` exit 0 ⇒ **改了归属表 ≠ 放宽门禁**。

**未验证部分（原样转述，不软化）**：Windows 实机未跑；**未跑 CI / 13 组门禁 / vitest ⇒ 不构成「CI 全绿」证明**；未在仓库内真跑 `git commit` 端到端（用 `SYNO_STAGED_FILES` 注入）；未做独立 clone 验证；D 条变异施加于 `/tmp` 副本（等价性由 sha256 + `git diff HEAD` 空 + `grep -c` 支撑）；resolver 在 CI 干净检出 / Windows 路径下的交互未验；零丢失为**行级**（同义改写不被识别，实测未命中 0）；PR 上下文 `check-pr-budget.sh`、DOCS-REGISTRY 登记门禁未跑。

---

## 七、未清项登记（未做=未做）

| # | 项 | 证据 / 现状 | 建议 |
|---|---|---|---|
| 1 | **Phase 2 `ci.yml` 拆分** | §四（三件未合并，`dirty/blocked/dirty`） | 另派卡，待三件合并/作废 |
| 2 | `task-state/D954.json` 未落声明 | 4 个未合并分支在改；仅真实 index 红→绿演示（测后 `git restore`，零残留） | 随 D954 线合并后落 |
| 3 | **resolver 锚点污染**（新缺陷） | 分支名 `fix/d979-**d328**-shared-decl` 的 `d328` 被 `_ids_of` 当强锚点 ⇒ 命中陈旧 brief `2026-09-06-D582-CT-60-D328-*`。**不在写集，未修** | **立卡**：锚点须限定卡位（如 `^(feat\|fix\|docs\|chore)/d(\d+)-`），描述文字里的 `d<数字>` 不得当锚点 |
| 4 | **栈式 PR 的 CI 盲区**（新缺陷） | `push:[main,feat/*]` + `pull_request:[main]` ⇒ `fix/*` 栈式 PR **零 CI**（#815/#819 实测 0 check-run） | **立卡**：触发面须含 base≠main 的 PR（#800 已部分触及，未合并） |
| 5 | **D734 ① PR 文件数按整栈口径** | 栈算 `21 > 12`；PR3 相对其 **PR base** 实为 **11 件 ≤12**。未调上限、未加豁免 | 立卡：基线改为「PR 实际 base」 |
| 6 | `check-brief-parseable.sh` Done 契约未文档化 | 只认 `- [ ]`/`- [x]` 且 `verify:` **必须同行**；头部注释与实际实现不一致（T1 曾因 fail-open 假绿） | 立卡 |
| 7 | `DOCS-REGISTRY` 两处缺陷 | ① `README.md` 曾「已登记」实为 **basename 子串匹配撞别处条目 = 假绿**；② 该 yaml 在 line 35 `founder + DSH` 裸标量处**既有**解析失败（改前副本同样失败） | 立卡 |
| 8 | `.gitignore` 语义缺口 | `*.log` 使 `.gitignore:78-79`「证据目录 git add 无需 -f」失效（实拦 11 文件） | 立卡：`!docs/synova/product-lines/evidence/**/*.log` |
| 9 | 生成器未接 CI | `gen-watermark.sh --check` 可跑，但 `ci.yml` = Phase 2 禁碰 ⇒ 漂移暂无自动拦截 | 随 Phase 2 接 |
| 10 | `PR归属与派单参数-20260926.md` 指针 | 实测**不在 main** | 不制造断链；FIX-012 落 main 后补 |
| 11 | 问题修复台 FIX-011 → 已闭环 | `问题修复台-20260925.md` 实测**不在 main**（本机 untracked / 另分支） | 待其落 main 后更新 |
| 12 | K3 号段未定义 | 原始件只约定 Mac/Win；`K3.md` 只报实测最大 D1000 | 另卡 |
| 13 | `号段/README.md` 声明行重复 2 次 | verifier 现象登记（不影响力） | 顺手清理 |
| 14 | Note 状态未收口 | `memory/notes/implemented/2026-09-26-fix011-*.md` 头字段仍写「proposed」 | CTO 侧收口 |
| 15 | `/tmp/.synova-before-brief` 陈旧证据 | 2026-09-24 别的 session 的 D941 ⇒ 1 条无关软提示；无作废机制 | 立卡 |

---

## 八、升级创始人知情（派单件 §五 硬要求）

派单件：「**同类第 2 次**（今日已 3 次）⇒ 完成后**升级创始人知情**」。
本件为**同类第 4 次同族事件**（D328 劫持误判），且本轮**又暴露 2 条同族新缺陷**（未清项 #3 锚点污染、#4 栈式 PR CI 盲区）。**已按规则升级**：同一类问题反复出现 = 防线系统性失效，非个别失误。

---

## 九、证据索引（全部提交进仓库）

| 件 | 路径 / PR |
|---|---|
| 队长前提复现原始输出 | `docs/synova/product-lines/evidence/D979-队长收尾-20260926/0-队长前提复现-原始输出.txt` |
| 队长收尾报告（本文件） | `docs/synova/product-lines/evidence/D979-队长收尾-20260926/队长收尾报告.md` |
| T2 独立自验证据 | `docs/synova/product-lines/evidence/D979-独立自验-20260926/`（PR #819） |
| T3 迁移落地证据 | `docs/synova/product-lines/evidence/D979-迁移落地-20260926/`（PR #820/#823） |
| T4 迁移落地复验证据 | `docs/synova/product-lines/evidence/D979-迁移落地复验-20260926/`（PR #825） |

**栈式 PR 拓扑**（base 逐级声明，全部非 main）：
```
#809 docs/d979-fix011-shared-file-mechanism   （派单件，已开）
 └─ #815 fix/d979-d328-shared-decl            （机制 T1）
     ├─ #819 docs/d979-verify-evidence        （T2 证据）
     └─ #820 feat/d979-dispatch-tpl-dir       （PR2 派单模板）
         └─ #823 feat/d979-watermark-gen      （PR3 号段 + 登记 + 归属）
             └─ #825 docs/d979-migrate-verify-evidence（T4 证据）
 └─ (本件) docs/d979-lead-closeout            （队长收尾）
```

---

*本报告所有数字均取自命令原始输出；凡未经独立复算者已在 §六 末段逐条标注。*