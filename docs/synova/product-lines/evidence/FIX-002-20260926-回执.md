# FIX-002 回执｜#796 与 D331 同改：新 clone 态对账器 rc=0（读面改动）

- **卡号 / 分支 / 基线 sha**: FIX-002（问题修复台 `docs/synova/coordination/问题修复台-20260925.md`，PR #805 未落 main）｜`feat/d969-daily-cto-board-paired-test`｜改前 HEAD = `41b97740`
- **PR**: #796（已存在，产出者自更新，未 force push）
- **工作树**: `.synova-wt-fix796`（本任务自建；未复用他人工作树，未碰 `.synova-wt-d962m`）
- **写集（逐文件）**: `scripts/control-tower/check-bypass-log.sh`（读面）｜`tests/control-tower/check-bypass-log.test.sh`（成对夹具）｜`memory/notes/proposed/2026-09-26-fix002-ledger-absent-vacuous-pass.md`（决策 Note）｜本文件。**未碰** `.github/workflows/ci.yml`（本批单写者 = FIX-013 任务）、`scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`。
- **判定**: 待 CTO 收件闸 + K3 终审（**本人只给自验结论，不自称通过**）

---

## 0. 前提实测（先跑再改）

**0-1 新 clone 态 rc（判据不成立 ⇒ 按卡执行读面改动）**

```
$ rm -rf /tmp/repro796 && git clone --no-hardlinks /Users/wane/SynovaAgent /tmp/repro796
Cloning into '/tmp/repro796'...
done.
$ cd /tmp/repro796 && git checkout 41b97740 && git log --oneline -1
41b97740 chore(D969): write_set 补 .claude/bypass.log（账本出库解析件）
$ bash scripts/control-tower/check-bypass-log.sh; echo rc=$?
⚠ fetch 失败——base 可能陈旧（push URL 不更新 tracking ref）；建议 git fetch origin 后重试
❌ bypass.log 不存在: /private/tmp/repro796/.claude/bypass.log
  执行证据链缺失 — 请确认提交均经 synova-commit（含 COMMITTED 记录）或一次性补记
rc=1
```

**0-2 账本是否在树里**

```
$ git ls-files scripts/control-tower/ | grep -i ledger; echo rc=$?
scripts/control-tower/bypass-ledger.sh
rc=0
$ git ls-files | grep -i "bypass.*ledger"; echo rc=$?
memory/notes/proposed/2026-09-14-bypass-ledger-out-of-git.md
scripts/control-tower/bypass-ledger.sh
tests/control-tower/bypass-ledger.test.sh
rc=0
$ ls -la .claude/ | grep -i bypass
-rw-r--r--@   1 wane  wheel   4419 Sep 26 00:24 bypass.log.archive
```

⇒ 账本 `.claude/bypass.log` **不在树里**（仅 `bypass.log.archive`，且该归档不被本分支的 `bypass-ledger.sh sources` 纳入）⇒ rc=1 复现成立，执行读面改动。

**0-3 默认 base 在真实远端存在（排除「靠改默认 base 才 rc=0」的取巧路径）**

```
$ curl -s -o /dev/null -w "http=%{http_code}\n" -H "Authorization: Bearer $TOKEN" \
    https://api.github.com/repos/tangbaobao520/SynovaAgent/branches/feat/prompt-architecture
http=200      # sha d869560f350d80dc247407cb835cd94ed6d8a31a
```

**0-4 冲突扫描（改前，完整输出；排除 task-briefs/audit-reports/coordination/plans/task-state/memory 与 .html 生成物）**

```
$ git grep -n "check-bypass-log" -- ':!*.html' ':!.claude/task-briefs/**' ':!docs/synova/audit-reports/**' \
    ':!docs/synova/coordination/**' ':!docs/plans/**' ':!task-state/**' ':!memory/**'
共 30 处
.codex/control-tower/VERSION.md:195,211,232,340                      （文档：历史记录）
docs/synova/product-lines/evidence/D922-phase0-recon-20260923.md:263,309,332,379,381,515（历史证据）
docs/synova/project/ledger.json:3182                                  （派生台账）
scripts/control-tower/check-bypass-log.sh:3,13,63,90                  （本文件）
scripts/control-tower/pre-audit-summary.sh:14,44,46                   （调用方，只读引用）
scripts/pre-push-check.sh:425,444                                     （唯一生产调用方：门禁 7，传 origin/main）
tests/control-tower/bypass-ledger.test.sh:12,25,101                   （union 读断言）
tests/control-tower/check-bypass-log.test.sh:6,16,24                  （本测试文件）
tests/control-tower/pre-audit-summary.test.sh:72,73,75
tests/control-tower/tag-bypass-wiring.test.sh:34
```

- 唯一生产调用方 = `scripts/pre-push-check.sh:425`（门禁 7），其 base 取 `origin/main`（`:438` D521 注释「对账基永远取 origin/main」）⇒ 默认 base 仅影响无参数调用（测试/排障）。
- **写集冲突声明（须 CTO 裁决，已 send_message 上报）**: #799（`fix/d970-bypass-ledger-stage2`，sha `1c7ab52f`）**改了同一文件**（`git diff --stat origin/main 1c7ab52f -- scripts/control-tower/check-bypass-log.sh` = `43 +-`），且其读面语义为「空范围 vacuous pass / **有提交待对账但全部来源不可读 → exit 1**」——与本卡 ⒜「无账本 → rc=0」在「范围非空 + 无来源」这一态**结论相反**。本件按本卡字面口径落地；两分支合入时该文件必冲突，需 CTO 择一收敛（见 Note §与 D970 Stage 2 的关系）。

---

## 1. 改动（`git diff --stat` 原始输出）

```
$ git diff --stat
 scripts/control-tower/check-bypass-log.sh    |  43 ++++++++----
 tests/control-tower/check-bypass-log.test.sh | 100 +++++++++++++++++++++------
 2 files changed, 111 insertions(+), 32 deletions(-)

$ git diff --numstat -- scripts/control-tower/check-bypass-log.sh tests/control-tower/check-bypass-log.test.sh
31	12	scripts/control-tower/check-bypass-log.sh
80	20	tests/control-tower/check-bypass-log.test.sh
```

读面语义（新）：

| 态 | 旧 | 新 |
|---|---|---|
| 本机无任何账本来源（新 clone / 无残留） | exit 1（拒推） | **exit 0 + 显式可见输出 + degraded-events 记录** |
| 有来源 + 范围内提交无登记 | exit 1 | exit 1（不变） |
| 显式 `SYNO_BASE_REF` 不可解析 | exit 1 | exit 1（不变，且**先于**账本态判定） |
| 非显式默认 base 不可解析（fetch 后） | exit 2 | exit 2（不变） |
| `git log` 执行失败 | exit 2 | exit 2（不变） |

非静默证据：放行必须打印 `⚠️ 本机无账本来源 — 判「无绕过记录」放行 (D331 读面 / FIX-002)` + `已查来源: …`，并追加 `.claude/degraded-events.log`。**无 `|| true`、无删红路径。**

---

## 2. 判据逐条

### ⒜ 无账本 → 判「无绕过记录」放行 rc=0（新 clone 态，卡面原样命令）

材料：`/tmp/repro796`（一次性 clone；`origin/feat/prompt-architecture`、`origin/main` 均按真实 GitHub 远端刷新：`origin/main = ef299746`）；账本不在树（§0-2）；付与将推送的同版本脚本（sha256 `8623cce3d15cd9f5c5aa8b01f63e74acfd64b71fc26ade9fbce452eca6963b4d`）。

```
$ ls -la .claude/ | grep -i bypass
-rw-r--r--@   1 wane  wheel   4419 Sep 26 00:24 bypass.log.archive     # 无 bypass.log
$ bash scripts/control-tower/check-bypass-log.sh; echo rc=$?
⚠ fetch 失败——base 可能陈旧（push URL 不更新 tracking ref）；建议 git fetch origin 后重试
⚠️  本机无账本来源 — 判「无绕过记录」放行 (D331 读面 / FIX-002)
  已查来源: /private/tmp/repro796/.claude/bypass.log
  注: 账本不随仓走（#796 删除旧路径 + D970 Stage 2 停写）；本机零登记 = 零可对账证据
rc=0
```

（`⚠ fetch 失败` 一行是因该 clone 的 `origin` 仍指向本机路径 `/Users/wane/SynovaAgent`——该机没有 `feat/prompt-architecture` 本地分支；与判据无关，真 GitHub clone 上该 fetch 会成功。rc 不受其影响。）

副作用实测：脚本未弄脏 clone 工作区（`git status --porcelain` 仅显示我手工覆盖的脚本本身；`degraded-events.log` 被 `.gitignore:8 *.log` 覆盖 —— `git check-ignore -v .claude/degraded-events.log` → `.gitignore:8:*.log`）。

### ⒝ 成对反例（判别性夹具，非 grep 型静态判据）

夹具在 `tests/control-tower/check-bypass-log.test.sh`，沙箱 `GIT_DIR`/`GIT_WORK_TREE` 显式绑定（同 D508 防宿主污染约定）：

- 绿：沙箱内**无任何账本来源** + 有一个未登记提交 → `exit 0`，且断言输出**必须含**「本机无账本来源」（显式可见；否则用例失败）。
- 红：沙箱内**账本存在且含 1 条 `detected-bypass` 记录**（形态取自 `scripts/hooks/post-commit.sh:113`：`detected-bypass head-mismatch marker=… parent=…`，不含提交 HASH）+ 范围内提交无 `COMMITTED` 登记 → `exit≠0`；随后**补记该提交 → 同一夹具转绿**（阳性对照：隔离变量，证明红来自「缺记录」而非夹具本身）。

测试原始输出（改后）：

```
$ bash tests/control-tower/check-bypass-log.test.sh; echo test_rc=$?
=== D414/U1c check-bypass-log 对账门禁测试 ===
  ✅ 接线: git log 失败 fail-closed exit 2 已接入
  ✅ 无新提交（空范围，有账本来源）→ exit 0
  ✅ 无账本来源（新 clone 态）→ exit 0，且放行显式可见
  ✅ 有账本 + detected-bypass + 提交缺登记 → exit≠0；补记后转绿（门禁未削弱）
  ✅ 显式 base 不可解析 → exit 1
  ✅ D508: merge main 后 main 侧提交不再要求补记（死循环根治）
  ✅ D508: 无记录新提交仍被拦（对账强度不降）
  ✅ D508: 宿主 index 未被沙箱污染
  ✅ D513/③: 防御 fetch 已接线

结果: 9 通过, 0 失败
test_rc=0
```

**判别力实验（改坏即红）** —— 在脚本副本上做两处变异，跑同一夹具，跑完按 sha256 复原：

| 变异 | 内容 | 夹具结果 | 复原 |
|---|---|---|---|
| A | 把缺记录红路径改成 `if false; then`（模拟 `|| true` 式掩盖） | `结果: 5 通过, 2 失败`（`❌ 有账本 + detected-bypass 时漏拦`、`❌ D508: 无记录提交漏拦！`）`rc=1` | sha256 `8623cce3…6b4d` 一致 |
| B | 把放行路径去掉（`if ! _source_exists && false; then`，= 回退旧行为） | `结果: 8 通过, 1 失败`（`❌ 放行必须显式可见（输出未含『本机无账本来源』）`）`rc=1` | sha256 `8623cce3…6b4d` 一致，复跑 9/9 绿 |

⇒ 夹具对「红路径被削弱」与「放行路径消失」两侧都报红，绿不是空壳。

> 附带修正（同文件内）：D508 沙箱块原为裸子 shell，块内 `exit 1` 与 `PASS=$((PASS+3))` **都不回传父计数器**（块内失败不上报）。本件改为 `if ( … ); then PASS=$((PASS+3)); else FAIL=$((FAIL+1)); fi` —— 由变异体 A 可见 `D508: 无记录提交漏拦！` 现已计入失败数。此为**收紧**，非放宽。

### ⒞ CI 复跑结论 + `git ls-remote` 回执

见本文件 §4（push 后追加）。

---

## 3. 红线段（保留哪些红；禁改断言/删断言/`|| true` 的证据）

代码 `exit` 语句（只统计非注释行）：

```
$ git show HEAD:scripts/control-tower/check-bypass-log.sh | grep -nE '^[[:space:]]*exit [0-9]+'
51:  exit 1       # ← 被本件替换的唯一红路径：LOG 不存在 → 无条件 exit 1
58:    exit 1     # 显式 SYNO_BASE_REF 不可解析（保留，且提前到账本态判定之前）
64:    exit 2     # 非显式 base 不可解析（保留）
91:  exit 2       # git log 失败（保留）
112:  exit 1       # 范围内提交缺登记（保留）
116:exit 0
$ grep -nE '^[[:space:]]*exit [0-9]+' scripts/control-tower/check-bypass-log.sh
54:  exit 1       # 显式 SYNO_BASE_REF 不可解析（保留）
74:  exit 0       # 新增：本机无账本来源 → 显式放行（minimal 变更面）
83:    exit 2     # 非显式 base 不可解析（保留）
110:  exit 2       # git log 失败（保留）
131:  exit 1       # 范围内提交缺登记（保留）
135:exit 0
```

`|| true`（非注释行）：

```
$ git show HEAD:… | grep -cE '^[^#]*\|\| true'
5      # fetch 容错 1 + degraded 日志追加 2 + D451 文件列表过滤 2
$ grep -cE '^[^#]*\|\| true' scripts/control-tower/check-bypass-log.sh
6      # +1 = 放行态的 degraded-events 追加（与既有 2 处同型：只护日志追加，不参与判定）
```

⇒ 唯一从红转绿的态是「本机零来源」，且其输出**必须可见**（测试断言）；缺记录红路径、base 红路径、`git log` 红路径**一条未删**，判定语句未改。新增 `|| true` 只出现在日志追加上，与既有两处同型，不落在任何判定路径上。

---

## 4. CI 复跑 + ls-remote 回执（push 后追加）

（待补：push 后追加 PR #796 的 check-runs 原始结论 + `git ls-remote --heads origin | grep feat/d969` 原始输出。）

---

## 5. 遗留清单（未清项 / 已知缺口）

| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| L1 | 本件与 #799（D970 Stage 2）**改同一文件且读面口径在「范围非空 + 无来源」态互斥**；合入顺序/收敛口径须 CTO 裁决 | CTO | 已上报（send_message + Note），**未清** |
| L2 | `tests/control-tower/check-bypass-log.test.sh` **不在 CI 的 CT 测试 for 清单**（`grep -n "check-bypass-log" .github/workflows/ci.yml` → 0 命中）。它经 `scripts/pre-commit-check.sh:617` → `ct-test-gate.sh`（配对测试门禁）在**改动该脚本时**被执行，但 CI 不保证每轮跑 | CTO / ci.yml（本批单写者 = FIX-013 任务） | 登记，**未清**（与 FIX-003 同类） |
| L3 | 本机账本「从未存在」与「被人为删除」不可区分 ⇒ 该态放行；根治需账本存活证明（提交 trailer / git notes） | 控制塔线（后续卡） | 登记，继承 D970 已登记缺口 |
| L4 | `/tmp/repro796` 的 `origin` 指向本机路径（非 GitHub），故 §⒜ 中 `fetch` 告警不代表真 GitHub clone 行为；真 GitHub clone 全量 clone 本机实测 `failed to store: 100001`（未能完成），已改用「clone + 按 GitHub 远端刷新 ref」等价构造并如实标注 | 本件 | 已显式标注，**不当作已验证真 clone** |
| L5 | 归属：PR #796 的更新由本件完成；`.synova-wt-fix796` 工作树保留至 CTO/K3 收口（不自动释放） | CTO | 待收口 |
