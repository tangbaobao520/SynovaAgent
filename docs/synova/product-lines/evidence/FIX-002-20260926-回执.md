# FIX-002 回执｜#796 撤销账本删除（主路径 A）+ 被删证据链修复

- **卡号 / 分支 / 基线 sha**: FIX-002（`docs/synova/coordination/问题修复台-20260925.md`，PR #805 未落 main）｜`feat/d969-daily-cto-board-paired-test`｜基线（CTO 冻结）= `41b97740`
- **PR**: #796（已存在；产出者 = 本小队，未 force push）
- **工作树**: `.synova-wt-fix796`（本任务自建；未复用他人工作树，未碰 `.synova-wt-d962m`）
- **写集（逐文件）**: `.claude/bypass.log`（撤销删除 + 证据链修复）｜`docs/synova/product-lines/evidence/FIX-002-20260926-回执.md`｜`memory/notes/rejected/2026-09-26-fix002-ledger-absent-vacuous-pass.md`（被否决提案留档）｜`.claude/task-briefs/2026-09-26-FIX-002-796-restore-ledger.md`。**未碰**：`scripts/control-tower/check-bypass-log.sh`、`tests/control-tower/check-bypass-log.test.sh`（已回退原状）、`.github/workflows/ci.yml`（本批单写者 = FIX-013 任务）、`scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`。
- **判定**: 待 CTO 收件闸 + K3 终审（**本人只给自验结论，不自称通过**）

---

## 0. 前提实测与**口径更正**（CTO 裁决，2026-09-26）

### 0-1 CTO 口径更正（原文要点，必须作为未清项回报台账）

原判据「新 clone 态跑 `check-bypass-log.sh` rc=0」**不可直接执行**，缺两点：

- ⒜ **必须显式写 base**：唯一生产口径是 `bash scripts/control-tower/check-bypass-log.sh origin/main`（调用方 `scripts/pre-push-check.sh:425-444`：`CHECK_BYPASS=…/check-bypass-log.sh` + `BYPASS_BASE="origin/main"`，注释「对账基永远取 origin/main」）。**裸跑**默认 base = `origin/feat/prompt-architecture` ⇒ range = **2226 提交** ⇒ main/#796/#799 全 rc=1，属**测量假象**。
- ⒝ 该判据**只在 range 为空**（clone 处于 main）时成立；**带提交的分支在新 clone 上必然 rc=1**（跨机登记缺口）。
- 建议改写为：**「新 clone 于 main HEAD 跑 `check-bypass-log.sh origin/main` rc=0，且 #796 合入后仍 rc=0」**。

CTO 四态矩阵（正确口径，raw 输出）：

| 态 | range | rc | 输出要点 |
|---|---|---|---|
| main ef299746 | 0 | **0** | ✅ 全部提交有记录 |
| PR-2 e1353350（归档在树） | 4 | **0** | ✅ 全部提交有记录 |
| #799 1c7ab52f | 8 | **1** | ❌ 有待对账提交但执行证据链整体不可读 |
| #796 41b97740 | 9 | **1** | ❌ bypass.log 不存在 |

附带实测（CTO）：**#799 读面 + PR-2 归档**（模拟终态）→ 仍 rc=1；该分支 8 个 HASH 在冻结归档中 **0 命中** ⇒ D970 brief 已登记的「未来跨机缺口」实测成立。

### 0-2 本件复现（同一口径）

```
$ bash scripts/control-tower/check-bypass-log.sh origin/main; echo rc=$?      # 41b97740 态
❌ bypass.log 不存在: …/.claude/bypass.log
  执行证据链缺失 — 请确认提交均经 synova-commit（含 COMMITTED 记录）或一次性补记
rc=1
$ git log --format='%h %s' origin/main..41b97740 | cat
41b97740 chore(D969): write_set 补 .claude/bypass.log（账本出库解析件）
3ce59bbb chore(D969): 账本出库解析——移除 .claude/bypass.log（使 PR 相对 main 无该文件冲突）
a6bb28ab Merge remote-tracking branch 'origin/main' into feat/d969-daily-cto-board-paired-test
3bfe33df chore: bypass COMMITTED 登记 (auto hook, D521)
ab12d4d4 chore(D969): 改号 D1008→D969（Mac 段 D9xx，CTO 裁决）+ 补 write_set（D708 夹带修复）
f2b9b8ab chore: bypass COMMITTED 登记 (auto hook, D521)
db96fc5c docs(D1008): 收尾三件（diff / 自验结论 / 遗留清单）+ ACK 完整输出落库
a42b8940 chore: bypass COMMITTED 登记 (auto hook, D521)
ee649323 feat(D1008): daily-cto-board 配对测试 + 对账探针 runner + 默认 REPO 去硬编码
```

---

## 1. 裁决与执行（主路径 A）

**CTO 裁定**：#796 撤销绕道删除（`3ce59bbb` 是「为躲 union 冲突的本地绕道」，不是迁移）；#796 不再承担 D331 迁移；迁移留在 D970，顺序 **PR-2（归档）→ #799 → #796**。**禁止自创第二套读面语义**（与 #799 分叉）。

本件执行：

1. **回退读面分叉**：`scripts/control-tower/check-bypass-log.sh`、`tests/control-tower/check-bypass-log.test.sh` 恢复到 `41b97740` 原状 —— 实测差异 **0 行**：

```
$ git diff 41b97740 -- scripts/control-tower/check-bypass-log.sh tests/control-tower/check-bypass-log.test.sh | wc -l
0
```

2. **撤销删除（恢复 tracked，内容取 origin/main 版本 = 相对 main 零内容分叉）**：

```
$ git checkout origin/main -- .claude/bypass.log
$ wc -l < .claude/bypass.log
    1810
$ git ls-files .claude/bypass.log
.claude/bypass.log
```

3. **被该删除破坏的证据链修复**（详见 §2）。

---

## 2. 证据链修复：3 条逐字还原 + 1 条显式补记

恢复 `origin/main` 版本账本后，生产口径对账仍列出 **4 条缺失**（原始输出）：

```
$ bash scripts/control-tower/check-bypass-log.sh origin/main; echo rc=$?
❌ bypass.log 缺以下提交记录（执行证据链断裂）:
  chore(D969): write_set 补 .claude/bypass.log（账本出库解析件） [41b97740]
  chore(D969): 改号 D1008→D969（Mac 段 D9xx，CTO 裁决）+ 补 write_set（D708 夹带修复） [ab12d4d4]
  docs(D1008): 收尾三件（diff / 自验结论 / 遗留清单）+ ACK 完整输出落库 [db96fc5c]
  feat(D1008): daily-cto-board 配对测试 + 对账探针 runner + 默认 REPO 去硬编码 [ee649323]
  请确认提交经 synova-commit（含 COMMITTED 记录）或一次性补记后再推送
rc=1
```

豁免核对（`--no-merges` + D451 纯补记豁免）：`a6bb28ab` = merge（排除）；`3bfe33df`/`f2b9b8ab`/`a42b8940` 仅改 `.claude/bypass.log`（D451 豁免）；`3ce59bbb` 仅改 `.claude/bypass.log`（删除动作本身，豁免）。⇒ **恰为上述 4 条**。

### 2-1 三条**真实 hook 记录逐字还原**（源 = 被 `3ce59bbb` 删除前的分支账本 blob）

```
$ git show 3ce59bbb^:.claude/bypass.log | grep <hash>       # 逐条原始行
2026-09-25T17:41:45+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=ab12d4d4152a790e18699641a725c3f55accc4f2
2026-09-25T16:49:43+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=db96fc5c48c600bb0284970a4f7a9f1e3ef53adc
2026-09-25T16:48:18+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=ee64932351ff02be7cde41d6c01a38d0d7ad56e1
```

⇒ 这 3 条**不是新写证据**：它们是 `3ce59bbb` 从树里删掉的原证据，逐字（含原时间戳与原文）还原。任何人可用上面这条命令复核。

### 2-2 第 4 条（`41b97740`）**显式一次性补记**（D451）

证据可得性实测（本机真实账本 + 全部可达 blob，均 0 命中）：

```
$ for rev in 3ce59bbb^ 3ce59bbb 41b97740; do git show $rev:.claude/bypass.log 2>/dev/null | grep -c "$(git rev-parse 41b97740)"; done
0
0
0
$ grep -c 41b97740ac1d2d869c1db9334f77859e9d8cf4de /Users/wane/SynovaAgent/.claude/bypass.log   # 主工作区本机账本（1813 行）
0
$ cat /Users/wane/SynovaAgent/.sessions/*/bypass.log 2>/dev/null | grep -c 41b97740ac1d2d869c1db9334f77859e9d8cf4de
0
```

⇒ 该提交的 hook 原文只存在于**产生它的那台机器**（D970 已登记的跨机缺口）。按 D331/D451 既有机制做**一次性补记**，并在行内**显式标注来源缺失**（不伪装成 hook 原文）：

```
2026-09-26T…Z | COMMITTED | 一次性补记（D451 跨机登记缺口；本机与全部可达 blob 无该提交 hook 原文，源提交仅改 task-state/D969.json） | HASH=41b97740ac1d2d869c1db9334f77859e9d8cf4de
```

---

## 3. 判据

```
$ bash scripts/control-tower/check-bypass-log.sh origin/main; echo rc=$?
✅ bypass.log 对账通过: 481805a88bbc7c26a17a1812c82c8b8d6478cdc9..HEAD 全部提交有记录
rc=0

$ git ls-files .claude/bypass.log
.claude/bypass.log

$ bash tests/control-tower/check-bypass-log.test.sh; echo test_rc=$?
=== D414/U1c check-bypass-log 对账门禁测试 ===
  ✅ 接线: git log 失败 fail-closed exit 2 已接入
  ✅ 无新提交对账 → exit 0
  ✅ bypass.log 缺失 → exit 1
  ✅ 显式 base 不可解析 → exit 1
  ✅ D508: merge main 后 main 侧提交不再要求补记（死循环根治）
  ✅ D508: 无记录新提交仍被拦（对账强度不降）
  ✅ D508: 宿主 index 未被沙箱污染
  ✅ D513/③: 防御 fetch 已接线

结果: 5 通过, 0 失败
test_rc=0
```

被 CTO 否决的读面提案**未留在树内**：`git diff 41b97740 -- scripts/…/check-bypass-log.sh tests/…/check-bypass-log.test.sh` = 0 行；提案全文移入 `memory/notes/rejected/`（四态语义）。

---

## 4. CI 复跑 + `git ls-remote` 回执（push 后追加）

（待补：push 后追加 PR #796 check-runs 原始结论 + `git ls-remote --heads origin | grep feat/d969` 原始输出。）

---

## 5. 遗留清单（未清项 / 已知缺口）

| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| L1 | **台账 FIX-002 判据需改写**（口径更正）：裸跑不可执行（base 必须显式 = `origin/main`）；「新 clone 态 rc=0」只在 **range 为空**（clone 处于 main）时成立，带提交的分支在新 clone 上必然 rc=1 | CTO（台账单写者） | **未清**，本件已实测并回报 |
| L2 | **跨机登记缺口**（D970 已登记，本件实测复现）：他机产生的登记只落其本机；本机与全部可达 blob 对 `41b97740` **0 命中** ⇒ 只能一次性补记 | 控制塔线（后续卡：提交 trailer / git notes 存活证明） | 登记，**未清** |
| L3 | 本件为 `41b97740` 做了一次 D451 补记（来源缺失已行内标注）；若 CTO 要求**零补记**，需由产生该提交的机器提供其本机 `bypass.log` 原文替换该行 | CTO | 待裁决 |
| L4 | `.claude/bypass.log` 已恢复 tracked ⇒ 与 D970（#799 删除该文件）合并时**必然 modify/delete 冲突**，按既定顺序 PR-2 → #799 → #796，收口时取「删除」侧 | CTO 合并时 | 已知，**未清** |
| L5 | `tests/control-tower/check-bypass-log.test.sh` **不在 CI 的 CT 测试 for 清单**（`grep -n "check-bypass-log" .github/workflows/ci.yml` → 0 命中）；经 `pre-commit-check.sh:617 → ct-test-gate.sh` 在改动脚本时执行 | CTO / ci.yml（本批单写者 = FIX-013 任务） | 登记，**未清**（与 FIX-003 同类） |
| L6 | 归属：PR #796 由本件更新；`.synova-wt-fix796` 保留至 CTO/K3 收口（不自动释放） | CTO | 待收口 |

---

## 6. 独立自验退回后的处置（2026-09-26，verifier 新 clone 复跑发现 `8137999f` 缺记录）

### 6-1 退回事实（verifier 在 GitHub 一次性 clone 上复跑，tip `e56ef15e`）

```
$ bash scripts/control-tower/check-bypass-log.sh origin/main
❌ bypass.log 缺以下提交记录（执行证据链断裂）:
  fix(FIX-002): D331 读面 — 本机无账本来源判「无绕过记录」显式放行（新 clone 态 rc=0） [8137999f]
EXIT=1
```
范围 `481805a8..HEAD` 共 11 条非 merge 提交：10 条命中/豁免，唯一缺口 = `8137999f`。

### 6-2 根因（**不是流程违规**，是 D735 Stage 1 落点不可持久）

- `8137999f` 由 `synova-commit` 创建：该次日志 `/tmp/synova-commit-fix796.log` 原文 `✅ 全部 13 组通过`（pre-commit 真跑过），全文 `grep -c -- "--no-verify"` = **0**；`git rev-list --parents -n 1 8137999f` = `8137999f 41b97740`（**线性单父**，非 amend：AuthorDate = CommitDate = `Sat Sep 26 00:31:43 2026 +0800`）。
- 同一次日志有 hook 显式降级行：`⚠️  post-commit: bypass 登记提交失败（identity 未配置?）— 降级，对账时按 D451 补记`——因为当时 `.claude/bypass.log` 已被 `3ce59bbb` 出库（untracked + 命中 `.gitignore` 的 `*.log`），影子登记提交无法 `git add` 它。
- **记录真实存在，只是落在 per-session 落点**（实测原始输出）：

```
$ grep -rl "8137999f" .sessions/*/bypass.log
.sessions/1dce9ba8-f328-416b-a484-ed86e5781147/bypass.log
$ grep -rh "8137999f66c0d38237b870faa4c7632ffc7cbb61" .sessions/*/bypass.log
2026-09-26T00:31:47+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=8137999f66c0d38237b870faa4c7632ffc7cbb61
```

⇒ `.sessions/` 是 **gitignored**（D735 Stage 1 落点）⇒ **任何新 clone 都读不到它** ⇒ 对账面判「缺记录」。
⇒ 这与上一条 `41b97740`（全机无 hook 原文，只能标「来源缺失」的补记）**性质不同**：本条是**真实 hook 原文**，不需要「来源缺失」标注。

### 6-3 处置：hook 原文**逐字落入 tracked 账本**

```
$ printf '%s\n' "$THELINE" >> .claude/bypass.log        # THELINE = 上行原样（含原时间戳）
$ grep -n "8137999f" .claude/bypass.log
1817:2026-09-26T00:31:47+08:00 | COMMITTED | pre-commit PASS (hook 层登记) | HASH=8137999f66c0d38237b870faa4c7632ffc7cbb61
```

### 6-4 方法论陷阱（verifier 实测，必须写进实验规范）

用**本地路径** `git clone /Users/wane/SynovaAgent …` 会把源仓**陈旧 12 天**的 `refs/heads/main`（`519edadf`）映成 clone 的 `origin/main`；而脚本 D513 的防御性 fetch 在「origin 指向本地路径」时会把该陈旧值**再拉回来** ⇒ 得到「缺 12 条」的**假红**。
⇒ **凡 base 相关实验必须从 GitHub clone（或把 origin 重指 GitHub）**。本文 §0-1/§3 的 rc 值均为**本机口径**；新 clone 口径见 §6-5。

### 6-5 新 clone 形态复测（verifier 方法：从 GitHub clone）

见本文件 §7（本轮 push 后追加）。

### 6-6 数值归属更正（「0 行差异」不能字面读）

上轮我以 `git diff 41b97740 e56ef15e -- <读面两文件> | wc -l` = 0 证明「回退成功」，但该式**同时**对应两种情形：(i) 回退成功；(ii) 改动被 `caa12cd8` 整体 revert。本机复测（原始输出）：

```
$ git diff 41b97740 8137999f -- scripts/control-tower/check-bypass-log.sh tests/control-tower/check-bypass-log.test.sh | wc -l
215
$ git diff 8137999f caa12cd8 -- scripts/control-tower/check-bypass-log.sh tests/control-tower/check-bypass-log.test.sh | wc -l
215
$ git diff --numstat 41b97740 8137999f -- <同两文件>
31	12	scripts/control-tower/check-bypass-log.sh
80	20	tests/control-tower/check-bypass-log.test.sh
$ git diff --numstat 8137999f caa12cd8 -- <同两文件>
12	31	scripts/control-tower/check-bypass-log.sh
20	80	tests/control-tower/check-bypass-log.test.sh
```

⇒ 第一臂（8137999f 引入读面）与第二臂（caa12cd8 逐行 revert）**行数相等（215 = 215）、方向相反（31+/12- ↔ 12+/31-）** ⇒ **第二臂不成立**：本卡并未「自带 D331 读面」，读面改动已被完整撤回；主路径 A 的方向不变。方案记为 `memory/notes/rejected/…`（未用 `|| true` 掩盖）是正确的。
（lead 转述 verifier 的「70 行」与本节 215 行为不同测量命令下的数值；结论一致：两臂等值 + 终态 0。此处以本节命令与输出为准。）

---

## 7. 新 clone 形态复测（tip `d7455857`，verifier 方法）

### 7-1 构造（本机无法直连 GitHub 大包：`failed to store: 100001`，如实标注）

```
$ git clone --no-hardlinks /Users/wane/SynovaAgent /tmp/gh796d          # 全新工作树（无 .sessions / 无本机残留）
$ git remote set-url origin https://x-access-token:***@github.com/tangbaobao520/SynovaAgent.git
$ git fetch origin main            # origin/main: 519edadf → ef299746（真 GitHub main）
$ git checkout d7455857            # tip 取自 GitHub 分支 ref
$ ls -d .sessions                  # → 不存在（无本机残留）
```
⚠ 本机直连 GitHub 全量 clone/fetch 失败（`failed to store: 100001`，会话网络限制）；故用「本地 clone + origin 重指 GitHub + 从 GitHub 刷新 ref」等价构造。**内容按 sha 校验，无本地路径依赖**。

### 7-2 陷阱复现（verifier 口径，实测）

`origin` 仍指向本地路径时（脚本 D513 防御性 fetch 会把本地陈旧 main 拉回来）：

```
$ git remote get-url origin → /Users/wane/SynovaAgent      # 本地路径
$ bash scripts/control-tower/check-bypass-log.sh origin/main; echo rc=$?
❌ bypass.log 缺以下提交记录（执行证据链断裂）:
  docs(D965-D968): 落地哨兵体系整改指引规格件（架构域·单域单件） (#788) [481805a8]
  feat(D964-P1b): check-citations 加 archive/** 豁免根（归档断链不误拦） (#771) [057d8ca0]
  …（共 11 条，均为 **main 侧 squash 提交**）
rc=1
$ git rev-parse --short origin/main → 519edadf          # 被拉回陈旧值（12 天前）
```
⇒ **假红**：这些是 main 侧提交，与本分支证据链无关；根因是 origin 指向本地路径 + D513 fetch 回拉陈旧 ref。

### 7-3 正口径复测（origin = GitHub）

```
$ git rev-parse --short origin/main → ef299746
$ bash scripts/control-tower/check-bypass-log.sh origin/main; echo rc=$?
⚠ fetch 失败——base 可能陈旧（push URL 不更新 tracking ref）；建议 git fetch origin 后重试
✅ bypass.log 对账通过: 481805a88bbc7c26a17a1812c82c8b8d6478cdc9..HEAD 全部提交有记录
rc=0
```
（`⚠ fetch 失败` = 本会话直连 GitHub 大包失败的非阻断降级；base 已由 §7-1 的 fetch 刷新为真 main `ef299746`，故不影响判定。）

⇒ **判据达成**：新 clone 形态（无本机残留 + origin=GitHub）下 `check-bypass-log.sh origin/main` = **rc=0**，范围 `481805a8..HEAD` 全部提交有记录（含本轮补入的 `8137999f` 真实 hook 原文）。

### 7-4 `git ls-remote` 回执

见 §7-5（push 后追加）。

### 7-5 终态 tip 复测（`0f04c835`）+ `git ls-remote` 回执

本会话**直连 GitHub fetch 失败**（`Failed to connect to github.com port 443 after 53105 ms`）⇒ 用 GitHub API 实测值设定 origin/main，并用注入缝跳过脚本内防御 fetch（该 fetch 在正常网络下无需跳过）：

```
$ curl -s .../branches/main → ef2997466677324b94d7b2921a5b3de0b7ad57d3
$ git update-ref refs/remotes/origin/main ef2997466677324b94d7b2921a5b3de0b7ad57d3
$ git checkout -q 0f04c835            # 新 clone（--no-checkout 起步，无 .sessions / 无本机残留）
$ SYNO_BASE_REF=origin/main bash scripts/control-tower/check-bypass-log.sh; echo rc=$?
✅ bypass.log 对账通过: 481805a88bbc7c26a17a1812c82c8b8d6478cdc9..HEAD 全部提交有记录
rc=0

$ git ls-remote --heads origin | grep feat/d969-daily-cto-board-paired-test
0f04c8351f6f327d9b871066620d282ca2d9b75d	refs/heads/feat/d969-daily-cto-board-paired-test
```

⇒ 判据在新 clone 形态下**两次独立成立**：(i) `d7455857`（origin=GitHub，防御 fetch 真跑了、仅告警）；(ii) `0f04c835`（终态 tip，base 取 GitHub API 实测 sha）。两次结论一致：`rc=0`、范围全有记录。
