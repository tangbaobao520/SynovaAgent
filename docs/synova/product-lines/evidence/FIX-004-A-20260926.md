# FIX-004 A 段证据 — 写集豁免收紧（audit-reports 精确键 + archive 仅真重命名）

> 卡：台账 FIX-004 A（P0）｜承接 D964｜产出者：fix-coder-b｜日期：2026-09-26
> 分支：`fix/fix004a-writeset-exempt-abuse`（stacked，base = `chore/cto-doc-slim-phase1-writeset-exempt` @ `b8573c7a`）
> 台账判据：三处「**滥用场景必须仍红**」反例存在（本文件覆盖 A 段；B/C 段见另一 stacked 分支的 evidence）
> **门禁语义变更 ⇒ K3 复审通过前不得合并。** 所有数字来自命令原始输出；沙箱 = mktemp git 仓库，零真实仓库改动。

---

## 0. 结论摘要（A 段）

| 场景 | 修前（分支现状，`b8573c7a`） | 修后 | 判据 |
|---|---|---|---|
| ⒜ `docs/synova/audit-reports/INDEX.md`（合法） | rc=0 豁免 | rc=0 豁免（理由改为精确键口径） | 合法仍绿 |
| ⒝-1 `docs/synova/audit-reports/evil.md`（滥用） | **rc=0 静默豁免** ← 口子 | **rc=1 点名夹带** | 滥用仍红 |
| ⒝-2 复制 `src/secret.ts` 进 audit-reports/ | rc=1（仅点名源） | **rc=1（源与副本双点名）** | 滥用仍红 |
| ⒝-3 新增 `archive/junk/new.md`（非重命名） | **rc=0 静默豁免** ← 口子 | **rc=1 点名夹带** | 滥用仍红 |
| ⒜-2 真 `git mv` → `archive/`（合法归档） | **rc=1 误判夹带** ← 误伤 | **rc=0 两侧打印豁免** | 合法仍绿 |

## 1. 修前原始输出（gate = `git show HEAD:...merge_writeset_gate.py`）

```
════ gate = /tmp/gate-prefix.py ════
【⒜-1 legit INDEX.md】rc=0
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
   豁免 1 条（显式，逐条打印理由）:
     · docs/synova/audit-reports/INDEX.md   ← [builtin] K3 报告指针式索引（INDEX.md 一行一条），跨任务公共登记产物，不属单一任务写集
【⒝-1 abuse audit-reports/evil.md】rc=0
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
     · docs/synova/audit-reports/evil.md   ← [builtin] K3 报告指针式索引（INDEX.md 一行一条），跨任务公共登记产物，不属单一任务写集
【⒜-2 legit git mv → archive】rc=1
❌ 结论: block — 检测到 1 个写集外文件（夹带）
   豁免 1 条（显式，逐条打印理由）:
     · archive/docs/a.md   ← [builtin] 文档减负归档移动（历史文档无单一属主任务，归档路径统一收口）
   夹带文件 1 个（不匹配任何声明项）:
     ③ 显式豁免: 在 PR 正文/声明文件加 `## 写集豁免` 段落，每行 `- <路径> — <理由>`（无理由不生效）
     ⚠ 豁免/声明条目必须逐条**精确路径**（或 `<dir>/**` glob）——
【⒝-2 abuse archive/junk/new.md（新增非重命名）】rc=0
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
   豁免 1 条（显式，逐条打印理由）:
     · archive/junk/new.md   ← [builtin] 文档减负归档移动（历史文档无单一属主任务，归档路径统一收口）
【⒝-3 abuse audit-reports/secret.ts（源码夹带）】rc=1
❌ 结论: block — 检测到 1 个写集外文件（夹带）
   豁免 1 条（显式，逐条打印理由）:
     · docs/synova/audit-reports/secret.ts   ← [builtin] K3 报告指针式索引（INDEX.md 一行一条），跨任务公共登记产物，不属单一任务写集
   夹带文件 1 个（不匹配任何声明项）:
     - src/secret.ts
     ③ 显式豁免: 在 PR 正文/声明文件加 `## 写集豁免` 段落，每行 `- <路径> — <理由>`（无理由不生效）
sandbox=/var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.kG5nxQYvSl/sb
```

## 2. 修后原始输出（同一夹具脚本、同一命令，仅换 gate）

```
════ gate = /Users/wane/SynovaAgent/.synova-wt-fix004a/scripts/control-tower/merge_writeset_gate.py ════
【⒜-1 legit INDEX.md】rc=0
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
   豁免 1 条（显式，逐条打印理由）:
     · docs/synova/audit-reports/INDEX.md   ← [builtin] K3 报告指针式索引（仅一行式登记文件 INDEX.md；同目录其它文件不豁免）
【⒝-1 abuse audit-reports/evil.md】rc=1
❌ 结论: block — 检测到 1 个写集外文件（夹带）
   夹带文件 1 个（不匹配任何声明项）:
     - docs/synova/audit-reports/evil.md
       - docs/synova/audit-reports/evil.md — <理由：为何此文件属于本任务>
【⒜-2 legit git mv → archive】rc=0
✅ 结论: pass — 提交文件集 ⊆ 声明写集（无夹带）
   豁免 2 条（显式，逐条打印理由）:
     · archive/docs/a.md   ← [builtin] 文档减负归档（仅放行 --find-renames 判为 R 的重命名；新增/改写/删除不走此豁免）
     · docs/a.md   ← [builtin] 文档减负归档（仅放行 --find-renames 判为 R 的重命名；新增/改写/删除不走此豁免）
【⒝-2 abuse archive/junk/new.md（新增非重命名）】rc=1
❌ 结论: block — 检测到 1 个写集外文件（夹带）
   夹带文件 1 个（不匹配任何声明项）:
     - archive/junk/new.md
     ③ 显式豁免: 在 PR 正文/声明文件加 `## 写集豁免` 段落，每行 `- <路径> — <理由>`（无理由不生效）
     ⚠ 豁免/声明条目必须逐条**精确路径**（或 `<dir>/**` glob）——
     可直接粘贴的精确豁免行（补全理由后放入 PR 正文 `## 写集豁免`）:
【⒝-3 abuse audit-reports/secret.ts（源码夹带）】rc=1
❌ 结论: block — 检测到 2 个写集外文件（夹带）
   夹带文件 2 个（不匹配任何声明项）:
     - docs/synova/audit-reports/secret.ts
     - src/secret.ts
     ③ 显式豁免: 在 PR 正文/声明文件加 `## 写集豁免` 段落，每行 `- <路径> — <理由>`（无理由不生效）
     ⚠ 豁免/声明条目必须逐条**精确路径**（或 `<dir>/**` glob）——
sandbox=/var/folders/bs/ltqkv_rd45z6b7p5wq90vv500000gn/T/tmp.aJApOuRTZb/sb
```

## 3. 门禁自身注入夹具（改坏即红）

```
$ bash tests/control-tower/merge_writeset_gate.test.sh
=== D708: merge-writeset-gate ===
  ✅ ⑨⒜ 合法: audit-reports/INDEX.md 精确键豁免 → exit 0
  ✅ ⑨⒜ 打印收紧后的豁免理由（精确键）
  ✅ ⑨⒝-1 滥用: audit-reports/evil.md → exit 1（不再静默豁免）
  ✅ ⑨⒝-1 逐文件点名夹带
  ✅ ⑨⒝-2 滥用: 源码夹带进 audit-reports/ → exit 1
  ✅ ⑨⒝-2 源与副本双点名
  ✅ ⑨⒝-3 滥用: 新增 archive/junk/new.md → exit 1（glob 豁免已撤）
  ✅ ⑨⒝-4 滥用: mv+改写（A+D 非 R）→ exit 1
  ✅ ⑨⒜-2 合法: 纯重命名 git mv → archive → exit 0
  ✅ ⑨⒜-2 重命名源与目标两侧均打印豁免（可审计）
  ✅ ⑨⒜-2 打印归档豁免口径（仅 R）
  ✅ ⑨ 改坏即红成立：glob 变异体立即放行 evil.md（收紧确有判别力）
  ✅ ⑨b CJK 文件名被正确判为夹带并原样点名（quotepath 已关）
  ✅ ⑨b 前提: 变更集非空（断言有判别性）
  ✅ ⑨b CJK 路径加入声明后转绿（匹配语义正确）
  ✅ ⑨ --json 输出契约（component/status/smuggled/declared）
=== D708 复核修复: D# 推断 ===
  结果: 58 通过, 0 失败
```

（完整 65 行日志可复跑；上列为 ⑨ 段全部断言 + 汇总行。）

⑨ 段六条新断言 = ⒜ INDEX.md 精确键绿 / ⒜-2 真 rename 绿（源+目标双侧打印）/ ⒝-1 非 INDEX 红 / ⒝-2 源码夹带双点名 / ⒝-3 新增进 archive 红 / ⒝-4 mv+改写（A+D 非 R）红；
外加**变异体反证**：把精确键改回 glob → 变异体立即放行 `evil.md`（若断言写错方向，此步会翻成失败）。

## 4. blast radius + 调用方（实测）

```
$ git ls-tree -r --name-only HEAD docs/synova/audit-reports/ | wc -l
     151        # 收紧后仅 INDEX.md 豁免，其余 150 个「出现在变更集时」需声明/豁免
$ git ls-tree -r --name-only HEAD | grep -c "^archive/"
0             # 顶层 archive/ 当前 0 tracked 文件（D964 阶段 4 实际归档到 .claude/task-briefs/archive/**）
$ grep -rn "merge_writeset_gate" .github scripts tests
.github/workflows/ci.yml:102:          python3 scripts/control-tower/merge_writeset_gate.py \
.github/workflows/ci.yml:259:            tests/control-tower/merge_writeset_gate.test.sh \
scripts/control-tower/merge_writeset_gate.py:4:scripts/control-tower/merge_writeset_gate.py — D708 合并级写集对账 gate
scripts/control-tower/check-citations.py:37:  与 merge_writeset_gate.py 的写集豁免同形）。
tests/control-tower/check-name-allocation.test.sh:48:#    merge_writeset_gate.test.sh:36 / check-citations.test.sh:36 同款。
tests/control-tower/alloc-task-id.test.sh:477:# 沿 check-progress-freshness.test.sh:44 / merge_writeset_gate.test.sh:36 同款。
tests/control-tower/merge_writeset_gate.test.sh:6:# merge_writeset_gate.test.sh — D708 合并级写集对账 gate 的密封测试
tests/control-tower/merge_writeset_gate.test.sh:38:GATE="$REPO/scripts/control-tower/merge_writeset_gate.py"
tests/control-tower/merge_writeset_gate.test.sh:47:grep -q 'merge_writeset_gate.py' "$REPO/.github/workflows/ci.yml" \
tests/control-tower/merge_writeset_gate.test.sh:48:  && ok "接线: ci.yml 调用 merge_writeset_gate.py" || no "接线: ci.yml 未调用（M3 未接线）"
tests/control-tower/merge_writeset_gate.test.sh:53:cp "$REPO/scripts/control-tower/merge_writeset_gate.py" "$SB/scripts/control-tower/"
tests/control-tower/merge_writeset_gate.test.sh:73:  (cd "$SB" && "$PYBIN" "$SB/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:202:OUT=$( (cd "$SB" && "$PYBIN" "$SB/scripts/control-tower/merge_writeset_gate.py" --repo-root "$SB" --base "$BASE_MV" --head HEAD --branch fix/D708-sandbox 2>&1) ); rc=$?
tests/control-tower/merge_writeset_gate.test.sh:212:OUT=$( (cd "$SB" && "$PYBIN" "$SB/scripts/control-tower/merge_writeset_gate.py" --repo-root "$SB" --base "$BASE_MV2" --head HEAD --branch fix/D708-sandbox 2>&1) ); rc=$?
tests/control-tower/merge_writeset_gate.test.sh:240:OUT=$(cd "$SB" && "$PYBIN" "$SB/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:291:sp = u.spec_from_file_location("g", sys.argv[1] + "/scripts/control-tower/merge_writeset_gate.py")
tests/control-tower/merge_writeset_gate.test.sh:306:cp "$REPO/scripts/control-tower/merge_writeset_gate.py" "$SB2/scripts/control-tower/"
tests/control-tower/merge_writeset_gate.test.sh:316:OUT=$(cd "$SB2" && "$PYBIN" "$SB2/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:337:OUT=$(cd "$SB3" && GITHUB_EVENT_PATH="$TMPD/event.json" "$PYBIN" "$SB3/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:352:  cp "$REPO/scripts/control-tower/merge_writeset_gate.py" "$d/scripts/control-tower/"
tests/control-tower/merge_writeset_gate.test.sh:379:OUT=$(cd "$SB4" && "$PYBIN" "$SB4/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:384:OUT=$(cd "$SB4" && "$PYBIN" "$SB4/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:393:OUT=$(cd "$SB4" && "$PYBIN" "$SB4/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:400:OUT=$(cd "$SB4" && "$PYBIN" "$SB4/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:417:OUT=$(cd "$SB6" && "$PYBIN" "$SB6/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:425:OUT=$(cd "$SB6" && "$PYBIN" "$SB6/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:440:OUT=$(cd "$SB5" && "$PYBIN" "$SB5/scripts/control-tower/merge_writeset_gate.py" \
tests/control-tower/merge_writeset_gate.test.sh:459:OUT=$(cd "$SB4" && "$PYBIN" "$SB4/scripts/control-tower/merge_writeset_gate.py" \
```

## 5. 红线自查

- 未以改断言/删断言/`|| true` 把红变绿：⑨ 段改写为**成对反例 + 变异体反证**（断言数量净增）
- 未碰 `scripts/audit/**`、`docs/synova/audit-reports/**`（本 PR 零触碰该目录）
- 未改 `.github/workflows/ci.yml`（单写者 fix-coder-a）
- 未 `--no-verify`、未 `git stash`、未 force push
- **门禁语义变更 ⇒ 本 PR 在 K3 复审通过前不得合并**（与 D964 原两 PR #764/#766 同规）

## 6. 未清项

1. **B/C 段（同目录另一 stacked 分支）**：`scripts/doc-system/doc-registry-gate.sh` 的同目录约束与「取代目标必须存在」两处修正 + 各自成对反例。
2. **`archive/` 豁免的残余面**：真 R 重命名把 `src/**.ts` 移进 `archive/` 仍被豁免（台账口径即「仅 R 放行」）；若需「仅文档类型可归档」，属判据语义变更，须过 K3 另立卡。
3. **豁免的可见性**：归档豁免逐条打印理由（已做），但**未**在 step summary 单列「本次行使了哪些内置豁免」——属可观测性增强，非本卡判据。
