# FIX-004 B/C 段证据 — 同类文档唯一性两处修正（同目录约束 + 取代目标存在性）

> 卡：台账 FIX-004 B/C（P1）｜承接 D964｜产出者：fix-coder-b｜日期：2026-09-26
> 分支：`fix/fix004b-doc-dup-rule`（stacked，base = `chore/cto-doc-slim-phase3-dup-rule` @ `8d8b46a3`）
> 台账判据：三处「**滥用场景必须仍红**」反例存在（本文件覆盖 B/C；A 段见 `FIX-004-A-20260926.md`）
> **门禁语义变更 ⇒ K3 复审通过前不得合并。**

---

## 0. 结论摘要

| 漏洞 | 症状（修前实测） | 修后 | 判据 |
|---|---|---|---|
| **B**（P1）新目录 `README.md` **误拦** | 归一化只取 basename ⇒ 新目录放 README.md 与仓库任意同名文件「撞名」→ rc=1（假红） | 加**同目录约束**：同目录必判 / 跨目录仅非通用名判；通用名 README/index/CHANGELOG… 放行 | 合法仍绿（⑦⒜）、滥用仍红（⑦⒝-1/2） |
| **C**（P1）`取代: 不存在` **仍放行** | 声明只匹配「取代: <任意非空 token>」⇒ 空声明即免罚 → rc=0 | 声明目标必须 ∈ tracked 集合 ∪ 本次新增集合，否则判红并指名 | 合法仍绿（⑧⒜/③）、滥用仍红（⑧⒝） |

## 1. 修后（当前分支）原始输出

```
$ bash tests/doc-system/doc-dup-rule.test.sh
  ✅ ③ 写明「取代:」后放行 → exit 0
  ✅ ③ 输出标注已声明取代/合并
  ✅ ⑦⒜ 合法: 跨目录通用名 README.md → exit 0（不再误拦）
  ✅ ⑦⒝-1 滥用: 同目录归一化撞名 → exit 1
  ✅ ⑦⒝-1 输出点名同目录撞名
  ✅ ⑦⒝-2 滥用: 跨目录非通用名撞名 → exit 1
  ✅ ⑦⒝-2 输出点名跨目录撞名
  ✅ ⑧⒜ 合法: 取代目标存在 → exit 0
  ✅ ⑧⒝ 滥用: 取代目标不存在 → exit 1
  ✅ ⑧⒝ 输出点名「取代声明目标不存在」
  结果: 21 通过, 0 失败
```

## 2. 修前对照（同一夹具脚本，gate = `git show <base>:scripts/doc-system/doc-registry-gate.sh`）

```
$ bash /tmp/fix004b-pre/tests/doc-system/doc-dup-rule.test.sh
  ❌ ⑦⒜ 新目录 README 被误拦 (rc=1)
  ✅ ⑦⒝-1 滥用: 同目录归一化撞名 → exit 1
  ❌ ⑦⒝-1 未点名同目录
  ✅ ⑦⒝-2 滥用: 跨目录非通用名撞名 → exit 1
  ❌ ⑦⒝-2 未点名跨目录
  ✅ ⑧⒜ 合法: 取代目标存在 → exit 0
  ❌ ⑧⒝ 空声明仍放行（口子未堵）(rc=0)
  ❌ ⑧⒝ 未点名目标不存在
  结果: 16 通过, 5 失败
```

⇒ 成对反例成立：**同一夹具**在修前 5 条断言失败（⑦⒜ 误拦 = 假红；⑧⒝ 空声明放行 = 漏拦），修后 21/0 全过。

## 3. 夹具设计（判别性，非 grep 型静态判据）

- **⑦⒜ 合法**：既有 `docs/synova/README.md`（已提交）+ 新增 `docs/synova/sub/README.md`（跨目录通用名）→ 期望 rc=0
- **⑦⒝-1 滥用**：同目录 `docs/synova/sub/RULES-20260101.md` 已提交 + 新增同目录 `RULES-20260926.md`（归一化同名）→ 期望 rc=1 且输出含`同目录`
- **⑦⒝-2 滥用**：跨目录**非通用名** `docs/synova/other/RULES.md`（既有 `docs/synova/RULES.md`）→ 期望 rc=1 且输出含`跨目录`（证明约束不放宽非通用名）
- **⑧⒜ 合法**：`取代: docs/synova/RULES-20260101.md`（真实 tracked 文件）→ 期望 rc=0
- **⑧⒝ 滥用**：`取代: docs/synova/DOES-NOT-EXIST-20260926.md` → 期望 rc=1 且输出点名`取代声明目标不存在`

## 4. 红线自查

- 未以改断言/删断言/`|| true` 把红变绿：断言数量 13 → 21（净增 8 条），并给修前/修后成对对照
- 未碰 `scripts/audit/**`、`docs/synova/audit-reports/**`
- 未改 `.github/workflows/ci.yml`（单写者 fix-coder-a）
- 未 `--no-verify`、未 `git stash`、未 force push
- **门禁语义变更 ⇒ 本 PR 在 K3 复审通过前不得合并**

## 5. 未清项

1. **通用名清单**（README/index/CHANGELOG/license/contributing/authors/makefile/todo/summary/overview）为本卡自定口径；若需扩充或收窄，属判据语义变更 → 另立卡 + K3。
2. **取代声明只校验"存在"**，不校验语义（例如指向一个不相关文件也放行）；若需"目标必须是同主题/同目录"属新判据，须过 K3。
3. **指纹比对只扫 `tracked[:800]`**（既有实现，本卡未改）；超大仓库可能存在漏比。
4. **A 段**（写集豁免收紧）在另一条 stacked 分支：`fix/fix004a-writeset-exempt-abuse`。
