# FIX-006 / 007 / 008 / 015 / 016 〇-补证（原始输出）

> **任务**：共享 task-6（〇-补证）｜**作者**：fix-verifier（独立自验成员，只读）
> **日期**：2026-09-26
> **登记处**：`docs/synova/coordination/问题修复台-20260925.md`（PR #805，未落 main）
> **口径**：只出证据，**不动手修**。判定 ∈ {复现, 未复现, 无法确认}。
> **红线遵守**：未改产品代码 / 夹具 / 断言 / CI 配置 / `scripts/**`。实验仅在
> ① 一次性 clone `/tmp/verif-clone`（`git clone --no-hardlinks`，起始 `ef299746`）
> ② `/tmp` 夹具。真仓 `/Users/wane/SynovaAgent` **仅只读**。
> **本件结论效力**：自验结论，**≠ 审计通过**（审计结论只认 K3）。

## 〇 环境与冻结前提复核（开工前实测）

| 前提（lead 冻结值） | 我的实测 | 判定 |
|---|---|---|
| `origin/main` = `ef299746` | `git rev-parse origin/main` → `ef2997466677324b94d7b2921a5b3de0b7ad57d3` | ✅ 一致 |
| worktree 数 = 287 | 见下方时序 | ⚠️ **已过时（live 增长）** |
| `ci.yml` 无独立 `iron-laws` job | 未复核（非本件 5 条所需） | 未核 |
| PR #803 sha = `31971419` | `pulls/803` → `head.sha=31971419ae58b5e69a4d9f133ffd982e97d620ee` | ✅ 一致 |
| #803 红 job = CT(win)+CT(ubuntu)+TS+Lint+IronLaws | run `36156860254` → 见 §五 | ✅ 一致（3 红） |

**worktree 数时序（实测，非估计）**：

```
开工时  git worktree list | wc -l   →   289
FIX-006 探针时（porcelain 计数）    →   297
收尾核对时 git worktree list | wc -l →   298
```

⇒ 冻结前提「287」**已过时**；且该值在**本 session 内持续增长**（并行成员在 `git worktree add`）。
**任何以 worktree 绝对数为判据的卡面必须先固化测量时刻**，否则必然漂移。

**取 CI 日志方法（`gh` 不存在）**：`curl` + `Authorization: Bearer $TOKEN`。
⚠️ **实测坑**：`/actions/runs?head_sha=<短 sha>` 返回 `total_count: 0`（**静默空结果**，易被误读为"该 sha 无 CI"）；
必须用 **40 位全 sha**：

```
$ curl -s -H "Authorization: Bearer $TOKEN" ".../actions/runs?head_sha=31971419"                    → total_count 0
$ curl -s -H "Authorization: Bearer $TOKEN" ".../actions/runs?head_sha=31971419ae58b5e69a4d9f133ffd982e97d620ee" → total_count 2
```

---

## 一 FIX-015（**优先项**，解阻 task-12）

### 判定：**未复现（作为"生产代码违规"）**｜原始数字 9/3 复现，但 **100% 自伤，0 处生产真红**

**CI 原始片段**（run `36156860254`，job `108143405913` = `TypeScript + Lint + Iron Laws`，日志第 1068 / 1076 行）：

```
❌ 禁止 DiagnosticModule: 新模块须实现 Sentinel 接口: 3 处  [CI strict——软提示转硬]
     +#   #33 绕过审计 / #3 硬编码(改判保留) / #30 DiagnosticModule / #38 G10 / #39 G11 /
     +soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" "${NEW_DIAG:-}"
     +LBL_g7="禁止 DiagnosticModule: 新模块须实现 Sentinel 接口"
  ✅ G10: 无条件归属(跳过)
  ✅ G11: 测试覆盖检查通过
  ✅ Secrets 扫描（全工作区）
── CI 权威区（D962 迁移判定）──
  ❌ as any / as never / as unknown as 零容忍（铁律38）: 9 处  [CI strict——软提示转硬]
     +1. as any 探针（CI strict）：`❌ as any / as never / as unknown as 零容忍（铁律38）: 1 处 [CI strict——软提示转硬]` → exit 1
     +  - 质量根（as any/桥接/接线/G12）本地 soft + SYNO_CI=1 转硬——hard-gate-convergence
     +  M=$(echo "$GIT_CACHED_DIFF" | grep -E '^\+' | grep -v '^+++' | grep -E 'as (any|never)\b|as unknown as' | grep -vE '^\+\s*(//|/\*|\*|#)' || true)
     +  soft_check "as any / as never / as unknown as 零容忍（铁律38）" "$M"
     +  'soft_check "as any / as never / as unknown as 零容忍（铁律38）"'
     +[ "$rcA" -eq 1 ] && ok "行为A: as any 探针 CI strict 硬拦 (exit 1; V5.3 质量根在 CI)" || no "行为A: 应 exit 1, 实际 $rcA"
     +[ "$rcA2" -eq 1 ] && ok "行为A2: as never 探针 CI 硬拦 (exit 1)" || no "行为A2: 应 exit 1, 实际 $rcA2"
     +[ "$rcA3" -eq 1 ] && ok "行为A3: as unknown as 双断言 CI 硬拦 (exit 1)" || no "行为A3: 应 exit 1, 实际 $rcA3"
```

**⚠️ 日志只显示 8 行，但计数是 9 处** — 因 `soft_check()` 自身截断（`scripts/pre-commit-check.sh` of `31971419`）：

```bash
33: soft_check() {
34:   local name="$1" matches="$2" count=0
35:   [ -n "$matches" ] && count=$(echo "$matches" | grep -c . 2>/dev/null) || count=0
...
44:   echo "$matches" | head -8 | while read -r l; do [ -n "$l" ] && echo "     ${l}"; done
```

⇒ **CI 日志结构上不可能给出第 9 行**。故我按**检查自身正则**复跑 `git diff origin/main...31971419`，逐行标注 file:line。

**复现命令 + 计数（逐字对齐 CI 的 9 / 3）**：

```
$ git diff origin/main...31971419 > /tmp/verif-ev/diff-803.patch      # 415595 bytes / 7496 lines
# 对每个 + 行按 hunk 头累计新行号，套用检查自身两条 grep 链
=== as any / as never / as unknown as 命中（共 9 处）===
=== DiagnosticModule 命中（共 3 处）===
```

### A/B/C 分类表（12 处全量，逐条 file:line）

| # | 类 | file:line | 命中内容 |
|---|---|---|---|
| 1 | **A** 测试断言文本 | `tests/control-tower/hard-gate-convergence.test.sh:55` | `+[ "$rcA" -eq 1 ] && ok "行为A: as any 探针 …"` |
| 2 | **A** | `tests/control-tower/hard-gate-convergence.test.sh:82` | `+[ "$rcA2" -eq 1 ] && ok "行为A2: as never 探针 CI 硬拦 (exit 1)"` |
| 3 | **A** | `tests/control-tower/hard-gate-convergence.test.sh:84` | `+[ "$rcA3" -eq 1 ] && ok "行为A3: as unknown as 双断言 CI 硬拦 (exit 1)"` |
| 4 | **A** 注入夹具 | `tests/control-tower/hard-gate-convergence.test.sh:44` | `+'soft_check "as any / as never / as unknown as 零容忍（铁律38）"'` |
| 5 | **A** 标签字面量 | `tests/control-tower/precommit-groups-injection.test.sh:193` | `+LBL_g1="as any / as never / as unknown as 零容忍（铁律38）"` |
| 6 | **A** 标签字面量 | `tests/control-tower/precommit-groups-injection.test.sh:195` | `+LBL_g7="禁止 DiagnosticModule: 新模块须实现 Sentinel 接口"` |
| 7 | **C** 自伤·检查自身 | `scripts/pre-commit-check.sh:254` | `+ M=$(echo "$GIT_CACHED_DIFF" \| grep -E … 'as (any\|never)\b\|as unknown as' …)` |
| 8 | **C** 自伤·检查自身 | `scripts/pre-commit-check.sh:255` | `+ soft_check "as any / as never / as unknown as 零容忍（铁律38）" "$M"` |
| 9 | **C** 自伤·检查自身 | `scripts/pre-commit-check.sh:5` | `+#   #33 绕过审计 / #3 硬编码(改判保留) / #30 DiagnosticModule / …` |
| 10 | **C** 自伤·检查自身 | `scripts/pre-commit-check.sh:211` | `+soft_check "禁止 DiagnosticModule: 新模块须实现 Sentinel 接口" "${NEW_DIAG:-}"` |
| 11 | **C** 文档字面量 | `docs/synova/coordination/D962-PR803-body.md:22` | `+1. as any 探针（CI strict）：…` |
| 12 | **C** 文档字面量 | `memory/notes/proposed/2026-09-25-d962-2a1-precommit.md:21` | `+  - 质量根（as any/桥接/接线/G12）本地 soft + SYNO_CI=1 转硬` |

**B 类（生产真红）= 0 处**：12 处全部落在 `scripts/` + `tests/` + `docs/` + `memory/`，**`src/` 零命中**。

### 根因：检查的**自伤（self-reference false positive）**

两条链都扫 `$GIT_CACHED_DIFF`；**#803 的 diff 恰恰就是在改这个检查本身** ⇒ 检查的模式字符串、标签、
探针夹具都出现在自己的 diff 里。

- `as any` 链（`scripts/pre-commit-check.sh:254`）**没有任何文件白名单** ⇒ 任何触碰「检查自身 / 其测试 / 其文档」的 diff **必然自伤**。
- `DiagnosticModule` 链（`scripts/pre-commit-check.sh:229`）**有**排除：
  ```bash
  NEW_DIAG=$(echo "$GIT_CACHED_DIFF" | grep "^+.*DiagnosticModule" \
    | grep -Ev "scripts/pre-commit-check.sh|\.md|\.html|//|@deprecated|import type|^\+\+\+|hard_check|禁止新 DiagnosticModule|不要再使用 DiagnosticModule" || true)
  ```
  但排除的是**"行内含该路径字面量"的行**；diff 的**内容行不含路径**（只有 `+++ b/<path>` 头含）
  ⇒ 排除形同虚设，3 处全漏。
  另注：排除词表写的是 `hard_check` 与 `禁止新 DiagnosticModule`，而实际命中行是 `soft_check` 与
  `禁止 DiagnosticModule`（无「新」）⇒ **词表与实际字面量不匹配**，双重失效。

### 未清项（含给 task-12 的方向提示，我不下场改）

1. **"豁免 A 类 + 修 B 类"会跑偏**：B 类本就 0 处；豁免掉 A 类后**仍红**，因为 #7–#10 是**检查自身的模式与标签**。
2. 正确修法方向（供决策，非本件结论）：把排除从"行内字面量"改为**hunk 级路径判定**（按 `+++ b/<path>` 归属整段），
   或让检查显式跳过 `$0` 自身 + `tests/**` + `docs/**`。
3. **CI 侧第 9 行结构上不可见**（`soft_check` `head -8`）——若要求 CI 自证，须改 `soft_check` 截断。

---

## 二 FIX-016

### 判定：**部分复现** — CI 侧 23/1 ✅ 复现（双平台）；本地数字 ✅ 复现但**只在 #803 分支**（main 上是 **38/0**，不是 24/0）；**定位到具体断言 = 无法确认**（CI 侧被截断、且零 artifact）

### ⒜ CI 侧原始片段（两平台，逐字）

**ubuntu-latest**（job `108143405907`，日志 1051–1052 行）：

```
── tests/control-tower/grep-oP-regression.test.sh
FAIL: tests/control-tower/grep-oP-regression.test.sh
##[error]  ✅ 无「sed 吃掉文件参数」形态（$FILE 必须在管道左侧）|  ✅ 可执行: export 提取（真实形态 foo,BAR）|  ✅ 可执行: #CRITERIA 提取（真实形态 A）|  ✅ 行首 BOM 与存量待清清单一致（10 个待清，无新增；本批已清 scripts/pre-doc-audit.sh）|  ✅ 哨兵: 塞入 BOM 文件必被扫到（扫描器非空转）||  结果: 23 通过, 1 失败|  Status: ❌ grep -P 回归网未通过|
```

**windows-latest**（job `108143406027`，日志 1083–1084 行）：**同一条 `结果: 23 通过, 1 失败`**，尾部 5 条断言全绿且逐字相同。

⇒ **两平台同数**（23/1）。**失败断言不在这 5 条尾部之内**，位于前 19 条之一。

### ⒝ 为什么 CI 日志给不出具体断言（**结构原因，非取数疏忽**）

`.github/workflows/ci.yml:285-287`：

```bash
if ! bash "$t" > /tmp/ct-out.log 2>&1; then
  echo "FAIL: $t"
  MSG=$(tail -8 /tmp/ct-out.log | tr '\n' '|' | tr -d '%' | cut -c1-450)
  echo "::error title=$t::$MSG"
```

- `tail -8` + `cut -c1-450` ⇒ 只留尾部 8 行、450 字符；
- 该 run **零 artifact**（`/actions/runs/36156860254/artifacts` → `total_count: 0`）；
- `ci.yml` 无 `upload-artifact` 步骤。

⇒ **CI 侧"定位到具体断言"在现有接线下属不可完成**。

### ⒞ 本地复跑（原始输出，**关键：两平台 vs 本地是不同 revision**）

| revision | 夹具规模 | 本地结果 |
|---|---|---|
| **main `ef299746`** | 243 行 | **38 通过, 0 失败**（rc=0） |
| **#803 `31971419`** | 220 行（**-63 行改动**） | **24 通过, 0 失败**（rc=0） |

```
$ git checkout --quiet ef299746 && bash tests/control-tower/grep-oP-regression.test.sh
  结果: 38 通过, 0 失败
  Status: ✅ grep -P 回归网全部通过        (rc=0)

$ git checkout --quiet 31971419 && bash tests/control-tower/grep-oP-regression.test.sh
  结果: 24 通过, 0 失败
  Status: ✅ grep -P 回归网全部通过        (rc=0)
```

**⇒ 口径混用**：台账写的「夹具在 main ✅」+「本地 24/0」把**两个 revision** 混为一句。
`24/0` 是 **#803 分支**的数；**main 上是 38/0**。

### ⒟ 是不是"既存红"？—— 用 main 自己的 CI 判

```
run 36146340288 (head_sha = ef2997466677324b94d7b2921a5b3de0b7ad57d3)
  108108342617  Control Tower Gate Tests (windows-latest)  success
  108108342680  Control Tower Gate Tests (ubuntu-latest)   success
```

⇒ **main 的 CI 双平台绿**。故 `23/1` **不是既存红，是 #803 引入**（属本 PR 引入）。

### ⒠ 已排除的解释（实测否证）

| 假设 | 实验 | 结果 |
|---|---|---|
| CI 环境变量（`GITHUB_ACTIONS` / `SYNO_CI` / `SYNO_DIFF_BASE`）触发分支 | `GITHUB_ACTIONS=true SYNO_DIFF_BASE=origin/main SYNO_CI=1 CI=true bash tests/…` | 仍 **24/0**（否证） |
| base 非祖先导致 merge 内容不同 | `git merge-base --is-ancestor ef299746 31971419` | **YES ancestor**，merge 内容 = head 内容（否证） |
| 强制 `LC_ALL=C` | 改夹具副本 `LC_ALL=C.UTF-8`→`C` | **18 条全红**（不是"恰好 1 条"，且属另一机制，否证） |

### ⒡ 最强候选（**待验，非结论**）

`tests/control-tower/grep-oP-regression.test.sh`（`31971419`）中**唯一被"期望值反转"**的断言：

```diff
-# 边界（行为等价保持）: 全角冒号原 PCRE 的 [:=\s] 本就不匹配 → 转译后同样不匹配，非本次回归
-eqs '\K 边界: 全角冒号不匹配（与原 PCRE 同）' "scripts/pre-commit-check.sh" \
-   '#CRITERIA[[:space:]]*[:=][[:space:]]*[A-D]' '#CRITERIA：C' '' 's/.*[=:][[:space:]]*//'
+# V5.3 边界翻转: 全角冒号在组7a 增强后【应匹配】（原 PCRE [:=\s] 不匹配——转译语义演进，密封随迁）
+eqs '\K 边界: 全角冒号 V5.3 起匹配（组7a 增强）' "scripts/pre-commit-check.sh" \
+   '#CRITERIA[[:space:]]*[:=：][[:space:]]*[A-D]' '#CRITERIA：C' 'C' 's/.*[=:：][[:space:]]*//'
```

该断言把 **多字节全角冒号 `：` 放进括号表达式** `[:=：]`，且期望由"不匹配"翻转为"匹配"
⇒ 对 **locale/多字节处理**敏感（CI 双平台为 GNU grep；本地为 `grep (BSD grep, GNU compatible) 2.6.0-FreeBSD`；
本地**无** `ggrep`/`gsed`/`gawk`，**无** Docker，**无法**在本地跑 GNU grep 验证）。
**⚠️ 本项为候选，未证实**；红线要求"定位前不得改夹具"，故**未做任何夹具改动**。

### 未清项

1. **"定位到具体断言"= 无法确认**：除非改 `ci.yml:287` 的截断（`tail -8 | cut -c1-450` → 全量或 artifact），
   否则该差异**结构上不可定位**。这本身是坑清单「CI 失败断言给完整片段（禁 `tail -8`）」的**在册实现级违背**。
2. 台账「本地 24/0」需更正口径为 **"#803 分支 24/0；main 38/0"**。
3. 若要坐实候选 ⒡，需**有 GNU grep 的环境**（本机无 Docker）跑一次该单条断言。

---

## 三 FIX-006

### 判定：**挂死 = 一次性 clone 内未复现；但"真仓扫描 92s"机制 = 复现**（两者并不矛盾，见 ⒞）

### ⒜ 一次性 clone 内实测（rc + 耗时 + 完整输出）

```
$ rm -rf /tmp/verif-clone && git clone --no-hardlinks --quiet /Users/wane/SynovaAgent /tmp/verif-clone
$ cd /tmp/verif-clone && git checkout --quiet ef299746    # HEAD=ef2997466677324b94d7b2921a5b3de0b7ad57d3
$ git status --porcelain | wc -l                          # 0（实验前干净）

$ /tmp/verif-ev/tmo.sh 60 bash scripts/control-tower/alloc-task-id.sh "侦察用-verifier-补证"
rc=0
elapsed_sec=3.042
--- 完整输出 ---
D1008
已登记: /tmp/verif-clone/task-state/D1008.json (status=claimed)
brief 骨架已生成: /tmp/verif-clone/.claude/task-briefs/2026-09-26-D1008-侦察用-verifier-补证.md（填写后开工）
```

（macOS 无 GNU `timeout`/`gtimeout`，用 `/tmp/verif-ev/tmo.sh` 等价替代：后台 + `kill -9` 兜底。）

⇒ **未挂死**：rc=0，**3.042s** 完成。**号只烧在 clone 内（D1008），真仓零污染。**

### ⒝ 为什么 clone 复现不了（**关键反直觉点**）

```
$ git worktree list | wc -l      # 在 /tmp/verif-clone 内
       1
```

**新 clone 只有自己 1 个 worktree**，而真仓是 **289–298** 个。
`alloc-task-id.sh` 的慢路径正是"遍历全部 worktree 的 task-state"。⇒ **clone 结构上不承载该负载**，
「clone 内 rc=0」**不足以否证**真仓挂死。

### ⒞ 真仓只读复刻（**不写任何文件**）—— 复现

只读复刻 `alloc-task-id.sh:279-300` 的扫描循环（逐字同构，含字符串累加）：

```
worktrees=297  task-state文件数=87130
git worktree list 耗时=0.167s
扫描循环耗时=91.956s
总耗时=92.123s
WORKTREE_USED 字节数=348559
```

⇒ **扫描循环 91.956s**（≈1.5 分钟），远超 `LOCK_WAIT_SEC=30`；对使用者即**挂死**体验。
旁证：脚本注释自陈 D940 返工前该循环实测 **258s**（`alloc-task-id.sh:285-289`）：

```
286:        #   原为 `bn=$(basename "$f")` —— 每文件一次子进程。本机 200 个 worktree × 50,788 个
287:        #   task-state 文件 = 50,788 次 fork → 该循环实测 258s，使取号入口 ≈4.3 分钟（≫ LOCK_WAIT_SEC=30）。
288:        #   改纯参数展开：同字符串、零子进程、语义等价（见 D940 证据件「等价性 + 耗时」双证据）。
289:        #   仅此一处；WORKTREE_USED 累计展开等其它噪音不在本卡。
```

### ⒟ 计时对照（dry-run，零副作用）

```
clone 内 worktree 数=1
含 worktree 扫描（默认）:  3.024s / 3.172s / 3.055s
SYNO_ALLOC_NO_WORKTREE=1:  2.663s / 3.215s / 3.169s     ← 差异在噪声内（因 clone 无额外 worktree）
```

⇒ 该对照**在 clone 内无鉴别力**，再次印证 ⒝。

### ⒠ 「字符串累加」处数（原始输出）

台账记「**5 处**（O(n²) 为推断非实测）」。两种口径实测：

**口径 1 — 真·自引用累加（`VAR="${VAR}…` / `VAR="$VAR…`）**：

```
$ grep -nE '[A-Z_]+="\$\{?[A-Z_]+\}?[^"]*"' scripts/control-tower/alloc-task-id.sh
62:TASK_STATE_DIR="${SYNO_TASK_STATE_DIR:-$ROOT/task-state}"
63:TEMPLATE="$TASK_STATE_DIR/TEMPLATE.json"
66:PREFIX="${SYNO_ALLOC_PREFIX:-}"
80:TITLE="${_POS[0]:-}"
97:LOCK_DIR="${SYNO_LOCK_DIR:-$ROOT/.alloc-task-id.lock}"
164:    REMOTE_BRANCH_REFS="$_LSR_OUT"
227:  _CN="${CHECK_ID#[Dd]}"
318:    [ -n "$BRANCH_IDS" ] && BRANCH_USED="$BRANCH_IDS"
326:MAX="${MAX:-0}"
354:BRIEF_DIR="${SYNO_BRIEF_DIR:-$ROOT/.claude/task-briefs}"
359:BRIEF_FILE="$BRIEF_DIR/$(date +%Y-%m-%d)-${NEW_ID}-${SAFE_TITLE}.md"
369:STATE_FILE="$TASK_STATE_DIR/$NEW_ID.json"
--- 计数 ---
12
```

其中**唯一构成"循环内累加"的只有 1 处**（其余 11 处是普通赋值/默认值展开）：

```
295:            WORKTREE_USED="${WORKTREE_USED}${n}
296: "
```

**口径 2 — 宽松赋值形态**：10 处（含 `${VAR:-default}` 展开，非累加）。

⇒ **「5 处」在任何一种口径下都不可复现**；真·循环内累加 = **1 处**（`:295-296`）。
且脚本注释 `:289` 自陈「**仅此一处**；WORKTREE_USED 累计展开等其它噪音不在本卡」——**与新实测一致**。

**O(n²) 已从"推断"升为实测**：87,130 次迭代把 `WORKTREE_USED` 累加到 **348,559 字节**
→ bash 字符串线性重复扩容，实测 **91.956s**。

### ⒡ 占用表「读取面 ≠ 拒绝面」——**复现（不对称确实存在，且被注释声明为有意设计）**

| 位置 | **读取面**（`USED` → `MAX` → `NEXT`） | **拒绝面**（`_occupy_locations`） |
|---|---|---|
| 本目录 task-state | ✅ `:252` | ✅ `:173` |
| origin/main task-state | ✅ `:261` | ✅ `:178` |
| **worktree 的 task-state** | ✅ `:272-300` | ❌ **无**（只查 worktree **目录名**） |
| **`git branch -r` 远端跟踪分支** | ✅ `:317` | ❌ 改用 ls-remote 权威面 |
| **远端分支（ls-remote）** | ❌ **有意不喂** | ✅ `:183-194` |
| **本地分支名 `refs/heads`** | ❌ | ✅ `:197-203` |
| **worktree 目录名** | ❌ | ✅ `:206-217` |

原文（节选）：

```bash
-reading  252: USED=$(ls "$TASK_STATE_DIR"/D*.json 2>/dev/null | sed 's/.*\/D\([0-9]*\)\.json/\1/' | grep -E '^[0-9]+$' || true)
-reading  317:     BRANCH_IDS=$(git -C "$TS_TOP" branch -r --format='%(refname:short)' 2>/dev/null | grep -ioE 'D[0-9]+' | …)
-reading  323: ALL_USED=$(printf "%s\n" "$USED" | grep -E '^[0-9]+$' | sort -n | uniq || true)
-reading  328: NEXT=$((10#$MAX + 1))
-reject   335: _CONFLICTS="$(_occupy_locations "$NEXT")"
```

**⚠️ 但设计注释显式声明该不对称是刻意的**（`alloc-task-id.sh:35-37`）：

```
34: #     ① 远端占用改由 `git ls-remote --heads origin` 权威查询（`branch -r` 依赖本地 tracking ref，
35: #        未 fetch 即漏号——D736 撞号 / D730 登记项现场复现）。
36: #        ⚠ 设计决定: ls-remote 只喂**发号前校验**，不喂"下一个号"计算表 —— 二者若都喂，
37: #        NEXT 会直接跳过已被远端占用的号，"拒绝并点名"永不触发（与卡面验收 1 互斥）。
```

⇒ **"读数面 ≠ 拒绝面"作为事实成立**；但**不能据此判为缺陷**——ls-remote 那条是**有意设计**。
真正需要判的是"**worktree task-state 只在读取面、不在拒绝面**"这一条（`:272-300` vs `_occupy_locations` 无此项），
本件**未做判别性实验**，见未清项。

### 未清项

1. 真仓扫描 **91.956s** 属**机制复现**：要坐实"取号入口挂死"，须在**真仓**跑一次完整
   `alloc-task-id.sh`（**会烧真号 + 写 task-state**）——**超出我只读写集，未执行**，需 CTO 授权。
2. "worktree task-state 未进拒绝面"是否可致**实际撞号**，本件**未做判别性实验**（需构造"号仅存在于他 worktree 的 task-state"场景）。
3. worktree 数在增长（289→298），②的负载随时放大。

---

## 四 FIX-007

### 判定：**复现** — 熔断条件**无"陈旧 marker"判定**，且 `loop-context.sh:87` 把**正常提交**与**陈旧 marker 产物**都计入 bypass 计数（误熔断）

### ⒜ 熔断实现共 4 处（`grep -rn "熔断" scripts/ --include="*.sh"` → **16 行 / 4 个文件**）

| # | file:line | 条件 | 是否新鲜度/陈旧 marker 判定 |
|---|---|---|---|
| 1 | `scripts/pre-commit-check.sh:242` | `BYPASS_COUNT=$(grep -c "${TODAY}.*detected-bypass" "$BYPASS_LOG" …)` | ❌ 无 |
| 2 | `scripts/pre-commit-check.sh:1085` | 强弱信号分离（D438）`grep -cE "$(date +%Y-%m-%d).*detected-bypass"` | ❌ 无 |
| 3 | **`scripts/workflow/loop-context.sh:87`** | `BYPASS_COUNT=$(grep -c "$TODAY" "$BYPASS_LOG" …)`；`:90` `-ge 3` → 熔断 | ❌ **无（且判据放宽为"任意含今日日期的行"）** |
| 4 | `scripts/hooks/post-commit.sh:53-54` | merge 提交 → `:` 跳过（CT-45 豁免） | — （**写侧豁免已实现**） |

条件 3 原文（`scripts/workflow/loop-context.sh:83-93`）：

```bash
84: BYPASS_LOG="$ROOT/.claude/bypass.log"
85: if [ -f "$BYPASS_LOG" ]; then
86:   TODAY=$(date +%Y-%m-%d)
87:   BYPASS_COUNT=$(grep -c "$TODAY" "$BYPASS_LOG" 2>/dev/null | tr -d '\r' || echo 0)
88:   BYPASS_COUNT=${BYPASS_COUNT//[^0-9]/}
89:   [ -z "$BYPASS_COUNT" ] && BYPASS_COUNT=0
90:   if [ "${BYPASS_COUNT:-0}" -ge 3 ]; then
91:     echo -e "  ${RED}熔断: 今日 --no-verify ${BYPASS_COUNT} 次 (阈值 3)${RESET}"
```

**与条件 1 的对照**：`pre-commit-check.sh:242` 在 V4.5.1 已修为
`grep -c "${TODAY}.*detected-bypass"`（注释：*"V4.5.1: 只匹配 detected-bypass 行。COMMITTED 行是正常提交成功标记，不是绕过。"*），
但**同一修法未同步到条件 3** ⇒ **两处判据漂移**。

### ⒝ 反例实验（夹具在 `/tmp/verif-ev/fix007-repo`，**isolated**：只放 `loop-context.sh`，其余信号源缺失）

| 例 | 账本内容 | `grep -c detected-bypass` | 熔断器输出 | rc |
|---|---|---|---|---|
| **A0 对照** | 空 | 0 | `绕过检查: 00 次 (正常)` / `系统正常 — 无需熔断` | **0** |
| **A1 滥用** | **3 条今日 `COMMITTED`**（正常提交成功标记） | **0** | **`熔断: 今日 --no-verify 3 次 (阈值 3)`** / `熔断触发 — 需要人工介入` | **1** |
| **A2 对照** | 2 条今日 `COMMITTED` | 0 | `警告: 今日 --no-verify 2 次` / `系统正常` | **0** |
| **A3 陈旧 marker** | **3 条今日 `possible-bypass diff=99999s`**（陈旧 marker 的产物） | **0** | **`熔断: 今日 --no-verify 3 次 (阈值 3)`** | **1** |

A1 原始输出：

```
$ printf '%s | COMMITTED | pre-commit PASS (hook 层登记) | HASH=aaa\n…（共 3 条）\n' "$TODAY" > $FX/.claude/bypass.log
$ grep -c "detected-bypass" $FX/.claude/bypass.log
0
$ bash $FX/scripts/workflow/loop-context.sh --check
  [0;32m循环轮次: 无活跃任务[0m
  [0;31m熔断: 今日 --no-verify 3 次 (阈值 3)[0m
    见 pre-commit 第 7 组检查。请修复根因而非绕过。
[0;31m熔断触发 — 需要人工介入[0m
rc=1
```

⇒ **判别性成立**：A0/A2（绿）与 A1/A3（红）成对；A1 的账本里 **`detected-bypass` 计数为 0**，
熔断器却报 **"今日 --no-verify 3 次"** ⇒ **纯假红**。

### ⒞ "merge 提交 + 陈旧 marker" 是否仍熔断 —— **成对反例（写侧）**

`/tmp/verif-ev/fix007-git`（`git init` 夹具 + 真实 `post-commit.sh`）：

**B1（merge 提交 + 陈旧 marker）**：

```
HEAD=cc8b072  HEAD^2=83c2da1                  ← 确为 merge commit
.claude/last-precommit-success = deadbeefcafe|1790267513   ← HEAD 既不匹配、也非祖先（陈旧污染）
$ bash scripts/hooks/post-commit.sh
post_commit_rc=0
--- .claude/bypass.log 内容 ---
(无账本)
--- grep -c detected-bypass ---
0
```

⇒ **merge 提交写侧豁免有效**（CT-45 生效，`post-commit.sh:53-54`）——**这部分症状已修复**。

**B2（非 merge 提交 + 同一陈旧 marker）**：

```
HEAD=4757f22  HEAD^2=NONE                     ← 非 merge
$ bash scripts/hooks/post-commit.sh
post_commit_rc=0
--- .claude/bypass.log ---
2026-09-25T16:32:01Z detected-bypass head-mismatch marker=deadbeefcafe parent=cc8b072211889cc9974182a40e8716b1e2cebfb5
--- grep -c detected-bypass ---
1
```

⇒ **对照成立**：同一陈旧 marker，非 merge 即写 `detected-bypass`。

### ⒟ 结论链

1. **写侧**：merge 提交已豁免（CT-45）✅；但**陈旧 marker 本身不参与判定** —— `post-commit.sh:113` 的
   `detected-bypass head-mismatch` **无任何新鲜度闸门**（`FRESHNESS_SEC` 只作用于 **PASS_WAY≠0** 分支内的
   `possible-bypass`）。
2. **消费侧**：`loop-context.sh:87` **连 `COMMITTED` 与 `possible-bypass` 都计入** ⇒ 任一来源的今日污染
   即熔断全队（阈值 3）。**这正是 D524 事故机理的未修分支**。
3. ⇒ **台账判据「熔断条件含"陈旧 marker"判定」= 不满足（未实现）**；且存在**独立于 marker 的假红通道**（A1）。

### 未清项

1. `pre-commit-check.sh:242` 侧（GATEKEEPER 硬阻断）对 `possible-bypass` 是否计数：其判据为
   `${TODAY}.*detected-bypass`，而 `possible-bypass` **不含**子串 `detected-bypass` ⇒ 静态上不计数；
   **本件未跑该脚本的完整消费链**（脚本体量大、依赖多），**未做行为级验证**。
2. 条件 2（`pre-commit-check.sh:1085`）与条件 1 是否语义等价，未逐条实证。

---

## 五 FIX-008

### 判定：**复现** —— 该测试**确实改写 tracked 文件**（**推翻** CTO 的「实测为读取非改写（:76-77）」）

### ⒜ 一次性 clone 内跑测试的 `git status --porcelain` **前/后对照**

```
HEAD=ef299746

=== 跑测试前 ===
?? ".claude/task-briefs/2026-09-26-D1008-侦察用-verifier-补证.md"
?? task-state/D1008.json
(行数= 2)                       ← 仅我自己 FIX-006 实验的 untracked；**零 M**

$ bash tests/control-tower/founder-truth.test.sh
  ✅ 接线: 三问面板函数（ledger/north-star/ci/alert）+ git 核验逻辑存在
  ❌ 正常: 输出结构异常
  ❌ 判定: 缺红绿灯标记
  ✅ 边界: 北星对齐含'无对应'方向漂移标记
  ✅ 边界: 诚信账本按 agent 计分（含诚信百分比）
  ✅ 降级: git 异常路径被处理（exit 2，非崩溃）
  ✅ HTML: 生成自包含页面 founder-console.html
结果: 5 通过, 2 失败
test_rc=1

=== 跑测试后 ===
 M docs/synova/founder-alerts.md
 M docs/synova/founder-console.html
?? ".claude/task-briefs/2026-09-26-D1008-侦察用-verifier-补证.md"
?? task-state/D1008.json
(行数= 4)                       ← **+2 个 tracked 文件被改写**

$ git diff --stat
 docs/synova/founder-alerts.md    | 13 +++++++++++--
 docs/synova/founder-console.html | 14 +++++++-------
 2 files changed, 18 insertions(+), 9 deletions(-)

$ shasum -a256 docs/synova/founder-console.html
前: 11eaf1912bca59623b6eb0b27c030056de6621c618b010465f16814d5eddd49a
后: e1318e002d07472fdfc43c013aa514e0ddd9b6b43cd5b838d2327c70407cd479
```

⇒ **判据「跑测试后 `git status --porcelain` 干净」= 不满足（复现）**。
两个被改写文件均 **`git ls-files --error-unmatch` 确认 tracked**：

```
$ git ls-files --error-unmatch docs/synova/founder-console.html
docs/synova/founder-console.html
```

### ⒝ 写点 file:line（**CTO 的 `:76-77` 是"断言读取处"，不是写点**）

`tests/control-tower/founder-truth.test.sh` 中 3 处生成器调用：

```
16: GEN="$REPO/scripts/control-tower/founder-truth.py"
33: OUT=$(python3 "$GEN" --offline 2>&1)                 ← 触发 ALERT_OUT 写（无条件）
65: GIT_DIR="$FAKEGIT" python3 "$GEN" --offline >/dev/null 2>&1
75: HTML=$(python3 "$GEN" --offline --html 2>&1)          ← 触发 HTML_OUT 写
76: if echo "$HTML" | grep -q "已生成" && [ -f "$REPO/docs/synova/founder-console.html" ]   ← CTO 只看这里
```

`scripts/control-tower/founder-truth.py` 写点（8 处命中，关键 5 处）：

```
32: HTML_OUT = REPO / "docs" / "synova" / "founder-console.html"
33: ALERT_OUT = REPO / "docs" / "synova" / "founder-alerts.md"
286:             ALERT_OUT.write_text("✅ 当前无红灯（所有声称均有物理支撑）。\n", encoding="utf-8")
294:     ALERT_OUT.write_text("\n".join(lines) + "\n", encoding="utf-8")
418:         HTML_OUT.write_text(render_html(rows, green, yellow, red, git_ok, ledger, north, ci), encoding="utf-8")
```

⇒ **`:76-77` 是断言（读），写发生在 `:75`（`--html` → `:418`）与 `:33`（→ `:294`）**。
CTO 的"未证实改写"系**只看断言行、未看生成器调用**所致。

### ⒞ 旁证：`founder-alerts.md` 也**确实出现**

台账记「`founder-alerts.md` 未出现」——实测**出现了**（`M docs/synova/founder-alerts.md`，13 行改动）。

### ⒟ 附带发现（**非本件结论，供登记**）

`tests/control-tower/founder-truth.test.sh` 在 **main `ef299746` 上自身即红**：
`结果: 5 通过, 2 失败`（`❌ 正常: 输出结构异常` / `❌ 判定: 缺红绿灯标记`），rc=1。
该测试**不在 `ci.yml` 的 CT 密封清单内**，故 CI 侧不暴露。

### 未清项

1. 本件只证"改写 tracked 文件"，**未定位**为何两个输出与库内版本不一致（生成器内容漂移 vs 测试自身红）——
   两者可能同源，需另开卡。
2. "测试写隔离目录"的修法**未验证**（属 task-11 范畴，我只读）。

---

## 六 汇总表

| 条 | 判定 | 一句话 |
|---|---|---|
| **006** | **部分复现** | clone 内未挂死（rc=0/3.042s）；**真仓只读复刻扫描循环 91.956s 复现机制**；「字符串累加 5 处」不可复现（真·累加 **1 处** `:295-296`）；读取面≠拒绝面**成立**但 ls-remote 那条是有意设计 |
| **007** | **复现** | **无「陈旧 marker」判定**；`loop-context.sh:87` 用 `grep -c "$TODAY"` ⇒ **3 条正常 `COMMITTED`（0 条 bypass）即熔断 rc=1**（A1）；陈旧 marker 产物 `possible-bypass` 同样计入（A3）；merge 写侧豁免（CT-45）**已验证有效**（B1/B2 成对） |
| **008** | **复现** | 测试改写 **2 个 tracked 文件**（`founder-console.html`+`founder-alerts.md`），sha256 变更，`git status` 前后 2→4 行；CTO「读取非改写」**被推翻**；写点 `test:75`/`test:33` → `py:418`/`py:294` |
| **015** | **未复现（作为生产违规）** | 9/3 逐字复现但 **100% 自伤**：0 处 `src/`；12 处 = 6 A（测试）+ 4 C（检查自身模式/标签）+ 2 C（文档）；两链排除机制失效（`as any` 链无白名单；`DiagnosticModule` 链按行内字面量排除 ⇒ 内容行不含路径） |
| **016** | **部分复现 / 定位=无法确认** | CI 双平台 **23/1** 复现；本地 **#803=24/0 / main=38/0**（台账口径混用）；main 自身 CI 双绿 ⇒ **#803 引入**；**具体断言不可定位**（`ci.yml:287` `tail -8\|cut -c1-450` + 零 artifact）；最强候选=全角冒号断言（**未证实**） |

## 七 未清项汇总（**不得作为可修依据的部分**）

1. **016 的具体断言未定位** ⇒ 按 task-8 红线「定位前不得改夹具」，**task-8 不具备开工条件**。
2. **006 的"真仓取号挂死"未在真仓实跑**（会烧真号 + 写 task-state，超我只读写集）⇒ 需 CTO 授权后才可判"入口挂死"。
3. **006 的"worktree task-state 未进拒绝面是否致实际撞号"未做判别性实验**。
4. **007 的 `pre-commit-check.sh` 侧消费链未做行为级验证**（只做静态判据读解，已显式标注）。
5. **015 的修法方向属建议**，非本件结论；本件**未改任何代码**。
6. 冻结前提「worktree = 287」**已过时**（实测 289→298，session 内持续增长）。

---

*本件为「自验结论」，可提请独立审计；**不得表述为"审计通过"**（审计结论只认 K3）。*
