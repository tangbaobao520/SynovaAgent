# FIX-008 证据 — 测试副作用碰 tracked 文件（生成物输出隔离）

> 卡：台账 FIX-008（M8 家族「测试副作用碰 tracked 文件」**第 3 例**，复现确立）｜产出者：fix-coder-b｜日期：2026-09-26
> 分支：`fix/fix008-test-isolation`（非栈式，base = `origin/main` @ `ef299746`）
> 台账判据：**测试写入隔离目录**；「跑测试后 `git status` 干净」判据。
> ⚠️ 本测试在 main 上另有 **既存红**（见 §4），**不属本卡**，未改断言凑绿。

---

## 1. 写点与触发点（file:line）

- 写点：`scripts/control-tower/founder-truth.py`
  - `HTML_OUT = REPO / "docs" / "synova" / "founder-console.html"`（原 :32）
  - `ALERT_OUT = REPO / "docs" / "synova" / "founder-alerts.md"`（原 :33）
  - 写入调用：`write_alert()`（原 :289/:298）与 `--html` 分支（原 :422）
- 触发：`tests/control-tower/founder-truth.test.sh:75`（`python3 "$GEN" --offline --html`）——测试直接跑生成器 ⇒ 写到仓库 tracked 生成物

## 2. 修前（复现，原始输出）

```
$ git status --porcelain -- docs/synova/founder-console.html docs/synova/founder-alerts.md
（空=干净）
$ bash tests/control-tower/founder-truth.test.sh
  ✅ 接线: 三问面板函数（ledger/north-star/ci/alert）+ git 核验逻辑存在
  ❌ 正常: 输出结构异常
  ❌ 判定: 缺红绿灯标记
  ✅ 边界: 北星对齐含'无对应'方向漂移标记
  ✅ 边界: 诚信账本按 agent 计分（含诚信百分比）
  ✅ 降级: git 异常路径被处理（exit 2，非崩溃）
  ✅ HTML: 生成自包含页面 founder-console.html
结果: 5 通过, 2 失败
$ git status --porcelain          # 跑完测试后
 M docs/synova/founder-alerts.md
 M docs/synova/founder-console.html
共 2 处
$ git diff --stat -- docs/synova/founder-console.html docs/synova/founder-alerts.md
 docs/synova/founder-alerts.md    | 12 ++++++++++--
 docs/synova/founder-console.html | 14 +++++++-------
 2 files changed, 17 insertions(+), 9 deletions(-)
```

⇒ 副作用复现：跑一次测试即污染 2 个 tracked 文件（切分支/提交会被拦）。

## 3. 修后（本分支，原始输出）

改动：
1. `founder-truth.py` 加 **env 注入缝** `SYNO_FOUNDER_OUT_DIR`（缺省 = `REPO/docs/synova`，**生产行为不变**），并在两处写前 `OUT_DIR.mkdir(parents=True, exist_ok=True)`；
2. `founder-truth.test.sh` 全部生成调用改走 `mktemp -d` 隔离目录；新增判别性判据（下）。

```
$ bash tests/control-tower/founder-truth.test.sh
  ❌ 正常: 输出结构异常
  ❌ 判定: 缺红绿灯标记
  ❌ 边界: 北星对齐缺'无对应'标记（或当前全部对齐）
  [⒜] 仓库脏度: 跑测试前=2 跑测试后=2；生成物 porcelain 行数: 前=0 后=0
  ✅ FIX-008⒜ 隔离: 仓库两个 tracked 生成物 git status 为空（绝对判据，基线干净时必满足）
  ✅ FIX-008⒜ 隔离: 本测试未增加仓库脏度（前=2 后=2）
  ✅ FIX-008⒜ 隔离: HTML 实落在隔离目录（非仓库）
  [⒝] 沙箱脏树: 改动前 porcelain 行数=0 → 故意改 tracked 后=1
  ✅ FIX-008⒝ 反例: 同一判据在脏树报红（判别性成立，非恒真）
结果: 8 通过, 3 失败
$ git status --porcelain          # 跑完测试后（仅本卡自身的 2 个改动文件）
 M scripts/control-tower/founder-truth.py
 M tests/control-tower/founder-truth.test.sh
```

### 判别性判据（成对反例，非 grep 型静态判据）

| 侧 | 场景 | 判据实现 | 实测 |
|---|---|---|---|
| ⒜ | 隔离输出（两次生成：`--offline` / `--offline --html`） | 仓库两个生成物 `git status --porcelain` 行数；仓库总脏度增量 | 生成物 **前=0 后=0**；总脏度 **前=2 后=2**（= 本卡自身 2 个编辑，未增加）→ **绿** |
| ⒝ | **沙箱 git 仓**内故意改 tracked 生成物（绝不在真仓库制造脏树） | 同一 `gen_paths_dirty()` 实现 | **前=0 → 后=1** ⇒ 判据在脏树报红 → **判别性成立（非恒真）** |
| ⒜ | HTML 落地位置 | `[ -s "$OUTDIR/founder-console.html" ]` | 隔离目录内存在 → **绿** |

## 4. 既存红（**不属本卡**，另立卡；原始输出）

本测试在 main（`ef299746`）上即失败，修前/修后**同一组断言**：

```
修前: 结果: 5 通过, 2 失败   ❌ 正常: 输出结构异常   ❌ 判定: 缺红绿灯标记
修后: 结果: 8 通过, 3 失败   ❌ 正常: 输出结构异常   ❌ 判定: 缺红绿灯标记   ❌ 边界: 北星对齐缺'无对应'标记
连跑 3 次（失败集合抖动）：
  结果: 6 通过, 5 失败 / 结果: 7 通过, 4 失败 / 结果: 6 通过, 5 失败（抖动集合不同）
```

**定位（只读，未改断言）**：把同一份输出落盘后直连 grep，全部断言目标内容**都存在**：

```
$ SYNO_FOUNDER_OUT_DIR=$(mktemp -d) python3 scripts/control-tower/founder-truth.py --offline > /tmp/fix008-out.txt
$ grep -c "创始人控制台" /tmp/fix008-out.txt   → 1
$ grep -c "小结"         → 1
$ grep -c "诚信账本"     → 1
$ grep -c "北星对齐"     → 1
$ grep -c "CI 最近一次"  → 1
$ grep -cE "🟢|🟡|🔴|⚪"  → 371
$ grep -cE "诚信 [0-9]+%" → 32
```

⇒ 失败机制 = **夹具自身的 EPIPE 缺陷**：`set -uo pipefail` 下 `echo "$OUT" | grep -q PATTERN`，`grep -q` 命中即退出 → `echo` 写破管（`write error: Broken pipe`）→ 管道状态非 0 → `if` 判否（**与内容是否存在无关**），且随调度抖动。属**该夹具既存缺陷**（main 上已存在），本卡按 lead 口径不改其断言；已作为**新 FIX 候选**上报。

## 5. 红线自查

- 未以改断言/删断言/`|| true` 把红变绿：**本卡零新增断言改动之外**，既存失败断言原样保留（§4）
- 未提交生成物改动：实验期污染用 `git checkout -- docs/synova/founder-console.html docs/synova/founder-alerts.md` 还原，工作树最终只有本卡 2 个改动文件
- 未碰 `scripts/audit/**`、`docs/synova/audit-reports/**`、`.github/workflows/ci.yml`
- 未 `--no-verify`、未 `git stash`、未 force push

## 6. 未清项

1. **夹具 EPIPE 缺陷**（§4）：`echo | grep -q` + `pipefail` 造成假失败/抖动 ⇒ 建议改「先落盘再 grep 文件」；属该夹具质量修复，**另立卡**（lead 已记为新 FIX 候选）。
2. **`--html` 生产路径未变**：缺省输出仍是 `docs/synova/founder-console.html`（本卡只加注入缝）；是否有其它调用方依赖该路径 ⇒ `grep -rn "founder-truth.py"` 见下。
3. 其它生成物（product-progress 等）是否同样碰 tracked 文件，不在本卡范围。


---

## 7. 提交期硬阻断与「机制修正」追加（pre-commit 组 2 U7/CT-40）

### 7.1 阻断原始输出（`bash scripts/pre-commit-check.sh` → exit 1）

```
  ❌ 控制塔脚本测试门禁 (U7/CT-40): 2 处  [硬阻断]
     scripts/control-tower/founder-truth.py 配对测试红: tests/control-tower/founder-truth.test.sh
```

规则：**改动 `scripts/control-tower/**` 脚本时，其配对测试必须绿**。而该夹具在 main 上即红（§4，EPIPE 缺陷）⇒ 本卡「加注入缝」与「配对测试红」正面冲突，提交被物理阻断（`synova-commit` 无 `--no-verify`）。

### 7.2 追加修正（**仅机制，不动断言语义**）

把夹具中 6 处 `echo "$OUT" | grep -q PATTERN` 改为 `grep -q PATTERN <<< "$OUT"`（herestring，不产生 SIGPIPE）——**pattern 与预期一字不动**：

| 位置 | 改前 | 改后 |
|---|---|---|
| 正常面板 5 连 | `echo "$OUT" \| grep -q "创始人控制台" && …` | `grep -q "创始人控制台" <<< "$OUT" && …` |
| 红绿灯 / 北星无对应 / 诚信% / HTML 已生成 | 同上 | 同上 |

### 7.3 成对证据（改前 / 改后 / 变异体）

```
改前（同一断言、同一内容）: 结果: 8 通过, 3 失败（且失败集合逐次抖动：3/4/5 条）
  内容实证（落盘直连 grep）: 创始人控制台=1 小结=1 诚信账本=1 北星对齐=1 CI 最近一次=1 红绿灯=371 诚信%=32

改后（机制替换）: 结果: 11 通过, 0 失败

变异体（把 "诚信账本" 改成 "诚信账本-NONEXISTENT-PATTERN"）: 结果: 10 通过, 1 失败
  ❌ 正常: 输出结构异常  → 断言仍具判别力（未变成恒真）
```

⇒ 原 3 条「既存红」**不是内容缺失，而是夹具 EPIPE 缺陷**；本卡按物理门禁要求做**机制修正**（不改断言、不删断言、不 `|| true`），并附变异体反证。
**给 lead 的显式说明**：此追加超出原批准口径（原口径「既存红不属本卡」），因 U7/CT-40 硬阻断无其它通路；如需拆出，可回退该 6 处替换（回退后本卡无法提交，需 lead 另裁）。
