# FIX-013 + FIX-015 回执（stacked on feat/d962-2a-merge @ 31971419）

- **分支**: `fix/d962-2a-iron-laws-wiring`（stacked；**PR base 显式 = `feat/d962-2a-merge` @ 31971419**）
- **工作树**: `.synova-wt-fix803`（本任务自建；未碰 `.synova-wt-d962m`，其内他人未提交改动未动）
- **写集**: `scripts/pre-commit-check.sh`｜`.github/workflows/ci.yml`（本批 ci.yml 单写者）｜`tests/control-tower/iron-laws-live-point.test.sh`｜`tests/control-tower/iron-laws-selfdiff.test.sh`｜决策 Note｜本文件。**未碰** `scripts/audit/**`、`docs/synova/audit-reports/**`、`src/**`。
- **判定**: 待 CTO 收件闸 + K3 终审（**本人只给自验结论，不自称通过**）

## FIX-013

### ⒜ 活点（job 名 + 行号原文）

```
$ grep -n "^  iron-laws:" .github/workflows/ci.yml
111:  iron-laws:
$ sed -n '111,127p' .github/workflows/ci.yml
  iron-laws:
    name: Iron Laws (gate semantics)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # SYNO_DIFF_BASE=origin/main 的 ...HEAD 三点 diff 需完整历史
      - name: Iron laws (moved gate semantics; CI authority)
        run: bash scripts/pre-commit-check.sh
        env:
          SYNO_DIFF_BASE: "origin/main"
          SYNO_CI: "1"   # 软提示转硬——与 quality job 同口径；失败行经 ::error 注解可见
```

**无 `needs`**（`tests/control-tower/iron-laws-live-point.test.sh` 断言）：上游 `quality` 红不牵连 ⇒ 活点可独立绿/红，绕开「quality 红 ⇒ needs 下游 skip ⇒ 必需检查永不回报」的链。

### ⒝ 改坏即红（判别性夹具）

`tests/control-tower/iron-laws-live-point.test.sh` 内：把 `ci.yml` 复制到 mktemp 沙箱、**用 python 删掉 `iron-laws` 整块**，用**同一提取器**再判一次 —— 必须报缺失。

```
$ bash tests/control-tower/iron-laws-live-point.test.sh; echo rc=$?
  ✅ ⒜ 活点存在: .github/workflows/ci.yml:111 → job 'iron-laws'
  ✅ 活点执行 scripts/pre-commit-check.sh（移出判定面的单一实现）
  ✅ 活点注入 SYNO_CI=1（软提示在 CI 转硬 = 权威口径）
  ✅ 活点注入 SYNO_DIFF_BASE=origin/main（判定面扫描范围 = 本 PR 变更集）
  ✅ 活点无 needs（上游红不牵连，可独立绿/红）
  ✅ 脚本含 CI 权威区（被移出判定面的落点）
  ✅ FIX-015 源面代码判定面（按路径排除自伤面）已接线
  ✅ ⒝ 判别性夹具: 删除 job 后提取器报缺失（删掉即红，判别力成立）
  ✅ ⒝ 变异体实测: 真 ci.yml 行 111 ↔ 变异体无该 job
结果: 9 通过, 0 失败 / rc=0
```
⇒ 真删 `.github/workflows/ci.yml:111` 的 job ⇒ 第 1 条断言红 ⇒ 测试 exit 1（非 grep 型静态判据：删除夹具用同一提取器对变异体重判）。

### ⒞ 有它则绿（真实 CI run）

见 §CI（push 后追加 run URL + 结论）。

### 与 `quality` 的职责边界（lead 问 4）

- **同脚本同口径、有意重复**：`quality` 内 Iron laws step（`if: docs_only != true`）+ 本 job（无 if、无 needs）都跑 `scripts/pre-commit-check.sh`。
- **为何不删 quality 里的 step**：`TypeScript + Lint + Iron Laws` 是 main 分支保护的**既有必需检查**；把 Iron Laws 从它里面摘掉 = 削弱已生效的必需面（若新 job 未同步加入保护，就是净损失）⇒ 本卡只**增**不**减**。
- **代价（如实登记）**：同一脚本在 ubuntu 上多跑一次（实测本地 ~2s；CI 含 checkout/setup ~20-40s）。**是否收敛为单点（改分支保护 + 摘 step）属 CTO 决策**，见未清项。
- **为何不是装饰 job**：它无 `if`/无 `needs`，且失败经 `::error title=IronLaws:` 可见；其判定面在本地 diff 上实测可红（见 FIX-015 成对反例）。

### D721 结论（lead 要求：修 or 独立登记，不许沉默）

**结论：独立 FIX 条目登记（不在本卡修）。** 本机复核原文（`ci.yml` 内 `test` job 的 Run tests step）：

```
BASE=$(git merge-base refs/remotes/origin/main HEAD 2>/dev/null || git merge-base origin/main HEAD 2>/dev/null || echo "")
if [ -n "$BASE" ]; then CHANGED=$(git diff --name-only "$BASE"..HEAD …) … fi
…
if [ -n "$FAILED_TESTS" ] && [ -n "$CHANGED" ]; then   # 存量红放行分支
```
**main push 上 `merge-base(origin/main, HEAD) == HEAD` ⇒ `CHANGED` 恒空 ⇒ 放行分支不可达** ⇒ 存量红在 main push 上恒判红（lead 实测 run 36146340288）。与 2a 无关、非本卡引入。
**登记内容**：修法应为「`BASE == HEAD`（在 main 上）时回退 `HEAD~1..HEAD`」；夹具应为「构造 BASE==HEAD + 已知存量红 + 最后一提交未触及失败文件 → job 必须绿；把回退分支删掉 → 必须红」。**本卡不动它**（改判决语义 + 需独立夹具，且分支保护面受影响）。

## FIX-015（成对反例，本机原始输出）

`tests/control-tower/iron-laws-selfdiff.test.sh`（注入缝 `SYNO_TEST_ARM=1` + `SYNO_GIT_CACHED_DIFF`，仅在测试进程内武装；不写源工作树）：

```
$ bash tests/control-tower/iron-laws-selfdiff.test.sh; echo rc=$?
  ✅ Arm-A 源面注入 as any → ❌ 1 处（真注入仍被拦）
  ✅ Arm-A 失败经 ::error 注解可见（含失败断言原文）
  ✅ Arm-A2 源面注入 DiagnosticModule → ❌ 1 处
  ✅ Arm-B 自伤面（$0/tests/docs）注入 as any → ✅（不再自伤）
  ✅ Arm-B 自伤面注入 DiagnosticModule → ✅（不再自伤）
  ✅ Arm-C 12 处注入 → 计数 12 处
  ✅ Arm-C 12 行明细全部打印（原 head -8 会吞掉第 9-12 行）
结果: 7 通过, 0 失败 / rc=0
```
- **红（滥用/真注入仍红）**：`src/probe.ts` 加 `const y = JSON.parse(s) as any;` → `❌ as any …: 1 处` + `::error title=IronLaws:…::+const y = JSON.parse(s) as any;`
- **绿（自伤面转绿）**：同字面量只出现在 `scripts/pre-commit-check.sh` / `tests/**/*.test.sh` / `docs/*.md` → `✅`（按 hunk 级 `+++ b/<path>` 排除，**非**行内白名单）
- 判别力：去掉排除（或写成行内白名单）→ Arm-B 红；把判定面关掉 → Arm-A 绿 ⇒ 两向都可证伪。
- `_emit_matches()`：CI 下打印全部命中行 + `::error` 注解（原 `head -8` 使第 9 行起 CI 不可见）。

## 本地 CI 口径预演（**不是 ⒞**）

```
$ GITHUB_ACTIONS=true SYNO_CI=1 SYNO_DIFF_BASE=origin/main bash scripts/pre-commit-check.sh; echo rc=$?
✅ 本地清单通过（D962 2a① 终态；33 项判定见 CI iron-laws）  [CI 权威区 6 项全 ✅]
rc=0
```

## 未清项
| # | 事项 | 归属 | 状态 |
|---|---|---|---|
| L1 | `quality` 内 Iron laws step 与本 job 重复执行（CI 时长 +~30s）；是否收敛为单点（含分支保护同步） | CTO | 登记，**未清** |
| L2 | D721（main push 上放行分支不可达）已登记为**独立 FIX 条目**，本卡不修 | 控制塔线 | 登记，**未清** |
| L3 | 既有「CI 挂载归 2b」注释：本轮改指活点的为 `tests/control-tower/g8-file-driven.test.sh` 与 `tests/control-tower/sync-dsh-skills.test.sh` 各 1 行；`g5-architecture.test.sh:12` / `precommit-groups-injection.test.sh:207,430` 的注释未改（夹具本体行为不动）⇒ 其原断言面（如组 2 结构性不红）是否恢复更强断言，属 D962 2b 后续 | CTO | 登记 |
