# FIX-014 证据 — main `Vitest (2/2)` 既有红：复现 + 棘轮登记（D954 disposition_due）

> 任务：task-4（FIX-014）｜产出者：fix-coder-b（Mac 小队编排成员）｜日期：2026-09-26
> 统一登记处：`docs/synova/coordination/问题修复台-20260925.md` FIX-014（CTO 单写者，本文件只读引用）
> 台账判据：**另开卡 + 按 D954 棘轮登记（含 `disposition_due` + 责任人）；不得豁免计绿**
> 所有数字均来自命令原始输出；本节所有命令在工作树 `/Users/wane/SynovaAgent/.synova-wt-fix014` @ `ef299746` 执行。

---

## 0. 结论摘要

| 项 | 结论 | 依据 |
|---|---|---|
| main `Vitest (2/2)` 红复现 | **复现**（main push run 36146340288 @ ef299746，唯一失败用例 1 个） | §1 |
| 归属分列 | **既存红**（父提交 0f709900 同名同 file:line 已红）；**本推送引入 = 0** | §2 |
| 棘轮登记缺口 | main 版 `ci-red-baseline.txt` 数据行 6 条中 `disposition_due` 命中 = **0**；`Vitest (2/2)` owner = `UNASSIGNED` | §3 |
| 登记后判别力 | 逾期 → 红（exit 1）；条目删除 → 红（exit 1）；在期 → 绿（exit 0） | §4 |
| 是否"豁免计绿" | **否**。棘轮只把已登记红计为"基线命中"（不再重复追责），**红本身仍在**：CI run 全站结论 = failure；vitest job 真 exit 1 | §1 + §3 |
| PR 侧 skip 不计绿 | PR 上 `Vitest (${{ matrix.shard }})` = **skipped**（上游 `quality` 红致矩阵未展开）→ 记"未执行"，**不记绿** | §5 |

---

## 1. 复现：main `Vitest (2/2)` = failure（原始输出）

### 1.1 取 main push run（REST，gh 不存在，走 curl）

```
$ TOKEN=$(grep -E '^\s*GITHUB_TOKEN:' ~/.dsh/.credentials.yaml | sed 's/.*GITHUB_TOKEN:[[:space:]]*//' | tr -d '\r\n')
$ curl -s -H "Authorization: Bearer $TOKEN" \
    "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/36146340288/jobs?per_page=100" -o /tmp/fix014-jobs.json
$ python3 -c "import json;d=json.load(open('/tmp/fix014-jobs.json'));[print(f\"{j['name']!r:45} | {j['status']:10} | {j['conclusion']} | id={j['id']}\") for j in d['jobs']]"
'Integration Contract Check'                  | completed  | success | id=108108342181
'npm audit'                                   | completed  | success | id=108108342422
'Test-Kit Architecture Tests (ubuntu-latest)' | completed  | success | id=108108342466
'Control Tower Gate Tests (windows-latest)'   | completed  | success | id=108108342617
'Architecture Check'                          | completed  | success | id=108108342654
'Control Tower Gate Tests (ubuntu-latest)'    | completed  | success | id=108108342680
'Test-Kit Architecture Tests (windows-latest)' | completed  | success | id=108108342690
'TypeScript + Lint + Iron Laws'               | completed  | success | id=108108342789
'Gate Integrity (pattern sentinel + injection fixture + ci-reds)' | completed  | success | id=108108342835
'Checker Review (maker/checker)'              | completed  | skipped | id=108108396004
'Vitest (2/2)'                                | completed  | failure | id=108108419949
'Vitest (1/2)'                                | completed  | cancelled | id=108108420119
'Golden Case F1 Gate'                         | completed  | skipped | id=108109461052
```

- run URL：https://github.com/tangbaobao520/SynovaAgent/actions/runs/36146340288
- 口径：workflow = `CI — SynovaAgent`｜event = push｜branch = main｜head_sha = `ef2997466677324b94d7b2921a5b3de0b7ad57d3`｜run conclusion = **failure**

### 1.2 失败 job 日志（job 108108419949，8381 行 / 1,752,784 bytes）

```
$ curl -sL -H "Authorization: Bearer $TOKEN" \
    "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/jobs/108108419949/logs" -o /tmp/fix014-vitest22.log
$ wc -l /tmp/fix014-vitest22.log ; wc -c /tmp/fix014-vitest22.log
    8381 /tmp/fix014-vitest22.log
 1752784 /tmp/fix014-vitest22.log
```

失败片段（**完整断言原文，未截断**）：

```
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/l3/graphbridge-wiring.test.ts > GraphBridge → ModuleRunner.afterRun → Phase 1 > Given a failing module, When afterRun with GraphBridge, Then only successful modules create nodes
AssertionError: expected +0 to be 1 // Object.is equality

- Expected
+ Received

- 1
+ 0

 ❯ tests/l3/graphbridge-wiring.test.ts:79:31
     77|
     78|     // Only hona created a node; broken did not
     79|     expect(graphNodes.length).toBe(1);
       |                               ^
     80|   });
     81|


 Test Files  1 failed | 293 passed | 1 skipped (295)
      Tests  1 failed | 2123 passed | 5 skipped (2129)
   Start at  14:18:24
   Duration  41.23s (transform 7.98s, setup 0ms, import 17.54s, tests 58.15s, environment 43ms)

EXIT:1
##[error]Process completed with exit code 1.
```

判别点（job 内自述，log:1030）：

```
变更集基准: merge-base(origin/main, HEAD)=ef2997466677324b94d7b2921a5b3de0b7ad57d3（三点差：仅本 PR 引入的改动）
```

即 `BASE == HEAD == ef299746` → `CHANGED` 为空 → D721 的「存量红放行」分支不可达 → 直接 `exit 1`。
（该 D721 逻辑位于 `.github/workflows/ci.yml:131-157`，ci.yml 单写者 = fix-coder-a，本卡不改，只作事实记录。）

---

## 2. 「本推送引入 / 既存」显式分列

### 2.1 既存（父提交同红，原始输出）

```
$ curl -s -H "Authorization: Bearer $TOKEN" \
    "https://api.github.com/repos/tangbaobao520/SynovaAgent/actions/runs/36129254913/jobs?per_page=100"
  'TypeScript + Lint + Iron Laws'                    | success | id=108052458884
  'Gate Integrity (pattern sentinel + injection fixture + ci-reds)' | success | id=108052459025
  'Vitest (2/2)'                                     | failure | id=108052526513
  'Vitest (1/2)'                                     | success | id=108052526522
```

run 36129254913 = main push @ `0f7099005a62406fbfaa546bbb00117ba1bcc589`（ef299746 的父提交）。
其 Vitest (2/2) 日志（job 108052526513）同用例同名同 file:line：

```
 FAIL  tests/l3/graphbridge-wiring.test.ts > GraphBridge → ModuleRunner.afterRun → Phase 1 > Given a failing module, When afterRun with GraphBridge, Then only successful modules create nodes
 Test Files  1 failed | 293 passed | 1 skipped (295)
      Tests  1 failed | 2123 passed | 5 skipped (2129)
变更集基准: merge-base(origin/main, HEAD)=0f7099005a62406fbfaa546bbb00117ba1bcc589（三点差：仅本 PR 引入的改动）
```

再上一代：run 36128571272 @ `ce231ff1` → `Vitest (2/2)` = failure、`Vitest (1/2)` = cancelled（连续三代 main 同红）。

### 2.2 本推送引入 = 0（原始输出）

```
$ git show --name-only --format="" ef299746 | grep -c "graphbridge-wiring"
0
$ git log --oneline -1 origin/main -- src/l3/graph-bridge.ts
（该路径在 origin/main 上无提交——测试引用的源不属本次推送改动集）
$ git show --stat --oneline ef299746 | tail -3
 15 files changed, 2452 insertions(+), 314 deletions(-)
```

**判定：既存红；本推送引入 0 处。** 台账口径「既存红不得算绿」在本卡的执行方式 = 登记为既有红（基线命中），**同时保留其真红属性**（job 仍 exit 1，全站 run 仍 failure）。

---

## 3. 棘轮登记：D954 口径 + 改动前后（原始输出）

### 3.1 D954 消费实现（读 `origin/main`，file:line 原文）

`scripts/control-tower/check-gate-integrity.sh`：

```
:749    due="$(_key_value "$line" disposition_due)"
:757    # D954: disposition_due = **处置期限**，与 expires 同级硬门（逾期判红）。
:758    #   键**缺失不判**（存量条目不回溯；缺 expires 仍按既有规则 exit 2 —— 两者语义不同）。
:759-765 if [ -n "$due" ]; then … if [[ "$due" < "$today" ]]; then violation "CI 红处置逾期（disposition_due 硬门）: …"
:678    local red_base="${SYNO_CI_RED_BASELINE:-$ROOT/scripts/control-tower/ci-red-baseline.txt}"
```

棘轮文件确切路径 = **`scripts/control-tower/ci-red-baseline.txt`**（默认值 `:678`；`SYNO_CI_RED_BASELINE` 可覆盖，本卡夹具即用此注入缝，不改判据）。

### 3.2 改动前缺口（main 版实测）

```
$ git show origin/main:scripts/control-tower/ci-red-baseline.txt | grep -c "disposition_due"
0
$ git show origin/main:scripts/control-tower/ci-red-baseline.txt | awk '/^Vitest \(2\/2\)/ {print}' | grep -o 'owner=[^|]*'
owner=UNASSIGNED
```

### 3.3 改动后（本工作树）

```
$ python3 - (按键取值复算，同 _key_value 语义)
line 46: name='Vitest (2/2)'
   first_seen = 2026-09-24
   owner = Mac-小队
   expires = 2026-10-24
   disposition_due = 2026-10-24
line 47: name='Vitest (1/2)'
   first_seen = 2026-09-24
   owner = CTO-Mac
   expires = 2026-10-25
   disposition_due = 2026-10-25
```

`git diff --stat`：`scripts/control-tower/ci-red-baseline.txt | 3 +++--`（**只加不删条目**：1 行表头注释 + 2 条数据行加键/改 owner + 追加复现证据）。

---

## 4. 判别性夹具（改坏即红；非 grep 型静态判据）

夹具输入 = **真实 check-runs JSON**（`GET /commits/ef299746/check-runs?per_page=100`）：

```
total_check_runs=21
conclusions= {'success': 17, 'skipped': 2, 'cancelled': 1, 'failure': 1}
failure names= ['Vitest (2/2)']
```

统一命令（C 段 = 棘轮对账本体；`--patterns-only` 跳过 B 段登记 gate，避免无关面干扰）：

```
bash scripts/control-tower/check-gate-integrity.sh --patterns-only --root "$PWD" \
  --base-ref origin/main --ci-reds /tmp/fix014-checkruns.json
```

### (a) 合法场景仍绿（在期 disposition_due）

```
对账对象 = origin/main @ ef2997466677324b94d7b2921a5b3de0b7ad57d3
  CI-RED-BASELINE: Vitest (2/2)（owner=Mac-小队；expires=2026-10-24）
  CI-RED-BASELINE: Vitest (2/2)（disposition_due=2026-10-24）
CI-RED-CHECK: 失败检查 1 项；基线命中 1 项；基线条目 6 条；今天 2026-09-25；处置逾期 0 项
GATE-INTEGRITY: OK
EXIT=0
```

### (b) 滥用场景必须仍红 1：处置逾期（`disposition_due=2020-01-01`）

```
VIOLATION: CI 红处置逾期（disposition_due 硬门）: Vitest (2/2)（disposition_due 2020-01-01 < 2026-09-25）
CI-RED-CHECK: 失败检查 1 项；基线命中 1 项；基线条目 6 条；今天 2026-09-25；处置逾期 1 项
GATE-INTEGRITY: VIOLATION(1)
EXIT=1
```

### (c) 滥用场景必须仍红 2：条目删除（未登记红不得静默）

```
VIOLATION: 未登记 CI 失败: Vitest (2/2)
CI-RED-CHECK: 失败检查 1 项；基线命中 0 项；基线条目 5 条；今天 2026-09-25；处置逾期 0 项
GATE-INTEGRITY: VIOLATION(1)
EXIT=1
```

### (e) 边界（已知限制，如实登记）：删 `disposition_due` 键 → 不红

```
CI-RED-CHECK: 失败检查 1 项；基线命中 1 项；基线条目 6 条；今天 2026-09-25；处置逾期 0 项
GATE-INTEGRITY: OK
EXIT=0
```

原因 = D954 明文语义「**键缺失不判**（存量条目不回溯）」；此时仍有 `expires=2026-10-24` 兜底判红（§3.1 `:754-756`）。
⇒ 诚实结论：`disposition_due` 是**加严项**（在期才放行、逾期必红），**不是** 替代 `expires` 的唯一门；"删键即免罚"由 `expires` 段封住，属可接受语义。**未清项**登记于卡内。

---

## 5. PR 侧 `Vitest` = skipped（"未执行"，**不得计绿**）

```
$ curl -s .../commits/41b97740/check-runs?per_page=100        # PR #796 head
  'Vitest (2/2)'                  | completed | success
  'Vitest (1/2)'                  | completed | success
  'TypeScript + Lint + Iron Laws' | completed | success

$ curl -s .../commits/31971419/check-runs?per_page=100        # PR #803 head
  'Vitest (${{ matrix.shard }})'  | completed | skipped
  'TypeScript + Lint + Iron Laws' | completed | failure

$ curl -s .../commits/1c7ab52f/check-runs?per_page=100        # PR #799 head
  'Vitest (${{ matrix.shard }})'  | completed | skipped
  'TypeScript + Lint + Iron Laws' | completed | failure
```

判定：PR 上 `Vitest (${{ matrix.shard }})` 字面量 + skipped = `quality`（TypeScript + Lint + Iron Laws）红导致矩阵未展开、`needs` 整块跳过 → **未执行**态，既非红亦非绿；**不得计绿**（台账口径）。main 侧 `Vitest (2/2)` = 真红（§1），二者不得混算。

---

## 6. 回执 / 复现前提

- 工作树：`/Users/wane/SynovaAgent/.synova-wt-fix014`｜分支：`fix/fix014-vitest-preexisting-red`｜base = `origin/main` = `ef2997466677324b94d7b2921a5b3de0b7ad57d3`
- 关键命令均可重跑：§1 curl 两条、§4 三条夹具命令（夹具副本落 `/tmp/fix014-m-*.txt`，仓库零篡改）
- 本文件**不含**产品代码/审计脚本改动；`scripts/audit/**`、`docs/synova/audit-reports/**` 零触碰

---

## 7. 附带发现：D708 合并写集 gate 对本 PR 的 D# 误判（命令 + 输出 + 成对反例）

> 路由（lead 裁决 1，2026-09-26）：不进本 PR 修（gate 属所有权门禁域，且 `merge_writeset_gate.py` 不在本卡写集）；作为**新 FIX 候选**上报 CTO，标注**与 FIX-011 同类**（所有权门禁假阳性）⇒ 按「同类第 2 次及以上」🔴 升级创始人由 CTO 落台账。
> 本 PR 的应对 = gate **自有文档**的修法 ③（PR 正文 `## 写集豁免` 逐条带理由，逐条打印），非静默绕过；声明位置见 PR #811 正文 `## 写集豁免` 段。

### 7.1 本地复现 CI 同口径命令（原样可重跑）

```
$ python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
    --branch fix/fix014-vitest-preexisting-red
── merge-writeset-gate (D708) 合并级写集对账 ──
❌ 结论: block — 检测到 5 个写集外文件（夹带）
   任务: D945 | 分支: fix/fix014-vitest-preexisting-red
   D# 推断来源: commit-subject → D945
   变更集: 6 个文件（merge-base ef299746）
   …（按 D945 的 14 条声明写集判定）
   夹带文件 5 个（不匹配任何声明项）:
     - .claude/task-briefs/2026-09-26-FIX-014-Vitest-既有红-棘轮登记.md
     - docs/synova/coordination/FIX-014-Vitest既有红-处置卡-20260926.md
     - docs/synova/product-lines/evidence/FIX-014-20260926.md
     - memory/notes/proposed/2026-09-26-fix014-vitest-preexisting-red-ratchet-disposition.md
     - scripts/control-tower/ci-red-baseline.txt
EXIT=1
```

**根因**（代码级，`merge_writeset_gate.py:137-152` docstring 自述）：`infer_did` 顺序 = 分支名 → 提交 subject 回退；本 PR 分支名/subject **均无 D#**（FIX-### 不占 D# 号段）→ 回退**向前遍历到 main tip 的 `fix(D945)…` 提交**（`ef299746`）→ 误认任务 D945。**D814（改用 `--first-parent` 只扫分支自身提交）仍未落地**（`task-state/D814.json` status=claimed / impl=null）。

### 7.2 成对反例（同一 HEAD，仅换 PR 正文可得）

```
# ⒜ 不带 PR 正文（等效 CI 里 PR 正文不可用）→ block
$ python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
    --branch fix/fix014-vitest-preexisting-red
❌ 结论: block — 检测到 5 个写集外文件（夹带）
EXIT=1

# ⒝ 带 PR 正文（含 `## 写集豁免` 5 条带理由）→ pass
$ python3 scripts/control-tower/merge_writeset_gate.py --base origin/main --head HEAD \
    --branch fix/fix014-vitest-preexisting-red --pr-body /tmp/fix014-pr-body.md
   豁免 6 条（显式，逐条打印理由）:
     · scripts/control-tower/ci-red-baseline.txt   ← [declared] FIX-014 的登记目标本体（…）（PR 正文声明）
     · …（共 5 条 + builtin bypass.log）
EXIT=0
```

⇒ 判别性成立：**同一变更集**在「有无逐条豁免声明」两种输入下 rc 0/1 对立；同时证明该 gate 对无 D# 卡（FIX-###）**恒误判任务**，属结构性缺口。
