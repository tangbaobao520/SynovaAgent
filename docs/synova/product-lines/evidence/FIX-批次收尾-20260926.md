# FIX 批次收尾（2026-09-26）— 小队自验口径

> **作者**：fix-verifier（独立自验成员，本批只读写集 = `docs/synova/product-lines/evidence/**` + `/tmp`）
> **载体**：PR #810 `chore/fix-verify-evidence`（证据分支；产出者自开）
> **结论性质**：本件全部为**「自验结论」/「可提请独立审计」**；**K3 复审通过前不得合并**；**永不使用「审计通过」**（审计结论只认 K3）。
> **证据规则**：本件每条结论均挂**原始输出位置**（命令 / 证据件路径 / API URL）；**禁无证据结论**。
> **本件写作纪律**：凡我未独立实测的，显式标「未独立核（转述 lead 口径）」；凡我自己更正过的口径，在 §5 显式列出。
> **🔄 round-3/4 补记（2026-09-26 追加）**：§1.2 FIX-001/FIX-013、§2 PR 快照、§3.2/§3.3、§4 N6/N10 已更新至实况；**新增 N11（D331 两层结构性发现）**、**N12（🔴 CI 事实停摆）**。补记处均带「🔄」标记，便于 K3 分辨原始判定与后续进展。

---

## 1 逐条判定（本批 9 条 + 〇-补证 5 条）

### 1.1 〇-补证五条（task-6，证据件 `docs/synova/product-lines/evidence/FIX-006-016-补证-20260926.md`，commit `81604d54`）

| 条 | 判定 | 关键原始输出 | 未清项 |
|---|---|---|---|
| **FIX-006** | **部分复现** | 一次性 clone 内 `rc=0 / elapsed_sec=3.042`；**真仓只读复刻** `alloc-task-id.sh:279-300` 扫描循环 → `worktrees=297 / task-state文件数=87130 / 扫描循环耗时=91.956s`；「字符串累加 5 处」**不可复现**，真·循环内累加 **1 处**（`:295-296`） | 真仓 exec 未跑（会烧真号）；「worktree task-state 未进拒绝面是否致撞号」未做判别性实验 |
| **FIX-007** | **复现** | `loop-context.sh:87` 判据 = `grep -c "$TODAY"`（数**任意**含今日日期的行）⇒ 反例 A1：3 条正常 `COMMITTED`（`grep -c detected-bypass` = 0）→ **熔断 `rc=1`**；A0/A2 对照绿；A3 陈旧 `possible-bypass` 同样计入 | `pre-commit-check.sh` 侧消费链未做行为级验证（仅静态读解，已标注） |
| **FIX-008** | **复现（推翻 CTO「读取非改写」）** | 一次性 clone 内跑测试：`git status --porcelain` **2 行 → 4 行**；`M docs/synova/founder-alerts.md` + `M docs/synova/founder-console.html`；sha256 `11eaf191…` → `e1318e00…`；写点 `founder-truth.test.sh:33`/`:75` → `founder-truth.py:294`/`:418`（`:76-77` 只是断言） | 「生成内容为何与库内版本不一致」未定位 |
| **FIX-015** | **未复现（作为生产违规）** | CI job log `108143405913` 第 1068/1076 行原文 `as any…: 9 处` / `DiagnosticModule…: 3 处`，**逐字复现**；12 处全量 file:line 分类 = **A 6 / C 6 / B（`src/` 生产真红）0**；根因 = 检查**自伤**（`as any` 链无白名单；`DiagnosticModule` 链 `grep -Ev` 排除的是含路径字面量的行 ⇒ 形同虚设） | 修法方向属建议；`soft_check()` `head -8` 致第 9 行 CI 不可见 |
| **FIX-016** | **部分复现 / 定位 = 无法确认** | CI 双平台 `结果: 23 通过, 1 失败`（ubuntu `108143405907` / windows `108143406027` 同数）；本地 **main `ef299746` = 38/0、#803 `31971419` = 24/0**；main 自身 CI `36146340288` 双平台绿 ⇒ 本 PR 引入；**结构性不可定位**：`ci.yml:285-287` `> /tmp/ct-out.log` + `tail -8` + `cut -c1-450`，`artifacts total_count = 0`，`grep-oP-regression` 在 job log 中**仅 3 处**（零逐断言输出） | **未闭环**；未改夹具（守红线） |

### 1.2 本批 9 条（含交付/复审）

| 条 | 判定 | 依据（我的独立实测） | 未清项 |
|---|---|---|---|
| **FIX-001** | 🔄 **可提请独立审计（判据 1–5、7 通过）** | 本体**已完成并落版**：`fix/d970-bypass-ledger-stage2` = **`d38eeabe`**（A 跨域拆件 `367e4304` → B 最小步 `ea5b9d46` → 第二半 `d38eeabe`）。第二半实测：改动仅 1 测试文件 `+48/−41`、**零产品代码改动**；夹具 **13/0 EXIT=0**；D734 `EXIT=0`（① `12 ≤ 12`｜② 单域 mac 11 件｜③ 落后 0）；PR #799 body 含 **6 行 before/after 对照表**（逐条与真实 diff 一致；C2 改测「append 失败 exit 2」、C1 `:75-76` 同时罩 `.sessions/` 与 `scripts/`）；**⒝ 两向变异由我自做** MUTANT-A **8/5**、MUTANT-B **12/1（`重复登记 1→2`）**，**零差异还原**（sha256 已核）；⒞ 声明「对齐已裁 Stage-2 语义、非消红」+「K3 前不得合并」 | **仅差 CI 结论**（镜像 run `36168998716` 全 queued）；裁决 C（win，未独立核） |
| **FIX-002** | **闭环（小队范围）· 可提请独立审计** | 新 clone（origin=GitHub）`bash scripts/control-tower/check-bypass-log.sh origin/main` → **rc=0**，tip `f4da7a82`，对账集合 17 条（10 有记录 / 7 D451 豁免 / **0 缺**）；成对反例：删 `88d53d3a` 记录行 → **rc=1**，还原 → rc=0 且 `git status` 空；补记行与 per-session 原文 **sha256 双端一致** `06b162b2…` | 台账判据须显式 base（本地路径 clone 陷阱）；端态待 #798/#799 合后复测 |
| **FIX-013** | **闭环 · 可提请独立审计** | 活点三件套：⒜ `ci.yml:111`（`iron-laws:`，无 `needs`，`SYNO_DIFF_BASE/SYNO_CI` 在 `:121-122`）｜⒝ 我的独立 awk 变异（删 111–123）→ **4 通过/5 失败 EXIT=1** 且 `❌ ⒜ 活点缺失`｜⒞ **head 级绿 run** `36165068449` / job `108170700199` = `Iron Laws (gate semantics)` **success**，run+job **双层** `head_sha=1f130703`（防顶包通过；`0d22dbab` 的绿 run 是另一独立 run） | 🔄 tip 已前移至 **`145e00dc`**（merge `ee721b0a`）；**`145e00dc` 上 head 级 CI 尚无结论**（run `36170457536` 全 queued）⇒ **上轮 `1f130703` 的绿证据不覆盖新 tip**；`quality` 内 step 重复；D708 栈式口径（`ci.yml:102 --base origin/main` ⇒ 84 文件变更集） |
| **FIX-014** | **可提请独立审计** | PR #811 @ `31bc0e6c`，behind 0 / mergeable=True；棘轮文件 `+3/−2`、6 条数据行**零删除**、`expires`/`first_seen` 逐字未变；三夹具 (a) `EXIT=0` /(b) `EXIT=1` 且 violation 含 `disposition_due` 硬门原文 /(c) `EXIT=1`；两测试 **58/0**、**21/0**（修前 51/7、16/5） | 声称 tip `4e45ffd1` 已过时（现 `31bc0e6c`）；`docs/synova/audit-reports/` 收紧后 150 文件不再自动豁免 |
| **FIX-015** | **补证闭环 + 修复已由 CI 实证归零** | 分类表（见 §1.1）；修复后 CI job 日志 `108170700199` **945 行** `✅ 禁止 DiagnosticModule…`、**950 行** `✅ as any / as never / as unknown as 零容忍（铁律38）` | 与 FIX-013 同属 #816，随其合并 |
| **FIX-016** | **未闭环（结构性不可定位）** | 同 §1.1；另证 `grep-oP-regression` 在双 job log 中**各 3 处**、`❌` 8 处**均属别的测试**，`artifacts total_count = 0` | **处置建议**：立卡修两条截断（`ci.yml:287` + `soft_check()` `head -8`）+ 上 artifact；**未改夹具**（守「定位前不得改」红线） |
| **FIX-006** | **可提请独立审计** | PR #821 @ `966a6374`，behind 0 / mergeable=True；计时对照 **97.5s → 7.1s**，两次均输出 `D1009`（同输入同输出）、真仓 `task_state` 零污染；拒绝面 ⑥ 成对反例（D8888→rc=0 / D9999→rc=1 点名 `worktree-task-state`，旧版 rc=0 漏判）；我的 mutant（⑥ 外层条件 → `if false`，diff 仅 1 行）→ ⒝ 重新漏判；夹具 **58/0**，跑后仓库干净 | 同步前曾有 union 假冲突（**该因果已由我撤回**，见 §5-6） |
| **FIX-007** | **可提请独立审计** | PR #818 @ `9a5eb564`，behind 0 / mergeable=True；修后 **11/0**、修前对照（head 测试 + main 脚本）**7/4**；四例对照（⒜ 3×COMMITTED 旧 rc=1 误熔断 → 新 rc=0；⒞ 旧计 **7 次** → 新计 **3 次**）；我的 mutant（pattern 还原 `$TODAY`）→ ⒜ 立即误熔断；测试在 `mktemp` 沙箱内跑（跑后仓库 `porcelain` = 0） | 声称「修前 8/3」**不可复现**（我 7/4）；该测试含状态依赖断言（见 §4-N8 同族） |
| **FIX-008** | **可提请独立审计** | PR #817 @ `5ebafe78`，behind 0 / mergeable=True；14 个 `grep` 模式**机械比对**：旧/新集合差 = `set()` / `set()`（**pattern 与预期一字未动**）；`git status --porcelain` 前=0 后=0；沙箱反例 0→1；我的变异体（生成器 6 处「诚信账本」→ 不存在串）→ **10/1 EXIT=1**；缺省不设 env 跑 `--html` → 落 `$REPO/docs/synova/`（生产行为不变） | 声称「修前 8/3」不可复现（我 7/4）；两条「既存红」在 head 上已转绿，台账表述需更正 |
| **FIX-004** | **可提请独立审计** | PR #812 `b7f39e1d` / #813 `af095d29`；修前/修后 6 例 EXIT 对照（C2/C3/C4 旧版 `pass` = 静默豁免口子 → 新版 `block`；C5 旧版 `block` 假红 → 新版 `pass`；C6 非 R 不放行）；`merge_writeset_gate.test.sh` **58/0**（修前 51/7）、`doc-dup-rule.test.sh` **21/0**（修前 16/5）；豁免机制判别性：删 1 条 → block 且点名该文件 | **残余面**：`R097` 改名 + 注入内容仍 `pass`（`archive/**` 豁免 type-blind + content-blind）⇒ 建议另卡 |
| **FIX-011** | **已让位** | **未独立核（转述 lead 口径）**：CTO 线 #815/#819/#820 在飞，**本小队零改动** | — |

---

## 2 批次级：PR 清单与合并顺序

**本小队产出（查询时刻快照 2026-09-26；head 在本 session 内多次前移，合并前须重取）**：

| PR | 分支 | head（快照） | 状态 | 说明 |
|---|---|---|---|---|
| **#810** | `chore/fix-verify-evidence` | `7565d3b9` | open | **本件载体**（证据分支，产出者自开） |
| #811 | `fix/fix014-vitest-preexisting-red` | `31bc0e6c` | open / blocked | FIX-014 |
| #812 | `fix/fix004a-writeset-exempt-abuse` | `b7f39e1d` | open / unstable | FIX-004 A（**stacked**，base=`chore/cto-doc-slim-phase1-writeset-exempt`） |
| #813 | `fix/fix004b-doc-dup-rule` | `af095d29` | open / unstable | FIX-004 B/C（**stacked**，base=`chore/cto-doc-slim-phase3-dup-rule`） |
| #816 | `feat/d962-2b-iron-laws-wiring` | 🔄 **`145e00dc`** | 🔄 open / GitHub `mergeable=None`（未重算） | FIX-013 + FIX-015（**stacked**，base=`feat/d962-2a-merge`）；🔄 **已 sync main `ee721b0a`**：2 处 add/add **按 main 侧**对齐（sha256 与 main 逐字一致）、纯 merge 2 parents、P1 修复 sha256 零改动、**behind=0**、**fast-forward 非 force** |
| #817 | `fix/fix008-test-isolation` | `5ebafe78` | open / blocked | FIX-008 |
| #818 | `fix/fix007-loop-context-breaker` | `9a5eb564` | open / blocked | FIX-007 |
| #821 | `fix/fix006-alloc-task-id` | `966a6374` | open / blocked | FIX-006 |

**既有（非本小队产出，供并单参考）**：#796 `f4da7a82`（= FIX-002 分支）｜#798 `e1353350`（dirty）｜🔄 #799 **`d38eeabe`**（**draft**，dirty；tip 已两次实测为 `d38eeabe`，**`ea5b9d46` 已过时**，且它是 `d38eeabe` 的祖先 ⇒ 前进非回退）｜#803 `80ce4ec6`（blocked）｜**#804 `29c31c51` — 已 merged**。

**合并顺序（lead 口径）**：
- **`#798 → #799 → #796`**（D970 账本出库链）
- **`#804（已合）→ #803`**（D962 链）
- ⚠️ 顺序均为**前置依赖**，不得跳序。

**红线**：**全部「K3 复审通过前不得合并」**。

---

## 3 红线段与红线自检

### 3.1 唯一「转绿」处 = FIX-008 夹具机制修复（非把红变绿）
- **pattern/预期逐字未变**（机械比对：旧/新 `grep` 模式集合差均为 `set()`，共 14 条）
- 变更仅两点：调用形态 `echo X | grep -q P` → `grep -q P <<< X`；HTML 断言目标路径 `$REPO/docs/synova/` → `$OUTDIR/`（**隔离机制本体**，且新增 `[ -s "$OUTDIR/founder-console.html" ]` 加强）
- **变异体证据**：把生成器 6 处「诚信账本」改为不存在串 → **10/1 EXIT=1**（断言随真实行为翻转，非恒真）

### 3.2 红线自检（逐条）
| 红线 | 结论 |
|---|---|
| 禁 `--no-verify` | **未使用** |
| 禁 `git stash` | **未使用**（替代：一次性 clone / `git checkout -f` 还原） |
| 禁 force push | **未使用**（#816 `7fe06f33 → 1f130703` 🔄 `→ 145e00dc`、#796/#799 各次前移均为 fast-forward，已用 `merge-base --is-ancestor` 核） |
| 禁碰 `scripts/audit/**` | **未触碰** |
| 禁碰 `docs/synova/audit-reports/**` | **未触碰** |
| 禁自我审计 | 本件全部为「自验结论」，**未使用「审计通过」** |
| 只读写集 | 仅 `docs/synova/product-lines/evidence/**` + `/tmp` + 自建工作树 `.synova-wt-evidence` |

### 3.3 未清零的红（须随对应 PR 处置，**不由本件代偿**）
1. 🔄 **#816 的 2 处 add/add 真冲突 —— 已解析**：`145e00dc` 按 **main 侧**对齐（两文件 sha256 与 `origin/main` **逐字一致**：`7f42a901…` / `5d5d6d54…`；与合并前 `1f130703` 的 `a12282bf…` / `e6358af7…` 确为不同版本）；纯 merge（2 parents）、`behind=0`、fast-forward。
2. 🔄 **#816 的 head 级 CI 无结论 —— 现已被 N12（CI 事实停摆）阻塞，非我方问题**：run `36170457536`（`145e00dc`）11 job **全 queued**；run `36168998716`（`d38eeabe` 镜像）末次实测 9 job = **queued 7 / in_progress 1 / completed 1（`Test-Kit Architecture Tests (windows-latest)` = success）**，run 级仍 `queued / conclusion=None`。⇒ **未出结论，不得推定**；其上一次红（`0d22dbab`）失败 step = **10 `Merge write-set reconciliation (D708)`**。
3. 🔄 **#816 GitHub 侧未重算**：`head.sha = 1f130703`（滞后于 `145e00dc`）｜`mergeable = None`｜`mergeable_state = unknown` ⇒ **不推定可合**。
4. **FIX-016 的 CI 23/1** 未闭环（结构性不可定位）。

---

## 4 新 FIX 候选（N1–N10）

| # | 候选 | 证据要点 | 建议 |
|---|---|---|---|
| **N1** | **两条截断 + 零 artifact**：`ci.yml:287` `MSG=$(tail -8 /tmp/ct-out.log \| tr '\n' '\|' \| cut -c1-450)`；`pre-commit-check.sh:44` `soft_check()` `head -8` | FIX-016 全量扫描：`grep-oP-regression` 在 job log 仅 3 处、零逐断言输出；`artifacts total_count = 0`；FIX-015 第 9 行 CI 不可见 | 立卡；**红腿告警必须可见** |
| **N2** | 🔴 **D708 对无 D# / `FIX-###` 结构性 fail-closed** | PR #812/#813 用 `fix/fix004*` 分支 ⇒ gate 从 branch 与 subject 都取不到 D# → 回落别卡写集 → 误判夹带（#812 曾 `任务: D945`、#813 亦然）；#816 则 `任务: D962`（**不同回落到不同卡**） | **与 FIX-011 同族 ⇒ 建议升级创始人**；根因 = `merge_writeset_gate.py:409/:144/:182/:195/:198` 的 `commit-subject` 回退跨了分支边界（D814 未落地） |
| **N3** | **`FIX-###` 无取号入口** | 登记处明文「编号用 `FIX-###`，不占 D# 号段」⇒ 治理侧无对应 task-state 载体，写集声明只能走 PR 正文豁免 | 立卡：给 `FIX-###` 一个轻量登记载体（否则每个 FIX PR 都要豁免） |
| **N4** | **per-session 落点不可持久** | `8137999f` 的 hook 原文只存在于 `.synova-wt-fix796/.sessions/1dce9ba8…/bypass.log`，`.sessions/` **gitignored** ⇒ 新 clone 丢失（FIX-002 退回的唯一成因，后以「逐字搬运进 tracked 账本」绕过） | 归 **D970**；**不建议**以「每次补记」长期替代（那正是 D451 补记循环形态） |
| **N5** | **本地路径 clone 的 `origin/main` 陈旧 + D513 反向固化** | 源仓 `refs/heads/main=519edadf`（陈旧 12 天）vs `refs/remotes/origin/main=ef299746`；本地 clone 把 heads 映成 `origin/*` ⇒ 脚本内 `git fetch origin main` **把跟踪 ref 拉回陈旧值** ⇒ 假红 11–12 条 | 立卡（与 FIX-016 base 口径同族）；脚本在 fetch 失败/回退时应显式告警 |
| **N6** | **栈式 PR 零 CI（需 `feat/*-ci-mirror`）** | `ci.yml:3-7` `push.branches=[main, feat/*]` + `pull_request.branches=[main]` ⇒ 覆盖条件 = `(base==main) OR (head 匹配 feat/*)`；#812/#813（base=`chore/*` 且 head=`fix/*`）两者皆不满足 ⇒ 团队手造 `feat/fix004a-ci-mirror` 代跑（各 10 条 check-runs 全 queued）。🔄 **新增维度**：**draft PR 不跑 `pull_request` 事件** ⇒ `#799`（base=main 但 `draft=True`，head=`fix/*`）**每个 tip 零 CI**；已建镜像 ref `feat/d970-bypass-ledger-ci-mirror` @ `d38eeabe`（**新建 ref、非 force**）⇒ run `36168998716`（复测仍 `queued`，9 job 无一完成） | 立卡：把镜像做法写进规范，或扩触发面；**draft 态同样计入覆盖条件** |
| **N7** | **main push 上 D721 放行分支不可达** | `ci.yml:131-157` `BASE=merge-base(origin/main, HEAD)` 在 main push 上恒 = `HEAD` ⇒ `CHANGED` 恒空 ⇒ 存量红放行永不可达 ⇒ main 的 `Vitest (2/2)` 恒红（run `36146340288` 实测） | **未独立核（转述 fix-coder-b + lead 口径）**；建议立卡 |
| **N8** | **`founder-truth.test.sh` 自身红 + 不在 CT 密封清单 + 非 hermetic** | main `ef299746` 上 `结果: 5 通过, 2 失败`（本件作者实测）；`ci.yml` CT 清单中**无该测试**；同一 `main/main` 组态在两个 clone 得 **5/2 vs 4/3**（`北星无对应` 随环境漂移） | 立卡：先消除状态依赖再决定是否入 CI |
| **N9** | **FIX-004 §7 残余面** | `git mv` + 注入 1 行 → git 判 **`R097`** → 新 gate **`pass`**（`archive/**` 豁免只看 R 标签，**type-blind + content-blind**） | 另卡（属判据语义变更，须过 K3） |
| **N10** | 🔄 **#816 的 2 处 add/add 真冲突**（**与 union 无关**）—— **已由 `145e00dc` 解析** | 原证据：`git merge-tree --write-tree ee721b0a 1f130703` → `rc=1`；`CONFLICT (add/add): .claude/task-briefs/2026-09-25-D956-ci-failmsg.md` / `task-state/D956.json`；同时 `Auto-merging .claude/bypass.log` / `.github/workflows/ci.yml` **成功**。🔄 解析后：`git diff origin/main 145e00dc -- <两文件>` = **0 行**，sha256 双端一致 | ✅ **已闭环**（保留作模式记录：add/add 于 sync 时按 main 侧对齐） |
| **N11** | 🔴 **D331 读面随 Stage-2 迁移的「两层」结构性问题**（新增） | **代码层：已迁移** —— `check-bypass-log.sh:30-34` 改调 `bypass-ledger.sh sources`（归档+旧路径+全部 per-session），`:40` 任一来源存在即放行。**证据可达性层：未闭合** —— 干净 clone（`.claude/bypass.log` 已出库=0、`.sessions/` 不随 clone、`docs/authority/bypass-ledger-archive/` 不在本分支）⇒ `bypass-ledger.sh sources` **空输出** ⇒ `check-bypass-log.sh origin/main` **rc=1**「无任何可用来源」。**隐藏变量**：推送能力由**从哪个工作树推**决定 —— `.synova-wt-fix799`（coder-a）本机账本 7 行 D451 补记（`grep -c 补记`=7、`hook 层登记`=0）+ `.sessions` 3 行 ⇒ **9/9 命中**；`.synova-wt-fix796`（coder-b）**0/9**；主工作区 **0/9**。另：`.gitattributes:14` 仍留 `merge=union`（对已出库文件的死声明，随 PR-2 `#798` 线清理） | 归 **D970**（归档在 `#798` 线）；**台账若以「新 clone rc=0」为判据，必须写明端态定义**；与 N4「per-session 落点不可持久」同族 |
| **N12** | 🔴 **CI 事实停摆（新增）** —— **所有 PR 的「CI 全绿」判据当前不可得** | 仓库级实测（`/actions/runs?per_page=60`，**我独立复核**）：**`queued = 28` / `in_progress = 10` / `completed = 22`**。**10 条 `in_progress` 全部远超正常时长**（我方 workflow 正常 <10 分钟）：`36165397656 feat/d979-watermark-gen` 58.1 min｜`36165493739 fix/fix008-test-isolation` 57.2 min｜`36165510847 feat/d979-watermark-gen` 57.0 min｜`36165671710 feat/d979-dispatch-tpl-dir` 55.5 min｜`36166761519 chore/stage0-selfverify-20260926` 45.2 min｜`36167103289` 42.0｜`36167351846` 39.6｜`36167438475` 38.7｜`36167584213` 37.4｜`36167600703` 37.2（**范围 37.2–58.1 min**）。⇒ **挂死 run 占满并发配额 ⇒ 28 条 queued 永不启动**，queued 最久 5 条：`36165558930 fix/fix014-vitest-preexisting-red` **56.6 min**｜`36165766559` 54.6｜`36165869942` 53.6｜`36167717254` 36.1｜`36167977426` 33.7。**我方两条收口 run 亦受此阻塞**（见 §3.3-2）。 | ⚠️ **处置权在 CTO / 账号所有者**：取消挂死 `in_progress` run 或提升并发/配额。**我方不擅自取消他线 run**（避免误伤并行线在途工作）；本件只记录事实与量级，不代偿、不结论 |

---

## 5 台账改写请求（口径更正 6 条，均可复核）

> 以下均为**我独立实测**得出、与既有台账表述不符者。请以本件为准改写台账相应单元格。

| # | 既有表述 | 实测更正 | 原始输出位置 |
|---|---|---|---|
| 5-1 | 冻结前提「worktree 数 = 287」 | **已过时且 live 增长**：开工 289 → 297 → 298 → 309（本 session 内）；凡以 worktree 绝对数为判据的卡面须**固化测量时刻** | `git worktree list \| wc -l`；`git worktree list --porcelain \| grep -c '^worktree '` |
| 5-2 | FIX-016「夹具在 main ✅ + 本地 **24/0**」 | **口径混用**：`24/0` 是 **#803 分支**（`31971419`）的数；**main（`ef299746`）= 38/0** | 两 revision 各自 `bash tests/control-tower/grep-oP-regression.test.sh` |
| 5-3 | FIX-006「字符串累加 **5 处**」 | **不可复现**：真·循环内累加 **1 处**（`alloc-task-id.sh:295-296`）；脚本注释 `:289` 自陈「仅此一处」，与实测一致 | `grep -nE '[A-Z_]+="\$\{?[A-Z_]+\}?[^"]*"' …`（12 行，其中仅 1 处为循环内累加） |
| 5-4 | FIX-008「CTO 实测该测试为**读取非改写**（`:76-77`）」 | **推翻**：`:76-77` 是断言（读）；写在 `test:75`/`:33` → `founder-truth.py:418`/`:294`；跑测试后 `M` 两个 tracked 文件、sha256 变更 | 一次性 clone 内 `git status --porcelain` 前/后对照（2 行 → 4 行） |
| 5-5 | 「#812/#813 = **0** check-runs」（零 CI 触发缺口） | **不成立**：二者各 **10 条 check-runs**（全部 `status=queued`）；缺的是**其真实头分支不触发**，团队以 `feat/fix004*-ci-mirror` 代跑（`event=push`, `head_branch=feat/fix004a-ci-mirror`） | `commits/<sha>/check-runs`；`actions/runs/<id>` 的 `head_branch` |
| 5-6 | （我在本 session 中间轮回报中给出）「#811/#817/#818 的 `dirty` 唯一成因 = `.claude/bypass.log` **union 假冲突**」 | **🔴 由我本人撤回**：`git config --get merge.union.driver` **为空**（union driver 从未在本地生效）；`merge-tree` 对「`dirty` 组」与「`mergeable` 组」**返回完全相同（均 rc=0）** ⇒ **无隔离证据**。同步后四者 behind=0 且 `mergeable=True` ⇒ 阻塞已消除；`.claude/bypass.log` 双改**不是** `dirty` 的充分条件（#812/#813/#816 同样双改却 `mergeable=True`） | `git merge-tree --write-tree ee721b0a <head>` ×5；`git config --get merge.union.driver` |

**另附一条非「更正」的精确化**（供 N6 立卡）：CI 覆盖条件 = **`(base == main) OR (head 匹配 feat/*)`** —— `pull_request.branches=[main]` 过滤的是 **base**。#818（base=main）自然触发 12 条 check-runs（`event=pull_request`）即为反证。

---

## 6 团队运行记录（**未释放**）

| 成员 | 角色 | 状态 | 共享任务 | 工作树 / tip |
|---|---|---|---|---|
| **lead** | 队长（只协调不下场） | running | 派单/裁决/收件闸 | — |
| **fix-coder-a** | 编码（并行 CTO 线） | running | task-1（FIX-002/#796）、task-2（FIX-013/#803）、task-3（FIX-001/#799） | `.synova-wt-fix796` / `.synova-wt-fix803` / `.synova-wt-fix799`；tip 见 §2 |
| **fix-coder-b** | 编码（控制塔 + Mac 线） | inactive（本 session 末） | task-4（FIX-014）、task-5（FIX-004）、task-7（FIX-011）、task-9/10/11/12（FIX-006/007/008/015） | 各任务自建工作树；tip 见 §2 |
| **fix-verifier**（本件作者） | **独立自验（只读）** | running — **未释放** | **task-6（〇-补证，completed）**；另承接 lead 逐 PR 自验派单（FIX-002/014/004/013+015/008/006/007/001-B + #816 head CI） | `.synova-wt-evidence`（`chore/fix-verify-evidence` @ `7565d3b9`）+ `/tmp/verif-fix014`、`/tmp/verif-fix002` 等一次性 clone（均 `dirty=0`） |
| — | — | — | **task-8（FIX-016）冻结**：解冻条件 = 定位到具体断言 **或** lead 明确改判 | — |

**编制**：队长 + 2 编码 + 1 自验（≤4，符合 squad-discipline §16）｜**自验未兼任编码**｜重型验证（vitest/全量门禁/门禁自测）全程**串行**并抢 `/tmp/synova-heavy.lock`，每次跑完 `rmdir`。
**收尾状态**：**无锁残留**；两个一次性 clone `dirty=0`；**真仓 `task-state` 零污染**。

---

## 7 尾注

- 本件所有条目均为**「自验结论」/「可提请独立审计」**，**≠ 审计通过**；**K3 复审通过前一律不得合并**。
- 本件**永未使用且不会使用「审计通过」**表述（审计结论只认 K3 报告）。
- 本件**未改动任何产品代码 / 测试夹具 / 断言 / CI 配置 / `scripts/**`**；唯一写入 = 本文件（+ `post-commit` auto-hook 自动追加的 `.claude/bypass.log` 登记行，治理产物）。
- 查询时刻：**2026-09-26**；head / mergeable / behind 均为**快照值**，合并前须重取。
