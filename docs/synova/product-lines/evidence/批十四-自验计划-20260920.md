# 批十四 · 队内自验计划（阶段 1，编码未交付时预先登记）

> 自验队员（独立于三名编码，不写产品代码）｜2026-09-20｜工作树 `.synova-wt-team14` 分支 `team/win-batch14`
> 基线 = `d527abcb`（main + 派单件）；`git status --porcelain` 实测仅 1 个 untracked（冲突扫描件）
> **本文件是"预登记"**：所有判定规则、命令、反向验收做法**在交付前写死**，避免事后改口径。
> 结论措辞只允许两种：`可提请独立审计` / `退回（附理由）`（M3）。
> 各卡证据落盘（M5，按任务号命名）：`D820-verify-20260920.json`、`D822-verify-20260920.json`、`D823-verify-20260920.json`、`D829-verify-20260920.json`

---

## 〇、基线回归档（我实测，作为交付后的对照）

命令（workdir = `.synova-wt-team14`）：

```bash
node_modules/.bin/vitest run tests/store/session-event-log.test.ts tests/routes/diagnosis-report-persistence.test.ts tests/routes/conversations.test.ts tests/routes/ga-calibration.test.ts
```

原始输出摘要（2026-09-20 实测）：`Test Files 4 passed (4)` / `Tests 68 passed (68)` / `Duration 6.01s`。
→ **交付后这 4 个文件必须仍全绿**；任何一个转红且不是交付方声明过的"缺陷固化测试被改写"，即进入退回审查。

四条禁止的物理核验（每卡必跑）：

```bash
git status --porcelain                                   # 越写集文件 = 退回
git diff --name-only HEAD                                # 逐文件比对卡面 write_set
git diff HEAD -- vitest.config.ts .github/               # 非空 = 放宽排除清单 = 退回
git diff HEAD -U0 -- src/ | grep -nE "^\+.*(NODE_ENV *===? *'test'|process\.env\.VITEST|isTest|__TEST__)"   # 测试专用开关 = 退回
```

**逃生舱预登记规则（铁律 38 / CT-46）**：`as any` / `as never` / `as unknown as`
① 出现在**本卡 diff 触及行**（`git diff HEAD -U0 -- <file> | grep '^+'`）→ **退回**；
② 仅**存量未触及行** → 记入证据并披露行号，不单独作为退回依据；
③ 但若交付方**声称"文件内 = 0"而实测非零** → M2（声称 vs 事实）→ **退回**。

---

## 一、A1 · D820（删会话级联 + 报告列表过滤）

### 基线事实（我实测）
| 项 | 位置 | 实测 |
|---|---|---|
| `deleteSession` | `src/store/session-store.ts:355-358` | 只删 `agent_messages` + `agent_sessions` |
| `deleteDiagnosisCheckpoints` | `:581-583` | **全仓零调用者**（`grep -rn "deleteDiagnosisCheckpoints" src/ tests/` = 仅定义行） |
| `listDiagnosisReports` | `:598-639`（SQL `:604`、`:607`） | `WHERE phase = 5`，**不 join `agent_sessions`** |
| 生产调用方 | `deleteSession` ← `src/routes/sessions.ts:117`、`src/l1/im-inbound.ts:232`、`src/l4/data-purger.ts:291`；`listDiagnosisReports` ← `src/routes/diagnosis.ts:947`（`GET /api/diagnosis/reports`） | 链路真实 |

### ⚠️ 预登记风险 R-D820-1（最重要，交付方最可能踩）
`diagnosis_checkpoints.session_id` 在 **phase=5 行里存的是 reportId（`rpt_…`）而不是 sessionId**：
- consult 路径：`src/routes/diagnosis.ts:600-601` → `sessionId: reportIdForArchive`
- 对话桥路径：`src/routes/conversations.ts:307-308` → `sessionId: conversationReportId`
- 只有 phase<5 行是真 sessionId：`src/agent/diagnosis-launcher.ts:166`、`src/services/graceful-shutdown.ts:137`
- 既有 `tests/routes/diagnosis-report-persistence.test.ts` **不建** `agent_sessions` 行（`grep -n "createSession\|agent_sessions"` = 0 命中）

→ **若"报告列表加会话存在性过滤"被字面实现为 `session_id IN (SELECT id FROM agent_sessions)`，D593 全部报告行会被过滤掉**，`tests/routes/diagnosis-report-persistence.test.ts` 用例 1/5/8 转红。
→ 我的判定：**用例 1/5/8 转红 = 退回（修复把"删会话不删报告"换成"报告列表永远空"，是另一种假绿）**；正确解法必须能把 `phase=5` 归档行与会话建立可判定的关联（例如级联时按 partial_report/写入键回收，或在写入侧登记会话键），而**不是**盲 join。若交付方为此改写了 `diagnosis-report-persistence.test.ts` → **退回**（该文件不在 D820 写集内）。

### 验收点 → 可判定命令
```bash
# A1-1 级联接线（谁调用它）
grep -rn "deleteDiagnosisCheckpoints" src/store/session-store.ts
sed -n '350,370p' src/store/session-store.ts

# A1-2 端到端级联（真实 store）
node_modules/.bin/vitest run tests/store/delete-session-cascade.test.ts

# A1-3 列表 API 真入口（不是只查库）——测试内必须有 express + listen(0) + fetch
grep -n "listen(0)" tests/store/delete-session-cascade.test.ts
grep -n "fetch(" tests/store/delete-session-cascade.test.ts
grep -n "api/diagnosis/reports" tests/store/delete-session-cascade.test.ts

# A1-4 回归门（D593 报告列表不被误伤）
node_modules/.bin/vitest run tests/routes/diagnosis-report-persistence.test.ts tests/routes/conversations.test.ts tests/store/session-store.test.ts
```

**反向验收做法（我亲自复现）**：抽掉 `deleteSession` 内新增的检查点删除调用（还原为基线 2 行）→ `delete-session-cascade` 必须报红 → 还原 → 全绿。两次原始输出都入证据。

---

## 二、A2 · D822（降级信号自遮蔽）

### 基线事实（我实测）
| 项 | 位置 | 实测 |
|---|---|---|
| 缺陷点① | `src/store/session-store.ts:377-383` | 成功分支 `:382 this.lastDegraded = false` → 同轮后一次成功抹掉前一次失败 |
| 缺陷点② | `src/routes/conversations.ts:249`（user 写）/`:255`（assistant 写）/`:260`（**只查一次**） | 两次写之后才判 `store.lastDegraded` |
| 固化缺陷的旧测试 | `tests/store/session-event-log.test.ts:146-166` | 名"降级信号非粘滞…重置为 false"，**:164 断言 false** = 缺陷本身（卡面写 :146-164，实际到 :166，行号窄不算造假，但要确认改的就是这段） |
| 其他消费方 | `src/orchestrator/session-manager.ts:86`（会话落库断言） | **修法不得破坏**；`tests/orchestrator/session-manager-eventlog.test.ts` 必须仍绿 |
| 既有路由用例⑭ | `tests/routes/conversations.test.ts:600-617` | 用"全轮所有 insert 都失败的 trigger"→**两次都失败**，所以检查时仍 true，**这正是缺陷被掩盖的原因**；该文件**不在 D822 写集内**，交付若改它 = 越写集 → 退回 |

### 验收点 → 可判定命令
```bash
# A2-1 实现是否真透传（不是加布尔别名）
sed -n '360,395p' src/store/session-store.ts
sed -n '245,266p' src/routes/conversations.ts
git diff HEAD -U0 -- src/store/session-store.ts | head -60

# A2-2 新测试（非对称场景：一次失败 + 一次成功 → 帧仍发）
node_modules/.bin/vitest run tests/store/degraded-signal-not-masked.test.ts
grep -n "STORE_DEGRADED" tests/store/degraded-signal-not-masked.test.ts
grep -nE "RAISE\(ABORT|WHEN NEW" tests/store/degraded-signal-not-masked.test.ts   # 非对称注入手段
grep -n "listen(0)\|fetch(" tests/store/degraded-signal-not-masked.test.ts        # 是否真穿路由

# A2-3 旧固化测试是否"真被改写"（不是删掉、不是恒真）
git diff HEAD -- tests/store/session-event-log.test.ts
git show HEAD:tests/store/session-event-log.test.ts | grep -c "it(" ; grep -c "it(" tests/store/session-event-log.test.ts

# A2-4 回归门
node_modules/.bin/vitest run tests/store/session-event-log.test.ts tests/routes/conversations.test.ts tests/orchestrator/session-manager-eventlog.test.ts
```

**DSH 借鉴核查（队长给的行号，我独立核对）**：`dsh-session-stats/lib/index.js:11`（step 计数放 finally → 四种结束都计数）、`:129`（steps+1）、`:142-148`（结算吐累计值）。
→ 核要点：交付方是否**真的改成"每轮独立事件/累计"**（失败不会被后续成功覆盖），还是只加了个布尔别名/只在路由里加一个局部变量**却没让失败可观测**。

**反向验收做法**：把 `:382` 的 `this.lastDegraded = false;` 还原（即恢复"成功即清零"）或抽掉新增的轮次级累计 → `degraded-signal-not-masked` 必须报红 → 还原 → 全绿。

---

## 三、B · D823（im-inbound 调不存在的方法）

### 基线事实（我实测）
| 项 | 位置 | 实测 |
|---|---|---|
| 缺陷点 | `src/l1/im-inbound.ts:195` | `(conv as unknown as { addToHistory?: (r,c)=>void }).addToHistory?.(…)` |
| 该方法是否存在 | `grep -rn "addToHistory" src/` | **全仓仅此 1 处** → `ConversationEngine` 无此方法 → 可选链静默空转 |
| 可用真实 API | `src/agent/conversation-engine.ts:833-847`（`static fromState(provider, state, wiring)`，内部 `engine.messages = [...state.messages]`）、`:819 serialize()`、`:516-518 getMessages()` | — |
| 存量逃逸舱 | `:126-127 as unknown as Record`、`:183 as never`、`:189 as unknown as Parameters<…>`、`:195`（本卡目标） | 见 §〇 规则 |

### ⚠️ 预登记风险 R-D823-1
`fromState` 直接覆盖 `messages`、**不注入 system prompt**（构造器 `:412` 才建 system prompt）。
→ 若修复改走 `fromState` 却不补 system prompt，第 2 轮上下文会丢掉诊断行为基线 ⇒ 我核**第 2 轮 provider 入参 `messages[0].role === 'system'`**；缺 system = 退回（新引入的静默降级）。

### 验收点 → 可判定命令
```bash
# B-1 假动作是否清掉
grep -rn "addToHistory" src/ --include=*.ts        # 期待 0 命中
sed -n '185,205p' src/l1/im-inbound.ts             # 必须是真实 API（fromState/serialize/公开方法）

# B-2 第 2 轮含第 1 轮（真断言）
node_modules/.bin/vitest run tests/l1/im-inbound-history.test.ts
grep -n "chatCalls\|messages" tests/l1/im-inbound-history.test.ts
grep -n "vi.mock" tests/l1/im-inbound-history.test.ts    # 只允许 mock provider/detect/config，禁 mock 引擎/管线
grep -n "toContain\|expect(" tests/l1/im-inbound-history.test.ts | head -20

# B-3 diff 触及行逃逸舱
git diff HEAD -U0 -- src/l1/im-inbound.ts | grep -nE "^\+.*(as any|as never|as unknown as)"
```

**反向验收做法**：把 `:195` 还原为缺陷写法（或抽掉历史注入）→ `im-inbound-history` 必须报红 → 还原 → 全绿。

---

## 四、C · D829（@synova/evolution feedback-collector 接线）

### ⚠️ 基线前提纠正（我实测，**与冲突扫描 §四 不一致，请队长裁决**）
冲突扫描称 `packages/evolution/src/feedback-collector.ts:45 collectFeedback` **全仓零调用**。**该说法不成立**：

| 事实 | 证据（基线实测） |
|---|---|
| `@synova/evolution` 的 `collectFeedback` **已有 1 处生产调用** | `src/routes/chat.ts:70-71`：`const { collectFeedback } = await import('@synova/evolution'); collectFeedback({orgId, actionId, decision, reason}).catch(() => {})` |
| 该路径**已挂载、可达** | `src/server.ts:28` import + `src/server.ts:367 app.use(chatRoutes)`；`POST /api/proposal/:id/resolve`（`src/routes/chat.ts:56`） |
| 包导出**早已存在** | `packages/evolution/src/index.ts:50-54` 已 `export { collectFeedback, getFeedbackByAction, getFeedbackByOrg }` |
| 真实缺口（我判定） | ① 结果**不可查**：`getFeedbackByAction`/`getFeedbackByOrg` 在 `src/` **零消费**；② `chat.ts:70` 那处**结果被丢弃**（`.catch(() => {})` + `catch { log.debug }`，无 degraded 传播） |

→ **推论**：D829 验收点②（"grep `from '@synova/evolution'` 在 src/ 命中 ≥1 生产调用点"）**在基线上即近似满足**（动态 import 已存在），**不能作为"新接线"的证据**。我核验时**只认**：新增调用点的 `file:line` + 该点被触发的真实业务路径 + **结果可查**的真断言。

### 验收点 → 可判定命令
```bash
# C-1 新增调用点（file:line）+ 指向的是不是 @synova/evolution（防同名混淆 src/growth/feedback-collector）
grep -rn "collectFeedback" src/routes/ga-calibration.ts
grep -rn "@synova/evolution" src/routes/ga-calibration.ts
grep -rn "from '@synova/evolution'" src/ --include=*.ts | grep -v test    # 基线 0

# C-2 真实路由 → 收集器可查（端到端）
node_modules/.bin/vitest run tests/routes/ga-calibration.test.ts
grep -rn "getFeedbackByAction\|getFeedbackByOrg" tests/ | grep -v "tests/evolution/feedback-collector.test.ts"

# C-3 既有 GA 校准回归（用例⑥ 含 feedback_log 回流断言）
node_modules/.bin/vitest run tests/routes/ga-calibration.test.ts tests/evolution/feedback-collector.test.ts
```

**反向验收做法**：抽掉新增的 `collectFeedback` 调用 → 新测试必须报红 → 还原 → 全绿。

**D829 红线（验收点④）**：交付文档/卡面不得出现"越用越准 / 进化能力"类宣称 ⇒ `grep -nE "越用越准|进化能力" <交付文档/卡面>`，命中即退回。

---

## 四-b、DSH 借鉴参照物**独立性核验**（队长给了绝对路径与行号；我亲自打开核对，防"编造参考"）

包：`/Users/wane/.nvm/versions/node/v24.19.0/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-session-stats/lib/index.js`（实测存在，**177 行**）

| 队长给的行号 | 我实测所见 | 判定 |
|---|---|---|
| `:11` | 注释原文："`step/end` … is the step lifecycle authority: the loop appends exactly one per entered step, **in a `finally`, so completed, failed, cancelled, and max-tokens steps all land one**" | ✅ 真实（原文位于 :9–:15 段） |
| `:30-36` | `sessionStatsSchema` **8 字段**：turns/steps/llmMs/toolMs/ttftMs/ttftSteps/decodeMs/decodeTokens | ✅ 真实 |
| `:94` | `llmMs: state.llmMs + Math.max(0, event.time - open.startTime)` | ✅ 累加 |
| `:103` | `next.decodeTokens += outputTokens` | ✅ 累加 |
| `:129` | `steps: state.steps + 1` | ✅ 累加 |
| `:142-148` | `view: (state) => ({ turns, steps, llmMs, …, decodeTokens })` | ✅ 吐累计值 |

**→ 该范式的内核 =「每个独立事件都累计一次，结果不由"最后一次成功"决定」。**
D822 核要点（写死）：交付必须让**失败可观测且不被后续成功抹掉**（累计/事件化/轮次内 sticky）。
**只加布尔别名、或在路由里放个局部变量却让失败最终不可见 = 未照范式 → 退回。**

## 四-c、反向验收执行法（**我亲自抽、亲自看红、亲自还原**，并证明还原后字节一致）

```bash
# ① 备份 + 指纹（还原后的物理一致性证明）
cp <目标文件> /tmp/verify-bak-<卡号>-<文件名>
shasum -a 256 <目标文件> /tmp/verify-bak-<卡号>-<文件名>      # 交付态指纹，入证据
# ② 抽掉修复（edit 工具，绝对路径；一次只抽一处）
# ③ 跑该卡主命令 → 必须报红（记录失败用例名 + 报错原文）
# ④ 还原：cp /tmp/verify-bak-<卡号>-<文件名> <目标文件>
shasum -a 256 <目标文件>                                     # 必须等于 ① 的指纹
# ⑤ 重跑 → 必须全绿
```
实测：`/tmp` 可写、`/usr/bin/shasum` 存在。
**红线遵守**：仅在反向验收窗口内改动交付文件，还原后指纹必须一致；不使用 `git stash` / `--no-verify` / force push；不改产品逻辑（只做"抽掉—还原"，不代改）。

## 五、每卡三问自查的核对方式（不代答，只核）

| 问 | 我怎么核 |
|---|---|
| ① 谁调用它？ | 交付方给的 `file:line` **我亲自 sed 打开看**，并在 `src/` 里独立 grep 调用方；「应该会被调用」= 退回 |
| ② 有没有一条测试穿过真实实现？ | 读测试：必须有 `expect()` + 真实 store/真实 express（`listen(0)`+`fetch`）/真实引擎；`vi.mock` 只准 provider/detect/config；mock 掉被验对象本身 = 退回 |
| ③ 删掉会坏什么？ | 我做反向验收（抽掉修复 → 报红），答不出 + 抽掉不报红 = 退回 |

## 六、退回触发清单（预登记，命中任一即退回）

1. 用 grep 命中当"完成"；用"文件已创建/表已建/字段已加/函数已被调用"当验收
2. `src/**` 出现"测试专用开关"（`NODE_ENV==='test'` / `process.env.VITEST` 等分支）
3. 为变绿放宽排除清单（`vitest.config.ts` / CI exclude 被改）
4. 证据文件未按任务号命名（M5）
5. 本卡 diff 触及行出现 `as any` / `as never` / `as unknown as`（铁律 38 / CT-46）
6. 声称与实际不符（M2）：含"声称跑过但改口"、声称零逃生舱而实测非零
7. 越写集改动（尤其：D822 动 `tests/routes/conversations.test.ts`、D820 动 `tests/routes/diagnosis-report-persistence.test.ts`、D829 动 `src/store/**`）
8. 反向验收未真做，或抽掉修复后测试**仍绿**（= 测试没锁住缺陷）
9. 引入被禁止的替代品：删掉旧固化测试而非改写；把断言改成恒真
10. 命中 §一 R-D820-1（报告列表被盲 join 清空）或 §三 R-D823-1（第 2 轮丢 system prompt）

## 八、队长裁定回执（阶段 1 收口，2026-09-20）+ 由裁定派生出的**三条新增必核点**

队长裁定（已采纳为准入判据）：
1. R-D820-1 口径确认：**D593 用例 1/5/8 转红 = 退回**；交付若改写 `tests/routes/diagnosis-report-persistence.test.ts` = 越写集 = **退回**。
   **A 的批后解法**：`diagnosis_checkpoints` 增 `origin_session_id` 列（对话桥写入时记链）；删侧双删；读侧谓词
   `phase = 5 AND (origin_session_id IS NULL OR EXISTS (SELECT 1 FROM agent_sessions s WHERE s.id = diagnosis_checkpoints.origin_session_id))`。
2. D829 验收点② 重定：只认「新增调用点 file:line + 真实业务路径 + **结果可查真断言**」；不认 grep ≥1 生产调用点。
3. 计划文件保留在 `evidence/`；最终交付件：每卡 `D8xx-verify-20260920.json`（M5 按任务号）+ 一页结论 `docs/synova/coordination/批十四-自验结论-20260920.md`（队长已扩写集覆盖该文件）。
4. D823 逃生舱：`as unknown as` 在 **diff 触及行 + `:195` 所在函数体**内必须为 0（硬）；存量 `:126-127`/`:183`/`:189` 二选一（清理 or 披露），**不清理不退回但必须披露**；声称"文件内 = 0"而实测非零 → M2 → 退回。

### 新增必核点 N1（D820）：`origin_session_id` 必须有**生产写入者**，否则整卡是"字段已加"式假绿
裁定后的解法只在"写链"存在时才有效。若该列**只有 DDL + 测试**在写、生产路径永远写 NULL，则删侧永远删不到、读侧谓词恒真 ⇒ **旧缺陷原封不动**（正是四条禁止里的"字段已加当验收"）。
```bash
grep -rn "origin_session_id" src/ --include=*.ts    # 必须出现：① DDL ② ALTER 迁移 ③ delete 级联 ④ 列表谓词 ⑤ **生产写入点（对话桥 src/routes/conversations.ts）**
grep -rn "origin_session_id" src/routes/conversations.ts   # 生产写链（不是只在 tests/ 里写）
grep -rn "origin_session_id" tests/                        # 若仅测试写 = 退回
```

### 新增必核点 N2（D820）：**存量库迁移**——`CREATE TABLE IF NOT EXISTS` 不会给已存在的表加列
`src/store/session-store.ts:266` 是 `CREATE TABLE IF NOT EXISTS diagnosis_checkpoints` ⇒ 只在 CREATE 语句里加列，**老库（data/synova.db）不会有该列** ⇒ 任何引用它的查询抛错 ⇒ `listDiagnosisReports` catch → `ok:false` → `GET /api/diagnosis/reports` **503 STORE_UNAVAILABLE**（桌面恢复入口坏）。而全新内存库（既有测试全用它）**照样全绿** ⇒ 假绿。
```bash
grep -n "ALTER TABLE diagnosis_checkpoints" src/store/session-store.ts   # 必须有幂等迁移（先例：:275 user_id、:319 title）
```
**实证做法（需一个真断言，不接受只看代码）**：手工造"无该列"的旧库 → `new SessionStore(db)` → 断言该列存在且 `listDiagnosisReports()` 返回 `ok:true`。
（旁证：`tests/e2e/checkpoint-e2e.test.ts:14-21` 自己手搓了 `diagnosis_checkpoints` 表且**不含**新列——它只用裸 SQL、不调 SessionStore，所以不会红；这也说明"手搓旧表"的形态在本仓库是存在的。）

### 新增必核点 N3（D820）：读侧谓词对 NULL 行必须**照旧列出**（两个方向都不能假绿）
- 方向一（不能变空）：`origin_session_id IS NULL` 的行（D593 全部用例 + 全部存量旧行）必须仍被列出 ⇒ `tests/routes/diagnosis-report-persistence.test.ts` 用例 1/5/8 全绿。
- 方向二（不能没修）：新写的、`origin_session_id` 指向**已删除会话**的报告行必须查不到。
```bash
node_modules/.bin/vitest run tests/routes/diagnosis-report-persistence.test.ts   # 方向一
node_modules/.bin/vitest run tests/store/delete-session-cascade.test.ts          # 方向二（须含"未删会话仍可见"反向用例）
```

### 补充事实（供队长做波 2/其他卡参考，**不属本队四卡**）
`src/agent/diagnosis-launcher.ts:162` 的 `saveCheckpoint` 是**只定义、零调用**的死函数（`grep -rn "saveCheckpoint" src/` 仅 :162 一处）⇒ 生产上 `diagnosis_checkpoints` 的 phase=5 行**只有** `src/routes/diagnosis.ts:600`（consult）与 `src/routes/conversations.ts:307`（对话桥）两个写入者，**且两者都把 reportId 当 session_id 写**——这正是"会话↔报告无链接"的根因，也是 A 新增 `origin_session_id` 的必要性来源。另：`:858` 的报告冷读是 `getDiagnosisCheckpoint(reportId)` ⇒ **键语义（reportId 作键）是承重的，不得改**。

## 十、折入项：卡外阻塞缺陷 FTS 同步触发器（队长 2026-09-20 裁定折入本批，**独立 commit、独立可审**）

### 10.1 我已**独立复现基线缺陷**（不采信转述；基线 DDL/触发器原样复刻后实测）
```
[baseline] messages = 1 fts = 1
[baseline] DELETE agent_messages -> FAIL: SQL logic error
[baseline] DELETE agent_sessions -> FAIL: SQL logic error
[baseline] after: messages = 1 fts = 1          ← 删不掉
[baseline] FTS search hits after delete attempt = 1   ← 已"删除"的内容仍可召回（隐私面）
```
复刻依据：基线 `agent_messages_fts` 是**普通** FTS5 表（`session_id UNINDEXED, content, tokenize='unicode61'`，无 `content=''`），而 `agent_msg_fts_delete` 用的是 **contentless/external-content 专用** 的 `INSERT INTO ...(fts, session_id, content) VALUES('delete', old.session_id, old.content)`。
⇒ 队长描述成立；`DELETE /api/sessions/:id`（`src/routes/sessions.ts:117`）在有消息的会话上必然 500。

### 10.2 我对**修复后工作树**的独立实测（非交付方自述）
| 场景 | 命令/脚本 | 实测结果 |
|---|---|---|
| 全新库删会话 | `/tmp/verify-fts-migration.ts`（fresh 段） | deleteSession → OK；`messages=0, fts=0, search hits=0` ✅ |
| 旧库迁移（旧触发器+已有消息+**人为 rowid 漂移**） | 同上 legacy 段 | 插入触发器 SQL 变为含 `rowid`；删除触发器变为 `DELETE ... WHERE rowid = old.id`；FTS rowid 由 `[2,100]` → `[1,2]`（对齐）✅ |
| 幂等（只跑一次） | 迁移后插入孤儿 FTS 行 `rowid=999`，再 `new SessionStore(db)` | 孤儿仍在 ⇒ **第二次未重跑重建** ✅ |
| 旧库迁移后删除+隐私 | `/tmp/verify-fts-legacy-clean.ts` | 删前 search hits=1 → 删后 `messages=0, fts=0, search hits=0` ✅ |
| 列定义未动（条件 1/2） | 工作树 `:221-225` | 仍为 `session_id UNINDEXED, content, tokenize='unicode61'`，**无 `content=''`** ✅ |

### 10.3 交付时仍需核的（我自己的原话，非转述）
1. **commit 顺序与独立**：`git log --oneline` 必须出现「FTS 修复」commit 在 D820 commit **之前**，且单独成 commit（卡外改动单独可审）。
2. **条件 6（迁移路径必须有测试）**：`grep -n "CREATE TRIGGER IF NOT EXISTS agent_msg_fts_delete" tests/**` 与 `grep -rn "agent_messages_fts" tests/` → 交付必须有一条**手搓旧触发器/旧库**的用例；**若只用全新内存库 ⇒ 退回**（新库全绿、老库坏，与 N2 同型）。
3. **条件 4（真实入口）**：有消息的会话 → `DELETE /api/sessions/:id` 返回 **200**（走 `src/routes/sessions.ts:117`；该文件不在 A 写集内 ⇒ A 只能用测试穿它，不得改它）。若交付只在 store 层断言、无 HTTP 层用例 ⇒ 该条未穿真实入口。
4. **条件 5（隐私面）**：交付用例须断言删除后 `SELECT COUNT(*) FROM agent_messages_fts` = 0 **且** `search()` 召回 0（我刚已独立实测通过）。
5. **不得顺手改无关逻辑**：`git diff <FTS-commit>` 只应含 FTS 触发器 + 迁移 + 其测试（若含 `diagnosis_checkpoints`/`origin_session_id` 等 D820 内容 ⇒ 混提交 ⇒ 退回）。
6. 结论中**单列一节**，**不并入 D820 的验收叙述**（队长要求：该缺陷须可被 K3 单独审）。

> 观察（非退回项，供队长判断）：修复块未包 `BEGIN/COMMIT`（DROP 成功但 CREATE 失败会留下"无触发器"窗口）。与同文件 `:249-272` session_events 迁移的 BEGIN/COMMIT 先例不一致；风险低（下次启动会重建），记录备查。

## 十一、措辞纪律（M3）

- 我只会说：`可提请独立审计` / `退回（附理由）`。
- 禁止出现"审计通过 / 审计合格 / 已通过审计"。
- 我不改产品代码；发现缺陷 → 写退回理由 → 交队长裁决。
