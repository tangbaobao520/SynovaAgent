# 第一章 条件A 部署独立审计

> commit: `431e16b` | 2026-08-01 | 证据日志: `_tools/evidence-log.txt`
> 工作区说明：`.codex/signals/` 有 13 个未提交删除，审计以工作区+git HEAD 双核对，结论不受影响

## 完成度估计：85%

## 一、证据表（证据强度：S 强 / M 中 / W 弱）

| # | 审计项 | 三态 | 证据强度 | 证据（文件:行号） |
|---|--------|------|:---:|------------------|
| A1 | 客户自安装 4 步向导 | ✅ | S | `app/js/setup.js:2-12`（D283：Welcome→Configure→Test→Done 四步 STEPS）；`app/setup.html`、`app/css/setup.css` 存在 |
| A2 | GA 到期日管理 UI | ✅ | S | `app/js/admin.js:103-115`（D281：date input + `PUT /api/enterprise/ga-access/:token/expiry`） |
| A3 | 权限检查接线 | ✅ | S | `src/middleware/rbac.ts:135,201,214`（isGaContractExpired/canAccessWorkspace）；调用方 `workspaces-api.ts:130-132`、`server.ts:19,208` |
| A4 | 多用户存储 | ✅ | M | `src/growth/user-store.ts` 被 `routes/auth.ts`、`routes/enterprise.ts`、`synova-agent.ts` 消费 |
| A5 | 租户/企业隔离 | ⚠️ | M | `src/routes/enterprise.ts:41-89`（orgId 字段 + getOrgId）；DB 查询级 orgId 过滤覆盖范围未全量验证 |
| A6 | 前端服务 | ✅ | S | `src/server.ts:278-279`（`app.use('/app', express.static(...))` + `/` → `/app/index.html`） |
| A7 | 安装脚本 | ✅ | S | `scripts/install.sh`、`install.ps1`、`setup.ps1`、`setup-env.ps1` 存在 |
| A8 | 桌面端 | ✅ | M | `electron/main.cjs`（package.json `electron:dev`）+ `electron-main.ts` + `src/notifications/electron-adapter.ts` |

## 二、缺口清单

| 缺口 | 四态 | 说明 |
|------|:---:|------|
| A-G1 多租户隔离强度未全量验证 | ⚠️ | orgId 在路由层存在，DB 查询层过滤覆盖范围未逐表核查（待查线索，不计入正式结论） |
| A-G2 安装引导(D283) 与 admin onboarding(D246) 双入口 | ⚠️ | 两个引导流程并存，潜在 UX 冲突 |
| A-G3 AGENTS.md 市场定位 5-300 人 vs 实际 5-1000 | 📄 | 文档滞后，待更新 |

## 三、演示相关性

安装向导、登录、管理后台为演示可能触达环节，均已有真实实现（A1-A3/A6 S 证据）。若演示流程包含"客户自安装→配置→登录"，该链路可走通。

## 四、对抗性质询

问：多租户隔离未全量验证是否应降为 P0？答：隔离强度属于安全纵深，演示无法从外部观察单租户数据越权；不满足 P0 的"演示可见度"条件，维持 P1 候选。
