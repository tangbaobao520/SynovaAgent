# 第二章 条件B 诊断自主审计

> commit: `431e16b` | 2026-08-01 | 证据日志: `_tools/evidence-log.txt`

## 完成度估计：78%

## 一、证据表

| # | 审计项 | 三态 | 证据强度 | 证据（文件:行号） |
|---|--------|------|:---:|------------------|
| B1 | 6 阶段诊断管线 | ✅ | S | `src/agent/diagnosis-launcher.ts:77-124`（phaseLabels、phase_started 事件、每阶段检查点、NRA 安全门禁） |
| B2 | 对话/诊断入口接线 | ✅ | S | conversation-engine 被 `diagnosis-launcher.ts:80`、`src/l1/im-inbound.ts:170-180`、`tool-loop-executor.ts` 消费 |
| B3 | 哨兵体系 | ✅ | S | `src/sentinel/sentinel-loader.ts:56-92` 扫描 `extensions/sentinels/`（45 个含 manifest 的哨兵模块、56 aggregates）；`src/deploy/bootstrap.ts:368-397` Phase 2a 启动接线 |
| B4 | 信号聚合 | ✅ | M | `src/sentinel/signal-aggregator.ts` 被 `sentinel-service.ts`、`runner.ts`、`server.ts` 消费 |
| B5 | 专家体系 | ✅ | S | `expert/expert-registry.yaml` v2.0（7 位：host+5 cycle+tech）；7 位专家 RULES/THEORY/TOOLS/IDENTITY/PROMPT 齐全非空 |
| B6 | 循环调度 | ✅ | S | `loop-scheduler.ts` 接线 `synova-agent.ts` + `bootstrap.ts:513-520`（registerDefaultLoops）；`main-agent.ts:270-283` selectHandler：loop-1→诊断(带任务分解)、loop-2→导航、loop-3/5→进化、loop-6→溢出、**loop-4→默认诊断处理器（无自检专属逻辑）** |
| B7 | 方向监测接线 | ❌ | S | `direction-monitor.ts` 全 src 无生产调用（仅自身 + 测试 `tests/loops/direction-monitor.test.ts`） |
| B8 | 中层进化接线 | ❌ | S | `middle-evolution-engine.ts` 全 src 无生产调用（仅自身 + 测试） |
| B9 | NCI 非共识检测 | ❌ | S | `src/` 无 nci/non-consensus 相关代码 |

## 二、关键发现：哨兵数量之谜（已解开）

文档称"50 哨兵/20 适配器"，`src/sentinel/adapters/` 仅 5 个文件（4 个哨兵 + helpers）。真相：

- **真实生效主体** = `extensions/sentinels/` 文件驱动体系（47 目录中 **45 个含 manifest**、56 aggregates），由 `sentinel-loader.ts` 启动加载，`bootstrap.ts` 接线
- `src/sentinel/adapters/` 的 4 个是 D1-D4 时代遗留的内置适配器，由 `builtins.ts` 自动注册
- 文档"50"≈45 扩展哨兵 + 4 内置哨兵（49）的统计口径差异 → 📄 文档口径需更新，但**机制真实且量级一致**

## 三、缺口清单

| 缺口 | 四态 | 说明 |
|------|:---:|------|
| B-G1 direction-monitor 未接线 | ❌ | 权威02 核心，有完整实现+测试，生产零调用（"写了没接"典型） |
| B-G2 middle-evolution-engine 未接线 | ❌ | 07-16 修正方案核心（循环七/GA缺位保护），零生产调用 |
| B-G3 NCI 零代码 | ❌ | 研究完成，实现为零 |
| B-G4 loop-4(系统自检) 无专属处理器 | ⚠️ | `main-agent.ts:283` selectHandler 默认 fallback：loop-4 路由到 `defaultDiagnosisHandler`，自检循环无专属实现（S 证据）；loop-5 路由到 evolution handler |
| B-G5 文档哨兵口径滞后 | 📄 | "50 哨兵/20 适配器" vs 47 扩展 + 4 内置 |

## 四、演示相关性

诊断管线（B1-B5/B6）为演示主链路，S 证据已通。**若演示叙事宣称"Agent 会自主进化/越用越准"，B-G1/B-G2 将直接暴露**；若不展示进化叙事，则不触达。
