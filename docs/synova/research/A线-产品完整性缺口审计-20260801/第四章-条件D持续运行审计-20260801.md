# 第四章 条件D 持续运行审计

> commit: `431e16b` | 2026-08-01 | 证据日志: `_tools/evidence-log.txt`

## 完成度估计：85%

## 一、证据表

| # | 审计项 | 三态 | 证据强度 | 证据（文件:行号） |
|---|--------|------|:---:|------------------|
| D1 | Cron 持久化 | ✅ | S | `src/cron/scheduler.ts:131-153`（cron_jobs 表 + trigger_type/event_types 列）、`:230` INSERT OR REPLACE、`:393-395` persistRun |
| D2 | 重启恢复 | ✅ | S | `synova-agent.ts:78-79`（RestartRecovery.recoverInterruptedSessions） |
| D3 | 优雅停机 | ✅ | S | `synova-agent.ts:39-40` 初始化 + `:277` drain |
| D4 | 循环心跳 | ✅ | S | `loop-scheduler.ts:71-72`（heartbeat.json）+ `:103-106` 每日 stalled 检查 |
| D5 | 故障恢复服务 | ✅ | M | `src/services/fault-recovery.ts`、`config-recovery.ts`、`stuck-session-detector.ts` 存在 |
| D6 | 资源监控 | ✅ | M | `resource-monitor.ts`、`memory-monitor.ts` 存在 |
| D7 | 升级检查 | ✅ | M | `update-checker.ts` 被 `routes/health.ts:7` 引用 |
| D8 | 健康自诊断 | ⚠️ | S | `product-health.py`、`self-diagnosis.py` 存在；self-diagnosis 完成度映射有 bug（63 任务全 0.167），已转交开发 |
| D9 | 信号数据源 | ❌ | S | `.codex/signals/` 13 个信号文件工作区缺失（git 有备份），控制塔视图无数据（已转交开发） |

## 二、缺口清单

| 缺口 | 四态 | 说明 |
|------|:---:|------|
| D-G1 静默升级/回滚自动化 | ⚠️ | update-checker 存在（版本检查），权威09 承诺的静默升级+回滚是否自动化未证实（待查线索） |
| D-G2 自诊断测量失真 | ❌ | 完成度引擎批量误判 + 信号文件缺失（已转交，跟踪中） |
| D-G3 heartbeat.json 当前为空 | ⚠️ | 机制存在但未生成实际心跳记录（可能未运行或首次启动未完成） |

## 三、演示相关性

若演示展示控制塔/健康度/门禁面板，D-G2 为 P0（但已转交开发，需在演示前确认修复完成）。重启恢复、停机、Cron 持久化均为 S 证据已通，演示不会暴露问题。
