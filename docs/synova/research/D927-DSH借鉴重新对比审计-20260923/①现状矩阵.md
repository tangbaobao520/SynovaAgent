# ① 现状矩阵 — B-01…B-20 全覆盖

> **断面**：`/Users/wane/src/deepseek-harness-017` @ `00102833`（`0.1.7-alpha.2`）｜**日期**：2026-09-23
> **口径**：`代码在 main` 一律用 `git grep … HEAD` / `git ls-tree HEAD` 判定——工作树有未跟踪文件，用 `ls`／工作树 grep 会把未合并内容算进来。

## 一、已动手的 10 张（B-01…B-10）

| 卡 | D# | 代码在 main | grep 证据（`file:line`） | 审计 verdict | 证据回写 | 今日能否运行 |
|---|---|---|---|---|---|---|
| **B-01** LLM 错误码 | D586 | ✓ | [types.ts:19](/Users/wane/SynovaAgent/src/errors/types.ts) ErrorCode / `:744` normalizeLlmFailure / [base.ts:39](/Users/wane/SynovaAgent/src/providers/base.ts) 接线 | **PASS** | ✗（D806 不含） | **✓** |
| **B-02** 重试配置化 | D593 | ✓ | [retry-middleware.ts:15](/Users/wane/SynovaAgent/src/llm/retry-middleware.ts) / `:44` / `:134` / `:281` | **PASS** | ✓ D806→22-1 | **✓** |
| **B-03** token 四桶 | D598 | ✓ | [token-meter.ts:32-39](/Users/wane/SynovaAgent/src/llm/token-meter.ts) / `:53` / `:236-246` | **`None`** | ✓ D806→21-3（点已绑，**卡未审计**） | **✓** |
| **B-04** 工具结果修剪 | D587 | ✓ | [tool-result-pruner.ts:23](/Users/wane/SynovaAgent/src/llm/tool-result-pruner.ts) / `:37-39` / `:64` | **PASS** | ✓ D806→20-3 | **✓** |
| **B-05** 上下文压缩 | D594 ⚠️ | ✓ | [context-compaction.ts:69](/Users/wane/SynovaAgent/src/agent/context-compaction.ts) / `:102-105` | **`None`** ⚠️ | ✓ D806→20-3 | **✓** |
| **B-06** 超时三层 | D593 | ✓ | [timeout.ts:21](/Users/wane/SynovaAgent/src/llm/timeout.ts) / `:46` / `:65` / `:106` | **PASS** | ✓ D806→20-5 / 22-1 | **✓** |
| **B-07** 会话投影 | D588 | ✓ | [session-projection.ts:61](/Users/wane/SynovaAgent/src/store/session-projection.ts) / `:135` / `:184` / `:200` | **CONDITIONAL PASS（＝未通过）** | ✓ D806→18-2 | **✓** |
| **B-08** 原子写+文件锁 | — | **✗** | `git grep -n -E "writeFileAtomic\|withFileLock" HEAD -- src scripts` → **零命中 exit=1**；全仓 16 行命中**全在 `docs/` 5 文件** | — | ✗ | —（未动手） |
| **B-09** preset=目录 | D599 | ✓ | [config-layers.ts:75](/Users/wane/SynovaAgent/src/config/config-layers.ts) / `:81` / `:114` / `:173` | **`None`** | ✓ D806→25-6 | **✓** |
| **B-10** 配置三层+dump | D599 | ✓ | [customer-config-package.ts:35](/Users/wane/SynovaAgent/src/config/customer-config-package.ts) / `:413` / `:427` | **`None`** | ✗（D806 明文"如实标未计分"） | **✓** |

**落点行数（判空壳）**：794 / 484 / 448 / 447 / 410 / 323 / 299 / 207 / 149 / 142 —— **10/10 ≥142 行，无占位件**。
**落地 commit**（`git log --oneline -3 HEAD -- <path>`）：`56c4dd1d`(B-01) / `14e54722`(B-02+B-06) / `4c74faf0`(B-03) / `75f13b56`(B-04) / `f509b3cf`(B-05) / `eebd9c99`(B-07) / `95a71a8f`(B-09+B-10)，9 个 commit 全部 `git merge-base --is-ancestor … HEAD` 为真。

**⚠️ `task-state/D594.json` 不是 B-05。** 它是 "L1-P2交互卡片链路"、`status=closed`、note 写明"被 D602 取代（L1 审计编号重分配，CT-63）"；而 commit `f509b3cf` 却标 `feat(D594): …B-05` → **编号撞车，B-05 目前零审计承载**。

## 二、未动手的 10 张（B-11…B-20）

> **极易误读**：`src/mcp/`、`src/sentinel/`、`src/growth/` 三个目录**在 main 真实存在且有成体系代码**，但都是**存量 Synova 代码，与 D760/D761/D762 无关**（各自 `git log` 最新提交为 `bd904566 feat(mcp)` / `52126cc9 feat(D752)` / `a32f0971 feat(d702)`，**无一含借鉴卡 commit**）。**目录存在 ≠ 借鉴卡落地。**

| 卡 | D# | 代码在 main | grep 证据 | 审计 | 回写 |
|---|---|---|---|---|---|
| B-11 MCP client | D760 `claimed` | ✗ | [bridge.ts:93](/Users/wane/SynovaAgent/src/mcp/bridge.ts) `class MCPBridge` 存在但**存量** | — | ✗ |
| B-12 webhook | D761 `claimed` | ✗ | `src/sentinel/runner.ts:619` 仅一句 webhook 注释（D17） | — | ✗ |
| B-13 goal 状态机 | D762 `claimed` | ✗ | `git grep -n "foldGoal" HEAD` → 8 处**全在 docs/task-state** | — | ✗ |
| B-14 消息反馈 | D763 `claimed` | ✗ | `git ls-tree HEAD -- src/evolution` → **空返回** | — | ✗ |
| B-15 权限预设 | D764 `claimed` | ✗ | `permissionPreset` 零命中；顶层 `security/` 不存在 | — | ✗ |
| B-16 不可信上下文 | D765 `claimed` | ✗ | `sessionReference` 零命中（`tools.ts:301-317` 的 untrusted 是 Hermes P7 存量） | — | ✗ |
| B-17 skill provider | D766 `claimed` | ✗ | `skillProvider` 零命中 | — | ✗ |
| B-18 不变量注册表 | D767 `claimed` | ✗ | `InvariantRegistry` 零命中 | — | ✗ |
| B-19 持久化分级 | D768 `claimed` | ✗ | tier/分级 在 `src/store`+`src/cron` 零命中 | — | ✗ |
| B-20 速览族六项 | D769 `claimed` | ✗ | 六项：output-retention **0** / repeat-tool-reminder **2**（存量 `src/l3/tool-guard.ts:40,57`，D473）/ scrubbedParentEnv **0** / time-context **0** / session-query-sqlite **0** / session-checkpoint-policy **0** | — | ✗ |

**B-11…B-20 在证据目录完全零记录**：`git grep -l -E "D76[0-9]" HEAD -- docs/synova/product-lines/evidence/` → **exit=1**。

## 三、CTO 起步现状复核（逐条裁定）

| CTO 说 | 实测 | 裁定 |
|---|---|---|
| D586 / D587 / D575 PASS | `audit.verdict` 均为 `"PASS"` | **✓ 符** |
| D588 = CONDITIONAL PASS（＝未通过） | `verdict="CONDITIONAL PASS"`，`fix_task_id=D597` | **✓ 符** |
| D598、D599 = `impl_done` + `audit=None` | 两卡均符 | **✓ 符** |
| B-08 未落（grep 零命中） | `src`+`scripts` 零命中，exit=1 | **✓ 符** |
| B-11 D760 `claimed` | `status="claimed"`、`impl=null` | **✓ 符** |
| G3 证据回写 2/20 | 内容口径 2 个文件 | **✓ 符（须括注）** |

**CTO 未提到、实测发现的 4 条：**

1. **零审计的不止 2 张，是 4 张**：除 D598(B-03)、D599(B-09+B-10) 外，**B-05 也零审计**（因 D594 编号撞车）。
2. **⚠️ `task-state/D594.json` 不是 B-05**（见 §一注）。
3. **G1 全仓口径是 227，不是 199**：`git grep -l -E "deepseek-harness|@deepseek-ai" HEAD | wc -l` = **227**；同命令在 `1062ba2f`（09-22，PR #699）= **199**（CTO 基线逐字复现），此后 **+28/−0**。**这是"文档引用计数"快照，不是桥接计数**——硬守卫成立：`src`+`packages` 的 `@deepseek-ai` = **0 命中**，全仓 **0 条真实 import 语句**。
4. **G3 的 2 个文件里只有 1 个是借鉴卡证据**：`D806-borrow-cards-20260918.json` 是真证据（覆盖 B-02/03/04/05/06/07/09/10，6 个验收点绑定）；`D870-verify-20260921.md` 的 `B-01…B-35` 是**另一套命名空间**（官方 UI patterns 切片 B），只有 `:151`/`:256` 两处真指借鉴卡。

**另两处状态与文档不符（非 CTO 陈述，但影响判断）：**
- **「D769 未登记」已过期**：`task-state/D769.json` 在 main（title="DSH 借鉴卡 B-20…速览族六项…"，`claimed`），加入 commit `e418cddc`（09-20，D842 归档恢复）。`board-backlog.json` 里 `PLAN-d808-b20-unregistered` 仍写"D769 不存在"，**与物理矛盾**。
- **指引 §11.3 的"当前全表 0/20 回写"（`DSH借鉴指引-v2-20260904.md:258`）已被 D806 部分超越但未回改**。

## 四、G3 回写现状（诚实口径）

- 证据目录共 68 个文件；**按文件名**筛 `B-0x/B-1x/B-20` → **0 个**；按内容筛 → **2 个**。
- **真借鉴卡绑定证据 = 1 个**（`D806-borrow-cards-20260918.json`）：绑定 20-3(←B-04+B-05) / 20-5(←B-06) / 21-3(←B-03) / 18-2(←B-07) / 22-1(←B-02+B-06) / 25-6(←B-09)，覆盖 8 张卡；B-10 明文"如实标未计分，不造假证据"。
- **零覆盖清单**：B-01、B-08、B-10，以及 B-11…B-20 全部。
- `scripts/product-lines/evidence-writer.py` 在 main，五个 flag 齐（`:97-104`）；`:59` founder_demo 必须带 quote（防空壳核验）。

## 五、今日能否运行（创始人口径）

**B-01…B-10 十张全部「今日仍能运行」——10 test files / 112 tests 全绿 / 6.36s。**
原始输出见 [附录-冒烟验证原始输出.md](附录-冒烟验证原始输出.md)（含跑在哪个 checkout）。
**→ 不触发 FIX 卡登记**（派单条件"已动手但不可运行"未成立）。

需登记的**不是"不能运行"，而是文档/流程欠账**——见 [④处置建议.md](④处置建议.md) §三。
