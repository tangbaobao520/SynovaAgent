# ② 锚点重核表 — 旧锚点（npm tarball）→ 新锚点（`0.1.7-alpha.2` 源码树）

> **断面**：`R = /Users/wane/src/deepseek-harness-017` @ `00102833`｜**日期**：2026-09-23｜**只读**
> **判定四值**：**命中**（符号都在，位置可能变）／**位移**（符号仍在但文件或行号变）／**被上游取代**（旧锚点消失，功能由别处承担 → 必给新承担者）／**已消失**（找不到等价物）。
> **纪律**：行号只对 `00102833` 成立。按 **D797 锚点三原则**，落码时只锚 **包 + 文件 + 符号**，行号**现查**。
> **引用纪律**：源码目录名无 `dsh-` 前缀 → 按 npm 包名 `find`/`grep` 全失效，须走 `package.json` 的 `name` 反查；`lib/` 是被 gitignore 的本地构建产物，grep 须限定 `src/`。

---

## B-01 LLM 稳定错误码 taxonomy — `packages/llm/llm/src/`

| 旧锚点（npm tarball） | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `dsh-llm lib/types/error.js` 四 canonical 码 | `error.ts:25` `CONTEXT_WINDOW_EXCEEDED_CODE` / `:28` `QUOTA_EXCEEDED_CODE` / `:39` `EMPTY_RESPONSE_CODE` / `:48` `INVALID_CREDENTIAL_CODE` | 四码俱在 | **位移** |
| 设计注释 "route on this, never by parsing message" | `error.ts:14`（逐字一致） | 无 | **命中** |
| （旧卡未列）第 5 码 | `error.ts:172` `IMAGE_OFFLOAD_REQUIRED_CODE` | **新增**；生命周期 = 码 → `compaction/summary-error` → `compaction-image-offload/src/index.ts:33` | 新增 |
| `isXxxError` **守卫族** | `isContextWindowExceededError:80`、`isQuotaExceededError:94`、`isHarnessError:161` | **只有 3 个，且前两个是 `(detail: string)` 正则分类器**——与同文件 `:14` 的 "never by parsing message" **恰好相反** | **已消失（口径错）** |
| （旧卡隐含）`isEmptyResponseError` / `isInvalidCredentialError` | **零命中** | **`src/` 与 `lib/` 双侧为零**；扩展普查 `isRateLimitError`/`isServerError`/`isTimeoutError` 同样为零 → **非摇树造成** | **已消失** |
| `lib/types/adapter-failure.js` `normalizeLlmFailure` | `adapter-failure.ts:16`（**不在 `error.ts`**） | 已加固（防 SDK accessor trap） | **位移** |

```bash
grep -n "CONTEXT_WINDOW_EXCEEDED_CODE\|QUOTA_EXCEEDED_CODE\|EMPTY_RESPONSE_CODE\|INVALID_CREDENTIAL_CODE" \
  "$R/packages/llm/llm/src/error.ts"
grep -rn "isEmptyResponseError\|isInvalidCredentialError\|isRateLimitError" "$R/packages/llm/llm/src" "$R/packages/llm/llm/lib"   # → 零
```
**★ 照卡抄会抄错**：卡面"守卫族"应改写为 **「canonical 码（出站路由）＋ 正则分类器（入站归类）」两层**，后者必须按"必然有误判率"对待。

## B-02 重试策略配置化 + 投影化 — `packages/llm/llm-retry/src/` + `packages/llm/llm/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `inject=["agents","sessionProjections"]` | `llm-retry/src/index.ts:22`（**逐字一致**） | 无 | **命中** |
| 退避 `initialDelayMs*2^(retry-1)` 截 maxDelayMs ± jitter | `llm-retry/src/index.ts:59-64`（`:61` exponential、`:62` jitter） | 无 | **位移** |
| **先 append 意图 → 等待 → append started** | `:188` append(`llm/retry`) → `:189` await → `:190` append(`llm/retry-started`) | **顺序逐行一致** | **命中** |
| `dsh-llm lib/types/retry-policy.js` `RetryPolicySchema`、`maxRetries=5` | `llm/src/retry-policy.ts:14` `DEFAULT_MAX_RETRIES = 5`、`:18-24` `DEFAULT_RETRYABLE_CODES`、`:100` `RetryPolicySchema` | 词表一致 | **位移** |

**⚠️ 移植前必测**：`always` 无界模式的真实行为（旧卡未提，成本不可控）。
**⚠️ 旁证**：`MAX_TIMER_DELAY_MS` = `util/timeout/src/index.ts:25` = `2_147_483_647`，而 `maxDelayMs` 默认 10s → **该 schema 上限形同虚设**。

## B-03 token 计量四桶 + 成本护栏 — `packages/llm/token-meter/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `lib/types/estimate.js` `CHARS_PER_TOKEN=4` + block overhead 4 | `estimate.ts:13` / `:16` | 无 | **位移** |
| （旧卡未列） | `estimate.ts:19` `ROLE_OVERHEAD = 4`（**exported**） | **新增** | 新增 |
| 四桶 `uncachedInput/output/cacheRead/cacheWrite` | `usage-projection.ts:14-19`、`bucketsFrom:21` | 无 | **位移** |
| plan/commit 两段式 | **`surface-fold.ts:107` `planSurfaceTokens` / `:136` `commitSurfaceTokens`** | 无 | **位移** |
| `projectedTokens` 外推 | `projection.ts:69/73/215` | 无 | **位移** |

**★ 两处卡面错误（照卡抄会抄错）**：
1. **plan/commit 不是"成本两段式"**，是 **surface 折叠的异常安全事务对**（`surface-fold.ts:7-9` 逐字：read-only plan ＋ in-place commit）。成本护栏由 `projectedTokens`/`pressureTokens` 承担，**是不同机制**。
2. **`projectedTokens` 不可用于中文计费或硬闸门** —— DSH 自述 estimator **系统性低估 CJK** 并已将其**排除在 occupancy 之外**（`projection.ts:54-56`）；`CHARS_PER_TOKEN=4` 是拉丁文密度假设。

## B-04 工具结果修剪器 — `packages/compaction/compaction-tool-result-pruner/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `PRUNE_MARKER`（旧记 tarball L8） | **`config.ts:7`** | 值逐字一致；**旧记 L8 → 实为 L7** | **位移** |
| `DEFAULTS{thresholdChars:8192, headChars:4096, tailChars:1024}` | `config.ts:10` 声明、`:11/12/13` 值 | 三值逐字一致 | **位移** |
| `codePointLength` 码点计量 | `config.ts:27` | 无 | **位移** |
| "replay-safe" 派生式修剪 | `index.ts:2` ＋ `:145-150` ＋ `:165-171` | 无 `replay-safe` 字面量，语义等价 | **位移** |

**★ B-04 ↔ B-03 在 0.1.7 硬耦合**：修剪器 `static inject = ['tokenMeter']`（`index.ts:47`），且每次发 `compaction/prune` 影子价格（`:160-164`）→ **两卡不能拆开移植**（旧卡当独立卡写）。

## B-05 上下文压缩引擎四件套 — `packages/compaction/compaction-basic/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| threshold 0.8 / retain 0.16 | `config.ts:20` `DEFAULT_THRESHOLD_RATIO` / `:23` `DEFAULT_RETAIN_RATIO` | 无 | **命中** |
| 事件 `compaction/start\|summary\|end` | `region.ts:210` / `:491` / `:237,245` | 无 | **命中** |
| `shadowedTokenCount` 影子价格 | `compaction/src/types.ts:40/88/119` | 无 | **命中** |
| KV-cache 前缀复用 | `summarizer.ts:30` | 无 | **命中** |
| 恢复臂 `CONTEXT_WINDOW_EXCEEDED → 强制压缩 → retry` | `index.ts:194` | 无 | **命中** |
| 〔映〕09-07 表把它记在 `dsh-compaction lib/types/` | `compaction/`（无后缀）**是抽象 seam**：`package.json:3` 自述 "Abstract compaction service seam"；只含 `abstract class CompactionEngine:119` ＋ 三个事件类型 `types.ts:24/34/72`，**无 config/region/summarizer** | 实体是 `compaction-basic`（`index.ts:113` `BasicCompactionEngine extends CompactionEngine`） | **归属错误已纠正** |

**→ 旧卡归属（`compaction-basic`）正确；错的是 09-07 映射表。**
**副产品（建议进路线图）**：seam/实体分离 ＋ `summarize()` 是官方明示的 "sole subclass customization hook"（`index.ts:236-246`）→ **0.1.7 给我们的真实可插拔缝隙。**

## B-06 协作式超时三层 — `packages/util/timeout/src/` + `packages/guard/timeout-policy/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `MAX_TIMER_DELAY_MS=2147483647`（tarball L27） | `util/timeout/src/index.ts:25` | 无 | **命中** |
| `clampTimeout`（tarball L43） | `:45` | 无 | **命中** |
| `deadline`（tarball L57） | `:91` | 无 | **命中** |
| `idleWatchdog`（tarball L85） | `:126` | 无 | **命中** |
| `timeoutOf`（tarball L134） | `:184` | 无 | **命中** |
| `TOOL_TIMEOUT`（tarball L80） | `guard/timeout-policy/src/index.ts:25` | 无 | **命中** |
| 超时**返回结果不抛异常** | `timeout-policy/src/index.ts:41` 构造 ＋ `:74` return（`:46` `code: TOOL_TIMEOUT`） | 无 | **命中** |
| **〔映〕"B-06 超时在 `dsh-agent-loop`"** | **两断面一致证伪**：源码树 `grep -ric timeout packages/core/agent-loop/src/*.ts` → **8 文件全 0**（共 2449 行）；tarball `lib/index.js` 1950 行 `-ic timeout` = **0**、`-ic abort` = **98** | 定位不成立 | **已消失（旧定位）** |

```bash
grep -ric timeout "$R/packages/core/agent-loop/src/"*.ts   # → 全 0
grep -rl '"@deepseek-ai/dsh-timeout"' "$R/packages"/*/*/package.json | wc -l   # → 依赖方
```

## B-07 会话投影注册表 — `packages/session/session-projection/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `register({key,stateVersion,init,apply})` | `index.ts:233`/`:253` `register`；`:48` `ProjectionDefinition` | **契约扩张**（`stateSchema` 等） | **位移** |
| checkpoint `{seq, ver, state}` | `:127` `ProjectionCheckpointRow`；`checkpoint():396`，落值 `:400-404` | **字段名变** | **位移** |
| `restoreFloor` | `:425`（ver 不匹配 → floor 0，`:429-431`） | 无 | **命中** |
| ver 不匹配自动全量重放 | `restore:495`、`usable:510`、`from:523` | **三重判定 + `baseSeq>0` 时 throw** | **位移** |

## B-08 原子写 + 文件锁 — `packages/util/atomic-write/src/index.ts` ★未动手、锚点全活

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `writeFileAtomic` | `:78` | 无 | **命中** |
| 随机后缀同目录临时文件 | `:85` `` `${filename}.${randomBytes(6).toString('hex')}.tmp` `` | 无 | **命中** |
| **独占创建 flag** | `:87` `{ mode: options.mode, flag: 'wx' }` | 无 | **命中** |
| rename 提交 | `:88` → `renameAtomicTemp:29` → `rename:33` | 无 | **命中** |
| 权限 `mode` 必填 | `:53` 参数无 `?`；说明 `:45-46` | 无 | **命中** |
| `withFileLock` 跨进程锁 | `:158` | 无 | **命中** |
| 锁文件命名 + 独占 | `:163` `<file>.lock`；`:169` `{mode:0o600, flag:'wx'}` | 无 | **命中** |
| 释放路径（含异常） | `:186-190` try/finally → `:189` `rm(force)` | 异常已覆盖 | **命中** |
| Windows 瞬时 rename 重试 | `:17` `Set(['EACCES','EBUSY','EPERM'])`；判据 `:23-26`；重试 `:29-42` | 无 | **命中** |
| `tests/` | `tests/atomic-write.spec.ts`（**246 行 / 13 例**） | 新增 | **位移** |
| `src/` 外实现（win32.ts） | **无**（仅 tests + 构建产物） | — | **命中（"无 win32.ts"）** |

### ★ B-08 的 39 行差异归因（v1.0 结论的理由必须换掉）

| 证据 | 结果 |
|---|---|
| `diff -q` 源码树 `lib/index.js` vs tarball `lib/index.js` | **IDENTICAL** |
| `md5 -q` 两者 | 同为 **`7a93bb9d98ad09092eb9b46128ba637a`**（均 152 行）→ **两断面发布同一个 JS** |
| 计数闭合 | 191 − 152 = **39 = 24 注释 + 11 空行 + 4 类型擦除**；`lib` 中 TS 类型语法 grep = **0**（exit 1） |
| 符号面 | lib 保留 `WINDOWS_TRANSIENT_RENAME_ERRORS:16`、`renameAtomicTemp:29`、两处 `wx`(`:69`/`:132`)、`:152 export { withFileLock, writeFileAtomic }` |

**→ 纯转译，零逻辑差异。** v1.0 的结论（原子写无变更、1 天量级）**维持**，但**理由必须换掉**：不是"两边都是 npm 产物"，而是"**源码树自带与 tarball 同源的构建产物**"。

**⚠️ 三条警告**：
1. 该对拍**只覆盖 atomic-write**；同批抽样 8 包中 **`dsh-settings` 是真差异**（见 B-10），**不可外推**。
2. **上游自己也不测"并发写零撕裂"**：`tests/atomic-write.spec.ts` 13 例是 **mock `node:fs/promises`**（`:8` lockPermissionFailures、`:9` releaseLockBeforeProbe、`:28` 注入 EPERM、`:62` waitForLock、`:106/124/165/192` Windows 重试与锁争用分支），测各错误码分支的可重试性与清理，**不跑真并发**。
3. → **"1 天"只覆盖"抄机制"，不覆盖"自建并发写验收"。** B-08 卡面验收条件"并发写测试零撕裂"**必须我们自建**，不能引用上游背书。

## B-09 preset=目录 — `packages/preset/` ★★机制已被上游废弃

| 旧锚点 | 新源码 | 语义变化 | 判定 |
|---|---|---|---|
| `COMPOSITION_FILE = 'agent.cordis.yml'`（tarball `discovery.js:35`） | **零命中** | 机制更换 | **被上游取代** |
| `METADATA_FILE = 'preset.yml'`（tarball `authoring.js:19`） | **零命中** | 机制更换 | **被上游取代** |
| 包名 `dsh-agent-presets`（`types/{discovery,mount,authoring}.js`） | 拆为 `@deepseek-ai/dsh-agent-preset-registry`（**9 文件 / 1096 行**）＋ 全新 `@deepseek-ai/dsh-agent-preset`（**30 行**） | 拆包 | **被上游取代** |
| `mountPreset` | `agent-preset-registry/src/mount.ts:254` | **签名改** `(ctx, id, plugins)` | **位移** |
| `leakedServices` 泄漏审计 | `agent-preset-registry/src/mount.ts:85` | 无 | **命中** |

**★ 上游自己的作废声明**（`agent-preset/skills/editing-cordis-compositions/SKILL.md:70` 原文）：

> Before declaration rows, a user preset was a directory `$DSH_HOME/.agent-presets/<id>/` holding `preset.yml` (display `name`, `description`, `order`) and `agent.cordis.yml` (the plugin entry list). **Nothing reads that directory any more.**

**新范式**：Cordis composition 里的 **declaration row**（`agent-preset/src/index.ts:1` "A declarative preset row in an ordinary Cordis composition"；`:12` `class AgentPreset`、`:16-23` Config = `{id, name, description, order, plugins}`）＋ `packages/bundle/web-app/presets/*.patch.yml`。
**保留项**：`mountPreset` / `leakedServices` 的**注册表语义**可续用。
**⚠️ 最薄弱处**：判定依据是**源码树零命中 + skill/note 文本**（`.agents/notes/archived/…` 亦载 "Nothing reads that directory any more"），且**只 grep 了 `.ts`**。若有**非 TS 载体**仍在读 `agent.cordis.yml`，该判定降级为"部分残留"。

## B-10 配置三层 + 可 dump — `packages/settings/settings/src/` + `packages/boot/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `mergeLayers` | `settings/src/index.ts:175` | 无 | **命中** |
| 非 plain object 整值替换 | `:176` `if (!isPlainObject(under) \|\| !isPlainObject(over)) return over` | 逐字一致 | **命中** |
| **namespace 注册制** | **无 `registerNamespace`**；改由 `SettingsForms:223` ＋ `SettingsNamespaceView[]`（`types.ts:63`）＝ **profile entry id + 插件 Config schema 推导** | **机制更换** | **被上游取代** |
| **`patchNode` 注释保留叶子级 patch** | **全树零命中**；tarball 侧在 `dsh-settings-file lib/index.js:53` | 机制更换 | **被上游取代** |
| `dsh lib/dump-config-*.js` CLI `--dump-config` 逐层带来源 | 调用 `apps/cli/src/dump-config.ts:41`；实现 `boot/app-boot/src/index.ts:417`；flag `apps/cli/src/args.ts:170-171` | **位置变** | **位移** |
| `dsh-settings` 服务类名 | `SettingsForms`（`src/index.ts:223`）vs tarball `SettingsProvider` | **跨版本真重命名**（repo lib 544 行 vs tarball 610 行） | **位移** |

**`patchNode` 的新承担者（两条不同语义，别混）**：
- **注释保留叶子级 patch** → `boot/config-editor/src/index.ts:75` `ConfigEditor.edit()`（`:99` `parseDocument`、`:117` `visit`、`:12` `import {isMap,isSeq,parseDocument,Scalar,visit} from 'yaml'`）
- **保住未回传 secret 的稀疏 patch** → `SettingsPathOp:81` ＋ `applyPathOp:86`（动机说明 `:73-80`）

**旁证**：`settings/settings-file` 包**在源码树不存在**；`settings.yaml` 被源码标注为 "the **removed** `settings.yaml`"，由 `importLegacyDocument()` 一次性迁移。
**⚠️**：`dsh-settings` 是抽样 8 包里**唯一**测出真实跨版本差异的包 → **`lib/` 对拍结论不可外推**。

## B-11 MCP client 双 transport — `packages/mcp/mcp-client/src/` ★范式已变

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| stdio / StreamableHTTP 双 transport | `transport.ts:31-45`（stdio `:34`／http `:41`）；消费 `connection.ts:307` | **是官方 SDK 类型化薄封装** | **位移** |
| **（★根基）** 官方 SDK 依赖 | import `transport.ts:9-11`、`connection.ts:18`、`tools.ts:17`；`package.json:42` `"@modelcontextprotocol/client": "2.0.0"`（**dependencies 段**） | — | **位移** |
| **工具表变更通知重载** | `connection.ts:263-269` | **非 DSH 自研**：`listChanged` 是 **SDK 的 `Client` 构造选项**（SDK 类型 `dist/index.d.mts:1716`） | **被上游取代** |
| `scrubbedParentEnv` | 定义 `packages/subprocess/subprocess/src/index.ts:66`；消费 `transport.ts:12`/`:22` | 无 | **位移** |

**★ 直接证据（三段齐全：import 行 + dependencies 声明 + 已安装实体）**：
```bash
grep -rn "@modelcontextprotocol" "$R/packages/mcp/mcp-client/src"*.ts
#  connection.ts:18 / tools.ts:17 / transport.ts:9,10,11
grep -n "@modelcontextprotocol/client" "$R/packages/mcp/mcp-client/package.json"   # → 42: "2.0.0"
```
`transport.ts` 全 46 行里 DSH 自己写的只有 `buildChildEnv`（`:21-23`，9 行），且**自述此事**（`:16-19`）："**The MCP SDK owns the actual spawn**, so this transport shares the scrub definition rather than the spawn path."
→ **"借 transport 无意义"成立。**

**DSH 真正自研的是"重注册的注册表语义"**：`autoRefresh:false` → `onChanged:267` → `refreshTools():296-300` → `enqueueSync` → `syncTools`（`tools.ts:113`，用 `createHash`/`isDeepStrictEqual` 做差异）。
**⚠️ 该条置信度 = 中**（只读 SDK **类型声明**，未读 `dist/index.mjs` 实现体）。建议补证：读 SDK 实现体，约 5 分钟。

**★ DSH 侧没有 MCP server 包**：`packages/mcp/` 恰好 2 个包（`mcp-client`、`mcp-resources`），**全为 client 侧**；全仓 `McpServer` 唯一命中是 `acp/acp/src/mcp.ts:7` 的 ACP **类型**（把 ACP 编辑器传入的 server 声明转成 mcp-client 配置）。
→ **我方 `src/mcp/`（Synova 自己的 MCP server）在 DSH 无参照物**，DSH 在 MCP 上**是纯消费方**。

## B-12 webhook 事件触发路 — `packages/webhook/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| **GitHub HMAC 验签** | `webhook-github/src/handler.ts:5` import、`:101` `new Webhooks({secret}).verify(body, signature)`；`package.json:44` `"@octokit/webhooks": "^14.2.0"`（dependencies） | **完全委托 octokit** | **被上游取代** |
| source / rule / delivery 三 id | `webhook/src/brand.ts:6`/`:9`/`:12`（工厂 `:19`/`:28`/`:37`） | 无 | **位移** |
| delivery 校验 + `deepFreeze` | `webhook/src/index.ts:39`（校验 `:40-51`、`:52`、`deepFreeze:54`） | 无 | **位移** |
| provenance `source:{kind:"webhook",…}` | `webhook/src/session.ts:155-163`（`kind:'webhook'` `:156`）；类型 `types.ts:75` | 字段更全 | **位移** |

**★ 反证（关键）**：`packages/webhook/` 下 `createHmac` / `timingSafeEqual` **零命中**（全仓命中在 speech-to-text / browser-auth / account-platform，与 webhook 无关）→ **零命中 + 单点委托调用 = 验签不可能在 DSH 内完成**。
**→ "需改法"成立、根基钉死：我们要自研验签（不引 octokit 依赖），不能照卡抄。**
**可复用部分集中在 `webhook`（adapter 无关规则运行时）而不在 `webhook-github`。**

## B-13 goal 事件溯源状态机四件套 — `packages/goal/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `foldGoal` | `goal/src/fold.ts:339` | 无 | **位移** |
| `GOAL_CHANGE_VERSION` 投影 | 定义 `goal/src/runtime.ts:8`（**值 = 1**）；拒绝 `fold.ts:136-138`（fail-loud） | 无 | **位移** |
| goal-round-driver 竞态围栏 | `goal-round-driver/src/index.ts:75-76`；`competingQueued:41/86/108/300`；step fence `:243`、`:264` | 无 | **位移** |
| 续轮提示 | `goal-round-driver/src/prompt.ts:12`；校验 `invariant.ts:45`/`:56` | 无 | **位移** |
| `tool-goal` 执行时 authority | `tool-goal/src/authority.ts:48/100/111`；消费 `index.ts:207/229-230/264/271/279/299` | **细化**：二值 `{kind:'direct-human'}` / `{kind:'goal-round',goal}` | **位移** |

## B-14 消息级反馈 sidecar — `packages/feedback/message-feedback/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| **`types/spec.js`（域 spec）** | **不存在**（`src/` 仅 `index.ts` + `types.ts`） | — | **已消失**（**"自始"不可证**，见下） |
| 会话生命周期指纹围栏 `createdAt+cwd` | `index.ts:249-255`，比对 `[header.id, header.createdAt, header.cwd]`（`:250` vs `:251`），`isDeepStrictEqual:249`，不匹配即 `throw:254` | **实为三要素，旧卡漏 `id`** | **位移 + 扩充** |
| （旧卡未提）承担者分工 | 域词汇/TS 类型 ∈ `src/types.ts:14/17/20/139`；**运行时 schema ∈ `src/index.ts:62-73`（zod）** | 旧卡 `types/{spec,types}.js` 二分法在本断面**无对应物** | **位移** |

**★ 撤回 v1.0 的"自始不成立"**：本断面是**单 commit 浅克隆**（`rev-parse --is-shallow-repository` = `true`，`rev-list --count HEAD` = **1**）→ `git log` **不可用作历史证据**。只能证"**本断面不存在**"，证不了"**自始**"。**观测不到 ≠ 不存在。**
**意义**：`id` 参与比对意味着"同 cwd、同 createdAt 但不同 session id"**也被拒**。`:245` doc 说明防的缺陷："Listener participation alone does not prove this Session has a persistence writer."

## B-15 权限预设写穿 — `packages/interaction/permission-presets/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| 预设 = 旋钮打包（sandbox-mode + approval-policy） | `index.ts:2-3` doc；`PresetSpec:64-70` | 无 | **命中** |
| 写穿到会话事件（预设**不成为第二真相源**） | 写穿 doc `:3-5`；预设事件 doc `:53-58`；写侧 `:393/404/435-436/445-448` | **语义成立，但原句非源码原文** | **命中（释义）** |
| knob schema | `sandbox-policy/src/session-mode.ts:42`；`user-approval/src/index.ts:70` | 无 | **位移** |
| fold | `sandbox-policy/src/index.ts:138`；`permission-presets/src/index.ts:124`；`user-approval/src/index.ts:237` | 无 | **位移** |
| setter | 定义 `sandbox-policy/src/session-mode.ts:53`、`user-approval/src/index.ts:100`；预设包仅调用 | 旋钮所有权在 owner 包 | **位移** |

**⚠️ 必须标注的更正**：「预设**不成为第二真相源**」**不是 DSH 源码原文**（`grep second source\|source of truth` 零命中），是旧卡的**释义**。应引实际原文 `permission-presets/src/index.ts:53-58`："Records the selected preset as **durable, log-only user intent**. The **knob events follow in the same turn and control execution**; this event **stays out of the model transcript**…"
**权威归属有行为证据（不只文字声明）**：执行侧读 knob fold（`:344`/`:403`）；setter **定义在 owner 包**，预设包只 import 调用（`:25`/`:30`）→ **要让预设变第二真相源，必须先把 setter 搬进预设包，代码布局使这一步在架构上显眼。**

## B-16 不可信上下文标注 — `packages/context/session-reference/src/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| URI 编解码 | `uri.ts:16` `encodeSessionReferenceUri`；`:36` 规范化回比校验（`throw 'URI is not canonical'`）；`:50` mention 渲染 | 无 | **位移** |
| 大小上限 | `config.ts:4` `MAX_REFERENCES = 3`；`:8` `DEFAULT_MAX_REFERENCE_BYTES = 65_536` | 无 | **位移** |
| 候选**限流** | `config.ts:6` `DEFAULT_CANDIDATE_LIMIT = 50`（import `index.ts:25-27`） | **实为"限件数"，非限流** | **位移（口径）** |
| 不可信模型上下文标记 | `projection.ts:18` "Snapshot data serialized inside the untrusted prompt"；`types.ts:78` "Aggregated untrusted snapshot…" | 无 | **位移** |

**★ 口径更正**：全包**无** `throttle` / `rate limit` / `backoff` 字样 → 卡面"候选限流"应改为 **"候选限件数（上限 50）"**。

## B-17 skill provider 两阶段触发 — `packages/skill/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| dispatcher 主动发现 | `skill-filesystem/src/index.ts:197` 消费、`:723` `discoverRoot` | 无 | **位移** |
| 模型主动拉取 | `tool-skill/src/index.ts:11` import `defineTool`、`:81` `defineTool({…})` | 无 | **位移** |
| seam 契约 | `skill/src/index.ts:230` "One catalog observation plus whether discovery completed within a **stable catalog revision**"、`:234` "…without a concurrent catalog revision"、`:238` "…is **authoritative**"；`:367` `private revision = 0`、`:475` | **新增 stable-revision / authoritative 语义** | **位移** |

## B-18 运行时不变量注册表 — `packages/runtime-diagnostics/invariants/src/index.ts`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| `InvariantRegistry`（旧记 "123 行"） | `:94` `export class InvariantRegistry extends Service`；文件 **200 行** | 123 行是 **tarball 产物**行数；源码 200 行 → 已量化（类型/注释被剥离） | **命中** |
| "检查可选、违约必炸" | doc `:2-3` "Every workspace package registers checks from a `./invariant` companion"；`:25` "Throw a package-attributed invariant failure" | 无 | **命中** |
| `InvariantError` | `:50` `export class InvariantError extends Error` | 无 | **命中** |
| 导出面 | `:200` `export default InvariantRegistry` | 无 | **命中** |

## B-19 持久化分级哲学 — `packages/schedule/` + `packages/jobs/`

| 旧锚点 | 新源码 `file:line` | 语义变化 | 判定 |
|---|---|---|---|
| 提醒/会话走**日志**（冷恢复免费） | `schedule/src/domain.ts:41` "durable Schedule data"、`:47` "durable-log failure"、`:577` "durable mutations **in log order**"、`:624` "Fold the package-owned stream after the durable fork seed boundary" | 无 | **位移** |
| 长跑作业走**内存** | `jobs/src/index.ts:48` "Abstract background job registry"、`:85` `export abstract class JobRegistry extends Service`、`:87-91` **构造即抛错**（"load an implementation such as `@deepseek-ai/dsh-jobs-local` instead"） | 本包**无内存实现** | **被上游取代** |
| — | 内存实体：`jobs-local/src/index.ts:4` "…**in memory** and hands out fresh projections"、`:124` "The **in-memory** `jobs` registry" | — | 新承担者 |

## B-20 速览族六项 — 逐项

| # | 项 | 包 | 新源码 `file:line` | 判定 |
|---|---|---|---|---|
| 1 | 截断诚实披露 | `util/output-retention` | `index.ts:11-15` doc（"`truncated` means … omitted otherwise-available content because of a budget"、"never folded into `truncated`"）；`:52`/`:64`/`:82` `truncated: boolean`；`:139` | **命中** |
| 2 | 重复调用防呆 | `guard/repeat-tool-reminder` | `index.ts:16` context 声明、`:24` `export const name = 'repeat-tool-reminder'`、`:29` thresholds 校验（`<2` 或重复即抛） | **命中** |
| 3 | 敏感环境擦除 | `subprocess/subprocess` | **定义 `index.ts:66` `export function scrubbedParentEnv()`**；擦除规则 `:47` `SENSITIVE_ENV_PATTERN = /KEY\|PASSWORD\|SECRET\|TOKEN/i`；doc `:58-60` **直接点名本卡场景** | **命中** |
| 4 | 时间上下文注入 | `context/time-context` | `index.ts:28` import `formatTimestamp`；`:43` "latest **durable injection**"；`:45`；`:59` 最小注入间隔；`:112` 注入文案；`:170` `injected` | **命中** |
| 5 | 会话 FTS5 全文检索 | `session-query/session-query-sqlite` | `schema.ts:127` `CREATE VIRTUAL TABLE IF NOT EXISTS persisted_docs USING fts5(`；`schema.ts:158` `temp.live_docs USING fts5(`；`index.ts:819` `WHERE persisted_docs MATCH ?`、`:842` `live_docs MATCH`；`query.ts:29` 外层谓词预算 | **命中** |
| 6 | 副作用前检查点 | `session/session-checkpoint-policy` | `index.ts:2` "Semantic durability checkpoints for model requests, top-level tool dispatch…"；`:15` `export const name`；`:18` `inject = ['llm','sessionPersistence','sessions','tools']`；`:22` "A checkpoint rejection prevents adapter dispatch"；`:27` "checkpoints before requesting its first chunk"；`:40`/`:53` | **命中** |

**第 3 项权属澄清**：`scrubbedParentEnv` **定义在 `subprocess`**（`:66`），`dsh-subprocess-local` 等只是**消费方**。定义处 doc 解释为何 MCP client 用**函数**而非 subprocess **服务**："…spawners that cannot route through the service (node-pty backends, **SDK-managed transports**) share the one scrub definition"。
**第 6 项归属分裂须裁**：`session-checkpoint-policy` 在 `DSH借鉴指引-v2-20260904.md` §11.3 挂 **B-20**，在 `DSH源码锚点映射-20260907/README.md:29` 挂 **B-07**。

---

## 附：判定分布

| 判定 | 计数 |
|---|---|
| 命中 | **多**（B-02 顺序、B-04、B-05 五项、B-06 七项、B-08 十项、B-09 leakedServices、B-10 mergeLayers、B-15 两项、B-18 四项、B-20 六项） |
| 位移 | **多**（绝大多数"文件从 `lib/*.js` 变为 `src/*.ts`"、行号重排、契约扩张） |
| 被上游取代 | **5**：B-09 `COMPOSITION_FILE`/`METADATA_FILE`/旧包拆分、B-10 `namespace 注册制`/`patchNode`、B-11 `listChanged`、B-12 GitHub HMAC、B-19 `jobs` 内存实现 |
| 已消失 | **3**：B-01 `isEmptyResponseError`/`isInvalidCredentialError`（及同族）、B-06 旧定位 `dsh-agent-loop`、B-14 `types/spec.js` |

---

## 会推翻本表 7 条件

1. **HEAD 移动或工作树变脏** → 全部行号作废（只对 `00102833` 有效）。
2. **浅克隆**（`rev-list --count HEAD` = 1）→ **历史类判定不可证**。已因此撤回 B-14"自始不成立"；`isXxxError` 属"从未存在"还是"被官方删掉"**无法区分**（两者借鉴结论不同）；B-01/B-03 三个"新增项"的首次出现版本无法确证。
3. 若 `lib/` 曾被手工从 tarball 拷入 → B-08 的 md5 同一性不再是"同源构建"证据（但 39 行拆解独立成立）。
4. 上游把 `lib/` 纳入 git（改 `.gitignore:7`）→ "源码树自带构建产物"失效。
5. 换对照 tarball（本次只用 `0.1.6-alpha.2`）→ 差异结论重算。
6. **B-09 最薄弱处**：判定依赖源码树零命中 + skill/note 文本，**只 grep 了 `.ts`**；若有非 TS 载体仍在读 `agent.cordis.yml`，降级为"部分残留"。
7. B-07「`stateSchema` 必填」读的是 `src` 的 TS 声明；若运行时 `lib` 做了可选化，措辞需让步。

**另一条结构性限制**：两个旧物理断面（`0.1.2-alpha.2` / `0.1.2-rc.1`）**已不在磁盘** → "位移 vs 命中"的基线是**旧文档文字记录，不是实物**。该限制无法用更多检索消除。

---

## 附二：★ 行号漂移**不均一** —— 单靠"行号重锚"必错

本次三方对拍（旧文档行号 → 源码树 `src/` → 盘上 `lib/` 产物）测出**同一断面内漂移量差两个数量级**：

| 对象 | 旧文档行号 | 盘上产物行号 | 漂移 |
|---|---|---|---|
| `schedule` domain 族 | L442 / L476 / L497 / L532 | 一致 | **+29**（族内一致） |
| `schedule` `dueDecision` | L728 | — | **+1** |
| `jobs-local` `start` | L131 | L412 | **+281** |
| `jobs-local` `settle` | L365 | L736 | **+371** |
| `jobs-local` `assertAccess` | L313 | L565 | **+252** |
| `session-query-sqlite` 引擎 | L475 | L476 | **+1** |
| `jobs` 总行数 | 65 | 123 | **近翻倍** |
| `invariants` | 旧卡"123 行" | 源码 **200 行** | 旧数字本身是**产物**行数 |

**并伴随符号改名**：`DEFAULT_MAX_CONCURRENT_TASKS_PER_OWNER` → `…JOBS_PER_OWNER`（`jobs-local/src/index.ts:33`）。

**→ 方法学硬结论（建议写进借鉴纪律）**：
1. **行号重锚不足以覆盖漂移** —— 必须**逐符号核验存在性**，行号只作"现查"结果使用。
2. **禁止拿本表行号去读 `lib/`** —— 必然对不上（+1 … +371）。
3. **`grep 'class X'` 在 esbuild 产物上会假阴性**（产物是 `var X = class`）；本次曾据此误判"引擎类消失"，复查后纠正。
4. **禁止用 `git log` / `git blame` 论证引入时间**（浅克隆，`rev-list --count HEAD` = 1）。
5. **判断面必须用 `git ls-files`，不能用 `ls`** —— `lib/` 是被 `.gitignore:7` 忽略的本地产物（307 个 `index.js` 与 `src/` 并存，构建于 09-23 14:29，晚于源码 09-22 23:45）。
