# ③ 过时判定 — 仍成立 7 / 需改法 12 / 已无必要 1

> **断面**：`/Users/wane/src/deepseek-harness-017` @ `00102833`（`0.1.7-alpha.2` 源码树）｜**日期**：2026-09-23
> **判定标准**（以**机制**为准，不以路径为准——因为"npm 包名 → 源码路径"是全表共性变化，若按路径判则 20 张全是"需改法"，三档失去分辨力）：
> - **仍成立** = 机制与形态都活，只需把 npm 包路径换成源码路径
> - **需改法** = 除换锚点外，**机制／形态／口径有实质变化**，要改实现方案或卡面
> - **已无必要** = 上游已内建或**范式已变**，**不要按原样做**
>
> **v1.0 作废**：上一版基于 Desktop 打包运行时（`0.1.6-alpha.2`）判定的三档结论全部作废。

---

## 一、总表

| 判定 | 数量 | 卡 |
|---|---|---|
| **仍成立** | **7** | B-02 / B-04 / B-08 / B-13 / B-15 / B-17 / B-20 |
| **需改法** | **12** | B-01 / B-03 / B-05 / B-06 / B-07 / B-10 / B-11 / B-12 / B-14 / B-16 / **B-18** / B-19 |
| **已无必要** | **1** | **B-09** |

> **B-18 从"仍成立"改判"需改法"**：机制本身完整（200 行服务 / 38 个 companion / 12+ 注册点全部实证），**但卡面只写了"借注册表"，没写"借装配"**——`dsh-invariants` **只被 `sdk-minimal` 挂载**（`bundle/sdk-minimal/cordis.patch.yml:106-119`），`base`/`web-app`/`headless`/`acp-app` 的 patch **零挂载**，而 companion 一律 `inject:['invariants']` → **服务缺席即静默不激活、不报错**。**照抄机制而不照抄装配 = 抄了一个不会运行的正确检查**（正是我方 `agent_metrics` 零写入者、`checkpoint()` 零消费者那类「接线断裂」）。→ 卡面须补装配要求。

---

## 二、仍成立（8 张）

| 卡 | 依据（新断面 `file:line`） |
|---|---|
| **B-02** 重试配置化 | `llm-retry/src/index.ts:22` `inject=['agents','sessionProjections']` **逐字一致**；三段顺序 `:188/189/190` **逐行一致**；`retry-policy.ts:14/18-24/100` 词表与 schema 俱在。**唯一动作 = 换锚点写法** |
| **B-04** 工具结果修剪 | `config.ts:7` PRUNE_MARKER（值逐字）、`:11-13` 8192/4096/1024 三值逐字、`:27` `codePointLength`。**但注意 B-04↔B-03 硬耦合**（见下） |
| **B-08** 原子写 + 文件锁 | 十项全命中（`index.ts:78/85/87/88/29/33/53/158/163/169/186-190/17`）。**两断面 `lib/index.js` md5 相同**（`7a93bb9d…`）→ 零逻辑差异 |
| **B-13** goal 状态机 | `fold.ts:339` `foldGoal`、`runtime.ts:8` `GOAL_CHANGE_VERSION = 1`、`fold.ts:136-138` fail-loud 版本拒绝、`goal-round-driver/src/index.ts:75-76` 竞态围栏、`prompt.ts:12` 续轮提示、`tool-goal/src/authority.ts:48/100/111` authority |
| **B-15** 权限预设写穿 | `permission-presets/src/index.ts:2-3` doc 原文：预设＝独立 `sandbox-mode` + `approval-policy` 旋钮之上；`:53-58` "durable, log-only user intent… knob events follow in the same turn and control execution"；setter **定义在 owner 包**（`sandbox-policy/src/session-mode.ts:53`、`user-approval/src/index.ts:100`）→ **归属有行为证据** |
| **B-17** skill provider | 两阶段俱在：发现侧 `skill-filesystem/src/index.ts:723` `discoverRoot`（`:150`/`:186` 注册、`registerProvider:136`）；拉取侧 `tool-skill/src/index.ts:82`（tool 名 `skill`）、`:161` `tools.register`、catalog 注入 `:213/222/254`。seam 新增 `stable revision`/`authoritative`（`skill/src/index.ts:230/238`；实现 `:367/523/533/622`）属**增强**，不改变借用目标。**⚠️ 旧卡写"五包"，实为 6 包**（`tool-workspace-dependencies` 不是 provider） |
| **B-18** 不变量注册表 | `invariants/src/index.ts:94` `InvariantRegistry`、`:136` `register`（disposer `:192`）、`:140-141` 重复注册抛错、`:50` `InvariantError`（`code='INVARIANT'` `:52`、`packageName` `:54`、ctor `:61`）、`:200` `export default`；doc `:2-3` "Every workspace package registers checks from a `./invariant` companion"。源码 200 行 ↔ tarball 123 行 = 类型/注释剥离 → **判定见下（已改判需改法）** |

> **B-18 已从"仍成立"改判"需改法"**——机制完整，但**装配没写上**：`dsh-invariants` **只被 `bundle/sdk-minimal/cordis.patch.yml:106-119` 挂载**；`base` / `web-app` / `headless` / `acp-app` 的 patch **零挂载**，而 companion 一律 `inject:['invariants']` → **服务缺席即静默不激活、不报错**。**照抄机制而不照抄装配 = 抄了一个不会运行的正确检查**（正是我方 `agent_metrics` 零写入者、`checkpoint()` 零消费者那类「接线断裂」）。可复用规律：**seam 包不出现在任何装配行**（`jobs`↔`jobs-local`、`subprocess`↔`subprocess-local` 同构）。
| **B-20** 速览族六项 | **六项全命中**：`output-retention/src/index.ts:11-15/52/64/82`；`repeat-tool-reminder/src/index.ts:16/24/29`；`subprocess/src/index.ts:66`（定义）+`:47`（规则）；`time-context/src/index.ts:28/43/59/112`；`session-query-sqlite/src/schema.ts:127/158` + `index.ts:819/842` + `query.ts:29`；`session-checkpoint-policy/src/index.ts:2/15/18/22/27/40/53` |

**仍成立的 8 张里，唯一需要额外注意的两条**：
- **B-04 ↔ B-03 硬耦合**：修剪器 `static inject = ['tokenMeter']`（`compaction-tool-result-pruner/src/index.ts:47`），且每次发 `compaction/prune` 影子价格（`:160-164`）→ **两卡不能拆开移植**（旧卡当独立卡写）。所以 B-04 虽"仍成立"，其**施工顺序**受 B-03 约束。
- **B-08 的"1 天"只覆盖抄机制**：上游 `tests/atomic-write.spec.ts`（246 行/13 例）**mock `node:fs/promises`，不跑真并发** → 卡面验收条件"**并发写测试零撕裂**"**必须我们自建**，不能引上游背书。

---

## 三、需改法（11 张）

| 卡 | 改什么 | 依据（新断面 `file:line`） |
|---|---|---|
| **B-01** | **口径收窄**：卡面"`isXxxError` 守卫族"改写为「**canonical 码（出站路由）＋ 正则分类器（入站归类）**」两层 | 专用守卫只有 **3 个**：`error.ts:80` `isContextWindowExceededError`、`:94` `isQuotaExceededError`、`:161` `isHarnessError`。**前两个是 `(detail: string)` 正则分类器 —— 与同文件 `:14` 的 "never by parsing message" 恰好相反**。`isEmptyResponseError`/`isInvalidCredentialError`/`isRateLimitError`/`isServerError`/`isTimeoutError` **`src/` 与 `lib/` 双侧零命中**（非摇树）。`normalizeLlmFailure` 在 `adapter-failure.ts:16`，**不在 `error.ts`** |
| **B-03** | **改两处卡面错误 + 换落点** | ① **plan/commit 不是成本两段式**，是 **surface 折叠的异常安全事务对**（`surface-fold.ts:7-9` 逐字：read-only plan ＋ in-place commit）→ 在 `surface-fold.ts:107`/`:136`；② **`projectedTokens` 不可用于中文计费或硬闸门** —— DSH 自述 estimator **系统性低估 CJK** 并已排除在 occupancy 之外（`projection.ts:54-56`）；`CHARS_PER_TOKEN=4` 是拉丁文密度假设；③ 新增 `estimate.ts:19` `ROLE_OVERHEAD` |
| **B-05** | **重锚 + 归属** | 实体在 `compaction-basic`（`index.ts:113` `BasicCompactionEngine extends CompactionEngine`）；`compaction`（无后缀）**是抽象 seam**（`package.json:3` 自述；只含 `abstract class CompactionEngine:119` ＋ 三个事件类型 `types.ts:24/34/72`）。**旧卡归属正确，错的是 09-07 映射表**（它记在 `dsh-compaction lib/types/`）。五要素仍全命中：`config.ts:20/23`、`region.ts:210/491/237,245`、`compaction/src/types.ts:40/88/119`、`summarizer.ts:30`、`index.ts:194` |
| **B-06** | **重锚（旧定位完全错）** | 〔映〕"超时在 `dsh-agent-loop`"**两断面一致证伪**：源码树 `packages/core/agent-loop/src/` 8 文件 `grep -ric timeout` **全 0**（2449 行）；tarball 1950 行 `-ic timeout` = **0**、`-ic abort` = **98**。真实承担者：`util/timeout/src/index.ts:25/45/91/126/184` + `guard/timeout-policy/src/index.ts:25/41/74`（`:46` `code: TOOL_TIMEOUT`） |
| **B-07** | **重锚 + 契约形状** | `register:233/253`（`:48` `ProjectionDefinition`，**契约扩张**含 `stateSchema`）；checkpoint 行 `:127`、`checkpoint():396` 落值 `:400-404`；`restoreFloor:425`（`:429-431` 归零）；`restore:495`/`:510`/`:523`，**三重判定 + `baseSeq>0` 时 throw** |
| **B-10** | **两半机制被换掉（不是换行号）** | ① `mergeLayers` **仍活**：`settings/src/index.ts:175`，`:176` 非 plain object 整值替换逐字一致；② **`namespace 注册制` 消失** → 改由 `SettingsForms:223` ＋ `SettingsNamespaceView[]`（`types.ts:63`）＝ profile entry id + 插件 Config schema 推导；③ **`patchNode` 全树零命中**，`settings/settings-file` **包不存在**，`settings.yaml` 被标注 "the **removed** settings.yaml"，由 `importLegacyDocument()` 一次性迁移；④ `--dump-config` 仍在但落点变：`apps/cli/src/dump-config.ts:41` → 实现 `boot/app-boot/src/index.ts:417`、flag `apps/cli/src/args.ts:170-171`。**`dsh-settings` 是抽样 8 包里唯一测出真实跨版本差异者**（`SettingsProvider` → `SettingsForms`，repo lib 544 行 vs tarball 610 行） |
| **B-11** | **切 client + 换蓝本** | ① **transport 借来无意义**：`mcp-client` 自己 import 官方 SDK `@modelcontextprotocol/client@2.0.0`（`transport.ts:9-11`、`connection.ts:18`、`tools.ts:17`；`package.json:42` 在 **dependencies** 段）。DSH 在 `transport.ts` 46 行里自写仅 9 行，且自述 "**The MCP SDK owns the actual spawn**"（`:16-19`）；② **旧卡方向说反一处**：`listChanged` 是 **SDK 的 `Client` 构造选项**（`connection.ts:263-269`；SDK `dist/index.d.mts:1716`），**非 DSH 自研** → 该锚点**被上游取代**；③ DSH 真正自研的是 `autoRefresh:false` → `refreshTools():296-300` → `syncTools`（`tools.ts:113`）的**重注册注册表语义**；④ **DSH 侧没有 MCP server 包**（`packages/mcp/` 2 包全为 client 侧）→ **我方 `src/mcp/` 在 DSH 无参照物** |
| **B-12** | **验签要改法；"自研"是建议不是被迫** | GitHub HMAC **完全委托** `@octokit/webhooks@^14.2.0`（`packages/webhook/webhook-github/src/handler.ts:5` import、`:101` `new Webhooks({secret}).verify(...)`；`package.json:44`）。**分层口径反证**：`createHmac`/`timingSafeEqual` 在**生产源码侧 0 命中**；含 tests 则 `createHmac` **4 命中（全在 `tests/*.spec.ts`，是测试自己"签发"签名喂被测代码，非验签实现）**，`timingSafeEqual` 全口径 **0 命中**。→ "DHS 自己不实现验签"成立。**选型二选一**：①自研 `crypto.createHmac`+`timingSafeEqual`（零新依赖，**建议**）②跟随 DSH 引 octokit（新增运行时依赖，与 G5 冲突）——**属产品/依赖政策决定**。可复用部分在 `webhook`（adapter 无关规则运行时：`index.ts:39-54` delivery 校验+deepFreeze、`session.ts:155-163` provenance），**不在 `webhook-github`** |
| **B-14** | **重锚 + 撤回"自始"** | ① `types/spec.js` **本断面不存在**（`src/` 仅 `index.ts` + `types.ts`）；**v1.0 的"自始不成立"撤回**——浅克隆（`rev-list --count HEAD` = 1）不可证历史，只能证"本断面不存在"；② **指纹围栏实为三要素** `[id, createdAt, cwd]`（`index.ts:249-255`，比对 `:250` vs `:251`，`isDeepStrictEqual:249`，不匹配 `throw:254`）—— **旧卡漏了 `id`**；③ 承担者分工：类型 ∈ `types.ts:14/17/20/139`，**zod schema ∈ `index.ts:62-73`** |
| **B-16** | **措辞** | "候选限流"实为"候选**限件数**"：`config.ts:6` `DEFAULT_CANDIDATE_LIMIT = 50`；全包**无** `throttle`/`rate limit`/`backoff` 字样。其余命中：`uri.ts:16/36/50`、`config.ts:4` `MAX_REFERENCES=3`、`:8` `DEFAULT_MAX_REFERENCE_BYTES=65_536`、`projection.ts:18`、`types.ts:78` |
| **B-18** | **卡面须补"装配"这一半** | `dsh-invariants` **只被 `sdk-minimal` 挂载**（`bundle/sdk-minimal/cordis.patch.yml:106-119`），`base`/`web-app`/`headless`/`acp-app` **零挂载**；companion 一律 `inject:['invariants']` → 缺席即**静默不激活**。机制侧全部实证：38 个 companion、12+ 注册点、`:94`/`:136`/`:140-141`/`:50`/`:200` |
| **B-19** | **重锚（对照对象换掉）** | ① schedule 走日志**命中**：`foldScheduleEvents` `domain.ts:629`、`createAfter/At/EveryScheduleRecord` `:674/706/757`、`dueDecision` `runtime.ts:42`、`schedule/change` append `runtime.ts:299/306`＋`tools.ts:383/445`＋`index.ts:102`、flush `persistence.ts:24/26`（`SchedulePersistenceError:7`）；② **`jobs` 是抽象 seam 且构造即抛错**（`jobs/src/index.ts:85` `export abstract class JobRegistry`、`:91` 点名加载 `jobs-local`）→ **内存实体在 `jobs-local/src/index.ts:128`**（`extends JobRegistry` **全仓仅此 1 处** → **"是否有第三处作业存储"假说被否定**）；③ **符号改名**：`DEFAULT_MAX_CONCURRENT_TASKS_PER_OWNER` → `…JOBS_PER_OWNER`（`jobs-local/src/index.ts:33`） |

---

## 四、已无必要（1 张）

### B-09 preset=目录 — **上游自己宣布该范式已死**

**依据（DSH 随包发布的迁移指引原文，`packages/preset/agent-preset/skills/editing-cordis-compositions/SKILL.md:70`）：**

> Before declaration rows, a user preset was a directory `$DSH_HOME/.agent-presets/<id>/` holding `preset.yml` (display `name`, `description`, `order`) and `agent.cordis.yml` (the plugin entry list). **Nothing reads that directory any more.** To migrate one, create a bundle as above whose declaration takes `id` from the directory name, `name`, `description`, and `order` from `preset.yml`, and `plugins` from `agent.cordis.yml` verbatim; check each plugin name against `cordis-composition-reference` **because packages renamed since the preset was written fail at activation.**

**硬证据**：
- `COMPOSITION_FILE`（tarball `discovery.js:35` = `'agent.cordis.yml'`）→ 源码树**零命中**
- `METADATA_FILE`（tarball `authoring.js:19` = `'preset.yml'`）→ 源码树**零命中**
- 包 `dsh-agent-presets` **不存在**；拆为 `@deepseek-ai/dsh-agent-preset-registry`（9 文件 / 1096 行）＋ 全新 `@deepseek-ai/dsh-agent-preset`（30 行）

**新范式**：Cordis composition 里的 **declaration row** —— `agent-preset/src/index.ts:1` "A declarative preset row in an ordinary Cordis composition"；`:12` `class AgentPreset`；`:16-23` `Config = {id, name, description, order, plugins}`；承载物 `packages/bundle/web-app/presets/*.patch.yml`。

**结论**：**卡面让抄的"目录 + 双 yml"整套作废，按原样实现会做出一个上游已不再读取的东西。**
**保留项（不要一起丢）**：
- `leakedServices` 泄漏审计 **仍活**：`agent-preset-registry/src/mount.ts:85`（**命中**）
- `mountPreset` **仍活但签名变**：`mount.ts:254` `(ctx, id, plugins)`（**位移**）

**⚠️ 最薄弱处（诚实标注）**：判定依赖（a）源码树零命中（b）skill/note 文本（`.agents/notes/archived/…` 亦载同句），且**只 grep 了 `.ts`**。**若有非 TS 载体仍在读 `agent.cordis.yml`，本判定降级为"部分残留"**。

---

## 五、需裁决一项：B-20 第 6 项归属分裂

`session-checkpoint-policy` 在两份文档里挂不同卡：
- `DSH借鉴指引-v2-20260904.md` §11.3 → **B-20**
- `DSH源码锚点映射-20260907/README.md:29` → **B-07**

源码树事实：`session/session-checkpoint-policy/src/index.ts:18` `inject = ['llm','sessionPersistence','sessions','tools']`（**不含投影服务**）；`:22` "A checkpoint rejection prevents adapter dispatch"；`:29` `afterCheckpoint`（先 flush 再 `yield* next()`）；`:64` `llm/stream`（adapter dispatch 前）；`:70` `tools/execute`（tool body 前，仅顶层）；`:79` `pre-step`；`:38` `abortedBeforeDispatchResult`；文件 **83 行**，**导出恰为 `name`/`inject`/`apply`**。

**★ 裁定证据（决定性）**：两者 **零引用**——`session-checkpoint-policy` 的 `inject` 不含投影服务，`session-projection` 也不引用它。
**两者 checkpoint 是"同词不同机制"**：
- `session-checkpoint-policy` = `ctx.sessions.flush()` **持久化屏障**
- `session-projection` = `session-projection/src/index.ts:396`(`checkpoint`)/`:425`(`restoreFloor`)/`:449`(`viewCheckpoint`) **投影状态快照**

→ **旧文档"一处挂 B-20、一处挂 B-07"属命名撞车。建议唯一归 B-20，且两卡不合并。** 同时回改 `DSH源码锚点映射-20260907/README.md:29`。

---

## 六、什么会推翻本判定

1. **HEAD 移动或工作树变脏** → 全部行号作废（只对 `00102833` 有效）。
2. **浅克隆**（`rev-list --count HEAD` = 1）→ **历史类判定不可证**。已据此撤回 B-14"自始不成立"；`isXxxError` 属"从未存在"还是"被官方删掉"**无法区分**（两者借鉴结论不同）。
3. **`lib/` 对拍不可外推**：抽样 8 包，6 包字节相同、`dsh-settings` **真差异**（`SettingsProvider` → `SettingsForms`）→ "两断面等价"**不成立为通则**；B-08 的结论只对 atomic-write 有效。
4. **B-09 的非 TS 载体未排查**（见 §四最薄弱处）。
5. **旧物理断面已不在磁盘** → "位移 vs 命中"的基线是**旧文档文字记录，不是实物**。结构性限制。
6. **B-11 的 `listChanged` 归属置信度 = 中**：只读 SDK **类型声明**，未读 `dist/index.mjs` 实现体 → 若 SDK 实现体与声明不一致，该条翻回"位移"。
