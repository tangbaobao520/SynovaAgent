# ④ 处置建议

> **断面**：`/Users/wane/src/deepseek-harness-017` @ `00102833`（`0.1.7-alpha.2` 源码树）｜**日期**：2026-09-23
> **纪律**：本审计**只登记、不自修**。涉及 `src/` 改动一律**另立编码卡**，走 SynovaAgent 自己的流程。

---

## 一、未动手的：按最新范式重写实现方案

### 1.1 B-08 原子写 + 文件锁 — **现在能做；按原方案做，1 天量级**（**不需要"按最新范式重写"**）

**为什么不需要重写**：新断面十项锚点**全部命中**，且**两断面 `lib/index.js` md5 相同**（`7a93bb9d98ad09092eb9b46128ba637a`，均 152 行）→ 上游自 0.1.2 线以来**未改动该机制**。

**实现路径**（读范式自研，抄进本仓即自有代码；**零依赖**）：

| # | 要做的事 | 新断面锚点 |
|---|---|---|
| 1 | 落点 `scripts/control-tower/`（状态文件 + 备份脚本），按铁律 0-3/0-4 | — |
| 2 | `writeFileAtomic(file, content, {mode})`：同目录随机后缀临时文件 + **独占创建 + rename 提交** | `util/atomic-write/src/index.ts:78/85/87/88/29/33` |
| 3 | `withFileLock(file, op, opts)`：`<file>.lock` 的 `wx` 独占创建 + 跨进程串行化 + **异常路径释放** | `:158/163/169/186-190` |
| 4 | **`mode` 必填**（DSH 设为 required，防 umask 改权限） | `:53`，说明 `:45-46` |
| 5 | **Windows 瞬时 rename 重试集** `{EACCES, EBUSY, EPERM}` | `:17`，判据 `:23-26`，重试 `:29-42` |
| 6 | 抄写处按 **M3 / D797 锚点三原则**留注释：**包 + 文件 + 符号 + 现验命令**（行号现查，不照抄） | — |

**成本量级：1 天**（原估不变；只覆盖"抄机制"）。

**⚠️ 三条必须写进卡里的约束**：
1. **卡面验收条件"并发写测试零撕裂"必须我们自建**——上游 `tests/atomic-write.spec.ts`（246 行/13 例）**mock `node:fs/promises`，不跑真并发**（`:8` lockPermissionFailures、`:9` releaseLockBeforeProbe、`:28` 注入 EPERM、`:62` waitForLock、`:106/124/165/192` 分支）→ **上游不测真并发，我们不能引它背书**。
2. **开卡前置 = M2**：`scripts/control-tower/` 至今 **零线归属**（指引 §12.2 登记为"零归属"）→ **无归属 = 无从回写 = 不得开卡**。**先定线归属，再开卡。**
3. **不要在源码树里按 npm 包名查**——`packages/util/atomic-write/`，无 `dsh-` 前缀。

### 1.2 B-11 MCP client — **现在能做，但"要借什么"必须重新定义**

**先切需求（这是产品问题，不是技术问题）**：
- 我方 `src/mcp/index.ts` 是 **Synova 自己的 MCP server**（`npm run mcp`）
- B-11 卡面要的是 **Synova 作为 client 连企业系统**
- **DSH 侧没有 MCP server 包**：`packages/mcp/` 2 个包（`mcp-client`、`mcp-resources`）**全为 client 侧** → **我方 server 在 DSH 无参照物，DSH 只能给 client 侧面参考**

**实现路径**：

| # | 做什么 | 依据 |
|---|---|---|
| 1 | **transport 层不借**——直接用官方 SDK（`Client` + `StdioClientTransport` / `StreamableHTTPClientTransport`） | `mcp-client/src/transport.ts:9-11`；DSH 在 46 行里自写仅 9 行，并自述 "The MCP SDK owns the actual spawn"（`:16-19`） |
| 2 | **不引 octokit 式依赖**；MCP SDK 我方已于 2026-09-08 采纳（`MCP-SDK-评估结论-20260908.md:65`） | — |
| 3 | **仍值得自研的三块**：① **工具表重注册的注册表语义**（不是通知机制）② `scrubbedParentEnv` 敏感环境擦除 ③ 连接生命周期与失败分类（复用我方 B-01 错误码） | ① `connection.ts:263-269` + `:296-300` + `tools.ts:113`；② `subprocess/src/index.ts:66`（定义）/`:47`（规则） |
| 4 | **契约冻结前置**：MCP 多轮对话工具随 `SYNOVA-MCP-CONTRACT` 冻结再动 | `MCP-SDK-评估结论:74-75` |

**成本量级：3–5 天**（不含需求切分与契约冻结）；**比原估低**，因为 transport 不用自研。

**建议补证（约 5 分钟）**：读 MCP SDK `dist/index.mjs` 实现体，确认 `listChanged` 的去抖与通知解析确在 SDK 内部执行——本审计只读到**类型声明**级证据，该条置信度**中**。

### 1.3 B-09 → **改换借用目标**（原范式已废弃，但注册表语义可续用）

| 原卡面（作废） | 改为借什么 |
|---|---|
| `agent.cordis.yml` 组合行 + `preset.yml` 元数据 + 目录扫描 | **作废**。上游自述 "Nothing reads that directory any more"（`agent-preset/skills/editing-cordis-compositions/SKILL.md:70`） |
| — | **借"注册表语义"**：`mountPreset`（`agent-preset-registry/src/mount.ts:254`，签名 `(ctx, id, plugins)`）＋ **`leakedServices` 泄漏审计**（`mount.ts:85`，**命中**） |
| — | **借"declaration row"形态**：`agent-preset/src/index.ts:1/12/16-23`（`{id, name, description, order, plugins}`）；承载物 `bundle/web-app/presets/*.patch.yml` |

**副产（建议进路线图）**：`compaction` 的 **seam/实体分离** ＋ `summarize()` 是官方明示的 "sole subclass customization hook"（`compaction-basic/src/index.ts:236-246`）→ **这是 0.1.7 给我们的真实可插拔缝隙**，与 CFG 客户配置包的"四层叠加"蓝本相关。

### 1.4 B-10 → **两半机制被换掉，卡面需重写**

| 原卡面要素 | 现状 | 改法 |
|---|---|---|
| `mergeLayers`（对象递归/数组整值替换） | **仍活**：`settings/src/index.ts:175`，`:176` 逐字一致 | 保留 |
| namespace 注册制 | **消失** | 改为「profile entry id + 插件 Config schema 推导」（`SettingsForms:223`、`SettingsNamespaceView[]` `types.ts:63`） |
| **`patchNode` 注释保留叶子级 patch** | **全树零命中** | 改为 `boot/config-editor/src/index.ts:75` `ConfigEditor.edit()`（`:99` `parseDocument`、`:117` `visit`、`:12` yaml import）；另一条语义在 `SettingsPathOp:81` + `applyPathOp:86`（`dsh-settings-file` 包已不存在，`settings.yaml` 被标注 "the **removed** settings.yaml"） |
| CLI `--dump-config` 逐层带来源 | **仍活**，落点变 | `apps/cli/src/dump-config.ts:41` → 实现 `boot/app-boot/src/index.ts:417`；flag `apps/cli/src/args.ts:170-171` |

### 1.5 B-12 / B-14 / B-16 / B-19 — 卡面小改，机制照做

- **B-12**：**验签要改法；"自研"是建议不是被迫**。上游**完全委托** `@octokit/webhooks`（`packages/webhook/webhook-github/src/handler.ts:5` import、`:101` verify）。**分层口径**：`createHmac` 在生产源码侧 **0 命中**；含 tests 则 **4 命中（全在 `tests/*.spec.ts`，测试自签自验，非实现）**；`timingSafeEqual` 全口径 **0 命中**。→ **不能照卡抄**（卡面要借的 HMAC 实现在上游已不存在）。**二选一**：① 自研 `crypto.createHmac` + `crypto.timingSafeEqual`（零新依赖，约 20 行，**建议**）；② 跟随 DSH 引 octokit（新增运行时依赖，与 G5 冲突）。**属产品/依赖政策决定。** 可复用部分在 `webhook`（`index.ts:39-54` delivery 校验+deepFreeze、`session.ts:155-163` provenance），**不在 `webhook-github`**。
- **B-14**：`spec.js` 不存在（**"自始不成立"已撤回**——浅克隆不可证历史）；指纹围栏是**三要素** `[id, createdAt, cwd]`（`index.ts:249-255`），**卡面须补 `id`**；承担者分工 = 类型 `types.ts` / **zod schema `index.ts:62-73`**。
- **B-16**：措辞改"候选限流" → "候选**限件数**（上限 50）"（`config.ts:6`）；全包无 throttle 字样。
- **B-19**：对照对象由 `jobs` 换成 **`jobs-local`**（`jobs/src/index.ts:85` 是 abstract seam，`:87-91` 构造即抛错）。

### 1.6 B-18 运行时不变量注册表 — ★ **机制能做，但必须连"装配"一起借**

**新断面事实**：机制侧**全部实证** —— `runtime-diagnostics/invariants/src/index.ts`（**200 行**）；`:94` `InvariantRegistry`、`:136` `register`（disposer `:192`）、`:140-141` 重复注册抛错、`:50` `InvariantError`（`code='INVARIANT'` `:52`、`packageName` `:54`）、`:200` `export default`；**38 个 companion 实证"检查可选"真落地**（12+ 注册点）。

**★ 风险（本卡真正的坑）**：`dsh-invariants` **只被 `bundle/sdk-minimal/cordis.patch.yml:106-119` 挂载**；`base` / `web-app` / `headless` / `acp-app` 的 patch **零挂载**。而 companion 一律 `inject:['invariants']` → **服务缺席即静默不激活、不报错**。

> **照抄机制而不照抄装配 = 抄了一个不会运行的正确检查。**

这正是我方已发生过的缺陷类型（`agent_metrics` 零写入者、`checkpoint()` 零消费者 —— "接线断裂"）。**因为无效检查静默失败，比没有检查更危险**（它给出虚假的安全感）。

**卡面须补的两条**：
1. 借 **`InvariantRegistry` 机制** ＋ **`./invariant` companion 约定**（每包一个 companion，`inject:['invariants']`）。
2. **借"装配必须显式"这条规律** —— 落码时加一条**可判定的在场性断言**（服务缺席即 fail-loud，而非静默跳过）。**这一条是我们自己的改进，不是照抄**：上游此处恰恰是静默的。

**可复用规律（值得进路线图）**：**seam 包不出现在任何装配行**（`jobs`↔`jobs-local`、`subprocess`↔`subprocess-local`、`invariants`↔挂载点 三者同构）→ 凡引入 seam 体系，必须同时给出"装配在哪"的答案。

### 1.7 B-13 / B-15 / B-17 / B-20 — 按原方案推进，无需改机制

锚点全部命中，只是路径形态从 npm 包变为源码路径。**开工前必须做的一件事**：`DSH借鉴指引-v2` §11.3 与 `DSH源码锚点映射-20260907` 里的 `dsh-*` 包名**全部作废**，凡引用它们的卡面/注释一律按本件重锚。

---

## 二、已动手的：只列"能否运行"

### **结论：B-01…B-10 十张全部「今日仍能运行」——10 test files / 112 tests 全绿 / 6.36s。**

原始输出见 [附录-冒烟验证原始输出.md](附录-冒烟验证原始输出.md)（含跑在哪个 checkout）。

**→ 不登记 FIX 卡。** 派单规定的触发条件（"已动手但不可运行"）**未成立**。

**但必须登记的是文档/流程欠账**（见下节）——这些不是"不能运行"，而是"按 M2 不算通过"。

---

## 三、建议登记的欠账（本审计只登记，不自行开卡）

| # | 问题 | 建议处置 | 证据 |
|---|---|---|---|
| 1 | **D594 编号撞车**：`task-state/D594.json` 是 "L1-P2交互卡片链路"（`closed`，被 D602 取代），但 commit `f509b3cf` 标 `feat(D594): …B-05` → **B-05 零审计承载** | 立新卡：为 B-05 补独立审计；同时修 commit ↔ 卡号映射纪律 | `git show HEAD:task-state/D594.json`；`ledger.json:4347` / `:4435` |
| 2 | **零审计 4 张**（不止 CTO 说的 D598/D599 两张）：**B-03 / B-05 / B-09 / B-10** = `impl_done` + `audit=None` | 立审计卡（可合并为一张"B-03/05/09/10 补审计"） | task-state 各卡原字段 |
| 3 | **B-07 仍 CONDITIONAL PASS，修复卡 D597 自 2026-09-08 停在 `impl_done`** | 催 D597 收口并复审，否则 B-07 永不计通过 | `task-state/D597.json`：`status=impl_done`、`updated_at=2026-09-08` |
| 4 | **G1 全仓计数 199 已过期（实为 227）**；指引 §11.3 "0/20 回写" 已被 D806 超越但未回改 | 回改两处引数；把 199→227 的差额归因写清（**+28 全是新增文档，非桥接**） | `git grep -l -E "deepseek-harness\|@deepseek-ai" HEAD \| wc -l` = 227；同命令 `1062ba2f` = 199 |
| 5 | **「D769 未登记」结论已过期**：`task-state/D769.json` 在 main（`e418cddc`，09-20）；`board-backlog.json` 的 `PLAN-d808-b20-unregistered` 仍写"D769 不存在"，**与物理矛盾** | 回改 backlog 与 D808 表 | `git show HEAD:task-state/D769.json` |
| 6 | **M3 存量缺口 4 文件**：`src/llm/tool-result-pruner.ts`、`src/llm/timeout.ts`、`src/agent/context-compaction.ts`、`src/store/session-projection.ts` 的锚点注释引 **Win 路径 + `0.1.1-rc.2`**，无现验命令 | 立**编码卡**补锚点注释（属 `src/` 改动，须走正常流程） | 指引 §10.4 已登记；`grep -rln "deepseek-harness/packages\|D:/deepseek-harness" src/` |
| 7 | **B-05/B-07 等"仍成立/需改法"卡的锚点注释若已在 `src/` 落码**，需按本件重锚（`dsh-*` 包名已作废） | 并入第 6 条编码卡 | 本件 ② 表 |
| 8 | **B-20 第 6 项 `session-checkpoint-policy` 归属分裂**（B-20 vs B-07） | **裁定归 B-20**（两者**零引用**：其 `inject:18` 不含投影服务；checkpoint 是**同词不同机制**——`sessions.flush()` 持久屏障 vs `session-projection:396/425/449` 投影状态快照）→ 回改 09-07 映射表 `README.md:29`。**两卡不合并** | ③ 表 §五 |
| 9 | **B-17 卡面写"五包"，实为 6 包**（`tool-workspace-dependencies` 不是 provider） | 回改卡面 | ③ 表 |
| 10 | **`lib/` 是 gitignored 构建产物这一事实未入仓内文档**：本次是 v1.0 误判断面的**物理根因** | 在 DSH 借鉴纪律文档里加一条硬规则：**判断面必须用 `git ls-files` 而非 `ls`；grep 必须限定 `src/`**（当前 `lib/` 有 307 个 `index.js` 与 `src/` 并存，且构建时间 09-23 14:29 晚于源码 09-22 23:45） | `git check-ignore -v …/lib/index.js` → `.gitignore:7: lib/`；`git ls-files` = 0 |

---

## 四、护城河视角（接口理论线 H3，一句话）

本轮 20 张卡借来的都是 `dsh-*` 的 **MIT 公开实现**，任何人读上游源码即可获得，且上游持续在改——**没有一张构成护城河**。它们的真实价值是**把工程基线抬高**（错误码统一、重试可配置、成本可测、长会话不爆窗、原子写不撕裂），属"**不落后**"而非"**别人抄不走**"。

**唯一可能换成议价权的落点**是 **B-09/B-10 的 `src/config/`——客户配置包机制**。理由：配置即交付物，转换成本来自**我们自己的契约设计**（多客户配置隔离 + 四层叠加 + profile patch 语义），不来自 DSH。**其余 18 张按"阶段性优势"记账，不进护城河叙事。**

**新增的一条技术判断**：`compaction` 的 **seam/实体分离**（`compaction-basic/src/index.ts:236-246` `summarize()` 是官方明示的 sole customization hook）是 0.1.7 给我们的**真实可插拔缝隙**——若客户侧需要定制压缩策略，这是官方认可的扩展点，值得进 CFG/插件化路线的设计输入。

---

## 五、下一步建议顺序

1. **立即**：回改 G1 计数、§11.3 回写表、D769 backlog 矛盾（纯文档，0 代码风险）。
2. **本周**：B-08 开卡前的**线归属裁定**（M2 前置）；B-03/05/09/10 补审计；催 D597。
3. **本周**：M3 存量缺口 4 文件的锚点注释补齐（编码卡）。
4. **下周**：B-09 / B-10 / B-11 卡面重写（三张"机制变了"的卡）。
5. **B-12…B-20**：按原方案推进，锚点按本件替换。

**不建议现在做的**：按 v1.0 的 `dsh-*` 包名锚点开任何新卡——那些锚点在源码树里**全部找不到**。
