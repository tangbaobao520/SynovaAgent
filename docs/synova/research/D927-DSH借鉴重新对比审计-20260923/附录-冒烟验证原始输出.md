# 附录 — 冒烟验证原始输出

> **创始人口径**：「已动手的确保可以运行即可」——**不追新，只验可用**。
> **日期**：2026-09-23｜**执行**：山河研究院技术线

---

## 一、跑在哪个 checkout（必注明）

```
checkout = /Users/wane/SynovaAgent
branch   = main
HEAD     = 967a58e5
```

**说明**：
- 冒烟对象是 **SynovaAgent 的自己代码**（B-01…B-10 十个落点），**不是 DSH 树**。DSH 树（`/Users/wane/src/deepseek-harness-017`）本次**全程只读，未构建、未跑测试**。
- 跑前 `git status --porcelain` 的 tracked 文件集**为空**（仅有 6 个未跟踪文件：`task-state/D927.json`、`task-state/D928.json`、3 个 `D923-*` 证据件、2 个 `.claude/task-briefs/*.md`）→ **被测源码 = `main` HEAD，无未提交污染**。
- 跑后 `git status --porcelain` **与跑前一致**（无 tracked 文件改动）。

---

## 二、命令（原样可复现）

```bash
cd /Users/wane/SynovaAgent && npx vitest run \
  tests/error-types.test.ts \
  tests/llm-resilience.test.ts \
  tests/llm/call-resilience.test.ts \
  tests/store/retry-projection.test.ts \
  tests/llm/token-meter.test.ts \
  tests/llm/tool-result-pruner.test.ts \
  tests/agent/context-compaction.test.ts \
  tests/store/session-projection.test.ts \
  tests/config/config-layers.test.ts \
  tests/config/customer-config-package.test.ts
```

**env**：`vitest/4.1.8 darwin-arm64`，`node v22.23.2`（仓库 `package.json` 要求 `>=22.19.0`）。

> **环境备注（诚实标注）**：首次执行被文件沙箱以
> `EPERM: operation not permitted, open '/Users/wane/SynovaAgent/node_modules/.vite-temp/vitest.config.ts.timestamp-1790154163816-2a5ef1fb3b9db.mjs'`
> 拒绝（vitest 必须在仓库内写临时配置，该路径在会话工作区之外），**经一次授权后原样重跑成功**。**命令本身未变。**

---

## 三、原始输出（summary，逐字）

```
 RUN  v4.1.8 /Users/wane/SynovaAgent

 Test Files  10 passed (10)
      Tests  112 passed (112)
   Start at  17:58:16
   Duration  6.36s (transform 1.05s, setup 0ms, import 1.61s, tests 6.34s, environment 1ms)

VITEST_EXIT=0
```

**→ 10 test files / 112 tests 全绿 / 0 失败 / exit 0。**

---

## 四、逐卡拆解（`--reporter=verbose`，按 `✓` 行计数）

| 卡 | 测试文件 | 用例数 | 结果 |
|---|---|---|---|
| **B-01** | `tests/error-types.test.ts` | 14 | ✓ |
| **B-02** | `tests/llm-resilience.test.ts` (14) ＋ `tests/llm/call-resilience.test.ts` (13) ＋ `tests/store/retry-projection.test.ts` (6) | 33 | ✓ |
| **B-03** | `tests/llm/token-meter.test.ts` | 11 | ✓ |
| **B-04** | `tests/llm/tool-result-pruner.test.ts` | 6 | ✓ |
| **B-05** | `tests/agent/context-compaction.test.ts` | 18 | ✓ |
| **B-06** | （与 B-02 同套件：`llm-resilience` / `call-resilience`） | — | ✓ |
| **B-07** | `tests/store/session-projection.test.ts` | 7 | ✓ |
| **B-08** | —（**未动手，无测试**） | — | — |
| **B-09 / B-10** | `tests/config/config-layers.test.ts` (15) ＋ `tests/config/customer-config-package.test.ts` (8) | 23 | ✓ |
| | **合计** | **112** | **✓** |

> 计数闭合：14+14+13+6+11+6+18+7+15+8 = **112** ✓

---

## 五、证明"跑的是真逻辑、不是空壳"的原始行

以下均截自同一份 stdout（`--reporter=verbose` / 默认 reporter），**未改写**。

**退避与重试（B-02/B-06）** — 延迟序列 1 → 2 ms，符合 `initialDelayMs * 2^(retry-1)`：
```
{"level":40,…,"service":"llm/retry-middleware","provider":"stub","code":"EMPTY_RESPONSE","attempt":0,"delayMs":1,…}
{"level":40,…,"service":"llm/retry-middleware","provider":"stub","code":"EMPTY_RESPONSE","attempt":1,"delayMs":2,…}
{"level":30,…,"service":"llm/retry-middleware","provider":"stub","attempt":2,"totalAttempts":3,…,"msg":"LLM 重试成功"}
{"level":40,…,"service":"llm/retry-middleware","provider":"stub","code":"AUTH_FAILED","attempt":0,…,"msg":"不可重试错误，返回 outcome"}
{"level":40,…,"service":"llm/retry-middleware","provider":"stub","timeoutMs":25,"attempt":0,…,"msg":"LLM 调用截止超时 — TOOL_TIMEOUT"}
{"level":40,…,"service":"llm/retry-middleware","provider":"stub","idleMs":20,…,"msg":"流空闲超时且不重试 — TOOL_TIMEOUT"}
{"level":40,…,"service":"llm/circuit-breaker","failures":2,…,"msg":"熔断器触发 → OPEN"}
```

**上下文压缩（B-05）** — 影子价格与事件序列真实产生：
```
{"service":"agent/context-compaction","trigger":"pressure","shadowedMessages":5,"shadowedTokenCount":250,"summaryTokenCount":26,…,"msg":"compacted 5 messages (250 tokens) into 26-token summary"}
{"service":"agent/context-compaction","trigger":"context-overflow","shadowedMessages":1,"shadowedTokenCount":50,…}
{"service":"agent/context-compaction","provider":"p","attempt":1,…,"msg":"上下文溢出 — 强制压缩后重试"}
```

**客户配置包（B-09/B-10）** — 泄漏键 fail-closed 与 degraded 兜底真实触发：
```
{"service":"config/customer-config-package","orgId":"org-leak","audit":[{"path":"$.process","key":"process","kind":"reserved-global"}],…,"msg":"customer-config: 泄漏键剥离（fail-closed，degraded）"}
{"service":"config/customer-config-package","orgId":"org-broken","broken":"config.yml 缺失——目录仍占用其 id；删除该目录或恢复文件","msg":"customer-config: 包 broken — default 层兜底（degraded）"}
{"service":"config/customer-config-package","orgId":"org-ghost","roots":[…],"msg":"customer-config: 无客户配置包 — default 层兜底（degraded）"}
{"service":"config/customer-config-package","orgId":"org-badyaml","broken":"配置不是合法 YAML: … unexpected end of the stream within a flow collection (2:1)",…}
```

**会话投影（B-07）** — whole-value 断言真实报警：
```
{"service":"store/session-projection","sessionId":"sess_mudwv76e_c1u8","key":"p-delta","seq":2,"msg":"whole-value 断言失败——apply(前状态,e) 与 apply(init,e) 不一致，状态承载事件未携带完整 post-change 状态（裸 delta）"}
```

**代表性测试名（证明确实在测 DSH 借鉴语义）**：
```
tests/agent/context-compaction.test.ts > ContextCompaction 配置校验 > retainRatio ≥ thresholdRatio 抛错（DSH validateRatioRetention 语义）
tests/agent/context-compaction.test.ts > ContextCompaction 配置校验 > retainRatio 与 retainTokens 互斥（DSH 语义）
tests/agent/context-compaction.test.ts > tool 配对平衡（DSH toolPairingBalanced 范式） > 保留边界落在 tool result 上时回退到其 assistant — 配对不拆散
tests/agent/context-compaction.test.ts > createProviderSummarizer > 压缩指令作为最后一条 user message 追加 — 前缀原样复用（KV-cache 命中）
tests/config/config-layers.test.ts > D599 config-layers: mergeLayers > 数组整值替换：禁止逐元素合并产生混合数组（决策点 1）
tests/store/session-projection.test.ts > D588 会话投影注册表 > ver 不匹配 → restoreFloor 归零 + restore 从 init 全量重放（one-below 锚点）
tests/llm/tool-result-pruner.test.ts > D587 pruneToolResult > replay-safe：同输入同输出，且对已修剪输出再修剪不变（幂等）
```

---

## 六、两条必要的诚实声明

1. **本冒烟只证"能运行"，不证"满足卡面验收"。** 例如 **B-08 卡面要求"并发写测试零撕裂"**——该卡**未动手**故无测试；且经查**上游自己也不测真并发**（`0.1.7-alpha.2` 的 `packages/util/atomic-write/tests/atomic-write.spec.ts` 246 行/13 例全是 **mock `node:fs/promises`** 的错误码分支测试）→ 该验收条件**必须我们自建**，不能用上游背书。
2. **跑的是 `main` HEAD 的自研代码，与 DSH 版本无关。** B-01…B-10 是自研落点（G1 零桥接成立：`src`+`packages` 的 `@deepseek-ai` = **0 命中**，全仓 **0 条真实 import 语句**）→ **DSH 上游从 `0.1.6-alpha.2` 变到 `0.1.7-alpha.2`，不影响这十张的可运行性**。这也是为什么"已动手的只需确保可运行"在本轮**全部通过**。

---

## 七、复现清单

- [ ] `cd /Users/wane/SynovaAgent && git rev-parse --short HEAD` → `967a58e5`
- [ ] `git rev-parse --abbrev-ref HEAD` → `main`
- [ ] 跑 §二 的命令 → `Test Files 10 passed (10)` / `Tests 112 passed (112)`
- [ ] `git status --porcelain | grep -v '^??'` → 空（跑前跑后一致）
