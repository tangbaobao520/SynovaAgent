# DSH 借鉴指引 v2 — 全包处置 + 代码锚点级借鉴卡（2026-09-04）

> 登记：DOC-0117 | type: research→decision | status: draft（待创始人复核后转 active）| owner: founder
> 2026-09-04 | 上游：DOC-0114《DSH迁移施工图》§4/§5.2 + DOC-0116 Stage1 任务卡 | 调研基线：桌面端实装 **dsh@0.1.2-alpha.2**（224 包）+ 上游 master `d347e70`（**dsh@0.1.3-alpha.1**，2026-09-04 发布）
> 附录 A：《DSH-224包处置表》（本目录，375 行）| 附录 B：《DSH运行时机制深潜》（本目录，221 行，8 机制 + 4 速览 + 代码锚点行号级）
> 最高约束（**2026-09-17 创始人裁定更新**，取代 DOC-0114 红线 1 的"绝不引入代码"表述）：**抄代码 = 允许且鼓励；桥接引用 = 禁止**（禁 `import @deepseek-ai/*`、禁 re-export 壳）；抄写处必须留锚点注释 + 现验命令 → 见 §10
> 治理更新 **D808（2026-09-17）**：新增 §10 合规口径 / §11 G3 守卫物理化 / §12 落点归属；与 V1 元断言（M1/M2/M3）的逐条对照见 **附录 C**

---

## 0. 这份指引是什么、不是什么

**是**：DOC-0114 的借鉴层操作化。0114 回答"哪些模块死守/借 DSH/搬走/删除"（架构级四色）；本指引回答"**具体借 DSH 的哪一段代码、落到 Synova 哪个文件、修哪条产品线缺口、怎么验收**"（锚点级借鉴卡），并给出 224 包**逐一处置表**保证无遗漏。

**不是**：Stage 3 迁移命令。入场券三条（哇呢宝贝首季数据 / DSH 脱离预览 / R9 收益验证）今天一条都不满足，本指引全部产出在"零依赖"前提下生效。

> ⚠️ **术语澄清（D808，2026-09-17 起）**：本文出现的"零依赖"一律指 **零包依赖**——不 `import`/`require` `@deepseek-ai/*`、不建 re-export 桥接（§10.2）。**"零依赖"≠"禁止抄码"**：抄码（含整段搬实现）自 2026-09-17 起**允许且鼓励**（§10.1，M3）；抄进来的代码即自有代码，不构成 DSH 依赖。§3 标题"零依赖"、§6 G5 均按此口径读。

**为什么需要 v2**：① 0114 基于	rc.5（8-20），上游 15 天跨 3 个版本线（rc.5 → 0.1.2-alpha.2 → 0.1.3-alpha.1），API 面仍在动（版本号 rc→alpha 重置 = 破坏性变更窗口敞开）；② 0114 的借鉴清单是方向级（D1-D9），不到代码锚点；③ 0116 六卡之外存在图纸内部缺口（§4 写了"借鉴 cron 冷恢复"但六卡无此卡）。

## 1. 版本 delta（rc.5 → 现在）

| 时点 | 版本 | 证据 |
|---|---|---|
| 2026-08-20（0114 基线） | v0.1.0-rc.5 | DOC-0114 §2.1（源：Win 机 D:\deepseek-harness） |
| 2026-09-04（桌面实装） | 0.1.2-alpha.2 | `~/Library/.../deepseek-harness-desktop/dependencies/dsh/node_modules/@deepseek-ai/dsh/package.json` |
| 2026-09-04（上游 master） | **0.1.3-alpha.1**（commit d347e70） | GitHub master，PR 编号已到 #3554+ |

- **入场券②复核：仍不满足**，且上游活跃度（PR #3109→#3558 在一天内合并）说明 alpha 期会持续。Stage 3 计划不变，借鉴产出不受影响（守卫 G1/G5）。
- **对已交付借鉴的增量提示**：`session-persistence-jsonl` 于 9-04 合并跨进程写锁（flock / named semaphore，PR #3362）——**D500（事件溯源）应对表此项**：Synova 多进程写同一会话日志的场景（如有）需同类锁语义；其余可见改动集中在 client/web UI 与 attachment（通用文件上传）、skill 模糊发现，不动我们的借鉴面。
- 重验程序：沿用 0114 §10，每次启动新借鉴卡前 `git ls-remote` 对照版本；锚点失效的卡局部刷新。

## 2. 调研方法与无遗漏保证

- **盘点对象 = 224 个实装包**（桌面端 node_modules 全集，权威于 CLI 的 80 依赖清单），逐一读 package.json + 重点包 lib/*.js 全文（esbuild 产物含 `//#region lib/xxx.js` 源路径标记，可核）。
- **五值强制处置**：借鉴-范式 65 / 接缝-预留 28 / 替换-候选 6 / 不适用 70 / 基建-无需 55（脚本校验：无重复、对照 ls 零缺失）——**每一包必须归属一个处置，不允许"未分类"**，这就是"确保没有遗漏"的物理机制（附录 A）。
- **遗漏二次扫描**：16 个名字不起眼但确认有价值的包单列（附录 A §三：output-retention、repeat-tool-reminder、session-reference、session-checkpoint-policy、hook-protocol、atomic-write、deque、session-stats、launch-environment、storage-domain、subprocess、time-context、goal-round-driver、fs-observation-policy、session-telemetry、session-query-sqlite）；反向结论 8 包确认无价值（tmux/demo/平台专属等）。
- 机制级证据：附录 B，8 大机制 + 4 速览，锚点带行号（如 `Session.append` L483-532、`compactSurfaceRegion` L419、`httpErrorCode` L1512）；仅 2 处标注"推断"。

## 3. A 组借鉴卡（Stage 1.5 · 立即可做 · 零依赖 · 每张独立 PR）

> 通用验收前缀（**D808 更新：五项全过才算通过**）：
> ① 零桥接引用：`grep -rn "@deepseek-ai" src/ packages/ --include="*.ts" --include="*.json" | grep -v node_modules` 零结果（守卫 G1 → §10.2）；
> ② vitest 三路径（正常/降级/边界，铁律 48）；
> ③ **回写验收点 + 落绑定证据**（M2 → §11.1）：`python3 scripts/product-lines/evidence-writer.py --type <test|scenario|ci|founder_demo> --verdict pass --points <点id> --source <测试/场景> --quote <佐证>`——未回写 = **不计通过**；
> ④ 抄码处锚点注释 = 包 + 文件 + 符号，并附现验命令（M3 → §10.3）；
> ⑤ 落点已归属某条线的 `modules`（§12）——未归属的落点**不得开卡**（无归属 = 无从回写）。

### B-01 LLM 稳定错误码 taxonomy
- **DSH 锚点**：`dsh-llm lib/types/error.js`（`CONTEXT_WINDOW_EXCEEDED_CODE/QUOTA_EXCEEDED_CODE/EMPTY_RESPONSE_CODE/INVALID_CREDENTIAL_CODE` + `isXxxError` 守卫族）+ `lib/types/adapter-failure.js`（`normalizeLlmFailure`：任意 throw → frozen `{message, code, status?, providerRetryAfterMs?}`）。设计注释原文："route on this, never by parsing message"。
- **Synova 落点**：`src/llm/` + `src/providers/` 错误分类收敛（对照铁律 32 `.code+.phase+.retryable`——DSH 给了现成词汇表）。
- **产品缺口**：模型底座 0%。**预估**：2-3 天。**验收**：错误码单测 + 一次真实 429/超限路径分类正确。

### B-02 重试策略配置化 + 投影化
- **DSH 锚点**：`dsh-llm-retry lib/index.js`（186 行全读：`inject=["agents","sessionProjections"]`、退避 `initialDelayMs*2^(retry-1)` 截断 maxDelayMs ± jitter、**先 append 意图再等待再 append started**）；`dsh-llm lib/types/retry-policy.js`（`RetryPolicySchema`：maxRetries=5、retryableCodes 默认 `['EMPTY_RESPONSE','RATE_LIMIT','SERVER','TIMEOUT','TRANSPORT']`、策略归 provider 配置所有）。
- **Synova 落点**：`src/providers/` 重试配置化；**重试计数进 D500 事件流（投影 `llmRetry` 范式）**——跨进程恢复、可审计。
- **产品缺口**：模型底座 0% + 自诊断可信度。**预估**：3-4 天。**验收**：重试事件回放一致。

### B-03 token 计量四桶 + 成本护栏
- **DSH 锚点**：`dsh-token-meter lib/types/estimate.js`（`CHARS_PER_TOKEN=4` + block overhead 4）+ `usage-projection.js`/`surface-projection.js`（四桶 `uncachedInput/output/cacheRead/cacheWrite`、`projectedTokens` 外推、plan/commit 两段式）。
- **Synova 落点**：诊断管线计量 + 预算护栏（超限 → Sentinel 告警）。
- **产品缺口**：模型底座与成本策略 0%（对应此前风险分析中"成本不可预料"项：单次诊断 6 专家并发 × 多轮工具 × 长上下文，无预算护栏则成本失控）。**预估**：4-5 天。**验收**：诊断报告带 token 四桶报表；注入超预算用例触发告警。

### B-04 工具结果修剪器
- **DSH 锚点**：`dsh-compaction-tool-result-pruner lib/index.js`（197 行全读：`DEFAULTS{thresholdChars:8192, headChars:4096, tailChars:1024}`、`PRUNE_MARKER`、`codePointLength` 码点计量中文友好、"replay-safe" 派生式修剪）。
- **Synova 落点**：诊断证据块/工具结果进提示词前的瘦身层。
- **产品缺口**：对话交互 0% + 报告体系 0%。**预估**：2 天。**验收**：修剪幂等（同输入同输出）+ 非文本 block 保留。

### B-05 上下文压缩引擎四件套
- **DSH 锚点**：`dsh-compaction-basic lib/types/{config,region,summarizer}.js`（threshold 0.8 / retain 0.16、压缩事件 `compaction/start|summary|end` + **影子价格 `shadowedTokenCount`**、`compaction/start` 兼 durable 锁 + 表面稳定复查、摘要调用**复用会话前缀保 KV-cache**、溢出恢复臂 `CONTEXT_WINDOW_EXCEEDED → 强制压缩 → retry`）。
- **Synova 落点**：ConversationEngine 长会话（sliding-window 之上升级）。
- **产品缺口**：对话交互 0%。**预估**：1 周。**验收**：压缩后回放一致 + tool 配对不拆散。

### B-06 协作式超时三层
- **DSH 锚点**：`dsh-timeout lib/index.js`（`clampTimeout/deadline/idleWatchdog`、`MAX_TIMER_DELAY_MS=2147483647` 防 Node 定时器溢出，实证 L27、"只计时分类不终止"）+ `dsh-tool-call-timeout-policy`（超时**返回 `TOOL_TIMEOUT` 结果**而非抛异常，协作式取消）。
- **Synova 落点**：`toolTimeoutMs` 治理（现有实现对照升级）。
- **产品缺口**：自诊断与稳定性 17%。**预估**：2-3 天。**验收**：超时路径返回分类结果码。

### B-07 会话投影注册表
- **DSH 锚点**：`dsh-session-projection lib/index.js`（413 行：`register({key, stateVersion, init, apply})`、checkpoint `{seq, ver, state}`、ver 不匹配自动全量重放 `restoreFloor`）。
- **Synova 落点**：`src/store/`（D500 事件溯源的直接配套——token/统计/调度态/goal 全部同构为投影）。
- **产品缺口**：Agent 运行底座 17% → 派生状态统一。**预估**：3-4 天。**验收**：≥2 个投影 stateVersion 版本化 + 重放恢复。

### B-08 原子写 + 文件锁
- **DSH 锚点**：`dsh-atomic-write`（`writeFileAtomic`：独占创建随机后缀临时文件 + 带权限 rename；`withFileLock` 跨进程锁）。
- **Synova 落点**：`scripts/control-tower/` 状态文件、备份脚本（铁律 0-3/0-4 直接可用）。
- **产品缺口**：治理可靠性。**预估**：1 天。**验收**：并发写测试零撕裂。

## 4. B 组借鉴卡（Stage 2 设计期输入 · 简卡，锚点详见附录 B）

| 卡 | 借鉴物（DSH 锚点） | Synova 落点 | 产品缺口 |
|---|---|---|---|
| B-09 | **preset=目录**：`agent.cordis.yml` 组合行 + `preset.yml` 元数据 + `mountPreset` + **`leakedServices` 泄漏审计**（dsh-agent-presets types/{discovery,mount,authoring}.js） | **CFG 客户配置包的机制蓝本**（§5.4 四层叠加 = scope layering）；诊断/导航模式映射 | 插件化扩展 0% |
| B-10 | **配置三层+可 dump**：`mergeLayers`（对象递归/数组整值替换）、namespace 注册制、`patchNode` 注释保留叶子级 patch、CLI `--dump-config` 逐层带来源（dsh-settings/dsh-settings-file/dsh lib/dump-config-*.js） | CFG 四层叠加实现蓝本 + 支持诊断第一命令 | 权限治理 0% |
| B-11 | **MCP client 双 transport**：stdio/StreamableHTTP + 工具表变更通知重载 + `scrubbedParentEnv`（dsh-mcp-client lib/index.js） | Synova 作为 client 连企业系统（Stage 2 契约） | MCP 集成 0% |
| B-12 | **webhook 事件触发路**：source/rule/delivery 三 id、delivery 校验+deepFreeze、provenance 注入 `source:{kind:"webhook",...}`、GitHub HMAC 验签（dsh-webhook + dsh-webhook-github） | 外部事件触发诊断（与 Sentinel cron 互补）；企业系统接入 | 告警推送 0% |
| B-13 | **goal 事件溯源状态机四件套**：`foldGoal/GOAL_CHANGE_VERSION` 投影 + `goal-round-driver` 竞态围栏与续轮提示 + `tool-goal` 执行时 authority（dsh-goal 族） | 目标导航与行动闭环 0%；Sentinel 工单状态机对照 | 目标导航 0% |
| B-14 | **消息级反馈 sidecar**：域 spec + schema 族 + 会话生命周期指纹围栏（`createdAt+cwd`，反馈永不污染错误会话）（dsh-message-feedback types/{spec,types}.js） | E1 数据回流（N13 闭环）的定位粒度 | 进化闭环 0% |
| B-15 | **权限预设写穿**：预设=旋钮打包（sandbox-mode+approval-policy），写穿到会话事件，**预设不成为第二真相源**（dsh-permission-presets） | 五角色 RBAC 的部署档位 | 权限治理 0% |
| B-16 | **不可信上下文标注**：跨会话引用 URI 编解码/大小上限/候选限流 + 不可信模型上下文标记（dsh-session-reference） | prompt-injection 防线（安全与信任 17%→深） | 安全与信任 |
| B-17 | **skill provider 两阶段触发**：dispatcher 主动发现 + 模型主动拉取（dsh-skill/skill-filesystem/tool-skill） | skills/ 体系升级对照 | 插件化扩展 0% |
| B-18 | **运行时不变量注册表**：`InvariantRegistry` + "检查可选、违约必炸"（dsh-invariants 123 行） | 铁律运行时执法层（pre-commit 静态之外：五层边界/降级信号在场性） | Agent 运行底座 |
| B-19 | **持久化分级哲学**：提醒/会话走日志（冷恢复免费），伴随会话的长跑作业走内存——"按死后是否需要复活选路线"（dsh-schedule vs dsh-jobs 对照） | Sentinel 工单/哨兵任务的存储选型准则 | 持续监测 25% |
| B-20 | **速览族**：截断诚实披露（output-retention）、重复调用防呆（repeat-tool-reminder）、敏感环境擦除（subprocess `scrubbedParentEnv`）、时间上下文注入（time-context）、会话 FTS5 全文检索（session-query-sqlite）、副作用前检查点（session-checkpoint-policy） | 各对应一个小缺口，按需取用 | 分散 |

## 5. C 组：Stage 3 壳试点（替换-候选，入场券后）

`dsh`（CLI 入口）+ `dsh-base`（基座 bundle，85 deps）起步；按形态加 `dsh-headless`（无头批量诊断 runner）或 `dsh-web-app`（Web GUI）；`dsh-sdk-minimal`/`dsh-sdk-app` 为 SDK 消费形态备选。**六包之外所有"接缝-预留 28 项"在 Stage 2 只定契约词汇不写实现**（附录 A 有逐包对接注记）。

## 6. 五条硬守卫（借鉴不翻车的全部纪律）

1. **G1 零桥接引用**（M3 口径）：借鉴 = 读 DSH 源码自研，**或直接抄码进本仓**（抄进来即自有代码，可改可测）；**禁**的是 `import`/`require` `@deepseek-ai/*` 与 re-export 壳。验证命令：`grep -rn "@deepseek-ai" src/ packages/ --include="*.ts" --include="*.json" | grep -v node_modules` 零结果（**2026-09-17 实测 0 命中**；建议固化为 `scripts/check-no-dsh-deps.sh` 并入 npm run check 族）。壳判定照铁律 46（非 import/export/注释的有效代码行 = 0 = 纯桥接）。
2. **G2 独立 PR + 接线审计**：每卡单独 PR；grep 调用方 + 集成测试走真实路由（4 次接线失败史）。
3. **G3 守卫物理化 · 回写验收点 + 绑定证据**（**2026-09-17 起，M2**）：每卡必须回写到 ≥1 个 26 线验收点，并生成**绑定该点 id 的证据文件**；**未回写、或无绑定证据者，该卡不计通过**——禁止"为借鉴而借鉴"（engine-core 事故教训：无验收标准的迁移 = 欺诈温床）。三个物理动作见 §11.1。
4. **G4 grep 物理证明 + 可回退**：声称借鉴完成必须 grep/测试物理证明（铁律 47）；每个机制带 enable/disable 开关。
5. **G5 Stage 3 前零运行时依赖**：入场券三条不满足前，`package.json` 不引 `@deepseek-ai/*`、运行时不靠外部 DSH 包承载；DSH 方向变了随时弃坑，核心零伤。**与 M3 不冲突**：禁的是"依赖 / 桥接"，不是"抄码"——抄进本仓的代码是我们的代码。判据：删掉 `node_modules/@deepseek-ai` 后本仓仍能构建、仍能跑测试。

## 7. 对 DOC-0114 / DOC-0116 的修订建议

| # | 修订 | 依据 |
|---|---|---|
| 1 | 0114 §4 映射表"定时调度→Stage 1 借鉴冷恢复"**未落卡**（0116 六卡无此卡）→ 由本指引 B-07（投影注册表）+ B-19（持久化分级）覆盖，0116 增补 S1-7 卡 | 图纸内部缺口 |
| 2 | 0114 §5.2 矩阵新增 **#19 配置与凭证表面**（客户看的表面=品牌层自建；运营者表面=DSH 原生复用；key 流转=壳 UI → 核心 vault 单向下发）、**#20 产品模式机制**（诊断/导航 ↔ preset/persona，`mode` 入 MCP 契约）→ 机制蓝本 = 本指引 B-09/B-10 | 创始人 9-04 问询 |
| 3 | 0116 S1-5（D5）/S1-6（CFG）仍无认领——**CFG 的实现蓝本就是 B-09/B-10**，认领时直接引用本指引 | 命门卡空转 |
| 4 | 0116 增补"**Stage 1.5 借鉴卡序列**"= 本指引 A 组 B-01~B-08（认领制/门禁/接线审计同规则） | 借鉴系统化 |
| 5 | 0114 §8 风险清单 R1 增量：上游 15 天 3 版本线（rc.5→0.1.2-alpha.2→0.1.3-alpha.1），R9 第三问（值不值一个额外进程）维持"待 Stage 2.5 spike 数据" | 本文 §1 |
| 6 | 0114 附注 D500 对表项：session-persistence-jsonl 跨进程写锁（PR #3362，9-4 合并） | 本文 §1 |
| 7 | **0114「红线 1 = 绝不引入代码」表述作废**，改按 M3：抄码允许且鼓励、禁桥接引用（0114 正文与 §8 风险清单需同步引用本指引 §10） | 创始人 2026-09-17 裁定（M3）+ 本文 §10 |

## 8. 执行顺序建议

1. **本周**：D573 分支合并（PR 流程）→ B-01（错误码）+ B-04（修剪器）开卡——两者最小、直接改善诊断链路健壮性。
2. **下周起**：B-02 → B-03（成本护栏，哇呢宝贝部署前应有）→ B-06 → B-08。
3. **Stage 2 启动时**：B-07 打底 + CFG 认领（机制蓝本 B-09/B-10）+ Stage 2.5 spike（两周垂直验证）。
4. **持续**：B 组其余卡在对应产品线启动时取用；C 组冻结至入场券。

## 9. 残余不确定性（诚实声明）

1. 本指引锚点基于桌面端**实装产物**（0.1.2-alpha.2）+ 上游 master 树（d347e70），上游继续演进后个别行号/文件名会漂移——每卡开工前按 §1 重验程序核对锚点，失效局部刷新。
2. 附录 B 仅 2 处标注"推断"（Cordis 作用域优先级语义、webhook 可靠性兜底=GitHub 重投），写卡实现时如依赖这两点需先实测。
3. 本指引未经 K3 审计线独立验证——按协作协议够格进入评审，不应跳过审计。
4. 224 包处置为双人独立调研交叉（全包表 + 机制深潜），但"借鉴-范式 65"的逐包优先级仍需在认领时按产品线节奏二次裁剪。
5. §10/§11/§12 的三条元断言（M1/M2/M3）**逐字取自创始人 2026-09-17 裁定**（本文不改写、不重排标点）；V1 v0.2 文件落库后按**附录 C** 的核验命令对两侧逐字复核——对不上 = 措辞漂移，回改本文。
6. §11.3「建议验收点」是 D808 建的**待确认表**（按 V1 断言语义推得），最终点 id 由卡主在线主确认后落 `product-lines.yaml`；本表**不代替**回写动作。

## 10. 合规口径（创始人 2026-09-17 裁定 · 元断言 M3）

**M3 原文（逐字引用，禁改一字一标点 · 出处：创始人 2026-09-17 裁定）**

```text
M3（合规边界 · 创始人 2026-09-17 裁定）
抄代码 = 允许且鼓励（对方实现更优时，该抄就抄）；
桥接引用 = 禁止（禁 import @deepseek-ai/*、禁 re-export 壳）；
抄写处必须留锚点注释（包 + 文件 + 符号）+ 现验命令（行号现查）。
```

### 10.1 允许：抄代码（鼓励）
- 读 DSH 源码后，**可以整段把实现搬进本仓**（也可改写 / 本地化）——对方实现更好就该抄；抄进来即**自有代码**，可改、可测、可演进。
- 无隐性依赖判据：删掉 `node_modules/@deepseek-ai`（或用一台没装 DSH 的机器）后，本仓仍能构建、仍能跑测试。

### 10.2 禁止：桥接引用（红线；违反 = 该卡不计通过）
| # | 禁止形态 | 判定标准 | 现验命令（2026-09-17 实测） |
|---|---|---|---|
| 1 | `import` / `require` `@deepseek-ai/*` | `src/`、`packages/` 下任意 `.ts` / `.tsx` / `.json` 出现该串（含相对路径穿透 / `file:` 形态） | `grep -rn "@deepseek-ai" src/ packages/ --include="*.ts" --include="*.json" \| grep -v node_modules` → **实测 0 命中** |
| 2 | re-export 壳（桥接代理文件） | **铁律 46 判定标准**：文件内非 import/export/注释的**有效代码行 = 0** = 纯桥接；有原创代码但仍直接 import DSH 包 = 部分桥接 | 铁律 46 同款检查（pre-commit 第 5 组已在跑同族规则） |
| 3 | 抄了不标出处 | 抄写处无锚点注释 = 违反 M3 第三句 | 见 §10.3 注释模板 |

**与旧口径的关系**：本文原 §6 G1「借鉴止于范式，绝不引入代码依赖」（DOC-0114 红线 1）的表述**作废**，以 M3 为准；G1 的物理检查（零 `@deepseek-ai` 引用）保留并继续跑。

### 10.3 抄的必须留：锚点注释（包 + 文件 + 符号）+ 现验命令
按 **D797 锚点三原则**（2026-09-17 生效；出处 `docs/synova/coordination/DSH锚点重锚审计-20260917.md` §三）：

1. 只锚 **包 + 文件 + 符号**（行号随版本漂移，**符号优先**）；
2. 给锚点必须**附现验命令**；
3. **现验失败 = 该借鉴作废，重找**，不许"差不多就抄"。

注释模板（抄写处文件头 / 符号上方）：
```ts
/**
 * 借鉴源锚点（M3 · 2026-09-17 起）:
 *   包   @deepseek-ai/dsh-llm
 *   文件 lib/types/error.js
 *   符号 CONTEXT_WINDOW_EXCEEDED_CODE / normalizeLlmFailure
 * 现验: f="$DSH/node_modules/@deepseek-ai/dsh-llm/lib/types/error.js"; [ -f "$f" ] && grep -n 'CONTEXT_WINDOW_EXCEEDED_CODE' "$f"
 * 抄写 2026-09-17 ｜ 抄写时版本 0.1.6-alpha.1 ｜ 行为差异: 无（原样）
 */
```
现验命令自检（2026-09-17 实测，可复现）：`文件 ✓ 152 行` + `符号 ✓ 22:export const CONTEXT_WINDOW_EXCEEDED_CODE = 'CONTEXT_WINDOW_EXCEEDED';`。

> ⚠️ **现行安装 = `deepseek-harness-pkg 0.1.6-alpha.1`（2026-09-17 02:01，目录 mtime 实测）**；本文内所有历史 `file:line` **禁止照抄**——一律按上式现查（行号现取）。

### 10.4 存量缺口（诚实登记 → 另立编码卡）
旧口径下交付的锚点注释引的是 **Win 开发路径 + 旧版本**，按 M3 需补"包名 + 符号 + 现验命令"：

| 文件 | 现状锚点（旧口径） | M3 缺口 |
|---|---|---|
| `src/llm/tool-result-pruner.ts` | `D:/deepseek-harness/packages/compaction/compaction-tool-result-pruner/lib/index.js (0.1.1-rc.2)` | 缺包名、无现验命令、版本过时 |
| `src/llm/timeout.ts` | 同型（Win 路径 + 旧版本） | 同上 |
| `src/agent/context-compaction.ts` | 同型 | 同上 |
| `src/store/session-projection.ts` | 同型 | 同上 |

实测（2026-09-17）：`grep -rln "deepseek-harness/packages\|D:/deepseek-harness" src/ --include="*.ts"` = **4 文件**。
**处置**：补锚点属 `src/` 代码改动 → **另立编码卡**（本文只登记、不改码——CTO 不写产品代码）。

## 11. G3 守卫物理化：借鉴卡必须回写验收点 + 生成绑定证据（元断言 M2）

**M2 原文（逐字引用，禁改一字一标点 · 出处：创始人 2026-09-17 裁定）**

```text
M2（G3 物理化 · 借鉴卡必须回写台账）
每张 DSH 借鉴卡（B-01…B-20）必须回写到至少一个 26 线验收点，并生成绑定该点 id 的证据文件；
未回写、或无绑定证据者，该卡不计通过。
```

### 11.1 「回写」= 三个物理动作（缺一不算回写）
| # | 动作 | 命令 / 落点 | 判据 |
|---|---|---|---|
| 1 | **改 yaml**：把落点写成可判定的验收点 | `docs/synova/product-lines/product-lines.yaml` 对应线 `acceptance_points` | desc 按 **M1** 写「接入/配置/约束 DSH 的 <能力> 并验证」，不写「实现 X」 |
| 2 | **落证据**：生成绑定该点 id 的证据文件 | `python3 scripts/product-lines/evidence-writer.py --type <test\|scenario\|ci\|founder_demo> --verdict pass --points <点id> --source <测试/场景> --quote <佐证>` → 写 `docs/synova/product-lines/evidence/<type>-<日期>.json` | D808 现验实测可跑：`已写入证据: …（test / 1 个验收点 / 结论=pass）` |
| 3 | **绑定核验** | 证据文件内 `verdicts[].acceptance_point` == 点 id，且 `verdict = pass` | schema 见 D774 机器证据样本（`test-2026-09-16.json`） |

### 11.2 为什么"git 有、看板没有"（D808 物理取证 · 2026-09-17）
看板**只读「证据 × 验收点」绑定**（`calc-progress.py` 按 evidence 的 `acceptance_point` 裁决；`ledger.json` 同源），**不读代码存在性**。

| 物证 | 实测（可复现） |
|---|---|
| 借鉴卡代码已在 main | B-01（D586 56c4dd1d：`src/errors/types.ts`、`src/providers/base.ts`）/ B-02+B-06（D593 14e54722：`src/llm/retry-middleware.ts`、`src/llm/timeout.ts`）/ B-03（D598 4c74faf0：`src/llm/token-meter.ts`）/ B-04（D587 75f13b56：`src/llm/tool-result-pruner.ts`）/ B-05（D594 f509b3cf：`src/agent/context-compaction.ts`）/ B-07（D588 eebd9c99：`src/store/session-projection.ts`）/ B-09+B-10（D599 95a71a8f：`src/config/{config-layers,customer-config-package}.ts`）——`git log --oneline -- <落点>` 可复现 |
| 这些卡**零绑定证据** | `docs/synova/product-lines/evidence/` 全目录 grep「借鉴 / B-0 / D587 / D593 / D598 / D599」→ **仅 README.md 命中**（无任何一张卡的证据记录）；41 个 pass 点里**不含 21-3（成本可测量）、20-3（上下文策略）**——21-1/21-2/22-1 的证据来自 `run-machine-evidence.sh` 通用重跑（`source` 字段实测），不是卡的回写 |
| 看板结果 | `ledger.json`（2026-09-17 01:26）：`v1_total=125 / v1_passed=21 / delivery_pct=16.8%`；`product-progress.json`（2026-09-16 19:00）：**线20/21/22 = 0%** |
| A1 机制也救不了 | "modules 路径有变更 → 该线证据失效转黄"的前提是**先有绑定**；没有绑定 = 没有可失效的对象（且这些落点至今未归属任何线，见 §12.2） |

**结论：代码进 git ≠ 验收点通过。** 借鉴卡的"交付"以**回写 + 绑定证据**为准。

### 11.3 回写清单（B-01…B-20；D808 建表，「建议验收点」待卡主确认）
| 卡 | 落点（§3/§4） | 已落代码（main 实测） | 建议验收点 | 回写状态 |
|---|---|---|---|---|
| B-01 LLM 错误码 | `src/llm/` + `src/providers/` | ✓ D586 | 21-1 / 22-1 | ✗ 未回写 |
| B-02 重试配置化 | `src/providers/` | ✓ D593（实际落 `src/llm/retry-middleware.ts`） | 22-1 | ✗ |
| B-03 token 四桶 | `src/llm/` + `src/providers/` | ✓ D598 | **21-3（成本可测量）** | ✗（21-3 零证据） |
| B-04 工具结果修剪 | `src/llm/` | ✓ D587 | 20-3 / 21-4 | ✗ |
| B-05 上下文压缩 | `src/agent/` | ✓ D594 | 20-3（长会话不爆窗） | ✗ |
| B-06 超时三层 | `src/llm/` | ✓ D593（`src/llm/timeout.ts`） | 22-1 | ✗ |
| B-07 会话投影注册表 | `src/store/` | ✓ D588 | 20-1（会话状态机） | ✗ |
| B-08 原子写 + 文件锁 | `scripts/control-tower/` | ✗ 未落（`grep -rn "writeFileAtomic\|withFileLock" src/ scripts/` 零命中） | 无 V1 点直连 → 先落线归属（§12） | ✗ |
| B-09/B-10 CFG 配置 | `src/config/` | ✓ D599 | 待定（线23/25 归属待确认，§12.2） | ✗ |
| B-11 MCP client | `src/mcp/` | ✗（卡已登记 D760，`claimed`） | 26-1 / 26-2 | ✗ |
| B-12 webhook 触发路 | `src/sentinel/` / `src/services/` | ✗（D761 `claimed`） | 8-2 / 8-6 | ✗ |
| B-13 goal 状态机 | `src/growth/` | ✗（D762 `claimed`） | 16-1 / 16-4 | ✗ |
| B-14 消息反馈 sidecar | `src/evolution/` | ✗（D763 `claimed`） | 17-1 | ✗ |
| B-15 权限预设写穿 | `src/infra/` + `security/` | ✗（D764 `claimed`） | 23-1 / 23-2 | ✗ |
| B-16 不可信上下文标注 | `security/` | ✗（D765 `claimed`） | 24-3（防注入） | ✗ |
| B-17 skill provider 两阶段 | `skills/` + `src/l3/` | ✗（D766 `claimed`） | 25-2（技能插件） | ✗ |
| B-18 运行时不变量注册表 | `src/infra/` / `src/l2/` | ✗（D767 `claimed`） | 22-1 | ✗ |
| B-19 持久化分级 | `src/store/` / `src/cron/` | ✗（D768 `claimed`） | 7-3（重启不丢） | ✗ |
| B-20 速览族（6 项） | 分散 | ✗（**D769 未登记**） | 按项定 | ✗ |

（"已落代码" = `git log -- <落点>` 有该卡提交；"未回写" = evidence 目录无该点绑定记录。本表把缺口摆到明面，**不代替**卡主回写。）
**当前全表 0/20 回写** —— 按 M2，**今天没有任何一张借鉴卡"通过"**。

## 12. 落点归属：`modules` 归属规则 + `product-lines.yaml` 同步（元断言 M1）

**M1 原文（逐字引用，禁改一字一标点 · 出处：创始人 2026-09-17 裁定）**

```text
M1（DSH 复用口径 · 全 26 线适用）
凡某验收点的能力与现行 DSH 已有能力重叠时，该点必须写成
「接入/配置/约束 DSH 的 <能力> 并验证」，不得写成「实现 <能力>」。
判定：该点的 verify 须同时指向 ①DSH 侧配置/接入点 ②本仓的验证断言两段；
只写「实现 X」的点视为口径不合格，不计通过。
```

### 12.1 规则（R1–R6，即刻适用）
- **R1 单一归属**：一个落点（文件/目录）必须归属到**恰好一条**线的 `modules`；两条线都要 = 先裁决归属，再开卡。
- **R2 归属判据 = 该落点服务的验收点所在线**（不是"代码像哪一层 / 在哪个架构目录"）。先答一句："这张卡让哪条 V1 断言的 `fail_when` 不再成立？"——答不出 = **无落点，退回设计**。
- **R3 `modules` 值必须是真实存在、且能被 git pathspec 命中的路径前缀**——`calc-progress.py` 用 `git log --since=<证据日期> --name-only -- <modules>` 做 A1 惰性失效；**写错路径 = 该线的失效检查静默空转**（物证见 §12.2）。
- **R4 新增目录 / 新落点必须同 PR 同步 `product-lines.yaml`**（线集增删只有创始人能改，但**落点归属**是技术事实，可由卡主在 PR 内落位）——否则 M2 无从回写。
- **R5 不得整目录挂靠**：混合目录（如 `src/services/` 30 文件跨多线）按**文件或子目录**粒度归属。
- **R6 未归属 = 不得开卡**（开了也过不了 M2）。

### 12.2 现状（2026-09-17 实测）+ 归属草案
| 落点 | 现状 | 归属草案 | 依据（服务的验收点） | 需做的 yaml 动作 |
|---|---|---|---|---|
| `src/llm/` | 7 文件存在；**没有任何线的 `modules` 包含它** → A1 永不触发 | 线21 模型底座（辅：线20） | 21-3 成本可测量（token 预算/缓存）、21-4 长上下文策略 | 线21 `modules` 追加 `"src/llm/"` |
| `src/store/` | 6 文件；零归属 | 线20 Agent 运行底座 | 20-1 会话状态机（`session-store` / `session-projection`） | 线20 `modules` 追加 `"src/store/"` |
| `src/config/` | 2 文件（B-09/B-10 落点）；零归属 | **待裁决**（候选：线23 权限治理 / 线25 插件化） | B-09/B-10 = CFG 客户配置包机制蓝本 | 先定线，再追加 `"src/config/"` |
| `src/services/` | 30 文件；零归属 | **按文件粒度**（不整目录挂靠，R5） | 逐文件判定 | 逐条追加（示例见下） |
| `src/providers/` | 12 文件；yaml 写的是 `"providers/"`（**该目录不存在**） | 线21 | 21-1 DeepSeek 适配器 | 把 `"providers/"` 改为 `"src/providers/"`（线20、线21 的 `modules` 各一处） |

**物证 · paths 写法错误 = 失效检查空转**（2026-09-17 实测）：
```bash
git log --oneline --name-only -- providers/      # yaml 现写值 → 零输出（该路径不存在）
git log --oneline --name-only -- src/providers/  # 真实目录   → 命中 D586 / D598（B-01 / B-03 落点）
git log --oneline -- src/llm/                    # 有 4 次提交（D587/D593/D598 等），但无任何线 modules 命中它
```
`src/services/` 粒度示例（三例，非穷举）：`deepseek-balance.ts` → 线21（成本）；`audit-service.ts` → 线24（安全与信任）；`db-encryption.ts` → 线24（本地数据不出门）。

> 说明：§12.2 是**归属草案**（技术判据已给全）；线级归属由 CTO 与线主确认后落 `product-lines.yaml`。**未归属的落点不得开借鉴卡**（M2 无法回写 = 不计通过）。

## 附录 C：与 V1 v0.2 元断言的一致性核对表（D808）

> **元断言来源**：创始人 **2026-09-17 裁定原文**（M1 / M2 / M3）。本文引用时**逐字**（含标点、换行），不改写、不重排、不合并——"同一句话两处措辞相同，不许各写各的"。
> **一致性 = 本文引文块 与 V1 v0.2 文件内对应段 逐字相同**；V1 v0.2 落库后跑表末命令对表（`grep -F` 计数命中两文件）。

| # | V1 元断言 | 本文条款（引文位置） | 引用形态 | 一致性判据 | 实测 |
|---|---|---|---|---|---|
| 1 | **M1**（DSH 复用口径 · 全 26 线适用） | §12 章首引文块 | `text` 代码块，4 行原文 | 与 V1 v0.2 的 M1 段逐字相同 | 本文已逐字引用创始人原文；与 v0.2 对表待其落库 |
| 2 | **M2**（G3 物理化 · 借鉴卡必须回写台账） | §11 章首引文块 | `text` 代码块，2 行原文 | 同 M1 | 同上 |
| 3 | **M3**（合规边界 · 创始人 2026-09-17 裁定） | §10 章首引文块 | `text` 代码块，3 行原文 | 同 M1 | 同上 |
| 4 | M2 → G3 守卫 | §6 G3（改写为物理化口径） | 条件句改写（**不含**引文） | 语义等价：回写 + 绑定证据 + 未回写不计通过 | §11.1 三动作可操作化 |
| 5 | M3 → G1 守卫 | §6 G1（零桥接引用） | 条件句改写 | 语义等价：禁 import / re-export 壳，**不禁抄码** | 现验命令实测 0 命中 |
| 6 | M3 → G5 守卫 | §6 G5（零运行时依赖 + 与 M3 不冲突） | 条件句改写 | 语义等价：禁依赖 ≠ 禁抄码 | 判据：删 `node_modules/@deepseek-ai` 仍可构建 |
| 7 | M3 第三句 → 锚点要求 | §10.3（D797 锚点三原则 + 注释模板 + 现验命令） | 引 D797 原文三条 | 逐条对应"包+文件+符号 / 现验命令 / 行号现查" | 现验 demo 实测：文件 ✓ 152 行 / 符号 ✓ L22 |
| 8 | M2 → 回写清单 | §11.3（B-01…B-20 × 验收点） | 表格 | 每卡有落点 + 建议点 + 回写状态；未回写者明确标 ✗ | 全表 **0/20 回写** |
| 9 | M1 → 落点归属 | §12.1 R2 / R4 / R5 | 规则条款 | R2 = 按"服务的验收点"归属（M1 的落点侧） | 归属草案见 §12.2 |
| 10 | 口径作废声明 | 文首最高约束行 + §10.2 注 + §7 表第 7 行 | 声明 | 旧「绝不引入代码依赖」（DOC-0114 红线 1）作废，以 M3 为准 | 本文已改；0114 待同步 |

**核验命令**（V1 v0.2 落库后执行）：
```bash
GUIDE=docs/synova/research/DSH迁移施工图-20260820/DSH借鉴指引-v2-20260904.md
V02=docs/synova/project/26线-V1验收标准-v0.2.md   # 落库名以 V1 v0.2 实际文件名为准
# 每句在本指引内应命中 2 次（1 = §10/§11/§12 引文块，1 = 本核验命令自身）；在 V1 v0.2 内应命中 1 次
while IFS= read -r s; do
  printf '%s → 本指引 %s / V1v0.2 %s（期望 2 / 1）\n' "${s:0:24}…" "$(grep -Fc "$s" "$GUIDE")" "$(grep -Fc "$s" "$V02")"
done <<'EOS'
抄代码 = 允许且鼓励（对方实现更优时，该抄就抄）；
每张 DSH 借鉴卡（B-01…B-20）必须回写到至少一个 26 线验收点，并生成绑定该点 id 的证据文件；
凡某验收点的能力与现行 DSH 已有能力重叠时，该点必须写成
EOS
# 任一条不是 2 / 1 = 措辞漂移，回改本文
```

> 本表**自我一致**已核（D808 实测 2026-09-17）：三个引文块与创始人裁定原文**byte 级逐字相同**（提取代码块比对：`M1 ✅ / M2 ✅ / M3 ✅`）；"与 V1 v0.2 逐字一致"待 v0.2 落库后按上式补跑——**未跑前不得声称"已对齐 v0.2"**。

---

> 定稿记录：2026-09-04 创始人发起（"DSH 已多次改版，需要新指引确保无遗漏"）→ 双子代理并行调研（224 包处置表 + 8 机制深潜）→ DSH 汇总定稿。版本基线：桌面 0.1.2-alpha.2 / 上游 0.1.3-alpha.1。
> **D808 更新记录（2026-09-17）**：创始人 9-17 裁定落为三条元断言（M1/M2/M3）→ CTO 增设 §10 / §11 / §12 + 附录 C。口径变更点 = 抄码由"禁"转"允许且鼓励"、桥接引用维持禁止、G3 由"挂台账行"升级为"回写验收点 + 绑定证据"。存量缺口（4 文件锚点、B-01…B-10 零回写、5 个落点零归属）已在 §10.4 / §11.3 / §12.2 登记，待派卡。
> 后续变更规则：上游发版 → §1 重验 → 受影响卡片局部刷新；借鉴卡交付一张在 §3/§4 标注交付 commit；**每卡交付必须同时满足 §3 通用验收前缀五项（含 M2 回写）**。
