# DSH 迁移施工图（全量）

> 登记：DOC-0114 | type: decision | status: active（2026-08-20 创始人定稿）| owner: founder
> 2026-08-20 | 分析 session：Kimi | 基线：origin/main `23b6de9d`（快照：`tmp/synova-main-ref/`）
> 借鉴操作化：**DOC-0117《DSH 借鉴指引 v2》(2026-09-04)**——224 包全量处置表 + 代码锚点级借鉴卡（A/B/C 组）+ 五条硬守卫；版本基线更新 rc.5 → 0.1.2-alpha.2（桌面实装）/ 0.1.3-alpha.1（上游 master d347e70）；§4"借鉴冷恢复"与 §5.2 #19/#20 由 0117 承接
> 上游输入：8-16 战略研究（七章+附录）、A线/C线审计结论
> 创始人已拍板：终态 = A 形态（DSH 壳 + Synova 核心独立服务 + MCP 边界）
> 摘要版：同目录《DSH施工图-一页纸大纲-20260820.md》（DOC-0115）

---

## 0. 这份图纸是什么、不是什么

**是**：把 8-16 战略研究的结论落成模块级执行清单——哪些代码死守、哪些借 DSH、哪些搬走、哪些删除；以及从现状到 A 形态终态的分阶段路径。

**不是**：立即迁移的命令。本图纸的最高约束是**哇呢宝贝种子客户部署优先**，迁移轨永远不阻塞部署轨。

**与 8-16 研究的关系与差异**：

| 项 | 8-16 研究 | 本图纸 |
|---|---|---|
| 路径 A 定义 | 引擎重建在 DSH 上，领域层作为插件 | 创始人拍板的 A 形态：核心保持独立服务，DSH 壳经 MCP 消费——侵入性更低、可逆性更强 |
| 迁移时机 | "现在不做，Q4 后评估" | 采纳。Stage 3（建立 DSH 依赖）设双重入场券 |
| 借鉴清单 | D1-D9 方向级 | 直接采用为 Stage 1 内容，不重复设计 |

---

## 1. 基线与边界

- **盘点对象**：`origin/main @ 23b6de9d` 的干净快照（`tmp/synova-main-ref/`，只读）。**不是**本地工作区——本地落后远程 258 个提交，盘点旧版本等于图纸出生即过时。
- **盘点范围**：`src/`、`packages/`、`expert/`、`scripts/`、`electron/`、`app/`、根级入口文件与根级目录。
- **执行前必须重验基线**：见 §10。main 在多 Agent 流水线里每天都有提交，执行当天 diff 一次，变动涉及已盘点模块则局部刷新对应章节。

---

## 2. 已验证事实（全部一手核验）

### 2.1 DSH 侧（源码：`D:\deepseek-harness`，关键文档已拷入 `tmp/dsh-ref/`）

| 事实 | 证据 | 含义 |
|---|---|---|
| 许可证 **MIT**，Copyright (c) 2026 DeepSeek | LICENSE 全文 | 可商用、可保留自有品牌；唯一义务是保留版权声明 |
| 版本 **v0.1.0-rc.5** 开发者预览 | package.json | 官方明示 "THERE WILL BE COMPATIBILITY-BREAKING CHANGES" |
| 一切皆插件 + Cordis 时空可组合 | architecture.md | 无特权核心，agent loop 本身也是插件、可换 |
| Profile/Bundle 组合机制 | architecture.md | 品牌与能力以分层 patch 组合——**这正是"保留自有品牌"的官方机制**：synova profile = dsh-base + 自有 bundle |
| 扩展点完整 | architecture.md "Where new behavior goes" 表 | 人类命令（ctx.commands）、后台作业（ctx.jobs）、持久状态（SessionEventMap）、MCP client 均有正式接缝 |
| 工程门禁体系 | verify-* 30+ 脚本、snapshot 测试、Agent Notes 四态 | 🟡 治理层移植的落点存在 |

### 2.2 Synova 侧（main 快照实测）

| 事实 | 数据 |
|---|---|
| 规模 | src/ 417 ts 文件 / 74,144 LOC；packages/ 12 包 / 9,038 LOC；测试 524 个；治理脚本 153 个 |
| 生产入口链 | `src/index.ts` → `SynovaAgent(db).start()` → `createServer()`（Express）+ `CronScheduler` + `SentinelRunner` |
| 对外接缝已存在 | `src/mcp/`（1,034 LOC）——Synova 已是 MCP server，**A 形态的接缝天然成立，不需要新建** |
| AGENTS.md 与代码漂移 | AGENTS.md 所述 `packages/engine-core`（543 文件）**已不存在**；本体实际在 `packages/ontology`（432）+ `packages/sog-core`（1,116）+ `packages/evolution`（2,503）；`expert/` 已文件驱动化（md+json，无 ts）。→ 施工图以代码为准，AGENTS.md 需同步修正 |
| 文件驱动加载 | `adapter-scanner`/`file-scanner`/`file-driven-loaders` 存在动态扫描——静态零引用 ≠ 死代码，删除前必须复核 |
| 部署形态 | 单容器 Docker + SQLite 卷挂载（`./data/synova.db`）——A 形态下核心服务始终持有数据库，换壳不动库 |

---

## 3. 模块四色清单（实测，LOC 为 main 快照数据）

### 🟢 死守（领域核心 + 品牌表层，继续投入，约占 60-65%）

| 模块 | LOC | 接线证据 | 说明 |
|---|---:|---|---|
| `packages/ontology` + `packages/sog-core` | 1,548 | 被 l4/agent 引用 | 42 边因果本体（池-阀-流-溢出）——护城河核心 |
| `packages/evolution` | 2,503 | routes/sentinel/l3 均消费 | L0 自我进化引擎（三层结构，已接线，见 §5.3） |
| `src/l4/` | 4,988 | 被 28 文件引用 | GraphBridge/EntityResolver/CommunityReports |
| `src/l3/` + `src/expert-platform/` + `expert/` | 6,283 | 被 11 文件引用 | 7 位专家体系（专家定义为 md+json 文件驱动） |
| `src/sentinel/` | 3,108 | 生产入口直挂 | SentinelRunner + 20 哨兵 + 信号聚合 |
| `src/growth/` | 5,274 | 被 9 文件引用 | 增长导航（Goal-Proposal-工作台）+ ActionStore + 反馈收集 |
| `src/cycles/` | 1,259 | cycle-registry 文件驱动 | **业务循环计算内核**：资本/客户循环溢出计算、跨尺度验证（防虚假乐观）、循环图桥接——基于特定场景开发，DSH 完全无对应 |
| `src/loops/` | 1,596 | 被 3 文件引用 + 消费 cron | **领域循环调度**：6循环×3尺度触发矩阵（D91）、Gate7 方向有效性监测、中层进化引擎——业务方法论，不是通用 cron |
| `src/interview/` + `src/connectors/` + `src/ingest/` | 2,689 | 文件驱动+引用 | 无数据诊断三路径（问卷/导入/连接器），含飞书桥接 |
| `src/evidence/` + `src/pipeline/` + `src/playbook/` | 1,181 | — | 证据链、诊断管道、行业模板 |
| **品牌表层**：`src/routes/` + `src/mcp/` + `app/` + `electron/` | 10,247+ | 生产入口直挂 | 客户可见的一切：API、MCP、报表/仪表盘 HTML、桌面壳。**品牌活在这一层** |
| **交互触达**：`src/notifications/` + `proactive-push` + `boss-mailbox` | ~1,000 | 飞书/桌面通知接线 | 主动推送与老板信箱——客户触达通道，属表层 |
| `src/security/`（PII 部分） | 部分 | 被 14 文件引用 | PIIScrubber 是企业场景领域合规能力 |
| **根级领域资产**：`extensions/` + `skills/` + `knowledge/` + `theory/` + `synthesis/` + `synova_worker/` | 750+ 文件 | 文件驱动加载器扫描 | 详见 §3.6——**"未来无限拓展"的物理载体全部在这里** |

### 🔵 借 DSH（通用管道，冻结新功能 → Stage 3-4 逐块替换，约占 20%）

| 模块 | LOC | DSH 对应 | 处置 |
|---|---:|---|---|
| `src/store/`（SessionStore） | 658 | core/session 事件溯源 log | Stage 1 先借鉴范式（D1），Stage 3 后换实现 |
| `src/cron/`（CronScheduler 定时基建） | 426 | schedule（冷恢复）+ goal + feedback | 冻结；DSH schedule 的冷恢复范式可借鉴。**注意：领域循环调度在 src/loops/，已归 🟢，不在此列** |
| `src/orchestrator/`（SubAgentCoordinator/ModuleRunner） | 2,934 | subagent seam（acp/claude-code/codex/fork/spawn 多后端） | 冻结；Stage 2 冻结接口，Stage 3 后评估替换 |
| `src/providers/` + `src/llm/` | 1,551 | llm seam（adapter 可换+重试+token meter） | 冻结；DSH 有 DeepSeek 原生 adapter |
| `src/agent/` 中的通用部分（conversation-engine 等） | 部分 | core/agent-loop | ⚠️ 混合模块，见 §3.5 |
| `src/services/` 通用部分（restart-recovery/graceful-shutdown/memory-monitor 等 7 个文件） | 部分 | DSH 运行时自带生命周期 | ⚠️ 混合模块，见 §3.5 |
| `src/deploy/` + `src/monitoring/` | 4,600 | DSH telemetry/otel | 冻结 |
| `packages/logger` + `packages/error-types` + `packages/engine-auth` | 876 | DSH 基建 | 冻结，替换成本低 |

### 🟡 搬走（工程治理层——现在继续服役，独立节奏移植）

| 资产 | 规模 | 终态落点 |
|---|---|---|
| `scripts/`（checks/control-tower/workflow/hooks/golden-scenarios/doc-system） | 153+ 个文件 | DSH verify-* 门禁 + hooks 包族 |
| 铁律 + memory/ + task-brief 体系 | — | DSH Agent Notes 四态（proposed/implemented/archived/rejected） |
| `.claude/skills` ↔ `.dsh/skills` 双轨 | — | 统一进 DSH skill 机制 |

**为什么"现在服役"**：多 Agent 开发流水线（Codex/Claude Code/DSH 角色 + K3 审计）每天靠这套门禁运转——main 近期提交中大量是门禁修复（D366/D454/D456）与文档体系治理（doc-system 六脚本），证明它是活的生产设施。Stage 3 影子运行期间，逐组改写验证、过一组换一组。**这条线与运行时迁移是独立节奏，不绑在同一辆车上。**

### ⚫ 删除候选（复核零引用后删，遵循铁律 37）

> **CTO 审计修正（2026-08-20）**：`src/locale/` 与 `src/infra/` 曾误列入本表——locale 经 `file-driven-loaders.ts:24` 动态 `import()` 加载（§5.2 扩展点 #13 语言/本地化的载体），infra/command-lanes 经 `synova-agent.ts:254` 运行时 `require()` 加载（工具执行路径隔离 + 60s 超时的安全机制）。根因：**静态 grep 数 `from` 引用，数不到 `require()` / 动态 `import()` / 文件驱动扫描**——§2.2 已立此原则，初版违反了它。本表已经全形态引用检测（require / import( / 扫描路径全查）重验。两者归宿：locale → 🟢 品牌表层（#13）；infra → 保留（运行时安全机制，DSH guard/sandbox 范式可借鉴）。

| 候选 | LOC | 证据（全形态检测后） | 动作 |
|---|---:|---|---|
| `src/tui-v3/` | 121 | 全仓全形态零引用 | 删 |
| `src/skills/`（复数） | 74 | 全形态零引用；活体是 `src/skill/`（单数，`bootstrap.ts:412` 动态加载） | 删复数目录 |
| 根级 `mvp-server.cjs` | ~21k | 活副本是 `src/mvp-server.ts`（package.json "mvp" 脚本），根级 .cjs 零引用 | 删根级 .cjs |
| `packages/_archived/` | — | 全形态零引用 | 确认不进 tsconfig/构建后删 |
| `packages/agent-observer-mcp/` | 110 | 全形态零引用；活体是 `src/agent-observer/`（`routes/agent-observer.ts` 提供 POST /api/agent-observer/report） | 删 packages 版 |
| `prototypes/`（根级） | 3 文件 | 全形态零引用 | 确认后归档 |

### ⚠️ 混合模块（四色之外，需二级拆分——Stage 2 的核心工作）

| 模块 | LOC | 拆分方向 |
|---|---:|---|
| `src/agent/` | 9,823 | 通用面实际很小（conversation-engine/tool-loop-executor/main-agent/tools 基建）；43 个文件中绝大多数是领域（expert-*/knowledge-*/diagnosis-launcher/report-assembler/sentinel-*/ontology-syncer/boss-mailbox）→ 拆分后 **🟢 为主** |
| `src/services/` | 4,347 | 通用仅小头（restart-recovery/graceful-shutdown/memory-monitor/resource-monitor/retry/fault-recovery/config-recovery）；其余多为领域/运营（anomaly-detector/audit-service/escalation-engine/federated-pipeline/pattern-matcher/solution-generator/role-template-store）→ 拆分后 **🟢 为主** |
| `src/security/` | 2,176 | PIIScrubber/凭证库 → 🟢（企业合规）；DataBoundary → 借鉴 DSH sandbox 范式 |
| `src/middleware/` | 1,007 | auth/rbac 属"企业多用户"【双缺】机会 → 偏 🟢 做深 |

**细化后的结论**：两个混合模块拆分后，🔵 通用管道的实际占比约 **20%**——真正会被 DSH 替代的代码比直觉估计的少，迁移工作量与风险同步下降。

### 3.6 根级目录

除 `src/`、`packages/`、`expert/`、`scripts/` 外，根级还有一批目录，其中**多个是领域资产，且恰是"未来无限拓展"的物理载体**：

| 目录 | 规模 | 归属 | 说明 |
|---|---:|---|---|
| `extensions/` | 677 文件 | 🟢 | 文件驱动扩展体系：industries（行业）/business-models（商业模式）/frameworks（框架）/adapters/context-strategies/llm-providers/locales——新增行业模板、新专家能力的落点 |
| `skills/`（根级） | 46 个技能包 | 🟢 | 领域技能：finance/org/marketing/cross_validate/detect_contradiction/human_calibration/match_pattern 等 |
| `synova_worker/` | 14 文件（Python） | 🟢 | Python 侧 worker：connectors（企业数据接入）+ observer + sog——本体计算的另一半 |
| `knowledge/` + `theory/` + `synthesis/` | 19 文件 | 🟢 | 领域方法论与知识库：核心方程/尺度模型/交叉验证/报告结构——诊断方法论文档化资产 |
| `observer-adapters/` | 5 | 🟡 | 对外集成适配（claude-code-hook/hermes-plugin/openclaw-skill/python-sdk），随生态策略走 |
| `vendor/` | 31 文件 | 保留不动 | vendored 第三方（mcp-servers/pandoc-skill），不维护、不删除 |
| `prototypes/` | 3 文件 | ⚫ 复核 | 原型残留，确认零引用后归档 |
| `data/` | 运行时数据 | — | 不进图纸，部署轨资产 |

---

## 4. 能力映射表（Synova ↔ DSH，迁移方式）

| Synova 能力 | DSH 对应包 | 迁移方式 |
|---|---|---|
| 会话存储 | core/session（事件溯源，model-visible⟺logged） | **Stage 1 借鉴理念自研**；Stage 3 后换实现 |
| Agent loop | core/agent-loop（唯一 loop 逻辑，可换） | Stage 3 后替换 ConversationEngine |
| 子代理 | subagent seam（5+ 后端） | Stage 2 冻结接口；长期换 |
| LLM 适配 | llm seam（DeepSeek adapter 现成） | Stage 3 随壳接入 |
| 定时调度 | schedule（持久状态在 log，冷恢复） | Stage 1 借鉴冷恢复范式 |
| 后台作业 | ctx.jobs | Stage 3 哨兵挂载点 |
| 反馈闭环 | feedback（log-only 不进模型） | Stage 1 借鉴（补 N13 断裂） |
| 安全边界 | sandbox（landlock/windows-acl） | 长期借鉴，不引代码 |
| 门禁体系 | verify-* + snapshot + Agent Notes | 🟡 层移植落点 |
| **MCP 接缝** | mcp-client 包消费 **Synova 现有 `src/mcp/` server** | **A 形态的连接点，两边都已存在** |

---

## 5. 目标架构（A 形态终态）

```
┌─ 品牌层（客户可见，100% 自有）──────────────────────────┐
│  Synova 报表/仪表盘（app/）· 桌面壳（electron/）· 方法论   │
│  命名（42边/7专家/增长导航）· synova profile 皮肤          │
├─ DSH 壳（运行时，客户不可见）───────────────────────────┤
│  synova profile = dsh-base（模型/工具/持久化/沙箱/审批）    │
│                + synova-core 插件（经 mcp-client 消费 ↓）   │
│                + synova-gates（治理层移植：verify/hooks）   │
├─ MCP 边界（契约层）─────────────────────────────────────┤
│  输入：企业数据/问卷/连接器  输出：诊断报告/工单/增长方案      │
│  契约固化于 Stage 2，版本化，向后兼容                        │
├─ Synova 核心独立服务（现有代码演进而来）──────────────────┤
│  🟢 全部领域核心 + routes/mcp 表层                         │
│  保持独立可运行：不依赖 DSH 也能服务（=可逆性/反锁定）        │
└──────────────────────────────────────────────────────────┘
```

**品牌的三个锚点**：① 产品表层（app/electron/报告模板）② 方法论命名与数据（42 边、专家体系、积累的组织数据）③ profile/bundle 是 DSH 官方的品牌组合机制。**runtime 不可见，客户从头到尾只看到 Synova。**

**反锁定设计**：核心服务不 import 任何 DSH 包，只出 MCP/HTTP 契约。DSH 壳是消费者之一。将来换底座（DSH 方向变了/自建薄壳），核心不动。

### 5.1 "未来无限拓展"在终态里如何保证

"无限拓展"需求在 A 形态终态里有**三层扩展通道**，全部保留、互不阻塞：

1. **核心服务内部（主通道，不受影响）**：文件驱动体系原样保留——新增行业模板、新专家、新技能、新本体边 = 往 `extensions/`、`expert/`、`skills/` 放文件，loader 扫描即生效，**零代码改动**。DSH 壳不碰这一层。
2. **MCP 契约层**：新增诊断能力（新报告类型、新工单类型）时扩展 MCP 工具集，契约版本化、向后兼容。
3. **DSH 壳层（额外多出来的一层）**：Cordis 插件组合提供运行时扩展（新模型适配、新工具、新表面）——这是迁移后**白赚**的扩展位，不替代前两层。

一句话：**场景拓展能力（行业/专家/技能/边）长在你的核心服务里，永远归你；DSH 只提供运行时扩展，且是增量不是替代。**

### 5.2 完整可插拔性矩阵（多客户时代的扩展点全集）

> 每项含**现状代码证据**（main 快照实测）与终态归属层。
> 归属层三选一：核心服务（🟢 自有，文件驱动）/ 客户配置包（每客户一份）/ DSH 壳层（Stage 3 后白赚）。

| # | 扩展点 | 现状（代码证据） | 终态归属 | 差距/动作 |
|---|---|---|---|---|
| 1 | 本体节点/边 | `extensions/ontology/edge-types/` **55 个 JSON Schema 是唯一事实源**，`packages/ontology/edge-types.ts` 是镜像 | 核心服务 | 已可插拔 ✅；**但 JSON→TS 镜像需手动同步，有漂移风险** → 加自动生成步骤 |
| 2 | 计算模块（测量器） | `packages/sog-core/sog-schema-registry.ts` + l4 compute | 核心服务 | 注册机制在；测量器粒度注册待 Stage 2 细化确认 |
| 3 | 哨兵 | `sentinel-loader.ts` 从 `extensions/sentinels/{name}/` 加载 + `adapters/` 目录自动扫描，"加新哨兵=建目录" | 核心服务 | **已完全可插拔 ✅**（双层机制） |
| 4 | 专家技能 | 根级 `skills/` 46 个技能包（finance/org/cross_validate/detect_contradiction…） | 核心服务 | 已可插拔 ✅ |
| 5 | 专家结构配置 | `expert/expert-registry.yaml` 声明式："加新专家=加条目+建目录，**不需要改任何 TypeScript 文件**" | 核心服务 | **已完全可插拔 ✅** |
| 6 | 系统提示词 | `expert/*/PROMPT.md`（文件）+ `prompt-assembler.ts`（装配逻辑） | 核心服务 | 提示词内容已文件化 ✅；装配规则是引擎代码，合理 |
| 7 | L5 数据连接器 | `packages/connector-registry` + `src/connectors` + `synova_worker/connectors` | 核心服务 | 注册机制在 ✅；**每客户的凭证/字段映射属客户配置包（#9）** |
| 8 | 行业模板 | `extensions/industries` + `src/playbook` | 核心服务 | 已可插拔 ✅ |
| 9 | **租户级配置分层**（默认值→行业→客户→工作区） | `middleware/rbac` + `workspace-service` 有雏形，但无"客户配置包"概念 | **客户配置包（新增）** | ⚠️ **多客户时代最大缺口**：不是"能不能加扩展点"，而是"同一套核心如何为不同客户加载不同配置"。**DSH 的 profile/bundle 分层组合思想可直接借鉴到这里**（机制详见 §5.4） |
| 10 | 哨兵阈值/基线参数 | manifest `thresholds` + `baseline-store` | 客户配置包 | 阈值应随客户校准下沉，不能全局一套 |
| 11 | 报告模板 | `l3/report-template-loader`（文件驱动，已接线） | 核心服务+品牌表层 | 已可插拔 ✅；客户品牌定制（logo/配色）走客户配置包 |
| 12 | 通知/触达渠道 | `notifications/registry` + feishu/electron adapter | 品牌表层 | 已可插拔 ✅；新渠道=新 adapter |
| 13 | 语言/本地化 | `locale-loader` + `extensions/locales` | 品牌表层 | 已可插拔 ✅ |
| 14 | LLM 提供商/模型路由 | `providers/`（现状）→ DSH `llm seam` | DSH 壳层 | Stage 3 后白赚：换模型不改核心 |
| 15 | 知识库/组织记忆 | `knowledge/`（custom/industry/shared 三层） | 核心服务 | 已分层 ✅；OpenViking 应对见 D5 红线 |
| 16 | 凭证管理 | `security/credential-vault` | 客户配置包 | 每客户凭证隔离是租户配置层的一部分 |
| 17 | 反馈规则回流（N13） | `growth/feedback-collector` | 核心服务 | 自进化的前置闭环，见 §5.3 |
| 18 | 运行时工具/模型表面 | 现状靠改代码 | DSH 壳层 Cordis 插件 | Stage 3 后白赚 |

**矩阵结论**：常见担心的扩展点（边/哨兵/专家/技能/提示词）里，**5 项今天已经是文件驱动/声明式的**——领域层的可插拔性相当扎实。真正的缺口是 **#9/#10/#16：客户级配置分层**——把"全局一套参数"变成"每客户一个配置包"，这才是多客户扩展的命门。而它恰好可以整体借鉴 DSH 的 profile/bundle 分层思想：**核心服务是引擎，客户配置包是每辆车的调校单。**

### 5.3 自进化路线（分级定义 + 红线 + 正确顺序）

"自进化"必须分级谈，否则会把产品信任赌进去：

| 级 | 形态 | 机制 | 状态 |
|---|---|---|---|
| **E0** 人工迭代 | 人改文件 → 生效 | 现有文件驱动体系 | ✅ 现状 |
| **E1** 数据回流校准（半自动） | 部署数据 → 校准建议 → **人审** → 更新规则/阈值 | GA 闭环（S4）+ N13 反馈回流 + 知识沉淀（S5-3） | ⚠️ N13 断裂待修，**这是哇呢宝贝部署的验收项** |
| **E2** 规则自动回流（带审批闸） | 低风险参数（阈值/权重）自动调；规则级变更仍人审 | 前置：事件溯源可回放（D1）+ snapshot 可复现（D3）+ 准确率度量（`sentinel-accuracy.ts` **已存在**） | 🔜 E1 跑通 + Stage 1 基建落地后 |
| **E3** 运行时自调整 | agent 检查/挂载自己的插件 | DSH creative 模式 / self-modification 包——**DSH 官方自己标注为实验性** | ⏸️ 等 DSH 成熟 + 多客户数据规模 |

**重要事实**：E1 不是从零开始——`packages/evolution` 已实现 L0 自我进化引擎三层结构（SessionLearner 会话内学习·内存级 / OrgAdapter 组织自适应·持久化 / GlobalAnalyzer 全局进化·行业聚合 + 规则版本管理 RuleVersionManager），且**已接线**（`routes/evolution.ts`、`routes/ga-evolution.ts`、`post-diagnosis-processor`、`sentinel/runner`、L3 诊断引擎均消费）。E1 的真实含义是**修好并闭环已有引擎**——N13 断裂（C线 S5-1）正是其中一环——而不是新建一套机制。这也让 E1 的工作量显著低于"从零构建"的直觉估计。

**三条红线（重申 8-16 研究 §7.4，一条不能松）**：

1. **不宣称"越用越准/自我进化"，直到 E1 闭环真实跑通并有数据证明。** 哇呢宝贝部署期对外话术禁用此类表述。
2. **诊断结论影响企业经营决策**——一条自动回流的错误规则可能误导客户。信任（C 线 S0"让老板放下直觉相信系统"）是产品核心价值，自进化必须证据门禁化，不能为了炫技牺牲可信度。
3. **架构上现在预留接缝，但不提前启用全自动**：事件溯源（可回放审计）+ Agent Notes（结构化沉淀）+ feedback log-only（干净反馈流）三个接缝在 Stage 1 就建好，E2/E3 将来直接插上。

**正确顺序**：E1 先做扎实——它本身就是产品卖点（人机协同 GA 闭环），不是自进化的妥协版 → Stage 1 建基建（D1/D3/D4）→ E2 只放开低风险参数 → E3 等外部条件成熟。**自进化对 Synova 的正确形态不是"AI 自己改自己"，而是"每服务一个客户，方法论资产就厚一分"——这个资产沉淀在 `extensions/`、`expert/`、`knowledge/` 里，归你所有，不随任何 harness 迁移。**

### 5.4 客户配置包详解（多客户时代的命门机制）

**问题**：今天阈值、边权重、启用专家集、提示词、报告样式全是**全局一套**。服务哇呢宝贝一家时无所谓；服务五家不同行业的客户时，同一条"现金流预警线"不可能同时适用于外贸母婴（账期长、季节性）和软件公司（预付年费制）。要么改全局（客户间互相打架），要么改代码（每客户分叉，死路一条）。

**机制：四层叠加，引擎不变，每客户一份"调校单"**：

```
Layer 0  全局默认    产品出厂的方法论默认（42边定义、默认阈值、全专家启用）
   ↑ 覆盖
Layer 1  行业包      extensions/industries/{制造业|外贸|母婴…} 的行业级覆盖
   ↑ 覆盖
Layer 2  客户包      customers/{哇呢宝贝}/ —— 连接器凭证、校准后阈值、启用哨兵集、
                     报告品牌（logo/配色）、GA 审批人、飞书 webhook、角色模板微调
   ↑ 覆盖
Layer 3  工作区包    客户内部部门/子组织的局部覆盖（厂长看生产数据、外贸经理看客户循环）
```

运行时计算客户 X 的诊断 = `merge(L0, L1, L2, L3)`——每层只写自己与上层不同的部分。**新增一个客户的全部动作 = 复制模板文件夹、填配置，零代码改动。**这就是"边际成本趋零、反定位传统咨询公司"的物理实现。

**哇呢宝贝客户包内容示例**：① 连接器凭证（ERP/飞书，存 credential-vault，按客户隔离）② 数据字段映射（他们的 ERP 字段 → 本体节点）③ 哨兵启用集 + 校准阈值 ④ 行业包引用（产销一体外贸型）⑤ 报告品牌定制 ⑥ 权限角色实例（老板=admin、对接人=liaison、中层=manager、GA 审批人=ga + 合同到期约束）

**与自进化（§5.3）的关系**：E1 校准产出**写回客户包**（版本化、可审计、可回滚），不污染全局方法论；只有被创始人审核确认具有普适性的规律，才升格进 `extensions/`/`expert/` 全局资产。**客户数据养客户的调校单，跨客户只沉淀方法论，不串数据**——这同时解决了数据边界合规问题。

**对 DSH 的借鉴点**：profile/bundle 的"有序层级、上层覆盖下层、`--dump-config` 可检查实际生效配置"三件套，原样搬到客户配置包。实现位置在核心服务内，Stage 2（MCP 边界收敛）同期做雏形。

### 5.5 企业多用户权限管理

**现状实证**（main 快照）：`middleware/rbac.ts` 已有五角色 `admin / manager / liaison / staff / ga` + `RoleTemplate`/`BUILTIN_TEMPLATES` 模板派生权限 + 工作区级 `canAccessWorkspace`/`canModifyWorkspace` + GA 角色专属约束（含合同到期 `contractExpiry`）。**底子比预想完整**，但 A 线审计标注"企业多用户仅设计未全量验证"（A5）。

**完整四要素**（部署哇呢宝贝前必须逐项实测）：

| 要素 | 内容 | 现状 |
|---|---|---|
| 数据边界 | 薪酬类数据仅老板+HR 可见；部门级可见性；诊断报告分级 | ⚠️ 工作区级已有，数据类目级待验证 |
| 动作权限 | 谁能触发诊断 / 谁在 GA 里验证·否决·仲裁（C线 S5-2）/ 谁能接数据源 / 谁能邀请用户 | ⚠️ 模板有雏形，真实接线待验证 |
| 审计 | 谁在何时看了什么、改了什么、批了什么 | ✅ 事件溯源（D1/B1）落地后天然获得 |
| 凭证与跨客户隔离 | 每客户凭证独立（credential-vault）；**Synova 运营人员的支持访问也要被权限管住** | ⚠️ 待验证 |

**与 DSH 的关系**：DSH 定位单 agent 单机，**没有企业多用户概念**——这不是弱点，是 8-16 研究认定的【双缺】差异化机会（S9）。权限体系建在核心服务里（🟢 做深），DSH 壳不提供也不冲突。**对客户讲的故事：别人给你的是一个 agent，Synova 给你的是全公司分级使用、责任可追溯的诊断系统。**

**与客户配置包的衔接**：角色模板的客户级微调（比如哇呢宝贝要加一个"厂长"角色）作为客户包的一部分下发，全局模板不动。

---

## 6. 分阶段执行序列（绞杀者模式）

### Track A — 部署轨（最高优先级，永远不被阻塞）
哇呢宝贝部署用**现有代码**跑通。迁移轨的任何阶段若与部署冲突，迁移轨让路。

### Track B — 迁移轨

| 阶段 | 时间锚 | 内容 | 验收标准 | 回退 |
|---|---|---|---|---|
| **Stage 0** 清理 | **默认排到部署后** | 仅 ⚫ 死代码删除 + AGENTS.md 漂移修正；**部署验收前连低风险改动也不做——部署前只动文档与配置** | grep 零引用 + vitest 全绿 | 单项 revert |
| **Stage 1** 范式借鉴 | 9~10月 | D1 事件溯源 session log / D2 Agent Notes 四态 / D3 snapshot 测试 / D4 guard（开发转交方向已定稿，直接采用） | 每项独立 PR；补 C 线缺口可验证（S3-5/S0/S5-1/S5-3） | 均为新增机制，可关开关回退 |
| **Stage 2** MCP 边界收敛 | 10~11月 | 固化对外契约（HTTP+MCP）并版本化；冻结内部 harness 接口；拆分 4 个混合模块（§3.5）；**客户配置包雏形（§5.4）同期做** | 契约测试：仅经 MCP 边界可完成"数据进→诊断出"全链路 | 契约未冻结前不进 Stage 3 |
| **Stage 3** DSH 壳试点 | Q4 评估通过后 | 建 synova profile（dsh-base + synova-core 插件）；品牌表层平移；**影子运行**（与现有部署并行，不接真实客户流量） | 影子环境跑通同一客户数据的诊断全链路，输出与现网对账一致 | 影子环境整体废弃，现网无损 |
| **Stage 4** 切换与归档 | 影子验证后 | 切流量 → 旧 harness 打 tag `archive/pre-dsh-harness` → 铁律 37 流程删除残余 → 治理层移植收尾 | 客户无感切换 + **存量数据零丢失**；`git checkout <tag>` 可完整复活旧栈 | 切回旧栈（tag 保证） |

**Stage 3 双重入场券（缺一不开工）**：① 哇呢宝贝首季度数据回收完成；② DSH 脱离 rc 预览或 API 面收敛到可钉版本。另加第三问（见 R9）：收益是否值一个额外运行时进程——答案弱则 Stage 3 合法地永久挂起。

---

## 7. 旧 harness 三幕处置（创始人已确认）

1. **冻结**（现在起）：🔵 层只修部署相关 bug，不加新功能
2. **逐块替换、验证后删**（Stage 3-4）：每块独立 commit、可回退，遵循铁律 37
3. **归档进 git tag**（Stage 4）：**不另起工作区**——活代码只留一份（main），死代码只留一个入口（tag）

例外：🟡 治理层按独立节奏移植，移植完成前原样服役（多 Agent 流水线依赖它）。

---

## 8. 风险清单

| # | 风险 | 缓解 |
|---|---|---|
| R1 | DSH rc.5 破坏性变更 | Stage 3 前零 DSH 代码依赖（借鉴理念自研）；Stage 3 钉版本 + profile 层隔离 |
| R2 | 许可证 | ✅ 已验证 MIT；义务：分发时保留 DeepSeek copyright notice；THIRD_PARTY_NOTICES 随附 |
| R3 | OpenViking AGPLv3 传染 | 红线：不复制其代码；仅 HTTP 接口或理念借鉴（D5 方向） |
| R4 | 接线失败史（4 次） | 每阶段验收含接线审计（grep 调用方 + 集成测试走真实路由） |
| R5 | 迁移干扰哇呢宝贝部署 | 双轨隔离；Track A 绝对优先；Stage 3 必须影子运行 |
| R6 | 多 Agent 流水线依赖治理层 | 🟡 层独立排期；不因运行时迁移同时动门禁 |
| R7 | main 漂移使图纸过时 | §10 重验基线程序；图纸钉 commit `23b6de9d` |
| R8 | OPC 单人带宽 | §9 粗估 + "可以不做"清单；每阶段都有"不做"的合法选项 |
| R9 | **Stage 3 运维成本**：现有部署为单容器 Docker + SQLite 卷挂载，DSH 壳 = 客户现场多一个运行时进程 | Stage 3 入场券第三问："llm seam/subagent/telemetry 的收益是否值一个额外进程？"答案弱则 Stage 3 **合法地永久挂起**——A 形态的价值主要在反锁定与扩展位，不必须执行 |
| R10 | **数据连续性**：切换时哇呢宝贝存量数据（基线/证据/报告历史）不能丢 | A 形态设计红利——核心服务始终持有 SQLite（`./data/synova.db` 卷挂载），换壳不动库；Stage 4 验收增加"存量数据零丢失 + 会话历史转换策略（仅当 ConversationEngine 迁入 DSH 事件溯源时才需要）" |

---

## 9. 工作量粗估（OPC + AI 辅助，诚实的数字）

| 阶段 | 预估 | 可以不做 / 可缓 |
|---|---|---|
| Stage 0 | 2~3 天 | locale/infra 合并可缓 |
| Stage 1 | 2~3 周 | D3 snapshot 测试可缓一个迭代 |
| Stage 2 | 2~3 周 | 混合模块只拆 agent/services 两个也算达标 |
| Stage 3 | 3~4 周 | DSH 的 UI 组件体系不采用，品牌表层直接平移 |
| Stage 4 | 1 周 | — |

**总口径**：从现在到 DSH 壳上线约 3~4 个月，且前两个月与部署完全并行、不抢部署带宽。

---

## 10. 执行前重验基线（每次启动新阶段时跑）

```bash
git fetch origin
# 对比图纸基线与最新 main 在已盘点目录上的变动
git diff --stat 23b6de9d..origin/main -- src/ packages/ expert/ scripts/
# 若变动涉及本图纸已分类的模块 → 局部刷新对应章节（四色归属/LOC/接线证据）
# 若仅涉及 docs/tests → 图纸继续有效
```

---

## 11. 残余不确定性（诚实声明）

1. ⚫ 删除候选已经过一轮全形态引用检测（CTO 审计修正后），但执行删除前仍需按铁律 37 以当日 main 重跑零引用确认（基线漂移风险，见 §10）
2. 测量器（计算模块）的逐文件注册机制未全部展开——Stage 2 细化时还需一轮
3. 本图纸基于公开研究 + 代码实证，**未经过 K3 审计线独立验证**——按协作协议，够格进入评审流程，但不应跳过审计

---

> 定稿记录：2026-08-20 创始人定稿。过程：Kimi 分析 session 出品（基线 `23b6de9d`）→ 三轮创始人复核（业务循环归属纠正 / 可插拔矩阵与自进化分级 / 客户配置包与权限管理）→ CTO 审计一轮（删除候选动态加载误判修正 + 全形态重验）→ 按新文档体系登记安置（DOC-0114/0115）。
> 后续变更规则：执行期发现图纸与现实漂移时，按 §10 重验基线程序局部修订，并更新本记录。
