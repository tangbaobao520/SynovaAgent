# DSH 全部子包处置表（224 包，确保无遗漏）

> 生成方式：`ls` 全量枚举 224 包 + 逐包读取 package.json（name/description/dependencies）+ 对重点包与遗漏候选包 grep lib/*.js 的 esbuild bundle 尾部 `export {...}` 权威导出清单、`//#region` 源码路径标记与 import 头。
> 处置五值定义：`借鉴-范式`（有可复用设计模式，注明 Synova 缺口）/ `接缝-预留`（Stage 2 契约层预留接口）/ `替换-候选`（仅 Stage 3 DSH 壳试点整体换用）/ `不适用`（纯 UI、平台专属、demo）/ `基建-无需`（cordis 框架件与 util，用 DSH 壳时自然获得）。
> 前提：Synova 终态 = DSH 做壳 + Synova 核心独立服务 + MCP 边界；**绝不引入代码依赖**，借鉴止于范式。
>
> **处置分布**：借鉴-范式 65 · 接缝-预留 28 · 替换-候选 6 · 不适用 70 · 基建-无需 55（合计 224）

## 一、处置总表（224 行，按字母序）

| 包名 | 一句话职责 | 处置 | 理由 |
|---|---|---|---|
| cordis-plugin-group | cordis 嵌套插件组（一组插件挂成一个单元） | 基建-无需 | cordis 框架件，用壳自然获得 |
| cordis-plugin-hmr | cordis 插件热替换（开发期 reload） | 基建-无需 | 开发体验件，壳自带 |
| cordis-plugin-include | cordis 配置中 include 文件 | 基建-无需 | 配置装配件 |
| cordis-plugin-loader | cordis 插件加载器 | 基建-无需 | 框架件 |
| cordis-plugin-timer | cordis 定时服务（interval/timeout 随插件生命周期释放） | 基建-无需 | 定时 Synova 已自研（src/cron）；此处仅框架级 timer |
| cordis | 元框架：插件生命周期、依赖注入、事件总线 | 基建-无需 | DSH"一切皆插件"的地基；不引依赖，用壳时获得 |
| cosmokit | 通用工具集（promise/debug/object 工具，cordis 生态配套） | 基建-无需 | util 类 |
| dsh-acp-app | ACP profile bundle：仅自动化 JSON-RPC stdio + 进程生命周期 | 不适用 | ACP 自动化形态 Synova 无需求（对外是 HTTP+MCP） |
| dsh-acp | ACP server：经 stdio JSON-RPC 驱动 DSH agent | 不适用 | 同上 |
| dsh-agent-default-model | Agent 入口共享的默认模型选择 | 基建-无需 | 壳配置细节 |
| dsh-agent-instructions | 工作区 AGENTS.md/CLAUDE.md 指令文件加载器 | 借鉴-范式 | 缺口：项目级指令文件自动注入（Synova skills/expert 文件加载有，工作区上下文注入无） |
| dsh-agent-loop | 具体 agent loop 插件（DSH 运行时循环本体） | 借鉴-范式 | 对照自研 conversation-engine：并行工具调用上限（DEFAULT_MAX_PARALLEL_TOOL_CALLS）、turn 边界投影、请求标记可补缺口 |
| dsh-agent-presets | 每会话 agent 组合：从 preset cordis.yml 装配 | 借鉴-范式 | 缺口：声明式 agent 组合 + 挂载/泄漏检测（leakedServices），可对照升级 expert-registry.yaml 体系 |
| dsh-agent-spine-demo | 无执行器/无 UI 的 agent spine demo（回退标题、重试、goals） | 不适用 | demo |
| dsh-agent-tool-presentation | agent 面呈现选择器：工具按 PTC/native/双模组合 | 不适用 | 呈现层，Synova 无此需求 |
| dsh-agent | Agent 接口、注册表、发起者作用域、事件词汇 | 接缝-预留 | Stage 2：自研 orchestrator 与壳 agent 词汇的对接面 |
| dsh-anonymous-user-id | 遥测/反馈关联的匿名用户标识 | 不适用 | Synova 有企业身份体系 |
| dsh-api-gateway | Typert Remote Host 分发器 + Client API 端点 | 接缝-预留 | Stage 3：Synova 服务对接 Host API 的网关面 |
| dsh-api-remotes | 应用所选 Host 能力的 Remote BFF 装配 | 接缝-预留 | 远程契约面 |
| dsh-api-session-controller | Session Remote 命令、冷读、实时控制通道 | 接缝-预留 | 远程契约面 |
| dsh-api-settings-controller | settings 域的 Remote owner | 接缝-预留 | 远程契约面 |
| dsh-api-workspace-controller | Workspace Remote 命令 + 重连安全状态传输 | 接缝-预留 | 远程契约面 |
| dsh-app-boot | app bin 共享启动胶水（.env、fail-loud 守卫、快照感知配置） | 基建-无需 | 壳启动件 |
| dsh-atomic-write | 零依赖原子文件替换：独占创建随机后缀临时文件 + 带权限 rename；另有 withFileLock | 借鉴-范式 | 缺口：状态文件/控制塔数据原子写（writeFileAtomic 思路可直接抄进 scripts/） |
| dsh-attachment-local | 私有内容寻址 DSH_HOME 附件存储 | 借鉴-范式 | 轻量：内容寻址存储范式可对照 evidence 存储设计 |
| dsh-attachment | 不可变附件存储 seam | 接缝-预留 | Stage 2：诊断附件/证据文件契约 |
| dsh-authorization | 授权 seam：插件自有流程通过对话向人获取凭证 | 借鉴-范式 | 缺口：企业数据源 OAuth/凭证授权流（对话式补授权） |
| dsh-base | 共享核心 profile bundle（85 deps，第一层 patch） | 替换-候选 | Stage 3 DSH 壳试点的基座 bundle |
| dsh-bash-local | bash 执行器 seam 的本地子进程实现 | 基建-无需 | 壳自带 |
| dsh-bash-sandbox | bash 执行器的沙箱消费实现（每命令经 ctx.sandbox，回报 denial/enforcement 事实） | 基建-无需 | 壳自带 |
| dsh-brand | 无状态品牌字符串原语（branded type） | 基建-无需 | util 类 |
| dsh-client-connection | 客户端认证 RPC 传输 + generation 生命周期 + 浏览器 fixture | 不适用 | client 侧 |
| dsh-client-hmr | script 加载客户端入口的热重载驱动（SSE→invalidate→fiber swap） | 不适用 | client 开发件 |
| dsh-client-locale | locale 插件：Host 偏好、可扩展语言目录、浏览器回退 | 不适用 | i18n，Synova 暂无需求 |
| dsh-client-modules | 客户端模块系统双面（node 装配 __DSH_BOOT__ 图 + 浏览器懒 CJS 模块表） | 不适用 | client 基建 |
| dsh-client-ui-agent-preset | agent-preset UI 面（后续会话默认/本会话席位/组合编辑器） | 不适用 | 纯 UI |
| dsh-client-ui-approval | 审批 composer takeover UI | 不适用 | 纯 UI |
| dsh-client-ui-attachment | 附件呈现插件（输入/消息图/轨迹图槽位） | 不适用 | 纯 UI |
| dsh-client-ui-brand-official | Web 客户端侧栏官方品牌槽位 | 不适用 | 纯 UI（Synova 自有品牌层） |
| dsh-client-ui-chat | 会话 target、节点定义、渲染器、详情面 | 不适用 | 纯 UI |
| dsh-client-ui-commands | 客户端命令面（全局目录缓存、'/' 源、三类命令 UI、popupSelect 注册表） | 不适用 | 纯 UI |
| dsh-client-ui-conversation | target 无关会话装配、shell、composer、队列、视图导航 | 不适用 | 纯 UI |
| dsh-client-ui-cordis | cordis 动态插件定义卡片（cordis_define 工具行 + 运行/停止开关） | 不适用 | 纯 UI |
| dsh-client-ui-deliverables | 产出文件 turn 尾部 + 最终回复可点文件引用（Web） | 不适用 | 纯 UI |
| dsh-client-ui-directory-picker-browse | 应用内目录浏览面（host 列表/创建原语的渲染方） | 不适用 | 纯 UI |
| dsh-client-ui-directory-picker-native | 原生目录选择器面（驱动 host OS chooser） | 不适用 | 纯 UI |
| dsh-client-ui-goal | 会话目标 UI（composer 上方 GoalBar，读 goal 投影） | 不适用 | 纯 UI |
| dsh-client-ui-input-trigger | 输入触发管线（'/' '@' 检测、候选菜单、pick 路由） | 不适用 | 纯 UI |
| dsh-client-ui-jobs | 会话头后台任务列表（镜像 session/jobs 帧） | 不适用 | 纯 UI |
| dsh-client-ui-layout | 三栏 AppFrame shell + 拖拽手柄 + ctx.layout 视图状态服务 | 不适用 | 纯 UI |
| dsh-client-ui-message-feedback | 助手消息操作条上的消息反馈控件（接 messageFeedback Remote） | 不适用 | 纯 UI |
| dsh-client-ui-model-selection | 模型选择（共享目录 + Session 投影 + session.selectModel） | 不适用 | 纯 UI |
| dsh-client-ui-permission-presets | 权限面（General 设置新会话默认 + 当前会话 /permission popup） | 不适用 | 纯 UI |
| dsh-client-ui-plan | plan-mode composer 控件（plan 投影 + /plan 命令通道） | 不适用 | 纯 UI |
| dsh-client-ui-reference | 统一 Web @file/@session 引用源 | 不适用 | 纯 UI |
| dsh-client-ui-renderer | 浏览器 UI 渲染器（React slot 绑定、ctx.uiRenderer、应用根装配） | 不适用 | 纯 UI |
| dsh-client-ui-schedule | Web 会话头只读活跃 Schedule 目录 | 不适用 | 纯 UI |
| dsh-client-ui-session | Session Controller 的 React 适配 + 会话作用域槽位 | 不适用 | 纯 UI |
| dsh-client-ui-settings-general | General 设置 + 产品 onboarding + 设置字典 + 版本化欢迎公告 | 不适用 | 纯 UI |
| dsh-client-ui-settings-models | 模型设置 + 共享 onboarding 对话框 | 不适用 | 纯 UI |
| dsh-client-ui-settings-plugin-inventory | Web Plugins 设置中的只读 Loader 清单 tab | 不适用 | 纯 UI |
| dsh-client-ui-settings-plugins | 插件设置区（特性自有 tab + host 插件卡片） | 不适用 | 纯 UI |
| dsh-client-ui-settings | settings 域基插件（namespace scope 服务 + slot 类型契约） | 不适用 | 纯 UI |
| dsh-client-ui-sidebar | 侧栏：会话多级树、搜索、分组、状态点 | 不适用 | 纯 UI |
| dsh-client-ui-skill | Web 技能引用 + 专用技能工具行 | 不适用 | 纯 UI |
| dsh-client-ui-subagent | 子代理会话目录、续聊路由 UI、'@' 引用源 | 不适用 | 纯 UI |
| dsh-client-ui-theme | 主题插件（pre-plugin palette 引导、DOM-free ThemeRuntime、token 样式） | 不适用 | 纯 UI |
| dsh-client-ui-tool | 工具调用树渲染器 + keyed 每工具呈现槽位 | 不适用 | 纯 UI |
| dsh-client-ui-trajectory | 轨迹事件账本 + 交互式时间总览（纯消费插件） | 不适用 | 纯 UI |
| dsh-client-ui-user-questions | ask_user_question composer takeover + plan 评审呈现 UI | 不适用 | 纯 UI |
| dsh-client-ui-workflow-run | 持久 workflow-run 会话节点 + 嵌套成员展开 | 不适用 | 纯 UI |
| dsh-client-ui-workspace | workspace picker 插件（注册进侧栏/空态槽位） | 不适用 | 纯 UI |
| dsh-cmdline | 不可变命令行交接（launcher→注入 cmdlineArgs 的 app 插件） | 基建-无需 | 壳内部 |
| dsh-code-runtime-worker-thread | code-execution seam 的 worker-thread 实现 | 不适用 | Synova compute 自研纯函数，不执行模型代码 |
| dsh-code-runtime | 抽象代码执行 seam（ctx.codeRuntime） | 不适用 | 同上 |
| dsh-command-compact | 显式触发会话压缩的人类斜杠命令 | 基建-无需 | 壳命令 |
| dsh-command-feedback | 仅日志的会话反馈生产者 + 斜杠命令 | 基建-无需 | 壳命令 |
| dsh-command-goal | 持久化同会话目标的斜杠命令 | 基建-无需 | 壳命令 |
| dsh-commands | 插件自有的人类命令注册表（面向 DSH UI） | 借鉴-范式 | 缺口：命令面与 UI 解耦的统一注册表（TUI/API 命令同源登记） |
| dsh-compaction-basic | token-meter 驱动的压缩策略 + LLM 摘要后端 | 借鉴-范式 | 缺口：长会话上下文压缩（thresholdRatio 0.8 触发 / retainRatio 0.16 保留 / summarizationProvider 可插拔） |
| dsh-compaction-tool-result-pruner | 重放安全、无模型的工具结果头/中/尾修剪 | 借鉴-范式 | 缺口：工具结果膨胀控制（PRUNE_MARKER + 派生式非破坏修剪） |
| dsh-compaction | 抽象压缩服务 seam（ctx.compaction） | 接缝-预留 | Stage 2：压缩策略契约（引擎可插拔） |
| dsh-cordis-client-runner | 双面插件浏览器半：事件订阅、闭包求值、guard facade、loader 入口 | 不适用 | client 基建 |
| dsh-cordis-host-runner | 动态包定义注册表、host 半沙箱生命周期、模型挂载双面包的 invoke 表 | 不适用 | 模型挂载动态包，Synova 不开放该能力 |
| dsh-credentials-local | 文件凭证提供者（$DSH_HOME/.env + 活进程环境） | 基建-无需 | 壳自带 |
| dsh-credentials | 抽象凭证 seam：settings 只存引用、provider 持有值 | 借鉴-范式 | 缺口：秘密管理（引用/值分离，配 security/ 的 PIIScrubber 体系） |
| dsh-deepseek-llm-api-extensions | 官方 DeepSeek LLM API 适配器的加法式请求字段注册表 | 借鉴-范式 | 轻量：不改核心扩展 API 字段的注册表范式（providers/ 可用） |
| dsh-deque | 零依赖环形双端队列（摊还 O(1) 端操作、有界空置存储） | 借鉴-范式 | 数据结构可直接复用思路：流式 buffer/有界队列（对照铁律 41 的流式管线） |
| dsh-file-reference-local | ctx.fileReferences 本地实现（有界模糊索引） | 基建-无需 | 壳自带 |
| dsh-file-reference | 文件引用发现契约 + 共享 @file 语法 | 借鉴-范式 | 轻量：@引用语法规范式（诊断材料引用可对照） |
| dsh-fs-local | ctx.fs 本地文件系统实现 | 基建-无需 | 壳自带 |
| dsh-fs-observation-policy | 文件上下文策略：观察态、读前先读、版本守卫写/编辑（经 fs/* 事件门，无服务 API） | 借鉴-范式 | 缺口：文件编辑安全（防覆盖并发写；"先读再改"从门禁纪律升级为运行时机制） |
| dsh-fs-sandbox | 沙箱执行 fs 实现（按 per-call 模式围栏写/编辑：只读拒绝变更、workspace-write 限制在 workspace+temp） | 基建-无需 | 壳自带 |
| dsh-fs | 抽象文件系统能力 seam（ctx.fs 词汇类型 + fs/* 策略事件词汇 + 版本守卫原子变更） | 接缝-预留 | Stage 2：文件访问契约 |
| dsh-goal-round-driver | 竞态围栏的同会话 goal 续轮驱动 | 借鉴-范式 | 缺口：长诊断任务自动续轮（goal_round 提示注入 + 防双驱动竞态） |
| dsh-goal | 事件溯源的同会话目标状态与生命周期服务 | 借鉴-范式 | 缺口：目标态跟踪（变更事件 + fold 投影 + 变更版本；对照 Sentinel 工单闭环状态机） |
| dsh-headless | one-shot bundle：无 Host/HTTP/浏览器的核心 Agent/Session runner | 替换-候选 | Stage 3：无头批量诊断 runner 的试点形态 |
| dsh-home-paths | DSH_HOME 共享文件系统路径助手 | 基建-无需 | util 类 |
| dsh-hook-protocol | Claude Code/Codex hook 线协议共享件（matcher 引擎、stdin/exit-code/stdout 编解码、多 hook 合并、hook/* 事件） | 借鉴-范式 | Synova 控制塔即 hook 体系：matcher 语义/超时/stderr 摘要/合并策略的成熟参照 |
| dsh-hooks-claude-code | 桥接插件：在 DSH 拦截 seam 上运行 Claude Code hooks.json 配置 | 接缝-预留 | Stage 2/3：Synova 控制塔 hooks 资产可原样跑在壳上 |
| dsh-hooks-codex | 同上（Codex hooks 配置桥接） | 接缝-预留 | 同上 |
| dsh-host-directory-picker-auto | 目录选择 seam 自适应选择器（启动时解析宿主情形定 native/browse） | 不适用 | Web GUI 专属 |
| dsh-host-directory-picker-browse | 目录选择 seam 应用内浏览后端（列表/创建原语） | 不适用 | Web GUI 专属 |
| dsh-host-directory-picker-native | 目录选择 seam 原生 OS chooser 后端 | 不适用 | Web GUI 专属 |
| dsh-host-directory-picker | 工作区目录选择抽象 seam（ctx.directoryPicker） | 不适用 | Web GUI 专属 |
| dsh-host-frontend-static | Web shell SPA dist 服务器（webserver fallback seat、穿越拒绝、404） | 基建-无需 | 壳 web 件 |
| dsh-host-plugin-inventory | 当前 Cordis Loader 插件状态只读 Remote 投影 | 基建-无需 | 壳管理面 |
| dsh-host-webserver | Web 路由注册插件（HTTP/upgrade 路由、index 变换 tap、静态 fallback） | 基建-无需 | 壳 web 件 |
| dsh-invariants | 包自有运行时不变量的注册表服务 | 借鉴-范式 | 缺口：铁律的运行时执法（pre-commit 静态门之外，运行时注册/校验不变量并 fail-loud） |
| dsh-jobs-local | ctx.jobs 的进程内实现 | 基建-无需 | 壳自带 |
| dsh-jobs | 后台任务注册表 seam（共享 ids、owner 隔离、轮询、取消、完成监听） | 接缝-预留 | Stage 2：长任务（哨兵检查/批量诊断）的任务语义契约 |
| dsh-launch-environment | 不可变启动环境，记录每个值由哪一层供给 | 借鉴-范式 | 轻量：配置来源可追溯范式（env/launch/config 分层留痕） |
| dsh-llm-deepseek | DeepSeek chat-completions 适配器 | 基建-无需 | Synova providers/ 已自研；用壳时自然获得 |
| dsh-llm-pi-ai | pi-ai 背书的 DeepSeek 适配器（dsh-llm-deepseek 的设计验证孪生） | 不适用 | 设计验证用途，无直接价值 |
| dsh-llm-retry | 按路由的 LLM 请求重试策略 | 借鉴-范式 | 缺口：providers/ 统一重试策略（per-provider 路由规则 + 重试历史投影；对照铁律 32 retryable） |
| dsh-llm | 提供商中立 LLM 服务接口（消息/分块词汇、错误分类、BlockAssembler、图片卸载策略） | 借鉴-范式 | 缺口：稳定错误码 taxonomy（CONTEXT_WINDOW_EXCEEDED/QUOTA_EXCEEDED/INVALID_CREDENTIAL/EMPTY_RESPONSE + isXxxError 守卫）与消息组装原语 |
| dsh-mcp-client | MCP client 桥：连接 MCP server 并把其工具注册到 ctx.tools | 借鉴-范式 | MCP 边界重点：stdio/StreamableHTTP 双 transport、工具表变更通知重载、环境擦除、图片准入错误处理——Stage 2 Synova 作为 client 连外部 MCP 的范式 |
| dsh-message-feedback | 生命周期绑定的每消息评分/备注 sidecar | 借鉴-范式 | 缺口：反馈闭环的消息级锚点（GA 评审→记忆层的定位粒度） |
| dsh-native-command | 宿主原生命令与路径打开（shell-free 执行、取消、桌面检测、WSL 交接） | 不适用 | 桌面 OS 集成 |
| dsh-output-retention | 零依赖有界保留原语（ItemRetainer/TextRetainer + 中性省略说明助手） | 借鉴-范式 | 缺口：截断的诚实披露（"保留了什么/省略了什么"标准文案） |
| dsh-permission-presets | 产品级权限预设：一个 Permissions 选择打包 sandbox-mode+approval-policy 旋钮并写穿到各自会话事件 | 借鉴-范式 | 缺口：部署级权限档位（预设→旋钮写穿，下游仍见规范单旋钮状态） |
| dsh-persona | 组合编写的部署人格段 | 借鉴-范式 | 缺口：部署级人格（企业语气/边界）与专家人格分层注入系统提示 |
| dsh-plan-mode | 每 agent plan mode：部署指导、直连斜杠命令、用户评审出口 | 借鉴-范式 | 轻量：plan-then-approve 机制（高风险操作先计划后人审） |
| dsh-plugin-package-inventory-deepseek | 面向官方 DeepSeek API 请求的活跃 Loader 插件包清单 | 基建-无需 | 遥测装饰 |
| dsh-pwsh-local | bash 执行器 seam 的本地 PowerShell 实现 | 基建-无需 | Windows 机可用，属壳件 |
| dsh-pwsh-sandbox | PowerShell 执行器的沙箱消费实现 | 基建-无需 | 同上 |
| dsh-repeat-tool-reminder | 重复工具调用守卫：agent 对相同调用打转时给 advisory 提醒 | 借鉴-范式 | 缺口：专家 ReAct 卡死/循环防呆（advisory 不阻断，不打断主循环） |
| dsh-sandbox-local | 本地进程沙箱后端：bwrap/npm landlock-run/Seatbelt/Windows ACL 限制令牌（功能探测、fail-closed） | 不适用 | 平台专属 OS 沙箱，Synova 不自建 |
| dsh-sandbox-policy | per-call 沙箱策略解析器 + 当前模型上下文（部署回退 + 会话模式与 workspace 根） | 基建-无需 | 壳安全配置 |
| dsh-sandbox-windows-acl | Windows ACL 写限制沙箱后端（限制令牌 spawn + capability-SID 写允许表） | 不适用 | 平台专属 |
| dsh-sandbox | 抽象进程沙箱 seam（同世界围禁词汇 + SandboxProvider 契约） | 接缝-预留 | Stage 2：若需工具围禁的契约占位 |
| dsh-schedule | agent 作用域的持久 after/at/fixed-rate 提醒（基于会话事件日志） | 借鉴-范式 | 缺口：会话级定时（src/cron 是进程级；schedule 把定时事件化进会话日志、随会话重放恢复、agent 作用域隔离） |
| dsh-scope | 作用域上下文注册原语（scope 标签、scope 过滤事件分发） | 基建-无需 | cordis 件 |
| dsh-sdk-app | dsh SDK profile bundle：stdio JSON-RPC 服务 + 进程生命周期（叠在 dsh-base 上） | 替换-候选 | Stage 3：以 SDK 形态消费壳时的 bundle |
| dsh-sdk-jsonrpc-server | 面向进程外 SDK 客户端的 stdio JSON-RPC 服务器插件 | 接缝-预留 | Stage 2：SDK 对接面 |
| dsh-sdk-minimal | 独立最小 SDK profile bundle（JSON-RPC + 一个 DeepSeek 适配器 + 持久 shell + 编辑器 + JSONL 会话） | 替换-候选 | Stage 3：最小壳试点形态 |
| dsh-sdk-protocol | SDK 运行时共享线协议（NDJSON stdio 传输 + 命名请求/结果/通知类型） | 接缝-预留 | Stage 2：对接协议词汇 |
| dsh-session-checkpoint-policy | 模型请求与工具副作用前的语义会话耐久检查点 | 借鉴-范式 | 缺口：副作用前检查点（写库/建工单前先落盘会话态，崩溃可恢复） |
| dsh-session-log-deepseek | 官方 DeepSeek LLM API 的增量无损会话日志请求扩展 | 借鉴-范式 | 轻量：请求级会话日志上报范式（DeepSeek 生态特性，providers/ 可对照） |
| dsh-session-log-export | Web 会话日志导出命令 + 共享下载对话框 | 不适用 | Web UI |
| dsh-session-persistence-jsonl | JSONL 持久会话存储后端 | 借鉴-范式 | 重点机制：每会话 JSONL + zstd 压缩帧 + 校验和 + 撕裂尾容错 + pack 压实——对照 src/store 的导出/审计格式与压实策略 |
| dsh-session-persistence | 抽象持久会话存储 seam（ctx.sessionPersistence） | 接缝-预留 | Stage 2：存储契约（Synova store 保持独立） |
| dsh-session-projection-cache | 持久投影缓存（session_projcache 存储域上每会话检查点记录、节流 write-behind、缓存列表读） | 借鉴-范式 | 轻量：派生状态缓存范式（投影重算贵时的 write-behind 策略） |
| dsh-session-projection | 会话投影 seam：可合并扩展的投影类型表 + provider 契约 + ctx.sessionProjections 注册表（供日志派生每会话状态的全量现值） | 借鉴-范式 | 重点范式：日志派生会话状态的统一投影注册表（todo/统计/标题/目标/计量全部同构）——Synova 会话派生状态缺口 |
| dsh-session-query-sqlite | ctx.sessionQuery 具体后端（SQLite FTS5 搜索） | 借鉴-范式 | Synova L5 即 SQLite：会话全文检索（FTS5）可直接借鉴落地 |
| dsh-session-query | 组合会话查询服务契约（具体读、traces、过滤器） | 接缝-预留 | Stage 2：查询契约 |
| dsh-session-reference | 跨会话快照引用 + 持久不可信模型上下文（ctx.sessionReferenceResolver） | 借鉴-范式 | 缺口：跨会话引用（@session URI 编解码/大小上限/候选限流）+ 不可信内容标注（prompt-injection 防线） |
| dsh-session-stats | 全日志会话计数与墙钟时间投影（sessionStats） | 借鉴-范式 | 轻量：投影范式最小实例（会话统计是 Synova 报告的现成素材） |
| dsh-session-telemetry-otel | telemetry seam 的 OpenTelemetry 后端（把捕获的会话记录交给 OTel JS SDK 日志管线） | 不适用 | Synova 暂无 OTel 需求 |
| dsh-session-telemetry | SessionTelemetryBackend seam：会话事件捕获、投影、脱敏、交给上报后端 | 借鉴-范式 | 缺口：遥测四段式管线（脱敏在出口强制，对照 PIIScrubber 的现有位置） |
| dsh-session-title-first-prompt-llm | 会话标题的首条消息 LLM provider | 基建-无需 | 壳功能 |
| dsh-session-title-llm | 会话标题 provider 的共享 LLM 生成策略 | 基建-无需 | 壳功能 |
| dsh-session-title | 日志背书的会话标题服务 + provider 注册表 | 基建-无需 | 壳功能 |
| dsh-session | 事件溯源会话存储（SessionStore、事件类型、fold、surface 二分、fork） | 借鉴-范式 | 对照 src/store：append/replace surface 事件二分、seq 区间编码、格式版本、fork 错误、packChunkRuns——ES 细节丰富 |
| dsh-settings-file | settings.yaml 文件后端 settings provider | 借鉴-范式 | 缺口：声明式分层配置文件（workspace/home 两级 + schema 化 spec 解析 resolveSpec） |
| dsh-settings | 抽象用户设置 seam（ctx.settings） | 接缝-预留 | Stage 2：配置契约 |
| dsh-shell-env | 工具无关的托管 DSH_* shell 环境注册表 | 基建-无需 | 壳内部 |
| dsh-shell | 抽象 bash 执行器 seam（ctx.shell） | 接缝-预留 | Stage 2：命令执行契约 |
| dsh-skill-badge | 内置 dsh badge 技能 provider | 不适用 | 演示性内置技能 |
| dsh-skill-filesystem | 本地文件系统技能 provider | 借鉴-范式 | 直接对照 Synova skills/：SKILL.md 发现/校验/按需加载的注册表化实现 |
| dsh-skill | agent 技能 provider 注册表 | 借鉴-范式 | 轻量：技能源可插拔（文件/远程）的 provider 抽象 |
| dsh-spill-local | ctx.spillStore 本地实现（会话私有文件、sweepSpillRoots 清扫、win32 段编码） | 借鉴-范式 | 轻量：溢出文件生命周期管理（sweep）范式 |
| dsh-spill-policy | 工具结果溢出策略：超长纯文本工具结果替换为保留预览 + 溢出文件路径（无服务 API，纯事件门） | 借鉴-范式 | 重点范式：纯事件门策略（不占服务位）——超长工具结果的上下文经济学（preview/spillNotice/spillReplacement） |
| dsh-spill | 抽象溢出存储 seam（ctx.spillStore：存超大工具文本、返回检索定位器） | 接缝-预留 | Stage 2：溢出契约 |
| dsh-storage-domain | 域数据形态（ctx.storage.domain）：schema 校验、事件发射的 KV 域（defineDomain） | 借鉴-范式 | 缺口：轻量持久化层（哨兵配置/反馈行等不必全进 SQLite；带 schema 与事件的 KV 域范式） |
| dsh-storage-json | 存储枢纽的 JSON 文件 KV 后端 | 基建-无需 | 壳自带 |
| dsh-storage | 存储枢纽（ctx.storage：命名后端注册表 + 挂载数据形态设施） | 接缝-预留 | Stage 2：存储枢纽契约 |
| dsh-subagent-fork-in-process | 进程内 fork 子代理后端（用父日志前缀种子子代理） | 借鉴-范式 | 缺口：orchestrator 的 fork 型子代理（继承上下文前缀 vs spawn 全新——两种委派语义） |
| dsh-subagent-in-process-driver | 共享进程内子代理运行驱动（spawn/fork 后端共用，驱动 ctx.agents 上的子代理） | 基建-无需 | 壳内部复用件 |
| dsh-subagent-spawn-in-process | 进程内 spawn 子代理后端（ctx.agents 上跑全新子代理） | 借鉴-范式 | 轻量：spawn/fork 二分语义对照 SubAgentCoordinator |
| dsh-subagent | 抽象子代理 seam（ctx.subagents：命名 provider 注册表、续聊） | 接缝-预留 | Stage 2：与自研 orchestrator 的边界契约（委派/续聊/打断/列表词汇） |
| dsh-subprocess-local | subprocess seam 本地实现 | 基建-无需 | 壳自带 |
| dsh-subprocess | subprocess seam：托管进程组、有界溢出输出、升级式 kill、敏感环境擦除（scrubbedParentEnv/SENSITIVE_ENV_PATTERN） | 借鉴-范式 | 缺口：ModuleRunner 子进程治理（敏感环境变量擦除对 security/ 直接有用） |
| dsh-system-prompt | 系统提示组装注册表 | 借鉴-范式 | 缺口：专家提示分段注册组装（persona/instructions/技能段统一席位制） |
| dsh-terminal-bash | 持久 shell PTY 后端（subprocess terminal 原语之上） | 不适用 | Synova 无终端产品面 |
| dsh-terminal | 持久 PTY 会话 seam（owner 作用域 ids、后端注册表、交互发送、信号、等待清理） | 不适用 | 同上 |
| dsh-time-context | 可选持久每步上下文：当前时间与已耗时间 | 借鉴-范式 | 轻量：哨兵/专家判定"数据新鲜度"需要时间上下文注入 |
| dsh-timeout | 零依赖超时/截止原语：clampTimeout、deadline、timeoutOf、idleWatchdog、TimeoutReason（只计时+分类，不终止） | 借鉴-范式 | 职责分离范式：计时/分类与终止解耦——对照铁律 32 的 timeout 分类；MAX_TIMER_DELAY_MS 防 Node 定时器溢出 |
| dsh-tmux-context | 可选持久每步上下文：本 agent 的 tmux pane/window 位置 | 不适用 | tmux 专属 |
| dsh-token-meter | 重放感知 token 计量服务（ctx.tokenMeter） | 借鉴-范式 | 缺口：token/成本核算（CHARS_PER_TOKEN=4 估算 + surface fold 重放推导 + route-pricing 成价 + 用量投影） |
| dsh-tool-ask-user | 面向模型的 ask_user_question 工具（ctx.userQuestions 之上） | 借鉴-范式 | 轻量：结构化提问 schema（诊断追问可复用该交互契约） |
| dsh-tool-bash-persistent | owner 作用域持久 Bash 工具（Harness PTY 服务背书） | 基建-无需 | 壳工具 |
| dsh-tool-bash | bash 工具（可选通用后台任务与沙箱升级支持） | 基建-无需 | 壳工具 |
| dsh-tool-call-timeout-policy | 工具调用超时策略：tools/execute 包装器在 exec.signal 上挂 per-tool deadline，赢家时返回 TOOL_TIMEOUT | 借鉴-范式 | 缺口：工具级超时（超时作为结果返回而非抛异常，实现方自行响应信号——无强制 kill 的协作式取消） |
| dsh-tool-cordis | 自指 cordis 工具集：检视运行时、挂载/处置模型写的插件 | 不适用 | Synova 不开放模型写插件 |
| dsh-tool-fs-search | 面向模型的文件发现工具（glob/grep，打包 ripgrep 二进制） | 基建-无需 | 壳工具 |
| dsh-tool-fs | 面向模型的文件工具（read/write/edit，ctx.fs 之上） | 基建-无需 | 壳工具 |
| dsh-tool-goal | 面向模型的同会话 goal 工具（执行时权限检查） | 借鉴-范式 | 轻量：执行时 authority 判定范式（谁能调 complete/blocked） |
| dsh-tool-jobs | 面向模型的后台任务控制工具（job_output/job_list/job_kill） | 基建-无需 | 壳工具 |
| dsh-tool-pwsh-persistent | owner 作用域持久 PowerShell 工具（PTY 背书） | 基建-无需 | 壳工具 |
| dsh-tool-pwsh | 面向模型的 pwsh 工具（bash 执行器 seam 之上） | 基建-无需 | 壳工具 |
| dsh-tool-ralph | 面向模型的 fresh-agent Ralph loop（workflow+subagent seam 之上） | 不适用 | Synova 无此模式需求 |
| dsh-tool-skill | 面向模型的技能加载工具 | 借鉴-范式 | 轻量："模型主动拉技能"与 Synova dispatcher 主动发现互补（两阶段触发） |
| dsh-tool-str-replace-editor | 面向模型的 view/create/literal replace/line insert 编辑工具 | 基建-无需 | 壳工具 |
| dsh-tool-subagent-control | 全局命名 send_message/interrupt_agent/list_agents 工具（ctx.subagents 续聊之上） | 借鉴-范式 | 轻量：子代理续聊控制面词汇对照 orchestrator |
| dsh-tool-subagent-report | 子作用域 report 工具（ctx.subagents 续聊之上） | 借鉴-范式 | 轻量：子代理结果回传契约 |
| dsh-tool-subagent | 面向模型的子代理委派工具（ctx.subagents seam 之上） | 借鉴-范式 | 轻量：委派参数 schema 与 named-provider 选择 |
| dsh-tool-todo | 面向模型的 todo_write 工具（事件溯源会话日志背书） | 借鉴-范式 | 轻量：计划状态进事件日志（可重放可投影）——Synova 计划持久化缺口 |
| dsh-tool-web | 面向模型的 web 工具（web_search/web_fetch，ctx.web 之上） | 基建-无需 | 壳工具 |
| dsh-tool-workflow | 面向模型的 workflow 工具（JS 编排脚本跑 ctx.workflowEngine） | 不适用 | 多 agent 编排 Synova 自研 orchestrator 覆盖 |
| dsh-tools | 工具注册表与执行管线 | 接缝-预留 | Stage 2：MCP 边界的工具注册/执行管线契约 |
| dsh-typert-loader | 生成 Typert 包贡献的 Loader 集成 | 基建-无需 | 壳内部 |
| dsh-typert-protocol | 编译器无关 Remote 元数据与 Typert provider 协议 | 接缝-预留 | 远程契约协议词汇 |
| dsh-typert-registry | 生成反射与 Zod schema 的运行时注册表 | 基建-无需 | 壳内部 |
| dsh-user-approval | 用户审批 seam（ctx.approval）：一次性权限决策经审批/请求瀑布分发到组合 answerers，默认 fail-closed | 借鉴-范式 | 缺口：工具执行审批流（企业部署安全；fail-closed + answerer 瀑布） |
| dsh-user-questions | 抽象用户提问 seam（ctx.userQuestions：agent 运行中问人） | 接缝-预留 | Stage 2：人机交互契约 |
| dsh-util-crypto | 零依赖浏览器安全 UUID 与字节编码助手 | 基建-无需 | util |
| dsh-util-time | 线边界共享时间词汇（canonicalClientTimeZone：IANA 时区校验/规范化，不做格式化） | 基建-无需 | util（Synova cron 用 Host 时区，思路可对照） |
| dsh-util-values | 重复安装安全的值原语（deepFreeze 等） | 基建-无需 | util |
| dsh-util-workspace-path | 浏览器安全 Workspace 路径与显示助手 | 基建-无需 | util |
| dsh-web-app | dsh 浏览器面 bundle：dsh-base 上的 web patch 层 + 运行时胶水插件（前端 dist 服务、web-surface 提示、bash 运行时变量、URL line） | 替换-候选 | Stage 3：若试点 DSH Web GUI 整体换用；Synova 桌面品牌层自研时仅作参考 |
| dsh-web-fetch-http | ctx.web 的匿名公网 HTTP(S) fetch provider | 基建-无需 | 壳自带 |
| dsh-web-frontend | Web 应用入口：对 dsh-client-web shell 库的 vite 构建；dist 由 dsh web 服务 | 不适用 | Web 前端构建 |
| dsh-web-search-deepseek | ctx.web 的 DeepSeek 搜索 provider（Anthropic 兼容 API 的原生 web_search） | 基建-无需 | 壳自带 |
| dsh-web | 抽象 web 能力 seam（ctx.web：搜索/fetch provider 注册表、注册顺序无关选择、请求/结果词汇、WebError 分类） | 接缝-预留 | Stage 2：联网能力契约 |
| dsh-webhook-github | webhook 运行时的签名 GitHub HTTP webhook 适配器 | 借鉴-范式 | 轻量：签名校验适配器范式（企业系统事件接入可对照） |
| dsh-webhook | fire-and-forget webhook 规则运行时：创建 Workspace 背书的 DSH 会话 | 借鉴-范式 | 缺口：外部事件触发诊断（企业系统 webhook→规则匹配→新会话；与 Sentinel cron 互补的事件触发路） |
| dsh-win32-process | 面向 Windows 沙箱的低层 Win32 进程/stdio/Job Object 原语 | 不适用 | 平台专属 |
| dsh-workflow-worker-thread | worker-thread workflow 引擎：模型编排脚本离主事件循环执行，agent() 调用桥回 ctx.subagents | 借鉴-范式 | 轻量：编排脚本隔离执行（worker thread 不阻塞 host 事件循环）的隔离范式 |
| dsh-workflow | workflow 能力 seam（ctx.workflowEngine 服务、run 词汇、workflow/* 事件） | 接缝-预留 | Stage 2：编排词汇契约（若需 fan-out 对接） |
| dsh-workspace | workspace 实体注册表（ctx.workspaceRegistry：域数据形态上的持久 workspace 记录 + 校验会话挂接） | 接缝-预留 | Stage 2：多企业/多工作区对接 |
| dsh | dsh CLI：profile boot、插件管理、browser UI 别名 | 替换-候选 | Stage 3 壳试点的入口本体 |
| node-addon-landlock-run | Linux Landlock 自限后 exec 启动器（每平台预编译静态二进制 + 探测/对话 CLI 契约的 JS seam） | 不适用 | 平台专属 |
| schemastery | 类型驱动 schema 校验器 | 借鉴-范式 | 缺口：manifest/registry 文件的 schema 驱动校验（expert-registry.yaml/sentinel manifest 的统一校验原语；不引依赖，抄"类型声明即得校验+默认值"范式） |

## 二、重点包详注（22 个候选重点）

### 1. dsh-schedule
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{domain,persistence,projection,runtime,tools,transaction,client,types}.js`
- **主要导出**：`ScheduleId`、`ScheduleInputError/ScheduleLogError`、`createAfterScheduleRecord / createAtScheduleRecord / createEveryScheduleRecord`、`apply / foldScheduleEvents / decodeScheduleChange`、`resolveEveryOccurrence`、`registerScheduleTools`、`renderReminderFraming / renderEveryReminderBatchFraming`、`scheduleView`、`MIN_EVERY_INTERVAL_SECONDS / SCHEDULE_CHANGE_VERSION`
- **机制**：三种提醒（after 延时 / at 绝对时刻 / every 固定频率）作为 schedule 变更事件追加进会话事件日志；`apply`/`foldScheduleEvents` 把事件折叠成当前调度态，runtime 按 agent 挂定时器（bundle 内可见 `schedule.runtime()`/`schedule.lifecycle()` 生命周期），到期把 reminder framing 注入会话；`registerScheduleTools` 给模型暴露增删查工具，持久化层负责 flush，`MIN_EVERY_INTERVAL_SECONDS` 防止过密轮询。**与 src/cron 的关系**：cron 是进程级作业调度，schedule 是会话内 agent 作用域定时，二者互补不互替。

### 2. dsh-session-persistence-jsonl
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{format,index,win32,zstd-private-decoder,zstd-public-decoder,zstd}.js`
- **主要导出**：`JsonlSessionPersistence`（default）、`JsonlCompressionSchema`
- **机制**：实现 ctx.sessionPersistence seam：每会话一个 JSONL 追加日志。内部含 zstd 压缩帧编解码（`compressZstdFrame`、`ZSTD_MAGIC`、私有/公共解码器、解码让步间隔）、帧校验和（`CHECKSUM_OPTIONS`）、撕裂尾容错（`INCOMPLETE_FRAME_OPTIONS`）、分块解码（`DECODE_CHUNK_SIZE`）、pack 压实（`DEFAULT_PACK_CHUNKS`）、win32 目录/`MOVEFILE_WRITE_THROUGH` 耐久 rename 处理。**可借鉴**：追加写 + 压缩帧 + 校验和 + 容错重放的完整耐久日志方案，对照 src/store 的审计导出与压实。

### 3. dsh-session-projection
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{index,invariant,types}.js`
- **主要导出**：`SessionProjectionRegistry`（default）
- **机制**：定义"投影"这一抽象：每个投影 = 从会话日志折叠出的每会话派生状态，带类型表（可合并扩展）与 provider 契约；ctx.sessionProjections 注册表对外供全量现值。todo、统计、标题、目标、token 计量全部走同一投影机制——**Synova 缺口**：会话派生状态目前各自为政，此注册表范式可统一。

### 4. dsh-compaction-basic
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{config,index,region,summarizer}.js`
- **主要导出**：`BasicCompactionEngine`（default）
- **机制**：token-meter 驱动的压缩：上下文用量越过 `DEFAULT_THRESHOLD_RATIO=0.8` 触发，压缩后保留 `DEFAULT_RETAIN_RATIO=0.16` 近期内容；摘要由可配置 `summarizationProvider/summarizationModel` 经 dsh-llm 执行；产出 compact checkpoint source 记录（配合 dsh-compaction 的 `compactCheckpointSource`/`toolPairingBalanced*`）保证重放一致、tool_use/tool_result 配对不被拆散。

### 5. dsh-compaction-tool-result-pruner
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{config,index}.js`
- **主要导出**：`ToolResultPruner`（default）、`DEFAULTS`、`PRUNE_MARKER`、`codePointLength`、`resolveConfig`
- **机制**：无模型参与的工具结果修剪：对 tool-result surface 节点做头/中/尾保留，中部以 `PRUNE_MARKER` 标记；修剪是派生式（replay-safe）而非破坏性写回，任意时刻可从原日志重算；`codePointLength` 按码点而非字节计量（中文友好）。

### 6. dsh-agent-presets
- **lib/ 源文件**：`index.js`、`invariant.js`、`typert.host.js`、`typert.remote-client.js`、`types/{authoring,composition-inventory,discovery,index,metadata,mount,preset,session,specifier}.js`
- **主要导出**：`AgentPresets`（default）、`discoverPresets`、`mountPreset`、`copyComposition`、`readComposition / readPresetMetadata / renderPresetMetadata`、`scanRoot`、`livePresetMounts`、`leakedServices`、`standingMountFor`、`writableRoot`、`inactiveRows`、`serviceForAgent`、`agentPresetProjectionDefinition`、常量 `COMPOSITION_FILE / METADATA_FILE / SHIPPED_PRESET_ROOT / SETTINGS_NAMESPACE / AgentPresetSettingsSchema`
- **机制**：preset = 目录（cordis.yml 组合文件 + 元数据文件）；会话可挂载一个 preset 决定其 agent 组合（插件/指令/模型）；扫描发现（`scanRoot/discoverPresets`）、挂载账目（`livePresetMounts/standingMountFor`）、泄漏检测（`leakedServices` 抓逃出 preset 作用域的服务）与投影定义（供 UI/Remote 读态）。**可借鉴**：声明式 agent 组合 + 挂载泄漏检测，对照 expert-registry.yaml 的下一阶段。

### 7. dsh-persona
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`Config`、`PERSONA_SECTION`、`apply`、`inject`、`name`
- **机制**：组合（composition）可声明一段部署人格文本；插件把它作为 PERSONA_SECTION 注入系统提示组装（配合 dsh-system-prompt 的席位）。极小但对 Synova 的意义在分层：**部署级人格（企业语气/边界）与 7 位专家人格分开管理**。

### 8. dsh-settings-file
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`FileSettingsProvider`（default）、`resolveSpec`
- **机制**：settings.yaml 文件后端实现 ctx.settings seam；`resolveSpec` 把设置 spec 解析到具体文件位置（workspace/home 分层）；bundle 内可见 ENOENT/EEXIST 区分、patchNode 补丁式写回。**可借鉴**：声明式分层配置 + schema 化 spec 解析（Synova 目前配置散在 env/代码）。

### 9. dsh-jobs-local
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`LocalJobRegistry`（default）、`TASK_WAIT_TIMEOUT`
- **机制**：ctx.jobs 的进程内实现：后台作业的 id 分配、owner 隔离（每个作业绑定 owner 会话/agent）、轮询读取、取消、完成监听。是 bash 后台任务、workflow 等长工作的统一"后台作业"原语。Synova 的 loops 已有类似物，契约词汇可对照。

### 10. dsh-token-meter
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{breakdown-projection,client,estimate,index,invariant,projection,route-pricing,surface-fold,surface-projection,turn-usage,types,usage-projection}.js`
- **主要导出**：`TokenMeter`（default）
- **机制**：重放感知计量：不从运行时计数器取数，而从会话日志 surface 事件折叠估算（bundle 内 `CHARS_PER_TOKEN=4`、结构块 overhead），因此重放/重启后数字仍一致；route-pricing 按模型路由计价；breakdown/turn-usage/usage 多个投影供 UI 与预算检查。**可借鉴**：诊断成本可见性与 token 预算控制的完整方案。

### 11. dsh-llm-retry
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{brand,history,index,invariant,types}.js`
- **主要导出**：`Config`、`RetryId`、`apply`、`inject`（注入 `agents`、`sessionProjections`）、`name`
- **机制**：按 provider 路由配置重试策略（哪些错误/哪些路由重试、退避参数），注入 agents 生效；重试历史作为会话投影（types/history）留存供观测。**可借鉴**：重试策略配置化 + 重试史可观测，对照铁律 32 的 `.retryable` 分类落地。

### 12. dsh-webhook
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{brand,index,invariant,session,types}.js`
- **主要导出**：`WebhookRuntime`（default）、`WebhookDeliveryId`、`WebhookRuleId`、`WebhookSourceId`
- **机制**：规则运行时：外部 HTTP webhook 按 source/rule 匹配后 fire-and-forget 地创建 Workspace 背书的新会话，请求体经校验（`requiredString` 等）绑定为会话上下文摘要（boundContextSummary）；三类 id（source/rule/delivery）支撑追踪。**可借鉴**：外部事件→新会话的触发路，与 Sentinel cron 互补（事件驱动哨兵）。

### 13. dsh-spill-policy
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`Config`、`apply`、`inject`、`name`
- **机制**：纯事件门策略（无服务 API）：拦截超长纯文本工具结果，把进入上下文的内容替换为保留预览（`preview`）+ 溢出文件定位（配合 ctx.spillStore），并发 `spillNotice/spillReplacement` 说明性内容；`flattenPlainText` 判定适用面，`ownerSessionId` 保证溢出文件归属会话。**可借鉴**："策略即事件门"模式 + 上下文经济学（长输出不占窗口）。

### 14. dsh-timeout
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`clampTimeout`、`deadline`、`timeoutOf`、`idleWatchdog`、`TimeoutReason`、`MAX_TIMER_DELAY_MS`
- **机制**：零依赖计时原语：`clampTimeout` 收敛非法值、`deadline` 组合绝对截止、`timeoutOf` 从多种输入解析超时、`idleWatchdog` 闲置看门狗；**只做计时与分类（TimeoutReason），不做终止**——终止责任在调用方。`MAX_TIMER_DELAY_MS` 防 Node 定时器 2^31 溢出。

### 15. dsh-tool-call-timeout-policy
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`TOOL_TIMEOUT`、`apply`、`inject`、`name`
- **机制**：作为 tools/execute 包装器：每次工具执行前在 `exec.signal` 上挂 per-tool deadline；超时时**返回 TOOL_TIMEOUT 结果**而非抛异常，不强制杀实现——实现方自行响应信号（协作式取消）。与 dsh-timeout 的"计时分类不终止"哲学配套。

### 16. dsh-invariants
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`InvariantRegistry`（default）、`InvariantError`
- **机制**：包自有运行时不变量的注册表服务：各包把"运行时必须成立的条件"注册进来（`register`），违反时抛 `InvariantError` fail-loud。**对 Synova 的特殊意义**：铁律目前靠 pre-commit 静态执法，这是"架构约束在运行时也被执法"的补全范式（如五层边界、降级信号必须在场）。

### 17. dsh-message-feedback
- **lib/ 源文件**：`index.js`、`invariant.js`、`typert.host.js`、`typert.remote-client.js`、`types/{index,invariant,spec,types}.js`
- **主要导出**：`MessageFeedbackService`（default）、`messageFeedbackDomainSpec`、`messageFeedbackItemSchema / messageFeedbackRatingSchema / messageFeedbackRowSchema / messageFeedbackSessionIdentitySchema / messageFeedbackVersionSchema`
- **机制**：每消息评分/备注作为 sidecar 行存进 storage domain（schema 全套校验、带版本）；生命周期绑定到会话事件（消息存在则反馈存在），经 Host Remote 供 UI。**可借鉴**：反馈闭环（GA 评审→记忆层）的消息级锚点 + "域 spec + schema 族"的持久化定义方式。

### 18. dsh-goal
- **lib/ 源文件**：`index.js`、`invariant.js`、`typert.host.js`、`typert.remote-client.js`、`types/{client,domain,fold,index,invariant,runtime,types}.js`
- **主要导出**：`GoalService`（default）、`GoalId`、`GoalError`、`GOAL_CHANGE_VERSION`、`applyGoalProjection`、`foldGoal`、`decodeGoalChange`、`goalChangeRef`、`goalProjectionDefinition`
- **机制**：同会话目标的事件溯源状态机：set/progress/complete/blocked/clear 都是带版本的变更事件追加进会话日志；`foldGoal` 折叠出当前目标快照（objective/round/max/BlockReason），经 projection 注册表供 UI 与工具读取；权限（谁能 complete）由 goal 工具在执行时判定。与 dsh-goal-round-driver（续轮驱动）、dsh-tool-goal、dsh-command-goal 四件套配套。

### 19. dsh-permission-presets
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{client,index,invariant,types}.js`
- **主要导出**：`PermissionPresetService`（default）、`CUSTOM_PRESET`、`PERMISSION_SETTINGS_NAMESPACE`
- **机制**：把 sandbox-mode 与 approval-policy 两个安全旋钮打包为一个产品级 Permissions 预设（如 ask/默认/全权）；选择预设时**写穿（write-through）到各旋钮自己的会话事件**——下游消费方仍只读规范的单一旋钮状态，预设本身不成为第二真相源。**可借鉴**：预设=旋钮打包而非平行状态，避免双写不一致。

### 20. dsh-skill-filesystem
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/index.js`
- **主要导出**：`FileSystemSkillProvider`、`Config`、`apply`、`inject`、`name`
- **机制**：ctx.skills 注册表的本地文件系统 provider：扫描技能目录（SKILL.md + frontmatter），校验后登记名称/描述供模型按需加载（dsh-tool-skill 拉取内容）。**直接对照 Synova skills/**：同构机制，可对照其发现/校验/加载边界的实现取舍。

### 21. dsh-mcp-client
- **lib/ 源文件**：`index.js`、`invariant.js`、`types/{connection,index,tools,transport}.js`
- **主要导出**：`Config`、`apply`、`inject`、`name`
- **机制**：基于 @modelcontextprotocol/sdk：按配置连接多个 MCP server（StdioClientTransport / StreamableHTTPClientTransport 双 transport），ListTools 后把每个工具注册为 ctx.tools 的普通工具（`ctx.tools.register`）；监听 `ToolListChangedNotification` 重载工具表；传递 `scrubbedParentEnv` 擦除后的环境；对接 dsh-attachment 的图片准入错误。**对 Synova**：MCP 边界的另一侧——Synova 现在是 MCP server，Stage 2 要作为 client 连企业系统/外部 MCP 时此为范式蓝本。

### 22. dsh-llm
- **lib/ 源文件**：`index.js`、`invariant.js`、`typert.host.js`、`typert.remote-client.js`、`types/{adapter-failure,api-key,assembler,attribution,brand,call-config,content,error,index,invariant,message,retry-policy,types}.js`
- **主要导出**：`LlmRuntime`（default）、`LlmAdapter`、`BlockAssembler`、`createUserMessage / createAssistantMessage / createToolResultMessage / createMessage`、`freezeMessage`、`LlmError / HarnessError`、稳定码常量 `CONTEXT_WINDOW_EXCEEDED_CODE / QUOTA_EXCEEDED_CODE / INVALID_CREDENTIAL_CODE / EMPTY_RESPONSE_CODE`、守卫 `isContextWindowExceededError / isQuotaExceededError / isHarnessError`、`RetryPolicySchema / resolveRetryPolicy`、图片卸载族 `offloadRequestImagesWithPolicy / projectImagesForTextModel / resolveImageAttachmentAccess` 等、`attributionHeaders / userAgent / assertUsableApiKey / normalizeApiKey`
- **机制**：provider 中立 LLM 契约层：消息/分块词汇 + 适配器接口 + 运行时分发；错误以稳定码 taxonomy 分类并配类型守卫；BlockAssembler 负责内容块组装；纯文本模型的图片卸载策略族；API key 规范化与可用性断言。**可借鉴**：Synova providers/ 已有适配，但稳定错误码 + isXxxError 守卫 + 图片卸载策略是铁律 32 错误分类的具体化参照。

## 三、遗漏检查（名字不起眼但确认有价值的包）

1. **dsh-output-retention**（`ItemRetainer / TextRetainer / describeOmitted / formatRetentionNotice`）——有界保留原语 + "省略说明"中性文案。极易漏掉，但它把"截断"从信息损失变成诚实披露，直接可用于 Synova 长列表/长报告的截断呈现（对照铁律 44 的消息截断算法）。
2. **dsh-repeat-tool-reminder**——同参工具调用循环的 advisory 提醒。名字像小插件，实际是 ReAct 卡死防呆的成熟实现（提醒不阻断），对照专家循环质量。
3. **dsh-session-reference**——跨会话快照引用（URI 编解码、大小上限、候选限流）+ **不可信模型上下文标注**。后者是 prompt-injection 防线的具体机制，安全价值最高、名字最不显眼。
4. **dsh-session-checkpoint-policy**——模型请求与工具副作用前的语义耐久检查点。只有 `apply/inject/name` 三个导出，极易当成空壳跳过；实际是"副作用前先落盘"的崩溃恢复策略，对照 Sentinel 写库/建工单路径。
5. **dsh-hook-protocol**——名字像协议定义，实际含 matcher 引擎、stdin/exit-code/stdout 编解码、多 hook 合并、stderr 摘要、分离运行的完整实现。Synova 控制塔就是 hook 体系，这是最同构的可参照物。
6. **dsh-atomic-write**——`writeFileAtomic + withFileLock`。两个导出的 util 包，但原子替换+文件锁是控制塔状态文件/备份脚本（铁律 0-3/0-4）可直接借鉴的原语。
7. **dsh-deque**——只有一个 `Deque` 导出，零依赖环形双端队列，摊还 O(1)、有界空置存储。流式 buffer、有界历史队列的标准解，对照铁律 41 的流式管线约束。
8. **dsh-session-stats**——只有 `apply/inject/name`，纯投影：全日志会话计数与墙钟时间。是"投影范式最小实例"，也是 Synova 会话报告统计的现成公式。
9. **dsh-launch-environment**——不可变启动环境且**记录每个值来自哪一层**。配置来源可追溯（审计友好）范式，Synova 配置散在 env/yaml/代码，此范式值得抄。
10. **dsh-storage-domain**——`defineDomain`：schema 校验 + 事件发射的 KV 域。给"不够格进 SQLite 又不能只是内存"的状态（哨兵配置、反馈行、元数据）一个中间层，Synova 缺的就是这一层。
11. **dsh-subprocess**——seam 包但内部有 `scrubbedParentEnv / SENSITIVE_ENV_PATTERN`（敏感环境变量擦除）与进程组/升级式 kill/有界溢出输出的完整治理词汇，security/ 与 ModuleRunner 直接可用。
12. **dsh-time-context**——每步注入当前时间与耗时。哨兵/专家判断"数据新鲜度"（哪天的财报、距上次检查多久）的最小机制，名字完全看不出价值。
13. **dsh-goal-round-driver**——容易被并进 dsh-goal 一起看而漏掉其独立价值：**竞态围栏**（防双驱动）+ `renderGoalRoundPrompt` 的续轮提示注入（含"完成前先取证"措辞），是长任务自动续轮的完整方案。
14. **dsh-fs-observation-policy**——只有 `apply/name` 两个导出，实际是"观察态 + 读前先读 + 版本守卫写"的完整编辑安全策略（无服务 API 的事件门）。把 Synova 的"先读再改"纪律变成运行时机制。
15. **dsh-session-telemetry**——seam 包，但四段式（捕获→投影→**脱敏**→上报）管线值得对照：PIIScrubber 应处于上报出口强制位而非散点调用。
16. **dsh-session-query-sqlite**——名字平淡，但 SQLite FTS5 会话全文检索与 Synova L5 同栈，几乎可以直接落地。

**反向结论（看过、确认无遗漏价值）**：`dsh-tmux-context`（tmux 专属）、`dsh-skill-badge`（演示技能）、`dsh-agent-spine-demo`（demo）、`dsh-llm-pi-ai`（设计验证孪生）、`dsh-cordis-host-runner`（模型挂载动态包，Synova 不开放）、`dsh-code-runtime`/`dsh-code-runtime-worker-thread`（Synova compute 自研纯函数，不执行模型代码）、`dsh-win32-process`/`node-addon-landlock-run`/`dsh-sandbox-*`（OS 级沙箱，平台专属）。

## 四、落地顺序建议（按处置聚合）

- **Stage 1/2 设计期（借鉴-范式 62 项）**：优先读 10 个机制包——`dsh-session`（surface/fold 细节）、`dsh-session-projection`（投影注册表）、`dsh-compaction-basic` + `dsh-compaction-tool-result-pruner`（上下文经济学）、`dsh-token-meter`（重放感知计量）、`dsh-llm`（错误 taxonomy）、`dsh-timeout` + `dsh-tool-call-timeout-policy`（协作式超时）、`dsh-mcp-client`（MCP client 侧）、`dsh-webhook`（事件触发路）。
- **Stage 2 契约层（接缝-预留 28 项）**：先定 6 个核心 seam 的对接词汇——`dsh-tools`、`dsh-subagent`、`dsh-jobs`、`dsh-session-persistence`、`dsh-settings`、`dsh-web`；其余按需补。
- **Stage 3 壳试点（替换-候选 6 项）**：`dsh`（CLI）+ `dsh-base`（基座）起步，按形态加 `dsh-headless`（无头批量）或 `dsh-web-app`（Web GUI）；`dsh-sdk-minimal`/`dsh-sdk-app` 作为 SDK 消费形态备选。
