# DSH 运行时机制深潜报告 — 供 SynovaAgent 借鉴指引文档使用

> 调研日期：2026-09-06 ｜ 调研对象：DeepSeek Harness (DSH) 本地安装
> 位置：`/Users/wane/Library/Application Support/io.github.hairyf.deepseek-harness-desktop/dependencies/dsh/node_modules/@deepseek-ai/`（224 个包，版本 0.1.2-alpha.2）
> 方法：逐包读 `package.json`（description/exports）→ `lib/` 目录 → esbuild 产物中的 `//#region lib/xxx.js` 源码路径标记 → 直接读关键文件全文。
> **准确性声明**：下文所有包名、lib 路径、类名、函数名、常量名均为本次实际读到的内容（标注文件+行号区间的为逐行核对）。DSH 的 `lib/*.js` 是 esbuild 产物，多数包将源码内联在 `lib/index.js` 并以 `//#region lib/types/xxx.js` 标记源文件名；单独存在的 `lib/types/*.js` 是可独立 import 的源文件。无法确认的推断均标注"推断"。

---

## 1. LLM 适配 seam + 重试（dsh-llm / dsh-llm-deepseek / dsh-llm-retry）

### 机制综述

DSH 的 LLM 层是三层分离：**dsh-llm** 定义 provider 无关的抽象服务 `LlmRuntime`（adapter 注册表 + 可被 `llm/stream` waterfall 拦截的流式调用 API）和抽象基类 `LlmAdapter`；**dsh-llm-deepseek** 是第一个真实适配器（直连 fetch 的 chat-completions 实现），负责把 HTTP 状态码/错误文案归类为稳定错误码；**dsh-llm-retry** 是独立插件，挂在 agent loop 的 `agent/request-error` 恢复扩展点上执行 provider 拥有的重试策略。关键设计：重试策略归 **provider（adapter）** 所有而非重试插件所有（`RetryPolicySchema` 嵌在每个 provider 配置里），重试计数器本身是**会话事件溯源投影**（`llmRetry` projection），使重试状态跨进程恢复、可回放。错误分类用"稳定机器码 vs 人类消息"二分（`HarnessError.code`，注释明确写"route on this, never by parsing message"），并用正则集合识别各家 provider 的 context-overflow/quota 文案。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-llm | `lib/types/index.js` (L139-205) | `class LlmAdapter` | 适配器抽象基类。方法：`providerInfo(provider)`、`providerRetryPolicy(provider)`、`imageRequestPricing(provider, model)`、`listModels(provider)`、`resolveModel(provider, model, signal)`、`prepareCall(provider, model, signal)`（返回 `{model, stream(options)}` 一次性绑定） |
| dsh-llm | `lib/types/index.js` (L210-870) | `class LlmRuntime extends TypertRemoteService` | `llm` 服务：`registerAdapter(providers, adapter)`（重复注册抛 `DUPLICATE_ADAPTER`，all-or-nothing）、`prepareCall(config, signal)`（prepared call 只能 dispatch 一次，config 变更抛 `INVALID_PREPARED_CALL`）、`stream(options)` 经 `ctx.waterfall(this, 'llm/stream', options, …)` 可被中间件拦截；`adapterStream()` 把适配器一切失败转成终态 `finish` chunk（`{kind:'error',failure}` / `{kind:'aborted',failure}`），而中间件/消费者失败保持 throw |
| dsh-llm | `lib/types/error.js` | `class HarnessError extends Error`；`CONTEXT_WINDOW_EXCEEDED_CODE='CONTEXT_WINDOW_EXCEEDED'`、`QUOTA_EXCEEDED_CODE='QUOTA'`、`EMPTY_RESPONSE_CODE='EMPTY_RESPONSE'`、`INVALID_CREDENTIAL_CODE='INVALID_CREDENTIAL'` | 全家桶错误基类 + 规范化 provider 无关错误码；`isContextWindowExceededError(detail)` / `isQuotaExceededError(detail)` 用 5 组正则识别 OpenAI 兼容 provider 的溢出/配额文案；`errorChain(value)` 渲染完整 cause 链（只用于诊断显示，禁止解析） |
| dsh-llm | `lib/types/adapter-failure.js` | `normalizeLlmFailure(value)` | 把适配器边界抛出的任意值规整为可序列化 frozen `{message, code, status?, providerRetryAfterMs?, requestId?}`；跨包拷贝丢失类身份时用 own-property 验证兜底 |
| dsh-llm | `lib/types/index.js` (L65-100, L119-132) | `class LlmError extends HarnessError`（携带 frozen `failure` 快照）；`assertUsableApiKey(raw, pkg, ref)` | API key 校验在 dsh-llm 主类型文件内（不在 api-key.js）：`normalizeApiKey` 先静默 trim，空/非法字符分别报错，错误消息**绝不回显 key 内容**，只提示 `ref`（凭证引用名）和修复位置 |
| dsh-llm | `lib/types/api-key.js` | `normalizeApiKey(raw)`；正则 `LEGAL_API_KEY = /^[\x21-\x7E]+$/` | "能进 HTTP header"的传输不变量定义；拒绝 Latin-1 以避免不透明 401 |
| dsh-llm | `lib/types/retry-policy.js` | `RetryPolicySchema`（schemastery union：`normal`/`always` 两模式）；`resolveRetryPolicy(config, path)` | normal：`maxRetries`(默认5)、`retryableCodes`(默认 `['EMPTY_RESPONSE','RATE_LIMIT','SERVER','TIMEOUT','TRANSPORT']`)、`backoff{initialDelayMs:500, maxDelayMs:10000, jitterRatio:0.1}`；always：永不放弃，忽略 maxRetries。校验拒绝未知 key、重复 code |
| dsh-llm-deepseek | `lib/index.js`（bundle，region `lib/types/*.js`） | `class DeepSeekAdapter extends LlmAdapter` (L1536)；`httpErrorCode(status, error)` (L1512)；`providerRetryAfterMs(value)` (L1489)；`providerErrorDetail` (L589) | HTTP 状态映射：401/403→`AUTH`、413→`INVALID_REQUEST`、429→`RATE_LIMIT`、400→文案判 `CONTEXT_WINDOW_EXCEEDED` 否则 `INVALID_REQUEST`、≥500→`SERVER`、其余→`HTTP_<status>`；解析 `retry-after` 头（秒数或 HTTP 日期）；`x-request-id`/`x-deepseek-request-id` 提取 requestId |
| dsh-llm-deepseek | `lib/index.js` (L2015-2018) | `resolveApiKey` 内调用 `assertUsableApiKey(hit.value, "llm-deepseek", ref)` | key 从凭证 seam / 环境变量（`apiKeyEnv: z.string().role("credential-ref")`, L1860）解析后立即校验 |
| dsh-llm-retry | `lib/index.js`（全 186 行，单文件 bundle） | 插件 `name="llm-retry"`，`inject=["agents","sessionProjections"]`；`recover({agent,turn,step,provider,failure,retryPolicy,signal}, next)`；`backoff(...)`；`localDelay(config, retry, random)`；`register` 投影 `key:"llmRetry"` | 挂 `ctx.on("agent/request-error")`：失败码不在 `retryableCodes` → 放行 next；指数退避 `initialDelayMs * 2^(retry-1)` 截断到 `maxDelayMs` 再乘 jitter（±jitterRatio）；provider `Retry-After` 优先且超过 `maxDelayMs` 时 normal 模式放弃；**先 `session.append("llm/retry", …)` 持久化重试意图再可取消等待，再 append `"llm/retry-started"`**；重试计数从 `llmRetry` 投影读（`stateOf(agent.session,"llmRetry")[retryStateKey(provider,policyKey)]`），进程重启后计数不丢；`mode:"always"` 只转发已有决策，不自己加计数 |

### 对 Synova 的借鉴要点

Synova 的 `providers/`（DeepSeek/OpenAI/Gateway）应收敛为一个 `LlmAdapter` 式窄接口（`providerInfo/providerRetryPolicy/resolveModel/prepareCall→stream`），把 HTTP→错误码归类和 `Retry-After` 解析压进 adapter 层，编排层只 route on `code`（对齐铁律 32 的 `.code/.phase/.retryable`）。最值得抄的是 **重试计数 = 事件溯源投影**：把每次重试意图/启动写成 append-only 事件（等价于 `llm/retry`），Sentinel 长跑任务的重试就能跨进程恢复、可审计；以及 **重试策略由 provider 配置拥有** 而非重试器拥有，让不同 LLM 供应商可声明不同 `retryableCodes`。

---

## 2. token 计量 / 预算（dsh-token-meter）

### 机制综述

`ctx.tokenMeter` 是**单一 replay-aware 计量服务**：它把会话事件流折算成三种 O(1) 状态投影（用量 `tokenUsage`、上下文压力 `contextPressure`、构成 `contextBreakdown`），同时维护一个按节点计价的"model-visible 表面"供 `measure()` 输出。计量口径：provider 上报的四桶 `uncachedInputTokens / outputTokens / cacheReadTokens / cacheWriteTokens`（缓存读写分开计）；启发式估价按固定密度 `CHARS_PER_TOKEN=4`、每 block 结构开销 4 token 递归计价 text/reasoning/tool-call/tool-result，图片走 route 专属定价。**该包本身没有预算/上限强制机制**——它只产出 `pressureTokens/projectedTokens/contextWindow` 等数字；阈值判定和压缩动作属于 dsh-compaction-basic（见 §5）。投影状态必须 O(1)（要整包 checkpoint 持久化），因此替换范围的价格通过"影子价格协议"传递：替换前紧邻 append 一条 `compaction/summary` 或 `compaction/prune` 事件携带 `shadowedTokenCount`，折算器武装一个 claim、由紧随的 replace 事件消费。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-token-meter | `lib/index.js`（bundle；region `lib/types/estimate.js`） | `estimateContent(blocks)`、`estimateMessage(message)`、`estimateSystemTokens(header)`、`estimateToolsTokens(header)`；常量 `CHARS_PER_TOKEN=4`、`BLOCK_OVERHEAD=4` | 固定密度启发式计价器，计量服务与投影单元共用同一实现，保证两口径数字一致 |
| dsh-token-meter | region `lib/types/surface-projection.js` | `foldSurfaceProjection(claim, event)` | O(1) 表面 token 折算：compaction 事件武装 claim（`shadowedRange`+`shadowedTokenCount`），表面 replace 事件消费 claim 得出带符号 delta；replace 无相邻 claim → 静默零 delta（保回放），claim 范围不匹配 → fail loud |
| dsh-token-meter | region `lib/types/usage-projection.js` | 投影定义 `tokenUsageProjectionDefinition`（key `tokenUsage`，stateVersion 2）、`contextPressureProjectionDefinition`（key `contextPressure`，stateVersion 4） | 用量投影：`assistant/chunk` 的 usage chunk 是早期样本、`assistant/message` 是同 attempt 终样本，重复样本替换不重复计，`llm/retry-started` 关闭替换槽；压力投影：`pressureTokens = inputTokens + cacheRead + cacheWrite`（prompt 侧），`projectedTokens = pressureTokens + (surfaceTokens - sampledSurfaceTokens)` 把样本后表面变化外推进下一请求 |
| dsh-token-meter | region `lib/types/surface-fold.js` | `planSurfaceTokens(nodes, event)` / `commitSurfaceTokens(nodes, plan)`（plan/commit 两段式） | 逐节点计价表面：先只读校验（replace 范围必须存在于 nodes，否则判定日志损坏 fail loud），再原地提交，异常不产生半应用状态 |
| dsh-token-meter | region `lib/types/route-pricing.js` | `priceSurface(nodes, pricing)` | 按路由模型重定价图片：adapter 声明的 `imageRequestPricing` 替换图片结构价（visualTokens + 模型可见文本价）；定价返回条数不匹配 → fail loud |
| dsh-token-meter | region `lib/types/index.js` | `class TokenMeter extends Service`（`static inject=["sessionProjections"]`，`ctx.get("tokenMeter")`）；`measure(session, requestHeader)`；`_sync(session)`；`_foldEvent(...)`；`_estimateProviderAssistant(...)` | 服务门面：注册三个投影、监听 `session/event` 增量同步；`measure()` 返回 deep-frozen `{logRevision, baseline(kind:'usage'/'estimated'/'none'), surfaceDeltaTokens, totalTokens, surfaceTokens, nodes[]}`；usage 锚点仅在最新请求 envelope 匹配且总量不低于锚点估价时复用，否则全量重估 |

### 对 Synova 的借鉴要点

Synova 的 Sentinel/诊断长对话需要同款"**计量即投影**"范式：token 用量和上下文压力不是临时变量，而是从会话日志折叠出的持久状态（进程重启可恢复、UI 可直读），Compaction 触发器只消费数字不做测量。优先抄三件：四桶用量（缓存读写分离，DeepSeek 缓存定价差异大）、`projectedTokens`（把样本后表面增量外推，避免"压力读数永远滞后一个请求"）、以及 `plan/commit` 两段式折算（先全量校验再变异，坏事件永不半应用——直接对齐铁律 24/31 的可审计降级）。

---

## 3. 定时调度与冷恢复（dsh-schedule）

### 机制综述

`dsh-schedule` 提供 agent 级的 after/at/every 三种提醒（"Agent-scoped durable after, at, and fixed-rate reminders over the session event log"）。**没有独立的 job 持久化文件**：job（reminder）定义就是 frozen 记录 `{id, kind, prompt, afterSeconds|at|everySeconds, scheduledAt}`，其 create/delete/dispatch 全部以 `schedule/change` 事件写进所属会话的事件日志；"持久化在哪"的答案是**会话 JSONL 日志本身**（经 `ctx.sessions.flush` 持久化屏障落盘）。冷恢复没有专门函数——它是**结构性的**：每次唤醒都从事件日志重折（`foldScheduleEvents`），进程重启后 `ScheduleRuntime.start()` 第一次 `driveOnce()` 就重折出全部活跃 reminder，`dueDecision` 对错过的 every 触发点**不枚举、只取最新一个到期 occurrence 批量补发**（"skip missed occurrences, and batch one latest occurrence per overdue rule"）。投递是 session-local 的：会话活着才准点触发，否则变 overdue 直到会话恢复；工具描述 `CREATE_DESCRIPTION` 原文即如此声明。`at` 规则支持 IANA 时区 + 本地日期时间 + DST 解析（`resolveLocalInstant`/`canonicalizeTimeZone`）。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-schedule | `lib/index.js` bundle region `lib/types/domain.js` | `createAfterScheduleRecord(id, prompt, afterSeconds, now)` (L476)、`createAtScheduleRecord` (L497)、`createEveryScheduleRecord` (L532)、`ScheduleInputError`、`ScheduleLogError` | 构造三种 frozen job 记录；prompt 必须非空、`after_seconds`/`every_seconds` 必须正整数（every ≥ 300s，见 `CREATE_DESCRIPTION`） |
| dsh-schedule | region `lib/types/domain.js` | `foldScheduleEvents(events, seedLength)` (L442)、`applyScheduleChanges(folded, changes)` (L408)、`resolveEveryOccurrence(record, acceptedAt)` (L367)、`allocateScheduleId(folded)` (L458) | **唯一迁移权威**（"single transition authority shared by full-log replay and the incremental Session projection"）：create（id 不得复用）/delete（目标必须活跃）/dispatch（one-shot 不得带 acceptedAt，every 必须带；every 按 `target + floor((acceptedAt-target)/interval)*interval` 算最新到期点并推进 `scheduledAt`） |
| dsh-schedule | region `lib/types/projection.js` | `scheduleProjectionDefinition`（key `schedule`，stateVersion 1） | 会话投影单元：从 `seedLength` 起折叠 `schedule/change` 事件，`view` 直接输出活跃记录数组；superRefine 校验 seen/active id 唯一性 |
| dsh-schedule | region `lib/types/persistence.js` | `flushSchedulePersistence(ctx, session)`、`class SchedulePersistenceError` | 调 `ctx.sessions.flush(session)` 并要求至少一个监听者显式确认持久化完成——dispatch 前后各一次的 durable barrier |
| dsh-schedule | region `lib/types/transaction.js` | `runScheduleTransaction(agent, operation)` | 每 agent 串行化（WeakMap promise tail 链），避免并发折算/变更竞争 |
| dsh-schedule | region `lib/types/runtime.js` | `class ScheduleRuntime`：`start()`、`requestDrive()`、`driveOnce()` (L890)、`dueDecision(folded, now)` (L728)、`readFolded()`、`arm(target, now)`、`waitForIdle()` | 进程本地一次性运行时：`driveOnce` = 清计时器 → 持久化 preflight → `readFolded`（corrupt log 则 faulted + warn）→ `dueDecision` 三分支（`one-shot` 取最早到期；`every` 批量最新 occurrence；`wait` 按最近未来目标 arm timer，`MAX_TIMER_DELAY_MS` 截断）→ `agent.runMaintenance()` 内 `agent.followup(createUserMessage(..., source:{kind:"plugin", plugin:"schedule"}))` 注入提醒帧 + append `schedule/change{operation:"dispatch"}` → 持久化 postflight。agent 忙时改走 `waitForIdle()` |
| dsh-schedule | region `lib/types/tools.js` | `CREATE_DESCRIPTION`、`validateCreateArgs`、`foldForTool` | 模型可见的三个 schedule 工具；描述文本明确"Delivery is session-local: the reminder runs on time only while this session is live and otherwise becomes overdue until the session is resumed." |

### 对 Synova 的借鉴要点

Synova 的 `src/cron/CronScheduler` 已有持久化作业，但 DSH 展示了更简的范式：**定时规则不必有独立存储，它可以是会话事件流的一个投影**——冷恢复 = 重放日志，天然免费拿到"重启后补发/跳过"语义。Sentinel 的 cron 触发若改为"rule 定义持久化 + 每次唤醒重折 + dispatch 前后各一次 durability flush + agent 忙则等 idle 注入 user message"，能同时解决错跑恢复与"触发不打断正在跑的诊断"两个痛点。`dueDecision` 的"one-shot 优先 + every 只补最新点 + wait 按最近目标"三分支决策值得整段照抄。

---

## 4. 会话事件溯源（dsh-session / -persistence-jsonl / -projection / -query-sqlite）

### 机制综述

`dsh-session` 是整个系统的心脏：`Session` 持有严格 append-only 的事件日志（`seq = log.length` 连续契约），**log 是唯一事实源，"model-visible 表面"是从日志派生的投影**——`user/message / assistant/message / tool/result` 三类事件必须带 `surfaceOp` 标记（`append` 或 `replace{start,end}`）声明自己如何进入表面，非表面类型携带标记直接抛错。模型可见⟺已记录的强制点有两处：① `Session.append()` 在入口做 JSON 可序列化快照 + surface 合同校验（replace 必须完整覆盖被影子化节点、`sourceEventSeqs` 必须唯一且更早、tool/result 替换只许改 content）；② agent loop 构造请求时用 `session.deriveMessages()`（日志派生），不存在第二条内存消息通道——dsh-agent-presets 的注释原文："it is required outright by the repo's model-visible ⟺ logged rule"。JSONL 后端把日志落成 per-project/session 目录（可 zstd 压缩、torn-tail 恢复）；`dsh-session-projection` 提供带 `stateVersion` 版本化和 checkpoint 恢复的投影注册表（§1 重试计数、§2 token 计量、§5 压缩状态全部 ride 在上面）；`dsh-session-query-sqlite` 把已持久化会话索引进 SQLite FTS5 供全局搜索。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-session | `lib/types/known-event-types.js` | `KNOWN_SESSION_EVENT_TYPES`（Set，73 个类型） | 完整事件枚举：`user/message, assistant/chunk, assistant/message, tool/call, tool/result, tool/code-dispatch, step/start, step/end, turn/start, turn/end, request/header, request/context, compaction/start|end|summary|prune, llm/retry, llm/retry-started, schedule/change, agent-preset/selected, approval/asked|decided|policy, feedback/record, goal/change, todo/write, hook/invoked|result, sandbox/mode, session/title, session/end-seed, subagent/*, team/*, tool-workflow/*` 等。GENERATED 文件（`scripts/gen-persistence-catalog.ts` 生成），读到未知类型且无 `ignorable` 标记 → 拒绝解释（新版 harness 写的日志不能静默跳过） |
| dsh-session | `lib/types/index.js` (L483-532) | `class Session`：`append(type, data, ...opts)`、`get seq()`、`events` 快照 | **append-only 的物理保证**：唯一写路径是 `this.log.push(event)`（全文件无 splice/pop/索引赋值）；事件 `deepFreeze`（`seq=log.length`、`time=Date.now()`）；`snapshotJsonValue` 单次遍历拒绝 BigInt/函数/循环/Map/Set/Date；重入 append 抛错；返回值是进入日志的快照而非调用方仍可变的输入 |
| dsh-session | `lib/types/surface.js` | `SURFACE_EVENT_TYPES`（三类型）、`isSurfaceEvent` / `isAppendSurfaceEvent` / `isReplacementSurfaceEvent`、`surfaceOpOf(event)`（L124，违规即抛）、`planSurfaceEvent` / `applySurfaceEvent` / `replaySurface`、`deriveEventMessage(event)` | 表面层：replace 生成 `shadowedSeqs` 并 `state.nodes.splice(...)`；替换校验 `assertToolResultRewrite`（tool/result 替换只许改 content）；`isAppendSurfaceEvent` 注释明确"model-visible surface deliberately shadows replaced ranges, so it is the wrong source for a human transcript"——人类转录读 append-origin 事件，模型读表面 |
| dsh-session | `lib/invariant.js`（companion 插件 `session-invariant`，`inject:["invariants"]`） | `validateEvent(trace, event, fail)`、`requireOpenStep(...)` | 包拥有的关系不变量检查：seq 严格递增、turn/step 开闭配对（turn/start 时不得有 open turn、step 必须从 nextStep 开始、assistant/* 与 tool/* 必须命名当前 open turn/step 等）——见 dsh-invariants §附录 |
| dsh-session-persistence | `lib/index.js`（1537 行，抽象 seam） | `class SessionPersistence extends Service`（`ctx.sessionPersistence`） | 抽象接口：`create/ensureMaterialized/append/prepare/load/inspect/borrowSession/readFrom/flush`；`flush` 是共享持久化屏障（schedule 等包显式调用） |
| dsh-session-persistence-jsonl | `lib/index.js`（region `lib/types/format.js` L13） | `logSuffix(compression)`、`projectDir(root, cwd)`、`sessionDir`、`logPath(root, cwd, id, compression)`；region `win32.js` 目录 fsync 语义；`class JsonlSessionPersistence extends SessionPersistence` (L797)、`PersistenceCoordinator` | 落盘布局：`<root>/<projectDir(cwd)>/<sessionDir(id)>/*.jsonl[.zstd]`；SessionId 是未验证 branded string 必须 path-encode（防穿越）；首行 header + 事件行（`packChunks` 可把 chunk 打包）；`readRaw` 返回逐字节原文；torn final frame 按已提交前缀语义省略 |
| dsh-session-projection | `lib/index.js`（413 行） | `class SessionProjections`（`ctx.sessionProjections`）：`register(definition)`、`stateOf(session, key)`、`checkpoint(session)`、`restoreFloor(checkpoint)`、`viewCheckpoint(checkpoint, keys)` | 投影合同：`{key, stateVersion, stateSchema, init(header), apply(state, event), wire:{viewSchema, view}}`；同 key 不允许两个 stateVersion 共存（注册冲突抛错）；checkpoint 按 `{key:{seq, ver, state}}` 持久化，restore 时 `stateVersion` 不匹配 → 从 0 重放 |
| dsh-session-query-sqlite | `lib/index.js`（1148 行） | `class SqliteSessionQueryEngine extends SessionQueryEngine` (L475)；表：`search_state`、`persisted_sessions`、`persisted_docs`(FTS5 virtual)、`temp.live_sessions`、`temp.live_docs` (L71-135) | SQLite FTS5 全局搜索后端：持久化会话与活跃会话分表（global_generation 指纹比对增量重建），检索列含 `session_id, seq, type, time, surface, codepoint_length`，`ROW_NUMBER()` 窗口去重排序 |

### 对 Synova 的借鉴要点

Synova 的 `store/SessionStore` 若向此靠拢，收益是结构性的：**对话 = append-only JSONL 事件日志，模型可见历史 = 日志的纯函数投影**，则"模型看到了什么"永远可以从日志重建（审计/回放/UI 三合一），且压缩（§5）变成"追加 replace 事件"而非原地改写——这直接回答了铁律 24 想要的可审计降级。优先抄：① `surfaceOp` 标记协议（模型表面 vs 人类转录分离，人类永远看得到被压缩掉的内容）；② 投影注册表的 `stateVersion` + checkpoint/restoreFloor（投影代码升级后自动从零重放，不需迁移脚本）；③ append 入口的单遍 JSON 快照校验（一个 getter 不能给校验一个值、给存储另一个值）。

---

## 5. 上下文压缩（dsh-compaction / -basic / -tool-result-pruner）

### 机制综述

压缩是"**摘要替换，不删日志**"：`dsh-compaction` 定义抽象 seam（`ctx.compaction`，子类拥有触发策略/保留策略/摘要器），`dsh-compaction-basic` 是默认实现，`dsh-compaction-tool-result-pruner` 是模型无关的预处理。触发条件两条路：① **压力触发**——每步开始（`agent/pre-step` 钩子）用 token meter 测 `totalTokens`，≥ `thresholdTokens = contextWindow × thresholdRatio`（默认 0.8）则压缩；② **溢出恢复**——`agent/request-error` 钩子上 `failure.code === CONTEXT_WINDOW_EXCEEDED` 时强制压缩（绕过阈值，retain=0）并 `{kind:"retry"}` 重试请求。basic 策略：头锚定选择压缩范围，按 token 从尾往前累计保留 `retainTokens`（默认 `contextWindow × retainRatio`，0.16）的 verbatim 尾部，且绝不切开 assistant tool-call/result 配对（`toolPairingBalancedBefore/After`）。压缩执行：append `compaction/start`（兼作 durable 锁）→ 用**当前会话自己的 system prompt + tools + 被压缩区消息前缀**调 `ctx.llm.stream()`（摘要指令作为最后一条 user message 追加——这是刻意的 KV-cache 复用设计）→ append `compaction/summary`（含 `shadowedRange/shadowedSeqs/shadowedTokenCount` 影子价格）→ append 带框摘要的 `user/message`（`surfaceOp:{op:"replace",start,end}` + `sourceEventSeqs` 引用）→ append `compaction/end`；摘要必须严格小于被影子内容，否则 fail loud。回指方式：替换节点文本被 `<compacted-summary>…</compacted-summary>` 标签包裹 + 前言"This is an automatically generated checkpoint…"，且旧摘要块若存在会被指令要求合并而非照抄。tool-result-pruner：对每个超 `thresholdChars`（默认 8192）的 tool/result 文本做头 4096 + `[… middle pruned …]` + 尾 1024 的确定性裁剪，同样走 `compaction/prune` 影子价格 + `tool/result` replace 事件，"replay-safe"指替换规则是确定性的（同输入同输出），重放可重建。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-compaction | `lib/types/index.js` | `class CompactionEngine extends Service`（`ctx.compaction`）、`class ManualCompactionError`（code: `busy`/`changed`/`summary`/`commit`/`persistence`/`cancelled`）、`toolPairingBalancedBefore/After`、`compactCheckpointSource` | 抽象服务 + 手动压缩错误分类 + tool-call/result 配对平衡谓词 |
| dsh-compaction-basic | `lib/index.js` region `lib/types/config.js` | 常量 `DEFAULT_THRESHOLD_RATIO=.8`、`DEFAULT_RETAIN_RATIO=.16`；`resolveConfig`、`resolveTargetPolicy(config, target)`、`resolveCompactSpec(policy, contextWindow)` | 配置（顶层默认 + `modelPolicies` 按 provider/model 精确覆盖）：`thresholdRatio/retainRatio|retainTokens/summarizationProvider+Model/maxTokens(8192)/compactionRetries(1)/maxOverflowRetries(1)/auto` |
| dsh-compaction-basic | region `lib/types/summarizer.js` | `COMPACTION_INSTRUCTION`、`SUMMARY_OPEN_TAG/CLOSE_TAG`、`CHECKPOINT_PREAMBLE`、`summarizeWithLlm(ctx, config, input, agent, signal)`、`frameSummary(summary)` | 缓存复用摘要调用（envelope `purpose:"compaction"`）；8 段 Markdown 结构（Primary Request / Key Technical Concepts / Files and Code / Errors and Fixes / Pending Jobs / Current Work / Next Step / Critical Context）；拒绝图片输出 |
| dsh-compaction-basic | region `lib/types/region.js` | `selectCompactableRange(session, measurement, retainTokens)` (L380)、`compactSurfaceRegion(dependencies, session, start, end, agent, options, signal)` (L419)、`validateSurfaceRegion`、`prepareCompaction`、`summarizeCompaction`、`assertWholeSurfaceUnchanged` / `assertSelectedSpanStable`、`commitCompactionBody` (L585)、`inspectCompactionEntryState`、`buildSummarizationInput` (L650) | 选择→预检（durable 锁 = `compaction/start`，未匹配 start 未被 end/seed 闭合 → `busy`）→ 摘要 → 表面稳定复查 → 提交（summary 事件 + replace user/message）→ end；异步摘要期间表面变了 → `SurfaceChangedError` |
| dsh-compaction-basic | region `lib/types/index.js` | `class BasicCompactionEngine extends CompactionEngine`（`inject=["llm","tokenMeter","sessions"]`）；`_registerAutomaticCompaction()`（`agent/pre-step` + `agent/request-error` + `agent/status` 钩子）、`compactIfNeeded(agent, trigger, signal)` (L857)、`compactRegion`、`compactNow(agent, signal, sourceCommandId)`（手动，仅 idle agent，`runMaintenance`） | 两个自动触发源 + 压缩后重测循环（至多 `compactionRetries+1` 次）；溢出路径先 `toolResultPruner.pruneSession()` 再压缩 |
| dsh-compaction-tool-result-pruner | `lib/index.js`（全 197 行） | `class ToolResultPruner extends Service`（`ctx.get("toolResultPruner")`，`inject=["tokenMeter"]`）；常量 `PRUNE_MARKER="\n\n[... tool result middle pruned ...]\n\n"`、`DEFAULTS{thresholdChars:8192, headChars:4096, tailChars:1024}`；`pruneContent(blocks)`、`pruneSession(session)` | 只裁 tool/result 的纯文本（非文本 block 原样保留、顺序不变）；按 Unicode code point 切（不劈 surrogate pair）；每次替换 = append `compaction/prune`（影子价格）+ append `tool/result`（replace 单节点，`sourceEventSeqs:[原seq]`），事件数据除 `content` 外全保留 |

### 对 Synova 的借鉴要点

Synova 诊断会话（六阶段管道 + Sentinel 长跑）迟早撞上下文墙，DSH 给出的完整答案是三层防线，可整体映射：**溢出裁剪（spill/pruner，§附录）→ 工具结果瘦身（pruner）→ LLM 摘要替换（basic）**。最值得抄的三个细节：① 压缩事件四件套 `start/summary/replace/end` + 影子价格，让 token 投影 O(1) 地知道"替换省了多少"；② 摘要调用复用会话前缀（system/tools/消息前缀原样 + 指令追加为末条 user message）——对 DeepSeek 缓存命中是实打实的成本优化；③ `compaction/start` 即 durable 锁 + 表面稳定复查，把"压缩期间会话又动了"这类竞态变成可分类错误（`busy/changed`）而非静默错替换。

---

## 6. 预设与人格（dsh-agent-presets / dsh-persona）

### 机制综述

**preset = 一个目录**，目录里必须有组合文件 `agent.cordis.yml`（Cordis `Include` 可加载的插件行列表），旁边可选 `preset.yml` 显示元数据（`name/description/order`，只承载显示文本——`id` 是目录名、`trust` 来自发现根，都不可在此声明，防止本地预设伪装成官方）。组合文件是插件行列表：每行 `{name, disabled?, group?}`，`name` 按 `classifyRowSpecifier` 分为 `cordis:` 内建 / `./` 相对（预设自带文件）/ `file:` 与绝对路径 / 包名四类；`disabled` 支持字面量或 `!!js` 表达式。**模型/工具/系统提示词/权限全部以"挂载对应插件行"表达**：preset 不自带 schema 字段，而是通过组合行挂 `dsh-agent-default-model`（模型）、`dsh-persona`（人格）、工具包、`dsh-permission-presets` 等实现能力差异。挂载（`mountPreset`）在 agent 的 **scope context** 下 plug 子树，`ctx.tools`/`ctx.systemPrompt` 注册随 agent 生命周期 unwind；两道护栏：未达可用状态的行被拒、向 root realm 泄漏服务的行被拒（`leakedServices` 检查）。发现根分 `.system`（随部署，只读）与 `.agent-presets`（用户可写）；authoring 只允许整目录拷贝（不收组合文本，"authoring grants no capability the copied preset did not already carry"）。**多层叠加不是字段 merge 而是作用域 layering**：全局组合（部署层）→ agent preset 子树（per-session 层）→ 会话内选择（`agent-preset/selected` 事件）；同名 service 越接近 agent 作用域越优先（Cordis 作用域语义，推断——未见显式 merge 函数）。`dsh-persona` 是 scope-only 插件行：`{text, complete, includeRuntimeContext}`，向 `ctx.systemPrompt.section()` 注册名为 `deployment:persona`（`PERSONA_SECTION`）的 section，放在 `DEPLOYMENT_PERSONA`（order 0）槽位，仅遮蔽本会话的部署人格；全局挂载会与注册表自身注册冲突并 fail loud。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-agent-presets | `lib/index.js` region `lib/types/metadata.js` | `METADATA_FILE="preset.yml"`、`readPresetMetadata(directory)`、`renderPresetMetadata(metadata)` | 显示元数据读写；所有读取失败降级为空（坏名字不能导致 agent 起不来） |
| dsh-agent-presets | region `lib/types/preset.js` | `PRESET_ID = /^[a-z0-9][a-z0-9-]*$/` | id 即路径段，containment 边界 |
| dsh-agent-presets | region `lib/types/discovery.js` | `COMPOSITION_FILE="agent.cordis.yml"`、`USER_PRESET_DIR=".agent-presets"`、discovery + health pass（不执行任何插件代码即判定组合可解析） | 每次调用重读根目录（运行中新增预设免重启可见）；坏目录报为 roster 坏行而非跳过 |
| dsh-agent-presets | region `lib/types/specifier.js` | `classifyRowSpecifier(name)` | 四类 specifier 分类；discovery 健康检查与 mount import 必须用同一分类（否则"报健康却加载失败"） |
| dsh-agent-presets | region `lib/types/mount.js` | `class PresetTree extends Include`（`write()` 置空——preset 是输入不是持久化目标）、`mountPreset`、`livePresetMounts(within)`、`leakedServices(ctx, mount)` | scope 挂载 + 泄漏审计；bare 包名从 harness 自身 base 解析（本地预设目录的 node_modules 向上走不到 harness 依赖） |
| dsh-agent-presets | region `lib/types/authoring.js` | `copyComposition(roots, source, id, name)`、`deleteComposition`、`writableRoot(roots, presetId)`、`tightenModes(dir)` | 只允许整目录拷贝到第一个 `user` 根；拷贝收紧为 owner-only 权限；拷贝重写元数据时丢弃 name/order 只留 description |
| dsh-agent-presets | region `lib/types/composition-inventory.js` | `disabledContribution(value, evaluator)`、`combineDisabled(outer, own)`、`flattenRows(...)` | 组合清单的结构化读取（插件行 + 有效启用态），供 UI 展示 |
| dsh-agent-presets | region `lib/types/session.js` | `agentPresetProjectionDefinition`（key `agentPreset`，stateVersion 1）、事件 `agent-preset/selected` | 会话实际运行哪个 preset 记入事件日志；注释原文点名 model-visible⟺logged 规则（preset 决定模型看到的工具 schema 和 prompt section，所以换 preset 必须记录） |
| dsh-persona | `lib/index.js`（全 44 行） | `name="persona"`、`inject=["systemPrompt"]`、`Config = z.object({text, complete, includeRuntimeContext})`、`apply(ctx, config)` | `ctx.systemPrompt.section({name: PERSONA_SECTION("deployment:persona"), order: getSectionOrder("DEPLOYMENT_PERSONA"), text, complete?})`；`includeRuntimeContext:false` 时 `suppressRuntimeContext()` |

### 对 Synova 的借鉴要点

Synova 的文件驱动专家体系（`expert/` 8 位专家 + `expert-registry.yaml`）与 DSH preset 是同构思想，但 DSH 多两样值得抄：① **preset = 目录 = 组合文件 + 自带文件 + 元数据**，"挂载即注入"（模型/工具/人格/权限都是组合行）替代散落字段；② **scope 泄漏审计**（`leakedServices`：挂载子树向 root realm 发布 service 即拒绝）——对应 Synova 的"专家注册必须不污染全局"可写成物理检查。人格注入的 lesson 是"scope-only 行 + 固定 order 槽位 + 全局冲突 fail loud"，比字符串拼 prompt 可控得多。

---

## 7. 配置分层（dsh-settings / dsh-settings-file）

### 机制综述

`ctx.settings` 是抽象 settings seam：provider 存一个按 namespace 分节的原始文档；每个插件注册自己的 namespace schema（schemastery），读取时按固定顺序叠加 **schema 默认值 → 注册方组合 `base` 层 → 用户文档该 namespace 节**（`register` 的 docstring 原文："layers schema defaults, the registrant's composition `base`, and the user document section, in that order"）。合并算法 `mergeLayers`：plain object 递归合并，其余值（含数组）整值替换；写路径 `cloneJsonShaped` 剥离 `undefined` 实现 sparse-patch 不抹掉下层 key。`dsh-settings-file` 是文件 provider：默认 `<dshHome>/settings.yaml`（支持 .yaml/.yml/.json），chokidar 监视外部编辑热发布（自写抑制 = 文本缓存比对），写路径 = `withFileLock` 跨进程文件锁 → 先从磁盘 reconcile → YAML `patchNode` **注释保留的叶子级 diff**（改一个 key 不动兄弟 key 的注释/锚点/格式）→ `writeFileAtomic`（mode 0600）。**"dump-config 可检查实际生效配置"在 `dsh` CLI 包实现**：`dsh --profile <name> --dump-config`（互斥选项 `--dump-default-config`）→ `runDumpConfig(profile, defaultOnly, patches)` —— 不启动进程、不求值 `!!js`，把 profile 的 patch 层栈（每 bundle 一层 + profile 自身 patch 文件 + home patch 文件 + 每个 `--patch` 覆盖层）经 `renderConfigDump` 渲染成带来源注释的组合树打印。profile 定义为"an ordered stack of plugin-bundle patch layers under your own overrides"（`dsh` CLI 描述原文）。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-settings | `lib/index.js` region `lib/types/index.js`（L74 起） | `class SettingsProvider extends Service`（`ctx.settings`）：`register(ns, schema, options)`（options 带 `base` 与 `validate`）、`section(ns)`、`resolve(schema, base, userSection, validate)`、`update(ns, …)`、`reset(ns)`、`mergePatch`；`class SettingsConflictError`（code `SETTINGS_CONFLICT`，expected/actual revision）；`mergeLayers(under, over)` (L210)；`cloneJsonShaped(root, reject)` (L173)；`applyPathOp(section, op)` (L118) | namespace 命名 `/^[a-z][a-z0-9-]*$/`；注册是调用插件 fiber 上的 effect（插件卸载即注销）；写按 namespace 排队，revision 冲突显式报错 |
| dsh-settings | region `lib/types/redact.js` | `redactSecrets(schema, value)` | 敏感值在配置面展示前脱敏（描述中 `dumpView` 对 user/base 两层分别 redact 便于 UI 标记哪些字段被覆盖——推断自上下文） |
| dsh-settings-file | `lib/index.js`（全 285 行） | `class FileSettingsProvider extends SettingsProvider`：`load()`、`persist(ns, section)`、`persistSection`、`reconcileFromDisk()`、`refresh()`、`prepareDocument()`、`parse(text)`、`renderYaml(ns, section)`、`renderJson`、`enqueue(operation)`；`patchNode(document, path, current, next)` (L53)；`resolveSpec(config)` (L31) | 监视（`awaitWriteFinish` 防抖）+ 排队串行化（reload 与 write 永不交叉）；reload 失败警告并保留最后好文档，仅 invariant 违规向上传播；写 = 锁内 reconcile → 叶子级 patch → 原子写 |
| dsh | `lib/bin.js` (L77, L143-145) + `lib/dump-config-BNQ_bV66.js` | CLI 选项 `--dump-config` / `--dump-default-config`；`runDumpConfig(profile, defaultOnly, patches)`；依赖 `@deepseek-ai/dsh-app-boot` 的 `loadOptionalPatches/loadOverlayPatches/renderConfigDump`、`prepareProfile`（region `lib/types/dump-config.js`） | 生效配置检查：分层来源标签（bundle 包名 / profile patch 路径 / home patch 路径 / --patch 绝对路径）逐层打印 patches；default 模式跳过用户层与 overlay（`cordis.patch.yml` 损坏时的恢复诊断） |

### 对 Synova 的借鉴要点

Synova 的配置目前散在环境变量 + settings.json，值得抄的是三件套：① **namespace 注册制**（每模块声明自己的 schema + 组合 base 默认值，用户文档只存覆盖——resolve 顺序固定，"实际生效值"永远可计算可展示）；② **注释保留的叶子级 YAML patch**（`patchNode` 思路：人手写的 settings.yaml 不会因为程序写一次就丢注释）；③ **dump-config 范式**（不 boot 进程、逐层带来源标签打印组合结果，作为支持/调试的第一命令——对 Synova 的 `check:all` 家族是低成本高价值补充）。

---

## 8. 后台作业 + webhook（dsh-jobs / dsh-jobs-local / dsh-webhook / dsh-webhook-github）

### 机制综述

`dsh-jobs` 定义抽象 `ctx.jobs`（`JobRegistry`，子类实现），`dsh-jobs-local` 是进程内存实现（**作业不持久化**：进程重启即失，与 §3 schedule 的持久化形成对比——DSH 把"提醒/定时"归日志、把"伴随会话的长跑工具工作"归内存）。生命周期：`start(spec)` 校验（必须有 job controller 服务该 owner、kind/label 非空、owner 活跃作业数 < `maxConcurrentJobsPerOwner`（默认 10））→ 生成 `<kind>-N` id → 记录 `{status:"running", startedAt, reported:false, settled promise}` → 生产者的 `done` promise 结算；终态三类 `completed/killed/failed`（settlement first-wins，迟到生产者无效）；`kill()` → `cancel(reason)` + status `stopping`；teardown（owner dispose 或服务 dispose）→ cancel + await settled + 删记录，抛异常的 cancel 强制 fail 该记录并报告"可能孤儿"。owner 隔离：有 owner 的 job 只能被同 session id 的 caller 读/停（`assertAccess`，注释："Ids are predictable, so authorization — not secrecy — is the boundary"）；completion listener 和 change observer 按 scope 分层（`ScopedLayers`），只投递给 owner 作用域链上的注册者。`dsh-webhook` 是 provider 无关的 fire-and-forget 规则运行时：适配器投递 frozen delivery `{kind, source, deliveryId, receivedAt, ...}` → `dispatch` 同步启动所有匹配 kind 的规则 → 规则 `run(delivery, signal)` 返回 null 或 Session 请求 `{workspacePath, title, prompt, agentPreset, permissionPreset, model?}` → `createWebhookSession` 创建 Workspace 挂载的根 Session（失败逐级回滚：detach workspace → dispose agent）。**投递保证 = 无**：fire-and-forget，202 在内存 dispatch 后立即返回，不等待规则结算，不重试（失败仅 logger.warn）——可靠性靠 GitHub 的 webhook 重投机制兜底（推断）。`dsh-webhook-github` 是签名适配器：强制 POST + JSON content-type、Content-Length 声明校验 + 流式字节上限、`x-hub-signature-256` 用 `@octokit/webhooks` HMAC 验签（secret 走 credential seam，未配置返回 503）、三个 GitHub 头必填且唯一（`headersDistinct` 防头注入歧义）、验证通过才 parse JSON。

### 代码锚点表

| 包 | lib 路径 | 类/函数/常量 | 作用 |
|---|---|---|---|
| dsh-jobs | `lib/index.js`（全 65 行） | `JobId(id)`（branded，格式 `<kind>-N`）、`class JobRegistry extends Service` | 抽象契约 docstring 明确六条语义：注册outlive 生产者/控制器 fiber；owner 会话 id 隔离；settlement first-wins；无 controller 服务 owner 时 `start` 拒绝；完成通知在记录提交后最后发 |
| dsh-jobs-local | `lib/index.js` | `class LocalJobRegistry extends JobRegistry`：`start(spec)` (L131)、`list/get/read/kill/wait`、`settle(job, outcome)` (L365)、`servesOwner(owner)`、`assertAccess(job, caller)` (L313)、`ensureOwnerCleanup(owner)`、`disposeOwned(owner)`、`cancelForTeardown`、`attachController(name)`、`onJobDone(listener)`、`onJobsChanged(listener)`；常量 `DEFAULT_MAX_CONCURRENT_TASKS_PER_OWNER=10`、`TASK_WAIT_TIMEOUT` | `spec = {kind, label, owner?, run(): {cancel, readOutput?, done}, outputLimitBytes?}`；`wait` 支持 deadline + caller signal（可取消等待）；job 快照含 `reported` 标记（读或 kill 即视为已报告，供 UI 判"新结果"） |
| dsh-webhook | `lib/index.js`（全 284 行） | `class WebhookRuntime extends Service`（`ctx.webhookRuntime`）：`register(rule)`（rule = `{id, kind, run}`）、`dispatch(delivery)` (L248)、`snapshotDelivery(delivery)`、`startInvocation(registration, delivery)`、`disposeRegistration`；`createWebhookSession(ctx, delivery, ruleId, request, signal)` (L93)；brands `WebhookRuleId/WebhookSourceId/WebhookDeliveryId` | fire-and-forget 运行时：delivery 校验 + deepFreeze 后广播；每规则 AbortController + active 集合，dispose = abort + drain；Session 创建含 `installInitialModelSelection`（首个 durable request header 前固定创建时模型选择）与 prompt 注入（`source:{kind:"webhook", provider, deliveryId, ruleId, form:"notice"}`） |
| dsh-webhook-github | `lib/index.js`（全 193 行） | `createGitHubWebhookHandler(ctx, config)` (L109)、`readBoundedUtf8Body(request, maxBodyBytes)`、`contentLength(request)`、`requiredHeader(request, name)`（用 `headersDistinct`）、`isJsonContentType`、`parsePayload(body)`、`class WebhookHttpError`（携带 status）；插件 `name="webhook-github"`，`inject=["webServer","webhookRuntime","credentials"]`，`Config={source, path, secretEnv(credential-ref), maxBodyBytes}`，`apply(ctx, config)` 注册 exact 路由 | 验签 `new Webhooks({secret}).verify(body, signature)`（octokit）；响应策略：202（已受理）在 dispatch 同步返回后；400/401/405/413/415/503 各有明确分类；secret 不可用 → 503（不是 401，区分配置问题与攻击） |

### 对 Synova 的借鉴要点

Synova 若做"外部事件 → Agent 任务"入口（如客户系统 webhook 触发按需诊断），DSH 的分层值得照抄：**传输适配器（验签/限长/头校验 → 统一 delivery 形状）与规则运行时（fire-and-forget dispatch → 创建带 preset 的会话）彻底分离**，每条投递进会话时带完整来源 provenance（`source:{kind:"webhook", deliveryId, ruleId}`），审计链免费获得。后台作业方面，`maxConcurrentJobsPerOwner` + owner-scoped listener 分层 + `reported` 标记是三个低成本即可抄的细节；但要注意 DSH 作业**不持久化**，Synova Sentinel 工单这类必须跨重启的作业应走 §3 的日志投影路线而非此路线。

---

## 附录：快速速览（各 2-3 句）

**dsh-invariants（不变量强制机制）** — `lib/index.js`（123 行）：`class InvariantRegistry extends Service` + `class InvariantError extends Error`（`code="INVARIANT"`，`packageName` 归属）。每个包在自己的 `lib/invariant.js` companion 插件（如 dsh-session 的 `session-invariant`、dsh-compaction 的 compaction invariant）里向注册表登记运行时不变量检查（如会话日志的 turn/step 配对、seq 连续性），支持 `package_allowlist`/`package_blocklist` 正则过滤与 `enabled` 开关；普通包入口保持零诊断依赖，加载 companion 才启用——"检查可选、违约必炸"（INVARIANT 码的错误会穿透普通 contain 逻辑，见 dsh-llm `emitAdaptersUpdated` 对 INVARIANT 的特判）。

**dsh-spill-policy（上下文溢出处理）** — `lib/index.js`（169 行，插件 `spill-policy`，`inject=["tools"]`）：`tools/post-execute` 结果转换器——终态结果 UTF-8 超过 `maxInlineBytes` 时，全文存入会话级 spill 产物（`ctx.spillStore`），模型侧替换为 `TextRetainer` 头尾预览 + 定位符 + 取回指引（`spillNotice`/`preview` 函数）。第二臂 `tools/ptc-dispatch-log` 给 `tool/code-dispatch` 事件的日志副本设上限（模型值不动）；`read` 工具被模型臂跳过以防 `read→spill→read again` 死循环；spill 失败永不把成功工具调用变成 error（best-effort）。

**dsh-timeout + dsh-tool-call-timeout-policy（超时分层）** — dsh-timeout（140 行，零依赖）：`TimeoutReason extends Error`（携带能力自有 code + timeoutMs）、`clampTimeout(requested, def, max)`、`deadline(upstream, timeoutMs, code)`（AbortSignal 融合 + `MAX_TIMER_DELAY_MS=2147483647` 钳制）、`idleWatchdog`（只在 iterator `next()` 在途时计时的可重臂看门狗）——"只通知，不终止"，各能力自己停工并翻译原因。dsh-tool-call-timeout-policy（144 行，插件 `timeout-policy`）：`tools/execute` 包装器按工具声明的 `timeoutMs` 武装 deadline，自己的计时器赢了才把结果替换为 `isError` + `error.code=TOOL_TIMEOUT`（`toolTimeoutResult`），用 code 作用域防止嵌套外层 deadline 被误读为本插件超时——三层超时（进程 clamp / 每调用 deadline / 流 idle watchdog）各管一段。

**dsh-message-feedback（反馈闭环）** — `lib/index.js`（420 行）：`class MessageFeedbackService`（`ctx.messageFeedback`）+ 纯 schema 层（`messageFeedbackDomainSpec`，storage-domain 声明：`message_feedback` 表，行 = `{session:{createdAt, cwd?}, items:[{messageId, rating:'positive'|'negative', note?, version(uuid), createdAt, updatedAt}]}`，messageId/version 唯一性 superRefine 强制）。反馈是**与日志生命周期绑定的 sidecar**：`messageFeedbackSessionIdentitySchema` 用会话的 `createdAt+cwd` 指纹把 sidecar 行"围栏"到一个日志生命周期，fork/重放后的新生命周期读不到旧行——反馈永不污染错误会话（对应 DSH 的 `feedback/record` 事件则是进日志的另一路）。

---

### 总范式清单（给写借鉴指引文档的人的一句话索引）

1. **错误码先行**：稳定机器码 + 冻结可序列化 failure 快照，永远 route on code 不 parse message（§1）。
2. **状态即投影**：重试计数、token 计量、调度规则、preset 选择全部从 append-only 日志折叠，带 stateVersion（§2/3/4/6）。
3. **表面/日志二分**：模型可见历史是日志的纯函数；人类转录走 append-origin 事件（§4）。
4. **替换而非删除**：压缩/裁剪 = append replace 事件 + 影子价格（§5/附录 spill）。
5. **能力 = 作用域挂载**：preset/persona/权限都是 scope 子树的插件行，泄漏即 fail loud（§6）。
6. **配置三层 + 可 dump**：schema 默认 → 组合 base → 用户文档；`--dump-config` 带来源分层打印（§7）。
7. **持久化分级**：提醒/会话走日志（冷恢复免费），伴随会话的长跑作业走内存（重启即失）——按"死后是否需要复活"选路线（§3/8）。
8. **超时三层 + 只通知不终止**：clamp / per-call deadline / idle watchdog（附录）。
