<!--
  权威文档17-开发者任务地图-v1-0-20260730
  状态: 开发指引——Graph结构依赖任务地图
  依赖: 权威文档17-预期状态模型-v3-1, 代码审计报告
  结构: 每个任务=Graph节点(文件+状态+验收+依赖边)
-->

# Synova 开发者任务地图 (Graph结构)

> 2026-07-30 | 13个任务节点, 每节点标注: 入边(依赖什么) / 出边(被谁依赖) / 文件范围 / 验收标准
> 用法: 找无入边的节点→立即开始。入边全部变绿后→本节点解锁。

---

## 一、任务依赖图

```
          ┌──────────────────────────────┐
          │ N1: GA权限到期日 (A2)        │ ← 无依赖, 立即开始
          │ 文件: rbac.ts+enterprise.ts  │
          └──────────────────────────────┘

┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ N2: 专家清理 │     │ N3: GraphSt │     │ N4: Push通道│
│ 14→7+格式   │     │ ore增量查询 │     │ 注入真实    │
│ 文件: regis │     │ 文件: diag- │     │ pushChannel │
│ try+RULES*5 │     │ nosis-graph │     │ 文件: proac │
└──────┬──────┘     └──────┬──────┘     │ tive-push   │
       │                   │            └─────────────┘
       ├───────────────────┤
       ↓                   ↓
┌─────────────┐     ┌─────────────┐
│ N5: 报告统一│     │ N8: 自诊断  │
│ 新专家补金字│     │ Phase1 CLI  │
│ 塔格式     │     │ 文件: self- │
│ 依赖: N2   │     │ diagnosis.p │
└─────────────┘     │ y + product │
                    │ -health.py  │
┌─────────────┐     │ 依赖: N3    │
│ N6: 管道监控│     └─────────────┘
│ 文件: data- │
│ pipeline-mo │     ┌─────────────┐
│ nitor.ts    │     │ N9: V3视图 │
└─────────────┘     │ Views 4+5   │
                    │ 依赖: N2    │
┌─────────────┐     └─────────────┘
│ N7: 资源监控│
│ 文件: resou │
│ rce-monitor │     ┌─────────────┐
│ .ts         │     │ N10: 安装引导│
└─────────────┘     │ 文件: setup │
                    │ .html+.js   │
┌─────────────┐     └─────────────┘
│ N11: 跨部门 │
│ 信号机制    │     ┌─────────────┐
│ 文件: dept- │     │ N12: 知识审批│
│ memory-stor │     │ 文件: knowle│
│ e.ts        │     │ dge-store   │
└─────────────┘     └─────────────┘
```

---

## 二、节点详情

### N1: GA权限到期日 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/middleware/rbac.ts` (修改), `src/routes/enterprise.ts` (修改), `app/js/admin.js` (修改) |
| 当前状态 | `canAccessWorkspace` 对 ga 返回 true——无时间约束 |
| 目标状态 | ga 账户增加 `expiresAt` 字段。到期后 `canAccessWorkspace` 返回 false |
| 验收 | 设置GA到期日→到期当天GA无法登录 |
| 预估 | 1天 |

### N2: 专家体系清理 14→7 [入边: 0, 出边: N5,N9] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `expert/expert-registry.yaml` (修改), `expert/capital-cycle/RULES.md` (修改), `expert/customer-cycle/RULES.md` (修改), `expert/talent-cycle/RULES.md` (修改), `expert/finance-structure/RULES.md` (修改), `expert/competitive-strategy/RULES.md` (修改) |
| 当前状态 | 14个目录(9在registry+5未注册)。5新专家已建目录。旧7位仍在registry中active |
| 目标状态 | registry=7条目(host+3核心+tech+2扩展)。旧7位移入 `expert/_deprecated/` |
| 验收 | registry只有7个条目。旧7位不在registry中。新专家5个在registry中 |
| 预估 | 1天 |
| **被依赖**: N5(报告格式), N9(V3视图)——N2完成后它们才能准确检测新专家的RULES.md格式 |

### N3: GraphStore 增量查询 [入边: 0, 出边: N8] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/l4/diagnosis-graph-query.ts` (新增方法) |
| 当前状态 | `queryNodes(type, filters, graph)` 存在——无按时间过滤 |
| 目标状态 | 新增 `queryNodesCreatedAfter(graph, days)` 方法 |
| 验收 | 传入7→返回最近7天节点列表。传入0→返回空数组 |
| 预估 | 半天 |
| **被依赖**: N8(自诊断)——product-health.py 的数据管道维度需要此方法 |

### N4: ProactivePush 通道接通 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/agent/proactive-push.ts` (修改), `src/agent/synova-agent.ts` (修改) |
| 当前状态 | `new ProactivePush([])`——通道为空 |
| 目标状态 | 注入 Electron Notification + 系统托盘通道。`pushChannel` 非空数组 |
| 验收 | P0哨兵触发→Electron弹出系统通知→DND时段静默→紧急穿透DND |
| 预估 | 1天 |

### N5: 诊断报告格式统一 [入边: N2, 出边: 0]

| 属性 | 值 |
|------|-----|
| 文件 | 同 N2 的新专家 RULES.md × 5 |
| 当前状态 | 8/14专家有金字塔格式。5个新专家缺失 |
| 目标状态 | 5个新专家 RULES.md 补: Governing Thought + Key Judgments + 认识论标注(📊/🧠/🔮) + 数据充分度三级控制 |
| 验收 | 新专家 RULES.md 含金字塔三段式 |
| 预估 | 半天 |
| **入边**: 依赖 N2——专家清理后才知道哪些是新专家、哪些已废弃 |

### N6: 数据管道健康监控 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/ingest/data-pipeline-monitor.ts` (新建) |
| 当前状态 | `src/ingest/` 只有 index.ts——无管道健康监控 |
| 目标状态 | 检测最近7天 GraphStore 新增节点 > 0 |
| 验收 | 管道停止→标记degraded。管道恢复→自动恢复正常 |
| 预估 | 1天 |

### N7: 资源使用监控 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/services/resource-monitor.ts` (新建) |
| 当前状态 | 无系统资源监控 |
| 目标状态 | 内存/磁盘/CPU——接近上限时提前告警 |
| 验收 | 内存使用>80%→日志告警。磁盘<1GB→日志告警 |
| 预估 | 1天 |

### N8: 自诊断 Phase 1 CLI + 视图 [入边: N3,N6,N7, 出边: 0]

| 属性 | 值 |
|------|-----|
| 文件 | `scripts/audit/self-diagnosis.py` (新建), `scripts/control-tower/product-health.py` (新建), `scripts/control-tower/views/completion.py` (新建) |
| 当前状态 | Phase 0(D262-D264)已完成。Phase 1 待构建 |
| 目标状态 | `self-diagnosis.py` 六条件判定可用。`product-health.py` 五维检测可用 |
| 验收 | `python self-diagnosis.py --mode dev`→completion-scores.json。`python product-health.py`→product-health.json |
| 预估 | 3天 |
| **入边**: 依赖 N3(GraphStore增量查询→数据管道维度), N6(管道监控模块→数据管道维度), N7(资源监控模块→资源使用维度)。product-health.py 5维中有3维依赖这3个节点 |

### N9: 控制塔 V3 P2 视图 [入边: N2, 出边: 0]

| 属性 | 值 |
|------|-----|
| 文件 | `scripts/control-tower/views/workflow_graph.py` (新建), `scripts/control-tower/views/agent_health.py` (新建), `scripts/control-tower/generate-dashboard.py` (路由扩展) |
| 当前状态 | Views 1/2/3 已交付。View 4(工作流图)+View 5(Agent链路健康) 待构建 |
| 目标状态 | 工作流图可交互。Agent链路显示衰减检测 |
| 验收 | 仪表盘5视图可切换。工作流图节点可悬停查看详情 |
| 预估 | 3天 |
| **入边**: 依赖 N2——Graph Builder依赖专家体系的知识，N2完成后清理的专家目录作为输入 |

### N10: 安装引导与首次配置页 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `app/setup.html` (新建), `app/js/setup.js` (新建), `app/css/setup.css` (新建) |
| 当前状态 | 无——客户部署靠人工 |
| 目标状态 | 启动→配置页→输入serverUrl→测试连接→完成 |
| 验收 | 不了解Synova的人能在10分钟内完成安装配置 |
| 预估 | 2-3天 |

### N11: 跨部门信号机制 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/l4/department-memory-store.ts` (新建), `src/l4/agent-memory-store.ts` (扩展) |
| 当前状态 | 部门记忆不存在 |
| 目标状态 | AI检测跨部门关键词→匿名摘要→推送到关联部门 |
| 验收 | 销售部记录"客户投诉产品缺陷"→研发部manager收到匿名摘要通知 |
| 预估 | 2天 |

### N12: 知识库审批流水线 [入边: 0, 出边: 0] ← 立即开始

| 属性 | 值 |
|------|-----|
| 文件 | `src/l4/knowledge-store.ts` (修改), `src/routes/admin.ts` (新建) |
| 当前状态 | 知识写入直接active——无审批 |
| 目标状态 | draft→pending_review→approved/rejected。管理员审批界面 |
| 验收 | 新知识写入→管理员后台看到待审批列表→确认后全员可见 |
| 预估 | 2天 |

---

## 三、执行顺序：按入边数量分组

| 轮次 | 节点 | 入边 | 说明 |
|:---:|------|:---:|------|
| 第1轮 | N1,N2,N3,N4,N6,N7,N10,N11,N12 | 0 | 9个节点无依赖——全部可立即并行 |
| 第2轮 | N5,N8,N9 | 1 | N5依赖N2, N8依赖N3, N9依赖N2——第1轮完成后解锁 |
| 第3轮 | — | — | 全部节点在第2轮后可完成 |

9个节点可同时启动(第1轮)，3个节点等第1轮完成后启动(第2轮)。两轮完成全部12个节点。
