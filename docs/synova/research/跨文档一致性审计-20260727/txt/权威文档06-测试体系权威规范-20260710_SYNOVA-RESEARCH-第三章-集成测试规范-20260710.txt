Ch3: 集成测试规范 :root{--bg:#fafaf5;--s:#fff;--b:#e0dcd0;--t:#1c1c1a;--m:#6b6b63;--blue:#2563eb;--green:#2b8a3e;--red:#dc2626;--orange:#e67e22;--purple:#8e44ad} *{margin:0;padding:0;box-sizing:border-box}body{background:var(--bg);color:var(--t);font-family:Inter,PingFang SC,sans-serif;padding:24px} .page{max-width:1200px;margin:0 auto;background:var(--s);border-radius:8px;padding:40px} h1{font-size:2rem;font-weight:800}h2{font-size:1.3rem;font-weight:700;margin:32px 0 12px;border-bottom:2px solid #eeede8;padding-bottom:6px} h3{font-size:1.05rem;font-weight:600;margin:20px 0 8px;color:var(--blue)} table{width:100%;border-collapse:collapse;margin:10px 0 18px;font-size:.82rem} th{text-align:left;padding:7px 10px;border-bottom:2px solid var(--b);color:var(--m);font-weight:600;font-size:.73rem;background:#fafaf7} td{padding:6px 10px;border-bottom:1px solid var(--b);font-size:.79rem;line-height:1.55;vertical-align:top} .tag{display:inline-block;padding:2px 7px;border-radius:3px;font-size:.68rem;font-weight:600;color:#fff} .tg{background:var(--green)}.tr{background:var(--red)}.tb{background:var(--blue)}.to{background:var(--orange)} .block{border-left:3px solid var(--blue);background:#f8fafd;padding:10px 14px;margin:10px 0;font-size:.84rem} code{background:#f0f0eb;padding:1px 5px;border-radius:2px;font-size:.82rem} .fault-table td:first-child{width:140px} 第三章：集成测试规范——L2a 哨兵接线 + L2b 专家推理 + L2c 循环基础设施 三个子维度共享 L2 集成测试层 | 2026-07-10 3.1 L2a：哨兵接线测试 3.1.1 前置 Phase 0：Traversal 路径 YAML 入库 核心原则 ：接线测试需要"已知正确的 traversal 路径"作为基准。基准未入库 = 无法判断偏离。Phase 0 的路径 YAML 入库是 L2a 的前置条件。 项目 内容 输入来源 哨兵权威规范（第一章）中每个哨兵的"可执行路径规范"字段——已定义 DiagnosisContextNode→42 边→compute 的完整链路 输出格式 extensions/traversal/{sentinel-name}.yaml ——结构化 YAML 路径模板 {sentinelName, diagnosisContext→causalEdges[{id, parameters[]}]→compute[{contractId, inputParams[]}]→findingSchema} 产出数量 50 个 YAML 文件（对应哨兵权威规范中的 50 个哨兵） 3.1.2 接线测试模板 每个哨兵 1 个集成测试用例，验证以下 5 项： # 验证项 通过标准 失败时阻断 1 L0 存活探针 哨兵进程启动 → 消费输入 → 在 30s 内输出符合 Schema 的 Finding 阻断 2 Traversal 路径匹配 代码中的 traversal 路径（哨兵→边→compute）与基准 YAML 一致 阻断 3 Finding 非空 给定 mock 本体数据，哨兵产出的 Finding[] 非空数组 阻断 4 边引用验证 Finding 中引用的边 ID 在 42 边体系中存在（grep 全量 42 边 ID 列表确认） 阻断 5 严重度阈值匹配 Finding.severity 与哨兵规范中定义的阈值表一致（正常输入→无 Finding 或 severity=LOW；异常输入→severity 匹配预期级别） 告警 3.2 L2b：专家推理验证（新增维度） 3.2.1 为什么需要专家推理验证 诊断质量的灵魂不在 compute 函数里，在专家 Agent 的系统提示词里。compute 输出的是"信号"，专家把信号转化为"诊断结论"。如果提示词设计不当——死锁性偏见、认知嫁接、闭合循环——compute 再正确也是白费。 只测 compute 不测专家 = 只验证"系统会不会算错"，不验证"系统会不会想错"。 3.2.2 五个验证维度 维度 验证内容 故障模式 验收标准 1. 角色对齐 给定一组 Finding，专家是否只输出自己职责范围内的诊断？ 角色漂移（组织专家开始做财务分析） 角色越界率 2. 证据链一致性 专家 B 引用的证据是否真实存在于专家 A 或上游推理中？ 认知嫁接（引用了不存在的前序推理）、闭合循环（引用 B→A→B 自循环） 证据幻觉率 = 0（零容忍） 3. 输出稳定性 同一组 Finding 输入 10 次，专家输出是否一致？ LLM 随机性导致诊断摇摆（"上周健康，这周重病"） 噪声度 4. 边界识别 当 Finding 不足以支撑诊断时，专家是否输出"信息不足"而非编造结论？ 幻觉生成（数据不足时强行给出具体结论） "无法判断"率在信息不足场景下 ≥90% 5. 因果推理完整性 从 Finding 到诊断结论，因果链是否完整且可追溯？ 跳跃推理（"毛利率下降，所以应该换 CEO"） 每个诊断结论可追溯到 ≥1 条 42 边 3.2.3 专家推理验证集格式 字段 内容 验证集 ID EXPERT-VERIFY-{expertType}-{seq} ，如 EXPERT-VERIFY-STRATEGY-001 输入 一组标准化的 Finding[]（JSON 数组）——模拟 42 边不同边类型的信号组合（单边信号 / 多边交叉 / 信息不足 / 冲突信号） 预期输出 诊断结论模板——应包含：核心发现 × 1-3 条 / 每条可追溯到 ≥1 条 42 边 / 置信度 / 不应包含的越界内容 评分规则 完全匹配（核心发现+因果链均正确）= 1.0 / 部分匹配（核心发现正确但因果链不完整）= 0.5 / 完全不匹配（方向错误）= 0 / 幻觉（编造不存在的信息）= -1.0（ 阻断 ） 3.2.4 每位专家的最低验证用例数 专家 正常场景 边界/冲突 信息不足 最低总数 战略专家 2 1 1 4 商业模式专家 2 1 1 4 组织专家 2 1 1 4 财务专家 2 1 1 4 营销专家 2 1 1 4 技术专家 2 1 1 4 行动专家 2 1 1 4 知识专家 2 1 1 4 合计 16 8 8 32 3.3 L2c：循环基础设施测试（新增维度） 5 个 Loop Engineering 循环是 Agent 的生命线。以下 4 个验证维度确保循环"能跑通、断了能恢复"。 # 验证维度 测试方法 验收标准 1 循环启动 mock Cron/事件触发 → 检查循环管理器是否正确启动 5 个循环 100% 启动成功。启动失败 → 阻断 2 状态机 模拟 running→completed / running→failed 转换 → 检查状态是否正确写入 状态转换正确率 100%。连续 3 次 failed → 必须触发断裂告警 3 三级恢复 模拟循环中途中止 → 验证 L1 自动重试 → 若 L1 也失败 → L2 半自动（GA 一键恢复） → 若 L2 也失败 → L3 手动（管理员介入） 三级恢复链路完整。L1→L2→L3 升级正确 4 依赖传递 模拟企业诊断循环完成 → 检查输出是否正确传递到部门导航循环。模拟 GA 校准操作 → 检查 GA 进化循环是否消费了校准数据 跨循环数据传递正确。消费者循环在输入到达后 1 分钟内感知 3.4 Phase 0 前置条件总结 条件 L2a 依赖 L2b 依赖 L2c 依赖 Traversal 路径 YAML 入库 必须 不依赖 不依赖 JSDoc @contract 补全 必须 不依赖 不依赖 专家推理验证集设计 不依赖 必须 不依赖 循环管理器可用 不依赖 不依赖 必须