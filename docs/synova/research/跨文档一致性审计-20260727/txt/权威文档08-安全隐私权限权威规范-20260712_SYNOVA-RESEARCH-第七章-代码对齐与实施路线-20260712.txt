Ch7: 代码对齐与实施路线 :root{--bg:#fafaf5;--s:#fff;--b:#e0dcd0;--t:#1c1c1a;--m:#6b6b63;--blue:#2563eb;--green:#2b8a3e;--red:#dc2626;--orange:#e67e22} *{margin:0;padding:0;box-sizing:border-box}body{background:var(--bg);color:var(--t);font-family:Inter,PingFang SC,sans-serif;padding:24px} .page{max-width:1200px;margin:0 auto;background:var(--s);border-radius:8px;padding:40px} h1{font-size:2rem;font-weight:800}h2{font-size:1.3rem;font-weight:700;margin:32px 0 12px;border-bottom:2px solid #eeede8;padding-bottom:6px} h3{font-size:1.05rem;font-weight:600;margin:20px 0 8px;color:var(--blue)} table{width:100%;border-collapse:collapse;margin:10px 0 18px;font-size:.78rem} th{text-align:left;padding:6px 8px;border-bottom:2px solid var(--b);color:var(--m);font-weight:600;font-size:.7rem;background:#fafaf7} td{padding:5px 8px;border-bottom:1px solid var(--b);font-size:.74rem;line-height:1.5;vertical-align:top} .tag{display:inline-block;padding:1px 5px;border-radius:3px;font-size:.65rem;font-weight:600;color:#fff} .tg{background:var(--green)}.tr{background:var(--red)}.tb{background:var(--blue)}.to{background:var(--orange)} .block{border-left:3px solid var(--blue);background:#f8fafd;padding:10px 14px;margin:10px 0;font-size:.84rem} .map-card{border:1px solid var(--b);border-left:4px solid var(--green);background:#f6faf4;padding:10px 14px;margin:8px 0;font-size:.8rem} .map-card-title{font-weight:700;color:var(--green);margin-bottom:4px} 第七章：代码对齐与实施路线 逐文件审计 | ✅🔧➕判定标准 | 原则-实现映射卡汇总 | 实施优先级P0/P1/P2 | 2026-07-12 7.1 代码对齐判定标准 符号 含义 标准 ✅ 可复用 核心接口/数据结构/实现逻辑与规范完全一致，无需任何修改即可直接使用 🔧 需修改 核心逻辑方向正确，但接口签名、数据范围、边界条件需扩展或调整。若已有部分实现→标记为🔧+标注"扩展模块，核心逻辑保留"，不新建 ➕ 需新建 该功能在现有代码中完全不存在，需从零实现（参考技术实现方案的接口定义） 7.2 逐文件审计 File 1: src/security/pii-scrubber.ts 维度 内容 判定 ✅ 可复用核心逻辑 + 🔧 需扩展 复用部分 S1-S4分级脱敏引擎(L36-L74)——四个PII_PATTERNS的正则表达式定义完整；opt-out机制(L140-L141)；roleMask角色掩盖(L146-L153)。scrub方法(L76)的分级脱敏逻辑方向正确。 需修改 (1)增加隐私预算追踪——每次PII字段被访问时记录访问日志(L76的scrub调用点增加审计事件)。(2)S3薪资脱敏策略扩展——当前仅为hash，需改为区间分组(如"10-20k")以满足老板保护场景。(3)增加PolicyEngine集成——scrub方法根据调用者角色(role参数)选择脱敏级别，而不只是"当前用户级别" 对应原则 P5 数据不用于训练 + T2 PII数据泄露 + 3.3老板保护场景 File 2: src/security/credential-vault.ts 维度 内容 判定 ✅ 可复用 复用部分 AES-256-GCM加密存储——connector_credentials表结构(encrypted_data+iv+auth_tag)已满足S4禁止数据的加密存储要求。主进程不持有明文的设计正确。 需修改 无。该文件已满足S4禁止数据的安全存储标准。 对应原则 T2 PII数据泄露——S4禁止数据存储 File 3: src/middleware/auth.ts 维度 内容 判定 🔧 需修改 复用部分 JWT HMAC-SHA256签名逻辑——sub/role/orgId/iat/exp/jti字段设计完整。jti唯一性(L27)+撤销列表机制设计正确。devMode降级逻辑可保留。 需修改 (1)增加tenant_id强制绑定——JWT payload中的orgId字段(L27)必须强制非空。当前开发环境下可能为undefined。(2)增加GA租户切换审计——GA切换激活租户时生成新的JWT(新orgId)，记录切换日志。(3)生产环境下移除L43-L44的"默认admin"降级——改为"默认staff+PolicyEngine裁决" 对应原则 P1 租户隔离 + T1 跨租户数据泄露 + T9 身份伪造 File 4: src/middleware/rbac.ts 维度 内容 判定 🔧 需修改（核心改动最多） 复用部分 五角色定义(L11)——admin/manager/liaison/staff/ga。DEFAULT_ROLE='staff'机制(L13)——最受限角色作为默认，方向正确。canAccessWorkspace(L53-L59)和canModifyWorkspace的department隔离逻辑(L72) 需修改 (1) 扩展六角色 ——增加boss角色(新增L11类型定义)。(2) 强制tenantId注入 ——所有workspace访问函数增加tenantId参数。SQL查询必须携带tenant_id过滤条件。(3) PolicyEngine集成 ——将当前if/else角色判断(L54/L71-L76)替换为PolicyEngine.evaluate(role, dataLevel, SOI)调用。(4) GA跨租户隔离 ——GA角色增加activeTenantId绑定。切换租户需显式操作+记录审计日志。(5) 生产环境默认安全 ——移除L43-L44开发环境默认admin降级 对应原则 P1租户隔离+P2默认安全+P10中国墙+T1/T3/T5 File 5: src/l4/audit-store.ts 维度 内容 判定 🔧 需修改 复用部分 append-only表结构——无UPDATE/DELETE能力，设计正确。字段snake_case+camelCase双格式兼容。 需修改 (1)增加prev_hash和current_hash字段形成哈希链。(2)增加RootHashPublisher模块——定时(每小时)计算根Hash→发布到外部不可变存储。(3)增加哈希链完整性验证查询——verifyChain()→{valid: boolean, brokenAt?: datetime} 对应原则 T6审计日志篡改+T12操作的不可否认性缺失 File 6: src/l4/knowledge-store.ts 维度 内容 判定 🔧 需修改 复用部分 KnowledgeChunk的权限标签结构——accessLevel/accessTeamId/accessOwnerId/accessSensitivity已定义。FilterClause不感知调用方身份的设计方向正确。 需修改 (1)增加PreUploadValidator调用——所有上传操作必须先通过PII/机密关键词预检。(2)增加匿名化聚合功能——查询接口在返回跨企业聚合数据时自动应用K-匿名性(检查k≥3)和差分隐私噪声(ε≤1.0)。(3)增加定期重审Cron任务——每季度检查已发布的聚合数据的匿名化有效性 对应原则 T5知识基座隐私泄露+T15数据生命周期泄露 7.3 需新建的模块 模块 接口 来源 PolicyEngine evaluate(role, dataLevel, soi)→{allow, denyReason} 权限研究-技术实现方案 TraversalPermissionFilter filterNodes(userContext, nodes[])→nodes[] 技术实现方案 DataExporter export(tenantId)→{archive, manifest} GDPR Art.20 DataPurger purge(tenantId)→{stages[]} GDPR Art.17 RootHashPublisher publishRootHash(chainRoot)→{txId} 审计合规 PreUploadValidator validate(file, tenantId)→{ok, warnings, blocked} 技术实现方案 PromptInjectionDetector detect(input)→{injected, pattern} Agent对标路线3 7.4 实施优先级 优先级 内容 依赖 P0 rbac.ts tenantId强制注入 + auth.ts tenant_id绑定 + PolicyEngine + DataExporter + DataPurger 合规底线——缺失=法律风险 P1 TraversalPermissionFilter + audit-store哈希链 + RootHashPublisher + PreUploadValidator + GA跨租户隔离 信任建立——中国墙+审计不可篡改+知识基座安全 P2 匿名化聚合(K-匿名性+差分隐私) + 提示注入防护 + 数据投毒应急响应 + 数据生命周期定期重审 世界级标准——联邦进化+对抗性安全+隐私防护