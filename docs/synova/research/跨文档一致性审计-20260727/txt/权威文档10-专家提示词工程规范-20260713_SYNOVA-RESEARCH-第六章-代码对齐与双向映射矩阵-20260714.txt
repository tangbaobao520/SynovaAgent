Ch6: 代码对齐与双向映射矩阵 :root{--bg:#fafaf5;--s:#fff;--b:#e0dcd0;--t:#1c1c1a;--m:#6b6b63;--blue:#2563eb;--green:#2b8a3e;--red:#dc2626;--orange:#e67e22} *{margin:0;padding:0;box-sizing:border-box}body{background:var(--bg);color:var(--t);font-family:Inter,PingFang SC,sans-serif;padding:24px} .page{max-width:1200px;margin:0 auto;background:var(--s);border-radius:8px;padding:40px} h1{font-size:2rem;font-weight:800}h2{font-size:1.3rem;font-weight:700;margin:32px 0 12px;border-bottom:2px solid #eeede8;padding-bottom:6px} h3{font-size:1.05rem;font-weight:600;margin:20px 0 8px;color:var(--blue)} table{width:100%;border-collapse:collapse;margin:10px 0 18px;font-size:.74rem} th{text-align:left;padding:5px 6px;border-bottom:2px solid var(--b);color:var(--m);font-weight:600;font-size:.68rem;background:#fafaf7} td{padding:4px 6px;border-bottom:1px solid var(--b);font-size:.7rem;line-height:1.45;vertical-align:top} .tag{display:inline-block;padding:1px 4px;border-radius:3px;font-size:.62rem;font-weight:600;color:#fff} .tg{background:var(--green)}.tr{background:var(--red)}.tb{background:var(--blue)}.to{background:var(--orange)} .block{border-left:3px solid var(--blue);background:#f8fafd;padding:8px 12px;margin:8px 0;font-size:.8rem} 第六章：代码对齐与双向映射矩阵 逐文件审计 | 32×N映射矩阵 | 嵌入式测试蓝图 | Token预算 | 2026-07-14 6.1 双向映射矩阵 — 方法论 正向映射 （第一至五章每条规则已标注）：每个提示词模块→显式标注L2b验证用例编号。 反向映射 （本章产出）：每个L2b验证用例→检查现有8位专家提示词中是否有一一对应的模块。 映射矩阵与测试蓝图的关系 ：嵌入式测试蓝图是映射矩阵的可执行验证层。每个映射条目（提示词模块→验证用例）必须附带一个测试蓝图。测试失败→先修改提示词→再更新映射矩阵。映射矩阵永远反映当前提示词的真实状态，而非理想状态。 6.2 32×N映射矩阵（核心交付物） 用例 验证维度 对应提示词模块 当前8位专家状态 C-01 角色对齐 M1 角色定义（第一章AgentSpec.角色声明） ✅ IDENTITY.md有定义。需补充edgeScope/computeScope C-02 角色对齐 M1 角色定义 ✅ C-03 角色对齐 M1 角色定义 ✅ C-04-08 输出稳定性(5位专家×10次重复) M4 交叉验证规则（第三章输出稳定性指令） 🔧 当前无稳定性控制指令 C-09-16 证据链一致性(8位专家) M4 交叉验证规则（第三章结构化引用格式） ➕ 当前完全不支持结构化引用 C-17-24 因果推理完整性(8位专家) M2 工具调用指令 + M3 推理链模板 🔧 有四层追溯概念但无提示词强制指令 C-17-24 因果推理完整性 M2 工具调用+M3 推理链模板 🔧 有四层追溯概念但无提示词层的强制指令 C-25-27 边界识别 M5 边界识别规则（第一章AgentSpec.不可跨界+第四章边界输出） ✅ ExpertDefinition.boundaries已定义 C-28-29 边界识别(信息不足) M5 边界识别规则（第四章信息不足标准输出） ➕ 当前未强制信息不足时的标准输出格式 C-30-32 数据冲突+信息不足 M6 数据冲突感知（第四章has_conflict模板+置信度标注） ➕ 完全不支持has_conflict感知 6.3 逐文件代码审计 文件 路径 判定 具体说明 ExpertDefinition packages/engine-core/src/pipeline/diagnosis/expert-prompts.ts L30-200 🔧 缺edgeScope/computeScope/crossDomainRule字段。缺business_model和knowledge专家定义（当前仅6位）。buildSystemPrompt函数(L210)需重构为6模块加载器 IDENTITY.md ×8 expert/{name}/IDENTITY.md ✅ 8个文件均已有角色声明和领域定义。可直接作为M1模块原材料 expert-prompts.json extensions/locales/zh-CN/expert-prompts.json ✅ role_prefix/domain_scope/not_generic/insufficient_evidence可直接复用 PROMPT.md ×8 expert/{name}/PROMPT.md ➕ 8个文件全部空白。每份需按本规范新建完整提示词（6模块全部纳入） expert-prompts.test.ts packages/engine-core/src/pipeline/diagnosis/__tests__/expert-prompts.test.ts 🔧 需扩展为验证6模块结构的测试。增加本规范的嵌入式测试蓝图 6.4 嵌入式测试蓝图 每个提示词模块附带至少1个测试蓝图。验证项可被自动化脚本判定： 模块 自动判定标准 M1 角色定义 输出中是否包含"你是{专家名}"声明；是否在越界查询时输出拒绝句式 M2 工具调用 输出中是否包含evidenceRefs字段；confidence字段是否在0-1范围 M3 推理链 输出中是否包含从证据到结论的追溯链；是否标注了置信度 M4 交叉验证 引用其他专家时是否包含[source: expert_name, evidence_id: XXX]格式 M5 边界识别 信息不足时是否输出"无法判断"而非编造结论 M6 数据冲突 has_conflict=true时输出是否必然包含"数据冲突"标注 6.5 Token预算 模块 估算Tokens 加载条件 M1 角色定义 ~300 始终加载 M2 工具调用 ~800 始终加载 M3 推理链 ~1200 始终加载 M4 交叉验证 ~500 多专家场景加载 M5 边界识别 ~400 始终加载 M6 数据冲突 ~400 有冲突数据时加载 合计(单专家) ~3100-3600 ≤4,000预算