---
status: implemented
date: 2026-08-18
task: D451
tags: [control-tower, ct-42, d331, session-brief]
---

# D451 — CT-42 session 专属 brief 读侧接线 + D331 补记死循环豁免

## 决策
修两个今日反复踩坑的体系缺陷：

1. **CT-42（并行隔离）**：attach.py 已写 session 专属 current-brief（`.claude/current-brief.<sid>`，D329），但读侧 pre-commit-check.sh + sop-gate.sh 仍读全局文件 → 写专属读全局，脱节。修复：读侧优先 `DSH_SESSION_ID` 专属文件，无则回退全局。

2. **D331（补记死循环）**：补记提交本身改 bypass.log，它自己在 `$BASE..HEAD` 范围内且缺记录 → 每次补记→新提交→再缺，无限循环。修复：check-bypass-log.sh 豁免"只改 .claude/bypass.log 的提交"（纯补记提交）——语义正确：补记改的就是证据文件本身，不能被要求"自己被自己记录"。

## 理由
- CT-42 是"多路并行"的地基（创始人强调要并行加速）
- D331 死循环今天浪费最多时间（反复补记+推送失败）
- 两处都是 M3 模式（机制已建未接线）的变体
