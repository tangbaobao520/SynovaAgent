---
status: implemented
date: 2026-08-18
task: D454
tags: [ct-41, pr-status, ci, token]
---

# D454 — CT-41⑥ PR 合并状态入仪表盘 + API token

## 决策
CT-41 六项里不依赖 K3 的：⑥ PR 状态集成（③ P0 积压自动化另做，⑤ 已由方案 B 覆盖）。

gen-cto-health.py 加 analyze_prs（开放 PR 列表入 §七）+ _gh_token（匿名 API rate limit → token 走 GITHUB_TOKEN env / .synova-ci-token 本地文件，不硬编码防 Secrets 门禁）。

## 理由
- 匿名 API 被 rate limit（今天 CI 检测多次调用耗尽）
- token 不入库（.synova-ci-token 是 CI 注入），Secrets 门禁安全
