---
状态: implemented
日期: 2026-08-19
决策: D445 用真实路由 + 表行数断言 + 二次触发计数构建 GS-05 告警闭环场景（复用 GS-03 fixture，零改产品代码）
理由: S0-3 验收点「sentinel_tickets 有行 + 推送去重键稳定」需要机器可执行证据。四决策点走双参考系收敛：触发用 POST /api/sentinel/run/:id 真实路由（铁律 12 + D453 已修 runSentinelOnce db:undefined）；断言用 sqlite 临时库表行数（rows>=1 / ==N，真空零显式声明防 K3 P0-3 fail-open）；去重稳定性用同窗口二次触发计数不变（INSERT OR REPLACE + D6 去重窗口的物理可观察结果，信号 id 动态不可静态预测）；fixture 复用 GS-03 erp-low-cash（现金 3 万/月耗 12 万 → runway 0.25 < critical 6，最少机制）。
---

# D445 — GS-05 告警闭环场景脚本

## 决策上下文

- **触发场景**: 产品进度页 S0-3（告警闭环）无机器证据——sentinel_tickets 有行 + 推送去重键稳定只有"人工看过"没有"机器验证"。
- **方案**: scripts/golden-scenarios/GS-05-alert-closure/（run.sh + fixtures + expect.json + README），复用 D361 common/ 四工具与 GS-03 同构模式；不改任何 src/ 产品代码。
- **依赖边界**: 主链路（触发/工单/去重）依赖 main 现有实现（runner.ts INSERT OR REPLACE + D6 去重窗口 + D272 finding.id 去重键 + D453 runSentinelOnce 修复）；D356 越阈告警增强若未合 main，则阈值告警断言按诚实 RED 交付。
- **参考系**: 参考：Anthropic（真实路由/物理输出/机器可验）+ 第一性原理（最少机制复用）——四决策点收敛无分歧。

## 相关 D#

- D445（本任务，GS-05 告警闭环）
- D442（GS-03 资本循环先例——同构模板 + erp-low-cash fixture 来源）
- D361（GSS common/ 基建）
- D453（runSentinelOnce db:undefined 修复——触发链路前置）
- D356（哨兵阈值告警——GS-05 依赖，待合 main）
