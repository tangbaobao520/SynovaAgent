---
状态: implemented
日期: 2026-08-21
决策: D462 升级 better-sqlite3 v11.10.0 → v12.11.1 解锁 Node 24（服务启动崩溃根因）+ 修 GS-03 run.sh 三处（JWT/SYNOVA_DB_PATH/后台 bootstrap）
理由: 排查实测 better-sqlite3 v11.10.0 的 Statement::~Statement() 在 Node 24 下触发 `Assertion failed: (env) != nullptr`（api/hooks.cc:142）必然崩溃——哨兵加载期 prepared statement 被 GC 时崩，服务起不来，GS-03/GS-05 全部阻塞；`file is not a database` 是同一阻塞链次生症状。开源实证（WiseLibs/better-sqlite3#1376 + 多项目 11.10.0→12.x 升级）指向 v12 为 Node 24 兼容线；v11 是代码级不兼容，rebuild 无效；Node 22 逃生路已断（新 addon ABI 137 无法在 127 加载）。共享 node_modules 用 --no-save 直升解锁全 session，package.json/lock 走 PR 正式落地。GS-03 run.sh 同步 GS-05 的三修复（JWT 自举防 401、SYNOVA_DB_PATH 显式指向临时库防写真实库、后台 bootstrap 防命令替换挂起）。GS-03 由诚实 RED 转全绿（3/3）。
---

# D462 — better-sqlite3 v12 升级（Node 24）+ GS-03 解锁

## 决策上下文

- **触发场景**: CTO 派发 GS-03 排查（`file is not a database`）+ GS-05 重跑，两场景服务器启动必崩。
- **根因链**: better-sqlite3 v11.10.0 + Node 24 teardown 不兼容（崩溃）→ 服务起不来 → db 初始化时序混乱 → `file is not a database` 次生症状。
- **修复**: ① 升级 better-sqlite3 ^12.0.0（锁 12.11.1）；② GS-03 run.sh 三修复；③ 全绿验证。
- **参考系**: 参考：Anthropic（机器可验）+ 第一性原理（最少机制）+ 开源实证（better-sqlite3#1376）

## 相关 D#

- D462（本任务）
- GS-03 = D442（场景转绿）、GS-05 = D445（场景重跑，仍诚实 RED——D356 工单缺口）
- D355/D453（阈值告警链路依赖，已入 main）
