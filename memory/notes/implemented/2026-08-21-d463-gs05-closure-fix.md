---
状态: implemented
日期: 2026-08-21
决策: D463 用选项 A 完成 GS-05 告警闭环——run-once 接 runner 管线 + critical/emergency 按严重度自动建工单（专家增强不门控），复用 I2 单源
理由: 诊断实测 runSentinelOnce 绕过 runner 管线（工单永不落库），且工单路径依赖 ExpertDispatcher（需 LLM，headless 未初始化）。选项 A（创始人批准）：工单=告警闭环产物（按严重度自动，INSERT OR REPLACE 幂等），专家=增强。配合 D354（信号 id 去时间戳）→ 去重键稳定。GS-05 由 2/4 诚实 RED 转 4/4 全绿。
---

# D463 — GS-05 告警闭环修复

## 决策上下文

- **根因**: ① runSentinelOnce 直连 check 绕过 runner 管线（tickets API 有视图工单但 DB 表 0 行）② 工单唯一写路径 storeExpertReport 依赖 ExpertDispatcher（SubAgentCoordinator 需 LLM，headless 未初始化 → "[runner] ExpertDispatcher 未初始化"）。
- **修复（选项 A）**: run-once 走 runner.runOnce + aggregateAndDispatch（复用管线）；dispatchSignalsToExperts 对 critical/emergency 自动建工单（createAutoTicket，诊断=finding 摘要），专家可用时 enrich。
- **参考系**: 参考：Anthropic（机器可验/fail-closed）+ 第一性原理（告警闭环=按严重度可追踪）+ 开源惯例（监控→工单按严重度）。

## 相关 D#

- D463（本任务）
- D354（去重键去时间戳——dedup-stable 前置，已合 main）
- D356（阈值告警接线——此前误判为阻塞，实为独立缺口）
- GS-05=D445（场景，2/4 RED → 4/4 全绿）
