# D516 CI strict 模式 — K3 D515 FAIL 返修决策记录

> 触发: K3 2026-08-24-D515.md FAIL（P0-1「CI 为权威」幻觉）

## 决策
1. **SYNO_CI strict 模式**（P0-1 根治）：soft_check/v5_soft/warn_check 三函数加 SYNO_CI=1 分支 → CI 上软提示转 HARD_FAIL。ci.yml Iron Laws job 注入 SYNO_CI: "1"。这才是 spec 项 3「本地减负+CI 权威」的物理落地——D515 实现只做了前半（本地软），后半（CI 硬）缺失致 K3 判 FAIL。
2. **去重 ~690 行**（P1-1）：三重函数定义/FASTLANE 块/并行告警块各收敛为一。1444→~760 行。
3. **P2×4**：version.log V9.9.9 污染清除 / D515 impl.files 修正 / tag V5.0.0 待合并后重打。

## 教训（M14 候选）
spec 写「ci.yml 已有权威兜底」时未物理验证 CI 是否真有 strict 路径——**声称基础设施存在必须 grep 验证**（与 F5 口径教训同源：声称必须来自命令输出）。
