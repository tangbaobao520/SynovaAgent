# 决策 Note：verify-parallel 已完成任务文档豁免（V5.0.1）

## 状态
2026-08-24 | 已实施（创始人授权越界补丁，DSH 预审） | P2

## 日期
2026-08-24

## 决策
`verify-parallel --scan-today` 对比今日全部 dev doc 写集时，跳过"写集全部已在 origin/main"的文档（任务已交付）；origin/main 不可解析时不豁免（fail-closed）。活跃任务间写集重叠仍 block。

## 理由
第一性原理：写集文件已在 main = 任务已合并交付的物理判定；后续任务修订已完成任务的写集是串行演进（D481 → D483 同文件），不是并行冲突。Anthropic 基线：判定可机器验证（git cat-file -e origin/main:path），保护不削弱（活跃重叠仍拦）。
