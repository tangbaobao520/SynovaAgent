---
状态: implemented
日期: 2026-09-06
决策: CT-60 D328 提交声明提取大小写/后缀兼容——MSG_DID 正则 \(D[0-9]+\) 扩展为 scope 前缀提取（[Dd] 兼容 + 归一大写 + 后缀兼容）+ 旧格式回退
理由: 2026-09-06 CTO 单日实测三次误拦（docs(d580)/docs(d578) 全被判「声明(无)」）——conventional commits 的 scope 是自由字符串，门禁只认纯大写 (D578) 形态 = 合法声明被拒。之前未暴露纯因「无认领 brief 时 fail-open」侥幸。
---

## 变更清单
1. scripts/commit-msg-check.sh L54: MSG_DID 提取两段式——①conventional scope 优先（sed 提取括号内容 → grep ^[Dd][0-9]+ → tr 归一大写）②回退旧模式（首行独立 (D578)/(d578) 引用）
2. tests/control-tower/commit-msg-consistency.test.sh: +3 用例（11 小写一致绿 / 12 后缀提取绿 / 13 小写劫持仍拦——大小写兼容不放松劫持检测）

## 同批测试债修复（同文件存量失败 6 例，根因非本变更）
- fixture 路径 scripts/control-tower/gen-task-board.py → src/utils/gen-task-board.ts：D534 Note 门禁（2026-08 后加）对 control-tower 路径要求 Note 引用，测试消息无 Note → 全部「期望 0 实际 1」。fixture 换中性路径后 Note 门禁不触发，D328 语义测试恢复纯粹
- 用例 1 STAGED_COUNT: macOS BSD wc -l 前导空格 → 字符串比较恒失败（tr -d ' ' 修复）
- 结果: 套件从 7/6（基线红）→ 16/16 全绿

## 效果与边界
- 劫持检测不放松：小写 (d318) vs 认领 D320 仍 exit 1（用例 13 锚定）
- fail-open 语义不变：无认领/无 python/消息异常仍跳过（铁律 24 显式提示）
- 参考系: 第一性原理（门禁必须认得所有合法声明格式）+ conventional commits 规范（scope 自由字符串）+ 今日三次误拦实证
