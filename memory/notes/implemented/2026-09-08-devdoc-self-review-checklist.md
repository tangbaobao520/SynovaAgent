---
状态: implemented
日期: 2026-09-08
决策: dev-doc 提交前必须走完整自审 checklist，禁止「抽查章节结构+锚点」冒充完整复核
理由: D600 只做部分复核就提交，被抓出 2 处真实错误——①reportDepth 优先级 §3.1「客户配置>scope」与 §4/§4.5「scope>客户配置」自相矛盾；②路由挂载写「server.ts/index.ts」，仓库无 routes/index.ts，真实挂载在 src/server.ts:41/354。
---

## 固化复核流程（每次 dev-doc 提交前逐条过，缺一即修）

### 0. 前置（写前已做，复核抽查）

- DSH 源码已读（file:line 真路径，非 dsh-* 包名）
- Synova 现状 grep 实证（每处 file:line 真实存在，不凭记忆）
- 无重复造轮子审计（§2.3 四表格齐全）

### 1. 结构（机器可查）

- 8 节齐全：HTML 头 + §1 权威 + §2 审计 + §3 写集 + §4 测试 + §4.5 决策 + §5 接线 + §6 DS + §7 自检 + §8 交付声明
- 无重复 `## N` 编号（「无重复造轮子」必须是 §2.3，不是 §3——已连犯两次，重点查）
- 写集标题计数 `(N 修改 + M 新建 + K 测试)` 与表格行数一一对应
- HTML 头含 `借鉴:`（S-14 强制）与 `依赖:`/`并行:`

### 2. 交叉一致性（本次新增，最容易漏）

- §3.1 写集 / §4 测试 / §4.5 决策点 / §8 交付声明 四处对同一件事的描述必须逐字一致（优先级顺序、命名空间键名、端点路径、file:line）
- DS1..DSn 与 §8 交付声明表行一一对应（S-10/G12d），过程项 DS 也给证据命令
- 接线路径引用真实文件（先 grep 确认：路由挂载在 src/server.ts，不是 index.ts）

### 3. 技术正确性

- 锚点/函数名/file:line 全部 grep 实证（不凭记忆/不凭转述）
- 生产调用点 ≥1（测试调用不计），DS 命令真实可跑（无发明 flag）
- 遵循最新专家权威（专家名禁 cycle；成本治理对象=推理层+主持层；配置分层≠专家/循环）

### 4. 红线

- 零 @deepseek-ai / as any / as never / as unknown as
- 不越界（src/sentinel 归 DSH、scripts/audit 归 K3）
- 自检清单以「不是凭记忆」「不用 --no-verify」收尾

### 参考系

- synova-dev-doc SKILL references/requirements.md 22 项 pre-dispatch checklist（本文是其落地补强，不替代）
- 第一性原理：复核是「逐条验证」，不是「抽查抽样」；抽查只适合写前粗查，不能替代提交前终审
