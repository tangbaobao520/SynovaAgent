---
状态: implemented
日期: 2026-09-12
决策: D-1「告警去重窗口」问题框架作废，改为**状态驱动通知模型**（创始人 2026-09-12 裁定，CTO 起草）：① 5 分钟窗口降级为抖动合并器（不承担产品语义）；② 产品规则＝问题生命周期六态（新发现立刻推并询问节奏格式 / 已告知待回应按契约升级 / 已认领停止重复转约定节奏 / 实质变化立刻再推 / 已解决复验关闭并复盘 / 明确不处理则记录理由定期回顾）；③ 节奏与格式由老板在首次告警时确认，技术载体＝监测契约（文件化：阈值/渠道/节奏/格式/升级规则/责任人）；④ 政策值：24h 未回应提醒、7 天无进展进周报首页、默认周报 1 次一页纸、"明确不处理"每周回顾。同时 D-2 裁定「桌面端先行」（覆盖原建议「资本循环」）。落地判定同步写入 product-lines.yaml：线 8 done_definition 改写 + 8-3 收窄为「键稳定」+ 新增 8-6「状态驱动 + 监测契约」（uncommitted，显式记录未实现）。
理由: 原问题把「告警」（事件，一次性）与「跟踪」（订阅，有节奏）混为一个时间窗机制，在真实经营场景里两头都错——窗口短则反复轰炸已知问题导致告警疲劳（监测功能失效），窗口长则误合并不同事件丢掉真实变化。创始人以首个客户（哇呢宝贝）的真实形态指正：企业经营问题是慢变量、需流程、需时间，不可能一两天解决，因此通知语义必须按**问题状态**而非**时间**驱动。物理证据：工单状态机早已存在（src/sentinel/runner.ts:178 TicketStatus；transitionFindingStatus L1021），但通知决策只查 last_sent_ms（L1321），不读状态——机制有、未接线＝M3 型缺口。跨线影响已按创始人要求同步（Win 侧交互/报告/记忆/进化）。
---

## 一、决策过程（可复核）

1. 仪表盘 `cockpit-override.yaml` D-1 原为 open，选项「5 分钟 / 30 分钟」，建议 5 分钟
2. 创始人质疑该框架：问题长期存在时，哨兵会持续告警到问题解决为止；而经营问题需要流程和时间
3. CTO 复核代码证实：通知决策（`src/sentinel/runner.ts:1321`）只查「上次推送时间」表，**完全忽略工单状态**；而状态机（L178 `open/acknowledged/resolved/dismissed`、L1021 `transitionFindingStatus`）已存在 → 缺口是接线，不是新机制
4. CTO 提出状态驱动模型 + 咨询师类比 + 4 个政策点建议值
5. 创始人「完全同意」，并特别确认「这其实是产品主叙事」这一判断，要求 CTO 总体把关；同时要求**涉及 Win 侧必须做好同步**

## 二、交付物

| 交付 | 位置 |
|---|---|
| 决策全文（产品模型 + 六态 + 咨询师机制映射 + 落地路径 + 风险） | `docs/synova/product-lines/DECISION-D1-状态驱动通知模型-20260912.md` |
| 跨线同步（Mac/Win 归属、接口契约、写集边界、时序） | `docs/synova/coordination/跨线同步-D1状态驱动通知模型-Mac-Win-20260912.md` |
| 待裁决清单裁决落库 | `docs/synova/product-lines/cockpit-override.yaml`（D-1/D-2 → resolved + 裁决正文） |
| 验收点改写 | `docs/synova/product-lines/product-lines.yaml`（线 8 done_definition；8-3 收窄；新增 8-6） |
| 进度重算 | `product-progress.json` / `product-progress.html`（refresh-all.sh） |

## 三、附带发现（两条，均已处置或登记）

1. **8-3 的「verified vs 备注不稳」矛盾系 note 陈旧**：K3 D580 已验证去重键稳定化（finding-id 去时间戳），而 yaml note 仍是旧的「N14 去重键不稳定」→ 已订正。教训：**note 必须随证据更新**，否则仪表盘读者无法判断谁对（等于制造假信号）。
2. **刷新后总体进度 7% → 1%（已验证仅 1/163 点）**：不是产品退步，而是 4 天高强度开发（D593→D710 批次）触发证据新鲜度门禁，各行 `modules` 内文件变更导致**整行证据失效转 stale**（stale 18→29，verified 11→1）。暴露机制粒度问题：**一行内任一文件变更 → 该行全部证据失效**，粒度太粗会让仪表盘长期显示"没进度"，反而制造信号失效（M1 类）。→ 已登记 backlog 待评估按点绑定失效范围。

## 四、边界

- 本决策**不改任何产品代码**；接线（步 1）与跨线落地（步 2-4）分别归 Mac DSH 编码线 / Win 线，需各自出 spec
- 契约文件与 loader 分属两线，写集边界已在同步文档显式声明（避免 verify-parallel 判重叠）
- 原「5 分钟窗口」技术参数保留，但语义降级——后续不得再以窗口大小作为产品语义讨论
