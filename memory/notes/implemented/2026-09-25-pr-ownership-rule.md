# 决策 Note — PR 归属规则改为「产出者自开」（R1–R4，D977）

**状态**：implemented（已落位）
**日期**：2026-09-26
**决策**：废止「PR 由 CTO 开」⇒ **R1 产出者自开** + **R2 模板六项** + **R3 CTO 只保留收件闸/K3 派单/门禁变更批准** + **R4 队列不经 CTO**；
并更正派单参数（**`--repo-root`**，无 `--repo`）。
**理由**：旧规则把可由门禁机械保证的「写集/红线/判据」变成 CTO 单点 ⇒ 交付滞留（D962 2a 实测：15:09 交付 → 数小时后才轮到 CTO）。
**承载方式**：**新建文件承载**（`docs/synova/coordination/PR归属与派单参数-20260926.md`），**不直接改 `派单模板.md`**
—— 后者归属 D931/D943，改它会被 `D328` 判「并行劫持」（CTO 本轮亲历 3 次）。
**未清**：① 派单模板一行指针待 FIX-011（共享文件机制）修复后补 ② R2 六项尚无机械校验。
