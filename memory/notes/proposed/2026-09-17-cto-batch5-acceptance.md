---
状态: proposed
日期: 2026-09-17
决策: 批次五交付（D788/D786/D782/D774）+ K3 批次五审计（D789）验收结论 = 全部通过；K3 两条 P0（派单门禁哈希真相源绑本地树 / 文件名 glob 盲区）CTO 独立复现后**认同**，裁决「先修后合」；B1 证据静默丢弃归 control-tower；新增红线级发现 `alloc-task-id.sh:242` 全角 $VAR 崩溃（D370 家族第二次，无免疫细胞）→ 升级创始人
理由: ① D788 修复解锁了被预算门禁误拦的 51 文件审计批次（审查面 0 / 治理 51 → PASS），十项中 9 项复现、1 项例证不实（机制成立）→ 通过但须订正例证；② D786/D782/D774 落点齐、红线（calc-progress 零改动）实测成立；③ K3 审计的两条 P0 我用「同一文档两树对比」与「真实文档名实弹」复现，判定不是"实现漏做"而是"规格把命名当语义"——带着缺陷合入会让 D778 门禁在第一天就产出假红/假绿，而 D778 编码指令自己写着「假红即噪音，噪音导致绕过」；④ 仪表盘 1% 在 09-17 归零的根因是 09-02 K3 证据跨 14 天 TTL，不是产品退化 → 说明"进度%"实际度量的是证据新鲜度，需在仪表盘文案里显性化
---

## 触发场景

创始人一次性交付五批成果（D788 门禁口径修正、D786 D774 收口、D782 文档真相防线、D774 证据保鲜流水线、K3 批次五审计），要求 CTO 逐项仔细验收、确保互不污染/互相干扰，并先报验收计划。

## 关键判定（含不可复现项）

- **D788**：39/39 测试；审查面 0+治理 51 PASS；13 件拦；真跨域拦；#572 双模式一致。**未见项**：K3 表 item 4 的例证（`src/x.ts`+`scripts/y.sh`）复现为单域 PASS（不实，机制成立）。
- **D786**：F6 机理三例（bot PR check-runs=0 / PAT PR=12）；看门狗三态 + 边界 + JSON 损坏；`calc-progress.py` 自 93a32779 起 0 提交。
- **D782**：8 套 doc-system 测试 88 断言 0 失败；反向摘线 7/2；探针三态；I2=4/I3=35 findings。附带观察：校验器跑一次即刷新 registry 锚点（脏树）。
- **D774**：证据落库齐全；连跑 4 次零新增文件、六态不变（仅 generated_at 变）；**幂等口径应写"零新增文件 + 结论一致"**，非字节级。环境受限：无 node_modules → GS 8 全 exit=1 且证据=no，不能判 GS 真伪。
- **K3 批五**：2×P0 复现并认同；B1-P1（GS 证据不入 calc 输入集，`:83` glob 非递归）独立证实；数字订正 1 处（派单-\* 20 → 40）。
- **互不污染**：五组文件集交集仅 4 个（bypass.log ×5、ci.yml、ownership.yaml、AUDIT-FINDINGS-LEDGER.md）；ci.yml 并集实证两边都在（doc-system 6 条 + 看门狗测试相邻共存）；`#572 ∩ #604` 第二个落地必冲突 → 须 union。

## 参考

- K3 批五报告 §A1.5 / §A1.3 / §B1（file:line 见验收报告 §5）
- 铁律 11（不静默降级）· 铁律 48（测试非空壳）· D515（docs-only CI 瘦身）· D778（派单复核门禁）· D790（度量口径 — 本轮新增两条失效面证据）

## 落点

- `docs/synova/coordination/验收-批次五交付与K3批五审计-20260917.md`（本批验收全文 + 可转发指令）
- 待办：① P0-1/P0-2 修复卡（先修后合，待创始人点头）；② K3 推分支开 PR 落批五报告；③ `alloc-task-id.sh:242` 全角修复 + `$VAR` 全角边界常驻扫描落地（红线级）；④ 队列合并顺序 `#589 → #604 → #584 → #572`（union 冲突）
