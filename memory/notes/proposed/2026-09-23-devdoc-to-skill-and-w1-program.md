# 决策 Note — dev-doc 预设退役技能化 + 双 DSH 提升程序（W1）

- 状态: proposed（创始人 2026-09-23 已批准方向；动作待 K3 审 + 落位确认后 git mv 到 implemented/）
- 责任方: synova-cto
- 触发: 创始人 2026-09-23 指令——① 提升方案三批合并为长任务（程序卡+分 Wave 子卡已选）② 评估 dev-doc 预设是否仍有必要（选定 B：降级为技能 + 触发式独立）③ W1 派发给 🎽 小队队长

## 决策 1：dev-doc 预设退役，能力技能化（B 案）

**实测依据**：近 7 天 dev doc 产物 **0 变更**；`synova-devdoc` 仍停在 08-16 版本；`synova-squad-lead`（09-21 新建）成员编制 = 队长 + ≤2 编码 + 1 自验，**无 dev-doc 位**；卡面 `spec` 字段已是「派单即规格」（DT 821 起）。

**动作**：
- 新增 skill `dev-doc-spec`（11 节活规格模板 + 独立性硬要求 + 触发条件），双写 `.claude/skills` / `.dsh/skills`
- `install-dsh-preset.sh` 注册表：`synova-devdoc` 标退役，默认安装集不含它
- **触发式独立**：仅跨层 / 新产品线 / 超预算任务强制独立撰写者，且**撰写者不得实现该规格**

**风险与代价（如实记）**：规格从"独立常驻"变"CTO 派单件 + 触发式"，制衡从三角（Codex规划/Claude执行/K3审计）退化为两方（CTO/K3）→ **K3 必经门禁（D1）成为该形态的承重墙**，W1 首项即它。

## 决策 2：提升方案 = 程序（长任务）+ 3 Wave（W1 即开）

**依据**：裁定 §二/§四 —— W2 的 B1 前置是 D911 切片 B（仍 `impl=None`）；W3 的 B3 需先钉计数配方、C1 需先观测、D2 等 Win 机器 → 三批前置满足度不同，**捆成单任务必然空等**。

**形状**：D922 程序卡 + 9 子卡 + Wave 门（K3 审 + CTO 收件闸）。
**W1 = D923(D1 K3 必经门禁) / D924(B4 子进程协议) / D925(C2 deps-interface)**；D1 前置已由 CTO 于 2026-09-23 解除（5 份 K3 报告入 main + verdict 回填）。
**Phase 0**：小队先出《总计划》（依赖图 + 每卡三路径夹具设计 + 失效条件 + 前置冻结清单），CTO 复核后放行执行。

## 附：本件修正的一条台账记录

`alloc-task-id.sh` 的 `BRIEF_FILE�: unbound variable`（line 242）**不止中文任务名触发**——本次四个**纯 ASCII** 任务名连续触发 4 次。原登记（2026-09-23 台账第 ④ 条）需按此修正。
