# Note 2026-09-08 — dev doc 交付两条铁纪律（创始人点名，D591 交付返工触发）

状态: proposed
触发: 2026-09-08 D591 dev doc 交付，创始人指出两条反复遗漏：「写完有没有复核？必须复核再交付」「为什么还是忘记给我一段能直接复制给编码 session 的指令？」——属"同类错误第二次出现"级别（此前任务已有同样提醒）。

## 纪律 1：交付前置复核（复核不 = 门禁脚本）

dev-doc-gatekeeper/check-dev-doc-write-set exit 0 ≠ 复核完成。交付前必须追加一次**人工级自检**（bash 批量断言，输出贴进交付汇报）：
- [ ] 11 节结构逐节 grep 存在
- [ ] 写集表标题下一行即表头（awk 验证，D381 字面要求）
- [ ] DS 编号连续、与 §7 测试表/§8 接线表对得上
- [ ] spec 内抽查的 file:line 声称重新 sed/grep 核验（行号漂移即修）
- [ ] front-matter north-star 块在
- 复核发现问题当场修 + 复跑门禁，然后才许汇报。

## 纪律 2：编码指令必须内嵌交付正文

三件套落盘（spec/task-state/编码指令文件）不满足交付——创始人要的是**能在对话里直接复制给编码 session 的指令全文**。dev-doc 交付汇报必须包含：指令文件全文原样粘贴在回复正文（或至少完整核心段：文档表+硬约束+复核清单+「开始吧。」），不能只给文件路径。

## 落地

- dev-doc session 的交付 checklist 固化为上述两条；每次交付汇报末尾附「复核输出」段 + 「编码指令（复制即用）」段。
- 若连续两次任务仍漏，按防线失效协议升级创始人。
