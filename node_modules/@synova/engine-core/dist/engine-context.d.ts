/**
 * EngineContext — 外部依赖注入接口
 *
 * 所有 engine-core 模块通过 getEngineContext() 获取外部服务，
 * 默认使用 stub 实现（console logger + no-op 安全/市场/数据库/协议）。
 * Novis 启动时调用 setEngineContext() 注入真实实现。
 */
import type { AppLogger } from './infra/logger';
export interface SkillSecurityReport {
    score: number;
    level: string;
    findings: Array<{
        severity: string;
        description: string;
    }>;
    installable: boolean;
}
export interface SecurityAuditReport {
    score: number;
    level: string;
    summary: string;
    checkedAt: string;
    items: any[];
}
export interface MarketplaceService {
    search(query: string, type: string, signal?: AbortSignal): Promise<{
        skills?: any[];
    } | null>;
    publishSkill(req: any, signal?: AbortSignal): Promise<{
        success: boolean;
    } | null>;
}
export declare class CircuitOpenError extends Error {
    readonly endpoint: string;
    readonly opensAt: string;
    constructor(endpoint: string, opensAt: string);
}
export interface CircuitBreakerLike {
    call<T>(fn: () => Promise<T>): Promise<T>;
    getStatus(): {
        state: string;
        failures: number;
        lastFailure?: string;
        opensAt?: string;
    };
}
export interface EngineContext {
    logger: AppLogger;
    securityAudit: {
        auditSkillContent(name: string, content: string): SkillSecurityReport;
        auditExtension(type: string, content: string, name: string): SecurityAuditReport;
    };
    marketplace: MarketplaceService;
    database: {
        getDb(): any;
    };
    protocol: {
        createRuleEngine(): any;
    };
    circuitBreaker: {
        createBreaker(endpoint: string, config?: {
            threshold?: number;
            cooldownMs?: number;
            onTrip?: (endpoint: string, failures: number, lastFailure: string) => void;
        }): CircuitBreakerLike;
    };
    alerting: {
        notifyCircuitBreakerTrip(endpoint: string, failures: number, lastFailure: string): Promise<void>;
    };
    filePaths: {
        skillBlocklistPath: string;
        skillRegistryPath: string;
    };
    personal?: {
        publish: (blueprint: import('./types').BlueprintDTO, options?: import('./pipeline/personal-publisher').PublishOptions) => import('./pipeline/personal-publisher').PublishResult;
        listAgents: (solohubDir?: string) => import('./pipeline/personal-publisher').ManifestEntry[];
        getAgent: (blueprintId: string, solohubDir?: string) => {
            synovaYml: string;
            blueprint: import('./types').BlueprintDTO;
        } | null;
    };
}
export declare function setEngineContext(ctx: Partial<EngineContext>): void;
export declare function getEngineContext(): EngineContext;
