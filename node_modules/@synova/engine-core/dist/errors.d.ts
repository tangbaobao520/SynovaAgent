/**
 * engine-server/errors.ts — 统一 ApiErrorResponse 结构
 *
 * 9 种错误码，严格对齐 CONTRACT-01 错误码总表。
 * 所有错误通过工厂函数创建，确保结构统一。
 */
import type { ApiErrorResponse, ApiErrorDetail, ErrorCode } from './types';
/**
 * 创建统一错误响应
 */
export declare function createApiError(code: ErrorCode, requestId: string, details?: ApiErrorDetail[], messageOverride?: string): ApiErrorResponse;
/**
 * 便捷工厂函数
 */
export declare const errors: {
    invalidRequestId: (requestId: string) => ApiErrorResponse;
    invalidSchemaVersion: (requestId: string) => ApiErrorResponse;
    invalidTaskDef: (requestId: string, details?: ApiErrorDetail[]) => ApiErrorResponse;
    insufficientSanitization: (requestId: string, details: ApiErrorDetail[]) => ApiErrorResponse;
    unauthorized: (requestId: string) => ApiErrorResponse;
    forbidden: (requestId: string) => ApiErrorResponse;
    taskRequestNotFound: (requestId: string) => ApiErrorResponse;
    rateLimit: (requestId: string) => ApiErrorResponse;
    engineError: (requestId: string, messageOverride?: string) => ApiErrorResponse;
    engineUnavailable: (requestId: string) => ApiErrorResponse;
};
/**
 * 获取错误对应的 HTTP 状态码
 */
export declare function getHttpStatus(code: ErrorCode): number;
