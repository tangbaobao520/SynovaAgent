/**
 * knowledge-ingest/parsers/index.ts — 文档解析器注册表
 */
import { parsePdf } from './pdf-parser';
import { parseDocx } from './docx-parser';
import { parseExcel } from './excel-parser';
export interface TextChunk {
    id: string;
    text: string;
    sourceFileName: string;
    sourceFormat: string;
    pageNum?: number;
    rowNum?: number;
    chunkIndex: number;
    totalChunks: number;
}
export interface ParseResult {
    success: boolean;
    chunks: TextChunk[];
    error?: string;
    metadata: {
        fileName: string;
        format: string;
        totalChars: number;
        chunkCount: number;
        parsedAt: string;
    };
}
export declare function parseDocument(buffer: Buffer, fileName: string, mimeType?: string): Promise<ParseResult>;
declare function parseText(buffer: Buffer, fileName: string): Promise<ParseResult>;
export { parsePdf, parseDocx, parseExcel, parseText };
