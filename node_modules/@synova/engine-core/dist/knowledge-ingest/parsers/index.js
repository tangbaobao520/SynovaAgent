"use strict";
/**
 * knowledge-ingest/parsers/index.ts — 文档解析器注册表
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseExcel = exports.parseDocx = exports.parsePdf = void 0;
exports.parseDocument = parseDocument;
exports.parseText = parseText;
const pdf_parser_1 = require("./pdf-parser");
Object.defineProperty(exports, "parsePdf", { enumerable: true, get: function () { return pdf_parser_1.parsePdf; } });
const docx_parser_1 = require("./docx-parser");
Object.defineProperty(exports, "parseDocx", { enumerable: true, get: function () { return docx_parser_1.parseDocx; } });
const excel_parser_1 = require("./excel-parser");
Object.defineProperty(exports, "parseExcel", { enumerable: true, get: function () { return excel_parser_1.parseExcel; } });
const parsers = {
    pdf: pdf_parser_1.parsePdf,
    docx: docx_parser_1.parseDocx,
    xlsx: excel_parser_1.parseExcel,
};
const EXT_MAP = {
    '.pdf': 'pdf',
    '.docx': 'docx',
    '.xlsx': 'xlsx',
    '.txt': 'text',
    '.csv': 'text',
    '.md': 'text',
};
async function parseDocument(buffer, fileName, mimeType) {
    const ext = fileName.toLowerCase().includes('.')
        ? '.' + fileName.split('.').pop().toLowerCase()
        : '';
    const format = EXT_MAP[ext] || 'text';
    if (format === 'text') {
        return parseText(buffer, fileName);
    }
    const parser = parsers[format];
    if (!parser) {
        return {
            success: false,
            chunks: [],
            error: `不支持的文件格式: ${format}`,
            metadata: { fileName, format, totalChars: 0, chunkCount: 0, parsedAt: new Date().toISOString() },
        };
    }
    return parser(buffer, fileName);
}
async function parseText(buffer, fileName) {
    const text = buffer.toString('utf-8');
    const chunks = splitIntoChunks(text, fileName, 'text');
    return {
        success: true,
        chunks,
        metadata: {
            fileName,
            format: 'text',
            totalChars: text.length,
            chunkCount: chunks.length,
            parsedAt: new Date().toISOString(),
        },
    };
}
function splitIntoChunks(text, fileName, format) {
    const MAX_CHUNK_CHARS = 3000;
    const chunks = [];
    const paragraphs = text.split(/\n\n+/).filter(p => p.trim().length > 0);
    let currentChunk = '';
    let chunkIndex = 0;
    for (const para of paragraphs) {
        if (currentChunk.length + para.length > MAX_CHUNK_CHARS && currentChunk.length > 0) {
            chunks.push({
                id: `${fileName}_${chunkIndex}`,
                text: currentChunk.trim(),
                sourceFileName: fileName,
                sourceFormat: format,
                chunkIndex,
                totalChunks: 0, // updated below
            });
            chunkIndex++;
            currentChunk = para;
        }
        else {
            currentChunk += (currentChunk ? '\n\n' : '') + para;
        }
    }
    if (currentChunk.trim().length > 0) {
        chunks.push({
            id: `${fileName}_${chunkIndex}`,
            text: currentChunk.trim(),
            sourceFileName: fileName,
            sourceFormat: format,
            chunkIndex,
            totalChunks: 0,
        });
        chunkIndex++;
    }
    if (chunks.length === 0) {
        chunks.push({
            id: `${fileName}_0`,
            text: text.slice(0, MAX_CHUNK_CHARS),
            sourceFileName: fileName,
            sourceFormat: format,
            chunkIndex: 0,
            totalChunks: 1,
        });
    }
    for (const c of chunks) {
        c.totalChunks = chunks.length;
    }
    return chunks;
}
