/**
 * harness/config-guardian.ts — 用户修改配置的风险分析引擎
 *
 * 职责：在用户修改协作模式或 Agent 配置之前，分析变更影响和风险等级。
 *
 * 核心原则：
 *   - 硬编码影响矩阵，不调 LLM（确定性、可测试、零延迟）
 *   - 只读分析，不执行任何写操作
 *   - 返回结构化风险报告，供前端 ChangeConsequenceModal 展示
 *
 * @packageDocumentation
 */
import type { CollaborationModeBlue, CollaborationMode } from '../types';
export type GapDimension = 'division_of_labor' | 'information_flow' | 'authority_governance' | 'trust_incentive' | 'knowledge_sharing' | 'external_interface';
export declare const GAP_LABELS: Record<GapDimension, string>;
export interface GapDiffResult {
    gap: GapDimension;
    label: string;
    fromValue: string;
    toValue: string;
    fromDetail: string;
    toDetail: string;
    impact: 'info' | 'warning' | 'critical';
    summary: string;
}
export interface ModeSwitchRiskAssessment {
    overall: 'info' | 'warning' | 'critical';
    teamSizeIssues: string[];
    gapChanges: GapDiffResult[];
    decisionSpeedImpact: string;
    safetyBaselineChanges: string[];
}
export interface EditRisk {
    file: string;
    section: string;
    severity: 'info' | 'warning' | 'critical';
    message: string;
    suggestion: string;
}
export interface IronRuleCheck {
    intact: boolean;
    missingRules: string[];
    severity: 'critical' | 'warning' | 'info';
}
export declare const MODE_SIZE_THRESHOLDS: Record<string, {
    minTotal: number;
    minL2: number;
    maxTotal?: number;
}>;
export declare function diffCollaborationMode(from: CollaborationModeBlue, to: CollaborationModeBlue): GapDiffResult[];
export declare function assessModeSwitchRisk(fromMode: CollaborationMode, toMode: CollaborationMode, teamSize: number, l2Count: number): {
    teamSizeIssues: string[];
    overall: ModeSwitchRiskAssessment['overall'];
};
export declare function validateAgentEdit(_agentId: string, fileName: string, oldContent: string, newContent: string): EditRisk[];
export declare function checkIronRuleIntegrity(content: string): IronRuleCheck;
