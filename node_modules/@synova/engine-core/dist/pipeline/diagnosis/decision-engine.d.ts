/**
 * decision-engine.ts — 持续决策引擎 (Phase C2)
 *
 * 事件→行动项自动创建。本体图事件绑定工作流。
 * 对标 Palantir: 事件触发→规则匹配→行动生成→状态追踪。
 */
import type { GraphStore } from './graph-store';
import { type GraphAlert } from './graph-monitor';
export interface DecisionRule {
    id: string;
    name: string;
    /** 触发条件: alert type */
    triggerOn: GraphAlert['type'];
    /** 匹配的边类型 */
    edgeType?: string;
    /** 最小严重度 */
    minSeverity: GraphAlert['severity'];
    /** 生成的行动项 */
    action: {
        title: string;
        description: string;
        priority: 'critical' | 'high' | 'medium' | 'low';
        assigneeRole: string;
        estimatedHours: number;
    };
    /** 冷却时间 (ms), 同规则不重复触发 */
    cooldownMs: number;
}
export interface DecisionAction {
    id: string;
    ruleId: string;
    alertId: string;
    title: string;
    description: string;
    priority: 'critical' | 'high' | 'medium' | 'low';
    assigneeRole: string;
    estimatedHours: number;
    status: 'open' | 'in_progress' | 'resolved' | 'dismissed';
    createdAt: string;
    resolvedAt?: string;
}
export declare function runDecisionEngine(store: GraphStore, graph: string): DecisionAction[];
export declare function getDecisionActions(status?: DecisionAction['status']): DecisionAction[];
export declare function resolveAction(actionId: string): boolean;
export declare function dismissAction(actionId: string): boolean;
export declare function clearDecisionEngine(): void;
