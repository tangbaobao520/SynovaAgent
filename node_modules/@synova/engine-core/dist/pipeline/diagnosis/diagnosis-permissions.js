"use strict";
/**
 * diagnosis-permissions.ts — 诊断代理 7 层权限决策树
 *
 * 对标 Claw-Code permissions.rs:
 *   - PermissionPolicy 结构体 + builder 模式
 *   - 7 层决策树（EVERYONE → NEVER）
 *   - 规则覆盖机制（deny > allow）
 *   - RecordingPermissionStore 用于测试断言
 *
 * 层级语义：
 *   EVERYONE(0)            — 公开诊断摘要、行业基准
 *   ORG_MEMBER(1)          — 同组织成员可见脱敏诊断
 *   DIAGNOSIS_PARTICIPANT(2) — 诊断参与者可见本阶段证据
 *   INITIATOR_ONLY(3)      — 仅发起人可见完整报告
 *   FDE_OVERRIDE(4)        — FDE 可覆盖 PARTICIPANT 限制
 *   ADMIN_ONLY(5)          — 仅系统管理员可见
 *   NEVER(6)               — 永不暴露（密钥、个人身份信息）
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.RecordingPermissionStore = exports.PermissionPolicy = void 0;
exports.createDefaultPermissionPolicy = createDefaultPermissionPolicy;
const types_1 = require("./types");
// ====================================================================
// PermissionPolicy
// ====================================================================
/** 权限策略——7 层决策树引擎 */
class PermissionPolicy {
    rules = [];
    /** 添加规则（builder 模式，对标 Claw-Code #[must_use] with_*） */
    withRule(rule) {
        this.rules.push(rule);
        return this;
    }
    /** 批量添加规则 */
    withRules(rules) {
        for (const r of rules)
            this.rules.push(r);
        return this;
    }
    /** 移除指定名称的规则 */
    withoutRule(name) {
        this.rules = this.rules.filter(r => r.name !== name);
        return this;
    }
    /**
     * 执行权限检查。
     *
     * 决策顺序（对标 Claw-Code 7 层决策树）：
     *   1. 资源级规则优先匹配（deny > allow）
     *   2. 无匹配规则时，按请求者最高权限等级 fallback
     *   3. FDE 可覆盖 PARTICIPANT 级限制
     */
    check(request) {
        // Step 1: 资源级规则匹配（优先级降序）
        const matched = this.rules
            .filter(r => this.matchResource(r.resourcePattern, request.resource))
            .sort((a, b) => b.priority - a.priority);
        if (matched.length > 0) {
            // deny 优先——一条 deny 即可拒绝
            const denyRule = matched.find(r => r.action === 'deny');
            if (denyRule) {
                const requesterLevel = this.effectiveLevel(request.context);
                if (requesterLevel < denyRule.minLevel) {
                    return {
                        allowed: false,
                        reason: `${request.resource}: 规则 "${denyRule.name}" 拒绝访问（需 ≥${types_1.DiagnosisPermissionLevel[denyRule.minLevel]}）`,
                        suggestedAction: '联系诊断发起人提升权限',
                    };
                }
            }
            // 所有匹配的 deny 都被 override → allow
            const allowRule = matched.find(r => r.action === 'allow');
            if (allowRule) {
                const requesterLevel = this.effectiveLevel(request.context);
                if (requesterLevel >= allowRule.minLevel) {
                    return { allowed: true };
                }
            }
        }
        // Step 2: 默认 fallback——按资源类型映射最低权限
        const defaultMinLevel = this.defaultLevelForResource(request.resource, request.action);
        const requesterLevel = this.effectiveLevel(request.context);
        if (requesterLevel >= defaultMinLevel) {
            return { allowed: true };
        }
        return {
            allowed: false,
            reason: `${request.resource}: 默认权限不足（需 ≥${types_1.DiagnosisPermissionLevel[defaultMinLevel]}，当前 ${types_1.DiagnosisPermissionLevel[requesterLevel]}）`,
            suggestedAction: '加入诊断参与者列表或请求发起人授权',
        };
    }
    /** 检查单条证据是否可读 */
    canReadEvidence(evidence, context) {
        // NEVER: 标记为私有的证据 + 请求者非发起人 → 拒绝
        if (evidence.isPrivate && !context.isInitiator && !context.isFDE) {
            return {
                allowed: false,
                reason: `证据 ${evidence.id}: 标记为私有（${evidence.privateReason ?? '未说明原因'}）`,
                suggestedAction: '联系诊断发起人请求脱敏版本',
            };
        }
        return this.check({
            resource: `evidence:${evidence.dimension}`,
            context,
            action: 'read',
        });
    }
    /** 批量过滤可读证据 */
    filterReadableEvidence(evidenceList, context) {
        const readable = [];
        const denied = [];
        for (const ev of evidenceList) {
            const result = this.canReadEvidence(ev, context);
            if (result.allowed) {
                readable.push(ev);
            }
            else {
                denied.push(ev.id);
            }
        }
        return { readable, denied };
    }
    // ── 内部方法 ──
    /** 计算请求者有效权限等级（FDE 可获 +1 提升） */
    effectiveLevel(context) {
        const base = this.baseLevel(context);
        // FDE 可覆盖 PARTICIPANT 级限制（对标 FDE_OVERRIDE）
        if (context.isFDE && base < types_1.DiagnosisPermissionLevel.FDE_OVERRIDE) {
            return types_1.DiagnosisPermissionLevel.FDE_OVERRIDE;
        }
        return base;
    }
    /** 根据角色计算基础权限等级 */
    baseLevel(context) {
        if (context.isInitiator)
            return types_1.DiagnosisPermissionLevel.INITIATOR_ONLY;
        // 同团队 → ORG_MEMBER，跨团队 → EVERYONE
        if (context.requesterTeamId === context.targetTeamId) {
            return types_1.DiagnosisPermissionLevel.ORG_MEMBER;
        }
        return types_1.DiagnosisPermissionLevel.EVERYONE;
    }
    /** 简单 glob 匹配（支持 * 通配符和精确匹配） */
    matchResource(pattern, resource) {
        if (pattern === '*' || pattern === resource)
            return true;
        if (pattern.endsWith('*')) {
            return resource.startsWith(pattern.slice(0, -1));
        }
        if (pattern.startsWith('*')) {
            return resource.endsWith(pattern.slice(1));
        }
        return false;
    }
    /** 资源 → 默认最低权限等级 */
    defaultLevelForResource(resource, action) {
        // 写入/删除操作 → 至少 INITIATOR_ONLY
        if (action === 'write' || action === 'delete') {
            return types_1.DiagnosisPermissionLevel.INITIATOR_ONLY;
        }
        // 完整报告 → INITIATOR_ONLY
        if (resource.startsWith('report:') || resource === 'full-report') {
            return types_1.DiagnosisPermissionLevel.INITIATOR_ONLY;
        }
        // 诊断摘要 → ORG_MEMBER
        if (resource.startsWith('summary:') || resource === 'diagnosis-summary') {
            return types_1.DiagnosisPermissionLevel.ORG_MEMBER;
        }
        // 行业基准 → EVERYONE
        if (resource.startsWith('benchmark:') || resource === 'industry-benchmark') {
            return types_1.DiagnosisPermissionLevel.EVERYONE;
        }
        // 证据 → PARTICIPANT
        if (resource.startsWith('evidence:')) {
            return types_1.DiagnosisPermissionLevel.DIAGNOSIS_PARTICIPANT;
        }
        // 默认 → ORG_MEMBER
        return types_1.DiagnosisPermissionLevel.ORG_MEMBER;
    }
}
exports.PermissionPolicy = PermissionPolicy;
// ====================================================================
// 内置规则集
// ====================================================================
/** 创建生产环境默认权限策略 */
function createDefaultPermissionPolicy() {
    return new PermissionPolicy()
        .withRule({
        name: 'public-benchmark',
        resourcePattern: 'benchmark:*',
        action: 'allow',
        minLevel: types_1.DiagnosisPermissionLevel.EVERYONE,
        priority: 10,
    })
        .withRule({
        name: 'org-summary-access',
        resourcePattern: 'summary:*',
        action: 'allow',
        minLevel: types_1.DiagnosisPermissionLevel.ORG_MEMBER,
        priority: 20,
    })
        .withRule({
        name: 'participant-evidence-access',
        resourcePattern: 'evidence:*',
        action: 'allow',
        minLevel: types_1.DiagnosisPermissionLevel.DIAGNOSIS_PARTICIPANT,
        priority: 30,
    })
        .withRule({
        name: 'initiator-full-report',
        resourcePattern: 'report:*',
        action: 'allow',
        minLevel: types_1.DiagnosisPermissionLevel.INITIATOR_ONLY,
        priority: 40,
    })
        .withRule({
        name: 'admin-all',
        resourcePattern: '*',
        action: 'allow',
        minLevel: types_1.DiagnosisPermissionLevel.ADMIN_ONLY,
        priority: 100,
    });
}
// ====================================================================
// 测试 Spy（对标 Claw-Code RecordingPrompter）
// ====================================================================
/** 记录型权限存储——测试中用于断言权限检查调用 */
class RecordingPermissionStore {
    seen = [];
    check(req) {
        this.seen.push({ ...req });
        return { allowed: true };
    }
    /** 最近一次请求 */
    lastRequest() {
        return this.seen[this.seen.length - 1];
    }
    /** 按资源名过滤 */
    requestsFor(resource) {
        return this.seen.filter(r => r.resource === resource);
    }
    reset() {
        this.seen = [];
    }
}
exports.RecordingPermissionStore = RecordingPermissionStore;
//# sourceMappingURL=diagnosis-permissions.js.map