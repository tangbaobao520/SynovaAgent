/**
 * entity-registry.ts — L1 确定性实体解析 (ARCH-20 Phase A3)
 * 8条零假阳性规则, 直接写入本体图.
 */
import type { GraphStore } from './graph-store';
export declare function resolvePersonByEmail(graph: GraphStore, email: string, orgGraph: string): string | null;
export declare function resolveToolByUrl(graph: GraphStore, url: string, orgGraph: string): string | null;
export declare function resolveClientByNameAndIndustry(graph: GraphStore, name: string, industry: string, orgGraph: string): {
    id: string;
    confidence: number;
} | null;
export declare function resolveTeamByName(graph: GraphStore, name: string, orgGraph: string): string | null;
export declare function resolvePersonByFeishuId(graph: GraphStore, feishuId: string, orgGraph: string): string | null;
export declare function resolvePersonByGithubUser(graph: GraphStore, username: string, orgGraph: string): string | null;
export declare function resolveProcessByJiraKey(graph: GraphStore, jiraKey: string, orgGraph: string): string | null;
export declare function resolveOrgByRegistration(graph: GraphStore, name: string, regNumber: string, orgGraph: string): {
    id: string;
    confidence: number;
} | null;
/** 合并两个节点: 将 source 的所有边重定向到 target, 然后删除 source */
export declare function mergePersonNodes(graph: GraphStore, sourceId: string, targetId: string, orgGraph: string): void;
