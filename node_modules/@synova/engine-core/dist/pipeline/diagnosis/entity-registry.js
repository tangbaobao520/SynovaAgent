"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePersonByEmail = resolvePersonByEmail;
exports.resolveToolByUrl = resolveToolByUrl;
exports.resolveClientByNameAndIndustry = resolveClientByNameAndIndustry;
exports.resolveTeamByName = resolveTeamByName;
exports.resolvePersonByFeishuId = resolvePersonByFeishuId;
exports.resolvePersonByGithubUser = resolvePersonByGithubUser;
exports.resolveProcessByJiraKey = resolveProcessByJiraKey;
exports.resolveOrgByRegistration = resolveOrgByRegistration;
exports.mergePersonNodes = mergePersonNodes;
const sog_core_schema_1 = require("../../sog/sog-core-schema");
function resolvePersonByEmail(graph, email, orgGraph) {
    if (!email)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, { email: email.toLowerCase() }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolveToolByUrl(graph, url, orgGraph) {
    if (!url)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.TOOL, { url }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolveClientByNameAndIndustry(graph, name, industry, orgGraph) {
    if (!name || !industry)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.CLIENT, { name, industry }, orgGraph);
    if (existing.length > 0)
        return { id: existing[0].id, confidence: 1.0 };
    // 3b: name only, confidence 0.95
    const nameOnly = graph.queryNodes(sog_core_schema_1.SOGNodeType.CLIENT, { name }, orgGraph);
    if (nameOnly.length === 1)
        return { id: nameOnly[0].id, confidence: 0.95 };
    return null;
}
function resolveTeamByName(graph, name, orgGraph) {
    if (!name)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.TEAM, { name }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolvePersonByFeishuId(graph, feishuId, orgGraph) {
    if (!feishuId)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, { feishuId }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolvePersonByGithubUser(graph, username, orgGraph) {
    if (!username)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, { username }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolveProcessByJiraKey(graph, jiraKey, orgGraph) {
    if (!jiraKey)
        return null;
    const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.PROCESS, { jiraKey }, orgGraph);
    return existing.length > 0 ? existing[0].id : null;
}
function resolveOrgByRegistration(graph, name, regNumber, orgGraph) {
    if (!name)
        return null;
    // 8a: has registration number
    if (regNumber) {
        const existing = graph.queryNodes(sog_core_schema_1.SOGNodeType.TEAM, { name, registrationNumber: regNumber }, orgGraph);
        if (existing.length > 0)
            return { id: existing[0].id, confidence: 1.0 };
    }
    // 8b: name + domain + industry
    const byName = graph.queryNodes(sog_core_schema_1.SOGNodeType.TEAM, { name }, orgGraph);
    if (byName.length === 1)
        return { id: byName[0].id, confidence: 0.95 };
    return null;
}
/** 合并两个节点: 将 source 的所有边重定向到 target, 然后删除 source */
function mergePersonNodes(graph, sourceId, targetId, orgGraph) {
    const edges = graph.queryEdges(undefined, sourceId, undefined, orgGraph);
    for (const e of edges) {
        graph.createEdge(e.type, targetId, e.to, e.weight, e.props, orgGraph);
    }
    const incoming = graph.queryEdges(undefined, undefined, sourceId, orgGraph);
    for (const e of incoming) {
        graph.createEdge(e.type, e.from, targetId, e.weight, e.props, orgGraph);
    }
    graph.deleteNode(sourceId, orgGraph);
}
//# sourceMappingURL=entity-registry.js.map