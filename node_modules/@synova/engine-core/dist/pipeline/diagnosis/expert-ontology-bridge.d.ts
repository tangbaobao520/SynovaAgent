import type { ExpertReport, ExpertType, NodeType, EdgeType } from './types';
import type { GraphStore } from './graph-store';
export interface OntologyPatch {
    createNodes?: Array<{
        type: NodeType;
        props: Record<string, unknown>;
        confidence: number;
    }>;
    createEdges?: Array<{
        type: EdgeType;
        from: string;
        to: string;
        weight?: number;
        props?: Record<string, unknown>;
        confidence: number;
    }>;
    mergeNodes?: Array<{
        nodeA: string;
        nodeB: string;
        reason: string;
    }>;
    updateProps?: Array<{
        nodeId: string;
        props: Record<string, unknown>;
    }>;
}
export interface AppliedPatchResult {
    expertType: ExpertType;
    nodesCreated: number;
    edgesCreated: number;
    mergesApplied: number;
    propsUpdated: number;
    conflicts: string[];
}
export declare function applyOntologyPatches(reports: ExpertReport[], graphStore: GraphStore, orgGraph: string): AppliedPatchResult[];
/** Extract ontology patches from expert reports (for ExpertReport extension) */
export declare function collectExpertPatches(reports: ExpertReport[]): OntologyPatch[];
