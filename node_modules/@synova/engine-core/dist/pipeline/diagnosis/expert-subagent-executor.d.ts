/**
 * expert-subagent-executor.ts — 专家子 Agent 执行器
 *
 * Step 3: 对标 Claw-Code build_agent_runtime+run_agent_job + Hermes _build_child_agent+_run_single_child。
 * 6 专家并行 Promise.all, 独立 LLM 会话, 工具白名单, 超时隔离, 结构化输出解析。
 */
import type { DiagnosisLLMClient } from './diagnosis-orchestrator';
import type { ToolExecutor } from './diagnosis-orchestrator';
import type { DiagnosisEvidence, ExpertReport, ExpertType, SessionBrief, DataAccessPolicy } from './types';
export interface ExpertSubAgentContext {
    expertType: ExpertType;
    diagnosisId: string;
    orgName: string;
    teamId: string;
    sessionBrief: SessionBrief;
    allowedEvidence: DiagnosisEvidence[];
    allowedTools: string[];
    dataPolicy: DataAccessPolicy;
    systemPrompt: string;
    expertKnowledge: string;
    outputSchema: string;
    timeoutMs: number;
    maxRetries: number;
}
export declare const EXPERT_REPORT_SCHEMA = "{\n  \"findings\": [{ \"id\": \"f1\", \"dimension\": \"...\", \"statement\": \"\u2264200\u5B57\", \"confidence\": 0.0-1.0, \"evidenceRefs\": [\"ev-xxx\"], \"severity\": \"critical|high|medium|low\", \"suggestedActions\": [\"...\"] }],\n  \"overallAssessment\": \"\u2264300\u5B57\",\n  \"uncertainties\": [{ \"description\": \"...\", \"reason\": \"\u6570\u636E\u4E0D\u8DB3|\u8D85\u51FA\u9886\u57DF|\u9700\u8981\u4EBA\u5DE5\u5224\u65AD\", \"suggestedNextStep\": \"...\" }],\n  \"conflictingSignals\": [{ \"dimension\": \"...\", \"myFinding\": \"...\", \"myConfidence\": 0.5, \"potentialOpposingExpert\": \"org_diagnostician\", \"reason\": \"...\" }],\n  \"crossReferences\": [{ \"dimension\": \"...\", \"expertType\": \"financial_analyst\", \"reason\": \"...\", \"priority\": \"advisory|important|critical\" }]\n}";
export declare class ExpertSubAgentExecutor {
    private llmClient;
    private toolExecutor;
    constructor(llmClient: DiagnosisLLMClient, toolExecutor: ToolExecutor);
    /** 执行单个专家 */
    executeExpert(context: ExpertSubAgentContext): Promise<ExpertReport>;
    /** 并行执行所有专家 */
    executeAll(contexts: ExpertSubAgentContext[]): Promise<ExpertReport[]>;
    private runWithRetry;
    private runExpert;
    private extractOntologyPatches;
    private buildUserMessage;
    private parseStructuredOutput;
}
