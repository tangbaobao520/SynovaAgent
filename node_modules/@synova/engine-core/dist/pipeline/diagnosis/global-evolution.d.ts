/**
 * global-evolution.ts — 全局进化 Layer 3 (Phase C3)
 *
 * 跨组织本体模式学习。仅基于已确认的实体关联训练。
 * 候选关联不进Layer 3——这是硬边界。
 */
import type { GraphStore } from './graph-store';
export interface CrossOrgPattern {
    id: string;
    patternType: 'edge_weight_benchmark' | 'node_type_frequency' | 'community_structure';
    description: string;
    confidence: number;
    /** 参与组织数 (匿名化) */
    orgCount: number;
    /** 模式适用的行业 */
    industries: string[];
    /** 生成的基准值 */
    benchmarks: Record<string, number>;
    /** 生成的诊断规则建议 */
    suggestedRule?: {
        name: string;
        condition: string;
        severity: 'high' | 'medium' | 'low';
        recommendation: string;
    };
    discoveredAt: string;
}
/** 从多个组织的匿名化本体图中发现跨组织模式 */
export declare function discoverCrossOrgPatterns(stores: Array<{
    store: GraphStore;
    graph: string;
    industry: string;
}>): CrossOrgPattern[];
export declare function getPatterns(industry?: string): CrossOrgPattern[];
export declare function clearEvolution(): void;
