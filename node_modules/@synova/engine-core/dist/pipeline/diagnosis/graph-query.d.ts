/**
 * graph-query.ts — 图查询引擎 (Phase B1)
 *
 * Spec: shortestPath/centrality/communities/anomalies/contradictory/dimensional
 * Performance: <200ms for graphs ≤10K nodes
 */
import type { GraphStore } from './graph-store';
import type { EdgeType, OntologyPath, SubGraph } from './types';
export declare function shortestPath(store: GraphStore, from: string, to: string, edgeType?: EdgeType, graph: string): OntologyPath | null;
export declare function degreeCentrality(store: GraphStore, nodeId: string, graph: string): number;
export declare function detectCommunities(store: GraphStore, minSize: number | undefined, graph: string): Array<{
    id: string;
    members: string[];
    modularity: number;
}>;
export declare function findAnomalySubgraphs(store: GraphStore, threshold: number | undefined, graph: string): SubGraph[];
export declare function detectEdgeWeightDrop(store: GraphStore, edgeType: EdgeType, dropThreshold: number | undefined, graph: string): Array<{
    from: string;
    to: string;
    previousWeight: number;
    currentWeight: number;
    dropPercent: number;
}>;
export declare function findContradictoryPaths(store: GraphStore, dimension: string, threshold: number | undefined, graph: string): Array<{
    pathA: OntologyPath;
    pathB: OntologyPath;
    dimension: string;
}>;
export declare function findCrossDimensionalLinks(store: GraphStore, dimensions: string[], graph: string): Array<{
    dimension: string;
    relatedDimension: string;
    sharedNodes: string[];
    strength: number;
}>;
