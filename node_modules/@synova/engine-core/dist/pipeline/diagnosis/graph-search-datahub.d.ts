/**
 * graph-search-datahub.ts — Graph-then-Search (Phase B, 决策 4)
 *
 * 对标 DataHub LineageSearchService:
 *   1. 图遍历 (LineageRegistry edges) → 候选实体
 *   2. 文本过滤 (可选) → 排序
 *   3. Lightning (纯图) vs Tortoise (图+文本) 双路径
 */
import type { GraphStore } from './graph-store';
import type { LineageRegistry } from './entity-registry-datahub';
export type LineageDirection = 'UPSTREAM' | 'DOWNSTREAM';
export interface GraphSearchResult {
    entityId: string;
    entityType: string;
    entityName: string;
    degree: number;
    paths: string[][];
    textMatchScore?: number;
}
/** 图遍历 + 可选文本搜索 (对标 DataHub searchAcrossLineage) */
export declare function searchAcrossLineage(store: GraphStore, lineage: LineageRegistry, startEntityId: string, direction: LineageDirection, maxHops: number, graph: string, textQuery?: string): GraphSearchResult[];
