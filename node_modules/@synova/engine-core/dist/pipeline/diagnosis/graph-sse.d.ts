/**
 * graph-sse.ts — SSE 协议扩展 (Phase B4)
 *
 * ARCH-20: 前端图可视化 + 实时异常推送。
 * 新增 SSE 事件类型: graph_update, graph_action, insight_flash。
 */
import type { SubGraph } from './types';
export type InsightType = 'anomaly' | 'trend' | 'connection';
export type InsightSeverity = 'critical' | 'high' | 'medium' | 'low' | 'info';
/** 推送本体子图到前端 */
export declare function buildGraphUpdateEvent(subGraph: SubGraph, highlights?: string[]): string;
/** 实时推送异常发现 */
export declare function buildInsightFlash(type: InsightType, message: string, severity: InsightSeverity, nodeIds?: string[]): string;
/** 用户在图上的操作回传给引擎（前端→后端） */
export declare function buildGraphActionEvent(action: 'connect' | 'disconnect' | 'mark' | 'delete', payload: Record<string, unknown>): string;
export declare class GraphSSEEncoder {
    private events;
    addGraphUpdate(subGraph: SubGraph, highlights?: string[]): this;
    addInsightFlash(type: InsightType, message: string, severity: InsightSeverity, nodeIds?: string[]): this;
    addGraphAction(action: 'connect' | 'disconnect' | 'mark' | 'delete', payload: Record<string, unknown>): this;
    encode(): string;
    clear(): void;
}
