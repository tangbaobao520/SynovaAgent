"use strict";
/**
 * diagnosis/module-registry.ts — 诊断模块插件注册表
 *
 * 统一注册所有诊断 compute 函数，支持按需编排执行。
 * 每个模块独立 compute，失败不影响其他模块。
 *
 * 注册表使得：
 *   1. 第三方可通过 registerModule() 接入新诊断维度
 *   2. /plan 端点可按优先级/依赖顺序编排执行
 *   3. 前端可动态列出可用模块及状态
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.registerModule = registerModule;
exports.listModules = listModules;
exports.getModule = getModule;
exports.runModule = runModule;
exports.runModules = runModules;
// ── Registry ──
const registry = new Map();
/**
 * Register a diagnostic module.
 * If a module with the same id already exists, it is replaced.
 */
function registerModule(module) {
    registry.set(module.id, module);
}
/**
 * List all registered modules, optionally filtered by priority.
 */
function listModules(priority) {
    const modules = [...registry.values()];
    if (priority)
        return modules.filter(m => m.priority === priority);
    return modules.sort(byPriority);
}
/**
 * Get a single module by id.
 */
function getModule(id) {
    return registry.get(id);
}
/**
 * Run a single module by id, returning its result.
 */
async function runModule(teamId, id) {
    const mod = registry.get(id);
    if (!mod) {
        return { moduleId: id, status: 'failed', error: `未知模块: ${id}` };
    }
    try {
        const output = await mod.compute(teamId);
        const summary = buildSummary(mod.id, output);
        return { moduleId: id, status: 'ok', summary };
    }
    catch (err) {
        return {
            moduleId: id,
            status: 'degraded',
            error: err.message,
        };
    }
}
/**
 * Run selected modules (or all) in dependency order.
 *
 * Execution order respects logical dependencies:
 *   1. Observation (gaps) + event-based modules run first
 *   2. Derivation modules (dynamics) run next
 *   3. Reasoning modules run last
 *
 * Within each tier, modules run sequentially but independently
 * — failure in one does not block others in the same tier.
 */
async function runModules(teamId, moduleIds) {
    const ids = moduleIds ?? [...registry.keys()];
    const sorted = topologicalSort(ids);
    const results = [];
    // Execute tier by tier
    // Tier 0: no deps (gaps, attention, identity, cpc, hona, capability-spectrum,
    //          intent-alignment, seven-powers)
    // Tier 1: depends on snapshot data (dynamics, self-awareness, hacd, ipu, htm, eob)
    // Tier 2: depends on assembled diagnosis (financial-impact, token-economics)
    const tier0 = sorted.filter(id => !TIER_1_IDS.has(id) && !TIER_2_IDS.has(id));
    const tier1 = sorted.filter(id => TIER_1_IDS.has(id));
    const tier2 = sorted.filter(id => TIER_2_IDS.has(id));
    for (const id of tier0) {
        results.push(await runModule(teamId, id));
    }
    for (const id of tier1) {
        results.push(await runModule(teamId, id));
    }
    for (const id of tier2) {
        results.push(await runModule(teamId, id));
    }
    return results;
}
// ── Module dependency tiers ──
const TIER_1_IDS = new Set([
    'dynamics', 'self-awareness', 'hacd', 'ipu', 'htm', 'eob',
]);
const TIER_2_IDS = new Set([
    'financial-impact', 'token-economics',
]);
// ── Helpers ──
function byPriority(a, b) {
    const order = { P0: 0, P1: 1, P2: 2, P3: 3 };
    return order[a.priority] - order[b.priority];
}
function topologicalSort(ids) {
    // Simple: sort by tier. Modules not in the registry are appended at the end.
    const tiered = [...ids].sort((a, b) => {
        const tierA = TIER_2_IDS.has(a) ? 2 : TIER_1_IDS.has(a) ? 1 : 0;
        const tierB = TIER_2_IDS.has(b) ? 2 : TIER_1_IDS.has(b) ? 1 : 0;
        return tierA - tierB;
    });
    return tiered;
}
function buildSummary(id, output) {
    if (output === null || output === undefined)
        return '无数据';
    if (Array.isArray(output))
        return `${output.length} 条记录`;
    if (typeof output === 'object' && output !== null) {
        const o = output;
        if (typeof o.interpretation === 'string')
            return o.interpretation;
        if (typeof o.overallCoverage === 'number')
            return `覆盖度 ${(o.overallCoverage * 100).toFixed(0)}%`;
        if (typeof o.overallMoatStrength === 'number')
            return `壁垒强度 ${(o.overallMoatStrength * 100).toFixed(0)}%`;
        if (typeof o.totalInefficiencyCost === 'number')
            return `低效成本 ¥${o.totalInefficiencyCost}`;
        if (typeof o.totalCost === 'number')
            return `Token 成本 ¥${o.totalCost}`;
        if (typeof o.boundaryHealth === 'number')
            return `边界健康度 ${(o.boundaryHealth * 100).toFixed(0)}%`;
        if (typeof o.trustHealthScore === 'number')
            return `信任健康度 ${(o.trustHealthScore * 100).toFixed(0)}%`;
        return '已计算';
    }
    return undefined;
}
// ====================================================================
// Auto-register all built-in modules
// ====================================================================
const gap_recorder_1 = require("./gap-recorder");
const gap_dynamics_1 = require("./gap-dynamics");
const attention_allocator_1 = require("./attention-allocator");
const identity_extractor_1 = require("./identity-extractor");
const path_dependency_1 = require("./path-dependency");
const self_awareness_1 = require("./self-awareness");
const hacd_1 = require("./hacd");
const cpc_1 = require("./cpc");
const ipu_overload_1 = require("./ipu-overload");
const hona_1 = require("./hona");
const capability_spectrum_1 = require("./capability-spectrum");
const intent_alignment_1 = require("./intent-alignment");
const seven_powers_1 = require("./seven-powers");
const htm_1 = require("./htm");
const eob_1 = require("./eob");
const financial_impact_1 = require("./financial-impact");
const token_economics_1 = require("./token-economics");
function registerBuiltinModules() {
    const modules = [
        {
            id: 'gaps',
            version: '1.0.0',
            priority: 'P0',
            requiredDataSources: {},
            compute: (teamId) => (0, gap_recorder_1.getLatestSnapshot)(teamId),
            confidenceModel: 'deterministic',
            label: '六缝隙快照',
            description: '六维度协作模式评分（分工、信息流、权限、信任、知识共享、外部接口）',
        },
        {
            id: 'dynamics',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: {},
            compute: (teamId) => (0, gap_dynamics_1.computeDynamics)(teamId) ?? null,
            confidenceModel: 'statistical',
            label: '时间动态',
            description: '变化速度、加速度、相位耦合、粘性维度检测',
        },
        {
            id: 'attention',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => (0, attention_allocator_1.computeAttention)(teamId),
            confidenceModel: 'statistical',
            label: '注意力配置',
            description: '团队注意力主题分布、内外视比例、运营/创新比',
        },
        {
            id: 'identity',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => (0, identity_extractor_1.extractIdentityMarkers)(teamId),
            confidenceModel: 'statistical',
            label: '身份标记',
            description: '从团队对话中提取身份标记词及趋势',
        },
        {
            id: 'path-dependency',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: {},
            compute: (teamId) => (0, path_dependency_1.detectPathDependency)(teamId),
            confidenceModel: 'statistical',
            label: '路径依赖',
            description: '检测组织维度中的路径锁定及异常',
        },
        {
            id: 'self-awareness',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { questionnaire: true },
            compute: (teamId) => (0, self_awareness_1.computeSelfAwareness)(teamId),
            confidenceModel: 'statistical',
            label: '自知偏差',
            description: '引擎观测 vs 团队自评的偏差分析',
        },
        {
            id: 'hacd',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { interactionLogs: true },
            compute: (teamId) => (0, hacd_1.computeHACD)(teamId),
            confidenceModel: 'statistical',
            label: '人机协作深度',
            description: 'L0-L4 协作等级、人工介入比例、自主性趋势',
        },
        {
            id: 'cpc',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: {},
            compute: (teamId) => (0, cpc_1.computeCPC)(teamId),
            confidenceModel: 'deterministic',
            label: '协议完备性',
            description: '人机协作协议各维度完备度评估',
        },
        {
            id: 'ipu',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { agentEvents: true, interactionLogs: true },
            compute: (teamId) => (0, ipu_overload_1.computeIPU)(teamId),
            confidenceModel: 'statistical',
            label: '信息过载',
            description: 'Agent 队列深度、死锁率、瓶颈检测',
        },
        {
            id: 'hona',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { interactionLogs: true },
            compute: (teamId) => (0, hona_1.computeHONA)(teamId),
            confidenceModel: 'statistical',
            label: '异质节点网络',
            description: 'Agent 交互网络密度、中心性、结构类型',
        },
        {
            id: 'capability-spectrum',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => (0, capability_spectrum_1.computeCapabilitySpectrum)(teamId),
            confidenceModel: 'deterministic',
            label: '能力谱系',
            description: '6 业务能力维度覆盖度及缺口',
        },
        {
            id: 'intent-alignment',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { humanEvents: true, agentEvents: true },
            compute: (teamId) => (0, intent_alignment_1.computeIntentAlignment)(teamId),
            confidenceModel: 'llm_inferred',
            label: '意图对齐',
            description: '人-组织-Agent 三方目标对齐偏差',
        },
        {
            id: 'seven-powers',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => (0, seven_powers_1.computeSevenPowers)(teamId),
            confidenceModel: 'llm_inferred',
            label: '7 Powers',
            description: '规模经济、网络效应、差异化、转换成本、品牌、独家资源、流程优势',
        },
        {
            id: 'htm',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { interactionLogs: true, agentEvents: true },
            compute: (teamId) => (0, htm_1.computeHTM)(teamId),
            confidenceModel: 'statistical',
            label: '混合信任',
            description: '人对Agent信任曲线、Agent间信任、单点依赖风险',
        },
        {
            id: 'eob',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => (0, eob_1.computeEOB)(teamId),
            confidenceModel: 'statistical',
            label: '弹性边界',
            description: 'Agent流失率、弹性响应速度、僵尸权限检测',
        },
        {
            id: 'financial-impact',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true },
            compute: (teamId) => {
                // Financial impact needs the assembled diagnosis
                const { assembleFullDiagnosisV2 } = require('./diagnosis-assembler');
                const diag = assembleFullDiagnosisV2(teamId);
                const baseline = (0, financial_impact_1.loadFinancialBaseline)(teamId);
                return (0, financial_impact_1.computeFinancialImpact)(diag, baseline ?? undefined);
            },
            confidenceModel: 'deterministic',
            label: '财务影响',
            description: '诊断维度→财务金额映射，改善ROI估算',
        },
        {
            id: 'token-economics',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { agentEvents: true, interactionLogs: true },
            compute: (teamId) => {
                const baseline = (0, financial_impact_1.loadFinancialBaseline)(teamId);
                return (0, token_economics_1.computeTokenEconomics)(teamId, baseline ?? undefined);
            },
            confidenceModel: 'statistical',
            label: 'Token经济学',
            description: 'Token消耗归因、浪费分析、效率指标',
        },
    ];
    for (const mod of modules) {
        registerModule(mod);
    }
    // ── FDE Modules (ARCH-08) ──
    const fdeModules = [
        {
            id: 'auto-interpreter',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true },
            compute: async (teamId) => {
                const { assembleFullDiagnosisV2 } = await Promise.resolve().then(() => __importStar(require('./diagnosis-assembler')));
                const diag = assembleFullDiagnosisV2(teamId);
                const { generateMultiRoleNarrative } = await Promise.resolve().then(() => __importStar(require('./auto-interpreter')));
                return generateMultiRoleNarrative(diag);
            },
            confidenceModel: 'llm_inferred',
            label: '多角色解读',
            description: 'CEO/团队负责人/HRBP 三视角自动解读诊断结果',
        },
        {
            id: 'auto-action',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true, agentEvents: true },
            compute: async (teamId) => {
                const { assembleFullDiagnosisV2 } = await Promise.resolve().then(() => __importStar(require('./diagnosis-assembler')));
                const diag = assembleFullDiagnosisV2(teamId);
                const { generateActionPlan } = await Promise.resolve().then(() => __importStar(require('./auto-action')));
                return generateActionPlan(diag);
            },
            confidenceModel: 'llm_inferred',
            label: '自动行动',
            description: '基于诊断发现自动生成 Jira/Linear 可执行任务',
        },
        {
            id: 'task-integration',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true, agentEvents: true },
            compute: async (teamId) => {
                const { assembleFullDiagnosisV2 } = await Promise.resolve().then(() => __importStar(require('./diagnosis-assembler')));
                const diag = assembleFullDiagnosisV2(teamId);
                const { generateActionPlan } = await Promise.resolve().then(() => __importStar(require('./auto-action')));
                const plan = await generateActionPlan(diag);
                const { pushActionItems } = await Promise.resolve().then(() => __importStar(require('./task-integration')));
                return pushActionItems(teamId, plan.items);
            },
            confidenceModel: 'deterministic',
            label: '任务集成',
            description: '将诊断行动建议 push 到 Jira/Linear 外部任务系统',
        },
    ];
    for (const mod of fdeModules) {
        registerModule(mod);
    }
    // ── Marketing Modules (ARCH-19) ──
    const marketingModules = [
        {
            id: 'category-clarity',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { questionnaire: true },
            compute: (teamId) => {
                // Requires customer survey responses stored per team.
                // Returns null when < 3 responses available.
                return null;
            },
            confidenceModel: 'statistical',
            label: '品类认知清晰度',
            description: '客户用同一个品类词描述公司吗？主导词一致性比例 + 混乱度判定',
        },
        {
            id: 'positioning-consistency',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { questionnaire: true },
            compute: (teamId) => {
                // Requires three-party positioning data (external claims, internal descriptions, customer perceptions).
                // Returns null when any party data is empty.
                return null;
            },
            confidenceModel: 'statistical',
            label: '定位三方一致性',
            description: '对外声称 vs 内部共识 vs 客户感知的三方对齐度，含缺口诊断',
        },
        {
            id: 'differentiation-validation',
            version: '1.0.0',
            priority: 'P1',
            requiredDataSources: { questionnaire: true, humanEvents: true },
            compute: (teamId) => {
                // Requires claimed differentiation + customer perception data.
                // Rule-based overlap + optional LLM semantic equivalence refinement.
                return null;
            },
            confidenceModel: 'llm_inferred',
            label: '差异化实质性验证',
            description: '公司声称的差异化是否被客户感知 + 组织能力是否支撑',
        },
    ];
    for (const mod of marketingModules) {
        registerModule(mod);
    }
    // ── P2 Modules (ARCH-08 Benchmark + Enricher) ──
    const p2Modules = [
        {
            id: 'benchmark',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: { humanEvents: true },
            compute: async (teamId) => {
                const { computeBenchmark } = await Promise.resolve().then(() => __importStar(require('./benchmark-engine')));
                return computeBenchmark(teamId);
            },
            confidenceModel: 'statistical',
            label: '基准对比',
            description: '跨团队百分位排名，对比同类团队的协作健康度',
        },
        {
            id: 'data-enricher',
            version: '1.0.0',
            priority: 'P2',
            requiredDataSources: {},
            compute: async (teamId) => {
                const { enrichDiagnosis } = await Promise.resolve().then(() => __importStar(require('./data-enricher')));
                return enrichDiagnosis(teamId);
            },
            confidenceModel: 'statistical',
            label: '数据富化',
            description: '从本地 Git、软件生态、GitHub API 获取外部上下文',
        },
    ];
    for (const mod of p2Modules) {
        registerModule(mod);
    }
}
// Auto-register on import
registerBuiltinModules();
//# sourceMappingURL=module-registry.js.map