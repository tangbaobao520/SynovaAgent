import { DiagnosticAgentError } from './diagnosis-error';
/** 图数据库操作失败 (SQLite 锁、连接断开、磁盘满) — 可重试 */
export declare class GraphStoreError extends DiagnosticAgentError {
    constructor(message: string, cause?: Error);
}
/** 文档摄取校验失败 (缺少必填字段、格式不合法) — 不可重试 */
export declare class IngestionError extends DiagnosticAgentError {
    constructor(message: string, cause?: Error);
}
/** 请求的实体不存在 (快照未找到、节点已删除) — 不可重试 */
export declare class EntityNotFoundError extends DiagnosticAgentError {
    constructor(message: string, cause?: Error);
}
/** 图查询超时 (遍历深度过大、图规模超限) — 可重试 (减小范围) */
export declare class QueryTimeoutError extends DiagnosticAgentError {
    constructor(message: string, cause?: Error);
}
/** 实体合并版本冲突 (keyAspect 不匹配) — 不可重试 */
export declare class VersionConflictError extends DiagnosticAgentError {
    constructor(message: string, cause?: Error);
}
/** 检查错误是否为可重试的本体错误。对 unknown 安全。 */
export declare function isRetryableOntologyError(err: unknown): boolean;
