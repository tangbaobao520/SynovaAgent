/**
 * ontology-templates/index.ts — 行业本体模板加载器 (Phase B5)
 */
import type { NodeType, EdgeType } from '../types';
export interface OntologyTemplate {
    id: string;
    name: string;
    industry: string;
    version: string;
    nodeTypes: Array<{
        type: NodeType;
        label: string;
        description: string;
        exampleProps: Record<string, unknown>;
    }>;
    edgeTypes: Array<{
        type: EdgeType;
        label: string;
        description: string;
        fromNodes: NodeType[];
        toNodes: NodeType[];
    }>;
    keyMetrics: Array<{
        id: string;
        name: string;
        formula: string;
        unit: string;
        threshold_warning?: number;
        threshold_critical?: number;
    }>;
    diagnosticRules: Array<{
        id: string;
        name: string;
        condition: string;
        severity: 'high' | 'medium' | 'low';
        recommendation: string;
    }>;
}
export declare function getTemplate(id: string): OntologyTemplate | undefined;
export declare function listTemplates(): OntologyTemplate[];
export declare function getTemplateIds(): string[];
