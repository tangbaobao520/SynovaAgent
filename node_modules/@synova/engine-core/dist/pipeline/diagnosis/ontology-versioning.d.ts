/**
 * ontology-versioning.ts — 本体版本管理 (Phase C4)
 *
 * 双时序查询 + diff + 回滚。所有三元组带 valid_from/valid_to。
 * 永不物理删除——这是硬边界。
 */
import type { GraphStore } from './graph-store';
import type { Triple, GraphNode } from './types';
export interface OntologySnapshot {
    id: string;
    graph: string;
    timestamp: string;
    nodeCount: number;
    edgeCount: number;
    nodes: GraphNode[];
    triples: Triple[];
}
export declare function createSnapshot(store: GraphStore, graph: string): OntologySnapshot;
export interface OntologyDiff {
    addedNodes: GraphNode[];
    removedNodes: GraphNode[];
    addedEdges: Triple[];
    removedEdges: Triple[];
    nodeCountDelta: number;
    edgeCountDelta: number;
}
export declare function diffSnapshots(prev: OntologySnapshot, curr: OntologySnapshot): OntologyDiff;
export declare function getNodeHistory(store: GraphStore, nodeId: string, graph: string): Array<{
    timestamp: string;
    props: Record<string, unknown>;
}>;
export declare function rollbackToSnapshot(store: GraphStore, snapshotId: string, graph: string): boolean;
export declare function clearVersioning(): void;
