"use strict";
/**
 * diagnosis/persistence.ts — SQLite 持久化层
 *
 * 在现有内存 Map 基础上追加持久化写入，进程重启后从 SQLite 恢复到内存。
 * 写入：内存 Map + SQLite 双写（SQLite 写入失败不阻塞内存操作）
 * 读取：从内存 Map 读取（SQLite 仅用于启动恢复）
 *
 * 通过 EngineContext.database.getDb() 获取 SQLite 实例。
 * 数据库未注入时降级为纯内存模式。
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveSnapshot = saveSnapshot;
exports.loadTimeline = loadTimeline;
exports.loadAllTimelines = loadAllTimelines;
exports.saveAttentionLog = saveAttentionLog;
exports.loadAttentionLogs = loadAttentionLogs;
exports.saveIdentityData = saveIdentityData;
exports.loadIdentityData = loadIdentityData;
exports.saveSelfAssessment = saveSelfAssessment;
exports.loadSelfAssessments = loadSelfAssessments;
exports.saveFinancialBaselineToDb = saveFinancialBaselineToDb;
exports.loadFinancialBaselineFromDb = loadFinancialBaselineFromDb;
exports.loadAllFinancialBaselines = loadAllFinancialBaselines;
const engine_context_1 = require("../../engine-context");
const logger_1 = require("../../infra/logger");
const log = (0, logger_1.createLogger)('engine-server/pipeline/diagnosis/persistence');
// ====================================================================
// DB access helper
// ====================================================================
function getDb() {
    try {
        return (0, engine_context_1.getEngineContext)().database.getDb();
    }
    catch {
        log.debug('[diagnosis/persistence] database not available via engine context, falling back to memory-only mode');
        return null;
    }
}
// ====================================================================
// Snapshots (gap-recorder)
// ====================================================================
function saveSnapshot(snapshot) {
    const db = getDb();
    if (!db)
        return;
    try {
        db.prepare(`
      INSERT INTO diagnosis_snapshots (team_id, snapshot_json, created_at)
      VALUES (?, ?, ?)
    `).run(snapshot.teamId, JSON.stringify(snapshot), new Date().toISOString());
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId: snapshot.teamId }, '[diagnosis/persistence] 保存快照失败，继续使用内存存储');
    }
}
function loadTimeline(teamId) {
    const db = getDb();
    if (!db)
        return [];
    const log = (0, engine_context_1.getEngineContext)().logger;
    const snapshots = [];
    try {
        const rows = db.prepare(`
      SELECT snapshot_json, rowid FROM diagnosis_snapshots
      WHERE team_id = ?
      ORDER BY created_at ASC
    `).all(teamId);
        for (const row of rows) {
            try {
                snapshots.push(JSON.parse(row.snapshot_json));
            }
            catch (parseErr) {
                log.warn({ parseErr, teamId, rowid: row.rowid }, '[diagnosis/persistence] 跳过损坏的快照行');
            }
        }
    }
    catch (err) {
        log.warn({ err, teamId }, '[diagnosis/persistence] 加载快照失败');
        return [];
    }
    return snapshots;
}
function loadAllTimelines() {
    const db = getDb();
    if (!db)
        return new Map();
    const log = (0, engine_context_1.getEngineContext)().logger;
    const timelines = new Map();
    let badRows = 0;
    try {
        const rows = db.prepare(`
      SELECT team_id, snapshot_json, rowid FROM diagnosis_snapshots
      ORDER BY created_at ASC
    `).all();
        for (const row of rows) {
            try {
                const snapshots = timelines.get(row.team_id) ?? [];
                snapshots.push(JSON.parse(row.snapshot_json));
                timelines.set(row.team_id, snapshots);
            }
            catch (parseErr) {
                badRows++;
                log.warn({ parseErr, teamId: row.team_id, rowid: row.rowid }, '[diagnosis/persistence] 跳过损坏的快照行');
            }
        }
    }
    catch (err) {
        log.warn({ err }, '[diagnosis/persistence] 加载全部快照失败');
    }
    if (badRows > 0) {
        log.warn({ badRows, totalTeams: timelines.size }, '[diagnosis/persistence] 部分快照行损坏已跳过');
    }
    return timelines;
}
// ====================================================================
// Attention logs (attention-allocator)
// ====================================================================
function saveAttentionLog(teamId, logType, key, count) {
    const db = getDb();
    if (!db)
        return;
    try {
        db.prepare(`
      INSERT INTO diagnosis_attention_logs (team_id, log_type, key, count, updated_at)
      VALUES (?, ?, ?, ?, ?)
      ON CONFLICT(team_id, log_type, key) DO UPDATE SET
        count = excluded.count,
        updated_at = excluded.updated_at
    `).run(teamId, logType, key, count, new Date().toISOString());
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId, logType, key }, '[diagnosis/persistence] 保存注意力日志失败');
    }
}
function loadAttentionLogs(teamId) {
    const db = getDb();
    const empty = {
        topicLogs: new Map(),
        decisionLogs: new Map(),
        interactionCounts: { ops: 0, innovation: 0 },
        agentConsumption: new Map(),
    };
    if (!db)
        return empty;
    try {
        const rows = db.prepare(`
      SELECT log_type, key, count FROM diagnosis_attention_logs
      WHERE team_id = ?
    `).all(teamId);
        for (const row of rows) {
            switch (row.log_type) {
                case 'topic':
                    empty.topicLogs.set(row.key, row.count);
                    break;
                case 'decision':
                    empty.decisionLogs.set(row.key, row.count);
                    break;
                case 'interaction_ops':
                    empty.interactionCounts.ops += row.count;
                    break;
                case 'interaction_innovation':
                    empty.interactionCounts.innovation += row.count;
                    break;
                case 'agent_consumption':
                    empty.agentConsumption.set(row.key, row.count);
                    break;
            }
        }
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 加载注意力日志失败');
    }
    return empty;
}
// ====================================================================
// Identity data (identity-extractor)
// ====================================================================
function saveIdentityData(teamId, sentences) {
    const db = getDb();
    if (!db)
        return;
    try {
        db.prepare(`
      INSERT INTO diagnosis_identity_data (team_id, sentences_json, updated_at)
      VALUES (?, ?, ?)
      ON CONFLICT(team_id) DO UPDATE SET
        sentences_json = excluded.sentences_json,
        updated_at = excluded.updated_at
    `).run(teamId, JSON.stringify(sentences), new Date().toISOString());
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 保存身份数据失败');
    }
}
function loadIdentityData(teamId) {
    const db = getDb();
    if (!db)
        return [];
    try {
        const row = db.prepare(`
      SELECT sentences_json FROM diagnosis_identity_data
      WHERE team_id = ?
    `).get(teamId);
        if (row) {
            return JSON.parse(row.sentences_json);
        }
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 加载身份数据失败');
    }
    return [];
}
// ====================================================================
// Self-assessments (self-awareness)
// ====================================================================
function saveSelfAssessment(record) {
    const db = getDb();
    if (!db)
        return;
    try {
        db.prepare(`
      INSERT INTO diagnosis_self_assessments (team_id, dimension, score, recorded_at)
      VALUES (?, ?, ?, ?)
    `).run(record.teamId, record.dimension, record.score, record.recordedAt);
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId: record.teamId }, '[diagnosis/persistence] 保存自评数据失败');
    }
}
function loadSelfAssessments(teamId) {
    const db = getDb();
    if (!db)
        return [];
    try {
        const rows = db.prepare(`
      SELECT team_id, dimension, score, recorded_at FROM diagnosis_self_assessments
      WHERE team_id = ?
      ORDER BY recorded_at ASC
    `).all(teamId);
        return rows.map(r => ({
            teamId: r.team_id,
            dimension: r.dimension,
            score: r.score,
            recordedAt: r.recorded_at,
        }));
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 加载自评数据失败');
        return [];
    }
}
// ====================================================================
// Financial baselines (financial-impact)
// ====================================================================
function saveFinancialBaselineToDb(teamId, baseline) {
    const db = getDb();
    if (!db)
        return;
    try {
        db.prepare(`
      INSERT INTO diagnosis_financial_baselines (team_id, baseline_json, updated_at)
      VALUES (?, ?, ?)
      ON CONFLICT(team_id) DO UPDATE SET
        baseline_json = excluded.baseline_json,
        updated_at = excluded.updated_at
    `).run(teamId, JSON.stringify(baseline), new Date().toISOString());
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 保存财务基线失败');
    }
}
function loadFinancialBaselineFromDb(teamId) {
    const db = getDb();
    if (!db)
        return null;
    try {
        const row = db.prepare(`
      SELECT baseline_json FROM diagnosis_financial_baselines
      WHERE team_id = ?
    `).get(teamId);
        if (row) {
            return JSON.parse(row.baseline_json);
        }
    }
    catch (err) {
        (0, engine_context_1.getEngineContext)().logger.warn({ err, teamId }, '[diagnosis/persistence] 加载财务基线失败');
    }
    return null;
}
function loadAllFinancialBaselines() {
    const db = getDb();
    const baselines = new Map();
    if (!db)
        return baselines;
    const log = (0, engine_context_1.getEngineContext)().logger;
    try {
        const rows = db.prepare(`
      SELECT team_id, baseline_json FROM diagnosis_financial_baselines
    `).all();
        for (const row of rows) {
            try {
                baselines.set(row.team_id, JSON.parse(row.baseline_json));
            }
            catch (parseErr) {
                log.warn({ parseErr, teamId: row.team_id }, '[diagnosis/persistence] 跳过损坏的财务基线行');
            }
        }
    }
    catch (err) {
        log.warn({ err }, '[diagnosis/persistence] 加载全部财务基线失败');
    }
    return baselines;
}
//# sourceMappingURL=persistence.js.map