/**
 * posture-weights.ts — 三种战略姿态的模块权重、阈值、叙事配置
 *
 * 纯数据文件，无逻辑。由 posture-lens.ts 消费，注入诊断编排器。
 * 五种姿态中 P1 实现前三种（护城河/稳健经营/生存突破），
 * 自由生活型和使命驱动型留到 P2。
 */
import type { PostureConfig } from './types';
/** 三种 P1 姿态的完整配置 */
export declare const POSTURE_CONFIGS: Record<string, PostureConfig>;
/** 根据姿态 ID 获取配置 */
export declare function getPostureConfig(posture: string): PostureConfig | undefined;
/** 默认姿态（无问卷答案时回退） */
export declare const DEFAULT_POSTURE: PostureConfig;
