"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPromptCache = createPromptCache;
/**
 * prompt-cache.ts — Prompt 缓存 (对标 Claw-Code prompt_cache.rs)
 *
 * Hash-based: FNV-1a(systemPrompt + userMessage + model) → cached response.
 * Disk-based with TTL: 30s for completions, 5min for prompt state.
 */
const crypto = __importStar(require("crypto"));
function createPromptCache(ttlMs = 30_000) {
    const store = new Map();
    let hits = 0, misses = 0, sets = 0, invalidations = 0;
    return {
        buildKey(systemPrompt, userMessage, model = 'default') {
            const hash = crypto.createHash('sha256')
                .update(systemPrompt.slice(0, 4000)) // Truncate for stable hash
                .update(userMessage.slice(0, 4000))
                .update(model)
                .digest('hex');
            return hash;
        },
        get(key) {
            const entry = store.get(key);
            if (!entry) {
                misses++;
                return null;
            }
            if (Date.now() - entry.cachedAt > ttlMs) {
                store.delete(key);
                invalidations++;
                misses++;
                return null;
            }
            hits++;
            return entry.response;
        },
        set(key, response) {
            store.set(key, { response, cachedAt: Date.now() });
            sets++;
        },
        clear() { store.clear(); },
        stats() { return { hits, misses, sets, invalidations }; },
    };
}
//# sourceMappingURL=prompt-cache.js.map