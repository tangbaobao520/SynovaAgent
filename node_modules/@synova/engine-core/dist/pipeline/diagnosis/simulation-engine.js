"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runSimulation = runSimulation;
exports.newHireScenario = newHireScenario;
exports.restructureScenario = restructureScenario;
const sog_core_schema_1 = require("../../sog/sog-core-schema");
/**
 * simulation-engine.ts — 组织仿真引擎 (ARCH-23 Phase C)
 *
 * 数字孪生的终极能力: 在本体图上运行 "what-if" 仿真。
 * Clone 图 → 应用变换 → 运行诊断 → 对比基线 → 生成建议。
 *
 * 关键设计决策: Clone, don't mutate-and-rollback.
 * 仿真在独立的 clone store 上运行，原始 store 完全不受影响。
 */
const graph_store_1 = require("./graph-store");
const graph_query_1 = require("./graph-query");
const graph_monitor_1 = require("./graph-monitor");
const decision_engine_1 = require("./decision-engine");
const ontology_versioning_1 = require("./ontology-versioning");
const logger_1 = require("../../infra/logger");
const log = (0, logger_1.createLogger)('diagnosis/simulation-engine');
// ═══ ID generator (deterministic, no Date.now()) ═══
let _idCounter = 0;
function nextId(prefix) {
    return `${prefix}_${(++_idCounter).toString(36)}`;
}
/** Clone all nodes and edges from source store into a new in-memory store. */
function cloneStore(source, graph) {
    const BetterSqlite3 = require('better-sqlite3');
    const clone = (0, graph_store_1.createGraphStore)('sqlite', new BetterSqlite3(':memory:'));
    const nodeMap = new Map();
    const edgeMap = new Map();
    // Clone all node types
    const nodeTypes = [sog_core_schema_1.SOGNodeType.PERSON, sog_core_schema_1.SOGNodeType.TEAM, sog_core_schema_1.SOGNodeType.AGENT, sog_core_schema_1.SOGNodeType.TOOL, sog_core_schema_1.SOGNodeType.CLIENT, sog_core_schema_1.SOGNodeType.PROCESS, sog_core_schema_1.SOGNodeType.EVENT, sog_core_schema_1.SOGNodeType.DOCUMENT, sog_core_schema_1.SOGNodeType.FINANCIAL];
    for (const ntype of nodeTypes) {
        const nodes = source.queryNodes(ntype, undefined, graph);
        for (const node of nodes) {
            const newId = clone.createNode(ntype, { ...node.props }, graph);
            nodeMap.set(node.id, newId);
        }
    }
    // Clone all edges
    const edges = source.queryEdges(undefined, undefined, undefined, graph);
    for (const edge of edges) {
        const newFrom = nodeMap.get(edge.from) || edge.from;
        const newTo = nodeMap.get(edge.to) || edge.to;
        const newId = clone.createEdge(edge.type, newFrom, newTo, edge.weight, { ...edge.props }, graph);
        edgeMap.set(edge.id, newId);
    }
    log.info({ nodes: nodeMap.size, edges: edgeMap.size, graph }, '[simulation] Cloned store');
    return { store: clone, nodeMap, edgeMap };
}
// ═══ Engine ═══
/** 在图上运行仿真场景。原始 store 不会被修改。 */
function runSimulation(store, graph, scenario) {
    // 1. 基线快照 (原始 store)
    const baseline = (0, ontology_versioning_1.createSnapshot)(store, graph);
    if (!baseline)
        return null;
    // 2. 检查基线是否有可分析的内容
    const baselinePersons = store.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, undefined, graph);
    if (baselinePersons.length === 0) {
        log.warn({ graph, scenario: scenario.id }, '[simulation] Empty baseline — nothing to simulate');
        return null;
    }
    // 3. 基线指标 (原始 store)
    const baselineCent = computeAllCentralities(store, graph);
    const baselineCommunities = (0, graph_query_1.detectCommunities)(store, 2, graph).length;
    // 4. Clone store + 应用变换
    const clone = cloneStore(store, graph);
    const changes = applyTransformations(clone, graph, scenario.transformations);
    // 5. 仿真后快照 + 指标
    const simulated = (0, ontology_versioning_1.createSnapshot)(clone.store, graph);
    const simCentRaw = computeAllCentralities(clone.store, graph);
    const simCommunities = (0, graph_query_1.detectCommunities)(clone.store, 2, graph).length;
    // Build reverse map: clone ID → original ID
    const reverseMap = new Map();
    for (const [origId, cloneId] of clone.nodeMap) {
        reverseMap.set(cloneId, origId);
    }
    // Translate clone centrality entries to use original node IDs
    const simCent = simCentRaw.map(s => ({
        nodeId: reverseMap.get(s.nodeId) || s.nodeId,
        centrality: s.centrality,
    }));
    // 6. 中心性变化 (delta > 0.05 视为有意义)
    const centralityChanges = baselineCent.map(b => {
        const sim = simCent.find(s => s.nodeId === b.nodeId);
        const simulatedVal = sim?.centrality || 0;
        return {
            nodeId: b.nodeId,
            baseline: b.centrality,
            simulated: simulatedVal,
            delta: simulatedVal - b.centrality,
        };
    }).filter(c => Math.abs(c.delta) > 0.05);
    // 7. 路径对比 (关键人员之间的协作效率)
    //    基线路径用原始 store，仿真路径用 clone store
    const pathChanges = computePathChanges(store, clone.store, graph);
    // 8. 告警 + 行动 (在 clone 上运行)
    const alerts = (0, graph_monitor_1.runMonitorTick)(clone.store, graph, {
        edgeTypes: [sog_core_schema_1.SOGEdgeType.INTERACTS_WITH],
        weightThreshold: 0.2,
        centralityShiftThreshold: 0.3,
    });
    const actions = (0, decision_engine_1.runDecisionEngine)(clone.store, graph);
    return {
        scenarioId: scenario.id,
        scenarioName: scenario.name,
        baselineSnapshotId: baseline.id,
        simulatedSnapshotId: simulated.id,
        delta: { addedNodes: changes.addedNodes, addedEdges: changes.addedEdges, modifiedEdges: changes.modifiedEdges },
        centralityChanges,
        communityChanges: {
            baselineCommunities,
            simulatedCommunities: simCommunities,
        },
        newAlerts: alerts,
        triggeredActions: actions,
        pathChanges,
        generatedAt: new Date().toISOString(),
    };
}
// ═══ Path Comparison ═══
function computePathChanges(baselineStore, simStore, graph) {
    const changes = [];
    const basePersons = baselineStore.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, undefined, graph);
    // Compare paths between consecutive persons (limited to 5 pairs to avoid O(N²))
    for (let i = 0; i < Math.min(basePersons.length - 1, 5); i++) {
        for (let j = i + 1; j < Math.min(basePersons.length, i + 3); j++) {
            const fromId = basePersons[i].id;
            const toId = basePersons[j].id;
            const basePath = (0, graph_query_1.shortestPath)(baselineStore, fromId, toId, sog_core_schema_1.SOGEdgeType.INTERACTS_WITH, graph);
            const simPath = (0, graph_query_1.shortestPath)(simStore, fromId, toId, sog_core_schema_1.SOGEdgeType.INTERACTS_WITH, graph);
            const baseLen = basePath ? basePath.length : Infinity;
            const simLen = simPath ? simPath.length : Infinity;
            // Only report if there's a meaningful change
            if (baseLen !== simLen) {
                changes.push({
                    from: fromId,
                    to: toId,
                    baselineLength: baseLen === Infinity ? 99 : baseLen,
                    simulatedLength: simLen === Infinity ? 99 : simLen,
                    improved: simLen < baseLen,
                });
            }
        }
    }
    return changes;
}
// ═══ Helpers ═══
function computeAllCentralities(store, graph) {
    const persons = store.queryNodes(sog_core_schema_1.SOGNodeType.PERSON, undefined, graph);
    return persons.map(p => ({ nodeId: p.id, centrality: (0, graph_query_1.degreeCentrality)(store, p.id, graph) }));
}
function applyTransformations(clone, graph, transforms) {
    let addedNodes = 0, addedEdges = 0, modifiedEdges = 0;
    for (const t of transforms) {
        switch (t.type) {
            case 'add_node': {
                clone.store.createNode(t.nodeType, t.props, graph);
                addedNodes++;
                break;
            }
            case 'remove_node': {
                // Verify node exists before attempting delete
                const node = clone.store.getNode(t.nodeId, graph);
                if (node) {
                    clone.store.deleteNode(t.nodeId, graph);
                }
                else {
                    log.warn({ nodeId: t.nodeId }, '[simulation] remove_node skipped — node not found');
                }
                break;
            }
            case 'add_edge': {
                clone.store.createEdge(t.edgeType, t.from, t.to, t.weight, getDefaultEdgeProps(t.edgeType), graph);
                addedEdges++;
                break;
            }
            case 'modify_edge': {
                const edges = clone.store.queryEdges(t.edgeType, t.from, t.to, graph);
                if (edges.length > 0) {
                    clone.store.deleteEdge(edges[0].id, graph);
                    clone.store.createEdge(t.edgeType, t.from, t.to, t.newWeight, getDefaultEdgeProps(t.edgeType), graph);
                    modifiedEdges++;
                }
                else {
                    log.warn({ from: t.from, to: t.to, edgeType: t.edgeType }, '[simulation] modify_edge skipped — edge not found');
                }
                break;
            }
            case 'add_team': {
                // FIXED: Capture the created Team node ID
                const teamId = clone.store.createNode(sog_core_schema_1.SOGNodeType.TEAM, { name: t.name, teamType: 'permanent' }, graph);
                addedNodes++;
                for (const memberId of t.members) {
                    clone.store.createEdge(sog_core_schema_1.SOGEdgeType.BELONGS_TO, memberId, teamId, 1, {}, graph);
                    addedEdges++;
                }
                break;
            }
            case 'restructure_team': {
                for (const mid of (t.addMembers || [])) {
                    clone.store.createEdge(sog_core_schema_1.SOGEdgeType.BELONGS_TO, mid, t.teamId, 1, {}, graph);
                    addedEdges++;
                }
                for (const mid of (t.removeMembers || [])) {
                    const edges = clone.store.queryEdges(sog_core_schema_1.SOGEdgeType.BELONGS_TO, mid, t.teamId, graph);
                    for (const e of edges) {
                        clone.store.deleteEdge(e.id, graph);
                    }
                }
                break;
            }
        }
    }
    return { addedNodes, addedEdges, modifiedEdges };
}
// ═══ Scenario Templates ═══
/** 根据边类型返回 SOG 必需的默认属性 */
function getDefaultEdgeProps(edgeType) {
    switch (edgeType) {
        case 'INTERACTS_WITH': return { channel: 'direct_message' };
        case 'OWNS': return { ownershipType: 'manages' };
        case 'CORRESPONDS_TO': return { correspondenceType: 'related', confidence: 0.9 };
        case 'AFFECTS': return { direction: 'positive' };
        case 'DEPENDS_ON': return { criticality: 'optional' };
        case 'CONSUMES': return { amount: 1, period: 'P1M' };
        case 'ALIGNS_WITH': return { alignmentStrength: 0.5, alignmentType: 'direct' };
        default: return {};
    }
}
function newHireScenario(personName, teamId) {
    return {
        id: nextId('sim_hire'),
        name: `新成员加入: ${personName}`,
        description: `模拟 ${personName} 加入团队后的组织影响`,
        transformations: [
            { type: 'add_node', nodeType: sog_core_schema_1.SOGNodeType.PERSON, props: { name: personName, role: 'new_hire' } },
            { type: 'add_edge', edgeType: sog_core_schema_1.SOGEdgeType.BELONGS_TO, from: 'PLACEHOLDER', to: teamId, weight: 1 },
        ],
    };
}
function restructureScenario(teamName, members) {
    return {
        id: nextId('sim_restruct'),
        name: `团队重组: ${teamName}`,
        description: `模拟创建新团队 ${teamName} 并移入 ${members.length} 名成员`,
        transformations: [{ type: 'add_team', name: teamName, members }],
    };
}
//# sourceMappingURL=simulation-engine.js.map