/**
 * synthesizer.ts — 专家报告合成器 (Step 4)
 *
 * 对标 Hermes MoA aggregator + curator _parse_structured_summary。
 * 6 份 ExpertReport → crossReference分析 → highContention标记 → 假设卡片。
 */
import type { DiagnosisLLMClient } from './diagnosis-orchestrator';
import type { ExpertReport, SynthesisReport, DiagnosisEvidence } from './types';
export declare function synthesizeExpertReports(reports: ExpertReport[], evidencePool: DiagnosisEvidence[], llmClient: DiagnosisLLMClient): Promise<SynthesisReport>;
