/**
 * engine-server/pipeline/phase-b-distill-genome.ts — Phase B (L2): 蒸馏角色认知基因
 *
 * 输入：TaskDefinitionDTO + Phase A 结果
 * 输出：PersonaGenomeBlue[] + IncubationFrame
 *
 * 增强策略（AR-07）：
 * 1. 框架注入：从 45 个认知框架库中为每个角色匹配候选框架，注入 LLM prompt
 * 2. 冲突检测：LLM 输出后检查框架组合冲突
 * 3. 质量检查：4 维度评分（ocean/mental model/honest boundary/anti-pattern）
 *
 * @packageDocumentation
 */
import type { TaskDefinitionDTO, PhaseAResult, PhaseBResult, PersonaGenomeBlue, RoleBlue } from '../types';
export declare function buildFallbackGenomes(roles: RoleBlue[], job: string): any;
export declare function mapToPersonaGenomes(parsed: any): PersonaGenomeBlue[];
export declare function runPhaseB(taskDef: TaskDefinitionDTO, phaseA: PhaseAResult, locale: string, abortSignal: AbortSignal, orgAmmoText?: string, diagnosisContext?: string): Promise<PhaseBResult>;
/**
 * 单角色认知基因蒸馏。从 batch 逻辑中提取，保留团队上下文注入。
 * 供 runPipelinePartial 的 add_role / split_role 等增量操作使用。
 */
export declare function distillSingleRole(taskDef: TaskDefinitionDTO, role: RoleBlue, existingTeamContext: string, locale: string, abortSignal: AbortSignal): Promise<PersonaGenomeBlue>;
