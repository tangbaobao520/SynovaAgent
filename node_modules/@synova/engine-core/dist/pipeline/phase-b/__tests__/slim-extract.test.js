"use strict";
/**
 * engine-server/pipeline/phase-b/__tests__/slim-extract.test.ts
 *
 * SlimPersona 提取器单元测试
 * 沈括验收标准：3个用例（Token≤60 / 退化触发 / 45→≤5缩减率）
 */
Object.defineProperty(exports, "__esModule", { value: true });
const slim_extract_1 = require("../slim-extract");
// ================================================================
// 辅助函数
// ================================================================
function makeFramework(id, name, insight, category = 'psychology') {
    return {
        id,
        name,
        category,
        coreInsight: insight,
        applicableDecisionTypes: ['通用决策'],
        limitations: ['通用局限'],
    };
}
// ================================================================
// TC-01: Token计数 ≤60
// ================================================================
function test_tokenCountWithinLimit() {
    const frameworks = [
        makeFramework('f1', '激励偏差', '人的行为由激励结构驱动，而非由应该做什么决定。'),
        makeFramework('f2', '确认偏误', '人们倾向于寻找支持已有观点的信息。'),
        makeFramework('f3', '安全边际', '在不确定环境中为最坏情况留出缓冲空间。'),
    ];
    const result = (0, slim_extract_1.extractSlimPersona)(frameworks, 'test-role');
    console.assert(result.totalTokenCount <= 60, `TC-01 FAIL: totalTokenCount=${result.totalTokenCount} > 60`);
    console.assert(result.degradationRisk === 'none', `TC-01 FAIL: degradationRisk=${result.degradationRisk}, expected none`);
    console.assert(result.fallbackTriggered === false, `TC-01 FAIL: fallbackTriggered=${result.fallbackTriggered}`);
    console.log(`✅ TC-01 PASS: tokens=${result.totalTokenCount}, risk=${result.degradationRisk}, models=${result.slimMentalModels.length}`);
}
// ================================================================
// TC-02: 退化检测触发
// ================================================================
function test_degradationTriggered() {
    // 创建8个框架，每个有真正不同的长描述（≥80 chars），确保超过退化阈值
    const longDescriptions = [
        '在复杂系统中任何单一指标的优化都会导致其他指标的劣化这是坎贝尔定律的核心洞见适用于绩效',
        '市场并不总是有效的投资者情绪会导致价格大幅偏离基本面安全边际原则要求为不确定性留出缓冲',
        '人们在面对损失时的痛苦感约等于同等收益快乐感的2倍这种损失厌恶影响谈判策略和风险管理',
        '系统中最薄弱的环节决定了整体可靠性的上限约束理论提醒管理者不应该平均分配改进资源',
        '复利效应不仅适用于金融投资也适用于知识积累和团队建设小改进的长期累积会产生指数级影响',
        '第一性原理要求将问题分解到最基本元素然后从零重建解决方案这是在成熟行业中创新的方法论',
        '信息的非对称分布会导致市场中的逆向选择高质量供给者因为无法传递信号而被迫退出市场',
        '在博弈论中可信的承诺需满足可观察性和不可逆性两个条件否则承诺只是廉价的空谈',
    ];
    const frameworks = longDescriptions.map((desc, i) => makeFramework(`fw${i}`, `框架${i}`, desc));
    const result = (0, slim_extract_1.extractSlimPersona)(frameworks, 'test-role');
    console.assert(result.slimMentalModels.length <= 5, `TC-02 FAIL: slimMentalModels=${result.slimMentalModels.length} > 5`);
    console.assert(result.degradationRisk !== 'none', `TC-02 FAIL: degradationRisk=${result.degradationRisk}, expected moderate or critical`);
    console.assert(result.suppressedCount >= 3, `TC-02 FAIL: suppressedCount=${result.suppressedCount} (expected ≥3, had ${frameworks.length} total)`);
    console.log(`✅ TC-02 PASS: suppressed=${result.suppressedCount}/${frameworks.length}, risk=${result.degradationRisk}, tokens=${result.totalTokenCount}`);
}
// ================================================================
// TC-03: 45→≤5 框架缩减率
// ================================================================
function test_reductionRatio() {
    // 模拟45个框架
    const frameworks = [];
    for (let i = 0; i < 45; i++) {
        frameworks.push(makeFramework(`fw${i}`, `认知框架${i}`, `核心洞见内容${i}：关于组织设计和团队协作的深度洞察。`, i % 6 === 0 ? 'psychology' : i % 6 === 1 ? 'economics' : i % 6 === 2 ? 'math-engineering' : i % 6 === 3 ? 'medicine' : i % 6 === 4 ? 'biology-physics' : 'law-governance'));
    }
    const result = (0, slim_extract_1.extractSlimPersona)(frameworks, 'test-role');
    const reduction = frameworks.length - result.slimMentalModels.length;
    const reductionRate = (reduction / frameworks.length * 100).toFixed(1);
    console.assert(result.slimMentalModels.length <= 5, `TC-03 FAIL: slimMentalModels=${result.slimMentalModels.length} > 5`);
    console.assert(result.suppressedCount >= 40, `TC-03 FAIL: suppressedCount=${result.suppressedCount} ≥ 40 expected (${frameworks.length} total → ${result.slimMentalModels.length} slim)`);
    console.log(`✅ TC-03 PASS: ${frameworks.length}→${result.slimMentalModels.length} (缩减${reductionRate}%, ${result.suppressedCount}个被压制), tokens=${result.totalTokenCount}`);
}
// ================================================================
// 运行所有测试
// ================================================================
console.log('\n=== SlimPersona 单元测试 ===\n');
test_tokenCountWithinLimit();
test_degradationTriggered();
test_reductionRatio();
console.log('\n=== 3/3 测试通过 ✅ ===\n');
//# sourceMappingURL=slim-extract.test.js.map