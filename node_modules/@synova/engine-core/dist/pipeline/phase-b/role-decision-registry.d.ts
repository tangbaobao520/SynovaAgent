/**
 * 角色-决策类型映射表 (Role-to-DecisionType Registry)
 *
 * 修复点2: S1 决策提取的第二通道
 *
 * QA-07 暴露的核心问题: 所有角色只从 L0 constraints 提取决策类型,
 * 当 constraints 太少时(2-3条), 所有角色的心智模型都稀疏且重叠.
 * 此映射表为每个角色注入 3-5 个角色特有的决策类型.
 *
 * @date 2026-05-09 沈括
 */
import type { DecisionType } from './decision-extractor.js';
export type FrameworkCategory = 'psychology' | 'economics' | 'math-engineering' | 'medicine' | 'biology-physics' | 'law-governance';
export declare function extractDecisionsFromRole(roleName: string): DecisionType[];
export declare function getRoleCategoryBias(roleName: string): FrameworkCategory[];
