"use strict";
/**
 * pipeline/phase-b/skill-signal-collector.ts — P3-15 M3 技能信号采集
 *
 * 为什么做：技能安装/调用/废弃信号不回流传给框架层 = 引擎永远不学习
 * 对项目意义：连接"生成团队"和"实际运行"，形成闭环
 * 完成标准：事件可记录、可批处理、权重可观测变化
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.recordSkillEvent = recordSkillEvent;
exports.aggregateAndProcessFeedback = aggregateAndProcessFeedback;
exports.recordSkillInstalled = recordSkillInstalled;
exports.getPendingSignalCount = getPendingSignalCount;
exports.clearSignals = clearSignals;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const os = __importStar(require("os"));
const framework_feedback_1 = require("./framework-feedback");
const framework_library_1 = require("./framework-library");
const logger_1 = require("../../infra/logger");
const log = (0, logger_1.createLogger)('engine-server/pipeline/phase-b/skill-signal-collector');
const SIGNAL_FILE = path.join(os.homedir(), '.claworg', 'harness', 'skill-signals.json');
let _signals = null;
function loadSignals() {
    if (_signals !== null)
        return _signals;
    try {
        if (!fs.existsSync(SIGNAL_FILE)) {
            _signals = [];
            return [];
        }
        _signals = JSON.parse(fs.readFileSync(SIGNAL_FILE, 'utf-8'));
        return _signals;
    }
    catch (err) {
        log.warn('[skill-signal] 信号文件损坏，重置:', err.message);
        _signals = [];
        return [];
    }
}
function saveSignals() {
    try {
        const d = path.dirname(SIGNAL_FILE);
        if (!fs.existsSync(d))
            fs.mkdirSync(d, { recursive: true });
        const t = SIGNAL_FILE + '.tmp';
        fs.writeFileSync(t, JSON.stringify(_signals, null, 2), 'utf-8');
        fs.renameSync(t, SIGNAL_FILE);
    }
    catch (err) {
        log.error('[skill-signal] 信号持久化失败:', err.message);
    }
}
function recordSkillEvent(event) {
    const signals = loadSignals();
    signals.push(event);
    _signals = signals;
    saveSignals();
}
function aggregateAndProcessFeedback() {
    const all = loadSignals();
    const now = new Date().toISOString();
    const bySkill = new Map();
    for (const s of all) {
        const k = `${s.skillName}::${s.sourceFrameworkId}`;
        if (!bySkill.has(k))
            bySkill.set(k, { skillName: s.skillName, sourceFrameworkId: s.sourceFrameworkId, totalInstalls: 0, totalInvocations: 0, totalSuccesses: 0, totalFailures: 0, uniqueTeams: 0, successRate: 0, deprecationRate: 0, lastUsed: s.timestamp });
        const st = bySkill.get(k);
        if (s.eventType === 'installed')
            st.totalInstalls++;
        else if (s.eventType === 'invoked')
            st.totalInvocations += (s.invocationCount || 1);
        else if (s.eventType === 'success')
            st.totalSuccesses++;
        else if (s.eventType === 'failure')
            st.totalFailures++;
        if (s.timestamp > st.lastUsed)
            st.lastUsed = s.timestamp;
    }
    for (const [, st] of bySkill) {
        st.successRate = st.totalInvocations > 0 ? st.totalSuccesses / st.totalInvocations : 0;
        st.uniqueTeams = new Set(all.filter(s => s.skillName === st.skillName).map(s => s.teamId)).size;
        st.deprecationRate = st.totalInstalls > 0 ? all.filter(s => s.skillName === st.skillName && (s.eventType === 'deprecated' || s.eventType === 'uninstalled')).length / st.totalInstalls : 0;
    }
    let ws = now;
    let we = '1970-01-01';
    for (const s of all) {
        if (s.timestamp < ws)
            ws = s.timestamp;
        if (s.timestamp > we)
            we = s.timestamp;
    }
    const batch = { signals: all, windowStart: ws, windowEnd: we, aggregated: Array.from(bySkill.values()) };
    (0, framework_feedback_1.processFeedbackBatch)(batch, framework_library_1.SEED_FRAMEWORKS);
    _signals = [];
    saveSignals();
    log.info(`[skill-signal] 批处理: ${all.length} 信号 → ${bySkill.size} 技能`);
    return batch;
}
function recordSkillInstalled(p) {
    recordSkillEvent({ skillName: p.skillName, sourceFrameworkId: p.sourceFrameworkId, eventType: 'installed', timestamp: new Date().toISOString(), teamId: p.teamId, roleName: p.roleName, engineRecommended: p.engineRecommended });
}
function getPendingSignalCount() { return loadSignals().length; }
function clearSignals() { _signals = []; saveSignals(); }
