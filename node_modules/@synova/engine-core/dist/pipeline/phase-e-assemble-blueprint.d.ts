/**
 * engine-server/pipeline/phase-e-assemble-blueprint.ts — Phase E (L5): 组装最终蓝图
 *
 * 输入：所有前置阶段结果 + TaskDefinitionDTO
 * 输出：FiveFormatsOutput + RiskCoverageEntry[] + DesignRationaleEntry[] + IncubationFrame
 *
 * 这是管道的最后一步，整合所有前置产物，生成策略性输出。
 */
import type { TaskDefinitionDTO, PhaseAResult, PhaseBResult, PhaseCResult, PhaseDResult, PhaseEResult } from '../types';
export declare function runPhaseE(taskDef: TaskDefinitionDTO, phaseA: PhaseAResult, phaseB: PhaseBResult, phaseC: PhaseCResult, phaseD: PhaseDResult, locale: string, abortSignal: AbortSignal): Promise<PhaseEResult>;
