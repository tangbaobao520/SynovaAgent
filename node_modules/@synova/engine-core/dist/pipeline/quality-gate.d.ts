/**
 * quality-gate.ts — S1 推理链质量检查
 *
 * M3 进化引擎 S1 信号层：在 Phase E 完成后、Blueprint 组装前运行。
 * 检查诊断报告 → 团队蓝图之间的推理一致性。
 *
 * 非阻塞：失败只记录信号，不中断 Pipeline 主流程。
 */
import type { DiagnosisReport, BlueprintDTO, InferenceQualityResult, KnowledgeGap, TaskDefinitionDTO } from '../types';
/**
 * 主入口：执行推理链质量检查
 *
 * 纯结构字段对照，不调用额外 LLM。
 * blueprint 在组装前为 null（只做知识缺口检查），
 * 组装后传入完整 BlueprintDTO 做风险覆盖检查。
 */
export declare function runInferenceQualityCheck(taskDef: TaskDefinitionDTO, diagnosisReport?: DiagnosisReport, blueprint?: BlueprintDTO): InferenceQualityResult;
/**
 * 将 S1 知识缺口发布到弹药工厂的 missing/ 目录。
 * 供每日 09:00 弹药采集定时任务优先读取。
 */
export declare function publishKnowledgeGaps(gaps: KnowledgeGap[]): Promise<void>;
/**
 * 将 research/ammo-factory/missing/ 中的知识缺口草稿，
 * 转换为弹药草案（AmmoEntry JSON）发布到 entries/ 目录，
 * 供弹药采集定时任务（每日 09:00）优先读取和加工。
 *
 * 转换后删除对应的 gap 文件，避免重复处理。
 * @returns 本次转换的弹药草案数量
 */
export declare function publishGapsAsAmmoDrafts(): Promise<number>;
