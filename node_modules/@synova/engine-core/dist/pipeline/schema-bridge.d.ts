/**
 * engine-server/pipeline/schema-bridge.ts — Schema v3.1 ↔ engine-server 类型桥接
 *
 * V1.2 第一阶段：双仓库合并的核心桥梁。
 * 将 E:\culture-forge\src\types\schema-v3.1.ts 的类型映射为
 * engine-server/types.ts 的可消费类型，使 evolution/ 等模块可被 engine-server 调用。
 *
 * 设计原则：
 * 1. 最小映射：只映射 evolution/ 实际消费的字段
 * 2. 单向流动：schema-v3.1 → engine-server types，不反向修改
 * 3. 向后兼容：不修改 engine-server/types.ts 现有定义
 * 4. 降级安全：映射失败时回退到默认值，不抛异常
 * 5. 类型内联：因 tsconfig rootDir 限制，schema-v3.1 核心类型内联于此，不跨仓库 import
 *
 * @packageDocumentation
 */
import type { GapDivisionOfLabor, GapInformationFlow, GapAuthorityGovernance, GapTrustIncentive, TeamStructureBlue, CollaborationModeBlue } from '../types';
/** 信源可达性 */
export type SourceAccessibility = 'public_verifiable' | 'semi_visible' | 'invisible';
/** 6缝隙维度标识 */
export type GapDimension = 'division_of_labor' | 'information_flow' | 'authority_governance' | 'trust_incentive' | 'knowledge_sharing' | 'external_interface';
/** 碎片审计状态 */
export type FragmentAuditStatus = 'verified' | 'mixed' | 'unverifiable' | 'removed';
/** 可扩展的模式描述对象 */
export interface PatternModeV3 {
    name: string;
    label: string;
    confidence: 'high' | 'medium' | 'low';
    claim_type: 'observed' | 'designed';
    source_evidence: string[];
    designer_notes?: string;
    applicable_scenarios?: string[];
    composability_tags?: string[];
    evolution_stability?: 'stable' | 'experimental' | 'volatile';
    evolution_history?: Array<{
        timestamp: string;
        event: 'degraded' | 'experiment_created' | 'replaced' | 'confidence_restored';
        old_confidence?: 'high' | 'medium' | 'low';
        new_confidence?: 'high' | 'medium' | 'low';
        experiment_id?: string;
    }>;
    crowd_validation?: {
        validated: boolean;
        method: 'ab_experiment' | 'observational' | 'none';
        total_validations?: number;
        last_validated_at?: string;
    };
}
/** 证据碎片 */
export interface EvidenceFragment {
    evidence_id: string;
    text: string;
    source_url: string;
    source_type: 'Tier-1' | 'Tier-2' | 'Tier-3';
    source_name?: string;
    timestamp: string;
    is_direct_quote: boolean;
    original_text?: string;
    audit_status: FragmentAuditStatus;
    audit_note?: string;
}
/** 缝隙证据包 */
export interface GapEvidence {
    gap_dimension: GapDimension;
    source_accessibility: SourceAccessibility;
    tier_distribution: {
        'Tier-1': number;
        'Tier-2': number;
        'Tier-3': number;
        unqualified: number;
    };
    verified_rate: number;
    fragments: EvidenceFragment[];
    collection_strategy?: string;
}
/**
 * 将 v3.1 旧 power_distribution 模式名映射为 authority_governance.authority 枚举
 */
export declare function mapPowerDistributionToAuthority(modeName: string): GapAuthorityGovernance['authority'];
/**
 * 将 v3.1 旧 trust_model 模式名映射为 trust_incentive 的 initialTrust/updateMechanism
 */
export declare function mapTrustModel(modeName: string): Pick<GapTrustIncentive, 'initialTrust' | 'updateMechanism' | 'degradationTriggers'>;
/**
 * 将 v3.1 旧 conflict_resolution 模式名映射为 authority_governance 的 strategy/deadlockTimeout
 */
export declare function mapConflictResolution(modeName: string): Pick<GapAuthorityGovernance, 'strategy' | 'deadlockTimeoutSeconds'>;
export declare function mapDivisionOfLabor(modeName: string): Pick<GapDivisionOfLabor, 'mode' | 'substitutable'>;
export declare function mapInformationFlow(modeName: string): Pick<GapInformationFlow, 'topology' | 'syncMode'>;
export declare function buildCollaborationModeFromGaps(gaps: Partial<Record<GapDimension, GapEvidence>>, selectedModes?: Partial<Record<string, {
    name: string;
    label: string;
    confidence: string;
}>>): CollaborationModeBlue;
export interface RuntimeCollaborationEvent {
    timestamp: string;
    gapDimension: GapDimension;
    eventType: 'conflict' | 'flow' | 'decision' | 'share';
    roles: {
        from: string;
        to: string;
    };
    data: {
        modeUsed: string;
        outcome: 'resolved' | 'escalated' | 'deadlocked';
        durationMs?: number;
        humanIntervention?: boolean;
    };
}
export interface EvolutionSignal {
    gapDimension: GapDimension;
    currentMode: string;
    signalStrength: number;
    signalType: 'mode_conflict' | 'deadlock_frequent' | 'overridden' | 'satisfaction_drop';
    suggestedAction: 'keep' | 'generate_variant' | 'degrade_confidence' | 'replace_mode';
    sampleSize: number;
    latestEventAt: string;
}
export declare function extractRoleTopology(teamStructure: TeamStructureBlue): {
    roleCount: number;
    governanceRoles: string[];
    executionRoles: string[];
    collaborationGraph: Record<string, string[]>;
};
export interface EvolutionOverride {
    id: string;
    scope: 'mode' | 'genome' | 'template' | 'inference';
    gapDimension?: GapDimension;
    oldValue: string;
    newValue: string;
    confidenceDelta: number;
    experimentId?: string;
    sampleSize: number;
    createdAt: string;
    merged: boolean;
    reviewNotes?: string;
}
export interface EvolutionOverridesFile {
    version: '1.0';
    generatedAt: string;
    overrides: EvolutionOverride[];
    metadata: {
        totalOverrides: number;
        pendingReview: number;
        merged: number;
        lastReviewAt?: string;
    };
}
export declare function createEmptyOverrides(): EvolutionOverridesFile;
