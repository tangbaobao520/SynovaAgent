"use strict";
/**
 * engine-server/pipeline/template-update-checker.ts — 模板版本管理 + 更新检测
 *
 * V1.2 发布配套：为 FE-33 版本更新推送功能提供引擎侧数据。
 *
 * 逻辑：
 * 1. 读取模板库中所有模板的 manifest.json
 * 2. 对比用户已安装模板版本
 * 3. 返回有更新的模板列表
 *
 * @packageDocumentation
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadAllTemplates = loadAllTemplates;
exports.checkTemplateUpdates = checkTemplateUpdates;
exports.getEngineUpdateInfo = getEngineUpdateInfo;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const logger_1 = require("../infra/logger");
const log = (0, logger_1.createLogger)('pipeline/template-update-checker');
// ====================================================================
// 模板库路径
// ====================================================================
const FLAGSHIP_DIRS = (() => {
    const candidates = [
        path.join(process.cwd(), 'src', 'template', 'presets', 'flagship'),
        path.join(__dirname, '..', '..', '..', 'template', 'presets', 'flagship'),
        path.join(__dirname, '..', '..', 'template', 'presets', 'flagship'),
    ];
    for (const c of candidates) {
        if (fs.existsSync(c))
            return [c];
    }
    // 开发模式：返回 src 下的路径
    return [path.join(process.cwd(), 'src', 'template', 'presets', 'flagship')];
})();
// ====================================================================
// 读取模板库
// ====================================================================
function loadTemplateManifest(templateDir) {
    const manifestPath = path.join(templateDir, 'manifest.json');
    try {
        if (fs.existsSync(manifestPath)) {
            const raw = fs.readFileSync(manifestPath, 'utf-8');
            return JSON.parse(raw);
        }
    }
    catch (_e) {
        log.debug('模板 manifest 解析失败: %s', String(_e));
    }
    return null;
}
/**
 * 加载旗舰模板目录下的所有模板
 */
function loadAllTemplates() {
    const templates = new Map();
    for (const baseDir of FLAGSHIP_DIRS) {
        if (!fs.existsSync(baseDir))
            continue;
        const entries = fs.readdirSync(baseDir, { withFileTypes: true });
        for (const entry of entries) {
            if (!entry.isDirectory())
                continue;
            const manifest = loadTemplateManifest(path.join(baseDir, entry.name));
            if (manifest && manifest.id) {
                templates.set(manifest.id, manifest);
            }
        }
    }
    return templates;
}
// ====================================================================
// 更新检测
// ====================================================================
/**
 * 对比已安装模板和最新模板，返回更新信息。
 *
 * @param installedVersions - 用户已安装模板版本映射 { templateId: version }
 * @returns 更新检测结果
 */
function checkTemplateUpdates(installedVersions = {}) {
    const latestTemplates = loadAllTemplates();
    const allTemplates = [];
    const updates = [];
    const newTemplates = [];
    for (const [id, manifest] of latestTemplates) {
        const installed = installedVersions[id] || null;
        const hasUpdate = installed !== null && compareVersions(manifest.version, installed) > 0;
        const isNew = installed === null;
        const info = {
            templateId: id,
            templateName: manifest.name,
            latestVersion: manifest.version,
            installedVersion: installed,
            hasUpdate,
            isNew,
            changelog: hasUpdate
                ? [`从 ${installed} 升级到 ${manifest.version}`, manifest.distillNotes || '模板内容已更新']
                : [],
        };
        allTemplates.push(info);
        if (isNew) {
            newTemplates.push(info);
        }
        else if (hasUpdate) {
            updates.push(info);
        }
    }
    return {
        allTemplates,
        updates,
        newTemplates,
        totalCount: latestTemplates.size,
        checkedAt: new Date().toISOString(),
    };
}
// ====================================================================
// 版本比较
// ====================================================================
/**
 * 比较语义化版本号。
 * @returns > 0 if a > b, < 0 if a < b, 0 if equal
 */
function compareVersions(a, b) {
    const partsA = a.split('.').map(Number);
    const partsB = b.split('.').map(Number);
    const len = Math.max(partsA.length, partsB.length);
    for (let i = 0; i < len; i++) {
        const va = partsA[i] || 0;
        const vb = partsB[i] || 0;
        if (va !== vb)
            return va - vb;
    }
    return 0;
}
// ====================================================================
// 引擎版本
// ====================================================================
/**
 * 获取引擎当前版本（兼容墨翟 /api/update/check API）
 */
function getEngineUpdateInfo() {
    const pkgPath = path.join(process.cwd(), 'package.json');
    let version = '3.1.0';
    try {
        if (fs.existsSync(pkgPath)) {
            const pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf-8'));
            version = pkg.version || version;
        }
    }
    catch (_e) {
        log.debug('package.json 读取失败，使用默认版本: %s', String(_e));
    }
    return {
        currentVersion: version,
        releaseDate: '2026-05-14',
        changelog: [
            'V1.2 发布：运行时治理层激活',
            '新增独立审计 Agent（8维度审查）',
            '新增安全基线 6 条 SB 规则硬阻断',
            '新增 6缝隙协作事件采集',
            '新增 M3 进化闭环（信号→变体→overrides）',
            '新增壁垒四血缘追踪证据链导出',
            '新增弹药库注入（5行业领域事实）',
        ],
    };
}
