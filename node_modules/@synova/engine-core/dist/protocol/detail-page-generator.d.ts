/**
 * 模板详情页生成器 —— 自动产出每个行业模板的详情页
 * 位置: E:\scenario-forge-v2\src\team-protocol\detail-page-generator.ts
 * 沈括 @ 2026-05-06
 *
 * 设计原则:
 *   1. 从 team-protocol.json + 角色文件自动生成，不靠手工写
 *   2. 随每次 G-04 流水线重跑自动更新（版本号绑定）
 *   3. 明确告知边界: 能做什么、不能做什么、什么情况下该换模板
 */
interface DetailPageInput {
    templateName: string;
    industry: string;
    region: string;
    category: string;
    recommendedMode: string;
    modeScores: Record<string, number>;
    roleNames: string[];
    gapCoverage: {
        high: number;
        medium: number;
        low: number;
    };
    consistencyPassed: boolean;
    version: string;
    generatedAt: string;
    warnings: string[];
}
export declare function generateDetailPage(input: DetailPageInput): string;
export {};
