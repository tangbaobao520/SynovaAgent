/**
 * LLM 桥接器 —— 替代手写 domain-bridge
 * 调用 deepseek-v4-pro 推断缝隙策略值
 * 沈括 @ 2026-05-06
 */
export interface GapEvidenceInput {
    gapDimension: string;
    fragments: {
        text: string;
        sourceUrl: string;
    }[];
}
export interface GapInference {
    gap: string;
    mode: string;
    confidence: 'high' | 'medium' | 'low';
    reasoning: string;
    rolesNeeded: string[];
    keywords: string[];
}
/**
 * 构建 LLM prompt
 */
export declare function buildInferencePrompt(gap: string, fragments: GapEvidenceInput['fragments']): string;
/**
 * 解析 LLM 返回的推断结果
 * 容错: 如果 JSON 解析失败，回退到 default mode
 */
export declare function parseInference(gap: string, rawResponse: string): GapInference;
/**
 * 一次 LLM 调用（由 Agent 注入，适配不同 LLM 后端）
 */
export type LLMCallFn = (prompt: string) => Promise<string>;
/**
 * 完整的 LLM 桥接: 8 次调用 → 8 个推断
 */
export declare function llmBridge(evidences: GapEvidenceInput[], llmCall: LLMCallFn): Promise<GapInference[]>;
