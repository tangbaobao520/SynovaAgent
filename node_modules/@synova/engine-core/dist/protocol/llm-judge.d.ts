import type { GapDimension, AgentMessage, TeamProtocol } from './types';
import type { LLMJudgeResponse } from './types';
export declare class LLMJudge {
    private llmCall;
    constructor(llmCall: (systemPrompt: string, userPrompt: string) => Promise<string>);
    /**
     * 裁决一条消息是否违规
     *
     * @param message  嫌疑消息
     * @param gap      规则引擎命中的缝隙
     * @param protocol 团队协议
     * @returns LLM 裁决响应，或 null（超时/解析失败）
     */
    judge(message: AgentMessage, gap: GapDimension, protocol: TeamProtocol): Promise<LLMJudgeResponse | null>;
    /**
     * 提取缝隙值（供 LLM prompt 使用）
     */
    private getGapValueForLLM;
    /** 带超时的 Promise */
    private withTimeout;
}
