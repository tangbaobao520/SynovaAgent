/**
 * storage.ts — 存储抽象层（接口 + 注入 + 内存降级）
 *
 * engine-core 独立运行时使用内存 Map；宿主应用（ClawOrg-BOX）通过 setStorageBackend()
 * 注入 SQLite/harness-store 实现，数据重启不丢失。
 */
import type { Snapshot } from './snapshots/snapshot-manager';
import type { TeamHealthSnapshot, DegradationWarning, OptimizationSuggestion, EvolutionEvent } from './observer/team-observer-types';
import type { MemoryEntry } from './types';
export interface StorageBackend {
    saveSnapshot(snapshot: Snapshot): void;
    loadSnapshots(teamId: string): Snapshot[];
    deleteSnapshot(teamId: string, snapshotId: string): boolean;
    saveHealthSnapshot(snapshot: TeamHealthSnapshot): void;
    loadHealthSnapshots(blueprintId: string): TeamHealthSnapshot[];
    saveWarnings(blueprintId: string, warnings: DegradationWarning[]): void;
    loadWarnings(blueprintId: string): DegradationWarning[];
    saveEvolutionEvent(blueprintId: string, event: EvolutionEvent): void;
    loadEvolutionEvents(blueprintId: string): EvolutionEvent[];
    saveSuggestions(blueprintId: string, suggestions: OptimizationSuggestion[]): void;
    loadSuggestions(blueprintId: string): OptimizationSuggestion[];
    savePendingOutcomes(outcomes: PendingOutcome[]): void;
    loadPendingOutcomes(): PendingOutcome[];
    saveKnowledgeRecord(record: KnowledgeInjectionRecord): void;
    loadKnowledgeRecords(blueprintId: string): KnowledgeInjectionRecord[];
    saveMemoryEntry(entry: MemoryEntry): void;
    loadMemoryEntries(teamId: string, filters?: {
        type?: string;
        authorRoleId?: string;
    }): MemoryEntry[];
    updateMemoryReuseCount(entryId: string): void;
    resetAll(): void;
}
export interface PendingOutcome {
    suggestionId: string;
    suggestionTitle: string;
    eventId: string;
    blueprintId: string;
    beforeScore: number;
    appliedAt: string;
    dimension?: string | null;
}
export declare function setStorageBackend(backend: StorageBackend): void;
export declare function getStorageBackend(): StorageBackend | null;
interface KnowledgeInjectionRecord {
    blueprintId: string;
    timestamp: string;
    totalEntries: number;
    entriesWithImplication: number;
    avgDeviation: number;
    agentCount: number;
    sharedCount: number;
}
export declare function saveSnapshotDB(snapshot: Snapshot): void;
export declare function loadSnapshotsDB(teamId: string): Snapshot[];
export declare function deleteSnapshotDB(teamId: string, snapshotId: string): boolean;
export declare function saveHealthSnapshotDB(snapshot: TeamHealthSnapshot): void;
export declare function loadHealthSnapshotsDB(blueprintId: string): TeamHealthSnapshot[];
export declare function saveWarningsDB(blueprintId: string, warnings: DegradationWarning[]): void;
export declare function loadWarningsDB(blueprintId: string): DegradationWarning[];
export declare function saveEvolutionEventDB(blueprintId: string, event: EvolutionEvent): void;
export declare function loadEvolutionEventsDB(blueprintId: string): EvolutionEvent[];
export declare function saveSuggestionsDB(blueprintId: string, suggestions: OptimizationSuggestion[]): void;
export declare function loadSuggestionsDB(blueprintId: string): OptimizationSuggestion[];
export declare function savePendingOutcomesDB(outcomes: PendingOutcome[]): void;
export declare function loadPendingOutcomesDB(): PendingOutcome[];
export declare function saveKnowledgeRecordDB(record: KnowledgeInjectionRecord): void;
export declare function loadKnowledgeRecordsDB(blueprintId: string): KnowledgeInjectionRecord[];
export declare function saveMemoryEntryDB(entry: MemoryEntry): void;
export declare function loadMemoryEntriesDB(teamId: string, filters?: {
    type?: string;
    authorRoleId?: string;
}): MemoryEntry[];
export declare function updateMemoryReuseCountDB(entryId: string): void;
export declare function resetAllHarnessData(): void;
export {};
