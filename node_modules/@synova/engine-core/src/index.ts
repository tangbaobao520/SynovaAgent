/**
 * @synova/engine-core — Vendor entry point
 *
 * Synova Engine Core provides the domain logic for:
 * - Observer: team health monitoring and gap metrics
 * - Evolution: optimization suggestion engine
 * - Snapshots: configuration snapshots and rollback
 * - Pipeline: collaboration event collection and schema bridges
 * - Protocol: runtime enforcement types
 * - Knowledge-Ingest: knowledge injection pipeline
 */

// ── Observer ──
export * from './observer/team-observer-types';
export {
  collectHealthSnapshot,
  getLatestSnapshot,
  getSnapshotHistory,
  getWarnings,
  getTeamList,
  clearTeamData,
  recordKnowledgeInjection,
  getKnowledgeInjectionHistory,
  getLatestKnowledgeInjection,
} from './observer/team-observer';
export type { KnowledgeInjectionRecord } from './observer/team-observer';
export { welchTTest, wilsonCILower } from './observer/stats-utils';

// ── Evolution ──
export {
  analyzeAndGenerateSuggestions,
  getSuggestions,
  getSuggestion,
  markSuggestionApplied,
  markSuggestionDismissed,
  getEvolutionHistory,
  runEvolutionCycle,
  detectKnowledgeDeviations,
  getOutcomeStats,
  recordEvolutionEvent,
  evaluateOutcomes,
} from './evolution/evolution-engine';
export type { KnowledgeDeviationInput } from './evolution/evolution-engine';

// ── Snapshots ──
export {
  saveSnapshot,
  restoreSnapshot,
  restoreSnapshotSoft,
  restoreSnapshotHard,
  listSnapshots,
  getSnapshot,
  getLatestSnapshot as getProtocolSnapshot,
  deleteSnapshot,
  clearSnapshots,
  generateSnapshotId,
  resetAllSnapshots,
  diffSnapshots,
} from './snapshots/snapshot-manager';
export type {
  Snapshot,
  SnapshotSummary,
  SnapshotDiff,
  ProtoDiffEntry,
  AgentFileDiffEntry,
} from './snapshots/snapshot-manager';

// ── Pipeline observations ──
export {
  recordCollaborationEvent,
  collectEvolutionSignals,
  getAllStats,
  getRecentEvents,
  getDimensionStats,
} from './pipeline/collaboration-collector';
export {
  mapPowerDistributionToAuthority,
  mapTrustModel,
  mapConflictResolution,
  mapDivisionOfLabor,
  mapInformationFlow,
  buildCollaborationModeFromGaps,
} from './pipeline/schema-bridge';
export type {
  RuntimeCollaborationEvent,
  EvolutionSignal,
  GapDimension,
  GapEvidence,
  PatternModeV3,
  EvolutionOverride,
  EvolutionOverridesFile,
} from './pipeline/schema-bridge';

// ── Protocol (runtime enforcement) ──
export { RuleEngine } from './protocol/rule-engine';
export { ProtocolInterceptor } from './protocol/interceptor';
export { RulingCache } from './protocol/cache';
export { CircuitBreaker } from './protocol/circuit-breaker';
export { LLMJudge } from './protocol/llm-judge';
export type {
  AgentMessage,
  CollaborationContext,
  ProtocolInterceptResult,
  ProtocolViolation,
  TeamProtocol,
  ConstraintSeverity,
} from './protocol/types';

// ── Information-Flow (GAP-2) ──
export {
  evaluateRouting,
  computeRelevance,
  getLoadSnapshot,
  updateLoad,
  isOverloaded,
  resetLoad,
  resetAllLoad,
  getAllLoadSnapshots,
  generateAugmentationCard,
} from './information-flow';
export type {
  RoutingResult,
  RouteDecision,
  RouteAction,
  LoadSnapshot,
  AugmentationParams,
  RoutingConfig,
} from './information-flow';

// ── Knowledge-Ingest ──
export { distributeKnowledge } from './knowledge-ingest/mapper';
export type { AgentKnowledgeAssignment, KnowledgeDistributionResult } from './knowledge-ingest/mapper';
export type { KnowledgeAmmoEntry } from './knowledge-ingest/refiner';

// ── Knowledge-Sharing (GAP-4) ──
export {
  addMemory,
  getTeamMemories,
  incrementReuse,
  getMemory,
  upsertMemory,
  createMemoryEntry,
  updateSyncIndex,
  generateSyncMd,
} from './knowledge-sharing';
export type { SyncIndexEntry } from './knowledge-sharing';

// ── Storage (注入接口) ──
export { setStorageBackend, getStorageBackend } from './storage';
export type { StorageBackend, PendingOutcome } from './storage';

// ── Core Types ──
export type {
  MemoryEntry,
  MemoryEntryType,
  MemoryVisibility,
  MemorySource,
  OutputContract,
  GapKnowledgeSharing,
  CollaborationModeBlue,
} from './types';

// ── Engine Context (依赖注入) ──
export {
  getEngineContext,
  setEngineContext,
} from './engine-context';
export type {
  EngineContext,
  SkillSecurityReport,
  SecurityAuditReport,
  MarketplaceService,
} from './engine-context';

// ── LLM Client ──
export { chat } from './llm-client';

// ── Pipeline Config ──
export {
  ENGINE_PORT,
  ENGINE_HOST,
  INPUT_CONSTRAINTS,
  ESTIMATED_GENERATION_SECONDS,
  SYNC_TIMEOUT_MS,
  TASK_TTL_MIN_SECONDS,
  TASK_CLEANUP_INTERVAL_MS,
  MAX_CONCURRENT_TASKS,
  ENGINE_VERSION,
  PIPELINE_VERSION,
  BLUEPRINT_SCHEMA_VERSION,
  AUTH_TOKEN_PREFIX,
  RATE_LIMIT_AUTH,
  RATE_LIMIT_GENERATE,
  RATE_LIMIT_POLL,
  getValidTokens,
} from './pipeline-config';
export { errors, getHttpStatus } from './errors';
export type { ApiErrorResponse, ApiErrorDetail, ErrorCode } from './types';

// ── Pipeline Orchestrator ──
export {
  runPipeline,
  runPipelinePartial,
  buildCoordinatorSeeds,
  incubateCoordinator,
} from './pipeline/orchestrator';
export type {
  PartialPipelineAction,
  ChangeSummary,
  PartialPipelineResult,
  OrgContext,
} from './pipeline/orchestrator';

// ── Pipeline Evolution ──
export { consumeFeedback } from './pipeline/evolution-loop';

// ── Pipeline Template Seeder ──
export { templateToSeeds, templateToTaskDefinition } from './pipeline/template-seeder';
export { convertBlueprintToTeamTemplate } from './pipeline/blueprint-converter';

// ── Phase B Genome ──
export { distillSingleRole, buildFallbackGenomes, mapToPersonaGenomes } from './pipeline/phase-b-distill-genome';

// ── Skill Mapper ──
export { mapSkillsForRole } from './pipeline/skill-mapper';

// ── Framework Library & Matcher ──
export { SEED_FRAMEWORKS } from './pipeline/phase-b/framework-library';
export { matchFrameworksByConstraint } from './pipeline/phase-b/framework-matcher';

// ── Phase A ──
export { prioritizeConstraints } from './pipeline/phase-a-derive-roles';

// ── Framework Feedback ──
export {
  updateWeights,
  loadWeights,
  saveWeights,
  processFeedbackBatch,
  detectSkillGaps,
  shouldTransfer,
  computeBlendedScore,
} from './pipeline/phase-b/framework-feedback';
export type {
  SkillFeedbackSignal,
  FeedbackBatch,
  FrameworkSkillWeights,
  SkillWeight,
  SkillGapReport,
  SkillGap,
  CrossTeamTransferRule,
} from './pipeline/phase-b/framework-feedback';

// ── Skill Signal Collector ──
export {
  recordSkillEvent,
  aggregateAndProcessFeedback,
  recordSkillInstalled,
  getPendingSignalCount,
  clearSignals,
} from './pipeline/phase-b/skill-signal-collector';

// ── Template Update Checker ──
export { checkTemplateUpdates, getEngineUpdateInfo } from './pipeline/template-update-checker';
export type { TemplateUpdateCheckResult } from './pipeline/template-update-checker';

// ── Ammo Depot ──
export { getAmmoDepot } from './pipeline/phase-b/ammo-depot';

// ── Skill Seeds ──
export { ALL_SEED_SKILLS } from './pipeline/skill-seeds';

// ── QA ──
export { evaluateBlueprint, runQASuite, saveReport, DEFAULT_CONFIG } from './qa/qa-runner';

// ── Skill Generation ──
export { generateSkillFromScratch } from './pipeline/skill-generator';
export { writeSkillsToRegistry, publishNewSkillsToCloud } from './pipeline/skill-registry-writer';

// ── Converters ──
export {
  synovaToLangGraph,
  langGraphToJSON,
  langGraphToPython,
} from './pipeline/converters';
export type {
  LangGraphConfig,
  LangGraphNode,
  LangGraphEdge,
  LangGraphTool,
} from './pipeline/converters';

// ── Phase Steward ──
export { buildOrgSteward } from './pipeline/phase-steward';

// ── Config Guardian ──
export {
  MODE_SIZE_THRESHOLDS,
  diffCollaborationMode,
  assessModeSwitchRisk,
  validateAgentEdit,
  checkIronRuleIntegrity,
} from './pipeline/config-guardian';

// ── Dialogue Quality ──
export { generateDialogueQualityBrief, assessIFPrecision } from './pipeline/dialogue-quality';
export type { IFPrecisionResult } from './pipeline/dialogue-quality';

// ── LLM JSON Repair ──
export { tryParseRepaired } from './pipeline/llm-json-repair';

// ── Diagnosis (ARCH-04) ──
export {
  assembleFullDiagnosis,
  assembleFullDiagnosisAsync,
  assembleFullDiagnosisV2,
  assembleFullDiagnosisV2Async,
  assembleFullDiagnosisV3Async,
  buildGapSnapshot,
  recordGapSnapshot,
  getGapTimeline,
  getLatestSnapshot as getLatestGapSnapshot,
  getSnapshotCount,
  clearTeamSnapshots,
  computeDynamics,
  computeAttention,
  recordTopicKeyword,
  recordDecisionType,
  recordInteraction,
  recordAgentConsumption,
  clearTeamLogs,
  extractIdentityMarkers,
  recordIdentitySentence,
  recordIdentitySentences,
  clearIdentityData,
  detectPathDependency,
  computeSelfAwareness,
  recordSelfAssessment,
  getSelfAssessments,
  clearTeamSelfAssessments,
  recordLegacyGapSnapshot,
  GAP_DIMENSIONS,
  bridgeHealthSnapshot,
  bridgeAndRecord,
  computeHACD,
  computeCPC,
  computeIPU,
  computeHONA,
  recordAgentInteraction,
  clearAgentInteractions,
  saveSnapshot as saveDiagnosisSnapshot,
  loadTimeline as loadDiagnosisTimeline,
  loadAllTimelines as loadAllDiagnosisTimelines,
  saveAttentionLog,
  loadAttentionLogs,
  saveIdentityData,
  loadIdentityData,
  saveSelfAssessment as saveDiagnosisSelfAssessment,
  loadSelfAssessments as loadDiagnosisSelfAssessments,
  computeCapabilitySpectrum,
  computeIntentAlignment,
  computeSevenPowers,
  computeFinancialImpact,
  simulateImprovement,
  loadFinancialBaseline,
  saveFinancialBaseline,
  computeTokenEconomics,
  getModelPricing,
  getDefaultModelPriceTable,
  computeHTM,
  computeEOB,
  registerModule,
  listModules,
  getModule,
  runModule,
  runModules,
  generateMultiRoleNarrative,
  generateActionPlan,
  pushActionItems,
  computeBenchmark,
  enrichDiagnosis,
  // SynovaAgent P1a 运行时模块
  DiagnosisOrchestrator,
  MemorySessionTracer,
  PermissionPolicy,
  RecordingPermissionStore,
  createDefaultPermissionPolicy,
  RecoveryExecutor,
  RecoveryContext,
  DiagnosisFailureScenario,
  createDefaultRecipes,
  createDefaultRecoveryExecutor,
  DiagnosisSessionCompactor,
  estimateMessageTokens,
  DEFAULT_SESSION_CONFIG,
  DiagnosisPromptBuilder,
  createScopePromptBuilder,
  createHypothesisPromptBuilder,
  EvidenceManager,
  renderDiagnosisReport,
  renderFullDiagnosisReport,
  renderMarketingSection,
  // P2-01 错误归一化 + P2-02 事件流
  normalizeDiagnosisError,
  RECOVERABLE_CODES,
  DiagnosisEventStream,
  // P2-03 钩子系统 + P2-04 Agent工具注册 + P2-05 子Agent隔离 + P2-06 会话转录
  DiagnosisHookMap,
  SessionTranscriptor,
  registerTool,
  registerTools,
  getTool,
  listTools,
  listToolNames,
  toolCount,
  hasTool,
  executeTool,
  SubAgentIsolator,
  // P1-06 关键人才风险
  analyzeKeyPersonRisk,
  buildDependenciesFromRoles,
  buildKnowledgeDomains,
  // P1b 咨询沟通层
  renderEmpathyMessage,
  renderMultiRoleMessages,
  getRoleLabel,
  getRoleTone,
  adaptDetailLevel,
  detectSensitiveFields,
  scanContentForSensitivity,
  redactField,
  redactObject,
  getBuiltinRules,
  createCustomRule,
  buildIntervieweeProfile,
  listRoleTypes,
  suggestRoleType,
  getDimensionPriority,
  aggregateTeamPriorities,
  queryQuestions,
  countByDimension,
  countByRole,
  addCustomQuestion,
  addCustomQuestions,
  removeCustomQuestion,
  getQuestionCount,
  clearCustomQuestions,
  getSeedQuestions,
  generateQuestionnaire,
  // 营销数据存储
  loadMarketingData,
  saveMarketingData,
  deleteMarketingData,
  // P1b 外部数据层
  computeFinancialSnapshot,
  assessFinancialImpact,
  createEmptyEntry,
  validateEntry,
  queryProducts,
  getProduct,
  isVersionDeprecated,
  isVersionOutdated,
  getCategories,
  fuzzySearch,
  analyzeAIInventory,
  // P1b 知识+改造层
  archiveDiagnosis,
  getArchive,
  queryArchives,
  getTeamHistory,
  getLatestDiagnosis,
  cleanupArchive,
  resetArchive,
  extractKnowledge,
  addKnowledge,
  upsertKnowledge,
  getKnowledge,
  queryKnowledge,
  citeKnowledge,
  deleteKnowledge,
  getKnowledgeStats,
  resetKnowledge,
  extractFromDiagnosis,
} from './pipeline/diagnosis';
export type {
  FullDiagnosis,
  FullDiagnosisV2,
  FullDiagnosisV3,
  GapSnapshot,
  GapDimensionScore,
  GapDynamics,
  PhaseCoupling,
  StickyDimension,
  AttentionAllocation,
  IdentityMarkers,
  PathDependency,
  SelfAwarenessReport,
  SelfAwarenessDelta,
  BlindSpotDeclaration,
  SelfAssessmentInput,
  SelfAssessmentRecord,
  HACDReport,
  CPCReport,
  CPCDimensionDetail,
  CPCGap,
  IPUReport,
  HONAReport,
  HONANode,
  HONAEdge,
  CapabilitySpectrum,
  CapabilityDim,
  IntentAlignment,
  SevenPowersReport,
  PowerAssessment,
  FinancialBaseline,
  ModelPricing,
  FinancialImpactReport,
  CostBreakdown,
  TokenEconomicsReport,
  TokenSourceBreakdown,
  TokenWasteBreakdown,
  HTMReport,
  TrustCurve,
  TrustDecayEvent,
  SinglePointRisk,
  EOBReport,
  DiagnosticModule,
  DiagnosticPriority,
  ConfidenceModel,
  DiagnosticDataSourceRequirements,
  ModuleRunResult,
  MultiRoleNarrative,
  ImprovementActionItem,
  ActionPlan,
  TaskIntegrationResult,
  AssemblyOptions,
  FDEDiagnosisExtensions,
  BenchmarkReport,
  DimensionBenchmark,
  EnrichedData,
  LocalGitMetrics,
  SoftwareEnrichment,
  GitHubMetrics,
  EnricherPlugin,
  // SynovaAgent P1a 运行时类型
  InitiatorProfile,
  DiagnosisScope,
  DiagnosisEvidence,
  ModuleFinding,
  EvidenceFilter,
  ContradictionSignal,
  DiagnosisHypothesis,
  CausalNode,
  CausalEdge,
  CausalChain,
  RootCause,
  RootCauseTree,
  StructuredDiagnosisReport,
  DeliveryResult,
  ConsultationResult,
  AgentIterationState,
  DiagnosisErrorCode,
  NormalizedDiagnosisError,
  DiagnosisEvent,
  DiagnosisPermissionLevel,
  PermissionContext,
  PermissionResult,
  DiagnosisSessionMessage,
  CompactResult,
} from './pipeline/diagnosis';
export type {
  DiagnosisLLMClient,
  ToolExecutor,
  ToolResult,
  LLMResponse,
  SessionTracer,
  DiagnosisEventWriter,
  OrchestratorConfig,
} from './pipeline/diagnosis';
export type {
  PermissionRule,
  PermissionRequest,
} from './pipeline/diagnosis';
export type {
  RecoveryRecipe,
  RecoveryStep,
  RecoveryResult,
} from './pipeline/diagnosis';
export type {
  DiagnosisSessionConfig,
} from './pipeline/diagnosis';
export type {
  PromptSection,
  PhaseContext,
  AgentRoleDefinition,
  SkillCard,
} from './pipeline/diagnosis';
export type {
  HookFn,
  HookKind,
  HookContext,
  BeforePhaseContext,
  AfterModuleContext,
  BeforeReportContext,
  BeforeToolCallContext,
} from './pipeline/diagnosis';
export type { AgentTool, AgentToolContext } from './pipeline/diagnosis';
export type {
  SubAgentType,
  SubAgentResult,
  SubAgentContext,
  SubAgentRunOptions,
} from './pipeline/diagnosis';
export type {
  RoleDependency,
  KnowledgeDomain,
  RoleRiskProfile,
  KeyPersonRiskReport,
  // P1b 咨询沟通层
  EmpathyScenario,
  RecipientRole,
  RiskSeverity,
  EmpathyTemplateParams,
  EmpathyMessage,
  SensitivityCategory,
  RedactAction,
  SensitivityMatch,
  SensitivityRule,
  RedactedField,
  RedactionAuditEntry,
  IntervieweeRoleType,
  DecisionAuthority,
  CommunicationPreference,
  IntervieweeProfile,
  ProfileBuildInput,
  QuestionType,
  DiagnosticQuestion,
  QuestionChoice,
  QuestionFilter,
  TargetRole,
  TargetDimension,
  // P1b 外部数据层
  FinancialEntry,
  FinancialSnapshot,
  FinancialDiagnosisImpact,
  AIProduct,
  AIProductCategory,
  ProductQuery,
  TeamAIToolEntry,
  AIInventoryAnalysis,
  // P1b 知识+改造层
  ArchiveEntry,
  ArchiveFilter,
  ExtractedKnowledge,
  KnowledgeType,
  OrgKnowledgeEntry,
  KnowledgeQuery,
  KnowledgeStats,
  // 营销模块
  MarketingSectionInput,
  MarketingDataRecord,
  MarketingDataInput,
} from './pipeline/diagnosis';

// ── FDE Toolset (Agent 工具注册 — ARCH-08) ──
export {
  FDE_TOOLS,
  getFdeTool,
  createFdeToolExecutor,
  listFdeToolDescriptions,
} from './pipeline/diagnosis';
export type { FdeToolDefinition } from './pipeline/diagnosis';

// ── LLM Client extra ──
export { getCircuitBreakerStatus } from './llm-client';

// ── Ontology (ARCH-20) ──
export { createGraphStore, SQLiteGraphStore } from './pipeline/diagnosis/graph-store';
export type { GraphStore } from './pipeline/diagnosis/graph-store';
export { ingestDocument } from './pipeline/diagnosis/ontology-adapter';
export { shortestPath, degreeCentrality, detectCommunities } from './pipeline/diagnosis/graph-query';
export { searchAcrossLineage } from './pipeline/diagnosis/graph-search-datahub';
export { EntityRegistry, MergedEntityRegistry, LineageRegistry } from './pipeline/diagnosis/entity-registry-datahub';
export type { EntitySpec, AspectSpec, RelationshipAnnotation, LineageSpec, EdgeInfo } from './pipeline/diagnosis/entity-registry-datahub';
export { FeishuAirbyteAdapter, GitAirbyteAdapter } from './pipeline/diagnosis/airbyte-ontology-adapter';
export type { AirbyteOntologyAdapter, ConnectorSpecification, AirbyteConnectionStatus } from './pipeline/diagnosis/airbyte-ontology-adapter';
export { NatsOntologyPublisher, NatsGraphStoreConsumer, getPipelineMetrics, pipelineHealth, getDeadLetterQueue } from './pipeline/diagnosis/nats-ontology-pipeline';
export type { PipelineMetrics, PipelineHealth } from './pipeline/diagnosis/nats-ontology-pipeline';

// ── Graph Monitor (ARCH-23-B3) ──
export { runMonitorTick, monitorEdgeWeight, detectCentralityShift } from './pipeline/diagnosis/graph-monitor';
export type { GraphAlert, MonitorConfig } from './pipeline/diagnosis/graph-monitor';

// ── Decision Engine (ARCH-23-B4) ──
export { runDecisionEngine, getDecisionActions, resolveAction, dismissAction, clearDecisionEngine } from './pipeline/diagnosis/decision-engine';
export type { DecisionRule, DecisionAction } from './pipeline/diagnosis/decision-engine';

// ── Ontology Versioning (ARCH-23-A6) ──
export { createSnapshot, diffSnapshots, getNodeHistory, rollbackToSnapshot, clearVersioning } from './pipeline/diagnosis/ontology-versioning';
export type { OntologySnapshot, OntologyDiff } from './pipeline/diagnosis/ontology-versioning';

// ── Rule Registry (Phase 2.1: 统一规则注册中心) ──
export { RuleRegistry, snapshotRule } from './pipeline/diagnosis/rule-registry';
export type { DiagnosticRule, RuleEvolutionMeta, RuleFilter } from './pipeline/diagnosis/rule-registry';

// ── Feedback Persistence (Phase 1: 闭环反馈) ──
export { FeedbackStore } from './pipeline/diagnosis/feedback-persistence';

// ── Ontology Errors (ARCH-23 Phase 2) ──
export {
  GraphStoreError,
  IngestionError,
  EntityNotFoundError,
  QueryTimeoutError,
  VersionConflictError,
  isRetryableOntologyError,
} from './pipeline/diagnosis/ontology-errors';

// ── Simulation Engine (ARCH-23 Phase C) ──
export { runSimulation, newHireScenario, restructureScenario } from './pipeline/diagnosis/simulation-engine';
export type { SimulationScenario, SimulationTransform, SimulationResult } from './pipeline/diagnosis/simulation-engine';

// ── Personal Publisher (SoloHub) ──
export {
  publishToPersonal,
  listPublishedAgents,
  getPublishedAgent,
} from './pipeline/personal-publisher';
export type {
  PublishResult,
  PublishOptions,
  ManifestEntry,
  SoloHubManifest,
} from './pipeline/personal-publisher';

// ── Re-export all core types ──
export * from './types';
