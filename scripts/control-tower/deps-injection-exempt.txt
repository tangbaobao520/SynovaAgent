# deps-injection-exempt.txt — D925 deps 注入标准 显式豁免清单
#
# 格式（**每行必带理由**）:
#   <仓库相对路径> — <理由>
#
# 硬约束（CTO 裁定 5：降级开关只允许显式，仅两条缝 —— ①测试注入缝 ②本文件）:
#   · **无理由的行不生效**（缺理由 = 该行被忽略，绝不"全豁免"）
#   · **文件缺失 / 格式坏 → 0 条豁免**（fail-closed：报得更多，不是放行更多）
#   · **禁静默 bypass**；**禁环境变量一键全豁免**（本检查器不提供此类开关）
#
# 与 baseline 的区别:
#   baseline = 承认的历史存量，随标准推行应**缩短至空**（改造完成即删行）
#   exempt   = 语义上**本就不适用**的例外（例：某入口函数的"协作者"是进程级不可替换资源）
#
# 当前 0 条 —— 截至 2026-09-23：尚无需要"理由豁免"的文件（2 个存量走 baseline）。
# 添加时请连同**为何本就不适用**写清，供 K3 与后人复核。
