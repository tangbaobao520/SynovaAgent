# subprocess-protocol-baseline.txt — D924 子进程输出协议 存量清单（ratchet：只可缩短）
#
# 格式: <仓库相对路径>:<规则>     规则 ∈ {R1, R2}
#   R1 = 哨兵混流（curl 写出口模板含自定义哨兵，负载未分流）
#   R2 = 哨兵分割解析（按哨兵做字符串分割还原）
#
# 语义: 列在此处的命中**允许存在**（历史存量）；不在清单内的命中 = 新增违规 → exit 1。
#       本文件只可缩短，不可增长（增长 = 开口子，须 CTO 裁定）。
#
# 存量: 0 —— 截至 2026-09-23 测量（D924 已顺带最小修复 assert.ts:101/:109，全仓可执行源码面清零）
# 命令: bash scripts/control-tower/check-subprocess-protocol.sh --dir .
#       → SUBPROCESS-PROTOCOL: OK (files=N, violations=0, exempted=E, baseline=0)
#
# 空清单是刻意的：保留 ratchet 机制（新增即拦），而非取消该机制。
