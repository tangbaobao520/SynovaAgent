{
  "scenario_id": "GS-07",
  "evidence_map": [
    { "acceptance_point": "S7-1", "assertion_ids": ["pii-scrubbed", "rbac-denied"] },
    { "acceptance_point": "S7-2", "assertion_ids": ["pii-boundary-s1"] }
  ],
  "assertions": [
    {
      "id": "pii-scrubbed",
      "desc": "scrub(S2) 脱敏：姓名→[姓名]，手机号→[手机号]（敏感数据脱敏）",
      "purpose": "证明 PII 脱敏引擎可用：S2 级人名/联系方式脱敏（S7-1；敏感数据 → 脱敏）",
      "check": { "type": "file", "path": "__DATA_DIR__/pii-s2.json" },
      "expect": { "exists": true, "contains": "[姓名]" }
    },
    {
      "id": "rbac-denied",
      "desc": "staff 访问 private workspace → HTTP 403（越权拒绝）",
      "purpose": "证明 RBAC 越权拒绝 fail-closed（S7-1；越权访问被拒）",
      "check": { "type": "file", "path": "__DATA_DIR__/rbac-status.txt" },
      "expect": { "exists": true, "contains": "403" }
    },
    {
      "id": "pii-boundary-s1",
      "desc": "scrub(S1) 姓名保持原文（敏感度级别正确）",
      "purpose": "证明脱敏级别边界正确：S1 不脱敏内部级字段（S7-2；级别语义）",
      "check": { "type": "file", "path": "__DATA_DIR__/pii-s1.json" },
      "expect": { "exists": true, "contains": "张三" }
    }
  ]
}
