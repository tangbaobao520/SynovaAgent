/**
 * extensions/types.ts — re-export from @synova/extension-registry
 */
export type {
  ExtensionManifest,
  ExtensionType,
  ExtensionState,
  ExtensionLifecycleEvent,
  ResolvedExtension,
} from '@synova/extension-registry';
