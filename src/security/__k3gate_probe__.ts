// D923 两态 CI 实证 · 临时探针（态①）
//
// 存在理由：本文件位于三类路径 **A_security**（policy glob `src/security/**`），
//   用于让 CI 的**独立 job `K3 Merge Gate`** 在真实流水线上判红（三类命中 + 卡内无 K3 结论 → 「排队中」）。
//
// 生命周期：**态③ 收尾时 `git rm` 本文件与其配对测试**，不留在交付内（brief Q2 已声明为临时件）。
// 本文件**无任何实现体**（仅 `export {}` 使其成为可被测试导入的模块）。
export {};
