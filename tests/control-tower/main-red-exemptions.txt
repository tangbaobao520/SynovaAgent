# main 新增红豁免登记 — D653 合入即绿门禁
# 语义: 豁免 = main 上的新增红**已有归属**（环境依赖登记 / 带任务号在途），放行不阻断。
#       匿名红（无豁免登记）→ check-main-green.sh 阻断 + 写 CTO 待办（MAIN-RED-NEW.md）。
# 格式: <测试文件路径> # <任务号/理由>（每行一条, # 后为归属说明必填）
# 纪律: 豁免必须带任务号或显式环境依赖说明; 修复后移除豁免行（只减不增）。
# 样例（勿删注释行）: tests/foo.test.ts # D999 Windows 本地无 GPU, CI 环境依赖
